# Repository working agreement

- Local Git commits are authorized. Use the repository's configured Git identity.
- Do not add `Co-authored-by`, `Signed-off-by`, or any Codex/AI attribution trailer to commit messages unless the user explicitly asks.
- Never push to a remote, create a pull request, or use another GitHub write action unless the user explicitly asks in the current conversation.
