---
phase: 27-dashboard-list-view
plan: 08
type: execute
wave: 6
depends_on: [27-06]
files_modified:
  - src/utils/dates.ts
  - src/utils/dates.test.ts
  - src/components/list-row-content.ts
  - src/components/list-row-content.test.ts
autonomous: true
gap_closure: true
requirements: [LISTV-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Out-of-range numeric local timestamp components fail parsing rather than being normalized by JavaScript Date."
    - "Malformed snooze values fail closed and a malformed non-null last_contact produces neutral List recency text instead of a render crash."
  artifacts:
    - path: src/utils/dates.ts
      provides: "Strict local wall-clock parser used by snooze and List recency logic."
    - path: src/components/list-row-content.ts
      provides: "Fail-safe neutral recency rendering for corrupt persisted values."
    - path: src/utils/dates.test.ts
      provides: "Numeric malformed date/time and snooze fail-closed regression coverage."
    - path: src/components/list-row-content.test.ts
      provides: "Malformed last_contact rendering fallback coverage."
  key_links:
    - from: "SQLite timestamp text"
      to: "parseLocalMs → isSnoozed and formatListRecency"
      via: "component-range and calendar round-trip validation before Date-derived presentation"
      pattern: "invalid timestamp is rejected; snooze is false and List recency is neutral"
---

<objective>
Close LISTV-05's malformed-timestamp gap by making local parsing strict and List recency presentation fail-safe.

Purpose: Persisted text that SQLite treats as unusable must not activate snooze or crash a List row when JavaScript would otherwise normalize its components.
Output: Strict parsing with numeric-invalid tests, fail-closed snooze behavior, and a neutral recency fallback.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dashboard-list-view/27-VERIFICATION.md
@.planning/phases/27-dashboard-list-view/27-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject normalized numeric-invalid local timestamps before snooze evaluation</name>
  <files>src/utils/dates.ts, src/utils/dates.test.ts</files>
  <read_first>
    - src/utils/dates.ts:20-65 (lexical-only `parseLocalMs` and the `isSnoozed` fail-closed caller)
    - src/utils/dates.test.ts (existing local-calendar and malformed-text coverage)
    - src/db/dashboard-read.ts (stored `last_contact` and `snooze_until` values flow to List presentation)
    - .planning/phases/27-dashboard-list-view/27-VERIFICATION.md (directly observed `99:00:00` false-positive and required missing behavior)
    - .planning/phases/27-dashboard-list-view/27-01-PLAN.md (LISTV-05 requires a SQL-equivalent fail-closed presentation contract)
  </read_first>
  <behavior>
    - `parseLocalMs` rejects numeric-invalid month, hour, minute, and second values rather than letting `Date` normalize them.
    - It rejects calendar rollover dates such as a non-leap-year February 29 after construction/round-trip validation, while accepting a real leap day and valid local timestamp forms already supported by the parser.
    - `isSnoozed` returns false for every rejected value, including `2026-09-05 99:00:00`, out-of-range month, and out-of-range minute/second, and remains true only for a valid date strictly after local today.
  </behavior>
  <action>Per D-07, write the numeric-malformed tests first, then strengthen `parseLocalMs` so validation occurs before consumers use the resulting milliseconds. Retain the accepted Orbit local date and timestamp grammar, but validate numeric component ranges and verify the constructed local `Date` round-trips its intended calendar components; throw on invalid values rather than accepting JavaScript rollover normalization. Keep `isSnoozed` as the defensive boundary that catches parser failure and returns false, matching the repository's SQLite `date(...)` null-comparison behavior for invalid month/time input. Preserve `formatLocalDate` and local-calendar/DST semantics; do not add a UTC conversion or a permissive fallback parser.</action>
  <verify>
    <automated>npx vitest run src/utils/dates.test.ts</automated>
  </verify>
  <done>Invalid numeric local dates/times cannot become valid through JavaScript normalization, and snooze evaluation is demonstrably false for the verifier's `99:00:00` case plus out-of-range month/time cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render a neutral List recency fallback for corrupt non-null last-contact text</name>
  <files>src/components/list-row-content.ts, src/components/list-row-content.test.ts</files>
  <read_first>
    - src/components/list-row-content.ts:1-30 (`formatListRecency` currently calls `parseLocalMs` without a recovery path)
    - src/components/list-row-content.test.ts (recency copy contract and pure-node test pattern)
    - src/components/ListRow.tsx:100-126 (the renderer consumes `formatListRecency`; it needs a string, not a thrown exception)
    - src/utils/dates.ts and src/utils/dates.test.ts (Task 1's exact strict-parser behavior)
    - .planning/phases/27-dashboard-list-view/27-VERIFICATION.md (List recency must not crash for malformed stored `last_contact`)
  </read_first>
  <behavior>
    - A null `last_contact` continues to render `No interactions yet`.
    - A malformed non-null numeric timestamp renders the same neutral safe recency copy and never throws.
    - Valid today, yesterday, and day-age recency strings retain their current local-calendar output.
  </behavior>
  <action>Make `formatListRecency` an explicit presentation safety boundary around Task 1's strict parser: for any malformed non-null persisted `last_contact`, return the established neutral `No interactions yet` copy rather than throwing through `ListRow`. Add pure render-content tests covering an out-of-range time and an invalid calendar date, plus the existing valid cases, so List row rendering cannot regress to an exception. This is a display fallback only: do not rewrite database values, fabricate a status, change active-population SQL, or weaken `parseLocalMs` to support the renderer.</action>
  <verify>
    <automated>npx vitest run src/utils/dates.test.ts src/components/list-row-content.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>Every malformed persisted timestamp is safe at both consumers: snooze is inactive and List recency remains neutral and renderable, while valid local-calendar copy is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite timestamp text → date utility | Persisted values may be corrupt or out of range before entering JavaScript date construction. |
| date utility → List rendering | A thrown parser error can otherwise propagate into a visible row render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08-01 | Tampering | stored timestamp parsing | medium | mitigate | Validate ranges and local calendar round-trip before accepting a timestamp; test invalid numeric components. |
| T-27-08-02 | Denial of service | ListRow recency render | medium | mitigate | Catch parser failure at the display boundary and return neutral text, with regression tests. |
| T-27-08-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Run the two focused test files, TypeScript, and the colour-token gate. Tests must include out-of-range month and time cases, the verifier's `99:00:00` input, and a List recency fallback assertion.
</verification>

<success_criteria>
Numeric-invalid dates/times fail closed before Date normalization; invalid snooze text is never active; malformed non-null `last_contact` cannot crash the List and renders neutral recency text.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-08-SUMMARY.md` when done.
</output>
