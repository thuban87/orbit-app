---
phase: 27-dashboard-list-view
plan: 03
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/db/dashboard-knowledge-read.ts
  - src/db/dashboard-knowledge-read.test.ts
  - src/logic/list-row-selection.ts
  - src/logic/list-row-selection.test.ts
  - src/components/icons/icon-registry.ts
autonomous: true
requirements: [LISTV-03, LISTV-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LISTV-03: `dashboard-knowledge-read.ts` returns a BOUNDED candidate set per contact via a SINGLE batch `contact_id IN (...)` read per source (never per-row queries in renderItem). Exclusions are PER-SOURCE, not universal (review LOW #3): memory candidates are filtered through the EXISTING registry-default-aware choke point `resolveVisibility(type, hidden) === \"show\"` (memories-read.ts:101) — NOT a raw SQL `hidden = 1` predicate (review cycle-3 MED #4, matching Plan 06's corpus discipline) — plus `deleted_at IS NOT NULL`, `outdated = 1`, and birthday-type exclusions; relationship candidates are filtered through `resolveRelationshipVisibility(hidden) === \"show\"` (relationships-read.ts:39) plus `deleted_at IS NOT NULL`; `current_state_entries` has NONE of `hidden`/`outdated`/`deleted_at` columns (migration 016:47-57) — its ONLY predicate is `is_current = 1` (below). The memory read SELECTs `type`+`hidden` and the relationship read SELECTs `hidden` so the choke points can run; a safe `hidden = 1` fast-path may stay inside the SQL `ROW_NUMBER` partition (a strict SUBSET of the choke point — it never removes a row the choke point would show), with `resolveVisibility`/`resolveRelationshipVisibility` as the AUTHORITATIVE registry-default-aware filter in TS. RANKED_FUEL_EXCLUSIONS apply only if any fuel text is reused"
    - "LISTV-03 (cycle-1 HIGH #2): current-state candidates from `current_state_entries` MUST filter `is_current = 1` — that table retains history, and the repo's canonical current-value reads require the predicate (`src/db/current-state-history-read.ts:31` and `:48`). `outdated`/`deleted_at` do NOT substitute (those columns don't exist on this table): a superseded (`is_current = 0`) row would otherwise let a stale former value win line-3"
    - "LISTV-03 (cycle-2 HIGH #2 — per-CONTACT bound, NOT per-query): a plain per-source `LIMIT N` over `contact_id IN (...)` caps the WHOLE batch result, so contacts later in SQL order receive ZERO candidates and wrongly render a completeness prompt despite having knowledge. The bound MUST be PER CONTACT — a `ROW_NUMBER() OVER (PARTITION BY contact_id ORDER BY <deterministic key>)` subquery filtered to a BOUNDED per-contact window (`row_number <= candidateBudget + CANDIDATE_OVER_FETCH`; see the cycle-4 MED over-fetch/re-cap truth below), with a DETERMINISTIC intra-partition `ORDER BY`. A contact with deep history must NEVER starve another contact of its candidates"
    - "LISTV-03 (cycle-4 MED — visibility-after-window starvation): because `resolveVisibility`/`resolveRelationshipVisibility` run in TS AFTER the SQL `ROW_NUMBER()` window, a `hidden = null` row of a FUTURE `visibilityDefault: \"hide\"` memory type would pass the SQL safe `hidden = 1` fast-path, CONSUME a contact's per-contact budget, then be dropped by the TS choke point — starving a later visible row and yielding a FALSE completeness prompt. Latent today (all shipped memory types AND `RELATIONSHIPS_GROUP` default `\"show\"` — memory-registry.ts:36/46/56/110) but silent + invisible (no test failure). FIX (robust option): the SQL per-contact window OVER-FETCHES beyond `candidateBudget` (a headroom cap `candidateBudget + CANDIDATE_OVER_FETCH` — still a bounded per-contact `ROW_NUMBER()` window, so cycle-2 HIGH #2 stands), then the AUTHORITATIVE TS visibility choke points filter, then a PER-CONTACT re-cap to `candidateBudget` is applied in TS — so budget-consuming invisible rows within the headroom never starve visible candidates. `candidateBudget` and `CANDIDATE_OVER_FETCH` are tunable constants at the top of the file (CLAUDE.md)"
    - "LISTV-03 (review MED #2 — stable-id ties): every candidate carries a stable `id` and `createdAt`, so the pure selector's tie-breaks are deterministic over stable identity and never depend on accidental SQL/input row ordering"
    - "LISTV-03: `list-row-selection.ts` selects the line-3 item DETERMINISTICALLY by priority — imminent/time-sensitive (meaningful_date near window, birthdays EXCLUDED) → pinned/high-value (memories.pinned / relationships.pinned) → other useful (most-recent non-hidden/non-outdated/non-deleted) → a stable per-contact completeness prompt; no AI-generated copy"
    - "LISTV-03: when no useful context exists, the completeness prompt is chosen by a stable per-contact hash (`contactId % PROMPTS.length`) so it never changes on re-render; PROMPTS strings are the UI-SPEC Copywriting Contract canon (reused verbatim)"
    - "LISTV-04: the semantic `favorite` icon-registry pair resolves to `star-outline` (outline) / `star` (filled) — a single-source registry change; no literal glyph, no forked icon source (D-05)"
  artifacts:
    - "src/db/dashboard-knowledge-read.ts — readLine3Candidates(exec, contactIds) batch read (contact_id IN), PER-CONTACT bounded via ROW_NUMBER() PARTITION BY contact_id (cycle-2 HIGH #2) that OVER-FETCHES to `candidateBudget + CANDIDATE_OVER_FETCH` then TS re-caps to `candidateBudget` AFTER the visibility choke points so a null-hidden hide-default row can't starve a visible candidate (cycle-4 MED), per-source exclusions (LOW #3) routing memory visibility through resolveVisibility(type,hidden) and relationship visibility through resolveRelationshipVisibility(hidden) (cycle-3 MED #4 — registry-default-aware choke points, not raw hidden=1), candidate shape carries stable id + createdAt (MED #2)"
    - "src/logic/list-row-selection.ts — selectLine3() deterministic selector + PROMPTS + stable per-contact prompt hash (pure, node-tested)"
    - "src/components/icons/icon-registry.ts — favorite: { outline: 'star-outline', filled: 'star' }"
  key_links:
    - "dashboard-knowledge-read.ts candidates → list-row-selection.ts selectLine3 → (27-04) ListRow line 3 — the deterministic data+logic path feeding line 3"
    - "icon-registry.favorite → Icon(name='favorite') consumed by (27-04) ListRow star — single icon source, ADR-075 binary membership"
  prohibitions:
    - "no AI-generated adaptive row copy"
    - "no profile-completeness scoring or gamification (the prompt is a gentle cue, not a score)"
    - "birthdays are excluded from the adaptive line (they have dedicated representation elsewhere)"
    - "no forked second icon source; the favorite pair is the single registry source"
    - "no per-row DB reads inside renderItem — line-3 candidates are batched"
---

<objective>
Build the deterministic line-3 data + selection primitives and the star icon primitive that the ListRow content plan (27-04) consumes: a batch knowledge read keyed by `contact_id IN (...)`, a pure deterministic line-3 selector with stable-per-contact completeness prompts, and the single-source `favorite` registry change from heart to star (owner-decided).

Purpose: LISTV-03 requires a deterministic adaptive context line drawn from existing contact knowledge (no AI, birthdays excluded, stable per contact); LISTV-04 requires the always-visible star to resolve through the shared registry. Both are independent files that unblock 27-04 without touching ListRow/HomeScreen.
Output: `dashboard-knowledge-read.ts`, `list-row-selection.ts`, and the `favorite` registry star change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-RESEARCH.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/db/knowledge-search-read.ts
@src/db/dashboard-read.ts
@src/components/contact-card-ring.ts
@src/components/icons/icon-registry.ts
@src/db/migrations/016-contact-knowledge.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Star the favourite — single-source registry change</name>
  <files>src/components/icons/icon-registry.ts</files>
  <read_first>
    - src/components/icons/icon-registry.ts (the favorite pair at :37 currently outline heart-outline / filled heart; IconName/IconTone types)
    - src/components/icons/Icon.tsx:43-44 (tsc-validates Ionicons glyph names — star/star-outline are in the Ionicons glyphmap)
    - .planning/phases/27-dashboard-list-view/27-PATTERNS.md (icon-registry section — grep confirms NO `<Icon name="favorite">` consumer anywhere; only the registry line + its existence test; the literal "★" in ContactCard.tsx:189 is Card View / Phase 28, untouched here)
  </read_first>
  <acceptance_criteria>
    - The `favorite` entry in `src/components/icons/icon-registry.ts` maps outline to `star-outline` and filled to `star`.
    - `npx tsc --noEmit` passes (Icon.tsx validates the Ionicons glyph names at compile time).
    - The existing icon-registry existence test remains green: `npx vitest run src/components/icons/icon-registry.test.ts` (or the matching test file present in the repo).
    - `npm run check:colors` clean (no colour change).
  </acceptance_criteria>
  <action>
    Change the single `favorite` registry pair so outline resolves to `star-outline` and filled resolves to `star`. Do not add a second favourite icon entry, do not introduce a literal glyph string, and do not touch `ContactCard.tsx` (its literal star is Phase 28's concern). This is a Theme-owned single-source edit. FORWARD NOTE (review LOW #9): this is a GLOBAL single-source registry change — the semantic `favorite` name now resolves to a star, not a heart, for EVERY future consumer. Phase 28 (Card View, not yet planned) will inherit the star; record this so a later `Icon name="favorite"` consumer is not surprised.
  </action>
  <verify>
    <automated>npx vitest run src/components/icons/icon-registry.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The registry `favorite` pair resolves to star-outline/star; tsc + the registry test pass; no consumer breaks.</done>
  <reversibility rating="reversible">One-line registry edit; trivially revertable.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batch line-3 knowledge read (contact_id IN)</name>
  <files>src/db/dashboard-knowledge-read.ts, src/db/dashboard-knowledge-read.test.ts</files>
  <read_first>
    - src/db/knowledge-search-read.ts (placeholders() at :86; batch `contact_id IN (${placeholders(ids)})` at :105/:113/:137 — the exact batch idiom to reuse)
    - src/db/dashboard-read.ts:43-51 (injection posture: static strings, closed-constant fragments, `?`-bound runtime values; RANKED_FUEL_EXCLUSIONS at :45-50)
    - src/db/migrations/016-contact-knowledge.ts:10-57 (memories columns id/type/value/note/url/meaningful_date/pinned/outdated/hidden/created_at/deleted_at; relationships columns id/person_name/relation_type/note/pinned/hidden/created_at/deleted_at; current_state_entries columns id/field_key/value/is_current/created_at — VERIFY on disk: it has NO hidden/outdated/deleted_at, so its only visibility predicate is `is_current = 1`, review LOW #3. Note `id` + `created_at` exist on all three — surface them as the candidate's stable `id`/`createdAt`, review MED #2)
    - src/db/current-state-history-read.ts:20-58 (VERIFY on disk: `current_state_entries` retains history; the canonical current-value reads filter `is_current = 1` at :31 and :48 — the line-3 current-state candidate read MUST mirror this predicate)
    - src/db/memories-read.ts:101-109 (the EXISTING registry-default-aware memory visibility choke point `resolveVisibility(type, hidden)`: `hidden=1 → hide`, `hidden=0 → show`, null → the type's `visibilityDefault`; unknown type → show. REUSE it for line-3 memory candidates — review cycle-3 MED #4 — do NOT hand-roll a raw `hidden = 1` predicate; this matches Plan 06's corpus discipline. Latent today (all shipped memory types default to `"show"`) but a future `visibilityDefault: "hide"` type would leak a null-hidden row through a raw predicate)
    - src/db/relationships-read.ts:39-43 (the EXISTING relationship visibility choke point `resolveRelationshipVisibility(hidden)`: `1 → hide`, `0 → show`, null → `RELATIONSHIPS_GROUP.visibilityDefault`. REUSE it for line-3 relationship candidates — review cycle-3 MED #4)
    - src/db/memory-registry.ts:36/46/56/110 + `RELATIONSHIPS_GROUP` (VERIFY on disk: ALL shipped memory types AND the relationships group currently set `visibilityDefault: "show"`, so the visibility-after-window starvation is LATENT today — but a future `visibilityDefault: "hide"` type makes it real; the SQL over-fetch to `candidateBudget + CANDIDATE_OVER_FETCH` + the TS re-cap to `candidateBudget` below is the forward-safe guard, review cycle-4 MED)
    - src/db/memories-read.ts, src/db/relationships-read.ts (per-contact read shapes — for the columns/exclusions, NOT to call per-row; note the memory read must SELECT `type`+`hidden` and the relationship read `hidden` so the choke points above can run)
  </read_first>
  <behavior>
    - readLine3Candidates(exec, contactIds): given N contact ids, issues a SINGLE batch query per source (memories / relationships / current-state) using `contact_id IN (${placeholders})`, returning candidate rows tagged with kind, contactId, a stable `id`, `createdAt`, a display value, type, pinned, and meaningfulDate where relevant (review MED #2 — id + createdAt drive the selector's deterministic tie-breaks).
    - PER-SOURCE exclusions (review LOW #3 — NOT universal): memory candidates SELECT `type`+`hidden` and are filtered through `resolveVisibility(type, hidden) === "show"` (memories-read.ts:101, the registry-default-aware choke point — review cycle-3 MED #4, NOT a raw `hidden = 1` predicate), plus `deleted_at IS NOT NULL`, `outdated = 1`, and birthday-type exclusions; relationship candidates SELECT `hidden` and are filtered through `resolveRelationshipVisibility(hidden) === "show"` (relationships-read.ts:39) plus `deleted_at IS NOT NULL`; `current_state_entries` has none of those columns (migration 016:47-57), so it applies ONLY `is_current = 1`. A `hidden = 1` fast-path may remain inside the SQL `ROW_NUMBER` partition (a strict SUBSET of the choke point that never removes a would-show row) so the per-contact budget window is already narrowed, but the TS choke point is the AUTHORITATIVE visibility gate. RANKED_FUEL_EXCLUSIONS apply only if fuel text is reused.
    - Current-state candidates (from `current_state_entries`) filter `is_current = 1` (cycle-1 HIGH #2): the table retains superseded history; without this predicate a stale former value can win line-3.
    - PER-CONTACT bound (cycle-2 HIGH #2) + over-fetch/re-cap (cycle-4 MED): the candidate budget is enforced PER CONTACT, not per query. Wrap each per-source read in a `ROW_NUMBER() OVER (PARTITION BY contact_id ORDER BY <deterministic key>)` subquery and keep only `row_number <= candidateBudget + CANDIDATE_OVER_FETCH` — a still-BOUNDED per-contact window (cycle-2 HIGH #2 holds: no unbounded fetch), but OVER-FETCHED beyond `candidateBudget` so the TS visibility choke points can drop budget-consuming invisible rows (a `hidden = null` row of a future `visibilityDefault: "hide"` type) WITHOUT starving visible candidates; AFTER the TS choke points filter, RE-CAP each contact to `candidateBudget` in TS. A bare `LIMIT N` over the whole `contact_id IN (...)` batch is FORBIDDEN — it caps the total result set and starves contacts later in SQL order. The intra-partition `ORDER BY` is deterministic (e.g. `pinned DESC, meaningful_date DESC / created_at DESC, id DESC`). `candidateBudget` and `CANDIDATE_OVER_FETCH` are tunable constants at the top of the file (CLAUDE.md).
    - Empty `contactIds` → returns an empty result with no query issued (no `IN ()`).
    - No network; async `getAllAsync` only; all runtime values `?`-bound, only closed code-constants interpolated.
  </behavior>
  <acceptance_criteria>
    - `src/db/dashboard-knowledge-read.ts` exports `readLine3Candidates(exec, contactIds)` issuing at most one batch query per source via `contact_id IN (${placeholders(...)})` (reusing the placeholders idiom), never a query per contact.
    - The candidate shape includes a stable `id` and `createdAt` alongside kind, contactId, value, type, pinned, meaningfulDate (review MED #2).
    - `npx vitest run src/db/dashboard-knowledge-read.test.ts` passes and asserts, PER SOURCE (review LOW #3): memory candidates are filtered through `resolveVisibility(type, hidden) === "show"` (review cycle-3 MED #4) — a `hidden = 1` memory is excluded AND a `hidden = null` memory whose type's `visibilityDefault` is `"hide"` is ALSO excluded (register/stub such a type in the test so the registry-default path is exercised, since all shipped types currently default to `"show"`), while a `hidden = 0` (or null-default-show) memory is returned; a deleted/outdated memory and a birthday-typed memory are excluded; relationship candidates are filtered through `resolveRelationshipVisibility(hidden) === "show"` — a `hidden = 1` relationship is excluded, a `hidden = 0`/null-default-show relationship returned; a deleted relationship is excluded; a current-state row with `is_current = 0` is NEVER returned (cycle-1 HIGH #2 regression); an empty `contactIds` returns empty and issues no `IN ()`; the batch returns the expected candidate shape (kind, contactId, id, createdAt, value, type, pinned, meaningfulDate).
    - MULTI-CONTACT boundedness test (cycle-2 HIGH #2): seed contact A with a DEEP history (well beyond the candidate budget) AND contact B with real knowledge, request BOTH ids in one batch, and assert (1) A yields no more than the per-contact budget (after the TS re-cap) and (2) B still receives ITS candidates — i.e. A's history does NOT starve B. This is the exact failure a single-contact boundedness test misses. Each per-contact result is in the deterministic intra-partition order.
    - VISIBILITY-STARVATION regression (cycle-4 MED): seed a contact with several `hidden = null` rows of a STUBBED `visibilityDefault: "hide"` memory type ordered AHEAD of a genuine visible row within the deterministic window, request that contact, and assert the visible row STILL survives to the result — the invisible rows were over-fetched (SQL window `candidateBudget + CANDIDATE_OVER_FETCH`), dropped by `resolveVisibility`, and did NOT consume the visible row's budget (no false completeness prompt). Also assert each contact's final result is re-capped to no more than `candidateBudget` after the TS filter.
    - Contact ids enter the query only as `?` placeholders; no id is string-concatenated into SQL.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <action>
    Create `dashboard-knowledge-read.ts` exporting `readLine3Candidates(exec, contactIds)` that batch-reads the candidate knowledge per contact via `contact_id IN (${placeholders(contactIds)})` (reuse the `placeholders` idiom from `knowledge-search-read.ts`). Apply exclusions PER SOURCE, not universally (review LOW #3): SELECT `type`+`hidden` on the memory read and `hidden` on the relationship read, then filter memory candidates through `resolveVisibility(type, hidden) === "show"` (memories-read.ts:101) and relationship candidates through `resolveRelationshipVisibility(hidden) === "show"` (relationships-read.ts:39) — the EXISTING registry-default-aware choke points, NOT a hand-rolled `hidden = 1` predicate (review cycle-3 MED #4, matching Plan 06's corpus discipline); a `hidden = 1` fast-path may stay inside the SQL `ROW_NUMBER` partition as a safe subset to narrow the per-contact budget window, with the TS choke point authoritative. Memories additionally exclude `deleted_at IS NOT NULL`, `outdated = 1`, and birthday-typed items; relationships additionally exclude `deleted_at IS NOT NULL`; `current_state_entries` (which has none of those columns) applies ONLY `is_current = 1`, mirroring `current-state-history-read.ts:31/:48` so a superseded history row can never surface (cycle-1 HIGH #2); RANKED_FUEL_EXCLUSIONS apply only to any fuel reuse. Bound the candidate budget PER CONTACT (cycle-2 HIGH #2) with an OVER-FETCH + TS re-cap (cycle-4 MED): wrap each per-source read in a `ROW_NUMBER() OVER (PARTITION BY contact_id ORDER BY <deterministic key>)` subquery and keep only `row_number <= candidateBudget + CANDIDATE_OVER_FETCH` — a still-BOUNDED per-contact window (cycle-2 HIGH #2 holds), but over-fetched beyond `candidateBudget` so the TS visibility choke points can drop budget-consuming invisible rows (a `hidden = null` row of a future `visibilityDefault: "hide"` type) without starving visible candidates; do NOT use a bare `LIMIT N` over the whole `contact_id IN (...)` batch, which caps the total set and starves contacts later in SQL order. AFTER the TS `resolveVisibility`/`resolveRelationshipVisibility` filter runs, RE-CAP each contact to `candidateBudget` in TS. Use a deterministic intra-partition `ORDER BY` (e.g. `pinned DESC, meaningful_date DESC / created_at DESC, id DESC`); keep `candidateBudget` and `CANDIDATE_OVER_FETCH` as tunable constants at the top of the file. Return a flat, node-testable candidate shape carrying a stable `id` and `createdAt` (review MED #2) alongside kind, contactId, value, type, pinned, meaningfulDate — the pure selector in Task 3 ranks it and tie-breaks over `id`/`createdAt`; this module does the read only. Guard the empty-ids case to avoid an invalid `IN ()`. Add `dashboard-knowledge-read.test.ts` covering the batch shape (incl. id/createdAt), the per-source exclusions via the choke points (a `hidden = 1` memory AND a `hidden = null` memory of a stubbed `visibilityDefault: "hide"` type are both excluded via `resolveVisibility`; a `hidden = 1` relationship excluded via `resolveRelationshipVisibility`; a visible/non-outdated memory + visible relationship returned — review cycle-3 MED #4), the `is_current = 0` regression, the MULTI-contact per-contact boundedness (A's deep history does not starve B), the VISIBILITY-starvation regression (over-fetched null-hidden hide-default rows do not starve a visible row; per-contact result re-capped to `candidateBudget` after the TS filter — cycle-4 MED), and the empty-ids guard.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-knowledge-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>A single batched knowledge read returns candidate line-3 rows with the correct in-query exclusions and empty-ids guard; test + tsc green.</done>
  <reversibility rating="reversible">New read module; additive and revertable.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic line-3 selection + stable completeness prompts</name>
  <files>src/logic/list-row-selection.ts, src/logic/list-row-selection.test.ts</files>
  <read_first>
    - src/components/contact-card-ring.ts:45-103 (the PURE, TOTAL, deterministic, react-native-free mapper idiom to follow so the selector is Vitest-testable)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Copywriting Contract — the ~10 completeness prompt strings; the priority order imminent → pinned/high-value → other → prompt)
    - docs/dossier/milestone-2/phase-06-dashboard-list-view-dossier.md (§F priority direction, §G completeness prompts, birthdays excluded, deterministic-not-random)
  </read_first>
  <behavior>
    - selectLine3(candidates, contactId, name, now): returns the single deterministic line-3 item by priority — imminent/time-sensitive (meaningful_date within a near window, NON-birthday) → pinned/high-value (pinned candidate) → other useful (most-recent non-hidden/non-outdated/non-deleted) → completeness prompt.
    - When no useful candidate exists, returns the prompt at `PROMPTS[contactId % PROMPTS.length]` interpolated with `name` — stable across re-render for a given contact.
    - Ties within a priority tier break deterministically over the candidate's stable `id`/`createdAt` (review MED #2), NEVER over accidental input/SQL row order — so shuffling the candidate array yields the same winner.
    - Pure, react-native-free, node-tested.
  </behavior>
  <acceptance_criteria>
    - `src/logic/list-row-selection.ts` exports a pure `selectLine3(...)` and a `PROMPTS` array whose strings match the 27-UI-SPEC Copywriting Contract completeness prompts verbatim.
    - `npx vitest run src/logic/list-row-selection.test.ts` passes and asserts: imminent (non-birthday) beats pinned beats other beats prompt; a birthday-typed candidate never wins the imminent tier; the same contactId + candidates yields the same result across repeated calls; SHUFFLING the candidate input array (equal-priority-tier ties) yields the SAME winner via the stable `id`/`createdAt` tie-break (review MED #2 — not input order); the completeness prompt equals `PROMPTS[contactId % PROMPTS.length]` (stable per contact).
    - The module imports no `react-native` symbol (node-testable).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <action>
    Create `list-row-selection.ts` with a pure `selectLine3` implementing the D-11/§F priority order over the candidate rows from Task 2, excluding birthdays from the imminent tier, and a `PROMPTS` array holding the UI-SPEC Copywriting Contract completeness strings verbatim. Break within-tier ties deterministically over the candidate's stable `id`/`createdAt` (review MED #2), never over input array order. Choose the prompt by the stable hash `contactId % PROMPTS.length` so selection never changes on re-render. Keep the module pure and node-tested. Add `list-row-selection.test.ts` proving: imminent beats pinned beats other beats prompt; birthdays never win the imminent tier; same contact + same candidates → same result across repeated calls; a SHUFFLED candidate array yields the same winner via the stable-id tie-break; the prompt is stable per contactId.
  </action>
  <verify>
    <automated>npx vitest run src/logic/list-row-selection.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>selectLine3 is deterministic across the four tiers, excludes birthdays, and returns a stable per-contact prompt; tests + tsc green.</done>
  <reversibility rating="reversible">Pure logic module; additive and revertable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| list contact ids → batch SQLite knowledge read | ids must enter the `IN (...)` list only as `?`-bound placeholders |
| stored knowledge rows → line-3 candidate → rendered row | hidden/off-limits/deleted/outdated knowledge must never reach line 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05 | Tampering | dashboard-knowledge-read.ts `contact_id IN (...)` | high | mitigate | All ids bound as `?` placeholders via the `placeholders()` idiom; only closed code-constants interpolated; empty-ids guarded to avoid a malformed `IN ()`. Mirrors the proven injection posture of the shared read. |
| T-27-06 | Information disclosure | line-3 candidate selection | high | mitigate | Memory visibility is resolved through the registry-default-aware choke point `resolveVisibility(type, hidden)` (memories-read.ts:101) and relationship visibility through `resolveRelationshipVisibility(hidden)` (relationships-read.ts:39) — review cycle-3 MED #4, not a raw `hidden = 1` predicate that would leak a null-hidden row of a future `visibilityDefault: "hide"` type. `deleted_at IS NOT NULL`, `outdated = 1`, `is_current = 1` (current-state), birthday-type, and RANKED_FUEL_EXCLUSIONS are applied in the read (not a UI `.filter()`), so off-limits / hidden / deleted / outdated / superseded knowledge can never surface on line 3. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase; no supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/db/dashboard-knowledge-read.ts` — `readLine3Candidates(exec, contactIds)` (single batch `contact_id IN` read; PER-CONTACT bounded via `ROW_NUMBER() PARTITION BY contact_id` per cycle-2 HIGH #2, over-fetching to `candidateBudget + CANDIDATE_OVER_FETCH` then TS re-capping to `candidateBudget` after the choke points so a null-hidden hide-default row can't starve a visible candidate per cycle-4 MED; per-source exclusions per LOW #3 with memory/relationship visibility routed through `resolveVisibility`/`resolveRelationshipVisibility` per cycle-3 MED #4; candidate shape carries stable `id` + `createdAt` per MED #2).
- `src/logic/list-row-selection.ts` — `selectLine3()` deterministic selector + `PROMPTS` (UI-SPEC canon) + stable per-contact prompt hash.
- `src/components/icons/icon-registry.ts` — `favorite: { outline: 'star-outline', filled: 'star' }`.

<verification>
- `npx vitest run src/db/dashboard-knowledge-read.test.ts src/logic/list-row-selection.test.ts src/components/icons/icon-registry.test.ts` green.
- `npx tsc --noEmit` + `npm run check:colors` clean.
- Selection is deterministic and birthday-excluding; the star pair validates at tsc.
</verification>

<success_criteria>
The deterministic line-3 data + selector + star primitive exist, are pure/batched/node-tested, and are ready for 27-04 to render — with hidden/off-limits knowledge excluded in-query and no AI copy.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-03-SUMMARY.md` when done.
</output>
