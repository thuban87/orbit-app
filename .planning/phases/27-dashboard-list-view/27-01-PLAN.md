---
phase: 27-dashboard-list-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
  - src/components/list-row-content.ts
  - src/components/list-row-content.test.ts
  - src/components/ListRow.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [LISTV-01, LISTV-02, LISTV-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "TRACER: one real ListRow renders end-to-end for the list viewMode on HomeScreen and is visible on the Pixel — avatar + line 1 name + line 2 recency·category + status border + status glyph (D-06 no hardcoded colour)"
    - "E1 populated: List renders the three-line row anatomy through the incumbent FlatList at default text size (~5–6 rows visible target, not an a11y invariant)"
    - "E1 overflow: the list is a virtualized vertical scroll surface over the `background` token"
    - "E1 zero-one-many: 0 contacts → the existing shared empty host; 1..many → rows mapped from the shared Phase 25 read model (List does not implement its own query logic)"
    - "E1 partial: never-contacted rows inside All Contacts / Not Contacted populations render `No interactions yet` + neutral `border` + NO status glyph (D-08 row presentation, NOT a change to the Active predicate; BASE_WHERE still excludes never-contacted from Active)"
    - "LISTV-02: line 2 renders `{recency} · {category}` (e.g. `18d ago · Friend`, `Today · Family`) from the additively-widened `DashboardRow.last_contact`, via a DST-safe local-midnight calendar-day formatter (never the UTC ISO-slice idiom)"
    - "LISTV-05: unsnoozed status shows a same-weight status-coloured border (constant width via `ringVisual(status,colors).color`, NEVER `ring.width`) plus a distinct `StatusGlyph`; `null` status → neutral `border` and no glyph mounted; snoozed → neutral `border` + `status-snoozed` glyph composed by the consumer from `snooze_until`"
    - "The read model is ADDITIVELY widened (last_contact + snooze_until projected onto DashboardRow); every existing DashboardRow consumer still typechecks (review the code, not the diff)"
  artifacts:
    - "src/db/dashboard-read.ts — DashboardRow.last_contact + DashboardRow.snooze_until projected onto listDashboardPopulation + listDashboardSearch"
    - "src/components/list-row-content.ts — formatListRecency() + formatLine2() (pure, node-tested)"
    - "src/components/ListRow.tsx — dense three-line row (identity + recency·category + status border/glyph); star/line3/swipe added by later plans"
    - "src/screens/HomeScreen.tsx — renderItem branches to ListRow when query.viewMode === 'list'"
  key_links:
    - "dashboard-read.ts DashboardRow → HomeScreen renderItem → ListRow props (recency, status, snooze) — the widened projection is the single data source; no per-row DB reads in renderItem"
    - "contact-card-ring.ts ringVisual()/statusGlyph()/StatusDisplayState → ListRow border colour + glyph — single status source, never forked (D-05)"
  prohibitions:
    - "no per-contact swipe configuration"
    - "no additional swipe actions beyond the two (right = configured log, left = Edit)"
    - "no multi-button swipe drawers"
    - "no destructive swipe actions"
    - "no permanent in-List gesture tutorial (gesture education belongs to onboarding)"
    - "no contact-frequency / cadence goal beside recency (no actual-vs-target progress framing)"
    - "no AI-generated adaptive row copy"
    - "no profile-completeness scoring or gamification"
    - "no multi-category / primary-category semantics (one displayed category per contact)"
    - "no user-controlled density"
    - "no ranked-favourites UX; the List never sorts by favourite rank (ADR-075)"
    - "no hardcoded colour anywhere in row rendering (D-06 / check:colors)"
    - "never drive animation from React state (CLAUDE.md)"
    - "no network call on the read path (local-first)"
---

<objective>
Prove the Dashboard List View architecture end-to-end with the thinnest production-quality slice: additively widen the shared Phase 25 read model to carry recency (`last_contact`) and snooze presentation (`snooze_until`), add a pure DST-safe recency formatter, render ONE real `ListRow` (avatar + name + `recency · category` + status border + status glyph, null-safe and snooze-aware), and wire it into `HomeScreen` for the list viewMode so it is visible on the Pixel.

Purpose: catch any architectural dead-end (read-model widening shape, status-source reuse, FlatList recycling correctness) on the agent's best early-context tokens — before six expansion layers are committed on top. This tracer is written for keeps: it becomes the skeleton every later plan (star, line 3, swipe, search, motion) builds on. Functionality gaps (no star/line3/swipe yet) are acceptable; architectural gaps are not.

Output: a widened `DashboardRow`, `list-row-content.ts`, `ListRow.tsx`, and a `HomeScreen` list-mode branch — all committed and verified green + device-visible.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@docs/dossier/milestone-2/phase-06-dashboard-list-view-dossier.md
@src/db/dashboard-read.ts
@src/components/ContactCard.tsx
@src/components/contact-card-ring.ts
@src/components/Avatar.tsx
@src/services/fuel-age.ts
@src/screens/HomeScreen.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a scannable status-bearing row" — one real ListRow, one path</name>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/components/list-row-content.ts, src/components/list-row-content.test.ts, src/components/ListRow.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/db/dashboard-read.ts (DashboardRow interface at :91-113; injection posture at :43-51; CARD_STATUS at :148-149 and BASE_WHERE/SORT already reference c.last_contact/c.snooze_until — they exist on `contacts`, just not projected)
    - src/db/dashboard-read.test.ts (existing projection assertions to extend, not fork)
    - src/services/fuel-age.ts:33-122 (parseLocalMs + calendarDaysBetween — the DST-safe local-midnight calendar-day math to REUSE; formatFuelAge emits "3 days ago" — do NOT call it verbatim, line 2 wants compact "18d ago")
    - src/utils/dates.ts (formatLocalDate — the mandated date renderer)
    - src/components/contact-card-ring.ts (ringVisual at :45 returns escalating .width 2/3/4/3 — use .color only; statusGlyph at :87; StatusDisplayState = ProfileStatus | "snoozed" | null at :73)
    - src/components/icons/StatusGlyph.tsx (state labels at :37-44; state={null} renders status-neutral — must NOT be mounted for null)
    - src/components/Avatar.tsx:45-82 (contactId recyclingKey + cacheBust=modified_at — anti-face-flash CORRECTNESS in a recycling list)
    - src/components/ContactCard.tsx (purely-presentational discipline at :1-72 to copy; raw px styles at :214-265 to NOT copy)
    - src/screens/HomeScreen.tsx (FlatList at :677, renderItem→ContactCard at :680-681, keyExtractor at :679, query.viewMode at :145/:672)
    - src/theme/tokens/{spacing,typography,radii,icon-size}.ts (SPACING, typography roles, RADII.lg=16, ICON_SIZE.md=20)
  </read_first>
  <behavior>
    - formatListRecency(lastContact, now): null last_contact → "No interactions yet"; same calendar day → "Today"; 1 day → "Yesterday"; N days → "{N}d ago"; computed on local-midnight calendar-day diff (DST-safe, no UTC off-by-one).
    - formatLine2(recency, category): category present → "{recency} · {category}"; category null → recency alone; never-contacted → "No interactions yet" (optionally with category).
    - DashboardRow projects last_contact (string|null) and snooze_until (string|null) additively on BOTH listDashboardPopulation and listDashboardSearch; never-contacted rows carry last_contact === null and existing status === null.
    - ListRow status display: displayState computed by the consumer = "snoozed" when snooze_until is active, else the row's ProfileStatus|null; border uses ringVisual(displayState,colors).color at a constant 2px; StatusGlyph mounted only when displayState !== null.
  </behavior>
  <acceptance_criteria>
    - `DashboardRow` declares `last_contact: string | null` and `snooze_until: string | null`, both SELECTed in the `listDashboardPopulation` and `listDashboardSearch` projections; `BASE_WHERE` is unchanged (Active still excludes never-contacted).
    - `npx vitest run src/db/dashboard-read.test.ts src/components/list-row-content.test.ts` passes, including a never-contacted case (last_contact null → `formatListRecency` returns `No interactions yet`) and a `{recency} · {category}` composition case.
    - The ListRow border width is a single constant value for every status (the escalating width returned by `ringVisual` is not wired into the border); the border colour comes from `ringVisual(displayState, colors).color`.
    - The `StatusGlyph` mount in `ListRow.tsx` is guarded so it renders only for the four real states and `"snoozed"`, never for a null displayState.
    - `npx tsc --noEmit` and `npm run check:colors` pass across `src/`.
  </acceptance_criteria>
  <action>
    Wire ONE entry point through every layer to the far end of the stack. (1) In `dashboard-read.ts`, additively add `c.last_contact AS last_contact` and `c.snooze_until AS snooze_until` to the `listDashboardPopulation` and `listDashboardSearch` SELECT projections and to the `DashboardRow` interface — static column names only, all runtime values stay `?`-bound per the file's injection posture; do NOT touch `BASE_WHERE` (Active must still exclude never-contacted). (2) Create `list-row-content.ts` with pure `formatListRecency` and `formatLine2` reusing the calendar-day helper from `fuel-age.ts` and `formatLocalDate()` from `src/utils/dates.ts`; never the UTC ISO-slice date idiom. (3) Create `ListRow.tsx` as a purely-presentational component (explicit props, no DB read, no `getExecutor`, no `useNavigation`) reusing `Avatar` (contactId recyclingKey + cacheBust=modifiedAt, 64–72 class), line 1 name (`numberOfLines={1}` ellipsize tail), line 2 via `formatLine2`, a full-row border via `ringVisual(displayState, colors).color` at a CONSTANT `2px` width (never `ring.width`), and `StatusGlyph` mounted only when `displayState !== null`; compose `"snoozed"` onto the status when `snooze_until` is active (neutral border + moon glyph). All colours resolve through `useTheme().colors`; all sizes through `SPACING`/`RADII.lg`/typography roles/`ICON_SIZE` tokens — no raw px, no hex. (4) In `HomeScreen.tsx`, branch `renderItem` to return `ListRow` when `query.viewMode === 'list'` and keep `ContactCard` otherwise (Phase 28 owns card mode); pass the widened recency/status/snooze props. Do NOT add star, adaptive line 3, or swipe here — those are later plans. Extend `dashboard-read.test.ts` with the new projection assertions and add `list-row-content.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/components/list-row-content.test.ts src/db/dashboard-read.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel (desktop-build-pipeline runbook, Orbit Metro :8082 + adb reverse), open the Dashboard in list viewMode: rows show avatar + name + "{recency} · {category}", a never-contacted row shows "No interactions yet" with a neutral border and no glyph, and status-bearing rows show a same-weight coloured border + celestial glyph. uiautomator dump confirms the status label is exposed without colour.</human-check>
  </verify>
  <done>list-row-content + dashboard-read tests green; tsc + check:colors clean; a real ListRow is visible on the Pixel for list viewMode with identity, recency·category, and null-safe status border/glyph; the single happy path is committed.</done>
  <reversibility rating="reversible">Additive read projection + new presentational component + a renderItem branch; fully revertable with no schema or data change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search/query input → SQLite read (`dashboard-read.ts`) | untrusted term/params cross into SQL; must stay parameterized |
| stored SQLite contact/status rows → rendered ListRow | already-owned local data; row must not resurface rows the shared read excludes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | dashboard-read.ts widened projection | medium | mitigate | Only static column names (`c.last_contact`, `c.snooze_until`) added to the SELECT; all runtime values remain `?`-bound; follow the file's documented injection posture (:43-51). No dynamic ORDER BY or identifier interpolation added. |
| T-27-02 | Information disclosure | ListRow / HomeScreen renderItem | medium | mitigate | The widening is additive only; `BASE_WHERE` exclusions (archived/unbound/never-contacted-in-Active) are untouched, so the row renders exactly the shared read's population — no UI `.filter()` that could resurface excluded rows. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); FlatList/Avatar/gesture-handler/reanimated already present. No supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

This tracer plan produces:
- `DashboardRow.last_contact: string | null` and `DashboardRow.snooze_until: string | null` (additive projection on `listDashboardPopulation` + `listDashboardSearch`).
- `src/components/list-row-content.ts` — `formatListRecency()`, `formatLine2()` (pure, node-tested).
- `src/components/ListRow.tsx` — dense three-line row (identity + recency·category + status border/glyph; star/line3/swipe added by 27-04/05/06).
- `src/screens/HomeScreen.tsx` — `renderItem` list-mode branch to `ListRow`.

Downstream plans add: migration 020 + `dashboard_right_swipe_action` column + preference DAO fns + `PORTABLE_SETTINGS_KEYS` addition (27-02); `dashboard-knowledge-read.ts` + `list-row-selection.ts` + the `favorite` registry star change (27-03); star/line3/a11y-description render (27-04); swipe host + a11y actions (27-05); search rendering + motion (27-06).

<verification>
- `npx vitest run src/components/list-row-content.test.ts src/db/dashboard-read.test.ts` green.
- `npx tsc --noEmit` and `npm run check:colors` clean across `src/`.
- Every existing `DashboardRow` consumer (HomeScreen mapping, ContactCard mapping, dashboard-read.test) typechecks after the additive widen.
- Device: list viewMode renders real rows with identity/recency/status; never-contacted → "No interactions yet" + neutral border + no glyph.
</verification>

<success_criteria>
The list viewMode shows scannable status-bearing rows sourced only from the widened shared read, proving the read→component→screen path end-to-end with correct null/snooze handling and zero hardcoded colour.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-01-SUMMARY.md` when done.
</output>
