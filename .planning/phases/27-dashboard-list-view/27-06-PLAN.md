---
phase: 27-dashboard-list-view
plan: 06
type: execute
wave: 5
depends_on: [27-04, 27-05]
files_modified:
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
  - src/db/dashboard-search-read.ts
  - src/db/dashboard-search-read.test.ts
  - src/components/list-row-content.ts
  - src/components/list-row-content.test.ts
  - src/components/ListRow.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [LISTV-06, LISTV-10]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LISTV-06 data path: during search the displayed rows AND their match descriptors come from the corpus search model — `listKnowledgeSearchCandidates` (batch corpus read) → `searchDashboard(...)` → a `DashboardSearchResult` per contact — composed in the shared `src/db`/`src/logic` layer (`composeDashboardSearch`), NEVER from `listDashboardSearch.snippet` (which is name+fuel only and carries no count and no per-source categories). These corpus modules (`dashboard-search-match.ts` / `searchDashboard`, `knowledge-search-read.ts` / `listKnowledgeSearchCandidates`) are BUILT + unit-TESTED but were UNWIRED — no `src/` consumer existed before this plan; wiring them is part of this fix."
    - "LISTV-06 coverage: the corpus search runs over the A3-aware term-free eligible universe so identity / relationship / memory / custom-field matches surface even when the contact's name and fuel do NOT contain the term (a memory-only match is displayable). `listDashboardSearch` is composed ALONGSIDE (its shipped name+fuel semantics unchanged) so its fuel-only matches still surface."
    - "LISTV-06 / E3 populated: during search each row keeps the name on line 1 and replaces lines 2–3 with a compact match explanation built from `DashboardSearchResult` — `formatMatchExplanation(totalMatchCount, categories, moreMatchesLabel)` yields `3 matches · Memory, Relationship` — plus the strongest highlighted snippet (`matches[0].snippet` with `matches[0].highlights`)"
    - "LISTV-06 sources: the count is `DashboardSearchResult.totalMatchCount`; the strongest categories are the distinct `matches[].sourceKind` (identity | relationship | memory-or-custom-field | note-or-body) mapped to the UI-SPEC category labels in descriptor-priority order; the `+N more` copy is `DashboardSearchResult.moreMatchesLabel` (no inline action)"
    - "LISTV-06: the matched run is highlighted by rendering the `matches[].highlights` (offset+length) run at label weight (600) on `textPrimary` inside an otherwise-caption line — no highlight background colour, no hex"
    - "LISTV-06 fuel-only fallback: a row matched only by `listDashboardSearch` name+fuel (no corpus `DashboardSearchResult`) renders `1 match` (via `formatMatchExplanation` with no categories) plus the `DashboardRow.snippet` fuel text on line 3; the fuel snippet's highlight run MAY be derived by reusing the shared scorer `matchCandidateEntries` (no new/forked search logic)"
    - "E3 partial: `+N more` is shown with no inline action; the strongest match categories are named"
    - "E3 zero-one-many: pluralised count copy (`1 match` / `3 matches · …`)"
    - "E3 empty: zero matches (no corpus match AND no fuel match for the term) routes to the shared `No matches for \"{term}\"` empty state (E1)"
    - "E3 overflow / long-text: the match-explanation caption and the snippet are single-line ellipsize tail; no wrap"
    - "E1 empty: cause-aware shared empty states reused verbatim from the shared host (first-run CTA `Add your first contact`, `No matches for \"{term}\"`, filtered-empty population-labelled, everyone-hidden pointer lines) — copy shared with Card View"
    - "E1 error: `Couldn't load your contacts` + `Pull down to try again.` reused from the shared host"
    - "E1 loading / E2 loading: skeleton rows on INITIAL or meaningfully-delayed load only; fast Population/Filter/Sort/Search changes keep current content visible and update in place; the skeleton matches row geometry; no per-row spinner during in-place updates"
    - "LISTV-10: fast query changes update rows in place with short restrained transitions gated by `useReducedMotion()` (instant apply, no position animation when reduced); tokens MOTION.fast/base + EASING.decelerate"
  artifacts:
    - "src/db/dashboard-read.ts — `listDashboardSearchEligible()` (the A3-aware term-free search-scoped eligible read; shares `listDashboardSearch`'s scope via an extracted behaviour-preserving helper — `listDashboardSearch`'s name+fuel semantics are UNCHANGED)"
    - "src/db/dashboard-search-read.ts — `composeDashboardSearch(exec, query, term, now)` → `DashboardSearchRow[]` (each = `DashboardRow` + `DashboardSearchResult | null`), composing the eligible universe + `listKnowledgeSearchCandidates` (batch) + `searchDashboard` + `listDashboardSearch` (fuel) — all local, parameterized SQL inherited"
    - "src/components/list-row-content.ts — `formatMatchExplanation(matchCount, categories, moreMatchesLabel?)` + a `DashboardSearchSourceKind` → category-label map (pure, node-tested)"
    - "src/components/ListRow.tsx — search-mode rendering (name fixed; lines 2–3 → match explanation + highlighted snippet at label weight; fuel-only fallback)"
    - "src/screens/HomeScreen.tsx — search data path (`reload()` calls `composeDashboardSearch` when a term is present and plumbs the per-row `DashboardSearchResult` into `renderItem`) + reduced-motion-aware in-place result transitions + shared empty/error/loading host wiring for list mode"
  key_links:
    - "composeDashboardSearch (listKnowledgeSearchCandidates → searchDashboard → DashboardSearchResult, alongside listDashboardSearch for fuel) → HomeScreen renderItem → ListRow search-mode lines — the List renders a compact view of the shared corpus search-result model (no fork, no per-row query, no renderer-side search logic)"
    - "DashboardSearchResult.totalMatchCount / matches[].sourceKind+fieldLabel / moreMatchesLabel / matches[].snippet+highlights → formatMatchExplanation + ListRow highlighted snippet — the match explanation is a pure render of the corpus result"
    - "selectDashboardEmptyState / dashboard-empty-logic (shared host) → HomeScreen list empty/error — semantics shared with Card View"
    - "useReducedMotion() → HomeScreen result transitions — instant vs animated"
  prohibitions:
    - "no network call on the read path (local-first; search runs over the shared local corpus + dashboard reads)"
    - "no contact-frequency / cadence goal beside recency"
    - "no re-implementation of query / search / tokenizer logic in the renderer OR inside composeDashboardSearch — consume searchDashboard / matchCandidateEntries (the sole tokenizer boundary)"
    - "do NOT change listDashboardSearch's shipped name+fuel semantics — only additive changes (a behaviour-preserving extracted scope helper + a new sibling eligible read)"
    - "no skeleton flash on every fast query change"
    - "no highlight background colour / no hex — highlight is weight-only"
---

<objective>
Complete the List renderer's search-result presentation and its motion/empty/error behaviour by establishing the REAL LISTV-06 data path. During search, keep the name and replace lines 2–3 with a compact match explanation plus the strongest highlighted snippet — sourced from the corpus search result model (`DashboardSearchResult`), not from `listDashboardSearch`'s name+fuel `snippet`. Keep fast query changes flicker-free with restrained reduced-motion-aware transitions, and reuse the shared cause-aware empty/error/loading semantics.

Purpose: LISTV-06 (search rendering) and LISTV-10 (in-place updates, restrained motion, skeletons only on initial/delayed load, shared empty/error). The corpus search modules that carry the match count, per-source categories, `+N more`, and offset-preserving highlights are already BUILT + TESTED but were UNWIRED (no `src/` consumer); this plan wires them into a shared composition step and renders a compact view of them. This closes the phase over the swipe-complete row.
Output: a shared `composeDashboardSearch` read composition (+ the A3-aware term-free eligible read it needs), search-mode ListRow rendering + `formatMatchExplanation`, and HomeScreen search-data-path + motion/empty/error wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/components/ListRow.tsx
@src/components/list-row-content.ts
@src/db/dashboard-read.ts
@src/db/knowledge-search-read.ts
@src/logic/dashboard-search-match.ts
@src/services/knowledge-search.ts
@src/logic/dashboard-empty-logic.ts
@src/screens/HomeScreen.tsx
@src/theme/use-reduced-motion.ts
@src/theme/tokens/motion.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared Dashboard search composition (corpus result model wired in)</name>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/dashboard-search-read.ts, src/db/dashboard-search-read.test.ts</files>
  <read_first>
    - src/logic/dashboard-search-match.ts (READ IN FULL — `searchDashboard(candidates, term, orderedEligibleIds)` returns `DashboardSearchResult[]`, each carrying `totalMatchCount`, `matches[]` with `sourceKind`/`fieldLabel`/`snippet`/`highlights`, and `moreMatchesLabel`; `buildDashboardSearchResult` builds one result without re-ranking; `DashboardSearchSourceKind` is the closed union)
    - src/db/knowledge-search-read.ts (`listKnowledgeSearchCandidates(exec, { eligibleIds })` — the batch corpus read; `contact_id IN (...)`/`c.id IN (...)` all `?`-bound via `placeholders()`; empty `eligibleIds` returns `[]` with no query; fuel is deliberately NOT a corpus source)
    - src/services/knowledge-search.ts (`matchCandidateEntries(term, entries)` — the sole tokenizer boundary; reuse it for the fuel-snippet highlight offsets, never re-tokenize elsewhere)
    - src/db/dashboard-read.ts (`listDashboardSearch` at :398-446 — the A3 scope at :412-415 (implicit-Active relaxes to `c.archived_at IS NULL AND DASHBOARD_BOUND_WHERE`, else `composition.where.sql`), the `termPredicate` at :416-419 and fuel `snippet` at :420 to OMIT for the eligible read; `listDashboardPopulation` at :362-391 for the projection + `applyPopulationPostProcessing` shape to mirror; injection posture at :43-51)
    - src/db/dashboard-read.test.ts (existing projection/scope assertions to extend, not fork)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Copywriting Contract search rows + UI Considerations E3 — the shared result model provides the count/categories/`+N more`; the List renders a compact view of it)
  </read_first>
  <behavior>
    - listDashboardSearchEligible(exec, query, now): returns the A3-aware term-free search-scoped eligible `DashboardRow[]` in Dashboard order — identical scope to `listDashboardSearch` (implicit-Active relaxation included) but WITHOUT the term predicate and WITHOUT the fuel snippet — by extracting the scope construction into a behaviour-preserving helper both functions call. `listDashboardSearch`'s existing rows/order/snippet output is byte-for-byte unchanged.
    - composeDashboardSearch(exec, query, term, now): empty/whitespace term → returns `[]`. Otherwise it reads the eligible universe (row data + `orderedEligibleIds` in Dashboard order), reads the batch corpus via `listKnowledgeSearchCandidates(exec, { eligibleIds })`, ranks + builds descriptors via `searchDashboard(candidates, term, orderedEligibleIds)`, and reads `listDashboardSearch(exec, query, term, now)` alongside for name+fuel matches. It returns `DashboardSearchRow[]` = the eligible rows whose id is a corpus match OR a fuel match, in Dashboard (eligible) order, each carrying its `DashboardRow` and its `DashboardSearchResult | null` (null for a fuel-only match; that row keeps its `DashboardRow.snippet`).
    - All reads are local SQLite; no id is string-concatenated into SQL (the batch corpus read is `?`-bound; the eligible read inherits `listDashboardSearch`'s parameterization).
  </behavior>
  <acceptance_criteria>
    - `src/db/dashboard-read.ts` exports `listDashboardSearchEligible(exec, query, now)` returning the term-free A3-aware eligible `DashboardRow[]`; `listDashboardSearch` still produces its shipped name+fuel rows + `snippet` unchanged (its existing tests stay green).
    - `src/db/dashboard-search-read.ts` exports `composeDashboardSearch(...)` and a `DashboardSearchRow` type; it produces one entry per displayed contact (corpus match OR fuel match), attaches `DashboardSearchResult | null`, and returns them in Dashboard (eligible) order; an empty term returns `[]`.
    - The composition surfaces a memory-only match: a contact whose term appears only in a Memory (name/fuel do NOT match) appears in the result with a non-null `DashboardSearchResult` carrying that memory descriptor. A fuel-only match (term in fuel, not in the corpus) appears with `match === null` and a non-null `DashboardRow.snippet`.
    - `npx vitest run src/db/dashboard-read.test.ts src/db/dashboard-search-read.test.ts` passes, including the memory-only-match case, the fuel-only-fallback case, the empty-term case, and a corpus+fuel union case; contact ids reach SQL only as `?` placeholders.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <action>
    In `dashboard-read.ts`, extract `listDashboardSearch`'s scope construction (the implicit-Active vs explicit-population `scope` at :412-415) into a small behaviour-preserving helper and have BOTH `listDashboardSearch` and a new `listDashboardSearchEligible(exec, query, now)` call it; the eligible read runs the same `CARD_FROM`/projection/`applyPopulationPostProcessing` path as `listDashboardPopulation` under that scope but WITHOUT the `termPredicate` and with `NULL AS snippet`, so it returns the A3-aware term-free eligible universe in Dashboard order. Leave `listDashboardSearch`'s rows/order/snippet identical. Create `dashboard-search-read.ts` exporting `composeDashboardSearch(exec, query, term, now)` and a `DashboardSearchRow` type: bail to `[]` on an empty term; otherwise read the eligible rows, derive `orderedEligibleIds` from them, call `listKnowledgeSearchCandidates(exec, { eligibleIds: orderedEligibleIds })` then `searchDashboard(candidates, term, orderedEligibleIds)` to get the ranked `DashboardSearchResult[]` (map by `contactId`), and call `listDashboardSearch(exec, query, term, now)` for the name+fuel match set (and its `snippet`); then emit the eligible rows whose id is present in either the corpus map or the fuel set, in eligible order, each `{ row, match }` where `match` is the corpus `DashboardSearchResult` or `null`. Do NOT re-tokenize or re-implement scoring anywhere — the descriptors come only from `searchDashboard`; the fuel-only fallback highlight (used later by the renderer) is derived only via the shared `matchCandidateEntries`. Extend `dashboard-read.test.ts` for `listDashboardSearchEligible` and add `dashboard-search-read.test.ts` for the union, memory-only, fuel-only, and empty-term cases.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts src/db/dashboard-search-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`composeDashboardSearch` returns corpus-backed `DashboardSearchResult`s (with count/categories/highlights) unioned with fuel-only fallbacks over the A3-aware term-free eligible universe; `listDashboardSearch` is unchanged; tests + tsc green; the previously-unwired corpus modules now have a `src/` consumer.</done>
  <reversibility rating="reversible">Additive: a behaviour-preserving scope extraction + a new sibling read + a new composition module; revertable with no schema or data change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Search-mode rendering + match explanation + HomeScreen data path</name>
  <files>src/components/list-row-content.ts, src/components/list-row-content.test.ts, src/components/ListRow.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/logic/dashboard-search-match.ts (the `DashboardSearchResult` shape the renderer consumes — `totalMatchCount`, `matches[].sourceKind`/`fieldLabel`/`snippet`/`highlights`, `moreMatchesLabel`; `DashboardSearchSourceKind` is the closed union to map to category labels)
    - src/db/dashboard-search-read.ts (Task 1: `composeDashboardSearch` + `DashboardSearchRow` — the per-row `{ row, match }` this task plumbs into `renderItem`)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Copywriting Contract search rows: `3 matches · Memory, Relationship`, `+N more` with no inline action; Typography — the matched run at label weight 600 on textPrimary, no highlight background; E3 rows)
    - src/components/ListRow.tsx (the content-complete row from 27-04/05 to add a search branch to; keep it purely presentational)
    - src/components/list-row-content.ts (existing formatters + a11y description to compose with)
    - src/screens/HomeScreen.tsx (:282-:322 `reload()` — the term-present branch currently calls `listDashboardSearch`; the `setRows` state; the list-mode `renderItem` from 27-01/04/05 that maps `DashboardRow` → ListRow props)
    - src/services/knowledge-search.ts (`matchCandidateEntries` — reuse ONLY for the fuel-only-fallback snippet highlight; never re-tokenize elsewhere)
  </read_first>
  <behavior>
    - formatMatchExplanation(matchCount, categories, moreMatchesLabel?): pluralises the count (`1 match`, `3 matches`), appends `· ` + the strongest categories joined by `, ` when categories is non-empty, and appends the render-ready `moreMatchesLabel` (e.g. `+2 more`) when present — no inline action. Empty categories → count alone (the fuel-only fallback path).
    - A `DashboardSearchSourceKind` → category-label map turns the distinct `matches[].sourceKind` (in descriptor-priority order, deduped) into the UI-SPEC category labels (e.g. memory-or-custom-field → `Memory`, relationship → `Relationship`).
    - In search mode, ListRow keeps line 1 = name (unchanged); line 2 = the match explanation; line 3 = the strongest snippet (`matches[0].snippet`) with `matches[0].highlights` rendered at label weight (600) on textPrimary within the caption line (no background colour). A fuel-only row (match === null) shows `1 match` on line 2 and `DashboardRow.snippet` on line 3 (highlight run optional, via `matchCandidateEntries`).
    - Non-search mode is unchanged (recency·category + adaptive line 3).
  </behavior>
  <acceptance_criteria>
    - `src/components/list-row-content.ts` exports a pure `formatMatchExplanation(matchCount, categories, moreMatchesLabel?)` producing pluralised copy (`1 match`, `3 matches`), a strongest-categories join (`· Memory, Relationship`), and the passed-through `+N more`; plus a pure `DashboardSearchSourceKind` → category-label mapping.
    - `npx vitest run src/components/list-row-content.test.ts` passes with cases for count=1 (singular), count>1 (plural), the category join from distinct sourceKinds, the `+N more` pass-through, and the empty-categories (fuel-only) path.
    - In `ListRow.tsx` search mode, line 1 stays the name; lines 2–3 render the match explanation and the strongest snippet from the `DashboardSearchResult`; the matched run renders at label weight (600) on `textPrimary` with no highlight background colour; a `match === null` row falls back to `1 match` + `DashboardRow.snippet`.
    - `HomeScreen.reload()` calls `composeDashboardSearch(exec, query, term, now)` when the term is non-empty (replacing the direct `listDashboardSearch` call) and passes each row its `DashboardSearchResult | null` into the list-mode `renderItem`/ListRow props; `rows` remains `DashboardRow[]` for the existing empty-state/count consumers.
    - `npx tsc --noEmit` and `npm run check:colors` pass.
  </acceptance_criteria>
  <action>
    Add a pure `formatMatchExplanation(matchCount, categories, moreMatchesLabel?)` and a pure `DashboardSearchSourceKind` → category-label map to `list-row-content.ts`, producing the pluralised count + strongest categories + passed-through `+N more`. Add a search branch to `ListRow.tsx`: when the row carries a search `DashboardSearchResult` (a new prop), keep line 1 name and replace lines 2–3 with the match explanation (count from `totalMatchCount`, categories from the distinct `matches[].sourceKind`, `+N more` from `moreMatchesLabel`) and the strongest snippet (`matches[0].snippet`), rendering the `matches[0].highlights` run at label weight 600 on `textPrimary` inside the caption line — no highlight background, no hex, single-line ellipsize tail; for a null result carrying a fuel snippet, render `1 match` + `DashboardRow.snippet` (highlight run optional via `matchCandidateEntries`). Consume the shared corpus result model only — do not re-implement or re-tokenize search logic in the renderer. In `HomeScreen.tsx`, switch the term-present branch of `reload()` to `composeDashboardSearch`, keep `rows` as the ordered `DashboardRow[]`, hold the per-contact `DashboardSearchResult | null` (e.g. a `Map` keyed by contact id), and pass it into the list-mode `renderItem` so each ListRow gets its descriptor. Extend `list-row-content.test.ts` for pluralisation, the category-map join, the `+N more` pass-through, and the empty-categories path.
  </action>
  <verify>
    <automated>npx vitest run src/components/list-row-content.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: type a search term — rows keep the name, line 2 shows `N matches · …` (categories from Memory/Relationship/etc.), line 3 shows the strongest snippet with the matched run visually emphasised (weight, not colour); `+N more` appears with no tap target; a contact matched only by a memory (name/fuel do not contain the term) still appears; zero matches shows `No matches for "{term}"`.</human-check>
  </verify>
  <done>Search rows show name + corpus-backed match explanation (count + categories + `+N more`) + highlighted strongest snippet (weight-only highlight), with a fuel-only fallback; HomeScreen search reads via `composeDashboardSearch`; tests + tsc + check:colors green.</done>
  <reversibility rating="reversible">Additive pure formatter + a render branch + a screen read swap; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 3: In-place transitions + shared empty/error/loading host</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/theme/use-reduced-motion.ts:122-132 (useReducedMotion React twin — re-renders on live OS toggle, unlike RN's boot-only hook)
    - src/theme/tokens/motion.ts (MOTION.fast=120 / MOTION.base=200; EASING.decelerate)
    - src/logic/dashboard-empty-logic.ts + src/screens/HomeScreen.tsx (selectDashboardEmptyState + the existing shared empty/error host and RefreshControl; the copy is shared with Card View — reuse, do not re-author)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Motion Contract + Copywriting Contract; skeletons only on initial/delayed load)
  </read_first>
  <acceptance_criteria>
    - Fast Population/Filter/Sort/Search changes keep the current rows visible and update in place — no skeleton flash on each query change; skeleton rows appear only on initial load or a meaningfully delayed load and match row geometry.
    - Result transitions are gated by `useReducedMotion()`: animated (short position/fade via MOTION.fast/base + EASING.decelerate) when motion is allowed; instant apply with no position animation when reduced.
    - The list mode reuses the shared `selectDashboardEmptyState` host for first-run CTA, `No matches for "{term}"`, filtered-empty, everyone-hidden pointers, and the `Couldn't load your contacts` / `Pull down to try again.` error — identical semantics to Card View.
    - `npx tsc --noEmit` + `npm run check:colors` clean; `npm test` (full suite) green at wave merge.
  </acceptance_criteria>
  <action>
    Wire the list-mode result transitions through `useReducedMotion()`: keep current content visible on fast query changes and apply a short restrained enter/position transition (MOTION.fast/base + EASING.decelerate) only when motion is allowed, falling back to instant apply under reduced motion — never a skeleton flash per query change. Reserve skeleton rows (matching row geometry) for initial/meaningfully-delayed load. Route the list-mode empty/error/loading states through the existing shared `selectDashboardEmptyState` host so the cause-aware copy is identical to Card View (reuse verbatim; do not re-author strings). Keep all reads on the local shared model — no network on the read path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: change Population/Filters/Sort/Search quickly — rows update in place with a restrained transition and no skeleton flash; enable OS reduced motion and confirm transitions become instant; force zero results / a load error and confirm the shared empty/error copy matches Card View; initial load shows a row-shaped skeleton.</human-check>
  </verify>
  <done>Fast query changes update in place with reduced-motion-aware restrained transitions, skeletons only on initial/delayed load, and shared cause-aware empty/error states; tsc + check:colors + full suite green.</done>
  <reversibility rating="reversible">Screen-level transition/host wiring; revertable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search term → shared local reads (`listDashboardSearch` + `listKnowledgeSearchCandidates`) | untrusted term crosses into SQL; must stay parameterized in the shared reads |
| list eligible contact ids → batch corpus read (`listKnowledgeSearchCandidates`) | ids must enter the `IN (...)` list only as `?`-bound placeholders |
| shared corpus result model (`DashboardSearchResult`) → rendered snippet/highlight | must render only the shared reads' already-excluded content; no UI re-query, no renderer-side tokenization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-10 | Tampering | search term → listDashboardSearch / eligible read | high | mitigate | The term is `?`-bound + `escapeLike`-escaped in `listDashboardSearch` (dashboard-read.ts:411-420); `listDashboardSearchEligible` reuses the SAME scope helper and omits the term predicate entirely, so it adds no term-derived SQL; the List does not re-implement query logic. |
| T-27-13 | Tampering | composeDashboardSearch → listKnowledgeSearchCandidates `IN (...)` | high | mitigate | Eligible ids enter the batch corpus read only as `?` placeholders via the read's `placeholders()` idiom (knowledge-search-read.ts:86-137); the empty-ids case returns `[]` with no query; `composeDashboardSearch` interpolates no id into SQL. |
| T-27-11 | Information disclosure | corpus/fuel snippet rendering | medium | mitigate | Corpus descriptors come from `listKnowledgeSearchCandidates`, which reads only live, user-facing entries (no storage ids); the fuel snippet comes from `listDashboardSearch`'s in-query `RANKED_FUEL_EXCLUSIONS`; the renderer adds no `.filter()` that could resurface excluded content and re-uses `matchCandidateEntries` (the sole tokenizer) rather than a new scan. |
| T-27-12 | Information disclosure | read path network | low | accept | No fetch anywhere on the read path (local-first, CLAUDE.md); the row render and both search reads are pure local SQLite + pure TS scoring. No mitigation needed beyond preserving the no-network posture. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this plan; `dashboard-search-match.ts` / `knowledge-search-read.ts` / `knowledge-search.ts` already ship in `src/`. No supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/db/dashboard-read.ts` — `listDashboardSearchEligible()` (A3-aware term-free search-scoped eligible read via an extracted behaviour-preserving scope helper; `listDashboardSearch` unchanged).
- `src/db/dashboard-search-read.ts` — `composeDashboardSearch()` → `DashboardSearchRow[]` (corpus `DashboardSearchResult` per contact via `listKnowledgeSearchCandidates` + `searchDashboard`, unioned with `listDashboardSearch` fuel matches).
- `src/components/list-row-content.ts` — `formatMatchExplanation()` (pluralised count + categories + `+N more`, pure, node-tested) + the `DashboardSearchSourceKind` → category-label map.
- `src/components/ListRow.tsx` — search-mode rendering (name fixed; lines 2–3 → match explanation + weight-highlighted strongest snippet; fuel-only fallback).
- `src/screens/HomeScreen.tsx` — search data path (`composeDashboardSearch` + per-row descriptor plumbing) + reduced-motion-aware in-place result transitions + shared empty/error/loading host wiring for list mode.

<verification>
- `npx vitest run src/db/dashboard-read.test.ts src/db/dashboard-search-read.test.ts src/components/list-row-content.test.ts` green; `npx tsc --noEmit` + `npm run check:colors` clean; `npm test` full suite green at wave merge.
- Device UAT (end-of-phase): search rendering (count + categories + highlighted snippet; a memory-only match surfaces; fuel-only fallback), in-place transitions, reduced-motion instant apply, shared empty/error/skeleton states.
</verification>

<success_criteria>
Search keeps identity stable while showing a corpus-backed compact match explanation (count + per-source categories + `+N more`) and a highlighted strongest snippet — with memory/relationship/custom matches surfacing and fuel matches preserved; fast query changes update in place with restrained reduced-motion-aware motion; empty/error/loading semantics match Card View — all over the shared local read model with no network.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-06-SUMMARY.md` when done.
</output>
