---
phase: 27-dashboard-list-view
plan: 06
type: execute
wave: 5
depends_on: [27-04, 27-05]
files_modified:
  - src/components/ListRow.tsx
  - src/components/list-row-content.ts
  - src/components/list-row-content.test.ts
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [LISTV-06, LISTV-10]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "LISTV-06 / E3 populated: during search each row keeps the name on line 1 and replaces lines 2–3 with a compact match explanation (`3 matches · Memory, Relationship`) plus the strongest highlighted snippet"
    - "LISTV-06: the matched run is highlighted by rendering it at label weight (600) on `textPrimary` inside an otherwise-caption line — no highlight background colour, no hex"
    - "E3 partial: `+N more` categories are shown with no inline action; the strongest match categories are named"
    - "E3 zero-one-many: pluralised count copy (`1 match` / `3 matches · …`)"
    - "E3 empty: zero matches routes to the shared `No matches for \"{term}\"` empty state (E1)"
    - "E3 overflow / long-text: the match-explanation caption and the snippet are single-line ellipsize tail; no wrap"
    - "E1 empty: cause-aware shared empty states reused verbatim from the shared host (first-run CTA `Add your first contact`, `No matches for \"{term}\"`, filtered-empty population-labelled, everyone-hidden pointer lines) — copy shared with Card View"
    - "E1 error: `Couldn't load your contacts` + `Pull down to try again.` reused from the shared host"
    - "E1 loading / E2 loading: skeleton rows on INITIAL or meaningfully-delayed load only; fast Population/Filter/Sort/Search changes keep current content visible and update in place; the skeleton matches row geometry; no per-row spinner during in-place updates"
    - "LISTV-10: fast query changes update rows in place with short restrained transitions gated by `useReducedMotion()` (instant apply, no position animation when reduced); tokens MOTION.fast/base + EASING.decelerate"
  artifacts:
    - "src/components/list-row-content.ts — formatMatchExplanation() (count + categories + '+N more', pluralised; pure, node-tested)"
    - "src/components/ListRow.tsx — search-mode rendering (name fixed; lines 2–3 → match explanation + highlighted snippet at label weight)"
    - "src/screens/HomeScreen.tsx — reduced-motion-aware in-place result transitions + shared empty/error/loading host wiring for list mode"
  key_links:
    - "listDashboardSearch snippet + match descriptors (Phase 25) → ListRow search-mode lines — List renders a compact view of the shared search result model (no fork)"
    - "selectDashboardEmptyState / dashboard-empty-logic (shared host) → HomeScreen list empty/error — semantics shared with Card View"
    - "useReducedMotion() → HomeScreen result transitions — instant vs animated"
  prohibitions:
    - "no network call on the read path (local-first; search runs over the shared local read model)"
    - "no contact-frequency / cadence goal beside recency"
    - "no re-implementation of query/search logic in the renderer (consume the shared model)"
    - "no skeleton flash on every fast query change"
    - "no highlight background colour / no hex — highlight is weight-only"
---

<objective>
Complete the List renderer's search-result presentation and its motion/empty/error behaviour: during search keep the name and replace lines 2–3 with a compact match explanation plus the strongest highlighted snippet; keep fast query changes flicker-free with restrained reduced-motion-aware transitions; and reuse the shared cause-aware empty/error/loading semantics.

Purpose: LISTV-06 (search rendering) and LISTV-10 (in-place updates, restrained motion, skeletons only on initial/delayed load, shared empty/error). This closes the phase over the swipe-complete row.
Output: search-mode ListRow rendering + `formatMatchExplanation` + HomeScreen motion/empty/error wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/components/ListRow.tsx
@src/components/list-row-content.ts
@src/db/dashboard-read.ts
@src/logic/dashboard-empty-logic.ts
@src/screens/HomeScreen.tsx
@src/theme/use-reduced-motion.ts
@src/theme/tokens/motion.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Search-mode rendering + match explanation</name>
  <files>src/components/list-row-content.ts, src/components/list-row-content.test.ts, src/components/ListRow.tsx</files>
  <read_first>
    - src/db/dashboard-read.ts (DashboardRow.snippet at :105-106; the match descriptors the shared search read surfaces — up to three prioritized + total count; listDashboardSearch)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Copywriting Contract search rows: `3 matches · Memory, Relationship`, `+N more` no inline action; Typography — matched run at label weight 600 on textPrimary, no highlight background)
    - src/components/ListRow.tsx (the content-complete row from 27-04/05 to add a search branch to)
    - src/components/list-row-content.ts (existing formatters + a11y description to compose with)
    - docs/dossier/milestone-2/phase-06-dashboard-list-view-dossier.md (§L search presentation — strongest inline match + count, no expansion)
  </read_first>
  <behavior>
    - formatMatchExplanation(matchCount, categories): pluralises (`1 match`, `3 matches`), joins the strongest categories (`· Memory, Relationship`), and appends `+N more` when categories exceed the shown set — no inline action.
    - In search mode, ListRow keeps line 1 = name (unchanged), line 2 = the match explanation, line 3 = the strongest highlighted snippet; the matched run renders at label weight (600) on textPrimary within the caption line (no background colour).
    - Non-search mode is unchanged (recency·category + adaptive line 3).
  </behavior>
  <acceptance_criteria>
    - `src/components/list-row-content.ts` exports a pure `formatMatchExplanation(matchCount, categories)` producing pluralised copy (`1 match`, `3 matches`), a strongest-categories join (`· Memory, Relationship`), and `+N more` with no inline action.
    - `npx vitest run src/components/list-row-content.test.ts` passes with cases for count=1 (singular), count>1 (plural), category join, and the `+N more` overflow.
    - In `ListRow.tsx` search mode, line 1 stays the name; lines 2–3 render the match explanation and the strongest snippet; the matched run renders at label weight (600) on `textPrimary` with no highlight background colour.
    - `npx tsc --noEmit` and `npm run check:colors` pass.
  </acceptance_criteria>
  <action>
    Add a pure `formatMatchExplanation` to `list-row-content.ts` producing the pluralised count + strongest categories + `+N more` copy. Add a search branch to `ListRow.tsx`: when the row is in search mode (a `searchDescriptor`/`snippet` prop is present), keep line 1 name and replace lines 2–3 with the match explanation and the strongest highlighted snippet, rendering the matched run at label weight 600 on `textPrimary` inside the caption line — no highlight background, no hex, single-line ellipsize. Consume the shared search result model (snippet + match descriptors from `listDashboardSearch`); do not re-implement search logic. Extend `list-row-content.test.ts` for the pluralisation, category join, and `+N more`.
  </action>
  <verify>
    <automated>npx vitest run src/components/list-row-content.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: type a search term — rows keep the name, line 2 shows `N matches · …` and line 3 shows the strongest snippet with the matched run visually emphasised (weight, not colour); `+N more` appears with no tap target; zero matches shows `No matches for "{term}"`.</human-check>
  </verify>
  <done>Search rows show name + match explanation + highlighted strongest snippet (weight-only highlight); the explanation copy pluralises and collapses to `+N more`; tests + tsc + check:colors green.</done>
  <reversibility rating="reversible">Additive pure formatter + a render branch; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: In-place transitions + shared empty/error/loading host</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/theme/use-reduced-motion.ts:122-132 (useReducedMotion React twin — re-renders on live OS toggle, unlike RN's boot-only hook)
    - src/theme/tokens/motion.ts (MOTION.fast=120 / MOTION.base=200; EASING.decelerate)
    - src/logic/dashboard-empty-logic.ts + src/screens/HomeScreen.tsx (selectDashboardEmptyState + the existing shared empty/error host and RefreshControl; the copy is shared with Card View — reuse, do not re-author)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Motion Contract + Copywriting Contract; skeletons only on initial/delayed load)
  </read_first>
  <acceptance_criteria>
    - Fast Population/Filter/Sort/Search changes keep the current rows visible and update in place — no skeleton flash on each query change; skeleton rows appear only on initial load or a meaningfully delayed load and match row geometry.
    - Result transitions are gated by `useReducedMotion()`: animated (short position/fade via MOTION.fast/base + EASING.decelerate) when motion is allowed; instant apply with no position animation when reduced.
    - The list mode reuses the shared `selectDashboardEmptyState` host for first-run CTA, `No matches for "{term}"`, filtered-empty, everyone-hidden pointers, and the `Couldn't load your contacts` / `Pull down to try again.` error — identical semantics to Card View.
    - `npx tsc --noEmit` + `npm run check:colors` clean; `npm test` (full suite) green at wave merge.
  </acceptance_criteria>
  <action>
    Wire the list-mode result transitions through `useReducedMotion()`: keep current content visible on fast query changes and apply a short restrained enter/position transition (MOTION.fast/base + EASING.decelerate) only when motion is allowed, falling back to instant apply under reduced motion — never a skeleton flash per query change. Reserve skeleton rows (matching row geometry) for initial/meaningfully-delayed load. Route the list-mode empty/error/loading states through the existing shared `selectDashboardEmptyState` host so the cause-aware copy is identical to Card View (reuse verbatim; do not re-author strings). Keep all reads on the local shared model — no network on the read path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: change Population/Filters/Sort/Search quickly — rows update in place with a restrained transition and no skeleton flash; enable OS reduced motion and confirm transitions become instant; force zero results / a load error and confirm the shared empty/error copy matches Card View; initial load shows a row-shaped skeleton.</human-check>
  </verify>
  <done>Fast query changes update in place with reduced-motion-aware restrained transitions, skeletons only on initial/delayed load, and shared cause-aware empty/error states; tsc + check:colors + full suite green.</done>
  <reversibility rating="reversible">Screen-level transition/host wiring; revertable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search term → shared local read (`listDashboardSearch`) | untrusted term crosses into SQL; must stay parameterized in the shared read |
| shared search result model → rendered snippet | must render only the shared read's already-excluded snippet; no UI re-query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-10 | Tampering | search term → listDashboardSearch | high | mitigate/accept | The term is `?`-bound + `escapeLike`-escaped IN the shared read (dashboard-read.ts:411-420); the List does NOT re-implement query logic, so no new injection surface is added — the control is inherited from Phase 25. |
| T-27-11 | Information disclosure | search snippet rendering | medium | mitigate | Snippets come from the shared read, which applies RANKED_FUEL_EXCLUSIONS / off-limits / hidden exclusions in-query; the renderer adds no `.filter()` that could resurface excluded content. |
| T-27-12 | Information disclosure | read path network | low | accept | No fetch anywhere on the read path (local-first, CLAUDE.md); the row render and search read are pure local SQLite. No mitigation needed beyond preserving the no-network posture. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase; no supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/components/list-row-content.ts` — `formatMatchExplanation()` (pluralised count + categories + `+N more`; pure, node-tested).
- `src/components/ListRow.tsx` — search-mode rendering (name fixed; lines 2–3 → match explanation + weight-highlighted strongest snippet).
- `src/screens/HomeScreen.tsx` — reduced-motion-aware in-place result transitions + shared empty/error/loading host wiring for list mode.

<verification>
- `npx vitest run src/components/list-row-content.test.ts` green; `npx tsc --noEmit` + `npm run check:colors` clean; `npm test` full suite green at wave merge.
- Device UAT (end-of-phase): search rendering, in-place transitions, reduced-motion instant apply, shared empty/error/skeleton states.
</verification>

<success_criteria>
Search keeps identity stable while showing a compact match explanation + highlighted strongest snippet; fast query changes update in place with restrained reduced-motion-aware motion; empty/error/loading semantics match Card View — all over the shared local read model with no network.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-06-SUMMARY.md` when done.
</output>
