---
phase: 27-dashboard-list-view
plan: 05
type: execute
wave: 4
depends_on: [27-02, 27-04]
files_modified:
  - src/services/quick-log-command.ts
  - src/services/quick-log-command.test.ts
  - src/components/UniversalFab.tsx
  - src/screens/HomeScreen.tsx
  - src/components/ListRow.tsx
autonomous: true
requirements: [LISTV-07, LISTV-08, LISTV-09]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LISTV-07 / E4 populated: each row is wrapped in `ReanimatedSwipeable`; progressive reveal (row translates → action surface behind → semantic action icon → threshold/resistance communicates commit); the action executes ON COMMIT (onSwipeableOpen), never on a second tap of a revealed button"
    - "LISTV-07 / E4 partial: exactly one row is swipe-revealed at a time (a list-level open-row ref closes the previous on any row's will-open); tapping a partially-swiped row CLOSES the swipe rather than navigating; a closed row's tap opens the Profile"
    - "LISTV-07 / E4 error: no destructive swipe actions; a sub-threshold release returns the row; the routed logging flow owns its own failure handling"
    - "LISTV-07: right swipe reveals the logging action, left swipe reveals Edit Contact; both commit-execute (no revealed-button second tap)"
    - "LISTV-08: on right-swipe commit the row reads `dashboardRightSwipeAction` (from getAppSettings) and runs Quick Log or Log Contact accordingly; default Quick Log"
    - "LISTV-07/08 single-sourced Quick Log (review MED #5): the swipe Quick Log does NOT duplicate `recordTouchpoint()`. `UniversalFab.logContact`'s behaviour (single-flight guard, undo controller, success/error snackbar with Undo/Retry, Light haptic, `notifyWidgetDataChanged` + `bumpShellRefresh`) is EXTRACTED into a shared `runQuickLog` command reused by BOTH the FAB and the List swipe, so Undo/retry/haptic/refresh stay identical and single-sourced (no forked capture path)"
    - "LISTV-07 detailed logging + edit routing (review MED #5): Log Contact and Edit Contact route through the SAME root-navigation dispatch the FAB uses (`navigationRef` → `DashboardTab` screen+params, UniversalFab.tsx:290-315), never a local `navigation.navigate` assumption from HomeScreen"
    - "LISTV-09 / D-10: the row exposes `accessibilityActions` equivalent to the gestures — Log Interaction and Edit Contact — so AT users never need to swipe (colour-free row description covering name/category/recency/favourite/status-or-snooze is authored in 27-04)"
    - "Swipe translation runs on the worklet/UI thread inside ReanimatedSwipeable; commit callbacks run on the JS thread (safe for nav/DAO); translation is NEVER mirrored into React state (CLAUDE.md)"
  artifacts:
    - "src/services/quick-log-command.ts — `runQuickLog(...)` shared Quick Log command (single-flight guard + undo controller + snackbar Undo/Retry + Light haptic + notifyWidgetDataChanged + bumpShellRefresh), extracted from UniversalFab.logContact so FAB and swipe share one truthful capture path"
    - "src/components/UniversalFab.tsx — `logContact` refactored to a thin caller of `runQuickLog` (behaviour unchanged)"
    - "src/screens/HomeScreen.tsx — ReanimatedSwipeable host with a list-level openRowRef single-open coordinator, right/left renderActions, onSwipeableOpen commit routing (reads dashboardRightSwipeAction; Quick Log via runQuickLog; Log Contact/Edit via the root-nav dispatch), tap-close gating, and Log/Edit handlers shared with a11y actions"
    - "src/components/ListRow.tsx — accessibilityActions (Log Interaction, Edit Contact) on the row Pressable + onAccessibilityAction wiring to the same handlers"
  key_links:
    - "UniversalFab.logContact + List swipe Quick Log → shared `runQuickLog` command — one single-flight/undo/snackbar/haptic/refresh path, never a forked recordTouchpoint (review MED #5)"
    - "getAppSettings.dashboardRightSwipeAction (27-02) → onSwipeableOpen('right') → runQuickLog | root-nav Log Contact routing — the configured action executes on commit"
    - "openRowRef (SwipeableMethods.close()) → single-open coordination + tap-to-close — one revealed row at a time"
    - "shared Log/Edit handlers (root-nav dispatch) → both swipe commit AND accessibilityActions — gesture and AT parity"
  prohibitions:
    - "no per-contact swipe configuration (the preference is global)"
    - "no additional swipe actions beyond the two"
    - "no multi-button swipe drawers"
    - "no destructive swipe actions (no Delete/Archive on swipe)"
    - "no permanent in-List gesture tutorial (onboarding owns gesture education)"
    - "never drive swipe translation from React state (worklet thread only)"
    - "swipe action surfaces use their own semantic tokens, never accent"
---

<objective>
Wire the two non-destructive swipe gestures and their accessible equivalents: right swipe executes the globally configured logging action (Quick Log or Log Contact, read from the 27-02 preference) on gesture commit, left swipe routes to Edit Contact, exactly one row is revealed at a time, tapping a partially-swiped row closes it, and AT users get equivalent row actions.

Purpose: LISTV-07 (swipe/tap-close/single-open), LISTV-08 (configured action executes on commit), LISTV-09 (accessibility actions). This is the phase's single biggest correctness risk — the worklet/JS-thread boundary must be respected.
Output: the ReanimatedSwipeable host on HomeScreen + accessibilityActions on the row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-RESEARCH.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/screens/HomeScreen.tsx
@src/components/ListRow.tsx
@src/db/app-settings-dao.ts
@src/components/icons/icon-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared Quick Log command (single-source the capture path)</name>
  <files>src/services/quick-log-command.ts, src/services/quick-log-command.test.ts, src/components/UniversalFab.tsx</files>
  <read_first>
    - src/components/UniversalFab.tsx:120-296 (the CURRENT private Quick Log path to lift verbatim: `quickLogPending` single-flight ref at :126/:230, `quickLogUndoController` at :127-135, `undoQuickLog` at :198-226, `logContact` at :228-279 (recordTouchpoint → success snackbar with Undo + `notifyWidgetDataChanged` + `bumpShellRefresh` + success haptic; catch → error snackbar with Retry; finally clears the pending guard), `selectPickerContact`/`dispatchAction` root-nav routing at :281-319)
    - src/services/ (the established business-logic/service home; recordTouchpoint/deleteTouchpoint/createQuickLogUndoController already live in the services layer — VERIFY the exact import paths on disk)
    - src/db/interactions or the touchpoint writer module (recordTouchpoint signature: contactId, uid, occurredAt, now, channel:'unspecified', direction:'outbound', connected:1, quality:null, source:'manual')
  </read_first>
  <behavior>
    - `runQuickLog({ contactId })` (or an equivalently-named shared command) performs the SAME work `UniversalFab.logContact` does today: a module-scoped single-flight guard (a second call while one is in flight is ignored), `recordTouchpoint(...)` with the current-time stamp, a success snackbar carrying Undo (wired to the shared undo controller / `deleteTouchpoint`), a Light-then-Success haptic per the current idiom, `notifyWidgetDataChanged()` + `bumpShellRefresh()`, and an error snackbar carrying Retry on failure — behaviour identical to the pre-refactor FAB path.
    - `UniversalFab.logContact` becomes a thin caller of `runQuickLog`; no behaviour change observable from the FAB.
    - The command is the SINGLE source of Quick Log capture behaviour; the List swipe (Task 2) calls the same command — no duplicated `recordTouchpoint()`.
  </behavior>
  <acceptance_criteria>
    - `src/services/quick-log-command.ts` exports a callable `runQuickLog(...)` owning the single-flight guard, undo controller, success/error snackbar (Undo/Retry), haptic, and `notifyWidgetDataChanged` + `bumpShellRefresh` — the behaviour previously inlined in `UniversalFab.logContact`.
    - `UniversalFab.tsx` calls `runQuickLog` instead of inlining `recordTouchpoint` + snackbar + undo; the FAB Quick Log behaviour is unchanged (single-flight, Undo, Retry, haptic, refresh).
    - `npx vitest run src/services/quick-log-command.test.ts` passes: a success path records once + shows Undo + refreshes; a second call while pending is ignored (single-flight); a failed write shows Retry and clears the pending guard; Undo deletes the touchpoint and refreshes.
    - `npx tsc --noEmit` + `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Extract `UniversalFab.logContact`'s body (and its `quickLogPending` single-flight guard, `quickLogUndoController`, and `undoQuickLog`) into a shared `runQuickLog` command in `src/services/quick-log-command.ts` (confirm the established shell/services home on disk). The command owns single-flight, `recordTouchpoint`, the success snackbar with Undo (via the shared undo controller / `deleteTouchpoint`), the Light/Success haptic, `notifyWidgetDataChanged` + `bumpShellRefresh`, and the error snackbar with Retry — verbatim behaviour, just relocated. Rewire `UniversalFab.logContact` to call `runQuickLog`. Do NOT change the FAB's observable behaviour. Add `quick-log-command.test.ts` covering success/single-flight/failure-retry/undo. This exists so Task 2's swipe Quick Log reuses ONE truthful capture path (review MED #5), never a forked `recordTouchpoint`.
  </action>
  <verify>
    <automated>npx vitest run src/services/quick-log-command.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: FAB Quick Log still works end-to-end — one write, success snackbar with Undo, success haptic, dashboard + widget refresh; Undo removes the interaction; a failed write shows Retry. Behaviour matches the pre-refactor FAB.</human-check>
  </verify>
  <done>The Quick Log capture path is single-sourced in `runQuickLog`; the FAB is a thin caller with unchanged behaviour; the command is unit-tested; tsc + check:colors clean.</done>
  <reversibility rating="reversible">Behaviour-preserving extraction + caller rewire; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Swipe host — ReanimatedSwipeable, single-open, commit routing</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - .planning/phases/27-dashboard-list-view/27-RESEARCH.md (Pattern 1: ReanimatedSwipeable execute-on-commit; renderLeftActions/renderRightActions convention — renderLeftActions is revealed by a RIGHTWARD swipe; onSwipeableWillOpen/onSwipeableOpen(direction); friction/leftThreshold/rightThreshold/overshoot*={false}; list-level openRowRef pattern; worklet/JS-thread boundary)
    - .planning/phases/27-dashboard-list-view/27-PATTERNS.md (No Analog Found — no Swipeable usage in src; GestureHandlerRootView already mounted at App.tsx:408; commit callbacks on JS thread)
    - src/screens/HomeScreen.tsx (the list-mode renderItem from 27-01/04; the freshness/reload effects; note HomeScreen currently uses `navigation.navigate("Profile", ...)` for Profile — but detailed Log Contact/Edit must use the root-nav dispatch, below)
    - src/services/quick-log-command.ts (Task 1: `runQuickLog` — the shared Quick Log capture path the right-swipe Quick Log action MUST call; do NOT re-inline recordTouchpoint)
    - src/components/UniversalFab.tsx:290-319 (`selectPickerContact`/`dispatchAction` — the root-nav dispatch idiom for detailed Log Contact + Edit: `navigationRef.current?.navigate("DashboardTab", { screen, params: { contactId } })`; reuse this shape, not a local stack assumption)
    - src/db/app-settings-dao.ts (getAppSettings → dashboardRightSwipeAction from 27-02)
    - src/components/icons/icon-registry.ts (semantic action icon names: message/add/sparkle for logging, edit for Edit — action surfaces use their own semantic tokens, never accent)
  </read_first>
  <acceptance_criteria>
    - Each list row is wrapped in `ReanimatedSwipeable` with `renderLeftActions` (revealed by rightward swipe → logging action) and `renderRightActions` (revealed by leftward swipe → Edit), `overshootLeft={false}`/`overshootRight={false}`, and thresholds/friction set (exact values device-tuned, `[DEFERRED]`).
    - `onSwipeableOpen('right')` runs the configured logging action read from `getAppSettings().dashboardRightSwipeAction`: Quick Log calls the shared `runQuickLog(...)` command (Task 1 — NOT a re-inlined recordTouchpoint, review MED #5); Log Contact routes via the root-nav dispatch (`navigationRef` → `DashboardTab` screen+params, pre-targeted to the contact). `onSwipeableOpen('left')` routes to Edit Contact via the same root-nav dispatch. Neither requires a second tap.
    - A list-level `openRowRef` holds the currently-open row's `SwipeableMethods`; any row's will-open closes the previous ref (exactly one open at a time).
    - A closed row's `onPress` opens Profile; a partially-swiped row's `onPress` calls `.close()` and returns without navigating.
    - No `setState`/React-state write mirrors the swipe translation (grep the swipe host for per-frame state writes — translation stays inside ReanimatedSwipeable/worklets).
    - `npx tsc --noEmit` + `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Wrap the list-mode row in `ReanimatedSwipeable`. Render the logging-action surface via `renderLeftActions` (rightward swipe) and the Edit surface via `renderRightActions` (leftward swipe), using semantic action icons from the registry and their own semantic tokens (not accent), with `overshoot*={false}` and device-deferred thresholds/friction. On `onSwipeableOpen('right')`, read `dashboardRightSwipeAction` via `getAppSettings`; if Quick Log, call the shared `runQuickLog({ contactId })` command from Task 1 (never a re-inlined `recordTouchpoint` — review MED #5); if Log Contact, route via the root-nav dispatch (`navigationRef` → `DashboardTab` screen+params, pre-targeted to the contact, mirroring UniversalFab.tsx:290-315). On `onSwipeableOpen('left')`, route to Edit Contact via the same root-nav dispatch — both on commit, no revealed-button second tap. Maintain a list-level `openRowRef` to the open row's `SwipeableMethods` and close the previous on any will-open (single-open). Gate the row `onPress`: if this row is the open one, close it and return; otherwise open the Profile. Keep all translation on the worklet thread inside ReanimatedSwipeable — never mirror it into React state. Extract the Quick Log / Log Contact / Edit routing into shared handlers Task 3's accessibility actions reuse. No destructive actions, no per-contact config, no tutorial.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel (input long-press-swipe per the adb input taxonomy, or by hand): swipe a row right → the configured logging action fires on commit; change the preference and confirm the other action fires; swipe left → Edit Contact opens; open one row then swipe another → the first closes; tap a partially-open row → it closes without navigating; a closed-row tap opens Profile. Scroll performance stays smooth (worklet-driven).</human-check>
  </verify>
  <done>Both gestures execute on commit, one row opens at a time, tap-closes-then-navigates correctly, the right action honours the global preference, and translation never touches React state; tsc + check:colors clean.</done>
  <reversibility rating="reversible">Screen-level gesture wiring; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Accessibility actions — gesture parity for AT users</name>
  <files>src/components/ListRow.tsx</files>
  <read_first>
    - src/components/ListRow.tsx (the row Pressable + accessibilityLabel from 27-04)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Accessibility Contract — row actions mirror the gestures: Log Interaction and Edit Contact)
    - src/screens/HomeScreen.tsx (the shared Log/Edit handlers wired in Task 2 — Quick Log via `runQuickLog`, Log Contact/Edit via root-nav dispatch — passed as props)
  </read_first>
  <acceptance_criteria>
    - The row Pressable declares `accessibilityActions` including `Log Interaction` and `Edit Contact`, wired via `onAccessibilityAction` to the SAME handlers the swipe commit uses (passed as props from HomeScreen).
    - The favourite star remains a separately-exposed toggle; the status glyph remains decorative (state carried by the row description) — no double-announcement.
    - `npx tsc --noEmit` + `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Add `accessibilityActions` (Log Interaction, Edit Contact) and an `onAccessibilityAction` dispatcher to the row Pressable, wired to the shared Log/Edit handlers passed from HomeScreen (the same functions the swipe commit calls). Keep the favourite star's toggle exposure and the decorative glyph unchanged. Present as props so ListRow stays presentational.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel with TalkBack: the row offers "Log Interaction" and "Edit Contact" as accessibility actions that perform the same routing as the swipes; the star toggles favourite; the glyph is not separately announced.</human-check>
  </verify>
  <done>AT users can invoke Log Interaction and Edit Contact without swiping, via the same handlers as the gestures; tsc + check:colors clean.</done>
  <reversibility rating="reversible">Presentational a11y additions; revertable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored preference (`dashboardRightSwipeAction`) → swipe commit routing | the read value selects which local flow runs; must be a validated union value |
| gesture commit → navigation/DAO (JS thread) | routing crosses into local nav/logging flows only (no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08b | Tampering | right-swipe action routing | low | mitigate | The action value is read via `getAppSettings`, which is constrained by the CHECK constraint + `assertDashboardRightSwipeAction` union (27-02); the router switches only over the closed `RIGHT_SWIPE_ACTIONS` set, defaulting to Quick Log. |
| T-27-09 | Denial of service (jank) | swipe translation thread | medium | mitigate | Translation stays on the worklet/UI thread inside ReanimatedSwipeable; no per-frame `setState`, honouring the CLAUDE.md no-animation-from-React-state rule (JS-thread jank avoidance). |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase (ReanimatedSwipeable ships in the already-present gesture-handler 2.32.0); no supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/services/quick-log-command.ts` — `runQuickLog(...)` shared Quick Log command (single-flight + undo controller + snackbar Undo/Retry + haptic + widget/shell refresh), extracted from `UniversalFab.logContact` (review MED #5).
- `src/components/UniversalFab.tsx` — `logContact` refactored to a thin `runQuickLog` caller (behaviour unchanged).
- `src/screens/HomeScreen.tsx` — `ReanimatedSwipeable` host: `openRowRef` single-open coordinator, right/left action surfaces, `onSwipeableOpen` commit routing (reads `dashboardRightSwipeAction`; Quick Log via `runQuickLog`; Log Contact/Edit via root-nav dispatch), tap-close gating, shared Log/Edit handlers.
- `src/components/ListRow.tsx` — `accessibilityActions` (Log Interaction, Edit Contact) + `onAccessibilityAction` wiring.

<verification>
- `npx vitest run src/services/quick-log-command.test.ts` green (success / single-flight / failure-retry / undo).
- `npx tsc --noEmit` + `npm run check:colors` clean.
- Device UAT: FAB Quick Log behaviour unchanged after the extraction; both gestures commit-execute; single-open; tap-close; right Quick Log runs via the shared command; Log Contact/Edit route via root-nav; right action honours the preference; a11y actions match the gestures; smooth scroll.
- Grep confirms no per-frame React-state write mirrors the swipe translation, and no re-inlined `recordTouchpoint` in the swipe path (single-sourced via `runQuickLog`).
</verification>

<success_criteria>
Right swipe runs the configured logging action and left swipe opens Edit — both on commit, one row open at a time, tap-closes-first, worklet-driven, with accessible equivalents — no destructive actions and no per-contact config.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-05-SUMMARY.md` when done.
</output>
