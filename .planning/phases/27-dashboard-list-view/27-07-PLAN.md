---
phase: 27-dashboard-list-view
plan: 07
type: execute
wave: 6
depends_on: [27-06]
files_modified:
  - src/logic/favourite-optimistic.ts
  - src/logic/favourite-optimistic.test.ts
  - src/screens/HomeScreen.tsx
autonomous: true
gap_closure: true
requirements: [LISTV-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After a rapid set-favourite success followed by a latest clear-favourite failure, the List row renders favourite and SQLite still stores a non-null favourite_rank."
    - "A pending optimistic choice remains visible across reloads, while every successful per-contact write updates the screen base even if a newer choice is pending."
  artifacts:
    - path: src/logic/favourite-optimistic.ts
      provides: "Per-contact reconciliation that distinguishes a committed base from the latest optimistic overlay."
    - path: src/logic/favourite-optimistic.test.ts
      provides: "Deferred-write, real-SQLite regression coverage for older-success/latest-failure settlement."
    - path: src/screens/HomeScreen.tsx
      provides: "Screen handler that applies every successful durable membership commit without broad reloads."
  key_links:
    - from: "setFavouriteRank / clearFavouriteRank settlement"
      to: "HomeScreen rows and optimistic overlay"
      via: "per-contact reconciliation result plus functional base-row patch"
      pattern: "older success commits base while newer overlay remains; latest failure clears only the overlay"
---

<objective>
Close LISTV-04's rapid-toggle reconciliation gap without changing the binary-favourite or Dashboard reload contracts.

Purpose: A successful earlier SQLite write must remain represented after a newer intent fails, rather than requiring a later reload to repair the row.
Output: A per-contact committed-base reconciliation model, a HomeScreen settlement path, and a real SQLite deferred-write regression test.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dashboard-list-view/27-VERIFICATION.md
@.planning/phases/27-dashboard-list-view/27-04-SUMMARY.md
@.planning/phases/27-dashboard-list-view/27-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile per-contact committed favourite membership through deferred writes</name>
  <files>src/logic/favourite-optimistic.ts, src/logic/favourite-optimistic.test.ts</files>
  <read_first>
    - src/logic/favourite-optimistic.ts (the current generation-only overlay drops an older success as stale and has no committed-base state)
    - src/logic/favourite-optimistic.test.ts (existing pure store contracts to extend, not replace)
    - src/db/favourites-dao.ts:29-76 (the only List write APIs: binary set/clear use the write transaction and throw when no row changes; neither triggers HomeScreen reload)
    - src/db/favourites-dao.test.ts:18-67 (node:sqlite migration and seeded-contact fixture idiom)
    - src/db/database.ts (MIGRATIONS and TARGET_VERSION for the current real-schema test fixture)
    - .planning/phases/27-dashboard-list-view/27-VERIFICATION.md (the required older-success/latest-failure sequence)
  </read_first>
  <behavior>
    - Starting false, begin set-to-true then begin clear-to-false for the same contact; settle the actual set write successfully while clear remains pending: effective rendered membership remains false from the newest overlay, but the reconciler records true as the committed base.
    - Reject the latest clear write: its overlay is removed and the reconciler's effective membership becomes true; a real SQLite read of the seeded contact has non-null favourite_rank.
    - A stale failure never removes a newer overlay; a success for any generation records its durable membership even when that generation is no longer the latest.
    - Different contact IDs retain independent committed/optimistic state.
  </behavior>
  <action>Per D-04, retain binary membership only: extend the per-contact optimistic controller so it owns or returns the authoritative committed base membership separately from the latest overlay. Preserve `useSyncExternalStore`-safe immutable snapshots and the existing per-contact generation protection, but do not discard a successful older generation merely because a newer intent is pending. Its resolution contract must let the screen update the base for every successful `setFavouriteRank`/`clearFavouriteRank` settlement while leaving a newer overlay visible; a latest failure clears only that overlay so the last confirmed base is rendered. Write the regression first in the existing test file with the repository's node:sqlite executor, `MIGRATIONS`, and `TARGET_VERSION`: seed one contact, coordinate deferred promises so the real set DAO completes before the latest clear path rejects, then assert both the reconciled effective membership and persisted `favourite_rank` remain favourite. Do not introduce rank ordering, `listFavourites`, a global pending flag, or a reload as race recovery.</action>
  <verify>
    <automated>npx vitest run src/logic/favourite-optimistic.test.ts</automated>
  </verify>
  <done>The tested set-success/clear-failure sequence leaves both the effective List membership and the real SQLite row favourite, while a newer pending choice and a different contact remain isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply every durable settlement to the HomeScreen base without broad reloads</name>
  <files>src/screens/HomeScreen.tsx, src/logic/favourite-optimistic.ts, src/logic/favourite-optimistic.test.ts</files>
  <read_first>
    - src/screens/HomeScreen.tsx:706-756 (current `toggleFavourite`; it patches rows only when a success resolves as latest)
    - src/screens/HomeScreen.tsx:282-336 (reload replaces the complete Dashboard row base, while an overlay must continue to win during a pending write)
    - src/logic/favourite-optimistic.ts and src/logic/favourite-optimistic.test.ts (Task 1's precise committed-base/overlay API and regression)
    - src/db/favourites-dao.ts:29-76 (writes are serialized by `inWriteTransaction`, bind their values, and do not call shell-refresh)
    - .planning/phases/27-dashboard-list-view/27-04-PLAN.md (D-04's existing binary, reactive-overlay, and no-ranked-UX constraints)
  </read_first>
  <behavior>
    - The screen uses the reactive overlay when present and the rows' binary favourite base otherwise.
    - An older successful set patches the functional `rows` base even while a latest clear overlay is still visible; when that latest clear fails, rendering reveals the patched favourite base.
    - A latest write failure retains the existing error snackbar and Retry action; success still produces no success snackbar and fires the immediate light haptic at intent time.
  </behavior>
  <action>Consume Task 1's reconciliation result in `toggleFavourite`. Keep D-04's immediate reactive optimistic flip, light haptic, binary `setFavouriteRank`/`clearFavouriteRank` calls, failure notification, and Retry behavior. On every successful durable write, use a functional `setRows` update with `applyCommittedMembership` to commit that generation's membership to the row base even if it is stale relative to a newer overlay; leave any newer overlay installed until its own settlement. On a latest failure, remove only the latest overlay so the already-patched older base becomes visible; stale failures remain non-disruptive. Preserve the existing cancellation-aware reload behavior and rendered `overlay ?? (favourite_rank !== null)` derivation. Do not trigger a broad reload, alter Dashboard ordering, surface rank semantics, or make the write state global: the DAO's transaction serialization and the controller's per-contact state are the intended API constraints.</action>
  <verify>
    <automated>npx vitest run src/logic/favourite-optimistic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>HomeScreen preserves D-04's immediate binary interaction and error feedback, and the deferred older-success/latest-failure test proves that rendered and persisted membership reconcile without a reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| List tap → SQLite favourite DAO | A user intent is translated into a durable contact update and then returned to UI state. |
| SQLite write settlement → List base | Async completion order crosses from serialized persistence into per-contact rendered state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-07-01 | Tampering | favourite settlement reconciliation | medium | mitigate | Maintain a committed base per contact and test the older-success/latest-failure sequence against real SQLite. |
| T-27-07-02 | Denial of service | rapid taps | low | accept | Existing per-contact generation handling and DAO transaction serialization bound each action to a binary write; no new global queue is added. |
| T-27-07-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Run the focused deferred-write regression, then TypeScript and the repository colour-token check. The test must use the real favourites DAO and query the same SQLite row after the forced latest failure.
</verification>

<success_criteria>
The proven rapid sequence is: initial non-favourite → actual set succeeds → latest clear fails → row membership is favourite and persisted `favourite_rank` is non-null, without a dashboard reload. Other contacts and newer overlays are unaffected.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-07-SUMMARY.md` when done.
</output>
