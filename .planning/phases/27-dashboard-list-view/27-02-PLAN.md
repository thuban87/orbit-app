---
phase: 27-dashboard-list-view
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/db/migrations/020-dashboard-swipe-pref.ts
  - src/db/migrations/020-dashboard-swipe-pref.test.ts
  - src/db/database.ts
  - src/logic/dashboard-query-logic.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
autonomous: false
requirements: [LISTV-08]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LISTV-08: migration 020 adds `app_settings.dashboard_right_swipe_action TEXT NOT NULL DEFAULT 'quick-log' CHECK(dashboard_right_swipe_action IN ('quick-log','log-contact'))`; a v-any→v20 forward jump seeds the singleton row with 'quick-log' (default Quick Log, D-09/O)"
    - "LISTV-08: `getAppSettings` returns `dashboardRightSwipeAction`; `updateAppSettings` round-trips it and rejects any non-union value via `assertDashboardRightSwipeAction` before any UPDATE (CHECK constraint is the second line of defence). Writability requires the OPTIONAL key on `PortableSettingsSnapshot` (AppSettingsPatch derives from it) — adding it only to `AppSettings`/`AppSettingsRow`/`COLUMN_OF` would NOT make it patchable (review MED #2)"
    - "LISTV-08: `dashboardRightSwipeAction` is allowlisted in `PORTABLE_SETTINGS_KEYS` so `assertPortableSettings` accepts it — allowlist ONLY (no `getPortableSettingsSnapshot` emit, no BACKUP_FORMAT_VERSION bump, no FORWARD_MIGRATIONS entry — that is Phase 36, matching the Phase 25 precedent)"
    - "D-03: TARGET_VERSION is bumped to 20 and `migration020` is registered in the MIGRATIONS array in order; head+1=020 verified on disk (fold-into-Phase-25 foreclosed — 019 already shipped); a shipped migration is never edited"
  artifacts:
    - "src/db/migrations/020-dashboard-swipe-pref.ts — migration020 (version 20)"
    - "src/db/database.ts — TARGET_VERSION = 20 + migration020 registered in MIGRATIONS"
    - "src/logic/dashboard-query-logic.ts — RIGHT_SWIPE_ACTIONS closed union + RightSwipeAction type (mirrors DASHBOARD_SORT_MODES)"
    - "src/db/app-settings-dao.ts — dashboardRightSwipeAction wired through the FULL writable-key set (review MED #2): AppSettings (read type, :51) + `dashboardRightSwipeAction?: RightSwipeAction` OPTIONAL on PortableSettingsSnapshot (:218-224, mirroring `dashboardSort?`) from which AppSettingsPatch derives (:229-231) + AppSettingsRow (:290) + WritableSettingsKey (:255) + COLUMN_OF (:368) + getAppSettings SELECT/row map + validateAppSettingsPatch guard + assertDashboardRightSwipeAction"
    - "src/backup/backup-schema.ts — 'dashboardRightSwipeAction' added to PORTABLE_SETTINGS_KEYS (allowlist only)"
  key_links:
    - "migration020 CHECK constraint ↔ assertDashboardRightSwipeAction validator ↔ RIGHT_SWIPE_ACTIONS union — three redundant guards on the same closed value set, enforced on the `updateAppSettings` WRITE path"
    - "PORTABLE_SETTINGS_KEYS allowlist → assertPortableSettings (rejects non-allowlisted KEYS) — allowlist must precede any future format-N backup that carries the key. NOTE (review LOW #3): `assertPortableSettings` performs KEY allowlisting only, NOT value validation for dashboard keys; a tampered value is not restore-revalidated until Phase 36 actually emits/consumes the key."
  prohibitions:
    - "do not bump BACKUP_FORMAT_VERSION (Phase 36 owns the closing bump)"
    - "do not emit dashboardRightSwipeAction in getPortableSettingsSnapshot this phase"
    - "do not add a FORWARD_MIGRATIONS entry this phase"
    - "do not store this preference in AsyncStorage (durable app_settings column only — R-16)"
    - "do not edit an already-shipped migration (001–019); add 020 only"
---

<objective>
Ship the durable, backup-portable global right-swipe logging preference (Quick Log vs Log Contact, default Quick Log) as an `app_settings` column via standalone migration 020, its DAO read/write/validate plumbing, and its backup allowlist entry. This phase owns storage + read only; the Settings UI is Phase 37's and the onboarding early choice is Phase 39's.

Purpose: LISTV-08 requires the swipe action (wired in 27-05) to read a durable preference. The fold-into-Phase-25 option is FORECLOSED — migration 019 already shipped, so this is standalone migration 020 (head+1, verified on disk).
Output: migration 020 + `dashboard_right_swipe_action` column + `RIGHT_SWIPE_ACTIONS` union + app-settings DAO plumbing + `PORTABLE_SETTINGS_KEYS` allowlist entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-RESEARCH.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/db/migrations/019-dashboard-prefs.ts
@src/db/database.ts
@src/db/app-settings-dao.ts
@src/backup/backup-schema.ts
@src/logic/dashboard-query-logic.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Ship standalone migration 020 as an irreversible one-way schema change on `app_settings` — confirm its shape before it is written.</decision>
  <context>
Migration 020 is IRREVERSIBLE in production. Per CLAUDE.md there is no remote access to a user's database, migrations are forward-only and run in strict order, and anything a migration gets wrong is permanent for that user. Any user may jump from any prior version straight to v20 in one update. Verified on disk this session: `ls src/db/migrations/` head is `019-dashboard-prefs.ts`; `src/db/database.ts:54` `TARGET_VERSION = 19` — so head+1 = 020 and the fold-into-Phase-25 option is foreclosed (019 already shipped). The executor MUST re-verify head+1 on disk at execution time (numbers drift every schema phase) and use the number it observes. `blocking-human`: this one-way door is never auto-approved.
  </context>
  <options>
    <option id="ship-as-specified">
      <name>Ship migration 020 as specified</name>
      <pros>Matches the verified 019/002 additive-column pattern: `dashboard_right_swipe_action TEXT NOT NULL DEFAULT 'quick-log' CHECK(dashboard_right_swipe_action IN ('quick-log','log-contact'))`; `NOT NULL DEFAULT 'quick-log'` seeds the singleton row cleanly on any v-any→v20 jump; no backup-format bump (Phase 36 owns the closing bump), allowlist-now/emit-later matches the Phase 25 precedent.</pros>
      <cons>Column name, default, and CHECK value set become permanent for every user who runs it — a later change requires a new forward-only migration, never an edit.</cons>
    </option>
    <option id="correct-the-shape">
      <name>Correct the column name / default / CHECK values / number before shipping</name>
      <pros>Any naming or value-set concern is fixed while it is still cheap (pre-ship).</pros>
      <cons>Deviates from the verified established pattern; must stay a closed union aligned with `RIGHT_SWIPE_ACTIONS` and default Quick Log (D-09/O).</cons>
    </option>
  </options>
  <resume-signal>Select `ship-as-specified` to proceed, or `correct-the-shape` and state the corrected column name / default / CHECK values / migration number.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 020 — additive right-swipe preference column</name>
  <files>src/db/migrations/020-dashboard-swipe-pref.ts, src/db/migrations/020-dashboard-swipe-pref.test.ts, src/db/database.ts</files>
  <read_first>
    - src/db/migrations/019-dashboard-prefs.ts (the exact additive ALTER TABLE app_settings pattern to mirror; Migration/MigrationDeps/SqlExecutor imports)
    - src/db/migrations/002-app-settings.ts (original app_settings column idiom)
    - src/db/database.ts (TARGET_VERSION at :54; MIGRATIONS array at :57-76; migration imports at :40-43; runMigrations call at :153)
    - src/db/migrations/019-dashboard-prefs.test.ts (forward-jump + column + default test shape to mirror)
  </read_first>
  <behavior>
    - After migration 020, `PRAGMA table_info(app_settings)` includes `dashboard_right_swipe_action`.
    - The singleton app_settings row reads `dashboard_right_swipe_action = 'quick-log'` after a GENUINELY-EARLY forward jump: build the DB at the version where `app_settings` first exists (migration 002), seed/populate the singleton row THERE, then run migrations forward to 20 — exercising the real v-any→v20 path a long-dormant user takes, NOT a freshly-bootstrapped latest-schema fixture that only ever sees the column already present (review LOW #11).
    - An attempt to write a value outside ('quick-log','log-contact') is rejected by the CHECK constraint.
  </behavior>
  <acceptance_criteria>
    - `src/db/migrations/020-dashboard-swipe-pref.ts` exports `migration020` with `version: 20`; it is imported and listed in the `MIGRATIONS` array in `database.ts` after `migration019`; `TARGET_VERSION` is `20`.
    - `npx vitest run src/db/migrations/020-dashboard-swipe-pref.test.ts` passes and asserts: `PRAGMA table_info(app_settings)` includes `dashboard_right_swipe_action`; the singleton row reads `'quick-log'` after a GENUINELY-EARLY forward jump — the fixture is built at the migration-002 era (where `app_settings` first exists) with its singleton row seeded THERE, then migrated forward to 20 (review LOW #11 — not merely a latest-bootstrap fixture); and an out-of-set value is rejected by the CHECK constraint.
    - No file under `src/db/migrations/` numbered 001–019 is modified (forward-only).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <action>
    Re-verify head+1 on disk first; if head is still `019`, create `src/db/migrations/020-dashboard-swipe-pref.ts` exporting `migration020: Migration` with `version: 20` and an `apply` that runs a single additive `ALTER TABLE app_settings ADD COLUMN dashboard_right_swipe_action TEXT NOT NULL DEFAULT 'quick-log' CHECK(dashboard_right_swipe_action IN ('quick-log','log-contact'))` inside the runner's per-step transaction. Register `migration020` in the `MIGRATIONS` array (in order, after `migration019`) and bump `TARGET_VERSION` to 20 in `database.ts`. Add `020-dashboard-swipe-pref.test.ts` asserting the column exists, the singleton seeds to `'quick-log'` after a GENUINELY-EARLY forward jump (build the fixture at the migration-002 era where `app_settings` first exists, seed its singleton row there, then run migrations forward to 20 — review LOW #11, exercising the real v-any→v20 path, not a latest-bootstrap fixture), and a CHECK violation on an out-of-set value. Forward-only; do not edit any shipped migration.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/020-dashboard-swipe-pref.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 020 exists, is registered, TARGET_VERSION=20; the column + default + CHECK are proven by a forward-jump test; tsc clean.</done>
  <reversibility rating="one-way">A shipped SQLite migration cannot be inspected, repaired, or rolled back on user devices (CLAUDE.md); the column and its default are permanent for every user who runs it. Gated by Task 1.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preference DAO read/write/validate + backup allowlist</name>
  <files>src/logic/dashboard-query-logic.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <read_first>
    - src/logic/dashboard-query-logic.ts (DASHBOARD_SORT_MODES closed-union idiom at :118-126 to mirror)
    - src/db/app-settings-dao.ts (the FULL dashboardSort precedent to mirror end-to-end: `AppSettings` read type at :51; `PortableSettingsSnapshot` with the deferred `dashboardSort?` OPTIONAL key at :224; `AppSettingsPatch = Partial<Omit<PortableSettingsSnapshot,"modifiedAt">>` at :229-231 (derives — do NOT try to add a key to it directly); `WritableSettingsKey` at :255; `AppSettingsRow` at :290; `COLUMN_OF` at :368; `getAppSettings` SELECT at :417; the `assertDashboardSort` validator + `validateAppSettingsPatch`; `updateAppSettings` at :880)
    - src/db/app-settings-dao.test.ts (round-trip + validator-rejection test shape)
    - src/backup/backup-schema.ts (PORTABLE_SETTINGS_KEYS at :132, "dashboardSort" allowlist entry at :172, assertPortableSettings guard at :201, allowlist-now/emit-later comments)
    - src/backup/backup-schema.test.ts (allowlist-acceptance test shape)
  </read_first>
  <behavior>
    - `RIGHT_SWIPE_ACTIONS` is a readonly closed union `['quick-log','log-contact']` with type `RightSwipeAction`.
    - `getAppSettings` returns `dashboardRightSwipeAction: RightSwipeAction` from the `dashboard_right_swipe_action` column.
    - `updateAppSettings({ dashboardRightSwipeAction: 'log-contact' })` persists and re-reads as 'log-contact'; a non-union value throws from `assertDashboardRightSwipeAction` before any UPDATE.
    - `assertPortableSettings` accepts an object carrying `dashboardRightSwipeAction`; a non-allowlisted sibling key is still rejected.
  </behavior>
  <acceptance_criteria>
    - `dashboard-query-logic.ts` exports `RIGHT_SWIPE_ACTIONS` as a readonly `['quick-log','log-contact']` tuple and a `RightSwipeAction` type derived from it.
    - `app-settings-dao.ts` adds `dashboardRightSwipeAction` end-to-end mirroring the `dashboardSort` precedent: `AppSettings` (read, required) + `PortableSettingsSnapshot` (`dashboardRightSwipeAction?: RightSwipeAction`, OPTIONAL — this is what makes it writable through the derived `AppSettingsPatch`, review MED #2) + `AppSettingsRow` (snake row) + `WritableSettingsKey` + `COLUMN_OF` (→ `dashboard_right_swipe_action`) + the `getAppSettings` SELECT + row map + the `validateAppSettingsPatch` guard; `assertDashboardRightSwipeAction` throws on any value not in `RIGHT_SWIPE_ACTIONS`.
    - A test asserts writability specifically: `updateAppSettings({ dashboardRightSwipeAction: 'log-contact' })` type-checks and round-trips (guards against the MED #2 trap where the key is added to `AppSettings` but not `PortableSettingsSnapshot`, silently non-patchable).
    - `backup-schema.ts` adds `"dashboardRightSwipeAction"` to `PORTABLE_SETTINGS_KEYS`; `BACKUP_FORMAT_VERSION` is unchanged and no `FORWARD_MIGRATIONS` entry is added.
    - `npx vitest run src/db/app-settings-dao.test.ts src/backup/backup-schema.test.ts` passes, covering a get/update round-trip, a validator rejection, and allowlist acceptance.
    - `npx tsc --noEmit` and `npm run check:colors` pass.
  </acceptance_criteria>
  <action>
    Add `RIGHT_SWIPE_ACTIONS` + `RightSwipeAction` to `dashboard-query-logic.ts` mirroring `DASHBOARD_SORT_MODES`. In `app-settings-dao.ts`, wire `dashboardRightSwipeAction` through the FULL writable-key set exactly as `dashboardSort` is wired (review MED #2): (1) `AppSettings` read type (required `RightSwipeAction`); (2) `PortableSettingsSnapshot` as an OPTIONAL `dashboardRightSwipeAction?: RightSwipeAction` (mirroring the deferred `dashboardSort?` at :224) — this is what flows into the derived `AppSettingsPatch` and makes it patchable; do NOT attempt to add a key to `AppSettingsPatch` itself (it is `Partial<Omit<PortableSettingsSnapshot,...>>`); (3) `AppSettingsRow` snake field; (4) `WritableSettingsKey`; (5) `COLUMN_OF` (`dashboardRightSwipeAction: "dashboard_right_swipe_action"`, alongside `dashboardSort`); (6) the `getAppSettings` SELECT + its row→object map; (7) the `validateAppSettingsPatch` guard calling a new `assertDashboardRightSwipeAction(field, v)` that throws unless `v` is in `RIGHT_SWIPE_ACTIONS`. In `backup-schema.ts`, add `"dashboardRightSwipeAction"` to `PORTABLE_SETTINGS_KEYS` after `"dashboardSort"` — allowlist ONLY; do not emit it in `getPortableSettingsSnapshot`, do not bump `BACKUP_FORMAT_VERSION`, do not add a `FORWARD_MIGRATIONS` entry (Phase 36). Extend `app-settings-dao.test.ts` (get/update round-trip, a writability assertion for the patch, validator rejection) and `backup-schema.test.ts` (allowlist acceptance).
  </action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts src/backup/backup-schema.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>getAppSettings/updateAppSettings round-trip the pref; the validator rejects out-of-union values; the portable key is allowlisted and accepted by assertPortableSettings with no format bump; tests + tsc + check:colors green.</done>
  <reversibility rating="reversible">DAO/union/allowlist code is fully revertable; the durable value it guards is governed by the (one-way) migration in Task 2.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restored backup file → `app_settings` preference | untrusted external value re-enters the DB at restore; must be validated + allowlisted |
| caller `updateAppSettings` patch → SQLite UPDATE | patch values must pass a closed-union validator before the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Tampering | dashboard_right_swipe_action write path | high | mitigate | Three redundant guards on the closed value set: CHECK constraint (migration 020), `assertDashboardRightSwipeAction` before any UPDATE, and `RIGHT_SWIPE_ACTIONS` union at compile time — all on the `updateAppSettings` WRITE path. (review LOW #3: value validation on the RESTORE path does not exist for this key until Phase 36 emits/consumes it — the CHECK constraint remains the durable backstop against an out-of-set value reaching the column.) |
| T-27-04 | Tampering | PORTABLE_SETTINGS_KEYS allowlist | medium | mitigate | Key is added to the allowlist so `assertPortableSettings` accepts the KEY (allowlisting only, not value validation for dashboard keys — review LOW #3); a non-allowlisted or secret-shaped sibling key remains rejected (SECRET_SHAPED_KEY + allowlist gate at backup-schema.ts:201). No credential/secret is added; emission/restore-validation is Phase 36's format bump. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase; no supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/db/migrations/020-dashboard-swipe-pref.ts` — `migration020` (version 20).
- `src/db/database.ts` — `TARGET_VERSION = 20`; `migration020` registered in `MIGRATIONS`.
- `src/logic/dashboard-query-logic.ts` — `RIGHT_SWIPE_ACTIONS` (`['quick-log','log-contact']`) + `RightSwipeAction`.
- `src/db/app-settings-dao.ts` — `AppSettings.dashboardRightSwipeAction` (read), `PortableSettingsSnapshot.dashboardRightSwipeAction?` (optional → derived `AppSettingsPatch`, review MED #2), `AppSettingsRow` snake field, `WritableSettingsKey` entry, `COLUMN_OF` entry (`dashboard_right_swipe_action`), `assertDashboardRightSwipeAction`, `getAppSettings` SELECT + row map, `validateAppSettingsPatch` guard.
- `src/backup/backup-schema.ts` — `"dashboardRightSwipeAction"` in `PORTABLE_SETTINGS_KEYS` (allowlist only).

<verification>
- `npx vitest run src/db/migrations/020-dashboard-swipe-pref.test.ts src/db/app-settings-dao.test.ts src/backup/backup-schema.test.ts` green.
- `npx tsc --noEmit` + `npm run check:colors` clean.
- On-device (end-of-phase UAT): a DEBUG build reads `user_version = 20` and `run-as` DB read shows the column defaulted to `'quick-log'` (device-UAT run-as pattern).
</verification>

<success_criteria>
The global right-swipe preference persists durably in `app_settings`, is validated at every entry, is backup-portable via the allowlist, and defaults to Quick Log — ready for 27-05 to read at gesture commit.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-02-SUMMARY.md` when done.
</output>
