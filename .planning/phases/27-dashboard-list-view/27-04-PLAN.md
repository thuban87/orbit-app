---
phase: 27-dashboard-list-view
plan: 04
type: execute
wave: 3
depends_on: [27-01, 27-03]
files_modified:
  - src/components/icons/StatusGlyph.tsx
  - src/components/list-row-content.ts
  - src/components/list-row-content.test.ts
  - src/components/ListRow.tsx
  - src/logic/favourite-optimistic.ts
  - src/logic/favourite-optimistic.test.ts
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [LISTV-03, LISTV-04, LISTV-09]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "E2 populated: the row renders line 1 name / line 2 recency·category / line 3 adaptive-or-completeness, with avatar, status border, lower-right glyph, and upper-right star (full three-line anatomy)"
    - "E2 partial: missing line-3 context renders the deterministic per-contact completeness prompt (D-11) via selectLine3; present knowledge renders the selected adaptive item"
    - "E2 overflow: line 1 `numberOfLines={1}` ellipsize tail; lines 2/3 single-line ellipsize tail; category chip `maxWidth` ellipsize"
    - "LISTV-04 / E5 star populated: an always-visible binary star — filled = `accent`, unfilled = `textSecondary`, ≥44px hit area — via the shared `favorite` registry name (no literal glyph)"
    - "LISTV-04 / E5 star initial membership (review LOW #5): a row's initial favourite state derives from `DashboardRow.favourite_rank !== null` (projected via `CARD_FAVOURITE_RANK` at dashboard-read.ts:375/:428) — NOT the optional `isFavourite?` field (populated ONLY by the favourites-population read at :228) and NEVER `listFavourites` (prohibited; that is ranked-favourites UX). Rank is read ONLY as a null/non-null membership test, never as an order."
    - "LISTV-04 / E5 star loading: toggle is optimistic — immediate fill/unfill + light haptic, NO spinner, NO pending state, NO success snackbar (D-04)"
    - "LISTV-04 / E5 star error: persistence failure reverts the optimistic state and shows `Couldn't update favourite. Try again.`; the favourite write is binary membership only (setFavouriteRank/clearFavouriteRank), never read back as rank"
    - "LISTV-04 / E5 rapid double-tap safety (review MED #4): the optimistic favourite path carries a PER-CONTACT mutation generation. Each toggle bumps the contact's generation and captures it; a failed write reverts ONLY if its captured generation is still the latest for that contact — so a slow older failure can NEVER stomp a newer optimistic choice. Serialising the DAO write alone does not prevent a stale failure handler from reverting newer UI state; the generation guard does. This reconciliation lives in a PURE `src/logic/favourite-optimistic.ts` controller so it is node-testable without a HomeScreen render harness."
    - "LISTV-04 / E5 reload-vs-optimistic reconciliation (review cycle-3 MED #5): HomeScreen reloads on focus/foreground/pull/shell-refresh and replaces `rows` WHOLESALE (HomeScreen.tsx:312/:338), so the mutation-generation guard alone (which fixes stale FAILURE handlers) does NOT stop an overlapping reload from erasing an in-flight optimistic choice. The `favourite-optimistic` controller therefore holds a PER-CONTACT optimistic OVERLAY (the pending membership + its generation); each row's rendered star membership is `overlayFor(contactId) ?? (row.favourite_rank !== null)`, so a wholesale reload returning the pre-toggle DB truth CANNOT visually erase a newer optimistic choice. RULE: the overlay WINS over reload while a contact's latest mutation is unsettled; the overlay is cleared for that contact ONLY when its LATEST generation settles — success (DB now matches; drop overlay so reload truth shows) or failure of the latest gen (revert; drop overlay). A stale (non-latest) settle never clears or reverts. Pure + node-tested in `favourite-optimistic.test.ts` (no render harness needed)."
    - "LISTV-09: the row's accessible description covers name, category, recency, favourite state, and relationship/snooze state WITHOUT colour, reusing the StatusGlyph state labels (Stable/Wobbling/Decaying/Rogue/Snoozed/Not yet contacted) via an EXPORTED pure `statusDisplayLabel(state)` from `src/components/icons/StatusGlyph.tsx` — the previously-private `STATUS_LABEL` (:37) is now the single source both StatusGlyph and this description consume (review cycle-3 MED #3 — verbatim reuse was not achievable while the labels were private); the status glyph stays decorative (state carried by the row description, not double-announced)"
    - "LISTV-03 async lifecycle (review HIGH #3): the async `readLine3Candidates()` load is NOT computed in a `useMemo` (which cannot await). It is folded into HomeScreen's existing cancellation-aware `reload()` (`src/screens/HomeScreen.tsx:282-336`) so rows + a line3-by-contact-id map are produced ATOMICALLY for one query generation, set together after the same `if (cancelled) return` guard, and a superseded in-flight response is dropped by the same `cancelled` flag — a stale line-3 load can never overwrite newer state, and line-3 values never churn per-row on scroll/recycle"
    - "LISTV-06 forward prop-shape (review #8): `ListRow`'s prop contract RESERVES a search-mode `DashboardSearchResult | null` slot (default null / undefined in this plan) so Plan 06 can populate it WITHOUT a late prop-shape retrofit — no executor may calcify a `snippet`-only search implementation here"
    - { statement: "E2 long-text: OS/accessibility text scaling reflows row height (increase), never clamps below readable size (§C/§Q, LISTV-09)", verification: backstop }
  artifacts:
    - "src/components/icons/StatusGlyph.tsx — an EXPORTED pure `statusDisplayLabel(state)` (review cycle-3 MED #3): the previously-private `STATUS_LABEL` (:37) becomes the single source both StatusGlyph's own render and buildRowAccessibilityDescription consume (no forked second label source)"
    - "src/components/list-row-content.ts — buildRowAccessibilityDescription() consuming the exported statusDisplayLabel (pure, node-tested)"
    - "src/components/ListRow.tsx — star (registry favorite) + line 3 (selectLine3 result) + colour-free accessibility description"
    - "src/logic/favourite-optimistic.ts — a PURE per-contact optimistic-OVERLAY + mutation-generation controller (review MED #4 + cycle-3 MED #5): begin(contactId, desiredMembership) → generation (records the overlay); overlayFor(contactId) → boolean | undefined (the pending membership to render over a wholesale reload, or undefined when none pending); resolve(contactId, generation, outcome) → \"applied\" | \"stale\" (clears the overlay on the LATEST generation's settle — success or failure — and no-ops a stale generation); node-tested (no HomeScreen render harness needed)"
    - "src/screens/HomeScreen.tsx — optimistic favourite handler (haptic + favourites-dao + per-contact generation revert/notify via favourite-optimistic controller) + line-3 data wiring folded into the cancellation-aware `reload()` (readLine3Candidates → selectLine3 → `line3ByContactId` state, committed atomically with rows under the `cancelled` guard)"
  key_links:
    - "readLine3Candidates (27-03) → selectLine3 (27-03) → HomeScreen `reload()` (line3-by-id map, same query generation, cancellation-guarded) → ListRow line 3 — batched, deterministic, no per-row read, no stale overwrite"
    - "favourites-dao setFavouriteRank/clearFavouriteRank → HomeScreen optimistic handler (favourite-optimistic per-contact overlay + generation) → ListRow star props — binary membership; the per-contact overlay (`overlayFor(contactId) ?? favourite_rank !== null`) survives a wholesale `reload()` and is generation-gated on settle (review MED #4 + cycle-3 MED #5)"
    - "DashboardRow.favourite_rank !== null (CARD_FAVOURITE_RANK :375/:428) → ListRow star initial membership — null/non-null test only, never rank-as-order, never listFavourites (review LOW #5)"
  prohibitions:
    - "no profile-completeness scoring or gamification"
    - "no ranked-favourites UX; never read favourite_rank as order and never call listFavourites for the list star"
    - "no hardcoded colour in row rendering (D-06 / check:colors)"
    - "no success snackbar on favourite toggle (D-04)"
    - "no AI-generated adaptive row copy"
---

<objective>
Complete the ListRow's static content and accessibility over the tracer skeleton: render the always-visible binary favourite star (optimistic toggle + haptic + revert), the deterministic line-3 adaptive-or-completeness item, and a colour-free accessible row description. Wire the batched line-3 data and the optimistic favourite handler from HomeScreen.

Purpose: LISTV-03 (line 3 render), LISTV-04 (star), LISTV-09 (accessible description) build directly on the proven tracer row and the 27-03 primitives.
Output: the completed row content + a11y description; the swipe gestures and search-mode rendering follow in 27-05/27-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/27-dashboard-list-view/27-UI-SPEC.md
@.planning/phases/27-dashboard-list-view/27-PATTERNS.md
@src/components/ListRow.tsx
@src/components/list-row-content.ts
@src/components/contact-card-ring.ts
@src/components/icons/StatusGlyph.tsx
@src/components/icons/Icon.tsx
@src/db/favourites-dao.ts
@src/db/dashboard-knowledge-read.ts
@src/logic/list-row-selection.ts
@src/screens/HomeScreen.tsx
@src/components/UniversalFab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Colour-free accessible row description</name>
  <files>src/components/icons/StatusGlyph.tsx, src/components/list-row-content.ts, src/components/list-row-content.test.ts</files>
  <read_first>
    - src/components/list-row-content.ts (the tracer's formatListRecency/formatLine2 to compose with)
    - src/components/icons/StatusGlyph.tsx:37-44 (the exact state labels Stable/Wobbling/Decaying/Rogue/Snoozed/Not yet contacted — but `STATUS_LABEL` is a PRIVATE `const` at :37 (verified), so it CANNOT be imported as written; "verbatim reuse" is only achievable by EXPORTING a pure `statusDisplayLabel(state)` here and routing StatusGlyph's own render through it — review cycle-3 MED #3. Do NOT re-author the strings in a second module, which would create a drift-prone second source)
    - src/components/contact-card-ring.ts:73 (StatusDisplayState union)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Accessibility Contract — the required description coverage)
  </read_first>
  <behavior>
    - statusDisplayLabel(state): a pure exported function on StatusGlyph.tsx returning the human-readable state name (Stable/Wobbling/Decaying/Rogue/Snoozed/Not yet contacted) — the single source that both StatusGlyph's own render AND buildRowAccessibilityDescription consume (review cycle-3 MED #3); StatusGlyph's internal lookup is refactored to call it (behaviour-preserving, its rendered label unchanged).
    - buildRowAccessibilityDescription({ name, category, recency, isFavourite, displayState }): returns one string naming contact name, category, recency phrase, favourite membership, and the relationship-or-snooze state via `statusDisplayLabel(displayState)` — never referencing colour.
    - null displayState → uses the "Not yet contacted" label; "snoozed" → uses "Snoozed"; the four real states use their labels — all via the shared statusDisplayLabel, never re-authored strings.
    - Favourite state is named ("Favourite" / not) so AT users get membership without seeing the star fill.
  </behavior>
  <acceptance_criteria>
    - `src/components/icons/StatusGlyph.tsx` EXPORTS a pure `statusDisplayLabel(state)` and its own render is refactored to use it, so `npx vitest run` on any existing StatusGlyph label test stays green and the rendered label is unchanged (review cycle-3 MED #3 — single source, no drift).
    - `src/components/list-row-content.ts` exports a pure `buildRowAccessibilityDescription(...)` that names contact name, category, recency phrase, favourite membership, and relationship/snooze state — reusing the StatusGlyph labels via the imported `statusDisplayLabel` (NOT re-authored strings).
    - `npx vitest run src/components/list-row-content.test.ts` passes with cases for each of the four real states, snoozed, never-contacted, favourite/not-favourite, and missing category.
    - The description encodes relationship/snooze state only via the shared `statusDisplayLabel` output (Stable/Wobbling/Decaying/Rogue/Snoozed/Not yet contacted), so the state is legible without any visual perception.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <action>
    FIRST, in `src/components/icons/StatusGlyph.tsx`, export a pure `statusDisplayLabel(state)` and refactor the component's private `STATUS_LABEL` lookup (:37-44) to route through it — behaviour-preserving, the rendered label unchanged — so there is ONE label source (review cycle-3 MED #3; the strings were previously un-importable). THEN extend `list-row-content.ts` with a pure `buildRowAccessibilityDescription` that composes name + category + recency (via the existing formatters) + favourite membership + the relationship/snooze state via the imported `statusDisplayLabel` (single source, NOT re-authored strings). No colour words. Add cases to `list-row-content.test.ts` for each display state, snoozed, never-contacted, favourite/not, and missing category.
  </action>
  <verify>
    <automated>npx vitest run src/components/list-row-content.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The a11y description covers name/category/recency/favourite/state without colour for every state; tests + tsc green.</done>
  <reversibility rating="reversible">Pure additive function; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: ListRow — star + line 3 + accessible description</name>
  <files>src/components/ListRow.tsx</files>
  <read_first>
    - src/components/ListRow.tsx (the tracer row to extend; keep the purely-presentational contract — explicit props, no DB read/getExecutor/useNavigation)
    - src/components/icons/Icon.tsx (Icon name/state/tone props; favorite now resolves to star via 27-03)
    - .planning/phases/27-dashboard-list-view/27-UI-SPEC.md (Row Anatomy — star upper-right ≥44px hit area, filled=accent/unfilled=textSecondary; line 3 leading icon optional; Typography roles; ellipsize rules)
    - .planning/phases/27-dashboard-list-view/27-PATTERNS.md (ListRow section — the Icon favorite usage; do NOT copy ContactCard raw px)
    - src/theme/tokens/{spacing,radii,typography,icon-size}.ts (SPACING/RADII.lg/typography roles/ICON_SIZE)
  </read_first>
  <acceptance_criteria>
    - `ListRow` renders line 3 from a `line3` prop (the selectLine3 result) with optional leading `Icon` only when the item maps to a registered `IconName`; otherwise plain text.
    - The star renders `Icon` with the semantic name `favorite`, `state` active/default from `isFavourite`, `tone` `accent` when favourite else `textSecondary`; its tappable hit area is ≥44px (via hitSlop or a ≥44 wrapper) and calls the `onToggleFavourite` prop.
    - Line 1 uses `numberOfLines={1}` ellipsize tail; lines 2 and 3 are single-line ellipsize tail; the category chip ellipsizes with a maxWidth.
    - The row Pressable carries `accessibilityLabel` from `buildRowAccessibilityDescription`; the status glyph is `accessibilityElementsHidden`/decorative (state carried by the description).
    - `ListRow`'s prop contract RESERVES an optional search-mode slot `searchResult?: DashboardSearchResult | null` (forward note, review #8) — declared but unrendered in this plan (Plan 06 wires the search-mode branch). This prevents a late prop-shape retrofit and forbids an executor from calcifying a `DashboardRow.snippet`-only search implementation.
    - `npm run check:colors` clean; `npx tsc --noEmit` clean. No raw px font sizes / border radii; tokens only.
  </acceptance_criteria>
  <action>
    Extend `ListRow.tsx` to render (a) the upper-right favourite star through `Icon name="favorite"` with active/default state and accent/textSecondary tone and a ≥44px hit area calling `onToggleFavourite`; (b) line 3 from a `line3` prop, showing a leading semantic `Icon` only when the item type maps to a registered `IconName` and plain text otherwise; (c) `accessibilityLabel` on the row Pressable from `buildRowAccessibilityDescription`, keeping the status glyph decorative so the state is not double-announced. Reserve an optional `searchResult?: DashboardSearchResult | null` prop slot now (declared, not rendered) so Plan 06's search-mode branch is a pure addition, not a prop-shape change (review #8). Consume `SPACING`/`RADII.lg`/typography roles/`ICON_SIZE` tokens and `useTheme().colors` only. Keep the component purely presentational — the caller owns the favourite write and line-3 data.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: each row shows an always-visible star (tap toggles fill/unfill with a light haptic, no snackbar) and a line-3 adaptive item or completeness prompt. TalkBack reads a colour-free description covering name, category, recency, favourite, and relationship/snooze state; the star is a togglable control; the glyph is not separately announced.</human-check>
  </verify>
  <done>The row shows a tappable binary star + deterministic line 3 + a colour-free a11y description; tsc + check:colors clean; device UAT confirms star toggle and TalkBack description.</done>
  <reversibility rating="reversible">Presentational render additions; revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 3: HomeScreen — optimistic favourite handler + line-3 data wiring</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx:282-336 (the cancellation-aware `reload()` `useCallback`: `let cancelled` at :283, the `Promise.all` at :299-311, the single `if (cancelled) return` guard at :312, `setRows(list)` at :313 — which REPLACES `rows` WHOLESALE; freshness paths re-invoke the returned canceller at :346/:359/:375 (focus/foreground/pull/shell-refresh at :338) — line-3 MUST be produced inside THIS flow, keyed to the same query generation, NOT in a `useMemo`. The `cancelled`-guard is the PROVEN stale-drop pattern rows already depend on; line-3 + the optimistic-favourite base state inherit it by committing in the SAME guarded state-set, not a separate async setState. NOTE cycle-3 MED #5: because reload replaces `rows` wholesale, the rendered star membership must be `overlayFor(contactId) ?? (favourite_rank !== null)` — an in-flight optimistic choice cannot be resolved by the mutation-generation guard alone against an overlapping reload, so the per-contact OVERLAY must override the reloaded base until the contact's latest mutation settles)
    - src/db/dashboard-read.ts (VERIFY on disk: `DashboardRow.favourite_rank: number | null` at :100 is projected via `CARD_FAVOURITE_RANK` at :375 and :428 on BOTH population and search reads — the star's initial/base membership is `favourite_rank !== null`; the optional `isFavourite?` at :108 is populated ONLY by the favourites-population read at :228, and `listFavourites` at :467-470 is ranked-favourites UX and PROHIBITED here — review LOW #5)
    - src/db/favourites-dao.ts:32-76 (setFavouriteRank/clearFavouriteRank — inWriteTransaction, `?`-bound single-column UPDATE, changes===1 guard, modified_at bump; binary mark/clear)
    - src/logic/favourite-optimistic.ts (this plan: the PURE per-contact optimistic-OVERLAY + mutation-generation controller — `begin(contactId, desiredMembership)`, `overlayFor(contactId)`, `resolve(contactId, generation, outcome)` — the handler uses it to render the pending membership over a wholesale reload AND to gate reverts on settle — review MED #4 + cycle-3 MED #5)
    - src/components/UniversalFab.tsx:165 (Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light) idiom)
    - src/db/dashboard-knowledge-read.ts (readLine3Candidates) + src/logic/list-row-selection.ts (selectLine3) from 27-03
    - src/utils/dates.ts (localDateTime/formatLocalDate for the write timestamp)
  </read_first>
  <acceptance_criteria>
    - Line-3 is loaded INSIDE `reload()`'s existing async flow, NOT a `useMemo` (which cannot await a DB read, review HIGH #3): after the population/search read resolves, `reload()` calls `readLine3Candidates(exec, loadedIds)` for the exact loaded row-id set and builds a `line3ByContactId` map via `selectLine3(...)`; rows and the line-3 map are committed TOGETHER after the same `if (cancelled) return` guard (a new state field, e.g. `line3ByContactId`, set alongside `setRows`), so a superseded in-flight response is dropped by the same `cancelled` flag and can never overwrite newer state. `renderItem` reads the memo/state map — never a per-row DB read. (Forward note, review cycle-3 LOW #8: line 3 is UNCONSUMED in LIST SEARCH mode — lines 2–3 become the match explanation — so Plan 06 will SCOPE this line-3 load to the non-search branch of `reload()`; this plan loads it for the list path and Plan 06 gates it, avoiding wasted work.)
    - Line-3 cancellation is a STRUCTURAL criterion (review LOW #5): the line-3 map (and the optimistic-favourite base membership) are committed ONLY inside `reload()`'s existing `if (cancelled) return` guard, in the SAME state-set as `setRows` — grep confirms no `setState` for line-3 outside that guard and no separate un-guarded async line-3 path. The cancellation guarantee is inherited from the proven rows pattern (a stale in-flight response is dropped by the same `cancelled` flag). A full HomeScreen render test is NOT added — this repo has no RN component test harness (node/vitest only); the review-offered "test seam" is satisfied by (a) this structural guard criterion and (b) the pure `favourite-optimistic` controller test below, which covers the state-commit-race half directly.
    - A row's BASE favourite membership is derived from `DashboardRow.favourite_rank !== null` (review LOW #5) — never from the optional `isFavourite?` field and never via `listFavourites` — and the RENDERED membership is `overlayFor(contactId) ?? (favourite_rank !== null)` so a wholesale `reload()` returning the pre-toggle DB truth cannot erase an in-flight optimistic choice (review cycle-3 MED #5).
    - The favourite handler: on tap it records the per-contact overlay + generation via `begin(contactId, desiredMembership)`, flips the optimistic star + fires the Light haptic immediately, then calls `clearFavouriteRank`/`setFavouriteRank` (binary); it settles via `resolve(contactId, generation, outcome)`: on SUCCESS no snackbar and the overlay is dropped (only if this generation is still latest) so subsequent reloads show the committed DB truth; on FAILURE it reverts and shows `Couldn't update favourite. Try again.` ONLY when this generation is still latest (review MED #4 — a slow older failure never stomps a newer choice; a stale settle no-ops).
    - `npx vitest run src/logic/favourite-optimistic.test.ts` passes: (a) MED #4 — a stale generation's failure revert is SUPPRESSED after a newer `begin(contactId, …)` (rapid double-tap: an older failed write cannot revert the newer optimistic choice); a failure whose generation is still latest DOES revert; (b) cycle-3 MED #5 — after `begin(contactId, true)`, `overlayFor(contactId) === true` (so it renders over a reloaded base of `false`); once the LATEST generation's `resolve(...)` runs (success OR failure), `overlayFor(contactId) === undefined` (overlay cleared → base/reload truth shows); a `resolve(...)` for a stale generation leaves the newer overlay intact.
    - The list favourite path never calls `listFavourites` and never orders by favourite rank (grep the list render path).
    - `npx tsc --noEmit` + `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Wire the optimistic favourite handler and the batched line-3 data into HomeScreen's list-mode path. Fold the line-3 load into the existing cancellation-aware `reload()` (do NOT use `useMemo` over an async read, review HIGH #3): after the list read resolves and BEFORE the `if (cancelled) return` guard's state commit, call `readLine3Candidates(exec, loadedIds)` for the resolved row ids, build a `line3ByContactId` map with `selectLine3(...)`, and set it alongside `setRows(list)` in one guarded commit so rows + line-3 are atomic for one query generation and a stale in-flight response is dropped by the same `cancelled` flag. `renderItem` reads the map (memo/state) and passes each row its `line3` prop — never a per-row read. Derive each row's BASE star membership from `DashboardRow.favourite_rank !== null` (review LOW #5 — never the optional `isFavourite?` field, never `listFavourites`) and RENDER `overlayFor(contactId) ?? base` so an in-flight optimistic choice survives a wholesale `reload()` (review cycle-3 MED #5). Create the pure `src/logic/favourite-optimistic.ts` controller owning a per-contact map of `{ generation, membership }`: `begin(contactId, desiredMembership) → generation` (bumps the generation and records the overlay), `overlayFor(contactId) → boolean | undefined` (the pending membership, or undefined once settled), `resolve(contactId, generation, outcome) → "applied" | "stale"` (for the LATEST generation, clears the overlay on success OR failure and returns "applied"; for a stale generation returns "stale" and leaves the newer overlay untouched) — with `favourite-optimistic.test.ts`. Implement `onToggleFavourite` as: `const gen = controller.begin(contactId, next)`, flip the rendered optimistic star (via the overlay) + `Haptics.impactAsync(...Light)` immediately, then call `setFavouriteRank`/`clearFavouriteRank` (binary membership); on success call `controller.resolve(contactId, gen, "success")` (no snackbar; the overlay clears if latest so a later reload shows the committed DB truth); on failure call `controller.resolve(contactId, gen, "failure")` and, only when it returns "applied", revert and notify with the error copy (review MED #4 + cycle-3 MED #5 — a stale older failure must not stomp a newer optimistic choice, and an overlapping reload must not erase the in-flight one). Treat the star strictly as binary membership — the list ordering stays the shared Default/population ordering.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/db/dashboard-knowledge-read.test.ts src/logic/list-row-selection.test.ts src/logic/favourite-optimistic.test.ts</automated>
    <human-check>On the Pixel: toggling a star is instant with a light haptic and no snackbar; simulating a write failure (e.g. via a forced error) reverts the star and shows the error notice; rapid double-tapping a star does not leave it stuck on the wrong state after a slow failure. Line 3 shows the deterministic item/prompt and is stable across scroll/recycle.</human-check>
  </verify>
  <done>Favourite toggles optimistically with revert-on-failure and no success snackbar; line-3 data is batched + deterministic + recycle-stable; tsc + check:colors + upstream logic tests green.</done>
  <reversibility rating="reversible">Screen-level wiring; revertable. Favourite writes are reversible binary mark/clear.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected knowledge candidate → rendered line 3 | must render only the already-filtered candidate from 27-03; no UI re-query or `.filter()` resurfacing |
| favourite toggle → favourites-dao write | binary mark/clear only; failure must not leave a false-positive UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-07 | Information disclosure | ListRow line 3 / HomeScreen line-3 wiring | medium | mitigate | Line 3 consumes only `readLine3Candidates` output, which applies hidden/deleted/outdated/off-limits exclusions IN-QUERY (27-03); HomeScreen adds no UI `.filter()` that could resurface excluded knowledge. |
| T-27-08 | Spoofing (false success) | optimistic favourite toggle | medium | mitigate | The UI reverts the optimistic state and notifies on persistence failure (no success snackbar), so the star never claims a membership change that did not commit (mirrors the shell's commit-truthful contract). A PER-CONTACT optimistic OVERLAY + mutation generation (`favourite-optimistic` controller, review MED #4 + cycle-3 MED #5) ensures a stale older failure reverts ONLY if it still owns the latest generation AND that the rendered membership (`overlayFor(contactId) ?? favourite_rank !== null`) is the in-flight optimistic choice until its latest mutation settles — so neither a rapid double-tap NOR an overlapping wholesale `reload()` can leave the star showing a state that contradicts the last committed intent. |
| T-27-SC | Tampering | package installs | low | accept | No packages installed this phase; no supply-chain checkpoint required. |
</threat_model>

## Artifacts this phase produces

- `src/components/icons/StatusGlyph.tsx` — an EXPORTED pure `statusDisplayLabel(state)` (review cycle-3 MED #3), with StatusGlyph's own render routed through it (single label source).
- `src/components/list-row-content.ts` — `buildRowAccessibilityDescription()` consuming the imported `statusDisplayLabel` (pure, node-tested).
- `src/components/ListRow.tsx` — favourite star (registry `favorite`) + line-3 render + colour-free `accessibilityLabel`.
- `src/logic/favourite-optimistic.ts` — pure per-contact optimistic-OVERLAY + mutation-generation controller (`begin`/`overlayFor`/`resolve`), node-tested (review MED #4 + cycle-3 MED #5).
- `src/screens/HomeScreen.tsx` — optimistic favourite handler (haptic + favourites-dao + per-contact generation-gated revert/notify; initial membership from `favourite_rank !== null`) + batched line-3 data wiring folded into the cancellation-aware `reload()` (`readLine3Candidates` → `selectLine3` → `line3ByContactId`, committed atomically with rows under the `cancelled` guard — review HIGH #3).

<verification>
- `npx vitest run src/components/list-row-content.test.ts src/db/dashboard-knowledge-read.test.ts src/logic/list-row-selection.test.ts src/logic/favourite-optimistic.test.ts` green.
- `npx tsc --noEmit` + `npm run check:colors` clean.
- Device UAT: star toggle (instant, haptic, no snackbar, revert-on-failure; rapid double-tap safe); deterministic line 3; TalkBack colour-free description.
</verification>

<success_criteria>
The row is content-complete for the non-search state — binary star with optimistic/revert semantics, deterministic line 3, and a colour-free accessible description — with no hardcoded colour and no rank leakage.
</success_criteria>

<output>
Create `.planning/phases/27-dashboard-list-view/27-04-SUMMARY.md` when done.
</output>
