---
phase: 34-rapid-capture-update-flows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/AccordionSection.tsx
  - src/screens/create-contact-logic.ts
  - src/screens/create-contact-logic.test.ts
  - src/screens/CreateContactScreen.tsx
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-03, CAPT-14]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-01/02/03 + D-05/D-10)
    - "Add Contact presents three primary accordion sections — Identity (open by default), Relationship Basics, Contact Methods — with a Show More action revealing the advanced enrichment sections (CAPT-01, dossier §D)."
    - "Name is the only field required to Save; canSave gates Name-only (name.trim().length > 0) and Save routes to the new Contact Profile (CAPT-01, dossier §C/§G)."
    - "The tri-state last-spoke control (today / on date / not yet) stays in Relationship Basics defaulting \"today\"; \"Not yet\" creates NO interaction row (firstInteractionOccurredAt returns null → no firstInteraction) — never an interaction with a null date (CAPT-02, D-05, ADR-016)."
    - "A contact created with no Contact Frequency is Unbound (trackingEnabled=false); selecting any cadence turns Bound on; a dormant cadence (Bound off, interval retained) is permitted (CAPT-03, D-10, ADR-062 re-verified on disk against contacts_prevent_cadence_clear)."
    - "Validation errors reveal + focus the containing accordion section rather than leaving the user hunting collapsed drawers; an unchanged form exits with no confirmation (CAPT-14, dossier §AD/§AF)."
    # Specless edge probe — CAPT-01, CAPT-03 (authored)
    - "Add Contact with an empty/whitespace Name cannot Save (canSave false); a Name-only contact with all other fields absent persists successfully (CAPT-01 empty edge)."
    - "Contact creation (contact + optional first interaction + custom values) is one atomic transaction (createContactFull); an interrupted create rolls back fully — never a partial contact (CAPT-01 concurrency edge; ADR-016)."
    - statement: "Name validation uses trimmed JS string length; a whitespace-only name is rejected and multi-byte/emoji names persist unchanged with no normalization or coercion (CAPT-01 encoding edge)."
      verification: backstop
    - statement: "Submitting Add Contact twice creates two distinct contacts (no silent dedupe); duplicate-name detection via isDuplicateName is advisory, not a hard block (CAPT-01 idempotency edge)."
      verification: backstop
    - "A contact with no Contact Frequency is Unbound; selecting a valid cadence turns Bound on; turning Bound on with no cadence requires selecting a valid interval (CAPT-03 boundary edge; dossier §F)."
    - statement: "Contact Frequency interval is stored as an integer number of days via FrequencyPicker preset/validated-custom mapping, with no rounding or precision loss (CAPT-03 precision edge)."
      verification: backstop
    # UI-SPEC ## UI Considerations — surface E1 (Add Contact)
    - "empty (E1): Add Contact requires only Name and persists with all else absent; 'Not yet' records no interaction (UI-SPEC, covered)."
    - "error (E1): a save failure renders locked \"Couldn't save / Please try again.\", preserves form state, never shows completion; a future last-spoke date shows FUTURE_DATE_MESSAGE in danger text keeping the prior selection (UI-SPEC, covered)."
    - "partial (E1): optional fields absent is a valid saved state (name-only contact) (UI-SPEC, covered)."
    - "populated (E1): Show More reveals the advanced enrichment sections at full volume (UI-SPEC, covered)."
    - "loading (E1): the form hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "overflow (E1): revealed enrichment sections and repeated values stay bounded and scrollable with no clipped/unreachable rows across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
    - statement: "long-text (E1): long contact names and custom-field names truncate or wrap without breaking layout at large OS text scale in all four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT drop the tri-state last-spoke control from Add Contact, and MUST NOT represent \"Not yet\" as an interaction with a null date — \"Not yet\" creates NO interaction row, preserving the genuinely never-contacted case (D-05, ADR-016, ESCALATE trip-wire)."
    - "MUST NOT turn Add Contact into the full Edit Contact form or require Category / Contact Frequency / contact methods / photo to create a contact — the quiet 3-section initial state and Show More boundary are product requirements (dossier §D/§AN)."
    - "MUST NOT dismiss the form or show completion when persistence fails — a failed save preserves entered text/date/selection state and exposes Retry where recoverable (CAPT-14, dossier §AE)."
  artifacts:
    - src/components/ui/AccordionSection.tsx
    - "src/screens/create-contact-logic.ts (extended section/save helpers)"
    - src/screens/CreateContactScreen.tsx
  key_links:
    - "CreateContactScreen composes AccordionSection + TriStateLastSpoke + FrequencyPicker + ContactMethodsEditor + FieldValueInput; the tri-state → firstInteraction rule stays in create-contact-logic.firstInteractionOccurredAt."
    - "createContactFull is the ONLY production create path (seeds the definition-pair matrix + first interaction in one txn); the screen must not inline INSERTs."
---

<objective>
Restructure Add Contact into the streamlined three-section accordion form (Identity / Relationship Basics / Contact Methods) with Show More, delivering CAPT-01/02/03 and the Add-Contact slice of CAPT-14 — composing the existing shipped create path, not rewriting it. `CreateContactScreen` + `create-contact-logic.ts` are already the real create surface (not placeholders); `canSave`, `firstInteractionOccurredAt`, and `buildCreateInput` already implement the load-bearing rules. The work is information-architecture + a reusable `AccordionSection` primitive (there is no existing accordion component; verified on disk) that 34-05 (Edit Contact) will also consume.

Per D-01/D-02 the dossier (§C/§D/§F/§G/§AD/§AF) and planning-notes (R-13) are ground truth. D-05 (tri-state last-spoke) and D-10 (Bound/Unbound vs ADR-062) are the ESCALATE trip-wires — re-verify ADR-062 (`tracking_enabled`, dormant cadence, `contacts_prevent_cadence_clear`) on disk before touching cadence writes.

Purpose: fewest honest taps to create a contact while preserving the never-contacted backlog case and cadence semantics.
Output: `AccordionSection` primitive; extended `create-contact-logic` + test; restructured `CreateContactScreen`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
@docs/decisions/ADR-016-fixed-first-contact-forms-and-atomic-contact-creation.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: AccordionSection primitive + end-to-end name-only Add Contact through Identity section</name>
  <files>src/components/ui/AccordionSection.tsx, src/screens/CreateContactScreen.tsx</files>
  <reversibility rating="reversible">New presentational primitive + IA edit; revisable.</reversibility>
  <read_first>
    - src/screens/CreateContactScreen.tsx (current flat View/Text field layout, header lines 197-295; imports FrequencyPicker/TriStateLastSpoke/ContactMethodsEditor/FieldValueInput; getAppSettings; createContactFull)
    - src/screens/create-contact-logic.ts (canSave lines 64-69; firstInteractionOccurredAt 76-88; buildCreateInput 96-127)
    - src/components/ui/ (Button/AppText/GlassSurface/Sheet primitives + button-roles MIN_TOUCH_TARGET)
    - src/theme/ (tokens; useTheme().colors.* — check:colors gate)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Spacing/Typography/Color/Copywriting contracts; accordion a11y: expanded/collapsed announced)
  </read_first>
  <action>
    Create `src/components/ui/AccordionSection.tsx`: a controlled expand/collapse section with a heading-role header (AppText `heading`), a ≥44×44 touch target (MIN_TOUCH_TARGET), an `accessibilityState={{ expanded }}` and accessible name matching the visible header, body slotting arbitrary children, all colours via `useTheme().colors.*` (surface/surfaceElevated body, border/borderStrong outline). Then restructure `CreateContactScreen` so the Identity section (Name + Category) is the first `AccordionSection`, open by default, wired to the existing `canSave` gate and `createContactFull` save → navigate to Profile. Keep this tracer thin: one working section + Save + route. Replace the screen's raw `Text` with `AppText` roles for touched elements (do not rewrite untouched sub-editors). The remaining sections land in Task 2/3.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/create-contact-logic.test.ts</automated>
    <human-check>On the Pixel debug build: Add Contact opens with Identity expanded; entering a Name and Save creates the contact and lands on its Profile.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0 (zero new colour literals).
    - `npx vitest run src/screens/create-contact-logic.test.ts` exits 0 (existing canSave/firstInteraction coverage still green).
    - `grep -c "accessibilityState" src/components/ui/AccordionSection.tsx` ≥ 1.
  </acceptance_criteria>
  <done>AccordionSection exists with a11y expanded/collapsed state; name-only Add Contact works end-to-end through the Identity section and routes to Profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relationship Basics — tri-state last-spoke + Bound/Unbound cadence (CAPT-02/03)</name>
  <files>src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/CreateContactScreen.tsx</files>
  <reversibility rating="costly">Touches the ADR-016 last-spoke behavior and ADR-062 Bound/Unbound coordination; a regression silently reverses a recorded decision. Preserve, do not re-derive.</reversibility>
  <read_first>
    - src/screens/create-contact-logic.ts (firstInteractionOccurredAt 76-88; buildCreateInput 118-125 — "not yet" omits firstInteraction)
    - src/screens/create-contact-logic.test.ts (extend)
    - src/components/TriStateLastSpoke.tsx + tri-state-last-spoke-logic.ts (LastSpokeValue today/date/not-yet; future-date guard; FUTURE_DATE_MESSAGE)
    - src/components/FrequencyPicker.tsx (preset + validated custom interval → interval_days)
    - src/db/contacts-dao.ts (createContactFull takes trackingEnabled; ADR-062 tracking_enabled)
    - src/db/migrations/011-contact-lifecycle-schema.ts (contacts_prevent_cadence_clear trigger ~181-186 — re-verify per D-10)
  </read_first>
  <behavior>
    - firstInteractionOccurredAt({kind:"today"}, now) === now; ({kind:"date", date}) === `${date} 00:00:00`; ({kind:"not-yet"}) === null.
    - buildCreateInput omits `firstInteraction` when occurredAt is null (no interaction row for "not yet").
    - A create input with no cadence yields trackingEnabled=false (Unbound); selecting a cadence yields trackingEnabled=true (Bound).
    - A dormant-cadence input (Bound off + interval present) is representable and valid.
    - A future last-spoke date is rejected via the shared guard (FUTURE_DATE_MESSAGE), keeping the prior selection.
  </behavior>
  <action>
    Place `TriStateLastSpoke` (default `{kind:"today"}`) in the Relationship Basics `AccordionSection`, and `FrequencyPicker` + a Bound toggle. Extend `create-contact-logic.ts` only as needed to expose Bound/Unbound coordination as a pure, node-tested helper (no cadence → Unbound; cadence selected → Bound on; Bound-on-with-no-cadence requires a valid interval), keeping `firstInteractionOccurredAt`'s "not yet" → null branch intact. Re-verify ADR-062 on disk before touching cadence writes; do not model Unbound as `interval_days = null`. Extend `create-contact-logic.test.ts` for the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/screens/create-contact-logic.test.ts</automated>
    <human-check>On the Pixel: default last-spoke is Today; choosing "Not yet" shows "no interaction is recorded" and creates a contact with NULL last_contact; choosing a cadence turns Bound on.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/create-contact-logic.test.ts` exits 0 including the "not yet" → null and Bound/Unbound cases.
    - Test asserts firstInteractionOccurredAt("not-yet") === null and buildCreateInput omits firstInteraction.
    - No inline `interval_days = null` used to mean Unbound (grep the logic module).
  </acceptance_criteria>
  <done>Tri-state last-spoke default "today" with "not yet" → no interaction; Unbound/Bound coordinated with optional cadence; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Contact Methods section + Show More enrichment + validation-reveal (CAPT-01/14)</name>
  <files>src/screens/CreateContactScreen.tsx</files>
  <reversibility rating="reversible">IA composition; revisable.</reversibility>
  <read_first>
    - src/screens/CreateContactScreen.tsx (ContactMethodsEditor + FieldValueInput usage; existing validation)
    - src/components/ContactMethodsEditor.tsx (canonical contact-method editor + validation)
    - src/screens/create-contact-logic.ts (buildCreateInput assembly)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Show More; error rows; §AD reveal-and-focus accordion)
  </read_first>
  <action>
    Put `ContactMethodsEditor` in the Contact Methods `AccordionSection`. Add a `Show More` (tertiary/accentText) action that reveals the advanced enrichment sections (Last Talked About, Key People, Current Location, Memories, Custom Fields, Off Limits) as additional `AccordionSection`s using the same vocabulary as Edit Contact, composing existing editors — do NOT auto-spawn rows (explicit "Add another"). Wire validation so a malformed populated optional field (e.g. an invalid email) reveals + focuses its containing accordion with field-level actionable text rather than silently dropping content; an unchanged form exits without a discard prompt. Keep one form-level Save.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/create-contact-logic.test.ts</automated>
    <human-check>On the Pixel: Show More reveals enrichment sections; entering an invalid email and Save reveals + focuses the Contact Methods section with actionable error text and does not clear the form.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green.
    - Show More toggles the enrichment sections; a malformed optional field blocks Save and reveals its section (human-check).
  </acceptance_criteria>
  <done>Contact Methods + Show More enrichment compose existing editors; validation reveals the relevant accordion; unchanged form exits silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input → create-contact-logic → createContactFull | Untrusted name/methods/custom values cross into persistence. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08 | Tampering | createContactFull writes | high | mitigate | All writes route through createContactFull's parameterized composition + recency cores in one txn (V5); the screen inlines no SQL. |
| T-34-09 | Tampering | future / malformed last-spoke date | medium | mitigate | Shared future-date guard (FUTURE_DATE_MESSAGE) rejects future dates in UI exactly as the DAO does; "not yet" writes no row (never a null-dated interaction). |
| T-34-10 | Repudiation/integrity | partial contact on interrupted create | medium | mitigate | Single atomic transaction (ADR-016); rollback leaves no partial contact. |

No package installs this phase — package-legitimacy gate N/A (zero new dependencies; AccordionSection is an in-repo primitive).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- create-contact-logic tests assert the tri-state and Bound/Unbound behavior.
- On-device UAT (end-of-phase): three-section Add Contact, Show More, name-only save → Profile, "Not yet" → no interaction.
</verification>

<success_criteria>
Add Contact is a quiet three-section accordion form with Show More; Name-only saves and routes to Profile; the tri-state last-spoke control and Bound/Unbound-with-optional-cadence semantics are preserved exactly (CAPT-01/02/03/14).
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/ui/AccordionSection.tsx` — reusable controlled accordion primitive (consumed by 34-05).
- `src/screens/create-contact-logic.ts` — extended Bound/Unbound coordination helper (`canSave`/`firstInteractionOccurredAt`/`buildCreateInput` retained).
- `src/screens/CreateContactScreen.tsx` — restructured into Identity / Relationship Basics / Contact Methods + Show More.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **CAPT-02 `unclassified` edge (probe) — surfaced not dropped.** The probe could not classify a data-shape edge for CAPT-02; the governing behavior (today/date/not-yet; "not yet" writes no interaction row) is fully covered by the must_haves truths above and the create-contact-logic tests. No separate must_haves entry authored for the unclassified edge by design.
- **CAPT-14 `unclassified` edge (probe) — surfaced not dropped.** CAPT-14's failure/validation/dirty-state contract is covered as UI-SPEC error/partial truths and the prohibition on false completion; the unclassified shape edge has no additional authored criterion.
</flagged_assumptions>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-03-SUMMARY.md` when done.
</output>
