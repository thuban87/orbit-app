---
phase: 34-rapid-capture-update-flows
plan: 08
type: execute
wave: 3
depends_on: ["34-03", "34-05"]
files_modified:
  - src/screens/EditContactScreen.tsx
autonomous: true
requirements: [CAPT-04, CAPT-14]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-04 complete-record IA + CAPT-14 dirty-state)
    - "Edit Contact exposes the complete editable record as direct-access top-level accordion sections (Identity, Relationship Basics, Contact Methods, Last Talked About, Key People/Relationships, Current Location, Memories, Custom Fields, Off Limits) using the same vocabulary as Add Contact's expanded state (CAPT-04, dossier §E)."
    - "The five knowledge subdomains are directly findable as top-level editing sections — NOT buried in a second nested accordion — composing the SAME canonical editors ThingsToRememberScreen uses: MemoryEditor (Memories), RelationshipEditor (Key People/Relationships), FuelEditor (Off Limits), and current-state value inputs (Last Talked About, Current Location). No editor is rebuilt (CAPT-04, dossier §E/§AN)."
    - "Every knowledge-subdomain section actually PERSISTS: its draft feeds the single Save through 34-05's extended updateContactFull (which composes the *Core writers atomically) — no editor renders whose value would silently fail to save (CAPT-04, Review cycle-2 HIGH 34-05; the cycle-1 silent-non-persistence hazard is closed)."
    - "Edit Contact keeps a SINGLE form-level Save button (not per-drawer saving); the existing deliberate TWO-TRANSACTION boundary is retained — metadata + all knowledge subdomains via updateContactFull, THEN links via applyLinkDiff (EditContactScreen.tsx:415, verified on disk) — so this is one Save at the button level over two transactions, NOT one collapsed transaction (dossier §E/§AD, dossier line 553)."
    - "Multiple sections may remain open simultaneously (dossier §E)."
    - "Each knowledge subdomain ROUND-TRIPS through the screen: entering/changing a value in its section and pressing Save persists it, and reopening Edit Contact shows the change — the POSITIVE assertion replacing the cycle-1 no-op `grep -ci ThingsToRemember` gate (CAPT-04, Review cycle-2 HIGH/LOW 34-05)."
    - "A validation error reveals + focuses the containing accordion via the AccordionSection validation interface (sectionId / expanded / first-invalid-field ref) driven by edit-contact-logic's resolveErrorSection (34-05); leaving after meaningful unsaved edits prompts Discard changes / Keep editing, while an unchanged form exits with no prompt (CAPT-14, dossier §AD/§AF; useDiscardKeepGuard)."
    # UI-SPEC ## UI Considerations — surface E6 (Edit Contact accordion)
    - "empty (E6): Edit Contact always shows the full record; a section with no value renders empty and editable (UI-SPEC, covered)."
    - "error (E6): a save failure renders locked \"Couldn't save / Please try again.\", preserves form state, never shows completion; a blocking validation error reveals + focuses the containing accordion with field-level actionable text (UI-SPEC, covered)."
    - "partial (E6): optional fields absent is a valid saved state everywhere in the record; the retained two-transaction boundary means a links-only failure keeps committed metadata+knowledge and re-seeds for retry without false completion (UI-SPEC, covered)."
    - "loading (E6): Edit Contact hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "long-text (E6): long names, notes, memory values, relationship names, and custom-field names truncate or wrap without breaking layout at large OS text scale across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT re-nest the knowledge subdomains inside a second accordion solely to mirror Profile grouping — direct-access top-level editing is the product requirement (CAPT-04, dossier §E)."
    - "MUST NOT render a knowledge-subdomain editor whose value would not persist through the single Save — every section routes to 34-05's extended updateContactFull (Review cycle-2 HIGH 34-05)."
    - "MUST NOT collapse the metadata+knowledge and links writes into one transaction — retain the deliberate two-transaction boundary (updateContactFull then applyLinkDiff, EditContactScreen.tsx:415) so a links failure leaves committed metadata+knowledge intact for retry (dossier §AE)."
    - "MUST NOT silently discard meaningful unsaved edits — the discard/keep guard fires only after meaningful edits and an unchanged form exits with no prompt (CAPT-14, dossier §AF)."
    - "MUST NOT collapse Update Contact's fast editors into Edit Contact or vice-versa merely because fields overlap — Edit Contact owns exhaustive identity/admin editing (e.g. Category), Update Contact owns fast relationship-knowledge capture (dossier §A/§X)."
  artifacts:
    - src/screens/EditContactScreen.tsx
  key_links:
    - "EditContactScreen composes the AccordionSection primitive + validation interface (34-03) with the canonical editors: existing ContactMethodsEditor/LinksEditor/FrequencyPicker/FieldValueInput/TriStateLastSpoke PLUS MemoryEditor/RelationshipEditor/FuelEditor/current-state inputs (the same set ThingsToRememberScreen composes)."
    - "Single Save builds the input via edit-contact-logic.buildEditInput (34-05, now carrying the knowledge-subdomain payloads) and persists via updateContactFull (metadata + knowledge, one txn) THEN applyLinkDiff (links, second txn) — EditContactScreen.tsx:415 boundary retained."
    - "Validation reveal-and-focus uses resolveErrorSection (34-05) + the AccordionSection validation interface (34-03); useDiscardKeepGuard provides meaningful-dirty-state protection."
---

<objective>
Restructure `EditContactScreen` into direct-access top-level accordion sections exposing the COMPLETE canonical editable record (dossier §E) — all nine sections — so every knowledge subdomain is a first-class top-level editing section rather than being absent or buried, and preserve the failure/validation/dirty-state contract (CAPT-14). This is the SCREEN half of the cycle-2 HIGH: it consumes 34-05's extended `updateContactFull` (which now persists Memories, Key People/Relationships, Last Talked About, Current Location, Off Limits) and 34-03's `AccordionSection` primitive + validation interface.

`EditContactScreen` + `edit-contact-logic.ts` already exist (the real edit path, `updateContactFull`, `useDiscardKeepGuard`, the seeded↔draft dirty signature). The knowledge editors already exist too — `MemoryEditor`, `RelationshipEditor`, `FuelEditor`, and the current-state value pattern are all composed today by `ThingsToRememberScreen` (the reference implementation). This plan is IA restructuring + composing those existing editors into the single-Save edit path — NOT new persistence (that shipped in 34-05) and NOT new editors.

Per D-01/D-02 the dossier (§E, §AD, §AF, and line 553 "one form-level Save") is ground truth. Per D-10 the Contact Knowledge model gates the editable sections — compose the canonical editors, do not invent an Edit-Contact-specific schema. Depends on 34-05 (extended DAO + edit-contact-logic assembly/resolver) and 34-03 (AccordionSection + validation interface).

Purpose: make Edit Contact's "complete editable record" real and discoverable — every §E subdomain editable in place, saved atomically under one Save.
Output: restructured `EditContactScreen` with top-level accordion sections, single Save over the retained two-transaction boundary, reveal-and-focus validation, and the dirty-state guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
@docs/dossier/milestone-2/phase-13-rapid-capture-update-flows-dossier.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Restructure EditContactScreen into top-level accordion sections + wire ONE knowledge section (Memories) end-to-end edit → Save → reload</name>
  <files>src/screens/EditContactScreen.tsx</files>
  <reversibility rating="reversible">IA restructure over the existing edit path; revisable.</reversibility>
  <read_first>
    - src/screens/EditContactScreen.tsx (1031 lines — current flat layout; imports ContactMethodsEditor, LinksEditor, FrequencyPicker, FieldValueInput, TriStateLastSpoke, PhotoSourcePicker; useDiscardKeepGuard at 536; updateContactFull at 69; the TWO-TRANSACTION boundary at 415 — metadata via updateContactFull then links via applyLinkDiff; the seeded↔draft dirty signature at 146-173 + 523-534)
    - src/screens/ThingsToRememberScreen.tsx (the reference implementation composing MemoryEditor + editMemory/addMemory — the list-then-edit-in-place pattern for the Memories section draft state)
    - src/screens/edit-contact-logic.ts (34-05 — buildEditInput now carries the memories payload; resolveErrorSection; canSave; seedEditState)
    - src/components/MemoryEditor.tsx (the Memories editor to compose; MemoryDraft/initialDraft)
    - src/components/ui/AccordionSection.tsx (34-03 — sectionId / controlled expanded / first-invalid-field ref; the section container)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Typography heading role for section headers; Spacing lg between groups; a11y expanded/collapsed)
  </read_first>
  <action>
    Restructure `EditContactScreen` so the record renders as top-level `AccordionSection`s (Identity, Relationship Basics, Contact Methods, Custom Fields become sections; the existing Bound/Unbound + Frequency + Photo + Name + Category + Birthday + Social battery + toggles map into Identity / Relationship Basics). Then wire ONE knowledge subdomain — Memories — end-to-end: compose `MemoryEditor` (seeded from the contact's existing Memories, draft state like the methods/links drafts) inside a top-level Memories `AccordionSection`, feed the draft into `buildEditInput`'s memories payload (34-05), and keep the SINGLE Save routing through `updateContactFull` (metadata + knowledge, one txn) THEN `applyLinkDiff` (links, second txn) — the deliberate two-transaction boundary at EditContactScreen.tsx:415 is retained, not collapsed. Keep the tracer thin: full record renders as top-level sections and a Memory edited in its section saves through updateContactFull and shows on reload. Replace touched raw `Text` with `AppText` roles; all colours via theme tokens. The remaining four knowledge sections land in Task 2.
  </action>
  <verify>
    <automated>npm run check:colors && npm test</automated>
    <human-check>On the Pixel: Edit Contact shows the record as top-level accordion sections including a top-level Memories section; adding/editing a Memory in its section and pressing the single Save persists it and returns; reopening Edit Contact shows the Memory (round-trip).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green (edit-contact-logic + contacts-dao suites from 34-05 included).
    - EditContactScreen imports AccordionSection from `@/components/ui/AccordionSection` and MemoryEditor from `@/components/MemoryEditor`.
    - `grep -c "updateContactFull" src/screens/EditContactScreen.tsx` ≥ 1 AND `grep -c "applyLinkDiff" src/screens/EditContactScreen.tsx` ≥ 1 (single Save retains the two-transaction boundary — metadata+knowledge then links).
    - Memory round-trips through the edit path (human-check: edit → Save → reopen shows it), backed by 34-05's contacts-dao round-trip test.
  </acceptance_criteria>
  <done>Edit Contact renders the record as direct-access top-level accordion sections; the Memories section edits in place and saves through updateContactFull over the retained two-transaction boundary, round-tripping on reload.</done>
</task>

<task type="auto">
  <name>Task 2: Add the remaining knowledge sections (Key People/Relationships, Last Talked About, Current Location, Off Limits) as top-level accordions — each persisting through the single Save</name>
  <files>src/screens/EditContactScreen.tsx</files>
  <reversibility rating="reversible">IA composition over the extended edit path; revisable.</reversibility>
  <read_first>
    - src/screens/EditContactScreen.tsx (Task 1 accordion structure + single-Save wiring + draft-state pattern)
    - src/screens/ThingsToRememberScreen.tsx (RelationshipEditor composition + addRelationship/editRelationship; setCurrentStateValue usage ~274; FuelEditor/off-limits composition — the exact editors + draft patterns to reuse)
    - src/components/RelationshipEditor.tsx (Key People/Relationships editor)
    - src/components/FuelEditor.tsx (Off Limits editor; fuel kind off_limits)
    - src/db/current-state-history-read.ts (getCurrentStateValues — seed for Last Talked About + Current Location sections)
    - src/db/memory-registry.ts (CURRENT_STATE_FIELD_KEYS last_talked_about/current_location + their display names)
    - src/screens/edit-contact-logic.ts (34-05 — buildEditInput carries relationships/currentStateEntries/offLimits payloads assembled from seed+draft)
  </read_first>
  <action>
    Add the remaining four knowledge subdomains as top-level `AccordionSection`s composing the EXISTING editors (do NOT rebuild them): Key People/Relationships via `RelationshipEditor`, Off Limits via `FuelEditor` (kind off_limits), and Last Talked About + Current Location via the current-state value inputs — seeding each section's draft from the contact's read (existing memories/relationships/current-state/fuel) exactly as the reference screen in read_first does. Each section's draft feeds `buildEditInput`'s corresponding payload so the SINGLE Save persists it through `updateContactFull` (one metadata+knowledge txn) — the links two-transaction boundary (applyLinkDiff) is unchanged. Extend the seeded↔draft dirty signature so edits to these sections mark the form dirty. Replace the cycle-1 no-op grep gate with a POSITIVE round-trip proof per subdomain. All colours via theme tokens; section headers use AppText heading role.
  </action>
  <verify>
    <automated>npm run check:colors && npm test</automated>
    <human-check>On the Pixel: Edit Contact shows top-level Key People/Relationships, Last Talked About, Current Location, and Off Limits sections; entering a value in EACH and pressing the single Save persists all of them (reopening Edit Contact shows every change); a name-only edit with no knowledge changes still saves.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green.
    - EditContactScreen imports RelationshipEditor and FuelEditor and composes current-state value inputs for last_talked_about/current_location.
    - `grep -c "ThingsToRemember\|Things to Remember" src/screens/EditContactScreen.tsx` returns 0 (no nested Things-to-Remember drawer as an editing container) AND each knowledge subdomain round-trips through Save (human-check: edit each → Save → reopen shows it) — the positive assertion that replaces the cycle-1 no-op gate (Review cycle-2 HIGH/LOW 34-05).
    - All nine top-level sections render (Identity, Relationship Basics, Contact Methods, Last Talked About, Key People/Relationships, Current Location, Memories, Custom Fields, Off Limits) with one form-level Save (human-check).
  </acceptance_criteria>
  <done>All five knowledge subdomains are top-level accordion sections composing existing editors, each round-tripping through the single Save over the retained two-transaction boundary; the complete §E record is editable in place.</done>
</task>

<task type="auto">
  <name>Task 3: Reveal-and-focus validation + meaningful dirty-state guard + failure-safety (CAPT-14)</name>
  <files>src/screens/EditContactScreen.tsx</files>
  <reversibility rating="reversible">Validation/guard wiring; revisable.</reversibility>
  <read_first>
    - src/screens/EditContactScreen.tsx (Task 1/2 accordion structure; existing handleSave + reseedMetadataAfterPartialSave at 358-383; hasUnsavedChanges signature at 523-534; useDiscardKeepGuard at 536)
    - src/screens/edit-contact-logic.ts (34-05 — resolveErrorSection(errors, sectionFieldMap) + the Edit Contact section↔field map)
    - src/components/ui/AccordionSection.tsx (34-03 — validation interface: sectionId, controlled expanded/onExpandedChange, first-invalid-field ref/scroll target)
    - src/navigation/discard-keep-guard.ts (meaningful-dirty-state protection contract)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Discard changes / Keep editing; Couldn't save / Please try again; error rows)
  </read_first>
  <action>
    Wire reveal-and-focus: on a blocked Save call `resolveErrorSection` (34-05) to find the sectionId owning the first blocking error, then expand that `AccordionSection` via `onExpandedChange` and scroll to its registered first-invalid-field ref, showing field-level actionable text — rather than leaving the user hunting collapsed drawers. Confirm `useDiscardKeepGuard` fires Discard changes / Keep editing ONLY after meaningful edits (extend the seeded↔draft signature to cover the knowledge-subdomain drafts) and that an unchanged form exits with no prompt. On save failure preserve state, show "Couldn't save / Please try again.", never show completion; retain the existing partial-save recovery (metadata+knowledge committed, links failed → reseed + keep links draft for retry, no false completion). Keep one form-level Save.
  </action>
  <verify>
    <automated>npm run check:colors && npm test</automated>
    <human-check>On the Pixel: entering an invalid value in a collapsed section and Save expands + focuses that section with actionable error text; backing out of an unchanged form exits silently; backing out after edits (including a knowledge-section edit) prompts Discard/Keep; a forced save failure keeps the form and shows the failure copy.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green (including 34-05's resolveErrorSection unit coverage).
    - `grep -c "resolveErrorSection" src/screens/EditContactScreen.tsx` ≥ 1 (validation routes through the tested resolver).
    - Validation reveals the containing accordion; an unchanged form (including no knowledge-section edits) exits with no prompt; an edit to any knowledge section marks the form dirty (human-check).
  </acceptance_criteria>
  <done>Edit Contact reveals + focuses the erroring accordion via the tested resolver, guards meaningful dirty state across all sections, and is failure-safe with the partial-save recovery intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edit form draft → updateContactFull / applyLinkDiff | Untrusted edited fields (metadata + five knowledge subdomains + links) cross into the contacts / knowledge / links tables. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-26 | Tampering | single-Save write path | high | mitigate | The screen inlines no SQL; metadata + all knowledge subdomains persist through 34-05's parameterized updateContactFull (one txn, `?`-bound, scoped by contact id V4/V5), links through applyLinkDiff (second txn); no bare cores from the screen. |
| T-34-27 | Repudiation/data-loss | silent discard of unsaved edits / false completion | medium | mitigate | useDiscardKeepGuard prompts only on meaningful dirty state (signature extended to the knowledge drafts); save failure preserves form state and never shows completion; the two-transaction boundary keeps committed metadata+knowledge on a links-only failure (reseed + retry, no false completion). |

No package installs this phase — package-legitimacy gate N/A (composes existing editors + the 34-03 AccordionSection + the 34-05 DAO extension; zero new dependencies).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- On-device UAT (end-of-phase): nine top-level accordion sections; each knowledge subdomain edits in place and round-trips through the single Save; validation reveal-and-focus; dirty-state guard; failure-safety + partial-save recovery.
</verification>

<success_criteria>
Edit Contact exposes the complete §E record as direct-access top-level accordion sections composing existing editors, with one form-level Save persisting metadata + all five knowledge subdomains atomically (updateContactFull) then links (applyLinkDiff) over the retained two-transaction boundary, each subdomain round-tripping; validation reveals the erroring section via the tested resolver and the dirty-state guard fires only on meaningful edits (CAPT-04/14).
</success_criteria>

<artifacts_this_phase_produces>
- `src/screens/EditContactScreen.tsx` — restructured into direct-access top-level accordion sections (all nine §E sections) composing the existing canonical editors (ContactMethodsEditor/LinksEditor/FrequencyPicker/FieldValueInput/TriStateLastSpoke + MemoryEditor/RelationshipEditor/FuelEditor/current-state inputs); single form-level Save over the retained two-transaction boundary; reveal-and-focus validation via resolveErrorSection + the AccordionSection validation interface; meaningful dirty-state guard across all sections.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **CAPT-04 `unclassified` edge (probe) — surfaced not dropped.** CAPT-04's IA contract (direct-access top-level accordions, no nested Things-to-Remember, complete editable record) is covered by the must_haves truths + prohibitions; the probe's unclassified shape edge has no additional authored criterion.
- **§E scope correction (five knowledge subdomains, not three) — see 34-05.** This screen surfaces all five as top-level editable sections because 34-05 persists all five; the correction to the owner's premise is surfaced in the planning return.
</flagged_assumptions>

<review_findings_addressed>
Cross-AI review (34-REVIEWS.md, cycle 2) findings incorporated into this plan (the SCREEN half of the 34-05 HIGH):
- **HIGH (claude — Edit Contact adds knowledge-subdomain sections with NO persistence path, on a false "nested drawer" premise):** this new plan restructures EditContactScreen into direct-access top-level accordion sections composing the existing editors (MemoryEditor/RelationshipEditor/FuelEditor/current-state inputs), each feeding the SINGLE Save that persists through 34-05's extended updateContactFull (metadata+knowledge, one txn) then applyLinkDiff (links, retained two-transaction boundary). The false "remove the nested Things-to-Remember drawer" framing is dropped (there is none in EditContactScreen); the cycle-1 no-op `grep -ci ThingsToRemember` gate is replaced by POSITIVE per-subdomain round-trip proof. No editor renders whose value would not persist.
- **MEDIUM (codex — reveal-and-focus validation has no testable focus contract):** Task 3 drives the AccordionSection validation interface (34-03) with the pure resolveErrorSection resolver (34-05), so reveal-and-focus is backed by a node-tested resolver, not human-check only.
</review_findings_addressed>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-08-SUMMARY.md` when done.
</output>
