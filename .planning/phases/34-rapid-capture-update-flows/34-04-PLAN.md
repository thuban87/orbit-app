---
phase: 34-rapid-capture-update-flows
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/screens/log-interaction-logic.ts
  - src/screens/log-interaction-logic.test.ts
  - src/screens/LogInteractionScreen.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/components/UniversalFab.tsx
autonomous: true
requirements: [CAPT-07, CAPT-08, CAPT-09, CAPT-10, CAPT-11, CAPT-13, CAPT-14]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-07/08/09/10/11 consumer + D-04/D-06/D-08/D-09/D-11)
    - "Detailed Log Interaction fills the LogContact route and exposes date/time (default now, freely backdateable with no age warnings), Channel, Direction, Connected where meaningful, optional Tone, Note, Allow AI, and Duration under More Options — composing TouchpointRefineForm (CAPT-07, dossier §N/§W)."
    - "The Channel chooser offers exactly Message / Call / In Person; Direction defaults Outbound for Message/Call and Mutual for In Person; Connected defaults Yes and is hidden for In Person; once the user overrides Direction the form does not re-fight it on unrelated re-renders (CAPT-08, dossier §R/§S)."
    - "Tone offers Positive / Neutral / Negative, is optional, defaults null/unset, and an omitted Tone is never coerced to Neutral (CAPT-09, D-08, dossier §T)."
    - "The Allow AI toggle sits with the Note field, defaults OFF (allow_ai=0 via coerceAllowAi), survives save, and stays editable via canonical Edit Interaction (CAPT-10, D-04, ADR-078)."
    - "The form initializes Channel from the Default Interaction Channel preference — a fixed pref selects that channel, 'remember' reads remembered_interaction_channel (Message first-use fallback); remembered_interaction_channel updates ONLY after a successful ordinary save, never on cancel/failure (CAPT-11, D-09)."
    - "All interaction writes go through the recency spine (recordTouchpoint / editTouchpointFull composed with recomputeLastContactCore in one transaction); the screen inlines no SQL and calls no bare …Core (ADR-010/024/071)."
    - "The user-facing form/CTA reads \"Log Interaction\" (D-11) while the internal route id LogContact and 'log-contact' enum are unchanged."
    - "A preselected contactId skips the picker; an untargeted invocation uses ContactPicker; a History-originated invocation with prefillDate prefills exactly that day (CAPT-13, dossier §AC/§W)."
    - "A failed save preserves form state, never shows completion, and exposes Retry where recoverable (CAPT-14, dossier §AE)."
    # Specless edge probe — CAPT-08 / CAPT-10 / CAPT-11 (consumer) / CAPT-13
    - "On open the Channel initializes from the Default Interaction Channel preference (or Message fallback), never empty; the chooser offers exactly Message/Call/In Person (CAPT-08 empty edge)."
    - statement: "Channel/Direction/Connected defaulting compares against the exact migrated stored channel literals ('Message'/'Call'/'In Person'), never legacy or free-text values (CAPT-08 encoding edge)."
      verification: backstop
    - statement: "Allow AI is a strict per-interaction boolean via coerceAllowAi — any non-explicit value resolves OFF (0); ON requires an explicit 1/true (CAPT-10 adjacency edge)."
      verification: backstop
    - "Allow AI defaults OFF (allow_ai=0) for a new interaction and when unset; it survives save and stays editable (CAPT-10 empty edge)."
    - statement: "Persisting Allow AI is idempotent — saving the same interaction twice with the toggle unchanged does not flip or drift allow_ai (CAPT-10 idempotency edge)."
      verification: backstop
    - statement: "allow_ai is written within the interaction's single recency-spine transaction; a concurrent edit never leaves allow_ai inconsistent with its row (CAPT-10 concurrency edge)."
      verification: backstop
    - "remembered_interaction_channel updates only after a successful ordinary (non-group) save; a cancelled/failed form never mutates it; re-saving the same channel is idempotent (CAPT-11 idempotency edge; D-09)."
    - statement: "The remembered-channel write is confined to the successful-save path; an interrupted save that did not persist the interaction leaves remembered_interaction_channel unchanged (CAPT-11 concurrency edge)."
      verification: backstop
    - "When the invoking context supplies contactId, Log Interaction preselects that contact and skips the picker; re-navigating with the same param preselects the same contact (CAPT-13 idempotency edge)."
    - statement: "Preselection reads the route param at mount; a History-originated Log Interaction with prefillDate prefills exactly that day and no other; an untargeted invocation shows the ContactPicker (CAPT-13 concurrency edge)."
      verification: backstop
    # UI-SPEC ## UI Considerations — surface E3 (Detailed Log Interaction)
    - "error (E3): future interaction date/time uses locked FUTURE_DATETIME_MESSAGE in danger text keeping the prior selection; save failure preserves state and never shows completion (UI-SPEC, covered)."
    - "partial (E3): omitted Tone shows no selection and is never Neutral; optional fields absent is a valid saved interaction (UI-SPEC, covered)."
    - "loading (E3): the form hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "long-text (E3): long notes and contact names truncate or wrap without breaking layout at large OS text scale across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT ship the Allow AI toggle default-ON, and MUST NOT route a Group Note through the per-interaction Allow AI toggle — either widens AI egress beyond the owner-ratified gate; Group Notes are never AI-eligible under any Allow AI value (D-04, ADR-078, STOP-AND-ASK)."
    - "MUST NOT silently interpret an omitted Tone as Neutral — an unrated interaction (quality NULL) stays distinguishable from an explicitly-Neutral one (D-08)."
    - "MUST NOT mutate remembered_interaction_channel from a cancelled/failed save or from Group Log — only a successful ordinary save updates it (D-09)."
    - "MUST NOT expand ai-context-read to transmit interaction notes — the note column is deliberately never selected there; note egress is future work outside this phase (ADR-078, RESEARCH anti-pattern)."
    - "MUST NOT dismiss the form or show completion when persistence fails (CAPT-14, dossier §AE)."
  artifacts:
    - src/screens/log-interaction-logic.ts
    - src/screens/log-interaction-logic.test.ts
    - src/screens/LogInteractionScreen.tsx
  key_links:
    - "LogInteractionScreen composes TouchpointRefineForm (controlled) + ContactPicker; defaulting lives in log-interaction-logic, NOT in the form."
    - "Save path: recordTouchpoint/editTouchpointFull → recomputeLastContactCore (one txn) THEN updateAppSettings({rememberedInteractionChannel}) only on success (34-01 DAO)."
    - "DashboardStack registers LogContact → LogInteractionScreen (replacing LogContactPlaceholderScreen)."
---

<objective>
Fill the `LogContact` route with the real detailed Log Interaction screen (CAPT-07/08/09/10), wire the Default Interaction Channel preference consumer (CAPT-11, reading 34-01's columns), and deliver the Log-Interaction slices of preselection (CAPT-13) and failure-safety (CAPT-14). The screen composes the existing shipped `TouchpointRefineForm` (channel/direction/connected/tone/note/allowAi/duration + visibleFields) — it does NOT clone it. The genuinely-new logic is the channel-sensitive Direction/Connected defaulting and the preference/remembered-value coordination, which live in a new node-tested `log-interaction-logic.ts` (Wave-0 gap).

This is the phase's privacy-critical surface. Per D-04/ADR-078 the Allow AI toggle ships default OFF and Group Notes are never AI-eligible; per D-08 omitted Tone is never Neutral; per D-09 the remembered channel updates only on a successful ordinary save. All writes route through the recency spine (ADR-010/024/071). Per D-11 user-facing copy reads "Log Interaction" while the internal `LogContact` route id stays (renaming it is owner-scope — STOP-AND-ASK).

Purpose: the canonical ordinary detailed interaction authoring form, honest about consent and Tone.
Output: `log-interaction-logic.ts` + test; `LogInteractionScreen.tsx`; route registration; "Log Interaction" copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
@docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: log-interaction-logic.ts — channel defaulting + pref resolution + remembered-write gate (pure, node-tested)</name>
  <files>src/screens/log-interaction-logic.ts, src/screens/log-interaction-logic.test.ts</files>
  <reversibility rating="reversible">New pure logic module; revisable. Encodes D-04/D-08/D-09 rules — a regression reverses a recorded decision, so treat the rule set as fixed.</reversibility>
  <read_first>
    - src/components/TouchpointRefineForm.tsx (CHANNEL_OPTIONS = Message/Call/In Person/other/unspecified line 76-88; TONE_OPTIONS Positive/Neutral/Negative; direction enum outbound|inbound|mutual + connected 0/1 line 85, 287-301)
    - src/components/touchpoint-refine-logic.ts (coerceAllowAi 176-178; combineDateAndTime 69-83; future-datetime guard; FUTURE_DATETIME_MESSAGE)
    - src/screens/create-contact-logic.ts (the RN-screen ↔ pure-logic split analog: build + gate helpers)
    - src/db/app-settings-dao.ts (getAppSettings/updateAppSettings + defaultInteractionChannel/rememberedInteractionChannel from 34-01)
    - RESEARCH.md Pattern 2 (defaultsForChannel skeleton) + dossier §R/§S (Direction/Connected defaults)
  </read_first>
  <behavior>
    - defaultsForChannel("In Person") → { direction:"mutual", connectedHidden:true, connected:1 }.
    - defaultsForChannel("Message") and ("Call") → { direction:"outbound", connectedHidden:false, connected:1 }.
    - Applying channel defaults is guarded: once the user has explicitly overridden Direction, a channel change does not re-fight the override (a userOverrodeDirection flag is respected).
    - resolveInitialChannel(pref) → the fixed channel when pref ∈ {Message,Call,In Person}; when pref==='remember' → the remembered value; falls back to "Message" when remembered is somehow absent.
    - shouldUpdateRemembered({ saveSucceeded, isGroupLog }) → true only when saveSucceeded && !isGroupLog; false on cancel/failure and for Group Log.
    - An omitted Tone stays null (never mapped to "Neutral"); allowAi resolves 0 unless explicitly 1/true (delegates to coerceAllowAi).
  </behavior>
  <action>
    Create `src/screens/log-interaction-logic.ts` (react-native-free) exporting `defaultsForChannel`, `resolveInitialChannel`, `shouldUpdateRemembered`, and a `buildLogInteractionInput` builder that assembles the recency-spine input from the controlled form value (Tone passthrough null, allowAi via coerceAllowAi, duration optional). Reuse `combineDateAndTime` / the future-datetime guard from `touchpoint-refine-logic` — do not re-author date math. Write `log-interaction-logic.test.ts` covering the full behavior block (channel enum literals verbatim from TouchpointRefineForm). This module is the correctness core the screen delegates to.
  </action>
  <verify>
    <automated>npx vitest run src/screens/log-interaction-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/log-interaction-logic.test.ts` exits 0.
    - Test asserts defaultsForChannel("In Person").connectedHidden === true and direction "mutual"; Message/Call → "outbound".
    - Test asserts shouldUpdateRemembered returns false for isGroupLog:true and for saveSucceeded:false; true only for successful ordinary save.
    - Test asserts an omitted Tone remains null (not "Neutral") and allowAi defaults 0.
  </acceptance_criteria>
  <done>The pure defaulting/pref/remembered/Tone/allowAi rules are node-tested green; the screen has a correctness core to compose.</done>
</task>

<task type="auto">
  <name>Task 2: LogInteractionScreen.tsx — compose TouchpointRefineForm end-to-end through the recency spine</name>
  <files>src/screens/LogInteractionScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <reversibility rating="reversible">New screen wiring; revisable.</reversibility>
  <read_first>
    - src/screens/placeholders/FabActionPlaceholders.tsx (LogContactPlaceholderScreen being replaced, lines 32-39 — note it uses raw Text; the real screen uses AppText roles)
    - src/navigation/types.ts (LogContact: { contactId?: number; prefillDate?: string })
    - src/navigation/tabs/DashboardStack.tsx (registration line 45)
    - src/components/TouchpointRefineForm.tsx + touchpoint-refine-logic.ts (visibleFields allow-list; allowAi; duration under More Options)
    - src/components/ContactPicker.tsx (untargeted contact selection)
    - src/db/recency-dao.ts (recordTouchpoint / editTouchpointFull — mutexed wrappers, lines 200-502; never a bare …Core)
    - src/db/interaction-edit-read.ts (readInteractionForEdit seeds an edit)
    - src/screens/log-interaction-logic.ts (Task 1)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Save; Allow AI label + caption; Color: Allow AI is an ordinary Switch on surface, NOT accent; Tone never status hues)
  </read_first>
  <action>
    Create `src/screens/LogInteractionScreen.tsx` filling the `LogContact` route. It reads `{ contactId?, prefillDate? }`; when `contactId` is absent it shows `ContactPicker`, else preselects. It initializes the form Channel via `resolveInitialChannel(getAppSettings().defaultInteractionChannel, …remembered…)`, applies `defaultsForChannel` on channel change (respecting a user-override flag), prefills the date from `prefillDate` when present else `localDateTime()` now. It composes `TouchpointRefineForm` with `visibleFields`, placing the Allow AI toggle (default OFF, ordinary Switch on `surface`, caption "Let AI use this note's text. Off by default.") with the Note field, Tone optional (no selection ≠ Neutral), Duration under More Options. Save builds the input via `buildLogInteractionInput`, writes via `recordTouchpoint`/`editTouchpointFull`, and — only on success and only for ordinary logging — calls `updateAppSettings({ rememberedInteractionChannel })`; on failure it preserves state, shows "Couldn't save / Please try again." and Retry, never completion. Register the screen in `DashboardStack.tsx` (LogContact → LogInteractionScreen). All colours via theme tokens.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/log-interaction-logic.test.ts</automated>
    <human-check>On the Pixel: Log Interaction from a Profile preselects the contact; switching Channel to In Person flips Direction to Mutual and hides Connected; Allow AI is OFF by default; a successful save updates the remembered channel and returns to the Profile; a forced failure keeps the form and shows Retry.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0 (no new colour literals; Allow AI Switch not accent-tinted).
    - `grep -c "recomputeLastContact\|insertInteractionCore" src/screens/LogInteractionScreen.tsx` returns 0 (no bare cores; writes go through mutexed wrappers).
    - DashboardStack registers LogContact → LogInteractionScreen (grep confirms the component swap).
    - `grep -c "ai-context-read" src/screens/LogInteractionScreen.tsx` returns 0 (no egress expansion).
  </acceptance_criteria>
  <done>Log Interaction works end-to-end: preselect/picker, channel-sensitive defaults, Allow AI OFF, recency-spine save, remembered-channel-on-success, failure-safe.</done>
</task>

<task type="auto">
  <name>Task 3: "Log Interaction" user-facing copy (D-11) — keep internal LogContact id</name>
  <files>src/components/UniversalFab.tsx, src/screens/LogInteractionScreen.tsx</files>
  <reversibility rating="reversible">Copy-only change; revisable.</reversibility>
  <read_first>
    - src/components/UniversalFab.tsx (line 53 `LogContact: { accessibilityLabel: "Log Contact" }` — user-facing; line 61 navigate union)
    - src/screens/LogInteractionScreen.tsx (Task 2 — screen title)
    - .planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md (D-11: canonical name "Log Interaction"; "Log Contact" legacy)
  </read_first>
  <action>
    Change the user-facing FAB accessibility label in `UniversalFab.tsx` from "Log Contact" to "Log Interaction", and set the `LogInteractionScreen` title to "Log Interaction". Do NOT rename the `LogContact` route id, the `'log-contact'` stored enum, or the navigate union member — those are internal identifiers (renaming is owner-scope, STOP-AND-ASK #6). Only user-visible strings change.
  </action>
  <verify>
    <automated>npm run check:colors</automated>
    <human-check>On the Pixel: the FAB action and screen title read "Log Interaction"; navigation still works (route id unchanged).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Log Contact" src/components/UniversalFab.tsx` returns 0 (user-facing label updated).
    - `grep -c "\"LogContact\"\|'LogContact'\|LogContact:" src/navigation/types.ts` unchanged (route id preserved).
    - `npm test` full suite green.
  </acceptance_criteria>
  <done>User-facing copy reads "Log Interaction"; internal LogContact identifiers untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input → recency spine | Untrusted note/channel/tone/duration cross into the interactions table. |
| interaction note → AI-egress reader | Whether a note becomes AI-eligible is gated per-interaction by Allow AI. |
| Group Note → AI | Absolute ban regardless of any Allow AI value. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-11 | Information disclosure | Allow AI default / egress | high | mitigate | Allow AI ships OFF (coerceAllowAi → 0 unless explicit); note egress not expanded here; Group Notes never routed through the toggle (D-04/ADR-078). |
| T-34-12 | Tampering | interaction write path | high | mitigate | Writes go through recency-spine mutexed wrappers in one txn; `?`-bound params; scoped by id AND contact_id (V4/V5); no bare …Core. |
| T-34-13 | Repudiation/integrity | Tone coercion | medium | mitigate | Omitted Tone persists NULL; never coerced to Neutral (D-08) — preserves explicit-vs-missing audit distinction. |
| T-34-14 | Tampering | remembered channel mutation | low | mitigate | shouldUpdateRemembered gates the write to successful ordinary saves only; cancel/failure/Group Log never mutate it. |
| T-34-15 | Access control | preselected contact resolution | medium | mitigate | The screen scopes the interaction to the resolved contactId; reads/writes bound by id AND contact_id (V4). |

No package installs this phase — package-legitimacy gate N/A (composes existing primitives; zero new dependencies).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- log-interaction-logic tests assert channel defaults, Tone-null, allowAi-off, remembered-on-success-only.
- On-device UAT (end-of-phase): preselect, In-Person Direction/Connected behavior, Allow AI OFF, backdate, remembered-channel update, failure-preserves-state.
</verification>

<success_criteria>
The detailed Log Interaction form is live on the LogContact route with Message/Call/In Person channel + channel-sensitive Direction/Connected defaults, optional never-Neutral Tone, Allow-AI-OFF-with-Note, Duration under More Options, preference-seeded channel with remembered-on-success-only, preselection/prefillDate, failure-safety, and "Log Interaction" copy (CAPT-07/08/09/10/11/13/14).
</success_criteria>

<artifacts_this_phase_produces>
- `src/screens/log-interaction-logic.ts` — exports `defaultsForChannel`, `resolveInitialChannel`, `shouldUpdateRemembered`, `buildLogInteractionInput`.
- `src/screens/LogInteractionScreen.tsx` — new screen filling the `LogContact` route.
- `src/navigation/tabs/DashboardStack.tsx` — LogContact now renders LogInteractionScreen.
- `src/components/UniversalFab.tsx` — user-facing label "Log Interaction" (route id unchanged).
</artifacts_this_phase_produces>

<flagged_assumptions>
- **CAPT-07 `unclassified` edge (probe) — surfaced not dropped.** CAPT-07's field-set contract is covered by the must_haves truths (date/time/channel/direction/connected/tone/note/allowAi/duration) and the log-interaction-logic tests; no additional shape-edge criterion is authored.
- **CAPT-09 `unclassified` edge (probe) — surfaced not dropped.** CAPT-09's Tone contract (optional, null default, never Neutral) is fully covered by a must_haves truth + the D-08 prohibition; no additional shape-edge criterion.
- **CAPT-10 `ordering` edge (probe) — inapplicable, surfaced not dropped.** Allow AI is a per-row boolean with no collection ordering; no ordering contract to assert.
- **CAPT-11 `ordering` edge (probe) — inapplicable, surfaced not dropped.** The channel preference is a scalar, not an ordered collection; the ordering-of-choices in the SegmentedControl is a fixed presentational order (Message/Call/In Person), not a data ordering contract.
</flagged_assumptions>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-04-SUMMARY.md` when done.
</output>
