---
phase: 34-rapid-capture-update-flows
plan: 07
type: execute
wave: 3
depends_on: ["34-02", "34-04"]
files_modified:
  - src/screens/update-contact-chooser-logic.ts
  - src/screens/update-contact-chooser-logic.test.ts
  - src/screens/UpdateContactScreen.tsx
  - src/screens/MemoryScreen.tsx
  - src/navigation/tabs/DashboardStack.tsx
autonomous: true
requirements: [CAPT-06, CAPT-12, CAPT-13, CAPT-14]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-06/12 + D-10 knowledge model + D-11 default type)
    - "Update Contact opens a compact chooser after the contact is known — built-in rows Last Talked About, Key People/Relationships, Current Location, Memory, Off Limits, Contact Method, Contact Frequency, plus applicable named custom fields and a generic Custom Fields row (CAPT-12, dossier §Y/§Z)."
    - "Selecting an item opens a focused editor; its inner Save returns to the chooser with the SAME contact targeted; the user updates more items and exits only via Done — the session is not a rigid checklist (CAPT-12, dossier §AA)."
    - "Each inner save persists independently (not one giant transaction); individual updates persist as saved (CAPT-12, dossier §AA)."
    - "The full Memory creation/editing experience lives in Update Contact's Memory editor: type is selected INSIDE the editor and less-common metadata (label, note, date, URL, pinning, visibility) sits behind More Options. The 'Allow AI to use this' control is EDIT-only — it appears when editing an existing Memory (MemoryEditor.tsx:384-411 → setMemoryAllowAi), not during creation (CAPT-06, dossier §K)."
    - "New-Memory AI permission on CREATE is registry-default-only (decision, Review cycle-2 MEDIUM 34-07): addMemoryCore derives allow_ai from MEMORY_TYPE_REGISTRY[type].aiDefault (memories-dao.ts:94), which is `false` on disk for general/custom/imported → a newly created Memory is Allow-AI OFF, consistent with D-04's default-OFF posture. The create path does NOT add a new create-time AI-permission control and does NOT extend MemoryDraft / NewMemoryInput / MemoryEditor's create mode; a user grants AI permission by saving the Memory, then editing it (which already exposes the control). This resolves cycle-1's incomplete 'AI permission behind More Options on creation' claim WITHOUT widening egress (D-04, ADR-078)."
    - "MemoryScreen supports both create AND edit-in-place: it lists the contact's existing Memories (listMemoriesForContact) plus an Add affordance; selecting an existing Memory seeds the editor via initialDraft(item) and saves through editMemory (updates in place, no duplicate), while Add saves through addMemory; either inner Save RETURNS to the Update Contact chooser with the same contact targeted. The `Memory` route stays `{ contactId? }` — selection happens in-screen, so no memoryId route param is required (Review MEDIUM 34-07)."
    - "The Memory editor requests/uses types through the registry (MEMORY_TYPE_REGISTRY, DEFAULT_MEMORY_TYPE_KEY) — never a hardcoded type ID or display name (D-11)."
    - "Category is NOT an Update Contact action — it stays in Edit Contact (CAPT-12, dossier §X/§Y)."
    - "Update Contact preselects the contact when the invoking context identifies one (CAPT-13, dossier §AC)."
    - "A failed inner save preserves that editor's state and never shows completion (CAPT-14, dossier §AE)."
    # Specless edge probe — CAPT-06 / CAPT-12
    - statement: "Saving the Memory editor with unchanged fields does not create a duplicate Memory; addMemory creates exactly one row per save and editMemory updates in place (CAPT-06 idempotency edge)."
      verification: backstop
    - statement: "Memory add/edit each run as a single DAO transaction; a concurrent/interrupted save never leaves a Memory with a partially-applied metadata patch (CAPT-06 concurrency edge)."
      verification: backstop
    - statement: "The chooser always shows the built-in rows plus applicable named custom-field rows; an inapplicable named custom-field row is omitted while the generic Custom Fields row always remains — never zero rows (CAPT-12 adjacency edge)."
      verification: backstop
    - "The chooser is never empty — built-in rows always render; Category is never present (CAPT-12 empty edge)."
    - statement: "Named custom-field rows are keyed/displayed by the canonical custom-field definition name (no duplicate schema); multi-byte/long names render without creating duplicate fields (CAPT-12 encoding edge)."
      verification: backstop
    - statement: "Chooser row order is deterministic and stable across re-entry within a session (built-ins in a fixed order, then applicable named custom fields) (CAPT-12 ordering edge)."
      verification: backstop
    - "After each inner Save the chooser returns with the same contact targeted (session does not exit); Done exits (CAPT-12 idempotency edge)."
    - statement: "Each inner-editor save is its own independent persistence; an interrupted inner save affects only that item and leaves prior saved items intact (CAPT-12 concurrency edge)."
      verification: backstop
    # UI-SPEC ## UI Considerations — surfaces E4 (Update chooser) + E5 (Memory editor)
    - "empty (E4/E5): the Update chooser is never truly empty (built-in rows always present); the Memory editor opens empty with the default type preselected by key (UI-SPEC, covered)."
    - "error (E4/E5): a save failure renders the locked \"Couldn't save / Please try again.\", preserves state, never shows completion, and exposes Retry where recoverable (UI-SPEC, covered)."
    - "partial (E4/E5): optional fields absent is a valid saved state; the chooser omits inapplicable named custom-field rows while keeping the generic Custom Fields row (UI-SPEC, covered)."
    - "populated (E4): the happy path is the full built-in action list plus any applicable named custom-field rows (UI-SPEC, covered)."
    - "loading (E5): the Memory editor hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "overflow (E4): with many custom fields the chooser stays bounded and scrollable (rank/search/group/collapse rather than an unbounded flat wall) with no clipped/unreachable rows (UI-SPEC, backstop)."
      verification: backstop
    - statement: "long-text (E4/E5): long custom-field names, Memory values, and preview strips truncate or wrap without breaking layout at large OS text scale across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
    - statement: "zero-one-many (E4): the repeated-update session reads correctly at zero, one, and many completed updates, returning with a subtle recent-success indication rather than a rigid checklist (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT include Category (or other identity/admin editing) in Update Contact — those stay in Edit Contact; Update Contact owns fast relationship-knowledge capture only (dossier §X/§Y)."
    - "MUST NOT make Update Contact a rigid session-completion checklist or wrap the whole session in one giant transaction — each inner save persists independently and Done ends the session (dossier §AA)."
    - "MUST NOT flatten Contact Knowledge into generic text to simplify chooser routing, and MUST NOT hardcode storage tables into navigation — the chooser is driven by the semantic Contact Knowledge registry (dossier §Y/§AN)."
    - "MUST NOT treat Last Talked About as a generic Memory type — it is a distinct first-class chooser action over the retained-history model (dossier §L)."
    - "MUST NOT default a newly-created Memory's allow_ai ON, and MUST NOT add a create-time AI-permission control or extend MemoryDraft/NewMemoryInput to set allow_ai at creation — create uses the registry default (OFF); AI permission is granted only by editing an existing Memory (D-04, ADR-078, Review cycle-2 MEDIUM 34-07)."
  artifacts:
    - src/screens/update-contact-chooser-logic.ts
    - src/screens/update-contact-chooser-logic.test.ts
    - src/screens/UpdateContactScreen.tsx
    - src/screens/MemoryScreen.tsx
  key_links:
    - "UpdateContactScreen fills the UpdateContact route; its Memory row navigates to the Memory route (MemoryScreen) with contactId and returns to the chooser."
    - "MemoryScreen lists the contact's Memories (listMemoriesForContact) + Add, composing the existing MemoryEditor (type inside + More Options metadata); Add→addMemory, existing→editMemory (in place, no duplicate); each Save returns to the chooser. The Memory route stays contactId-only (selection in-screen, no memoryId param)."
    - "update-contact-chooser-logic builds chooser rows from the Contact Knowledge registry + applicable custom-field defs (applicability filtering), never a hardcoded table."
    - "DashboardStack registers UpdateContact → UpdateContactScreen and Memory → MemoryScreen (replacing the placeholders); ordered after 34-04's LogContact swap (shared file)."
---

<objective>
Fill the `UpdateContact` and `Memory` routes with the real Update Contact chooser loop (CAPT-12) and the full Memory editor (CAPT-06), plus Update Contact's preselection (CAPT-13) and failure-safety (CAPT-14). Both routes are placeholders today (verified on disk). The chooser is a compact action surface driven by the Contact Knowledge registry (dossier §Y) that returns to itself after each inner save until Done; the Memory editor composes the existing shipped `MemoryEditor` (type inside, metadata behind More Options) — it is not cloned. The chooser's row-assembly/applicability filtering lives in a new node-tested `update-contact-chooser-logic.ts` (Wave-0 gap).

Per D-01/D-02 the dossier (§K, §L, §X, §Y, §Z, §AA) is ground truth. Per D-10 the Contact Knowledge model gates these surfaces — compose the registry, do not plan against an unbuilt or flattened model. Per D-11 Memory types are requested through the registry. This depends on 34-02 (reconciled registry) and is ordered after 34-04 (shared `DashboardStack.tsx` route-registry file).

Purpose: fast, repeatable relationship-knowledge capture and the canonical full Memory editor, without the Edit Contact form.
Output: `update-contact-chooser-logic.ts` + test; `UpdateContactScreen.tsx`; `MemoryScreen.tsx`; route registrations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: update-contact-chooser-logic.ts + end-to-end chooser → Memory editor → back to chooser</name>
  <files>src/screens/update-contact-chooser-logic.ts, src/screens/update-contact-chooser-logic.test.ts, src/screens/UpdateContactScreen.tsx, src/screens/MemoryScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <reversibility rating="reversible">New pure logic module + screen skeleton; revisable.</reversibility>
  <read_first>
    - src/screens/placeholders/FabActionPlaceholders.tsx (UpdateContactPlaceholderScreen 50-57; MemoryPlaceholderScreen 59 — being replaced)
    - src/navigation/types.ts (UpdateContact: { contactId? } line 59; Memory: { contactId? } line 60)
    - src/navigation/tabs/DashboardStack.tsx (registration lines 47-51 — swap after 34-04's LogContact change)
    - src/components/MemoryEditor.tsx (type inside 44-71; initialDraft 162; More-Options metadata)
    - src/db/memories-dao.ts (addMemory 296; editMemory 308)
    - src/db/memory-registry.ts (MEMORY_TYPE_REGISTRY, DEFAULT_MEMORY_TYPE_KEY; contact-knowledge field registry for chooser rows)
    - dossier §Y (built-in chooser rows), §Z (custom-field discoverability), §AA (repeated-update session)
  </read_first>
  <behavior>
    - buildChooserRows(contact, applicableDefs) returns the fixed built-in rows in a stable order (Last Talked About, Key People, Current Location, Memory, Off Limits, Contact Method, Contact Frequency), then applicable named custom-field rows, then the generic Custom Fields row — never Category, never zero rows.
    - An inapplicable named custom field is omitted while the generic Custom Fields row still appears.
    - Row order is deterministic across repeated calls.
    - The chooser session state supports "return to chooser with same contact after save" and "Done exits" (a pure reducer/selector, not React).
  </behavior>
  <action>
    Create `src/screens/update-contact-chooser-logic.ts` (react-native-free) exporting a `ChooserRow` type, `buildChooserRows(contact, applicableDefs)` (registry-driven built-ins + applicability-filtered named custom fields + generic Custom Fields row; excludes Category), and a small session selector for the return-to-chooser/Done loop. Then create a thin `UpdateContactScreen` filling the `UpdateContact` route that renders the chooser rows, and a `MemoryScreen` filling the `Memory` route that composes `MemoryEditor`; wire the chooser's Memory row → navigate to Memory route with contactId → inner Save returns to the chooser. Register both in `DashboardStack.tsx` (after 34-04's LogContact swap). Keep the tracer thin: chooser → Memory editor → save → back-to-chooser → Done. Write `update-contact-chooser-logic.test.ts` covering the behavior block.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/update-contact-chooser-logic.test.ts</automated>
    <human-check>On the Pixel: Update Contact (preselected) opens the chooser; Memory → editor → Save returns to the chooser with the same contact; Done exits.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/update-contact-chooser-logic.test.ts` exits 0.
    - Test asserts buildChooserRows never includes Category, always includes the generic Custom Fields row, and is order-stable across calls.
    - `npm run check:colors` exits 0.
    - DashboardStack registers UpdateContact → UpdateContactScreen and Memory → MemoryScreen (grep confirms the component swaps).
  </acceptance_criteria>
  <done>The chooser row model is node-tested; the chooser → Memory editor → back-to-chooser → Done loop works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Full Memory editor in Update Contact (type inside + More Options metadata) — CAPT-06</name>
  <files>src/screens/MemoryScreen.tsx</files>
  <reversibility rating="reversible">Screen composition over the existing editor; revisable.</reversibility>
  <read_first>
    - src/components/MemoryEditor.tsx (MemoryDraft/MemoryEditPatch/MemoryEditorProps 44-71; onAdd/onEdit 64-65; type picker; More-Options metadata; initialDraft(item) 162 seeds an EDIT; the "Allow AI to use this" control renders ONLY when `editing` is truthy at lines 384-411 — it is EDIT-only, verified on disk; do NOT add a create-mode AI-permission control, Review cycle-2 MEDIUM 34-07)
    - src/screens/ThingsToRememberScreen.tsx (the established list-then-edit-in-place pattern: MemoryEditor 613/660 + editMemory 305/355 — the analog for selecting and editing an existing Memory)
    - src/db/memories-read.ts (listMemoriesForContact 54 — the contact's existing Memories for selection)
    - src/db/memories-dao.ts (addMemory 296; editMemory 308; setMemoryAllowAi 319; assertRegisteredMemoryType; addMemoryCore at :94 derives allow_ai from MEMORY_TYPE_REGISTRY[type].aiDefault — the create-time AI posture, NOT a caller field)
    - src/db/memory-registry.ts (types by key; label from 34-02: general "Memory", custom "Custom"; aiDefault is `false` for general/custom/imported → new Memories are Allow-AI OFF)
    - src/navigation/types.ts (Memory: { contactId? } line 60 — stays contactId-only; selection is in-screen, no memoryId param needed)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: More Options; Memory-editor title heading role; Privacy contract for the Memory AI-permission control)
  </read_first>
  <action>
    Build `MemoryScreen` (Memory route) around `MemoryEditor` so the full CREATE and EDIT experience lives here (CAPT-06). Route contract (Review MEDIUM 34-07): the screen receives `{ contactId? }` only; it lists the contact's existing Memories via `listMemoriesForContact` plus an "Add Memory" affordance. Selecting an existing Memory opens the editor seeded by `initialDraft(item)` and saves through `editMemory` (updates in place — no duplicate); Add opens the editor empty and saves through `addMemory` — mirroring the shipped ThingsToRememberScreen pattern. Memory Type is selected INSIDE the editor (no separate type-chooser page), content is the primary field, and less-common metadata (label, note, meaningful date, URL, pinning, Profile visibility) sits behind More Options per the Contact Knowledge registry, requesting types by registry key. AI permission is EDIT-only (Review cycle-2 MEDIUM 34-07): on CREATE, allow_ai comes from the registry default (`MEMORY_TYPE_REGISTRY[type].aiDefault`, currently OFF) via `addMemoryCore` — do NOT add a create-time "Allow AI" control and do NOT extend MemoryDraft/NewMemoryInput to carry allow_ai; the "Allow AI to use this" control appears only when editing an existing Memory (MemoryEditor's existing `editing`-gated control → `setMemoryAllowAi`). Either inner Save RETURNS to the Update Contact chooser with the same contact targeted; no memoryId route param is added (selection is in-screen). On failure preserve state and show the locked failure copy. All colours via theme tokens.
  </action>
  <verify>
    <automated>npm run check:colors && npm test</automated>
    <human-check>On the Pixel: from Update Contact → Memory, the screen lists existing Memories + Add; Add creates a Memory (type inside, metadata behind More Options); selecting an existing Memory edits it IN PLACE (no duplicate); each Save returns to the chooser with the same contact.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green.
    - `grep -c "\"General\"\|PROVISIONAL_" src/screens/MemoryScreen.tsx` returns 0 (types by key, not literal names).
    - MemoryScreen reads listMemoriesForContact for selection and routes Add→addMemory / existing→editMemory; a test/human-check confirms editing an existing Memory updates in place (no duplicate) and returns to the chooser.
    - Memory type selection is inside the editor (no separate type-chooser route added); the Memory route param is unchanged (contactId-only).
    - A newly created Memory takes its AI posture from the registry default (aiDefault=false → OFF for general/custom) via addMemoryCore — MemoryScreen adds NO create-time AI-permission control and does NOT extend MemoryDraft/NewMemoryInput to set it; the MemoryEditor "Allow AI" control is `editing`-gated and renders only when editing an existing Memory (human-check + memory-registry aiDefault fact, Review cycle-2 MEDIUM 34-07).
  </acceptance_criteria>
  <done>MemoryScreen lists + creates + edits-in-place the contact's Memories via addMemory/editMemory (type inside, metadata behind More Options), returning to the chooser after each save, with no route-param change.</done>
</task>

<task type="auto">
  <name>Task 3: Chooser rows → focused editors + repeated-update session (CAPT-12/13/14)</name>
  <files>src/screens/UpdateContactScreen.tsx</files>
  <reversibility rating="reversible">Screen wiring; revisable.</reversibility>
  <read_first>
    - src/screens/UpdateContactScreen.tsx (Task 1 skeleton)
    - src/screens/update-contact-chooser-logic.ts (Task 1 row model + session selector)
    - src/components/ (FrequencyPicker, ContactMethodsEditor, LinksEditor, TriStateLastSpoke/Last-Talked-About, FieldValueInput — the focused editors composed per row)
    - src/components/ContactPicker.tsx (untargeted invocation)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Done; empty-state chooser row list; error rows; §AA subtle recent-success indication)
  </read_first>
  <action>
    Wire each chooser row to its focused editor (Last Talked About, Key People, Current Location, Off Limits, Contact Method, Contact Frequency, named custom fields, generic Custom Fields) composing the canonical editors. Each inner Save persists independently and returns to the chooser with the same contact targeted and a subtle recent-success indication; Done (tertiary/accentText) exits the session. Preselect the contact from the route param; when untargeted, use `ContactPicker`. A failed inner save preserves that editor's state and never shows completion. Do NOT add Category. Reuse the canonical Contact Frequency control (Bound/Unbound follows the canonical lifecycle rules, dossier §AB). All colours via theme tokens.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/update-contact-chooser-logic.test.ts</automated>
    <human-check>On the Pixel: select several rows in sequence (e.g. Current Location, then a named custom field), each Save returns to the chooser with a recent-success cue; Done exits; a contact with no applicable named custom fields still shows the generic Custom Fields row; Category is absent.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npm test` full suite green.
    - `grep -ci "Category" src/screens/UpdateContactScreen.tsx` shows Category is not a chooser action (absent or only in an exclusion comment allowlisted).
    - Repeated-update loop returns to the chooser after each save; Done exits (human-check).
  </acceptance_criteria>
  <done>The chooser loop routes every row to a focused editor, persists each save independently, returns to itself until Done, preselects the contact, and is failure-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chooser editor input → Memory/custom-field/frequency DAOs | Untrusted relationship-knowledge values cross into persistence. |
| preselected/target contact resolution | Update targets must be scoped to the resolved contact. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-21 | Tampering | Memory / knowledge writes | high | mitigate | addMemory/editMemory enforce registered types + provenance; custom-field/frequency writes go through canonical DAOs with `?`-bound params scoped by contact_id (V4/V5); no inline SQL. |
| T-34-22 | Access control | chooser row applicability + target scoping | medium | mitigate | Rows/edits are scoped to the resolved contactId; buildChooserRows filters by applicable defs (V4). |
| T-34-23 | Integrity | independent inner saves | medium | mitigate | Each inner save is its own transaction; an interrupted save affects only that item, leaving prior saves intact (dossier §AA). |
| T-34-24 | Information disclosure | Memory AI-permission control | medium | mitigate | A newly created Memory's allow_ai comes from the registry default (aiDefault=false → OFF) via addMemoryCore — no create-time control widens it; AI permission is granted only by editing an existing Memory (setMemoryAllowAi). No egress widening beyond the per-item gate (D-04, ADR-078, Review cycle-2 MEDIUM 34-07). |

No package installs this phase — package-legitimacy gate N/A (composes existing primitives; zero new dependencies).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- update-contact-chooser-logic tests assert row model (no Category, generic Custom Fields always present, order-stable, applicability filtering).
- On-device UAT (end-of-phase): chooser loop, Memory editor, repeated updates, Done, preselect, no Category.
</verification>

<success_criteria>
Update Contact is a compact registry-driven chooser that returns to itself after each independent inner save until Done, surfaces named custom fields directly plus a generic Custom Fields row (never Category), and hosts the full Memory editor with type inside and metadata behind More Options; preselection and failure-safety hold (CAPT-06/12/13/14).
</success_criteria>

<artifacts_this_phase_produces>
- `src/screens/update-contact-chooser-logic.ts` — exports `ChooserRow`, `buildChooserRows`, chooser session selector.
- `src/screens/UpdateContactScreen.tsx` — new screen filling the `UpdateContact` route.
- `src/screens/MemoryScreen.tsx` — new screen filling the `Memory` route (composes MemoryEditor).
- `src/navigation/tabs/DashboardStack.tsx` — UpdateContact → UpdateContactScreen, Memory → MemoryScreen.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **UI-SPEC E4 loading transient (⚠ unresolved) — flagged planner assumption.** While resolving the preselected contact (DAO read) there is no shipped loading pattern mandated; the planner assumes the local-first read completes fast enough to render the chooser immediately (consistent with the E4 populated/empty resolutions), and does NOT introduce a blocking spinner. If a perceptible delay appears on device, treat the transient treatment as an open UI decision for the owner.
- **Update Contact custom-field ranking/overflow threshold (RESEARCH Open Question 2 / dossier §Z DERIVED) — implementation tuning.** Exact ranking/search/group/collapse threshold for many custom fields is Claude's discretion; the overflow backstop above bounds the behavior.
</flagged_assumptions>

<review_findings_addressed>
Cross-AI review (34-REVIEWS.md, cycle 1) findings incorporated into this plan:
- **MEDIUM (codex — Memory full-edit route underspecified):** the route contract is now explicit — MemoryScreen (contactId-only route, unchanged) lists existing Memories via listMemoriesForContact + Add, selects an existing Memory to edit IN PLACE via editMemory (initialDraft(item)), creates via addMemory, and returns to the chooser after each save; no memoryId route param is needed because selection is in-screen (mirrors the shipped ThingsToRememberScreen pattern). Incorporated into must_haves, Task 2 read_first/action/verify/acceptance_criteria, and key_links, with an edit-in-place (no-duplicate) test/human-check.

Cross-AI review (34-REVIEWS.md, cycle 2) findings incorporated into this plan:
- **MEDIUM (codex — Memory AI-permission on creation unspecified):** decided registry-default-only on CREATE (allow_ai from `MEMORY_TYPE_REGISTRY[type].aiDefault`, verified `false`/OFF on disk for general/custom/imported), consistent with D-04's default-OFF posture; the "Allow AI to use this" control stays EDIT-only (MemoryEditor's `editing`-gated control → setMemoryAllowAi). The cycle-1 "AI permission behind More Options on creation" claim is dropped; MemoryDraft/NewMemoryInput/MemoryEditor's create mode are NOT extended. Incorporated into a dedicated must_haves truth, a new prohibition, Task 2 read_first/action/acceptance_criteria, and T-34-24.
</review_findings_addressed>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-07-SUMMARY.md` when done.
</output>
