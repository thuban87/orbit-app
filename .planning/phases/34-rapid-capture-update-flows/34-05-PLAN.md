---
phase: 34-rapid-capture-update-flows
plan: 05
type: execute
wave: 2
depends_on: ["34-03"]
files_modified:
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/screens/edit-contact-logic.ts
  - src/screens/edit-contact-logic.test.ts
autonomous: true
requirements: [CAPT-04]
user_setup: []

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-04 complete-record persistence + dossier §E + D-10 knowledge model)
    - "Edit Contact can PERSIST the complete canonical editable record (dossier §E): the FIVE knowledge subdomains that today have NO edit-path write — Memories, Key People/Relationships, Last Talked About, Current Location, Off Limits — become durable fields on UpdateContactFullInput and are written by updateContactFull (CAPT-04, dossier §E, owner decision 2026-09-12 'extend persistence / honor §E')."
    - "Scope correction verified on disk (owner premise refined, not reversed): the owner's directive named three missing subdomains believing LinksEditor covered Key People/Relationships and TriStateLastSpoke covered Last Talked About. On disk LinksEditor is EXTERNAL URL links (contact-links-dao/applyLinkDiff) and TriStateLastSpoke is the never-contacted FIRST-interaction control — neither is a knowledge subdomain. Honoring §E's complete-record requirement therefore extends the edit path for FIVE knowledge subdomains, not three: Key People/Relationships (relationships-dao), Last Talked About + Current Location (current_state field_keys last_talked_about/current_location), Memories (memories-dao), Off Limits (fuel kind off_limits)."
    - "Every supplied knowledge-subdomain edit is written INSIDE updateContactFull's SINGLE metadata inWriteTransaction (contacts-dao.ts:418), composing the existing exec-scoped *Core writers — addMemoryCore/editMemoryCore/deleteMemoryCore, addRelationshipCore/editRelationshipCore/deleteRelationshipCore, addFuelCore/editFuelCore/deleteFuelCore, setCurrentStateValueCore — never a bare wrapper that opens its own transaction (the shared mutex is NON-REENTRANT). This EXTENDS the metadata transaction; it does NOT collapse the deliberate two-transaction boundary where links persist separately via applyLinkDiff (EditContactScreen.tsx:415, retained by 34-08)."
    - "Collection subdomains (Memories, Key People/Relationships, Off Limits) use ONE pinned diff contract (Review cycle-3 LOW): the LOGIC layer (edit-contact-logic.buildEditInput, 34-05 Task 3) computes explicit `{add, edit, delete}` lists keyed by a row-identity `id` from seed-vs-draft; `updateContactFull` (the DAO) APPLIES those lists — adds via addMemoryCore/addRelationshipCore/addFuelCore, edits via editMemoryCore/editRelationshipCore/editFuelCore, deletes via deleteMemoryCore/deleteRelationshipCore/deleteFuelCore — so editing an existing row updates in place (no duplicate), a new row inserts, and a removed row deletes, all in the one transaction. The DAO never re-diffs; there is no 'diff shape OR lists' ambiguity across the task boundary."
    - "The aggregate edit transaction bumps `data_revision` EXACTLY ONCE per successful edit: `updateContactFull` is the SOLE bumper (it calls the composed `applyContactMethodDiffCore` with `bumpRevision:false`, the param 34-03 adds), so an edit that supplies unchanged method drafts but changes a knowledge subdomain — and a knowledge-only edit — still advances backup-freshness state exactly once, and no path double-bumps. Verified gap on disk: today updateContactFull self-bumps only when `methodSaveResult === null` (contacts-dao.ts:516) and applyContactMethodDiffCore bumps only when methods `changed` (contact-methods-dao.ts:277), so a knowledge-only edit with supplied-but-unchanged method drafts bumps nowhere (Review cycle-3 MEDIUM data_revision)."
    - "Current-state subdomains (Last Talked About, Current Location) write via setCurrentStateValueCore ONLY when the value changed from the stored current value — retaining prior values as history (never rewriting history); an unchanged value writes nothing."
    - "Each of the five knowledge subdomains ROUND-TRIPS through the edit path: a value applied via updateContactFull is read back by the contact's read path (contacts-dao.test.ts proves it per subdomain) — replacing the cycle-1 no-op `grep -ci ThingsToRemember` gate with a POSITIVE persistence assertion (Review cycle-2 HIGH 34-05)."
    - "An interrupted edit ROLLS BACK fully: a forced mid-transaction throw leaves the contact metadata unchanged AND no partial/orphan knowledge rows (memories/relationships/current_state/fuel) — one atomic metadata transaction, asserted by contacts-dao.test.ts (Review cycle-2 HIGH 34-05)."
    - "The lean edit path is preserved: an UpdateContactFullInput that supplies NO knowledge subdomains behaves exactly as today (metadata + custom values only) — the new fields are all OPTIONAL and omitting them writes zero knowledge rows."
    - "edit-contact-logic assembles the knowledge-subdomain payloads from the edit form's draft/seed state (pure, node-tested), and exposes the pure resolveErrorSection(errors, sectionFieldMap) resolver 34-08 uses for reveal-and-focus (Review cycle-2 MEDIUM 34-03/34-05)."
    # Reachability / non-reversal
    - "NO new migration and NO interactions change: every target table already exists on disk (memories, relationships, current_state_entries, fuel; migrations run 001..026) — verify head on disk before touching schema and do NOT invent a migration (CLAUDE.md forward-only; §E extension needs none)."
  prohibitions:
    - "MUST NOT narrow the §E complete-record requirement to fewer than the five knowledge subdomains, and MUST NOT render/accept a subdomain edit that would not persist — silent non-persistence on a shared table is the exact cycle-1 HIGH #4 hazard (dossier §E, Review cycle-2 HIGH 34-05, CLAUDE.md 'review the code')."
    - "MUST NOT open a second inWriteTransaction inside updateContactFull for the knowledge writes, and MUST NOT call the mutexed wrappers (addMemory/addRelationship/addFuel/setCurrentStateValue) from inside it — compose only the exec-scoped *Core writers (non-reentrant mutex; nesting is a permanent hang, RESEARCH Pitfall 1)."
    - "MUST NOT write last_contact from the knowledge composition — the single-writer invariant (DATA-04) is untouched; recompute stays gated exactly as today (rarely_responds flip OR first interaction)."
    - "MUST NOT rewrite current_state history to satisfy a value edit — setCurrentStateValueCore demotes the prior current row and inserts a new current row; never mutate historical rows to coerce a value (CLAUDE.md current-state history is a bounded local trace)."
    - "MUST NOT author a new migration or ALTER any table — all target tables exist; a redundant migration on un-reachable devices is the STOP-AND-ASK double-migration hazard (D-07, CLAUDE.md)."
  artifacts:
    - "src/db/contacts-dao.ts (UpdateContactFullInput extended with optional memories/relationships/currentStateEntries/offLimits payloads; updateContactFull composes their add/edit/delete *Core writers + setCurrentStateValueCore inside the single metadata transaction)"
    - src/db/contacts-dao.test.ts
    - "src/screens/edit-contact-logic.ts (knowledge-subdomain payload assembly + pure resolveErrorSection resolver)"
    - src/screens/edit-contact-logic.test.ts
  key_links:
    - "updateContactFull's single metadata inWriteTransaction (contacts-dao.ts:418) is where the five subdomains' *Core writers compose — mirroring 34-03's createContactFullCore enrichment composition; a *Core writer NOT composed there is the silent-non-persistence hazard."
    - "Collection diffs mirror applyContactMethodDiffCore (contacts-dao.ts already composes it) / applyLinkDiff (contact-links-dao); current-state uses setCurrentStateValueCore (current-state-history-dao) keyed by last_talked_about / current_location."
    - "34-08 (Edit Contact IA screen) consumes this extended DAO for its single Save and the edit-contact-logic payload assembly + resolveErrorSection resolver."
    - "`data_revision` bump ownership (edit): `updateContactFull` bumps EXACTLY ONCE (it passes `bumpRevision:false` — the optional param 34-03 adds to applyContactMethodDiffCore — and owns the single bump), so a knowledge-only edit and an unchanged-method edit both advance backup-freshness state without under- or double-bumping (Review cycle-3 MEDIUM)."
---

<objective>
Extend the EDIT persistence path (`updateContactFull`) so Edit Contact can persist the COMPLETE canonical editable record the dossier §E requires — specifically the FIVE knowledge subdomains that today have NO write path in the edit flow: **Memories, Key People/Relationships, Last Talked About, Current Location, Off Limits**. This mirrors, for the edit path, exactly what 34-03 did for the create path (`createContactFullCore` enrichment). This is the persistence FOUNDATION; the screen restructure that surfaces these as top-level accordion sections is 34-08 (which depends on this plan).

**Owner decision (authoritative, 2026-09-12): "Extend persistence (honor dossier §E)."** Do NOT narrow §E. **Scope correction verified on disk (per CLAUDE.md "review the code, not the diff"):** the owner's directive named three missing subdomains (Memories/Current Location/Off Limits) on the premise that `LinksEditor → applyLinkDiff` already covered Key People/Relationships and `TriStateLastSpoke` covered Last Talked About. On disk those premises don't hold — `LinksEditor` is external URL links (`contact-links-dao`) and `TriStateLastSpoke` is the never-contacted first-interaction control. "Last talked about" and "Current location" are BOTH `current_state` field_keys; "Off Limits" is `fuel` kind `off_limits`; "Key People/Relationships" is `relationships-dao`. So honoring §E's complete-record requirement extends the edit path for FIVE knowledge subdomains, not three. This refines the owner's factual premise; it does not reverse the decision (which is to honor §E fully). Surfaced to the owner in the planning return.

Per D-01/D-02 the dossier (§E, §AD, §AF) is ground truth. Per D-10 the Contact Knowledge model gates these subdomains — compose the canonical *Core writers, do not invent an Edit-Contact-specific schema. Per CLAUDE.md this ships NO migration: every target table already exists (verify head on disk).

Purpose: make Edit Contact's "complete editable record" honest — every knowledge subdomain it will expose actually saves, atomically, under one form-level Save.
Output: extended `UpdateContactFullInput` + `updateContactFull` composition; `edit-contact-logic` payload assembly + `resolveErrorSection`; contacts-dao round-trip + rollback tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
@docs/dossier/milestone-2/phase-13-rapid-capture-update-flows-dossier.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend updateContactFull to persist ONE knowledge subdomain (Memories) end-to-end — round-trip + rollback</name>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <reversibility rating="costly">Extends the ADR-016 atomic-edit DAO contract on a shared table; a regression risks a partial contact or silently-dropped knowledge. Preserve the single-transaction atomicity + single-writer last_contact invariant — do not re-derive them.</reversibility>
  <read_first>
    - src/db/contacts-dao.ts (UpdateContactFullInput 276-304 — the input to extend; updateContactFull 385-519 opens exactly ONE inWriteTransaction at :418 and composes updateContactMetadataCore + upsertValueCore×N + conditional recompute; applyContactMethodDiffCore is the in-file diff-composition analog at :504-515; createContactFullCore's 34-03 enrichment composition is the create-side analog to mirror)
    - src/db/memories-dao.ts (addMemoryCore 94, editMemoryCore 151, deleteMemoryCore 239 — exec-scoped, composable in a caller txn; NewMemoryInput shape; assertRegisteredMemoryType; allow_ai derives from the registry — NOT a create-time caller field)
    - src/db/memories-read.ts (listMemoriesForContact — the seed for the diff baseline + the round-trip read-back)
    - src/db/transaction.ts (inWriteTransaction non-reentrancy — never nest; compose *Core only)
    - src/db/data-revision-dao.ts (bumpDataRevisionCore — the single backup-freshness bump)
    - src/db/contact-methods-dao.ts (applyContactMethodDiffCore bumps only when methods `changed` at :277; 34-03 adds an optional `bumpRevision` param — updateContactFull passes `bumpRevision:false` and owns the single bump so a knowledge-only edit with unchanged supplied method drafts still bumps once)
    - src/db/contacts-dao.test.ts (the createContactFull enrichment round-trip + mid-transaction rollback tests from 34-03 are the exact test analog to mirror)
  </read_first>
  <behavior>
    - UpdateContactFullInput gains an OPTIONAL `memories` payload that is EXACTLY `{ add, edit, delete }` lists keyed by a row-identity `id` (pinned single contract, Review cycle-3 LOW — NOT "a diff shape OR lists"): `add` elements match addMemoryCore input minus the injected fields, `edit` elements match editMemoryCore input (carry `id`), `delete` elements carry `id`. The logic layer (Task 3 buildEditInput) computes these lists; the DAO only APPLIES them and never re-diffs. Omitting the payload writes zero memory rows and preserves today's behavior.
    - updateContactFull, inside its SINGLE existing inWriteTransaction, applies the memory lists: `add` via addMemoryCore, `edit` via editMemoryCore, `delete` via deleteMemoryCore — no new transaction opened, no mutexed wrapper called.
    - A memory added/edited/deleted via updateContactFull ROUND-TRIPS: after the call, listMemoriesForContact reflects the change.
    - A forced mid-transaction throw (e.g. an invalid memory input after the metadata UPDATE) ROLLS BACK the whole edit — contact metadata unchanged AND no partial memory rows.
    - last_contact is NOT written by the memory composition; the existing recompute gating (rarely_responds flip OR first interaction) is unchanged.
    - updateContactFull bumps `data_revision` EXACTLY ONCE per successful edit — it calls the composed `applyContactMethodDiffCore` with `bumpRevision:false` (the param 34-03 adds) and owns the single bump itself — so an edit with supplied-but-unchanged method drafts plus a changed knowledge subdomain (and a knowledge-only edit) still advances `data_revision` by exactly 1, and no path double-bumps.
  </behavior>
  <action>
    Extend `UpdateContactFullInput` (contacts-dao.ts) with an OPTIONAL `memories` payload that is EXACTLY `{ add, edit, delete }` lists keyed by row-identity `id` (the single pinned contract — the logic layer in Task 3 computes the lists, the DAO only applies them; do NOT accept a raw seed-vs-draft "diff shape" as an alternative). Have `updateContactFull` apply the lists via the exec-scoped cores — `add` → addMemoryCore, `edit` → editMemoryCore, `delete` → deleteMemoryCore — INSIDE the SAME `inWriteTransaction` that already runs `updateContactMetadataCore` + custom-value upserts (do NOT open a new transaction; do NOT call the mutexed `addMemory`/`editMemory`/`deleteMemory` wrappers — non-reentrant mutex). Keep the memory application AFTER the metadata UPDATE and BEFORE/around the existing method diff, entirely within the one metadata transaction — this extends the metadata transaction only; the links two-transaction boundary lives in the screen (34-08) and is untouched here. Make `updateContactFull` the SOLE `data_revision` bumper for the composed edit: bump EXACTLY ONCE (the metadata UPDATE is always a change), and call the composed `applyContactMethodDiffCore` with `bumpRevision:false` (the optional param 34-03 adds, default true for all other callers) so the method path no longer self-bumps inside the aggregate — closing the under-bump (a knowledge-only / unchanged-method edit advancing `data_revision` nowhere) and any double-bump. Mirror 34-03's create-side enrichment composition and the shipped applyContactMethodDiffCore diff pattern. Extend `contacts-dao.test.ts` to prove the memory round-trip (add/edit/delete each reflected by listMemoriesForContact), full rollback on a forced mid-transaction failure (no partial memory rows, metadata unchanged), and the exactly-once `data_revision` bump for a knowledge-only edit that supplies unchanged method drafts. Do NOT author a migration.
  </action>
  <verify>
    <automated>npx vitest run src/db/contacts-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/contacts-dao.test.ts` exits 0.
    - contacts-dao.test.ts asserts a memory add/edit/delete via updateContactFull round-trips through listMemoriesForContact AND that a forced mid-transaction failure rolls back the contact + memory rows (no partial state).
    - contacts-dao.test.ts asserts `data_revision` advances by EXACTLY 1 for a knowledge-only edit that supplies UNCHANGED method drafts and changes one memory (proving the bump fires), and by exactly 1 for an edit that also changes methods (proving no double-bump).
    - Atomicity is proven by the rollback round-trip test above (a forced mid-transaction failure leaves ZERO partial rows across contact + knowledge tables), NOT by a file-wide grep. Static guard replaces the fragile `awk '/updateContactFull/,/^}/' … | grep -c inWriteTransaction` (Review cycle-3 LOW — "updateContactFull" also appears in the file header comment :5-7 and the EDIT comment block :254-273, so the awk address range mis-starts): instead prove the single-transaction contract with a dedicated `contacts-dao.test.ts` assertion — e.g. spy/count `inWriteTransaction` (or BEGIN) invocations across one `updateContactFull` call and assert exactly 1 — with the forced-rollback round-trip remaining the authoritative atomicity proof. (If a static grep is still wanted, scope it to the function's explicit line range, not an open-ended awk range.)
    - `grep -cE "ALTER TABLE|migration0" src/db/contacts-dao.ts` returns 0 (no migration authored in the DAO).
  </acceptance_criteria>
  <done>updateContactFull persists Memories via composed *Core writers in its single metadata transaction, round-tripping and rolling back atomically; the composition contract is proven end-to-end at the DAO level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the remaining four knowledge subdomains (Relationships, Last Talked About, Current Location, Off Limits) in the same transaction</name>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <reversibility rating="costly">Same shared-table atomic-edit contract as Task 1; a regression risks orphan knowledge rows or silent non-persistence. Preserve single-transaction atomicity.</reversibility>
  <read_first>
    - src/db/contacts-dao.ts (Task 1's extended UpdateContactFullInput + updateContactFull composition — extend the SAME single transaction)
    - src/db/relationships-dao.ts (addRelationshipCore 68, editRelationshipCore 97, deleteRelationshipCore 146 — exec-scoped; NewRelationshipInput requires personName; assertNotSelfLink)
    - src/db/current-state-history-dao.ts (setCurrentStateValueCore 55 — demotes prior current row + inserts new current row, retaining history; keyed by fieldKey)
    - src/db/memory-registry.ts (CURRENT_STATE_FIELD_KEYS = last_talked_about, current_location — the two current-state field_keys; isCurrentStateFieldKey)
    - src/db/current-state-history-read.ts (getCurrentStateValue — the round-trip read-back + the changed-vs-stored comparison seed)
    - src/db/fuel-dao.ts (addFuelCore 131, editFuelCore 165, deleteFuelCore 228 — exec-scoped; NewFuelItem requires kind; FuelKind includes "off_limits" line 43)
    - src/db/relationships-read.ts / src/db/fuel-read.ts (the round-trip read-backs + diff seeds for relationships / off-limits fuel)
  </read_first>
  <behavior>
    - UpdateContactFullInput gains OPTIONAL payloads: `relationships` (`{add,edit,delete}` lists keyed by `id`, applied via addRelationshipCore/editRelationshipCore/deleteRelationshipCore), `currentStateEntries` (per field_key value for last_talked_about + current_location via setCurrentStateValueCore), and `offLimits` (fuel kind off_limits `{add,edit,delete}` lists keyed by `id`, applied via addFuelCore/editFuelCore/deleteFuelCore) — same pinned diff contract as Task 1's memories (logic computes lists, DAO applies); each optional; omitting all preserves the lean edit path.
    - All four apply INSIDE the same single metadata inWriteTransaction as Task 1's memories — one atomic edit.
    - setCurrentStateValueCore is called for a current-state field ONLY when the incoming value differs from the stored current value (getCurrentStateValue); an unchanged value writes nothing (no spurious history row).
    - Each subdomain round-trips: relationship add/edit/delete reflected by the relationships read; last_talked_about/current_location value reflected by getCurrentStateValue; off-limits fuel add/edit/delete reflected by the fuel read.
    - A forced mid-transaction throw in ANY subdomain rolls back the whole edit (no orphan rows in any table, metadata unchanged).
  </behavior>
  <action>
    Extend `UpdateContactFullInput` with OPTIONAL `relationships`, `currentStateEntries`, and `offLimits` payloads and compose their writes into the SAME single `inWriteTransaction` Task 1 uses: relationships and off-limits fuel by applying the `{add,edit,delete}` lists (computed by Task 3's buildEditInput, keyed by row-identity `id`) via their exec-scoped `*Core` writers (mirroring applyContactMethodDiffCore — the DAO applies, it does not re-diff), and the two current-state fields via `setCurrentStateValueCore` keyed by `last_talked_about` / `current_location`, called only when the value changed from `getCurrentStateValue`. The single `data_revision` bump (Task 1) covers these subdomains too — do not add per-subdomain bumps. Compose only the non-mutexed `*Core` writers — never a wrapper that opens its own transaction. Extend `contacts-dao.test.ts` to prove per-subdomain round-trip (add/edit/delete or value-change reflected by the corresponding read) and full rollback on a forced mid-transaction failure in each. Do NOT author a migration; do NOT write last_contact from these writes.
  </action>
  <verify>
    <automated>npx vitest run src/db/contacts-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/contacts-dao.test.ts` exits 0.
    - contacts-dao.test.ts asserts round-trip for EACH of relationships, last_talked_about, current_location, and off-limits fuel through updateContactFull, plus a forced-failure rollback leaving no orphan rows in any table.
    - Test asserts an UNCHANGED current-state value writes no new current_state row (setCurrentStateValueCore skipped when equal to stored).
    - `grep -c "setCurrentStateValueCore\|addRelationshipCore\|addFuelCore" src/db/contacts-dao.ts` ≥ 3 (all composed into the edit path).
    - `grep -cE "ALTER TABLE|migration0" src/db/contacts-dao.ts` returns 0 (no migration).
  </acceptance_criteria>
  <done>All five knowledge subdomains persist atomically through updateContactFull's single metadata transaction, each round-tripping and rolling back together; no migration, no last_contact write, current-state history preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: edit-contact-logic — knowledge-subdomain payload assembly + pure resolveErrorSection resolver</name>
  <files>src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts</files>
  <reversibility rating="reversible">Pure logic module + tests; revisable.</reversibility>
  <read_first>
    - src/screens/edit-contact-logic.ts (existing buildEditInput / canSave / seedEditState / isNeverContacted — the RN-free assembly the screen delegates to; the seed↔draft signature pattern EditContactScreen uses for dirty detection)
    - src/db/contacts-dao.ts (Task 1/2 extended UpdateContactFullInput — the payload shapes this module assembles)
    - src/screens/create-contact-logic.ts (34-03's resolveErrorSection + enrichment-array assembly — the analog to mirror for the edit surface)
    - src/components/ui/AccordionSection.tsx (34-03 validation interface — sectionId / expanded / first-invalid-field ref — the reveal-and-focus contract resolveErrorSection feeds)
  </read_first>
  <behavior>
    - buildEditInput (extended) assembles the OPTIONAL knowledge-subdomain payloads (memories/relationships/currentStateEntries/offLimits) from the edit form's seeded baseline + current draft, producing explicit `{ add, edit, delete }` lists keyed by row-identity `id` for the collections (the single pinned contract — the DAO applies these lists directly and never re-diffs) and per-field values for current-state — omitting a subdomain the user did not touch.
    - The assembly is a pure function of (seed, draft); an unchanged subdomain yields an empty/absent payload (no spurious writes), preserving the existing dirty-state signature semantics.
    - resolveErrorSection(errors, sectionFieldMap) returns the sectionId owning the first blocking validation error (deterministic order) and null when none — the same contract 34-03 defined, applied to the nine Edit Contact sections.
    - The module stays react-native-free and node-tested.
  </behavior>
  <action>
    Extend `edit-contact-logic.ts` (react-native-free) so `buildEditInput` assembles the OPTIONAL knowledge-subdomain payloads (memories/relationships/currentStateEntries/offLimits) from the edit form's seed + draft as explicit `{ add, edit, delete }` lists keyed by row-identity `id` (collections) and per-field values (current-state), omitting untouched subdomains — mirroring 34-03's create-side enrichment assembly and preserving the existing dirty-state signature approach. This is the SINGLE diff-ownership contract: the logic computes the lists here, `updateContactFull` applies them (Tasks 1/2) — do NOT leave the DAO to re-diff a raw seed+draft. Add the pure `resolveErrorSection(errors, sectionFieldMap)` resolver (and the Edit Contact section↔field map) that maps the first blocking validation error to its containing AccordionSection sectionId, matching 34-03's interface. Extend `edit-contact-logic.test.ts` for the assembly (touched → payload present; untouched → absent) and the error→section resolution. Keep `canSave`/`seedEditState`/`isNeverContacted` intact.
  </action>
  <verify>
    <automated>npx vitest run src/screens/edit-contact-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/edit-contact-logic.test.ts` exits 0.
    - Test asserts buildEditInput emits a knowledge-subdomain payload only when that subdomain's draft differs from its seed (untouched → omitted), and that a collection payload is `{ add, edit, delete }` keyed by row-identity `id` — an edited existing row lands in `edit` (not `add`, so no duplicate) and a removed row lands in `delete`.
    - Test asserts resolveErrorSection maps the first blocking error to its section id and returns null when there are no blocking errors.
    - `npm test` full suite green.
  </acceptance_criteria>
  <done>edit-contact-logic assembles the five knowledge-subdomain payloads from draft/seed and resolves errors to their section, all node-tested; 34-08 has a pure correctness core to compose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edit form draft → updateContactFull | Untrusted edited knowledge (memories/relationships/current-state/off-limits) crosses into shared per-contact tables. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-16 | Tampering | updateContactFull knowledge writes | high | mitigate | All knowledge writes route through parameterized exec-scoped *Core writers composed in updateContactFull's single transaction; `?`-bound; scoped by contact id (V4/V5); no inline SQL. assertRegisteredMemoryType / assertNotSelfLink / FuelKind guards hold. |
| T-34-17 | Repudiation/data-loss | partial contact / orphan knowledge on interrupted edit | high | mitigate | Single atomic metadata transaction composes every knowledge *Core write; a mid-transaction throw rolls back the contact + all knowledge rows (no partial/orphan state), asserted by contacts-dao.test.ts. last_contact single-writer (DATA-04) untouched. |
| T-34-25 | Integrity | current-state history rewrite | medium | mitigate | Current-state values write via setCurrentStateValueCore (demote prior current + insert new current) only on change; historical rows are never mutated to coerce a value. |

No package installs this phase — package-legitimacy gate N/A (composes existing in-repo DAO primitives; zero new dependencies; no migration).
</threat_model>

<verification>
- `npm test` green; contacts-dao + edit-contact-logic suites green.
- contacts-dao.test.ts proves each of the five knowledge subdomains round-trips through updateContactFull and rolls back atomically on a forced failure.
- No new migration file exists; no `interactions` change; `TARGET_VERSION` untouched.
</verification>

<success_criteria>
updateContactFull persists the five Edit Contact knowledge subdomains (Memories, Key People/Relationships, Last Talked About, Current Location, Off Limits) atomically inside its single metadata transaction — each round-tripping and rolling back together — and edit-contact-logic assembles the payloads + resolves errors to sections, all node-tested, with no migration and the last_contact single-writer invariant intact (CAPT-04, dossier §E).
</success_criteria>

<artifacts_this_phase_produces>
- `src/db/contacts-dao.ts` — `UpdateContactFullInput` extended with optional `memories` / `relationships` / `currentStateEntries` / `offLimits` payloads; `updateContactFull` composes their add/edit/delete `*Core` writers + `setCurrentStateValueCore` inside the single metadata transaction.
- `src/db/contacts-dao.test.ts` — per-subdomain round-trip + atomic-rollback coverage for the edit path.
- `src/screens/edit-contact-logic.ts` — extended `buildEditInput` knowledge-subdomain payload assembly + pure `resolveErrorSection` resolver (`canSave`/`seedEditState`/`isNeverContacted` retained).
- `src/screens/edit-contact-logic.test.ts` — assembly + error→section coverage.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **§E scope correction surfaced, not silently applied.** The owner's directive named three missing knowledge subdomains; on-disk verification shows five (LinksEditor ≠ Key People/Relationships; TriStateLastSpoke ≠ Last Talked About). This plan extends persistence for all five to honor §E's "complete editable record" without narrowing, and the correction is surfaced to the owner in the planning return for ratification — it refines the owner's premise, it does not reverse the decision.
- **Add/edit/delete diff depth (implementation shape) — planner's discretion within §E.** The knowledge collections use the shipped applyContactMethodDiffCore/applyLinkDiff seeded-vs-current diff pattern so existing rows edit in place and removed rows delete; the executor may extract small per-subdomain diff cores if cleaner. §E requires the record be editable, not merely appendable.
</flagged_assumptions>

<review_findings_addressed>
Cross-AI review (34-REVIEWS.md, cycle 2) findings incorporated into this plan:
- **HIGH (claude — 34-05 Edit Contact adds knowledge-subdomain sections with NO persistence path):** per the owner's authoritative 2026-09-12 decision ("extend persistence / honor §E"), 34-05 is repurposed from an IA-only plan into the EDIT-path PERSISTENCE foundation — `UpdateContactFullInput` + `updateContactFull` are extended to atomically persist the five knowledge subdomains via composed exec-scoped `*Core` writers inside the single metadata transaction (mirroring 34-03's create-side fix), with per-subdomain round-trip + rollback tests. The cycle-1 no-op `grep -ci ThingsToRemember` gate is replaced by POSITIVE persistence assertions. Scope corrected on disk from three subdomains to five (LinksEditor is URL links, not relationships; TriStateLastSpoke is the first-interaction control, not last_talked_about) and surfaced to the owner. The screen restructure moves to the new 34-08 (depends on this plan).
- **MEDIUM (codex — reveal-and-focus validation has no testable focus contract):** the pure `resolveErrorSection` resolver + Edit Contact section↔field map land in edit-contact-logic (Task 3) with node-level unit coverage, feeding the AccordionSection validation interface 34-08 drives — consistent with the same interface added in 34-03.

Cross-AI review (34-REVIEWS.md, cycle 3 FINAL) findings incorporated into this plan:
- **MEDIUM (codex — data_revision bump gap, edit):** verified on disk that `updateContactFull` self-bumps only when `methodSaveResult === null` (:516) and `applyContactMethodDiffCore` bumps only when methods `changed` (contact-methods-dao.ts:277), so a knowledge-only edit with supplied-but-unchanged method drafts bumps nowhere. Fixed by making `updateContactFull` the SOLE bumper (bumps exactly once; composes `applyContactMethodDiffCore` with `bumpRevision:false`, the optional param 34-03 introduces). Incorporated into a must_haves truth + key_link, Task 1 read_first/behavior/action/acceptance (knowledge-only-edit bump test + no-double-bump test) and Task 2 action (single bump covers all subdomains). No contact-methods-dao.ts edit needed here — 34-03 (wave 1) adds the `bumpRevision` param; this plan (wave 2, depends_on 34-03) only passes it at the updateContactFull call site.
- **LOW (claude — collection-diff ownership ambiguous):** the "seeded-vs-current diff shape OR {add,edit,delete} lists" is pinned to ONE contract — edit-contact-logic.buildEditInput (Task 3) computes explicit `{add,edit,delete}` lists keyed by row-identity `id`; `updateContactFull` (Tasks 1/2) applies them and never re-diffs. The "or" is removed from Task 1 behavior; Task 2/Task 3 behavior/action + a Task 3 acceptance (edit→`edit` not `add`; remove→`delete`) enforce it.
- **LOW (claude — fragile awk static guard):** the `awk '/updateContactFull/,/^}/' … | grep -c inWriteTransaction` guard (which mis-starts because "updateContactFull" appears in the file header + EDIT comment blocks) is replaced by a dedicated `contacts-dao.test.ts` assertion counting `inWriteTransaction`/BEGIN invocations across one call (exactly 1); the forced-rollback round-trip remains the authoritative atomicity proof.
</review_findings_addressed>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-05-SUMMARY.md` when done.
</output>
