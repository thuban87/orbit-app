---
phase: 34-rapid-capture-update-flows
plan: 05
type: execute
wave: 2
depends_on: ["34-03"]
files_modified:
  - src/screens/edit-contact-logic.ts
  - src/screens/edit-contact-logic.test.ts
  - src/screens/EditContactScreen.tsx
autonomous: true
requirements: [CAPT-04, CAPT-14]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-04 + D-10 knowledge model + CAPT-14 dirty-state)
    - "Edit Contact exposes the complete editable record as direct-access top-level accordion sections (Identity, Relationship Basics, Contact Methods, Last Talked About, Key People/Relationships, Current Location, Memories, Custom Fields, Off Limits) using the same vocabulary as Add Contact's expanded state (CAPT-04, dossier §E)."
    - "Things-to-Remember subdomains are directly findable as top-level editing sections — NOT buried in a second nested accordion (CAPT-04, dossier §E/§AN)."
    - "Multiple sections may remain open simultaneously; Edit Contact keeps ONE form-level Save (not per-drawer saving); updateContactFull is the single write path (dossier §E/§AD)."
    - "A validation error reveals + focuses the containing accordion; leaving after meaningful unsaved edits prompts Discard changes / Keep editing, while an unchanged form exits with no prompt (CAPT-14, dossier §AD/§AF; useDiscardKeepGuard)."
    # UI-SPEC ## UI Considerations — surface E6 (Edit Contact accordion)
    - "empty (E6): Edit Contact always shows the full record; a section with no value renders empty and editable (UI-SPEC, covered)."
    - "error (E6): a save failure renders locked \"Couldn't save / Please try again.\", preserves form state, never shows completion; a blocking validation error reveals + focuses the containing accordion with field-level actionable text (UI-SPEC, covered)."
    - "partial (E6): optional fields absent is a valid saved state everywhere in the record (UI-SPEC, covered)."
    - "loading (E6): Edit Contact hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "long-text (E6): long names, notes, and custom-field names truncate or wrap without breaking layout at large OS text scale across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT re-nest the Things-to-Remember subdomains inside a second accordion solely to mirror Profile grouping — direct-access top-level editing is the product requirement (CAPT-04, dossier §E)."
    - "MUST NOT silently discard meaningful unsaved edits — the discard/keep dirty-state guard fires only after meaningful edits and an unchanged form exits with no prompt (CAPT-14, dossier §AF)."
    - "MUST NOT collapse Update Contact's fast editors into Edit Contact or vice-versa merely because fields overlap — Edit Contact owns exhaustive identity/admin editing (e.g. Category), Update Contact owns fast relationship-knowledge capture (dossier §A/§X)."
  artifacts:
    - "src/screens/edit-contact-logic.ts (extended accordion-section descriptors)"
    - src/screens/EditContactScreen.tsx
  key_links:
    - "EditContactScreen composes the AccordionSection primitive from 34-03 + existing editors (ContactMethodsEditor, LinksEditor, FrequencyPicker, FieldValueInput, TriStateLastSpoke)."
    - "useDiscardKeepGuard (navigation/discard-keep-guard) provides the meaningful-dirty-state protection; updateContactFull is the single persistence path."
---

<objective>
Restructure Edit Contact into direct-access top-level accordion sections (CAPT-04), removing the nested Things-to-Remember drawer so every knowledge subdomain is a first-class editing section, and preserve the failure/validation/dirty-state contract for this surface (CAPT-14). `EditContactScreen` + `edit-contact-logic.ts` already exist (the real edit path, `updateContactFull`, `useDiscardKeepGuard`) — this is IA restructuring + reuse of the `AccordionSection` primitive built in 34-03, NOT new persistence.

Per D-01/D-02 the dossier (§E, §AD, §AF) is ground truth. Per D-10 the Contact Knowledge model gates the editable sections — compose the canonical field editors, do not invent an Edit-Contact-specific schema. This depends on 34-03 for the shared `AccordionSection` primitive.

Purpose: complete-record editing optimized for field discovery, not Profile-nesting mirroring.
Output: extended `edit-contact-logic` + test; restructured `EditContactScreen`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Edit Contact top-level accordion sections (compose AccordionSection) end-to-end edit → save</name>
  <files>src/screens/EditContactScreen.tsx, src/screens/edit-contact-logic.ts</files>
  <reversibility rating="reversible">IA restructure over the existing edit path; revisable.</reversibility>
  <read_first>
    - src/screens/EditContactScreen.tsx (1030 lines — current flat layout; imports ContactMethodsEditor, LinksEditor, FrequencyPicker, FieldValueInput, TriStateLastSpoke, PhotoSourcePicker; useDiscardKeepGuard line 77; updateContactFull line 69)
    - src/screens/edit-contact-logic.ts (existing build/gate helpers)
    - src/components/ui/AccordionSection.tsx (from 34-03 — the shared primitive with a11y expanded/collapsed state)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Typography heading role for section headers; Spacing lg between groups; a11y)
  </read_first>
  <action>
    Restructure `EditContactScreen` so each knowledge subdomain is a top-level `AccordionSection` (Identity, Relationship Basics, Contact Methods, Last Talked About, Key People/Relationships, Current Location, Memories, Custom Fields, Off Limits) — no nested Things-to-Remember accordion. Multiple sections may be open at once. Keep ONE form-level Save wired to the existing `updateContactFull`; compose the existing per-domain editors inside sections (do not rebuild them). Replace touched raw `Text` with `AppText` roles; all colours via theme tokens. Keep the tracer honest: full record renders as top-level sections and an edit to one field saves through `updateContactFull`.
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/screens/edit-contact-logic.test.ts</automated>
    <human-check>On the Pixel: Edit Contact shows all subdomains as top-level accordions (no nested Things-to-Remember); editing one field and Save persists and returns.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0.
    - `npx vitest run src/screens/edit-contact-logic.test.ts` exits 0.
    - `grep -ci "ThingsToRemember\|Things to Remember" src/screens/EditContactScreen.tsx` shows the nested-drawer grouping removed as an editing container (Profile nesting elsewhere is unaffected).
    - EditContactScreen imports AccordionSection from `@/components/ui/AccordionSection`.
  </acceptance_criteria>
  <done>Edit Contact renders the complete record as direct-access top-level accordion sections and saves through updateContactFull.</done>
</task>

<task type="auto">
  <name>Task 2: Validation-reveal + dirty-state guard for Edit Contact (CAPT-14)</name>
  <files>src/screens/EditContactScreen.tsx, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts</files>
  <reversibility rating="reversible">Validation/guard wiring; revisable.</reversibility>
  <read_first>
    - src/screens/EditContactScreen.tsx (useDiscardKeepGuard usage; existing validation)
    - src/navigation/discard-keep-guard.ts (meaningful-dirty-state protection contract)
    - src/screens/edit-contact-logic.ts (dirty/validation helpers)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Discard changes / Keep editing; Couldn't save / Please try again; error rows)
  </read_first>
  <action>
    Ensure a blocking validation error reveals + focuses the containing `AccordionSection` (expand it and scroll to the first blocking field) with field-level actionable error text, rather than leaving the user hunting collapsed drawers. Confirm `useDiscardKeepGuard` fires the Discard changes / Keep editing prompt ONLY after meaningful edits and that an unchanged form exits with no prompt. On save failure, preserve state, show "Couldn't save / Please try again." and never show completion. Extend `edit-contact-logic.test.ts` for any pure dirty/validation helpers touched.
  </action>
  <verify>
    <automated>npx vitest run src/screens/edit-contact-logic.test.ts</automated>
    <human-check>On the Pixel: entering an invalid value in a collapsed section and Save expands + focuses that section; backing out of an unchanged form exits silently; backing out after edits prompts Discard/Keep.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/edit-contact-logic.test.ts` exits 0.
    - `npm test` full suite green.
    - Validation reveals the containing accordion (human-check); unchanged-form exit shows no prompt (human-check).
  </acceptance_criteria>
  <done>Edit Contact reveals the relevant accordion on validation error, guards meaningful dirty state, and is failure-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edit form input → updateContactFull | Untrusted edited fields cross into the contacts/custom-value tables. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-16 | Tampering | updateContactFull writes | high | mitigate | Single parameterized write path; `?`-bound; scoped by contact id (V4/V5); no inline SQL in the screen. |
| T-34-17 | Repudiation/data-loss | silent discard of unsaved edits | medium | mitigate | useDiscardKeepGuard prompts only on meaningful dirty state; save failure preserves form state (never false completion). |

No package installs this phase — package-legitimacy gate N/A (composes existing primitives + the 34-03 AccordionSection).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- On-device UAT (end-of-phase): top-level accordion sections, no nested Things-to-Remember, validation-reveal, dirty-state guard.
</verification>

<success_criteria>
Edit Contact exposes the complete record as direct-access top-level accordion sections with one form-level Save, validation reveals the relevant section, and dirty-state protection fires only on meaningful edits (CAPT-04/14).
</success_criteria>

<artifacts_this_phase_produces>
- `src/screens/edit-contact-logic.ts` — extended accordion-section descriptor/validation helpers (existing build/gate retained).
- `src/screens/EditContactScreen.tsx` — restructured into top-level accordion sections consuming `AccordionSection`.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **CAPT-04 `unclassified` edge (probe) — surfaced not dropped.** CAPT-04's IA contract (direct-access top-level accordions, no nested Things-to-Remember) is covered by the must_haves truths + prohibition; the probe's unclassified shape edge has no additional authored criterion.
</flagged_assumptions>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-05-SUMMARY.md` when done.
</output>
