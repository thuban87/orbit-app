---
phase: 34-rapid-capture-update-flows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/memory-registry.ts
  - src/db/memory-registry.test.ts
  - src/db/ai-context-read.test.ts
  - src/db/digest-read.test.ts
autonomous: true
requirements: [CAPT-15]
user_setup: []

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # D-11 owner-resolved naming (feeds CAPT-05/06 Memory surfaces)
    - "MEMORY_TYPE_REGISTRY.general.displayName resolves to \"Memory\" and MEMORY_TYPE_REGISTRY.custom.displayName resolves to \"Custom\" (D-11 swap, owner-resolved 2026-09-12); no two built-in memory types share a displayName."
    - "Rapid capture requests the default type by DEFAULT_MEMORY_TYPE_KEY = \"general\", never by a hardcoded display name or type ID (D-11)."
    # CAPT-15 satisfied-by-dependency (D-06/D-07 — Phase 32 migration 025 already shipped the vocabulary migration + consumers)
    - "CAPT-15 is satisfied-by-dependency: the quality→Tone and 6-value→Message/Call/In Person migration plus every literal consumer shipped in Phase 32 (migration 025). This phase VERIFIES and ships NO second interactions migration (D-07)."
    - "No straggler legacy interaction vocabulary literal ('good'/'fine'/'hard' quality or 'text'/'email' channel) remains in any interactions writer or reader outside the single vocabulary-remap source (interaction-vocabulary.ts) and the migration files themselves."
    - "ai-context-read and digest-read tests remain green comparing the migrated Tone literals 'Positive'/'Neutral'/'Negative' (not good/fine/hard)."
    # Specless edge probe — CAPT-15 (authored)
    - statement: "Legacy channel values 'other'/'unspecified' remain representable post-migration (pass-through); text/email collapse to Message; no destructive removal of legacy values (CAPT-15 adjacency edge)."
      verification: backstop
    - "quality NULL (unset Tone) is preserved as NULL and never coerced to Neutral (CAPT-15 empty edge; D-08)."
    - statement: "Every literal consumer (ai-context-read, digest-read, restore-apply, timeline, TouchpointRefineForm) compares migrated literals Positive/Neutral/Negative and Message/Call/In Person, not legacy good/fine/hard/text/email (CAPT-15 encoding edge)."
      verification: backstop
  prohibitions:
    - "MUST NOT author a second interactions vocabulary migration — a redundant UPDATE on the shared table risks silent data corruption on un-reachable devices; CAPT-15 was shipped by Phase 32 (D-07 STOP-AND-ASK)."
    - "MUST NOT rename the internal `LogContact` route id or the `'log-contact'` stored enum to satisfy D-11 naming — only user-facing copy changes; renaming stored identifiers is owner-scope (RESEARCH Open Question 1)."
  artifacts:
    - "src/db/memory-registry.ts — general.displayName = \"Memory\", custom.displayName = \"Custom\""
    - src/db/memory-registry.test.ts
  key_links:
    - "MemoryEditor renders MEMORY_TYPE_REGISTRY[type].displayName (memory-registry.ts is the single label source) — the swap changes the label everywhere."
    - "interaction-vocabulary.ts remapLegacyQuality/Channel is the single vocabulary source consumers must route through; a straggler literal elsewhere silently diverges AI/digest."
---

<objective>
Two small, decoupled integrity edits that unblock the Memory-facing capture flows (34-06, 34-07) and close CAPT-15 without over-scoping:

1. **D-11 Memory-type display-name reconciliation** (owner-resolved 2026-09-12): swap `general` → "Memory" and `custom` → "Custom" in `memory-registry.ts` to remove the current duplicate-label collision (today `general` shows "General", `custom` shows "Memory"). Verified on disk this session.
2. **CAPT-15 verification (D-06/D-07)**: the interaction vocabulary migration and every literal consumer were **already shipped by Phase 32 (migration 025)**. This plan is a grep-audit + regression assertion that no legacy straggler remains — it authors **NO** `interactions` migration (that is the double-migration STOP-AND-ASK trip-wire).

Per D-01/D-02 the dossier (§J, §O, §T, §AN) and planning-notes (R-03) are ground truth; the planning-notes' 2026-09-01 legacy-value citations are STALE (Phase 32 migrated them 2026-09-11) — verified against disk.

Purpose: honest, unambiguous Memory labels; provable vocabulary integrity; zero redundant migration risk.
Output: `memory-registry.ts` swap + assert test; a straggler-literal grep audit + green consumer regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: D-11 Memory-type displayName swap (general → "Memory", custom → "Custom")</name>
  <files>src/db/memory-registry.ts, src/db/memory-registry.test.ts</files>
  <reversibility rating="reversible">A registry constant edit; freely revisable. It implements an owner-resolved decision (D-11), so a further change is an owner call.</reversibility>
  <read_first>
    - src/db/memory-registry.ts (verified on disk: line 10 PROVISIONAL_MEMORY_LABEL="Memory"; line 12 PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME="General"; line 15 DEFAULT_MEMORY_TYPE_KEY="general"; line 30 general.displayName=PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME → shows "General"; line 50 custom.displayName=PROVISIONAL_MEMORY_LABEL → shows "Memory")
    - src/components/MemoryEditor.tsx (renders MEMORY_TYPE_REGISTRY[type].displayName — the single consumer of the label)
    - .planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md (D-11 verbatim, authoritative over RESEARCH's paraphrase)
  </read_first>
  <behavior>
    - MEMORY_TYPE_REGISTRY.general.displayName === "Memory".
    - MEMORY_TYPE_REGISTRY.custom.displayName === "Custom".
    - The set of built-in displayName values contains no duplicates.
    - DEFAULT_MEMORY_TYPE_KEY is still "general" (unchanged); the default type is still requested by key.
  </behavior>
  <action>
    In `src/db/memory-registry.ts`, set `MEMORY_TYPE_REGISTRY.general.displayName` to "Memory" and `MEMORY_TYPE_REGISTRY.custom.displayName` to "Custom". You may either repoint the two provisional constants to their new final values or set the displayName strings directly and retire the now-misleading `PROVISIONAL_*` constant names — planner's discretion per D-11, but the resolved displayName values are fixed. Do NOT change `DEFAULT_MEMORY_TYPE_KEY`. Add `src/db/memory-registry.test.ts` (or extend if present) asserting the behavior block, including a no-duplicate-displayName check across the built-in registry.
  </action>
  <verify>
    <automated>npx vitest run src/db/memory-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/memory-registry.test.ts` exits 0.
    - Test asserts general.displayName === "Memory" and custom.displayName === "Custom" and no duplicate built-in displayName.
    - `grep -n "DEFAULT_MEMORY_TYPE_KEY" src/db/memory-registry.ts` still shows `"general"`.
  </acceptance_criteria>
  <done>The registry shows "Memory"/"Custom" with no duplicate label; the default type is still requested by key; test green.</done>
</task>

<task type="auto">
  <name>Task 2: CAPT-15 verification — legacy-vocabulary straggler audit + consumer regression (NO migration)</name>
  <files>src/db/ai-context-read.test.ts, src/db/digest-read.test.ts (+ a single straggler fix in its writer/reader only if the audit finds one — never a migration)</files>
  <reversibility rating="reversible">A grep audit + test assertions; no production data change.</reversibility>
  <read_first>
    - src/db/migrations/025-interaction-history-schema.ts (the shipped quality→Tone + channel remap, lines 44-72 — the source of truth CAPT-15 depends on)
    - src/db/interaction-vocabulary.ts (remapLegacyQuality/Channel — the single remap source; the ONLY place raw legacy literals legitimately appear)
    - src/db/ai-context-read.ts (compares 'Positive'/'Neutral'/'Negative' — lines 137-143)
    - src/db/digest-read.ts (readGentleLine tallies migrated Tone literals — lines 162-170)
    - src/backup/restore-apply.ts (remaps on ingest + forces allow_ai=0 — lines 190-201)
    - src/db/ai-context-read.test.ts, src/db/digest-read.test.ts (extend/assert)
  </read_first>
  <action>
    Run a repository grep audit for legacy interaction-vocabulary stragglers: search `src/` (excluding `src/db/interaction-vocabulary.ts` and `src/db/migrations/`) for interaction `quality` literals `'good'|'fine'|'hard'` and `channel` literals `'text'|'email'` used as comparison/write values against the `interactions` table. Record the audit command + result in the SUMMARY. If a genuine straggler is found in a writer/reader, fix it to route through `remapLegacy*` or the migrated literal — this is a small fix, NOT a migration. Add/extend a regression assertion in `ai-context-read.test.ts` and `digest-read.test.ts` confirming the consumers compare the migrated Tone literals and that a NULL quality is preserved (not coerced to Neutral). Do NOT create any `027`/`028` `interactions` migration; do NOT ALTER the interactions table.
  </action>
  <verify>
    <automated>npx vitest run src/db/ai-context-read.test.ts src/db/digest-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/ai-context-read.test.ts src/db/digest-read.test.ts` exits 0.
    - Audit command recorded in SUMMARY; zero unrouted legacy literals in interactions writers/readers (or the single straggler fixed and cited by file:line).
    - `ls src/db/migrations/ | grep -E "interaction"` shows only the pre-existing 025 (no new interactions migration authored).
  </acceptance_criteria>
  <done>CAPT-15 proven satisfied-by-dependency: consumers use migrated literals, NULL Tone preserved, no straggler, no second migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored interaction row → AI-egress reader (ai-context-read) | Tone/channel values projected toward the explicitly-invoked AI feature. |
| backup file → restore-apply | Legacy vocabulary may arrive on restore and must remain representable. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Tampering | duplicate/ambiguous Memory type label | low | mitigate | No-duplicate-displayName assert prevents two built-in types sharing "Memory", which would let a user mis-file a Memory. |
| T-34-06 | Information disclosure | straggler legacy Tone literal in ai-context-read | medium | mitigate | Regression asserts the egress reader compares migrated literals; a straggler would silently mis-tally Tone sent to AI. |
| T-34-07 | Tampering | accidental second interactions migration | high | mitigate | Audit + acceptance criterion prove no new interactions migration authored (double-migration hazard on un-reachable devices, D-07). |

No package installs this phase — package-legitimacy gate N/A (zero new dependencies).
</threat_model>

<verification>
- `npm test` green (full suite).
- `npm run check:colors` green.
- Grep audit result recorded; no new `interactions` migration file exists.
</verification>

<success_criteria>
Memory labels read "Memory"/"Custom" with no collision; CAPT-15 is provably satisfied-by-dependency with green consumer regressions and zero redundant migration.
</success_criteria>

<artifacts_this_phase_produces>
- `src/db/memory-registry.ts` — `general.displayName = "Memory"`, `custom.displayName = "Custom"` (constant values changed; provisional constant names may be retired).
- `src/db/memory-registry.test.ts` — registry label assertions (new or extended).
- No new source symbols; no new migration.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **CAPT-15 `ordering` edge (probe) — inapplicable, surfaced not dropped.** The vocabulary remap is a value mapping, not an ordered collection; there is no output-ordering contract to assert. No must_haves truth authored for it by design.
</flagged_assumptions>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-02-SUMMARY.md` when done.
</output>
