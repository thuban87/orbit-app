---
phase: 34-rapid-capture-update-flows
plan: 06
type: execute
wave: 3
depends_on: ["34-02", "34-04"]
files_modified:
  - src/screens/post-log-note-logic.ts
  - src/screens/post-log-note-logic.test.ts
  - src/stores/snackbar-store.ts
  - src/stores/snackbar-store.test.ts
  - src/components/Snackbar.tsx
  - src/services/quick-log-command.ts
  - src/services/quick-log-command.test.ts
  - src/components/PostLogNoteEditor.tsx
  - src/screens/HomeScreen.tsx
  - src/components/UniversalFab.tsx
autonomous: true
requirements: [CAPT-05, CAPT-13]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # Goal-backward (CAPT-05 + D-08 immediate/no-duration + D-11 default memory type)
    - "Quick Log stays immediate and current-time: runQuickLog writes at localDateTime() with no backdating and never asks for duration; success shows the locked \"Logged\" snackbar with Undo, failure shows \"Couldn't log\" with Retry (CAPT-05, D-08, dossier §H)."
    - "The success snackbar presents BOTH Undo AND Add Note simultaneously — this requires the snackbar store/type/component to carry an OPTIONAL secondaryAction (the current single-action QuickLogSnackbar/SnackbarMessage/Snackbar renderer cannot show two actions). Undo is preserved (SHELL-11); Add Note opens a small post-log editor bound to the just-created interactionId (CAPT-05, dossier §I, Review HIGH #3). The redesign is additive: existing single-action snackbar callers (ThingsToRemember/SystemsManagement/etc.) omit secondaryAction and are unchanged."
    - "Undo and Add Note are mutually exclusive: tapping EITHER action dismisses the snackbar (the renderer dismisses on action press), so both cannot fire on one snackbar; opening the post-log editor consumes/commits the Undo (Undo is no longer reachable once the editor opens) (CAPT-05, Review MEDIUM 34-06)."
    - "The post-log editor's Note branch handles a MISSING interaction gracefully: if the interaction was already undone/deleted (Add-Note-after-Undo race), the Note save no-ops or surfaces a friendly error rather than editTouchpointFull-ing a deleted row (Review MEDIUM 34-06)."
    - "The post-log editor has its OWN save guard (single-flight) separate from runQuickLog's pendingRef, which only protects the initial Quick Log write; a double-tap of the post-log Save commits at most once (Review MEDIUM 34-06)."
    - "The post-log editor saves EITHER an Interaction Note (via editTouchpointFull seeded from readInteractionForEdit) OR a Memory (via addMemory using DEFAULT_MEMORY_TYPE_KEY) — never both; Create Memory Instead means the text becomes a Memory and is NOT duplicated into the Interaction Note (CAPT-05, dossier §I)."
    - "After a basic Memory is created, the editor offers Edit Memory → the full MemoryEditor; the quick Memory path stays basic and does not redirect into the full editor before saving (CAPT-05, dossier §I/§K)."
    - "The basic Memory is requested by DEFAULT_MEMORY_TYPE_KEY = \"general\" (label \"Memory\" after 34-02), never by a hardcoded display name or type ID (D-11)."
    - "Quick Log preselects the contact when the invoking context identifies one (Profile/Dashboard/FAB-after-picker) (CAPT-13, dossier §AC)."
    # Specless edge probe — CAPT-05
    - "runQuickLog is single-flight (pendingRef): a double invocation writes exactly one interaction; the post-log editor's save is Note XOR Memory, never both, and re-opening after a save does not duplicate (CAPT-05 idempotency edge)."
    - statement: "A Quick Log write and its Undo each run as one recency-spine transaction; an interrupted post-log save leaves either the created interaction unchanged or a single new Memory, never a half-written pair (CAPT-05 concurrency edge)."
      verification: backstop
    # UI-SPEC ## UI Considerations — surface E2 (Quick Log + post-log editor)
    - "empty (E2): the post-log editor opens empty; the Memory branch preselects the default type by key (UI-SPEC, covered)."
    - "error (E2): a failed post-log save renders the locked failure copy, preserves entered text, never shows completion, and exposes Retry where recoverable (UI-SPEC, covered)."
    - "partial (E2): the post-log editor saves an Interaction Note or a Memory, never both (UI-SPEC, covered)."
    - "loading (E2): the editor hydrates from on-device SQLite with no network on the read path and renders immediately — no blocking spinner (UI-SPEC, covered)."
    - statement: "long-text (E2): a long note wraps without breaking the editor layout at large OS text scale across the four resolved palettes (UI-SPEC, backstop)."
      verification: backstop
  prohibitions:
    - "MUST NOT duplicate the entered text into BOTH an Interaction Note and a Memory — Create Memory Instead means the text becomes a Memory only, leaving the Interaction Note empty (CAPT-05, dossier §I: no drift/sync problem)."
    - "MUST NOT convert Quick Log into a pre-submit form that delays the immediate write to collect an optional note — the note is captured after the write, not before (dossier §H/§I)."
    - "MUST NOT change Quick Log's hardcoded channel:'unspecified' to adopt the Default Interaction Channel preference without owner sign-off — Quick Log is not a form and collects no channel (RESEARCH A2, flag if consequential)."
  artifacts:
    - src/screens/post-log-note-logic.ts
    - src/screens/post-log-note-logic.test.ts
    - "src/stores/snackbar-store.ts (optional secondaryAction)"
    - "src/components/Snackbar.tsx (renders the optional secondary action)"
    - src/components/PostLogNoteEditor.tsx
  key_links:
    - "The success snackbar's action=Undo + optional secondaryAction=Add Note is the ONLY way both appear at once; the store/renderer redesign (Task 2) is the load-bearing prerequisite for CAPT-05 + SHELL-11 coexisting."
    - "runQuickLog (quick-log-command.ts) injects an openPostLogEditor dep; the Add Note secondaryAction opens PostLogNoteEditor bound to the returned interactionId; tapping either action dismisses the snackbar (mutual exclusion)."
    - "Note branch → editTouchpointFull(seeded from readInteractionForEdit; 'missing' outcome when undone); Memory branch → addMemory(DEFAULT_MEMORY_TYPE_KEY); Edit Memory → MemoryEditor."
    - "runQuickLog callers (HomeScreen, UniversalFab) construct the deps + navigation to the editor."
---

<objective>
Add the post-log Note/Memory capture to Quick Log (CAPT-05) without disturbing its immediate-write contract, and confirm Quick Log's preselection (CAPT-13). `runQuickLog` already writes immediately with the locked Logged/Undo (and Couldn't-log/Retry) snackbar and single-flight `pendingRef`; the new work is an **Add Note** affordance on the success snackbar opening a small post-log editor bound to the just-created interaction, saving EITHER an Interaction Note OR a basic Memory (never both), with an Edit Memory path into the full `MemoryEditor`. The branch decision lives in a new node-tested `post-log-note-logic.ts` (Wave-0 gap).

Per D-01/D-02 the dossier (§H, §I, §K) is ground truth. Per D-08 Quick Log is immediate/current-time and never asks duration. Per D-11 the basic Memory is requested by `DEFAULT_MEMORY_TYPE_KEY` (label finalized in 34-02), never a hardcoded name. This depends on 34-02 for the reconciled Memory registry.

Purpose: let a Quick Log grow a note or Memory after the fact, with no text duplication and no pre-submit form.
Output: `post-log-note-logic.ts` + test; extended `quick-log-command` + test; `PostLogNoteEditor.tsx`; caller wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-rapid-capture-update-flows/34-CONTEXT.md
@.planning/phases/34-rapid-capture-update-flows/34-RESEARCH.md
@.planning/phases/34-rapid-capture-update-flows/34-PATTERNS.md
@.planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: post-log-note-logic.ts — Note-XOR-Memory branch (pure, node-tested)</name>
  <files>src/screens/post-log-note-logic.ts, src/screens/post-log-note-logic.test.ts</files>
  <reversibility rating="reversible">New pure logic module; revisable. Encodes the dossier §I no-both rule — keep it fixed.</reversibility>
  <read_first>
    - src/services/quick-log-command.ts (RunQuickLogDeps injected-deps shape lines 38-48; the returned interactionId; the snackbar contract 81-126; hardcoded channel:'unspecified' 92-97)
    - src/db/interaction-edit-read.ts (readInteractionForEdit seeds the note branch, lines 58-85)
    - src/db/recency-dao.ts (editTouchpointFull 310-374 — the Interaction Note write path)
    - src/db/memories-dao.ts (addMemory 296; editMemory 308; setMemoryAllowAi 319)
    - src/db/memory-registry.ts (DEFAULT_MEMORY_TYPE_KEY = "general")
    - dossier §I (Interaction Note vs Memory; Create Memory Instead; Edit Memory)
  </read_first>
  <behavior>
    - resolvePostLogSave({ kind:"note", text, interactionId, interactionExists:true }) → an editTouchpointFull patch that sets note=text on the existing interaction and leaves no Memory created.
    - resolvePostLogSave({ kind:"memory", text }) → an addMemory input of type DEFAULT_MEMORY_TYPE_KEY with value=text, and the Interaction Note is left empty (no duplication).
    - The two branches are mutually exclusive — the resolver returns exactly one target ("note" | "memory" | "missing" | "noop"), never both.
    - Empty/whitespace text yields a no-op result (nothing saved) rather than an empty Note or empty Memory.
    - kind="note" with interactionExists=false (the Add-Note-after-Undo race — the interaction was undone/deleted) yields a "missing" no-op outcome (NO editTouchpointFull patch) so the caller surfaces a friendly error instead of editing a deleted row. The Memory branch is unaffected (it does not depend on the interaction).
  </behavior>
  <action>
    Create `src/screens/post-log-note-logic.ts` (react-native-free) exporting a `PostLogSaveTarget` union ("note" | "memory" | "missing" | "noop") and `resolvePostLogSave(input)` that maps the editor's choice to exactly one persistence intent (Note via editTouchpointFull patch, or Memory via addMemory input keyed by DEFAULT_MEMORY_TYPE_KEY) — never both, per dossier §I. Take an `interactionExists` signal so the note branch returns "missing" (no-op) when the interaction was undone/deleted. Handle empty text as "noop". Write `post-log-note-logic.test.ts` covering the behavior block including the missing-interaction case. This is the correctness core; the editor and command compose it.
  </action>
  <verify>
    <automated>npx vitest run src/screens/post-log-note-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/post-log-note-logic.test.ts` exits 0.
    - Test asserts the "memory" branch never produces a note patch and the "note" branch never produces an addMemory input (mutual exclusion).
    - Test asserts the memory branch uses DEFAULT_MEMORY_TYPE_KEY (imported), not a literal display name.
    - Test asserts kind="note" with interactionExists=false returns "missing" (no note patch) — the Add-Note-after-Undo case (Review MEDIUM 34-06).
  </acceptance_criteria>
  <done>The Note-XOR-Memory branch is node-tested green with no text duplication and empty-text no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-action snackbar — add optional secondaryAction to the store/type/renderer (CAPT-05 prerequisite)</name>
  <files>src/stores/snackbar-store.ts, src/stores/snackbar-store.test.ts, src/components/Snackbar.tsx</files>
  <reversibility rating="reversible">Additive store/type/renderer change; revisable. Must stay backward-compatible with every existing single-action caller.</reversibility>
  <read_first>
    - src/stores/snackbar-store.ts (SnackbarMessage/SnackbarAction — single `action` today, lines 5-15; show/dismiss + 6s timer 26-41)
    - src/components/Snackbar.tsx (renders exactly ONE Pressable action, lines 25-37; dismisses on press)
    - EVERY existing snackbar caller (must stay working with NO secondaryAction): src/screens/ThingsToRememberScreen.tsx, src/screens/SystemsManagementScreen.tsx, src/screens/HomeScreen.tsx, src/components/UniversalFab.tsx, src/services/quick-log-command.ts
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Color: snackbar action uses accentText; a11y ordering)
  </read_first>
  <behavior>
    - SnackbarMessage gains an OPTIONAL `secondaryAction?: SnackbarAction`; single-action callers omit it and render unchanged (one Pressable).
    - When secondaryAction is present, Snackbar renders TWO action Pressables in a defined a11y order (primary/Undo first, secondary/Add Note second); tapping EITHER calls dismiss() then that action's onPress (so the two actions are mutually exclusive per snackbar).
    - The 6s auto-dismiss timer and existing show/dismiss semantics are unchanged.
  </behavior>
  <action>
    Extend `SnackbarMessage` in `snackbar-store.ts` with an OPTIONAL `secondaryAction?: SnackbarAction` (reuse the existing SnackbarAction shape). Update `Snackbar.tsx` to render the secondary action's Pressable when present, AFTER the primary, each dismissing on press, with semantic a11y labels and a defined focus/read order; colours via theme tokens (accentText). Do NOT make secondaryAction required — every existing single-action caller must be untouched. Add `snackbar-store.test.ts` asserting a message with and without secondaryAction (the optional field is respected and the store still shows/dismisses).
  </action>
  <verify>
    <automated>npm run check:colors && npx vitest run src/stores/snackbar-store.test.ts</automated>
    <human-check>On the Pixel: a Quick Log success snackbar shows BOTH Undo and Add Note; a non-Quick-Log snackbar (e.g. a favourite-toggle error) still shows one action.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/snackbar-store.test.ts` exits 0.
    - `grep -c "secondaryAction" src/stores/snackbar-store.ts` ≥ 1 AND `grep -c "secondaryAction" src/components/Snackbar.tsx` ≥ 1 (type + renderer).
    - `npm run check:colors` exits 0.
  </acceptance_criteria>
  <done>The snackbar carries two actions (Undo + Add Note) additively; single-action callers are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Note secondary action in runQuickLog (Undo preserved, immediate write + single-flight)</name>
  <files>src/services/quick-log-command.ts, src/services/quick-log-command.test.ts</files>
  <reversibility rating="reversible">Extends an injected-deps service; revisable.</reversibility>
  <read_first>
    - src/services/quick-log-command.ts (runQuickLog 81-126; QuickLogSnackbar single `action` 16-24; RunQuickLogDeps 38-48; pendingRef single-flight; Logged/Undo + Couldn't-log/Retry snackbar)
    - src/services/quick-log-command.test.ts (extend — single-flight + snackbar coverage)
    - src/stores/snackbar-store.ts (Task 2 — the new optional secondaryAction the success snackbar sets)
    - src/screens/post-log-note-logic.ts (Task 1)
  </read_first>
  <behavior>
    - runQuickLog still writes immediately at localDateTime() and never collects a pre-submit note or duration.
    - On success the snackbar sets action=Undo (unchanged, SHELL-11) AND secondaryAction=Add Note; Add Note invokes an injected openPostLogEditor({ interactionId, contactId }) with the just-created interactionId.
    - Double invocation is still single-flight via pendingRef (exactly one write).
    - channel remains 'unspecified' (unchanged) unless the owner signs off (flagged).
  </behavior>
  <action>
    Add an optional `secondaryAction` to the `QuickLogSnackbar` type (mirroring the store's SnackbarMessage.secondaryAction), extend `RunQuickLogDeps` with an injected `openPostLogEditor(args: { interactionId: number; contactId: number })`, and on the SUCCESS snackbar keep `action` = Undo and set `secondaryAction` = Add Note that calls openPostLogEditor with the just-created interactionId. Preserve the immediate write, the localDateTime() timestamp, the no-duration contract, the single-flight `pendingRef`, and the locked snackbar copy. Do NOT change the hardcoded `channel:'unspecified'`. Extend `quick-log-command.test.ts` to assert the success snackbar carries BOTH Undo and Add Note, that Add Note invokes openPostLogEditor with the returned interactionId, and that single-flight still holds.
  </action>
  <verify>
    <automated>npx vitest run src/services/quick-log-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/quick-log-command.test.ts` exits 0 including the two-action + single-flight cases.
    - Test asserts the success snackbar has action.label "Undo" AND a secondaryAction (Add Note) invoking openPostLogEditor with the created interactionId.
    - `grep -c "channel: \"unspecified\"\|channel: 'unspecified'" src/services/quick-log-command.ts` unchanged (still present; not swapped to the pref).
  </acceptance_criteria>
  <done>The success snackbar offers Undo AND Add Note (bound to the created interaction); immediate write, timestamp, no-duration, and single-flight are intact.</done>
</task>

<task type="auto">
  <name>Task 4: PostLogNoteEditor (save guard + missing-interaction handling) + wire callers</name>
  <files>src/components/PostLogNoteEditor.tsx, src/screens/HomeScreen.tsx, src/components/UniversalFab.tsx</files>
  <reversibility rating="reversible">New editor component + caller wiring; revisable.</reversibility>
  <read_first>
    - src/screens/HomeScreen.tsx (runQuickLog caller line 494 — construct deps incl. the new openPostLogEditor)
    - src/components/UniversalFab.tsx (runQuickLog caller line 201 — construct deps)
    - src/components/MemoryEditor.tsx (full editor for the Edit Memory path; MemoryDraft/MemoryEditPatch props 44-71)
    - src/db/interaction-edit-read.ts (readInteractionForEdit — returns null when the interaction was undone/deleted; the missing-interaction check)
    - src/db/recency-dao.ts (editTouchpointFull — the Note write path)
    - src/db/memories-dao.ts (addMemory — the Memory write path)
    - src/components/ui/ (Sheet/Modal/Button/AppText primitives)
    - src/screens/post-log-note-logic.ts (Task 1 resolver, incl. the "missing" outcome)
    - .planning/phases/34-rapid-capture-update-flows/34-UI-SPEC.md (Copywriting: Add Note → Create Memory Instead → Edit Memory; Color: text-only affordances use accentText/tertiary; error/failure copy)
  </read_first>
  <action>
    Create `src/components/PostLogNoteEditor.tsx` as a small Sheet/Modal bound to `{ interactionId, contactId }`: a note text field, a Create Memory Instead action, and (after a basic Memory is created) an Edit Memory action opening the full `MemoryEditor`. It carries its OWN save-in-flight guard (a single-flight ref, separate from runQuickLog's pendingRef which only protects the initial write) so a double-tapped Save commits at most once. Before a Note save it reads the interaction (readInteractionForEdit); if it no longer exists (Add-Note-after-Undo — resolvePostLogSave returns "missing"), it surfaces a friendly error and does NOT editTouchpointFull a deleted row. Its Save routes through `resolvePostLogSave` → `editTouchpointFull` (Note) or `addMemory` (Memory), never both. Because opening this editor consumes the Undo (the snackbar dismissed when Add Note was tapped), the Note branch is the only place the missing-interaction race can appear. Wire `HomeScreen` and `UniversalFab` to pass an `openPostLogEditor` dep into `runQuickLog` that opens this editor. All colours via theme tokens; text-only affordances use `accentText`/tertiary. On save failure, preserve entered text, show the locked failure copy, and expose Retry.
  </action>
  <verify>
    <automated>npm run check:colors && npm test</automated>
    <human-check>On the Pixel: Quick Log → snackbar shows Undo AND Add Note → Add Note → typing + Save writes an Interaction Note; re-run → Add Note → Create Memory Instead writes a Memory (not a note) → Edit Memory opens the full editor; a double-tapped Save writes at most once.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check:colors` exits 0 (no new colour literals).
    - `npm test` full suite green.
    - `grep -c "PROVISIONAL_MEMORY_LABEL\|\"Memory\"\|\"General\"" src/components/PostLogNoteEditor.tsx` returns 0 (Memory requested by key, not literal name).
    - The editor has its own save-in-flight guard (grep confirms a single-flight ref distinct from runQuickLog's pendingRef).
    - Note-vs-Memory mutual exclusion + missing-interaction "missing" path verified (human-check + post-log-note-logic test).
  </acceptance_criteria>
  <done>The post-log editor saves a Note or a basic Memory (never both), guards its own Save against double-tap, handles a missing (undone) interaction gracefully, offers Edit Memory into the full MemoryEditor, and is wired from both Quick Log callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post-log editor input → editTouchpointFull / addMemory | Untrusted note/memory text crosses into interactions/memories. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-18 | Tampering | post-log Note/Memory write | high | mitigate | Note via editTouchpointFull (recency spine, one txn, scoped by interaction id AND contact_id, V4/V5); Memory via addMemory (registered-type + provenance enforced); no inline SQL. |
| T-34-19 | Integrity | double-write / duplicate text / edit-of-deleted-row | medium | mitigate | resolvePostLogSave returns exactly one target; runQuickLog's pendingRef prevents a duplicate initial write; the post-log editor adds its OWN save-in-flight guard so a double-tapped Save commits once; the Note branch checks the interaction exists (readInteractionForEdit → "missing") so Add-Note-after-Undo never edits a deleted row (Review MEDIUM 34-06). |
| T-34-20 | Information disclosure | basic Memory AI eligibility | low | mitigate | The basic Memory follows the registry's default AI posture; the post-log path does not widen egress (Allow AI on notes stays OFF-by-default, ADR-078). |

No package installs this phase — package-legitimacy gate N/A (composes existing primitives; zero new dependencies).
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` green.
- post-log-note-logic + quick-log-command tests assert mutual exclusion, single-flight, Add Note wiring.
- On-device UAT (end-of-phase): Quick Log immediacy, Add Note, Create Memory Instead, Edit Memory.
</verification>

<success_criteria>
Quick Log stays immediate/current-time with Undo, its success snackbar offers Add Note → a post-log editor saving an Interaction Note OR a basic Memory (never both) → optional Edit Memory, requesting the default Memory type by key, with Quick Log preselecting the contact (CAPT-05/13).
</success_criteria>

<artifacts_this_phase_produces>
- `src/screens/post-log-note-logic.ts` — exports `PostLogSaveTarget` ("note"|"memory"|"missing"|"noop"), `resolvePostLogSave`.
- `src/stores/snackbar-store.ts` — `SnackbarMessage.secondaryAction?` (optional second action).
- `src/components/Snackbar.tsx` — renders the optional secondary action Pressable.
- `src/components/PostLogNoteEditor.tsx` — new post-log Note/Memory editor (Sheet/Modal) with its own save guard.
- `src/services/quick-log-command.ts` — `QuickLogSnackbar.secondaryAction?`, `RunQuickLogDeps.openPostLogEditor` + Add Note secondary action on the success snackbar.
- `src/screens/HomeScreen.tsx`, `src/components/UniversalFab.tsx` — inject the openPostLogEditor dep.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **Quick Log channel (RESEARCH A2) — flagged, unchanged pending owner.** Quick Log keeps `channel:'unspecified'` rather than adopting the Default Interaction Channel preference; the dossier §P scopes the preference to Log Interaction forms and Quick Log collects no channel. If the owner wants Quick Log to honor the preference, revisit — do not change silently.
</flagged_assumptions>

<review_findings_addressed>
Cross-AI review (34-REVIEWS.md, cycle 1) findings incorporated into this plan:
- **HIGH #3 (codex — single-action snackbar cannot show Undo + Add Note):** new Task 2 redesigns the snackbar store/type/renderer with an OPTIONAL secondaryAction (additive; existing single-action callers unchanged), and Task 3 sets action=Undo + secondaryAction=Add Note on the success snackbar — so CAPT-05 (Add Note) and SHELL-11 (Undo) coexist without replacing Undo. Files (snackbar-store.ts, snackbar-store.test.ts, Snackbar.tsx) added to frontmatter; must_haves, key_links, artifacts, and estimate updated.
- **MEDIUM (codex + claude — Undo/Add-Note contract, save guard, missing-interaction):** mutual exclusion (tapping either action dismisses the snackbar; opening the editor consumes Undo), a post-log-editor save guard distinct from runQuickLog's pendingRef, and graceful missing-interaction handling ("missing" outcome in resolvePostLogSave + readInteractionForEdit check) — with an "Add Note after Undo" unit test and a double-tap human-check. Incorporated into must_haves, Task 1, Task 4, and T-34-19.
</review_findings_addressed>

<output>
Create `.planning/phases/34-rapid-capture-update-flows/34-06-SUMMARY.md` when done.
</output>
