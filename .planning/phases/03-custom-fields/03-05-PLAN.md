---
phase: 03-custom-fields
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/db/field-type-change.ts
  - src/db/field-type-change.test.ts
autonomous: true
requirements: [FLD-04]
must_haves:
  truths:
    - "A type change is ONE UPDATE custom_field_defs SET type — contact_custom_values bytes are NEVER rewritten"
    - "Pre-flight is read-only and partitions stored values into convert (parse-clean) vs flag (unconvertible)"
    - "preflightOptionsChange partitions stored values into keep vs flag by option-membership (isValueInOptions) so an options edit shows the same convert/flag summary"
    - "Applying a type change snapshots the pre-change state to field_history in the SAME transaction, encoding the transition in operation (type_change:<old>-><new>) since field_history has no type column"
    - "No confirmation prompt is required beyond the pre-flight summary; unconvertible values are flagged, never cleared"
  artifacts:
    - src/db/field-type-change.ts
  key_links:
    - "pre-flight uses the Plan 02 parsers[target] to classify every stored value; options pre-flight uses Plan 02 isValueInOptions"
    - "apply writes field_history (operation 'type_change:<old>-><new>') + UPDATE defs.type atomically, leaving values byte-identical"
    - "imports inWriteTransaction from src/db/transaction.ts (Plan 01) — no re-copy, no nesting"
---

<objective>
Build the type-change layer (FLD-04): a read-only pre-flight that partitions a field's stored values
into convert-vs-flag using the target parser, and an apply step that is ONE `UPDATE custom_field_defs
SET type` plus a same-transaction `field_history` audit snapshot — with `contact_custom_values` never
touched.

Purpose: This is the phase's most-misread requirement. §14.2 + CLAUDE.md make it non-negotiable that a
type change has "blast radius zero" — it must NOT touch `contact_custom_values` at all. So "auto-convert
clean values" means: clean values are already VALID under the new type and render/sort correctly at read
time (via the widget + sortExpr); unconvertible values render a tap-to-fix error state on the profile.
Nothing is coerced or cleared. §14.6's "snapshot in the same transaction" is satisfied by writing a
field_history audit row for the pre-change state, still leaving stored bytes byte-identical. (This is
the CONTEXT-recorded reconciliation of the two spec clauses — enforcing the dominant invariant, not
reversing a decision. Surfaced for the owner's --to 3 review.)

Output: `src/db/field-type-change.ts` (preflightTypeChange, preflightOptionsChange, applyTypeChange),
node:sqlite-proven, including the load-bearing "values unchanged" invariant assertion.

Undo caveat (review MED — surface at the --to 3 gate, NO decision reversal): `field_history` has no
type column (it is `contact_id, field_col_name, old_value, operation, created_at` —
001-initial.ts:155-163). A type change leaves values byte-identical, so `old_value` alone carries zero
recoverable type information. The transition is therefore encoded in the free-text `operation`
(`type_change:<old>-><new>`, e.g. `type_change:number->text`) so the snapshot records WHAT changed. This
is an audit trail; no undo/restore surface is built this phase (FLD-01…07 do not require one), and that
narrowing of CONTEXT's "undo" framing is flagged for the owner's --to 3 review. No value is ever rewritten.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-custom-fields/03-RESEARCH.md
@.planning/phases/03-custom-fields/03-CONTEXT.md
@src/db/recency-dao.ts
@src/db/field-parsers.ts
@src/db/migrations/001-initial.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: preflightTypeChange + preflightOptionsChange — read-only convert/flag partitions</name>
  <read_first>
    - .planning/phases/03-custom-fields/03-RESEARCH.md "Type-change pre-flight (read-only partition)" code example
    - HANDOFF.md §14.4 (the pre-flight summary: "8 of 12 will convert automatically. 4 need your input.")
    - src/db/field-parsers.ts (parsers[target] — the classifier — AND isValueInOptions for the options pre-flight; Plan 02 artifacts, hence depends_on 03-02)
    - src/db/col-name.ts (isSafeColName guard) + src/db/field-types.ts (CustomFieldDef, FieldType) — Plan 01 artifacts (depends_on 03-01)
  </read_first>
  <files>src/db/field-type-change.ts (Task 1 portion)</files>
  <action>
    Create `src/db/field-type-change.ts`. Export `preflightTypeChange(exec, field:
    Pick<CustomFieldDef,'col_name'>, target: FieldType): Promise<{ total: number; convert: number[];
    flag: number[] }>` — READ-ONLY: guard col_name with isSafeColName, SELECT contact_id + the value for
    every row where the value is non-null, run `parsers[target]` on each, and partition the contact_ids
    into `convert` (parse ok) vs `flag` (parse not ok). Writes NOTHING. This produces the summary the UI
    shows ("N of M will convert automatically; K need your input"). Do not open a transaction.

    Also export `preflightOptionsChange(exec, field: Pick<CustomFieldDef,'col_name'>, nextOptions: string
    | null): Promise<{ total: number; keep: number[]; flag: number[] }>` (addresses review MED —
    dropdown options edited to exclude a stored value must show the SAME convert/flag summary as a type
    change, and render out-of-list values as tap-to-fix). READ-ONLY: guard col_name with isSafeColName,
    SELECT contact_id + non-null value per row, and partition on `isValueInOptions({ type: 'dropdown',
    options: nextOptions }, value)` — `keep` (still a member of the NEW options) vs `flag` (excluded by
    the edit). Writes NOTHING; opens no transaction. Plan 08 runs this before committing an options edit;
    the actual `UPDATE options` is `changeFieldOptions` in Plan 03 (this stays read-only, like its
    type-change sibling).
  </action>
  <verify>
    <automated>npx vitest run src/db/field-type-change.test.ts && npx tsc --noEmit && npx biome check src/db/field-type-change.ts</automated>
  </verify>
  <done>
    - preflightTypeChange partitions stored values into convert/flag via the target parser
    - preflightOptionsChange partitions stored values into keep/flag via isValueInOptions against the NEW options
    - Both are strictly read-only (no BEGIN, no write); col_name guarded + `?`-bound value reads
  </done>
</task>

<task type="auto">
  <name>Task 2: applyTypeChange — defs.type UPDATE + same-txn history snapshot, values untouched</name>
  <read_first>
    - src/db/transaction.ts (inWriteTransaction — IMPORT it, do NOT copy; Plan 01 artifact)
    - .planning/phases/03-custom-fields/03-CONTEXT.md "Type-change semantics" (defs.type UPDATE ONLY; values NEVER rewritten; field_history snapshots pre-change state; NO confirmation prompt)
    - src/db/migrations/001-initial.ts lines 155-163 (field_history columns: contact_id, field_col_name, old_value, operation, created_at — NO type column, so the transition is encoded in operation)
    - HANDOFF.md §14.6 (one mechanism, two uses — snapshot before a destructive op, in a transaction)
  </read_first>
  <files>src/db/field-type-change.ts (Task 2 portion), src/db/field-type-change.test.ts</files>
  <action>
    Add `applyTypeChange(exec, field: Pick<CustomFieldDef,'id'|'col_name'|'type'>, target: FieldType, now:
    string): Promise<void>` to `src/db/field-type-change.ts` (import `inWriteTransaction` from
    `@/db/transaction`). Note the signature carries `field.type` — the CURRENT (pre-change) type — because
    field_history has no type column and the transition must be recorded in `operation`. Inside ONE
    inWriteTransaction: (a) INSERT into field_history one row per contact with a non-null value, capturing
    the col_name, the current (pre-change) old_value, operation `type_change:${field.type}->${target}`
    (e.g. `type_change:number->text` — review MED, this is the non-destructive old-type encoding), and
    `now` — the audit snapshot of the pre-change state; (b) `UPDATE custom_field_defs SET type = ?,
    modified_at = ? WHERE id = ?`. That is the ENTIRE write. Do NOT `UPDATE contact_custom_values`, do NOT
    run any value normalization, do NOT ALTER the column. There is no separate confirmation prompt (§14.4
    — the pre-flight summary is enough).

    Write `src/db/field-type-change.test.ts` (node:sqlite): create a number field, write a mix of
    clean ("42") and unconvertible ("about 60k") values across contacts; preflight reports the right
    convert/flag split for a target type; a dropdown field with options `["a","b"]` and a stored "c" →
    preflightOptionsChange against `["a"]` puts that contact in `flag`; then capture the raw stored bytes
    of the value column, call applyTypeChange(number→text), and assert (i) custom_field_defs.type is the
    new type, (ii) field_history holds the pre-change snapshot rows with operation
    `type_change:number->text` (the transition is recorded), and (iii) THE VALUE COLUMN BYTES ARE
    BYTE-IDENTICAL to the pre-change capture — the "blast radius zero" invariant (an unconvertible value
    is neither cleared nor coerced).
  </action>
  <verify>
    <automated>npx vitest run src/db/field-type-change.test.ts && npx tsc --noEmit && npx biome check src/db/field-type-change.ts src/db/field-type-change.test.ts</automated>
  </verify>
  <done>
    - applyTypeChange is exactly UPDATE defs.type + a same-txn field_history snapshot; contact_custom_values untouched
    - The "values byte-identical after a type change" invariant is explicitly asserted (blast radius zero)
    - field_history rows carry operation `type_change:<old>-><new>` (the transition, since there is no type column) and the pre-change old_value
    - No confirmation prompt; unconvertible values remain, flagged (not cleared); tsc + biome clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored value → read-time re-typing | Changing a field's type re-interprets existing stored strings; the risk is accidentally rewriting or clearing data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial (data loss) | applyTypeChange | high | mitigate | Type change is UPDATE defs.type ONLY — a byte-identical-values test forbids any contact_custom_values write; unconvertible values are flagged at read time, never cleared. |
| T-03-03 | Integrity | history vs defs consistency | medium | mitigate | The field_history snapshot and the defs UPDATE share one transaction (both or neither). |
| T-03-01 | Tampering | col_name in preflight/apply reads | high | mitigate | col_name guarded by isSafeColName and double-quoted; every value `?`-bound. |

No package installs (phase adds zero dependencies) — no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npx vitest run src/db/field-type-change.test.ts` green, including the byte-identical-values assertion
- `npx tsc --noEmit` + `npx biome check` clean
</verification>

<success_criteria>
- Type change auto-converts clean values (read-time), flags the rest tap-to-fix, destroys no data, and snapshots in the same transaction (FLD-04)
- The dominant "must not touch contact_custom_values" invariant is enforced and tested
</success_criteria>

<output>
Create `.planning/phases/03-custom-fields/03-05-SUMMARY.md` when done.
</output>
