---
phase: 13-orrery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/003-orrery-settings.ts
  - src/db/migrations/003-orrery-settings.test.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
autonomous: true
requirements: [ORR-05, ORR-06]
must_haves:
  truths:
    - "A fresh v1 install and a v2 install both migrate cleanly to v3, gaining sun_contact_id + self_sun_colour, each defaulting NULL."
    - "getAppSettings returns sunContactId (number|null) and selfSunColour (string|null); a NULL self_sun_colour reads back as null (resolved to starPalette[0] later at render, never here)."
    - "updateAppSettings validates sun_contact_id (null|positive int) and self_sun_colour (null|/^#[0-9A-Fa-f]{6}$/) BEFORE the write opens; a bad value throws and writes nothing."
  artifacts:
    - src/db/migrations/003-orrery-settings.ts
    - src/db/app-settings-dao.ts (widened)
  key_links:
    - "database.ts TARGET_VERSION bumped 2→3 AND migration003 appended to the runner array — both edits, or v3 never applies."
    - "sun_contact_id INTEGER REFERENCES contacts(id) ON DELETE SET NULL — a hard-purged sun-contact auto-reverts to self (A1)."
---

<objective>
Migration 003 adds the two app-level sun columns to the single-row `app_settings` table, and the app-settings DAO is widened to read/write/validate them. This is the storage half of the assignable-sun (ORR-06) and self-sun-colour (ORR-05) features. It is the first schema change since migration 002 and is irreversible on real devices.

Purpose: Give the orrery a persistent home for "who is the sun" (`sun_contact_id`, NULL = self) and "what colour is my star" (`self_sun_colour`, NULL = resolve to `starPalette[0]` at read). App-level state, NOT a per-contact column.
Output: `migrations/003-orrery-settings.ts`, registration in `database.ts`, widened `app-settings-dao.ts`, and node:sqlite tests proving both migration paths and the DAO round-trip + validation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-RESEARCH.md
@.planning/phases/13-orrery/13-PATTERNS.md
</context>

## Artifacts this phase produces (source-grounding allowlist)

Every symbol below is NEW in Phase 13 — a source-grounding reviewer must treat these as created-this-phase, not drift:

- New files: `src/db/migrations/003-orrery-settings.ts`, `src/logic/orrery-geometry-logic.ts`, `src/logic/orrery-ring-logic.ts`, `src/logic/ring-reorder-logic.ts`, `src/logic/sun-occupant-logic.ts`, `src/db/orrery-read.ts`, `src/db/ring-seq-dao.ts`, `src/db/sun-picker-read.ts`, `src/screens/OrreryScreen.tsx`, `src/components/SegmentedControl.tsx`, `assets/Inter-SemiBold.ttf` (font).
- New functions: `migration003`, `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`.
- New theme tokens (in `theme-types.ts` / `theme-presets.ts`): `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`.
- New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New DAO fields: `AppSettings.sunContactId`, `AppSettings.selfSunColour`.
- New nav route: `Orrery`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 003 — additive sun columns + runner registration</name>
  <files>src/db/migrations/003-orrery-settings.ts, src/db/migrations/003-orrery-settings.test.ts, src/db/database.ts</files>
  <read_first>
    - src/db/migrations/002-app-settings.ts (the additive-forward-migration analog — module shape, `Migration` typing, `exec.execAsync` DDL)
    - src/db/migrations/runner.ts (the per-step `BEGIN; apply(); PRAGMA user_version = N; COMMIT` wrapper — do NOT hand-roll a transaction)
    - src/db/migrations/001-initial.test.ts (the node:sqlite `@/db/__testkit__/node-sqlite` harness — `nodeSqliteExecutor`, `openTestDb`, running migrations to a target)
    - src/db/database.ts:37 (`TARGET_VERSION = 2`) and :108-113 (the `runMigrations(exec, [migration001, migration002], TARGET_VERSION, …)` array)
  </read_first>
  <behavior>
    - v1→v3: running migrations 001+002+003 to version 3 on a fresh db leaves user_version=3 and `PRAGMA table_info(app_settings)` includes `sun_contact_id` and `self_sun_colour`.
    - v2→v3: seeding a db already at user_version 2 (001+002 applied) then running to 3 adds exactly the two columns and reaches user_version=3 with no error.
    - Both new columns default NULL: after either path, `SELECT sun_contact_id, self_sun_colour FROM app_settings WHERE id=1` returns `{ null, null }`.
    - Idempotent registration: `TARGET_VERSION` is 3 and the runner array ends with `migration003`.
  </behavior>
  <action>
    Create `src/db/migrations/003-orrery-settings.ts` mirroring the module shape of `002-app-settings.ts`. Export two DDL string constants and the `migration003: Migration` object (version 3). The migration issues two `ALTER TABLE app_settings ADD COLUMN` statements via `exec.execAsync`: `sun_contact_id INTEGER REFERENCES contacts(id) ON DELETE SET NULL` (NULL = self; the FK makes a hard-purged sun-contact auto-revert — A1) and `self_sun_colour TEXT` (NULL = unresolved). Both columns are nullable with NO non-null default — this is what makes `ADD COLUMN` with a `REFERENCES` clause legal in SQLite, and keeps the migration starting-state-independent (forward-only). Do NOT store a hex literal default for `self_sun_colour` (Pitfall 3 — NULL is resolved to `starPalette[0]` at read, keeping the palette single-sourced). Do NOT open your own transaction — the runner wraps each step atomically. In `database.ts`: change `TARGET_VERSION` to 3 and append `migration003` (and its import) to the `runMigrations` array. Write the node:sqlite test asserting the two behaviors above (v1→v3 and v2→v3), both columns present, both default NULL, following the `001-initial.test.ts` harness. This migration is IRREVERSIBLE on device — never edit a shipped migration; treat it as permanent.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/003-orrery-settings.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `003-orrery-settings.test.ts` proves BOTH v1→v3 and v2→v3 land at user_version=3 with both columns present and defaulting NULL (green).
    - No hex literal appears anywhere in `003-orrery-settings.ts` (self_sun_colour default is NULL).
    - `database.ts` `TARGET_VERSION === 3` and the runner array is `[migration001, migration002, migration003]`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Migration 003 exists, is registered, applies from both v1 and v2, and its node:sqlite test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen app-settings-dao for sun fields + validation</name>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <read_first>
    - src/db/app-settings-dao.ts (whole file — the four parallel structures to widen: `AppSettings` interface :30, `AppSettingsRow` :51, `COLUMN_OF` :77, `getAppSettings` SELECT :93, and the `assertHour`/`assertToggle` validators :114-130 that new validators must mirror; the validate-BEFORE-`inWriteTransaction` posture at :145)
    - src/db/app-settings-dao.test.ts (the existing suite to extend — read the existing round-trip + validation cases and mirror them for the new fields)
  </read_first>
  <behavior>
    - Read round-trip: after migration to v3, `getAppSettings` returns `sunContactId: null` and `selfSunColour: null` on a fresh seed.
    - Write round-trip: `updateAppSettings(exec, { sunContactId: 5 }, now)` then `getAppSettings` returns `sunContactId: 5`; `updateAppSettings(exec, { selfSunColour: '#F2C14E' }, now)` reads back that string.
    - Clear: `updateAppSettings(exec, { sunContactId: null }, now)` reads back null.
    - Validation (throws, no write): `updateAppSettings(exec, { sunContactId: 0 })` and `{ sunContactId: -3 }` and `{ sunContactId: 1.5 }` throw; `{ selfSunColour: 'red' }` and `{ selfSunColour: '#FFF' }` and `{ selfSunColour: '#GGGGGG' }` throw; `{ selfSunColour: '#f2c14e' }` (lowercase 6-hex) is accepted.
  </behavior>
  <action>
    Widen `app-settings-dao.ts` in place, mirroring the shipped `deliveryHour`/`notificationsEnabled` treatment across all four structures: add `sunContactId: number | null` + `selfSunColour: string | null` to `AppSettings`; add `sun_contact_id: number | null` + `self_sun_colour: string | null` to `AppSettingsRow`; add `sunContactId: "sun_contact_id"` + `selfSunColour: "self_sun_colour"` to `COLUMN_OF`; extend the `getAppSettings` SELECT column list and return-object mapping (map raw NULL straight through as null — do NOT resolve to a palette colour here; the DAO cannot import theme). Add two validators mirroring `assertHour`/`assertToggle`: one asserting `sun_contact_id` is null or a positive integer, one asserting `self_sun_colour` is null or matches `/^#[0-9A-Fa-f]{6}$/`. Call them in `updateAppSettings` for the two new fields BEFORE the transaction opens (the existing :145 posture), so a malformed value never reaches SQL (V5 input validation; palette MEMBERSHIP is enforced UI-side, not here). Extend `app-settings-dao.test.ts` with the behavior cases above.
  </action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The widened suite covers read round-trip, write round-trip, clear-to-null, and every validation throw/accept case above (green).
    - `updateAppSettings` validation for both new fields runs before `inWriteTransaction` opens (a bad value throws with zero writes).
    - `getAppSettings` returns raw NULL as `null` for `selfSunColour` (no palette resolution in the DAO).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The DAO reads, writes, clears, and validates both sun fields; its node:sqlite suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → app-settings-dao | User-chosen sun-occupant id and star colour cross into a SQL write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Tampering | `updateAppSettings` write path | low | mitigate | Every runtime value `?`-bound; only closed column-name constants interpolated (matches every shipped DAO). |
| T-13-01-02 | Tampering/DoS | `sun_contact_id` / `self_sun_colour` values | low | mitigate | Validate before the write opens: null|positive-int and null|`/^#[0-9A-Fa-f]{6}$/`; `changes===1` guard rolls back a bad id. |
| T-13-01-03 | Info disclosure | migration/read path | low | accept | Fully offline, local SQLite only — no network introduced; nothing to disclose. |

No high-severity threats. No new dependency, no network, no secrets.
</threat_model>

<verification>
- `npx vitest run src/db/migrations/003-orrery-settings.test.ts src/db/app-settings-dao.test.ts` — both green.
- `npx tsc --noEmit` and `npm run check:colors` clean (no hex left this file set outside theme-presets).
- Migration 003 is byte-additive: `git diff` shows migrations 001/002 unchanged.
</verification>

<success_criteria>
Both migration paths (v1→v3, v2→v3) land clean; the DAO round-trips and validates both sun fields; `TARGET_VERSION=3` with `migration003` registered.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-01-SUMMARY.md` when done.
</output>
