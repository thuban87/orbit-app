---
phase: 13-orrery
plan: 08
type: execute
wave: 4
depends_on: [13-05, 13-06, 13-07]
files_modified:
  - .planning/phases/13-orrery/13-VERIFICATION.md
  - .planning/phases/13-orrery/13-08-SUMMARY.md
autonomous: false
requirements: [ORR-01, ORR-02, ORR-03, ORR-04, ORR-05, ORR-06]
must_haves:
  truths:
    - "A release APK built through the desktop pipeline (with the bundled .ttf) installs and launches on the physical Pixel, and the orrery is reachable from the dashboard ◎ button."
    - "On device: rings/planets/sun render with correct status colours; a contact photo decodes in a planet via Skia file:// (fallback swatch+initials when absent); the Status↔Relationship morph feels right (~500ms, angle+colour, radius fixed); tap→profile and radial drag→ring_seq work; the ambient layer pauses when backgrounded/blurred; the empty-orbit prompt shows."
    - "The owner has signed off on the subjective feel (morph, ambient subtlety), the on-device performance, and the A2 angle-map — or logged tuning to apply."
  artifacts:
    - .planning/phases/13-orrery/13-VERIFICATION.md (UAT evidence — screenshots + observations)
  key_links:
    - "The .ttf must be bundled by `expo prebuild --clean` before the initials fallback works on device (build-phase asset)."
    - "Perf/battery claims are physical-Pixel-only (Skia render loop; the emulator cannot assess it) and must state the thread the evidence covers."
---

<objective>
Build the orrery through the desktop pipeline and verify it on the physical Pixel — the render surface's only real acceptance test. Agent-driven build + adb/uiautomator capture of every ORR behavior, then the owner's sign-off on the subjective feel, on-device performance, and the A2 angle-map.

Purpose: The orrery is a Skia render-loop feature; correctness of geometry/DAO math is proven node-side, but render, motion feel, `file://` decode, and perf are UI-observable-only and Pixel-only. This plan closes them.
Output: an installed release build on the Pixel, captured UAT evidence, and owner sign-off (or a tuning list).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-VALIDATION.md
@docs/runbooks/desktop-build-pipeline.md
</context>

## Artifacts this phase produces (source-grounding allowlist)

New files: `orrery-geometry-logic.ts`, `orrery-ring-logic.ts`, `ring-reorder-logic.ts`, `sun-occupant-logic.ts`, `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts`, `OrreryScreen.tsx`, `SegmentedControl.tsx`, `src/components/orrery/OrbitBody.tsx`, `src/components/orrery/OrreryCanvas.tsx`, `src/components/orrery/SunBody.tsx`, `migrations/003-orrery-settings.ts`, `assets/Inter-SemiBold.ttf`. New functions: `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `deriveOrreryMetrics`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`, `migration003`. New theme tokens: `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`. New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New nav route: `Orrery`.

<tasks>

<task type="auto">
  <name>Task 1: Desktop prebuild + release APK + install on the Pixel</name>
  <files>(no source changes — build + install)</files>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (the full commit → tar-over-ssh to `droid` → `npm ci` + `expo prebuild --clean` + `gradlew.bat assembleRelease` → scp APK back → `adb -s <serial> install` loop; the package id `com.bwales.orbit`)
    - .planning/STATE.md "Build/test pipeline" blocker note (transport is scp/tar-over-ssh, NOT git push; `ssh droid` via Tailscale; JDK 17 + Android SDK on droid)
  </read_first>
  <action>
    Confirm the working tree is committed locally (do NOT push — agents never push). Run the desktop-build-pipeline runbook: transport the repo to `droid` via tar-over-ssh (NOT git push), `npm ci`, `expo prebuild --clean` (REQUIRED so the new `assets/Inter-SemiBold.ttf` and any native config are picked up), `gradlew.bat assembleRelease`, scp the APK back, and `adb -s <serial-from-adb-devices> install -r` onto the Pixel. Resolve the serial from `adb devices -l` (never hardcode). Run `emu-connect` first if anything looks disconnected. Confirm the app launches to the dashboard and the ◎ Orbit button is present. If the phone shows `unauthorized`, ask the owner to accept the USB-debugging prompt (only the owner can).
  </action>
  <verify>
    <automated>adb devices -l | awk '$2=="device"' | wc -l</automated>
    <human-check>A release APK built with the bundled .ttf installs on the Pixel and launches to the dashboard; the ◎ Orbit button opens the orrery. (Agent drives the build + install; owner confirms the app is running the new build.)</human-check>
  </verify>
  <acceptance_criteria>
    - `expo prebuild --clean` + `assembleRelease` succeed on `droid`; the APK installs on the Pixel (one `device` in `adb devices`).
    - The app launches to the dashboard and the orrery opens from the ◎ button.
    - No `git push` was used (transport was scp/tar-over-ssh).
  </acceptance_criteria>
  <done>The new release build is installed and running on the physical Pixel with the orrery reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the Pixel + capture UAT evidence for every ORR behavior</name>
  <files>.planning/phases/13-orrery/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-orrery/13-VALIDATION.md "Manual-Only Verifications" (the six device-UAT items + how to test each)
    - CLAUDE.md "Android device — you can drive the app yourself" (adb screencap / uiautomator dump / input tap-swipe; the tap false-negative caveat; perf-thread caveats)
  </read_first>
  <action>
    Drive the app on the Pixel with `adb`/`uiautomator` and capture screenshots + notes for each ORR behavior into `13-VERIFICATION.md`: (ORR-01) open the orrery — capture the render; confirm rings/planets/sun render and statuses are correctly coloured; confirm a never-contacted contact is ABSENT. (H2) with a large orbiting set (enough contacts to overflow the viewport), confirm the furthest ring stays on-screen and tappable — the `deriveOrreryMetrics` proportional gap compression working on the real screen size. (ORR-05) confirm the sun renders the self photo/initials with the picked star colour; set a contact as the sun in Settings and confirm the sun glows that contact's status; capture the empty-orbit prompt (temporarily, or on a fresh profile). (ORR-01 file://) confirm a contact WITH a photo decodes in its planet (Skia `useImage(file://)`), and a photo-less contact shows the swatch+initials fallback (the .ttf path). (ORR-04) confirm a rogue contact shows the cold/extinguished body + faint-trace ring at max (bounded) drift and is still tappable. (ORR-02) toggle Status↔Relationship — capture both; confirm radius unchanged, angle+colour morph. (ORR-03) tap a planet → Profile; background the app / navigate away and confirm the canvas stops animating (unmount). (ORR-06) drag a planet radially and confirm the ring reorders on release; re-open and confirm ring_seq persisted. Use `uiautomator dump` for exact tap bounds (not eyeballed pixels; heed the small-Pressable tap false-negative — verify against code if a tap "misses"). Record which THREAD any perf observation covers (JS vs UI/render). NEW-to-repo Skia idioms first proven on THIS device build (outside the proven `CropPhotoScreen` surface, so first-time on-device verification points — a failure is expected-to-be-caught here, not a surprise): (i) `useFonts` — confirm the planet/sun initials fallback actually renders glyphs for a photo-less contact (real initials, not tofu/blank boxes); (ii) `useClock` — confirm the ambient starfield/sun-pulse loop animates while focused AND fully stops when the `<Canvas>` unmounts on blur/background (this doubles as the ORR-03 pause check).
  </action>
  <verify>
    <automated>adb exec-out screencap -p > /tmp/claude-1000/-home-bwales-projects-orbit-app/042f241e-8520-4da5-a8e2-6188fa880f7c/scratchpad/orrery-uat.png && echo captured</automated>
    <human-check>Every ORR behavior above was exercised on the Pixel and evidence (screenshots + observations) recorded in 13-VERIFICATION.md, including a confirmed `file://` photo decode in a planet.</human-check>
  </verify>
  <acceptance_criteria>
    - 13-VERIFICATION.md records captured evidence for ORR-01 (render + never-contacted excluded), ORR-02 (morph), ORR-03 (tap→profile + pause-on-blur), ORR-04 (rogue), ORR-05 (sun/star/empty), ORR-06 (drag→ring_seq persists).
    - The Skia `file://` photo decode happy-path is confirmed on device (a contact photo renders in a planet); the initials fallback renders for a photo-less contact.
    - The two new-to-repo Skia idioms are confirmed on device: `useFonts` renders real initials for a photo-less contact (no tofu), and `useClock` ambient animates while focused and halts on blur/background (canvas unmount).
    - H2 responsive layout confirmed on device: with a large orbiting set (enough contacts to overflow the viewport), the furthest ring stays on-screen and tappable (proportional `effectiveGap` compression from `deriveOrreryMetrics`), not clipped off-canvas.
    - Tap targets were resolved via `uiautomator dump` bounds, not eyeballed pixels.
  </acceptance_criteria>
  <done>On-device evidence for all six ORR requirements is captured, including the file:// decode spike.</done>
</task>

<task type="auto">
  <name>Task 3: Owner sign-off — feel, performance, and the A2 angle map</name>
  <files>.planning/phases/13-orrery/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-orrery/13-VERIFICATION.md (this plan Task 2 — the captured evidence the owner reviews against)
    - .planning/phases/13-orrery/13-RESEARCH.md §"Assumptions Log" (A2 angle map, A7 archived-sun) and §"Open Questions" (the device-tuning calls to surface)
  </read_first>
  <action>
    Present the on-device orrery to the owner for sign-off (end-of-phase human verification; no checkpoint task per human_verify_mode=end-of-phase). Walk the owner through: (1) the render reads as a calm solar system (rings by closeness, planets coloured by status, sun at centre); (2) the Status｜Relationship morph feels right (~500ms ease-in-out, angle+colour fade, radius steady, no long-way spin); (3) the ambient layer is SUBTLE and stops while the app is backgrounded/blurred; (4) tap→profile and radial-drag→ring_seq (persists); (5) the A2 angle map on device — does interval-progress-as-angle read correctly (fresh at top, overdue drifting), or would a top/bottom-emphasis curve read better (progressToAngle is a single swappable pure fn); (6) on-device performance of the ambient layer + morph (Pixel-only; state the thread the evidence covers). Record the owner's verdict + any tuning (morph duration, ambient intensity, angle-map shape, star/muted hexes, drift spans) in 13-VERIFICATION.md. Any tuning is a top-of-file-constant or single-pure-fn edit, not a redesign.
  </action>
  <verify>
    <human-check>Owner has reviewed the orrery on the Pixel and either approved Phase 13 or listed tuning to apply; the verdict is recorded in 13-VERIFICATION.md. Owner reply "approved" closes the phase; a tuning list routes to a follow-up pass.</human-check>
  </verify>
  <acceptance_criteria>
    - The owner has exercised render, morph, ambient/pause, tap→profile, and drag→ring_seq on the Pixel and given a verdict.
    - The A2 angle-map and on-device performance were judged on device; any tuning is captured as constant/pure-fn edits in 13-VERIFICATION.md.
  </acceptance_criteria>
  <done>Owner sign-off (or a tuning list) is recorded; Phase 13 is ready to close on "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → device | A release APK is transported to `droid`, built, and installed on the Pixel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08-01 | Tampering | build transport | low | mitigate | Transport is scp/tar-over-ssh to the owner's own desktop over Tailscale; no `git push`; no third-party CI. |
| T-13-08-02 | Tampering | bundled font provenance | low | mitigate | The `.ttf` is a vendored, licence-verified asset (OFL/Apache) added in 13-05 — not a registry dependency. |
| T-13-08-03 | Info disclosure | on-device UAT | low | accept | The orrery is fully offline; UAT observes only local render. No network egress introduced. |

No high-severity threats. No new dependency, no network, no secrets in this plan.
</threat_model>

<verification>
- Release APK installs and launches on the Pixel; the orrery is reachable and renders.
- All six ORR device-UAT items exercised with captured evidence in 13-VERIFICATION.md.
- Owner sign-off recorded (or tuning list captured for a follow-up pass).
</verification>

<success_criteria>
The orrery is proven on the physical Pixel — render, morph, ambient/pause, rogue, tap→profile, drag→ring_seq, Settings sun/star, and the `file://` decode — with owner sign-off on feel, performance, and the angle map.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-08-SUMMARY.md` when done; record UAT evidence in `.planning/phases/13-orrery/13-VERIFICATION.md`.
</output>
