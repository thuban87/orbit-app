---
phase: 13-orrery
plan: 06
type: execute
wave: 2
depends_on: [13-01, 13-03, 13-04]
files_modified:
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [ORR-05, ORR-06]
must_haves:
  truths:
    - "Settings has a 'Your star' row of the ~6 starPalette swatches; tapping one writes self_sun_colour; the current pick (or starPalette[0] when unset) carries the accent selection ring."
    - "Settings has a 'Sun / centre' row opening a picker of 'Me' first, then favourites, then all contacts; selecting writes sun_contact_id (NULL for 'Me')."
    - "Both controls persist via the widened updateAppSettings and reload their displayed state on focus."
  artifacts:
    - src/screens/SettingsScreen.tsx (Your star + Sun / centre controls)
  key_links:
    - "The picker source is listSunCandidates (favourites-first) with a synthetic 'Me' (NULL id) prepended by the UI."
    - "self_sun_colour NULL renders the gold starPalette[0] as selected (read-time resolution — no stored hex default)."
    - "M4: if the stored sun_contact_id points to an archived or missing contact, the row shows 'Me' — the SAME self fallback the canvas applies (resolveSunOccupant, 13-04/13-05) — so Settings and canvas never disagree."
    - "M6: both writes (swatch + occupant) go through the screen's try/catch + Logger.error posture (mirror the `persist` helper at SettingsScreen :147-157) so a future non-conforming token surfaces a handled error, not an unhandled throw."
    - "M3: switching sun_contact_id here needs NO ring_seq normalization — orrery-read derives the display rank densely at read (13-03), so a sun change can never corrupt ring order."
    - "This IS ORR-06's 'assign the sun', RELOCATED to Settings by owner decision — NOT an orrery gesture. Do not read the absence of an orrery long-press as an ORR-06 gap."
---

<objective>
Add the two Settings controls that own the sun: "Your star" (the self-sun colour picker over the themed `starPalette`) and "Sun / centre" (the occupant picker: Me / favourites / all contacts). Both write `app_settings` through the widened DAO. This is ORR-05's self-sun colour and the owner-relocated half of ORR-06 (assign the sun from Settings, not the orrery).

Purpose: Let the user choose their star colour and who sits at the centre, mirroring the shipped "Your photo" self pattern — without a canvas gesture (long-press was owner-rejected).
Output: the "Your star" swatch row + "Sun / centre" picker in `SettingsScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-UI-SPEC.md
@.planning/phases/13-orrery/13-CONTEXT.md
</context>

## Artifacts this phase produces (source-grounding allowlist)

New files: `orrery-geometry-logic.ts`, `orrery-ring-logic.ts`, `ring-reorder-logic.ts`, `sun-occupant-logic.ts`, `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts`, `OrreryScreen.tsx`, `SegmentedControl.tsx`, `src/components/orrery/OrbitBody.tsx`, `src/components/orrery/OrreryCanvas.tsx`, `src/components/orrery/SunBody.tsx`, `migrations/003-orrery-settings.ts`, `assets/Inter-SemiBold.ttf`. New functions: `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `deriveOrreryMetrics`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`, `migration003`. New theme tokens: `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`. New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New nav route: `Orrery`.

## Owner decision to record (never mis-flag as an ORR-06 gap)

ORR-06 reads "assign the sun from the orrery". The owner **relocated** sun-occupant assignment to this Settings picker and **rejected** an orrery long-press (accident-prone, collides with the radial drag). The half of ORR-06 that stays on the canvas is the `ring_seq` radial drag (13-07). A plan-reviewer must NOT restore an orrery-based sun assignment as a gap fix.

**Doc-sync pointer (L10):** REQUIREMENTS.md (ORR-06) and ROADMAP crit-4 still say the literal "assign the sun from the orrery". That wording is satisfied here in Settings by the owner's CONTEXT relocation decision — a diff-scoped auditor seeing no orrery sun-assignment gesture must read this as SATISFIED, not a gap. Do NOT edit REQUIREMENTS.md; this pointer is the record.

<tasks>

<task type="auto">
  <name>Task 1: "Your star" self-sun colour swatch row</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx:82-116 (the self-record load + `reloadProfile` on-focus pattern to mirror for the settings load) and :537-554 ("Your photo" row — the self-control chrome to mirror) and :681-721 (styles.row / styles.sectionHeading / styles.helper) and :147-157 (the `persist` helper — the try/catch + `Logger.error(LOG_SCOPE, …)` write posture BOTH new writes must mirror, M6)
    - src/db/app-settings-dao.ts (this phase — `getAppSettings` → `selfSunColour`, `updateAppSettings({ selfSunColour })`)
    - src/theme/theme-presets.ts (`starPalette` seed — the swatches to render; gold index 0 is the unset default)
    - src/theme/theme-types.ts (`ThemePalette.starPalette` typing)
  </read_first>
  <action>
    Add a new "Your orbit" section (sectionHeading idiom) to `SettingsScreen.tsx` above or near "Your photo". Inside it add a "Your star" row (helper: "Pick the colour of your star at the centre of your orbit." per the UI-SPEC Copywriting Contract) rendering a horizontal row of the `useTheme().colors.starPalette` swatches as circular ≥44px-diameter Pressables (mirror the Phase-5 avatar-swatch discipline). Load the current `selfSunColour` via `getAppSettings` in a focus effect (cancelled-flag async guard, like `reloadProfile`); the SELECTED swatch = the one equal to `selfSunColour ?? starPalette[0]` (NULL resolves to gold at read — no stored hex default) and carries an `accent` ring/check (the reserved-accent list). Tapping a swatch calls `updateAppSettings(getExecutor(), { selfSunColour: <token> }, localDateTime())` then reloads. M6 (addresses review concern): wrap that write in the screen's existing error posture — a `try { await updateAppSettings(…); reload } catch (err) { Logger.error(LOG_SCOPE, "failed to persist star colour", err) }` mirroring the `persist` helper at :147-157 — so if the deferred design pass ever seeds a non-`#RRGGBB` `starPalette` token (which the DAO validator rejects), the tap surfaces a handled, logged error instead of an unhandled promise rejection. Every colour via tokens (the swatch fills ARE `starPalette` tokens — legitimate token use, not hex). testID `settings-your-star-row`, each swatch `settings-star-swatch-{index}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - "Your star" renders all `starPalette` swatches as ≥44px targets; the selected one (selfSunColour, or starPalette[0] when unset) shows the accent ring.
    - Tapping a swatch persists `self_sun_colour` via `updateAppSettings` and the selection updates on reload.
    - M6: the swatch write is wrapped in a try/catch that logs via `Logger.error` (mirroring the `persist` helper) — a rejected write cannot escape as an unhandled rejection.
    - `npm run check:colors` passes (swatch colours are starPalette tokens, no hex); `npx tsc --noEmit` + `npm test` green.
  </acceptance_criteria>
  <done>The self-sun colour is user-pickable from the themed star palette and persists; render is device-UAT in 13-08.</done>
</task>

<task type="auto">
  <name>Task 2: "Sun / centre" occupant picker (Me / favourites / all)</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx:556-654 (the "Home screen" section + a Pressable row idiom; the row chrome for a tappable settings row)
    - src/db/sun-picker-read.ts (this phase — `listSunCandidates` favourites-first list)
    - src/db/app-settings-dao.ts (this phase — `getAppSettings` → `sunContactId`, `updateAppSettings({ sunContactId })`; NULL = self) and :147-157 of SettingsScreen (the `persist` try/catch posture, M6)
    - src/db/contact-read.ts:65 (`getContactHeader` — resolves the occupant name AND `archived_at`; an archived/missing occupant is the M4 self-fallback trigger)
    - src/screens/SettingsScreen.tsx (any existing Modal+FlatList picker idiom in-file, else the Phase-3 dropdown Pressable+Modal+FlatList pattern — zero picker deps)
  </read_first>
  <action>
    Add a "Sun / centre" row (helper: "Choose who sits at the centre — you, or someone you orbit around.") to the "Your orbit" section. The row shows the current occupant name: "Me" when `sunContactId` is NULL. M4 (addresses review concern): when `sunContactId` is non-null, resolve the name via `getContactHeader(getExecutor(), sunContactId)` and show "Me" if the header is missing OR `archived_at !== null` — the SAME archived/missing→self fallback the canvas applies (resolveSunOccupant, 13-05), so Settings and the orrery never disagree about a hidden occupant. (Optionally also clear the stored setting to NULL when the occupant is found archived; showing "Me" is the minimum.) The row opens a picker on press. Build the picker as a `Pressable` row → `Modal` + `FlatList` (the zero-dependency dropdown idiom already used in the app — no new picker library). The list is `[{ id: null, name: 'Me' }, ...listSunCandidates(getExecutor())]` — the synthetic "Me" (NULL id) first, then favourites-first candidates (already archived-excluded by the read). Selecting a row calls `updateAppSettings(getExecutor(), { sunContactId: <id | null> }, localDateTime())`, closes the modal, and reloads the displayed occupant; M6: wrap that write in the same try/catch + `Logger.error` posture as the swatch write (mirror the `persist` helper at :147-157). Load current `sunContactId` + resolve its name (with the archived fallback) in the focus effect. Every colour via tokens; the modal scrim uses `colors.background` at opacity (the shipped scrim idiom — no colour literal). testID `settings-sun-centre-row`, picker `settings-sun-picker`, option `settings-sun-option-{id|me}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - "Sun / centre" opens a picker with "Me" first, then favourites, then other contacts (from `listSunCandidates`); the current occupant name shows on the row.
    - Selecting "Me" writes `sun_contact_id = NULL`; selecting a contact writes its id; both persist and reload.
    - M4: when the stored `sun_contact_id` points to an archived or missing contact, the row shows "Me" (the same self fallback the canvas uses) — not a stale/blank name.
    - M6: the occupant write is wrapped in the try/catch + `Logger.error` posture (mirroring `persist`).
    - `npm run check:colors` passes (scrim via background token); `npx tsc --noEmit` + `npm test` green.
  </acceptance_criteria>
  <done>The sun occupant is assignable from Settings (Me / favourites / all) and persists — ORR-06's relocated half; render is device-UAT in 13-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings picker → app-settings-dao | The chosen star colour + sun occupant id cross into a validated SQL write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06-01 | Tampering | self_sun_colour write | low | mitigate | The UI only ever writes a `starPalette` token; the DAO validator (13-01) additionally rejects a non-`#RRGGBB` value before the write. |
| T-13-06-02 | Tampering | sun_contact_id write | low | mitigate | The picker only offers real candidate ids or NULL; the DAO validator rejects a non-positive-int; the migration FK reverts a later hard-purged occupant to self. |
| T-13-06-03 | Info disclosure | picker list | low | accept | `listSunCandidates` reads local SQLite only; no network on the read path. |

No high-severity threats.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npm test` — green.
- Persistence is proven by the 13-01 DAO tests; the Settings render + pick flow is DEVICE-UAT (13-08).
</verification>

<success_criteria>
The user can pick their star colour and assign the sun (Me / favourites / all) from Settings, both persisting through the widened DAO — closing ORR-05's colour pick and ORR-06's relocated sun-assignment.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-06-SUMMARY.md` when done.
</output>
