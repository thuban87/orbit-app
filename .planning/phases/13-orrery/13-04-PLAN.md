---
phase: 13-orrery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/theme-types.ts
  - src/theme/theme-presets.ts
  - src/logic/orrery-ring-logic.ts
  - src/logic/orrery-ring-logic.test.ts
  - src/logic/sun-occupant-logic.ts
  - src/logic/sun-occupant-logic.test.ts
autonomous: true
requirements: [ORR-01, ORR-04, ORR-05]
must_haves:
  truths:
    - "space-dark gains starPalette (~6 ordered star colours, gold #F2C14E at index 0), mutedStable/Wobble/Decay, and rogueExtinguished — all in theme-presets.ts, the only hex file."
    - "orreryRingStyle maps status→{color,opacity,width,strokeStyle,bodyFill}: solid→dashed→faded→faint-trace, reusing ringVisual's colour; the rogue BODY fill is rogueExtinguished while the rogue RING stays colors.rogue."
    - "resolveSunOccupant: NULL→self (glow selfSunColour ?? starPalette[0]); a live contact id→that contact (glow = its status colour); an archived/missing id→self."
  artifacts:
    - src/theme/theme-presets.ts (new tokens)
    - src/logic/orrery-ring-logic.ts
    - src/logic/sun-occupant-logic.ts
  key_links:
    - "orrery-ring-logic REUSES ringVisual() colour from contact-card-ring.ts — status→colour is never re-mapped."
    - "self_sun_colour NULL resolves to starPalette[0] here at read (no hex in the migration — Pitfall 3)."
---

<objective>
The orrery's visual vocabulary: the new theme tokens (star palette, muted morph endpoints, extinguished-rogue body), the pure status→ring-style mapping that extends the shipped `ringVisual` with a stroke-style axis, and the pure sun-occupant resolver. All colours land in `theme-presets.ts` (the single hex file); the logic modules take a resolved palette and stay node-testable.

Purpose: Give the render screen one source of truth for every orrery colour and ring style, and one pure function that decides who/what colour the sun is — including the archived/missing fallback to self (A7).
Output: token declarations + seeds, `orrery-ring-logic.ts`, `sun-occupant-logic.ts` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-UI-SPEC.md
@.planning/phases/13-orrery/13-PATTERNS.md
</context>

## Artifacts this phase produces (source-grounding allowlist)

New files: `orrery-geometry-logic.ts`, `orrery-ring-logic.ts`, `ring-reorder-logic.ts`, `sun-occupant-logic.ts`, `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts`, `OrreryScreen.tsx`, `SegmentedControl.tsx`, `migrations/003-orrery-settings.ts`, `assets/Inter-SemiBold.ttf`. New functions: `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`, `migration003`. New theme tokens: `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`. New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New nav route: `Orrery`.

## Assumptions surfaced for plan-review (do not silently resolve)

- **A7 (archived/missing sun occupant → self):** archiving is soft (`archived_at`), so the migration-003 FK `ON DELETE SET NULL` does NOT fire on archive. `resolveSunOccupant` therefore treats an archived OR missing `sun_contact_id` as self, so the sun never glows a hidden contact. Owner may instead prefer the archived contact keep glowing — surfaced for plan-review.
- **Palette/muted exact hexes** are design-pass seeds (CONTEXT deferred, owner §12.4 taste) — ship the UI-SPEC values as the tunable default, mirroring the `avatarSwatches`/`gravityTiers` "owner-tunable seed" discipline.

<tasks>

<task type="auto">
  <name>Task 1: Add orrery theme tokens (types + space-dark seeds)</name>
  <files>src/theme/theme-types.ts, src/theme/theme-presets.ts</files>
  <read_first>
    - src/theme/theme-types.ts:29-119 (`ThemePalette` — the shape to extend; note the `avatarSwatches`/`gravityTiers` `readonly string[]` declarations with their "order-stable, never reorder" doc comments to mirror)
    - src/theme/theme-presets.ts:22-70 (`space-dark.dark` — the ONLY hex-literal file; the `avatarSwatches`/`statusStable…`/`gravityTiers` seed block to append after)
    - .planning/phases/13-orrery/13-UI-SPEC.md "New theme tokens this phase adds" (the exact seed hexes + the rationale for each nudge)
  </read_first>
  <action>
    Declare five new keys on `ThemePalette` in `theme-types.ts`, each with a doc comment mirroring the `avatarSwatches`/`gravityTiers` "owner-tunable seed, order-stable" idiom: `starPalette: readonly string[]` (~6 ordered self-sun star colours — reordering restyles every self-sun; gold at index 0 is the default self-sun), `mutedStable: string`, `mutedWobble: string`, `mutedDecay: string` (the desaturated colour endpoints the morph fades toward — same hue, low saturation, NOT greyscale), and `rogueExtinguished: string` (the cold dark rogue BODY fill, distinct from the warm `rogue` ring). Seed the values in `space-dark.dark` in `theme-presets.ts` — the ONLY file where a hex literal is permitted — using the UI-SPEC seeds: `starPalette` = the six curated star colours (gold, amber, rose-red, violet, cyan, ice-white) with gold as index 0; the three `muted*` desaturated status hues; and the cold blue-grey `rogueExtinguished`. Do NOT put any of these hexes anywhere but `theme-presets.ts`. Add nothing to `light` (only `dark` ships).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `ThemePalette` declares `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`; `space-dark.dark` provides all five (starPalette length ≥ 6, gold at index 0).
    - `npm run check:colors` passes — every new hex is inside `theme-presets.ts` only.
    - `npx tsc --noEmit` clean (no other consumer breaks from the widened palette type).
  </acceptance_criteria>
  <done>The five orrery tokens are declared and seeded in the one hex file; check:colors + tsc are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: orrery-ring-logic — status→stroke vocabulary (reuses ringVisual)</name>
  <files>src/logic/orrery-ring-logic.ts, src/logic/orrery-ring-logic.test.ts</files>
  <read_first>
    - src/components/contact-card-ring.ts:44-61 (`ringVisual(status, colors)` → `{color,opacity,width}` — REUSE its colour selection; do NOT re-map status→colour)
    - src/theme/theme-types.ts (`ThemePalette` — the resolved palette this pure module receives as a param, incl. the new `rogueExtinguished`)
    - .planning/phases/13-orrery/13-UI-SPEC.md "Status colours" table (the solid/dashed/faded/faint-trace + body-fill contract per status)
  </read_first>
  <behavior>
    - stable → { color: statusStable, strokeStyle: 'solid', bodyFill: full-colour (statusStable) }.
    - wobble → { color: statusWobble, strokeStyle: 'dashed' }.
    - decay → { color: statusDecay, strokeStyle: 'faded' } (lower opacity per ringVisual/UI-SPEC).
    - rogue → { color (ring): colors.rogue, strokeStyle: 'faintTrace', bodyFill: colors.rogueExtinguished } — the RING is warm rogue amber, the BODY is the cold extinguished fill.
    - null (never-contacted) is not an orrery case (they are excluded upstream), but the function returns a safe neutral (border) rather than throwing.
    - Every returned colour comes from the passed palette — no hex literal in the module.
  </behavior>
  <action>
    Create `orrery-ring-logic.ts` (pure, RN-free, takes a `ThemePalette`). Export `orreryRingStyle(status, colors)` returning `{ color, opacity, width, strokeStyle, bodyFill }` and a `StrokeStyle` union `'solid'|'dashed'|'faded'|'faintTrace'`. REUSE `ringVisual(status, colors)` for `{color, opacity, width}` (import it — status→colour is never re-mapped here) and ADD the `strokeStyle` axis (solid→dashed→faded→faintTrace by status) plus `bodyFill` (the planet body fill: the full status colour for stable/wobble/decay, `colors.rogueExtinguished` for rogue while the ring keeps `colors.rogue`). Every colour resolves through the passed palette — NO hex (check:colors). Add the node test covering each status→style row above.
  </action>
  <verify>
    <automated>npx vitest run src/logic/orrery-ring-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Test proves the solid→dashed→faded→faintTrace vocabulary and the rogue ring/body split (ring colors.rogue, body colors.rogueExtinguished) — green.
    - The module imports and reuses `ringVisual` for colour (no second status→colour map); no hex literal appears (check:colors passes).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`orreryRingStyle` extends ringVisual with the stroke vocabulary + extinguished-rogue body, node-tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sun-occupant-logic — resolveSunOccupant (self / contact / archived fallback)</name>
  <files>src/logic/sun-occupant-logic.ts, src/logic/sun-occupant-logic.test.ts</files>
  <read_first>
    - src/logic/favourites-reorder-logic.ts (the pure resolver module shape to mirror)
    - .planning/phases/13-orrery/13-RESEARCH.md §"Sun Occupant Resolution" (the NULL→self / live-id→contact-status / archived-missing→self contract, A7)
    - src/db/contact-status-read.ts (`ProfileStatus` type — the status the contact-sun glow uses)
  </read_first>
  <behavior>
    - sunContactId null → { kind: 'self', photo: selfPhoto, glowColor: selfSunColour ?? starPalette[0] }.
    - sunContactId null AND selfSunColour null → glowColor = starPalette[0] (the gold default, resolved at read — not stored).
    - sunContactId = a live contact present in the lookup → { kind: 'contact', contactId, photo: contact.photo, glowColor: statusColorFor(contact.status) } (the user's star pick applies to self only; a contact-sun glows its STATUS colour).
    - sunContactId = an archived contact (lookup marks archived) → falls back to { kind: 'self', … } (A7).
    - sunContactId = a missing/unknown id (lookup returns null) → falls back to self.
  </behavior>
  <action>
    Create `sun-occupant-logic.ts` (pure, RN-free). Export `resolveSunOccupant(input)` where input carries `sunContactId: number | null`, `selfSunColour: string | null`, `starPalette: readonly string[]`, `selfPhoto: string | null`, and a resolved `occupant: { photo: string | null; status: ProfileStatus; archived: boolean } | null` (the screen looks the contact up before calling; a null occupant means missing). Return a `SunOccupant` discriminated union `{ kind: 'self' | 'contact', photo, glowColor, contactId? }`. Logic: NULL id → self with `glowColor = selfSunColour ?? starPalette[0]`; a present, non-archived occupant → contact with `glowColor` = the status colour (pass a `statusColor(status, palette)` mapping in, or take the resolved colour as an input so this module stays palette-agnostic — reuse `ringVisual` colour selection rather than re-mapping); an archived or null occupant → self (A7). No hex literal. Add the node test covering all five behavior cases.
  </action>
  <verify>
    <automated>npx vitest run src/logic/sun-occupant-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Test proves NULL→self, NULL+no-colour→starPalette[0], live-contact→status-glow, archived→self, missing→self (green).
    - The resolver never invents a hex (glowColor comes from selfSunColour/starPalette or a passed status colour); check:colors passes.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`resolveSunOccupant` handles self, contact-status glow, and the archived/missing→self fallback, node-tested green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Theme seeds + two pure resolver modules — no I/O, no SQL, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04-01 | Tampering | hardcoded colour leaking outside the theme | low | mitigate | Every new hex lives only in `theme-presets.ts`; the logic modules take a resolved palette; `check:colors` enforces it. |
| T-13-04-02 | Info disclosure | sun glowing a hidden (archived) contact | low | mitigate | `resolveSunOccupant` falls back to self for an archived/missing occupant (A7) — a hidden contact is never surfaced at the centre. |

No high-severity threats. Pure logic + theme seeds; no trust boundary crossed.
</threat_model>

<verification>
- `npx vitest run src/logic/orrery-ring-logic.test.ts src/logic/sun-occupant-logic.test.ts` — green.
- `npm run check:colors` passes (all new hex inside theme-presets.ts); `npx tsc --noEmit` clean.
</verification>

<success_criteria>
The five tokens exist in the one hex file, the ring-style vocabulary reuses ringVisual, and the sun resolver handles self/contact/archived — all node-tested and colour-clean.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-04-SUMMARY.md` when done.
</output>
