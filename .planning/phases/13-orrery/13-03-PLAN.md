---
phase: 13-orrery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/orrery-read.ts
  - src/db/orrery-read.test.ts
  - src/db/ring-seq-dao.ts
  - src/db/ring-seq-dao.test.ts
  - src/db/sun-picker-read.ts
  - src/db/sun-picker-read.test.ts
autonomous: true
requirements: [ORR-01, ORR-06]
must_haves:
  truths:
    - "listOrbitingContacts returns one row per live, contacted contact (never-contacted and archived excluded), each carrying id/name/photo/ring_seq/status/progress/rarely_responds, densely ordered by ring_seq then created_at then id."
    - "The sun occupant, when a contact id is passed, is excluded from the orbiting scan (it does not also orbit)."
    - "rewriteRingSeq writes ring_seq = array index for the RENDERED orbiting set (the orbiting WHERE minus the excludeContactId sun occupant) in ONE transaction, guarded by unique-ids / count-match / scoped changes===1 — a partial/duplicate/stale list rolls back."
    - "listSunCandidates lists non-archived contacts, favourites first (by rank) then the rest by name."
  artifacts:
    - src/db/orrery-read.ts
    - src/db/ring-seq-dao.ts
    - src/db/sun-picker-read.ts
  key_links:
    - "orrery-read composes PROGRESS_SQL/STATUS_SQL from status.ts — status is never recomputed (a parity test guards it, Pitfall 1)."
    - "rewriteRingSeq's guard set mirrors orrery-read's RENDERED orbiting set — the orbiting WHERE (last_contact IS NOT NULL AND archived_at IS NULL) PLUS the id <> excludeContactId occupant exclusion — so the guard count and the dragged (sun-excluded, N−1) list always agree."
    - "rewriteRingSeq is the FIRST writer of contacts.ring_seq; the single-writer last_contact DAO is untouched."
---

<objective>
The orrery's data layer: a read chokepoint that scans the orbiting set (composing the status SQL, never recomputing it), the first-ever `ring_seq` writer (a near-verbatim clone of the proven `rewriteFavouriteRanks` transactional reorder, its guard scope extended by an `excludeContactId` occupant-exclusion param so the guard matches the sun-excluded rendered set), and the favourites-first contact list the Settings sun-picker consumes.

Purpose: Give the render screen (13-05/13-07) and the Settings sun-picker (13-06) their SQL surfaces, each mirroring a shipped analog exactly, so status stays single-sourced and the `ring_seq` write inherits the favourites-DAO's three guards.
Output: `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts` (+ node:sqlite tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-RESEARCH.md
@.planning/phases/13-orrery/13-PATTERNS.md
</context>

## Artifacts this phase produces (source-grounding allowlist)

New files: `orrery-geometry-logic.ts`, `orrery-ring-logic.ts`, `ring-reorder-logic.ts`, `sun-occupant-logic.ts`, `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts`, `OrreryScreen.tsx`, `SegmentedControl.tsx`, `migrations/003-orrery-settings.ts`, `assets/Inter-SemiBold.ttf`. New functions: `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`, `migration003`. New theme tokens: `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`. New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New nav route: `Orrery`.

## Assumptions surfaced for plan-review (do not silently resolve)

- **A5 (ring_seq leave-gap on archive/purge):** stored `ring_seq` values encode only RELATIVE order — radius rank is derived densely at read (`ORDER BY COALESCE(ring_seq, 1e9), created_at, id`). No renumber sweep is added on archive/purge (matches the shipped `favourite_rank` "ranks need not be gap-free" convention; nothing watches a timestamp). Recommended by CONTEXT.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: orrery-read — orbiting-contact scan (composes status.ts)</name>
  <files>src/db/orrery-read.ts, src/db/orrery-read.test.ts</files>
  <read_first>
    - src/db/dashboard-read.ts:124-144 (CARD_STATUS / CARD_FROM / BASE_WHERE — the `last_contact IS NOT NULL` load-bearing NULL-safety pattern and the CASE-wrapped status/progress projection) and :284-295 (`listFavourites` idiom + `FavouriteRow`)
    - src/db/status.ts:59,67 (`PROGRESS_SQL`, `STATUS_SQL` — IMPORT and compose; never recompute)
    - src/db/dashboard-read.test.ts (the node:sqlite read-test harness + the fuel-parity guard idiom to mirror as a status-parity guard)
    - src/services/photos/photo-storage.ts:130 (`resolvePhotoUri` — the screen resolves the returned relative `photo` to file://; the read returns the raw relative path only)
  </read_first>
  <behavior>
    - Returns one row per contact with `archived_at IS NULL AND last_contact IS NOT NULL`; a never-contacted (last_contact NULL) and an archived contact are both absent.
    - Each row carries id, name, photo (raw relative path or null), ring_seq (nullable), status (from STATUS_SQL), progress (from PROGRESS_SQL), rarely_responds.
    - Ordering is dense: `ORDER BY COALESCE(ring_seq, 1e9), created_at, id` — a contact with a set ring_seq sorts before a NULL one; ties break by created_at then id. Row index (0-based) IS the render rank.
    - Passing an `excludeContactId` omits that one contact (the sun occupant does not also orbit); passing null/undefined excludes nobody.
    - Status parity: the composed SQL contains the imported `STATUS_SQL`/`PROGRESS_SQL` fragments (guard test), so status can never drift from status.ts.
  </behavior>
  <action>
    Create `orrery-read.ts` mirroring `dashboard-read.ts`. Export `listOrbitingContacts(exec, opts?: { excludeContactId?: number | null })` returning `OrbitingContact[]`. Compose `PROGRESS_SQL`/`STATUS_SQL` imported from `@/db/status` (do NOT recompute thresholds — Pitfall 1). WHERE is `archived_at IS NULL AND last_contact IS NOT NULL` (the shipped BASE_WHERE NULL-safety half; snooze is NOT filtered — the orrery shows snoozed contacts) plus, when `excludeContactId` is provided, `AND id <> ?` (`?`-bound). Select id, name, photo, ring_seq, `(PROGRESS_SQL) AS progress`, `(STATUS_SQL) AS status`, rarely_responds. `ORDER BY COALESCE(ring_seq, 1e9), created_at, id`. Pure async read — no transaction, no network, no localtime on stored columns beyond what the imported fragments already do. Add the node:sqlite test covering the behavior cases plus a status-parity assertion (the query string includes the imported fragment), mirroring dashboard-read's fuel-parity guard.
  </action>
  <verify>
    <automated>npx vitest run src/db/orrery-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test proves never-contacted + archived exclusion, the field set, dense ring_seq ordering, excludeContactId omission, and status-parity with status.ts (green).
    - The module imports `PROGRESS_SQL`/`STATUS_SQL` and interpolates only closed code constants + `?`-bound `excludeContactId` (no user string interpolated).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`listOrbitingContacts` scans the orbiting set with single-sourced status and excludes the sun occupant; its test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ring-seq-dao — rewriteRingSeq (first ring_seq writer, favourites clone)</name>
  <files>src/db/ring-seq-dao.ts, src/db/ring-seq-dao.test.ts</files>
  <read_first>
    - src/db/favourites-dao.ts:105-143 (`rewriteFavouriteRanks` — the verbatim clone target: the 3 guards, N raw UPDATEs in ONE `inWriteTransaction`, `changes===1` per row) and :92-101 (the non-reentrancy + empty-list-accepted notes)
    - src/db/transaction.ts (the non-reentrant mutex — NEVER call a wrapped single-writer inside the loop)
    - src/db/database.ts:46 (`localDateTime()` — the `now` source; never `toISOString()`, Pitfall 6)
    - src/db/favourites-dao.test.ts (the guard-test shape to clone)
  </read_first>
  <behavior>
    - Happy path (self-sun / no occupant): rewriteRingSeq(exec, [c1,c2,c3], now, null) sets ring_seq 0,1,2 on those live contacted contacts and bumps modified_at; a re-read via orrery-read reflects the new order. `excludeContactId = null` binds NO `AND id <> ?` term — the guard scope is the full orbiting set, exactly as the favourites clone.
    - Guard 1 (unique): a list with a duplicate id throws and writes nothing.
    - Guard 2 (count-match): a list shorter/longer than the EFFECTIVE orbiting-set count (orbiting WHERE minus excludeContactId) throws and writes nothing.
    - Guard 3 (scoped): a stale id (archived, or never-contacted, or non-existent) fails its scoped UPDATE (changes!==1) and rolls back the whole batch.
    - Contact-sun path: with N live contacted contacts and ONE of them passed as `excludeContactId` (the sun occupant), reordering the remaining N−1 SUCCEEDS — Guard 2's COUNT and every Guard 3 UPDATE append `AND id <> ?`, so the guard's effective set is N−1, matching the sun-excluded list orrery-read renders. Writes dense ring_seq 0..N−2.
    - Contract-mismatch guard (the seam the current tests miss): passing the FULL N-length list while `excludeContactId` is set still FAILS Guard 2 (N ≠ N−1) and writes nothing — the sun-excluded caller must pass the N−1 list.
    - The last_contact recency column is never WRITTEN by this DAO (it appears only in the read-scope guard WHERE, never in a SET clause).
  </behavior>
  <action>
    Create `ring-seq-dao.ts` by cloning `rewriteFavouriteRanks` as `rewriteRingSeq(exec, orderedIds, now, excludeContactId: number | null)`, swapping the column `favourite_rank → ring_seq` and the scope predicate. Guard 2's COUNT and Guard 3's per-row UPDATE WHERE both start from `last_contact IS NOT NULL AND archived_at IS NULL` (the orbiting set, NOT the favourites set) and, when `excludeContactId` is non-null, ALSO append `AND id <> ?` (`?`-bound; the term is omitted entirely and no id is bound when `excludeContactId` is null) — so the guard's effective set mirrors `orrery-read`'s RENDERED orbiting set (the orbiting WHERE PLUS the `id <> excludeContactId` occupant exclusion), and the sun-excluded (N−1) drag list agrees with the count. When `excludeContactId` is null the scope is the full orbiting set (self-sun path, unchanged from the favourites clone). Keep it as N RAW `?`-bound UPDATEs inside ONE `inWriteTransaction` with a `changes===1` assertion per row; NEVER call a wrapped single-writer inside (the mutex is non-reentrant — permanent hang). Accept an empty list as a no-op (0===0), documented in-file. `now` is `localDateTime()`. Add the node:sqlite guard test cloning `favourites-dao.test.ts` shape, INCLUDING the sun-occupant seam: (a) one live contact is the sun (`excludeContactId` set) → reordering the remaining N−1 succeeds and writes dense ring_seq 0..N−2; (b) passing the wrong full-N list while `excludeContactId` is set still FAILS Guard 2 (count mismatch); (c) the self-sun `excludeContactId = null` path still reorders the full set.
  </action>
  <verify>
    <automated>npx vitest run src/db/ring-seq-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test proves the happy-path write (ring_seq 0..n-1), all three guards (unique / count-match / scoped stale-id rollback), and empty-list no-op (green).
    - Contact-sun coverage: with `excludeContactId` set to one live contact, reordering the remaining N−1 succeeds (dense ring_seq 0..N−2); passing the full-N list with `excludeContactId` set still fails Guard 2; the self-sun `excludeContactId = null` path reorders the full set unchanged (green).
    - `rewriteRingSeq` issues N raw UPDATEs inside one `inWriteTransaction` and never calls a wrapped single-writer (no nested `inWriteTransaction`); the `AND id <> ?` term is bound only when `excludeContactId` is non-null.
    - The DAO never WRITES `last_contact` — `grep -nE 'last_contact[[:space:]]*=' src/db/ring-seq-dao.ts` shows zero matches (it appears only in the read-scope guard WHERE, never in a SET clause); `modified_at` uses the passed `now`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`rewriteRingSeq` is the first ring_seq writer, cloning the favourites reorder's three guards (guard scope extended by the `excludeContactId` occupant exclusion), node-tested green including the contact-sun seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sun-picker-read — favourites-first candidate list</name>
  <files>src/db/sun-picker-read.ts, src/db/sun-picker-read.test.ts</files>
  <read_first>
    - src/db/dashboard-read.ts:284-295 (`listFavourites` — the favourites read + `FavouriteRow` shape to mirror)
    - src/db/orrery-read.ts (this plan Task 1 — reuse the same relative-photo/field conventions)
  </read_first>
  <behavior>
    - Returns non-archived contacts only (a favourite that is archived is absent; never-contacted contacts ARE included — anyone can be the sun).
    - Favourites (favourite_rank IS NOT NULL) come first, ordered by favourite_rank ASC; non-favourites follow, ordered by name COLLATE NOCASE.
    - Each row carries id, name, photo — enough to render a picker row with an avatar.
    - No synthetic "Me" row here (the Settings UI prepends the self option); the read is pure contacts.
  </behavior>
  <action>
    Create `sun-picker-read.ts` exporting `listSunCandidates(exec): Promise<SunCandidate[]>` — a single `SELECT id, name, photo FROM contacts WHERE archived_at IS NULL ORDER BY (favourite_rank IS NULL), favourite_rank ASC, name COLLATE NOCASE, id`. The `(favourite_rank IS NULL)` sort key (0 for favourites, 1 for the rest) puts favourites first by rank, then everyone else alphabetically. Pure async read, no transaction, no network. The "Me / self" option (NULL id) is added by the Settings picker UI, not here. Add the node:sqlite test covering: archived excluded, favourites-before-non-favourites ordering, never-contacted included.
  </action>
  <verify>
    <automated>npx vitest run src/db/sun-picker-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test proves archived exclusion, favourites-first-by-rank then name ordering, and never-contacted inclusion (green).
    - No "Me" row is synthesized in the DAO; it returns only real contacts.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`listSunCandidates` returns the favourites-first picker list for Settings; its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orrery drag → ring-seq-dao | A drag-computed target order crosses into a batched SQL write |
| Screen/Settings → read DAOs | Read-only projections; no user input reaches the SQL except a `?`-bound id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03-01 | Tampering | `rewriteRingSeq` batch write | low | mitigate | 3 guards (unique / count-match / scoped `changes===1`) roll the whole batch back on any partial/duplicate/stale list; ranks can never land on an archived/non-orbiting row. |
| T-13-03-02 | Tampering | SQL injection via reads | low | mitigate | Only closed code constants (status fragments, sort keys) are interpolated; the sole runtime value (`excludeContactId`) is `?`-bound. |
| T-13-03-03 | Info disclosure | read path | low | accept | Fully offline local reads; no network on any read path (local-first commitment). |

No high-severity threats. The `last_contact` single-writer invariant is preserved (this DAO never writes it).
</threat_model>

<verification>
- `npx vitest run src/db/orrery-read.test.ts src/db/ring-seq-dao.test.ts src/db/sun-picker-read.test.ts` — all green.
- `npx tsc --noEmit` clean; `grep -nE 'last_contact[[:space:]]*=' src/db/ring-seq-dao.ts` returns nothing — `last_contact` is never assigned, only read in the guard WHERE (single-writer intact).
</verification>

<success_criteria>
The orbiting scan (single-sourced status, occupant-excluded), the guarded first `ring_seq` writer, and the favourites-first sun-picker list all exist and are node-tested green.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-03-SUMMARY.md` when done.
</output>
