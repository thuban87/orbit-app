---
phase: 13-orrery
plan: 07
type: execute
wave: 3
depends_on: [13-05, 13-02, 13-03]
files_modified:
  - src/screens/OrreryScreen.tsx
  - src/components/orrery/OrbitBody.tsx
  - src/components/orrery/OrreryCanvas.tsx
  - src/components/orrery/SunBody.tsx
autonomous: true
requirements: [ORR-02, ORR-03, ORR-06]
must_haves:
  truths:
    - "The segmented control morphs the canvas between Status and Relationship on ONE canvas via a single `morph` shared value (~500ms ease-in-out): angle fades status-progress↔even-spread and colour fades full↔muted, while radius stays FIXED (the shared axis)."
    - "A subtle starfield twinkle + slow sun pulse animate via Skia's clock; bodies do NOT animate; the whole ambient loop PAUSES by unmounting <Canvas> when the screen is blurred or the app is backgrounded."
    - "Dragging a planet radially live-previews the target ring and, on release, snaps to the nearest rank and commits ring_seq through rewriteRingSeq in one transaction; the angular component is ignored."
  artifacts:
    - src/screens/OrreryScreen.tsx (morph + ambient + gestures)
    - src/components/orrery/OrbitBody.tsx (per-body morph useDerivedValue)
    - src/components/orrery/OrreryCanvas.tsx (useClock ambient loop, conditionally mounted)
    - src/components/orrery/SunBody.tsx (sun pulse)
  key_links:
    - "H1 (Rules of Hooks): the per-body morph `useDerivedValue`s live INSIDE `OrbitBody` (keyed by contact id), NOT in a `.map()` in OrreryScreen; the sun pulse lives in `SunBody`; `useClock` lives in `OrreryCanvas`."
    - "M5: `useClock` lives INSIDE the conditionally-mounted `OrreryCanvas` `<Canvas>` subtree, so unmounting it on blur actually stops the loop (an acceptance check asserts this boundary)."
    - "M5: pan hit-testing reads a WORKLET-SAFE shared-value snapshot of resting body positions + an `activeDragId` shared value (or an explicitly-stated JS-thread drag-start-selection policy) — never a plain JS array from a worklet."
    - "H2: the drag-release rank map uses the SAME `deriveOrreryMetrics` object (effectiveGap/ringInner/DRIFT_MAX) as render + hit-test — `(releaseRadius − C.ringInner) / C.effectiveGap`, never the raw fixed RING_GAP."
    - "Angle interpolation uses shortestAngleDelta (Pitfall 2 — never spin the long way across the wrap)."
    - "Pause = UNMOUNT <Canvas> (OrreryCanvas) on !(useIsFocused && AppState active) (Pitfall 4 — gating derived values does NOT stop the clock)."
    - "Drag-release → computeRingReorder → rewriteRingSeq(…, sunContactId) (runOnJS, ONE txn); passing the sun occupant as excludeContactId keeps the N−1 dragged list and the DAO guard aligned (else a contact-sun reorder rolls back on Guard 2); never nest inWriteTransaction (Pitfall 5, non-reentrant mutex)."
---

<objective>
Upgrade the static orrery into the full interactive surface: the two-view morph (ORR-02), the ambient starfield/sun-pulse layer with pause-on-blur (ORR-03), and the `ring_seq` radial drag with transactional commit (ORR-06 canvas half). Changes land in `OrreryScreen.tsx` and its keyed children (`OrbitBody` gains the per-body morph worklets, `OrreryCanvas` gains the `useClock` ambient loop, `SunBody` gains the pulse — the H1/M5 decomposition from 13-05), consuming the already-tested geometry, reorder, and DAO modules.

Purpose: Complete the orrery's motion and direct-manipulation, driven entirely off the UI thread (Reanimated worklets), pausing cleanly when unfocused/backgrounded — with every per-body/clock hook inside a keyed child (Rules of Hooks) and every layout number from the one shared `deriveOrreryMetrics` object.
Output: morph + ambient + gesture upgrades across `OrreryScreen.tsx`, `OrbitBody.tsx`, `OrreryCanvas.tsx`, `SunBody.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orrery/13-UI-SPEC.md
@.planning/phases/13-orrery/13-PATTERNS.md
@src/screens/CropPhotoScreen.tsx
</context>

## Artifacts this phase produces (source-grounding allowlist)

New files: `orrery-geometry-logic.ts`, `orrery-ring-logic.ts`, `ring-reorder-logic.ts`, `sun-occupant-logic.ts`, `orrery-read.ts`, `ring-seq-dao.ts`, `sun-picker-read.ts`, `OrreryScreen.tsx`, `SegmentedControl.tsx`, `src/components/orrery/OrbitBody.tsx`, `src/components/orrery/OrreryCanvas.tsx`, `src/components/orrery/SunBody.tsx`, `migrations/003-orrery-settings.ts`, `assets/Inter-SemiBold.ttf`. New functions: `progressToAngle`, `evenSpreadAngle`, `ringRadius`, `drawnRadius`, `polarToXY`, `hitTest`, `shortestAngleDelta`, `deriveOrreryMetrics`, `orreryRingStyle`, `computeRingReorder`, `resolveSunOccupant`, `listOrbitingContacts`, `rewriteRingSeq`, `listSunCandidates`, `migration003`. New theme tokens: `starPalette`, `mutedStable`, `mutedWobble`, `mutedDecay`, `rogueExtinguished`. New `app_settings` columns: `sun_contact_id`, `self_sun_colour`. New nav route: `Orrery`.

## Owner decision to record (never mis-flag as an ORR-06 gap)

The ONLY ORR-06 gesture on the canvas is the `ring_seq` radial drag (below). Sun-occupant assignment was owner-relocated to Settings (13-06) and long-press was owner-rejected. Do not add an orrery long-press or an orrery sun-assignment gesture.

<tasks>

<task type="auto">
  <name>Task 1: Two-view morph (Status ↔ Relationship on one canvas)</name>
  <files>src/screens/OrreryScreen.tsx, src/components/orrery/OrbitBody.tsx</files>
  <read_first>
    - src/screens/OrreryScreen.tsx + src/components/orrery/OrbitBody.tsx (this phase, 13-05 — the static render, the SegmentedControl mount, the keyed OrbitBody child, and the JS-computed status-view layout to extend)
    - THIS PLAN's key_links (H1) + 13-05's "Render architecture" section — the per-body morph hooks go INSIDE OrbitBody, never in a `.map()` in OrreryScreen
    - src/logic/orrery-geometry-logic.ts (this phase — `evenSpreadAngle`, `shortestAngleDelta`, `MORPH_MS`, `progressToAngle`, `deriveOrreryMetrics`)
    - src/screens/CropPhotoScreen.tsx:126-210 (the `useSharedValue` + `useDerivedValue` worklet idioms; never per-frame setState)
    - .planning/phases/13-orrery/13-UI-SPEC.md "Two-view Morph" (angle status-progress↔even-spread; colour full↔muted; radius fixed; rogue → rogueExtinguished both views)
  </read_first>
  <action>
    Add a single Reanimated `morph = useSharedValue(0)` in OrreryScreen (0 = Status, default; 1 = Relationship). Wire the `SegmentedControl` onChange to set `morph.value = withTiming(target, { duration: MORPH_MS, easing: Easing.inOut(Easing.ease) })` (MORPH_MS from geometry-logic; the single tunable). Precompute BOTH layouts in JS per read from the SAME measured `C` (deriveOrreryMetrics): the status-view angle (`progressToAngle(progress)`) and the relationship-view even-spread angle (`evenSpreadAngle(index, count)`), radius identical for both (the shared axis). Pass `morph` plus each body's two precomputed angles + fixed radius + full/muted colour tokens as PROPS into the keyed `<OrbitBody key={contact.id} …/>`. INSIDE `OrbitBody` (H1 — one hook set per body, never in a map), in `useDerivedValue` WORKLETS: interpolate the ANGLE from statusAngle→restAngle using `shortestAngleDelta` so the morph takes the short way across the 2π wrap (Pitfall 2 — never a full loop); interpolate the COLOUR with `interpolateColor(morph.value, [0,1], [fullStatusToken, mutedToken])` (rogue interpolates toward `rogueExtinguished` in both views); keep the RADIUS FIXED (never interpolated). The morph is event-driven and self-terminating — it does NOT need pause-on-blur. Update the hit-test bodies snapshot to reflect whichever view `morph` has settled on (recompute the resting layout on toggle-end; a tap mid-morph hit-tests the resting target — acceptable). All colours via tokens; no per-frame setState.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Toggling the SegmentedControl drives one `morph` shared value with `withTiming` at MORPH_MS ease-in-out; angle + colour interpolate, radius is never interpolated.
    - H1: the per-body morph `useDerivedValue`s live inside `OrbitBody` — grep shows no `useDerivedValue` call inside a `.map()` in OrreryScreen; OrreryScreen's hook count is contact-count-independent.
    - Angle interpolation uses `shortestAngleDelta` (no long-way spin across the wrap).
    - Positions/colours come from `useDerivedValue` worklets — grep shows no per-frame `setState`; `npm run check:colors`, `npx tsc --noEmit`, `npm test` green.
  </acceptance_criteria>
  <done>Both views share one canvas and morph on toggle (angle+colour, radius fixed) off the JS thread via per-body OrbitBody worklets; feel is device-UAT in 13-08.</done>
</task>

<task type="auto">
  <name>Task 2: Ambient layer (starfield twinkle + sun pulse) + pause-on-blur</name>
  <files>src/screens/OrreryScreen.tsx, src/components/orrery/OrreryCanvas.tsx, src/components/orrery/SunBody.tsx</files>
  <read_first>
    - src/screens/OrreryScreen.tsx + src/components/orrery/OrreryCanvas.tsx + src/components/orrery/SunBody.tsx (this phase — the Canvas subtree + sun render to animate)
    - THIS PLAN's key_links (M5) — `useClock` MUST live inside `OrreryCanvas` (the conditionally-mounted subtree), not in OrreryScreen
    - .planning/phases/13-orrery/13-UI-SPEC.md "Ambient layer (animated) + pause-on-blur" (subtle twinkle + ~2-4s sun pulse; bodies do NOT animate; pause = unmount Canvas)
    - .planning/phases/13-orrery/13-RESEARCH.md §"Ambient Layer + Pause" (useClock is the only loop; Pitfall 4 — gating derived values does not stop the frame callback)
    - src/screens/HomeScreen.tsx (the shipped `useFocusEffect` + AppState 'change' listener → boolean freshness pattern to mirror for the mount gate)
  </read_first>
  <action>
    Add the ONLY animated loop: a subtle starfield twinkle (low-opacity star dots in `textSecondary`/`textPrimary`/`starPalette` tones) inside `OrreryCanvas` and a slow sun pulse (glow radius/opacity oscillate over ~2-4s) inside `SunBody`, both driven off Skia's `useClock()` inside `useDerivedValue` worklets. M5 (addresses review concern): `useClock()` MUST be called INSIDE `OrreryCanvas` (the conditionally-mounted `<Canvas>` subtree), NEVER in OrreryScreen — otherwise unmounting the canvas would not stop the clock. OrreryScreen computes the mount boolean and renders `{visible ? <OrreryCanvas …/> : null}`. Bodies do NOT animate (they stay at their placed-on-focus positions). Implement pause-on-blur by conditionally UNMOUNTING `OrreryCanvas` when `!(useIsFocused() && appStateActive)` — wire `useIsFocused()` (@react-navigation/native) + an `AppState` 'change' listener (mirror HomeScreen) into a boolean in OrreryScreen, and render `OrreryCanvas` only when true (Pitfall 4 — do NOT merely gate the derived values; the clock keeps ticking otherwise). The RN chrome (SegmentedControl, empty-state overlay, header) stays mounted; only `OrreryCanvas` unmounts. All ambient colours via tokens; no per-frame setState. NOTE: `useClock` is a NEW-to-repo Skia idiom — it is NOT part of the proven `CropPhotoScreen` surface (which uses only `useImage`/`useSharedValue`/`useDerivedValue`), so treat it as new-to-repo rather than a `CropPhotoScreen` clone; its on-device behavior (and that unmounting `OrreryCanvas` truly stops the clock) is first proven in 13-08 UAT — a failure there is an expected verification point, not a surprise.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The ambient starfield + sun pulse animate via `useClock`/`useDerivedValue`; bodies remain static.
    - M5: `useClock` is called inside `OrreryCanvas` (the conditionally-mounted subtree) — grep confirms `useClock` appears in `OrreryCanvas.tsx` and NOT in `OrreryScreen.tsx`, so unmounting the canvas stops the loop.
    - `OrreryCanvas` is conditionally unmounted when `!(useIsFocused && AppState active)` — grep confirms OrreryScreen gates the `<OrreryCanvas>` element, not just derived values.
    - `npm run check:colors`, `npx tsc --noEmit`, `npm test` green.
  </acceptance_criteria>
  <done>A subtle ambient layer animates and PAUSES by unmounting OrreryCanvas (which owns useClock) on blur/background; verified on device in 13-08.</done>
</task>

<task type="auto">
  <name>Task 3: Radial drag → ring_seq commit (ORR-06 canvas half)</name>
  <files>src/screens/OrreryScreen.tsx</files>
  <read_first>
    - src/screens/OrreryScreen.tsx (this phase — the existing `Gesture.Tap` to compose with a `Gesture.Pan`; the measured `C` from deriveOrreryMetrics; the resting bodies array)
    - THIS PLAN's key_links (M5 + H2) — the worklet-safe body-position snapshot + activeDragId shared values, and the shared metrics object for release-rank mapping
    - src/logic/ring-reorder-logic.ts (this phase — `computeRingReorder(orderedIds, from, to)`)
    - src/db/ring-seq-dao.ts (this phase — `rewriteRingSeq(exec, orderedIds, now, excludeContactId)`; ONE txn, non-reentrant mutex; `excludeContactId` scopes the guard to the sun-excluded rendered set)
    - src/logic/orrery-geometry-logic.ts (this phase — `deriveOrreryMetrics` → `C.ringInner`/`C.effectiveGap` for release-radius → rank; `C.HIT_RADIUS`/`hitTest` for the touch-down body pick)
    - src/screens/CropPhotoScreen.tsx:245,333 (`Gesture.Simultaneous`/`GestureDetector` composition proven with gesture-handler 2.32; the shared-value snapshot idiom — geometry mirrored into shared values so the UI-thread worklet reads it without touching JS state)
  </read_first>
  <action>
    M5 (addresses review concern — worklet-safe pan hit-testing): mirror the resting body positions into a WORKLET-SAFE Reanimated shared value `bodiesShared = useSharedValue<{id,x,y}[]>` (updated whenever the JS-computed resting layout changes, exactly as CropPhotoScreen mirrors geometry into shared values), and add an `activeDragId = useSharedValue<number | null>(null)`. Add a `Gesture.Pan().minDistance(10)` that, onBegin, hit-tests the body under the touch-down point ON THE UI THREAD against `bodiesShared.value` (a worklet-safe `hitTest`, using `C.HIT_RADIUS`) and sets `activeDragId.value` — NOT a plain JS array read from the worklet. (If any hit-test dependency cannot be made worklet-safe, fall back to an explicit JS-thread drag-start selection via `runOnJS` and state that the ~one-frame selection latency is accepted — but the shared-value snapshot is the preferred path.) onUpdate, live-previews the target ring as an `accent` ghost-ring highlight (a shared value → a `useDerivedValue` ring radius; the reserved-accent drag-preview from the UI-SPEC); onEnd, maps the release radius to `targetRank = clamp(round((releaseRadius − C.ringInner) / C.effectiveGap), 0, N−1)` — H2: using the SAME `deriveOrreryMetrics` object as render + hit-test, never the raw fixed `RING_GAP` — then `runOnJS(commitRingSeq)(movedId, targetRank)` and clears `activeDragId.value`. `commitRingSeq` (a JS fn, off the worklet) computes `computeRingReorder(currentOrderedIds, fromRank, targetRank)` — where `currentOrderedIds` is the rendered orbiting list (the sun-excluded N−1 set from `listOrbitingContacts({ excludeContactId: sunContactId })`, so any contact occupying the sun is already absent) — and awaits `rewriteRingSeq(getExecutor(), newIds, localDateTime(), sunContactId)` in its ONE transaction. Passing the current `sunContactId` (the same value the screen read from `getAppSettings`, held in state) as `excludeContactId` is REQUIRED: it makes `rewriteRingSeq`'s Guard 2 count and Guard 3 scope the same N−1 set as the dragged list — otherwise a contact-sun (ORR-05) + drag always rolls back on Guard 2 (N−1 ≠ N). When no contact occupies the sun (self-sun, `sunContactId` is null), `excludeContactId` is null and the guard scopes the full set unchanged. Never nest `inWriteTransaction`, never call a wrapped single-writer inside (Pitfall 5, non-reentrant mutex); a persist failure alerts + re-reads (reload). The angular component is IGNORED during the drag (radius only). Compose the gestures as `Gesture.Race(tap, pan)`: a stationary touch → tap → Profile; movement past `minDistance` → pan → radial drag. After a successful commit, re-read `listOrbitingContacts({ excludeContactId: sunContactId })` so the new dense rank/radius reflows. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - M5: pan onBegin hit-tests against a worklet-safe `bodiesShared` shared value and sets an `activeDragId` shared value (or, if any dependency is not worklet-safe, an explicit `runOnJS` drag-start policy with the one-frame latency noted) — grep shows the pan worklet does not read a plain JS bodies array.
    - `Gesture.Race(tap, pan)` composes: stationary → Profile (existing tap), movement → radial drag with an accent ghost-ring preview.
    - H2: release maps radius → clamped rank using `C.ringInner`/`C.effectiveGap` from `deriveOrreryMetrics` (the SAME object as render/hit-test), then `computeRingReorder` → `rewriteRingSeq(exec, newIds, now, sunContactId)` in ONE transaction via `runOnJS`; no nested `inWriteTransaction`; the angular component is ignored.
    - `commitRingSeq` passes the current `sunContactId` as `excludeContactId`, so a reorder SUCCEEDS while a contact occupies the sun (the N−1 dragged list matches the guard's N−1 effective set) — and the self-sun (null) path still succeeds on the full set.
    - After commit the orbiting set is re-read with `{ excludeContactId: sunContactId }` (reflow); `npm run check:colors`, `npx tsc --noEmit`, `npm test` green.
  </acceptance_criteria>
  <done>A radial drag reorders a contact's ring and commits ring_seq transactionally on release; verified on device in 13-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drag gesture → ring-seq-dao | A release-radius-derived target order crosses (via runOnJS) into a batched SQL write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07-01 | Tampering | drag → `rewriteRingSeq` | low | mitigate | `computeRingReorder` clamps the rank; `rewriteRingSeq`'s 3 guards roll back any partial/duplicate/stale batch (13-03); the commit is one transaction. |
| T-13-07-02 | DoS | animation loop battery drain while backgrounded | low | mitigate | Pause = UNMOUNT `<Canvas>` on blur/background (not a derived-value gate) so the clock stops (Pitfall 4). |
| T-13-07-03 | Tampering | reorder re-entrancy | low | mitigate | `commitRingSeq` runs `rewriteRingSeq` off the worklet (runOnJS) in ONE `inWriteTransaction`; never nests the non-reentrant mutex (Pitfall 5). |

No high-severity threats. No network, no secrets; the only write is `ring_seq` (single-writer `last_contact` untouched).
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npm test` — green.
- Morph feel, ambient pause-on-blur, and drag→ring_seq are DEVICE-UAT on the Pixel (13-08) — perf claims are Pixel-only (Skia render loop; the emulator cannot assess it).
</verification>

<success_criteria>
The orrery morphs between views on one canvas, animates a subtle ambient layer that pauses by unmounting on blur, and lets a user reorder `ring_seq` by radial drag with a transactional commit — completing ORR-02, ORR-03 motion, and ORR-06's canvas half.
</success_criteria>

<output>
Create `.planning/phases/13-orrery/13-07-SUMMARY.md` when done.
</output>
