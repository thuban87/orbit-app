---
phase: 26-dashboard-control-surface
plan: 07
type: execute
wave: 3
depends_on: ["26-01", "26-02", "26-03", "26-04"]
files_modified:
  - src/screens/HomeScreen.tsx
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
  - src/components/SegmentedControl.tsx
  - src/stores/dashboard-query-store.ts
  - src/stores/dashboard-query-store.test.ts
autonomous: true
requirements: [DASHC-01, DASHC-07]
estimate:
  tokens: 96000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    # --- DASHC-01 hierarchy complete ---
    - "The Dashboard reads top-to-bottom: root header (title + destinations + ⋯) → Population/Filters/Sort control row → Search + List/Card row → contact collection; the control row's vertical position is invariant to header/search state (DASHC-01)."
    # --- DASHC-07 search + toggle ---
    - "Search sits on Row 3 below the control row, is collapsible/expandable (dashboard-search-toggle), and shares its row with a right-aligned accessible List/Card toggle (dashboard-view-toggle, segments dashboard-view-toggle-list/-card); search takes most of the row width and the toggle keeps a real 44px target (DASHC-07 / §K-L)."
    - "Search text is EPHEMERAL session state in useDashboardSessionStore (searchText/setSearchText), restored on Dashboard→Profile→Back and cleared on fresh launch; dashboard-search-clear clears the term; there is no separate permanent results-heading row (DASHC-07 / Phase 25)."
    - "SegmentedControl is extended so SegmentedControlOption carries an OPTIONAL `icon?: IconName` field (the `IconName` type imported from icon-registry.ts, NOT a bare string) + filled/outline treatment, and renders the semantic Icon when present; existing label-only consumers (Orrery view toggle) keep working unchanged (addresses cross-AI review MEDIUM — the current option type is {label,value} and renders only <Text>, SegmentedControl.tsx:22,75). Typing the field `IconName` is MANDATORY (addresses cross-AI review CYCLE-3 MEDIUM): a bare-string icon would let an unregistered icon name slip through at the SegmentedControl CALL SITE with no tsc gate; `icon?: IconName` makes `tsc --noEmit` reject an unregistered icon where the List/Card options are declared, not only inside Icon.tsx."
    - "The List/Card toggle sets viewMode via setViewMode(getExecutor(), mode) GUARDED by an equality check (addresses cross-AI review CYCLE-4 #3): the toggle's onChange short-circuits `if (mode === useDashboardQueryStore.getState().viewMode) return;` BEFORE calling setViewMode, AND `setViewMode` in the store is hardened to no-op on an unchanged value (`if (viewMode === get().viewMode) return;` at its top, before the persist + set) — so re-selecting the already-active segment neither persists to SQLite nor calls set() nor triggers a reload (true idempotency, DASHC-07). The active segment shows the FILLED icon variant (via the extended SegmentedControl) + accessibilityState.selected (non-colour redundancy). Phase 26 ships the toggle + persisted viewMode only — the row/grid rendering it switches is Phase 27/28 (§L)."
    - "Expand/collapse of the search uses restrained motion (MOTION.base, EASING.decelerate) and respects reduced motion (collapse to instant) — Reanimated, never setState-per-frame; the collapse/expand animation PAUSES/settles to its final value when useIsFocused()===false OR AppState is not 'active' (CLAUDE.md non-negotiable — all animation pauses on unfocus + background; mirror OrreryScreen.tsx:287-295), same invariant as the Plan 01 panel animation (DASHC-07; addresses cross-AI review CYCLE-2 MEDIUM)."
    # --- D-12 read wiring + legacy retirement (no dual-read) ---
    - "HomeScreen reads the no-term list via listDashboardPopulation(exec, query, now) and the has-term list via the new listDashboardSearch(exec, query, term, now) — where `now` is the single per-reload `localDateTime()` (see the single-now must_have) (D-12); a burst of keystrokes settles to ONE read via an explicit DEBOUNCE (~220ms, retaining the legacy debounce interval) over the session-backed search text, with the cancelled-flag guard as the stale-result backstop — the cancelled flag alone does NOT collapse a keystroke burst to one query, so the debounce is required (addresses cross-AI review MEDIUM)."
    - "Legacy listDashboard AND listNeverContacted are RETIRED from dashboard-read.ts (no runtime caller remains) and their obsolete test blocks are removed from dashboard-read.test.ts — no dual-read is left in place (D-12)."
    - "PRESERVED per D-03: countNeverContacted, readIncludeUnboundNeverContacted, and the include_unbound_never_contacted app_settings read stay (DigestScreen + HomeScreen counts still consume them); the app_settings columns / PORTABLE_SETTINGS_KEYS are NOT dropped — legacy-key removal is coordinated with the Phase 36 backup bump."
    - "The empty-state gate is fed the new shape: hasTerm from the session search, activePopulations + activeFilters from the query store, and populationCounts so the population-specific empties (birthdays/not-contacted/snoozed) resolve; a zero-result search shows search-empty before any filter/population copy (DASHC-01/07)."
    - "populationCounts is derived from CHEAP dedicated count reads, NOT N extra full listDashboardPopulation reads (addresses cross-AI review CYCLE-1 + CYCLE-2 — the derivation must be defined, efficient, AND correct): not-contacted ← the existing countNeverContacted; snoozed ← the existing countSnoozed; birthdays ← the NEW bound-only countBirthdayPopulation(exec, now) from Plan 03 (one cheap count, same cost class as countNeverContacted/countSnoozed) — it counts ONLY the bound-only non-archived 0–30-day window, so it matches the Birthdays population and never over-counts by including Unbound candidates; a gravity-filtered zero result is treated as the filter-empty case (rowCount===0 after the gravity post-pass), not a population-empty. No per-population full listDashboardPopulation call is issued just to size the empty state. (Supersedes the earlier 'reuse the single listBirthdayCandidates resolution — do not re-scan' wording, which was unimplementable across the module boundary AND would have over-counted, per cross-AI review CYCLE-2 HIGH.)"
    - "ONE `now` is captured per reload and threaded to every birthday-resolving read/count (addresses cross-AI review CYCLE-3 MEDIUM): reload() computes `const now = localDateTime()` ONCE and passes that SAME `now` to listDashboardPopulation / listDashboardSearch AND to countBirthdayPopulation — NOT independent localDateTime() calls per read. `listDashboardPopulation` resolves the birthday window from its passed date (dashboard-read.ts:274), so if the list read and the empty-state count each called localDateTime() independently they could straddle local midnight and disagree, producing a wrong Birthdays empty state for one refresh. A single per-reload `now` keeps the birthday list and its empty-state count coherent (aligns with the CLAUDE.md local-date rule)."
    # --- edge-probe lift: DASHC-01 ---
    - "The four rows stack in fixed order with no overlap and the control row position is invariant to header/search fit (adjacency)."
    - "With zero contacts the hierarchy still renders header + control row + search/toggle row + the first-run CTA in the collection slot (empty)."
    - "Row order is fixed header → control row → search+toggle → collection and never reorders (ordering)."
    # --- edge-probe lift: DASHC-07 ---
    - "Search + List/Card toggle share one row: toggle right-aligned, search takes the remaining width, no overlap, toggle keeps a 44px target (adjacency)."
    - "Empty/whitespace search term shows the full population list; dashboard-search-clear returns to the population list (empty)."
    - "The two toggle segments render in fixed order List then Card; active = filled icon + accessibilityState.selected (ordering)."
    - "Toggling to the already-active view / setting viewMode to its current value is a no-op that does not thrash the read (idempotency) — ENFORCED by the equality guard at the call site AND the hardened `setViewMode` store setter (no persist, no set(), no reload on an unchanged value), covered by a node test on the store setter (cross-AI review CYCLE-4 #3)."
    - "A burst of keystrokes settles to ONE search read; the cancelled-flag guard drops a stale async result so a newer query is never clobbered (concurrency)."
    # --- UI-SPEC ## UI Considerations lift (S5) ---
    - "S5 empty: hasTerm + zero rows → Phase 25 search-empty ('No matches for {term}'); precedence unchanged."
    - "S5 loading: local SQLite + TS scoring is synchronous-fast — no spinner / 'searching…' label."
    - "S5 zero-one-many: two fixed toggle segments; active = filled icon + selected (non-colour redundancy)."
    - statement: "S5 partial: collapsed vs expanded search both valid; expand/collapse respects reduced motion (collapse to instant)."
      verification: backstop
    - statement: "S5 overflow: search takes most of the row and the List/Card toggle keeps a 44px target at all supported widths/OS text scales."
      verification: backstop
  artifacts:
    - "src/screens/HomeScreen.tsx (search+toggle row, session-store search, D-12 read wiring, view-toggle equality guard)"
    - "src/db/dashboard-read.ts (listDashboard + listNeverContacted retired; countNeverContacted + policy read preserved)"
    - "src/stores/dashboard-query-store.ts :: setViewMode hardened to no-op on an unchanged value (List/Card toggle idempotency — cross-AI review CYCLE-4 #3) + a dashboard-query-store.test.ts no-op case"
  key_links:
    - "HomeScreen search → useDashboardSessionStore.searchText → hasTerm → listDashboardSearch(exec, query, term, localDateTime()); no-term → listDashboardPopulation"
    - "HomeScreen List/Card toggle (SegmentedControl) → setViewMode(getExecutor(), mode) → app_settings"
    - "HomeScreen empty-state → selectDashboardEmptyState({hasTerm, activePopulations, activeFilters, populationCounts, live/neverContacted/snoozed/archived/unbound, rowCount})"
  prohibitions:
    - "MUST NOT leave a dual-read: once listDashboardSearch is wired, listDashboard is removed, not kept in parallel (D-12)."
    - "MUST NOT drop countNeverContacted, readIncludeUnboundNeverContacted, the include_unbound_never_contacted read, or any app_settings/PORTABLE_SETTINGS_KEYS entry — key removal is Phase 36's, coordinated with the backup bump (D-03). If a migration seems required, STOP AND ASK (verify head+1 on disk, never assume)."
    - "MUST NOT drive the search collapse from setState per frame or use Skia (Reanimated only); MUST honour reduced motion."
    - "MUST NOT pass toISOString() as the read now arg — use localDateTime() (UTC evening off-by-one; the read throws unless now starts YYYY-MM-DD)."
    - "MUST NOT persist the search term (it is ephemeral session state) or add a permanent results-heading row (§K)."
---

<objective>
Complete the Dashboard hierarchy (DASHC-01) with Row 3 — the collapsible search + right-aligned List/Card toggle (DASHC-07) — restore search on the new query world by wiring the D-12 `listDashboardSearch` read, and retire the legacy `listDashboard`/`listNeverContacted` reads so no dual-read remains. Search text becomes ephemeral session state; the List/Card preference persists via `setViewMode`.

Purpose: Close the last hierarchy row + the search seam (D-12) and remove the legacy reads the tracer left behind, without dropping the D-03-protected `countNeverContacted`/`include_unbound_never_contacted` machinery.
Output: the final HomeScreen search+toggle row, D-12 read wiring, and the retired legacy reads + their obsolete test blocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md
@.planning/phases/26-dashboard-control-surface/26-01-SUMMARY.md
@.planning/phases/26-dashboard-control-surface/26-03-SUMMARY.md
@.planning/phases/26-dashboard-control-surface/26-04-SUMMARY.md

@src/stores/dashboard-session-store.ts
@src/components/SegmentedControl.tsx
@src/logic/dashboard-empty-logic.ts
@src/theme/tokens/motion.ts
@src/theme/use-reduced-motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search + List/Card toggle row (session-store search, collapsible, reduced-motion, viewMode toggle)</name>
  <read_first>
    - src/screens/HomeScreen.tsx (post Plan 01+04 state — the search box was removed by the tracer; re-add on Row 3. Note the empty-state gate wiring + counts, and the LEGACY 220ms debounce idiom the tracer removed — HomeScreen.tsx:168 pre-tracer — to reinstate over the session searchText)
    - src/stores/dashboard-session-store.ts (searchText/setSearchText/clearSession — the ephemeral store)
    - src/stores/dashboard-query-store.ts:65-72 (setViewMode) + viewMode in state — NOTE: Plan 01 already added the generation bump to every setter; this plan adds an equality no-op guard at the TOP of setViewMode (`if (viewMode === get().viewMode) return;`) before the persist+set (cross-AI review CYCLE-4 #3), and adds a case to src/stores/dashboard-query-store.test.ts asserting a same-value setViewMode performs no write and no generation bump
    - src/stores/dashboard-query-store.test.ts (Plan 01's stale-hydrate case lives here — add the setViewMode no-op test alongside it)
    - src/components/SegmentedControl.tsx (WHOLE FILE — SegmentedControlOption is currently {label,value} at :22-26 and renders ONLY a <Text> at :75-83; it must gain OPTIONAL icon data + render an Icon, WITHOUT breaking the Orrery label-only consumer)
    - src/components/icons/Icon.tsx + icon-registry.ts (list/grid icons added in Plan 01; search icon exists — the semantic Icon the extended SegmentedControl renders; `IconName` is exported from icon-registry.ts:69 — this is the type the new `SegmentedControlOption.icon` field MUST use so tsc gates unregistered icons at the call site)
    - src/screens/OrreryScreen.tsx (the existing label-only SegmentedControl consumer that MUST keep compiling + rendering unchanged)
    - src/theme/tokens/motion.ts (MOTION.base/EASING.decelerate) + src/theme/use-reduced-motion.ts:122 (useReducedMotion)
    - src/screens/OrreryScreen.tsx:37,50,287-295 (the useIsFocused + AppState 'change' derivation to MIRROR so the search collapse/expand animation pauses off-focus/background — CLAUDE.md non-negotiable; same invariant Plan 01 applied to the panel)
  </read_first>
  <files>src/components/SegmentedControl.tsx, src/screens/HomeScreen.tsx, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts</files>
  <action>
    Extend `SegmentedControl.tsx` (addresses cross-AI review MEDIUM + CYCLE-3 MEDIUM): add an OPTIONAL `icon?: IconName` to `SegmentedControlOption<V>` — the field type MUST be `IconName` imported from `@/components/icons/icon-registry` (a react-native-free type-only import), NOT a bare `string`, so `tsc --noEmit` catches an unregistered icon name at the SegmentedControl CALL SITE (where the List/Card options are declared), not merely inside Icon.tsx. Render the semantic `Icon` (filled when the segment is active, outline when inactive) alongside/instead of the label when `icon` is present. Keep the existing `{label,value}` behavior for options WITHOUT an icon so the Orrery view toggle (label-only consumer) renders and compiles unchanged — the label stays the accessibilityLabel in both cases; every colour still resolves through useTheme (no literal). Do NOT change the container/segment testID scheme (`${testID}-${value}`).
    Add Row 3 below the DashboardControlRow: a collapsible search input (testID dashboard-search-input, placeholder 'Search people and notes', clear affordance dashboard-search-clear labelled 'Clear') whose value is useDashboardSessionStore.searchText via setSearchText (NOT local useState — ephemeral session state restored on Dashboard→Profile→Back), plus a toggle (testID dashboard-search-toggle, a11y 'Search'/'Close search') that collapses/expands the input with Reanimated withTiming(MOTION.base, EASING.decelerate→Easing.out(Easing.ease)), collapsing to INSTANT when useReducedMotion() is true AND pausing/settling to its final value when useIsFocused()===false OR AppState is not 'active' (mirror OrreryScreen.tsx:287-295 — CLAUDE.md non-negotiable, same invariant as the Plan 01 panel animation; addresses cross-AI review CYCLE-2 MEDIUM). Keep the INPUT VALUE immediately responsive (typing is not laggy) but DEBOUNCE (~220ms, the legacy interval) the searchText value that drives the read (Task 2) so a keystroke burst fires ONE read — the cancelled-flag guard is a stale-result backstop, not the burst-collapser. On the SAME row, right-aligned, place the extended `SegmentedControl` (testID dashboard-view-toggle) with List/Card segments (dashboard-view-toggle-list/-card) using the list/grid semantic icons — active segment = filled icon + accessibilityState.selected. **View-toggle idempotency (addresses cross-AI review CYCLE-4 #3):** the toggle's onChange short-circuits `if (mode === useDashboardQueryStore.getState().viewMode) return;` BEFORE calling `setViewMode(getExecutor(), mode)`, so re-selecting the already-active segment neither persists nor reloads. ALSO harden the store setter: add `if (viewMode === get().viewMode) return;` at the TOP of `setViewMode` in `src/stores/dashboard-query-store.ts` (before the persist+set — Plan 01 already added the generation bump; the guard sits above it so an unchanged value skips persist, set(), and the generation bump), and add a case to `src/stores/dashboard-query-store.test.ts` asserting a same-value `setViewMode` writes nothing (no updateAppSettings call) and does not change the store. Search gets most of the row width; the toggle keeps a 44px target. No permanent results-heading row. Every colour via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/stores/dashboard-query-store.test.ts</automated>
    <human-check>On the Pixel: Row 3 shows a collapsible search + a right-aligned List/Card toggle; toggling view flips the filled icon + persists across relaunch; re-tapping the already-active segment does nothing (no visible flicker/reload); with reduce-motion ON the collapse is instant; search restores after Dashboard→Profile→Back and clears on a fresh app launch.</human-check>
  </verify>
  <acceptance_criteria>
    - `SegmentedControlOption` gains an optional `icon?: IconName` field (typed `IconName`, NOT a bare string — grep: `icon?: IconName` + the `IconName` import from icon-registry in SegmentedControl.tsx) and the component renders the semantic Icon (filled active / outline inactive) when present. Because the field is `IconName`, an unregistered icon name at a SegmentedControl call site would fail `tsc --noEmit` (cross-AI review CYCLE-3 MEDIUM — the compile-time gate fires at the call site, not only inside Icon.tsx). OrreryScreen (label-only consumer) still compiles unchanged under `tsc --noEmit` (backward compatibility proven by the typecheck, since it passes no icon).
    - testIDs dashboard-search-input / dashboard-search-clear / dashboard-search-toggle / dashboard-view-toggle / dashboard-view-toggle-list / dashboard-view-toggle-card present.
    - `grep -n "useDashboardSessionStore\|setViewMode" src/screens/HomeScreen.tsx` present; search term is session-store-backed (no local useState for the term); a ~220ms debounce over the searchText drives the read (grep: a debounce/timeout around the search value, not just the cancelled flag).
    - The search collapse/expand animation pauses off-focus/background: `grep -nE "useIsFocused|AppState" src/screens/HomeScreen.tsx` shows the focus + AppState derivation gating the Reanimated timing (CLAUDE.md).
    - View-toggle idempotency is enforced (cross-AI review CYCLE-4 #3): the HomeScreen toggle onChange guards `if (mode === useDashboardQueryStore.getState().viewMode) return;` before setViewMode (grep HomeScreen for the equality guard around the view-toggle onChange); `setViewMode` in dashboard-query-store.ts no-ops on an unchanged value (grep: an early return comparing `viewMode` to `get().viewMode` at the top of setViewMode, before updateAppSettings); and dashboard-query-store.test.ts asserts a same-value setViewMode issues NO updateAppSettings write and does not change the store (the store test passes under vitest).
    - `tsc --noEmit` + `check:colors` green.
    - On-device: collapsible search (reduced-motion aware; no half-run animation after backgrounding) + a List/Card toggle whose active segment shows the FILLED icon + persisted viewMode + session-restored search; a fast keystroke burst issues one read.
  </acceptance_criteria>
  <done>Row 3 ships a collapsible session-backed search sharing its row with a right-aligned accessible List/Card toggle that persists viewMode.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the D-12 listDashboardSearch read + feed the empty-state gate the new shape</name>
  <read_first>
    - src/screens/HomeScreen.tsx (the reload() callback + the selectDashboardEmptyState call — from Plan 01; note the ~220ms debounce idiom reinstated in Task 1 over the session searchText — reload MUST key off the DEBOUNCED value, not getState().searchText)
    - src/db/dashboard-read.ts (listDashboardSearch signature + the NEW bound-only countBirthdayPopulation(exec, now) from Plan 03; listDashboardPopulation; countNeverContacted/countSnoozed — the cheap counts)
    - src/components/control-surface/control-labels.ts (POPULATION_LABELS — the single source of truth for population display names used in empty-state copy; Plan 01)
    - src/logic/dashboard-empty-logic.ts:60-84,128-179 (DashboardEmptyInput — activeFilter + optional activePopulations/activeFilters/populationCounts + hasTerm; the precedence)
    - src/db/database.ts (localDateTime — the now arg)
  </read_first>
  <files>src/screens/HomeScreen.tsx</files>
  <action>
    **Use the DEBOUNCED search value everywhere (addresses cross-AI review CYCLE-2 MEDIUM):** reload() MUST key off the REACTIVE debounced search value — a `debouncedSearchText` derived from the session store's searchText via the ~220ms debounce (the legacy idiom the tracer removed, HomeScreen.tsx:168-175 pre-tracer), NOT `useDashboardSessionStore.getState().searchText` (the immediate value). Compute `term = debouncedSearchText.trim()` and use THAT SAME value for the read branch, `hasTerm`, and the empty-state term so the read / hasTerm / empty-state copy cannot diverge during focus/refresh races. reload() is a useCallback whose dependency list includes `debouncedSearchText` (so it re-fires when the debounce settles); the clear affordance still updates the immediate (session) value for input responsiveness. **Capture ONE `now` per reload (addresses cross-AI review CYCLE-3 MEDIUM):** at the top of reload() compute `const now = localDateTime()` ONCE and pass that SAME `now` to every birthday-resolving read/count in this reload — the list read AND `countBirthdayPopulation` (below) — never a fresh `localDateTime()` per call. When term is non-empty call `listDashboardSearch(exec, query, term, now)`, else `listDashboardPopulation(exec, query, now)`. (listDashboardPopulation resolves the birthday window from the passed date, dashboard-read.ts:274, so an independent second localDateTime() in the count could straddle local midnight and disagree with the list.) The debounce is what collapses a keystroke burst to ONE read; the cancelled flag only drops a stale async result (it does not reduce the number of queries). Keep the Promise.all counts + the cancelled-flag guard + the freshness triple.
    **populationCounts (addresses cross-AI review CYCLE-2 HIGH):** feed selectDashboardEmptyState with hasTerm (term!==''), activePopulations: query.populations, activeFilters: query.filters, and populationCounts derived from CHEAP dedicated counts (do NOT issue per-population full reads just to size the empty state): not-contacted ← the existing countNeverContacted, snoozed ← the existing countSnoozed, and **birthdays ← the NEW bound-only `countBirthdayPopulation(exec, now)` from Plan 03** (the SAME per-reload `now` passed to the list read — cross-AI review CYCLE-3 MEDIUM) — add it to the existing Promise.all counts (one cheap count, NOT a reuse of an internal resolution that the read never exposes, and NOT a raw listBirthdayCandidates count which would over-count by including Unbound). A gravity-filtered zero-result is the filter-empty case (rowCount===0), not a population-empty. Include the existing live/neverContacted/snoozed/archived/unbound counts and rowCount; keep activeFilter:'all'. Render the Phase 25 search-empty / population-specific empty copy accordingly, sourcing population display names from `control-labels.ts` (POPULATION_LABELS — the single source of truth from Plan 01), never re-authored inline. NEVER pass toISOString().
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: type a query → results narrow to name+notes matches (bound-only, no Unbound/archived); a no-match query shows 'No matches for "…"'; select the Not Contacted population with no such contacts → the not-contacted empty copy; results update behind an open panel (live-apply still holds).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "listDashboardSearch\|listDashboardPopulation" src/screens/HomeScreen.tsx` shows both reads wired (search vs no-term branch); `grep -n "toISOString" src/screens/HomeScreen.tsx` returns nothing.
    - reload() keys off the DEBOUNCED search value, NOT the immediate one: `grep -n "getState().searchText" src/screens/HomeScreen.tsx` returns nothing in the reload path — the read/hasTerm/empty-state all use the reactive `debouncedSearchText` (which is also in reload's dependency list); a keystroke burst produces one read (the debounce, not the cancelled flag, does the collapsing).
    - ONE `now` per reload (cross-AI review CYCLE-3 MEDIUM): reload() computes a single `const now = localDateTime()` and threads it to listDashboardPopulation/listDashboardSearch AND countBirthdayPopulation — grep shows exactly one `localDateTime()` call inside reload() and the list read + `countBirthdayPopulation` both reference that `now` variable (no fresh `localDateTime()` per read/count), so the birthday list and its empty-state count stay coherent across local midnight.
    - The empty-state gate receives hasTerm + activePopulations + activeFilters + populationCounts; `grep -n "countBirthdayPopulation" src/screens/HomeScreen.tsx` present — birthdays is sourced from the bound-only count (NOT listBirthdayCandidates, NOT an internal reuse), not-contacted/snoozed from the existing counts, with no added per-population full reads; search-empty resolves before filter/population copy; population display names come from `control-labels` (no inline population strings).
    - `tsc --noEmit` + `check:colors` + `npm test` green.
    - On-device: search returns bound-only name/notes matches; no-match + population-specific empties (birthdays / not-contacted / snoozed) render; the Birthdays empty count matches the Birthdays population and does not include Unbound.
  </acceptance_criteria>
  <done>HomeScreen search runs on the D-12 read with the empty-state gate fed the new population/filter/term shape.</done>
</task>

<task type="auto">
  <name>Task 3: Retire legacy listDashboard + listNeverContacted (no dual-read); preserve the D-03 count/policy machinery</name>
  <read_first>
    - **MANDATORY before any deletion (addresses cross-AI review CYCLE-3 LOW):** RE-READ `src/db/dashboard-read.test.ts` IN FULL first. Every line number cited in this task (943-1041, 1019-1041, 1069-1124, 366-665, and the describe-block line hints below) is APPROXIMATE — the file was last line-verified in a prior cycle and may have drifted. Locate each describe block BY ITS NAME in the actual current file and confirm the block boundaries (and that lines 1030/1039 still sit inside the `listNeverContacted` block) against what you read on disk, NOT against these numbers. This is the single largest load-bearing line-citation in the phase; treat the numbers as guidance and the block NAMES as authoritative.
    - src/db/dashboard-read.ts:365-503 (listDashboard 365-442 + listNeverContacted/listNeverContactedWithPolicy 459-503 to retire; readIncludeUnboundNeverContacted 466-475 + countNeverContacted 527-541 to PRESERVE)
    - src/db/dashboard-read.test.ts — delete ONLY the describe blocks literally named for the retired reads, BY BLOCK NAME (do NOT delete by line range 174-941, which engulfs a must-keep block): remove `listDashboard — default population + exclusions + snooze` (174), `listDashboard — sorts` (222), `listDashboard — filters (four mutually-exclusive branches)` (254), `listDashboard — favourites metadata + LOW-2 precedence` (666), `listDashboard — search` (696), `listDashboard — fuelText parity + A-1 null-progress ordering` (847), `listDashboard — LOW-1 tiebreak binds c.name not cat.name` (896), `listDashboard — full card projection shape` (916), and `listNeverContacted` (943-1041). MUST PRESERVE INTACT: `listDashboardPopulation — Phase 25 Active universe` (366-665, sits INSIDE the old range), the new `listDashboardSearch` + `countBirthdayPopulation` blocks (Plan 03 — do NOT delete these; countBirthdayPopulation is a KEEPER, not a retired read), `listFavourites` (1043), `counts` (1069-1124), `Bound SQL predicate parity` (1126), `listBirthdayCandidates` (1134), the countNeverContacted assertion at 1088 (inside the surviving `counts` block), and digest-read.test countNeverContacted usage.
    - **D-03 coverage reconciliation (addresses cross-AI review CYCLE-2 MEDIUM — the OLD note's "preserve 1030/1039" was IMPOSSIBLE):** lines 1030/1039 (`countNeverContacted` × `include_unbound_never_contacted` opt-in) live INSIDE the `listNeverContacted` describe block (943-1041, the test "excludes Unbound contacts by default and includes them in both list and count after persisted opt-in", 1019-1041) which ALSO calls `listNeverContacted(exec,{...})` directly — so they CANNOT survive that block's deletion (they will not compile once listNeverContacted is removed from dashboard-read.ts). The `counts` block (1069-1124) does NOT cover the include_unbound_never_contacted × countNeverContacted interaction. To avoid silently dropping the ONLY coverage of the exact setting D-03 says to preserve/coordinate-with-Phase-36: BEFORE deleting the `listNeverContacted` block, LIFT a NEW test into the surviving `describe("counts")` block that seeds an Unbound never-contacted contact and asserts `countNeverContacted` EXCLUDES it by default and INCLUDES it after the persisted `include_unbound_never_contacted` opt-in (port the assertion logic from 1019-1041 but exercise ONLY `countNeverContacted` — never `listNeverContacted`). This preserves the D-03-adjacent coverage without keeping the retired function. (Do NOT delete the include_unbound_never_contacted / countNeverContacted / readIncludeUnboundNeverContacted machinery itself — that would reverse D-03; this reconciles only the TEST coverage.)
    - src/services/widget/widget-data.ts:13-24 (uses listDashboardPopulation at runtime; the listDashboard mention at :18 is a stale COMMENT — update the comment, do not expect a runtime break)
    - stale COMMENT references to the retired reads to sweep (cross-AI review LOW): fuel-read.ts:12, HomeScreen.tsx:3 + :17 header comments, and dashboard-read.ts comments at :23-24/:147/:211/:448/:450 — update comments that NAME a now-deleted symbol; do NOT alter functional error strings inside PRESERVED functions (e.g. the kept string near dashboard-read.ts:473)
    - grep results confirming no other runtime caller of listDashboard/listNeverContacted
  </read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts</files>
  <action>
    Delete `listDashboard` and `listNeverContacted`/`listNeverContactedWithPolicy` from dashboard-read.ts (no runtime caller remains after Task 2). KEEP `countNeverContacted`, `readIncludeUnboundNeverContacted`, the `include_unbound_never_contacted` read, all counts (countLiveContacts/countSnoozed/countArchived/countUnbound-adjacent), listBirthdayCandidates, listDashboardPopulation, the new listDashboardSearch, AND the new `countBirthdayPopulation` (Plan 03 — a keeper consumed by HomeScreen's empty-state count, NOT a retired read). Remove now-unused legacy types ONLY if tsc/biome flags them unused (keep DashboardRow; keep DashboardFilter if dashboard-empty-logic still imports it — verify before removing). In dashboard-read.test.ts: FIRST lift the D-03 coverage per read_first — add a NEW test to the surviving `describe("counts")` block asserting `countNeverContacted` excludes an Unbound never-contacted contact by default and includes it after the persisted `include_unbound_never_contacted` opt-in (exercising ONLY countNeverContacted, never listNeverContacted) — THEN remove the obsolete describe blocks SURGICALLY BY BLOCK NAME (the nine `listDashboard — …` blocks + the `listNeverContacted` block listed in read_first) and their now-unused imports, EXPLICITLY PRESERVING `listDashboardPopulation — Phase 25 Active universe` (which sits inside the old line span) and KEEPING the countNeverContacted / listDashboardPopulation / listDashboardSearch / countBirthdayPopulation / listFavourites / counts / Bound-SQL-parity / listBirthdayCandidates blocks green. Do NOT attempt to "preserve" the old lines 1030/1039 in place — they die with the listNeverContacted block; the lifted `counts` test is their replacement. Sweep stale COMMENT references to the retired reads (widget-data.ts:18, fuel-read.ts:12, HomeScreen.tsx:3/:17, dashboard-read.ts comment lines) — cosmetic; runtime already uses listDashboardPopulation — but do NOT alter functional strings inside preserved functions. Do NOT touch any migration, app_settings column, or PORTABLE_SETTINGS_KEYS (D-03).
  </action>
  <verify>
    <automated>grep -rnE "listDashboard\s*\(|export (async )?function listDashboard\b|\blistNeverContacted\s*\(|export (async )?function listNeverContacted\b" src --include=*.ts --include=*.tsx | grep -vE "listDashboardPopulation|listDashboardSearch" ; npx tsc --noEmit && npm run check:colors && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The grep for a bare `listDashboard` (excluding Population/Search) returns nothing in runtime src (only possibly an updated comment/none) — no dual-read remains. (Note: this grep gate is best-effort — it would not catch an aliased import like `import { listDashboard as ld }`; `tsc --noEmit` + `npm test` are the real safety net for a genuinely removed export — cross-AI review LOW.)
    - `listNeverContacted` is gone; `countNeverContacted` + `readIncludeUnboundNeverContacted` + the include_unbound read remain (grep confirms) and DigestScreen still compiles/consumes them.
    - The D-03 coverage was LIFTED, not dropped: a new test in the surviving `describe("counts")` block asserts `countNeverContacted` excludes an Unbound never-contacted contact by default and includes it after the `include_unbound_never_contacted` opt-in (grep the new assertion in the counts block); it exercises countNeverContacted only (no listNeverContacted call survives).
    - The `listDashboardPopulation — Phase 25 Active universe` describe block STILL EXISTS and passes (grep the block name in dashboard-read.test.ts) — proof the surgical deletion did not engulf it; the new listDashboardSearch + countBirthdayPopulation blocks also pass.
    - No stale COMMENT naming a deleted read survives in the swept files (widget-data.ts, fuel-read.ts, HomeScreen.tsx header, dashboard-read.ts comments); no functional error string in a preserved function was altered.
    - No migration/app_settings/PORTABLE_SETTINGS_KEYS change (git diff scope = dashboard-read.ts + its test + the swept comments).
    - The executor RE-READ `dashboard-read.test.ts` in full before deleting and located every target describe block BY NAME (cited line numbers treated as approximate) — the surgical by-name deletion + the preserved-block grep (`listDashboardPopulation — Phase 25 Active universe` still present) confirm the drift-tolerant approach held (cross-AI review CYCLE-3 LOW).
    - `tsc --noEmit` + `check:colors` + `npm test` all green.
  </acceptance_criteria>
  <done>Legacy listDashboard + listNeverContacted are retired with their obsolete tests removed; the D-03 count/policy machinery and all schema keys are preserved; full suite green with no dual-read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session search term → listDashboardSearch SQL | untrusted text drives the has-term read |
| viewMode toggle → dashboard-query-store → app_settings | persisted view preference write |
| legacy-read retirement → dashboard-read.ts / app_settings | code removal must not touch schema |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-20 | Tampering | search term → SQL | high | mitigate | listDashboardSearch (Plan 03) escapeLike + ?-binds the term; HomeScreen passes only the raw term string. |
| T-26-21 | Tampering | viewMode write | medium | mitigate | setViewMode → app-settings-dao assertDashboardViewMode; no raw SQL. |
| T-26-22 | Info disclosure | search scope leak | high | mitigate | The D-12 read is bound-only + archived-excluded (Plan 03); Unbound retrieval is the D-08 own-route search. |
| T-26-23 | Data loss / integrity | legacy-read retirement dropping the include_unbound machinery | high | mitigate | Task 3 explicitly preserves countNeverContacted + readIncludeUnboundNeverContacted + the include_unbound_never_contacted read + all app_settings keys (D-03); grep + tsc + npm test gate it. |
| T-26-24 | Info disclosure | device egress | low | accept | No network on any read path; no PORTABLE_SETTINGS_KEYS/emission change (D-03). |
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + `npm run check:colors`; Tasks 2-3 also `npm test`; Task 3 also the bare-listDashboard grep gate.
- Pixel backstops: DASHC-07 collapsible search reduced-motion + search+toggle row 44px at 200% scale; live search results (26-VALIDATION.md).
</verification>

<success_criteria>
The Dashboard hierarchy is complete (header → controls → search+toggle → collection); search runs on the D-12 read as ephemeral session state with a persisted List/Card toggle; legacy listDashboard + listNeverContacted are retired with no dual-read, while countNeverContacted + the include_unbound machinery + all schema keys are preserved (D-03).
</success_criteria>

<review_feedback_incorporated>
Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 4 (current) findings addressed in this plan:
- **[MEDIUM] "already-active view is a no-op" not enforced by the write path** (Codex, CYCLE-4 #3): `setViewMode` (dashboard-query-store.ts:65) always writes SQLite + calls set() even for the current value, so the idempotency must_have was unmet. Fixed with an equality guard at the call site (`if (mode === useDashboardQueryStore.getState().viewMode) return;` in the HomeScreen toggle onChange) AND a hardened store setter (`if (viewMode === get().viewMode) return;` at the top of setViewMode, above the Plan-01 generation bump) so an unchanged value skips persist + set() + reload; a dashboard-query-store.test.ts case asserts the no-op (no updateAppSettings write). Added src/stores/dashboard-query-store.ts + .test.ts to files_modified + Task 1 <files>; updated must_haves (toggle + idempotency edge-probe), Task 1 read_first/action/verify/acceptance, and artifacts. No app_settings/schema change — D-03 intact; robustness addition, no decision reversal.

Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 3 (history) findings addressed in this plan:
- **[MEDIUM] capture `now` once per reload** (Codex, dashboard-read.ts:274): reload() now computes ONE `const now = localDateTime()` and threads it to listDashboardPopulation/listDashboardSearch AND countBirthdayPopulation, so the birthday list and its empty-state count can't disagree across local midnight (must_have + the D-12 read-wiring must_have + Task 2 action/acceptance).
- **[MEDIUM] type the new SegmentedControl `icon` field as `IconName`** (Claude): the field is now MANDATORILY `icon?: IconName` (imported from icon-registry.ts, not a bare string), so `tsc --noEmit` catches an unregistered icon at the SegmentedControl call site, not only inside Icon.tsx (must_have + Task 1 read_first/action/acceptance).
- **[LOW] mandate a full re-read of `dashboard-read.test.ts` before deleting** (both lanes): Task 3 read_first now opens with a MANDATORY full re-read directive treating every cited line number (943-1041, 1019-1041, 1069-1124, 366-665, block hints) as approximate and the block NAMES as authoritative; an acceptance criterion records the drift-tolerant by-name approach.

Cross-AI review CYCLE 2 (history — addressed in commit 957aa8c, retained):
- **[HIGH] Birthdays populationCounts unimplementable + would over-count** (both lanes + orchestrator): the birthdays empty-state count now consumes the NEW bound-only `countBirthdayPopulation(exec, localDateTime())` from Plan 03 (added to HomeScreen's Promise.all counts) — a real, cheap, bound-only derivation that matches the Birthdays population, NOT the unimplementable "reuse the single listBirthdayCandidates resolution — do not re-scan" (which never crossed the module boundary and would have over-counted by including Unbound). must_have + Task 2 action/read_first/acceptance rewritten; the old wording is explicitly superseded.
- **[MEDIUM] reload() must read the DEBOUNCED value, not getState().searchText** (Codex): Task 2 now keys reload/hasTerm/empty-state off a reactive `debouncedSearchText` (in reload's dependency list), never `useDashboardSessionStore.getState().searchText` (immediate) — so read/hasTerm/empty-state term cannot diverge in focus/refresh races (must_have + Task 2 action/acceptance grep gate on `getState().searchText`).
- **[MEDIUM] Task 3 "preserve 1030/1039" is impossible + strands D-03 coverage** (Claude): the false claim is dropped; the include_unbound_never_contacted × countNeverContacted scenario is LIFTED into the surviving `counts` block as a new countNeverContacted-only test BEFORE the listNeverContacted block is deleted — preserving the D-03-adjacent coverage without keeping the retired function (read_first + Task 3 action + acceptance). The D-03 machinery itself is untouched (no reversal).
- **[MEDIUM] search collapse/expand animation must pause off-focus/background** (Codex): the collapse/expand Reanimated timing now pauses/settles on useIsFocused()===false / AppState background, mirroring OrreryScreen (must_have + Task 1 action/read_first/acceptance).
- **[cosmetic] missing `<acceptance_criteria>` opening tag in Task 3** (orchestrator): restored.

Cross-AI review CYCLE 1 (history — addressed in commit 1866d6d, retained):
- **[MEDIUM] List/Card icon impossible with current SegmentedControl** (Codex, verified → MEDIUM): SegmentedControl.tsx is in files_modified + Task 1; `SegmentedControlOption` gains optional icon data + a filled/outline render path, preserving the Orrery label-only consumer (proven by tsc).
- **[LOW] Wave-3 barrier / depends_on omits 26-02** (Codex → LOW): `depends_on` includes `26-02` (owns DashboardControlRow, which HomeScreen mounts); the `wave: 3` field remains the real barrier.
- **[MEDIUM] debounce vs cancelled flag** (Codex): Task 1/2 require an explicit ~220ms debounce over the session searchText to collapse a keystroke burst to one read; the cancelled flag is only the stale-result backstop.
- **[MEDIUM] test-deletion line range engulfs a must-keep block** (orchestrator fact-drift): Task 3 deletes the legacy describe blocks BY BLOCK NAME, explicitly preserving `listDashboardPopulation — Phase 25 Active universe` (366-665), with a grep gate proving it survives.
- **[LOW] stale strings** (orchestrator): Task 3 sweeps stale comments in widget-data.ts / fuel-read.ts / HomeScreen header / dashboard-read.ts, leaving functional strings in preserved functions alone.
- **[LOW] grep-gate robustness** (Claude): acceptance notes the bare-listDashboard grep is best-effort (aliased imports); tsc + npm test are the real safety net.
</review_feedback_incorporated>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-07-SUMMARY.md` when done.
</output>
