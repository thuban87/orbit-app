---
phase: 26-dashboard-control-surface
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/control-surface/FilterPanelContent.tsx
  - src/components/control-surface/SortPanelContent.tsx
  - src/components/control-surface/DashboardControlRow.tsx
  - src/components/control-surface/filter-summary.ts
  - src/components/control-surface/filter-summary.test.ts
  - src/components/control-surface/control-labels.ts
  - src/components/control-surface/control-labels.test.ts
  - src/logic/dashboard-query-logic.ts
autonomous: true
requirements: [DASHC-03, DASHC-06]
estimate:
  tokens: 70000
  raw_tokens: 37000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The control row shows three separate equal controls — Population, Filters, Sort (testIDs dashboard-population-control / dashboard-filters-control / dashboard-sort-control); they are never combined into one 'Manage View' control (DASHC-03 / dossier §C [REJECTED])."
    - "Each control's summary shows the SELECTED OPTION LABELS (not the family names) collapsed to '+N' via collapseSummary — e.g. Charger + Weekly + Deep selected reads 'Charger, Weekly +1', category ids resolved to their category names; a control at its default reads neutral ('Filters' / 'Default') and a non-default control gets a restrained active treatment (accent border/label + a non-colour cue), not a full filled block (DASHC-03; addresses cross-AI review MEDIUM — summary must expose option labels, not family names)."
    - "All filter-family / filter-option / sort-mode user-facing copy is rendered from the SINGLE source-of-truth label module src/components/control-surface/control-labels.ts (created in Plan 01 with population + axis-default labels; this plan EXTENDS it with the filter families/options + sort modes) — FilterPanelContent, SortPanelContent, and the DashboardControlRow summaries all import their labels from it, and Plan 07's empty-state copy reads from it too; NO panel/summary/empty-state re-authors these strings inline (addresses cross-AI review CYCLE-2 MEDIUM — one label source, not re-authored across surfaces)."
    - "Tapping Filters opens the largest anchored panel with one section per Phase 25 filter family (Category, Social Battery, Needs Attention, Gravity, Contact Frequency; section testIDs dashboard-filter-family-{family}, options dashboard-filter-option-{family}-{value}); options toggle live and a Clear filters affordance (dashboard-filters-clear) calls setFilters(getExecutor(),{}) (DASHC-06 / §I)."
    - "The Gravity family options are sourced from the authoritative GRAVITY_TIERS constant (src/services/impact.ts:63 → values thin/building/solid/deep via GRAVITY_TIERS.map(t=>t.name)), NOT hand-listed and NOT from DASHBOARD_FILTER_FAMILIES (which carries only the family name, no tier values); the emitted gravity option values are exactly the tier names filterByGravity compares against, so a displayed tier always survives filtering (DASHC-06; addresses cross-AI review MEDIUM)."
    - "The Social Battery and Needs Attention family option VALUES are sourced from the authoritative closed constants exported from dashboard-query-logic.ts — `SOCIAL_BATTERY_VALUES` (the battery option values) and `NEEDS_ATTENTION_VALUE` (the single Needs-Attention toggle value) — NOT hand-listed in the panel (addresses cross-AI review CYCLE-5 MEDIUM — those two constants are currently module-private at dashboard-query-logic.ts:38-39 with no export, unlike the already-exported CONTACT_FREQUENCY_BANDS at :29). This plan EXPORTS both constants (adds `export` to their existing declarations, mirroring CONTACT_FREQUENCY_BANDS) and adds `src/logic/dashboard-query-logic.ts` to files_modified; FilterPanelContent imports them and its emitted option values EQUAL those exported sets, so the panel and buildFilterWhere's binding (dashboard-query-logic.ts:92-99,108) cannot drift — exactly the anti-drift route already used for Gravity via GRAVITY_TIERS. The exported values are query VALUES only; their DISPLAY labels still come from control-labels.ts (label vs value separation, single source of truth)."
    - "DashboardControlRow's `listCategories` fetch has a specified refetch lifecycle (addresses cross-AI review CYCLE-3 MEDIUM — otherwise a category renamed/added in another screen mid-session leaves a stale Category option list): it fetches `listCategories(getExecutor())` inside a `useFocusEffect` (with the repo's cancelled-flag async guard), so the Category family options + the summary's category-id→name resolution refresh whenever the Dashboard regains focus after a category edit — NOT a mount-only fetch."
    - "Tapping Sort opens the compact anchored panel listing the Phase 25 sort modes (dashboard-sort-option-{mode}) as a single-select including an explicit Default (dashboard-sort-option-default → setSort(getExecutor(),'default')) (DASHC-06 / §J)."
    - "FilterPanelContent and SortPanelContent (layer 2) take INTENT-BASED props — FilterPanelContent `{state, categories, onToggleFilter(family,value), onClearFilters}` and SortPanelContent `{state, onSelectSort(mode)}` — and import neither AnchoredPanel internals nor app-settings-dao. `state` drives which options render selected; the panels emit only the user's INTENT (which family+value toggled / clear / which mode) and compute NO resolved DashboardFilters object from their render snapshot. The serialized owner (DashboardControlRow) derives `next` from useDashboardQueryStore.getState() at press time and calls setFilters/setSort(getExecutor(), next) (presentation seam D-11; addresses cross-AI review CYCLE-4 #1 — a layer-2 onChange(completeObject) contract would contradict the getState()-at-press serialization and let two fast presses race off the same stale snapshot; reconciled with Plan 01's onTogglePopulation contract)."
    - "Persistence-failure path (owner-side): the DashboardControlRow intent-callback wiring awaits setFilters/setSort and, on rejection (dashboard-query-store setters can reject before state changes), retains the prior selection and surfaces a themed non-blocking error (theme tokens only, CLAUDE.md) — never an unhandled promise (robustness; addresses cross-AI review MEDIUM, mirrors Plan 01). The layer-2 panels only emit intents; they never await or handle the write."
    - "Concurrent async setters cannot lose a selection (addresses cross-AI review CYCLE-3 MEDIUM + CYCLE-4 #1 — the store setters at dashboard-query-store.ts:81-92 do not serialize writes, so two fast toggles computed from a rendered snapshot can persist an older state last). Because FilterPanelContent/SortPanelContent emit INTENTS (onToggleFilter(family,value) / onClearFilters / onSelectSort(mode)) rather than resolved objects, the DashboardControlRow intent-callback handlers use a CONCRETE serialization mechanism: (1) they DERIVE each mutation's `next` from `useDashboardQueryStore.getState()` at press time from the emitted INTENT — never from a captured panel render snapshot (this is exactly WHY the layer-2 callbacks are intent-based and not onChange(completeObject); a resolved-object callback would make clause (1) impossible — cross-AI review CYCLE-4 #1) — and (2) they DISABLE further option/mode presses while a setFilters/setSort write is in flight (a per-row/per-axis pending flag), re-enabling on settle/rejection. This composes with Plan 01's store generation guard: no two setter writes overlap, and each write derives from the freshest state."
    - "Selecting a filter/sort applies immediately (no Apply step); a zero-result selection leaves the panel open with the cause-aware empty state behind it; switching from one open control to another happens in one tap and only one panel is open at a time (DASHC-03/04/05). The Filters/Sort triggers open through the SAME Plan 01 mechanism the Population trigger uses: each measures its anchor (measureInWindow) and reports {anchorRect, size, content} to the root-level DashboardOverlayHost — they do NOT render an inline AnchoredPanel inside the FlatList header (consistent with the Plan 01 cycle-2 overlay-host fix; D-11 in-tree non-Modal)."
    # --- UI-SPEC ## UI Considerations lift (S1 remaining) ---
    - "S1 (Filters/Sort): 0 selected → default summary ('Filters' / 'Default'); 1 → the name; many → names then '+N' (zero-one-many, populated)."
    - "Filters family options and sort modes render in a fixed order derived from DASHBOARD_FILTER_FAMILIES / DASHBOARD_SORT_MODES so the controls cannot drift; Sort is single-select, Filters multi-select OR-within-family."
  artifacts:
    - src/components/control-surface/FilterPanelContent.tsx
    - src/components/control-surface/SortPanelContent.tsx
    - "src/components/control-surface/filter-summary.ts :: selectedFilterLabels(filters, categories) (pure; ordered selected option labels for the Filters summary)"
    - "src/components/control-surface/control-labels.ts :: EXTENDED with filter-family/option + sort-mode display labels (the single source of truth created in Plan 01)"
    - "src/logic/dashboard-query-logic.ts :: SOCIAL_BATTERY_VALUES + NEEDS_ATTENTION_VALUE promoted to exports (were module-private at :38-39) so FilterPanelContent can source the Social Battery / Needs Attention option values from the same closed constants buildFilterWhere binds against"
  key_links:
    - "FilterPanelContent.onToggleFilter(family,value) / onClearFilters → DashboardControlRow derives next from useDashboardQueryStore.getState().filters under a per-axis pending lock → setFilters(getExecutor(), next) / setFilters(getExecutor(), {}) → app-settings-dao assertDashboardFilters"
    - "SortPanelContent.onSelectSort(mode) → DashboardControlRow under a per-axis pending lock → setSort(getExecutor(), mode) → app-settings-dao assertDashboardSort"
    - "DashboardControlRow renders all three triggers, each opening the shared Plan 01 AnchoredPanel with its layer-2 content"
  prohibitions:
    - "MUST NOT combine Population/Filters/Sort into one modal/'Manage View' control (dossier §C [REJECTED])."
    - "MUST NOT re-author Phase 25 filter-family / sort labels or copy — render only (D-11 layer-1)."
    - "MUST NOT offer a sort-by-favourite_rank option or any ranked-favourites affordance (ADR-075 / D-06)."
    - "MUST NOT write app-settings-dao or SQL directly from a layer-2 content component; MUST NOT import AnchoredPanel internals into layer 2 (D-11 seam)."
---

<objective>
Complete the three-equal-control row (DASHC-03) by adding the Filters and Sort anchored panels alongside the tracer's Population control, all through the same AnchoredPanel primitive and the same presentation seam (D-11). Filters clear in-panel and Sort offers an explicit Default (DASHC-06).

Purpose: Expand the proven Population slice horizontally to the other two query axes without touching HomeScreen (DashboardControlRow is self-contained) or forking Phase 25 state.
Output: `FilterPanelContent`, `SortPanelContent`, and a `DashboardControlRow` that renders all three triggers + summaries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md
@.planning/phases/26-dashboard-control-surface/26-01-SUMMARY.md

@src/logic/dashboard-query-logic.ts
@src/stores/dashboard-query-store.ts
@src/components/SegmentedControl.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterPanelContent (largest panel, per-family sections, live options + Clear filters)</name>
  <read_first>
    - src/components/control-surface/PopulationPanelContent.tsx (the layer-2 contract to mirror — INTENT-BASED `{state, onTogglePopulation}`: the panel emits a toggle INTENT (the key), never a resolved object; the serialized owner derives next from getState() at press time. Mirror this for Filters/Sort. Also: redundant non-colour selected affordance, labels from control-labels)
    - src/components/control-surface/control-labels.ts (Plan 01 — the single source-of-truth label module to EXTEND with filter families/options + sort modes; the axis defaults live here too)
    - src/components/control-surface/DashboardControlRow.tsx (where the Filters trigger + panel wire in — it reports anchor+content to the Plan 01 DashboardOverlayHost, and OWNS the listCategories fetch)
    - src/logic/dashboard-query-logic.ts:14-116 (DASHBOARD_FILTER_FAMILIES, DashboardFilters shape, buildFilterWhere families — Category/Social Battery/Needs Attention/Gravity/Contact Frequency; SOCIAL_BATTERY_VALUES at :38; CONTACT_FREQUENCY_BANDS at :29; NEEDS_ATTENTION_VALUE at :39). NOTE: DASHBOARD_FILTER_FAMILIES carries only the family NAME — it is NOT the source of gravity tier values. NOTE (cross-AI review CYCLE-5 MEDIUM): SOCIAL_BATTERY_VALUES and NEEDS_ATTENTION_VALUE are currently `const` with NO `export` (module-private), unlike the already-exported CONTACT_FREQUENCY_BANDS — this task PROMOTES both to exports so the panel can source its Social Battery / Needs Attention values from them. Confirm the on-disk export state before editing.
    - src/services/impact.ts:63 (GRAVITY_TIERS — the AUTHORITATIVE gravity tier list: thin/building/solid/deep; the gravity family options come from GRAVITY_TIERS.map(t=>t.name), and these are exactly the values filterByGravity at dashboard-read.ts:324-331 compares against)
    - src/stores/dashboard-query-store.ts:81-88 (setFilters(exec,filters)) — note setFilters can reject; the DashboardControlRow intent-callback wiring must await + handle failure (see must_haves)
    - src/components/SegmentedControl.tsx:54-84 (filled-accent + accessibilityState.selected idiom)
    - src/db/contact-read.ts (listCategories — Category family option source; read at the DashboardControlRow level and pass names in as state, do NOT read the DB inside layer-2 content; the category NAME resolved here is also what the active-filter summary shows for a selected category id)
  </read_first>
  <files>src/components/control-surface/FilterPanelContent.tsx, src/components/control-surface/DashboardControlRow.tsx, src/components/control-surface/filter-summary.ts, src/components/control-surface/filter-summary.test.ts</files>
  <action>
    Create a PURE `filter-summary.ts` (no react-native import) exporting `selectedFilterLabels(filters: DashboardFilters, categories: {id:number;name:string}[]): string[]` that returns the SELECTED OPTION LABELS in a deterministic family order (Category names resolved from the categories arg, then Social Battery / Needs Attention / Gravity / Contact Frequency display labels) — this is the array DashboardControlRow feeds into collapseSummary for the Filters summary. Author `filter-summary.test.ts` (vitest node) covering: a single-family selection, a MIXED multi-family selection (asserting order + that category ids resolve to names), an unknown/missing category id (graceful — skip or fallback, not a crash), and the empty-filters case (returns []). This is the node test the review asks for (mixed-family summary).
    First, in `src/logic/dashboard-query-logic.ts`, PROMOTE the two module-private filter constants to exports (addresses cross-AI review CYCLE-5 MEDIUM — they are `const` with no `export` at :38-39; add `export` to each declaration, mirroring the already-exported CONTACT_FREQUENCY_BANDS at :29): `export const SOCIAL_BATTERY_VALUES` and `export const NEEDS_ATTENTION_VALUE`. No other change to that file — buildFilterWhere already consumes them internally, so this is purely widening visibility. (Confirm the on-disk state first; do not touch DASHBOARD_FILTER_FAMILIES.)
    Create `FilterPanelContent.tsx` (LAYER 2): INTENT-BASED props `{ state: DashboardQueryState; categories: {id:number;name:string}[]; onToggleFilter: (family: DashboardFilterFamily, value: string) => void; onClearFilters: () => void }`. Render one section per DASHBOARD_FILTER_FAMILIES in fixed order with a Label-role family header (testID `dashboard-filter-family-{family}`) and its options: Category → one option per passed-in category (value = the category id as string), Social Battery → one option per value in the exported `SOCIAL_BATTERY_VALUES` (imported from dashboard-query-logic.ts — do NOT hand-list the battery values), Needs Attention → a single toggle whose value is the exported `NEEDS_ATTENTION_VALUE` (imported from dashboard-query-logic.ts — do NOT hard-code the literal), Gravity → `GRAVITY_TIERS.map(t=>t.name)` imported from src/services/impact.ts (values thin/building/solid/deep — do NOT hand-list them and do NOT read them from DASHBOARD_FILTER_FAMILIES), Contact Frequency → the CONTACT_FREQUENCY_BANDS buckets (weekly/monthly/quarterly/yearly). The Social Battery / Needs Attention / Contact Frequency / Gravity option VALUES all come from these authoritative closed constants (never hand-typed in the panel), exactly as the emitted values equal what buildFilterWhere binds against — the same anti-drift route used for Gravity. Each option (testID `dashboard-filter-option-{family}-{value}`) calls `onToggleFilter(family, value)` with the toggled family+value INTENT only — it does NOT compute the next DashboardFilters from its render snapshot (that derivation moves to the serialized owner, DashboardControlRow — cross-AI review CYCLE-4 #1); it reads `state.filters` only to render the selected affordance. Carry `accessibilityState={{selected}}` + redundant non-colour affordance. A `Clear filters` control (testID `dashboard-filters-clear`) calls `onClearFilters()`. **Labels vs values (single source of truth — addresses cross-AI review CYCLE-2 MEDIUM):** the family HEADER text and each option's DISPLAY LABEL come from `control-labels.ts` — EXTEND that module (created in Plan 01) in this plan with the filter-family + filter-option labels (and the sort-mode labels in Task 2), keyed off the query values; the query VALUES/option keys still come from GRAVITY_TIERS / CONTACT_FREQUENCY_BANDS / SOCIAL_BATTERY_VALUES / NEEDS_ATTENTION_VALUE. Render labels from the module, never author display strings inline. Import NEITHER AnchoredPanel internals NOR app-settings-dao. Do NOT read the DB here — categories come in as a prop.
    Edit `DashboardControlRow.tsx`: add the Filters trigger (testID `dashboard-filters-control`, label `Filters` from control-labels, default summary `Filters`). The Filters SUMMARY is `collapseSummary(selectedFilterLabels(state.filters, categories), maxShown)` — the SELECTED OPTION LABELS across all families (NOT the family names), category ids resolved to names, then join + '+N' (addresses cross-AI review MEDIUM — DASHC-03). Open the panel via the SAME Plan 01 mechanism the Population trigger uses: measure the trigger (measureInWindow) and report {anchorRect, size: large, content: `<FilterPanelContent … />`, onDismiss} to the root-level DashboardOverlayHost — do NOT render an inline AnchoredPanel in the FlatList header (consistent with the Plan 01 cycle-2 overlay-host fix). `FilterPanelContent`'s INTENT callbacks are handled in DashboardControlRow (cross-AI review CYCLE-4 #1 — the panel emits `onToggleFilter(family,value)` / `onClearFilters`, NOT a resolved DashboardFilters object): `onToggleFilter(family, value)` derives `const current = useDashboardQueryStore.getState().filters` at press time (NOT a captured render snapshot), computes `next` by toggling `value` within `current[family]` (OR-within-family), and calls `setFilters(getExecutor(), next)` AWAITED with the persistence-failure path from must_haves (retain prior selection + themed error on rejection, theme tokens only); `onClearFilters` calls `setFilters(getExecutor(), {})`. **Serialize the writes (addresses cross-AI review CYCLE-3 MEDIUM + CYCLE-4 #1 — dashboard-query-store.ts:81-92 does not serialize):** because the derivation reads `getState()` at press (not a snapshot) AND further option presses are DISABLED while a setFilters write is in flight (a `pending` flag flipped around the awaited setter, cleared on settle/rejection), two fast toggles cannot persist an older state last. **DashboardControlRow OWNS the category fetch (single owner — addresses cross-AI review CYCLE-2 LOW):** fetch `listCategories(getExecutor())` in DashboardControlRow inside a `useFocusEffect` with the repo's cancelled-flag async guard (addresses cross-AI review CYCLE-3 MEDIUM — a mount-only fetch would leave a stale Category list after a rename/add in another screen; refetch on Dashboard focus). Plan 01 already dropped the orphaned HomeScreen fetch — do NOT re-add it there and do NOT accept categories as a HomeScreen prop; pass the fetched names into FilterPanelContent. Restrained active treatment when any family has selections.
  </action>
  <verify>
    <automated>npx vitest run src/components/control-surface/filter-summary.test.ts && npx tsc --noEmit && npm run check:colors && npx vitest run src/logic/dashboard-query-logic.test.ts</automated>
    <human-check>On the Pixel: open Filters → largest panel with all five family sections; toggle Category + Social Battery + Gravity options → list re-queries live and the control summary shows the selected OPTION labels (e.g. 'Charger, {Category} +1'), not family names; Clear filters resets to none and the summary returns to 'Filters'.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -nE "AnchoredPanel|app-settings-dao|getAllAsync|getExecutor" src/components/control-surface/FilterPanelContent.tsx` returns nothing (layer-2 seam clean; no DB read).
    - `grep -n "GRAVITY_TIERS" src/components/control-surface/FilterPanelContent.tsx` present — the gravity options import from src/services/impact.ts; the four rendered gravity option values equal `GRAVITY_TIERS.map(t=>t.name)` (thin/building/solid/deep), NOT a hand-typed list.
    - Both filter constants are now exported (cross-AI review CYCLE-5 MEDIUM): `grep -nE "^export const (SOCIAL_BATTERY_VALUES|NEEDS_ATTENTION_VALUE)" src/logic/dashboard-query-logic.ts` returns BOTH; and `grep -nE "SOCIAL_BATTERY_VALUES|NEEDS_ATTENTION_VALUE" src/components/control-surface/FilterPanelContent.tsx` present — the panel imports both from dashboard-query-logic.ts and its rendered Social Battery option values equal `SOCIAL_BATTERY_VALUES` while its Needs Attention toggle value equals `NEEDS_ATTENTION_VALUE` (not hand-typed); `tsc --noEmit` stays green (buildFilterWhere still consumes them internally).
    - `selectedFilterLabels` is pure (no react-native import) and `filter-summary.test.ts` passes: single-family, mixed multi-family (order + category-id→name resolution), unknown-category graceful, empty→[].
    - Filter-family/option DISPLAY labels are rendered from `control-labels` (single source of truth): `grep -n "control-labels" src/components/control-surface/FilterPanelContent.tsx` present; control-labels.ts was EXTENDED with the filter families/options (grep the new keys) and its test still passes; no inline family/option display-string literals in FilterPanelContent.
    - Section testIDs `dashboard-filter-family-{family}` and option testIDs `dashboard-filter-option-{family}-{value}` + `dashboard-filters-clear` are present.
    - `listCategories` is fetched ONLY in DashboardControlRow (single owner): `grep -rn "listCategories" src/screens/HomeScreen.tsx` returns nothing; `grep -n "listCategories" src/components/control-surface/DashboardControlRow.tsx` present, inside a `useFocusEffect` (grep shows `useFocusEffect` around the listCategories fetch — refetch-on-focus, not mount-only; cross-AI review CYCLE-3 MEDIUM).
    - FilterPanelContent emits INTENTS, not resolved objects (cross-AI review CYCLE-4 #1): it takes `onToggleFilter(family,value)` + `onClearFilters` and does NOT emit a resolved DashboardFilters (grep FilterPanelContent.tsx: no `DashboardFilters` onChange / no `setFilters`). Setter serialization lives in DashboardControlRow: the `onToggleFilter` handler derives `next` from `useDashboardQueryStore.getState().filters` (grep: `getState()` in DashboardControlRow's filter handler) and disables further presses while a write is pending (grep: a `pending`/in-flight flag around the awaited setFilters) — no two writes overlap.
    - `tsc --noEmit` + `check:colors` green.
    - On-device: options apply live; the summary shows selected option labels + '+N'; Clear filters empties every family and updates the summary.
  </acceptance_criteria>
  <done>Filters is a live-applying largest anchored panel with per-family sections and in-panel Clear, wired through setFilters.</done>
</task>

<task type="auto">
  <name>Task 2: SortPanelContent (compact single-select panel with explicit Default)</name>
  <read_first>
    - src/components/control-surface/FilterPanelContent.tsx (the layer-2 pattern just authored)
    - src/components/control-surface/control-labels.ts (extend with sort-mode labels; reuse AXIS_DEFAULT_LABELS.sort)
    - src/logic/dashboard-query-logic.ts:118-133 (DASHBOARD_SORT_MODES: default/name-asc/name-desc/least-recent/most-recent/status)
    - src/stores/dashboard-query-store.ts:89-92 (setSort(exec,sort))
    - src/components/control-surface/DashboardControlRow.tsx (Sort trigger wire-in — reports anchor+content to the Plan 01 DashboardOverlayHost)
  </read_first>
  <files>src/components/control-surface/SortPanelContent.tsx, src/components/control-surface/DashboardControlRow.tsx</files>
  <action>
    Create `SortPanelContent.tsx` (LAYER 2): INTENT-BASED props `{ state: DashboardQueryState; onSelectSort: (mode: DashboardSortMode) => void }`. Render the DASHBOARD_SORT_MODES as single-select rows (testID `dashboard-sort-option-{mode}`) in fixed order, INCLUDING an explicit `Default` row (`dashboard-sort-option-default`); tapping a row calls `onSelectSort(mode)` with the selected mode INTENT (it reads `state.sort` only to render the active affordance — cross-AI review CYCLE-4 #1). The active row carries `accessibilityState={{selected}}` + a redundant check/filled affordance. Sort-mode DISPLAY labels come from `control-labels.ts` — EXTEND that module with the sort-mode labels + reuse its `AXIS_DEFAULT_LABELS.sort` ('Default'); render from it, never re-author sort strings inline (single source of truth). Import neither AnchoredPanel internals nor app-settings-dao.
    Edit `DashboardControlRow.tsx`: add the Sort trigger (testID `dashboard-sort-control`, label `Sort`, summary via collapseSummary/direct-name — `AXIS_DEFAULT_LABELS.sort` ('Default') when sort==='default', else the selected sort label from control-labels) opening the panel via the SAME Plan 01 overlay-host mechanism: measure the trigger and report {anchorRect, size: compact, content: `<SortPanelContent … />`, onDismiss} to the root-level DashboardOverlayHost (no inline AnchoredPanel in the FlatList header). `SortPanelContent`'s `onSelectSort(mode)` is handled in DashboardControlRow: because sort is single-select the emitted `mode` IS the complete next value (nothing accumulates from a prior snapshot, so there is no getState()-derivation to do for the value itself), but the write is still SERIALIZED like Filters (cross-AI review CYCLE-3 MEDIUM + CYCLE-4 #1) — call `setSort(getExecutor(), mode)` AWAITED under the same per-axis pending lock (disable further mode presses while a setSort write is in flight, cleared on settle/rejection) with the persistence-failure path (retain prior sort + themed non-blocking error on rejection, theme tokens only; mirrors the Filters wiring), so a rapid re-select cannot persist an older sort last. Restrained active treatment when sort !== 'default'. Confirm the three triggers now render as equal controls in one row.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/components/control-surface</automated>
    <human-check>On the Pixel: open Sort → compact panel; pick 'Least recent' → list re-sorts + summary shows the label; pick 'Default' → summary shows 'Default'. Confirm tapping another control while Sort is open switches directly (one panel open at a time).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -nE "AnchoredPanel|app-settings-dao" src/components/control-surface/SortPanelContent.tsx` returns nothing (seam clean).
    - Sort-mode labels are rendered from control-labels: `grep -n "control-labels" src/components/control-surface/SortPanelContent.tsx` present; control-labels.ts was extended with the sort-mode labels; no inline sort display-string literals.
    - Sort rows include `dashboard-sort-option-default`; there is NO favourite_rank / ranked-favourites sort option (grep confirms absence).
    - SortPanelContent emits an INTENT (`onSelectSort(mode)`), not a resolved object, and does NOT call `setSort` itself (grep SortPanelContent.tsx: no `setSort`). The Sort handler in DashboardControlRow uses the same serialization as Filters (cross-AI review CYCLE-3 MEDIUM + CYCLE-4 #1): the emitted `mode` is the complete next value (single-select, so no getState() value-derivation) but the write is still awaited under a per-axis pending lock (grep: a `pending`/in-flight flag around the awaited setSort in DashboardControlRow's sort handler) so a rapid re-select cannot persist an older sort last.
    - `tsc --noEmit` + `check:colors` green; `npx vitest run src/components/control-surface` passes.
    - On-device: three equal controls render; sort applies live; direct control-to-control switch works.
  </acceptance_criteria>
  <done>Sort is a compact single-select anchored panel with an explicit Default; the control row shows three equal live-applying controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter/sort panel content → dashboard-query-store → app-settings-dao → SQLite | filter/sort selections cross into persisted query state |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-05 | Tampering | Filter/Sort write path | high | mitigate | intent callbacks (onToggleFilter/onClearFilters/onSelectSort) → DashboardControlRow derives next from getState() → setFilters/setSort(exec,…) → app-settings-dao assertDashboardFilters/assertDashboardSort reject unknown families/values/modes; layer-2 emits only intents and never writes DAO/SQL directly (grep-verified). |
| T-26-06 | Tampering | buildFilterWhere SQL | high | mitigate | Phase 25 buildFilterWhere selects only closed SQL constants and ?-binds every value (category ids, battery strings, frequency bounds); this plan adds no interpolation. |
| T-26-07 | Info disclosure | device egress | low | accept | No network; no new emitted setting (D-03). |
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + `npm run check:colors` + `npx vitest run src/components/control-surface` / `src/logic/dashboard-query-logic.test.ts`.
- Pixel backstop: three controls live-apply and switch directly; Filters/Sort panels honour size variants (26-VALIDATION.md DASHC-04/05).
</verification>

<success_criteria>
Three equal Population/Filters/Sort controls each open their own live-applying anchored panel through one AnchoredPanel primitive; Filters clears in-panel, Sort offers explicit Default; both layer-2 components are seam-clean and Phase-25-state-only.
</success_criteria>

<review_feedback_incorporated>
Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 5 (current) findings addressed in this plan:
- **[MEDIUM] Plan 02 sources filter option values from un-exported, out-of-scope constants** (Codex/orchestrator, CYCLE-5 #1): `FilterPanelContent` was required to source its Social Battery / Needs Attention option values from `SOCIAL_BATTERY_VALUES` / `NEEDS_ATTENTION_VALUE`, but both are module-private at dashboard-query-logic.ts:38-39 (no `export`, unlike the exported CONTACT_FREQUENCY_BANDS at :29) and that file was absent from `files_modified`. Fixed additively: this plan now PROMOTES both constants to exports (Task 1 action), adds `src/logic/dashboard-query-logic.ts` to `files_modified` + artifacts, adds a must_have that the panel's emitted Social Battery / Needs Attention values EQUAL the exported sets (the same anti-drift route already used for Gravity via GRAVITY_TIERS), and adds an acceptance grep asserting both exports exist and the panel imports them. No decision reversal — widening two constants' visibility does not touch any D-NN/ADR; the values are query VALUES only, DISPLAY labels still come from control-labels.ts.

Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 4 (history) findings addressed in this plan:
- **[MEDIUM] Plans 01+02 — layer-2 onChange(completeObject) contract contradicts the getState()-at-press serialization** (Codex/orchestrator, CYCLE-4 #1): FilterPanelContent/SortPanelContent contracts are changed from `onChange(filters)`/`onChange(mode)` to INTENT-BASED `onToggleFilter(family,value)` + `onClearFilters` and `onSelectSort(mode)`. The panels emit only the user's intent (never a resolved object computed from their render snapshot), so the serialized owner (DashboardControlRow) genuinely derives `next` from `useDashboardQueryStore.getState().filters` at press time under a per-axis pending lock — making the previously-contradictory serialization must_have satisfiable, and reconciling Plan 02 with Plan 01's onTogglePopulation contract so the two plans share one consistent callback shape (must_haves + key_links + Task 1 read_first/action/acceptance for FilterPanelContent + Task 2 action/acceptance for SortPanelContent). No decision reversal — implementation-contract reconciliation upholding D-11 (no layer-1 fork).

Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 3 (history) findings addressed in this plan:
- **[MEDIUM] concurrent async setters can lose a selection (no serialization)** (Codex, dashboard-query-store.ts:81-92): a concrete mechanism is specified — the DashboardControlRow onChange wiring derives each mutation from `useDashboardQueryStore.getState()` at press time and disables further presses while a setFilters/setSort write is in flight (per-axis pending flag), composing with Plan 01's store generation guard (must_have + Task 1/2 action + acceptance grep).
- **[MEDIUM] DashboardControlRow owns the listCategories fetch but no refetch trigger** (Claude): the refetch lifecycle is specified as a `useFocusEffect` (cancelled-flag guarded) so a category renamed/added in another screen mid-session refreshes on Dashboard focus, not a stale mount-only fetch (must_have + Task 1 action + acceptance grep).

Cross-AI review CYCLE 2 (history — addressed in commit 957aa8c, retained):
- **[MEDIUM] name the single source of truth for filter/sort/population copy** (Codex): all filter-family/option + sort-mode DISPLAY labels are rendered from `control-labels.ts` (created in Plan 01 with population + axis-default copy; EXTENDED here) — FilterPanelContent, SortPanelContent, and the DashboardControlRow summaries import from it, Plan 07 reads population names from it; no strings re-authored across surfaces (must_have + Task 1/2 action + acceptance + artifacts + files_modified add control-labels.ts).
- **[LOW] listCategories fetch ownership** (Claude): DashboardControlRow is the SINGLE owner of the `listCategories(getExecutor())` fetch; Plan 01 dropped HomeScreen's orphaned fetch — the "or accept it as a prop from HomeScreen" ambiguity is removed (must_have context + Task 1 action + acceptance grep that HomeScreen has no listCategories).
- **[consistency] Plan 01 cycle-2 overlay-host fix** (orchestrator): the Filters/Sort triggers open via the root-level DashboardOverlayHost (report measured anchor + content), NOT an inline AnchoredPanel in the FlatList header — kept consistent with Plan 01 so the two plans do not contradict (D-11 in-tree non-Modal).

Cross-AI review CYCLE 1 (history — addressed in commit 1866d6d, retained):
- **[MEDIUM] gravity option source** (Codex/orchestrator): the Gravity family sources its options from the authoritative `GRAVITY_TIERS` constant (src/services/impact.ts:63), named in read_first + must_haves + action, with an acceptance gate that the emitted values equal `GRAVITY_TIERS.map(t=>t.name)` (which is exactly what `filterByGravity` compares against).
- **[MEDIUM] active-filter summary** (Codex, DASHC-03): the Filters summary collapses over selected OPTION labels (category ids resolved to names), not family names, via a pure `selectedFilterLabels` helper with a node test for mixed-family selections.
- **[MEDIUM] onChange persistence-failure path** (Codex): setFilters/setSort are awaited with prior-selection retention + a themed non-blocking error on rejection (must_have + both tasks).
</review_feedback_incorporated>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-02-SUMMARY.md` when done.
</output>
