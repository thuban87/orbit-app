---
phase: 26-dashboard-control-surface
plan: 04
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/screens/dashboard-overflow-actions.ts
  - src/screens/dashboard-overflow-actions.test.ts
  - src/components/OverflowMenu.tsx
  - src/components/ShellAppBar.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [DASHC-02, DASHC-08, DASHC-10]
estimate:
  tokens: 92000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    # --- Header destinations (DASHC-02 / D-04) ---
    - "The Dashboard root app-bar trailing region has TWO co-equal header destinations before the ⋯ overflow — Your Week (icon your-week, label 'Your Week', route Digest, testID dashboard-your-week-entry) and Group Events (icon group-events, label 'Group Events', route GroupEvents, testID dashboard-group-events-entry) — each a first-class icon+label destination (DASHC-02 / D-04)."
    - "Responsive fallback: when both labels cannot stay readable at the supported width/OS text scale, BOTH destinations fall back to icon-only with unchanged accessibilityLabels; the fallback is driven by a MEASURED-INTRINSIC label width — a hidden/invisible OFF-SCREEN label probe measured via onTextLayout (the labels are NOT visibly rendered by default, so the intrinsic width must come from a probe, not from the currently-rendered content) OR a reserved trailing-width budget — NOT a character-count/nominal-width heuristic AND NOT a 'measure the currently-rendered bar and compare' approach (with icon-only the mandated default there is no rendered expanded label to compare against — addresses cross-AI review CYCLE-4 #2); the header never wraps to a second line, never shrinks label text below role size, and never pushes the control row / search row down (D-04, locked)."
    - "The fit decision is OWNED at the ShellAppBar level, not from HomeScreen alone (addresses cross-AI review CYCLE-3 MEDIUM — ShellAppBar.tsx:42-66 owns the title (flex:1) / trailing / overflow / padding / flex layout, so measuring HomeScreen's trailing content alone yields only its post-flex allocated width, not whether title + two labels + overflow can coexist). ShellAppBar measures the full bar (its own root width via onLayout + the rendered title width via onTextLayout) AND computes the `compact` decision by comparing the available trailing space against a MEASURED-INTRINSIC expanded-trailing width — a hidden off-screen onTextLayout label probe OR a reserved trailing-width budget, NEVER the currently-rendered icon-only trailing content which carries no label width (addresses cross-AI review CYCLE-4 #2) — then passes the resulting `compact` trailing-fit signal into its trailing content; HomeScreen's two destinations consume that signal to decide icon-only vs icon+label. Existing ShellAppBar consumers (variant='child' Archived/Unbound, any other trailing ReactNode) are unaffected — the trailing-fit signal is additive and backward-compatible."
    - "Icon-only is the MANDATED pre-measurement DEFAULT, not a conditional (addresses cross-AI review CYCLE-3 MEDIUM — Claude's flash point): before the first measurement lands, BOTH destinations render ICON-ONLY (fail-safe), and labels are shown ONLY after ShellAppBar signals they fit (expand-if-room). The label→collapse flash therefore cannot occur — the header never renders labels first and then collapses. Upholds D-04's locked icon-only fallback."
    # --- Overflow (DASHC-08 / D-05/06/09/10) ---
    - "The ⋯ overflow renders exactly the amended 5-row set in order: Group Events (dashboard-group-events-overflow-entry) · Unbound Contacts (dashboard-unbound-overflow-entry) · Archived Contacts (dashboard-archived-overflow-entry) · Select Contacts (dashboard-select-contacts-entry) · Reset Dashboard View (dashboard-reset-view-entry) (DASHC-08)."
    - "Select Contacts ships DISABLED using the REAL Pressable `disabled` prop AND accessibilityState.disabled AND reduced emphasis, because Phase 28 Card multi-select does not exist yet; it is never a standalone screen and offers no import (D-09/D-10)."
    - "Tapping the disabled Select Contacts row is a true no-op: OverflowMenu's row onPress SHORT-CIRCUITS for a disabled row so it neither calls action.onPress NOR calls close() — the overflow sheet STAYS OPEN and the row reads as non-interactive to AT (addresses cross-AI review MEDIUM — OverflowMenu.tsx:102-105 currently fires close() unconditionally)."
    - "Reset Dashboard View calls resetDashboardView(getExecutor()) + clearSession() with NO confirmation dialog and restores Active Contacts / no filters / Default sort / cleared search while PRESERVING the List/Card viewMode (DASHC-10 / §O)."
    - "Reset persistence-failure path (addresses cross-AI review CYCLE-4 #4): `onReset` is a HANDLED async — it awaits resetDashboardView(getExecutor()) and, on rejection (the store setters can reject BEFORE state changes, dashboard-query-store.ts:93), surfaces a themed non-blocking error (theme tokens only, CLAUDE.md) and does NOT run clearSession() (so persisted query state and the ephemeral session never diverge into a partial reset), never leaving an unhandled promise rejection; on success it clears the session search. Because OverflowAction.onPress returns void (OverflowMenu.tsx:28), `onReset` is a synchronous `() => void` that invokes the handled async internally (its own try/catch) — consistent with the Plan 01/02 persistence-failure pattern."
    - "Removed from the pre-four-tab-shell overflow: Your Week (→ header), Backup/Restore, Orrery, Settings (all bottom-nav tabs, ADR-080). Never present: Manage Favorites (ADR-075/D-06) and any contact-import entry (D-10)."
    - "The overflow-actions array is produced by a PURE buildDashboardOverflowActions({ navigation, onReset }) builder taking a SINGLE OBJECT param (node-tested), so the 5-row set + disabled Select is asserted off-device (DASHC-08). The object-param signature is the settled contract (addresses cross-AI review CYCLE-2 LOW — no `(navigation)`-positional vs reset-callback ambiguity); onReset is the reset callback the screen supplies (resetDashboardView(getExecutor()) + clearSession())."
    - "The Not-yet-contacted + Unbound footer entries removed from HomeScreen are replaced by the Not Contacted population (Population panel, D-07) and the Unbound overflow entry (D-05) respectively — this closes the real footer self-navigation dead-end (the Not-yet-contacted footer entry currently self-navigates to Home, HomeScreen.tsx:410) with no navigation dead-ends left. (Re-pointed from the untraceable 'D-14' label per cross-AI review fact-drift; 26-CONTEXT.md defines only D-01..D-12.)"
    # --- edge-probe lift: DASHC-02 ---
    - "Two header destinations always both present; label-vs-icon-only is chosen by fit, never dropping a destination (empty/zero-one-many)."
    - "The two destinations render in fixed order (Your Week then Group Events); order is stable (ordering)."
    - statement: "Header equality: when both destinations are present at equal priority they sit side-by-side without overlap/merge at the supported width (adjacency)."
      verification: backstop
    - statement: "Header labels compare by grapheme/rendered width under OS scaling — the icon-only fallback triggers on rendered fit, not byte length (encoding)."
      verification: backstop
    # --- edge-probe lift: DASHC-08 ---
    - "buildDashboardOverflowActions returns exactly 5 rows in the fixed order, Select Contacts flagged disabled, when two adjacent identical routes would collide the intentional Group Events header+overflow redundancy is preserved (adjacency)."
    - "The overflow set is fixed (never empty, never variable-count); an empty/undefined nav still yields the 5 labelled rows (empty)."
    - "Row order is deterministic and stable across renders (ordering)."
    # --- edge-probe lift: DASHC-10 ---
    - "Reset from a default (already-reset) state is a no-op that still yields Active/none/Default/cleared and preserves viewMode (adjacency/idempotency)."
    - "Reset with no populations/filters/search active still clears session + returns Active without error (empty)."
    - "Reset restores axes to a deterministic canonical state regardless of prior selection order (ordering)."
    # --- UI-SPEC ## UI Considerations lift (S3/S4) ---
    - "S3: both destinations present; label vs icon-only by fit (D-04) — covered above."
    - "S4: the overflow is a fixed 5-row set that never scrolls/grows (overflow); Select Contacts is a first-class disabled row, not a dead tap (partial)."
    - statement: "S3: at large OS text scale both header destinations fall back to icon-only rather than wrapping/shrinking/displacing the control row."
      verification: backstop
  artifacts:
    - src/screens/dashboard-overflow-actions.ts
    - src/screens/dashboard-overflow-actions.test.ts
    - "OverflowMenu.tsx :: OverflowAction gains optional disabled field + disabled render/no-op"
    - "ShellAppBar.tsx :: measures the full bar (root + title width) and exposes a `compact` trailing-fit signal (trailing widened to accept a `compact`-aware render form, backward-compatible) — owns the D-04 icon-only fit decision"
  key_links:
    - "HomeScreen ShellAppBar trailing → two header destinations (Your Week/Group Events) via Icon(your-week/group-events)"
    - "HomeScreen overflow ← buildDashboardOverflowActions({ navigation, onReset }) → ShellAppBar overflow → OverflowMenu (disabled Select Contacts)"
    - "Reset Dashboard View → resetDashboardView(getExecutor()) + useDashboardSessionStore.clearSession()"
  prohibitions:
    - "MUST NOT add a Manage Favorites overflow entry or any ranked-favourites affordance (ADR-075 / D-06)."
    - "MUST NOT add a contact-import entry to Select Contacts or the overflow (D-10)."
    - "MUST NOT enable Select Contacts or route it to a standalone screen in Phase 26 — it stays disabled until Phase 28 Card multi-select lands (D-09)."
    - "MUST NOT show a confirmation dialog for Reset Dashboard View — it is non-destructive and applies immediately (§O)."
    - "MUST NOT let a header label wrap, shrink below role size, or push the control/search rows down — icon-only is the only fallback (D-04)."
---

<objective>
Refit the Dashboard's header and overflow chrome for the four-tab shell (ADR-080): two co-equal header destinations (Your Week + Group Events) with an icon-only fallback (DASHC-02/D-04), and the amended 5-row overflow (Group Events / Unbound / Archived / Select Contacts [disabled] / Reset) with a non-destructive Reset (DASHC-08/DASHC-10). This edits HomeScreen's chrome only — it does not touch the control row or search (Plan 01/07 own those).

Purpose: Land the header/overflow half of DASHC-01's hierarchy, enforce the D-05/D-06/D-09/D-10 overflow decisions, and close the footer navigation dead-end (the Not-yet-contacted footer entry self-navigates to Home — a real gap, re-pointed from the untraceable 'D-14' label per review fact-drift).
Output: a pure `buildDashboardOverflowActions` builder + test, an `OverflowMenu` disabled state, and the refit HomeScreen chrome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md
@.planning/phases/26-dashboard-control-surface/26-01-SUMMARY.md

@src/components/OverflowMenu.tsx
@src/components/ShellAppBar.tsx
@src/components/icons/Icon.tsx
@src/stores/dashboard-session-store.ts
@src/screens/unbound-list-logic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverflowMenu disabled state + pure buildDashboardOverflowActions builder + node test</name>
  <read_first>
    - src/components/OverflowMenu.tsx (OverflowAction {label,onPress,accessibilityLabel?,testID?}; the Modal action sheet at 66-115 — add a disabled render path here)
    - src/screens/HomeScreen.tsx:107-150 (the CURRENT overflowActions array to replace — note the testIDs to keep: dashboard-group-events-overflow-entry, dashboard-archived-overflow-entry)
    - src/screens/unbound-list-logic.ts (the pure helper-module idiom for the new builder file)
    - src/stores/dashboard-query-store.ts:93-105 (resetDashboardView(exec)) + src/stores/dashboard-session-store.ts:26 (clearSession)
  </read_first>
  <files>src/components/OverflowMenu.tsx, src/screens/dashboard-overflow-actions.ts, src/screens/dashboard-overflow-actions.test.ts</files>
  <action>
    Extend `OverflowAction` in OverflowMenu.tsx with an optional `disabled?: boolean`. When true: (1) render the row with reduced emphasis (textSecondary label via useTheme, never a colour literal); (2) set the REAL Pressable `disabled` prop AND `accessibilityState={{ disabled: true }}`; (3) MODIFY the row onPress handler at OverflowMenu.tsx:102-105 so it SHORT-CIRCUITS for a disabled row — it must NOT call `close()` and must NOT call `action.onPress()` (currently `close()` fires unconditionally, which would dismiss the whole sheet on a disabled tap). Net effect: tapping a disabled row does nothing and the sheet STAYS OPEN. Keep every existing prop/testID behavior + the unconditional close()+onPress()+refocus flow for NON-disabled rows unchanged.
    Create `dashboard-overflow-actions.ts` exporting a PURE `buildDashboardOverflowActions({ navigation, onReset })` — a SINGLE OBJECT param (settled signature, addresses cross-AI review CYCLE-2 LOW: no positional-`navigation` vs reset-callback ambiguity) — that returns the amended ordered OverflowAction[] with the exact labels/testIDs/state: Group Events (dashboard-group-events-overflow-entry → navigation.navigate("GroupEvents")), Unbound Contacts (dashboard-unbound-overflow-entry → navigation.navigate("UnboundContacts")), Archived Contacts (dashboard-archived-overflow-entry → navigation.navigate("Archived")), Select Contacts (dashboard-select-contacts-entry, disabled:true, onPress no-op), Reset Dashboard View (dashboard-reset-view-entry → `onReset`). `onReset` is supplied by the screen (resetDashboardView(getExecutor())+clearSession()) so the builder stays pure and node-testable. Do NOT include Your Week / Backup / Orrery / Settings / Manage Favorites / any import row.
    Create `dashboard-overflow-actions.test.ts` (vitest node): call the builder directly with the OBJECT param (`buildDashboardOverflowActions({ navigation: <stub>, onReset: <spy> })`) and assert the array is length 5, in the fixed order, with the exact testIDs, Select Contacts disabled===true, NO 'Manage Favorites'/'Import'/'Settings'/'Backup'/'Orrery'/'Your Week' label present, and that the Reset row's onPress invokes the passed `onReset` spy (proving the object-param wiring).
  </action>
  <verify>
    <automated>npx vitest run src/screens/dashboard-overflow-actions.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `buildDashboardOverflowActions` takes a SINGLE OBJECT param `{ navigation, onReset }` (grep the signature), is pure (no react-native import), and its test (called with the object param) asserts the 5-row order + disabled Select + forbidden-label absence + that the Reset row invokes the passed `onReset`.
    - `OverflowAction.disabled` renders reduced-emphasis + the REAL Pressable `disabled` prop + accessibilityState.disabled; the OverflowMenu row onPress short-circuits for a disabled row so NEITHER `close()` NOR `action.onPress()` runs (grep OverflowMenu.tsx: the `onPress` handler guards on `action.disabled` before `close()`).
    - `tsc --noEmit` + `check:colors` green.
  </acceptance_criteria>
  <done>A node-tested pure overflow builder yields the amended 5-row set with a disabled Select Contacts, and OverflowMenu supports a disabled row.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen header destinations — Your Week + Group Events with icon-only fallback</name>
  <read_first>
    - src/screens/HomeScreen.tsx:533-553 (the current ShellAppBar trailing single Group Events Pressable + styles 640-656 to replace) — note this is the post-tracer HomeScreen from Plan 01
    - src/components/ShellAppBar.tsx:42-66 (the trailing slot renders before the overflow; ShellAppBar owns the root flex row — title flex:1, trailing, overflow — so it, not HomeScreen, must own the measured fit decision; this file is now in files_modified)
    - src/components/icons/Icon.tsx + icon-registry.ts (your-week / group-events semantic names added in Plan 01)
    - .planning/phases/26-dashboard-control-surface/26-UI-SPEC.md "Header Destinations" table + "Responsive fallback contract"
  </read_first>
  <files>src/components/ShellAppBar.tsx, src/screens/HomeScreen.tsx</files>
  <action>
    **Move the fit decision into ShellAppBar (addresses cross-AI review CYCLE-3 MEDIUM):** ShellAppBar (now in files_modified) owns the root flex row (title flex:1 / trailing / overflow / padding, ShellAppBar.tsx:42-66), so HomeScreen alone cannot know whether title + two labels + overflow can coexist — it only sees its trailing content's post-flex allocated width. Extend ShellAppBar to measure the full bar (its root width via onLayout + the rendered title width via onTextLayout) and expose a `compact` trailing-fit signal to its trailing content — e.g. widen `trailing` to accept `ReactNode | ((opts: { compact: boolean }) => ReactNode)` (backward-compatible: existing ReactNode callers — variant='child' Archived/Unbound — are unchanged; only HomeScreen passes the function form). The fit decision MUST come from the full-bar measurement, NOT a HomeScreen-local trailing measurement. **MANDATE an intrinsic-width method (addresses cross-AI review CYCLE-4 #2):** since icon-only is the pre-measurement DEFAULT the expanded labels are never in the tree to measure, so the `compact` decision compares the available trailing space against a MEASURED-INTRINSIC expanded-label width obtained from EITHER (a) a hidden/invisible OFF-SCREEN label probe measured once via onTextLayout (rendered off-screen / at zero opacity so it never visibly flashes), OR (b) a reserved trailing-width budget the compact decision is computed against. A plain 'measure the currently-rendered bar and compare' approach is NOT viable and is forbidden here — with icon-only the default there is no rendered expanded label to compare against, so that path would leave the header stuck compact or reflowing.
    Replace the EXISTING single Group Events trailing Pressable (HomeScreen.tsx:539, testID `dashboard-group-events-entry`) with a trailing region (passed via the `compact`-aware trailing render) holding TWO co-equal destinations: Your Week (Icon name="your-week", visible label "Your Week" when not compact, onPress navigation.navigate("Digest"), testID dashboard-your-week-entry, accessibilityLabel "Your Week") and Group Events (Icon name="group-events", visible label "Group Events" when not compact, onPress navigation.navigate("GroupEvents"), testID dashboard-group-events-entry, accessibilityLabel "Group Events"). The new Group Events destination REUSES the existing `dashboard-group-events-entry` testID — grep-verify it appears EXACTLY ONCE in the trailing region afterward (no orphaned/duplicate old Pressable), and remove the now-dead single-Pressable styles (the old trailing styles ~640-656). Implement the D-04 fallback via the ShellAppBar `compact` signal: when `compact` (both labels cannot stay readable at the current width/OS text scale) render BOTH icon-only (labels hidden but accessibilityLabel retained); never wrap, never shrink below the Label role size, never push the control row down. **Icon-only is the MANDATED fail-safe DEFAULT before the first measurement lands (addresses cross-AI review CYCLE-3 MEDIUM):** render icon-only until ShellAppBar's measurement confirms the labels fit, THEN expand to icon+label (expand-if-room) — NEVER render labels first and collapse, so there is no first-paint label→collapse flash. The fallback MUST be driven by MEASURED layout (onLayout/onTextLayout), NOT a character-count or nominal-width heuristic (text scaling + localization make char/nominal inference unreliable); the control row's vertical position must be invariant, including across the first measurement frame. Every colour/glyph via tokens + the semantic Icon (no raw Ionicons, no colour literal). Each destination keeps a real 44px touch target (hitSlop where needed).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: both destinations show icon+label at default scale and route to Digest / GroupEvents; at 200% OS font scale both fall back to icon-only, the header does not wrap, and the control row does not move (DASHC-02 backstop). Because icon-only is the MANDATED pre-measurement default (labels only appear after the fit measurement confirms room — expand-if-room, never label-then-collapse), confirm on COLD launch and on an OS-scale change that there is NO label→collapse flash and the control row never visibly moves, including on the first measurement frame (addresses cross-AI review CYCLE-3 MEDIUM — the fail-safe default replaces the earlier conditional "gate if a flash is observed").</human-check>
  </verify>
  <acceptance_criteria>
    - testIDs dashboard-your-week-entry + dashboard-group-events-entry both present in the trailing region; both use the semantic Icon (grep: no raw `Ionicons`/glyph string in HomeScreen for these).
    - `grep -c "dashboard-group-events-entry" src/screens/HomeScreen.tsx` returns exactly 1 (the old single Pressable is replaced, not duplicated); the dead trailing-Pressable styles are removed.
    - The icon-only fallback is driven by a MEASURED-INTRINSIC label width, not a hardcoded character/width constant and not a compare against currently-rendered content (cross-AI review CYCLE-4 #2): the fit test compares available trailing space against EITHER a hidden off-screen onTextLayout label probe OR a reserved trailing-width budget (grep ShellAppBar.tsx for the off-screen probe / reserved-budget mechanism; the expanded-label width must NOT be read from the currently-rendered icon-only trailing content).
    - The fit decision is owned in ShellAppBar (cross-AI review CYCLE-3 MEDIUM): `grep -nE "onLayout|onTextLayout|compact" src/components/ShellAppBar.tsx` shows ShellAppBar measures the full bar (root width + title width) and exposes a `compact` trailing-fit signal; the `trailing` prop accepts the function/`compact`-aware form additively, and existing ReactNode consumers (Archived/Unbound variant='child') still compile (`tsc --noEmit`).
    - Icon-only is the mandated pre-measurement default (cross-AI review CYCLE-3 MEDIUM): the header destinations render icon-only until the fit measurement confirms room, then expand — never labels-first-then-collapse (verified by the human-check no-flash observation + the render defaulting to compact before first measure).
    - `tsc --noEmit` + `check:colors` green.
    - On-device: icon+label at normal scale, icon-only at large scale, control row position invariant AND no first-paint header flash/reflow on cold launch or OS-scale change.
  </acceptance_criteria>
  <done>Two co-equal header destinations render with a locked icon-only fallback that never displaces the control row.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the amended overflow + Reset into HomeScreen and remove the footer nav gap</name>
  <read_first>
    - src/screens/HomeScreen.tsx (post-Task-2: the overflowActions array + the footer entries 407-442 — Not-yet-contacted self-navigates to Home (the footer self-nav dead-end, HomeScreen.tsx:410), Unbound → UnboundContacts)
    - src/screens/dashboard-overflow-actions.ts (the builder from Task 1)
    - src/stores/dashboard-query-store.ts (resetDashboardView) + src/stores/dashboard-session-store.ts (clearSession)
  </read_first>
  <files>src/screens/HomeScreen.tsx</files>
  <action>
    Replace HomeScreen's inline overflowActions with `buildDashboardOverflowActions({ navigation, onReset })` (the settled object-param signature), where `onReset` is a HANDLED `() => void` (OverflowAction.onPress returns void, OverflowMenu.tsx:28) that internally awaits `resetDashboardView(getExecutor())` and, on success, calls `useDashboardSessionStore.getState().clearSession()` — NO confirmation dialog. On resetDashboardView REJECTION (the store setters can reject before state changes, dashboard-query-store.ts:93), it surfaces a themed non-blocking error (theme tokens only, CLAUDE.md; mirror the Plan 01/02 persistence-failure pattern) and does NOT call clearSession() (so persisted query state + the ephemeral session do not diverge into a partial reset), never leaving an unhandled promise rejection (addresses cross-AI review CYCLE-4 #4). Pass the result to `ShellAppBar overflow={…}`. Remove the Not-yet-contacted + Unbound footer entries (the Not Contacted population now lives in the Population panel from Plan 01, D-07; Unbound is now the overflow entry, D-05) so no footer route dead-end (the Not-yet-contacted self-nav-to-Home at HomeScreen.tsx:410) remains. Keep the freshness triple, counts, empty-state gate, and the FlatList list intact. Confirm no Manage Favorites / Settings / Backup / Orrery / import entry appears anywhere on the screen.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: open ⋯ → exactly Group Events / Unbound Contacts / Archived Contacts / Select Contacts (greyed) / Reset Dashboard View; tapping the greyed Select Contacts does NOTHING and the sheet STAYS OPEN (does not dismiss); Reset immediately returns to Active/no-filters/Default/cleared-search while the List/Card preference is unchanged; the old footer entries are gone and nothing dead-ends to Home.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "dashboard-not-yet-contacted-entry\|navigation.navigate(\"Home\")" src/screens/HomeScreen.tsx` returns nothing (footer self-nav dead-end removed).
    - `grep -n "buildDashboardOverflowActions" src/screens/HomeScreen.tsx` present; the overflow is sourced from the pure builder.
    - `onReset` is a handled async (cross-AI review CYCLE-4 #4): on resetDashboardView rejection it surfaces a themed non-blocking error and SKIPS clearSession (grep HomeScreen: a try/catch or `.catch` around the awaited `resetDashboardView`, with `clearSession()` only on the resolve path and a themed-error surface on rejection) — no unhandled promise rejection, consistent with the Plan 01/02 pattern.
    - `tsc --noEmit` + `check:colors` + `npm test` green.
    - On-device: 5-row overflow with disabled Select; Reset is confirmation-free and preserves viewMode.
  </acceptance_criteria>
  <done>HomeScreen renders the amended 5-row overflow with a confirmation-free viewMode-preserving Reset and no footer navigation dead-ends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overflow Reset → dashboard-query-store + session store | reset mutates persisted query state + ephemeral session |
| header/overflow → navigation | entry affordances route to child/tab destinations only |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-12 | Tampering | Reset write path | medium | mitigate | resetDashboardView writes through app-settings-dao (validated); clearSession is in-memory only; no raw SQL. |
| T-26-13 | Elevation of function | disabled Select Contacts | medium | mitigate | Disabled row is a no-op (no onPress) + accessibilityState.disabled; cannot route to non-existent Phase 28 mutations (D-09). |
| T-26-14 | Info disclosure | device egress | low | accept | No network; no new emitted setting (D-03). Reset touches only existing app_settings columns. |
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + `check:colors`; Task 1 vitest on the builder; Task 3 `npm test`.
- Pixel backstops: DASHC-02 icon-only header fallback at 200% scale; overflow 5-row set + disabled Select + confirmation-free Reset (26-VALIDATION.md).
</verification>

<success_criteria>
Two co-equal header destinations with a locked icon-only fallback; a pure-builder-sourced 5-row overflow with a disabled Select Contacts and a confirmation-free viewMode-preserving Reset; no Manage Favorites / import / footer dead-ends.
</success_criteria>

<review_feedback_incorporated>
Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 4 (current) findings addressed in this plan:
- **[MEDIUM] header fit-measurement METHOD underspecified** (Codex, CYCLE-4 #2): with icon-only mandated as the pre-measurement default the expanded labels are never rendered, so a 'measure-and-compare the currently-rendered bar' method has nothing to compare against. The plan now MANDATES an intrinsic-width method — a hidden off-screen onTextLayout label probe OR a reserved trailing-width budget — and explicitly forbids the compare-against-rendered-content path; the fit test compares available space against a MEASURED-INTRINSIC (not currently-rendered) label width (must_haves for the responsive fallback + ShellAppBar-owned fit decision, Task 2 action, Task 2 acceptance). Keeps icon-only-until-measured as the fail-safe default — upholds D-04's locked icon-only fallback.
- **[LOW] Reset persistence-failure path unhandled** (Codex, CYCLE-4 #4): `onReset` awaited `resetDashboardView` with no rejection handling. It is now a HANDLED `() => void` (OverflowAction.onPress is void) that internally awaits resetDashboardView and, on rejection, surfaces a themed non-blocking error and SKIPS clearSession (no partial reset), never leaving an unhandled promise — mirroring the Plan 01/02 persistence-failure pattern (new must_have + Task 3 action + Task 3 acceptance). Robustness addition; no decision reversal.

Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 3 (history) findings addressed in this plan:
- **[MEDIUM] measured icon-only header fallback not robustly implementable within HomeScreen.tsx alone + first-render default not mandated** (Codex file-scope + Claude flash, one MEDIUM with two parts): `src/components/ShellAppBar.tsx` is added to `files_modified` + Task 2 `<files>`; ShellAppBar now owns the fit decision (measures the full bar — root + title width — and exposes a `compact` trailing-fit signal to its trailing content, additively/backward-compatibly), because ShellAppBar.tsx:42-66 owns the title/trailing/overflow flex layout that HomeScreen cannot measure from the trailing content alone. AND icon-only-until-measured is now the MANDATED fail-safe default (expand-if-room), replacing the earlier conditional "gate if a flash is observed" human-check, so the label→collapse flash cannot occur (must_haves + Task 2 read_first/action/human-check/acceptance + artifacts). Upholds D-04's locked icon-only fallback.

Cross-AI review CYCLE 2 (history — addressed in commit 957aa8c, retained):
- **[LOW] settle the pure builder signature** (Codex): `buildDashboardOverflowActions({ navigation, onReset })` is the single settled OBJECT-param contract everywhere (must_have + Task 1 action/acceptance/test + Task 3 action) — no `(navigation)`-positional vs reset-callback ambiguity; the test calls it with the object param and asserts onReset is invoked by the Reset row.
- **[LOW] first-paint header flash/reflow** (Claude): Task 2 human-check now specifically watches for a cold-launch / OS-scale-change first-frame flash of the measured-layout header (renders-then-measures), with a stated remedy (gate the label render until the first measure lands) so the control row never visibly moves (Task 2 human-check + acceptance).

Cross-AI review CYCLE 1 (history — addressed in commit 1866d6d, retained):
- **[MEDIUM] disabled Select Contacts under-specified** (both reviewers): the row sets the REAL Pressable `disabled` prop + accessibilityState.disabled, and OverflowMenu's onPress short-circuits for a disabled row so it calls NEITHER `close()` NOR `action.onPress()` — the sheet stays open (must_haves + Task 1 action/acceptance + Task 3 human-check).
- **[MEDIUM] responsive header fallback heuristic** (Codex): the icon-only fallback is driven by MEASURED layout (onLayout/onTextLayout), not a character/nominal-width heuristic (must_have + Task 2 action/acceptance).
- **[MEDIUM] stale header testID** (Claude): Task 2 grep-verifies `dashboard-group-events-entry` appears exactly once and removes the dead trailing-Pressable styles.
- **[MEDIUM] D-14 citation untraceable** (orchestrator fact-drift): all D-14 references re-pointed to the real footer self-navigation dead-end (HomeScreen.tsx:410, Not-yet-contacted → Home), closed by D-07 (Not Contacted population) + D-05 (Unbound overflow entry).
</review_feedback_incorporated>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-04-SUMMARY.md` when done.
</output>
