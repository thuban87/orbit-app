---
phase: 26-dashboard-control-surface
plan: 05
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/screens/ArchivedContactsScreen.tsx
autonomous: true
requirements: [DASHC-09]
estimate:
  tokens: 44000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "ArchivedContactsScreen presents as a first-class Dashboard child route: its hand-rolled Back Pressable + raw 24/700 title are replaced by ShellAppBar variant='child' title='Archived', whose Back routes through resolveBackIntent/dismissTop (origin-aware return) (DASHC-09 / D-05)."
    - "Both entry points — the Dashboard overflow (Plan 04) and the Settings row — route to this ONE screen (registered in DashboardStack and SettingsStack); the refactor keeps both registrations (D-05)."
    - "Origin-aware return holds: Dashboard → Archived → Profile → Back → Archived (the native per-tab stack owns this; ADR-019 stack-root assumption superseded by ADR-080)."
    - "The ADR-018 archive-before-purge two-stage gate is preserved verbatim: confirmPurge native destructive Alert, purgeBody copy, computeImpact/impactSummaryLines, the POST-COMMIT photo+notification cleanup fan-out, and the in-app danger-token 'Delete permanently' trigger are byte-unchanged; this phase introduces NO new destructive action."
    - "Every existing testID is preserved (archived-contacts-screen, archived-count, archived-empty, archived-row-{id}, archived-restore-{id}, archived-delete-{id}); the Back control keeps an accessible label."
    # --- UI-SPEC ## UI Considerations lift (S6) ---
    - "S6 loading: the local listArchived read has no network; an error shows the existing themed load-failure alert (retained)."
    - "S6 populated: count header + per-row Restore / Delete-permanently (ADR-018 two-stage) retained."
    - "S6 empty: the existing 'No archived contacts' empty state is retained through the refactor."
    - "S6 error: the existing read-failure + purge-failure alerts are retained and never claim success on failure."
  artifacts:
    - "src/screens/ArchivedContactsScreen.tsx (chrome migrated to ShellAppBar variant='child')"
  key_links:
    - "ArchivedContactsScreen header → ShellAppBar variant='child' → resolveBackIntent/dismissTop (system + app-bar Back agree)"
    - "Route registrations unchanged: DashboardStack.Screen 'Archived' + SettingsStack 'Archived' → one screen (D-05)"
  prohibitions:
    - "MUST NOT alter the ADR-018 purge flow (confirmPurge/purgeBody/computeImpact/POST-COMMIT cleanup/danger trigger) — reversing or weakening it is an owner decision, not a refactor (STOP AND ASK if a change seems required)."
    - "MUST NOT greenfield-reimplement Archived logic or its DAOs — refactor + reuse only (dossier §N)."
    - "MUST NOT remove either the overflow or the Settings entry point — both route to one screen (D-05)."
    - "MUST NOT introduce a colour literal; danger/onDanger stay the only destructive tokens (check:colors green)."
---

<objective>
Re-present the existing `ArchivedContactsScreen` as a first-class Dashboard child route (DASHC-09 / D-05): swap its hand-rolled Back + raw fonts for the shared `ShellAppBar variant="child"` and Phase 23 tokens, while preserving the ADR-018 archive-before-purge gate and every testID verbatim. Chrome-only refactor — no logic, DAO, or destructive-flow change.

Purpose: Make Archived read as a child route (not a legacy Settings sub-screen) with origin-aware Back, without touching its purge substance.
Output: a chrome-migrated ArchivedContactsScreen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md

@src/screens/ArchivedContactsScreen.tsx
@src/components/ShellAppBar.tsx
@src/navigation/back-intent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ArchivedContactsScreen chrome to ShellAppBar variant='child'; preserve ADR-018 verbatim</name>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx (WHOLE FILE — the header 180-196 to replace; the purge flow confirmPurge 58-74, purgeBody 84-87, doPurge 128-172, the danger trigger 242-252 to PRESERVE verbatim; every testID)
    - src/components/ShellAppBar.tsx:20-67 (variant='child' gives Back via resolveBackIntent/dismissTop + a title)
    - src/screens/UnboundContactsScreen.tsx (sibling child route being refactored in Plan 06 — mirror the chrome approach for consistency)
  </read_first>
  <files>src/screens/ArchivedContactsScreen.tsx</files>
  <action>
    Replace the hand-rolled header View (the `archived-back` Pressable + the raw 24/700 title) with `<ShellAppBar variant="child" title="Archived" />` at the top of the screen, keeping the ScrollView content below it. Remove the now-dead header styles (backBtn/title/header) but keep the rest of the StyleSheet. Do NOT change the ScrollView root background token usage, the count header, the empty state, the row list, Restore, or ANY part of the purge flow (confirmPurge, purgeBody, computeImpact/impactSummaryLines, purgeContact + the POST-COMMIT photo/notification cleanup, the danger-token trigger). Preserve every testID exactly (archived-contacts-screen, archived-count, archived-empty, archived-row-{id}, archived-restore-{id}, archived-delete-{id}). Do NOT touch route registrations (Archived stays in both DashboardStack and SettingsStack). Every colour via useTheme().colors.*.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: reach Archived from BOTH the Dashboard overflow and the Settings row → same screen with a ShellAppBar child Back; Dashboard → Archived → tap a row → Profile → Back → returns to Archived; Restore works; Delete permanently still shows the native destructive confirm with the ADR-018 impact body.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "ShellAppBar" src/screens/ArchivedContactsScreen.tsx` present with variant="child" title="Archived"; `grep -n "archived-back" src/screens/ArchivedContactsScreen.tsx` returns nothing (hand-rolled Back removed).
    - `git diff` shows the purge functions (confirmPurge/purgeBody/doPurge/computeImpact/purgeContact call) UNCHANGED; all six testIDs still present.
    - `tsc --noEmit` + `check:colors` + `npm test` green.
    - On-device: both entry points → one screen; origin-aware Back; purge confirm intact.
  </acceptance_criteria>
  <done>Archived is a ShellAppBar child route with origin-aware Back, both entry points intact, and the ADR-018 purge flow preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archived screen → purge-dao/contacts-dao | destructive purge + reversible restore cross into SQLite |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-15 | Tampering | ADR-018 purge gate | high | mitigate | The archive-before-purge two-stage gate + native destructive confirm are preserved verbatim (git-diff-verified unchanged); chrome-only refactor. |
| T-26-16 | Repudiation | accidental purge | medium | accept | Purge remains behind the two-stage gate + explicit destructive Alert; no new one-tap destructive path added. |
</threat_model>

<verification>
- `npx tsc --noEmit` + `check:colors` + `npm test`.
- Pixel: dual entry points → one screen; origin-aware Back; purge confirm intact (26-VALIDATION.md DASHC-09).
</verification>

<success_criteria>
ArchivedContactsScreen reads as a first-class child route via ShellAppBar with origin-aware Back and both entry points, with the ADR-018 purge flow and all testIDs preserved verbatim.
</success_criteria>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-05-SUMMARY.md` when done.
</output>
