---
phase: 26-dashboard-control-surface
plan: 03
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
autonomous: true
requirements: [DASHC-07]
estimate:
  tokens: 74000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A NEW additive read listDashboardSearch(exec, query: DashboardQueryState, term: string, now: string) is authored in dashboard-read.ts; it composes the current Population + Filters universe with a search term and returns DashboardRow[] with the SAME card projection (CASE-wrapped status/progress, ranked fuel line, term-only snippet) as listDashboardPopulation (D-12)."
    - "It PRESERVES the recorded A3 search-scope semantics (dashboard-read.ts:38 header): a present term relaxes the never-contacted/snooze exclusions of the implicit Active default to archived-only, while the no-term list keeps the full exclusion — copied verbatim from legacy listDashboard branch 1 (dashboard-read.ts:396-413); the semantics are neither silently narrowed nor widened (D-12, recorded owner decision)."
    - "It stays BOUND-ONLY (DASHBOARD_BOUND_WHERE / tracking_enabled=1) so Unbound contacts never surface in Dashboard search (D-13 leak stays closed; Unbound retrieval is D-08's separate child-route search)."
    - "It does NOT add a term param to, or otherwise fork, listDashboardPopulation (no layer-1 fork — D-11/D-12); listDashboardPopulation is left byte-unchanged."
    - "Injection invariant holds (V5): only closed SQL constants are interpolated; every runtime value — the escaped term (escapeLike + LIKE ? ESCAPE), category ids, battery values — is ?-bound; the snippet subquery bind is pushed FIRST because it sits in the SELECT list."
    - "The now arg is validated to start YYYY-MM-DD (reuse the localMidnightFromReadNow guard) and throws otherwise; callers pass localDateTime(), never toISOString()."
    # --- edge-probe-adjacent behavior authored as tests ---
    - "An empty/whitespace term returns [] (the read is the search path only; the no-term list is listDashboardPopulation's job) — empty-input behavior is explicit and node-tested."
    - "escapeLike escapes backslash-then-%-then-_ so a literal %/_ in the term matches only rows literally containing it; a name-only match still returns the row with snippet null; a fuel match returns the snippet regardless of a concurrent name match."
  artifacts:
    - "src/db/dashboard-read.ts :: listDashboardSearch (new export)"
    - "src/db/dashboard-read.test.ts :: listDashboardSearch describe block"
  key_links:
    - "listDashboardSearch composes buildPopulationWhere(query.populations) + buildFilterWhere(query.filters) + the A3 term relaxation; reuses RANKED_FUEL_EXCLUSIONS + escapeLike + CARD_STATUS + FUEL_LINE from the same module"
    - "HomeScreen (Plan 07) will call listDashboardSearch for the hasTerm path and retire legacy listDashboard"
  prohibitions:
    - "MUST NOT silently narrow or widen the A3 search-scope semantics — they are a recorded owner decision (D-12); if the correct composition of Population+Filters WITH the A3 relaxation is ambiguous for any case, STOP AND ASK rather than pick silently."
    - "MUST NOT add a term param to listDashboardPopulation or fork it (D-11 layer-1)."
    - "MUST NOT surface Unbound (tracking_enabled=0) or archived rows in this read (D-13 / DASHQ-08)."
    - "MUST NOT interpolate any identifier or runtime value into SQL — closed constants only, every value ?-bound (V5)."
---

<objective>
Author the D-12 population-aware Dashboard search read — the additive Phase-26 read that lets the migrated Dashboard search box work in the Phase 25 query world without forking `listDashboardPopulation` and without a dual-read. This plan authors and tests the read ONLY (the HomeScreen wiring + legacy-read retirement is Plan 07); it does not touch any screen.

Purpose: Close the search-read seam gap (RESEARCH O-1 → D-12) with a node-tested read that composes population + filters + term and preserves the recorded A3 search-scope semantics verbatim.
Output: `listDashboardSearch` in `dashboard-read.ts` + a comprehensive `describe` block in `dashboard-read.test.ts`.

## Flagged Assumption (surfaced, not silently resolved)
- **FA-D12-compose:** D-12 says "composes Population + Filters + a search term and preserves the A3 semantics (term relaxes never-contacted/snooze exclusions to archived-only)." The faithful interpretation this plan encodes: when populations is EMPTY (implicit Active default) a term relaxes that default's never-contacted/snooze exclusion to archived-only + BOUND (the verbatim A3 branch, dashboard-read.ts:404-412); when populations is EXPLICIT, apply buildPopulationWhere(populations) + buildFilterWhere(filters) + the name-OR-fuel term predicate (the explicit populations already govern never-contacted/snooze inclusion). Filters (buildFilterWhere) AND across the term in both cases. **If any case makes this composition ambiguous, the executor STOPS AND ASKS (D-12 is a recorded owner decision — do not silently change scope).**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md

@src/db/dashboard-read.ts
@src/logic/dashboard-query-logic.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN): author listDashboardSearch preserving A3 semantics, with its failing-first test suite</name>
  <read_first>
    - src/db/dashboard-read.ts (WHOLE FILE — the chokepoint header §SEARCH SCOPE (A3) at :38, listDashboardPopulation 274-336, legacy listDashboard branch 1 at 396-413 for the verbatim A3 term mechanics, CARD_STATUS/FUEL_LINE/CARD_FAVOURITE_RANK/CARD_FROM, escapeLike/RANKED_FUEL_EXCLUSIONS imports, localMidnightFromReadNow 256-272, DASHBOARD_BOUND_WHERE)
    - src/logic/dashboard-query-logic.ts (buildPopulationWhere, buildFilterWhere, ACTIVE_SEGREGATION_WHERE, resolveDefaultSort, POPULATION_SORT usage)
    - src/db/dashboard-read.test.ts (the existing harness: the in-memory exec + seed helpers; the "listDashboard — search" describe at 696 is the A3 behavioral oracle to port assertions from)
    - src/db/fuel-read.ts (escapeLike, RANK_CASE, RANKED_FUEL_EXCLUSIONS — reuse, never re-derive)
  </read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts</files>
  <behavior>
    - term present + populations empty (Active default): returns non-archived BOUND rows matching name LIKE OR eligible-fuel EXISTS, INCLUDING never-contacted and currently-snoozed people (A3 relaxation to archived-only); archived excluded; Unbound excluded.
    - term present + explicit populations (e.g. favourites): scoped to buildPopulationWhere(populations) AND the name-OR-fuel term predicate AND buildFilterWhere(filters); still archived-excluded + BOUND-only.
    - empty/whitespace term → [].
    - name-only match → row present, snippet null; fuel match → snippet populated even when the name also matches (mirrors legacy MEDIUM-6).
    - literal %/_ in the term (via escapeLike) matches only rows literally containing them.
    - a never-contacted matched row reads status=null/progress=null (CARD_STATUS CASE wrap), never 'stable'.
    - now not starting YYYY-MM-DD throws.
  </behavior>
  <action>
    First write the failing `describe("listDashboardSearch — population-aware search + A3 scope", …)` block in `dashboard-read.test.ts` encoding every behavior above (port the A3 assertions from the legacy "listDashboard — search" block as the oracle), then implement `listDashboardSearch(exec, query, term, now)` in `dashboard-read.ts` to pass it. Reuse the module's CARD_STATUS / FUEL_LINE / CARD_FAVOURITE_RANK / CARD_FROM and the escapeLike / RANKED_FUEL_EXCLUSIONS imports; compose buildPopulationWhere + buildFilterWhere from dashboard-query-logic; copy the A3 term mechanics VERBATIM from legacy branch 1 (snippet subquery bind pushed first; `c.name LIKE ? ESCAPE` OR eligible-fuel EXISTS; archived-only relaxation for the Active default). Sort via the same resolveDefaultSort → POPULATION_SORT the population read uses. Do NOT touch listDashboardPopulation, listDashboard, or listNeverContacted. Trim/guard empty term → []. Validate `now` via the existing localMidnightFromReadNow guard (or an equivalent leading-YYYY-MM-DD assertion).
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - New `describe("listDashboardSearch …")` block exists and passes; it asserts A3 relaxation (term finds never-contacted + snoozed non-archived), archived exclusion, BOUND-only (no Unbound), empty-term [], snippet rules, and null-status for never-contacted matches.
    - `listDashboardPopulation` and `listDashboard` bodies are byte-unchanged (git diff shows additions only around the new function + reused helpers).
    - `grep -n "tracking_enabled = 1\|DASHBOARD_BOUND_WHERE" src/db/dashboard-read.ts` confirms the new read is bound-only.
    - No new SQL interpolation of a runtime value (every value ?-bound); `tsc --noEmit` + `check:colors` green.
  </acceptance_criteria>
  <done>listDashboardSearch exists, is node-tested green, preserves A3 verbatim, stays bound-only, and does not fork the population read.</done>
</task>

<task type="auto">
  <name>Task 2 (REFACTOR + injection/ordering hardening): parity + edge tests, no dual behavior drift</name>
  <read_first>
    - src/db/dashboard-read.test.ts (the new block from Task 1 + the existing fuelText-parity + null-progress-ordering blocks 847-895 to mirror)
    - src/db/dashboard-read.ts (the new listDashboardSearch + FUEL_LINE parity contract in the module header)
  </read_first>
  <files>src/db/dashboard-read.test.ts</files>
  <action>
    Extend the test block with: (1) the fuelText parity assertion (the search read's ranked fuel line equals getRankedFuel[0].text, reusing the module's parity oracle); (2) ordering stability — equal-progress rows tie-break on `c.name COLLATE NOCASE, c.id`; (3) an injection guard — a term containing `%`, `_`, `\`, and `'` returns only literal matches and does not error (proving ?-binding + escapeLike); (4) a filters-composition case — an explicit category/battery filter AND-composes with the term. Do NOT change dashboard-read.ts in this task unless a test surfaces a genuine bug (if so, note it and fix minimally). Keep vitest node-env.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Parity, ordering-tiebreak, injection-literal, and filters-composition cases pass.
    - `npm test` (full suite) is green — no existing block regressed.
  </acceptance_criteria>
  <done>listDashboardSearch is covered for parity, deterministic ordering, injection-literal safety, and filter composition; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search term (user text) → listDashboardSearch SQL | untrusted free text crosses into a LIKE scan |
| persisted populations/filters → listDashboardSearch SQL | persisted query state scopes the read |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Tampering | search term → SQL | high | mitigate | escapeLike escapes \\ % _ then the term is ?-bound with `LIKE ? ESCAPE '\\'`; no term text is interpolated. Test (3) proves literal %/_/\\/' handling. |
| T-26-09 | Tampering | populations/filters → SQL | high | mitigate | buildPopulationWhere/buildFilterWhere interpolate only closed constants and ?-bind ids/values (Phase 25); reused, not re-derived. |
| T-26-10 | Info disclosure | scope leak (Unbound/archived) | high | mitigate | Read is archived-excluded + BOUND-only; test asserts no Unbound/archived row surfaces (D-13/DASHQ-08). |
| T-26-11 | Info disclosure | A3 scope drift | high | mitigate | A3 semantics copied verbatim + asserted; FA-D12-compose STOP-AND-ASK trip-wire for any ambiguous case. |
</threat_model>

<verification>
- Per task: `npx vitest run src/db/dashboard-read.test.ts` + `tsc --noEmit` + `check:colors`; Task 2 also `npm test`.
- This is pure data-layer logic — fully unit-testable in the node env (no on-device UAT for the read itself; wiring is Plan 07).
</verification>

<success_criteria>
A node-tested `listDashboardSearch` composes population + filters + term, preserves the recorded A3 search-scope semantics verbatim, stays bound-only/archived-excluded, does not fork `listDashboardPopulation`, and is injection-safe.
</success_criteria>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-03-SUMMARY.md` when done.
</output>
