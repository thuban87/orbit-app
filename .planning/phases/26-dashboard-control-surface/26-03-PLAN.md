---
phase: 26-dashboard-control-surface
plan: 03
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
autonomous: true
requirements: [DASHC-07]
estimate:
  tokens: 74000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A NEW additive read listDashboardSearch(exec, query: DashboardQueryState, term: string, now: string) is authored in dashboard-read.ts; it composes the current Population + Filters universe with a search term and returns DashboardRow[] with the SAME card projection (CASE-wrapped status/progress, ranked fuel line, term-only snippet) as listDashboardPopulation (D-12)."
    - "It REPLICATES listDashboardPopulation's FULL non-SQL post-processing so search agrees with the visible Population/Filter controls (addresses cross-AI review HIGH): (a) when populations includes 'birthdays' it resolves birthday candidate IDs + a birthdayDays map via listBirthdayCandidates + daysUntilBirthday (dashboard-read.ts:279-291) and passes birthdayIds into BOTH buildPopulationWhere AND populationMatchColumns — WITHOUT this an explicit Birthdays population yields 0 rows (buildPopulationWhere returns '0', dashboard-query-logic.ts:194); (b) it applies the filterByGravity post-query pass (dashboard-read.ts:324-335) so a Gravity filter is honored in search, not silently ignored (buildFilterWhere emits no gravity SQL, dashboard-query-logic.ts:113); (c) it ports the soonest-birthday post-query JS sort (dashboard-read.ts:315-322) using the birthdayDays map."
    - "The preferred implementation extracts a SHARED PRIVATE helper (birthday-id resolution + WHERE composition + resolved-sort + soonest-birthday post-sort + filterByGravity) that BOTH listDashboardPopulation and listDashboardSearch call, so the two reads cannot drift; listDashboardPopulation's PUBLIC CONTRACT (its exported signature — NO term param) is unchanged, and its existing 'Phase 25 Active universe' test block stays green as the parity oracle. Extracting a private helper is de-duplication, NOT a fork (D-11 forbids adding a term to / forking listDashboardPopulation, not sharing internals)."
    - "It PRESERVES the recorded A3 search-scope semantics (dashboard-read.ts:38 header): a present term relaxes the never-contacted/snooze exclusions of the implicit Active default to archived-only, while the no-term list keeps the full exclusion — copied verbatim from legacy listDashboard branch 1 (dashboard-read.ts:396-413); the semantics are neither silently narrowed nor widened (D-12, recorded owner decision)."
    - "It stays BOUND-ONLY (DASHBOARD_BOUND_WHERE / tracking_enabled=1) so Unbound contacts never surface in Dashboard search (the DASHQ-08 Unbound-in-search leak stays closed per ADR-062; Unbound retrieval is D-08's separate child-route search)."
    - "It does NOT add a term param to, or otherwise fork, listDashboardPopulation (no layer-1 fork — D-11/D-12); listDashboardPopulation's exported signature and public behavior are unchanged (its body MAY call the shared private helper above)."
    - "Injection invariant holds (V5): only closed SQL constants are interpolated; every runtime value — the escaped term (escapeLike + LIKE ? ESCAPE), category ids, battery values — is ?-bound; the snippet subquery bind is pushed FIRST because it sits in the SELECT list."
    - "The now arg is validated to start YYYY-MM-DD (reuse the localMidnightFromReadNow guard) and throws otherwise; callers pass localDateTime(), never toISOString()."
    # --- edge-probe-adjacent behavior authored as tests ---
    - "An empty/whitespace term returns [] (the read is the search path only; the no-term list is listDashboardPopulation's job) — empty-input behavior is explicit and node-tested."
    - "escapeLike escapes backslash-then-%-then-_ so a literal %/_ in the term matches only rows literally containing it; a name-only match still returns the row with snippet null; a fuel match returns the snippet regardless of a concurrent name match."
  artifacts:
    - "src/db/dashboard-read.ts :: listDashboardSearch (new export)"
    - "src/db/dashboard-read.test.ts :: listDashboardSearch describe block"
  key_links:
    - "listDashboardSearch composes buildPopulationWhere(query.populations) + buildFilterWhere(query.filters) + the A3 term relaxation; reuses RANKED_FUEL_EXCLUSIONS + escapeLike + CARD_STATUS + FUEL_LINE from the same module"
    - "HomeScreen (Plan 07) will call listDashboardSearch for the hasTerm path and retire legacy listDashboard"
  prohibitions:
    - "MUST NOT silently narrow or widen the A3 search-scope semantics — they are a recorded owner decision (D-12); if the correct composition of Population+Filters WITH the A3 relaxation is ambiguous for any case, STOP AND ASK rather than pick silently."
    - "MUST NOT add a term param to listDashboardPopulation or fork it into a divergent read (D-11 layer-1). Extracting a SHARED PRIVATE helper both reads call is explicitly PERMITTED and preferred (it is de-duplication, not a fork)."
    - "MUST NOT surface Unbound (tracking_enabled=0) or archived rows in this read (closes the DASHQ-08 Unbound-in-search leak; ADR-062)."
    - "MUST NOT interpolate any identifier or runtime value into SQL — closed constants only, every value ?-bound (V5)."
---

<objective>
Author the D-12 population-aware Dashboard search read — the additive Phase-26 read that lets the migrated Dashboard search box work in the Phase 25 query world without forking `listDashboardPopulation` and without a dual-read. This plan authors and tests the read ONLY (the HomeScreen wiring + legacy-read retirement is Plan 07); it does not touch any screen.

Purpose: Close the search-read seam gap (RESEARCH O-1 → D-12) with a node-tested read that composes population + filters + term and preserves the recorded A3 search-scope semantics verbatim.
Output: `listDashboardSearch` in `dashboard-read.ts` + a comprehensive `describe` block in `dashboard-read.test.ts`.

## Flagged Assumption (surfaced, not silently resolved)
- **FA-D12-compose:** D-12 says "composes Population + Filters + a search term and preserves the A3 semantics (term relaxes never-contacted/snooze exclusions to archived-only)." The faithful interpretation this plan encodes: when populations is EMPTY (implicit Active default) a term relaxes that default's never-contacted/snooze exclusion to archived-only + BOUND (the verbatim A3 branch, dashboard-read.ts:404-412); when populations is EXPLICIT, apply buildPopulationWhere(populations, {birthdayIds}) + buildFilterWhere(filters) + the name-OR-fuel term predicate (the explicit populations already govern never-contacted/snooze inclusion), THEN the filterByGravity post-pass and the resolved sort (incl. soonest-birthday). Filters (buildFilterWhere) AND across the term in both cases. **If any case makes this composition ambiguous, the executor STOPS AND ASKS (D-12 is a recorded owner decision — do not silently change scope).** NOTE (addresses cross-AI review HIGH): the FA-D12-compose STOP-AND-ASK covers A3-relaxation *ambiguity* only — it does NOT excuse omitting the mechanical birthday-id / gravity / soonest-birthday post-processing, which is REQUIRED (see must_haves), not optional. Birthdays+term and gravity+term must behave exactly as they do in listDashboardPopulation; there is no "simplify birthdays+term to status sort" fallback — the soonest-birthday sort is ported.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md

@src/db/dashboard-read.ts
@src/logic/dashboard-query-logic.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN): author listDashboardSearch preserving A3 semantics, with its failing-first test suite</name>
  <read_first>
    - src/db/dashboard-read.ts (WHOLE FILE — the chokepoint header §SEARCH SCOPE (A3) at :38, listDashboardPopulation 274-336 INCLUDING its birthday-id resolution 279-291, the soonest-birthday post-sort 315-322, and the filterByGravity post-pass 324-335 (all THREE must be replicated), legacy listDashboard branch 1 at 396-413 for the verbatim A3 term mechanics, CARD_STATUS/FUEL_LINE/CARD_FAVOURITE_RANK/CARD_FROM, escapeLike/RANKED_FUEL_EXCLUSIONS imports, localMidnightFromReadNow 256-272, DASHBOARD_BOUND_WHERE, listBirthdayCandidates + daysUntilBirthday, populationMatchColumns, filterByGravity + getImpactInputs)
    - src/logic/dashboard-query-logic.ts (buildPopulationWhere — note it returns '0' for birthdays when birthdayIds is empty, :194; buildFilterWhere — emits NO gravity SQL, :113; ACTIVE_SEGREGATION_WHERE, resolveDefaultSort — returns 'soonest-birthday' for a lone birthdays population, POPULATION_SORT usage)
    - src/db/dashboard-read.test.ts (the existing harness: the in-memory exec + seed helpers; the "listDashboard — search" describe at 696 is the A3 behavioral oracle to port assertions from)
    - src/db/fuel-read.ts (escapeLike, RANK_CASE, RANKED_FUEL_EXCLUSIONS — reuse, never re-derive)
  </read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts</files>
  <behavior>
    - term present + populations empty (Active default): returns non-archived BOUND rows matching name LIKE OR eligible-fuel EXISTS, INCLUDING never-contacted and currently-snoozed people (A3 relaxation to archived-only); archived excluded; Unbound excluded.
    - term present + explicit populations (e.g. favourites): scoped to buildPopulationWhere(populations, {birthdayIds}) AND the name-OR-fuel term predicate AND buildFilterWhere(filters); still archived-excluded + BOUND-only.
    - term present + populations=['birthdays']: birthday candidate IDs are resolved (listBirthdayCandidates + daysUntilBirthday, 0..30 days) and passed into buildPopulationWhere + populationMatchColumns, so ONLY upcoming-birthday contacts that ALSO match the term return (NOT 0 rows); with default sort the rows are ordered soonest-birthday (birthdayDays post-sort), NOT status.
    - term present + filters.gravity=['deep'] (or any tier): the filterByGravity post-query pass runs, so ONLY contacts in the selected tier(s) survive — a matching contact in another tier is excluded (gravity is NOT silently ignored).
    - empty/whitespace term → [].
    - name-only match → row present, snippet null; fuel match → snippet populated even when the name also matches (mirrors legacy MEDIUM-6).
    - literal %/_ in the term (via escapeLike) matches only rows literally containing them.
    - a never-contacted matched row reads status=null/progress=null (CARD_STATUS CASE wrap), never 'stable'.
    - now not starting YYYY-MM-DD throws.
  </behavior>
  <action>
    First write the failing `describe("listDashboardSearch — population-aware search + A3 scope", …)` block in `dashboard-read.test.ts` encoding every behavior above (port the A3 assertions from the legacy "listDashboard — search" block as the oracle, AND add the birthdays+term, gravity+term, and soonest-birthday-sort cases), then implement `listDashboardSearch(exec, query, term, now)` in `dashboard-read.ts` to pass it.
    **Replicate listDashboardPopulation's full post-processing (addresses cross-AI review HIGH):** (a) resolve birthday candidate IDs + a birthdayDays map exactly as listDashboardPopulation:279-291 (via listBirthdayCandidates + daysUntilBirthday, days in 0..30) when populations includes 'birthdays', and pass birthdayIds into BOTH buildPopulationWhere(populations, {birthdayIds}) AND populationMatchColumns(populations, birthdayIds); (b) after the SQL, apply the soonest-birthday post-query JS sort (dashboard-read.ts:315-322) when the resolved sort is 'soonest-birthday', using the birthdayDays map; (c) then apply the filterByGravity post-query pass (dashboard-read.ts:324-335) when query.filters.gravity is non-empty, returning only surviving ids.
    **Preferred structure — shared private helper:** extract a private helper both listDashboardPopulation and listDashboardSearch call, encapsulating birthday-id resolution + WHERE composition (buildPopulationWhere + buildFilterWhere) + resolvedSort/populationMatchColumns + the soonest-birthday post-sort + filterByGravity; the two public reads differ only in the term predicate / snippet SELECT and the A3 relaxation. Preserve listDashboardPopulation's EXPORTED SIGNATURE (no term param) and keep its existing 'Phase 25 Active universe' test block green as the parity oracle. If a clean shared helper is impractical, FULLY duplicate the birthday/gravity/sort behavior and add a parity test — never ship the search read missing any of the three.
    Reuse the module's CARD_STATUS / FUEL_LINE / CARD_FAVOURITE_RANK / CARD_FROM and the escapeLike / RANKED_FUEL_EXCLUSIONS imports; copy the A3 term mechanics VERBATIM from legacy branch 1 (snippet subquery bind pushed first; `c.name LIKE ? ESCAPE` OR eligible-fuel EXISTS; archived-only relaxation for the Active default). Sort via the same resolveDefaultSort → POPULATION_SORT the population read uses. Do NOT change listDashboardPopulation's public behavior; do NOT touch listDashboard or listNeverContacted. Trim/guard empty term → []. Validate `now` via the existing localMidnightFromReadNow guard (or an equivalent leading-YYYY-MM-DD assertion).
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - New `describe("listDashboardSearch …")` block exists and passes; it asserts A3 relaxation (term finds never-contacted + snoozed non-archived), archived exclusion, BOUND-only (no Unbound), empty-term [], snippet rules, and null-status for never-contacted matches.
    - **Birthdays+term** case passes: populations=['birthdays'] + a term returns ONLY upcoming-birthday candidates matching the term (NOT [] and NOT the full list), ordered soonest-birthday under default sort.
    - **Gravity+term** case passes: filters.gravity=[tier] + a term excludes a term-matching contact in a different tier (filterByGravity is applied, not skipped).
    - `listDashboardPopulation`'s EXPORTED SIGNATURE is unchanged and it gains no term param; its existing 'Phase 25 Active universe' describe block still passes unchanged (parity oracle). If a shared private helper was extracted, listDashboardPopulation's observable behavior is identical (that block proves it); `listDashboard` remains untouched.
    - `grep -n "tracking_enabled = 1\|DASHBOARD_BOUND_WHERE" src/db/dashboard-read.ts` confirms the new read is bound-only; `grep -n "filterByGravity\|listBirthdayCandidates\|populationMatchColumns" src/db/dashboard-read.ts` confirms the search path reuses the post-processing (not a stripped copy).
    - No new SQL interpolation of a runtime value (every value ?-bound); `tsc --noEmit` + `check:colors` green.
  </acceptance_criteria>
  <done>listDashboardSearch exists, is node-tested green, preserves A3 verbatim, replicates the birthday-id / gravity / soonest-birthday post-processing (shared helper preferred), stays bound-only, and does not fork the population read's public contract.</done>
</task>

<task type="auto">
  <name>Task 2 (REFACTOR + injection/ordering hardening): parity + edge tests, no dual behavior drift</name>
  <read_first>
    - src/db/dashboard-read.test.ts (the new block from Task 1 + the existing fuelText-parity + null-progress-ordering blocks 847-895 to mirror)
    - src/db/dashboard-read.ts (the new listDashboardSearch + FUEL_LINE parity contract in the module header)
  </read_first>
  <files>src/db/dashboard-read.test.ts</files>
  <action>
    Extend the test block with: (1) the fuelText parity assertion (the search read's ranked fuel line equals getRankedFuel[0].text, reusing the module's parity oracle); (2) ordering stability — equal-progress rows tie-break on `c.name COLLATE NOCASE, c.id`; (3) an injection guard — a term containing `%`, `_`, `\`, and `'` returns only literal matches and does not error (proving ?-binding + escapeLike); (4) a filters-composition case — an explicit category/battery filter AND-composes with the term; (5) **birthday post-sort parity** — for a seeded set of upcoming-birthday contacts that ALL match a term, the soonest-birthday order returned by listDashboardSearch equals the order listDashboardPopulation returns for the same candidates (proves the ported post-sort is not a stub); (6) **gravity parity** — with a gravity tier selected, the surviving id set from listDashboardSearch equals the id set listDashboardPopulation keeps for the same rows (proves filterByGravity runs identically). Do NOT change dashboard-read.ts in this task unless a test surfaces a genuine bug (if so, note it and fix minimally). Keep vitest node-env.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Parity (fuelText, birthday post-sort, gravity), ordering-tiebreak, injection-literal, and filters-composition cases pass.
    - `npm test` (full suite) is green — no existing block regressed (incl. the preserved 'listDashboardPopulation — Phase 25 Active universe' block).
  </acceptance_criteria>
  <done>listDashboardSearch is covered for parity (incl. birthday sort + gravity), deterministic ordering, injection-literal safety, and filter composition; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search term (user text) → listDashboardSearch SQL | untrusted free text crosses into a LIKE scan |
| persisted populations/filters → listDashboardSearch SQL | persisted query state scopes the read |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Tampering | search term → SQL | high | mitigate | escapeLike escapes \\ % _ then the term is ?-bound with `LIKE ? ESCAPE '\\'`; no term text is interpolated. Test (3) proves literal %/_/\\/' handling. |
| T-26-09 | Tampering | populations/filters → SQL | high | mitigate | buildPopulationWhere/buildFilterWhere interpolate only closed constants and ?-bind ids/values (Phase 25); reused, not re-derived. |
| T-26-10 | Info disclosure | scope leak (Unbound/archived) | high | mitigate | Read is archived-excluded + BOUND-only; test asserts no Unbound/archived row surfaces (closes the DASHQ-08 Unbound-in-search leak; ADR-062). |
| T-26-11 | Info disclosure | A3 scope drift | high | mitigate | A3 semantics copied verbatim + asserted; FA-D12-compose STOP-AND-ASK trip-wire for any ambiguous case. |
</threat_model>

<verification>
- Per task: `npx vitest run src/db/dashboard-read.test.ts` + `tsc --noEmit` + `check:colors`; Task 2 also `npm test`.
- This is pure data-layer logic — fully unit-testable in the node env (no on-device UAT for the read itself; wiring is Plan 07).
</verification>

<success_criteria>
A node-tested `listDashboardSearch` composes population + filters + term, replicates listDashboardPopulation's birthday-id / gravity / soonest-birthday post-processing (shared helper preferred), preserves the recorded A3 search-scope semantics verbatim, stays bound-only/archived-excluded, does not fork `listDashboardPopulation`'s public contract, and is injection-safe.
</success_criteria>

<review_feedback_incorporated>
Cross-AI review 2026-09-05 (26-REVIEWS.md) findings addressed in this plan:
- **[HIGH] search read omits listDashboardPopulation post-processing** (both reviewers + orchestrator): listDashboardSearch now MUST replicate (a) birthday-id resolution + populationMatchColumns, (b) the filterByGravity post-pass, (c) the soonest-birthday post-sort — encoded in must_haves, Task 1 behavior/action, and dedicated birthdays+term / gravity+term tests. The Claude HIGH on the resolveDefaultSort/soonest-birthday interaction is the same gap and is covered by (c) + the birthday post-sort parity test — the sort is PORTED, not simplified to status.
- **[MEDIUM] shared factoring discouraged by 'byte-unchanged'** (Codex): the must_have/acceptance now permit and prefer a shared private helper both reads call, preserving only listDashboardPopulation's PUBLIC contract; the 'Phase 25 Active universe' test block is the parity oracle.
- **[MEDIUM] D-13 citation untraceable** (orchestrator fact-drift): all D-13 references re-pointed to the real DASHQ-08 Unbound-in-search leak / ADR-062 (26-CONTEXT.md defines only D-01..D-12).
</review_feedback_incorporated>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-03-SUMMARY.md` when done.
</output>
