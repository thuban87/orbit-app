---
phase: 26-dashboard-control-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/control-surface/anchor-position.ts
  - src/components/control-surface/anchor-position.test.ts
  - src/components/control-surface/control-summary.ts
  - src/components/control-surface/control-summary.test.ts
  - src/components/control-surface/control-labels.ts
  - src/components/control-surface/control-labels.test.ts
  - src/components/icons/icon-registry.ts
  - src/components/control-surface/AnchoredPanel.tsx
  - src/components/control-surface/DashboardOverlayHost.tsx
  - src/components/control-surface/PopulationPanelContent.tsx
  - src/components/control-surface/DashboardControlRow.tsx
  - src/screens/HomeScreen.tsx
  - src/stores/dashboard-query-store.ts
  - src/stores/dashboard-query-store.test.ts
autonomous: true
requirements: [DASHC-01, DASHC-03, DASHC-04, DASHC-05, DASHC-06]
estimate:
  tokens: 118000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    # --- Goal-backward (Population control end-to-end) ---
    - "Tapping the Population control opens an anchored floating panel (in-tree, NOT an RN Modal) positioned below the control row and hosted by a ROOT-LEVEL DashboardOverlayHost (sibling to ShellAppBar + FlatList inside HomeScreen's root View — NOT rendered inside the FlatList ListHeaderComponent); the dashboard list stays visible behind a light FULL-SURFACE scrim and visibly re-queries when a population is toggled (DASHC-04). (Addresses cross-AI review CYCLE-2 HIGH: an overlay mounted inside the FlatList header cannot scrim the sibling app bar, cannot catch an outside tap there, and scrolls with the list.)"
    - "The panel + scrim are rendered by the root-level DashboardOverlayHost (in-tree, non-Modal, D-11 — a root overlay host is still in the React tree); DashboardControlRow measures each trigger via measureInWindow and reports {anchorRect (window space), size, panel content, onDismiss} to the host over a lightweight in-tree channel (a tiny dashboard-panel store OR lifted HomeScreen state — Claude's discretion), so the scrim/dismiss/inert scope covers the ENTIRE dashboard surface and the panel anchors correctly regardless of list scroll; exactly one panel is hosted at a time (DASHC-04/05, upholds D-11)."
    - "Selecting/deselecting a population applies immediately with no Apply/Done step; a live selection that yields zero rows leaves the panel open and shows the Phase 25 cause-aware empty state behind it (DASHC-04)."
    - "While the panel is open EVERY Dashboard element except the panel, the full-surface scrim, and the control-row triggers is interaction-inert (pointerEvents) AND removed from accessibility focus (importantForAccessibility='no-hide-descendants') — this INCLUDES the header region (title, Your Week + Group Events destinations, the ⋯ overflow trigger), the search/toggle row, the footer, and the contact collection; the root-level DashboardOverlayHost renders the full-surface scrim + panel ABOVE all of them, and only the panel + scrim + control-row triggers are focusable/interactive (DASHC-05). The cycle-1 inert wrappers (background regions) and the cycle-2 root-level scrim are CONSISTENT, not contradictory: the wrappers inert/a11y-hide the background; the host's scrim covers-and-dismisses over the WHOLE surface incl. the sibling app bar; the control-row triggers sit ABOVE the scrim in z-order so re-tap dismiss + one-tap control switching still work."
    - "The open panel dismisses on re-tap of its control, on an outside/scrim tap ANYWHERE on the surface (incl. over the app bar, since the scrim is root-level and full-surface), and on Android system Back — Back dismisses the panel BEFORE any route navigation via shellTransientStore/resolveBackIntent; only one panel is open at a time (DASHC-05)."
    - "HomeScreen reads its population list through listDashboardPopulation(getExecutor(), query, localDateTime()) — the Phase 25 population read — with query from useDashboardQueryStore, hydrated on mount; it no longer reads the retired useDashboardPrefs AsyncStorage store (D-11 layer-1 consume-only)."
    - "The Population panel lists All Contacts as an ordinary selectable row; Active Contacts is the implicit unlisted default and deselecting the last explicit population returns to Active (buildPopulationWhere returns ACTIVE_SEGREGATION_WHERE for empty selection) (DASHC-06 / D-07)."
    - "PopulationPanelContent (layer 2) takes only {state,onChange} props: it imports neither AnchoredPanel internals (positioning/scrim/animation) nor app-settings-dao; onChange calls setPopulations(getExecutor(), …) (presentation seam D-11)."
    - "All dashboard control copy (population row names + the axis default labels 'Active Contacts' / 'Filters' / 'Default') is rendered from a SINGLE source-of-truth label module src/components/control-surface/control-labels.ts (British copy sourced from the UI-SPEC); panels + summaries render these labels, never re-author them. Plan 02 EXTENDS the same module with filter-family/option + sort-mode labels and Plan 07 reads population names from it for empty-state copy (addresses cross-AI review CYCLE-2 MEDIUM — one label source, not strings re-authored across panels/summaries/empty-states)."
    - "Panel enter/exit animates via Reanimated withTiming(MOTION.base) and collapses to instant when useReducedMotion() is true; the animation PAUSES/settles to its final value when useIsFocused()===false OR AppState is not 'active' (CLAUDE.md non-negotiable — all animation pauses on unfocus + background; mirror the OrreryScreen focus+AppState derivation at OrreryScreen.tsx:287-295); no React-state-per-frame and no Skia (CLAUDE.md). (Addresses cross-AI review CYCLE-2 MEDIUM.)"
    # --- Edge-probe lift: DASHC-03 (control-summary pure) ---
    - "collapseSummary(names,maxShown): when names.length === maxShown exactly, all names show joined with no '+N' appended at the boundary (adjacency)."
    - "collapseSummary([], maxShown) returns '' so the caller substitutes the axis default label ('Active Contacts' for Population) (empty)."
    - "collapseSummary counts array ELEMENTS (population names), never string length; a multi-byte/grapheme name counts as one element (encoding)."
    - "collapseSummary preserves input order via names.slice(0,maxShown); equal/duplicate names are not reordered (ordering)."
    # --- Edge-probe lift: DASHC-04 (anchor-position pure) ---
    - "clampAnchorPosition constrains horizontal geometry independently (NOT a re-feed of its own output, which is type-invalid — input is {x,y,width,height}, output is {top,left,width}): the returned `left` always lies within `[gutter, viewport.width - width - gutter]`, `width === min(panelSize.width, viewport.width - 2*gutter)` regardless of anchorRect.x, and feeding an anchorRect whose `x` already equals a previously-clamped `left` reproduces that same `left` (a fixed point over the horizontal axis, not over the full result type) (idempotency; addresses cross-AI review CYCLE-3 LOW)."
    - statement: "Opening a second control while a panel is open dismisses the first then opens the second, so exactly one AnchoredPanel is registered in shellTransientStore at any time (DASHC-04 concurrency)."
      verification: backstop
    # --- Edge-probe lift: DASHC-06 (population) ---
    - "Deselecting the last explicit population immediately returns to the implicit Active Contacts default (adjacency)."
    - "With nothing selected the Population summary reads 'Active Contacts' and the list shows the Active universe (empty)."
    - "Population rows render in the fixed UI-SPEC order All Contacts, Favourites, Birthdays, Not Contacted, Snoozed and that order is stable across reopen (ordering)."
    # --- UI-SPEC ## UI Considerations lift (S1/S2 surfaces) ---
    - "S1: the Population control summary shows current-axis names with '+N' collapse (populated); 0 selected → 'Active Contacts', 1 → the name, many → names then '+N' (zero-one-many)."
    - "S2: selected panel rows carry accent + a redundant non-colour selected affordance + accessibilityState.selected/checked — colour is never the only channel (populated/selection)."
    - "S2: panel content hydrates synchronously from the in-memory dashboard-query-store (local app_settings) — no async spinner/loading state (loading)."
    - "S2: while open, ALL background content (header destinations + ⋯ overflow, search/toggle row, footer, collection) is interaction-inert AND removed from a11y focus; only panel + scrim + the control-row triggers are focusable (partial)."
    - "onChange persistence-failure path: PopulationPanelContent.onChange awaits setPopulations and, on rejection, retains the prior selection (no optimistic drift) and surfaces a themed non-blocking error — never an unhandled promise (DASHC-06 robustness; addresses cross-AI review MEDIUM)."
    - "The dashboard-query-store guards against a hydration race: `hydrate()` captures the store generation on entry, every setter (setPopulations/setFilters/setSort/setViewMode/resetDashboardView) bumps that generation, and a late-resolving `hydrate()` applies its parsed snapshot ONLY when the generation is unchanged (otherwise it drops its `set(...)` and only marks the store hydrated). Consequently, opening the Population control and toggling a population before hydration settles can NEVER be overwritten by the older persisted snapshot (addresses cross-AI review CYCLE-3 MEDIUM — dashboard-query-store.ts:64-92 currently has no generation/hydrated guard)."
    - statement: "S1: a long population/category summary collapses to '+N' (not mid-word ellipsis) under OS font scaling."
      verification: backstop
    - statement: "S2: many populations still fit the compact/medium panel; content scrolls within the capped height and a reopened panel begins at its top."
      verification: backstop
  artifacts:
    - src/components/control-surface/anchor-position.ts
    - src/components/control-surface/control-summary.ts
    - src/components/control-surface/control-labels.ts
    - src/components/control-surface/AnchoredPanel.tsx
    - src/components/control-surface/DashboardOverlayHost.tsx
    - src/components/control-surface/PopulationPanelContent.tsx
    - src/components/control-surface/DashboardControlRow.tsx
    - "icon-registry.ts entries: filter, sort, list, grid, your-week, group-events, chevron-down"
  key_links:
    - "DashboardControlRow (measureInWindow anchor + panel content) → dashboard-panel channel (tiny store / lifted state) → root-level DashboardOverlayHost → AnchoredPanel → PopulationPanelContent → onChange → setPopulations(getExecutor()) → app-settings-dao (assertDashboardPopulations guard) → SQLite app_settings"
    - "DashboardOverlayHost (root-level, sibling to ShellAppBar + FlatList) renders the full-surface scrim + AnchoredPanel; open/close → shellTransientStore.openTransient/closeTransient → resolveBackIntent/dismissTop (system Back + app-bar Back + active-tab retap)"
    - "HomeScreen panelOpen state → importantForAccessibility + pointerEvents inert wrappers on the app-bar region + the FlatList (background); control-row triggers kept outside the inert wrappers + above the scrim"
    - "HomeScreen → listDashboardPopulation(exec, useDashboardQueryStore query, localDateTime()) — replaces useDashboardPrefs + legacy listDashboard population path"
  prohibitions:
    - "MUST NOT render the scrim or panel from inside the FlatList ListHeaderComponent / DashboardControlRow subtree — the scrim + panel MUST be hosted by a ROOT-LEVEL DashboardOverlayHost sibling to ShellAppBar + FlatList so the scrim covers the whole surface and catches an outside tap over the app bar (cross-AI review CYCLE-2 HIGH; upholds D-11 in-tree non-Modal)."
    - "MUST NOT drive the panel animation from React setState per frame, and MUST NOT use Skia for the panel — Reanimated shared values only (CLAUDE.md non-negotiable). The panel animation MUST pause/settle on useIsFocused()===false and AppState background (CLAUDE.md)."
    - "MUST NOT reuse RN Modal / BaseOverlay for the anchored panel — a Modal renders in a separate native window that breaks one-tap control switching and the readable live-updating background (Pitfall 1 / D-11)."
    - "MUST NOT add a permanent birthday banner/module — Birthdays is a Population row only (ADR-076 / D-07)."
    - "MUST NOT fork or add a term param to listDashboardPopulation, and MUST NOT write app-settings-dao directly from a layer-2 content component (D-11 / D-12 layer-1)."
    - "MUST NOT introduce a colour literal (incl. in any Reanimated/scrim style); every colour resolves through useTheme().colors.* and check:colors stays green."
---

<objective>
Prove the Dashboard Control Surface architecture end-to-end on its riskiest surface: the **Population control**, wired through all three presentation-seam layers (D-11) and the Phase 25 query store, replacing the old AsyncStorage prefs store on `HomeScreen`. This is the tracer — one control, one path, production-quality — so an architectural dead-end (in-tree overlay vs RN Modal, background a11y-hide, old→new store migration) surfaces after one commit, not ten.

Purpose: De-risk the AnchoredPanel primitive, the presentation seam, and the highest-risk piece of the phase (RESEARCH: the HomeScreen old→new store migration) before any expansion. Implements D-04-adjacent hierarchy start (DASHC-01), the anchored-panel behavior (DASHC-04/05), the Population panel (DASHC-06/D-07), and the pure `+N` summary + anchor-position helpers (DASHC-03/04).

Ground truth (D-01, D-02): the phase dossier (`docs/dossier/milestone-2/phase-05-dashboard-control-surface-dossier-amended-group-events.md`) and its planning-notes are the authoritative decision record and a binding appendix — read them IN FULL before implementing; every REPLAN finding is reflected in these plans and every trip-wire (D-03 migration, D-08 Unbound retrieval, D-11 no layer-1 fork, D-12 A3 semantics) is a STOP-AND-ASK, never a "fix". Reopening any [DECIDED]/[REJECTED] item or reversing an Accepted ADR/HANDOFF entry is an owner decision.
Output: `control-surface/` primitive set (AnchoredPanel + PopulationPanelContent + DashboardControlRow + two pure node-tested helpers), 7 new semantic icon-registry names, and a HomeScreen migrated onto `useDashboardQueryStore` + `listDashboardPopulation`.

## Artifacts this phase produces (phase-level index for the convergence source-grounding pass)

New symbols created across Phase 26 (exclude these from drift verification — they are new, not existing-drift):
- `src/components/control-surface/anchor-position.ts` → `clampAnchorPosition(anchorRect, panelSize, viewport, gutter)` → `{ top, left, width }` (pure).
- `src/components/control-surface/control-summary.ts` → `collapseSummary(names: string[], maxShown: number): string` (pure).
- `src/components/control-surface/control-labels.ts` → the single source-of-truth dashboard control-copy label map (population names + axis defaults; extended by Plan 02 with filter/sort labels) + `.test.ts` (pure).
- `src/components/control-surface/AnchoredPanel.tsx` → `AnchoredPanel` (layer 3 container — the HUD swap point) + its size-variant prop.
- `src/components/control-surface/DashboardOverlayHost.tsx` → `DashboardOverlayHost` (root-level, in-tree, non-Modal host for the full-surface scrim + AnchoredPanel — the DASHC-04/05 overlay scope; Plan 01).
- `src/components/control-surface/PopulationPanelContent.tsx` → `PopulationPanelContent` (layer 2).
- `src/components/control-surface/FilterPanelContent.tsx` → `FilterPanelContent` (layer 2, Plan 02).
- `src/components/control-surface/SortPanelContent.tsx` → `SortPanelContent` (layer 2, Plan 02).
- `src/components/control-surface/DashboardControlRow.tsx` → `DashboardControlRow` (triggers + summaries; measures anchors + reports panel content to the overlay host).
- `src/components/control-surface/filter-summary.ts` → `selectedFilterLabels(filters, categories)` pure summary-label helper + `.test.ts` (Plan 02).
- `src/screens/dashboard-overflow-actions.ts` → `buildDashboardOverflowActions({ navigation, onReset })` pure builder (object param) + `.test.ts` (Plan 04).
- `src/db/dashboard-read.ts` → new `listDashboardSearch(exec, query, term, now)` population-aware search read + a shared private post-processing helper both reads call + a bound-only `countBirthdayPopulation(exec, now)` helper for the Birthdays empty-state count (D-12, Plan 03; consumed by Plan 07).
- `src/screens/unbound-list-logic.ts` → new `filterUnboundByName(rows, term)` + `unboundCountLabel(count, { matching })` mode extension (Plan 06).
- icon-registry keys: `filter`, `sort`, `list`, `grid`, `your-week`, `group-events`, `chevron-down`.
- `OverflowMenu` gains an optional `disabled` per-action field + a disabled-row short-circuit (Plan 04).
- `SegmentedControlOption` gains an optional `icon: IconName` field + filled/outline icon render (Plan 07).
- `dashboard-query-store` gains a hydration-race guard: an internal `generation` counter + a `hydrated` boolean (new fields on the existing store; Plan 01, cross-AI review CYCLE-3).
- `ShellAppBar` gains a measured trailing-fit signal (a `compact` decision passed into its trailing content) so the header icon-only fallback is owned at the app-bar level (Plan 04, cross-AI review CYCLE-3).

## Flagged Assumptions (no silent drop — edge/probe rows that stay unresolved)
- **FA-DASHC-05 (unclassified edge → flagged):** DASHC-05's background-inert + a11y-focus-removal behavior on an in-tree (non-Modal) overlay is not mechanically unit-testable in the render-free vitest env (RESEARCH A4). It is authored as an on-device TalkBack backstop (see must_haves + Task 2 verify) and validated on the Pixel across the FULL background scope (header destinations + ⋯ overflow, search/toggle row, footer, AND the contact collection — not just a contact row); if TalkBack still reaches ANY background element behind an open panel, that is a genuine failure to fix, not an abstain.
- **Named fallback for the a11y-hide mechanism (stated up front, not "fix when found"):** if `importantForAccessibility="no-hide-descendants"` alone proves insufficient on the New Architecture on-device, the pre-decided second line of defense is to ALSO set `accessible={false}` on the background wrappers AND re-assert accessibility focus onto the open panel content via `AccessibilityInfo.setAccessibilityFocus` (the overlay-base focus-on-open pattern already replicated in AnchoredPanel). This contingency is authored into Task 2 so it is not discovered mid-UAT after six downstream plans built on the primitive (addresses cross-AI review MEDIUM).

## Release sequencing note (addresses cross-AI review MEDIUM — search-removal regression)
Plans 01→07 form a NON-RELEASEABLE sequence: this tracer deliberately removes the Dashboard search box, which does not return until Plan 07 wires the D-12 `listDashboardSearch` read. The intermediate commits (after Plan 01, before Plan 07 lands) are integration checkpoints that MUST NOT be cut into a release build — the phase must complete through Wave 3 before any user-facing build ships. This is an in-phase functionality gap, not an architectural gap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md

# Presentation seam + reuse targets (read on disk before editing)
@src/stores/dashboard-query-store.ts
@src/stores/dashboard-session-store.ts
@src/logic/dashboard-query-logic.ts
@src/stores/shell-transient-store.ts
@src/components/ui/overlay-base.tsx
@src/components/ui/GlassSurface.tsx
@src/theme/tokens/motion.ts
@src/theme/tokens/radii.ts
@src/components/icons/icon-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 pure foundations): anchor-position + control-summary + control-labels helpers, their node tests, and the 7 icon-registry names</name>
  <read_first>
    - src/screens/unbound-list-logic.ts (the pure react-native-free helper-module idiom to mirror; node-testable)
    - src/logic/dashboard-query-logic.ts (pure logic-module pattern; the DASHBOARD_POPULATIONS keys/order + DashboardPopulation type — the label module keys off these)
    - .planning/phases/26-dashboard-control-surface/26-UI-SPEC.md (the AUTHORITATIVE user-facing British copy for population names + axis defaults — the label module's source; do NOT invent copy)
    - src/components/icons/icon-registry.ts:33-66 (the ICON_REGISTRY map — add names here ONLY; `search` already exists, do not re-add)
    - src/components/icons/Icon.tsx:36-45 (glyph flows into `<Ionicons name={glyph}/>` — tsc validates every glyph string here)
    - src/theme/tokens/spacing.ts (SPACING.base = 16, the gutter the caller passes into clampAnchorPosition)
  </read_first>
  <files>src/components/control-surface/anchor-position.ts, src/components/control-surface/anchor-position.test.ts, src/components/control-surface/control-summary.ts, src/components/control-surface/control-summary.test.ts, src/components/control-surface/control-labels.ts, src/components/control-surface/control-labels.test.ts, src/components/icons/icon-registry.ts</files>
  <action>
    Create `control-summary.ts` exporting a PURE `collapseSummary(names: string[], maxShown: number): string` with NO react-native import (mirror `unbound-list-logic.ts`): empty array → `""` (caller substitutes the axis default); `names.length <= maxShown` → `names.join(", ")`; otherwise `${names.slice(0,maxShown).join(", ")} +${names.length - maxShown}`. Preserve input order; count elements not characters. Do NOT re-author copy — the caller passes already-localised names from `control-labels.ts`.
    Create `control-labels.ts` (PURE, no react-native import — the SINGLE SOURCE OF TRUTH for dashboard control copy; addresses cross-AI review CYCLE-2 MEDIUM): export a `POPULATION_LABELS: Record<DashboardPopulation, string>` map (British copy from the UI-SPEC — e.g. `all-contacts`→"All Contacts", `favourites`→"Favourites", `birthdays`→"Birthdays", `not-contacted`→"Not Contacted", `snoozed`→"Snoozed") and the axis default labels `AXIS_DEFAULT_LABELS = { population: "Active Contacts", filters: "Filters", sort: "Default" }` (plus a `populationLabel(key)` accessor if convenient). This module is the ONLY place dashboard control copy is authored; PopulationPanelContent + the DashboardControlRow population summary render from it, Plan 02 EXTENDS it (filter families/options + sort modes), Plan 07 reads population names from it for empty-state copy — nothing re-authors these strings inline.
    Create `anchor-position.ts` exporting a PURE `clampAnchorPosition(anchorRect: {x,y,width,height}, panelSize: {width,height}, viewport: {width,height}, gutter: number): {top,left,width}`: top edge is `anchorRect.y + anchorRect.height` (just below the control row); left aligns to `anchorRect.x` but is clamped into `[gutter, viewport.width - width - gutter]`; width is `min(panelSize.width, viewport.width - 2*gutter)`. The horizontal clamp MUST be a fixed point over `left` (an anchorRect whose `x` already equals a previously-returned `left` yields that same `left`); do NOT frame idempotency as re-feeding the `{top,left,width}` output back in as an `{x,y,width,height}` anchor — the types differ, so that is not a valid fixed-point test (cross-AI review CYCLE-3 LOW). No RN import; portrait-locked so no rotation branch.
    Author `control-summary.test.ts`, `anchor-position.test.ts`, and `control-labels.test.ts` (vitest, environment node) covering the edge rows in must_haves: boundary `names.length === maxShown` (no `+N`), empty input `""`, order preservation, element-vs-character counting; the anchor horizontal-clamp fixed point (an anchorRect whose `x` already equals a returned `left` yields the same `left` — NOT re-feeding the {top,left,width} output as an {x,y,width,height} anchor) + both clamp edges (leading-off-screen and trailing-off-screen) + width cap; control-labels: every DashboardPopulation key has a non-empty label and the three axis defaults resolve (guards against a key drifting label-less).
    In `icon-registry.ts` ADD to `ICON_REGISTRY` (map only, D-05) exactly these semantic names with outline/filled Ionicons pairs — glyphs are Claude's discretion but must pass tsc in Icon.tsx: `filter`(filter-outline/filter), `sort`(swap-vertical-outline/swap-vertical), `list`(list-outline/list), `grid`(grid-outline/grid), `your-week`(calendar-outline/calendar), `group-events`(people-outline/people), `chevron-down`(chevron-down/chevron-down). Do NOT touch existing entries; do NOT add `search` (present).
  </action>
  <verify>
    <automated>npx vitest run src/components/control-surface/control-summary.test.ts src/components/control-surface/anchor-position.test.ts src/components/control-surface/control-labels.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `collapseSummary`/`clampAnchorPosition` are exported, pure (no `react-native` import), and their test files pass under vitest node env.
    - `control-labels.ts` is pure (no `react-native` import), exports the population label map + axis defaults, and its test asserts every `DashboardPopulation` key has a label; the module is the single source (grep: PopulationPanelContent + DashboardControlRow import population copy from `control-labels`, no inline population string literals).
    - `tsc --noEmit` is clean, proving the 7 new icon glyph strings are valid Ionicons names in Icon.tsx.
    - `npm run check:colors` is green (no colour literal introduced).
    - `ICON_REGISTRY` contains all 7 new keys and no existing entry was modified (grep: the 7 names present exactly once each).
  </acceptance_criteria>
  <done>Three pure helpers (anchor-position, control-summary, control-labels) exist with passing node tests; 7 semantic icon names are registered and tsc-valid.</done>
</task>

<task type="tracer" tdd="false">
  <name>Task 2 (TRACER): AnchoredPanel + DashboardOverlayHost + PopulationPanelContent + DashboardControlRow (Population only) wired end-to-end through the migrated HomeScreen</name>
  <read_first>
    - src/screens/HomeScreen.tsx (the full screen — the migration target; note useDashboardPrefs at 63/101-104, listDashboard at 193, listCategories fetch at 199 feeding FilterChipRow, FilterChipRow at 360, inline sort row 362-403, the freshness triple 224-266, useBottomClearance 62, useShellRefresh 224, footer 407-442, header trailing 537-551. CRITICAL COMPOSITION FACT: ShellAppBar (:533) and FlatList (:554) are SIBLINGS inside the root View, and the control row is inside FlatList's ListHeaderComponent (:571) — this is exactly why the panel/scrim must NOT mount inside the header)
    - src/stores/dashboard-query-store.ts (hydrate + setters take (exec,value); defaults render pre-hydration)
    - src/logic/dashboard-query-logic.ts:5-11,146-213 (DASHBOARD_POPULATIONS, ACTIVE_SEGREGATION_WHERE, buildPopulationWhere empty→Active)
    - src/db/dashboard-read.ts:274-336 (listDashboardPopulation(exec,query,now) — the read HomeScreen adopts; validates now starts YYYY-MM-DD and throws otherwise)
    - src/db/database.ts (getExecutor + localDateTime — the now arg source; NEVER toISOString)
    - src/stores/shell-transient-store.ts:22-56 (openTransient/closeTransient/dismissTop) + src/navigation/back-intent.ts + src/navigation/RootNavigator.tsx:86-113 (system Back → dismissTop)
    - src/components/ui/overlay-base.tsx:76-84 (the a11y focus-on-open pattern to replicate; SCRIM_OPACITY=0.85 to LIGHTEN)
    - src/components/ui/GlassSurface.tsx (density="dense" panel body; already token-only, RADII.lg corners)
    - src/screens/OrreryScreen.tsx:37,50,287-295,658 (the useIsFocused + AppState 'change' derivation to MIRROR so the panel animation pauses off-focus/background — CLAUDE.md non-negotiable)
    - src/components/control-surface/control-labels.ts (Task 1 — the population labels + axis defaults the panel + summary render)
    - src/logic/dashboard-empty-logic.ts (selectDashboardEmptyState — accepts activeFilter + optional activeFilters/activePopulations/populationCounts + hasTerm)
    - src/theme/use-reduced-motion.ts:122 (useReducedMotion boolean)
  </read_first>
  <files>src/components/control-surface/AnchoredPanel.tsx, src/components/control-surface/DashboardOverlayHost.tsx, src/components/control-surface/PopulationPanelContent.tsx, src/components/control-surface/DashboardControlRow.tsx, src/screens/HomeScreen.tsx, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts</files>
  <action>
    Build `AnchoredPanel.tsx` (LAYER 3, the HUD swap point) as an IN-TREE absolutely-positioned overlay — NOT an RN Modal (Pitfall 1). It is RENDERED BY `DashboardOverlayHost` (below), never mounted inside DashboardControlRow / the FlatList header. Props: `{ anchorRect, size, onDismiss, children }` plus a `visible`/mount contract. Full-screen `Pressable` scrim (`colors.background` at an opacity LOWER than SCRIM_OPACITY 0.85 so the live list stays readable — exact value is Claude's discretion/device-tunable) whose onPress calls `onDismiss` (outside-tap). Position the floating body via `clampAnchorPosition(anchorRect, sizeToDims(size), viewport, SPACING.base)`; body is `GlassSurface density="dense"` (RADII.lg corners). On open call `shellTransientStore.getState().openTransient("dashboard-panel", onDismiss)` and on close `closeTransient("dashboard-panel")` (free Back + active-tab dismiss). Replicate overlay-base's a11y focus-on-open (contentRef + findNodeHandle + AccessibilityInfo.setAccessibilityFocus) — this same `setAccessibilityFocus` re-assertion is also the named fallback (below) for the background-hide mechanism. Animate enter/exit with Reanimated `withTiming(…, { duration: MOTION.base })` mapping EASING.decelerate → `Easing.out(Easing.ease)`; when `useReducedMotion()` is true collapse to instant; AND when `useIsFocused()===false` OR AppState is not 'active' the animation PAUSES/settles to its final value (mirror OrreryScreen.tsx:287-295's `useIsFocused` + AppState 'change' derivation — CLAUDE.md non-negotiable, all animation pauses off-focus/background). Support size variants compact/medium/large (UI-SPEC). NO colour literal; NO Skia; NO setState-per-frame.
    Build `DashboardOverlayHost.tsx` (LAYER 3, the DASHC-04/05 overlay scope — addresses cross-AI review CYCLE-2 HIGH): a ROOT-LEVEL in-tree component (NOT an RN Modal) that HomeScreen renders as a sibling to `ShellAppBar` and `FlatList` (see the HomeScreen migration below), so its full-surface scrim + `AnchoredPanel` cover the ENTIRE dashboard surface incl. the sibling app bar — an overlay mounted inside the FlatList `ListHeaderComponent` physically cannot scrim the app bar, cannot catch an outside tap there, and scrolls with the list. It subscribes to the dashboard-panel channel (a tiny `dashboard-panel` Zustand store OR lifted HomeScreen state — Claude's discretion; the store keeps DashboardControlRow self-contained so Plan 02 adds Filters/Sort triggers WITHOUT touching HomeScreen) and, when a panel is requested, renders `AnchoredPanel` with the reported `{ anchorRect (window space), size, onDismiss }` and the reported panel content as children. When no panel is open it renders nothing (or a zero-cost placeholder). Exactly one panel is hosted at a time (reuse the shellTransientStore single-registration invariant). NO colour literal; NO RN Modal.
    Build `PopulationPanelContent.tsx` (LAYER 2, presentation-agnostic): props `{ state: DashboardQueryState; onChange: (populations: DashboardPopulation[]) => Promise<void> | void }` ONLY — it imports NEITHER AnchoredPanel internals NOR app-settings-dao. Render one tappable row in the FIXED UI-SPEC order — All Contacts, Favourites, Birthdays, Not Contacted, Snoozed — with testIDs `dashboard-population-option-all-contacts`/`-favourites`/`-birthdays`/`-not-contacted`/`-snoozed`. Do NOT derive this order by `.map()`-ing `DASHBOARD_POPULATIONS` (its constant order lists `all-contacts` LAST, dashboard-query-logic.ts:5-11, which differs from the UI order); pin the rows to the UI-SPEC order explicitly (addresses cross-AI review LOW: constant-order mismatch). Active Contacts is NOT a row (implicit default, D-07). Tapping a row toggles that key in the populations array and calls `onChange`; each row carries `accessibilityState={{selected}}` + a redundant non-colour selected affordance (check/filled) + accent treatment via the SegmentedControl filled-accent idiom. Row labels come from `control-labels.ts` (POPULATION_LABELS) — render only, never re-authored inline (single source of truth).
    Persistence-failure path (addresses cross-AI review MEDIUM): `setPopulations` can reject BEFORE Zustand state changes (dashboard-query-store.ts:65-73). The `onChange` wiring in DashboardControlRow MUST `await` the setter and, on rejection, retain the prior selection (no optimistic UI drift) and surface a themed non-blocking error (e.g. a token-styled inline notice / toast) — never leave an unhandled promise rejection. Guard against rapid conflicting presses so a failed write cannot leave the panel and the store disagreeing.
    Build `DashboardControlRow.tsx` (the triggers): in THIS tracer render ONLY the Population trigger (Filters/Sort arrive in Plan 02) with testID `dashboard-population-control`, its label `Population` (Label role) and summary from `collapseSummary(selectedPopulationNames, maxShown)` — where `selectedPopulationNames` come from `control-labels.ts` (POPULATION_LABELS) — substituting the axis default `AXIS_DEFAULT_LABELS.population` ("Active Contacts") when empty. It reads populations from `useDashboardQueryStore`, measures the trigger with `measureInWindow` (WINDOW-space rect), and on open PUBLISHES `{ anchorRect, size, content: <PopulationPanelContent state onChange>, onDismiss }` to the dashboard-panel channel (the tiny store / lifted state) that the root-level `DashboardOverlayHost` consumes — it does NOT render `AnchoredPanel` itself (that would sit inside the FlatList header). `onChange` calls `setPopulations(getExecutor(), next)`. Re-tapping the active trigger, or tapping another trigger, dismisses/switches via the same channel (one panel at a time). Restrained active treatment (accent border/label + non-colour cue) when populations is non-empty; neutral at default. Expose an `onPanelOpenChange?: (open: boolean) => void` prop (HomeScreen drives its inert wrappers off it).
    Add a hydration-race generation guard to `dashboard-query-store.ts` (addresses cross-AI review CYCLE-3 MEDIUM — dashboard-query-store.ts:64-92 has no guard): add an internal monotonic `generation` counter (and a `hydrated: boolean`, initial false); have EVERY setter (setPopulations/setFilters/setSort/setViewMode/resetDashboardView) bump `generation` when it applies its `set(...)`; have `hydrate()` capture the generation at entry (BEFORE the `await getAppSettings`) and, after the read resolves, apply `set(parseStoredState(...))` ONLY if the generation is unchanged — otherwise SKIP the parsed snapshot (a user mutation already wrote the newer state) and only set `hydrated: true`. Net: a late-resolving `hydrate()` can never overwrite a selection the user made before hydration settled. Keep the existing setter await→persist→set ordering and the malformed-prefs try/catch fallback unchanged. Add a `dashboard-query-store.test.ts` case (node) that simulates a slow hydrate resolving AFTER a `setPopulations` and asserts the store retains the user's populations (the stale hydrate snapshot is dropped), and that `hydrated` becomes true either way.
    Migrate `HomeScreen.tsx`: replace `useDashboardPrefs` with `useDashboardQueryStore` (call `hydrate(getExecutor())` once on mount; defaults render pre-hydration). Replace the population read with `listDashboardPopulation(exec, query, localDateTime())` (query = the store state). Remove `FilterChipRow` + the inline `SORT_OPTIONS` sort row and mount `<DashboardControlRow onPanelOpenChange={setPanelOpen} />`. **Drop the `listCategories(getExecutor())` fetch from `reload()`** — its only consumer was the removed `FilterChipRow`; category fetching moves to `DashboardControlRow` when Plan 02 adds the Filters panel (single owner — resolves the cross-AI review CYCLE-2 listCategories-ownership ambiguity: no orphaned fetch left in HomeScreen). Temporarily REMOVE the old search box + the `listDashboard(term)` call (search returns in Plan 07 on the D-12 read — a deliberate in-phase functionality gap, NOT an architectural gap; see the Release sequencing note — the 01→07 sequence is non-releaseable); keep the four counts + `selectDashboardEmptyState` fed with `activeFilter:"all"`, `activeFilters: query.filters`, `activePopulations: query.populations`, `hasTerm:false`. **Root-level overlay host + reconciled inert scope (addresses cross-AI review CYCLE-2 HIGH; consistent with the cycle-1 inert fix):** render `<DashboardOverlayHost/>` as a ROOT-LEVEL sibling to `<ShellAppBar/>` and `<FlatList/>` inside HomeScreen's root View (NOT inside the FlatList header) so its full-surface scrim + panel cover the whole screen. While `panelOpen`, wrap the BACKGROUND regions — the app-bar region (title + Your Week/Group Events destinations + ⋯ overflow trigger) AND the `FlatList` (which holds the search/toggle row, the collection, and the footer) — in inert wrappers (`importantForAccessibility="no-hide-descendants"` + `pointerEvents` `none`/`box-none`). Keep the `DashboardControlRow` triggers OUTSIDE those wrappers and ABOVE the scrim in z-order (e.g. render the control row as its own root-level sibling with an elevated zIndex while open, so re-tap dismiss + one-tap control switching still work while everything else is inert). The cycle-1 background inert wrappers and the cycle-2 root-level full-surface scrim are complementary, not contradictory. **Named fallback (stated up front):** if `no-hide-descendants` alone still lets TalkBack reach a background element on-device, ALSO set `accessible={false}` on those wrappers and rely on AnchoredPanel's `AccessibilityInfo.setAccessibilityFocus` re-assertion onto the open panel — do not defer this to a mid-UAT discovery. Preserve the freshness triple, `useBottomClearance`, `useShellRefresh`, `isFavourite={item.favourite_rank !== null}` (binary membership), the footer entries (Not-yet-contacted + Unbound — untouched here; Plan 04 removes them), and the existing single Group Events header trailing Pressable (Plan 04 replaces it). Every colour via `useTheme().colors.*`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/stores/dashboard-query-store.test.ts src/logic/dashboard-query-logic.test.ts</automated>
    <human-check>On the Pixel (per the verify-UI-on-the-Pixel runbook): open the Population control → panel anchors below the control row, list re-queries visibly behind a readable FULL-SURFACE scrim (the scrim also covers the app-bar area); toggle a population → list updates with no Apply step; tap-outside ANYWHERE incl. over the app bar / re-tap / system Back each dismiss; with reduce-motion ON the panel enter/exit is instant and backgrounding the app mid-open does not leave a half-run animation; with TalkBack ON confirm the app-bar destinations, ⋯ overflow, footer, and list rows behind the open panel are NOT reachable and focus stays on panel+scrim+triggers (FA-DASHC-05 full-scope backstop).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Modal" src/components/control-surface/AnchoredPanel.tsx src/components/control-surface/DashboardOverlayHost.tsx` returns nothing (no RN Modal — in-tree overlay + in-tree root host).
    - `grep -nE "AnchoredPanel|app-settings-dao|measureInWindow|StyleSheet.absoluteFill" src/components/control-surface/PopulationPanelContent.tsx` returns nothing (layer-2 seam clean).
    - `AnchoredPanel` is rendered by `DashboardOverlayHost`, NOT by `DashboardControlRow` (grep: `AnchoredPanel` appears in DashboardOverlayHost.tsx and is ABSENT from DashboardControlRow.tsx — the control row reports content to the host, it does not render the panel/scrim itself).
    - `DashboardOverlayHost` is mounted at the ROOT level of HomeScreen as a sibling to `ShellAppBar` + `FlatList` (grep/structure: `DashboardOverlayHost` appears in HomeScreen.tsx outside the `ListHeaderComponent`/`renderItem` closures — the scrim is full-surface, not inside the list).
    - `grep -n "useDashboardPrefs\|FilterChipRow\|listCategories" src/screens/HomeScreen.tsx` returns nothing (old store + chip row removed AND the orphaned category fetch dropped — category fetch now owned by DashboardControlRow in Plan 02).
    - `grep -n "listDashboardPopulation\|localDateTime" src/screens/HomeScreen.tsx` shows the population read uses the local-date helper (no `toISOString`).
    - The panel animation pauses off-focus/background: `grep -nE "useIsFocused|AppState" src/components/control-surface/AnchoredPanel.tsx` (or the host) shows the focus + AppState derivation gating the Reanimated timing (CLAUDE.md).
    - Population copy is rendered from the single source of truth: `grep -n "control-labels\|POPULATION_LABELS" src/components/control-surface/PopulationPanelContent.tsx src/components/control-surface/DashboardControlRow.tsx` shows both import labels from `control-labels`, with no inline population string literals.
    - `tsc --noEmit` + `npm run check:colors` green; the existing query-store + query-logic suites still pass.
    - The hydration-race guard exists and is tested: `grep -nE "generation|hydrated" src/stores/dashboard-query-store.ts` shows the counter + flag; `dashboard-query-store.test.ts` has a case proving a hydrate resolving AFTER a setter does NOT clobber the user's populations (stale snapshot dropped), and `hydrated` becomes true (cross-AI review CYCLE-3 MEDIUM).
    - The inert/`no-hide-descendants` wrapper(s) enclose the app-bar region AND the FlatList (search/toggle row, collection, footer) — with the `DashboardControlRow` triggers left OUTSIDE them and above the scrim (grep/structure check: the `no-hide-descendants` prop is applied to the background regions, and the control row is not a descendant of an inert wrapper).
    - On-device (TalkBack): with a panel open, NONE of the header destinations, the ⋯ overflow, the footer, or the contact rows are reachable; an outside tap ANYWHERE (incl. over the app bar) dismisses; focus stays on panel + scrim + control-row triggers (FA-DASHC-05 full-scope backstop).
  </acceptance_criteria>
  <done>Population control opens an in-tree anchored panel that live-applies through the Phase 25 store and read, with an a11y-hidden inert background and three dismissal paths — committed and green.</done>
  <reversibility rating="costly">The old→new store migration + population-read swap on HomeScreen is git-revertible but the highest-risk step in the phase; it is a refactor (no data one-way door) so it is flagged, not gated behind a checkpoint.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel content → dashboard-query-store → app-settings-dao → SQLite | population selections cross from UI into persisted query state |
| stored query state → listDashboardPopulation SQL | persisted populations/filters drive a read query |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | PopulationPanelContent write path | high | mitigate | onChange calls setPopulations(exec,…) → app-settings-dao assertDashboardPopulations guard; the panel NEVER writes app-settings-dao or SQL directly (grep-verified layer-2 seam). |
| T-26-02 | Tampering | listDashboardPopulation SQL | high | mitigate | Phase 25 read interpolates only closed SQL constants and ?-binds every runtime value; this plan adds no new interpolation and passes only the store query object. |
| T-26-03 | DoS (local) | store hydrate on malformed prefs | low | accept | parseStoredState try/catch already falls back to empty axes (dashboard-query-store.ts:38-49); no new failure surface. |
| T-26-04 | Info disclosure | device egress | low | accept | No network on any read path; no PORTABLE_SETTINGS_KEYS change (D-03) — none added. |
</threat_model>

<verification>
- `npx vitest run src/components/control-surface` (pure helpers) + `npx tsc --noEmit` + `npm run check:colors` after each task.
- Wave merge: `npm test`.
- Phase-gate backstops (Pixel): DASHC-04/05 panel open/switch/live-apply/inert + TalkBack a11y-hide + Back dismissal (uiautomator dump), per 26-VALIDATION.md.
</verification>

<success_criteria>
Population control is a live-applying anchored panel over the Phase 25 store/read with an a11y-hidden inert background; the three presentation-seam layers are grep-provable; HomeScreen no longer touches useDashboardPrefs; two pure helpers + 7 icons ship with passing tests.
</success_criteria>

<review_feedback_incorporated>
Cross-AI review 2026-09-05 (26-REVIEWS.md) — CYCLE 3 (current) findings addressed in this plan:
- **[MEDIUM] query-store hydration can clobber a just-made control selection** (Codex/orchestrator, dashboard-query-store.ts:64-92): a generation guard is added to the store — `hydrate()` captures the generation on entry, every setter bumps it, and a late hydrate applies its snapshot only if the generation is unchanged (else it drops the set and only marks `hydrated`). Added `dashboard-query-store.ts` + its test to files_modified + Task 2 `<files>`; added the must_have, the Task 2 action paragraph, an acceptance grep/test criterion, and a stale-hydrate test case.
- **[LOW] clampAnchorPosition idempotency must_have is not type-valid** (Codex): the idempotency must_have + the anchor-position action + the test row are redefined as a fixed point over the horizontal `left` axis (an anchorRect whose `x` already equals a returned `left` yields the same `left`) + a constrained-width assertion, NOT a type-invalid re-feed of the `{top,left,width}` output back in as an `{x,y,width,height}` anchor.

Cross-AI review CYCLE 2 (history — addressed in commit 957aa8c, retained):
- **[HIGH] anchored panel/scrim cannot dismiss/inert over the sibling app bar from inside the FlatList header** (Codex/orchestrator, verified): the panel + scrim are now hosted by a ROOT-LEVEL `DashboardOverlayHost` (sibling to ShellAppBar + FlatList), NOT rendered from DashboardControlRow/the ListHeaderComponent; DashboardControlRow measures its trigger (measureInWindow) and reports content to the host; the full-surface scrim covers the whole surface incl. the app bar (must_haves + prohibitions + Task 2 action/acceptance + files_modified/artifacts add DashboardOverlayHost.tsx). This is reconciled with — not contradictory to — the cycle-1 background inert wrappers (wrappers inert the background; the host's scrim dismisses over the whole surface). Upholds D-11 (root host is still in-tree, non-Modal).
- **[MEDIUM] panel enter/exit animation must pause on useIsFocused===false + AppState background** (Codex): the Reanimated timing now pauses/settles off-focus/background, mirroring OrreryScreen.tsx:287-295 (must_have + prohibition + Task 2 action/acceptance + human-check).
- **[MEDIUM] single source of truth for control copy** (Codex, filed on Plan 02, resolved foundationally here): a pure `control-labels.ts` label module is authored in Task 1 (population names + axis defaults) and rendered by PopulationPanelContent + the DashboardControlRow summary; Plan 02 extends it and Plan 07 consumes it — no re-authored strings.
- **[LOW] listCategories fetch ownership** (Claude): the tracer now DROPS the orphaned `listCategories` fetch from HomeScreen's reload() (its only consumer, FilterChipRow, is removed); Plan 02's DashboardControlRow becomes the single owner (Task 2 action + acceptance grep).

Cross-AI review CYCLE 1 (history — addressed in commit 1866d6d, retained):
- **[HIGH] a11y-inert scope** (Codex/orchestrator): the inert + `no-hide-descendants` wrapper covers the header destinations + ⋯ overflow, search/toggle row, footer, AND collection — not the FlatList alone — with the control-row triggers deliberately left focusable.
- **[MEDIUM] a11y-hide mechanism fallback** (Claude): named contingency (`accessible={false}` + `setAccessibilityFocus` re-assertion) stated up front in the Flagged Assumptions + Task 2 action, not deferred to UAT.
- **[MEDIUM] panel onChange persistence-failure path** (Codex): PopulationPanelContent onChange awaits the setter, retains prior selection on rejection, surfaces a themed non-blocking error, and guards rapid presses.
- **[MEDIUM] search-removal regression** (Codex): Release sequencing note declares Plans 01→07 non-releaseable; the tracer's search removal is an explicit in-phase gap that closes in Plan 07.
- **[LOW] DASHBOARD_POPULATIONS constant order** (orchestrator): Task 2 pins the UI-SPEC row order explicitly and forbids `.map()`-ing the constant (whose order differs).
</review_feedback_incorporated>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-01-SUMMARY.md` when done.
</output>
