---
phase: 26-dashboard-control-surface
plan: 06
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/screens/unbound-list-logic.ts
  - src/screens/unbound-list-logic.test.ts
  - src/screens/UnboundContactsScreen.tsx
autonomous: true
requirements: [DASHC-09]
estimate:
  tokens: 58000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "UnboundContactsScreen presents as a first-class Dashboard child route: its hand-rolled Back + raw 24/600 title are replaced by ShellAppBar variant='child' title='Unbound contacts', with origin-aware Back via resolveBackIntent/dismissTop (DASHC-09 / D-05)."
    - "The Unbound child route gains its OWN name-lookup search (testIDs unbound-contacts-search-input / unbound-contacts-search-clear) as the ADR-062 retrieval-path replacement for the D-13 removal of Unbound from Dashboard search (D-08)."
    - "Search is a pure name filter over the loaded Unbound rows via filterUnboundByName(rows, term) in unbound-list-logic.ts (node-tested); listUnbound stays the fixed WHERE + name-ordered read (reuse + refactor, no greenfield)."
    - "A term matching no unbound contact shows a calm no-match state; an empty/whitespace term shows the full list; the existing loading (rows===null) / error / empty ('No unbound contacts') states are retained."
    # --- edge-probe lift: DASHC-09 (unclassified → see Flagged Assumptions) + name-filter behavior ---
    - "filterUnboundByName is case-insensitive substring over name; empty/whitespace term returns the input list unchanged; a single-element / empty input list is handled without error (empty)."
    - "filterUnboundByName preserves the input (name COLLATE NOCASE, id) order of the surviving rows and does not mutate its input array (ordering / purity)."
    # --- UI-SPEC ## UI Considerations lift (S7) ---
    - "S7 empty: existing 'No unbound contacts' empty state retained."
    - "S7 populated: list + the new own-route search (ADR-062 retrieval path)."
    - "S7 loading: existing rows===null loading state retained; offline read."
    - "S7 error: existing error state retained."
    - statement: "S7: a search term matching no unbound contact shows a calm no-match state (copy default 'Search unbound contacts' placeholder; owner-confirm)."
      verification: backstop
    - statement: "S6/S7: long contact names wrap/truncate within the row without breaking layout under OS text scale."
      verification: backstop
  artifacts:
    - "src/screens/unbound-list-logic.ts :: filterUnboundByName (new pure export)"
    - "src/screens/unbound-list-logic.test.ts :: filterUnboundByName tests"
    - "src/screens/UnboundContactsScreen.tsx (ShellAppBar child + own search)"
  key_links:
    - "UnboundContactsScreen header → ShellAppBar variant='child'; search input → filterUnboundByName(rows, term) over listUnbound(getExecutor()) rows"
  prohibitions:
    - "MUST NOT ship the Unbound child route WITHOUT its own search (or without confirming the shell picker covers Unbound by explicit search) — removing Unbound retrieval without a replacement weakens ADR-062 → STOP AND ASK, do not close as cleanup (D-08)."
    - "MUST NOT greenfield-reimplement Unbound logic or listUnbound — reuse + refactor only (dossier §N)."
    - "MUST NOT add status/progress/favourite chrome to Unbound rows — they stay presentation-neutral (unbound-read contract)."
    - "MUST NOT put a blocking network call on the read path; the filter runs client-side over the offline SQLite read (the Unbound-retrieval prohibition is the load-bearing ADR-062 trip-wire for this phase)."
---

<objective>
Re-present the existing `UnboundContactsScreen` as a first-class Dashboard child route (DASHC-09 / D-05) AND give it its own name-lookup search — the ADR-062 retrieval-path replacement (D-08) for the D-13 removal of Unbound from Dashboard search. Reuse `listUnbound` and the pure `unbound-list-logic` helpers; add one pure name-filter.

Purpose: Keep Unbound contacts retrievable by name after Dashboard search went bound-only (D-13), satisfying the ADR-062 trip-wire, while modernizing the chrome.
Output: a pure `filterUnboundByName` + test, and a ShellAppBar-child UnboundContactsScreen with a working search.

## Flagged Assumption (no silent drop)
- **FA-DASHC-09 (unclassified edge → flagged):** the DASHC-09 edge probe returned `unclassified`. It is resolved here as: the child-route + origin-aware-return behavior is on-device UAT (Pixel), and the retrieval-replacement is the new own-route name search (node-tested pure filter). The child-route Back/return behavior is not mechanically unit-testable in the render-free env; it is a Pixel backstop. If the shell contact picker is later confirmed to cover Unbound by explicit search, that would be the alternative ADR-062 path — but this plan ships the own-route search unless the owner directs otherwise.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dashboard-control-surface/26-CONTEXT.md
@.planning/phases/26-dashboard-control-surface/26-RESEARCH.md
@.planning/phases/26-dashboard-control-surface/26-PATTERNS.md
@.planning/phases/26-dashboard-control-surface/26-UI-SPEC.md

@src/screens/UnboundContactsScreen.tsx
@src/screens/unbound-list-logic.ts
@src/db/unbound-read.ts
@src/components/ShellAppBar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure filterUnboundByName name filter + node tests (DASHC-09 retrieval path)</name>
  <read_first>
    - src/screens/unbound-list-logic.ts (the existing pure helpers unboundCountLabel/unboundRowAccessibilityLabel/usesNeutralUnboundRow — add the new filter beside them)
    - src/db/unbound-read.ts (UnboundRow shape: {id,name,photo,modified_at,trackingEnabled,favourite_rank,status,progress}; listUnbound WHERE + ORDER BY name COLLATE NOCASE, id)
    - src/db/dashboard-read.ts:38 + fuel-read escapeLike (for reference on the app's search/escape idiom — but this is an in-memory JS substring filter, NOT SQL)
  </read_first>
  <files>src/screens/unbound-list-logic.ts, src/screens/unbound-list-logic.test.ts</files>
  <behavior>
    - filterUnboundByName(rows, "") and whitespace-only → returns rows unchanged (same reference contents / order).
    - case-insensitive substring match on name ("ann" matches "Joanna", "ANNA").
    - empty rows array → []; single element handled.
    - order of surviving rows preserved (the input listUnbound order); input array not mutated.
    - a term matching nothing → [].
  </behavior>
  <action>
    Add a PURE `filterUnboundByName(rows: UnboundRow[], term: string): UnboundRow[]` to unbound-list-logic.ts: trim the term; empty → return rows as-is; else case-insensitive substring match on `row.name` (e.g. `name.toLowerCase().includes(term.trim().toLowerCase())`), returning a NEW array preserving input order without mutating the input. Keep the module react-native-free. Author `unbound-list-logic.test.ts` (vitest node) covering every behavior above (case-insensitivity, empty/whitespace passthrough, no-match [], order preservation, no input mutation, single/empty list).
  </action>
  <verify>
    <automated>npx vitest run src/screens/unbound-list-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `filterUnboundByName` exported pure (no RN import); test file green under node env.
    - Tests assert case-insensitivity, empty/whitespace passthrough, no-match [], order preservation, and non-mutation.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A node-tested pure Unbound name filter exists, satisfying the DASHC-09 retrieval logic off-device.</done>
</task>

<task type="auto">
  <name>Task 2: UnboundContactsScreen → ShellAppBar child + own-route search wired to filterUnboundByName</name>
  <read_first>
    - src/screens/UnboundContactsScreen.tsx (WHOLE FILE — header 54-70 to replace; loading/error/empty/list states to retain; the neutral row via unboundRowAccessibilityLabel)
    - src/screens/unbound-list-logic.ts (filterUnboundByName from Task 1)
    - src/components/ShellAppBar.tsx:20-67 (variant='child')
    - src/screens/HomeScreen.tsx (the search-row input idiom — testID/clear pattern to mirror; but this is the pre-migration or Plan-01 HomeScreen search idiom for visual consistency)
    - .planning/phases/26-dashboard-control-surface/26-UI-SPEC.md "Management Route Refactor" (Unbound search testIDs + placeholder 'Search unbound contacts')
  </read_first>
  <files>src/screens/UnboundContactsScreen.tsx</files>
  <action>
    Replace the hand-rolled header (Back Pressable + raw title) with `<ShellAppBar variant="child" title="Unbound contacts" />`. Add a search input above the list: testID `unbound-contacts-search-input`, placeholder `Search unbound contacts` (owner-confirm default), with a `unbound-contacts-search-clear` clear control shown when the term is non-empty; hold the term in local useState. Render `filterUnboundByName(rows, term)` for the list; when the filtered result is empty AND a term is active, show a calm no-match state (distinct from the existing 'No unbound contacts' empty state). Retain the loading (rows===null), error, and empty states + the neutral row rendering (Avatar + name + 'Unbound' label + unboundRowAccessibilityLabel) + row → Profile navigation. Every colour via useTheme().colors.*; the search input keeps a 44px target. Do NOT add status/progress/favourite chrome. Do NOT change listUnbound or the route registration.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: reach Unbound from the Dashboard overflow → ShellAppBar child Back; type a partial name → the list narrows; clear → full list; a no-match term shows the calm no-match state; Unbound → Profile → Back returns to Unbound (origin-aware).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "ShellAppBar" src/screens/UnboundContactsScreen.tsx` present (variant="child"); `grep -n "unbound-contacts-back" ...` returns nothing (hand-rolled Back removed).
    - testIDs `unbound-contacts-search-input` + `unbound-contacts-search-clear` present; list renders `filterUnboundByName(rows, term)`.
    - existing testIDs (unbound-contacts-screen/-loading/-error/-empty/-count/-row-{id}) retained.
    - `tsc --noEmit` + `check:colors` + `npm test` green.
    - On-device: child Back + working name search + calm no-match state + origin-aware return.
  </acceptance_criteria>
  <done>Unbound is a ShellAppBar child route with its own working name-lookup search (the ADR-062 retrieval replacement) and origin-aware Back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unbound search term → in-memory filter | untrusted text filters already-loaded rows (no SQL) |
| UnboundContactsScreen → unbound-read (listUnbound) | offline SQLite read |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-17 | Tampering | Unbound search | low | mitigate | Filter is an in-memory JS substring over loaded rows — no SQL, no interpolation; listUnbound's WHERE is a fixed constant with no runtime value. |
| T-26-18 | Info disclosure | retrieval-path removal | high | mitigate | The own-route search REPLACES the removed Dashboard-search retrieval for Unbound (ADR-062/D-08); shipping without it is a STOP-AND-ASK trip-wire, not silent removal. |
| T-26-19 | Info disclosure | device egress | low | accept | No network on the read/filter path (local-first). |
</threat_model>

<verification>
- Per task: `npx vitest run src/screens/unbound-list-logic.test.ts` (Task 1) + `tsc --noEmit` + `check:colors`; Task 2 `npm test`.
- Pixel: child Back + name search + no-match state + origin-aware return (26-VALIDATION.md DASHC-09).
</verification>

<success_criteria>
Unbound is a first-class child route with a node-tested own-route name search that satisfies the ADR-062 retrieval trip-wire (D-08), reusing listUnbound and staying presentation-neutral.
</success_criteria>

<output>
Create `.planning/phases/26-dashboard-control-surface/26-06-SUMMARY.md` when done.
</output>
