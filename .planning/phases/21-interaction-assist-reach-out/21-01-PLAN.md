---
phase: 21-interaction-assist-reach-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/014-interaction-assists.ts
  - src/db/database.ts
  - src/db/interaction-assist-dao.ts
  - src/db/interaction-assist-dao.test.ts
  - src/db/interaction-assist-read.ts
  - src/db/interaction-assist-read.test.ts
  - src/logic/assist-eligibility.ts
  - src/logic/assist-eligibility.test.ts
autonomous: true
requirements: [IAS-02, IAS-03]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "database.ts TARGET_VERSION === 14 and migration014 is appended to MIGRATIONS after migration013; a fresh openAndMigrate() creates the interaction_assists table and the app_settings.interaction_assist_enabled column (default 1)."
    - "createPendingAssist inserts one row with status='pending', channel in ('call','text','email'), handoff_at = the supplied local wall-clock time, and returns the row uid (minted via newUid, never crypto.randomUUID)."
    - "Creating a 6th pending assist expires the OLDEST pending (status becomes 'expired', resolved_at set) so at most 5 pending rows remain — enforced in the SAME transaction as the insert (Cluster M)."
    - "markAssistLogged for a Call 'Yes' writes exactly one interactions row (via insertInteractionCore + recomputeLastContactCore) with direction='outbound', connected=1, channel=assist.channel, occurred_at=assist.handoff_at, source='assist', and flips the assist to status='logged' — all inside ONE inWriteTransaction."
    - "markAssistLogged for Call 'No answer' writes an interactions row with connected=0 (all else identical), preserving attempted-contact history (Cluster T); the confirmation still accepts an optional note."
    - "markAssistLogged passes note through to insertInteractionCore when supplied and null when omitted (Cluster K); the confirmed interaction's occurred_at equals the assist handoff_at, NOT localDateTime() at confirmation (Cluster R)."
    - "markAssistDismissed flips the assist to status='dismissed' and writes NO interactions row (Cluster W)."
    - "markAssistFailed flips the assist to status='failed' and writes NO interactions row; a failed assist is never eligible to prompt (Cluster F)."
    - "The confirmation write composes the recency DAO's OWN authoritative cores — insertInteractionCore + recomputeLastContactCore + bumpDataRevisionCore (the same operations recordTouchpoint performs) — so contacts.last_contact is written ONLY by recomputeLastContactCore and interaction-assist-dao.ts performs NO bespoke INSERT INTO interactions and NO direct UPDATE of contacts.last_contact — the single-writer invariant (DATA-04) is preserved (Addresses review HIGH-1/HIGH-3)."
    - "markAssistLogged runs as ONE outer inWriteTransaction that composes the non-mutexed cores then the status flip atomically; it MUST NOT call the mutexed recordTouchpoint and MUST NOT nest inWriteTransaction (a nested inWriteTransaction on the non-reentrant mutex is a PERMANENT hang — transaction.ts:11-29) (Addresses review HIGH-1)."
    - "Resolving an assist whose status is not 'pending' writes no interaction and no duplicate: markAssistLogged reads the assist status FIRST inside its transaction and returns early (no-op) unless status='pending', and the flip is status-guarded (WHERE status='pending'), so a double-tapped 'Yes' cannot log twice (edge: idempotency — a status-guarded check-then-write, NOT UNIQUE-error sniffing; Addresses review HIGH-3)."
    - "When two pending assists share the same created_at, cap-5 pruning and newest-first ordering break the tie by id DESC — deterministic and stable, never a nondeterministic collision or reorder (edge: adjacency)."
    - "listEligiblePendingAssists returns pending rows newest-first ordered by (created_at DESC, id DESC) — a stable total order even when created_at values compare equal (edge: ordering)."
    - "deriveReachRoutes(actionablePrimary) enables Call AND Text when a phone method is actionable and Email when an email method is actionable, and returns an empty route set when neither exists (phone/email-granular per Cluster A/D)."
    - "Re-confirming a pending assist after a crash during markAssistLogged does not create a duplicate interaction — the interaction insert, recency recompute, revision bump, and status flip all commit inside ONE inWriteTransaction, so a crash rolls back atomically (status stays 'pending') and the status-guarded retry re-runs exactly once; no deterministic uid and no UNIQUE-error catch is used (edge: concurrency; Addresses review HIGH-3)."
    - "The assist-logged interaction row carries a plain newUid() uid (no deterministic 'reachout:' prefix), so restore/import treat it identically to any other interaction via restore-apply.ts:170 ON CONFLICT(uid) DO UPDATE; interaction_assists rows are operational state and are NOT part of the backup/export entity set (Cluster X) (Addresses review MEDIUM — restore/uid compatibility)."
  prohibitions:
    - "Interaction Assist writes must add NO network call on any path — the assist table, queue, and confirmation are wholly on-device; contact data never leaves the device (locked: local-first / Cluster AI)."
    - "Do not invent alternate recency logic — never UPDATE contacts.last_contact directly or write a bespoke INSERT INTO interactions; the confirmation reuses the recency DAO's own authoritative cores (recomputeLastContactCore is the sole last_contact writer — the same recompute recordTouchpoint uses) (locked: Cluster U / DATA-04)."
    - "markAssistLogged must NOT call the mutexed recordTouchpoint and must NOT catch/sniff a SQLite UNIQUE (or any DB error) to infer 'already logged' — idempotency is a status-guarded read-then-write inside one transaction, never driver-specific error-shape detection (locked: review HIGH-1/HIGH-3)."
  artifacts:
    - src/db/migrations/014-interaction-assists.ts
    - src/db/interaction-assist-dao.ts
    - src/db/interaction-assist-read.ts
    - src/logic/assist-eligibility.ts
  key_links:
    - "interaction-assist-dao.markAssistLogged → insertInteractionCore + recomputeLastContactCore + bumpDataRevisionCore (recency-dao.ts:425-428 / data-revision-dao), composed in ONE inWriteTransaction — the authoritative recency write, NOT the mutexed recordTouchpoint wrapper (recomputeLastContactCore stays the sole last_contact writer)."
    - "database.ts MIGRATIONS list + TARGET_VERSION → migration014 (the irreversible schema foundation)."
---

<objective>
Ship the durable Interaction Assist data spine and prove the phase's central architectural risk end-to-end at the data layer: a confirmed assist logs an outbound interaction at the ORIGINAL handoff time through the existing authoritative recency path (`recomputeLastContactCore` — the sole `last_contact` writer, the same recompute `recordTouchpoint` composes; markAssistLogged reuses the recency-dao cores atomically inside ONE transaction and does NOT call the mutexed `recordTouchpoint`), never a bespoke `interactions`/`last_contact` write. This is the TRACER — it wires storage → assist DAO → the single-writer recency path and is fully node-verifiable before any UI expansion (the native handoff and banner are device-UAT-only, so the honest "verified before expansion" proof lives here).

Purpose: The #1 correctness hazard in this phase (per CLAUDE.md) is a second writer of `contacts.last_contact`. Proving the confirmation path routes through the authoritative recency recompute (reusing recency-dao's own cores, never a second `last_contact` writer, never a nested mutex) — with cap-5, 24h/15s eligibility, and merge/purge durability semantics all expressed as DATA, not timers — de-risks every later slice.
Output: migration 014 (new `interaction_assists` table + `interaction_assist_enabled` settings column), `interaction-assist-dao.ts` (create-before-handoff, resolve→`recordTouchpoint`, dismiss, fail), `interaction-assist-read.ts` (eligible-queue query + route derivation), and `src/logic/assist-eligibility.ts` (pure ≥15s/≤24h/newest+count).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-interaction-assist-reach-out/21-RESEARCH.md
@.planning/phases/21-interaction-assist-reach-out/21-PATTERNS.md
@docs/dossier/21-interaction-assist-reach-out.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migration 014 + interaction-assist write DAO through the authoritative writer</name>
  <files>src/db/migrations/014-interaction-assists.ts, src/db/database.ts, src/db/interaction-assist-dao.ts, src/db/interaction-assist-dao.test.ts</files>
  <read_first>
    - src/db/database.ts (TARGET_VERSION at :48 = 13; MIGRATIONS list :51-64; localDateTime :74 — RE-VERIFY TARGET_VERSION on disk before writing, it drifts every schema phase)
    - src/db/migrations/013-reconciliation-and-merge.ts (new-table migration idiom + the two-arg `async apply(exec, _deps)` signature)
    - src/db/migrations/005-digest-settings.ts (the `ALTER TABLE app_settings ADD COLUMN ... INTEGER NOT NULL DEFAULT 1` idiom)
    - src/db/migrations/001-initial.ts (interactions schema: uid TEXT NOT NULL UNIQUE; channel/direction/source are free TEXT with NO CHECK)
    - src/db/recency-dao.ts (recordTouchpoint :217-243 is the MUTEXED wrapper — DO NOT call it from markAssistLogged. It EXPORTS non-mutexed CORES at :414-428 — `insertInteractionCore` + `recomputeLastContactCore` — the exact primitives it composes; recomputeLastContactCore is the SOLE writer of contacts.last_contact. Read the insertInteraction body :178-214, recomputeLastContact :159-176, and the core-export doc block :414-428 + the createContactFull composition precedent — this is the atomic-compose pattern markAssistLogged follows.)
    - src/db/data-revision-dao.ts (bumpDataRevisionCore — the non-mutexed revision bump recordTouchpoint calls at recency-dao.ts:240; call it inside the SAME outer transaction so dependent UI refreshes)
    - src/db/merge-dao.ts (inWriteTransaction + ?-bound multi-statement idiom; reparent() sets contact_id + modified_at)
    - src/db/transaction.ts (inWriteTransaction — NON-REENTRANT: NEVER nest one inside another; compose non-mutexed cores inside ONE outer txn per the doc block :11-29)
    - src/db/uid.ts (newUid at :18 — Hermes-safe; guards globalThis.crypto?.randomUUID; the assist-logged interaction uid is a plain newUid(), no deterministic prefix)
    - src/db/merge-dao.test.ts and src/db/recency-dao.test.ts (the node __testkit__ executor + migration-run test harness shape every DAO test follows)
  </read_first>
  <behavior>
    - Migration: creates table interaction_assists(id PK, uid TEXT NOT NULL UNIQUE, contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE, channel TEXT NOT NULL CHECK(channel IN ('call','text','email')), endpoint_value TEXT (nullable — operational handoff context only, Cluster D), status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','logged','dismissed','expired','failed')), handoff_at TEXT NOT NULL, resolved_at TEXT, created_at TEXT NOT NULL, modified_at TEXT NOT NULL) + index (status, created_at DESC); ALTER app_settings ADD COLUMN interaction_assist_enabled INTEGER NOT NULL DEFAULT 1.
    - createPendingAssist(exec, {contactId, channel, endpointValue, now}): one INSERT (status='pending') + a same-txn cap-5 prune that expires any pending beyond the newest 5 (ORDER BY created_at DESC, id DESC LIMIT 5); returns the new uid.
    - markAssistLogged(exec, {assistUid, connected, note, now}): runs ONE inWriteTransaction — (1) SELECT the assist row (id, contact_id, channel, handoff_at, status) WHERE uid=?; (2) if missing OR status!=='pending', return (no-op — status-guarded idempotency for double-tap/retry); (3) insertInteractionCore(exec, contact_id, now, {uid: newUid(), occurredAt: handoff_at, channel, direction:'outbound', connected (1=Yes / 0=No answer), note: note??null, source:'assist'}); (4) recomputeLastContactCore(exec, contact_id, now); (5) bumpDataRevisionCore(exec); (6) UPDATE interaction_assists SET status='logged', resolved_at=now, modified_at=now WHERE uid=? AND status='pending'. NO call to the mutexed recordTouchpoint, NO nested inWriteTransaction, NO UNIQUE-error catch — the whole unit is atomic so a crash rolls back and a retry re-runs exactly once.
    - markAssistDismissed(exec, {assistUid, now}): status-guarded UPDATE to 'dismissed', no interaction.
    - markAssistFailed(exec, {assistUid, now}): status-guarded UPDATE to 'failed', no interaction.
    - Tests: create→cap-5 prune (6th expires oldest, ≤5 pending); markLogged writes ONE outbound/connected=1 interaction at handoff_at through the recency cores and flips status; No answer → connected=0; dismissed/failed write no interaction; calling markAssistLogged TWICE for the same assist writes exactly one interaction (status-guarded no-op on the second call); markAssistLogged on an already-logged/dismissed assist is a no-op; the assist-logged interaction uid is a plain newUid() (no 'reachout:' prefix); equal-created_at tiebreak by id is deterministic; note pass-through.
  </behavior>
  <action>Create migration014 following 013's `{ version: 14, async apply(exec, _deps) { await exec.execAsync(`...DDL...`); } }` two-arg shape. Bump `TARGET_VERSION` to 14 and append `migration014` (import + array entry) in database.ts. Create interaction-assist-dao.ts exporting createPendingAssist, markAssistLogged, markAssistDismissed, markAssistFailed. Every write uses `inWriteTransaction` for its own multi-statement unit and `?`-bound params; timestamps come from the caller-supplied local `now` (localDateTime()), NEVER toISOString(). markAssistLogged is ONE `inWriteTransaction` that: SELECTs the assist (id, contact_id, channel, handoff_at, status) WHERE uid=?; returns early unless it is still 'pending' (status-guarded idempotency — a check-then-write, NEVER a driver error catch); then composes `insertInteractionCore` + `recomputeLastContactCore` + `bumpDataRevisionCore` (imported from recency-dao / data-revision-dao) with occurredAt=assist.handoff_at, direction:'outbound', connected, channel=assist.channel, note:note??null, source:'assist', uid:newUid(); then flips status='logged' via `UPDATE ... WHERE uid=? AND status='pending'` — all in that single transaction. markAssistLogged MUST NOT call the mutexed `recordTouchpoint`, MUST NOT nest `inWriteTransaction`, and MUST NOT write a bespoke `INSERT INTO interactions` or `UPDATE contacts SET last_contact`. Addresses review concern HIGH-1 (no txn nesting → no permanent hang) and HIGH-3 (status-guarded check-then-write, no driver-specific error-shape sniffing). Author the vitest suite RED first (assert the behaviors above against the __testkit__ executor after running MIGRATIONS through 014), then implement to GREEN.</action>
  <verify>
    <automated>npx vitest run src/db/interaction-assist-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/database.ts` exports `TARGET_VERSION = 14` and MIGRATIONS ends with `migration014`.
    - `npx vitest run src/db/interaction-assist-dao.test.ts` exits 0.
    - Test proves markAssistLogged inserts exactly one interactions row with direction='outbound', source='assist', occurred_at === the assist handoff_at, and connected reflecting Yes(1)/No answer(0).
    - Test proves a 6th createPendingAssist leaves at most 5 rows with status='pending'.
    - Test proves calling markAssistLogged twice for the same assist results in exactly one interactions row, and that markAssistLogged on an already-logged OR dismissed assist writes nothing (status-guarded no-op).
    - Test proves the assist-logged interaction uid is a plain newUid()-shaped value (does not start with 'reachout:').
    - `grep -n "insertInteractionCore\|recomputeLastContactCore\|bumpDataRevisionCore" src/db/interaction-assist-dao.ts` returns matches — the confirmation composes the recency cores (HIGH-1); markAssistLogged wraps its body in exactly ONE inWriteTransaction and neither nests another inWriteTransaction nor calls the mutexed recordTouchpoint (verify by reading markAssistLogged).
    - `grep -n "crypto.randomUUID\|toISOString" src/db/interaction-assist-dao.ts` returns no matches.
    - interaction-assist-dao.ts contains no direct `INSERT INTO interactions` and no `UPDATE contacts SET last_contact` (the interaction/recency write goes only through insertInteractionCore + recomputeLastContactCore).
  </acceptance_criteria>
  <reversibility rating="costly">Migration 014 is node-reversible until it ships in a release; once installed on a user device it is permanent (CLAUDE.md forward-only rule). The real pre-ship gate is the Plan 06 device UAT; the schema itself is fully derived from locked dossier decisions, so no decision checkpoint is warranted.</reversibility>
  <done>Migration 014 registered; the assist write DAO creates/prunes/logs/dismisses/fails through recordTouchpoint with node tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Eligible-queue read + route derivation + pure eligibility logic</name>
  <files>src/db/interaction-assist-read.ts, src/db/interaction-assist-read.test.ts, src/logic/assist-eligibility.ts, src/logic/assist-eligibility.test.ts</files>
  <read_first>
    - src/db/contact-methods-read.ts (listActionablePrimaryMethods :30 and listContactMethodGroups :7 — reuse verbatim; is_actionable is a single per-method boolean, phone/email-granular)
    - src/logic/contact-method-normalization.ts (isActionable semantics — phone line-type is discarded, so any actionable phone backs both Call and Text)
    - src/db/interaction-assist-dao.ts (the row shape from Task 1)
    - src/db/database.ts (localDateTime :74)
    - src/db/contact-methods-dao.ts (ContactMethodRow type)
  </read_first>
  <behavior>
    - assist-eligibility.ts (PURE, no SQL, no react-native): isAssistEligible(handoffAt, now) === true when now-handoffAt ≥ 15s AND now-handoffAt ≤ 24h — BOTH bounds measured against handoff_at, the phase's single canonical time field (Codex LOW: never created_at), wall-clock deltas from the stored local strings, never a timer; selectBannerState(pendingRows, now) → { newest: eligible row | null, morePendingCount: number } surfacing the newest eligible + the count of the remaining eligible pending.
    - interaction-assist-read.ts: listEligiblePendingAssists(exec, now) → pending rows with (now-handoff_at) ≥ 15s AND (now-handoff_at) ≤ 24h, ordered (created_at DESC, id DESC); countPending(exec, now); deriveReachRoutes(exec, contactId) → { call: boolean, text: boolean, email: boolean, primaryPhone: ContactMethodRow|null, primaryEmail: ContactMethodRow|null, hidden: boolean } where call===text===(actionable phone exists), email===(actionable email exists), hidden===(no actionable phone AND no actionable email).
    - Tests: eligibility boundary at exactly 15s and exactly 24h (both against handoff_at); empty/single/many banner-state selection; deriveReachRoutes hides when no methods, enables Call+Text from one phone, enables Email from one email; ordering is stable under equal created_at.
  </behavior>
  <action>Create src/logic/assist-eligibility.ts with the two pure functions above (parse the local `YYYY-MM-DD HH:MM:SS` strings to epoch ms for the delta; no Date-timezone traps — treat both operands as local). Use named constants ELIGIBLE_AFTER_SECONDS = 15 and EXPIRE_AFTER_HOURS = 24 at the top of the file so tuning is a single-number edit (CLAUDE.md tunable-constants rule). Create interaction-assist-read.ts taking `exec: SqlExecutor`; reuse listActionablePrimaryMethods for deriveReachRoutes rather than re-querying contact_methods. Both the 15-second buffer and the 24-hour expiry bound compare against handoff_at (the single canonical time field — the launch sweep in Plan 04 reuses EXPIRE_AFTER_HOURS against handoff_at too); created_at is used only for the cap-5 creation-order pruning. The eligible query's WHERE clause MUST use the same 15s/24h thresholds (via the shared constants) so the SQL filter and the client re-check agree. Author both vitest suites RED then GREEN.</action>
  <verify>
    <automated>npx vitest run src/db/interaction-assist-read.test.ts src/logic/assist-eligibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/interaction-assist-read.test.ts src/logic/assist-eligibility.test.ts` exits 0.
    - assist-eligibility.ts defines ELIGIBLE_AFTER_SECONDS and EXPIRE_AFTER_HOURS as top-of-file constants and isAssistEligible returns false below 15s and false beyond 24h.
    - deriveReachRoutes returns hidden=true when a contact has no actionable phone/email, and call===text when an actionable phone exists.
    - listEligiblePendingAssists orders results (created_at DESC, id DESC) and excludes rows younger than 15s or older than 24h — both bounds measured against handoff_at (the canonical time field).
    - `grep -n "setTimeout\|setInterval" src/logic/assist-eligibility.ts src/db/interaction-assist-read.ts` returns no matches.
  </acceptance_criteria>
  <reversibility rating="reversible">Pure/read-only additive code with no schema or contract impact.</reversibility>
  <done>The eligible-queue read and pure eligibility/route-derivation logic are node-tested green and share one set of 15s/24h constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App JS → on-device SQLite | All assist writes cross here; every value is `?`-bound through `SqlExecutor`. |
| (No network boundary) | Interaction Assist has no server, no egress — local-first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | interaction_assists writes | medium | mitigate | All params `?`-bound; channel/status CHECK-constrained at the schema; no string interpolation of user values. |
| T-21-02 | Repudiation/Integrity | contacts.last_contact | high | mitigate | Confirmation routes ONLY through recordTouchpoint (the single writer); no second writer path exists in the DAO — enforced by the acceptance grep. |
| T-21-03 | DoS | pending-assist growth | low | accept | Cap-5 + 24h expiry bound the pending set at write time and in the sweep (Plan 04); resolved rows pruned at 30 days. |
| T-21-04 | Info disclosure | assist rows | low | accept | Rows are local-only; endpoint_value is operational handoff context, never transmitted (local-first). |
</threat_model>

<verification>
- `npx vitest run src/db/interaction-assist-dao.test.ts src/db/interaction-assist-read.test.ts src/logic/assist-eligibility.test.ts` all green.
- `npx tsc --noEmit` clean for the touched files.
- Manual read-through confirms no `INSERT INTO interactions` / `UPDATE contacts SET last_contact` in the assist DAO.
</verification>

<success_criteria>
Migration 014 creates the assist table + settings column; the assist DAO creates-with-cap-5, logs through recordTouchpoint at handoff time (outbound; No answer connected=0), dismisses, and fails — all node-tested; the eligible-queue read + pure 15s/24h eligibility + route derivation are node-tested.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (for convergence source-grounding):
- Files: `src/db/migrations/014-interaction-assists.ts`, `src/db/interaction-assist-dao.ts`, `src/db/interaction-assist-read.ts`, `src/logic/assist-eligibility.ts` (+ their `.test.ts`).
- Table: `interaction_assists` (columns: id, uid, contact_id, channel, endpoint_value, status, handoff_at, resolved_at, created_at, modified_at) and index `idx_interaction_assists_pending`.
- Column: `app_settings.interaction_assist_enabled` (INTEGER NOT NULL DEFAULT 1).
- Functions: `createPendingAssist`, `markAssistLogged`, `markAssistDismissed`, `markAssistFailed`, `listEligiblePendingAssists`, `countPending`, `deriveReachRoutes`, `isAssistEligible`, `selectBannerState`; constants `ELIGIBLE_AFTER_SECONDS`, `EXPIRE_AFTER_HOURS`; interaction `source='assist'`. (The confirmation write REUSES the EXISTING recency-dao cores `insertInteractionCore` / `recomputeLastContactCore` and `bumpDataRevisionCore` — pre-existing symbols, not new artifacts; the assist-logged interaction uid is a plain `newUid()` with NO deterministic prefix. There is no `reachout:<assist-uid>` interaction-uid form.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-interaction-assist-reach-out/21-01-SUMMARY.md` when done.
</output>
