---
phase: 21-interaction-assist-reach-out
plan: 04
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/backup/backup-schema.ts
  - src/screens/SettingsScreen.tsx
  - src/stores/assist-store.ts
  - src/components/AssistBanner.tsx
  - src/components/PendingConfirmationsSheet.tsx
  - src/services/interaction-assist-sweep.ts
  - src/services/interaction-assist-sweep.test.ts
  - App.tsx
autonomous: true
requirements: [IAS-02]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Settings 'Interaction Assist' toggle reads/writes app_settings.interaction_assist_enabled through the standard settings DAO (getAppSettings + updateAppSettingsCore with the new interactionAssistEnabled key in COLUMN_OF); default is ON (the migration-014 column default is 1) (Cluster G)."
    - "interactionAssistEnabled is added to PortableSettingsSnapshot + getPortableSettingsSnapshot (SELECT + mapping, app-settings-dao.ts) AND to PORTABLE_SETTINGS_KEYS (backup-schema.ts:106-113) — exactly as digestEnabled — so the preference is exported/restored and passes backup manifest validation on another device (Addresses review MEDIUM — backup portability)."
    - "setInteractionAssistEnabled bumps data_revision EXACTLY ONCE per toggle: its inWriteTransaction composes updateAppSettingsCore + the clear-pending UPDATE + a single bumpDataRevisionCore(exec) — because updateAppSettingsCore deliberately does NOT bump (only the public updateAppSettings wrapper does, app-settings-dao.ts:605-606), and interactionAssistEnabled is in PORTABLE_SETTINGS_KEYS, so without this bump a toggle ON/OFF would not advance data_revision and could miss the backup-on-change path (Addresses review MEDIUM — data-revision bump). A test asserts data_revision increments by exactly one for BOTH an ON→OFF and an OFF→ON change."
    - "Resolving a pending assist from the review sheet calls notifyWidgetDataChanged() after a successful markAssistLogged, the same widget-freshness call the banner makes (Addresses review MEDIUM — widget freshness)."
    - "A joint test registers BOTH installSweepTrigger and the assist-store AppState subscription against ONE fake AppState and asserts they fire independently — the sweep dedupes once-per-launch on background→active, the banner refreshes on EVERY background→active — without clobbering each other (Addresses review MEDIUM — AppState double-subscription)."
    - "Toggling Interaction Assist OFF clears the pending queue immediately in ONE transaction — updateAppSettingsCore + an UPDATE of every status='pending' assist to 'expired' — and the banner hides at once; it is a normal Switch with NO confirmation dialog ('off means off') (Cluster G)."
    - "Cleared assists are not restored when the setting is re-enabled and create no interaction; re-enabling starts fresh (Cluster G)."
    - "When ≥2 assists are eligible, the banner shows the newest + '{N} more pending' and a tap opens the 'Pending confirmations' review sheet listing the pending assists; the review surface is a transient sheet, not a permanent dashboard feature (Cluster P)."
    - "When the last pending assist resolves, the review sheet shows 'You're all caught up' / 'No confirmations waiting.' (edge: empty)."
    - "The launch-sweep hook expires every pending assist older than 24h (measured against handoff_at — the canonical eligibility field, via the shared EXPIRE_AFTER_HOURS constant, NOT created_at) to status='expired' and DELETEs resolved rows (logged/dismissed/expired/failed) older than 30 days; expired assists never write an interaction (Cluster N, X) (Addresses review Codex LOW — canonical timestamp)."
    - "Expiry and retention are wall-clock deltas evaluated at sweep time — there is NO setTimeout/setInterval; the sweep runs once per real foreground launch via registerSweepHook and is never reached from a headless widget/notification tap (Cluster Q; CLAUDE.md no-scheduler rule)."
    - "The Settings toggle row is the stored on/off value on load and reflects flips immediately (edge: populated)."
    - "Cap-5 pruning (Plan 01, synchronous inside createPendingAssist) and the launch sweep's 24h expiry converge on the SAME interaction_assists rows without conflict: an integration test creates 6 pending assists (cap-5 expires the oldest at write time), then runs BOTH the cap-5 write path and interactionAssistSweep, and asserts a STABLE end state — at most 5 rows ever pending, every non-pending row status='expired' (idempotent — a second sweep is a no-op), and ZERO interactions rows produced by either expiry path (Addresses review finding #3 — double-expiry composition)."
    - "getAppSettings().interactionAssistEnabled is the SINGLE canonical read of the Interaction Assist toggle once this plan lands the COLUMN_OF key; the transitional raw interaction_assist_enabled column reads added in ContactProfileScreen (21-02) and ComposeScreen (21-03) are retired to this canonical getter by a tracked, acceptance-gated 21-06 task (not left as prose), so no two read paths to the column persist past the phase (Addresses review finding #4)."
  prohibitions:
    - "Interaction Assist is user-initiated with no passive monitoring — the queue/expiry logic must add no background timer, no polling, and no observation of native call/text/email activity (locked: Cluster AJ; CLAUDE.md)."
  artifacts:
    - src/components/PendingConfirmationsSheet.tsx
    - src/services/interaction-assist-sweep.ts
  key_links:
    - "setInteractionAssistEnabled(enabled) → updateAppSettingsCore + clear-pending (when disabling) + bumpDataRevisionCore in one inWriteTransaction (off means off; exactly one data-revision bump — Addresses review MEDIUM)."
    - "registerSweepHook(interactionAssistSweep) in App.tsx post-migration effect — the 24h/30d prune, foreground-only."
---

<objective>
Complete the durable-queue lifecycle: the default-on Settings toggle (with immediate off-clears-queue semantics), the multiple-pending review surface with the '{N} more pending' affordance, and the launch-sweep prune that enforces 24h expiry and 30-day resolved-row retention as data, never timers.

Purpose: These are the guarantees that keep Interaction Assist lightweight and durable — the cap-5/24h bounds, the "off means off" privacy affordance, and process-death-safe cleanup that a scheduler-less SQLite app must run at launch.
Output: the `interactionAssistEnabled` settings key + toggle, `PendingConfirmationsSheet.tsx`, the '{N} more pending' banner affordance, and `interaction-assist-sweep.ts` registered on the launch sweep.
</objective>

<decisions>
- **Wave-3 file ownership — no undeclared same-wave write conflict (Addresses review finding #2).** The
  cycle-2 review read 21-02 and 21-04 as SAME-WAVE editors of App.tsx / assist-store.ts / AssistBanner.tsx.
  That premise is corrected here: 21-04 is Wave 3 with `depends_on: [21-02]` while 21-02 is Wave 2, so 21-04
  runs STRICTLY AFTER 21-02 — its App.tsx / assist-store.ts / AssistBanner.tsx edits EXTEND 21-02's
  already-committed artifacts, they never race them. Within Wave 3 itself the three parallel plans are
  file-DISJOINT (verified against files_modified on disk): 21-03 owns EndpointSelector / ReachOutRouter /
  ComposeScreen; 21-04 owns app-settings-dao / backup-schema / SettingsScreen / assist-store / AssistBanner /
  PendingConfirmationsSheet / interaction-assist-sweep / App.tsx; 21-05 owns merge / purge / widget / types /
  ContactProfileScreen. No file appears in two Wave-3 plans, so Wave 3 is safely parallel.
- **Transitional raw `interaction_assist_enabled` read — tracked cleanup, executed in 21-06 (Addresses
  review finding #4).** 21-02 (Wave 2, ContactProfileScreen) and 21-03 (Wave 3, ComposeScreen) read the raw
  `interaction_assist_enabled` column defensively (default 1) because the DAO key does not exist until THIS
  plan (21-04) lands `getAppSettings().interactionAssistEnabled`. That leaves two read paths to one column
  transiently. The swap to the canonical `getAppSettings().interactionAssistEnabled` is NOT left as prose: it
  is a tracked, acceptance-gated task in 21-06 (Wave 4). It CANNOT live in 21-04 without a same-wave write
  conflict — the two raw-read sites are ContactProfileScreen (owned by 21-05 in Wave 3) and ComposeScreen
  (owned by 21-03 in Wave 3), so Wave 4 is the first safe point to edit them. This plan's job is to make the
  canonical getter exist and be the single source of truth; 21-06 retires the transitional raw reads BEFORE
  its full-suite gate so `npm test` covers the consolidated read path.
</decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-interaction-assist-reach-out/21-RESEARCH.md
@.planning/phases/21-interaction-assist-reach-out/21-PATTERNS.md
@.planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md
@docs/dossier/21-interaction-assist-reach-out.md
@.planning/phases/21-interaction-assist-reach-out/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: interactionAssistEnabled settings key + toggle-off-clears-queue + Settings row</name>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/db/app-settings-dao.ts (follow digestEnabled as the EXACT analog at EVERY site: AppSettings :49; AppSettingsPatch/keys :188; TOGGLE_FIELDS :256; COLUMN_OF :276; getAppSettings SELECT + mapping :311-333; PortableSettingsSnapshot :132-136; getPortableSettingsSnapshot SELECT + mapping :379-427; updateAppSettingsCore :617; validateAppSettingsPatch :545)
    - src/backup/backup-schema.ts (PORTABLE_SETTINGS_KEYS :106-113 — the restore-manifest allow-list; add "interactionAssistEnabled" here, same as digestEnabled, or restore on another device drops/rejects the preference)
    - src/db/migrations/005-digest-settings.ts (digest_enabled precedent — same idiom, migration 014 already added the column in Plan 01)
    - src/db/transaction.ts (inWriteTransaction)
    - src/db/data-revision-dao.ts (bumpDataRevisionCore — already imported in app-settings-dao.ts:23; the public updateAppSettings wrapper calls it at :605-606 AFTER updateAppSettingsCore, but updateAppSettingsCore itself deliberately does NOT bump. setInteractionAssistEnabled composes the core directly, so it MUST call bumpDataRevisionCore(exec) itself once — mirror the updateAppSettings wrapper at :604-607)
    - src/db/interaction-assist-dao.ts (the interaction_assists table + status values)
    - src/screens/SettingsScreen.tsx (the digest toggle row ~:895-971 — styles.row + toggleRow + Switch + helper copy; trackColor.true: accent, thumbColor: surfaceElevated)
    - .planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md (Copywriting: label "Interaction Assist", helper "Ask me to log calls, texts and emails started from Orbit."; Color: NOT destructive, no confirm dialog)
  </read_first>
  <behavior>
    - app-settings-dao: add interactionAssistEnabled: 0|1 to AppSettings/AppSettingsPatch/the row type, to TOGGLE_FIELDS, to COLUMN_OF (→ interaction_assist_enabled), to both getAppSettings SELECTs + mappings, AND to PortableSettingsSnapshot + getPortableSettingsSnapshot (SELECT column list + mapping) — every site digestEnabled appears at; validate as a 0|1 toggle. Add setInteractionAssistEnabled(exec, enabled, now) wrapping, in ONE inWriteTransaction: updateAppSettingsCore({interactionAssistEnabled: enabled}, now); then, if enabled===0, UPDATE interaction_assists SET status='expired', resolved_at=?, modified_at=? WHERE status='pending'; then bumpDataRevisionCore(exec) EXACTLY ONCE at the end of the transaction. The bump is required because updateAppSettingsCore does not bump on its own (only the updateAppSettings wrapper does, :605-606) and interactionAssistEnabled is a PORTABLE_SETTINGS_KEYS member — without it the toggle would not advance data_revision and could miss the backup-on-change path (Addresses review MEDIUM — data-revision bump).
    - backup-schema.ts: add "interactionAssistEnabled" to PORTABLE_SETTINGS_KEYS (:106-113) so the exported settings snapshot validates on restore (finding #2 — same scope as digestEnabled).
    - SettingsScreen: an "Interaction Assist" toggle row (digest-row idiom) bound to getAppSettings().interactionAssistEnabled, calling setInteractionAssistEnabled on change — a plain Switch, no confirm dialog.
    - Test: toggling off expires all pending assists in the same call and writes no interaction; toggling on does not restore them; the key round-trips through getAppSettings AND through getPortableSettingsSnapshot; PORTABLE_SETTINGS_KEYS contains interactionAssistEnabled; data_revision increments by EXACTLY ONE for an ON→OFF change AND by exactly one for an OFF→ON change (read data_revision before/after — Addresses review MEDIUM — data-revision bump).
  </behavior>
  <action>Extend app-settings-dao.ts exactly as digestEnabled is wired at EVERY digestEnabled site (types, TOGGLE_FIELDS, COLUMN_OF, both getAppSettings SELECT column lists + row mappings, validateAppSettingsPatch, AND PortableSettingsSnapshot + getPortableSettingsSnapshot SELECT + mapping). Add "interactionAssistEnabled" to PORTABLE_SETTINGS_KEYS in src/backup/backup-schema.ts (:106-113) — same scope as digestEnabled — so the exported preference passes restore manifest validation on another device (Addresses review concern: backup portability). Add exported setInteractionAssistEnabled with the one-transaction toggle-off-clear above, calling bumpDataRevisionCore(exec) EXACTLY ONCE at the end of the transaction (mirror updateAppSettings :604-607 — the core does not self-bump; Addresses review MEDIUM — data-revision bump); validation runs before the txn opens (mirror updateAppSettings at :598-605). Add the Settings toggle row using the shipped digest-toggle idiom and the exact UI-SPEC copy — a normal Switch, never a destructive/confirm styling. Extend app-settings-dao.test.ts to prove the off-clears-pending semantics, the getAppSettings round-trip, AND the getPortableSettingsSnapshot round-trip (+ that PORTABLE_SETTINGS_KEYS contains interactionAssistEnabled).</action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/app-settings-dao.test.ts` exits 0.
    - COLUMN_OF contains `interactionAssistEnabled: "interaction_assist_enabled"` and getAppSettings returns it.
    - getPortableSettingsSnapshot returns interactionAssistEnabled, and `grep -n "interactionAssistEnabled" src/backup/backup-schema.ts` shows it in PORTABLE_SETTINGS_KEYS.
    - The test proves setInteractionAssistEnabled(exec, 0, now) flips all pending assists to 'expired' and writes zero interactions, and that a subsequent enable does not restore them.
    - The test proves setInteractionAssistEnabled increments data_revision by exactly one for an ON→OFF change and by exactly one for an OFF→ON change, and `grep -n "bumpDataRevisionCore" src/db/app-settings-dao.ts` shows the call inside setInteractionAssistEnabled's transaction (Addresses review MEDIUM — data-revision bump).
    - SettingsScreen contains the label "Interaction Assist" and helper "Ask me to log calls, texts and emails started from Orbit." with a plain Switch (no Alert confirm around the toggle).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive settings key + UI row; no schema change (the column shipped in Plan 01).</reversibility>
  <done>The default-on toggle round-trips through the settings DAO and, when turned off, clears the pending queue in one transaction with no confirmation dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Multiple-pending review sheet + '{N} more pending' banner affordance</name>
  <files>src/stores/assist-store.ts, src/components/AssistBanner.tsx, src/components/PendingConfirmationsSheet.tsx</files>
  <read_first>
    - src/stores/assist-store.ts (Plan 02 — queue/newest/morePendingCount + refresh)
    - src/components/AssistBanner.tsx (Plan 02 — the overlay this extends)
    - src/components/OverflowMenu.tsx (sheet + ScrollView idiom for the review surface)
    - src/db/interaction-assist-read.ts (listEligiblePendingAssists — the full eligible list for the review sheet)
    - src/db/interaction-assist-dao.ts (markAssistLogged/markAssistDismissed — resolving from the review sheet)
    - src/services/widget/widget-refresh.ts (notifyWidgetDataChanged — call after a successful markAssistLogged resolve from the review sheet, the same widget-freshness call the banner makes)
    - .planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md (Copywriting: "{N} more pending", review title "Pending confirmations", empty "You're all caught up"/"No confirmations waiting."; Color: "{N} more pending" may use accent as a disclosure affordance)
  </read_first>
  <behavior>
    - assist-store: expose the full eligible list (not just newest) so the review sheet can render all pending; keep newest + morePendingCount for the banner.
    - AssistBanner: when morePendingCount>0, render the "{N} more pending" affordance; tapping it opens PendingConfirmationsSheet. A single pending shows the bare question with no count.
    - PendingConfirmationsSheet: a transient sheet (OverflowMenu idiom) titled "Pending confirmations" listing each eligible pending assist with its confirmation actions; when the list empties, show "You're all caught up" / "No confirmations waiting." Resolving an item calls markAssistLogged/markAssistDismissed, then (on a logged write) notifyWidgetDataChanged() and store.refresh() — the same widget-freshness call as the banner (finding #1).
  </behavior>
  <action>Extend assist-store to surface the full eligible queue alongside newest/morePendingCount. Add the "{N} more pending" Pressable to AssistBanner (accent disclosure per UI-SPEC) that opens PendingConfirmationsSheet. Create PendingConfirmationsSheet.tsx (transient sheet, ScrollView, per-item AssistConfirmation) with the exact UI-SPEC title/empty copy; each resolve calls markAssistLogged/markAssistDismissed then, after a successful markAssistLogged, notifyWidgetDataChanged() (from @/services/widget/widget-refresh) and store.refresh() (Addresses review concern: widget freshness after confirmation). Every colour via useTheme; 44px touch targets.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run check:colors` both pass.
    - AssistBanner renders "{N} more pending" only when morePendingCount>0 and a single-pending banner shows no count.
    - PendingConfirmationsSheet contains the strings "Pending confirmations", "You're all caught up", and "No confirmations waiting.".
    - The review sheet is a component-level transient surface (no new nav route registered).
    - The review-sheet resolve handler calls notifyWidgetDataChanged() after a successful markAssistLogged (grep PendingConfirmationsSheet for the notifyWidgetDataChanged import + call).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive UI + store field.</reversibility>
  <done>The banner exposes the pending count and a transient review sheet that lists and resolves the queue, with a calm all-caught-up empty state.</done>
</task>

<task type="auto">
  <name>Task 3: Launch-sweep prune hook (24h expiry + 30-day retention)</name>
  <files>src/services/interaction-assist-sweep.ts, src/services/interaction-assist-sweep.test.ts, App.tsx</files>
  <read_first>
    - src/services/launch-sweep.ts (registerSweepHook :45; installSweepTrigger :102-115 + the AppStateLike shape :34-39 for the joint test; the NEGATIVE rules — no module-scope side effect, foreground-only, unreachable from headless taps :11-16)
    - src/services/digest-schedule.ts or the digest sweep registration (a shipped registerSweepHook consumer that takes getExecutor lazily — mirror its shape)
    - src/stores/assist-store.ts (subscribeAppState — Plan 02; the joint test registers this AND installSweepTrigger against ONE fake AppStateLike)
    - App.tsx (the post-migration effect :156-251 where sweep hooks register once via module guards, BEFORE installSweepTrigger fires the cold-start sweep :244)
    - src/db/interaction-assist-dao.ts (the interaction_assists table + status/timestamps)
    - src/db/database.ts (getExecutor, localDateTime)
    - src/logic/assist-eligibility.ts (EXPIRE_AFTER_HOURS=24 — reuse the same constant, applied to handoff_at)
  </read_first>
  <behavior>
    - interaction-assist-sweep.ts exports interactionAssistSweep(getExecutor): a SweepHook that, at sweep time: UPDATE interaction_assists SET status='expired', resolved_at=now WHERE status='pending' AND (now - handoff_at) > 24h (measured against handoff_at — the SAME canonical field as eligibility, via EXPIRE_AFTER_HOURS, NOT created_at); DELETE FROM interaction_assists WHERE status IN ('logged','dismissed','expired','failed') AND (now - resolved_at) > 30 days. Wall-clock deltas via localDateTime; a named RETAIN_RESOLVED_DAYS=30 constant at the top. No timer.
    - App.tsx: register the hook once (module guard, like the other sweeps) inside the ready post-migration effect, before installSweepTrigger fires.
    - Test: a pending assist older than 24h (by handoff_at) becomes 'expired' with no interaction; a resolved assist older than 30 days is DELETEd; younger rows survive.
    - Double-expiry integration test (finding #3): create 6 pending assists (cap-5 inside createPendingAssist already expires the oldest at write time), then run BOTH the cap-5 creation path and interactionAssistSweep against the SAME executor; assert a stable end state — at most 5 rows were ever pending, every non-pending row is status='expired' and stays 'expired' when the sweep runs a SECOND time (idempotent, written once), and NO interactions row was written by either expiry path. This proves cap-5's synchronous prune and the launch sweep's 24h expiry (different WHERE-scopes, both writing status='expired') compose safely on the same rows.
    - Joint AppState test (finding #6): register BOTH installSweepTrigger(fakeAppState) and the assist-store subscribeAppState(fakeAppState) against ONE hand-rolled AppStateLike fake; drive background→active transitions and assert they fire INDEPENDENTLY and do not clobber each other — the sweep runs its hooks once per launch (deduped) while the banner refresh fires on every background→active; an inactive→active drives neither's launch path.
  </behavior>
  <action>Create src/services/interaction-assist-sweep.ts exporting interactionAssistSweep(getExecutor) that performs the 24h-expire + 30d-delete described, taking the executor lazily inside the hook (never at module scope — headless-tap safety). The 24h expiry compares against handoff_at — the canonical eligibility field — NOT created_at (Addresses review Codex LOW). Reuse EXPIRE_AFTER_HOURS from assist-eligibility and add RETAIN_RESOLVED_DAYS=30 as a top-of-file constant. Register it in App.tsx via registerSweepHook with a one-shot module guard alongside the existing sweep registrations, BEFORE installSweepTrigger(AppState). Author interaction-assist-sweep.test.ts against the __testkit__ executor proving the expire/delete/retain boundaries, AND a joint AppState test that registers installSweepTrigger and assist-store's subscribeAppState against one fake AppStateLike and asserts independent, non-clobbering firing (Addresses review concern: AppState double-subscription), AND a double-expiry integration test that creates 6 pending assists then runs both the cap-5 creation path and interactionAssistSweep and asserts a stable end state (≤5 ever pending, non-pending rows status='expired' and idempotent under a second sweep, zero interactions rows written) (Addresses review concern #3: cap-5 + sweep converge on the same rows). Confirm no setTimeout/setInterval anywhere.</action>
  <verify>
    <automated>npx vitest run src/services/interaction-assist-sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/interaction-assist-sweep.test.ts` exits 0.
    - The test proves a >24h (by handoff_at) pending assist becomes 'expired' (no interaction written) and a >30d resolved assist is deleted while younger rows remain.
    - A joint test registers installSweepTrigger and assist-store's subscribeAppState against one fake AppState and proves independent firing (sweep once-per-launch; banner every background→active) with no clobbering (finding #6).
    - An integration test proves cap-5 pruning and the sweep compose to a stable end state on the same rows: 6 pending → at most 5 ever pending, non-pending rows are status='expired' (idempotent under a second sweep), and zero interactions rows are written (finding #3).
    - `grep -n "setTimeout\|setInterval" src/services/interaction-assist-sweep.ts` returns no matches.
    - App.tsx registers interactionAssistSweep via registerSweepHook once (module guard) before installSweepTrigger.
    - interaction-assist-sweep.ts has no module-scope executor access (executor taken inside the hook).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive foreground sweep hook.</reversibility>
  <done>A launch sweep expires >24h pending assists and prunes >30-day resolved rows, foreground-only and timer-free, registered in the app shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App JS → on-device SQLite | Toggle/queue/sweep writes; all `?`-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-11 | Info disclosure / privacy | pending queue after opt-out | high | mitigate | Toggle OFF clears pending immediately in one txn ("off means off") — no stale prompts survive the opt-out (test-enforced). |
| T-21-12 | DoS | unbounded assist rows | low | mitigate | 24h expiry + 30d retention sweep bound the table; cap-5 bounds pending; all wall-clock, no timer. |
| T-21-13 | Tampering | headless tap reaching the sweep | medium | mitigate | Hook registered foreground-only via registerSweepHook; module has no import-time side effect (mirrors the launch-sweep contract). |
</threat_model>

<verification>
- `npx vitest run src/db/app-settings-dao.test.ts src/services/interaction-assist-sweep.test.ts` green.
- `npx tsc --noEmit` + `npm run check:colors` clean.
- Grep: no setTimeout/setInterval in the sweep; UI-SPEC copy present; no confirm dialog around the toggle.
- Toggle + review-sheet UI are device-UAT (Plan 06).
</verification>

<success_criteria>
The default-on toggle clears the queue immediately when turned off; the banner exposes a '{N} more pending' review sheet with a calm empty state; a launch sweep enforces 24h expiry and 30-day retention without any timer.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates: settings key `interactionAssistEnabled` (column `interaction_assist_enabled`) + `setInteractionAssistEnabled`; the interactionAssistEnabled entries in `PortableSettingsSnapshot` / `getPortableSettingsSnapshot` / `PORTABLE_SETTINGS_KEYS` (backup portability); the Settings "Interaction Assist" toggle row; `src/components/PendingConfirmationsSheet.tsx`; the "{N} more pending" banner affordance; `src/services/interaction-assist-sweep.ts` (`interactionAssistSweep`, `RETAIN_RESOLVED_DAYS`); the joint AppState coexistence test; the cap-5 + sweep double-expiry integration test (finding #3).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-interaction-assist-reach-out/21-04-SUMMARY.md` when done.
</output>
