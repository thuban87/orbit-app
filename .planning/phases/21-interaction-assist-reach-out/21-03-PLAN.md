---
phase: 21-interaction-assist-reach-out
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - src/components/EndpointSelector.tsx
  - src/components/ReachOutRouter.tsx
  - src/screens/ComposeScreen.tsx
autonomous: true
requirements: [IAS-01, IAS-02]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When a chosen channel has exactly one actionable endpoint, the router launches directly after the channel tap (≤2 taps); when it has ≥2 actionable endpoints, the router shows the endpoint selector as a THIRD tap — the maximum routing depth is three taps (Cluster B)."
    - "The endpoint selector lists all actionable endpoints of the chosen type (from listContactMethodGroups), emphasizes the primary with a 'Primary' marker + accent, and heads 'Choose a number' (phone) / 'Choose an email' (email); secondary endpoints remain selectable (Cluster B)."
    - "Selecting an endpoint (primary or secondary) hands off through the same performReachOut path — the selected endpoint is operational handoff context only; the resulting interaction stores the coarse channel, never which endpoint was used (Cluster D)."
    - "Compose 'Send' hands to native Messages and, when Interaction Assist is ON, creates a pending Text assist BEFORE the SMS launch via the shared handoff helper; Compose still writes NO interaction row at Send time — only later banner confirmation writes it (Cluster AC)."
    - "The endpoint/route lists scroll within the sheet (ScrollView idiom) rather than clipping when a contact has many methods (edge: overflow)."
  prohibitions:
    - "Do not persist the selected endpoint as permanent per-endpoint interaction history — endpoint_value on the assist row is transient handoff context; the interaction records only the coarse channel (locked: Cluster D)."
  artifacts:
    - src/components/EndpointSelector.tsx
  key_links:
    - "EndpointSelector → performReachOut (shared handoff) — the 3rd tap in the ≤3-tap contract."
    - "ComposeScreen.onSend → shared handoff createPendingAssist (Text) — the Compose→assist seam (Cluster AC)."
---

<objective>
Add the third routing tap (endpoint selection when a channel has multiple actionable endpoints, primary emphasized) and wire the existing Compose 'Send' into the shared assist-creation model so a text sent from Compose participates in the return-confirmation lifecycle without writing an interaction at Send time.

Purpose: Completes the ≤3-tap Reach Out contract for multi-endpoint contacts and unifies Compose's Send with Reach Out's handoff-then-confirm model (the alternative — Compose writing directly — is the exact inconsistency the dossier forbids).
Output: `EndpointSelector.tsx`, the router's selector wiring, and the additive Compose→assist seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-interaction-assist-reach-out/21-RESEARCH.md
@.planning/phases/21-interaction-assist-reach-out/21-PATTERNS.md
@.planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md
@docs/dossier/21-interaction-assist-reach-out.md
@.planning/phases/21-interaction-assist-reach-out/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EndpointSelector (≥2 endpoints, primary emphasized) + router wiring</name>
  <files>src/components/EndpointSelector.tsx, src/components/ReachOutRouter.tsx</files>
  <read_first>
    - src/components/ReachOutRouter.tsx (Plan 02 — the modal + route buttons this extends)
    - src/components/OverflowMenu.tsx (sheet + Pressable-row + ScrollView idiom)
    - src/db/contact-methods-read.ts (listContactMethodGroups :7 — all actionable endpoints per type, ordered)
    - src/db/contact-methods-dao.ts (ContactMethodRow — is_primary, is_actionable, display_value)
    - .planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md (Copywriting: "Choose a number"/"Choose an email"/"Primary"; Color: primary endpoint filled accent, secondary accent-outline)
    - src/services/reach-out/handoff.ts (performReachOut)
  </read_first>
  <behavior>
    - EndpointSelector(props: { type:'phone'|'email', endpoints: ContactMethodRow[], onPick(endpoint), onCancel }): renders a heading ("Choose a number"/"Choose an email"), a scrollable list of endpoints filtered to is_actionable===1, primary row marked "Primary" + accent-filled, others accent-outline; a Pressable per endpoint (44px). Only rendered by the router when ≥2 actionable endpoints exist for the chosen channel.
    - ReachOutRouter: on a Call/Text/Email tap, count actionable endpoints of that type; if exactly 1 → performReachOut directly (≤2 taps); if ≥2 → show EndpointSelector; on pick → performReachOut with the chosen endpoint. Call and Text both draw from the phone endpoint list; Email from the email list.
  </behavior>
  <action>Create src/components/EndpointSelector.tsx per the OverflowMenu/ScrollView idiom with the UI-SPEC copy and colour rules. Extend ReachOutRouter to branch on the actionable-endpoint count of the tapped channel: single → performReachOut immediately, multiple → render EndpointSelector then performReachOut on pick. The endpoint passed to performReachOut is the method's actionable value (display/canonical per the handoff need); it is used only to perform the handoff and to stamp endpoint_value on the pending assist — never surfaced as interaction history. Keep every colour via useTheme.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run check:colors` both pass.
    - EndpointSelector renders a ScrollView list and marks the is_primary row with the "Primary" label.
    - ReachOutRouter launches directly for a single-endpoint channel and shows EndpointSelector for a ≥2-endpoint channel (verify by reading the branch logic).
    - `grep -n "Choose a number\|Choose an email\|Primary" src/components/EndpointSelector.tsx` matches the UI-SPEC copy.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive presentational component + router branch.</reversibility>
  <done>Multi-endpoint contacts get a primary-emphasized endpoint selector as the third tap; single-endpoint channels launch directly.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Send → shared assist creation seam</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (onSend :436-458 — currently SMS.sendSMSAsync with NO interaction write; keep it that way, add assist creation only)
    - src/services/reach-out/handoff.ts (performReachOut / the shared createPendingAssist path — reuse, do not duplicate)
    - src/db/interaction-assist-dao.ts (createPendingAssist, markAssistFailed)
    - docs/dossier/21-interaction-assist-reach-out.md (Cluster AC; Cross-Milestone note line 756 — M2 Phase 12 will own Compose Send UX and MUST map to this handoff model)
  </read_first>
  <behavior>
    - onSend: when Interaction Assist is ON, create a pending Text assist (channel='text', endpoint=the actionable phone) BEFORE SMS.sendSMSAsync; on a thrown launch mark it failed. When Assist is OFF, behave exactly as today (launch only, no assist). In all cases Compose writes NO interaction row at Send time — confirmation via the banner is the only interaction writer.
  </behavior>
  <action>Modify ComposeScreen.onSend to route the SMS handoff through the shared assist-aware path: read interaction_assist_enabled (defensively defaulting to enabled until Plan 04 wires the settings read), and when enabled call createPendingAssist(channel='text') immediately before SMS.sendSMSAsync, marking it failed in the catch branch. Do NOT add any interaction/log write. Add a short code comment recording the M2 Phase 12 seam: Compose's Send maps to handoff-to-native + assist-creation, never a direct interaction write. Preserve the existing Copy path and the existing "Couldn't open your messages app" Alert.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is clean.
    - ComposeScreen.onSend calls createPendingAssist before SMS.sendSMSAsync when Assist is enabled and still contains no `recordTouchpoint`/interaction insert.
    - `grep -n "recordTouchpoint\|INSERT INTO interactions" src/screens/ComposeScreen.tsx` returns no matches (Send writes no interaction — Cluster AC).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive to an existing handler; the interaction-writing behavior is unchanged (still none at Send).</reversibility>
  <done>Compose Send creates a pending Text assist (when Assist is ON) that flows into the return banner, without writing an interaction at Send time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App JS → OS Messages intent | Compose/router hand off to the OS; no server egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09 | Info disclosure | endpoint selection | low | accept | The chosen endpoint is transient handoff context (endpoint_value) and never becomes permanent interaction history (Cluster D). |
| T-21-10 | Integrity | Compose double-write | medium | mitigate | Compose Send still writes no interaction; only banner confirmation writes, preventing intent-vs-confirmed conflation (grep-enforced). |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run check:colors` clean.
- Grep: no interaction write added to Compose; UI-SPEC copy present in EndpointSelector.
- Multi-endpoint UI + Compose Send handoff are device-UAT (Plan 06).
</verification>

<success_criteria>
Multi-endpoint contacts reach a primary-emphasized endpoint selector within three taps; Compose Send creates a pending Text assist (Assist ON) and defers the interaction write to banner confirmation.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates: `src/components/EndpointSelector.tsx`; extends the router with the multi-endpoint branch; adds the Compose Send → assist seam.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-interaction-assist-reach-out/21-03-SUMMARY.md` when done.
</output>
