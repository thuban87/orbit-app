---
phase: 37.1-category-management
plan: 03
type: execute
wave: 3
depends_on: ["37.1-02"]
files_modified:
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/backup/reconciliation.ts
  - src/backup/reconciliation.test.ts
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/backup/phase-17-integration.test.ts
autonomous: true
requirements: []
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Backup format remains v5 while category tombstones are validated, exported, reconciled, and applied."
    - "Merge deletion wins timestamp ties, prevents older category rules/ref state from resurrecting, nulls all-status local import-session references, and preserves unrelated rules."
    - "A pure validator checks the normalized post-reconciliation local+incoming survivor union and rejects category/System visible-name collisions in either direction before staging or mutation."
    - "Replace-all reproduces the selected backup's exact ordered taxonomy, including zero categories, without extras or reseeding, and atomically tombstones every removed destination category but no incoming survivor."
  artifacts:
    - { path: src/backup/backup-schema.ts, provides: "Closed v5 category-tombstone and graph validation" }
    - { path: src/backup/reconciliation.ts, provides: "Category row/tombstone winner and dependent suppression" }
    - { path: src/backup/restore-apply.ts, provides: "Pre-staging survivor validation, dependency-safe category merge/delete, and tombstone-safe exact replace-all" }
  key_links:
    - { from: src/backup/reconciliation.ts, to: src/backup/restore-apply.ts, via: "surviving category UID set and category-aware delete plan" }
    - { from: src/backup/restore-apply.ts, to: src/db/categories-dao.ts, via: "same parent-last fallout invariants with merge-specific null target" }
---

<objective>Make portable restore honor user-owned mutable taxonomy without changing the v5 wire format.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@docs/decisions/ADR-056-tombstone-backed-uid-reconciliation-for-portable-restores.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Admit category deletion evidence at the v5 manifest boundary</name>
  <read_first>src/backup/backup-schema.ts, src/backup/export-manifest.ts, src/db/tombstones-dao.ts, src/db/systems-dao.ts</read_first>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts</files>
  <behavior>- v5 parses/exports valid category tombstones and rejects malformed or duplicate entity+UID evidence.
- Manifest-local category/System rows and deletion evidence satisfy the existing closed-graph, duplicate, and normalized visible-name checks before destination state is read.</behavior>
  <action>Extend the existing closed tombstone allowlist and manifest-local graph validation; keep BACKUP_FORMAT_VERSION at 5. Reuse canonical category/System normalized-name semantics for the incoming manifest only. Leave destination-aware post-reconciliation union validation to Tasks 2 and 3. Do not add fields, infer category identity from name, or expose backup content in diagnostics.</action>
  <acceptance_criteria>Category deletion evidence round-trips in v5 and malformed or internally ambiguous incoming manifests fail at the schema/export boundary.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts</automated></verify>
  <done>The v5 schema/export boundary safely represents the approved semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile category winners and suppress only tombstone-lost dependents</name>
  <read_first>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/logic/system-rule-resolver.ts</read_first>
  <files>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts</files>
  <behavior>- Category tombstone wins equal timestamp and maps to the category entity.
- Rules/ref state tied to a category that lost specifically to a tombstone are suppressed; unrelated/historical missing-category diagnostics retain existing behavior.
- All surviving unrelated rules and Systems remain unchanged.
- A pure final-union validator accepts normalized post-reconciliation surviving category/System rows and rejects case-insensitive NFC visible-name collisions across the two entity types, regardless of which side supplied the category or System.</behavior>
  <action>Add category-aware reconciliation and a survivor-driven dependent normalization pass. Distinguish deletion evidence from simple absence so this phase does not reverse the existing historical missing-rule diagnostic contract. Add and unit-test a pure validator over the finalized category/System survivor rows; it must report incompatibility without writing or staging anything and must cover local-category/incoming-System and local-System/incoming-category collisions.</action>
  <acceptance_criteria>Merge cannot semantically resurrect a tombstoned category or erase unrelated System intent, and reconciliation exposes one pure final-survivor uniqueness gate for restore application.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/reconciliation.test.ts</automated></verify>
  <done>Category reconciliation outputs a safe, complete apply plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply category-aware merge deletion and exact replace-all</name>
  <reversibility rating="costly">Restore ordering is a durable compatibility contract consumed by every future backup.</reversibility>
  <read_first>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts, src/db/migrations/012-import-sessions.ts</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts</files>
  <behavior>- Merge tombstone repairs dependencies before deleting category parent; every local pending/complete/discarded import session referencing it becomes null because merge has no interactive target.
- Replace-all deletes destination category dependents and categories in FK-safe order, then inserts exactly the incoming ordered set; empty input leaves zero categories.
- Before Replace-all removes destination category rows, it writes category tombstones in the same restore transaction for every destination UID absent from the incoming survivor set; incoming survivor UIDs are never tombstoned.
- Restore application invokes the pure final category/System survivor-union validator after reconciliation/plan normalization and before no-op return, photo/background staging, or transaction mutation; either collision direction returns incompatibility with zero mutation.
- Any apply failure rolls back the entire graph and no seed helper runs.</behavior>
  <action>Route category merge deletion through a category-aware apply step rather than generic raw parent deletion. For merge only, use null/Uncategorized for all referencing import statuses. After winner normalization and dependent reconciliation, call Task 2's pure validator on the exact surviving category/System rows before totals/no-op handling and before any photo/background staging or write transaction; restore-level tests must prove both local-category/incoming-System and local-System/incoming-category collisions return incompatibility with byte-for-byte unchanged destination state and no staging calls. In Replace-all's one outer transaction, capture destination category UIDs, subtract incoming category survivors, and upsert a `category` tombstone for each actually removed UID using the restore's canonical deletion timestamp before FK-safe dependent/category deletion and exact incoming insertion; never tombstone an incoming survivor. Add a follow-up Merge fixture whose older category row cannot resurrect a destination category removed by Replace-all. Retain parse/plan-before-mutate and never call a seed helper.</action>
  <acceptance_criteria>Merge and replace-all satisfy pre-staging final-union validation, tombstone-backed non-resurrection, all-status import, exact-empty-taxonomy, dependency-order, zero-mutation rejection, and rollback fixtures with no format or schema bump.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/restore-apply.test.ts src/backup/phase-17-integration.test.ts src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts</automated></verify>
  <done>Backup v5 safely round-trips arbitrary renamed, reordered, deleted, and empty taxonomies.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-07 | Spoofing: ambiguous crafted catalog | high | mitigate | parse-before-mutate validation of final normalized survivor union |
| T-37.1-08 | Tampering: deleted-category resurrection | high | mitigate | category tombstone mapping, deletion wins ties, survivor-driven dependent suppression |
| T-37.1-09 | DoS/data loss: unsafe restore order | high | mitigate | dependency-safe plan and full transactional rollback tests including zero taxonomy |
</threat_model>
<verification>Run the complete backup command, typecheck, and assert BACKUP_FORMAT_VERSION remains 5.</verification>
<success_criteria>Portable backup/restore reproduces actual user taxonomy and never resurrects deletion evidence.</success_criteria>
## Artifacts this phase produces
- Category-aware v5 backup validation, reconciliation, and restore application.
<output>Create `.planning/phases/37.1-category-management/37.1-03-SUMMARY.md` when done.</output>
