---
phase: 37.1-category-management
plan: 03
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/db/systems-dao.ts
  - src/db/systems-dao.test.ts
  - src/logic/system-rule-resolver.ts
  - src/logic/system-rule-resolver.test.ts
  - src/db/systems-catalog-read.ts
  - src/db/systems-catalog-read.test.ts
  - src/screens/SystemsManagementScreen.tsx
  - src/screens/SystemsManagementScreen.test.tsx
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Category and custom-System writers enforce one NFC/case-normalized visible-name namespace while preserving stable ID/UID relationships."
    - "Removing a category rule never deletes a custom System or alters unrelated rules."
    - "A System with another meaningful rule or an explicit include remains valid; an empty or exclude-only definition is Needs Attention."
    - "Historical genuinely missing category rules remain canonical broken-rule diagnostics; runtime deletion does not retain or fake the deleted rule."
  artifacts:
    - path: src/logic/system-rule-resolver.ts
      provides: "Canonical positive-definition/Needs-Attention diagnostic"
  key_links:
    - from: src/db/systems-dao.ts
      to: src/logic/category-logic.ts
      via: "shared normalized visible-name key"
    - from: src/logic/system-rule-resolver.ts
      to: src/screens/SystemsManagementScreen.tsx
      via: "typed definition validity beside brokenRules"
---

<objective>
Make cross-kind naming symmetric and extend the canonical System validity model for category-rule removal without fake stale rules or loss of manual-only Systems.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/dossier/milestone-2/phase-37.1-category-management-dossier.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share normalized visible-name uniqueness across Categories and Systems</name>
  <reversibility rating="reversible">Validation logic is code-only and leaves existing stored identities and schema unchanged.</reversibility>
  <files>src/db/systems-dao.ts, src/db/systems-dao.test.ts</files>
  <read_first>
    - src/db/systems-dao.ts and src/db/systems-dao.test.ts (full file; inspect every System metadata/rule/override/pref writer and name lookup)
    - src/logic/category-logic.ts and src/logic/category-logic.test.ts (full shared normalization contract from Plan 01)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full reciprocal collision writer)
    - src/db/systems-catalog-read.ts (full built-in/category/custom catalog labels)
    - docs/dossier/milestone-2/phase-37.1-category-management-dossier.md §§D–E
  </read_first>
  <behavior>
    - Both writer families reject NFC/case-equivalent collisions with categories, custom Systems, and built-in labels.
    - Capitalization-only rename excludes the edited identity; no relationship or ref token changes.
    - Existing duplicate-name generation also uses the shared key, not a parallel lowercase rule.
  </behavior>
  <action>
Replace the Systems DAO's independent lowercase/COLLATE-only comparison logic with the shared normalized visible-name key from `category-logic`, including create, rename, duplicate-name generation, and category collision checks. Preserve existing System-specific blank/length/error contracts and the database UNIQUE backstop. Add symmetric tests that create through each writer and collide through the other, including composed/decomposed Unicode and capitalization-only self rename. Do not add a SQLite NOCASE index or rewrite existing names/UIDs.
  </action>
  <verify>
    <automated>npx vitest run src/db/systems-dao.test.ts src/db/categories-dao.test.ts src/logic/category-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Category/System collision behavior is symmetric and covered for Unicode NFC and case variants.
    - Rename updates only the owning name/modified timestamp; numeric IDs, UIDs, rules, refs, and presentation assignments remain unchanged.
    - No schema/index/migration change is present.
  </acceptance_criteria>
  <done>Visible naming is one shared contract across all three System kinds and category rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonical Needs Attention semantics after category-rule removal</name>
  <reversibility rating="reversible">The resolver projection and consumers can be revised without stored-data conversion.</reversibility>
  <files>src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts, src/db/systems-catalog-read.ts, src/db/systems-catalog-read.test.ts, src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx</files>
  <read_first>
    - src/logic/system-rule-resolver.ts and src/logic/system-rule-resolver.test.ts (full resolver and all rule families/broken diagnostics)
    - src/db/systems-catalog-read.ts and src/db/systems-catalog-read.test.ts (full catalog status projection)
    - src/screens/SystemsManagementScreen.tsx and src/screens/SystemsManagementScreen.test.tsx (full Needs Attention consumer and reorder/write behavior)
    - src/db/systems-dao.ts (full rule/override semantics, especially explicit include/exclude rows)
    - docs/systems/orrery-systems-backup-contract.md (full current historical-missing-rule contract; update is owned by Plan 05)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§8–11
  </read_first>
  <behavior>
    - Another meaningful valid rule after category-rule removal → ready.
    - No rules plus at least one explicit include override → ready/manual-only.
    - No rules/no includes, or exclude-only overrides → Needs Attention.
    - Unrelated malformed/historical missing rules remain in brokenRules and still imply Needs Attention.
  </behavior>
  <action>
Add a typed positive-definition/attention field to the canonical resolver result rather than synthesizing a missing-category BrokenRule. Derive it from meaningful valid rules plus explicit include overrides; exclude-only is not positive membership. Thread the field through catalog/member projections and Systems Management wherever current code equates attention solely with read status or `brokenRules.length`. Keep the deleted category rule physically absent and preserve unrelated rules/Systems. Add the four closed-boundary tests plus catalog/management consumption tests.
  </action>
  <verify>
    <automated>npx vitest run src/logic/system-rule-resolver.test.ts src/db/systems-dao.test.ts src/db/systems-catalog-read.test.ts src/screens/SystemsManagementScreen.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four validity boundaries are directly asserted.
    - Consumers use the typed canonical status; no category-specific sentinel or fake missing rule exists.
    - Deleting one rule cannot delete a System or modify unrelated rules/overrides.
  </acceptance_criteria>
  <done>Runtime category-rule fallout produces truthful valid/Needs Attention state without breaking manual includes or historical diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Mutable names → shared System catalog | Ambiguous labels could spoof selection identity. |
| Deleted rule → membership resolver | Incorrect validity can silently widen/narrow visible contacts. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-10 | Spoofing | Visible System names | high | mitigate | Shared NFC/case key at both serialized writers; stable refs remain authoritative. |
| T-37.1-11 | Tampering | System rule fallout | high | mitigate | Delete exact UID rule only; resolver tests prove unrelated rules and explicit includes survive. |
| T-37.1-12 | Denial of service | Empty definitions | medium | mitigate | Typed Needs Attention result keeps System manageable instead of deleting/crashing it. |

Package legitimacy: N/A.
</threat_model>

<artifacts>
## Artifacts this phase produces
- Shared Systems-writer use of the normalized visible-name key.
- Typed positive-definition/Needs-Attention resolver diagnostic consumed by catalog and management UI.
</artifacts>

<verification>
Run all Systems DAO/resolver/catalog/management focused tests, typecheck, and changed-file Biome.
</verification>

<success_criteria>
Renames remain identity-safe, cross-kind labels are unambiguous, and every custom System stays preserved with correct membership validity after category deletion.
</success_criteria>

<output>Create `.planning/phases/37.1-category-management/37.1-03-SUMMARY.md` when done.</output>
