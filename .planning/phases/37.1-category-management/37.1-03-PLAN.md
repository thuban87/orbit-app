---
phase: 37.1-category-management
plan: 03
type: execute
wave: 3
depends_on: ["37.1-02"]
files_modified:
  - src/backup/types.ts
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/backup/reconciliation.ts
  - src/backup/reconciliation.test.ts
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/backup/phase-17-integration.test.ts
  - src/services/backup/backup-service.test.ts
  - src/backup/orrery-preferences-portability.test.ts
autonomous: true
requirements: []
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Backup wire format is bumped from v5 to v6; category tombstones are validated, exported, reconciled, and applied at v6. A minimal v5→v6 relabel step keeps the existing forward-migration chain consistent; no v5-restore accommodation or v5-scenario test is added (no v5 backups exist in the wild)."
    - "Merge deletion wins timestamp ties, prevents older category rules/ref state from resurrecting, nulls all-status local import-session references, and preserves unrelated rules."
    - "A pure validator enforces one normalized visible-name set (trim → NFC → locale-independent toLowerCase) across surviving categories, custom Systems, and built-in System labels, rejecting same-kind and cross-kind collisions from either source before staging or mutation."
    - "Replace-all reproduces the selected backup's exact ordered taxonomy, including zero categories, without extras or reseeding; it atomically tombstones every removed destination category, tombstones no incoming survivor, and clears any stale destination category tombstone whose UID is an incoming live survivor."
  artifacts:
    - { path: src/backup/backup-schema.ts, provides: "Closed v6 category-tombstone and graph validation" }
    - { path: src/backup/reconciliation.ts, provides: "Category row/tombstone winner and dependent suppression" }
    - { path: src/backup/restore-apply.ts, provides: "Pre-staging survivor validation, dependency-safe category merge/delete, and tombstone-safe exact replace-all" }
  key_links:
    - { from: src/backup/reconciliation.ts, to: src/backup/restore-apply.ts, via: "surviving category UID set and category-aware delete plan" }
    - { from: src/backup/restore-apply.ts, to: src/db/categories-dao.ts, via: "same parent-last fallout invariants with merge-specific null target" }
---

<objective>Make portable restore honor user-owned mutable taxonomy by bumping the portable backup wire format to v6.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@docs/decisions/ADR-056-tombstone-backed-uid-reconciliation-for-portable-restores.md
@docs/decisions/ADR-063-versioned-lifecycle-backup-and-dormant-cadence-restore.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bump backup format to v6 and admit category deletion evidence at the v6 manifest boundary</name>
  <read_first>src/backup/types.ts, src/backup/backup-schema.ts, src/backup/export-manifest.ts, src/db/tombstones-dao.ts, src/db/systems-dao.ts</read_first>
  <files>src/backup/types.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/services/backup/backup-service.test.ts, src/backup/orrery-preferences-portability.test.ts</files>
  <behavior>- BACKUP_FORMAT_VERSION (defined in src/backup/types.ts) becomes 6; v6 parses/exports valid category tombstones and rejects malformed or duplicate entity+UID evidence.
- Manifest-local category/System rows and deletion evidence satisfy the existing closed-graph, duplicate, and normalized visible-name checks before destination state is read.
- A minimal v5→v6 step advances the version counter so the existing forward-migration chain (v2→…→v5) reaches v6; it parses no v5-specific payload, synthesizes no category evidence, and adds no v5-restore accommodation or v5-scenario test. Every existing test asserting format version 5 is updated to 6.</behavior>
  <action>Bump BACKUP_FORMAT_VERSION from 5 to 6 in src/backup/types.ts (the ADR-063-compliant path for a portable wire-shape change) and extend the closed tombstone allowlist and manifest-local graph validation to admit the `category` entity at v6. Add the minimal v5→v6 forward-migration step needed to keep the migration chain and its tests consistent — a version-counter/identity relabel only, with NO v5 payload parsing, NO synthesized category evidence, NO dev harness, and NO v5-restore/v5-scenario test (zero v5 backups exist in the wild; the owner cut only serious v5-restore piping, not framework integrity). Update every existing test that asserts format version 5 (src/backup/orrery-preferences-portability.test.ts, the legacy-chain expectations in src/backup/backup-schema.test.ts, and src/services/backup/backup-service.test.ts) to expect 6. Reuse canonical category/System normalized-name semantics for the incoming manifest only. Leave the persisted Dashboard category filter token to Plan 06, and destination-aware post-reconciliation union validation to Tasks 2 and 3. Do not add fields, infer category identity from name, or expose backup content in diagnostics.</action>
  <acceptance_criteria>Category deletion evidence round-trips in v6, malformed or internally ambiguous incoming manifests fail at the schema/export boundary, and the full backup suite is green with every format-version assertion at 6 behind only a minimal version-relabel v5→v6 step.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts</automated></verify>
  <done>The v6 schema/export boundary safely represents the approved semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile category winners and suppress only tombstone-lost dependents</name>
  <read_first>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/logic/system-rule-resolver.ts</read_first>
  <files>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts</files>
  <behavior>- Category tombstone wins equal timestamp and maps to the category entity.
- Rules/ref state tied to a category that lost specifically to a tombstone are suppressed; unrelated/historical missing-category diagnostics retain existing behavior.
- All surviving unrelated rules and Systems remain unchanged.
- A pure final-union validator enforces one normalized visible-name set (trim → NFC → locale-independent toLowerCase) across surviving categories, custom Systems, and built-in System labels, rejecting same-kind (category↔category, System↔System) and cross-kind collisions regardless of which side supplied each row.</behavior>
  <action>Add category-aware reconciliation and a survivor-driven dependent normalization pass. Distinguish deletion evidence from simple absence so this phase does not reverse the existing historical missing-rule diagnostic contract. Add and unit-test a pure validator over the finalized category/System survivor rows; it must report incompatibility without writing or staging anything and must cover category↔category, custom-System↔custom-System, category↔custom-System (both source directions), and category/System↔built-in-label collisions, using the deterministic trim → NFC → locale-independent toLowerCase key.</action>
  <acceptance_criteria>Merge cannot semantically resurrect a tombstoned category or erase unrelated System intent, and reconciliation exposes one pure final-survivor uniqueness gate for restore application.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/reconciliation.test.ts</automated></verify>
  <done>Category reconciliation outputs a safe, complete apply plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply category-aware merge deletion and exact replace-all</name>
  <reversibility rating="costly">Restore ordering is a durable compatibility contract consumed by every future backup.</reversibility>
  <read_first>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts, src/db/migrations/012-import-sessions.ts, src/db/categories-dao.ts</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts</files>
  <behavior>- Merge tombstone repairs dependencies before deleting category parent; every local pending/complete/discarded import session referencing it becomes null because merge has no interactive target.
- Replace-all deletes destination category dependents and categories in FK-safe order, then inserts exactly the incoming ordered set; empty input leaves zero categories.
- Before Replace-all removes destination category rows, it writes category tombstones in the same restore transaction for every destination UID absent from the incoming survivor set; incoming survivor UIDs are never tombstoned.
- Before importing the selected manifest's category evidence, Replace-all deletes any destination `category` tombstone whose UID is an incoming live survivor, so a restored-live category is never shadowed by a stale local tombstone on a later export or merge.
- Restore application invokes the pure final category/System survivor-union validator after reconciliation/plan normalization and before no-op return, photo/background staging, or transaction mutation; any collision class returns incompatibility with zero mutation.
- Any apply failure rolls back the entire graph and no seed helper runs.</behavior>
  <action>Route category merge deletion through Plan 02's exported transaction-composable category-fallout core (invoked in merge-null target mode) under restore's own outer transaction, rather than generic raw parent deletion or a divergent re-implementation; if any restore-specific step cannot reuse that core, cover the divergence with an explicit runtime-vs-restore parity test. For merge only, use null/Uncategorized for all referencing import statuses. After winner normalization and dependent reconciliation, call Task 2's pure validator on the exact surviving category/System rows before totals/no-op handling and before any photo/background staging or write transaction; restore-level tests must prove representative same-kind and cross-kind collisions (including a built-in-label collision) return incompatibility with byte-for-byte unchanged destination state and no staging calls. In Replace-all's one outer transaction, capture destination category UIDs, subtract incoming category survivors, upsert a `category` tombstone for each actually removed UID using the restore's canonical deletion timestamp before FK-safe dependent/category deletion and exact incoming insertion, and delete any pre-existing destination `category` tombstone whose UID is an incoming live survivor before importing the manifest's tombstones; never tombstone an incoming survivor. Add a fixture with a newer local `category:X` tombstone plus an incoming live category X proving replace-all, a subsequent export, and a subsequent merge all preserve X with no silent re-deletion. Add a follow-up Merge fixture whose older category row cannot resurrect a destination category removed by Replace-all. Retain parse/plan-before-mutate and never call a seed helper.</action>
  <acceptance_criteria>Merge and replace-all satisfy pre-staging final-union validation, tombstone-backed non-resurrection, all-status import, exact-empty-taxonomy, dependency-order, zero-mutation rejection, and rollback fixtures at format v6 with no SQLite schema migration.</acceptance_criteria>
  <verify><automated>npx vitest run src/backup/restore-apply.test.ts src/backup/phase-17-integration.test.ts src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts</automated></verify>
  <done>Backup v6 safely round-trips arbitrary renamed, reordered, deleted, and empty taxonomies.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-07 | Spoofing: ambiguous crafted catalog | high | mitigate | parse-before-mutate validation of final normalized survivor union |
| T-37.1-08 | Tampering: deleted-category resurrection | high | mitigate | category tombstone mapping, deletion wins ties, survivor-driven dependent suppression |
| T-37.1-09 | DoS/data loss: unsafe restore order | high | mitigate | dependency-safe plan and full transactional rollback tests including zero taxonomy |
</threat_model>
<verification>Run the complete backup command, typecheck, and assert BACKUP_FORMAT_VERSION is 6 behind only a minimal version-relabel v5→v6 step (no v5-restore accommodation).</verification>
<success_criteria>Portable backup/restore reproduces actual user taxonomy and never resurrects deletion evidence.</success_criteria>
## Artifacts this phase produces
- Category-aware v6 backup validation, reconciliation, and restore application.
<output>Create `.planning/phases/37.1-category-management/37.1-03-SUMMARY.md` when done.</output>
