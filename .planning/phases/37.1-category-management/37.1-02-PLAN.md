---
phase: 37.1-category-management
plan: 02
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
  - src/db/import-session-dao.ts
  - src/db/tombstones-dao.ts
  - src/db/tombstones-dao.test.ts
  - src/db/systems-dao.ts
  - src/db/systems-dao.test.ts
  - src/logic/system-rule-resolver.ts
  - src/logic/system-rule-resolver.test.ts
  - src/db/systems-catalog-read.ts
  - src/db/systems-catalog-read.test.ts
  - src/db/orrery-system-read.ts
  - src/db/orrery-system-read.test.ts
  - src/screens/SystemsManagementScreen.tsx
  - src/screens/SystemsManagementScreen.test.tsx
  - src/db/app-settings-dao.ts
autonomous: true
requirements: []
estimate: { tokens: 50000, raw_tokens: 50000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Deletion preview and lock-time fingerprint cover every durable fallout class and lightweight deletion is used only when all counts are zero."
    - "Interactive deletion reassigns contacts and every pending, complete, or discarded import session to one selected target/null and repairs all dependent state atomically."
    - "The category row is deleted last, one category tombstone is written, and data_revision advances exactly once; any stage failure restores the complete pre-operation state."
    - "Removing a category rule preserves unrelated rules/System rows and produces one typed canonical Needs Attention validity signal only when no meaningful rule or explicit include remains; every management and Orrery read model carries that signal."
  artifacts:
    - { path: src/db/categories-dao.ts, provides: "Preview fingerprint and one-lock destructive aggregate" }
    - { path: src/db/categories-dao.test.ts, provides: "Success, stale-preview, and exhaustive injected-failure matrix" }
    - { path: src/logic/system-rule-resolver.ts, provides: "Canonical empty-definition/override validity" }
    - { path: src/db/orrery-system-read.ts, provides: "Validity-bearing System member and scene snapshots" }
  key_links:
    - { from: src/db/categories-dao.ts, to: src/db/import-session-dao.ts, via: "non-mutexed all-status set-based reassignment" }
    - { from: src/db/categories-dao.ts, to: src/db/app-settings-dao.ts, via: "updateAppSettingsCore for Dashboard filter and Orrery fallback" }
    - { from: src/db/categories-dao.ts, to: src/db/tombstones-dao.ts, via: "insertTombstoneCore without nested revision bump" }
---

<objective>Implement the phase's hardest invariant: permanent category deletion as one revalidated, fully rollback-tested local transaction.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/dossier/milestone-2/phase-37.1-category-management-dossier.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock canonical System validity and transaction-composable fallout helpers</name>
  <read_first>src/db/transaction.ts, src/db/import-session-dao.ts, src/db/systems-dao.ts, src/logic/system-rule-resolver.ts, src/db/systems-catalog-read.ts, src/db/orrery-system-read.ts, src/screens/SystemsManagementScreen.tsx, src/db/app-settings-dao.ts, src/db/profile-presentation-dao.ts, src/db/tombstones-dao.ts</read_first>
  <files>src/db/import-session-dao.ts, src/db/tombstones-dao.ts, src/db/tombstones-dao.test.ts, src/db/systems-dao.ts, src/db/systems-dao.test.ts, src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts, src/db/systems-catalog-read.ts, src/db/systems-catalog-read.test.ts, src/db/orrery-system-read.ts, src/db/orrery-system-read.test.ts, src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx, src/db/app-settings-dao.ts</files>
  <behavior>- A non-mutexed core moves every import session referencing a category, with no status predicate, preserving status and unrelated fields.
- Runtime category rule removal deletes only matching UID rules; a remaining meaningful rule or include override has `valid` canonical validity, while empty/exclude-only definitions have `needs-attention` canonical validity.
- `ResolvedCustomSystemMembers`, `SystemMemberCount`, and `OrreryMembersResult`/scene snapshots carry the same typed validity independently of `brokenRules`; Systems Management treats non-ready status, broken rules, or `needs-attention` validity as broken.
- `category:<uid>` prefs/overrides and active selection fallback use existing closed ref grammar; category becomes an admitted tombstone entity.</behavior>
  <action>Extract or expose only transaction-composable cores needed by the outer category transaction; never call public transactional wrappers from inside it. Define one typed resolver validity contract (`valid` | `needs-attention`) from meaningful valid rules or an explicit include override, keeping genuinely malformed/historical references in `brokenRules` instead of retaining a deleted rule or inventing a Category-only sentinel. Propagate that validity through catalog counts and Orrery member/snapshot read models, then make Systems Management consume it alongside existing missing/broken diagnostics. Add focused resolver, catalog, Orrery-read, and management tests covering meaningful-rule, include-only, empty, and exclude-only definitions. Add `category` to the tombstone TypeScript vocabulary without schema DDL or a user-facing history surface.</action>
  <acceptance_criteria>All fallout helpers can run under one existing writer lock, preserve unrelated state, and express the same typed valid versus Needs Attention result through resolver, catalog, Orrery snapshots, and Systems Management.</acceptance_criteria>
  <verify><automated>npx vitest run src/db/tombstones-dao.test.ts src/db/systems-dao.test.ts src/logic/system-rule-resolver.test.ts src/db/systems-catalog-read.test.ts src/db/orrery-system-read.test.ts src/screens/SystemsManagementScreen.test.tsx src/db/import-session-dao.test.ts</automated></verify>
  <done>The deletion aggregate has composable, tested primitives and canonical System validity semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the full preview, stale-confirmation guard, deletion aggregate, and rollback matrix</name>
  <reversibility rating="costly">The cross-subsystem destructive contract has many consumers, though it remains replaceable behind the category DAO without a schema migration.</reversibility>
  <read_first>src/db/categories-dao.ts, src/db/contacts-dao.ts, src/db/import-session-dao.ts, src/db/systems-dao.ts, src/db/profile-presentation-dao.ts, src/db/app-settings-dao.ts, src/db/tombstones-dao.ts, src/db/migrations/001-initial.ts, src/db/migrations/012-import-sessions.ts, src/db/migrations/019-dashboard-prefs.ts, src/db/migrations/021-orrery-preferences.ts, src/db/migrations/022-orrery-systems.ts, src/db/migrations/profile-presentation.ts</read_first>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts</files>
  <behavior>- Preview counts all contacts including archived/Unbound; imports separately by pending/complete/discarded; rules/Systems; category prefs/overrides; Profile assignments; Dashboard filter; and any audited durable reference.
- Final call rereads source, target, and complete fingerprint under the lock; disappeared/changed state returns refreshed preview and performs zero writes.
- Success repairs contacts/imports, rules, ref state, active selection, Profile presentation, Dashboard filter, order, tombstone, then deletes parent last and bumps revision once.
- An injected throw after each named stage restores every affected table and leaves data_revision unchanged.</behavior>
  <action>Implement one `inWriteTransaction` aggregate with checked set-based updates and exact row/count assertions. Interactive import reassignment applies identically to pending, complete, and discarded sessions using the selected surviving category or null. Dashboard filter cleanup removes only the deleted numeric ID and removes the family if empty; active category System falls back to `builtin:all-contacts`; presentation is deleted rather than transferred. Add a test-only dependency/callback seam in the DAO test contract for stage injection—no alternate database, executor override, dev route, or production test infrastructure.</action>
  <acceptance_criteria>Success leaves no dangling durable category identity and exactly one revision bump; every stale-preview and injected-stage rejection produces zero persistent change.</acceptance_criteria>
  <verify><automated>npx vitest run src/db/categories-dao.test.ts src/db/systems-dao.test.ts src/logic/system-rule-resolver.test.ts</automated></verify>
  <done>Atomic deletion and its complete rollback proof are green.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-04 | Tampering: TOCTOU preview | high | mitigate | full fingerprint equality and target validation inside the writer lock |
| T-37.1-05 | Tampering/DoS: partial delete | high | mitigate | one non-reentrant-safe transaction, parent last, exhaustive injected-failure snapshots |
| T-37.1-06 | Information disclosure | medium | mitigate | preview/error surfaces expose aggregate counts and identifiers, never private rows |
</threat_model>
<verification>Focused suites plus typecheck; explicitly assert TARGET_VERSION=29 and no migration 030.</verification>
<success_criteria>Permanent deletion is truthful, atomic, identity-safe, all-status-import-safe, and rollback-complete.</success_criteria>
## Artifacts this phase produces
- Canonical deletion preview/transaction and System validity behavior.
<output>Create `.planning/phases/37.1-category-management/37.1-02-SUMMARY.md` when done.</output>
