---
phase: 37.1-category-management
plan: 02
type: tdd
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
  - src/db/tombstones-dao.ts
  - src/db/tombstones-dao.test.ts
  - docs/systems/persistence-core.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Delete preview counts every affected durable category reference; the lightweight path is allowed only when every fallout class is zero."
    - "Final deletion revalidates the complete preview and target under the writer lock; changed fallout, removed source, or stale target produces zero writes and a refreshed preview."
    - "A successful delete repairs every numeric FK, UID/ref token, JSON preference, and Profile assignment; writes durable category tombstone evidence; deletes the category last; and advances data_revision exactly once."
    - "A fault after any deletion stage restores byte-for-byte/logically equal pre-operation state and leaves data_revision unchanged."
  artifacts:
    - path: src/db/categories-dao.ts
      provides: "Typed deletion preview/fingerprint and the canonical atomic category deletion aggregate"
    - path: src/db/categories-dao.test.ts
      provides: "Full-chain fixture and per-stage fault-injection rollback matrix"
  key_links:
    - from: src/db/categories-dao.ts
      to: src/db/transaction.ts
      via: "one outer inWriteTransaction with non-mutexed cores only"
    - from: src/db/categories-dao.ts
      to: src/db/tombstones-dao.ts
      via: "insertTombstoneCore(category, bumpRevision:false) before final category-row delete"
---

<objective>
Specify and implement the category deletion aggregate as one rollback-proven local transaction.

Purpose: make the core lookup entity safely mutable without partial fallout, stale confirmations, nested-writer deadlocks, resurrection, or revision overcount.
Output: full preview/fingerprint contract, atomic delete API, category tombstone vocabulary, and exhaustive failure matrix.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/dossier/milestone-2/phase-37.1-category-management-dossier.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-VALIDATION.md
@docs/decisions/ADR-056-tombstone-backed-uid-reconciliation-for-portable-restores.md
@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md
</context>

<feature>
  <name>Atomic category deletion with lock-time preview equality</name>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/db/tombstones-dao.ts, src/db/tombstones-dao.test.ts</files>
  <behavior>
    RED: create a full-migration SQLite fixture containing archived and Unbound contacts, pending/complete/discarded import sessions, mixed/sole category rules, include-only and exclude-only overrides, category prefs/overrides, active category System, Profile presentation, Dashboard filters with other families, gapped category order, and tombstone collision. Assert preview and every stale/success/failure outcome before implementation.

    GREEN: implement the aggregate with one writer lock and these ordered stages: lock-time preview+target reread/equality; contacts reassignment; pending-import default reassignment only; matching category-rule deletion; `category:<uid>` override deletion; matching pref deletion; active selection fallback through `updateAppSettingsCore`; Profile presentation deletion; Dashboard-filter cleanup through the pure helper and `updateAppSettingsCore`; contiguous order normalization; category tombstone insertion with its own bump disabled; exact category-row deletion last; one final `bumpDataRevisionCore`.

    Cases: source/target missing or any decision-relevant count/identity change → stale result, refreshed preview, no writes. Target may be null/Uncategorized or another current non-source category. Last real category may be deleted. Successful operation preserves unrelated rows/rules/settings and returns committed aggregate facts without private record contents.
  </behavior>
  <implementation>Use private/public transaction-composable cores and bound set-based SQL in `categories-dao`; never call public `setSessionBatchCategory`, `setSystemRules`, `updateAppSettings`, `bulkSetCategory`, or profile-assignment wrappers inside the outer transaction. Add `category` to the closed tombstone TypeScript/assertion vocabulary without schema DDL. Expose an injectable fault hook limited to tests so each stage can throw. Compare complete table snapshots plus `data_revision` for every injected failure. Keep runtime deletion permanent with tombstone evidence only—no field_history, history UI, Undo, quarantine, recovery window, migration, or reseed.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — lock the full preview, success, stale, and rollback matrix</name>
  <reversibility rating="reversible">This task adds tests and fixture-only fault injection seams; it transforms no production data.</reversibility>
  <files>src/db/categories-dao.test.ts, src/db/tombstones-dao.test.ts</files>
  <read_first>
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full Plan-01 files)
    - src/db/migrations/001-initial.ts, 007-tombstones.ts, 012-import-sessions.ts, 019-dashboard-prefs.ts, 021-orrery-preferences.ts, 022-orrery-systems.ts, and profile-presentation.ts (full shipped schema; do not edit)
    - src/db/contacts-dao.ts and src/db/recency-dao.ts (full current contacts.category_id writers)
    - src/db/import-session-dao.ts (full only import_sessions writer)
    - src/db/systems-dao.ts (full rules/overrides/prefs writer)
    - src/db/profile-presentation-dao.ts (full presentation writer)
    - src/db/app-settings-dao.ts (full app_settings writers; use updateAppSettingsCore only)
    - src/db/tombstones-dao.ts and src/db/transaction.ts (full tombstone/revision/non-reentrant transaction contract)
    - src/backup/restore-apply.ts (full alternate writer for every affected table; do not assume the runtime DAO is the only writer)
    - src/db/field-defs-dao.test.ts (full rollback/reorder test precedent)
  </read_first>
  <action>
Before production changes, build the full-chain fixture and expected preview/fingerprint. Snapshot every affected table plus `data_revision`. Add one test per mandated fault stage: contacts, pending imports, rules, category overrides, category prefs, active fallback, Profile assignment, Dashboard filter, order normalization, tombstone, final category delete. Each rejection must compare the complete state with the pre-call snapshot and prove unchanged revision. Add stale-preview cases for changed counts, removed source, removed/changed target, and a success case proving the exact final graph and one revision increment. Include all-null target, delete-to-category, zero-category result, archived/Unbound count inclusion, complete/discarded import preservation, and tombstone max-timestamp behavior.
  </action>
  <verify>
    <automated>npx vitest run src/db/categories-dao.test.ts src/db/tombstones-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New behavior tests fail for missing aggregate behavior before GREEN implementation.
    - Exactly eleven fault points are covered, and each assertion includes every affected table plus data_revision.
    - The fixture distinguishes pending from complete/discarded imports and positive include from exclude-only System overrides.
  </acceptance_criteria>
  <done>The destructive contract is executable and exhaustive before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the one-lock category deletion aggregate</name>
  <reversibility rating="costly">The runtime delete is intentionally permanent and tombstone-backed; undoing committed user deletions would require importing an external backup, though the implementation adds no schema or wire-format change.</reversibility>
  <files>src/db/categories-dao.ts, src/db/tombstones-dao.ts, docs/systems/persistence-core.md</files>
  <read_first>
    - src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/logic/category-logic.ts, src/logic/category-logic.test.ts (full current RED contract)
    - src/db/transaction.ts, src/db/tombstones-dao.ts, src/db/data-revision-dao.ts, src/db/app-settings-dao.ts (full composing primitives)
    - docs/systems/persistence-core.md (full living transaction/schema contract; append Changelog)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§6, 10–11
  </read_first>
  <action>
Implement the typed preview, deterministic fingerprint/equality, stale result, and atomic delete precisely as the feature block orders it. All dynamic values are parameters; any placeholder list is generated only from a closed row count. Require exact affected-row equality against the locked preview so concurrent drift throws/returns stale rather than partially proceeding. Delete the category row only after all repairs and tombstone insertion; perform exactly one final revision bump. Add `category` to `TombstoneEntityType` and its assertion. Keep category names/IDs/rules out of error logs except safe local IDs and aggregate status. Update `persistence-core.md` to describe the runtime aggregate, category tombstone, no-migration decision, non-nesting cores, and append its Changelog.
  </action>
  <verify>
    <automated>npx vitest run src/db/categories-dao.test.ts src/db/tombstones-dao.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx biome check src/db/categories-dao.ts src/db/categories-dao.test.ts src/db/tombstones-dao.ts src/db/tombstones-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All rollback, stale, success, zero-taxonomy, and exactly-one-revision tests pass.
    - The aggregate contains one outer `inWriteTransaction` and calls no public transactional writer within it.
    - Every fallout class is included in both preview and lock-time reread; unused means all classes zero.
    - No migration, TARGET_VERSION change, backup-format bump, field_history write, Undo, quarantine, recovery window, or reseed is introduced.
  </acceptance_criteria>
  <done>The deletion aggregate is atomic, TOCTOU-safe, privacy-safe, tombstone-backed, and rollback-proven after every stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Preview UI snapshot → final DAO call | Potentially stale category/target/count data crosses into a destructive action. |
| DAO aggregate → multiple SQLite tables | One logical delete crosses several FK/TEXT/JSON persistence forms. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-05 | Tampering | Preview/confirm TOCTOU | high | mitigate | Reread source, target, and complete fingerprint under the writer lock; changed state returns refreshed stale preview with zero writes. |
| T-37.1-06 | Tampering / DoS | Multi-table deletion | high | mitigate | One non-nested transaction, exact row-count guards, dependency-first order, category last, and fault injection after every stage. |
| T-37.1-07 | Tampering | Backup resurrection evidence | high | mitigate | Insert indefinite category tombstone in the same transaction with its local bump disabled; aggregate owns one final bump. |
| T-37.1-08 | Information disclosure | Fallout/error diagnostics | medium | mitigate | Typed aggregate counts and safe status only; no contact names, rule values, profile data, or backup content. |
| T-37.1-09 | Tampering | SQL input | high | mitigate | Parameter-bind source ID, target ID, UID, timestamps, and JSON; generate identifiers only from closed code. |

Package legitimacy: N/A — no package install.
</threat_model>

<artifacts>
## Artifacts this phase produces

- `CategoryDeletionPreview`, its complete fallout-count type, deterministic fingerprint, stale result, and public deletion API.
- Transaction-composable category deletion cores and test-only stage fault hook.
- `category` member in the closed tombstone vocabulary.
- Exhaustive rollback/stale/success SQLite test fixture.
</artifacts>

<verification>
Focused DAO/logic/tombstone tests must pass in under the normal targeted-feedback budget; then run typecheck and Biome. Confirm migration and backup version files are untouched.
</verification>

<success_criteria>
Deletion repairs the complete live graph atomically, stale confirmation cannot mutate, every injected failure rolls back to complete equality, and success produces one tombstone plus one revision increment with the category deleted last.
</success_criteria>

<output>
Create `.planning/phases/37.1-category-management/37.1-02-SUMMARY.md` when done.
</output>
