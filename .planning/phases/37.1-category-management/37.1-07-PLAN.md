---
phase: 37.1-category-management
plan: 07
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-05"]
files_modified:
  - src/components/orrery/SystemRuleAccordion.tsx
  - src/screens/SystemBuilderScreen.tsx
  - src/screens/SystemBuilderScreen.test.tsx
  - src/components/orrery/orrery-controls-logic.ts
  - src/components/orrery/orrery-controls-logic.test.ts
  - src/components/orrery/OrrerySystemSelector.tsx
  - src/stores/orrery-system-store.ts
  - src/stores/orrery-system-store.test.ts
  - src/screens/orrery-screen-framing.test.ts
autonomous: true
requirements: []
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "System Builder exposes every real category in canonical order, adds search at 13+, and filtering never clears hidden selections."
    - "Category rename preserves UID-backed rule selection; deletion removes only the matching rule and presents canonical Needs Attention where appropriate."
    - "Orrery selector contains exactly Built-in, Categories, and Custom Systems collapsible groups with existing row/count/status semantics intact."
    - "The selected group alone opens on each selector open; state is session-only, Categories use display_order, empty sections remain visible, and stale selection falls back safely."
  artifacts:
    - { path: src/components/orrery/SystemRuleAccordion.tsx, provides: "Complete searchable category-rule editor" }
    - { path: src/components/orrery/orrery-controls-logic.ts, provides: "Exact three-group projection and per-open state" }
    - { path: src/components/orrery/OrrerySystemSelector.tsx, provides: "Accessible grouped selector" }
  key_links:
    - { from: src/screens/SystemBuilderScreen.tsx, to: src/logic/system-rule-resolver.ts, via: "UID rules and canonical Needs Attention" }
    - { from: src/components/orrery/OrrerySystemSelector.tsx, to: src/stores/orrery-system-store.ts, via: "selected ref and missing-category fallback" }
---

<objective>Integrate mutable categories into System authoring and deliver the approved bounded Orrery selector grouping.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-05-SUMMARY.md
@docs/decisions/ADR-104-durable-orrery-preferences-and-live-system-scope.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete searchable Category rules with hidden-selection retention</name>
  <read_first>src/components/orrery/SystemRuleAccordion.tsx, src/screens/SystemBuilderScreen.tsx, src/db/systems-dao.ts, src/logic/system-rule-resolver.ts</read_first>
  <files>src/components/orrery/SystemRuleAccordion.tsx, src/screens/SystemBuilderScreen.tsx, src/screens/SystemBuilderScreen.test.tsx</files>
  <behavior>- Real-category UID choices remain complete and canonical; zero shows `No categories available`; 13+ adds `Search categories`.
- Search filters rows only; selected values remain in the complete draft and persistent selected count.
- Rename changes label only; deleted UID rule disappears; remaining positive rule/include stays valid, while empty/exclude-only becomes Needs Attention.</behavior>
  <action>Apply the shared real-only catalog/search policy to the Category rule family without chips, nested management, cutoffs, or inline Add. Keep stable UIDs in drafts/storage and consume the canonical validity status from Plan 02.</action>
  <acceptance_criteria>System Builder remains complete and truthful across large catalogs, rename, delete, hidden selection, and zero categories.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/SystemBuilderScreen.test.tsx src/logic/system-rule-resolver.test.ts src/db/systems-dao.test.ts</automated></verify>
  <done>Category-rule authoring honors the mutable taxonomy contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render exact per-open three-group Orrery selector and safe fallback</name>
  <read_first>src/components/orrery/orrery-controls-logic.ts, src/components/orrery/OrrerySystemSelector.tsx, src/stores/orrery-system-store.ts, src/db/systems-catalog-read.ts</read_first>
  <files>src/components/orrery/orrery-controls-logic.ts, src/components/orrery/orrery-controls-logic.test.ts, src/components/orrery/OrrerySystemSelector.tsx, src/stores/orrery-system-store.ts, src/stores/orrery-system-store.test.ts, src/screens/orrery-screen-framing.test.ts</files>
  <behavior>- Exact group order Built-in/Categories/Custom Systems; existing choice counts, severity, overrides, radio selection, Manage Systems, busy/error, scroll and focus remain.
- Category group follows display_order; Custom/Built-in retain established order; empty Categories/Custom groups show exact copy.
- On each closed→open transition only selected group starts expanded; session toggles reset next open and are not persisted.
- Deleting selected category resolves both durable and already-open state to `builtin:all-contacts`; reduced motion removes decorative disclosure animation only.</behavior>
  <action>Project the existing ordered choices into three typed groups and add accessible 44px disclosure headers to the current anchored GlassSurface selector. Reuse existing missing-System fallback and transient focus contract. Do not add selector search, favorites, recents, truncation, persistence, or broader IA changes.</action>
  <acceptance_criteria>The narrow selector cleanup is exact, bounded, accessible, reduced-motion aware, and identity-safe.</acceptance_criteria>
  <verify><automated>npx vitest run src/components/orrery/orrery-controls-logic.test.ts src/stores/orrery-system-store.test.ts src/screens/orrery-screen-framing.test.ts</automated></verify>
  <done>System Builder and Orrery fully consume the mutable category model.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-19 | Tampering: hidden selection loss | high | mitigate | selected state derived from complete draft, never filtered rows |
| T-37.1-20 | DoS: stale selected category ref | high | mitigate | durable cleanup plus store fallback to closed safe token |
| T-37.1-21 | DoS/accessibility exclusion | medium | mitigate | bounded scroll, visible empty groups, semantic disclosure state, reduced motion |
</threat_model>
<verification>Focused System/Orrery tests, typecheck, Biome, and color check.</verification>
<success_criteria>Category Systems remain stable by UID and all System consumers stay usable after mutation.</success_criteria>
## Artifacts this phase produces
- Searchable System Builder category family and exact three-group Orrery selector.
<output>Create `.planning/phases/37.1-category-management/37.1-07-SUMMARY.md` when done.</output>
