---
phase: 37.1-category-management
plan: 15
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-03", "37.1-07"]
files_modified:
  - src/components/orrery/orrery-controls-logic.ts
  - src/components/orrery/orrery-controls-logic.test.ts
  - src/components/orrery/OrrerySystemSelector.tsx
  - src/stores/orrery-system-store.ts
  - src/stores/orrery-system-store.test.ts
  - src/screens/orrery-screen-framing.test.ts
  - docs/systems/orrery.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The Orrery selector has exactly Built-in, Categories, Custom Systems groups while preserving counts, status, selection, focus, Manage Systems, and bounded scrolling."
    - "Only the selected group opens on each fresh selector session; expansion is never persisted."
    - "Disclosure changes honor the live reduced-motion setting and deleted category selection falls back to All Contacts without blank/crash."
  artifacts:
    - path: src/components/orrery/orrery-controls-logic.ts
      provides: "Three-group projection, per-open initialization, and motion-mode model"
  key_links:
    - from: src/components/orrery/OrrerySystemSelector.tsx
      to: src/theme/use-reduced-motion.ts
      via: "live non-Skia disclosure motion choice"
---

<objective>Deliver the approved narrow Orrery grouping as an independently executable integration, including reduced-motion and missing-category parity.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Model exact groups, per-open state, and safe selection fallback</name>
  <files>src/components/orrery/orrery-controls-logic.ts, src/components/orrery/orrery-controls-logic.test.ts, src/stores/orrery-system-store.ts, src/stores/orrery-system-store.test.ts</files>
  <read_first>
    - src/components/orrery/orrery-controls-logic.ts and test (full ordering/status/transient logic)
    - src/stores/orrery-system-store.ts and test (full requested/committed/stale fallback paths)
    - src/db/systems-catalog-read.ts, src/db/orrery-system-read.ts, src/logic/orrery-system-logic.ts (full catalog identity/order)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §9
  </read_first>
  <behavior>
    - Exact group order/labels and empty groups; only selected group initializes expanded on every closed→open transition.
    - Row order/status/count/overrides stay unchanged; missing category uses `builtin:all-contacts` parity with missing custom.
  </behavior>
  <action>Create typed pure grouping and expansion initialization helpers over existing choices; keep session expansion outside app_settings and durable Zustand state. Extend stale-category resolution to the established All Contacts path while retaining meaningful feedback. Add group/order/empty/per-open reset and category/custom fallback parity tests; do not add search, favorites, recents, truncation, or new preference state.</action>
  <verify><automated>npx vitest run src/components/orrery/orrery-controls-logic.test.ts src/stores/orrery-system-store.test.ts src/db/systems-catalog-read.test.ts</automated></verify>
  <acceptance_criteria>
    - Group/order/empty/reset contracts and stale fallback are directly asserted.
    - No expansion preference or broader selector IA enters durable state.
  </acceptance_criteria>
  <done>The selector has a pure stable grouping/session model and safe category fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render accessible reduced-motion-aware disclosures</name>
  <files>src/components/orrery/OrrerySystemSelector.tsx, src/screens/orrery-screen-framing.test.ts, docs/systems/orrery.md</files>
  <read_first>
    - src/components/orrery/OrrerySystemSelector.tsx (full overlay/focus/Back/busy/error/row behavior)
    - src/theme/use-reduced-motion.ts and src/theme/use-reduced-motion.test.ts (full live OS contract)
    - src/screens/orrery-screen-framing.test.ts (full source contract)
    - docs/systems/orrery.md (full living contract; append Changelog)
  </read_first>
  <behavior>
    - Headers are 44px disclosure buttons with expanded state, count, chevron, and exact empty copy.
    - Ordinary motion may use the established restrained transition; reduced motion updates disclosure content immediately while preserving focus, meaning, and controls.
  </behavior>
  <action>Render the three groups inside the existing anchored GlassSurface/ScrollView and consume `useReducedMotion()` for React-tree disclosure changes. Under reduced motion avoid animated layout/rotation/crossfade; switch expanded content and chevron state immediately. Preserve existing focus, close, busy/error, selection, row indicators, Manage Systems, and bounded scroll. Add source/model tests proving the hook is used and both modes produce identical semantic expanded state/content. Update `orrery.md` with grouping, per-open state, reduced-motion behavior, fallback parity, and a Phase 37.1 Changelog row.</action>
  <verify><automated>npx vitest run src/components/orrery/orrery-controls-logic.test.ts src/stores/orrery-system-store.test.ts src/screens/orrery-screen-framing.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Exact groups, semantics, empty copy, per-open reset, and reduced-motion branches are tested.
    - Existing selector semantics remain green and Orrery doc has a Changelog entry.
  </acceptance_criteria>
  <done>The grouped Orrery selector is accessible, motion-sensitive, identity-safe, and narrowly scoped.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted/stale ref → selector state | Deleted category ref must not create blank or wrong membership. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-45 | Denial of service | Stale category selection | high | mitigate | Durable repair plus in-session All Contacts fallback tests. |
| T-37.1-46 | Denial of service | Motion-sensitive disclosure | medium | mitigate | Live reduced-motion hook and semantic-equivalence tests. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Exact typed Orrery selector groups and per-open expansion model.
- Reduced-motion-aware disclosure UI and missing-category fallback parity.
- Updated Orrery living contract.
</artifacts>
<verification>Focused Orrery/store/catalog tests, typecheck, colors, and changed-file Biome pass.</verification>
<success_criteria>The approved Orrery grouping works with stable category identity, safe deletion fallback, and live reduced-motion behavior.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-15-SUMMARY.md` when done.</output>
