---
phase: 37.1-category-management
plan: 16
type: tdd
wave: 4
depends_on: ["37.1-02", "37.1-04", "37.1-07"]
files_modified:
  - src/logic/dashboard-query-logic.ts
  - src/logic/dashboard-query-logic.test.ts
  - src/stores/dashboard-query-store.ts
  - src/stores/dashboard-query-store.test.ts
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "One closed durable token represents Dashboard Uncategorized; malformed values never enter SQL or persisted state."
    - "Null-only and mixed ID+null filters emit bound, closed predicates; an empty sanitized category family means established unfiltered behavior."
    - "Store persistence/hydration and backup validation preserve the valid token and reject malformed category-filter values."
  artifacts:
    - path: src/logic/dashboard-query-logic.ts
      provides: "Closed Uncategorized token, parser/sanitizer, and null-aware SQL builder"
  key_links:
    - from: src/backup/backup-schema.ts
      to: src/logic/dashboard-query-logic.ts
      via: "closed Dashboard filter parser"
---

<objective>Make explicit Uncategorized filtering real and safe at the pure query, durable store, and backup-validation boundaries before UI integration.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-04-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md</context>

<feature>
  <name>Closed null-category Dashboard filter semantics</name>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts</files>
  <behavior>
    - token: export `DASHBOARD_UNCATEGORIZED_TOKEN = "uncategorized"`; no other string means null.
    - null-only → `c.category_id IS NULL`, no params; IDs-only → bound IN; mixed → one parenthesized OR of IN and IS NULL.
    - malformed/duplicate/zero/negative/unsafe IDs drop; if the category family has no valid values it contributes no WHERE group while other families remain unchanged.
  </behavior>
  <implementation>Extend the existing closed parser/builder rather than interpolating values or inventing a second filter model. Preserve within-family OR and across-family AND. Canonicalize persisted values deterministically and keep the Dashboard store as the single durable authority.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement null-only/mixed SQL and durable canonicalization</name>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts</files>
  <read_first>
    - src/logic/dashboard-query-logic.ts and src/logic/dashboard-query-logic.test.ts (full current positive-numeric-only builder/parser)
    - src/stores/dashboard-query-store.ts and src/stores/dashboard-query-store.test.ts (full persist/hydrate/reset contract)
    - src/db/dashboard-read.ts and src/db/dashboard-read.test.ts (full consumer of buildFilterWhere)
    - src/logic/category-logic.ts (full deleted-ID sanitizer and null policy)
  </read_first>
  <action>Write failing boundary tests, then add the exact single closed token `DASHBOARD_UNCATEGORIZED_TOKEN = "uncategorized"` and canonical category-filter parser/sanitizer. Generate only constant SQL fragments with bound numeric IDs. Cover null-only, ID-only, mixed, duplicates, malformed tokens, empty family, other-family preservation, hydration, persistence, reset, and deletion sanitization. An empty/invalid category family must not emit a false predicate because the established Dashboard meaning is unfiltered for that family.</action>
  <verify><automated>npx vitest run src/logic/dashboard-query-logic.test.ts src/stores/dashboard-query-store.test.ts src/db/dashboard-read.test.ts</automated></verify>
  <acceptance_criteria>
    - Null-only, mixed, malformed, persistence, hydration, deletion, and empty-family behavior are directly asserted.
    - SQL contains no runtime token interpolation and parameter order is deterministic.
  </acceptance_criteria>
  <done>Dashboard query/store semantics can represent Uncategorized safely and durably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the closed token at backup validation</name>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <read_first>
    - src/backup/backup-schema.ts and src/backup/backup-schema.test.ts (full appSettings/dashboardFilters parse-before-mutate path after Plan 04)
    - src/logic/dashboard-query-logic.ts and test (Task-1 closed parser)
    - src/backup/types.ts (full unchanged v5 settings representation)
  </read_first>
  <action>Route v5 `appSettings.dashboardFilters` validation through the closed Dashboard parser without changing the wire field or backup version. Accept positive numeric category strings plus the exact Uncategorized token, reject malformed category tokens/shape before apply, and preserve all other valid filter families. Add round-trip/invalid/empty-family tests and prove `BACKUP_FORMAT_VERSION` remains 5.</action>
  <verify><automated>npx vitest run src/backup/backup-schema.test.ts src/logic/dashboard-query-logic.test.ts src/stores/dashboard-query-store.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Valid null token survives parsing; malformed tokens reject before mutation.
    - No backup-format or type-shape bump occurs.
  </acceptance_criteria>
  <done>The portable settings boundary recognizes exactly the same closed category-filter values as runtime.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted/backup JSON → SQL builder | Crafted strings must not become SQL or ambiguous filter state. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-47 | Tampering | Dashboard category tokens | high | mitigate | Closed parser, bound IDs, constant null predicate, malformed backup rejection. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Closed Dashboard Uncategorized token and null-aware SQL semantics.
- Store persistence/hydration and v5 backup validation coverage.
</artifacts>
<verification>Focused query/store/backup tests, typecheck, and changed-file Biome pass.</verification>
<success_criteria>Uncategorized filtering works alone or with real categories and cannot be widened by malformed durable input.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-16-SUMMARY.md` when done.</output>
