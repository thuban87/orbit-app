---
phase: 37.1-category-management
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
autonomous: true
gap_closure: true
requirements: [] # Spec-less probe: Phase 37.1 has no assigned requirement IDs.
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Category manager load/readback returns an explicit current-success, stale, or failure outcome to its mutation callers."
    - "After create, rename, or delete commits but readback fails, the manager enters an explicit readback-pending state, shows the approved locked load/readback error, and publishes neither success nor shell refresh."
    - "While readback is pending, mutation submission and destructive replay are disabled; Try again performs only canonical readback and never repeats create, rename, or delete."
    - "A successful readback-only retry reconciles rows/count, closes the retained editor/destructive UI, and publishes the original operation's approved success feedback exactly once."
    - "Reorder remains consistent with its actual no-readback commit boundary: successful DAO completion commits the submitted order, while DAO rejection restores the prior order."
  artifacts:
    - { path: src/screens/CategoryManagementScreen.tsx, provides: "Explicit manager readback outcome and truthful mutation transitions" }
    - { path: src/screens/CategoryManagementScreen.test.tsx, provides: "Wired create/rename/delete post-commit readback state, retry-only, call-count, and reorder regressions" }
  key_links:
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/db/categories-dao.ts, via: "write result followed by explicit `listCategoriesForManagement` readback outcome" }
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/stores/snackbar-store.ts, via: "success publication only after current successful readback" }
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/db/categories-dao.ts, via: "readback-only retry cannot re-enter mutation DAO" }
---

<objective>Model committed-write/readback-pending explicitly so retry reconciles SQLite truth without replaying a mutation, then publishes success exactly once.</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-category-management/37.1-VERIFICATION.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-04-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Return an explicit management readback outcome</name>
  <read_first>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx, src/db/categories-dao.ts, src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx</read_first>
  <files>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <behavior>- A current successful read updates committed rows/count and returns success.
- A current rejected read retains committed rows and returns failure without pretending the already-committed mutation was rejected.
- An older superseded read returns stale and cannot be mistaken for a successful readback by a mutation handler.</behavior>
  <action>Refactor the existing guarded `load` into an explicit typed outcome rather than a catch-and-resolve void function. Preserve generation protection, initial-loading behavior, and committed rows. Add a typed post-commit/readback-pending record containing only the operation kind, approved success label, and the UI finalization needed after reconciliation; it must contain no callable mutation or mutation arguments. Initial/focus callers retain existing load handling, while mutation callers can distinguish current success, stale, and failure. Use the already approved exact load/readback copy from UI-SPEC — “Couldn’t load categories. Try again.” with “Try again” — because the write did commit and the locked Save error's claim that changes were not applied is false here. Export a pure coordinator/state machine only if it is the one invoked by real handlers.</action>
  <acceptance_criteria>No mutation handler can proceed to success publication merely because `load()` resolved after swallowing a rejection or stale completion.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx</automated></verify>
  <done>The manager has a tested, explicit readback contract suitable for every mutation flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile post-commit failures through readback-only retry</name>
  <read_first>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx, src/stores/snackbar-store.ts, src/stores/shell-refresh-store.ts</read_first>
  <files>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <behavior>- After a successful create or rename DAO call plus rejected readback, the editor/value remain visible, the approved load/readback error and Try again action appear, mutation submit/cancel replay paths are disabled, and neither success snackbar nor shell refresh fires.
- After a successful delete DAO call plus rejected readback, the retained destructive UI cannot submit the deletion again; the approved load/readback error and Try again action appear, with no success publication.
- Try again calls only `listCategoriesForManagement` and `countUncategorizedContacts`; it never makes a second create/rename/delete DAO call. On success it reconciles, closes the retained UI, clears pending state, bumps shell refresh, and publishes the original approved success label once.
- Repeated readback failure remains pending and repeatable without mutation replay; focus refresh cannot silently publish success without consuming/finalizing the pending operation.
- Reorder is explicitly tested at its real boundary: no list readback follows success; DAO rejection restores committedRows and shows REORDER_ERROR.</behavior>
  <action>Wire create, rename, and delete handlers (or a pure state machine directly invoked by them) so the mutation is called once, then control moves irreversibly for that operation into readback-pending until canonical readback succeeds. Render the UI-SPEC's approved load error and Try again control in the retained editor/delete surface, disable mutation and dismissal controls that could cause replay, and make retry invoke readback only. A current successful retry performs the operation-specific closure and approved success publication; ensure focus-triggered loads use the same pending finalizer or leave the explicit retry in control, never an ambiguous half-state. Test the wired handler/state-machine boundary with mocked create/rename/delete, list readback, snackbar, shell refresh, editor/overlay state, and exact mutation/read call counts. Keep reorder's direct-commit/rollback contract because it has no post-write list readback.</action>
  <acceptance_criteria>Component-flow tests for create, rename, and delete each prove one mutation call across one or more failed readbacks, zero success publications until readback succeeds, then one closure/shell refresh/snackbar; reorder retains its tested boundary.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx && npx tsc --noEmit && npm run check:colors</automated></verify>
  <done>Every manager mutation reports only the truth it can prove from committed SQLite readback.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed SQLite write → UI readback/publication | A successful write does not prove that the currently rendered catalog was refreshed. |

## STRIDE Threat Register — ASVS L1, blocking threshold: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-G13-01 | Repudiation | success snackbar/announcement | medium | mitigate | Publish only after a current successful canonical readback. |
| T-37.1-G13-02 | Tampering | stale read completion | medium | mitigate | Typed stale outcome prevents mutation success transition. |
| T-37.1-G13-03 | Denial of Service | repeated mutation retry | high | mitigate | Post-commit state has no mutation callback/arguments; retry performs canonical readback only. |
| T-37.1-G13-04 | Information Disclosure | error UI | low | mitigate | Approved generic load/readback error exposes no category/contact contents. |
</threat_model>

<verification>Run manager component/model tests, typecheck, color gate, and Biome on both changed files. Spec-less probe skip: Phase 37.1 has no requirement IDs.</verification>
<success_criteria>Manager success UI means one mutation and a current canonical readback succeeded; post-commit failures are recoverable by readback alone and can never replay the mutation.</success_criteria>

## Artifacts this phase produces
- A typed internal management-load outcome plus post-commit/readback-pending state (exact symbol names chosen during implementation); no new file.

<output>Create `.planning/phases/37.1-category-management/37.1-13-SUMMARY.md` when done.</output>
