---
phase: 37.1-category-management
plan: 13
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/stores/snackbar-store.ts
  - src/stores/snackbar-store.test.ts
  - src/components/Snackbar.tsx
  - src/components/Snackbar.test.tsx
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The shared shell Snackbar supports actionless messages without regressing existing one- and two-action callers."
    - "Category Add publishes exact actionless success only after the canonical SQLite commit; failures retain input and publish no success."
  artifacts:
    - path: src/components/Snackbar.tsx
      provides: "Actionless shared shell rendering with no empty action slot"
  key_links:
    - from: src/screens/CategoryManagementScreen.tsx
      to: src/stores/snackbar-store.ts
      via: "post-commit Category added publication"
---

<objective>
Complete the tracer's committed feedback through the existing shared shell Snackbar without keeping shared-shell changes inside the broad route/DAO/UI tracer plan.
</objective>

<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the shared Snackbar store and renderer for actionless messages</name>
  <files>src/stores/snackbar-store.ts, src/stores/snackbar-store.test.ts, src/components/Snackbar.tsx, src/components/Snackbar.test.tsx</files>
  <read_first>
    - src/stores/snackbar-store.ts and src/stores/snackbar-store.test.ts (full current message/action/timer state contract)
    - src/components/Snackbar.tsx and src/components/Snackbar.test.tsx (full host/live-region/action layout contract)
    - src/components/ui/AppText.tsx and src/theme/tokens/typography.ts (full typography roles)
  </read_first>
  <behavior>
    - Existing one- and two-action messages retain callbacks, order, and timing.
    - Actionless messages render label only, reserve no action container, remain a polite live region, and auto-dismiss after 6000ms.
  </behavior>
  <action>Make `SnackbarMessage.action` optional while preserving existing callers and secondary-action invariants. Render action containers only when present, route text through established AppText roles, and retain the single shell host/timer/live-region path. Do not create a category-only toast or a second timer.</action>
  <verify><automated>npx vitest run src/stores/snackbar-store.test.ts src/components/Snackbar.test.tsx</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Actionless, one-action, and two-action cases are directly tested.
    - Actionless markup has no blank action slot and timer remains 6000ms.
  </acceptance_criteria>
  <done>The shared shell host has one backward-compatible actionless message contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish Category Add success only from committed truth</name>
  <files>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <read_first>
    - src/screens/CategoryManagementScreen.tsx and src/screens/CategoryManagementScreen.test.tsx (full Plan-01 tracer)
    - src/stores/snackbar-store.ts (Task-1 API)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§3, 10 and Copywriting Contract
  </read_first>
  <behavior>
    - Resolved create commit emits exactly `Category added.` with no action and a matching polite announcement.
    - Rejected create keeps the sheet/input/committed rows and emits no success.
  </behavior>
  <action>Wire the Add flow to the actionless shared Snackbar only after `createCategory` resolves and the committed manager state refreshes. Preserve the exact locked error/input behavior. Do not optimistically close or publish and do not add Undo.</action>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx src/stores/snackbar-store.test.ts src/components/Snackbar.test.tsx</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Exact success copy is post-commit only; rejection publishes nothing.
    - Existing manager load/add behavior from Plan 01 remains green.
  </acceptance_criteria>
  <done>Add Category completes through the shared committed-feedback path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DAO completion → shell feedback | UI must not claim a write committed before SQLite truth exists. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-42 | Repudiation | Success feedback | medium | mitigate | Publish exact success only from the resolved commit path and assert failure silence. |
Package legitimacy: N/A.
</threat_model>

<artifacts>
## Artifacts this phase produces
- Backward-compatible actionless `SnackbarMessage` support.
- Post-commit Category Add success publication.
</artifacts>
<verification>Focused Snackbar/manager tests, typecheck, colors, and changed-file Biome pass.</verification>
<success_criteria>The shared Snackbar renders committed actionless category success without regressing existing messages.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-13-SUMMARY.md` when done.</output>
