---
phase: 37.1-category-management
plan: 07
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/logic/category-logic.ts
  - src/logic/category-logic.test.ts
  - src/components/category/CategoryChoiceSheet.tsx
  - src/components/category/CategoryChoiceSheet.test.tsx
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Every category catalog remains complete and canonical; search appears at exactly 13 real categories and never acts as a maximum or reorder."
    - "Uncategorized is a policy-driven null option pinned last, never a category row; real-only consumers never synthesize it."
    - "Filtering visible rows never clears hidden selections, and stale selected identities resolve through the surface's explicit fallback."
  artifacts:
    - path: src/components/category/CategoryChoiceSheet.tsx
      provides: "Shared expanded single-select/search sheet for 13+ real categories"
  key_links:
    - from: src/components/category/CategoryChoiceSheet.tsx
      to: src/logic/category-logic.ts
      via: "one canonical ordering/search/null/stale-selection model"
---

<objective>
Build one renderer-independent category catalog policy and the shared 13+ choice Sheet used by every ordinary single-select consumer.
</objective>

<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock shared complete-catalog/search/selection policies</name>
  <files>src/logic/category-logic.ts, src/logic/category-logic.test.ts</files>
  <read_first>
    - src/logic/category-logic.ts and src/logic/category-logic.test.ts (full Plan-01 model)
    - src/logic/dashboard-query-logic.ts and src/logic/dashboard-query-logic.test.ts (full pure closed-model precedent)
    - src/db/contact-read.ts, src/db/systems-catalog-read.ts, src/db/orrery-system-read.ts (full category read projections/order)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§7–8 and Consumer Integration Matrix
  </read_first>
  <behavior>
    - Fixture sizes 0, 1, 12, 13, and many preserve `display_order, uid`; search visibility is false through 12 and true at 13.
    - Search is local case-insensitive/NFC-aware, returns complete matching rows, preserves selected IDs/UIDs outside the visible result, and clearing restores all.
    - includeUncategorized yields one null option last; realOnly yields none; stale selection returns the caller's declared null/error policy.
  </behavior>
  <action>
Expand `category-logic` with typed catalog projection/filter/selection APIs parameterized by `includeUncategorized`, `realOnly`, and single/multi selection. Keep the complete source separate from visible filtered rows and selected identities. Use canonical order and the exact threshold constant. Add closed-boundary tests including long Unicode and no-match copy. Do not add a count limit, alphabetize, persist search, or introduce inline management.
  </action>
  <verify><automated>npx vitest run src/logic/category-logic.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - 0/1/12/13/many, canonical order, null policy, hidden selection, stale identity, and Unicode search are directly tested.
    - `CATEGORY_SEARCH_THRESHOLD` remains exactly 12, meaning search renders at 13+.
  </acceptance_criteria>
  <done>All consumers can share one complete, ordered, policy-explicit category model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared expanded CategoryChoiceSheet for large single-select catalogs</name>
  <files>src/components/category/CategoryChoiceSheet.tsx, src/components/category/CategoryChoiceSheet.test.tsx</files>
  <read_first>
    - src/components/ui/Sheet.tsx, src/components/ui/AppText.tsx, src/components/ui/Button.tsx (full shared overlay/typography/action primitives)
    - src/components/control-surface/FilterPanelContent.tsx and src/components/orrery/SystemRuleAccordion.tsx (full current multi-option rendering precedents)
    - src/logic/category-logic.ts (full Task-1 API)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §7 plus Copywriting/Accessibility contracts
  </read_first>
  <behavior>
    - 13+ renders labeled Search categories field and complete scrollable radio choices; no match shows exact query copy without clearing selection.
    - Uncategorized appears last only when allowed; full names remain accessible; stale value returns null/refresh signal rather than submitting dead ID.
  </behavior>
  <action>
Create a themed expanded shared Sheet adapter for large single-select category catalogs. Accept complete categories, selected ID/null, explicit Uncategorized policy, visibility/close/select callbacks, busy/error state, and accessible labels. Use the pure model for order/filter/stale behavior, AppText/theme tokens/44px controls, and full scrollability. It must not own SQLite, create categories, persist search, or replace established 0–12 controls.
  </action>
  <verify><automated>npx vitest run src/components/category/CategoryChoiceSheet.test.tsx src/logic/category-logic.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Search/no-match/null/real-only/stale/hidden-selection/long-name contracts are covered.
    - Component contains no SQL/network and introduces no parallel name/order logic.
  </acceptance_criteria>
  <done>A single approved large-catalog choice surface is ready for all ordinary consumers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted catalog/selection → filtered UI | Filtering must not erase identities or make records inaccessible. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-23 | Denial of service | Large catalog | high | mitigate | Complete scrollable results plus local search at 13+, never truncation. |
| T-37.1-24 | Tampering | Hidden selections | high | mitigate | Separate complete selection state from visible filtered rows; direct boundary tests. |
| T-37.1-25 | Information disclosure | Search | low | accept | Search is ephemeral and entirely in-memory/on-device; no logs or network. |

Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Complete catalog/search/selection policy types and tests.
- `CategoryChoiceSheet` shared expanded large-catalog component.
</artifacts>
<verification>Focused model/component tests, typecheck, color gate, and Biome pass.</verification>
<success_criteria>No category becomes inaccessible, reordered, or silently deselected at any catalog size.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-07-SUMMARY.md` when done.</output>
