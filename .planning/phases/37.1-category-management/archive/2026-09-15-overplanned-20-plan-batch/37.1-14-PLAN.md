---
phase: 37.1-category-management
plan: 14
type: tdd
wave: 3
depends_on: ["37.1-02", "37.1-03"]
files_modified:
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Canonical `renameCategory` and `reorderCategories` public APIs exist before the manager calls them."
    - "Both operations validate under the serialized writer lock, assert exact row counts, roll back fully on failure, and bump data_revision exactly once."
    - "Capitalization-only rename excludes its own row while all other normalized category/System collisions reject."
  artifacts:
    - path: src/db/categories-dao.ts
      provides: "Canonical serialized renameCategory and reorderCategories APIs"
  key_links:
    - from: src/screens/CategoryManagementScreen.tsx
      to: src/db/categories-dao.ts
      via: "Plan 06 rename/reorder calls"
---

<objective>
Implement the concrete canonical rename and contiguous reorder APIs that the complete manager consumes.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md</context>

<feature>
  <name>Serialized category rename and reorder</name>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts</files>
  <behavior>
    - rename: source missing, invalid normalized name, category/System collision, or unexpected row count → throw and roll back with no revision change; capitalization-only self rename succeeds and retains id/uid/order.
    - reorder: submitted IDs must equal the lock-time category ID set exactly once; stale/missing/duplicate/extra IDs reject with no writes; success writes contiguous 0..N-1 and one shared timestamp, then bumps revision once.
  </behavior>
  <implementation>Use one `inWriteTransaction` per public API, transaction-composable cores, bound values, and `bumpDataRevisionCore` exactly once after all exact row-count guards pass. Reuse the shared visible-name key and all-category/System collision query from Plans 01/03. Do not rewrite UIDs, dependent refs, or names during reorder.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement lock-safe renameCategory</name>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts</files>
  <read_first>
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full current DAO and deletion fixture)
    - src/db/systems-dao.ts and src/db/systems-dao.test.ts (full shared cross-kind name validation after Plan 03)
    - src/logic/category-logic.ts and src/logic/category-logic.test.ts (full normalization/error contract)
    - src/db/data-revision-dao.ts and src/db/transaction.ts (full bump/non-reentrant contract)
  </read_first>
  <action>Write failing rename tests, then expose `renameCategory(exec,{categoryId,name,now})`. Normalize and validate before and again inside the serialized transaction; exclude only `categoryId` from category duplicate comparison, so capitalization-only self rename succeeds. Recheck built-in/custom-System collisions under the same lock. Update name/modified_at with a bound exact-one-row statement, preserve ID/UID/order/dependents, and bump revision once only after success. Inject a statement failure in tests and compare complete category/dependent state plus revision.</action>
  <verify><automated>npx vitest run src/db/categories-dao.test.ts src/db/systems-dao.test.ts src/logic/category-logic.test.ts</automated></verify>
  <acceptance_criteria>
    - Capitalization-only self rename succeeds; every other normalized cross-kind collision rejects.
    - Missing row, collision, and injected failure leave all state/revision unchanged.
    - Success preserves stable identity and bumps once.
  </acceptance_criteria>
  <done>The canonical manager rename API is concrete, serialized, identity-safe, and rollback-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lock-safe contiguous reorderCategories</name>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts</files>
  <read_first>
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (Task-1 current files)
    - src/db/field-defs-dao.ts and src/db/field-defs-dao.test.ts (full exact-row reorder precedent)
    - src/db/systems-dao.ts and src/db/systems-dao.test.ts (full stale-set reorder precedent)
  </read_first>
  <action>Write failing reorder tests, then expose `reorderCategories(exec,{orderedIds,now})`. Under one lock, read the complete current ID set, reject duplicate/missing/extra/stale submissions, update each row to its array index with the identical timestamp and exact-one-row guard, and bump revision once after the loop. Empty taxonomy with empty order is an idempotent no-write/no-bump result; nonempty no-op behavior must follow the DAO's established revision convention and be asserted. Inject failure after each index in a gapped fixture and prove original order/timestamps/revision restore.</action>
  <verify><automated>npx vitest run src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Success produces exactly contiguous positions matching the submitted canonical sequence.
    - Duplicate/missing/extra/stale IDs and every injected row failure roll back fully.
    - Exactly one revision bump occurs per successful mutating reorder.
  </acceptance_criteria>
  <done>The canonical reorder API exists with lock-time set equality, contiguous results, exact guards, and rollback coverage.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI name/order payload → serialized DAO | Stale or ambiguous values can corrupt durable taxonomy identity/order. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-43 | Spoofing | Category rename | high | mitigate | Shared normalized namespace, self exclusion by stable ID, lock-time recheck. |
| T-37.1-44 | Tampering | Category reorder | high | mitigate | Exact lock-time set equality, exact row counts, one transaction and rollback matrix. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Public `renameCategory` and `reorderCategories` APIs plus composable cores.
- Rename/reorder collision, stale-set, revision, and rollback tests.
</artifacts>
<verification>Focused category/System tests, typecheck, and changed-file Biome pass.</verification>
<success_criteria>Plan 06 consumes real canonical rename/reorder APIs whose invariants are proven independently of UI.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-14-SUMMARY.md` when done.</output>
