---
phase: 37.1-category-management
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-category-catalog-refresh.ts
  - src/hooks/use-category-catalog-refresh.test.ts
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/category-catalog-consumer-refresh.test.ts
  - src/screens/category-consumer-contract.test.ts
autonomous: true
gap_closure: true
requirements: [] # Spec-less probe: Phase 37.1 has no assigned requirement IDs.
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Create Contact, Edit Contact, and Import Review route every initial-hydration and focus category publication through one authority and retain the newest committed catalog when either read rejects."
    - "An older initial or focus completion, or a completion after blur/unmount, cannot replace a newer catalog or selected value."
    - "Successful current refreshes still reconcile selected IDs against current DAO truth."
    - "Create's definitions/settings hydration and Edit's contact/definition/knowledge hydration plus photo focus refresh remain independent of category publication ordering."
  artifacts:
    - { path: src/hooks/use-category-catalog-refresh.ts, provides: "One rejection-safe generation authority shared by initial and focus category reads" }
    - { path: src/hooks/use-category-catalog-refresh.test.ts, provides: "Initial/focus ordering, reject, blur, and unmount behavior tests" }
    - { path: src/screens/category-catalog-consumer-refresh.test.ts, provides: "Create/Edit/Import Review real-loader boundary regressions with mocked category and non-category reads" }
    - { path: src/screens/category-consumer-contract.test.ts, provides: "Consumer wiring contract proving both publication paths use the shared authority" }
  key_links:
    - { from: src/hooks/use-category-catalog-refresh.ts, to: src/db/contact-read.ts, via: "one request generation for initial and focus listCategories reads" }
    - { from: src/screens/CreateContactScreen.tsx, to: src/hooks/use-category-catalog-refresh.ts, via: "initial and focus publications update catalog and reconcile selection through one callback" }
    - { from: src/screens/EditContactScreen.tsx, to: src/hooks/use-category-catalog-refresh.ts, via: "initial and focus publications update catalog/form category through one callback" }
    - { from: src/screens/ImportReviewScreen.tsx, to: src/hooks/use-category-catalog-refresh.ts, via: "initial and focus publications update catalog/import draft through one callback" }
---

<objective>Give Create, Edit, and Import Review one tested category-publication authority spanning initial hydration and focus refresh, so independently completing reads cannot overwrite newer committed truth.</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-category-management/37.1-VERIFICATION.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-05-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Build one category request/publication authority for initial and focus reads</name>
  <read_first>src/db/contact-read.ts, src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/ImportReviewScreen.tsx</read_first>
  <files>src/hooks/use-category-catalog-refresh.ts, src/hooks/use-category-catalog-refresh.test.ts</files>
  <behavior>- Initial hydration and focus refresh both begin requests against the same monotonically increasing generation and resolve at most once into the same commit callback.
- Rejection is caught and never calls the commit callback or leaks an unhandled promise.
- Whichever initial/focus request starts later invalidates the older request; blur/unmount invalidates every pending request.
- The hook owns no empty fallback and therefore cannot erase the consumer's last committed catalog.</behavior>
  <action>Create a narrow hook/request coordinator around `listCategories(getExecutor())` whose returned initial-load trigger and internal `useFocusEffect` share one generation/lifecycle guard and one stable success callback. Catch read rejection and optionally report only the operation class through the existing logger without setting catalog state. Do not let separate initial loaders call `listCategories` outside this boundary. Do not add loading UI, alerts, retries, network access, or empty-array fallback.</action>
  <acceptance_criteria>Deterministic tests cover initial-then-focus and focus-then-initial out-of-order completion, rejection from either source, blur, and unmount; only the latest authorized success publishes.</acceptance_criteria>
  <verify><automated>npx vitest run src/hooks/use-category-catalog-refresh.test.ts</automated></verify>
  <done>Every category read a consumer starts can be ordered against every other category read for that mounted screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Create and Edit through the shared refresh boundary</name>
  <read_first>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/category-consumer-contract.test.ts, src/logic/category-logic.ts</read_first>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/category-catalog-consumer-refresh.test.ts, src/screens/category-consumer-contract.test.ts</files>
  <behavior>- Create's initial category result cannot overwrite a newer focus result, nor vice versa; rejected/stale reads leave catalog and selection unchanged while definitions/settings still hydrate.
- Edit's initial category result cannot overwrite a newer focus result, nor vice versa; rejected/stale reads preserve catalog and extant form while contact/definition/knowledge hydration still completes.
- Edit's independent photo focus refresh remains intact and does not share category generation state.</behavior>
  <action>Remove category publication from each independent aggregate loader and invoke the shared initial trigger alongside it, so non-category initialization retains its current error/loading behavior and does not wait on or publish category state. Route focus category refresh through the same authority. Keep Create/Edit save-time revalidation, 12/13 selector behavior, unsaved form fields, and Edit's separate photo-only focus refresh unchanged. Add behavioral tests at the coordinator actually invoked by each screen's initial and focus handlers, mocking delayed `listCategories` plus representative non-category loaders and asserting final catalog/selection/form state and call ordering; retain source-contract assertions only as adoption backstops.</action>
  <acceptance_criteria>Create and Edit each have an initial-vs-focus race regression at their wired handler boundary, and their non-category initial/photo work still completes without publishing stale categories.</acceptance_criteria>
  <verify><automated>npx vitest run src/hooks/use-category-catalog-refresh.test.ts src/screens/category-catalog-consumer-refresh.test.ts src/screens/category-consumer-contract.test.ts && npx tsc --noEmit</automated></verify>
  <done>Create and Edit order all category publications without changing unrelated initial or focus behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route Import Review through the same refresh authority and lock all-consumer coverage</name>
  <read_first>src/screens/ImportReviewScreen.tsx, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/hooks/use-category-catalog-refresh.ts, src/screens/category-catalog-consumer-refresh.test.ts, src/screens/category-consumer-contract.test.ts, src/logic/category-logic.ts</read_first>
  <files>src/screens/ImportReviewScreen.tsx, src/screens/category-catalog-consumer-refresh.test.ts, src/screens/category-consumer-contract.test.ts</files>
  <behavior>- Import Review's initial category result cannot overwrite a newer focus result, nor vice versa.
- Rejected or stale reads and completions after blur/unmount leave the current import draft, selection, and last committed catalog unchanged.
- Session/contact/settings/method/photo hydration still completes independently of category publication.
- Create, Edit, and Import Review each have behavioral initial-vs-focus race, rejection, stale-completion, blur, and unmount coverage at the coordinator invoked by their real handlers.</behavior>
  <action>Remove category publication from Import Review's independent initial Promise.all and start the shared initial category trigger alongside the preserved session/contact/settings/method/photo loader. Route its focus refresh through the same authority and commit categories plus resolved selection only from the authorized callback. Complete the behavioral suite for all three consumers using delayed/rejected listCategories calls and representative non-category loaders; assert final catalog, selection/form/draft state, non-category hydration, and call ordering. Strengthen the source contract only as an adoption backstop proving no named consumer publishes initial or focus category results outside the shared authority. Preserve save/import-time current-catalog validation and do not broaden this gap to Bulk Import Setup.</action>
  <acceptance_criteria>One plan and one test suite prove the entire shared invariant across Create, Edit, and Import Review; none can bypass it from either initial hydration or focus refresh.</acceptance_criteria>
  <verify><automated>npx vitest run src/hooks/use-category-catalog-refresh.test.ts src/screens/category-catalog-consumer-refresh.test.ts src/screens/category-consumer-contract.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>All three verification-named consumers are rejection-safe, jointly latest-only across initial and focus reads, and lifecycle-safe without disturbing unrelated hydration.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| asynchronous SQLite read → mounted focused React state | Completion order and lifecycle determine whether a result is authorized to publish. |

## STRIDE Threat Register — ASVS L1, blocking threshold: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-G12-01 | Tampering | out-of-order focus completion | medium | mitigate | Generation check admits only the newest active request. |
| T-37.1-G12-02 | Denial of Service | SQLite read rejection | medium | mitigate | Catch locally and retain last committed catalog/form state. |
| T-37.1-G12-03 | Information Disclosure | refresh diagnostics | low | mitigate | Log operation class only; do not include category/contact contents. |
</threat_model>

<verification>Run the hook, behavioral consumer-race, and source-contract suites; typecheck; and Biome all seven files. Spec-less probe skip: no requirement IDs exist for this phase.</verification>
<success_criteria>Create, Edit, and Import Review initial and focus reads are rejection-safe, jointly latest-only, lifecycle-safe, and preserve unrelated hydration behavior.</success_criteria>

## Artifacts this phase produces
- `useCategoryCatalogRefresh`, its focused test file, and wired Create/Edit/Import Review race regressions.

<output>Create `.planning/phases/37.1-category-management/37.1-12-SUMMARY.md` when done.</output>
