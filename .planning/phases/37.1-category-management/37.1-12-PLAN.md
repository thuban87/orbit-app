---
phase: 37.1-category-management
plan: 12
type: execute
wave: 9
depends_on: ["37.1-19"]
files_modified:
  - .planning/phases/37.1-category-management/37.1-DEVICE-UAT.md
  - .planning/phases/37.1-category-management/37.1-VALIDATION.md
autonomous: false
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
user_setup:
  - service: android-device
    why: "Confirm the Orbit Android package and Metro tmux session, then connect/authorize the physical phone before device UAT."
    dashboard_config:
      - task: "Tell the executor the exact package and Metro session; connect and authorize the physical phone."
        location: "Local development machine / phone USB debugging prompt"
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Every device command uses `~/.local/bin/adb`, a dynamically discovered single serial, and the owner-confirmed package/session."
    - "Destructive UAT runs only in Plans 20/19's app-wide disposable synthetic database, records baseline/cleanup evidence, and cannot alter the owner's personal taxonomy."
    - "Physical Android evidence covers reduced motion, rollback-visible restoration, accessibility, long text, large catalogs, and cross-screen refresh."
  artifacts:
    - path: .planning/phases/37.1-category-management/37.1-DEVICE-UAT.md
      provides: "Owner-confirmed topology plus isolated physical-device evidence and cleanup record"
  key_links:
    - from: .planning/phases/37.1-category-management/37.1-DEVICE-UAT.md
      to: src/screens/__dev__/CategoryManagementUatScreen.tsx
      via: "isolated synthetic manager/fault harness"
---

<objective>Complete safe physical-device verification and record the final phase gate after all implementation and automated audit work is done.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-18-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-19-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-20-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-VALIDATION.md</context>

<tasks>
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Confirm package, Metro session, and authorized physical target</name>
  <files>.planning/phases/37.1-category-management/37.1-DEVICE-UAT.md</files>
  <read_first>
    - AGENTS.md Android device section (full; never guess repo-specific values or use bare/apt adb)
    - docs/runbooks/desktop-build-pipeline.md and current device/UAT runbook (full procedures)
  </read_first>
  <action>Ask the owner for the exact Orbit Android package name and Metro tmux session name and for the physical phone to be connected/authorized. Record the confirmed values. Only after the reply, run `emu-connect status` and `~/.local/bin/adb devices -l`; resolve exactly one authorized serial dynamically. Every subsequent adb command must invoke `~/.local/bin/adb` explicitly and target that resolved serial where applicable. Halt if the target is remote, unauthorized, or cardinality differs from one. Never install apt adb.</action>
  <instructions>Ask for the exact package and Metro tmux session plus confirmation that the physical phone is connected and authorized.</instructions>
  <verification>After the owner replies, record both values, run `emu-connect status`, then use `~/.local/bin/adb devices -l` to resolve exactly one authorized physical transport dynamically.</verification>
  <resume-signal>Reply with the Orbit Android package name and Metro tmux session name after connecting/authorizing the physical phone.</resume-signal>
  <verify><automated>emu-connect status &amp;&amp; test "$(~/.local/bin/adb devices | awk '$2==\"device\"{n++} END{print n+0}')" -eq 1</automated></verify>
  <acceptance_criteria>
    - Owner-confirmed values precede device access; one physical serial is dynamically recorded.
    - Every adb command uses the SDK symlink path explicitly; apt adb is never installed.
  </acceptance_criteria>
  <done>The owner-confirmed physical topology is safe and ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Drive isolated physical UAT, clean up, and approve evidence</name>
  <files>.planning/phases/37.1-category-management/37.1-DEVICE-UAT.md, .planning/phases/37.1-category-management/37.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md and 37.1-VALIDATION.md (full UI/manual contract)
    - src/db/database.ts/tests plus src/screens/__dev__/CategoryManagementUatScreen.tsx and category-management-uat.ts/tests (full app-wide isolation, production-unreachability, fault, and cleanup contract)
    - All modified manager/selector/Orrery screens and related tests (full files)
  </read_first>
  <what-built>The complete Category Management phase plus a compile-time development-only control route that switches every ordinary screen to a separate synthetic SQLite database and provides a one-shot rollback fault.</what-built>
  <how-to-verify>Follow the action below on the owner-confirmed physical phone, capture aggregate/synthetic evidence in DEVICE-UAT.md, cleanly delete the isolated database, then present the evidence for owner approval.</how-to-verify>
  <resume-signal>Reply `approved` after reviewing the recorded physical-device evidence, or describe the failing observation.</resume-signal>
  <action>First run the full automated gate. Enter through the compile-time development launcher, explicitly create/activate the UAT database, and record its closed filename, synthetic banner, baseline aggregate row counts/order/revision, and normal-database non-use assertion; do not record names or private contents. Use `~/.local/bin/adb -s "$SERIAL" shell uiautomator dump ...` and `~/.local/bin/adb -s "$SERIAL" pull ...` before coordinate taps, and the same explicit binary/serial for screenshots/input/log deltas. From the active harness navigate to the ordinary Category Management, Dashboard, Profile, Orrery, and System Builder routes and exercise zero/one/13+, 100-character Unicode, large text, Standard/Galaxy, 44px actions, drag and Move controls, anchored-menu geometry, focus/TalkBack/Back/non-dismissal, all-status import preview, used/unused/last deletion, and cross-screen live refresh against the shared synthetic executor. Toggle Android Remove animations/reduced motion and prove manager reorder plus Orrery disclosure behavior becomes immediate/restrained while semantics remain. Explicitly arm one deletion failure, execute it through the ordinary manager, confirm locked error and full visible restoration across every affected consumer, and confirm auto-disarm. Return to the harness, Reset, deactivate, Close/delete the dedicated DB, record absence and normal-executor restoration, then restart the app and confirm the harness is inactive by default. Never perform destructive manager actions before the synthetic-active banner is present. Keep automated backup fixtures separate from UI UAT and make no emulator performance claim. Present the evidence for owner verification; only after approval mark manual VALIDATION rows and final frontmatter compliant.</action>
  <verify><automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors &amp;&amp; npx biome check src</automated><human-check>Owner reviews DEVICE-UAT evidence confirming isolated dataset, rollback restoration, reduced motion, accessibility, complete catalogs, cross-screen refresh, and successful cleanup.</human-check></verify>
  <acceptance_criteria>
    - All device evidence comes from the physical phone and isolated synthetic DB; baseline and cleanup are recorded.
    - Rollback-visible failure is reproducible through real screen→DAO behavior and production remains unreachable.
    - Owner approves the evidence; full automated gate is green and VALIDATION is then accurately finalized.
  </acceptance_criteria>
  <done>Phase 37.1 has safe, owner-reviewed physical-device and complete automated evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Agent → shared adb topology | Wrong binary/serial can disrupt the owner's tooling. |
| Device UAT → local data | Destructive tests must remain isolated from personal taxonomy. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-53 | Denial of service | adb topology | high | mitigate | `emu-connect`, SDK adb path, dynamic one-device serial, owner-confirmed package/session. |
| T-37.1-54 | Tampering | Personal database | high | mitigate | Dev-only dedicated database, synthetic fixture, exact cleanup guard, no destructive normal-route UAT. |
| T-37.1-55 | Information disclosure | UAT record | high | mitigate | Aggregate/synthetic evidence only; no personal names/rules/profile/backup contents. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Physical-device evidence using only `~/.local/bin/adb` and dynamically discovered serial.
- Isolated fixture baseline, rollback, reduced-motion, and cleanup record.
- Final validated phase evidence after owner approval.
</artifacts>
<verification>Full automated gate and owner-approved physical-device UAT are both required.</verification>
<success_criteria>Every manual contract is proven safely on the physical phone with no personal-data risk or adb-policy violation.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-12-SUMMARY.md` when done.</output>
