---
phase: 37.1-category-management
plan: 06
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-03", "37.1-07"]
files_modified:
  - src/components/ui/AnchoredMenu.tsx
  - src/components/ui/AnchoredMenu.test.ts
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 44000, raw_tokens: 44000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The manager renders canonical rows as handle → two-line name/all-contact count → far-right overflow, with Add above and informational Uncategorized last."
    - "Rename, drag reorder, Move earlier/later, and delete use the exact locked sheets/dialog/menu behavior; failed writes restore committed state and preserve input/target."
    - "Used deletion previews every nonzero fallout class, defaults reassignment to Uncategorized, revalidates on submit, and mutates only after the non-dismissible final confirmation."
    - "The reusable anchored menu clamps, flips, scrolls, supports Back/outside close and focus restoration, and remains accessible at large text/long names."
  artifacts:
    - path: src/components/ui/AnchoredMenu.tsx
      provides: "Reusable themed measured/clamped/flipped/focus-managed menu primitive"
    - path: src/screens/CategoryManagementScreen.tsx
      provides: "Complete Category CRUD/reorder/delete manager"
  key_links:
    - from: src/screens/CategoryManagementScreen.tsx
      to: src/db/categories-dao.ts
      via: "preview + final fingerprint/target delete call"
    - from: src/screens/CategoryManagementScreen.tsx
      to: src/components/category/CategoryChoiceSheet.tsx
      via: "complete canonical reassignment choices at 13+"
---

<objective>
Complete the centralized manager over the proven tracer, including accessible reorder, reusable overflow popover, and the permanent two-stage delete/reassignment flow.
</objective>

<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the reusable themed AnchoredMenu contract</name>
  <reversibility rating="reversible">A reusable UI primitive and pure geometry functions are code-only.</reversibility>
  <files>src/components/ui/AnchoredMenu.tsx, src/components/ui/AnchoredMenu.test.ts</files>
  <read_first>
    - src/components/control-surface/DashboardControlRow.tsx (full measurement/overlay-host precedent)
    - src/components/orrery/OrrerySystemSelector.tsx and src/stores/shell-transient-store.ts (full modal-a11y/transient/focus precedent)
    - src/components/ui/GlassSurface.tsx and src/components/ui/overlay-base.tsx (full theme/package/back/scrim behavior)
    - src/components/ui/ConfirmDialog.tsx and src/components/ui/Sheet.tsx (full focus/dismissibility contracts)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §5 plus overflow/long-text UI Considerations
  </read_first>
  <behavior>
    - Prefer below/end; clamp to 16px safe-area edges; flip above when below is insufficient; otherwise constrain height and scroll internally.
    - Remeasure anchor on open and layout/viewport changes; long labels do not resize anchor/menu beyond bounds.
    - One menu open; outside/Back closes without action; focus enters first menuitem then returns to origin; background hidden from assistive technology.
  </behavior>
  <action>
Create pure geometry functions plus `AnchoredMenu` accepting anchor ref/measurement, visible, close callback, preferred alignment, accessible label, and typed menu items/separators. Render via the existing overlay/transient host above scroll content using `GlassSurface density="dense"`; preserve its iOS Galaxy behavior and add no Android elevation/shadow. Apply 44px minimum items, menuitem roles, danger tokens/icons, scroll fallback, Back/outside dismissal, one-open-only transient ownership, and deterministic focus management. Test narrow/tall/below/above/neither-fit geometry and state callbacks without pixel snapshots.
  </action>
  <verify><automated>npx vitest run src/components/ui/AnchoredMenu.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Geometry tests cover clamp, flip, safe-area inset, constrained height, and remeasurement inputs.
    - Accessibility roles/state/focus/Back/outside contracts are represented in testable helpers/source tests.
    - No hardcoded color/opacity/elevation or category-specific behavior appears.
  </acceptance_criteria>
  <done>A reusable, themed, bounded, accessible anchored menu exists for the manager.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete manager rename, reorder, and permanent deletion UX</name>
  <reversibility rating="costly">Confirmed delete is a permanent owner-approved runtime operation with no Undo/history; the preview and atomic DAO are its safety gates.</reversibility>
  <files>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <read_first>
    - src/screens/CategoryManagementScreen.tsx and src/screens/CategoryManagementScreen.test.tsx (full tracer implementation)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full committed CRUD/preview/delete outcomes)
    - src/components/ui/AnchoredMenu.tsx, src/components/category/CategoryChoiceSheet.tsx, src/components/ui/Sheet.tsx, src/components/ui/ConfirmDialog.tsx (full overlay contracts)
    - src/screens/SystemsManagementScreen.tsx (full installed reorder-list/committed-order precedent)
    - src/stores/shell-refresh-store.ts, src/stores/dashboard-query-store.ts, src/stores/orrery-system-store.ts (full post-commit refresh/hydration seams)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§2–6, 10 and exact Copywriting Contract
  </read_first>
  <behavior>
    - Row/count/order/two-line/44px/a11y contracts and accessible Move actions match the UI-SPEC.
    - Rename uses compact Sheet, exact validation/copy, capitalization-only rename, and post-commit `Category renamed.`.
    - Reorder keeps dropped order while saving, blocks another change, normalizes, restores committed order + announces locked error on failure.
    - Unused delete is only all-zero fallout; used delete shows each nonzero count class, target choices/default/explanations, `Review deletion`, final non-dismissible dialog, stale-preview refresh, exact error restoration, and post-commit `Category deleted.` with no Undo.
  </behavior>
  <action>
Implement the locked row hierarchy and `react-native-reorderable-list` handle-only drag, plus accessible Move earlier/later through the same DAO reorder call. Add Rename and delete overlays using shared primitives and exact copy. Do not dismiss destructive ConfirmDialog on scrim/Back. Keep selection/input/committed rows during saves; on stale preview refresh counts and retain a still-valid target else use Uncategorized. The UI renders aggregate counts only and never enumerates private records. After successful commit, reload rows, call `bumpShellRefresh`, rehydrate the Dashboard/Orrery durable stores changed by the transaction, close overlays, announce politely, and show actionless success. No success or publication precedes commit.
  </action>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx src/db/categories-dao.test.ts src/components/ui/AnchoredMenu.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Tests cover zero/one/many, singular/plural, 100-char Unicode, menu action order/bounds, reorder rollback, rename failure retention, unused/used delete, all fallout copy, stale preview, target fallback, non-dismissal, and no Undo.
    - Exact UI-SPEC copy is used for every marked label/error/success string.
    - Post-commit refresh reaches shell and durable preference stores; failure publishes nothing and restores committed state.
  </acceptance_criteria>
  <done>The centralized manager fully implements the approved CRUD/reorder/permanent-delete interaction contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Screen preview/target → destructive DAO | Stale or crafted UI state must not authorize mutation. |
| Overlay → accessibility focus/Back | Hidden destructive or inaccessible controls can cause unintended action. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-19 | Tampering | Delete submission | high | mitigate | Pass fingerprint+target to lock-time DAO revalidation; no UI-side SQL or mutation. |
| T-37.1-20 | Repudiation | Permanent confirmation | high | mitigate | Explicit fallout preview and non-dismissible final ConfirmDialog with exact target summary. |
| T-37.1-21 | Information disclosure | Fallout UI | high | mitigate | Aggregate counts/types only; never names, rule values, profile data, or backup contents. |
| T-37.1-22 | Denial of service | Long menus/lists | medium | mitigate | Clamp/flip/scroll, 44px controls, large-text reflow, and complete target list. |

Package legitimacy: N/A; reorder package already installed/audited.
</threat_model>

<artifacts>
## Artifacts this phase produces
- `AnchoredMenu` and pure geometry helpers/tests.
- Complete manager row, rename, reorder, used/unused delete, refresh, and failure-state behavior.
</artifacts>

<verification>Focused UI/DAO/geometry tests, typecheck, color gate, and changed-file Biome pass.</verification>
<success_criteria>Every manager operation is reachable, accessible, exact-copy, commit-truthful, and rollback-safe, with no recovery UI or private fallout leakage.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-06-SUMMARY.md` when done.</output>
