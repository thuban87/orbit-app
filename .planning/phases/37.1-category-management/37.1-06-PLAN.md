---
phase: 37.1-category-management
plan: 06
type: execute
wave: 4
depends_on: ["37.1-02", "37.1-03", "37.1-05"]
files_modified:
  - src/logic/dashboard-query-logic.ts
  - src/logic/dashboard-query-logic.test.ts
  - src/stores/dashboard-query-store.ts
  - src/stores/dashboard-query-store.test.ts
  - src/components/control-surface/DashboardControlRow.tsx
  - src/components/control-surface/FilterPanelContent.tsx
  - src/components/control-surface/filter-summary.ts
  - src/components/control-surface/filter-summary.test.ts
  - src/screens/HomeScreen.tsx
  - src/screens/dashboard-category-contract.test.ts
  - src/backup/backup-schema.ts
  - src/components/profile/ProfileTemplateManager.tsx
  - src/components/profile/ProfileBackgroundManager.tsx
  - src/components/profile/profile-category-contract.test.ts
autonomous: true
requirements: []
estimate: { tokens: 46000, raw_tokens: 46000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Dashboard filters can explicitly select Uncategorized alone or with real categories, persist a closed token safely, and become unfiltered when deletion removes the last real selection."
    - "Dashboard bulk Set Category includes Uncategorized last, supports 13+ search, rejects stale targets, and refreshes list/card labels after commit."
    - "Profile Layout/Background managers expose complete bounded searchable real-category catalogs; rename preserves assignment and delete removes rather than transfers presentation."
  artifacts:
    - { path: src/logic/dashboard-query-logic.ts, provides: "Explicit null-category SQL and filter canonicalization" }
    - { path: src/components/profile/ProfileTemplateManager.tsx, provides: "Complete real-category layout assignment" }
  key_links:
    - { from: src/components/control-surface/FilterPanelContent.tsx, to: src/stores/dashboard-query-store.ts, via: "closed durable null/ID category filter" }
    - { from: src/components/profile/ProfileBackgroundManager.tsx, to: src/db/profile-presentation-dao.ts, via: "stable numeric category assignment" }
---

<objective>Integrate the shared category contract into Dashboard control/bulk flows and both Profile presentation managers.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-05-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete Dashboard null filtering, bulk assignment, and live refresh</name>
  <read_first>src/logic/dashboard-query-logic.ts, src/stores/dashboard-query-store.ts, src/components/control-surface/DashboardControlRow.tsx, src/components/control-surface/FilterPanelContent.tsx, src/screens/HomeScreen.tsx, src/backup/backup-schema.ts</read_first>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts, src/components/control-surface/DashboardControlRow.tsx, src/components/control-surface/FilterPanelContent.tsx, src/components/control-surface/filter-summary.ts, src/components/control-surface/filter-summary.test.ts, src/screens/HomeScreen.tsx, src/screens/dashboard-category-contract.test.ts</files>
  <behavior>- Closed Uncategorized token produces `category_id IS NULL`; mixed null+IDs uses correct OR grouping; invalid persisted values fail/canonicalize safely.
- Category filter keeps canonical order, search/hidden selections, explicit null last, and grammatical summary.
- Bulk Set Category writes ID/null, refreshes current cards/list after commit, and rejects a deleted target.
- Category DAO cleanup is reflected immediately in hydrated Dashboard state without relaunch.</behavior>
  <action>Add explicit null-category semantics to the existing pure query/parser/store contract and integrate the shared catalog policy into filter and bulk surfaces. Define the closed durable Dashboard category token (the uncategorized literal plus canonical positive-ID grammar) once as a shared grammar and validate it at BOTH the runtime/store boundary and the persisted `dashboardFilters` backup boundary — extend Plan 03's v6 manifest validation in `src/backup/backup-schema.ts` to call that same shared grammar rather than reimplementing it (this plan is wave 4, after Plan 03 establishes v6 in wave 3). Plan 03 does not define this Dashboard-specific validator; this plan owns it. After committed deletion rehydrate/publish the Dashboard store; do not call Zustand from inside the DAO transaction.</action>
  <acceptance_criteria>Uncategorized and real-category Dashboard filters/bulk actions are correct, durable, searchable, stale-safe, and live-refreshing.</acceptance_criteria>
  <verify><automated>npx vitest run src/logic/dashboard-query-logic.test.ts src/stores/dashboard-query-store.test.ts src/components/control-surface/filter-summary.test.ts src/screens/dashboard-category-contract.test.ts</automated></verify>
  <done>Dashboard has complete category filtering and assignment behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound and synchronize Profile category presentation managers</name>
  <read_first>src/components/profile/ProfileTemplateManager.tsx, src/components/profile/ProfileBackgroundManager.tsx, src/db/profile-presentation-dao.ts, src/db/profile-read.ts</read_first>
  <files>src/components/profile/ProfileTemplateManager.tsx, src/components/profile/ProfileBackgroundManager.tsx, src/components/profile/profile-category-contract.test.ts</files>
  <behavior>- Both managers show only real categories in canonical order, use complete search at 13+, and show `No categories available` at zero.
- Category UID/ID identity survives rename and refreshes visible label; delete removes the source assignment without copying it to target.
- Long catalogs are bounded rather than an unbounded button column.</behavior>
  <action>Adopt the shared real-only catalog/search model in both Profile managers while retaining the existing presentation DAO and UID backup identity. Refresh on focus/shell revision and ensure the live Profile resolves the reassignment target or Uncategorized without cached-name identity.</action>
  <acceptance_criteria>Profile presentation remains stable across rename, correctly clears on delete, and scales to arbitrary taxonomy size.</acceptance_criteria>
  <verify><automated>npx vitest run src/components/profile/profile-category-contract.test.ts src/db/profile-presentation-dao.test.ts</automated></verify>
  <done>Dashboard and Profile category consumers honor the shared mutation contract.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-16 | Tampering: malformed persisted filter | high | mitigate | closed token/positive-ID parser and backup validation |
| T-37.1-17 | Tampering: stale in-memory category | high | mitigate | commit-first store rehydrate and stale target rejection |
| T-37.1-18 | DoS: unbounded Profile list | medium | mitigate | bounded complete searchable catalog |
</threat_model>
<verification>Focused Dashboard/Profile tests, typecheck, Biome, and colors.</verification>
<success_criteria>Dashboard and Profile respond correctly to add/rename/reorder/delete without stale identity or inaccessible catalogs.</success_criteria>
## Artifacts this phase produces
- Dashboard Uncategorized semantics and Profile category-consumer integration.
<output>Create `.planning/phases/37.1-category-management/37.1-06-SUMMARY.md` when done.</output>
