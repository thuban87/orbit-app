---
phase: 37.1-category-management
plan: 04
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-05"]
files_modified:
  - src/components/ui/AnchoredMenu.tsx
  - src/components/ui/AnchoredMenu.test.ts
  - src/stores/snackbar-store.ts
  - src/stores/snackbar-store.test.ts
  - src/components/Snackbar.tsx
  - src/components/Snackbar.test.tsx
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
autonomous: true
requirements: []
estimate: { tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "The manager presents canonical ordered rows, accurate all-contact counts, a separated Uncategorized block, and exact empty/loading/error states."
    - "Add/Rename/Reorder/Delete publish only committed truth; rejected DAO calls restore the last committed state and exact error feedback."
    - "Reorder works by labeled drag handle and accessible Move earlier/later actions with reduced-motion behavior."
    - "Permanent used deletion shows complete count-only fallout, one transaction-local reassignment choice, lock-time revalidation, and final non-dismissible confirmation."
  artifacts:
    - { path: src/components/ui/AnchoredMenu.tsx, provides: "Reusable themed, clamped, focus-safe menu" }
    - { path: src/screens/CategoryManagementScreen.tsx, provides: "Complete manager interaction state machine" }
    - { path: src/components/Snackbar.tsx, provides: "Actionless six-second committed-success feedback" }
  key_links:
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/db/categories-dao.ts, via: "preview/rename/reorder/delete calls with committed-state restoration" }
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/components/ui/AnchoredMenu.tsx, via: "far-right overflow origin and accessible actions" }
---

<objective>Complete the centralized manager's accessible interaction, confirmation, and commit-truthful feedback contracts.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the reusable themed anchored menu</name>
  <read_first>src/components/control-surface/DashboardControlRow.tsx, src/components/orrery/OrrerySystemSelector.tsx, src/components/ui/GlassSurface.tsx, src/components/icons/icon-registry.ts</read_first>
  <files>src/components/ui/AnchoredMenu.tsx, src/components/ui/AnchoredMenu.test.ts</files>
  <behavior>- Measure/re-measure in window coordinates, prefer below/end, clamp within 16px safe-area edges, flip above, otherwise bound internal scrolling.
- One menu is open; outside tap/Back closes without action; focus enters first menuitem and returns to origin; background is hidden to assistive technology.
- Items are at least 44px; danger semantics use tokens; Android adds no elevation/shadow.</behavior>
  <action>Compose the existing transient/focus and GlassSurface patterns into one reusable primitive with pure geometry helpers under test. Accept anchor measurement/ref, visibility, close callback, ordered items, and alignment; do not create a Category-only absolute overlay.</action>
  <acceptance_criteria>The menu meets geometry, one-open, menuitem, focus, Back, safe-area, token, and touch-target contracts.</acceptance_criteria>
  <verify><automated>npx vitest run src/components/ui/AnchoredMenu.test.ts</automated></verify>
  <done>The admitted shared primitive is reusable and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete manager rows, sheets, reorder, and actionless success feedback</name>
  <read_first>src/screens/CategoryManagementScreen.tsx, src/screens/SystemsManagementScreen.tsx, src/components/ui/Sheet.tsx, src/components/Snackbar.tsx, src/stores/snackbar-store.ts, src/stores/shell-refresh-store.ts</read_first>
  <files>src/stores/snackbar-store.ts, src/stores/snackbar-store.test.ts, src/components/Snackbar.tsx, src/components/Snackbar.test.tsx, src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <behavior>- Row order is handle → two-line name/all-contact count → far-right overflow; Uncategorized is separate, immutable, and last.
- Add/Rename use exact compact Sheet fields/actions/copy; reorder disables overlap, keeps dropped state while saving, and restores committed order on failure.
- Actionless `Category added.`, `Category renamed.`, `Category deleted.` use the existing six-second host/live region with no empty action space.</behavior>
  <action>Extend the shared Snackbar message action to optional and render through project typography. Complete the manager with exact UI-SPEC hierarchy, counts including archived/Unbound, drag-handle-only gesture, Move earlier/later menu items, focus restoration, reduced-motion signal, generation-guarded initial load, and post-commit shell refresh. Keep mutation controls centralized.</action>
  <acceptance_criteria>Every non-destructive manager state and accessibility/copy contract is covered; failure never leaves optimistic state published.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx src/components/Snackbar.test.tsx src/stores/snackbar-store.test.ts</automated></verify>
  <done>The manager can load, add, rename, and reorder accessibly against committed DAO truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render complete permanent-delete preview and confirmation flow</name>
  <read_first>src/screens/CategoryManagementScreen.tsx, src/components/ui/ConfirmDialog.tsx, src/components/ui/Sheet.tsx, src/db/categories-dao.ts, src/components/category/CategoryChoiceSheet.tsx</read_first>
  <files>src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx</files>
  <behavior>- Unused path is available only when every fallout class is zero and uses exact direct-confirm copy.
- Used sheet conditionally renders separate contacts, pending/completed/discarded imports, rules/Systems, prefs/overrides, Profile counts and a reassignment target selector that reuses Plan 05's shared searchable CategoryChoiceSheet (canonical order, 13+ search, Uncategorized policy) so a large catalog's target is never inaccessible.
- Review deletion opens a non-dismissible final dialog; Keep category retains a still-valid target; changed preview performs no write and refreshes.
- DAO rejection restores committed list/order and locked save error; no contact/rule/private values are shown.</behavior>
  <action>Wire the full typed preview to expanded Sheet and ConfirmDialog with exact copy and aggregate counts. Default target to Uncategorized, present target choices through Plan 05's shared searchable CategoryChoiceSheet (so 13+ catalogs stay reachable), omit the deleted row, preserve target only within the transaction flow, disable final confirmation during commit, and close/show success only after commit. Do not add Undo, history, quarantine, an alternate database, or device fault controls.</action>
  <acceptance_criteria>Used/unused/stale/error/success deletion states precisely match the UI-SPEC and cannot mutate before final confirmation.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/CategoryManagementScreen.test.tsx src/db/categories-dao.test.ts</automated></verify>
  <done>The complete manager safely drives the atomic deletion aggregate.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-10 | Tampering: stale/duplicate UI actions | high | mitigate | disable in-flight actions; DAO revalidation remains authority |
| T-37.1-11 | Information disclosure | medium | mitigate | count-only fallout/copy and sanitized locked errors |
| T-37.1-12 | DoS/accessibility exclusion | medium | mitigate | bounded overlays, 44px targets, accessible reorder alternative, focus/Back contracts |
</threat_model>
<verification>Focused manager/menu/Snackbar tests, typecheck, Biome, and color gate.</verification>
<success_criteria>The Settings manager exposes all approved category operations without false success, inaccessible controls, or private fallout detail.</success_criteria>
## Artifacts this phase produces
- Reusable AnchoredMenu, complete manager, and actionless Snackbar support.
<output>Create `.planning/phases/37.1-category-management/37.1-04-SUMMARY.md` when done.</output>
