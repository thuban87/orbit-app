---
phase: 37.1-category-management
plan: 04
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-03"]
files_modified:
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/backup/reconciliation.ts
  - src/backup/reconciliation.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Backup v5 accepts and exports typed category tombstones while rejecting malformed or duplicate evidence before mutation."
    - "Deletion wins an equal category row/tombstone timestamp and an older backup cannot resurrect a deleted category or its category-specific rules/ref state."
    - "Crafted backups with category/System visible-name collisions or invalid dependent survivor references fail before apply."
    - "Unrelated historical missing-category rules retain the established diagnostic contract."
  artifacts:
    - path: src/backup/reconciliation.ts
      provides: "Category-tombstone survival and dependent suppression in the pre-apply plan"
  key_links:
    - from: src/backup/backup-schema.ts
      to: src/db/tombstones-dao.ts
      via: "closed `category` entity vocabulary"
    - from: src/backup/reconciliation.ts
      to: src/backup/restore-apply.ts
      via: "normalized survivor/actions plan consumed by Plan 05"
---

<objective>
Extend the existing v5 parse/export/reconciliation contract so category deletion evidence is valid, collision-safe, and resurrection-proof before any restore transaction opens.
</objective>

<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@docs/decisions/ADR-056-tombstone-backed-uid-reconciliation-for-portable-restores.md
@docs/decisions/ADR-060-versioned-portable-method-graph-and-collision-normalized-restoration.md
@docs/decisions/ADR-063-versioned-lifecycle-backup-and-dormant-cadence-restore.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit and validate category deletion evidence in backup v5</name>
  <reversibility rating="costly">The portable v5 parser/export semantics become a compatibility contract, but the format number and representable wire shape stay unchanged.</reversibility>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/backup/backup-schema.ts and src/backup/backup-schema.test.ts (full v1→v5 parser, tombstone allowlist, graph checks, parse-before-mutate guarantees)
    - src/backup/export-manifest.ts and src/backup/export-manifest.test.ts (full category/tombstone export)
    - src/backup/types.ts (full v5 manifest wire contract; do not bump version)
    - src/db/tombstones-dao.ts (full closed entity type including Plan-02 category member)
    - src/db/systems-catalog-read.ts and src/logic/category-logic.ts (built-in/custom/category label namespace)
  </read_first>
  <behavior>
    - Valid `category` tombstone round-trips in format 5.
    - Empty UID, invalid timestamp, unknown type, and duplicate `(category, uid)` fail validation before mutation.
    - NFC/case-equivalent category/category and category/built-in/custom-System visible-name collisions fail before apply.
    - No backup-version increment occurs.
  </behavior>
  <action>
Add `category` to the schema's tombstone allowlist and retain all existing UID/timestamp/duplicate checks. Extend complete-manifest validation with the shared normalized visible-name namespace and category-dependent survivor checks needed to prevent ambiguous catalogs. Keep parsing pure and complete before mutation. Confirm export already emits tombstones generically; change only what is required for typing/coverage. Add v5 round-trip and crafted-input rejection cases. Do not add fields or bump `BACKUP_FORMAT_VERSION`.
  </action>
  <verify><automated>npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Format 5 category tombstones parse/export exactly through existing manifest fields.
    - Malformed/duplicate/colliding evidence rejects before any apply collaborator can run.
    - BACKUP_FORMAT_VERSION remains 5.
  </acceptance_criteria>
  <done>The v5 boundary safely understands category deletion evidence with no format change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile category winners and suppress only tombstone-lost dependents</name>
  <reversibility rating="costly">Changing merge winner semantics affects portable restore outcomes across installations, though it uses the already accepted ADR-056 timestamp rule.</reversibility>
  <files>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts</files>
  <read_first>
    - src/backup/reconciliation.ts and src/backup/reconciliation.test.ts (full entity metadata, survivor graph, tie behavior, normalization)
    - src/backup/restore-apply.ts and src/backup/restore-apply.test.ts (full consumer of reconciliation actions and current categories:null tombstone mapping)
    - docs/systems/orrery-systems-backup-contract.md (full distinction between historical missing rules and runtime deletion)
    - docs/dossier/milestone-2/phase-37.1-category-management-dossier.md §§I, O
  </read_first>
  <behavior>
    - Category row newer than tombstone survives; tombstone newer or equal deletes.
    - Rules and `category:<uid>` refs tied to a category that specifically lost to tombstone are suppressed/repaired.
    - A rule pointing to a historically absent category without matching deletion evidence remains a broken diagnostic.
    - Unrelated System rules/refs are unchanged.
  </behavior>
  <action>
Map `categories` to the exact `category` tombstone type and preserve the established strict `row.modified_at > deleted_at` winner rule. Extend plan normalization/survivor metadata so dependent category rules and ref-keyed state are suppressed only when their UID lost to durable category tombstone evidence, not merely because the category is absent. Return an apply-ready dependency-safe plan for Plan 05. Add equal/newer/older timestamp cases, category-rule/ref suppression, and unrelated historical-missing-rule preservation.
  </action>
  <verify><automated>npx vitest run src/backup/reconciliation.test.ts src/backup/backup-schema.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Equal timestamp deletion wins and is explicitly tested.
    - Older category rules/ref state cannot resurrect a tombstoned category.
    - Unrelated missing rules and unrelated Systems survive unchanged.
  </acceptance_criteria>
  <done>The pre-apply plan distinguishes deliberate category deletion from historical absence and prevents resurrection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted backup file → parsed manifest | Crafted local files can contain collisions or malformed deletion evidence. |
| Local/incoming rows+tombstones → merge plan | Winner calculation controls resurrection and dependent survival. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-13 | Spoofing | Crafted category/System names | high | mitigate | Complete NFC/case collision validation before apply. |
| T-37.1-14 | Tampering | Tombstone reconciliation | high | mitigate | Typed category mapping, deletion-wins ties, and survivor-driven dependent suppression tests. |
| T-37.1-15 | Information disclosure | Validation errors | medium | mitigate | Report safe path/reason only; never echo contact/rule/profile/backup contents. |

Package legitimacy: N/A.
</threat_model>

<artifacts>
## Artifacts this phase produces
- Format-v5 category tombstone schema/export support.
- Category survivor/tombstone reconciliation and tombstone-specific dependent suppression metadata.
</artifacts>

<verification>Focused backup schema/export/reconciliation tests, typecheck, and changed-file Biome all pass; version remains 5.</verification>
<success_criteria>Backup planning is parse-first, collision-safe, deletion-wins, and cannot resurrect tombstoned category intent.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-04-SUMMARY.md` when done.</output>
