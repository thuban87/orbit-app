---
phase: 37.1-category-management
plan: 11
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-03", "37.1-07"]
files_modified:
  - src/components/orrery/SystemRuleAccordion.tsx
  - src/screens/SystemBuilderScreen.tsx
  - src/screens/SystemBuilderScreen.test.tsx
  - src/components/orrery/orrery-controls-logic.ts
  - src/components/orrery/orrery-controls-logic.test.ts
  - src/components/orrery/OrrerySystemSelector.tsx
  - src/stores/orrery-system-store.ts
  - src/stores/orrery-system-store.test.ts
  - src/screens/orrery-screen-framing.test.ts
  - docs/systems/orrery.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 44000, raw_tokens: 44000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "System Builder always exposes all real category UIDs in canonical order; search appears at 13+, filters visible rows only, and retains hidden selected count/state."
    - "The Orrery selector has exactly Built-in, Categories, Custom Systems groups in that order while preserving row counts, Empty/Needs Attention, selection, Manage Systems, busy/error, focus, and bounded scrolling."
    - "Each selector open expands only the selected group; session toggles reset on the next open and no preference is persisted."
    - "Renaming preserves the category-System selection; deleting it repairs durable and stale in-session selection to All Contacts without blank/crash."
  artifacts:
    - path: src/components/orrery/orrery-controls-logic.ts
      provides: "Exact three-group System selector projection and per-open expansion initializer"
  key_links:
    - from: src/components/orrery/SystemRuleAccordion.tsx
      to: src/logic/category-logic.ts
      via: "complete canonical category filter with independent selected identities"
    - from: src/stores/orrery-system-store.ts
      to: src/db/categories-dao.ts
      via: "safe missing-category parity after committed deletion"
---

<objective>
Integrate category mutability into System Builder and perform only the approved three-group Orrery selector cleanup, preserving all existing selection/membership semantics.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/decisions/ADR-104-durable-orrery-preferences-and-live-system-scope.md
@.planning/phases/37.1-category-management/37.1-03-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete searchable Category rules with hidden-selection retention</name>
  <files>src/components/orrery/SystemRuleAccordion.tsx, src/screens/SystemBuilderScreen.tsx, src/screens/SystemBuilderScreen.test.tsx</files>
  <read_first>
    - src/components/orrery/SystemRuleAccordion.tsx (full current family accordion, selection derivation, checkbox semantics)
    - src/screens/SystemBuilderScreen.tsx and src/screens/SystemBuilderScreen.test.tsx (full category load/draft/save/Needs Attention behavior)
    - src/logic/system-rule-resolver.ts and tests (full Plan-03 validity contract)
    - src/logic/category-logic.ts (full complete/real-only/multi-select search helpers)
    - src/db/systems-dao.ts (full rule writers and UID identity)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §8
  </read_first>
  <behavior>
    - 0 real categories shows `No categories available`; 1–12 complete checkbox list; 13+ labeled Search categories.
    - Query filters rows only; complete draft retains hidden UID selections and header retains `{N} selected`.
    - Rename updates label by UID; delete removes only UID rule; valid/Needs Attention uses Plan-03 canonical status.
  </behavior>
  <action>
Use the shared real-only multi-select category model in the category family only. Keep selected UIDs derived from the complete draft rather than visible rows, add search/no-match/zero state at the exact threshold, and refresh current categories on focus/shell revision. Preserve all other rule families and current save transaction. Add tests for 0/12/13/many, hidden selection, rename label, deleted UID removal, remaining-rule/include validity, sole-rule/exclude-only Needs Attention. No chips, nested management, Add action, or category cutoff.
  </action>
  <verify><automated>npx vitest run src/screens/SystemBuilderScreen.test.tsx src/logic/system-rule-resolver.test.ts src/db/systems-dao.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Complete UID selection survives filtering and visible selected count remains correct.
    - No other rule family/authoring flow changes.
    - Runtime deletion never leaves the deleted category rule as a fake broken rule.
  </acceptance_criteria>
  <done>System Builder remains complete, identity-stable, and validity-correct for mutable categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exact three-group Orrery selector and missing-category fallback parity</name>
  <files>src/components/orrery/orrery-controls-logic.ts, src/components/orrery/orrery-controls-logic.test.ts, src/components/orrery/OrrerySystemSelector.tsx, src/stores/orrery-system-store.ts, src/stores/orrery-system-store.test.ts, src/screens/orrery-screen-framing.test.ts, docs/systems/orrery.md</files>
  <read_first>
    - src/components/orrery/orrery-controls-logic.ts and test (full current choice ordering/status/transient logic)
    - src/components/orrery/OrrerySystemSelector.tsx (full anchored surface, accessibility, focus, busy/error, Manage Systems behavior)
    - src/stores/orrery-system-store.ts and test (full requested/committed/stale custom/category fallback paths)
    - src/db/systems-catalog-read.ts, src/db/orrery-system-read.ts, src/logic/orrery-system-logic.ts (full generated category-System UID/order/membership reads)
    - src/screens/orrery-screen-framing.test.ts (full framing/source contract)
    - docs/systems/orrery.md (full living contract; append Changelog)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §9
  </read_first>
  <behavior>
    - Exact group order/labels: Built-in, Categories, Custom Systems; empty copy remains visible for last two.
    - Disclosure headers are 44px, expanded semantics/count/chevron; only selected group starts open each false→true open.
    - Existing row order/status/count/override/selection/Manage Systems/busy/error/focus/bounded scroll remain unchanged.
    - Missing selected category uses All Contacts fallback like missing custom; durable ref is already repaired by delete aggregate.
  </behavior>
  <action>
Project existing choices into exactly three typed groups without changing row semantics. Initialize expansion anew on each open transition, never store it in app_settings/Zustand durable preference. Render accessible disclosure headers and required empty bodies inside the existing anchored GlassSurface/ScrollView. Extend stale category handling to the safe All Contacts path while retaining meaningful feedback. Add pure group/open-reset tests, store fallback tests, and framing guards against search/favorites/recents/truncation scope creep. Update `orrery.md` and append Changelog.
  </action>
  <verify><automated>npx vitest run src/components/orrery/orrery-controls-logic.test.ts src/stores/orrery-system-store.test.ts src/screens/orrery-screen-framing.test.ts src/db/systems-catalog-read.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Exact groups/order/empty copy/per-open reset are tested.
    - Existing selector row semantics and Manage Systems behavior remain tested/unchanged.
    - Missing category cannot leave blank/crash/stale selection; no selector search/recents/favorites/truncation is added.
    - Orrery doc has Phase 37.1 Changelog.
  </acceptance_criteria>
  <done>The approved narrow Orrery grouping ships with stable category identity and safe deletion fallback.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Filtered rule options → complete saved draft | Hidden selected UIDs must not be dropped. |
| Persisted/stale System ref → Orrery selection | Deleted category ref must not produce blank/crash or incorrect membership. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-35 | Tampering | Filtered rule draft | high | mitigate | Separate complete draft from visible options and assert hidden selections survive. |
| T-37.1-36 | Denial of service | Stale active ref | high | mitigate | Durable delete repair plus in-session All Contacts fallback tests. |
| T-37.1-37 | Information disclosure | Catalog/search | low | accept | Entirely local, and System Builder search is ephemeral; no egress/logging. |

Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Searchable complete System Builder category-rule behavior.
- Exact typed Orrery selector groups and per-open expansion model.
- Missing-category All Contacts fallback parity.
- Updated Orrery living contract with Changelog.
</artifacts>
<verification>Focused builder/resolver/Orrery/store/catalog tests, typecheck, colors, and Biome pass.</verification>
<success_criteria>Category rules and generated category Systems remain complete, stable, valid, and safely selectable after rename/delete, with no Orrery scope creep.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-11-SUMMARY.md` when done.</output>
