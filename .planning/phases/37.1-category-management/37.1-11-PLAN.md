---
phase: 37.1-category-management
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/category/CategoryChoiceSheet.tsx
  - src/components/category/CategoryChoiceSheet.test.tsx
autonomous: true
gap_closure: true
requirements: [] # Spec-less probe: Phase 37.1 has no assigned requirement IDs.
estimate: { tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The shared CategoryChoiceSheet renders search only when the eligible real-category catalog contains 13 or more rows."
    - "At 12 or fewer eligible real categories the query is ignored and reset, and every eligible choice remains visible."
    - "Uncategorized never affects the threshold, and an excluded deletion target is removed before the threshold is calculated."
  artifacts:
    - { path: src/components/category/CategoryChoiceSheet.tsx, provides: "Canonical searchable decision carried from model to renderer" }
    - { path: src/components/category/CategoryChoiceSheet.test.tsx, provides: "12/13, Uncategorized, exclusion, and query-reset boundary tests" }
  key_links:
    - { from: src/components/category/CategoryChoiceSheet.tsx, to: src/logic/category-logic.ts, via: "buildCategoryChoices(...).searchable" }
---

<objective>Restore the approved progressive-search boundary on the shared complete category-choice surface.</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-category-management/37.1-VERIFICATION.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-05-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Carry canonical searchable state through the choice model</name>
  <read_first>src/components/category/CategoryChoiceSheet.tsx, src/components/category/CategoryChoiceSheet.test.tsx, src/logic/category-logic.ts, src/logic/category-logic.test.ts</read_first>
  <files>src/components/category/CategoryChoiceSheet.tsx, src/components/category/CategoryChoiceSheet.test.tsx</files>
  <behavior>- Twelve eligible real categories produce `searchable: false`; thirteen produce `searchable: true`.
- Adding Uncategorized does not change the real-category count.
- Excluding the deletion source by ID or UID occurs before threshold calculation and can move a 13-row input back below the boundary.
- A nonempty query filters only when searchable; otherwise the complete eligible rows are returned.</behavior>
  <action>Expose `searchable` in `categoryChoiceSheetModel` from `buildCategoryChoices`, after applying exclusion policy. Derive an effective query that is empty whenever search is unavailable, so stale state cannot filter a small catalog. Preserve canonical order, hidden selected identity, real-only policy, and the pinned-last Uncategorized row.</action>
  <acceptance_criteria>The pure model is the single authority for threshold, query applicability, and eligible-row filtering at 12/13.</acceptance_criteria>
  <verify><automated>npx vitest run src/components/category/CategoryChoiceSheet.test.tsx src/logic/category-logic.test.ts</automated></verify>
  <done>The model proves the exact progressive-search contract including deletion exclusion and Uncategorized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and reset search only for large eligible catalogs</name>
  <read_first>src/components/category/CategoryChoiceSheet.tsx, src/components/ui/Sheet.tsx</read_first>
  <files>src/components/category/CategoryChoiceSheet.tsx, src/components/category/CategoryChoiceSheet.test.tsx</files>
  <behavior>- The Search categories input exists only while model.searchable is true.
- Closing the sheet or crossing from searchable to non-searchable clears query state.
- A below-threshold sheet never renders no-match copy caused by an old query.</behavior>
  <action>Conditionally render the existing themed TextInput from `model.searchable`. Reset query when the sheet closes and whenever the eligible model drops below the threshold; ensure the model already ignores the pending stale state during that render. Extend the renderer/model harness rather than creating a second picker or changing established 0–12 native controls.</action>
  <acceptance_criteria>Users see unchanged complete controls through 12 eligible categories and the searchable Sheet at 13+, with no stale-query filtering.</acceptance_criteria>
  <verify><automated>npx vitest run src/components/category/CategoryChoiceSheet.test.tsx && npx tsc --noEmit && npm run check:colors</automated></verify>
  <done>The shared Sheet enforces the UI-SPEC boundary in model and rendering behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| complete local catalog → filtered rendered rows | UI query state must not hide choices when search is outside the contracted threshold. |

## STRIDE Threat Register — ASVS L1, blocking threshold: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-G11-01 | Tampering | excluded deletion source | medium | mitigate | Apply ID/UID exclusion before computing searchable state. |
| T-37.1-G11-02 | Denial of Service | stale hidden query | medium | mitigate | Ignore and reset query below threshold; retain the complete catalog. |
| T-37.1-G11-03 | Information Disclosure | local search | low | accept | Search remains renderer-local over on-device data with no egress. |
</threat_model>

<verification>Run focused chooser/logic tests, typecheck, color gate, and Biome on both changed files. Spec-less probe skip: there are no Phase 37.1 requirement IDs.</verification>
<success_criteria>The shared choice surface is complete and non-searching at 0–12 eligible categories and searchable at exactly 13+.</success_criteria>

## Artifacts this phase produces
- `categoryChoiceSheetModel.searchable`; no new files.

<output>Create `.planning/phases/37.1-category-management/37.1-11-SUMMARY.md` when done.</output>
