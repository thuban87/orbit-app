---
phase: 37.1-category-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logic/category-logic.ts
  - src/logic/category-logic.test.ts
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
  - src/db/systems-dao.ts
  - src/db/systems-dao.test.ts
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
  - src/navigation/settings-routes.ts
  - src/navigation/settings-routes.test.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-contacts-model.ts
  - src/screens/settings-contacts-model.test.ts
autonomous: true
requirements: [] # Phase 37.1 has no assigned requirement IDs; dossier/UI-SPEC provide coverage.
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The reserved Categories Settings destination is live only when a working manager and canonical SQLite writer exist."
    - "Users can add, rename, and reorder ordinary category rows while zero real categories remains valid and seeds are never reconciled back."
    - "Names are trimmed, NFC-normalized, case-folded with locale-independent toLowerCase (not toLocaleLowerCase), bounded to 100 characters, and unique case-insensitively across categories and visible System names; capitalization-only rename is valid."
    - "Canonical display order is contiguous, new rows append, and Uncategorized remains a null option outside that order."
  artifacts:
    - { path: src/logic/category-logic.ts, provides: "Shared name, order, search-threshold, and Uncategorized policy model" }
    - { path: src/db/categories-dao.ts, provides: "Serialized category CRUD and manager reads" }
    - { path: src/screens/CategoryManagementScreen.tsx, provides: "Reachable production Add Category tracer" }
  key_links:
    - { from: src/screens/CategoryManagementScreen.tsx, to: src/db/categories-dao.ts, via: "committed manager load/add path" }
    - { from: src/screens/settings-contacts-model.ts, to: src/navigation/tabs/SettingsStack.tsx, via: "registered CategoryManagement row and screen" }
---

<objective>Establish the production-quality category-management tracer and the complete non-destructive domain contract.</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@docs/dossier/milestone-2/phase-37.1-category-management-dossier.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-PATTERNS.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Add Category through the live Settings route</name>
  <read_first>src/db/contact-read.ts, src/db/transaction.ts, src/db/systems-dao.ts, src/screens/SystemsManagementScreen.tsx, src/navigation/settings-routes.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-contacts-model.ts, src/components/ui/Sheet.tsx, src/components/ShellAppBar.tsx</read_first>
  <files>src/logic/category-logic.ts, src/logic/category-logic.test.ts, src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx, src/navigation/settings-routes.ts, src/navigation/settings-routes.test.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-contacts-model.ts, src/screens/settings-contacts-model.test.ts</files>
  <behavior>- 0 categories loads as valid empty taxonomy with Add and the informational Uncategorized row; no seed write occurs.
- Add trims, NFC-normalizes, and locale-independently case-folds Unicode (deterministic across device locales), rejects blank/101-character/category-duplicate/System-collision names with exact UI-SPEC copy, and appends at max display_order + 1.
- CategoryManagement becomes a registered child Settings route immediately after Custom Fields, with Back parity, initial busy/error states, and SQLite-only load.
- A failed DAO call preserves typed input and committed rows; success appears only after commit.</behavior>
  <action>Create the renderer-independent category model and canonical DAO using bound SQL and one outer `inWriteTransaction` per mutation. Activate Phase-37 D-03's reserved route and build the smallest real manager/Add-sheet path using existing Settings shell and Sheet primitives. Category rows keep stable integer ID and UID; names are display/search text only. Do not add or edit a migration, seed reconciliation, network call, inline screen SQL, or hardcoded color.</action>
  <acceptance_criteria>The Settings row reaches a working manager; Add persists a valid category and refreshes committed UI; every invalid-name case is distinct; zero taxonomy remains zero; TARGET_VERSION remains 29. This tracer commit is an internal (non-release) checkpoint; the route is release-ready only once Plan 04 completes the full manager.</acceptance_criteria>
  <verify><automated>npx vitest run src/logic/category-logic.test.ts src/db/categories-dao.test.ts src/navigation/settings-routes.test.ts src/screens/settings-contacts-model.test.ts src/screens/CategoryManagementScreen.test.tsx</automated></verify>
  <done>The live route→screen→DAO→SQLite→refresh tracer is production-quality and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete lock-safe rename and contiguous reorder APIs</name>
  <read_first>src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/db/systems-dao.ts, src/db/systems-dao.test.ts</read_first>
  <files>src/logic/category-logic.ts, src/logic/category-logic.test.ts, src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/db/systems-dao.ts, src/db/systems-dao.test.ts</files>
  <behavior>- Rename changes only normalized name/modified_at and preserves ID, UID, assignments, rules, refs, and presentation identity.
- Capitalization-only rename excludes the edited row; cross-kind collision validation is symmetric with System writers.
- Reorder rejects duplicate, missing, or stale ID sets before writes; success normalizes all real positions to 0..N-1 and bumps revision once.</behavior>
  <action>Add public create/rename/reorder wrappers over transaction-composable cores, loud row/cardinality checks, deterministic `display_order, uid` reads, and exact revision behavior. Share the same normalized visible-name key (trim → NFC → locale-independent toLowerCase, applied identically in TypeScript and in any SQLite comparison rather than relying on COLLATE NOCASE for Unicode folding) with System writers rather than leaving asymmetric validation. Keep seeds ordinary and Uncategorized outside the stored list.</action>
  <acceptance_criteria>Rename preserves durable identity; invalid reorder rolls back fully; all successful CRUD writes advance `data_revision` exactly once.</acceptance_criteria>
  <verify><automated>npx vitest run src/logic/category-logic.test.ts src/db/categories-dao.test.ts src/db/systems-dao.test.ts</automated></verify>
  <done>The non-destructive category API is complete and safe for manager and consumer integration.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-01 | Tampering: crafted category names/SQL | high | mitigate | NFC/length/collision validation under writer lock and parameter-bound values |
| T-37.1-02 | Tampering: stale reorder | high | mitigate | compare complete ID set/cardinality before any update; rollback-tested transaction |
| T-37.1-03 | Information disclosure | low | accept | all reads remain in the app sandbox and errors contain no contact data |
</threat_model>

<verification>Run focused tests, then `npx tsc --noEmit`, Biome on changed files, and `npm run check:colors`.</verification>
<success_criteria>The user can reach Categories and safely add, rename, and reorder without migration, reseed, identity drift, or optimistic false success.</success_criteria>

## Artifacts this phase produces
- Canonical category logic/DAO and the live CategoryManagement tracer.

<output>Create `.planning/phases/37.1-category-management/37.1-01-SUMMARY.md` when done.</output>
