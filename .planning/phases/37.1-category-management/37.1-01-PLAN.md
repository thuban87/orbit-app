---
phase: 37.1-category-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logic/category-logic.ts
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
  - src/screens/CategoryManagementScreen.tsx
  - src/screens/CategoryManagementScreen.test.tsx
  - src/navigation/settings-routes.ts
  - src/navigation/settings-routes.test.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-contacts-model.ts
  - src/screens/settings-contacts-model.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Settings → Contacts & Relationships exposes the formerly reserved Categories route and a real manager that can load the current taxonomy and add a category without network access."
    - "A new category is trimmed, NFC-normalized, nonblank, at most 100 characters, unique across categories and visible built-in/custom System names, and appended after the current real categories."
    - "Zero categories is a valid loaded state; the manager never reseeds Family, Friends, Work, or Community."
    - "Successful creation is published only after commit; failed creation retains the typed name and last committed list, and the shared actionless success Snackbar is added by Plan 13."
  artifacts:
    - path: src/logic/category-logic.ts
      provides: "Shared normalized category-name, visible-name-key, ordering, search-threshold, Uncategorized-policy, and persisted-filter helpers"
    - path: src/db/categories-dao.ts
      provides: "Canonical category read/create boundary and transaction-composable category cores"
    - path: src/screens/CategoryManagementScreen.tsx
      provides: "Reachable manager shell and production Add flow"
  key_links:
    - from: src/screens/CategoryManagementScreen.tsx
      to: src/db/categories-dao.ts
      via: "focus-aware list read and createCategory commit"
    - from: src/db/categories-dao.ts
      to: src/db/systems-dao.ts
      via: "shared normalized visible-name collision contract over categories, built-ins, and custom Systems"
    - from: src/screens/settings-contacts-model.ts
      to: src/navigation/tabs/SettingsStack.tsx
      via: "CategoryManagement route row backed by a registered Stack.Screen"
---

<objective>
Deliver the production tracer: activate Phase 37's reserved Categories IA slot and carry one real Add Category path from the Settings row through a themed manager and canonical DAO into SQLite, with committed refresh and exact success/error behavior.

Purpose: prove the route → screen → model → serialized SQLite writer → refreshed UI architecture before expanding destructive and cross-consumer behavior.
Output: shared category logic, canonical DAO, registered manager screen, Add sheet, committed refresh seam, and focused tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@docs/dossier/milestone-2/phase-37.1-category-management-dossier.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-PATTERNS.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-08-SUMMARY.md
</context>

<assumption_delta_decision noun="category identity" decision="no-change">
The deterministic detector fired on “custom,” but this phase adds custom-System consumers rather than another category-membership identity. Keep one nullable numeric category ID as local contact identity and one stable category UID for portable/System references; do not add a parallel name identity, tag identity, or multi-category representation.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Add Category through the live Settings route</name>
  <reversibility rating="reversible">The new runtime DAO, route, and UI are code-only additions over the existing schema; rollback removes them without transforming stored rows.</reversibility>
  <files>src/logic/category-logic.ts, src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/screens/CategoryManagementScreen.tsx, src/screens/CategoryManagementScreen.test.tsx, src/navigation/settings-routes.ts, src/navigation/settings-routes.test.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-contacts-model.ts, src/screens/settings-contacts-model.test.ts</files>
  <read_first>
    - HANDOFF.md (full decision record; especially local-first, migration discipline, theme tokens, and §14 Phase-16 supersession)
    - docs/dossier/milestone-2/phase-37.1-category-management-dossier.md (full authoritative phase decisions; especially A–F, Q, and deferrals)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md (full approved manager/Add/copy/a11y contract)
    - src/db/migrations/001-initial.ts and src/db/migrations/007-tombstones.ts (full shipped category schema/seeds/UID assignment; read only, never edit)
    - src/db/transaction.ts, src/db/data-revision-dao.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts (full transaction/core/revision/reorder precedents)
    - src/db/systems-dao.ts and src/db/systems-dao.test.ts (full visible System-name uniqueness writers; both category and System writers must share one normalized comparison contract)
    - src/db/contact-read.ts and src/db/contact-read.test.ts (full current listCategories consumers/read shape)
    - src/screens/SystemsManagementScreen.tsx and src/screens/SystemsManagementScreen.test.tsx (full list, committed-order, focus-load, accessibility, and Settings visual precedent)
    - src/components/ui/Sheet.tsx, src/components/ui/AppText.tsx, src/components/ui/Button.tsx, src/components/ui/ShellAppBar.tsx (full approved primitives)
    - src/navigation/settings-routes.ts, src/navigation/settings-routes.test.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/types.ts, src/screens/settings-contacts-model.ts, src/screens/settings-contacts-model.test.ts (full Phase-37 reserved route/IA contract to invert)
  </read_first>
  <behavior>
    - Name model: whitespace-only → blank error; trimmed NFC strings of length 1–100 accepted; 101 rejected; Unicode retained; category and visible System comparisons use one locale-independent normalized key; capitalization-only rename can later exclude its own row.
    - DAO create: validates again inside the serialized writer, rejects category/built-in/custom-System visible-name collisions, allocates a stable UID, appends at max display_order + 1 (zero rows → 0), and bumps data_revision exactly once.
    - Manager: unresolved initial read shows the locked busy state; failure shows `Couldn’t load categories. Try again.`; zero rows shows the locked empty state plus Add and Uncategorized; Add uses the compact shared Sheet and locked copy.
    - Route contract: CategoryManagement changes from typed/inert reservation to registered route row immediately after Custom Fields.
  </behavior>
  <action>
Write failing DAO tests and one integrated screen/source-contract test first; keep pure model cases in those two owned test files so this tracer does not spread into three additional test owners. Create `category-logic.ts` with `CATEGORY_NAME_MAX_LENGTH = 100`, `CATEGORY_SEARCH_THRESHOLD = 12`, trim+NFC normalization, a shared case-folded visible-name key, deterministic `display_order, uid` sorting, explicit null/Uncategorized policy types, selection-preserving local filtering, and a pure Dashboard category-filter sanitizer. Do not make names durable identity.

Create `categories-dao.ts` as the only runtime writer for the existing `categories` table. Expose typed list/manager-count reads and transaction-composable cores plus public `createCategory`. Serialize with one `inWriteTransaction`, bind every value, recheck category and built-in/custom System names under the lock, use `newUid()`, append without reseeding, assert expected row counts, and bump `data_revision` once. Do not edit migrations, `TARGET_VERSION`, seed rows, or backup format.

Create `CategoryManagementScreen.tsx` in the existing Settings leaf-shell pattern. Implement initial loading, retry/error, zero/populated manager rows, the full-width `+ Add Category` anchor, informational Uncategorized row/count, and compact Add Sheet with exact labels/errors. Counts include archived and Unbound contacts. Preserve input and committed rows on error; prevent duplicate submit; close and publish only after commit. Use only DAO calls, theme tokens, AppText roles, established spacing, 44px controls, and local SQLite.

Activate `CategoryManagement` per Phase 37 decision D-03 in `SETTINGS_REGISTERED_ROUTES` and `SettingsStack`; the existing param list already reserves the route and must remain type-compatible without modification. Replace the inert reservation with the exact `Categories` / `Create and organize the categories your contacts use.` route row immediately after Custom Fields. Update the two existing source-contract suites that currently enforce the reservation so the repository never enters a knowingly failing state. No inline CRUD appears on any other surface.
  </action>
  <verify>
    <automated>npx vitest run src/db/categories-dao.test.ts src/screens/CategoryManagementScreen.test.tsx src/navigation/settings-routes.test.ts src/screens/settings-contacts-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - The focused tests prove blank/100/101/NFC/case behavior, cross-kind collisions, append order, zero taxonomy, exactly one revision bump, route registration, locked Add/empty/load-error copy, and no reseed.
    - `CategoryManagementScreen` issues no SQL and no network request; all category reads/writes use `categories-dao`.
    - The initial four rows remain ordinary mutable data; migrations 001/007 and TARGET_VERSION are unchanged.
    - A real user can navigate Settings → Contacts & Relationships → Categories, add a valid category, see it appended, and expose a post-commit success publication seam consumed by Plan 13.
  </acceptance_criteria>
  <done>The first production-quality vertical slice is real and green: Settings route → Add Sheet → canonical serialized DAO → existing SQLite schema → refreshed manager.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Category name / route action → DAO → SQLite | User-controlled Unicode text crosses into durable local storage. |
| Category catalog → Settings UI | A complete local list must remain available without network or truncation. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-01 | Tampering | Category-name writes | high | mitigate | Normalize and validate in shared logic and again under the serialized DAO transaction; bind all SQL values; assert row counts. |
| T-37.1-02 | Spoofing | Cross-kind visible names | high | mitigate | Compare one normalized key against categories, closed built-in labels, and custom Systems under the writer lock. |
| T-37.1-03 | Information disclosure | Manager diagnostics | medium | mitigate | Surface locked generic errors and aggregate counts only; no contact names/rules/backup contents in logs or UI. |
| T-37.1-04 | Denial of service | Complete category list | medium | mitigate | Local ordered reads, scrolling, and the shared search threshold; no hard cutoff or network dependency. |

Package legitimacy: N/A — no package install.
</threat_model>

<artifacts>
## Artifacts this phase produces

- `category-logic.ts`: `CATEGORY_NAME_MAX_LENGTH`, `CATEGORY_SEARCH_THRESHOLD`, normalization/key/order/search/null-policy helpers.
- `categories-dao.ts`: category row/count types, list/create APIs, and composable cores used by later deletion work.
- `CategoryManagementScreen.tsx` and its focused contract test.
- Live `CategoryManagement` Settings route and row.
- A post-commit Add success publication seam consumed by Plan 13's shared Snackbar extension.
</artifacts>

<verification>
Run the focused tests, typecheck, color gate, and Biome on every changed file. Confirm `git diff -- src/db/migrations src/db/database.ts src/backup/types.ts` is empty.
</verification>

<success_criteria>
The live Settings entry can add a valid category end-to-end with exact loading/error/empty/success behavior, shared validation, stable identity, canonical append order, no migration/reseed/network path, and objective focused coverage.
</success_criteria>

<output>
Create `.planning/phases/37.1-category-management/37.1-01-SUMMARY.md` when done.
</output>
