---
phase: 37.1-category-management
plan: 05
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - src/logic/category-logic.ts
  - src/logic/category-logic.test.ts
  - src/components/category/CategoryChoiceSheet.tsx
  - src/components/category/CategoryChoiceSheet.test.tsx
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/category-consumer-contract.test.ts
autonomous: true
requirements: []
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Every ordinary selector uses canonical order, a complete catalog, and explicit per-surface Uncategorized/real-only policy."
    - "At 0–12 real categories established controls remain; at 13+ a complete local searchable choice Sheet appears with hidden selections retained and no cutoff."
    - "Create/Edit and Bulk Import setup/review resolve stale IDs safely and never provide inline category CRUD."
    - "Import deletion semantics remain all-status in storage while open import consumers refresh from committed truth."
  artifacts:
    - { path: src/components/category/CategoryChoiceSheet.tsx, provides: "Shared large-catalog single-select surface with an optional exclude-by-id/uid prop (consumed by Plan 04's used-delete reassignment picker to omit the deleted row)" }
    - { path: src/logic/category-logic.ts, provides: "Closed selection/search/Uncategorized policies" }
  key_links:
    - { from: src/components/category/CategoryChoiceSheet.tsx, to: src/logic/category-logic.ts, via: "threshold/filter/order/selection model" }
    - { from: src/screens/BulkImportSetupScreen.tsx, to: src/db/import-session-dao.ts, via: "real ID or null session choice" }
---

<objective>Build one complete, policy-driven category-choice model and integrate the contact/import consumers that share it.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-01-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock the shared complete-catalog model and searchable choice Sheet</name>
  <read_first>src/logic/category-logic.ts, src/db/contact-read.ts, src/components/ui/Sheet.tsx, src/components/ui/AppText.tsx</read_first>
  <files>src/logic/category-logic.ts, src/logic/category-logic.test.ts, src/components/category/CategoryChoiceSheet.tsx, src/components/category/CategoryChoiceSheet.test.tsx</files>
  <behavior>- `CATEGORY_SEARCH_THRESHOLD = 12`; search appears at 13+, filters locally/case-insensitively, preserves canonical order, and never truncates.
- Clearing query restores all rows; no-match exact copy appears without clearing selection.
- Uncategorized is a distinct null option pinned last only when policy permits; real-only consumers receive no synthetic row.
- 0/1/12/13/many/long Unicode/stale-selection fixtures are covered.</behavior>
  <action>Extend the renderer-independent category model and build a shared themed expanded single-select Sheet used only for large catalogs. Keep selection independent from filtered visibility and preserve each surface's existing small-catalog control. Load through DAO/read boundaries; no inline SQL or inline management affordance.</action>
  <acceptance_criteria>Every catalog size remains fully reachable and policy-consistent with exact search/empty copy.</acceptance_criteria>
  <verify><automated>npx vitest run src/logic/category-logic.test.ts src/components/category/CategoryChoiceSheet.test.tsx</automated></verify>
  <done>The reusable selection contract is closed and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Create/Edit Contact and Bulk Import setup/review</name>
  <read_first>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/BulkImportSetupScreen.tsx, src/screens/ImportReviewScreen.tsx, src/db/contact-read.ts, src/db/import-session-dao.ts</read_first>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/BulkImportSetupScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/category-consumer-contract.test.ts</files>
  <behavior>- All four screens display real categories in display_order with Uncategorized last and use search at exactly 13+.
- Stored values remain numeric ID/null; deleted/stale choices resolve to null and cannot submit a dead FK.
- Focus/open refreshes the catalog; complete/discarded import-session rows remain unchanged except when the deletion aggregate deliberately reassigns their batch category.</behavior>
  <action>Adopt the shared model/Sheet while preserving existing small-list interaction and validation. Replace legacy `No category` user copy with canonical `Uncategorized`. Refresh at focus/open and reject stale submission against current DAO truth. Do not add Add/Rename/Delete controls outside Settings.</action>
  <acceptance_criteria>Create/Edit/Import consumers are complete, searchable, stale-safe, and identity-correct at arbitrary category count.</acceptance_criteria>
  <verify><automated>npx vitest run src/screens/category-consumer-contract.test.ts src/db/import-session-dao.test.ts src/db/contact-read.test.ts</automated></verify>
  <done>Contact and import selectors share one canonical behavior.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-37.1-13 | Tampering: stale selected ID | high | mitigate | refresh/open plus DAO/FK validation before submit |
| T-37.1-14 | DoS: inaccessible long catalog | medium | mitigate | complete bounded list and local search at 13+, no cutoff |
| T-37.1-15 | Information disclosure | low | accept | local filtering only; no logging/network path |
</threat_model>
<verification>Focused model/component/consumer tests plus typecheck and color gate.</verification>
<success_criteria>Contact and import category selection stays complete, canonical, and safe after runtime mutation.</success_criteria>
## Artifacts this phase produces
- Shared CategoryChoiceSheet and contact/import integrations.
<output>Create `.planning/phases/37.1-category-management/37.1-05-SUMMARY.md` when done.</output>
