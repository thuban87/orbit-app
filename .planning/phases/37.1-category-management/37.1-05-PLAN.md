---
phase: 37.1-category-management
plan: 05
type: execute
wave: 4
depends_on: ["37.1-04"]
files_modified:
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/backup/phase-17-integration.test.ts
  - docs/systems/backup-restore.md
  - docs/systems/orrery-systems-backup-contract.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 46000, raw_tokens: 46000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Merge applies category deletion in dependency-safe order and never issues a raw parent delete while numeric/TEXT/JSON dependents still reference it."
    - "Replace-all restores exactly the selected backup taxonomy and order, including zero categories, with no destination extras and no seed reconciliation."
    - "A forced apply failure rolls back categories, contacts, Systems, settings, presentation, tombstones, and revision as one graph."
  artifacts:
    - path: src/backup/restore-apply.ts
      provides: "Category-aware merge delete and exact replace-all reset/application"
  key_links:
    - from: src/backup/reconciliation.ts
      to: src/backup/restore-apply.ts
      via: "survivor-normalized category actions"
    - from: src/backup/restore-apply.ts
      to: src/db/categories-dao.ts
      via: "transaction-composable category fallout core or equivalent category-aware apply step"
---

<objective>
Apply the v5 category reconciliation plan safely for merge and reproduce the exact taxonomy for replace-all, including an intentionally empty catalog.
</objective>

<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@docs/decisions/ADR-056-tombstone-backed-uid-reconciliation-for-portable-restores.md
@docs/decisions/ADR-058-optional-encrypted-backups-and-previewed-local-restoration.md
@docs/decisions/ADR-063-versioned-lifecycle-backup-and-dormant-cadence-restore.md
@.planning/phases/37.1-category-management/37.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category-aware merge deletion and rollback-safe apply</name>
  <reversibility rating="costly">Merge reconciliation is a portable data contract; a wrong apply can destroy live rows, so the established preview/snapshot/transaction gates remain mandatory.</reversibility>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts</files>
  <read_first>
    - src/backup/restore-apply.ts and src/backup/restore-apply.test.ts (full file; inspect all entity order, writers, settings apply, rollback, and current raw category delete path)
    - src/backup/reconciliation.ts and src/backup/reconciliation.test.ts (full Plan-04 plan/survivor contract)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full transaction-composable category fallout implementation; do not nest its public wrapper)
    - src/db/app-settings-dao.ts, src/db/systems-dao.ts, src/db/profile-presentation-dao.ts, src/db/tombstones-dao.ts (full alternate writers/composable cores)
    - docs/systems/backup-restore.md and docs/systems/orrery-systems-backup-contract.md (full living contracts)
  </read_first>
  <behavior>
    - A tombstone-winning category is repaired/deleted with all FK/ref/rule/filter/presentation dependents before the parent row.
    - Older category-specific rule/ref state is absent after apply; unrelated historical broken rules remain.
    - Forced failure at the category-aware apply stage rolls back the complete graph.
  </behavior>
  <action>
Replace generic raw category deletion with a category-aware apply step using transaction-composable cores or equivalent bound statements inside restore's single existing `inWriteTransaction`; never call the public category delete wrapper from within it. Consume Plan-04 survivor metadata, repair nullable FKs and ref/JSON dependents, and preserve restore's global apply ordering. Keep complete plan construction and normalization before mutation. Add merge fixtures for active selection, Dashboard filters, import-local reset interaction, category rules/refs, Profile presentation, equal-timestamp tombstones, unrelated rules, and forced rollback.
  </action>
  <verify><automated>npx vitest run src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - No generic category parent DELETE executes ahead of dependent repair.
    - Merge cannot resurrect category-specific rules/refs and retains unrelated historical diagnostics.
    - Forced failure proves whole-graph rollback.
  </acceptance_criteria>
  <done>Merge category deletion is dependency-safe, tombstone-aware, and atomic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exact replace-all taxonomy including zero categories, plus living contracts</name>
  <reversibility rating="costly">Replace-all intentionally overwrites live local state and is protected by the existing verified pre-restore snapshot; this task changes no wire version.</reversibility>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts, docs/systems/backup-restore.md, docs/systems/orrery-systems-backup-contract.md</files>
  <read_first>
    - src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/phase-17-integration.test.ts (full current replace-all reset/apply/integration fixtures)
    - src/db/migrations/001-initial.ts and src/db/migrations/007-tombstones.ts (seed creation is migration-only; never call from restore)
    - docs/systems/backup-restore.md and docs/systems/orrery-systems-backup-contract.md (full docs; append Changelogs)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§10–11
  </read_first>
  <behavior>
    - Nonempty backup replaces destination categories/order exactly.
    - Empty-category backup results in zero categories and valid Uncategorized contacts.
    - Destination-only categories and their dependents do not survive; no seed row returns.
    - Failure after reset begins rolls back the entire graph.
  </behavior>
  <action>
Add `categories` to replace-all reset only after category-dependent rows and contacts are removed and before incoming category upserts. Ensure incoming contacts resolve category UID only against incoming survivors and zero-category manifests remain zero. Do not invoke seed logic or bump v5. Extend integration tests for nonempty exactness, empty taxonomy, destination extras, and forced failure. Update `backup-restore.md` with category tombstones/exact taxonomy and append its Changelog. Update `orrery-systems-backup-contract.md` to explicitly distinguish runtime category deletion/tombstone-lost restore rules (remove affected rule/ref) from unrelated historical missing-category rules (preserve diagnostic), and append its Changelog; do not edit ADRs.
  </action>
  <verify><automated>npx vitest run src/backup/restore-apply.test.ts src/backup/phase-17-integration.test.ts src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Replace-all is exact for both nonempty and zero-category inputs and leaves no factory seeds/extras.
    - Full apply rollback remains green.
    - Both living docs describe implemented behavior and contain Phase 37.1 Changelog entries.
    - BACKUP_FORMAT_VERSION remains 5.
  </acceptance_criteria>
  <done>Merge and replace-all both honor the user's real taxonomy, durable deletion evidence, and atomic restore contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Reconciliation plan → local SQLite transaction | Prevalidated untrusted backup decisions become destructive writes. |

## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-16 | Tampering | Merge apply order | high | mitigate | Category-aware dependency-first repair and exact row checks inside one existing transaction. |
| T-37.1-17 | Tampering / DoS | Replace-all reset | high | mitigate | Delete children/contacts before categories, apply exact incoming set, preserve pre-restore snapshot gate, rollback test. |
| T-37.1-18 | Repudiation | Restore result | medium | mitigate | Keep existing typed result/diagnostic contract and durable tombstones; do not expose private contents. |

Package legitimacy: N/A.
</threat_model>

<artifacts>
## Artifacts this phase produces
- Category-aware restore apply path.
- Exact category-inclusive replace-all reset.
- Updated backup and Orrery-System living contracts with Phase 37.1 Changelogs.
</artifacts>

<verification>Focused backup integration, typecheck, and changed-file Biome pass; format remains v5 and no migration is added.</verification>
<success_criteria>Merge and replace-all safely apply the complete category graph with no resurrection, extras, reseed, partial state, or wire bump.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-05-SUMMARY.md` when done.</output>
