---
phase: 37.1-category-management
plan: 08
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-07"]
files_modified:
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/category-consumer-contract.test.ts
  - docs/systems/contacts.md
  - docs/systems/contact-import.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Create/Edit persist numeric category ID or null and expose canonical real categories plus Uncategorized last; at 13+ all choices remain reachable through the shared Sheet."
    - "Bulk Import setup/review use the same complete policy, and a pending default deleted mid-session moves atomically to the delete target."
    - "A picker reopened/focused after mutation refreshes; a stale/deleted selection resolves to Uncategorized and never submits a dead ID."
  artifacts:
    - path: src/screens/category-consumer-contract.test.ts
      provides: "Cross-screen source/model contract for contact/import category consumers"
  key_links:
    - from: src/screens/CreateContactScreen.tsx
      to: src/components/category/CategoryChoiceSheet.tsx
      via: "13+ single-select adapter"
    - from: src/screens/BulkImportSetupScreen.tsx
      to: src/db/import-session-dao.ts
      via: "validated pending session category ID/null"
---

<objective>
Integrate the shared complete category model into contact authoring and durable import review without changing their established small-catalog controls or single-category model.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@docs/decisions/ADR-065-durable-resumable-contact-import-sessions-with-failure-isolated-photos.md
@docs/decisions/ADR-066-deliberate-reviewed-import-with-unbound-bulk-defaults.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create/Edit complete category choices and stale-submit safety</name>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/category-consumer-contract.test.ts</files>
  <read_first>
    - src/screens/CreateContactScreen.tsx and src/screens/EditContactScreen.tsx (full files, including listCategories load, current category controls, focus/error/save state)
    - src/db/contact-read.ts and src/db/contacts-dao.ts (full read and every contact category writer)
    - src/components/category/CategoryChoiceSheet.tsx and src/logic/category-logic.ts (full shared policy)
    - src/stores/shell-refresh-store.ts (full refresh subscription)
    - docs/systems/contacts.md (full living contract; append Changelog)
  </read_first>
  <behavior>
    - 0–12 retains each screen's control with canonical options and Uncategorized label; 13+ opens shared searchable Sheet.
    - Mutation while screen/picker is open refreshes catalog; deleted selected ID becomes null; save preflight/foreign key rejects a stale ID and refreshes instead of persisting it.
    - Rename changes only displayed joined label after refresh.
  </behavior>
  <action>
Replace independent category option shaping with shared logic/Sheet while preserving existing create/edit validation, draft, and save boundaries. Subscribe/refresh through focus and shell revision. Use `Uncategorized` instead of `No category`; never introduce inline CRUD. Validate current selected ID against the refreshed complete catalog before submit and resolve missing to null with the established error/refresh path. Add source/model integration tests and update `contacts.md` identity/order/search/stale behavior with a Phase 37.1 Changelog row.
  </action>
  <verify><automated>npx vitest run src/screens/category-consumer-contract.test.ts src/logic/category-logic.test.ts src/db/contacts-dao.test.ts src/db/contact-read.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Both screens use the shared 13+ Sheet/model and keep numeric ID/null persistence.
    - Stale IDs cannot be submitted; zero categories still offers Uncategorized.
    - docs/systems/contacts.md is updated with Changelog.
  </acceptance_criteria>
  <done>Create/Edit remain complete, ordered, single-category, and safe across live mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulk Import setup/review complete choices and pending-default refresh</name>
  <files>src/screens/BulkImportSetupScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/category-consumer-contract.test.ts, docs/systems/contact-import.md</files>
  <read_first>
    - src/screens/BulkImportSetupScreen.tsx and src/screens/ImportReviewScreen.tsx (full durable session/category picker flows)
    - src/db/import-session-dao.ts, src/db/import-session-read.ts, and their tests (full direct session writers/readers and pending/complete/discarded status semantics)
    - src/db/contacts-dao.ts and src/services/import/import-driver.ts (full shared contact-create/import execution seams)
    - src/components/category/CategoryChoiceSheet.tsx and src/logic/category-logic.ts (full shared policy)
    - docs/systems/contact-import.md (full living contract; append Changelog)
  </read_first>
  <behavior>
    - Setup/review expose complete canonical choices and Uncategorized at all sizes; search begins at 13.
    - Pending session default refreshes/resolves after category mutation; complete/discarded historical rows are not rewritten by runtime delete.
    - Delete-to-category/null is observable on resumed pending session and no stale ID submits.
  </behavior>
  <action>
Integrate the shared model/Sheet into both import surfaces, retaining their durable session transitions and established 0–12 controls. Refresh on focus/open and validate the selected default before persistence. Ensure UI reflects Plan-02's pending-only atomic default reassignment without rewriting complete/discarded history. Extend the cross-screen contract test/DAO fixture and update `contact-import.md` with the pending default deletion behavior, complete/searchable catalog, and Phase 37.1 Changelog.
  </action>
  <verify><automated>npx vitest run src/screens/category-consumer-contract.test.ts src/db/import-session-dao.test.ts src/db/import-session-read.test.ts src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Both import screens use shared complete/searchable category behavior.
    - Pending defaults follow the chosen deletion target atomically; complete/discarded rows are unchanged.
    - docs/systems/contact-import.md is updated with Changelog.
  </acceptance_criteria>
  <done>Contact/import authoring shares one complete category contract and never persists a deleted selection.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Screen draft/session → contact/import DAO | Stale numeric IDs can cross into durable FK columns. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-26 | Tampering | Stale category submit | high | mitigate | Refresh complete catalog, preflight selected ID, retain FK rejection as backstop. |
| T-37.1-27 | Denial of service | Large picker | high | mitigate | Shared complete Sheet/search at 13+, no cutoff. |
| T-37.1-28 | Information disclosure | Local search | low | accept | Ephemeral on-device filter only; no logging/network. |

Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Cross-screen contact/import category consumer contract test.
- Updated contact/import living contracts with Phase 37.1 Changelogs.
</artifacts>
<verification>Focused consumer/DAO/model tests, typecheck, color gate, and Biome pass.</verification>
<success_criteria>Every contact/import category choice remains complete, canonical, null-safe, mutation-aware, and offline.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-08-SUMMARY.md` when done.</output>
