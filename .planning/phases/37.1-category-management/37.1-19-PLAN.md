---
phase: 37.1-category-management
plan: 19
type: execute
wave: 8
depends_on: ["37.1-20"]
files_modified:
  - src/screens/__dev__/CategoryManagementUatScreen.tsx
  - src/screens/__dev__/CategoryManagementUatScreen.test.tsx
  - src/components/ui/__dev__/ThemePreviewScreen.tsx
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/types.ts
  - src/navigation/settings-routes.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "A developer must deliberately activate the synthetic category UAT database before destructive controls become available."
    - "The harness is reachable only through an existing compile-time development surface and has no production route, deep link, persisted switch, or external intent."
    - "The normal Category Management, Dashboard, Profile, Orrery, and System Builder screens all observe the same isolated executor while UAT is active."
  artifacts:
    - path: src/screens/__dev__/CategoryManagementUatScreen.tsx
      provides: "Explicit activation, aggregate baseline, one-shot failure, reset, and cleanup controls"
  key_links:
    - from: src/components/ui/__dev__/ThemePreviewScreen.tsx
      to: src/screens/__dev__/CategoryManagementUatScreen.tsx
      via: "development-only launcher and guarded Settings stack route"
    - from: src/screens/__dev__/CategoryManagementUatScreen.tsx
      to: src/screens/CategoryManagementScreen.tsx
      via: "normal navigation after Plan 20 activates the app-wide isolated executor"
---

<objective>Expose Plan 20's disposable app-wide database and one-shot fault through an explicit, production-unreachable device-UAT control surface.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-20-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build explicit UAT activation, fault, and cleanup controls</name>
  <files>src/screens/__dev__/CategoryManagementUatScreen.tsx, src/screens/__dev__/CategoryManagementUatScreen.test.tsx</files>
  <read_first>
    - src/screens/__dev__/category-management-uat.ts and test (full Plan-20 activation, fixture, one-shot fault, reset, and cleanup API)
    - src/screens/CategoryManagementScreen.tsx, src/screens/HomeScreen.tsx, src/screens/ContactProfileScreen.tsx, src/screens/OrreryScreen.tsx, and src/screens/SystemBuilderScreen.tsx (full consumers reached after activation)
    - src/components/BackupEncryptionBenchmarkHarness.tsx (full existing explicit development-harness precedent)
  </read_first>
  <behavior>
    - The initial screen says the normal database is active and offers only an explicit Create and activate synthetic database action.
    - Successful activation shows the closed UAT filename, synthetic-data warning, aggregate row counts/order/revision only, and enables normal-screen navigation plus one-shot failure, reset, and close/delete controls.
    - Destructive controls reject use until activation; fault arming is explicit and one-shot; cleanup deactivates first, closes/deletes only the exact UAT file, and confirms the normal executor is restored.
  </behavior>
  <action>Build an unmistakable development-only UAT control screen over Plan 20's closed API. Do not inject an executor prop into CategoryManagementScreen: activate the app-wide development override, then navigate to the ordinary Category Management route and the ordinary Dashboard/Profile/Orrery/System Builder routes so their existing `getExecutor()` calls share the disposable database. Show only synthetic labels and aggregate counts; expose no SQL, parameters, row contents, or normal-database metadata. Require an explicit button press to activate and a separate explicit press to arm one generic category-delete failure. Make reset deterministic. Cleanup must deactivate before closing/deleting the exact UAT database and visibly confirm both file absence and restoration of the normal executor. A reload/crash must start with no override because enablement is memory-only.</action>
  <verify><automated>npx vitest run src/screens/__dev__/CategoryManagementUatScreen.test.tsx src/screens/__dev__/category-management-uat.test.ts src/db/database.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - The normal screens, not harness copies, exercise the real screen→DAO paths against one shared synthetic database.
    - Fault injection is default-off and one-shot; private values and normal-database metadata are never rendered or logged.
    - Cleanup restores the normal executor before exact-target close/delete and reports the result.
  </acceptance_criteria>
  <done>The safe UAT controls can activate, exercise, reset, and remove the isolated app-wide dataset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register a compile-time development-only route and launcher</name>
  <files>src/components/ui/__dev__/ThemePreviewScreen.tsx, src/navigation/tabs/SettingsStack.tsx, src/navigation/types.ts, src/navigation/settings-routes.test.ts, src/screens/__dev__/CategoryManagementUatScreen.test.tsx</files>
  <read_first>
    - src/navigation/tabs/SettingsStack.tsx, src/navigation/types.ts, and src/navigation/settings-routes.test.ts (full route registry and existing `__ThemePreview` guard)
    - src/components/ui/__dev__/ThemePreviewScreen.tsx (full existing development-only surface)
    - app.config.ts and src/navigation/linking.ts (full production package/config and linking surfaces; assert no UAT registration)
  </read_first>
  <behavior>
    - `__CategoryManagementUat` is required and rendered only inside compile-time `__DEV__` guards.
    - The only launcher is a development-only control inside the already guarded Theme Preview screen.
    - Release-mode source/config tests prove there is no ordinary Settings row, deep link, persisted enablement, external intent, or route registration.
  </behavior>
  <action>Follow the established `const X = __DEV__ ? require(...).default : null` and `__DEV__ &amp;&amp; X` Stack.Screen pattern exactly. Add the launcher only inside the existing development-only ThemePreviewScreen; do not add a normal Settings row. Keep the route out of linking configuration and external intents. Tests must evaluate the release branch/source contract and prove the harness module is not required or registered when `__DEV__` is false, activation is not persisted, and production has no navigation entry. Keep all colors on theme tokens.</action>
  <verify><automated>npx vitest run src/navigation/settings-routes.test.ts src/screens/__dev__/CategoryManagementUatScreen.test.tsx</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - The harness is reachable for a deliberate development build while remaining absent from release navigation and linking.
    - Automated proof covers both the compile-time require/render guard and lack of persisted/external enablement.
  </acceptance_criteria>
  <done>The physical-UAT control surface fails closed in production and is intentionally reachable in development.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Development launcher → UAT controls | Fault tooling must remain absent from production and require deliberate entry. |
| UAT controls → normal app routes | Every destructive action must operate on the visibly active synthetic executor. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-50 | Elevation of privilege | Fault harness route | high | mitigate | Compile-time `__DEV__` require/render guards, development-only launcher, no linking/ordinary Settings row, release-source tests. |
| T-37.1-51 | Tampering | Normal app routes | high | mitigate | Visible active-state gate and Plan-20 app-wide executor override before normal navigation. |
| T-37.1-52 | Information disclosure | UAT display and diagnostics | high | mitigate | Synthetic labels and aggregate counts only; never expose SQL parameters, private rows, or normal-database metadata. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Compile-time development-only Category Management UAT route and launcher.
- Explicit activation, one-shot failure, reset, aggregate baseline, and cleanup controls for ordinary app routes.
</artifacts>
<verification>Focused UAT-screen and route tests, release-unreachability proof, typecheck, colors, and changed-file Biome pass.</verification>
<success_criteria>Physical rollback and cross-screen refresh UAT can use ordinary production UI paths while production builds cannot reach the controls.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-19-SUMMARY.md` when done.</output>
