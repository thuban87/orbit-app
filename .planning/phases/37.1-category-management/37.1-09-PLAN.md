---
phase: 37.1-category-management
plan: 09
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-07"]
files_modified:
  - src/screens/HomeScreen.tsx
  - src/components/control-surface/DashboardControlRow.tsx
  - src/components/control-surface/FilterPanelContent.tsx
  - src/components/control-surface/filter-summary.ts
  - src/components/control-surface/filter-summary.test.ts
  - src/stores/dashboard-query-store.ts
  - src/stores/dashboard-query-store.test.ts
  - src/screens/dashboard-category-contract.test.ts
  - docs/systems/dashboard.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Dashboard category filters preserve numeric-ID selections plus an explicit Uncategorized/null choice, canonical order, complete 13+ search, and hidden selections."
    - "Deleting a category removes its durable filter token before results refresh; an emptied category family uses established unfiltered behavior without altering other filter families."
    - "Bulk Set Category includes Uncategorized last, rejects stale targets, and refreshes list/card labels only after commit."
  artifacts:
    - path: src/screens/dashboard-category-contract.test.ts
      provides: "Dashboard filter/bulk mutation category integration contract"
  key_links:
    - from: src/components/control-surface/FilterPanelContent.tsx
      to: src/logic/category-logic.ts
      via: "shared complete multi-select filtering with explicit null policy"
    - from: src/screens/HomeScreen.tsx
      to: src/components/category/CategoryChoiceSheet.tsx
      via: "13+ bulk Set Category selection"
---

<objective>
Integrate category mutability into Dashboard filtering and bulk assignment without forking query state, truncating catalogs, or retaining dead IDs.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/decisions/ADR-092-durable-shared-dashboard-query-state.md
@docs/decisions/ADR-093-scoped-composable-dashboard-population-and-filter-model.md
@docs/decisions/ADR-103-atomic-composed-dashboard-bulk-mutations.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete searchable Dashboard category filters with durable cleanup</name>
  <files>src/components/control-surface/DashboardControlRow.tsx, src/components/control-surface/FilterPanelContent.tsx, src/components/control-surface/filter-summary.ts, src/components/control-surface/filter-summary.test.ts, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts, src/screens/dashboard-category-contract.test.ts</files>
  <read_first>
    - src/components/control-surface/DashboardControlRow.tsx, FilterPanelContent.tsx, filter-summary.ts, and their tests (full filter panel/load/summary behavior)
    - src/stores/dashboard-query-store.ts and src/stores/dashboard-query-store.test.ts (full durable hydrate/persist/reset behavior)
    - src/logic/dashboard-query-logic.ts and src/logic/dashboard-query-logic.test.ts (full numeric category/null query contract)
    - src/db/app-settings-dao.ts (full dashboardFilters patch/core; Plan-02 delete uses composable core)
    - src/logic/category-logic.ts (full shared catalog/filter sanitizer)
    - docs/systems/dashboard.md (full living contract; append Changelog)
  </read_first>
  <behavior>
    - 0–12 complete canonical checkboxes include Uncategorized last; 13+ adds local search and persistent selected count; hidden selections remain.
    - Deleted numeric token is removed durably under the aggregate; empty category family disappears while other families remain byte-for-byte/logically unchanged.
    - Open/focused panels rehydrate after shell refresh and never display stale labels.
  </behavior>
  <action>
Route category filter option building/search through the shared model, including an explicit null token whose query semantics select `category_id IS NULL`. Retain the Dashboard store as the single durable authority and existing panel container. Subscribe to shell refresh and rehydrate after category deletion; do not persist search. Add store/query/source tests for stale-token sanitization, unfiltered empty-family fallback, other-family preservation, 13+ hidden selection, no-match, and Uncategorized summary. Update `dashboard.md` and append its Changelog.
  </action>
  <verify><automated>npx vitest run src/screens/dashboard-category-contract.test.ts src/stores/dashboard-query-store.test.ts src/logic/dashboard-query-logic.test.ts src/components/control-surface/filter-summary.test.ts src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Durable filter JSON contains only live numeric IDs plus the explicit null policy; deleted tokens are removed in the delete transaction.
    - Search never truncates/reorders or clears hidden selected values.
    - Dashboard doc has Phase 37.1 Changelog.
  </acceptance_criteria>
  <done>Dashboard filtering remains canonical, complete, durable, and immediately consistent after deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulk Set Category includes Uncategorized and rejects stale targets</name>
  <files>src/screens/HomeScreen.tsx, src/screens/dashboard-category-contract.test.ts</files>
  <read_first>
    - src/screens/HomeScreen.tsx (full file, including multi-select claim lifecycle, category picker load, bulk action, error restoration, list/card refresh)
    - src/db/bulk-actions-dao.ts and src/db/contacts-dao.ts (full bulk/contact category writers)
    - src/components/category/CategoryChoiceSheet.tsx and src/logic/category-logic.ts (full 13+ and stale selection APIs)
    - src/stores/shell-refresh-store.ts and src/stores/dashboard-query-store.ts (full publication paths)
  </read_first>
  <behavior>
    - Bulk picker includes null Uncategorized last and all real categories in canonical order; at 13+ uses shared Sheet/search.
    - Category removed between open and submit cannot be written; chooser refreshes and selection falls back safely.
    - Successful bulk assignment updates rendered category labels after commit; failure retains selection/claim and committed cards.
  </behavior>
  <action>
Replace the real-row-only bulk picker projection with the shared single-select policy and add Uncategorized. Validate target against a fresh read immediately before the existing bulk DAO write and handle FK/stale outcome by refreshing without partial assignment. Preserve HomeScreen's claim/session and list/card renderer behavior, publish only after commit, and add focused source/model/DAO integration assertions.
  </action>
  <verify><automated>npx vitest run src/screens/dashboard-category-contract.test.ts src/db/bulk-actions-dao.test.ts src/db/contacts-dao.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Null assignment is reachable and persists to all claimed contacts.
    - Stale/deleted target changes zero contacts and refreshes the chooser.
    - List and card labels reflect committed target/Uncategorized without relaunch.
  </acceptance_criteria>
  <done>Dashboard bulk assignment is complete, null-capable, stale-safe, and commit-truthful.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Durable Dashboard JSON / bulk target → query/write | Dead or crafted IDs can alter visible population or FK writes. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-29 | Tampering | Persisted filter JSON | high | mitigate | Parse via closed Dashboard helper, sanitize deleted ID in same transaction, preserve unrelated families. |
| T-37.1-30 | Tampering | Bulk target | high | mitigate | Fresh target preflight + FK/transaction backstop; zero-write stale tests. |
| T-37.1-31 | Denial of service | Complete filter catalog | medium | mitigate | Scrollable complete list and local search at 13+, no cutoff. |

Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Dashboard category integration contract tests.
- Uncategorized-aware complete filter and bulk-assignment behavior.
- Updated Dashboard living contract with Changelog.
</artifacts>
<verification>Focused Dashboard/store/query/bulk tests, typecheck, colors, and Biome pass.</verification>
<success_criteria>Dashboard contains no dead category identity after rename/delete and all categories remain reachable for filter and bulk assignment.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-09-SUMMARY.md` when done.</output>
