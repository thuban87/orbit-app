---
phase: 37.1-category-management
plan: 20
type: execute
wave: 7
depends_on: ["37.1-18"]
files_modified:
  - src/db/database.ts
  - src/db/database.test.ts
  - src/screens/__dev__/category-management-uat.ts
  - src/screens/__dev__/category-management-uat.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "A dedicated synthetic SQLite file temporarily becomes the executor seen by every ordinary app screen without opening, writing, closing, or deleting the owner's normal database."
    - "The override is compile-time development guarded, disabled by default, memory-only, explicit, and throws in production builds."
    - "One deletion failure can be armed for the real DAO path, auto-disarms after one attempt, and never exposes SQL parameters or row data."
    - "Cleanup targets only the exact UAT filename, restores the normal executor first, and confirms the disposable file is absent."
  artifacts:
    - path: src/db/database.ts
      provides: "Fail-closed development executor override at the canonical getExecutor boundary"
    - path: src/screens/__dev__/category-management-uat.ts
      provides: "Dedicated database lifecycle, synthetic fixture, aggregate baseline, and one-shot fault API"
  key_links:
    - from: src/db/database.ts
      to: all getExecutor consumers
      via: "one canonical app-wide override checked only in development builds"
    - from: src/screens/__dev__/category-management-uat.ts
      to: src/db/categories-dao.ts
      via: "one-shot SqlExecutor decorator around the dedicated migrated database"
---

<objective>Create a fail-closed, app-wide development database override so rollback and cross-screen refresh can be exercised on a disposable synthetic database through ordinary screens.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-18-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add a fail-closed app-wide development executor override</name>
  <files>src/db/database.ts, src/db/database.test.ts</files>
  <read_first>
    - src/db/database.ts plus src/db/migrations/runner.test.ts and src/services/notifications/notification-actions.test.ts (full singleton connection and existing executor/bootstrap test precedents; `src/db/database.test.ts` is created by this task)
    - app.config.ts and src/navigation/tabs/SettingsStack.tsx (full build config and established compile-time `__DEV__` route guard)
    - Every `getExecutor()` consumer returned by `rg -l 'getExecutor\(' src` (confirm the canonical boundary covers Category Management, Dashboard, Profile, Orrery, and System Builder; do not infer from a diff)
  </read_first>
  <behavior>
    - With no override, `getExecutor()` retains its current opened/migrated `orbit.db` behavior exactly.
    - A development-only setter accepts an already-created SqlExecutor only after explicit activation; clear restores normal behavior; neither state nor filename persists across reload.
    - When `__DEV__` is false, set/clear attempts throw before changing state and `getExecutor()` cannot return an override.
  </behavior>
  <action>Add the narrowest override at the canonical `getExecutor()` boundary, leaving `openAndMigrate()` and `getDb()` unchanged. Store only an in-memory SqlExecutor reference, default null; do not persist a setting, accept a database name/path, alter the production database cache, or add a consumer-specific path. Export explicit development set/clear/status functions that begin with a fail-closed `__DEV__` guard and reject double activation or mismatched cleanup tokens. `getExecutor()` may return the override only inside an explicit `__DEV__` branch. Tests must execute both development and production-mode module paths and prove production set/clear throws, production getExecutor cannot return the synthetic executor, ordinary behavior is unchanged, reload resets state, and no main-database close/delete API is exposed.</action>
  <verify><automated>npx vitest run src/db/database.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - All ordinary screen/service consumers share the override solely through their existing `getExecutor()` calls.
    - Release-mode automated proof shows the override cannot be installed or returned.
    - Production connection/cache/open behavior and database filename remain unchanged.
  </acceptance_criteria>
  <done>The canonical executor boundary supports an explicit memory-only development override that is unreachable in production.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the exact-target disposable fixture and one-shot failing executor</name>
  <files>src/screens/__dev__/category-management-uat.ts, src/screens/__dev__/category-management-uat.test.ts, src/db/database.test.ts</files>
  <read_first>
    - src/db/database.ts, src/db/migrations/index.ts, src/db/migrations/runner.ts, and src/db/types.ts (full open/migration/executor contracts)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full delete transaction, closed fault stages, and automated rollback matrix)
    - src/db/migrations/012-import-sessions.ts and src/db/import/import-session-dao.ts (full table/status/writer contract)
    - src/db/dashboard-query-dao.ts, src/logic/dashboard-query-logic.ts, src/db/system-dao.ts, and src/db/profile-presentation-dao.ts (full fixture consumer schemas)
  </read_first>
  <behavior>
    - Setup opens one closed filename distinct from `orbit.db`, applies the real migrations, and seeds deterministic synthetic zero/one/13+ categories, contacts, every import-session status, System rules, Profile assignments, and Dashboard filters.
    - Activation never opens or writes the normal database and records only synthetic aggregate row counts, category order, and data revision.
    - The one-shot decorator is inert until explicitly armed, throws a generic error only at a closed category-deletion SQL stage, auto-disarms, and logs neither SQL parameters nor row data.
    - Reset recreates the deterministic fixture; cleanup clears the app-wide override before close/delete and rejects every target except the exact closed UAT filename.
  </behavior>
  <action>Implement a development-only lifecycle API over a closed constant such as `orbit-category-uat.db`, using `expo-sqlite` directly only in this `__dev__` harness and the real migration runner/chain. Keep the filename private and non-parameterized. Seed synthetic data sufficient for all-status import reassignment, used/unused/last deletion, 100-character Unicode, large catalogs, Dashboard null/mixed filters, System rules, and Profile assignments. Wrap its executor with an explicitly armed one-shot failure using the same closed deletion-stage contract tested by categories-dao; return generic status/aggregate evidence only. Activation installs the Task-1 override after fixture success. Cleanup must clear that override first, close the dedicated connection, delete only the closed filename, and confirm absence; on any error return a recovery state that still cannot target `orbit.db`. Tests prove no production import path can activate it, every import status is present, normal DB open/delete functions are never called, reset determinism, one-shot disarm, full transaction rollback snapshots, exact-target rejection, cleanup ordering, and absence.</action>
  <verify><automated>npx vitest run src/screens/__dev__/category-management-uat.test.ts src/db/database.test.ts src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Fixture and fault operations touch only the exact dedicated UAT database and contain no private data.
    - All pending, complete, and discarded import-session rows participate in synthetic delete/rollback coverage.
    - Deactivation precedes close/delete; restart/reload clears activation; exact-target guards make normal-database deletion impossible.
  </acceptance_criteria>
  <done>A disposable synthetic database safely powers real app-wide screen/DAO UAT and is recoverably removable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Development override → canonical executor | A test executor must never become reachable in a production build. |
| Disposable lifecycle → SQLite files | Cleanup must be incapable of selecting the owner's normal database. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-56 | Elevation of privilege | Development executor override | high | mitigate | Default-null memory state, explicit activation token, compile-time `__DEV__` gates, production-mode tests. |
| T-37.1-57 | Tampering | SQLite file lifecycle | high | mitigate | Closed non-parameterized filename, main DB APIs untouched, deactivate-before-close/delete ordering, exact-target tests. |
| T-37.1-58 | Information disclosure | Failure and baseline output | high | mitigate | Synthetic fixtures plus generic errors and aggregate evidence only; no SQL params/private rows. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Canonical fail-closed app-wide development executor override.
- Dedicated migrated synthetic database, deterministic fixtures, one-shot deletion fault, and exact cleanup guards.
- Production-unreachability, all-status rollback, and normal-database non-interference tests.
</artifacts>
<verification>Focused database/harness/categories tests, production-mode assertions, typecheck, and changed-file Biome pass.</verification>
<success_criteria>Ordinary screens can share one disposable UAT database while production and the owner's personal database remain technically unreachable.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-20-SUMMARY.md` when done.</output>
