---
phase: 37.1-category-management
plan: 20
type: execute
wave: 7
depends_on: ["37.1-18"]
files_modified:
  - src/db/database.ts
  - src/db/database.test.ts
  - src/db/database-access-audit.test.ts
  - src/screens/__dev__/category-management-uat.ts
  - src/screens/__dev__/category-management-uat.test.ts
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Normal bootstrap may already have opened orbit.db; while category UAT is active, every ordinary-screen database access routes category-UAT reads/writes to orbit-category-uat.db, and category-UAT close/delete can target only that exact disposable file."
    - "The route is compile-time development guarded, disabled by default, memory-only, explicit, and every production activation/control attempt throws."
    - "Automated inventory and runtime tests cover getExecutor(), direct getDb() consumers, registered callbacks/default parameters, and cached executor references; activation fails closed if any consumer cannot be routed safely."
    - "One deletion failure can be armed for the real DAO path, auto-disarms after one attempt, and exposes no SQL parameters, private rows, or normal-database metadata."
  artifacts:
    - path: src/db/database.ts
      provides: "Fail-closed development database routing across getExecutor(), getDb(), and cached executor references"
    - path: src/db/database-access-audit.test.ts
      provides: "Repository-wide database accessor/cached-reference inventory guard"
    - path: src/screens/__dev__/category-management-uat.ts
      provides: "Exact-file disposable lifecycle, synthetic fixture, aggregate baseline, and one-shot fault API"
  key_links:
    - from: src/db/database.ts
      to: all database accessor consumers
      via: "one memory-only active-database route covering both accessors and cached executor references"
    - from: src/screens/__dev__/category-management-uat.ts
      to: src/db/categories-dao.ts
      via: "one-shot SqlExecutor decorator around the exact migrated disposable database"
---

<objective>Create the owner-approved fail-closed app-wide development database route so rollback and cross-screen refresh can be exercised through ordinary screens on exactly `orbit-category-uat.db`, without claiming that normal bootstrap never opens `orbit.db`.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/37.1-category-management/37.1-18-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-RESEARCH.md
@.planning/phases/37.1-category-management/37.1-PATTERNS.md
@.planning/phases/37.1-category-management/37.1-VALIDATION.md</context>

<owner_ruling date="2026-09-15">The owner explicitly approved this development-only category UAT database/fault harness with exact fixed filename `orbit-category-uat.db`, compile-time `__DEV__` and memory-only activation, production activation that throws, exact-file-only cleanup, automated coverage of `getExecutor()`, direct `getDb()` users, and cached executor references, and synthetic/private-free evidence. Normal `orbit.db` may open during app bootstrap; no category-UAT read, write, close, or delete may target it.</owner_ruling>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route every actual database access boundary safely</name>
  <files>src/db/database.ts, src/db/database.test.ts, src/db/database-access-audit.test.ts</files>
  <read_first>
    - src/db/database.ts, src/db/migrations/runner.ts, src/db/migrations/runner.test.ts, src/db/types.ts, src/services/notifications/notification-actions.ts, and src/services/notifications/notification-actions.test.ts (full singleton connection, live migration registry, executor, headless bootstrap, and test precedents)
    - App.tsx, src/services/backup-sweep.ts, src/services/import/contact-import-resume-sweep.ts, src/services/import/reconcile-resume-sweep.ts, src/services/interaction-assist-sweep.ts, src/services/photos/background-reconcile-sweep.ts, src/services/photos/restore-photo-finalize-sweep.ts, src/services/notifications/notification-schedule.ts, src/services/notifications/digest-schedule.ts, src/services/widget/widget-render.tsx, and src/services/widget/widget-task-handler.tsx (full bootstrap, registered callback, headless, and potentially long-lived accessor consumers)
    - app.config.ts and src/navigation/tabs/SettingsStack.tsx (full build config and established compile-time `__DEV__` guard)
    - Every live `getExecutor()` and `getDb()` consumer returned by `rg -l '\b(getExecutor|getDb)\b' App.tsx src`, plus every executor reference whose lifetime can cross activation (classify comments/tests separately; inspect actual full files rather than a changed-file list)
  </read_first>
  <behavior>
    - With no UAT active, `openAndMigrate()`, `getDb()`, and `getExecutor()` retain current migrated `orbit.db` behavior.
    - Development activation installs an already-open migrated disposable SQLiteDatabase and executor under an unforgeable memory-only token; both accessors and executor references acquired before activation route subsequent category-UAT calls to the disposable connection.
    - Activation waits for or rejects unsafe in-flight/cached access under one tested routing contract; a transaction can never switch connections between BEGIN and COMMIT/ROLLBACK. Any unrouteable shape throws instead of falling through to the normal database.
    - Under production `__DEV__ === false`, activation/clear/status controls throw before state changes and neither accessor can return the disposable connection.
  </behavior>
  <action>Implement one development routing state in `database.ts` covering both public accessors and references returned by `getExecutor()`. Preserve `openAndMigrate()` as the normal bootstrap/cache owner: `orbit.db` may already be open and remains open, but it is not selected by category-UAT access while the disposable route is active. Use a stable routing executor or equivalently proven lease so a pre-activation cached executor cannot keep reading/writing the normal connection; bind a transaction to one connection through COMMIT/ROLLBACK and make activation wait for or reject an unsafe transition. `getDb()` returns the active disposable SQLiteDatabase in development and the normal cached database otherwise. Store only in-memory connection/executor/token state; accept no filename/path, persist nothing, never close/replace the normal cache, and reject double activation or mismatched cleanup tokens. Every development control starts with a compile-time `__DEV__` guard; production activation/clear/status throws.

Create `database-access-audit.test.ts` from the actual repository inventory. Enumerate every live accessor import/call, registered callback/default-parameter consumer, cached executor whose lifetime can cross activation, and direct expo-sqlite open/close/delete site. Assert there are currently no unclassified direct `getDb()` consumers outside `database.ts`, while keeping the gate able to fail if one appears. Runtime tests acquire an executor before activation, activate distinguishable synthetic state, and prove that cached reference, fresh `getExecutor()`, and `getDb()` route reads/writes only to synthetic state; prove a transaction cannot straddle activation; clear and prove normal routing resumes. Execute production-mode module paths and prove controls throw, disposable state cannot be returned, reload resets state, and no normal-database close/delete capability is exposed.</action>
  <verify><automated>npx vitest run src/db/database.test.ts src/db/database-access-audit.test.ts src/services/notifications/notification-actions.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Every actual accessor/callback/direct-SQLite/cached-reference boundary is classified; an unclassified consumer fails the inventory test.
    - A pre-activation cached executor cannot bypass active UAT routing and transactions cannot straddle connections.
    - Production cannot activate or observe the disposable route; normal connection/cache/open behavior stays unchanged.
  </acceptance_criteria>
  <done>Every actual database access boundary supports one explicit memory-only development route that is cached-reference-safe and unreachable in production.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the exact-target disposable fixture and one-shot failing executor</name>
  <files>src/screens/__dev__/category-management-uat.ts, src/screens/__dev__/category-management-uat.test.ts, src/db/database.test.ts, src/db/database-access-audit.test.ts</files>
  <read_first>
    - src/db/database.ts, src/db/migrations/runner.ts, and src/db/types.ts (full live open/migration-registry/executor contracts; `MIGRATIONS` and `TARGET_VERSION` are exported by database.ts and there is no migrations/index.ts)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full delete transaction, closed fault stages, and rollback matrix)
    - src/db/migrations/012-import-sessions.ts, src/db/import-session-dao.ts, and src/db/import-session-read.ts (full table/status/writer/read contract)
    - src/db/dashboard-read.ts, src/stores/dashboard-query-store.ts, src/logic/dashboard-query-logic.ts, src/db/systems-dao.ts, src/db/systems-catalog-read.ts, src/db/orrery-system-read.ts, and src/db/profile-presentation-dao.ts (full live fixture consumer schemas and paths)
  </read_first>
  <behavior>
    - Setup opens exactly `orbit-category-uat.db`, applies real PRAGMAs and the registered migration chain, and seeds deterministic synthetic zero/one/13+ categories, contacts, every import-session status, System rules, Profile assignments, and Dashboard filters.
    - Normal `orbit.db` may already be open from bootstrap; after activation no category-UAT read/write/close/delete targets it, and evidence contains only synthetic aggregate counts/order/revision.
    - The one-shot decorator is inert until armed, throws a generic error only at a closed category-delete stage, auto-disarms, and logs no SQL parameters or row data.
    - Reset recreates the deterministic fixture; cleanup deactivates first, closes only the disposable handle, deletes only the exact constant, and confirms absence.
  </behavior>
  <action>Implement the lifecycle API over the exact private constant `CATEGORY_UAT_DATABASE_NAME = "orbit-category-uat.db"`, using `expo-sqlite` directly only in this `__dev__` harness and the real `MIGRATIONS`/`TARGET_VERSION` plus `runMigrations`. The filename is non-parameterized and cleanup accepts no arbitrary target. Before setup, delete only a stale file whose name equals that exact constant; setup opens it, configures the same required PRAGMAs, migrates it, and seeds synthetic all-status import reassignment, used/unused/last deletion, 100-character Unicode, large catalogs, Dashboard null/mixed filters, System rules, and Profile assignments. Wrap its executor with an explicitly armed one-shot failure using the same closed deletion-stage contract as `categories-dao`; return generic aggregate evidence only. Activate Task 1's database+executor route only after fixture success. Cleanup clears/deactivates first, closes only the dedicated handle, deletes only the exact constant, and confirms absence; an error leaves a recovery state that still cannot select `orbit.db`.

Tests spy on SQLite open/close/delete calls and routed executor/database calls. They may observe the earlier normal bootstrap open, but from activation through cleanup no category-UAT operation may read, write, close, or delete the normal connection/file. Also prove production import paths cannot activate the harness, pending/complete/discarded fixtures all receive the same deletion target while status/unrelated fields remain unchanged, reset determinism, one-shot disarm, complete rollback snapshots, exact-target rejection, cleanup ordering, and file absence.</action>
  <verify><automated>npx vitest run src/screens/__dev__/category-management-uat.test.ts src/db/database.test.ts src/db/database-access-audit.test.ts src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Fixture/fault operations touch only `orbit-category-uat.db` and contain no private data; tests distinguish allowed prior bootstrap open from forbidden active-UAT normal-database I/O/close/delete.
    - Pending, complete, and discarded import rows participate in deletion and rollback using the identical target.
    - Deactivation precedes close/delete; restart clears activation; exact-target guards make normal-database deletion impossible.
  </acceptance_criteria>
  <done>A disposable synthetic database safely powers real app-wide UAT and is recoverably removable without category-UAT access to the normal database.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Development route → getExecutor/getDb/cached references | A disposable database must never be reachable in production, and active UAT must never fall through to the normal connection. |
| Disposable lifecycle → SQLite files | Cleanup must be incapable of selecting the owner's normal database. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-56 | Elevation of privilege | Development database routing | high | mitigate | Default-null memory state, explicit token, compile-time `__DEV__` guards, production-mode tests, and both accessors covered. |
| T-37.1-57 | Tampering | SQLite file lifecycle | high | mitigate | Exact non-parameterized filename, normal cache never closed/replaced, deactivate-before-close/delete, exact-target tests. |
| T-37.1-58 | Information disclosure | Failure/baseline output | high | mitigate | Synthetic fixtures and generic aggregate evidence only; no SQL params/private rows/normal metadata. |
| T-37.1-59 | Tampering | Cached/in-flight executor | high | mitigate | Full inventory plus routing/lease tests prove cached references cannot bypass activation and transactions cannot straddle connections. |
Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- App-wide development database routing for `getExecutor()`, `getDb()`, registered callbacks, and cached references.
- Exact `orbit-category-uat.db` migrated fixture, deterministic synthetic data, one-shot fault, and exact cleanup guards.
- Production-unreachability, all-status rollback, active-UAT normal-database non-interference, and inventory tests.
</artifacts>
<verification>Focused database/access-inventory/harness/categories tests, production-mode assertions, typecheck, and changed-file Biome pass.</verification>
<success_criteria>Ordinary screens share exactly `orbit-category-uat.db` during explicit development UAT; production cannot activate it, and active category-UAT reads/writes/close/delete cannot target the normal database even though ordinary bootstrap may have opened it earlier.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-20-SUMMARY.md` when done.</output>
