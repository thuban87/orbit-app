---
phase: 37.1-category-management
plan: 10
type: execute
wave: 3
depends_on: ["37.1-02", "37.1-07"]
files_modified:
  - src/components/profile/ProfileTemplateManager.tsx
  - src/components/profile/ProfileBackgroundManager.tsx
  - src/components/profile/profile-category-contract.test.ts
  - docs/systems/profile.md
autonomous: true
requirements: [] # No phase REQ IDs exist; coverage is dossier/UI-SPEC based.
estimate: { tokens: 33000, raw_tokens: 33000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Profile Layout and Background managers expose every real category in canonical order, become searchable/bounded at 13+, and never synthesize Uncategorized."
    - "Rename preserves category presentation by stable local ID/portable UID; deletion removes the source assignment and does not transfer it to the reassignment target."
    - "Profile/contact summaries use live joined labels and refresh to the target/Uncategorized without stale text."
  artifacts:
    - path: src/components/profile/profile-category-contract.test.ts
      provides: "Shared Profile manager/identity/delete presentation contract"
  key_links:
    - from: src/components/profile/ProfileTemplateManager.tsx
      to: src/logic/category-logic.ts
      via: "real-only complete/searchable category projection"
    - from: src/db/profile-presentation-dao.ts
      to: src/db/categories-dao.ts
      via: "explicit count/delete in atomic category fallout"
---

<objective>
Make both Profile presentation managers scale with the mutable taxonomy while preserving their independent stable category assignment axes.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/decisions/ADR-108-durable-independent-axis-profile-presentation-and-inheritance.md
@docs/decisions/ADR-112-app-owned-profile-background-derivatives-and-launch-reconciliation.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
@.planning/phases/37.1-category-management/37.1-07-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete bounded category overrides in both Profile managers</name>
  <files>src/components/profile/ProfileTemplateManager.tsx, src/components/profile/ProfileBackgroundManager.tsx, src/components/profile/profile-category-contract.test.ts</files>
  <read_first>
    - src/components/profile/ProfileTemplateManager.tsx and ProfileBackgroundManager.tsx (full files, all category list/sheet/assignment/clear/error/focus behavior)
    - src/db/profile-presentation-dao.ts, src/db/profile-presentation-read.ts, and tests (full category assignment/read/usage writers)
    - src/components/category/CategoryChoiceSheet.tsx and src/logic/category-logic.ts (full real-only policy and search)
    - src/stores/shell-refresh-store.ts (full refresh seam)
    - .planning/phases/37.1-category-management/37.1-UI-SPEC.md §§7, 10–11
  </read_first>
  <behavior>
    - 0 real categories shows `No categories available`; 1–12 retain established control; 13+ is searchable and bounded.
    - Choices are real-only/canonical; hidden selection survives query; deleted open selection clears safely.
    - Layout and background axes remain independent.
  </behavior>
  <action>
Replace each manager's unbounded category button projection with the shared real-only model and 13+ bounded/searchable choice surface while retaining existing 0–12 interaction, assignment DAO, clear behavior, and independent layout/background axes. Refresh on focus/shell change and clear a deleted selection without inventing Uncategorized presentation. Add cross-manager tests for 0/12/13/many, hidden selection, stale identity, and independent axis writes.
  </action>
  <verify><automated>npx vitest run src/components/profile/profile-category-contract.test.ts src/db/profile-presentation-dao.test.ts</automated><automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <acceptance_criteria>
    - Both managers use the shared real-only canonical/search model and no unbounded category column remains.
    - Uncategorized is absent and zero-state exact copy is present.
    - Layout/background independence remains tested.
  </acceptance_criteria>
  <done>Both Profile managers provide complete accessible category assignment at every catalog size.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove rename/delete presentation identity and live Profile refresh</name>
  <files>src/components/profile/profile-category-contract.test.ts, docs/systems/profile.md</files>
  <read_first>
    - src/db/profile-read.ts, src/db/profile-presentation-read.ts, src/db/dashboard-read.ts (full live category joins/inheritance reads)
    - src/db/categories-dao.ts and src/db/categories-dao.test.ts (full rename/delete aggregate)
    - src/db/profile-presentation-dao.ts and tests (full local-ID/backup-UID identity)
    - docs/systems/profile.md (full living contract; append Changelog)
  </read_first>
  <behavior>
    - Rename retains same profile_category_presentation row and changes visible label through live join.
    - Delete removes source assignment, creates none for target, and moved contacts inherit target/default behavior naturally.
    - Open Profile refreshes after commit and never shows stale source name.
  </behavior>
  <action>
Add integration assertions around DAO rename/delete plus Profile presentation reads: snapshot identity before rename, prove row/UID assignment preservation, delete to both null and another category, prove source presentation removed and target untouched, then prove live read resolves correct inherited presentation/label. Update `profile.md` with stable category presentation identity, no-transfer deletion semantics, complete manager catalogs, and Phase 37.1 Changelog.
  </action>
  <verify><automated>npx vitest run src/components/profile/profile-category-contract.test.ts src/db/profile-presentation-dao.test.ts src/db/categories-dao.test.ts</automated><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Stable identity and no-transfer semantics are tested through actual DAO/read boundaries.
    - Profile doc is current and includes Changelog.
  </acceptance_criteria>
  <done>Profile presentation and visible labels respond correctly to mutable categories without identity drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Category selection → Profile presentation DAO | A stale ID could create an invalid or unintended presentation override. |
## STRIDE Threat Register (ASVS L1; HIGH blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-32 | Tampering | Presentation assignment | high | mitigate | Real-only fresh catalog, existing FK/DAO validation, stale clear tests. |
| T-37.1-33 | Information disclosure | Category search | low | accept | Ephemeral local search; no logging/network. |
| T-37.1-34 | Denial of service | Long category catalog | medium | mitigate | Shared bounded Sheet at 13+, complete results/no cutoff. |

Package legitimacy: N/A.
</threat_model>
<artifacts>
## Artifacts this phase produces
- Profile category integration contract tests.
- Complete bounded real-only category selection in both Profile managers.
- Updated Profile living contract with Changelog.
</artifacts>
<verification>Focused Profile/DAO tests, typecheck, colors, and Biome pass.</verification>
<success_criteria>Profile assignment identity survives rename, deletion removes rather than transfers, and both managers remain complete at arbitrary category count.</success_criteria>
<output>Create `.planning/phases/37.1-category-management/37.1-10-SUMMARY.md` when done.</output>
