---
phase: 37.1-category-management
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/categories-dao.ts
  - src/db/categories-dao.test.ts
autonomous: true
gap_closure: true
requirements: [] # Spec-less probe: Phase 37.1 has no assigned requirement IDs.
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Deleting a category fails closed when durable dashboard_filters is malformed JSON, null/an array/a primitive, contains an unknown family, or contains an invalid category token."
    - "A rejected deletion preserves the dashboard_filters text byte-for-byte, the category and every fallout target, and data_revision."
    - "Valid dashboard filter objects still remove only the deleted category ID and preserve unrelated filter families."
  artifacts:
    - { path: src/db/categories-dao.ts, provides: "Strict dashboard-filter read boundary inside category preview/deletion" }
    - { path: src/db/categories-dao.test.ts, provides: "Malformed/non-object full-rollback regression coverage" }
  key_links:
    - { from: src/db/categories-dao.ts, to: src/logic/dashboard-query-logic.ts, via: "canonical DashboardFilters parser before any deletion write" }
    - { from: src/db/categories-dao.ts, to: src/db/transaction.ts, via: "parse rejection propagates through the one outer transaction" }
---

<objective>Close the destructive-data-loss gap by making category deletion reject corrupt durable Dashboard filter state before it can rewrite or delete anything.</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@.planning/phases/37.1-category-management/37.1-VERIFICATION.md
@.planning/phases/37.1-category-management/37.1-UI-SPEC.md
@.planning/phases/37.1-category-management/37.1-02-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Fail the deletion preview on non-canonical durable Dashboard filters</name>
  <read_first>src/db/categories-dao.ts, src/logic/dashboard-query-logic.ts, src/db/app-settings-dao.ts, src/stores/dashboard-query-store.ts, src/db/migrations/019-dashboard-prefs.ts</read_first>
  <files>src/db/categories-dao.ts, src/db/categories-dao.test.ts</files>
  <behavior>- Valid JSON objects accepted by the canonical Dashboard parser continue into preview and deletion, including arbitrary string selections in the four non-category families that the current parser intentionally accepts.
- Malformed JSON, null, arrays, primitives, unknown families, non-array family values, non-string selections, and invalid category tokens reject instead of becoming an empty object.
- Preview and lock-time revalidation use the same strict interpretation, so the fingerprint cannot conceal corrupt source state.</behavior>
  <action>Replace the permissive local `parseDashboardFilters` fallback with the existing `parseDashboardFilters` contract from `dashboard-query-logic`. Parse the stored JSON text and require a non-null canonical result; throw a category-deletion-specific error otherwise. Match that parser exactly: do not add semantic-vocabulary validation for social-battery, needs-attention, gravity, or contact-frequency, because the canonical parser accepts strings for those families and later consumers narrow them. Keep the raw stored value untouched and do not add repair, migration, reseed, telemetry, or network behavior. Start with focused tests for malformed JSON, top-level null/array/primitive, unknown family, invalid category token, and a valid object containing parser-accepted arbitrary strings in non-category families.</action>
  <acceptance_criteria>`readCategoryDeletionPreview` rejects precisely the shapes the canonical parser rejects, still counts a deleted valid category token, and does not broaden Dashboard preference validation.</acceptance_criteria>
  <verify><automated>npx vitest run src/db/categories-dao.test.ts</automated></verify>
  <done>The end-to-end preview path cannot normalize corrupt durable settings into an apparently safe fingerprint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove malformed settings roll back the complete deletion aggregate</name>
  <read_first>src/db/categories-dao.ts, src/db/categories-dao.test.ts, src/db/data-revision-dao.ts, src/db/transaction.ts</read_first>
  <files>src/db/categories-dao.test.ts</files>
  <behavior>- If dashboard_filters becomes corrupt after preview but before final delete, the final lock-time read rejects.
- After rejection, contacts, all-status imports, rules, overrides, prefs, active selection, Profile presentation, order, tombstones, category row, and the original setting equal their pre-call state.
- data_revision is unchanged.</behavior>
  <action>Add a DAO regression around the public `deleteCategory` boundary using the existing full-chain fixture and database snapshot style. Obtain a valid preview, mutate only the durable dashboard_filters column to malformed/non-object text, invoke deletion with the prior fingerprint, and assert rejection plus exact preservation of the setting and every deletion target. Include the revision assertion. Do not introduce a development route, alternate database, executor override, migration, or production fault harness.</action>
  <acceptance_criteria>The public deletion call cannot partially mutate any target or advance revision when durable Dashboard filters are corrupt.</acceptance_criteria>
  <verify><automated>npx vitest run src/db/categories-dao.test.ts && npx tsc --noEmit</automated></verify>
  <done>The blocking atomicity gap has a full rollback regression at the real DAO boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| durable SQLite text → deletion aggregate | Persisted JSON may be corrupt and must be treated as untrusted before destructive writes. |

## STRIDE Threat Register — ASVS L1, blocking threshold: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37.1-G10-01 | Tampering | `dashboard_filters` parse/deletion fingerprint | high | mitigate | Strict canonical parse before mutation; propagate rejection through the outer transaction. |
| T-37.1-G10-02 | Denial of Service | malformed local setting | low | accept | Deletion is unavailable until state is valid; preserving data is the safe failure mode. |
| T-37.1-G10-03 | Information Disclosure | test/error path | low | mitigate | Errors identify the setting class only and never log filter contents or contact data. |
</threat_model>

<verification>Run the focused DAO suite, `npx tsc --noEmit`, and `npx biome check src/db/categories-dao.ts src/db/categories-dao.test.ts`. Spec-less probe skip is explicit: Phase 37.1 has no requirement IDs to probe.</verification>
<success_criteria>Corrupt durable Dashboard filters make category deletion fail closed with the complete database graph and revision unchanged.</success_criteria>

## Artifacts this phase produces
- No new file or public API; the existing category deletion boundary becomes strict and gains rollback regressions.

<output>Create `.planning/phases/37.1-category-management/37.1-10-SUMMARY.md` when done.</output>
