---
phase: 37.1
slug: category-management
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-15
---

# Phase 37.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Phase 37.1 has no dedicated requirement IDs; coverage is traced to the owner-approved dossier and UI-SPEC contracts.

## Owner rulings — 2026-09-15

- Preview, fingerprint, mutation, rollback, restore merge, UI copy, tests, and docs must reassign every referencing `import_sessions.batch_category_id` row — pending, complete, and discarded — to the same deletion target or Uncategorized. This supersedes every pending-only validation instruction.
- **Testing-scope supersession — 2026-09-15:** the owner withdrew the development-only database-substitution/fault-route harness. Validation must not require alternate database files, executor overrides, dev-only routes, cleanup code, or test-only production infrastructure. DAO tests inject failure after every destructive stage; manager component/model tests prove restoration to the last committed state plus locked error feedback; physical-device UAT covers ordinary successful behavior only.

## Owner rulings — 2026-09-16

- **Backup format v6 (owner-clarified 2026-09-16):** Phase 37.1 bumps `BACKUP_FORMAT_VERSION` 5→6 to carry category tombstones (the ADR-063-compliant path for a portable wire-shape change). A **minimal** v5→v6 relabel step is included — only what the existing forward-migration chain and its version-assertion tests require to stay green (a version-counter advance; existing `toBe(5)` assertions become 6). What is **cut**: serious v5-backup-restore piping — no v5 payload accommodation, no synthesized category evidence, no dev harness, and no v5-scenario/restore tests (zero v5 backups exist in the wild). Owns: Plan 03.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest `^4.1.10`, Node environment |
| **Config file** | `vitest.config.ts` |
| **Quick logic command** | `npx vitest run src/logic/category-logic.test.ts` |
| **Quick DAO command** | `npx vitest run src/db/categories-dao.test.ts` |
| **Backup command** | `npx vitest run src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts src/backup/restore-apply.test.ts src/backup/phase-17-integration.test.ts` |
| **Full suite command** | `npm test` |
| **Static gates** | `npx tsc --noEmit`; `npx biome check <changed-files>`; `npm run check:colors` |
| **Estimated quick runtime** | Target under 30 seconds per focused command |

---

## Sampling Rate

- **After every task commit:** Run the focused test file(s) named by the task's `<verify>` block.
- **After every plan wave:** Run the category DAO tests, affected subsystem suites, `npx tsc --noEmit`, and Biome over changed files.
- **Before `$gsd-verify-work`:** Run `npm test`, `npx tsc --noEmit`, `npm run check:colors`, focused source-contract tests, and physical-device UAT.
- **Max feedback latency:** 30 seconds for the per-task focused test target where practical.

---

## Per-Contract Verification Map

| Contract | Source | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|----------|--------|------------|-----------------|-----------|-------------------|-------------|--------|
| Name trim/NFC/blank/100-character bound/case collisions/capitalization-only rename/System collisions | UI-SPEC §§4, 8 | T-37.1-01 | Names are normalized and bound; SQL values remain parameterized | unit + SQLite | `npx vitest run src/logic/category-logic.test.ts src/db/categories-dao.test.ts` | ✅ | ✅ green |
| Seed rows are ordinary; zero-category databases remain zero; no migration/reseed occurs | Dossier §§B, H | T-37.1-02 | Runtime CRUD cannot resurrect user-deleted taxonomy | full-chain SQLite | `npx vitest run src/db/categories-dao.test.ts src/db/migrations/runner.test.ts` | ✅ | ✅ green |
| Create appends; reorder/delete produce contiguous `display_order`; stale/duplicate reorder rolls back | Dossier §F | T-37.1-03 | Serialized writes preserve catalog integrity | SQLite | `npx vitest run src/db/categories-dao.test.ts` | ✅ | ✅ green |
| Preview counts every contact and durable fallout class; lock-time changes or a removed target cause zero writes and a refreshed preview | UI-SPEC §§5–6 | T-37.1-04 | TOCTOU cannot produce partial or misrepresented deletion | SQLite integration | `npx vitest run src/db/categories-dao.test.ts` | ✅ | ✅ green |
| Delete atomically reassigns contacts and every referencing pending/complete/discarded import session to one target, removes rules/ref state/presentation/filter state, repairs active System, writes tombstone, and bumps revision exactly once | Owner ruling 2026-09-15; Dossier §§G–J; UI-SPEC §10 | T-37.1-05 | Every destructive side effect commits together or rolls back | SQLite integration | `npx vitest run src/db/categories-dao.test.ts` | ✅ | ✅ green |
| Injected failure after every deletion stage restores the complete pre-operation state and leaves `data_revision` unchanged | Dossier §H | T-37.1-06 | No partial destructive state survives a thrown error | fault-injection SQLite | `npx vitest run src/db/categories-dao.test.ts` | ✅ | ✅ green |
| Custom System remains valid with another rule or explicit include; sole removed rule becomes Needs Attention; exclude-only is not a positive definition | Dossier §I; UI-SPEC §7 | T-37.1-07 | Deleted identities are removed without fake stale rules | unit + resolver SQLite | `npx vitest run src/logic/system-rule-resolver.test.ts src/db/systems-dao.test.ts src/db/systems-catalog-read.test.ts` | ✅ | ✅ green |
| Shared catalog preserves canonical order, adds Uncategorized only by policy, searches at exactly 13+, and retains hidden selections | UI-SPEC §§7, 11 | T-37.1-08 | Complete local access without truncation or stale submission | unit | `npx vitest run src/logic/category-logic.test.ts` | ✅ | ✅ green |
| Orrery selector has exactly Built-in/Categories/Custom Systems groups, retains status/count semantics, opens the selected group, and resets expansion each open | UI-SPEC §9 | — | N/A | pure UI model/source contract | `npx vitest run src/components/orrery/orrery-controls-logic.test.ts src/screens/orrery-screen-framing.test.ts` | ✅ | ✅ green |
| Reserved route and Settings row become live | UI-SPEC §1 | — | N/A | source contract | `npx vitest run src/navigation/settings-routes.test.ts src/screens/settings-contacts-model.test.ts` | ✅ | ✅ green |
| Backup v6 accepts/exports category tombstones and rejects malformed/duplicate input before mutation; no serious v5→v6 compatibility path exists beyond the approved version relabel | Dossier §N; UI-SPEC §11 | T-37.1-09 | Crafted backup cannot create ambiguous or resurrected taxonomy | schema/export | `npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts` | ✅ | ✅ green |
| Merge tombstone wins ties and suppresses older category rules/ref state without changing unrelated rules | Dossier §N; UI-SPEC §11 | T-37.1-10 | Deleted categories cannot resurrect through merge | reconciliation/restore | `npx vitest run src/backup/reconciliation.test.ts src/backup/restore-apply.test.ts` | ✅ | ✅ green |
| Replace-all restores the exact nonempty or empty category taxonomy with no extras/reseed and rolls back on failure | Dossier §N; UI-SPEC §11 | T-37.1-11 | Restore is exact and atomic | restore integration | `npx vitest run src/backup/restore-apply.test.ts src/backup/phase-17-integration.test.ts` | ✅ | ✅ green |
| Category data and diagnostics never leave the device; delete UI exposes aggregate counts only | UI-SPEC §10 | T-37.1-12 | Private contact/category contents are not logged or transmitted | source/UI contract | `npx vitest run src/screens/CategoryManagementScreen.test.tsx` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

All fourteen contract rows above are ✅ green as of 2026-09-17. The focused closeout command covered 16 files / 191 tests, and the plan's repository-wide source audit adds seven passing contract tests.

---

## Required Delete Failure Matrix

Inject a thrown error after each stage: contact reassignment, all-status import-session reassignment, category-rule deletion, category-System override deletion, category-System preference deletion, active-selection fallback, Profile presentation deletion, Dashboard filter update, order normalization, tombstone write, and final category-row delete. The fixture distinguishes pending, complete, and discarded rows in preview/fingerprint evidence and proves all three receive the same target while retaining status and unrelated fields. After every rejection, compare the complete pre-operation database snapshot and assert that `data_revision` did not advance. A successful deletion must advance it exactly once.

---

## Wave 0 Requirements

- [x] `src/logic/category-logic.test.ts` — name, catalog, ordering, search, filter, and null-option invariants.
- [x] `src/db/categories-dao.test.ts` — full-chain fixture, CRUD, preview, stale confirmation, atomic fallout, category tombstone, exact revision, and fault-injected rollback.
- [x] `src/screens/CategoryManagementScreen.test.tsx` — manager state machine, accessibility contracts, and locked copy.
- [x] Category-tombstone and zero-taxonomy fixtures in existing backup schema, reconciliation, restore, and integration suites.
- [x] Shared-selector fixtures for 0, 1, 12, 13, many, long Unicode, and stale selected identities.

---

## Manual-Only Verifications

| Behavior | Source | Why Manual | Test Instructions |
|----------|--------|------------|-------------------|
| Touch targets, TalkBack labels/focus return, Android Back, and non-dismissible final delete confirmation | UI-SPEC §§1–6, Accessibility Contract | Platform accessibility and focus behavior require a running Android target | Confirm the live target with `emu-connect`; inspect the UI tree and exercise Back/focus paths. Confirm app package and Metro session with the owner before first device use. |
| Large text, 100-character Unicode names, 13+ search, drag plus Move earlier/later, and anchored-menu placement on narrow/tall layouts | UI-SPEC §§2–8 | Native layout/gesture behavior is only partially represented by source tests | Exercise each boundary on the running app; verify no inaccessible category and no clipped required copy. |
| Live post-commit refresh across Dashboard, Profile, Orrery, System Builder, and open category consumers | UI-SPEC §§10–11 | Cross-screen lifecycle/store publication needs end-to-end observation | Rename and delete populated categories, then traverse every consumer without relaunching; confirm no stale label, ID, rule, or blank selection remains. |
| Successful manager error-free path | UI-SPEC §§5–6 | Native overlay, focus, and layout behavior require a running Android target | Exercise committed Add/Rename/Reorder/Delete flows; rollback-visible error recovery is covered by automated DAO and manager component/model tests rather than an induced device fault. |

Performance claims are out of scope unless the target is the physical Pixel; emulator rendering is not valid performance evidence.

---

## Validation Sign-Off

- [x] Every planned task has an automated verify command or an explicit Wave 0 dependency.
- [x] Sampling continuity: no three consecutive implementation tasks lack automated verification.
- [x] Wave 0 covers every missing test reference above.
- [x] No watch-mode flags appear in verification commands.
- [x] Focused feedback latency remains under 30 seconds where practical.
- [x] Physical-device UAT is retained in `37.1-DEVICE-UAT.md` with successful manager, accessibility, Back/focus, reduced-motion, large-text, search, reorder, deletion, consumer-refresh, and cleanup evidence.
- [x] `nyquist_compliant: true` is satisfied by the recorded physical-device UAT plus the automated contract matrix.

### Automated closeout — 2026-09-17

- `npm test`: 388 files / 3,679 tests passed; the sole failed suite is the known pre-existing `src/components/orrery/orrery-controls-render.test.tsx` loader error, `SyntaxError: Unexpected token 'typeof'`. No new failure was hidden by this exception.
- Focused category/backup/System/navigation gate: 16 files / 191 tests passed.
- `npx tsc --noEmit`: passed.
- `npm run check:colors`: passed.
- `npx biome check src/category-identity-audit.test.ts`: passed.
- The literal plan command `npx biome check src docs/systems` remains non-green because the repository already contains 535 unrelated formatting errors and 77 warnings across generated fixtures and prior source. This plan did not rewrite those out-of-scope files; the new audit file is clean and Markdown is not checked by the configured Biome project.

**Approval:** automated contracts complete; physical-device closeout recorded below.

### Physical-device closeout — 2026-09-17

- Physical Pixel 6 Pro UAT passed under the owner-confirmed `com.bwales.orbit` package and live `orbit` Metro session; see `37.1-DEVICE-UAT.md`.
- The production UI proved zero/one/13+ catalogs, add/rename, 100-character layout, drag and accessible reorder, anchored-menu clamp/flip, both deletion targets, destructive Back non-dismissal, exact Orrery grouping, System Builder hidden selections, and live Dashboard/Profile/Orrery refresh without relaunch.
- Reduced-motion animation scales, font scale, and enabled accessibility services were captured before change, restored exactly, and read back. No DAO failure, alternate database, executor override, development route, or test-only production infrastructure was used.
- **Approval:** Phase 37.1 validation complete.
