---
phase: 17-backup-export-restore
plan: 08
type: tdd
wave: 6
depends_on: [17-04, 17-05, 17-06, 17-07, 17-12]
files_modified: [src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/photos/photo-storage.ts, src/services/photos/photo-storage.test.ts, src/services/photos/restore-photo-finalize-sweep.ts, src/services/photos/restore-photo-finalize-sweep.test.ts, src/services/notifications/notification-schedule.ts, src/services/notifications/digest-schedule.ts, App.tsx]
autonomous: true
requirements: [BKP-04]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A selected backup decrypts, parses, forward-migrates, validates all UIDs/parents/value pairs, and produces aggregate preview data before any restore write."
    - "Merge resolves parents by UID, applies normalized NULL clears, restores one durable custom-field value pair for every final contact/definition combination, uses tombstone precedence, and recomputes last_contact rather than importing it."
    - "Merge applies the singleton non-secret app settings only when its modifiedAt wins, resolving sunContactUid to the reconciled local contact ID; Replace-all replaces the singleton settings state and preserves null as self."
    - "A sunContactUid naming a contact tombstoned by this same restore resolves to null/self, whether the winning settings row was incoming or locally-retained — never a dangling reference."
    - "Every photo's bytes are decoded and staged durably under avatars/_restore_pending/, keyed by stable UID (never an OS temp/cache location), and proven writable BEFORE the outer transaction opens; a staging failure aborts the whole restore with zero DB or canonical-file writes."
    - "An OS-level process kill at ANY point after a photo is durably staged — before finalization is even attempted, or mid-finalization — leaves a trace under avatars/_restore_pending/ that a new launch-sweep step (registered in App.tsx) detects and drives to completion (or garbage-collects if its target no longer exists) on every subsequent foreground launch, with no manual intervention and no silent, permanent dangling photo reference (cycle-2 review HIGH — the prior OS-cache-location design was not sweep-recoverable)."
    - "A post-commit per-photo finalization failure in a still-live process is caught individually and surfaced as an explicit non-blocking count in the result, never only a private log line; the staged file is left in place (not deleted) so the launch sweep retries it."
    - "A post-commit schedule-rebuild failure is reported as committed-with-resync-pending, never restore failure, and relies on the already-registered 11-10/15-03 launch-sweep reconcilers to self-heal on the next foreground launch."
    - "Replace-all creates/verifies a safety snapshot when configured, restores fresh photos, and rebuilds schedules after commit."
  artifacts:
    - "src/backup/restore-apply.ts"
    - "src/backup/restore-apply.test.ts"
    - "src/services/photos/restore-photo-finalize-sweep.ts"
  key_links:
    - "The validated in-memory model is the only input to the single restore transaction."
    - "Post-commit recency and schedule rebuilds occur after the database write boundary."
    - "Settings sunContactUid is validated against backup contact UIDs and translated only after parent contact reconciliation."
    - "restore-photo-finalize-sweep.ts is registered on the Phase-2 launch-sweep registry in App.tsx, mirroring registerPhotoReconcileSweep's one-shot-guard pattern, so a killed finalization resumes on next launch without new background-timer machinery."
  prohibitions:
    - "Restore must not partially apply malformed data or turn a local path from a file into a trusted write target. [flagged-unverified]"
    - "Pre-commit photo staging must not write to an OS cache/temp directory the OS can clear, and must not use the avatars/<name>.tmp naming the existing reconcilePhotoDir sweep already treats as an orphan-to-delete. [flagged-unverified]"
---

<objective>
Build the validated-before-write Restore engine for previewed Merge and confirmed Replace-all.

Purpose: make a backup usable without allowing a damaged file, stale tombstone, or filesystem path to corrupt local state.
Output: load/preview validation boundary and transactional restore-apply core.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/transaction.ts
@src/db/recency-dao.ts
@src/services/photos/photo-storage.ts
@src/services/photos/photo-reconcile-sweep.ts
@src/services/notifications/notification-schedule.ts
@src/services/notifications/digest-schedule.ts
@src/services/launch-sweep.ts
@App.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Turn one selected file into a validated aggregate restore preview without writes</name>
  <read_first>src/backup/backup-schema.ts; src/services/backup/encryption.ts; src/backup/reconciliation.ts; 17-CONTEXT.md D-05, D-14, D-15; 17-UI-SPEC.md restore selection states</read_first>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/services/backup/backup-service.ts, src/backup/restore-apply.test.ts</files>
  <behavior>
    - Plaintext or successfully decrypted older-compatible payloads forward-migrate, validate wholly, and return only date/version/encryption/row/photo aggregate preview data.
    - App settings are exactly one allowlisted non-secret singleton with modifiedAt and sunContactUid that is null for self or references an included contact UID.
    - Wrong passphrase, damaged/incomplete content, duplicate UID, orphan parent including an unknown settings sunContactUid, duplicate normalized pair, secret-shaped settings member, and newer-app format return typed failure before any executor write.
    - A photo byte payload that fails base64 decode is a structural validation failure, surfaced the same way as any other malformed field, before any restore write or filesystem staging is attempted.
    - Empty valid state and tombstone-less older data remain supported.
  </behavior>
  <action>Implement a loadBackupForPreview service path that copies/reads a selected file, decrypts when needed, runs the schema forward migrations and graph validation, invokes reconciliation only for aggregates, and returns a typed preview model. Extend the strict schema with the singleton non-secret app-settings object: require modifiedAt, reject local sunContactId and all secret/key/passphrase members, and accept sunContactUid only when null (self) or present in the backup contact UID set. Enforce D-05 and D-06 by asserting no restore-apply call or SQL write can start until this immutable validated model exists; redact all content/paths from errors and result data.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every pre-commit error is classified with zero target-database writes, and a valid file exposes only the approved aggregate preview fields.</acceptance_criteria>
  <done>One backup can safely reach preview without changing local data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Merge and Replace-all through one outer transaction</name>
  <read_first>src/db/transaction.ts; src/db/contacts-dao.ts createContactFull and its complete definition-pair seeding contract; src/db/app-settings-dao.ts getAppSettingsForBackup/updateAppSettingsCore; src/db/recency-dao.ts exported cores; src/db/field-values-dao.ts upsertValueCore; src/services/photos/photo-storage.ts; 17-CONTEXT.md D-02 through D-06 and D-14</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/photos/photo-storage.ts, src/services/photos/photo-storage.test.ts, src/services/notifications/notification-schedule.ts</files>
  <behavior>
    - Apply resolves category/profile/contact/definition parents by UID before integer FK children, resolves appSettings.sunContactUid only after contacts, uses the shared reconciliation actions, and writes value=null as a durable clear.
    - The completed restored relation has exactly one normalized value row for every final contact × field-definition pair; absent pairs are seeded as value=null before reconciled incoming values are applied.
    - Newer tombstones delete/retain correctly, equal-second deletion wins, and a newer row can restore an older tombstone.
    - Merge retains newer local singleton settings, applies newer incoming allowlisted settings, and maps an incoming sunContactUid to its local contact ID even when the source and destination integer IDs differ; a null sun remains self.
    - Whichever settings row wins (incoming or locally-retained), its sunContactUid is re-validated against the FINAL post-tombstone surviving-contact UID set and resolved to null/self (via 17-04's reference-to-tombstoned-parent fallback) if the named contact did not survive — even when the winning row was the locally-retained one, since this same restore pass may tombstone its sun contact.
    - Replace-all writes the validated incoming singleton settings regardless of modifiedAt, maps sunContactUid after contact replacement, and leaves no secret/key/passphrase state in SQLite or the restore model.
    - The outer apply transaction contains no nested public DAO transaction; malformed plan or an injected write error rolls back all rows.
    - Every referenced photo's bytes are decoded and staged DURABLY under `avatars/_restore_pending/`, keyed by stable backup UID (never an OS temp/cache location, and never the `avatars/<name>.tmp` naming the existing `reconcilePhotoDir` sweep already treats as an interrupted-swap orphan to delete), and proven writable BEFORE the outer transaction opens; a staging failure for any photo aborts the entire restore with zero DB or canonical-photo-file writes.
    - After commit, staged photos are finalized into their canonical location by contact/definition UID (now resolvable to a local id) via the existing, unmodified `persistMaster`; a per-photo finalization failure in a still-live process is caught individually, never aborts the committed restore, is reported as an explicit non-blocking count in the typed result, and LEAVES the staged `_restore_pending` file in place (not deleted) so Task 3's launch sweep retries it.
    - A post-commit notification/digest schedule-rebuild failure is reported as a non-blocking "will self-heal on next launch" result, relying on the existing 11-10/15-03 launch-sweep reconcilers, which already fully re-read DB state — never treated as restore failure and never silently swallowed.
  </behavior>
  <action>Implement restore-apply using only non-mutexed DAO cores under one inWriteTransaction; do not call createContactFull because it owns its own write transaction. Before writes, validate that every serialized contact/definition pair is present exactly once and that each value, including value=null, has valid UID parents. Review findings (HIGH, source-verified — both close real gaps, not stylistic notes):

(1) Photo durability, redesigned for OS-kill recovery (cycle-2 review HIGH — closes a real gap the cycle-1 two-phase design left open: an OS process kill between DB commit and photo finalization was NOT recoverable, because `reconcilePhotoWrites`/`reconcilePhotoDir` (`src/services/photos/photo-storage.ts:213-283`) only ever scan `avatars/<name>.tmp`/`avatars/<name>.bak` — files `persistMaster` itself creates mid-swap inside `avatars/` — and the prior design staged bytes to an OS temp/cache location OUTSIDE `avatars/` with no marker the sweep could ever find). The fix keeps staging durable and sweep-visible by construction, not by inventing a new marker table: add to `photo-storage.ts` (the FS-only chokepoint; keep it free of any executor/DB import, matching its existing "single expo-file-system class-API user" design) a NEW, parallel `avatars/_restore_pending/` subdirectory the EXISTING `reconcilePhotoDir` sweep never lists (it only lists `avatars/` itself, not subdirectories, so this is zero-risk to the shipped `.tmp`/`.bak` swap logic). Add pure helpers: `restorePendingRelPath(target: { kind: "contact"; uid: string } | { kind: "profile" } | { kind: "customField"; uid: string; colName: string }): string` producing `avatars/_restore_pending/contact-<uid>.jpg`, `avatars/_restore_pending/profile.jpg`, or `avatars/_restore_pending/cv-<uid>-<colName>.jpg` (mirrors `relPathForTarget`'s switch shape, but keyed by the backup-stable UID string instead of a not-yet-known local integer id); `stageRestorePending(srcUri, relative): Promise<void>` — copy into `<relative>.stage-tmp` first, then `move()` (an atomic rename, not a byte copy) to the final `relative` name, so a kill mid-copy leaves only an unambiguous `.stage-tmp` orphan, never a corrupt "ready" file; `listRestorePendingPhotos(): { relative: string; isStageTmpOrphan: boolean }[]` — an FS listing of `_restore_pending/`; `deleteRestorePending(relative): void` — best-effort delete, reusing the same `assertSafeRelative` guard `persistMaster`/`deletePhoto` already apply. BEFORE opening the outer transaction, decode every referenced photo's base64 bytes (contact avatars, profile, custom photo-field values) and call `stageRestorePending` for each, keyed by its backup-stable UID reference (contact uid, or contact-uid+col_name for a custom photo field). If any stage write/rename fails (disk full, unexpected decode failure), abort the whole restore before the transaction opens — local data and every existing canonical photo file remain untouched, honoring D-05/D-15's "local data unchanged on failure" literally, not just for SQL rows. Only once every photo stages successfully does the outer inWriteTransaction open, build UID-to-local-ID maps in parent-first order, execute Merge actions or validated Replace-all replacement, seed/apply normalized value pairs, and commit. AFTER commit, for each staged file, resolve its now-known local contact id / col_name and call the existing, unmodified `persistMaster(restorePendingFileUri, canonicalRelativeName)` to finalize it into place — this reuses persistMaster's own crash-safe `.tmp`/`.bak` swap inside `avatars/` unmodified. If `persistMaster` succeeds, delete the `_restore_pending` staged file (its job is done). If `persistMaster` throws for one photo (a live-process failure — disk full mid-swap, permission revoked), catch it per-photo — do not abort or roll back the other photos or the committed restore — log the technical cause privately (Logger, no content), add that photo to a `photosNeedingAttention` count in the typed apply result, and DELIBERATELY LEAVE the staged `_restore_pending` file in place (Task 3's sweep will retry it on next launch — do not delete on failure, only on success). An OS-level kill at ANY point in this AFTER-commit window — before `persistMaster` is even called, or mid-`persistMaster` swap — now leaves the staged file sitting durably under `_restore_pending/`, which Task 3's new launch-sweep step will find and finish on the very next foreground launch; no window is silent or unrecoverable.

(2) Schedule rebuild is a separate, independently-failable post-commit step from photo finalization; do not couple their error handling. If notification/digest schedule reconciliation throws after commit, catch it, log privately, and set a `schedulesNeedResync: true` flag on the typed apply result (surfaced as "reminders will resync automatically" — non-blocking, never "restore failed"). This is honest, not aspirational: `reconcileSchedule` (`src/services/notifications/notification-schedule.ts`, registered via 11-10) and `reconcileDigestSchedule` (`src/services/notifications/digest-schedule.ts`, registered via 15-03) are BOTH already-registered launch-sweep hooks that fully re-read current DB state and reconcile OS schedules on every subsequent real foreground launch — no new marker/table is needed for this case; add a regression that, after simulating a rebuild-call failure, invokes those existing reconcile functions and asserts the mismatch clears.

(3) sunContactUid vs. tombstones. Resolve sunContactUid from the completed contact UID map immediately before the settings-apply call, but validate it against the reconciliation module's reference-to-tombstoned-parent fallback (17-04) rather than a bare map lookup: if the named contact UID lost to a winning tombstone in this same restore pass, resolve to null/self regardless of whether the incoming or the locally-retained settings row is the one being written — a locally-retained row can dangle too if this restore is what tombstones its sun contact. Apply the one validated app-settings singleton through Plan 17-12's updateAppSettingsCore: in Merge compare modifiedAt through the reusable D-02/D-03 policy and retain a newer local row (after the sunContactUid re-validation above); in Replace-all write the incoming allowlisted non-secret row. Never write the serialized source integer id and never invoke SecureStore.

Query/assert the final pair count and uniqueness against the full contact × definition matrix before commit. Never assign contacts.last_contact from payload, and invoke recomputeLastContactCore after interaction mutation. Before Replace-all, call the existing verified automatic snapshot path when configured, otherwise return the required no-destination disclosure state. Do not import field_history or serialized schedule/path state.</action>
  <verify><automated>npm test -- src/backup/restore-apply.test.ts src/services/photos/photo-storage.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tests prove one BEGIN for apply, total rollback on injected error, UID parent mapping, deletion-tie behavior, derived recency, and post-commit schedule rebuild calls; a multi-contact/multi-definition regression proves every final pair exists exactly once and both incoming and seeded NULL clears remain durable. A photo-staging test proves a pre-commit staging failure aborts with zero DB writes and zero canonical photo files touched; a separate test proves `stageRestorePending` writes to `_restore_pending/` via stage-tmp-then-rename (never to an OS cache path, never to `avatars/<name>.tmp`); a third test proves a post-commit `persistMaster` failure for one photo does not roll back the committed restore, appears in the typed result's non-blocking count, and leaves the staged file undeleted. A schedule-rebuild-failure test proves the restore reports committed-with-resync-pending and that the existing 11-10/15-03 reconcilers clear the injected mismatch on a simulated next run. Settings regressions prove cross-device ID translation, null/self sun, tombstone-wins-over-either-settings-row sunContactUid resolution, Merge LWW retain/apply behavior, Replace-all replacement, orphan rejection with no write, and exclusion of API keys, passphrases, and all encryption/key material.</acceptance_criteria>
  <done>Both restore modes have a complete transactional engine behind a validated preview boundary, with photo bytes staged durably (not to OS cache) and validated before any DB commit, schedule-rebuild failures self-healing via existing launch-sweep machinery, and UID-safe non-secret singleton settings restoration that can never dangle a sun reference to a tombstoned contact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register a launch-sweep step that finishes or garbage-collects any pending restore photo</name>
  <read_first>src/services/photos/photo-reconcile-sweep.ts (the pattern to mirror — one idempotent Logger-guarded hook, FS-only, no DB); src/services/launch-sweep.ts registerSweepHook; App.tsx's ready-gated effect and its existing one-shot registration guards (photoReconcileRegistered et al.); src/services/photos/photo-storage.ts restorePendingRelPath/listRestorePendingPhotos/deleteRestorePending (Task 2); src/db/contacts-dao.ts contact-by-uid lookup shape</read_first>
  <files>src/services/photos/restore-photo-finalize-sweep.ts, src/services/photos/restore-photo-finalize-sweep.test.ts, App.tsx</files>
  <behavior>
    - On every real foreground launch, after migration readiness, the sweep lists avatars/_restore_pending/, deletes any orphaned .stage-tmp file unconditionally (always incomplete by definition — a fully staged file is never left with that suffix), and for every fully-staged file resolves its UID against the current DB.
    - A staged contact/profile/custom-field-value photo whose UID still resolves to a live row is finalized via the existing unmodified persistMaster into its canonical avatars/ location and the staged file is deleted on success; a persistMaster failure here is caught, logged, and the staged file is left for the next launch's retry (never a crash, never data loss).
    - A staged photo whose UID no longer resolves to any row (the contact or field value was deleted after the restore committed but before this launch) is deleted as best-effort orphan cleanup — it can never be finalized meaningfully.
    - The hook is idempotent across repeated launches: a fully-finalized restore leaves zero entries under _restore_pending/, so subsequent launches do no work.
    - The hook never runs from module import or a headless notification/widget path — registration happens inside App.tsx's ready-gated effect, following the identical one-shot-guard pattern already used for the photo/field/notification/digest/widget sweeps.
  </behavior>
  <action>Review finding (HIGH, cycle-2 — this task is the other half of Task 1's durable-staging fix, and is what makes the guarantee real rather than aspirational): a durable staged file under `avatars/_restore_pending/` is only a genuine recovery mechanism if something actually drains it after a kill; without this task, Task 2's staging change alone would just leave orphaned bytes on disk forever. Implement `registerRestorePhotoFinalizeSweep(getExecutor: () => SqlExecutor): void` in a new file mirroring `photo-reconcile-sweep.ts`'s shape (one `registerSweepHook` call, Logger-scoped try/catch around the whole pass so a thrown error never wedges the launch sweep, matching the existing `registerFieldSweep(getExecutor)`/`registerNotificationScheduleSweep(getExecutor)` executor-taking convention since this hook needs a DB read to resolve UID → local id, unlike the executor-free `registerPhotoReconcileSweep`). For each entry from `listRestorePendingPhotos()`: if it is a `.stage-tmp` orphan, delete it unconditionally via `deleteRestorePending`. Otherwise, parse the entry's kind/UID/colName back out of its filename (the inverse of `restorePendingRelPath`), resolve the current local id with a plain read (`SELECT id FROM contacts WHERE uid = ?` for contact/profile-adjacent entries; the profile row is fixed-name and singleton so it needs no UID resolution at all — just finalize it directly), call `persistMaster(pendingFileUri, canonicalRelativeName)`, and on success call `deleteRestorePending`; on a thrown `persistMaster`, log privately and leave the file for the next pass; on an unresolved UID (row no longer exists), call `deleteRestorePending` as orphan cleanup. Register the hook in `App.tsx`'s existing `ready`-gated effect, adding one more one-shot guard flag (e.g. `restorePhotoFinalizeSweepRegistered`) alongside the existing `photoReconcileRegistered`/`fieldSweepRegistered`/etc., called with `getExecutor` BEFORE `installSweepTrigger` fires its cold-start pass — identical ordering/reasoning to every other DB-backed sweep hook already there. This closes the loop: any kill after Task 2's pre-commit staging — whether before finalize starts, mid-`persistMaster`, or after a caught live-process failure — self-heals within one foreground launch, indefinitely, with zero manual intervention and zero silent data loss.</action>
  <verify><automated>npm test -- src/services/photos/restore-photo-finalize-sweep.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>A staged file for a still-live contact/profile/custom-field-value photo is finalized into its canonical avatars/ location and removed from _restore_pending/ on a simulated sweep pass; a staged file whose UID no longer resolves is deleted as an orphan; a .stage-tmp orphan is always deleted regardless of age; a thrown persistMaster during the sweep does not crash the launch sweep and leaves the file for retry; App.tsx registers the hook exactly once, after migration readiness, before the cold-start sweep fires, and never reachable from a headless path.</acceptance_criteria>
  <done>A killed restore's pending photo finalization is never permanent — the next real foreground launch finishes it or cleans it up, with no new background timer and no change to the existing tmp/bak swap the launch sweep already reconciles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected backup → parser/decryptor | User-selected and potentially malicious file becomes in-memory model. |
| validated model → SQLite/photos/OS schedules | Restore transforms portable state into durable local and platform state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-18 | Tampering | backup parser/graph | high | mitigate | Full schema, UID, parent, pair, version, and decrypt validation before preview/write. |
| T-17-19 | Tampering | restore transaction and singleton settings | high | mitigate | One outer transaction, non-mutexed cores, UID translation, LWW/Replace policy tests, and rollback coverage. |
| T-17-20 | Tampering | photo file restoration | high | mitigate | Restore bytes via generated safe photo-storage names; never trust serialized paths. |
| T-17-21 | Denial of service | replace safety copy | medium | mitigate | Reuse verified automatic write, disclose no destination, and preserve local state on pre-apply failure. |
| T-17-36 | Tampering | post-commit photo/schedule durability | high | mitigate | Two-phase photo staging (validate+write durably to avatars/_restore_pending/ pre-commit, finalize to canonical avatars/ path post-commit) converts the named failure scenario into a pre-commit whole-restore abort in the common case; residual per-photo/schedule failures are caught individually, never rolled back, and explicitly surfaced in the typed result rather than only logged. |
| T-17-37 | Tampering | app_settings sunContactUid resurrection | high | mitigate | sunContactUid is re-validated against the final post-tombstone surviving-contact set for whichever settings row wins, resolving to null/self rather than a dangling reference. |
| T-17-40 | Tampering | OS-level kill during post-commit photo finalization | high | mitigate | Staging bytes durably under avatars/_restore_pending/ (never an OS-clearable cache path, never a name the existing tmp/bak sweep would delete as an orphan) plus a dedicated launch-sweep step (Task 3) that finalizes or garbage-collects every pending entry on the next foreground launch — a kill at any point after staging is recoverable indefinitely, not only while the process stays alive. |
| T-17-SC | Tampering | package installs | high | mitigate | Dependencies were approved by 17-01. |
</threat_model>

<verification>Run restore/schema suites, tsc, then full Vitest suite after wave completion.</verification>
<success_criteria>Nothing can reach a restore write until the entire backup is safe to apply, committed restores rebuild derived local state correctly, and a process kill at any point after commit — including mid-photo-finalization — self-heals via the launch sweep with no permanent dangling photo reference.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-08-SUMMARY.md` when done</output>
