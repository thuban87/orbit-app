---
phase: 17-backup-export-restore
plan: 08
type: tdd
wave: 5
depends_on: [17-02, 17-04, 17-05, 17-06, 17-07]
files_modified: [src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/photos/photo-storage.ts, src/services/notifications/notification-schedule.ts, src/services/notifications/digest-schedule.ts]
autonomous: true
requirements: [BKP-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A selected backup decrypts, parses, forward-migrates, validates all UIDs/parents/value pairs, and produces aggregate preview data before any restore write."
    - "Merge resolves parents by UID, applies normalized NULL clears, uses tombstone precedence, and recomputes last_contact rather than importing it."
    - "Replace-all creates/verifies a safety snapshot when configured, restores fresh photos, and rebuilds schedules after commit."
  artifacts:
    - "src/backup/restore-apply.ts"
    - "src/backup/restore-apply.test.ts"
  key_links:
    - "The validated in-memory model is the only input to the single restore transaction."
    - "Post-commit recency and schedule rebuilds occur after the database write boundary."
  prohibitions:
    - "Restore must not partially apply malformed data or turn a local path from a file into a trusted write target. [flagged-unverified]"
---

<objective>
Build the validated-before-write Restore engine for previewed Merge and confirmed Replace-all.

Purpose: make a backup usable without allowing a damaged file, stale tombstone, or filesystem path to corrupt local state.
Output: load/preview validation boundary and transactional restore-apply core.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/transaction.ts
@src/db/recency-dao.ts
@src/services/photos/photo-storage.ts
@src/services/notifications/notification-schedule.ts
@src/services/notifications/digest-schedule.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Turn one selected file into a validated aggregate restore preview without writes</name>
  <read_first>src/backup/backup-schema.ts; src/services/backup/encryption.ts; src/backup/reconciliation.ts; 17-CONTEXT.md D-05, D-14, D-15; 17-UI-SPEC.md restore selection states</read_first>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/services/backup/backup-service.ts, src/backup/restore-apply.test.ts</files>
  <behavior>
    - Plaintext or successfully decrypted older-compatible payloads forward-migrate, validate wholly, and return only date/version/encryption/row/photo aggregate preview data.
    - Wrong passphrase, damaged/incomplete content, duplicate UID, orphan parent, duplicate normalized pair, and newer-app format return typed failure before any executor write.
    - Empty valid state and tombstone-less older data remain supported.
  </behavior>
  <action>Implement a loadBackupForPreview service path that copies/reads a selected file, decrypts when needed, runs the schema forward migrations and graph validation, invokes reconciliation only for aggregates, and returns a typed preview model. Enforce D-05 by asserting no restore-apply call or SQL write can start until this immutable validated model exists; redact all content/paths from errors and result data.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every pre-commit error is classified with zero target-database writes, and a valid file exposes only the approved aggregate preview fields.</acceptance_criteria>
  <done>One backup can safely reach preview without changing local data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Merge and Replace-all through one outer transaction</name>
  <read_first>src/db/transaction.ts; src/db/recency-dao.ts exported cores; src/db/field-values-dao.ts; src/services/photos/photo-storage.ts; 17-CONTEXT.md D-02 through D-06 and D-14</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/photos/photo-storage.ts, src/services/notifications/notification-schedule.ts</files>
  <behavior>
    - Apply resolves category/profile/contact/definition parents by UID before integer FK children, uses the shared reconciliation actions, and writes value=null as a durable clear.
    - Newer tombstones delete/retain correctly, equal-second deletion wins, and a newer row can restore an older tombstone.
    - The outer apply transaction contains no nested public DAO transaction; malformed plan or an injected write error rolls back all rows.
    - Photos are regenerated through photo storage; last_contact and OS schedules are rebuilt after commit; Replace-all requests a verified pre-restore automatic snapshot when a destination exists.
  </behavior>
  <action>Implement restore-apply using only non-mutexed DAO cores under one inWriteTransaction. Build UID-to-local-ID maps in parent-first order, execute Merge actions or validated Replace-all replacement, never assign contacts.last_contact from payload, and invoke recomputeLastContactCore after interaction mutation. After commit, persist fresh photo bytes through safe generated relative names and call notification/digest reconciliation; make failures typed and log privately. Before Replace-all, call the existing verified automatic snapshot path when configured, otherwise return the required no-destination disclosure state. Do not import field_history or serialized schedule/path state.</action>
  <verify><automated>npm test -- src/backup/restore-apply.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tests prove one BEGIN for apply, total rollback on injected error, UID parent mapping, NULL clears, deletion-tie behavior, fresh photo targets, derived recency, and post-commit schedule rebuild calls.</acceptance_criteria>
  <done>Both restore modes have a complete transactional engine behind a validated preview boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected backup → parser/decryptor | User-selected and potentially malicious file becomes in-memory model. |
| validated model → SQLite/photos/OS schedules | Restore transforms portable state into durable local and platform state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-18 | Tampering | backup parser/graph | high | mitigate | Full schema, UID, parent, pair, version, and decrypt validation before preview/write. |
| T-17-19 | Tampering | restore transaction | high | mitigate | One outer transaction, non-mutexed cores, rollback tests, and centralized policy actions. |
| T-17-20 | Tampering | photo file restoration | high | mitigate | Restore bytes via generated safe photo-storage names; never trust serialized paths. |
| T-17-21 | Denial of service | replace safety copy | medium | mitigate | Reuse verified automatic write, disclose no destination, and preserve local state on pre-apply failure. |
| T-17-SC | Tampering | package installs | high | mitigate | Dependencies were approved by 17-01. |
</threat_model>

<verification>Run restore/schema suites, tsc, then full Vitest suite after wave completion.</verification>
<success_criteria>Nothing can reach a restore write until the entire backup is safe to apply, and committed restores rebuild derived local state correctly.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-08-SUMMARY.md` when done</output>
