---
phase: 17-backup-export-restore
plan: 08
type: tdd
wave: 6
depends_on: [17-04, 17-05, 17-06, 17-07, 17-12]
files_modified: [src/db/migrations/008-restore-photo-journal.ts, src/db/migrations/008-restore-photo-journal.test.ts, src/db/database.ts, src/db/restore-photo-journal-dao.ts, src/db/restore-photo-journal-dao.test.ts, src/db/photo-relative-path.ts, src/db/photo-relative-path.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/backup/backup-service.test.ts, src/services/photos/photo-storage.ts, src/services/photos/photo-storage.test.ts, src/services/photos/restore-photo-finalize-sweep.ts, src/services/photos/restore-photo-finalize-sweep.test.ts, src/services/notifications/notification-schedule.ts, src/services/notifications/notification-schedule.test.ts, src/services/notifications/digest-schedule.ts, src/services/notifications/digest-schedule.test.ts, App.tsx]
autonomous: true
requirements: [BKP-04]
estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A selected backup decrypts, parses, forward-migrates, validates all UIDs/parents/value pairs, rejects a live child whose parent already lost within the SAME file's own live-row+tombstone evidence, and produces aggregate preview data before any restore write."
    - "Merge resolves parents by UID, applies normalized NULL clears, restores one durable custom-field value pair for every final contact/definition combination, uses tombstone precedence, blocks (never inserts/updates) a child whose local OR incoming parent did not survive, and recomputes last_contact WITHOUT inflating each contact's reconciled modified_at (Pass-2 audit B-6 — recomputeLastContactCore's now argument is passed the contact's own final reconciled modified_at, never restore wall-clock time, so the recency write is a no-op on modified_at while still correcting last_contact)."
    - "A custom_field_values pair-key collision or a custom_field_defs.col_name collision between local and incoming state — reported by 17-04's reconciliation as a whole-restore incompatibility — rejects the ENTIRE restore with zero writes before the outer transaction opens, per D-05 (Pass-2 audit B-3, B-5)."
    - "Merge applies the singleton non-secret app settings only when its modifiedAt wins, resolving sunContactUid to the reconciled local contact ID; Replace-all replaces the singleton settings state and preserves null as self."
    - "A sunContactUid naming a contact tombstoned by this same restore resolves to null/self, whether the winning settings row was incoming or locally-retained — never a dangling reference."
    - "Restore-apply advances app_settings.data_revision exactly once inside its own successful outer transaction for every non-no-op Merge and every Replace-all, composed per 17-06's coalescing rule and captured after all mutations — never via a backup-bookkeeping write, never omitted (Pass-2 audit A-05)."
    - "Replace-all is a tombstone-aware delete: every hard-deleted current mergeable row gets a captured-UID tombstone via 17-02's core in the same outer transaction (retaining/importing incoming tombstones too), follows one specified FK-safe reset sequence (null/reset app_settings.sun_contact_id first; dependents before parents including an explicit field_history purge; parents before children on insert; settings applied only after final contact UID-to-id mapping), and a raw table clear or FK cascade is never treated as deletion evidence (Pass-2 audit A-06, B-7)."
    - "Before Replace-all ever deletes a row, a FORCED createVerifiedPreRestoreSnapshot always writes and verifies a distinct snapshot (locally and in SAF) when a destination is configured, and blocks Replace-all on any configured-destination failure; only a genuinely absent destination may continue after the explicit data-loss warning — the ordinary not-due/no-change automatic path is never reused for this call (Pass-2 audit B-14)."
    - "Restore-pending staging names are flat (avatars/_restore_pending/<kind>-<uid>-<session>.<ext>) and validated by a SEPARATE, narrowly-scoped assertSafeRestorePendingRelative guard in src/db/photo-relative-path.ts — never the generic avatars/<name>.<ext> SAFE_RELATIVE grammar, which forbids subdirectories and would throw on any nested pending path (Pass-3 audit B-9)."
    - "Every photo's bytes are decoded and staged durably under avatars/_restore_pending/, keyed by stable UID plus a per-restore session token (never an OS temp/cache location, never a name collision across separate restore attempts), and proven writable BEFORE the outer transaction opens; a staging failure aborts the whole restore with zero DB or canonical-file writes."
    - "A file is staged/journaled/finalized ONLY when its owning row's final reconciliation action (from 17-04) is insert or a winning update; a photo belonging to a retained-local, blocked, or losing-incoming row is never staged or finalized, so a newer local photo can never be overwritten by a stale incoming one (Pass-3 audit B-11)."
    - "Every staged finalize candidate and every determined post-commit deletion candidate is recorded as one row in the migration-008 restore_photo_journal table, INSERTED inside the SAME outer restore transaction Task 2 already opens (immediately before its COMMIT) — never as a bare filesystem convention alone — so a rolled-back or never-opened transaction leaves ZERO committed journal rows regardless of what already exists on disk, and only a file with a COMMITTED journal row may ever be finalized onto a live canonical photo (Pass-3 audit B-10)."
    - "An OS-level process kill at ANY point after a photo is durably staged and journaled — before finalization is even attempted, or mid-finalization — leaves a trace under avatars/_restore_pending/ AND a matching committed journal row that a new launch-sweep step (registered in App.tsx) detects and drives to completion (or garbage-collects if its target no longer exists or no committed journal row matches it) on every subsequent foreground launch, with no manual intervention and no silent, permanent dangling photo reference (cycle-2 review HIGH, sharpened by Pass-3 audit B-10 — a staged file with no committed journal row is always GC'd, never finalized)."
    - "A post-commit per-photo finalization failure in a still-live process is caught individually and surfaced as an explicit non-blocking count in the result, never only a private log line; the staged file AND its journal row are left in place (neither deleted) so the launch sweep retries it."
    - "A winning explicit photo clear, a row lost to a winning tombstone, and every row Replace-all's reset removes each produce a durable post-commit 'delete' journal row for their PRIOR canonical photo path — read from the local DB's own current state immediately before the owning row changes, NEVER derived from the backup file's serialized content — so a superseded or orphaned canonical master (including one a reused, non-AUTOINCREMENT integer id could otherwise resurface for a different person) is durably removed rather than left on disk (Pass-3 audit B-11, B-13)."
    - "The launch-sweep finalize step (Task 3) resolves every journal row's target from its own typed identity columns (contact_uid for a contact; value_uid plus a live custom_field_defs.type='photo' definition plus contact mapping for a custom photo value; the fixed profile singleton needs no lookup) — never by re-parsing the staged filename — and re-verifies that identity with a fresh read immediately before persistMaster/deletePhoto as a final current-target check; a 'finalize' row whose target no longer resolves, and a 'delete' row, are both drained the same way regardless of process-kill timing (Pass-3 audit B-12)."
    - "A post-commit schedule-rebuild failure inside restore-apply is reported as committed-with-resync-pending, never restore failure, and relies on the already-registered 11-10/15-03 launch-sweep reconcilers to self-heal on the next foreground launch."
    - "The launch-sweep registry's notification and digest schedule hooks each isolate their own failure with a Logger-guarded catch (mirroring the established field-sweep.ts convention), so a thrown notification reconcile can never prevent the digest reconcile — or any later-registered hook — from running on the SAME or a later foreground launch (Pass-2 audit B-15)."
    - "Replace-all creates/verifies a safety snapshot when configured, restores fresh photos, and rebuilds schedules after commit."
  artifacts:
    - "src/backup/restore-apply.ts"
    - "src/backup/restore-apply.test.ts"
    - "src/services/photos/restore-photo-finalize-sweep.ts"
    - "src/db/migrations/008-restore-photo-journal.ts"
    - "src/db/restore-photo-journal-dao.ts"
  key_links:
    - "The validated in-memory model is the only input to the single restore transaction."
    - "Post-commit recency and schedule rebuilds occur after the database write boundary."
    - "Settings sunContactUid is validated against backup contact UIDs and translated only after parent contact reconciliation."
    - "restore-photo-finalize-sweep.ts is registered on the Phase-2 launch-sweep registry in App.tsx, mirroring registerPhotoReconcileSweep's one-shot-guard pattern, so a killed finalization resumes on next launch without new background-timer machinery."
    - "restore_photo_journal (migration 008) is the single source of truth Task 3's sweep drains for both 'finalize' and 'delete' actions; the avatars/_restore_pending/ FS listing only ever finds CANDIDATE files — a staged file with no matching committed journal row is always garbage-collected, never finalized (Pass-3 audit B-10, B-12)."
    - "restore-apply.ts consumes 17-04's reconciliation.ts blocked-action and incompatibilities contract directly rather than reimplementing parent-survival or collision detection; any non-empty incompatibilities list aborts before the outer transaction opens."
    - "restore-apply.ts's recency recompute call binds recomputeLastContactCore's now parameter to each contact's own final reconciled modified_at (from 17-04's LWW-decided value), never to restore wall-clock time, so a later Merge's D-03 timestamp comparison is never corrupted by restore itself (17-AUDIT-B-FINDINGS.md finding 6)."
    - "Replace-all's tombstone-aware deletes call 17-02's tombstones-dao.ts insertion core (which already bumps data_revision) inside the same outer transaction as every hard delete, before that row disappears."
    - "createVerifiedPreRestoreSnapshot (backup-service.ts) reuses 17-06's SAF write/verify primitives but is never gated by auto-backup-policy.ts's due/changed decision — it always executes when a destination is configured, distinct from the ordinary automatic-backup path."
  prohibitions:
    - "Restore must not partially apply malformed data or turn a local path from a file into a trusted write target. [flagged-unverified]"
    - "Pre-commit photo staging must not write to an OS cache/temp directory the OS can clear, and must not use the avatars/<name>.tmp naming the existing reconcilePhotoDir sweep already treats as an orphan-to-delete. [flagged-unverified]"
    - "Restore-pending staging must never use or broaden the generic avatars/<name>.<ext> SAFE_RELATIVE grammar to permit a subdirectory; it must use the separate, narrowly-scoped restore-pending guard instead, and that guard must never be consulted by the DB write boundary or by persistMaster's canonical-path check (Pass-3 audit B-9). [flagged-unverified]"
    - "A staged or queued photo file/operation must never be finalized or otherwise treated as authoritative unless a COMMITTED restore_photo_journal row names it — a rolled-back or never-opened restore transaction must leave the filesystem inert from the sweep's point of view (Pass-3 audit B-10). [flagged-unverified]"
    - "A post-commit photo deletion/GC path must never be derived from the backup file's serialized path/content — only from the local database's own current state, read before the owning row changes (Pass-3 audit B-13). [flagged-unverified]"
    - "Restore-apply must never call the shared recomputeLastContactCore with actual restore wall-clock time as its now argument — doing so silently overwrites each restored contact's reconciled modified_at and reverses D-03 for every subsequent Merge. [flagged-unverified]"
    - "Replace-all must never skip or reuse the ordinary due/changed automatic-backup path in place of the forced pre-restore snapshot — a not-due or unchanged result from that path is not evidence a pre-restore snapshot was written. [flagged-unverified]"
---

<objective>
Build the validated-before-write Restore engine for previewed Merge and confirmed Replace-all.

Purpose: make a backup usable without allowing a damaged file, stale tombstone, or filesystem path to corrupt local state.
Output: load/preview validation boundary and transactional restore-apply core.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/transaction.ts
@src/db/recency-dao.ts
@src/services/photos/photo-storage.ts
@src/services/photos/photo-reconcile-sweep.ts
@src/db/photo-relative-path.ts
@src/services/notifications/notification-schedule.ts
@src/services/notifications/digest-schedule.ts
@src/services/launch-sweep.ts
@App.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Turn one selected file into a validated aggregate restore preview without writes</name>
  <read_first>src/backup/backup-schema.ts; src/services/backup/encryption.ts; src/backup/reconciliation.ts (Pass-2: the exported same-file live-row+tombstone survivor helper); 17-CONTEXT.md D-05, D-14, D-15; 17-UI-SPEC.md restore selection states; 17-AUDIT-B-FINDINGS.md finding 2 (B-2, whole-file half)</read_first>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/services/backup/backup-service.ts, src/backup/restore-apply.test.ts</files>
  <behavior>
    - Plaintext or successfully decrypted older-compatible payloads forward-migrate, validate wholly, and return only date/version/encryption/row/photo aggregate preview data.
    - App settings are exactly one allowlisted non-secret singleton with modifiedAt and sunContactUid that is null for self or references an included contact UID.
    - Wrong passphrase, damaged/incomplete content, duplicate UID, orphan parent including an unknown settings sunContactUid, duplicate normalized pair, secret-shaped settings member, and newer-app format return typed failure before any executor write.
    - A live (non-tombstoned) child row in the file — interactions/events/fuel/contact_links by contact_id, or custom_field_values by contact_id or field_def_id — whose declared parent uid is NOT in that parent entity type's SAME-FILE survivor set (computed purely from the file's own live rows and tombstones, via 17-04's exported survivor helper — no local DB state involved) is a structural validation failure, distinct from and in addition to the existing "orphan parent" (unknown-UID) check (Pass-2 audit B-2, whole-file half).
    - A photo byte payload that fails base64 decode is a structural validation failure, surfaced the same way as any other malformed field, before any restore write or filesystem staging is attempted.
    - Empty valid state and tombstone-less older data remain supported.
  </behavior>
  <action>Implement a loadBackupForPreview service path that copies/reads a selected file, decrypts when needed, runs the schema forward migrations and graph validation, invokes reconciliation only for aggregates, and returns a typed preview model. Extend the strict schema with the singleton non-secret app-settings object: require modifiedAt, reject local sunContactId and all secret/key/passphrase members, and accept sunContactUid only when null (self) or present in the backup contact UID set. Enforce D-05 and D-06 by asserting no restore-apply call or SQL write can start until this immutable validated model exists; redact all content/paths from errors and result data.

Review finding (HIGH, Pass-2 audit B-2, whole-file half, source-verified): the existing "orphan parent" check only catches a child citing a UID absent from the file entirely; it does not catch a live child citing a UID that IS present in the file but only as a tombstone (or as a live row whose own file-internal tombstone is same-or-newer per D-03's tie-break) — a structurally incoherent submission the graph-validation pass must reject before preview, independent of whatever the local database happens to contain. Call 17-04's exported same-file survivor helper once per parent entity type present in the file (contacts, custom_field_defs) — feeding it ONLY that file's own live-row array and tombstone array for the type, never local state — to get the file-internal survivor uid set. For every live child row of that parent type (interactions/events/fuel/contact_links by contact_id; custom_field_values checked against BOTH its contact_id and field_def_id survivor sets independently), reject the whole file with a typed structural-validation failure if its declared parent uid is missing from the corresponding survivor set. This is a pure, local-DB-free check reusing the identical D-03 tie-break the two-sided Merge reconciliation will apply later, so the same file never validates differently depending on what a device's local database happens to contain.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every pre-commit error is classified with zero target-database writes, and a valid file exposes only the approved aggregate preview fields.</acceptance_criteria>
  <done>One backup can safely reach preview without changing local data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Merge and Replace-all through one outer transaction</name>
  <read_first>src/db/transaction.ts; src/db/contacts-dao.ts createContactFull and its complete definition-pair seeding contract; src/db/app-settings-dao.ts getAppSettingsForBackup/updateAppSettingsCore; src/db/recency-dao.ts exported cores (recomputeLastContactCore's `now` argument sets BOTH last_contact and contacts.modified_at); src/db/field-values-dao.ts upsertValueCore; src/services/photos/photo-storage.ts; src/db/tombstones-dao.ts (17-02's non-mutexed insertion core, already bumps data_revision); src/db/data-revision-dao.ts (17-02's bumpDataRevisionCore/readDataRevision); src/db/migrations/003-orrery-settings.ts (app_settings.sun_contact_id ON DELETE SET NULL); src/db/purge-dao.ts (the FK-safe explicit fan-out ordering pattern to mirror for Replace-all's reset); src/backup/reconciliation.ts (17-04 Pass-2: blocked actions and the incompatibilities result — restore-apply consumes both, never reimplements them); src/services/backup/saf-storage.ts and src/backup/export-manifest.ts (the write/verify primitives createVerifiedPreRestoreSnapshot reuses); src/services/launch-sweep.ts registerSweepHook/runLaunchSweep (does not isolate hook-internal failures itself — each hook owns that); src/services/field-sweep.ts (the established per-hook Logger-guarded try/catch convention to mirror for B-15); src/db/photo-relative-path.ts (assertSafeRelative and the new assertSafeRestorePendingRelative guard this plan adds, Pass-3 B-9); src/db/migrations/008-restore-photo-journal.ts and src/db/restore-photo-journal-dao.ts (new in this plan — the committed pending-photo journal that makes finalization provably tied to a committed restore, Pass-3 B-10); 17-CONTEXT.md D-02 through D-06 and D-14; 17-AUDIT-A-FINDINGS.md A-05, A-06; 17-AUDIT-B-FINDINGS.md findings 2 (local-parent half), 3, 5, 6, 7, 9, 10, 11, 12, 13, 14, 15</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/services/backup/backup-service.ts, src/services/backup/backup-service.test.ts, src/services/photos/photo-storage.ts, src/services/photos/photo-storage.test.ts, src/services/notifications/notification-schedule.ts, src/services/notifications/notification-schedule.test.ts, src/services/notifications/digest-schedule.ts, src/services/notifications/digest-schedule.test.ts</files>
  <behavior>
    - Apply resolves category/profile/contact/definition parents by UID before integer FK children, resolves appSettings.sunContactUid only after contacts, uses the shared reconciliation actions, and writes value=null as a durable clear.
    - The completed restored relation has exactly one normalized value row for every final contact × field-definition pair; absent pairs are seeded as value=null before reconciled incoming values are applied.
    - Newer tombstones delete/retain correctly, equal-second deletion wins, and a newer row can restore an older tombstone.
    - Merge retains newer local singleton settings, applies newer incoming allowlisted settings, and maps an incoming sunContactUid to its local contact ID even when the source and destination integer IDs differ; a null sun remains self.
    - Whichever settings row wins (incoming or locally-retained), its sunContactUid is re-validated against the FINAL post-tombstone surviving-contact UID set and resolved to null/self (via 17-04's reference-to-tombstoned-parent fallback) if the named contact did not survive — even when the winning row was the locally-retained one, since this same restore pass may tombstone its sun contact.
    - Replace-all writes the validated incoming singleton settings regardless of modifiedAt, maps sunContactUid after contact replacement, and leaves no secret/key/passphrase state in SQLite or the restore model.
    - The outer apply transaction contains no nested public DAO transaction; malformed plan or an injected write error rolls back all rows.
    - Only a photo belonging to a row whose FINAL reconciliation action (already computed before staging begins) is `insert` or a winning `update` is decoded and staged; a retained-local, `blocked`, or losing-incoming row's photo is never staged, so a newer local photo can never be clobbered by a stale incoming one (Pass-3 audit B-11). Staged bytes go DURABLY under `avatars/_restore_pending/` using the SEPARATE `assertSafeRestorePendingRelative` guard (never the generic `SAFE_RELATIVE`/`avatars/<name>.tmp` grammar the existing `reconcilePhotoDir` sweep already treats as an interrupted-swap orphan — Pass-3 audit B-9), keyed by stable backup UID plus a per-restore session token (never an OS temp/cache location, never colliding across separate restore attempts), and proven writable BEFORE the outer transaction opens; a staging failure for any photo aborts the entire restore with zero DB or canonical-photo-file writes.
    - Immediately before the outer transaction's COMMIT, one `restore_photo_journal` row (migration 008) is inserted for every successfully staged finalize candidate (typed target identity plus the now-resolvable canonical relative path) and for every determined post-commit deletion candidate (a winning explicit clear, a row lost to a winning tombstone, or a Replace-all-removed row that HAD a non-null local photo — captured by reading the local DB's own current photo column immediately before that row changes, never derived from the backup file's content); a rolled-back or never-committed transaction therefore commits ZERO journal rows regardless of what already exists under `_restore_pending/` (Pass-3 audit B-10, B-11, B-13). After commit, staged finalize entries are finalized into their canonical location via the existing, unmodified `persistMaster`, and delete entries remove their canonical path via the existing `deletePhoto`; a per-photo failure (finalize or delete) in a still-live process is caught individually, never aborts the committed restore, is reported as an explicit non-blocking count in the typed result, and LEAVES the staged file/journal row in place (nothing deleted) so Task 3's launch sweep retries it.
    - A post-commit notification/digest schedule-rebuild failure INSIDE restore-apply's own call is reported as a non-blocking "will self-heal on next launch" result, relying on the existing 11-10/15-03 launch-sweep reconcilers, which already fully re-read DB state — never treated as restore failure and never silently swallowed.
    - A child action (interactions/events/fuel/contact_links/custom_field_values) that 17-04's reconciliation returns as `blocked` because its local OR incoming parent did not survive contributes zero writes and zero rows to the pair-completeness/count checks; it is never coerced into an insert/update, and its exclusion is reflected in the typed apply result's totals (Pass-2 audit B-2, apply half).
    - Any non-empty `incompatibilities` list from 17-04's reconciliation (a pair-key or col_name collision between local and incoming state) aborts the ENTIRE restore before the outer transaction opens — zero DB writes, zero photo staging — surfaced as a typed "incompatible destination" failure distinct from ordinary structural-malformed failures (Pass-2 audit B-3, B-5; D-05).
    - After every interaction-affecting mutation for a contact is applied inside the outer transaction, restore calls `recomputeLastContactCore(exec, contactId, reconciledModifiedAt)` — binding its `now` argument to that contact's OWN final reconciled `modified_at` (the value the contacts row was just written with), never restore wall-clock time — so `last_contact` is corrected while `modified_at` is rewritten to the exact value it already holds (a no-op on that column). A later Merge against this restored database sees the true reconciled timestamp, not an inflated restore-time one (Pass-2 audit B-6).
    - Restore-apply calls `bumpDataRevisionCore(exec)` exactly once, inside its own outer transaction, immediately before commit, for any Merge that performed at least one write action (insert/update/delete/blocked-with-side-effect) or for any Replace-all — never for a true Merge no-op, and never via a backup-bookkeeping write (Pass-2 audit A-05).
    - Replace-all captures the uid of every current mergeable row about to be hard-deleted and inserts its tombstone (via 17-02's tombstones-dao.ts core) in the SAME outer transaction, before that row disappears; incoming tombstones from the file are also retained/imported. The reset sequence inside that one transaction is: (a) `UPDATE app_settings SET sun_contact_id = NULL` unconditionally first; (b) delete dependents before parents for every current row — interactions, events, fuel, custom_field_values, contact_links, and an explicit `field_history` purge (it has no FK and never cascades) — mirroring purge-dao.ts's explicit non-cascade fan-out; (c) delete parents (contacts, custom_field_defs, categories) after their captured children; (d) insert incoming parents before incoming children; (e) apply the incoming/retained singleton settings row, including sunContactUid-to-local-id mapping, only AFTER the final contact UID-to-id mapping is known (Pass-2 audit A-06, B-7).
    - Before Replace-all deletes anything, a FORCED `createVerifiedPreRestoreSnapshot` call always writes and locally-plus-SAF-verifies a distinct snapshot when a destination is configured, and BLOCKS Replace-all (zero deletes) on any configured-destination write/verify failure; it is never the ordinary due/changed-gated automatic path, which may report "not due" or "unchanged" and skip writing entirely. Only a genuinely absent (unconfigured) destination may proceed after the existing explicit data-loss disclosure (Pass-2 audit B-14).
    - The launch-sweep registry's `registerNotificationScheduleSweep` and `registerDigestScheduleSweep` hooks (src/services/notifications/notification-schedule.ts, digest-schedule.ts) each wrap their own reconcile call in a Logger-guarded try/catch, mirroring field-sweep.ts's established per-hook isolation convention, so a thrown notification reconcile can never prevent the digest hook — registered after it in App.tsx — from running on the SAME foreground launch, and both self-heal independently on a later launch (Pass-2 audit B-15). This is general launch-sweep hygiene, not restore-specific, but restore's "schedules will self-heal on next launch" claim depends on it being true.
  </behavior>
  <action>Implement restore-apply using only non-mutexed DAO cores under one inWriteTransaction; do not call createContactFull because it owns its own write transaction. Before writes, validate that every serialized contact/definition pair is present exactly once and that each value, including value=null, has valid UID parents. Review findings (HIGH, source-verified — both close real gaps, not stylistic notes):

(1) Photo durability, redesigned for OS-kill recovery AND for provable finalize-only-after-commit safety (cycle-2 review HIGH, sharpened by Pass-3 audit B-9/B-10/B-11/B-12/B-13 — closes two distinct real gaps: an OS process kill between DB commit and photo finalization was NOT recoverable under the cycle-1 design, and — the Pass-3 finding — even the cycle-2 "stage under avatars/_restore_pending/" fix (a) used a subdirectory path the EXISTING `assertSafeRelative` guard, `src/db/photo-relative-path.ts`, rejects outright, since `SAFE_RELATIVE` permits only `avatars/<name>.<ext>` with no subdirectory segment, and (b) had no durable way to prove a staged file belonged to a restore that actually committed, so a naive sweep could finalize stale bytes onto a live photo from a restore that rolled back or never began). None of the following touches `reconcilePhotoWrites`/`reconcilePhotoDir` (`src/services/photos/photo-storage.ts:213-283`, which only ever lists `avatars/` itself, not subdirectories — zero risk to the shipped `.tmp`/`.bak` swap logic).

(1a) Separate staging grammar (B-9). Add to `src/db/photo-relative-path.ts` — NEVER broadening the existing `SAFE_RELATIVE` — a second, independent allowlist `SAFE_RESTORE_PENDING_RELATIVE` matching only `avatars/_restore_pending/<name>.<ext>` (plus its `.stage-tmp` suffix variant) with `<name>` restricted to the same safe charset, and an `assertSafeRestorePendingRelative(relative: string): void` guard mirroring `assertSafeRelative`'s null-byte/regex checks. This new guard is used EXCLUSIVELY by the staging helpers below; `persistMaster`/`deletePhoto`/the DB write boundary keep using the untouched, unbroadened `assertSafeRelative` for every canonical path. Add direct safety tests (`src/db/photo-relative-path.test.ts`) proving: a subdirectory-shaped path (`avatars/_restore_pending/x.jpg`) is REJECTED by `assertSafeRelative` (documenting exactly why the cycle-2 design broke) and ACCEPTED by `assertSafeRestorePendingRelative`; and a traversal/null-byte/absolute-path string is rejected by both guards.

(1b) Staging helpers, gated on the FINAL reconciliation action (B-11) and collision-proof across attempts. In `photo-storage.ts` (the FS-only chokepoint; still free of any executor/DB import), add pure helpers: `restorePendingRelPath(target: { kind: "contact"; uid: string } | { kind: "profile" } | { kind: "customField"; uid: string; colName: string }, sessionToken: string): string` producing `avatars/_restore_pending/contact-<uid>-<sessionToken>.jpg`, `avatars/_restore_pending/profile-<sessionToken>.jpg`, or `avatars/_restore_pending/cv-<uid>-<colName>-<sessionToken>.jpg` (mirrors `relPathForTarget`'s switch shape, keyed by the backup-stable UID string plus a per-restore-invocation `sessionToken` minted once via `newUid()` at the start of this restore-apply call, so a retried restore after a prior kill can never collide with a stale name from the earlier attempt); `stageRestorePending(srcUri, relative): Promise<void>` — validated by `assertSafeRestorePendingRelative`, copies into `<relative>.stage-tmp` first, then `move()`s (an atomic rename) to the final `relative` name, so a kill mid-copy leaves only an unambiguous `.stage-tmp` orphan, never a corrupt "ready" file; `listRestorePendingPhotos(): { relative: string; isStageTmpOrphan: boolean }[]` — an FS listing of `_restore_pending/`; `deleteRestorePending(relative): void` — best-effort delete, validated by the SAME new guard.

BEFORE opening the outer transaction: from the ALREADY-COMPUTED reconciliation action results (Merge) — never from the raw backup file alone — classify every photo-bearing row (contact avatar, profile, custom photo-field value) into at most one of two candidate sets. FINALIZE candidate: final action is `insert` or a winning `update` AND the winning (incoming) row's photo field is non-null — decode its base64 bytes and `stageRestorePending` it under the session-scoped name. MERGE-PATH DELETE candidate: final action is `delete` (lost to a winning tombstone) and the row's CURRENT local photo column (one plain pre-transaction SELECT — a read, not a write, needs no transaction) is non-null; OR final action is a winning `update`/`insert` whose incoming row explicitly clears the photo (photo field present-but-null) while the current local photo column is non-null. A row whose action is `retain` or `blocked` is neither candidate — its photo, if any, is left untouched. Replace-all's own delete candidates are identified separately, inline, at the point described in (1c) below. If ANY finalize candidate's stage write/rename fails (disk full, unexpected decode failure), abort the whole restore before the transaction opens — local data and every existing canonical photo file remain untouched, honoring D-05/D-15's "local data unchanged on failure" literally, not just for SQL rows.

(1c) A committed pending-photo journal (B-10), drained post-commit with typed-identity liveness proof (B-12/B-13). Add a new additive, forward-only migration `src/db/migrations/008-restore-photo-journal.ts` (raises `TARGET_VERSION` to 8, registered in `database.ts` after migration 007 and its 17-12 extensions) creating `restore_photo_journal(id INTEGER PRIMARY KEY, relative_path TEXT NOT NULL UNIQUE, action TEXT NOT NULL CHECK(action IN ('finalize','delete')), target_kind TEXT NOT NULL CHECK(target_kind IN ('contact','profile','customField')), contact_uid TEXT, value_uid TEXT, field_def_uid TEXT, canonical_relative_path TEXT NOT NULL, created_at TEXT NOT NULL)`. Add `src/db/restore-photo-journal-dao.ts` with three non-mutexed cores (the field-ddl/purge-dao "assumes an already-open transaction" convention): `insertJournalEntryCore(exec, entry)`, `listJournalEntriesCore(exec)` (a plain read, usable by Task 3's sweep outside any transaction), and `deleteJournalEntryCore(exec, relativePath)`.

Once every finalize candidate has staged successfully, the outer `inWriteTransaction` opens and this task's Merge/Replace-all apply logic proceeds exactly as the rest of this action already describes. Within that SAME transaction: for each finalize candidate, insert its `restore_photo_journal` row (`action='finalize'`, the typed target identity — `contact_uid` for a contact; `value_uid`+`field_def_uid`+`contact_uid` for a custom photo value; no identity columns for the singleton profile — and `canonical_relative_path` built from the row's local id via the existing `contactPhotoRelPath`/`profilePhotoRelPath`/`customFieldPhotoRelPath` builders) as soon as that row's local id is resolved — the same point this task's existing parent-first UID-to-local-ID mapping already reaches for every other write, requiring no separate pass. For each Merge-path delete candidate from (1b), insert its row (`action='delete'`, same typed identity, `canonical_relative_path` equal to the row's own already-known canonical path, since a row's local integer id never changes across Merge) as soon as that row's fate is applied. For Replace-all: at the exact same existing point this task's Replace-all reset sequence already captures each about-to-be-removed row's uid before its tombstone/DELETE, ALSO read that row's current photo column as one additional SELECT column on that same existing read (not a new read pass); when non-null, insert a `action='delete'` journal row for it right there, inline. Because every one of these INSERTs is itself a write inside the outer transaction, a rollback (an injected write error, a validation failure, a mid-transaction crash) leaves ZERO committed journal rows — a staged file with no matching committed row is therefore, by construction, provably NOT part of a restore that actually happened, which is exactly B-10's fix: a candidate Merge that stages a ready photo and then rolls back or never begins can never have its bytes finalized onto a live photo, because nothing durable ever attests the restore committed.

AFTER commit, read back every journal row via `listJournalEntriesCore` (a plain, non-mutexed read). For each `action='finalize'` row: re-verify the target is still live with a fresh read as a final current-target check (see Task 3 for the exact per-kind query this mirrors), then call the existing, unmodified `persistMaster(restorePendingFileUri, entry.canonical_relative_path)`; on success, delete BOTH the staged file (`deleteRestorePending`) and the journal row (`deleteJournalEntryCore`); on a thrown `persistMaster` (a live-process failure — disk full mid-swap, permission revoked), catch it per-photo — do not abort or roll back the other photos or the committed restore — log the technical cause privately (Logger, no content), add that photo to a `photosNeedingAttention` count in the typed apply result, and DELIBERATELY LEAVE both the staged file and its journal row in place (delete on success only; Task 3's sweep retries the rest). For each `action='delete'` row: best-effort call the existing `deletePhoto(entry.canonical_relative_path)`; on success, delete the journal row; on a caught failure, leave the journal row in place, log privately, and add the photo to a new `photoCleanupPending` count in the typed apply result — the same non-blocking, never-restore-failure pattern as `photosNeedingAttention` — for Task 3's sweep to retry. An OS-level kill at ANY point in this AFTER-commit window — before this draining loop even starts, or mid-loop — leaves exactly the rows the transaction actually committed sitting in `restore_photo_journal`, which Task 3's launch-sweep step finds and finishes (or GCs) on the very next foreground launch; no window is silent, unrecoverable, or capable of finalizing a photo the restore never actually applied.

(2) Schedule rebuild is a separate, independently-failable post-commit step from photo finalization; do not couple their error handling. If notification/digest schedule reconciliation throws after commit, catch it, log privately, and set a `schedulesNeedResync: true` flag on the typed apply result (surfaced as "reminders will resync automatically" — non-blocking, never "restore failed"). This is honest, not aspirational: `reconcileSchedule` (`src/services/notifications/notification-schedule.ts`, registered via 11-10) and `reconcileDigestSchedule` (`src/services/notifications/digest-schedule.ts`, registered via 15-03) are BOTH already-registered launch-sweep hooks that fully re-read current DB state and reconcile OS schedules on every subsequent real foreground launch — no new marker/table is needed for this case; add a regression that, after simulating a rebuild-call failure, invokes those existing reconcile functions and asserts the mismatch clears.

(3) sunContactUid vs. tombstones. Resolve sunContactUid from the completed contact UID map immediately before the settings-apply call, but validate it against the reconciliation module's reference-to-tombstoned-parent fallback (17-04) rather than a bare map lookup: if the named contact UID lost to a winning tombstone in this same restore pass, resolve to null/self regardless of whether the incoming or the locally-retained settings row is the one being written — a locally-retained row can dangle too if this restore is what tombstones its sun contact. Apply the one validated app-settings singleton through Plan 17-12's updateAppSettingsCore: in Merge compare modifiedAt through the reusable D-02/D-03 policy and retain a newer local row (after the sunContactUid re-validation above); in Replace-all write the incoming allowlisted non-secret row. Never write the serialized source integer id and never invoke SecureStore.

(4) Recency without modified_at inflation (HIGH, Pass-2 audit B-6, source-verified): `recomputeLastContactCore` (`src/db/recency-dao.ts:157-174`) is transaction-only by contract AND, in the same statement, sets `contacts.modified_at` to whatever `now` argument it is given — it has no way to recompute `last_contact` without also touching `modified_at`. Calling it after commit violates the "transaction-only" contract; calling it with the actual restore wall-clock time would overwrite every touched contact's carefully reconciled `modified_at` with "now," making every restored contact look artificially newer than it really is — the next Merge would then wrongly retain the restored (inflated-timestamp) scalar values over a genuinely newer external edit, reversing D-03. The fix needs no new DAO function: call the EXISTING `recomputeLastContactCore(exec, contactId, reconciledModifiedAt)` inside the outer transaction, immediately after that contact's interaction actions are applied, passing the contact row's own FINAL reconciled `modified_at` (the value Merge/Replace-all just decided to write for that contact) as the `now` argument — not `Date.now()`/restore time. This one call therefore does two correct things at once: it recomputes `last_contact` via the existing MAX-over-current-rows subquery, and it rewrites `modified_at` back to the exact value it already holds (a byte-identical no-op write). Do this for every contact whose interaction set changed during this restore (inserted, updated, or deleted interaction rows), not only ones that received a scalar contact update. Add a dedicated "next Merge after restore" regression: restore a backup, capture each touched contact's `modified_at`, run a second reconciliation pass against a synthetic incoming file with an older `modified_at` for the same contacts, and assert the older incoming values LOSE (proving restore never silently advanced the timestamp).

(5) data_revision bump ownership for restore (HIGH, Pass-2 audit A-05, source-verified): 17-06's coalescing rule (leaf SQL cores never bump; the owning outer/public operation bumps exactly once) extends to restore-apply as its own outer operation — 17-06's DAO-chokepoint threading does not cover restore, since restore composes non-mutexed cores directly rather than calling the public DAO wrappers 17-06 instruments. Call `bumpDataRevisionCore(exec)` (from `src/db/data-revision-dao.ts`, 17-02) exactly once, inside restore-apply's own outer transaction, immediately before commit — never inside a per-row loop, never via Plan 17-12's separate backup-bookkeeping core (which must never bump, by that core's own self-inflation guard). Bump for: any Merge that performed at least one insert/update/delete/tombstone action (a true no-op Merge — nothing to write — does not bump), and unconditionally for any Replace-all (it is definitionally a full-dataset change). Add a pre-restore-snapshot-then-Replace-all regression: record `last_backup_data_revision` from a snapshot, Replace-all restore a different file, and assert `readDataRevision(exec)` now exceeds the recorded snapshot value — proving backup health correctly reports changed/stale until the next verified automatic backup, per the audit's exact failure scenario.

(6) Replace-all as a tombstone-aware, FK-safe complete reset (HIGH, Pass-2 audit A-06 + B-7, source-verified): a naive Replace-all that clears tables or relies on FK CASCADE is not deletion evidence (a later Merge-restore of another old file would resurrect anything Replace-all silently discarded) and can violate FK ordering or strand `app_settings.sun_contact_id`/`field_history`. The audit's exact scope: Replace-all only needs to tombstone rows it is REMOVING — every current mergeable row whose uid is ABSENT from the validated incoming file's corresponding entity array; a current row whose uid IS present in the incoming file is an ordinary UID match (delete-then-reinsert-with-same-uid or in-place update, executor's choice) and must NOT be tombstoned, since it is not being removed from the reconciled dataset — spuriously tombstoning a surviving uid would pollute the indefinitely-retained tombstone table with false deletion evidence. Implement ONE reset sequence inside Replace-all's outer transaction, mirroring purge-dao.ts's explicit-fan-out discipline (`src/db/purge-dao.ts:169-212`) but for the WHOLE dataset rather than one contact: first, unconditionally null `app_settings.sun_contact_id` (it will be re-set correctly in the settings-apply step after contact replacement, per (3) above); second, per mergeable entity type, diff current local uids against the incoming file's uids for that type; for every current uid ABSENT from the incoming file, capture it and insert its tombstone via 17-02's tombstones-dao.ts core BEFORE deleting that row — dependents before parents: interactions, events, fuel, custom_field_values, contact_links, THEN contacts/custom_field_defs; retain and insert any tombstones present in the incoming file too (do not drop them). Independent of that uid diff, ALWAYS perform an explicit `field_history` purge for the whole dataset (it has no FK to contacts and never cascades, and carries no uid/tombstone identity of its own — an advertised full replace that skips it leaves the prior local dataset's private field values on disk regardless of which contacts survive). `categories` has no delete writer today (17-04) and is not expected to differ in practice; leave its rows in place and update in place by uid rather than deleting/tombstoning them. Third, insert or update the incoming validated rows parents-before-children (profile/custom_field_defs/contacts, then their children) by uid, building the UID-to-local-id map as each parent is resolved. Fourth, apply the incoming allowlisted non-secret settings row LAST, only once the final contact UID-to-id map is complete, so sunContactUid resolves against real local ids. Test every FK edge named above, an old `field_history` row (must be gone after Replace-all regardless of which contacts survived), a contact uid present in both local and incoming (must NOT receive a tombstone), a contact uid absent from incoming (must receive a tombstone), and a contact that was the sun before Replace-all (must not dangle).

(7) Forced pre-restore snapshot, never the ordinary due/changed path (HIGH, Pass-2 audit B-14, source-verified): D-14 requires Replace-all to "write and verify a fresh automatic snapshot when a destination is configured" before any data loss — reusing 17-06's ordinary automatic-backup call is unsafe because that path is explicitly GATED on cadence-elapsed AND data-changed (17-06 Task 2/3); a destination can be configured yet cadence not due or `data_revision` unchanged, in which case the ordinary path returns "not due"/"unchanged" and writes NOTHING, and Replace-all would then destroy the only current copy with no fresh safety net — directly contradicting D-14. Add `createVerifiedPreRestoreSnapshot` to `src/services/backup/backup-service.ts`: it reuses the SAME low-level generate/write/verify primitives 17-06's automatic path already built (export-manifest.ts's manifest, saf-storage.ts's write-then-reread verification) but skips the due/changed gate entirely and skips retention pruning — it ALWAYS attempts a distinct timestamped write plus local-and-SAF verification when a destination is configured, and returns a typed failure (never a soft "degraded health" result) on any write or verify failure at a configured destination. Replace-all calls this BEFORE opening its own outer transaction and BLOCKS (writes nothing, deletes nothing) if it fails. Only when NO destination is configured at all does Replace-all proceed after the existing explicit "local data will be lost, no automatic destination" disclosure (D-14/D-15) — a configured-but-currently-failing destination is never treated as the no-destination case. Test: not-due state (must still snapshot), unchanged-data state (must still snapshot), a failed write (Replace-all blocked), a failed verification (Replace-all blocked), and the correct ordering (snapshot fully verified strictly before any Replace-all delete begins).

(8) Schedule-hook isolation is a launch-sweep-registry fix, not a restore fix (MEDIUM, Pass-2 audit B-15, source-verified): item (2) above already makes restore-apply's OWN post-commit schedule-rebuild call fail non-blockingly, but that does not make the "self-heals on next launch" claim true — `runLaunchSweep` (`src/services/launch-sweep.ts:71-90`) iterates its hook array with no per-hook try/catch (its own header says so: it "does not isolate hook-internal failures itself"), so if `registerNotificationScheduleSweep`'s hook throws, `registerDigestScheduleSweep`'s hook — registered strictly after it in `App.tsx` (`registerNotificationScheduleSweep` at line ~153, `registerDigestScheduleSweep` at line ~162) — never runs that pass, on ANY foreground launch, not just after a restore. Fix this at its source, following the EXACT pattern `field-sweep.ts` already uses and documents for this identical class of problem (`src/services/field-sweep.ts:33-37`, wrapping each composed unit in its own Logger-guarded try/catch so one failure never wedges the rest of the pass): wrap `reconcileSchedule(getExec())` inside `registerNotificationScheduleSweep`'s hook body in its own try/catch, logging privately via `Logger.error` on failure and returning normally (never rethrowing) so the hook loop always proceeds to the next hook; do the identical wrap for `reconcileDigestSchedule(getExec())` inside `registerDigestScheduleSweep`. Do not modify `launch-sweep.ts` itself — this mirrors the codebase's already-established convention of isolating failures at the hook-registration site, not inside the shared runner. Add a dedicated regression (in `notification-schedule.test.ts`, alongside its existing `registerNotificationScheduleSweep` describe block) that registers both real sweep hooks against `runLaunchSweep`, injects a throw from `reconcileSchedule`, and asserts `reconcileDigestSchedule` still ran on that SAME pass; then, on a second `runLaunchSweep()` call with the injected failure removed, assert `reconcileSchedule` itself succeeds — proving both reconcilers independently self-heal.

Query/assert the final pair count and uniqueness against the full contact × definition matrix before commit, EXCLUDING any pair `blocked` by (5)'s parent-survival check from the completeness expectation (a blocked pair is neither inserted nor counted as a surviving pair). Never assign contacts.last_contact from payload; apply recency per (4) above — bind `recomputeLastContactCore`'s `now` to the contact's own final reconciled `modified_at`, never restore wall-clock time. Before Replace-all, call the FORCED `createVerifiedPreRestoreSnapshot` per (7) above when a destination is configured — never the ordinary due/changed-gated automatic path — otherwise return the required no-destination disclosure state. Do not import field_history or serialized schedule/path state.</action>
  <verify><automated>npm test -- src/backup/restore-apply.test.ts src/services/photos/photo-storage.test.ts src/services/backup/backup-service.test.ts src/services/notifications/notification-schedule.test.ts src/services/notifications/digest-schedule.test.ts src/db/migrations/008-restore-photo-journal.test.ts src/db/restore-photo-journal-dao.test.ts src/db/photo-relative-path.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tests prove one BEGIN for apply, total rollback on injected error, UID parent mapping, deletion-tie behavior, derived recency, and post-commit schedule rebuild calls; a multi-contact/multi-definition regression proves every final pair exists exactly once and both incoming and seeded NULL clears remain durable, with any parent-survival-blocked pair correctly excluded from that count. A photo-staging test proves a pre-commit staging failure aborts with zero DB writes and zero canonical photo files touched; a separate test proves `stageRestorePending` writes to `_restore_pending/` via stage-tmp-then-rename, guarded by the SEPARATE `assertSafeRestorePendingRelative` grammar (never to an OS cache path, never to `avatars/<name>.tmp`, and never accepted by the unbroadened `assertSafeRelative` — Pass-3 B-9). A test proves a row whose action is `retain` or `blocked`, or a losing incoming update, is never staged, journaled, or finalized, and that a newer local photo's canonical bytes are byte-identical before and after a restore that also carries an older backup photo for the same row (Pass-3 B-11). A test proves a Merge/Replace-all that rolls back after staging succeeds (an injected write error) commits ZERO `restore_photo_journal` rows, and that a launch-sweep pass afterward therefore GCs — never finalizes — the orphaned staged file left behind (Pass-3 B-10). A test proves a winning explicit photo clear, a tombstoned row, and a Replace-all-removed row each produce a committed `action='delete'` journal row for their PRIOR canonical path, sourced only from the local DB's own current state and never from the backup file's serialized content (Pass-3 B-11, B-13). A test proves a post-commit `persistMaster` failure for one finalize row does not roll back the committed restore, appears in the typed result's non-blocking `photosNeedingAttention` count, and leaves both the staged file and its journal row undeleted; an equivalent test proves a post-commit `deletePhoto` failure for one delete row appears in a `photoCleanupPending` count and leaves its journal row undeleted. A schedule-rebuild-failure test proves the restore reports committed-with-resync-pending and that the existing 11-10/15-03 reconcilers clear the injected mismatch on a simulated next run. Settings regressions prove cross-device ID translation, null/self sun, tombstone-wins-over-either-settings-row sunContactUid resolution, Merge LWW retain/apply behavior, Replace-all replacement, orphan rejection with no write, and exclusion of API keys, passphrases, and all encryption/key material. A recency regression proves a restored contact's modified_at is byte-identical to its reconciled value immediately after restore, and that a subsequent Merge against an older incoming row for that contact correctly loses (B-6). A data_revision regression proves exactly one bump per non-no-op Merge and per Replace-all, and that a pre-restore-snapshot-then-Replace-all sequence leaves data_revision strictly greater than the recorded last_backup_data_revision (A-05). A Replace-all reset regression proves every FK edge is respected, old field_history is purged, a prior sun contact does not dangle, and every displaced row receives a tombstone before its DELETE (A-06/B-7). A forced-snapshot regression proves createVerifiedPreRestoreSnapshot always writes/verifies regardless of due/changed state, blocks Replace-all on write or verify failure, and only the true no-destination case proceeds without one (B-14). A launch-sweep regression proves an injected reconcileSchedule throw does not prevent reconcileDigestSchedule from running on the same pass, and both self-heal on a later pass (B-15). A pair-uid/col_name incompatibility test proves the whole restore rejects with zero writes when 17-04's reconciliation reports a non-empty incompatibilities list (B-3/B-5).</acceptance_criteria>
  <done>Both restore modes have a complete transactional engine behind a validated preview boundary, with photo bytes staged durably (never to OS cache, never via a broadened canonical-path grammar) and finalized ONLY when a committed `restore_photo_journal` row proves both that the restore that staged them actually committed and that their owning row actually won reconciliation, with superseded/orphaned canonical masters (winning clears, tombstoned rows, Replace-all removals) durably GC'd via the same journal rather than left on disk, schedule-rebuild failures self-healing via existing (and now individually isolated) launch-sweep machinery, UID-safe non-secret singleton settings restoration that can never dangle a sun reference to a tombstoned contact, a data_revision counter that always reflects a restore's real changes, a tombstone-aware FK-safe Replace-all reset that never destroys deletion evidence, a forced verified pre-restore snapshot that can never be silently skipped, and recency recomputation that never inflates a restored contact's reconciled timestamp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register a launch-sweep step that drains the committed pending-photo journal — finalizing, deleting, or garbage-collecting each entry</name>
  <read_first>src/services/photos/photo-reconcile-sweep.ts (the pattern to mirror — one idempotent Logger-guarded hook, FS-only, no DB); src/services/launch-sweep.ts registerSweepHook; App.tsx's ready-gated effect and its existing one-shot registration guards (photoReconcileRegistered et al.); src/services/photos/photo-storage.ts restorePendingRelPath/listRestorePendingPhotos/deleteRestorePending/deletePhoto (Task 2); src/db/restore-photo-journal-dao.ts listJournalEntriesCore/deleteJournalEntryCore (Task 2, Pass-3 audit B-10); src/db/field-types.ts FieldType ("photo"); 17-AUDIT-B-FINDINGS.md findings 10, 12, 13 (B-10, B-12, B-13)</read_first>
  <files>src/services/photos/restore-photo-finalize-sweep.ts, src/services/photos/restore-photo-finalize-sweep.test.ts, App.tsx</files>
  <behavior>
    - On every real foreground launch, after migration readiness, the sweep reads ALL restore_photo_journal rows (never by re-parsing a staged filename) and drains each by its typed `action`.
    - A `finalize` row whose typed target identity re-resolves to a still-live row (contact_uid → a current contacts.id; value_uid → a current custom_field_values row whose joined definition.type is still 'photo' and whose joined contact_id matches the row's recorded contact_uid; the profile kind needs no lookup, since the singleton is never purged) is finalized via the existing, unmodified persistMaster into entry.canonical_relative_path, then BOTH the staged file and the journal row are deleted on success. If the staged file no longer exists (a prior pass's persistMaster succeeded but its own cleanup was interrupted before this pass), the row is treated as already-finalized and only the journal row is removed — persistMaster is not retried against a missing source.
    - A `finalize` row whose typed target identity no longer resolves to any live row is garbage-collected: delete the staged file (if present) and the journal row, without ever calling persistMaster.
    - A `delete` row is drained by a best-effort deletePhoto(entry.canonical_relative_path) — idempotent by that helper's own existing contract — followed by removing the journal row on success; a caught deletion failure leaves the journal row for the next pass and is logged privately, never crashing the sweep.
    - Separately from the journal-driven pass, the sweep lists avatars/_restore_pending/ and deletes any orphaned .stage-tmp file unconditionally (always incomplete by definition), AND deletes any fully-staged (non .stage-tmp) file that has NO matching journal row at all — proof that the restore which staged it never actually committed (Pass-3 audit B-10) — without ever finalizing it.
    - A persistMaster or deletePhoto failure inside the sweep is caught per-entry, logged, and the entry is left for the next launch's retry — never a crash, never data loss.
    - The hook is idempotent across repeated launches: a fully-drained restore leaves zero rows in restore_photo_journal and zero entries under _restore_pending/, so subsequent launches do no work.
    - The hook never runs from module import or a headless notification/widget path — registration happens inside App.tsx's ready-gated effect, following the identical one-shot-guard pattern already used for the photo/field/notification/digest/widget sweeps.
  </behavior>
  <action>Review finding (HIGH, cycle-2, sharpened by Pass-3 audit B-10/B-12 — this task is the other half of Task 2's durable-staging-plus-journal fix, and is what makes the "only finalize what actually committed" guarantee real rather than aspirational): a committed `restore_photo_journal` row is only a genuine recovery mechanism if something actually drains it after a kill, and a staged file with no journal row must actually be garbage-collected rather than optimistically finalized. Implement `registerRestorePhotoFinalizeSweep(getExecutor: () => SqlExecutor): void` in a new file mirroring `photo-reconcile-sweep.ts`'s shape (one `registerSweepHook` call, Logger-scoped try/catch around the whole pass so a thrown error never wedges the launch sweep, matching the existing `registerFieldSweep(getExecutor)`/`registerNotificationScheduleSweep(getExecutor)` executor-taking convention, since this hook needs DB reads to resolve journal-row liveness).

Journal-driven pass: call `listJournalEntriesCore(exec)`. For each `action='finalize'` row, re-resolve liveness with a FRESH read as a final current-target check (B-12): `contact` kind — `SELECT id FROM contacts WHERE uid = ?` bound to `contact_uid`; `customField` kind — a join query resolving `value_uid` to its current `custom_field_values` row, its parent `custom_field_defs.type`, and its current `contact_id`'s uid, requiring `type === 'photo'` AND the resolved contact uid to still equal the row's recorded `contact_uid` before treating it as live; `profile` kind — always live (the singleton profile row is never purged). If live, resolve `canonicalRelativeName` directly from the journal row's own `canonical_relative_path` column (never by re-deriving or re-parsing a filename) and call `persistMaster(pendingFileUri, canonicalRelativeName)`; on success, `deleteRestorePending` the staged file and `deleteJournalEntryCore` the row. If the staged file is already missing (`listRestorePendingPhotos()` does not list it), skip the `persistMaster` call and just `deleteJournalEntryCore` — an interrupted PRIOR success, not a failure. If `persistMaster` throws, catch it, log privately, and leave both the file and the row for the next pass. If NOT live, `deleteRestorePending` the staged file (if present) and `deleteJournalEntryCore` the row, WITHOUT ever calling `persistMaster` — this is the concrete case a deleted/retyped-away-from-photo custom field or a purged contact must resolve to, closing B-12's "no safe target proof" gap. For each `action='delete'` row, call `deletePhoto(entry.canonical_relative_path)` (already best-effort/idempotent per its existing contract), then `deleteJournalEntryCore` on success; leave the row for retry and log privately if `deletePhoto` throws.

FS-driven orphan pass (independent of the journal-driven pass above, and it must run regardless of whether the journal-driven pass found any rows): call `listRestorePendingPhotos()`. Delete every `.stage-tmp` entry unconditionally via `deleteRestorePending`. For every remaining (fully-staged) entry, check whether ANY journal row's `relative_path` matches it (build this from the SAME `listJournalEntriesCore` result already fetched above — no second DB read); if none matches, `deleteRestorePending` it as an orphan — this is B-10's concrete guarantee that a staged file from a rolled-back or never-committed restore transaction is garbage-collected, never finalized, since by definition no committed journal row will ever reference it.

Register the hook in `App.tsx`'s existing `ready`-gated effect, adding one more one-shot guard flag (e.g. `restorePhotoFinalizeSweepRegistered`) alongside the existing `photoReconcileRegistered`/`fieldSweepRegistered`/etc., called with `getExecutor` BEFORE `installSweepTrigger` fires its cold-start pass — identical ordering/reasoning to every other DB-backed sweep hook already there. This closes the loop: any kill after Task 2's pre-commit staging or post-commit journaling — whether before finalize/delete starts, mid-`persistMaster`/`deletePhoto`, or after a caught live-process failure, or even before the restore transaction itself ever committed — self-heals or safely GCs within one foreground launch, indefinitely, with zero manual intervention, zero silent data loss, and zero risk of finalizing bytes from a restore that never actually applied. Note the residual, accepted limitation this task does NOT attempt to close: the fresh liveness read immediately before `persistMaster`/`deletePhoto` narrows, but cannot fully eliminate, a sub-second race against a concurrent in-app photo edit on the exact same target — `persistMaster` has no FS-level lock for ANY caller today (restore included), so full serialization would require a new cross-subsystem primitive out of proportion to this edge case; the final current-target check is the scoped mitigation Pass-3 audit B-12 calls for.</action>
  <verify><automated>npm test -- src/services/photos/restore-photo-finalize-sweep.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>A `finalize` journal row whose target (contact, profile, or a still-photo-typed live custom field value) is still live is finalized into its canonical avatars/ location and both the staged file and journal row are removed on a simulated sweep pass; the same row with an already-missing staged file (simulating an interrupted prior success) is cleaned up WITHOUT a retried persistMaster call; a `finalize` row whose target no longer resolves (deleted contact, deleted value, or a field retyped away from `photo`) is garbage-collected — staged file and journal row removed — WITHOUT ever calling persistMaster; a `delete` row removes its canonical file (or no-ops safely if already gone) and its journal row; a staged file under `_restore_pending/` with NO matching journal row (simulating a rolled-back or never-committed restore transaction) is deleted as an orphan and is proven, by a dedicated test, never to reach persistMaster; a `.stage-tmp` orphan is always deleted regardless of age; a thrown persistMaster or deletePhoto during the sweep does not crash the launch sweep and leaves its entry for retry; App.tsx registers the hook exactly once, after migration readiness, before the cold-start sweep fires, and never reachable from a headless path.</acceptance_criteria>
  <done>A killed or rolled-back restore's pending photo work is never permanent and never wrongly applied — the next real foreground launch finishes every committed journal entry, deletes every superseded canonical master the restore determined, or safely garbage-collects anything that never actually committed, with no new background timer and no change to the existing tmp/bak swap the launch sweep already reconciles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected backup → parser/decryptor | User-selected and potentially malicious file becomes in-memory model. |
| validated model → SQLite/photos/OS schedules | Restore transforms portable state into durable local and platform state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-18 | Tampering | backup parser/graph | high | mitigate | Full schema, UID, parent, pair, version, and decrypt validation before preview/write. |
| T-17-19 | Tampering | restore transaction and singleton settings | high | mitigate | One outer transaction, non-mutexed cores, UID translation, LWW/Replace policy tests, and rollback coverage. |
| T-17-20 | Tampering | photo file restoration | high | mitigate | Restore bytes via generated safe photo-storage names; never trust serialized paths. |
| T-17-21 | Denial of service | replace safety copy | medium | mitigate | A FORCED createVerifiedPreRestoreSnapshot always writes and verifies a distinct snapshot when a destination is configured — never the ordinary due/changed-gated automatic path, which can report not-due/unchanged and write nothing; only a genuinely absent destination proceeds after the explicit data-loss disclosure (Pass-2 audit B-14). |
| T-17-36 | Tampering | post-commit photo/schedule durability | high | mitigate | Two-phase photo staging (validate+write durably to avatars/_restore_pending/ pre-commit, finalize to canonical avatars/ path post-commit) converts the named failure scenario into a pre-commit whole-restore abort in the common case; residual per-photo/schedule failures are caught individually, never rolled back, and explicitly surfaced in the typed result rather than only logged. |
| T-17-37 | Tampering | app_settings sunContactUid resurrection | high | mitigate | sunContactUid is re-validated against the final post-tombstone surviving-contact set for whichever settings row wins, resolving to null/self rather than a dangling reference. |
| T-17-40 | Tampering | OS-level kill during post-commit photo finalization | high | mitigate | Staging bytes durably under avatars/_restore_pending/ (never an OS-clearable cache path, never a name the existing tmp/bak sweep would delete as an orphan) plus a committed restore_photo_journal row proving the staging restore actually committed, plus a dedicated launch-sweep step (Task 3) that finalizes, deletes, or garbage-collects every journal entry and every unjournaled staged file on the next foreground launch — a kill or rollback at any point, before or after commit, is recoverable/safe indefinitely, not only while the process stays alive (Pass-3 audit B-10, B-12). |
| T-17-54 | Tampering | restore-pending path grammar vs. the canonical SAFE_RELATIVE guard | high | mitigate | A separate, narrowly-scoped assertSafeRestorePendingRelative allowlist governs only the avatars/_restore_pending/ subdirectory; the generic canonical SAFE_RELATIVE grammar is never broadened, so a stored DB value can never traverse into the staging area and a staging helper can never write outside it (Pass-3 audit B-9). |
| T-17-55 | Tampering | stale/losing incoming photo overwriting a newer local photo | high | mitigate | Staging, journaling, and finalization are gated on the row's FINAL reconciliation action (insert or winning update only); a retained-local, blocked, or losing-incoming row's photo is never staged or finalized, so an older backup photo can never overwrite a newer local one (Pass-3 audit B-11). |
| T-17-56 | Repudiation | orphaned/superseded canonical photo masters left on disk | medium | mitigate | Every winning explicit clear, tombstoned row, and Replace-all-removed row produces a committed delete journal entry, read from local DB state before the row changes and never from serialized backup content, drained durably (with retry-on-failure reporting) by the same post-commit/launch-sweep machinery as finalization — closing the exposure a reused non-AUTOINCREMENT integer id could otherwise create (Pass-3 audit B-13). |
| T-17-49 | Tampering | orphaned-child insertion / pair-col_name collision | high | mitigate | Restore-apply consumes 17-04's blocked actions (never inserts/updates a child whose local or incoming parent did not survive) and its incompatibilities list (a non-empty list aborts the whole restore with zero writes before the transaction opens), rather than reimplementing parent-survival or collision logic (Pass-2 audit B-2 apply half, B-3, B-5). |
| T-17-50 | Repudiation | restored contact modified_at inflation | high | mitigate | recomputeLastContactCore's now argument is bound to each contact's own final reconciled modified_at, never restore wall-clock time, so recency recomputation never masks the true D-03 timestamp a later Merge must compare against (Pass-2 audit B-6). |
| T-17-51 | Repudiation | restore omitted from data_revision / backup health | high | mitigate | Restore-apply bumps data_revision exactly once inside its own outer transaction for every non-no-op Merge and every Replace-all, so automatic-backup health correctly reflects restored changes instead of silently reporting "unchanged" (Pass-2 audit A-05). |
| T-17-52 | Tampering | Replace-all deletion evidence and FK-safe reset | high | mitigate | Every row Replace-all hard-deletes is tombstoned in the same outer transaction before its DELETE; one specified FK-safe sequence (sun_contact_id reset, dependents-before-parents including explicit field_history purge, parents-before-children insert, settings applied last) replaces reliance on FK cascade or a raw table clear (Pass-2 audit A-06, B-7). |
| T-17-53 | Tampering | cross-hook launch-sweep failure isolation | medium | mitigate | registerNotificationScheduleSweep and registerDigestScheduleSweep each catch their own reconcile failure with a Logger-guarded try/catch, mirroring field-sweep.ts's established convention, so one throwing hook can never block a later-registered hook on the same or a subsequent foreground launch (Pass-2 audit B-15). |
| T-17-SC | Tampering | package installs | high | mitigate | Dependencies were approved by 17-01. |
</threat_model>

<verification>Run restore/schema suites, tsc, then full Vitest suite after wave completion.</verification>
<success_criteria>Nothing can reach a restore write until the entire backup is safe to apply, committed restores rebuild derived local state correctly, a process kill or rollback at any point, before or after commit, self-heals or safely garbage-collects via the launch sweep and the committed pending-photo journal with no permanent dangling photo reference and no photo ever finalized from a restore that did not actually commit, a stale/losing incoming photo never overwrites a newer local one, every superseded canonical master is durably removed rather than left on disk, restore never inflates a contact's reconciled modified_at or omits data_revision, Replace-all is a tombstone-aware FK-safe reset preceded by a forced verified pre-restore snapshot, and no single launch-sweep hook failure can block another hook's self-healing.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-08-SUMMARY.md` when done</output>
