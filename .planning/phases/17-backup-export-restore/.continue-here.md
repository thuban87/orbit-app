---
context: phase
phase: 17-backup-export-restore
task: 2
total_tasks: 3
status: blocked_on_decision
last_updated: 2026-08-25T22:50:00.000Z
---

# Phase 17 Plan 07 — Task 2 checkpoint

Plan 17-07 is paused at its deliberate blocking-human decision gate. Task 1 is complete and committed:

- `9c333d4 test(17-07): add failing encryption envelope vectors`
- `61d2817 feat(17-07): add parameterized backup encryption envelope`

Task 1 verification passed:

- `npm test -- src/services/backup/encryption.test.ts` — 5 tests passed
- `npx tsc --noEmit` — passed

## Required decision

Do not select a shipping encrypted-backup profile without current physical-Pixel release-build evidence. The profile must include format version, PBKDF2 identifier and iteration count, derived-key length, salt length, IV length, AES-256-GCM cipher, measured duration, and release build identity.

## Verified infrastructure status

- Pixel 6 Pro (`1A071FDEE002BU`, `raven`) is attached.
- `droid` is reachable.
- Its available release APK predates the Task 1 timing seam and cannot supply valid evidence.
- The required route is documented in `docs/runbooks/desktop-build-pipeline.md`: sync source to `droid`, build a current release APK, install it on the Pixel, and measure the timing seam.
- The current app has no user-facing timing-seam invocation or existing measured profile. Do not fabricate either result.

## Resume instructions

1. Obtain user authorization for the release-build benchmark route/harness and a resulting profile approval, or have the user provide an already measured full profile with release-build identity.
2. Resume Plan 17-07 at Task 2. Preserve the committed Task 1 work; do not re-run the plan from scratch.
3. Complete Task 3 only after the profile is measured and approved, then run the plan verification and create `17-07-SUMMARY.md`.

## Repository state

- Branch: `main`, local commits only, no remote push.
- Preserve unrelated untracked `.planning/milestone.lock`.
