---
context: phase
phase: 17-backup-export-restore
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-08-26T00:15:56.237Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Plan 17-08 Task 1 has a committed RED test and uncommitted partial implementation. Inspect and preserve the exact files below; continue from them and do not redispatch the plan as a blank working tree.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Re-dispatching an interrupted task from scratch | The terra-high executor was deliberately paused by the user after committing Task 1's failing tests and beginning implementation, so a blind restart could overwrite useful partial work or skip the RED-to-GREEN trail. | blocking | Before edits, inspect `git status`, commit `d8d0472`, and the two partial files; keep the test commit as Task 1's RED baseline and finish the existing work in place. |

<current_state>

Phase 17 executes serially on `main` (`workflow.use_worktrees=false`, `parallelization=false`). Plans 17-01 through 17-07 and 17-12 are complete. Plan 17-08 began in Wave 6 and was paused at Task 1 only to preserve remaining account usage for a handoff.

Task 1 RED commit exists: `d8d0472 test(17-08): add failing restore preview coverage`.

Uncommitted partial Task 1 implementation — inspect and preserve:

- `src/backup/backup-schema.ts`
- `src/services/backup/backup-service.ts`

No Task 1 GREEN commit, no `17-08-SUMMARY.md`, no async jobs, and no running watchers remain. `.planning/milestone.lock` is an unrelated untracked file and must remain untouched.
</current_state>

<completed_work>

- **17-05 complete:** validated plaintext snapshot export and verified manual sharing. Commits `73105bc`, `3321a78`, docs `d84f490`.
- **17-06 complete:** monotonic export revisions, verified foreground SAF snapshots, retention, and health. Implementation commits `f95e570`, `beb1366`, `c27a110`, `42d7feb`, `9f15dcb`; full suite passed: 114 files / 1392 tests.
- **17-07 complete:** native encrypted envelope, typed SecureStore cache, and fail-closed lifecycle. Commits `9c333d4`, `61d2817`, `24f9144`, `9fed72a`, `1535874`, `32d06ae`, `7fca75f`.
- **17-08 Task 1 RED:** committed focused restore-preview coverage in `d8d0472`; partial schema/preview implementation began but is not yet verified or committed.
</completed_work>

<remaining_work>

1. Finish Plan 17-08 Task 1's validated, aggregate-only restore preview from the existing partial files. Run its focused tests and TypeScript check; commit GREEN atomically.
2. Execute Plan 17-08 Task 2 (single transactional Merge/Replace-all engine, migration 008 photo journal, forced pre-restore safety snapshot) and Task 3 (foreground photo-journal drain), then create and commit `17-08-SUMMARY.md`.
3. Continue Phase 17 in serial wave order: 17-09, 17-10, then 17-11; run required full gates and phase closeout workflows.
</remaining_work>

<decisions_made>

- **Encryption profile:** owner approved v1 PBKDF2-HMAC-SHA256 with **600,000 iterations**, 32-byte key, 16-byte salt, 12-byte IV, AES-256-GCM, and 8 MiB ciphertext cap. It measured a 53 ms median on a Pixel 6 Pro release APK.
- **Benchmark evidence:** fresh release APK from `droid`, SHA-256 `87af511c210aeae56df76df9b4300f18e216dbdcd3a2ad39db947b98625baffb`; Pixel `1A071FDEE002BU` (`raven`); harness commit `24f9144`. This is recorded in `17-07-SUMMARY.md`.
- **Execution style:** user requested terra-high executor subagents. Continue with `gpt-5.6-terra` at high reasoning effort when dispatching executors.
- **Device/build authority:** user approved the documented multi-computer build/install procedure and local ADB use. Use `docs/runbooks/desktop-build-pipeline.md` exactly; never push to GitHub or improvise remote paths/cleanup.
</decisions_made>

<blockers>

- No current technical or human blocker. The pause is solely to transfer the work to another account.
</blockers>

## Required Reading (in order)

1. `.planning/phases/17-backup-export-restore/.continue-here.md` — this exact pause state and mandatory preservation rule.
2. `.planning/HANDOFF.json` — machine-readable state.
3. `.planning/phases/17-backup-export-restore/17-08-PLAN.md` — resume at Task 1.
4. `git show d8d0472` plus `src/backup/backup-schema.ts` and `src/services/backup/backup-service.ts` — RED baseline and partial implementation to finish.
5. `.planning/phases/17-backup-export-restore/17-07-SUMMARY.md` — approved encryption contract and release evidence used by preview/decrypt paths.
6. `docs/runbooks/desktop-build-pipeline.md` — mandatory before later device work.

## Infrastructure State

- Branch: `main`, ahead of `origin/main`; no remote push occurred.
- No async jobs, servers, or watchers are active.
- Current Pixel 6 Pro release benchmark APK is installed; the Pixel and `droid` were reachable during 17-07.
- The repo uses sequential execution without worktrees.
- Preserve untracked `.planning/milestone.lock`.

<next_action>

Start with: inspect `git status`, `d8d0472`, and the two partial Task 1 files. Then continue the existing RED-to-GREEN Task 1 implementation without discarding any partial work.
</next_action>
