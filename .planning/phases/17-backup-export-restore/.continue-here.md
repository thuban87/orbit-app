---
context: phase
phase: 17-backup-export-restore
plan: 11
task: 2
total_tasks: 2
status: awaiting_human_verify
last_updated: 2026-08-26T22:02:00-05:00
---

# Plan 17-11: Owner UAT Checkpoint

> The prior Plan 17-08 tracer note below is retained only as historical context.
> Plan 17-11 Task 1 is now complete; resume at this owner-operated Task 2 gate.

## Task 1 automated evidence

- Added `src/backup/phase-17-integration.test.ts`: a populated v6 fixture runs
  through the registered migration chain to imported `TARGET_VERSION`, exports
  via the production manifest builder, and restores through both Merge and
  Replace-all. It verifies NULL normalized values, custom-photo bytes, a prior
  hard-delete tombstone, a non-self sun contact, and each contact resolving to
  the destination database's local category ID by category UID.
- The same suite guards a 50-contact, 180 KiB-photo export under the documented
  5-second snapshot budget and scans production `src/db/*.ts` delete writers
  against the closed tombstone vocabulary (with explicit non-mergeable journal
  and field-history exemptions).
- `npm test -- --run` — passed: 125 files, 1452 tests.
- `npx tsc --noEmit` — passed.
- `npm run check:colors` — passed.
- `npx expo config --type public --json` — passed. `react-native-quick-crypto`
  occurs exactly once in public config; no new Phase 17 config-plugin or
  prebuild requirement was found, so the desktop build runbook remains correct.
- The envelope default matches the prior release-build Pixel record: version 1,
  AES-256-GCM, PBKDF2-HMAC-SHA256/600,000, 32-byte key, 16-byte salt, 12-byte
  IV, and 8 MiB cap. The released Pixel 6 Pro benchmark remains 53 ms median;
  node timing was not substituted for device evidence.

## Awaiting owner UAT (Task 2)

1. Follow `docs/runbooks/desktop-build-pipeline.md` to build a fresh release APK
   on `droid` (`npm ci`, `set "CI=1" & npx expo prebuild --platform android --clean --no-install`,
   then `gradlew.bat assembleRelease --console=plain`) and install it on the
   physical Pixel using the currently reported device serial.
2. Use only disposable contacts/photos and complete every Plan 17-11 Task 2
   scenario: landing states, SAF persistence and provider loss, plaintext share,
   encrypted round trips and error responses, Merge/Replace behavior, restored
   photos, schedule rebuild, and 320dp/accessibility checks.
3. Record only aggregate pass/fail evidence and the first failing scenario; do
   not include relationship data, filenames, passphrases, or screenshots with
   private content.

## Historical Plan 17-08 tracer note

## Current State

Plan 17-08 Task 1 is complete and committed. The prior partial implementation was
preserved and completed in place; no existing work was reset or re-dispatched.

- RED baseline: `d8d0472 test(17-08): add failing restore preview coverage`
- GREEN commit: `95d294d feat(17-08): validate restore preview`
- Unrelated `.planning/milestone.lock` remains untracked and untouched.
- No async jobs, watchers, or interrupted agents are active.

## What Was Built

- A write-free `loadBackupForPreview` boundary that decrypts (when necessary),
  parses, forward-migrates, and wholly validates the selected backup before it
  returns only aggregate preview metadata.
- Strict non-secret singleton settings validation, valid tombstone identities,
  same-file parent-survivor checks, category-reference validation, and base64
  photo-byte validation.
- Typed, content-free preview failures for wrong passphrase, damaged/incomplete
  content, and newer-app backups.

## Verification

- `npm test -- src/backup/backup-schema.test.ts src/services/backup/backup-service.test.ts` — 19 passed
- `npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts` — 7 passed
- `npx tsc --noEmit` — passed
- `git diff --check` — passed

## Awaiting Human Verification

This is the Plan 17-08 tracer checkpoint. Review the focused implementation if
desired, then reply `approved` to begin Task 2: the single-transaction
Merge/Replace-all engine, migration 008 photo journal, pre-restore safety
snapshot, and post-commit recovery work.
