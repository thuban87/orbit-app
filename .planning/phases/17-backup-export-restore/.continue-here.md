# Continue Here — Phase 17 / Plan 17-11 / Task 2

**Paused:** 2026-08-26T10:39:42.358Z
**Status:** Automated gates are complete. Owner-led Pixel release UAT is still open.

## Read first

- `.planning/HANDOFF.json`
- `17-11-PLAN.md`
- `17-VALIDATION.md`
- `docs/runbooks/desktop-build-pipeline.md`
- `.planning/debug/manual-export-failure.md`
- `.planning/debug/replace-apply-failure.md`

## Exact current state

- Task 1 is complete in `f9f2273`.
- `a49b925` fixes a genuine Replace-restore bug: after confirmation, the restore screen reread a module-scoped preview cache that could expire. The fix captures the validated candidate before confirmation. Focused checks and the full local verifier passed when it landed.
- `a24bcb5` added temporary, redacted pre-restore snapshot diagnostics. They were intentionally removed in `612ea7c`; do not bring them back without a controlled reproduction.
- A prior UAT fixture changed from `Preview expired` to a generic restore failure. Source tracing shows that generic error is from creation of the pre-restore safety snapshot, before restore apply. It is not evidence of a Merge/Replace transaction failure.
- A direct fresh control was eventually run: wiped data, made a no-photo contact, encryption off, then **Backup → Export now**. It passed and opened Android's native share chooser. Therefore the prior export failure is fixture-specific or photo-related, not a baseline export defect.

## Build blocker

The last attempt to build a clean release from the current source produced **BUILD FAILED** after about 1m42s while configuring Android CMake for `armeabi-v7a`. The visible output ended at `cmake.exe` exit 1; no clean APK was produced and the first actionable CMake error was not captured.

Use the documented droid pipeline with a persistent SSH PTY/session so build output is retained. Do not speculate about app code or create logging first. Resolve or capture the actual host CMake error, then rebuild the clean APK.

The installed Pixel APK is an earlier diagnostic release. It must not be treated as the final current-source artifact. The Pixel app data was wiped for the fresh baseline control and currently contains only a minimal no-photo contact.

## Next execution sequence

1. Keep **one continuous Terra-high owner** on this thread. Do not subdivide reproduce, test, fix, and build among separate agents.
2. Confirm the current local state around `a49b925` and `612ea7c`; run only relevant local checks while preparing the build.
3. Build a clean release through `docs/runbooks/desktop-build-pipeline.md`, retaining full remote CMake output if it fails again.
4. **After the APK build succeeds, pause and ask the user whether sufficient usage remains before APK install or more UAT.** This is an explicit user instruction.
5. Once approved, install the clean release and run fresh disposable-fixture UAT. Start with plaintext export, then add a controlled photo fixture before claiming a photo/export defect. Configure SAF storage and a known disposable passphrase before encrypted round-trip checks.

## Remaining official UAT coverage

Nothing below is fully closed by the partial checks already performed:

- Actual Merge and Replace apply/results.
- Wrong-passphrase, damaged-file, and newer-schema handling.
- Passphrase change, re-encrypt/future-only choice, and encryption disable.
- Encrypted automatic/manual round trips and retention.
- Folder loss and recovery.
- Restored-photo freshness.
- Recency plus notification/digest rebuild.
- 320dp layout, accessible controls, and progress behavior.

Partial evidence already recorded: release launch, Backup entry/health state, SAF selection and persistence, foreground health refresh, plaintext/encrypted share chooser, encrypted preview aggregate-only metadata, and configured-destination disclosure. These reduce setup work only; they do not close the scenarios above.

## Device / safety notes

- Expected Pixel serial: `1A071FDEE002BU`; always query live ADB state rather than hardcode commands.
- The user permits wiping this test app's data, disposable contacts/photos/passphrases, and a local Documents SAF folder.
- Do not push or create a PR.
- Preserve unrelated untracked files: `.planning/milestone.lock`, `orbit-home.png`, and `ui.xml`. Preserve `.planning/debug/replace-apply-failure.md`; it records the unsuccessful diagnostic branch, but do not stage it in the handoff commit.

## User-critical constraints

The prior agent consumed excessive usage through fragmented debugging and premature instrumentation. Avoid that pattern: one owner per issue, direct control first, targeted source changes only when proven, and no repeat build/UAT cycles on a stale diagnostic fixture.
