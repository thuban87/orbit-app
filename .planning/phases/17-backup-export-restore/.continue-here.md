---
context: phase
phase: 17-backup-export-restore
task: 2
total_tasks: 3
status: blocked_on_decision
last_updated: 2026-08-25T23:15:00.000Z
---

# Phase 17 Plan 07 — Task 2 checkpoint

Plan 17-07 is paused at its deliberate blocking-human decision gate. Task 1 is complete and committed:

- `9c333d4 test(17-07): add failing encryption envelope vectors`
- `61d2817 feat(17-07): add parameterized backup encryption envelope`

Task 1 verification passed:

- `npm test -- src/services/backup/encryption.test.ts` — 5 tests passed
- `npx tsc --noEmit` — passed

## Required decision

Do not select a shipping encrypted-backup profile without current physical-Pixel release-build evidence. The profile must include format version, PBKDF2 identifier and iteration count, derived-key length, salt length, IV length, AES-256-GCM cipher, measured duration, and release build identity.

## Benchmark-build evidence

- Pixel 6 Pro (`1A071FDEE002BU`, `raven`) is attached.
- `droid` is reachable.
- Its available release APK predates the Task 1 timing seam and cannot supply valid evidence.
- The release-only, build-flag-gated timing harness is committed in `24f9144`.
- The documented droid route completed: `npm ci`, quoted-CI prebuild, then `gradlew.bat assembleRelease --console=plain`; RNQC 1.1.7 compiled successfully after a non-destructive retry for a stale Nitro CMake-log deletion failure.
- Fresh artifact: `17-07-benchmark-release.apk`, 191,456,750 bytes, SHA-256 `87af511c210aeae56df76df9b4300f18e216dbdcd3a2ad39db947b98625baffb`.
- The executor re-confirmed the attached Pixel serial immediately before the successful install.
- The harness measures non-shipping PBKDF2-HMAC-SHA256 candidates (300k, 600k, and 900k iterations; 32-byte key; 16-byte salt; 12-byte IV; AES-256-GCM) with five samples after warm-up.
- The phone is presently locked and the Android notification shade obscures Orbit. Do not select a profile until the on-device samples are captured.

## Resume instructions

1. Ask the user to unlock the attached Pixel and reply `unlocked`.
2. Resume Plan 17-07 at Task 2, relaunch the installed release APK, and capture the samples. Preserve committed Task 1 work; do not re-run the plan from scratch.
3. Present the measured evidence for user approval before selecting the shipping profile or continuing to Task 3.

## Repository state

- Branch: `main`, local commits only, no remote push.
- Preserve unrelated untracked `.planning/milestone.lock`.
