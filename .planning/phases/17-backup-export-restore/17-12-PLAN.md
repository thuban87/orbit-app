---
phase: 17-backup-export-restore
plan: 12
type: tdd
wave: 2
depends_on: [17-02]
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts]
autonomous: true
requirements: [BKP-01, BKP-02, BKP-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Backup cadence, retention, folder metadata, automatic-success metadata, encryption-enabled state, and nudge state are durable non-secret settings with positive integer validation."
    - "The automatic-success change marker is last_backup_data_revision, an integer snapshot of app_settings.data_revision (17-02) — never a timestamp — so Plan 17-06's changed-since-last-backup check cannot miss a same-second edit (cycle-2 review HIGH)."
    - "The singleton settings row exposes its modified_at timestamp for Merge LWW and a transaction-composable update core for Restore."
    - "A settings backup can express the sun relationship as a contact UID at the wire boundary, never as a portable device-local integer ID."
    - "TWO DISJOINT allowlists exist, not one 'all non-secret fields' snapshot (cycle-2 review A-07 HIGH): a PORTABLE settings-wire snapshot (existing AI provider/model/custom-endpoint/prompt-template fields, general notification/orrery settings, and the D-09 cadence/retention day counts) and a LOCAL-ONLY backup-bookkeeping type (data_revision, last_backup_data_revision, SAF folder/diagnostic/health/automatic-success fields, encryption_enabled, nudge-dismissal marker) that share no field name and are never both read by the same accessor."
    - "updateAppSettingsCore preserves the existing ai_ack_custom-resets-on-endpoint-change behavior identically to updateAppSettings, and NEVER calls bumpDataRevisionCore itself — bump ownership belongs to the calling outer transaction (17-06's updateAppSettings wrapper, or 17-08's restore transaction), never this core."
    - "A separate, narrowly-typed backup-bookkeeping SQL core (e.g. recordAutomaticBackupHealthCore) persists automatic-backup success/health fields; its patch type is structurally disjoint from both AppSettingsPatch and the portable snapshot type, and it never calls bumpDataRevisionCore (self-inflation guard)."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/app-settings-dao.ts"
  key_links:
    - "Plan 17-05 reads getPortableSettingsSnapshot's PORTABLE allowlist only (never getAppSettings or any bookkeeping field) and translates sunContactId to sunContactUid for export."
    - "Plan 17-08 uses the non-mutexed settings update core under its single restore transaction after resolving sunContactUid, and bumps data_revision itself once per its own A-05 restore contract — updateAppSettingsCore never bumps."
    - "Plan 17-06 Task 3 persists automatic-backup success/health exclusively through the new backup-bookkeeping core, never through updateAppSettingsCore or updateAppSettings, so recording a successful backup can never itself bump data_revision."
    - "Plan 17-09 imports the exported day-bound validator (assertBackupDays) rather than re-declaring the 1..3650 literals."
---

<objective>
Create the non-secret singleton settings persistence and composable DAO seam required by export, automatic backup, and UID-safe restore.

Purpose: keep backup configuration and app state durable without turning a device-local contact ID or any secret into portable data.
Output: migration-007 settings columns, validated settings mapping, read snapshot, and non-mutexed update core.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@src/db/migrations/002-app-settings.ts
@src/db/migrations/007-tombstones.ts
@src/db/app-settings-dao.ts
@src/db/app-settings-dao.test.ts
@src/db/transaction.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one safe automatic-backup settings path in migration 007</name>
  <read_first>src/db/migrations/002-app-settings.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/007-tombstones.test.ts; 17-CONTEXT.md D-06 and D-08 through D-10</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts</files>
  <behavior>
    - A v6 database upgraded through migration 007 has defaults of one backup day and seven retention days, plus nullable folder/health metadata and durable encryption/nudge state.
    - The settings schema persists only non-secret configuration and health fields; it has no passphrase, API key, raw encryption key, KDF secret, or key-material column.
    - Existing migration-007 tombstone upgrade behavior remains valid after the additive settings change.
  </behavior>
  <action>Start RED in the existing migration007 suite. Add D-08–D-10's backup cadence, retention, SAF diagnostic metadata, encryption-enabled state, and nudge-dismissal marker as additive app_settings columns with safe defaults. Review finding (HIGH, cycle-2, source-verified): the "verified-success/change marker" this task adds MUST be `last_backup_data_revision INTEGER NOT NULL DEFAULT 0` — an integer snapshot of the same-migration `app_settings.data_revision` counter 17-02 adds, NOT a timestamp. A `MAX(modified_at)`-style timestamp marker cannot detect a same-second edit (all local timestamps are second-granularity), so Plan 17-06 compares `readDataRevision(exec) !== storedRevision` (inequality on a monotonic integer) rather than a wall-clock `>` comparison. Add this column to the same additive ALTER set as the other backup-configuration columns; it depends only on `data_revision` already existing from 17-02's earlier task in this same migration file. Keep the cached passphrase exclusively in SecureStore and do not add API-key, key-material, or passphrase storage to SQLite. Preserve a lost folder's configuration for diagnosis rather than clearing it. Review finding (HIGH, cycle-2, A-07): every column this task adds is LOCAL-ONLY backup mechanism/health state, never portable settings content — the SAF folder URI/permission/diagnostic metadata, the automatic-success/health fields, `last_backup_data_revision`, `encryption_enabled` (coupled to this device's SecureStore-cached passphrase, not portable app content), and the nudge-dismissal marker (D-18, a local dashboard-UI dismissal, not relationship data) all belong to the LOCAL-ONLY bookkeeping allowlist Task 2 defines below — none of them may ever reach the portable settings snapshot Plan 17-05 serializes. `data_revision` itself (17-02) is likewise local-only. Task 2 owns drawing this exact line; this task only needs to add the columns with safe defaults.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Migration 007 upgrades both tombstones and the singleton's safe backup state without weakening the existing v6-to-v7 proof or persisting a secret.</acceptance_criteria>
  <done>A migrated installation has durable default backup configuration and no SQLite secret cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split the settings snapshot into two disjoint allowlists — a portable wire projection and a local-only bookkeeping core</name>
  <read_first>src/db/app-settings-dao.ts getAppSettings/updateAppSettings; src/db/app-settings-dao.test.ts; src/db/transaction.ts; 17-CONTEXT.md D-02, D-06, and D-09; 17-AUDIT-A-FINDINGS.md finding A-07</read_first>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <behavior>
    - Reads expose every non-secret app_settings field plus modified_at, including the nullable local sunContactId, while a backup-only snapshot has no passphrase, API key, raw key, or KDF material.
    - TWO DISJOINT explicit allowlists exist, not one "all non-secret fields" projection (A-07): a PORTABLE settings-wire projection/restore-patch type, and a separate LOCAL-ONLY backup-bookkeeping/config type. No field name appears in both.
    - The portable settings-wire snapshot explicitly includes the existing AI provider/model/custom-endpoint/prompt-template settings (aiProvider, aiModel, aiCustomEndpoint, aiCustomModel, aiPromptTemplate) and the notification/orrery settings already generically writable, plus the D-09 backup cadence/retention day counts (ordinary user-configurable settings, not backup-bookkeeping) — none of these are secrets and a narrowly-scoped "backup settings" projection must not silently drop valid pre-existing user configuration.
    - The portable settings-wire snapshot's TYPE structurally EXCLUDES `data_revision`, `last_backup_data_revision`, every SAF-folder URI/permission/diagnostic/automatic-success/health field, `encryption_enabled`, and the nudge-dismissal marker — none of these keys exist on the portable type, so a caller that tries to read one off it is a compile error, not a runtime omission to remember.
    - Cadence and retention accept whole values 1..3650 before any transaction begins, validated by one exported, named function; malformed patches leave the id=1 row unchanged.
    - updateAppSettingsCore performs the same allowlisted, bound update without opening a transaction and NEVER calls bumpDataRevisionCore itself (bump ownership belongs to the calling outer transaction — Plan 17-06's `updateAppSettings` bumps once after a successful core call; Plan 17-08's restore transaction bumps once itself per its own A-05 restore contract, never via this core), while updateAppSettings remains the one-transaction public wrapper; it preserves the existing ai_ack_custom-resets-when-aiCustomEndpoint-changes behavior identically to today's updateAppSettings.
    - A SEPARATE, narrowly-typed backup-bookkeeping SQL core exists for recording automatic-backup health/success (including `last_backup_data_revision`, the SAF folder/diagnostic fields, and automatic-success timestamps): its patch type is structurally disjoint from both the portable snapshot type and `AppSettingsPatch`, so it cannot accept any portable settings field even if a caller tries; it never calls `bumpDataRevisionCore` (self-inflation guard — recording a successful backup must never itself advance data_revision).
  </behavior>
  <action>Write DAO regressions before implementation. Extend the row mapping, typed non-secret snapshot read, COLUMN_OF allowlist, and pre-transaction validators for the new migration fields. Review finding (MEDIUM, source-verified): `app_settings` already carries non-secret AI provider/model/custom-endpoint/prompt-template configuration (`src/db/app-settings-dao.ts` — `aiProvider`, `aiModel`, `aiCustomEndpoint`, `aiCustomModel`, `aiPromptTemplate`, all already excluded from secrecy since keys live only in `ai-key-store.ts`'s SecureStore). BKP-01 requires "non-secret settings" in the export, so the PORTABLE settings-wire snapshot this task exposes MUST include these five existing fields plus the general notification/orrery settings and the D-09 cadence/retention day counts, not only the newly-added backup-bookkeeping columns — an overly narrow "backup settings" projection would silently lose valid AI configuration on restore.

Review finding (HIGH, cycle-2, A-07 — the plan-level correction this task owns): the plan previously described one "backup-facing non-secret snapshot" that would have exported `data_revision`, `last_backup_data_revision`, the SAF folder URI/diagnostic/health/automatic-success fields, and possibly `encryption_enabled`. Restoring an old `data_revision`/`last_backup_data_revision` marker can make changed-state detection lie; restoring a foreign/revoked SAF URI can overwrite the current device's destination and health; restoring another device's `encryption_enabled` flag without its matching SecureStore-cached passphrase reproduces the fail-open ambiguity Session B's encryption findings warn about. Fix this by defining and exporting TWO DISJOINT types from this file: (1) `PortableSettingsSnapshot` — exactly the fields named in Behavior's portable bullet above, read by a new `getPortableSettingsSnapshot(exec)` that projects ONLY that allowlist (never the full `AppSettings` shape) — Plan 17-05 consumes this, never `getAppSettings`, when building the export manifest; sunContactId stays a raw nullable local integer ON THIS TYPE (Plan 17-05 is the layer that resolves it to `sunContactUid` and Plan 17-08 the layer that resolves it back — this DAO never does the translation itself, matching the existing design). (2) The backup-bookkeeping fields (`data_revision`, `last_backup_data_revision`, SAF folder/diagnostic/health/automatic-success fields, `encryption_enabled`, nudge-dismissal marker) are read only through the existing general accessor(s) this phase's other plans already use for UI/policy purposes (17-06's automatic policy, 17-09/17-10's settings screens) — they are NEVER added to `PortableSettingsSnapshot` and NEVER read by Plan 17-05's export path.

Export a single named day-bound validator (e.g. `assertBackupDays`) alongside `assertHour`/`assertToggle`/`assertSunContactId` so Plan 17-09's UI can import and reuse the identical 1..3650 bound rather than re-declaring the literals. Factor validation and the exact-one-row bound UPDATE into an exported non-mutexed `updateAppSettingsCore`, then retain `updateAppSettings` as its transaction-owning, now-bumping wrapper (Plan 17-06 Task 1 adds that bump) — `updateAppSettingsCore` MUST reproduce the existing C3-H3b behavior verbatim (reading the current `ai_custom_endpoint` and appending `ai_ack_custom = 0` to the SET clause when the patch changes it to a different value), since Plan 17-08's Restore will call this core directly and a core/wrapper split that drops this special-case would let restore and normal settings edits diverge. `updateAppSettingsCore` itself never calls `bumpDataRevisionCore` — see Behavior.

Add the SEPARATE backup-bookkeeping core Plan 17-06 Task 3 will call to persist automatic-backup success/health (a concrete name, e.g. `recordAutomaticBackupHealthCore(exec, patch)`): its `patch` parameter type is a fresh interface naming ONLY the backup-bookkeeping columns (`lastBackupDataRevision`, the automatic-success timestamp, SAF folder/diagnostic-health fields) — it shares no key with `AppSettingsPatch` or `PortableSettingsSnapshot`, so a caller cannot pass a portable settings field even by mistake (the SQL binds only the named columns from this narrow type, never spreads or serializes the whole object). It is non-mutexed (assumes an already-open transaction, matching the core/wrapper convention this DAO already uses) and NEVER calls `bumpDataRevisionCore` — a self-inflation guard, since bumping on the write that records "we just backed up N" would immediately invalidate that same snapshot's freshness on the next due-cycle check.

Include modified_at in the portable snapshot so Plan 17-08 can apply D-02's LWW policy. Prove all secret exclusions, the two allowlists' disjointness (a TypeScript compile check plus a runtime assertion that no bookkeeping key ever appears in a value returned by `getPortableSettingsSnapshot`), the validator/no-write behavior, the AI-settings inclusion, the ai_ack_custom reset parity between `updateAppSettings` and `updateAppSettingsCore`, and that neither `updateAppSettingsCore` nor `recordAutomaticBackupHealthCore` ever calls `bumpDataRevisionCore`, in node:sqlite tests.</action>
  <verify><automated>npm test -- src/db/app-settings-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Settings callers can read and transactionally compose an allowlisted non-secret singleton with its timestamp, including existing AI provider/model/endpoint configuration, while invalid day values and secret-shaped fields never reach SQLite; a test proves `updateAppSettingsCore` resets ai_ack_custom on an endpoint change exactly like the existing `updateAppSettings`; the portable settings type and the backup-bookkeeping type share no field name, proven by both a compile-time type check and a runtime test that the portable snapshot's serialized keys never include a bookkeeping field name; neither `updateAppSettingsCore` nor the new bookkeeping core ever bumps `data_revision`.</acceptance_criteria>
  <done>The settings DAO is ready for UID-safe export and single-transaction restore without nested mutex acquisition, restore can never regress a user's AI configuration or the endpoint-change acknowledgement-reset behavior, and a backup can never leak or restore this device's local-only backup mechanism state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| backup settings UI/service → SQLite | Untrusted configuration becomes durable singleton application state. |
| singleton settings → backup wire model | Device-local settings are prepared for portable export and later restore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-31 | Tampering | app_settings backup fields | medium | mitigate | Validate day bounds before a transaction; use allowlisted columns, bound values, and an exact-one-row guard. |
| T-17-32 | Information disclosure | backup settings snapshot | high | mitigate | Two disjoint explicit allowlists (portable settings-wire snapshot vs local-only backup-bookkeeping type, cycle-2 review A-07) — never one "all non-secret fields" projection. The portable type structurally excludes data_revision, last_backup_data_revision, SAF folder/diagnostic/health/automatic-success fields, encryption_enabled, and the nudge marker; SecureStore secrets and key material have no SQLite or snapshot representation at all. |
| T-17-33 | Tampering | restore settings composition | high | mitigate | Export a non-mutexed core for the one outer restore transaction, retain the public transaction wrapper, and preserve the ai_ack_custom endpoint-change reset in both; the core never bumps data_revision itself (bump ownership stays with the calling outer transaction). |
| T-17-41 | Repudiation | backup-bookkeeping self-inflation | high | mitigate | A dedicated, narrowly-typed backup-bookkeeping SQL core (structurally disjoint from AppSettingsPatch and the portable snapshot type) persists automatic-success/health fields and never calls bumpDataRevisionCore, so recording a successful backup can never itself mark data as changed on the next due-cycle check (cycle-2 review A-09). |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>Run the focused migration/settings DAO suites and TypeScript compilation before dependent export or restore plans.</verification>
<success_criteria>Backup settings are durable and non-secret, and the singleton has the exact snapshot/core seams required for UID-aware export and atomic Restore.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-12-SUMMARY.md` when done</output>
