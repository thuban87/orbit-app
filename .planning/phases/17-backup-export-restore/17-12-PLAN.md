---
phase: 17-backup-export-restore
plan: 12
type: tdd
wave: 2
depends_on: [17-02]
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts]
autonomous: true
requirements: [BKP-01, BKP-02, BKP-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Backup cadence, retention, folder metadata, automatic-success metadata, encryption-enabled state, and nudge state are durable non-secret settings with positive integer validation."
    - "The automatic-success change marker is last_backup_data_revision, an integer snapshot of app_settings.data_revision (17-02) — never a timestamp — so Plan 17-06's changed-since-last-backup check cannot miss a same-second edit (cycle-2 review HIGH)."
    - "The singleton settings row exposes its modified_at timestamp for Merge LWW and a transaction-composable update core for Restore."
    - "A settings backup can express the sun relationship as a contact UID at the wire boundary, never as a portable device-local integer ID."
    - "The backup-facing settings snapshot includes the existing non-secret AI provider/model/custom-endpoint/prompt-template fields, not only newly-added backup-configuration columns."
    - "updateAppSettingsCore preserves the existing ai_ack_custom-resets-on-endpoint-change behavior identically to updateAppSettings, so restore cannot diverge from normal settings edits."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/app-settings-dao.ts"
  key_links:
    - "Plan 17-05 reads the non-secret settings snapshot and translates sunContactId to sunContactUid for export."
    - "Plan 17-08 uses the non-mutexed settings update core under its single restore transaction after resolving sunContactUid."
    - "Plan 17-09 imports the exported day-bound validator (assertBackupDays) rather than re-declaring the 1..3650 literals."
---

<objective>
Create the non-secret singleton settings persistence and composable DAO seam required by export, automatic backup, and UID-safe restore.

Purpose: keep backup configuration and app state durable without turning a device-local contact ID or any secret into portable data.
Output: migration-007 settings columns, validated settings mapping, read snapshot, and non-mutexed update core.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@src/db/migrations/002-app-settings.ts
@src/db/migrations/007-tombstones.ts
@src/db/app-settings-dao.ts
@src/db/app-settings-dao.test.ts
@src/db/transaction.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one safe automatic-backup settings path in migration 007</name>
  <read_first>src/db/migrations/002-app-settings.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/007-tombstones.test.ts; 17-CONTEXT.md D-06 and D-08 through D-10</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts</files>
  <behavior>
    - A v6 database upgraded through migration 007 has defaults of one backup day and seven retention days, plus nullable folder/health metadata and durable encryption/nudge state.
    - The settings schema persists only non-secret configuration and health fields; it has no passphrase, API key, raw encryption key, KDF secret, or key-material column.
    - Existing migration-007 tombstone upgrade behavior remains valid after the additive settings change.
  </behavior>
  <action>Start RED in the existing migration007 suite. Add D-08–D-10's backup cadence, retention, SAF diagnostic metadata, encryption-enabled state, and nudge-dismissal marker as additive app_settings columns with safe defaults. Review finding (HIGH, cycle-2, source-verified): the "verified-success/change marker" this task adds MUST be `last_backup_data_revision INTEGER NOT NULL DEFAULT 0` — an integer snapshot of the same-migration `app_settings.data_revision` counter 17-02 adds, NOT a timestamp. A `MAX(modified_at)`-style timestamp marker cannot detect a same-second edit (all local timestamps are second-granularity), so Plan 17-06 compares `readDataRevision(exec) !== storedRevision` (inequality on a monotonic integer) rather than a wall-clock `>` comparison. Add this column to the same additive ALTER set as the other backup-configuration columns; it depends only on `data_revision` already existing from 17-02's earlier task in this same migration file. Keep the cached passphrase exclusively in SecureStore and do not add API-key, key-material, or passphrase storage to SQLite. Preserve a lost folder's configuration for diagnosis rather than clearing it.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Migration 007 upgrades both tombstones and the singleton's safe backup state without weakening the existing v6-to-v7 proof or persisting a secret.</acceptance_criteria>
  <done>A migrated installation has durable default backup configuration and no SQLite secret cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose validated non-secret settings snapshot and restore-composable core</name>
  <read_first>src/db/app-settings-dao.ts getAppSettings/updateAppSettings; src/db/app-settings-dao.test.ts; src/db/transaction.ts; 17-CONTEXT.md D-02, D-06, and D-09</read_first>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <behavior>
    - Reads expose every non-secret app_settings field plus modified_at, including the nullable local sunContactId, while a backup-only snapshot has no passphrase, API key, raw key, or KDF material.
    - The backup-facing non-secret snapshot explicitly includes the existing AI provider/model/custom-endpoint/prompt-template settings (aiProvider, aiModel, aiCustomEndpoint, aiCustomModel, aiPromptTemplate) alongside the new backup-configuration fields — none of these are secrets (credentials remain SecureStore-only via ai-key-store.ts) and a narrowly-scoped "backup settings" snapshot must not silently drop valid pre-existing user configuration.
    - Cadence and retention accept whole values 1..3650 before any transaction begins, validated by one exported, named function; malformed patches leave the id=1 row unchanged.
    - updateAppSettingsCore performs the same allowlisted, bound update without opening a transaction, while updateAppSettings remains the one-transaction public wrapper; it preserves the existing ai_ack_custom-resets-when-aiCustomEndpoint-changes behavior identically to today's updateAppSettings.
  </behavior>
  <action>Write DAO regressions before implementation. Extend the row mapping, typed non-secret snapshot read, COLUMN_OF allowlist, and pre-transaction validators for the new migration fields. Review finding (MEDIUM, source-verified): `app_settings` already carries non-secret AI provider/model/custom-endpoint/prompt-template configuration (`src/db/app-settings-dao.ts` — `aiProvider`, `aiModel`, `aiCustomEndpoint`, `aiCustomModel`, `aiPromptTemplate`, all already excluded from secrecy since keys live only in `ai-key-store.ts`'s SecureStore). BKP-01 requires "non-secret settings" in the export, so the backup-facing snapshot this plan exposes MUST include these five existing fields, not only the newly-added backup-configuration columns — an overly narrow "backup settings" projection would silently lose valid AI configuration on restore. Export a single named day-bound validator (e.g. `assertBackupDays`) alongside `assertHour`/`assertToggle`/`assertSunContactId` so Plan 17-09's UI can import and reuse the identical 1..3650 bound rather than re-declaring the literals. Factor validation and the exact-one-row bound UPDATE into an exported non-mutexed updateAppSettingsCore, then retain updateAppSettings as its transaction-owning wrapper — updateAppSettingsCore MUST reproduce the existing C3-H3b behavior verbatim (reading the current `ai_custom_endpoint` and appending `ai_ack_custom = 0` to the SET clause when the patch changes it to a different value), since Plan 17-08's Restore will call this core directly and a core/wrapper split that drops this special-case would let restore and normal settings edits diverge. Include modified_at in the backup-facing snapshot so Plan 17-08 can apply D-02's LWW policy. Keep sunContactId explicitly local-only here: Plan 17-05 must translate it to a contact UID at export and Plan 17-08 must translate the UID back after contact reconciliation. Prove all secret exclusions, the validator/no-write behavior, the AI-settings inclusion, and the ai_ack_custom reset parity between updateAppSettings and updateAppSettingsCore in node:sqlite tests.</action>
  <verify><automated>npm test -- src/db/app-settings-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Settings callers can read and transactionally compose an allowlisted non-secret singleton with its timestamp, including existing AI provider/model/endpoint configuration, while invalid day values and secret-shaped fields never reach SQLite; a test proves updateAppSettingsCore resets ai_ack_custom on an endpoint change exactly like the existing updateAppSettings.</acceptance_criteria>
  <done>The settings DAO is ready for UID-safe export and single-transaction restore without nested mutex acquisition, and restore can never regress a user's AI configuration or the endpoint-change acknowledgement-reset behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| backup settings UI/service → SQLite | Untrusted configuration becomes durable singleton application state. |
| singleton settings → backup wire model | Device-local settings are prepared for portable export and later restore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-31 | Tampering | app_settings backup fields | medium | mitigate | Validate day bounds before a transaction; use allowlisted columns, bound values, and an exact-one-row guard. |
| T-17-32 | Information disclosure | backup settings snapshot | high | mitigate | Expose only explicit non-secret fields (including existing AI provider/model/endpoint configuration); SecureStore secrets and key material have no SQLite or snapshot representation. |
| T-17-33 | Tampering | restore settings composition | high | mitigate | Export a non-mutexed core for the one outer restore transaction, retain the public transaction wrapper, and preserve the ai_ack_custom endpoint-change reset in both. |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>Run the focused migration/settings DAO suites and TypeScript compilation before dependent export or restore plans.</verification>
<success_criteria>Backup settings are durable and non-secret, and the singleton has the exact snapshot/core seams required for UID-aware export and atomic Restore.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-12-SUMMARY.md` when done</output>
