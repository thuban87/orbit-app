---
phase: 17-backup-export-restore
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts]
autonomous: false
requirements: [BKP-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A migrated database has a unique, indefinitely retained tombstone for each deleted mergeable UID."
    - "Purging an archived contact captures every mergeable child UID before its explicit deletion in the same transaction."
    - "Purging the sun contact nulls app_settings.sun_contact_id and bumps app_settings.modified_at in the same transaction, so a later Merge cannot resurrect the reference via a stale timestamp."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/tombstones-dao.ts"
  key_links:
    - "database.ts runs migration007 after migration006 before app code obtains the executor."
    - "purgeContact writes child and contact tombstones before its existing fan-out deletes."
  prohibitions:
    - "Restore evidence must not allow a deleted person to reappear from an older file. [flagged-unverified]"
---

<objective>
Establish the migration-007 deletion evidence on which every later backup operation relies.

Purpose: make permanent local deletion merge-safe before any export can represent it.
Output: tombstone schema/DAO and transactional contact-purge coverage.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/migrations/006-normalize-custom-field-values.ts
@src/db/purge-dao.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Record D-01 as the already-approved migration decision</name>
  <decision>Proceed with the tombstone migration and deletion-evidence contract exactly as D-01 already records.</decision>
  <context>D-01 is a prior owner approval in 17-CONTEXT.md, not a new schema choice. This record-only gate establishes that approval before the forward-only migration and halts if the recorded scope has changed.</context>
  <options>
    <option id="recorded-d01">
      <name>Use recorded D-01</name>
      <pros>Preserves the owner-approved migration, indefinitely retained tombstones, and complete hard-delete coverage.</pros>
      <cons>Changing the scope later requires a new schema/file-format compatibility decision.</cons>
    </option>
  </options>
  <resume-signal>Reply that D-01 remains approved as recorded, or stop execution with the changed decision.</resume-signal>
  <verify><automated>rg -q 'D-01:.*generic tombstones in migration 007' .planning/phases/17-backup-export-restore/17-CONTEXT.md</automated></verify>
  <acceptance_criteria>The execution record identifies D-01 as the prior approval before migration 007 runs.</acceptance_criteria>
  <done>The already-approved one-way migration scope is traceable, or execution is halted for a changed owner decision.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Add and prove migration 007 tombstone storage</name>
  <reversibility rating="one-way">D-01 publishes migration 007 and permanent deletion evidence; changing either after users create backups requires a data/file-format migration.</reversibility>
  <precondition>D-01's migration-007 choice is an already-recorded owner approval in 17-CONTEXT.md; halt if that decision is absent or changed.</precondition>
  <read_first>src/db/migrations/006-normalize-custom-field-values.ts; src/db/database.ts; src/db/purge-dao.ts; src/db/transaction.ts; src/db/migrations/006-normalize-custom-field-values.test.ts; 17-CONTEXT.md D-01</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts</files>
  <behavior>
    - A v6 database upgrades to user_version 7 with tombstones(entity_type, entity_uid, deleted_at) and a unique logical key.
    - Tombstone cores only accept closed entity types and bind uid/timestamp values.
    - Inserting a tombstone with an entity_type outside the closed vocabulary throws before any write.
  </behavior>
  <action>Start RED with migration tests. Implement migration007 after migration006 and raise TARGET_VERSION to 7. Define typed, non-mutexed tombstone insertion/query cores with literal entity-type allowlisting and bound values. Declare the closed `entity_type` vocabulary as an exported TS union (`TombstoneEntityType`) enumerating exactly the entities with a real hard-delete writer this phase covers — `'contact' | 'interaction' | 'event' | 'fuel' | 'contact_link' | 'custom_field_def' | 'custom_field_value'` — and enforce it with both TypeScript exhaustiveness (a `switch`/allowlist, never a bare string param) and a runtime guard in the tombstone core, since a JS caller can bypass a type-only check (review MEDIUM: "a generic string type without a closed vocabulary makes omissions and typos silently non-reconcilable"). Implement D-01's additive schema exactly and preserve its indefinitely retained unique logical evidence; no cascade is treated as deletion evidence. Do not add `category`/`profile` to the vocabulary yet — they have no hard-delete writer today (see 17-04's reconciliation-registry note); extending the vocabulary later is additive.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The migration is forward-only, existing v6 upgrades succeed, the tombstone core has unique queryable evidence by UID/type, and an out-of-vocabulary entity_type is rejected at both compile time and runtime.</acceptance_criteria>
  <done>Migration 007 supplies durable tombstone storage for every later delete path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make archived-contact purge record all fan-out tombstones and keep the sun reference merge-safe</name>
  <read_first>src/db/purge-dao.ts; src/db/purge-dao.test.ts; src/db/tombstones-dao.ts; src/db/transaction.ts; src/db/app-settings-dao.ts; src/db/migrations/003-orrery-settings.ts; 17-CONTEXT.md D-01</read_first>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/tombstones-dao.ts</files>
  <behavior>
    - Purging an archived contact creates tombstones for the contact, interactions, events, fuel, links, and normalized values before each corresponding delete.
    - field_history is removed by the existing purge but contributes no tombstone.
    - Purging the contact currently assigned as `app_settings.sun_contact_id` explicitly nulls it and bumps `app_settings.modified_at`, in the same transaction; purging any other contact leaves `app_settings` untouched.
  </behavior>
  <action>Write the purge regression first. In purgeContact, select every mergeable child UID before the current explicit fan-out (asserting the captured set includes every interaction, event, fuel, link, and custom_field_value UID for the contact, not just their counts), insert their evidence plus the contact tombstone within its existing outer transaction, then preserve post-commit cleanup. Do not rely on FK cascade evidence and retain the existing archived guard. Also add one bound `UPDATE app_settings SET sun_contact_id = NULL, modified_at = ? WHERE id = 1 AND sun_contact_id = ?` (contactId) inside the same transaction, immediately before the contacts DELETE. This closes a review HIGH finding: `sun_contact_id`'s `ON DELETE SET NULL` FK action (migration 003) fires passively on the contacts DELETE and reverts the sun to self, but does not advance `app_settings.modified_at` (verified: no trigger exists) — a later Merge-restore's newer-`modified_at`-wins settings row could otherwise silently reintroduce the reference to the now-tombstoned contact. Owning this update explicitly in purgeContact, rather than trusting the passive FK alone, keeps the singleton settings row's LWW timestamp honest.</action>
  <verify><automated>npm test -- src/db/purge-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Archived purge has one BEGIN/COMMIT boundary and all expected delete evidence is queryable, asserted per entity type (interactions/events/fuel/links/values), not just aggregate counts; field_history remains excluded; purging the current sun contact leaves `app_settings.sun_contact_id = NULL` and a freshly bumped `modified_at`.</acceptance_criteria>
  <done>One real permanent contact purge leaves complete mergeable tombstones transactionally and never leaves a stale-timestamped dangling sun reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI/service → SQLite | Backup configuration and delete requests cross into durable local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-02 | Tampering | migration007 and purgeContact | high | mitigate | Forward-only test matrix, unique tombstone key, capture-before-delete, and one transaction. |
| T-17-04 | Tampering | app_settings.sun_contact_id staleness | high | mitigate | purgeContact explicitly nulls sun_contact_id and bumps app_settings.modified_at in the same transaction it purges the sun contact, instead of relying on the passive ON DELETE SET NULL FK action, which does not advance modified_at. |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Focused Vitest migration/purge tests, TypeScript compilation, then the full suite at wave completion.
</verification>

<success_criteria>
Migration 007 makes deletion evidence durable without weakening Phase 16 value invariants.
</success_criteria>

<output>Create `.planning/phases/17-backup-export-restore/17-02-SUMMARY.md` when done</output>
