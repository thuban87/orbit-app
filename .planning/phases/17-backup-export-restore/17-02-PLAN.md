---
phase: 17-backup-export-restore
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/data-revision-dao.ts, src/db/data-revision-dao.test.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/screens/ArchivedContactsScreen.tsx]
autonomous: false
requirements: [BKP-04]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A migrated database has a unique, indefinitely retained tombstone for each deleted mergeable UID."
    - "Purging an archived contact captures every mergeable child UID before its explicit deletion in the same transaction."
    - "Purging the sun contact nulls app_settings.sun_contact_id and bumps app_settings.modified_at, using a caller-supplied now (not a DAO-internal clock), in the same transaction, so a later Merge cannot resurrect the reference via a stale or missing timestamp."
    - "A monotonic app_settings.data_revision counter exists from migration 007, and every tombstone insertion advances it by exactly one, giving Plan 17-06 a non-timestamp change signal that cannot miss a same-second mutation the way MAX(modified_at) can (cycle-2 review HIGH)."
    - "Migration 007 migrates the singleton profile row's uid to a FIXED RESERVED constant on every installation, fresh or upgraded, so UID-keyed reconciliation (Plan 17-04) does not need a D-02 identity exception for the one seeded singleton row (cycle-2 review B-4 HIGH — owner-approved fix)."
    - "Migration 007 also migrates the four seeded categories' uid columns to FIXED RESERVED constants (RESERVED_CATEGORY_UIDS, one per seeded display_order 0..3) on every installation, fresh or upgraded — extending the identical owner-approved reserved-UID pattern to the sibling category singleton-seed identity problem — so Plan 17-04's UID-keyed reconciliation resolves all four categories with no per-install identity drift and no duplicate-inserting restore (round-2 confirmation review HIGH)."
    - "Running the REAL registered migration list (migration001..migration007+, as wired in database.ts) from a fresh v0 connection through runMigrations reaches the imported TARGET_VERSION with exactly one 007 step applied after 006, the tombstones table plus its unique key, app_settings.data_revision, the reserved profile UID, the four reserved category UIDs, and every migration-007-added app_settings singleton column at its documented default — proven against the real migration chain, not only a hand-built v6-shaped fixture (cycle-2 review A-11 HIGH), and asserted against the imported TARGET_VERSION constant rather than a hardcoded literal so a later migration (e.g. Plan 17-08's migration 008) requires no edit to this assertion (round-2 confirmation review ACTIONABLE)."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/tombstones-dao.ts"
    - "src/db/data-revision-dao.ts"
    - "src/db/migrations/full-chain.test.ts"
  key_links:
    - "database.ts runs migration007 after migration006 before app code obtains the executor."
    - "purgeContact writes child and contact tombstones before its existing fan-out deletes."
    - "tombstones-dao.ts's insertion core calls bumpDataRevisionCore, so every Phase-17 delete path (17-02/17-03/17-04) advances data_revision without a separate edit."
    - "ArchivedContactsScreen.tsx supplies purgeContact's now from its existing localDateTime() import, the same clock source it already uses for restoreContact."
    - "Plan 17-04's reconciliation registry (a later pass) relies on migration 007's reserved profile UID to resolve the singleton profile row without a D-02 exception; this plan owns making that UID exist and stay fixed, pass 2 owns consuming it."
    - "Plan 17-04's reconciliation registry and Plan 17-08's restore-apply both rely on migration 007's exported RESERVED_CATEGORY_UIDS to resolve all four seeded categories without per-install identity drift, and on Plan 17-08 translating each contact's category_id against those same UIDs; this plan owns making the four UIDs exist and stay fixed (round-2 confirmation review HIGH)."
  prohibitions:
    - "Restore evidence must not allow a deleted person to reappear from an older file. [flagged-unverified]"
    - "purgeContact must not synthesize now internally — the DAO-internal-clock deviation 17-03 explicitly rejected for fuel/link deletes applies equally here. [flagged-unverified]"
    - "Migration 007 must never use IF NOT EXISTS or catch-and-ignore ALTER masking — the runner's atomic BEGIN/user_version/COMMIT is the sole idempotence boundary. [flagged-unverified]"
---

<objective>
Establish the migration-007 deletion evidence on which every later backup operation relies.

Purpose: make permanent local deletion merge-safe before any export can represent it.
Output: tombstone schema/DAO and transactional contact-purge coverage.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/migrations/006-normalize-custom-field-values.ts
@src/db/purge-dao.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Record D-01 as the already-approved migration decision</name>
  <decision>Proceed with the tombstone migration and deletion-evidence contract exactly as D-01 already records.</decision>
  <context>D-01 is a prior owner approval in 17-CONTEXT.md, not a new schema choice. This record-only gate establishes that approval before the forward-only migration and halts if the recorded scope has changed.</context>
  <options>
    <option id="recorded-d01">
      <name>Use recorded D-01</name>
      <pros>Preserves the owner-approved migration, indefinitely retained tombstones, and complete hard-delete coverage.</pros>
      <cons>Changing the scope later requires a new schema/file-format compatibility decision.</cons>
    </option>
  </options>
  <resume-signal>Reply that D-01 remains approved as recorded, or stop execution with the changed decision.</resume-signal>
  <verify><automated>rg -q 'D-01:.*generic tombstones in migration 007' .planning/phases/17-backup-export-restore/17-CONTEXT.md</automated></verify>
  <acceptance_criteria>The execution record identifies D-01 as the prior approval before migration 007 runs.</acceptance_criteria>
  <done>The already-approved one-way migration scope is traceable, or execution is halted for a changed owner decision.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Add and prove migration 007 tombstone storage and the data-revision counter</name>
  <reversibility rating="one-way">D-01 publishes migration 007 and permanent deletion evidence; changing either after users create backups requires a data/file-format migration.</reversibility>
  <precondition>D-01's migration-007 choice is an already-recorded owner approval in 17-CONTEXT.md; halt if that decision is absent or changed.</precondition>
  <read_first>src/db/migrations/006-normalize-custom-field-values.ts; src/db/migrations/001-initial.ts (CREATE_PROFILE + the id=1 seed); src/db/database.ts (the full registered migration list + TARGET_VERSION); src/db/migrations/runner.ts; src/db/purge-dao.ts; src/db/transaction.ts; src/db/migrations/006-normalize-custom-field-values.test.ts; 17-CONTEXT.md D-01; 17-AUDIT-B-FINDINGS.md finding 4 (B-4); 17-AUDIT-A-FINDINGS.md finding A-11</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/data-revision-dao.ts, src/db/data-revision-dao.test.ts</files>
  <behavior>
    - A v6 database upgrades to user_version 7 with tombstones(entity_type, entity_uid, deleted_at) and a unique logical key, plus a new app_settings.data_revision INTEGER NOT NULL DEFAULT 0 column.
    - Tombstone cores only accept closed entity types and bind uid/timestamp values.
    - Inserting a tombstone with an entity_type outside the closed vocabulary throws before any write.
    - Inserting any tombstone advances app_settings.data_revision by exactly one, in the same transaction as the tombstone row.
    - Migration 007 also rewrites the singleton profile row (id=1) so its uid equals a fixed, exported reserved constant — on EVERY installation, whether it upgrades from v6 (replacing migration 001's random per-install uid) or is created fresh via the full v0-to-v7 chain (migration 001 seeds a random uid, migration 007 immediately overwrites it in the same install's migration run).
    - Migration 007 also rewrites all four seeded `categories` rows (Family/Friends/Work/Community, display_order 0..3) so each row's uid equals one of four fixed, exported reserved constants — on EVERY installation, fresh or upgraded, by the identical unconditional-UPDATE mechanism as the profile fix above, keyed by the row's stable `display_order`.
    - Running the real registered migration array from database.ts (migration001 through migration007, via runMigrations, from a fresh v0 connection) reaches the imported TARGET_VERSION with: exactly one 007 step registered after 006; the tombstones table and its unique key; app_settings.data_revision; profile.uid equal to the reserved profile constant; all four categories' uid columns equal to their reserved category constants; and every 007-added app_settings singleton column at its documented default.
  </behavior>
  <action>Start RED with migration tests. Implement migration007 after migration006 and raise TARGET_VERSION to 7. Define typed, non-mutexed tombstone insertion/query cores with literal entity-type allowlisting and bound values. Declare the closed `entity_type` vocabulary as an exported TS union (`TombstoneEntityType`) enumerating exactly the entities with a real hard-delete writer this phase covers — `'contact' | 'interaction' | 'event' | 'fuel' | 'contact_link' | 'custom_field_def' | 'custom_field_value'` — and enforce it with both TypeScript exhaustiveness (a `switch`/allowlist, never a bare string param) and a runtime guard in the tombstone core, since a JS caller can bypass a type-only check (review MEDIUM: "a generic string type without a closed vocabulary makes omissions and typos silently non-reconcilable"). Implement D-01's additive schema exactly and preserve its indefinitely retained unique logical evidence; no cascade is treated as deletion evidence. Do not add `category`/`profile` to the vocabulary yet — they have no hard-delete writer today (see 17-04's reconciliation-registry note); extending the vocabulary later is additive.

Review finding (HIGH, cycle-2, source-verified): 17-06's automatic-backup "exportable data changed" check compares `MAX(modified_at)` scalars, but every local timestamp (`formatLocalDate`/`localDateTime`, src/utils/dates.ts, src/db/database.ts) is SECOND-granularity — an edit landing in the same wall-clock second as the stored marker is indistinguishable from "no change" and stays silently unprotected until an unrelated later-second write happens to advance the max. Per 17-CONTEXT.md's explicit fix direction, this is a non-timestamp change-signal problem, not a clock-precision problem (do not widen any timestamp format, and do not touch D-03's reconciliation same-second tie-break rule — that governs Merge conflict resolution, a separate concern from backup-eligibility detection). Add `app_settings.data_revision INTEGER NOT NULL DEFAULT 0` to this same migration007 ALTER set. Create `src/db/data-revision-dao.ts` with two exports: `bumpDataRevisionCore(exec: SqlExecutor): Promise<void>` — a non-mutexed core (assumes an already-open transaction, matching the field-ddl/purge-dao "core" convention) that runs `UPDATE app_settings SET data_revision = data_revision + 1 WHERE id = 1`; and `readDataRevision(exec: SqlExecutor): Promise<number>` — a pure read of the current value. Call `bumpDataRevisionCore(exec)` from inside the SAME tombstone-insertion core this task already defines, so every present and future tombstone-writing transaction (this plan's purgeContact, 17-03's interaction/fuel/link deletes, 17-04's permanent field-definition delete) advances `data_revision` automatically with no separate per-caller edit — closing the "proof that every exportable mutation advances it" gap structurally for the delete side. (Plan 17-06 separately threads `bumpDataRevisionCore` through the create/update DAO chokepoints for the non-delete side.) Add a regression proving a tombstone insert advances `data_revision` by exactly one and that two same-second tombstone inserts (or a same-second tombstone insert immediately following a same-second create/update elsewhere) both register as distinct, detectable revisions — this is the concrete same-second case the review flagged.

Review finding (HIGH, cycle-2, B-4, source-verified — owner-approved fix, the D-02-preserving option): `src/db/migrations/001-initial.ts`'s `CREATE_PROFILE` + seed creates exactly one `profile` row, `id INTEGER PRIMARY KEY CHECK (id = 1)`, with a RANDOM per-install `uid` (`deps.newUid()` at migration001 line ~230). Plan 17-04's UID-keyed reconciliation registry (a later pass) cannot resolve this singleton by UID across two independently-installed devices: a fresh destination's `profile.uid` never matches a source backup's `profile.uid`, so naive UID reconciliation would see the incoming row as "missing" and either violate the `id=1` uniqueness on insert or require a bespoke `id=1`-adoption exception to D-02's stable-UID identity rule (an exception 17-AUDIT-B-FINDINGS.md explicitly flags as needing owner approval). The owner has approved the D-02-preserving fix instead: migrate the singleton to a FIXED RESERVED UID on all installations, so ordinary UID reconciliation works with zero exception. Implement this in THIS migration007, not a later one — reserved UIDs are cheapest to establish before any backup file exists. Define and export a fixed constant, e.g. `export const RESERVED_PROFILE_UID = "00000000-0000-4000-8000-000000000001";` (a syntactically valid, permanently fixed v4-shaped UUID literal — never generated by `newUid()`, never re-derived) from `src/db/migrations/007-tombstones.ts`, and add `UPDATE profile SET uid = ? WHERE id = 1` (binding `RESERVED_PROFILE_UID`) to migration007's same transaction, unconditionally — this is correct whether the row already has migration 001's random uid (the v6-upgrade case) or was JUST seeded by migration001 earlier in the SAME v0-to-v7 run (the fresh-install case), since either way the UPDATE runs after CREATE_PROFILE's seed and leaves exactly the reserved value. This is a PERMANENT, forward-only migration step (mark it with the same one-way reversibility as the rest of migration007: once a device or a published backup carries the reserved UID, changing it requires a new migration and breaks compatibility with every already-created backup file) — make it idempotent in the sense that matters here: running the full migration chain on any fresh or upgraded database always converges on the SAME `profile.uid` value regardless of starting state, and prove this directly in `007-tombstones.test.ts` with both a v6-fixture upgrade test and (see the full-chain test below) a fresh v0 test, both asserting `profile.uid === RESERVED_PROFILE_UID` after migration. Do not edit `001-initial.ts` (never edit a shipped migration) — the random seed there is harmless precisely because 007 always overwrites it in the same or a later migration run, before any UID is ever exported. Note for Plan 17-04 (a later pass): its reconciliation registry may now treat `profile` as an ordinary UID-keyed singleton with a known-in-advance UID, with no D-02 exception required.

Review finding (HIGH, round-2 confirmation review, source-verified — owner-approved fix, extends the identical D-02-preserving pattern above from `profile` to `categories`): `src/db/migrations/001-initial.ts`'s `migration001.apply` seeds the four fixed categories (Family/Friends/Work/Community, `display_order` 0..3) with `deps.newUid()` in the exact same loop shape as the profile row — a RANDOM per-install uid on every install. Plan 17-04's UID-keyed reconciliation registry names `categories` as an ordinary mergeable UID-bearing table, and `contacts.category_id INTEGER REFERENCES categories(id)` (nullable) is the FK a restored contact must resolve against a destination category after Plan 17-08 translates it via the source backup's own exported `categories` array. Exactly like the pre-fix `profile` bug, a fresh destination install's four `categories.uid` values can never match a source backup's four `categories.uid` values, so naive UID reconciliation sees all four incoming categories as "missing" and inserts four duplicates — permanently, since `categories` has no delete writer today (verified: no `DELETE FROM categories` anywhere in `src/db/*.ts`). Fix this in THIS migration007, the same one that already rewrites `profile.uid`: define and export four fixed constants from `src/db/migrations/007-tombstones.ts`, e.g. `export const RESERVED_CATEGORY_UIDS: Readonly<Record<number, string>> = { 0: "00000000-0000-4000-8000-000000000101", 1: "00000000-0000-4000-8000-000000000102", 2: "00000000-0000-4000-8000-000000000103", 3: "00000000-0000-4000-8000-000000000104" };` (four syntactically valid, permanently fixed v4-shaped UUID literals keyed by the seeded `display_order` — never generated by `newUid()`, never re-derived, and distinct from `RESERVED_PROFILE_UID`). Add, in migration007's SAME transaction as the profile UPDATE, one bound `UPDATE categories SET uid = ? WHERE display_order = ?` per entry of `RESERVED_CATEGORY_UIDS` (four unconditional UPDATEs, keyed by `display_order` — the one column `migration001`'s seed guarantees is stable and unique across the four rows, unlike `name`, which a user could theoretically edit before upgrading if a future phase ever allows category renaming). This is correct whether each row still carries migration001's random uid (the v6-upgrade case) or was seeded moments earlier in the SAME v0-to-v7 run (the fresh-install case), identically to the profile fix. This is a PERMANENT, forward-only migration step under the same one-way reversibility as the rest of migration007. Do not edit `001-initial.ts`. Prove convergence directly in `007-tombstones.test.ts` with both a v6-fixture upgrade test and a fresh v0 test, both asserting all four `categories.uid` values equal their `RESERVED_CATEGORY_UIDS` entry (keyed by `display_order`) after migration, and that this holds regardless of category row insertion order. Note for Plan 17-04 (a later pass): its reconciliation registry and fixtures should use these exact four constants for `categories`, exactly mirroring how the profile fixture uses `RESERVED_PROFILE_UID`. Note for Plan 17-08 (a later pass): restore-apply resolves each contact's `category_id` (a source-local raw integer, exported as-is since `categories` is a normal exported table) to `categoryUid` by cross-referencing the SAME backup file's own exported `categories` array (id -> uid), then resolves that uid to the destination's local category id via the reconciled category id-map this migration's fixed UIDs make possible — this plan owns making the four UIDs exist and stay fixed; Plan 17-08 owns the translation.

Review finding (HIGH, cycle-2, A-11, source-verified): the audit found this task previously specified only a v6-shaped upgrade proof (a fixture built to look like a v6 database), never a proof through the REAL registered migration chain a fresh v0/v1 install actually runs. `src/db/database.ts:25-31,41,113-126` registers `migration001` through the current set and sets `TARGET_VERSION`; `src/db/migrations/runner.ts:32-68` applies every pending version in ascending order inside its own atomic per-step transaction. A v6-shaped hand-built fixture cannot prove migration007's interaction with the REAL earlier migrations (in particular, that the reserved-profile-UID and reserved-category-UID UPDATEs above correctly follow migration001's REAL seeds, and that 006's REAL normalized-value shape is what 007 builds on). Add `src/db/migrations/full-chain.test.ts`: import the actual registered migration array and the actual `TARGET_VERSION` the same way `database.ts` does (import the real modules, do not hand-roll a parallel migration list), open a fresh in-memory/temp node:sqlite connection at `user_version = 0`, run `runMigrations` with that real array up to the imported `TARGET_VERSION`, then assert: `user_version === TARGET_VERSION` (assert against the IMPORTED CONSTANT, never a hardcoded literal `7` — this is a deliberate round-2 fix: Plan 17-08 later adds migration 008 and raises `TARGET_VERSION` to 8, and asserting against the imported constant means that later change requires zero edits to this test, self-updating with any future migration); the `tombstones` table exists with its unique logical key; `app_settings.data_revision` exists and defaults to `0`; `profile.uid === RESERVED_PROFILE_UID`; all four `categories.uid` values equal their `RESERVED_CATEGORY_UIDS` entry; and every 007-added `app_settings` singleton column (this plan's + 17-12's, once both land — if 17-12 has not yet landed when this test is authored, assert only this plan's own 007 columns and leave a code comment for 17-12 to extend the same test rather than duplicating a second full-chain test) is present at its documented default. Also run the SAME assertions starting from a v6-shaped fixture (the upgrade path) so both the fresh-install and upgrade paths are proven through the real runner. Do NOT add `IF NOT EXISTS` or catch-and-ignore `ALTER` masking anywhere in migration007 to make this test pass more easily — the runner's atomic `BEGIN`/`user_version`/`COMMIT` per step is the ONLY intended idempotence boundary; a masked partial-007 would hide a real migration bug behind a green test.

Coordination note for Plan 17-08 (round-2 confirmation review ACTIONABLE — resolves the stale-assertion gap the confirmation review found): because this test asserts against the imported `TARGET_VERSION` constant rather than a literal, Plan 17-08 raising `TARGET_VERSION` to 8 when it registers migration 008 requires NO edit to this file — the assertion self-updates. Plan 17-08's own `008-restore-photo-journal.test.ts` independently proves the `restore_photo_journal` table and the exact "one 008 step after 007" shape; this shared file does not need duplicating for that.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts src/db/data-revision-dao.test.ts src/db/migrations/full-chain.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The migration is forward-only, existing v6 upgrades succeed, the tombstone core has unique queryable evidence by UID/type, an out-of-vocabulary entity_type is rejected at both compile time and runtime, a tombstone insert plus a same-second follow-up insert each advance data_revision by one (proving the counter cannot collide the way a wall-clock max can), the singleton profile row's uid equals the reserved constant and all four categories' uid values equal their reserved constants after both a fresh v0-to-v7 run and a v6-upgrade run, and the real registered migration array (via full-chain.test.ts) reaches the imported TARGET_VERSION with exactly one 007 step and every asserted artifact present at its documented default, asserted against the imported TARGET_VERSION constant rather than a hardcoded literal.</acceptance_criteria>
  <done>Migration 007 supplies durable tombstone storage, a monotonic data-revision counter for every later delete path, a fixed reserved profile UID, and four fixed reserved category UIDs — all proven through the real registered migration chain, not only a hand-built v6 fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make archived-contact purge record all fan-out tombstones, keep the sun reference merge-safe, and thread a real clock into the modified_at bump</name>
  <read_first>src/db/purge-dao.ts; src/db/purge-dao.test.ts; src/db/tombstones-dao.ts; src/db/transaction.ts; src/db/app-settings-dao.ts; src/db/migrations/003-orrery-settings.ts; src/screens/ArchivedContactsScreen.tsx (doPurge, ~line 128-163, and the existing localDateTime() import used by doRestore at ~line 114); 17-CONTEXT.md D-01</read_first>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/tombstones-dao.ts, src/screens/ArchivedContactsScreen.tsx</files>
  <behavior>
    - Purging an archived contact creates tombstones for the contact, interactions, events, fuel, links, and normalized values before each corresponding delete.
    - field_history is removed by the existing purge but contributes no tombstone.
    - Purging the contact currently assigned as `app_settings.sun_contact_id` explicitly nulls it and bumps `app_settings.modified_at` to a caller-supplied `now`, in the same transaction; purging any other contact leaves `app_settings` untouched.
    - `now` is a required field of `PurgeOptions`, and `purgeContact`'s `opts` parameter is no longer optional — a caller cannot compile without supplying a clock value, so there is no runtime path where the modified_at bump could bind an undefined timestamp.
  </behavior>
  <action>Write the purge regression first. In purgeContact, select every mergeable child UID before the current explicit fan-out (asserting the captured set includes every interaction, event, fuel, link, and custom_field_value UID for the contact, not just their counts), insert their evidence plus the contact tombstone within its existing outer transaction, then preserve post-commit cleanup. Do not rely on FK cascade evidence and retain the existing archived guard. Also add one bound `UPDATE app_settings SET sun_contact_id = NULL, modified_at = ? WHERE id = 1 AND sun_contact_id = ?` (contactId) inside the same transaction, immediately before the contacts DELETE. This closes a review HIGH finding: `sun_contact_id`'s `ON DELETE SET NULL` FK action (migration 003) fires passively on the contacts DELETE and reverts the sun to self, but does not advance `app_settings.modified_at` (verified: no trigger exists) — a later Merge-restore's newer-`modified_at`-wins settings row could otherwise silently reintroduce the reference to the now-tombstoned contact. Owning this update explicitly in purgeContact, rather than trusting the passive FK alone, keeps the singleton settings row's LWW timestamp honest.

Review finding (ACTIONABLE, cycle-2, source-verified): `purgeContact(exec, contactId, opts?)` and its `PurgeOptions` type today carry no `now`/clock parameter, and the sole production caller, `src/screens/ArchivedContactsScreen.tsx`'s `doPurge` (`await purgeContact(exec, id, {...})`), supplies none — mirroring exactly the gap 17-03 caught and fixed for `deleteFuelCore`/`removeLinkCore`. Fix this at the type level so there is no runtime fallback to get wrong: add a required `now: string` field to `PurgeOptions` (a `YYYY-MM-DD HH:MM:SS` local-wall-clock string, the codebase's caller-supplied-clock convention — never synthesize it inside the DAO, matching 17-03's explicit rejection of a DAO-internal clock) and change `purgeContact`'s third parameter from `opts?: PurgeOptions` to `opts: PurgeOptions` (no longer optional) — `onPurgeExtensions` stays optional WITHIN `PurgeOptions`, only the options bag itself and its new `now` field become mandatory. This makes an omitted clock a compile error, not a runtime possibility, at every call site. Use `opts.now` as the bound `?` value in the `sun_contact_id`/`modified_at` UPDATE above (bind unconditionally — the UPDATE's `WHERE ... AND sun_contact_id = ?` guard already makes it a no-op when the purged contact is not the current sun, so no conditional branch is needed). Update `doPurge` in `ArchivedContactsScreen.tsx` to pass `{ now: localDateTime(), onPurgeExtensions: ... }` — the screen already imports and uses `localDateTime()` for `doRestore` a few lines above, so this is a same-file, same-pattern addition, not a new dependency. `tsc --noEmit` will flag every other call site (there is currently exactly one) that needs the same update.</action>
  <verify><automated>npm test -- src/db/purge-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Archived purge has one BEGIN/COMMIT boundary and all expected delete evidence is queryable, asserted per entity type (interactions/events/fuel/links/values), not just aggregate counts; field_history remains excluded; purging the current sun contact leaves `app_settings.sun_contact_id = NULL` and `modified_at` set to the exact caller-supplied `now`; `tsc --noEmit` confirms `ArchivedContactsScreen.tsx` compiles against the widened `PurgeOptions`.</acceptance_criteria>
  <done>One real permanent contact purge leaves complete mergeable tombstones transactionally and never leaves a stale-timestamped or clock-source-less dangling sun reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI/service → SQLite | Backup configuration and delete requests cross into durable local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-02 | Tampering | migration007 and purgeContact | high | mitigate | Forward-only test matrix, unique tombstone key, capture-before-delete, and one transaction. |
| T-17-04 | Tampering | app_settings.sun_contact_id staleness | high | mitigate | purgeContact explicitly nulls sun_contact_id and bumps app_settings.modified_at to a caller-supplied now (threaded from ArchivedContactsScreen.tsx's existing localDateTime() import) in the same transaction it purges the sun contact, instead of relying on the passive ON DELETE SET NULL FK action (which does not advance modified_at) or a DAO-internal clock. |
| T-17-38 | Repudiation | automatic-backup change detection | high | mitigate | app_settings.data_revision is a monotonic counter bumped by every tombstone insertion (17-02/17-03/17-04) and every create/update DAO chokepoint (17-06), giving Plan 17-06 a non-timestamp change signal that cannot miss a same-second mutation the way MAX(modified_at) can. |
| T-17-44 | Repudiation | profile singleton UID identity | high | mitigate | Migration 007 rewrites profile.uid to a fixed reserved constant on every installation (fresh or upgraded), so Plan 17-04's UID-keyed reconciliation resolves the singleton without a D-02 exception, and a stale/mismatched per-install UID can never cause a Merge to treat the profile as a spuriously "missing" or duplicate row (cycle-2 review B-4). |
| T-17-45 | Tampering | migration chain correctness beyond the targeted fixture | medium | mitigate | A dedicated full-chain regression runs the REAL registered migration array (migration001..migration007+) via the real runner from a fresh v0 connection, asserting against the imported TARGET_VERSION constant (never a hardcoded literal, so a later migration such as Plan 17-08's 008 requires no edit here), proving the shipped entry point's complete sequence — not only a hand-built v6-shaped fixture (cycle-2 review A-11; round-2 confirmation review ACTIONABLE). No IF NOT EXISTS/catch-and-ignore masking is used; the runner's atomic per-step transaction is the sole idempotence boundary. |
| T-17-62 | Repudiation | categories singleton-seed UID identity | high | mitigate | Migration 007 rewrites all four seeded categories' uid columns to fixed RESERVED_CATEGORY_UIDS constants (keyed by display_order) on every installation, fresh or upgraded — extending the identical profile fix — so Plan 17-04's UID-keyed reconciliation resolves categories without per-install identity drift, and Plan 17-08's contacts.category_id translation has a stable cross-install target instead of restoring onto a fresh install inserting four permanent, undeletable duplicate categories (round-2 confirmation review HIGH). |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Focused Vitest migration/purge tests, TypeScript compilation, then the full suite at wave completion.
</verification>

<success_criteria>
Migration 007 makes deletion evidence durable without weakening Phase 16 value invariants.
</success_criteria>

<output>Create `.planning/phases/17-backup-export-restore/17-02-SUMMARY.md` when done</output>
