---
phase: 17-backup-export-restore
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/data-revision-dao.ts, src/db/data-revision-dao.test.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/screens/ArchivedContactsScreen.tsx]
autonomous: false
requirements: [BKP-04]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A migrated database has a unique, indefinitely retained tombstone for each deleted mergeable UID."
    - "Purging an archived contact captures every mergeable child UID before its explicit deletion in the same transaction."
    - "Purging the sun contact nulls app_settings.sun_contact_id and bumps app_settings.modified_at, using a caller-supplied now (not a DAO-internal clock), in the same transaction, so a later Merge cannot resurrect the reference via a stale or missing timestamp."
    - "A monotonic app_settings.data_revision counter exists from migration 007, and every tombstone insertion advances it by exactly one, giving Plan 17-06 a non-timestamp change signal that cannot miss a same-second mutation the way MAX(modified_at) can (cycle-2 review HIGH)."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/tombstones-dao.ts"
    - "src/db/data-revision-dao.ts"
  key_links:
    - "database.ts runs migration007 after migration006 before app code obtains the executor."
    - "purgeContact writes child and contact tombstones before its existing fan-out deletes."
    - "tombstones-dao.ts's insertion core calls bumpDataRevisionCore, so every Phase-17 delete path (17-02/17-03/17-04) advances data_revision without a separate edit."
    - "ArchivedContactsScreen.tsx supplies purgeContact's now from its existing localDateTime() import, the same clock source it already uses for restoreContact."
  prohibitions:
    - "Restore evidence must not allow a deleted person to reappear from an older file. [flagged-unverified]"
    - "purgeContact must not synthesize now internally — the DAO-internal-clock deviation 17-03 explicitly rejected for fuel/link deletes applies equally here. [flagged-unverified]"
---

<objective>
Establish the migration-007 deletion evidence on which every later backup operation relies.

Purpose: make permanent local deletion merge-safe before any export can represent it.
Output: tombstone schema/DAO and transactional contact-purge coverage.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/migrations/006-normalize-custom-field-values.ts
@src/db/purge-dao.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Record D-01 as the already-approved migration decision</name>
  <decision>Proceed with the tombstone migration and deletion-evidence contract exactly as D-01 already records.</decision>
  <context>D-01 is a prior owner approval in 17-CONTEXT.md, not a new schema choice. This record-only gate establishes that approval before the forward-only migration and halts if the recorded scope has changed.</context>
  <options>
    <option id="recorded-d01">
      <name>Use recorded D-01</name>
      <pros>Preserves the owner-approved migration, indefinitely retained tombstones, and complete hard-delete coverage.</pros>
      <cons>Changing the scope later requires a new schema/file-format compatibility decision.</cons>
    </option>
  </options>
  <resume-signal>Reply that D-01 remains approved as recorded, or stop execution with the changed decision.</resume-signal>
  <verify><automated>rg -q 'D-01:.*generic tombstones in migration 007' .planning/phases/17-backup-export-restore/17-CONTEXT.md</automated></verify>
  <acceptance_criteria>The execution record identifies D-01 as the prior approval before migration 007 runs.</acceptance_criteria>
  <done>The already-approved one-way migration scope is traceable, or execution is halted for a changed owner decision.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Add and prove migration 007 tombstone storage and the data-revision counter</name>
  <reversibility rating="one-way">D-01 publishes migration 007 and permanent deletion evidence; changing either after users create backups requires a data/file-format migration.</reversibility>
  <precondition>D-01's migration-007 choice is an already-recorded owner approval in 17-CONTEXT.md; halt if that decision is absent or changed.</precondition>
  <read_first>src/db/migrations/006-normalize-custom-field-values.ts; src/db/database.ts; src/db/purge-dao.ts; src/db/transaction.ts; src/db/migrations/006-normalize-custom-field-values.test.ts; 17-CONTEXT.md D-01</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/data-revision-dao.ts, src/db/data-revision-dao.test.ts</files>
  <behavior>
    - A v6 database upgrades to user_version 7 with tombstones(entity_type, entity_uid, deleted_at) and a unique logical key, plus a new app_settings.data_revision INTEGER NOT NULL DEFAULT 0 column.
    - Tombstone cores only accept closed entity types and bind uid/timestamp values.
    - Inserting a tombstone with an entity_type outside the closed vocabulary throws before any write.
    - Inserting any tombstone advances app_settings.data_revision by exactly one, in the same transaction as the tombstone row.
  </behavior>
  <action>Start RED with migration tests. Implement migration007 after migration006 and raise TARGET_VERSION to 7. Define typed, non-mutexed tombstone insertion/query cores with literal entity-type allowlisting and bound values. Declare the closed `entity_type` vocabulary as an exported TS union (`TombstoneEntityType`) enumerating exactly the entities with a real hard-delete writer this phase covers — `'contact' | 'interaction' | 'event' | 'fuel' | 'contact_link' | 'custom_field_def' | 'custom_field_value'` — and enforce it with both TypeScript exhaustiveness (a `switch`/allowlist, never a bare string param) and a runtime guard in the tombstone core, since a JS caller can bypass a type-only check (review MEDIUM: "a generic string type without a closed vocabulary makes omissions and typos silently non-reconcilable"). Implement D-01's additive schema exactly and preserve its indefinitely retained unique logical evidence; no cascade is treated as deletion evidence. Do not add `category`/`profile` to the vocabulary yet — they have no hard-delete writer today (see 17-04's reconciliation-registry note); extending the vocabulary later is additive.

Review finding (HIGH, cycle-2, source-verified): 17-06's automatic-backup "exportable data changed" check compares `MAX(modified_at)` scalars, but every local timestamp (`formatLocalDate`/`localDateTime`, src/utils/dates.ts, src/db/database.ts) is SECOND-granularity — an edit landing in the same wall-clock second as the stored marker is indistinguishable from "no change" and stays silently unprotected until an unrelated later-second write happens to advance the max. Per 17-CONTEXT.md's explicit fix direction, this is a non-timestamp change-signal problem, not a clock-precision problem (do not widen any timestamp format, and do not touch D-03's reconciliation same-second tie-break rule — that governs Merge conflict resolution, a separate concern from backup-eligibility detection). Add `app_settings.data_revision INTEGER NOT NULL DEFAULT 0` to this same migration007 ALTER set. Create `src/db/data-revision-dao.ts` with two exports: `bumpDataRevisionCore(exec: SqlExecutor): Promise<void>` — a non-mutexed core (assumes an already-open transaction, matching the field-ddl/purge-dao "core" convention) that runs `UPDATE app_settings SET data_revision = data_revision + 1 WHERE id = 1`; and `readDataRevision(exec: SqlExecutor): Promise<number>` — a pure read of the current value. Call `bumpDataRevisionCore(exec)` from inside the SAME tombstone-insertion core this task already defines, so every present and future tombstone-writing transaction (this plan's purgeContact, 17-03's interaction/fuel/link deletes, 17-04's permanent field-definition delete) advances `data_revision` automatically with no separate per-caller edit — closing the "proof that every exportable mutation advances it" gap structurally for the delete side. (Plan 17-06 separately threads `bumpDataRevisionCore` through the create/update DAO chokepoints for the non-delete side.) Add a regression proving a tombstone insert advances `data_revision` by exactly one and that two same-second tombstone inserts (or a same-second tombstone insert immediately following a same-second create/update elsewhere) both register as distinct, detectable revisions — this is the concrete same-second case the review flagged.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts src/db/data-revision-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The migration is forward-only, existing v6 upgrades succeed, the tombstone core has unique queryable evidence by UID/type, an out-of-vocabulary entity_type is rejected at both compile time and runtime, and a tombstone insert plus a same-second follow-up insert each advance data_revision by one (proving the counter cannot collide the way a wall-clock max can).</acceptance_criteria>
  <done>Migration 007 supplies durable tombstone storage and a monotonic data-revision counter for every later delete path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make archived-contact purge record all fan-out tombstones, keep the sun reference merge-safe, and thread a real clock into the modified_at bump</name>
  <read_first>src/db/purge-dao.ts; src/db/purge-dao.test.ts; src/db/tombstones-dao.ts; src/db/transaction.ts; src/db/app-settings-dao.ts; src/db/migrations/003-orrery-settings.ts; src/screens/ArchivedContactsScreen.tsx (doPurge, ~line 128-163, and the existing localDateTime() import used by doRestore at ~line 114); 17-CONTEXT.md D-01</read_first>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/tombstones-dao.ts, src/screens/ArchivedContactsScreen.tsx</files>
  <behavior>
    - Purging an archived contact creates tombstones for the contact, interactions, events, fuel, links, and normalized values before each corresponding delete.
    - field_history is removed by the existing purge but contributes no tombstone.
    - Purging the contact currently assigned as `app_settings.sun_contact_id` explicitly nulls it and bumps `app_settings.modified_at` to a caller-supplied `now`, in the same transaction; purging any other contact leaves `app_settings` untouched.
    - `now` is a required field of `PurgeOptions`, and `purgeContact`'s `opts` parameter is no longer optional — a caller cannot compile without supplying a clock value, so there is no runtime path where the modified_at bump could bind an undefined timestamp.
  </behavior>
  <action>Write the purge regression first. In purgeContact, select every mergeable child UID before the current explicit fan-out (asserting the captured set includes every interaction, event, fuel, link, and custom_field_value UID for the contact, not just their counts), insert their evidence plus the contact tombstone within its existing outer transaction, then preserve post-commit cleanup. Do not rely on FK cascade evidence and retain the existing archived guard. Also add one bound `UPDATE app_settings SET sun_contact_id = NULL, modified_at = ? WHERE id = 1 AND sun_contact_id = ?` (contactId) inside the same transaction, immediately before the contacts DELETE. This closes a review HIGH finding: `sun_contact_id`'s `ON DELETE SET NULL` FK action (migration 003) fires passively on the contacts DELETE and reverts the sun to self, but does not advance `app_settings.modified_at` (verified: no trigger exists) — a later Merge-restore's newer-`modified_at`-wins settings row could otherwise silently reintroduce the reference to the now-tombstoned contact. Owning this update explicitly in purgeContact, rather than trusting the passive FK alone, keeps the singleton settings row's LWW timestamp honest.

Review finding (ACTIONABLE, cycle-2, source-verified): `purgeContact(exec, contactId, opts?)` and its `PurgeOptions` type today carry no `now`/clock parameter, and the sole production caller, `src/screens/ArchivedContactsScreen.tsx`'s `doPurge` (`await purgeContact(exec, id, {...})`), supplies none — mirroring exactly the gap 17-03 caught and fixed for `deleteFuelCore`/`removeLinkCore`. Fix this at the type level so there is no runtime fallback to get wrong: add a required `now: string` field to `PurgeOptions` (a `YYYY-MM-DD HH:MM:SS` local-wall-clock string, the codebase's caller-supplied-clock convention — never synthesize it inside the DAO, matching 17-03's explicit rejection of a DAO-internal clock) and change `purgeContact`'s third parameter from `opts?: PurgeOptions` to `opts: PurgeOptions` (no longer optional) — `onPurgeExtensions` stays optional WITHIN `PurgeOptions`, only the options bag itself and its new `now` field become mandatory. This makes an omitted clock a compile error, not a runtime possibility, at every call site. Use `opts.now` as the bound `?` value in the `sun_contact_id`/`modified_at` UPDATE above (bind unconditionally — the UPDATE's `WHERE ... AND sun_contact_id = ?` guard already makes it a no-op when the purged contact is not the current sun, so no conditional branch is needed). Update `doPurge` in `ArchivedContactsScreen.tsx` to pass `{ now: localDateTime(), onPurgeExtensions: ... }` — the screen already imports and uses `localDateTime()` for `doRestore` a few lines above, so this is a same-file, same-pattern addition, not a new dependency. `tsc --noEmit` will flag every other call site (there is currently exactly one) that needs the same update.</action>
  <verify><automated>npm test -- src/db/purge-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Archived purge has one BEGIN/COMMIT boundary and all expected delete evidence is queryable, asserted per entity type (interactions/events/fuel/links/values), not just aggregate counts; field_history remains excluded; purging the current sun contact leaves `app_settings.sun_contact_id = NULL` and `modified_at` set to the exact caller-supplied `now`; `tsc --noEmit` confirms `ArchivedContactsScreen.tsx` compiles against the widened `PurgeOptions`.</acceptance_criteria>
  <done>One real permanent contact purge leaves complete mergeable tombstones transactionally and never leaves a stale-timestamped or clock-source-less dangling sun reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI/service → SQLite | Backup configuration and delete requests cross into durable local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-02 | Tampering | migration007 and purgeContact | high | mitigate | Forward-only test matrix, unique tombstone key, capture-before-delete, and one transaction. |
| T-17-04 | Tampering | app_settings.sun_contact_id staleness | high | mitigate | purgeContact explicitly nulls sun_contact_id and bumps app_settings.modified_at to a caller-supplied now (threaded from ArchivedContactsScreen.tsx's existing localDateTime() import) in the same transaction it purges the sun contact, instead of relying on the passive ON DELETE SET NULL FK action (which does not advance modified_at) or a DAO-internal clock. |
| T-17-38 | Repudiation | automatic-backup change detection | high | mitigate | app_settings.data_revision is a monotonic counter bumped by every tombstone insertion (17-02/17-03/17-04) and every create/update DAO chokepoint (17-06), giving Plan 17-06 a non-timestamp change signal that cannot miss a same-second mutation the way MAX(modified_at) can. |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Focused Vitest migration/purge tests, TypeScript compilation, then the full suite at wave completion.
</verification>

<success_criteria>
Migration 007 makes deletion evidence durable without weakening Phase 16 value invariants.
</success_criteria>

<output>Create `.planning/phases/17-backup-export-restore/17-02-SUMMARY.md` when done</output>
