---
phase: 17-backup-export-restore
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts]
autonomous: true
requirements: [BKP-01, BKP-02, BKP-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A migrated database has a unique, indefinitely retained tombstone for each deleted mergeable UID."
    - "Purging an archived contact captures every mergeable child UID before its explicit deletion in the same transaction."
    - "Backup cadence, retention, folder metadata, and automatic-success metadata are non-secret settings with positive integer validation."
  artifacts:
    - "src/db/migrations/007-tombstones.ts"
    - "src/db/tombstones-dao.ts"
  key_links:
    - "database.ts runs migration007 after migration006 before app code obtains the executor."
    - "purgeContact writes child and contact tombstones before its existing fan-out deletes."
  prohibitions:
    - "Restore evidence must not allow a deleted person to reappear from an older file. [flagged-unverified]"
---

<objective>
Establish the migration-007 deletion evidence and safe automatic-backup configuration on which every later backup operation relies.

Purpose: make permanent local deletion merge-safe before any export can represent it.
Output: tombstone schema/DAO, transactional contact-purge coverage, and backup settings columns.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
@src/db/migrations/006-normalize-custom-field-values.ts
@src/db/purge-dao.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add and prove migration 007 tombstone storage</name>
  <reversibility rating="one-way">D-01 publishes migration 007 and permanent deletion evidence; changing either after users create backups requires a data/file-format migration.</reversibility>
  <read_first>src/db/migrations/006-normalize-custom-field-values.ts; src/db/database.ts; src/db/purge-dao.ts; src/db/transaction.ts; src/db/migrations/006-normalize-custom-field-values.test.ts; 17-CONTEXT.md D-01</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/database.ts, src/db/tombstones-dao.ts</files>
  <behavior>
    - A v6 database upgrades to user_version 7 with tombstones(entity_type, entity_uid, deleted_at) and a unique logical key.
    - Tombstone cores only accept closed entity types and bind uid/timestamp values.
  </behavior>
  <action>Start RED with migration tests. Implement migration007 after migration006 and raise TARGET_VERSION to 7. Define typed, non-mutexed tombstone insertion/query cores with literal entity-type allowlisting and bound values. Implement D-01's additive schema exactly and preserve its indefinitely retained unique logical evidence; no cascade is treated as deletion evidence.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The migration is forward-only, existing v6 upgrades succeed, and the tombstone core has unique queryable evidence by UID/type.</acceptance_criteria>
  <done>Migration 007 supplies durable tombstone storage for every later delete path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make archived-contact purge record all fan-out tombstones</name>
  <read_first>src/db/purge-dao.ts; src/db/purge-dao.test.ts; src/db/tombstones-dao.ts; src/db/transaction.ts; 17-CONTEXT.md D-01</read_first>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/tombstones-dao.ts</files>
  <behavior>
    - Purging an archived contact creates tombstones for the contact, interactions, events, fuel, links, and normalized values before each corresponding delete.
    - field_history is removed by the existing purge but contributes no tombstone.
  </behavior>
  <action>Write the purge regression first. In purgeContact, select every mergeable child UID before the current explicit fan-out, insert their evidence plus the contact tombstone within its existing outer transaction, then preserve post-commit cleanup. Do not rely on FK cascade evidence and retain the existing archived guard.</action>
  <verify><automated>npm test -- src/db/purge-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Archived purge has one BEGIN/COMMIT boundary and all expected delete evidence is queryable; field_history remains excluded.</acceptance_criteria>
  <done>One real permanent contact purge leaves complete mergeable tombstones transactionally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add backup configuration and health state to the singleton settings row</name>
  <read_first>src/db/migrations/002-app-settings.ts; src/db/app-settings-dao.ts; src/db/app-settings-dao.test.ts; 17-CONTEXT.md D-08 through D-10</read_first>
  <files>src/db/migrations/007-tombstones.ts, src/db/migrations/007-tombstones.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <behavior>
    - New settings default to cadence 1 day and retention 7 days, and accept whole numbers from 1 through 3650.
    - Folder display metadata, last verified automatic success, export-change marker, encryption-enabled flag, and nudge dismissal are export-safe state; API secrets and passphrases are absent.
    - Invalid values reject before an UPDATE transaction begins.
  </behavior>
  <action>Add migration007 columns and app-settings DAO mapping/allowlist/validators for D-08–D-10. Keep the cached passphrase exclusively outside SQLite, represent folder access loss without deleting diagnostic configuration, and preserve arbitrary positive day values rather than a preset enum. Add node:sqlite regression tests for migration defaults, boundary validation, and secret exclusion.</action>
  <verify><automated>npm test -- src/db/migrations/007-tombstones.test.ts src/db/app-settings-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Settings reads and writes expose only non-secret backup configuration, while invalid cadence/retention causes no database mutation.</acceptance_criteria>
  <done>Backup health/configuration has a durable, validated local settings representation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI/service → SQLite | Backup configuration and delete requests cross into durable local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-02 | Tampering | migration007 and purgeContact | high | mitigate | Forward-only test matrix, unique tombstone key, capture-before-delete, and one transaction. |
| T-17-03 | Tampering | app settings | medium | mitigate | Bound/validated positive integer fields before transactional update. |
| T-17-04 | Information disclosure | settings export | high | mitigate | Restrict persisted/exported backup settings to non-secret metadata. |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>
Focused Vitest migration/purge/settings tests, TypeScript compilation, then the full suite at wave completion.
</verification>

<success_criteria>
Migration 007 makes deletion evidence and backup settings durable without weakening Phase 16 value invariants.
</success_criteria>

<output>Create `.planning/phases/17-backup-export-restore/17-02-SUMMARY.md` when done</output>
