---
phase: 17-backup-export-restore
plan: 03
type: tdd
wave: 2
depends_on: [17-02]
files_modified: [src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts]
autonomous: true
requirements: [BKP-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Deleting an individual interaction, fuel item, or contact link writes its tombstone with the same committed mutation."
  artifacts:
    - "Updated hard-delete DAO tests"
  key_links:
    - "Every standalone wrapper composes tombstone cores without nested write transactions."
  prohibitions:
    - "Individual deletes must not create a route for stale backup content to resurrect. [flagged-unverified]"
---

<objective>
Extend deletion evidence from contact purge to every standalone hard-delete writer discovered in the actual Phase 16 code.

Purpose: keep merge correctness consistent regardless of how a user deletes a child record.
Output: tombstone-aware interaction, fuel, and link delete cores/wrappers.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/db/recency-dao.ts
@src/db/fuel-dao.ts
@src/db/contact-links-dao.ts
@src/db/tombstones-dao.ts
@.planning/phases/17-backup-export-restore/17-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one direct touchpoint deletion merge-safe</name>
  <read_first>src/db/recency-dao.ts deleteTouchpoint and exported cores; src/db/recency-dao.test.ts; src/db/transaction.ts; src/db/tombstones-dao.ts</read_first>
  <files>src/db/recency-dao.ts, src/db/recency-dao.test.ts</files>
  <behavior>
    - Deleting a matching interaction writes an interaction tombstone containing its UID and the deletion timestamp in the same transaction.
    - A nonmatching interaction throws with no tombstone and leaves last_contact unchanged.
  </behavior>
  <action>Write the focused failure first, then update deleteTouchpoint to retrieve the target UID inside its existing write boundary, insert deletion evidence through the tombstone core, delete once, and retain the existing recency recomputation semantics. Keep all SQL parameters bound and ensure the wrapper owns exactly one transaction.</action>
  <verify><automated>npm test -- src/db/recency-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>A direct interaction deletion is atomic with its tombstone and remains a valid recency mutation.</acceptance_criteria>
  <done>One user-triggered child delete has end-to-end durable anti-resurrection evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover fuel and link hard-delete wrappers</name>
  <read_first>src/db/fuel-dao.ts deleteFuelCore/deleteFuel; src/db/contact-links-dao.ts removeLink; their matching test files; src/db/tombstones-dao.ts</read_first>
  <files>src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts</files>
  <behavior>
    - Fuel and link deletion records the respective UID/type before removal.
    - Failed and zero-change deletes produce no deletion evidence.
    - Existing non-mutexed cores remain composable by restore code.
  </behavior>
  <action>Extend deleteFuelCore and removeLink using the same target-read, tombstone-core, exact-change pattern as the tracer. Keep public wrappers as single outer transaction owners and preserve current update/create behavior. Add focused tests that assert one transaction and no tombstone for rejected input.</action>
  <verify><automated>npm test -- src/db/fuel-dao.test.ts src/db/contact-links-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Fuel and link deletes leave exactly one type-correct tombstone only when the matching row is actually removed.</acceptance_criteria>
  <done>All presently standalone child-delete writers are tombstone-aware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Delete action → SQLite | A user mutation permanently removes mergeable local records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-05 | Tampering | child delete DAOs | high | mitigate | Target UID capture and tombstone insertion are in the same transaction as deletion. |
| T-17-06 | Repudiation | deletion evidence | medium | mitigate | Unique logical tombstone records preserve evidence indefinitely. |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>Run focused DAO suites and compilation; wave completion runs the full suite.</verification>
<success_criteria>No standalone child deletion can silently omit its merge evidence.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-03-SUMMARY.md` when done</output>
