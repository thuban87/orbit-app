---
phase: 17-backup-export-restore
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, package-lock.json]
autonomous: false
requirements: [BKP-01, BKP-03]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Native backup dependencies are independently verified before installation."
  artifacts:
    - "package.json and package-lock.json record only verified Phase 17 packages."
  key_links:
    - "The human verification gate precedes package-manager writes."
  prohibitions:
    - "Manual exports must not be represented as automatic-backup health. [flagged-unverified]"
---

<objective>
Verify the three flagged native packages before adding the file-sharing and cryptographic capabilities.

Purpose: prevent a supply-chain decision from being silently automated.
Output: documented human approval to install the audited package set.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-RESEARCH.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify expo-sharing provenance before installation</name>
  <read_first>package.json; 17-RESEARCH.md Package Legitimacy Audit; npmjs.com/package/expo-sharing; Expo's expo/expo repository</read_first>
  <files>package.json, package-lock.json</files>
  <action>Per the Package Legitimacy Audit, have the owner verify the npm publisher, current package page, and Expo upstream repository for expo-sharing. After explicit approval, install the SDK-57-compatible package with expo install and retain its lockfile resolution; this enables D-07 manual share-sheet export without changing automatic-backup health.</action>
  <what-built>Package provenance has been independently checked before native code is added.</what-built>
  <how-to-verify>Compare npm package metadata and publisher with the linked Expo repository, then approve or reject this package.</how-to-verify>
  <resume-signal>Reply that expo-sharing provenance is approved, or identify the package evidence that failed review.</resume-signal>
  <verify><automated>npm view expo-sharing name version repository.url --json</automated></verify>
  <acceptance_criteria>The owner records approval for expo-sharing before package.json or package-lock.json is changed for it.</acceptance_criteria>
  <done>expo-sharing provenance is approved or execution is halted with no package write.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify react-native-quick-crypto provenance before installation</name>
  <read_first>17-RESEARCH.md Package Legitimacy Audit and platform-encryption.md; npmjs.com/package/react-native-quick-crypto; github.com/margelo/react-native-quick-crypto</read_first>
  <files>package.json, package-lock.json</files>
  <action>Per D-11 and the Package Legitimacy Audit, have the owner verify react-native-quick-crypto's publisher, upstream repository, Expo config-plugin instructions, and published peer range before installation. Record the approved pinned compatible version and use native PBKDF2 plus AES-256-GCM only; no hand-implemented password derivation is authorized.</action>
  <what-built>The proposed native encryption package and its peer contract have been presented for owner review.</what-built>
  <how-to-verify>Compare npm metadata, publisher, upstream repository, and Expo integration guidance, then approve or reject this package.</how-to-verify>
  <resume-signal>Reply that react-native-quick-crypto provenance is approved, or identify the package evidence that failed review.</resume-signal>
  <verify><automated>npm view react-native-quick-crypto name version repository.url peerDependencies --json</automated></verify>
  <acceptance_criteria>The owner approves the native crypto package and its declared peer contract before any package-manager write.</acceptance_criteria>
  <done>RNQC provenance is approved or execution is halted with no crypto package write.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify react-native-nitro-modules provenance before installation</name>
  <read_first>17-RESEARCH.md Package Legitimacy Audit; npmjs.com/package/react-native-nitro-modules; github.com/mrousavy/nitro</read_first>
  <files>package.json, package-lock.json</files>
  <action>Have the owner verify react-native-nitro-modules as RNQC's required native peer, including publisher, repository, and the peer version selected by npm. After all three approvals, install expo-document-picker, expo-sharing, react-native-quick-crypto, react-native-nitro-modules, and react-native-quick-base64 using the documented Expo/native install commands; do not add an unverified package or a postinstall hook.</action>
  <what-built>The RNQC native peer dependency and complete approved install set are ready for confirmation.</what-built>
  <how-to-verify>Compare npm metadata and upstream repository, then confirm the selected peer version matches the approved RNQC range.</how-to-verify>
  <resume-signal>Reply that react-native-nitro-modules provenance is approved, or identify the package evidence that failed review.</resume-signal>
  <verify><automated>npm view react-native-nitro-modules name version repository.url --json</automated></verify>
  <acceptance_criteria>All SUS packages have individual owner approval and the resulting dependency tree contains the intended native packages only.</acceptance_criteria>
  <done>Approved packages are ready for Phase 17 implementation, or the checkpoint remains blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package registry → native build | Third-party native code enters the Android application. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-01 | Tampering | npm dependencies | high | mitigate | Blocking owner provenance review for every SUS package before install. |
| T-17-SC | Tampering | package installs | high | mitigate | Package-legitimacy gate and human verification before writes. |
</threat_model>

<verification>
Run the three npm metadata commands and retain owner approvals before package installation.
</verification>

<success_criteria>
No flagged native package is installed without the named human verification.
</success_criteria>

<output>Create `.planning/phases/17-backup-export-restore/17-01-SUMMARY.md` when done</output>
