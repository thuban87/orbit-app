---
phase: 17-backup-export-restore
plan: 07
type: tdd
wave: 4
depends_on: [17-01, 17-05, 17-06]
files_modified: [src/services/backup/encryption.ts, src/services/backup/encryption.test.ts, src/services/backup/passphrase-store.ts, src/services/backup/passphrase-store.test.ts, src/services/backup/backup-service.ts, src/backup/types.ts]
autonomous: true
requirements: [BKP-03]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Encryption uses a versioned PBKDF2-HMAC-SHA256/AES-256-GCM envelope with random salt and nonce, authenticated metadata, and no personal metadata outside ciphertext."
    - "A passphrase is cached only in SecureStore while enabled; disable deletes it without altering old encrypted files."
    - "Changing a passphrase verifies the old one and re-encrypts accessible automatic files by default, with a future-files-only choice."
  artifacts:
    - "src/services/backup/encryption.ts"
    - "src/services/backup/passphrase-store.ts"
  key_links:
    - "Manual and automatic backup services select the same encrypted envelope machinery."
  prohibitions:
    - "Backup encryption must not be coupled to future sync E2EE or reveal content metadata outside ciphertext. [flagged-unverified]"
---

<objective>
Add optional native passphrase encryption and its irreversible-but-honest lifecycle to the shared backup service.

Purpose: offer protection without weakening the default readable export path or implying passphrase recovery.
Output: tested envelope, narrow SecureStore repository, re-encryption orchestration.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/dossier/workpapers/15-backup/platform-encryption.md
@src/services/ai-key-store.ts
@src/services/backup/backup-service.ts
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Encrypt and authenticate one backup payload with the native envelope</name>
  <reversibility rating="costly">D-11's versioned encrypted envelope is a published compatibility contract for backup files and later readers.</reversibility>
  <read_first>platform-encryption.md; 17-RESEARCH.md Standard Stack and Common Pitfall 6; 17-CONTEXT.md D-11; package.json after 17-01 approval</read_first>
  <files>src/services/backup/encryption.ts, src/services/backup/encryption.test.ts, src/backup/types.ts</files>
  <behavior>
    - Encrypt/decrypt round trips use random salt and nonce, PBKDF2-HMAC-SHA256, AES-256-GCM, and authenticated technical envelope metadata.
    - Wrong passphrase, modified ciphertext/tag/metadata, unsupported envelope version, and malformed binary input fail with typed safe errors.
    - The public envelope leaves only format/encryption/cipher/KDF/salt/IV metadata visible.
  </behavior>
  <action>Start with deterministic injected-RNG/native-crypto vectors, then implement a versioned RNQC-backed PBKDF2 and AES-GCM envelope. Bind technical header metadata as AAD, document the parameter object in types, and expose no raw key/passphrase in errors or Logger. Keep KDF/cipher code confined to this service and keep backup-at-rest key material entirely separate from any future sync system per D-11.</action>
  <verify><automated>npm test -- src/services/backup/encryption.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tampering and wrong passphrases cannot yield plaintext or a preview, while valid encrypted bytes decrypt compatibly through the declared envelope version.</acceptance_criteria>
  <done>A native authenticated encryption seam protects one backup payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manage cached passphrases and verified re-encryption safely</name>
  <read_first>src/services/ai-key-store.ts and tests; src/services/backup/saf-storage.ts; src/services/backup/backup-service.ts; 17-CONTEXT.md D-12 and D-13</read_first>
  <files>src/services/backup/passphrase-store.ts, src/services/backup/passphrase-store.test.ts, src/services/backup/backup-service.ts, src/services/backup/encryption.test.ts</files>
  <behavior>
    - SecureStore get/set/delete is the only cache API; no passphrase is serialized, routed, logged, or placed in SQLite.
    - Disable clears only the cached current secret and preserves existing encrypted files.
    - Change verifies the old passphrase and defaults to verified re-encryption of accessible automatic snapshots; a future-files-only option leaves them unchanged, and forgotten-passphrase flow creates protection only for future files.
    - Encryption-enabled manual export defaults encrypted but requires an explicit readable override decision.
  </behavior>
  <action>Mirror the injected lazy SecureStore backend pattern for one namespaced backup passphrase. Extend backup-service with lifecycle results that distinguish verification failure, inaccessible automatic files, re-encrypted files, and future-only selection; verify each rewritten automatic file before replacing it. Preserve manually shared files under their old protection and expose an explicit manual plaintext choice only to UI callers under D-12 and D-13.</action>
  <verify><automated>npm test -- src/services/backup/passphrase-store.test.ts src/services/backup/encryption.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>All passphrase lifecycle branches are testable without native SecureStore, and none records secret material in a backup model or app setting.</acceptance_criteria>
  <done>Encryption can be enabled, changed, forgotten, or disabled without false recovery promises or secret leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user passphrase → native crypto | Human secret becomes key-derived encryption material. |
| encrypted file → decrypt/preview | Untrusted ciphertext and technical envelope metadata are processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-15 | Information disclosure | envelope/passphrase cache | high | mitigate | Native PBKDF2/AES-GCM, SecureStore-only cache, AAD, no secret logs or metadata leakage. |
| T-17-16 | Tampering | encrypted backup bytes | high | mitigate | Authenticated decryption rejects wrong passphrases and all envelope/ciphertext modification. |
| T-17-17 | Repudiation | passphrase-change writes | medium | mitigate | Verify old passphrase and every re-encrypted automatic artifact before reporting success. |
| T-17-SC | Tampering | native crypto packages | high | mitigate | 17-01 human provenance approval is a dependency. |
</threat_model>

<verification>Run encryption/passphrase vectors and typecheck; physical benchmark and Android round trip are gated in 17-11.</verification>
<success_criteria>Encryption remains optional, strong, compatible, and truthful about unrecoverable old passphrases.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-07-SUMMARY.md` when done</output>
