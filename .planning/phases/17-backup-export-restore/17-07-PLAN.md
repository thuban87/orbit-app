---
phase: 17-backup-export-restore
plan: 07
type: tdd
wave: 5
depends_on: [17-01, 17-05, 17-06]
files_modified: [src/services/backup/encryption.ts, src/services/backup/encryption.test.ts, src/services/backup/passphrase-store.ts, src/services/backup/passphrase-store.test.ts, src/services/backup/backup-service.ts, src/backup/types.ts]
autonomous: false
requirements: [BKP-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Encryption uses the physical-Pixel-approved versioned PBKDF2-HMAC-SHA256/AES-256-GCM envelope with random salt and nonce, authenticated metadata, and no personal metadata outside ciphertext."
    - "A passphrase is cached only in SecureStore while enabled; disable deletes it without altering old encrypted files."
    - "Changing a passphrase verifies the old one and re-encrypts accessible automatic files by default, with a future-files-only choice."
  artifacts:
    - "src/services/backup/encryption.ts"
    - "src/services/backup/passphrase-store.ts"
  key_links:
    - "The physical Pixel KDF decision fixes the versioned parameter object before manual and automatic services select the shared envelope machinery."
  prohibitions:
    - "Backup encryption must not be coupled to future sync E2EE or reveal content metadata outside ciphertext. [flagged-unverified]"
---

<objective>
Add optional native passphrase encryption and its irreversible-but-honest lifecycle to the shared backup service.

Purpose: offer protection without weakening the default readable export path or implying passphrase recovery.
Output: tested envelope, narrow SecureStore repository, re-encryption orchestration.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/dossier/workpapers/15-backup/platform-encryption.md
@src/services/ai-key-store.ts
@src/services/backup/backup-service.ts
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Parameterize and benchmark one native encrypted backup envelope</name>
  <reversibility rating="costly">D-11's versioned encrypted envelope is a published compatibility contract for backup files and later readers.</reversibility>
  <read_first>platform-encryption.md; 17-RESEARCH.md Standard Stack and Common Pitfall 6; 17-CONTEXT.md D-11; package.json after 17-01 approval</read_first>
  <files>src/services/backup/encryption.ts, src/services/backup/encryption.test.ts, src/backup/types.ts</files>
  <behavior>
    - Encrypt/decrypt round trips use random salt and nonce, PBKDF2-HMAC-SHA256, AES-256-GCM, authenticated technical envelope metadata, and an explicit caller-supplied versioned KDF parameter object.
    - Wrong passphrase, modified ciphertext/tag/metadata, unsupported envelope version, and malformed binary input fail with typed safe errors.
    - The native timing seam reports PBKDF2 duration for a supplied candidate object without selecting a shipping default.
  </behavior>
  <action>Start with deterministic injected-RNG/native-crypto vectors, then implement a parameterized RNQC-backed PBKDF2 and AES-GCM envelope plus a native timing seam that accepts candidate versioned KDF parameters. Bind technical header metadata as AAD, document the parameter object in types, and expose no raw key/passphrase in errors or Logger. Do not freeze a shipping iteration count or default format version in this task; keep KDF/cipher code confined to this service and keep backup-at-rest key material entirely separate from any future sync system per D-11.</action>
  <verify><automated>npm test -- src/services/backup/encryption.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tampering and wrong passphrases cannot yield plaintext or a preview; valid encrypted bytes decrypt through their supplied versioned parameter object, and no production default is selected before the physical-Pixel decision.</acceptance_criteria>
  <done>A native authenticated encryption and timing seam is ready for the physical-device parameter decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve the first versioned PBKDF2 profile from physical Pixel evidence</name>
  <decision>Choose the initial encrypted-envelope format version and PBKDF2-HMAC-SHA256 parameter object after measuring the native timing seam on the physical Pixel.</decision>
  <context>D-11 permits only technical decryptability metadata, while RESEARCH Assumption A1 and Open Question 1 leave the KDF cost unresolved. The selected profile is a published compatibility/security contract: it must name formatVersion, KDF identifier, iteration count, derived-key length, salt length, IV length, cipher, and the recorded Pixel duration for the tested release build.</context>
  <options>
    <option id="approve-measured-profile">
      <name>Approve measured profile</name>
      <pros>Freezes a versioned parameter object with measured device evidence and lets automatic/manual writers share it.</pros>
      <cons>Future tuning requires a new compatible envelope version rather than silently changing existing files.</cons>
    </option>
    <option id="revise-and-rerun">
      <name>Revise candidates and rerun</name>
      <pros>Lets the owner reject an unsuitable security/latency balance before any encrypted backup defaults ship.</pros>
      <cons>Encryption lifecycle implementation remains paused until an approved measured profile exists.</cons>
    </option>
  </options>
  <resume-signal>Reply with the approved parameter object and Pixel timing record, or request revised candidates and another measurement.</resume-signal>
  <verify><automated>npm test -- src/services/backup/encryption.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The checkpoint response records the exact selected versioned parameters and physical-Pixel timing, resolving RESEARCH Assumption A1 and Open Question 1 before any default is frozen.</acceptance_criteria>
  <done>An owner-approved, measured KDF profile is available for the shipping encrypted-envelope default, or the plan remains blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze the approved envelope profile and manage passphrase lifecycle</name>
  <read_first>resolved Task 2 checkpoint record; src/services/ai-key-store.ts and tests; src/services/backup/saf-storage.ts; src/services/backup/backup-service.ts; 17-CONTEXT.md D-11 through D-13</read_first>
  <files>src/services/backup/passphrase-store.ts, src/services/backup/passphrase-store.test.ts, src/services/backup/backup-service.ts, src/services/backup/encryption.test.ts</files>
  <behavior>
    - SecureStore get/set/delete is the only cache API; no passphrase is serialized, routed, logged, or placed in SQLite.
    - Disable clears only the cached current secret and preserves existing encrypted files.
    - Change verifies the old passphrase and defaults to verified re-encryption of accessible automatic snapshots; a future-files-only option leaves them unchanged, and forgotten-passphrase flow creates protection only for future files.
    - Encryption-enabled manual export defaults encrypted but requires an explicit readable override decision.
  </behavior>
  <action>Freeze only the Task 2 owner-approved versioned KDF/cipher parameter object as the default and add regression coverage that the serialized envelope metadata equals that record. Mirror the injected lazy SecureStore backend pattern for one namespaced backup passphrase. Extend backup-service with lifecycle results that distinguish verification failure, inaccessible automatic files, re-encrypted files, and future-only selection; verify each rewritten automatic file before replacing it. Preserve manually shared files under their old protection and expose an explicit manual plaintext choice only to UI callers under D-12 and D-13.</action>
  <verify><automated>npm test -- src/services/backup/passphrase-store.test.ts src/services/backup/encryption.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>The shipping default exactly matches the Task 2 selected versioned parameter record; all passphrase lifecycle branches are testable without native SecureStore, and none records secret material in a backup model or app setting.</acceptance_criteria>
  <done>Encryption can be enabled, changed, forgotten, or disabled without false recovery promises or secret leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user passphrase → native crypto | Human secret becomes key-derived encryption material. |
| encrypted file → decrypt/preview | Untrusted ciphertext and technical envelope metadata are processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-15 | Information disclosure | envelope/passphrase cache | high | mitigate | Native PBKDF2/AES-GCM, SecureStore-only cache, AAD, no secret logs or metadata leakage. |
| T-17-16 | Tampering | encrypted backup bytes | high | mitigate | Authenticated decryption rejects wrong passphrases and all envelope/ciphertext modification. |
| T-17-17 | Repudiation | passphrase-change writes | medium | mitigate | Verify old passphrase and every re-encrypted automatic artifact before reporting success. |
| T-17-SC | Tampering | native crypto packages | high | mitigate | 17-01 human provenance approval is a dependency. |
</threat_model>

<verification>Run encryption/passphrase vectors and typecheck; the physical Pixel parameter decision is a blocking Task 2 gate, while the full Android round trip remains gated in 17-11.</verification>
<success_criteria>Encryption remains optional, strong, compatible, and truthful about unrecoverable old passphrases.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-07-SUMMARY.md` when done</output>
