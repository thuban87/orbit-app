---
phase: 17-backup-export-restore
plan: 04
type: tdd
wave: 2
depends_on: [17-02]
files_modified: [src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/tombstones-dao.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/backup/types.ts]
autonomous: true
requirements: [BKP-01, BKP-04]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Permanent custom-field-definition expiry tombstones the definition and its normalized values while field_history remains transient, and only when the definition delete actually changed a row."
    - "One reusable pure policy decides UID conflicts, including deletion winning equal-second ties, NULL custom-value clears, and resolving a reference to a tombstoned parent to a defined fallback."
    - "The reconciliation entity-policy registry names every mergeable UID-bearing table (contacts, interactions, events, fuel, links, custom_field_defs, custom_field_values, categories, profile), not only the ones with a hard-delete writer today; categories reconcile against 17-02's four fixed RESERVED_CATEGORY_UIDS constants, extending the identical owner-approved reserved-UID pattern already applied to profile, so categories restore onto a fresh install with no per-install identity drift and no undeletable duplicate insertions (round-2 confirmation review HIGH)."
    - "Live-row uid uniqueness and tombstone uid uniqueness are independent domains: one entity may legitimately carry both a live row and a tombstone (a delete-then-restore-newer state D-03 expressly permits), resolved through the one shared timestamp/tombstone helper; a duplicate is only ever rejected within the SAME domain (two live rows, or two tombstones, sharing a uid) (Pass-2 audit B-1)."
    - "Reconciliation computes final surviving parent UIDs before deciding any child action and never returns insert/update for a child whose local OR incoming parent UID did not survive; every mandatory-parent contact child (interactions, events, fuel, contact_links) and both custom_field_values parents (contact, field definition) are covered (Pass-2 audit B-2)."
    - "A custom_field_values pair-key collision (distinct value uids independently created for the same contactUid+fieldDefUid) or a custom_field_defs.col_name collision (distinct def uids sharing a col_name) between local and incoming state is reported as a whole-restore incompatibility with zero actions for the colliding rows — never resolved by reusing the existing pair/col_name UPSERT as UID reconciliation (Pass-2 audit B-3, B-5; owner-ratified reject-incompatible rule for this phase)."
  artifacts:
    - "src/backup/reconciliation.ts"
    - "src/backup/reconciliation.test.ts"
  key_links:
    - "Restore and later sync can call the same reconciliation policy rather than duplicate timestamp logic."
    - "Plan 17-08 calls the reference-to-tombstoned-parent fallback for app_settings.sunContactUid rather than reimplementing dangling-reference handling."
    - "Plan 17-08's whole-file structural validation reuses reconciliation's same-file survivor helper to reject a live child whose parent already lost within the submitted backup file, before any local DB state is consulted (Pass-2 audit B-2, whole-file half)."
    - "Plan 17-08's Merge apply calls the parent-first / survivor-set reconciliation contract in dependency order (categories/profile/contacts/custom_field_defs before their children) and treats any non-empty incompatibilities list as a whole-restore rejection per D-05."
  prohibitions:
    - "A clear custom value must not be reduced to missing data during reconciliation. [flagged-unverified]"
    - "A pair-uid or col_name collision must never be silently resolved by reusing the existing pair/col_name UPSERT as UID reconciliation, and must never be treated as ordinary D-02 identity for alias-merging. [flagged-unverified]"
---

<objective>
Finish tombstone coverage for normalized custom fields and establish the reusable UID reconciliation decision core.

Purpose: preserve Phase 16 semantics while making all later Merge decisions deterministic.
Output: permanent-field deletion evidence and a tested pure reconciliation contract.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/decisions/ADR-001-normalized-custom-field-values.md
@src/db/field-ddl.ts
@src/db/field-values-dao.ts
@.planning/sync-milestone/PHASE-17-SYNC-READINESS.md
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve field-definition and value deletion evidence in one permanent-expiry path</name>
  <read_first>src/db/field-ddl.ts deleteOrQuarantineField/dropField/expireFieldIfStale; src/db/field-ddl.test.ts; src/db/field-values-dao.ts; ADR-001 Consequences; 17-CONTEXT.md D-01</read_first>
  <files>src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/tombstones-dao.ts</files>
  <behavior>
    - A permanent definition deletion retains the current field_history snapshot behavior and creates tombstones for the definition plus every normalized value UID before removal.
    - Quarantine and field_history retention do not create tombstones.
    - Attempting to permanently delete a stale/already-gone definition (zero rows actually deleted) throws, rolls back, and leaves no tombstone for that definition or its values.
  </behavior>
  <action>Begin with failing tests around permanent deletion and expiry. Within the established field transaction, capture the definition's own `uid` (not just `id`/`col_name` — widen the `DropTarget` shape or read the definition row directly, since `custom_field_defs.uid` is not currently in `DropTarget`) and every dependent `custom_field_values` UID, insert their tombstones, retain snapshot-before-delete behavior, then remove rows. Review finding (MEDIUM, source-verified): unlike `purgeContact`, which asserts `changes === 1` on its contacts DELETE, `dropFieldValues`'s definition DELETE currently has no change-count assertion — add one (`changes === 1`, matching the purgeContact idiom) so a stale/missing definition throws and rolls back the whole transaction, including any tombstone insert, rather than ever committing a tombstone for an entity that was not actually removed. Keep the quarantine branch untouched except for proving it creates no deletion evidence; implement D-01 without altering Phase 16's raw nullable TEXT model.</action>
  <verify><automated>npm test -- src/db/field-ddl.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Permanent deletion is one transaction containing history snapshot, value/definition tombstones, and deletes with an exact-one-row-changed guard on the definition DELETE; quarantine has no tombstone; a stale/already-deleted definition id rolls back with zero tombstones written.</acceptance_criteria>
  <done>Normalized custom-field deletion cannot be undone by an older backup while field_history stays excluded, and no tombstone can ever be written for a definition that was not actually removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and test the caller-supplied reconciliation policy — domain-separated uniqueness, parent-survival blocking, and collision rejection</name>
  <reversibility rating="costly">D-02 makes the reconciliation contract shared by backup restore and the later sync apply path, so changing its public types touches both consumers.</reversibility>
  <read_first>.planning/sync-milestone/PHASE-17-SYNC-READINESS.md; 17-CONTEXT.md D-02, D-03, D-04, D-05; src/screens/settings-ai-logic.ts; src/db/field-values-dao.ts upsertValueCore; src/db/migrations/001-initial.ts CREATE_CONTACTS/CREATE_INTERACTIONS/CREATE_EVENTS/CREATE_FUEL/CREATE_CONTACT_LINKS/CREATE_CUSTOM_FIELD_DEFS (mandatory NOT NULL FK columns, the col_name UNIQUE constraint, and contacts.category_id — nullable, no hard-delete writer on categories); src/db/migrations/006-normalize-custom-field-values.ts CREATE_CUSTOM_FIELD_VALUES (UNIQUE(contact_id, field_def_id)); src/db/migrations/007-tombstones.ts RESERVED_PROFILE_UID and RESERVED_CATEGORY_UIDS (17-02, round-2 fix); 17-AUDIT-B-FINDINGS.md findings 1-5 (B-1 through B-5)</read_first>
  <files>src/backup/types.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts</files>
  <behavior>
    - Rows match by uid; a later modified_at wins, a missing row inserts, and a tombstone wins an equal-second tie.
    - A genuinely newer row can supersede an older tombstone.
    - Parent references remain UID-shaped in the returned action plan, and value=null is a present mutable row.
    - A reference field naming a UID that did not survive reconciliation (deleted by a winning tombstone) resolves to the field's defined "orphaned-parent" fallback rather than a dangling UID.
    - The exported entity-policy registry explicitly names every mergeable UID-bearing table in the current schema, including ones with no hard-delete writer today.
    - B-1 — INDEPENDENT UNIQUENESS DOMAINS: a live row's uid is unique only within the live-row array for that entity type; a tombstone's entity_uid is unique only within the tombstone array for that entity type. One live row and one tombstone sharing the same uid is a PERMITTED, legitimate state (D-03's "genuinely newer incoming row may restore a tombstoned logical entity") and is resolved by feeding both to the one shared timestamp/tombstone comparison helper — never rejected as a duplicate. A duplicate is rejected only when it occurs within the SAME domain (two live rows sharing a uid, or two tombstones sharing an entity_uid, in the same array).
    - B-2 — MANDATORY-PARENT SURVIVAL AS A FIRST-CLASS RESULT: parent entity types (contacts, custom_field_defs, categories, profile) are reconciled before their children. The reconciliation caller receives, per parent entity type, the set of uids that survive (insert/update/retain outcomes). Reconciling a child entity type accepts that survivor set per declared parent field and returns a `blocked` outcome — never insert or update — for any child row (local or incoming) whose relevant parent uid is not in the survivor set. Every mandatory contact-child relationship is covered: interactions.contact_id, events.contact_id, fuel.contact_id, contact_links.contact_id, and BOTH custom_field_values parents (contact_id AND field_def_id).
    - B-3 — PAIR-UID COLLISION IS A WHOLE-RESTORE INCOMPATIBILITY, NEVER A SILENT UPSERT: when a local custom_field_values row and an incoming custom_field_values row share the same (contactUid, fieldDefUid) pair but carry DISTINCT value uids, reconciliation reports a pair-key collision incompatibility instead of an insert/update/retain action for either row. It never falls back to the existing `upsertValueCore` ON CONFLICT(contact_id, field_def_id) pair-upsert as a UID-reconciliation mechanism, since that would silently discard whichever uid loses and corrupt the stable-uid identity guarantee.
    - B-5 — COL_NAME COLLISION IS ALSO A WHOLE-RESTORE INCOMPATIBILITY: when a local custom_field_defs row and an incoming custom_field_defs row share the same col_name but carry DISTINCT def uids, reconciliation reports a col_name collision incompatibility instead of an insert/update/retain action for either row. col_name is never treated as D-02 identity and is never alias-merged.
    - The same-file survivor computation (a set of live rows plus a set of tombstones for ONE entity type, no local state involved) is independently callable so Plan 17-08's whole-file structural validation can determine, from a single submitted backup file alone, which parent uids survive by the file's own evidence, and reject a live child citing a parent that already lost within that same file.
  </behavior>
  <action>Implement small exported wire/action types and a pure reconciliation function accepting caller-provided local/incoming participating sets and policy. Centralize timestamp/tombstone comparison in ONE helper (name it explicitly and reuse it everywhere a uid's live-vs-tombstone fate is decided), return explicit insert/update/retain/delete/blocked actions plus aggregate totals, and deliberately omit contacts.last_contact from scalar decisions. Export the shared timestamp/tombstone comparison helper directly (not only as a private implementation detail) so it doubles as the same-file survivor primitive B-2's whole-file half needs: given just a live-row array and a tombstone array for one entity type (no local/incoming split), it returns the surviving uid set by the identical D-03 tie-break rule (newer wins, equal-second favors deletion).

Review finding (MEDIUM, source-verified): document and export a complete entity-policy registry — not only the tables 17-02/17-03/17-04 give hard-delete tombstones to. Explicitly enumerate `categories` (LWW on modified_at; no delete API exists today — verified via repo-wide grep for `DELETE FROM categories`; no tombstone type yet — reconciled by 17-02's four fixed `RESERVED_CATEGORY_UIDS` constants exported from `src/db/migrations/007-tombstones.ts`, one per seeded `display_order`, so a fixture using those exact four constants proves ordinary UID reconciliation resolves all four categories with no per-install identity drift and no undeletable duplicate insertion — this is the identical `profile` fix pattern extended to the sibling singleton-seed identity problem, round-2 confirmation review HIGH), `profile` (single-row LWW, same no-delete status — reconciled by 17-02's fixed `RESERVED_PROFILE_UID` exported from `src/db/migrations/007-tombstones.ts`, so a fixture using that exact constant proves ordinary UID reconciliation resolves the singleton with no D-02 exception per Pass-2 audit B-4, owner-approved), and `events` (immutable UID-bearing rows — insert-if-missing, otherwise retain as read-only; never update event content) alongside the tombstone-bearing entities, so D-02's "future sync apply path reuses this contract" promise does not silently omit tables that simply have no delete path yet. Add a generic "reference-to-tombstoned-parent" policy function: given a nullable UID-shaped reference field and the reconciled parent-survival set, return the caller-supplied fallback value (e.g., null) when the referenced UID lost to a tombstone — this is the general primitive Plan 17-08 will apply to both `app_settings.sunContactUid` (so a Merge can never leave a settings row pointing at a contact the same pass just tombstoned) and, by the identical mechanism, to a `categoryUid` a restored contact resolves that is absent from the destination's reconciled category set (categories never tombstone today, but the same fallback-to-null primitive is the correct, forward-compatible handling for that case too — round-2 confirmation review HIGH).

Review finding (HIGH, Pass-2 audit B-1, source-verified): the previous "duplicate UID rejection" language was unscoped and would reject a legitimate delete-then-restore-newer state (a contact deleted at 10:00:00, then genuinely restored by a newer incoming row at 10:00:01 — D-03 expressly permits this, and both the tombstone and the newer live row are correct exported state). Implement two independent uniqueness domains per entity type: reject a duplicate uid ONLY when it recurs within the live-row array, or ONLY when an entity_uid recurs within the tombstone array; a live row and a tombstone sharing one uid across the two domains is not a duplicate — feed both into the shared comparison helper above. Add export→parse→Merge tests for: an older tombstone with a newer live row (row wins, restoring the entity), equal timestamps (tombstone wins, D-03's deletion-wins tie-break), and a newer tombstone over an older live row (tombstone wins).

Review finding (HIGH, Pass-2 audit B-2, source-verified): the previous contract only supplied a fallback for nullable UID references (sunContactUid); interaction/event/fuel/link parents and both custom_field_values parents are MANDATORY NOT NULL foreign keys (verified: `interactions.contact_id`, `events.contact_id`, `fuel.contact_id`, `contact_links.contact_id` are all `NOT NULL REFERENCES contacts(id)`; `custom_field_values.contact_id` and `.field_def_id` are both `NOT NULL REFERENCES ...`), so a UID-only child decision could return an insert for a child whose parent tombstoned in the same pass, corrupting the apply or reviving a deleted parent. Make mandatory-parent survival a first-class reconciliation RESULT: the caller reconciles parent entity types first (contacts, custom_field_defs, categories, profile), computes the final surviving parent uid set from each result's insert/update/retain outcomes, then supplies that survivor set to the reconciliation call for each dependent child entity type via a declared parent-field mapping (e.g. interactions/events/fuel/contact_links declare a `contact_id` parent field pointing at the contacts survivor set; custom_field_values declares BOTH a `contact_id` parent field and a `field_def_id` parent field, each checked independently). A child row (from either local or incoming) whose declared parent uid is missing from the relevant survivor set becomes a `blocked` action — reported for observability, excluded from insert/update — never invented, never left to fail at the FK layer. Cover all contact children and the two-parent normalized value row with dedicated tests: a child blocked by a locally-lost parent, a child blocked by an incoming-lost parent, and a custom_field_values row blocked by EITHER of its two parents losing independently.

Review finding (HIGH, Pass-2 audit B-3, source-verified — OWNER DECISION, reject-incompatible): two independently-created databases can each mint a distinct durable value-row uid for the same (contactUid, fieldDefUid) pair (local `L`, incoming `R`). Naive UID reconciliation would see `R` as "missing" and, once mapped to local ids, its insert would violate `UNIQUE(contact_id, field_def_id)`; silently falling back to `upsertValueCore`'s ON CONFLICT pair-upsert would retain uid `L` while applying `R`'s scalar data, breaking the stable-uid identity guarantee this whole module exists to provide. Implement a pre-write, pair-keyed collision scan run AFTER parent uid resolution (so only rows whose parents both survive are even candidates): index local and incoming custom_field_values rows by BOTH their own uid and their (contactUid, fieldDefUid) pair key; when a pair key resolves to two DIFFERENT uids across local and incoming, emit a pair-collision incompatibility (entityType, the pair key, the local uid, the incoming uid) instead of any insert/update/retain action for either row, and surface it on a dedicated `incompatibilities` result field distinct from the ordinary action list. This is the owner-approved v1 boundary for this reconciliation module: a pair collision always rejects, and a smart pair-merge or rename protocol is explicitly out of scope for this module, to be designed as part of a future sync apply path rather than invented here. Test distinct value uids for one remapped pair, including a NULL clear on one side, and a later re-import/tombstone against the SAME pair after the incompatibility was reported (proving the module does not cache stale state across calls).

Review finding (MEDIUM, Pass-2 audit B-5, source-verified — same reject-incompatible boundary): a separately-created local `custom_field_defs` definition can share a `col_name` with an incoming definition while carrying a different def uid (`custom_field_defs.col_name TEXT NOT NULL UNIQUE`, migration 001). UID reconciliation would declare the incoming definition "missing" and its insert would violate the UNIQUE constraint; alias-matching by col_name would be worse, since it would merge two genuinely distinct stable definitions and misattach every value pair. Implement the identical pre-write scan pattern used for B-3, scoped to custom_field_defs.col_name: when a col_name resolves to two different def uids across local and incoming, emit a col_name-collision incompatibility on the same `incompatibilities` result field — zero insert/update/retain action for either row. Do not treat col_name as D-02 identity; a rename/migration protocol remains out of scope for this module, matching B-3's boundary.

Document, in a code comment on the exported `incompatibilities` result type, that ANY non-empty incompatibilities list is a whole-restore-reject signal per D-05 — Plan 17-08 must never apply a partial result when incompatibilities are present.</action>
  <verify><automated>npm test -- src/backup/reconciliation.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>All decision outcomes are dependency-free, deterministic, and callable by both restore orchestration and a future sync caller; the entity-policy registry lists categories/profile/events with their documented no-delete-path LWW policy alongside the tombstone-bearing tables, the profile fixture uses 17-02's exact RESERVED_PROFILE_UID constant, and the categories fixture uses 17-02's exact four RESERVED_CATEGORY_UIDS constants (round-2 confirmation review HIGH); a reference-to-tombstoned-parent test proves the fallback resolution, including a categoryUid absent from the destination's reconciled category set resolving to the caller-supplied fallback (null); a live-row-plus-tombstone-same-uid test proves the two uniqueness domains never falsely reject D-03's delete-then-restore-newer state, across older-tombstone/newer-row, equal-timestamp, and newer-tombstone cases; a mandatory-parent-survival test proves every contact-child relationship AND both custom_field_values parents block rather than insert/update when their declared parent does not survive; pair-uid and col_name collision tests prove a whole-restore incompatibility is reported with zero actions for the colliding rows, never a silent pair/col_name UPSERT.</acceptance_criteria>
  <done>A single tested reconciliation module owns mutable-row, tombstone, dangling-parent-reference, mandatory-parent-survival, and non-uid collision-rejection conflict semantics for every mergeable table in the schema, exposing both the two-sided local/incoming reconciliation contract and the same-file survivor primitive Plan 17-08's whole-file validation reuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Backup/sync input → reconciliation | Externalized row state is compared to local durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-07 | Tampering | reconciliation policy | high | mitigate | Pure tests cover UID identity, LWW, tombstone precedence, and explicit NULL clears. |
| T-17-08 | Tampering | permanent field deletion | high | mitigate | Capture definition/value UIDs before removal within the existing transaction. |
| T-17-46 | Repudiation | live-row/tombstone uniqueness domains | high | mitigate | Live-row uid and tombstone entity_uid are independent uniqueness domains; a legitimate delete-then-restore-newer coexistence is resolved by the one shared comparison helper instead of falsely rejected as a duplicate (Pass-2 audit B-1). |
| T-17-47 | Tampering | mandatory-parent survival | high | mitigate | Parent entity types reconcile before children; a child whose local or incoming parent did not survive is a blocked, non-writable action, covering every mandatory contact-child FK and both custom_field_values parents (Pass-2 audit B-2). |
| T-17-48 | Tampering | pair-uid / col_name collision | high | mitigate | A custom_field_values pair-key collision or a custom_field_defs.col_name collision between independently-created local and incoming rows is reported as a whole-restore incompatibility with zero actions, never a silent pair/col_name UPSERT (Pass-2 audit B-3, B-5 — owner-ratified reject-incompatible rule). |
| T-17-63 | Repudiation | categories singleton-seed UID identity | high | mitigate | The entity-policy registry and its test fixtures reconcile categories against 17-02's four fixed RESERVED_CATEGORY_UIDS constants, and the reference-to-tombstoned-parent fallback primitive extends to a categoryUid absent from the destination's reconciled set — extending the identical owner-approved profile reserved-UID pattern so a restore onto a fresh install can never insert four permanent, undeletable duplicate categories (round-2 confirmation review HIGH). |
| T-17-SC | Tampering | package installs | high | mitigate | No package installation occurs in this plan. |
</threat_model>

<verification>Run the two focused Vitest suites and TypeScript compilation.</verification>
<success_criteria>Every existing hard-delete writer and reconciliation edge required by D-01–D-05 is covered before export or restore is built, including the Pass-2 audit's uniqueness-domain, parent-survival, and collision-rejection corrections.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-04-SUMMARY.md` when done</output>
