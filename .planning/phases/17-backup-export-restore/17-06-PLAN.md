---
phase: 17-backup-export-restore
plan: 06
type: tdd
wave: 4
depends_on: [17-02, 17-03, 17-04, 17-05, 17-12]
files_modified: [src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts, src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/db/field-values-dao.ts, src/db/field-values-dao.test.ts, src/db/events-dao.ts, src/db/events-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/data-revision-dao.test.ts, src/backup/auto-backup-policy.ts, src/backup/auto-backup-policy.test.ts, src/services/backup/saf-storage.ts, src/services/backup/saf-storage.test.ts, src/services/backup/backup-service.ts, src/services/backup-sweep.ts, App.tsx]
autonomous: true
requirements: [BKP-02]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Eligible foreground launches create one distinct verified automatic snapshot only when data changed and cadence elapsed."
    - "The 'data changed' decision compares app_settings.data_revision (a monotonic counter, not a timestamp) by inequality against the last-stored last_backup_data_revision snapshot, so a same-second edit can never be indistinguishable from no change (cycle-2 review HIGH — the prior MAX(modified_at) design provably missed same-second edits)."
    - "Every create/update DAO chokepoint for an exportable table (contacts, interactions, fuel, contact_links, custom_field_defs, custom_field_values, events, app_settings) advances data_revision by exactly one inside its existing write transaction; every delete/tombstone path already advances it for free via 17-02's tombstone core."
    - "Retention prunes only after verified write and cannot remove the newest verified automatic snapshot, even under a non-owned lookalike filename, an unreadable listed file, or a device clock rollback."
    - "Lost SAF access leaves configuration diagnosable and reports degraded health rather than false protection."
  artifacts:
    - "src/backup/auto-backup-policy.ts"
    - "src/services/backup/saf-storage.ts"
    - "src/services/backup-sweep.ts"
  key_links:
    - "App registers the backup sweep before the foreground launch trigger."
    - "Verified SAF write precedes success metadata and pruning."
    - "Every exportable-table writer calls bumpDataRevisionCore (src/db/data-revision-dao.ts, from 17-02) inside its own already-open inWriteTransaction; delete paths get this for free via the tombstone core, create/update paths get it from this plan's Task 1."
  prohibitions:
    - "A shared manual export must not be counted as verified automatic protection. [flagged-unverified]"
    - "The changed-since-last-backup check must not compare modified_at/deleted_at timestamps by inequality (>) as its sole signal — same-second collisions are a proven miss. [flagged-unverified]"
---

<objective>
Add foreground-only automatic SAF snapshots with safe rotation and truthful health metadata, backed by a change signal that cannot miss a same-second edit.

Purpose: protect current local data without making an unfulfilled background-scheduling promise, and without a silent, indefinite same-second detection gap.
Output: a data-revision counter threaded through every exportable write chokepoint, pure due/rotation policy, SAF adapter, backup sweep registration.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/services/launch-sweep.ts
@src/services/field-sweep.ts
@App.tsx
@src/db/app-settings-dao.ts
@src/db/data-revision-dao.ts
@src/db/tombstones-dao.ts
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thread the data-revision counter through every exportable create/update chokepoint</name>
  <read_first>src/db/data-revision-dao.ts (bumpDataRevisionCore/readDataRevision, from 17-02); src/db/tombstones-dao.ts (already calls bumpDataRevisionCore on every tombstone insert, from 17-02); src/db/contacts-dao.ts createContactFull/updateContactFull/archiveContact/restoreContact/setContactPhoto/clearContactPhoto; src/db/recency-dao.ts recordTouchpoint/editTouchpointFull/createContactWithInteraction; src/db/fuel-dao.ts addFuelCore/editFuelCore/confirmFuelCore; src/db/contact-links-dao.ts addLinkCore/updateLinkCore/applyLinkDiff; src/db/field-ddl.ts createField; src/db/field-defs-dao.ts renameField/reorderFields/changeFieldOptions/updateFieldCuration/updateFieldShareWithAi/quarantineField/restoreField; src/db/field-values-dao.ts upsertValueCore; src/db/events-dao.ts recordEventCore; src/db/app-settings-dao.ts updateAppSettings/acknowledgeProvider</read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts, src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/db/field-values-dao.ts, src/db/field-values-dao.test.ts, src/db/events-dao.ts, src/db/events-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/data-revision-dao.test.ts</files>
  <behavior>
    - Creating, editing, archiving, restoring, or photo-updating a contact advances app_settings.data_revision by exactly one per call.
    - Recording, editing, or creating-with-interaction a touchpoint; adding, editing, or confirming fuel; adding, editing, or diff-applying a link; creating, renaming, reordering, retyping, curating, sharing-toggling, quarantining, or restoring a custom field definition; upserting a normalized field value; recording an event; and a general (non-backup-bookkeeping) app_settings update each advance data_revision by exactly one per call.
    - Two calls landing in the same wall-clock second (e.g., a contact edit immediately followed by a fuel add) each independently advance data_revision, so a same-second collision that would defeat a MAX(modified_at) comparison is provably detectable via inequality on this counter.
    - Calling any of these functions with input that throws before its transaction commits (validation failure, constraint violation) does not advance data_revision — the bump lives inside the same transaction as the writer's own mutation, so a rollback undoes both together.
  </behavior>
  <action>Review finding (HIGH, cycle-2, source-verified — the concrete remedy for 17-06's own same-second marker bug): 17-02 already defines `bumpDataRevisionCore(exec)` in `src/db/data-revision-dao.ts` and wires it into the shared tombstone-insertion core, so every delete/tombstone path (17-02 purgeContact, 17-03 interaction/fuel/link deletes, 17-04 permanent field-definition delete) already advances `data_revision` with no further edit here. This task closes the other half: every CREATE/UPDATE chokepoint for an exportable table. Add exactly one `await bumpDataRevisionCore(exec);` call inside each of the following already-open `inWriteTransaction` bodies (never opening a second transaction, never calling it outside the existing transaction): `contacts-dao.ts` — `createContactFull`, `updateContactFull`, `archiveContact`, `restoreContact`, `setContactPhoto`, `clearContactPhoto`; `recency-dao.ts` — `recordTouchpoint`, `editTouchpointFull`, `createContactWithInteraction` (its own contacts+interaction insert, not `deleteTouchpoint`, which 17-03 already covers via the tombstone core); `fuel-dao.ts` — `addFuelCore`, `editFuelCore`, `confirmFuelCore` (not `deleteFuelCore`, already covered); `contact-links-dao.ts` — `addLinkCore`, `updateLinkCore`, and `applyLinkDiff`'s insert/update branches (its delete branch is already covered via the tombstone core 17-03 wires in); `field-ddl.ts` — `createField` (not `dropField`/`deleteOrQuarantineField`/`expireFieldIfStale`'s delete branch, already covered; the quarantine branch of `deleteOrQuarantineField` IS a mutation with no tombstone, so it also needs the bump — add it there too); `field-defs-dao.ts` — `renameField`, `reorderFields`, `changeFieldOptions`, `updateFieldCuration`, `updateFieldShareWithAi`, `quarantineField`, `restoreField`; `field-values-dao.ts` — `upsertValueCore`; `events-dao.ts` — `recordEventCore`; `app-settings-dao.ts` — `updateAppSettings` and `acknowledgeProvider` (general user-facing settings changes only — this deliberately EXCLUDES whatever narrower internal write Plan 17-06's own Task 2/3 below use to persist ONLY automatic-backup bookkeeping fields such as `last_backup_data_revision`/`last_automatic_backup_at`/health state: if that bookkeeping write also bumped `data_revision`, the counter would advance past its own just-recorded `last_backup_data_revision` snapshot on every successful backup, making the very next due-cycle think data changed even with zero user edits — defeating "only when data changed." Keep the bookkeeping write as a narrow, separate, non-bumping statement, distinct from `updateAppSettingsCore`/`updateAppSettings`.). Do not add the bump to `getAppSettings` or any other pure read. Add ONE new dedicated regression file `src/db/data-revision-dao.test.ts` (extending 17-02's file of the same name) that imports each of the above writer functions against a freshly migrated node:sqlite db and asserts `readDataRevision` advances by exactly one per call, including two calls made back-to-back within the same wall-clock second — this is the completeness proof the review demanded ("a proof that every exportable mutation advances it"), kept in one file rather than duplicated per-DAO. Existing per-DAO test files for the functions above need no behavior changes (the bump is an internal side effect on a different table); run the full suite, not just the touched files, since an unrelated existing test asserting an exact `app_settings` row snapshot after an unrelated write is the one class of test this incidental extra UPDATE could affect.</action>
  <verify><automated>npm test -- --run && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every listed writer function measurably advances `data_revision` by exactly one, including two same-second calls; a throwing writer leaves `data_revision` unchanged; the full Vitest suite remains green (no existing test broken by the incidental app_settings write); no bookkeeping-only backup-health write anywhere in this plan bumps the counter.</acceptance_criteria>
  <done>Every exportable-table mutation in the app — create, update, and delete alike — advances one monotonic, collision-proof counter.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove one due automatic snapshot is verified before rotation</name>
  <read_first>17-CONTEXT.md D-08 through D-10; src/services/photos/photo-storage.ts; src/backup/export-manifest.ts; src/db/data-revision-dao.ts; 17-RESEARCH.md SAF automatic snapshot sequence</read_first>
  <files>src/backup/auto-backup-policy.ts, src/backup/auto-backup-policy.test.ts, src/services/backup/saf-storage.ts, src/services/backup/saf-storage.test.ts, src/services/backup/backup-service.ts</files>
  <behavior>
    - A changed eligible state produces one timestamped automatic filename, verifies the local and SAF copies, persists success metadata, then prunes matching older files.
    - An unchanged state, a not-yet-due state, or concurrent sweep invocation creates no duplicate file.
    - The newest verified automatic snapshot survives any retention boundary; SAF access failure produces lost-folder state and skips pruning.
    - Retention correctly ignores filenames that merely look similar but are not this app's owned automatic snapshots, tolerates a listed-but-unreadable file, and never deletes forward when a device clock rollback makes a "newer" file's timestamp look older than a retained one.
    - A same-second edit landing in the exact wall-clock second of the last successful automatic backup is still detected as "changed" on the very next due cycle — proven by a dedicated regression, not merely asserted.
  </behavior>
  <action>Write policy/storage tests first. Review finding (HIGH, cycle-2, source-verified, supersedes the cycle-1 MEDIUM this task previously closed): the "exportable data changed" marker cannot be `MAX(modified_at)` compared by `newMarker > storedMarker` — every local timestamp (`formatLocalDate`/`localDateTime`) is SECOND-granularity, so an edit landing in the exact same wall-clock second as the stored marker leaves `newMarker === storedMarker`, silently and indefinitely missed until an unrelated later-second write happens to advance the max (verified false claim: the cycle-1 text asserted "a same-second edit after a same-second backup is still eligible next due cycle," which is incorrect). Do not widen any timestamp's precision — this is a non-timestamp change-signal problem per 17-CONTEXT's explicit fix direction, and it must not touch D-03's reconciliation same-second tie-break rule (a separate, decided Merge-conflict concern). Instead, use Task 1's `readDataRevision(exec)` (a monotonic integer, bumped by every exportable create/update/delete this plan and 17-02/17-03/17-04 now cover) and compare it to the last-stored `last_backup_data_revision` (Plan 17-12) by simple inequality: `newRevision !== storedRevision` (equivalently `>`, since the counter only increases) — an integer comparison that cannot collide across two calls the way a wall-clock scalar can, since `bumpDataRevisionCore` runs inside each writer's own transaction and always advances by exactly one regardless of how many writes land in the same second. This read must also run inside `inReadSnapshot` (17-05) for the same read-consistency reason export does. Implement pure due/change/retention decisions and an isolated StorageAccessFramework adapter that creates only strict-prefix automatic names, writes bytes, re-reads them for parser/decrypt verification, and treats each use as an access probe. Add retention tests for: a non-owned file whose name merely resembles the automatic-snapshot prefix (must be ignored, never pruned or counted), a SAF listing entry that throws/fails to read (must be skipped, not crash the sweep), and a simulated device clock rollback where a freshly written file's timestamp sorts before an already-retained one (retention must key off the recorded verified-write order/metadata, not solely a parsed filename timestamp, so it never deletes forward past the true newest verified snapshot). Add the specific same-second regression the review demanded: seed `data_revision = N` and `last_backup_data_revision = N` (simulating "just backed up"), call a writer function within the same synthetic second (or with an injected same-value clock) to advance `data_revision` to `N + 1`, and assert the policy now reports "changed" — proving the inequality catches what a timestamp-max comparison would have missed. Extend backup-service with a single-flight automatic operation whose ordered sequence is generate, verify, create/write SAF, verify, persist success (the bookkeeping-only write from Task 1's note — never bumps data_revision itself), then prune. Implement D-08 exactly and preserve manual-export separation.</action>
  <verify><automated>npm test -- src/backup/auto-backup-policy.test.ts src/services/backup/saf-storage.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every automatic success is backed by a verified distinct SAF file, every failed access/write leaves previous verified snapshots and health metadata intact, the data-revision-based change check is proven to detect every exportable-table mutation type including a same-second one, and the three named retention edge cases (non-owned lookalike name, unreadable listed file, clock rollback) are covered by tests.</acceptance_criteria>
  <done>One complete automatic snapshot path is safe, rotating, concurrency-controlled, and accurately detects real data changes with no same-second blind spot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register foreground-only automatic backup and persist truthful health</name>
  <read_first>src/services/launch-sweep.ts; src/services/field-sweep.ts; App.tsx bootstrap effect; src/db/app-settings-dao.ts; 17-CONTEXT.md D-10 and D-18</read_first>
  <files>src/services/backup-sweep.ts, App.tsx, src/db/app-settings-dao.ts, src/backup/auto-backup-policy.test.ts</files>
  <behavior>
    - The hook runs once for a real foreground launch after database readiness and never from module import or headless notification/widget paths.
    - A later foreground launch catches up with one eligible backup rather than a backlog series.
    - Health derives solely from a verified automatic SAF result and meaningful exportable data-revision changes, never from a timestamp comparison.
  </behavior>
  <action>Implement registerBackupSweep with the existing lazy-executor, module-level one-shot pattern; register it in App before installSweepTrigger. Use app-settings DAO fields to record last verified automatic success/last_backup_data_revision and folder accessibility, with Logger-safe failure handling, via the narrow bookkeeping-only write Task 1/2 deliberately keep separate from `updateAppSettingsCore` (so recording success never itself bumps `data_revision`). Keep the reusable policy free of AppState so future inexact background work can call it, while D-10 limits this phase to foreground launches.</action>
  <verify><automated>npm test -- src/backup/auto-backup-policy.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>The automatic hook cannot run before migration readiness or from an unrelated headless path, and manual sharing never makes health healthy.</acceptance_criteria>
  <done>Automatic backup now runs safely through the established foreground launch lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| app → SAF provider | Android/provider permission and file operations can fail or be revoked. |
| launch sweep → backup service | Reentrant foreground lifecycle events invoke durable file writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-12 | Denial of service | SAF folder access | high | mitigate | Verify every write, retain diagnostics, surface lost-folder state, and avoid destructive prune on failure. |
| T-17-13 | Tampering | automatic file rotation | high | mitigate | Strict owned filename matching; write/verify before success metadata/prune; newest verified file protected. |
| T-17-14 | Repudiation | backup health | medium | mitigate | Health derives only from recorded verified automatic results, using an integer data-revision comparison rather than a same-second-collidable timestamp comparison. |
| T-17-39 | Repudiation | same-second change detection | high | mitigate | app_settings.data_revision, bumped by every exportable create/update (this plan's Task 1) and delete (17-02's tombstone core), compared by inequality — not MAX(modified_at) compared by > — so a same-second edit can never be silently indistinguishable from no change. |
| T-17-SC | Tampering | package installs | high | mitigate | SAF/package imports only follow approved 17-01 dependencies. |
</threat_model>

<verification>Run policy/storage tests, tsc/colors, then the full suite after the wave.</verification>
<success_criteria>Automatic snapshots are accurate, foreground-only, fail visibly without destroying the last known protection, and can never silently miss a same-second edit.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-06-SUMMARY.md` when done</output>
