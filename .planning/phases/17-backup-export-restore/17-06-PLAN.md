---
phase: 17-backup-export-restore
plan: 06
type: tdd
wave: 4
depends_on: [17-02, 17-03, 17-04, 17-05, 17-12]
files_modified: [src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts, src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/db/field-values-dao.ts, src/db/field-values-dao.test.ts, src/db/events-dao.ts, src/db/events-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/data-revision-dao.test.ts, src/db/favourites-dao.ts, src/db/favourites-dao.test.ts, src/db/ring-seq-dao.ts, src/db/ring-seq-dao.test.ts, src/db/profile-dao.ts, src/db/profile-dao.test.ts, src/db/field-type-change.ts, src/db/field-type-change.test.ts, src/db/snooze-dao.ts, src/db/snooze-dao.test.ts, src/backup/auto-backup-policy.ts, src/backup/auto-backup-policy.test.ts, src/services/backup/saf-storage.ts, src/services/backup/saf-storage.test.ts, src/services/backup/backup-service.ts, src/services/backup-sweep.ts, App.tsx]
autonomous: true
requirements: [BKP-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Eligible foreground launches create one distinct verified automatic snapshot only when data changed and cadence elapsed."
    - "The 'data changed' decision compares app_settings.data_revision (a monotonic counter, not a timestamp) by inequality against the last-stored last_backup_data_revision snapshot, so a same-second edit can never be indistinguishable from no change (cycle-2 review HIGH — the prior MAX(modified_at) design provably missed same-second edits)."
    - "Every create/update DAO chokepoint for an exportable table (contacts, favourite/ring-seq ordering, profile, interactions, fuel, contact_links, custom_field_defs including type changes, custom_field_values, events, snooze state, app_settings) advances data_revision by exactly one PER OUTER OPERATION inside its existing write transaction; every delete/tombstone path already advances it for free via 17-02's tombstone core; the WRITER CENSUS TABLE in 17-AUDIT-A-FINDINGS.md is the completeness checklist (A-01/A-02/A-03/A-04/A-10 close its four HIGH + one MEDIUM gaps)."
    - "Leaf SQL cores composed by more than one outer writer (upsertValueCore, recordEventCore, addLinkCore/updateLinkCore/removeLinkCore, updateContactMetadataCore, insertInteractionCore, recomputeLastContactCore) never call bumpDataRevisionCore directly; only the owning outer/public operation bumps, exactly once, so createContactFull's N-value seeding, createField's per-contact fan-out, archiveContact/restoreContact's composed event insert, and a multi-row applyLinkDiff each bump once, never +N or +2 (cycle-2 review A-08/A-09's self-contradiction, now resolved by this single coalescing rule)."
    - "Retention prunes only after verified write and cannot remove the newest verified automatic snapshot, even under a non-owned lookalike filename, an unreadable listed file, or a device clock rollback."
    - "Lost SAF access leaves configuration diagnosable and reports degraded health rather than false protection."
  artifacts:
    - "src/backup/auto-backup-policy.ts"
    - "src/services/backup/saf-storage.ts"
    - "src/services/backup-sweep.ts"
  key_links:
    - "App registers the backup sweep before the foreground launch trigger."
    - "Verified SAF write precedes success metadata and pruning."
    - "Every exportable-table writer calls bumpDataRevisionCore (src/db/data-revision-dao.ts, from 17-02) inside its own already-open inWriteTransaction; delete paths get this for free via the tombstone core, create/update paths get it from this plan's Task 1 under the leaf-never-bumps/outer-bumps-once coalescing rule."
    - "Task 3's automatic-success bookkeeping write calls Plan 17-12's dedicated, narrowly-typed backup-health core (never updateAppSettingsCore, never a bumping path), so recording a successful backup can never itself advance data_revision (self-inflation guard)."
  prohibitions:
    - "A shared manual export must not be counted as verified automatic protection. [flagged-unverified]"
    - "The changed-since-last-backup check must not compare modified_at/deleted_at timestamps by inequality (>) as its sole signal — same-second collisions are a proven miss. [flagged-unverified]"
---

<objective>
Add foreground-only automatic SAF snapshots with safe rotation and truthful health metadata, backed by a change signal that cannot miss a same-second edit.

Purpose: protect current local data without making an unfulfilled background-scheduling promise, and without a silent, indefinite same-second detection gap.
Output: a data-revision counter threaded through every exportable write chokepoint, pure due/rotation policy, SAF adapter, backup sweep registration.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/services/launch-sweep.ts
@src/services/field-sweep.ts
@App.tsx
@src/db/app-settings-dao.ts
@src/db/data-revision-dao.ts
@src/db/tombstones-dao.ts
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
@.planning/phases/17-backup-export-restore/17-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thread the data-revision counter through every exportable create/update chokepoint, exactly once per outer operation</name>
  <read_first>src/db/data-revision-dao.ts (bumpDataRevisionCore/readDataRevision, from 17-02); src/db/tombstones-dao.ts (already calls bumpDataRevisionCore on every tombstone insert, from 17-02); src/db/contacts-dao.ts createContactFull/updateContactFull/updateContactMetadataCore/archiveContact/restoreContact/setContactPhoto/clearContactPhoto; src/db/recency-dao.ts recordTouchpoint/editTouchpointFull/createContactWithInteraction/insertInteractionCore/recomputeLastContactCore; src/db/fuel-dao.ts addFuelCore/editFuelCore/confirmFuelCore/addFuel/editFuel/confirmFuel; src/db/contact-links-dao.ts addLinkCore/updateLinkCore/removeLinkCore/addLink/updateLink/removeLink/applyLinkDiff; src/db/field-ddl.ts createField/deleteOrQuarantineField/dropFieldValues; src/db/field-defs-dao.ts renameField/reorderFields/changeFieldOptions/updateFieldCuration/updateFieldShareWithAi/quarantineField/restoreField; src/db/field-values-dao.ts upsertValueCore/upsertValue; src/db/events-dao.ts recordEventCore/recordEvent; src/db/app-settings-dao.ts updateAppSettings/acknowledgeProvider; src/db/favourites-dao.ts setFavouriteRank/clearFavouriteRank/rewriteFavouriteRanks; src/db/ring-seq-dao.ts rewriteRingSeq; src/db/profile-dao.ts setProfilePhoto/clearProfilePhoto; src/db/field-type-change.ts applyTypeChange; src/db/snooze-dao.ts snoozeContact/clearSnooze; 17-AUDIT-A-FINDINGS.md WRITER CENSUS TABLE (the completeness checklist)</read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/fuel-dao.ts, src/db/fuel-dao.test.ts, src/db/contact-links-dao.ts, src/db/contact-links-dao.test.ts, src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/db/field-values-dao.ts, src/db/field-values-dao.test.ts, src/db/events-dao.ts, src/db/events-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/data-revision-dao.test.ts, src/db/favourites-dao.ts, src/db/favourites-dao.test.ts, src/db/ring-seq-dao.ts, src/db/ring-seq-dao.test.ts, src/db/profile-dao.ts, src/db/profile-dao.test.ts, src/db/field-type-change.ts, src/db/field-type-change.test.ts, src/db/snooze-dao.ts, src/db/snooze-dao.test.ts</files>
  <behavior>
    - COALESCING RULE (cycle-2 review A-08/A-09, resolving the prior self-contradictory "exactly one bump per call"): non-mutexed SQL *cores* — functions that assume an already-open transaction and exist to be composed by more than one caller (`upsertValueCore`, `recordEventCore`, `addLinkCore`/`updateLinkCore`/`removeLinkCore`, `updateContactMetadataCore`, `insertInteractionCore`, `recomputeLastContactCore`) — NEVER call `bumpDataRevisionCore` directly. Every PUBLIC/outer write operation (a function that itself opens `inWriteTransaction`) calls `bumpDataRevisionCore(exec)` EXACTLY ONCE, at the end of its own transaction body, before commit. A composed outer operation that invokes a leaf core N times therefore still bumps exactly once for the whole call, never +N or +2.
    - Creating, editing, archiving, restoring, or photo-updating a contact advances app_settings.data_revision by exactly one per call — `archiveContact`/`restoreContact` compose `recordEventCore` (a non-bumping leaf) and must NOT double-bump.
    - Recording, editing, or creating-with-interaction a touchpoint (the standalone mutexed wrappers only); adding, editing, or confirming fuel (standalone wrappers only); adding, editing, or removing a link one at a time (standalone wrappers only), or diff-applying a batch of links via `applyLinkDiff` (ONE bump for the whole diff, not one per inserted/updated/removed row, and no bump at all when the diff is a true no-op); creating, renaming, reordering, retyping (`applyTypeChange`), curating, sharing-toggling, quarantining, or restoring a custom field definition; upserting a normalized field value through the standalone `upsertValue` wrapper (never through the composed `upsertValueCore` leaf); recording an event through the standalone `recordEvent` wrapper (never through the composed `recordEventCore` leaf); marking, clearing, or reordering a favourite (skipping the bump only on `rewriteFavouriteRanks`' accepted empty-list no-op); reordering the orrery ring (`rewriteRingSeq`, same empty-list no-op exception); setting or clearing the self profile photo; snoozing or clearing a snooze (bumped explicitly by the outer function itself, never inferred from its composed `recordEventCore` insert); and a general (non-backup-bookkeeping) app_settings update — each advance data_revision by exactly one per call.
    - Two calls landing in the same wall-clock second (e.g., a contact edit immediately followed by a fuel add) each independently advance data_revision, so a same-second collision that would defeat a MAX(modified_at) comparison is provably detectable via inequality on this counter.
    - Calling any of these functions with input that throws before its transaction commits (validation failure, constraint violation) does not advance data_revision — the bump lives inside the same transaction as the writer's own mutation, so a rollback undoes both together.
    - A populated `createContactFull` (N seeded pair inserts via `upsertValueCore` plus M submitted custom values) and a populated `createField` (K existing contacts, each getting an `upsertValueCore` pair insert) each advance data_revision by exactly ONE per call, not N/M/K+1 — the concrete regression disproving A-08's "+N" failure mode.
    - `archiveContact` and `restoreContact` each advance data_revision by exactly ONE per call despite composing `recordEventCore` — the concrete regression disproving A-08's "archiveContact would bump directly and then unconditionally call the newly bumping recordEventCore... +2" failure mode.
    - A multi-row `applyLinkDiff` call (at least one insert, one update, and one remove in the same call) advances data_revision by exactly ONE, not once per composed core call.
  </behavior>
  <action>Review finding (HIGH, cycle-2, source-verified — supersedes this task's prior draft, which the audit caught bumping both an outer operation and the leaf cores it composes, a real +2/+N bug, A-08/A-09): 17-02 already defines `bumpDataRevisionCore(exec)` in `src/db/data-revision-dao.ts` and wires it into the shared tombstone-insertion core, so every delete/tombstone path (17-02 purgeContact, 17-03 interaction/fuel/link deletes, 17-04 permanent field-definition delete) already advances `data_revision` with no further edit here. This task closes the other half — every CREATE/UPDATE chokepoint for an exportable table — under the owner-approved coalescing rule stated in Behavior above: LEAF cores never bump; the owning OUTER/public operation bumps exactly once.

LEAVES — do NOT add a bump inside any of these (they stay exactly as 17-02/17-03/17-04/earlier phases left them): `upsertValueCore` (field-values-dao.ts), `recordEventCore` (events-dao.ts), `addLinkCore`/`updateLinkCore`/`removeLinkCore` (contact-links-dao.ts), `updateContactMetadataCore`/`insertInteractionCore`/`recomputeLastContactCore` (contacts-dao.ts / recency-dao.ts, already leaves by construction), `dropFieldValues` (field-ddl.ts, tombstone-covered, not this task's concern).

OUTER OPERATIONS — add exactly one `await bumpDataRevisionCore(exec);` call inside each of the following already-open `inWriteTransaction` bodies (never a second transaction, never outside the existing transaction, never inside a leaf):
- `contacts-dao.ts` — `createContactFull` (ONE bump despite composing `upsertValueCore` N times for the seeded pair matrix plus M times for submitted values, and conditionally `insertInteractionCore`/`recomputeLastContactCore`), `updateContactFull` (ONE bump despite composing `upsertValueCore` per submitted custom value and conditionally the interaction/recompute leaves), `archiveContact`, `restoreContact` (ONE bump each despite composing `recordEventCore` — the concrete A-08 fix: `recordEventCore` never bumps, so archive/restore cannot become +2), `setContactPhoto`, `clearContactPhoto`.
- `recency-dao.ts` — `recordTouchpoint`, `editTouchpointFull`, `createContactWithInteraction` (its own contacts+interaction insert, not `deleteTouchpoint`, already covered via the tombstone core). These three are outer public wrappers, not leaves — `insertInteractionCore`/`recomputeLastContactCore` stay non-bumping leaves.
- `fuel-dao.ts` — `addFuel`, `editFuel`, `confirmFuel` (the standalone mutexed WRAPPERS; `addFuelCore`/`editFuelCore`/`confirmFuelCore` stay non-bumping leaves so a future multi-attach composer, per the fuel-dao module header, can compose them under its own single outer bump without doubling).
- `contact-links-dao.ts` — `addLink`, `updateLink`, `removeLink` (the standalone mutexed WRAPPERS; `addLinkCore`/`updateLinkCore`/`removeLinkCore` stay non-bumping leaves), and `applyLinkDiff` (ONE bump for the whole diff — the concrete A-08 fix for "applyLinkDiff can invoke multiple newly bumping insert/update cores": track whether the diff body performed at least one insert/update/remove and bump once at the end only if so; an empty/no-op diff — seeded === current, nothing to insert/update/remove — does not bump, mirroring the favourites/ring-seq no-op convention below).
- `field-ddl.ts` — `createField` (ONE bump despite composing `upsertValueCore` once per existing contact); the quarantine branch ONLY of `deleteOrQuarantineField` (a mutation with no tombstone, so it needs its own bump; the delete branch already bumps once via the tombstone core inside `dropFieldValues`/`dropField` — do not ALSO bump `deleteOrQuarantineField` unconditionally at its top, which would double-count the delete branch).
- `field-defs-dao.ts` — `renameField`, `reorderFields` (ONE bump per call, placed once in the transaction body, never once per loop iteration over `orderedIds`), `changeFieldOptions`, `updateFieldCuration`, `updateFieldShareWithAi`, `quarantineField`, `restoreField`.
- `field-values-dao.ts` — `upsertValue` (the standalone mutexed WRAPPER only; `upsertValueCore` — which `createContactFull`/`updateContactFull`/`createField` all compose directly — stays a non-bumping leaf; this is the concrete A-08 fix for the create/update/createField "+N" failure mode).
- `events-dao.ts` — `recordEvent` (the standalone mutexed WRAPPER only, for API completeness and any future direct caller; `recordEventCore` — which `archiveContact`/`restoreContact`/`snoozeContact`/`clearSnooze` all compose directly — stays a non-bumping leaf; this is the concrete A-08 fix for the archive/restore "+2" failure mode. `recordEvent` currently has no production caller — grep-verified — so this bump is defensive completeness for the public API surface, not a currently-reachable path; add the regression anyway so a future caller inherits correct behavior by construction).
- `app-settings-dao.ts` — `updateAppSettings` and `acknowledgeProvider` (general user-facing settings changes only). This deliberately EXCLUDES Plan 17-12's `updateAppSettingsCore`, which stays non-bumping: `updateAppSettings` calls that core inside its own already-open transaction and bumps once AFTER it succeeds — the same leaf/outer split as everywhere else in this task. This ALSO deliberately EXCLUDES Plan 17-12's separate, narrowly-typed backup-bookkeeping SQL core (see 17-12 Task 2 — its patch type structurally cannot accept any portable settings field, and it never calls `bumpDataRevisionCore`), which Task 3 below uses ONLY to persist automatic-backup health/success metadata such as `last_backup_data_revision`: if that bookkeeping write bumped `data_revision`, the counter would advance past its own just-recorded `last_backup_data_revision` snapshot on every successful backup, making the very next due-cycle think data changed even with zero user edits — defeating "only when data changed." Do not add the bump to `getAppSettings` or any other pure read.
- **GAP CLOSURE (A-01/A-02/A-03/A-04/A-10 — currently-uncovered exportable-table writers; completeness pass against 17-AUDIT-A-FINDINGS.md's WRITER CENSUS TABLE so no exportable-table writer is left out):**
  - `favourites-dao.ts` — `setFavouriteRank`, `clearFavouriteRank` (ONE unconditional bump each — they always change exactly one row or throw). `rewriteFavouriteRanks` bumps ONCE after a nonempty successful batch; the module's already-accepted empty-`orderedIds` no-op does NOT bump — add a dedicated test proving an empty reorder leaves `data_revision` unchanged while a nonempty one advances it by exactly one, regardless of how many ids were rewritten.
  - `ring-seq-dao.ts` — `rewriteRingSeq` bumps ONCE after a nonempty successful batch, same no-op exception as `rewriteFavouriteRanks` (an empty `orderedIds` is an accepted no-op).
  - `profile-dao.ts` — `setProfilePhoto`, `clearProfilePhoto` (ONE bump each). This file currently has no test file — create `src/db/profile-dao.test.ts` covering set and clear independently, both the existing `changes===1` guard behavior and the new bump.
  - `field-type-change.ts` — `applyTypeChange` (ONE bump, placed after its two existing statements — the `field_history` audit INSERT and the `custom_field_defs` type UPDATE — inside its existing transaction; the history insert itself is transient and is not a separate bump site).
  - `snooze-dao.ts` — `snoozeContact`, `clearSnooze` (ONE bump each, added EXPLICITLY to this task's writer list with a dedicated regression, rather than relying on the incidental fact that both already compose `recordEventCore` in the same transaction — per A-10, a future refactor that removes/suppresses/changes the audit event must not silently remove backup detection. Since `recordEventCore` is a non-bumping leaf under this task's coalescing rule anyway, `snoozeContact`/`clearSnooze` MUST own their bump directly regardless of the event insert.)

Do a final completeness pass before closing this task: cross-reference every row of `17-AUDIT-A-FINDINGS.md`'s WRITER CENSUS TABLE marked "Nominal 17-06" or "GAP" against this task's explicit list above; every such row must be accounted for by name, either as a covered outer operation or as a leaf intentionally left non-bumping with the outer operation that covers it named alongside it.

Add ONE new dedicated regression file `src/db/data-revision-dao.test.ts` (extending 17-02's file of the same name) that imports each of the above OUTER writer functions — never the leaf cores directly — against a freshly migrated node:sqlite db and asserts `readDataRevision` advances by exactly one per call, including: two calls made back-to-back within the same wall-clock second; a populated `createContactFull`/`createField` call (proving N composed-leaf-calls still yields exactly one bump, not N+1); `archiveContact`/`restoreContact` (proving composing `recordEventCore` does not double-bump); a multi-row `applyLinkDiff` (proving one bump for a whole batched diff, not one per row); and the two documented empty-list no-ops (`rewriteFavouriteRanks([])`, `rewriteRingSeq([])`) leaving the counter unchanged — this is the completeness proof the review demanded ("a proof that every exportable mutation advances it"), kept in one file rather than duplicated per-DAO. Existing per-DAO test files for the functions above need no behavior changes beyond adding the specific bump/no-bump assertions named above (the bump is otherwise an internal side effect on a different table); run the full suite, not just the touched files, since an unrelated existing test asserting an exact `app_settings` row snapshot after an unrelated write is the one class of test this incidental extra UPDATE could affect.</action>
  <verify><automated>npm test -- --run && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every listed OUTER writer function measurably advances `data_revision` by exactly one per call, including two same-second calls, a populated multi-composed-leaf call (createContactFull/createField), archive/restore, and a multi-row applyLinkDiff; no leaf core (upsertValueCore, recordEventCore, addLinkCore/updateLinkCore/removeLinkCore) ever bumps directly; the two documented empty-list no-ops leave the counter unchanged; a throwing writer leaves `data_revision` unchanged; the full Vitest suite remains green (no existing test broken by the incidental app_settings write); no bookkeeping-only backup-health write anywhere in this plan bumps the counter; every WRITER CENSUS TABLE row is accounted for by name.</acceptance_criteria>
  <done>Every exportable-table mutation in the app — create, update, and delete alike — advances one monotonic, collision-proof counter exactly once per logical operation, never per composed leaf call.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove one due automatic snapshot is verified before rotation</name>
  <read_first>17-CONTEXT.md D-08 through D-10; src/services/photos/photo-storage.ts; src/backup/export-manifest.ts; src/db/data-revision-dao.ts; 17-RESEARCH.md SAF automatic snapshot sequence</read_first>
  <files>src/backup/auto-backup-policy.ts, src/backup/auto-backup-policy.test.ts, src/services/backup/saf-storage.ts, src/services/backup/saf-storage.test.ts, src/services/backup/backup-service.ts</files>
  <behavior>
    - A changed eligible state produces one timestamped automatic filename, verifies the local and SAF copies, persists success metadata, then prunes matching older files.
    - An unchanged state, a not-yet-due state, or concurrent sweep invocation creates no duplicate file.
    - The newest verified automatic snapshot survives any retention boundary; SAF access failure produces lost-folder state and skips pruning.
    - Retention correctly ignores filenames that merely look similar but are not this app's owned automatic snapshots, tolerates a listed-but-unreadable file, and never deletes forward when a device clock rollback makes a "newer" file's timestamp look older than a retained one.
    - A same-second edit landing in the exact wall-clock second of the last successful automatic backup is still detected as "changed" on the very next due cycle — proven by a dedicated regression, not merely asserted.
  </behavior>
  <action>Write policy/storage tests first. Review finding (HIGH, cycle-2, source-verified, supersedes the cycle-1 MEDIUM this task previously closed): the "exportable data changed" marker cannot be `MAX(modified_at)` compared by `newMarker > storedMarker` — every local timestamp (`formatLocalDate`/`localDateTime`) is SECOND-granularity, so an edit landing in the exact same wall-clock second as the stored marker leaves `newMarker === storedMarker`, silently and indefinitely missed until an unrelated later-second write happens to advance the max (verified false claim: the cycle-1 text asserted "a same-second edit after a same-second backup is still eligible next due cycle," which is incorrect). Do not widen any timestamp's precision — this is a non-timestamp change-signal problem per 17-CONTEXT's explicit fix direction, and it must not touch D-03's reconciliation same-second tie-break rule (a separate, decided Merge-conflict concern). Instead, use Task 1's `readDataRevision(exec)` (a monotonic integer, bumped by every exportable create/update/delete this plan and 17-02/17-03/17-04 now cover) and compare it to the last-stored `last_backup_data_revision` (Plan 17-12) by simple inequality: `newRevision !== storedRevision` (equivalently `>`, since the counter only increases) — an integer comparison that cannot collide across two calls the way a wall-clock scalar can, since `bumpDataRevisionCore` runs inside each writer's own transaction and always advances by exactly one regardless of how many writes land in the same second. This read must also run inside `inReadSnapshot` (17-05) for the same read-consistency reason export does. Implement pure due/change/retention decisions and an isolated StorageAccessFramework adapter that creates only strict-prefix automatic names, writes bytes, re-reads them for parser/decrypt verification, and treats each use as an access probe. Add retention tests for: a non-owned file whose name merely resembles the automatic-snapshot prefix (must be ignored, never pruned or counted), a SAF listing entry that throws/fails to read (must be skipped, not crash the sweep), and a simulated device clock rollback where a freshly written file's timestamp sorts before an already-retained one (retention must key off the recorded verified-write order/metadata, not solely a parsed filename timestamp, so it never deletes forward past the true newest verified snapshot). Add the specific same-second regression the review demanded: seed `data_revision = N` and `last_backup_data_revision = N` (simulating "just backed up"), call a writer function within the same synthetic second (or with an injected same-value clock) to advance `data_revision` to `N + 1`, and assert the policy now reports "changed" — proving the inequality catches what a timestamp-max comparison would have missed. Extend backup-service with a single-flight automatic operation whose ordered sequence is generate, verify, create/write SAF, verify, persist success (via Plan 17-12's dedicated backup-bookkeeping SQL core, named and typed so it cannot accept a portable settings field and never calls `bumpDataRevisionCore` — see Task 1's note), then prune. Implement D-08 exactly and preserve manual-export separation.</action>
  <verify><automated>npm test -- src/backup/auto-backup-policy.test.ts src/services/backup/saf-storage.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Every automatic success is backed by a verified distinct SAF file, every failed access/write leaves previous verified snapshots and health metadata intact, the data-revision-based change check is proven to detect every exportable-table mutation type including a same-second one, and the three named retention edge cases (non-owned lookalike name, unreadable listed file, clock rollback) are covered by tests.</acceptance_criteria>
  <done>One complete automatic snapshot path is safe, rotating, concurrency-controlled, and accurately detects real data changes with no same-second blind spot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register foreground-only automatic backup and persist truthful health</name>
  <read_first>src/services/launch-sweep.ts; src/services/field-sweep.ts; App.tsx bootstrap effect; src/db/app-settings-dao.ts; 17-CONTEXT.md D-10 and D-18</read_first>
  <files>src/services/backup-sweep.ts, App.tsx, src/db/app-settings-dao.ts, src/backup/auto-backup-policy.test.ts</files>
  <behavior>
    - The hook runs once for a real foreground launch after database readiness and never from module import or headless notification/widget paths.
    - A later foreground launch catches up with one eligible backup rather than a backlog series.
    - Health derives solely from a verified automatic SAF result and meaningful exportable data-revision changes, never from a timestamp comparison.
  </behavior>
  <action>Implement registerBackupSweep with the existing lazy-executor, module-level one-shot pattern; register it in App before installSweepTrigger. Use app-settings DAO fields to record last verified automatic success/last_backup_data_revision and folder accessibility, with Logger-safe failure handling, via Plan 17-12's dedicated backup-bookkeeping SQL core (never `updateAppSettingsCore`, never `updateAppSettings`) — its patch type is structurally disjoint from the portable settings snapshot, so recording success can never itself bump `data_revision` (self-inflation guard). Keep the reusable policy free of AppState so future inexact background work can call it, while D-10 limits this phase to foreground launches.</action>
  <verify><automated>npm test -- src/backup/auto-backup-policy.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>The automatic hook cannot run before migration readiness or from an unrelated headless path, and manual sharing never makes health healthy.</acceptance_criteria>
  <done>Automatic backup now runs safely through the established foreground launch lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| app → SAF provider | Android/provider permission and file operations can fail or be revoked. |
| launch sweep → backup service | Reentrant foreground lifecycle events invoke durable file writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-12 | Denial of service | SAF folder access | high | mitigate | Verify every write, retain diagnostics, surface lost-folder state, and avoid destructive prune on failure. |
| T-17-13 | Tampering | automatic file rotation | high | mitigate | Strict owned filename matching; write/verify before success metadata/prune; newest verified file protected. |
| T-17-14 | Repudiation | backup health | medium | mitigate | Health derives only from recorded verified automatic results, using an integer data-revision comparison rather than a same-second-collidable timestamp comparison. |
| T-17-39 | Repudiation | same-second change detection | high | mitigate | app_settings.data_revision, bumped by every exportable create/update (this plan's Task 1) and delete (17-02's tombstone core), compared by inequality — not MAX(modified_at) compared by > — so a same-second edit can never be silently indistinguishable from no change. |
| T-17-40 | Repudiation | data_revision writer-census gaps and double-counting | high | mitigate | Task 1 closes A-01/A-02/A-03/A-04/A-10's five uncovered writers (favourites, ring-seq, profile photo, field type-change, snooze) against the audit's WRITER CENSUS TABLE, and resolves A-08/A-09's self-contradiction with one coalescing rule: leaf SQL cores never bump, only the owning outer/public operation bumps exactly once — proven by dedicated regressions for createContactFull/createField, archive/restore, and a multi-row applyLinkDiff. |
| T-17-SC | Tampering | package installs | high | mitigate | SAF/package imports only follow approved 17-01 dependencies. |
</threat_model>

<verification>Run policy/storage tests, tsc/colors, then the full suite after the wave.</verification>
<success_criteria>Automatic snapshots are accurate, foreground-only, fail visibly without destroying the last known protection, and can never silently miss a same-second edit.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-06-SUMMARY.md` when done</output>
