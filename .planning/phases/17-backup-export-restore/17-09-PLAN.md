---
phase: 17-backup-export-restore
plan: 09
type: execute
wave: 6
depends_on: [17-05, 17-06, 17-07]
files_modified: [src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/screens/BackupScreen.tsx, src/screens/BackupSettingsScreen.tsx, src/screens/backup-health-logic.ts, src/screens/backup-health-logic.test.ts, src/screens/backup-settings-logic.ts, src/screens/backup-settings-logic.test.ts, src/screens/HomeScreen.tsx]
autonomous: true
requirements: [BKP-01, BKP-02, BKP-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Backup & Restore is reachable from a temporary Dashboard link, with no new bottom navigation."
    - "Landing health distinguishes not configured, healthy, stale, and lost SAF folder and bases healthy only on verified automatic files."
    - "Settings safely configures SAF folder, arbitrary whole-day cadence/retention, and passphrase lifecycle with explicit readable-export consent."
  artifacts:
    - "src/screens/BackupScreen.tsx"
    - "src/screens/BackupSettingsScreen.tsx"
  key_links:
    - "Dashboard temporary entry and typed native-stack routes reach landing/settings screens."
    - "Landing actions use backup-service results rather than inventing health state in the screen."
  prohibitions:
    - "The user interface must not present a permanent dashboard card, an empty Sync page, or a manual share as health success. [flagged-unverified]"
---

<objective>
Create the permanent Backup & Restore health/action surface and configuration flow, including the temporary Dashboard entry.

Purpose: make export, automatic protection, and encryption understandable without introducing deferred navigation or cloud scope.
Output: typed routes, health/settings pure logic, themed landing and settings screens.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-UI-SPEC.md
@src/screens/SettingsScreen.tsx
@src/screens/ArchivedContactsScreen.tsx
@src/screens/HomeScreen.tsx
@src/navigation/types.ts
@src/navigation/RootNavigator.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one truthful Backup & Restore landing state reachable</name>
  <read_first>17-UI-SPEC.md Interaction and State Contract and Copywriting Contract; src/screens/HomeScreen.tsx top bar; src/navigation/types.ts; src/navigation/RootNavigator.tsx; src/screens/settings-ai-logic.ts</read_first>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/screens/BackupScreen.tsx, src/screens/backup-health-logic.ts, src/screens/backup-health-logic.test.ts</files>
  <behavior>
    - The Dashboard has an accessible temporary Backup & Restore entry beside existing top-bar entries; it navigates to Backup and no bottom navigation is added.
    - Health resolver distinguishes loading/not configured/healthy/stale/lost folder and derives healthy only from verified automatic result plus current change marker.
    - Landing renders approved calm hero/action copy, Export now, Restore a backup, encryption status, a rare dismissible nudge only after meaningful data, and focus-safe reload placeholders.
  </behavior>
  <action>Write node tests for the health/nudge resolver before screen wiring. Add serializable Backup, BackupSettings, RestorePreview, and RestoreResult routes for the later restore UI, then implement D-16's BackupScreen health/action surface with themed cards, 44px accessible Pressables, cancellation-safe focus reload, stable testIDs, approved UI-SPEC copy, and typed service dispatch. Implement D-18's rare dismissible meaningful-data nudge with reset only when its health condition changes. Manual export invokes the verified share service but does not change health; no navigation-bar or Sync route is introduced.</action>
  <verify><automated>npm test -- src/screens/backup-health-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>A tester can navigate from Dashboard to a landing screen that never flashes false healthy state and whose health/nudge semantics are pure-test-covered.</acceptance_criteria>
  <done>The first production UI slice exposes truthful backup status and manual actions end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure SAF cadence, retention, and encryption lifecycle</name>
  <read_first>17-UI-SPEC.md positive-integer/encryption/folder states; src/screens/SettingsScreen.tsx; src/services/backup/saf-storage.ts; src/services/backup/passphrase-store.ts; src/db/app-settings-dao.ts</read_first>
  <files>src/screens/BackupSettingsScreen.tsx, src/screens/backup-settings-logic.ts, src/screens/backup-settings-logic.test.ts, src/screens/HomeScreen.tsx</files>
  <behavior>
    - Folder selection persists SAF configuration; lost/open-folder unavailable states use the approved recovery message without in-app file browsing.
    - Cadence and retention accept only whole 1..3650 values, retain invalid input, and preserve the last saved setting until Save.
    - Encryption setup confirms matching passphrases, shows irrecoverability warning, supports verified change/future-only/forgotten/disable actions, and gates readable export behind the specified confirmation.
  </behavior>
  <action>Create pure integer/form-state helpers and test them first. The 1..3650 bound the UI enforces MUST be the exact same constant/validator Plan 17-12 exports from `src/db/app-settings-dao.ts` (the day-bound assertion added alongside `assertHour`/`assertToggle`/`assertSunContactId` for the new cadence/retention columns) — import and reuse it in `backup-settings-logic.ts` rather than re-declaring `1`/`3650` as local literals, so the UI can never accept a value the DAO would reject or vice versa (review LOW finding). Implement D-17's temporary accessible Dashboard Backup & Restore entry beside existing Orrery/Settings controls, then implement BackupSettingsScreen with folder picker/open behavior through the SAF adapter, native Alerts for approved destructive/portability confirmations, secure inputs, no passphrase echo, and accessible status/testIDs. Connect all choices to backup/passphrase services and refresh Backup on return. Preserve D-09, D-11–D-13 exactly; do not expose path internals or add a backup-history browser.</action>
  <verify><automated>npm test -- src/screens/backup-settings-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Every documented settings state has a recoverable UI path and invalid settings cannot mutate stored configuration.</acceptance_criteria>
  <done>Users can configure automatic protection and optional encryption from a dedicated settings screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user input → backup services | Folder, cadence, retention, passphrase, and export choices drive local persistence/file operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-22 | Information disclosure | landing/settings UI | high | mitigate | Aggregate-only health, secure inputs, no passphrase/path/key logging, explicit plaintext confirmation. |
| T-17-23 | Spoofing | protection status | high | mitigate | Derive healthy from verified automatic SAF metadata only. |
| T-17-24 | Denial of service | lost SAF permission | medium | mitigate | Preserve diagnostic config and expose reconnect/retry state. |
| T-17-SC | Tampering | package installs | high | mitigate | Native dependencies were approved in 17-01. |
</threat_model>

<verification>Run pure UI logic tests, tsc, color gate, and later physical checks in 17-11.</verification>
<success_criteria>The landing and settings flows are calm, reachable, and never misstate automatic backup health.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-09-SUMMARY.md` when done</output>
