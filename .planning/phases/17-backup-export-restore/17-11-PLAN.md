---
phase: 17-backup-export-restore
plan: 11
type: execute
wave: 8
depends_on: [17-02, 17-03, 17-04, 17-05, 17-06, 17-07, 17-08, 17-09, 17-10]
files_modified: [docs/runbooks/desktop-build-pipeline.md]
autonomous: false
requirements: [BKP-01, BKP-02, BKP-03, BKP-04]
user_setup:
  - service: Android device and SAF provider
    why: Physical device behavior is required for persisted SAF access, provider revocation, native crypto, picker, and share-sheet proof.
    dashboard_config:
      - task: Select a disposable SAF folder and use Android Files provider during release-build UAT.
        location: Pixel device / Android Files
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Node/Vitest, TypeScript, and color checks pass for the completed phase."
    - "A physical Android release build proves manual sharing, SAF persistence/loss, encrypted round-trip, Merge/Replace, and approved UI states using disposable data."
    - "An automated integration test proves a real v6-populated database migrates through the app's full current migration chain (v7 plus Plan 17-08's migration 008, i.e. the imported TARGET_VERSION — not a hardcoded literal, consistent with 17-02's self-parameterized full-chain.test.ts) and round-trips through export and both restore modes correctly, including a contact's category_id surviving translation to the destination's own local category id (round-2 confirmation review ACTIONABLE — coordination consistency with 17-02/17-08's categories and migration-version fixes)."
    - "The tombstone entity_type vocabulary is proven exhaustive against every real DELETE FROM in the final src/db tree, not just the tables each plan individually audited."
    - "A large-photo-library export (~50 contacts, realistic master-sized photos) run through inReadSnapshot completes under a documented duration budget, giving the mutex-held-write-availability tradeoff (17-05/17-06) a measured reference number instead of an unverified assumption (cycle-2 review ACTIONABLE)."
    - "Integration-level (not merely unit-level) regressions prove the real restore-apply/photo-storage/launch-sweep wiring survives a post-commit process-kill simulation, never overwrites a newer local photo with a stale incoming one, garbage-collects an unjournaled staged file without ever calling persistMaster, and that a SecureStore failure, a malicious KDF parameter, and an unknown envelope key each fail closed through the real module boundaries (Pass-3 audit B-9 through B-13, B-16, B-19, B-20)."
  artifacts:
    - "Phase 17 verification evidence in its SUMMARY and device-UAT record."
  key_links:
    - "The physical Pixel validates the native APIs that node fakes cannot prove."
  prohibitions:
    - "Device evidence must not use or disclose real relationship data. [flagged-unverified]"
---

<objective>
Run the phase-wide automated gates and obtain the required owner-led Android proof for native backup/restore behavior.

Purpose: validate the actual SAF, picker, share, crypto, and screen seams with disposable device data.
Output: green automated gates and a release-build UAT record.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/17-backup-export-restore/17-VALIDATION.md
@.planning/phases/17-backup-export-restore/17-UI-SPEC.md
@.planning/phases/17-backup-export-restore/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run complete node, type, color, and package configuration gates</name>
  <read_first>package.json; app.config.ts; 17-VALIDATION.md; all Phase 17 focused test files; docs/runbooks/desktop-build-pipeline.md; 17-AUDIT-B-FINDINGS.md findings 9-13 (photo restore safety) and 16, 19, 20 (encryption fail-closed/envelope posture)</read_first>
  <files>docs/runbooks/desktop-build-pipeline.md</files>
  <action>Run the full Vitest suite, TypeScript no-emit check, color gate, and Expo config inspection after the final dependency/native config is installed. Record exact commands/results and update the build runbook only if this phase introduces a verified config-plugin/prebuild requirement. Compare the implementation's default envelope version and parameter object with the blocking 17-07 physical-Pixel decision record (including that it was measured on a release build, per 17-07's amended checkpoint); do not use node timing as a substitute for that device benchmark. Review finding (MEDIUM): the phase's automated gate must also prove a real v6→current migration path plus a full export→restore round trip on populated fixtures, not only the targeted unit suites each plan already runs in isolation. Add one integration test (or extend an existing one) that: seeds a v6-shaped database (via the migration runner run from v1 through v6, using Phase 16's normalized custom_field_values/cascading-FK shape) with a representative mix of contacts, categories (at least two contacts assigned to DIFFERENT categories, so category_id translation is actually exercised, not merely present), normalized custom-field values (including at least one NULL clear and one custom photo field), fuel, links, tombstones from at least one prior hard delete, and non-default app_settings (including a non-self sun_contact_id); runs the full migration chain to the app's current TARGET_VERSION (imported, never hardcoded — v7 plus Plan 17-08's migration 008 once it lands, matching 17-02's full-chain.test.ts self-parameterization, round-2 confirmation review ACTIONABLE); exports it via export-manifest; restores that export into a second fresh database (migrated to the same current TARGET_VERSION) via both Merge and Replace-all; and asserts the resulting data matches the source (accounting for recomputed last_contact, re-minted local ids, and each restored contact's category_id resolving to the DESTINATION's own local id for the same reserved category uid — round-2 confirmation review HIGH, proving 17-02/17-04/17-08's categories reserved-UID fix end-to-end). This closes the gap between per-plan unit coverage and an actual end-to-end proof that the whole phase composes correctly. Also add one repository-search regression (a repo-wide grep/AST scan over `src/db/*.ts` for `DELETE FROM <table>`) asserting that every table with a real hard-delete writer is represented in the 17-02 tombstone `entity_type` closed vocabulary — this is the completeness check the per-plan audits (17-02/17-03/17-04) individually assumed but never cross-verified against the full, final `src/db` tree after all of this phase's own DAO changes landed.

Coordination note (audit pass 1, avoid duplicating 17-02's work): Plan 17-02 now owns `src/db/migrations/full-chain.test.ts`, a v0-to-current regression through the REAL registered migration array (`migration001`..`migration007`+, via `database.ts`/`runner.ts`, asserted against the imported `TARGET_VERSION` constant rather than a hardcoded literal — round-2 confirmation review ACTIONABLE, so a later migration such as Plan 17-08's 008 requires no edit there) asserting exactly one 007 step, the tombstones table/unique key, `app_settings.data_revision`, the reserved profile UID (`RESERVED_PROFILE_UID` — cycle-2 review B-4), and the four reserved category UIDs (`RESERVED_CATEGORY_UIDS` — round-2 confirmation review HIGH) at their documented defaults. This task's own populated v6→current→export→restore integration test does NOT need to re-prove the bare schema chain — build its v6-populated fixture on top of 17-02's already-proven chain, and additionally seed a non-default `profile.name`/`photo` to prove the reserved-UID row still carries real user data through the chain (not just a bare default row). This task's fixture must also carry a `backupFormatVersion` consistent with 17-05's registry (cycle-2 review B-18) — do not invent a second, unrelated version field when building the export/restore round trip.

Review finding (ACTIONABLE, cycle-2, 17-05/17-06): `inReadSnapshot` and `inWriteTransaction` share one non-reentrant mutex, so an in-progress export or automatic-backup sweep blocks every other app write (including a headless notification-tap/widget write) for the export's full duration — no plan measures this against a realistic photo library. Extend the same populated-fixture integration test above with a "large library" variant: seed roughly 50 contacts each with a ~150-200KB JPEG-sized base64 photo payload (representative of the shipped 512px master size), run `export-manifest`'s `inReadSnapshot`-wrapped export, and record its wall-clock duration. Assert it stays under a generous documented budget (e.g., 5 seconds) as a regression guard against an accidental O(n²) or otherwise pathological read pattern — this is not a tight SLA, it exists to catch a gross correctness-adjacent performance regression, not to tune UX timing. Note the measured duration in the phase SUMMARY so the accepted mutex-held-duration tradeoff documented in `transaction.ts` (17-05) has a concrete reference number.

Review finding (Pass-3 audit — coordinate with, do not duplicate, 17-07's and 17-08's own component-level suites): the full Vitest run this task already requires (`npm test -- --run`) already executes every fault-injection/regression test 17-07 (Task 1 and Task 3) and 17-08 (Task 2 and Task 3) added for the Pass-3 photo-restore-safety and encryption-fail-closed findings — this task does not need to re-specify those component-level cases. This task DOES need one additional INTEGRATION-level regression per area, exercising the real module boundaries together (restore-apply → SQLite → photo-storage → the launch-sweep hook), not a narrower per-module mock, so a wiring gap between components is caught even if each component's own unit tests pass in isolation:

- Photo process-kill integration test: run a real Merge restore-apply call whose backup includes at least one winning new/updated contact photo (producing a `restore_photo_journal` `finalize` row) and at least one winning explicit photo clear or tombstoned row (producing a `delete` row); after the outer transaction commits, do NOT let restore-apply's own post-commit draining loop run to completion (stop after the transaction commit, before or partway through the loop, simulating the process-kill window) — then invoke `registerRestorePhotoFinalizeSweep`'s underlying drain function directly against the SAME database/filesystem state (simulating the next foreground launch) and assert every committed journal row is correctly finalized or deleted, and `restore_photo_journal` plus `avatars/_restore_pending/` both end up empty.
- Stale/losing-photo non-overwrite integration test: seed a local contact with a newer `modified_at` and a live canonical photo, restore-apply a Merge backup carrying an OLDER `modified_at` and a DIFFERENT photo for the same contact uid, and assert the canonical photo bytes on disk are byte-identical to the pre-restore local bytes (never staged, journaled, or finalized) per Pass-3 audit B-11.
- Orphan-GC integration test: manually place a fully-staged (non `.stage-tmp`) file under `avatars/_restore_pending/` with NO corresponding `restore_photo_journal` row (simulating a restore that staged then rolled back or crashed before COMMIT), run the launch-sweep drain function, and assert the file is deleted WITHOUT `persistMaster` ever being called (spy/mock assertion) per Pass-3 audit B-10.
- Crypto fault-injection integration tests (Pass-3 audit B-16/B-19/B-20): (a) with `encryption_enabled=true` and a real cached passphrase, inject a SecureStore `getItemAsync` throw and attempt an automatic-write call; assert it returns a typed `blocked` result with zero bytes written to the destination and `encryption_enabled` unchanged; (b) construct an envelope object with a KDF `iterations` field set to an out-of-profile value (e.g. `2^31`) and attempt to decrypt it; spy on the native PBKDF2 entry point and assert it is called exactly zero times; (c) construct an otherwise-valid envelope object with one additional unexpected top-level key and assert parsing rejects it before any decrypt attempt.</action>
  <verify><automated>npm test -- --run && npx tsc --noEmit && npm run check:colors && npx expo config --type public</automated></verify>
  <acceptance_criteria>All automated gates pass, the build configuration contains only the intended approved native-module setup, the default encrypted-envelope version/parameters match the 17-07 release-build-measured physical-Pixel record, the v6→current-TARGET_VERSION migration plus export→restore (Merge and Replace-all) integration test passes on populated fixtures — including two contacts with different source categories both resolving to the correct destination local category id — and the large-library export-duration regression passes under its documented budget with the measured value recorded in the phase SUMMARY. The photo process-kill integration test proves the launch-sweep, invoked directly against real post-commit DB/FS state, drains every committed journal row to the correct outcome; the stale/losing-photo test proves a newer local photo's canonical bytes never change from an older incoming one; the orphan-GC test proves an unjournaled staged file is deleted without ever reaching `persistMaster`; and the three crypto fault-injection tests prove a SecureStore failure blocks an automatic write with zero bytes written, a malicious KDF parameter triggers zero native PBKDF2 calls, and an envelope with an unknown key is rejected before decryption (Pass-3 audit B-9 through B-13, B-16, B-19, B-20).</acceptance_criteria>
  <done>The codebase is ready for a release APK and owner-operated UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner verifies backup and restore on a physical Pixel release build</name>
  <precondition>A release APK containing the approved native dependencies is built through docs/runbooks/desktop-build-pipeline.md and disposable test data is available.</precondition>
  <read_first>docs/runbooks/desktop-build-pipeline.md; 17-UI-SPEC.md Interaction and State Contract; 17-VALIDATION.md; 17-CONTEXT.md D-07 through D-18</read_first>
  <files>docs/runbooks/desktop-build-pipeline.md</files>
  <action>On the owner-operated Pixel, confirm the installed encrypted-envelope version/parameters match the Task 2 benchmark decision, then use only disposable contacts/photos to verify: temporary Dashboard entry and all landing states; SAF folder selection, persisted automatic write, retained newest snapshot, and revoked/lost-folder recovery; manual plaintext share; encryption setup/change/future-only/disable plus encrypted manual and automatic round trip; wrong-passphrase/damaged/newer file responses; Merge with NULL clear/tombstone deletion; confirmed Replace-all with and without configured destination; fresh restored photos; recency and notification/digest rebuild. Confirm long folder/copy layouts at 320dp and accessible controls/progress. Record only aggregate evidence and no private data.</action>
  <what-built>The release APK contains the complete local backup, encryption, and restore flow.</what-built>
  <how-to-verify>Follow the listed disposable-data Pixel scenarios and mark each pass or failure in the phase UAT record.</how-to-verify>
  <resume-signal>Reply with the release-build UAT result and the first failed scenario, if any.</resume-signal>
  <verify><automated>npm test -- --run && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>The owner marks each native scenario pass/fail, including the approved versioned KDF profile, provider loss, and encrypted restore, with release-build/device details and no sensitive content captured.</acceptance_criteria>
  <done>Required Android behavior is proven or the phase stops with the failed scenario documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| release APK → Android provider/device | Native filesystem, picker, sharing, SecureStore, and crypto behavior is executed outside node fakes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-28 | Denial of service | SAF provider lifecycle | high | mitigate | Physical proof includes persisted grant and loss/reconnect behavior. |
| T-17-29 | Information disclosure | device UAT fixtures | high | mitigate | Owner uses disposable data and records aggregate evidence only. |
| T-17-30 | Tampering | native encrypted round trip | high | mitigate | Physical encrypted/wrong-passphrase/tamper scenarios complement deterministic unit vectors. |
| T-17-SC | Tampering | native package supply chain | high | mitigate | UAT is contingent on the completed 17-01 package legitimacy checkpoint. |
</threat_model>

<verification>Automated gates run before release build; owner checkpoint supplies the native proof unavailable in this environment.</verification>
<success_criteria>All Phase 17 behavior is green in code and on a real Android provider/device before phase verification is marked complete.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-11-SUMMARY.md` when done</output>
