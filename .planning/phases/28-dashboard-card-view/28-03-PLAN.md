---
phase: 28-dashboard-card-view
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/dashboard-selection-store.ts
  - src/stores/dashboard-selection-store.test.ts
autonomous: true
requirements: [CARDV-05, CARDV-06, CARDV-12]
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A pure in-memory Zustand selection store exists: mode (boolean), selectedIds (Set<number>), frozenUniverse (number[]) — no persistence, no DB, no exec argument (contrast dashboard-query-store whose setters take exec + hit updateAppSettings)."
    - "enterSelection(universe, seedId?) snapshots frozenUniverse exactly as passed and never recomputes it while mode is on; an optional seedId is added to selectedIds; entering when already active is a no-op that does not re-snapshot."
    - "toggle(id) is a NO-OP unless frozenUniverse.includes(id) — an id outside the frozen snapshot can never be added to selectedIds even if a live re-query renders it; for an in-universe id, toggle adds/removes it (idempotent: two toggles return to the prior state). This fences the frozen selection universe at the store boundary so a post-selection dashboard refresh cannot introduce a selectable out-of-universe id (D-12/SS S/Pitfall-6; addresses review A1). selectAll() sets selectedIds to exactly the frozenUniverse set (CARDV-06)."
    - "removeFromUniverse(ids) removes those ids from BOTH selectedIds and frozenUniverse (so archived cards vanish and can never be re-selected via Select All) (CARDV-12/SS AB)."
    - "exitSelection() clears mode, selectedIds, and frozenUniverse back to defaults."
    - "CARDV-05 boundary: a selected-count selector returns exactly selectedIds.size; 0 selected is a valid active state (bulk actions guarded by callers, store stays consistent)."
    - "CARDV-06 empty: selectAll over an empty frozenUniverse selects nothing (no crash); enterSelection with an empty universe is valid."
    - "CARDV-06 ordering: selectedIds membership is order-independent (a Set); selectAll depends only on the frozen id set, not its order."
    - "CARDV-05 idempotency: toggling the same id twice net-restores its state; selectAll then selectAll is stable."
    - "CARDV-05 concurrency: store mutations are synchronous and independent of any in-flight DB write; a bulk write completing does not mutate selection except via an explicit removeFromUniverse call."
  artifacts:
    - src/stores/dashboard-selection-store.ts
    - src/stores/dashboard-selection-store.test.ts
  key_links:
    - "long-press Select / overflow Select Contacts -> enterSelection(frozenUniverse, seedId?)"
    - "card tap in selection mode -> toggle(id)"
    - "bulkArchive success -> removeFromUniverse(archivedIds)"
    - "Back / explicit exit -> exitSelection()"
  prohibitions:
    - "The selection store must never persist to AsyncStorage or write to SQLite — it is ephemeral UI session state only (contrast dashboard-query-store / dashboard-prefs-store)."
    - "frozenUniverse must never be recomputed from the live result set while selection mode is active — it is snapshotted once at enterSelection (D-12 / SS S; Pitfall 6)."
    - "toggle(id) must never add an id absent from frozenUniverse — the membership guard is the store-boundary fence that keeps a post-selection live re-query from smuggling a selectable out-of-universe id (D-12; addresses review A1)."
---

<objective>
Create the in-memory multi-select state model that makes multi-select Orbit's bulk-management surface. This store holds selection mode, the selected id set, and the frozen eligible universe captured when selection began — the invariant that prevents ambiguous selection surviving a changing Population/Filter/Search result set (D-12).

Purpose: a single, tested source of selection truth the Card renderer (Plan 06) and bulk-action surface (Plan 07) both read.
Output: src/stores/dashboard-selection-store.ts (+ test). Pure UI session state — no persistence, no DB, no migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `useDashboardSelectionStore` (src/stores/dashboard-selection-store.ts): { mode, selectedIds:Set<number>, frozenUniverse:number[], enterSelection, toggle, selectAll, removeFromUniverse, exitSelection }
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard-selection-store — mode, selectedIds, frozenUniverse + actions</name>
  <files>src/stores/dashboard-selection-store.ts</files>
  <read_first>
    - src/stores/dashboard-session-store.ts (the ANALOG: a plain create<T>()((set) => ({...})) pure in-memory session store — no persist, no DB, synchronous setters, a clear* reset)
    - src/stores/dashboard-query-store.ts (CONTRAST ONLY — its setters take exec and call updateAppSettings/persist; the selection store must NOT do that)
    - .planning/phases/28-dashboard-card-view/28-RESEARCH.md (Selection state — no store exists yet; the interface sketch) and 28-PATTERNS.md ("dashboard-selection-store.ts" — the pattern-to-copy + D-12/SS R/SS S invariants)
  </read_first>
  <behavior>
    - enterSelection([1,2,3], 2): mode=true, frozenUniverse=[1,2,3], selectedIds={2}. Called again while mode=true: no-op (frozenUniverse and selectedIds unchanged).
    - toggle(1): selectedIds={1,2}; toggle(1) again: selectedIds={2}. toggle(99) when 99 ∉ frozenUniverse: NO-OP (selectedIds unchanged) — the frozen-universe fence.
    - selectAll(): selectedIds = new Set(frozenUniverse).
    - removeFromUniverse([2]): selectedIds and frozenUniverse both drop 2.
    - exitSelection(): mode=false, selectedIds=empty, frozenUniverse=[].
    - A count selector (or derived) returns selectedIds.size.
  </behavior>
  <action>
    Create useDashboardSelectionStore via `create<DashboardSelectionStore>()((set) => ({...}))` copying dashboard-session-store's plain pattern (NO persist middleware, NO exec params, NO SQLite). State: mode:boolean (default false), selectedIds:Set<number> (default new Set()), frozenUniverse:number[] (default []). Actions: enterSelection(universe:number[], seedId?:number) — if mode already true, return unchanged; else set mode=true, frozenUniverse=[...universe], selectedIds = seedId != null ? new Set([seedId]) : new Set(); toggle(id:number) — GUARD FIRST: if !frozenUniverse.includes(id) return unchanged (the frozen-universe fence — a live re-query can render a row outside the snapshot and route its tap here; it must never enter selectedIds; addresses review A1); otherwise copy the Set, add/delete, set it; selectAll() — set selectedIds = new Set(frozenUniverse); removeFromUniverse(ids:number[]) — set frozenUniverse filtered to exclude ids AND selectedIds with those ids deleted; exitSelection() — reset all three to defaults. Keep every setter synchronous and pure (new Set/array each time so Zustand change-detects). Export the store hook.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -Lq "persist\|updateAppSettings\|getExecutor\|AsyncStorage" src/stores/dashboard-selection-store.ts && echo "no-persist OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file imports only `create` from zustand (no persist middleware, no exec/DB import, no AsyncStorage).
    - Source assertion: enterSelection returns unchanged when mode is already true (guarded re-entry).
    - Source assertion: toggle(id) returns unchanged when id is not in frozenUniverse (the membership guard is the first statement of toggle).
    - Source assertion: removeFromUniverse mutates both selectedIds and frozenUniverse.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The selection store exists, pure and persistence-free; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dashboard-selection-store.test.ts — invariants of enter/toggle/selectAll/remove/exit</name>
  <files>src/stores/dashboard-selection-store.test.ts</files>
  <read_first>
    - src/stores/dashboard-session-store.test.ts and src/stores/dashboard-query-store.test.ts (the Zustand store test harness pattern in this repo — getState()/setState resets between cases)
    - src/stores/dashboard-selection-store.ts (under test)
  </read_first>
  <behavior>
    - Covers: enterSelection snapshots + seed; re-enter no-op; toggle idempotency; toggle of an out-of-universe id is a no-op (the frozen-universe fence); selectAll over frozenUniverse; selectAll over empty universe (no crash); removeFromUniverse drops from both; exitSelection resets; count = selectedIds.size.
  </behavior>
  <action>
    Write src/stores/dashboard-selection-store.test.ts using the repo Zustand test pattern (reset the store to defaults in beforeEach via setState). Assert each behavior above, explicitly including: (1) enterSelection([1,2,3],2) then enterSelection([9],9) leaves frozenUniverse=[1,2,3] and selectedIds={2} (re-entry no-op — Pitfall 6 freeze); (2) selectAll() = {1,2,3}; (3) selectAll() over an empty frozenUniverse yields an empty set (no throw); (4) removeFromUniverse([2]) after selectAll leaves selectedIds={1,3} and frozenUniverse=[1,3]; (5) toggle(1);toggle(1) net no change; (6) exitSelection() clears all three; (7) THE FENCE: enterSelection([1,2,3]) then toggle(99) (99 ∉ frozenUniverse) leaves selectedIds empty — an out-of-universe id can never be selected (addresses review A1).
  </action>
  <verify>
    <automated>npm test -- src/stores/dashboard-selection-store.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/stores/dashboard-selection-store.test.ts` passes all cases.
    - The re-entry no-op case proves frozenUniverse is not re-snapshotted while active.
    - The out-of-universe toggle case proves toggle(id) cannot add an id absent from frozenUniverse (the frozen-universe fence).
    - Full `npm test` stays green.
  </acceptance_criteria>
  <done>Selection-store invariants are test-proven; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live result set -> frozen universe | the eligible universe is snapshotted once; the live query must not leak back in during selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-08 | Tampering | ambiguous selection across a changing result set | medium | mitigate | frozenUniverse snapshotted at enterSelection, never recomputed while active; Select All operates over it (test-proven) |
| T-28-09 | Information disclosure | selection persisting to durable storage | low | mitigate | Store is pure in-memory (no persist/AsyncStorage/DB) — selection never survives relaunch by design |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test -- src/stores/dashboard-selection-store.test.ts` green; full `npm test` green.
- Grep confirms no persist/DB/AsyncStorage import in the store.
</verification>

<success_criteria>
- The selection store models mode + selectedIds + frozen universe with tested enter/toggle/selectAll/remove/exit (CARDV-05/06).
- removeFromUniverse supports archived-cards-vanish + persisted selection (CARDV-12).
- Pure ephemeral state — no persistence, no DB, no migration.
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-03-SUMMARY.md` when done.
</output>
