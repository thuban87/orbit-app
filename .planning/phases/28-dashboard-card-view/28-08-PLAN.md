---
phase: 28-dashboard-card-view
plan: 08
type: execute
wave: 6
depends_on: [28-07]
files_modified:
  - src/components/BulkActionSurface.tsx
  - src/screens/HomeScreen.tsx
  - src/logic/dashboard-bulk-action-session.ts
  - src/logic/dashboard-bulk-action-session.test.ts
  - src/stores/dashboard-selection-store.ts
  - src/stores/dashboard-selection-store.test.ts
  - src/db/snooze-dao.ts
  - src/db/bulk-actions-dao.ts
  - src/db/bulk-actions-dao.test.ts
autonomous: true
gap_closure: true
requirements: [CARDV-05, CARDV-06, CARDV-07, CARDV-08, CARDV-10, CARDV-11, CARDV-12]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A second press cannot begin any Card View bulk action while an already claimed bulk action is pending; the action surface visibly reports busy/disabled state, and the one completed small Quick Log batch retains its own Undo receipt. (CARDV-07/CARDV-08, verification gap)"
    - "A category picker is tied to one active selection session: category loading and picker commit both reject an exited or replaced session, and a valid commit obtains its target IDs from the current session selection at commit time. (CARDV-06/CARDV-07, verification gap)"
    - "Selection entry stores a deduplicated frozen universe and can seed only an ID within that universe. (CARDV-05/CARDV-06, verifier warning)"
    - "One bulk Snooze batch resolves one local target date and writes that identical date for every selected contact. (CARDV-07, verifier warning)"
  artifacts:
    - src/logic/dashboard-bulk-action-session.ts
    - src/logic/dashboard-bulk-action-session.test.ts
    - src/components/BulkActionSurface.tsx
    - src/screens/HomeScreen.tsx
    - src/stores/dashboard-selection-store.ts
    - src/stores/dashboard-selection-store.test.ts
    - src/db/snooze-dao.ts
    - src/db/bulk-actions-dao.ts
    - src/db/bulk-actions-dao.test.ts
  key_links:
    - "BulkActionSurface pending prop -> disabled/busy controls -> HomeScreen synchronous ref fence -> exactly one DAO invocation and snackbar Undo receipt"
    - "HomeScreen category picker token -> dashboard-selection-store sessionId/mode -> current selectedIds at commit -> bulkSetCategory"
    - "bulkSnooze -> one local-date resolver call -> transaction-owned snoozeContactCore calls -> identical contacts.snooze_until values"
---

<objective>
Close every automated verification failure and warning in Phase 28's bulk-management path without changing the established Card View action set, visual contract, or device-UAT scope.

Purpose: make bulk writes correct under ordinary asynchronous UI timing, so a Quick Log batch remains undoable and a category change cannot target a selection that has ended or been replaced.
Output: a single-flight/session guard, pending-aware bulk controls, deterministic regressions for deferred Quick Log/category sequences, a closed selection-store seed boundary, and one-date-per-bulk-Snooze semantics.

Decision fidelity: D-03: retain the existing-schema, no-migration boundary. D-04: retain one transaction composed from single-contact cores for every bulk mutation. D-05: leave Quick Log on its existing interaction and recency cores. D-06: leave Archive on its immutable lifecycle-event cores. D-07: retain Archive as the recoverable removal and the frequency-only sensitive surface. D-08: retain the existing archive lifecycle rather than adding state or a purge flow. D-09: retain explicit favourite/snooze actions and binary membership behavior. D-10: retain the existing count-aware detailed-log route. D-11: retain the existing semantic icon/status system. D-12: retain multi-select as the control-area replacement with a frozen universe and explicit exit. D-13: keep every bulk action applicable to all currently selected contacts without tracking-state eligibility filtering.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-VERIFICATION.md
@.planning/phases/28-dashboard-card-view/28-REVIEW.md
@.planning/phases/28-dashboard-card-view/28-07-SUMMARY.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and wire the synchronous bulk-operation fence through the Quick Log Undo path</name>
  <files>src/logic/dashboard-bulk-action-session.ts, src/logic/dashboard-bulk-action-session.test.ts, src/components/BulkActionSurface.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (selection state around lines 434-464; `performBulkQuickLog`, `reportBulkFailure`, and `commitBulkOutcome` around lines 803-922; every `onBulk*` entry point around lines 914-1004; picker/confirmation close paths around lines 1690-1869)
    - src/components/BulkActionSurface.tsx (the `disabled` derivation and every action/sensitive control)
    - src/services/quick-log-command.ts and src/services/quick-log-command.test.ts (the repository's `pendingRef` synchronous-fence and deferred-write regression pattern)
    - src/stores/snackbar-store.ts and src/components/Snackbar.tsx (singleton message replacement and Undo action lifecycle)
    - .planning/phases/28-dashboard-card-view/28-VERIFICATION.md (the failed Quick Log truth and exact receipt-loss sequence)
  </read_first>
  <behavior>
    - Given a deferred bulk Quick Log write, two synchronous attempts before the deferred promise settles invoke the DAO once; resolving it emits one success snackbar whose Undo callback closes over that one returned receipt.
    - Given a claimed action, Quick Log, detailed Log, favourite, Snooze/Unsnooze, Set Category, Archive, Frequency, and sensitive controls are disabled and expose `busy: true`; the explicit exit control remains available to end selection.
    - Given a bulk write rejection, the fence is released before the Retry snackbar action is available; given success, it is released only after success feedback and refresh have been scheduled.
    - Given a confirmation or picker is opened by a claimed action, its cancel/dismiss route releases the claim; its confirm/choice route owns the same claim until its write settles.
  </behavior>
  <action>
    Create `src/logic/dashboard-bulk-action-session.ts` as the React-free owner of two small contracts used by HomeScreen: a synchronous single-flight gate backed by a mutable ref-like state (`tryAcquire` must change the guard before any await, `release` must be idempotent), and helpers that model a running operation's completion callback without changing its receipt. Write the RED test in `dashboard-bulk-action-session.test.ts` before implementation using the established `deferred()` promise shape: start bulk Quick Log twice against one gate, assert one DAO call while unresolved, resolve a distinguishable `QuickLogBatchReceipt`, and assert exactly one captured snackbar Undo handler receives that same receipt. Keep this test independent of React Native rendering.

    In HomeScreen, create one `bulkActionPendingRef` plus rendered `bulkActionPending` state. Route every bulk-action entry from `BulkActionSurface` through the gate, including the count-aware Log route, every DAO writer, confirmation launch/confirm/cancel, Snooze/Frequency/Category picker launch/choice/dismiss, and error Retry. A rejected second invocation must return before constructing IDs, issuing navigation, reading categories, calling a DAO, or showing feedback. Preserve D-04/D-05/D-06 writers exactly: this task changes orchestration only, never their transaction composition. Use `try/finally` or equivalent settled-path ownership so an error cannot strand the UI pending; retain the successful Quick Log receipt in the only success snackbar produced by its claimed operation.

    Add `pending` to `BulkActionSurfaceProps`. Derive each bulk action's disabled state from `selectedCount === 0 || pending`, set the action controls' accessibility state to include disabled/busy, and render a token-styled, visible pending label while a claim is held. The existing Done/exit control must remain operable so leaving selection can invalidate a picker session; the host must clear pending state as part of that exit path. Do not add actions, a bottom bar, or a second snackbar mechanism (D-12).
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-bulk-action-session.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - The deferred double-attempt regression asserts one Quick Log DAO invocation, one success snackbar, and an Undo handler holding the first-and-only receipt.
    - A synchronous ref fence is acquired before every HomeScreen bulk action can begin asynchronous work, with settled success, failure, and picker/confirmation cancellation paths releasing it.
    - BulkActionSurface visibly identifies an active operation and disables every bulk control while pending; accessibility exposes both disabled and busy state.
    - No extra category, interaction, archive, favourite, snooze, frequency, or navigation action is introduced.
  </acceptance_criteria>
  <done>Overlapping bulk submissions are impossible at the host boundary, and the single committed Quick Log batch retains a valid Undo receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the asynchronous category picker to the live selection session and commit-time IDs</name>
  <files>src/logic/dashboard-bulk-action-session.ts, src/logic/dashboard-bulk-action-session.test.ts, src/stores/dashboard-selection-store.ts, src/stores/dashboard-selection-store.test.ts, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (`onBulkOpenCategoryPicker` around lines 973-979; category picker state and rows around lines 440-444 and 1742-1774; selection entry and exit call sites)
    - src/stores/dashboard-selection-store.ts and src/stores/dashboard-selection-store.test.ts (mode, frozen universe, selected IDs, and entry/exit semantics)
    - src/logic/dashboard-bulk-action-session.ts and its test from Task 1 (reuse the session helper rather than adding a parallel async guard)
    - .planning/phases/28-dashboard-card-view/28-VERIFICATION.md (the stale-category failure and `STALE_TARGET` trace)
    - .planning/phases/28-dashboard-card-view/28-CONTEXT.md (D-12 frozen-universe and explicit-exit contract)
  </read_first>
  <behavior>
    - Given category loading begins in session A and the user exits, the resolved read does not open a picker and does not call `bulkSetCategory`.
    - Given category picker is open in session A, exit followed by a new selection session B makes its category choice a no-op; it cannot write the IDs from session A.
    - Given a valid unchanged session, category commit reads `selectedIds` at the moment of choice; toggling the active selection while the picker is open changes the valid target set to those current IDs.
    - Given `enterSelection([1, 2, 2, 3], 99)`, frozenUniverse is `[1, 2, 3]` and selectedIds is empty; a seed inside the deduplicated universe remains selected.
  </behavior>
  <action>
    Extend the ephemeral selection store with a monotonically increasing numeric `sessionId`, advancing only when a new selection mode is successfully entered. `enterSelection` must first construct a first-occurrence-preserving deduplicated frozen universe, then seed only when `seedId` belongs to that deduplicated universe; preserve the existing no-resnapshot behavior while mode is active. Export a narrow snapshot/token helper from `dashboard-bulk-action-session.ts` or the store that validates both `mode === true` and matching `sessionId`, so HomeScreen does not duplicate session-validity logic. Add direct store regressions for duplicate universe input and out-of-universe seed input.

    Replace HomeScreen's category picker payload from captured IDs to `{ sessionId, categories }`. When Set Category is claimed, capture only the current session token, await `listCategories`, then re-read the selection store before opening the picker. If the token is no longer current, close/avoid the picker and release the held bulk claim without calling a writer. While the picker is open, its category choice must revalidate the same token, obtain a fresh `Array.from(current.selectedIds)`, and only then call `bulkSetCategory`; an invalid session or zero current selection closes the picker and performs no mutation. The category sheet's request-close and the explicit selection exit path must clear picker state and release any held category claim. For a valid session, use the fresh IDs consistently in DAO input and success copy.

    Add deferred category-read and stale-open-picker regression cases to `dashboard-bulk-action-session.test.ts`: model exit before the category promise resolves, then model exit/re-enter before choice. Each must prove no writer invocation; include a valid same-session changed-selection case that proves commit uses the current IDs. Keep category membership single-valued and do not alter the action menu or frozen-universe rendering contract (D-12/D-13).
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-bulk-action-session.test.ts src/stores/dashboard-selection-store.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Category loading and category commit each validate an active matching selection session; stale session paths make no `bulkSetCategory` call.
    - A valid category commit targets only IDs selected at choice time, not IDs captured before `listCategories` awaited.
    - Tests deterministically cover exit during the deferred read, exit/re-enter while the picker is open, and valid changed selection.
    - Selection store tests prove deduplicated frozen-universe order and reject an out-of-universe seed.
  </acceptance_criteria>
  <done>Set Category cannot mutate a selection session that has ended or been replaced, and valid commits use the live selected set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve one local Snooze target date for each transactional bulk batch</name>
  <files>src/db/snooze-dao.ts, src/db/bulk-actions-dao.ts, src/db/bulk-actions-dao.test.ts</files>
  <read_first>
    - src/db/bulk-actions-dao.ts (`bulkSnooze` transaction loop and the shared non-reentrant `inWriteTransaction` contract)
    - src/db/snooze-dao.ts (`PRESET_MODIFIERS`, local SQLite date resolution, `snoozeContactCore`, immutable event write)
    - src/db/bulk-actions-dao.test.ts (existing node-SQLite migration fixture and bulk Snooze event test)
    - src/db/snooze-dao.test.ts (single-contact local-date contract)
    - .planning/phases/28-dashboard-card-view/28-VERIFICATION.md (the bulk-Snooze midnight warning)
  </read_first>
  <behavior>
    - Given two contacts in one `bulkSnooze` call, the local date resolver executes once and both stored `snooze_until` values equal the same resolved target.
    - Given a resolver fixture that returns a different local date for each invocation, a multi-contact batch still writes only the first target date to every selected contact and preserves one immutable Snooze event per contact.
    - Existing single-contact snooze behavior continues to resolve its own local preset target and retains its atomic contact-plus-event write.
  </behavior>
  <action>
    Separate local target-date resolution from the already-transaction-owned Snooze write core. Make the core receive a resolved local `YYYY-MM-DD` target rather than issue its own date query, and keep the public single-contact writer responsible for resolving one target for its one operation. In `bulkSnooze`, resolve the preset target once before iterating, then pass that exact string to every core invocation inside the one outer `inWriteTransaction`; retain D-04's N composed single-contact operations, the immutable event per contact, one data-revision bump, bound values, and the non-reentrant transaction rule.

Add a deterministic bulk DAO regression using the established node-SQLite executor fixture wrapped/spied at its date-query boundary. Make repeated resolver calls observably return distinct dates, call `bulkSnooze` for multiple contacts, assert one resolver invocation, assert every selected `contacts.snooze_until` equals the first resolved target, and retain the existing event-count assertion. Do not use wall-clock timing, midnight sleeps, or JavaScript date arithmetic in the test.
  </action>
  <verify>
    <automated>npx vitest run src/db/bulk-actions-dao.test.ts src/db/snooze-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - bulkSnooze resolves the SQLite local date once per non-empty batch and supplies that one target to every core write.
    - The deterministic regression fails if a multi-contact batch reintroduces per-contact date resolution and proves equal persisted dates plus immutable events.
    - Single-contact snooze tests still pass, and no bulk operation changes to a set-based update (D-04).
  </acceptance_criteria>
  <done>Every contact in one bulk Snooze batch receives the same local target date, including a batch that spans local midnight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection UI -> HomeScreen bulk orchestrator | Repeated presses and late picker callbacks can otherwise produce unintended durable mutations. |
| HomeScreen -> SQLite bulk composers | Selected contact IDs cross from ephemeral UI state into transactional writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-08-01 | Tampering | HomeScreen bulk callbacks | medium | mitigate | Synchronous ref gate claims each bulk action before asynchronous work and disables the rendered controls until the owned operation settles. |
| T-28-08-02 | Tampering | category picker -> bulkSetCategory | medium | mitigate | Session-token validation before opening and committing, followed by commit-time selected-ID read, prevents stale callbacks from writing former selections. |
| T-28-08-03 | Integrity | bulkSnooze date resolution | low | mitigate | Resolve one SQLite local target once per batch and pass it to every transaction core; deterministic test guards repeated resolution. |
| T-28-08-04 | Denial of service | pending bulk UI | low | accept | All settle, dismiss, and explicit-exit paths release the in-memory claim; no network or external lock is introduced. |
| T-28-08-SC | Tampering | package installs | high | mitigate | No package installation is part of this closure plan. |
</threat_model>

<verification>
Run the focused guard/session/store suite and focused bulk/snooze DAO suite, then `npx tsc --noEmit` and `npm run check:colors`. The regression tests must use deferred promises and a controlled date-query boundary; neither test may depend on device interaction or wall-clock timing.
</verification>

<success_criteria>
- The two blocked verification truths are demonstrably safe under deterministic duplicate-submit and stale-category sequences.
- The two warnings become test-protected invariants: valid seed membership and one Snooze date per batch.
- Existing Phase 28 UI scope, action list, undo snackbar host, selection persistence, and Pixel-only UAT backstops remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-08-SUMMARY.md` when done.
</output>
