---
phase: 28-dashboard-card-view
plan: 06
type: execute
wave: 4
depends_on: [28-05, 28-03]
files_modified:
  - src/screens/HomeScreen.tsx
  - src/components/GridCard.tsx
  - src/components/CardGrid.tsx
  - src/screens/dashboard-overflow-actions.ts
autonomous: true
requirements: [CARDV-05, CARDV-06, CARDV-12]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Multi-select can be entered two ways: the long-press menu Select item (Plan 05) and the Dashboard overflow Select Contacts entry — the latter is enabled (was disabled:true) and, when not already in Card View, AWAITS the async persisted view switch (`await setViewMode(getExecutor(), 'card')`, the 2-arg (exec, viewMode) store API at dashboard-query-store.ts:79-89) BEFORE calling enterSelection, so selection never begins before the view switches and persists; on a persist failure it reports the error and does NOT enter selection; if already in Card View it enters selection directly (CARDV-05; addresses review CYCLE-3 finding 3)."
    - "Selection controls appear ONLY in selection mode: each card shows a top-left selection control (unselected = empty circle, selected = accent check via the registry select pair); normal Card View shows no persistent selection controls before selection starts (SS P/SS Q)."
    - "In selection mode a card TAP toggles its selection (not Profile nav); Profile navigation does not occur while selection mode is active; the favourite star becomes visually-readable but non-interactive (SS Q)."
    - "The selected count is clearly displayed and announced on change ({n} selected) (SS Q/SS AF)."
    - "Multi-select LOCKS AND REPLACES the Dashboard control area (Population/Filters/Sort AND Search cease to function) with selection-state controls — it does NOT add a new bottom bulk-action bar (SS R, CARDV-06)."
    - "Select All selects all contacts in the FROZEN eligible universe as it stood when selection began; the query controls stay frozen until selection exits (SS S, CARDV-06)."
    - "While selection mode is active, only rows whose id ∈ frozenUniverse are rendered as cards — HomeScreen filters the live `rows` (which it re-queries on focus/foreground/pull-to-refresh) to frozenUniverse membership before passing them to CardGrid, so a row that entered the result set after selection began is never rendered or selectable. This is the renderer-boundary half of the D-12 fence, complementing Plan 03's store-boundary toggle guard (defense in depth; addresses review A1)."
    - "The frozen universe is seeded from the FULL result array (all of `rows`, the complete DashboardRow[] the shared read returns), NEVER a windowed/paged subset — FlatList/CardGrid virtualizes RENDERING only, it does not page the data, so `rows.map(r => r.id)` is the entire eligible universe and Select All can never silently under-select (addresses review A7)."
    - "Back (Android hardware Back and the shell Back) exits multi-select (exitSelection) BEFORE any route navigation while selection mode is active (SS AC, CARDV-12)."
    - "Entering/leaving selection preserves the pre-selection Dashboard query state so normal browsing resumes where appropriate (SS AC)."
    - "CARDV-05 boundary: 0 selected is a valid active state; the count reads 0 selected and updates on every toggle; bulk actions are guarded at 0 (Plan 07 owns the actions)."
    - "CARDV-06 adjacency: entering selection freezes the eligible universe exactly as it stood; the (now inert) Population/Filters/Sort/Search cannot mutate the frozen universe."
    - "CARDV-06 empty: Select All over an empty frozen universe selects nothing (no crash)."
    - "E5 selection populated/empty/overflow: top-left circle per card, tap toggles, count displayed+announced, stars non-interactive, control area locked/replaced; Select All over the frozen universe; 0-selected keeps mode active."
  artifacts:
    - "HomeScreen selection-mode wiring (entry, tap-toggles, count, control-area replacement, Back handler)"
    - "GridCard selection control (top-left circle) + selection-mode star/tap behavior"
    - "CardGrid selection-mode prop threading"
    - "dashboard-overflow-actions Select Contacts enabled"
  key_links:
    - "overflow Select Contacts / long-press Select -> enterSelection(frozenUniverse) -> selection-mode UI"
    - "card tap (mode on) -> toggle(id); Back -> exitSelection before nav"
    - "control-surface region -> replaced by selection-state controls while mode on"
  prohibitions:
    - "Multi-select must not add a new bottom bulk-action bar — it reuses/replaces the existing Dashboard control area (SS R)."
    - "frozenUniverse must not be recomputed from the live result set while selection mode is active (SS S / Pitfall 6)."
    - "While selection mode is active, a row outside frozenUniverse must never be rendered as a selectable card — HomeScreen filters the live rows to frozenUniverse membership before CardGrid (a live re-query or post-op refresh must not smuggle a selectable out-of-universe card) (D-12; addresses review A1)."
    - "The frozen universe must be seeded from the full result array, never a windowed/paged/rendered subset (addresses review A7)."
    - "No separate standalone bulk-management screen — multi-select IS the surface (SS N / CONTEXT deferred)."
---

<objective>
Turn the selection store (Plan 03) and the Select entry (Plan 05) into the live multi-select surface: selection controls on cards, tap-to-toggle, a displayed+announced count, the locked/replaced control area, Select All over the frozen universe, and Back-exits-selection. This is the bulk-management surface itself (no separate screen).

Purpose: CARDV-05/06 and the exit/Back half of CARDV-12.
Output: HomeScreen selection-mode wiring, GridCard selection control, CardGrid threading, and the enabled overflow Select Contacts entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- HomeScreen selection-mode orchestration (entry, frozen-universe render filter, tap-toggle, count, control-area lock/replace, Back handler)
- GridCard top-left selection control + selection-mode star/tap behavior
- Enabled `dashboard-select-contacts-entry` overflow item
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GridCard selection control (top-left circle) + selection-mode tap/star behavior</name>
  <files>src/components/GridCard.tsx, src/components/CardGrid.tsx</files>
  <read_first>
    - src/components/GridCard.tsx (the card Pressable, onPress, onLongPress, favourite star from Plans 01/05)
    - src/components/icons/icon-registry.ts (the select pair added in Plan 01) + Icon.tsx
    - src/components/CandidateCardGrid.tsx (multiSelect ? toggleSelection : onInspect pattern 158-170 — an in-repo selection-mode card analog)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (SS Q selection visual state; the anatomy sketch — selection circle top-left, ONLY in selection mode)
  </read_first>
  <behavior>
    - selectionMode=false: no selection control; onPress opens Profile; star interactive.
    - selectionMode=true: a top-left control renders (unselected = Icon select default/empty circle, selected = Icon select active accent check); onPress toggles selection (does NOT open Profile); the favourite star renders but is non-interactive (no onPress).
    - The card a11y description includes selection state when in selection mode.
  </behavior>
  <action>
    Add props to GridCard: selectionMode:boolean, selected:boolean, onToggleSelect?: () => void. When selectionMode is true: render a top-left selection control using Icon name="select" (state active + tone accent when selected, else default + textSecondary); route the card's outer Pressable onPress to onToggleSelect instead of onPress(Profile); render the favourite star WITHOUT its onPress (visually readable, non-interactive); include selection state in the buildRowAccessibilityDescription inputs (or append a selected/not-selected clause). When false, behave as before. Thread selectionMode/selected/onToggleSelect through CardGrid renderItem. Keep all colours tokenized.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the selection control renders only when selectionMode is true (guarded); it uses the registry select pair (not a raw glyph).
    - Source assertion: in selectionMode the card onPress calls onToggleSelect and the star has no onPress.
    - `npx tsc --noEmit`, `npm run check:colors` clean.
  </acceptance_criteria>
  <done>GridCard renders the top-left selection control and toggles selection on tap in selection mode; star non-interactive; tsc + check:colors green.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen selection-mode wiring — entry, tap-toggle, count, control-area lock/replace, Select All</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (CardGrid wiring; goToProfile; the control-surface region render — DashboardControlRow / search + view-toggle row ~1040-1053; the header)
    - src/stores/dashboard-selection-store.ts (mode, selectedIds, frozenUniverse, enterSelection, toggle, selectAll, exitSelection)
    - src/components/control-surface/DashboardControlRow.tsx (the region that is locked/replaced during selection)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (SS R control-area replacement; SS S Select All; SS Q count displayed + announced)
  </read_first>
  <action>
    Read the selection store in HomeScreen. When mode is on: (0) FRONT-FENCE THE RENDER PATH — pass to CardGrid ONLY the rows whose id ∈ frozenUniverse (build a `new Set(frozenUniverse)` and `rows.filter(r => frozen.has(r.id))`); HomeScreen re-queries `rows` on focus (HomeScreen.tsx:678), foreground/AppState-active (:690), and pull-to-refresh (:706), and Plan 07 refreshes after every bulk op — any of these can introduce a row that entered the live result set AFTER selection began, and an unfenced render would let its card be tapped and (absent the Plan 03 guard) selected. Rendering only frozen-universe rows during selection mode is the renderer-boundary half of the D-12 fence; Plan 03's toggle membership guard is the store-boundary half — defense in depth (addresses review A1). In normal (non-selection) mode, pass the full `rows` unchanged. (1) pass selectionMode + per-card selected (selectedIds.has(id)) + onToggleSelect (toggle(id)) to CardGrid; (2) REPLACE the Dashboard control area (the DashboardControlRow / Population-Filters-Sort + Search + view-toggle region) with a selection-state control area showing the count ({n} selected) and a Select All control (calls selectAll()) plus a placeholder region for bulk actions (Plan 07 fills the actions) and an explicit exit affordance (calls exitSelection()); the normal Population/Filters/Sort/Search controls do not render/function while mode is on (locked/replaced, NOT a new bottom bar). (3) Announce the count on change via an accessibility live region / AccessibilityInfo.announceForAccessibility when selectedIds size changes. Select All uses the store's frozenUniverse (already snapshotted at enter). Ensure entering selection preserves the pre-selection query state (do not mutate dashboard-query-store / dashboard-session-store on enter/exit). The frozen universe seed for the overflow entry is the FULL current result array's ids (`rows.map(r => r.id)`, never a windowed subset — Task 3 passes it; addresses review A7); the long-press Select entry already seeded it in Plan 05.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: while mode is on, HomeScreen renders the selection-state control area in place of DashboardControlRow (grep shows the conditional swap) and does NOT render a separate bottom bar.
    - Source assertion (A1): while selection mode is active, the rows passed to CardGrid are filtered to frozenUniverse membership (grep shows a `frozenUniverse`/`frozen`-derived Set filtering `rows` before CardGrid); in normal mode the full rows are passed. A row that enters the live result set after selection begins is not rendered as a selectable card.
    - Source assertion: card tap in mode calls toggle; Select All calls store.selectAll(); count derives from selectedIds.size and is announced on change.
    - Source assertion: enter/exit do not write to dashboard-query-store or dashboard-session-store (query state preserved).
    - `npx tsc --noEmit`, `npm run check:colors`, full `npm test` clean.
  </acceptance_criteria>
  <done>Selection mode replaces the control area with count + Select All + exit; card taps toggle; count announced; query state preserved; tsc + check:colors + suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Enable overflow Select Contacts + Android Back exits multi-select before navigation</name>
  <files>src/screens/dashboard-overflow-actions.ts, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/dashboard-overflow-actions.ts (the Select Contacts entry disabled:true / onPress no-op at lines 40-43; buildDashboardOverflowActions signature + input shape 8-22; its test dashboard-overflow-actions.test.ts)
    - src/screens/HomeScreen.tsx (buildDashboardOverflowActions call site ~356; useFocusEffect/useIsFocused usage; goToProfile / navigation; how viewMode is set — the existing call idiom `useDashboardQueryStore.getState().setViewMode(getExecutor(), mode)` at :540; getExecutor from @/db/database)
    - src/stores/dashboard-query-store.ts (setViewMode is `async (exec, viewMode)` at :79-89 — it persists via updateAppSettings THEN set()s Zustand, and has an idempotency guard that no-ops when the view is already active; the menu's Select entry must AWAIT this before entering selection — NOT the 1-arg `setViewMode("card")`, which is tsc-caught and unpersisted — addresses review CYCLE-3 finding 3)
    - react-native BackHandler (hardwareBackPress) — no existing usage in HomeScreen; add a focused handler gated on selection mode
    - src/stores/dashboard-selection-store.ts (enterSelection with the current eligible ids; exitSelection)
  </read_first>
  <action>
    Extend buildDashboardOverflowActions' input with an onSelectContacts callback and change the Select Contacts entry from disabled:true/no-op to enabled with onPress: onSelectContacts (keep the testID dashboard-select-contacts-entry). Update its test accordingly. In HomeScreen, provide an ASYNC onSelectContacts (addresses review CYCLE-3 finding 3 — the setViewMode call shape): if already in Card view, call `enterSelection(rows.map(r => r.id))` directly; otherwise `await setViewMode(getExecutor(), "card")` (the 2-arg (exec, viewMode) persisted store API at dashboard-query-store.ts:79-89 — NOT the 1-arg `setViewMode("card")`, which is tsc-caught and unpersisted) and only AFTER it resolves call `enterSelection(rows.map(r => r.id))`, so selection never begins before the view switches and persists. Wrap the await in try/catch: on a persistence failure, report the error (Logger + a snackbar) and RETURN WITHOUT entering selection (do not enter selection on a failed persist). currentEligibleIds = the FULL current result array's ids, i.e. `rows.map(r => r.id)` over the entire DashboardRow[] the shared read returns — NEVER a windowed/paged/rendered subset; CardGrid virtualizes rendering only, so seeding from the full array is required or Select All would silently under-select — this becomes the frozen universe (addresses review A7). Add a BackHandler hardwareBackPress listener (registered while the screen is focused, e.g. inside useFocusEffect) that, when selection mode is active, calls exitSelection() and returns true (consuming the event so no route navigation happens); when mode is off it returns false (default Back). Also ensure any visible/shell Back path exits selection first if mode is on. Do not use git worktrees; work in place.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test -- src/screens/dashboard-overflow-actions.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the Select Contacts overflow entry is no longer disabled:true and its onPress is onSelectContacts; the testID is unchanged; its test asserts the enabled entry.
    - Source assertion: HomeScreen registers a hardwareBackPress handler that returns true + calls exitSelection when selection mode is active, false otherwise.
    - Source assertion (finding 3): onSelectContacts is async and, when not already in Card view, calls `setViewMode(getExecutor(), "card")` with the correct 2-arg (exec, viewMode) signature and AWAITS it BEFORE `enterSelection(...)` (grep shows `await setViewMode(` with the enterSelection call ordered after it, never a 1-arg `setViewMode("card")`); on a persist failure it reports the error and does NOT call enterSelection; if already in Card view it calls enterSelection directly.
    - Source assertion: onSelectContacts seeds enterSelection with the FULL current result array's ids (`rows.map(r => r.id)` over the whole DashboardRow[], not a windowed/rendered subset — A7).
    - `npx tsc --noEmit`, `npm run check:colors`, and the overflow-actions test pass; full `npm test` green.
  </acceptance_criteria>
  <done>Select Contacts enters multi-select, AWAITING the 2-arg persisted setViewMode(exec, "card") before enterSelection and skipping selection on a persist failure; Android Back exits multi-select before navigating; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live query controls -> frozen selection | while selection is active the query controls are inert; they must not mutate the frozen universe |
| Android Back -> navigation | Back must be intercepted to exit selection before any route change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-14 | Tampering | selection surviving a changing result set | medium | mitigate | Control area locked/replaced; Select All over the frozen universe; query stores untouched on enter/exit |
| T-28-15 | Denial of service (UX) | Back navigating away mid-selection losing intent | low | mitigate | hardwareBackPress consumes Back to exitSelection while mode is active (returns true) |
| T-28-16 | Information disclosure | Profile opening on a selection tap | low | mitigate | In selection mode card tap toggles selection; goToProfile is suppressed |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors` clean.
- `npm test` green (overflow-actions test updated).
- Device UAT: enter selection via long-press Select and via overflow Select Contacts; circles appear only in mode; tap toggles; count shows + announces; control area replaced (no bottom bar); Select All selects the frozen universe; Back exits selection first.
</verification>

<success_criteria>
- Multi-select entered from both paths; selection controls only in mode; tap toggles; count shown+announced (CARDV-05).
- Control area locked/replaced; Select All over the frozen universe (CARDV-06).
- Back exits multi-select before navigating; query state preserved (CARDV-12 exit half).
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-06-SUMMARY.md` when done.
</output>
