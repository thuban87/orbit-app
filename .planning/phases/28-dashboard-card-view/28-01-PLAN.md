---
phase: 28-dashboard-card-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons/icon-registry.ts
  - src/components/GridCard.tsx
  - src/components/CardGrid.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [CARDV-01, CARDV-02, CARDV-03]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "HomeScreen's card branch renders a virtualized avatar-first grid over the shared DashboardRow[] (dashboard-read.ts), 3 columns on a normal portrait phone, wired end-to-end so real contacts appear in Card View on the Pixel."
    - "Tapping a card opens the contact Profile via the caller-owned nav (goToProfile) — GridCard stays presentational (no DB read, no getExecutor, no useNavigation)."
    - "The always-visible favourite star toggles binary membership using the SAME optimistic overlay + toggleFavourite path the list branch already uses (favouriteOverlay.get(id) ?? favourite_rank !== null); no new favourite mutation path is introduced."
    - "Status renders as a status-coloured avatar ring (ringVisual().color + ringVisual().width) PLUS a distinct StatusGlyph attached to the avatar edge — never colour-only; the card a11y description names the state textually (buildRowAccessibilityDescription)."
    - "CARDV-01 empty: when the shared query returns zero rows the card branch shows the SAME shared cause-aware empty state the list branch uses (not a blank grid); one row renders one card; a null photo / null categoryLabel renders without crashing."
    - "CARDV-02 empty/null: status === null (never-contacted) renders a faint neutral border ring and NO glyph — StatusGlyph is not mounted and status-neutral is not shown; an active snooze_until renders a neutral ring + snooze moon glyph regardless of underlying status (isSnoozed + StatusDisplayState composition, identical to ListRow)."
    - "CARDV-02 adjacency: a contact exactly at a status-band threshold shows whatever band the shared status read (DashboardRow.status) assigns — Card never re-derives the band."
    - "CARDV-02 ordering: card order is exactly the shared result-model order; Card never sorts by favourite_rank or any Card-local key."
    - "CARDV-03 empty: the star renders binary from membership (favourite_rank !== null); with no search term the card shows normal content, not search rows."
    - "CARDV-01 responsive: column count is derived at runtime from measured width + fontScale (useWindowDimensions); the grid FlatList sets a key that includes numColumns so a column-count change re-mounts cleanly."
    - "E1 grid populated: avatar-first grid of floating avatar bubbles over background, each card = GlassSurface scrim + circular Avatar + status ring + edge glyph + top-right star (Grid & Card Anatomy)."
    - "CardGrid exposes the FULL shared-list surface contract as explicit props — it does NOT inherit them implicitly: rows/data-gating for error+initial-skeleton (data = error||showInitialSkeleton ? [] : rows), ListEmptyComponent (showInitialSkeleton ? loading skeleton : shared listEmpty), refreshControl (pull-to-refresh + the shared error-retry), ListHeaderComponent (listHeader), and contentContainerStyle carrying bottomClearance. Because a numColumns grid needs its OWN FlatList these cannot be inherited from the list branch's FlatList — CardGrid owns them (addresses review: Plan 01 CardGrid prop contract)."
    - "E1 grid loading/error/empty: the card branch reuses the SAME shared dashboard loading skeleton, error state (Pull down to try again.), cause-aware empty states, header, and bottom clearance the list branch uses — content/copy shared, layout renderer-specific; card mode retains pull-to-refresh AND the error-retry path (not a forked/blank grid)."
    - "HomeScreen selects between the list FlatList and CardGrid at CONTAINER level (per query.viewMode) INSIDE the existing resultTransitionStyle Animated.View (HomeScreen.tsx:1054) — NOT at renderItem level as the single FlatList does today; exactly one scroll container is mounted per view mode."
    - "E4 star populated/loading: filled = accent, unfilled = textSecondary, at-least-44px hit area; toggle is optimistic (immediate fill/unfill + restrained haptic), no spinner, no success snackbar."
    - { statement: "E2 card long-text: OS/accessibility text scaling reflows card height and/or drops to 2 columns, never clamps below readable size (SS AF, CARDV-01) — verified by device UAT on the Pixel.", verification: backstop }
    - { statement: "CARDV-01 encoding: contact names with multi-byte / grapheme content (emoji, combining marks) render single-line ellipsized without breaking layout via React Native grapheme-aware text rendering, not byte length — verified on the Pixel.", verification: backstop }
  artifacts:
    - src/components/GridCard.tsx
    - src/components/CardGrid.tsx
    - "src/components/icons/icon-registry.ts (7 new semantic entries)"
    - "src/screens/HomeScreen.tsx (card branch replaced with CardGrid)"
  key_links:
    - "DashboardRow[] (dashboard-read.ts) -> CardGrid -> GridCard render"
    - "GridCard star -> HomeScreen favouriteOverlay + toggleFavourite (existing) -> favourites-dao"
    - "GridCard status -> ringVisual/statusGlyph/StatusGlyph (single source, shared with ListRow)"
    - "ICON_REGISTRY new entries -> IconName type -> Icon.tsx (a missing entry fails tsc)"
  prohibitions:
    - "favourite_rank must never be read for ordering or leak into Card UX — membership is favourite_rank !== null only (ADR-075 / D-09)."
    - "No second icon source — every Card icon resolves a semantic name through the single ICON_REGISTRY; never a raw Ionicons glyph, never a literal star character (D-11 / F-2)."
---

<objective>
Tracer slice for Phase 28: render the Dashboard Card View as a real, production-quality avatar-first grid over the existing shared query, wired end-to-end into HomeScreen's card branch — proving the DashboardRow -> CardGrid -> GridCard -> tap/favourite architecture before any expansion.

This replaces the placeholder card branch (currently the legacy ContactCard in a single column with a non-functional star, src/screens/HomeScreen.tsx:1091-1104) with the new avatar-dominant floating-bubble grid. It also lands the phase-foundational ICON_REGISTRY entries every later wave consumes.

Purpose: prove the card renderer architecture on the agent's best early-context tokens, so an architectural dead-end surfaces after one commit instead of after ten layers.
Output: GridCard.tsx, CardGrid.tsx, 7 new icon-registry entries, and the rewired HomeScreen card branch. Row-3 adaptive context, search-mode rendering, long-press, and multi-select are deliberate functionality gaps filled by later plans — the architecture is complete here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New/changed symbols introduced in THIS plan (convergence source-grounding excludes these from drift verification):
- `GridCard` (src/components/GridCard.tsx) — new avatar-first card component
- `CardGrid` (src/components/CardGrid.tsx) — new virtualized grid renderer
- New `ICON_REGISTRY` entries: `select`, `select-all`, `archive`, `snooze`, `category`, `frequency`, `overflow` (consumed by Plans 05/06/07)
- HomeScreen card-branch rewrite (renders `CardGrid`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Add the 7 Card-action/selection semantic icons to the single ICON_REGISTRY</name>
  <files>src/components/icons/icon-registry.ts</files>
  <read_first>
    - src/components/icons/icon-registry.ts (the ICON_REGISTRY `as const` map, entries lines 33-73; IconName = keyof typeof ICON_REGISTRY; a missing name fails tsc at Icon.tsx)
    - src/components/icons/Icon.tsx (how IconName is consumed — the tsc gate)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Flag F-1 suggested pairs) and 28-PATTERNS.md ("Icon registry entries" — exact pair shape)
  </read_first>
  <action>
    Add seven new semantic-name entries to ICON_REGISTRY, each an { outline, filled } Ionicons pair matching the existing `as const` shape (do NOT fork a second icon source — D-11). Names + suggested pairs (confirm each glyph name resolves in `@expo/vector-icons` Ionicons at build): select = ellipse-outline / checkmark-circle; select-all = checkmark-done-outline / checkmark-done; archive = archive-outline / archive; snooze = alarm-outline / alarm (a distinct ACTION snooze icon — the existing status-snoozed moon stays the status glyph); category = pricetag-outline / pricetag; frequency = repeat-outline / repeat; overflow = ellipsis-horizontal / ellipsis-horizontal. These are phase-foundational config consumed by Plans 05, 06, 07. Do not remove or rename any existing entry.
  </action>
  <verify>
    <automated>grep -Eq "^\s+select:" src/components/icons/icon-registry.ts && grep -Eq "select-all:" src/components/icons/icon-registry.ts && grep -Eq "archive:" src/components/icons/icon-registry.ts && grep -Eq "category:" src/components/icons/icon-registry.ts && grep -Eq "frequency:" src/components/icons/icon-registry.ts && grep -Eq "overflow:" src/components/icons/icon-registry.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new keys (select, select-all, archive, snooze, category, frequency, overflow) resolve as `IconName` and `npx tsc --noEmit` is clean.
    - No existing ICON_REGISTRY entry is removed or renamed (git diff shows additions only in the map).
    - `npm run check:colors` is clean (no colour literals introduced).
  </acceptance_criteria>
  <done>The single ICON_REGISTRY carries the 7 new Card-action/selection semantic names; tsc green.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: GridCard component — avatar-first floating bubble (avatar+ring+glyph, name, recency, star)</name>
  <files>src/components/GridCard.tsx</files>
  <read_first>
    - src/components/ListRow.tsx (PRIMARY analog / sibling renderer: props interface lines 59-105; imports 5-27; snooze+ring+a11y composition 105-121; never-contacted glyph guard 267-275; favourite star 251-266). Copy the shared-primitive usage verbatim; Card differs in LAYOUT only.
    - src/components/ContactCard.tsx (REFERENCE ONLY — the escalating avatar-ring anatomy at 113,134-147 (borderColor: ring.color, borderWidth: ring.width, opacity: ring.opacity); do NOT copy its literal star character or 48px corner-ring layout)
    - src/components/contact-card-ring.ts (ringVisual, statusGlyph, StatusDisplayState = ProfileStatus | "snoozed" | null)
    - src/components/icons/StatusGlyph.tsx, src/components/Avatar.tsx (contactId recyclingKey + cacheBust, lines 32-45,78)
    - src/components/ui/GlassSurface.tsx (theme-keyed scrim; density drives opacity; blurAvailable fallback per UI-SPEC F-4)
    - src/components/list-row-content.ts (formatListRecency, buildRowAccessibilityDescription)
    - src/utils/dates.ts (isSnoozed), src/db/dashboard-read.ts (DashboardRow fields 91-110), .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Grid & Card Anatomy Contract, Status Presentation Contract, Typography, Color)
  </read_first>
  <behavior>
    - Given status="decay" and no active snooze: renders ring color = ringVisual("decay",colors).color and ring width = ringVisual().width (escalating weight on the AVATAR ring), and mounts StatusGlyph state="decay".
    - Given status=null (never-contacted): renders a faint neutral border ring at reduced opacity and does NOT mount StatusGlyph (no status-neutral silhouette).
    - Given snooze_until active for `now`: displayState resolves to "snoozed" via isSnoozed, ring becomes neutral border, glyph = StatusGlyph state="snoozed" (moon), underlying status suppressed.
    - Given isFavourite=true: star renders Icon name="favorite" state="active" tone="accent"; false -> tone="textSecondary"; pressing it calls onToggleFavourite once; hit area >= 44px (hitSlop).
    - a11y: the card exposes a colour-independent description from buildRowAccessibilityDescription (name, category, recency, favourite, displayState).
  </behavior>
  <action>
    Create `GridCard`, a presentational component (NO DB read, NO getExecutor, NO useNavigation — the ContactCard/ListRow contract). Props mirror the relevant subset of ListRowProps: contactId, name, photo, modifiedAt, categoryLabel, lastContact, snoozeUntil, status (ProfileStatus | null), now, onPress, isFavourite, onToggleFavourite, and (added later) line3 / searchResult / searchSnippet — declare optional line3/search props now so Plan 04 wires them without a signature change. Anatomy per UI-SPEC: a `GlassSurface` scrim (RADII.lg/xl, density-driven opacity, blurAvailable=false default per F-4) containing a large circular `Avatar` (contactId + cacheBust=modifiedAt — anti-face-flash correctness in a virtualized grid), the status ring wired onto the avatar (borderColor = ringVisual(displayState==="snoozed" ? null : displayState, colors).color, borderWidth = ringVisual().width, opacity = ringVisual().opacity), a `StatusGlyph` attached to the avatar edge (mounted only when displayState !== null; snooze -> state="snoozed"), then three single-line text rows (numberOfLines=1, ellipsizeMode tail): row 1 name (label role, textPrimary), row 2 recency via formatListRecency(lastContact, now) (caption, textSecondary), row 3 reserved for adaptive context/prompt (Plan 04 — render null/placeholder for now). Top-right favourite star: `Icon name="favorite"`, tone accent when marked else textSecondary, `hitSlop={SPACING.sm}` for the >=44px target, `onPress={onToggleFavourite}`. Compose displayState exactly as ListRow (const displayState = isSnoozed(snoozeUntil, now) ? "snoozed" : status). Category chip and literal status text are OMITTED (UI-SPEC SS D). All colours resolve through theme tokens; no hex anywhere. Add a co-located `src/components/GridCard.test.tsx` only if the render is node-testable under the repo Vitest RN setup; otherwise the render is device-UAT and the automated gate is tsc + check:colors (see verify).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: a GridCard shows the circular avatar dominant, a status ring + edge glyph for an evaluated contact, no glyph for a never-contacted contact, a neutral ring + moon for a snoozed contact, and a tappable star that fills/unfills instantly with a light haptic.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: GridCard imports ringVisual, StatusGlyph, Avatar, Icon, GlassSurface, isSnoozed, formatListRecency, buildRowAccessibilityDescription — and does NOT import getExecutor, useNavigation, or any src/db read module (grep the file).
    - Source assertion: GridCard never references or sorts by favourite_rank; isFavourite is a boolean prop.
    - `npx tsc --noEmit` and `npm run check:colors` both clean.
    - Never-contacted (status=null) path does not mount StatusGlyph (source: StatusGlyph render is guarded by displayState !== null).
  </acceptance_criteria>
  <done>GridCard renders the avatar-first anatomy with ring+glyph+star from shared primitives, presentational-only, tsc + check:colors green.</done>
</task>

<task type="tracer">
  <name>Task 3: CardGrid renderer + replace HomeScreen card branch (real grid, tap->Profile, favourite toggle)</name>
  <files>src/components/CardGrid.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (the SINGLE shared FlatList 1055-1119 wrapped in the resultTransitionStyle Animated.View at 1054; the renderItem viewMode branch 1058-1104; the card branch to REPLACE 1091-1104; the list branch's favourite overlay + toggle 1071-1079 (favouriteOverlay.get(item.id) ?? item.favourite_rank !== null; toggleFavourite); listNow; goToProfile; and CRUCIALLY the FlatList-level props the card branch must also honour: data={error||showInitialSkeleton?[]:rows} (1056), ListHeaderComponent={listHeader} (1105), ListEmptyComponent={showInitialSkeleton?<ListLoadingSkeleton/>:listEmpty} (1106), contentContainerStyle with paddingBottom:bottomClearance (1107-1110), refreshControl RefreshControl onRefresh/refreshing (1111-1118))
    - src/components/CandidateCardGrid.tsx (in-repo FlatList numColumns/columnWrapperStyle grid analog, lines 155-170)
    - src/db/dashboard-read.ts (DashboardRow shape 91-110)
    - .planning/phases/28-dashboard-card-view/28-RESEARCH.md (Grid & Renderer Architecture: numColumns re-mount gotcha, Pitfall 4; Avatar recycling correctness)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Grid & Card Anatomy Contract — column rules, portrait-locked)
  </read_first>
  <action>
    Create `CardGrid`: a virtualized grid renderer whose prop contract EXPLICITLY threads the full shared-list surface (addresses review — the CardGrid prop contract must not silently drop pull-to-refresh/error-retry/header/clearance). Props: `rows: DashboardRow[]`, `now: string`, `onPressContact(id)`, `favouriteOverlay`, `onToggleFavourite(id, next)`, PLUS the list-surface props the shared FlatList owns today at HomeScreen.tsx:1055-1119 — `error: boolean`, `showInitialSkeleton: boolean` (to reproduce the `data = error||showInitialSkeleton ? [] : rows` gating), `listHeader` (→ ListHeaderComponent), `listEmpty` (→ ListEmptyComponent = showInitialSkeleton ? <ListLoadingSkeleton/> : listEmpty), `refreshing`+`onRefresh` (→ the same RefreshControl pull-to-refresh, which is ALSO the error-retry affordance), and `bottomClearance` (→ contentContainerStyle paddingBottom). Derive numColumns at runtime from `useWindowDimensions()` width + fontScale (3 on a normal portrait phone; 2 on very narrow width or large fontScale; more on wide width) — a single derived function; exact breakpoint px are DEFERRED to device UAT (UI-SPEC F-6). Render CardGrid's OWN `FlatList` with `numColumns={numColumns}`, `key={\`grid-${numColumns}\`}` (the re-mount gotcha — RESEARCH Pitfall 4), `keyExtractor={(item) => String(item.id)}`, `columnWrapperStyle` for the inter-card gutter (SPACING tokens), the threaded `data`/`ListHeaderComponent`/`ListEmptyComponent`/`refreshControl`/`contentContainerStyle` mirroring the list FlatList, and `renderItem` mapping each DashboardRow to `GridCard` with contactId=item.id, cacheBust via modifiedAt, status=item.status, isFavourite = favouriteOverlay.get(item.id) ?? (item.favourite_rank !== null), onToggleFavourite wired to compute rendered membership then call onToggleFavourite(item.id, !rendered), onPress => onPressContact(item.id). App is portrait-locked: "responsive" means column count as a function of width/fontScale, never a landscape layout. Then in HomeScreen, REPLACE the renderItem-level `viewMode === "card"` branch with a CONTAINER-LEVEL swap: inside the existing `<Animated.View style={[styles.listRegion, resultTransitionStyle]}>` (HomeScreen.tsx:1054), render the existing list `<FlatList>` when `query.viewMode === "list"` and `<CardGrid>` when `"card"`, passing CardGrid the same `rows`, `listNow`, `goToProfile`, `favouriteOverlay`, `toggleFavourite` AND the same `error`, `showInitialSkeleton`, `listHeader`, `listEmpty`, `refreshing`, `onRefresh`, `bottomClearance` the list FlatList consumes — so card mode keeps identical empty/error/loading/header/refresh/clearance behaviour. Exactly one scroll container mounts per view mode (never two nested); the branch moves from renderItem level to container level (it is NOT chosen the way it is today).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: Card View shows a 3-column avatar-first grid of real contacts; tapping a card opens the Profile; tapping a star fills/unfills instantly; switching List/Card preserves scroll-appropriate behaviour; zero-result populations show the same empty state as List.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: HomeScreen's `viewMode === "card"` branch renders `<CardGrid>` (not the legacy single-column `ContactCard`); grep HomeScreen shows CardGrid imported and used, and the swap is at container level inside the `resultTransitionStyle` Animated.View (not a renderItem branch).
    - Source assertion: CardGrid's grid FlatList sets a `key` containing `numColumns` (re-mount gotcha handled) and passes `cacheBust` derived from `modified_at` to each Avatar.
    - Source assertion: CardGrid accepts and applies the full list-surface prop set — `data` gated by `error`/`showInitialSkeleton`, `ListEmptyComponent` (skeleton vs listEmpty), `refreshControl` (pull-to-refresh + error-retry), `ListHeaderComponent` (listHeader), and `contentContainerStyle` with `bottomClearance` — so card mode retains pull-to-refresh, the "Pull down to try again" retry, the header, and bottom clearance (grep CardGrid for refreshControl/ListHeaderComponent/ListEmptyComponent/contentContainerStyle).
    - `npx tsc --noEmit`, `npm run check:colors`, and `npm test` (full node suite) all clean.
  </acceptance_criteria>
  <done>The card branch renders the real avatar-first grid end-to-end; tap opens Profile; star toggles via the existing optimistic path; empty/error/loading are the shared states. Tracer proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared query -> Card renderer | DashboardRow[] crosses from the shared read into a new renderer; the renderer must not re-derive status/favourite semantics |
| user tap -> nav / favourite write | a card tap either navigates (read-only) or triggers the existing favourite write path (no new write introduced here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | Avatar in virtualized grid | medium | mitigate | Pass contactId recyclingKey + cacheBust=modified_at so a recycled cell never shows the wrong face (correctness, not optimization) |
| T-28-02 | Information disclosure | favourite_rank in Card UX | low | mitigate | Membership read only (favourite_rank !== null); never sort/read rank for ordering (ADR-075) |
| T-28-03 | Spoofing (of state) | status ring/glyph source | low | mitigate | Ring + glyph resolve from the single DashboardRow.status via ringVisual/StatusGlyph; Card never re-derives the band |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO external package (RESEARCH: no new deps); no supply-chain surface |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across src.
- `npm run check:colors` clean (no colour literals in GridCard/CardGrid).
- `npm test` (full node suite) green — no regression in existing DAO/logic suites.
- Device UAT on the Pixel: 3-column grid renders real contacts; status ring+glyph; never-contacted neutral+no-glyph; snoozed neutral+moon; tap->Profile; star toggles optimistically.
</verification>

<success_criteria>
- Card View renders a real avatar-first grid over the shared DashboardRow[] (CARDV-01).
- Each card shows name + recency + status ring + edge glyph + always-visible star; never colour-only; never-contacted neutral+glyph-less (CARDV-02).
- Star toggles binary favourite membership via the existing optimistic path (CARDV-03).
- The 7 phase-foundational icon entries exist in the single registry; tsc green.
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-01-SUMMARY.md` when done.
</output>
