---
phase: 28-dashboard-card-view
plan: 07
type: execute
wave: 5
depends_on: [28-06, 28-02]
files_modified:
  - src/screens/HomeScreen.tsx
  - src/components/BulkActionSurface.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [CARDV-07, CARDV-08, CARDV-09, CARDV-10, CARDV-11, CARDV-12]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The replaced control area exposes the bulk actions: Quick Log, Log Interaction, Add to Favorites / Remove from Favorites, Snooze / Unsnooze, Set Category, Archive, and a More / Sensitive Operations entry — Bulk Edit and multi-recipient Message are NOT offered (SS T, CARDV-07)."
    - "Favourite and snooze bulk actions are EXPLICIT (Add to / Remove from Favorites, Snooze / Unsnooze) — never an ambiguous toggle-all (SS U, D-09)."
    - "Bulk Snooze and Set Category each open a NET-NEW picker (no existing bulk picker to reuse) composed from Sheet/overlay-base: the snooze picker offers the three SnoozePreset values (3d/1w/1m → '3 days'/'1 week'/'1 month', matching ContactProfileScreen SNOOZE_PRESETS) with testIDs bulk-snooze-preset-{preset} and calls bulkSnooze(preset); the category picker is sourced from listCategories (contact-read.ts:49, {id,name}[] by display_order) with testIDs bulk-category-{id} and calls bulkSetCategory(categoryId). Each row is a >=44px Pressable with an accessibility label; a committed write announces its outcome; neither is ConfirmDialog-gated (reversible, non-destructive — only large Quick Log / Archive / Frequency confirm) (addresses review A2)."
    - "Bulk Quick Log writes one generic current-time interaction per selected contact via bulkQuickLog (composed recency cores) and captures its returned { contactId, interactionId }[] receipt; small selections are immediate + an Undo Snackbar whose Undo calls undoBulkQuickLog(receipt) (the atomic batch reversal from Plan 02 — NOT the single-interaction createQuickLogUndoController, which keys on one interactionId at universal-fab-logic.ts:56,64-75 and cannot reverse N writes); large selections show a non-destructive ConfirmDialog (Log {n} interactions?) before the write; the threshold is a tuning constant (Claude's discretion) (CARDV-08, SS V; addresses review HIGH-2)."
    - "Detailed Log Interaction is count-aware: exactly 1 selected -> the individual detailed Log flow; 2+ selected -> Group Log; 0 -> guarded/disabled (CARDV-09, D-10). Because the GroupLog route currently takes NO params (navigation/types.ts:34), is a placeholder screen (DashboardStack.tsx), and Group-domain work is owned by Phase 33, THIS phase delivers the routing HANDOFF: it extends the GroupLog route param additively+serializably to carry the selected participant ids (GroupLog: { participantIds?: number[] } | undefined) and navigates 2+ selected to GroupLog with participantIds = the selected ids. Phase 28 owns ONLY the action + routing + the serializable param contract; CONSUMING participantIds into a real Group Event form is Phase 33's domain (explicitly deferred). This does NOT reverse any recorded decision (additive typing; the FAB's 'never preselects' at universal-fab-logic.ts:92 is FAB-scoped, not multi-select-scoped) (addresses review HIGH-1)."
    - "Bulk Archive is directly available (outside Sensitive Operations, a reversible lifecycle op), behind a non-destructive ConfirmDialog whose copy describes ARCHIVING not quarantine (Archived contacts leave the Dashboard. You can restore them from Archived Contacts.); it calls bulkArchive (composed immutable events) (CARDV-10, D-06/D-08, SS X)."
    - "Sensitive Operations is a distinct subsurface containing Change Contact Frequency ONLY; bulk Frequency applies one positive-integer value to all selected via bulkSetFrequency behind a ConfirmDialog summarizing count + value; Gravity is never offered as a bulk action (CARDV-11, SS Y/SS Z)."
    - "There is NO bulk Delete and NO destructive confirmation in Card View; permanent deletion stays a manual per-contact action on the Archived Contacts list (ADR-018) (CARDV-10, D-07)."
    - "After a successful ordinary bulk op, selection mode + selectedIds PERSIST (Quick Log stays selected + Undo/error feedback; Favorite/Snooze/Category/Frequency stay selected); Archive removes archived ids from selectedIds AND frozenUniverse via removeFromUniverse so archived cards vanish; the user exits explicitly (CARDV-12, SS AB)."
    - "The post-op dashboard refresh this plan triggers is safe ONLY because the frozen-universe fence is in place: Plan 03's toggle membership guard (store boundary) and Plan 06's frozen-universe render filter (renderer boundary) together guarantee a refresh that admits a new (out-of-universe) row cannot render a selectable card or add that id to the selection. This plan MUST NOT re-seed or recompute frozenUniverse after a bulk op — only removeFromUniverse on archive mutates it (D-12; addresses review A1)."
    - "Bulk feedback is commit-truthful (snackbar/announcement only on a committed write; a failed transaction rolls back and reports an error) — reusing the existing fast-action contract (SS AB/SS AF)."
    - "After each COMMITTED bulk write (and after a committed undoBulkQuickLog), HomeScreen calls notifyWidgetDataChanged() and bumpShellRefresh() ONCE — not once per contact — mirroring the single quick-log path (HomeScreen.tsx:405-406); a dashboard reload alone does NOT refresh the home-screen widget or other shell consumers (addresses review: widget/shell refresh once per commit)."
    - "CARDV-09 boundary: the 1 vs 2+ Log routing is exact (selectedIds.size); 0 selected disables Log routing."
    - "E6 bulk populated/empty/loading/error/overflow: actions in the replaced control area; 0-selected guards every action; large Quick Log / Archive / Frequency confirm BEFORE the write; a failed bulk op rolls back + reports; secondary/sensitive actions live in the More/Sensitive subsurface, not overflowing the control area."
    - "Result appearance/disappearance uses short restrained transitions gated by useReducedMotion() (instant apply when reduced motion is on); no animation is driven from React state per frame; motion pauses when unfocused/backgrounded (CLAUDE.md, SS AD)."
  artifacts:
    - src/components/BulkActionSurface.tsx
    - "HomeScreen bulk-action wiring (composers, confirm/undo via undoBulkQuickLog receipt, count-aware routing, after-op persistence, archived-vanish, widget/shell refresh, reduced-motion)"
    - "src/navigation/types.ts — GroupLog route param extended additively to { participantIds?: number[] } | undefined (the Phase 28->33 handoff contract)"
  key_links:
    - "BulkActionSurface actions -> bulk-actions-dao composers (one txn each) -> refresh shared model + notifyWidgetDataChanged() + bumpShellRefresh() once"
    - "bulkQuickLog -> receipt -> snackbar Undo -> undoBulkQuickLog(receipt) (atomic batch reversal)"
    - "bulkArchive success -> selection-store.removeFromUniverse(archivedIds) (cards vanish, selection persists)"
    - "Log Interaction -> count-aware route (1 individual / 2+ GroupLog with participantIds=selected ids; Phase 33 consumes them)"
    - "Quick Log -> snackbar Undo (small) / ConfirmDialog (large); Archive/Frequency -> ConfirmDialog"
  prohibitions:
    - "No bulk Delete action exists in Card View / multi-select; no destructive confirmation is shown (D-07)."
    - "Gravity must never be offered as a bulk action (derived-never-stored) (SS Y)."
    - "Bulk favourite/snooze must never be an ambiguous toggle-all — only explicit Add/Remove and Snooze/Unsnooze (D-09)."
    - "Bulk Archive confirmation copy must describe archiving, never quarantine or deletion (D-08)."
    - "The bulk surface must not implement Group Event persistence/participant/lifecycle/fan-out — it only routes 2+ to Group Log (D-10)."
---

<objective>
Wire the bulk-action data layer (Plan 02) into the multi-select surface (Plan 06): the action set in the replaced control area, count-aware Log routing, confirmation/Undo, Sensitive Operations (Frequency only), after-op selection persistence, archived-cards-vanish, and reduced-motion-aware result transitions. This completes Card View as Orbit's Dashboard bulk-management surface.

Purpose: CARDV-07/08/09/10/11 and the persistence half of CARDV-12.
Output: src/components/BulkActionSurface.tsx + HomeScreen bulk wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- `BulkActionSurface` (src/components/BulkActionSurface.tsx) — the selection-mode action set + Sensitive Operations subsurface
- Net-new bulk **Snooze preset picker** (host-owned, composed from Sheet/overlay-base): 3 rows for SnoozePreset 3d/1w/1m, testIDs `bulk-snooze-preset-3d` / `-1w` / `-1m`
- Net-new bulk **Set Category picker** (host-owned, composed from Sheet/overlay-base): rows from listCategories, testIDs `bulk-category-{id}`
- HomeScreen bulk wiring: composer calls, ConfirmDialog/Snackbar feedback, undoBulkQuickLog(receipt) wiring, count-aware Log routing, snooze/category pickers + outcome announcements, removeFromUniverse on archive, notifyWidgetDataChanged()+bumpShellRefresh() per commit, reduced-motion transitions
- `GroupLog` route param contract extended to `{ participantIds?: number[] } | undefined` in src/navigation/types.ts (Phase 28->33 handoff; consumption owned by Phase 33)
</artifacts_this_phase_produces>

<flagged_assumption owner_bucket="true">
**OPEN — per-action eligibility of the frozen selection universe (OWNER DECISION, not the planner's to settle).** Bulk actions currently apply to EVERY selected id regardless of contact type. Two combinations are schema-legal but semantically odd: (a) bulkSetFrequency on an Unbound contact (`tracking_enabled = 0`) — the migration-011 `CHECK (tracking_enabled = 0 OR interval_days IS NOT NULL)` is satisfied by any positive value, so it sets a cadence on an untracked contact; (b) bulk Snooze on a never-contacted contact. The dossier/UI-SPEC define the frozen universe as the "eligible result universe" but never define PER-ACTION eligibility. This changes product behavior, so per CLAUDE.md ("risk/product posture is the owner's bucket") it is surfaced, not decided here.

**Safe default this plan ships pending the owner's answer:** apply each bulk action to ALL selected contacts (no per-action filtering) — the least-surprising, schema-legal behavior, matching how the single-contact actions already behave. If the owner wants per-action filtering (e.g. hide/disable Set Frequency when the selection contains Unbound contacts), that is an additive follow-up. The convergence orchestrator surfaces this to the owner; execute-phase proceeds with the safe default unless told otherwise. (Recorded here so it is visible to execute-phase and the reviewer — addresses review: per-action eligibility owner-bucket item.)
</flagged_assumption>

<tasks>

<task type="auto">
  <name>Task 1: BulkActionSurface component — action set + Sensitive Operations (Frequency only)</name>
  <files>src/components/BulkActionSurface.tsx</files>
  <read_first>
    - src/components/ui/Sheet.tsx / overlay-base.tsx (the More/Sensitive Operations subsurface pattern — COMPOSE from these). NOTE src/components/OverflowMenu.tsx is text-only (OverflowAction = label/onPress/disabled/accessibilityLabel/testID at 27-37, literal ⋯ trigger at 65) — do NOT assume it renders semantic-icon action rows; if a shared row is wanted, compose Sheet directly or deliberately extend the API (addresses review: OverflowMenu text-only)
    - src/components/ui/Button.tsx + button-roles.ts, src/components/icons/Icon.tsx + icon-registry.ts (archive/category/frequency/snooze/select-all semantic icons added in Plan 01)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Multi-Select / Bulk-Management Contract SS T/SS U/SS X/SS Y/SS Z; Copywriting Contract bulk-operation copy; Color — Archive is NOT styled destructive)
    - .planning/phases/28-dashboard-card-view/28-CONTEXT.md (D-07 no Delete; D-09 explicit fav/snooze; D-10 count routing; D-11 Sensitive Ops = Frequency only)
  </read_first>
  <behavior>
    - Renders (in the replaced control area) the ordinary actions: Quick Log, Log Interaction, Add to Favorites, Remove from Favorites, Snooze, Unsnooze, Set Category, Archive, and a More / Sensitive Operations entry.
    - The Sensitive Operations subsurface contains Change Contact Frequency only. No Delete anywhere. No Gravity.
    - Every action is a callback prop; actions are disabled/guarded when the selected count is 0.
    - Archive is presented as an ordinary (non-destructive-styled) action.
  </behavior>
  <action>
    Create BulkActionSurface, presentational. Props: selectedCount, plus a callback per action (onQuickLog, onLogInteraction, onAddFavourites, onRemoveFavourites, onSnooze, onUnsnooze, onSetCategory, onArchive, onChangeFrequency) and onExit. Render the ordinary action controls (Buttons/Pressables >=44px with semantic Icons) in the replaced control area; place Change Contact Frequency behind a distinct More / Sensitive Operations subsurface COMPOSED from Sheet/overlay-base (do NOT route icon rows through the text-only OverflowMenu/OverflowAction API — compose Sheet directly, or deliberately extend the row API if a shared abstraction is preferred). Favourite and snooze are EXPLICIT separate controls (Add to Favorites / Remove from Favorites, Snooze / Unsnooze) — never a toggle-all. Archive is an ordinary control (NOT destructive-styled — it is reversible). Do NOT render Delete or any Gravity control. Disable/guard all actions when selectedCount === 0. Add locked testIDs (e.g. bulk-action-{name}, bulk-sensitive-frequency). No DB read, no navigation, no ConfirmDialog logic, and NO snooze-preset/category picker inside this component — onSnooze and onSetCategory are plain callbacks; the host (Task 2) presents the net-new snooze-preset and category pickers and owns confirm/undo/routing. All colours tokenized; Archive uses ordinary tokens, not danger.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: BulkActionSurface renders explicit Add/Remove Favorites + Snooze/Unsnooze (not a toggle-all); Archive control uses ordinary tokens (grep shows no danger/destructive styling on Archive); no Delete/Gravity control exists.
    - Source assertion: Change Contact Frequency is the ONLY item in the Sensitive Operations subsurface.
    - Source assertion: all actions guard on selectedCount === 0; no DB/nav import in the component.
    - `npx tsc --noEmit`, `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The bulk action surface + Sensitive Operations (Frequency only) exists, explicit fav/snooze, no Delete/Gravity, Archive non-destructive; tsc + check:colors green.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen — wire composers, confirm/undo, count-aware Log routing</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (the selection-mode control-area replacement from Plan 06 — mount BulkActionSurface there; selectedIds/frozenUniverse/removeFromUniverse from the store; getExecutor usage; goToProfile; the individual Log Interaction route; the commit-truthful Quick Log path 394-409 and CRUCIALLY notifyWidgetDataChanged + bumpShellRefresh called at 405-406 after a committed quick log — the shell/widget refresh helpers to reuse)
    - src/db/bulk-actions-dao.ts (bulkQuickLog returns { contactId, interactionId }[] receipt; undoBulkQuickLog(exec, receipt, now); bulkArchive, bulkAddFavourites, bulkRemoveFavourites, bulkSnooze, bulkUnsnooze, bulkSetCategory, bulkSetFrequency)
    - src/navigation/types.ts (DashboardStackParamList — GroupLog is currently `undefined` at :34, a placeholder; this task extends it additively to `{ participantIds?: number[] } | undefined`; note the serializable-only route-param convention documented throughout this file)
    - src/components/universal-fab-logic.ts (createQuickLogUndoController keys on ONE interactionId at :56,64-79 — do NOT use it for bulk; the FAB "never preselects" comment at :92 is FAB-scoped, NOT a constraint on multi-select routing)
    - src/components/ui/ConfirmDialog.tsx (title/message/confirmLabel/onConfirm/onCancel/destructive — use destructive=false for all Card confirms)
    - src/stores/snackbar-store.ts (showSnackbar({kind,label,action:{label,onPress}}) — the Undo affordance)
    - src/db/snooze-dao.ts (SnoozePreset "3d"|"1w"|"1m" at :39, PRESET_MODIFIERS at :46 — the bulk snooze preset choice) and src/screens/ContactProfileScreen.tsx (SNOOZE_PRESETS 127-129: labels "3 days"/"1 week"/"1 month" — reuse the same labels for the bulk snooze picker so the affordance matches the profile)
    - src/db/contact-read.ts (listCategories at :49 — `listCategories(exec) -> { id: number; name: string }[]` ordered by display_order; THIS is the Set Category source, the same read the create/edit category pickers use) and src/screens/ImportReviewScreen.tsx / BulkImportSetupScreen.tsx (existing category-picker precedent: a Picker over listCategories with an "Uncategorized"/"No category" option — mirror the option shape)
    - src/utils/dates.ts (localDateTime()/formatLocalDate() — the now value; never toISOString)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Copywriting Contract — exact confirm/undo/announcement copy)
  </read_first>
  <action>
    First, extend the GroupLog route param in src/navigation/types.ts from `GroupLog: undefined` to `GroupLog: { participantIds?: number[] } | undefined` — an ADDITIVE, serializable-only change (matching the file's route-param convention); this is the Phase 28->33 handoff contract and reverses nothing (Phase 33 consumes participantIds later). Then mount BulkActionSurface in the selection-mode control area (Plan 06) with selectedCount = selectedIds.size. Wire each action against getExecutor() and a localDateTime() now (never toISOString):
    onQuickLog -> if size <= THRESHOLD (a top-of-file tuning constant, Claude's discretion per SS V) call bulkQuickLog immediately, capture its returned { contactId, interactionId }[] receipt, then showSnackbar success with an Undo action whose onPress calls undoBulkQuickLog(getExecutor(), receipt, localDateTime()) (the atomic batch reversal — do NOT use createQuickLogUndoController, which reverses only one interaction); else show a non-destructive ConfirmDialog (Log {n} interactions?) then bulkQuickLog on confirm.
    onAddFavourites/onRemoveFavourites -> bulkAddFavourites/bulkRemoveFavourites (explicit).
    onSnooze -> present a NET-NEW bulk snooze-preset PICKER (there is no existing bulk preset picker to reuse; the profile's SNOOZE_PRESETS chips are per-contact) — a small picker COMPOSED from Sheet/overlay-base offering all three SnoozePreset values in order: "3d" ("3 days"), "1w" ("1 week"), "1m" ("1 month") (matching ContactProfileScreen SNOOZE_PRESETS labels), each row a Pressable >=44px with accessibilityRole="button" + label "Snooze {n} contacts for {label}", testID `bulk-snooze-preset-{preset}` (bulk-snooze-preset-3d / -1w / -1m); selecting a preset calls bulkSnooze(getExecutor(), [...selectedIds], preset, localDateTime()) then announces the outcome (commit-truthful; see below). onUnsnooze -> bulkUnsnooze (no preset).
    onSetCategory -> present a NET-NEW bulk category PICKER (also net-new for the bulk surface) — COMPOSED from Sheet/overlay-base, sourced from listCategories(getExecutor()) (contact-read.ts:49, `{ id, name }[]` ordered by display_order — the same read the create/edit pickers use), each category a Pressable >=44px row with accessibilityRole="button" + label "Set category to {name} for {n} contacts", testID `bulk-category-{id}`; selecting one calls bulkSetCategory(getExecutor(), [...selectedIds], categoryId, localDateTime()) then announces the outcome. (Set Category applies an existing category; it does NOT create categories and shows no "Uncategorized" write option — bulkSetCategory binds a real category_id.) (addresses review A2: both net-new pickers specified — affordance, source, testIDs, a11y/announce copy.)
    Bulk Snooze and Set Category are reversible, non-destructive ops and are NOT gated by a ConfirmDialog (the Copywriting Contract confirms only large Quick Log, Archive, and Frequency) — the picker SELECTION is the commit trigger; on a committed write announce the outcome via announceForAccessibility: Snooze -> "Snoozed {n} contacts" (n = selectedIds.size), Set Category -> "Set category to {name} for {n} contacts" (a failed/rolled-back write announces the shared error instead). These join the bulk-outcome announcements wired in Task 3.
    onArchive -> non-destructive ConfirmDialog with the archiving copy (Archive {n} contacts? / Archived contacts leave the Dashboard. You can restore them from Archived Contacts.) then bulkArchive on confirm, and on success call selection-store.removeFromUniverse(archivedIds) so archived cards vanish while remaining selections persist.
    onChangeFrequency -> collect a positive-integer interval then a ConfirmDialog summarizing count + value, then bulkSetFrequency.
    onLogInteraction -> COUNT-AWARE: if selectedIds.size === 1 route to the individual detailed Log Interaction flow for that id; if >= 2 navigate to GroupLog with params { participantIds: [...selectedIds] } (the selected ids preloaded as participants — this is the routing handoff; do NOT implement Group Event domain behavior, which is Phase 33's); if 0, disabled.
    After every ordinary successful op, KEEP selection mode + selectedIds (do not exit); feedback is commit-truthful (snackbar/announcement only on a committed write; a rolled-back failure reports an error). After each COMMITTED bulk op (and after a committed undoBulkQuickLog), call notifyWidgetDataChanged() and bumpShellRefresh() ONCE (not per contact — mirror HomeScreen.tsx:405-406) so the widget + shell consumers update, THEN refresh the shared dashboard model so cards reflect new recency/favourite/snooze/category (reuse the existing refresh path). Do NOT add a bulk Delete, a destructive confirm, or a Gravity action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test 2>&1 | tail -5</automated>
    <human-check>On the Pixel: with N selected, each bulk action applies to all N and gives commit-truthful feedback; small Quick Log is immediate + Undo, large Quick Log / Archive / Frequency confirm first; Archive makes those cards vanish while selection persists; Log Interaction opens the individual flow for 1 and Group Log (preloaded) for 2+; the bulk Snooze picker shows 3 days / 1 week / 1 month and the Set Category picker lists the real categories; no Delete/Gravity anywhere.</human-check>
    <human-check>Frozen-universe fence (A1) on the Pixel: enter selection, then cause a mid-selection refresh that changes the matching rows (e.g. commit a bulk op, or trigger a focus/pull-to-refresh re-query that would admit a new contact) and confirm NO newly-appearing contact renders a selectable card or can be added to the selection — only the frozen snapshot's contacts remain selectable.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/navigation/types.ts GroupLog is `{ participantIds?: number[] } | undefined` (additive, serializable); no other route param is changed.
    - Source assertion: every bulk action calls its bulk-actions-dao composer with getExecutor() + a localDateTime() now (no toISOString anywhere in the wiring).
    - Source assertion: the small-selection Quick Log Undo onPress calls undoBulkQuickLog(...receipt...) — NOT createQuickLogUndoController; the receipt returned by bulkQuickLog is captured and passed to it.
    - Source assertion: onLogInteraction branches on selectedIds.size (1 -> individual; >=2 -> navigate("GroupLog", { participantIds: [...selectedIds] })); no Group Event persistence is implemented in HomeScreen.
    - Source assertion: each committed bulk op (and the undo) calls notifyWidgetDataChanged() and bumpShellRefresh() exactly once (not inside a per-contact loop).
    - Source assertion: onArchive uses a non-destructive ConfirmDialog (destructive=false) with archiving copy and calls removeFromUniverse on success; no bulk Delete / destructive confirm exists.
    - Source assertion (A2): the bulk snooze picker offers exactly the three SnoozePreset values with testIDs bulk-snooze-preset-3d / -1w / -1m and calls bulkSnooze with the chosen preset; the bulk category picker is sourced from listCategories, renders rows with testID bulk-category-{id}, and calls bulkSetCategory(categoryId); each picker row is a >=44px Pressable with an accessibility label, and a committed snooze/category write announces its outcome ("Snoozed {n} contacts" / "Set category to {name} for {n} contacts").
    - Source assertion (A1): the bulk-refresh wiring relies on the frozen-universe fence being in place — Plan 03's toggle membership guard AND Plan 06's frozen-universe render filter — so the post-op dashboard refresh this task triggers cannot make a new (out-of-universe) row selectable. Reading the bulk-op success handlers confirms none re-seed or recompute the frozen universe after a bulk op; only removeFromUniverse (on archive) mutates it.
    - Source assertion: after ordinary ops selection mode is not exited (no exitSelection in the success paths except the explicit exit control).
    - `npx tsc --noEmit`, `npm run check:colors`, full `npm test` clean.
  </acceptance_criteria>
  <done>All bulk actions are wired with confirm/undo + count-aware Log routing + archived-vanish + persisted selection; commit-truthful; tsc + check:colors + suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Reduced-motion-aware result transitions + selection-mode a11y announcements</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (the CardGrid render + the resultTransitionStyle the list branch already uses ~1053; how the list branch handles reduced motion)
    - src/theme/use-reduced-motion.ts (useReducedMotion() React twin; useReducedMotionShared() Skia twin)
    - src/theme/tokens (MOTION.fast/MOTION.base, EASING.decelerate)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Motion Contract SS AD; Accessibility Contract SS AF — count + bulk-outcome announcements)
    - CLAUDE.md (no setState-per-frame; pause on useIsFocused===false / AppState background)
  </read_first>
  <action>
    Apply the shared restrained result-transition treatment to the CardGrid result region (reuse the list branch's resultTransitionStyle / MOTION tokens), gated by useReducedMotion(): when reduced motion is on, apply results instantly (no enter/exit/reorder animation). Do not drive any grid animation from React state per frame; ensure continuous motion (if any) pauses on useIsFocused===false and AppState background (mirror the existing dashboard rules). Add commit-truthful accessibility announcements for bulk-operation outcomes (e.g. announceForAccessibility on success/failure of each bulk op) in addition to the selection-count announcement from Plan 06, so assistive-tech users get outcome feedback. Keep the empty/error/loading states shared with List (already wired). All colours tokenized.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test 2>&1 | tail -5</automated>
    <human-check>On the Pixel with Reduced Motion ON: result changes apply instantly (no card enter/exit animation); with it OFF: short restrained transitions; bulk-op outcomes are announced by TalkBack.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: the CardGrid result transition is gated by useReducedMotion() (instant apply when on); no per-frame setState animation is introduced.
    - Source assertion: bulk-operation outcomes are announced via AccessibilityInfo.announceForAccessibility (or equivalent live region).
    - `npx tsc --noEmit`, `npm run check:colors`, full `npm test` clean.
  </acceptance_criteria>
  <done>Result transitions are reduced-motion-aware and focus/background-paused; bulk outcomes are announced; tsc + check:colors + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection surface -> bulk DAO | a confirmed action fans a value across N contacts; correctness/atomicity live in the DAO (Plan 02) |
| destructive-looking action -> lifecycle | Archive removes from the Dashboard but is reversible; permanent deletion is out of this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-17 | Repudiation | bulk op reporting success without a commit | high | mitigate | Commit-truthful feedback — snackbar/announcement only on a committed transaction; a rolled-back failure reports an error (reuse the fast-action contract) |
| T-28-18 | Elevation of privilege | an unconfirmed large destructive-feeling batch | medium | mitigate | Large Quick Log / Archive / Frequency require a ConfirmDialog BEFORE the write; small Quick Log is reversible via Undo |
| T-28-19 | Tampering | bulk Delete / quarantine / auto-purge sneaking in | high | mitigate | No bulk Delete, no destructive confirm, no quarantine/auto-purge (D-07/D-08); Archive is the only removal and is reversible |
| T-28-20 | Repudiation | archived cards lingering / re-selectable | low | mitigate | removeFromUniverse drops archived ids from selectedIds + frozenUniverse on success |
| T-28-21 | Information disclosure | GroupLog participant handoff over route params | low | mitigate | participantIds is a serializable number[] (no PII, no callbacks — matches the file's deep-link-safe route-param convention); Phase 28 only passes ids, Phase 33 owns consumption |
| T-28-22 | Repudiation | widget/shell showing stale state after a bulk write | low | mitigate | notifyWidgetDataChanged()+bumpShellRefresh() fire once after each committed bulk op (and undo), mirroring the single quick-log path — a dashboard reload alone would leave the widget stale |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors` clean.
- `npm test` green (no regression; the bulk composers' node tests from Plan 02 remain the correctness proof).
- Device UAT on the Pixel: each bulk action applies to all selected + commit-truthful feedback; small vs large Quick Log; Archive confirm + vanish + persisted selection; count-aware Log routing (1 individual / 2+ Group Log preloaded); Sensitive Ops Frequency confirm; no Delete/Gravity; reduced-motion instant apply; TalkBack announcements. Observe a partial-failure rollback (inject a bad id) leaves no partial state.
</verification>

<success_criteria>
- Full bulk action set (Quick Log, Log, explicit Fav add/remove, Snooze/Unsnooze, Set Category, Archive, Sensitive Ops=Frequency only); no Bulk Edit / multi-recipient Message / Delete / Gravity (CARDV-07/11).
- Bulk Quick Log via composed recency cores, immediate + Undo (undoBulkQuickLog receipt) / large-confirm (CARDV-08).
- Count-aware Log routing 1->individual, 2+->GroupLog with participantIds handed off via the additive serializable route param (Phase 33 consumes; Phase 28 owns action+routing only) (CARDV-09).
- Bulk Archive = reversible confirmation + archived-vanish; permanent delete stays manual per-contact (CARDV-10).
- After ordinary ops selection persists; commit-truthful feedback; reduced-motion + a11y announcements (CARDV-12).
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-07-SUMMARY.md` when done.
</output>
