---
phase: 28-dashboard-card-view
plan: 05
type: execute
wave: 3
depends_on: [28-04, 28-03]
files_modified:
  - src/components/CardContextMenu.tsx
  - src/components/GridCard.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [CARDV-04]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Long-press on a card opens a per-contact context menu with the LOCKED 8-item order: View Profile, Quick Log, Log Interaction, Message, Edit Contact, Favorite/Unfavorite, Snooze/Unsnooze, Select (CARDV-04)."
    - "Favorite/Unfavorite and Snooze/Unsnooze labels reflect the contact's current membership/snooze state; the item labels switch accordingly."
    - "The menu EXCLUDES Delete and Archive (and any high-impact bulk op) — those never appear in the long-press menu (SS M)."
    - "Card View does NOT add swipe gestures — it does not mirror List View's swipe accelerators; power interactions are long-press + multi-select (SS L)."
    - "Menu actions route to canonical flows (Card owns routing only, not forms/business rules): View Profile -> Profile; Quick Log -> immediate-write contract; Log Interaction -> individual detailed Log flow; Message -> compose/message flow; Edit Contact -> edit flow; Favorite -> favourites-dao mark/clear; Snooze/Unsnooze -> snooze flow; Select -> enterSelection (Plan 03 store)."
    - "CARDV-04 adjacency: a tap and a long-press are disambiguated by the gesture recognizer — a tap opens the Profile, a long-press opens the menu; they never both fire."
    - "CARDV-04 idempotency: invoking a menu action closes the menu; opening the menu twice on the same card yields the same actions (no duplicate mount)."
    - "CARDV-04 concurrency: only one context menu is open at a time (opening on one card dismisses any other)."
    - "CARDV-04 ordering: the 8-item order is fixed and never reorders."
    - "The per-contact actions are exposed as card-level accessibilityActions + onAccessibilityAction ON THE GRIDCARD ITSELF (following ListRow.tsx:141-155's accessibilityActions/onAccessibilityAction pattern) — at minimum Select, View Profile, Quick Log, Log Interaction, Message, Edit — so assistive-tech users reach them WITHOUT performing a long-press to open the menu; making the opened menu's rows accessible is NOT sufficient on its own (addresses review: card-level accessibility actions, SS AF)."
    - "The context menu is built by composing the shared Sheet/overlay-base primitives (or a deliberately icon-extended action row) — NOT by assuming OverflowMenu supports semantic-icon rows: OverflowAction is text-only (label/onPress/disabled/accessibilityLabel/testID, OverflowMenu.tsx:27-37) and renders a literal ⋯ trigger, so the icon-per-row menu (surfaceElevated sheet over a background scrim; each row a Pressable at-least-44px with a semantic Icon) is composed from Sheet/overlay-base, not from OverflowMenu as-is (addresses review: OverflowMenu text-only API)."
  artifacts:
    - src/components/CardContextMenu.tsx
    - "GridCard onLongPress prop"
    - "HomeScreen context-menu state + routing"
  key_links:
    - "GridCard onLongPress -> HomeScreen opens CardContextMenu for that contact"
    - "menu items -> canonical routes (Profile / Quick Log / Log / Message / Edit / Favourite / Snooze) + enterSelection"
  prohibitions:
    - "The long-press menu must never contain Delete, Archive, or any high-impact bulk operation (SS M)."
    - "Card View must not add swipe gestures (no duplication of List swipe accelerators) (SS L)."
---

<objective>
Add Card View's normal-mode power interaction: the long-press per-contact context menu with the locked 8-item order, routing each action to its canonical flow. This is the entry that also seeds multi-select via its Select item. Card owns the menu + routing only — never the forms or business rules.

Purpose: CARDV-04 — tap opens Profile, long-press exposes the decided per-contact power menu, no swipe duplication.
Output: src/components/CardContextMenu.tsx, GridCard onLongPress, HomeScreen menu state + routing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- `CardContextMenu` (src/components/CardContextMenu.tsx) — the 8-item long-press action sheet
- GridCard `onLongPress` prop
- HomeScreen context-menu open state + action routing
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CardContextMenu component — locked 8-item action sheet (state-aware labels, a11y actions)</name>
  <files>src/components/CardContextMenu.tsx</files>
  <read_first>
    - src/components/OverflowMenu.tsx (REFERENCE for the sheet/scrim STYLING only — but note OverflowAction is TEXT-ONLY: label/onPress/disabled/accessibilityLabel/testID at 27-37, NO icon field, and the trigger is a literal ⋯ Text at 65; it does NOT support the semantic-icon rows this menu needs)
    - src/components/ui/Sheet.tsx, src/components/ui/overlay-base.tsx (surfaceElevated sheet over a background scrim; overlay lifecycle — COMPOSE the icon menu from these, not from OverflowMenu as-is)
    - src/components/icons/Icon.tsx + icon-registry.ts (semantic icons incl. the new select/snooze/edit/message entries)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (Interaction Contract — Normal Mode; the locked 8-item order; SS AF a11y actions)
  </read_first>
  <behavior>
    - Renders exactly 8 rows in order: View Profile, Quick Log, Log Interaction, Message, Edit Contact, Favorite|Unfavorite (by isFavourite), Snooze|Unsnooze (by isSnoozed), Select.
    - Each row is a Pressable >=44px with a semantic Icon and a callback prop; invoking a row closes the menu.
    - Exposes the same actions as accessibility actions so they are reachable without long-press dexterity.
  </behavior>
  <action>
    Create CardContextMenu as a presentational action sheet by COMPOSING Sheet/overlay-base (surfaceElevated sheet over a background scrim) — do NOT try to pass icon rows to OverflowMenu, whose OverflowAction is text-only (OverflowMenu.tsx:27-37); if a shared row abstraction is wanted, either compose Sheet directly with semantic-icon rows (preferred) or DELIBERATELY extend OverflowAction with an optional semantic icon field (a conscious API change, not an accidental assumption). Props: visible, contactId, name, isFavourite, isSnoozed, and one callback per action (onViewProfile, onQuickLog, onLogInteraction, onMessage, onEditContact, onToggleFavourite, onToggleSnooze, onSelect), plus onRequestClose. Render the LOCKED 8-row order with semantic Icons (favorite/message/edit/snooze/select from the registry; View Profile/Quick Log/Log Interaction use suitable existing semantic names). Item 6 label is Favorite or Unfavorite by isFavourite; item 7 is Snooze or Unsnooze by isSnoozed. Every row Pressable >=44px; selecting a row fires its callback then onRequestClose. Do NOT include Delete/Archive. Add locked testIDs (e.g. card-context-menu-{action}). Ensure each row has accessibilityRole=button + label so assistive tech reaches them inside the open sheet (the card-level accessibilityActions in Task 2 are the primary no-long-press path). No DB read, no navigation inside the component (caller wires callbacks). All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: CardContextMenu renders exactly the 8 actions in the locked order; no Delete/Archive row exists (grep the file).
    - Source assertion: the icon rows are composed from Sheet/overlay-base (or a deliberately icon-extended row) — NOT by passing an icon into the text-only OverflowAction (grep shows the menu does not attempt an `icon` field on OverflowAction).
    - Source assertion: Favorite/Snooze row labels are conditional on isFavourite/isSnoozed props.
    - Source assertion: no DB/nav import in the component (callbacks only).
    - `npx tsc --noEmit`, `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The 8-item context menu component exists, state-aware labels, no destructive rows; tsc + check:colors green.</done>
</task>

<task type="auto">
  <name>Task 2: GridCard onLongPress + tap/long-press disambiguation</name>
  <files>src/components/GridCard.tsx</files>
  <read_first>
    - src/components/GridCard.tsx (the card Pressable / onPress wiring from Plan 01)
    - src/components/ListRow.tsx (the row Pressable structure AND the accessibilityActions/onAccessibilityAction pattern at 141-155 — the exact idiom to copy for card-level a11y actions: an accessibilityActions array of {name,label} + an onAccessibilityAction switch on event.nativeEvent.actionName)
    - react-native Pressable onLongPress semantics (tap vs long-press are mutually exclusive by default)
  </read_first>
  <action>
    Add an optional onLongPress?: () => void prop to GridCard and wire it to the card's outer Pressable's onLongPress so a long-press opens the menu while a tap still calls onPress (RN Pressable fires either onPress OR onLongPress, not both — the disambiguation). Do not add any swipe gesture. Keep the favourite star's own Pressable independent (its press must not trigger the card long-press). ALSO add card-level accessibility actions on the outer Pressable following ListRow.tsx:141-155: declare optional per-action callback props (onViewProfile, onQuickLog, onLogInteraction, onMessage, onEditContact, onSelect) and, when they are provided, set accessibilityActions to the corresponding {name,label} entries (view-profile, quick-log, log-interaction, message, edit-contact, select) plus an onAccessibilityAction that switches on event.nativeEvent.actionName and invokes the matching callback — so an assistive-tech user reaches these WITHOUT a long-press. Thread onLongPress and the new a11y-action callbacks through CardGrid's renderItem (a props addition, no re-mount change).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: GridCard's outer Pressable wires both onPress and onLongPress; no PanGestureHandler/Swipeable is added to the card.
    - Source assertion: GridCard's outer Pressable declares accessibilityActions + onAccessibilityAction covering at least Select, View Profile, Quick Log, Log Interaction, Message, Edit (following ListRow.tsx:141-155) — not only the opened-menu rows.
    - Source assertion: CardGrid threads onLongPress and the per-action a11y callbacks to GridCard.
    - `npx tsc --noEmit`, `npm run check:colors` clean.
  </acceptance_criteria>
  <done>GridCard supports long-press without adding swipe; tap and long-press are disambiguated; per-contact actions are exposed as card-level accessibility actions.</done>
</task>

<task type="auto">
  <name>Task 3: HomeScreen — open the menu on long-press + route each action to its canonical flow</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (goToProfile; onLogInteraction/onEditContact used by the list branch; toggleFavourite + favouriteOverlay; how Quick Log / Message routing works from the shell/other surfaces; the CardGrid wiring from Plans 01/04)
    - src/db/favourites-dao.ts / snooze-dao.ts (single-contact mark/clear + snooze/clearSnooze for the menu's Favorite/Snooze actions)
    - src/stores/dashboard-selection-store.ts (enterSelection — the Select item entry; pass the current eligible universe + the long-pressed contact as seed)
    - src/components/CardContextMenu.tsx (the callback surface)
    - .planning/phases/28-dashboard-card-view/28-UI-SPEC.md (routing rules SS M; count-aware Log routing belongs to Plan 07 — here Log Interaction from the single-contact menu is the INDIVIDUAL flow)
  </read_first>
  <action>
    In HomeScreen, hold context-menu state (open contactId or null) and pass onLongPress to CardGrid so a card long-press opens CardContextMenu for that contact (only one open at a time; opening replaces any current). Wire each menu callback to the canonical flow: onViewProfile -> goToProfile(id); onQuickLog -> the existing immediate Quick Log write contract (reuse the shell/list Quick Log path with commit-truthful snackbar); onLogInteraction -> the INDIVIDUAL detailed Log Interaction flow (single contact — count-aware 2+ routing is Plan 07's multi-select concern, not here); onMessage -> the canonical Message/compose route; onEditContact -> onEditContact(id); onToggleFavourite -> toggleFavourite(id, next) (reuse the optimistic path); onToggleSnooze -> snooze/clearSnooze via the existing single-contact snooze flow then refresh; onSelect -> useDashboardSelectionStore.enterSelection(currentEligibleIds, id) (seed the long-pressed contact; the eligible id list is the current rows' ids — this becomes the frozen universe; the selection-mode UI is built in Plan 06). Pass isFavourite/isSnoozed for the tapped contact so labels reflect state. ALSO thread the SAME canonical-flow callbacks (onViewProfile, onQuickLog, onLogInteraction, onMessage, onEditContact, onSelect — each bound to the row's contactId) through CardGrid into GridCard's card-level accessibilityActions (Task 2), so an assistive-tech user triggers the identical routing without opening the menu; the long-press menu and the card a11y actions share one routing implementation. Do NOT add Archive/Delete. Ensure the menu closes after any action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: HomeScreen opens CardContextMenu on card long-press and wires all 8 callbacks; onSelect calls enterSelection with the current eligible ids + seed id.
    - Source assertion: HomeScreen also passes the per-contact canonical-flow callbacks through CardGrid to GridCard's card-level accessibilityActions (the a11y path and the menu path share the same routing).
    - Source assertion: onLogInteraction from the menu (and the card a11y action) routes to the individual flow (not Group Log) — 2+ routing is not introduced here.
    - `npx tsc --noEmit`, `npm run check:colors`, full `npm test` clean.
  </acceptance_criteria>
  <done>Long-press opens the routed 8-item menu; Select seeds multi-select via the store; tsc + check:colors + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long-press -> canonical flows | the menu launches routable flows; Card must not embed forms/business rules |
| menu Favorite/Snooze -> single-contact writers | these reuse existing mutexed writers (no new write path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-12 | Elevation of privilege | destructive action in an ordinary menu | medium | mitigate | Delete/Archive are excluded from the long-press menu (SS M); grep-asserted no such rows |
| T-28-13 | Tampering | Quick Log from the menu writing untruthfully | medium | mitigate | Reuse the existing commit-truthful Quick Log contract (snackbar only on a committed write) |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors` clean.
- `npm test` green.
- Device UAT: long-press opens the 8-item menu in locked order; tap opens Profile; Favorite/Snooze labels reflect state; no swipe on cards; Select enters selection mode (visuals land in Plan 06).
</verification>

<success_criteria>
- Long-press exposes the locked 8-item per-contact menu with state-aware labels, routed to canonical flows (CARDV-04).
- Delete/Archive excluded; no swipe duplication.
- Select seeds multi-select via the selection store.
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-05-SUMMARY.md` when done.
</output>
