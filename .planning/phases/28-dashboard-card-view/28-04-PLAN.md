---
phase: 28-dashboard-card-view
plan: 04
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/logic/card-line3-selection.ts
  - src/logic/card-line3-selection.test.ts
  - src/components/GridCard.tsx
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [CARDV-02, CARDV-03]
estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Card row 3 shows ONE adaptive context item drawn from the SAME candidate read the List view uses (readLine3Candidates) — the read is NOT forked; only a Card-specific compactness-biased selection is added."
    - "selectCardLine3 PRESERVES the analog's strict priority tiers (imminent -> pinned -> other -> prompt, list-row-selection.ts:77-102) with birthdays pre-excluded; compactness is a WITHIN-TIER tiebreak only (it reorders candidates inside a tier, favouring concise first-class/custom values, relationship/person names, counts like children/pets, short pinned Memories) — it is NOT an additive score across tiers, so a compact 'other' candidate can NEVER outrank an imminent meaningful date (addresses review: within-tier tiebreak, not additive score)."
    - "Birthdays are EXCLUDED from Card adaptive ranking (birthday info is surfaced elsewhere) (SS I)."
    - "When no useful adaptive context exists, row 3 shows a completeness prompt from a NEW short Grid-biased prompt list defined in card-line3-selection.ts (e.g. 'Add a detail', 'Remember something', 'Add some context', 'Add a detail about {name}') — the List PROMPTS (list-row-selection.ts:5-16) are full sentences unsuitable for the compact grid and are NOT reused; the cue is unobtrusive, never a warning/score/gamified metric; the choice is deterministic/stable per contact (indexed by a stable function of contactId) and does not change on arbitrary re-render (SS J; addresses review: separate short Card prompt list)."
    - "The adaptive row is NOT separately tappable; a normal card tap still opens the Profile (SS I)."
    - "CARDV-02 ordering: selection is deterministic per contact (stable across re-renders for the same inputs) — equal-ranked candidates break ties by a stable key, never by render order."
    - "CARDV-02 empty: a contact with zero candidates yields a completeness prompt (never a blank row); a single candidate is selected directly."
    - "CARDV-03 search: during search the card keeps its three-row geometry — row 1 name, row 2 matched field/type label, row 3 strongest highlighted snippet (label-weight run on textPrimary, no highlight background) — reusing ListRow's HighlightedSnippet / formatMatchExplanation / formatMatchCategories / DashboardSearchResult; Card adds no separate interaction for additional matches; +N more only if it fits."
    - "CARDV-03 search empty: an empty/whitespace term shows normal (non-search) card content; zero matches route to the shared grid empty state (Plan 01)."
    - { statement: "CARDV-02 encoding: adaptive/snippet text with multi-byte/grapheme content renders single-line ellipsized (grapheme-aware), not byte-truncated — verified on the Pixel.", verification: backstop }
    - "E3 search populated/overflow/long-text: matched label + strongest snippet are single-line ellipsize within the compact card; the highlight run renders at label weight (600) on textPrimary with no background colour (SS K)."
  artifacts:
    - src/logic/card-line3-selection.ts
    - src/logic/card-line3-selection.test.ts
    - "GridCard row-3 adaptive/prompt/search rendering wired"
    - "HomeScreen card branch reads readLine3Candidates + card selection + passes search props"
  key_links:
    - "readLine3Candidates (dashboard-knowledge-read) -> selectCardLine3 (new, compactness-biased) -> GridCard line3"
    - "dashboard-search-match descriptors + ListRow HighlightedSnippet -> GridCard search rows"
  prohibitions:
    - "The Card must not fork the line-3 candidate read (readLine3Candidates) or the search-match descriptor logic — it reuses the shared reads and only applies a compactness/presentation bias (SS I / dossier SS A)."
---

<objective>
Fill the card's information hierarchy: row 3 adaptive context with a Card-specific compactness bias, and the search-mode three-row rendering. Both reuse the shared reads (readLine3Candidates, dashboard-search-match) and the List view's presentation helpers — the Card differs in layout and ranking bias, not in semantics.

Purpose: complete CARDV-02 (adaptive item) and CARDV-03 (search geometry) on the tracer's card without forking shared query/knowledge logic.
Output: src/logic/card-line3-selection.ts (+ test), plus GridCard + HomeScreen card-branch wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
@.planning/phases/28-dashboard-card-view/28-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- `selectCardLine3` (src/logic/card-line3-selection.ts) — pure compactness-biased adaptive-context selector
- GridCard row-3 rendering (adaptive / completeness prompt / search snippet)
- HomeScreen card-branch line3 + search wiring
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: card-line3-selection — pure compactness-biased adaptive-context selector</name>
  <files>src/logic/card-line3-selection.ts</files>
  <read_first>
    - src/logic/list-row-selection.ts (selectLine3 at 77-102 — the ANALOG: STRICT priority tiers imminent(88-89) -> pinned(91-92) -> other(94-95) -> prompt(97-101), NOT a scored function; Line3Selection type at 21-28; the type-only DB import at line 2 `import type { Line3Candidate }`; compareCandidates stable tiebreak 52-58; firstStable 60-62; PROMPTS 5-16 are FULL SENTENCES — too long for the grid; IMMINENT_DAYS/isBirthday/isImminent 19-50)
    - src/db/dashboard-knowledge-read.ts (readLine3Candidates at 161; Line3CandidateKind = "memory" | "relationship" | "current-state" at 20; the candidate row shape at ~24)
    - .planning/phases/28-dashboard-card-view/28-RESEARCH.md (Adaptive context — reuse the read, add a card variant) and 28-UI-SPEC.md (SS I adaptive rules, SS J completeness prompts, deterministic-per-contact)
  </read_first>
  <behavior>
    - selectCardLine3 keeps the analog's strict tier order (imminent -> pinned -> other -> prompt); compactness ONLY changes WHICH candidate wins WITHIN a tier, never which tier wins. So given an imminent meaningful date and a compact 'other' fact, the imminent date is still selected.
    - Within a tier, a naturally compact/recognizable candidate (concise value, person/relationship name, small count, short pinned Memory) is preferred over a longer one; a trivial short fact does NOT jump tiers to beat a more useful (higher-tier) candidate.
    - Birthdays are excluded (defensive filter, mirroring the analog).
    - Given zero eligible candidates, returns a completeness-prompt marker from a NEW short Grid-biased prompt list (deterministic per contact, keyed on contactId so it does not change on re-render).
    - Ties within a tier break by a stable key (compareCandidates-style: createdAt/id/kind/type), never by input array order.
  </behavior>
  <action>
    Create src/logic/card-line3-selection.ts exporting a pure, react-native-free selectCardLine3(candidates, ctx) mirroring selectLine3's SHAPE and its STRICT-TIER structure (same candidate input from readLine3Candidates; returns a Line3-style result carrying text + optional leading iconName, or a completeness-prompt variant). Structure: filter to useful non-birthday candidates, then evaluate the SAME tiers in the SAME order as selectLine3 — imminent, then pinned, then other — and within EACH tier pick the winner using a compactness-biased comparator (prefer shorter concise values / person-relationship names / small counts / short pinned Memories) with compareCandidates as the deterministic final tiebreak. Do NOT collapse the tiers into a single additive score and do NOT let a compact lower-tier candidate outrank a higher-tier one (that is the review's core correction). Define a NEW module-level SHORT prompt list (e.g. "Add a detail", "Remember something", "Add some context", "Add a detail about {name}") — do NOT reuse list-row-selection's long PROMPTS; select the empty-case prompt deterministically by indexing this list with a stable function of contactId (mirror selectLine3's `contactId % PROMPTS.length` idiom) so it never flickers. Import the candidate type with `import type` only (matching list-row-selection.ts:2); do NOT import any runtime src/db value. Do NOT read the DB here and do NOT fork readLine3Candidates.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/logic/card-line3-selection.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: card-line3-selection.ts imports no react-native and no RUNTIME src/db value; the candidate type is a `import type`-only reference (permitted, matching list-row-selection.ts:2) — the "no src/db module" bar means no runtime import, not no type import.
    - Source assertion: the selector evaluates imminent/pinned/other as ordered tiers (not one additive score) and defines its own short prompt list distinct from list-row-selection's PROMPTS.
    - Test: an imminent meaningful date is selected even when a shorter 'other' candidate exists (tiers not crossed).
    - Test: within the 'other' tier, a concise candidate is preferred over a longer one of the same tier; birthdays are never selected.
    - Test: the same inputs always yield the same pick (determinism); the empty case yields a stable per-contact SHORT prompt.
  </acceptance_criteria>
  <done>selectCardLine3 exists, pure, tier-preserving with within-tier compactness, its own short prompt list, deterministic, birthday-excluding; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: card-line3-selection.test.ts — ranking bias, birthday exclusion, determinism, empty prompt</name>
  <files>src/logic/card-line3-selection.test.ts</files>
  <read_first>
    - src/logic/list-row-selection.test.ts (if present — the pure-logic Vitest pattern for line-3 selection)
    - src/logic/card-line3-selection.ts (under test)
  </read_first>
  <action>
    Write src/logic/card-line3-selection.test.ts covering: TIER PRESERVATION (an imminent meaningful date is selected even when a shorter 'other'-tier candidate is present — compactness never crosses tiers); within-tier compactness (a short concise value ranks above a long one of the SAME tier); the trivial-fact guard (a much more useful higher-tier candidate wins over a trivial short lower-tier one); birthday exclusion; determinism (repeated calls -> identical pick; ties broken by the stable key, not array order); empty-candidates -> a stable per-contact completeness prompt drawn from the NEW short Card prompt list (two calls same prompt; different contacts may differ; assert the chosen prompt is from the short list, not list-row-selection's long PROMPTS). Pure node Vitest, no DB.
  </action>
  <verify>
    <automated>npm test -- src/logic/card-line3-selection.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All cases above pass; tier-preservation, within-tier compactness, determinism, and birthday-exclusion explicitly asserted.
    - The empty-case test asserts the prompt comes from the short Card list (distinct from list-row-selection PROMPTS).
    - Full `npm test` stays green.
  </acceptance_criteria>
  <done>Selector behavior is test-locked (tiers preserved, within-tier compactness, short prompts); suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire row-3 adaptive/prompt + search rendering into GridCard and the HomeScreen card branch</name>
  <files>src/components/GridCard.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/components/GridCard.tsx (the optional line3/searchResult/searchSnippet props declared in Plan 01; the three-row text block)
    - src/components/ListRow.tsx (search-row presentation + HighlightedSnippet inner component 29-58,122-134,178-233; the highlight run styling 332-337 — label weight on textPrimary, no background)
    - src/components/list-row-content.ts (formatMatchExplanation, formatMatchCategories)
    - src/screens/HomeScreen.tsx (the list branch's line3ByContactId + selectLine3 usage 79,93,600-630; CRUCIALLY the candidate-read GATE at 601 `if (query.viewMode === "list" && !isListSearch)` — card mode currently gets NO candidates; searchResultsByContactId + isListSearchMode + item.snippet wiring 1080-1088)
    - src/db/dashboard-knowledge-read.ts (readLine3Candidates 161)
  </read_first>
  <action>
    In GridCard: render row 3 from the line3 prop — when a search result is active (searchResult/searchSnippet provided) render the SEARCH rows (row 2 = matched field/type label via formatMatchExplanation/formatMatchCategories, row 3 = strongest highlighted snippet using ListRow's HighlightedSnippet pattern verbatim: label-weight run on textPrimary, no highlight background), otherwise render the adaptive line3 (text + optional leading Icon) or the completeness prompt. All three rows stay numberOfLines=1 ellipsize tail. The adaptive/search row is NOT wrapped in its own Pressable (not separately tappable). In HomeScreen's card branch: WIDEN the candidate-read gate at HomeScreen.tsx:601 — today it reads candidates only when `query.viewMode === "list" && !isListSearch`, so card mode has NO line-3 data; change the condition so candidates are read in BOTH normal list AND normal card mode (i.e. `viewMode is "list" OR "card"`, still `&& !isListSearch`) — without this widening selectCardLine3 has nothing to select from (addresses review: view-mode candidate gate). Then, over that SAME single readLine3Candidates read (do NOT add a second read), build the card line3 map by calling selectCardLine3 (Plan 04) when viewMode is "card" and selectLine3 when "list". Pass line3, and in search mode pass searchResult (from searchResultsByContactId) + searchSnippet (item.snippet), mirroring the list branch's isListSearchMode wiring, into each GridCard via CardGrid. Extend CardGrid's props to thread line3 + search props through to GridCard (a signature addition, no re-mount logic change).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test 2>&1 | tail -5</automated>
    <human-check>On the Pixel: normal cards show a compact row-3 context item (or a completeness prompt for sparse contacts, never a warning); during search each card keeps its geometry showing name, matched-field label, and one highlighted snippet.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: the HomeScreen.tsx:601 candidate-read gate is widened to include card mode (grep shows the condition now admits `viewMode === "card"`, still excluding search) so card mode has candidate data; the read is still performed ONCE (no second readLine3Candidates call added).
    - Source assertion: HomeScreen's card branch calls selectCardLine3 over that existing read; grep shows selectCardLine3 imported.
    - Source assertion: GridCard's row-3/search rendering reuses ListRow's HighlightedSnippet pattern (label-weight run, no background colour) and does not wrap the adaptive row in its own Pressable.
    - `npx tsc --noEmit`, `npm run check:colors`, and full `npm test` clean.
  </acceptance_criteria>
  <done>Card row 3 shows adaptive context / prompt in normal mode and matched-label + snippet in search mode, geometry preserved, candidate gate widened to card mode; tsc + check:colors + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared knowledge read -> Card selector | candidate rows cross into a Card-specific ranking; the Card must not re-query or fork the read |
| search descriptor -> Card render | matched offsets/snippets from the shared match model render as highlighted text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-10 | Information disclosure | adaptive/snippet content | low | mitigate | Card reuses the shared eligible corpus + descriptors; it never surfaces internal metadata or archived/unbound content (owned by Phase 25 reads) |
| T-28-11 | Tampering | non-deterministic prompt flicker | low | mitigate | Completeness prompt + tie-breaks are deterministic per contact (stable key), test-proven |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors` clean.
- `npm test` green (new card-line3-selection.test.ts + no regression).
- Device UAT: adaptive row / completeness prompt / search rows render single-line with preserved geometry.
</verification>

<success_criteria>
- Card shows one compactness-biased adaptive item, birthdays excluded, deterministic per contact, with a completeness prompt fallback (CARDV-02).
- Search preserves the three-row geometry (name / matched label / highlighted snippet) reusing shared helpers (CARDV-03).
- No fork of the candidate read or search-match logic.
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-04-SUMMARY.md` when done.
</output>
