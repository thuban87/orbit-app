---
phase: 28-dashboard-card-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/bulk-actions-dao.ts
  - src/db/bulk-actions-dao.test.ts
  - src/db/contacts-dao.ts
  - src/db/favourites-dao.ts
  - src/db/snooze-dao.ts
  - src/db/recency-dao.ts
  - src/db/events-dao.ts
autonomous: true
requirements: [CARDV-07, CARDV-08, CARDV-10, CARDV-11]
estimate:
  tokens: 75000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each bulk composer performs N single-contact operations inside ONE inWriteTransaction, reusing extracted non-mutexed *Core primitives — never a set-based SQL UPDATE/INSERT, never a top-level mutexed writer in the loop (the mutex is non-reentrant; nesting hangs). Every extracted *Core OMITS bumpDataRevisionCore (it assumes an open transaction); the bulk composer calls bumpDataRevisionCore exactly once at the end of its transaction (D-04). Copying the setContactPhotoCore shape means copying ONLY its ?-bound single-column UPDATE + changes===1 guard — NOT its trailing bumpDataRevisionCore call (that lives in setContactPhoto's public wrapper, contacts-dao.ts:658, not in a bulk core) (addresses review: downgraded HIGH-3 no-bump clarity)."
    - "bulkQuickLog composes insertInteractionCore + recomputeLastContactCore per contact in one transaction and writes exactly one interaction per selected contact whose column shape EXACTLY matches the canonical single-contact Quick Log (quick-log-command.ts:87-97 / QuickLogInput): channel='unspecified', direction='outbound', connected=1, quality=null, source='manual' — direction is pinned to 'outbound' explicitly because insertInteractionCore defaults direction to null, not 'outbound' (recency-dao.ts:205); it recomputes last_contact via the sole recency writer and calls rejectFutureOccurredAt for the current-time value before/at the top of the transaction (D-05, ADR-010/024/071, CARDV-08; addresses review: bulkQuickLog interaction shape)."
    - "bulkQuickLog RETURNS a batch receipt — an array of { contactId, interactionId } for every inserted interaction (interactionId = insertInteractionCore's lastInsertRowId) — so a bulk Undo can reverse exactly those rows; a single-interaction receipt is insufficient for N writes (addresses review HIGH-2: bulk Quick Log Undo contract)."
    - "undoBulkQuickLog(exec, receipt, now) reverses a bulk Quick Log atomically: in ONE inWriteTransaction it composes an extracted non-mutexed deleteInteractionCore per receipt entry (uid lookup + insertTombstoneCore + DELETE FROM interactions scoped by id AND contact_id + recomputeLastContactCore for each affected contact), then bumpDataRevisionCore once — behavior-identical to the single-contact Quick Log undo (deleteTouchpoint) but over N rows; a bad/already-deleted receipt entry rolls the whole undo back (CARDV-08, addresses review HIGH-2)."
    - "bulkArchive composes archiveContactCore (extracted from archiveContact) per contact — a ?-bound UPDATE contacts SET archived_at guarded by archived_at IS NULL + changes===1, THEN recordEventCore type 'archive' — so every archive writes its immutable lifecycle event; a bare set-wise archive is never used (D-06, ADR-025, CARDV-10)."
    - "bulkAddFavourites / bulkRemoveFavourites compose setFavouriteRankCore / clearFavouriteRankCore (extracted from the mutexed twins) — explicit add/remove, never a toggle-all; membership stays binary; no rank-rewrite path is introduced (D-09, ADR-075, CARDV-07)."
    - "bulkSnooze / bulkUnsnooze compose extracted snooze cores that set snooze_until (SQLite date('now','localtime',modifier)) / NULL and compose recordEventCore type 'snooze'/'unsnooze' inside the SAME outer transaction (CARDV-07)."
    - "bulkSetCategory composes a NEW single-column setContactCategoryCore (?-bound UPDATE contacts SET category_id, modified_at WHERE id, changes===1) mirroring setContactPhotoCore — NOT updateContactMetadataCore (which clobbers unrelated columns) (CARDV-07)."
    - "bulkSetFrequency composes a NEW single-column setContactFrequencyCore that rejects a non-positive-integer interval (!Number.isInteger(x) || x <= 0) BEFORE the write, so the value passes the v11 CHECK(interval_days > 0) and never sets NULL (never trips the contacts_prevent_cadence_clear trigger) (CARDV-11)."
    - "CARDV-07 concurrency/error: any mid-batch failure rolls back the whole transaction atomically (no partial writes) — each *Core throws loud on changes!==1 via assertOneChange."
    - "CARDV-07 empty: a bulk composer called with an empty id list performs a no-op transaction (no writes, no crash)."
    - "CARDV-07 idempotency: re-running bulkAddFavourites over already-favourite contacts keeps membership binary (rank may move but membership is unchanged); the star reads membership, not rank."
    - "CARDV-07 encoding: Set Category binds category_id as a ? parameter; no category label text is ever interpolated into SQL (only static column names are literal)."
    - "CARDV-10 adjacency: archiving a contact already archived matches 0 rows (archived_at IS NULL guard) and throws -> rollback; it can never silently no-op."
    - "CARDV-11 boundary: interval_days = 1 is accepted; 0 and negatives and non-integers are rejected before any write; NULL is never written by the frequency path."
  artifacts:
    - src/db/bulk-actions-dao.ts
    - src/db/bulk-actions-dao.test.ts
    - "archiveContactCore (exported from contacts-dao.ts)"
    - "setContactCategoryCore, setContactFrequencyCore (new single-column cores in contacts-dao.ts)"
    - "setFavouriteRankCore, clearFavouriteRankCore (exported from favourites-dao.ts)"
    - "snoozeContactCore, clearSnoozeCore (exported from snooze-dao.ts)"
    - "deleteInteractionCore (extracted + exported from recency-dao.ts — the non-mutexed inner body of deleteTouchpoint)"
    - "bulkQuickLog batch receipt type ({ contactId, interactionId }[]) + undoBulkQuickLog composer (bulk-actions-dao.ts)"
    - "events-dao.ts stale-comment correction (snooze/unsnooze DO have a producer: snooze-dao.ts)"
  key_links:
    - "bulk-actions-dao composers -> extracted *Core primitives -> inWriteTransaction (one per bulk op) -> bumpDataRevisionCore once"
    - "bulkQuickLog -> insertInteractionCore + recomputeLastContactCore (recency single-writer spine) -> returns { contactId, interactionId }[] receipt"
    - "undoBulkQuickLog(receipt) -> deleteInteractionCore per entry + recomputeLastContactCore per contact -> bumpDataRevisionCore once"
    - "bulkArchive -> archiveContactCore -> recordEventCore (immutable lifecycle events)"
  prohibitions:
    - "Bulk Quick Log must never write interactions set-wise / must never bypass the single recency writer (recomputeLastContactCore) — it composes the recency cores per contact in one transaction (D-05)."
    - "Bulk archive must never set archived_at without composing the immutable lifecycle event per contact in the same transaction (D-06)."
    - "No bulk mutation may loop a mutexed top-level writer inside inWriteTransaction — the write mutex is non-reentrant and nesting is a permanent hang."
    - "This phase builds NO contact quarantine state and NO launch-time auto-purge sweep; it ships no migration and adds no quarantine/purge-deadline column (D-03/D-08)."
    - "Category/frequency must never route through updateContactMetadataCore (a full-metadata rewrite that clobbers unrelated columns) — a dedicated single-column core is used."
---

<objective>
Build the data layer for Card View bulk management: extract the missing non-mutexed *Core primitives and compose them into atomic bulk composers, one inWriteTransaction per bulk operation. This is the phase's highest-risk work — in this repo the expensive bugs live in the data layer; the risk is entirely about WHERE the transaction boundary sits and WHICH writers get composed.

Per RESEARCH's on-disk-verified Data-Layer Composition Map: only recency (insertInteractionCore/recomputeLastContactCore) and events (recordEventCore) expose non-mutexed cores today. Archive, favourites, and snooze expose only mutexed top-level writers; category/frequency have no single-column setter at all. Because the write mutex is non-reentrant, a bulk op cannot loop the mutexed writers inside one transaction — so we extract cores (the established bulk-review-dao core/wrapper idiom) and compose them.

Purpose: correct-by-construction bulk writes that honour the single recency writer (ADR-010/024/071) and the immutable lifecycle-event trail (ADR-025).
Output: src/db/bulk-actions-dao.ts (+ test) and the extracted cores in contacts-dao / favourites-dao / snooze-dao.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-dashboard-card-view/28-CONTEXT.md
@.planning/phases/28-dashboard-card-view/28-RESEARCH.md
@.planning/phases/28-dashboard-card-view/28-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `bulk-actions-dao.ts` composers: bulkQuickLog (returns { contactId, interactionId }[] receipt), undoBulkQuickLog, bulkArchive, bulkAddFavourites, bulkRemoveFavourites, bulkSnooze, bulkUnsnooze, bulkSetCategory, bulkSetFrequency
- `QuickLogBatchReceipt` type ({ contactId: number; interactionId: number }[]) (bulk-actions-dao.ts)
- Extracted cores: archiveContactCore, setContactCategoryCore, setContactFrequencyCore (contacts-dao.ts); setFavouriteRankCore, clearFavouriteRankCore (favourites-dao.ts); snoozeContactCore, clearSnoozeCore (snooze-dao.ts); deleteInteractionCore (recency-dao.ts)
</artifacts_this_phase_produces>

<assumption_delta_decision>
Trigger: multi-select introduces N-contact (plural) operations where only single-contact operations existed.
Primary noun: the single-contact operation *Core (insertInteractionCore, archiveContactCore, setFavouriteRankCore, ...).
Decision: **no-change** (promote NOT applicable). Each bulk operation is deliberately a *composition of N single-contact cores* inside one transaction (D-04) — the single-contact core stays the primary identity and the single-writer/immutable-event invariants remain owned there. No set-based generalization is introduced that would demote the single-contact writer. This is the correct model per ADR-010/024/071/025; a set-based bulk writer would be the accepted-debt add-alongside we are explicitly avoiding. Advisory gate; non-blocking.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract non-mutexed cores (archive, favourite, snooze, delete-interaction) + new single-column category/frequency cores + events-dao comment fix</name>
  <files>src/db/contacts-dao.ts, src/db/favourites-dao.ts, src/db/snooze-dao.ts, src/db/recency-dao.ts, src/db/events-dao.ts</files>
  <read_first>
    - src/db/contacts-dao.ts (archiveContact 536-563 — the mutexed body to lift into a core; setContactPhotoCore 637-658 — the single-column core PATTERN to copy for category/frequency; NOTE setContactPhoto's PUBLIC wrapper calls bumpDataRevisionCore at :658 — a bulk core must NOT copy that trailing bump; updateContactMetadataCore 312-339 — the full-metadata writer to AVOID for single columns)
    - src/db/favourites-dao.ts (setFavouriteRank 32-52, clearFavouriteRank 59-76 — mutexed twins; the file header 1-15 cites the setContactPhoto extraction pattern)
    - src/db/snooze-dao.ts (snoozeContact 79-113, clearSnooze 122-146 — mutexed twins that compose recordEventCore internally at 103-110 / 136-143 — snooze-dao IS the producer of snooze/unsnooze events; PRESET_MODIFIERS 46-50)
    - src/db/recency-dao.ts (deleteTouchpoint 313-346 — the mutexed body to lift into deleteInteractionCore: uid lookup 321-329, insertTombstoneCore 330-334, DELETE scoped by id AND contact_id 335-343, recomputeLastContact 344; the aliased *Core exports 425-428)
    - src/db/events-dao.ts (recordEventCore 56-82; EventType includes 'snooze'/'unsnooze' at line 39; the STALE comment at 14-19 and 36-38 claiming snooze/unsnooze have "no producing feature until a later phase" — snooze-dao.ts:103-110,136-143 already produce them, so that comment is factually wrong and must be corrected)
    - src/db/bulk-review-dao.ts (assertOneChange 34-44; the core/wrapper split idiom 72-137)
    - src/db/migrations/011-contact-lifecycle-schema.ts (interval_days CHECKs 36-37; contacts_prevent_cadence_clear trigger 181-185)
    - src/db/transaction.ts (inWriteTransaction 49-64), src/db/mutex.ts (non-reentrant chain 32-36)
  </read_first>
  <behavior>
    - archiveContactCore(exec,id,now): inside an open txn, UPDATE archived_at with the archived_at IS NULL guard; assertOneChange; then recordEventCore type 'archive'. Re-archiving an already-archived id -> changes 0 -> throws.
    - setFavouriteRankCore(exec,id,now) / clearFavouriteRankCore(exec,id,now): the inner bodies of the mutexed twins, minus inWriteTransaction and minus bumpDataRevisionCore.
    - snoozeContactCore(exec,{contactId,uid,preset,now}) / clearSnoozeCore(exec,{contactId,uid,now}): the inner bodies of the mutexed twins (set snooze_until / NULL + compose recordEventCore), minus inWriteTransaction and bump.
    - deleteInteractionCore(exec,{contactId,interactionId,now}): the inner body of deleteTouchpoint (uid lookup scoped by id AND contact_id; throw if not found; insertTombstoneCore for the interaction uid; DELETE FROM interactions WHERE id=? AND contact_id=? with changes===1 guard; recomputeLastContactCore for that contact), minus inWriteTransaction. It does NOT call bumpDataRevisionCore (deleteTouchpoint doesn't today either — keep the wrapper behavior-identical).
    - setContactCategoryCore(exec,id,categoryId,now): ?-bound single-column UPDATE category_id + modified_at; changes===1 or throw.
    - setContactFrequencyCore(exec,id,intervalDays,now): reject !Number.isInteger(intervalDays) || intervalDays <= 0 BEFORE the UPDATE; then ?-bound single-column UPDATE interval_days + modified_at; changes===1 or throw. Never writes NULL.
  </behavior>
  <action>
    Following the bulk-review-dao core/wrapper idiom: for archive, favourite, snooze, AND delete-interaction, extract the inner transaction body of each existing mutexed writer into an exported non-mutexed `*Core(exec, ...)` that ASSUMES an open transaction and does NOT call bumpDataRevisionCore, and rewrite each existing public writer to delegate to its core inside its own inWriteTransaction (+ trailing bumpDataRevisionCore where the current wrapper has one — archive/favourite/snooze do; deleteTouchpoint does NOT, so its wrapper stays bump-free) so all current single-contact callers stay behavior-identical. Name them archiveContactCore, setFavouriteRankCore, clearFavouriteRankCore, snoozeContactCore, clearSnoozeCore, and deleteInteractionCore (the last in recency-dao.ts, alongside the existing aliased insertInteractionCore/recomputeLastContactCore exports at 425-428). For category and frequency, ADD two new single-column cores in contacts-dao.ts — setContactCategoryCore and setContactFrequencyCore — copying ONLY the setContactPhotoCore statement shape (a ?-bound single-column `UPDATE contacts SET <col> = ?, modified_at = ? WHERE id = ?` with a changes===1 loud-failure guard) and NOT the trailing bumpDataRevisionCore that lives in setContactPhoto's public wrapper (contacts-dao.ts:658) — a bulk core never bumps (the bulk composer bumps once); do NOT route them through updateContactMetadataCore. setContactFrequencyCore carries the positive-integer guard (!Number.isInteger(x) || x <= 0 rejects) to respect the v11 CHECK(interval_days > 0) and the contacts_prevent_cadence_clear trigger (it never sets NULL). Reuse assertOneChange (copy or import the helper). Do NOT introduce any favourite-rank-rewrite path. Keep every value ?-bound; only column names are static text. FINALLY, correct the stale events-dao.ts comment (14-19, 36-38): snooze/unsnooze are NO LONGER "reserved with no producer" — snooze-dao.ts's snoozeContact/clearSnooze already compose recordEventCore for them; rewrite those comment lines to say all four EventType values (archive|restore|snooze|unsnooze) have producers, so a future agent does not "reserve" an already-live event type. This is a comment-only edit — no code/vocabulary change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/db/contacts-dao.test.ts src/db/favourites-dao.test.ts src/db/snooze-dao.test.ts src/db/recency-dao.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: contacts-dao.ts exports archiveContactCore, setContactCategoryCore, setContactFrequencyCore; favourites-dao.ts exports setFavouriteRankCore, clearFavouriteRankCore; snooze-dao.ts exports snoozeContactCore, clearSnoozeCore; recency-dao.ts exports deleteInteractionCore.
    - Source assertion: each `*Core` contains no `inWriteTransaction(` call and no `bumpDataRevisionCore(` call (they assume an open transaction); the archive/favourite/snooze public wrappers still call both; the deleteTouchpoint wrapper stays bump-free (behavior-identical).
    - Source assertion: setContactCategoryCore / setContactFrequencyCore do NOT call updateContactMetadataCore; each is a single-column ?-bound UPDATE with a changes===1 guard and no trailing bump.
    - Source assertion: setContactFrequencyCore rejects a non-positive-integer before the UPDATE and never binds NULL to interval_days.
    - Source assertion: the events-dao.ts comment no longer claims snooze/unsnooze lack a producer (grep the file: the phrase "no producing feature" / "no producer yet" is gone or corrected).
    - Existing single-contact suites (contacts-dao / favourites-dao / snooze-dao / recency-dao) stay green (wrappers behavior-identical).
  </acceptance_criteria>
  <done>The six extracted cores (archive, favourite x2, snooze x2, delete-interaction) + two new single-column cores exist; the events-dao comment is corrected; existing single-contact tests green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bulk-actions-dao composers — N cores in one transaction per bulk op</name>
  <files>src/db/bulk-actions-dao.ts</files>
  <read_first>
    - src/db/recency-dao.ts (insertInteractionCore/recomputeLastContactCore aliased exports 425-428; deleteInteractionCore added in Task 1; recordTouchpoint 216-243 for the compose shape + insertInteraction defaults 178-214 — direction defaults to null at 205; rejectFutureOccurredAt usage 227-231)
    - src/services/quick-log-command.ts (QuickLogInput 26-36 + runQuickLog 81-126 — the CANONICAL single-contact Quick Log column shape bulkQuickLog MUST match: channel:"unspecified", direction:"outbound", connected:1, quality:null, source:"manual")
    - src/db/log-guards.ts (rejectFutureOccurredAt 68-82 — throws synchronously; call it before/at the top of the txn)
    - src/db/bulk-review-dao.ts (assertOneChange 34-44; core/wrapper idiom 72-137)
    - src/db/data-revision-dao.ts (bumpDataRevisionCore 5-20 — call ONCE at the end of each bulk txn)
    - src/db/transaction.ts (inWriteTransaction 49-64), src/db/mutex.ts (non-reentrant 32-36)
    - src/db/uid.ts or the existing newUid() source (see how recordTouchpoint/archiveContact mint uids)
    - .planning/phases/28-dashboard-card-view/28-RESEARCH.md (Data-Layer Composition Map SS1-SS6 + Code Examples — the verbatim compose patterns)
    - the cores added in Task 1
  </read_first>
  <behavior>
    - bulkQuickLog(exec, ids, now): rejectFutureOccurredAt(now, now); one inWriteTransaction; per id insertInteractionCore(exec,id,now,{uid:newUid(),occurredAt:now,channel:"unspecified",direction:"outbound",connected:1,quality:null,source:"manual"}) capturing the returned lastInsertRowId, then recomputeLastContactCore(exec,id,now); bumpDataRevisionCore once. Writes exactly ids.length interactions whose columns match the canonical Quick Log (direction PINNED to "outbound", NOT the null default); each contact's last_contact = MAX(occurred_at). RETURNS a receipt: { contactId, interactionId }[] (one per inserted interaction).
    - undoBulkQuickLog(exec, receipt, now): one inWriteTransaction; per receipt entry deleteInteractionCore(exec,{contactId,interactionId,now}) (uid lookup + tombstone + DELETE + recompute for that contact); bumpDataRevisionCore once. Reverses exactly the receipt's rows atomically; a receipt entry that matches 0 rows throws -> whole undo rolls back. Behavior-identical to N single-contact deleteTouchpoint undos.
    - bulkArchive(exec, ids, now): one txn; per id archiveContactCore; bump once. Writes N archive events + N archived_at updates atomically; a bad/already-archived id throws -> whole batch rolls back.
    - bulkAddFavourites / bulkRemoveFavourites(exec, ids, now): one txn; per id setFavouriteRankCore / clearFavouriteRankCore; bump once. Explicit add/remove (never toggle-all).
    - bulkSnooze(exec, ids, preset, now) / bulkUnsnooze(exec, ids, now): one txn; per id snoozeContactCore/clearSnoozeCore (each mints its own event uid); bump once.
    - bulkSetCategory(exec, ids, categoryId, now): one txn; per id setContactCategoryCore; bump once.
    - bulkSetFrequency(exec, ids, intervalDays, now): validate positive-integer ONCE up front; one txn; per id setContactFrequencyCore; bump once.
    - Empty ids / empty receipt -> a no-op transaction (no writes); bulkQuickLog returns an empty receipt.
  </behavior>
  <action>
    Create src/db/bulk-actions-dao.ts exporting the nine composers above (the eight bulk ops + undoBulkQuickLog) and the QuickLogBatchReceipt type ({ contactId: number; interactionId: number }[]). Every composer opens exactly ONE inWriteTransaction, loops the extracted non-mutexed *Core over the id/receipt list, and calls bumpDataRevisionCore(exec) once at the end — never inside the loop, never a nested inWriteTransaction (the mutex is non-reentrant; nesting hangs permanently). bulkQuickLog pins the interaction column shape to the canonical Quick Log (channel:"unspecified", direction:"outbound", connected:1, quality:null, source:"manual") — do NOT rely on insertInteractionCore's defaults, which set direction to null (recency-dao.ts:205); it collects each insertInteractionCore lastInsertRowId into the returned { contactId, interactionId }[] receipt so undoBulkQuickLog can reverse exactly those rows. Up-front validation runs BEFORE the transaction: bulkQuickLog calls rejectFutureOccurredAt(now, now); bulkSetFrequency rejects a non-positive-integer interval. Use newUid() for each interaction/event uid. Guard against an empty id list / empty receipt by returning a no-op (bulkQuickLog returns []); do not open a transaction that produces partial writes. NEVER write interactions set-wise; NEVER set archived_at without the composed event; NEVER read/rewrite favourite_rank for ordering. All values ?-bound (inherited from the cores).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/db/bulk-actions-dao.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: bulk-actions-dao.ts contains exactly one `inWriteTransaction(` per composer and no nested inWriteTransaction; each composer calls bumpDataRevisionCore once.
    - Source assertion: bulkQuickLog passes direction:"outbound" (and channel:"unspecified", connected:1, quality:null, source:"manual") explicitly to insertInteractionCore, and returns a { contactId, interactionId }[] receipt; undoBulkQuickLog composes deleteInteractionCore per receipt entry.
    - Test (node:sqlite): bulkQuickLog over N ids inserts N interactions whose columns match the canonical Quick Log (direction="outbound" etc.) and sets each contact's last_contact to the logged time; a future `now` is rejected before any write; the returned receipt has one { contactId, interactionId } per contact.
    - Test: undoBulkQuickLog(receipt) deletes exactly the receipt's interactions, writes their tombstones, recomputes each affected contact's last_contact (reverts to prior MAX or NULL), and rolls the whole undo back if a receipt entry matches 0 rows.
    - Test: bulkArchive over N ids writes N 'archive' events + sets archived_at on all N; injecting one already-archived id rolls back the whole batch (0 events, 0 updates persisted).
    - Test: bulkSetFrequency rejects intervalDays of 0 / -1 / 1.5 with no write; accepts 1 and passes the CHECK.
    - Test: an empty id list produces no writes; bulkQuickLog([]) returns [].
  </acceptance_criteria>
  <done>All eight bulk composers + undoBulkQuickLog exist, atomic (one txn each), bulkQuickLog matches the canonical Quick Log shape and returns a batch receipt; node:sqlite tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bulk-actions-dao.test.ts — node:sqlite coverage of atomicity, recency spine, event trail, guards</name>
  <files>src/db/bulk-actions-dao.test.ts</files>
  <read_first>
    - src/db/recency-dao.test.ts, src/db/events-dao.test.ts, src/db/favourites-dao.test.ts, src/db/snooze-dao.test.ts, src/db/011-contact-lifecycle-schema.test.ts (the node:sqlite harness setup + how each single-contact invariant is asserted — reuse the fixtures/adapter)
    - src/db/bulk-actions-dao.ts (the composers under test)
  </read_first>
  <behavior>
    - Covers: bulkQuickLog (N interactions + canonical column shape + recompute + future-reject + rarely_responds recency + returned receipt), undoBulkQuickLog (deletes exactly the receipt + recompute + rollback), bulkArchive (N events + archived_at + rollback on bad id), bulkAddFavourites/bulkRemoveFavourites (membership binary + idempotent re-add), bulkSnooze/bulkUnsnooze (snooze_until set/clear + event rows), bulkSetCategory (category_id set, other columns untouched), bulkSetFrequency (positive-int guard, CHECK passes, no NULL), empty-list no-op.
  </behavior>
  <action>
    Write src/db/bulk-actions-dao.test.ts using the existing node:sqlite Vitest harness (mirror recency-dao.test.ts setup). Assert per behavior above. Critically: (1) after bulkQuickLog assert each contact's last_contact equals the logged time (not stale) — the single-recency-writer spine — AND assert the written interaction row columns match the canonical Quick Log (channel="unspecified", direction="outbound", connected=1, quality=null, source="manual"), explicitly asserting direction="outbound" (guards against the null default at recency-dao.ts:205); (2) a rarely_responds=1 contact case: bulkQuickLog writes a connected=1 interaction and last_contact DOES advance (the recompute filter `rarely_responds = 0 OR i.connected = 1` is satisfied) — proving the pinned connected=1 matters; (3) the returned receipt has one { contactId, interactionId } per contact, and undoBulkQuickLog(receipt) deletes exactly those interactions, reverts each contact's last_contact (to prior MAX or NULL), writes tombstones, and a receipt entry matching 0 rows rolls the whole undo back; (4) after bulkArchive assert the events table has one 'archive' row per contact AND archived_at set, then a separate case injects an already-archived id and asserts the ENTIRE batch rolled back (no events, no archived_at changes persisted) — atomicity; (5) bulkSetCategory asserts unrelated metadata columns (name, interval_days, social_battery) are unchanged (no clobber); (6) bulkSetFrequency asserts 0/-1/1.5 reject with no write and 1 is accepted; (7) empty-list cases write nothing and bulkQuickLog([]) returns [].
  </action>
  <verify>
    <automated>npm test -- src/db/bulk-actions-dao.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/db/bulk-actions-dao.test.ts` passes with cases for all eight composers + undoBulkQuickLog.
    - The bulkQuickLog case asserts the canonical column shape incl. direction="outbound" and the rarely_responds=1 recency-advance case.
    - The undoBulkQuickLog case proves exact-row reversal + recency revert + rollback atomicity.
    - The bulkArchive rollback case proves atomicity (no partial state after a mid-batch throw).
    - The bulkSetCategory case proves no column-clobber (single-column write).
    - Full suite `npm test` stays green.
  </acceptance_criteria>
  <done>bulk-actions-dao.test.ts green; atomicity + recency spine + canonical shape + receipt/undo + event trail + guards all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI selection -> bulk DAO | a set of contact ids + a value cross into N composed writes; correctness lives here |
| bulk write -> shared tables (contacts, interactions, events) | the single-writer recency invariant and immutable-event trail must not be bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Tampering | partial-write on bulk failure | high | mitigate | One inWriteTransaction per bulk op -> atomic rollback; each core throws loud on changes!==1 (test-proven) |
| T-28-05 | Repudiation | recency / audit divergence | high | mitigate | bulkQuickLog composes the sole recency writer; bulkArchive composes the immutable event per contact — never bypassed |
| T-28-06 | Tampering | SQL injection via bulk args | low | mitigate | All values ?-bound in every core; only static column names are literal (verified across the source DAOs) |
| T-28-07 | Denial of service | non-reentrant mutex deadlock | high | mitigate | Compose non-mutexed *Cores; enter the mutex exactly once per bulk op; test that a multi-id op resolves (does not hang) |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external package installed (RESEARCH: no new deps) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` (full node suite) green — including the new bulk-actions-dao.test.ts and the unchanged single-contact suites.
- Manual grep: exactly one inWriteTransaction per composer; no nested inWriteTransaction; bumpDataRevisionCore once per composer.
</verification>

<success_criteria>
- All eight bulk composers exist and are atomic (CARDV-07).
- Bulk Quick Log composes the recency spine; bulk Archive composes the event trail (CARDV-08/10).
- Bulk frequency respects the v11 CHECK/trigger via a positive-integer guard (CARDV-11).
- No set-based mutation, no mutex nesting, no rank leak, no schema change (D-03/04/05/06/09).
</success_criteria>

<output>
Create `.planning/phases/28-dashboard-card-view/28-02-SUMMARY.md` when done.
</output>
