---
phase: 15-weekly-digest
plan: 06
type: execute
wave: 4
depends_on: ["15-03", "15-04", "15-05"]
files_modified: []
autonomous: false
requirements: [DGST-01, DGST-02, DGST-03]
must_haves:
  truths:
    - "The digest:weekly trigger fires on the intended weekday/hour on the physical Pixel and re-registers across a reboot (pre-57 repeat-bug de-risk)."
    - "The Sunday notification tap opens the live 'your week' screen; Back returns to the dashboard."
    - "The screen renders the retrospective, overlooked groups (incl. a muted rogue), the gentle line (or its conservative absence), and the calm empty state."
    - "The Settings 'Weekly digest' toggle arms/cancels the WEEKLY trigger; the choice survives a relaunch."
  artifacts: []
  key_links:
    - "Build path: desktop-build-pipeline runbook (ssh droid -> release APK -> rsync/scp to Pixel), NOT git push."
---

<objective>
Owner-gated on-device verification: the one thing that cannot be proven node-side — that the
WEEKLY trigger fires correctly and survives reboot on the physical Pixel — plus the screen's
render/interaction UAT.

## Phase Goal (user story)
**As a** person tending my relationships, **I want to** actually receive the Sunday
notification and land on a correct "your week" screen on my phone, **so that** the feature
works in the real app, not just in tests.

Purpose: DGST-01's weekly-fire + reboot persistence is device-only (RESEARCH Pitfall 2 /
Assumptions A6/A7 — the emulator cannot validate timing per CLAUDE.md, and pre-57 repeat
bugs #34782/#30577 exist). This plan is a dedicated checkpoint (no code) per the
"checkpoints get their own plan" rule.

Node gates (full `npm test`, `tsc --noEmit`, `check:colors`, biome) are re-run by the
orchestrator before this checkpoint; this plan verifies only what a device can.

## Rig (project memory + CLAUDE.md)
- Build the RELEASE APK on `droid` (desktop) via the desktop-build-pipeline runbook; transport
  by rsync/scp (NEVER git push). Release is the clean verification path (debug+Metro has an
  intermittent expo-sqlite flake).
- `emu-connect device` (physical Pixel; auto-falls back only if absent). Resolve the serial
  from `adb devices` — never hardcode. Metro on orbit's port if a debug channel is needed.
- Drive with `uiautomator dump` for exact bounds + the locked testIDs (adb tap can
  false-negative small RN Pressables — verify against code first).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-weekly-digest/15-CONTEXT.md
@.planning/phases/15-weekly-digest/15-UI-SPEC.md
@.planning/phases/15-weekly-digest/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device weekly-digest UAT (owner-gated)</name>
  <files>(none — verification only; no code changes)</files>
  <action>
    Build the RELEASE APK on `droid` (desktop-build-pipeline runbook) and install to the
    Pixel over rsync/scp (never git push). Connect with `emu-connect device` and drive the
    verification steps in how-to-verify using uiautomator + the locked digest testIDs. This
    is a verification-only checkpoint — no source changes. On any failure, hand back to a
    fix (schedule/reconcile fix, or a one-number tune of DIGEST_WEEKDAY/deliveryHour) before
    sign-off.
  </action>
  <what-built>
    The full weekly-digest feature: a WEEKLY (Sunday) `digest:weekly` notification riding the
    11-notify engine (its own reconcile + launch-sweep hook + digest-v1 PRIVATE channel), a
    live "your week" DigestScreen (retrospective + overlooked + gentle line + calm empty
    state), the dashboard "Your week" entry, notification-tap routing into the screen, and a
    master-gated Settings "Weekly digest" toggle (defaults ON).
  </what-built>
  <how-to-verify>
    Prereq: build the RELEASE APK on `droid` (desktop-build-pipeline runbook) and install to
    the Pixel over rsync/scp. `emu-connect device`; confirm exactly one device via `adb devices`.

    1. WEEKLY FIRE (timing de-risk): to observe without waiting a week, temporarily set
       DIGEST_WEEKDAY / deliveryHour (or the device clock) so the next WEEKLY slot is minutes
       away; relaunch so the launch sweep arms it; confirm the notification "Your week in
       Orbit" / "A look back at who you reached." arrives ONCE (not twice — pre-57 repeat bug),
       on the intended day, at the intended hour. Confirm via the shade + logcat, not a
       stale Metro pane.
    2. REBOOT PERSISTENCE: with the trigger armed, reboot the Pixel; after boot (app NOT
       opened), confirm the pending `digest:weekly` still exists / still fires — i.e. it
       survives reboot with no custom boot receiver. Reopen; confirm the launch sweep does
       NOT create a duplicate (getAllScheduledNotificationsAsync shows exactly one).
    3. TAP -> SCREEN: tap the digest notification -> lands on the "your week" screen
       (testID digest-root); Back returns to the dashboard.
    4. IN-APP ENTRY: from the dashboard, tap "Your week" (digest-your... entry) -> same screen.
    5. CONTENT: with seeded data, confirm the retrospective lists people reached this week
       (all touchpoints, day tags), the overlooked shows Drifting/Gone quiet with a MUTED
       rogue contact still present, the backlog nudge counts + taps to NeverContacted, and
       (if the conservative threshold is met) the gentle line names people; every row/name
       taps to Profile.
    6. EMPTY STATE: on an all-quiet week, the screen opens to "All quiet this week" (no crash,
       no nag) — the notification still fired unconditionally.
    7. TOGGLE: Settings -> Notifications -> "Weekly digest" OFF cancels the trigger; ON
       (master on) re-arms it; the choice survives a relaunch (digest_enabled persisted).
    8. Restore DIGEST_WEEKDAY / deliveryHour / device clock to their real values afterward.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the observed weekday/hour + reboot result), or describe issues to fix.</resume-signal>
  <verify>
    <human-check>Owner confirms weekly fire (once, correct day/hour), reboot persistence, tap-to-screen, content, empty state, and toggle behaviour on the physical Pixel.</human-check>
  </verify>
  <done>Owner types "approved" after the on-device checks pass (or the noted issues are fixed and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Lock screen (public surface) | Confirm on-device that the digest body names no one on the lock screen. |
| OS scheduled set / reboot | The AlarmManager-backed trigger must survive reboot without a custom receiver. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Information disclosure | on-device lock-screen copy | low | mitigate | Device check: the frozen generic body shows no contact name on the lock screen (digest-v1 PRIVATE). |
| T-15-08 | Denial of service | duplicate / mis-timed weekly fire | low | mitigate | Device check: exactly-once fire on the intended day + no duplicate after reboot/relaunch (pre-57 repeat-bug de-risk). |
| T-15-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- Owner confirms: weekly fire once/day-correct, reboot-persistent, no duplicate; tap + in-app
  entry open the screen; content + empty + toggle behave; lock-screen copy names no one.
- Any failure routes to a fix (e.g. GEMINI-style one-number tune of DIGEST_WEEKDAY/deliveryHour,
  or a schedule/reconcile fix) before sign-off.
</verification>

<success_criteria>
DGST-01/02/03 are owner-accepted on the physical Pixel: the Sunday notification fires
correctly and survives reboot, opens the live screen, and the digest is fully toggleable.
</success_criteria>

<output>
Create `.planning/phases/15-weekly-digest/15-06-SUMMARY.md` when done.
</output>
