---
phase: 15-weekly-digest
plan: 05
type: execute
wave: 3
depends_on: ["15-02", "15-03", "15-04"]
files_modified:
  - src/services/notifications/notification-nav.ts
  - src/services/notifications/notification-nav.test.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [DGST-01]
must_haves:
  truths:
    - "resolveNotificationNav({kind:'digest'}) returns a RESET intent onto [Home, Digest] (index 1) — mirroring the decay reset — so Back ALWAYS lands on the dashboard regardless of the warm stack the OS handed us (review H3 / T-11-BACKSTACK), NOT a bare navigate that only lands Back-on-Home from a cold start; a digest payload carries NO contactId, so there is nothing to forge into a profile navigation."
    - "A malformed / unknown payload still returns null (unchanged); decay/birthday routing is byte-unchanged."
    - "The Settings Notifications section has a 4th 'Weekly digest' toggle row (after Birthday alerts, before lock-screen), gated by the master switch, defaulting ON."
    - "EVERY settings write that affects the digest — master ON/OFF, delivery-hour, AND the digest toggle — synchronously runs reconcileDigestSchedule via the SHARED post-write path, so the WEEKLY trigger arms/cancels/re-times immediately (not just on the digest Switch, and not only at next launch)."
  artifacts:
    - src/services/notifications/notification-nav.test.ts
  key_links:
    - "notification-gate's EXISTING reset branch (applyBodyNav notification-gate.tsx:91-93) applies the digest RESET intent unchanged — the gate already handles BOTH reset and navigate intents (review H3), so NO gate edit is needed; the Digest route exists from 15-04."
    - "The SHARED `persist` callback (SettingsScreen.tsx:350-360) runs BOTH reconcileSchedule (decay/birthday) AND reconcileDigestSchedule (from 15-03) after every app_settings write — so master ON/OFF (:373/:380), the delivery-hour picker (:397/:403), and the digest toggle all reconcile the digest (review H1 / Pitfall 7). No one-row persistDigest callback."
    - "digestEnabled is read/written through the DAO from 15-02; the digest Switch calls persist({ digestEnabled }) through that shared path (digestEnabled is in WritableSettingsKey from 15-02)."
---

<objective>
Close the loop: route a tapped Sunday notification into the digest screen, and add the
Settings toggle that arms/cancels the weekly trigger.

## Phase Goal (user story)
**As a** person tending my relationships, **I want to** tap the Sunday notification to land
on my "your week" screen, and turn the whole digest off if I want, **so that** the feature
is fully in my control and the notification actually goes somewhere.

This is the final wiring wave: it depends on the schedule service (15-03: reconcileDigestSchedule)
and the Digest route (15-04). After it, the notification-tap -> screen path and the Settings
toggle both work end-to-end.

Purpose: DGST-01 (independently toggleable; the tap opens the live screen). Output: the
notification-nav digest RESET branch (+ test, mirroring decay so Back -> dashboard on warm
stacks, review H3) and the Settings row. The gate is untouched — its existing reset branch
applies the digest reset.

## Non-negotiables
- Warm-tap Back->dashboard (review H3): the digest intent MUST be a RESET onto [Home, Digest]
  (index 1), NOT a bare nav.navigate("Digest"). A navigate only lands Back-on-Home from a COLD
  start; on a WARM tap over Home->Profile/Settings it pushes Digest on top and Back returns to
  Profile/Settings — violating the dossier's "Back -> dashboard" contract. Mirror the decay reset
  (notification-nav.ts:63-72, in-code comment "a reset (not a navigate) so Back ALWAYS lands on
  the dashboard regardless of the stack" / T-11-BACKSTACK). The gate's existing reset branch
  applies it with NO gate change. Keep the decay and birthday intents byte-unchanged.
- Untrusted OS input (V5): extend the resolver's narrowing to accept kind:"digest" WITHOUT
  trusting a contactId — a digest intent carries nothing to forge. Malformed -> null.
- The digest reconcile MUST live in the SHARED settings post-write path, not a one-row callback
  (review H1 / RESEARCH Pitfall 7). The shipped `persist` (:350-360) is the sole handler behind
  master ON/OFF (onToggleMaster :373/:380), the delivery-hour picker (onPickTime :397/:403), AND
  the digest toggle. Persisting the flag alone never arms/cancels the WEEKLY trigger; wiring the
  reconcile only to the digest Switch leaves master-OFF with digest:weekly still armed (a
  notification-control violation) and a delivery-hour change re-timing decay/birthday but not the
  digest until next launch. Fold reconcileDigestSchedule into `persist` so all three paths reconcile.
- Do NOT widen the shipped NotificationData interface or touch notification-actions.ts /
  notification-schedule.ts: the digest routing is handled in the resolver (which takes
  `unknown`) so the decay/birthday action + idempotency machinery is left byte-untouched
  (minimal surgical change, CLAUDE.md).

## Artifacts this phase produces (source-grounding exclusion list)
See 15-01 objective. This plan adds the `resolveNotificationNav` digest branch + the Digest
RESET NavIntent variant ([Home, Digest]) and the Settings "Weekly digest" toggle row. The
notification-gate is NOT modified — its existing reset branch applies the digest reset.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-weekly-digest/15-UI-SPEC.md
@.planning/phases/15-weekly-digest/15-PATTERNS.md

# Analogs to READ ON DISK before writing:
@src/services/notifications/notification-nav.ts
@src/services/notifications/notification-nav.test.ts
@src/navigation/notification-gate.tsx
@src/screens/SettingsScreen.tsx
@src/db/app-settings-dao.ts
@src/services/notifications/digest-schedule.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Digest tap routing — RESET onto [Home, Digest] (resolver only) — RED first</name>
  <read_first>
    - src/services/notifications/notification-nav.ts (NavIntent :27-36 — the decay RESET tuple + the birthday navigate; isNotificationData narrowing :43-52; resolveNotificationNav :58-80, esp. the decay reset :63-72 and its "a reset (not a navigate) so Back ALWAYS lands on the dashboard regardless of the stack" comment / T-11-BACKSTACK)
    - src/services/notifications/notification-nav.test.ts (the existing decay-reset / birthday-navigate / malformed cases)
    - src/navigation/notification-gate.tsx (applyBodyNav :82-96 — CONFIRM the existing reset branch :91-93 `nav.reset({ index: intent.index, routes: intent.routes })` already applies ANY reset intent; the digest reset needs NO gate edit)
  </read_first>
  <files>src/services/notifications/notification-nav.ts, src/services/notifications/notification-nav.test.ts</files>
  <behavior>
    - resolveNotificationNav({ kind: "digest" }) returns EXACTLY { type: "reset", index: 1, routes: [{ name: "Home" }, { name: "Digest" }] } — a reset (mirroring decay), NOT a navigate.
    - resolveNotificationNav for decay/birthday is byte-unchanged; a malformed/unknown payload (null, non-object, unknown kind) still returns null.
    - The digest payload needs NO contactId — the narrowing accepts kind:"digest" without a numeric contactId (V5 boundary; nothing to forge).
    - The gate's existing applyBodyNav reset branch applies the digest reset with no code change, so a WARM tap over Home->Profile/Settings still lands Back on the dashboard.
  </behavior>
  <action>
    Extend notification-nav.test.ts FIRST with the digest cases (must fail). Then edit
    notification-nav.ts:
    - Add a NavIntent RESET variant `{ type: "reset"; index: 1; routes: [{ name: "Home" }, { name: "Digest" }] }`
      to the union, alongside the existing decay reset variant. Add a DISTINCT Digest reset tuple —
      do NOT reuse or loosen the decay variant's `Compose{contactId}` tuple.
    - In resolveNotificationNav, add a digest pre-check BEFORE the existing isNotificationData
      path: if data is a non-null object whose `kind` === "digest", return the digest RESET intent
      `{ type: "reset", index: 1, routes: [{ name: "Home" }, { name: "Digest" }] }`. This mirrors
      the decay reset so Back ALWAYS returns to the dashboard regardless of the warm stack the OS
      handed us (review H3 / T-11-BACKSTACK). Leave the existing decay/birthday narrowing
      (isNotificationData) and its contactId requirement exactly as-is (do NOT loosen the shipped
      NotificationData type). Malformed still falls through to null.

    Do NOT edit notification-gate.tsx: applyBodyNav's existing reset branch (:91-93,
    `nav.reset({ index: intent.index, routes: intent.routes })`) already applies ANY reset intent,
    so the digest reset routes through unchanged (review H3 — the gate already handles both reset
    and navigate intents). Confirm this by reading the branch; do NOT add a param-less navigate
    special-case.
  </action>
  <verify>
    <automated>npx vitest run src/services/notifications/notification-nav.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: resolveNotificationNav({kind:"digest"}) deep-equals EXACTLY { type: "reset", index: 1, routes: [{ name: "Home" }, { name: "Digest" }] } (assert the FULL reset shape, review H3 — a bare navigate must NOT satisfy this).
    - Test: resolveNotificationNav({kind:"digest", contactId:99}) returns the SAME [Home, Digest] reset and does NOT forward the contactId anywhere (nothing to forge).
    - Test: existing decay-reset / birthday-navigate / malformed cases stay byte-green (decay reset unchanged; null for unknown kinds).
    - `git diff --stat src/services/notifications/notification-schedule.ts src/services/notifications/notification-actions.ts src/navigation/notification-gate.tsx` shows NO changes (shipped payload machinery + the gate untouched).
  </acceptance_criteria>
  <done>A digest notification tap resolves to a [Home, Digest] RESET; the gate's existing reset branch applies it so Back returns to the dashboard on both cold AND warm stacks; decay/birthday routing is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Settings "Weekly digest" toggle row</name>
  <read_first>
    - src/screens/SettingsScreen.tsx (the "Birthday alerts" row :702-738, the SHARED persist callback :350-360, onToggleMaster :367-384 and onPickTime :389-406 — both route through persist, confirming why folding the digest reconcile into persist covers master + delivery-hour + toggle, the gated-label idiom :674-678, masterOn :362)
    - .planning/phases/15-weekly-digest/15-UI-SPEC.md (Settings — Weekly digest toggle section)
    - src/services/notifications/digest-schedule.ts (reconcileDigestSchedule signature)
  </read_first>
  <files>src/screens/SettingsScreen.tsx</files>
  <action>
    In SettingsScreen.tsx insert a 4th notification-type row AFTER "Birthday alerts" (:738)
    and BEFORE "Show names on lock screen" (:741), copying the birthday row shell exactly:
    styles.row (surface fill, border outline, radius 10), the gated label (masterOn ?
    textPrimary : textSecondary) reading "Weekly digest", a Switch with
    testID="settings-notifications-digest", accessibilityLabel="Weekly digest",
    accessibilityState={{ disabled: !masterOn, checked: settings?.digestEnabled === 1 }},
    disabled={!masterOn}, value={settings?.digestEnabled === 1}, trackColor {{ false:
    colors.border, true: colors.accent }}, thumbColor colors.surfaceElevated, and the helper
    "A Sunday-morning look back at your week — who you reached, and who's slipping quietly."

    Persist + reconcile — FOLD INTO THE SHARED PATH (review H1 / Pitfall 7), do NOT add a
    one-row persistDigest: the existing `persist` (:350-360) currently fires only
    reconcileSchedule (decay/birthday) and is the SOLE handler behind master ON/OFF, the
    delivery-hour picker, and (about to be) the digest toggle. Add ONE line to `persist` so it
    runs BOTH reconciles after every write: keep `void reconcileSchedule(exec);` and add
    `void reconcileDigestSchedule(exec);` right after it (inside the same try, after
    setSettings(next)). reconcileDigestSchedule reads settings fresh, is idempotent, and is
    defer-one guarded (15-03), so running it on every settings write is harmless — a
    quiet-window-only change reconciles to "matching -> leave" (no-op), while master ON/OFF and a
    delivery-hour change now arm/cancel/re-time the digest immediately. Import
    reconcileDigestSchedule from the digest-schedule module.

    Wire the digest Switch onValueChange={(v) => void persist({ digestEnabled: v ? 1 : 0 })}
    (digestEnabled is in WritableSettingsKey / AppSettingsPatch from 15-02, so this typechecks
    through updateAppSettings). Default ON is inherited from the column default; the row reflects
    settings.digestEnabled. Do NOT create a separate persistDigest callback — the whole point of
    H1 is that a single shared post-write path reconciles the digest for master, delivery-hour,
    and toggle alike, so no future settings write can silently skip it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/screens/SettingsScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reconcileDigestSchedule" src/screens/SettingsScreen.tsx` shows the call INSIDE the shared `persist` callback (:350-360), NOT a separate persistDigest — `grep -c "persistDigest" src/screens/SettingsScreen.tsx` is 0.
    - `grep -n "settings-notifications-digest" src/screens/SettingsScreen.tsx` shows the digest row, whose onValueChange calls `persist({ digestEnabled: ... })` (the same shared path master ON/OFF and the delivery-hour picker use).
    - The row sits between the birthday row and the lock-screen row and is gated by masterOn.
    - The per-transition OS schedule state (master OFF cancels, master ON arms, delivery-hour re-times, digest OFF/ON) is proven at the reconcile layer in digest-schedule.test.ts (15-03) — SettingsScreen.tsx is device-UAT (project pattern); 15-06 re-verifies all three transition paths on the Pixel.
    - check:colors + biome clean.
  </acceptance_criteria>
  <done>The Settings Notifications section has a master-gated "Weekly digest" toggle that persists and arms/cancels the WEEKLY trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OS notification tap -> app | The tapped payload's `data` is untrusted; the resolver re-validates it. |
| Settings write -> DAO + schedule | The toggle writes a 0/1 flag and triggers a reconcile of the OS schedule. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Spoofing / Tampering | resolveNotificationNav digest branch | low | mitigate | The resolver treats OS `data` as untrusted and re-narrows; a digest intent carries no contactId, so there is nothing to forge into a profile navigation. Cold-start replay is already cleared by the gate (clearLastNotificationResponseAsync). |
| T-15-05 | Tampering / DoS | digest toggle write | low | mitigate | The shared `persist` path writes digestEnabled through updateAppSettings, where assertToggle (15-02) rejects anything but 0/1. |
| T-15-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `npx vitest run src/services/notifications/notification-nav.test.ts` passes (digest routing + unchanged cases).
- `npx tsc --noEmit && npm run check:colors && npx biome check src/screens/SettingsScreen.tsx` clean.
- notification-schedule.ts / notification-actions.ts / notification-gate.tsx show no diff (shipped machinery + the gate untouched — the digest reset rides the existing reset branch).
</verification>

<success_criteria>
Tapping the Sunday notification opens the digest screen via a [Home, Digest] RESET, so Back
returns to the dashboard on both cold AND warm stacks (review H3); and the shared Settings
post-write path reconciles the digest for every relevant write — the master toggle,
the delivery-hour picker, and the master-gated "Weekly digest" toggle all arm/cancel/re-time
the WEEKLY trigger immediately (review H1) — DGST-01's "independently toggleable" and "opens a
live screen" fully wired, with no path that leaves a disabled digest armed.
</success_criteria>

<output>
Create `.planning/phases/15-weekly-digest/15-05-SUMMARY.md` when done.
</output>
