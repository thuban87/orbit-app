---
phase: 06-interaction-log-status-impact
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/db/status.ts
  - src/db/status.test.ts
  - src/db/contact-status-read.ts
  - src/db/contact-status-read.test.ts
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [LOG-05, LOG-04]

must_haves:
  truths:
    - "A contact reaches rogue at the shared multiple-of-interval constant (ROGUE_K) OR via the Rarely-responds path"
    - "Rogue carries a reason: 'overdue' (beyond-decay time) or 'unresponsive' (Rarely-responds path)"
    - "The rogue state + reason renders in-app on the profile only, never as a notification"
    - "A Rarely-responds contact shows its label; a rogue contact shows why"
    - "A never-contacted contact (last_contact NULL) shows no status/rogue label"
  artifacts:
    - path: "src/db/status.ts"
      provides: "REASON_SQL — rogue reason CASE mirroring STATUS_SQL branch order"
      exports: ["REASON_SQL"]
    - path: "src/db/contact-status-read.ts"
      provides: "getContactStatus(exec, contactId) — single-contact query-time status + reason (includes rogue)"
      exports: ["getContactStatus"]
    - path: "src/screens/ContactProfileScreen.tsx"
      provides: "Rogue label + reason render (in-app only), colour via tokens"
      contains: "getContactStatus"
  key_links:
    - from: "src/db/contact-status-read.ts"
      to: "src/db/status.ts"
      via: "SELECT composes STATUS_SQL + REASON_SQL for one contact"
      pattern: "REASON_SQL"
    - from: "src/screens/ContactProfileScreen.tsx"
      to: "src/db/contact-status-read.ts"
      via: "getContactStatus loaded in the focus effect"
      pattern: "getContactStatus"
---

<objective>
Give the rogue state a reason and surface it (in-app only) on the profile. Extend the query-time
status engine with `REASON_SQL` (a CASE mirroring STATUS_SQL's branch order so status and reason
never disagree), add a single-contact status read that returns status+reason (a query-time status
type that INCLUDES rogue — not the legacy OrbitStatus), and render the rogue label + reason on the
profile. The "Rarely responds" label already renders in the scaffold — reuse it.

Purpose: LOG-05 (rogue at the shared constant or via Rarely-responds; reason recorded; in-app only)
and LOG-04's rendering half (the label + the fact that rarely_responds drives recency).

Output: `REASON_SQL` + status.test extension; `contact-status-read.ts` + test; the profile render.

CRITICAL — do NOT redefine ROGUE_K (it lives in status.ts and is imported by orrery/notify/digest —
one value, one place). Do NOT type query-time results as `src/types.ts OrbitStatus` (it excludes
rogue) and do NOT call the legacy `calculateStatus()`. Read status from the SQL engine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interaction-log-status-impact/06-RESEARCH.md
@.planning/phases/06-interaction-log-status-impact/06-PATTERNS.md
@docs/dossier/04-log.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REASON_SQL + contact-status-read (single-contact query-time status + reason)</name>
  <files>src/db/status.ts, src/db/status.test.ts, src/db/contact-status-read.ts, src/db/contact-status-read.test.ts</files>
  <read_first>
    - src/db/status.ts (STATUS_SQL branch order :67-73; PROGRESS_SQL :59 + its NULL note :53-57; thresholds STABLE_MAX/WOBBLE_MAX/ROGUE_K :40-42; the timezone contract :44-59 — do NOT re-run last_contact through 'localtime')
    - src/db/status.test.ts :1-50 (how the SQL fragments are evaluated in a SELECT against seeded contacts; daysAgo helper)
    - src/db/queries.ts :14-29 (STATUS_SCAN — how STATUS_SQL is composed into a read; the load-bearing `last_contact IS NOT NULL` pre-filter)
    - src/db/contact-read.ts :55-94 (getContactHeader — the single-contact by-id read signature to mirror)
    - src/types.ts (Pitfall 5: OrbitStatus = stable|wobble|decay|snoozed — legacy, excludes rogue; DO NOT reuse for query-time)
  </read_first>
  <behavior>
    - REASON_SQL returns 'unresponsive' when rarely_responds=1 AND progress >= WOBBLE_MAX
    - REASON_SQL returns 'overdue' when progress >= ROGUE_K
    - REASON_SQL returns NULL otherwise (branch order identical to STATUS_SQL: rarely_responds branch first)
    - For a rogue-by-time contact: getContactStatus → { status:'rogue', reason:'overdue' }
    - For a rarely_responds contact past WOBBLE_MAX: getContactStatus → { status:'rogue', reason:'unresponsive' }
    - For a wobble/decay/stable contact: status matches STATUS_SQL, reason null
    - For last_contact NULL (never-contacted): getContactStatus → { status:null, reason:null } (never mislabelled 'stable')
  </behavior>
  <action>
    In src/db/status.ts add `export const REASON_SQL` — a CASE with the SAME branch order as STATUS_SQL: WHEN rarely_responds = 1 AND (PROGRESS_SQL) >= WOBBLE_MAX THEN 'unresponsive'; WHEN (PROGRESS_SQL) >= ROGUE_K THEN 'overdue'; ELSE NULL. Interpolate ONLY the existing code constants (WOBBLE_MAX, ROGUE_K, PROGRESS_SQL) — never user input, matching the module's injection posture. Add a doc-comment noting the branch order mirrors STATUS_SQL so status and reason never disagree, and that reason value naming (overdue/unresponsive) is the RESEARCH A4 assumption while the branch logic is CITED and firm. Do NOT touch STATUS_SQL, PROGRESS_SQL, or the thresholds.

    Create src/db/contact-status-read.ts exporting `getContactStatus(exec, contactId)` returning `{ status: ProfileStatus | null, reason: RogueReason, progress: number | null, rarely_responds: number, last_contact: string | null } | null` where `ProfileStatus = 'stable'|'wobble'|'decay'|'rogue'` and `RogueReason = 'unresponsive'|'overdue'|null` (define these query-time types here — do NOT import OrbitStatus). Implement as a single by-id SELECT that returns rarely_responds, last_contact, (PROGRESS_SQL) AS progress, (STATUS_SQL) AS status, (REASON_SQL) AS reason, `?`-bound on id. In TS, when last_contact IS NULL force status=null, reason=null, progress=null (STATUS_SQL alone would bucket NULL as 'stable' — the STATUS_SCAN NULL pre-filter isn't present on a single by-id seek, so guard it here). No transaction (pure read). Header doc-comment: derived-never-stored, read-only, `?`-bound, no network.

    Extend src/db/status.test.ts to evaluate REASON_SQL alongside STATUS_SQL for the buckets above (overdue at >= ROGUE_K; unresponsive on the rarely_responds path; NULL otherwise). Write src/db/contact-status-read.test.ts (node:sqlite) covering the getContactStatus behaviors incl. the never-contacted NULL case.
  </action>
  <verify>
    <automated>npx vitest run src/db/status.test.ts src/db/contact-status-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - REASON_SQL mirrors STATUS_SQL branch order; only code constants interpolated; STATUS_SQL/PROGRESS_SQL/thresholds unchanged; ROGUE_K not redefined
    - getContactStatus returns a query-time ProfileStatus incl. rogue + a RogueReason; never imports OrbitStatus / calculateStatus
    - last_contact NULL → status/reason/progress all null (never mislabelled)
    - status+reason agree for overdue and unresponsive rogue paths (tested)
    - `npx vitest run src/db/status.test.ts src/db/contact-status-read.test.ts` passes
  </acceptance_criteria>
  <done>Rogue has a correct, node-tested reason and a single-contact query-time status read that includes rogue.</done>
</task>

<task type="auto">
  <name>Task 2: Render rogue label + reason on the profile (in-app only)</name>
  <files>src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx (the Rarely-responds label render :141-148 to reuse; the focus-effect load; useTheme colours; where to place the status line relative to the header)
    - src/db/contact-status-read.ts (getContactStatus + ProfileStatus/RogueReason from Task 1)
    - .planning/phases/05-photos/05-UI-SPEC.md :26-60 (tokens + spacing scale)
    - src/theme/theme-types.ts :29-66 (available tokens — danger/accent/border/textSecondary/textPrimary; NO status-ring/gravity-tier tokens exist)
  </read_first>
  <action>
    Load `getContactStatus(getExecutor(), contactId)` in the profile's focus effect (add it to `load` alongside the header + timeline) and hold status/reason in state. When status === 'rogue', render a rogue label + reason line, in-app only (this is never a notification): copy such as "No longer in a working orbit · overdue" / "· unresponsive" mapping reason → human text. Keep the existing "Rarely responds" label (do not duplicate it). Render nothing status-related for a never-contacted contact (status null). Use EXISTING theme tokens only — the rogue label uses `colors.danger` (or `colors.textSecondary` for the secondary reason text); the palette has NO status-ring/gravity-tier tokens and this plan MUST NOT hardcode any colour. Give the rogue label a stable testID (e.g. `contact-profile-rogue`).

    FLAGGED DECISION (default taken — surface to owner at the pre-execution pause, do NOT block):
    a dedicated status/gravity-tier colour token set is an owner-taste addition (mirroring how
    danger/avatarSwatches were added in Phase 4/5). DEFAULT here is to render rogue with the existing
    danger/textSecondary tokens; note in the SUMMARY that a status-token set can be introduced now or
    deferred to Phase 8 (which owns the status-ring card). Do NOT add tokens without owner sign-off.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/screens/ContactProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The profile renders a rogue label + reason when status==='rogue', using existing tokens (danger/textSecondary) — no hardcoded colour (check:colors passes)
    - Never-contacted contacts show no status/rogue label; the existing Rarely-responds label is preserved (not duplicated)
    - getContactStatus is loaded in the focus effect; no notification code introduced (in-app only)
    - `npx tsc --noEmit` and `npx biome check` pass
  </acceptance_criteria>
  <done>A rogue contact shows why on its profile, in-app only, with no new colour token hardcoded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAO → UI | contactId crosses into a read-only status SELECT; status/reason render on the profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering (SQL injection) | REASON_SQL / contact-status-read | high | mitigate | Only code constants (WOBBLE_MAX, ROGUE_K, PROGRESS_SQL) interpolated into SQL fragments; contactId `?`-bound; read-only |
| T-06-09 | Tampering (status/reason disagreement) | REASON_SQL | medium | mitigate | REASON_SQL branch order is identical to STATUS_SQL (rarely_responds branch first); node-tested to agree |
| T-06-SC | Tampering (supply chain) | npm installs | n/a | accept | No packages installed this phase |
</threat_model>

<artifacts>
## Artifacts this plan produces

New files:
- `src/db/contact-status-read.ts` — `getContactStatus(exec, contactId)` + query-time types `ProfileStatus` ('stable'|'wobble'|'decay'|'rogue') and `RogueReason` ('unresponsive'|'overdue'|null)
- `src/db/contact-status-read.test.ts` — node:sqlite coverage incl. never-contacted NULL case

Modified files / new symbols:
- `src/db/status.ts` — `REASON_SQL` (rogue reason CASE, mirrors STATUS_SQL branch order); STATUS_SQL/PROGRESS_SQL/ROGUE_K unchanged
- `src/db/status.test.ts` — REASON_SQL bucket coverage (overdue / unresponsive / NULL)
- `src/screens/ContactProfileScreen.tsx` — rogue label + reason render (in-app only), loaded via getContactStatus in the focus effect; uses existing danger/textSecondary tokens

No new colour tokens (owner-taste; flagged). No new migrations. No packages installed.
</artifacts>

<verification>
- `npx vitest run src/db/status.test.ts src/db/contact-status-read.test.ts` green; `npm test` full suite green
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean
- On-device (Pixel): drive a contact past ROGUE_K (or set Rarely responds + overdue) and confirm the rogue label + correct reason renders on the profile; confirm no notification is produced by this surface
</verification>

<success_criteria>
- LOG-05: a contact reaches rogue at the shared constant or via Rarely-responds; rogue + reason surfaced in-app only, never notified
- LOG-04: the Rarely-responds label renders; rarely_responds drives the recency/reason path
</success_criteria>

<output>
Create `.planning/phases/06-interaction-log-status-impact/06-04-SUMMARY.md` when done.
</output>
