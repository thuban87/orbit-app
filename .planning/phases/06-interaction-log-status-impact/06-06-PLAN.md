---
phase: 06-interaction-log-status-impact
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - src/services/impact.ts
  - src/services/intensity-logic.ts
  - src/services/intensity-logic.test.ts
  - src/components/IntensityLine.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [LOG-03]

must_haves:
  truths:
    - "The profile shows intensity as a neutral rate vs intended frequency (no judgement)"
    - "Intensity absorbs the actual-vs-intended cadence as its long-run trailing view (e.g. 'Monthly intended · 47-day average · 2× this period')"
    - "Intensity counts outbound/mutual (you reached out), not inbound-only volume"
    - "Intensity ignores non-connected rows for a Rarely-responds contact (same filter as recency)"
    - "Intensity is derived at read time, never stored, and renders on the profile only"
  artifacts:
    - path: "src/services/intensity-logic.ts"
      provides: "Pure computeIntensity(interactions, intervalDays, rarelyResponds, now) → period rate + trailing cadence"
      exports: ["computeIntensity"]
    - path: "src/services/impact.ts"
      provides: "Intensity period tunable + computeContactIntensity orchestration"
      exports: ["computeContactIntensity"]
    - path: "src/components/IntensityLine.tsx"
      provides: "Neutral rate + trailing-average render, colour via tokens"
      exports: ["IntensityLine"]
  key_links:
    - from: "src/services/impact.ts"
      to: "src/services/intensity-logic.ts"
      via: "computeContactIntensity orchestrates computeIntensity over impact inputs"
      pattern: "computeIntensity"
    - from: "src/screens/ContactProfileScreen.tsx"
      to: "src/db/impact-read.ts"
      via: "reuse getImpactInputs (from Plan 05), fed to computeContactIntensity"
      pattern: "computeContactIntensity"
---

<objective>
Render intensity — this period's contact rate vs intended frequency — on the profile as a NEUTRAL
rate with no judgement, absorbing the actual-vs-intended cadence as its long-run trailing view.
Intensity must know that YOU reached out (count outbound/mutual, not inbound volume) and must ignore
non-connected rows for a Rarely-responds contact (the same filter recency uses, so the metric never
disagrees with the orbit).

Purpose: LOG-03's intensity half — the second derived quantity, presented alongside gravity but
never blended into one score.

Output: `intensity-logic.ts` (pure) + test, an intensity period tunable + orchestration in
`impact.ts`, `IntensityLine.tsx`, and the profile render (reusing Plan 05's `getImpactInputs`).

CRITICAL — intensity is DERIVED-NEVER-STORED (no column, no write). Frequency is a FLOOR, not a
ceiling: the rate is stated as fact ("5× this period vs Monthly intended"), never as a scold. The
period tunable sits at the top of impact.ts (single-number edit).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interaction-log-status-impact/06-RESEARCH.md
@.planning/phases/06-interaction-log-status-impact/06-PATTERNS.md
@docs/dossier/04-log.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: intensity-logic (period rate + trailing cadence) + impact.ts period tunable + tests</name>
  <files>src/services/intensity-logic.ts, src/services/intensity-logic.test.ts, src/services/impact.ts</files>
  <read_first>
    - src/services/photos/crop-geometry.test.ts (pure node-test convention)
    - src/services/impact.ts (from Plan 05 — the tunables block + orchestration to extend; add the period tunable here)
    - src/db/impact-read.ts (ImpactInputs — occurredAt/connected/direction/intervalDays/rarelyResponds — the input shape from Plan 05)
    - .planning/phases/06-interaction-log-status-impact/06-RESEARCH.md "Recommended intensity" + docs/dossier/04-log.md Cluster G (neutral rate; absorbs cadence; direction='outbound' matters; decision-without-you #8 ignores non-connected for Rarely responds)
  </read_first>
  <behavior>
    - computeIntensity counts interactions in the last PERIOD_DAYS (= interval_days, A3 assumption) where direction is outbound or mutual (you reached out) → currentCount
    - intendedPerPeriod is 1 (frequency is one contact per interval); the multiple = currentCount / 1
    - trailingAvgGapDays = mean gap (days) between consecutive counted occurred_at across the whole history (null when < 2 qualifying rows)
    - For a rarely_responds contact, only connected rows are counted (matches recency) — a non-connected outbound attempt does not inflate intensity
    - Inbound-only volume does not raise currentCount (they blew up your phone ≠ you over-contacting)
    - Zero qualifying rows → currentCount 0, trailingAvgGapDays null (never throws / never divides by zero)
  </behavior>
  <action>
    In src/services/impact.ts add a top-of-file `INTENSITY_PERIOD_DAYS` policy: default the period to the contact's interval_days (A3 — "one interval-length"; note ASSUMED/owner-deferred in a comment, since a Yearly contact has no meaningful week). Export `computeContactIntensity(inputs: ImpactInputs, now: string)` delegating to intensity-logic with the period = inputs.intervalDays.

    Create src/services/intensity-logic.ts (pure, react-native-free) exporting `computeIntensity(interactions: {occurredAt: string; connected: number; direction: string | null}[], periodDays: number, rarelyResponds: number, now: string)` returning `{ periodDays: number; currentCount: number; intendedPerPeriod: number; multiple: number; trailingAvgGapDays: number | null }`. Count only rows where direction is 'outbound' or 'mutual'; for rarelyResponds=1 additionally require connected=1 (Pitfall 7 — same filter as recency). currentCount = qualifying rows whose occurredAt is within periodDays of now (parse local `YYYY-MM-DD HH:MM:SS`; day-granular is fine; never toISOString). trailingAvgGapDays = mean day-gap between consecutive qualifying rows over all history (null if fewer than 2). Header doc-comment matching the -logic convention (neutral rate; frequency is a floor; direction/connected filters and WHY). Write src/services/intensity-logic.test.ts covering the behaviors above (outbound-counts, inbound-doesn't, rarely_responds connected filter, trailing average, empty/single-row null cases).
  </action>
  <verify>
    <automated>npx vitest run src/services/intensity-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - computeIntensity counts outbound/mutual only; inbound volume does not raise currentCount (tested)
    - rarely_responds=1 restricts to connected rows (matches recency); tested
    - trailingAvgGapDays is the mean consecutive gap, null when < 2 qualifying rows; no divide-by-zero
    - INTENSITY_PERIOD_DAYS policy lives in impact.ts; intensity-logic is react-native-free; no write statement
    - `npx vitest run src/services/intensity-logic.test.ts` passes
  </acceptance_criteria>
  <done>Intensity math is pure, node-tested, and filter-consistent with recency — no stored score.</done>
</task>

<task type="auto">
  <name>Task 2: IntensityLine component + render intensity on the profile</name>
  <files>src/components/IntensityLine.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx (from Plan 05 — the impact section + getImpactInputs load in the focus effect; useTheme colours)
    - src/components/GravityBar.tsx (from Plan 05 — the sibling impact component's token/styling conventions to match)
    - .planning/phases/05-photos/05-UI-SPEC.md :26-60 (tokens + spacing/typography scale)
    - src/services/impact.ts (computeContactIntensity from Task 1)
    - src/types.ts FREQUENCY_DAYS (to phrase "Monthly intended" from interval_days, if a mapping exists) — otherwise phrase from interval_days directly
  </read_first>
  <action>
    Create src/components/IntensityLine.tsx exporting `IntensityLine` that takes the intensity result and renders a NEUTRAL rate line — e.g. "{multiple}× this period · {intendedLabel} intended · {trailingAvgGapDays}-day average" — stated as fact with no judgemental language (no "too much"/"slow"/warning styling). Omit the trailing-average clause when trailingAvgGapDays is null. Use `colors.textPrimary` for the primary figure and `colors.textSecondary` for the caption; no danger/warning colour (intensity is neutral). No hardcoded colour. Stable testID (`contact-profile-intensity`).

    In ContactProfileScreen, reuse the `getImpactInputs` result already loaded for gravity (Plan 05) — pass it through `computeContactIntensity(inputs, localDateTime())` and render `<IntensityLine>` in the same impact section, beside gravity (presented together, never blended into one score — Cluster G). Intensity renders on the PROFILE ONLY. Graceful when there are no qualifying rows (show "No recent activity" or hide — pick and note).

    FLAGGED DECISION (default taken — surface to owner, do NOT block): the intensity PERIOD (one
    interval-length, A3) and the neutral phrasing are ASSUMED; note in the SUMMARY that the period
    choice and copy await owner sign-off. No new colour token added (neutral text tokens only).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/components/IntensityLine.tsx src/screens/ContactProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IntensityLine renders a neutral rate + intended + trailing-average (average omitted when null); no judgemental/warning styling; colour via text tokens only (check:colors passes)
    - The profile reuses the Plan-05 getImpactInputs result → computeContactIntensity(inputs, localDateTime()) and renders IntensityLine beside gravity (not blended)
    - Intensity appears on the profile only (no dashboard/card surface touched)
    - `npx tsc --noEmit` and `npx biome check` pass
  </acceptance_criteria>
  <done>The profile shows intensity as a neutral rate + long-run cadence beside gravity, derived at read time, profile-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAO → service → UI | Impact-read rows feed pure intensity math; the rate renders on the profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering (stored derived value rots) | intensity | medium | mitigate | Intensity is derived-never-stored — no column, no write statement; computed at read time from the shared impact-read rows |
| T-06-10 | Correctness (metric disagrees with orbit) | intensity-logic | medium | mitigate | Intensity ignores non-connected rows for rarely_responds (same filter as recency) and counts outbound/mutual only; node-tested |
| T-06-SC | Tampering (supply chain) | npm installs | n/a | accept | No packages installed this phase |
</threat_model>

<artifacts>
## Artifacts this plan produces

New files:
- `src/services/intensity-logic.ts` — pure `computeIntensity(...)` (period rate + trailing cadence)
- `src/services/intensity-logic.test.ts` — outbound/inbound/rarely_responds/trailing-average/empty coverage
- `src/components/IntensityLine.tsx` — `IntensityLine` (neutral rate render, text tokens only)

Modified files / new symbols:
- `src/services/impact.ts` — `INTENSITY_PERIOD_DAYS` policy (top-of-file) + `computeContactIntensity`
- `src/screens/ContactProfileScreen.tsx` — IntensityLine rendered beside GravityBar in the impact section (reuses the Plan-05 getImpactInputs load)

Reuses (no edit): `src/db/impact-read.ts` `getImpactInputs` (read-only import). No new migrations. No packages installed.
</artifacts>

<verification>
- `npx vitest run src/services/intensity-logic.test.ts` green; `npm test` full suite green
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean
- On-device (Pixel): open a contact with several outbound touchpoints; confirm the neutral intensity line renders beside gravity on the profile and NOT on the dashboard card; confirm a Rarely-responds contact's intensity ignores non-connected attempts
</verification>

<success_criteria>
- LOG-03 (intensity): the profile shows intensity as a neutral rate vs intended plus the long-run cadence, derived-never-stored, profile-only, presented beside gravity and never blended into one score
</success_criteria>

<output>
Create `.planning/phases/06-interaction-log-status-impact/06-06-SUMMARY.md` when done.
</output>
