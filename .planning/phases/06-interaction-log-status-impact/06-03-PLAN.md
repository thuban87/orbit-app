---
phase: 06-interaction-log-status-impact
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/db/recency-dao.ts
  - src/db/recency-dao.test.ts
  - src/components/touchpoint-refine-logic.ts
  - src/components/touchpoint-refine-logic.test.ts
  - src/components/TouchpointRefineForm.tsx
  - src/components/TimelineRow.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [LOG-01, LOG-02, LOG-04, LOG-06]

must_haves:
  truths:
    - "A user can refine a just-logged touchpoint: channel, direction, connected, quality, note, and date+time"
    - "Editing any of those fields updates the row and recomputes recency in one path (always recompute)"
    - "Editing a touchpoint's date can change the contact's status (recency moves)"
    - "A user can delete a touchpoint behind a confirm; the delete is unrecoverable and moves recency back if the newest row was removed"
    - "A future occurred_at is rejected on the edit path too"
    - "Marking a touchpoint not-connected on a Rarely-responds contact does not advance its orbit"
  artifacts:
    - path: "src/db/recency-dao.ts"
      provides: "editTouchpointFull — updates every editable column and always recomputes, single path"
      exports: ["editTouchpointFull"]
    - path: "src/components/touchpoint-refine-logic.ts"
      provides: "Pure two-dialog date+time carry-state chaining + combined local datetime + validity"
      exports: ["combineDateAndTime"]
    - path: "src/components/TouchpointRefineForm.tsx"
      provides: "Controlled refine form: channel/direction/connected/quality/note + two sequential pickers"
      exports: ["TouchpointRefineForm"]
  key_links:
    - from: "src/components/TouchpointRefineForm.tsx"
      to: "src/db/recency-dao.ts"
      via: "save routes through editTouchpointFull(getExecutor(), input)"
      pattern: "editTouchpointFull"
    - from: "src/screens/ContactProfileScreen.tsx"
      to: "src/db/recency-dao.ts"
      via: "delete confirm routes through deleteTouchpoint(getExecutor(), input)"
      pattern: "deleteTouchpoint"
    - from: "src/db/recency-dao.ts"
      to: "src/db/log-guards.ts"
      via: "editTouchpointFull pre-transaction rejectFutureOccurredAt"
      pattern: "rejectFutureOccurredAt"
---

<objective>
Make each timeline touchpoint editable and deletable in place — the "log fast, fix tonight"
workflow. Add ONE new edit path to the single-writer DAO (`editTouchpointFull`) that updates every
editable column and ALWAYS recomputes recency (idempotent MAX → one path, no forgotten recompute),
build the refine form with Android's unavoidable two-dialog date+time entry, and wire refine/edit
plus a confirmed, unrecoverable delete onto the timeline.

Purpose: LOG-01 (refine), LOG-02 (editable + deletable in place; edits change status; deletes
unrecoverable), LOG-04 (the connected axis is written here), LOG-06 (future reject on edit).

Output: `editTouchpointFull` + tests; `touchpoint-refine-logic.ts` (pure date+time chaining) + test;
`TouchpointRefineForm.tsx`; edit/delete affordances on `TimelineRow` + wiring on the profile.

CRITICAL — do NOT add a second edit path or a second last_contact writer. `editTouchpointFull` is a
composition inside ONE `inWriteTransaction`, calling `recomputeLastContactCore` (never nesting the
non-reentrant mutex). The Phase-2 `editTouchpoint` stays as-is; the refine UI uses the new full path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interaction-log-status-impact/06-RESEARCH.md
@.planning/phases/06-interaction-log-status-impact/06-PATTERNS.md
@docs/dossier/04-log.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: editTouchpointFull (all editable columns, always recompute, future-reject) + tests</name>
  <files>src/db/recency-dao.ts, src/db/recency-dao.test.ts</files>
  <read_first>
    - src/db/recency-dao.ts (editTouchpoint :224-257 — the WR-04 both-keys scoping + `changes===1` guard + recompute to COPY; recomputeLastContact :145-162; EditTouchpointInput :77-87; the non-mutexed core exports note :340-353)
    - src/db/transaction.ts :11-57 (inWriteTransaction non-reentrancy — compose via *Core, never nest)
    - src/db/log-guards.ts (rejectFutureOccurredAt from Plan 01)
    - src/db/recency-dao.test.ts (harness + existing edit/delete tests to mirror)
  </read_first>
  <behavior>
    - editTouchpointFull updates occurred_at, channel, direction, connected, quality, note, modified_at for the matched (id, contact_id) row
    - A note-only edit (same occurred_at) leaves last_contact unchanged (recompute is a no-op MAX)
    - Lowering the newest row's occurred_at moves last_contact back to the next-highest remaining row
    - A mismatched (id, contact_id) pair updates 0 rows → throws → rolls back (no partial write)
    - A future occurredAt rejects BEFORE the transaction opens (no row changed, last_contact unchanged)
    - On a rarely_responds contact, editing a row to connected=0 does NOT advance last_contact past a connected row (filtered-MAX)
  </behavior>
  <action>
    Add `editTouchpointFull(exec, input)` to src/db/recency-dao.ts and a `EditTouchpointFullInput` interface (interactionId, contactId, occurredAt, now, channel, direction, connected, quality, note). Call `rejectFutureOccurredAt(input.occurredAt, input.now)` in a pre-transaction reject path (mirror recordTouchpoint from Plan 01 / createContactWithInteraction's Promise.reject shape) so no transaction opens on a future date. Inside a single `inWriteTransaction`: UPDATE interactions SET occurred_at, channel, direction, connected, quality, note, modified_at WHERE id = ? AND contact_id = ? (all values `?`-bound); keep the WR-04 both-keys scoping and the `if (result.changes !== 1) throw` guard verbatim from editTouchpoint; then call `recomputeLastContactCore` unconditionally inside the SAME transaction. Do NOT nest inWriteTransaction; do NOT add any other last_contact writer. Leave the existing editTouchpoint untouched.

    Extend src/db/recency-dao.test.ts with the six behaviors above, reusing the existing harness/helpers. Include the rarely_responds connected=0 case (create the contact with rarelyResponds=1, insert a connected row + a later non-connected edit, assert last_contact stays at the connected row).
  </action>
  <verify>
    <automated>npx vitest run src/db/recency-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - editTouchpointFull updates all editable columns, scopes by both keys, asserts changes===1, and always calls recomputeLastContactCore inside one transaction
    - Future occurredAt rejects pre-transaction (no row changed); note-only edit keeps last_contact; lowering newest moves it back; rarely_responds connected=0 does not advance recency
    - No nested inWriteTransaction; no new last_contact writer (grep: the only such UPDATE is recomputeLastContact)
    - `npx vitest run src/db/recency-dao.test.ts` passes
  </acceptance_criteria>
  <done>The single full edit path is proven: every editable column updates and recency stays provably correct through the one writer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: touchpoint-refine-logic (pure date+time chaining) + TouchpointRefineForm</name>
  <files>src/components/touchpoint-refine-logic.ts, src/components/touchpoint-refine-logic.test.ts, src/components/TouchpointRefineForm.tsx</files>
  <read_first>
    - src/components/tri-state-last-spoke-logic.ts + src/components/TriStateLastSpoke.tsx :1-70 (the pure-logic + controlled-component split; native datetimepicker usage; future-date rejection copy in colors.danger; formatLocalDate never toISOString)
    - src/components/frequency-picker-logic.ts :1-20 (pure-logic file-header convention)
    - src/screens/EditContactScreen.tsx :620-640 (DateTimePicker mode="date" onChange pattern; showPicker state)
    - src/db/database.ts :40-50 (localDateTime — the `YYYY-MM-DD HH:MM:SS` contract the combined value must satisfy)
    - src/db/log-guards.ts (rejectFutureOccurredAt — reuse for inline validation)
  </read_first>
  <behavior>
    - combineDateAndTime picks the Y-M-D from the date dialog and the H:M:S from the time dialog into one `YYYY-MM-DD HH:MM:SS` local string
    - Carrying an already-chosen time through a new date pick preserves the time (and vice versa)
    - The combined string uses formatLocalDate/local components — never toISOString (no UTC shift for evening times)
    - A combined datetime later than `now` is flagged invalid (reuses rejectFutureOccurredAt semantics), and the prior selection is retained
  </behavior>
  <action>
    Create src/components/touchpoint-refine-logic.ts (pure, react-native-free) owning the Android two-dialog carry-state math: a `combineDateAndTime(datePart: Date|string, timePart: Date|string): string` producing a local `YYYY-MM-DD HH:MM:SS` (use the same local-component approach as localDateTime — never toISOString), plus a small validity helper that returns whether a combined datetime is in the future given `now` (delegate to rejectFutureOccurredAt or replicate its `>` compare). Add a header doc-comment matching the frequency-picker-logic convention (why extracted: the .tsx imports react-native + the native picker and can't load under node; Android has no combined date+time picker per dossier F7, so date and time are two sequential dialogs and the carry-state chaining is the correctness-critical pure part). Write touchpoint-refine-logic.test.ts covering the behaviors above.

    Create src/components/TouchpointRefineForm.tsx — a controlled component (parent owns state) with inputs for channel (the closed enum call|text|in-person|email|other|unspecified), direction (outbound|inbound|mutual|null), a connected toggle, quality (good|fine|hard|null), a note text input, and a "Correct date & time" affordance that opens the native date dialog then chains into the time dialog via `@react-native-community/datetimepicker` (two sequential dialogs; carry state via the logic module). Show an inline future-date rejection in `colors.danger` when the combined datetime is in the future, keeping the prior value (mirror TriStateLastSpoke). Emit an assembled input suitable for `editTouchpointFull` (the parent supplies interactionId/contactId/now and calls the DAO). Every colour via useTheme().colors.*; conform to the 05-UI-SPEC spacing/typography scale. No DAO import inside the form (keep it presentational + logic).
  </action>
  <verify>
    <automated>npx vitest run src/components/touchpoint-refine-logic.test.ts && npx tsc --noEmit && npm run check:colors && npx biome check src/components/touchpoint-refine-logic.ts src/components/TouchpointRefineForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - combineDateAndTime yields a local `YYYY-MM-DD HH:MM:SS` string, preserves carried time/date, never uses toISOString (test proves an evening time does not shift)
    - Future combined datetime is flagged invalid with the prior selection retained
    - TouchpointRefineForm is controlled, uses two sequential native dialogs, resolves all colour through tokens, and imports no DAO
    - `npx vitest run src/components/touchpoint-refine-logic.test.ts` passes; tsc/check:colors/biome clean
  </acceptance_criteria>
  <done>The refine form and its pure date+time chaining are ready; correctness-critical parts are node-tested, the native pickers are on-device UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Wire refine/edit + confirmed delete onto the timeline</name>
  <files>src/components/TimelineRow.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/components/TimelineRow.tsx (from Plan 02 — the touchpoint vs event render; add affordances only to touchpoint rows)
    - src/screens/ContactProfileScreen.tsx (from Plan 02 — the timeline list + focus-effect load; doArchive's Alert error pattern :87-95; localDateTime import)
    - src/db/recency-dao.ts (editTouchpointFull from Task 1; deleteTouchpoint :260-279 — already recomputes, both-keys, asserts one row)
    - docs/dossier/04-log.md Cluster C (no undo; the history view is the correction path; delete is unrecoverable — confirm copy must say so)
  </read_first>
  <action>
    Add edit + delete affordances to touchpoint rows only (events stay read-only) in TimelineRow.tsx — e.g. an "Add detail"/edit control and a delete control, each with a stable testID; expose them via props/callbacks so the row stays presentational (the screen owns the handlers and the DAO calls). In ContactProfileScreen, wire: (a) opening TouchpointRefineForm for a chosen touchpoint (seed it from that row's values), on save call `editTouchpointFull(getExecutor(), { interactionId, contactId, now: localDateTime(), ...refined })`, then reload the timeline + header; (b) delete via `Alert.alert` confirm whose copy states the deletion is permanent (no undo, unrecoverable — Cluster C), and on confirm call `deleteTouchpoint(getExecutor(), { interactionId, contactId, now: localDateTime() })` then reload. Handle errors with Logger.error + an Alert (mirror doArchive). Every colour via tokens. Do NOT introduce any last_contact write outside the DAO calls.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/components/TimelineRow.tsx src/screens/ContactProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Touchpoint rows expose edit + delete affordances (events do not); TimelineRow stays presentational (no DAO import)
    - Edit routes through editTouchpointFull and reloads; delete is behind an Alert confirm whose copy states it is permanent, routes through deleteTouchpoint, and reloads
    - now is localDateTime() everywhere; no toISOString; every colour via tokens (check:colors passes)
    - `npx tsc --noEmit` and `npx biome check` pass
  </acceptance_criteria>
  <done>Each touchpoint is editable and deletable in place; edits change status and deletes are unrecoverable behind a clear confirm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → DAO | Refined field values + corrected datetime cross into the write path |
| UI → DAO (destructive) | Delete removes a row permanently (no backup, no server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering (input validation, ASVS V5) | editTouchpointFull / refine-logic | high | mitigate | rejectFutureOccurredAt on the edit path (pre-transaction) + inline UI rejection; node-tested |
| T-06-03 | Tampering (recency corruption) | editTouchpointFull | high | mitigate | Both-keys scoping + changes===1 guard + always-recompute inside one transaction; composes *Core, never nests the non-reentrant mutex; no second writer |
| T-06-05 | Repudiation / data loss | delete touchpoint | medium | accept | Owner-accepted unrecoverable (Cluster C); blast radius one row; mitigated by an Alert confirm with explicit "permanent" copy and deleteTouchpoint's exactly-one-row assertion |
| T-06-02 | Tampering (SQL injection) | recency-dao UPDATE/DELETE | high | mitigate | Every value `?`-bound; no interpolation |
| T-06-SC | Tampering (supply chain) | npm installs | n/a | accept | No packages installed this phase |
</threat_model>

<artifacts>
## Artifacts this plan produces

New files:
- `src/components/touchpoint-refine-logic.ts` — `combineDateAndTime(...)` + future-validity helper (pure)
- `src/components/touchpoint-refine-logic.test.ts` — carry-state + no-UTC-shift + future-flag coverage
- `src/components/TouchpointRefineForm.tsx` — `TouchpointRefineForm` controlled refine component (two sequential native pickers)

Modified files / new symbols:
- `src/db/recency-dao.ts` — `editTouchpointFull` + `EditTouchpointFullInput` (new full edit path, always recompute, future-reject); existing editTouchpoint untouched
- `src/db/recency-dao.test.ts` — editTouchpointFull cases (all-cols, note-only, lower-newest, mismatch rollback, future-reject, rarely_responds connected=0)
- `src/components/TimelineRow.tsx` — edit + delete affordances on touchpoint rows (via callbacks; stays presentational)
- `src/screens/ContactProfileScreen.tsx` — refine-form open + save (editTouchpointFull) + confirmed delete (deleteTouchpoint) wiring; timeline reload after mutations

No new migrations. No packages installed.
</artifacts>

<verification>
- `npx vitest run src/db/recency-dao.test.ts src/components/touchpoint-refine-logic.test.ts` green; `npm test` full suite green
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean
- On-device (Pixel): open a touchpoint, correct its date via the two dialogs, change channel/direction/connected/quality/note, save, confirm the row updates and status changes if a bucket boundary was crossed; delete via the confirm and confirm the row is gone and recency moved back for a newest-row delete
</verification>

<success_criteria>
- LOG-01: refine channel/direction/connected/quality/note/date+time on a logged row
- LOG-02: touchpoints editable + deletable in place; edits change status; deletes unrecoverable
- LOG-04: the connected axis is written (feeds the rarely_responds filtered-MAX)
- LOG-06: a future occurred_at is rejected on the edit path
</success_criteria>

<output>
Create `.planning/phases/06-interaction-log-status-impact/06-03-SUMMARY.md` when done.
</output>
