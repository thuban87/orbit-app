---
phase: 12-home-screen-widget
plan: 07
type: execute
wave: 5
depends_on: [12-04, 12-06]
files_modified:
  - App.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [WDG-02, WDG-03]
must_haves:
  truths:
    - "The orbit:// deep-link gate is mounted inside the NavigationContainer, keyed on the same navReady flag as ShareIntentGate / NotificationResponseGate."
    - "The widget foreground launch recompute is registered exactly once (module-guarded), before the cold-start sweep fires."
    - "Settings shows an 'Add Orbit widget' row that calls requestPinWidget and degrades gracefully (fallback copy) on unsupported launchers / API < 26."
  artifacts:
    - "App.tsx mounts <WidgetLinkingGate isReady={navReady}/> and registers registerWidgetSweep() once."
    - "src/screens/SettingsScreen.tsx has the add-widget row (testID settings-add-widget)."
  key_links:
    - "App.tsx <WidgetLinkingGate> -> resolveWidgetUri -> navigationRef (12-04)."
    - "App.tsx registerWidgetSweep() (12-06) -> registerSweepHook(pushWidgetUpdate) before installSweepTrigger."
    - "SettingsScreen add-widget row -> requestPinWidget({ widgetName: 'OrbitFavourites' }) -> fallback copy on false/reject."
---

## Phase Goal

**As a** person keeping people in orbit, **I want to** see my favourites on a home-screen widget with their status colours and act on them in one tap, **so that** I can mark contact or open someone without launching the app.

> MVP slice: connect the widget to the running app — mount the deep-link gate so a tile tap navigates and "Back → dashboard" works, register the launch-refresh, and give the owner the in-app "Add Orbit widget" button. After this slice the widget is fully wired in JS; only the device build (12-08) remains.

<objective>
Wire the widget into the app shell: mount the `orbit://` navigation gate, register the foreground widget refresh, and add the Settings "Add Orbit widget" button.

Purpose: WDG-02's name/chevron + Message/Log deep links and "Back → dashboard" need the gate mounted on the shared navigationRef; WDG-03's launch-refresh needs the SweepHook registered before the cold-start sweep, and its "Add widget" affordance needs requestPinWidget with graceful fallback.
Output: App.tsx gate mount + one-shot widget-sweep registration; a Settings add-widget row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-screen-widget/12-RESEARCH.md   # Pattern 3 (gate wiring); requestPinWidget (Add-widget CTA / Assumption A5)
@.planning/phases/12-home-screen-widget/12-UI-SPEC.md    # "Add Orbit widget" button placement + fallback copy
@.planning/phases/12-home-screen-widget/12-PATTERNS.md   # SettingsScreen Pressable analog

# Analogs (verified on disk):
@App.tsx   # navReady flag :92,205; gate mounts :204-213; one-shot sweep-registration guards :73-81,120-139; installSweepTrigger :162
@src/navigation/linking.ts        # navigationRef + ShareIntentGate isReady idiom :34,51-61
@src/navigation/widget-linking.ts # WidgetLinkingGate (12-04)
@src/services/widget/widget-refresh.ts  # registerWidgetSweep (12-06)
@src/screens/SettingsScreen.tsx   # section/row idiom :229-291; header Pressable :212-220; useTheme colours :72
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the orbit:// gate + register the widget launch refresh (App.tsx)</name>
  <files>App.tsx</files>
  <read_first>App.tsx:69-139 (the module-scope one-shot registration flags + the ready-gated effect that registers the field/photo/notification sweeps before installSweepTrigger) and :204-213 (the NavigationContainer with ShareIntentGate + NotificationResponseGate keyed on navReady).</read_first>
  <action>Mount `<WidgetLinkingGate isReady={navReady} />` (from src/navigation/widget-linking) inside the NavigationContainer alongside <ShareIntentGate/> and <NotificationResponseGate/> (App.tsx:204-213), passing the same reactive navReady flag so a widget deep link routes the moment the navigator settles (pre-ready intents queue in the gate). Add a module-scope one-shot guard flag (mirroring fieldSweepRegistered / notificationScheduleRegistered at :73-81) and, inside the ready-gated effect (:120-139) BEFORE installSweepTrigger(AppState) fires the cold-start sweep, call registerWidgetSweep() (from src/services/widget/widget-refresh) once behind that flag — so the foreground launch recompute is registered on the sweep registry and fires once per real foreground launch (never on a headless tap, never at import). Do not otherwise change the migration/ready/error flow.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "WidgetLinkingGate" App.tsx && grep -c "registerWidgetSweep" App.tsx</automated>
  </verify>
  <done>WidgetLinkingGate is mounted keyed on navReady; registerWidgetSweep() is called once (module-guarded) before installSweepTrigger; tsc clean; the existing sweep registrations + gates untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Settings "Add Orbit widget" button (SettingsScreen.tsx)</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>SettingsScreen.tsx:205-291 (the ScrollView + section/row idiom, styles.section / styles.row / styles.sectionHeading, and the themed Pressable pattern) and :72 (useTheme colours).</read_first>
  <action>Add a settings row/button labelled "Add Orbit widget" (UI-SPEC copy contract) under a "Home screen" section heading (or the general block if adding a heading is heavy), styled with the existing styles.row / surface / border idiom and all colours via useTheme().colors.* (SettingsScreen is the in-app path with a ThemeProvider). Leading a grid/widget glyph (▦, textSecondary), a trailing chevron consistent with other rows. testID settings-add-widget. onPress calls requestPinWidget({ widgetName: "OrbitFavourites" }) (from react-native-android-widget) inside a try/catch; when it returns false OR rejects (unsupported launcher / API < 26 — Assumption A5; the provider must be prebuilt for the name to resolve, which 12-08 delivers), surface the fallback copy inline (or a toast), verbatim from the UI-SPEC: "Your launcher can't add it automatically — add Orbit from your home screen's widget menu." No crash, no dead button.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -c "settings-add-widget" src/screens/SettingsScreen.tsx</automated>
  </verify>
  <done>Settings has an "Add Orbit widget" row (testID settings-add-widget) calling requestPinWidget with a graceful fallback message; colours via tokens; tsc + check:colors clean. The live pin prompt is a device-UAT (12-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher (OS) -> app | orbit:// intents arrive at the mounted gate |
| share-intent singleton | The widget gate must not race the share provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering/Elevation | orbit:// gate on navigationRef | high | mitigate | Gate applies only resolveWidgetUri's strict output (12-04); Linking.addEventListener for orbit:// only, no react-navigation linking config, does not touch the share text/plain path (single-owner) |
| T-12-10 | DoS (dead affordance) | requestPinWidget button | low | mitigate | try/catch + false/reject -> fallback copy; never a crash or dead button |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run check:colors` clean.
- WidgetLinkingGate mounted on navReady; registerWidgetSweep called once before installSweepTrigger.
- Settings row present with graceful fallback.
- Live navigation + pin prompt are device-UATs (12-08).
</verification>

<success_criteria>
- WDG-02: widget deep links navigate (name/chevron -> Profile, Message -> Compose, Log -> Profile) with "Back → dashboard".
- WDG-03: the launch refresh is registered; the in-app "Add Orbit widget" button works with graceful fallback.
</success_criteria>

## Artifacts This Phase Produces (this plan)

New symbols introduced here — exclude from source-grounding drift checks:
- App.tsx: the `<WidgetLinkingGate>` mount + the one-shot `registerWidgetSweep()` registration guard.
- SettingsScreen.tsx: the add-widget row (testID `settings-add-widget`).

<output>
Create `.planning/phases/12-home-screen-widget/12-07-SUMMARY.md` when done.
</output>
