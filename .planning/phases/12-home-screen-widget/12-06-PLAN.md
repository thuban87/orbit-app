---
phase: 12-home-screen-widget
plan: 06
type: execute
wave: 4
depends_on: [12-05]
files_modified:
  - src/services/widget/widget-refresh.ts
  - src/services/widget/widget-task-handler.tsx
  - index.ts
autonomous: true
requirements: [WDG-01, WDG-02, WDG-03]
must_haves:
  truths:
    - "pushWidgetUpdate() re-renders every placed OrbitFavourites instance via requestWidgetUpdate and no-ops (widgetNotFound) when none is placed."
    - "pushWidgetUpdate() wraps its whole body in try/catch + Logger.error and ALWAYS resolves — a render failure can never reject the un-isolated launch-sweep hook loop (runLaunchSweep awaits hooks with no per-hook isolation, launch-sweep.ts:82) nor a fire-and-forget foreground publisher (addresses Claude M3)."
    - "A fire-and-forget notifyWidgetDataChanged() is exported for foreground call sites (wired in 12-07) so a successful user write is never rolled back by a widget-render failure."
    - "The task handler routes WIDGET_CLICK+WIDGET_MARK to the headless mark (openAndMigrate -> getExecutor -> widgetMarkContacted -> pushWidgetUpdate) and the render actions (added/update/resized) to renderFavourites(props.widgetInfo), inside a Logger-guarded try/catch, within the 30s budget."
    - "The handler imports NOTHING that reaches the foreground launch-sweep runner (a headless tap must never trigger quarantine/purge)."
    - "index.ts registers the handler with registerWidgetTaskHandler at module scope."
  artifacts:
    - "src/services/widget/widget-refresh.ts (pushWidgetUpdate [try/catch-guarded] + notifyWidgetDataChanged + registerWidgetSweep)."
    - "src/services/widget/widget-task-handler.tsx (widgetTaskHandler)."
    - "index.ts calls registerWidgetTaskHandler(widgetTaskHandler)."
  key_links:
    - "widgetTaskHandler WIDGET_CLICK -> openAndMigrate() -> getExecutor() -> widgetMarkContacted (12-05) -> pushWidgetUpdate."
    - "registerWidgetSweep -> registerSweepHook(pushWidgetUpdate) (foreground launch recompute; pushWidgetUpdate never rejects the hook loop)."
    - "notifyWidgetDataChanged -> (fire-and-forget) pushWidgetUpdate -> wired at foreground mutation call sites in 12-07."
    - "index.ts registerWidgetTaskHandler(widgetTaskHandler) — the library's own headless entry."
---

## Phase Goal

**As a** person keeping people in orbit, **I want to** see my favourites on a home-screen widget with their status colours and act on them in one tap, **so that** I can mark contact or open someone without launching the app.

> MVP slice: wire the widget's headless brain — the task handler that turns an OS click into a mark or a re-render, the event-push freshness wrapper, and the library entry registration. After this slice the widget is functionally complete in JS; the device build + boot receiver + UATs are 12-08.

<objective>
Register the widget's headless task handler and its freshness wrapper: route WIDGET_CLICK marks and render events, push event-driven updates, and register the foreground launch recompute.

Purpose: WDG-02 needs the headless mark handler; WDG-03 needs event-push + a launch recompute (no polling). The mark case mirrors the proven Phase-11 notification-actions structure (openAndMigrate before getExecutor; Logger-guarded; 30s budget). The launch recompute is a SweepHook (foreground-only); the boot/force-stop re-push is a SEPARATE native path (12-08).
Output: `widget-refresh.ts`, `widget-task-handler.tsx`, and the `registerWidgetTaskHandler` call in `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-screen-widget/12-RESEARCH.md   # Pattern 1 (index registration); Pattern 2 (WIDGET_CLICK); Pattern 5 (freshness); Pitfall 5 (sweep leak)
@.planning/phases/12-home-screen-widget/12-PATTERNS.md   # headless-bootstrap + sweep-isolation + index.ts discrepancy (bare Expo entry)

# Analogs (verified on disk):
@src/services/notifications/notification-actions.ts   # headless bootstrap-then-write structure :104-160 (openAndMigrate before getExecutor; try/catch + Logger)
@src/services/notifications/headless-task.ts           # error-guard + try/catch body shape :65-92 (mirror, but the library registers its own entry)
@src/services/launch-sweep.ts   # registerSweepHook :45; the runner :71 and its foreground trigger :102 — a headless tap must never reach either (:10-14)
@index.ts                        # bare Expo entry (registerRootComponent only) — this is a TWO-LINE ADDITION, not a rewrite
@src/db/database.ts              # openAndMigrate :95; getExecutor :136; localDateTime :46
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-push freshness wrapper + fire-and-forget publisher (widget-refresh.ts)</name>
  <files>src/services/widget/widget-refresh.ts</files>
  <read_first>RESEARCH Pattern 5 + the widget-refresh Code Example; launch-sweep.ts:71-90 (runLaunchSweep runs `for (const hook of hooks) { await hook(); }` with NO per-hook try/catch — a rejecting SweepHook propagates through the un-isolated loop and, since installSweepTrigger calls it with `void`, becomes an unhandled rejection; this is why pushWidgetUpdate must never reject); launch-sweep.ts:45 (registerSweepHook); App.tsx:120-139 (the one-shot registration idiom the sweep hook plugs into, wired in 12-07); notification-actions.ts / headless-task.ts:65-92 (the Logger-guarded try/catch-so-it-always-resolves discipline to mirror).</read_first>
  <action>Create widget-refresh.ts exporting `pushWidgetUpdate(): Promise<void>` that calls requestWidgetUpdate({ widgetName: "OrbitFavourites", renderWidget: renderFavourites (from 12-05), widgetNotFound: () => {} }) — the widgetNotFound no-op is what makes it safe to call when no instance is placed. WRAP the entire body in a try/catch that Logger.errors and SWALLOWS (never rethrows) so pushWidgetUpdate ALWAYS resolves (addresses Claude M3): it is registered as a launch-sweep hook and runLaunchSweep awaits hooks with no per-hook isolation (launch-sweep.ts:82), so a render/requestWidgetUpdate failure that rejected here would reject the whole fire-and-forget sweep (unhandled rejection; later hooks skipped). renderFavourites takes an optional widgetInfo (12-05); pass none here — a bare requestWidgetUpdate re-render defaults to the small layout, and the OS re-fires WIDGET_RESIZED with real size info when placed. Export `notifyWidgetDataChanged(): void` — a fire-and-forget publisher for FOREGROUND call sites (wired in 12-07): it calls `void pushWidgetUpdate()` (not awaited) so a successful user write is never rolled back or blocked by a widget-render failure (pushWidgetUpdate already swallows its own errors). Export `registerWidgetSweep()` = registerSweepHook(pushWidgetUpdate) so a real foreground launch recomputes + re-pushes (the actual registration call is made once, module-guarded, in App.tsx by 12-07). This launch recompute is foreground-only by construction (it fires from the sweep's foreground trigger, launch-sweep.ts:102); it is a SweepHook, never invoked from the headless tap path.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "widgetNotFound" src/services/widget/widget-refresh.ts && grep -Eq "try|catch" src/services/widget/widget-refresh.ts && grep -c "notifyWidgetDataChanged" src/services/widget/widget-refresh.ts</automated>
  </verify>
  <done>pushWidgetUpdate wraps requestWidgetUpdate (name OrbitFavourites, renderFavourites, widgetNotFound no-op) in a Logger-guarded try/catch that always resolves; notifyWidgetDataChanged is a fire-and-forget publisher; registerWidgetSweep registers the foreground recompute; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Widget task handler (widget-task-handler.tsx)</name>
  <files>src/services/widget/widget-task-handler.tsx</files>
  <read_first>notification-actions.ts:104-160 (the exact openAndMigrate-before-getExecutor + Logger-guarded try/catch structure to mirror); headless-task.ts:65-92 (the error-guard + try/catch-so-a-failure-never-crashes-the-headless-context body shape); launch-sweep.ts:10-14 (the negative invariant).</read_first>
  <action>Create widget-task-handler.tsx exporting `widgetTaskHandler` (the function registerWidgetTaskHandler expects). Switch on props.widgetAction: the render actions (WIDGET_ADDED / WIDGET_UPDATE / WIDGET_RESIZED) call props.renderWidget(await renderFavourites(props.widgetInfo)) to draw the tree — PASS props.widgetInfo so WIDGET_RESIZED selects the small vs large layout (12-05 pickLayout); WIDGET_CLICK with props.clickAction === "WIDGET_MARK" is the headless mark — read contactId from props.clickActionData (guard with Number.isInteger; ignore a non-integer), then await openAndMigrate() BEFORE getExecutor() (getExecutor throws pre-open, and in a killed-app launch React never mounts so App.tsx's open effect never runs — H1), compute now = localDateTime(), call widgetMarkContacted(getExecutor(), contactId, now) (12-05), then await pushWidgetUpdate() to re-render; WIDGET_DELETED is a no-op. Before writing the switch, CONFIRM the exact widgetAction enum member spellings (WIDGET_ADDED / WIDGET_UPDATE / WIDGET_RESIZED / WIDGET_CLICK / WIDGET_DELETED) and the props.widgetInfo field name against the installed types at node_modules/react-native-android-widget (RESEARCH verified WIDGET_CLICK/UPDATE/RESIZED first-hand; WIDGET_ADDED/WIDGET_DELETED are inherited from the dossier — addresses Claude L1). Wrap the whole body in a try/catch that Logger.errors and never rethrows (a thrown error must not crash the headless context — headless-task.ts shape). The write must fit the 30s headless budget (one SQLite write is well within it). CRITICAL: this handler must import NOTHING that reaches the foreground launch-sweep runner (launch-sweep.ts:71) or its trigger (launch-sweep.ts:102) — a headless tap must never fire quarantine expiry / purge / reconcile (launch-sweep.ts:10-14). Import only the mark seam, the render, the refresh wrapper, and the DB bootstrap. (Note: widget-refresh.ts is import-safe here because the launch-sweep module has zero module-scope side effects (launch-sweep.ts:10-14); the grep gate below checks this handler file, and the invariant additionally rests on that import-purity — never add an import that transitively reaches the foreground launch-sweep runner or its trigger.)</action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -rc "runLaunchSweep\|installSweepTrigger" src/services/widget/widget-task-handler.tsx) -eq 0 && grep -c "openAndMigrate" src/services/widget/widget-task-handler.tsx</automated>
  </verify>
  <done>widgetTaskHandler routes render actions to renderFavourites(props.widgetInfo) (size-selected layout) and WIDGET_MARK to openAndMigrate->getExecutor->widgetMarkContacted->pushWidgetUpdate, Logger-guarded; widgetAction enum + widgetInfo field confirmed against installed types; imports nothing reaching the launch sweep; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Register the handler in the app entry (index.ts)</name>
  <files>index.ts</files>
  <read_first>index.ts (the bare Expo entry — registerRootComponent(App) only; PATTERNS corrected RESEARCH: there is NO pre-existing share-intent registration, so this is a clean two-line addition).</read_first>
  <action>Add to index.ts (alongside the existing registerRootComponent(App)): import { registerWidgetTaskHandler } from "react-native-android-widget" and import { widgetTaskHandler } from "./src/services/widget/widget-task-handler", then call registerWidgetTaskHandler(widgetTaskHandler) at module scope. This is the library's own headless entry (you do NOT call TaskManager.defineTask yourself for the widget). Keep registerRootComponent(App) unchanged and first. package.json `main` already points at index.ts — no change there.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "registerWidgetTaskHandler(widgetTaskHandler)" index.ts</automated>
  </verify>
  <done>index.ts registers the widget task handler at module scope; registerRootComponent(App) preserved; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher click -> headless task | WIDGET_CLICK / render events arrive from the OS into a headless JS context |
| headless task -> foreground sweep | The headless task must not trigger foreground-only launch work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering / DoS | headless mark write | high | mitigate | openAndMigrate before getExecutor (H1); mark via widgetMarkContacted (single-writer DAO); Logger-guarded try/catch; 30s budget |
| T-12-07 | Elevation (side-effect) | handler importing the sweep | medium | mitigate | Handler imports nothing reaching the launch-sweep runner or trigger; grep-verified 0; launch recompute is a foreground SweepHook only |
| T-12-02 | Tampering | clickActionData.contactId | medium | mitigate | Number.isInteger guard before the mark; a non-integer is ignored; the DAO's guards + Logger-guarded catch swallow a stale/purged id |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the three files.
- `grep` confirms the handler imports nothing reaching the launch-sweep runner/trigger.
- index.ts registers the handler at module scope; registerRootComponent preserved.
- The killed-app headless mark round-trip is a device-UAT must-have (12-08) — bundled with the deferred Phase-11 check.
</verification>

<success_criteria>
- WDG-02: the headless mark handler is registered and structured like the proven notification-actions path.
- WDG-03: event-push freshness (requestWidgetUpdate, always-resolving) + the foreground launch recompute are in place (no polling); the fire-and-forget notifyWidgetDataChanged() publisher is exported here and WIRED at the concrete foreground mutation call sites in 12-07 (manual log, favourite toggle, favourite reorder, fuel edits) — so event-push freshness is genuinely delivered, not just a helper.
</success_criteria>

## Artifacts This Phase Produces (this plan)

New symbols introduced here — exclude from source-grounding drift checks:
- `src/services/widget/widget-refresh.ts`: `pushWidgetUpdate`, `notifyWidgetDataChanged`, `registerWidgetSweep`.
- `src/services/widget/widget-task-handler.tsx`: `widgetTaskHandler`.
- `index.ts`: the `registerWidgetTaskHandler(widgetTaskHandler)` registration.

<output>
Create `.planning/phases/12-home-screen-widget/12-06-SUMMARY.md` when done.
</output>
