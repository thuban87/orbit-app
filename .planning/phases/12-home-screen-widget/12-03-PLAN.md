---
phase: 12-home-screen-widget
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/services/widget/widget-photo.ts
  - src/services/widget/widget-photo.test.ts
  - src/services/widget/widget-colors.ts
  - src/services/widget/widget-colors.test.ts
autonomous: true
requirements: [WDG-01]
must_haves:
  truths:
    - "The base64 encoder downscales the 512px photo master to a small tile thumbnail and returns a data:image/jpeg;base64,... URI (or null when the contact has no photo)."
    - "The encoder never emits a file:// or http(s) source — the widget read path is base64-only, local-first."
    - "The widget colour resolver returns the status ring hex + border weight from the shared tokens WITHOUT calling the React theme hook (no ThemeProvider in the headless render)."
  artifacts:
    - "src/services/widget/widget-photo.ts (encodeWidgetThumb + THUMB_PX/THUMB_Q tunables)."
    - "src/services/widget/widget-colors.ts (widgetPalette + ringColor + ringWeight)."
    - "Both .test.ts files green."
  key_links:
    - "encodeWidgetThumb -> resolvePhotoUri (photo-storage.ts:130) -> ImageManipulator.manipulate().resize().renderAsync().saveAsync({base64:true})."
    - "widgetPalette -> resolvePalette(DEFAULT_PRESET_ID,'dark') -> palette.statusStable|statusWobble|statusDecay|rogue (12-01 tokens)."
---

## Phase Goal

**As a** person keeping people in orbit, **I want to** see my favourites on a home-screen widget with their status colours and act on them in one tap, **so that** I can mark contact or open someone without launching the app.

> MVP slice: the two remaining node-testable WDG-01 cores the widget render consumes — the base64 thumbnail encoder and the headless colour resolver. Pure/unit-tested, ahead of the native surface. Depends on 12-01 for the shared status tokens the resolver returns.

<objective>
Build the base64 avatar encoder and the headless status-colour resolver.

Purpose: WDG-01 requires "photos as base64" (RemoteViews cannot read file://, and network on the widget path violates local-first) and "each avatar carrying its status colour." The widget render runs in a headless task with NO ThemeProvider mounted, so colours must resolve directly from theme-presets, never useTheme.
Output: `widget-photo.ts` (data:-URI encoder reusing the existing ImageManipulator chain) and `widget-colors.ts` (palette + status→ring colour + weight), each with its Wave-0 test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-screen-widget/12-RESEARCH.md   # encoder Code Example; Pattern 4 (headless colour resolve); Pitfall 2 (bitmap ceiling)
@.planning/phases/12-home-screen-widget/12-PATTERNS.md   # widget-photo + widget-colors analogs
@.planning/phases/12-home-screen-widget/12-UI-SPEC.md    # ring weights (stable 2/wobble 3/decay 4/rogue 3); missing-photo fallback

# Analogs (verified on disk):
@src/services/photos/photo-pipeline.ts   # ImageManipulator.manipulate().resize().renderAsync()->saveAsync chain :27,85-92; tunable consts :38-42; PhotoPipelineError + Logger :49-54,93-95
@src/services/photos/photo-storage.ts    # resolvePhotoUri(relative) -> file:// of the 512px master :130
@src/theme/theme-presets.ts              # resolvePalette :96; DEFAULT_PRESET_ID "space-dark" :65; status seeds (12-01)
@src/components/ContactCard.tsx          # statusLabel switch shape :73-86 (mirror for the status->ring table)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Base64 tile thumbnail encoder (widget-photo.ts)</name>
  <files>src/services/widget/widget-photo.ts, src/services/widget/widget-photo.test.ts</files>
  <behavior>
    - encodeWidgetThumb(null) -> null (no photo).
    - encodeWidgetThumb(relativePath) -> a string beginning "data:image/jpeg;base64," (the manipulator base64 payload).
    - THUMB_PX and THUMB_Q are top-of-file tunable consts (device-spike-tunable; recommend ~88px, q~0.6).
    - The output is never a file:// or http(s) source.
  </behavior>
  <read_first>photo-pipeline.ts:27,38-42,85-95 (the chainable ImageManipulator API + tunable consts + typed-error/Logger convention); photo-storage.ts:130 (resolvePhotoUri).</read_first>
  <action>Create widget-photo.ts exporting `encodeWidgetThumb(relativePath: string | null): Promise<string | null>`. Return null immediately for a null/empty path (no photo -> the render uses the initials fallback). Otherwise resolve the master via resolvePhotoUri(relativePath) (the file:// of the 512px JPEG), then reuse the SDK-52+ chainable API: ImageManipulator.manipulate(fileUri).resize({ width: THUMB_PX, height: THUMB_PX }).renderAsync(), then saveAsync({ format: SaveFormat.JPEG, compress: THUMB_Q, base64: true }). Return the data URI by prefixing the returned base64 with "data:image/jpeg;base64,". Declare THUMB_PX and THUMB_Q as top-of-file tunable consts (CLAUDE.md single-number edit; small keeps the grid under the RemoteViews bitmap ceiling — a device spike in 12-08). Do NOT persist a file (WDG-01: no new persistent state — inline base64 only, no persistMaster, no DB write). Wrap decode failures in a typed error + Logger.error (photo-pipeline.ts convention). Write widget-photo.test.ts: mock expo-image-manipulator so manipulate().resize().renderAsync().saveAsync returns a known base64 string; assert the null-path branch returns null and a real path returns a string with the data:image/jpeg;base64, prefix.</action>
  <verify>
    <automated>npx vitest run src/services/widget/widget-photo.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>encodeWidgetThumb returns a data: URI for a photo path and null for no photo; THUMB_PX/THUMB_Q tunable at file top; no file persisted; test green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headless status-colour resolver (widget-colors.ts)</name>
  <files>src/services/widget/widget-colors.ts, src/services/widget/widget-colors.test.ts</files>
  <behavior>
    - widgetPalette() returns resolvePalette(DEFAULT_PRESET_ID, "dark") — the dark-first palette the headless render uses (no ThemeProvider).
    - ringColor(status, palette): stable -> palette.statusStable, wobble -> palette.statusWobble, decay -> palette.statusDecay, rogue -> palette.rogue, null -> palette.border.
    - ringWeight(status): stable 2, wobble 3, decay 4, rogue 3, null thin (e.g. 2).
    - No colour hex literal anywhere in this file; no React theme hook.
  </behavior>
  <read_first>theme-presets.ts:96-102 (resolvePalette), :65 (DEFAULT_PRESET_ID); theme-types.ts status token fields (12-01); ContactCard.tsx:73-86 (statusLabel switch shape to mirror for the table).</read_first>
  <action>Create widget-colors.ts. Export `widgetPalette()` returning resolvePalette(DEFAULT_PRESET_ID, "dark") (RESEARCH Pattern 4: the headless render inherits space-dark; light falls back to dark today). Export a WidgetPalette type alias for that palette shape. Export `ringColor(status: ProfileStatus | null, palette): string` and `ringWeight(status: ProfileStatus | null): number` as pure tables mirroring the ContactCard statusLabel switch shape, returning palette.statusStable/statusWobble/statusDecay/rogue/border and the UI-SPEC weights (stable 2 / wobble 3 / decay 4 / rogue 3 / null thin). Resolve every colour from the palette variable — this file must NOT call the React theme hook (no provider is mounted in the headless render) and must NOT contain a hex literal (check:colors allows hex only in theme-presets.ts, so passing a palette field is compliant). Write widget-colors.test.ts asserting the full colour+weight table for stable/wobble/decay/rogue/null against a palette fixture.</action>
  <verify>
    <automated>npx vitest run src/services/widget/widget-colors.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <done>widgetPalette resolves from theme-presets (no theme hook); ringColor/ringWeight cover all five status states from the shared tokens; check:colors + tsc clean; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget read path | Avatar bytes are rendered into an off-app bitmap; must never fetch over the network |
| theme literal surface | Colour hexes may only exist in theme-presets.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Information disclosure / local-first violation | widget-photo encoder | medium | mitigate | Encoder emits base64 data: only from the on-device master via resolvePhotoUri; never file:// or http(s); no network on the read path |
| T-12-08 | Tampering | widget-colors literals | low | mitigate | Colours resolve from theme-presets tokens; no hex in widget-colors.ts; check:colors gate enforced |
</threat_model>

<verification>
- `npx vitest run src/services/widget/widget-photo.test.ts src/services/widget/widget-colors.test.ts` green.
- `npm run check:colors` passes (no hex outside theme-presets.ts).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- WDG-01: photos encode to base64 data: URIs (local-first, no file://); status ring colour + weight resolve from the shared tokens without a theme provider.
</success_criteria>

## Artifacts This Phase Produces (this plan)

New symbols introduced here — exclude from source-grounding drift checks:
- `src/services/widget/widget-photo.ts`: `encodeWidgetThumb`, `THUMB_PX`, `THUMB_Q`.
- `src/services/widget/widget-colors.ts`: `widgetPalette`, `ringColor`, `ringWeight`, `WidgetPalette` type.

<output>
Create `.planning/phases/12-home-screen-widget/12-03-SUMMARY.md` when done.
</output>
