---
phase: 04-contact-crud-lifecycle
plan: 09
type: execute
wave: 6
depends_on: [04-08]
files_modified:
  - src/theme/theme-types.ts
  - src/theme/theme-presets.ts
  - src/db/purge-dao.ts
  - src/db/purge-dao.test.ts
  - src/screens/ArchivedContactsScreen.tsx
autonomous: true
requirements: [CRUD-06]
must_haves:
  truths:
    - "Purge deletes, in ONE transaction, the contact's interactions, events, fuel, contact_custom_values, contact_links, field_history, and the contacts row — explicit deletes, not relying on FK cascade."
    - "computeImpact returns the per-child counts that populate the impact-summary confirm; a zero-count line is omitted from the copy."
    - "Purge is guarded by a single impact-summary confirm ('Delete permanently') with no name-typing, reachable only from the Archived list."
    - "The Delete permanently button and any inline warning text reference the new danger theme token (#E5484D) — never an inline hex."
  artifacts:
    - "src/theme/theme-types.ts + theme-presets.ts — the danger token added to ThemePalette + space-dark"
    - "src/db/purge-dao.ts — computeImpact + purgeContact (one-transaction fan-out) with photo/notification extension points"
    - "src/screens/ArchivedContactsScreen.tsx — Delete permanently action + impact-summary confirm"
  key_links:
    - "purgeContact runs all deletes inside ONE inWriteTransaction; field_history (no FK to contacts) is deleted explicitly"
    - "photo-file + scheduled-notification cleanup are wired as required extension points (Phases 5/11 complete them) — documented, not fabricated"
    - "check:colors passes because danger resolves through the theme token"
---

## Phase Goal

**As a** person tending my relationships, **I want to** permanently delete a contact I am sure about, seeing exactly what will be destroyed first, **so that** I can clear out people for good without fear of a silent mistake.

**This slice (Plan 09):** the irreversible purge — the second stage of the lifecycle. After this plan the Archived list offers "Delete permanently", shows an impact summary, and fans out the deletes in one transaction. This completes CRUD-06 and the phase.

<objective>
Add the `danger` theme token (owner-approved `#E5484D`), the purge fan-out DAO (impact counts + one-transaction delete of every owned child + the contact, including `field_history`), and the "Delete permanently" action + impact-summary confirm on the Archived list. Photo-file and scheduled-notification cleanup are wired as required extension points that Phases 5 and 11 complete.

Purpose: CRUD-06. Purge is the only unrecoverable action and there is no server/backup — the impact-summary confirm is the safeguard, and explicit deletes keep the blast radius auditable (not reliant on FK cascade).
Output: danger token, `purge-dao.ts`, Archived-list purge UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-crud-lifecycle/04-UI-SPEC.md
@.planning/phases/04-contact-crud-lifecycle/04-PATTERNS.md
@.planning/phases/04-contact-crud-lifecycle/04-RESEARCH.md
@src/theme/theme-types.ts
@src/theme/theme-presets.ts
@src/db/field-ddl.ts
@src/db/migrations/001-initial.ts
@src/screens/ArchivedContactsScreen.tsx
@src/screens/CustomFieldsScreen.tsx
</context>

## Artifacts this plan produces

- `src/theme/theme-types.ts` (MODIFY) — `danger: string` added to `ThemePalette` (now 9 tokens).
- `src/theme/theme-presets.ts` (MODIFY) — `danger: "#E5484D"` in the `space-dark.dark` palette (the ONLY file a hex literal may appear in).
- `src/db/purge-dao.ts` — `computeImpact(exec, contactId): Promise<{ interactions, events, fuel, customValues, links }>`; `purgeContact(exec, contactId, { onPurgeExtensions? }): Promise<void>` (one-transaction fan-out); a documented extension-point hook for photo-file + notification cleanup.
- `src/db/purge-dao.test.ts`.
- Modified `src/screens/ArchivedContactsScreen.tsx` (Delete permanently + impact-summary confirm).

<tasks>

<task type="auto">
  <name>Task 1: Add the danger theme token</name>
  <files>src/theme/theme-types.ts, src/theme/theme-presets.ts</files>
  <read_first>
    - src/theme/theme-types.ts (ThemePalette interface 29-38 — the 8 current tokens)
    - src/theme/theme-presets.ts (space-dark.dark palette 22-31 — the sole hex-literal location)
    - UI-SPEC.md "Color → Resolved: add a danger token = #E5484D" + front-matter danger_token
  </read_first>
  <action>
    Add `danger: string;` to the `ThemePalette` interface in `theme-types.ts`. Add `danger: "#E5484D"` to the `space-dark.dark` palette object in `theme-presets.ts` (owner-approved 2026-08-14). This is the ONLY place a colour literal may appear (check:colors enforces). Do not add it anywhere else; do not introduce a light palette. Every downstream reference uses `useTheme().colors.danger`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `ThemePalette` now declares `danger`; `space-dark.dark` defines `danger: "#E5484D"`; no other file gains a hex.
    - Behavior: typecheck passes (the palette object satisfies the widened interface); `check:colors` green.
  </acceptance_criteria>
  <done>The danger token exists in the type + preset; the palette compiles and the colour gate stays green.</done>
</task>

<task type="tdd">
  <name>Task 2 (RED→GREEN): purge-dao fan-out + impact counts</name>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts</files>
  <read_first>
    - src/db/field-ddl.ts (deleteOrQuarantineField 165-187 — compute-then-act in ONE inWriteTransaction, explicit statements)
    - src/db/migrations/001-initial.ts (every child table + FK: interactions/events/fuel/contact_custom_values/contact_links CASCADE to contacts; field_history has NO FK)
    - RESEARCH.md "Pattern 3" + "Pitfall 3" (explicit deletes, field_history never cascades) + "Open Questions 2 & 3" (field_history in-scope; photo/notification extension points)
    - PATTERNS.md "purge-dao.ts"
  </read_first>
  <behavior>
    - computeImpact returns the correct COUNT per child (interactions, events, fuel, customValues rows, links) for a seeded contact.
    - purgeContact deletes ALL of that contact's interactions, events, fuel, contact_custom_values, contact_links, field_history rows, and the contacts row — leaving zero rows for that contact_id in every table.
    - It runs in ONE transaction: a forced failure mid-way leaves everything intact (rolled back).
    - A second contact's rows are untouched (scoped by contact_id / id).
  </behavior>
  <action>
    Write `src/db/purge-dao.test.ts` (node:sqlite harness): seed a contact with interactions, a custom-values row (add a value column first via the field DDL, or insert a values row directly), links, an events row, and a field_history row; assert computeImpact counts and that purgeContact removes everything for that contact while leaving a second contact intact. Then implement `src/db/purge-dao.ts`.
    `computeImpact(exec, contactId)`: five `SELECT COUNT(*)` reads (interactions, events, fuel, contact_custom_values, contact_links) `WHERE contact_id = ?`. `purgeContact(exec, contactId, opts?)`: ONE `inWriteTransaction` running explicit `DELETE FROM interactions/events/fuel/contact_custom_values/contact_links WHERE contact_id = ?`, `DELETE FROM field_history WHERE contact_id = ?` (no FK → never cascades; in-scope per OQ2), then `DELETE FROM contacts WHERE id = ?`. Explicit deletes are idempotent regardless of cascade order (belt-and-suspenders — keeps the blast radius auditable). Every value `?`-bound. Add an OPTIONAL `opts.onPurgeExtensions?: (contactId) => Promise<void>` hook called inside the transaction body for the photo-file + scheduled-notification cleanup that Phases 5/11 will register — this phase passes nothing (those subsystems + their deps don't exist yet); document it as a REQUIRED extension point in a module header comment (mirrors the DATA-06 launch-sweep registry pattern) so CRUD-06's "photo file + scheduled notifications" wording stays honest for what exists.
  </action>
  <verify>
    <automated>npx vitest run src/db/purge-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: purgeContact deletes all six child tables (incl. field_history) + contacts explicitly in ONE inWriteTransaction; nothing relies on cascade.
    - Source: computeImpact returns per-child counts; the extension-point hook is present + documented (not fabricating photo/notification deletes now).
    - Behavior: fan-out + count + rollback + scope-isolation tests pass.
  </acceptance_criteria>
  <done>Purge fan-out deletes every owned row in one transaction with correct impact counts; extension points wired.</done>
</task>

<task type="auto">
  <name>Task 3: Delete permanently + impact-summary confirm</name>
  <files>src/screens/ArchivedContactsScreen.tsx</files>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx (the marked Plan-09 slot from Plan 08; existing Restore row)
    - src/screens/CustomFieldsScreen.tsx (confirmSummary 85-98 — the Promise-wrapped Alert single gate to copy)
    - UI-SPEC.md "Copywriting Contract → Destructive confirmation purge" (exact title/body/actions; omit a zero-count line) + "Color → danger token / Scope note"
    - src/db/purge-dao.ts (computeImpact, purgeContact)
  </read_first>
  <action>
    In `ArchivedContactsScreen`, add a per-row "Delete permanently" Pressable styled with the `danger` token (text/border on the row's surface; reference `colors.danger`, never a hex). On press: `computeImpact(exec, id)`, then build the impact-summary body by interpolating the contact name + only the non-zero counts (e.g. "Permanently delete Chris and 12 interactions, 4 fuel items, 1 photo? This cannot be undone." — omit any line whose count is 0; "photo"/notification lines are not shown in Phase 4 since those subsystems don't exist). Present it through a Promise-wrapped `Alert.alert` (copy `confirmSummary`) titled "Delete permanently" with actions Cancel / "Delete permanently" (the native Alert keeps RN's built-in `style:"destructive"` — no token needed for the OS confirm). On confirm, `purgeContact(exec, id)` then reload the list; on throw, Logger.error + Alert. Single confirm, no name-typing. Theme tokens only; testID + accessibility.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src</automated>
  </verify>
  <acceptance_criteria>
    - Source: the Delete permanently button references `colors.danger` (no inline hex); the confirm is a single Promise-wrapped Alert with the exact title + destructive action, no name-typing.
    - Source: the impact body interpolates computeImpact counts and omits any zero-count line.
    - Behavior: `npx tsc --noEmit` + `npm run check:colors src` exit 0 (the whole src tree is clean, proving danger resolves through the token).
  </acceptance_criteria>
  <done>Delete permanently shows the impact summary and purges in one transaction, styled via the danger token; the phase's colour gate passes across src.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| purge action → DAO | An irreversible whole-contact delete crosses into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Repudiation / DoS-of-data (irreversible purge) | purgeContact | high | mitigate | Two-stage archive→purge (restore/purge only on the Archived list); a single impact-summary confirm shows the blast radius before deleting; there is no server/backup, so the confirm IS the safeguard |
| T-04-13 | Tampering (incomplete/inaccurate deletion) | fan-out vs FK cascade | high | mitigate | Explicit deletes of all six children (incl. field_history, which has no FK and never cascades) + the contact in ONE inWriteTransaction; impact counts come from explicit COUNTs, not assumed cascade behaviour |
| T-04-02 | Tampering (SQL injection) | purge/impact queries | high | mitigate | Every value `?`-bound; no interpolated identifier |

Local-first; the only non-DB targets (photo file, scheduled notifications) are wired as documented extension points for Phases 5/11 — not fabricated here.
</threat_model>

<verification>
- `npm test` (full suite incl. purge-dao) green; `npm run check:colors src App.tsx` green across the tree (proves danger resolves through the token).
- Device UAT (phase gate): Delete permanently on an archived contact shows the impact summary (correct counts, zero-count lines omitted) and removes the contact.
</verification>

<success_criteria>
The danger token exists; purge fans out every owned row (incl. field_history) in one transaction with accurate impact counts; the Archived list offers a single-confirm "Delete permanently" styled via the token; full suite + colour gate green.
</success_criteria>

<output>
Create `.planning/phases/04-contact-crud-lifecycle/04-09-SUMMARY.md` when done.
</output>
