---
phase: 04-contact-crud-lifecycle
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/db/recency-dao.ts
  - src/db/field-values-dao.ts
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/db/contact-read.ts
  - src/db/contact-read.test.ts
autonomous: true
requirements: [CRUD-01, CRUD-02]
must_haves:
  truths:
    - "Creating a contact writes the contacts row + (for Today/Pick date) exactly one interaction row through the single-writer recompute + any show_on_new custom values — ALL in ONE inWriteTransaction, never hanging (Pitfall 1)."
    - "The 'Not yet / don't know' path writes the contacts row with NULL last_contact and NO interaction row (never-contacted)."
    - "A non-positive or non-integer interval_days is rejected before any transaction opens."
    - "A duplicate-name check finds a live same-name contact, excludes archived rows and (on edit) excludes self."
  artifacts:
    - "src/db/contacts-dao.ts — createContactFull composing non-mutexed cores in one transaction"
    - "src/db/contact-read.ts — isDuplicateName, listCategories, getContactHeader reads"
    - "exported recomputeLastContact + insertInteractionCore from recency-dao.ts; upsertValueCore from field-values-dao.ts"
  key_links:
    - "createContactFull calls the extracted cores (no nested inWriteTransaction) — the non-reentrant mutex is entered exactly once"
    - "the first interaction is source='manual', direction=null (never outbound — would pollute intensity)"
    - "last_contact is set only via recomputeLastContact (single-writer invariant intact)"
---

## Phase Goal

**As a** person tending my relationships, **I want to** add, edit, archive, and permanently delete the people I track, **so that** my orbit only holds people I still care about and their details stay right.

**This slice (Plan 02):** the data layer for creating a contact. MVP-wise this is the failing-test-first end-to-end path proven headless via `node:sqlite` — the create-form UI (Plans 03–04) then makes it user-reachable. After this plan, `createContactFull` atomically creates a contact + first interaction + custom values, and the shared reads the forms need exist.

<objective>
Build the composed atomic-create core (RESEARCH Pattern 2 — the phase's central architectural task) plus the shared read helpers the create/edit forms need. This is TDD: the tests describe the atomicity + single-writer + not-yet behaviours first, then the cores make them pass.

Purpose: CRUD-01/02 require contact + interaction + `show_on_new` custom values to be written atomically, but the existing `createContactWithInteraction` and `upsertValue` each wrap their own `inWriteTransaction`, and the shared mutex is NON-reentrant — naively composing them is a permanent hang (Pitfall 1 / the HIGH-1 lesson). The fix is the established core/wrapper split.
Output: `contacts-dao.ts` (`createContactFull`), `contact-read.ts` (`isDuplicateName`, `listCategories`, `getContactHeader`), and extracted non-mutexed cores in the two existing DAOs — with their existing wrapped exports left unchanged for standalone callers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-crud-lifecycle/04-RESEARCH.md
@.planning/phases/04-contact-crud-lifecycle/04-PATTERNS.md
@src/db/recency-dao.ts
@src/db/field-values-dao.ts
@src/db/transaction.ts
@src/db/field-ddl.ts
@src/db/recency-dao.test.ts
@src/db/migrations/001-initial.ts
</context>

## Artifacts this plan produces

- `src/db/contacts-dao.ts` — `createContactFull(exec, input): Promise<{ contactId: number; interactionId: number | null }>` where `input` carries `{ uid, name, intervalDays, now, categoryId?, rarelyResponds?, firstInteraction?: FirstInteractionInput, rowUid?: string, customValues?: { col: string; value: string | null }[] }`.
- `src/db/contact-read.ts` — `isDuplicateName(exec, name, excludeId?): Promise<boolean>`; `listCategories(exec): Promise<{ id: number; name: string }[]>`; `getContactHeader(exec, contactId): Promise<{ id, name, rarely_responds, archived_at } | null>`.
- Modified `src/db/recency-dao.ts` — export `recomputeLastContact(exec, contactId, now)` (public entry) and `insertInteractionCore` (rename/export the existing private `insertInteraction`); existing `createContactWithInteraction`/`recordTouchpoint`/etc. unchanged.
- Modified `src/db/field-values-dao.ts` — export `upsertValueCore(exec, contactId, uid, colName, value, now)` (the body of `upsertValue` minus its `inWriteTransaction` wrapper); existing `upsertValue` unchanged, re-implemented to call the core inside its wrapper.
- New tests: `src/db/contacts-dao.test.ts`, `src/db/contact-read.test.ts`.

<tasks>

<task type="tdd">
  <name>Task 1 (RED): failing tests for atomic create + duplicate-name</name>
  <files>src/db/contacts-dao.test.ts, src/db/contact-read.test.ts</files>
  <read_first>
    - src/db/recency-dao.test.ts (the exact node:sqlite harness to copy: `openTestDb` + `nodeSqliteExecutor` + real `runMigrations([migration001])`, `uid()` counter, `lastContact`/`interactionCount` helpers)
    - src/db/field-values-dao.ts (upsertValue UID CONTRACT — the row uid is per-CONTACT, written on INSERT only)
    - RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" + "Wave 0 Gaps"
  </read_first>
  <behavior>
    - createContactFull with firstInteraction=Today writes 1 contact + 1 interaction; last_contact === the interaction occurred_at; interaction source==='manual' and direction===null.
    - createContactFull with NO firstInteraction ("not yet") writes 1 contact, 0 interactions, last_contact IS NULL.
    - createContactFull with firstInteraction + 2 customValues completes (does NOT hang) and the contact_custom_values row holds both values under one per-contact uid (guards Pitfall 1).
    - createContactFull rejects intervalDays 0, -1, and 1.5 before writing anything (0 contacts after a rejected call).
    - isDuplicateName returns true for an existing live same-name contact, false when the only match is archived (archived_at set), and false for the row itself when excludeId is passed.
    - listCategories returns the 4 seeded categories in display_order.
  </behavior>
  <action>
    Author `src/db/contacts-dao.test.ts` and `src/db/contact-read.test.ts` using the recency-dao.test.ts harness verbatim (fresh in-memory DB per test, real migration001, a `uid()` counter). Write the behaviours above as failing assertions against `createContactFull`, `isDuplicateName`, `listCategories` — which do not exist yet, so the suite fails to import/compile (RED). Do NOT write the implementation in this task. Include a test that a create+customValues call resolves within the test (no timeout) as the concrete Pitfall-1 deadlock guard.
  </action>
  <verify>
    <automated>npx vitest run src/db/contacts-dao.test.ts src/db/contact-read.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the two suites RUN and FAIL (missing exports / unmet assertions) — a genuine RED, not a syntax skip.
    - Source: tests import from `@/db/__testkit__/node-sqlite` and drive `runMigrations([migration001], 1, ...)` — the established DAO-test contract.
  </acceptance_criteria>
  <done>Both new suites exist and fail because the DAO functions are unimplemented.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): extract cores + createContactFull</name>
  <files>src/db/recency-dao.ts, src/db/field-values-dao.ts, src/db/contacts-dao.ts</files>
  <read_first>
    - src/db/field-ddl.ts (the core/wrapper precedent — `dropFieldColumns` private non-mutexed core, `dropField` wrapper, `deleteOrQuarantineField` composing the core inside ONE inWriteTransaction; lines 12-30 header + 106-187)
    - src/db/recency-dao.ts (the functions to modify — `insertInteraction` 156-192, `recomputeLastContact` 137-154, `createContactWithInteraction` 278-330 incl. the interval guard 289-298)
    - src/db/transaction.ts (NON-REENTRANCY note — never nest inWriteTransaction)
  </read_first>
  <action>
    In `src/db/recency-dao.ts`: export `recomputeLastContact` (change `async function` to `export async function`) as the public recompute entry the edit path (Plan 05) will also import; export the existing `insertInteraction` as `insertInteractionCore` (add an exported alias or export the function directly). These are non-mutexed cores (no `inWriteTransaction` inside them). Leave `createContactWithInteraction`, `recordTouchpoint`, `editTouchpoint`, `deleteTouchpoint` exactly as they are — they keep owning their own transaction for standalone callers.
    In `src/db/field-values-dao.ts`: extract the body of `upsertValue` (lines 107-118, the assertSafeCol + INSERT…ON CONFLICT) into a new exported `upsertValueCore(exec, contactId, uid, colName, value, now)` that assumes BEGIN is already open (NO inWriteTransaction); re-implement `upsertValue` to call `upsertValueCore` inside its existing `inWriteTransaction` wrapper. Preserve the UID CONTRACT comment — the uid is the per-contact row uid, written on INSERT only.
    Create `src/db/contacts-dao.ts`: implement `createContactFull(exec, input)`. Validate `Number.isInteger(input.intervalDays) && input.intervalDays > 0` BEFORE opening any transaction — `return Promise.reject(new Error(...))` on failure (mirror recency-dao.ts:289-298, so no txn opens and a `.catch()` sees it). Then ONE `inWriteTransaction`: INSERT the contacts row (uid, name, category_id, interval_days, rarely_responds, created_at, modified_at — reuse the column set from createContactWithInteraction) capturing lastInsertRowId; if `input.firstInteraction`, call `insertInteractionCore(exec, contactId, input.now, input.firstInteraction)` then `recomputeLastContact(exec, contactId, input.now)`; for each of `input.customValues ?? []` call `upsertValueCore(exec, contactId, input.rowUid, cv.col, cv.value, input.now)` (mint `rowUid` once per contact — the caller passes it). Return `{ contactId, interactionId }`. Every value `?`-bound; the only custom identifier reaches SQL through the guarded `upsertValueCore`. Never nest inWriteTransaction.
  </action>
  <verify>
    <automated>npx vitest run src/db/contacts-dao.test.ts src/db/recency-dao.test.ts src/db/field-values-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `contacts-dao.ts` opens `inWriteTransaction` exactly once in `createContactFull` and calls `insertInteractionCore`/`recomputeLastContact`/`upsertValueCore` (no call to the wrapped `createContactWithInteraction` or `upsertValue`).
    - Source: `recency-dao.ts` still exports `createContactWithInteraction` unchanged; `recomputeLastContact` + `insertInteractionCore` are now exported.
    - Source: `field-values-dao.ts` `upsertValue` behaviour is unchanged (still wraps the core) — its existing test suite stays green.
    - Behavior: `contacts-dao.test.ts` create/not-yet/interval/no-hang cases pass; `recency-dao.test.ts` and `field-values-dao.test.ts` remain green (no regression).
  </acceptance_criteria>
  <done>Atomic create passes; existing recency + field-values suites unregressed.</done>
</task>

<task type="tdd">
  <name>Task 3 (GREEN): contact-read reads</name>
  <files>src/db/contact-read.ts</files>
  <read_first>
    - src/db/field-values-dao.ts (getValuesForContact 63-87 — read-only, getFirstAsync, ?-bound contactId — the read-DAO shape to mirror)
    - src/db/queries.ts (STATUS_SCAN's `archived_at IS NULL` live-contact filter — the predicate isDuplicateName must reuse)
    - src/db/migrations/001-initial.ts (categories seed + contacts columns)
  </read_first>
  <action>
    Create `src/db/contact-read.ts`. Implement `isDuplicateName(exec, name, excludeId?)`: `SELECT 1 FROM contacts WHERE name = ? AND archived_at IS NULL AND id != ? LIMIT 1` — bind `excludeId ?? -1` so the self-exclusion is a no-op on create (no row has id -1); return `row !== null`. Implement `listCategories(exec)`: `SELECT id, name FROM categories ORDER BY display_order` (the create/edit category picker source). Implement `getContactHeader(exec, contactId)`: `SELECT id, name, rarely_responds, archived_at FROM contacts WHERE id = ?` returning the row or null (the Profile scaffold's light read — Plan 04). All reads are pure (no transaction), every value `?`-bound, no interpolation. Timestamps not written here.
  </action>
  <verify>
    <automated>npx vitest run src/db/contact-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `isDuplicateName` filters `archived_at IS NULL` and excludes `id != ?` — the Pitfall-6 query shape.
    - Source: all three functions are `?`-bound reads with no string interpolation.
    - Behavior: `contact-read.test.ts` duplicate/archived/self-exclusion + listCategories cases pass.
  </acceptance_criteria>
  <done>Shared reads pass; whole plan suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form dict → DAO | Untrusted user text (name, custom values) crosses into SQL here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering (SQL injection) | createContactFull / contact-read reads | high | mitigate | Every runtime value is `?`-bound; the ONLY interpolated identifier is a custom `col_name`, and it reaches SQL solely through the guarded `upsertValueCore` (`isSafeColName` + double-quote). No new interpolation site added. |
| T-04-03 | Tampering (data corruption) | atomic create composition | high | mitigate | Cores composed inside ONE `inWriteTransaction` (never nested — Pitfall 1); `last_contact` written only via `recomputeLastContact` (single-writer DATA-04); interval>0 rejected before any write. |
| T-04-04 | Repudiation | first-interaction provenance | medium | mitigate | The create interaction is `source='manual'`, `direction=null` (never defaulted to `outbound`, which would pollute intensity math). |

Local-first; no network/auth surfaces.
</threat_model>

<verification>
- `npm test` green (new suites + no regression in recency/field-values suites).
- The Pitfall-1 no-hang test proves composition does not deadlock the non-reentrant mutex.
</verification>

<success_criteria>
`createContactFull` creates contact + interaction + custom values atomically (or contact-only for "not yet"), rejects bad intervals, and never nests the mutex; `isDuplicateName`/`listCategories`/`getContactHeader` back the forms; all suites green.
</success_criteria>

<output>
Create `.planning/phases/04-contact-crud-lifecycle/04-02-SUMMARY.md` when done.
</output>
