---
phase: 31.1-app-wide-system-backgrounds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/RootNavigator.tsx
  - src/theme/navigation-theme.ts
  - src/stores/focused-route-store.ts
  - src/navigation/focused-route-classification.ts
  - src/navigation/focused-route-classification.test.ts
  - App.tsx
  - src/screens/HomeScreen.tsx
  - src/stores/theme-store.ts
  - src/stores/theme-store.test.ts
autonomous: true
requirements: [THEME-03, THEME-04, PROF-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The active package's System background (default when the stored slot is NULL) renders FIXED behind the Home screen end-to-end, driven by the theme store — not per-screen logic (THEME-04, D-04/D-10/D-18)."
    - "The background does NOT scroll with Home's content; it stays pinned while the list scrolls (THEME-04, D-04)."
    - "The Orrery tab shows ZERO System-image bleed: OrreryScreen's root stays opaque AND the shell host is forced slotId=\"none\" when OrreryTab is focused (D-05; overdraw guard, Pitfall 3)."
    - "A null/absent stored slot resolves to the package default; 'none' resolves to the themed solid (None/Solid empty-image state) — THEME-03/THEME-04 empty-edge, via resolveBackground NULL/none branches (backgrounds.ts:187-205)."
    - "densityForRoute returns the UI-SPEC D-19 A tier for each enumerated route and 'presentation' for unknown/undefined (THEME-04 ordering-edge; unknown-route backstop, Pitfall 4)."
    - "setBackgroundForActivePackage writes galaxyBackground when active=galaxy and standardBackground when active=standard, accepts null, and touches no other field — mirroring setAccentForActivePackage exactly (THEME-03 adjacency-edge: per-package memory, no cross-contamination)."
    - "Switching the active package makes the shell host read THAT package's own remembered column (galaxy_background vs standard_background) with no cross-contamination (THEME-03 adjacency-edge; BackgroundHost.tsx:96-102)."
    - statement: "Android native-stack / react-native-screens scenes composite transparent on the physical Pixel via the NavigationContainer transparent-theme cascade (the one MEDIUM-confidence architectural risk, A1/Pitfall 2)."
      verification: backstop
    - statement: "A Profile with no resolved app-owned photo falls back to the active package's stored System slot by construction — ContactProfileScreen's own host reads the store when appOwnedBackgroundUri is null (PROF-05, D-06; resolve-presentation.ts:52-70, ContactProfileScreen.tsx:295-305). No code change; the shell mount makes this fallback visible."
      verification: backstop
  artifacts:
    - "src/theme/navigation-theme.ts (NEW) — transparent NavigationContainer Theme object; the one sanctioned 'transparent' literal localized here (check:colors exempt path src/theme/**)."
    - "src/stores/focused-route-store.ts (NEW) — runtime-only zustand store holding the deepest focused route name + active tab name, fed by NavigationContainer onStateChange (mirrors tab-bar-layout-store idiom)."
    - "densityForRoute (NEW) in src/navigation/focused-route-classification.ts — pure route-name → SurfaceDensity helper."
    - "setBackgroundForActivePackage (NEW) in src/stores/theme-store.ts — per-package live background setter."
    - "src/stores/theme-store.test.ts (NEW) — unit suite for the setter."
    - "Single shell-level BackgroundHost mount in src/navigation/RootNavigator.tsx (full-bleed; SafeAreaView inside)."
  key_links:
    - "NavigationContainer transparent theme → cascades transparency to bottom-tabs sceneStyle + all four native-stack contentStyle defaults in one change (App.tsx:364; RootNavigator.tsx)."
    - "NavigationContainer onStateChange → focused-route-store → densityForRoute → shell BackgroundHost density prop."
    - "OrreryTab focused (active tab name from focused-route-store) → shell host slotId=\"none\" (exclusion + overdraw guard)."
    - "theme-store galaxyBackground/standardBackground (boot-hydrated) → BackgroundHost storedSlot read (render path)."

  flagged_assumptions:
    - "A1/Pitfall 2 (DEVICE): Android native-stack transparency via the theme cascade is MEDIUM-confidence and MUST be proven on the physical Pixel in this plan. Bounded fallback if it fails: explicit transparent contentStyle on the affected stack (literal localized to src/theme/**), last resort a thin per-scene transparent wrapper — NEVER per-screen BackgroundHost copies (D-10)."
    - "A2 (MECHANISM, D-18-delegated): the focused-route density source is an onStateChange-fed store because the shell host sits ABOVE Tab.Navigator (useNavigationState has no navigator context there). Adopted per RESEARCH recommendation; flagged because D-18 delegated the mechanism."
    - "A6 (VISUAL): full-bleed host (background fills behind the top safe-area inset; SafeAreaView mounted INSIDE the host) is adopted per RESEARCH Pitfall 7. Escalate to owner ONLY if it changes the product look."
    - "PROF-05 unclassified edge-probe: the Profile photo-wins / System-fallback precedence is satisfied by construction (no code change). Flagged for manual review in Plan 04 device UAT rows 7-8 rather than silently dismissed."
---

<objective>
Deliver the VERTICAL TRACER slice for app-wide System backgrounds: one production-quality path that
touches every layer the phase will modify — store → resolver → shell render → one cooperating screen —
wired end-to-end and proven on the physical Pixel BEFORE any horizontal expansion (the picker across
Settings, the ~31 other screen roots).

This plan proves the single genuine architectural risk (A1/Pitfall 2: Android native-stack / react-
native-screens scene transparency) on the agent's best early-context tokens, so an architectural
dead-end surfaces after one commit rather than after every screen has been flipped.

Purpose: Close the D-09 store-setter gap and adopt the single shell-level BackgroundHost mount (D-18),
making the selected (or package-default) System background render fixed behind the Home screen while the
Orrery stays excluded — without weakening any opaque semantic surface.

Output: transparent NavigationContainer theme module, focused-route store + densityForRoute helper, the
shell BackgroundHost mount, Home root cooperating, the per-package background setter + its new unit
suite, Orrery exclusion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-RESEARCH.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-UI-SPEC.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-VALIDATION.md

Project non-negotiables (CLAUDE.md): local-first (no network on any render/selection path);
all colours resolve through theme tokens (the 'transparent' literal is BLOCKED outside src/**/theme/**
by check-colors.sh:33 — achieve transparency by OMITTING backgroundColor, and localize the one nav-theme
literal to src/theme/navigation-theme.ts); never drive animation from React state; no worktrees, never
push — commit in place on the current branch; do NOT reverse any recorded decision D-01..D-21 / HANDOFF §7
/ ADR-087 (stop and escalate to the owner if a change would weaken one).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end shell background behind Home — one path, device-proven</name>
  <files>src/theme/navigation-theme.ts, src/stores/focused-route-store.ts, src/navigation/focused-route-classification.ts, src/navigation/focused-route-classification.test.ts, App.tsx, src/navigation/RootNavigator.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/navigation/RootNavigator.tsx:155-239 (the 4-tab shell; SafeAreaView edges={["top"]} wrapping Tab.Navigator; screenOptions headerShown:false, animation:"fade"; the tabOptions/getFocusedRouteNameFromRoute idiom at 161-175) — the mount surface.
    - src/components/ui/BackgroundHost.tsx:50-183 (props: density, slotId undefined=read store / "none"=force solid, themePackage; absoluteFill fixed layer + flex:1 content; reads galaxyBackground/standardBackground from the store at 96-102) — the primitive, reuse do not replace (D-10).
    - App.tsx:350-394 (NavigationContainer ref={navigationRef} onReady; it is passed NO theme prop today; app-level overlays UniversalFab/Snackbar/AssistBanner are SIBLINGS of RootNavigator) and App.tsx:2 (NavigationContainer import).
    - src/navigation/linking.ts:36 (navigationRef = createRef<NavigationContainerRef<...>>) — the container ref to read getCurrentRoute() from.
    - src/navigation/focused-route-classification.ts:1-32 (FOCUSED_WORKFLOW_ROUTES + isFocusedWorkflow) and src/navigation/focused-route-classification.test.ts:1-57 (it.each idiom) — extend, do not fork (checker note).
    - src/stores/tab-bar-layout-store.ts:1-25 (the runtime-shell zustand store idiom to mirror for focused-route-store).
    - src/theme/tokens/surface.ts:57-64 (SurfaceDensity union = presentation|comfortable|dense; SURFACE_DENSITIES order).
    - src/theme/backgrounds.ts:104-205 (BACKGROUND_ORDER, PACKAGE_DEFAULT_SLOT galaxy→galaxy-deep-space/standard→standard-dawn, resolveBackground NULL/none/tampered branches).
    - src/screens/HomeScreen.tsx:1560-1564 (root View: style={[styles.root, { backgroundColor: colors.background }]}) — the one cooperating root this tracer flips.
    - src/screens/OrreryScreen.tsx:1008-1014 (root paints { backgroundColor: colors.background } opaquely) — confirm it stays opaque (D-05); DO NOT edit it.
    - scripts/check-colors.sh:33-60 (named-colour blocklist incl. 'transparent'; the ^[^:]*/theme/ path exemption).
    - The UI-SPEC D-19 A density table (authoritative per-route tiers) in 31.1-UI-SPEC.md:231-292.
  </read_first>
  <action>
    Create src/theme/navigation-theme.ts exporting a React Navigation Theme object built from
    @react-navigation/native DefaultTheme/DarkTheme with colors.background set to the transparent
    literal (this is the ONE sanctioned 'transparent' string; it is check:colors-exempt because the file
    lives under src/theme/). Keep every other colour resolving through existing tokens — do not hardcode
    any other colour. Pass this theme to NavigationContainer in App.tsx (it currently has no theme prop,
    App.tsx:364) so transparency cascades to the bottom-tabs sceneStyle default AND all four native-stack
    contentStyle defaults in one change.

    Create src/navigation/focused-route-classification.ts addition: a pure densityForRoute(routeName:
    string | undefined): SurfaceDensity keyed on a ROUTE_DENSITY map. Assign every route its UI-SPEC
    D-19 A tier: dense for 
    LogContact/GroupLog/UpdateContact/Memory; comfortable for Digest; and let Home/Profile/Archived/
    UnboundContacts/RecentlyDeleted/GroupEvents/ThingsToRemember/MemoryHistory be absent → default
    presentation. undefined → presentation. Keep isFocusedWorkflow untouched; add densityForRoute as a
    sibling export in the SAME file (do NOT create a parallel map). Extend focused-route-classification.test.ts
    with it.each cases asserting each enumerated route resolves to its tier, an unknown route and
    undefined resolve to presentation.

    Create src/stores/focused-route-store.ts — a runtime-only zustand store (mirror tab-bar-layout-store)
    holding routeName (deepest focused route) and tabName (active top-level tab), with a setter. Wire it
    from NavigationContainer's onStateChange in App.tsx: on each change read navigationRef.current?.
    getCurrentRoute()?.name for routeName and derive the active tab name from the root navigation state,
    and write both to the store. (onStateChange is the mechanism because the shell host sits ABOVE
    Tab.Navigator; A2 flagged.)

    Mount ONE BackgroundHost in RootNavigator.tsx as the OUTERMOST element (full-bleed; A6), with the
    existing SafeAreaView edges={["top"]} and Tab.Navigator nested INSIDE it. Drive density from
    densityForRoute(focusedRoute) read from focused-route-store; force slotId="none" when the active tab
    is OrreryTab (exclusion + overdraw guard, Pitfall 3) and slotId undefined otherwise (host reads the
    stored slot). Do NOT pass a slotId for non-Orrery tabs. Do NOT add a second BackgroundHost anywhere.

    Make HomeScreen cooperate: on the outermost root View (HomeScreen.tsx:1560-1564) OMIT the inline
    { backgroundColor: colors.background } so the root is transparent by default and the shell background
    shows through. Do NOT touch any nested surface/card/GlassSurface fill inside HomeScreen — only the
    outermost root's opaque fill is removed. Leave OrreryScreen's opaque root untouched (D-05).

    Prove on the PHYSICAL PIXEL (emu-connect; Orbit Metro session on :8082 per MEMORY; debug dev client):
    the package-default System background renders fixed behind Home, stays pinned while the Home list
    scrolls, and the Orrery tab shows no System image under pan/pinch. If Android scenes paint opaque
    (A1/Pitfall 2), apply the bounded per-stack transparent contentStyle fallback (literal localized to
    src/theme/navigation-theme.ts or a src/theme/** module) — never per-screen BackgroundHost copies.

    Do NOT add any crossfade/animation (D-21). Do NOT add AsyncStorage or any new preference source (D-09).
  </action>
  <verify>
    <automated>npx vitest run src/navigation/focused-route-classification.test.ts && npx tsc --noEmit && npm run check:colors src App.tsx</automated>
    <human-check>On the physical Pixel: Home reveals the default System background behind content; background stays fixed while Home scrolls; Orrery tab shows no System image under pan/pinch/tilt/yaw; no white/grey flash on tab switch (A1 proof).</human-check>
  </verify>
  <acceptance_criteria>
    - src/theme/navigation-theme.ts exists and is the ONLY file introducing a 'transparent' colour literal; `npm run check:colors src App.tsx` passes.
    - `npx tsc --noEmit` passes (densityForRoute returns SurfaceDensity; store types sound).
    - focused-route-classification.test.ts asserts every enumerated route → its UI-SPEC tier and unknown/undefined → presentation; the suite passes.
    - RootNavigator mounts exactly ONE BackgroundHost, outermost, with SafeAreaView + Tab.Navigator nested inside; density wired from densityForRoute; slotId="none" only when OrreryTab is the active tab.
    - HomeScreen's outermost root no longer sets backgroundColor: colors.background; nested surface fills unchanged.
    - OrreryScreen is NOT modified (git diff shows no change to src/screens/OrreryScreen.tsx).
    - Device: Home reveals the background fixed-behind-scroll; Orrery shows zero System-image bleed.
  </acceptance_criteria>
  <reversibility rating="reversible">The shell mount + transparent nav theme + Home flip revert cleanly; no migration, no published contract, no schema change.</reversibility>
  <precondition>Physical Pixel 6 Pro attached via emu-connect (one authorized device) with the Orbit debug dev client and Orbit Metro session reachable on :8082 (user_setup).</precondition>
  <done>The package-default System background renders fixed behind Home end-to-end on the Pixel, the Orrery stays excluded, densityForRoute is unit-green, and the work is committed. The Android-transparency architectural risk is resolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-package live background setter (D-09) + unit suite</name>
  <files>src/stores/theme-store.ts, src/stores/theme-store.test.ts</files>
  <read_first>
    - src/stores/theme-store.ts:26-61 (ThemeStore interface with setModeForActivePackage/setAccentForActivePackage at 32-34, and their impls at 49-60 — branch on state.package === "galaxy") — the exact idiom to mirror; galaxyBackground/standardBackground already initialised null at 45-46; BackgroundSlotId already imported at line 2; the in-file comment at 22-25 forbidding AsyncStorage.
    - src/theme/theme-option-ids.ts:45-58 (BACKGROUND_SLOT_IDS + BackgroundSlotId type).
    - src/db/app-settings-dao.test.ts:1134-1175, 1207-1217 (assertBackgroundId is ALREADY fully tested — per-package round-trip, null passthrough, made-up rejected; do NOT duplicate DAO validation here).
  </read_first>
  <action>
    Add setBackgroundForActivePackage(background: BackgroundSlotId | null): void to the ThemeStore
    interface (beside setAccentForActivePackage at theme-store.ts:33-34) and implement it as a verbatim
    mirror of setAccentForActivePackage (theme-store.ts:55-60): when state.package === "galaxy" set
    { galaxyBackground: background }, else set { standardBackground: background }. Accept null (package
    default). Touch no other field. Do NOT add AsyncStorage, a zustand persist, or any second preference
    source (D-09, enforced by the in-file comment at 22-25) — durable persistence is the SettingsScreen
    persist() DAO write wired in Plan 02, not here.

    Create src/stores/theme-store.test.ts (no such file exists): assert that with package="galaxy" the
    setter writes galaxyBackground and leaves standardBackground untouched; with package="standard" it
    writes standardBackground and leaves galaxyBackground untouched; that it accepts null for each; and
    that it mutates no unrelated field (mode/accent). Use the store's getState/setState directly (node;
    no RN).
  </action>
  <verify>
    <automated>npx vitest run src/stores/theme-store.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - setBackgroundForActivePackage exists on ThemeStore, branches on state.package exactly like the accent setter, and accepts BackgroundSlotId | null.
    - src/stores/theme-store.test.ts exists and asserts: galaxy-active write hits galaxyBackground only; standard-active write hits standardBackground only; null accepted for each; no unrelated field mutated.
    - `npx vitest run src/stores/theme-store.test.ts` passes; `npx tsc --noEmit` passes.
    - No AsyncStorage / zustand persist / second preference source introduced (grep theme-store.ts for AsyncStorage returns nothing new).
  </acceptance_criteria>
  <done>The per-package background setter exists, is unit-proven for per-package memory and null, and is committed. The picker in Plan 02 can now drive live selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored DB slot value → render path | A persisted galaxy_background/standard_background TEXT value is read at boot and rendered; a tampered/orphaned value crosses here. |
| app-owned file URI → BackgroundHost | Profile's own host may pass a file:// URI; BackgroundHost rejects non-file URIs by design. |
| (foreclosed) network → render path | D-12 forbids any network read on the selection/render path; there is no such boundary in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-01 | Tampering | stored slot id → resolveBackground | medium | mitigate | Write-path assertBackgroundId (app-settings-dao.ts:812-822) + render-time tampered/unknown-id → package-default fallback (backgrounds.ts:193-198). Two layers; no new code needed, not weakened. |
| T-31.1-02 | Information disclosure | background render path vs local-first | high | mitigate | No network/URI download/remote-pack path added (D-12); bundled slots are local require() thunks; BackgroundHost accepts only file:// app-owned URIs. This plan adds no egress and cannot widen what leaves the device. |
| T-31.1-03 | Denial of service / Availability | asset decode at runtime | medium | mitigate | BackgroundHost onError latch → resolveRenderableBackground → None/Solid silent degrade (BackgroundHost.tsx:152-154); Orrery slotId="none" avoids decoding an occluded image (overdraw). |
| T-31.1-04 | Tampering (integrity of theme invariant) | colour literal smuggled past check:colors | low | mitigate | The one 'transparent' literal is localized to src/theme/navigation-theme.ts (exempt path); all cooperating roots achieve transparency by OMITTING backgroundColor; `npm run check:colors` gate runs in verify. |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No packages are installed this phase (RESEARCH Package Legitimacy Audit: none); nothing to gate. |
</threat_model>

<verification>
- `npx vitest run src/navigation/focused-route-classification.test.ts src/stores/theme-store.test.ts` green.
- `npx tsc --noEmit` clean; `npm run check:colors src App.tsx` clean.
- Physical Pixel: default System background fixed behind Home; Orrery excluded (A1 transparency proof).
- git diff shows OrreryScreen unchanged; exactly one BackgroundHost added to RootNavigator.
</verification>

<success_criteria>
The vertical tracer is complete: a System background renders fixed behind Home end-to-end on the device,
the Orrery is excluded, the per-package setter + density infra + transparent-shell architecture are
proven and committed, so Plans 02/03 can expand horizontally (picker, remaining roots) with the
architectural risk already retired.
</success_criteria>

<output>
Create `.planning/phases/31.1-app-wide-system-backgrounds/31.1-01-SUMMARY.md` when done.
</output>
