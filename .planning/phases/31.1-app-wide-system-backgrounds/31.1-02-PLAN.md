---
phase: 31.1-app-wide-system-backgrounds
plan: 02
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [THEME-03, THEME-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Appearance shows an inline Background picker directly after the Accent row: a labeled Galaxy subgroup (4 assets) + a labeled Standard subgroup (4 assets) + the shared None (Solid) option (D-03/D-17)."
    - "ALL 8 assets + None/Solid are ALWAYS visible regardless of the active package — the picker does NOT filter by active package (D-02; THEME-03 adjacency; UAT 9)."
    - "Thumbnails follow the stable per-package BACKGROUND_ORDER (Galaxy: Deep Space, Starfield, Nebula, Aurora; Standard: Dawn, Paper, Dusk, Mesh), with None/Solid rendered once as the shared trailing option (THEME-03 ordering-edge; backgrounds.ts:110-128)."
    - "Tapping a thumbnail updates the app-wide background immediately (live preview via setBackgroundForActivePackage → ThemeProvider re-render) AND persists durably via persist() → updateAppSettings — no Apply button, no crossfade (D-09/D-17/D-21; UAT 10)."
    - "Selecting a slot writes the ACTIVE package's remembered column only: galaxyBackground when package=galaxy, standardBackground when package=standard; the other package keeps its own choice (THEME-03 adjacency; D-02; UAT 11)."
    - "The selected-state cue (accent-colour border) reflects the active package's stored slot, resolving null → that package's PACKAGE_DEFAULT_SLOT (Galaxy→galaxy-deep-space, Standard→standard-dawn) — mirroring the accent row's null→default cue (THEME-03 empty-edge)."
    - "No cross-navigation: the picker adds NO link/hint to or from Profile's Background manager; the System picker is reachable only via Settings → Appearance (D-20)."
    - "Persistence is the DAO write only — no AsyncStorage, no second preference source, no new migration/table/backup-format bump (D-08/D-09/D-13)."
  artifacts:
    - "Inline Background picker section inside src/screens/SettingsScreen.tsx Appearance block (after the accent-swatch row at 876-962)."
    - "Human-readable thumbnail labels (Deep Space/Starfield/Nebula/Aurora/Dawn/Paper/Dusk/Mesh + None (Solid)) as display strings mapped 1:1 to slot ids — NOT re-keying backgrounds.ts."
    - "SettingsScreen outermost root made transparent (cooperating root, dense tier via shell host)."
  key_links:
    - "thumbnail onPress → setBackgroundForActivePackage(slot) (live) + persist({galaxyBackground|standardBackground: slot}) (durable) → updateAppSettings → assertBackgroundId → app_settings column."
    - "thumbnail preview image ← resolveBackground(pkg, slot).source() — renders any slot for any package (backgrounds.ts:183-205)."
  flagged_assumptions:
    - "A5 (LABELS): the human-readable labels are researcher-proposed display strings, not owner-locked; the owner may rename any without touching backgrounds.ts or slot ids/order."
---

<objective>
Horizontal expansion of the tracer: add the one new UI surface — an inline grouped thumbnail Background
picker in Settings → Appearance (D-17) — and make the Settings screen root cooperate with the shell host.
After this plan a real user can SELECT a System background and see it apply live and persist.

Purpose: Deliver THEME-03's selection surface exactly as the accent-swatch row works (inline, live
preview, immediate persist), with all 8 assets always visible (D-02) and per-package memory (D-02/D-17).

Output: the picker section + its labels inside SettingsScreen.tsx, and the Settings root flipped
transparent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-RESEARCH.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-UI-SPEC.md

Depends on 31.1-01: consumes setBackgroundForActivePackage (added to theme-store.ts in Plan 01).

Project non-negotiables (CLAUDE.md): all colours via theme tokens (accent border uses colors.accent,
frame uses colors.border/surface — no literals; check:colors gate); local-first (thumbnails are bundled
local require() thunks, no network); no AsyncStorage / no schema or backup change (D-08/D-09/D-13); do NOT
add cross-navigation to Profile (D-20); never push, no worktrees; do NOT reverse D-01..D-21.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline grouped Background thumbnail grid (render, all-8-always-visible, labels, a11y)</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx:140-155 (the store selectors block — add galaxyBackground/standardBackground/setBackgroundForActivePackage selectors beside the accent ones) and 876-962 (the EXACT accent-swatch row idiom to mirror: testID, accessibilityRole/Label/State, selected border colors.accent else colors.border, the Default chip for the null case, the helper line).
    - src/theme/backgrounds.ts:104-205 (BACKGROUND_ORDER per package ending in NONE_SLOT_ID; PACKAGE_DEFAULT_SLOT; resolveBackground + the asset source() thunk on the resolved "asset" kind for thumbnail previews).
    - src/theme/theme-option-ids.ts:45-58 (BACKGROUND_SLOT_IDS — do NOT re-key; ids/order are owned by backgrounds.ts).
    - 31.1-UI-SPEC.md:119-191 (Copywriting Contract label table + D-17 picker contract: subgroups, always-visible, ≥44px targets, reflow-not-truncate at large text, accent-border cue).
    - src/components/ui/BackgroundHost.tsx:133-137 (how an asset source() is turned into an Image source — the same resolveBackground(pkg,slot).source() feeds a thumbnail Image).
  </read_first>
  <action>
    Add store selectors beside the accent ones (SettingsScreen.tsx:147-155): galaxyBackground,
    standardBackground, setBackgroundForActivePackage from useThemeStore.

    Render a new inline picker section in the Appearance block directly AFTER the accent-swatch row
    (after line 962), mirroring the accent row's structure. Row label "Background"; helper line
    "Backgrounds stay fixed behind your screens. Each package remembers its own choice." Two labeled
    subgroups — "Galaxy" then "Standard" — each iterating its BACKGROUND_ORDER MINUS the trailing
    NONE_SLOT_ID, then ONE shared "None (Solid)" thumbnail rendered last. Render BOTH subgroups ALWAYS,
    regardless of the active package (D-02) — do NOT filter by themePackage. Each thumbnail shows its
    bundled preview via resolveBackground(subgroupPackage, slot).source() inside an Image (None/Solid
    shows the themed solid, no image). Each thumbnail carries its human-readable label from the UI-SPEC
    table (Deep Space/Starfield/Nebula/Aurora/Dawn/Paper/Dusk/Mesh/None (Solid)) as a display string
    mapped 1:1 to the slot id — do NOT rename slot ids.

    Compute the selected cue: activeBackground = themePackage === "galaxy" ? galaxyBackground :
    standardBackground; selectedSlot = activeBackground ?? PACKAGE_DEFAULT_SLOT[themePackage]; a thumbnail
    is selected when selectedSlot === its slot. Selected → border colors.accent; else colors.border
    (mirror accent). Frame/background use colors.surface/colors.border tokens only — NO colour literal.

    Accessibility: each thumbnail accessibilityRole="button", accessibilityLabel = label + " background",
    accessibilityState={{ selected }}, tap target ≥ 44×44px (pad the Pressable, do not shrink the art).
    Labels must reflow/wrap (never truncate or shrink) and the grid must re-column at the largest system
    font scale (D-14/THEME-07). Do NOT add any link/hint to Profile's Background manager (D-20).

    Write behaviour lands in Task 2 — in this task the onPress may be a no-op placeholder or omitted so
    the grid renders; do not persist yet.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/SettingsScreen.tsx</automated>
    <human-check>On the Pixel, Settings → Appearance shows Background with Galaxy(4)+Standard(4)+None(Solid) all visible in BOTH packages; selected-border cue on the active package's current slot; labels reflow at largest font.</human-check>
  </verify>
  <acceptance_criteria>
    - The Appearance section renders a Background picker after the accent row with a Galaxy subgroup (4), a Standard subgroup (4), and one shared None (Solid), all present regardless of active package.
    - Thumbnail order within each subgroup follows BACKGROUND_ORDER minus the trailing none; None/Solid rendered once, last.
    - Each thumbnail: human-readable label, accessibilityRole="button", accessibilityState reflects selected, tap target ≥ 44px; selected border = colors.accent, else colors.border.
    - Selected cue resolves null → PACKAGE_DEFAULT_SLOT for the active package.
    - `npx tsc --noEmit` passes; `npm run check:colors src/screens/SettingsScreen.tsx` passes (no colour literal).
  </acceptance_criteria>
  <done>The grouped thumbnail grid renders all 8 assets + None/Solid in both packages with correct order, labels, selected cue, and a11y; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire live-preview + immediate-persist write behaviour + flip Settings root transparent</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx:876-962 (accent onPress: setAccentForActivePackage(id); void persist(themePackage === "galaxy" ? {galaxyAccent:id} : {standardAccent:id})) — the EXACT write idiom to mirror.
    - src/screens/SettingsScreen.tsx:430-453 (the persist() useCallback: updateAppSettings → getAppSettings → setSettings → reconcile; AppSettingsPatch type) — reuse as-is.
    - src/db/app-settings-dao.ts:268-269, 342-343, 468-469, 812-822, 943 (galaxyBackground/standardBackground are writable patch keys routed through assertBackgroundId in updateAppSettings) — confirms the persist path validates.
    - Find the SettingsScreen outermost root View that sets { backgroundColor: colors.background } (the cooperating root to flip). Confirm it is the OUTERMOST container, not a nested surface.
  </read_first>
  <action>
    Wire each thumbnail onPress exactly like the accent row: call setBackgroundForActivePackage(slot)
    for instant live preview (ThemeProvider re-renders the shell host from the store), AND
    void persist(themePackage === "galaxy" ? { galaxyBackground: slot } : { standardBackground: slot })
    for the durable DB write. For the None (Solid) thumbnail, slot is the NONE_SLOT_ID ('none'). Do NOT
    introduce a separate write path, AsyncStorage, or an Apply button — persistence is the existing
    persist() DAO write only (D-09). No crossfade/animation (D-21).

    Flip the SettingsScreen outermost root transparent by OMITTING the inline { backgroundColor:
    colors.background } on that root View only (transparent-by-default; no 'transparent' literal). Do NOT
    touch any nested surface/card/row/GlassSurface fill — those opaque semantic surfaces (the picker rows,
    setting rows, modals) stay intact (D-11). Settings is classified dense, so the shell host applies a
    dense scrim automatically via densityForRoute("Settings").
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/SettingsScreen.tsx</automated>
    <human-check>On the Pixel: tapping thumbnails changes the app-wide background immediately (no Apply); selected-border moves; with package=Standard picking a Galaxy asset stores it in Standard and Galaxy keeps its own; force-kill + relaunch restores the selection.</human-check>
  </verify>
  <acceptance_criteria>
    - Each thumbnail onPress calls setBackgroundForActivePackage(slot) AND persist() with the active package's key; None/Solid passes 'none'.
    - The write idiom matches the accent row verbatim (per-package branch); no new persistence path, no AsyncStorage.
    - SettingsScreen's outermost root no longer sets backgroundColor: colors.background; nested surfaces unchanged.
    - `npx tsc --noEmit` and `npm run check:colors src/screens/SettingsScreen.tsx` pass.
    - Device: live preview + immediate persist + per-package independence + relaunch-restore all hold (UAT 10/11/12 preview).
  </acceptance_criteria>
  <done>Selecting a thumbnail previews live and persists per-package, the Settings root cooperates with the shell host, and the work is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker selection → stored DB slot value | A tapped slot id is written via persist() → updateAppSettings; validated at the write boundary. |
| (foreclosed) network → selection path | D-12 forbids any network on the selection path; thumbnails are local require() thunks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-01 | Tampering | persisted slot id | medium | mitigate | updateAppSettings routes galaxyBackground/standardBackground through assertBackgroundId (app-settings-dao.ts:943, 812-822) — only the known slot-id set + null persists; render-time fallback backs it up. |
| T-31.1-02 | Information disclosure | thumbnail/selection path vs local-first | high | mitigate | Thumbnails are bundled local require() assets; no URI download/remote pack/telemetry added (D-12). The picker cannot widen egress. |
| T-31.1-04 | Tampering (theme invariant) | colour literal in the picker | low | mitigate | Accent border = colors.accent, frame = colors.border/surface — zero literals; `npm run check:colors src/screens/SettingsScreen.tsx` in verify. |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No packages installed this phase (RESEARCH audit: none). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run check:colors src/screens/SettingsScreen.tsx` clean.
- Device: picker shows all 8 + None in both packages (not filtered); live preview + immediate persist; per-package write independence; relaunch restores selection.
- No cross-navigation link to/from Profile (grep SettingsScreen for any Profile-background navigation — none added).
</verification>

<success_criteria>
A user can select any of the 8 bundled backgrounds or None/Solid in Settings → Appearance; the choice
previews live app-wide, persists durably per package, and survives relaunch — with the picker never
filtering by active package and never touching Profile's separate template system.
</success_criteria>

<output>
Create `.planning/phases/31.1-app-wide-system-backgrounds/31.1-02-SUMMARY.md` when done.
</output>
