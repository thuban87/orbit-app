---
phase: 31.1
slug: app-wide-system-backgrounds
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-10
---

# Phase 31.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Seeded by plan-phase from
> `31.1-RESEARCH.md` § Validation Architecture. The planner fills the Per-Task Verification Map; this
> phase is ~95% wiring of already-verified primitives, so most logic is vitest-assertable and the
> app-wide *visual* behaviour is physical-Pixel UI-observable only (per CLAUDE.md: a screen rendering
> is not proof of an invariant).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing; `node:sqlite`-backed DAO tests) |
| **Config file** | `vitest.config.ts` (repo root) |
| **Quick run command** | `npm test -- <file>` |
| **Full suite command** | `npm test` |
| **Colour-literal gate** | `npm run check:colors` (blocks hardcoded colours incl. `'transparent'` outside `src/**/theme/**`) |
| **Estimated runtime** | ~tens of seconds (unit/integration); device UAT is manual |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- <touched test file>` + `npm run check:colors` when theme/scene code changed
- **After every plan wave:** Run `npm test` (full suite)
- **Before `/gsd-verify-work`:** Full suite green + `check:colors` clean
- **Max feedback latency:** ~60 seconds for the unit/integration layer

---

## Per-Task Verification Map

> Populated by the planner / validate-phase against the final PLAN.md tasks. Rows map each task to its
> requirement, test type, and automated command. Vitest-assertable vs Pixel-UI-observable split below.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | — | THEME-03 / THEME-04 | — | N/A (local-only, no network) | unit/integration | `npm test -- <file>` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Vitest-assertable (automated):**
- `setBackgroundForActivePackage` writes the active package's column and no other (store unit test).
- `assertBackgroundId` accepts all 8 slot ids + `none` regardless of package; rejects bogus ids.
- `resolveBackground` / `resolveRenderableBackground`: invalid/tampered id → package default; decode-failure latch → None/Solid.
- `densityForRoute` (new, added to `focused-route-classification.ts`): each enumerated route → its UI-SPEC density tier; unknown route → safe default.
- Profile precedence resolver (`resolve-presentation.ts`): photo chain `source:"theme"` ⇒ `appOwnedBackgroundUri = null` (System fallback path).

---

## Wave 0 Requirements

- [ ] Test stubs for the `theme-store` background setter + `densityForRoute` mapping (files TBD by planner)
- [ ] Reuse existing DAO/theme test fixtures — no new framework install (vitest already present)

*Existing infrastructure covers the automated layer; no framework install required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Single shell background renders fixed behind all ordinary screens; pinned on scroll | THEME-04 | App-wide visual composition is not assertable in vitest | UI-SPEC D-19 B matrix rows 1,2,4 on the physical Pixel |
| Presentation reveal vs dense readability across package × mode × text-size × motion | THEME-04 / D-14 | Contrast/legibility is device- and asset-dependent | UI-SPEC D-19 B rows 1–3, 16, 17 on Pixel, both packages |
| Orrery shows zero System-background bleed under any camera state | THEME-04 / D-05 | Skia canvas occlusion is render-path dependent | UI-SPEC D-19 B row 6 on Pixel (pan/pinch/tilt/yaw) |
| Profile photo override + Profile→System fallback | PROF-05 / THEME-04 | Resolved-presentation render is UI-observable | UI-SPEC D-19 B rows 7,8 on Pixel |
| No wrong-theme / None-Solid flash on relaunch; selection restored before first paint | THEME-03 | First-paint timing is device-observable only | UI-SPEC D-19 B row 12 on Pixel (force-kill + relaunch) |
| Android native-stack / react-native-screens scene transparency | THEME-04 | Transparency robustness is device-dependent (research MEDIUM-confidence unknown) | Prove early on the Pixel; per-stack `contentStyle` scoped fallback if it fails |
| Existing Profile template create/crop/assign/delete/reset unchanged | PROF-01 / PROF-04 | Regression guard on preserved behaviour | Exercise Profile background manager on Pixel; confirm no behaviour change |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
