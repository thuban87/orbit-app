---
phase: 31.1
plan: 04
target: Physical Pixel 6 Pro
device_serial: 1A071FDEE002BU
metro: 8082
date: 2026-09-10
status: complete
---

# Phase 31.1 Device UAT — App-Wide System Backgrounds

## Environment

- Physical Pixel 6 Pro, one authorized device (`adb devices` cardinality: 1).
- Orbit DEBUG dev client, dedicated Metro on `:8082`; reverse mappings were
  `tcp:8082 -> tcp:8082` and `tcp:8081 -> tcp:8082`.
- Evidence is retained under `/tmp/orbit-31.1-*.png` on the build host.

## Automated gate

| Command | Result |
| --- | --- |
| `npx tsc --noEmit` | PASS |
| `npm run check:colors src App.tsx` | PASS |
| `npm test` | BLOCKED outside this plan's files: 323/324 files and 2,904 tests passed; `src/components/orrery/orrery-controls-render.test.tsx` fails at module load with `SyntaxError: Unexpected token 'typeof'`. |

## D-19 B DEBUG-client matrix

| Row | Result | Physical-Pixel evidence |
| --- | --- | --- |
| 1 — presentation reveal | PASS | Dashboard and the no-photo Profile show the System art with legible content: `/tmp/orbit-31.1-before-uat.png`, `/tmp/orbit-31.1-u8-system-fallback.png`; Group Events, Unbound Contacts, and Archived route captures complete the navigation sweep: `/tmp/orbit-31.1-u1-group-events.png`, `/tmp/orbit-31.1-u1-unbound.png`, `/tmp/orbit-31.1-u1-archived.png`. |
| 2 — dense readability | PASS | Settings, Profile cards, and the exercised browse/system-management surfaces remained legible with the System layer behind content: `/tmp/orbit-31.1-before-uat.png`, `/tmp/orbit-31.1-u7-category-photo.png`, `/tmp/orbit-31.1-u6-debug-systems-management.png`. |
| 3 — Digest comfortable | PASS | DEBUG Dashboard → Your Week shows the comfortable reading view with all six digest rows legible: `/tmp/orbit-31.1-u3-digest.png`, `/tmp/orbit-digest.xml`. |
| 4 — fixed on scroll | PASS | Controlled DEBUG Dashboard long-list captures show the contact grid moving while the System layer remains pinned: `/tmp/orbit-31.1-u4-dashboard-before-scroll.png`, `/tmp/orbit-31.1-u4-dashboard-after-scroll.png`, `/tmp/orbit-u4.xml`. |
| 5 — no visual regressions | PASS | No opaque wash/tile/corner was observed across Dashboard, Digest, Profile, Settings, Orrery, System Builder, Group Events, Unbound, Archived, and the DEV harness; `check:colors` passed. The brightest Standard Paper asset kept status-bar icons legible in both forced Light and Dark modes: `/tmp/orbit-31.1-u5-statusbar-standard-light.png`, `/tmp/orbit-31.1-u5-statusbar-standard-dark.png`. |
| 6 — Orrery/SystemBuilder exclusion | PASS | DEBUG Orrery had no System-image bleed after navigation via keyboard focus and Enter: `/tmp/orbit-31.1-u6-debug-orrery-key.png`, `/tmp/orbit-orrery-key.xml`. Systems Management rendered as the permitted dense/flipped surface: `/tmp/orbit-31.1-u6-debug-systems-management.png`. A new System Builder opened with its authoring canvas free of System art and was cancelled without saving: `/tmp/orbit-31.1-u6-debug-system-builder.png`, `/tmp/orbit-system-builder.xml`. Memory child sheets opened from Dashboard (existing UAT Ada Memory) and Orrery (temporary UAT Grace Memory): `/tmp/orbit-31.1-u6-dashboard-memory-child.png`, `/tmp/orbit-31.1-u6-orrery-memory-child.png`. |
| 7 — Profile photo wins | PASS | Contact: Andrew `/tmp/orbit-31.1-u7-contact-photo.png`; Category: temporarily assigned existing app-owned template to Family then rendered UAT Ada `/tmp/orbit-31.1-u7-category-photo.png`; Global: temporarily assigned an existing app-owned template then rendered no-contact/no-category-template UAT test Row 1 `/tmp/orbit-31.1-u7-global-photo.png`. Exact original values were restored afterward. |
| 8 — Profile System fallback | PASS | With all three template sources absent, UAT test Row 1 renders the active Galaxy System art: `/tmp/orbit-31.1-u8-system-fallback.png`; UAT Grace renders the Standard-package fallback when Standard is active: `/tmp/orbit-31.1-u8-standard-system-fallback.png`. |
| 9 — all 8 + None picker | PASS | Both Galaxy (4), Standard (4), and None/Solid exposed in Settings: `/tmp/orbit-31.1-before-uat.png`. |
| 10 — immediate preview/persist | PASS | Picker switched immediately between None/Solid, Starfield, and Deep Space without an Apply action: `/tmp/orbit-31.1-u13-none-solid.png`, `/tmp/orbit-31.1-u10-standard-deep-space.png`. |
| 11 — active-package isolation | PASS | With Standard active, selecting Galaxy Deep Space produced durable `galaxy_background=galaxy-starfield`, `standard_background=galaxy-deep-space`; switching back to Galaxy showed Starfield selected in `/tmp/orbit-debug-galaxy-memory2.xml`. |
| 12 — relaunch restore | PASS | Force-stop/launch retained the selected per-package values before the exact slot restoration in the data-safety record. |
| 13 — None/Solid | PASS | Picker-selected themed solid: `/tmp/orbit-31.1-u13-none-solid.png`; failure-path solid: `/tmp/orbit-31.1-u15-force-fallback.png`. |
| 14 — invalid stored id | PASS | Original `galaxy_background=<NULL>`, `standard_background=<NULL>` snapshotted through `run-as`; bogus Galaxy id launched cleanly to package default (`/tmp/orbit-31.1-u14-invalid-default.png`); exact `NULL|NULL` restored and re-read. |
| 15 — runtime decode failure | PASS | `forceRenderError` gives solid fallback (`/tmp/orbit-31.1-u15-force-fallback.png`) and restores (`/tmp/orbit-31.1-u15-force-reset.png`); missing app-owned `file://` mounts a real Image, fails silently to solid (`/tmp/orbit-31.1-u15-missing-uri-fallback.png`), then restores (`/tmp/orbit-31.1-u15-missing-uri-reset.png`). No banner/crash. |
| 16 — large text reflow | PASS | At temporary Pixel `font_scale=1.3`, all four Galaxy and Standard cells plus None remained visible; `Deep Space` wrapped without truncation and targets remained large: `/tmp/orbit-31.1-u16-largest-font-picker-final.png`, `/tmp/orbit-font-picker-final.xml`. Original `font_scale=1.15` was restored. |
| 17 — Reduced Motion | PASS | The Pixel animation-scale accessibility equivalent was set to `0`; Settings remained reachable and Starfield selected immediately (`selected=true` in `/tmp/orbit-motion-selected.xml`, screenshot `/tmp/orbit-31.1-u17-reduced-motion-selection.png`). All three scales were restored to `1`, and exact original picker slots were restored. |

## Profile-template regression guard

- Read-only WAL-aware `run-as` query confirms `profile_background_templates` contains only three app-owned `profile-backgrounds/<uuid>.jpg` rows; no bundled System slot id is present.
- Contact, Category, and global precedence are proven by the three row-7 screenshots above. The existing Profile Background manager also completed a temporary native photo-pick, crop/reposition, template save, Category/global/contact assignment controls, inherit/reset, and delete-confirmation lifecycle. The created `UAT Ada's backgroundUAT` template was deleted, and UAT Ada visibly returned to System fallback: `/tmp/orbit-31.1-u-profile-crop.png`, `/tmp/orbit-31.1-u-profile-template-created.png`, `/tmp/orbit-31.1-u-profile-template-family.png`, `/tmp/orbit-31.1-u-profile-template-reset.png`. No Profile source was altered to make these probes.
- No Profile source file has been modified by this plan.

## Data-safety record

The invalid-id probe used a temporary, locally built Android sqlite helper only under
`run-as com.bwales.orbit` because the Pixel image has no `sqlite3` shell binary. The
app was force-stopped before direct access. Original values were recorded before the
write and restored exactly afterwards. The temporary helper was removed during closeout.

The Category/global precedence probe also force-stopped the app before a transaction.
The original state was: no `profile_category_presentation` row for category `1` and
`app_settings.profile_background_template_uid=NULL`. It temporarily used existing
app-owned template UIDs only, then deleted the category-1 row and restored the global
column to `NULL`; a re-read reported `0` rows and `<NULL>`. The System-picker probe
ended with exact original `galaxy_background=<NULL>` and
`standard_background=<NULL>`, confirmed by re-read. `files/orbit_sqlx` and its
`/data/local/tmp` copy were removed before relaunch.

The largest-font probe snapshotted `system.font_scale=1.15`, temporarily used `1.3`,
and restored `1.15`. The Reduced Motion probe snapshotted all three Android global
animation scales at `1`, temporarily set them to `0`, selected Starfield for the
immediate-selection check, then restored all three to `1`. A force-stopped direct
re-read restored `galaxy_background=<NULL>` and `standard_background=<NULL>`; its
temporary SQL helper was removed again.

The Profile-template lifecycle used an app-local temporary template only. It was deleted
through its confirmation dialog after the assignment/reset checks; the no-photo UAT Ada
screen visibly returned to the System fallback, and `run-as` confirmed no `orbit_sqlx`
helper remains in app files.

Browse-child equivalence used one clearly marked temporary UAT Grace `memories` row
(`uid=uat-311-browse-memory`) because that contact is accessible from the All Contacts
Orrery sheet. The existing UAT Ada Memory was opened through the Dashboard profile stack;
the temporary UAT Grace Memory was opened through the Orrery profile stack. After force-stopping,
the temporary row was hard-deleted and a direct count returned
`0`; both helper locations were absent before relaunch. The final restoration read also
confirmed `font_scale=1.15`, all animation scales `1`, and picker slots `NULL|NULL`.

For the status-bar contrast observation, the exact theme snapshot was `theme_package=galaxy`,
`galaxy_mode=system`, `standard_mode=system`, and both picker slots `NULL`. Standard Paper was
temporarily used in forced Light then Dark mode solely for the brightest-asset captures. The
final force-stopped re-read restored every snapshot value exactly, then verified both SQL-helper
locations were absent before the final DEBUG relaunch.

## Final native gate

PASS — after the owner explicitly authorized final packaging, a fresh standalone release
APK was built on droid from the current source by `npm ci`, non-interactive Expo Android
prebuild, and `gradlew.bat assembleRelease --console=plain`. The first Gradle attempt
reported a Windows lock on `react-native-quick-crypto`'s generated x86 CMake logs; the
release-build Gradle processes were stopped, only that generated package build directory
was removed, and the clean retry completed successfully in 4m 57s (1,070 actionable
tasks; 202 executed).

- Pulled artifact: `app-release-31.1-04.apk` on this Linux build host, 201,097,059 bytes,
  SHA-256 `ebb5267d742e4c6224feb5af04f74b6d9a0f963f5086c34372401061a9aa9df9`.
- Installed on the single authorized physical Pixel via `~/.local/bin/adb -s 1A071FDEE002BU
  install -r`; result: `Success`. The standalone release launch rendered the Orbit shell
  without Metro: `/tmp/orbit-31.1-release-dashboard.png`,
  `/tmp/orbit-release-dashboard.xml`.
- Release smoke passed: Orrery stayed free of System art
  (`/tmp/orbit-31.1-release-orrery.png`, visually inspected); Andrew Wales' app-owned
  Profile photo overrode the System background
  (`/tmp/orbit-31.1-release-profile-andrew.png`); no-photo UAT Ada rendered the Galaxy
  System fallback (`/tmp/orbit-31.1-release-profile-photo.png`); force-stop/relaunch
  returned the Orbit shell with no dev-server dependency
  (`/tmp/orbit-31.1-release-relaunch.png`, `/tmp/orbit-release-relaunch.xml`).
- Release Settings was opened with `input touchscreen tap`; its XML contained no
  `Dev Background Preview`, `Theme Preview`, `__ThemePreview`, `forceRenderError`, or
  missing-URI harness text (`/tmp/orbit-31.1-release-settings-no-dev.png`,
  `/tmp/orbit-release-settings-no-dev.xml`). Thus the DEBUG-only harness route/row was
  absent from the standalone release.
- Delivered the validated, checksum-matched artifact with `scp` (no push and no phone
  rsync) to `G:\\My Drive\\IT\\Software\\Orbit\\app-release-31.1-04.apk` on droid.
  `certutil -hashfile ... SHA256` reported the same SHA-256 and byte count.
