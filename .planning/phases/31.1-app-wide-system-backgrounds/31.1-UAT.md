---
phase: 31.1
plan: 04
target: Physical Pixel 6 Pro
device_serial: 1A071FDEE002BU
metro: 8082
date: 2026-09-10
status: in-progress
---

# Phase 31.1 Device UAT — App-Wide System Backgrounds

## Environment

- Physical Pixel 6 Pro, one authorized device (`adb devices` cardinality: 1).
- Orbit DEBUG dev client, dedicated Metro on `:8082`; reverse mappings were
  `tcp:8082 -> tcp:8082` and `tcp:8081 -> tcp:8082`.
- Evidence is retained under `/tmp/orbit-31.1-*.png` on the build host.

## Automated gate

| Command | Result |
| --- | --- |
| `npx tsc --noEmit` | PASS |
| `npm run check:colors src App.tsx` | PASS |
| `npm test` | BLOCKED outside this plan's files: 323/324 files and 2,904 tests passed; `src/components/orrery/orrery-controls-render.test.tsx` fails at module load with `SyntaxError: Unexpected token 'typeof'`. |

## D-19 B DEBUG-client matrix

| Row | Result | Physical-Pixel evidence |
| --- | --- | --- |
| 1 — presentation reveal | PASS | Dashboard/background selection visual: `/tmp/orbit-31.1-before-uat.png`. |
| 2 — dense readability | PASS | Settings dense surface visibly legible: `/tmp/orbit-31.1-before-uat.png`. |
| 3 — Digest comfortable | Pending release-gate retest | Digest route needs final navigation pass. |
| 4 — fixed on scroll | PASS | Harness scroll cards remain above the fixed host: `/tmp/orbit-31.1-u15-harness-initial.png`. |
| 5 — no visual regressions | PASS (spot check) | No opaque wash/tile/corner observed in Settings/harness; final color gate pending. |
| 6 — Orrery/SystemBuilder exclusion | Pending release-gate retest | Requires final native route traversal. |
| 7 — Profile photo wins | PASS (contact source) | Andrew's resolved contact photo visibly replaces System: `/tmp/orbit-31.1-u7-contact-photo.png`. Category/global source variants remain in final regression pass. |
| 8 — Profile System fallback | Pending final navigation pass | UAT Grace fixture has no app-owned photo. |
| 9 — all 8 + None picker | PASS | Both Galaxy (4), Standard (4), and None/Solid exposed in Settings: `/tmp/orbit-31.1-before-uat.png`. |
| 10 — immediate preview/persist | Pending final navigation pass | Final release smoke carries this check. |
| 11 — active-package isolation | Pending final navigation pass | Store/DAO unit coverage remains green within the full suite except the unrelated loader failure. |
| 12 — relaunch restore | PASS (default recovery) | Force-stop/launch preserved the package default after row 14 restoration. |
| 13 — None/Solid | PASS | Silent themed solid fallback shown by `/tmp/orbit-31.1-u15-force-fallback.png`. |
| 14 — invalid stored id | PASS | Original `galaxy_background=<NULL>`, `standard_background=<NULL>` snapshotted through `run-as`; bogus Galaxy id launched cleanly to package default (`/tmp/orbit-31.1-u14-invalid-default.png`); exact `NULL|NULL` restored and re-read. |
| 15 — runtime decode failure | PASS | `forceRenderError` gives solid fallback (`/tmp/orbit-31.1-u15-force-fallback.png`) and restores (`/tmp/orbit-31.1-u15-force-reset.png`); missing app-owned `file://` mounts a real Image, fails silently to solid (`/tmp/orbit-31.1-u15-missing-uri-fallback.png`), then restores (`/tmp/orbit-31.1-u15-missing-uri-reset.png`). No banner/crash. |
| 16 — large text reflow | Pending final navigation pass | Picker has responsive 48% grid implementation; physical largest-scale capture outstanding. |
| 17 — Reduced Motion | Pending final navigation pass | No transition is implemented; physical OS setting capture outstanding. |

## Profile-template regression guard

- Read-only WAL-aware `run-as` query confirms `profile_background_templates` contains only three app-owned `profile-backgrounds/<uuid>.jpg` rows; no bundled System slot id is present.
- Contact override is proven by the Andrew profile screenshot above. The required Category/global precedence and create/crop/assign/delete/reset UI regression are still pending final device traversal.
- No Profile source file has been modified by this plan.

## Data-safety record

The invalid-id probe used a temporary, locally built Android sqlite helper only under
`run-as com.bwales.orbit` because the Pixel image has no `sqlite3` shell binary. The
app was force-stopped before direct access. Original values were recorded before the
write and restored exactly afterwards. The temporary helper is removed during closeout.

## Final native gate

Pending Task 3: standalone droid release build, physical-Pixel install/smoke, and
delivery to `G:\\My Drive\\IT\\Software\\Orbit`.
