---
phase: 31.1-app-wide-system-backgrounds
plan: 03
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/screens/ArchivedContactsScreen.tsx
  - src/screens/UnboundContactsScreen.tsx
  - src/screens/RecentlyDeletedScreen.tsx
  - src/screens/GroupEventsScreen.tsx
  - src/screens/ThingsToRememberScreen.tsx
  - src/screens/MemoryHistoryScreen.tsx
  - src/screens/DigestScreen.tsx
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/screens/ComposeScreen.tsx
  - src/screens/CaptureScreen.tsx
  - src/screens/CropPhotoScreen.tsx
  - src/screens/placeholders/FabActionPlaceholders.tsx
  - src/screens/BackupScreen.tsx
  - src/screens/BackupSettingsScreen.tsx
  - src/screens/RestorePreviewScreen.tsx
  - src/screens/RestoreResultScreen.tsx
  - src/screens/CustomFieldsScreen.tsx
  - src/screens/LegacyContactPickerScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportProgressScreen.tsx
  - src/screens/DuplicateReviewScreen.tsx
  - src/screens/ImportCompleteScreen.tsx
  - src/screens/BulkReviewScreen.tsx
  - src/screens/ReconcileDetailScreen.tsx
  - src/screens/ReconcileGridScreen.tsx
  - src/screens/ReconcileCompleteScreen.tsx
  - src/screens/SurvivorSelectScreen.tsx
  - src/screens/MergeConflictsScreen.tsx
  - src/screens/SystemsManagementScreen.tsx
autonomous: true
requirements: [THEME-04, PROF-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every ordinary in-app screen that today paints an opaque colors.background root is made to cooperate — its OUTERMOST root is transparent so the single shell BackgroundHost shows through (D-04/D-11/D-18; THEME-04)."
    - "Opaque semantic surfaces are NEVER weakened: cards, GlassSurface, input rows, modals, bottom sheets, snackbars, and loading/error overlays keep their existing opaque/semi-opaque surface tokens — the background shows through the GAPS between surfaces, not through the surfaces themselves (D-11)."
    - "Presentation/browse screens (Archived, Unbound, RecentlyDeleted, GroupEvents, ThingsToRemember, MemoryHistory) reveal the art with the light presentation scrim; Digest uses the comfortable middle tier; dense forms/config/flows keep the more-opaque dense scrim — all driven by the shell host's densityForRoute, not per-screen density logic (D-19 A)."
    - "The Orrery is NOT flipped — OrreryScreen's opaque root is left untouched so it occludes the shell host under every camera state (D-05); ContactProfileScreen is NOT flipped — it already roots in its own BackgroundHost (exception 1)."
    - "Transparency is achieved by OMITTING backgroundColor (transparent-by-default), never by writing the 'transparent' literal — check:colors stays green (Pitfall 1)."
    - "Existing Profile Hero / layout and all non-background screen behaviour are unchanged — this plan removes only outermost opaque root fills, no structural or behavioural change (PROF-01 preserved; D-15)."
  artifacts:
    - "31 cooperating screen roots flipped transparent (30 screen files + src/screens/placeholders/FabActionPlaceholders.tsx)."
  key_links:
    - "Each flipped root → the single shell BackgroundHost (Plan 01) renders behind it; the focused-route density scrim (densityForRoute) keeps content readable."
  flagged_assumptions:
    - "A4 (DENSITY): DigestScreen = comfortable per the UI-SPEC; if device-UAT row 3 (Plan 04) shows any contrast miss, step it to dense (readability wins, D-04) — never looser."
    - "The cooperating set is the files that CURRENTLY paint backgroundColor: colors.background at root (enumerated in files_modified from a 2026-09-10 grep). The executor re-runs the grep at execution time and flips exactly that set MINUS HomeScreen (Plan 01), SettingsScreen (Plan 02), and OrreryScreen (D-05 exclusion); SystemBuilderScreen / MergeImpactSummary are NOT in the set (they root on surfaces, already cooperate)."
---

<objective>
Horizontal expansion of the tracer: make every remaining ordinary screen root cooperate with the single
shell BackgroundHost (D-11/D-18) by removing its opaque full-page fill, so the fixed System background
shows through behind all regular in-app pages — while the Orrery stays excluded, Profile keeps its own
host, and every opaque semantic surface (cards, sheets, dialogs, loading/error) is preserved.

Purpose: Complete THEME-04's "behind every ordinary page" requirement across the real screen tree, with
readability provided by the shell host's focused-route density scrim rather than opaque page washes.

Output: 31 screen roots flipped transparent, grouped by density tier for coherent device-UAT review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-RESEARCH.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-UI-SPEC.md

Depends on 31.1-01: the single shell BackgroundHost mount must exist (Wave 1) before these roots are
flipped, so a flipped root reveals the shell background rather than the raw window.

Project non-negotiables (CLAUDE.md): "Review the code, not the diff" — for EACH file, open it and confirm
the line you remove is the OUTERMOST root's { backgroundColor: colors.background }, not a nested surface;
all colours via theme tokens (achieve transparency by OMITTING the fill — NEVER write 'transparent',
Pitfall 1/check:colors); local-first; never push, no worktrees; do NOT touch OrreryScreen (D-05) or
ContactProfileScreen (already hosts its own BackgroundHost); do NOT reverse D-01..D-21.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip presentation / browse screen roots transparent (7 files)</name>
  <files>src/screens/ArchivedContactsScreen.tsx, src/screens/UnboundContactsScreen.tsx, src/screens/RecentlyDeletedScreen.tsx, src/screens/GroupEventsScreen.tsx, src/screens/ThingsToRememberScreen.tsx, src/screens/MemoryHistoryScreen.tsx, src/screens/DigestScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx:1560-1564 as executed in Plan 01 (the reference flip: remove only the outermost root's { backgroundColor: colors.background }).
    - Each of the 7 files' outermost root View (grep "backgroundColor: colors.background" per file, then open and confirm it is the OUTERMOST container wrapping the screen, not a nested card/list surface).
    - 31.1-UI-SPEC.md:231-292 (D-19 A density table — these are presentation, except Digest=comfortable).
    - src/components/ui/BackgroundHost.tsx:127-167 (how the density scrim composites over the background — confirms readability is provided by the shell scrim, not the screen root).
  </read_first>
  <action>
    For each of the 7 files, open the screen and remove ONLY the inline { backgroundColor:
    colors.background } from the OUTERMOST root View (the one wrapping the whole screen), leaving the
    remaining style (e.g. styles.root) intact so the root becomes transparent-by-default. Do NOT write the
    'transparent' literal. Do NOT alter any nested surface/card/list-row/GlassSurface/section fill — those
    opaque semantic surfaces stay (D-11). Do NOT change layout, props, or behaviour. Density is supplied
    by the shell host via densityForRoute (these resolve to presentation; Digest to comfortable) — do NOT
    add a per-screen density or scrim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/ArchivedContactsScreen.tsx src/screens/UnboundContactsScreen.tsx src/screens/RecentlyDeletedScreen.tsx src/screens/GroupEventsScreen.tsx src/screens/ThingsToRememberScreen.tsx src/screens/MemoryHistoryScreen.tsx src/screens/DigestScreen.tsx</automated>
    <human-check>On the Pixel, each of these screens reveals the System background behind content with a light/comfortable scrim; content stays legible; no opaque full-page wash (UAT 1,3).</human-check>
  </verify>
  <acceptance_criteria>
    - Each of the 7 files' outermost root no longer sets backgroundColor: colors.background.
    - No 'transparent' literal introduced; `npm run check:colors <these files>` passes.
    - `npx tsc --noEmit` passes.
    - Nested surface fills in each file are unchanged (git diff shows only the outermost-root fill removed per file).
  </acceptance_criteria>
  <done>The 7 presentation/browse roots cooperate with the shell host and are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip dense input/form screen roots + FAB placeholders transparent (6 files)</name>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/ComposeScreen.tsx, src/screens/CaptureScreen.tsx, src/screens/CropPhotoScreen.tsx, src/screens/placeholders/FabActionPlaceholders.tsx</files>
  <read_first>
    - Each file's outermost root View (grep "backgroundColor: colors.background"; for FabActionPlaceholders.tsx the shared FabActionPlaceholder root at line ~15 backs all four placeholder screens — flip that one shared root).
    - src/screens/CropPhotoScreen.tsx root (exception 3: the Skia crop viewport fills the screen with the user's photo and does NOT reveal art; classify dense — flip the root transparent anyway so it is consistent; the dense scrim + the opaque crop content handle it).
    - 31.1-UI-SPEC.md:218-228 (bounded per-screen exceptions: CropPhoto dense; Profile/Orrery excluded).
  </read_first>
  <action>
    For each file remove ONLY the outermost root's inline { backgroundColor: colors.background }
    (transparent-by-default; no literal). For FabActionPlaceholders.tsx flip the single shared
    FabActionPlaceholder root (covers LogContact/GroupLog/UpdateContact/Memory placeholders). Leave all
    input rows, form surfaces, the Skia crop content, sheets, and dialogs opaque (D-11). These routes
    resolve to dense via densityForRoute — do NOT add per-screen density. No layout/behaviour change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/CreateContactScreen.tsx src/screens/EditContactScreen.tsx src/screens/ComposeScreen.tsx src/screens/CaptureScreen.tsx src/screens/CropPhotoScreen.tsx src/screens/placeholders/FabActionPlaceholders.tsx</automated>
    <human-check>On the Pixel, Create/Edit/Compose/Capture show the background faintly behind a dense scrim with all fields AA-readable; Crop shows the photo viewport opaque as before (UAT 2).</human-check>
  </verify>
  <acceptance_criteria>
    - Each file's outermost root no longer sets backgroundColor: colors.background; FabActionPlaceholders shared root flipped once.
    - No 'transparent' literal; `npm run check:colors <these files>` passes; `npx tsc --noEmit` passes.
    - Input rows / crop viewport / form surfaces unchanged (git diff shows only outermost-root fills removed).
  </acceptance_criteria>
  <done>The 6 dense input/form roots (incl. the shared placeholder root) cooperate with the shell host and are committed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip dense config / backup / import / reconcile / merge screen roots transparent (18 files)</name>
  <files>src/screens/BackupScreen.tsx, src/screens/BackupSettingsScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/CustomFieldsScreen.tsx, src/screens/LegacyContactPickerScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/BulkImportSetupScreen.tsx, src/screens/ImportProgressScreen.tsx, src/screens/DuplicateReviewScreen.tsx, src/screens/ImportCompleteScreen.tsx, src/screens/BulkReviewScreen.tsx, src/screens/ReconcileDetailScreen.tsx, src/screens/ReconcileGridScreen.tsx, src/screens/ReconcileCompleteScreen.tsx, src/screens/SurvivorSelectScreen.tsx, src/screens/MergeConflictsScreen.tsx, src/screens/SystemsManagementScreen.tsx</files>
  <read_first>
    - Each file's outermost root View (grep "backgroundColor: colors.background" per file; open and confirm it is the outermost container). This is a uniform mechanical sweep — the same one-line removal the prior two tasks performed, across the config/flow screens.
    - 31.1-UI-SPEC.md:231-292 (all of these are dense).
    - CLAUDE.md "Review the code, not the diff": for the backup/restore screens especially, confirm the removed fill is the screen root and not a results/preview surface.
  </read_first>
  <action>
    Before editing, re-run `grep -rl "backgroundColor: colors.background" src/screens/*.tsx
    src/screens/**/*.tsx` and reconcile against this list; flip exactly the intersection MINUS HomeScreen,
    SettingsScreen, and OrreryScreen (already owned / excluded). For each file remove ONLY the outermost
    root's inline { backgroundColor: colors.background } (transparent-by-default; no 'transparent'
    literal). Leave every nested surface/card/section/result fill, modal, sheet, snackbar, and
    loading/error overlay opaque (D-11). All resolve to dense via densityForRoute — do NOT add per-screen
    density. No layout or behaviour change. Do NOT touch OrreryScreen (D-05) or ContactProfileScreen.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/BackupScreen.tsx src/screens/BackupSettingsScreen.tsx src/screens/RestorePreviewScreen.tsx src/screens/RestoreResultScreen.tsx src/screens/CustomFieldsScreen.tsx src/screens/LegacyContactPickerScreen.tsx src/screens/ImportReviewScreen.tsx src/screens/BulkImportSetupScreen.tsx src/screens/ImportProgressScreen.tsx src/screens/DuplicateReviewScreen.tsx src/screens/ImportCompleteScreen.tsx src/screens/BulkReviewScreen.tsx src/screens/ReconcileDetailScreen.tsx src/screens/ReconcileGridScreen.tsx src/screens/ReconcileCompleteScreen.tsx src/screens/SurvivorSelectScreen.tsx src/screens/MergeConflictsScreen.tsx src/screens/SystemsManagementScreen.tsx</automated>
    <human-check>On the Pixel, a backup flow, an import flow, a reconcile flow, and a merge flow each show the background faintly behind a dense scrim with all controls AA-readable; no unreadable field; no opaque full-page wash (UAT 2).</human-check>
  </verify>
  <acceptance_criteria>
    - Each of the 18 files' outermost root no longer sets backgroundColor: colors.background.
    - OrreryScreen.tsx and ContactProfileScreen.tsx are NOT modified (git diff shows no change to either).
    - No 'transparent' literal introduced; `npm run check:colors <these files>` passes; `npx tsc --noEmit` passes.
    - Nested surfaces/results/sheets unchanged (git diff shows only outermost-root fills removed).
  </acceptance_criteria>
  <done>The 18 dense config/flow roots cooperate with the shell host, Orrery and Profile remain untouched, and the work is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen root transparency → readability of content above the shell scrim | Removing an opaque root must not let the background wash out text; readability is provided by the shell density scrim + preserved opaque surfaces. |
| (integrity) colour-token invariant | Any transparency must not introduce a colour literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-04 | Tampering (theme invariant) | colour literal smuggled in while flipping roots | low | mitigate | Transparency via OMITTING backgroundColor (no literal); `npm run check:colors` over every touched file in verify. |
| T-31.1-05 | Denial of service / Availability | contrast regression hiding content on a cooperating screen | medium | mitigate | Density scrim preserved (shell host densityForRoute); opaque semantic surfaces untouched; Plan 04 Pixel UAT rows 1-3 are the acceptance proof; readability wins (any row may step denser). |
| T-31.1-06 | Tampering | accidentally flipping OrreryScreen root → System-image bleed into the excluded scene | medium | mitigate | Explicit exclusion of OrreryScreen.tsx + ContactProfileScreen.tsx; acceptance criteria assert git diff shows neither changed. |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run check:colors` clean over all 31 touched files.
- `git diff --name-only` shows exactly the 31 cooperating files changed; OrreryScreen.tsx and ContactProfileScreen.tsx NOT present.
- Per file: only the outermost root's opaque fill removed; nested surfaces intact.
- Device reveal + readability across tiers proven in Plan 04 (UAT 1-3, 5).
</verification>

<success_criteria>
Every ordinary in-app screen reveals the fixed System background behind its content with a density-
appropriate scrim, opaque semantic surfaces are preserved, and the Orrery and Profile exceptions are
untouched — completing THEME-04 across the real screen tree.
</success_criteria>

<output>
Create `.planning/phases/31.1-app-wide-system-backgrounds/31.1-03-SUMMARY.md` when done.
</output>
