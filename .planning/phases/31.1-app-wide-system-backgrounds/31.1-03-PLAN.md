---
phase: 31.1-app-wide-system-backgrounds
plan: 03
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/screens/ArchivedContactsScreen.tsx
  - src/screens/UnboundContactsScreen.tsx
  - src/screens/RecentlyDeletedScreen.tsx
  - src/screens/GroupEventsScreen.tsx
  - src/screens/ThingsToRememberScreen.tsx
  - src/screens/MemoryHistoryScreen.tsx
  - src/screens/DigestScreen.tsx
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/screens/ComposeScreen.tsx
  - src/screens/CaptureScreen.tsx
  - src/screens/CropPhotoScreen.tsx
  - src/screens/placeholders/FabActionPlaceholders.tsx
  - src/screens/BackupScreen.tsx
  - src/screens/BackupSettingsScreen.tsx
  - src/screens/RestorePreviewScreen.tsx
  - src/screens/RestoreResultScreen.tsx
  - src/screens/CustomFieldsScreen.tsx
  - src/screens/LegacyContactPickerScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportProgressScreen.tsx
  - src/screens/DuplicateReviewScreen.tsx
  - src/screens/ImportCompleteScreen.tsx
  - src/screens/BulkReviewScreen.tsx
  - src/screens/ReconcileDetailScreen.tsx
  - src/screens/ReconcileGridScreen.tsx
  - src/screens/ReconcileCompleteScreen.tsx
  - src/screens/SurvivorSelectScreen.tsx
  - src/screens/MergeConflictsScreen.tsx
  - src/screens/SystemsManagementScreen.tsx
  - src/components/MergeImpactSummary.tsx
  - src/components/FieldDefForm.tsx
autonomous: true
requirements: [THEME-04, PROF-01]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every ordinary in-app screen that today paints an opaque colors.background FULL-PAGE WASH is made to cooperate: EVERY full-page wash root/layer is flipped transparent so the single shell BackgroundHost shows through (D-04/D-11/D-18; THEME-04)."
    - "Flip mechanics are branch- and layer-aware, NOT a single per-file 'remove the outermost root' rule (addresses review HIGH): a screen with (a) multiple stacked full-bleed layers (e.g. ArchivedContacts root View + inner full-bleed ScrollView) or (b) separate loading/error/success state-branch roots (e.g. EditContact, Capture, ImportComplete, MergeConflicts, ReconcileDetail, ReconcileComplete, SurvivorSelect, RestorePreview) has ALL of those wash roots/layers flipped — leaving any one opaque hides the art on that state/layer."
    - "Discrete opaque surfaces that legitimately use colors.background are PRESERVED: TextInputs (e.g. MemoryHistory:155/181, BulkReview:182, SystemsManagement:269, Backup passphrase:313), buttons/chips (Compose:524/952), cards, GlassSurface, sheets, dialogs, snackbars, and loading/error OVERLAY surfaces keep their tokens — the background shows through the GAPS between surfaces, never through the surfaces (D-11)."
    - "MergeImpactSummary (src/components/MergeImpactSummary.tsx:21) is a full-page opaque flex:1 root on an ORDINARY route (registered in Dashboard/Orrery/Settings stacks) and IS flipped — the prior 'already cooperates' claim was factually false (addresses review HIGH #3)."
    - "FieldDefForm (src/components/FieldDefForm.tsx:190) is the SECOND src/components full-page wash: CustomFieldsScreen returns <FieldDefForm> as the ENTIRE screen in both its create (CustomFieldsScreen.tsx:317-325) and edit (:328-334) branches, and FieldDefForm roots on an opaque full-screen <ScrollView style={{ backgroundColor: colors.background }}> reached via the ordinary dense CustomFields route (SettingsStack.tsx:44) — so its ScrollView root IS flipped (its TextInput fills and the two foreground colors.background text colours at :244/:398 are KEEP). This completes the review HIGH #3 fix; the cycle-1 widen-to-src/components was verified INCOMPLETE by cycle-2 codex."
    - "SystemBuilderScreen is a SANCTIONED EXCEPTION per D-23: its opaque Skia <Fill color={colors.background}/> authoring canvas (SystemBuilderScreen.tsx:209) STAYS — it is NOT flipped and NOT in files_modified, recorded as a bounded exception exactly like the Orrery."
    - "The Orrery is NOT flipped — OrreryScreen's opaque root is left untouched so it occludes the shell host under every camera state (D-05); ContactProfileScreen is NOT flipped — it already roots in its own BackgroundHost (exception 1)."
    - "Presentation/browse screens (Archived, Unbound, RecentlyDeleted, GroupEvents, ThingsToRemember, MemoryHistory) reveal the art with the light presentation scrim; Digest uses the comfortable middle tier; dense forms/config/flows keep the more-opaque dense scrim — all driven by the shell host's densityForRoute, not per-screen density logic (D-19 A)."
    - "Transparency is achieved by OMITTING backgroundColor (transparent-by-default), never by writing the 'transparent' literal — check:colors stays green (Pitfall 1)."
    - "Existing Profile Hero / layout and all non-background screen behaviour are unchanged — this plan removes only full-page wash fills, no structural or behavioural change (PROF-01 preserved; D-15)."
  artifacts:
    - "A wash-vs-surface classification MANIFEST recorded in 31.1-03-SUMMARY.md: every colors.background occurrence across src/screens/** AND src/components/**, each classified wash (flip) or discrete surface (keep), with the exact line(s) flipped per file (addresses review HIGH — prevents a wrong-line removal that tsc/check:colors cannot catch)."
    - "33 cooperating files with their full-page wash roots/layers flipped transparent (30 screen files + src/screens/placeholders/FabActionPlaceholders.tsx + src/components/MergeImpactSummary.tsx + src/components/FieldDefForm.tsx); SystemBuilderScreen deliberately excluded (D-23)."
  key_links:
    - "Each flipped wash root/layer → the single shell BackgroundHost (Plan 01) renders behind it; the focused-route density scrim (densityForRoute) keeps content readable."
  flagged_assumptions:
    - "A4 (DENSITY): DigestScreen = comfortable per the UI-SPEC; if device-UAT row 3 (Plan 04) shows any contrast miss, step it to dense (readability wins, D-04) — never looser."
    - "The cooperating set is derived from a re-run grep at execution time (BOTH `backgroundColor: colors.background` and Skia `color={colors.background}` across src/screens/** AND src/components/**) classified by Task 1's manifest, MINUS HomeScreen (Plan 01), SettingsScreen (Plan 02), OrreryScreen (D-05 exclusion), SystemBuilderScreen (D-23 exception), the Skia <Fill> forms at CropPhoto:338 + SystemPreviewCanvas:134 (Skia backdrops, keep), ContactProfileScreen (own host), and all discrete non-wash surfaces. BOTH src/components washes — MergeImpactSummary AND FieldDefForm (whole-screen wash via CustomFields create/edit) — ARE in the set; SystemBuilder is NOT."
---

<objective>
Horizontal expansion of the tracer: make every remaining ordinary screen's opaque FULL-PAGE WASH cooperate
with the single shell BackgroundHost (D-11/D-18) by flipping every wash root/layer transparent, so the
fixed System background shows through behind all regular in-app pages — while the Orrery stays excluded
(D-05), SystemBuilder stays a sanctioned authoring-canvas exception (D-23), Profile keeps its own host,
and every discrete opaque semantic surface (cards, inputs, sheets, dialogs, loading/error overlays) is
preserved.

Purpose: Complete THEME-04's "behind every ordinary page" requirement across the real screen tree, with
readability provided by the shell host's focused-route density scrim rather than opaque page washes. This
plan corrects two cross-AI review HIGHs: (1) the naive "remove only THE outermost root" mechanic that
mishandles multi-layer and multi-state screens, and (2) the factually false inventory that excluded the
opaque-rooted MergeImpactSummary and could not even discover it (src/screens-only grep).

Output: a wash-vs-surface classification manifest recorded in the SUMMARY, plus 33 cooperating files with
their full-page wash roots/layers flipped transparent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-RESEARCH.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-UI-SPEC.md

Depends on 31.1-01: the single shell BackgroundHost mount must exist (Wave 1) before these roots are
flipped, so a flipped root reveals the shell background rather than the raw window.

Project non-negotiables (CLAUDE.md): "Review the code, not the diff" — for EACH file, open it and confirm
which colors.background occurrences are FULL-PAGE WASHES (flip) vs DISCRETE SURFACES (keep); a single file
may have several of each. All colours via theme tokens (achieve transparency by OMITTING the fill — NEVER
write 'transparent', Pitfall 1/check:colors); local-first; never push, no worktrees; do NOT touch
OrreryScreen (D-05), SystemBuilderScreen (D-23), or ContactProfileScreen (own host); do NOT reverse
D-01..D-23.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate + classify every colors.background occurrence (wash vs surface) and record the flip manifest</name>
  <files>.planning/phases/31.1-app-wide-system-backgrounds/31.1-03-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md "Review the code, not the diff" — this task exists BECAUSE tsc + check:colors cannot catch a wrong-line removal among a file's multiple colors.background occurrences; the manifest is the guard.
    - 31.1-UI-SPEC.md:231-292 (D-19 A density table — the authoritative per-route density tiers and the enumerated screen set).
    - 31.1-CONTEXT.md:153-174 (D-22 route-based Orrery exclusion; D-23 SystemBuilder sanctioned authoring-canvas exception).
    - src/screens/ArchivedContactsScreen.tsx:181-190 (the reference MULTI-LAYER case: root View flex:1 at :184 AND an inner full-bleed ScrollView at :188 — BOTH wash, BOTH flip).
    - src/components/MergeImpactSummary.tsx:21 (an opaque flex:1 styles.root on an ordinary route — a WASH that lives OUTSIDE src/screens, undiscoverable by a src/screens-only grep; its nested styles.row uses colors.surface = discrete KEEP, and its CTA <Text style={{ color: colors.background }}> is a FOREGROUND colour, not a fill — leave it).
    - src/screens/CustomFieldsScreen.tsx:314-336 (the create/edit branch returns <FieldDefForm> as the WHOLE screen at :317-325 and :328-334; the list branch's own ScrollView root at :341 is flipped in this task too since CustomFieldsScreen is in files_modified) AND src/components/FieldDefForm.tsx:187-190 (the SECOND src/components wash: an opaque full-screen <ScrollView style={{ backgroundColor: colors.background }}> at :190 reached via the ordinary dense CustomFields route — a WASH, flip its ScrollView root ONLY; its TextInputs are discrete KEEP, and the foreground colors.background text colours at :244/:398 are FOREGROUND not fills, KEEP). FieldValueInput references FieldDefForm only in a comment (no render), so flipping FieldDefForm's root affects ONLY the CustomFields create/edit path. This is the review HIGH #3 completeness gap (cycle-2 codex; verified on disk) — the cycle-1 widen-to-src/components did NOT actually enumerate this file.
    - src/screens/SystemBuilderScreen.tsx:198-219 (D-23 exception: the BuilderBackgroundCanvas Skia <Fill color={colors.background}/> at :209 — an authoring canvas, KEEP; the in-file comment at :198 already documents it as "its own decorative Orrery canvas, never the live screen").
    - src/screens/CropPhotoScreen.tsx:322,338 (the root View wash at :322 is flipped in Task 3; the Skia <Fill color={colors.background}/> at :338 is the crop-viewport backdrop under the user's opaque photo — a Skia EXCEPTION, KEEP, and a `backgroundColor:` removal cannot touch a Skia <Fill> anyway) and src/components/orrery/SystemPreviewCanvas.tsx:134 (a Skia <Fill color={colors.background}/> inside an embedded preview canvas — a Skia EXCEPTION, KEEP) — the two Skia-fill forms review LOW #5 asks Task 1 to enumerate as EXCEPTIONS.
  </read_first>
  <action>
    Run BOTH `grep -rn "backgroundColor: colors.background" src/screens src/components` AND
    `grep -rn "color={colors.background}" src/screens src/components` to enumerate every occurrence —
    the StyleSheet fill form AND the Skia `<Fill color={colors.background}/>` form — across BOTH directories
    (addresses review HIGH #3, the prior sweep was src/screens-only and missed src/components; and review
    LOW #5, the prior second grep was SystemBuilder-only and missed the Skia fills at CropPhotoScreen.tsx:338
    and src/components/orrery/SystemPreviewCanvas.tsx:134).

    For each occurrence, open the file and CLASSIFY it (do not rely on line numbers alone — they drift):
    - WASH (flip): the fill is on a full-page root View/ScrollView/Canvas that spans the whole screen area
      of a render branch. Includes EVERY separate state-branch root (loading/error/success) and EVERY
      stacked full-bleed layer within one branch.
    - SURFACE (keep): the fill is on a discrete element — a TextInput, button, chip, card, GlassSurface,
      sheet, dialog, snackbar, scrim/overlay, or the BackgroundHost's own None/Solid solid fill.
    - EXCLUDED (keep, recorded as a bounded exception): OrreryScreen root (D-05), SystemBuilderScreen Skia
      <Fill> at :209 (D-23), the Skia <Fill> forms at CropPhotoScreen.tsx:338 and
      src/components/orrery/SystemPreviewCanvas.tsx:134 (Skia backdrops, not StyleSheet washes — KEEP),
      ContactProfileScreen (own host), plus HomeScreen (Plan 01) and SettingsScreen root (Plan 02) which
      other plans own. src/components discrete surfaces (editors, menus, sheets, scrims, Profile managers,
      dev/benchmark harnesses) are SURFACE/keep — with the TWO verified EXCEPTIONS below.
    - CRITICAL src/components carve-out (do NOT over-generalize "component editors are discrete" — that is
      the exact cycle-1→cycle-2 miss): a component is a WASH when a screen returns it as its ENTIRE render
      output on an ordinary route. TWO such components exist and BOTH flip: MergeImpactSummary.tsx:21 (its
      styles.root) and FieldDefForm.tsx:190 (its ScrollView root, returned whole-screen by
      CustomFieldsScreen create/edit). All other src/components colors.background fills (FuelEditor,
      MemoryEditor, ContactMethodsEditor, RelationshipEditor, overlays/prompts/sheets/menus, Profile
      managers, BackgroundHost's own None/Solid solid at :161, benchmark harnesses) are embedded discrete
      surfaces = KEEP.

    Record the manifest in 31.1-03-SUMMARY.md as a table: file | occurrence line(s) | classification |
    flip? | note. It MUST call out, per file, exactly which line(s) get the backgroundColor removed and
    which stay. Enumerate the ACTUAL src/components full-page wash set (verified on disk: MergeImpactSummary.tsx:21
    + FieldDefForm.tsx:190 — do NOT hardcode a count that drifts) and confirm the total wash set equals the
    files_modified list (33 files). Record the Skia-<Fill> occurrences (SystemBuilderScreen:209 D-23,
    CropPhotoScreen:338, SystemPreviewCanvas:134) as EXCEPTIONS. If either grep surfaces a NEW full-page
    wash not in files_modified, STOP and surface it (do not silently flip an undeclared file).
  </action>
  <verify>
    <automated>grep -rn "backgroundColor: colors.background" src/screens src/components; grep -rn "color={colors.background}" src/screens src/components</automated>
    <human-check>31.1-03-SUMMARY.md contains the wash-vs-surface manifest; every occurrence from BOTH greps (StyleSheet fills AND Skia <Fill> forms) is classified; each multi-occurrence file lists exactly which line(s) flip and which stay; the enumerated src/components wash set is exactly {MergeImpactSummary.tsx:21, FieldDefForm.tsx:190}; SystemBuilder:209, CropPhoto:338, and SystemPreviewCanvas:134 are recorded as Skia EXCEPTIONS (keep); the total wash set equals the 33-file files_modified list.</human-check>
  </verify>
  <acceptance_criteria>
    - The manifest in 31.1-03-SUMMARY.md classifies every colors.background occurrence from BOTH grep forms (backgroundColor + Skia color={...}) across src/screens/** and src/components/**.
    - Multi-occurrence files (ArchivedContacts, EditContact, Capture, ImportComplete, MergeConflicts, ReconcileDetail, ReconcileComplete, SurvivorSelect, RestorePreview, ComposeScreen, MemoryHistory, BulkReview, SystemsManagement, BackupScreen, FieldDefForm) each list which line(s) are WASH (flip) vs SURFACE/FOREGROUND (keep).
    - MergeImpactSummary.tsx:21 AND FieldDefForm.tsx:190 are classified WASH (flip) — the src/components wash set is exactly those two, enumerated (not a hardcoded count). SystemBuilderScreen Skia Fill (:209), CropPhotoScreen Skia Fill (:338), and SystemPreviewCanvas Skia Fill (:134) are classified Skia EXCEPTION (keep); OrreryScreen + ContactProfileScreen are EXCLUDED.
    - The total wash set equals the 33-file files_modified list; any undeclared new wash surfaced by either grep is stopped-on, not silently flipped.
  </acceptance_criteria>
  <done>The wash-vs-surface flip manifest is recorded in the SUMMARY, guarding Tasks 2/3 against wrong-line removals; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip presentation / browse full-page wash roots transparent (7 components, incl. the multi-layer ArchivedContacts)</name>
  <files>src/screens/ArchivedContactsScreen.tsx, src/screens/UnboundContactsScreen.tsx, src/screens/RecentlyDeletedScreen.tsx, src/screens/GroupEventsScreen.tsx, src/screens/ThingsToRememberScreen.tsx, src/screens/MemoryHistoryScreen.tsx, src/screens/DigestScreen.tsx</files>
  <read_first>
    - Task 1's manifest in 31.1-03-SUMMARY.md — the authoritative per-file flip list for this task.
    - src/screens/HomeScreen.tsx:1560-1564 as executed in Plan 01 (the reference single-root flip: remove the root's { backgroundColor: colors.background }, leaving styles.root intact).
    - src/screens/ArchivedContactsScreen.tsx:181-190 — the MULTI-LAYER case: flip BOTH the root View at :184 AND the inner full-bleed ScrollView at :188 (removing only :184 leaves :188 opaque and hides the art).
    - src/screens/MemoryHistoryScreen.tsx:122,155,181 — flip ONLY the root View at :122; KEEP the two TextInput fills at :155 and :181 (discrete inputs).
    - 31.1-UI-SPEC.md:231-292 (these are presentation, except Digest=comfortable).
    - src/components/ui/BackgroundHost.tsx:127-167 (how the density scrim composites over the background — confirms readability is provided by the shell scrim, not the screen root).
  </read_first>
  <action>
    Guided by Task 1's manifest, for each of the 7 components remove the inline { backgroundColor:
    colors.background } from EVERY full-page WASH root/layer (transparent-by-default; do NOT write the
    'transparent' literal), leaving the remaining style (e.g. styles.root) intact. Specifically:
    ArchivedContacts — flip BOTH the outer root View AND the inner full-bleed ScrollView. MemoryHistory —
    flip the root View ONLY; keep the two TextInput fills. The other five (Unbound, RecentlyDeleted,
    GroupEvents, ThingsToRemember, Digest) are single-root flips. Do NOT alter any nested surface/card/
    list-row/GlassSurface/section/input fill — those discrete surfaces stay (D-11). Do NOT change layout,
    props, or behaviour. Density is supplied by the shell host via densityForRoute (these resolve to
    presentation; Digest to comfortable) — do NOT add a per-screen density or scrim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/ArchivedContactsScreen.tsx src/screens/UnboundContactsScreen.tsx src/screens/RecentlyDeletedScreen.tsx src/screens/GroupEventsScreen.tsx src/screens/ThingsToRememberScreen.tsx src/screens/MemoryHistoryScreen.tsx src/screens/DigestScreen.tsx</automated>
    <human-check>On the Pixel, each of these screens reveals the System background behind content with a light/comfortable scrim; content stays legible; no opaque full-page wash on ANY layer (ArchivedContacts reveals through the list scroll area too) (UAT 1,3).</human-check>
  </verify>
  <acceptance_criteria>
    - Every full-page wash root/layer in each of the 7 files no longer sets backgroundColor: colors.background (ArchivedContacts: both :184 and :188; others per manifest).
    - MemoryHistory's two TextInput fills (:155,:181) are UNCHANGED (discrete surfaces preserved).
    - No 'transparent' literal introduced; `npm run check:colors <these files>` passes; `npx tsc --noEmit` passes.
    - Nested surface fills in each file are unchanged (git diff shows only wash-root/layer fills removed).
  </acceptance_criteria>
  <done>The 7 presentation/browse components (including the multi-layer ArchivedContacts) reveal the shell background with no opaque wash on any layer; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip dense full-page wash roots transparent (26 files incl. MergeImpactSummary + FieldDefForm + every state-branch root; keep discrete surfaces; SystemBuilder excluded per D-23)</name>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/ComposeScreen.tsx, src/screens/CaptureScreen.tsx, src/screens/CropPhotoScreen.tsx, src/screens/placeholders/FabActionPlaceholders.tsx, src/screens/BackupScreen.tsx, src/screens/BackupSettingsScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/CustomFieldsScreen.tsx, src/screens/LegacyContactPickerScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/BulkImportSetupScreen.tsx, src/screens/ImportProgressScreen.tsx, src/screens/DuplicateReviewScreen.tsx, src/screens/ImportCompleteScreen.tsx, src/screens/BulkReviewScreen.tsx, src/screens/ReconcileDetailScreen.tsx, src/screens/ReconcileGridScreen.tsx, src/screens/ReconcileCompleteScreen.tsx, src/screens/SurvivorSelectScreen.tsx, src/screens/MergeConflictsScreen.tsx, src/screens/SystemsManagementScreen.tsx, src/components/MergeImpactSummary.tsx, src/components/FieldDefForm.tsx</files>
  <read_first>
    - Task 1's manifest in 31.1-03-SUMMARY.md — the authoritative per-file, per-line flip list for this task.
    - The MULTI-STATE cases (flip EVERY state-branch root): EditContactScreen (loading :551 + success :569); CaptureScreen (errorRoot :550 + success :746); ImportCompleteScreen (:110 + :120 + :134); MergeConflictsScreen (:161 + :162 + :169); ReconcileDetailScreen (:142 + :143 + :144); ReconcileCompleteScreen (:37 + :38); SurvivorSelectScreen (:38 + :39); RestorePreviewScreen (:168 + :181); ComposeScreen (roots :780 + :806).
    - The DISCRETE-SURFACE-KEEP cases: ComposeScreen aiTrigger :524 + actionBtn :952 (buttons, keep); BulkReviewScreen birthday input :182 (keep); SystemsManagementScreen renameInput :269 (keep); BackupScreen passphrase input :313 (keep). Flip ONLY those files' true page roots (Compose :780/:806, BulkReview :110, SystemsManagement :553, Backup :289).
    - src/components/MergeImpactSummary.tsx:21 (flip the styles.root View's { backgroundColor: colors.background }; KEEP the nested styles.row colors.surface fill and the CTA text colour).
    - src/components/FieldDefForm.tsx:187-190 (flip ONLY the full-screen ScrollView root's { backgroundColor: colors.background } at :190; KEEP every TextInput fill and the two FOREGROUND colours.background text colours at :244/:398 — they are text colour, not fills; addresses review HIGH #3 completion). It is a WASH because CustomFieldsScreen returns <FieldDefForm> as the whole screen in create/edit; FieldValueInput only mentions it in a comment, so this flip affects only the CustomFields create/edit path.
    - src/screens/SystemBuilderScreen.tsx:209 — D-23 EXCEPTION: DO NOT flip the Skia <Fill color={colors.background}/>; it is intentionally NOT in this task's files.
    - src/screens/CropPhotoScreen.tsx:322,338 (flip ONLY the root View StyleSheet wash at :322 — the crop viewport fills the screen with the user's photo and does NOT reveal art, but flip for consistency; the dense scrim + opaque crop content handle readability. The Skia <Fill color={colors.background}/> at :338 is a Skia EXCEPTION — KEEP it, and a `backgroundColor:` removal cannot touch a Skia <Fill> anyway).
    - CLAUDE.md "Review the code, not the diff": for the backup/restore/reconcile/merge screens especially, confirm each removed fill is a full-page wash root of a render branch, not a results/preview surface.
  </read_first>
  <action>
    Guided by Task 1's manifest, for each of the 26 files remove the inline { backgroundColor:
    colors.background } from EVERY full-page WASH root/layer (transparent-by-default; no 'transparent'
    literal). Flip ALL state-branch roots on the multi-state screens (listed above) — leaving any one
    opaque hides the art on that state. KEEP every discrete surface (the buttons/inputs listed above, all
    cards/sheets/dialogs/snackbars, and every loading/error OVERLAY surface) — D-11. For MergeImpactSummary
    flip only the styles.root View (addresses review HIGH #3). For FieldDefForm flip ONLY the full-screen
    ScrollView root at :190 — KEEP its TextInput fills and the two FOREGROUND colours.background text colours
    at :244/:398 (this completes review HIGH #3: FieldDefForm is the whole screen in CustomFields create/edit,
    so its list-view sibling in CustomFieldsScreen is not enough on its own). For FabActionPlaceholders flip
    the single shared FabActionPlaceholder root (covers LogContact/GroupLog/UpdateContact/Memory). Do NOT flip
    SystemBuilderScreen (D-23), OrreryScreen (D-05), or ContactProfileScreen (own host); do NOT touch any
    Skia <Fill> (CropPhoto:338, SystemPreviewCanvas:134 — Skia EXCEPTIONS). All these routes resolve to dense
    via densityForRoute — do NOT add per-screen density. No layout or behaviour change.
    CONTEXT-BUDGET NOTE (plan-checker scope warning, non-blocking): this task edits 26 files as uniform
    one-line wash removals, above the usual per-plan file threshold. The edits are homogeneous and Task 1's
    manifest guards each one, so a single pass is expected to succeed. If the executor's context runs hot
    mid-task, commit in TWO batches along the manifest boundary — batch A: import/reconcile/merge flows
    (Import*, Duplicate/BulkReview, Reconcile*, SurvivorSelect, MergeConflicts, MergeImpactSummary); batch B:
    backup/config/create-edit flows (Backup*, Restore*, CustomFields, FieldDefForm, LegacyContactPicker,
    Create/Edit/Compose/Capture/CropPhoto, FabActionPlaceholders, SystemsManagement). This is a
    commit-batching split only — it does NOT change the flip set, the manifest, or the plan dependency graph.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/screens/CreateContactScreen.tsx src/screens/EditContactScreen.tsx src/screens/ComposeScreen.tsx src/screens/CaptureScreen.tsx src/screens/CropPhotoScreen.tsx src/screens/placeholders/FabActionPlaceholders.tsx src/screens/BackupScreen.tsx src/screens/BackupSettingsScreen.tsx src/screens/RestorePreviewScreen.tsx src/screens/RestoreResultScreen.tsx src/screens/CustomFieldsScreen.tsx src/screens/LegacyContactPickerScreen.tsx src/screens/ImportReviewScreen.tsx src/screens/BulkImportSetupScreen.tsx src/screens/ImportProgressScreen.tsx src/screens/DuplicateReviewScreen.tsx src/screens/ImportCompleteScreen.tsx src/screens/BulkReviewScreen.tsx src/screens/ReconcileDetailScreen.tsx src/screens/ReconcileGridScreen.tsx src/screens/ReconcileCompleteScreen.tsx src/screens/SurvivorSelectScreen.tsx src/screens/MergeConflictsScreen.tsx src/screens/SystemsManagementScreen.tsx src/components/MergeImpactSummary.tsx src/components/FieldDefForm.tsx</automated>
    <human-check>On the Pixel, a backup flow, an import flow, a reconcile flow, and a merge flow (including the MergeImpactSummary screen) each show the background faintly behind a dense scrim with all controls AA-readable across loading/empty/loaded/error states; no unreadable field; no opaque full-page wash on any state-branch; SystemBuilder's authoring canvas stays opaque (D-23) (UAT 2,6).</human-check>
  </verify>
  <acceptance_criteria>
    - Every full-page wash root/layer across the 26 files (all state-branch roots per the manifest) no longer sets backgroundColor: colors.background; MergeImpactSummary's styles.root AND FieldDefForm's ScrollView root (:190) are flipped.
    - The discrete-surface / foreground KEEP set is unchanged: ComposeScreen :524/:952, BulkReview :182, SystemsManagement :269, BackupScreen :313, FieldDefForm's TextInput fills + its foreground text colours at :244/:398 (git diff shows these lines untouched).
    - SystemBuilderScreen.tsx, OrreryScreen.tsx, and ContactProfileScreen.tsx are NOT modified (git diff shows no change to any of the three); no Skia <Fill> (CropPhoto:338, SystemPreviewCanvas:134) is touched.
    - No 'transparent' literal introduced; `npm run check:colors <these files>` passes; `npx tsc --noEmit` passes.
    - Reveal outcome (device, Plan 04): each cooperating screen reveals the background with no opaque full-page wash across loading/empty/loaded/error states; discrete surfaces stay opaque.
  </acceptance_criteria>
  <done>The 26 dense components (incl. MergeImpactSummary + FieldDefForm and every state-branch root) cooperate with the shell host, discrete surfaces are preserved, SystemBuilder/Orrery/Profile remain untouched, and the work is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen root transparency → readability of content above the shell scrim | Removing an opaque root must not let the background wash out text; readability is provided by the shell density scrim + preserved discrete surfaces. |
| (integrity) colour-token invariant | Any transparency must not introduce a colour literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-04 | Tampering (theme invariant) | colour literal smuggled in while flipping roots | low | mitigate | Transparency via OMITTING backgroundColor (no literal); `npm run check:colors` over every touched file in verify. |
| T-31.1-05 | Denial of service / Availability | contrast regression hiding content on a cooperating screen | medium | mitigate | Density scrim preserved (shell host densityForRoute); discrete surfaces untouched; Plan 04 Pixel UAT rows 1-3 are the acceptance proof; readability wins (any row may step denser). |
| T-31.1-06 | Tampering | accidentally flipping OrreryScreen (D-05) or SystemBuilderScreen (D-23) → System-image bleed into an excluded canvas | medium | mitigate | Explicit exclusion of OrreryScreen.tsx + SystemBuilderScreen.tsx + ContactProfileScreen.tsx; Task 1 manifest records them as EXCEPTIONS; acceptance criteria assert git diff shows none of the three changed. |
| T-31.1-08 | Tampering | wrong-line removal among a file's multiple colors.background occurrences (tsc/check:colors cannot catch) | medium | mitigate | Task 1's wash-vs-surface manifest names the exact line(s) to flip vs keep per file; Tasks 2/3 execute against it; discrete-surface KEEP set asserted in acceptance. |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run check:colors` clean over all 33 touched files.
- `git diff --name-only` shows exactly the 33 cooperating files changed; OrreryScreen.tsx, SystemBuilderScreen.tsx, and ContactProfileScreen.tsx are NOT present.
- Per file: every full-page wash root/layer flipped per Task 1's manifest; discrete surfaces intact.
- Device reveal + readability across tiers and across state-branches proven in Plan 04 (UAT 1-3, 5, 6).
</verification>

<success_criteria>
Every ordinary in-app screen reveals the fixed System background behind its content across all its render
states (loading/empty/loaded/error) with a density-appropriate scrim, discrete semantic surfaces are
preserved, MergeImpactSummary no longer washes out the art, and the Orrery (D-05) and SystemBuilder (D-23)
exceptions are untouched — completing THEME-04 across the real screen tree.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 — for source-grounding exclusion)

Newly-created artifacts (exclude from source-grounding "must-already-exist" checks):
- The wash-vs-surface classification MANIFEST recorded in `31.1-03-SUMMARY.md` (new content), enumerating the two src/components washes (MergeImpactSummary.tsx:21 + FieldDefForm.tsx:190) and the three Skia-<Fill> EXCEPTIONS (SystemBuilder:209, CropPhoto:338, SystemPreviewCanvas:134).
- Flipped full-page wash roots/layers across 33 files (removals of `backgroundColor: colors.background`) — no new symbols, files, packages, or exports; behaviour/layout unchanged.

No new files, packages, stores, columns, migrations, or backup keys. SystemBuilderScreen is deliberately
NOT modified (D-23).
</artifacts_produced>

<review_findings_addressed>
## Cross-AI review findings addressed (Plan 03)

- **HIGH #2 (multi-layer / multi-state flip mechanics).** Replaced the naive "remove only the outermost
  root" per-file rule with a branch- and layer-aware flip: ArchivedContacts flips both its root View and
  inner full-bleed ScrollView; every screen with separate loading/error/success roots (EditContact,
  Capture, ImportComplete, MergeConflicts, ReconcileDetail, ReconcileComplete, SurvivorSelect,
  RestorePreview) flips ALL of them. Acceptance retargeted from a single-root/diff-shape assertion to a
  REVEAL OUTCOME across loading/empty/loaded/error states.
- **HIGH #3 (false inventory + narrow grep) — cycle-1 fix + cycle-2 COMPLETION.** Cycle 1 deleted the
  false "MergeImpactSummary already cooperates" clause, added `src/components/MergeImpactSummary.tsx` to
  files_modified, and added Task 1 to enumerate + classify every occurrence. Cycle 2 (this pass) COMPLETES
  the widen-to-src/components that was verified INCOMPLETE by codex: `src/components/FieldDefForm.tsx` — the
  full-screen `<ScrollView>` wash CustomFieldsScreen returns as its entire create/edit render (:317-334) —
  is now in files_modified, Task 3 files, the Task 1 manifest, the check:colors verify, and acceptance; its
  ScrollView root (:190) is flipped while its inputs + foreground text colours (:244/:398) are KEEP. Task 1's
  false "exactly one src/components wash" assertion is replaced by an ENUMERATED wash set (MergeImpactSummary
  + FieldDefForm) so the count cannot drift; the cooperating file count corrected 32 → 33.
- **LOW #5 (Skia-fill enumeration + count-only verify).** Task 1's second grep is widened to
  `color={colors.background}` across src/screens AND src/components (was SystemBuilder-only), catching the
  Skia `<Fill>` forms at CropPhoto:338 and SystemPreviewCanvas:134; both are recorded as Skia EXCEPTIONS
  (keep) alongside SystemBuilder:209. The `wc -l` verify is replaced by running both greps for classification
  and asserting the enumerated wash/exception set in acceptance rather than a bare count.
- **LOW/nit #7 (Task 3 title count).** Task 3 `<name>`, `<action>`, the CONTEXT-BUDGET NOTE, and `<done>`
  now consistently say 26 files (was "24 components"/"25 files"), reflecting the FieldDefForm addition.
- **D-23 (SystemBuilder).** Recorded SystemBuilderScreen as a sanctioned authoring-canvas EXCEPTION — its
  opaque Skia <Fill> at :209 stays, it is NOT in files_modified, and acceptance asserts it is unchanged.
  The false-inventory fix reaches MergeImpactSummary + FieldDefForm (flipped), not SystemBuilder (excluded).
</review_findings_addressed>

<output>
Create `.planning/phases/31.1-app-wide-system-backgrounds/31.1-03-SUMMARY.md` when done (including the wash-vs-surface manifest).
</output>
