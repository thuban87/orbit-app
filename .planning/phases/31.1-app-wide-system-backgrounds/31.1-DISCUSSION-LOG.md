# Phase 31.1 — Discussion Log

**Date:** 2026-09-10
**Mode:** discuss (default)
**Human reference only** — not consumed by researcher/planner/executor.

The phase entered discussion with a fully-formed seed CONTEXT.md (boundary
D-01–D-16 locked) and five explicit `<discussion_targets>`. Grounding reads
before questioning: `BackgroundHost.tsx`, `backgrounds.ts`, `theme-option-ids.ts`,
`theme-store.ts`, the Appearance section of `SettingsScreen.tsx`, `ADR-087`, and
`RootNavigator.tsx`. Key confirmations: the background primitive already exists;
`theme-store` lacks a background setter (D-09 gap real); `resolveBackground()`
renders any slot regardless of package (D-02 all-8 feasible); the shell is a
four-tab navigator whose screens each paint an opaque themed root.

## Areas selected for discussion

Owner selected **all four** offered gray areas: Picker in Appearance, Shell
mounting strategy, Density + UAT matrix, Profile→System route.

## Decisions

| Area | Options presented | Selection | Recorded as |
|------|-------------------|-----------|-------------|
| Picker presentation | Inline grid grouped / inline flat-8 / bounded sheet / focused preview | **Inline grid, grouped by package** | D-17 |
| Shell mounting | Single shell-root / per-navigator / per-screen | **Single shell-root mount** | D-18 |
| Reveal philosophy | Presentation-forward / conservative / uniform medium | **Presentation-forward** | D-19 |
| Profile→System route | Small link/hint / no cross-navigation | **No cross-navigation** (owner override of recommendation) | D-20 |
| Selection transition | (not separately asked — minor) | Immediate switch, Reduced-Motion-gated if ever added | D-21 |

## Notes

- D-20 was the one place the owner overrode a stated recommendation (a small
  Profile→System link). The two background systems now stay fully independent in
  both storage (D-07) and navigation.
- Density per-screen assignment and the physical-Pixel device-UAT matrix were
  deliberately delegated to research/planning under the presentation-forward
  intent (D-19) rather than fixed here — they require enumerating the live screen
  tree.

## Scope creep / deferred

None raised during discussion. `<out_of_scope>` in CONTEXT.md remains authoritative.
