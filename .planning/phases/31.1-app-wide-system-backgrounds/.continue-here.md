---
context: phase
phase: 31.1-app-wide-system-backgrounds
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-09-11T08:16:23.085Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: The owner’s production release test disproves the recorded visual acceptance — selected System backgrounds are not visibly applying on regular screens. Do not mark Phase 31.1 complete or create another release until this is reproduced, corrected, and revalidated.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treating a technically mounted image as an accepted background | `BackgroundHost` mounts the image, but immediately covers it with a near-opaque full-screen `colors.surface` scrim. Galaxy permits only 12%/7%/3% art and Standard only 3%/1%/0% across presentation/comfortable/dense. The prior UAT called static screenshots a pass without proving that changing slot A to slot B visibly changes normal release screens. | blocking | Before accepting a fix, capture the same regular release route after at least two materially different selected assets in each package; verify the selection is visible while text remains readable. |
| Treating generated UAT status as stronger than owner release feedback | The previous DEBUG and release UAT records report PASS, but the owner installed the delivered APK on a personal phone and found the feature unusable. | blocking | The owner report is the acceptance authority here. Record it as a blocking regression and test the corrected release artifact on a fresh device/install. |
| Broad destructive cleanup during Android validation | The phase used only temporary UAT data/helpers and restored it, but production changes must still preserve app data and the owner’s existing dirty files. | advisory | Keep direct DB diagnostics temporary, snapshot/restore exact values, and never stage `tsconfig.json` or Phase 30 review edits. |

<current_state>

Phase 31.1’s four planned implementation/UAT plans are committed, but the phase is paused and NOT complete. The owner reports the actual release artifact shows the Settings background picker and accepts selections, yet other pages show no visible change. The most likely cause is source-visible composition, not a missing picker write:

- `src/screens/SettingsScreen.tsx` immediately calls `setBackgroundForActivePackage(slot)` and persists `galaxyBackground` or `standardBackground` to `app_settings`.
- `src/components/ui/BackgroundHost.tsx` reads that store selection, renders the asset, then covers the entire host with `colors.surface` at `surfaceOpacityForDensity(...)`.
- `src/theme/tokens/surface.ts` sets Galaxy at `0.88 / 0.93 / 0.97` and Standard at `0.97 / 0.99 / 1.0` for presentation / comfortable / dense. Standard dense is completely opaque; normal Standard routes can therefore never visibly reveal art, and Galaxy is nearly obscured before individual cards render.

This must be confirmed on a standalone release and corrected as a gap before re-running phase verification. Do not silently lower readability protections or change visual density policy without reading the canonical visual decisions and surfacing any owner-taste tradeoff.
</current_state>

<completed_work>

Completed implementation (committed):

- Plan 01: `BackgroundHost` mounted at the root shell, route density/override classification, transparent navigation scenes, and store setters (`b71081c` through `3ad2538`).
- Plan 02: grouped Settings picker and per-package durable background selection (`e009420`, `99076a8`, `5a8a987`).
- Plan 03: removed full-page root washes from cooperating browse/dense screens while retaining Orrery, System Builder, and Profile exceptions (`79719c2`, `8359b98`, `412db0b`, `f3b0785`).
- Plan 04: DEV-only decode-failure harness, Pixel debug UAT, release build/install/smoke, and Drive artifact delivery (`09ba594`, `4f47852`, `7a9289f`, `2bf96f6`, `5111597`, `f57bbb8`).

The release artifact recorded by Plan 04 was `app-release-31.1-04.apk`, SHA-256 `ebb5267d742e4c6224feb5af04f74b6d9a0f963f5086c34372401061a9aa9df9`, delivered to `G:\My Drive\IT\Software\Orbit\app-release-31.1-04.apk`. It is evidence of the reported failure, not a finished deliverable.
</completed_work>

<remaining_work>

- Reproduce the owner’s visible failure on a standalone release using deliberately distinct slots (for example Galaxy Deep Space vs Aurora; Standard Dawn vs Paper) on the same ordinary route.
- Resolve whether opacity composition alone explains it; also check that selected `app_settings` values survive the actual route change/relaunch.
- Design and implement a corrective gap that preserves text contrast, package-local persistence, fixed-behind-scroll behavior, no network reads, Orrery’s `none` override, and Profile-photo precedence.
- Add tests that fail if a package/density’s background contribution becomes visually zero or imperceptibly low; node logic alone cannot prove visual distinction.
- Re-run device DEBUG UAT and a fresh standalone release UAT with explicit before/after selected-slot evidence. Replace/supplement the prior false-positive UAT claim rather than hiding it.
- Run independent phase verification. The prior `gsd-verifier` attempt did not complete because the agent hit a service usage limit; it generated no verification artifact.
</remaining_work>

<decisions_made>

- The owner explicitly required release packaging only after debug testing. That ordering was eventually followed: debug UAT first, then release build/install/delivery. Preserve that sequencing for the corrective build.
- The phase now pauses instead of being closed because a personal-device release test is a production acceptance failure.
- No source fix has been made in response to this report. The opacity diagnosis is evidence-based but still needs a clean-release reproduction before selecting a visual remedy.
</decisions_made>

<blockers>

- **Blocking:** backgrounds are not visibly observable on the owner’s personal-phone release install despite usable picker selections.
- **Technical:** current `BackgroundHost` composition has a near-opaque host-wide scrim that plausibly suppresses every selection. This is a design/implementation defect, not grounds to dismiss the owner report.
- **Non-blocking test debt:** `npm test` has a pre-existing Phase 29 module-load failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`); 2,904 tests in 323 files passed during Plan 04. It was untouched by Phase 31.1 but should remain visible in later gates.
</blockers>

## Required Reading (in order)

1. `HANDOFF.md` — project-level decisions and non-negotiable data/visual constraints.
1. `.planning/phases/31.1-app-wide-system-backgrounds/.continue-here.md` — this failure and exact pause state.
1. `.planning/HANDOFF.json` — machine-readable resume state.
1. `.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md` and `31.1-UI-SPEC.md` — the approved product and visual contract.
1. `docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md` §§D–F, Q, R and `docs/decisions/ADR-087-bundled-background-presets-and-package-specific-surface-treatment.md` — governing visual/readability decisions.
1. `src/components/ui/BackgroundHost.tsx`, `src/theme/tokens/surface.ts`, `src/theme/backgrounds.ts`, `src/navigation/RootNavigator.tsx`, `src/navigation/focused-route-classification.ts`, `src/screens/SettingsScreen.tsx`, `src/stores/theme-store.ts`, and `src/db/app-settings-dao.ts` — actual rendering and persistence path.
1. `.planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md` and `31.1-04-SUMMARY.md` — prior claimed evidence; treat it as incomplete/contradicted until the owner-reported release failure is resolved.
1. `docs/runbooks/desktop-build-pipeline.md` — required release transport once the correction has passed DEBUG UAT.

## Infrastructure State

- Dedicated Metro remains running on this host in tmux `orbit:2`, port `8082`. The previous DEBUG-client reverse mappings were `tcp:8082 -> tcp:8082` and `tcp:8081 -> tcp:8082`.
- The physical Pixel release package was installed during the previous gate. Do not assume its selection/device data matches the owner’s personal phone; always inspect `adb devices` and derive the serial.
- `droid` is reachable for documented Android release builds. Never push, use worktrees, or rsync to the phone. The owner pushes.
- There are no async job manifests or active Gradle/release jobs.
- Preserve the only unrelated dirty files: `tsconfig.json`, `.planning/phases/30-orrery-systems/30-REVIEW.md`, and `.planning/phases/30-orrery-systems/30-REVIEW-FIX.md`.

<context>

The phase’s implementation looked structurally complete: selection writes through the store and DAO, root host is mounted, and release build smoke passed. That was not sufficient. The decisive signal is the owner’s personal-device observation that selecting backgrounds makes no perceptible difference. The architecture itself explains why: a full-screen opaque-to-near-opaque readability scrim suppresses selected artwork before content is drawn. Resume with the owner report as the ground truth, make an explicit visual-composition decision with the owner if needed, and validate a corrected release with direct selected-slot comparisons.
</context>

<next_action>

Start with: acknowledge the two blocking constraints above, reproduce the selection on a standalone release, then compare the actual rendered composition for two high-contrast slots on a normal presentation route before planning any change.
</next_action>
