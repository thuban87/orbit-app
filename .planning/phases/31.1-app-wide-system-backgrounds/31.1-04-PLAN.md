---
phase: 31.1-app-wide-system-backgrounds
plan: 04
type: execute
wave: 3
depends_on: ["31.1-01", "31.1-02", "31.1-03"]
files_modified:
  - .planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/types.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [THEME-03, THEME-04, PROF-01, PROF-04, PROF-05]
user_setup:
  - service: physical-pixel
    why: "D-16 requires the device-UAT matrix + final native gate on the physical Pixel 6 Pro; only the owner can plug in the phone and accept the on-device 'Allow USB debugging' prompt if it shows unauthorized."
    dashboard_config:
      - task: "Ensure the Pixel 6 Pro is connected and USB debugging is authorized (emu-connect resolves exactly one authorized device); Orbit debug dev client installed; Orbit Metro reachable on :8082."
        location: "Physical device (per desktop-build-pipeline / device-UAT runbooks)"
      - task: "Deliver the final standalone release APK to G:\\My Drive\\IT\\Software\\Orbit on droid for on-device install."
        location: "droid desktop (ssh droid build)"

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full vitest suite is green and `npm run check:colors` + `npx tsc --noEmit` are clean across the phase's changes (automated regression gate)."
    - "The D-19 B device-UAT matrix passes on the physical Pixel across Package × Mode × Text-size × Motion: presentation screens reveal art, dense forms stay AA-readable, Digest (comfortable) legible, background fixed-on-scroll, no opaque wash / intrinsic-size tile / untinted corner (THEME-04; UAT 1-5)."
    - "The Orrery VISUALIZATION shows ZERO System-image bleed under pan/pinch/tilt/yaw (D-05/D-22), AND the System Builder authoring canvas shows ZERO bleed (D-23) — but Systems Management is a dense FLIPPED screen and legitimately reveals faint scrimmed art (do NOT fail it); the OrreryStack browse children reveal art identically via either tab (D-22; UAT 6)."
    - "Picker shows all 8 + None in BOTH packages (not filtered); live preview + immediate persist; selecting in one package writes only that package's column with cross-package independence; survives relaunch with no wrong-theme/None flash (THEME-03; UAT 9-12)."
    - "None/Solid renders themed solid + scrim; an invalid stored id (run-as write) → package default with no crash/error UI; a runtime decode failure → silent None/Solid, latch resets on next valid selection, exercised via the __DEV__-only decode-failure harness reachable on the DEBUG dev client (the forceRenderError toggle is provably absent from the release APK) (D-12; THEME-04; UAT 13-15)."
    - "Large-text reflow: picker labels reflow/wrap, grid re-columns, targets ≥ 44px; Reduced Motion ON keeps the immediate switch with no animation (D-14/D-21; UAT 16-17)."
    - "A resolved app-owned Profile photo (contact → Category → global) overrides the System background; a no-photo Profile falls back to the active package's selected System background (PROF-04/PROF-05; D-06; UAT 7-8)."
    - "Existing Profile background template behaviour (create/crop/assign/delete/reset/app-owned storage/render-failure) is unchanged, and the fixed Profile Hero/layout is intact (PROF-01/PROF-04; D-07/D-15 regression guard)."
    - statement: "The final standalone RELEASE APK (not just the debug dev client) passes a smoke of the background feature on the physical Pixel (reveal, Orrery exclusion, Profile override + fallback, relaunch) — the D-16 native gate."
      verification: backstop
  artifacts:
    - ".planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md — the executed D-19 B matrix results (per-row pass/fail + screenshot references) and the native-gate result."
  key_links:
    - "Every prior plan's device-observable behaviour converges here as the binary acceptance proof for ROADMAP success criteria 1-5."
  flagged_assumptions:
    - "DEVICE-ONLY: perf claims (overdraw behind the Orrery) and true contrast are valid only on the physical Pixel, never the emulator (CLAUDE.md); dumpsys gfxinfo sees the UI/render thread only — state which thread any perf evidence covers."
    - "PROF-05 unclassified edge-probe resolved here: the photo-wins / System-fallback precedence is verified manually on-device at UAT rows 7-8 (rather than silently dismissed)."
---

<objective>
Close the phase with the acceptance proof: run the full automated regression, then execute the D-19 B
physical-Pixel device-UAT matrix (the acceptance source of truth) across every cross-cutting condition,
verify the two Profile precedence rules + the Profile-template regression guard, and pass the final
standalone release-APK native gate (D-16). Record the results in 31.1-UAT.md.

Purpose: Prove ROADMAP success criteria 1-5 on the real device — the app-wide visual composition,
readability, Orrery exclusion, Profile precedence, and local-only fallbacks are UI-observable-only and
cannot be asserted in vitest (CLAUDE.md: a screen rendering is not proof of an invariant).

Output: an executed, committed 31.1-UAT.md with per-row pass/fail + screenshots and the native-gate result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-CONTEXT.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-RESEARCH.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-UI-SPEC.md
@.planning/phases/31.1-app-wide-system-backgrounds/31.1-VALIDATION.md

Depends on 31.1-01/02/03: the setter, picker, shell mount, and all cooperating roots must be in place.

Project non-negotiables (CLAUDE.md + MEMORY): VERIFY UI ON THE PIXEL YOURSELF — drive the app via
emu-connect + adb + uiautomator (do NOT default UI flows to human_needed); resolve exactly ONE authorized
device (adb devices | awk '$2=="device"' | wc -l must print 1); never hardcode a serial; Orbit Metro on
:8082 (quest-board owns :8081 — wrong mapping redboxes "Cannot find native module ExpoWebBrowser"); adb
input source taxonomy matters (plain tap for chips/rows, touchscreen tap for FAB/gear/buttons); a missed
small-Pressable tap is a FALSE negative — verify against code first; never `apt install adb` (use
~/.local/bin/adb); emulator CANNOT judge Orrery perf or true contrast; never push, no worktrees.
</context>

<tasks>

<task type="auto">
  <name>Task 1: __DEV__ decode-failure harness + full regression + D-19 B functional device-UAT matrix (UAT 1-6, 9-17)</name>
  <files>src/navigation/tabs/SettingsStack.tsx, src/navigation/types.ts, src/screens/SettingsScreen.tsx, .planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md</files>
  <read_first>
    - src/components/ui/__dev__/ThemePreviewScreen.tsx:61-113 (the EXISTING dev-only screen; default export ThemePreviewScreen; it holds a `forceError` toggle that passes `forceRenderError={forceError}` into a BackgroundHost, exercising the identical onError → resolveRenderableBackground None/Solid latch the shell host uses) — it is NOT registered in any stack today (grep confirms), which is why UAT row 15 is unreachable (addresses review MEDIUM #5).
    - src/navigation/tabs/SettingsStack.tsx (the stack to register a __DEV__-gated harness route in) and src/navigation/types.ts (the SettingsStack param list to add the __DEV__ route name to).
    - src/screens/SettingsScreen.tsx (add a __DEV__-only row that navigates to the harness route; place it so it is dead-code-eliminated in release).
    - src/components/ui/BackgroundHost.tsx:69-73,104-125,152-167 (forceRenderError prop is DEV-only by contract; the latch resets when the selection key changes — the "latch resets on next valid selection" behaviour UAT row 15 asserts).
    - 31.1-UI-SPEC.md:329-364 (the D-19 B matrix: the cross-cutting Package × Mode × Text-size × Motion grid and the 17 numbered scenarios with their device steps + pass criteria; note the D-22/D-23-updated row 6).
    - 31.1-VALIDATION.md:73-83 (Manual-Only Verifications) and 55-61 (vitest-assertable layer — run those too).
    - 31.1-RESEARCH.md:623-637 (UI-observable-only list + sampling rate) and Pitfalls 2/3/7 (Android transparency, Orrery overdraw, full-bleed).
    - CLAUDE.md "Android device" section (emu-connect targets, uiautomator dump, input taxonomy, logcat-vs-Metro delta) and the device-UAT run-as pattern in MEMORY (DEBUG build needed for run-as; WAL-aware DB reads).
  </read_first>
  <action>
    First, make UAT row 15 reachable (addresses review MEDIUM #5): register the existing dev-only
    ThemePreviewScreen as a __DEV__-GATED route so its forceRenderError toggle is reachable on the DEBUG
    dev client. Add a `{__DEV__ ? <Stack.Screen name="__ThemePreview" component={ThemePreviewScreen}/> : null}`
    child to SettingsStack.tsx, add the route name to the SettingsStack param list in types.ts, and add a
    __DEV__-only row in SettingsScreen that navigates to it. The __DEV__ guards must dead-code-eliminate the
    route AND the row in the release APK (row 15 is validated on the debug client only — consistent with
    D-16, which iterates on the debug client and reserves the release APK for the final native gate). This
    harness exercises the identical BackgroundHost onError → resolveRenderableBackground None/Solid latch
    the shell host uses. (An acceptable alternative is a __DEV__-injectable forceRenderError flag on the
    shell BackgroundHost driven by a __DEV__-only dev store — also provably absent from release; declare
    whichever files it touches.) Do NOT introduce any release-reachable decode-failure toggle.

    Then run the automated regression: `npm test` (full vitest), `npx tsc --noEmit`, and
    `npm run check:colors src App.tsx` — all must be green before device work.

    Bring up exactly one authorized device via emu-connect (confirm cardinality: adb devices |
    awk '$2=="device"' | wc -l == 1); run the Orbit debug dev client against Orbit Metro on :8082.
    Execute D-19 B scenarios 1-6 and 9-17 (defer 7-8 to Task 2) across the cross-cutting grid
    (Package: Galaxy/Standard; Mode: Light/Dark/Follow-System; Text: Default/Largest; Motion: Off/On).
    Drive via `uiautomator dump` + `adb` taps using the correct input source per control; capture
    `adb exec-out screencap` screenshots per key row. Specifically prove: presentation screens reveal art
    + dense forms AA-readable + Digest legible (1-3); background fixed on scroll (4); no opaque wash /
    intrinsic tile / untinted corner / hardcoded colour (5, spot-check check:colors clean); route-based
    exclusion (6, D-22/D-23): the Orrery VISUALIZATION shows zero bleed under pan/pinch/tilt/yaw AND the
    System Builder authoring canvas shows zero bleed — but Systems Management (dense, flipped) legitimately
    reveals faint scrimmed art (PASS, do NOT fail it), and the OrreryStack browse children (Things to
    Remember / Recently Deleted / Memory History) reveal art identically via the Orrery tab and the
    Dashboard tab; picker all-8 in both
    packages not filtered (9); live preview + immediate persist, no crossfade (10); active-package column
    write with cross-package independence (11); relaunch restores selection with no wrong-theme/None flash
    (12); None/Solid solid + scrim (13); invalid stored id via run-as → package default, no crash (14);
    decode failure via the __DEV__ harness's forceRenderError toggle → silent None/Solid, latch resets on next
    valid pick (15); large-text reflow + grid re-column + ≥44px (16); Reduced Motion ON = immediate switch, no animation (17).

    For the Orrery perf observation (overdraw, Pitfall 3): make any perf note on the PHYSICAL Pixel only
    and state which thread the evidence covers. Record every row's binary pass/fail + screenshot
    references in a new 31.1-UAT.md (follow the repo's UAT doc convention; reference the D-19 B matrix).
    If any dense row shows a contrast miss, step that route one tier denser (readability wins, D-04) —
    note it for a follow-up Plan 03 tweak; never loosen a tier.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run check:colors src App.tsx</automated>
    <human-check>31.1-UAT.md records a binary pass for D-19 B rows 1-6 and 9-17 across the cross-cutting grid on the physical Pixel, with screenshots; any contrast miss noted with its denser-tier remediation.</human-check>
  </verify>
  <acceptance_criteria>
    - The __DEV__ decode-failure harness is registered (route + entry) behind __DEV__ guards; `npx tsc --noEmit` passes; grep confirms no release-reachable forceRenderError entry (the route/row are __DEV__-gated).
    - `npm test` green; `npx tsc --noEmit` clean; `npm run check:colors src App.tsx` clean.
    - Exactly one authorized device used (cardinality check recorded).
    - 31.1-UAT.md exists with per-row pass/fail for rows 1-6, 9-17 across Package × Mode × Text × Motion + screenshot references.
    - Row 6 (route-based exclusion) passes: Orrery visualization AND System Builder canvas show zero bleed; Systems Management correctly PASSES while revealing faint scrimmed art; OrreryStack browse children reveal art identically via either tab (D-22/D-23). Any perf note states the target (Pixel) and thread.
    - Rows 14 (invalid id → default) and 15 (decode failure → silent None/Solid, latch resets) proven via run-as / the __DEV__ harness forceRenderError toggle with no crash and no error UI.
  </acceptance_criteria>
  <precondition>Pixel 6 Pro attached + USB debugging authorized (emu-connect resolves exactly one device); Orbit debug dev client installed; Orbit Metro on :8082 (user_setup).</precondition>
  <done>The automated regression is green and the functional D-19 B matrix (rows 1-6, 9-17) passes on the physical Pixel, recorded in 31.1-UAT.md and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Profile precedence (UAT 7-8) + Profile-template regression guard (PROF-01/04/05)</name>
  <files>.planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md</files>
  <read_first>
    - src/profile/resolve-presentation.ts:52-70 (contact → Category → global → theme chain; theme branch means no app-owned photo).
    - src/screens/ContactProfileScreen.tsx:293-307 (its own BackgroundHost: appOwnedBackgroundUri = resolved photo or null; null → host reads the stored System slot = the D-06 fallback).
    - 31.1-UI-SPEC.md:298-307 (Precedence & state contracts) and 329-364 rows 7-8.
    - HANDOFF.md §14 custom-field invariants and D-07/D-15 (Profile template system preserved, separate storage — do NOT merge System assets into profile_background_templates).
  </read_first>
  <action>
    On the physical Pixel, execute D-19 B rows 7-8 across Galaxy/Standard and Light/Dark: open a Profile
    WITH a resolved app-owned photo (via a contact override, a Category assignment, and a global Profile
    photo in turn) and confirm the photo renders, NOT the System background (UAT 7 / PROF-04); open a
    Profile with NO app-owned photo and confirm it shows the active package's selected System background
    (UAT 8 / PROF-05). Then regression-guard the EXISTING Profile template system (PROF-01): exercise the
    Profile background manager — create a template from a photo, crop/reposition, assign at contact /
    Category / global, delete, and reset — and confirm behaviour is UNCHANGED from Phase 31, the fixed
    Hero/layout is intact, and no bundled System asset has leaked into profile_background_templates
    (confirm via a run-as DB read that profile_background_templates holds only app-owned template rows,
    WAL-aware). Record rows 7-8 + the regression result + screenshots in 31.1-UAT.md. Do NOT modify any
    Profile code — this is verification only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>31.1-UAT.md records: photo-wins (row 7) and no-photo→System fallback (row 8) both pass in both packages; Profile template create/crop/assign/delete/reset unchanged; Hero/layout intact; no System asset in profile_background_templates.</human-check>
  </verify>
  <acceptance_criteria>
    - Row 7 (photo overrides System) passes for contact, Category, and global app-owned photos.
    - Row 8 (no-photo → active-package System background) passes in both packages.
    - Profile template create/crop/assign/delete/reset behaviour is unchanged (regression pass) and the Hero/layout is intact (PROF-01).
    - A run-as DB read confirms profile_background_templates contains only app-owned template rows (no bundled System slot id), proving D-07 separation held.
    - No Profile source file was modified by this plan (git diff shows none).
  </acceptance_criteria>
  <precondition>Pixel 6 Pro attached + USB debugging authorized; a test contact with and without an app-owned Profile photo available on-device (device-local contact fixtures per MEMORY).</precondition>
  <done>Both Profile precedence rules and the Profile-template regression guard pass on the Pixel, recorded in 31.1-UAT.md and committed.</done>
</task>

<task type="auto">
  <name>Task 3: Final standalone release-APK native gate (D-16)</name>
  <files>.planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md</files>
  <read_first>
    - 31.1-CONTEXT.md D-16 (iterate on the debug dev client; build the standalone RELEASE APK only for the final native gate; verify on the physical Pixel; never worktree, never push).
    - docs/runbooks/desktop-build-pipeline.md:9,22-23,48-49,68-85,119-139 — the AUTHORITATIVE transport: rsync is ABSENT on droid (:23); ship SOURCE → droid via tar-over-ssh (:68) or `scp -r` (:84); pull the APK → this Linux box via `scp` (:119-127); install on the Pixel via `~/.local/bin/adb -s "$S" install -r ./app-release.apk` (:137). NEVER git push (:9).
    - MEMORY build/test pipeline (APKs build on the desktop via ssh droid + test on the Pixel; transport is rsync/scp/tar-over-ssh, NOT git push) and APK delivery via Drive (deliver to G:\\My Drive\\IT\\Software\\Orbit on droid).
    - 31.1-RESEARCH.md Environment Availability (droid desktop build; release-APK native gate).
  </read_first>
  <action>
    Build the standalone RELEASE APK on the droid desktop (ssh droid; per the desktop-build-pipeline
    runbook). Transport per the runbook (addresses review MEDIUM #6 — rsync is ABSENT on droid): ship the
    SOURCE to droid via tar-over-ssh (or `scp -r`), build there, then pull the built APK back to THIS Linux
    box via `scp` (forward-slash remote path). Install on the physical Pixel from this box via
    `~/.local/bin/adb -s "$S" install -r ./app-release.apk` (install -r preserves data; resolve $S as the
    one authorized serial from `adb devices`). NEVER git push and NEVER rsync onto the phone. Smoke the
    background feature on the RELEASE build (not the debug client): a
    presentation screen reveals the art (row 1), the Orrery shows zero bleed (row 6), a Profile photo
    overrides + a no-photo Profile falls back to System (rows 7-8), and a force-kill + relaunch restores
    the selection with no wrong-theme/None flash (row 12). Confirm the release build composites the
    transparent scenes correctly (the Android-transparency risk, A1/Pitfall 2, on a release build — the
    debug client can mask native differences). Deliver the APK to G:\\My Drive\\IT\\Software\\Orbit on
    droid. Record the native-gate result (pass/fail + screenshots) in 31.1-UAT.md. If the release build
    diverges from the debug client, STOP and surface it — do not ship around it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>31.1-UAT.md records the release-APK native gate: reveal, Orrery exclusion, Profile override + fallback, and relaunch-restore all pass on the standalone release build installed on the physical Pixel; APK delivered to Drive.</human-check>
  </verify>
  <acceptance_criteria>
    - A standalone release APK is built on droid (not the debug client); SOURCE shipped to droid via tar-over-ssh/scp, APK pulled back via scp, and installed on the physical Pixel via `~/.local/bin/adb -s "$S" install -r` (per the desktop-build-pipeline runbook; NO rsync onto the phone, NO git push anywhere).
    - Release smoke passes: reveal (1), route-based Orrery + SystemBuilder exclusion (6, D-22/D-23), Profile override + fallback (7-8), relaunch-restore no flash (12).
    - The release build composites transparent scenes identically to the debug client (A1 confirmed on release), or any divergence is surfaced and NOT shipped around.
    - 31.1-UAT.md records the native-gate result; APK delivered to G:\\My Drive\\IT\\Software\\Orbit.
  </acceptance_criteria>
  <precondition>droid desktop reachable (ssh droid) for the release build; Pixel 6 Pro attached for install + smoke.</precondition>
  <done>The standalone release APK passes the background-feature native gate on the physical Pixel, is delivered to Drive, and the result is recorded in 31.1-UAT.md and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored slot id → render (device) | The invalid-id and decode-failure fallbacks are exercised on the real device (run-as write / forceRenderError). |
| release build → device | The release APK must not diverge from the debug client in a way that reintroduces egress or breaks the local-first/transparency guarantees. |
| (foreclosed) network | D-12: no network on any render/selection path; the UAT confirms no wrong-theme network fetch on relaunch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-01 | Tampering | invalid/tampered stored slot id | medium | mitigate | UAT row 14 proves a run-as bogus id → package default, no crash/error UI (render-time fallback verified on device). |
| T-31.1-02 | Information disclosure | local-first on the release build | high | mitigate | Release-build smoke confirms no network read on relaunch/render; bundled + file:// only; the feature cannot widen egress (D-12). |
| T-31.1-03 | Denial of service / Availability | runtime decode failure | medium | mitigate | UAT row 15 proves forceRenderError → silent None/Solid with latch reset; Orrery overdraw note taken on the physical Pixel only. |
| T-31.1-05 | Availability/usability | contrast regression hiding content | medium | mitigate | UAT rows 1-3 across Package × Mode × Text prove AA-equivalent readability on shipped assets; any miss steps denser (readability wins). |
| T-31.1-07 | Tampering (data separation) | System asset leaking into profile_background_templates | medium | mitigate | Task 2 run-as DB read asserts profile_background_templates holds only app-owned rows (D-07 separation verified). |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npm test` green; `npx tsc --noEmit` clean; `npm run check:colors src App.tsx` clean.
- 31.1-UAT.md records a binary pass for all D-19 B rows (1-17) across Package × Mode × Text × Motion on the physical Pixel, with screenshots.
- Profile precedence (7-8) + Profile-template regression (PROF-01) + D-07 DB-separation check pass.
- Standalone release-APK native gate passes and the APK is delivered to Drive.
</verification>

<success_criteria>
All five ROADMAP success criteria are proven TRUE on the physical Pixel (and on the standalone release
build): selection/persistence/per-package memory/relaunch (1), fixed app-wide reveal + density readability
+ no regressions (2), route-based Orrery + SystemBuilder exclusion (3, D-22/D-23), deterministic Profile
precedence + preserved template system (4), and local-only fallbacks (5) — recorded in a committed 31.1-UAT.md.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 — for source-grounding exclusion)

Newly-created artifacts (exclude from source-grounding "must-already-exist" checks):
- `.planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md` (NEW file) — executed D-19 B matrix + native-gate results.
- A __DEV__-gated decode-failure harness: a new `{__DEV__ ? <Stack.Screen>}` registration of the existing dev `ThemePreviewScreen` in `src/navigation/tabs/SettingsStack.tsx`, a new __DEV__ route name in the SettingsStack param list in `src/navigation/types.ts`, and a __DEV__-only navigation row in `src/screens/SettingsScreen.tsx` — all dead-code-eliminated in the release APK.

No packages, no schema, no backup-format change, no Profile-source change (verification-only for Profile).
</artifacts_produced>

<review_findings_addressed>
## Cross-AI review findings addressed (Plan 04)

- **MEDIUM #5 (unreachable decode-failure harness).** Added a __DEV__-gated harness (register the existing
  dev ThemePreviewScreen + a __DEV__ entry row) so UAT row 15 is reachable on the debug dev client, with
  declared modified files and __DEV__ cleanup (provably absent from release). An injectable shell-host
  __DEV__ flag is documented as an acceptable alternative.
- **MEDIUM #6 (transport contradicts the runbook).** Corrected Task 3 to the desktop-build-pipeline
  runbook: source → droid via tar-over-ssh/scp (rsync is ABSENT on droid), APK → this Linux box via scp,
  install on the Pixel via `~/.local/bin/adb -s "$S" install -r` — never rsync onto the phone, never push.
- **LOW #7 (ambiguous UAT row 6).** Reworded row 6 (here and in 31.1-UI-SPEC.md) so Systems Management is
  NOT failed for correctly revealing dense-scrimmed art; only the Orrery canvas (D-05/D-22) and the System
  Builder canvas (D-23) must show zero bleed; browse children reveal identically via either tab (D-22).
</review_findings_addressed>

<output>
Create `.planning/phases/31.1-app-wide-system-backgrounds/31.1-04-SUMMARY.md` when done, and the executed
`.planning/phases/31.1-app-wide-system-backgrounds/31.1-UAT.md`.
</output>
