---
phase: 31.1-app-wide-system-backgrounds
plan: 01
subsystem: ui
tags: [react-native, react-navigation, zustand, theme, backgrounds]
requires:
  - phase: 23-theme-visual-system
    provides: bundled background resolver, BackgroundHost primitive, and per-package theme selection
  - phase: 31-profile-experience
    provides: Profile-owned background precedence preserved by the shell mount
provides:
  - transparent navigation scenes behind one shell-level BackgroundHost
  - deepest-route density classification and Orrery-only solid override
  - in-memory per-package System-background setter
affects: [31.1-02, 31.1-03, 31.1-04, theme, navigation]
actuals:
  tokens: 5136
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns:
    - NavigationContainer onReady/onStateChange synchronizes the deepest route into a runtime-only Zustand store.
    - The shell owns background rendering; route helpers select density and the Orrery override.
key-files:
  created:
    - src/theme/navigation-theme.ts
    - src/stores/focused-route-store.ts
    - src/stores/theme-store.test.ts
  modified:
    - App.tsx
    - src/navigation/RootNavigator.tsx
    - src/navigation/focused-route-classification.ts
    - src/screens/HomeScreen.tsx
    - src/stores/theme-store.ts
key-decisions:
  - "Only the deepest Orrery route forces None/Solid; Orrery-tab browse children retain the System background."
  - "NavigationContainer supplies transparent scenes while screen roots reveal a single fixed shell host."
  - "Background selection remains package-local and database-persisted by a later Settings caller; no AsyncStorage source was added."
patterns-established:
  - "Route-driven shell presentation: synchronize the container leaf route once, then consume pure route helpers at the shell boundary."
requirements-completed: [THEME-03, THEME-04, PROF-05]
coverage:
  - id: D1
    description: Route density and Orrery-only background suppression are deterministic and unit-covered.
    requirement: THEME-04
    verification:
      - kind: unit
        ref: src/navigation/focused-route-classification.test.ts
        status: pass
    human_judgment: false
  - id: D2
    description: System backgrounds render fixed behind Home while the Orrery canvas remains image-free.
    requirement: THEME-04
    verification:
      - kind: manual_procedural
        ref: Physical Pixel 6 Pro tracer UAT accepted 2026-09-10
        status: pass
    human_judgment: true
    rationale: Visual fixed-layer behavior and native scene compositing require physical-device observation.
  - id: D3
    description: The active package's live background setter changes only that package and accepts null defaults.
    requirement: THEME-03
    verification:
      - kind: unit
        ref: src/stores/theme-store.test.ts
        status: pass
    human_judgment: false
duration: 35min
completed: 2026-09-10
status: complete
---

# Phase 31.1 Plan 01: App-Wide System Backgrounds Summary

**A route-aware shell BackgroundHost now renders the active package’s fixed local System background behind Home while preserving Orrery’s opaque canvas and package-local selection state.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-10T23:40:00Z
- **Completed:** 2026-09-11T00:15:37Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Added transparent navigation themes and a single full-bleed BackgroundHost around the tab shell.
- Synced the deepest focused route from NavigationContainer to select density and suppress only the Orrery visualization.
- Made Home’s outer root cooperate with the shell host and resolved status-bar icon style from the active theme mode.
- Added the package-local live background setter with unit coverage for isolation and null defaults.
- Independently verified the tracer on the physical Pixel: Home artwork remains fixed while scrolling and Orrery has no System-image bleed.

## Task Commits

1. **Task 1: End-to-end shell background behind Home — one path, device-proven** — `b71081c` (test), `3a9a513` (feat)
2. **Task 2: Per-package live background setter (D-09) + unit suite** — `8811784` (test), `93b4fdd` (feat)

## Files Created/Modified

- `src/theme/navigation-theme.ts` — transparent light/dark React Navigation themes.
- `src/stores/focused-route-store.ts` — runtime-only deepest-route store.
- `App.tsx` — navigator route synchronization, transparent navigation theme, and mode-aware status bar.
- `src/navigation/RootNavigator.tsx` — single shell BackgroundHost mount driven by pure route helpers.
- `src/navigation/focused-route-classification.ts` — density map and Orrery-only slot override.
- `src/navigation/focused-route-classification.test.ts` — route-density and override coverage.
- `src/screens/HomeScreen.tsx` — transparent outer dashboard root.
- `src/stores/theme-store.ts` — per-package background setter.
- `src/stores/theme-store.test.ts` — setter isolation and null-default coverage.

## Decisions Made

- Followed D-22 exactly: only `routeName === "Orrery"` forces the shell host to None/Solid; browse children reached through OrreryTab are not excluded.
- Kept the existing BackgroundHost resolver, local asset-only render path, tamper fallback, and opaque Orrery root unchanged.
- Localized the only transparent color literal to `src/theme/navigation-theme.ts`; all screen-facing colors remain theme-token-derived.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected stale plan position after state-counter parser failure**
- **Found during:** Plan state updates
- **Issue:** `state.advance-plan` could not parse the pre-existing `Plan: Not started` current-position text, despite Plan 01 being complete.
- **Fix:** Normalized the stale position to the completed Plan 01 state, then re-ran `state.advance-plan`, which advanced the SDK-managed counter to Plan 2 of 4.
- **Files modified:** `.planning/STATE.md`
- **Verification:** STATE.md now reports Plan 1 of 4 complete and ROADMAP.md reports 1/4 plans executed.

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking workflow metadata)
**Impact on plan:** No application behavior changed; the correction keeps execution state resumable for Plan 02.

## Issues Encountered

- The required physical-Pixel Metro precondition was initially absent on port 8082. After the owner supplied the dedicated debug session, the tracer proceeded and was accepted.
- Existing unrelated HomeScreen Biome findings were observed but left unchanged because this plan modifies only its outer root background style.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plans 02–04 can build the Settings picker and expand cooperating roots on the proven shell architecture. The transparent native-scene cascade was accepted on the physical Pixel; no fallback contentStyle wrapper is needed.

## Self-Check: PASSED

- All nine implementation files exist and the four task commits are present in Git history.
- `npx vitest run src/navigation/focused-route-classification.test.ts src/stores/theme-store.test.ts`, `npx tsc --noEmit`, and `npm run check:colors src App.tsx` passed.
