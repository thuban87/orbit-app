---
phase: 33-group-interaction-logging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContactPicker.tsx
  - src/components/contact-picker-multiselect.ts
  - src/components/contact-picker-multiselect.test.ts
  - src/navigation/types.ts
  - src/navigation/focused-route-classification.ts
  - src/navigation/focused-route-classification.test.ts
autonomous: true
requirements: [GRP-06, GRP-08]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ContactPicker gains a multi-select mode over the SAME FlatList/listPickerContacts/filterPicker foundation via a TYPE-SAFE DISCRIMINATED UNION — `mode: \"single\"` (or absent) requires `onSelect`; `mode: \"multi\"` requires `onConfirm` and never uses `onSelect`; existing single-select callers (which pass `onSelect` and no `mode`) still compile against the single arm (dossier §K, no fork; review: discriminated-union props)."
    - "Multi-select keeps the modal open, tracks a selected set, shows a selected count and a clear affordance, and confirms via an explicit Done action (dossier §K)."
    - "Archived contacts surface only via explicit search with an Archived marker; snoozed contacts stay selectable with a Snoozed marker (SHELL-10 markers retained)."
    - "Selected state is conveyed by a check icon + accent token together, never colour alone (THEME-08)."
    - "The multi-select selection reducer is a pure node-testable module: toggling an already-selected contact deselects it (idempotent toggle); toggling a distinct contact adds it; selection order is deterministic (GRP-06 boundary)."
    - "ContactPicker's shared base props carry `excludeContactIds?: number[]` (alongside the pre-existing single `excludeContactId?: number`): the row filter removes BOTH the single excluded id and every id in the set before filterPicker runs, so a caller can hide contacts already in a group event. The exclusion is a pure, node-testable helper (applyPickerExclusions) so 'an already-present contact is not offered' is verifiable without the device (GRP-06; review cycle-4 MED #3)."
    - "Route types GroupEventDetail, EditGroupEvent, and EditParticipant are added to EVERY stack param list that hosts ContactProfileScreen — DashboardStackParamList, OrreryStackParamList, AND SettingsStackParamList (GroupEventDetail: { groupEventId: number }, EditGroupEvent: { groupEventId: number }, EditParticipant: { groupEventId: number; interactionId: number; contactId: number }); GroupLog: { participantIds?: number[] } is reused unchanged. These three routes are navigated from HistorySection (inside ContactProfileScreen, hosted in all three stacks), so registering them in Dashboard ONLY type-checks (RootStackParamList is a TYPE INTERSECTION — types.ts:246-249) but crashes at runtime from an Orrery- or Settings(Archived)-hosted profile; this mirrors the EXISTING EditInteraction/ThingsToRemember/MemoryHistory cross-stack registration (types.ts:195-215) (GRP-08, A2; review cycle-2 HIGH #1: cross-stack route registration + HIGH #3: participant-override route owner)."
    - "EditGroupEvent and EditParticipant are classified as focused workflows (bottom nav + FAB hidden, discard/keep guard applies), matching GroupLog; GroupEventDetail is a presentation route, not focused (GRP-08, dossier §R). The focused-route classification is keyed by route NAME (stack-agnostic), so it covers the routes in every stack."
    - "EditParticipant is the concrete navigation owner for the participant-override editor reachable from the history surface — it carries serializable primitives only (no callbacks/contact content), so InteractionDetail's 'Edit individual interaction' scope choice (Plan 07) and a ParticipantCard's 'Edit participant record' (Plan 07) both have a real destination (review HIGH #3)."
  artifacts:
    - src/components/contact-picker-multiselect.ts
    - "ContactPicker multi-select mode (discriminated-union props: single+onSelect | multi+onConfirm) + `excludeContactIds?: number[]` set-exclusion on the shared base props + the pure `applyPickerExclusions` helper (review cycle-4 MED #3)"
    - "navigation route types GroupEventDetail, EditGroupEvent, EditParticipant added to ALL THREE Profile-hosting param lists (Dashboard/Orrery/Settings) + EditGroupEvent/EditParticipant focused classification"
  key_links:
    - "ContactPicker multi-select consumed by GroupLogScreen/EditGroupEventScreen (Plan 06) for add/remove participants"
    - "route types consumed by the new screens + DashboardStack registration (Plans 06/07)"
  prohibitions:
    - "A second bespoke picker system must never be introduced — the shared ContactPicker foundation is extended, never forked (dossier §K)."
---

<objective>
Build the two shared, DAO-independent primitives the Group Log surfaces need, in parallel with the data foundation: extend the canonical `ContactPicker` to a multi-select mode (no fork) and declare the new navigation route types + focused-route classification for the Group Event surfaces. These have no `group_event_id` dependency, so they run in Wave 1 alongside the schema tracer.

Purpose: the multi-select picker is GRP-06's participant-selection foundation; the route types + focused classification let Plans 06/07 register and navigate the new screens. Naming (`GroupEventDetail`, `EditGroupEvent`) is A2 — Claude's discretion, resolved to these names (the requirement — separate detail + edit surfaces, GRP-08 — is fixed).
Output: `ContactPicker` multi-select mode + a pure selection reducer module; `GroupEventDetail`/`EditGroupEvent` route types; `EditGroupEvent` focused-route classification.

Phase tracer note: the end-to-end tracer proving the data spine lives in Plan 01; this plan is a parallel Wave-1 expansion of DAO-independent UI primitives.
</objective>

<review_findings_addressed>
Cross-AI review cycle 4 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-4 MED #3 (Add Participant can't exclude current members): Task 1 extends ContactPicker's shared base props with `excludeContactIds?: number[]` (a set-exclusion alongside the pre-existing single `excludeContactId?`), filtering out those rows before filterPicker via a pure node-testable helper (`applyPickerExclusions`) — so Plans 06/07's "Add participants" flow passes the current participant set and an already-present contact is never offered.

Cross-AI review cycle 2 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-2 HIGH #1 (cross-stack route registration crash — consensus): Task 2 adds the GroupEventDetail/EditGroupEvent/EditParticipant route TYPES to DashboardStackParamList, OrreryStackParamList, AND SettingsStackParamList (all Profile-hosting stacks), mirroring the existing EditInteraction cross-stack registration. (Per-stack SCREEN registration lands in Plans 06/07.)
- Cycle-2 LOW (focused-route default test): Task 2's classification test now explicitly asserts `isFocusedWorkflow` returns false for an unknown route, so a refactor cannot silently flip the default.

Cycle-1 findings (preserved, not undone):
- HIGH #3 (participant-override navigation owner): Task 2 adds the `EditParticipant` route type + focused classification, giving the participant-override editor a concrete, type-checked navigation destination (screen + registration in Plan 06; wiring in Plan 07).
- Non-HIGH (discriminated-union props): Task 1 redefines `ContactPickerProps` as `single+onSelect | multi+onConfirm`, so multi mode cannot fire an accidental single-select callback; a `tsc --noEmit` gate proves it.
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Table `group_events`; `interactions.group_event_id` + `ge_follow_*`; `idx_group_member_unique`; `migration026`; tombstone `"group_event"`; extended `insertInteractionCore`; `createGroupEvent` (Plan 01)
- DAO `updateGroupEvent`, `setParticipantOverride`, `clearParticipantOverride`, `setParticipantFields`, `saveParticipantEdits` (Plan 03 edits), `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent` (Plan 05 lifecycle)
- Reads `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants` (Plan 04)
- Pure logic `src/logic/group-inheritance.ts` (Plan 03)
- ★ `src/components/contact-picker-multiselect.ts` (selection reducer + `applyPickerExclusions` helper) + `ContactPicker` multi-select mode (discriminated-union props) + `excludeContactIds?: number[]` set-exclusion (review cycle-4 MED #3)
- ★ Route types `GroupEventDetail`, `EditGroupEvent`, `EditParticipant`; `EditGroupEvent` + `EditParticipant` focused classification
- Screens `GroupLogScreen`, `EditGroupEventScreen`, `GroupEventDetailScreen`, replaced `GroupEventsScreen`; components `ParticipantOverrideEditor`, `ParticipantCard`; wired `GroupScopePrompt`, `InteractionDetail` (Plans 06/07)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-UI-SPEC.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend ContactPicker to multi-select (shared foundation, no fork)</name>
  <files>src/components/ContactPicker.tsx, src/components/contact-picker-multiselect.ts, src/components/contact-picker-multiselect.test.ts</files>
  <read_first>
    - src/components/ContactPicker.tsx:22-30,100-106 (ContactPickerProps single-select contract; select() dismiss-on-tap; the FlatList/listPickerContacts/filterPicker foundation; pickerRowMarkers archived/snoozed markers; shellTransientStore registration)
    - src/components/icons/icon-registry.ts (Icon component + a check glyph for selected state)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Reuse-first inventory: multi-select; Color — accent reserved for selected state; THEME-08 state-never-colour-alone)
  </read_first>
  <behavior>
    - toggleSelection(set, id) on an id not present adds it; on an id present removes it (pure, idempotent toggle).
    - selectionCount(set) returns the number selected; clearSelection returns an empty set.
    - orderedSelection(set) returns ids in a deterministic (insertion or ascending) order.
    - applyPickerExclusions(rows, { excludeContactId, excludeContactIds }) returns rows minus the single excluded id and minus every id in excludeContactIds; empty/undefined exclusions return rows unchanged (review cycle-4 MED #3).
    - Multi-select mode: tapping a row toggles membership (modal stays open); single-select mode is unchanged (tap fires onSelect and dismisses).
  </behavior>
  <action>
    Extract the selection-set logic into a react-native-free module `src/components/contact-picker-multiselect.ts` (`toggleSelection`, `selectionCount`, `clearSelection`, `orderedSelection`) so it runs under vitest, and ALSO add a pure `applyPickerExclusions(rows, { excludeContactId?, excludeContactIds? })` helper there that removes both the single excluded id and every id in the set (review cycle-4 MED #3). Redefine `ContactPickerProps` as a TYPE-SAFE DISCRIMINATED UNION over a shared base (`visible`, `onDismiss`, `excludeContactId?`, `excludeContactIds?: number[]`, `allowArchivedSearch?`): the single arm is `{ mode?: "single"; onSelect: (contactId: number) => void }` (mode optional/defaulting single so EVERY existing caller — which passes `onSelect` and no `mode` — keeps compiling), and the multi arm is `{ mode: "multi"; onConfirm: (contactIds: number[]) => void; initialSelected?: number[] }` where `onSelect` is NOT accepted (review: discriminated-union props — do not keep a required `onSelect` alongside optional `mode`/`onConfirm`, which risks accidental single-select callbacks in multi mode). In multi mode, render a selection check (Icon + `accent` token — never colour alone, THEME-08) on selected rows, keep the modal open on tap, show a selected count and a Clear affordance, and confirm with a Done action calling `onConfirm(orderedSelection(...))`. Reuse the existing FlatList, `filterPicker`/`listPickerContacts`, the `ActivityIndicator` loading state, the archived/snoozed `pickerRowMarkers` (SHELL-10), and `shellTransientStore` registration verbatim — do NOT fork the component. In `ContactPicker.tsx`, replace the inline single-id exclusion `rows.filter((row) => row.id !== excludeContactId)` at :91-93 with `applyPickerExclusions(rows, { excludeContactId, excludeContactIds })` before `filterPicker`, so BOTH the single-id and the set exclusion apply in single AND multi mode (review cycle-4 MED #3). All colours through theme tokens; honour MIN_TOUCH_TARGET on every row and the Done/Clear controls.
    Author `contact-picker-multiselect.test.ts` for the reducer behaviors above, plus `applyPickerExclusions` (single id removed; a set of ids removed; an already-present id is not returned; empty/undefined exclusions leave rows unchanged — review cycle-4 MED #3).
  </action>
  <verify>
    <automated>npx vitest run src/components/contact-picker-multiselect.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel debug build: open the multi-select picker, select 3 contacts (count updates, checks appear), search reveals an Archived-marked contact and selects it, Clear empties the set, Done returns the selected ids. Single-select callers (e.g. existing Log Contact) still dismiss on tap. (Device UAT per the desktop-build-pipeline runbook; :8082 Metro remap.)</human-check>
  <acceptance_criteria>
    - Reducer test green: toggle add/remove idempotency, count, clear, deterministic order.
    - applyPickerExclusions test green: single id removed, id-set removed, already-present id not returned, empty exclusions unchanged (review cycle-4 MED #3); `excludeContactIds?: number[]` is on the shared base props (visible to both single and multi arms).
    - `npm run check:colors` passes; no hex literals in ContactPicker changes.
    - Single-select `onSelect` path preserved (existing ContactPicker callers unaffected — grep confirms callers still pass onSelect); `npx tsc --noEmit` passes, proving the discriminated union type-checks and the multi arm does not accept `onSelect`.
  </acceptance_criteria>
  <done>ContactPicker supports a multi-select mode with a node-tested selection reducer, count/clear/Done, and SHELL-10 markers, without forking the shared picker; it also accepts `excludeContactIds?: number[]` (node-tested set-exclusion via applyPickerExclusions) so callers can hide current group members (review cycle-4 MED #3).</done>
</task>

<task type="auto">
  <name>Task 2: Group Event route types + focused-route classification</name>
  <files>src/navigation/types.ts, src/navigation/focused-route-classification.ts, src/navigation/focused-route-classification.test.ts</files>
  <read_first>
    - src/navigation/types.ts:37,45-51 (DashboardStackParamList GroupEvents + GroupLog param entries; add the three new routes here)
    - src/navigation/types.ts:150-229 (OrreryStackParamList + SettingsStackParamList — each registers Profile AND EditInteraction/ThingsToRemember/MemoryHistory, with the documented "RootStackParamList is a TYPE intersection — each stack must register the screen it can reach" rationale at :195-215; the three group routes must be added here too)
    - src/navigation/types.ts:246-249 (RootStackParamList = Dashboard & Orrery & Backup & Settings INTERSECTION — why Dashboard-only registration type-checks but crashes at runtime)
    - src/navigation/focused-route-classification.ts:6-33 (FOCUSED_WORKFLOW_ROUTES set + isFocusedWorkflow returns .has(name), default FALSE for unknown routes; GroupLog already present — mirror for EditGroupEvent/EditParticipant)
    - src/navigation/focused-route-classification.test.ts (existing assertions to extend)
  </read_first>
  <action>
    Add `GroupEventDetail: { groupEventId: number }`, `EditGroupEvent: { groupEventId: number }`, and `EditParticipant: { groupEventId: number; interactionId: number; contactId: number }` to ALL THREE Profile-hosting stack param lists in `types.ts` — `DashboardStackParamList`, `OrreryStackParamList`, AND `SettingsStackParamList` (review cycle-2 HIGH #1: these routes are navigated from `HistorySection` inside `ContactProfileScreen`, which is registered in all three stacks; `RootStackParamList` is a TYPE intersection at types.ts:246-249, so Dashboard-only registration type-checks but throws "no screen named …" at runtime from an Orrery- or Settings(Archived)-hosted profile — exactly the class already fixed for EditInteraction/ThingsToRemember/MemoryHistory at types.ts:195-215). Mirror the existing EditInteraction cross-stack registration, and add the same documenting comment on the Orrery/Settings entries. Reuse the existing `GroupLog: { participantIds?: number[] }` and `GroupEvents` entries unchanged (those are NOT navigated from the history surface, so they remain Dashboard-only). `EditParticipant` is the navigation owner for the participant-override editor reached from the history surface (Plan 07 wires it; Plans 06 registers the hosting screen in each stack) — serializable primitives only, no callbacks (native-stack serialization + deep-link safety, mirroring `EditInteraction: { contactId, interactionId }`). Add `"EditGroupEvent"` AND `"EditParticipant"` to the `FOCUSED_WORKFLOW_ROUTES` set (focused workflow — bottom nav/FAB hidden, discard/keep guard applies, matching `GroupLog`); leave `GroupEventDetail` out (ordinary presentation route, NOT focused, per dossier §R). Extend `focused-route-classification.test.ts` asserting `EditGroupEvent` and `EditParticipant` are focused, `GroupEventDetail` is not, AND — so a refactor cannot silently flip the default — an explicit assertion that an UNKNOWN route name (e.g. "GroupEventDetail" and a nonsense route) returns `false` from `isFocusedWorkflow` (review cycle-2 LOW: assert the default-false classification explicitly).
  </action>
  <verify>
    <automated>npx vitest run src/navigation/focused-route-classification.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `GroupEventDetail`/`EditGroupEvent` ({ groupEventId: number }) and `EditParticipant` ({ groupEventId, interactionId, contactId }) are present in DashboardStackParamList, OrreryStackParamList, AND SettingsStackParamList (review cycle-2 HIGH #1).
    - Classification test asserts `EditGroupEvent` and `EditParticipant` focused, `GroupEventDetail` not focused, AND that `isFocusedWorkflow` returns false for an unknown route (explicit default assertion — review cycle-2 LOW).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The new Group Event route types (incl. `EditParticipant`, the participant-override editor's route owner) exist in ALL THREE Profile-hosting stack param lists, and `EditGroupEvent`/`EditParticipant` are focused workflows with the default-false classification explicitly tested — ready for per-stack screen registration in Plans 06/07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text (picker search) → filter | search term filters the in-memory contact list |
| route params → screens | groupEventId passed between surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Tampering | picker search filter | low | accept | In-memory filter over already-loaded local contacts; no SQL built from the term here |
| T-33-06 | Information Disclosure | route params carry only a numeric groupEventId | low | accept | No contact content in params; screens read from local DB by id |
| T-33-07 | Spoofing/Elevation | n/a (single on-device user, no auth) | low | accept | Local-first; no access-control boundary in scope |
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` passes.
- Existing ContactPicker single-select callers unaffected.
- `excludeContactIds?: number[]` set-exclusion works via the node-tested applyPickerExclusions; existing single `excludeContactId` behavior unchanged (review cycle-4 MED #3).
</verification>

<success_criteria>
The shared ContactPicker supports multi-select with a node-tested reducer, and the Group Event route types + focused classification are in place — both DAO-independent, so Plans 06/07 can build the screens without re-deriving these primitives.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-02-SUMMARY.md` when done.
</output>
