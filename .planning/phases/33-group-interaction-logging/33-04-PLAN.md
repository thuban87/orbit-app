---
phase: 33-group-interaction-logging
plan: 04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/db/group-events-read.ts
  - src/db/group-events-read.test.ts
autonomous: true
requirements: [GRP-08, GRP-09]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "listGroupEvents returns events in reverse-chronological order by occurred_at; ties are broken deterministically by id descending (GRP-09 ordering, dossier §T)."
    - "searchGroupEvents matches on title OR participant name; a term equal to both a title and a participant name returns the event once (deduped) (GRP-09 adjacency)."
    - "An empty browse (no events) returns an empty list (the UI renders 'No group events yet'); an empty/whitespace search term returns the full reverse-chron list, not an error (GRP-09 empty)."
    - "The search term is `?`-bound (LIKE with escaped wildcards), never interpolated into SQL (V5, absolute rule)."
    - "readGroupEventDetail returns the event fields (title, occurred_at, channel, quality, duration, group_note) plus its resolved participants with per-field follow state, so the Detail surface can show inherit-vs-override by text (GRP-08, dossier §R/§S)."
    - "resolveParticipants returns each child's contact identity + resolved values + ge_follow_* state ordered deterministically (by child interaction id / add order); a zero-participant event resolves to an empty participant list (GRP-08 empty/ordering)."
    - "Reads open no write transaction and never mutate; they select explicit named column lists (never SELECT *) (dossier read idiom)."
  artifacts:
    - src/db/group-events-read.ts
    - "read functions listGroupEvents, searchGroupEvents, readGroupEventDetail, resolveParticipants"
  key_links:
    - "GroupEventsScreen browse (Plan 07) consumes listGroupEvents/searchGroupEvents"
    - "GroupEventDetailScreen + EditGroupEventScreen (Plans 06/07) consume readGroupEventDetail/resolveParticipants"
  prohibitions:
    - "A browse or search read must never surface the parent as a duplicate of a participant's history row (reads are over group_events, not a second interactions projection)."
---

<objective>
Build the pure read layer for Group Events: the reverse-chronological browse list, title-and-participant search, and the detail/participant resolution that the presentation surfaces render. No transactions, explicit column lists, `?`-bound search — parallel with the edit fan-out (Plan 03) since reads only need the schema from Plan 01.

Purpose: GRP-09 (lean reverse-chron browse searchable by title + participant) and the data side of GRP-08 (presentation-first detail with resolved participants).
Output: `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants` in `src/db/group-events-read.ts`.

Phase tracer note: the phase tracer is in Plan 01; this plan expands the read side on the proven schema.
</objective>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Table `group_events`; `interactions.group_event_id` + `ge_follow_*`; `idx_group_member_unique`; `migration026`; tombstone `"group_event"`; extended `insertInteractionCore`; `createGroupEvent` (Plan 01)
- Pure logic `group-inheritance.ts`; DAO shared/date edits + override set/clear (Plan 03)
- ★ Reads `src/db/group-events-read.ts` — `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants`
- DAO lifecycle + convert (Plan 05); `ContactPicker` multi-select + route types (Plan 02)
- Screens + wired seams (Plans 06/07)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-RESEARCH.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@src/db/group-events-dao.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Browse + search reads (listGroupEvents, searchGroupEvents)</name>
  <files>src/db/group-events-read.ts, src/db/group-events-read.test.ts</files>
  <read_first>
    - src/db/history-read.ts (read idiom: explicit named columns, ?-bound, deterministic ORDER BY, no write txn)
    - src/backup/export-manifest.ts:52 (named-column read list idiom)
    - src/db/group-events-dao.ts (createGroupEvent — used to seed test fixtures)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting Contract — empty/no-results copy the UI renders on these read results)
  </read_first>
  <behavior>
    - listGroupEvents({ limit?, offset? }) returns events ordered occurred_at DESC, id DESC; no events → empty array.
    - searchGroupEvents({ term }) returns events whose title matches OR that have a participant whose contact name matches; each event appears once; empty/whitespace term returns the full reverse-chron list.
    - The term is bound via LIKE with escaped % and _; no interpolation.
    - Each returned event row carries id, uid, title, occurred_at, channel, quality, duration, group_note, plus a participant count.
  </behavior>
  <action>
    Create `src/db/group-events-read.ts` with `listGroupEvents(exec, { limit?, offset? })` (SELECT explicit columns FROM group_events ORDER BY occurred_at DESC, id DESC) and `searchGroupEvents(exec, { term })`. Search matches title LIKE ? OR EXISTS (a participant child whose contact name LIKE ?), deduped to one row per event (GROUP BY / DISTINCT), `?`-bound with escaped LIKE wildcards. Reads use no write transaction (or `inReadSnapshot` only if a consistent multi-table read is needed). Author `group-events-read.test.ts` seeding events via `createGroupEvent` and asserting order, dedup, empty-term-returns-all, and no-events-empty.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-read.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - list order is occurred_at DESC then id DESC; no events → [].
    - search matches title OR participant name, deduped to one row; empty term → full list.
    - LIKE term is `?`-bound with escaped wildcards (no interpolation).
  </acceptance_criteria>
  <done>The browse list and title+participant search are implemented as pure, deterministic, injection-safe reads with green tests.</done>
</task>

<task type="tdd">
  <name>Task 2: Detail + participant resolution reads (readGroupEventDetail, resolveParticipants)</name>
  <files>src/db/group-events-read.ts, src/db/group-events-read.test.ts</files>
  <read_first>
    - src/logic/group-inheritance.ts (resolveDisplay — used by the UI over the rows this returns)
    - src/db/history-read.ts:104-106 (isGroupLinked)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §R/§S (Detail contents; participant card summary)
  </read_first>
  <behavior>
    - readGroupEventDetail({ groupEventId }) returns the event fields + its participants (via resolveParticipants); a missing id returns null.
    - resolveParticipants({ groupEventId }) returns per-participant: contact_id, contact name/avatar identity, the child interaction id, resolved channel/quality/duration/direction/connected, the ge_follow_* flags, and the participant note — ordered by child interaction id ascending (add order).
    - A zero-participant event resolves to an empty participant list (not null, not error).
  </behavior>
  <action>
    Add `readGroupEventDetail(exec, { groupEventId })` and `resolveParticipants(exec, { groupEventId })` to `group-events-read.ts`. resolveParticipants joins interactions (WHERE group_event_id=?) to the contact identity read, selecting explicit columns incl. the five resolved value columns + five ge_follow_* flags + note, ORDER BY interactions.id ASC. readGroupEventDetail reads the group_events row (null if absent) and attaches resolveParticipants output. No write txn. Extend the read test: detail for a mixed inherit/override event exposes follow flags per participant; zero-participant event → empty participants; missing id → null.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-read.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - readGroupEventDetail returns event + participants; missing id → null.
    - resolveParticipants exposes resolved values + ge_follow_* per participant, ordered by child id; zero-participant → [].
  </acceptance_criteria>
  <done>Detail and participant resolution reads return everything the presentation surfaces need to render inherit-vs-override by text, with green tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text (browse search term) → SQL LIKE | search crosses into a query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-11 | Tampering | SQL injection via search term | medium | mitigate | `?`-bound LIKE with escaped wildcards; never interpolate the term (V5) |
| T-33-12 | Information Disclosure | reads never touch the AI egress path | low | accept | Pure local reads for on-device presentation; no network |
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` passes.
- Reads open no write transaction and use explicit column lists.
</verification>

<success_criteria>
The Group Events browse, search, detail, and participant-resolution reads are implemented, injection-safe, deterministic, and cover the zero-participant and missing-id edges — ready for the presentation surfaces.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-04-SUMMARY.md` when done.
</output>
