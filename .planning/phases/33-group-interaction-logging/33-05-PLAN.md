---
phase: 33-group-interaction-logging
plan: 05
type: execute
wave: 3
depends_on: ["33-03"]
files_modified:
  - src/db/group-events-dao.ts
  - src/db/group-events-dao.test.ts
  - .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md
autonomous: true
requirements: [GRP-06, GRP-07, GRP-10, GRP-13]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "addParticipant stamps the new child's occurred_at at the event's date/time (NOT the add time) and seeds the event's CURRENT resolved shared values (Channel/Tone/Duration) with ge_follow_channel/quality/duration=1; the child's direction/connected come from the participant input with NO follow-flag (there are only three follow-flags — dossier §D/§E/§L, review cycle-2 HIGH #2) (GRP-06 boundary, dossier §M)."
    - "The added child's occurred_at equals the event occurred_at string verbatim (local wall-clock, no timezone conversion via formatLocalDate/localDateTime) (GRP-06 precision)."
    - "Adding a contact already in the event is rejected by UNIQUE(group_event_id, contact_id); no duplicate child is created (GRP-06 idempotency)."
    - "Adding an archived contact succeeds, advancing their last_contact, with no restore prompt and no added guard (D-07)."
    - "Remove-participant is three-way: 'Delete interaction' = deleteGroupChild (deleteInteractionCore: tombstone + recency recompute); 'Keep as individual interaction' = detachParticipant (group_event_id=NULL, the three ge_follow flags=NULL, values already materialized, note preserved, Group Note NOT copied); 'Cancel' = no-op (GRP-06, D-11, dossier §N/§O)."
    - "deleteGroupChild and detachParticipant each REQUIRE groupEventId and assert the child belongs to it (`WHERE … AND group_event_id=?`, changes===1) before mutating — a stale/malformed caller can never delete or detach a standalone or another group's child (review cycle-2 MED: membership scoping)."
    - "detachParticipant needs no recency recompute (values already materialized, occurred_at unchanged) and never copies group_note into the participant note (dossier §O)."
    - "addParticipant and every three-way-remove branch each run in exactly one inWriteTransaction; an interrupted/failed add or remove rolls back fully with no partial child or dangling link (GRP-06 concurrency, GRP-11)."
    - "Dissolve and Delete Group Event & Interactions each FETCH the parent by groupEventId inside the transaction (`SELECT uid FROM group_events WHERE id=?`), assert it exists, and DERIVE the tombstone entity_uid from that fetched row — never from a caller-supplied uid argument, so a mismatched input can never delete Group A while tombstoning Group B (review cycle-2 MED: derive tombstone UID from the fetched row)."
    - "Dissolve detaches ALL children (group_event_id=NULL, the three ge_follow flags=NULL), then DELETEs the parent and writes a group_event tombstone keyed by the FETCHED uid; children survive standalone with materialized values (GRP-10 adjacency, dossier §X)."
    - "Delete Group Event & Interactions deletes every child via deleteInteractionCore (tombstone + recompute each), then DELETEs the parent + writes a group_event tombstone (GRP-10 adjacency, dossier §X)."
    - "Dissolving/deleting a zero-participant event removes only the parent (+ tombstone), touching no interactions (GRP-10 empty)."
    - "Delete-with-interactions loops children then the parent in one transaction; child-loop order does not affect the fully-rolled-back-on-failure guarantee (GRP-10 ordering, GRP-11)."
    - "Dissolve and delete each write a group_event tombstone so restore-merge never resurrects the parent (GRP-10 idempotency)."
    - "Dissolve and delete each run in exactly one inWriteTransaction; partial dissolve/delete is never visible (GRP-10 concurrency, GRP-11)."
    - "deleteGroupChild removes only that participant's child; other participants are unaffected and the event may become zero-participant and remain valid (dossier §Y)."
    - "Convert preserves the ordinary interaction's id and uid via an in-place UPDATE (never delete/recreate); it creates a group_events parent with its OWN freshly-minted/supplied `uid` (the parent's `uid TEXT NOT NULL UNIQUE` — preserving the CHILD uid does NOT supply a PARENT uid; convert takes/mints an explicit parent uid), seeding the event's Channel/Tone/Duration from the interaction's current values and setting the interaction's group_event_id + ge_follow_channel/quality/duration=1 (the three follow-flags only; direction/connected are left as the interaction's existing values with no flag — review cycle-2 HIGH #2) (GRP-07 adjacency, dossier §P; review: parent UID on convert)."
    - "Convert rejects a blank/whitespace-only title (DAO-level trim/nonblank guard) BEFORE its transaction; with no note/sparse values it still seeds shared defaults from the interaction; the required event title is supplied by the convert flow (event-only, required — A2 resolved: prompt for title) (GRP-07 empty; review: title nonblank)."
    - "convertInteractionToGroupEvent RETURNS the new parent's `groupEventId` (the local `group_events.id` minted by the parent INSERT) — an explicit part of its contract, because Plan 07's convert entry navigates to GroupEventDetail with that id after the call (review cycle-3 LOW: convert return contract; mirrors createGroupEvent returning its new id)."
    - "After convert, the original interaction becomes the first child; further participants are added via addParticipant (GRP-07 ordering)."
    - "The backup handoff spec (33-BACKUP-HANDOFF.md) serializes the parent→child link by DURABLE UID, never the local integer: an exported interaction carries `groupEventUid` (resolved from group_events.uid via a JOIN on export — mirroring how interactions already export `contactUid`, not `contact_id`, at export-manifest.ts:52), and the local `interactions.group_event_id` INT is NEVER put on the wire because it cannot survive export/restore into a DB where parent ids differ; restore upserts parents by uid, maps uid→local id, then relinks children. Override/inheritance state (ge_follow_*) and materialized shared values survive without flattening (GRP-13, dossier §AC; review HIGH #1: backup linkage representation)."
    - "The handoff defines orphan handling: a restored child whose parent group_event uid is ABSENT has its local link cleared (group_event_id NULL) AND all ge_follow_* cleared, surviving as a standalone interaction — never dropped (A3 recommended default, owner-pending; dossier §AC)."
    - "Every lifecycle op (addParticipant, each three-way-remove branch, dissolve, delete-with-interactions, convert) performs EXACTLY ONE data-revision bump per transaction: inner deleteInteractionCore and inner insertTombstoneCore are called with { bumpRevision: false } and the composing transaction owns a single trailing bumpDataRevisionCore (matches the insertInteractionCore/editTouchpointFullCore 'composing caller owns the bump' contract; review: revision double-bump)."
  artifacts:
    - "DAO addParticipant, detachParticipant, deleteGroupChild, dissolveGroupEvent, deleteGroupEventAndInteractions, convertInteractionToGroupEvent"
    - .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md
  key_links:
    - "deleteGroupEventAndInteractions -> deleteInteractionCore (per child, tombstone+recompute) -> DELETE parent -> group_event tombstone, one txn"
    - "convertInteractionToGroupEvent -> in-place UPDATE interactions (id/uid preserved) + new group_events parent -> RETURNS the new parent groupEventId (Plan 07 navigates on it)"
    - "33-BACKUP-HANDOFF.md -> Phase 36 restore/orphan-repair path (executed there, defined here)"
  prohibitions:
    - "Deleting a group event must never rely on FK ON DELETE CASCADE (bypasses recency recompute + tombstone)."
    - "Detach / Keep-as-individual must never copy the Group Note into the participant note."
    - "A dissolve/delete/convert must never nest inWriteTransaction or bulk-mutate children outside the recency cores."
---

<objective>
Complete the Group Event mutation surface: participant add/remove (the three-way Delete / Keep-as-individual / Cancel), the two lifecycle operations (Dissolve vs Delete Group Event & Interactions), single-child delete, and Interaction→Group Event conversion — all atomic through the recency cores (D-05, GRP-11). Then write the backup entity-shape handoff spec for Phase 36 (D-10 — this phase does NOT bump the backup format or edit export-manifest.ts/restore-apply.ts; it hands over the shape, the tombstone, and the orphan-repair rule).

Purpose: GRP-06 (add/remove), GRP-07 (convert), GRP-10 (dissolve/delete), GRP-13 (backup shape + orphan rule).
Output: `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent`; `33-BACKUP-HANDOFF.md`.

Phase tracer note: the phase tracer is in Plan 01; this plan expands the DAO with lifecycle + convert on the proven spine.
</objective>

<review_findings_addressed>
Cross-AI review cycle 3 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-3 LOW (convert return contract): convertInteractionToGroupEvent now explicitly RETURNS the new parent's groupEventId (the local group_events.id) — stated in its signature/behavior/acceptance and the key_link — because Plan 07 navigates to GroupEventDetail with it.
- Cycle-3 LOW (DAO tsc): every task's <verify> adds `npx tsc --noEmit` so a DAO type regression is caught at the task boundary.

Cross-AI review cycle 2 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-2 HIGH #2 ripple: addParticipant and convert set EXACTLY the three follow-flags (ge_follow_channel/quality/duration); detach/dissolve clear those three; direction/connected are ordinary participant columns with no follow-flag; the backup handoff serializes three follow-flags. Matches Plan 01's schema.
- Cycle-2 MED (DAO group-membership scoping): deleteGroupChild + detachParticipant require groupEventId and assert `group_event_id=?` (changes===1) before mutating; dissolveGroupEvent + deleteGroupEventAndInteractions FETCH the parent by id inside the txn and derive the tombstone uid from that row (no caller-supplied uid arg).

Cycle-1 findings (preserved, not undone):
- HIGH #1 (backup linkage representation): Task 3's 33-BACKUP-HANDOFF.md serializes the parent→child link as the DURABLE `groupEventUid` (resolved via JOIN on export, mirroring `contactUid`), explicitly forbids putting the local `group_event_id` int on the wire, and defines the restore order + orphan handling. The Phase-36 execution stays deferred; only the handoff SPEC is fixed here.
- Non-HIGH (parent UID on convert): Task 3 gives `convertInteractionToGroupEvent` an explicit minted/supplied parent `uid` (child uid ≠ parent uid) + test.
- Non-HIGH (title nonblank): Task 3 adds a trim/nonblank title guard to convert (create's guard is in Plan 01).
- Non-HIGH (revision double-bump): Tasks 1–2 pass `{ bumpRevision: false }` to inner deleteInteractionCore/insertTombstoneCore and own one trailing bump, so every lifecycle op bumps the revision exactly once (asserted by counter delta).
- Non-HIGH (exhaustive writer audit): Task 1 read-first lists the exceptional interactions writers (restore-apply.ts, purge-dao.ts, migration025) to audit before asserting invariants.
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Table `group_events`; `interactions.group_event_id` + `ge_follow_*`; `idx_group_member_unique`; `migration026`; tombstone `"group_event"`; extended `insertInteractionCore`; `createGroupEvent` (Plan 01)
- Pure logic `group-inheritance.ts`; DAO shared/date edits + override set/clear (Plan 03)
- Reads `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants` (Plan 04)
- ★ DAO `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent`
- ★ `33-BACKUP-HANDOFF.md` (Phase-36 entity-shape + tombstone + orphan-repair spec)
- `ContactPicker` multi-select + route types (Plan 02); screens + wired seams (Plans 06/07)

<flagged_assumption id="A3-orphan-repair" owner_pending="true">
**A3 — restore orphan-repair outcome (D-10). OWNER-PENDING — do NOT implement this phase.** Recommended default: a restored child interaction whose parent `group_events` row is missing is **detached to standalone** (`group_event_id` cleared, ge_follow_* cleared), never dropped — dossier §AC prefers preserving a valid child Interaction as standalone over losing contact history. D-10 requires this outcome be a *decided* rule; the rule is **executed by Phase 36's restore path**, not here. This phase only (a) defines the `group_events` entity shape, (b) adds the `group_event` tombstone type (Plan 01), and (c) records this recommended outcome in `33-BACKUP-HANDOFF.md`. Confirm A3 with the owner before Phase 36 so it becomes a locked Phase-36 rule. Do NOT edit `export-manifest.ts` / `restore-apply.ts` in Phase 33.
</flagged_assumption>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-RESEARCH.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md
@src/db/group-events-dao.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Participant add + three-way remove building blocks (addParticipant, detachParticipant, deleteGroupChild)</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (createGroupEvent / edit fan-outs — same composition idiom)
    - src/db/recency-dao.ts:340-368 (deleteInteractionCore — tombstone BEFORE delete, then recompute; NOW takes an optional { bumpRevision } from Plan 01 — pass false in a composing loop and own one trailing bump)
    - src/db/recency-dao.ts:193-236,446-460 (insertInteractionCore + aliases)
    - src/db/tombstones-dao.ts:60-80 (insertTombstoneCore { bumpRevision } — pass false for a composed tombstone)
    - EXCEPTIONAL existing interactions/last_contact writers to audit before asserting any invariant (the graph cannot enumerate SQL writers — CLAUDE.md; review: exhaustive writer audit): src/backup/restore-apply.ts:190-201 (restore's direct INSERT…ON CONFLICT upsert of interactions — forces allow_ai=0, does NOT recompute last_contact, omits group columns; Phase 36 owns adding them); src/db/purge-dao.ts:82 (hard contact-purge DELETE of interactions by contact_id — bypasses the recency spine intentionally on full-contact purge); src/db/migrations/025-interaction-history-schema.ts:44-72 (migration-time UPDATE interactions backfill). Confirm each is intentionally outside the group-lifecycle path and unaffected by this plan.
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §M/§N/§O (add stamps event date/time; three-way remove; detach materializes, never copies Group Note)
    - docs/dossier/milestone-2/planning-notes/phase-12-planning-notes.md R-02 constraint 2 (archived participants allowed, no guard — D-07)
  </read_first>
  <behavior>
    - addParticipant reads the event's current resolved shared values + occurred_at, inserts one child via insertInteractionCore (occurred_at = event occurred_at, Channel/Tone/Duration materialized, ge_follow_channel/quality/duration=1, direction/connected from the participant input with no flag, group_event_id set), recomputes last_contact, bumps revision — one txn.
    - Adding an already-present contact throws on the UNIQUE index and rolls back (no duplicate).
    - Adding an archived contact succeeds (last_contact advances; no restore prompt).
    - deleteGroupChild requires groupEventId, asserts the child belongs (`WHERE … AND group_event_id=?`), calls deleteInteractionCore (tombstone + recompute); other participants unaffected; event may become zero-participant; a non-member child throws + rolls back.
    - detachParticipant requires groupEventId, asserts membership, sets group_event_id=NULL, the three ge_follow flags=NULL, modified_at=now (values already materialized, no recompute), preserves the note, does NOT copy group_note; bumps revision.
  </behavior>
  <action>
    Add `addParticipant(exec, { groupEventId, contactId, uid, direction?, connected?, note?, now })`, `deleteGroupChild(exec, { groupEventId, interactionId, contactId, now })`, and `detachParticipant(exec, { groupEventId, interactionId, now })` to `group-events-dao.ts`, each one `inWriteTransaction`. addParticipant reads the event's resolved shared values + occurred_at, then `insertInteractionCore` (event occurred_at per §M — never the add time; Channel/Tone/Duration materialized; ge_follow_channel/quality/duration=1; direction/connected from the input with no follow-flag — review cycle-2 HIGH #2; group_event_id) + `recomputeLastContactCore` + a single `bumpDataRevisionCore`. Do NOT add an archived guard (D-07). deleteGroupChild = assert membership first (e.g. `DELETE`/edit scoped by `AND group_event_id=?`, or fetch the row and verify its group_event_id === groupEventId — changes===1 or throw; review cycle-2 MED: membership scoping), then `deleteInteractionCore(exec, { interactionId, contactId, now }, { bumpRevision: false })` + a single trailing `bumpDataRevisionCore` (never raw DELETE, never FK cascade) — exactly one bump, not two (review: revision double-bump). detachParticipant = one UPDATE clearing group_event_id + the three ge_follow flags + set modified_at, SCOPED `WHERE id=? AND group_event_id=?` (`?`-bound), asserting `changes===1` (membership enforcement — a non-member child changes 0 rows → throw → rollback; review cycle-2 MED), then a single `bumpDataRevisionCore` — no recompute, and never copy group_note into the note (dossier §O). Extend `group-events-dao.test.ts`: add stamps event occurred_at; duplicate add rejected; archived add advances last_contact; deleteGroupChild leaves peers intact; detach materializes + preserves note + does not copy group_note; AND deleteGroupChild/detachParticipant called with a groupEventId the child does not belong to throw and write nothing (review cycle-2 MED: membership scoping).
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - addParticipant child occurred_at == event occurred_at; duplicate rejected; archived add advances last_contact; child has ge_follow_channel/quality/duration=1 and direction/connected materialized from the input (no extra flag).
    - deleteGroupChild removes only that child (tombstone written); peers intact; requires groupEventId and throws on a non-member child.
    - detachParticipant clears group_event_id + the three ge_follow flags, keeps note, does not copy group_note, no recency change; scoped by group_event_id (changes===1), throws on a non-member child.
    - Each op performs EXACTLY ONE data-revision bump (deleteGroupChild passes `{ bumpRevision: false }` to deleteInteractionCore + one trailing bump — asserted via the revision counter delta).
  </acceptance_criteria>
  <done>The add path and both destructive halves of the three-way remove (delete-child, detach-to-standalone) are implemented atomically through the cores, verified.</done>
</task>

<task type="tdd">
  <name>Task 2: Lifecycle — Dissolve vs Delete Group Event & Interactions</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (detachParticipant / deleteGroupChild from Task 1 — reuse the primitives)
    - src/db/recency-dao.ts:340-368 (deleteInteractionCore)
    - src/db/tombstones-dao.ts:61-80 (insertTombstoneCore — write the group_event tombstone inside the same txn)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §X (Dissolve vs Delete semantics)
  </read_first>
  <behavior>
    - dissolveGroupEvent: FETCH the parent uid by groupEventId inside the txn (assert it exists), for all children set group_event_id=NULL + the three ge_follow flags=NULL (values already materialized), DELETE the group_events row, write a group_event tombstone keyed by the FETCHED uid; one txn; children survive standalone; Group Note not copied.
    - deleteGroupEventAndInteractions: FETCH the parent uid by groupEventId inside the txn (assert it exists), for each child call deleteInteractionCore (tombstone + recompute), DELETE the group_events row, write a group_event tombstone keyed by the FETCHED uid; one txn.
    - Both on a zero-participant event touch no interactions and only remove the parent + write the tombstone.
    - A nonexistent groupEventId throws before any delete (no tombstone written); an injected mid-loop failure rolls the whole operation back (no partial dissolve/delete).
  </behavior>
  <action>
    Add `dissolveGroupEvent(exec, { groupEventId, now })` and `deleteGroupEventAndInteractions(exec, { groupEventId, now })` to `group-events-dao.ts`, each one `inWriteTransaction`. Each FIRST does `SELECT uid FROM group_events WHERE id = ?` inside the transaction and throws if no row — the tombstone entity_uid is DERIVED from that fetched row, NEVER from a caller-supplied uid argument (review cycle-2 MED: a mismatched caller uid could otherwise delete Group A while tombstoning Group B). Dissolve: UPDATE all children clearing group_event_id + the three ge_follow flags (no recompute — materialized), `DELETE FROM group_events WHERE id=?`, `insertTombstoneCore(exec, { entityType: "group_event", entityUid: fetchedUid, deletedAt: now }, { bumpRevision: false })`, then ONE trailing `bumpDataRevisionCore`. Delete-with-interactions: loop `deleteInteractionCore(exec, { interactionId, contactId, now }, { bumpRevision: false })` per child (children fetched by `WHERE group_event_id=?`), `DELETE FROM group_events WHERE id=?`, `insertTombstoneCore(exec, { entityType: "group_event", entityUid: fetchedUid, deletedAt: now }, { bumpRevision: false })`, then ONE trailing `bumpDataRevisionCore` (review: revision double-bump — inner cores suppress their bump so the whole lifecycle op bumps exactly once). Never FK cascade; never nest the mutex. Extend the DAO test: dissolve detaches all children (survive standalone, note kept, group_note not copied) + parent gone + tombstone written with the FETCHED uid; delete removes all children (tombstones) + parent + tombstone; both on zero-participant touch no interactions; a nonexistent groupEventId throws and writes no tombstone; injected failure rolls back fully; AND each op bumps the data revision exactly once (counter delta == 1).
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Dissolve: children detached (group_event_id + the three ge_follow flags NULL) + materialized + note kept + group_note not copied; parent deleted; group_event tombstone present, keyed by the uid FETCHED from the parent row (not a caller argument — review cycle-2 MED).
    - Delete: all children deleted (each tombstoned) + parent deleted + group_event tombstone (fetched uid); last_contact recomputed per affected contact.
    - A nonexistent groupEventId throws before any delete and writes no tombstone; zero-participant dissolve/delete touches no interactions; injected mid-loop failure → full rollback.
    - Each op bumps the data revision EXACTLY ONCE (inner deleteInteractionCore + parent insertTombstoneCore pass { bumpRevision: false }; one trailing bump) — asserted via the revision counter delta.
  </acceptance_criteria>
  <done>Dissolve and Delete Group Event & Interactions are implemented atomically with correct tombstones, recency, and a single data-revision bump, verified including rollback.</done>
</task>

<task type="tdd">
  <name>Task 3: Convert Interaction→Group Event + backup entity-shape handoff</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts, .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md</files>
  <read_first>
    - src/db/group-events-dao.ts (createGroupEvent — parent INSERT idiom to reuse)
    - src/db/recency-dao.ts (interaction row shape — the values to seed shared defaults from)
    - src/backup/export-manifest.ts:45-81 (current backup entity set + the interaction column read that omits duration/allow_ai — the pre-existing gap Phase 36 must also close; DO NOT edit this file)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §P (convert preserves id/uid, seeds shared defaults) + §AC (backup preserves links + override state; orphan preference)
    - .planning/phases/33-group-interaction-logging/33-RESEARCH.md (Pattern 5; Pitfall 6; Backup entity shape)
  </read_first>
  <behavior>
    - convertInteractionToGroupEvent creates a group_events parent with its OWN supplied/minted `uid` (distinct from the child's uid), seeding channel/quality/duration from the interaction's current values + the supplied (required, nonblank) title + the interaction's occurred_at, then an in-place UPDATE sets the interaction's group_event_id + ge_follow_channel/quality/duration=1 (the three follow-flags only — direction/connected keep the interaction's existing values with no flag, review cycle-2 HIGH #2) — the interaction's id AND uid are unchanged; no delete/recreate; one txn. It RETURNS the new parent's `groupEventId` (the local `group_events.id` from the parent INSERT) so the caller (Plan 07) can navigate to Group Event Detail (review cycle-3 LOW: convert return contract).
    - After convert, the interaction is the event's first (and only) child; addParticipant adds more.
    - A sparse interaction (no note) still converts; a blank title and an empty parent uid each reject before the transaction (no rows).
    - 33-BACKUP-HANDOFF.md exists and specifies the full Phase-36 entity shape with the parent→child link serialized as `groupEventUid` (durable, never the local `group_event_id` int), the restore order, the tombstone, and the orphan-repair rule.
  </behavior>
  <action>
    Add `convertInteractionToGroupEvent(exec, { interactionId, contactId, title, uid, now })` to `group-events-dao.ts`. The `uid` is the NEW PARENT group_events uid — supplied/minted by the caller (parent `uid TEXT NOT NULL UNIQUE`); preserving the child interaction's uid does NOT provide a parent uid, so convert takes its own (review: parent UID on convert). BEFORE the transaction, assert a non-empty trimmed `title` (reject blank/whitespace — review: title nonblank) and a non-empty `uid`. Then one `inWriteTransaction` — read the interaction's current values, INSERT a group_events parent (with the supplied `uid`; title required per §C/A2; channel/quality/duration seeded from the interaction; occurred_at = the interaction's occurred_at) and CAPTURE the new parent's local `group_events.id` (the insert rowid), then `UPDATE interactions SET group_event_id=?, ge_follow_channel=1, ge_follow_quality=1, ge_follow_duration=1, modified_at=? WHERE id=? AND contact_id=?` (the THREE follow-flags only — there is no ge_follow_direction/ge_follow_connected column; the interaction's direction/connected are left untouched, review cycle-2 HIGH #2; assert changes===1), then a single `bumpDataRevisionCore`, and RETURN the captured `groupEventId` (the new parent's local id — the caller/Plan 07 navigates to GroupEventDetail with it, mirroring createGroupEvent's returned id; review cycle-3 LOW: convert return contract). Never delete/recreate — the interaction's id/uid preserved (dossier §P). Extend the DAO test: convert mints a distinct parent uid, preserves the interaction id + uid, seeds shared values, sets follow flags, and the interaction is the sole child afterward; convert RETURNS the new parent's groupEventId (the local group_events.id) and re-reading that id yields the new parent (review cycle-3 LOW: convert return contract); a blank title and an empty uid each reject before the transaction (no rows).
    Write `33-BACKUP-HANDOFF.md` for Phase 36 — and get the LINKAGE REPRESENTATION right (review HIGH #1): the parent→child link MUST serialize as a DURABLE UID, never the local integer. Specify:
    (1) A `groupEvents[]` array keyed by `uid`, carrying the `group_events` entity columns (uid, title, occurredAt, channel, quality, duration, groupNote, createdAt, modifiedAt).
    (2) On the INTERACTION wire shape, add `groupEventUid` (the parent's durable uid, resolved on EXPORT via a JOIN `group_events ge ON ge.id = i.group_event_id` — exactly mirroring how interactions already export `contactUid`, not `contact_id`, at export-manifest.ts:52). The local `interactions.group_event_id` INTEGER is NEVER put on the wire (a local rowid cannot survive restore into a DB where parent ids differ — it would flatten/corrupt group relationships). ALSO add the durable `ge_follow_channel|ge_follow_quality|ge_follow_duration` flags (EXACTLY THREE — direction/connected are ordinary participant columns with no follow state, review cycle-2 HIGH #2; they ride on the wire as the existing interaction fields, not as follow-flags), PLUS close the pre-existing gap by finally serializing `duration` and `allow_ai` (both omitted today at export-manifest.ts:52).
    (3) The `group_event` tombstone — already serialized by the existing generic tombstone export at export-manifest.ts:62 (SELECT entity_type, entity_uid, deleted_at) once Plan 01 adds the `"group_event"` type; Phase 36 need only ensure restore honors it so a deleted/dissolved parent is never resurrected.
    (4) RESTORE ORDER: upsert parents by `uid` first, build a uid→local-id map, THEN upsert children resolving `groupEventUid` → the local `group_events.id`.
    (5) ORPHAN HANDLING (A3, OWNER-PENDING — confirm before Phase 36): a restored child whose `groupEventUid` is absent from `groupEvents[]` has its local link cleared (`group_event_id` NULL) AND all `ge_follow_*` cleared, surviving as a standalone interaction — never dropped (dossier §AC).
    State explicitly that Phase 33 does NOT bump the backup format or edit export-manifest.ts/restore-apply.ts — it hands over this shape, the tombstone type, and the orphan rule; Phase 36's final plan executes them.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - convert mints a distinct parent uid, preserves interaction id AND uid (assert unchanged), seeds shared values from the interaction, sets ge_follow_channel/quality/duration=1 (three flags; direction/connected untouched), and the interaction is the sole child; convert RETURNS the new parent's `groupEventId` (local group_events.id), which re-reads to the new parent (review cycle-3 LOW: convert return contract); a blank title and an empty uid each reject before the txn.
    - `33-BACKUP-HANDOFF.md` serializes the parent→child link as `groupEventUid` (durable) and explicitly states the local `group_event_id` int is NEVER on the wire; lists a `groupEvents[]` array keyed by uid, the interaction additive columns (ge_follow_* + the duration/allow_ai gap), the restore order (parents by uid → uid→id map → children), the group_event tombstone, the A3 orphan rule (marked owner-pending), and the no-format-bump / no-file-edit boundary.
    - No edit to export-manifest.ts / restore-apply.ts in this phase (git diff confirms).
  </acceptance_criteria>
  <done>Interaction→Group Event conversion preserves identity via an in-place UPDATE, and Phase 36 has a complete, verified backup entity-shape + orphan-repair handoff spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle mutation → many child rows + tombstones | dissolve/delete/convert fan out across interactions + group_events |
| delete → backup merge | tombstone must exist or restore resurrects the entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-13 | Tampering / integrity | partial dissolve/delete leaves orphaned or half-deleted state | high | mitigate | One inWriteTransaction; compose deleteInteractionCore/detach in a loop; full rollback (GRP-10/GRP-11) |
| T-33-14 | Repudiation / data loss | deleted group event resurrected on restore-merge | medium | mitigate | Write a `group_event` tombstone in the same txn as the delete/dissolve |
| T-33-15 | Tampering | convert delete/recreates and loses identity | medium | mitigate | In-place UPDATE preserving id + uid; never delete/recreate (dossier §P) |
| T-33-16 | Information Disclosure | detach copies Group Note into a standalone note | medium | mitigate | detach/dissolve never copy group_note (dossier §O) |
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` passes.
- No FK cascade reliance; no nested transactions; every child write through the recency cores.
- `export-manifest.ts` / `restore-apply.ts` untouched (D-10).
</verification>

<success_criteria>
The full Group Event mutation surface (add/remove three-way, dissolve, delete-with-interactions, single-child delete, convert) is atomic and correct through the recency spine, and Phase 36 has a decided backup entity-shape + orphan-repair handoff (A3 flagged owner-pending).
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-05-SUMMARY.md` when done.
</output>
