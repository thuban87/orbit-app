---
phase: 33-group-interaction-logging
plan: 05
type: execute
wave: 3
depends_on: ["33-03"]
files_modified:
  - src/db/group-events-dao.ts
  - src/db/group-events-dao.test.ts
  - .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md
autonomous: true
requirements: [GRP-06, GRP-07, GRP-10, GRP-13]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "addParticipant stamps the new child's occurred_at at the event's date/time (NOT the add time) and seeds the event's CURRENT resolved shared values with ge_follow_*=1 (GRP-06 boundary, dossier §M)."
    - "The added child's occurred_at equals the event occurred_at string verbatim (local wall-clock, no timezone conversion via formatLocalDate/localDateTime) (GRP-06 precision)."
    - "Adding a contact already in the event is rejected by UNIQUE(group_event_id, contact_id); no duplicate child is created (GRP-06 idempotency)."
    - "Adding an archived contact succeeds, advancing their last_contact, with no restore prompt and no added guard (D-07)."
    - "Remove-participant is three-way: 'Delete interaction' = deleteGroupChild (deleteInteractionCore: tombstone + recency recompute); 'Keep as individual interaction' = detachParticipant (group_event_id=NULL, ge_follow_*=NULL, values already materialized, note preserved, Group Note NOT copied); 'Cancel' = no-op (GRP-06, D-11, dossier §N/§O)."
    - "detachParticipant needs no recency recompute (values already materialized, occurred_at unchanged) and never copies group_note into the participant note (dossier §O)."
    - "addParticipant and every three-way-remove branch each run in exactly one inWriteTransaction; an interrupted/failed add or remove rolls back fully with no partial child or dangling link (GRP-06 concurrency, GRP-11)."
    - "Dissolve detaches ALL children (group_event_id=NULL, ge_follow_*=NULL), then DELETEs the parent and writes a group_event tombstone; children survive standalone with materialized values (GRP-10 adjacency, dossier §X)."
    - "Delete Group Event & Interactions deletes every child via deleteInteractionCore (tombstone + recompute each), then DELETEs the parent + writes a group_event tombstone (GRP-10 adjacency, dossier §X)."
    - "Dissolving/deleting a zero-participant event removes only the parent (+ tombstone), touching no interactions (GRP-10 empty)."
    - "Delete-with-interactions loops children then the parent in one transaction; child-loop order does not affect the fully-rolled-back-on-failure guarantee (GRP-10 ordering, GRP-11)."
    - "Dissolve and delete each write a group_event tombstone so restore-merge never resurrects the parent (GRP-10 idempotency)."
    - "Dissolve and delete each run in exactly one inWriteTransaction; partial dissolve/delete is never visible (GRP-10 concurrency, GRP-11)."
    - "deleteGroupChild removes only that participant's child; other participants are unaffected and the event may become zero-participant and remain valid (dossier §Y)."
    - "Convert preserves the ordinary interaction's id and uid via an in-place UPDATE (never delete/recreate); it creates a group_events parent seeding shared values from the interaction's current values and sets the interaction's group_event_id + ge_follow_*=1 (GRP-07 adjacency, dossier §P)."
    - "Convert with no note/sparse values still seeds shared defaults from the interaction; the required event title is supplied by the convert flow (event-only, required — A2 resolved: prompt for title) (GRP-07 empty)."
    - "After convert, the original interaction becomes the first child; further participants are added via addParticipant (GRP-07 ordering)."
    - "The backup handoff spec (33-BACKUP-HANDOFF.md) lists every group entity column + the group_event tombstone; a restored child whose parent is present relinks by uid; override/inheritance state (ge_follow_*) and shared values survive without flattening (GRP-13 adjacency/ordering, dossier §AC)."
  artifacts:
    - "DAO addParticipant, detachParticipant, deleteGroupChild, dissolveGroupEvent, deleteGroupEventAndInteractions, convertInteractionToGroupEvent"
    - .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md
  key_links:
    - "deleteGroupEventAndInteractions -> deleteInteractionCore (per child, tombstone+recompute) -> DELETE parent -> group_event tombstone, one txn"
    - "convertInteractionToGroupEvent -> in-place UPDATE interactions (id/uid preserved) + new group_events parent"
    - "33-BACKUP-HANDOFF.md -> Phase 36 restore/orphan-repair path (executed there, defined here)"
  prohibitions:
    - "Deleting a group event must never rely on FK ON DELETE CASCADE (bypasses recency recompute + tombstone)."
    - "Detach / Keep-as-individual must never copy the Group Note into the participant note."
    - "A dissolve/delete/convert must never nest inWriteTransaction or bulk-mutate children outside the recency cores."
---

<objective>
Complete the Group Event mutation surface: participant add/remove (the three-way Delete / Keep-as-individual / Cancel), the two lifecycle operations (Dissolve vs Delete Group Event & Interactions), single-child delete, and Interaction→Group Event conversion — all atomic through the recency cores (D-05, GRP-11). Then write the backup entity-shape handoff spec for Phase 36 (D-10 — this phase does NOT bump the backup format or edit export-manifest.ts/restore-apply.ts; it hands over the shape, the tombstone, and the orphan-repair rule).

Purpose: GRP-06 (add/remove), GRP-07 (convert), GRP-10 (dissolve/delete), GRP-13 (backup shape + orphan rule).
Output: `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent`; `33-BACKUP-HANDOFF.md`.

Phase tracer note: the phase tracer is in Plan 01; this plan expands the DAO with lifecycle + convert on the proven spine.
</objective>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Table `group_events`; `interactions.group_event_id` + `ge_follow_*`; `idx_group_member_unique`; `migration026`; tombstone `"group_event"`; extended `insertInteractionCore`; `createGroupEvent` (Plan 01)
- Pure logic `group-inheritance.ts`; DAO shared/date edits + override set/clear (Plan 03)
- Reads `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants` (Plan 04)
- ★ DAO `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent`
- ★ `33-BACKUP-HANDOFF.md` (Phase-36 entity-shape + tombstone + orphan-repair spec)
- `ContactPicker` multi-select + route types (Plan 02); screens + wired seams (Plans 06/07)

<flagged_assumption id="A3-orphan-repair" owner_pending="true">
**A3 — restore orphan-repair outcome (D-10). OWNER-PENDING — do NOT implement this phase.** Recommended default: a restored child interaction whose parent `group_events` row is missing is **detached to standalone** (`group_event_id` cleared, ge_follow_* cleared), never dropped — dossier §AC prefers preserving a valid child Interaction as standalone over losing contact history. D-10 requires this outcome be a *decided* rule; the rule is **executed by Phase 36's restore path**, not here. This phase only (a) defines the `group_events` entity shape, (b) adds the `group_event` tombstone type (Plan 01), and (c) records this recommended outcome in `33-BACKUP-HANDOFF.md`. Confirm A3 with the owner before Phase 36 so it becomes a locked Phase-36 rule. Do NOT edit `export-manifest.ts` / `restore-apply.ts` in Phase 33.
</flagged_assumption>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-RESEARCH.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md
@src/db/group-events-dao.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Participant add + three-way remove building blocks (addParticipant, detachParticipant, deleteGroupChild)</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (createGroupEvent / edit fan-outs — same composition idiom)
    - src/db/recency-dao.ts:340-368 (deleteInteractionCore — tombstone BEFORE delete, then recompute)
    - src/db/recency-dao.ts:193-236,446-460 (insertInteractionCore + aliases)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §M/§N/§O (add stamps event date/time; three-way remove; detach materializes, never copies Group Note)
    - docs/dossier/milestone-2/planning-notes/phase-12-planning-notes.md R-02 constraint 2 (archived participants allowed, no guard — D-07)
  </read_first>
  <behavior>
    - addParticipant reads the event's current resolved shared values + occurred_at, inserts one child via insertInteractionCore (occurred_at = event occurred_at, resolved values materialized, ge_follow_*=1, group_event_id set), recomputes last_contact, bumps revision — one txn.
    - Adding an already-present contact throws on the UNIQUE index and rolls back (no duplicate).
    - Adding an archived contact succeeds (last_contact advances; no restore prompt).
    - deleteGroupChild calls deleteInteractionCore (tombstone + recompute); other participants unaffected; event may become zero-participant.
    - detachParticipant sets group_event_id=NULL, ge_follow_*=NULL, modified_at=now (values already materialized, no recompute), preserves the note, does NOT copy group_note; bumps revision.
  </behavior>
  <action>
    Add `addParticipant(exec, { groupEventId, contactId, uid, direction?, connected?, note?, now })`, `deleteGroupChild(exec, { interactionId, contactId, now })`, and `detachParticipant(exec, { interactionId, now })` to `group-events-dao.ts`, each one `inWriteTransaction`. addParticipant reads the event's resolved shared values + occurred_at, then `insertInteractionCore` (event occurred_at per §M — never the add time; materialized shared values; ge_follow_*=1; group_event_id) + `recomputeLastContactCore` + `bumpDataRevisionCore`. Do NOT add an archived guard (D-07). deleteGroupChild = `deleteInteractionCore` + `bumpDataRevisionCore` (never raw DELETE, never FK cascade). detachParticipant = one UPDATE clearing group_event_id + ge_follow_* + set modified_at (`?`-bound), then `bumpDataRevisionCore` — no recompute, and never copy group_note into the note (dossier §O). Extend `group-events-dao.test.ts`: add stamps event occurred_at; duplicate add rejected; archived add advances last_contact; deleteGroupChild leaves peers intact; detach materializes + preserves note + does not copy group_note.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - addParticipant child occurred_at == event occurred_at; duplicate rejected; archived add advances last_contact.
    - deleteGroupChild removes only that child (tombstone written); peers intact.
    - detachParticipant clears group_event_id + ge_follow_*, keeps note, does not copy group_note, no recency change.
  </acceptance_criteria>
  <done>The add path and both destructive halves of the three-way remove (delete-child, detach-to-standalone) are implemented atomically through the cores, verified.</done>
</task>

<task type="tdd">
  <name>Task 2: Lifecycle — Dissolve vs Delete Group Event & Interactions</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (detachParticipant / deleteGroupChild from Task 1 — reuse the primitives)
    - src/db/recency-dao.ts:340-368 (deleteInteractionCore)
    - src/db/tombstones-dao.ts:61-80 (insertTombstoneCore — write the group_event tombstone inside the same txn)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §X (Dissolve vs Delete semantics)
  </read_first>
  <behavior>
    - dissolveGroupEvent: for all children set group_event_id=NULL + ge_follow_*=NULL (values already materialized), DELETE the group_events row, write a group_event tombstone (keyed by the event uid); one txn; children survive standalone; Group Note not copied.
    - deleteGroupEventAndInteractions: for each child call deleteInteractionCore (tombstone + recompute), DELETE the group_events row, write a group_event tombstone; one txn.
    - Both on a zero-participant event touch no interactions and only remove the parent + write the tombstone.
    - An injected mid-loop failure rolls the whole operation back (no partial dissolve/delete).
  </behavior>
  <action>
    Add `dissolveGroupEvent(exec, { groupEventId, uid, now })` and `deleteGroupEventAndInteractions(exec, { groupEventId, uid, now })` to `group-events-dao.ts`, each one `inWriteTransaction`. Dissolve: UPDATE all children clearing group_event_id + ge_follow_* (no recompute — materialized), `DELETE FROM group_events WHERE id=?`, `insertTombstoneCore({ entityType: "group_event", uid })`, `bumpDataRevisionCore`. Delete-with-interactions: loop `deleteInteractionCore` per child, `DELETE FROM group_events`, `insertTombstoneCore({ entityType: "group_event", uid })`, `bumpDataRevisionCore`. Never FK cascade; never nest the mutex. Extend the DAO test: dissolve detaches all children (survive standalone, note kept, group_note not copied) + parent gone + tombstone written; delete removes all children (tombstones) + parent + tombstone; both on zero-participant touch no interactions; injected failure rolls back fully.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Dissolve: children detached + materialized + note kept + group_note not copied; parent deleted; group_event tombstone present.
    - Delete: all children deleted (each tombstoned) + parent deleted + group_event tombstone; last_contact recomputed per affected contact.
    - Zero-participant dissolve/delete touches no interactions; injected mid-loop failure → full rollback.
  </acceptance_criteria>
  <done>Dissolve and Delete Group Event & Interactions are implemented atomically with correct tombstones and recency, verified including rollback.</done>
</task>

<task type="tdd">
  <name>Task 3: Convert Interaction→Group Event + backup entity-shape handoff</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts, .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md</files>
  <read_first>
    - src/db/group-events-dao.ts (createGroupEvent — parent INSERT idiom to reuse)
    - src/db/recency-dao.ts (interaction row shape — the values to seed shared defaults from)
    - src/backup/export-manifest.ts:45-81 (current backup entity set + the interaction column read that omits duration/allow_ai — the pre-existing gap Phase 36 must also close; DO NOT edit this file)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §P (convert preserves id/uid, seeds shared defaults) + §AC (backup preserves links + override state; orphan preference)
    - .planning/phases/33-group-interaction-logging/33-RESEARCH.md (Pattern 5; Pitfall 6; Backup entity shape)
  </read_first>
  <behavior>
    - convertInteractionToGroupEvent creates a group_events parent seeding channel/quality/duration from the interaction's current values + the supplied (required) title + the interaction's occurred_at, then an in-place UPDATE sets the interaction's group_event_id + ge_follow_*=1 — the interaction's id AND uid are unchanged; no delete/recreate; one txn.
    - After convert, the interaction is the event's first (and only) child; addParticipant adds more.
    - A sparse interaction (no note) still converts; a missing title is a required input (supplied by the caller/flow).
    - 33-BACKUP-HANDOFF.md exists and specifies the full Phase-36 entity shape + tombstone + orphan-repair rule.
  </behavior>
  <action>
    Add `convertInteractionToGroupEvent(exec, { interactionId, contactId, title, now })` to `group-events-dao.ts`: one `inWriteTransaction` — read the interaction's current values, INSERT a group_events parent (title required per §C/A2; channel/quality/duration seeded from the interaction; occurred_at = the interaction's occurred_at), then `UPDATE interactions SET group_event_id=?, ge_follow_channel=1, ge_follow_quality=1, ge_follow_duration=1, ge_follow_direction=1, ge_follow_connected=1, modified_at=? WHERE id=? AND contact_id=?` (assert changes===1), `bumpDataRevisionCore`. Never delete/recreate — id/uid preserved (dossier §P). Extend the DAO test: convert preserves id + uid, seeds shared values, sets follow flags, and the interaction is the sole child afterward.
    Write `33-BACKUP-HANDOFF.md` for Phase 36: (1) the `group_events` entity columns (uid, title, occurredAt, channel, quality, duration, groupNote, createdAt, modifiedAt); (2) the interaction additive columns Phase 36 must add to the backup read together — `group_event_id`, `ge_follow_channel|quality|duration|direction|connected`, PLUS the pre-existing gap `duration` and `allow_ai` (currently omitted at export-manifest.ts:52); (3) the `group_event` tombstone (serialized in the export-manifest tombstone idiom by Phase 36); (4) the orphan-repair rule per A3 (recommended: detach orphaned child to standalone, never drop — OWNER-PENDING, confirm before Phase 36); (5) restore preserves links + override state without flattening. State explicitly that Phase 33 does NOT bump the format or edit export-manifest.ts/restore-apply.ts.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - convert preserves interaction id AND uid (assert unchanged), seeds shared values from the interaction, sets ge_follow_*=1, and the interaction is the sole child.
    - `33-BACKUP-HANDOFF.md` lists the group_events columns, the interaction additive columns (incl. the duration/allow_ai gap), the group_event tombstone, the A3 orphan rule (marked owner-pending), and the no-format-bump / no-file-edit boundary.
    - No edit to export-manifest.ts / restore-apply.ts in this phase (git diff confirms).
  </acceptance_criteria>
  <done>Interaction→Group Event conversion preserves identity via an in-place UPDATE, and Phase 36 has a complete, verified backup entity-shape + orphan-repair handoff spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle mutation → many child rows + tombstones | dissolve/delete/convert fan out across interactions + group_events |
| delete → backup merge | tombstone must exist or restore resurrects the entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-13 | Tampering / integrity | partial dissolve/delete leaves orphaned or half-deleted state | high | mitigate | One inWriteTransaction; compose deleteInteractionCore/detach in a loop; full rollback (GRP-10/GRP-11) |
| T-33-14 | Repudiation / data loss | deleted group event resurrected on restore-merge | medium | mitigate | Write a `group_event` tombstone in the same txn as the delete/dissolve |
| T-33-15 | Tampering | convert delete/recreates and loses identity | medium | mitigate | In-place UPDATE preserving id + uid; never delete/recreate (dossier §P) |
| T-33-16 | Information Disclosure | detach copies Group Note into a standalone note | medium | mitigate | detach/dissolve never copy group_note (dossier §O) |
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` passes.
- No FK cascade reliance; no nested transactions; every child write through the recency cores.
- `export-manifest.ts` / `restore-apply.ts` untouched (D-10).
</verification>

<success_criteria>
The full Group Event mutation surface (add/remove three-way, dissolve, delete-with-interactions, single-child delete, convert) is atomic and correct through the recency spine, and Phase 36 has a decided backup entity-shape + orphan-repair handoff (A3 flagged owner-pending).
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-05-SUMMARY.md` when done.
</output>
