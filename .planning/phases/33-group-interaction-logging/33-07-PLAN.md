---
phase: 33-group-interaction-logging
plan: 07
type: execute
wave: 5
depends_on: ["33-02", "33-04", "33-05", "33-06"]
files_modified:
  - src/screens/GroupEventDetailScreen.tsx
  - src/screens/GroupEventsScreen.tsx
  - src/components/group/ParticipantCard.tsx
  - src/components/group/GroupTitlePromptSheet.tsx
  - src/components/history/GroupScopePrompt.tsx
  - src/components/history/InteractionDetail.tsx
  - src/components/history/HistorySection.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
  - src/navigation/tabs/SettingsStack.tsx
autonomous: true
requirements: [GRP-07, GRP-08, GRP-09, GRP-10]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Group Event Detail is presentation-first (read surface, not focused): title, date/time, Channel, Tone, Duration, Group Note, participant list, and Edit Group Event / Add Participant / lifecycle actions via overflow (GRP-08, dossier §R)."
    - "Participant cards are compact (avatar, name, concise override/inherit summary by text+icon, participant overflow with Edit participant record / Remove from group); 'Remove from group' opens the SHARED three-option RemoveParticipantSheet built in Plan 06 (Delete interaction / Keep as individual interaction / Cancel), wired here to deleteGroupChild/detachParticipant — not a screen-local re-implementation (review cycle-2 MED: detail-card remove Sheet); tapping a card opens that participant's child Interaction Detail (GRP-08, dossier §S)."
    - "GroupEventDetail is registered as a screen in DashboardStack, OrreryStack, AND SettingsStack — it is navigated from HistorySection's 'View Group Event' inside a profile hosted in all three stacks, so Dashboard-only registration type-checks but crashes at runtime from an Orrery/Settings-hosted profile (review cycle-2 HIGH #1)."
    - "A populated Detail renders participant cards; a zero-participant Detail shows 'No participants yet' + body, never an error (GRP-08 empty/populated, UI-SPEC)."
    - "Participant cards render in a deterministic stable order (child interaction id / add order); large sets virtualize (FlatList) (GRP-08 ordering)."
    - "The browse page is a lean reverse-chronological list searchable by title + participant, reachable from the Dashboard header (first-class icon+label) and Dashboard overflow (GRP-09, dossier §T; entries already routed SHELL-12/DASHC-02/08)."
    - "Empty browse shows 'No group events yet' + the documented body; empty search shows 'No group events match \"{term}\".' — searches title + participant name (GRP-09 empty/adjacency, UI-SPEC)."
    - "Browse list order is reverse-chronological by occurred_at (GRP-09 ordering) driven by listGroupEvents/searchGroupEvents from the read layer."
    - "Interaction Detail recognizes group membership once group_event_id exists — via the group fields Plan 01 added to HistoryInteractionRecord (groupEventId/groupTitle/groupNote), so buildGroupContext returns non-null: a restrained 'Group event' badge, the Group Note (labelled), the participant note, and a 'View Group Event' action (accentText link). The 'View Group Event' button routes to the GroupEventDetail ROUTE (it currently calls onEdit — a wiring bug fixed here), via a new onViewGroupEvent(groupEventId) callback HistorySection supplies (GRP-08, dossier §AD; review HIGH #2)."
    - "InteractionDetail gains explicit navigation callbacks (onViewGroupEvent, onEditGroupEvent, onEditParticipant, onConvertToGroup) that HistorySection wires to real navigation using interaction.groupEventId — it no longer overloads the single onEdit for edit + view + group scope (review HIGH #2/#3). HistorySection is in the edit file-list, not just read-first."
    - "Editing a group-linked interaction first asks scope via GroupScopePrompt: 'Edit individual interaction' navigates to the EditParticipant route (the participant override editor's owner, registered in Plan 06); 'Edit Group Event' navigates to the EditGroupEvent route — never a hybrid implicit-scope editor, and never a dead-end (the current onEditGroup is a no-op placeholder, fixed here) (D-11, dossier §Q; review HIGH #3)."
    - "'Make this a group interaction' in Interaction Detail overflow converts the ordinary interaction (preserving its id/uid) via convertInteractionToGroupEvent, prompting for the required (nonblank) event title through a reusable IN-APP input Sheet (GroupTitlePromptSheet — a Sheet + single TextInput + confirm/cancel, nonblank-enforced), NOT a platform prompt (Alert.prompt is iOS-only; this app is Android-first — review cycle-2 MED: Android conversion title entry), then minting a fresh PARENT uid and navigating to the new Group Event Detail (GRP-07, dossier §P, A2)."
    - "Dissolve and Delete Group Event & Interactions are ConfirmDialog destructive (danger fill + warning glyph, no scrim-dismiss) with the documented consequence-naming copy and confirm-button labels; each routes to dissolveGroupEvent / deleteGroupEventAndInteractions (GRP-10, dossier §X/§Z, UI-SPEC)."
    - "Deleting one child from a participant card affects only that participant; the event may become zero-participant and remain valid (GRP-10, dossier §Y)."
    - {statement: "Large participant sets on Detail and long titles/Group Notes/contact names reflow (grow height, virtualized list) rather than truncating critical semantics — held-out device visual check at a realistic large count.", verification: backstop}
  artifacts:
    - src/screens/GroupEventDetailScreen.tsx
    - "GroupEventDetail screen registered in DashboardStack + OrreryStack + SettingsStack (review cycle-2 HIGH #1)"
    - "replaced GroupEventsScreen browse page"
    - src/components/group/ParticipantCard.tsx
    - "src/components/group/GroupTitlePromptSheet.tsx — reusable in-app Sheet + TextInput for the nonblank convert title (Android-compatible; not a platform prompt — review cycle-2 MED)"
    - "InteractionDetail navigation callbacks (onViewGroupEvent/onEditGroupEvent/onEditParticipant/onConvertToGroup) + activated group block + fixed 'View Group Event' routing"
    - "HistorySection wires those callbacks to the GroupEventDetail/EditGroupEvent/EditParticipant routes using the group fields on HistoryInteractionRecord"
    - "wired GroupScopePrompt (onEditIndividual → EditParticipant, onEditGroup → EditGroupEvent) + 'Make this a group interaction' convert entry (via GroupTitlePromptSheet)"
  key_links:
    - "GroupEventsScreen -> listGroupEvents/searchGroupEvents; Detail -> readGroupEventDetail"
    - "ParticipantCard tap -> child InteractionDetail; overflow 'Edit participant record' -> EditParticipant route; 'Remove from group' -> shared RemoveParticipantSheet (Plan 06) -> deleteGroupChild / detachParticipant"
    - "InteractionDetail 'View Group Event' -> onViewGroupEvent -> GroupEventDetail route (fixes the current onEdit mis-wire); GroupScopePrompt onEditIndividual -> EditParticipant route, onEditGroup -> EditGroupEvent route"
    - "Interaction Detail overflow 'Make this a group interaction' -> title prompt + mint parent uid -> convertInteractionToGroupEvent -> GroupEventDetail"
    - "HistorySection supplies the navigation callbacks to InteractionDetail (reads interaction.groupEventId from the record Plan 01 populated)"
    - "DashboardStack + OrreryStack + SettingsStack each register GroupEventDetail (review cycle-2 HIGH #1); DashboardStack hosts the real GroupEventsScreen browse body (Dashboard-only)"
  prohibitions:
    - "A Group Event parent must never appear as a second history row or increment any count (the badge/context is read from the child only)."
    - "Editing a group-linked interaction must never drop the user into a hybrid implicit-scope editor — the scope prompt is mandatory."
---

<objective>
Deliver the presentation and discovery surfaces and activate the dormant Phase-32 seams: the presentation-first Group Event Detail (with compact participant cards), the lean reverse-chronological browse page (title + participant search, Dashboard header + overflow entry), the Interaction Detail group-context block + "View Group Event", the `GroupScopePrompt` wiring (individual vs Group Event edit), the "Make this a group interaction" convert entry, and the Dissolve / Delete destructive confirmations. Everything reuses shipped primitives and the read/DAO layers from earlier plans.

Purpose: GRP-08 (Detail presentation-first + participant cards), GRP-09 (browse), GRP-07 (convert UI entry), GRP-10 (dissolve/delete confirmations + single-child delete).
Output: `GroupEventDetailScreen`, replaced `GroupEventsScreen`, `ParticipantCard`; wired `GroupScopePrompt` + `InteractionDetail` group block + convert entry; DashboardStack registration.

Phase tracer note: the phase tracer is in Plan 01; this final plan activates the UI/history seams over the proven data spine.
</objective>

<review_findings_addressed>
Cross-AI review cycle 2 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-2 HIGH #1 (cross-stack route registration — consensus): Task 2 registers GroupEventDetail in DashboardStack, OrreryStack, AND SettingsStack (it is navigated from HistorySection's 'View Group Event' inside a profile hosted in all three stacks). The route TYPE lands in Plan 02; EditGroupEvent/EditParticipant screens register in Plan 06.
- Cycle-2 MED (Android conversion title entry): Task 3 creates a reusable in-app `GroupTitlePromptSheet` (Sheet + TextInput + nonblank-enforced confirm) for the convert title — not `Alert.prompt` (iOS-only).
- Cycle-2 MED (detail-card remove Sheet): Task 2's ParticipantCard "Remove from group" reuses the SHARED `RemoveParticipantSheet` built in Plan 06, wired to deleteGroupChild/detachParticipant — no screen-local re-implementation.
- Cycle-2 HIGH #2/#5 ripple: dissolve/delete call the no-uid DAO signatures; Detail renders from the three-follow-flag resolveParticipants.

Cycle-1 findings (preserved, not undone):
- HIGH #2 (group-context activation): `history-read.ts` is edited in Plan 01 (adds group_event_id + JOIN + groupEventId/groupTitle/groupNote), so buildGroupContext returns non-null here. Task 3 threads real group context + navigation callbacks into InteractionDetail via HistorySection (in the edit list, not read-first only).
- HIGH #3 (Edit individual has a navigation contract): Task 3 wires GroupScopePrompt.onEditIndividual → EditParticipant, onEditGroup → EditGroupEvent, and FIXES the "View Group Event" → onEdit mis-wire and the onEditGroup no-op. New explicit navigation callbacks replace the overloaded onEdit.
- Non-HIGH (UI verification beyond check:colors): every task adds `npx tsc --noEmit`.
- Non-HIGH (parent UID on convert): the convert entry mints a fresh parent uid (Plan 05 contract) and captures a nonblank title.
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Data spine (Plan 01); inheritance + edits (Plan 03); reads (Plan 04); lifecycle + convert + backup handoff (Plan 05); picker multi-select + routes (Plan 02); Group Log + Edit forms + override editor (Plan 06)
- ★ Screen `GroupEventDetailScreen` (registered in DashboardStack + OrreryStack + SettingsStack — review cycle-2 HIGH #1); replaced `GroupEventsScreen` browse body (Dashboard-only); component `ParticipantCard` (reuses the shared `RemoveParticipantSheet` from Plan 06)
- ★ `src/components/group/GroupTitlePromptSheet.tsx` — reusable in-app convert title Sheet (Android-compatible; review cycle-2 MED)
- ★ `InteractionDetail` navigation-callback props (onViewGroupEvent/onEditGroupEvent/onEditParticipant/onConvertToGroup) + activated group block + fixed "View Group Event" routing
- ★ `HistorySection` wiring of those callbacks to GroupEventDetail/EditGroupEvent/EditParticipant routes (reads the group fields Plan 01 added to `HistoryInteractionRecord`)
- ★ Wired `GroupScopePrompt` callbacks; "Make this a group interaction" convert entry (via GroupTitlePromptSheet); GroupEventDetail registration in all three Profile-hosting stacks + real `GroupEventsScreen` in DashboardStack
- (consumed, owned by Plan 01) `HistoryInteractionRecord.groupEventId/groupTitle/groupNote` + `history-read.ts` LEFT JOIN group_events (local presentation read)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-UI-SPEC.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@src/db/group-events-read.ts
@src/db/group-events-dao.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group Events browse page (reverse-chron, title+participant search, Dashboard entry)</name>
  <files>src/screens/GroupEventsScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <read_first>
    - src/screens/GroupEventsScreen.tsx:8-26 (the placeholder shell: ShellAppBar variant="child", ChromeScrim — replace the "Coming soon" body only)
    - src/db/group-events-read.ts (listGroupEvents, searchGroupEvents)
    - src/components/ListRow.tsx / src/components/MemoryCard.tsx (compact-row idiom for list rows)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting: empty 'No group events yet' + body, search no-results; list row content)
    - (Dashboard header + overflow entries are already routed per SHELL-12/DASHC-02/08 — confirm they navigate to 'GroupEvents')
  </read_first>
  <action>
    Replace the `GroupEventsScreen` "Coming soon" body with a reverse-chronological `FlatList` fed by `listGroupEvents`, plus a collapsible search field driving `searchGroupEvents` (title + participant name). Each row (ListRow/MemoryCard idiom) shows the event title, date/time (via `formatLocalDate`/`localDateTime`), and a participant count/summary; tapping a row navigates to `GroupEventDetail`. Empty list → "No group events yet" heading + the documented body; empty search → "No group events match \"{term}\".". Keep the existing `ShellAppBar variant="child"` + `ChromeScrim`. Verify the Dashboard header icon+label and Dashboard overflow both route here (already wired in earlier phases); no bottom-nav tab is added (dossier §T). All colours via tokens; virtualize the list.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel: reach Group Events from the Dashboard header icon+label AND from Dashboard overflow. With no events → "No group events yet". Create a few events (via FAB → Group Log), confirm reverse-chron order, search by a title and by a participant name (both match; deduped), and a no-match term shows the no-results copy.</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes; `npx tsc --noEmit` passes (GroupEventDetail navigation from rows type-checks — review: UI verification beyond check:colors); GroupEvents route resolves to the real browse body.
    - Device: reachable from header + overflow; reverse-chron order; title+participant search; documented empty + no-results copy.
  </acceptance_criteria>
  <done>The lean reverse-chronological, title+participant-searchable Group Events browse page is live and reachable from the Dashboard header and overflow.</done>
</task>

<task type="auto">
  <name>Task 2: Group Event Detail + participant cards + dissolve/delete confirmations</name>
  <files>src/screens/GroupEventDetailScreen.tsx, src/components/group/ParticipantCard.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx, src/navigation/tabs/SettingsStack.tsx</files>
  <read_first>
    - src/db/group-events-read.ts (readGroupEventDetail, resolveParticipants)
    - src/logic/group-inheritance.ts (resolveDisplay — per-participant inherit/override summary for Channel/Tone/Duration; direction/connected are plain values)
    - src/components/ListRow.tsx + src/components/ui/OverflowMenu.tsx (participant card idiom + overflow)
    - src/components/ui/ConfirmDialog.tsx (destructive variant: danger fill + warning glyph, no scrim-dismiss)
    - src/components/group/RemoveParticipantSheet.tsx (the SHARED three-option remove Sheet built in Plan 06 — reuse it here, do not re-implement)
    - src/db/group-events-dao.ts (dissolveGroupEvent({ groupEventId, now }), deleteGroupEventAndInteractions({ groupEventId, now }) — uid is now fetched internally; deleteGroupChild({ groupEventId, interactionId, contactId, now }), detachParticipant({ groupEventId, interactionId, now }))
    - src/navigation/tabs/OrreryStack.tsx + src/navigation/tabs/SettingsStack.tsx (Profile registered in each; register GroupEventDetail here too, mirroring EditInteraction — review cycle-2 HIGH #1)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting: Dissolve/Delete confirmation copy + confirm labels; three-way remove; Color: single Primary accent, danger for destructive)
  </read_first>
  <action>
    Create `GroupEventDetailScreen.tsx` (presentation-first read route, NOT focused): render title, date/time, Channel, Tone, Duration, the labelled Group Note, and a virtualized participant list of `ParticipantCard`s; overflow exposes Edit Group Event (→ EditGroupEvent), Add Participant (→ multi-select picker → addParticipant), Dissolve, and Delete Group Event & Interactions. Create `ParticipantCard.tsx` (ListRow idiom): avatar + name + a concise inherit-vs-override summary (text+icon via `resolveDisplay`, never colour alone) + a participant `OverflowMenu` (Edit participant record → navigate the `EditParticipant` route `{ groupEventId, interactionId, contactId }`, the EditParticipantScreen owner from Plan 06; Remove from group → the SHARED `RemoveParticipantSheet` from Plan 06, wiring onDelete→`deleteGroupChild({ groupEventId, interactionId, contactId, now })` and onKeep→`detachParticipant({ groupEventId, interactionId, now })` — review cycle-2 MED: reuse the shared Sheet, do not re-implement a screen-local one); tapping the card opens that participant's child `InteractionDetail`. Zero-participant Detail shows "No participants yet" + body. Wire Dissolve and Delete to `ConfirmDialog` destructive (danger fill + warning glyph, no scrim-dismiss) with the exact documented copy and confirm-button labels ("Dissolve group event" / "Delete group & interactions"), routing to `dissolveGroupEvent({ groupEventId, now })` / `deleteGroupEventAndInteractions({ groupEventId, now })` (the DAO fetches the parent uid internally — pass no uid; Plan 05/MED). Register `GroupEventDetail` in `DashboardStack.tsx`, `OrreryStack.tsx`, AND `SettingsStack.tsx` (reachable via 'View Group Event' from a profile hosted in all three stacks — review cycle-2 HIGH #1). All colours via tokens; portrait-only.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel: open a Group Event Detail — title/date/Channel/Tone/Duration/Group Note render; participant cards show inherit-vs-override summaries; tapping a card opens that contact's child Interaction Detail; a card's overflow "Edit participant record" opens the EditParticipant screen; "Remove from group" opens the three-option Sheet. Overflow → Dissolve shows the consequence-naming confirmation; confirm → children survive standalone (check a contact's history; Group Note not shown on the standalone). Overflow → Delete Group Event & Interactions confirmation → parent + all children gone. A zero-participant event shows "No participants yet". Open the Detail via 'View Group Event' from a profile reached through the Orrery AND through Settings→Archived (no "no screen named" crash).</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes; `npx tsc --noEmit` passes; GroupEventDetail registered in DashboardStack, OrreryStack, AND SettingsStack (review cycle-2 HIGH #1).
    - ParticipantCard "Remove from group" uses the SHARED RemoveParticipantSheet (Plan 06), wired to deleteGroupChild/detachParticipant with groupEventId (review cycle-2 MED); Dissolve/Delete call the no-uid DAO signatures.
    - Device: Detail presentation-first; participant card tap → child Interaction Detail; card overflow "Edit participant record" → EditParticipant; three-option remove behaves; Dissolve vs Delete confirmations match copy and produce correct outcomes; zero-participant empty state; reachable from Orrery/Settings-hosted profiles.
  </acceptance_criteria>
  <done>Group Event Detail presents the event and its participants presentation-first (registered in all three stacks), with the shared three-way remove Sheet, correct dissolve/delete destructive confirmations, and single-child removal.</done>
</task>

<task type="auto">
  <name>Task 3: Activate history seams — Interaction Detail group block, scope prompt, convert entry (+ HistorySection navigation wiring)</name>
  <files>src/components/history/InteractionDetail.tsx, src/components/history/HistorySection.tsx, src/components/history/GroupScopePrompt.tsx, src/components/group/GroupTitlePromptSheet.tsx</files>
  <read_first>
    - src/components/history/interaction-detail-logic.ts:44-53,140-159 (buildGroupContext — returns non-null once the record carries groupEventId; GroupLinkedDetailInput already has groupEventId/groupTitle/groupNote)
    - src/db/history-read.ts:38-54,104-106,136-175 (HistoryInteractionRecord NOW carries groupEventId/groupTitle/groupNote and SELECT_INTERACTIONS LEFT JOINs group_events — this activation is OWNED BY PLAN 01 Task 3, a dependency; this plan CONSUMES it, so the record passed to InteractionDetail already has real group context)
    - src/components/history/InteractionDetail.tsx:62-100,150-219 (the props are onEdit/onDeleted ONLY; the dormant group block at 150-180 calls buildGroupContext(interaction); BUG at :178 — the "View Group Event" button calls onEdit; BUG at :215-218 — GroupScopePrompt onEditGroup is a no-op placeholder. Both fixed here via new nav callbacks)
    - src/components/history/HistorySection.tsx:360-378 (where InteractionDetail is created and onEdit → navigation.navigate("EditInteraction",…); THIS is where the new group-navigation callbacks are supplied — HistorySection owns `navigation`)
    - src/components/history/GroupScopePrompt.tsx:26-58 (onEditIndividual / onEditGroup callback props — already present; wire them from InteractionDetail)
    - src/db/group-events-dao.ts (convertInteractionToGroupEvent — needs a minted parent uid + nonblank title, Plan 05)
    - src/components/ui/Sheet.tsx (the Sheet primitive GroupTitlePromptSheet builds on — Android-compatible, no platform prompt)
    - src/navigation/types.ts (GroupEventDetail / EditGroupEvent / EditParticipant route params — Plan 02, now in all three stack param lists)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting: 'Make this a group interaction'; 'View Group Event' accentText link; scope prompt fixed labels; convert title-entry copy)
  </read_first>
  <action>
    Give `InteractionDetail` explicit navigation callback props and STOP overloading `onEdit` (review HIGH #2/#3). Add `onViewGroupEvent?(groupEventId: number)`, `onEditGroupEvent?(groupEventId: number)`, `onEditParticipant?(groupEventId: number, interactionId: number, contactId: number)`, and `onConvertToGroup?()`. Then:
    - Activate the group block for group-linked interactions (buildGroupContext now returns non-null because the record carries `groupEventId`/`groupTitle`/`groupNote` from Plan 01): the restrained "Group event" badge, labelled Group Note, participant note, and a "View Group Event" action (accentText link) that calls `onViewGroupEvent(interaction.groupEventId)` — FIX the current `onPress={onEdit}` mis-wire at InteractionDetail.tsx:178.
    - Wire `GroupScopePrompt`: `onEditIndividual` → `onEditParticipant(interaction.groupEventId, interaction.id, contactId)`; `onEditGroup` → `onEditGroupEvent(interaction.groupEventId)` — FIX the current no-op at :215-218. Editing a group-linked interaction shows this prompt FIRST (never a hybrid implicit-scope editor — D-11/dossier §Q; the existing `onEditPress` gate at :94-100 already routes group-linked rows to the prompt).
    - Add a "Make this a group interaction" overflow entry (only for non-group interactions — `!interaction.groupLinked`) that calls `onConvertToGroup()`.
    In `HistorySection.tsx`, SUPPLY these callbacks to `InteractionDetail` using the group context on the record it already holds: `onViewGroupEvent` → `navigation.navigate("GroupEventDetail", { groupEventId })`; `onEditGroupEvent` → `navigation.navigate("EditGroupEvent", { groupEventId })`; `onEditParticipant` → `navigation.navigate("EditParticipant", { groupEventId, interactionId, contactId })`; `onConvertToGroup` → open the reusable in-app `GroupTitlePromptSheet` (create it: a `Sheet` + single `TextInput` + confirm/cancel, confirm disabled until the title is nonblank; NOT `Alert.prompt`, which is iOS-only and this app is Android-first — review cycle-2 MED: Android conversion title entry) to capture the required event title, mint a fresh PARENT uid (the app's uid generator), call `convertInteractionToGroupEvent(getExecutor(), { interactionId, contactId, title, uid, now })`, then `navigation.navigate("GroupEventDetail", { groupEventId })` for the new parent (GRP-07). Keep the existing standalone `onEdit` → `EditInteraction` path unchanged. Guard the badge/context to read from the child only — the parent never becomes a second history row (GRP-02/D-06). All colours via tokens.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit && npx vitest run src/components/history/interaction-detail-logic.test.ts src/db/history-read.test.ts</automated>
  </verify>
  <human-check>On the Pixel: open a group-linked interaction's Detail → "Group event" badge + Group Note + participant note + "View Group Event" → routes to Group Event DETAIL (not the edit form). Tap Edit on a group-linked interaction → GroupScopePrompt appears; "Edit individual interaction" → EditParticipant screen; "Edit Group Event" → the EditGroupEvent form. On an ordinary interaction, overflow → "Make this a group interaction" → prompts for a title, converts (its history row keeps its identity — verify uid unchanged), and opens the new Group Event Detail. Confirm the parent never appears as a second row in that contact's history.</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes; `npx tsc --noEmit` passes (proves the new InteractionDetail callback props + HistorySection route navigation + GroupScopePrompt wiring type-check — review: UI verification beyond check:colors); the group-context logic/history tests stay green (parent never counts).
    - "View Group Event" routes to GroupEventDetail (NOT onEdit); GroupScopePrompt routes to EditParticipant / EditGroupEvent (no no-op); convert captures the title via the in-app GroupTitlePromptSheet (not a platform prompt — review cycle-2 MED), mints a parent uid, preserves identity, and opens the new Group Event Detail.
    - Device: group block + correct routing for all four paths; no hybrid implicit-scope editor; convert title entry is an in-app Sheet (works on Android).
  </acceptance_criteria>
  <done>The Phase-32 dormant seams are live with a REAL navigation contract: InteractionDetail has explicit group-navigation callbacks (supplied by HistorySection), "View Group Event" and the scope prompt route to the correct registered screens, and ordinary interactions convert in place and open their new Group Event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history/detail read → presentation | group context must derive from the child, never a parent row that could double-count |
| destructive overflow actions → data loss | dissolve/delete/convert reachable from presentation surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-21 | Tampering / integrity | parent leaks into history as a second row via the badge/context | high | mitigate | Badge/context read from the child (buildGroupContext); GRP-02 tests (Plan 01) guard the invariant |
| T-33-22 | Repudiation / data loss | accidental Delete Group Event & Interactions | medium | mitigate | ConfirmDialog destructive, consequence-naming copy, no scrim-dismiss; Dissolve is the non-destructive alternative |
| T-33-23 | Information Disclosure | Group Note rendered where a standalone (post-detach) interaction is shown | medium | mitigate | Group Note rendered only in group context (buildGroupContext non-null); detach cleared the link (Plan 05) |
| T-33-24 | Tampering | convert loses interaction identity | medium | mitigate | convertInteractionToGroupEvent is in-place (id/uid preserved, Plan 05); device UAT confirms uid unchanged |
</threat_model>

<verification>
- `npm run check:colors` AND `npx tsc --noEmit` pass across all tasks (check:colors cannot catch route registration, prop compatibility, or navigation wiring — review: UI verification beyond check:colors).
- GroupEventDetail is registered in DashboardStack, OrreryStack, AND SettingsStack (review cycle-2 HIGH #1); convert title entry uses the in-app GroupTitlePromptSheet (not a platform prompt — review cycle-2 MED); ParticipantCard "Remove from group" reuses the shared RemoveParticipantSheet (review cycle-2 MED).
- `interaction-detail-logic.test.ts` + `history-read.test.ts` stay green (parent never counts; isGroupLinked live via the group fields Plan 01 added to the record).
- Device UAT confirms browse, detail, participant cards, group block + View Group Event (→ GroupEventDetail, reachable from Orrery/Settings-hosted profiles without crash), scope prompt (→ EditParticipant / EditGroupEvent), convert via in-app title Sheet, three-option remove, and dissolve/delete outcomes.
</verification>

<success_criteria>
Group Events are fully browsable, viewable, editable, convertible, and dissolvable/deletable from the UI; the Phase-32 dormant history seams are live; and the parent-never-counts invariant holds through the presentation layer.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-07-SUMMARY.md` when done.
</output>
