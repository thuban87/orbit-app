---
phase: 33-group-interaction-logging
plan: 06
type: execute
wave: 4
depends_on: ["33-02", "33-03", "33-04", "33-05"]
files_modified:
  - src/components/TouchpointRefineForm.tsx
  - src/screens/GroupLogScreen.tsx
  - src/screens/EditGroupEventScreen.tsx
  - src/components/group/ParticipantOverrideEditor.tsx
  - src/screens/EditParticipantScreen.tsx
  - src/navigation/tabs/DashboardStack.tsx
autonomous: true
requirements: [GRP-01, GRP-03, GRP-04, GRP-06, GRP-08, GRP-12]
user_setup: []

estimate:
  tokens: 92000
  raw_tokens: 47000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Global FAB → Group Log opens directly into the Group Log form with NO pre-picker; participant selection is not required to enter or save (GRP-01, dossier §I, SHELL-09)."
    - "The Group Log form defaults Channel to 'In Person', Tone unset, Duration unset, and is exempt from the ordinary Default Interaction Channel preference (GRP-03, D-09, dossier §J)."
    - "A zero-participant event is a valid saved state: the participant area shows 'No participants yet' + the documented body, never an error, and 'Log group' stays enabled (GRP-01 empty, dossier §C, UI-SPEC)."
    - "'Add participants' opens the shared ContactPicker in multi-select mode; adding after save inherits current shared values stamped at the event date/time via addParticipant; removing a saved participant opens a three-option Sheet (Delete interaction / Keep as individual interaction / Cancel) — NOT a binary dialog (GRP-06, D-11, dossier §N, UI-SPEC)."
    - "TouchpointRefineForm gains a declarative field-visibility API (a `visibleFields` allow-list prop) so a caller can render an arbitrary SUBSET of its controls; it defaults to ALL fields visible, so its sole existing caller (EditInteractionScreen) is unchanged. Group shared editing hides Direction/Connected/Note/Allow-AI; the participant override editor hides Date/Time (and Allow-AI). The form no longer 'always renders every field' — reuse is real, not assumed (GRP-03/04, review HIGH #4)."
    - "The participant override editor exposes ONLY Channel, Tone, Duration, Direction, Connected, and the participant note — never event date/time or title — via TouchpointRefineForm's visibleFields allow-list (GRP-04, dossier §L; review HIGH #4)."
    - "The participant override editor has a concrete navigation owner: EditParticipantScreen (registered at the focused EditParticipant route in DashboardStack) self-loads the event+participant via readGroupEventDetail/resolveParticipants and saves via setParticipantOverride/clearParticipantOverride; it is the destination for the history-surface 'Edit individual interaction' path (Plan 07) and is reused inline by EditGroupEventScreen (GRP-04/GRP-08, review HIGH #3)."
    - "Each participant row/editor shows a concise inherit-vs-override summary by text + icon (e.g. 'Follows event' vs 'Tone: Negative · 45m'), never colour alone; a per-field 'Follow event <field>' affordance (accentText link tone) clears an override (GRP-03/04 partial, THEME-08, UI-SPEC)."
    - "Save/fan-out failure keeps the form open with all input intact, nothing committed, control re-enabled, and shows the documented error copy — no optimistic vanish (GRP-11, dossier §AA, UI-SPEC)."
    - "A future event date is rejected with the locked 'Group events can't be in the future.' copy; date/time is authoritative and non-overridable per participant (GRP-12, dossier §AB)."
    - "Edit Group Event is a separate focused form (not the read Detail) that loads via readGroupEventDetail and saves shared edits through updateGroupEventShared/updateGroupEventDate (GRP-08, dossier §R)."
    - "GroupLog (real GroupLogScreen) and EditGroupEvent are registered in DashboardStack; the create form uses 'Log group' and the edit form uses 'Save changes' as the single Primary (accent) action (UI-SPEC Copywriting Contract)."
    - "Loading (picker fetch, edit-form event fetch) reuses the existing ActivityIndicator idiom; a spinner shows only on initial/meaningfully-delayed load (UI-SPEC, LISTV-10 idiom)."
    - {statement: "Large participant sets (no cap) render in a virtualized (FlatList) participant list and stay searchable in the picker without layout collapse at a realistic large count.", verification: backstop}
    - {statement: "A long event title, a long Group Note, and long contact names reflow (grow card/field height) rather than truncating critical semantics or shrinking text.", verification: backstop}
  artifacts:
    - "TouchpointRefineForm `visibleFields` field-visibility API (src/components/TouchpointRefineForm.tsx) — default all-visible, existing caller unchanged"
    - src/screens/GroupLogScreen.tsx
    - src/screens/EditGroupEventScreen.tsx
    - src/components/group/ParticipantOverrideEditor.tsx
    - "src/screens/EditParticipantScreen.tsx + EditParticipant route registration in DashboardStack"
  key_links:
    - "GroupLogScreen 'Log group' -> createGroupEvent; 'Add participants' -> ContactPicker multi-select -> addParticipant"
    - "EditGroupEventScreen -> readGroupEventDetail (load) + updateGroupEventShared/updateGroupEventDate (save)"
    - "ParticipantOverrideEditor (reuses TouchpointRefineForm visibleFields) -> setParticipantOverride / clearParticipantOverride; hosted by EditParticipantScreen at the EditParticipant route"
    - "EditGroupEventScreen shared Save -> updateGroupEventShared explicit patch ({channel?/quality?/duration?: {value}})"
    - "DashboardStack registers GroupLog (real) + EditGroupEvent + EditParticipant"
  prohibitions:
    - "The Group Log form must never apply the ordinary Default Interaction Channel preference (it is exempt; default is In Person)."
    - "Remove-participant must never be a binary confirm — it is a three-option Sheet (Delete / Keep as individual / Cancel)."
    - "The participant editor must never expose event date/time or title as editable fields."
---

<objective>
Build the two focused authoring surfaces — the canonical Group Log create form and the Edit Group Event form — plus the participant override editor, wiring them to the DAO/read layer from Plans 03/04/05 and the multi-select picker + routes from Plan 02, and registering the real `GroupLogScreen` + `EditGroupEvent` in the Dashboard stack. Every field control, sheet, and confirmation reuses shipped primitives (`TouchpointRefineForm`, `Sheet`, `ConfirmDialog`, the multi-select `ContactPicker`); this phase invents no new visual language (UI-SPEC).

Purpose: the UI side of GRP-01 (event-first create), GRP-03/04 (shared defaults + per-field override + "Follow event …"), GRP-06 (add/remove within the form), GRP-08 (Edit form separate from Detail), GRP-12 (future rejection copy).
Output: `GroupLogScreen`, `EditGroupEventScreen`, `ParticipantOverrideEditor`; DashboardStack registration.

Phase tracer note: the phase tracer is in Plan 01; this plan is the UI activation layer over the proven data spine.
</objective>

<review_findings_addressed>
Cross-AI review (33-REVIEWS.md) findings resolved in this plan:
- HIGH #4 (TouchpointRefineForm reuse): Task 1 (prerequisite) adds a `visibleFields` field-visibility API to TouchpointRefineForm BEFORE any screen reuses it — the form currently has no visibility API and always renders every field, so shared editing (hide Direction/Connected) and participant override (hide Date/Time) were impossible as written. Default all-visible keeps EditInteractionScreen unchanged.
- HIGH #3 (participant-override route owner): Task 3 creates EditParticipantScreen (hosts ParticipantOverrideEditor, self-loads, saves via set/clearParticipantOverride) and registers the EditParticipant route in DashboardStack — the concrete destination Plan 07 wires the history surface to.
- Non-HIGH (UI verification beyond check:colors): every task adds `npx tsc --noEmit` to catch route registration, prop compatibility, and navigation-wiring errors that check:colors cannot.
- Non-HIGH (thin logic-level regression for GRP-03/04/06): Task 2's acceptance references the node-level createGroupEvent→updateGroupEventShared→resolveParticipants round-trip (Plans 03/04 DAO/read tests) as the automated backstop for the fan-out-to-UI logic half.
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Data spine (Plan 01); inheritance logic + edits (Plan 03); reads (Plan 04); lifecycle + convert + backup handoff (Plan 05); picker multi-select + routes (Plan 02)
- ★ `TouchpointRefineForm` `visibleFields` field-visibility API (extends the shipped LOG-01 form; default all-visible)
- ★ Screens `GroupLogScreen`, `EditGroupEventScreen`, `EditParticipantScreen`; component `ParticipantOverrideEditor`; DashboardStack registration of `GroupLog` (real) + `EditGroupEvent` + `EditParticipant`
- Detail/browse screens + `ParticipantCard` + wired `GroupScopePrompt`/`InteractionDetail`/convert entry (Plan 07)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-UI-SPEC.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@src/db/group-events-dao.ts
@src/db/group-events-read.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (prerequisite): Field-visibility API on TouchpointRefineForm</name>
  <files>src/components/TouchpointRefineForm.tsx</files>
  <read_first>
    - src/components/TouchpointRefineForm.tsx:45-106,162-406 (TouchpointRefineFormProps = { value, onChange, now, testID } ONLY; it renders date/time, channel, direction, connected, tone, note, duration, allow-ai UNCONDITIONALLY — there is NO visibility API today, so the review HIGH #4 blocker is real)
    - src/screens/EditInteractionScreen.tsx:121 (the SOLE existing caller — must keep compiling and rendering all fields)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Reuse-first inventory: shared form vs participant override field sets)
  </read_first>
  <action>
    Extend `TouchpointRefineForm` with a declarative field-visibility API so a caller can render an arbitrary SUBSET of its controls (review HIGH #4 — it currently always renders every field and cannot be reused as "ONLY the overridable subset"). Add an optional `visibleFields?: ReadonlyArray<Field>` prop over the field keys `"datetime" | "channel" | "direction" | "connected" | "tone" | "note" | "duration" | "allowAi"`; when omitted, ALL fields render (so `EditInteractionScreen` — the sole existing caller — is byte-for-byte unchanged). When provided, render only the listed fields (each field's `<View style={styles.field}>` block is gated on membership). Do NOT change the controlled `value`/`onChange` contract, the two-dialog Android date flow, the future-date guard, or any colour token. This is the prerequisite both group surfaces depend on: the Group Log shared form passes a set WITHOUT `direction`/`connected`/`note`/`allowAi`; the participant override editor passes a set WITHOUT `datetime`/`allowAi`.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel debug build: the existing Edit Interaction form still shows every field (regression); a probe screen rendering TouchpointRefineForm with a restricted `visibleFields` shows only those controls.</human-check>
  <acceptance_criteria>
    - `visibleFields` omitted → all fields render (EditInteractionScreen unchanged); provided → only listed fields render.
    - `npx tsc --noEmit` passes; `npm run check:colors` passes; the existing touchpoint-refine-logic test suite stays green.
  </acceptance_criteria>
  <done>TouchpointRefineForm exposes a `visibleFields` allow-list (default all-visible), so the Group Log shared form and the participant override editor can reuse it with only their permitted controls — no fork, no "always renders every field" assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Group Log create form (event-first, defaults In Person, zero-participant valid)</name>
  <files>src/screens/GroupLogScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <read_first>
    - src/screens/EditInteractionScreen.tsx (screen structure: Sheet expanded workspace, save/guard, keyboard handling)
    - src/components/TouchpointRefineForm.tsx (reuse via the Task 1 `visibleFields` API — pass ONLY datetime/channel/tone/duration for the shared event; the Group Note is a SEPARATE field, not the participant note)
    - src/components/ContactPicker.tsx (multi-select mode from Plan 02: the discriminated-union `mode:"multi"` + `onConfirm` arm)
    - src/db/group-events-dao.ts (createGroupEvent, addParticipant signatures)
    - src/navigation/tabs/DashboardStack.tsx:12-39 (GroupLog currently maps to GroupLogPlaceholderScreen — replace with the real screen)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting Contract: 'Log group', 'Add participants', empty states, future-date copy; Color: accent reserved for the single Primary; typography/spacing)
  </read_first>
  <action>
    Create `GroupLogScreen.tsx` as a focused form (reuse `EditInteractionScreen` structure + `TouchpointRefineForm` with `visibleFields={["datetime","channel","tone","duration"]}` so Direction/Connected/Note/Allow-AI are NOT rendered on the shared event) with: a required title (non-blank — createGroupEvent enforces trim/nonblank in Plan 01, surface the rejection inline), a required date/time control seeded to now (backdateable through now; future rejected with the locked "Group events can't be in the future." copy — reuse the `rejectFutureOccurredAt` locked-copy idiom surfaced by createGroupEvent's Promise.reject), shared Channel defaulting "In Person" (exempt from the ordinary Channel-default pref — D-09), Tone unset, Duration unset, and a Group Note field (separate from any participant note). Participant area: an "Add participants" secondary action opening the multi-select `ContactPicker` (`mode:"multi"`, `onConfirm`); when empty show "No participants yet" + the documented body (valid, not an error); Save ("Log group", the single `accent` Primary) calls `createGroupEvent` with the selected participants (passing a freshly-minted `uid`). On save/fan-out failure show "Couldn't save this group event. Please try again." and keep the form open with input intact (GRP-11). Register the real `GroupLogScreen` for the `GroupLog` route in `DashboardStack.tsx` (replacing the placeholder import). All colours via theme tokens; honour MIN_TOUCH_TARGET; portrait-only; use `formatLocalDate()`/`localDateTime()` for any date formatting.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel debug build (desktop-build-pipeline runbook; :8082 Metro remap; dismiss the LogBox toast first): FAB → Group Log opens the form directly (no pre-picker). Save a zero-participant titled event → succeeds, shows on the browse page. Add 3 participants via the multi-select picker, set a future date → rejected with the locked copy; set today → saves; open the new event and confirm each participant got exactly one interaction. Verify Channel defaulted In Person. Confirm Direction/Connected/Allow-AI controls are NOT on the shared form.</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes (no hex literals); `npx tsc --noEmit` passes (proves the real GroupLogScreen registration + createGroupEvent/ContactPicker prop wiring type-check — check:colors cannot catch route/prop errors, review: UI verification beyond check:colors).
    - GroupLog route resolves to the real GroupLogScreen (placeholder import removed from DashboardStack).
    - The GRP-01/03 logic half is backstopped by the node createGroupEvent round-trip test in Plan 01/03's DAO tests (referenced here; the screen render itself stays device-verified).
    - Device: event-first save with zero participants works; future date rejected with locked copy; Channel defaults In Person; multi-select add works; shared form hides Direction/Connected/Allow-AI.
  </acceptance_criteria>
  <done>The canonical Group Log create form is live: event-first, In-Person default, zero-participant valid, multi-select add, atomic save with truthful failure handling.</done>
</task>

<task type="auto">
  <name>Task 3: Participant override editor + EditParticipant route owner ("Follow event …" clear, overridable fields only)</name>
  <files>src/components/group/ParticipantOverrideEditor.tsx, src/screens/EditParticipantScreen.tsx, src/screens/GroupLogScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <read_first>
    - src/components/TouchpointRefineForm.tsx (reuse via the Task 1 `visibleFields` API — pass ONLY channel/direction/connected/tone/note/duration; NEVER datetime or allowAi, dossier §L)
    - src/db/group-events-dao.ts (setParticipantOverride, clearParticipantOverride)
    - src/db/group-events-read.ts (readGroupEventDetail, resolveParticipants — EditParticipantScreen self-loads from route params)
    - src/logic/group-inheritance.ts (resolveDisplay — per-field {value, following, label} for the summary)
    - src/navigation/types.ts (EditParticipant: { groupEventId, interactionId, contactId } — the Plan 02 route type this screen registers)
    - src/navigation/tabs/DashboardStack.tsx:36-72 (the `<Stack.Screen name=… component=… />` registration idiom; EditInteraction at :59-62 is the analog)
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting Contract: per-field 'Follow event <field>' wording; Color: accentText link tone for the clear affordance; state-never-colour-alone)
  </read_first>
  <action>
    Create `ParticipantOverrideEditor.tsx` reusing `TouchpointRefineForm` via `visibleFields={["channel","direction","connected","tone","note","duration"]}` so it exposes ONLY Channel, Tone, Duration, Direction, Connected, and the participant note — never event date/time or title (dossier §L; this is now MECHANICALLY enforced by the Task 1 visibility API, not by assumption — review HIGH #4). For each overridable field, use `resolveDisplay` to show whether it currently follows the event ("Follows event") or is overridden (the concrete value), by text + icon (THEME-08, never colour alone). Editing a field calls `setParticipantOverride`; a per-field "Follow event <field>" affordance (rendered in the `accentText` link tone, never a filled accent) calls `clearParticipantOverride`.
    Create `EditParticipantScreen.tsx` — the concrete NAVIGATION OWNER for the participant-override editor (review HIGH #3): a focused screen that reads `route.params.{ groupEventId, interactionId, contactId }`, self-loads the event + that participant via `readGroupEventDetail`/`resolveParticipants`, hosts `ParticipantOverrideEditor`, and persists edits via `setParticipantOverride`/`clearParticipantOverride`. Register it as `<Stack.Screen name="EditParticipant" component={EditParticipantScreen} />` in `DashboardStack.tsx` (the EditParticipant route type + focused classification land in Plan 02). This screen is the destination Plan 07 navigates the history-surface "Edit individual interaction" scope choice to; it is ALSO reused inline by `EditGroupEventScreen` (Task 4) for the per-participant edit reached from a participant row. All colours via tokens.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel: open a participant's override editor, override Tone to Negative and Duration to 45m (row summary shows "Tone: Negative · 45m"), then tap "Follow event tone" → Tone re-follows the event value and the summary shows "Follows event". Confirm date/time and title are NOT present in this editor. Change the event Tone afterward and confirm the re-followed participant updates while the overridden one does not.</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes; `npx tsc --noEmit` passes (proves EditParticipant route registration + ParticipantOverrideEditor/TouchpointRefineForm prop wiring — review: UI verification beyond check:colors).
    - Editor exposes only the five overridable fields + note (via visibleFields); no date/time or title control present.
    - EditParticipant is registered in DashboardStack and resolves to EditParticipantScreen (the route owner the history surface navigates to).
    - Device: override sets a value + summary; "Follow event <field>" clears it and re-follows; colour-independent summary (icon+text).
  </acceptance_criteria>
  <done>Participants can be overridden per field and re-attached to live inheritance via explicit "Follow event …" wording; the override editor has a concrete route owner (EditParticipantScreen) registered for the history-surface individual-edit path.</done>
</task>

<task type="auto">
  <name>Task 4: Edit Group Event form + three-way remove within the form</name>
  <files>src/screens/EditGroupEventScreen.tsx, src/navigation/tabs/DashboardStack.tsx</files>
  <read_first>
    - src/screens/GroupLogScreen.tsx (Task 2 form — reuse its field layout + `visibleFields` set for the shared values)
    - src/db/group-events-read.ts (readGroupEventDetail — load the event + participants)
    - src/db/group-events-dao.ts (updateGroupEventShared — the EXPLICIT PATCH API `{ channel?: {value}, quality?: {value}, duration?: {value} }` from Plan 03; updateGroupEventDate, addParticipant, deleteGroupChild, detachParticipant)
    - src/components/group/ParticipantOverrideEditor.tsx (Task 3 — the per-participant edit surface reused inline here)
    - src/components/ui/Sheet.tsx (compact variant for the three-option remove prompt) + ConfirmDialog
    - .planning/phases/33-group-interaction-logging/33-UI-SPEC.md (Copywriting: 'Save changes'; three-way Remove-participant Sheet copy; error copy for date-edit fan-out failure)
  </read_first>
  <action>
    Create `EditGroupEventScreen.tsx` (focused route, separate from the read Detail — GRP-08): load via `readGroupEventDetail`, edit shared Channel/Tone/Duration/Group Note and date/time, Primary action "Save changes" (single `accent`). Shared-value Save builds the EXPLICIT PATCH for `updateGroupEventShared` — include a field key ONLY when the user changed it, and use `{ value: null }` to UNSET Tone/Duration (never send an omitted key to mean "clear"; omission = leave unchanged — Plan 03 HIGH #5 contract); date/time saves through `updateGroupEventDate` (future-rejected). List participants with the concise override summary + a per-participant action opening the `ParticipantOverrideEditor` (Task 3) inline. "Add participants" → multi-select picker (`mode:"multi"`) → `addParticipant`. Removing a saved participant opens a three-option `Sheet compact` (NOT a binary dialog) with the documented copy: "Delete interaction" (destructive → `deleteGroupChild`), "Keep as individual interaction" (→ `detachParticipant`), "Cancel". On a date-edit fan-out failure show "Couldn't update the group event. Your changes weren't saved." with the whole transaction rolled back and form state preserved. Register `EditGroupEvent` in `DashboardStack.tsx`. All colours via tokens; portrait-only.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <human-check>On the Pixel: open Edit Group Event, change the shared Tone → following participants update, overridden ones don't. Clear Tone/Duration (unset) → following participants' values clear. Change the date → all participants' interactions re-date (verify in each contact's history); a future date is rejected. Remove a saved participant → the three-option Sheet appears; "Keep as individual interaction" detaches them (their interaction survives standalone, Group Note not shown on it); "Delete interaction" removes only that child. Add a participant → inherits current shared values at the event date.</human-check>
  <acceptance_criteria>
    - `npm run check:colors` passes; `npx tsc --noEmit` passes; EditGroupEvent registered in DashboardStack (route/prop wiring type-checked — review: UI verification beyond check:colors).
    - Shared Save sends an explicit patch (changed keys only; `{value:null}` to unset Tone/Duration) to `updateGroupEventShared`.
    - Device: shared edit fans out to following participants; unset (null) clears following children; date edit re-dates all + rejects future; three-option remove Sheet behaves per copy; add inherits current shared values.
    - Error copy shown + form preserved on a simulated save failure.
  </acceptance_criteria>
  <done>Edit Group Event is a separate focused form driving the shared-value patch/date fan-outs and the three-way participant remove, with truthful atomic-failure handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input (title, note, date) → DAO | user free-text + dates cross into persistence |
| destructive participant/lifecycle actions → data loss | remove/delete are irreversible child deletions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-17 | Tampering | future/invalid date submitted from the form | medium | mitigate | createGroupEvent/updateGroupEventDate reject future (locked copy); date non-overridable |
| T-33-18 | Repudiation / data loss | accidental permanent child delete via remove | medium | mitigate | Three-option Sheet with explicit destructive labelling; "Keep as individual" is the non-destructive default path |
| T-33-19 | Information Disclosure | Group Note surfaced as a participant note on detach | medium | mitigate | detachParticipant never copies group_note (enforced in Plan 05; UI shows Group Note only in group context) |
| T-33-20 | Tampering / integrity | optimistic UI shows success on a rolled-back save | high | mitigate | Never mark success unless the write committed; error copy + form preserved on failure (GRP-11) |
</threat_model>

<verification>
- `npm run check:colors` AND `npx tsc --noEmit` pass across all four tasks (no hex literals; route registration / prop compatibility / navigation wiring type-check — check:colors alone cannot catch those, review: UI verification beyond check:colors).
- The GRP-01/03/04 logic half is backstopped automatically by the node DAO/read round-trip tests in Plans 01/03/04 (createGroupEvent → updateGroupEventShared patch → resolveParticipants); the screen render stays device-verified.
- Device UAT confirms event-first create, In-Person default, zero-participant valid, override/"Follow event", shared+date fan-out (incl. null-unset), three-way remove, and truthful failure handling.
- Existing single-select ContactPicker callers + EditInteractionScreen (TouchpointRefineForm default all-visible) unaffected.
</verification>

<success_criteria>
The Group Log create form and Edit Group Event form are live, event-first, defaults-correct, and drive the atomic DAO mutations with colour-independent inherit-vs-override presentation and truthful failure handling.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-06-SUMMARY.md` when done.
</output>
