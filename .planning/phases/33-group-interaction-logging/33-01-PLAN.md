---
phase: 33-group-interaction-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/026-group-events-schema.ts
  - src/db/migrations/026-group-events-schema.test.ts
  - src/db/database.ts
  - src/db/recency-dao.ts
  - src/db/recency-dao.test.ts
  - src/db/tombstones-dao.ts
  - src/db/group-events-dao.ts
  - src/db/group-events-dao.test.ts
  - src/db/ai-context-read.test.ts
  - src/db/history-read.ts
  - src/db/history-read.test.ts
  - src/db/contact-status-read.test.ts
  - src/services/history/buckets.test.ts
autonomous: false
requirements: [GRP-01, GRP-02, GRP-05, GRP-11, GRP-12]
user_setup: []

estimate:
  tokens: 93000
  raw_tokens: 47000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A zero-participant Group Event is valid, saves, and creates zero child interactions (GRP-01, D-06, dossier §C)."
    - "A contact may appear at most once per Group Event: the partial UNIQUE index on (group_event_id, contact_id) WHERE group_event_id IS NOT NULL rejects a duplicate participant. Standalone rows (group_event_id IS NULL) are EXCLUDED FROM THE INDEX ENTIRELY by the partial predicate — they are unconstrained by construction (not 'NULLs are distinct within the index'); only real (event, contact) pairs are deduped (GRP-01 adjacency, dossier §E; review: partial-index rationale accuracy)."
    - "createGroupEvent rejects a blank/whitespace-only title (DAO-level trim/nonblank guard) BEFORE opening its transaction, and requires a non-empty uid — group_events.title TEXT NOT NULL would otherwise accept '' (GRP-01, review: title nonblank + parent UID)."
    - "deleteInteractionCore accepts an optional { bumpRevision?: boolean } (default true) propagated to insertTombstoneCore, so a composing lifecycle transaction (Plan 05) can suppress the inner per-child bump and own exactly one trailing bumpDataRevisionCore; deleteTouchpoint and bulk-actions-dao callers are unchanged (default true) (GRP-10/GRP-11, review: revision double-bump)."
    - "history-read.ts LEFT JOINs group_events for LOCAL presentation only: SELECT_INTERACTIONS selects i.group_event_id plus ge.title/ge.group_note; HistoryInteractionRecord gains groupEventId/groupTitle/groupNote and populates groupLinked from the real column via isGroupLinked({ groupEventId }). This local read is NOT the AI egress path — ai-context-read.ts still never joins group_events, so the Group Note reaches local history but never egresses (GRP-02/GRP-08 infra, D-04; review HIGH: group-context activation)."
    - "A contact whose ONLY interaction is a group child has its Status/recency read driven by exactly that one child (last_contact = the child occurred_at); the group_events parent never contributes a second interaction to any metric read (Status/Gravity/Intensity/Heatmap) — a future JOIN/union that treated the parent as activity goes red (GRP-02, D-06; review: metric-path regression)."
    - "Participant fan-out order does not affect the persisted result; each child is created and its last_contact recomputed independently, so last_contact is correct regardless of order (GRP-01/GRP-11 ordering)."
    - "Each create produces a distinct group_events row keyed by a fresh uid; repeated creates do not merge/dedupe (GRP-01 idempotency)."
    - "createGroupEvent runs inside exactly one inWriteTransaction; an interrupted or mid-loop-failed create rolls back fully, leaving no partial group_events row and no partial child rows (GRP-01/GRP-11 concurrency)."
    - "Each participant receives exactly one canonical child interactions row carrying group_event_id; the parent group_events row is never an interactions row (GRP-02 adjacency, D-06)."
    - "Zero participants yields zero children and the parent still never counts in any metric (GRP-02 empty)."
    - "The parent group_events row never appears in history, heatmap, Status, Gravity, or Intensity — only child interaction rows do (GRP-02 ordering, D-06)."
    - "ai-context-read.ts never joins group_events; the Group Note is unreachable by the AI egress projection under any participant's Allow AI state (GRP-05 adjacency, D-04, ADR-078)."
    - "An absent Group Note and a populated Group Note are equally unreachable — the projection reads no note column from any table (GRP-05 empty)."
    - "The egress projection's column set/order is unaffected by group rows; group_event_id on a child never widens what egresses (GRP-05 ordering)."
    - "Re-running the AI egress read any number of times never surfaces a Group Note, because no code path reads group_events (GRP-05 idempotency)."
    - "The egress read is a pure read requiring no group_events access, so no interleaving/concurrency can expose the Group Note (GRP-05 concurrency)."
    - "An event dated exactly 'now' is accepted; one second in the future is rejected via rejectFutureOccurredAt (GRP-12 adjacency/boundary)."
    - "Future rejection fires before any transaction opens (Promise.reject), so a rejected create writes nothing (GRP-12 empty)."
    - "rejectFutureOccurredAt is applied to the event date and to every fan-out occurred_at write (GRP-12 ordering, D-08)."
    - "A mid-loop child failure rolls the whole fan-out back — no partial children, recency untouched (GRP-11 adjacency)."
    - "A no-op mutation (zero children) still commits or rolls back atomically (GRP-11 empty)."
    - "Every child write routes through insertInteractionCore + recomputeLastContactCore (never a bulk INSERT); order-independent recency correctness holds (GRP-11 ordering, D-05)."
    - "editTouchpointFull is split into a non-mutexed editTouchpointFullCore (UPDATE-every-column + changes===1 guard + recomputeLastContact) and its thin mutexed public wrapper (future-date guard + one inWriteTransaction + the single data-revision bump), with identical observable single-edit behavior; the core is the ONLY primitive a transaction-owning caller may compose, so the Plan 03 group fan-outs never nest inWriteTransaction (GRP-11, D-05; transaction.ts non-reentrancy)."
    - "Migration 026 is additive-only (CREATE TABLE + ALTER ADD COLUMN + CREATE INDEX), version 26 = head+1, forward-only, and never rewrites the interactions.note column (D-03)."
    - "Archived contacts may be participants and their last_contact advances with no restore prompt and no added guard (D-07)."
  artifacts:
    - src/db/migrations/026-group-events-schema.ts
    - src/db/group-events-dao.ts
    - "group_events table (id, uid, title, occurred_at, channel, quality, duration, group_note, created_at, modified_at)"
    - "interactions.group_event_id + interactions.ge_follow_channel/quality/duration/direction/connected columns"
    - "idx_group_member_unique partial UNIQUE index"
    - "tombstone entity type 'group_event'"
    - "editTouchpointFullCore — non-mutexed edit primitive extracted from editTouchpointFull (src/db/recency-dao.ts), exported alongside insertInteractionCore/recomputeLastContactCore/deleteInteractionCore"
    - "deleteInteractionCore extended with an optional { bumpRevision?: boolean } = {} (default true) forwarded to insertTombstoneCore (src/db/recency-dao.ts)"
    - "HistoryInteractionRecord extended with groupEventId/groupTitle/groupNote; SELECT_INTERACTIONS LEFT JOINs group_events (src/db/history-read.ts) — local presentation only, never AI egress"
  key_links:
    - "createGroupEvent -> insertInteractionCore (extended) -> recomputeLastContactCore, all inside one inWriteTransaction"
    - "editTouchpointFull (mutexed wrapper) -> editTouchpointFullCore (non-mutexed) — Plan 03 group fan-outs compose the CORE inside their own single inWriteTransaction; composing the wrapper would nest the non-reentrant mutex and hang permanently"
    - "ai-context-read.ts closed projection MUST NOT reference group_events (egress ban by omission)"
    - "database.ts MIGRATIONS array + TARGET_VERSION = GROUP_EVENTS_SCHEMA_VERSION (26)"
  prohibitions:
    - "The parent Group Event must never itself count as an interaction anywhere (history, Status, Gravity, Intensity, Heatmap)."
    - "A Group Note must never reach the AI egress projection under any participant's Allow AI state."
    - "A group fan-out must never bulk-INSERT children or nest inWriteTransaction."
---

<objective>
Lay the durable data spine for Group Interaction Logging: migration 026 (the `group_events` parent table, a nullable `interactions.group_event_id`, per-field `ge_follow_*` live-inheritance flags, and a partial `UNIQUE(group_event_id, contact_id)` index), the `group_event` tombstone type, an extension of `insertInteractionCore` to carry group linkage, the extraction of a non-mutexed `editTouchpointFullCore` (so later group fan-outs compose the edit primitive without nesting the non-reentrant write mutex — the deadlock this phase's Plan 03 would otherwise hit), and the canonical `createGroupEvent` fan-out. This is the phase tracer: it wires the schema layer through the single-writer recency spine end-to-end and proves persistence + recency + the parent-never-counts / Group-Note-never-egresses invariants with real tests before any UI is built.

Ground truth is the phase dossier (`docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md`, §A–§AG) and its planning-notes sibling (R-02/R-09) — read them IN FULL (D-01, D-02) before implementing; they are authoritative over CONTEXT.md and any `[DECIDED]`/`[REJECTED]`/ADR/HANDOFF item is not to be reversed here.

Purpose: every subsequent plan (edits, lifecycle, reads, UI) composes these primitives; the correctness of `contacts.last_contact` (ADR-010/024/071, D-05) and the AI-egress ban (D-04/ADR-078) are locked in here.
Output: migration 026 registered and versioned; extended `insertInteractionCore`; `editTouchpointFullCore` extracted (mutexed wrapper preserved); `createGroupEvent`; `group_event` tombstone; guard tests proving GRP-02 (parent never counts) and GRP-05 (Group Note never egresses).
</objective>

<review_findings_addressed>
Cross-AI review (33-REVIEWS.md) findings resolved in this plan:
- HIGH #2 (group-context activation): Task 3 now EDITS `history-read.ts` (LEFT JOIN group_events, adds groupEventId/groupTitle/groupNote, groupLinked from the real column) so Plan 07 can activate `buildGroupContext` — a local presentation read, never AI egress.
- Non-HIGH (title nonblank): Task 1 adds a DAO-level trim/nonblank title guard to `createGroupEvent` + rejection test.
- Non-HIGH (parent UID): Task 1 requires a non-empty `uid` on `createGroupEvent` (parent `uid TEXT NOT NULL UNIQUE`).
- Non-HIGH (revision double-bump): Task 4 extends `deleteInteractionCore` with a `{ bumpRevision }` passthrough so Plan 05 owns one trailing bump.
- Non-HIGH (exhaustive writer audit): Task 4 mandates a `grep -rn "editTouchpointFull(" src` caller audit + lists the exceptional writers (restore-apply.ts, purge-dao.ts, migration025) in read-first.
- Non-HIGH (metric-path regression): Task 3 adds a contact-status-read test that a group child drives Status and the parent never double-counts.
- Non-HIGH (partial-index rationale): Task 2 rewrites the index rationale/test to assert NULL rows are EXCLUDED from the index (not "NULLs are distinct").
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory, repeated in each plan so the convergence source-grounding pass can exclude newly-created symbols. Symbols owned by THIS plan are marked ★.)
- ★ Table `group_events` (id, uid, title, occurred_at, channel, quality, duration, group_note, created_at, modified_at)
- ★ Columns `interactions.group_event_id`, `interactions.ge_follow_channel|quality|duration|direction|connected`
- ★ Index `idx_group_member_unique` (partial UNIQUE on interactions(group_event_id, contact_id) WHERE group_event_id IS NOT NULL)
- ★ `GROUP_EVENTS_SCHEMA_VERSION = 26`, `migration026` (registered in `database.ts`, `TARGET_VERSION` bumped to 26)
- ★ Tombstone entity type `"group_event"`
- ★ Extended `insertInteractionCore` params: `groupEventId?`, `geFollowChannel?`, `geFollowQuality?`, `geFollowDuration?`, `geFollowDirection?`, `geFollowConnected?`
- ★ Non-mutexed `editTouchpointFullCore` extracted from `editTouchpointFull` (`src/db/recency-dao.ts`) — the edit primitive the Plan 03 group fan-outs compose inside one transaction
- ★ `deleteInteractionCore` extended with optional `{ bumpRevision?: boolean } = {}` (default true) forwarded to `insertTombstoneCore` — lets Plan 05 lifecycle loops own a single trailing bump
- ★ `HistoryInteractionRecord` extended (`groupEventId`/`groupTitle`/`groupNote`); `SELECT_INTERACTIONS` LEFT JOINs `group_events` for local presentation (never AI egress)
- ★ DAO `createGroupEvent` (in `src/db/group-events-dao.ts`) — trim/nonblank title guard + required non-empty uid
- DAO (later plans): `updateGroupEventShared`, `updateGroupEventDate`, `setParticipantOverride`, `clearParticipantOverride`, `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent`
- Reads (later plans): `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants`
- Pure logic (later plans): `src/logic/group-inheritance.ts` — `resolveDisplay`, `computeFollowingChildren`
- Screens (later plans): `GroupLogScreen`, `EditGroupEventScreen`, `GroupEventDetailScreen`, replaced `GroupEventsScreen`
- Components (later plans): `ParticipantOverrideEditor`, `ParticipantCard`, `ContactPicker` multi-select mode
- Routes (later plans): `GroupEventDetail`, `EditGroupEvent` (reusing `GroupLog`, `GroupEvents`)
- Wired seams (later plans): `GroupScopePrompt` callbacks, `InteractionDetail` group block + "Make this a group interaction"

<assumption_delta_decision>
Detected seam (pluralization / required→optional): an ordinary Interaction can now ALSO be a child of a Group Event (GRP-07 converts one into the other), and the previously singular "interaction = standalone" gains a plural context.

Primary noun: the **child Interaction** remains the canonical primary. Decision: **add-alongside** (accepted, not debt). The Group Event (`group_events` parent + `group_event_id` + `ge_follow_*`) is additive authoring/history metadata layered onto the unchanged one-contact-per-Interaction model; it is NOT a new primary and NOT a many-to-many rewrite. Rationale: the dossier §B and "Notes for GSD" explicitly forbid replacing the one-contact-per-Interaction model and state the parent is "an authoring/history context object, not an extra contact interaction." The child always retains resolved canonical values, so every existing consumer stays group-unaware. Invariant test encoding this intent: the GRP-02 metric-guard tests in Task 3 (parent never counts) go red the instant a future change lets the parent enter a metric.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-RESEARCH.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-12-planning-notes.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm irreversible migration 026 schema shape (one-way door)</name>
  <decision>
    Migration 026 is additive but IRREVERSIBLE in production — it runs at launch via PRAGMA user_version on devices Orbit can never reach or repair (CLAUDE.md). Confirm the frozen shape BEFORE Task 1 writes it:
    - `group_events` columns: id, uid (UNIQUE), title (NOT NULL), occurred_at (NOT NULL), channel, quality, duration, group_note, created_at (NOT NULL), modified_at (NOT NULL).
    - `interactions` additive columns: group_event_id (FK ON DELETE SET NULL, safety net only), ge_follow_channel, ge_follow_quality, ge_follow_duration, ge_follow_direction, ge_follow_connected.
    - Partial `UNIQUE(group_event_id, contact_id) WHERE group_event_id IS NOT NULL` (A1 discrete per-field flag storage shape).
    This is an owner-bucket call (risk posture on an irreversible schema change). It precedes the one-way Task 1.
  </decision>
  <options>
    - Approve the schema shape as specified → proceed to Task 1 and treat migration 026 as final.
    - Direct a change to columns/index/flag-storage → revise the shape before Task 1 writes the migration.
  </options>
  <resume-signal>Owner approves the migration 026 shape (or directs a change); execution then proceeds to Task 1.</resume-signal>
  <default_if_unmet>Halt — do not write migration 026 or any dependent plan until the shape is confirmed.</default_if_unmet>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Migration 026 + extended write-core + createGroupEvent fan-out (end-to-end data slice)</name>
  <reversibility rating="one-way">Migration 026 runs at launch via PRAGMA user_version and is irreversible on unreachable devices (no remote DB access) — a checkpoint:decision precedes this task.</reversibility>
  <files>src/db/migrations/026-group-events-schema.ts, src/db/database.ts, src/db/recency-dao.ts, src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/migrations/025-interaction-history-schema.ts (exact migration object shape + additive header discipline; version-constant pattern)
    - src/db/database.ts:50-92 (MIGRATIONS array + TARGET_VERSION wiring — verify head+1 = 026 on disk)
    - src/db/recency-dao.ts:75-236 (insertInteraction / insertInteractionCore body + FirstInteractionInput; the 13-column ?-bound INSERT to extend)
    - src/db/recency-dao.ts:159-191,446-460 (recomputeLastContact = the ONLY last_contact writer; *Core aliases)
    - src/db/contacts-dao.ts:116-200 (createContactFull — the reject-future-before-txn, enter-mutex-once, compose-*Core-in-a-loop exemplar to copy)
    - src/db/transaction.ts:49-64 (inWriteTransaction — NON-REENTRANT; never nest)
    - src/db/log-guards.ts (rejectFutureOccurredAt signature + locked copy)
    - .planning/phases/33-group-interaction-logging/33-RESEARCH.md (Recommended Migration 026 SQL; Pattern 1)
  </read_first>
  <behavior>
    - Migration 026 applied on a fresh DB creates group_events with columns id,uid,title,occurred_at,channel,quality,duration,group_note,created_at,modified_at; adds interactions.group_event_id + five ge_follow_* columns; creates the partial UNIQUE index. user_version becomes 26.
    - createGroupEvent with zero participants inserts one group_events row and zero interactions rows; returns a groupEventId; the event is valid.
    - createGroupEvent with N participants inserts exactly N interactions rows (each group_event_id set, occurred_at = event occurred_at verbatim, ge_follow_channel/quality/duration/direction/connected = 1, resolved shared values materialized on the child) and recomputes last_contact per contact.
    - Two participants with the same contact_id fail on the UNIQUE index; the whole transaction rolls back (no group_events row, no children).
    - A future event occurredAt rejects synchronously (Promise.reject) before any BEGIN — no group_events row written.
    - A blank / whitespace-only title rejects (Promise.reject) before any BEGIN — no group_events row written (review: title nonblank). A missing/empty uid rejects likewise.
    - Channel defaults to "In Person" when unset (D-09); quality and duration default NULL/unset.
  </behavior>
  <action>
    Verify on disk that head is `migration025` and `TARGET_VERSION = INTERACTION_HISTORY_SCHEMA_VERSION = 25`, so the new migration is 026 (D-03 — numbers drift; do not assume). Create `026-group-events-schema.ts` mirroring the `025` object shape: export `GROUP_EVENTS_SCHEMA_VERSION = 26` and `migration026: Migration` whose `apply` runs a single additive `execAsync` string — `CREATE TABLE group_events (...)`, `ALTER TABLE interactions ADD COLUMN group_event_id INTEGER REFERENCES group_events(id) ON DELETE SET NULL` (safety net only; children are always deleted explicitly through the core), five `ALTER TABLE interactions ADD COLUMN ge_follow_* INTEGER`, and `CREATE UNIQUE INDEX idx_group_member_unique ON interactions(group_event_id, contact_id) WHERE group_event_id IS NOT NULL`. Additive only — no rebuild, no DROP, no note rewrite; replicate 025's head+1 header comment. Register `migration026` in the `MIGRATIONS` array in `database.ts` and set `TARGET_VERSION = GROUP_EVENTS_SCHEMA_VERSION`.
    Extend `insertInteraction` (exported as `insertInteractionCore`) to accept optional `groupEventId?: number | null` and `geFollowChannel?/geFollowQuality?/geFollowDuration?/geFollowDirection?/geFollowConnected?: number | null` on its input object, appending them to the INSERT column list as `?`-bound params defaulting NULL. Existing call sites are unaffected (all new params optional).
    Create `src/db/group-events-dao.ts` with `createGroupEvent(exec, input)` where input carries `uid, title, occurredAt, now, channel?, quality?, duration?, groupNote?, participants: Array<{contactId, uid, direction?, connected?, note?}>`. Follow `createContactFull` exactly. BEFORE the transaction (so a rejected create writes nothing): (a) assert a non-empty trimmed `title` — `group_events.title TEXT NOT NULL` accepts `""`, so enforce `title.trim().length > 0` at the DAO and reject (Promise.reject) on blank/whitespace (review: title nonblank); (b) assert `uid` is a non-empty string (the parent `uid TEXT NOT NULL UNIQUE` — the caller mints it, the DAO refuses an empty/missing one; review: parent UID); (c) call `rejectFutureOccurredAt(occurredAt, now)` in try/catch returning `Promise.reject(err)` (GRP-12, D-08). Then ONE `inWriteTransaction` that INSERTs the parent (channel `?? "In Person"` per D-09, all values `?`-bound), then loops participants calling `insertInteractionCore` (occurredAt = event occurredAt per dossier §M, resolved shared values materialized, `groupEventId` set, `geFollow*` = 1) followed by `recomputeLastContactCore` per participant (D-05 — never a bulk INSERT, never nest the mutex), and ends with `bumpDataRevisionCore`. Do NOT add an archived-contact guard (D-07).
    Author `src/db/group-events-dao.test.ts` covering the behaviors above against an in-memory `node:sqlite` DB driven through the REAL migrations + REAL DAO (mirror `recency-dao.test.ts` harness).
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/group-events-dao.test.ts` passes with cases: zero-participant valid (1 group_events row, 0 interactions); 2-participant (exactly 2 interactions, each group_event_id set, last_contact recomputed per contact); duplicate contact rejected + full rollback; future occurredAt rejected before txn (no rows); blank/whitespace title rejected before txn (no rows); empty/missing uid rejected; channel defaults "In Person".
    - `src/db/database.ts` exports `TARGET_VERSION === 26` and `MIGRATIONS` includes `migration026`.
    - No existing `recency-dao`/`contacts-dao` test regresses (new insertInteractionCore params are optional).
  </acceptance_criteria>
  <done>Migration 026 is registered at version 26; `insertInteractionCore` carries optional group linkage; `createGroupEvent` persists a valid group event (0 or N participants) atomically through the recency spine, verified by a green DAO test.</done>
</task>

<task type="auto">
  <name>Task 2: group_event tombstone type + migration 026 shape test</name>
  <files>src/db/tombstones-dao.ts, src/db/migrations/026-group-events-schema.test.ts</files>
  <read_first>
    - src/db/tombstones-dao.ts:8-54 (TombstoneEntityType union + assertTombstoneEntityType allowlist arm — both must gain "group_event")
    - src/db/migrations/025-interaction-history-schema.test.ts (additive-shape + version assertion pattern to mirror)
    - .planning/phases/33-group-interaction-logging/33-PATTERNS.md (tombstones-dao section; migration 026 section)
  </read_first>
  <action>
    Add `"group_event"` to the `TombstoneEntityType` union and a `case "group_event":` arm to `assertTombstoneEntityType` (the runtime allowlist guarding export/reconcile — missing it makes every group-event tombstone throw). `insertTombstoneCore` body is unchanged. Then author `026-group-events-schema.test.ts` (mirror `025`'s test) asserting: after migration, `user_version` = 26; `group_events` exists with the exact column set; the five `ge_follow_*` + `group_event_id` columns exist on `interactions`; the partial UNIQUE index exists and enforces the ACTUAL partial-index behavior — rows with `group_event_id IS NULL` are EXCLUDED FROM THE INDEX ENTIRELY by the `WHERE group_event_id IS NOT NULL` predicate (so two standalone interactions with `group_event_id` NULL and the same `contact_id` coexist BECAUSE they are not indexed at all — not because "NULLs are distinct within the index"), while two rows with the same non-NULL `(group_event_id, contact_id)` pair violate the constraint. Write the test's rationale comment to describe this exclusion, not a "NULL-distinct" mechanism (review: partial-index rationale accuracy).
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/026-group-events-schema.test.ts src/db/tombstones-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration test asserts version 26, the group_events column set, the interactions additive columns, and the partial-UNIQUE behavior (both branches): non-null (group_event_id, contact_id) duplicate rejected; multiple group_event_id-NULL rows for the same contact coexist because the partial predicate excludes them from the index entirely.
    - `assertTombstoneEntityType("group_event")` does not throw; the existing tombstones-dao test suite stays green.
  </acceptance_criteria>
  <done>`"group_event"` is a first-class tombstone entity type and migration 026's additive shape + partial-index behavior (NULL rows excluded from the index; non-null pairs deduped) are asserted by a green test.</done>
</task>

<task type="auto">
  <name>Task 3: Activate history-read group-context (local only) + guard tests — Group Note never egresses (GRP-05) + parent never counts (GRP-02)</name>
  <files>src/db/history-read.ts, src/db/ai-context-read.test.ts, src/db/history-read.test.ts, src/services/history/buckets.test.ts, src/db/contact-status-read.test.ts</files>
  <read_first>
    - src/db/ai-context-read.ts:8-27,108-152 (the closed egress projection — reads name/category/channel/quality/connected aggregates + opted-in items, NEVER a note or group_events; this file MUST NOT be edited — D-04)
    - src/db/ai-context-read.test.ts (existing assertions to extend)
    - src/db/history-read.ts:38-54,98-106,136-175 (HistoryInteractionRecord = groupLinked:boolean ONLY today; isGroupLinked keyed on groupEventId; SELECT_INTERACTIONS does NOT select group_event_id and maps `isGroupLinked({})` hard-false. THIS TASK makes the seam live — review HIGH #2)
    - src/components/history/interaction-detail-logic.ts:44-53,146-159 (GroupLinkedDetailInput needs groupEventId/groupTitle/groupNote; buildGroupContext consumes them — the record must carry them)
    - src/db/group-events-dao.ts (createGroupEvent — seeds a real group_event_id to test against)
    - src/services/history/buckets.ts (heatmap bucketing over interaction rows — parent must not appear)
    - src/db/contact-status-read.ts (the Status/recency read over interactions — the metric-path regression target)
    - .planning/phases/33-group-interaction-logging/33-RESEARCH.md (Phase Requirements → Test Map)
  </read_first>
  <action>
    MODIFY `src/db/history-read.ts` to make the Phase-32 INERT group seam LIVE now that migration 026's `group_event_id` column exists (review HIGH #2 — history-read was only read-first before; it must be EDITED): (a) extend `SELECT_INTERACTIONS` to select `i.group_event_id` and `LEFT JOIN group_events ge ON ge.id = i.group_event_id` selecting `ge.title` and `ge.group_note`; (b) add `groupEventId: number | null`, `groupTitle: string | null`, `groupNote: string | null` to `HistoryInteractionRecord`; (c) populate `groupLinked` from the REAL column via `isGroupLinked({ groupEventId: row.group_event_id })` (no longer the hard-false `isGroupLinked({})`). This LEFT JOIN is a LOCAL presentation read ONLY — it is NOT the AI egress path. Do NOT touch `ai-context-read.ts`: the Group Note egress ban is preserved by omission there (D-04/ADR-078), so the Group Note reaches local history-detail but never egresses. Existing history-read assertions that deep-equal the record shape must be updated for the three additive fields (they are null/false for every pre-existing non-group row — no behavior change).
    Do NOT modify `ai-context-read.ts`. Extend `ai-context-read.test.ts`: seed a group event (via `createGroupEvent`) with a non-empty group_note and a participant whose child interaction has a distinctive note, assert the resulting PromptContext contains NO reference to `group_events`/the group_note string under both allow_ai=0 and allow_ai=1 for that child (only the participant's own note is ever eligible, and only when its own toggle is on — a Phase-34/36 concern; assert the group note specifically is absent regardless).
    Extend `history-read.test.ts` and `buckets.test.ts`: seed a group event with participants (real group_event_id now exists), assert `readContactHistory(...).interactions[0].groupLinked === true` for a child row and that `groupEventId`/`groupTitle`/`groupNote` are populated from the JOIN, and assert the parent group_events row never appears as a history record and never increments any heatmap bucket / count (GRP-02, D-06). Confirm each participant's child appears exactly once in that contact's history.
    Add the metric-path regression to `contact-status-read.test.ts` (review: metric-path regression): seed a contact whose ONLY interaction is a group child, assert the Status/recency read is driven by exactly that one child (last_contact = the child occurred_at, status reflects it), and that the `group_events` parent row contributes nothing to the read — a future JOIN/union treating the parent as activity would break this.
  </action>
  <verify>
    <automated>npx vitest run src/db/ai-context-read.test.ts src/db/history-read.test.ts src/services/history/buckets.test.ts src/db/contact-status-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `history-read.ts` selects `group_event_id` + LEFT JOINs `group_events` (title/group_note); `HistoryInteractionRecord` carries groupEventId/groupTitle/groupNote; `groupLinked` derives from the real column.
    - ai-context-read test asserts the group_note is absent from PromptContext under allow_ai 0 and 1, and that no `group_events` read occurs; `ai-context-read.ts` source is unchanged (ban is a non-action).
    - history-read/buckets tests assert a child's `groupLinked` true + group fields populated, each child counted exactly once, and the parent never counted anywhere.
    - contact-status-read test asserts a lone group child drives Status/recency and the parent never double-counts (metric-path regression).
  </acceptance_criteria>
  <done>history-read exposes group context for LOCAL presentation (never egress); automated tests lock in GRP-05 (Group Note never egresses), GRP-02 (parent never counts in history/heatmap/Status; child counted once) against the live group_event_id.</done>
</task>

<task type="auto">
  <name>Task 4: Extract non-mutexed editTouchpointFullCore from editTouchpointFull</name>
  <files>src/db/recency-dao.ts, src/db/recency-dao.test.ts</files>
  <read_first>
    - src/db/recency-dao.ts:281-337 (editTouchpointFull — the mutexed wrapper: rejectFutureOccurredAt guard, then inWriteTransaction wrapping the UPDATE-every-column + changes!==1 throw + recomputeLastContact + bumpDataRevisionCore body)
    - src/db/recency-dao.ts:340-368,446-460 (deleteInteractionCore + the *Core export block — the "non-mutexed primitive for callers that already own a transaction" contract to mirror: a core assumes BEGIN is open, takes NO mutex, and does NOT re-guard the future date. NOTE: deleteInteractionCore today calls insertTombstoneCore WITHOUT bumpRevision:false, so it self-bumps — this task adds the passthrough)
    - src/db/recency-dao.ts:193-236 (insertInteraction/insertInteractionCore — a core writes + the composing transaction owns the single bumpDataRevisionCore; mirror that so the core itself does NOT bump)
    - src/db/tombstones-dao.ts:60-80 (insertTombstoneCore already accepts { bumpRevision?: boolean } = true — deleteInteractionCore must forward it)
    - src/db/transaction.ts:10-29,49-64 (inWriteTransaction non-reentrancy — a nested acquisition is a PERMANENT hang; this is WHY the core must exist)
    - EXCEPTIONAL existing interactions/last_contact writers to audit before asserting any invariant (the graph cannot enumerate SQL writers — CLAUDE.md): src/backup/restore-apply.ts:190-201 (restore's direct INSERT…ON CONFLICT upsert of interactions — a SEPARATE writer that forces allow_ai=0 and does NOT recompute last_contact nor touch group columns); src/db/purge-dao.ts:82 (hard contact-purge DELETE of interactions by contact_id); src/db/migrations/025-interaction-history-schema.ts:44-72 (migration-time UPDATE interactions backfill). Confirm each preserves recency semantics / is intentionally outside the spine; none is altered by this phase.
  </read_first>
  <action>
    FIRST, discharge CLAUDE.md's "read every writer" mandate (review: exhaustive writer audit): run `grep -rn "editTouchpointFull(" src` across the whole tree and record the callers in the task summary — verified at plan time to be exactly one production caller (`src/screens/EditInteractionScreen.tsx:121`, via `src/screens/edit-interaction-logic.ts`), plus tests. Because the wrapper keeps identical observable behavior (below), that sole caller is unaffected; record the grep output to PROVE it rather than trusting the DAO suite alone.
    Extract a non-mutexed `editTouchpointFullCore(exec, input: EditTouchpointFullInput)` from `editTouchpointFull`. The core is the UPDATE-every-editable-column statement, the `result.changes !== 1` guard-throw, and the `recomputeLastContact(exec, input.contactId, input.now)` call — i.e. the transaction body MINUS the mutex, MINUS the future-date guard, and MINUS the data-revision bump. It assumes `BEGIN` is already open and takes NO mutex (mirror `deleteInteractionCore`'s "for callers that already own a transaction" contract; mirror `insertInteractionCore` in that the core does not bump — the composing transaction owns the single `bumpDataRevisionCore`). Do NOT call `rejectFutureOccurredAt` inside the core and do NOT call `bumpDataRevisionCore` inside the core.
    ALSO extend `deleteInteractionCore(exec, input, opts?: { bumpRevision?: boolean })` to accept an optional `{ bumpRevision = true } = {}` and forward it to its `insertTombstoneCore(exec, …, { bumpRevision })` call (review: revision double-bump). Default `true` preserves the existing callers verbatim — `deleteTouchpoint` (recency-dao.ts:376) and `bulk-actions-dao.ts:83` keep bumping exactly as today. Plan 05's lifecycle loops pass `{ bumpRevision: false }` so they own a single trailing bump. Add a test asserting `deleteInteractionCore(..., { bumpRevision: false })` writes the tombstone + recomputes but does NOT bump, while the default path still bumps once.
    Rewrite the public `editTouchpointFull` to stay the mutexed wrapper with IDENTICAL observable behavior: apply `rejectFutureOccurredAt(input.occurredAt, input.now)` (returning `Promise.reject(err)` on throw, unchanged), then `inWriteTransaction(exec, async () => { await editTouchpointFullCore(exec, input); await bumpDataRevisionCore(exec); })`. A single standalone edit therefore still runs one future-guard, one UPDATE, one recompute, and exactly one revision bump inside one transaction — no behavior change, so the existing recency-dao suite passes unchanged. (Placing the single bump in the wrapper — not the core — is deliberate: it matches insertInteractionCore's contract and lets the Plan 03 fan-outs own exactly one bump per transaction, including the zero-following-child case.)
    Export `editTouchpointFullCore` alongside `insertInteractionCore`/`recomputeLastContactCore`/`deleteInteractionCore`. Callers that already own a transaction (the Plan 03 group fan-outs) apply the future-date guard themselves BEFORE opening their transaction and own the single trailing `bumpDataRevisionCore` — exactly as `createGroupEvent` (Task 1) does with `insertInteractionCore`.
    Add a focused test to `recency-dao.test.ts`: compose `editTouchpointFullCore` inside a caller-owned `inWriteTransaction` against the node:sqlite harness, edit a child row, and assert the UPDATE + recompute committed without hanging; confirm the existing `editTouchpointFull` behavior (future-date reject before txn, single bump, changes!==1 rollback) still holds via the existing suite.
  </action>
  <verify>
    <automated>npx vitest run src/db/recency-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `editTouchpointFullCore` is exported and non-mutexed: it contains no `inWriteTransaction`, no `rejectFutureOccurredAt`, and no `bumpDataRevisionCore`; it performs the UPDATE, the `changes !== 1` throw, and `recomputeLastContact`.
    - `editTouchpointFull` retains identical observable behavior (future-date reject before txn; one UPDATE; one recompute; exactly one data-revision bump; changes!==1 → rollback), verified by the existing recency-dao suite passing unchanged.
    - A test composes `editTouchpointFullCore` inside a caller-owned transaction and commits without hanging.
    - The `grep -rn "editTouchpointFull(" src` caller audit is recorded in the task summary; the sole production caller (EditInteractionScreen) is confirmed unaffected.
    - `deleteInteractionCore(..., { bumpRevision: false })` writes the tombstone + recomputes but does NOT bump; the default (`bumpRevision` true) still bumps once; `deleteTouchpoint` and `bulk-actions-dao` callers remain unchanged.
  </acceptance_criteria>
  <done>`editTouchpointFullCore` exists as the non-mutexed edit primitive (mirroring insertInteractionCore/deleteInteractionCore); `editTouchpointFull` is its thin mutexed wrapper with unchanged behavior; `deleteInteractionCore` takes an optional `bumpRevision` passthrough; the editTouchpointFull caller audit is recorded; Plan 03's fan-outs can now compose the core inside one transaction without nesting the mutex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text → SQLite | title, group_note cross into persisted storage |
| child interactions → AI egress projection (`ai-context-read.ts`) | the one place contact data could leave the device (only when the user invokes AI) |
| migration code → on-device DB at launch | irreversible, unreachable-device schema change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering | title/group_note INSERT in group-events-dao | medium | mitigate | `?`-bound params only; never interpolate free-text (V5, recency-dao rule) |
| T-33-02 | Information Disclosure | Group Note → AI egress | high | mitigate | Structural non-action: `ai-context-read.ts` never joins `group_events`; Task 3 test asserts unreachability (D-04/ADR-078) |
| T-33-03 | Tampering / integrity | partial fan-out corrupts last_contact | high | mitigate | One `inWriteTransaction`, compose `*Core` in a loop, `recomputeLastContactCore` per child, full rollback on failure (GRP-11, D-05) |
| T-33-04 | Denial of Service (permanent, per-user) | irreversible migration 026 on unreachable device | high | mitigate | Additive-only (no DROP/rebuild), forward-only, no starting-state assumption; blocking checkpoint:decision confirms shape before ship |
</threat_model>

<verification>
- `npm test` (full vitest suite) green after this plan's tasks.
- `npm run check:colors` passes (no hex literals introduced).
- `TARGET_VERSION === 26`; migration 026 additive-only; `ai-context-read.ts` unchanged.
</verification>

<success_criteria>
Migration 026 ships the group_events schema; `createGroupEvent` persists valid events atomically through the single-writer recency spine; the parent-never-counts and Group-Note-never-egresses invariants are proven by green tests; the `group_event` tombstone type exists for later delete/dissolve backup correctness.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-01-SUMMARY.md` when done.
</output>
