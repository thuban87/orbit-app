---
phase: 33-group-interaction-logging
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/logic/group-inheritance.ts
  - src/logic/group-inheritance.test.ts
  - src/db/group-events-dao.ts
  - src/db/group-events-dao.test.ts
autonomous: true
requirements: [GRP-03, GRP-04, GRP-11, GRP-12]
user_setup: []

estimate:
  tokens: 92000
  raw_tokens: 48000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "updateGroupEvent is the SINGLE atomic save op for the Edit Group Event form: it accepts { groupEventId, now, occurredAt?, patch } and applies the event date change + the shared-value patch + the Group Note change together in ONE inWriteTransaction with ONE trailing bumpDataRevisionCore (review cycle-2 HIGH #5: no split-transaction save). There is no separate updateGroupEventShared/updateGroupEventDate call the form must sequence — a failure anywhere rolls back the whole save."
    - "updateGroupEvent FETCHES and asserts the parent group_events row exists (a guarded SELECT by groupEventId) as the FIRST statement inside its sole transaction; a parent DELETED between form-load and save throws before any write and WITHOUT bumping the data revision (full rollback, nothing persisted) — matching the loud-failure pattern at recency-dao.ts:295-335 and D-08 atomic semantics, never a silent zero-row success. An empty patch (no occurredAt, no fields) over an existing parent completes as a no-op with no child writes (review cycle-3 MED: parent-existence assertion)."
    - "The patch is an EXPLICIT per-field ops map whose key PRESENCE distinguishes 'leave unchanged' (key absent) from 'set to null/unset' (key present, value null): { channel?: { value: string }, quality?: { value: string|null }, duration?: { value: number|null }, groupNote?: { value: string|null } }. channel's value is a `string`, NEVER null: interactions.channel is NOT NULL DEFAULT 'unspecified' (migration 001:103) and EditTouchpointFullInput.channel is `string` (recency-dao.ts:96), so channel can be CHANGED but never UNSET — a null channel fan-out would fail tsc / violate NOT NULL (review cycle-3 MED: channel nullability). Setting quality/duration to null UNSETS them (GRP-03/D-09 — Tone/Duration unset is a real state; channel is exempt — it defaults In Person per D-09); groupNote is an EVENT-LEVEL field — setting it updates group_events.group_note only and does NOT fan out to or materialize on any child (dossier §H: one shared note, not duplicated into children); omitting a key leaves that field untouched (GRP-03/GRP-08, review cycle-1 HIGH #5 + cycle-2 HIGH #4: Group Note write path)."
    - "The event-level inheritable shared values are EXACTLY Channel, Tone (quality), and Duration — the only fields with a ge_follow flag and live fan-out. Group Note is event-level but NOT inheritable/materialized. Direction and Connected are participant-editable child fields with no event-level value (dossier §D/§E/§L, review cycle-2 HIGH #2), so they are NOT in the shared patch and have no fan-out."
    - "Shared Channel/Tone/Duration live on group_events and inherit LIVE, not by one-time copy: editing a shared value fans out to every child whose ge_follow_<field>=1, rewriting that child's resolved value; children with ge_follow_<field>=0 are untouched (GRP-03, dossier §G/§W)."
    - "The child row always holds the RESOLVED value in its ordinary column so existing consumers stay group-unaware; the ge_follow_<field> flag records whether that value follows the event (GRP-03, dossier §D-DERIVED)."
    - "A participant override whose value equals the current event value stays intentionally detached (ge_follow=0) and does NOT re-follow future event changes (GRP-03 adjacency, dossier §F)."
    - "An event with Tone or Duration unset (null) fans out that null/unset to following children without error; channel is never unset (NOT NULL — see the patch contract), so the unset-fan-out applies to quality/duration only (GRP-03 empty; review cycle-3 MED: channel nullability)."
    - "Shared-value fan-out over following children is order-independent; each child is rewritten via editTouchpointFullCore (the non-mutexed core extracted in Plan 01, never the mutexed editTouchpointFull) with its whole current record and only the changed field overwritten (GRP-03 ordering, Pitfall 3)."
    - "Per-participant override (setParticipantOverride) and its 'Follow event <field>' clear (clearParticipantOverride) apply ONLY to the three event-inheritable fields Channel, Tone, Duration. setParticipantOverride sets ge_follow_<field>=0 and writes the overriding value; clearParticipantOverride sets ge_follow_<field>=1 and rewrites the resolved value from the CURRENT event value (GRP-04, dossier §L)."
    - "Because editTouchpointFullCore writes only ordinary interaction columns and NEVER ge_follow_*, every override/clear performs an EXPLICIT scoped UPDATE interactions SET ge_follow_<field>=? WHERE id=? AND contact_id=? AND group_event_id=? in the SAME transaction as the editTouchpointFullCore value write; both the flag write and the value write (and their rollback) are one atomic unit — a failure leaves neither persisted (GRP-04/GRP-11, review cycle-2 HIGH #3)."
    - "Direction, Connected, and the participant note are participant-editable WITHOUT follow semantics (no ge_follow flag, no event value): setParticipantFields writes them directly onto the child via editTouchpointFullCore (which already owns the direction/connected/note columns). This is the persistence path for the participant editor's note (review cycle-2 MED: participant note write path) and its Direction/Connected controls (GRP-04, dossier §E/§L)."
    - "saveParticipantEdits is the SINGLE atomic op the participant editor's Save invokes: it composes EVERY selected field-class change — the Channel/Tone/Duration follow-vs-override value writes AND their ge_follow_<field> flag writes AND the direct Direction/Connected/note writes — in ONE inWriteTransaction with ONE trailing bumpDataRevisionCore, reading the child's whole current row + the event's current shared values once and issuing a SINGLE editTouchpointFullCore covering all changed ordinary columns plus one explicit scoped `UPDATE interactions SET ge_follow_<field>=? … AND group_event_id=?` per changed follow-field. A follow-field change AND a direct-field change in one Save therefore commit together or roll back together — an injected mid-save failure persists NEITHER (this is what makes Plan 06's 'nothing committed on failure' must-have and D-08 'participant changes commit completely or roll back completely' TRUE across field classes; setParticipantOverride/clearParticipantOverride/setParticipantFields each own their OWN transaction+bump and must NOT be chained by the editor for a multi-field Save — review cycle-4 HIGH #1)."
    - "saveParticipantEdits composes editTouchpointFullCore + explicit ge_follow UPDATEs DIRECTLY at the core level — it NEVER calls the mutexed setParticipantOverride/clearParticipantOverride/setParticipantFields (each opens its own inWriteTransaction; nesting is a permanent hang, transaction.ts non-reentrancy) — and asserts child membership by scoping every mutation with `AND group_event_id=?` + changes===1 (review cycle-4 HIGH #1; cycle-2 MED membership scoping)."
    - "Every override/clear/field-set op REQUIRES groupEventId and asserts the child belongs to it (scoping every UPDATE by AND group_event_id=? and requiring changes===1); a stale or malformed route can never apply Group A's value to a standalone or Group B child (review cycle-2 MED: DAO group-membership scoping)."
    - "Editing the event date fans out N occurred_at updates (date is non-overridable), each subject to rejectFutureOccurredAt, and N recency recomputes (GRP-04/GRP-12, dossier §W, D-08)."
    - "editTouchpointFullCore SETs every editable column unconditionally (no COALESCE); the fan-out reads the child's whole current record first and overwrites only the changed field, never nulling siblings (GRP-11, Pitfall 3)."
    - "Every shared/date edit runs inside exactly one inWriteTransaction composing *Core primitives — editTouchpointFullCore per child, NEVER the mutexed editTouchpointFull wrapper (a nested inWriteTransaction is a permanent hang; transaction.ts non-reentrancy); a mid-fan-out failure rolls back fully with no partial visible state and form state preserved (GRP-11, D-08)."
    - "resolveDisplay and computeFollowingChildren are pure node-testable functions with zero react-native imports (GRP-03/04)."
    - {statement: "An event date edit that would set any child occurred_at into the future is rejected before the transaction opens, leaving the event and all children unchanged (GRP-12 concurrency/atomicity backstop against a mixed future/past fan-out).", verification: backstop}
  artifacts:
    - src/logic/group-inheritance.ts
    - "DAO updateGroupEvent — the SINGLE atomic save op taking { groupEventId, now, occurredAt?, patch:{channel?/quality?/duration?/groupNote?:{value}} } (date fan-out + shared fan-out + Group Note update in one transaction; review HIGH #4/#5)"
    - "DAO setParticipantOverride, clearParticipantOverride (Channel/Tone/Duration follow-fields — explicit scoped ge_follow UPDATE + membership assertion), setParticipantFields (direction/connected/note direct edits — no follow flag)"
    - "DAO saveParticipantEdits — the SINGLE atomic participant-Save op composing all selected field-class edits (Channel/Tone/Duration follow/override value+flag writes AND direct direction/connected/note writes) in ONE inWriteTransaction with ONE trailing bump; the editor's Save (Plan 06) invokes ONLY this op (review cycle-4 HIGH #1)"
  key_links:
    - "updateGroupEvent -> (date) editTouchpointFullCore per child; (shared) computeFollowingChildren -> editTouchpointFullCore per following child; (groupNote) UPDATE group_events only — NEVER the mutexed editTouchpointFull (nesting inWriteTransaction is a permanent hang); one inWriteTransaction, one trailing bumpDataRevisionCore"
    - "setParticipantOverride/clearParticipantOverride -> editTouchpointFullCore (value) + explicit scoped UPDATE interactions SET ge_follow_<field> (flag) in the SAME txn (editTouchpointFullCore does not write ge_follow_*, review HIGH #3)"
    - "clearParticipantOverride re-resolves the child value from the current event value (live inheritance, not copy)"
    - "saveParticipantEdits -> ONE editTouchpointFullCore (all changed ordinary columns) + one explicit scoped `UPDATE interactions SET ge_follow_<field>` per changed follow-field, in ONE inWriteTransaction + ONE trailing bump; the participant editor's Save (Plan 06) calls ONLY this — never a chain of setParticipantOverride/clearParticipantOverride/setParticipantFields (review cycle-4 HIGH #1)"
  prohibitions:
    - "A shared-value edit must never leave a following child's resolved value stale."
    - "A participant editor must never expose event date/time or title as overridable fields."
    - "An override/clear must never persist a value change without its ge_follow_<field> flag change in the same transaction (the core does not write flags)."
    - "Any override/clear/field-set must never mutate a child it cannot prove belongs to groupEventId (scope every UPDATE by group_event_id, require changes===1)."
    - "The participant editor's Save must never chain the single-field ops (setParticipantOverride/clearParticipantOverride/setParticipantFields) across separate transactions — a multi-field Save is ONE saveParticipantEdits transaction, so a partial commit is impossible (review cycle-4 HIGH #1)."
---

<objective>
Implement live inheritance: the pure `group-inheritance.ts` resolver (which children follow which field; how to render inherit-vs-override) and the event-edit fan-outs on `group-events-dao.ts`. The three event-level inheritable shared values are Channel, Tone (quality), and Duration (dossier §D/§G); Group Note is an event-level field that does NOT fan out (§H); Direction and Connected are participant-editable child fields with no event-level value (§E/§L, review cycle-2 HIGH #2). Editing a shared Channel/Tone/Duration rewrites only children still following that field; editing the event date rewrites every child's `occurred_at` (date is non-overridable); the Group Note edit updates group_events only; overriding sets the follow flag off; "Follow event …" sets it back on and re-resolves from the current event value. A single `updateGroupEvent` op applies date + shared patch + Group Note in ONE transaction (review cycle-2 HIGH #4/#5 — no split-transaction save). All fan-outs compose the recency `*Core` primitives (D-05, D-08) — each child edited through `editTouchpointFullCore`, the non-mutexed core extracted in Plan 01, never the mutexed `editTouchpointFull` whose nested `inWriteTransaction` would hang permanently (transaction.ts non-reentrancy); because that core never writes `ge_follow_*`, every override/clear adds an explicit scoped flag UPDATE in the same transaction (review cycle-2 HIGH #3).

Purpose: GRP-03 (shared values, live inheritance), GRP-04 (per-field override + "Follow event …" clear + participant note/direction/connected edits), GRP-08 (Group Note write path), and the atomic/future-safe fan-out portion of GRP-11/GRP-12. Plan 06's participant editor exposes several field classes behind one Save, so `saveParticipantEdits` makes that Save atomic across them (D-08; review cycle-4 HIGH #1).
Output: `resolveDisplay`, `computeFollowingChildren`; DAO `updateGroupEvent` (single atomic date+shared+note save), `setParticipantOverride`, `clearParticipantOverride`, `setParticipantFields`, and `saveParticipantEdits` (the single atomic participant-Save op composing all field-class edits in one transaction — review cycle-4 HIGH #1).

Phase tracer note: the end-to-end data-spine tracer is in Plan 01; this plan expands the DAO with the inheritance fan-out on top of the proven `createGroupEvent`/recency spine.
</objective>

<review_findings_addressed>
Cross-AI review cycle 4 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-4 HIGH #1 (participant-editor Save not atomic across field classes): Task 4 adds `saveParticipantEdits`, a single atomic op composing every selected field-class edit (Channel/Tone/Duration follow/override value+flag writes AND direct direction/connected/note writes) in ONE inWriteTransaction with ONE trailing bump, with a mid-save injected-failure rollback test proving NEITHER a follow-field nor a direct-field change persists. Plan 06's editor Save invokes ONLY this op (the three single-field ops remain as primitives but must not be chained by the editor for a multi-field Save).

Cross-AI review cycle 3 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-3 MED (parent-existence assertion): Task 2's updateGroupEvent fetches/asserts the parent group_events row (guarded SELECT) as the FIRST statement in its transaction; a parent deleted before save throws with no write and no revision bump. Tests cover event-deleted-before-save and empty-patch.
- Cycle-3 MED (channel nullability): the patch types channel as `{ value: string }` (never null) — interactions.channel is NOT NULL (migration 001:103), EditTouchpointFullInput.channel is `string` (recency-dao.ts:96) — with a channel fan-out test; quality/duration stay nullable/unsettable.
- Cycle-3 LOW (DAO tsc): every task's <verify> adds `npx tsc --noEmit` so a DAO type regression (e.g. the channel mismatch) is caught at the task boundary.

Cross-AI review cycle 2 (33-REVIEWS.md, top) findings resolved in this plan:
- Cycle-2 HIGH #2 (direction/connected inheritance source): conformed to the dossier. group-inheritance.ts (Task 1) and the override/clear path (Task 3) treat ONLY Channel/Tone/Duration as event-inheritable follow-fields. Direction/Connected have no event value and no follow flag — they are direct participant edits via `setParticipantFields`. Matches Plan 01's three-flag schema.
- Cycle-2 HIGH #3 (editTouchpointFullCore cannot persist ge_follow_*): Task 3's setParticipantOverride/clearParticipantOverride add an EXPLICIT scoped `UPDATE interactions SET ge_follow_<field>=?` in the same transaction as the editTouchpointFullCore value write, with a rollback test proving neither persists on failure.
- Cycle-2 HIGH #4 (Group Note has no write path): Task 2's `updateGroupEvent` patch includes `groupNote?: {value}`, updating group_events.group_note (event-level, no fan-out, dossier §H).
- Cycle-2 HIGH #5 (split-transaction Edit Group Event save): Task 2 defines ONE atomic `updateGroupEvent({ groupEventId, now, occurredAt?, patch })` covering date + shared patch + Group Note in a single inWriteTransaction with one trailing bump — the Edit form (Plan 06) calls this single op, never two sequential DAO calls.
- Cycle-2 MED (DAO group-membership scoping): Task 3's override/clear/field-set require groupEventId and scope every UPDATE by `AND group_event_id=?` with a changes===1 assertion.
- Cycle-2 MED (participant note write path): Task 3 adds `setParticipantFields` (direction/connected/note direct edits via editTouchpointFullCore) — the participant editor's note/direction/connected persistence path.

Cycle-1 findings (preserved, not undone):
- HIGH #5 (nullable-patch contract): the explicit per-field patch where key presence distinguishes "leave unchanged" from "set to null/unset" is retained — now as the patch on `updateGroupEvent`; tests cover null-unset, multi-field-atomic, mixed follow/override, and rollback.
- Non-HIGH (thin logic-level regression for GRP-03/04/06): Task 2's createGroupEvent→updateGroupEvent→re-read round-trip is the node-level logic backstop Plan 06 references from its acceptance.
</review_findings_addressed>

## Artifacts this phase produces
(Full phase inventory; ★ = owned by this plan.)
- Table `group_events`; `interactions.group_event_id` + `ge_follow_*`; `idx_group_member_unique`; `migration026`; tombstone `"group_event"`; extended `insertInteractionCore`; non-mutexed `editTouchpointFullCore`; `createGroupEvent` (Plan 01)
- ★ Pure logic `src/logic/group-inheritance.ts` — `resolveDisplay`, `computeFollowingChildren` (over the three inheritable fields Channel/Tone/Duration)
- ★ DAO `updateGroupEvent` (single atomic save: `{ groupEventId, now, occurredAt?, patch:{ channel?/quality?/duration?/groupNote?: {value} } }`), `setParticipantOverride`, `clearParticipantOverride` (Channel/Tone/Duration follow-fields; explicit ge_follow UPDATE + membership assertion), `setParticipantFields` (direction/connected/note direct child edits, no follow flag), `saveParticipantEdits` (the single atomic participant-Save op composing all field-class edits in one transaction — review cycle-4 HIGH #1)
- DAO `addParticipant`, `detachParticipant`, `deleteGroupChild`, `dissolveGroupEvent`, `deleteGroupEventAndInteractions`, `convertInteractionToGroupEvent` (Plan 05)
- Reads `listGroupEvents`, `searchGroupEvents`, `readGroupEventDetail`, `resolveParticipants` (Plan 04)
- `ContactPicker` multi-select + route types `GroupEventDetail`/`EditGroupEvent` (Plan 02)
- Screens `GroupLogScreen`, `EditGroupEventScreen`, `GroupEventDetailScreen`, replaced `GroupEventsScreen`; components `ParticipantOverrideEditor`, `ParticipantCard`; wired `GroupScopePrompt`, `InteractionDetail` (Plans 06/07)

<flagged_assumption id="GRP-04-unclassified">
The edge probe classified GRP-04 as **unclassified** (a data-shape classifier miss on the per-field override / "Follow event…" clear semantics), so it is surfaced here rather than auto-resolved. Resolvable content from dossier §D/§G/§L:
- The three event-inheritable shared values with override/inheritance state are EXACTLY Channel, Tone (quality), Duration, stored as discrete per-field `ge_follow_*` flag columns (A1 — planner discretion, resolved: cleanest fan-out predicate + self-contained backup rows). Direction and Connected are participant-editable child fields with no event value and no follow flag (dossier §D/§E/§L; review cycle-2 HIGH #2) — they are direct edits, not overrides.
- "Follow event <field>" (explicit wording, never a bare "Reset") clears an override by setting `ge_follow_<field>=1` and re-resolving the child value from the CURRENT event value — it is NOT a one-time copy; a later event-value change flows through again. Applies only to Channel/Tone/Duration.
- An override may equal the current event value while remaining detached (dossier §F).
Resolution: implemented as the must_haves truths above. Not an owner-pending item (A1 is discretion); recorded here only because the probe could not classify it — no silent drop.
</flagged_assumption>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-RESEARCH.md
@.planning/phases/33-group-interaction-logging/33-PATTERNS.md
@docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md
@src/db/group-events-dao.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Pure inheritance resolver (group-inheritance.ts)</name>
  <files>src/logic/group-inheritance.ts, src/logic/group-inheritance.test.ts</files>
  <read_first>
    - src/components/history/interaction-detail-logic.ts:146-159 (buildGroupContext — the react-native-free logic-module pattern to mirror)
    - src/db/history-read.ts:104-106 (isGroupLinked discriminator)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §G (the Sarah/Mike/Jordan inheritance example — encode it as test cases)
  </read_first>
  <behavior>
    - resolveDisplay(child, event) returns, for each of the THREE inheritable fields (channel, quality=Tone, duration), { value, following: boolean, label } where following = (child.geFollow<field> === 1); when following, value = event value; when overridden, value = child value. Direction and Connected are returned as plain child values with NO `following` concept (no event value exists — dossier §D/§E/§L, review cycle-2 HIGH #2); resolveDisplay must NOT read a geFollowDirection/geFollowConnected flag (those columns do not exist).
    - computeFollowingChildren(children, field) accepts ONLY field ∈ {channel, quality, duration} and returns the subset of children with geFollow<field> === 1 (the fan-out target set).
    - Dossier §G case: event Tone Positive→Neutral, Duration 2h→3h; Sarah(Tone override Negative) stays Negative + inherits 3h; Mike(Duration override 45m) inherits Neutral + stays 45m; Jordan(no overrides) becomes Neutral/3h.
    - Zero react-native imports; runs under node/vitest.
  </behavior>
  <action>
    Create `src/logic/group-inheritance.ts` with `resolveDisplay(child, event)` and `computeFollowingChildren(children, field)` as pure functions over plain row shapes. The inheritable/follow fields are EXACTLY channel, quality (Tone), duration (each with its `geFollow<field>` flag). Direction and Connected are present on the child shape as ordinary editable values but are NOT follow fields — do not give them a ge_follow flag and do not treat them as inheritable (dossier §D/§E/§L; review cycle-2 HIGH #2). No DB, no react-native. Author `group-inheritance.test.ts` encoding the dossier §G example verbatim (Tone + Duration only), the equal-value-but-detached case (§F), and a case asserting direction/connected are surfaced as plain child values with no `following` flag.
  </action>
  <verify>
    <automated>npx vitest run src/logic/group-inheritance.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test encodes the dossier §G example and passes; equal-value-but-detached case passes; computeFollowingChildren (channel/quality/duration only) returns exactly the following subset per field.
    - resolveDisplay returns direction/connected as plain child values with no `following` flag and reads no geFollowDirection/geFollowConnected (review cycle-2 HIGH #2).
    - Module has no react-native import (loads under node).
  </acceptance_criteria>
  <done>The inherit-vs-override resolution (Channel/Tone/Duration) and the fan-out target-set computation are pure, node-tested functions; Direction/Connected are plain child values, not follow-fields.</done>
</task>

<task type="tdd">
  <name>Task 2: Single atomic event edit — updateGroupEvent (date + shared patch + Group Note, one transaction)</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (createGroupEvent from Plan 01 — same composition idiom)
    - src/db/recency-dao.ts (editTouchpointFullCore — the non-mutexed edit primitive extracted in Plan 01: SETs every editable ordinary column unconditionally, throws on changes!==1, then recomputes recency. It does NOT open a transaction, does NOT re-guard the future date, does NOT bump the revision, and does NOT write ge_follow_* — the composing caller owns all of those. NEVER call the mutexed editTouchpointFull wrapper inside a group txn — a nested inWriteTransaction is a permanent hang)
    - src/db/recency-dao.ts:446-460 (the *Core export block — insertInteractionCore, recomputeLastContactCore, deleteInteractionCore, editTouchpointFullCore)
    - src/logic/group-inheritance.ts (computeFollowingChildren — the fan-out predicate, channel/quality/duration only)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §H/§W (Group Note is ONE shared event-level note, not duplicated into children; editing it updates shared context only) + §D (event owns Channel/Tone/Duration) + §AA (one transactional boundary, commit-or-rollback)
    - EXCEPTIONAL existing interactions/last_contact writers to be aware of before asserting a fan-out invariant (the graph cannot enumerate SQL writers — CLAUDE.md; review: exhaustive writer audit): src/backup/restore-apply.ts:190-201 (restore upsert — separate writer, no recompute), src/db/purge-dao.ts:82 (contact-purge DELETE), src/db/migrations/025-interaction-history-schema.ts:44-72 (migration backfill). The fan-outs here touch ONLY group children via editTouchpointFullCore and never these paths.
    - .planning/phases/33-group-interaction-logging/33-RESEARCH.md (Pattern 2; Pitfall 3)
  </read_first>
  <behavior>
    - updateGroupEvent takes `{ groupEventId, now, occurredAt?, patch }` where `patch` = `{ channel?: { value: string }, quality?: { value: string|null }, duration?: { value: number|null }, groupNote?: { value: string|null } }` (channel's value is `string`, never null — interactions.channel is NOT NULL, so channel is changeable but not unsettable; review cycle-3 MED). It applies the DATE change (when occurredAt present), the SHARED-VALUE patch (channel/quality/duration), and the GROUP NOTE change together in exactly ONE inWriteTransaction with ONE trailing bumpDataRevisionCore (review cycle-2 HIGH #5: no split-transaction save — the Edit form calls this single op, never two sequential DAO calls).
    - updateGroupEvent FETCHES the parent (a guarded `SELECT … FROM group_events WHERE id=?` by groupEventId) as the FIRST statement inside the transaction and throws if absent — a parent deleted before save rejects with NO write and NO revision bump (full rollback); an empty patch over an existing parent completes as a no-op with no child writes (review cycle-3 MED: parent-existence assertion).
    - For each shared field (channel/quality/duration) PRESENT in the patch: UPDATE that column on group_events to `patch.<field>.value` (null → UNSET, distinct from an absent key = leave unchanged), then for each following child (ge_follow_<field>=1) read the child's whole current record, overwrite only that field, and call editTouchpointFullCore (never the mutexed editTouchpointFull); non-following children untouched.
    - groupNote present → UPDATE group_events.group_note only (event-level; NO fan-out, NOT materialized on any child — dossier §H); groupNote absent → the note is left unchanged; `{ value: null }` clears it (review cycle-2 HIGH #4: Group Note write path).
    - occurredAt present → rejectFutureOccurredAt(occurredAt, now) BEFORE the transaction opens (the core no longer re-guards; every child inherits the event occurred_at so one guard covers the whole fan-out), then UPDATE group_events.occurred_at and rewrite EVERY child's occurred_at (date non-overridable) via editTouchpointFullCore; last_contact recomputed per child.
    - A mid-save failure (e.g. an injected editTouchpointFullCore failure on one child, or a failing field UPDATE) rolls back the WHOLE transaction — date, every shared field, the Group Note, and every child revert together; nothing partially persists.
    - A multi-child (2+ following) fan-out COMMITS without hanging — the regression guard proving the fan-out composes editTouchpointFullCore, not the mutexed wrapper.
  </behavior>
  <action>
    Add ONE atomic `updateGroupEvent(exec, { groupEventId, now, occurredAt?, patch })` to `group-events-dao.ts` — it is the single write path the Edit Group Event form (Plan 06) calls (review cycle-2 HIGH #5 — do NOT ship separate updateGroupEventShared + updateGroupEventDate that the form must sequence across two transactions). `patch` is an explicit per-field ops map `{ channel?: { value: string }, quality?: { value: string | null }, duration?: { value: number | null }, groupNote?: { value: string | null } }` (review cycle-1 HIGH #5 contract, retained; channel's value is `string`, NOT `string | null` — interactions.channel is NOT NULL DEFAULT 'unspecified' (migration 001:103) and EditTouchpointFullInput.channel is `string` (recency-dao.ts:96), so channel can be CHANGED but never UNSET; the type constraint is the fix — do NOT coerce a null channel to a magic string, review cycle-3 MED): KEY PRESENCE = "change this field" (to `.value`; for quality/duration `.value` may be `null` = unset, for channel `.value` is always a real string); an ABSENT key = leave that field untouched — the only shape that distinguishes "leave Tone unchanged" from "set Tone to null/unset" (GRP-03/D-09), which a flat map cannot. The op opens exactly ONE `inWriteTransaction` and composes the non-mutexed `editTouchpointFullCore` (Plan 01) per child — NEVER the mutexed `editTouchpointFull`, whose own `inWriteTransaction` nested inside this one is a permanent hang (transaction.ts non-reentrancy). Sequence inside the single transaction: (0) FETCH the parent with a guarded `SELECT … FROM group_events WHERE id = ?` as the FIRST statement and throw if no row — a parent deleted between form-load and save must reject before any write and before the trailing `bumpDataRevisionCore` is reached (full rollback, NO revision bump, no silent zero-row success; matches the loud-failure pattern at recency-dao.ts:295-335, review cycle-3 MED: parent-existence assertion); (1) if `occurredAt` present, UPDATE group_events.occurred_at and `editTouchpointFullCore` EVERY child to the new occurred_at (recompute per child); (2) for each shared field in {channel, quality, duration} present in `patch`, UPDATE it on group_events (`?`-bound, value may be null), select that field's following children (`WHERE group_event_id=? AND ge_follow_<field>=1`), and for each read the full row and call `editTouchpointFullCore` with the whole record + only that field changed (Pitfall 3 — never pass a partial record); (3) if `patch.groupNote` present, UPDATE group_events.group_note only (no fan-out — dossier §H). Guard BEFORE the transaction: if `occurredAt` present, `rejectFutureOccurredAt(occurredAt, now)` returning `Promise.reject(err)` on throw (GRP-12/D-08 — one guard covers the whole date fan-out since every child inherits the event occurred_at). End with a single `bumpDataRevisionCore` (the core does not bump, so this trailing bump is the one revision bump for the whole save, covering the note-only or zero-following-child case too). Extend `group-events-dao.test.ts`: a 2+-following-child fan-out that COMMITS (regression guard against the wrapper-nesting hang); a `channel` change that fans out to every following child (channel survives the `channel: string` core contract — no null fan-out, review cycle-3 MED); a patch that sets `quality`/`duration` to null (both unset on event + following children); a Group-Note-only patch that updates group_events.group_note and touches NO child (and reading a child never sees the group note materialized); a combined save (occurredAt + channel + groupNote together) that commits atomically; a mixed follow/override child set (following child rewritten, overridden child untouched); an injected-failure rollback case where a mid-save child failure reverts date + all shared fields + the note together; a future occurredAt rejected before the txn opens (no writes); a parent DELETED before save (a groupEventId whose group_events row no longer exists) that throws before any write with NO revision bump (counter delta 0); and an empty patch (no occurredAt, no fields) over an existing parent that makes no child writes (review cycle-3 MED: parent-existence assertion). This createGroupEvent→updateGroupEvent→re-read round-trip is the node-level LOGIC backstop for GRP-03/04/06/08 that Plan 06 references (review: thin logic-level regression).
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - updateGroupEvent applies date + shared patch + Group Note in ONE inWriteTransaction with ONE trailing bump; there is no separate updateGroupEventShared/updateGroupEventDate the form must call (review cycle-2 HIGH #5).
    - updateGroupEvent asserts the parent exists FIRST (guarded SELECT); a groupEventId whose parent was deleted before save throws with NO write and NO revision bump (counter delta 0); an empty patch over an existing parent is a no-op over children (review cycle-3 MED: parent-existence assertion).
    - A Group-Note-only patch UPDATEs group_events.group_note and fans out to NO child (note never materialized on a child — dossier §H; review cycle-2 HIGH #4); `{ value: null }` clears the note.
    - A multi-child (2+ following) shared fan-out COMMITS without hanging and updates every following child's resolved value + recomputed last_contact (regression guard: the mutexed editTouchpointFull here would deadlock on device).
    - A patch that sets `quality`/`duration` to `null` UNSETS them on the event and following children; an OMITTED key leaves that field untouched (key-presence semantics).
    - A `channel` change fans out to every following child (channel survives editTouchpointFullCore's `channel: string` contract — no tsc error, no NOT NULL violation); the patch type forbids a null channel, so channel is never unset (review cycle-3 MED: channel nullability).
    - A combined save (occurredAt + a shared field + groupNote) commits atomically; an injected mid-save failure reverts date + every shared field + the note + all children together (full rollback).
    - Shared Tone edit rewrites only following children; overridden children unchanged (encode dossier §G). Date edit rewrites every child occurred_at + recomputes; a future date rejects before the txn opens (no writes).
    - No `editTouchpointFullCore` call passes a partial record (siblings never nulled); the op never calls the mutexed `editTouchpointFull`.
  </acceptance_criteria>
  <done>One atomic `updateGroupEvent` applies the event date, the shared Channel/Tone/Duration patch, and the Group Note through the recency cores in a single transaction — no split-transaction save, Group Note persisted, live-inheritance semantics verified.</done>
</task>

<task type="tdd">
  <name>Task 3: Per-participant override set/clear (Channel/Tone/Duration) + direct field edits (direction/connected/note)</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (updateGroupEvent from Task 2 — reuse the read-full-row-then-editTouchpointFullCore idiom)
    - src/db/recency-dao.ts:303-337 (editTouchpointFull UPDATE column list — it writes occurred_at/channel/direction/connected/quality/note/duration/allow_ai/modified_at and NO ge_follow_* column; editTouchpointFullCore inherits exactly this, which is WHY the follow-flag needs a separate explicit UPDATE — review cycle-2 HIGH #3)
    - src/db/recency-dao.ts:446-460 (the *Core export block — editTouchpointFullCore; compose it inside the DAO's own transaction, never the mutexed editTouchpointFull)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §E/§F/§L (participant-editable fields: Channel/Tone/Duration follow-fields + Direction/Connected/note direct edits; override semantics; date/title never participant-editable)
  </read_first>
  <behavior>
    - setParticipantOverride(field∈{channel,quality,duration}, value) writes the overriding value onto the child via editTouchpointFullCore AND sets ge_follow_<field>=0 via an explicit scoped UPDATE in the SAME transaction; the child stops following future event changes for that field.
    - clearParticipantOverride(field∈{channel,quality,duration}) re-resolves the child value from the CURRENT event value (via editTouchpointFullCore) AND sets ge_follow_<field>=1 via the explicit scoped UPDATE, in the same transaction (live inheritance re-established).
    - Override/clear accept ONLY channel, quality (Tone), duration (the three event-inheritable follow-fields). date/time and title are rejected; direction/connected/note are NOT follow-fields and are not valid override/clear targets.
    - setParticipantFields({ direction?, connected?, note? }) writes those ordinary columns directly onto the child via editTouchpointFullCore with NO follow-flag change — the persistence path for the participant editor's note, direction, and connected controls (review cycle-2 MED: participant note write path; direction/connected per dossier §E/§L).
    - Every op asserts the child belongs to groupEventId: each scoped UPDATE/edit requires `group_event_id=?` and the op fails (rollback) if it matches no row — a stale/malformed route can never apply one group's change to a standalone or another group's child (review cycle-2 MED: membership scoping).
    - An override equal to the current event value still sets ge_follow=0 (detached) and does not re-follow a later event change; a mid-op failure leaves NEITHER the value nor the flag persisted (atomic value+flag).
  </behavior>
  <action>
    Add `setParticipantOverride(exec, { interactionId, contactId, groupEventId, field, value, now })`, `clearParticipantOverride(exec, { interactionId, contactId, groupEventId, field, now })`, and `setParticipantFields(exec, { interactionId, contactId, groupEventId, fields: { direction?, connected?, note? }, now })` to `group-events-dao.ts`, each one `inWriteTransaction`. For override/clear: restrict `field` to the THREE event-inheritable fields (channel, quality=Tone, duration — never date/time, title, direction, connected, or note; dossier §D/§L). Read the child's full row, compute the resolved value (the given value for override; the CURRENT event value read from group_events for clear), apply it through `editTouchpointFullCore` (whole record, one field changed; never the mutexed `editTouchpointFull` — nesting `inWriteTransaction` hangs; occurred_at unchanged so no future-date guard), AND — because `editTouchpointFullCore` does NOT write ge_follow_* (review cycle-2 HIGH #3) — issue an explicit `UPDATE interactions SET ge_follow_<field> = ? WHERE id = ? AND contact_id = ? AND group_event_id = ?` (0 for override, 1 for clear) in the SAME transaction, asserting `changes === 1` (this both persists the flag and ENFORCES group membership — review cycle-2 MED; a child not belonging to groupEventId changes 0 rows → throw → rollback). For `setParticipantFields`: read the child's full row, overwrite only the present direction/connected/note fields, and apply through `editTouchpointFullCore` (these are ordinary columns the core already writes — no ge_follow flag); scope membership by performing/validating against `group_event_id=?` (e.g. assert the fetched child row's group_event_id === groupEventId before editing) so a non-member child is rejected. End each op with a single `bumpDataRevisionCore` (the core does not bump). Extend the DAO test: override then clear round-trip for Tone (assert ge_follow_quality flips 0→1 and the value re-resolves); a subsequent shared-value edit flows to a cleared child but not to an overridden one; equal-value override stays detached (§F); an injected failure AFTER the value write but BEFORE/at the flag UPDATE rolls back BOTH (value and flag unchanged — review cycle-2 HIGH #3); setParticipantFields persists a note + direction + connected with no follow-flag change; and every op called with a groupEventId the child does not belong to throws and writes nothing (review cycle-2 MED: membership scoping).
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Override sets the value via editTouchpointFullCore AND ge_follow_<field>=0 via an explicit scoped UPDATE; clear sets the re-resolved value AND ge_follow_<field>=1 — both in one inWriteTransaction; a mid-op failure rolls back BOTH the value and the flag (review cycle-2 HIGH #3).
    - Override/clear accept ONLY channel/quality/duration; date/title/direction/connected/note are rejected as override/clear targets.
    - `setParticipantFields` persists direction/connected/note directly (no follow-flag), the participant editor's write path (review cycle-2 MED: participant note).
    - Every override/clear/field-set scopes by `group_event_id=?` (changes===1) and throws + rolls back when the child does not belong to groupEventId (review cycle-2 MED: membership scoping).
    - After clear, a later shared edit reaches the child; after override, it does not; equal-value override stays detached.
  </acceptance_criteria>
  <done>Channel/Tone/Duration override + "Follow event …" clear persist value AND flag atomically with membership scoping; direction/connected/note have a direct-edit write path; verified by round-trip, rollback, and membership tests.</done>
</task>

<task type="tdd">
  <name>Task 4: Single atomic participant Save — saveParticipantEdits (all field classes, one transaction)</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/db/group-events-dao.ts (setParticipantOverride/clearParticipantOverride/setParticipantFields from Task 3 — the per-field semantics saveParticipantEdits composes at the CORE level; do NOT call these mutexed ops from inside saveParticipantEdits — each opens its own inWriteTransaction and nesting is a permanent hang, transaction.ts non-reentrancy)
    - src/db/group-events-dao.ts (updateGroupEvent from Task 2 — the read-full-row-then-editTouchpointFullCore-once + explicit scoped ge_follow UPDATE idiom to reuse)
    - src/db/recency-dao.ts:446-460 (the *Core export block — editTouchpointFullCore is non-mutexed, SETs every editable ordinary column unconditionally, does not open a transaction, does not bump, does not write ge_follow_*)
    - src/logic/group-inheritance.ts (resolveDisplay — to compute the re-resolved-from-event value for any follow-field set back to follow)
    - src/db/transaction.ts:49-64 (inWriteTransaction NON-REENTRANT — why saveParticipantEdits composes cores, never the mutexed per-field ops)
    - docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md §E/§L (the participant-editable field set) + §AA (one transactional boundary, commit-or-rollback)
  </read_first>
  <behavior>
    - saveParticipantEdits takes `{ interactionId, contactId, groupEventId, now, follow?: { channel?: {follow: boolean, value?: string}, quality?: {follow: boolean, value?: string|null}, duration?: {follow: boolean, value?: number|null} }, fields?: { direction?, connected?, note? } }` (a composite describing every field-class change the editor's Save collected) and applies ALL of them in ONE inWriteTransaction with ONE trailing bumpDataRevisionCore.
    - It reads the child's whole current row and the event's current shared values ONCE; builds the fully-updated ordinary-column record (for each PRESENT follow-field: the override value when follow=false, the CURRENT event value when follow=true; for each PRESENT direct field: the provided value; every unspecified column keeps its current value); calls editTouchpointFullCore ONCE with that whole record; then issues one explicit `UPDATE interactions SET ge_follow_<field>=? WHERE id=? AND contact_id=? AND group_event_id=?` (asserting changes===1) per PRESENT follow-field (0 when follow=false, 1 when follow=true).
    - Membership: every mutation is scoped by `AND group_event_id=?` (changes===1) so a child that does not belong to groupEventId changes 0 rows → throw → full rollback.
    - Atomicity: a mid-save injected failure (e.g. after the value write but before/at a flag UPDATE, or on the second of two field-class writes) rolls back the WHOLE transaction — NEITHER the follow-field value/flag NOR the direct field persists; the revision bumps exactly zero times on failure and exactly once on success.
    - occurred_at and title are never touched (date/title are not participant-editable — dossier §L); no future-date guard is needed (occurred_at unchanged).
  </behavior>
  <action>
    Add `saveParticipantEdits(exec, { interactionId, contactId, groupEventId, now, follow?, fields? })` to `group-events-dao.ts` — the SINGLE op the participant editor's Save calls (review cycle-4 HIGH #1: the editor must NOT chain setParticipantOverride/clearParticipantOverride/setParticipantFields across separate transactions, which leaves a partial commit when a later op fails). Open exactly ONE `inWriteTransaction`. Inside: (1) fetch the child's full current row scoped `WHERE id=? AND contact_id=? AND group_event_id=?` and throw on no row (membership assertion); (2) fetch the event's current shared values (channel/quality/duration) from group_events by groupEventId; (3) build the updated ordinary-column record from the fetched child row, overwriting — for each PRESENT `follow.<field>` the given `.value` when `follow===false` (override) or the current EVENT value when `follow===true` (re-resolve, live inheritance), and for each PRESENT `fields.<direction|connected|note>` the provided value — leaving every other column at its current value (never a partial SET — reuse the Pitfall-3 whole-record idiom); (4) call `editTouchpointFullCore(exec, updatedRecord)` ONCE (non-mutexed core — occurred_at unchanged so no future-date guard); (5) for each PRESENT `follow.<field>`, issue `UPDATE interactions SET ge_follow_<field> = ? WHERE id=? AND contact_id=? AND group_event_id=?` with 0 (override) or 1 (follow), asserting `changes===1`; (6) end with ONE `bumpDataRevisionCore`. NEVER call the mutexed setParticipantOverride/clearParticipantOverride/setParticipantFields from inside (nesting inWriteTransaction hangs). Extend `group-events-dao.test.ts`: a Save that changes a follow-field (Tone override) AND a direct field (note) together commits both and bumps once; a Save where an injected failure fires AFTER the editTouchpointFullCore value write but AT/BEFORE the ge_follow flag UPDATE rolls back BOTH the value and the flag AND the direct field (re-read shows the pre-save state, revision delta 0 — review cycle-4 HIGH #1); a Save setting a follow-field back to follow re-resolves that field from the current event value and sets ge_follow=1; a Save on a child that does not belong to groupEventId throws and writes nothing; a Save that changes only direct fields persists them with no ge_follow change and bumps once.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts && npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - saveParticipantEdits applies all present follow-field (value+flag) and direct (direction/connected/note) changes in ONE inWriteTransaction with ONE trailing bump; it composes editTouchpointFullCore + explicit ge_follow UPDATEs and NEVER calls the mutexed per-field ops (review cycle-4 HIGH #1).
    - A mid-save injected failure rolls back the WHOLE Save — neither a changed follow-field (value or flag) nor a changed direct field persists; revision delta is 0 on failure and 1 on success.
    - Every mutation is scoped by `AND group_event_id=?` (changes===1); a non-member child throws and writes nothing.
    - Setting a follow-field back to follow re-resolves it from the current event value and sets ge_follow=1; setting it to override writes the value and ge_follow=0.
  </acceptance_criteria>
  <done>saveParticipantEdits commits every field-class change the participant editor exposes in one atomic transaction; a partial participant Save is impossible, satisfying D-08 and Plan 06's 'nothing committed on failure' must-have.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-value edit → N child rows | one edit fans out to many interactions rows |
| event-date edit → N occurred_at + N recency recomputes | timestamp authority propagates to children |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering / integrity | partial fan-out leaves stale/partial child values | high | mitigate | One inWriteTransaction composing editTouchpointFullCore (never the mutexed editTouchpointFull → non-reentrant hang); read-full-record-then-edit (no partial SET); full rollback on failure (Pitfall 3, GRP-11) |
| T-33-09 | Tampering | future occurred_at reaches a child via date edit | medium | mitigate | rejectFutureOccurredAt on the event date BEFORE the txn (every child inherits the event occurred_at, so one guard covers the whole fan-out; editTouchpointFullCore itself does not re-guard) (GRP-12, D-08) |
| T-33-10 | Tampering | shared value fields interpolated into SQL | low | mitigate | `?`-bound params only for every field value |
| T-33-25 | Tampering / integrity | override persists value but not the ge_follow flag (or vice-versa) → child silently (un)follows | high | mitigate | The value write (editTouchpointFullCore) and the explicit scoped `UPDATE … SET ge_follow_<field>` run in ONE transaction; a mid-op failure rolls back both (review cycle-2 HIGH #3) |
| T-33-26 | Tampering / integrity | a stale/malformed route applies one group's change to a standalone or another group's child | high | mitigate | Every override/clear/field-set scopes UPDATEs by `AND group_event_id=?` + asserts changes===1 → a non-member child changes 0 rows → throw + rollback (review cycle-2 MED: membership scoping) |
| T-33-27 | Tampering / integrity | parent deleted between form-load and save → zero-row parent UPDATE silently "succeeds" and bumps the revision | medium | mitigate | updateGroupEvent fetches/asserts the parent (guarded SELECT) as the FIRST statement in its transaction; a missing parent throws before any write and before the trailing bumpDataRevisionCore (review cycle-3 MED: parent-existence assertion) |
| T-33-29 | Tampering / integrity | multi-field participant Save partially commits (follow-field commits, direct field fails, or vice-versa) → child in a half-saved state | high | mitigate | saveParticipantEdits composes ALL field-class changes in ONE inWriteTransaction with ONE trailing bump; a mid-save failure rolls back everything (review cycle-4 HIGH #1, D-08) |
</threat_model>

<verification>
- `npm test` green; `npm run check:colors` passes.
- No nested inWriteTransaction; every child write through editTouchpointFullCore (which recomputes recency), composed inside the DAO's single inWriteTransaction — never the mutexed editTouchpointFull.
- `updateGroupEvent` is the single atomic date+shared+note save (no separate shared/date ops the form sequences); override/clear persist value AND ge_follow flag in one transaction (explicit flag UPDATE) and scope every mutation by `group_event_id` (review cycle-2 HIGH #3/#4/#5 + MED membership).
- `saveParticipantEdits` is the single atomic participant-Save op (all field classes in one transaction, one bump, membership-scoped) with a mid-save rollback test; the editor's Save calls ONLY it (review cycle-4 HIGH #1).
</verification>

<success_criteria>
Live inheritance works end-to-end at the DAO level: shared edits reach exactly the following children, date edits reach all children future-safely, overrides detach and "Follow event …" re-attaches — all atomic, all through the recency cores.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-03-SUMMARY.md` when done.
</output>
