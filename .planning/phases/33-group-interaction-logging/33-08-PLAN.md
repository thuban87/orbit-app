---
phase: 33-group-interaction-logging
plan: 08
type: execute
wave: 6
depends_on: ["33-07"]
files_modified:
  - src/db/group-events-dao.ts
  - src/db/group-events-dao.test.ts
  - src/components/ContactPicker.tsx
  - src/components/contact-picker-multiselect.ts
  - src/components/contact-picker-multiselect.test.ts
  - src/screens/EditGroupEventScreen.tsx
  - src/screens/GroupEventDetailScreen.tsx
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/db/group-events-read.ts
  - src/db/group-events-read.test.ts
  - src/screens/group-event-detail-logic.ts
  - src/screens/group-event-detail-logic.test.ts
autonomous: true
gap_closure: true
requirements: [GRP-06, GRP-08, GRP-11, GRP-13]
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Adding multiple selected contacts to an already-saved Group Event commits every new canonical child and recency recomputation together, or commits none of them; a late child failure leaves the data revision unchanged (GRP-06, GRP-11; D-05/D-08)."
    - "If a saved-event participant batch rejects, the open multi-select picker keeps its selected contacts and presents the failure; a successful batch clears and dismisses it only after one reload completes (GRP-06, GRP-11; D-05/D-08)."
    - "Dissolving or deleting a Group Event retains its durable local group_event tombstone yet the current format-4 backup export continues to validate until Phase 36 serializes the Group Event wire entity (GRP-13; D-10)."
    - "Group Event Detail shows a seconds-backed duration with the shared duration formatter and passes the existing child allow_ai state to InteractionDetail without changing the settled participant-editor fields (GRP-08; D-04/D-11)."
  artifacts:
    - "src/db/group-events-dao.ts exposes one batch participant-add transaction composed from the canonical recency cores."
    - "src/components/ContactPicker.tsx awaits a multi-select confirmation outcome without changing existing synchronous picker callers."
    - "src/backup/export-manifest.ts contains the temporary pre-Phase-36 serialization guard for durable group_event tombstones."
    - "src/screens/group-event-detail-logic.ts is the tested view-model seam consumed by GroupEventDetailScreen for formatted duration and truthful participant Detail input."
  key_links:
    - "EditGroupEventScreen and GroupEventDetailScreen each invoke the batch DAO once per picker confirmation, rather than creating independent write transactions."
    - "ContactPicker retains its selected set and visible Modal until each saved-event owner's awaited batch-and-reload callback resolves successfully."
    - "The export snapshot filters only group_event tombstones; Phase 36's backup-schema, restore, and reconciliation registries remain untouched."
    - "resolveParticipants projects interactions.allow_ai AS allowAi and the Group Event Detail view model forwards it to InteractionDetail."
---

<objective>
Close the four verification-confirmed Phase-33 gaps without widening settled product scope or pulling Phase 36's coordinated backup wire-format/restore work forward.

Purpose: Preserve the atomic Group Event fan-out contract, keep the already-shipped format-4 export usable after a lifecycle action, and make Group Event Detail render existing data truthfully.

Output: One batch-add DAO/API with rollback coverage; a strictly temporary legacy-export compatibility guard with lifecycle integration coverage; and a tested Group Event Detail projection for duration and Allow-AI.

Artifacts this phase produces:
- `addParticipants` (or equivalently named plural batch API) in `src/db/group-events-dao.ts`, plus DAO rollback/success coverage.
- A format-4 manifest serialization guard in `src/backup/export-manifest.ts`, documented for Phase 36 removal/replacement.
- A small, node-testable Group Event Detail view-model module consumed by the screen, and projection/read tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-group-interaction-logging/33-VERIFICATION.md
@.planning/phases/33-group-interaction-logging/33-REVIEW.md
@.planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md
@.planning/phases/33-group-interaction-logging/33-CONTEXT.md
@.planning/phases/33-group-interaction-logging/33-05-SUMMARY.md
@.planning/phases/33-group-interaction-logging/33-07-SUMMARY.md
@docs/dossier/milestone-2/phase-12-group-interaction-logging-dossier.md
</context>

<source_audit>

| Source | ID | Closure item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | atomic fan-out and surviving backup | Saved-event multi-add must be all-or-nothing; current backup cannot fail after lifecycle actions | Tasks 1-2 | COVERED |
| REQ | GRP-06 | Saved multi-select additions inherit current parent values at the event occurrence time | Task 1 | COVERED |
| REQ | GRP-08 | Detail remains presentation-first and represents child data truthfully | Task 3 | COVERED |
| REQ | GRP-11 | Every Group Event fan-out mutation either commits fully or rolls back | Task 1 | COVERED |
| REQ | GRP-13 | Existing backup export remains valid pending the Phase-36 format/restore delivery | Task 2 | COVERED |
| CONTEXT | D-04 | Group Note stays structurally outside AI; only the child’s existing Allow-AI state is displayed | Task 3 | COVERED |
| CONTEXT | D-05 / D-08 | One outer write transaction, core composition, no nested transaction or direct bulk interaction writer | Task 1 | COVERED |
| CONTEXT | D-10 | Phase 33 carries the compatibility boundary; Phase 36 owns wire entity, validation allowlist, reconciliation, and restore | Task 2 | COVERED |
| CONTEXT | D-11 | Participant editor field scope is unchanged; this closure only fixes Detail’s read-through | Task 3 | COVERED |

All other Phase-33 requirements and D-01 through D-03, D-06 through D-09 are satisfied by Plans 01-07 and are not reopened by this closure. Title editing, widget refresh, and adding an Allow-AI editor control are explicitly excluded by the verifier and do not enter this plan.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace saved-event per-contact adds with one atomic batch fan-out</name>
  <files>src/db/group-events-dao.ts, src/db/group-events-dao.test.ts, src/components/ContactPicker.tsx, src/components/contact-picker-multiselect.ts, src/components/contact-picker-multiselect.test.ts, src/screens/EditGroupEventScreen.tsx, src/screens/GroupEventDetailScreen.tsx</files>
  <read_first>
    - src/db/group-events-dao.ts: `addParticipant`, `loadGroupEvent`, `loadGroupChildren`, `createGroupEvent`, and `updateGroupEvent` — use the existing one-mutex core-composition pattern; never invoke an exported transactional DAO from inside another transaction.
    - src/db/recency-dao.ts: `insertInteractionCore` and `recomputeLastContactCore` — each child must remain an ordinary Interaction and retain the sole last_contact write path.
    - src/db/transaction.ts and src/db/data-revision-dao.ts — `inWriteTransaction` is non-reentrant; the batch owns one trailing revision bump.
    - src/db/group-events-dao.test.ts: lifecycle rollback and existing one-add tests — extend this test fixture instead of creating an un-migrated SQLite fixture.
    - src/components/ContactPicker.tsx: multi-select `confirmSelection` currently calls the owner synchronously and immediately dismisses; change only the multi-select confirmation outcome contract, retaining single-select and other generic callers.
    - src/components/contact-picker-multiselect.ts and src/components/contact-picker-multiselect.test.ts: extend the existing pure selection test seam with the awaited confirmation result used by ContactPicker, rather than introducing an untestable screen-only state machine.
    - src/screens/EditGroupEventScreen.tsx and src/screens/GroupEventDetailScreen.tsx: `addSelected` callbacks both close the picker before awaiting one write per selected id; each must instead propagate the awaited plural batch-and-single-reload result back to ContactPicker.
    - .planning/phases/33-group-interaction-logging/33-VERIFICATION.md: the confirmed atomicity gap and required rollback proof.
  </read_first>
  <behavior>
    - RED: a two-contact batch whose second child write is injected to fail rejects with no added children, no changed contact recency, and no data-revision bump.
    - RED: a successful batch reads the parent in its transaction and creates all child values from the parent’s current Channel/Tone/Duration while stamping each `occurred_at` with the parent event’s local occurrence timestamp.
    - RED: a stale/deleted parent or an existing/duplicated membership fails the complete batch; no earlier selected child remains committed.
    - RED: the multi-select confirmation helper reports a rejected owner promise without clearing its ordered selected ids; ContactPicker keeps the Modal open, displays its in-picker write error, and does not call `onDismiss`.
    - GREEN: a resolved owner promise makes ContactPicker clear the selection and dismiss exactly once; existing synchronous multi-select owners retain their current successful confirmation behavior.
    - GREEN: each saved-event screen sends one plural DAO call and awaits one reload before resolving the picker owner callback; a rejected batch propagates to the picker with no reload or success transition. The focused contract coverage exercises both saved-event source paths as consumers of that contract.
  </behavior>
  <action>
    Per D-05 and D-08, add one plural batch input/API (for example `addParticipants`) that accepts the parent id, an array of participant inputs with caller-minted UIDs, and the local `now` string. It must enter `inWriteTransaction` exactly once, first re-read the Group Event parent inside that transaction, and re-read/validate the relevant current membership state there before inserting any child. Reject duplicate input contacts and already-linked contacts through a transaction-local validation plus the existing database uniqueness constraint as the final integrity backstop.

    For every requested child, compose `insertInteractionCore` and `recomputeLastContactCore` inside the same outer transaction. Resolve the child’s `occurredAt`, Channel, Tone/quality, and duration from the transaction-local current parent; preserve participant-owned Direction, Connected, and note input; set exactly the existing three follow flags. Do not add a set-based interaction write, do not call `addParticipant` or any other mutex-owning DAO from inside the batch, and do not introduce a fourth inheritance flag. Bump data revision once after all children and recency recomputations have completed.

    Define the ContactPicker multi-select outcome contract as an owner callback that may return `void` or `Promise<void>`. Extend the existing pure multi-select helper and its focused test to make the contract testable: it supplies the ordered selected ids to the owner, treats a resolved callback as success, and treats a rejection as failure while preserving that same selected set. In `ContactPicker`, await that outcome, show a picker-local error on rejection, retain the visible Modal and selections, and do not invoke `onDismiss` or any success cleanup on rejection. While awaiting, prevent duplicate Done submissions. Only after a resolved outcome may ContactPicker clear the selected set and call its normal dismiss path once. Do not alter single-select behavior or the existing generic multi-select callers that return synchronously; their `void` callbacks are successful outcomes under this contract.

    Replace `Promise.all(addParticipant(...))` in `EditGroupEventScreen` and the sequential `addParticipant` loop in `GroupEventDetailScreen` with one batch invocation per multi-select confirmation. Neither saved-event owner may close the picker before the await or swallow a batch rejection: each must let ContactPicker receive the rejection after recording any owner-level error it needs, with no reload or success state on that path. On successful batch completion each owner must await its existing `load` exactly once before resolving; ContactPicker then performs the sole selection clear and dismissal. This keeps the Edit form draft and both picker selections intact when the batch fails. The existing single-add public API may be retained only if it delegates to the plural operation without creating another transaction; do not leave either screen on the one-item transaction path.

    Extend the migrated DAO tests with a failure injected during a later `INSERT INTO interactions` (or equivalent late child core write) and assert zero batch children, unchanged `contacts.last_contact` for every requested contact, and unchanged `data_revision`. Add the success assertion for resolved parent values and parent `occurred_at`, including a historical parent timestamp. Assert an existing membership/duplicate batch input cannot leave a prefix committed. Extend `contact-picker-multiselect.test.ts` with the pure resolved/rejected-outcome cases and focused owner-contract assertions for both saved-event screens: failure retains selection and picker visibility with a visible error and no reload, while success performs one batch, one reload, and one picker dismissal.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-dao.test.ts src/components/contact-picker-multiselect.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both saved-event picker owners invoke one batch API once per confirmation; no per-contact transaction loop remains in either callback.
    - ContactPicker awaits the saved-event confirmation contract: an error remains visible in its open Modal with the same selections on rejection and no owner reload/dismiss; one resolved batch-and-reload clears the selections and dismisses exactly once.
    - Existing synchronous generic multi-select and single-select ContactPicker callers keep their current successful confirmation behavior.
    - The batch has one outer write transaction, uses `insertInteractionCore` plus `recomputeLastContactCore` for each child, and makes one revision bump only after every child succeeds.
    - The injected late-child failure proves no newly selected child, recency change, or revision increment persists; a successful batch proves current parent values and the event occurrence time are stored on every new child.
  </acceptance_criteria>
  <done>Post-save participant additions satisfy the Group Event atomicity and canonical-recency contracts for both entry surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep format-4 export valid while retaining local Group Event deletion evidence</name>
  <files>src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/db/group-events-dao.test.ts</files>
  <read_first>
    - src/backup/export-manifest.ts: the one snapshot read of `tombstones`, construction of `manifest.tombstones`, and final `parseBackupManifest` validation.
    - src/backup/backup-schema.ts: format-4’s closed tombstone allowlist. Do not modify it in this plan.
    - src/db/group-events-dao.ts: `dissolveGroupEvent` and `deleteGroupEventAndInteractions` both write durable `group_event` tombstones in their lifecycle transaction.
    - src/db/tombstones-dao.ts: `group_event` is a supported local tombstone entity type and must remain durable in SQLite.
    - .planning/phases/33-group-interaction-logging/33-BACKUP-HANDOFF.md: Phase 36 owns `groupEvents`, the validator allowlist, reconciliation, restore mappings, reset ordering, and orphan handling.
    - src/backup/export-manifest.test.ts and src/db/group-events-dao.test.ts: reuse their migration-backed fixtures and lifecycle evidence.
  </read_first>
  <behavior>
    - RED: after both dissolve and full delete, `buildExportManifest()` currently rejects the format-4 manifest because its serialized tombstone has entityType `group_event`.
    - GREEN: each lifecycle action leaves the local `group_event` tombstone present, but the current manifest builds and parses successfully without serializing that unsupported tombstone.
    - GREEN: ordinary supported tombstones remain serialized, `BACKUP_FORMAT_VERSION` remains 4, and no `groupEvents` wire array or Group Event restore behavior is introduced.
  </behavior>
  <action>
    Add a narrowly scoped pre-Phase-36 compatibility guard at the export-manifest serialization boundary: select or filter `group_event` tombstones out of the current format-4 manifest before `parseBackupManifest` validates it. Name and comment the guard as temporary format compatibility work, not deletion-policy behavior: lifecycle DAOs must continue inserting the durable local tombstone, and when Phase 36 adds the coordinated Group Event wire/restore support it must remove or replace this guard so stored Group Event tombstones serialize with that format.

    Do not change `BACKUP_FORMAT_VERSION`, `src/backup/backup-schema.ts`, `src/backup/restore-apply.ts`, `src/backup/reconciliation.ts`, backup types, or the Phase-36 handoff. Do not suppress lifecycle tombstone insertion and do not add `groupEvents` to the current wire shape. This guard is limited to the legacy manifest snapshot and preserves the existing export contract until the owned Phase-36 coordination ships.

    Extend export-manifest integration coverage to create a migrated Group Event, exercise dissolve and full delete as separate cases, and assert `buildExportManifest()` resolves at format 4 while the emitted tombstones omit only `group_event`. Retain or extend the Group Event DAO lifecycle assertions proving that the durable local tombstone still exists after both operations.
  </action>
  <verify>
    <automated>npx vitest run src/backup/export-manifest.test.ts src/db/group-events-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A dissolve and a full delete each leave a `group_event` row in the local tombstones table and each subsequent format-4 manifest build succeeds.
    - The serialized manifest retains supported tombstones but has no format-4 `group_event` tombstone; its format version and current top-level wire shape are unchanged.
    - Phase-36 validation, restore, reconciliation, and orphan-disposition files are untouched, with the export guard documenting the future replacement point.
  </acceptance_criteria>
  <done>Lifecycle actions no longer make the current portable export unusable, without weakening local deletion evidence or prematurely implementing Phase 36.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Carry actual child permission and seconds-aware duration through Group Event Detail</name>
  <files>src/db/group-events-read.ts, src/db/group-events-read.test.ts, src/screens/GroupEventDetailScreen.tsx, src/screens/group-event-detail-logic.ts, src/screens/group-event-detail-logic.test.ts</files>
  <read_first>
    - src/db/group-events-read.ts: `GroupEventParticipant` and `resolveParticipants`; it already projects the participant’s canonical Interaction fields and must add the existing `interactions.allow_ai` value without joining any AI context.
    - src/screens/GroupEventDetailScreen.tsx: its header currently formats raw `duration` and its participant Detail object currently supplies a static Allow-AI value.
    - src/components/touchpoint-refine-logic.ts: `formatDurationLabel` is the shared conversion for stored whole seconds; preserve its established output vocabulary rather than introducing a screen-local unit formatter.
    - src/components/history/InteractionDetail.tsx and src/components/history/interaction-detail-logic.ts: `InteractionDetail` consumes `allowAi` and its sparkle predicate is strictly `allowAi === 1`.
    - src/components/group/ParticipantOverrideEditor.tsx and .planning/phases/33-group-interaction-logging/33-VERIFICATION.md: participant editor scope is settled and must remain Channel, Tone, Duration, Direction, Connected, and participant note.
  </read_first>
  <behavior>
    - RED: participant resolution returns the stored child Allow-AI flag for both 0 and 1 instead of manufacturing a default at the presentation layer.
    - RED: the screen-consumed view-model maps a 3600-second event to the exact label returned by `formatDurationLabel` (currently `1h`), rather than presenting the raw stored number as minutes.
    - GREEN: the InteractionDetail input built from an opted-in child keeps `allowAi: 1`, so its existing strict presentation logic can expose the real state.
  </behavior>
  <action>
    Extend `GroupEventParticipant` and the bound `resolveParticipants` SELECT with `member.allow_ai AS allowAi`; update the migrated read test to prove both the parent Detail read and direct participant resolution preserve the stored binary value.

    Create a small node-testable view-model helper used by `GroupEventDetailScreen` to (a) derive the header duration label through `formatDurationLabel` for a non-null stored duration and (b) build the existing `InteractionDetail` input from a Group Event participant. Keep the screen as the presentation owner, but route both its header `DetailField` value and sheet input through this helper so the tests exercise the exact projection it consumes. Test whole-second conversion with `3600` against the shared helper’s current label, and test an opted-in child passes `allowAi: 1` to the InteractionDetail-facing shape.

    Make no participant-editor, Group Note, AI-context-read, schema, or navigation changes. Per D-04 and D-11, this is a truthful local Detail read-through only: it neither creates an Allow-AI control nor makes Group Note AI-eligible.
  </action>
  <verify>
    <automated>npx vitest run src/db/group-events-read.test.ts src/screens/group-event-detail-logic.test.ts src/components/history/interaction-detail-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Group Event participant reads expose the stored `allow_ai` value as `allowAi`, and the participant Detail input preserves that value without a static replacement.
    - A one-hour duration stored as 3600 uses the established shared duration label and is not displayed as a raw seconds number with a minute suffix.
    - ParticipantOverrideEditor and AI egress stay outside this patch; Group Note remains parent-local and unreachable from AI.
  </acceptance_criteria>
  <done>Group Event Detail faithfully renders duration and the existing child Allow-AI state while preserving the locked privacy and editor-scope boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| multi-select UI → Group Event transaction | A mutable selected-contact set becomes multiple durable child interactions and derived recency values. |
| local tombstone store → portable format-4 manifest | Durable deletion evidence crosses into a schema that cannot yet represent Group Events. |
| child Interaction read → Group Event Detail | Stored participant privacy/display state is presented through a secondary detail surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-25 | Tampering / integrity | `addParticipants` transaction | high | mitigate | One outer `inWriteTransaction`, transaction-local parent/membership checks, canonical cores for every child, one trailing revision bump, and an injected late-write rollback test. |
| T-33-26 | Denial of Service / data integrity | `buildExportManifest` after lifecycle action | high | mitigate | Filter only unsupported `group_event` tombstones from the legacy format-4 serialized snapshot while retaining local evidence; lifecycle export regressions cover dissolve and delete. |
| T-33-27 | Information disclosure | Group Event Detail participant sheet | medium | mitigate | Project only child `allow_ai`; retain the closed AI context projection and make no Group Note or editor changes. |
| T-33-28 | Spoofing / integrity | duration and consent presentation | medium | mitigate | Use the shared seconds formatter and a tested view-model that preserves the stored binary Allow-AI flag into InteractionDetail. |
| T-33-SC | Tampering | package installation | high | mitigate | No package installation is permitted or required; this plan uses existing in-repo SQLite, Vitest, and React Native patterns. |
</threat_model>

<verification>
- `npx vitest run src/db/group-events-dao.test.ts src/backup/export-manifest.test.ts src/db/group-events-read.test.ts src/screens/group-event-detail-logic.test.ts src/components/history/interaction-detail-logic.test.ts`
- `npx tsc --noEmit`
- `npm run check:colors`
- After automated closure, retain the verifier's device UAT: create zero- and many-participant events; add multiple saved participants; enter participant Detail from Dashboard, Orrery, and Settings-hosted profile routes; exercise dissolve/delete confirmations; inspect long content and large participant lists.
</verification>

<success_criteria>
The four verification failures are closed: saved multi-add is one rollback-safe canonical transaction, lifecycle actions no longer break the current export, Group Event Detail uses seconds-aware display, and Detail represents the child’s actual Allow-AI state. Phase 36's group wire entity, validation allowlist, restore/reconciliation work, and locked orphan disposition remain deferred exactly as recorded.
</success_criteria>

<output>
Create `.planning/phases/33-group-interaction-logging/33-08-SUMMARY.md` when done.
</output>
