---
phase: 38-your-week
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/up-next-read.ts
  - src/logic/digest-composition.ts
  - src/logic/dashboard-query-logic.ts
  - src/db/digest-read.ts
autonomous: true
requirements: [S-06, S-07, S-08]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Up Next ranks contacts by the canonical query-time status/progress engine (PROGRESS_SQL/STATUS_SQL/REASON_SQL) — overdue/rogue naturally outrank approaching, with NO Digest-specific urgency classification (D-04, dossier §D)"
    - "Up Next is scoped to the canonical ATTENTION population — `progress >= STABLE_MAX` AND not actively snoozed — reusing the same predicate the Dashboard needs-attention filter uses (dashboard-query-logic.ts:174-175); a stable (progress < STABLE_MAX) contact is NOT surfaced, so when nobody needs attention Up Next is genuinely empty and its neutral empty state renders (addresses codex HIGH — empty state was unreachable)"
    - "Up Next shows at most three contacts and takes first claim on them (D-04, dossier §D)"
    - "Horizon → Overlooked excludes the contact ids Up Next already claimed for the same condition (the Up Next↔Horizon dedup) so no person appears twice for the same reason (D-04, dossier §E)"
    - "Horizon → Birthdays uses a forward 7-day window (0..6 days), soonest-first, independent of the Your Week period, via the single daysUntilBirthday parser (D-06, dossier §F)"
    - "Horizon → Never Contacted is conditional (present only when the count is non-zero), reusing countNeverContacted; large Overlooked/Never-Contacted populations use a compact preview + count (dossier §G)"
    - "Per owner ruling D-10, the `not-contacted` population read is unbound-inclusive: `buildPopulationWhere` emits, for a `['not-contacted']` selection ONLY, the SAME predicate `countNeverContacted` uses (`c.archived_at IS NULL AND c.last_contact IS NULL AND (c.tracking_enabled = 1 OR (c.tracking_enabled = 0 AND (SELECT include_unbound_never_contacted FROM app_settings WHERE id = 1) = 1))`), so the Digest Never Contacted count == preview == drill when include_unbound_never_contacted=1; NO other population is broadened (owner-approved §G/ADR-062 extension scoped to not-contacted only)"
    - "Horizon → Overlooked (`readOverlooked`) excludes actively-snoozed contacts, matching its `needs-attention` drill target's snooze exclusion (dashboard-query-logic.ts:174-175), so a snoozed rogue does not appear in the preview then vanish on drill; the drill target remains a canonical SUPERSET (wobble+decay+rogue) of the rogue-only Overlooked preview by design"
  artifacts:
    - path: src/db/up-next-read.ts
      provides: "Read-only attention-ordered read of tracked contacts (progress DESC) for Up Next candidate selection"
      contains: "up-next-read"
    - path: src/logic/digest-composition.ts
      provides: "Pure composition: Up Next cap-3 selection, Up Next↔Horizon dedup, 7-day birthday filter/tags, Never-Contacted conditional, overflow preview"
      contains: "digest-composition"
    - path: src/logic/dashboard-query-logic.ts
      provides: "buildPopulationWhere: not-contacted population made unbound-inclusive (D-10) — the countNeverContacted predicate for a ['not-contacted'] selection only; all other populations unchanged"
      contains: "not-contacted"
    - path: src/db/digest-read.ts
      provides: "readOverlooked gains an active-snooze exclusion so its preview matches the needs-attention drill target's snooze semantics"
      contains: "readOverlooked"
  key_links:
    - from: src/db/up-next-read.ts
      to: src/db/status.ts
      via: "PROGRESS_SQL/STATUS_SQL/REASON_SQL + STATUS_CADENCE_PRECONDITION, ORDER BY progress DESC"
      pattern: "PROGRESS_SQL|STATUS_SQL"
    - from: src/logic/digest-composition.ts
      to: src/logic/birthday-logic.ts
      via: "daysUntilBirthday filtered to 0..6 days, soonest-first"
      pattern: "daysUntilBirthday"
---

<objective>
Build the pure, tested composition layer for Digest's **Up Next** and **Horizon** modules, plus the one new read Up Next needs. Everything else Up Next and Horizon require already exists as correct, tested reads — this plan reuses them and adds only (a) an attention-ordered candidate read and (b) the pure composition/dedup logic that has no existing helper.

Plan against the dossier (D-01). Up Next ordering uses the canonical relationship/orbit status/progress semantics — do NOT invent Digest-specific urgency classifications (D-04, §D). Up Next takes first claim on its (max three) contacts; Horizon must dedup against them and not re-surface the same condition (D-04, §E). The inherited birthday obligation lives at Horizon → Birthdays with a forward 7-day window (D-06, §F). Reuse canonical reads; do not create parallel definitions (D-05, §O).

Purpose: give the DigestScreen assembly (Plan 06) ready, tested Up Next/Horizon contracts so the screen is pure presentation wiring.
Output: `up-next-read.ts` (DAO) + `digest-composition.ts` (pure logic) + two Horizon read-path consistency fixes (Task 3): the `not-contacted` population made unbound-inclusive per owner ruling **D-10** (so Never Contacted count == preview == drill) and a snooze guard on `readOverlooked` (so its preview matches the `needs-attention` drill) — all with tests.

**Owner ruling D-10 (cycle-3):** the Digest Never Contacted surface (Horizon preview + its Contacts drill-through) INCLUDES opted-in unbound never-contacted contacts when `include_unbound_never_contacted = 1`, matching `countNeverContacted`. Task 3 delivers the read-path mechanism (a `not-contacted`-only scope relaxation in `buildPopulationWhere`); Plan 06 asserts the end-to-end count==preview==drill equivalence. This is an owner-approved §G/ADR-062 extension SCOPED TO the not-contacted population ONLY — no other population is broadened.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; hook-blocked). Commit in place on the current branch.
- Local-first: the read is async-only (`getAllAsync`, never `...Sync`), no writer, no network. Static SQL; only closed status/reason constants are interpolated (no user free-text) — matching the digest-read/status injection posture.
- Dates via `formatLocalDate()`; `daysUntilBirthday(stored, today: Date)` takes a `Date` — inject `today` for deterministic tests.
- No inline write actions surface in Up Next/Horizon (D-07, §D); this plan writes nothing to any interaction/relationship table.
- Review the actual code on disk (files in `<read_first>`), not a diff.
</execution_context>

<context>
@.planning/phases/38-your-week/38-CONTEXT.md
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: up-next-read.ts — attention-ordered candidate read</name>
  <files>src/db/up-next-read.ts, src/db/up-next-read.test.ts</files>
  <read_first>
    - src/db/status.ts (PROGRESS_SQL :59; STATUS_SQL :72-78; REASON_SQL :100-104; STATUS_CADENCE_PRECONDITION :62-64 — the required WHERE for any status-evaluating population; thresholds STABLE_MAX=0.8 :40/WOBBLE_MAX/ROGUE_K)
    - src/logic/dashboard-query-logic.ts (:173-176 — the canonical `needs-attention` filter: `(PROGRESS_SQL) >= STABLE_MAX AND (c.snooze_until IS NULL OR date(c.snooze_until) <= date('now','localtime'))` — the EXACT attention + snooze-exclusion predicate Up Next must reuse, so Up Next means the same population the Dashboard calls "needs attention")
    - src/db/digest-read.ts (readOverlooked :109-124 — the ORDER BY progress DESC idiom and the read-only header contract; note last_contact IS NOT NULL is load-bearing for STATUS_SQL)
    - src/db/types.ts (SqlExecutor)
    - 38-PATTERNS.md "Shared Patterns → Query-time status"; RESEARCH Pattern 1 (derived-never-stored) + Code Example "Reselect"/status; dossier §D + §P (empty state "nobody currently needs attention")
  </read_first>
  <behavior>
    - `readUpNextCandidates(exec)` returns non-archived, tracked contacts satisfying STATUS_CADENCE_PRECONDITION **AND the canonical attention floor** `(PROGRESS_SQL) >= STABLE_MAX` **AND not actively snoozed** `(c.snooze_until IS NULL OR date(c.snooze_until) <= date('now','localtime'))`, each with id/name/photo + computed progress/status/reason, ORDER BY progress DESC (then name COLLATE NOCASE, id) — overdue/rogue rows sort above approaching(wobble) rows with no Digest-local scoring.
    - When every tracked contact is stable (progress < STABLE_MAX) or actively snoozed, the read returns ZERO rows — so Up Next's neutral empty state is reachable (the codex HIGH: a precondition-only query returned all stable contacts and made the empty state unreachable).
    - It returns enough rows for Up Next's cap of 3 AND the Horizon dedup (i.e. it is not pre-capped to 3 in SQL) so the composition layer can take the top 3 and dedup Overlooked against them.
    - A contact with NULL last_contact or no cadence is excluded (the precondition guards STATUS_SQL's no-NULL-branch); an actively-snoozed contact is excluded.
  </behavior>
  <action>
    Create `src/db/up-next-read.ts` as a read-only DAO copying the `digest-read.ts` header contract (static SQL, async-only, local-wall-clock discipline, no writer/no network). Implement `readUpNextCandidates(exec)` selecting `c.id, c.name, c.photo, (PROGRESS_SQL) AS progress, (STATUS_SQL) AS status, (REASON_SQL) AS reason` from `contacts c` WHERE `c.archived_at IS NULL AND <STATUS_CADENCE_PRECONDITION> AND (${PROGRESS_SQL}) >= ${STABLE_MAX} AND (c.snooze_until IS NULL OR date(c.snooze_until) <= date('now','localtime'))`, `ORDER BY progress DESC, c.name COLLATE NOCASE, c.id`. This reuses the canonical `needs-attention` predicate verbatim (dashboard-query-logic.ts:174-175) — do NOT invent a Digest-local attention rule (D-04). Interpolate ONLY the imported `status.ts` fragments/constants (`PROGRESS_SQL`, `STABLE_MAX`); the snooze bound uses `date('now','localtime')`, no user free-text. Export the row type. Do NOT cap in SQL — the cap-3 + dedup happen in the composition layer (Task 2). Write `src/db/up-next-read.test.ts` proving: ordering (a more-overdue contact precedes a less-overdue one); the cadence-precondition exclusion (NULL last_contact / no cadence excluded); archived exclusion; **all-stable → zero rows (empty state reachable)**; **an actively-snoozed above-threshold contact is excluded**; a just-past-STABLE_MAX (wobble) contact is included.
  </action>
  <verify>
    <automated>npx vitest run src/db/up-next-read.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/up-next-read.ts` exists and exports `readUpNextCandidates`
    - It reuses the canonical engine (no re-derived thresholds): `grep -c 'PROGRESS_SQL' src/db/up-next-read.ts` returns at least 1, `grep -c 'STATUS_CADENCE_PRECONDITION' src/db/up-next-read.ts` returns 1, and it applies the attention floor `grep -c 'STABLE_MAX' src/db/up-next-read.ts` returns at least 1
    - It excludes active snoozes: `grep -c 'snooze_until' src/db/up-next-read.ts` returns at least 1
    - No sync executor / no UTC slicing: `grep -c 'getAllSync\|getFirstSync' src/db/up-next-read.ts` returns 0 and `grep -c 'toISOString' src/db/up-next-read.ts` returns 0
    - The query is not SQL-capped to three rows: `! grep -Eq 'LIMIT 3' src/db/up-next-read.ts`
    - The test proves all-stable → zero rows (empty state reachable) and snoozed-excluded; up-next-read tests + `npx tsc --noEmit` green
  </acceptance_criteria>
  <done>Up Next candidates read read-only in canonical attention order, scoped to the needs-attention floor (progress ≥ STABLE_MAX) with active snoozes excluded and the cadence precondition + archived exclusion, uncapped for downstream dedup; the all-stable-empty and snoozed-excluded cases are tested; tests + tsc green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: digest-composition.ts — cap-3, dedup, 7-day birthdays, conditional Never-Contacted</name>
  <files>src/logic/digest-composition.ts, src/logic/digest-composition.test.ts</files>
  <read_first>
    - src/logic/birthday-logic.ts (daysUntilBirthday :165-204 — signature (stored: string|null, today: Date) => number|null; 0 = today, Feb-29 handling; never throws)
    - src/db/dashboard-read.ts (BirthdayCandidate :129; listBirthdayCandidates :620-628 → {id,name,birthday}; countNeverContacted :540-554 honours include_unbound_never_contacted)
    - src/db/digest-read.ts (OverlookedRow :60-70 from readOverlooked — the rogue population Horizon Overlooked dedups against Up Next)
    - src/logic/digest-logic.ts (existing cap/split helpers — the capGroup/overflow idiom to mirror for the compact-preview "+N more" count)
    - src/db/up-next-read.ts (from Task 1 — the candidate row type)
    - 38-PATTERNS.md "Shared Patterns → Birthday days-until"; RESEARCH Pitfall 4 (Up Next↔Horizon duplicate) + Pitfall 5 (30-day vs 7-day birthday); dossier §D/§E/§F/§G
  </read_first>
  <behavior>
    - `pickUpNext(candidates)` returns the first ≤3 candidates (already attention-ordered) — the Up Next set.
    - `dedupOverlooked(overlooked, upNextIds)` returns the Overlooked (rogue) rows with any contact id already in the Up Next set removed (Up Next first claim; no same-condition duplicate).
    - `filterUpcomingBirthdays(candidates, today: Date)` maps via daysUntilBirthday, keeps 0..6 days, sorts soonest-first with a DETERMINISTIC tie-break for equal-day birthdays (days asc, then name case-insensitive, then id) so two people sharing a birthday always render in a stable order, and tags each: 0→"Today", 1→"Tomorrow", else a formatted local date (via formatLocalDate) — NOT the 30-day dashboard window.
    - `neverContactedConditional(count)` yields a present/absent flag so Horizon renders Never Contacted only when count > 0.
    - `previewWithOverflow(rows, cap)` returns {shown, overflow} for the compact preview + "+N more" count (mirroring the existing capGroup idiom).
  </behavior>
  <action>
    Create `src/logic/digest-composition.ts` as a pure module (no DB/store/component import; inputs are the already-fetched rows/counts). Implement `pickUpNext`, `dedupOverlooked`, `filterUpcomingBirthdays`, `neverContactedConditional`, and `previewWithOverflow` per the behaviors above. Reuse `daysUntilBirthday` for the 7-day birthday filter (0..6, soonest-first) — do NOT reuse `resolveBirthdayWindow`/`countBirthdayPopulation` (those are the 30-day dashboard population, a different surface). Reuse the existing `digest-logic` cap/overflow idiom for `previewWithOverflow`. Keep `today` injected for deterministic tests. Write `src/logic/digest-composition.test.ts` proving: cap at 3; a rogue contact chosen by Up Next does NOT reappear in the deduped Overlooked; the birthday filter keeps day 0 and day 6 but drops day 7 and negative, sorts soonest-first, breaks equal-day ties deterministically (two same-day birthdays order by name then id), and tags Today/Tomorrow correctly; Never Contacted is absent at count 0; overflow count is correct.
  </action>
  <verify>
    <automated>npx vitest run src/logic/digest-composition.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/logic/digest-composition.ts` exists and exports `pickUpNext`, `dedupOverlooked`, `filterUpcomingBirthdays`, `neverContactedConditional`, `previewWithOverflow`
    - It reuses the single birthday parser and NOT the 30-day window: `grep -c 'daysUntilBirthday' src/logic/digest-composition.ts` returns at least 1 and `grep -c 'resolveBirthdayWindow\|countBirthdayPopulation' src/logic/digest-composition.ts` returns 0
    - Pure module (no DB import): `grep -c "from \"@/db/database\"" src/logic/digest-composition.ts` returns 0
    - The test asserts a day-6 birthday is kept and a day-7 birthday is dropped, and asserts the Up Next↔Horizon dedup (a picked id is absent from deduped Overlooked); the suite passes
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Up Next cap-3, Up Next↔Horizon dedup, 7-day soonest-first birthdays with Today/Tomorrow tags, conditional Never-Contacted, and overflow preview are pure and tested; tests + tsc green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Horizon preview↔drill read-path consistency — Never Contacted unbound-inclusive (D-10) + Overlooked snooze guard</name>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts</files>
  <read_first>
    - src/logic/dashboard-query-logic.ts (buildPopulationWhere :248-283; DASHBOARD_POPULATION_SCOPE_WHERE :219-220 = `c.archived_at IS NULL AND c.tracking_enabled = 1`, the bound-only scope ANDed over the OR of ALL selected population predicates :280; NOT_CONTACTED_WHERE :222; the `not-contacted` case :268-269; ACTIVE_SEGREGATION_WHERE :214-216; the canonical needs-attention snooze clause :174-175 to mirror in readOverlooked)
    - src/db/dashboard-read.ts (countNeverContacted :540-553 — the EXACT unbound-inclusive predicate to match: `archived_at IS NULL AND last_contact IS NULL AND (tracking_enabled = 1 OR (tracking_enabled = 0 AND (SELECT include_unbound_never_contacted FROM app_settings WHERE id = 1) = 1))`; listDashboardPopulation :380-411 → composePopulationRead :310-334 → buildPopulationWhere, i.e. the SAME read the Digest preview AND the Contacts drill both issue; readIncludeUnboundNeverContacted :505-516)
    - src/db/digest-read.ts (readOverlooked :109-124 — rogue-only, `tracking_enabled = 1`, `last_contact IS NOT NULL`, NO snooze predicate; its comment :102-108 says the mute filter is DELIBERATELY omitted — that omission is the preview↔drill divergence)
    - CONTEXT D-10 (owner ruling, cycle-3); dossier §G / ADR-062 (population semantics)
  </read_first>
  <behavior>
    - With `include_unbound_never_contacted = 1` and an unbound (`tracking_enabled = 0`) never-contacted contact present, `listDashboardPopulation({ populations: ['not-contacted'], filters: {}, sort: 'default', viewMode: 'list' }, now)` returns the SAME rows (and length) as `countNeverContacted` — the unbound contact IS in the result (D-10).
    - With `include_unbound_never_contacted = 0` (default), that same unbound contact is EXCLUDED from the `not-contacted` population (behavior unchanged from today).
    - Neither `['all-contacts']` nor `['favourites']` includes the unbound never-contacted contact regardless of the flag — the relaxation is scoped to `not-contacted` ONLY (no other population broadened, per D-10).
    - `readOverlooked` EXCLUDES an actively-snoozed rogue contact (snoozed to a future date) and still RETURNS a non-snoozed rogue — matching the `needs-attention` drill target's snooze exclusion.
  </behavior>
  <action>
    Two scoped read-path consistency fixes the cross-AI review flagged on Horizon (Plan 06 asserts the end-to-end preview↔count↔drill equivalence against these reads):
    (1) **D-10 — Never Contacted unbound-inclusive (owner ruling; cite D-10).** In `buildPopulationWhere` (dashboard-query-logic.ts:248), when the deduped selected populations are EXACTLY `["not-contacted"]`, return the SAME predicate `countNeverContacted` uses (dashboard-read.ts:543-551): `c.archived_at IS NULL AND c.last_contact IS NULL AND (c.tracking_enabled = 1 OR (c.tracking_enabled = 0 AND (SELECT include_unbound_never_contacted FROM app_settings WHERE id = 1) = 1))`. This relaxes the bound-only `DASHBOARD_POPULATION_SCOPE_WHERE` (`tracking_enabled = 1`) so opted-in unbound never-contacted contacts are included, matching the count. Use the correlated subselect (matches countNeverContacted exactly; NO async plumbing — when the flag is 0 the subselect is false and unbound contacts are excluded, so the default is unchanged). This is an owner-approved §G/ADR-062 extension SCOPED TO the not-contacted population ONLY (D-10): do NOT change `all-contacts`, `favourites`, `snoozed`, `birthdays`, or the general multi-population OR branch — a mixed selection that merely CONTAINS `not-contacted` keeps the existing bound-only scope. Because the Digest Never Contacted preview read and the Contacts drill target BOTH issue `listDashboardPopulation` with `populations:['not-contacted']`, this one change makes count == preview == drill. Grep every existing `not-contacted`-population test and update any that assumed bound-only, adding an opted-in-unbound fixture (Review the code, not the diff — read all consumers).
    (2) **Overlooked snooze guard.** In `readOverlooked` (digest-read.ts:109-124) add `AND (c.snooze_until IS NULL OR date(c.snooze_until) <= date('now','localtime'))` to the WHERE (the canonical needs-attention snooze clause, dashboard-query-logic.ts:174-175) so an actively-snoozed rogue is excluded from the Overlooked preview — matching its `needs-attention` drill target. Leave the rogue-only STATUS range as-is: the drill target is intentionally a wobble+decay+rogue SUPERSET of the rogue-only Overlooked preview; that non-equivalence is documented and its "+N more →" copy handled in Plan 06 (a "see everyone needing attention" affordance, NOT an exact preview+N promise). Update the stale "mute filter DELIBERATELY omitted" comment to reflect the added snooze exclusion.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-query-logic.test.ts src/db/dashboard-read.test.ts src/db/digest-read.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `buildPopulationWhere` emits the unbound-inclusive predicate for `['not-contacted']` only: `grep -c 'include_unbound_never_contacted' src/logic/dashboard-query-logic.ts` returns at least 1
    - A test asserts, with `include_unbound_never_contacted = 1` + an unbound never-contacted fixture, `countNeverContacted(exec)` == `listDashboardPopulation({populations:['not-contacted'],filters:{},sort:'default',viewMode:'list'}, now).length` and the unbound contact is present (count==preview universe, D-10); with the flag 0 the unbound contact is EXCLUDED; and `['all-contacts']`/`['favourites']` never include it regardless of flag (scoped-to-not-contacted proof)
    - A test proves `readOverlooked` EXCLUDES an actively-snoozed rogue while RETURNING a non-snoozed rogue (preview↔`needs-attention`-drill snooze parity)
    - `npx tsc --noEmit` clean; the three suites pass
  </acceptance_criteria>
  <done>The `not-contacted` population read is unbound-inclusive for a `['not-contacted']` selection only (D-10, matching countNeverContacted; no other population broadened) and `readOverlooked` excludes active snoozes (matching its drill target); tests prove count==preview universe under the opted-in flag and the snooze parity; tsc green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 03)

New symbols/files:
- `src/db/up-next-read.ts` → `readUpNextCandidates` (+ row type)
- `src/logic/digest-composition.ts` → `pickUpNext`, `dedupOverlooked`, `filterUpcomingBirthdays`, `neverContactedConditional`, `previewWithOverflow`
- Modified: `src/logic/dashboard-query-logic.ts` (`buildPopulationWhere` — `not-contacted` unbound-inclusive for a `['not-contacted']` selection only, D-10), `src/db/digest-read.ts` (`readOverlooked` — active-snooze exclusion added)
- Test files: `src/db/up-next-read.test.ts`, `src/logic/digest-composition.test.ts`, extended `src/logic/dashboard-query-logic.test.ts` / `src/db/dashboard-read.test.ts` / `src/db/digest-read.test.ts`

## Review feedback addressed (Plan 03)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| Up Next selects all tracked contacts (incl. stable) → empty state unreachable | HIGH (codex #4) | Incorporated — Task 1 adds the canonical attention floor `progress >= STABLE_MAX`; all-stable → empty is tested |
| "Approaching" / snoozed treatment undefined vs canonical attention filter | MEDIUM (codex) | Incorporated — Task 1 reuses the exact `needs-attention` predicate incl. the active-snooze exclusion; snoozed-excluded is tested |
| Birthday equal-day tie ordering under-specified | LOW (codex) | Incorporated — Task 2 sorts days asc then name then id, with a same-day tie test |
| **[cycle 2] Up Next "why-present" line is NULL for wobble/decay (REASON_SQL non-null only for rogue)** | LOW (codex/claude) | `up-next-read` already returns both `reason` (nullable) and `status`; the copy derivation + NULL→status fallback is owned by Plan 06 Task 1 (presentation) — kept out of this pure selection layer so copy cannot alter population membership (codex's own guidance). No change needed here; single-owner is Plan 06 |
| **[cycle 3] HIGH-2 — Never Contacted count (countNeverContacted, unbound-inclusive) ≠ preview/drill (not-contacted population, bound to tracking_enabled=1). OWNER RULED D-10: include opted-in unbound** | HIGH (codex, disk-confirmed) | Incorporated — new Task 3 makes the `not-contacted` population read unbound-inclusive (the exact countNeverContacted predicate, for a `['not-contacted']` selection ONLY) so count == preview == drill; scoped to not-contacted only (owner-approved §G/ADR-062 extension per D-10, no other population broadened). Plan 06 asserts the end-to-end equivalence with an `include_unbound_never_contacted=1` fixture |
| **[cycle 3] Horizon Overlooked preview (readOverlooked, no snooze) diverges from its needs-attention drill target (snooze-excluded)** | MEDIUM (claude) | Incorporated — Task 3 adds the active-snooze exclusion to `readOverlooked` (mirroring the needs-attention snooze clause); the residual rogue-only vs wobble+decay+rogue status-range superset is documented and its "+N more →" copy handled in Plan 06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status fragments → up-next SQL | Only closed `status.ts` constants are interpolated; no user free-text reaches the query |
| composed rows → Digest presentation | On-device contact data only; dedup must not drop or leak a contact incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-SQLI | Tampering | up-next-read query | low | mitigate | Static SQL string; only PROGRESS_SQL/STATUS_SQL/REASON_SQL constants interpolated (digest-read posture); no user input |
| T-38-DEDUP | Information Disclosure | Up Next↔Horizon dedup correctness | low | mitigate | Dedup is pure and unit-tested (a picked contact cannot re-surface for the same condition); archived rows already excluded by the reused reads |
| T-38-EGRESS | Information Disclosure | Up Next read path | high | mitigate | Async on-device SQLite only; no network on the read path |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx vitest run src/db/up-next-read.test.ts src/logic/digest-composition.test.ts src/logic/dashboard-query-logic.test.ts src/db/dashboard-read.test.ts src/db/digest-read.test.ts` green.
- `npx tsc --noEmit` clean.
- Grep gate: `grep -c 'include_unbound_never_contacted' src/logic/dashboard-query-logic.ts` ≥ 1 (D-10 not-contacted relaxation present).
</verification>

<success_criteria>
Up Next candidate read (canonical order, cadence precondition, archived-excluded, uncapped) + pure composition (cap-3, Up Next↔Horizon dedup, 7-day soonest-first birthdays with Today/Tomorrow, conditional Never-Contacted, overflow preview) delivered with tests; all automated gates green.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-03-SUMMARY.md` when done.
</output>
