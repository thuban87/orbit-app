---
phase: 38-your-week
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/RootNavigator.tsx
  - src/navigation/tabs/DigestStack.tsx
  - src/navigation/tabs/EventsStack.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/types.ts
  - src/navigation/shell-contract.ts
  - src/navigation/shell-contract.test.ts
  - src/navigation/focused-route-classification.ts
  - src/navigation/linking.ts
  - src/screens/backup-dualhome-logic.ts
  - src/screens/backup-dualhome-logic.test.ts
  - src/components/icons/icon-registry.ts
  - src/constants/product-labels.ts
  - src/screens/GroupEventsScreen.tsx
autonomous: true
requirements: [S-01, S-02, S-04, S-05, S-14]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "On a fresh app launch the shell lands on the Digest tab (centered, default root) — per D-01/dossier §A"
    - "The bottom navigation shows five equal tabs in order Contacts · Events · Digest · Orrery · Settings; there is no Backup tab (dossier §K — Backup dual-homed in Settings since Phase 37, redundant tab removed)"
    - "The internal tab route id DashboardTab is unchanged (only its visible title becomes Contacts) so the shell-global FAB's hardcoded target still resolves"
    - "Each of the five tabs owns an independent native stack; reselecting the active tab pops that tab's stack to root"
    - "Origin-aware Profile Back works from the Digest and Events roots: Digest→Profile→Back→Digest and Events→GroupEventDetail→Profile→Back→Events"
    - "Every Profile-reachable route (including RecentlyDeleted) is registered in DigestStack and EventsStack, so a Profile opened from Digest/Events can navigate to it without a runtime unregistered-route crash (addresses codex HIGH — RecentlyDeleted omission)"
    - "The five-tab shape (tab ids + visible order + initial tab) and each new stack's registered route set live in a RUNTIME descriptor `src/navigation/shell-contract.ts` that RootNavigator and DigestStack/EventsStack render FROM (single source of truth), so the contract is unit-testable render-free in Node — the repo has no react-test-renderer/@testing-library and vitest.config.ts mandates render-free tests; the descriptor test replaces the previously-specified NavigationContainer mount/traversal (addresses cycle-3 HIGH-1)"
    - "The Events root (GroupEventsScreen) title and its user-facing search/a11y copy read Events, not Group Events (dossier §B; addresses S-05 / codex MEDIUM Events relabel)"
    - "The shared-backup deep link in linking.ts targets a live route after BackupTab removal (SettingsTab → Backup), so the .orbitbackup share-intent data-recovery path still resolves (addresses claude MEDIUM linking.ts:67)"
    - "After the Backup tab is removed, the surviving Settings-hosted Backup CONSUMES the shared-backup singleton (shouldConsumeSharedBackup true for the surviving host / share-intent origin), so the .orbitbackup share-intent path actually RESTORES, not merely resolves the route (addresses cycle-4 HIGH; §S.4 backup-before-removal)"
  artifacts:
    - path: src/navigation/tabs/DigestStack.tsx
      provides: "DigestTab native stack — root DigestScreen plus the shared child routes it can open (Profile and origin-aware siblings)"
      contains: "DigestStack"
    - path: src/navigation/tabs/EventsStack.tsx
      provides: "EventsTab native stack — root GroupEventsScreen plus GroupEventDetail/EditGroupEvent/EditParticipant/Profile and shared children"
      contains: "EventsStack"
    - path: src/constants/product-labels.ts
      provides: "Canonical user-facing product labels (DIGEST/CONTACTS/EVENTS) per dossier §B so a later rename is one edit"
      contains: "DIGEST"
    - path: src/navigation/types.ts
      provides: "TabParamList with DigestTab+EventsTab, BackupTab removed; DigestStackParamList + EventsStackParamList"
      contains: "DigestStackParamList"
    - path: src/navigation/shell-contract.ts
      provides: "Runtime shell/route descriptor (TAB_ORDER, INITIAL_TAB, DIGEST_STACK_ROUTES, EVENTS_STACK_ROUTES) — the render-free single source of truth RootNavigator + the new stacks render from and the shell-contract test asserts (no NavigationContainer mount)"
      contains: "TAB_ORDER"
  key_links:
    - from: src/navigation/RootNavigator.tsx
      to: src/navigation/tabs/DigestStack.tsx
      via: "Tab.Screen name=DigestTab component={DigestStack}, initialRouteName=DigestTab"
      pattern: "DigestTab"
    - from: src/components/icons/icon-registry.ts
      to: src/navigation/types.ts
      via: "TAB_ICON is Record<keyof TabParamList, IconName>; keys must match the new TabParamList exactly"
      pattern: "DigestTab|EventsTab"
---

<objective>
Restructure the app shell from the current four-tab layout (DashboardTab · OrreryTab · BackupTab · SettingsTab, with Digest and GroupEvents buried as pushed screens inside DashboardStack) into the permanent five-tab home: **Contacts · Events · Digest · Orrery · Settings**, with Digest centered and the default/root launch destination (dossier §A). This is the architecture-defining plan for Phase 38 — it leads with a tracer slice that boots the app end-to-end onto a real Digest tab before any expansion.

Plan against `docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md` (D-01), which is authoritative over every other artifact. Keep the phase narrow — do NOT redesign Contacts, Events, or Settings while restructuring navigation (D-03, dossier §K/§R).

Purpose: establish Orbit's durable home/navigation model. Every later Phase-38 plan builds on the tab shell this plan proves.
Output: five-tab RootNavigator (initialRoute Digest); new DigestStack + EventsStack; DashboardStack cleaned of the promoted screens; TabParamList/stack param lists; TAB_ICON; a canonical product-label constant.

<assumption_delta_decision>
Primary noun: the app's home surface. Decision: **promote** — Digest is promoted from a pushed sub-screen to the home tab root, and the former Dashboard root is demoted to the Contacts tab. Rationale: dossier §A/§B mandate the Digest-centered home and the Dashboard→Contacts relabel; this is a recorded product decision, not a planner expansion.
</assumption_delta_decision>

Note (spec-less probe fallback, visible skip): no concrete requirement IDs existed to probe this run, so no probe-derived edge predicates were generated. Edge/state coverage is supplied by the UI-SPEC `## UI Considerations` section (lifted into the Digest-surface plans 05/06) and the dossier §S verification checklist (mapped to requirement IDs S-01…S-15 across all plans).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees. `git worktree` is hook-blocked repo-wide (`workflow.use_worktrees: false`). Commit in place, on the current branch.
- All colours resolve through `useTheme().colors.*` / theme tokens — no hex or named colour literals anywhere (`npm run check:colors`). Icons resolve only through the semantic `Icon` + `TAB_ICON`/`ICON_MAP` registry.
- Local-first: no network on any read path; no new telemetry.
- Review the actual code on disk (the files in `<read_first>`), not a diff or a summary.
</execution_context>

<context>
@.planning/phases/38-your-week/38-CONTEXT.md
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end five-tab shell — boot to a real Digest tab</name>
  <files>src/constants/product-labels.ts, src/navigation/tabs/DigestStack.tsx, src/navigation/tabs/EventsStack.tsx, src/navigation/types.ts, src/components/icons/icon-registry.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/RootNavigator.tsx (current 4-tab shell; initialRouteName="DashboardTab" at :191; Tab.Screens :215-250; tabOptions :168-182; handleActiveTabPress :91-118)
    - src/navigation/tabs/DashboardStack.tsx (the plain native-stack shape to copy; note Digest at :69 and GroupEvents at :39 are pushed screens today)
    - src/navigation/tabs/OrreryStack.tsx — DO NOT copy its blur/useOrrerySessionStore listener; it is orrery-specific, the plain DashboardStack is the correct analog
    - src/navigation/types.ts (TabParamList :349-354; DashboardStackParamList :44-163; ProfileRouteParams :14)
    - src/components/icons/icon-registry.ts (TAB_ICON :107-111 is Record<keyof TabParamList, IconName>; ICON_REGISTRY has "your-week":calendar :50 and "group-events":people :51)
    - src/navigation/linking.ts (:67 navigate("BackupTab",{screen:"Backup"}) share-intent path; :71 navigate("DashboardTab",{screen:"Capture"}) — the DashboardTab one stays)
    - src/navigation/types.ts SettingsStackParamList Backup entry (:264) + src/navigation/tabs/SettingsStack.tsx (:206 name="Backup", host="settings") — the repoint target
    - src/screens/backup-dualhome-logic.ts (shouldConsumeSharedBackup :58-60 returns true ONLY for "backup-tab"; the :18 comment warning that "settings"/non-consuming risks a shared backup NEVER consumed; DEFAULT_BACKUP_HOST :22; the origin-aware return target :35) — the consumer contract to fix after tab removal
    - src/screens/BackupScreen.tsx (:272-286 the focus-effect consume guard `if (!shouldConsumeSharedBackup(host)) return;` before `consumeSharedBackup()`) + the orbit-backup-document-picker module (confirm consumeSharedBackup() is a no-op when nothing is staged)
    - src/screens/DigestScreen.tsx (the DigestTab root component) and src/screens/GroupEventsScreen.tsx (the EventsTab root component)
    - 38-PATTERNS.md "RootNavigator.tsx" + "DigestStack.tsx + EventsStack.tsx" sections; RESEARCH Pitfall 1 (never rename DashboardTab)
  </read_first>
  <action>
    Create the thinnest end-to-end path that boots the app onto a real Digest tab, wired through every navigation layer:
    (1) `src/constants/product-labels.ts` (new): export the canonical user-facing labels `DIGEST = "Digest"`, `CONTACTS = "Contacts"`, `EVENTS = "Events"` per dossier §B (a lightweight constant source so a later rename is one edit — NOT runtime config or a localization framework, per §B DERIVED).
    (1a) `src/navigation/shell-contract.ts` (new): the RUNTIME shell/route descriptor and single source of truth for the tab shape and each new stack's registered routes — export `TAB_ORDER` = `["DashboardTab","EventsTab","DigestTab","OrreryTab","SettingsTab"]` (the visible left-to-right order), `INITIAL_TAB = "DigestTab"`, and the route-name string arrays `DIGEST_STACK_ROUTES` / `EVENTS_STACK_ROUTES`. Keep this module pure — plain string/const arrays, NO React/react-navigation import — so it is node-testable render-free (the repo has no react-test-renderer/@testing-library and `vitest.config.ts` mandates render-free tests; addresses cycle-3 HIGH-1: the shell contract must be asserted from a runtime value, never by mounting a NavigationContainer). For the tracer, seed the arrays minimally (`DIGEST_STACK_ROUTES = ["Digest","Profile"]`; `EVENTS_STACK_ROUTES = ["GroupEvents","GroupEventDetail","Profile"]`); Task 2 expands them to the full Profile-reachable set.
    (2) `src/navigation/tabs/DigestStack.tsx` (new): a plain `createNativeStackNavigator<DigestStackParamList>()` copying DashboardStack's shape (`initialRouteName="Digest"`, `screenOptions={{ headerShown: false }}`). Render its `Stack.Screen`s by mapping `DIGEST_STACK_ROUTES` (from `shell-contract.ts`) through a LOCAL route-name→component registry defined in this file (root `Digest`→DigestScreen, `Profile`→ContactProfileScreen, etc.) so the REGISTERED route set is exactly the descriptor array (one source of truth: the test asserts the array, the stack renders it). Include a co-located completeness assertion so every descriptor route name resolves to a component (a missing entry is a build/test failure, not a runtime unregistered-route crash). Do NOT copy OrreryStack's session-blur listener.
    (3) `src/navigation/tabs/EventsStack.tsx` (new): same plain shape, `initialRouteName="GroupEvents"`, rendering its `Stack.Screen`s by mapping `EVENTS_STACK_ROUTES` through this file's local route-name→component registry (`GroupEvents`→GroupEventsScreen, `GroupEventDetail`, `Profile`), with the same completeness assertion.
    (4) `src/navigation/types.ts`: add `DigestTab` and `EventsTab` to `TabParamList` and remove `BackupTab`; add `DigestStackParamList` and `EventsStackParamList` type aliases (declared as `type`, not `interface`, so they satisfy the ParamListBase index-signature constraint), each registering at minimum `Digest`/`GroupEvents` roots + `Profile: ProfileRouteParams` + `GroupEventDetail` (Events). Keep `BackupStackParamList` intact (Backup stays dual-homed in Settings — dossier §K) and leave it in the `RootStackParamList` intersection. **Refresh the now-stale doc comment above the Settings `Backup` entry (types.ts:255-262), which still reads "reachable from both the Backup bottom tab (`BackupStack`) … The Backup tab stays (removal deferred, §R)" — that is false once `BackupTab` is removed in this task; update it to state Backup is Settings-only now (a bounded doc-sync adjacent to this edit, §Q — not a repo-wide sweep; addresses codex LOW).**
    (5) `src/components/icons/icon-registry.ts`: update `TAB_ICON` to the new key set — `DashboardTab: "dashboard"`, `EventsTab: "group-events"`, `DigestTab: "your-week"`, `OrreryTab: "orrery"`, `SettingsTab: "settings"` — removing the `BackupTab` entry (the `Record<keyof TabParamList, IconName>` type forces exact-key parity).
    (6) `src/navigation/RootNavigator.tsx`: set `initialRouteName={INITIAL_TAB}` (imported from `shell-contract.ts`, load-bearing for the default destination) and author the five `Tab.Screen`s INLINE in `TAB_ORDER` order — Contacts(DashboardTab) · Events(EventsTab) · Digest(DigestTab) · Orrery(OrreryTab) · Settings(SettingsTab). Keep the tabs inline (do NOT map TAB_ORDER here) so each tab's own `listeners`→`handleActiveTabPress` wiring stays explicit and Task 3's reselect-to-root wiring is unchanged; `TAB_ORDER` is the render-free-asserted contract (Task 2 test) and the rendered order is confirmed on-device (Plan 07). Relabel the DashboardTab title to the `CONTACTS` label via `tabOptions` while KEEPING the internal route name `DashboardTab` (RESEARCH Pitfall 1 — the FAB hardcodes `navigate("DashboardTab", …)`); title the new tabs from `EVENTS`/`DIGEST` labels; wire each new Tab.Screen's `listeners={{ tabPress }}` to the existing `handleActiveTabPress`; remove the `BackupTab` Tab.Screen and its `BackupStack` import.
    (7) `src/navigation/linking.ts`: the share-intent gate at `:67` runs `navigationRef.current?.navigate("BackupTab", { screen: "Backup" })` — the path that routes a shared `.orbitbackup` file into Restore (a data-recovery path). Removing `BackupTab` from `TabParamList` in this same task makes that call a tsc error and a dead route, so repoint it in the SAME commit to `navigationRef.current?.navigate("SettingsTab", { screen: "Backup" })` (Backup is registered under `SettingsStackParamList` at `types.ts:264` / `SettingsStack.tsx:206`, dual-homed since Phase 37 — dossier §K/§S.4). Leave the sibling `navigate("DashboardTab", { screen: "Capture" })` at `:71` unchanged (DashboardTab id is preserved). Addresses claude MEDIUM (linking.ts:67 Backup deep link, dossier §S.4).
    (7a) **Make the surviving Settings-hosted Backup actually CONSUME the shared backup (cycle-4 HIGH — the §S.4 restore path is otherwise silently broken by the tab removal).** Repointing the deep link (step 7) proves the route RESOLVES, not that the backup is RESTORED. On disk, `shouldConsumeSharedBackup(host)` returns true ONLY for `host === "backup-tab"` (`src/screens/backup-dualhome-logic.ts:58-60`); the Settings-hosted BackupScreen is registered with `host="settings"` (`SettingsStack.tsx`), so `BackupScreen.tsx:274` early-returns before `consumeSharedBackup()`. With the Backup TAB removed, the `"backup-tab"` consumer no longer exists, so a shared `.orbitbackup` file would land on a screen that NEVER drains the singleton — restore-via-share-intent breaks. FIX: after the Backup-tab removal, the Settings-hosted Backup is the SOLE Backup home and MUST drain the shared-backup singleton. Update `shouldConsumeSharedBackup` in `src/screens/backup-dualhome-logic.ts` so the surviving host consumes (return true for `"settings"`), and refresh the now-inverted comment at `:18` (it warns that a `"settings"`/non-consuming default "risks a shared backup NEVER" being consumed — that risk is now the bug). Keep the origin-aware post-restore RETURN target unchanged (Settings-entry still returns to Settings). **Confirm on disk that `consumeSharedBackup()` is a no-op when no share-intent file is staged** (`read_first` the picker module) so ordinary Settings→Backup navigation is unaffected; if it is NOT a safe no-op, instead gate consumption behind a share-intent origin marker (a `"share-intent"` `BackupHost` or a nav param that flips only the share-intent entry to consuming) rather than consuming on every Settings→Backup focus. Update `src/screens/backup-dualhome-logic.test.ts` accordingly. This restores intended §S.4 behavior after the tab removal — it does NOT change backup policy and is NOT a decision reversal (Phase 37's dual-home design assumed the tab was the consumer; removing the tab reassigns that role).
    Leave the DashboardStack still containing Digest+GroupEvents for THIS task (removed in Task 2) so the tracer compiles and boots; the tracer proves the tab architecture end-to-end before the promotion cleanup.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/navigation 2>&1 | tail -15 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>Build+install the debug APK on the Pixel (desktop-build-pipeline); cold-launch Orbit: it lands on the Digest tab (centered), five tabs read Contacts·Events·Digest·Orrery·Settings, no Backup tab. Tapping Contacts shows the former Home; tapping Events shows Group Events.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/navigation/shell-contract.ts` exists and exports `TAB_ORDER`, `INITIAL_TAB`, `DIGEST_STACK_ROUTES`, `EVENTS_STACK_ROUTES`, with `INITIAL_TAB` = `"DigestTab"`: `grep -Eq 'INITIAL_TAB[^=]*=[^=]*"DigestTab"' src/navigation/shell-contract.ts` succeeds and `grep -c '"SettingsTab"' src/navigation/shell-contract.ts` returns at least 1 (the exact five-id `TAB_ORDER` with no Backup tab is asserted positively by the render-free test's `TAB_ORDER` deep-equality in Task 2 — a positive exact-match, not a bare negative grep)
    - RootNavigator sets the initial tab from the descriptor: `grep -c 'INITIAL_TAB' src/navigation/RootNavigator.tsx` returns at least 1 (its five Tab.Screens stay authored inline in `TAB_ORDER` order — the tab set/order is the render-free-asserted contract in `shell-contract.ts` + device-UAT-confirmed; only the many-route STACKS are array-driven, so Task 3's per-tab listener wiring is unchanged)
    - `grep -c 'name="DashboardTab"' src/navigation/RootNavigator.tsx` returns 1 (internal id kept; NOT renamed)
    - RootNavigator has no `BackupTab` Tab.Screen: `! grep -q 'name="BackupTab"' src/navigation/RootNavigator.tsx`
    - `src/navigation/tabs/DigestStack.tsx` and `src/navigation/tabs/EventsStack.tsx` both exist and export `DigestStack`/`EventsStack`
    - `src/constants/product-labels.ts` exports `DIGEST`, `CONTACTS`, `EVENTS`
    - TAB_ICON has DigestTab + EventsTab keys and no BackupTab key (a prose comment may still mention it): `grep -Eq 'DigestTab:' src/components/icons/icon-registry.ts` and `grep -Eq 'EventsTab:' src/components/icons/icon-registry.ts` succeed, and `! grep -Eq 'BackupTab:' src/components/icons/icon-registry.ts`
    - The share-intent deep link no longer targets the removed tab: `! grep -q '"BackupTab"' src/navigation/linking.ts` and `grep -c 'navigate("SettingsTab", { screen: "Backup" })' src/navigation/linking.ts` returns 1
    - The surviving Settings-hosted Backup consumes the shared backup (cycle-4 HIGH): `shouldConsumeSharedBackup("settings")` returns true (or a share-intent origin does), proven by a `src/screens/backup-dualhome-logic.test.ts` case asserting the Settings-hosted Backup drains the shared-backup singleton on the restore path — so the `.orbitbackup` share-intent restore still works after the Backup tab is removed; `npx vitest run src/screens/backup-dualhome-logic.test.ts` is green
    - `npx tsc --noEmit` is clean; `npx vitest run src/navigation` passes; `npm run check:colors` passes
  </acceptance_criteria>
  <done>The app boots to the Digest tab; five tabs render in the specified order with no Backup tab; the internal DashboardTab id is preserved; tsc + navigation tests + check:colors are green; the slice is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Promote Digest + GroupEvents to tab roots; wire origin-aware Profile Back</name>
  <files>src/navigation/types.ts, src/navigation/shell-contract.ts, src/navigation/shell-contract.test.ts, src/navigation/tabs/DigestStack.tsx, src/navigation/tabs/EventsStack.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/focused-route-classification.ts</files>
  <read_first>
    - src/navigation/tabs/DashboardStack.tsx (remove the pushed Digest :69 and GroupEvents :39 screens; keep all other children)
    - src/navigation/types.ts (DashboardStackParamList children incl. the stale `GroupEvents: undefined` :47 and `Digest: undefined` :155 that must STAY this wave; `RecentlyDeleted: { contactId: number }` :76 (Dashboard) and :178 (Orrery); OrreryStackParamList :166-214 shows the full set of shared child routes registered where Profile is hosted — mirror the applicable ones, RecentlyDeleted included)
    - src/navigation/tabs/OrreryStack.tsx (:18 RecentlyDeletedScreen import; :64 `Stack.Screen name="RecentlyDeleted"` — the analog for the new stacks)
    - src/screens/GroupEventsScreen.tsx (navigates to GroupEventDetail :118; user-facing `title="Group Events"` at :72 (verified on disk — only :72, not :68) and the "group events" search/empty/a11y copy to relabel to Events — read the file for the exact copy line offsets) and the group-event screens: GroupEventDetailScreen, EditGroupEventScreen, EditParticipantScreen
    - src/services/notifications/notification-nav.ts (:43/:91 the digest reset intent still names `Digest` — the reason the DashboardStackParamList `Digest` entry must remain until Plan 04)
    - src/navigation/focused-route-classification.ts (isFocusedWorkflow — confirm the Digest and GroupEvents root route names are NOT classified as focused-workflows, so the tab bar stays visible on those roots)
    - 38-PATTERNS.md "DashboardStack.tsx (MODIFY)" + "types.ts (MODIFY)" sections; dossier §L (origin-aware Profile Back), §B (Events relabel)
  </read_first>
  <action>
    Complete the promotion so Digest and Events are true tab roots with correct origin-aware Back (dossier §L / S.2):
    (1) In `DigestStackParamList` register the full set of shared child routes the Digest surface can reach so Digest→row tap→Profile→Back→Digest resolves within the DigestTab stack: `Profile`, plus the origin-aware siblings a Profile can open that the Digest rows lead to (`Compose`, `ComposeResearch`, `LogContact`, `Edit`, `EditInteraction`, `ThingsToRemember`, `MemoryHistory`, `GroupEventDetail`, `EditGroupEvent`, `EditParticipant`, `CropPhoto`, `SurvivorSelect`, `MergeConflicts`, `MergeImpactSummary`, **`RecentlyDeleted: { contactId: number }`**) — mirror the exact shapes already registered in `OrreryStackParamList` (which registers `RecentlyDeleted` at `types.ts:178` / `OrreryStack.tsx:64` precisely because a Profile can reach it — the omission of `RecentlyDeleted` is the codex HIGH; a Profile opened from Digest/Events must not navigate to an unregistered route). In `EventsStackParamList` register `GroupEvents`, `GroupEventDetail`, `EditGroupEvent`, `EditParticipant`, `Profile`, `RecentlyDeleted`, and the same origin-aware siblings Events→detail→Profile can reach. Build the route set by mechanically comparing to every target reachable from `ContactProfileScreen`, not by hand-copying a partial list.
    (1b) Expand the descriptor arrays in `src/navigation/shell-contract.ts` — `DIGEST_STACK_ROUTES` and `EVENTS_STACK_ROUTES` — to the SAME full Profile-reachable route set registered in the param lists above (so the arrays and the ParamLists agree). Build the arrays by mechanically enumerating every route reachable from `ContactProfileScreen` (Profile, RecentlyDeleted, GroupEventDetail, EditGroupEvent, EditParticipant, Compose, ComposeResearch, LogContact, Edit, EditInteraction, ThingsToRemember, MemoryHistory, CropPhoto, SurvivorSelect, MergeConflicts, MergeImpactSummary), not by hand-copying a partial list. These arrays remain the render-free source of truth the stacks map over and the shell-contract test asserts.
    (2) Render the matching `Stack.Screen`s in `DigestStack.tsx` and `EventsStack.tsx` by mapping the expanded descriptor arrays through each file's local route-name→component registry (including `RecentlyDeleted` → `RecentlyDeletedScreen`, imported as in `OrreryStack.tsx:18`), importing the existing screen components (no new screens) — so the REGISTERED routes are exactly `DIGEST_STACK_ROUTES` / `EVENTS_STACK_ROUTES`, and the co-located completeness assertion (Task 1) guarantees every descriptor name has a component.
    (3) In `DashboardStack.tsx` remove the `Digest` and `GroupEvents` `Stack.Screen`s (now tab roots elsewhere) and their now-unused imports; keep every other screen unchanged. **Do NOT remove the `Digest: undefined` (`types.ts:155`) and `GroupEvents: undefined` (`types.ts:47`) type entries from `DashboardStackParamList` in this wave-1 plan** — `notification-nav.ts` still declares the digest reset intent as `routes: [{ name: "Home" }, { name: "Digest" }]` and `notification-gate.tsx` applies it, so `Digest` must remain a valid `DashboardStackParamList`/reset target until Plan 04 (wave 2) repoints that intent. Removing these entries here would break the wave-1 tsc gate; their removal is assigned to Plan 04 (addresses claude MEDIUM — stale param-list entries + wave-ordering constraint).
    (4) In `focused-route-classification.ts`, confirm the Digest and GroupEvents root names are treated as ordinary (non-focused-workflow) roots so the tab bar renders on them; if the classifier is name-based and would hide the bar on these roots, add them to the ordinary set.
    (5) **Own the Events relabel (dossier §B, S-05):** in `src/screens/GroupEventsScreen.tsx` change the user-facing root `title="Group Events"` (at `:72`) to the `EVENTS` product-label value ("Events"), and update the user-facing search/empty/a11y copy that reads "group events" (the search `accessibilityLabel`, item label, and error/empty copy — read the file for the exact offsets) to "events" where the shorter term is natural. Do NOT rename the internal `GroupEvent`/route identifiers or ADR language (§B DERIVED / §Q — no repository-wide terminology cleanup); this is the user-facing Events-root strings only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/navigation 2>&1 | tail -15</automated>
    <human-check>On the Pixel: from Digest, open a contact Profile and press system Back — it returns to Digest (not Contacts). From Events, open a Group Event detail → a participant Profile → Back returns toward the Events context.</human-check>
  </verify>
  <acceptance_criteria>
    - DashboardStack no longer registers the promoted roots: `grep -c 'name="Digest"' src/navigation/tabs/DashboardStack.tsx` returns 0 and `grep -c 'name="GroupEvents"' src/navigation/tabs/DashboardStack.tsx` returns 0
    - The new stacks are ARRAY-DRIVEN (rendered by mapping the descriptor arrays through a name→component registry, not literal `name="…"` JSX): `DIGEST_STACK_ROUTES` and `EVENTS_STACK_ROUTES` (shell-contract.ts) each include `Profile` and `RecentlyDeleted`, and `EVENTS_STACK_ROUTES` includes `GroupEventDetail` — `grep -c 'RecentlyDeleted' src/navigation/shell-contract.ts` returns at least 2 (present in both arrays) and `grep -c 'Profile' src/navigation/shell-contract.ts` returns at least 2. The registered route set == the arrays (proven by the render-free test below + each stack's co-located completeness assertion), so no Profile-reachable route is unregistered
    - `DigestStackParamList` and `EventsStackParamList` both include `Profile: ProfileRouteParams` and `RecentlyDeleted: { contactId: number }`
    - The stale DashboardStackParamList entries are RETAINED this wave (removed by Plan 04): `grep -c 'Digest: undefined' src/navigation/types.ts` returns 1 and `grep -c 'GroupEvents: undefined' src/navigation/types.ts` returns 1
    - Events root relabelled: `! grep -q 'title="Group Events"' src/screens/GroupEventsScreen.tsx` (title now the EVENTS label) and the user-facing search a11y copy no longer reads "group events" verbatim on the Events root
    - A RENDER-FREE `src/navigation/shell-contract.test.ts` (node env; NO NavigationContainer mount, NO react-test-renderer — the repo has neither) asserts `TAB_ORDER` is exactly `["DashboardTab","EventsTab","DigestTab","OrreryTab","SettingsTab"]`, `INITIAL_TAB === "DigestTab"`, and that `DIGEST_STACK_ROUTES` and `EVENTS_STACK_ROUTES` each CONTAIN every Profile-reachable route — asserted PER route (Profile, RecentlyDeleted, GroupEventDetail, EditGroupEvent, EditParticipant, Compose, ComposeResearch, LogContact, Edit, EditInteraction, ThingsToRemember, MemoryHistory, CropPhoto, SurvivorSelect, MergeConflicts, MergeImpactSummary), not just RecentlyDeleted/Profile/GroupEventDetail — so a mechanical route omission is caught (addresses cycle-3 HIGH-1 + the Plan 01 LOW route-set backstop). The live Profile→RecentlyDeleted TRAVERSAL is verified on the Pixel in Plan 07 (§S.14/S.15), never by mounting a navigator in vitest.
    - `npx tsc --noEmit` is clean (the RootStackParamList intersection still resolves every reached route); `npx vitest run src/navigation` passes
  </acceptance_criteria>
  <done>Digest and Events are independent tab roots with every Profile-reachable child route registered (incl. RecentlyDeleted); DashboardStack no longer hosts the promoted roots but retains their param-list entries until Plan 04; the Events root is relabelled; origin-aware Profile Back is preserved from both new roots; tsc + nav tests green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Reselect-to-root on all five tabs; assert no dangling in-stack navigations</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/RootNavigator.tsx (handleActiveTabPress :91-118 is route-agnostic popToTop; the four existing Tab.Screens wire it via listeners :219-249)
    - src/navigation/tabs/DashboardStack.tsx (its screens must not still navigate to the removed Digest/GroupEvents route names)
    - src/screens/HomeScreen.tsx (header shortcuts to Your Week/Group Events are removed in Plan 04, NOT here — do not touch HomeScreen in this plan)
    - 38-RESEARCH.md S.14 (reselect-to-root / fresh-launch / resume)
  </read_first>
  <action>
    Confirm every one of the five tabs wires `listeners={({ navigation, route }) => ({ tabPress: (event) => handleActiveTabPress(event, navigation, route) })}` (the new DigestTab and EventsTab included, exactly as the existing four do). Do not modify `handleActiveTabPress` — it is already route-agnostic. Then verify that no screen still registered inside DashboardStack performs an in-stack navigation to a route name that is no longer in DashboardStack (the promoted Digest/GroupEvents roots); such calls belong to Plan 04's HomeScreen cleanup, but any OTHER DashboardStack screen that referenced them must be corrected to the tab route or removed. Do not touch `src/screens/HomeScreen.tsx` in this plan (owned by Plan 04).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/navigation 2>&1 | tail -15</automated>
    <human-check>On the Pixel: drill into a stack on each tab (e.g. Contacts→Profile), then tap that same tab again — it pops back to the tab root. Background the app from a non-Digest tab, resume — it returns to that tab (does NOT reset to Digest).</human-check>
  </verify>
  <acceptance_criteria>
    - All five Tab.Screens wire the tabPress listener: `grep -c 'handleActiveTabPress' src/navigation/RootNavigator.tsx` returns 6 (one definition + five call sites)
    - No DashboardStack-hosted screen other than HomeScreen references the removed roots (HomeScreen is Plan 04): scoped check excluding HomeScreen finds none
    - `npx tsc --noEmit` clean; `npx vitest run src/navigation` passes
  </acceptance_criteria>
  <done>Reselect-to-root works on all five tabs; no dangling in-stack navigation to the promoted roots remains inside DashboardStack (excluding HomeScreen); tsc + nav tests green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01)

New symbols/files created here (for source-grounding exclusion in later review):
- `src/navigation/tabs/DigestStack.tsx` → `DigestStack`
- `src/navigation/tabs/EventsStack.tsx` → `EventsStack`
- `src/constants/product-labels.ts` → `DIGEST`, `CONTACTS`, `EVENTS`
- `src/navigation/shell-contract.ts` → runtime shell/route descriptor: `TAB_ORDER`, `INITIAL_TAB`, `DIGEST_STACK_ROUTES`, `EVENTS_STACK_ROUTES` (render-free, node-testable) + `src/navigation/shell-contract.test.ts`
- `src/navigation/types.ts` → new types `DigestStackParamList`, `EventsStackParamList`; `TabParamList` gains `DigestTab`, `EventsTab`, loses `BackupTab`
- `src/components/icons/icon-registry.ts` → `TAB_ICON` re-keyed (adds `DigestTab`,`EventsTab`; removes `BackupTab`)
- Modified: `src/navigation/RootNavigator.tsx`, `src/navigation/tabs/DashboardStack.tsx`, `src/navigation/focused-route-classification.ts`, `src/navigation/linking.ts` (BackupTab→SettingsTab deep-link repoint), `src/screens/GroupEventsScreen.tsx` (user-facing Group Events → Events relabel)
- `DigestStackParamList`/`EventsStackParamList` gain `RecentlyDeleted: { contactId: number }` (route already exists on other stacks — not a new symbol)

## Review feedback addressed (Plan 01)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| DigestStack/EventsStack omit RecentlyDeleted (Profile-reachable) | HIGH (codex #7) | Incorporated — Task 2 registers `RecentlyDeleted` in both stacks + traversal test |
| User-facing "Group Events" remains after Events relabel (GroupEventsScreen.tsx:68) | MEDIUM (codex #8 / S-05) | Incorporated — Task 2 relabels the Events-root title + search/a11y copy |
| linking.ts:67 BackupTab share-intent deep link breaks on tab removal | MEDIUM (claude) | Incorporated — Task 1 repoints to SettingsTab→Backup in the same wave-1 commit |
| **[cycle 4] shared-backup RESTORE silently broken by tab removal — Settings-hosted Backup (host="settings") never consumes the shared singleton (shouldConsumeSharedBackup returns true only for "backup-tab"), so the repointed deep link resolves but never restores** | HIGH (codex, disk-confirmed) | Incorporated — Task 1 step (7a) makes the surviving Settings-hosted Backup consume the shared-backup singleton (return true for the surviving host / gate behind a share-intent origin if consume is not a safe no-op), refreshes the inverted :18 comment, keeps the origin-aware return target, and adds a backup-dualhome-logic test proving the restore path drains the singleton |
| Stale DashboardStackParamList Digest/GroupEvents entries + wave ordering | MEDIUM (claude) | Incorporated — Task 2 retains them this wave; removal assigned to Plan 04 |
| Focused-route task mostly a no-op | LOW (codex) | Kept as an explicit verification step (Task 2 step 4) — confirms, does not add churn |
| **[cycle 2] refresh stale Backup-tab comment (types.ts:256-262)** | LOW (codex) | Incorporated — Task 1 step (4) refreshes the "Backup tab stays" comment to Settings-only once BackupTab is removed |
| **[cycle 2] add a nav mount test traversing a Profile-reachable route (e.g. RecentlyDeleted) in each new stack** | LOW (codex) | Already present — Task 2 acceptance mounts DigestStack + EventsStack and traverses Profile → RecentlyDeleted without an unregistered-route error |
| **[cycle 2] line-cite drift — `title="Group Events"` is only at :72, not :68/:72** | LOW (claude) | Incorporated — Task 2 read_first/action corrected to `:72` (verified on disk); the grep acceptance was already citation-independent |
| **[cycle 3] HIGH-1 — Task 2's "mount DigestStack/EventsStack and traverse Profile→RecentlyDeleted" acceptance is non-executable (no react-test-renderer/@testing-library on disk; vitest.config.ts mandates render-free)** | HIGH (codex, disk-confirmed) | Incorporated — Task 1 extracts a RUNTIME descriptor `shell-contract.ts` (TAB_ORDER/INITIAL_TAB + per-stack route arrays) that RootNavigator + the stacks render FROM; Task 2 asserts it via a render-free `shell-contract.test.ts` and moves the live traversal to Plan 07 device UAT. The mount/traversal acceptance is removed |
| **[cycle 3] Task 2 acceptance greps only RecentlyDeleted/Profile/GroupEventDetail — no backstop for the other Profile-reachable routes** | LOW (claude) | Incorporated — the render-free descriptor test asserts PER Profile-reachable route (incl. SurvivorSelect/MergeConflicts/MergeImpactSummary/…), a mechanical backstop complementing the HIGH-1 descriptor test |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification/deep-link → navigation intent | Handled in Plan 04; this plan only defines the tab targets those intents resolve to |
| in-app navigation state | Route names/params are typed serializable primitives; no external input crosses into route params here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | Tab route identity (DashboardTab kept vs renamed) | low | mitigate | Preserve the internal `DashboardTab` id so the FAB's typed `navigate("DashboardTab",…)` target cannot silently break (RESEARCH Pitfall 1); tsc enforces the `TabParamList`↔`TAB_ICON` key parity |
| T-38-02 | Information Disclosure | Digest/Events read paths on new roots | low | accept | Roots self-fetch on-device SQLite only; no network is introduced by promotion (verified against DigestScreen/GroupEventsScreen focus reads) |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the navigation surface (RootStackParamList intersection resolves every reached route).
- `npx vitest run src/navigation` green.
- `npm run check:colors` green (no colour literal introduced; the headerless shell needs none).
- On-device (§S.14): fresh launch → Digest; five tabs in order; no Backup tab; reselect-to-root on each tab; background/resume preserves the prior tab.
</verification>

<success_criteria>
Five-tab shell (Contacts·Events·Digest·Orrery·Settings) with Digest as initial route and centered; Backup tab removed (Backup still reachable via Settings, dossier §K); DigestStack + EventsStack own their roots with origin-aware Profile Back; internal DashboardTab id preserved; all automated gates green.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-01-SUMMARY.md` when done.
</output>
