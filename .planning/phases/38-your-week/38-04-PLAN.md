---
phase: 38-your-week
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/navigation/reset-intents.ts
  - src/services/notifications/notification-nav.ts
  - src/navigation/notification-gate.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/dashboard-overflow-actions.ts
  - src/screens/dashboard-overflow-actions.test.ts
  - src/navigation/types.ts
  - src/components/UniversalFab.tsx
  - src/components/universal-fab-logic.ts
  - src/components/universal-fab-logic.test.ts
autonomous: true
requirements: [S-03, S-04, S-05, S-13]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Tapping the Digest notification opens/selects the new Digest tab root (dossier §N, S.13) — not Contacts→Digest inside the former Dashboard stack"
    - "Back from a notification-opened Digest behaves sanely (Digest is a home tab root); the decay and birthday notification intents are unchanged; the existing notification-gate concurrency guard (an older async contact lookup cannot overwrite a newer destination) is preserved for contact intents while the Digest intent bypasses the lookup (addresses codex MEDIUM — stale-request coverage)"
    - "The Contacts (former Dashboard) header no longer shows the 'Your Week' or 'Group Events' shortcuts, and no replacement clutter is added (dossier §K)"
    - "No Contacts-stack navigation to the promoted roots remains after Plan 01's screen removal: the Contacts overflow 'Group Events' action (dashboard-overflow-actions.ts) is repointed to the Events tab (it would otherwise crash at runtime — the promoted screen is gone from DashboardStack), and the now-unused `Digest`/`GroupEvents` DashboardStackParamList entries are removed so no `navigate(\"Digest\")` typechecks-but-crashes footgun remains (addresses claude MEDIUM — stale param-list entries + latent runtime hazard)"
    - "The global FAB remains a shell affordance visible on Digest with the same actions available from Contacts; its actions still resolve because the internal DashboardTab route id is unchanged (Plan 01); the FAB current-contact context is recognized on Digest/Events-origin Profiles (dossier §M)"
  artifacts:
    - path: src/navigation/reset-intents.ts
      provides: "resetToDigestTab() — the root-level reset shape that selects the Digest tab at its root"
      contains: "resetToDigestTab"
    - path: src/services/notifications/notification-nav.ts
      provides: "digest tap intent repointed to the Digest tab root (distinct from the DashboardTab-nested compose/profile intents)"
      contains: "digest"
  key_links:
    - from: src/navigation/notification-gate.tsx
      to: src/navigation/reset-intents.ts
      via: "applyBodyNav applies resetToDigestTab() for the digest intent, resetToDashboardWith() for compose/profile"
      pattern: "resetToDigestTab"
    - from: src/screens/HomeScreen.tsx
      to: src/navigation/RootNavigator.tsx
      via: "removal of the header 'Your Week'/'Group Events' shortcuts (those roots are now tabs)"
      pattern: "trailingLabelProbe"
---

<objective>
Finish the shell transition's semantic wiring: repoint the Digest notification/deep-link to the new Digest tab root, remove the now-obsolete Contacts-header shortcuts, and audit the global FAB across the new roots. The five-tab shell (Plan 01) moved Digest and Group Events out of the Dashboard stack; the notification resolver and the Contacts header still assume the old shape and must be corrected (dossier §N/§K/§M; S.3/S.4/S.5/S.13).

Plan against the dossier (D-01). No legacy-navigation migration machinery — there are no production users through this shell transition (dossier §N planning note). Keep the phase narrow (D-03): do not redesign Contacts, and do not invent Digest-specific FAB actions (§M).

Purpose: the Digest notification opens Digest; the Contacts header is decluttered; the FAB behaves consistently on every root.
Output: `resetToDigestTab()`; a repointed digest notification intent + gate branch (with extended test); HomeScreen header-shortcut removal; a FAB mounting/visibility audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; hook-blocked). Commit in place on the current branch.
- `notification-nav.ts` must stay node-loadable: NO react-navigation / expo import; it returns a serializable, discriminated nav intent only.
- Local-first: no network on any path touched here. All colours via theme tokens (`npm run check:colors`).
- Do NOT rename the internal `DashboardTab` route id — the FAB hardcodes it; the FAB audit relies on it being unchanged (RESEARCH Pitfall 1).
- Review the actual code on disk (files in `<read_first>`), not a diff.
</execution_context>

<context>
@.planning/phases/38-your-week/38-CONTEXT.md
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the Digest notification to the Digest tab root</name>
  <files>src/navigation/reset-intents.ts, src/services/notifications/notification-nav.ts, src/navigation/notification-gate.tsx, src/services/notifications/notification-nav.test.ts, src/navigation/notification-gate.test.tsx</files>
  <read_first>
    - src/services/notifications/notification-nav.ts (NavIntent union :31-52; the digest pre-check branch :83-93 returning a [Home, Digest] reset; the malformed→null contract)
    - src/navigation/reset-intents.ts (resetToDashboardRoot :17-28; resetToDashboardWith :30-42 — both nest routes under DashboardTab; the TabResetState shape)
    - src/navigation/notification-gate.tsx (guardNotificationBodyIntent — only the Compose/decay case is guarded; applyBodyNav calls nav.reset(resetToDashboardWith(intent.routes[1])) :the apply function; the warm/cold flush effects)
    - src/services/notifications/notification-nav.test.ts + src/navigation/notification-gate.test.tsx (the existing intent-shape assertions to extend)
    - src/navigation/types.ts (TabParamList with DigestTab after Plan 01; DigestStackParamList with Digest root)
    - 38-PATTERNS.md "notification-nav.ts (MODIFY)" section; RESEARCH Pitfall 2; dossier §N / S.13
  </read_first>
  <action>
    Repoint the digest tap so it selects the new Digest tab root instead of nesting `Digest` inside DashboardTab:
    (1) In `src/navigation/reset-intents.ts` add `resetToDigestTab(): TabResetState` returning a root reset that selects the DigestTab at its `Digest` root (mirroring `resetToDashboardRoot`'s single-tab shape but for `DigestTab` → `{ index: 0, routes: [{ name: "Digest" }] }`).
    (2) In `src/services/notifications/notification-nav.ts` change the digest branch to return a distinct, serializable digest intent (a discriminated variant that names the Digest tab target, e.g. `{ type: "select-digest" }`) rather than a `[Home, Digest]` reset — keep the module node-loadable and keep the pre-narrowing digest check (a digest payload carries no contactId — the V5 boundary). Leave the decay (Compose) and birthday (Profile) intents unchanged.
    (3) In `src/navigation/notification-gate.tsx` branch the application: for the digest intent apply `nav.reset(resetToDigestTab())`; for the compose/profile reset intents keep `nav.reset(resetToDashboardWith(intent.routes[1]))`. The Compose contact-liveness guard still applies only to the decay case. **Preserve the existing `applyBodyNav` concurrency guard** (an older async contact lookup must not overwrite a newer destination — the `notification-gate.test.tsx` stale-request coverage): the contact intents keep the guarded lookup path; the digest intent carries NO contactId (the V5 boundary), does no lookup, and applies immediately — but the refactor must not weaken or bypass the guard for the contact path.
    (4) Extend `notification-nav.test.ts` and `notification-gate.test.tsx` to assert the new digest intent selects the Digest tab root AND that the stale-request/concurrency guard still holds for contact intents after branching; keep the decay/birthday/malformed cases green.
  </action>
  <verify>
    <automated>npx vitest run src/services/notifications/notification-nav.test.ts src/navigation/notification-gate.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
    <human-check>On the Pixel: fire/tap a Digest notification — it opens the Digest tab root (not Contacts→Digest); Back behaves as a home-tab root.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'resetToDigestTab' src/navigation/reset-intents.ts` returns 1 and `grep -c 'resetToDigestTab' src/navigation/notification-gate.tsx` returns at least 1
    - notification-nav.ts stays node-loadable: `grep -c "@react-navigation\|expo-" src/services/notifications/notification-nav.ts` returns 0
    - The digest intent no longer resets to a Home-nested Digest: the digest branch of notification-nav.ts does not return a `routes: [{ name: "Home" }, { name: "Digest" }]` shape
    - The gate test file is `src/navigation/notification-gate.test.tsx` (a `.tsx`, not `.test.ts`) and the verify command targets it; it still asserts the concurrency/stale-request guard for contact intents
    - notification-nav + notification-gate tests pass (decay/birthday/malformed unchanged, digest asserts the Digest-tab-root target, concurrency guard preserved); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The Digest notification resolves to the Digest tab root via resetToDigestTab(); decay/birthday intents unchanged; resolver stays node-loadable; tests + tsc green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the Contacts-header Your Week / Group Events shortcuts</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (the header shortcuts block around :1648-1711 — trailingLabelProbe and the two Pressables that navigate to the now-promoted roots; goToProfile :1277; other header content that stays)
    - 38-PATTERNS.md "HomeScreen.tsx (MODIFY)" section; dossier §K (remove the shortcuts, add no replacement clutter) + §Q (no repository-wide terminology cleanup)
  </read_first>
  <action>
    In `src/screens/HomeScreen.tsx`, remove the header shortcut affordances that pointed at the now-promoted Digest and Group Events roots (the `trailingLabelProbe` shortcut block and its two Pressables), per dossier §K. Add NO replacement clutter. Remove any now-unused imports/handlers left behind. Do not otherwise redesign Contacts (D-03/§K) and do not perform repository-wide terminology cleanup (§Q) — this is the header-shortcut removal only. After removal, the former in-stack `navigate` to those promoted roots must no longer exist in HomeScreen (those roots are tabs now, not DashboardStack children).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/screens 2>&1 | tail -15 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>On the Pixel: the Contacts screen header no longer shows the Your Week / Group Events shortcuts and shows no replacement clutter; Contacts browsing/search/filter/sort still work.</human-check>
  </verify>
  <acceptance_criteria>
    - The header-shortcut probe is gone: `grep -c 'trailingLabelProbe' src/screens/HomeScreen.tsx` returns 0
    - HomeScreen no longer navigates to the promoted roots from the removed header block (verified by reading the header region; no `navigate` to those tab roots remains in HomeScreen)
    - `npx tsc --noEmit` clean; `npx vitest run src/screens` passes; `npm run check:colors` passes
  </acceptance_criteria>
  <done>The Contacts header shortcuts to Your Week / Group Events are removed with no replacement clutter and no unused-import breakage; tsc + screen tests + check:colors green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Global FAB mounting / visibility audit across the new roots</name>
  <files>src/components/UniversalFab.tsx, src/components/universal-fab-logic.ts, src/components/universal-fab-logic.test.ts (audit; edit universal-fab-logic.ts for the Digest/Events context extension, and its test for the tab-set update)</files>
  <read_first>
    - App.tsx (the shell-global `<UniversalFab />` mount at :400, outside RootNavigator, floating over all tabs; NotificationResponseGate wiring)
    - src/components/UniversalFab.tsx (visibility logic and the hardcoded DashboardTab navigate target)
    - src/components/universal-fab-logic.ts (FabNavigateIntent.tab: "DashboardTab" :30; resolveFabTarget :82-106 returns DashboardTab targets; getFocusedContactContext :140-146 checks DashboardTab/OrreryTab)
    - 38-PATTERNS.md; dossier §M (FAB on Digest with the Contacts actions; no Digest-specific FAB actions) + RESEARCH S.3
  </read_first>
  <action>
    Audit the global FAB against the new five-tab shell. Confirm `<UniversalFab />` (App.tsx:400) still mounts globally and is visible on the Digest root, and that its actions still resolve — they target the internal `DashboardTab` route (kept unchanged in Plan 01), so "Add Contact"/"Log" continue to open the Contacts (DashboardTab) stack (dossier §M: the same actions available from Contacts). **Extend the current-contact context recognizer:** `getFocusedContactContext` (universal-fab-logic.ts:140-146) currently returns a contact context only when the focused tab is `DashboardTab` or `OrreryTab`, so a Profile opened from the Digest or Events tab loses its current-contact context and Quick Log falls back to the contact picker (codex LOW). Add `DigestTab` and `EventsTab` to that recognized-tab set so Quick Log keeps the current contact when launched from a Digest/Events-origin Profile. If the audit finds any OTHER real gap (FAB hidden on a new root, an action that no longer resolves), apply the minimal fix — do NOT invent Digest-specific FAB actions and do NOT rebuild the FAB. Record the audit outcome in the SUMMARY (visible-on-Digest confirmed; actions resolve; getFocusedContactContext extended to the new tabs). **Update the test's stale tab set (cycle-6 — `universal-fab-logic.test.ts:152-162`):** the "returns no contact from root-tab browse surfaces" case iterates the string array `["DashboardTab","OrreryTab","BackupTab","SettingsTab"]` — remove the now-removed `"BackupTab"` and cover the new five-tab set, asserting a bare tab ROOT (no Profile focused) still returns `{ originContactId: null }` for `DashboardTab`/`EventsTab`/`DigestTab`/`OrreryTab`/`SettingsTab`, AND add a case that a Profile focused inside `DigestTab`/`EventsTab` now DOES return the contact context (the extension above). Do not leave `"BackupTab"` in the test.
  </action>
  <verify>
    <automated>npx vitest run src/components/universal-fab-logic.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
    <human-check>On the Pixel: the FAB is visible on the Digest tab; opening it shows the same actions as Contacts; "Add Contact" and "Log" work from Digest (they land in the Contacts stack).</human-check>
  </verify>
  <acceptance_criteria>
    - The FAB target is unchanged and still resolves: `grep -c 'DashboardTab' src/components/universal-fab-logic.ts` returns at least 1 (internal id intact)
    - `getFocusedContactContext` recognizes the new tabs: `grep -c 'DigestTab\|EventsTab' src/components/universal-fab-logic.ts` returns at least 1, and a `universal-fab-logic.test.ts` case asserts a Digest/Events-origin Profile keeps the current-contact context (Quick Log does not fall back to the picker)
    - `npx vitest run src/components/universal-fab-logic.test.ts` passes
    - `npx tsc --noEmit` clean
    - The SUMMARY records the FAB audit result (visible on Digest; actions resolve to the Contacts stack; context recognizer extended)
  </acceptance_criteria>
  <done>The global FAB is confirmed visible on Digest with the Contacts actions resolving via the preserved DashboardTab id; getFocusedContactContext recognizes the new tabs; any minimal visibility/resolution fix is tested; tsc green; audit recorded; committed.</done>
</task>

<task type="auto">
  <name>Task 4: Repoint the Contacts overflow Group Events action + remove stale DashboardStackParamList entries</name>
  <files>src/screens/dashboard-overflow-actions.ts, src/screens/dashboard-overflow-actions.test.ts, src/screens/HomeScreen.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/screens/dashboard-overflow-actions.ts (interface `DashboardOverflowNavigation { navigate: (route: "GroupEvents" | "UnboundContacts" | "Archived") => void }` :3-6; `onPress: () => navigation.navigate("GroupEvents")` :28 — the Contacts overflow "Group Events" action; after Plan 01 removed the GroupEvents Stack.Screen from DashboardStack this navigate targets a route no longer in the Contacts stack and would CRASH at runtime. NOTE: this interface's `navigate` is a SINGLE-arg local-route callback — it CANNOT model a cross-tab `navigate("EventsTab", { screen: "GroupEvents" })` (that needs a second `{screen}` param), so widening the route union is the WRONG fix (codex LOW). Add a dedicated cross-tab callback instead — read how the caller screen wires `navigation` into this interface before choosing the callback name)
    - src/navigation/types.ts (`GroupEvents: undefined` :47 and `Digest: undefined` :155 in DashboardStackParamList — the stale entries Plan 01 deliberately retained; the `Digest: undefined` entry carries a now-stale doc comment :146-154 ("weekly 'your week' digest screen … Reached from the dashboard's discreet 'Your week' top-bar entry … initialRouteName stays Home") that is false post-Plan-01 and must be removed WITH the entry; EventsStackParamList/DigestStackParamList (Plan 01) now own GroupEvents/Digest; RootStackParamList is the intersection so `RootStackScreenProps<"GroupEvents">` still resolves after removal)
    - src/navigation/reset-intents.ts + src/services/notifications/notification-nav.ts (confirm Task 1 already repointed the digest intent so `Digest` is no longer referenced as a DashboardStack route)
    - src/screens/HomeScreen.tsx (confirm Task 2 removed the header shortcuts so `navigate("Digest")`/`navigate("GroupEvents")` no longer appear there; AND the overflow CALLER `buildDashboardOverflowActions({ navigation: … })` at :1464-1470 — once `openEvents()` becomes a REQUIRED member of `DashboardOverflowNavigation`, this caller must supply it or the project-wide `tsc` gate breaks, so this task owns the caller edit too, addresses cycle-3 Plan 04 LOW)
    - src/screens/dashboard-overflow-actions.test.ts (:5-14 constructs a `DashboardOverflowNavigation` test double with ONLY `navigate` — it must gain an `openEvents` stub or it fails to typecheck/asserts once `openEvents` is required)
  </read_first>
  <action>
    (1) In `src/screens/dashboard-overflow-actions.ts`, repoint the Contacts overflow "Group Events" action to open the Events tab — but do NOT widen the `navigate` route union to carry a cross-tab `{screen}` param (that single-arg callback cannot model it, codex LOW). Instead, ADD a dedicated cross-tab callback to the `DashboardOverflowNavigation` interface — e.g. `openEvents(): void` — and have the Group Events action call `navigation.openEvents()`; keep `navigate` for the remaining LOCAL routes (narrow its union to `"UnboundContacts" | "Archived"`, dropping `"GroupEvents"`). The caller screen wires `openEvents` to the real cross-tab navigation (`navigate("EventsTab", { screen: "GroupEvents" })` via the parent/root navigator, matching how other cross-tab jumps are done) — keeping the `{screen}`-shaped call out of the pure action builder and NOT weakening route typing. This is a correctness fix (the action would otherwise crash after Plan 01), not a redesign; do NOT remove the affordance (whether this now-redundant overflow entry should be dropped is an owner/product call, out of scope here — §K only named the header shortcuts).
    (2) After confirming (via `<read_first>`) that no code references `Digest`/`GroupEvents` as DashboardStack routes anymore (notification intent repointed in Task 1, header shortcuts removed in Task 2, overflow repointed above), remove the stale `Digest: undefined` (types.ts:155) and `GroupEvents: undefined` (types.ts:47) entries from `DashboardStackParamList` — AND remove the now-false doc comment above `Digest: undefined` (types.ts:146-154, which still describes Digest as a Dashboard-stack sub-screen "reached from the dashboard's … 'Your week' top-bar entry" with "initialRouteName stays Home"). This closes the "typechecks-but-crashes at runtime" footgun the reviewer named and clears a stale comment (§Q bounded doc-sync, not a repo-wide sweep). If a grep finds ANY remaining `navigate("Digest")`/`navigate("GroupEvents")` in a Contacts/Dashboard context, repoint it too before removing the entries.
    (3) **Update the caller and its test so `openEvents` becoming required does not break an out-of-task consumer (cycle-3 Plan 04 LOW):** in `src/screens/HomeScreen.tsx` at the `buildDashboardOverflowActions({ … })` call (:1464-1470), supply the new `openEvents` callback wired to the real cross-tab navigation — keeping the `{screen}`-shaped call in the screen, not the pure action builder. **Specify the cross-tab access precisely (cycle-4 LOW): `HomeScreen` is inside the DashboardStack (a stack navigator), not the tab navigator, so it CANNOT `navigate("EventsTab", …)` on its own `navigation` prop.** Obtain the tab navigator via `navigation.getParent<BottomTabNavigationProp<TabParamList>>()` (typed to `TabParamList` from `src/navigation/types.ts`) and call `parent?.navigate("EventsTab", { screen: "GroupEvents" })`. **Fail closed:** if `getParent()` returns `undefined` (no tab navigator mounted — e.g. a test/preview host), `openEvents` is a no-op with a dev-only warning and never throws (do NOT assume the parent exists). Match how any existing cross-tab jump in the codebase resolves its parent navigator (read one before choosing the exact typing). In `src/screens/dashboard-overflow-actions.test.ts` (:5-14) add an `openEvents` stub to the `DashboardOverflowNavigation` test double and assert the Group Events action invokes `openEvents()` (not `navigate("GroupEvents")`). Both edits are required for the project-wide `tsc` gate + the overflow suite to stay green after `openEvents` is required.
  </action>
  <verify>
    <automated>grep -rn 'navigation.navigate("GroupEvents")\|navigation.navigate("Digest")' src/screens 2>&1 | tail -5; npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/navigation src/screens 2>&1 | tail -15</automated>
    <human-check>On the Pixel: the Contacts overflow menu "Group Events" action opens the Events tab (not a crash); nothing else in Contacts navigates to a dead route.</human-check>
  </verify>
  <acceptance_criteria>
    - The overflow action no longer pushes GroupEvents in the Contacts stack: `! grep -q 'navigation.navigate("GroupEvents")' src/screens/dashboard-overflow-actions.ts`; it calls a dedicated cross-tab callback (e.g. `grep -c 'openEvents' src/screens/dashboard-overflow-actions.ts` returns at least 1) and the `navigate` union no longer includes `"GroupEvents"` (`! grep -q '"GroupEvents"' src/screens/dashboard-overflow-actions.ts` in the interface's route union) — the cross-tab `{screen}` call lives in the caller, not the pure builder
    - The stale entries are gone: `! grep -q 'Digest: undefined' src/navigation/types.ts` and `! grep -q 'GroupEvents: undefined' src/navigation/types.ts` (DashboardStackParamList) — while EventsStackParamList/DigestStackParamList still declare them; the now-false Digest doc comment (types.ts:146-154) is removed with the entry
    - No Contacts/Dashboard-context `navigate("Digest")`/`navigate("GroupEvents")` remains: the grep in verify returns nothing under src/screens
    - The overflow CALLER + its TEST are updated (cycle-3 LOW): `grep -c 'openEvents' src/screens/HomeScreen.tsx` returns at least 1 (the caller supplies the cross-tab callback), and `src/screens/dashboard-overflow-actions.test.ts` constructs a `DashboardOverflowNavigation` double with an `openEvents` stub and asserts the Group Events action invokes `openEvents()` (not `navigate("GroupEvents")`)
    - `npx tsc --noEmit` clean (RootStackParamList intersection still resolves GroupEvents/Digest via the Events/Digest stacks); nav + screen suites pass
  </acceptance_criteria>
  <done>The Contacts overflow Group Events action opens the Events tab, the stale DashboardStackParamList Digest/GroupEvents entries are removed, and no Contacts-stack navigation to a promoted-and-removed route remains; tsc + nav/screen tests green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 04)

New symbols/files:
- `src/navigation/reset-intents.ts` → new `resetToDigestTab`
- `src/services/notifications/notification-nav.ts` → digest intent variant repointed to the Digest tab root
- Modified: `src/navigation/notification-gate.tsx` (digest branch applies resetToDigestTab, concurrency guard preserved), `src/screens/HomeScreen.tsx` (header shortcuts removed), `src/screens/dashboard-overflow-actions.ts` (`DashboardOverflowNavigation` gains a cross-tab `openEvents()` callback; the Group Events action calls it; `navigate` union narrowed to local routes), `src/navigation/types.ts` (stale DashboardStackParamList Digest/GroupEvents entries + the now-false Digest doc comment removed), `src/components/universal-fab-logic.ts` (getFocusedContactContext extended to Digest/Events tabs)
- Also modified (Task 4, cycle-3 LOW): `src/screens/HomeScreen.tsx` overflow caller (:1464-1470) supplies the required `openEvents` cross-tab callback
- Extended tests: `src/services/notifications/notification-nav.test.ts`, `src/navigation/notification-gate.test.tsx`, `src/components/universal-fab-logic.test.ts`, `src/screens/dashboard-overflow-actions.test.ts` (double gains `openEvents`; asserts the Group Events action calls it)

## Review feedback addressed (Plan 04)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| Notification-gate concurrency/stale-request guard must survive branching | MEDIUM (codex) | Incorporated — Task 1 preserves the guard for contact intents; digest bypasses the lookup; tested |
| Wrong gate test filename (.test.ts vs .test.tsx) | MEDIUM (codex) | Incorporated (cycle-6 correction) — the read_first (:87), Task-1 verify (:99), and the plan-level verify (:234) said `notification-gate.test.ts` while only `.test.tsx` exists; with `passWithNoTests` that silently skipped the concurrency/stale-request suite. All three now say `.test.tsx`; `<files>` already pinned it |
| S-05 Events terminology still incomplete (GroupEventsScreen title) | MEDIUM (codex) | Assigned to Plan 01 Task 2 (owns the Events-root relabel) — noted here to avoid a double-owner |
| FAB context narrower than the new stacks (Digest/Events Profile loses current contact) | LOW (codex) | Incorporated — Task 3 extends getFocusedContactContext to DigestTab/EventsTab |
| **[cycle 6] `universal-fab-logic.test.ts:152-162` hardcodes the removed `"BackupTab"` in its root-tab browse-surface list and misses the new Digest/Events surfaces** | LOW (proactive) | Incorporated — Task 3 owns the test (`files_modified` + `<files>`): the browse-surface array becomes the five-tab set (no `"BackupTab"`), asserting a bare tab root returns null for each, plus a case that a Profile inside DigestTab/EventsTab returns the contact context |
| Stale DashboardStackParamList Digest/GroupEvents entries (latent runtime crash) | MEDIUM (claude) | Incorporated — Task 4 removes them + repoints the Contacts overflow Group Events action (a live crash after Plan 01) |
| **[cycle 2] cross-tab overflow typing — a widened route-string union can't model the `{screen}` second param** | LOW (codex) | Incorporated — Task 4 adds a dedicated `openEvents()` callback to `DashboardOverflowNavigation` (the caller owns the `{screen}`-shaped cross-tab navigate); the `navigate` union is narrowed to local routes, not widened |
| **[cycle 2] stale Digest doc comment at types.ts:146-154** | LOW (codex) | Incorporated — Task 4 removes the now-false comment together with the `Digest: undefined` entry (bounded doc-sync) |
| **[cycle 3] Task 4 file ownership omits the overflow CALLER + its test — `openEvents` becoming required breaks HomeScreen:1464-1470 + dashboard-overflow-actions.test.ts:5-14 (an out-of-task compile/test break)** | LOW (codex) | Incorporated — Task 4 `<files>` + action step (3) + acceptance now own `src/screens/HomeScreen.tsx` (caller supplies the cross-tab `openEvents`) and `src/screens/dashboard-overflow-actions.test.ts` (double gains `openEvents`, asserts the Group Events action calls it) |
| **[cycle 4] cross-tab caller ("via parent/root navigator") under-specified — HomeScreen is in a stack, not the tab navigator, so it can't `navigate("EventsTab")` directly** | LOW (codex) | Incorporated — Task 4 step (3) specifies `navigation.getParent<BottomTabNavigationProp<TabParamList>>()` + `parent?.navigate("EventsTab", { screen: "GroupEvents" })`, and fails closed (no-op + dev warning, never throws) when `getParent()` is undefined |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS notification tap payload → nav intent | An externally-delivered notification payload crosses into a navigation decision; must be validated before it routes |
| FAB action → tab navigation | The FAB's hardcoded tab target must resolve; a stale target would silently no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-SPOOF | Tampering | digest notification payload → navigation | low | mitigate | `resolveNotificationNav` validates the payload; the digest intent is view-only and carries no contactId (nothing to forge — the existing V5 boundary is preserved); malformed payloads still route nowhere |
| T-38-FAB | Denial of Service (usability) | FAB target after tab rename | low | mitigate | Internal `DashboardTab` id preserved (Plan 01); the audit + universal-fab-logic test confirm actions still resolve |
| T-38-EGRESS | Information Disclosure | notification/FAB paths | high | accept | No network is introduced; routing is local; no user content leaves the device |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx vitest run src/services/notifications/notification-nav.test.ts src/navigation/notification-gate.test.tsx src/components/universal-fab-logic.test.ts src/screens` green.
- `npx tsc --noEmit` clean; `npm run check:colors` green.
- On-device (§S.3/§S.13): Digest notification opens the Digest tab root; FAB visible on Digest with Contacts actions; Contacts header decluttered.
</verification>

<success_criteria>
Digest notification routes to the Digest tab root; decay/birthday intents unchanged; Contacts header shortcuts removed with no clutter; global FAB confirmed visible on Digest with resolving Contacts actions — all automated gates green.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-04-SUMMARY.md` when done.
</output>
