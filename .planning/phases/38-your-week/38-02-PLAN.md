---
phase: 38-your-week
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/history/week-window.ts
  - src/db/migrations/030-your-week-period.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/backup/types.ts
  - src/backup/backup-schema.ts
  - src/db/your-week-read.ts
autonomous: false
requirements: [S-10, S-11, S-12]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Your Week supports two periods — Rolling 7 Days (default) and locale-aware Calendar Week — defined once and reused (dossier §I, D-05)"
    - "The Calendar Week window honours the device/locale first-day-of-week (expo-localization getCalendars firstWeekday), degrading to Sunday-first if unavailable — no Orbit-specific Sunday/Monday preference"
    - "The Your Week period preference persists in app_settings via migration 030 (additive, forward-only) with TARGET_VERSION bumped to 30 — a settings preference is not relationship-domain schema, so D-02 holds (D-02 governs a durable Digest snapshot/cache, which this is NOT)"
    - "yourWeekPeriod is wired through the REAL generic writer contract — WritableSettingsKey + AppSettingsRow + COLUMN_OF (`yourWeekPeriod: \"your_week_period\"`) + validateAppSettingsPatch — so `updateAppSettings({ yourWeekPeriod })` actually persists via `Object.keys(COLUMN_OF)` and `getAppSettings()` reads it back; there is NO `KEY_TO_COLUMN` symbol (the cycle-2 hallucination). This is what makes the D-09 toggle/Settings row and D-08 restore work, and a restored malformed value is rejected by validateAppSettingsPatch, not just the SQLite CHECK"
    - "A Group Event is a standalone activity entity: it counts as exactly ONE unit in the Events metric, the heatmap date→count, and day detail regardless of whether its participants are currently archived; the archived-contact exclusion filters only contact-identity aggregations (peopleReached, per-contact rows)"
    - "The yourWeekPeriod preference is PORTABLE: it is emitted in getPortableSettingsSnapshot and BACKUP_FORMAT_VERSION bumps 6→7, following the LANDED Phase 36 backup policy (which already emits the formerly-deferred keys) — NOT the stale declare-optional/emission-deferred comment pattern (owner ruling D-08)"
    - "A format-6 backup restores cleanly on the new build via a FORWARD_MIGRATIONS[6] step that defaults yourWeekPeriod to 'rolling7'; an export→parse→restore round-trip preserves a non-default yourWeekPeriod (D-08 portability proof)"
    - "Your Week metrics (People reached / Interactions / Events) and the date→count heatmap feed are app-wide read-only aggregates over existing tables (contacts, interactions, group_events); no new relationship-domain schema, no persisted Digest cache (D-02)"
    - "A Group Event counts as ONE record in Events and in day detail, and as ONE activity unit in the heatmap date→count feed — it is never expanded into fake per-participant interaction rows, and its N group-linked child interactions never saturate a heatmap cell N-fold (dossier §J, S.11; addresses codex HIGH heatmap group-event saturation)"
  artifacts:
    - path: src/services/history/week-window.ts
      provides: "Rolling-7 (reuse buildWindow('7days')) + new locale-aware Calendar Week HistoryWindow builder"
      contains: "week-window"
    - path: src/db/migrations/030-your-week-period.ts
      provides: "Additive app_settings.your_week_period column (TEXT NOT NULL DEFAULT 'rolling7' CHECK in rolling7/calendar_week); YOUR_WEEK_PERIOD_SCHEMA_VERSION = 30"
      contains: "your_week_period"
    - path: src/db/your-week-read.ts
      provides: "App-wide read-only Your Week aggregates: period metrics, date→count map, app-wide day detail; group-event dedup"
      contains: "your-week-read"
  key_links:
    - from: src/db/database.ts
      to: src/db/migrations/030-your-week-period.ts
      via: "MIGRATIONS list append + TARGET_VERSION = YOUR_WEEK_PERIOD_SCHEMA_VERSION (30)"
      pattern: "migration030|YOUR_WEEK_PERIOD_SCHEMA_VERSION"
    - from: src/db/app-settings-dao.ts
      to: src/db/migrations/030-your-week-period.ts
      via: "yourWeekPeriod read/write + PORTABLE emission mirrors the LANDED historyLens pattern (declared optional on the snapshot, EMITTED in getPortableSettingsSnapshot) over the your_week_period column — D-08 portable, format 7"
      pattern: "your_week_period|yourWeekPeriod"
---

<objective>
Build the only genuinely new data work in Phase 38: Your Week's **app-wide** (all-contacts) aggregation, the **locale-aware Calendar Week** window builder, and the **period preference** persisted via migration 030. Every existing history read in the codebase is per-contact; there is no app-wide daily-activity read, and no Your Week period preference exists anywhere in `src` today (RESEARCH — both verified against disk).

Plan against the dossier (D-01). Reuse Phase 32 window/bucket infrastructure and canonical interaction/Group-Event reads; do NOT reimplement them or invent parallel metric definitions (D-05, dossier §H/§J/§O). This is derive-only: a settings-preference column is NOT relationship-domain schema, so D-02 holds — but if any task here would add a relationship-domain table/column or a persisted Digest cache, STOP and flag the owner (D-02).

Purpose: give the Digest presentation (Plans 05/06) ready, tested read contracts + a period preference to consume.
Output: `week-window.ts`, `your-week-read.ts`, migration 030 + registry bump, the `yourWeekPeriod` settings key, and its portable-backup emission (BACKUP_FORMAT_VERSION 7).

**Owner ruling D-08 (supersedes RESEARCH Pattern 3):** `yourWeekPeriod` **is portable** and `BACKUP_FORMAT_VERSION` **bumps 6→7**. The RESEARCH "declare-optional / emission-deferred" Pattern 3 (RESEARCH §Pattern 3, citing app-settings-dao.ts:464-473) is **stale and superseded** — Phase 36 already landed and `getPortableSettingsSnapshot` now emits the formerly-deferred keys (`history_lens`/`history_cycle_count` at `app-settings-dao.ts:960` SELECT and `:1013` return; verified on disk). This plan follows the LANDED Phase 36 policy: emit `yourWeekPeriod`, add it to the restore allowlist, bump the format, and add the required forward migration + round-trip test. Do NOT copy the stale deferred-comment pattern.

**This plan is NOT autonomous:** it ships migration 030 AND the `BACKUP_FORMAT_VERSION` 6→7 bump — two coupled one-way doors (the migration is an irreversible all-users schema change with no remote DB access; once a build emits format-7 backups, older builds reject them). A single `checkpoint:decision` gates both before they are written (REVERSIBILITY_GATES on).

**Planner metric decision (documented; reversible — no checkpoint):** per dossier §H/§J and RESEARCH A2/Open-Q2, this plan sets **two distinct unit families** (they are NOT the same number, by design — dossier §H headline vs §J activity):
- **Headline metrics:** Events = COUNT(group_events) in the period; People reached = COUNT(DISTINCT contact_id) over qualifying interactions (group participants included per §J DERIVED); Interactions = COUNT of qualifying `interactions` rows **including** group-linked children (the canonical "logged interactions" definition — no parallel definition invented).
- **Activity unit (heatmap date→count + day detail):** a Group Event is **ONE** activity unit on its date (never its N child rows), and each standalone interaction (`group_event_id IS NULL`) is one unit — so an N-participant event contributes exactly 1 to its heatmap cell and 1 day-detail record, NOT N (dossier §J; resolves the codex HIGH heatmap-saturation + `buckets()`-feed mismatch: the DAO returns a pre-deduped `{d,n}` activity-unit count, and Plan 05 builds the heatmap Map directly from it — it does NOT re-count child rows through `buckets()`).
The headline Interactions-includes-children choice is a read-time definition, freely changeable later (not a one-way door); it is surfaced to the owner as a held decision, not gated. The activity-unit dedup is a dossier §J requirement, not a discretionary choice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; `git worktree` is hook-blocked). Commit in place on the current branch.
- Migrations are forward-only and irreversible in production (no remote DB access). Never edit a shipped migration; never assume a migration number — this plan verified head=029 on disk, so the new step is 030.
- Local-first: every new read is async-only (`getAllAsync`/`getFirstAsync`, never `...Sync`), no writer on a read path, no network. Static SQL strings; the only interpolated values are closed constants; bind period dates as `?` parameters.
- Dates via `formatLocalDate()` (`src/utils/dates.ts`), never `toISOString().split('T')[0]`; stored columns are already local wall-clock — truncate with bare `date(col)`, never re-run through `'localtime'`.
- Review the actual code on disk (files in `<read_first>`), not a diff. Before asserting any invariant about `interactions`/`group_events`, read every writer you rely on.
</execution_context>

<context>
@.planning/phases/38-your-week/38-CONTEXT.md
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Locale-aware week-window builder (Rolling 7 + Calendar Week)</name>
  <files>src/services/history/week-window.ts, src/services/history/week-window.test.ts</files>
  <read_first>
    - src/services/history/window.ts (HistoryWindow shape :37-48; WindowCell :27-35; buildWindow :149-158; buildSevenDays :90-100 clamped-to-today; weekdayOf getDay()===0 Sunday-first :73-77; addLocalDays :60-66; formatLocalDate discipline in the header)
    - src/services/history/buckets.ts (the HistoryWindow it consumes; buckets :52-70; heatmapLevel :77-86 — the cell shape week-window must emit unchanged)
    - src/utils/dates.ts (formatLocalDate :17)
    - device-region.ts / expo-localization usage in the repo (getLocales is used; getCalendars()[0].firstWeekday is the correct first-day-of-week API)
    - 38-PATTERNS.md "week-window.ts (NEW)" section; RESEARCH Pitfall 6 (Sunday-first hardcode); dossier §I
  </read_first>
  <behavior>
    - Rolling 7: `buildYourWeekWindow('rolling7', today)` returns the same 7-cell window as `buildWindow('7days', today, today)` — clamped to today, last 7 local days.
    - Calendar Week (Expo firstWeekday = 1 = Sunday): a mid-week `today` yields a 7-cell window whose start is the most recent Sunday ≤ today and whose cells run Sun..Sat; future cells after today carry `isFuture: true`.
    - Calendar Week (Expo firstWeekday = 2 = Monday): the same `today` yields a window starting the most recent Monday ≤ today, cells Mon..Sun (this is the load-bearing off-by-one test — a firstWeekday of 2 must start on Monday, NOT Tuesday).
    - firstWeekday unavailable/undefined/out of range → degrades to Sunday-first (documented default).
    - Every returned window is a valid `HistoryWindow` (lens/ref/start/end/cells); `today` is injected (never read from the wall clock) for deterministic node tests.
  </behavior>
  <action>
    Create `src/services/history/week-window.ts`. Export a pure builder `buildYourWeekWindow(period: 'rolling7' | 'calendar_week', today: string, firstWeekday?: number): HistoryWindow`. For `rolling7`, delegate to the existing `buildWindow('7days', today, today)` (do NOT re-implement rolling-7 geometry — reuse it). For `calendar_week`, compute the week start by walking back from `today` to the most recent day whose 0-based `getDay()` equals **`firstWeekday - 1`** — this conversion is mandatory and load-bearing: `expo-localization` `firstWeekday` is **1-based** (1=Sunday…7=Saturday) while `window.ts`'s `weekdayOf` uses JS `getDay()` which is **0-based** (0=Sunday, `window.ts:74-76`); comparing the two spaces directly is off by one. Default to Sunday (`getDay()===0`) when `firstWeekday` is undefined or outside 1..7. Emit 7 consecutive `WindowCell`s with `isFuture` flagged for dates after `today`, matching `window.ts`'s `WindowCell`/`HistoryWindow` shapes and date discipline (all arithmetic via local `Date` components / `formatLocalDate`, never UTC ISO slicing). Document the `firstWeekday - 1` mapping in a code comment. Keep the module pure (no DB/store/component import). Also export a thin resolver `resolveFirstWeekday()` that reads `expo-localization` `getCalendars()[0]?.firstWeekday` for the runtime caller, isolated from the pure builder so the builder stays node-testable.
  </action>
  <verify>
    <automated>npx vitest run src/services/history/week-window.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/history/week-window.ts` exists and exports `buildYourWeekWindow`
    - Its written geometry uses no `toISOString`: `grep -c 'toISOString' src/services/history/week-window.ts` returns 0
    - `src/services/history/week-window.test.ts` covers rolling7 == buildWindow('7days'), Sunday-first (firstWeekday=1) and Monday-first (firstWeekday=2 → window starts Monday) calendar weeks, and the undefined-firstWeekday Sunday fallback; the suite passes
    - The `firstWeekday - 1` conversion is documented in a comment: `grep -c 'firstWeekday' src/services/history/week-window.ts` returns at least 1
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Rolling-7 reuses the existing builder; Calendar Week honours locale first-day-of-week with a Sunday fallback; the builder emits the standard HistoryWindow shape; week-window tests + tsc green; committed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Confirm the STAGED migration-030 + backup-format-7 MECHANICS are correct before the irreversible write (Tasks 2–3). This is the one-way-door confirmation the reversibility gate requires — NOT a re-vote on D-08. D-08 (yourWeekPeriod portable; BACKUP_FORMAT_VERSION 6→7) is already owner-ruled and is NOT being re-litigated here; the only question is whether the concrete irreversible changes match the ratified spec (cycle-2 review: the prior checkpoint wrongly re-asked the settled D-08 decision — this one confirms mechanics only).</decision>
  <context>
    Two coupled one-way doors ship together in Tasks 2–3, and both are irreversible for ALL users with NO remote repair path — so a human must SEE the exact staged changes before they land (a reversibility gate, not a product decision):
    (1) Migration 030 — a single additive `ALTER TABLE app_settings ADD COLUMN your_week_period TEXT NOT NULL DEFAULT 'rolling7' CHECK(your_week_period IN ('rolling7','calendar_week'))`, forward-only, safe on a v1→v30 jump, a settings preference (NOT relationship-domain schema, so D-02 holds).
    (2) BACKUP_FORMAT_VERSION 6→7 — emit `yourWeekPeriod`, add it to the restore allowlist, add `FORWARD_MIGRATIONS[6]`; once a build emits format-7 backups older builds reject them. This follows the LANDED Phase 36 emit-and-bump policy, per D-08.
    The confirmation is a mechanics checklist: migration number 030 + TARGET_VERSION=30; the exact additive SQL with no shipped-migration edit; format 7 + FORWARD_MIGRATIONS[6] + allowlist; and that NO relationship-domain table/column and NO persisted Digest cache snuck in (that would be a D-02 stop-and-confirm scope escalation, not a proceed).
  </context>
  <options>
    <option id="confirmed">
      <name>Confirmed: the staged mechanics match the ratified spec — proceed through the one-way door</name>
      <pros>Ships the owner-ruled D-08 changes with the irreversible mechanics human-verified; matches migration 029's additive posture + the landed Phase 36 policy</pros>
      <cons>Both doors are irreversible once written; correctness rests on the mechanics being right</cons>
    </option>
    <option id="hold">
      <name>Hold: a mechanic is wrong (number/SQL/format/allowlist/scope) — fix before writing</name>
      <pros>Catches a wrong column shape, a mis-shaped format-7 wire, or an accidental relationship-domain column BEFORE it becomes permanent</pros>
      <cons>Blocks Your Week persistence + portability until corrected</cons>
    </option>
  </options>
  <resume-signal>Select "confirmed" if the staged mechanics match the ratified spec, or "hold" and name the wrong mechanic (this is not a re-vote on D-08).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 030 + registry bump + yourWeekPeriod settings key</name>
  <reversibility rating="one-way">app_settings.your_week_period is a forward-only, all-users, irreversible schema change with no remote DB access; a wrong column shape cannot be undone in production.</reversibility>
  <files>src/db/migrations/030-your-week-period.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/db/migrations/030-your-week-period.test.ts</files>
  <read_first>
    - src/db/migrations/029-ai-configuration.ts (the exact additive ALTER TABLE app_settings pattern :15-28; AI_CONFIGURATION_SCHEMA_VERSION = 29; migration only declares version + apply, the runner owns PRAGMA user_version)
    - src/db/database.ts (MIGRATIONS list :70-100; TARGET_VERSION = AI_CONFIGURATION_SCHEMA_VERSION :67; the import block :25-56 — note the registry lives at src/db/database.ts, NOT src/db/migrations/database.ts)
    - src/db/app-settings-dao.ts — THE FULL WRITER CONTRACT (verified on disk 2026-09-19; there is **NO `KEY_TO_COLUMN`** in this file — that symbol was the cycle-2 hallucination; the real map is `COLUMN_OF`). A writable settings key needs FOUR separate pieces plus the read mapping and the restore validator, mirroring how `historyLens`/`history_lens` threads through all of them:
        · `AppSettings` interface — required `historyLens: HistoryLens` :315 / `historyCycleCount` :317 (add a REQUIRED `yourWeekPeriod: YourWeekPeriod` here)
        · `PortableSettingsSnapshot` optional `historyLens?` :472-473, feeding `AppSettingsPatch = Partial<Omit<PortableSettingsSnapshot,"modifiedAt">>` :506-508 (add optional `yourWeekPeriod?: YourWeekPeriod` here so it enters the patch → writable/restorable)
        · `WritableSettingsKey` union :531 with `"historyLens"|"historyCycleCount"` :570-571 (add `| "yourWeekPeriod"`)
        · `AppSettingsRow` :587 with `history_lens: string` :619 / `history_cycle_count: number` :620 (add `your_week_period: string`)
        · `COLUMN_OF: Record<WritableSettingsKey,string>` :692 with `historyLens:"history_lens"` :731 / `historyCycleCount:"history_cycle_count"` :732 (add `yourWeekPeriod: "your_week_period"`) — this is the map the generic partial-UPDATE writer iterates via `Object.keys(COLUMN_OF)` :1486-1489, so a key ABSENT here never reaches SQL
        · `validateAppSettingsPatch` :1270 with the `patch.historyLens`/`patch.historyCycleCount` runtime validators :1341-1345 (add a `patch.yourWeekPeriod` enum validator — define an `assertYourWeekPeriod` helper alongside the `assertHistoryLens` idiom)
        · `getAppSettings` SELECT (`history_lens, history_cycle_count,` :776) + return mapping (`historyLens: row.history_lens as HistoryLens` :841-842) — add `your_week_period` to the SELECT and `yourWeekPeriod: row.your_week_period as YourWeekPeriod` to the return
    - src/backup/restore-apply.ts — restore CASTS external manifest JSON to `AppSettingsPatch` at :1589-1592 (`Object.entries(manifest.appSettings).filter(...) as AppSettingsPatch`) and calls `updateAppSettingsCore` :1593, which runs `validateAppSettingsPatch(patch)` :1479. So the enum validator above is the ONLY runtime guard on a restored `yourWeekPeriod` (the TS union is bypassed by the cast) — omitting it defers a malformed format-7 value to the SQLite CHECK instead of rejecting it at the DAO boundary
    - src/backup/types.ts (BACKUP_FORMAT_VERSION = 6 :14 — must NOT change in this task)
    - src/db/migrations/runner.ts + full-chain.test.ts (how migrations are proven; the 029 test as the analog test shape)
    - 38-PATTERNS.md "030-your-week-period.ts", "database.ts (MODIFY — registry)", "app-settings-dao.ts (MODIFY)" sections; RESEARCH Pattern 3
  </read_first>
  <behavior>
    - Applying migrations through 030 on a fresh DB yields user_version 30 and an app_settings row with your_week_period = 'rolling7'.
    - A **v1→v30 jump** applies cleanly (migrations 2–29 run first, then 030) — NOT a standalone 030 invocation (migration 030 depends on the app_settings table existing from earlier migrations). Construct the v1 fixture EXPLICITLY: apply migration 001 directly to a fresh DB (the existing `full-chain.test.ts:15-22` starts at v0 and exports NO reusable v1 runner), then invoke `runMigrations` to advance v1→`TARGET_VERSION` (30). Prove it through the runner, not by calling `migration030.apply` in isolation.
    - `updateAppSettings({ yourWeekPeriod: 'calendar_week' })` actually PERSISTS (the generic `Object.keys(COLUMN_OF)` writer emits `your_week_period = ?`) and a subsequent `getAppSettings()` reads back `yourWeekPeriod: 'calendar_week'` — the full write→reload round-trip works (this is the regression the cycle-2 defect would have broken).
    - An invalid `yourWeekPeriod` (e.g. a restored `'weekly'`) is rejected by `validateAppSettingsPatch` at the DAO boundary (not merely deferred to the SQLite CHECK) — both a direct `updateAppSettings` call and a restore-cast patch hit the validator.
    - `getPortableSettingsSnapshot()` typechecks after this task with `yourWeekPeriod` declared OPTIONAL on `PortableSettingsSnapshot` but not yet emitted in the SELECT/return (emission + the format bump land atomically in Task 3 so the export never emits a non-allowlisted key).
  </behavior>
  <action>
    Create `src/db/migrations/030-your-week-period.ts` exporting `YOUR_WEEK_PERIOD_SCHEMA_VERSION = 30` and `migration030: Migration` whose `apply` runs a single additive `ALTER TABLE app_settings ADD COLUMN your_week_period TEXT NOT NULL DEFAULT 'rolling7' CHECK(your_week_period IN ('rolling7','calendar_week'))`, mirroring migration 029's structure (declare `version` + `apply` only; the runner owns `PRAGMA user_version`). In `src/db/database.ts`: import `{ YOUR_WEEK_PERIOD_SCHEMA_VERSION, migration030 }`, append `migration030` to the `MIGRATIONS` list after `migration029`, and repoint `export const TARGET_VERSION = YOUR_WEEK_PERIOD_SCHEMA_VERSION` (so TARGET_VERSION and the head migration constant never drift). In `src/db/app-settings-dao.ts`, define a `YourWeekPeriod = 'rolling7' | 'calendar_week'` type and wire `yourWeekPeriod` through the FULL live writer contract (there is NO `KEY_TO_COLUMN` in this file — the cycle-2 review confirmed that symbol does not exist; the generic writer iterates `Object.keys(COLUMN_OF)` at `:1486-1489`). Add ALL of the following, each mirroring how `historyLens`/`history_lens` is already threaded (addresses codex HIGH — Plan 02 writer wiring):
    (a) `AppSettings` interface (`:315-317` region): a REQUIRED `yourWeekPeriod: YourWeekPeriod` (the column is NOT NULL DEFAULT so it is always present — Plan 05's toggle reads it);
    (b) `PortableSettingsSnapshot` (`:472-473` region): an OPTIONAL `yourWeekPeriod?: YourWeekPeriod`, so it enters `AppSettingsPatch = Partial<Omit<PortableSettingsSnapshot,"modifiedAt">>` (`:506-508`) — this is what makes it writable via `updateAppSettings` and restorable via restore-apply;
    (c) `WritableSettingsKey` union (`:531`, alongside `"historyLens"|"historyCycleCount"` `:570-571`): add `| "yourWeekPeriod"`;
    (d) `AppSettingsRow` (`:587`, alongside `history_lens`/`history_cycle_count` `:619-620`): add `your_week_period: string`;
    (e) `COLUMN_OF` (`:692`, alongside `historyLens:"history_lens"`/`historyCycleCount:"history_cycle_count"` `:731-732`): add `yourWeekPeriod: "your_week_period"` — WITHOUT this entry the generic `Object.keys(COLUMN_OF)` writer never emits the column and `updateAppSettings({ yourWeekPeriod })` silently no-ops (this is the exact defect the review caught);
    (f) `validateAppSettingsPatch` (`:1270`, alongside the `patch.historyLens` validators `:1341-1345`): add and **EXPORT** an `assertYourWeekPeriod(label, value)` helper (throw unless the value is `'rolling7'|'calendar_week'`, mirroring the `assertHistoryLens`/`assertDefaultInteractionChannel` idiom) and invoke it on `patch.yourWeekPeriod` — restore casts external JSON to `AppSettingsPatch` (restore-apply.ts:1589-1592) and reaches this validator via `updateAppSettingsCore` (`:1479`). Export it because Task 3 reuses the SAME validator at the backup PARSE boundary (`assertPortableSettings`), exactly as `assertDefaultInteractionChannel`/`assertMessageMode` are shared between the DAO and `backup-schema.ts` (addresses cycle-3 Plan 02 MEDIUM — validate at parse, not only at the DAO);
    (g) `getAppSettings` SELECT (`:776`) + return mapping (`:841-842`): add `your_week_period` to the SELECT column list and `yourWeekPeriod: row.your_week_period as YourWeekPeriod` to the return.
    In THIS task do NOT yet add it to the `getPortableSettingsSnapshot` SELECT/return and do NOT change `BACKUP_FORMAT_VERSION` — emission + the bump are Task 3 and must ship in one commit (an emitted-but-not-allowlisted key would fail export validation). This is NOT the stale "emission deferred forever" comment pattern (that precedent is spent per D-08); it is a within-plan task boundary. Write the migration-030 test (additive column + default + version bump) driven through the **full migration runner from a v1 fixture** (the analog is `full-chain.test.ts`, not a standalone 029/030 apply) and extend the app-settings-dao test for read/write of `yourWeekPeriod`.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/030-your-week-period.test.ts src/db/app-settings-dao.test.ts src/db/migrations/full-chain.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/030-your-week-period.ts` exists and exports `YOUR_WEEK_PERIOD_SCHEMA_VERSION` and `migration030`
    - `grep -c 'migration030' src/db/database.ts` returns at least 2 (import + list entry)
    - `grep -c 'TARGET_VERSION = YOUR_WEEK_PERIOD_SCHEMA_VERSION' src/db/database.ts` returns 1
    - `yourWeekPeriod` is a REQUIRED field on `AppSettings` and OPTIONAL on `PortableSettingsSnapshot`, is in the `WritableSettingsKey` union, and `COLUMN_OF` maps `yourWeekPeriod` → `"your_week_period"`: `grep -c 'yourWeekPeriod' src/db/app-settings-dao.ts` returns at least 5 and `grep -c 'yourWeekPeriod: "your_week_period"' src/db/app-settings-dao.ts` returns 1
    - `validateAppSettingsPatch` guards `yourWeekPeriod`: `grep -c 'yourWeekPeriod' src/db/app-settings-dao.ts` includes the validator invocation, and a test proves an invalid restored value throws at the DAO boundary
    - The writer wires to the real generic map: `grep -c 'COLUMN_OF' src/db/app-settings-dao.ts` returns at least 3 and the `yourWeekPeriod: "your_week_period"` entry is present in it (the persist→reload round-trip test above proves the write actually reaches SQL — the real proof the wiring is correct)
    - A test asserts `updateAppSettings({ yourWeekPeriod: 'calendar_week' })` then `getAppSettings()` returns `'calendar_week'` (the persist→reload round-trip)
    - At THIS task (emission ships in Task 3) backup format is still 6 and the snapshot SELECT does not yet contain `your_week_period` — Task 3 flips both
    - The migration test drives the FULL runner from an EXPLICITLY-constructed v1 fixture (apply migration 001, then `runMigrations` to 30) — not a standalone `migration030.apply`; migration + settings + full-chain tests pass; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Migration 030 adds the additive settings column, TARGET_VERSION is 30, the yourWeekPeriod key is a required AppSettings field + writable/restorable via the patch (declared optional on the snapshot, emitted in Task 3); the migration is proven via the full v1→v30 runner; migration/settings/full-chain tests + tsc green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backup portability — emit yourWeekPeriod + bump BACKUP_FORMAT_VERSION 6→7 (D-08)</name>
  <reversibility rating="one-way">Once a build emits format-7 backups, older builds reject them (parseBackupManifest fails when version &gt; MAX_SUPPORTED). The wire shape cannot be un-shipped. Gated by the Task-2/3 blocking-human mechanics-verification checkpoint (a non-decision gate — D-08 is already ruled).</reversibility>
  <files>src/db/app-settings-dao.ts, src/backup/types.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/db/app-settings-dao.ts (getPortableSettingsSnapshot: the Pick&lt;AppSettingsRow,...&gt; union :891-948, the SELECT :950-969, the return map :978-1027 — copy the `history_lens` treatment: it is in the union, the SELECT at :960, and the return at :1013; the stale "EMISSION DEFERRED / do NOT bump" comments at :435-489 describe a state Phase 36 already superseded — step (4) CORRECTS them, do not merely avoid them)
    - src/backup/types.ts (BACKUP_FORMAT_VERSION = 6 :14 — the single source; container BACKUP_ENVELOPE_VERSION is independent, leave it)
    - src/backup/backup-schema.ts (MAX_SUPPORTED_BACKUP_FORMAT_VERSION = BACKUP_FORMAT_VERSION :24 — auto-follows the bump; PORTABLE_SETTINGS_KEYS Set :157-244 — the restore allowlist of camelCase manifest keys, note historyLens is already in it :215; the "NOT emitted... Phase 36 scope" comments at :180-215 are now stale (those keys ARE emitted post-Phase-36) — step (4) corrects them; FORWARD_MIGRATIONS :33 with its default-inject idiom `phoneRegionOverride ?? null`; the parse loop :987-1000 which REQUIRES a FORWARD_MIGRATIONS[from] for every step up to MAX, so bumping to 7 makes a FORWARD_MIGRATIONS[6] mandatory or parseBackupManifest hard-fails)
    - src/backup/backup-schema.ts `assertPortableSettings` (:267-334) — the PARSE-boundary validator that strictly rejects malformed portable settings BEFORE returning the manifest; note the reused-DAO-validator try/catch pattern for `defaultInteractionChannel` (:306-310) and `defaultMessageMode` (:325-326). `yourWeekPeriod` MUST get the SAME treatment: a `?? 'rolling7'` default in FORWARD_MIGRATIONS[6] only FILLS a missing value — it does NOT reject a crafted `yourWeekPeriod:"weekly"`, which would then survive parse and only be caught later at the DAO/CHECK (cycle-3 Plan 02 MEDIUM)
    - src/backup/export-manifest.ts (:238 `appSettings: { ...portable, sunContactUid }` — the whole portable snapshot is spread into the manifest, so emission in getPortableSettingsSnapshot IS the export path; :232 backupFormatVersion from the constant)
    - src/backup/restore-apply.ts (:1588-1596 builds an `AppSettingsPatch` from `manifest.appSettings` entries and calls `updateAppSettingsCore` — so once yourWeekPeriod is in AppSettingsPatch (Task 2) and the allowlist, restore applies it with no restore-apply code change)
    - src/backup/backup-schema.test.ts + src/backup/export-manifest.test.ts (the existing version=6 assertions to update to 7; the round-trip idiom to mirror)
    - the most recent FORWARD_MIGRATIONS entry (the Phase 37.1 5→6 step) as the analog for a minimal additive format bump
  </read_first>
  <behavior>
    - `BACKUP_FORMAT_VERSION === 7`; `MAX_SUPPORTED_BACKUP_FORMAT_VERSION` follows it automatically.
    - `getPortableSettingsSnapshot()` returns `yourWeekPeriod`, and an exported manifest's `appSettings` carries it (via the export spread).
    - Round-trip: an export with `yourWeekPeriod: 'calendar_week'` → `parseBackupManifest` → `restore-apply` writes `your_week_period = 'calendar_week'` back into app_settings.
    - A format-6 manifest (no yourWeekPeriod) → `parseBackupManifest` runs `FORWARD_MIGRATIONS[6]`, yielding backupFormatVersion 7 with `yourWeekPeriod` defaulted to `'rolling7'`, and validates (the allowlist now accepts the key).
    - A manifest (format 6 OR format 7) carrying `yourWeekPeriod:"weekly"` FAILS `parseBackupManifest` at the `assertPortableSettings` boundary via the reused `assertYourWeekPeriod` validator — it never reaches the DAO or the SQLite CHECK.
    - A manifest with backupFormatVersion &gt; 7 still fails with `UPDATE_FIRST_MESSAGE`.
  </behavior>
  <action>
    Make `yourWeekPeriod` portable per owner ruling D-08 (follow the LANDED Phase 36 emit-and-bump policy, NOT the stale deferred pattern):
    (1) `src/db/app-settings-dao.ts`: add `your_week_period` to the `getPortableSettingsSnapshot` `Pick<AppSettingsRow, ...>` union and to its SELECT column list (next to `history_lens, history_cycle_count`), and add `yourWeekPeriod: row.your_week_period as YourWeekPeriod` to its return map — mirroring the `history_lens` emission at `:960`/`:1013` exactly.
    (2) `src/backup/types.ts`: bump `BACKUP_FORMAT_VERSION` from 6 to 7 (portable JSON wire shape changed — this is exactly when the constant bumps).
    (3) `src/backup/backup-schema.ts`: add `"yourWeekPeriod"` (the camelCase MANIFEST key, matching AppSettingsPatch — NOT the snake_case column) to `PORTABLE_SETTINGS_KEYS`; and add a `FORWARD_MIGRATIONS[6]` entry that returns `{ ...manifest, backupFormatVersion: 7, appSettings: { ...settings, yourWeekPeriod: settings.yourWeekPeriod ?? 'rolling7' } }` — mirroring the `phoneRegionOverride ?? null` default-inject shape in FORWARD_MIGRATIONS[1] so an older format-6 backup validates against the format-7 allowlist. Do NOT edit the shipped FORWARD_MIGRATIONS[1..5] entries (never edit a shipped migration).
    (3a) **Validate `yourWeekPeriod` at the PARSE boundary (addresses cycle-3 Plan 02 MEDIUM):** in `src/backup/backup-schema.ts` `assertPortableSettings` (:267-334), add a `try { if (settings.yourWeekPeriod !== undefined) assertYourWeekPeriod("yourWeekPeriod", settings.yourWeekPeriod); } catch { fail("appSettings has an invalid Your Week period"); }` block, importing the EXPORTED `assertYourWeekPeriod` from `app-settings-dao.ts` (Task 2 step (f)) — mirroring the SAME reused-DAO-validator pattern that guards `defaultInteractionChannel` (:306-310) and `defaultMessageMode` (:325-326). This rejects a crafted `yourWeekPeriod:"weekly"` at parse time; the FORWARD_MIGRATIONS[6] `?? 'rolling7'` default only fills a MISSING value and must NOT be relied on to reject a malformed one.
    (4) **Correct the now-false "emission-deferred / Phase 36 scope" comments adjacent to this work (addresses claude LOW — stale confident comments are the exact landmine CLAUDE.md warns about; do NOT merely decline to follow them, FIX them):** the comment blocks at `src/db/app-settings-dao.ts:435-489` and `src/backup/backup-schema.ts:180-215` state that the theme/dashboard/history/channel/message keys are "EMISSION DEFERRED" and instruct "do NOT add these to getPortableSettingsSnapshot / do NOT bump BACKUP_FORMAT_VERSION". Phase 36 has LANDED and `getPortableSettingsSnapshot` now emits every one of those key families (verified: `history_lens` :960/:1013, `theme_package` :995, `dashboard_view_mode` :1002, `default_interaction_channel` :1015, `default_message_mode` :1019), so those instructions are false. Update the wording so it reflects the landed reality (the deferred-emission precedent is spent; these keys are emitted as of Phase 36) — this is a bounded doc-sync of exactly the two comment regions adjacent to the new `yourWeekPeriod` field, NOT a repository-wide terminology sweep (§Q). Ensure the NEW `yourWeekPeriod` field carries no "emission deferred / do NOT bump" comment (it IS emitted and DOES bump, per D-08); do not copy the stale pattern.
    (5) Tests: add a round-trip test (export → `parseBackupManifest` → `restore-apply`) proving a non-default `yourWeekPeriod` survives; add a v6→v7 forward-migration test (a format-6 fixture parses to format 7 with `yourWeekPeriod` defaulted `'rolling7'` and validates); **add PARSE-boundary rejection tests (cycle-3 MEDIUM): a crafted format-7 manifest with `yourWeekPeriod:"weekly"` FAILS `parseBackupManifest`, AND a crafted format-6 manifest carrying `yourWeekPeriod:"weekly"` (which the FORWARD_MIGRATIONS[6] `??` default does NOT overwrite) ALSO fails `parseBackupManifest` — not merely the DAO write**; update the existing export-manifest/backup-schema version assertions from 6 to 7. Do NOT touch AiService or any network path (backup is local-only).
  </action>
  <verify>
    <automated>npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts src/db/app-settings-dao.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Format bumped: `grep -c 'BACKUP_FORMAT_VERSION = 7' src/backup/types.ts` returns 1 and `! grep -q 'BACKUP_FORMAT_VERSION = 6' src/backup/types.ts`
    - Emitted in the snapshot: the `getPortableSettingsSnapshot` SELECT contains `your_week_period` and the return map contains `yourWeekPeriod` (`grep -c 'your_week_period' src/db/app-settings-dao.ts` returns at least 4 now)
    - Restore allowlisted: `grep -c '"yourWeekPeriod"' src/backup/backup-schema.ts` returns at least 1
    - A FORWARD_MIGRATIONS entry exists for from=6 (the parse loop would otherwise hard-fail): a `6:` key is present in the FORWARD_MIGRATIONS object
    - The stale emission-deferred comments were corrected (not just avoided): the `app-settings-dao.ts:435-489` and `backup-schema.ts:180-215` regions no longer instruct "do NOT add these to getPortableSettingsSnapshot / do NOT bump BACKUP_FORMAT_VERSION" as current guidance, and the new `yourWeekPeriod` field carries no "emission deferred" comment
    - `assertPortableSettings` guards `yourWeekPeriod` at the parse boundary: `grep -c 'assertYourWeekPeriod' src/backup/backup-schema.ts` returns at least 1, and parser tests prove a `yourWeekPeriod:"weekly"` manifest (both format 6 and format 7) FAILS `parseBackupManifest`, not merely the DAO write
    - The round-trip test and the v6→v7 forward-migration test pass; the updated version=7 assertions pass; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>yourWeekPeriod is emitted in getPortableSettingsSnapshot, BACKUP_FORMAT_VERSION is 7, the restore allowlist accepts it, a FORWARD_MIGRATIONS[6] defaults it for older backups, and round-trip + v6→v7 tests are green (D-08 satisfied); committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: your-week-read.ts — app-wide period metrics, date→count, day detail</name>
  <files>src/db/your-week-read.ts, src/db/your-week-read.test.ts</files>
  <read_first>
    - src/db/digest-read.ts (the read-only DAO posture to copy — header contract :1-42; readRetrospective :86-100 COUNT(DISTINCT)/MAX idiom; readOverlooked :109-124; timezone rule :30-35)
    - src/db/status.ts (local-wall-clock discipline; bare date(col) for stored columns)
    - src/db/migrations/026-group-events-schema.ts (group_events table + interactions.group_event_id — the group seam that must not double-count)
    - src/db/group-events-read.ts (existing Group Event reads — the Events-metric source)
    - src/services/history/buckets.ts (BucketInteraction shape :17-21; buckets consumes {occurredAt}; the count-only invariant :6-9 — note it warns the group seam was inert in Phase 32; the NEW app-wide SQL must handle group_event_id explicitly)
    - 38-PATTERNS.md "your-week-read.ts" section; RESEARCH Code Example "Your Week app-wide date→count", Pitfall 3 (group double-count), Open Q2/Q3
  </read_first>
  <behavior>
    - **Headline** `readYourWeekMetrics(exec, start, end)` (dates local YYYY-MM-DD, bound as `?`): peopleReached = COUNT(DISTINCT contact_id) over non-archived contacts' qualifying interactions in [start,end]; interactions = COUNT of those interaction rows (group-linked children included per the documented metric decision); events = COUNT(group_events) in [start,end].
    - **Activity-unit** `readYourWeekDateCounts(exec, start, end)` returns rows `{ d, n }`, one per active local date, where `n` is the count of Your Week ACTIVITY UNITS on `d` = (standalone interactions with `group_event_id IS NULL` on `d`) + (distinct `group_events` on `d`). A Group Event contributes exactly ONE unit on its date, NEVER its N child rows — so an N-participant event does not saturate the heatmap cell N-fold (this is the heatmap feed; it is NOT the raw child-inclusive interaction count). This shape is consumed by Plan 05 by building the date→count Map DIRECTLY from these `{d,n}` rows — it is NOT passed through `buckets()` (which counts one-per-row and would collapse `n`).
    - `readYourWeekDay(exec, date)` returns the app-wide activity for one local date, with a Group Event represented as ONE record (never one row per participant), and each standalone interaction row carrying enough identity (contact name) for an app-wide day list.
    - Archived contacts are excluded from CONTACT-IDENTITY aggregations everywhere (`c.archived_at IS NULL`): peopleReached counts only non-archived `contact_id`, and standalone day-detail rows for archived contacts are excluded; no interaction outside the window is counted; no double-count of a group event as N units in the date-count, the Events metric, or day detail.
    - **Archived-participant rule for the group-event activity unit (planner decision — apply UNIFORMLY across metric, date-count, and day-detail):** a `group_events` row is a STANDALONE logged activity entity (one parent row; the table has no archived state and no direct contact identity — migration 026), so it ALWAYS counts as exactly ONE unit — in `events` (headline), in `readYourWeekDateCounts` `n`, and as one `readYourWeekDay` record — REGARDLESS of whether its participants are currently archived. Archiving a participant contact later must not retroactively erase the event from Your Week. The `archived_at IS NULL` exclusion is a CONTACT-identity filter (it governs peopleReached and per-contact standalone rows), NOT a filter on the event entity. Consequence: an event whose participants are ALL archived still contributes 1 to `events` and 1 activity unit and 1 day-detail record, but 0 to peopleReached. Do NOT join group-event counting to participant archival state.
  </behavior>
  <action>
    Create `src/db/your-week-read.ts` as a pure read-only DAO copying the `digest-read.ts` header contract in spirit (static SQL string, async-only, local-wall-clock discipline, no writer/no network). Implement `readYourWeekMetrics`, `readYourWeekDateCounts`, and `readYourWeekDay`, binding the period start/end (and single-date) as `?` parameters (unlike digest-read's interpolated integer modifier, these bounds are dates → bind them). Follow the documented two-unit-family decision from this plan's objective:
    - Headline `readYourWeekMetrics`: Events = COUNT(group_events); People reached = COUNT(DISTINCT contact_id); Interactions = COUNT of qualifying interaction rows including group-linked children.
    - Activity-unit `readYourWeekDateCounts`: `n` per date = (standalone interactions `group_event_id IS NULL`) + (distinct `group_events`) — e.g. a UNION-ALL of standalone-interaction dates and group-event dates, `GROUP BY date`. A group event contributes ONE unit, not its child rows. This is the heatmap feed and must NOT be the raw child-inclusive interaction count.
    - `readYourWeekDay`: one record per group event + one per standalone interaction, each standalone row carrying contact identity.
    Apply the archived-participant rule from the behavior block: count `group_events` as a standalone entity (do NOT join to participant archival) so an all-archived-participant event still counts once in `events`, the date-count, and day detail; the `archived_at IS NULL` predicate constrains only peopleReached and per-contact standalone rows. Truncate stored `occurred_at`/`group_events.occurred_at` with bare `date(col)` (already local), only `now` gets `'localtime'`. Export the row types. Write `src/db/your-week-read.test.ts` proving: headline metric counts over a fixture window; the date→count map keys real dates only AND a group event with N (≥2) participants contributes exactly ONE unit to its date-count (NOT N — the heatmap-saturation regression); a group event yields ONE Events count and ONE day-detail record; archived contacts excluded from peopleReached/standalone rows; **and an event whose participants are ALL archived still counts as exactly ONE unit in the Events metric, the date-count, and day-detail (but adds 0 to peopleReached) — the archived-participant rule, verified across all three surfaces.**
  </action>
  <verify>
    <automated>npx vitest run src/db/your-week-read.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/your-week-read.ts` exists and exports `readYourWeekMetrics`, `readYourWeekDateCounts`, `readYourWeekDay`
    - No UTC slicing and no sync executor: `grep -c 'toISOString' src/db/your-week-read.ts` returns 0 and `grep -c 'getAllSync\|getFirstSync\|execSync' src/db/your-week-read.ts` returns 0
    - `src/db/your-week-read.test.ts` includes the heatmap-saturation case (a fixture group event with ≥2 participants contributes exactly ONE unit to `readYourWeekDateCounts` for its date, NOT N), the group-event-no-double-count case (Events == 1 and one day-detail record), an archived-excluded case (archived contact absent from peopleReached/standalone rows), AND the all-archived-participant-event case (event still counts 1 in events + date-count + day-detail, 0 in peopleReached); the suite passes
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Your Week app-wide headline metrics + a group-deduped activity-unit date-count feed + day detail read read-only from existing tables with correct group-event dedup and archived exclusion; your-week-read tests (incl. the N-participant→1-unit saturation regression) + tsc green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02)

New symbols/files:
- `src/services/history/week-window.ts` → `buildYourWeekWindow`, `resolveFirstWeekday`
- `src/db/migrations/030-your-week-period.ts` → `YOUR_WEEK_PERIOD_SCHEMA_VERSION`, `migration030`
- `src/db/your-week-read.ts` → `readYourWeekMetrics`, `readYourWeekDateCounts`, `readYourWeekDay` (+ row types)
- New `app_settings` column `your_week_period`; new `YourWeekPeriod` type; new `AppSettings` (required) / `PortableSettingsSnapshot` (optional, emitted) field `yourWeekPeriod`; new EXPORTED `assertYourWeekPeriod` validator (reused by `backup-schema.ts` at the parse boundary)
- `BACKUP_FORMAT_VERSION` becomes 7; new `FORWARD_MIGRATIONS[6]` entry; `"yourWeekPeriod"` added to `PORTABLE_SETTINGS_KEYS`
- Modified: `src/db/database.ts` (MIGRATIONS + TARGET_VERSION), `src/db/app-settings-dao.ts`, `src/backup/types.ts`, `src/backup/backup-schema.ts`
- Test files: `src/services/history/week-window.test.ts`, `src/db/migrations/030-your-week-period.test.ts`, `src/db/your-week-read.test.ts` (+ extended backup-schema/export-manifest/app-settings-dao tests)

## Review feedback addressed (Plan 02)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| Backup "emission deferred" precedent is stale; yourWeekPeriod portability undecided | HIGH / OWNER-ESCALATION (codex) | OWNER RULED D-08 — Task 3 emits yourWeekPeriod + bumps BACKUP_FORMAT_VERSION 6→7 + allowlist + FORWARD_MIGRATIONS[6] + round-trip/v6→v7 tests |
| Heatmap group-event saturation (readYourWeekDateCounts counts child rows) | HIGH (codex #2) | Incorporated — Task 4 date→count is a group-deduped ACTIVITY-UNIT count (group=1), with an N-participant→1-unit regression test |
| `buckets()` feed-contract incompatibility ({occurredAt} vs {d,n}) | HIGH (codex #3) | Incorporated — DAO returns `{d,n}`; Plan 05 builds the Map directly (not via buckets); documented in Task 4 behavior + Plan 05 |
| Conflates three aggregation units | MEDIUM (codex) | Incorporated — objective now defines headline metrics vs activity unit as two explicit families |
| Migration test needs a v1 fixture via the full runner | MEDIUM (codex) | Incorporated — Task 2 drives the full-chain runner from a v1 fixture, not a standalone apply |
| firstWeekday index-conversion off-by-one | MEDIUM (claude) | Incorporated — Task 1 documents `firstWeekday − 1` and tests firstWeekday=2 → Monday start |
| **[cycle 2] Task 2 wires yourWeekPeriod into a NONEXISTENT hallucinated column-map — never reaches the live `Object.keys(COLUMN_OF)` writer, breaking D-09 toggle + D-08 restore** | HIGH (codex, disk-confirmed) | Incorporated — Task 2 rewritten to the real writer contract: `WritableSettingsKey` :531, `AppSettingsRow` :587, `COLUMN_OF` :692 (`yourWeekPeriod:"your_week_period"`), `validateAppSettingsPatch` :1270, getAppSettings SELECT/mapping; the hallucinated map is gone; acceptance adds a persist→reload round-trip + a positive `COLUMN_OF`-wiring assertion + a validator-rejection test (the real proofs the write reaches SQL) |
| **[cycle 2] restore casts external JSON to AppSettingsPatch, bypassing the TS union** | HIGH sub-finding (codex) | Incorporated — Task 2 adds `assertYourWeekPeriod` to `validateAppSettingsPatch` (reached via restore→updateAppSettingsCore :1479), the only runtime guard on a restored value; tested |
| **[cycle 2] D-08 checkpoint re-asks a settled decision (drip-feed)** | MEDIUM (codex) | Incorporated — converted `checkpoint:decision` (proceed/hold on D-08) → `checkpoint:human-verify gate="blocking-human"` mechanics gate that confirms migration 030 SQL / format 7 / FORWARD_MIGRATIONS[6] mechanics, explicitly NOT a re-vote on D-08 |
| **[cycle 2] archived-participant rule undefined for the group-event activity unit** | MEDIUM (codex) | Incorporated — Task 4 defines the rule (a group event is a standalone entity → always counts 1 in metric/date-count/day-detail regardless of participant archival; `archived_at IS NULL` filters only contact-identity aggregations) + an all-archived-participant test across all three surfaces |
| **[cycle 2] now-false "emission-deferred / Phase 36 scope" comments (app-settings-dao :435-489, backup-schema :180-215)** | LOW (claude) | Incorporated — Task 3 step (4) CORRECTS them (not just avoids), and the new yourWeekPeriod field carries no stale deferred comment |
| **[cycle 3] `yourWeekPeriod` validated only at the DAO, not at the backup PARSE boundary — a crafted `yourWeekPeriod:"weekly"` format-6 manifest survives parse (the `??` default doesn't reject it)** | MEDIUM (codex) | Incorporated — Task 2 EXPORTS `assertYourWeekPeriod`; Task 3 step (3a) invokes it in `assertPortableSettings` (mirroring the channel/message-mode reused-validator pattern) + parser tests reject a `yourWeekPeriod:"weekly"` format-6 AND format-7 manifest at `parseBackupManifest` |
| **[cycle 3] v1-jump fixture construction unspecified (full-chain starts at v0, no reusable v1 runner)** | LOW (codex) | Incorporated — Task 2 behavior/action/acceptance specify: apply migration 001 directly, then `runMigrations` to 30 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → on-device SQLite | An irreversible, all-users schema mutation crosses here; there is no remote repair path |
| period bounds → Your Week SQL | Date bounds/period value reach the read queries; must be bound parameters/closed constants, never interpolated free-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-DB | Tampering | migration 030 data integrity (irreversible, all-users) | high | mitigate | Additive-with-DEFAULT + CHECK, forward-only, no shipped-migration edits; proven by the 030 migration test + full-chain test on a fresh DB and a v1→v30 jump; gated by the pre-migration `checkpoint:decision` |
| T-38-FMT | Tampering | backup format v6→v7 wire shape (D-08) | high | mitigate | Bump only the format constant; add `yourWeekPeriod` to the restore allowlist + a `FORWARD_MIGRATIONS[6]` default-inject so older format-6 backups still restore; never edit a shipped FORWARD_MIGRATIONS entry; round-trip + v6→v7 tests prove no data loss; gated by the same `checkpoint:decision` |
| T-38-SQLI | Tampering | period value/date bounds in Your Week reads | medium | mitigate | Bind start/end/date as `?` parameters; keep the digest-read static-string posture; no user free-text reaches any query |
| T-38-DEDUP | Information Disclosure | group-event / archived aggregation | medium | mitigate | `archived_at IS NULL` everywhere; Group Event counted once (Events = COUNT(group_events); one day-detail record) — no per-participant inflation or cross-contact leak; asserted by tests |
| T-38-EGRESS | Information Disclosure | Your Week read path | high | mitigate | Async on-device SQLite only; no network on any read path (CLAUDE.md); the AI feature is untouched |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx vitest run src/services/history/week-window.test.ts src/db/migrations/030-your-week-period.test.ts src/db/app-settings-dao.test.ts src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts src/db/your-week-read.test.ts src/db/migrations/full-chain.test.ts` green.
- `npx tsc --noEmit` clean; `npm run check:colors` unaffected (no UI here).
- Manual/grep gates: TARGET_VERSION = 30; BACKUP_FORMAT_VERSION = 7 (D-08); `your_week_period` present in the snapshot SELECT and `yourWeekPeriod` in the return; `yourWeekPeriod` in the PORTABLE_SETTINGS_KEYS allowlist; a FORWARD_MIGRATIONS[6] entry exists.
</verification>

<success_criteria>
Rolling-7 + locale Calendar Week defined once (with the firstWeekday−1 conversion); migration 030 adds the additive period-preference column (user_version 30); yourWeekPeriod is PORTABLE (emitted, BACKUP_FORMAT_VERSION 7, restore-allowlisted, forward-migrated) per D-08; your-week-read supplies app-wide headline metrics + a group-deduped activity-unit date-count feed + day detail with correct group-event dedup and no network — all automated gates green, both one-way doors confirmed at the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-02-SUMMARY.md` when done.
</output>
