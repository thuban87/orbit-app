---
phase: 38-your-week
plan: 08
type: execute
wave: 5
depends_on: [38-04, 38-06]
files_modified:
  - src/components/history/InteractionDetail.tsx
  - src/screens/GroupEventDetailScreen.tsx
  - src/screens/group-event-detail-logic.test.ts
  - src/components/ui/__dev__/Phase38UatControls.tsx
  - src/components/ui/__dev__/ThemePreviewScreen.tsx
  - src/services/notifications/__dev__/phase38-uat.ts
  - src/services/notifications/__dev__/phase38-uat.test.ts
  - .planning/phases/38-your-week/evidence/38-07/UAT-RESULTS.md
  - .planning/phases/38-your-week/38-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [S-02, S-07, S-13, S-15]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "From an Events participant's Interaction Detail, the user can open that participant's Profile and Back returns to the same Group Event context. (S-02)"
    - "A debug build can temporarily expose the existing opted-in unbound Never Contacted population through the canonical app-settings writer, allowing the non-zero Digest preview and Contacts drill-through to be exercised without creating fixture rows or running raw SQL; the original setting is then restored. (S-07/S-15)"
    - "A debug build can schedule one delayed, generic-copy Digest UAT notification through expo-notifications without changing the production Sunday singleton; tapping the delivered OS notification opens the Digest root. (S-13/S-15)"
    - "Only the three unresolved Plan-07 paths are rerun on the physical Pixel, every one records PASS, and all state changed for the rerun is restored or explicitly identified as pre-existing drift. (S-15)"
  artifacts:
    - { path: src/components/history/InteractionDetail.tsx, provides: "Optional contact-profile action on the canonical interaction detail sheet" }
    - { path: src/screens/GroupEventDetailScreen.tsx, provides: "Events-stack Profile navigation from a participant interaction while preserving Group Event Back context" }
    - { path: src/components/ui/__dev__/Phase38UatControls.tsx, provides: "Compile-time DEV-only controls for the two blocked UAT paths" }
    - { path: src/services/notifications/__dev__/phase38-uat.ts, provides: "One-shot delayed Digest notification probe isolated from the production weekly identifier" }
    - { path: .planning/phases/38-your-week/evidence/38-07/UAT-RESULTS.md, provides: "Focused rerun evidence and final disposition of items 3, 5, and 7" }
  key_links:
    - { from: src/screens/GroupEventDetailScreen.tsx, to: src/navigation/tabs/EventsStack.tsx, via: "close detail sheet, then navigate to the already-registered Profile route with participant contactId" }
    - { from: src/components/ui/__dev__/Phase38UatControls.tsx, to: src/db/app-settings-dao.ts, via: "getAppSettings/updateAppSettings on includeUnboundNeverContacted; no direct SQL" }
    - { from: src/services/notifications/__dev__/phase38-uat.ts, to: src/navigation/notification-gate.tsx, via: "real OS delivery with the production {kind: digest} payload exercises the existing response gate" }
---

<objective>
Close the three unresolved Phase-38 physical-device paths with one bounded gap plan: repair Events participant-detail to Profile navigation, add non-production UAT entry points for the otherwise time/data-gated checks, and rerun only those checks on the Pixel.

Purpose: Turn Plan 07's one FAIL and two BLOCKED mandatory checks into direct evidence without widening product scope, changing schema/backup, creating app-visible test settings, or mutating contact rows.
Output: One Events navigation fix, one compile-time DEV-only UAT control surface, and completed Plan-07 evidence.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
@docs/runbooks/desktop-build-pipeline.md
@docs/runbooks/local-notification-integration.md
</execution_context>

<context>
@HANDOFF.md
@.planning/phases/38-your-week/38-CONTEXT.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-04-SUMMARY.md
@.planning/phases/38-your-week/38-06-SUMMARY.md
@.planning/phases/38-your-week/38-07-PLAN.md
@.planning/phases/38-your-week/38-VALIDATION.md
@.planning/phases/38-your-week/evidence/38-07/UAT-RESULTS.md

The working tree already contains uncommitted Plan-07 evidence and validation edits. Preserve them; do not replace or revert them. Work in place on the current branch. Never create a branch/worktree and never push.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair Events participant-detail to Profile and preserve origin-aware Back</name>
  <read_first>src/screens/GroupEventDetailScreen.tsx, src/components/group/ParticipantCard.tsx, src/components/history/InteractionDetail.tsx, src/components/history/HistorySection.tsx, src/navigation/tabs/EventsStack.tsx, src/navigation/types.ts, src/screens/group-event-detail-logic.ts, src/screens/group-event-detail-logic.test.ts</read_first>
  <files>src/components/history/InteractionDetail.tsx, src/screens/GroupEventDetailScreen.tsx, src/screens/group-event-detail-logic.test.ts</files>
  <behavior>
    - A group participant Interaction Detail rendered from GroupEventDetail exposes a clearly labelled `View profile` action.
    - Activating it closes the sheet before navigating to `Profile` with that participant's numeric `contactId` in the current Events stack.
    - Android/visible Back from Profile reveals the same GroupEventDetail route, not Contacts or the Events list root.
    - Existing InteractionDetail callers without a profile callback retain their current actions and behavior.
  </behavior>
  <action>Add an optional profile callback to the shared InteractionDetail action area and pass it only from GroupEventDetailScreen. The Group Event caller must dismiss its local `detail` state before `navigation.navigate("Profile", { contactId })`, using the already-registered Events-stack Profile route so native stack Back naturally restores the originating event. Keep the action token-styled through the existing Button primitive and do not redesign ParticipantCard, the detail sheet, or other history surfaces. Extend the render-free Group Event detail contract test to pin the callback's contact ID, close-before-navigate ordering, and route registration assumption.</action>
  <verify>
    <automated>npx vitest run src/screens/group-event-detail-logic.test.ts src/navigation/shell-contract.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <done>The previously impossible Events → participant Interaction Detail → Profile path exists, and Profile Back retains the Group Event detail as its origin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a compile-time DEV-only harness for the two blocked device checks</name>
  <read_first>src/components/ui/__dev__/ThemePreviewScreen.tsx, src/navigation/tabs/SettingsStack.tsx, src/screens/SettingsHubScreen.tsx, src/db/app-settings-dao.ts, src/logic/dashboard-query-logic.ts, src/screens/DigestScreen.tsx, src/services/notifications/digest-schedule.ts, src/services/notifications/notification-ids.ts, src/services/notifications/channels.ts, src/navigation/notification-gate.tsx, __mocks__/expo-notifications.ts</read_first>
  <files>src/components/ui/__dev__/Phase38UatControls.tsx, src/components/ui/__dev__/ThemePreviewScreen.tsx, src/services/notifications/__dev__/phase38-uat.ts, src/services/notifications/__dev__/phase38-uat.test.ts</files>
  <behavior>
    - The DEV-only control reads and displays the current `includeUnboundNeverContacted` value and changes it only via `updateAppSettings`; it offers both states so the operator can restore the captured original value.
    - Enabling the existing setting makes the already-present unbound, never-contacted contacts eligible through the landed D-10 count/preview/drill query; the harness creates, edits, or deletes no contact.
    - The notification probe schedules a delayed one-shot request with production Digest title/body, `data: { kind: "digest" }`, and `DIGEST_CHANNEL`, under a UAT-only identifier distinct from `digest:weekly`.
    - The probe does not touch notification settings, the weekly schedule, contact data, or network APIs, and reports scheduling failure truthfully in the DEV screen.
    - No Phase-38 UAT row, control, or route is reachable in a release build; the controls remain inside the existing `__DEV__`-gated Theme Preview harness.
  </behavior>
  <action>Create a small Phase38UatControls component mounted inside the existing DEV-only ThemePreviewScreen. For Never Contacted, read the current app setting, record it in the control state for operator visibility, and call the canonical app-settings DAO writer with `localDateTime()`; do not add a production Settings row because the setting's lack of ordinary UI is outside this phase. For notifications, add a DEV-only helper that uses `scheduleNotificationAsync` with a short future DATE trigger and the exact production generic Digest content/channel/payload but a unique `digest:uat:*` identifier, leaving `DIGEST_IDENTIFIER` and `scheduleDigest()` unchanged. Unit-test the request shape and identifier separation with the existing expo-notifications mock. Keep all colors and typography on existing theme/UI primitives. Do not add schema, backup keys, fixture rows, direct SQLite, device-time changes, or a release-visible test surface.</action>
  <verify>
    <automated>npx vitest run src/services/notifications/__dev__/phase38-uat.test.ts src/services/notifications/digest-schedule.test.ts src/services/notifications/notification-nav.test.ts src/stores/dashboard-query-store.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <done>A debug build can safely make the existing Never Contacted population observable and deliver a real tappable Digest notification now, while production behavior and release UI remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun only Plan 07 items 3, 5, and 7 on the Pixel and close the evidence</name>
  <read_first>docs/runbooks/desktop-build-pipeline.md, docs/runbooks/local-notification-integration.md, .planning/phases/38-your-week/38-07-PLAN.md, .planning/phases/38-your-week/38-VALIDATION.md, .planning/phases/38-your-week/evidence/38-07/UAT-RESULTS.md</read_first>
  <files>.planning/phases/38-your-week/evidence/38-07/UAT-RESULTS.md, .planning/phases/38-your-week/38-VALIDATION.md</files>
  <action>Run the full automated phase gate, then use the connected Pixel 6 Pro with package `com.bwales.orbit`, Metro tmux session `orbit` on host port 8082, and device reverse `tcp:8081` to host `tcp:8082`; still re-resolve the one authorized serial from `~/.local/bin/adb devices -l` rather than hardcoding it. Use the existing contacts only. Rerun exactly: (3) Events → event → participant Interaction Detail → View profile → Back to the same event; (5) capture the current Never Contacted UAT toggle, enable it through the DEV control, verify a non-zero Digest preview, open a preview Profile and Back, use the overflow drill into the Contacts `not-contacted` population, then restore the captured toggle; (7) schedule the DEV one-shot, background Orbit before delivery, tap the actual notification in Android's shade, and observe the Digest root. Capture `uiautomator` and screenshots for each assertion. Do not infer notification PASS from a resolver test and do not use raw SQL. Restore every setting changed during this rerun, leave master notification state as found, consume/cancel any leftover UAT notification, and make no contact edits. Note the already-recorded `ZZ-UAT-Clean` cadence drift as pre-existing rather than attempting a noncanonical repair. Append the focused rerun to UAT-RESULTS.md, change the validation row only from direct evidence, and mark Plan 07 closed only when all three mandatory items PASS; otherwise retain the exact FAIL/BLOCKED state.</action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
    <human-check>UAT-RESULTS.md contains direct Pixel evidence for items 3, 5, and 7, including the restored setting/notification state, and none remains FAIL or BLOCKED.</human-check>
  </verify>
  <done>The sole Plan-07 failure and both blocks are directly passed on the physical Pixel, evidence is committed, and device state changed by this rerun is restored.</done>
</task>

</tasks>

<source_audit>

| SOURCE | ID | Gap-relevant obligation | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Digest-centered shell is fully navigable and device-verified | 38-08 | COVERED |
| REQ | S-02 | Events-origin Profile and Back | Task 1/3 | COVERED |
| REQ | S-07 | Never Contacted preview/drill parity | Task 2/3 | COVERED |
| REQ | S-13 | Actual Digest notification routes to Digest | Task 2/3 | COVERED |
| REQ | S-15 | Physical-device UAT closure | Task 3 | COVERED |
| RESEARCH | — | Local-only read paths; route through existing stacks; test OS behavior on device | Tasks 1–3 | COVERED |
| CONTEXT | D-01 | Dossier remains authoritative | All | COVERED |
| CONTEXT | D-02 | No Digest schema/cache | Task 2 | COVERED — explicitly unchanged |
| CONTEXT | D-03 | Narrow scope; no Contacts/Events redesign | Tasks 1–2 | COVERED |
| CONTEXT | D-04…D-09 | Landed Digest semantics/persistence remain unchanged | All | COVERED — regression gate only |
| CONTEXT | D-10 | Opted-in unbound Never Contacted population | Task 2/3 | COVERED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DEV harness → on-device SQLite | A debug-only control temporarily writes one existing app-settings preference through the canonical DAO. |
| DEV harness → Android notification manager | A one-shot local request exercises real delivery/tap routing without touching the production weekly singleton. |
| Android notification response → navigator | App-minted `{kind: "digest"}` data selects the Digest root through the existing response gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38-08-STATE | Tampering | Phase38UatControls | medium | mitigate | Use get/updateAppSettings only, show/capture the original value, restore it during UAT, and create no contact fixtures. |
| T-38-08-SPOOF | Spoofing | one-shot Digest probe | low | mitigate | Mint only the existing generic Digest payload, keep the helper under `__DEV__`, and test that its identifier cannot collide with `digest:weekly`. |
| T-38-08-DISCLOSE | Information Disclosure | notification shade | low | mitigate | Reuse the frozen generic title/body containing no names, counts, notes, or contact identifiers. |
| T-38-08-EGRESS | Information Disclosure | all gap paths | high | mitigate | Keep every path local; no AI call, telemetry, backend, or network dependency. |

No package install and no supply-chain surface are introduced.
</threat_model>

<verification>
- Targeted tests for the Events route, DEV notification request, production scheduler, notification resolver, and D-10 population parity pass.
- Full `npx vitest run`, `npx tsc --noEmit`, and `npm run check:colors` pass.
- Plan-07 UAT items 3, 5, and 7 each have direct physical-Pixel PASS evidence; no waiver or inferred PASS.
- `TARGET_VERSION` remains 30 and `BACKUP_FORMAT_VERSION` remains 7; there is no migration, backup-schema change, contact fixture, raw SQL, or release-visible test surface.
</verification>

<success_criteria>
Exactly the three unresolved Plan-07 paths are closed, all automated gates remain green, and Phase 38 can proceed to verification without additional gap plans.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-08-SUMMARY.md` when done.
</output>
