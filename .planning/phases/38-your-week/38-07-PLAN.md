---
phase: 38-your-week
plan: 07
type: execute
wave: 4
depends_on: ["38-04", "38-06"]
files_modified:
  - src/navigation/shell-contract.test.ts
  - .planning/phases/38-your-week/38-VALIDATION.md
autonomous: false  # Task 2's owner-confirmation <precondition> (package name / Metro session / single authorized target) is a hard stop the executor must HALT on if unconfirmed — an autonomous run cannot satisfy it (addresses cycle-2 HIGH: autonomous:true contradicted the must-halt precondition)
requirements: [S-02, S-14, S-15]
user_setup:
  - service: pixel-device-uat
    why: "Physical-device UAT first-use safety (repo runbook): confirm the exact target before driving the app"
    dashboard_config:
      - task: "Confirm Orbit's Android package name, the Metro tmux session name, and that exactly ONE authorized target is attached (adb devices shows a single device)"
        location: "Owner — before Task 2 runs; the device task must halt if unconfirmed rather than guess"
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The full automated suite is green with a clean type gate: vitest full run + npx tsc --noEmit + npm run check:colors all pass (vitest green ≠ tsc clean — both are required before phase close)"
    - "A shell-contract regression test asserts the five-tab shape (Contacts·Events·Digest·Orrery·Settings), initialRoute Digest, and no BackupTab by consuming the RUNTIME shell/route descriptor `shell-contract.ts` (TAB_ORDER/INITIAL_TAB) + runtime `Object.keys(TAB_ICON)` parity + the pure `resolveNotificationNav` digest resolver — RENDER-FREE (the repo has no react-test-renderer/@testing-library and vitest.config.ts mandates render-free tests). It does NOT mount a NavigationContainer, import the runtime-erased TabParamList type, or scan source (addresses cycle-3 HIGH-1 + the cycle-2 source-scanner risk). Reselect-to-root / initial-focus / live traversal are observed on-device in Task 2 (§S.14/S.15), not in vitest"
    - "38-VALIDATION.md is populated with the real vitest/tsc/check:colors commands and a per-task→test map (not the {pytest/jest/vitest} template)"
    - "The device UAT records PASS / FAIL / BLOCKED per §S.15 item, with a disposable fixture strategy for the non-empty states; a check that cannot be exercised (missing hardware/data) is recorded BLOCKED and NEVER inferred as PASS (addresses codex HIGH)"
    - "Physical-device UAT on the Pixel confirms the whole Phase-38 surface: fresh launch to Digest, background/resume preservation, five-tab switching, reselect-to-root, origin-aware Profile Back from Contacts/Events/Digest/Orrery, FAB on Digest, Horizon drill-through, Your Week period toggle + inline heatmap day detail, Digest notification routing, and identical Standard/Galaxy IA (dossier §S.15)"
    - "Standard and Galaxy present identical information architecture and interaction; differences are presentational only, and heatmap day selection is legible without colour (dossier §P, HIST-18)"
  artifacts:
    - path: src/navigation/shell-contract.test.ts
      provides: "Cross-plan RENDER-FREE regression test pinning the five-tab shell contract by asserting the runtime shell/route descriptor (shell-contract.ts) + TAB_ICON key parity + the pure digest resolver — no NavigationContainer mount, no erased-type import, no source scan (extends the Plan 01-created test)"
      contains: "shell-contract"
  key_links:
    - from: src/navigation/shell-contract.test.ts
      to: src/navigation/shell-contract.ts
      via: "asserts TAB_ORDER (five ids in order) + INITIAL_TAB=DigestTab + no BackupTab + Object.keys(TAB_ICON) parity + resolveNotificationNav digest intent — render-free"
      pattern: "TAB_ORDER|INITIAL_TAB"
---

<objective>
Close Phase 38 with a navigation regression gate and the physical-device UAT the dossier mandates (§S.14/§S.15). The prior plans each carried automated tests and `<human-check>` device notes; this plan consolidates them into one shell-contract regression test + one full phase gate, then runs the end-to-end on-device acceptance across all five tabs and both theme packages.

Plan against the dossier (D-01). This is verification, not redesign — do not modify feature code here except to add the regression test (and, if the gate surfaces a real defect, hand it back to the owning plan rather than patching cross-cutting behaviour blind).

Purpose: prove the Digest-centered five-tab home works end-to-end on real hardware and the automated gates are green before `/gsd-verify-work`.
Output: the RENDER-FREE `shell-contract.test.ts` (extending the Plan 01-created descriptor test) + the consolidated device-UAT record.

**This plan is NOT autonomous:** Task 2 (device UAT) carries an owner-confirmation `<precondition>` (Orbit's Android package name, the Metro tmux session, and exactly ONE authorized target) that the executor must ASSERT and HALT on if unconfirmed — the repo device runbook forbids guessing these. Task 1 (the automated gate) is autonomous; Task 2 is a hard stop until the owner confirms the target. `autonomous: false` makes the frontmatter and the precondition consistent (cycle-2 HIGH).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; hook-blocked). Commit in place on the current branch.
- Device UAT uses a DEBUG build on the Pixel (release ≠ run-as). Build on the desktop (`ssh droid`) and deliver per `docs/runbooks/desktop-build-pipeline.md` — **tar-over-SSH / scp, NOT rsync** (rsync is absent on `droid` — ADR-007); NEVER `git push`. Run Orbit's Metro on the remapped port (device:8081→host:8082; quest-board owns :8081) with `adb reverse`. Dismiss the debug "Open debugger" LogBox toast before tapping tabs (it eats tab taps). Use the adb input taxonomy (plain tap for chips/rows; touchscreen tap for FAB/buttons) and prefer `uiautomator dump` over pixel guesses.
- Do NOT trigger a real AI suggestion API call during UAT without clearing with the owner (out of Digest scope anyway).
- Review the actual code on disk, not a diff or a subagent's summary — verify claims against `git log`/`git show` and the working tree.
</execution_context>

<context>
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render-free shell-contract regression test + populate VALIDATION.md + full phase gate</name>
  <files>src/navigation/shell-contract.test.ts, .planning/phases/38-your-week/38-VALIDATION.md</files>
  <read_first>
    - src/navigation/RootNavigator.tsx (final five-tab shape; initialRouteName; handleActiveTabPress wiring)
    - src/components/icons/icon-registry.ts (final TAB_ICON — a RUNTIME Record whose keys are the tab ids; assertable at runtime, unlike TabParamList which erases)
    - src/navigation/shell-contract.ts (the RUNTIME shell/route descriptor Plan 01 created — `TAB_ORDER`, `INITIAL_TAB`, `DIGEST_STACK_ROUTES`, `EVENTS_STACK_ROUTES`) and src/navigation/shell-contract.test.ts (the Plan 01-created render-free descriptor test this task EXTENDS). There is NO react-test-renderer / @testing-library and NO existing test that mounts a NavigationContainer — the cycle-2 "reuse an existing navigator-mount harness" premise was FALSE (cycle-3 HIGH-1); assert the runtime descriptor, never a mounted navigator
    - src/services/notifications/notification-nav.test.ts (the pure-resolver assertion idiom — call `resolveNotificationNav` on a digest payload and assert the Digest-tab-root intent, no rendering)
    - .planning/phases/38-your-week/38-RESEARCH.md "## Validation Architecture" (Sampling Rate: per-wave full run + tsc + check:colors; the tsc-in-post-merge-gate rule — vitest green ≠ tsc clean; the per-requirement test map to lift into VALIDATION.md)
    - .planning/phases/38-your-week/38-VALIDATION.md (the template to POPULATE — it currently has `{pytest/jest/vitest}` placeholders and status: draft)
  </read_first>
  <action>
    (1) EXTEND `src/navigation/shell-contract.test.ts` (the render-free test Plan 01 created for the descriptor) into the phase's cross-plan regression gate — RENDER-FREE (node env; NO NavigationContainer mount, NO react-test-renderer, which the repo does not have — cycle-3 HIGH-1; the cycle-2 "existing navigator-mount harness" premise was false). Assert against RUNTIME values, never a mounted navigator or a source scan or the runtime-erased `TabParamList` type: (a) the descriptor `TAB_ORDER` is exactly `["DashboardTab","EventsTab","DigestTab","OrreryTab","SettingsTab"]` (Contacts·Events·Digest·Orrery·Settings) with no `BackupTab`, and `INITIAL_TAB === "DigestTab"` (Plan 01's descriptor test already covers these; confirm they hold); (b) the RUNTIME `Object.keys(TAB_ICON)` set is exactly `{DashboardTab, EventsTab, DigestTab, OrreryTab, SettingsTab}` (TAB_ICON is a runtime Record — assertable, unlike TabParamList); (c) `resolveNotificationNav` on a digest payload yields the Digest-tab-root intent (pure resolver, no rendering). Reselect-to-root / initial-focus / live stack traversal are runtime navigation behaviors that only a mounted NavigationContainer could exercise — they are moved to Task 2's physical-device UAT (§S.14/S.15) and are NOT asserted in vitest.
    (2) Finalize `.planning/phases/38-your-week/38-VALIDATION.md` (already populated at plan time — real Vitest commands, tsc/colour gates, and the per-task→requirement→test map for Plans 01–07): confirm it still matches the delivered tests, flip each task Status to ✅ as its suite lands, and set `wave_0_complete: true` once every Wave-0 file exists. Do NOT reintroduce any `{...}` template placeholder.
    (3) Run the full phase gate: the complete vitest suite, `npx tsc --noEmit`, and `npm run check:colors`.
    (4) **Repair loop (define + follow — explicit for a SEQUENTIAL executor, since an already-completed plan does not literally resume itself):** if the gate surfaces a real feature-code defect, do NOT blind-patch it here. The steps are: (a) STOP Plan 07 (do not proceed to Task 2); (b) record the defect and the OWNING plan (the plan that produced the file — e.g. a DAO defect → Plan 02, a Horizon defect → Plan 06); (c) apply a SCOPED fix to that owning plan's file(s) and re-run THAT plan's task `<verify>` command until green (the fix is attributed to the owning plan, not to Plan 07); (d) RE-RUN this full phase gate (`npx vitest run` + `npx tsc --noEmit` + `npm run check:colors`); (e) only once the full gate is green, RESUME to the Task-2 device UAT. In THIS plan's own commit, modify NOTHING but `shell-contract.test.ts` + `38-VALIDATION.md` (`git show --stat` proves it); the scoped feature fix lands as its own change attributed to the owning plan.
  </action>
  <verify>
    <automated>npx vitest run src/navigation/shell-contract.test.ts 2>&1 | tail -15 && npx vitest run 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/navigation/shell-contract.test.ts` exists and is RENDER-FREE — it asserts the five-tab set/order + initial=Digest + no Backup tab via the runtime descriptor, the runtime `Object.keys(TAB_ICON)` parity, and the pure `resolveNotificationNav` digest intent (no NavigationContainer mount / no erased-type import / no source scan — none is even possible, the repo ships no renderer): `grep -c 'TAB_ORDER' src/navigation/shell-contract.test.ts` returns at least 1, `grep -c 'TAB_ICON' src/navigation/shell-contract.test.ts` returns at least 1, and `grep -c 'resolveNotificationNav' src/navigation/shell-contract.test.ts` returns at least 1 (positive runtime-value assertions). Reselect-to-root / initial-focus / traversal are Task-2 device UAT, not vitest
    - `.planning/phases/38-your-week/38-VALIDATION.md` has no `{` template placeholder left: `! grep -q '{pytest' .planning/phases/38-your-week/38-VALIDATION.md` and it lists `npx vitest run`, `npx tsc --noEmit`, `npm run check:colors` and a per-task map; `nyquist_compliant: true`
    - The full `npx vitest run` suite passes; `npx tsc --noEmit` is clean; `npm run check:colors` passes
    - No feature-code file is modified in this plan (`git show --stat` lists only the test + VALIDATION.md); any real defect the gate found was routed to its owning plan and this gate re-run green
  </acceptance_criteria>
  <done>The shell contract is pinned by a RENDER-FREE descriptor regression test (runtime TAB_ORDER/INITIAL_TAB + TAB_ICON parity + digest resolver; no navigator mount), VALIDATION.md is populated (nyquist_compliant), the repair loop is defined, and the full suite + tsc + check:colors are green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidated physical-device UAT (Pixel, both themes)</name>
  <files>none — physical-device UAT; evidence (screenshots/uiautomator) recorded in 38-07-SUMMARY.md</files>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (the authoritative build+deliver pipeline — tar-over-SSH/scp, ADR-007; Metro remap device:8081→host:8082; the owner-confirmation first-use step other plans encode, e.g. 31-05-PLAN.md:119)
    - .planning/phases/38-your-week/38-RESEARCH.md "### On-Device UAT surface (§S.15 — Pixel 6 Pro, debug build)" (the 8-point checklist)
    - 38-UI-SPEC.md (Navigation & Interaction Contract; the long-text backstop held to §S.15)
    - dossier §S.15 (the full device acceptance list) and §P (Standard/Galaxy parity, colour-independent selection)
  </read_first>
  <precondition>Owner has confirmed Orbit's Android package name, the Metro tmux session name, and that exactly ONE authorized target is attached (`adb devices` shows a single device); the executor asserts this and HALTS if unconfirmed rather than hardcoding/guessing the remap or target (repo first-use safety).</precondition>
  <action>
    Build+install the DEBUG APK on the Pixel per `docs/runbooks/desktop-build-pipeline.md` (tar-over-SSH/scp, NOT rsync — ADR-007) and drive the app to verify the full Phase-38 surface, capturing evidence with `adb exec-out screencap` + `uiautomator dump`. Do not rely on a single screenshot — verify data/behaviour, not just that a screen rendered. Because `workflow.human_verify_mode = end-of-phase`, these are `<human-check>` items harvested at phase verification, not a mid-flight checkpoint.
    **Three outcomes, never inferred:** record each §S.15 item as PASS / FAIL / **BLOCKED** — a check that cannot be exercised because the hardware or the required data state is unavailable is recorded BLOCKED and is NEVER converted to PASS by "the screen rendered."
    **Fixture strategy for the non-empty states** (several checks need a specific state that an empty dev DB lacks): provision a disposable, clearly-labelled fixture (via `run-as` DB writes on the debug build, or seeded contacts/interactions) to exercise: a non-zero Never Contacted population; a multi-participant Group Event (to prove one heatmap unit + one day-detail record); at least one Digest notification; meaningful week activity across days; and both theme packages. If a fixture cannot be provisioned, record that item BLOCKED. Do NOT trigger a real AI suggestion API call (owner clearance required; out of Digest scope).
    Record for the run: build SHA, package name, device serial, Android API level, theme package, and the fixture identity used — so the evidence is reproducible.
  </action>
  <verify>
    <human-check>
    On the Pixel (debug build), verify and record each:
    1. Fresh launch lands on Digest (centered tab); background→resume preserves the prior tab/stack (does NOT reset to Digest). (Initial-focus is verified HERE on-device — vitest cannot mount a NavigationContainer, cycle-3 HIGH-1.)
    2. All five tabs switch (short crossfade, no swipe); reselecting the active tab returns it to root. (Reselect-to-root + live Profile→child traversal are the runtime behaviors moved from the automated test to this device UAT, cycle-3 HIGH-1.)
    3. Origin-aware Profile Back: Digest→Profile→Back→Digest; Contacts→Profile→Back→Contacts; Orrery→Profile→Back→Orrery; Events/detail→Profile→Back→Events context.
    4. FAB visible on Digest with the Contacts actions; Backup tab absent; Backup & Restore still reachable via Settings.
    5. Horizon Overlooked / Never-Contacted drill-through opens the canonical Contacts population/filter; Never Contacted hidden when zero.
    6. Your Week: period toggle (Rolling 7 / Calendar Week) drives metrics + heatmap + day detail consistently; day tap expands inline beneath the heatmap (no rotary); a group event shows as one record; birthday window unaffected by the toggle.
    7. Digest notification tap opens the Digest tab root; Back behaves.
    8. Standard + Galaxy: identical IA/interaction, presentational differences only; heatmap day selection legible without colour (structural selected state); no clip of day tags/status rings at large OS font scale (long-text backstop).
    9. **Offline (local-first, dossier §O):** put the device in airplane mode (or disable data/wifi), cold-launch, and open Digest — Up Next / Horizon / Your Week all render fully with NO network (no spinner-stall, no error sentinel from a network call); at least one Digest read-path is verified offline. (Do NOT trigger a real AI suggestion call — out of scope and requires owner clearance.)
    </human-check>
  </verify>
  <acceptance_criteria>
    - The owner-confirmation precondition (package + Metro session + single authorized target) is recorded as satisfied before any device driving; if it was not confirmable, the task halted and recorded so
    - The SUMMARY records PASS / FAIL / BLOCKED for each of the nine items (the eight §S.15 items + the offline Digest read-path check) — never inferring PASS from a render — with screenshot/uiautomator evidence references and the run metadata (build SHA, package, device serial, API level, theme, fixture identity)
    - The offline check is recorded: Digest renders fully in airplane mode (at least one Digest read-path verified with no network), consistent with the local-first acceptance condition (dossier §O)
    - The non-empty states used a provisioned disposable fixture (Never Contacted > 0, a multi-participant Group Event, a Digest notification, meaningful week activity, both themes) or were recorded BLOCKED
    - Any FAIL is filed against the owning plan (not silently patched here); the repair loop (Task 1) governs the rerun
    - The long-text backstop (§S.15) is exercised at a large OS font scale and recorded
  </acceptance_criteria>
  <done>The full §S.15 device UAT is exercised on the Pixel across both themes with a confirmed target, a disposable fixture for the non-empty states, PASS/FAIL/BLOCKED per item (never inferred), and recorded evidence + run metadata; failures routed to owning plans; phase is device-verified.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 07)

New symbols/files:
- `src/navigation/shell-contract.test.ts` → cross-plan RENDER-FREE shell-contract regression test (extends the Plan 01-created test; asserts the runtime descriptor + TAB_ICON parity + digest resolver — no navigator mount)
- `.planning/phases/38-your-week/38-VALIDATION.md` → populated validation contract (real commands + per-task map, nyquist_compliant)
- The consolidated §S.15 device-UAT record (in the SUMMARY), with PASS/FAIL/BLOCKED + run metadata

## Review feedback addressed (Plan 07)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| Autonomous UAT lacks first-use owner confirmation/precondition | HIGH (codex) | Incorporated — Task 2 `<precondition>` + `user_setup` (package/session/single target), halts if unconfirmed |
| No BLOCKED outcomes / no fixtures for non-empty states | HIGH (codex) | Incorporated — Task 2 defines PASS/FAIL/BLOCKED (never inferred) + a disposable fixture strategy + run metadata |
| Shell test likely to become a source scanner (erased types) | MEDIUM (codex) | Incorporated — Task 1 renders the navigator + asserts runtime TAB_ICON + the pure resolver; forbids importing TabParamList |
| "Fix nothing but the test" leaves an execution dead-end on a real defect | MEDIUM (codex) | Incorporated — Task 1 defines the repair loop (route to owning plan → its verify → re-run gate → device UAT) |
| Build instructions should cite the runbook/ADR-007 (tar-over-SSH, not rsync) | MEDIUM (codex) | Incorporated — execution_context + Task 2 cite desktop-build-pipeline runbook + ADR-007 |
| 38-VALIDATION.md is an unfilled template | LOW (claude) | Incorporated — Task 1 populates it (real commands + per-task map, nyquist_compliant) |
| **[cycle 2] `autonomous: true` contradicts the must-halt owner-confirmation precondition** | HIGH (codex, disk-confirmed) | Incorporated — frontmatter set `autonomous: false` (Task 1 autonomous, Task 2 gated by the halting `<precondition>`); objective states the consistency rationale |
| **[cycle 2] repair loop procedurally ambiguous for a sequential executor** | MEDIUM (codex) | Incorporated — Task 1 step (4) spells it out: STOP Plan 07 → record owning plan + defect → scoped fix under that plan's `<verify>` → rerun full phase gate → resume UAT; Plan 07's own commit touches only the test + VALIDATION.md |
| **[cycle 2] objective said `shell-contract.test.ts` (should be `.test.tsx`)** | LOW (codex) | Incorporated — objective Output corrected to `.test.tsx` (matches files_modified/Task 1/artifacts) |
| **[cycle 2] require offline mode on ≥1 Digest read-path check** | LOW (codex) | Incorporated — Task 2 human-check item 9 (airplane-mode cold-launch, Digest renders with no network) + acceptance |
| **[cycle 3] HIGH-1 — Task 1's rendered-navigator regression test is non-executable (no react-test-renderer/@testing-library on disk; vitest.config.ts mandates render-free) and rests on a FALSE "existing navigator-mount harness" premise** | HIGH (codex, disk-confirmed) | Incorporated — Task 1 is now RENDER-FREE: it extends the Plan 01-created `shell-contract.test.ts`, asserting the runtime descriptor (TAB_ORDER/INITIAL_TAB) + `Object.keys(TAB_ICON)` parity + the pure `resolveNotificationNav` digest intent; the false mount-harness premise is removed and the file renamed `.test.tsx`→`.test.ts`; reselect-to-root / initial-focus / live traversal move to Task 2 physical-device UAT (§S.14/S.15) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug build on device → on-device DB | UAT observes real on-device data; verification is UI-observable and must be cross-checked against data invariants, not screen appearance alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-EGRESS | Information Disclosure | full app under UAT | high | mitigate | Confirm no network on any Digest read path during UAT (offline check); do not trigger a real AI API call (owner clearance required, out of Digest scope) |
| T-38-VERIFY | Repudiation | UAT evidence integrity | low | mitigate | Capture screenshot + uiautomator evidence; verify data/behaviour not just render; cross-check subagent claims against git log / working tree |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- Full `npx vitest run` + `npx tsc --noEmit` + `npm run check:colors` green (the post-merge/phase gate).
- The shell-contract regression test passes.
- All eight §S.15 device-UAT items recorded pass on the Pixel across Standard + Galaxy, including the long-text backstop at a large font scale.
</verification>

<success_criteria>
Automated suite + type gate + colour gate green; shell contract pinned by regression test; the full Phase-38 surface verified on real hardware across both themes with recorded evidence — the phase is ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-07-SUMMARY.md` when done.
</output>
