---
phase: 38-your-week
plan: 06
type: execute
wave: 3
depends_on: ["38-01", "38-03", "38-05"]
files_modified:
  - src/screens/DigestScreen.tsx
  - src/components/digest/UpNextSection.tsx
  - src/components/digest/HorizonSection.tsx
autonomous: true
requirements: [S-02, S-06, S-07, S-08]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Digest screen presents exactly three fixed modules in order Up Next → Horizon → Your Week (act → be aware/look ahead → look back) (dossier §C)"
    - "Up Next shows at most three contacts, each exposing enough orbit/relationship context (status ring + a why-present line) to explain its presence; row tap opens Profile and Back returns to Digest; no inline Call/Message and no required View All (dossier §D)"
    - "Horizon shows Birthdays / Overlooked / Never Contacted as separate labeled subgroups (never one ranked feed); Up Next takes first claim and Horizon dedups those contacts (dossier §E)"
    - "Digest reads run async on focus (null-vs-loaded sentinel), never flash the empty state before resolution, use no ...Sync, no writer, and no network on the read path (dossier §O)"
    - "Major modules stay visible when empty with the neutral empty-state copy; empty Horizon subgroups disappear entirely (dossier §C/§P)"
    - "A thrown Digest read lands on the calm error sentinel and re-runs on next focus (shipped pattern)"
    - "Populated states: Up Next hard-capped at 3; Birthdays show all matches soonest-first; the heatmap renders the supplied window/counts (dossier §D/§F/§J)"
    - "Zero-one-many: Never Contacted is hidden at zero; counts use singular/plural copy; today's birthday reads Today, else Tomorrow or a formatted local date (dossier §F/§G)"
    - "Overflow: large Overlooked / Never Contacted populations use a compact preview + '+{n} more →' drill-through to the canonical Contacts population/filter — never an arbitrarily large list dumped into Digest, and never a Digest-only browser (dossier §G)"
    - "Partial: each module resolves its own empty/populated state independently and never depends on a sibling being populated; the Up Next↔Horizon dedup prevents double-surfacing the same contact/condition (dossier §E)"
    - "The Digest screen title is sourced from the canonical product-label constant (dossier §B); as a tab root it has no Back button; all colour via theme tokens and text via AppText roles (no raw fontSize, no colour literal)"
    - statement: "Long-text: contact/birthday/event names use AppText body with single-line ellipsis in dense rows, and module headings/labels reflow under OS text scaling with no clip of the day tag or status ring at large font scale"
      verification: backstop
  artifacts:
    - path: src/screens/DigestScreen.tsx
      provides: "The rewritten Digest tab root composing Up Next / Horizon / Your Week from the ready reads + composition + YourWeekSection"
      contains: "DigestScreen"
    - path: src/components/digest/UpNextSection.tsx
      provides: "Up Next module — ≤3 contacts with status ring + why-present line; row tap → Profile"
      contains: "UpNextSection"
    - path: src/components/digest/HorizonSection.tsx
      provides: "Horizon module — Birthdays / Overlooked / Never Contacted labeled subgroups with dedup, compact preview + drill-through, conditional Never Contacted"
      contains: "HorizonSection"
  key_links:
    - from: src/screens/DigestScreen.tsx
      to: src/logic/digest-composition.ts
      via: "pickUpNext / dedupOverlooked / filterUpcomingBirthdays / neverContactedConditional over the on-focus reads"
      pattern: "pickUpNext|dedupOverlooked|filterUpcomingBirthdays"
    - from: src/screens/DigestScreen.tsx
      to: src/components/digest/YourWeekSection.tsx
      via: "mounts <YourWeekSection/> as the third fixed module"
      pattern: "YourWeekSection"
    - from: src/components/digest/HorizonSection.tsx
      to: src/screens/HomeScreen.tsx
      via: "Overlooked / Never-Contacted '+N more' drill-through to the canonical Contacts population/filter"
      pattern: "DashboardTab|navigate"
---

<objective>
Assemble the Digest surface: rewrite `DigestScreen` from the shipped "your week" retrospective into the fixed three-module home — **Up Next → Horizon → Your Week** — composing the ready reads (Plan 03 up-next-read, existing readOverlooked/listBirthdayCandidates/countNeverContacted) through the pure composition layer (Plan 03), and mounting the Your Week module (Plan 05). This is the presentation counterpart to Plans 02/03/05; the data and logic are already built and tested, so this plan is pure presentation wiring against ready contracts.

Plan against the dossier (D-01). Fixed section order and empty-state model per §C/§P; Up Next per §D; Horizon per §E/§F/§G; derive-only, no new schema/cache (D-02, §O). This plan + Plan 05 jointly deliver the coherent Digest surface — the split is a context-cost split of one presentation surface (D-03), not a per-module product decomposition.

Purpose: a real user, on the Digest tab, sees who to reach out to, what to be aware of, and a retrospective of their week — all from existing relationship state.
Output: rewritten `DigestScreen.tsx`; `UpNextSection.tsx`; `HorizonSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; hook-blocked). Commit in place on the current branch.
- All colour via `useTheme().colors.*` (status ring only through the shared `ContactCard`; heatmap via `colors.heatmapScale`; accent reserved for focused-tab/period-toggle/drill-through per UI-SPEC) — no hex/named literal (`npm run check:colors`). Text via `<AppText role=...>` (display/heading/body/label/caption), never raw `fontSize`. Migrate the legacy DigestScreen inline `fontSize`/`padding:16`/`gap:12/24` literals to roles + `SPACING` tokens (module gap `lg`).
- Reads are async on focus with the cancelled-flag guard + null-vs-loaded sentinel; no `...Sync`, no writer, no network on the read path (dossier §O).
- No inline write actions in Up Next / Horizon (D-07, §D); if any interaction create/edit surfaced it would signal scope drift — none should.
- Dates via `formatLocalDate()`, never `toISOString().split('T')[0]`.
- Review the actual code on disk (files in `<read_first>`), not a diff.
</execution_context>

<context>
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpNextSection — ≤3 contacts with status ring + why-present line</name>
  <files>src/components/digest/UpNextSection.tsx, src/components/digest/UpNextSection.test.tsx</files>
  <read_first>
    - src/db/up-next-read.ts (readUpNextCandidates — Plan 03) and src/logic/digest-composition.ts (pickUpNext — Plan 03)
    - src/db/digest-read.ts (OverlookedRow reason values 'overdue'/'unresponsive' — the why-present copy source)
    - src/components/ContactCard.tsx (the shared status-ring card — the ONLY status colour the Digest uses; ProfileStatus)
    - src/screens/DigestScreen.tsx (the shipped ContactCard usage :326-340; goToProfile :121-122; the focus-effect read pattern :91-119)
    - src/components/AppText + 38-UI-SPEC.md (Typography roles; Up Next copy; empty state "You're all caught up" / "No one needs a nudge right now.")
    - dossier §D
  </read_first>
  <action>
    Create `src/components/digest/UpNextSection.tsx`: renders the Up Next module heading (AppText `heading` role, from copy) and at most three contact rows (the `pickUpNext` result). Each row uses the shared `ContactCard` for the orbit status ring (colours only via ContactCard — the Digest adds no status colour) plus a why-present context line (AppText `label`/`caption`) derived from the canonical `reason`/status (e.g. overdue vs unresponsive) — do NOT invent a Digest-local urgency classification (D-04). Row tap calls the provided `onOpenProfile(contactId)` (navigates to Profile within DigestStack; Back → Digest). No inline Call/Message, no required View All (§D). When Up Next is empty, render the neutral empty state ("You're all caught up" / "No one needs a nudge right now.") — the module stays visible. Use `AppText` roles + `SPACING` tokens; no colour literal.
  </action>
  <verify>
    <automated>npx vitest run src/components/digest/UpNextSection.test.tsx 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/digest/UpNextSection.tsx` exists and exports `UpNextSection`
    - It caps at three rows (a test with >3 candidates renders exactly 3) and renders the neutral empty state at zero
    - Status colour comes only via ContactCard: `grep -c 'ContactCard' src/components/digest/UpNextSection.tsx` returns at least 1; no colour literal (`npm run check:colors` passes)
    - `npx tsc --noEmit` clean; the suite passes
  </acceptance_criteria>
  <done>Up Next renders ≤3 canonical-order contacts with the shared status ring + why-present line and Profile tap, with the neutral empty state; section tests + tsc + check:colors green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: HorizonSection — Birthdays / Overlooked / Never Contacted subgroups</name>
  <files>src/components/digest/HorizonSection.tsx, src/components/digest/HorizonSection.test.tsx</files>
  <read_first>
    - src/logic/digest-composition.ts (filterUpcomingBirthdays / dedupOverlooked / neverContactedConditional / previewWithOverflow — Plan 03)
    - src/db/digest-read.ts (readOverlooked) and src/db/dashboard-read.ts (listBirthdayCandidates :620-628; countNeverContacted :540-554)
    - src/screens/HomeScreen.tsx (the Contacts population/filter entry — how the Contacts list receives a population/filter, for the drill-through target) and src/navigation/types.ts (DashboardTab navigation for cross-tab drill-through)
    - 38-UI-SPEC.md (Horizon subgroup labels Birthdays/Overlooked/Never Contacted; birthday tags Today/Tomorrow/date; '+{n} more →'; empty state "Nothing on the horizon" / "No birthdays or overlooked people to flag right now.")
    - dossier §E/§F/§G/§P
  </read_first>
  <action>
    Create `src/components/digest/HorizonSection.tsx`: renders the Horizon heading and three SEPARATE labeled subgroups (never one ranked feed) — Birthdays, Overlooked, Never Contacted. Birthdays: the `filterUpcomingBirthdays` result (7-day forward, soonest-first) with Today/Tomorrow/date tags; row tap → Profile. Overlooked: `dedupOverlooked(readOverlooked, upNextIds)` (excluding Up Next's claimed contacts) rendered as ContactCards, with a compact preview + `+{n} more →` (`previewWithOverflow`) that drills through to the canonical Contacts population/filter (navigate to the Contacts tab/DashboardTab at the appropriate population/filter using the existing Contacts filtering architecture; never a Digest-only browser — §G). Never Contacted: conditional (`neverContactedConditional` — rendered only when count > 0), also a compact preview + drill-through. An empty subgroup renders nothing (disappears); when ALL of Horizon is empty, render the neutral Horizon empty state. Singular/plural counts; `AppText` roles + `SPACING` tokens; no colour literal. Accept `upNextIds` as a prop so the dedup is driven by the screen's Up Next selection.
  </action>
  <verify>
    <automated>npx vitest run src/components/digest/HorizonSection.test.tsx 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>On the Pixel (once Task 3 mounts it): Horizon shows the three labeled subgroups; an empty subgroup disappears; a large Overlooked population shows a compact preview + "+N more →" that opens the canonical Contacts population/filter; birthdays show Today/Tomorrow/date.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/digest/HorizonSection.tsx` exists and exports `HorizonSection`
    - Subgroups are separate labeled groups (Birthdays/Overlooked/Never Contacted present as distinct labeled blocks); an empty subgroup renders nothing (test asserts a zero-count subgroup is absent while a populated sibling renders)
    - Never Contacted is conditional at zero; the overflow affordance drills through to a Contacts route (not a Digest-only browser): `grep -c 'DashboardTab' src/components/digest/HorizonSection.tsx` returns at least 1
    - No colour literal (`npm run check:colors` passes); `npx tsc --noEmit` clean; the suite passes
  </acceptance_criteria>
  <done>Horizon renders three deduped labeled subgroups with 7-day birthdays, compact preview + Contacts drill-through, conditional Never Contacted, and the empty state; section tests + tsc + check:colors green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: DigestScreen rewrite — compose the three modules on focus</name>
  <files>src/screens/DigestScreen.tsx, src/screens/DigestScreen.test.tsx</files>
  <read_first>
    - src/screens/DigestScreen.tsx (the full shipped screen — focus-effect + cancelled-flag + null-vs-loaded sentinel :79-176; the Back button :129-138 to REMOVE since Digest is now a tab root; error sentinel :147-157; styles :418-537 inline literals to migrate to roles/tokens)
    - src/constants/product-labels.ts (DIGEST — Plan 01) and src/components/digest/UpNextSection.tsx + HorizonSection.tsx (Tasks 1-2) + src/components/digest/YourWeekSection.tsx (Plan 05)
    - src/db/up-next-read.ts / src/db/digest-read.ts / src/db/dashboard-read.ts (the reads to run in the focus-effect Promise.all)
    - src/logic/digest-composition.ts (pickUpNext / dedupOverlooked / filterUpcomingBirthdays / neverContactedConditional)
    - 38-UI-SPEC.md (## UI Considerations — the loading/empty/error/populated/zero-one-many/overflow/partial/long-text states this screen must honour; section order; error copy "Couldn't load your Digest" / "Try opening it again in a moment.")
    - dossier §C/§O/§P
  </read_first>
  <action>
    Rewrite `src/screens/DigestScreen.tsx` (typed against `DigestStackParamList` now that Digest is a tab root) to compose the three fixed modules in order Up Next → Horizon → Your Week. Keep the shipped focus-effect + cancelled-flag guard + null-vs-loaded sentinel; replace the reads' `Promise.all` with the new composition inputs: `readUpNextCandidates`, `readOverlooked`, `listBirthdayCandidates`, `countNeverContacted` (async only, no network). Derive the Up Next set via `pickUpNext`, pass its ids to `HorizonSection` for `dedupOverlooked`, and run `filterUpcomingBirthdays(candidates, new Date())` + `neverContactedConditional`. Mount `<UpNextSection/>`, `<HorizonSection/>`, and `<YourWeekSection/>` (Plan 05). Remove the legacy Back button (a tab root has no Back). Title from the `DIGEST` product-label constant (§B). Honour every UI-consideration state: modules render blank bodies until reads resolve (no empty-state flash); each module resolves its own empty/populated state independently (partial); a thrown read shows the calm error sentinel ("Couldn't load your Digest") and re-runs on next focus; long names use `AppText body` single-line ellipsis in dense rows while headings/labels reflow. Migrate all inline `fontSize`/`padding`/`gap` literals to `AppText` roles + `SPACING` tokens (module gap `lg`). No colour literal; no `...Sync`; no writer.
  </action>
  <verify>
    <automated>npx vitest run src/screens/DigestScreen.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>On the Pixel: the Digest tab shows Up Next (≤3) → Horizon (labeled subgroups) → Your Week (metrics + heatmap + inline day detail) in that order, with no Back button; row taps open Profile and Back returns to Digest; empty modules show neutral copy; forcing a read error shows the calm error sentinel and recovers on re-focus. Repeat under a large OS font scale (long-text backstop, §S.15): no clip of day tags or status rings.</human-check>
  </verify>
  <acceptance_criteria>
    - DigestScreen composes all three modules: `grep -c 'UpNextSection\|HorizonSection\|YourWeekSection' src/screens/DigestScreen.tsx` returns at least 3
    - The legacy Back button is gone: `grep -c 'digest-back' src/screens/DigestScreen.tsx` returns 0
    - The title sources the product-label constant: `grep -c 'DIGEST' src/screens/DigestScreen.tsx` returns at least 1
    - No sync reads / no UTC slicing / no colour literal: `grep -c 'getAllSync\|getFirstSync' src/screens/DigestScreen.tsx` returns 0, `grep -c 'toISOString' src/screens/DigestScreen.tsx` returns 0, `npm run check:colors` passes
    - `npx vitest run src/screens/DigestScreen.test.tsx` passes (asserts section order, the empty/error states, and that Up Next↔Horizon dedup ids flow from the screen); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>DigestScreen renders the three fixed modules in order from on-focus reads with correct loading/empty/error/partial states, no Back button, product-label title, and token/role migration; screen tests + tsc + check:colors green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 06)

New symbols/files:
- `src/components/digest/UpNextSection.tsx` → `UpNextSection`
- `src/components/digest/HorizonSection.tsx` → `HorizonSection`
- Rewritten: `src/screens/DigestScreen.tsx` (now composes Up Next / Horizon / Your Week; typed against DigestStackParamList; no Back button)
- Test files: `src/components/digest/UpNextSection.test.tsx`, `HorizonSection.test.tsx`, `src/screens/DigestScreen.test.tsx`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Digest reads → screen render | On-device contact data composed for display; dedup/caps must not leak or double-surface a contact |
| drill-through → Contacts tab | Cross-tab navigation to a canonical Contacts population/filter; no Digest-only data browser is created |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-DEDUP | Information Disclosure | Up Next↔Horizon dedup at the screen | low | mitigate | Screen passes Up Next ids to Horizon; dedup is unit-tested (Plan 03) so a contact cannot double-surface for the same condition |
| T-38-EGRESS | Information Disclosure | Digest read path | high | mitigate | Async on-device SQLite only; no network on any read path; no writer surfaces in Up Next/Horizon (D-07) |
| T-38-NOSCHEMA | Tampering | derive-only Digest | medium | mitigate | Digest composes from existing reads only; no new relationship-domain schema and no persisted Digest cache (D-02) — this plan writes nothing to the DB |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx vitest run src/components/digest src/screens/DigestScreen.test.tsx` green.
- `npx tsc --noEmit` clean; `npm run check:colors` green.
- On-device (§S.2/§S.6/§S.7/§S.8, consolidated in Plan 07): three-module Digest in order; Up Next ≤3 + Profile Back; Horizon subgroups + dedup + drill-through; error/empty states; long-text backstop under large font scale.
</verification>

<success_criteria>
The Digest tab renders Up Next → Horizon → Your Week composed from existing reads via the pure composition layer, honouring every resolved UI-consideration state (loading/empty/error/populated/zero-one-many/overflow/partial) with the long-text backstop held to device UAT; no Back button, product-label title, derive-only; all automated gates green.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-06-SUMMARY.md` when done.
</output>
