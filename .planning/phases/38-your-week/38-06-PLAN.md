---
phase: 38-your-week
plan: 06
type: execute
wave: 3
depends_on: ["38-01", "38-03", "38-05"]
files_modified:
  - src/screens/DigestScreen.tsx
  - src/components/digest/UpNextSection.tsx
  - src/components/digest/HorizonSection.tsx
  - src/stores/dashboard-query-store.ts
autonomous: true
requirements: [S-02, S-06, S-07, S-08]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Digest screen presents exactly three fixed modules in order Up Next → Horizon → Your Week (act → be aware/look ahead → look back) (dossier §C)"
    - "Up Next shows at most three contacts, each exposing enough orbit/relationship context (status ring + a why-present line) to explain its presence; row tap opens Profile and Back returns to Digest; no inline Call/Message and no required View All (dossier §D)"
    - "Horizon shows Birthdays / Overlooked / Never Contacted as separate labeled subgroups (never one ranked feed); Up Next takes first claim and Horizon dedups those contacts (dossier §E)"
    - "Digest reads run async on focus (null-vs-loaded sentinel), never flash the empty state before resolution, use no ...Sync, no writer, and no network on the read path (dossier §O)"
    - "Major modules stay visible when empty with the neutral empty-state copy; empty Horizon subgroups disappear entirely (dossier §C/§P)"
    - "A thrown Digest read lands on the calm error sentinel and re-runs on next focus (shipped pattern)"
    - "Populated states: Up Next hard-capped at 3; Birthdays show all matches soonest-first; the heatmap renders the supplied window/counts (dossier §D/§F/§J)"
    - "Zero-one-many: Never Contacted is hidden at zero; counts use singular/plural copy; today's birthday reads Today, else Tomorrow or a formatted local date (dossier §F/§G)"
    - "Never Contacted has a real row source for its compact preview: `listDashboardPopulation(exec, query, now)` with the `not-contacted` population, NOT `countNeverContacted` which returns only a number (addresses codex HIGH — no row source existed). `listDashboardPopulation` has NO cap argument (signature `(exec, query, now)`, dashboard-read.ts:380) — it returns the FULL filtered `DashboardRow[]`, which the preview then slices in JS to `+{n} more` via `previewWithOverflow` (Plan 03); the read itself is not DB-capped (addresses codex/claude — 'capped read' wording was imprecise)"
    - "Per owner ruling D-10, the `not-contacted` population read is unbound-inclusive (delivered by Plan 03: `buildPopulationWhere` emits `countNeverContacted`'s exact predicate for a `['not-contacted']` selection), so the Digest Never Contacted COUNT (`countNeverContacted`), PREVIEW (`listDashboardPopulation(['not-contacted'])`), and DRILL (Contacts `['not-contacted']`) are provably EQUAL even when `include_unbound_never_contacted = 1` — the cycle-3 HIGH-2 mismatch is closed (§G/ADR-062 extension scoped to not-contacted only)"
    - "Overflow drill-through mutates the actual persisted state boundary AND sets BOTH query axes atomically to a known-complete state, so a pre-existing persisted filter/population cannot narrow or empty the drilled Contacts list (addresses cycle-2 HIGH — the cycle-1 fix set only one axis). It writes BOTH `dashboardPopulations` and `dashboardFilters` in ONE `updateAppSettings` via a dedicated store action (mirroring `resetDashboardView`) BEFORE navigating to the Contacts tab — navigating to a bare DashboardTab establishes no population and is insufficient. Never Contacted → populations `['not-contacted']` AND filters `{}` (matching the Digest preview/count which fetch with `filters:{}`, so the drilled list equals the preview exactly — and, per owner ruling D-10, that `not-contacted` read is unbound-inclusive (Plan 03), so count == preview == drilled rows even when `include_unbound_never_contacted = 1`); Overlooked → populations `[]` (the all-active base) AND filters `{ 'needs-attention': ['on'] }` (the canonical attention filter, since no exact 'overlooked' population token exists — dossier §G 'reuse canonical filtering, not a Digest-only browser'). The Overlooked drill is intentionally a canonical SUPERSET (wobble+decay+rogue, snooze-excluded) of the rogue-only Overlooked preview; with `readOverlooked` now snooze-guarded (Plan 03) the preview and drill agree on snooze exclusion, and the '+N more →' copy reads as 'see everyone needing attention', not an exact preview+N equivalence (unlike Never Contacted's exact contract)"
    - "Overflow: large Overlooked / Never Contacted populations use a compact preview + '+{n} more →' drill-through to the canonical Contacts population/filter — never an arbitrarily large list dumped into Digest, and never a Digest-only browser (dossier §G)"
    - "Partial: each module resolves its own empty/populated state independently and never depends on a sibling being populated; the Up Next↔Horizon dedup prevents double-surfacing the same contact/condition (dossier §E)"
    - "The Digest screen title is sourced from the canonical product-label constant (dossier §B); as a tab root it has no Back button; all colour via theme tokens and text via AppText roles (no raw fontSize, no colour literal)"
    - statement: "Long-text: contact/birthday/event names use AppText body with single-line ellipsis in dense rows, and module headings/labels reflow under OS text scaling with no clip of the day tag or status ring at large font scale"
      verification: backstop
  prohibitions:
    - statement: "No inline write action (Call/Message, or any interaction create/edit) renders in Up Next or Horizon; these modules are read-only presentation. If an interaction write ever surfaces here it must route through the single recency-writer core in one transaction (ADR-010/024/071) — a write surfacing in these modules signals scope drift (D-07, dossier §D)"
      status: resolved
      verification: explicit
  artifacts:
    - path: src/screens/DigestScreen.tsx
      provides: "The rewritten Digest tab root composing Up Next / Horizon / Your Week from the ready reads + composition + YourWeekSection"
      contains: "DigestScreen"
    - path: src/components/digest/UpNextSection.tsx
      provides: "Up Next module — ≤3 contacts with status ring + why-present line; row tap → Profile"
      contains: "UpNextSection"
    - path: src/components/digest/HorizonSection.tsx
      provides: "Horizon module — Birthdays / Overlooked / Never Contacted labeled subgroups with dedup, compact preview + drill-through, conditional Never Contacted"
      contains: "HorizonSection"
  key_links:
    - from: src/screens/DigestScreen.tsx
      to: src/logic/digest-composition.ts
      via: "pickUpNext / dedupOverlooked / filterUpcomingBirthdays / neverContactedConditional over the on-focus reads"
      pattern: "pickUpNext|dedupOverlooked|filterUpcomingBirthdays"
    - from: src/screens/DigestScreen.tsx
      to: src/components/digest/YourWeekSection.tsx
      via: "mounts <YourWeekSection/> as the third fixed module"
      pattern: "YourWeekSection"
    - from: src/components/digest/HorizonSection.tsx
      to: src/stores/dashboard-query-store.ts
      via: "'+N more' drill-through mutates the persisted canonical Contacts query state via a dedicated atomic store action that sets BOTH axes in one updateAppSettings BEFORE navigating to the Contacts tab — Never Contacted → populations ['not-contacted'] AND filters {}; Overlooked → populations [] AND filters { 'needs-attention': ['on'] }"
      pattern: "useDashboardQueryStore|setPopulationsAndFilters"
---

<objective>
Assemble the Digest surface: rewrite `DigestScreen` from the shipped "your week" retrospective into the fixed three-module home — **Up Next → Horizon → Your Week** — composing the ready reads (Plan 03 up-next-read, existing readOverlooked/listBirthdayCandidates/countNeverContacted) through the pure composition layer (Plan 03), and mounting the Your Week module (Plan 05). This is the presentation counterpart to Plans 02/03/05; the data and logic are already built and tested, so this plan is pure presentation wiring against ready contracts.

Plan against the dossier (D-01). Fixed section order and empty-state model per §C/§P; Up Next per §D; Horizon per §E/§F/§G; derive-only, no new schema/cache (D-02, §O). This plan + Plan 05 jointly deliver the coherent Digest surface — the split is a context-cost split of one presentation surface (D-03), not a per-module product decomposition.

Purpose: a real user, on the Digest tab, sees who to reach out to, what to be aware of, and a retrospective of their week — all from existing relationship state.
Output: rewritten `DigestScreen.tsx`; `UpNextSection.tsx`; `HorizonSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution constraints (project, non-negotiable):**
- Do NOT create git branches or git worktrees (`workflow.use_worktrees: false`; hook-blocked). Commit in place on the current branch.
- All colour via `useTheme().colors.*` (status ring only through the shared `ringVisual(status, colors)` primitive — re-exported from `ContactCard` at `ContactCard.tsx:49`, pure and theme-resolved; heatmap via `colors.heatmapScale`; accent reserved for focused-tab/period-toggle/drill-through per UI-SPEC) — no hex/named literal (`npm run check:colors`). Text via `<AppText role=...>` (display/heading/body/label/caption), never raw `fontSize`. Migrate the legacy DigestScreen inline `fontSize`/`padding:16`/`gap:12/24` literals to roles + `SPACING` tokens (module gap `lg`).
- **Component tests are RENDER-FREE (cycle-3):** `vitest.config.ts` mandates node-env render-free tests and the repo has NO `react-test-renderer` / `@testing-library/react-native`. Assert section behavior via the repo's render-free tree-walk idiom — call the component function and walk the returned React element tree, asserting props (row count, subgroup presence/absence, `onPress`/drill handler wiring) rather than mounting and firing taps. Analogs: `src/components/orrery/orrery-controls-render.test.tsx`, `src/components/category/CategoryChoiceSheet.test.tsx`. Store-mutation tests call the store action directly. Real press/navigation is verified on-device in Plan 07.
- Reads are async on focus with the cancelled-flag guard + null-vs-loaded sentinel; no `...Sync`, no writer, no network on the read path (dossier §O).
- No inline write actions in Up Next / Horizon (D-07, §D); if any interaction create/edit surfaced it would signal scope drift — none should.
- Dates via `formatLocalDate()`, never `toISOString().split('T')[0]`.
- Review the actual code on disk (files in `<read_first>`), not a diff.
</execution_context>

<context>
@.planning/phases/38-your-week/38-RESEARCH.md
@.planning/phases/38-your-week/38-PATTERNS.md
@.planning/phases/38-your-week/38-UI-SPEC.md
@docs/dossier/milestone-2/phase-38-digest-navigation-restructure-dossier.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpNextSection — ≤3 contacts with status ring + why-present line</name>
  <files>src/components/digest/UpNextSection.tsx, src/components/digest/UpNextSection.test.tsx</files>
  <read_first>
    - src/db/up-next-read.ts (readUpNextCandidates — Plan 03) and src/logic/digest-composition.ts (pickUpNext — Plan 03)
    - src/db/digest-read.ts (OverlookedRow reason values 'overdue'/'unresponsive' — the why-present copy source) and src/db/status.ts (REASON_SQL :100-104 returns NON-NULL only for rogue rows: 'unresponsive' (rarely_responds) / 'overdue' (progress≥ROGUE_K), ELSE NULL — so a wobble/decay Up Next row at progress≥STABLE_MAX but <ROGUE_K has `reason = NULL`; STATUS_SQL :72-78 buckets wobble/decay/rogue/stable, the fallback source)
    - src/components/contact-card-ring.ts (`ringVisual(status, colors)` — the PURE, RN-free status-ring mapping; re-exported from `ContactCard.tsx:49`) and src/components/Avatar.tsx (the avatar primitive). NOTE (cycle-3 MEDIUM): the full `ContactCard` requires `modifiedAt`/`categoryLabel`/`isFavourite`/`fuelText`/`snippet` (ContactCard.tsx:51-71) which the Plan-03 `up-next-read` row (id/name/photo/progress/status/reason) does NOT carry — do NOT pass false/null Dashboard fields into ContactCard. Build a dedicated compact Digest row from `Avatar` + `ringVisual(status, colors)` instead.
    - src/screens/DigestScreen.tsx (the shipped ContactCard usage :326-340; goToProfile :121-122; the focus-effect read pattern :91-119)
    - src/components/AppText + 38-UI-SPEC.md (Typography roles; Up Next copy; empty state "You're all caught up" / "No one needs a nudge right now.")
    - dossier §D
  </read_first>
  <action>
    Create `src/components/digest/UpNextSection.tsx`: renders the Up Next module heading (AppText `heading` role, from copy) and at most three contact rows (the `pickUpNext` result). **Each row is a dedicated COMPACT Digest row — `Avatar` + the shared `ringVisual(status, colors)` primitive (re-exported from `ContactCard.tsx:49`) + name + a why-present context line — NOT the full `ContactCard` (addresses cycle-3 MEDIUM):** `ContactCard` requires `modifiedAt`/`categoryLabel`/`isFavourite`/`fuelText`/`snippet` (ContactCard.tsx:51-71) that the `up-next-read` row (id/name/photo/progress/status/reason) does not carry, and supplying false/null Dashboard fields would make the component contract misleading. The status ring colour comes ONLY through `ringVisual` (theme-resolved — the Digest adds no status colour). The why-present line (AppText `label`/`caption`) is derived from the canonical `reason`/status — do NOT invent a Digest-local urgency classification (D-04). **The why-present line MUST fall back to the canonical `status` bucket when `reason` is NULL (addresses codex/claude LOW):** `REASON_SQL` is non-null only for rogue rows ('overdue'/'unresponsive'), so a wobble/decay Up Next contact (progress ≥ STABLE_MAX but < ROGUE_K, not rarely_responds) carries `reason = NULL` and would otherwise render a BLANK context line. Map: rogue+`reason` → the reason copy (overdue / unresponsive); else derive from `status` (wobble → approaching/due-soon copy, decay → overdue copy). Keep copy derivation separate from selection so changing prose cannot alter Up Next membership. Row tap calls the provided `onOpenProfile(contactId)` (navigates to Profile within DigestStack; Back → Digest). No inline Call/Message, no required View All (§D). When Up Next is empty, render the neutral empty state ("You're all caught up" / "No one needs a nudge right now.") — the module stays visible. Use `AppText` roles + `SPACING` tokens; no colour literal.
  </action>
  <verify>
    <automated>npx vitest run src/components/digest/UpNextSection.test.tsx 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/digest/UpNextSection.tsx` exists and exports `UpNextSection`
    - It caps at three rows (a test with >3 candidates renders exactly 3) and renders the neutral empty state at zero
    - A test asserts the why-present line falls back to `status` copy when `reason` is NULL: a wobble/decay candidate (reason=NULL) renders a non-blank context line derived from its status, and a rogue candidate renders the overdue/unresponsive reason copy
    - Status colour comes only via the shared ring primitive, and the full ContactCard is NOT used (its extra required fields are absent from the up-next row): `grep -c 'ringVisual' src/components/digest/UpNextSection.tsx` returns at least 1, `grep -c 'Avatar' src/components/digest/UpNextSection.tsx` returns at least 1, and `! grep -q '<ContactCard' src/components/digest/UpNextSection.tsx`; no colour literal (`npm run check:colors` passes)
    - A render-free tree-walk test asserts each row exposes the status ring (via `ringVisual`) and a non-blank why-present line, without requiring ContactCard's `modifiedAt`/`categoryLabel`/`isFavourite`/`fuelText`/`snippet`
    - `npx tsc --noEmit` clean; the suite passes
  </acceptance_criteria>
  <done>Up Next renders ≤3 canonical-order contacts with the shared status ring + why-present line and Profile tap, with the neutral empty state; section tests + tsc + check:colors green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: HorizonSection — Birthdays / Overlooked / Never Contacted subgroups</name>
  <files>src/components/digest/HorizonSection.tsx, src/components/digest/HorizonSection.test.tsx, src/stores/dashboard-query-store.ts, src/stores/dashboard-query-store.test.ts</files>
  <read_first>
    - src/logic/digest-composition.ts (filterUpcomingBirthdays / dedupOverlooked / neverContactedConditional / previewWithOverflow — Plan 03)
    - src/db/digest-read.ts (readOverlooked — now snooze-guarded by Plan 03 so its preview matches the `needs-attention` drill's snooze exclusion) and src/db/dashboard-read.ts (listBirthdayCandidates :620-628; countNeverContacted :540-554 returns ONLY a number — NOT a row source; listDashboardPopulation :380-411 signature `(exec, query, now)` returns the FULL filtered DashboardRow[] {id,name,photo,...} with NO cap arg — the preview is sliced in JS via previewWithOverflow, the read is not DB-capped)
    - **D-10 read-path guarantee (delivered by Plan 03, consumed here):** `buildPopulationWhere` makes a `['not-contacted']` selection unbound-inclusive (the exact `countNeverContacted` predicate), so `listDashboardPopulation({populations:['not-contacted'],filters:{}})` == `countNeverContacted` == the drilled Contacts list when `include_unbound_never_contacted = 1`. This HorizonSection consumes that read; it does NOT re-implement the scope. Cite D-10.
    - src/stores/dashboard-query-store.ts — CRITICAL for the cycle-2 fix: `setPopulations` :92-99 writes ONLY `dashboardPopulations` (leaves filters), `setFilters` :100-111 writes ONLY `dashboardFilters` (leaves populations), each in its own `updateAppSettings` + generation bump; the Contacts WHERE AND-composes population+filter, so setting only ONE axis leaves the other STALE and can empty/narrow the drilled list. `resetDashboardView` :116-128 is the precedent: it writes populations+filters+sort in ONE `updateAppSettings` — model the new atomic drill action on it. `setFilters` canonicalizes via `canonicalizeDashboardFilters` :101 — the atomic action must do the same for its filters axis
    - src/logic/dashboard-query-logic.ts (DASHBOARD_POPULATIONS :5-11 = favourites/birthdays/not-contacted/snoozed/all-contacts — 'not-contacted' is Never Contacted; DASHBOARD_FILTER_FAMILIES incl. 'needs-attention' :17 with NEEDS_ATTENTION_VALUE 'on' — the canonical Overlooked/attention filter, since there is NO exact 'overlooked'/'rogue' population token; `buildPopulationWhere` with EMPTY populations :253-254 returns `ACTIVE_SEGREGATION_WHERE` = all active contacts, so populations `[]` is the correct 'all-active base' for the Overlooked drill, not a no-rows state; `resetDashboardView` :285-294 returns `{populations:[], filters:{}, sort:'default'}`)
    - src/navigation/types.ts (cross-tab navigation to DashboardTab → Home; the Contacts tab hosts the canonical list that reads useDashboardQueryStore)
    - 38-UI-SPEC.md (Horizon subgroup labels Birthdays/Overlooked/Never Contacted; birthday tags Today/Tomorrow/date; '+{n} more →'; empty state "Nothing on the horizon" / "No birthdays or overlooked people to flag right now.")
    - dossier §E/§F/§G/§P
  </read_first>
  <action>
    Create `src/components/digest/HorizonSection.tsx`: renders the Horizon heading and three SEPARATE labeled subgroups (never one ranked feed) — Birthdays, Overlooked, Never Contacted. Birthdays: the `filterUpcomingBirthdays` result (7-day forward, soonest-first) with Today/Tomorrow/date tags; row tap → Profile. Overlooked: `dedupOverlooked(readOverlooked, upNextIds)` (excluding Up Next's claimed contacts) rendered as the dedicated COMPACT Digest row (`Avatar` + `ringVisual(status, colors)`) — NOT full `ContactCard` (cycle-3 MEDIUM: `OverlookedRow` = id/name/photo/rarely_responds/progress/status/reason, which LACKS ContactCard's required `modifiedAt`/`categoryLabel`/`isFavourite`/`fuelText`/`snippet`; do not pass false/null), with a compact preview + `+{n} more →` (`previewWithOverflow`). `readOverlooked` is snooze-guarded (Plan 03), so its preview and the `needs-attention` drill agree on snooze exclusion; the drill remains a canonical wobble+decay+rogue SUPERSET of the rogue-only preview (documented), so the `+{n} more →` copy reads as "see everyone needing attention", NOT an exact preview+N promise. Never Contacted: conditional (`neverContactedConditional` — rendered only when count > 0), a compact preview + `+{n} more →`, its **preview rows supplied by `listDashboardPopulation` with the `not-contacted` population** — NOT `countNeverContacted` (which returns only a number and cannot render a preview; the total count still comes from `countNeverContacted`). Per owner ruling D-10, that `not-contacted` read is unbound-inclusive (Plan 03) and so is `countNeverContacted`, so count == preview == drill even when `include_unbound_never_contacted = 1`. `listDashboardPopulation(exec, query, now)` has no cap argument: it returns the FULL filtered set, which `previewWithOverflow` slices in JS to the compact preview + `+{n} more` (do not claim a DB-capped read).
    **Drill-through contract (the real state boundary — cycle-2 HIGH: set BOTH axes ATOMICALLY):** the cycle-1 fix mutated only ONE query axis, leaving the orthogonal axis stale — with a pre-existing persisted `needs-attention` filter, the Never Contacted drill (`not-contacted` only) yields `not-contacted AND needs-attention` = empty (they are mutually exclusive) while the Digest preview/count still show N; a visible desync. FIX: add ONE atomic store action that persists BOTH axes in a SINGLE `updateAppSettings` (mirroring `resetDashboardView` :116-128), then navigate. Add to `src/stores/dashboard-query-store.ts` a `setPopulationsAndFilters(exec, populations, filters)` action that: canonicalizes filters via `canonicalizeDashboardFilters`, calls `updateAppSettings(exec, { dashboardPopulations: JSON.stringify(populations), dashboardFilters: JSON.stringify(canonical) }, localDateTime())`, and does ONE `set(...)` with ONE generation bump (both axes replaced together — no window where one axis is new and the other stale). The `+{n} more →` affordance, on press, calls this action BEFORE navigating to the Contacts tab root (a bare `navigate("DashboardTab")` sets no population and lands on the last-used view — insufficient). Values, both axes always explicit:
    - **Never Contacted →** `setPopulationsAndFilters(exec, ['not-contacted'], {})` — filters cleared to `{}`, matching the Digest preview/count which fetch with `filters:{}`, so the drilled Contacts list equals the preview EXACTLY regardless of any prior persisted filter.
    - **Overlooked →** `setPopulationsAndFilters(exec, [], { 'needs-attention': ['on'] })` — populations reset to `[]` (the all-active base, `buildPopulationWhere` :253-254) so a prior persisted population (e.g. `favourites`) cannot narrow the attention list; the `needs-attention` filter is the canonical attention population (§G — reuse canonical filtering, not a Digest-only browser; no exact 'overlooked' token exists).
    Take an `onDrillThrough(target)` callback prop (or the store+navigation handles) from the screen so the store write + tab navigate are testable. An empty subgroup renders nothing (disappears); when ALL of Horizon is empty, render the neutral Horizon empty state. Singular/plural counts; `AppText` roles + `SPACING` tokens; no colour literal. Accept `upNextIds` as a prop so the dedup is driven by the screen's Up Next selection. Add a `dashboard-query-store.test.ts` case for `setPopulationsAndFilters` (both axes persisted in one write; generation bumped once).
  </action>
  <verify>
    <automated>npx vitest run src/components/digest/HorizonSection.test.tsx 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>On the Pixel (once Task 3 mounts it): Horizon shows the three labeled subgroups; an empty subgroup disappears; a large Overlooked population shows a compact preview + "+N more →" that opens the canonical Contacts population/filter; birthdays show Today/Tomorrow/date.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/digest/HorizonSection.tsx` exists and exports `HorizonSection`
    - Subgroups are separate labeled groups (Birthdays/Overlooked/Never Contacted present as distinct labeled blocks); an empty subgroup renders nothing (test asserts a zero-count subgroup is absent while a populated sibling renders)
    - Never Contacted is conditional at zero; its preview rows come from `listDashboardPopulation` (`not-contacted`), not from a count: `grep -c 'listDashboardPopulation\|not-contacted' src/components/digest/HorizonSection.tsx` (or the screen that feeds it) returns at least 1
    - The drill-through sets BOTH axes atomically via the new store action: `grep -c 'setPopulationsAndFilters' src/stores/dashboard-query-store.ts` returns at least 1 and the HorizonSection (or its screen-provided handler) calls it; a `dashboard-query-store.test.ts` case asserts `setPopulationsAndFilters` writes both `dashboardPopulations` and `dashboardFilters` in ONE `updateAppSettings` and bumps generation once
    - **BEHAVIORAL drill-through test with a PRE-EXISTING persisted axis (the cycle-2 regression):** starting from a store seeded with a conflicting persisted filter (e.g. `{ 'needs-attention': ['on'] }`) and/or population, the Never Contacted overflow leaves the store at EXACTLY `populations:['not-contacted'], filters:{}` (so the drilled `listDashboardPopulation` result equals the Digest preview rows/count), and the Overlooked overflow leaves it at EXACTLY `populations:[], filters:{ 'needs-attention':['on'] }` — asserting the FINAL complete state of BOTH axes, not merely the changed one
    - **D-10 count==preview==drill equivalence test (cycle-3 HIGH-2):** with `include_unbound_never_contacted = 1` seeded AND an unbound (`tracking_enabled = 0`) never-contacted contact present, assert `countNeverContacted(exec)` === the Never Contacted preview length (`listDashboardPopulation({populations:['not-contacted'],filters:{}}, now).length`) === the drilled Contacts row count after the Never Contacted overflow — all three EQUAL and all INCLUDE the unbound contact (consuming Plan 03's D-10 read-path). Cite D-10
    - Overlooked preview↔drill snooze parity: a snoozed rogue is absent from BOTH the Overlooked preview (`readOverlooked`, snooze-guarded by Plan 03) and the `needs-attention` drilled list; the `+{n} more →` copy does not claim exact preview+N equivalence (the drill is a documented wobble+decay+rogue superset)
    - No colour literal (`npm run check:colors` passes); `npx tsc --noEmit` clean; the suite passes
  </acceptance_criteria>
  <done>Horizon renders three deduped labeled subgroups with 7-day birthdays, a real Never-Contacted preview from listDashboardPopulation (full read, JS-sliced), compact preview + an ATOMIC both-axes Contacts drill-through via `setPopulationsAndFilters` (Never Contacted → not-contacted+{}; Overlooked → []+needs-attention) that stays correct under a pre-existing persisted axis, conditional Never Contacted, and the empty state; section tests + the store both-axes test + the pre-existing-persisted-axis regression + tsc + check:colors green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: DigestScreen rewrite — compose the three modules on focus</name>
  <files>src/screens/DigestScreen.tsx, src/screens/DigestScreen.test.tsx</files>
  <read_first>
    - src/screens/DigestScreen.tsx (the full shipped screen — focus-effect + cancelled-flag + null-vs-loaded sentinel :79-176; the Back button :129-138 to REMOVE since Digest is now a tab root; the legacy `onPressBacklog={() => navigation.navigate("Home")}` at :171 — `Home` is a DashboardStack route NOT in DigestStackParamList, so it must NOT survive the rewrite (claude LOW); error sentinel :147-157; styles :418-537 inline literals to migrate to roles/tokens)
    - src/constants/product-labels.ts (DIGEST — Plan 01) and src/components/digest/UpNextSection.tsx + HorizonSection.tsx (Tasks 1-2) + src/components/digest/YourWeekSection.tsx (Plan 05)
    - src/db/up-next-read.ts / src/db/digest-read.ts / src/db/dashboard-read.ts (the reads to run in the focus-effect Promise.all: `readUpNextCandidates`, `readOverlooked`, `listBirthdayCandidates`, `countNeverContacted`, and `listDashboardPopulation` :380 with the `not-contacted` population for the Never-Contacted preview rows — it takes `(exec, query, now)` with NO cap arg and returns the full filtered set, so the preview is sliced in JS via `previewWithOverflow`)
    - src/stores/dashboard-query-store.ts (the store the Horizon drill-through mutates; DigestScreen wires the exec + navigation into HorizonSection's drill-through)
    - src/logic/digest-composition.ts (pickUpNext / dedupOverlooked / filterUpcomingBirthdays / neverContactedConditional)
    - 38-UI-SPEC.md (## UI Considerations — the loading/empty/error/populated/zero-one-many/overflow/partial/long-text states this screen must honour; section order; error copy "Couldn't load your Digest" / "Try opening it again in a moment.")
    - dossier §C/§O/§P
  </read_first>
  <action>
    Rewrite `src/screens/DigestScreen.tsx` (typed against `DigestStackParamList` now that Digest is a tab root) to compose the three fixed modules in order Up Next → Horizon → Your Week. Keep the shipped focus-effect + cancelled-flag guard + null-vs-loaded sentinel; replace the reads' `Promise.all` with the new composition inputs: `readUpNextCandidates`, `readOverlooked`, `listBirthdayCandidates`, `countNeverContacted`, and `listDashboardPopulation` with `{ populations: ['not-contacted'], filters: {}, sort: 'default', viewMode: 'list' }` at the current local `now` (async only, no network) — the FULL filtered set (no DB cap arg), sliced to the compact preview in JS via `previewWithOverflow`. Per D-10 (Plan 03) this `not-contacted` preview read is unbound-inclusive, matching `countNeverContacted`, so the Never Contacted count and preview agree. Derive the Up Next set via `pickUpNext`, pass its ids to `HorizonSection` for `dedupOverlooked`, and run `filterUpcomingBirthdays(candidates, new Date())` + `neverContactedConditional`; pass the not-contacted preview rows + total count into `HorizonSection`. Wire the exec + tab navigation into `HorizonSection`'s drill-through so its `+{n} more →` can mutate `useDashboardQueryStore` then navigate to the Contacts tab. Mount `<UpNextSection/>`, `<HorizonSection/>`, and `<YourWeekSection/>` (Plan 05). Remove the legacy Back button (a tab root has no Back) AND remove the legacy backlog nudge's `navigation.navigate("Home")` (`:171`) — `Home` is not a DigestStackParamList route; no `navigate("Home")` may survive. Title from the `DIGEST` product-label constant (§B). Honour every UI-consideration state: modules render blank bodies until reads resolve (no empty-state flash); each module resolves its own empty/populated state independently (partial); a thrown read shows the calm error sentinel ("Couldn't load your Digest") and re-runs on next focus; long names use `AppText body` single-line ellipsis in dense rows while headings/labels reflow. Migrate all inline `fontSize`/`padding`/`gap` literals to `AppText` roles + `SPACING` tokens (module gap `lg`). No colour literal; no `...Sync`; no writer.
  </action>
  <verify>
    <automated>npx vitest run src/screens/DigestScreen.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5 && npm run check:colors 2>&1 | tail -3</automated>
    <human-check>On the Pixel: the Digest tab shows Up Next (≤3) → Horizon (labeled subgroups) → Your Week (metrics + heatmap + inline day detail) in that order, with no Back button; row taps open Profile and Back returns to Digest; empty modules show neutral copy; forcing a read error shows the calm error sentinel and recovers on re-focus. Repeat under a large OS font scale (long-text backstop, §S.15): no clip of day tags or status rings.</human-check>
  </verify>
  <acceptance_criteria>
    - DigestScreen composes all three modules: `grep -c 'UpNextSection\|HorizonSection\|YourWeekSection' src/screens/DigestScreen.tsx` returns at least 3
    - The legacy Back button is gone: `grep -c 'digest-back' src/screens/DigestScreen.tsx` returns 0
    - No `navigate("Home")` survives (Home is not a DigestStack route): `! grep -q 'navigate("Home")' src/screens/DigestScreen.tsx`
    - The Never-Contacted preview has a real row source: `grep -c 'listDashboardPopulation' src/screens/DigestScreen.tsx` returns at least 1
    - The title sources the product-label constant: `grep -c 'DIGEST' src/screens/DigestScreen.tsx` returns at least 1
    - No sync reads / no UTC slicing / no colour literal: `grep -c 'getAllSync\|getFirstSync' src/screens/DigestScreen.tsx` returns 0, `grep -c 'toISOString' src/screens/DigestScreen.tsx` returns 0, `npm run check:colors` passes
    - `npx vitest run src/screens/DigestScreen.test.tsx` passes (asserts section order, the empty/error states, and that Up Next↔Horizon dedup ids flow from the screen); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>DigestScreen renders the three fixed modules in order from on-focus reads with correct loading/empty/error/partial states, no Back button, product-label title, and token/role migration; screen tests + tsc + check:colors green; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 06)

New symbols/files:
- `src/components/digest/UpNextSection.tsx` → `UpNextSection`
- `src/components/digest/HorizonSection.tsx` → `HorizonSection`
- Rewritten: `src/screens/DigestScreen.tsx` (now composes Up Next / Horizon / Your Week; typed against DigestStackParamList; no Back button; no `navigate("Home")`)
- Consumes existing: `listDashboardPopulation` (Never-Contacted preview, made unbound-inclusive by Plan 03's D-10 read-path); `ringVisual` (re-exported from `ContactCard`) + `Avatar` for the compact Up Next / Overlooked rows (NOT full `ContactCard`); `readOverlooked` (snooze-guarded by Plan 03) — no new symbols
- New symbol on the store: `src/stores/dashboard-query-store.ts` → `setPopulationsAndFilters(exec, populations, filters)` — an atomic action that persists BOTH query axes in one `updateAppSettings` (drill-through state boundary)
- Test files: `src/components/digest/UpNextSection.test.tsx`, `HorizonSection.test.tsx`, `src/screens/DigestScreen.test.tsx`, extended `src/stores/dashboard-query-store.test.ts`

## Review feedback addressed (Plan 06)

| Finding (REVIEWS cycle-1) | Severity | Disposition |
|---|---|---|
| Never Contacted has no row source for the preview (countNeverContacted is a number) | HIGH (codex #5) | Incorporated — Tasks 2-3 supply preview rows via `listDashboardPopulation` (`not-contacted`) |
| Cross-tab drill-through not defined at the Zustand state boundary | HIGH (codex #6) | Incorporated — Task 2 mutates `useDashboardQueryStore` (setPopulations/setFilters) BEFORE navigating; behavioral test asserts the store write |
| Overlooked has no exact canonical population | MEDIUM (codex) | Incorporated — Overlooked drills through via the `needs-attention` filter (canonical attention), not an invented population |
| Up Next empty behavior depends on Plan 03's predicate | MEDIUM (codex) | Resolved upstream — Plan 03 Task 1 now applies the attention floor (empty reachable) |
| `grep -c 'DashboardTab'` is not behavioral verification | LOW (codex) | Incorporated — Task 2 acceptance now requires a behavioral store-mutation test |
| Residual `navigate("Home")` in DigestScreen (:171) | LOW (claude) | Incorporated — Task 3 removes it; acceptance `! grep -q 'navigate("Home")'` |
| **[cycle 2] Drill-through set only ONE query axis; a stale orthogonal axis can empty/narrow the drilled list and desync it from the Digest preview/count** | HIGH (codex/claude, disk-confirmed) | Incorporated — Task 2 adds an atomic `setPopulationsAndFilters` (both axes in one `updateAppSettings`, mirroring `resetDashboardView`); Never Contacted → `['not-contacted']`+`{}`, Overlooked → `[]`+`{needs-attention:['on']}`; behavioral test starts from a PRE-EXISTING persisted filter and asserts the FINAL complete state of BOTH axes |
| **[cycle 2] "capped read" wording — `listDashboardPopulation` has no cap arg** | LOW (codex/claude) | Incorporated — must-haves/Task 2/Task 3 now state it returns the full filtered set (signature `(exec,query,now)`) sliced in JS via `previewWithOverflow`; no DB cap claimed |
| **[cycle 2] Up Next why-present line has no reason for wobble/decay (REASON_SQL NULL)** | LOW (codex/claude) | Incorporated — Task 1 falls back to canonical `status` copy when `reason` is NULL; a test pins wobble/decay non-blank copy and rogue reason copy |
| **[cycle 3] HIGH-2 — Never Contacted count (countNeverContacted, unbound-inclusive) ≠ preview/drill (not-contacted, bound-only). OWNER RULED D-10: include opted-in unbound** | HIGH (codex, disk-confirmed) | Incorporated — the read-path fix ships in Plan 03 (the `not-contacted` population made unbound-inclusive, the exact countNeverContacted predicate, scoped to not-contacted only per D-10/§G/ADR-062); this plan's must-haves + Task 2 assert count == preview == drill via an `include_unbound_never_contacted=1` + unbound-contact fixture. Cite D-10 |
| **[cycle 3] Horizon Overlooked preview (readOverlooked, no snooze) diverges from its needs-attention drill target (snooze-excluded)** | MEDIUM (claude) | Incorporated — the snooze guard ships in Plan 03; Task 2 documents the residual wobble+decay+rogue superset drill and reframes the '+N more →' copy as a 'see everyone needing attention' affordance (not exact preview+N), + a preview↔drill snooze-parity test |
| **[cycle 3] UpNextSection/Overlooked cannot pass the DAO row to ContactCard (missing modifiedAt/categoryLabel/isFavourite/fuelText/snippet)** | MEDIUM (codex) | Incorporated — Task 1 (Up Next) and Task 2 (Overlooked) render a dedicated COMPACT Digest row (`Avatar` + the shared `ringVisual` primitive), NOT full `ContactCard`; no false/null Dashboard fields supplied; Task 1 acceptance forbids `<ContactCard` in UpNextSection |
| **[cycle 3] codex rated Plan 06 component/screen tests HIGH (non-executable rendered tests)** | HIGH→ADJUSTED (aggregator scope-corrected) | Clarified, not restructured — HIGH-1 is sustained ONLY for the navigator-mount tests (Plans 01/07). Plan 06 section/store tests are feasible render-free via the tree-walk idiom + direct store-action calls, now named in execution_context; live press/navigation is verified in Plan 07 device UAT |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Digest reads → screen render | On-device contact data composed for display; dedup/caps must not leak or double-surface a contact |
| drill-through → Contacts tab | Cross-tab navigation to a canonical Contacts population/filter; no Digest-only data browser is created |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-DEDUP | Information Disclosure | Up Next↔Horizon dedup at the screen | low | mitigate | Screen passes Up Next ids to Horizon; dedup is unit-tested (Plan 03) so a contact cannot double-surface for the same condition |
| T-38-EGRESS | Information Disclosure | Digest read path | high | mitigate | Async on-device SQLite only; no network on any read path; no writer surfaces in Up Next/Horizon (D-07) |
| T-38-NOSCHEMA | Tampering | derive-only Digest | medium | mitigate | Digest composes from existing reads only; no new relationship-domain schema and no persisted Digest cache (D-02) — this plan writes nothing to the DB |

No package installs in this plan (no `T-38-SC` supply-chain surface).
</threat_model>

<verification>
- `npx vitest run src/components/digest src/screens/DigestScreen.test.tsx` green.
- `npx tsc --noEmit` clean; `npm run check:colors` green.
- On-device (§S.2/§S.6/§S.7/§S.8, consolidated in Plan 07): three-module Digest in order; Up Next ≤3 + Profile Back; Horizon subgroups + dedup + drill-through; error/empty states; long-text backstop under large font scale.
</verification>

<success_criteria>
The Digest tab renders Up Next → Horizon → Your Week composed from existing reads via the pure composition layer, honouring every resolved UI-consideration state (loading/empty/error/populated/zero-one-many/overflow/partial) with the long-text backstop held to device UAT; no Back button, product-label title, derive-only; all automated gates green.
</success_criteria>

<output>
Create `.planning/phases/38-your-week/38-06-SUMMARY.md` when done.
</output>
