---
phase: 20-contact-reconciliation-merge
plan: 04
type: execute
wave: 4
depends_on: [20-03]
files_modified:
  - src/db/reconcile-session-dao.ts
  - src/db/reconcile-session-dao.test.ts
  - src/db/reconcile-session-read.ts
  - src/db/reconcile-session-read.test.ts
  - src/logic/reconcile-bulk-eligibility.ts
  - src/logic/reconcile-bulk-eligibility.test.ts
  - src/components/CandidateCardGrid.tsx
  - src/components/candidate-card-grid-actions.ts
  - src/components/candidate-card-grid-actions.test.ts
  - src/screens/ReconcileGridScreen.tsx
  - src/screens/ReconcileCompleteScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
requirements: [RCN-02]
autonomous: true
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Settings → Contacts Integration gains a `Check linked contacts` row that scans all linked Orbit contacts and produces one review workflow + completion summary (RCN-02 / Cluster E, F, S)."
    - "NO SILENT ABANDON OF UNRESOLVED WORK (cycle-4 finding 3, safe default chosen) — `Check linked contacts` does NOT blindly create a new session when an unresolved one already exists: BEFORE scanning it reads `getNewestPendingReconcileSessionId(exec)`; if a pending session exists it does NOT silently start a second scan (which the resume-newest/sweep-older read would later silently discard), it instead routes the user to Resume-or-Discard the existing check first (the Resume/Discard prompt + wiring land in 20-05, which owns the reconcile resume UX; 20-04 provides the detection + the block so a new scan cannot silently abandon pending review work). This is the safe default; it does not reverse a locked decision (the import resume-newest/sweep-older pattern is a reused idiom, not an ADR/HANDOFF ruling)."
    - "The reconciliation workspace reuses the shipped CandidateCardGrid — EXTENDED ADDITIVELY, not forked: the `BulkAction` union and `actionLabels` gain `keep-orbit` and `use-contact-values` members, and the hardcoded `scoring` label (`Checking for matches…`) becomes a caller-supplied prop so the reconcile scan can read `Checking linked contacts…`. The existing import call sites keep passing only their original actions and are unaffected. No parallel grid is built (RCN-02 / Cluster H). CandidateCardGrid.tsx is in files_modified."
    - "The grid card chip is extended ADDITIVELY for reconcile (reviewer MEDIUM finding 5): CandidateCardGrid currently always renders `ConfidenceChip` from `item.outcome`, whose labels are import-specific ('Probable match'/'Needs review'/…). An optional additive prop (e.g. `chipLabel?: string` per card, or a `reconcileOutcome` mapping) is added so a reconcile card shows its own colourless advisory chip — 'Add' / 'Keep Orbit' / 'Manual review' / 'Source missing' (UI-SPEC:216-218) — mapped from `classifyReconciliation` in ReconcileGridScreen; when the prop is absent the grid renders the existing `ConfidenceChip` unchanged, so import call sites are unaffected and reconcile cards never show misleading import copy."
    - "Unchanged contacts never appear as cards — they exist only in the summary count; multiple source links for one Orbit person combine into one card (Cluster F, M)."
    - "PHOTO IS MANUAL-ONLY IN BULK (cycle-4 finding 7) — the photo family is NEVER auto-applied by bulk `Apply Recommendations` (nor by `Use Contact Values`): `classifyReconciliation` emits no auto-recommended/additive outcome for photo (photo requires an explicit side-by-side manual choice per Cluster Q / 20-03), so a photo change is only ever resolved through `onInspect` → ReconcileDetail's PhotoChoice. The bulk reducer explicitly excludes the photo family from every bulk auto-apply pass; a card whose ONLY change is a photo is not additive-only for `Use Contact Values` and its photo is left for manual review."
    - "Bulk actions: `Apply Recommendations` (recommendationExcludes: 'needs_review' keeps conflicts unresolved) and `Keep Orbit Values` are always available. `Use Contact Values` additive-only safety is enforced through the LIVE SELECTION, which lives INSIDE `CandidateCardGrid` (it owns `selectedIds` internally at :70 and computes `selectedItems` at :81 — VERIFIED — and exposes no selection to the parent, so the parent CANNOT gate the action by conditionally including it in the static `bulkActions` array, cycle-4 HIGH #2). Instead the grid is EXTENDED with a per-action eligibility predicate prop `isActionEligible?: (action: BulkAction, selectedItems: CandidateItem[]) => boolean` (additive, optional; absent = every action eligible, so import call sites are unaffected). The grid routes BOTH the sheet render and `runBulkAction` through ONE small PURE helper `isBulkActionAvailable(action, selectedItems, isActionEligible)` (returns `isActionEligible ? isActionEligible(action, selectedItems) : true`) exported from a NEW RN-FREE sibling module `src/components/candidate-card-grid-actions.ts` (no react-native/expo imports, so vitest imports it directly — mirroring the existing pure `field-def-form-logic.ts`): (1) the action sheet HIDES/DISABLES an action when `isBulkActionAvailable(...)` is false, and (2) `runBulkAction` RE-CHECKS `isBulkActionAvailable(...)` and returns early (never calls `onBulkAction`) for an unavailable action — defense in depth, not UI-only. Extracting the helper into a pure sibling makes the gate node-testable without an RN render harness (none exists in this repo). `ReconcileGridScreen` passes a predicate that, for `use-contact-values`, returns `isAdditiveOnlySelection(<the selected cards' reconcile outcomes>)`, so `Use Contact Values` is unavailable AND un-runnable the instant the live selection contains any conflict / removed-from-source / missing-source card (Cluster I/J — safety invariant realized at the live selection, not a static array)."
    - "A durable reconciliation session persists unresolved cards and survives process death: reopening the DB and resuming yields the same unresolved work; already-applied resolutions stay applied (RCN-02 / Cluster R, concurrency edge)."
    - "The persisted card's `diff_json` carries the FRESHNESS TOKEN 20-03 defines — the per-field scan-time Orbit baseline value + the contact's `modified_at` at scan — so a card applied later (this bulk path, or a 20-05 resume after process death) can REVALIDATE against the current Orbit state in the shared `applyReconcileSelections` helper and SKIP any field the user edited in Orbit after the scan, never silently overwriting it (reviewer HIGH #1). The bulk apply is the highest-risk window (large scan→apply gap), so this is where the freshness token most matters; bulk and detail both inherit the check via the shared helper."
    - "A card's persisted `diff_json` stores, for its ONE collapsed source photo (one-photo-per-card, 20-03 — the highest-priority active link's photo, other sources not staged), the DURABLE staged reference (the `reconcile-staging/` relative path + content hash from 20-03's `stageReconcileSourcePhoto`) — NEVER the evictable `photoTempUri`. The bulk scan stages that single source photo to durable staging BEFORE writing the card, so a resumed card (20-05) renders and applies a real file, not a vanished cache path (reviewer HIGH #2). The durable staged relative path is written to the first-class nullable `staged_photo_rel_path` column on the card (via `insertReconcileCardCore`), and because there is exactly ONE staged photo per card, 20-05's orphan-staging sweep can enumerate staged files by that column alone without parsing any card's `diff_json` and cannot orphan a second live staged option (reviewer LOW finding 9 + one-photo-per-card collapse)."
    - "The `Use Contact Values` additive-only eligibility is a NAMED, PURE helper `isAdditiveOnlySelection(cards)` (src/logic/reconcile-bulk-eligibility.ts) with a DEDICATED unit test proving it returns true ONLY when every selected card is additive-only and EXCLUDES any selection containing a conflict / removed-from-source / missing-source card — not a grep-only or device-UAT-only check (Cluster J safety invariant, reviewer finding 4). It is CONSUMED via the grid's `isActionEligible` predicate (HIGH #2) so the gate runs against the grid's live internal selection. Because the toolchain has NO RN component-render harness (no react-test-renderer / RNTL — VERIFIED absent in package.json), the grid's gating is made node-testable by extracting a PURE helper `isBulkActionAvailable(action, selectedItems, isActionEligible)` into an RN-free sibling module (`src/components/candidate-card-grid-actions.ts`) that BOTH the action-sheet render (to filter/disable) AND `runBulkAction` (to early-return) call, with a dedicated vitest proving `use-contact-values` is UNAVAILABLE for a selection the predicate reports non-additive and available for an all-additive one — so the block is proven by a real unit test, not only grep + device-UAT."
    - "`getNewestPendingReconcileSessionId(exec)` is a corruption-tolerant read that identifies the newest pending session's id WITHOUT parsing any card's `diff_json` payload, so a discard-only resume descriptor can be built even when card-payload parsing itself fails (consumed by 20-05's sweep; reviewer finding 7)."
    - "The scan builds ALL cards atomically: the session row and every card are written in ONE inWriteTransaction AFTER the full scan+classify completes, so a crash mid-scan leaves NO committed session (no misleading half-scanned 'pending' session that resume cannot distinguish from a complete review). The session is resumable only once fully built. The outer txn composes the NON-mutexed `createReconcileSessionCore` + `insertReconcileCardCore` (never the self-wrapping `createReconcileSession` wrapper, which would nest inWriteTransaction and deadlock — reviewer finding 4)."
    - "A partially-resolved card remains in the grid with a remaining-count hint until all actionable differences resolve (Cluster K)."
    - "The completion summary reuses the footerEntry idiom with Checked / Changed / Updated / Kept Orbit values / Source missing counts; a zero-change run still shows a calm summary, not an error (Cluster S)."
    - statement: "Per-card failure isolation is the CALLER's responsibility, not the shipped grid's: the grid's runBulkAction catch marks the WHOLE selection failed on any thrown onBulkAction (CandidateCardGrid.tsx:106-121), so onBulkAction MUST apply each card in its own try/catch, record per-card failures in card state, and RESOLVE (never throw for a single-card failure); a thrown onBulkAction is reserved for a catastrophic whole-batch failure and marks the whole selection retryable (E3 error backstop)."
      verification: backstop
    - statement: "Completion count rows pluralize (1 interaction vs n) — E8 zero-one-many backstop."
      verification: backstop
    - statement: "RCN-02 has an unclassified edge-probe row (category unclassified) with no defensible acceptance criterion beyond the durable-session and multi-source truths above; treated as a flagged planner assumption, not silently dropped."
      verification: backstop
  prohibitions:
    - "must NEVER surface `Use Contact Values` for a selection containing any conflicting/removal card (would silently overwrite Orbit names/birthdays)"
    - "must NEVER fork a second review grid or a second session store"
  artifacts:
    - src/db/reconcile-session-dao.ts
    - src/db/reconcile-session-read.ts
    - src/screens/ReconcileGridScreen.tsx
    - src/screens/ReconcileCompleteScreen.tsx
  key_links:
    - "Settings 'Check linked contacts' → scan → ReconcileGridScreen (CandidateCardGrid) → onInspect ReconcileDetail → ReconcileCompleteScreen"
    - "reconcile-session-dao mirrors import-session-dao core/wrapper + assertOneChange + finalize/discard"
    - "reconcile-session-read.getResumableReconcileSession mirrors import-session-read resume-newest/sweep-older"
---

<objective>
**As a** user with many linked contacts, **I want to** check them all at once in one card-grid workspace and resolve them at my own pace, **so that** I can catch every source change without losing my place if I leave mid-review.

This slice adds the bulk `Check linked contacts` flow on top of the per-contact reconciliation from 20-03: the durable/resumable session store (mirroring the shipped `import_sessions` model), the reused `CandidateCardGrid` review workspace with its locked additive-only bulk-action safety seam, and the completion summary. Durable resume across process death and missing-source/relink are completed in 20-05.

Purpose: reuse the shipped review workspace + durable-session pattern (not a parallel UI system, not a bespoke persistence) so the invariants Phase 19 proved carry over.
Output: `reconcile-session-dao`, `reconcile-session-read`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, and the Settings entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile-session-dao (durable session + cards state machine)</name>
  <files>src/db/reconcile-session-dao.ts, src/db/reconcile-session-dao.test.ts</files>
  <read_first>
    - src/db/import-session-dao.ts (109-119 assertOneChange; 121 createImportSession; 172-198 core/wrapper duality; 404 finalizeSessionIfTerminal; 431 discardSession — the exact idioms to mirror)
    - src/db/migrations/013-reconciliation-and-merge.ts (reconciliation_sessions + reconciliation_session_cards — from 20-01: status CHECK, card_status CHECK IN unresolved/partial/resolved/missing_source, diff_json, unresolved_count, UNIQUE(session_id, contact_id))
    - src/db/transaction.ts (inWriteTransaction — shared non-reentrant mutex; never nest)
    - src/logic/reconcile-diff.ts (the classified diff persisted as diff_json)
  </read_first>
  <action>
    Create `src/db/reconcile-session-dao.ts` mirroring import-session-dao. Provide BOTH a non-mutexed `createReconcileSessionCore(exec, { uid, totalChecked, now })` (returns the new session id/uid; NO inWriteTransaction of its own) AND a thin `createReconcileSession(exec, …)` wrapper that self-wraps `inWriteTransaction`. The atomic bulk-scan build (Task 3) MUST use `createReconcileSessionCore` inside its single outer `inWriteTransaction` — calling the self-wrapping `createReconcileSession` there would nest inWriteTransaction and PERMANENTLY HANG (the mutex is non-reentrant — VERIFIED transaction.ts; reviewer MEDIUM finding 4). Also provide a per-card insert `insertReconcileCardCore(exec, { sessionUid|sessionId, contactId, cardStatus, diffJson, unresolvedCount, stagedPhotoRelPath, now })` (writes the nullable `staged_photo_rel_path` column from migration 013 — finding 9); a `markCardStatusCore(exec, cardId, status, unresolvedCount, now)` using `assertOneChange`; `finalizeSessionIfTerminal(exec, sessionId, now)` — the DERIVED terminal rule (NO session-level counter): mark the session `complete` when the session has ZERO non-terminal cards, where a card is TERMINAL when `card_status IN ('resolved','missing_source')` and NON-TERMINAL (blocks completion) when `card_status IN ('unresolved','partial')`, via `SELECT COUNT(*) FROM reconciliation_session_cards WHERE session_id = ? AND card_status NOT IN ('resolved','missing_source')`. This is the SAME predicate 20-01's merge step 4c inlines after deleting an absorbed card, so the two paths finalize identically; and `discardSession(exec, sessionId, now)` (mark discarded, delete unresolved cards, return any staged paths for post-commit cleanup). Each mutation ships a non-mutexed `*Core` plus a thin inWriteTransaction wrapper. All values `?`-bound; table names literal. Write `src/db/reconcile-session-dao.test.ts` proving create/insert/mark transitions, unresolved_count bookkeeping, finalize-on-zero-unresolved, and discard deletes only unresolved cards.
  </action>
  <verify>
    <automated>npx vitest run src/db/reconcile-session-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/reconcile-session-dao.ts` contains BOTH `export ... createReconcileSessionCore(` (non-mutexed) and the `createReconcileSession` wrapper, plus `markCardStatusCore` using assertOneChange. `insertReconcileCardCore` accepts and writes `staged_photo_rel_path`.
    - `npx vitest run src/db/reconcile-session-dao.test.ts` exits 0 with create/mark/finalize/discard cases.
    - Card status values are constrained to unresolved/partial/resolved/missing_source (matches the 013 CHECK).
  </acceptance_criteria>
  <done>The durable reconciliation session + card state machine writes atomically and finalizes/discards correctly.</done>
</task>

<task type="auto">
  <name>Task 2: reconcile-session-read (resume-newest/sweep-older + completion counts)</name>
  <files>src/db/reconcile-session-read.ts, src/db/reconcile-session-read.test.ts</files>
  <read_first>
    - src/db/import-session-read.ts (54-67 mapSession db-row→domain mapper; 109-127 getResumableSession resume-newest/discard-older; 188-210, 226-281 grouped count queries)
    - src/db/reconcile-session-dao.ts (Task 1 — discardSession reused by the sweep)
    - src/db/migrations/013-reconciliation-and-merge.ts (reconciliation_sessions/status)
  </read_first>
  <action>
    Create `src/db/reconcile-session-read.ts` mirroring import-session-read. `getResumableReconcileSession(exec, now)` selects the newest `status='pending'` session (ORDER BY created_at DESC, id DESC), discards older pending sessions via discardSession, and returns the newest session plus any swept staged paths. ALSO export `getNewestPendingReconcileSessionId(exec)` — a corruption-tolerant read that returns just the newest pending session's id via a single `SELECT id FROM reconciliation_sessions WHERE status='pending' ORDER BY created_at DESC, id DESC LIMIT 1`, touching NO card `diff_json` payload, so 20-05's sweep can construct a discard-only descriptor even when a card payload is unparseable (reviewer finding 7). Add grouped count reads for the completion buckets — `Checked`, `Changed`, `Updated`, `Kept Orbit values`, `Source missing` — and a `mapReconcileSession` row→domain mapper. All `?`-bound. Write `src/db/reconcile-session-read.test.ts` proving: durable session survives a simulated process death (write, close, reopen the node:sqlite DB, resume the same unresolved cards); newest-resumed/older-swept; the completion counts; and that `getNewestPendingReconcileSessionId` returns the newest pending id without reading any diff_json.
  </action>
  <verify>
    <automated>npx vitest run src/db/reconcile-session-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/reconcile-session-read.ts` contains `export function getResumableReconcileSession(` (or async), `export ... getNewestPendingReconcileSessionId(`, and the grouped count reads.
    - `npx vitest run src/db/reconcile-session-read.test.ts` exits 0 including a reopen-DB-and-resume (process-death) case, a newest-resume/older-sweep case, and a `getNewestPendingReconcileSessionId` case (returns the newest pending id, reads no diff_json).
  </acceptance_criteria>
  <done>Resume-newest/sweep-older and completion-bucket counts work and survive process death.</done>
</task>

<task type="auto">
  <name>Task 3: Bulk reconcile UI — Settings entry, grid workspace, completion summary</name>
  <files>src/components/CandidateCardGrid.tsx, src/screens/ReconcileGridScreen.tsx, src/screens/ReconcileCompleteScreen.tsx, src/screens/SettingsScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/components/CandidateCardGrid.tsx (18-22 `BulkAction` union = link|import-new|skip|apply-recommendation; 54-59 `actionLabels` fixed record; 95-102 apply-recommendation filters needs_review; 124-130 `scoring` state with HARDCODED `Checking for matches…`; 106-121 runBulkAction catch marks the WHOLE batch failed on a thrown onBulkAction) — EXTEND ADDITIVELY: add `keep-orbit` + `use-contact-values` to the union and actionLabels; make the scoring label a caller-supplied prop (default keeps `Checking for matches…` so import call sites are unchanged). Do NOT fork a second grid.
    - src/components/CandidateCardGrid.tsx (the per-card `ConfidenceChip` render from `item.outcome`, ~:187) + src/components/ConfidenceChip.tsx (:4-16 import-specific `ConfidenceOutcome` labels) — ADD an optional additive per-card chip prop (`chipLabel?: string` or a reconcile-outcome variant) so a reconcile card renders its own colourless advisory chip instead of the import ConfidenceChip; absent prop = existing ConfidenceChip unchanged (import call sites unaffected). Map `classifyReconciliation` → 'Add'/'Keep Orbit'/'Manual review'/'Source missing' in ReconcileGridScreen (finding 5).
    - src/screens/ImportCompleteScreen.tsx (141-220, 316-324 footerEntry count-row idiom)
    - src/screens/SettingsScreen.tsx (736-757 Contacts Integration section — add `Check linked contacts` beside `Import contacts`, reuse section/sectionHeading idiom)
    - src/db/reconcile-session-dao.ts + src/db/reconcile-session-read.ts (Tasks 1-2)
    - src/logic/reconcile-diff.ts + src/screens/ReconcileDetailScreen.tsx (20-03 — onInspect target with `{ contactId, sessionId, cardId }` params; the classified outcome union types both the eligibility helper and the reconcile chip mapping; REUSE the shared apply helper 20-03 extracts — do NOT re-implement the writer sequence for bulk, so bulk and detail cannot drift and both carry the HIGH #1 data_revision bump + HIGH #2 post-commit photo step)
    - src/db/reconcile-apply.ts (20-03 — `applyReconcileSelections`, the shared in-txn writer composition to REUSE from bulk `onBulkAction`; do NOT re-implement the writer sequence) and src/db/data-revision-dao.ts (`bumpDataRevisionCore` — the shared helper bumps on a scalar change so a bulk scalar-only apply is backup-visible — HIGH #1)
    - src/services/photos/reconcile-photo.ts (20-03 — `stageReconcileSourcePhoto`: stage each source photo to durable `reconcile-staging/` + content hash BEFORE writing the card's diff_json, so the persisted card never holds a temp URI)
    - modules/orbit-contact-picker/index.ts (14-24 photoTempUri is evictable; readAllContacts for the bulk scan)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surfaces #2 scan, #3 grid, #8 completion; Cluster J `Use Contact Values` additive-only; recommendation chips colourless)
  </read_first>
  <action>
    Extend `src/components/CandidateCardGrid.tsx` additively FIRST: add `keep-orbit` and `use-contact-values` to the `BulkAction` union and to `actionLabels` (`Keep Orbit Values` / `Use Contact Values`), replace the hardcoded `scoring` label with an optional `scoringLabel?: string` prop defaulting to `Checking for matches…`, add an optional additive per-card chip prop (`chipLabel?: string` / reconcile-outcome) so reconcile cards render a colourless 'Add'/'Keep Orbit'/'Manual review'/'Source missing' advisory chip instead of the import `ConfidenceChip`; absent prop keeps the existing ConfidenceChip render (so import call sites stay identical — finding 5), AND add an optional per-action eligibility predicate prop `isActionEligible?: (action: BulkAction, selectedItems: CandidateItem[]) => boolean` (HIGH #2): when present, the action sheet render HIDES/DISABLES any `action` for which `isActionEligible(action, selectedItems)` is false (using the grid's OWN internal `selectedItems` at :81 — the live selection the parent cannot see), and `runBulkAction` RE-CHECKS the predicate and returns early WITHOUT calling `onBulkAction` for an ineligible action (defense in depth, not UI-only); absent prop = every action eligible, so the import call sites are unchanged. This is the mechanism that makes the additive-only `Use Contact Values` seam realizable through the grid, which owns its selection internally (VERIFIED :70/:81). Add a `Check linked contacts` row to the SettingsScreen Contacts Integration section; its handler FIRST reads `getNewestPendingReconcileSessionId(exec)` and, when a pending session exists, does NOT start a new scan — it routes to Resume-or-Discard the existing check (finding 3; the Resume/Discard prompt is wired in 20-05, so for wave 4 the handler at minimum blocks the new scan and surfaces that an unfinished check exists rather than silently creating a second session). Create the NAMED pure helper `src/logic/reconcile-bulk-eligibility.ts` exporting `isAdditiveOnlySelection(cards)` — returns true ONLY when every selected card is additive-only, and false if ANY selected card contains a conflict / removed-from-source / missing-source outcome (reads the classified outcome union from reconcile-diff.ts; no I/O). Write `src/logic/reconcile-bulk-eligibility.test.ts` proving: all-additive → true; a single conflict card → false; a removed-from-source card → false; a missing-source card → false; empty selection → false. Create `src/screens/ReconcileGridScreen.tsx`: on entry, run the scan+classify FIRST fully in memory (CandidateCardGrid `scoring` state with `scoringLabel="Checking linked contacts…"`, never a frozen blank) — read all linked Orbit contacts, `readAllContacts` their sources, apply the ONE-PHOTO-PER-CARD COLLAPSE (20-03) — stage ONLY the photo of the highest-priority active source link (first active `external_contact_link` by ascending link id with a photo) via `stageReconcileSourcePhoto` (durable staging + content hash), never the other sources' photos — so the card's single `staged_photo_rel_path` column + `diff_json` carry ONE durable staged reference NEVER the temp URI, classify each via reconcile-diff and CAPTURE the freshness token per surfaced field (the scan-time Orbit baseline value + the contact's `modified_at`) INTO the card's `diff_json` so a later/resumed apply can revalidate (HIGH #1) — THEN create the reconcile session and insert every CHANGED-contact card in ONE inWriteTransaction (atomic: a crash mid-scan commits no session, so no half-scanned resumable session; unchanged contacts never become cards; multiple source links → one card). Render the extended `CandidateCardGrid`. Wire `onInspect` → `ReconcileDetailScreen` with route params `{ contactId, sessionId, cardId }` (finding 7 — the full nav contract, not contactId alone) and register a focus/return refresh (a navigation focus listener or an onResolved callback param) that re-reads the card's status from `reconciliation_session_cards` on return and updates the grid's remaining-count hint (`1 of 3 left`) WITHOUT rescanning all contacts; on return call `markCardStatusCore(cardId, resolved|partial, remainingUnresolvedCount, now)` so a partial resolution from detail persists durably. Pass `bulkActions` = all three reconcile actions (`Apply Recommendations` using apply-recommendation with `recommendationExcludes: 'needs_review'`, `Keep Orbit Values`, and `Use Contact Values`) AND pass an `isActionEligible` predicate (HIGH #2) that returns `true` for `apply-recommendation`/`keep-orbit` and, for `use-contact-values`, returns `isAdditiveOnlySelection(<selectedItems mapped to their reconcile outcomes>)` — the grid then hides/disables and blocks `Use Contact Values` against its OWN live selection whenever that selection is not additive-only. The predicate closes over the card→reconcile-outcome map ReconcileGridScreen builds from `classifyReconciliation`, so it can resolve each selected `CandidateItem` to its outcome. Do NOT gate by conditionally omitting the action from the static `bulkActions` array (the parent cannot see the live selection — VERIFIED :70/:81). BULK APPLY CONTRACT (finding 6): the three reconcile bulk actions MUST reuse the SAME shared apply helper 20-03 extracts for ReconcileDetailScreen (identical writer composition: complete `UpdateContactFullInput`→updateContactMetadataCore, seed∪accepted→applyContactMethodDiffCore, per-link upsertReviewedSnapshotCore, `bumpDataRevisionCore` on any durable write — HIGH #1, and the POST-COMMIT photo promote/persist — HIGH #2) so bulk and detail cannot drift. Per action: `Apply Recommendations` applies only the recommended (additive) NON-PHOTO fields per card (the PHOTO family is NEVER auto-applied in bulk — it has no auto-recommendation and stays manual-only via ReconcileDetail's PhotoChoice, finding 7), writes reviewed snapshots for every reviewed family, leaves `needs_review` conflicts AND any photo change unresolved (card → partial with a remaining count), increments the `Updated` bucket; `Keep Orbit Values` writes the reviewed snapshots WITHOUT applying any source value (no Orbit scalar/method/photo changes), marks the card resolved, increments the `Kept Orbit values` bucket; `Use Contact Values` (additive-only-gated) applies every additive source value per card atomically, marks resolved, increments `Updated`. Each action updates `unresolved_count`/`card_status` via `markCardStatusCore` and the completion buckets accordingly. `onBulkAction` applies each selected card in its OWN try/catch, marks per-card failures in card state, and RESOLVES (never throws for a single-card failure — a thrown onBulkAction would mark the whole selection failed via the grid's failedIds; reserve that for a catastrophic failure). POST-COMMIT (best-effort) `deleteReconcileStaging` any source photo staged during the scan but not chosen by the applied action, so a completed bulk review does not leak staging files (finding 10). Create `src/screens/ReconcileCompleteScreen.tsx` using the footerEntry idiom: heading `Check complete`, rows `Checked (n)` / `Changed (n)` / `Updated (n)` / `Kept Orbit values (n)` / `Source missing (n)`, actions `View unresolved` (shown only when unresolved > 0) and `Done` (accent); a zero-change run still shows the summary. Register both screens in RootNavigator + types. All colours via tokens; count rows pluralize.
  </action>
  <verify>
    <automated>npx vitest run src/logic/reconcile-bulk-eligibility.test.ts src/components/candidate-card-grid-actions.test.ts && npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: Settings → Check linked contacts scans the linked contacts, shows only changed contacts as cards, resolving some then leaving keeps the rest; the completion summary shows correct counts; `Use Contact Values` is absent when a conflict card is selected. (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/CandidateCardGrid.tsx` `BulkAction` union includes `keep-orbit` and `use-contact-values` and `actionLabels` includes `Keep Orbit Values`/`Use Contact Values`; the scoring label is a prop (`grep -n "scoringLabel" src/components/CandidateCardGrid.tsx` shows it) and the import call sites still typecheck unchanged.
    - `src/screens/ReconcileGridScreen.tsx` imports (not re-implements) the extended `CandidateCardGrid`, passes `scoringLabel="Checking linked contacts…"`, passes reconcile chip labels (finding 5), and wires `onInspect` to ReconcileDetail with `{ contactId, sessionId, cardId }` (finding 7 — `grep -n "sessionId\|cardId" src/screens/ReconcileGridScreen.tsx` shows the full nav params) plus a focus/callback refresh that persists the returned resolved/partial status via `markCardStatusCore` and updates the remaining-count hint without a full rescan.
    - The three reconcile bulk actions reuse the SHARED apply helper (finding 6 — same writer composition as ReconcileDetailScreen, incl. `bumpDataRevisionCore` and the post-commit photo step): `grep -n` the shared apply helper is invoked from `onBulkAction` (not a re-implemented inline writer sequence); a bulk-apply integration test asserts, per action, which fields/snapshots are written and how the completion buckets increment.
    - Additive per-card chip prop is used for reconcile and import call sites are unchanged: `grep -n "chipLabel\|ConfidenceChip" src/components/CandidateCardGrid.tsx` shows the optional prop gating the reconcile chip vs the default ConfidenceChip (finding 5).
    - `src/screens/SettingsScreen.tsx` contains a `Check linked contacts` row whose handler reads `getNewestPendingReconcileSessionId` and does NOT start a new scan while a pending session exists (`grep -n "getNewestPendingReconcileSessionId" src/screens/SettingsScreen.tsx` shows the pre-scan guard — finding 3; the Resume/Discard routing completes in 20-05).
    - Session + all cards are written in one transaction after scan completes, via the NON-mutexed core (not the self-wrapping wrapper — finding 4): `grep -n "createReconcileSessionCore\|insertReconcileCardCore" src/screens/ReconcileGridScreen.tsx` shows both invoked within a single inWriteTransaction, and `grep -n "createReconcileSession(" src/screens/ReconcileGridScreen.tsx` does NOT show a bare wrapper call inside the transaction; the session is not created before the scan loop.
    - HIGH #2 — `Use Contact Values` is gated through the grid's live selection, not a static array: `CandidateCardGrid` exposes `isActionEligible?: (action, selectedItems) => boolean` and routes both the action-sheet render and `runBulkAction` through the pure helper `isBulkActionAvailable(action, selectedItems, isActionEligible)` in the RN-free sibling `src/components/candidate-card-grid-actions.ts` (`grep -n "isActionEligible\|isBulkActionAvailable" src/components/CandidateCardGrid.tsx` shows the prop AND the helper used in BOTH the sheet render (hide/disable) and `runBulkAction` (early return before `onBulkAction`)). A dedicated vitest `src/components/candidate-card-grid-actions.test.ts` over `isBulkActionAvailable` proves `use-contact-values` is UNAVAILABLE when `isActionEligible` reports the selection non-additive and available for an all-additive selection (node-testable — no RN render harness needed). `grep -n "isAdditiveOnlySelection\|isActionEligible\|use-contact-values" src/screens/ReconcileGridScreen.tsx` shows the predicate wiring `use-contact-values` to `isAdditiveOnlySelection` (not a conditional `bulkActions` array); `onBulkAction` wraps each card in its own try/catch (no bare throw for a single-card failure).
    - `src/logic/reconcile-bulk-eligibility.ts` contains `export function isAdditiveOnlySelection(`; `npx vitest run src/logic/reconcile-bulk-eligibility.test.ts` exits 0 with all-additive→true and conflict/removed/missing/empty→false cases.
    - The bulk scan stages source photos before persisting cards: `grep -n "photoTempUri" src/screens/ReconcileGridScreen.tsx` shows it ONLY passed into `stageReconcileSourcePhoto` (never written into diff_json); the card payload carries the durable staged path/hash AND the freshness token (scan-time Orbit baseline + modified_at — HIGH #1).
    - Photo is manual-only in bulk (finding 7): the bulk reducer excludes the photo family from `apply-recommendation` and `use-contact-values`; the bulk-apply integration test asserts a photo change is left unresolved (card → partial) after a bulk auto-apply pass and is never written by a bulk action.
    - `ReconcileCompleteScreen` uses the footerEntry count rows; `npx tsc --noEmit`, `npm run check:colors`, `npm test` pass.
  </acceptance_criteria>
  <done>Bulk `Check linked contacts` runs in the reused grid with durable sessions, the additive-only safety seam, and a completion summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android Contacts (source) → Orbit (local) | Bulk scan reads many linked contacts one-way; a bulk action must never overwrite Orbit conflicts. |
| in-flight review state → durable DB | Unresolved review work must survive process death without corruption or duplication. |

> No network on any read path. No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04-1 | Repudiation / Tampering | `Use Contact Values` bulk action | high | mitigate | Gated through the grid's LIVE internal selection via the `isActionEligible` predicate + pure `isBulkActionAvailable` helper (hidden in the sheet AND blocked in `runBulkAction`) whenever the selection is not additive-only — realizable because the grid owns selection internally (:70/:81); the parent cannot and does not gate by a static array (reviewer HIGH #2). `recommendationExcludes:'needs_review'` additionally keeps conflicts unresolved — no bulk source-wins over conflicts. |
| T-20-04-2 | Tampering / DoS | durable session persistence | medium | mitigate | Mirror the proven import_sessions model: assertOneChange, resume-newest/sweep-older, discard deletes only unresolved cards — no half-written session. Scan builds session+all cards in ONE transaction after classify completes, so a crash mid-scan commits nothing (no misleading half-scanned resumable session). |
| T-20-04-3 | Tampering | session SQL | low | mitigate | All values `?`-bound; literal table names; no identifier interpolation (ASVS V5). |
| T-20-04-4 | Data loss / Availability | photo reference in durable diff_json | high | mitigate | The bulk scan stages each source photo to durable `reconcile-staging/` (content hash) BEFORE writing the card, so `diff_json` (and the first-class `staged_photo_rel_path` column) holds a durable reference, never the evictable `photoTempUri`; a resumed card renders/applies a real file (reviewer HIGH #2 / finding 9). |
| T-20-04-5 | Data integrity | bulk apply escapes backup | high | mitigate | Bulk actions reuse the shared apply helper, which calls `bumpDataRevisionCore` on any durable write, so a bulk scalar-only apply advances the revision the backup sweep gates on and cannot be omitted from the next automatic backup (reviewer HIGH #1). |
| T-20-04-6 | Availability | non-reentrant transaction deadlock | medium | mitigate | The atomic session+cards build composes the NON-mutexed `createReconcileSessionCore` inside one outer `inWriteTransaction`; the self-wrapping `createReconcileSession` is never called there, so the non-reentrant mutex cannot deadlock (reviewer finding 4). |
</threat_model>

<verification>
- `npx vitest run src/db/reconcile-session-dao.test.ts src/db/reconcile-session-read.test.ts` green; `npm test` green after the wave.
- `npx tsc --noEmit`, `npm run check:colors` clean.
- Device-UAT (end-of-phase): bulk check + partial resolve + summary on the Pixel.
</verification>

<success_criteria>
- Bulk reconciliation reuses the shipped grid + durable-session pattern with the additive-only safety seam.
- Unchanged contacts stay out of the grid; multi-source collapses to one card; partial cards persist.
- Completion summary is calm and correct including the zero-change run.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`, `bulk_review_resolutions`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` (merge-dao.ts); `setContactPhotoCore` (contacts-dao.ts, 20-01); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` (bulk-review-dao.ts).
- **Pure logic:** `classifyReconciliation` + desired-method-list helper (reconcile-diff.ts); `isAdditiveOnlySelection` (src/logic/reconcile-bulk-eligibility.ts — the named additive-only-eligibility helper, dedicated test); `recommendSurvivor` (survivor-recommendation.ts).
- **Reads:** `getNewestPendingReconcileSessionId` (reconcile-session-read.ts — corruption-tolerant newest-pending-id read for discard-only resume, 20-04).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`, `ResumeReconcilePrompt` (20-05); shipped `CandidateCardGrid` EXTENDED additively — new `BulkAction` members `keep-orbit`/`use-contact-values`, their labels, a `scoringLabel` prop, an optional per-card reconcile `chipLabel` prop (finding 5), and an optional `isActionEligible(action, selectedItems)` predicate prop that gates a bulk action against the grid's LIVE internal selection (HIGH #2); the pure gating helper `isBulkActionAvailable` lives in the RN-free sibling `src/components/candidate-card-grid-actions.ts` (node-testable) (20-04).
- **Bulk-apply:** `createReconcileSessionCore` (non-mutexed, composes inside the outer scan txn — finding 4) alongside `createReconcileSession`; the SHARED `applyReconcileSelections` helper (src/db/reconcile-apply.ts, extracted in 20-03) reused by both ReconcileDetailScreen and bulk `onBulkAction` (finding 6), bumping `data_revision` on a scalar change (HIGH #1) and applying photos post-commit (HIGH #2).
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-apply.test.ts (incl. HIGH #1 freshness), reconcile-bulk-eligibility.test.ts, candidate-card-grid-actions.test.ts (HIGH #2 gating), reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-04-SUMMARY.md` when done.
</output>
