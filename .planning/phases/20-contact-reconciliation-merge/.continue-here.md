---
context: phase
phase: 20-contact-reconciliation-merge
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-31T08:42:50.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Verify every subagent claim against the live DB / actual code — never trust a subagent summary.** Subagents this session were accurate, but each PASS/FAIL was independently re-checked via a WAL-aware `run-as` DB read before it counted. Keep doing this.
- [ ] CONSTRAINT: **Bounded, non-opaque device delegation only.** Drive the device via bounded per-task subagents with explicit steps + required evidence; the orchestrator stays lean and verifies. Do NOT dispatch an open-ended "do the whole UAT" agent (that was the original failure).
- [ ] CONSTRAINT: **Never touch the owner's real data.** Orbit contacts 1–11 include real family PII (c3 Andrew Wales, c4 Joyce Milligan, c5 Dad, c6 Chris Wales). Android `com.google` contacts are real. Operate ONLY on `ZZ-UAT-` fixtures. Resolve IDs from the DB before any mutation.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| adb tap on wrong input source | RN gesture-handler buttons ignore plain `input tap`; grid cards need long-press | blocking | Use the taxonomy in MEMORY `adb-input-source-taxonomy` / 20-UAT.md: plain tap (chips/rows), `input touchscreen tap` (buttons/FAB/gear@y≈172), `input touchscreen swipe X Y X Y 800` (grid card select). |
| VCF import fails cross-app | Contacts app can't read a file:// handed to it | advisory | Create device-local fixtures via ContactsProvider `content insert` (null account) — MEMORY `device-local-contact-fixtures`. |
| Renaming a local contact to fake a conflict | Changes the lookup key → orphans the Orbit link → "Source missing" not a conflict | blocking | To test a conflict, change a METHOD VALUE (edit a phone) — never rename a local fixture (Finding A). |
| Native manifest edit needs a full rebuild | JS fast-refresh won't pick up a manifest/permission change; and the cap lived in TWO places | blocking | Any native/manifest change → `expo prebuild --clean` + `assembleDebug` on droid, `install -r`, and verify with `aapt2 dump badging` + `dumpsys package`. |

<current_state>
Phase 20 device UAT, in progress. **5 of 8 checks PASS and are independently DB-verified:** 0 (import), 1 (clean merge), 2 (conflict merge), 7a (birthday Fix), 7b (birthday Ignore). The original blocker — reconcile could not read contacts on API 37 — is **RESOLVED**: ADR-003 was owner-approved and implemented, a fresh debug APK ships `READ_CONTACTS` uncapped (granted on device), and the single-contact **additive** reconcile path works end-to-end (contact 13). Exercising the now-working reconcile surfaced three new correctness findings (Bugs B, C, and Finding A) that block the remaining scenarios.
</current_state>

<completed_work>
- Scenarios 0,1,2,7a,7b — PASS, DB-verified (details + evidence in `20-UAT.md`).
- ADR-003 (READ_CONTACTS on API 37) — written, implemented at BOTH enforcement points (module manifest + `plugins/withContactPickerPermission.js`), rebuilt on droid, installed, and verified: permission present + granted, "Update from Contacts" now prompts + reads. Automated suite green (185 files / 1783 tests, tsc, check:colors).
- Fixtures created on device (Orbit ids 7–17; device raw 1130–1141) — registry in `20-UAT.md`.
</completed_work>

<remaining_work>
1. **Bug B (blocker):** the bulk "Check linked contacts" scan crashes on the full 14-contact linked set. `ReconcileGridScreen.scan()` throws for one contact; the exception is swallowed by the silent `.catch()` at `ReconcileGridScreen.tsx:229-230` → generic "Could not check linked contacts". Per-contact path works. Needs instrument→build→reproduce→fix.
2. **Bug C (small):** `FieldChoiceGroup.tsx:60` keys options by `option.id`; multi-value families produce ≥2 options all `id="source"` → duplicate React key. Make keys unique (append value/index). Add a small unit test if feasible.
3. **Finding A (fixture redesign):** conflict fixtures must change a method value (phone), not rename a local contact.
4. **Scenarios 3 (conflict + no-re-nag), 4 (bulk check — after Bug B), 5 (process-death resume), 6 (source-missing/relink).**
5. **Close-out:** cleanup ZZ-UAT fixtures (Orbit + device), verify real data intact, write `20-06-SUMMARY.md`, run Phase 20 verification/completion gates, owner sign-off.
</remaining_work>

<decisions_made>
- ADR-003 supersedes ADR-002's "no READ_CONTACTS on API 37" clause for reconcile (owner-approved after verifying current Play policy permits it for CRM via declaration). Import unchanged.
- All device/build work is delegated to bounded subagents; orchestrator verifies DB claims.
</decisions_made>

<blockers>
- Bug B: root cause not yet pinned (silent catch, no trace). Instrument in a debug build to find the throwing contact.
- Play declaration (non-blocking): file at release time (CRM category).
</blockers>

## Recommended cadence for the next session (subagent split)
The owner suggested: (1) Bug B debug+fix, (2) Bug C fix + build, (3) tests. Refined split (Bug B's diagnosis needs a build, so separate DIAGNOSE from FIX for tight control):

1. **Subagent 1 — Diagnose Bug B.** Temporarily un-swallow the scan's `.catch` (or log `contactId` + error in the per-contact loop), build+install a debug APK on droid, run "Check linked contacts", capture the exact exception + the throwing contactId/stack. Return the root cause. (No fix yet.)
2. **Orchestrator (main) — apply fixes.** Write the Bug B fix (from the diagnosis) + the Bug C key fix (small code edits, kept in the main session for control). Run `npm test`/`tsc`/`check:colors` locally.
3. **Subagent 2 — Build + verify.** Full `prebuild --clean` + `assembleDebug` on droid, `install -r`, re-run the automated suite, and smoke that "Check linked contacts" now completes (no crash).
4. **Subagent 3 — Scenarios.** Create method-value-conflict fixtures (edit a phone), then run + DB-verify scenarios 3 (conflict + no-re-nag), 4 (bulk check: only-changed / partial / counts / no Use-Contact-Values on conflict), 5 (process-death resume), 6 (source-missing → relink).
5. **Orchestrator — close out.** Verify all evidence, update `20-UAT.md`, cleanup, `20-06-SUMMARY.md`, verification gates, owner sign-off.

(An equally valid cadence is the owner's original 3-subagent version if Bug B's fix is obvious from the first diagnosis — fold diagnose+fix into one.)

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable resume state.
2. `.planning/phases/20-contact-reconciliation-merge/20-UAT-FINDINGS-reconcile-bugs.md` — Bugs B, C, Finding A with file:line.
3. `.planning/phases/20-contact-reconciliation-merge/20-UAT.md` — scoreboard, fixture registry, verified device-driving procedure.
4. `docs/decisions/ADR-003-read-contacts-on-api37-for-reconcile.md` — the permission decision (already implemented).
5. `.planning/phases/20-contact-reconciliation-merge/20-UAT-RUNBOOK-RESEARCH.md` — code-verified per-scenario navigation.
6. MEMORY: `adb-input-source-taxonomy`, `device-local-contact-fixtures`, `device-uat-runas-pattern`, `build-test-pipeline`.

## Infrastructure State
- Pixel 6 Pro `1A071FDEE002BU` (API 37): fresh ADR-003 debug APK installed (`app-debug.apk`, gitignored), READ_CONTACTS granted. `run-as` works. Metro on :8082 (quest-board owns :8081); reverse `tcp:8081→8081`, `tcp:8082→8082` currently.
- Droid (desktop, `ssh droid`): build host. `expo prebuild --clean` + `gradlew.bat assembleDebug` (~6–7 min), APK pulled via tar-over-ssh. NEVER git push.
- On-device helper scripts at `/data/local/tmp/`: `mkcontact.sh`, `addphone.sh`, `rename.sh`, `delcontact.sh`, `verify-contacts.sh`.
- Fixtures present: Orbit contacts 7–17 (see 20-UAT.md registry); device raw 1130–1141. NOT yet cleaned up.
- Follow-up (non-blocking): dev-only redbox "two children with the same key" = Bug C.

<context>
The permission fix (the owner-approved deliverable) is done and works. The remaining work is fixing the next layer of reconcile bugs the device UAT surfaced — exactly what UAT is for. Keep the orchestrator lean: delegate device + build work, verify DB claims, and drive the phase to a completion state (all 8 checks green → SUMMARY → verification → sign-off).
</context>

<next_action>
Start with: acknowledge the blocking constraints, read the required docs, then dispatch Subagent 1 to DIAGNOSE Bug B (instrument the scan, build+install, reproduce "Check linked contacts", capture the throwing contactId + exception/stack) — do NOT write the fix until the root cause is captured.
</next_action>
