---
context: phase
phase: 20-contact-reconciliation-merge
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-31T04:50:03.488Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through failure. The resuming agent must acknowledge them before proceeding.

- [ ] CONSTRAINT: Bounded device-UAT control — Prior UAT subagents ran without a usable progress report or scenario evidence — first audit device state, write a bounded fixture/runbook checkpoint, and report before any scenario driving.
- [ ] CONSTRAINT: Correct hardware selection — A Pixel 3a/API 32 was initially used despite a Pixel 6 Pro/API 37 later becoming available — enumerate authorized ADB devices and confirm the API gate before build/install/app driving.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Opaque UAT delegation | A fixture/UAT executor was interrupted after substantial runtime without reporting fixture progress, a completed scenario, or cleanup. | blocking | No open-ended device subagent. Audit first, then use an explicit fixture setup/cleanup script or checklist with a time box and a checkpoint after fixture creation. |
| Wrong device selection | Initial device inspection used only the attached Pixel 3a (API 32), though the Pixel 6 Pro (API 37) was later authorized. | blocking | Run `adb devices -l`, inspect API levels for every authorized device, and target only `1A071FDEE002BU` before any UAT command. |

<current_state>
Phase 20 is paused in Plan 20-06, Task 3: the seven-scenario consolidated Pixel UAT. Plans 20-01 through 20-05 are complete and summarized. Plan 20-06 Tasks 1 and 2 are committed (`3d56911`, `a376901`). No `20-06-SUMMARY.md` exists, no device scenario has recorded evidence, and Phase 20 must not be marked complete.

The UAT target is Pixel 6 Pro, serial `1A071FDEE002BU`, Android API 37. Do not use Pixel 3a serial `943AY0JR4P` (API 32; contact picker requires API 37+).

Before fixture authorization, the Pixel 6 Pro had 6 contacts, 5 active source links, and zero reconciliation sessions/cards, bulk-review resolutions, and unreadable-birthday import rows. An executor was authorized to create dedicated fixtures and then interrupted without a report. It made no repository commit, but device-only fixture mutations are unknown.
</current_state>

<completed_work>

Completed Tasks:
- Plans 20-01 through 20-05: Done; summaries and commits exist.
- Plan 20-06 Task 1: Bulk birthday resolver + read + entry — Done (`3d56911`).
- Plan 20-06 Task 2: Phase verification gate — Done (`a376901`). Automated checks were reported green; the last fully observed full-suite gate was 183 files / 1,778 tests, plus clean TypeScript and color checks.
- Plan 20-06 Task 3: Device setup only — the Pixel 6 Pro debug build/install/launch and v13 migration were verified, but no UAT scenario is complete.
</completed_work>

<remaining_work>

- Audit the Pixel 6 Pro for any interrupted dedicated fixture data before editing or deleting anything.
- Create a bounded, explicit fixture setup/cleanup runbook (or script) with recorded fixture IDs.
- Run and evidence all seven UAT scenarios from `20-06-PLAN.md:115`:
  1. Atomic merge / tombstone / survivor landing.
  2. Merge conflict and primary-method choice.
  3. Per-contact reconciliation additions/conflicts/no-renag.
  4. Bulk changed-only check, partial resolution, and conflict action availability.
  5. Process-death resume preserving applied changes.
  6. Deleted source → Source missing → safe Relink.
  7. Bulk Review Fix and Ignore.
- Remove only positively identified fixture data, verify cleanup, obtain owner sign-off, write `20-06-SUMMARY.md`, then run Phase 20 verification and completion gates.
</remaining_work>

<decisions_made>

- Tombstone-only retirement: no `contact_redirects` in migration 013 (owner ruling, 2026-08-30).
- In merge fallbacks, populated absorbed scalar/custom values fill null/blank survivor values unless explicitly overridden (owner authorized in Plan 20-02).
- Only Pixel 6 Pro `1A071FDEE002BU` is eligible for this UAT; Pixel 3a is unsupported.
- Owner explicitly authorized creation and later removal of dedicated UAT fixtures, but personal/pre-existing device data must remain untouched.
</decisions_made>

<blockers>

- The compatible device baseline did not contain required reconciliation or unreadable-birthday fixtures.
- The interrupted agent gave no fixture IDs or completion report, so current device-only mutations are unknown.
- No automatic phase verification/completion may run until UAT evidence and owner sign-off exist.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state for `$gsd-resume-work`.
2. `.planning/phases/20-contact-reconciliation-merge/20-UAT-HANDOFF.md` — verified device facts, fixture requirements, scenario list, and interruption warning.
3. `.planning/phases/20-contact-reconciliation-merge/20-06-PLAN.md` — authoritative Task 3 acceptance and device-UAT wording.
4. `.planning/phases/20-contact-reconciliation-merge/20-VALIDATION.md` — required automated coverage inventory.
5. `.planning/STATE.md` — current project context; it is stale on Plan 1 and must not be used as evidence of phase completion.

## Infrastructure State

- Pixel 6 Pro: serial `1A071FDEE002BU`, API 37; use `adb -s 1A071FDEE002BU` on every command.
- Pixel 3a: serial `943AY0JR4P`, API 32; do not use it for Phase 20 UAT.
- Prior debug app checks: `run-as com.bwales.orbit id` succeeded; a Phase-20 debug build installed, launched, and migrated the DB to `user_version=13`; Metro was reached via Pixel-only reverse `tcp:8081 → tcp:8082`.
- No known external async jobs or background servers must be resumed.

<context>
The only unfinished work is honest device validation. The earlier approach failed because it delegated open-ended fixture creation/UAT without a visible runbook or progress checkpoint. Resume with a small, auditable fixture setup and capture evidence after each scenario. Do not claim a result from any prior agent run.
</context>

<next_action>
Start with: acknowledge the two blocking constraints, run `adb devices -l`, verify only the Pixel 6 Pro target, then audit its database/system-contact state for any interrupted fixture markers without mutating pre-existing contacts. Stop after the audit and present the fixture IDs plus a bounded scenario runbook before driving the app.
</next_action>
