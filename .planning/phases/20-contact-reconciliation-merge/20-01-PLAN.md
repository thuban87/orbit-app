---
phase: 20-contact-reconciliation-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/013-reconciliation-and-merge.ts
  - src/db/migrations/013-reconciliation-and-merge.test.ts
  - src/db/database.ts
  - src/db/merge-dao.ts
  - src/db/merge-dao.test.ts
  - src/db/contacts-dao.ts
  - src/backup/reconciliation.test.ts
  - src/db/merge-candidate-read.ts
  - src/db/merge-candidate-read.test.ts
  - src/screens/SurvivorSelectScreen.tsx
  - src/components/MergeImpactSummary.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: false
requirements: [RCN-03, RCN-04]
estimate:
  tokens: 72000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A user can merge two conflict-free Orbit contacts from the profile overflow 'Merge with another contact' and lands on the surviving contact's profile (RCN-03)."
    - "The second-candidate picker sources from a NET-NEW `listMergeCandidates(exec, excludeContactId)` that returns BOTH Bound AND Unbound live (non-archived) contacts — NOT `listSunCandidates`, which filters `tracking_enabled = 1` (VERIFIED sun-picker-read.ts:44) and would hide the Unbound imported duplicates a profile merge most needs to reach (reviewer MEDIUM finding 1)."
    - "mergeContacts reparents interactions, events, fuel, custom_field_values, contact_links, contact_methods, external_contact_links, contact_method_provenance (via method_id), AND field_history from the absorbed contact onto the survivor inside one inWriteTransaction; field_history has NO FK and would otherwise orphan on the absorbed-contact delete (RCN-03)."
    - "field_history is reparented with a TABLE-SPECIFIC `UPDATE field_history SET contact_id=? WHERE contact_id=?` that carries NO modified_at — field_history's columns are (id, contact_id, field_col_name, old_value, operation, created_at) with NO modified_at column (VERIFIED 001-initial.ts:156-162), so applying the generic `SET contact_id=?, modified_at=?` template to it would raise `no such column: modified_at` and ABORT the whole merge. The modified_at-bearing children (interactions, events, fuel, custom_field_values, contact_links, contact_methods, external_contact_links — all VERIFIED to have modified_at in 011-contact-lifecycle-schema.ts) use the generic modified_at template; field_history does not (RCN-03 field_history reparent edge)."
    - "Absorbed-only lifecycle columns on the contacts row (favourite_rank, ring_seq, snooze_until — all nullable, NOT part of UpdateContactFullInput so the scalar resolution path never carries them) follow an EXPLICIT survivor-wins policy consistent with the promote decision: the survivor keeps its own values and the absorbed row's values are dropped on the absorbed-row DELETE, BUT any non-null absorbed value for these three columns is snapshotted to field_history (operation e.g. 'merge-dropped') in the same transaction so the discard is auditable and never silent (RCN-03 lifecycle-column edge / Cluster reviewer)."
    - "A custom field both contacts hold for the same field_def_id is COLLISION-PRE-RESOLVED before reparent by CONSULTING `resolutions.customFields[field_def_id]` (same precedence rigor as the per-type `primaryMethod` consult): a missing entry defaults survivor-wins (survivor's value kept); an entry favoring the ABSORBED's value overwrites the survivor's row via `upsertValueCore` and snapshots the SURVIVOR's overwritten prior value to field_history. Either way the absorbed's losing custom_field_values row is snapshotted to field_history and deleted, and only field_def_ids the survivor lacks are reparented — so the UNIQUE(contact_id, field_def_id) constraint NEVER ABORTs. A dedicated merge-dao test proves BOTH directions: default survivor-wins keeps the survivor value, and a customFields resolution favoring the ABSORBED's value leaves the survivor holding the absorbed's value with the survivor's old value snapshotted (RCN-03 custom-field collision + resolution-precedence edge)."
    - "A custom field the ABSORBED holds for a field_def_id the SURVIVOR LACKS is REPARENTED onto the survivor (additive), distinct from the collision case — the merge combines the absorbed's unique custom fields rather than dropping them; this is proven by a dedicated merge test (RCN-03 additive custom-field reparent edge)."
    - "mergeContacts consumes a NAMED, EXPORTED `MergeResolutions` type (from merge-dao.ts) — a typed shape covering scalar choices, custom-field choices keyed by field_def_id, `primaryMethod` KEYED BY method_type (a per-method_type map — NOT a single choice — so a simultaneous phone AND email primary contention is held as two independent choices; VERIFIED gap, cycle-4 finding 1), and a photo choice — plus an exported pure `normalizeMergeResolutions`/validation function; 20-02 IMPORTS this type rather than matching an implied object shape by convention (reviewer finding 1)."
    - "The merge asserts exactly one contacts row is removed for the absorbed id; on any throw the whole transaction rolls back and both contacts remain intact (RCN-03 empty/atomic edge)."
    - "Merging a contact into itself, or into an absorbed/tombstoned survivor, is rejected before any write (RCN-03 adjacency edge)."
    - "Competing primaries are demoted and duplicate active source links retired BEFORE any reparent UPDATE, so idx_contact_methods_primary_type and idx_external_contact_links_active never ABORT; child-data consolidation order is deterministic (RCN-03 ordering edge)."
    - "A competing-primary contention (both contribute is_primary=1 for a method_type) CONSULTS `resolutions.primaryMethod[method_type]` before demoting: because `primaryMethod` is KEYED BY method_type, a simultaneous phone AND email contention is resolved INDEPENDENTLY per type (never collapsed to one choice — cycle-4 finding 1). If the user chose the ABSORBED's method for that type, that method is PROMOTED and the survivor's competing primary of that type demoted; with no choice (or a survivor choice) the default is survivor-wins (demote the absorbed's) — mirroring the custom-field collision branch (1d) which also consults its resolution, NOT an unconditional demote. So the 20-02 primary-contention control is honoured, never decorative; a dedicated merge-dao test proves the chosen absorbed primary wins for EACH contended type, including the phone-AND-email dual-contention case (RCN-03 primary-method resolution edge / reviewer HIGH #3 + cycle-4 finding 1)."
    - "Canonical-duplicate method dedupe captures absorbed method ids BEFORE dedup and REMAPS provenance (UPDATE contact_method_provenance SET method_id = survivorMethodId WHERE method_id = losingMethodId) BEFORE deleting a losing method, so provenance (method_id ON DELETE CASCADE) is never orphaned; surviving reparented methods keep their id so their provenance follows automatically (RCN-03 provenance edge)."
    - "A merge scalar resolution reads the survivor's FULL current metadata and constructs a complete UpdateContactFullInput (never a sparse patch), so a scalar-only resolution can never reset an unrelated survivor column (name/category/cadence/battery/birthday/toggles)."
    - "A merge photo resolution is applied atomically inside the merge transaction via the net-new non-mutexed setContactPhotoCore; updateContactMetadataCore omits photo and setContactPhoto self-wraps inWriteTransaction, so neither can carry the photo choice."
    - "last_contact is recomputed only through recomputeLastContactCore after interactions are reparented; the merge never writes contacts.last_contact directly."
    - "A survivor scalar overwritten by a merge resolution is snapshotted to field_history inside the same transaction."
    - "The absorbed identity is retired as a generic tombstone (entity_type='contact'), not archived; it never appears in listArchived and is not restorable via the archive lifecycle (RCN-04)."
    - "A merged-away contact's tombstone beats any older incoming row on reconciliation/restore, so future sync cannot resurrect it (RCN-04 unclassified edge)."
    - "PENDING RECONCILE-CARD LIFECYCLE ON MERGE (cycle-4 finding 2) — before the absorbed-row DELETE, mergeContacts EXPLICITLY handles any `reconciliation_session_cards` row whose `contact_id` is the absorbed id: that table's `contact_id` is `ON DELETE CASCADE` (migration 013), so an un-handled merge would let the absorbed delete SILENTLY cascade-drop a pending bulk-reconcile card, orphaning its still-'pending' session and leaving `unresolved_count`/completion counts stale. The writer instead, in the SAME txn, DISCARDS the absorbed's reconcile cards (DELETE the `reconciliation_session_cards` row(s) for the absorbed id) and then RE-FINALIZES each owning session by the DERIVED unresolved-card count — the SAME model 20-04 uses in `finalizeSessionIfTerminal` (a session is 'complete' when zero non-terminal cards remain): there is NO session-level `unresolved_count` column to decrement (migration 013's `reconciliation_sessions` DDL is id/uid/status/total_checked/created_at/modified_at only — `unresolved_count` exists ONLY on `reconciliation_session_cards`, VERIFIED 20-01 DDL). Because merge-dao is wave 1 and `finalizeSessionIfTerminal` is authored in wave-4's reconcile-session-dao, the merge writer performs an EQUIVALENT inline derived re-count (after deleting the absorbed's card, `SELECT COUNT(*)` the session's remaining non-terminal cards and `UPDATE reconciliation_sessions SET status='complete'` when zero), not a call into the wave-4 symbol — the finalization MODEL is identical, the code is self-contained. The survivor's own future reconcile re-scan re-derives any real remaining diff, so a stale pre-merge card is never silently vanished nor left dangling. (`reconcile_source_snapshot` is keyed by `external_contact_link_id`, which IS reparented in step 2, so it needs no separate handling — VERIFIED external_contact_links.contact_id ON DELETE CASCADE + reparented, 011:117-123.) Proven by a merge test with a pending reconcile card on the absorbed contact (RCN-03 / finding 2)."
    - statement: "ACCEPTED BOUNDED GAP (claude LOW-MEDIUM): the tracer's SurvivorSelect→MergeImpactSummary path has no MergeConflictsScreen yet (added in 20-02). If 20-01 ships to a device build before 20-02, picking two genuinely conflicting contacts runs mergeContacts with empty resolutions = survivor-wins silently. This is an explicit, bounded one-wave gap closed by 20-02's conflict step; the writer itself is conflict-correct (empty resolutions never overwrite a survivor scalar). Not a data-loss risk — the survivor keeps its own values; the absorbed's non-conflicting data still combines."
      verification: backstop
    - statement: "The merge impact-summary count rows pluralize correctly (1 interaction vs n interactions) for E13."
      verification: backstop
    - statement: "On merge failure the UI stays on the summary with 'Couldn't merge these contacts. Nothing was changed.' and the absorbed contact is observably fully intact (atomic rollback observable in UI) — E13 backstop."
      verification: backstop
  prohibitions:
    - "merge must NEVER leave a half-consolidated state — full consolidation succeeds or nothing changes"
    - "must NEVER resurrect a tombstoned/absorbed identity"
    - "must NEVER route the absorbed contact through the archive lifecycle (archiveContact/restoreContact/listArchived)"
    - "must NEVER write contacts.last_contact directly — only recomputeLastContactCore"
    - "must NEVER blind-reparent custom_field_values without collision pre-resolution (UNIQUE(contact_id, field_def_id) would ABORT the whole merge)"
    - "must NEVER delete a canonical-duplicate absorbed method before remapping its contact_method_provenance rows to the surviving method (ON DELETE CASCADE would silently drop provenance)"
    - "must NEVER leave absorbed field_history rows pointing at the deleted absorbed contact_id"
    - "must NEVER let the absorbed-row DELETE silently cascade-drop a pending `reconciliation_session_cards` row for the absorbed contact — the card MUST be explicitly deleted and its owning session RE-FINALIZED by the derived unresolved-card count in the same txn (ON DELETE CASCADE would orphan the session and leave it stuck 'pending' — finding 2)"
    - "must NEVER decrement a session-level `unresolved_count` column on `reconciliation_sessions` — no such column exists (session completion is DERIVED by counting non-terminal cards, matching 20-04's `finalizeSessionIfTerminal`); referencing it would raise `no such column` and ABORT the merge (HIGH #1)"
    - "must NEVER let canonical-method dedupe (step 1c) erase a method the user EXPLICITLY chose as the primary in step 1a — the selected primary wins the dedupe even when it is the absorbed's row (finding 6)"
    - "must NEVER apply the generic `SET contact_id=?, modified_at=?` reparent template to field_history — it has no modified_at column and the statement would raise `no such column: modified_at` and ABORT the merge; field_history takes a modified_at-free UPDATE"
    - "must NEVER drop a non-null absorbed favourite_rank/ring_seq/snooze_until without a field_history snapshot in the same transaction (survivor-wins is explicit, but the discard must be auditable, not silent)"
    - "the post-commit absorbed-photo unlink must NEVER delete a photo file the survivor's resolved photo still references"
  artifacts:
    - src/db/migrations/013-reconciliation-and-merge.ts
    - src/db/merge-dao.ts
    - src/screens/SurvivorSelectScreen.tsx
    - src/components/MergeImpactSummary.tsx
  key_links:
    - "migration013 registered in MIGRATIONS and TARGET_VERSION bumped to 13 in src/db/database.ts"
    - "mergeContacts calls recomputeLastContactCore(exec, survivorId, now) inside the same inWriteTransaction — never nested inWriteTransaction, never bare"
    - "profile overflow 'Merge with another contact' → SurvivorSelect → MergeImpactSummary → mergeContacts → survivor profile"
---

<objective>
**As a** solo Orbit user, **I want to** merge two duplicate Orbit contacts into one atomically, **so that** the duplicate that import/reconciliation exposed is resolved for good and the absorbed identity can never come back.

This is the phase **tracer**: the thinnest real end-to-end reconciliation-or-merge path — migration 013 + one real DB write (the atomic merge) + one real UI interaction (profile → survivor select → confirm). It proves the architecturally-riskiest layer (an all-or-nothing multi-table transaction against the v11 partial-unique-index hazards and the single-writer recency invariant) before any expansion. The merge happy path is conflict-free (non-conflicting fixed/custom data combines automatically, canonical-equal methods dedupe); scalar-conflict UI is expanded in 20-02.

Purpose: land the irreversible migration 013 once, correctly, behind an owner decision; establish `mergeContacts` as the sole atomic merge writer modeled on `purge-dao` (reparent, not delete); retire the absorbed contact as a resurrection-proof tombstone.
Output: migration 013 (registered, TARGET_VERSION=13), `merge-dao.mergeContacts`, minimal SurvivorSelect + MergeImpactSummary UI wired through the profile overflow, and node-proven atomicity/rollback/tombstone tests.
</objective>

<assumption_delta_decision>
- **now-primary noun:** the surviving Orbit contact identity (many identities → one survivor).
- **decision:** `promote` — the survivor is the canonical identity; the absorbed identity's child rows reparent onto it and the absorbed row is retired as a tombstone (not kept alongside).
- **rationale:** merge exists precisely to collapse duplicates into one authoritative Orbit person; keeping the absorbed identity alongside would recreate the duplicate merge resolved (dossier Cluster V/AB).
- **suggested contract test:** `merge-dao.test.ts` asserts exactly one surviving contacts row and one `contact` tombstone for the absorbed uid after merge.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Resolved owner decision (2026-08-30): tombstone-only — migration 013 adds NO contact_redirects table</name>
  <action>
    RESOLVED — OWNER RULING (2026-08-30): migration 013 relies on the generic `contact` tombstone ALONE and does NOT add a `contact_redirects` table. Rationale (owner): sync-related setup belongs to the future sync milestone, which will carry its own irreversible migrations, so this fits there; Phase 20 ships only what it needs now. The generic `contact` tombstone already satisfies RCN-04 resurrection-proofing (`compareRowAndTombstone`, backup/reconciliation.ts:151). If a future sync milestone ever needs absorbed→survivor reference rewriting, it adds the redirect table (and its portable-backup-schema plumbing) then, in its own migration. This is settled — there is NO human checkpoint to resolve. Implementation consequence, carried by Task 1 below: migration 013 does NOT create `contact_redirects`, and `mergeContacts` writes NO redirect row.
  </action>
  <acceptance_criteria>
    - `grep -n "contact_redirects" src/db/migrations/013-reconciliation-and-merge.ts` returns nothing (the table is never created).
    - `grep -n "contact_redirects" src/db/merge-dao.ts` returns nothing (no redirect row written on merge).
  </acceptance_criteria>
  <done>Redirect-table decision resolved to tombstone-only; no `contact_redirects` anywhere in Phase 20.</done>
</task>

<task type="tracer">
  <name>Task 1: Migration 013 + atomic mergeContacts writer (data-layer end-to-end)</name>
  <reversibility rating="one-way">Migration 013 ships as forward-only app code via PRAGMA user_version and is irreversible on unreachable devices; its table shapes cannot be changed after ship without another migration (the redirect-table question is resolved: tombstone-only — no contact_redirects).</reversibility>
  <files>src/db/migrations/013-reconciliation-and-merge.ts, src/db/migrations/013-reconciliation-and-merge.test.ts, src/db/database.ts, src/db/merge-dao.ts, src/db/merge-dao.test.ts, src/db/contacts-dao.ts, src/backup/reconciliation.test.ts</files>
  <read_first>
    - src/db/migrations/012-import-sessions.ts (migration authoring idiom — whole file: `export const migration012: Migration = { version, async apply(exec, _deps) }`, table shape idioms: id/uid UNIQUE, status CHECK, ON DELETE CASCADE, UNIQUE(session_id, contact_id))
    - src/db/database.ts (lines 25-58, 139 — MIGRATIONS array + `TARGET_VERSION = 12`; register migration013 and bump to 13)
    - src/db/migrations/011-contact-lifecycle-schema.ts (178-187 — the two partial unique indexes `idx_contact_methods_primary_type` UNIQUE(contact_id, method_type) WHERE is_primary=1 and `idx_external_contact_links_active` UNIQUE(provider, external_contact_id) WHERE is_active=1; the `contacts_prevent_cadence_clear` trigger that ABORTs interval_days→NULL)
    - src/db/purge-dao.ts (76-97 PURGE_CHILDREN child-table list; 204-283 the atomic fan-out structure — one inWriteTransaction, tombstone-then-mutate, sun_contact_id redirect at 253-258, assert-exactly-one-row at 260-268, post-commit onPurgeExtensions)
    - src/db/recency-dao.ts (150-176 — `recomputeLastContact`/exported `recomputeLastContactCore` at :427; the ONLY writer of contacts.last_contact; non-mutexed core, call ONLY inside an open inWriteTransaction, MAX over current rows with rarely_responds connected-only filter)
    - src/db/tombstones-dao.ts (8-18, 53 — `insertTombstoneCore(exec, {entityType, entityUid, deletedAt})`, TombstoneEntityType includes 'contact')
    - src/db/contacts-dao.ts (308-335 `updateContactMetadataCore(exec, input)` asserts one row and writes EVERY mutable column EXCEPT photo — a sparse patch resets unrelated columns; 521-620 archive/restore/listArchived — do NOT route merge through these; 600-645 `setContactPhoto` SELF-WRAPS inWriteTransaction + assertSafeRelative + bumpDataRevisionCore + changes===1 — has NO `*Core` variant, so a net-new `setContactPhotoCore` must be extracted here for atomic in-merge photo apply)
    - src/db/field-values-dao.ts (20-38 getValuesForContact; 64-82 `upsertValueCore(exec, contactId, fieldDefId, uid, value, now)` — ON CONFLICT(contact_id, field_def_id) DO UPDATE, non-mutexed, for custom-field collision resolution)
    - src/db/contact-methods-dao.ts (5-8, 47-54, 99-287 — canonical_value normalization, `applyContactMethodDiffCore` WHOLE-LIST diff, canonical dedupe/primary demotion; note contact_method_provenance.method_id is ON DELETE CASCADE per 011:228-233)
    - src/db/purge-dao.ts (76-87 PURGE_CHILDREN — provenance delete joins via method_id; 242-249 field_history is fanned out EXPLICITLY because it has NO FK and never cascades — the merge inverse is an explicit field_history REPARENT)
    - src/db/migrations/001-initial.ts (field_history schema: contact_id, field_col_name, old_value, operation, created_at — no FK)
    - src/backup/reconciliation.ts (151-155 `compareRowAndTombstone`)
    - src/db/transaction.ts (`inWriteTransaction` — shared non-reentrant mutex; a nested inWriteTransaction is a PERMANENT hang)
  </read_first>
  <action>
    Author `src/db/migrations/013-reconciliation-and-merge.ts` exporting `migration013: Migration` (version 13). In one `exec.execAsync` DDL block create: `reconciliation_sessions` (id PK, uid TEXT NOT NULL UNIQUE, status TEXT NOT NULL DEFAULT 'pending' CHECK IN ('pending','complete','discarded'), total_checked INTEGER DEFAULT 0, created_at, modified_at); `reconciliation_session_cards` (id PK, uid UNIQUE, session_id REFERENCES reconciliation_sessions(id) ON DELETE CASCADE, contact_id REFERENCES contacts(id) ON DELETE CASCADE, card_status TEXT DEFAULT 'unresolved' CHECK IN ('unresolved','partial','resolved','missing_source'), diff_json TEXT NOT NULL, unresolved_count INTEGER DEFAULT 0, staged_photo_rel_path TEXT, created_at, modified_at, UNIQUE(session_id, contact_id)) — `staged_photo_rel_path` is a NULLABLE first-class column holding the card's durable `reconcile-staging/` relative path (mirroring the import pattern's dedicated staging column in migration 012), so the 20-05 orphan-staging sweep can enumerate staged files WITHOUT parsing every card's `diff_json` — decoupling FS hygiene from serializer correctness (reviewer LOW finding 9); `reconcile_source_snapshot` (id PK, uid UNIQUE, external_contact_link_id REFERENCES external_contact_links(id) ON DELETE CASCADE, field_family TEXT CHECK IN ('name','phones','emails','birthday','photo'), reviewed_value TEXT, reviewed_at TEXT NOT NULL, UNIQUE(external_contact_link_id, field_family)); `bulk_review_resolutions` (id PK, uid TEXT NOT NULL UNIQUE, import_session_row_id INTEGER NOT NULL REFERENCES import_session_rows(id) ON DELETE CASCADE, flag_type TEXT NOT NULL CHECK IN ('birthday'), resolution TEXT NOT NULL CHECK IN ('fixed','ignored'), resolved_at TEXT NOT NULL, UNIQUE(import_session_row_id, flag_type)) — the DURABLE per-item resolution store consumed by 20-06 (source_payload is immutable import history and has no resolution field; this table is where a Fix/Ignore is recorded so a resolved flag never re-surfaces). The `flag_type CHECK IN ('birthday')` is deliberately SINGLE-VALUED for v1: the table + `(row_id, flag_type)` row idiom is schema-generic, but a second flag type will require BOTH a new irreversible migration to widen this CHECK AND new DAO/read code — the genericity is at the storage layer only, not the DAO (see 20-06); plus a status index on reconciliation_sessions(status). Per the RESOLVED owner decision (tombstone-only, 2026-08-30) migration 013 does NOT create a `contact_redirects` table — the generic `contact` tombstone alone is used for resurrection-proofing (RCN-04). Register `migration013` in `MIGRATIONS` and bump `TARGET_VERSION` to 13 in database.ts. RE-VERIFY the head is 12 before bumping (never assume head+1). Never edit a shipped migration.

    Extract a net-new `setContactPhotoCore(exec, id, relative, now)` in `src/db/contacts-dao.ts` — the NON-mutexed body of `setContactPhoto` (assertSafeRelative → `?`-bound `UPDATE contacts SET photo=?, modified_at=? WHERE id=?` → changes===1 guard → bumpDataRevisionCore), callable inside a caller-owned txn; refactor `setContactPhoto` to `inWriteTransaction(exec, () => setContactPhotoCore(...))` so behavior is unchanged and the existing contacts-dao tests stay green. This is the ONLY writer that can apply a photo choice atomically inside `mergeContacts` (updateContactMetadataCore omits photo; setContactPhoto self-wraps the mutex).

    Author `src/db/merge-dao.ts` exporting `mergeContacts(exec, { survivorId, absorbedId, resolutions, now })`. Export a NAMED `MergeResolutions` type from this module (fields: scalar choices for name/category/cadence/battery/birthday/toggles, `customFields` keyed by field_def_id, `primaryMethod` KEYED BY method_type — a per-method_type map (e.g. `Partial<Record<ContactMethodType, 'survivor' | 'absorbed'>>` or a per-type chosen method uid), NOT a single choice, so a phone contention and an email contention are carried as two independent entries (finding 1) — and an optional `photo` choice — a durable `avatars/<name>.<ext>` relative path or 'keep-survivor'); `normalizeMergeResolutions` normalizes the per-type `primaryMethod` map (a missing type = no choice = survivor-wins default) and a pure `normalizeMergeResolutions(input): MergeResolutions` validation/normalization function (unit-tested) that 20-02 imports and feeds; `mergeContacts` consumes the normalized `MergeResolutions`, never an implied ad-hoc object shape. Model the transaction STRUCTURE on purge-dao.purgeContact but INVERT delete→reparent. In one `inWriteTransaction`: (0) read both uids, assert both live (archived_at IS NULL), assert survivorId !== absorbedId, assert the absorbed is not already tombstoned; (1) PRE-RESOLVE integrity hazards BEFORE any reparent — (a) for EACH method_type where both contribute is_primary=1, CONSULT `resolutions.primaryMethod[method_type]` (do NOT unconditionally demote): because `primaryMethod` is keyed by method_type, a simultaneous phone-AND-email contention is resolved independently per type. If the user chose the ABSORBED's method for that type, PROMOTE it (leave/set is_primary=1) and DEMOTE the SURVIVOR's competing primary of that type; otherwise (no entry for that type, or the survivor's method chosen — the default) demote the ABSORBED's — this branches on the resolution exactly like the custom-field collision step (1d) does, so a user's explicit primary pick in 20-02 is honoured rather than silently overridden (reviewer HIGH #3); either way exactly one is_primary=1 remains for the type BEFORE reparent so idx_contact_methods_primary_type never ABORTs; (b) if both link the same (provider, external_contact_id) is_active=1, retire the absorbed's duplicate link; (c) collapse canonical-duplicate methods — the SELECTED PRIMARY WINS THE DEDUPE (finding 6): if step 1a promoted the ABSORBED's method as the type's primary and it is canonical-equal to the survivor's method, KEEP the chosen (absorbed-derived) method row as the survivor's method and treat the survivor's own row as the losing duplicate, so the explicit primary pick is never silently erased by a survivor-always-wins dedupe; otherwise keep the survivor's row. FIRST capture the losing duplicate's method id and `UPDATE contact_method_provenance SET method_id = <keptMethodId>, modified_at = ? WHERE method_id = <losingMethodId>` (remap provenance to the KEPT method) BEFORE deleting the losing method row, so provenance (method_id ON DELETE CASCADE) is never orphaned; (d) CUSTOM-FIELD collision pre-resolution — for every field_def_id BOTH contacts hold a custom_field_values row for, CONSULT `resolutions.customFields[field_def_id]` with the SAME rigor as the per-type `primaryMethod` consult in (1a): a missing entry = no choice = survivor-wins default (keep the survivor's value); an entry favoring the ABSORBED's value PROMOTES the absorbed value onto the survivor via `upsertValueCore(exec, survivorId, fieldDefId, survivorUid, absorbedValue, now)` (ON CONFLICT(contact_id, field_def_id) DO UPDATE overwrites the survivor's row) AND snapshots the SURVIVOR's prior value into field_history (the overwritten value is the audited one); either direction, snapshot the absorbed's now-losing value into field_history, then `DELETE FROM custom_field_values WHERE contact_id = <absorbed> AND field_def_id = <shared>` — so the reparent in (2) only moves field_def_ids the survivor lacks and UNIQUE(contact_id, field_def_id) never ABORTs. The custom-field resolution is honoured, never decorative — mirroring the primaryMethod dual-contention rigor; (2) reparent each remaining child. For the modified_at-BEARING children — interactions, events, fuel, custom_field_values (now collision-free; this also REPARENTS every field_def_id the survivor lacks, so the absorbed's UNIQUE custom fields are combined onto the survivor, additive), contact_links, contact_methods, external_contact_links — use `UPDATE <child> SET contact_id = ?, modified_at = ? WHERE contact_id = ?` (every one of these tables is VERIFIED to have a modified_at column in 011-contact-lifecycle-schema.ts; confirm on disk before using the template). For field_history, use a TABLE-SPECIFIC statement with NO modified_at: `UPDATE field_history SET contact_id = ? WHERE contact_id = ?` — field_history's columns are (id, contact_id, field_col_name, old_value, operation, created_at) with NO modified_at (VERIFIED 001-initial.ts:156-162); it has NO FK and never cascades, so reparenting keeps its rows from orphaning on the absorbed delete, but applying the generic modified_at template to it would raise `no such column: modified_at` and ABORT the whole merge. contact_method_provenance needs NO separate reparent: it is keyed by method_id, and reparenting contact_methods leaves method ids stable so provenance follows automatically (the only provenance write is the dedup remap in step 1c); (3) apply survivor scalar `resolutions` by READING the survivor's full current metadata, overlaying the chosen resolutions, and passing a COMPLETE `UpdateContactFullInput` to `updateContactMetadataCore` (never a sparse patch); for a photo resolution call `setContactPhotoCore`; and for each overwritten survivor scalar INSERT the old value into field_history (contact_id, field_col_name, old_value, operation, created_at) in the same txn; (4) redirect `UPDATE app_settings SET sun_contact_id = ?, modified_at = ? WHERE id = 1 AND sun_contact_id = ?`; (4b) LIFECYCLE-COLUMN policy — the absorbed row's favourite_rank/ring_seq/snooze_until are survivor-wins (NOT part of UpdateContactFullInput; the survivor keeps its own) but each NON-NULL absorbed value is snapshotted to field_history (field_col_name = the column, old_value = the absorbed value, operation e.g. 'merge-dropped', created_at = now) in this same txn BEFORE the absorbed-row delete, so the discard is auditable and never silent; (4c) PENDING RECONCILE-CARD handling (finding 2, HIGH #1 — DERIVED session finalization, NOT a session counter) — SELECT DISTINCT session_id from any `reconciliation_session_cards` WHERE contact_id = absorbedId, then `DELETE FROM reconciliation_session_cards WHERE contact_id = ?` (absorbed id). For each affected session_id, RE-FINALIZE by the DERIVED unresolved-card count: `SELECT COUNT(*) FROM reconciliation_session_cards WHERE session_id = ? AND card_status NOT IN ('resolved','missing_source')` and, when that count is zero, `UPDATE reconciliation_sessions SET status = 'complete', modified_at = ? WHERE id = ?` — all in THIS txn, BEFORE the absorbed-row delete. This is the SAME terminal-rule model 20-04's `finalizeSessionIfTerminal` applies (complete when zero non-terminal cards); merge-dao is wave 1 and that helper is authored in wave-4 reconcile-session-dao, so the merge writer inlines the EQUIVALENT derived re-count rather than calling the wave-4 symbol — the model is identical, the code self-contained. There is NO `reconciliation_sessions.unresolved_count` column to decrement (the migration 013 DDL creates that column ONLY on `reconciliation_session_cards`); a decrement of a session-level counter would raise `no such column` and ABORT the merge (HIGH #1). Explicit handling is required because `reconciliation_session_cards.contact_id` is ON DELETE CASCADE (migration 013): without it the absorbed delete would silently cascade-drop the card and leave its session stuck 'pending'. The survivor's own next reconcile scan re-derives any genuine remaining diff, so nothing real is lost. (`reconcile_source_snapshot` needs no handling here — it is keyed by external_contact_link_id, reparented in step 2.) (5) `insertTombstoneCore(exec, { entityType: 'contact', entityUid: absorbedUid, deletedAt: now })`; (6) `DELETE FROM contacts WHERE id = ?` for the absorbed id and assert `changes === 1`; (7) `await recomputeLastContactCore(exec, survivorId, now)` LAST. All values `?`-bound; table names are literals in constant SQL (no identifier interpolation). Best-effort notification cancels for `decay:<absorbedId>` / `birthday:<absorbedId>` run POST-COMMIT in a `.then()`, never inside the txn — reuse the existing `purge-notification-cleanup` adapter pattern (src/services/notifications/purge-notification-cleanup.ts) rather than reimplementing ad-hoc cancels; an absorbed-photo file unlink runs POST-COMMIT ONLY when the final survivor photo reference is NOT the absorbed's stored relative path (a blind unlink would delete the survivor's chosen photo). For this tracer `resolutions` is empty/survivor-wins (conflict-free path); the collision/provenance/photo branches are still authored and unit-tested here so the writer is complete before 20-02 feeds real resolutions.

    Write `src/db/merge-dao.test.ts` (node:sqlite) proving: reparent of all child tables INCLUDING field_history; **field_history reparent with a NON-EMPTY field_history fixture on the absorbed contact — the merge COMPLETES with NO `no such column: modified_at` error and every absorbed field_history row now points at the survivor's contact_id** (guards HIGH #1: the modified_at-free field_history UPDATE); assert-exactly-one contacts row absorbed; competing-primaries + duplicate-active-source-link resolution (no partial-index ABORT with two fully-populated contacts); **primary-method resolution honoured — with both contacts contributing a competing is_primary=1 method of the same type, `resolutions.primaryMethod[type]` selecting the ABSORBED's method makes THAT method primary on the survivor and demotes the survivor's, while empty/survivor resolutions keep survivor-wins (guards HIGH #3: the control is not decorative)**; **DUAL primary contention (finding 1) — both contacts contribute competing is_primary=1 PHONE AND competing is_primary=1 EMAIL, and `primaryMethod` picks the absorbed's phone but the survivor's email: after merge the survivor's primary phone is the absorbed-derived one and the primary email is the survivor's, each type resolved independently with exactly one is_primary=1 per type (no partial-index ABORT)**; **selected-primary-wins-dedupe (finding 6) — the chosen absorbed primary is canonical-EQUAL to the survivor's method of that type: after merge the survivor's surviving method is the CHOSEN (absorbed-derived) one, it is is_primary=1, and its provenance is intact (the explicit pick is not erased by canonical dedupe)**; **custom-field collision (default survivor-wins) — both contacts hold different values for the SAME field_def_id with NO `resolutions.customFields` entry: merge completes with NO UNIQUE ABORT, survivor value retained, absorbed loser deleted, and the discarded value snapshotted to field_history**; **custom-field resolution favours ABSORBED (precedence honoured) — a `resolutions.customFields[field_def_id]` entry selecting the ABSORBED's value: after merge the survivor holds the ABSORBED's value (via upsertValueCore), the survivor's overwritten prior value is snapshotted to field_history, no UNIQUE ABORT (mirrors the primaryMethod dual-contention rigor)**; **additive custom-field reparent — the ABSORBED holds a custom_field_values row for a field_def_id the SURVIVOR LACKS: after merge that row belongs to the survivor (combined, not dropped)**; **provenance survival — a canonical-duplicate method that is collapsed has its provenance rows remapped to the surviving method (no orphaned/lost provenance)**; **lifecycle-column drop — an absorbed contact with a non-null favourite_rank/ring_seq/snooze_until: the survivor keeps its own values (survivor-wins) and each dropped absorbed value is snapshotted to field_history**; a photo resolution applied via setContactPhotoCore updates contacts.photo inside the same txn; last_contact recomputed incl. a rarely_responds survivor; field_history snapshot on a scalar overwrite; a scalar-only resolution does NOT reset unrelated survivor columns (full-input); `normalizeMergeResolutions` validates/normalizes an input resolutions object (unit-tested); merge-into-self and merge-into-tombstoned rejected; **pending reconcile-card handling (finding 2, HIGH #1) — an unresolved `reconciliation_session_cards` row on the ABSORBED contact whose owning session has other cards: after merge the absorbed's card is gone AND its owning session is RE-FINALIZED by the DERIVED non-terminal-card count (marked 'complete' only when zero non-terminal cards remain, else left 'pending'), with NO reference to a non-existent `reconciliation_sessions.unresolved_count` column (no `no such column` abort), NOT silently cascade-dropped with a stuck 'pending' session — and a second case where the absorbed's card was the session's LAST unresolved card, so the session finalizes 'complete'**; a forced throw rolls back fully (both contacts intact); absorbed tombstoned as 'contact' and absent from listArchived. Write `src/db/migrations/013-reconciliation-and-merge.test.ts` proving a v12→v13 forward migration creates the tables (incl. bulk_review_resolutions) and FK integrity holds. Extend `src/backup/reconciliation.test.ts` with a merged-away-contact resurrection-proofing case (tombstone beats an older incoming row).
  </action>
  <verify>
    <automated>npx vitest run src/db/merge-dao.test.ts src/db/migrations/013-reconciliation-and-merge.test.ts src/backup/reconciliation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/database.ts` contains `TARGET_VERSION = 13` and registers `migration013` in `MIGRATIONS`.
    - `src/db/merge-dao.ts` contains `export function mergeContacts(` (or `export async function mergeContacts(`) AND `export type MergeResolutions` (or `export interface MergeResolutions`) plus `export function normalizeMergeResolutions(`.
    - `src/db/contacts-dao.ts` contains `export async function setContactPhotoCore(`; `setContactPhoto` delegates to it inside `inWriteTransaction`.
    - `npx vitest run src/db/merge-dao.test.ts` exits 0 with cases for reparent (incl. field_history), a NON-EMPTY field_history fixture merge (no `no such column: modified_at`, rows repointed to survivor), integrity pre-resolution, primary-method resolution (a `resolutions.primaryMethod[type]` pick of the ABSORBED's method makes it primary on the survivor / demotes the survivor's, and empty resolutions default to survivor-wins — HIGH #3), DUAL phone-AND-email primary contention resolved independently per type (finding 1), selected-primary-wins-canonical-dedupe (finding 6), custom-field collision default survivor-wins (no UNIQUE ABORT), custom-field resolution favouring the ABSORBED value (survivor ends with absorbed value via upsertValueCore + survivor's prior value snapshotted), additive custom-field reparent (absorbed-only field_def_id moves to survivor), provenance-survives-dedupe, lifecycle-column drop-with-snapshot (favourite_rank/ring_seq/snooze_until), pending-reconcile-card discard-with-DERIVED-session-finalize (finding 2 / HIGH #1 — no session-level unresolved_count reference; session finalized by counting remaining non-terminal cards), photo-resolution apply, full-input non-reset, recompute, field_history snapshot on scalar overwrite, normalizeMergeResolutions (incl. the per-type primaryMethod map), self/tombstoned rejection, rollback, and tombstone-not-archive.
    - `grep -n "primaryMethod" src/db/merge-dao.ts` shows step (1a) branching PER method_type on `resolutions.primaryMethod[...]` (a per-type consult, not a single unconditional demote — finding 1).
    - `grep -n "reconciliation_session_cards" src/db/merge-dao.ts` shows step (4c) DELETE-ing the absorbed's reconcile cards then RE-FINALIZING each owning session by a derived non-terminal-card COUNT (not a session `unresolved_count` decrement) BEFORE the absorbed-row delete (finding 2 / HIGH #1); `grep -n "unresolved_count" src/db/merge-dao.ts` returns nothing referencing `reconciliation_sessions` (the column exists only on the cards table).
    - `grep -n "field_history" src/db/merge-dao.ts` shows the modified_at-FREE reparent UPDATE (`UPDATE field_history SET contact_id = ? WHERE contact_id = ?` — no `modified_at`), the scalar-overwrite snapshot, and the lifecycle-column snapshot; `grep -n "contact_method_provenance" src/db/merge-dao.ts` shows the dedup remap (not a post-reparent method_id subquery).
    - `npx vitest run src/db/migrations/013-reconciliation-and-merge.test.ts` exits 0 (v12→v13 forward + FK integrity, incl. bulk_review_resolutions).
    - `grep -n "recomputeLastContactCore" src/db/merge-dao.ts` shows the call; `grep -n "UPDATE contacts SET last_contact" src/db/merge-dao.ts` returns nothing (no direct last_contact write).
  </acceptance_criteria>
  <done>Migration 013 registered + TARGET_VERSION=13; mergeContacts is the sole atomic merge writer with all node tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal merge UI wired end-to-end (profile → survivor → confirm → survivor profile)</name>
  <files>src/screens/SurvivorSelectScreen.tsx, src/components/MergeImpactSummary.tsx, src/screens/ContactProfileScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/db/merge-dao.ts (mergeContacts signature — from Task 1)
    - src/db/purge-dao.ts (121-198 `computeImpact` / `impactSummaryLines` — the count-row source pattern for MergeImpactSummary)
    - src/db/sun-picker-read.ts (29-55 `listSunCandidates(exec)` — VERIFIED to filter `tracking_enabled = 1` at :44, so it EXCLUDES Unbound contacts. Do NOT reuse it for the merge picker: imported duplicates start Unbound and would be invisible, exactly the class a profile-initiated merge most needs (reviewer MEDIUM finding 1). Read it only as the query SHAPE to mirror for a Bound+Unbound read.)
    - src/db/contacts-dao.ts / any existing non-archived contact list read — the shape for a merge-candidate read that includes BOTH Bound and Unbound live contacts (archived_at IS NULL, no tracking_enabled filter)
    - src/screens/ImportReviewScreen.tsx (388-420 — the A-vs-B choice-pair fill idiom for the two survivor cards; selected = accent fill + borderStrong)
    - src/screens/ImportCompleteScreen.tsx (141-220, 316-324 — the `footerEntry` count-row idiom for MergeImpactSummary)
    - src/screens/DuplicateReviewScreen.tsx (221-234 header idiom 24px/600 title; 262-301 modal action-sheet-over-scrim)
    - src/components/OverflowMenu.tsx (20-29 — `OverflowAction { label, onPress }`, `OverflowMenu({ actions })`)
    - src/screens/ContactProfileScreen.tsx (41 import, 786 existing OverflowMenu usage — add a 'Merge with another contact' action)
    - src/navigation/RootNavigator.tsx (50, 70-100 — `<Stack.Screen name= component=>` registration) and src/navigation/types.ts (RootStackParamList)
    - src/theme (useTheme().colors.* — accent, danger, surface, surfaceElevated, border, borderStrong, textPrimary, textSecondary, background; NO hex literals — check:colors gate)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surfaces #11 SurvivorSelect, #13 MergeImpactSummary; Color: merge confirm is ACCENT — owner-locked 2026-08-30 — with a danger warning line + native Alert destructive final guard)
  </read_first>
  <action>
    Create `src/screens/SurvivorSelectScreen.tsx` (route params: the two candidate contact ids). Header 'Which contact survives?' (24px/600). Two stacked contact cards (avatar + name + a short continuity line), single-select; selected card = borderStrong + accent check. For this tracer preselect the older-created contact as a plain default (the advisory `Recommended` badge + real heuristic arrive in 20-02). Primary CTA `Continue` (accent) → route to the merge impact summary with { survivorId, absorbedId }. Create `src/components/MergeImpactSummary.tsx` composing the `footerEntry` count-row idiom: subject `Merge {absorbed} into {survivor}`, count rows `{n} interactions` / `{n} fuel items` / `{n} events` / `{n} contact methods` / `{n} external links` sourced by counting the absorbed contact's child rows (reuse the purge-dao count SQL shapes — read-only counts), then a `danger` warning block (15px/600): `{absorbed} will be retired.` and `This can't be simply undone.`; primary CTA `Merge contacts` (ACCENT fill, owner-locked — never a danger-filled button). On tap, fire an OS-native `Alert.alert` destructive-style guard (title `Merge contacts?`, confirm `Merge` destructive / `Cancel`) BEFORE calling `mergeContacts`; on success navigate to the survivor's Profile; on throw stay and show `Couldn't merge these contacts. Nothing was changed.` + `Try again`. Register both screens in RootNavigator + RootStackParamList. Add a `Merge with another contact` action to the ContactProfileScreen OverflowMenu (always available; not styled destructive at the menu level) that opens SurvivorSelect with this contact as one candidate. For the second-candidate picker, create a NET-NEW read `listMergeCandidates(exec, excludeContactId)` in `src/db/merge-candidate-read.ts` returning {id, name, photo} for EVERY live (archived_at IS NULL) contact — BOTH Bound and Unbound — excluding the current contact, ordered favourites-first then name (mirror the `listSunCandidates` query SHAPE but WITHOUT its `tracking_enabled = 1` filter, which excludes Unbound). Do NOT reuse `listSunCandidates`: imported duplicates start Unbound and it would hide exactly the candidates a profile merge most needs (reviewer MEDIUM finding 1). Write `src/db/merge-candidate-read.test.ts` proving the read INCLUDES an Unbound (tracking_enabled=0) live contact and EXCLUDES archived contacts and the current contact. Feed `listMergeCandidates` into a thin single-select contact list; keep the picker minimal (a simple scrollable list select). All colours via useTheme().colors.* — no hex literals.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
    <human-check>On the Pixel (debug build, run-as DB reads): open a contact profile, tap ⋯ → Merge with another contact, pick a second contact, Continue, confirm the impact summary + native Merge guard, and verify the app lands on the survivor's profile and the absorbed contact is gone. (Consolidated into end-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/SurvivorSelectScreen.tsx` and `src/components/MergeImpactSummary.tsx` exist and are registered in RootNavigator/types.
    - `src/screens/ContactProfileScreen.tsx` contains a `Merge with another contact` OverflowMenu action.
    - `src/db/merge-candidate-read.ts` contains `export ... listMergeCandidates(` with NO `tracking_enabled` filter; `grep -n "tracking_enabled" src/db/merge-candidate-read.ts` returns nothing; `npx vitest run src/db/merge-candidate-read.test.ts` exits 0 with an Unbound-included case and an archived/self-excluded case. The SurvivorSelect second-candidate picker sources from `listMergeCandidates`, not `listSunCandidates`.
    - `MergeImpactSummary` confirm button uses `colors.accent` (not `colors.danger`); the warning line uses `colors.danger`.
    - `npm run check:colors` passes (no colour literals) and `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>A user can walk profile → survivor select → impact summary → native guard → mergeContacts → survivor profile for a conflict-free merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → local DB | A user-invoked merge mutates 8 child tables + contacts + app_settings + tombstones in one transaction; the risk is a partial/corrupt write, not an external attacker. |
| local DB → future sync/restore | The absorbed identity must remain retired so another device/restore cannot resurrect it. |

> No external network on any Phase 20 path (local-first). **No npm packages are installed this phase** (zero new dependencies) — no supply-chain / package-legitimacy checkpoint applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01-1 | Tampering | mergeContacts multi-table write | high | mitigate | Single `inWriteTransaction`, integrity pre-resolution before reparent, assert-exactly-one-row on the absorbed delete, rollback-on-throw — no half-consolidated state. |
| T-20-01-2 | Tampering | absorbed-identity retirement | medium | mitigate | Generic `contact` tombstone + `compareRowAndTombstone` makes the merged-away contact beat any older row on restore/sync (RCN-04). |
| T-20-01-3 | Tampering | merge SQL | low | mitigate | All values `?`-bound; table names are literals in constant SQL; no identifier interpolation (ASVS V5). |
| T-20-01-4 | Tampering / Data loss | custom_field_values reparent | high | mitigate | Collision pre-resolution deletes the absorbed's shared-field_def_id rows (snapshotting to field_history) before reparent, so UNIQUE(contact_id, field_def_id) cannot ABORT and silently roll back the merge; no custom value is coerced/rewritten (ADR-001). |
| T-20-01-5 | Tampering / Data loss | contact_method_provenance dedupe | high | mitigate | Provenance is remapped to the surviving method BEFORE any losing method is deleted, defeating the ON DELETE CASCADE that would otherwise erase the audit link; field_history is reparented, not orphaned. |
| T-20-01-6 | Tampering / Availability | field_history reparent SQL | high | mitigate | field_history is reparented with a modified_at-FREE `UPDATE field_history SET contact_id=? WHERE contact_id=?`; applying the generic modified_at template would raise `no such column: modified_at` and roll back every merge of a contact that has history rows. A non-empty-field_history merge test proves the ABORT cannot occur. |
| T-20-01-7 | Data loss | absorbed lifecycle columns dropped on delete | medium | mitigate | favourite_rank/ring_seq/snooze_until follow explicit survivor-wins, but each non-null absorbed value is snapshotted to field_history in the same txn before the absorbed-row delete — the discard is auditable, not silent. |
| T-20-01-8 | Tampering / Integrity | primary-method resolution silently overridden | medium | mitigate | Step (1a) consults per-method_type `resolutions.primaryMethod[type]` before demoting, so a user's explicit choice to keep the absorbed's primary is applied (promote absorbed / demote survivor) for EACH contended type independently rather than blindly demoted — the 20-02 control cannot ship decorative; a merge-dao test proves the chosen primary wins, including phone-AND-email dual contention (reviewer HIGH #3 / finding 1). |
| T-20-01-9 | Data loss / Integrity | absorbed pending reconcile card cascade-dropped | medium | mitigate | Before the absorbed-row DELETE, step (4c) explicitly deletes any `reconciliation_session_cards` for the absorbed contact and RE-FINALIZES the owning session by the DERIVED non-terminal-card count (the same terminal model as 20-04's `finalizeSessionIfTerminal`; NO session-level `unresolved_count` column exists to decrement — referencing one would abort the merge, HIGH #1) in the same txn, defeating the ON DELETE CASCADE that would otherwise silently drop the card and strand a 'pending' session; a merge test proves the session is finalized by the derived count and left consistent (reviewer finding 2 / HIGH #1). |
</threat_model>

<verification>
- `npx vitest run src/db/merge-dao.test.ts src/db/migrations/013-reconciliation-and-merge.test.ts src/backup/reconciliation.test.ts` green.
- `npm test` (full suite) green after the wave.
- `npm run check:colors` and `npx tsc --noEmit` clean.
- Device-UAT (harvested end-of-phase): merge atomicity + survivor-profile landing on the Pixel.
</verification>

<success_criteria>
- Migration 013 registered, TARGET_VERSION=13, forward migration proven v12→v13.
- `mergeContacts` is the sole atomic merge writer: reparent-not-delete, integrity pre-resolution, field_history on scalar overwrite, tombstone retirement, recompute-last, full rollback on throw.
- A conflict-free merge is walkable end-to-end from the profile.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — bumps `TARGET_VERSION` to **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards` (card_status/diff_json/unresolved_count/**`staged_photo_rel_path` nullable — finding 9**), `reconcile_source_snapshot` (external_contact_link_id/field_family/reviewed_value), `bulk_review_resolutions` (import_session_row_id/flag_type/resolution/resolved_at — durable Fix/Ignore store consumed by 20-06). No `contact_redirects` table (RESOLVED owner decision 2026-08-30: tombstone-only).
- **DB DAOs/functions:** `mergeContacts` + exported `MergeResolutions` type (**`primaryMethod` is KEYED BY method_type — a per-type map, finding 1**) + `normalizeMergeResolutions` (src/db/merge-dao.ts); mergeContacts step (4c) deletes the absorbed's pending `reconciliation_session_cards` + RE-FINALIZES the owning session by a derived non-terminal-card count in-txn (finding 2 / HIGH #1 — no session-level `unresolved_count` column exists); `listMergeCandidates` (net-new Bound+Unbound merge-candidate read, src/db/merge-candidate-read.ts — finding 1); `setContactPhotoCore` (net-new non-mutexed core extracted in src/db/contacts-dao.ts); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (src/db/reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (src/db/reconcile-session-dao.ts); `getResumableReconcileSession`/`getNewestPendingReconcileSessionId`/`mapReconcileSession`/completion-count reads (src/db/reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (src/db/reconcile-relink-dao.ts); the bulk-review flag read (src/db/bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` write path (src/db/bulk-review-dao.ts).
- **Photo staging (reconcile):** `SAFE_RECONCILE_STAGING_RELATIVE` + `assertSafeReconcileStagingRelative` (src/db/photo-relative-path.ts); `reconcileStagingRelPath`/`stageReconcilePhoto`/`resolveReconcileStagingUri`/`listReconcileStagingPhotos`/`deleteReconcileStaging` (src/services/photos/photo-storage.ts); `stageReconcileSourcePhoto` (durable copy + content hash) and `promoteReconcilePhoto` (resize→persistMaster→durable avatars path) (src/services/photos/reconcile-photo.ts) — the reconcile analog of the import photo-staging pipeline (20-03).
- **Pure logic:** `classifyReconciliation` + its outcome/field-family union types (src/logic/reconcile-diff.ts); `recommendSurvivor` (src/logic/survivor-recommendation.ts).
- **Service:** `registerReconcileResumeSweep` (src/services/import/reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup` (src/components/FieldChoiceGroup.tsx); `PhotoChoice` (src/components/PhotoChoice.tsx); `MergeImpactSummary` (src/components/MergeImpactSummary.tsx); `ResumeReconcilePrompt` (src/components/ResumeReconcilePrompt.tsx — reconcile-specific resume sheet built on the ResumeImportPrompt idiom, 20-05). Also EXTENDS shipped `CandidateCardGrid` (src/components/CandidateCardGrid.tsx) additively — new `BulkAction` members `keep-orbit`/`use-contact-values` + labels + a caller-supplied scan label (20-04).
- **Screens + routes (added to RootStackParamList/RootNavigator):** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-reconciliation-and-merge.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-bulk-eligibility.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, resume-prompt-precedence.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, and an extension to src/backup/reconciliation.test.ts.

> **Schema-gate note:** this project ships schema as forward-only application-code migrations via `PRAGMA user_version` (NOT an ORM with a `db push` step). The gate's push command does not apply; the migration is verified by `013-reconciliation-and-merge.test.ts` (v12→v13 forward + FK integrity), which is the equivalent verification. No `[BLOCKING]` push task is warranted.
> **API-coverage note:** Phase 20 integrates NO external/network API. It reads the on-device system Contacts provider through the already-shipped native `orbit-contact-picker` module (Phase 19); reconciliation is strictly local and one-way. No `COVERAGE.md` matrix rows are warranted (equivalent to a reasoned "No external API integration" declaration).

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-01-SUMMARY.md` when done.
</output>
