---
phase: 20-contact-reconciliation-merge
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/logic/survivor-recommendation.ts
  - src/logic/survivor-recommendation.test.ts
  - src/components/FieldChoiceGroup.tsx
  - src/components/PhotoChoice.tsx
  - src/screens/SurvivorSelectScreen.tsx
  - src/screens/MergeConflictsScreen.tsx
  - src/components/MergeImpactSummary.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [RCN-03]
estimate:
  tokens: 66000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SurvivorSelect badges one candidate `Recommended` (colourless advisory chip) from continuity signals — older created_at, more interactions, stronger external linkage, more complete data — and preselects it, fully overridable (RCN-03 / Cluster V)."
    - "Conflicting scalar fields (name, birthday, category, photo, scalar custom fields) each render a FieldChoiceGroup / PhotoChoice with NO preselection — the user must make an explicit manual choice (LOCKED 20-UI-SPEC.md:239, Cluster C); non-conflicting fixed/custom values combine automatically and are not shown as a choice (Cluster W/Z). Only additive/removed modes preselect (additive → source deselectable; removed → Orbit preselected)."
    - statement: "FLAGGED OWNER DEFERRAL (reviewer MEDIUM finding 3) — the LOCKED 20-UI-SPEC is INTERNALLY INCONSISTENT on merge-conflict preselection: reusable-widget contract surface #5 mandates conflicting-mode with NO preselection (VERIFIED :238-239), while merge-screen surface #12 says each scalar conflict renders the 'survivor value PRESELECTED but overridable' (VERIFIED :350). A single locked contract cannot mandate both for the same widget. 20-02 KEEPS the SAFER widget-contract #5 rule (no preselection — the user must choose, which cannot silently overwrite a survivor value) as the current implementation rule, and does NOT pick a side unilaterally. This is surfaced for the OWNER to reconcile the spec (#5 vs #12); the planner is neither reversing the locked doc nor silently choosing #12. If the owner rules for #12 (survivor-preselected merge conflicts), the conflict-mode preselection here changes accordingly."
      verification: backstop
    - "A primary phone/email choice appears ONLY when both contacts contribute competing primary methods; otherwise deterministic promotion applies silently (Cluster X). The chosen `primaryMethod` is carried on `MergeResolutions` and CONSUMED by `mergeContacts` step (1a) — 20-01 was fixed to consult it rather than unconditionally demote the absorbed's, so this control is not decorative (reviewer HIGH #3). Continue is DISABLED until a competing-primary contention is explicitly resolved."
    - "FieldChoiceGroup performs no DB writes — it returns the user's selection; the merge write applies only the selected values and the unselected values are simply not written (never silently destroyed)."
    - "The merge resolutions chosen in the conflicts step are threaded into mergeContacts and applied through the writers 20-01 established — fixed scalars through a COMPLETE UpdateContactFullInput to updateContactMetadataCore, scalar custom fields through upsertValueCore, and a photo choice through setContactPhotoCore — all inside the one merge transaction, with each overwritten survivor scalar snapshotted to field_history. 20-02 adds NO new write path and does not modify merge-dao.ts; it shapes and returns the NAMED `MergeResolutions` object it IMPORTS from merge-dao.ts (20-01) — the shape is type-enforced by import, not matched by convention (reviewer finding 1)."
    - "Primary-method contention reuses `choosePrimary(groups, uid): MethodGroups` from contact-methods-editor-model.ts — VERIFIED (line 112) to be a PURE, headless function (takes MethodGroups, returns MethodGroups; no editor-component/hook coupling), so it is called standalone in MergeConflictsScreen without forking a new picker (reviewer finding 10)."
    - "MergeImpactSummary shows the real child-row counts moving to the survivor and the accent confirm + danger warning line (owner-locked accent confirm, 2026-08-30)."
    - statement: "A long custom-field value or long name holds the FieldChoiceGroup option row without clipping the provenance label or the accent check (E5 long-text backstop)."
      verification: backstop
    - statement: "FieldChoiceGroup renders identically for a 2-option conflict and an N-option multi-source disagreement (E5 zero-one-many backstop)."
      verification: backstop
  prohibitions:
    - "a bulk source-wins action must NEVER overwrite a name/birthday/scalar conflict — only additive-only selections"
    - "merge must NEVER silently destroy the non-chosen scalar value without the field_history snapshot"
  artifacts:
    - src/logic/survivor-recommendation.ts
    - src/components/FieldChoiceGroup.tsx
    - src/components/PhotoChoice.tsx
    - src/screens/MergeConflictsScreen.tsx
  key_links:
    - "SurvivorSelect → MergeConflictsScreen (FieldChoiceGroup/PhotoChoice) → MergeImpactSummary → mergeContacts(resolutions)"
    - "FieldChoiceGroup and PhotoChoice are the ONE reusable renderers consumed by both merge (this plan) and reconciliation (20-03)"
    - "primary-method contention routes through ContactMethodsEditor's primary-choose model"
---

<objective>
**As a** user merging two contacts that actually disagree, **I want to** choose which name/birthday/category/photo/primary-method survives, **so that** the merge keeps the right values and never silently overwrites an Orbit choice.

This expands the 20-01 tracer from the conflict-free happy path to the full merge UX: the recommended-survivor heuristic, the reusable `FieldChoiceGroup` / `PhotoChoice` scalar resolvers (also consumed by reconciliation in 20-03), and the scalar-conflict + primary-method-contention resolution step that feeds real `resolutions` into `mergeContacts`.

Purpose: build the ONE reusable per-field A-vs-B(-vs-C) renderer (Cluster Z requires reuse, not per-call-site forks) and complete the serious merge flow.
Output: `survivor-recommendation` logic, `FieldChoiceGroup`, `PhotoChoice`, `MergeConflictsScreen`, and MergeImpactSummary real counts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: survivor-recommendation heuristic (pure) + Recommended badge in SurvivorSelect</name>
  <files>src/logic/survivor-recommendation.ts, src/logic/survivor-recommendation.test.ts, src/screens/SurvivorSelectScreen.tsx</files>
  <read_first>
    - src/services/import/duplicate-evidence.ts (5-32 — the pure, node-testable classify-with-a-tunable-ladder idiom: exported union types, tunable weight constants at file top per CLAUDE.md, no I/O)
    - src/screens/SurvivorSelectScreen.tsx (from 20-01 — add the recommended badge + preselect)
    - src/components/ConfidenceChip.tsx (the colourless surfaceElevated chip / textSecondary label idiom to reuse for the `Recommended` advisory badge)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #11; recommendation-state rule: colourless, no caution hue, never a raw score)
  </read_first>
  <action>
    Create `src/logic/survivor-recommendation.ts` — a pure function `recommendSurvivor(a, b)` returning which candidate id is recommended plus the continuity signals that drove it, with tunable weight constants at the file top (older created_at, more interactions, stronger external linkage — count of is_active external_contact_links, more complete data — filled scalar/method count). Advisory only; deterministic; no I/O; output must be overridable in the UI. Wire it into SurvivorSelectScreen: badge the recommended card with a colourless `Recommended` chip (ConfidenceChip idiom — surfaceElevated bg, textSecondary label, never accent/rogue/status colour) and preselect it, still fully overridable. Do NOT render a raw score. Write `src/logic/survivor-recommendation.test.ts` covering each signal and ties.
  </action>
  <verify>
    <automated>npx vitest run src/logic/survivor-recommendation.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `src/logic/survivor-recommendation.ts` contains `export function recommendSurvivor(`.
    - `npx vitest run src/logic/survivor-recommendation.test.ts` exits 0.
    - `grep -n "Recommended" src/screens/SurvivorSelectScreen.tsx` shows the advisory badge; the badge uses `colors.surfaceElevated`/`colors.textSecondary` (colourless), not accent/status tokens.
  </acceptance_criteria>
  <done>SurvivorSelect preselects and badges a recommended survivor from continuity signals, overridable and colourless.</done>
</task>

<task type="auto">
  <name>Task 2: FieldChoiceGroup + PhotoChoice reusable scalar resolvers</name>
  <files>src/components/FieldChoiceGroup.tsx, src/components/PhotoChoice.tsx</files>
  <read_first>
    - src/screens/ImportReviewScreen.tsx (388-420 selected=accent-fill+borderStrong choice-pair idiom; 352-361 expo-image Image with onError→Avatar fallback)
    - src/components/CandidateCardGrid.tsx (162-179 — photo render: pre-resolved file:// URI to expo-image Image, onError fall-through to Avatar photo={null})
    - src/components/Avatar.tsx (initials fallback; NOTE it throws synchronously on a raw/staged path — never pass a raw path)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md ("Reusable widget contract — FieldChoiceGroup" section in full; surface #5 and #6; preselection rules: additive preselect source deselectable / conflict NO preselect / removed preselect Orbit with a danger-labelled never-recommended Remove option)
    - src/theme (useTheme().colors.* — no hex literals)
  </read_first>
  <action>
    Create `src/components/FieldChoiceGroup.tsx` — the single reusable A-vs-B(-vs-C) scalar resolver (surface #5). Props: field label, an array of 2..N options (each { value, provenance label, recommended?, danger? }), preselection mode ('additive' | 'conflict' | 'removed'), and an onChange returning the selected option (performs NO DB writes). Header = label (13px/600 textSecondary). Option rows are Pressables (≥44px, radius 10, surface bg, border), single-select radio semantics, selected row = borderStrong outline + accent check ✓; the recommended option carries a colourless `Recommended` surfaceElevated chip. Preselection per mode: additive preselects the source value (deselectable), conflict preselects nothing (explicit manual choice), removed preselects the Orbit value with a `danger`-labelled `Remove` option that is never recommended. Long values/names must wrap or ellipsize without clipping the provenance label or the accent check. Create `src/components/PhotoChoice.tsx` — the photo-specialized sibling (surface #6): N photo thumbnails side by side via expo-image Image (pre-resolved file:// URIs only, onError fall-through to Avatar photo={null} — never a raw/staged path), each tappable to select, plus an explicit `No meaningful change — keep Orbit photo` option; same selected-outline idiom. Both consume useTheme().colors.* exclusively.
  </action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/FieldChoiceGroup.tsx` contains `export function FieldChoiceGroup(` and single-select radio semantics with a selected borderStrong+accent-check row.
    - `src/components/PhotoChoice.tsx` renders photos via expo-image Image with an onError fallback to Avatar (no raw path passed to Avatar).
    - `npm run check:colors` passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Both reusable choice renderers exist and honour the preselection + colourless-recommendation + resolved-URI rules; no DB writes inside them.</done>
</task>

<task type="auto">
  <name>Task 3: Merge conflict resolution step + real impact counts feeding mergeContacts</name>
  <files>src/screens/MergeConflictsScreen.tsx, src/components/MergeImpactSummary.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/db/merge-dao.ts (mergeContacts(exec, { survivorId, absorbedId, resolutions, now }) + the EXPORTED `MergeResolutions` type + `normalizeMergeResolutions` from 20-01 — IMPORT `MergeResolutions` and build the resolutions object against that type, do NOT re-declare an implied shape and do NOT change the writer here)
    - src/db/contacts-dao.ts (setContactPhotoCore from 20-01) + src/db/field-values-dao.ts (upsertValueCore) — the writers mergeContacts uses; 20-02 only shapes their inputs
    - src/components/contact-methods-editor-model.ts (112 — `choosePrimary(groups, uid): MethodGroups`, VERIFIED a PURE headless function, not editor-component-coupled; call it standalone for the competing-primary case) + src/db/contact-methods-dao.ts
    - src/db/purge-dao.ts (121-198 computeImpact / count SQL for the MergeImpactSummary counts)
    - src/components/FieldChoiceGroup.tsx, src/components/PhotoChoice.tsx (Task 2)
    - src/screens/SurvivorSelectScreen.tsx (Continue → route into this screen)
    - src/screens/MergeImpactSummary usage from 20-01 (wire real counts)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #12 — only conflicting scalar fields render; child data never shown row-by-row; primary contention only when both contribute competing primaries)
  </read_first>
  <action>
    Create `src/screens/MergeConflictsScreen.tsx` (route params { survivorId, absorbedId }). Detect scalar conflicts between survivor and absorbed across name, birthday, category, photo, and scalar custom fields — render one `FieldChoiceGroup` per conflicting scalar in `conflict` mode (NO preselection — explicit manual choice, per LOCKED 20-UI-SPEC.md:239) and a `PhotoChoice` for a photo conflict. Non-conflicting fixed/custom values combine automatically and are NOT shown; identical canonical methods dedupe and distinct methods are all preserved — none shown as a choice. Add primary-method contention ONLY when both contacts contribute a competing is_primary=1 method of the same type, using the ContactMethodsEditor `choosePrimary` model. Child data (interactions/fuel/events/links) is never shown row-by-row. The collected `resolutions` object is typed as the `MergeResolutions` type IMPORTED from merge-dao.ts (20-01) — carrying fixed-scalar choices, scalar-custom-field choices (keyed by field_def_id, applied by mergeContacts via upsertValueCore), a primary-method choice, and a photo choice (applied by mergeContacts via setContactPhotoCore); build it against the imported type (optionally via `normalizeMergeResolutions`), do NOT re-declare the shape, do NOT add a new write path or edit merge-dao.ts. `Continue` (accent) is DISABLED until every conflict field AND any competing-primary contention has an explicit selection (the enable predicate covers both scalar FieldChoiceGroups and the primary-method pick — finding 8). Update MergeImpactSummary (from 20-01) to compute the REAL child-row counts by running the purge-dao per-family count SQL shapes directly (read-only) — do NOT call `impactSummaryLines`, which omits methods/external links — and to call `mergeContacts(exec, { survivorId, absorbedId, resolutions, now })` with the resolutions. Register MergeConflictsScreen in RootNavigator + types. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: merge two contacts with a differing name + birthday + competing primary phone; confirm each conflict renders a FieldChoiceGroup / primary choice with NO preselection (Continue stays disabled until an explicit choice is made for every conflict), then choosing values and confirming writes the chosen values and retires the absorbed contact. (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/MergeConflictsScreen.tsx` exists, is registered in RootNavigator/types, and renders a FieldChoiceGroup per scalar conflict only, each in `conflict` mode (no preselection).
    - MergeConflictsScreen does NOT modify the merge writer: `git diff --name-only` for this task does not include `src/db/merge-dao.ts`; MergeConflictsScreen/MergeImpactSummary IMPORT the `MergeResolutions` type from `@/db/merge-dao` (`grep -n "MergeResolutions" src/screens/MergeConflictsScreen.tsx` shows the import); MergeImpactSummary calls `mergeContacts(` with a `resolutions` argument typed as `MergeResolutions`.
    - `grep -n "impactSummaryLines" src/components/MergeImpactSummary.tsx` returns nothing (counts come from the per-family count SQL, which includes methods + external links).
    - `grep -n "choosePrimary\|primary" src/screens/MergeConflictsScreen.tsx` shows primary-contention handling gated on both-contribute-competing-primary.
    - Continue stays DISABLED while a competing-primary contention is unresolved: MergeConflictsScreen's Continue-enabled predicate includes an unresolved-primary check (not only scalar `FieldChoiceGroup` selections), so a competing-primary case with no explicit pick blocks Continue exactly like an unresolved scalar conflict — verified by a test/assertion over the screen's enable predicate (reviewer finding 8; ties off the UI half of HIGH #3). The `primaryMethod` choice collected here is the one the 20-01 writer consumes in step (1a)."
    - `npx tsc --noEmit`, `npm run check:colors`, and `npm test` all pass.
  </acceptance_criteria>
  <done>A user can resolve real scalar/primary conflicts and the chosen resolutions flow atomically into mergeContacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → local DB | Conflict resolutions selected in the UI drive which survivor scalars are overwritten; the risk is silently destroying an unchosen value. |

> No external network; no npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02-1 | Repudiation / Tampering | survivor scalar overwrite | medium | mitigate | Overwrites apply only user-selected values and snapshot the old value to field_history inside the merge txn; unselected values are not written, not destroyed. |
| T-20-02-2 | Tampering | primary-method contention | low | mitigate | Competing primary demotion resolved before reparent via the ContactMethodsEditor model + partial-unique-index awareness. |
</threat_model>

<verification>
- `npx vitest run src/logic/survivor-recommendation.test.ts` green; `npm test` green after the wave.
- `npx tsc --noEmit` and `npm run check:colors` clean.
- Device-UAT (end-of-phase): conflict resolution + primary contention observed on the Pixel.
</verification>

<success_criteria>
- Recommended-survivor heuristic drives an overridable, colourless advisory badge.
- One reusable FieldChoiceGroup/PhotoChoice serves scalar conflicts (this plan) and reconciliation (20-03).
- Real conflict resolutions + real impact counts flow into the atomic merge.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — bumps `TARGET_VERSION` to **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`, `bulk_review_resolutions`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` + exported `MergeResolutions` type + `normalizeMergeResolutions` (merge-dao.ts); `setContactPhotoCore` (contacts-dao.ts, 20-01); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` (bulk-review-dao.ts).
- **Pure logic:** `classifyReconciliation` (reconcile-diff.ts); `recommendSurvivor` (survivor-recommendation.ts).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`, `ResumeReconcilePrompt` (20-05); shipped `CandidateCardGrid` EXTENDED additively (new `BulkAction` members + labels + scan label, 20-04).
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-bulk-eligibility.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, resume-prompt-precedence.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-02-SUMMARY.md` when done.
</output>
