---
phase: 20-contact-reconciliation-merge
plan: 06
type: execute
wave: 6
depends_on: [20-05]
files_modified:
  - src/screens/BulkReviewScreen.tsx
  - src/db/bulk-review-read.ts
  - src/db/bulk-review-read.test.ts
  - src/screens/SettingsScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
requirements: [RCN-02]
autonomous: true
estimate:
  tokens: 56000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A `Review flagged items` bulk-review surface lives alongside the reconciliation workspace and lists per-item flags the bulk import pipeline previously only counted — starting with unreadable birthdays retained in the import row's source_payload (carried forward from Phase 19.1, owner-approved 2026-08-30)."
    - "Each flagged birthday row offers `Fix` (birthday TextInput + normalizeEditedBirthday + invalid-date error state, reusing the ImportReview idiom; a valid value clears the flag and stores the birthday) and `Ignore` (clears the flag, keeps no birthday — non-destructive of any existing data)."
    - "Flags are durable and persist until resolved; the surface is generic over bulk-review cases (birthday is the first) so a later flag type reuses the same row idiom."
    - "Empty state: heading `Nothing to review`, body `No flagged items right now.`"
    - statement: "Fixing a birthday uses the single birthday parser/normalizer (formatLocalDate / normalizeEditedBirthday), never toISOString().split — no UTC off-by-one."
      verification: backstop
  prohibitions:
    - "Ignore must NEVER destroy any existing Orbit data — it only clears the review flag"
  artifacts:
    - src/screens/BulkReviewScreen.tsx
    - src/db/bulk-review-read.ts
  key_links:
    - "Settings/entry → BulkReviewScreen → Fix (normalizeEditedBirthday, store birthday) / Ignore (clear flag)"
    - "bulk-review-read reads unreadable-birthday flags from import row source_payload"
---

<objective>
**As a** user who bulk-imported contacts, **I want to** resolve the birthdays Orbit couldn't read at import time, **so that** those contacts get correct birthdays instead of the count just sitting on a summary.

This carries forward the Phase 19.1 deferred bulk-review surface: the durable per-item resolver for flags the bulk pipeline only counted before (starting with unreadable birthdays), living alongside the reconciliation workspace. It closes the phase and gates end-of-phase verification.

Purpose: give the counted-but-unresolvable import flags a durable resolve UI, generic enough for future flag types, then run phase verification + device UAT.
Output: `BulkReviewScreen`, `bulk-review-read`, the entry point, and the phase verification gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulkReviewScreen (unreadable-birthday resolver) + read + entry</name>
  <files>src/screens/BulkReviewScreen.tsx, src/db/bulk-review-read.ts, src/db/bulk-review-read.test.ts, src/screens/SettingsScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/screens/ImportReviewScreen.tsx (170-182, 487-515 — the birthday TextInput + normalizeEditedBirthday + invalid-date error copy idiom to reuse)
    - src/db/import-session-read.ts (277 area — the isBirthdayUnreadable / unreadable-birthday count idiom; find where the raw value is retained in the import row source_payload)
    - src/logic/picked-contact-map.ts (67 — birthday normalize/unreadable mapping)
    - src/utils/dates.ts (formatLocalDate — never toISOString().split('T')[0])
    - src/db/contacts-dao.ts (308-335 updateContactMetadataCore — to store a fixed birthday through the existing writer)
    - src/screens/SettingsScreen.tsx (Contacts Integration section — add the entry point beside Check linked contacts) and src/navigation/RootNavigator.tsx + types.ts
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #14 Bulk review; copy: `Review flagged items`, `Birthday: "{raw}" — couldn't be read`, Fix / Ignore, empty `Nothing to review` / `No flagged items right now.`)
  </read_first>
  <action>
    Create `src/db/bulk-review-read.ts` — read the per-item unreadable-birthday flags the bulk import pipeline retained (raw value in the import row `source_payload`), returning a generic flag shape (contact name/id, flag type `birthday`, raw value) so future flag types reuse it. Create `src/screens/BulkReviewScreen.tsx`: header `Review flagged items`; a single-column list, each row showing the contact name + `Birthday: "{raw}" — couldn't be read` (13px textSecondary) + two affordances: `Fix` (opens the birthday TextInput + `normalizeEditedBirthday` + invalid-date error state; on a valid value, store the birthday through `updateContactMetadataCore` using `formatLocalDate` — never toISOString().split — and clear the flag) and `Ignore` (clears the flag, keeps no birthday, destroys no existing data). Flags are durable until resolved. Empty state: heading `Nothing to review`, body `No flagged items right now.` Add the entry point to Settings Contacts Integration and register the route. All colours via tokens. Write `src/db/bulk-review-read.test.ts` proving the read surfaces unreadable-birthday flags from source_payload and omits resolved ones.
  </action>
  <verify>
    <automated>npx vitest run src/db/bulk-review-read.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: bulk-import contacts including one with an unparseable birthday; open Review flagged items, Fix one (valid date stores + clears the flag), Ignore another (flag clears, no birthday, contact otherwise intact). (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/BulkReviewScreen.tsx` exists, is registered, and renders Fix/Ignore per flagged row with the empty state.
    - Fix stores the birthday via `updateContactMetadataCore` using `formatLocalDate`: `grep -n "toISOString" src/screens/BulkReviewScreen.tsx` returns nothing.
    - `npx vitest run src/db/bulk-review-read.test.ts` exits 0; `npx tsc --noEmit`, `npm run check:colors` pass.
  </acceptance_criteria>
  <done>The durable bulk-review surface resolves unreadable-birthday flags via Fix/Ignore, generic over future flag types.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 20 verification gate + device-UAT harvest</name>
  <files>(verification only — no new files; any fixes land in the already-listed Phase 20 test files)</files>
  <read_first>
    - .planning/phases/20-contact-reconciliation-merge/20-VALIDATION.md (Wave 0 requirements + requirement→test map — confirm every RCN test file exists and is green)
    - src/db/merge-dao.test.ts, src/db/reconcile-session-read.test.ts, src/logic/reconcile-diff.test.ts (the RCN-01..04 coverage)
    - the device-UAT pattern in MEMORY (device-uat-runas-pattern): debug build, install -r, run-as WAL-aware DB reads, orbit Metro on :8082 + adb reverse
  </read_first>
  <action>
    Run the full suite and confirm every Phase 20 test file from 20-VALIDATION.md is green (merge-dao, migrations/013, reconcile-diff, reconcile-snapshot-dao, reconcile-session-dao, reconcile-session-read, reconcile-relink-dao, bulk-review-read, reconcile-resume-sweep, survivor-recommendation, and the extended backup/reconciliation resurrection test). Fix any red. Confirm `npx tsc --noEmit` and `npm run check:colors` are clean across the phase's touched files. Do not run git worktree, git push, or graph:build. This task carries the consolidated end-of-phase device-UAT (harvested from every `<human-check>` in plans 20-01..06): merge atomicity + survivor-profile landing; conflict + primary-method resolution; per-contact reconcile additive/conflict/no-renag; bulk check + partial resolve + summary; process-death resume with applied-changes-preserved; missing-source/relink non-destructive; bulk-review Fix/Ignore — all on the Pixel via the debug-build run-as pattern.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>Consolidated Pixel device-UAT (debug build, run-as DB reads): (1) merge two contacts atomically → survivor profile, absorbed gone + tombstoned (not archived); (2) merge with name/birthday/primary conflict resolves correctly; (3) per-contact Update from Contacts recommends additions, requires manual conflict choice, no re-nag on kept values; (4) bulk Check linked contacts shows only changed contacts, partial resolve persists, summary counts correct, Use Contact Values absent with a conflict selected; (5) kill mid-review → Resume restores unresolved, applied changes stay; (6) deleted phone contact → Source missing (not removals), Relink re-runs review, Orbit data intact; (7) Review flagged items Fix/Ignore. Owner sign-off gates phase completion.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green including all Phase 20 test files).
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - Every 20-VALIDATION.md Wave 0 test file exists and is green.
  </acceptance_criteria>
  <done>Full suite + tsc + check:colors green; all Phase 20 RCN test files present and passing; consolidated device-UAT harvested for owner sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained import flag (source_payload) → Orbit | Resolving a flag writes a birthday through the existing writer; Ignore must not touch existing data. |

> No network on any read path. No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06-1 | Input Validation | birthday Fix input | medium | mitigate | Route through normalizeEditedBirthday + invalid-date error state + formatLocalDate; store via updateContactMetadataCore (?-bound); Ignore only clears the flag. |
| T-20-06-2 | Tampering | flag read SQL | low | mitigate | Read is `?`-bound over import rows; no identifier interpolation. |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run check:colors` all green.
- Every Phase 20 test file from 20-VALIDATION.md present and passing.
- Consolidated device-UAT on the Pixel harvested for owner sign-off.
</verification>

<success_criteria>
- Durable bulk-review surface resolves unreadable-birthday flags (generic over future types) non-destructively.
- Phase 20 verification gate passes: full suite + tsc + check:colors green; device-UAT ready for owner sign-off.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` (merge-dao.ts); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); `bulk-review-read.ts` read.
- **Pure logic:** `classifyReconciliation` (reconcile-diff.ts); `recommendSurvivor` (survivor-recommendation.ts).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`.
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, bulk-review-read.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-06-SUMMARY.md` when done.
</output>
