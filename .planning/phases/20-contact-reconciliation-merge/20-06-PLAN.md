---
phase: 20-contact-reconciliation-merge
plan: 06
type: execute
wave: 6
depends_on: [20-05]
files_modified:
  - src/screens/BulkReviewScreen.tsx
  - src/db/bulk-review-read.ts
  - src/db/bulk-review-read.test.ts
  - src/db/bulk-review-dao.ts
  - src/db/bulk-review-dao.test.ts
  - src/screens/SettingsScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
requirements: [RCN-02]
autonomous: true
estimate:
  tokens: 56000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A `Review flagged items` bulk-review surface lives alongside the reconciliation workspace and lists per-item flags the bulk import pipeline previously only counted — starting with unreadable birthdays derived from the import row's immutable source_payload (carried forward from Phase 19.1, owner-approved 2026-08-30). A flag is shown only when it is UNRESOLVED: the read LEFT JOINs `bulk_review_resolutions` (migration 013) and excludes any import_session_row that already has a `(row_id, 'birthday')` resolution row."
    - "Each flagged birthday row offers `Fix` (birthday TextInput + normalizeEditedBirthday + invalid-date error state, reusing the ImportReview idiom; a valid value stores the birthday through updateContactMetadataCore AND writes a `resolution='fixed'` row into bulk_review_resolutions in ONE inWriteTransaction) and `Ignore` (writes a `resolution='ignored'` row only — keeps no birthday, destroys no existing data). Both durably clear the flag because the next read excludes resolved rows."
    - "BACKUP VISIBILITY ON FIX (cycle-4 finding 4, same class as HIGH #1) — the Fix txn calls `bumpDataRevisionCore(exec)` once, because `updateContactMetadataCore` does NOT self-bump (bare `UPDATE contacts` — VERIFIED contacts-dao.ts:308-335) and the launch backup sweep gates on `data_revision` (VERIFIED backup-sweep.ts:29-31). Without the bump, a Fix that changes a durable birthday would leave `data_revision` unchanged and the corrected birthday could be SILENTLY OMITTED from the next automatic backup. `Ignore` writes only a resolution row (no contact-data change) and need not bump. Proven by a test asserting `data_revision` ADVANCES after a Fix (read before/after via `readDataRevision`)."
    - "Durable resolution persistence is real, not derived: because source_payload is immutable import history with no resolution field, the durable state lives in the `bulk_review_resolutions` table added in migration 013 (20-01), UNIQUE(import_session_row_id, flag_type). Resolution survives app restart and a resolved case never re-surfaces. The persistence is SCHEMA-GENERIC over `flag_type` (the table + `(row_id, flag_type)` row idiom) BUT the read/write DAO is BIRTHDAY-SPECIFIC for v1 — `resolveBulkReviewFlagCore` takes a `birthday` parameter and the read scans `isBirthdayUnreadable`. The migration's `flag_type CHECK IN ('birthday')` is deliberately single-valued for v1; a second flag type requires BOTH a new (irreversible) migration to widen the CHECK and new DAO/read code. Do NOT over-claim function-level genericity (reviewer finding 6)."
    - "A flagged row without a linked contact is safely handled: import_session_rows.contact_id is nullable (ON DELETE SET NULL); the read either excludes rows with a NULL contact_id or Fix guards against applying a birthday when contact_id is NULL (no crash, no orphan write)."
    - "Empty state: heading `Nothing to review`, body `No flagged items right now.`"
    - statement: "Fixing a birthday uses the single birthday parser/normalizer (formatLocalDate / normalizeEditedBirthday), never toISOString().split — no UTC off-by-one."
      verification: backstop
  prohibitions:
    - "Ignore must NEVER destroy any existing Orbit data — it only clears the review flag"
    - "Fix must NEVER store a birthday via updateContactMetadataCore without calling bumpDataRevisionCore in the same txn — updateContactMetadataCore does not self-bump, so an un-bumped Fix would silently escape the automatic-backup sweep (finding 4, same class as HIGH #1)"
  artifacts:
    - src/screens/BulkReviewScreen.tsx
    - src/db/bulk-review-read.ts
  key_links:
    - "Settings/entry → BulkReviewScreen → Fix (normalizeEditedBirthday, store birthday) / Ignore (clear flag)"
    - "bulk-review-read reads unreadable-birthday flags from import row source_payload"
---

<objective>
**As a** user who bulk-imported contacts, **I want to** resolve the birthdays Orbit couldn't read at import time, **so that** those contacts get correct birthdays instead of the count just sitting on a summary.

This carries forward the Phase 19.1 deferred bulk-review surface: the durable per-item resolver for flags the bulk pipeline only counted before (starting with unreadable birthdays), living alongside the reconciliation workspace. It closes the phase and gates end-of-phase verification.

Purpose: give the counted-but-unresolvable import flags a durable resolve UI on a schema-generic `bulk_review_resolutions` table (the DAO/read stay birthday-specific for v1), then run phase verification + device UAT.
Output: `BulkReviewScreen`, `bulk-review-read`, the entry point, and the phase verification gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulkReviewScreen (unreadable-birthday resolver) + read + entry</name>
  <files>src/screens/BulkReviewScreen.tsx, src/db/bulk-review-read.ts, src/db/bulk-review-read.test.ts, src/db/bulk-review-dao.ts, src/db/bulk-review-dao.test.ts, src/screens/SettingsScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/screens/ImportReviewScreen.tsx (170-182, 487-515 — the birthday TextInput + normalizeEditedBirthday + invalid-date error copy idiom to reuse)
    - src/db/import-session-read.ts (269-278 — the isBirthdayUnreadable(sourceBirthday(source_payload)) idiom; source_payload is immutable, so a resolved flag CANNOT be cleared by mutating it — the resolution lives in the new table)
    - src/db/migrations/012-import-sessions.ts (30-49 import_session_rows — source_payload TEXT NOT NULL immutable; contact_id nullable ON DELETE SET NULL; NO resolution field, which is exactly why bulk_review_resolutions exists)
    - src/db/migrations/013-reconciliation-and-merge.ts (bulk_review_resolutions from 20-01: import_session_row_id REFERENCES import_session_rows(id) ON DELETE CASCADE, flag_type CHECK IN ('birthday'), resolution CHECK IN ('fixed','ignored'), resolved_at, UNIQUE(import_session_row_id, flag_type))
    - src/db/import-session-dao.ts (109-119 assertOneChange; 172-198 *Core + thin inWriteTransaction wrapper duality — mirror for bulk-review-dao)
    - src/logic/picked-contact-map.ts (67 — birthday normalize/unreadable mapping)
    - src/utils/dates.ts (formatLocalDate — never toISOString().split('T')[0])
    - src/db/contacts-dao.ts (308-335 updateContactMetadataCore — store a fixed birthday via a COMPLETE UpdateContactFullInput, non-mutexed core inside the wrapper txn; it does NOT bump data_revision — bare UPDATE contacts)
    - src/db/data-revision-dao.ts (`bumpDataRevisionCore(exec)` — call ONCE in the Fix txn so a corrected birthday is backup-visible; `readDataRevision` for the advances-test) and src/services/backup-sweep.ts (29-31 — the sweep gates on readDataRevision→shouldRunAutomaticBackup, the reason the bump matters — finding 4)
    - src/screens/SettingsScreen.tsx (Contacts Integration section — add the entry point beside Check linked contacts) and src/navigation/RootNavigator.tsx + types.ts
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #14 Bulk review; copy: `Review flagged items`, `Birthday: "{raw}" — couldn't be read`, Fix / Ignore, empty `Nothing to review` / `No flagged items right now.`)
  </read_first>
  <action>
    Create `src/db/bulk-review-read.ts` — read the per-item UNRESOLVED unreadable-birthday flags: scan import_session_rows for `isBirthdayUnreadable(sourceBirthday(source_payload))`, LEFT JOIN `bulk_review_resolutions` and EXCLUDE any row that already has a `(import_session_row_id, 'birthday')` resolution, and exclude/handle rows with a NULL contact_id. Return a flag shape (import_session_row_id, contact name/id, flag_type `birthday`, raw value) whose STORAGE (the table + `(row_id, flag_type)` idiom) is schema-generic, while this read itself is birthday-specific (a future flag type adds new read/write code + a migration to widen the CHECK). Create `src/db/bulk-review-dao.ts` mirroring the import-session-dao core/wrapper duality: `resolveBulkReviewFlagCore(exec, { importSessionRowId, contactId, birthday, now })` — in the caller's txn store the birthday through `updateContactMetadataCore` (a COMPLETE UpdateContactFullInput built from the contact's current metadata), call `bumpDataRevisionCore(exec)` once (updateContactMetadataCore does NOT self-bump, so without this the corrected birthday could be omitted from the next automatic backup — finding 4), then INSERT a `resolution='fixed'` row into bulk_review_resolutions; `ignoreBulkReviewFlagCore(exec, { importSessionRowId, now })` — INSERT a `resolution='ignored'` row only; plus thin `inWriteTransaction` wrappers. All `?`-bound; assert one row changed. Create `src/screens/BulkReviewScreen.tsx`: header `Review flagged items`; a single-column list, each row showing the contact name + `Birthday: "{raw}" — couldn't be read` (13px textSecondary) + two affordances: `Fix` (opens the birthday TextInput + `normalizeEditedBirthday` + invalid-date error state; on a valid value, call the resolve wrapper — birthday stored via `updateContactMetadataCore` using `formatLocalDate`, never toISOString().split, AND the `fixed` resolution row written in the SAME txn) and `Ignore` (call the ignore wrapper — writes the `ignored` resolution row only, keeps no birthday, destroys no existing data). Both durably clear the flag because the read excludes resolved rows. Empty state: heading `Nothing to review`, body `No flagged items right now.` Add the entry point to Settings Contacts Integration and register the route. All colours via tokens. Write `src/db/bulk-review-read.test.ts` proving the read surfaces unreadable-birthday flags and OMITS a row once a resolution exists (both `fixed` and `ignored`), and omits/handles NULL contact_id. Write `src/db/bulk-review-dao.test.ts` proving Fix stores the birthday + writes a `fixed` resolution atomically, Fix ADVANCES `data_revision` (read before/after via `readDataRevision` — finding 4, so the corrected birthday is backup-visible), Ignore writes only an `ignored` resolution AND does NOT advance data_revision, the UNIQUE(import_session_row_id, flag_type) holds, and resolution survives reopening the DB (durable across restart).
  </action>
  <verify>
    <automated>npx vitest run src/db/bulk-review-read.test.ts src/db/bulk-review-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: bulk-import contacts including one with an unparseable birthday; open Review flagged items, Fix one (valid date stores + clears the flag), Ignore another (flag clears, no birthday, contact otherwise intact). (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/BulkReviewScreen.tsx` exists, is registered, and renders Fix/Ignore per flagged row with the empty state.
    - `src/db/bulk-review-dao.ts` contains `export ... resolveBulkReviewFlagCore(` and `ignoreBulkReviewFlagCore(`, writing to `bulk_review_resolutions`; `src/db/bulk-review-read.ts` LEFT JOINs `bulk_review_resolutions` and excludes resolved rows (`grep -n "bulk_review_resolutions" src/db/bulk-review-read.ts` shows the join).
    - Fix stores the birthday via `updateContactMetadataCore` using `formatLocalDate`, calls `bumpDataRevisionCore`, and writes the resolution in one txn: `grep -n "toISOString" src/screens/BulkReviewScreen.tsx src/db/bulk-review-dao.ts` returns nothing; `grep -n "bumpDataRevisionCore" src/db/bulk-review-dao.ts` shows the bump inside the Fix path (finding 4).
    - `npx vitest run src/db/bulk-review-read.test.ts src/db/bulk-review-dao.test.ts` exits 0, including a resolved-row-omitted case, a durable-across-DB-reopen case, and a Fix-ADVANCES-`data_revision` case (Ignore does not — finding 4); `npx tsc --noEmit`, `npm run check:colors` pass.
  </acceptance_criteria>
  <done>The durable bulk-review surface resolves unreadable-birthday flags via Fix/Ignore on a schema-generic table with a birthday-specific DAO/read for v1.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 20 verification gate + device-UAT harvest</name>
  <files>(verification only — no new files; any fixes land in the already-listed Phase 20 test files)</files>
  <read_first>
    - .planning/phases/20-contact-reconciliation-merge/20-VALIDATION.md (Wave 0 requirements + requirement→test map — confirm every RCN test file exists and is green)
    - src/db/merge-dao.test.ts, src/db/reconcile-session-read.test.ts, src/logic/reconcile-diff.test.ts (the RCN-01..04 coverage)
    - the device-UAT pattern in MEMORY (device-uat-runas-pattern): debug build, install -r, run-as WAL-aware DB reads, orbit Metro on :8082 + adb reverse
  </read_first>
  <action>
    Run the full suite and confirm every Phase 20 test file from 20-VALIDATION.md is green (merge-dao — incl. custom-field-collision default survivor-wins AND custom-field resolution favouring the ABSORBED value (upsertValueCore + survivor's prior value snapshotted — actionable 1), additive custom-field reparent, provenance-survives-dedupe, lifecycle-column drop-with-snapshot, normalizeMergeResolutions (per-type primaryMethod map), the NON-EMPTY field_history reparent case asserting no `no such column: modified_at`, the primary-method-resolution case proving a chosen ABSORBED primary wins / default is survivor-wins — HIGH #3, the DUAL phone-AND-email contention case — finding 1, the selected-primary-wins-canonical-dedupe case — finding 6, AND the pending-reconcile-card-discard-with-DERIVED-session-finalize case (session re-finalized by counting non-terminal cards, no non-existent `reconciliation_sessions.unresolved_count` reference) — HIGH #1 / finding 2; merge-candidate-read — Unbound-included / archived-and-self-excluded — finding 1; migrations/013 — incl. bulk_review_resolutions and the nullable `staged_photo_rel_path` column; reconcile-diff (incl. `serializeMethodFamily` order-independence), reconcile-snapshot-dao — incl. the multi-method SET serialization round-trip (actionable 2), reconcile-photo — incl. durable-staging + content-hash, reconcile-apply — the data_revision-ADVANCES-after-scalar-only case — HIGH #1 backup-visibility, the FRESHNESS case proving a post-scan Orbit edit is NOT overwritten on a resumed apply (staleFields) — HIGH #1 freshness, and the rollback-leaves-photo-intact / persistMaster-is-post-commit case — HIGH #2; reconcile-bulk-eligibility — additive-only excludes conflict/removed/missing, candidate-card-grid-actions — `use-contact-values` unavailable/blocked on a non-additive selection — HIGH #2, reconcile-session-dao — incl. createReconcileSessionCore (non-mutexed, composes inside an outer txn without deadlock — finding 4), the bulk-apply integration test (per-action fields/snapshots/completion-bucket increments, photo excluded from bulk auto-apply — finding 6/7), reconcile-session-read — incl. getNewestPendingReconcileSessionId, reconcile-relink-dao — incl. duplicate-active-link preflight; bulk-review-read, bulk-review-dao — incl. durable-across-restart AND Fix-advances-data_revision — finding 4; reconcile-resume-sweep, resume-prompt-precedence, survivor-recommendation, and the extended backup/reconciliation resurrection test). Fix any red. Confirm `npx tsc --noEmit` and `npm run check:colors` are clean across the phase's touched files. Record in `20-06-SUMMARY.md` the two ACCEPTED v1 LIMITATIONS this phase carries (reviewer LOW findings 5 & 6, documented not built): (5) reconcile apply adds accepted source methods without writing `contact_method_provenance` (only the import path writes provenance), so reconciled method additions carry weaker source attribution than imported ones in v1; (6) the post-merge survivor notification refresh is eventually-consistent — reconciled at the next launch-sweep foreground pass, not re-scheduled instantly. Do not run git worktree, git push, or graph:build. This task carries the consolidated end-of-phase device-UAT (harvested from every `<human-check>` in plans 20-01..06): merge atomicity + survivor-profile landing; conflict + primary-method resolution; per-contact reconcile additive/conflict/no-renag; bulk check + partial resolve + summary; process-death resume with applied-changes-preserved; missing-source/relink non-destructive; bulk-review Fix/Ignore — all on the Pixel via the debug-build run-as pattern.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>Consolidated Pixel device-UAT (debug build, run-as DB reads). ACCEPTED v1 LIMITATION to note during UAT (reviewer LOW finding 6): after a merge, the survivor's decay/birthday notification is refreshed EVENTUALLY-CONSISTENTLY — the merge cancels the absorbed's notifications post-commit, but the survivor's own notification is reconciled at the next launch-sweep foreground pass, not re-scheduled instantly. So a survivor notification may lag until the next foreground launch; this is expected, not a bug, and no immediate-reschedule path is added this phase (the launch sweep already reconciles it). (1) merge two contacts atomically → survivor profile, absorbed gone + tombstoned (not archived); (2) merge with name/birthday/primary conflict resolves correctly; (3) per-contact Update from Contacts recommends additions, requires manual conflict choice, no re-nag on kept values; (4) bulk Check linked contacts shows only changed contacts, partial resolve persists, summary counts correct, Use Contact Values absent with a conflict selected; (5) kill mid-review → Resume restores unresolved, applied changes stay; (6) deleted phone contact → Source missing (not removals), Relink re-runs review, Orbit data intact; (7) Review flagged items Fix/Ignore. Owner sign-off gates phase completion.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green including all Phase 20 test files).
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - Every 20-VALIDATION.md Wave 0 test file exists and is green.
  </acceptance_criteria>
  <done>Full suite + tsc + check:colors green; all Phase 20 RCN test files present and passing; consolidated device-UAT harvested for owner sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained import flag (source_payload) → Orbit | Resolving a flag writes a birthday through the existing writer; Ignore must not touch existing data. |

> No network on any read path. No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06-1 | Input Validation | birthday Fix input | medium | mitigate | Route through normalizeEditedBirthday + invalid-date error state + formatLocalDate; store via updateContactMetadataCore (complete input, ?-bound), call `bumpDataRevisionCore` so the corrected birthday is backup-visible (finding 4), AND write the `fixed` resolution in one txn; Ignore writes only an `ignored` resolution (no data touched, no bump). |
| T-20-06-2 | Tampering | flag read/write SQL | low | mitigate | Read + resolution writes are `?`-bound over import rows and bulk_review_resolutions; literal table names; no identifier interpolation. UNIQUE(import_session_row_id, flag_type) prevents duplicate/conflicting resolutions. |
| T-20-06-3 | Tampering / Data integrity | resolution durability | medium | mitigate | Durable state lives in bulk_review_resolutions (migration 013), not the immutable source_payload; a resolved case is excluded by the read and never re-surfaces across restart — no reliance on mutating import history. |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run check:colors` all green.
- Every Phase 20 test file from 20-VALIDATION.md present and passing.
- Consolidated device-UAT on the Pixel harvested for owner sign-off.
</verification>

<success_criteria>
- Durable bulk-review surface resolves unreadable-birthday flags non-destructively on a schema-generic table (birthday-specific DAO/read for v1).
- Phase 20 verification gate passes: full suite + tsc + check:colors green; device-UAT ready for owner sign-off.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`, `bulk_review_resolutions` (durable Fix/Ignore store this plan consumes); **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` + `MergeResolutions` type + `normalizeMergeResolutions` (merge-dao.ts); `setContactPhotoCore` (contacts-dao.ts, 20-01); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`getNewestPendingReconcileSessionId`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` (bulk-review-dao.ts).
- **Photo staging (reconcile):** `SAFE_RECONCILE_STAGING_RELATIVE`/`assertSafeReconcileStagingRelative` (photo-relative-path.ts); reconcile staging helpers (photo-storage.ts); `stageReconcileSourcePhoto`/`promoteReconcilePhoto` (reconcile-photo.ts) — 20-03.
- **Pure logic:** `classifyReconciliation` (reconcile-diff.ts); `isAdditiveOnlySelection` (reconcile-bulk-eligibility.ts, 20-04); `recommendSurvivor` (survivor-recommendation.ts); `resolveActiveResumePrompt` (resume-prompt-precedence.ts, 20-05).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`, `ResumeReconcilePrompt` (20-05); shipped `CandidateCardGrid` EXTENDED additively (20-04).
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-bulk-eligibility.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, resume-prompt-precedence.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-06-SUMMARY.md` when done.
</output>
