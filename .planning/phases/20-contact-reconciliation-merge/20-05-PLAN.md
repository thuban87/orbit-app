---
phase: 20-contact-reconciliation-merge
plan: 05
type: execute
wave: 5
depends_on: [20-04]
files_modified:
  - src/services/import/reconcile-resume-sweep.ts
  - src/services/import/reconcile-resume-sweep.test.ts
  - App.tsx
  - src/screens/ReconcileDetailScreen.tsx
  - src/db/reconcile-relink-dao.ts
  - src/db/reconcile-relink-dao.test.ts
requirements: [RCN-02, RCN-01]
autonomous: true
estimate:
  tokens: 60000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On foreground launch, an unresolved reconciliation session is offered for resume via the reused ResumeImportPrompt idiom (heading `Resume your check?`), registered on the launch sweep — never a background timer (RCN-02 / Cluster R; 'nothing watches a timestamp')."
    - "Resume restores the unresolved cards; Discard clears only unresolved review state and already-applied resolutions stay applied; the session is durable across process death, reboot, and long absence (RCN-02 / Cluster R)."
    - "A missing/unresolvable linked source is a distinct card/detail state (`Source missing`) offering exactly Keep as is / Relink to another contact / Unlink source — never delete, archive, unbind, or clear the Orbit contact (RCN-01 / Cluster G)."
    - "Relink retains all Orbit-owned data, retires the stale external link, attaches the newly selected source, and immediately re-runs reconciliation against the new source; no Orbit relationship data resets on relink (RCN-01 / Cluster L)."
    - statement: "An unreadable/corrupt resumable session offers Discard without crashing (E9 error backstop)."
      verification: backstop
  prohibitions:
    - "resume/relink must NEVER destroy, archive, unbind, or clear an Orbit contact when its source is missing"
    - "must NEVER implement resume with a background timer or passive polling — launch-sweep only"
  artifacts:
    - src/services/import/reconcile-resume-sweep.ts
    - src/db/reconcile-relink-dao.ts
  key_links:
    - "reconcile-resume-sweep registers via registerSweepHook and is wired in App.tsx after migration readiness"
    - "ReconcileDetailScreen missing-source branch → Keep / Relink (system picker) / Unlink"
    - "relink retires the stale external_contact_link and attaches the new one, then re-runs reconcile-diff"
---

<objective>
**As a** user who left a big check half-done or whose linked contact vanished from my phone, **I want to** resume exactly where I left off and safely handle a missing source, **so that** no unresolved work is lost and a deleted phone contact never damages my Orbit data.

This completes reconciliation durability: the launch-time resume prompt (reusing the shipped `ResumeImportPrompt` + the sweep-hook idiom) and the missing-source/relink lifecycle (a distinct state, never destructive).

Purpose: honor the "durable across process death" and "missing source never destroys Orbit" invariants using the shipped launch-sweep mechanism — never a timer.
Output: `reconcile-resume-sweep` service + App.tsx wiring, and the missing-source/relink branch + `reconcile-relink-dao`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile-resume-sweep launch hook + ResumeImportPrompt wiring</name>
  <files>src/services/import/reconcile-resume-sweep.ts, src/services/import/reconcile-resume-sweep.test.ts, App.tsx</files>
  <read_first>
    - src/services/import/contact-import-resume-sweep.ts (11 registerSweepHook import; 116-147 registerImportResumeSweep — the exact registration shape to mirror: importing the module does no work; resume newest; cleanup staged; onResumable callback)
    - src/services/launch-sweep.ts (registerSweepHook — runs once per real foreground launch, never on a headless tap or module import)
    - src/db/reconcile-session-read.ts (getResumableReconcileSession — from 20-04)
    - App.tsx (28 registerImportResumeSweep import; 206 registerImportResumeSweep(setResumableImport, { getExecutor }) — register the reconcile resume sweep the same way, after migration readiness)
    - src/components/ResumeImportPrompt.tsx (16-79 ResumeImportPromptProps — the non-tap-dismissable Modal sheet, accent Resume / danger Discard)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #9 Resume/Discard; copy: `Resume your check?` / `Unresolved contacts are saved. Changes you already applied stay applied.` / Resume / Discard)
  </read_first>
  <action>
    Create `src/services/import/reconcile-resume-sweep.ts` exporting `registerReconcileResumeSweep(onResumable, { getExecutor, now } = {})`, mirroring registerImportResumeSweep: inside `registerSweepHook`, call `getResumableReconcileSession(exec, now())`; if a resumable session exists, describe it and pass the description to `onResumable`; on error, log and continue (an unreadable session must not crash — surface Discard). Importing the module must do no work. Wire it in App.tsx alongside the existing import resume sweep — register after migration readiness with a state setter, and render the reused `ResumeImportPrompt` for the reconcile case: heading `Resume your check?`, body `Unresolved contacts are saved. Changes you already applied stay applied.`, Resume (accent) → open ReconcileGrid at the resumed session, Discard (danger) → discardSession (clears only unresolved review state; applied changes persist). Never a timer or polling. Write `src/services/import/reconcile-resume-sweep.test.ts` proving the hook resolves a resumable session, that a thrown read is caught (no crash), and that importing the module triggers no work.
  </action>
  <verify>
    <automated>npx vitest run src/services/import/reconcile-resume-sweep.test.ts && npx tsc --noEmit</automated>
    <human-check>On the Pixel: start a bulk check, resolve some contacts, kill the app; on relaunch the `Resume your check?` prompt appears, Resume restores the remaining unresolved contacts, and the already-applied changes are still applied. (End-of-phase UAT — process-death durability.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/services/import/reconcile-resume-sweep.ts` contains `export function registerReconcileResumeSweep(` and calls `registerSweepHook`.
    - `grep -n "registerReconcileResumeSweep" App.tsx` shows the wiring; `grep -n "setInterval\|setTimeout" src/services/import/reconcile-resume-sweep.ts` returns nothing (no timer).
    - `npx vitest run src/services/import/reconcile-resume-sweep.test.ts` exits 0 including the thrown-read-is-caught case.
  </acceptance_criteria>
  <done>Unresolved reconciliation sessions resume on foreground launch via the sweep hook, with applied changes preserved and no timer.</done>
</task>

<task type="auto">
  <name>Task 2: Missing-source state + Relink / Unlink lifecycle</name>
  <files>src/db/reconcile-relink-dao.ts, src/db/reconcile-relink-dao.test.ts, src/screens/ReconcileDetailScreen.tsx</files>
  <read_first>
    - src/logic/reconcile-diff.ts (the missing-source distinct outcome — from 20-03)
    - src/screens/ReconcileDetailScreen.tsx (from 20-03 — add the missing-source branch)
    - src/db/imported-contact-dao.ts (external_contact_links read/link idiom — provider/external_contact_id/is_active) and modules/orbit-contact-picker/index.ts (the system picker for relink)
    - src/db/migrations/011-contact-lifecycle-schema.ts (117-124, 178-180 external_contact_links + idx_external_contact_links_active partial unique — retiring the stale link means is_active=0 before attaching a new active one)
    - src/db/transaction.ts (inWriteTransaction)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #7 missing source + relink; recommendation chip `Source missing`; actions Keep as is / Relink to another contact / Unlink source)
  </read_first>
  <action>
    Create `src/db/reconcile-relink-dao.ts` with `relinkExternalSource(exec, { contactId, staleLinkId, newProvider, newExternalContactId, now })` — in one inWriteTransaction: retire the stale external_contact_link (set is_active=0, modified_at) BEFORE inserting/activating the new link (to avoid the idx_external_contact_links_active partial-unique ABORT), attach the newly selected source as an active link for the same contact, and retain ALL Orbit-owned data (no field clears, no archive, no unbind). Add `unlinkExternalSource(exec, { staleLinkId, now })` (set is_active=0 only). Both `?`-bound, assert one row changed. In ReconcileDetailScreen add the missing-source branch: when reconcile-diff reports missing-source, render it as a distinct state with a colourless `Source missing` chip and exactly three actions — `Keep as is` (default), `Relink to another contact` (open the system picker → relinkExternalSource → immediately re-run reconcile-diff against the new source), `Unlink source` (unlinkExternalSource). Never delete/archive/unbind/clear the Orbit contact. Write `src/db/reconcile-relink-dao.test.ts` proving the retire-then-attach ordering (no partial-unique ABORT), that Orbit data is untouched, and that unlink only flips is_active.
  </action>
  <verify>
    <automated>npx vitest run src/db/reconcile-relink-dao.test.ts && npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: delete a linked contact from the phone, run Update from Contacts; confirm it shows `Source missing` (not field removals), Relink lets you pick a new system contact and re-runs the review, and the Orbit contact plus its interactions are fully intact throughout. (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/db/reconcile-relink-dao.ts` contains `export function relinkExternalSource(` and retires the stale link before attaching the new one.
    - `ReconcileDetailScreen` renders a distinct missing-source state with Keep as is / Relink / Unlink (no destructive Orbit action).
    - `npx vitest run src/db/reconcile-relink-dao.test.ts` exits 0 including the retire-then-attach ordering and Orbit-data-untouched cases; `npx tsc --noEmit`, `npm run check:colors`, `npm test` pass.
  </acceptance_criteria>
  <done>Missing source is a distinct, non-destructive state; relink retires the stale link, attaches a new source, and re-runs reconciliation without resetting Orbit data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| missing/removed source → Orbit | A source that disappears must never cascade into destroying Orbit data. |
| launch lifecycle → durable session | Resume must fire only on real foreground launch, not on a headless tap or timer. |

> No network on any read path. No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05-1 | Repudiation / Tampering | missing-source / relink | high | mitigate | Missing source is a distinct state; only Keep/Relink/Unlink; relink retires-then-attaches (no partial-unique ABORT) and never clears/archives/unbinds Orbit data. |
| T-20-05-2 | DoS / Tampering | resume durability | medium | mitigate | Launch-sweep hook (never a timer); unreadable session caught → Discard; applied changes persist. |
</threat_model>

<verification>
- `npx vitest run src/services/import/reconcile-resume-sweep.test.ts src/db/reconcile-relink-dao.test.ts` green; `npm test` green after the wave.
- `npx tsc --noEmit`, `npm run check:colors` clean.
- Device-UAT (end-of-phase): process-death resume + missing-source/relink on the Pixel.
</verification>

<success_criteria>
- Unresolved sessions resume on foreground launch; applied changes persist; no timer.
- Missing source is distinct and never destructive; relink preserves all Orbit data and re-runs review.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` (merge-dao.ts); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts).
- **Pure logic:** `classifyReconciliation` (reconcile-diff.ts); `recommendSurvivor` (survivor-recommendation.ts).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`.
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, bulk-review-read.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-05-SUMMARY.md` when done.
</output>
