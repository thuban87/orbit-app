---
phase: 20-contact-reconciliation-merge
plan: 05
type: execute
wave: 5
depends_on: [20-04]
files_modified:
  - src/services/import/reconcile-resume-sweep.ts
  - src/services/import/reconcile-resume-sweep.test.ts
  - src/services/resume-prompt-precedence.ts
  - src/services/resume-prompt-precedence.test.ts
  - src/components/ResumeReconcilePrompt.tsx
  - App.tsx
  - src/screens/ReconcileDetailScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/db/reconcile-relink-dao.ts
  - src/db/reconcile-relink-dao.test.ts
requirements: [RCN-02, RCN-01]
autonomous: true
estimate:
  tokens: 60000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On foreground launch, an unresolved reconciliation session is offered for resume via a NET-NEW `ResumeReconcilePrompt` (heading `Resume your check?`) built on the ResumeImportPrompt visual/interaction idiom but wired to reconcile routes + the reconcile `discardSession`. The shipped `ResumeImportPrompt` is import-coupled (props require `ResumableImport`, routing targets import screens, discard calls the import-session-dao) and CANNOT render a reconcile session, so it is reused as a pattern, not the component (RCN-02 / Cluster R)."
    - "Resume restores the unresolved cards; Discard clears only unresolved review state and already-applied resolutions stay applied; the session is durable across process death, reboot, and long absence (RCN-02 / Cluster R)."
    - "REQUIRE RESUME OR EXPLICIT DISCARD BEFORE A NEW CHECK (cycle-4 finding 3, safe default) — when the user taps `Check linked contacts` (20-04) while an unresolved reconciliation session is pending, the app does NOT silently create a second session and let resume-newest/sweep-older silently discard the older unresolved work. It intercepts with the `ResumeReconcilePrompt` (Resume the pending check, or explicitly Discard it) and only starts a fresh scan after the pending session is resumed or discarded. 20-04 provides the pre-scan detection via `getNewestPendingReconcileSessionId`; 20-05 completes the Resume/Discard interception UX. This is the safe default and reverses no locked decision (the import-style resume-newest/sweep-older read is a reused idiom, not an ADR/HANDOFF ruling). Verified at the seam the toolchain can reach: a node:sqlite test (in 20-04) proves `getNewestPendingReconcileSessionId` returns the pending id the handler blocks on, and the consolidated device-UAT confirms tapping `Check linked contacts` with a pending check surfaces Resume/Discard instead of silently starting a new scan (no RN render harness exists for a component-level assertion)."
    - "When both an import and a reconciliation session are resumable, App shows a deterministic single prompt at a time — import resume takes precedence and is dismissed before the reconcile prompt is offered (App holds one resumable state per kind; precedence is explicit, never two overlapping sheets). The precedence decision is a NAMED PURE helper `resolveActiveResumePrompt(resumableImport, resumableReconcile)` → 'import' | 'reconcile' | null, unit-tested for all four state combinations, so the two-modal precedence is verified by a real test and not only grep + device-UAT (reviewer finding 8)."
    - "A resumed reconciliation card renders and applies from DURABLE staged photo data (the `reconcile-staging/` path + hash 20-03/20-04 persist in diff_json AND the first-class `staged_photo_rel_path` column), never a vanished cache path; and abandoned reconcile-staging files (from a discarded/interrupted session) are cleaned up at launch by the reconcile resume sweep, enumerating live cards' `staged_photo_rel_path` column (not diff_json parsing — finding 9) — mirroring how the import resume sweep reconciles orphaned import-staging photos (reviewer HIGH #2)."
    - "A resumable session whose card payload cannot be parsed still yields a Discard-only descriptor built from `getNewestPendingReconcileSessionId` (a read that never parses diff_json), so a corrupt session is surfaceable as Discard without crashing and is never silently dropped (reviewer finding 7 / E9 backstop)."
    - "A RESUMED card's Apply REVALIDATES FRESHNESS before writing (reviewer HIGH #1) — the long-absence resume window is exactly when a card's persisted diff is most likely stale, so applying a resumed card goes through the SAME shared `applyReconcileSelections` helper (20-03), which re-reads the live Orbit value per accepted field against the card's persisted scan-time baseline + `modified_at` and SKIPS/re-surfaces any field the user edited in Orbit since the scan — a resumed apply never silently overwrites a post-scan Orbit edit. The resume path adds NO new writer and cannot bypass the freshness check."
    - "A missing/unresolvable linked source is a distinct card/detail state (`Source missing`) offering exactly Keep as is / Relink to another contact / Unlink source — never delete, archive, unbind, or clear the Orbit contact (RCN-01 / Cluster G)."
    - "Relink retains all Orbit-owned data, retires the stale external link, attaches the newly selected source, and immediately re-runs reconciliation against the new source; no Orbit relationship data resets on relink (RCN-01 / Cluster L)."
    - "Relink PREFLIGHTS the newly selected (provider, external_contact_id) against the GLOBAL active-link unique index (idx_external_contact_links_active is global, not per-contact); if that identity is already an active link on ANOTHER Orbit contact, relink does NOT ABORT/clobber — it surfaces a non-destructive outcome (reject + route to merge/reconcile, or require an explicit unlink from the other contact) rather than blindly inserting."
    - statement: "An unreadable/corrupt resumable session offers Discard without crashing (E9 error backstop)."
      verification: backstop
  prohibitions:
    - "resume/relink must NEVER destroy, archive, unbind, or clear an Orbit contact when its source is missing"
    - "must NEVER implement resume with a background timer or passive polling — launch-sweep only"
  artifacts:
    - src/services/import/reconcile-resume-sweep.ts
    - src/db/reconcile-relink-dao.ts
  key_links:
    - "reconcile-resume-sweep registers via registerSweepHook and is wired in App.tsx after migration readiness"
    - "ReconcileDetailScreen missing-source branch → Keep / Relink (system picker) / Unlink"
    - "relink retires the stale external_contact_link and attaches the new one, then re-runs reconcile-diff"
---

<objective>
**As a** user who left a big check half-done or whose linked contact vanished from my phone, **I want to** resume exactly where I left off and safely handle a missing source, **so that** no unresolved work is lost and a deleted phone contact never damages my Orbit data.

This completes reconciliation durability: the launch-time resume prompt (reusing the shipped `ResumeImportPrompt` + the sweep-hook idiom) and the missing-source/relink lifecycle (a distinct state, never destructive).

Purpose: honor the "durable across process death" and "missing source never destroys Orbit" invariants using the shipped launch-sweep mechanism — never a timer.
Output: `reconcile-resume-sweep` service + App.tsx wiring, and the missing-source/relink branch + `reconcile-relink-dao`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile-resume-sweep launch hook + ResumeReconcilePrompt + App wiring</name>
  <files>src/services/import/reconcile-resume-sweep.ts, src/services/import/reconcile-resume-sweep.test.ts, src/components/ResumeReconcilePrompt.tsx, App.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/services/import/contact-import-resume-sweep.ts (11 registerSweepHook import; 116-147 registerImportResumeSweep — the exact registration shape to mirror: importing the module does no work; resume newest; cleanup staged; onResumable callback; note the sweep catches read errors and passes a `discardOnly` descriptor for a corrupt session; 48-56 pruneStagedPhotos + 89-142 reconcileOrphanStagedPhotos — the launch-time staging orphan cleanup to MIRROR for reconcile-staging)
    - src/services/launch-sweep.ts (registerSweepHook — runs once per real foreground launch, never on a headless tap or module import)
    - src/db/reconcile-session-read.ts (getResumableReconcileSession AND `getNewestPendingReconcileSessionId` — from 20-04; the latter is the corruption-tolerant id read for the discard-only descriptor when card-payload parsing fails)
    - src/services/photos/photo-storage.ts (20-03 — `listReconcileStagingPhotos`/`deleteReconcileStaging`: enumerate + best-effort-delete abandoned reconcile-staging files at launch, mirroring listImportStagingPhotos/deleteImportStaging)
    - App.tsx (28 registerImportResumeSweep import; 123-124 the single `resumableImport` state; 206 registerImportResumeSweep(setResumableImport, { getExecutor }) — add a SEPARATE `resumableReconcile` state + setter and register the reconcile sweep the same way, after migration readiness)
    - src/components/ResumeImportPrompt.tsx (WHOLE FILE — the non-tap-dismissable Modal sheet idiom, accent Resume / danger Discard, `discardOnly` branch at 107-125. It is IMPORT-COUPLED: imports `discardSession` from `@/db/import-session-dao` (4), `resumeImport` navigates only to import routes (21-51), props require `ResumableImport` (16). Reuse the VISUAL/INTERACTION shape; do NOT reuse the component for reconcile.)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #9 Resume/Discard; copy: `Resume your check?` / `Unresolved contacts are saved. Changes you already applied stay applied.` / Resume / Discard)
  </read_first>
  <action>
    Create `src/services/import/reconcile-resume-sweep.ts` exporting `registerReconcileResumeSweep(onResumable, { getExecutor, now } = {})`, mirroring registerImportResumeSweep: inside `registerSweepHook`, call `getResumableReconcileSession(exec, now())`; if a resumable session exists, describe it and pass the description to `onResumable`; on a read/parse error, log, catch, and build a `discardOnly` reconcile descriptor from `getNewestPendingReconcileSessionId(exec)` (the corruption-tolerant id read that never parses diff_json — finding 7) so an unreadable/corrupt session is surfaceable as Discard-only, never silently dropped and never a crash. ALSO mirror the import sweep's launch-time staging reconcile: after resolving/discarding, best-effort enumerate `listReconcileStagingPhotos()` and `deleteReconcileStaging` any abandoned reconcile-staging file not referenced by any live pending card's first-class `staged_photo_rel_path` column (a direct column read, NOT diff_json parsing — mirroring how the import sweep queries import rows' dedicated staging column, per finding 9), so a discarded/interrupted session's staged photos do not accumulate even when a card payload is unparseable (HIGH #2 cleanup). Importing the module must do no work. Create `src/components/ResumeReconcilePrompt.tsx` — a reconcile-specific sheet built on the ResumeImportPrompt idiom (same non-tap-dismissable Modal, accent Resume / danger Discard, `discardOnly` branch) but wired to reconcile: Resume → navigate to `ReconcileGrid` at the resumed session, Discard → the reconcile `discardSession` (from reconcile-session-dao, NOT the import one); heading `Resume your check?`, body `Unresolved contacts are saved. Changes you already applied stay applied.`. Create the NAMED PURE precedence helper `src/services/resume-prompt-precedence.ts` exporting `resolveActiveResumePrompt(resumableImport, resumableReconcile)` → `'import' | 'reconcile' | null` (import wins when both pending; reconcile only when import is absent; null when neither), and `src/services/resume-prompt-precedence.test.ts` covering all four combinations. Wire in App.tsx: add a separate `resumableReconcile` state + setter, register the reconcile sweep after migration readiness, and render whichever prompt `resolveActiveResumePrompt` selects (so the two sheets can never overlap — import first, reconcile only after the import prompt is dismissed/absent). Never a timer or polling. ALSO complete the finding-3 new-check interception: the `Check linked contacts` handler (20-04 detects a pending session via `getNewestPendingReconcileSessionId`) surfaces the same `ResumeReconcilePrompt` (Resume the pending check, or explicitly Discard it via the reconcile `discardSession`) INSTEAD of starting a second scan — a fresh scan proceeds only after the pending session is resumed or explicitly discarded, so pending unresolved work is never silently abandoned. Write `src/services/import/reconcile-resume-sweep.test.ts` proving the hook resolves a resumable session, that a thrown read is caught and yields a `discardOnly` descriptor built from `getNewestPendingReconcileSessionId` (no crash), that abandoned reconcile-staging files are swept, and that importing the module triggers no work.
  </action>
  <verify>
    <automated>npx vitest run src/services/import/reconcile-resume-sweep.test.ts src/services/resume-prompt-precedence.test.ts && npx tsc --noEmit</automated>
    <human-check>On the Pixel: start a bulk check, resolve some contacts, kill the app; on relaunch the `Resume your check?` prompt appears, Resume restores the remaining unresolved contacts, and the already-applied changes are still applied. (End-of-phase UAT — process-death durability.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/services/import/reconcile-resume-sweep.ts` contains `export function registerReconcileResumeSweep(` and calls `registerSweepHook`; the corrupt-session path builds its descriptor from `getNewestPendingReconcileSessionId` (`grep -n "getNewestPendingReconcileSessionId" src/services/import/reconcile-resume-sweep.ts` shows it) and it sweeps abandoned reconcile-staging (`grep -n "listReconcileStagingPhotos\|deleteReconcileStaging" src/services/import/reconcile-resume-sweep.ts` shows the cleanup).
    - `src/services/resume-prompt-precedence.ts` contains `export function resolveActiveResumePrompt(`; `npx vitest run src/services/resume-prompt-precedence.test.ts` exits 0 with all four import/reconcile-pending combinations.
    - `src/components/ResumeReconcilePrompt.tsx` exists, navigates to `ReconcileGrid` on Resume, and imports the reconcile `discardSession` (from reconcile-session-dao), NOT `@/db/import-session-dao`: `grep -n "import-session-dao" src/components/ResumeReconcilePrompt.tsx` returns nothing.
    - `grep -n "resolveActiveResumePrompt\|resumableReconcile\|ResumeReconcilePrompt" App.tsx` shows the wiring, the separate reconcile state, and that the rendered prompt is chosen by `resolveActiveResumePrompt` (precedence: reconcile prompt not shown while an import resume is pending); `grep -n "setInterval\|setTimeout" src/services/import/reconcile-resume-sweep.ts` returns nothing (no timer).
    - Finding 3 — a new `Check linked contacts` with a pending session surfaces `ResumeReconcilePrompt` (Resume or explicit Discard) rather than silently starting a second scan: `grep -n "ResumeReconcilePrompt\|getNewestPendingReconcileSessionId" src/screens/SettingsScreen.tsx App.tsx` shows the pending-session interception on the new-check path (wired here in 20-05 on top of 20-04's detection).
    - `npx vitest run src/services/import/reconcile-resume-sweep.test.ts` exits 0 including the thrown-read-is-caught → `discardOnly` (via getNewestPendingReconcileSessionId) case and the staging-sweep case.
  </acceptance_criteria>
  <done>Unresolved reconciliation sessions resume on foreground launch via the sweep hook, with applied changes preserved and no timer.</done>
</task>

<task type="auto">
  <name>Task 2: Missing-source state + Relink / Unlink lifecycle</name>
  <files>src/db/reconcile-relink-dao.ts, src/db/reconcile-relink-dao.test.ts, src/screens/ReconcileDetailScreen.tsx</files>
  <read_first>
    - src/logic/reconcile-diff.ts (the missing-source distinct outcome — from 20-03)
    - src/screens/ReconcileDetailScreen.tsx (from 20-03 — add the missing-source branch)
    - src/db/imported-contact-dao.ts (external_contact_links read/link idiom — provider/external_contact_id/is_active) and modules/orbit-contact-picker/index.ts (the system picker for relink)
    - src/db/migrations/011-contact-lifecycle-schema.ts (117-124, 178-180 external_contact_links + idx_external_contact_links_active partial unique — retiring the stale link means is_active=0 before attaching a new active one)
    - src/db/transaction.ts (inWriteTransaction)
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #7 missing source + relink; recommendation chip `Source missing`; actions Keep as is / Relink to another contact / Unlink source)
  </read_first>
  <action>
    Create `src/db/reconcile-relink-dao.ts` with `relinkExternalSource(exec, { contactId, staleLinkId, newProvider, newExternalContactId, now })` — in one inWriteTransaction: FIRST PREFLIGHT the new identity — query for an EXISTING `is_active=1` external_contact_link with the same `(newProvider, newExternalContactId)` (the idx_external_contact_links_active unique index is GLOBAL, not per-contact); if one exists on a DIFFERENT contact, do NOT insert — return a typed `duplicate-active-link` outcome naming the other contact so the caller can route to merge/reconcile or require an explicit unlink (blindly inserting would ABORT the whole transaction). Otherwise retire the stale external_contact_link (set is_active=0, modified_at) BEFORE inserting/activating the new link (to avoid the same partial-unique ABORT on a same-identity swap), attach the newly selected source as an active link for the same contact, and retain ALL Orbit-owned data (no field clears, no archive, no unbind). Add `unlinkExternalSource(exec, { staleLinkId, now })` (set is_active=0 only). Both `?`-bound, assert one row changed. In ReconcileDetailScreen add the missing-source branch: when reconcile-diff reports missing-source, render it as a distinct state with a colourless `Source missing` chip and exactly three actions — `Keep as is` (default), `Relink to another contact` (open the system picker → relinkExternalSource → on `duplicate-active-link` show a non-destructive explanation routing to merge/reconcile, else immediately re-run reconcile-diff against the new source), `Unlink source` (unlinkExternalSource). Never delete/archive/unbind/clear the Orbit contact. Write `src/db/reconcile-relink-dao.test.ts` proving the retire-then-attach ordering (no partial-unique ABORT), the duplicate-active-link preflight returns the non-destructive outcome (no ABORT, no clobber) when the identity is already active elsewhere, that Orbit data is untouched, and that unlink only flips is_active.
  </action>
  <verify>
    <automated>npx vitest run src/db/reconcile-relink-dao.test.ts && npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel: delete a linked contact from the phone, run Update from Contacts; confirm it shows `Source missing` (not field removals), Relink lets you pick a new system contact and re-runs the review, and the Orbit contact plus its interactions are fully intact throughout. (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/db/reconcile-relink-dao.ts` contains `export function relinkExternalSource(` and retires the stale link before attaching the new one, and preflights the global active `(provider, external_contact_id)` identity returning a `duplicate-active-link` outcome instead of inserting when it belongs to another contact.
    - `ReconcileDetailScreen` renders a distinct missing-source state with Keep as is / Relink / Unlink (no destructive Orbit action) and handles the `duplicate-active-link` relink outcome non-destructively.
    - `npx vitest run src/db/reconcile-relink-dao.test.ts` exits 0 including the retire-then-attach ordering, the duplicate-active-link preflight (no ABORT/clobber), and Orbit-data-untouched cases; `npx tsc --noEmit`, `npm run check:colors`, `npm test` pass.
  </acceptance_criteria>
  <done>Missing source is a distinct, non-destructive state; relink retires the stale link, attaches a new source, and re-runs reconciliation without resetting Orbit data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| missing/removed source → Orbit | A source that disappears must never cascade into destroying Orbit data. |
| launch lifecycle → durable session | Resume must fire only on real foreground launch, not on a headless tap or timer. |

> No network on any read path. No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05-1 | Repudiation / Tampering | missing-source / relink | high | mitigate | Missing source is a distinct state; only Keep/Relink/Unlink; relink retires-then-attaches (no partial-unique ABORT), preflights the global active identity and returns a non-destructive `duplicate-active-link` outcome instead of clobbering/aborting when it belongs elsewhere, and never clears/archives/unbinds Orbit data. |
| T-20-05-2 | DoS / Tampering | resume durability | medium | mitigate | Launch-sweep hook (never a timer); unreadable/corrupt session caught → Discard-only descriptor built from `getNewestPendingReconcileSessionId` (no diff_json parse); applied changes persist; abandoned reconcile-staging photos swept at launch; import vs reconcile prompts show one-at-a-time by the unit-tested pure `resolveActiveResumePrompt` precedence; a new `Check linked contacts` while a session is pending requires Resume/Discard, never silently abandoning pending work (finding 3). |
| T-20-05-3 | Tampering / Data loss | resumed stale card overwrites a post-scan Orbit edit | high | mitigate | A resumed card applies through the shared `applyReconcileSelections` helper, which revalidates each field against the card's persisted scan-time Orbit baseline + `modified_at` and skips/re-surfaces any field Orbit changed since scan — the long-absence resume window cannot silently clobber a post-scan Orbit edit (reviewer HIGH #1). |
</threat_model>

<verification>
- `npx vitest run src/services/import/reconcile-resume-sweep.test.ts src/db/reconcile-relink-dao.test.ts` green; `npm test` green after the wave.
- `npx tsc --noEmit`, `npm run check:colors` clean.
- Device-UAT (end-of-phase): process-death resume + missing-source/relink on the Pixel.
</verification>

<success_criteria>
- Unresolved sessions resume on foreground launch; applied changes persist; no timer.
- Missing source is distinct and never destructive; relink preserves all Orbit data and re-runs review.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`, `bulk_review_resolutions`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` (merge-dao.ts); `setContactPhotoCore` (contacts-dao.ts, 20-01); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` (bulk-review-dao.ts).
- **Pure logic:** `classifyReconciliation` (reconcile-diff.ts); `recommendSurvivor` (survivor-recommendation.ts); `resolveActiveResumePrompt` (src/services/resume-prompt-precedence.ts — the unit-tested import-vs-reconcile precedence helper, 20-05).
- **Reads:** `getNewestPendingReconcileSessionId` (reconcile-session-read.ts — corruption-tolerant discard-only id read, consumed by the reconcile sweep).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts — also reconciles abandoned reconcile-staging photos at launch).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`, `ResumeReconcilePrompt` (net-new, src/components/ResumeReconcilePrompt.tsx — reconcile resume sheet on the ResumeImportPrompt idiom); shipped `CandidateCardGrid` EXTENDED additively (20-04).
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-bulk-eligibility.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, resume-prompt-precedence.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-05-SUMMARY.md` when done.
</output>
