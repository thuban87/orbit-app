---
phase: 20-contact-reconciliation-merge
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - src/logic/reconcile-diff.ts
  - src/logic/reconcile-diff.test.ts
  - src/db/reconcile-snapshot-dao.ts
  - src/db/reconcile-snapshot-dao.test.ts
  - src/db/reconcile-apply.ts
  - src/db/reconcile-apply.test.ts
  - src/db/photo-relative-path.ts
  - src/services/photos/photo-storage.ts
  - src/services/photos/reconcile-photo.ts
  - src/services/photos/reconcile-photo.test.ts
  - src/screens/ReconcileDetailScreen.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
requirements: [RCN-01]
autonomous: true
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per-contact `Update from Contacts` (profile overflow, shown only when the contact has ≥1 active external_contact_link) re-reads live source state and compares only name, phones, emails, birthday, and photo (RCN-01 / Cluster A)."
    - "reconcile-diff classifies each field family as additive, conflict, removed-from-source, missing-source, or unchanged-since-review; additive recommends the source value, conflict requires explicit manual choice, removed-from-source recommends keeping Orbit and never auto-deletes (RCN-01 / Cluster C)."
    - "Phone/email equality uses canonical normalized identity, not raw bytes or display formatting — (312) 555-1234 equals 312-555-1234 — while a label change (Mobile→Personal) IS a meaningful change (RCN-01 encoding + adjacency edges / Cluster P)."
    - "A missing/unresolvable source is a DISTINCT card state, never processed as five field-removals; it never deletes, archives, unbinds, or clears the Orbit contact (RCN-01 empty edge / Cluster G, Pitfall 3)."
    - "Multiple active source links for one Orbit person combine into ONE reconciliation card/detail, with per-value source provenance; conflicting source records never gain automatic authority (RCN-01/02 / Cluster M, N)."
    - "ONE-PHOTO-PER-CARD COLLAPSE (reviewer MEDIUM finding, was HIGH) — a card/detail has exactly ONE staged source photo, held in the single nullable `staged_photo_rel_path` column (migration 013) and rendered by the single side-by-side `PhotoChoice`. When several active source links each carry a `photoTempUri`, the card stages ONLY the photo of the HIGHEST-PRIORITY active source link — deterministically the FIRST active `external_contact_link` by ascending link id (creation order) that has a photo — and does NOT stage the other sources' photos. This one-slot collapse is what makes the single `staged_photo_rel_path` column and the 20-05 launch sweep (which deletes any reconcile-staging file not referenced by a live card's `staged_photo_rel_path`) correct: there is never a second live staged photo for the card to lose or leak. Photo remains a manual side-by-side choice (Orbit vs the one collapsed source photo); the other four families still aggregate per-source provenance normally. (RCN-01 photo durability / Cluster Q)."
    - "Re-running reconcile on unchanged source suppresses an already-reviewed discrepancy (narrow last-reviewed memory); a source value that changed again re-surfaces (RCN-01 idempotency edge / Cluster D)."
    - "Applying reconciliation selections writes ONLY the user-selected values, through the existing authoritative writers in one inWriteTransaction: fixed scalars via updateContactMetadataCore, and methods via applyContactMethodDiffCore — and the NON-PHOTO families' last-reviewed snapshots are written in the same txn. A chosen photo is applied POST-COMMIT per the all-or-nothing photo rule below, NOT in this txn; the PHOTO family's reviewed snapshot is likewise NOT written in this txn — it is DEFERRED and written only AFTER the post-commit promoteReconcilePhoto→setContactPhoto SUCCEEDS, so a failed photo apply never leaves a committed photo reviewed-hash that would suppress the un-applied chosen photo on the next scan via PHOTO NO-RE-NAG (reviewer HIGH — photo snapshot ordering)."
    - "FRESHNESS REVALIDATION — reconciliation never silently overwrites a POST-SCAN Orbit edit (reviewer HIGH #1). `updateContactMetadataCore` is a whole-row `UPDATE contacts … WHERE id=?` with NO optimistic-concurrency predicate (VERIFIED contacts-dao.ts:308-335), and a card's diff is persisted at SCAN and can be Applied much later (20-04 durable card / 20-05 resume after process death), so a blind apply would clobber any Orbit change the user made between scan and apply — violating the phase's own 'reconciliation must NEVER silently overwrite Orbit data' prohibition. So AT SCAN the diff captures, per surfaced field family, the CURRENT Orbit-side baseline value (the exact value the source was compared against — already computed by the classifier) PLUS the contact's `modified_at` as a coarse card-level staleness gate; both are carried in the persisted card. AT APPLY, inside the shared `applyReconcileSelections` helper and BEFORE writing each accepted source value, the live Orbit value for that field is RE-READ and compared to the scan-time baseline: if the Orbit side changed since scan, that field is NOT overwritten — it is SKIPPED and re-surfaced as a fresh conflict (the card is marked stale/partial with that field back to unresolved, `data_revision` is not bumped for a skipped field), so the user re-chooses against current state. Data_revision is bumped only for fields actually written. This lives in the SHARED helper so BOTH the per-contact detail path (20-03) and the durable/bulk path (20-04) inherit it. Proven by a test: a card scanned, then the Orbit value edited, then a resumed apply — the post-scan Orbit edit is PRESERVED (not overwritten) and the field re-surfaces for re-review (HIGH #1)."
    - "BACKUP VISIBILITY — when the reconcile apply writes a durable SCALAR change via `updateContactMetadataCore` (name/birthday/category), the apply txn calls `bumpDataRevisionCore(exec)` once, because `updateContactMetadataCore` does NOT self-bump (bare `UPDATE contacts` — VERIFIED contacts-dao.ts:308-335). Without it, a NAME/BIRTHDAY-only reconcile Apply changes durable relationship data but leaves `data_revision` unchanged, so the launch backup sweep (which gates on `data_revision` via `readDataRevision`→`shouldRunAutomaticBackup`, VERIFIED backup-sweep.ts:29-31) would SILENTLY OMIT the reconciled value from the next automatic backup. The method writer (`applyContactMethodDiffCore` bumps `if (changed)`) and the post-commit photo writer (`setContactPhoto` bumps) already cover their own paths, so this bump specifically closes the scalar gap; a reviewed-snapshot-only 'Keep Orbit values' with no scalar change need not bump. Proven by a test asserting `data_revision` advances after a scalar-only reconcile apply (reviewer HIGH #1)."
    - "The method apply SEEDS every existing Orbit method and builds a COMPLETE desired list (all current methods + the accepted source additions). applyContactMethodDiffCore is a whole-list diff that tombstones+DELETEs any seeded method absent from the desired list, so accepting one addition must NEVER drop an unrelated Orbit method — the desired list is current∪accepted, never just the additions (RCN-01 data-loss edge)."
    - "PHOTO DURABILITY — a picked source photo arrives ONLY as `PickedContact.photoTempUri`, an intentionally-evictable app-private cache `file://` path (VERIFIED modules/orbit-contact-picker/index.ts:19-24) that `setContactPhotoCore`/`assertSafeRelative` REJECT (they accept only a durable `avatars/<name>.<ext>` relative path — VERIFIED contacts-dao.ts:610-645, photo-relative-path.ts:22/47). So a net-new RECONCILE PHOTO-STAGING pipeline (mirroring the shipped import path stageImportPhoto→persistMaster) runs at SCAN time: `stageReconcileSourcePhoto` copies the temp URI bytes into a durable `reconcile-staging/` document-dir path AND computes a content hash. The classifier and PhotoChoice consume the DURABLE staged path + hash, NEVER the evictable temp URI (RCN-01 photo durability — reviewer HIGH #2)."
    - "PHOTO APPLY IS ALL-OR-NOTHING ACROSS DB AND THE CANONICAL FILE — the apply is restructured to mirror the SHIPPED import path EXACTLY (which persists POST-COMMIT, not pre-txn — VERIFIED persistImportedPhotoPostCommit in import-photo.ts:96-135 runs persistMaster→setContactPhoto AFTER the record txn resolved). The main apply `inWriteTransaction` commits ONLY the non-photo durable changes (scalars, methods, field_history snapshots, the NON-PHOTO families' reviewed snapshots, the data_revision bump — the PHOTO family's reviewed snapshot is NOT written here; it is deferred to the post-commit photo-success step, see PHOTO NO-RE-NAG below). A chosen source photo is applied POST-COMMIT via a reconcile analog of `persistImportedPhotoPostCommit`: `promoteReconcilePhoto` resizes the durable staged file and `persistMaster`s it to `avatars/contact-<id>.jpg`, THEN `setContactPhoto` (self-wrapped, which bumps data_revision) writes that durable relative path — so `setContactPhotoCore`/`assertSafeRelative` never receive a temp/cache URI. Because `persistMaster` OVERWRITES the canonical master in place and deletes its `.bak` (VERIFIED photo-storage.ts:325-378), doing it pre-txn would let a rolled-back apply leave the NEW bytes with the prior photo gone; running it POST-COMMIT means a rollback of the apply txn leaves BOTH the on-disk master AND the DB photo reference untouched, and a post-commit photo failure degrades to 'photo unchanged' (like import's photo_failed), never corruption. ONLY after the post-commit `setContactPhoto` SUCCEEDS is the PHOTO family's reviewed snapshot (source content hash, once per contributing external_contact_link_id) upserted, in its own small txn; if the post-commit photo apply FAILS, NO photo reviewed snapshot is written and the photo card/field is left UNRESOLVED so the next scan re-surfaces the un-applied chosen photo instead of suppressing it — never a committed photo reviewed-hash ahead of a failed apply (reviewer HIGH — photo snapshot ordering). A post-commit best-effort cleanup deletes the promoted staged file (reviewer HIGH #2)."
    - "PHOTO NO-RE-NAG — the reconcile_source_snapshot reviewed_value for the photo family is the source photo CONTENT HASH (computed from the staged bytes), so re-running reconcile on an unchanged source photo suppresses the already-reviewed photo discrepancy and a genuinely changed photo re-surfaces — the narrow last-reviewed memory covers photo like the other four families (RCN-01 idempotency / Pitfall 5; content-hash-of-staged-bytes is the net-new capability flagged in RESEARCH A3). This photo reviewed_value is written ONLY after the post-commit promoteReconcilePhoto→setContactPhoto succeeds (NOT inside the apply txn); a FAILED post-commit photo apply writes NO photo snapshot, so an un-applied chosen photo re-surfaces on the next scan instead of being permanently suppressed by its own reviewed-hash (reviewer HIGH — photo snapshot ordering)."
    - "A `Merge with another contact` affordance is reachable from ReconcileDetailScreen (routing into SurvivorSelect with this contact pre-filled), satisfying the reconciliation-detail merge entry of LOCKED UI-SPEC #10 / Cluster U — not only the profile overflow (reviewer finding 3)."
    - "When an Apply selection overwrites an existing Orbit scalar (name/birthday), the prior value is snapshotted to field_history inside the same txn — the same destructive-overwrite audit rule the merge writer honours (CLAUDE.md)."
    - "For a value reviewed from multiple contributing source links, the last-reviewed snapshot is upserted for EVERY contributing external_contact_link_id, not just the selected source's — otherwise the other link's disagreement re-surfaces unchanged on the next run (RCN-01/02 aggregate-memory edge)."
    - "Multi-source options and the per-field change list render in a deterministic, stable order (RCN-01 ordering edge)."
    - statement: "ReconcileDetailScreen shows progress/skeleton, not a frozen blank, while a contact's field diff and resolved photo URIs load (E4 loading backstop)."
      verification: backstop
    - statement: "ACCEPTED v1 LIMITATION (reviewer LOW finding 5) — reconcile apply adds accepted source methods via `applyContactMethodDiffCore`, which does NOT write `contact_method_provenance` (only the import path writes provenance via `insertMethodProvenanceCore`). So a method ADDED through reconciliation carries weaker source attribution than the same method added through import in v1. Recorded, not fixed: a provenance-insert on the reconcile path is deferred as low-value (the method value/canonical form and the reviewed-snapshot memory are unaffected; only the provenance audit link is thinner). Do NOT build a reconcile-path provenance insert this phase."
      verification: backstop
    - statement: "PRECISION NOTE on the post-commit photo 'unchanged on failure' guarantee (reviewer advisory-LOW, adjudicated not-counted) — for an EXISTING contact, `persistMaster` overwrites `avatars/contact-<id>.jpg` IN PLACE (old master moved to `.bak`, then `.bak` deleted — VERIFIED photo-storage.ts:325-378), so if `setContactPhoto` fails AFTER `persistMaster` post-commit, the canonical file already holds the NEW bytes while the DB reference is unchanged — i.e. the on-disk photo IS the new one even though the failure path is described as 'photo unchanged'. This window is a property of the SHIPPED `persistMaster`+`setContactPhoto` sequence used by every existing-photo replacement (import escapes it only because its target path is fresh), not a Phase-20 invention. The all-or-nothing guarantee this plan makes is precise for the APPLY TRANSACTION (a rolled-back txn leaves DB + master untouched because persistMaster is post-commit); the narrow post-commit persistMaster→setContactPhoto failure window inherits the shipped behavior and is accepted for v1, not re-architected (a unique-pending-filename swap is the future fix if ever warranted)."
      verification: backstop
  prohibitions:
    - "reconciliation must NEVER silently overwrite or delete Orbit data on source conflict, removal, or missing source"
    - "must NEVER write back to system Contacts — reconciliation is strictly one-way (System Contacts → Orbit)"
    - "must NEVER treat a missing/unresolvable source as field-removals"
    - "must NEVER pass a partial method list to applyContactMethodDiffCore — a desired list missing any current Orbit method tombstones+DELETEs it (data loss)"
    - "must NEVER route a photo through updateContactMetadataCore (it omits photo) — a photo choice applies via the post-commit promoteReconcilePhoto→setContactPhoto step"
    - "must NEVER `persistMaster`/`promoteReconcilePhoto` a chosen photo BEFORE or INSIDE the apply txn — persistMaster overwrites the canonical master in place and deletes its `.bak`, so a pre-txn persist + a rolled-back apply visibly loses the prior photo; the photo persist runs POST-COMMIT only (HIGH #2)"
    - "must NEVER commit a durable reconcile change without bumping data_revision in the same txn — updateContactMetadataCore does not bump, so a scalar-only apply would silently escape the automatic-backup sweep (HIGH #1 backup-visibility)"
    - "must NEVER apply a persisted reconcile selection over an Orbit field that CHANGED since scan — `applyReconcileSelections` re-reads the live Orbit value and compares it to the scan-time baseline before each write; a mismatch means the user edited Orbit after scan, so the field is SKIPPED and re-surfaced for fresh review, NEVER blindly overwritten (HIGH #1 freshness — the whole-row updateContactMetadataCore has no concurrency predicate, VERIFIED contacts-dao.ts:308-335)"
    - "must NEVER persist, compare against, or hand to PhotoChoice/setContactPhotoCore the evictable `photoTempUri` cache path — a source photo must first be staged to durable `reconcile-staging/` and applied only via the promoted `avatars/<name>.<ext>` master (a temp URI in a snapshot re-nags on cache eviction or points at a vanished file, and setContactPhotoCore would throw assertSafeRelative)"
    - "must NEVER write the PHOTO family's reviewed snapshot inside the apply txn (or otherwise before the post-commit photo apply succeeds) — a committed photo reviewed-hash ahead of a best-effort post-commit promoteReconcilePhoto→setContactPhoto that then FAILS makes the next scan see source-hash==reviewed-hash and SUPPRESS the un-applied chosen photo forever (never applied, never re-offered); the photo reviewed snapshot is written only AFTER setContactPhoto succeeds, else the photo card/field stays UNRESOLVED and re-surfaces (reviewer HIGH — photo snapshot ordering)"
  artifacts:
    - src/logic/reconcile-diff.ts
    - src/db/reconcile-snapshot-dao.ts
    - src/services/photos/reconcile-photo.ts
    - src/screens/ReconcileDetailScreen.tsx
  key_links:
    - "profile 'Update from Contacts' → readContactsByLookupKeys → reconcile-diff → ReconcileDetailScreen (FieldChoiceGroup/PhotoChoice) → apply via existing writers + snapshot"
    - "reconcile-diff canonical comparison reuses normalizeContactMethod/canonical_value from contact-methods-dao"
    - "reconcile-snapshot-dao upserts one row per (external_contact_link_id, field_family)"
---

<objective>
**As a** user whose linked system contact changed, **I want to** update one Orbit contact from Contacts and choose exactly what to accept, **so that** additions I want land while nothing I care about is silently overwritten or deleted.

This is the thin end-to-end reconciliation slice for ONE contact: profile `Update from Contacts` → re-read live source → classify the five field families → resolve in a detail view (reusing the 20-02 FieldChoiceGroup/PhotoChoice) → apply only the selected values through the existing authoritative writers, plus the narrow last-reviewed memory that stops re-nagging. Bulk `Check linked contacts`, durable sessions, and the card grid are 20-04; missing-source relink is 20-05.

Purpose: prove the one-way source→Orbit reconciliation path never destroys Orbit data and reuses the shipped writers, not a new write path.
Output: `reconcile-diff` (pure), `reconcile-snapshot-dao`, `ReconcileDetailScreen`, and the per-contact entry point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md
@.planning/phases/20-contact-reconciliation-merge/20-PATTERNS.md
@.planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md
@docs/dossier/20-contact-reconciliation-merge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile-diff pure classifier (five field families, five outcomes)</name>
  <files>src/logic/reconcile-diff.ts, src/logic/reconcile-diff.test.ts</files>
  <read_first>
    - src/services/import/duplicate-evidence.ts (5-32 — the pure classify idiom: exported union types, tunable constants at file top, no I/O)
    - modules/orbit-contact-picker/index.ts (14-25 PickedContact source shape; 69-123 readAllContacts + omittedCount — an absent/omitted source is a MISSING source, not field removals)
    - src/db/contact-methods-dao.ts (5-8 normalizeContactMethod / canonical_value — canonical equality for phones/emails; label is part of the compared value)
    - src/db/migrations/008-restore-photo-journal.ts is NOT relevant — instead read src/logic/picked-contact-map.ts (67 — birthday normalize/unreadable idiom) for birthday comparison shape
    - .planning/phases/20-contact-reconciliation-merge/20-RESEARCH.md (Pattern 3 narrow last-reviewed memory; Pitfall 3 missing source; Pitfall 5 photo has no stable fingerprint — manual choice is primary)
    - docs/dossier/20-contact-reconciliation-merge.md (Clusters C, D, M, N, O, P, Q)
  </read_first>
  <action>
    Create `src/logic/reconcile-diff.ts` — a pure, node-testable classifier. Exported union type of per-field outcome `additive | conflict | removed-from-source | missing-source | unchanged-since-review` over field families `name | phones | emails | birthday | photo`. `classifyReconciliation({ orbit, sources, lastReviewed })` where `sources` is one or more PickedContact records (multiple active links → ONE result set with per-value source provenance; conflicting source records surface as separate options, never auto-chosen). Rules: additive = source has a value Orbit lacks; conflict = both present and disagree; removed-from-source = Orbit retains a value no longer in the linked source; missing-source is detected FIRST as a distinct whole-card state (source absent / omittedCount) and short-circuits field diffing — never five removals. Phone/email comparison uses canonical normalized identity (reuse normalizeContactMethod semantics) so formatting/order differences are not changes, but a LABEL change is. Photo comparison uses a CONTENT-HASH signal: the classifier stays PURE and receives the source photo's content hash + durable staged path from its caller (the screen stages the evictable temp URI to durable `reconcile-staging/` and hashes the bytes BEFORE calling classify — the classifier never touches a temp URI or the FS); emit a photo entry when the source hash differs from the stored lastReviewed hash (or Orbit has no photo), resolved to a manual side-by-side choice against the staged image (never auto-apply). Narrow memory: suppress a field whose current source value equals the stored lastReviewed value; re-surface if the source changed again. Define and export a pure `serializeMethodFamily(methods)` here (reconcile-diff owns it — it is pure logic and 20-03 Task 2's snapshot-dao imports it) that encodes a MULTI-METHOD family (phones/emails) as a single stable string: the sorted list of `(method_type, canonical_value, label)` tuples (sort key `method_type` then `canonical_value` then `label`; canonical via `normalizeContactMethod`; label included), joined with a fixed delimiter — so the compared value for phones/emails is this SET serialization, not a single method. Reordering the source methods does not re-nag (byte-equal) but an added/removed/relabelled method does (byte-different). For photo the compared value is the content hash; for name/birthday the raw comparable value. Output order is deterministic. Tunable constants at the file top. Write `src/logic/reconcile-diff.test.ts` covering all five outcomes, canonical phone equality, label-change-is-change, missing-source distinctness, multiple-source-links→one-card, unchanged-since-review suppression + changed-again re-surface, AND a `serializeMethodFamily` multi-method case: same methods in a different order serialize byte-equal (suppress), an added/removed/relabelled method serializes byte-different (re-surface).
  </action>
  <verify>
    <automated>npx vitest run src/logic/reconcile-diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/logic/reconcile-diff.ts` contains `export function classifyReconciliation(`, `export ... serializeMethodFamily(`, and the five-outcome union type.
    - `npx vitest run src/logic/reconcile-diff.test.ts` exits 0 with cases for canonical equality, label-change, missing-source-distinct, multi-source→one-card, narrow-memory suppress/re-surface, and the `serializeMethodFamily` order-independent-suppress / added-removed-relabelled-resurface multi-method case.
    - The classifier performs no I/O (pure): `grep -n "getFirstAsync\|runAsync\|readAllContacts" src/logic/reconcile-diff.ts` returns nothing.
  </acceptance_criteria>
  <done>reconcile-diff deterministically classifies the five field families with canonical equality, missing-source distinctness, multi-source collapse, and narrow memory.</done>
</task>

<task type="auto">
  <name>Task 2: reconcile-snapshot-dao (narrow last-reviewed source memory)</name>
  <files>src/db/reconcile-snapshot-dao.ts, src/db/reconcile-snapshot-dao.test.ts</files>
  <read_first>
    - src/db/import-session-dao.ts (109-119 assertOneChange; 172-198 the *Core + thin inWriteTransaction wrapper duality)
    - src/db/contact-methods-dao.ts (canonical_value — the canonical form stored as the reviewed value for phones/emails)
    - src/db/migrations/013-reconciliation-and-merge.ts (reconcile_source_snapshot table — from 20-01)
    - src/db/transaction.ts (inWriteTransaction — shared non-reentrant mutex; never nest)
  </read_first>
  <action>
    Create `src/db/reconcile-snapshot-dao.ts` — one durable row per `(external_contact_link_id, field_family)` in `reconcile_source_snapshot`. Provide a `*Core` upsert (`upsertReviewedSnapshotCore(exec, { externalContactLinkId, fieldFamily, reviewedValue, reviewedAt })`) callable inside a caller-owned txn plus a thin inWriteTransaction wrapper, and a read (`getReviewedSnapshots(exec, externalContactLinkId)`) returning the map reconcile-diff consumes. A phones/emails family holds MULTIPLE methods, but the snapshot stores ONE `reviewed_value` per `(external_contact_link_id, field_family)` — so the family's methods MUST be encoded as a single stable, canonical SET serialization, NOT one method's value: use the pure `serializeMethodFamily(methods)` defined and exported by reconcile-diff.ts (Task 1) — the sorted list of `(method_type, canonical_value, label)` tuples joined with a fixed delimiter — so two runs with the same methods in a different source ORDER produce the SAME string (no false re-nag) and any added/removed/relabelled method produces a DIFFERENT string (no false suppress). BOTH the snapshot write here and the reconcile-diff narrow-memory comparison (Task 1) use the IDENTICAL encoding (the same imported function) — the stored reviewed_value and the freshly-computed source value must be byte-equal to suppress. Store the raw comparable value for name/birthday; store the source photo CONTENT HASH (computed from the staged bytes) as the reviewed_value for photo (so an unchanged photo is suppressed on the next run and a changed photo re-surfaces — the narrow memory covers photo like the other families). All values `?`-bound; assert exactly one row changed on upsert. Write `src/db/reconcile-snapshot-dao.test.ts` proving upsert-on-pair, the canonical-equality suppression round-trip, read shaping, AND a MULTI-METHOD round-trip: a phones family with two+ methods stored then re-serialized from the same methods in a DIFFERENT order suppresses (byte-equal), while adding a method, removing one, or relabelling one re-surfaces (byte-different).
  </action>
  <verify>
    <automated>npx vitest run src/db/reconcile-snapshot-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/reconcile-snapshot-dao.ts` contains `export async function upsertReviewedSnapshotCore(` and a read function, and IMPORTS `serializeMethodFamily` from reconcile-diff.ts for the phones/emails reviewed_value (the same serializer reconcile-diff uses for narrow-memory comparison).
    - `npx vitest run src/db/reconcile-snapshot-dao.test.ts` exits 0, including the MULTI-METHOD round-trip: same methods in different order → suppressed (byte-equal serialization); added/removed/relabelled method → re-surfaces (byte-different).
    - Upsert is keyed on `(external_contact_link_id, field_family)` and asserts one row changed; the phones/emails `reviewed_value` is the sorted-`(method_type, canonical_value, label)` SET serialization, not a single method's value.
  </acceptance_criteria>
  <done>The narrow last-reviewed-source memory persists one canonical row per link+field and round-trips suppression.</done>
</task>

<task type="auto">
  <name>Task 3: ReconcileDetailScreen + per-contact entry + apply-through-existing-writers</name>
  <precondition>modules/orbit-contact-picker exposes readContactsByLookupKeys / readAllContacts (Phase 19 native artifact) and external_contact_links (migration 011) is present.</precondition>
  <files>src/screens/ReconcileDetailScreen.tsx, src/screens/ContactProfileScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>
    - modules/orbit-contact-picker/index.ts (14-24 PickedContact.photoTempUri is an evictable cache `file://`; 69-123 readAllContacts(externalContactIds) live re-read)
    - src/services/import/import-acquire.ts (76-88 stage-picker-cache-photo idiom — `stageImportPhoto(mapped.photoTempUri, importStagingRelPath(...))`) and src/services/import/import-photo.ts (60-135 persistImportedPhotoPostCommit — resolve staged → resizeToMaster → persistMaster(avatars path) → setContactPhoto → cleanup: the EXACT pipeline to mirror for reconcile)
    - src/services/photos/photo-storage.ts (149-205 importStagingRelPath/stageImportPhoto/listImportStagingPhotos/deleteImportStaging + 310-316 resolveImportStagingUri; 325-378 persistMaster crash-safe `.bak` swap; 85-88 contactPhotoRelPath) — ADD the reconcile-staging analogs here
    - src/db/photo-relative-path.ts (22/37/47/71 SAFE_RELATIVE + SAFE_IMPORT_STAGING_RELATIVE + assertSafe* allowlists) — ADD `SAFE_RECONCILE_STAGING_RELATIVE` + `assertSafeReconcileStagingRelative` mirroring the import-staging namespace
    - src/logic/reconcile-diff.ts (Task 1) + src/db/reconcile-snapshot-dao.ts (Task 2)
    - src/components/FieldChoiceGroup.tsx, src/components/PhotoChoice.tsx (20-02 — the reusable resolvers; PhotoChoice takes a pre-resolved durable `file://` from resolveReconcileStagingUri, NEVER the temp URI)
    - src/components/OverflowMenu.tsx + src/screens/SurvivorSelectScreen.tsx (20-01/20-02 — the merge entry the reconcile-detail `Merge with another contact` affordance routes into)
    - src/db/contacts-dao.ts (308-335 updateContactMetadataCore — name/birthday/category writer, asserts one row, OMITS photo, and does NOT bump data_revision — bare UPDATE contacts; setContactPhotoCore from 20-01; construct a COMPLETE UpdateContactFullInput from the current contact + overlaid selections, never a sparse patch)
    - src/db/data-revision-dao.ts (`bumpDataRevisionCore(exec)` — call ONCE inside the apply txn so a scalar-only reconcile is backup-visible; setContactPhoto already bumps, applyContactMethodDiffCore bumps only if changed, updateContactMetadataCore never bumps — HIGH #1) and src/services/backup-sweep.ts (29-31 — the sweep gates on readDataRevision→shouldRunAutomaticBackup, the reason the bump matters)
    - src/services/import/import-photo.ts (96-135 `persistImportedPhotoPostCommit` — the EXACT post-commit ordering to mirror for the reconcile photo: it runs persistMaster→setContactPhoto AFTER the record txn resolved, catching errors to degrade not corrupt; do NOT persistMaster before the apply txn)
    - src/db/contact-methods-dao.ts (99-287 applyContactMethodDiffCore — WHOLE-LIST diff: `seeded` = every current Orbit method, `current` = the desired list; any seeded method absent from `current` is tombstoned+DELETEd at 191-215. The apply MUST seed all current methods and set desired = current∪accepted-additions, never just additions)
    - src/db/migrations/001-initial.ts (field_history schema: contact_id, field_col_name, old_value, operation, created_at — no FK) + src/db/field-type-change.ts (the existing in-txn `INSERT INTO field_history` idiom to mirror) — snapshot the prior value when an Apply selection overwrites an existing Orbit name/birthday, same-txn, mirroring the merge writer
    - src/db/reconcile-snapshot-dao.ts (upsertReviewedSnapshotCore — write one snapshot per CONTRIBUTING external_contact_link_id, not just the selected source)
    - src/db/transaction.ts (inWriteTransaction)
    - src/screens/DuplicateReviewScreen.tsx (221-234 header idiom)
    - src/screens/ContactProfileScreen.tsx (41 import, 786 OverflowMenu — add 'Update from Contacts', shown only when ≥1 active external_contact_link)
    - src/navigation/RootNavigator.tsx + src/navigation/types.ts
    - .planning/phases/20-contact-reconciliation-merge/20-UI-SPEC.md (surface #4 ReconcileDetail; #2 per-contact inline scan + 'No changes from Contacts.'; recommendation chips colourless)
  </read_first>
  <action>
    FIRST author the RECONCILE PHOTO-STAGING pipeline (net-new, mirroring the shipped import path so no evictable temp URI is ever persisted or handed to setContactPhotoCore): (a) in `src/db/photo-relative-path.ts` add `SAFE_RECONCILE_STAGING_RELATIVE = /^reconcile-staging\/[A-Za-z0-9_-]+\.(jpg|jpeg|png|webp)(?:\.stage-tmp)?$/` + `assertSafeReconcileStagingRelative`, mirroring the import-staging namespace (a separate namespace so canonical avatars paths can never collide with it); (b) in `src/services/photos/photo-storage.ts` add `reconcileStagingRelPath(token)`, `stageReconcilePhoto(cacheUri, relative)` (copy-then-atomic-rename, mirroring `stageImportPhoto`), `resolveReconcileStagingUri(relative)`, `listReconcileStagingPhotos()`, and `deleteReconcileStaging(relative)`; (c) create `src/services/photos/reconcile-photo.ts` mirroring `import-photo.ts` with an injectable FS boundary: `stageReconcileSourcePhoto(cacheUri, token)` copies the picker temp bytes to a durable `reconcile-staging/` path AND returns { stagedRelative, contentHash } (the content hash computed from the staged bytes — the net-new signal), and `promoteReconcilePhoto(fs, { contactId, stagedRelative })` resizes the staged file and `persistMaster`s it to `contactPhotoRelPath(contactId)` (`avatars/contact-<id>.jpg`), returning that durable relative path; write `src/services/photos/reconcile-photo.test.ts` (injected FS) proving staging produces a durable path + stable hash and promotion yields an `avatars/<name>.<ext>` path that passes `assertSafeRelative`.

    Create `src/screens/ReconcileDetailScreen.tsx` with route params typed `{ contactId: number; sessionId?: number; cardId?: number }` (finding 7 — DEFINE the contract now; the per-contact profile entry in this plan passes `{ contactId }` only). This plan defines the route-param SHAPE and reads the optional params, but does NOT itself write card status — the grid↔detail card-status persistence + focus refresh is wired in 20-04 (which owns the grid and ships `markCardStatusCore`), avoiding a wave-3 dependency on a wave-4 symbol. Surface a completed-selection result (e.g. a returned resolved/partial + remaining-unresolved count, or an onResolved callback param) that 20-04 can consume on return. On mount, read active external_contact_links for the contact, `readAllContacts` their externalContactIds (a brief in-progress state, never a frozen blank); apply the ONE-PHOTO-PER-CARD COLLAPSE — select the SINGLE highest-priority active source link that has a `photoTempUri` (the first active `external_contact_link` by ascending link id / creation order with a photo) and call `stageReconcileSourcePhoto` ONLY for that one source to move it to durable `reconcile-staging/` and compute its content hash BEFORE diffing (never pass the temp URI onward); the other sources' photos are NOT staged, so the card has exactly one staged photo written to the single `staged_photo_rel_path` column and the 20-05 sweep cannot orphan a second live staged option; load the reviewed snapshots, and run `classifyReconciliation` (feeding it the staged path + content hash for the photo family, never the temp URI). Render a scrollable per-field change list: each changed field a `FieldChoiceGroup` (additive preselect source / conflict no-preselect / removed preselect Orbit) or `PhotoChoice` (rendering the source photo via `resolveReconcileStagingUri(stagedRelative)` — a durable staged `file://`, NEVER the temp URI), each option carrying its source provenance label (Cluster M/N). Recommendation chips are colourless. Primary CTA `Apply` (accent), disabled until every conflict field has a selection (additive/removal preselected so they never block). Capture a FRESHNESS TOKEN at scan (HIGH #1): for every surfaced field family, record the CURRENT Orbit-side baseline value the source was compared against (the classifier already computes it) and the contact's `modified_at`; carry them in the diff (and, for the durable/bulk path, in the persisted card). On Apply, extract the write composition into a SHARED apply helper `applyReconcileSelections` in `src/db/reconcile-apply.ts` (so bulk 20-04 reuses the identical writer sequence — finding 6; it takes the exec + the resolved per-family selections + each selection's scan-time Orbit baseline + the promoted photo relative path, and returns whether a post-commit photo step is needed — including the chosen photo's content hash + contributing external_contact_link_ids so the caller writes the PHOTO family's reviewed snapshot ONLY after the post-commit photo apply succeeds — PLUS which fields were SKIPPED as stale) and run it as: in ONE inWriteTransaction, FIRST REVALIDATE FRESHNESS — for each accepted source value RE-READ the live Orbit value for that field and compare it to the scan-time baseline; if they differ (the user edited Orbit after scan) DROP that field from this apply (do NOT write it), collect it into a `staleFields` list, and leave it unresolved so the caller re-surfaces it as a fresh conflict — NEVER blindly overwrite a post-scan Orbit edit. Then apply ONLY the still-fresh selected NON-PHOTO values through the existing writers: build a COMPLETE `UpdateContactFullInput` from the contact's current metadata overlaid with the chosen (still-fresh) name/birthday/category and pass it to `updateContactMetadataCore`; snapshot the prior value to `field_history` (same txn) for any overwritten Orbit scalar; for methods, seed EVERY current Orbit method and pass a desired list = current methods ∪ accepted source additions to `applyContactMethodDiffCore` (a partial desired list would tombstone+DELETE unrelated Orbit methods); `upsertReviewedSnapshotCore` for every reviewed NON-PHOTO field family, once per CONTRIBUTING external_contact_link_id (so a multi-source value does not re-surface the other link's disagreement) — the PHOTO family's reviewed snapshot is DEFERRED out of this txn and written only in the post-commit photo step below, after setContactPhoto succeeds (reviewer HIGH — photo snapshot ordering); and call `bumpDataRevisionCore(exec)` ONCE in this txn when the apply wrote a durable SCALAR change via updateContactMetadataCore (which does NOT self-bump) so a scalar-only apply cannot escape the backup sweep; method changes self-bump via applyContactMethodDiffCore and the photo self-bumps via the post-commit setContactPhoto, so no double-handling is needed (HIGH #1 backup-visibility); a field SKIPPED as stale writes nothing and bumps nothing. Then close the txn. If the helper returned any `staleFields`, the screen does NOT report a clean apply — it re-runs the diff against the now-current Orbit + source for those fields and re-surfaces them as fresh conflicts (card → partial), so the post-scan Orbit edit is preserved and the user re-chooses against current state (HIGH #1 freshness). A chosen source PHOTO is applied POST-COMMIT (NOT in the txn), mirroring `persistImportedPhotoPostCommit` EXACTLY: `promoteReconcilePhoto` resizes the durable staged file and `persistMaster`s it to `avatars/contact-<id>.jpg`, THEN `setContactPhoto(exec, contactId, that-durable-avatars-relative-path, now)` (self-wrapped; bumps data_revision; NEVER a temp/staging URI — assertSafeRelative rejects anything but `avatars/<name>.<ext>`), the whole photo step wrapped best-effort so a failure degrades to 'photo unchanged' (like import's photo_failed) — never doing persistMaster before/inside the apply txn, so a rolled-back apply leaves the on-disk master and the DB photo reference intact (HIGH #2). ONLY when that post-commit `setContactPhoto` SUCCEEDS does the caller then `upsertReviewedSnapshotCore` the PHOTO family's reviewed snapshot (the source content hash, once per contributing external_contact_link_id, in its own small txn); if the post-commit photo apply FAILS, the caller writes NO photo reviewed snapshot and leaves the photo card/field UNRESOLVED, so the next scan re-surfaces the un-applied chosen photo rather than suppressing it via PHOTO NO-RE-NAG (reviewer HIGH — photo snapshot ordering). POST-COMMIT (best-effort, never in the txn) `deleteReconcileStaging` the promoted staged file AND any source photo that was STAGED at scan but NOT chosen at apply, so a normal completed review does not leak staging files (reviewer LOW finding 10). Derived values (last_contact, gravity, status) are recomputed by those writers/query-time, never shown as a choosable field. Add the `Update from Contacts` OverflowMenu action to ContactProfileScreen, shown only when the contact has ≥1 active external_contact_link; the per-contact scan shows a brief spinner then routes here if there are changes or shows `No changes from Contacts.` inline if none. Add a `Merge with another contact` affordance to ReconcileDetailScreen (a header/overflow action) that routes into `SurvivorSelect` with this contact pre-filled as one candidate (reusing the same `listMergeCandidates`-backed Bound+Unbound second-candidate select from 20-01 — not `listSunCandidates`) — this satisfies the reconciliation-detail merge entry of LOCKED UI-SPEC #10 / Cluster U, not only the profile overflow. (DEFERRED with rationale, flagged for owner: wiring `DuplicateReviewScreen.onInspect` (currently a no-op at :239, a Phase-19 import screen out of this plan's file scope) to a merge entry is NOT done here — the profile overflow (20-01) + this reconciliation-detail entry already cover RCN-03's merge reachability; the duplicate-review inspect→merge entry is a follow-up needing owner confirmation, recorded rather than silently dropped.) Register ReconcileDetailScreen in RootNavigator + types. All colours via tokens.
  </action>
  <verify>
    <automated>npx vitest run src/services/photos/reconcile-photo.test.ts && npx tsc --noEmit && npm run check:colors && npm test</automated>
    <human-check>On the Pixel (debug build): edit a linked system contact (add an email, change the name, change the photo), tap ⋯ → Update from Contacts; confirm the addition is recommended, the name conflict requires a manual choice, the source photo renders (from durable staging, not a blank), applying writes only the chosen values incl. the photo, and re-running immediately shows no re-nag for the kept name OR the unchanged photo. (End-of-phase UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/ReconcileDetailScreen.tsx` exists, is registered, composes FieldChoiceGroup/PhotoChoice from the classifier output, and exposes a `Merge with another contact` affordance routing into SurvivorSelect (UI-SPEC #10).
    - `src/screens/ContactProfileScreen.tsx` contains an `Update from Contacts` action gated on active external links.
    - The reconcile photo-staging pipeline exists: `src/db/photo-relative-path.ts` contains `SAFE_RECONCILE_STAGING_RELATIVE` + `assertSafeReconcileStagingRelative`; `src/services/photos/photo-storage.ts` contains `stageReconcilePhoto`/`resolveReconcileStagingUri`/`listReconcileStagingPhotos`/`deleteReconcileStaging`; `src/services/photos/reconcile-photo.ts` contains `export ... stageReconcileSourcePhoto(` and `promoteReconcilePhoto(`.
    - No evictable temp URI is persisted or applied: `grep -n "photoTempUri" src/screens/ReconcileDetailScreen.tsx` shows it ONLY passed into `stageReconcileSourcePhoto` (never into PhotoChoice, a snapshot, or a photo writer); the Apply photo path calls `promoteReconcilePhoto` then `setContactPhoto` with an `avatars/…` path. ONE-PHOTO-PER-CARD: exactly one source photo is staged (the highest-priority active link's), so the single `staged_photo_rel_path` slot is filled from one source and other sources' photos are not staged.
    - The Apply path calls the shipped writers (no new write path) from the shared apply helper: `grep -n "updateContactMetadataCore\|applyContactMethodDiffCore\|upsertReviewedSnapshotCore\|bumpDataRevisionCore" ` the apply helper (and its use by ReconcileDetailScreen) shows the four in-txn writers incl. the `bumpDataRevisionCore` call; a photo choice does NOT go through updateContactMetadataCore and the `promoteReconcilePhoto`/`persistMaster` call is POST-COMMIT, never before/inside the apply txn.
    - HIGH #1 backup-visibility test — a scalar-only (name/birthday) reconcile Apply ADVANCES `data_revision` (read before/after via `readDataRevision`): a node:sqlite test asserts the revision increments so the backup sweep sees the change.
    - HIGH #1 freshness test — a `reconcile-apply.test.ts` (node:sqlite) case: build a selection with a scan-time Orbit baseline for a scalar (name/birthday), then MUTATE that Orbit field directly (simulating a post-scan user edit), then call `applyReconcileSelections`; assert the stale field is NOT overwritten (the post-scan Orbit value survives), it is returned in `staleFields`, and `data_revision` did NOT advance for that skipped field — proving a resumed/delayed apply never silently clobbers a post-scan Orbit edit (HIGH #1). `grep -n "staleFields\|modified_at\|baseline" src/db/reconcile-apply.ts` shows the revalidation.
    - HIGH #2 test — rollback-leaves-photo-intact: with a source photo chosen, forcing the apply txn to throw leaves BOTH the DB `photo` reference AND the on-disk `avatars/contact-<id>.jpg` bytes unchanged (because persistMaster is POST-COMMIT); a test over the photo-apply ordering (injected FS) proves persistMaster is not called when the pre-photo txn rolls back.
    - PHOTO SNAPSHOT ORDERING test (reviewer HIGH — photo snapshot ordering) — the PHOTO family's reviewed snapshot is NOT written inside the apply txn: a `src/db/reconcile-apply.test.ts` (node:sqlite) case drives `applyReconcileSelections` with a chosen source photo and asserts NO `reconcile_source_snapshot` row for the photo family exists after the txn commits (only the non-photo families' snapshots are present); then, simulating a FAILED post-commit photo apply (setContactPhoto never reached / throws), asserts STILL no photo reviewed-snapshot is committed and a subsequent `getReviewedSnapshots` + `classifyReconciliation` RE-SURFACES the photo (not suppressed via PHOTO NO-RE-NAG); a SUCCESSFUL post-commit apply then upserts the photo snapshot and the following scan suppresses the unchanged photo. `grep -n "photo\|snapshot" src/db/reconcile-apply.ts` confirms the helper does not upsert the photo family's reviewed snapshot in-txn (the photo reviewed-value is written only by the caller's post-commit success path).
    - The method desired-list construction (current ∪ accepted additions) is a pure, node-testable helper (in reconcile-diff.ts or a sibling) with a test asserting that accepting ONE source addition retains EVERY pre-existing Orbit method in the desired list (guarding the applyContactMethodDiffCore whole-list-delete data-loss path); the helper never emits a desired list that omits a current seeded method.
    - `npx vitest run src/services/photos/reconcile-photo.test.ts` exits 0 (staging → durable path + stable hash; promotion → assertSafeRelative-valid avatars path); `npx tsc --noEmit`, `npm run check:colors`, `npm test` pass.
  </acceptance_criteria>
  <done>A user can reconcile one linked contact end-to-end, applying only chosen values through the shipped writers, with narrow memory suppressing re-nags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android Contacts (source) → Orbit (local) | Reconciliation reads system Contacts on-device and one-way; a source change must never silently overwrite or delete Orbit data, and Orbit never writes back. |
| device Contacts store → app | Untrusted external contact values cross here (names, methods, birthday, photo). |

> No network on any read path (local-first). No npm packages installed this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03-1 | Repudiation / Tampering | reconciliation apply | high | mitigate | Apply only user-selected values through existing writers; conflicts require explicit choice; removed/missing-source never auto-delete; overwritten Orbit scalars snapshot to field_history; snapshot written same-txn. |
| T-20-03-4 | Data loss | method apply via whole-list diff | high | mitigate | applyContactMethodDiffCore tombstones+DELETEs any seeded method absent from the desired list; the apply seeds every current method and sets desired = current∪accepted, verified by a pure-helper test, so accepting one addition cannot delete unrelated Orbit methods. |
| T-20-03-5 | Data loss / Availability | source photo persistence + apply atomicity | high | mitigate | The evictable `photoTempUri` is staged to durable `reconcile-staging/` (content-hashed) at scan; on Apply the promote/persistMaster + setContactPhoto run POST-COMMIT (mirroring persistImportedPhotoPostCommit), NOT before the apply txn, so a rolled-back apply cannot leave the canonical master overwritten with the prior photo `.bak` already deleted (persistMaster overwrites in place — photo-storage.ts:325-378); no temp/cache URI is ever persisted or applied; a post-commit photo failure degrades to 'photo unchanged', never corruption (reviewer HIGH #2). |
| T-20-03-6 | Data integrity / Availability | reconcile apply escapes backup | high | mitigate | The apply txn calls `bumpDataRevisionCore` on any durable write, so a name/birthday-only reconcile advances the revision the launch backup sweep gates on — the reconciled value cannot be silently omitted from the next automatic backup (updateContactMetadataCore does not bump; reviewer HIGH #1 backup-visibility). |
| T-20-03-7 | Tampering / Data loss | stale persisted diff overwrites a post-scan Orbit edit | high | mitigate | `applyReconcileSelections` captures a scan-time per-field Orbit baseline + `modified_at` and re-reads the live Orbit value before each write; a field that changed since scan is skipped and re-surfaced as a fresh conflict, never blindly written over the whole-row predicate-less `updateContactMetadataCore` — so a durable/resumed apply cannot silently clobber a user's post-scan Orbit edit (reviewer HIGH #1 freshness). |
| T-20-03-8 | Tampering / Data loss | photo reviewed-snapshot committed before the post-commit photo apply | high | mitigate | The PHOTO family's reviewed snapshot (source content hash) is NOT written in the apply txn; it is DEFERRED and upserted only after the post-commit `promoteReconcilePhoto`→`setContactPhoto` SUCCEEDS. A failed post-commit photo apply leaves NO committed photo reviewed-hash, so the next scan re-surfaces the un-applied chosen photo instead of suppressing it via PHOTO NO-RE-NAG (source-hash==reviewed-hash) — the chosen photo is never silently dropped-and-forgotten. Non-photo families' reviewed snapshots stay in-txn, unaffected (reviewer HIGH — photo snapshot ordering). |
| T-20-03-2 | Tampering | missing-source handling | medium | mitigate | Missing source is a distinct card state (Cluster G), never five field-removals; never deletes/archives/unbinds/clears. |
| T-20-03-3 | Input Validation / Tampering | source values into SQL | low | mitigate | All values `?`-bound; birthday via the shipped normalize/validate; method values via canonical normalizer; no identifier interpolation (ASVS V5). |
</threat_model>

<verification>
- `npx vitest run src/logic/reconcile-diff.test.ts src/db/reconcile-snapshot-dao.test.ts` green; `npm test` green after the wave.
- `npx tsc --noEmit`, `npm run check:colors` clean.
- Device-UAT (end-of-phase): per-contact reconcile additive/conflict/no-renag on the Pixel.
</verification>

<success_criteria>
- One-way source→Orbit reconciliation for a single contact never destroys Orbit data.
- Canonical equality, missing-source distinctness, multi-source collapse, and narrow memory all hold.
- Apply routes exclusively through the shipped authoritative writers.
</success_criteria>

## Artifacts this phase produces

Phase-wide manifest of NET-NEW symbols (spanning plans 20-01..06). The convergence
source-grounding pass excludes these newly-created symbols from drift verification.

- **Migration 013** (`src/db/migrations/013-reconciliation-and-merge.ts`, `migration013`) — `TARGET_VERSION` **13**. New tables: `reconciliation_sessions`, `reconciliation_session_cards`, `reconcile_source_snapshot`, `bulk_review_resolutions`; **`contact_redirects` ONLY if the 20-01 owner checkpoint selects add-redirect**.
- **DB DAOs/functions:** `mergeContacts` (merge-dao.ts); `setContactPhotoCore` (contacts-dao.ts, 20-01); `upsertReviewedSnapshotCore`/`getReviewedSnapshots` (reconcile-snapshot-dao.ts); `createReconcileSession`/`insertReconcileCardCore`/`markCardStatusCore`/`finalizeSessionIfTerminal`/`discardSession` (reconcile-session-dao.ts); `getResumableReconcileSession`/`mapReconcileSession` (reconcile-session-read.ts); `relinkExternalSource`/`unlinkExternalSource` (reconcile-relink-dao.ts); bulk-review read (bulk-review-read.ts) + `resolveBulkReviewFlagCore`/`ignoreBulkReviewFlagCore` (bulk-review-dao.ts).
- **Pure logic:** `classifyReconciliation` + desired-method-list helper (reconcile-diff.ts); `recommendSurvivor` (survivor-recommendation.ts).
- **Reconcile apply:** `applyReconcileSelections` (src/db/reconcile-apply.ts) — the SHARED in-txn writer composition (updateContactMetadataCore + applyContactMethodDiffCore + upsertReviewedSnapshotCore + `bumpDataRevisionCore` on a scalar change — HIGH #1 backup-visibility) reused by both ReconcileDetailScreen (20-03) and bulk `onBulkAction` (20-04, finding 6); it takes a per-field SCAN-TIME Orbit baseline (+ contact `modified_at`) and REVALIDATES freshness before each write, returning a `staleFields` list of fields skipped because Orbit changed since scan (HIGH #1 freshness — never silently overwrites a post-scan Orbit edit); the chosen photo is applied POST-COMMIT via promoteReconcilePhoto→setContactPhoto (HIGH #2), and the PHOTO family's reviewed snapshot is likewise DEFERRED out of the apply txn and written only after that post-commit photo apply succeeds, so a failed photo apply never suppresses the un-applied chosen photo (reviewer HIGH — photo snapshot ordering). The scan-time Orbit baseline + `modified_at` are the net-new FRESHNESS TOKEN carried in the diff and persisted in the durable card (20-04). ReconcileDetailScreen route params `{ contactId, sessionId?, cardId? }` (finding 7).
- **Photo staging (reconcile):** `SAFE_RECONCILE_STAGING_RELATIVE`/`assertSafeReconcileStagingRelative` (photo-relative-path.ts); `reconcileStagingRelPath`/`stageReconcilePhoto`/`resolveReconcileStagingUri`/`listReconcileStagingPhotos`/`deleteReconcileStaging` (photo-storage.ts); `stageReconcileSourcePhoto`/`promoteReconcilePhoto` (reconcile-photo.ts) — the reconcile analog of the import photo-staging pipeline; temp URIs are never persisted or applied (20-03).
- **Service:** `registerReconcileResumeSweep` (reconcile-resume-sweep.ts).
- **Components:** `FieldChoiceGroup`, `PhotoChoice`, `MergeImpactSummary`, `ResumeReconcilePrompt` (20-05); shipped `CandidateCardGrid` EXTENDED additively (20-04).
- **Screens + routes:** `SurvivorSelectScreen`, `MergeConflictsScreen`, `ReconcileDetailScreen`, `ReconcileGridScreen`, `ReconcileCompleteScreen`, `BulkReviewScreen`.
- **Test files:** merge-dao.test.ts, migrations/013-*.test.ts, reconcile-diff.test.ts, reconcile-snapshot-dao.test.ts, reconcile-photo.test.ts, reconcile-session-dao.test.ts, reconcile-session-read.test.ts, reconcile-relink-dao.test.ts, survivor-recommendation.test.ts, reconcile-resume-sweep.test.ts, bulk-review-read.test.ts, bulk-review-dao.test.ts, + extension to backup/reconciliation.test.ts.

<output>
Create `.planning/phases/20-contact-reconciliation-merge/20-03-SUMMARY.md` when done.
</output>
