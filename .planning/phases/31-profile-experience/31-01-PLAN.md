---
phase: 31-profile-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/024-profile-presentation.ts
  - src/db/migrations/024-profile-presentation.test.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/backup/backup-schema.ts
  - src/profile/types.ts
  - src/profile/module-registry.ts
  - src/profile/presentation-schema.ts
  - src/profile/presentation-schema.test.ts
  - src/profile/resolve-presentation.ts
  - src/profile/resolve-presentation.test.ts
autonomous: false
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-07, PROF-08]
estimate: { tokens: 76000, raw_tokens: 76000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "D-01/D-02: implementation follows the full dossier and binding planning-notes; no accepted/rejected decision is reopened."
    - "D-03: migration 024 is head+1, forward-only, chain-tested fresh and 023→024; durable global preferences are app_settings columns and enter PORTABLE_SETTINGS_KEYS without widening backup emission."
    - "D-04/D-05/D-06: the contract has no Profile AI-draft module, cadence is nullable, and the shared Unbound Intensity window is the current local calendar month labelled This month."
    - "D-07/D-09: durable layout/background definitions, assignments, contact overrides, and collapse state exist; fixed Hero remains outside ProfilePresentation while backgrounds are permitted."
    - "D-08/D-10/D-11: semantic knowledge owners remain separate, reset affects presentation only, Category/template fallout falls through, and History is a stable replaceable module ID."
    - "Factory order and legal nesting/sizes are closed semantic data; malformed persisted documents fall back safely."
    - "ProfilePresentation resolves layout and background independently contact → Category → global → factory/theme; explicit contact choices survive Category changes."
    - { statement: "All 50 specless edge rows are preserved and classified in 31-COVERAGE.md", verification: backstop }
  artifacts:
    - src/db/migrations/024-profile-presentation.ts
    - src/profile/module-registry.ts
    - src/profile/presentation-schema.ts
    - src/profile/resolve-presentation.ts
  key_links:
    - "migration 024 tables/settings → resolver inputs → generalized ProfilePresentation"
  prohibitions:
    - "Never edit migrations 001–023 or use a schema-push tool."
    - "Never persist component names, arbitrary nesting, Hero layout, or runtime SQL identifiers."
    - "Never widen backup format emission in Phase 31."
---

<objective>
Freeze the durable Profile-presentation contract and prove its end-to-end resolution path with one production tracer.

Purpose: every later renderer/editor depends on stable semantic IDs and inheritance; the migration is the phase's one-way door.
Output: migration 024, closed presentation contracts, factory registry, resolver, settings integration, and fresh/upgrade tests.

## Artifacts this phase produces
- `profile_layout_templates`, `profile_background_templates`, `profile_category_assignments`, and `contact_profile_presentations` tables
- `migration024`, `ProfilePresentation`, `PROFILE_MODULE_REGISTRY`, `parseAndCanonicalizeProfileLayout`, `resolveProfilePresentation`
- nullable `app_settings` global layout/background template UID fields and their portable-settings allowlist entries
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@.planning/phases/31-profile-experience/31-CONTEXT.md
@.planning/phases/31-profile-experience/31-RESEARCH.md
@.planning/phases/31-profile-experience/31-PATTERNS.md
@.planning/phases/31-profile-experience/31-UI-SPEC.md
@.planning/phases/31-profile-experience/31-COVERAGE.md
@docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-10-planning-notes.md
</context>

<assumption_delta_decision>
add-alongside — introduce generalized `ProfilePresentation` resolution for independently inherited layout and background, while the fixed Hero remains outside and invariant. Protect the shared hierarchy with a layout/background matrix test.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve migration 024 presentation schema</name>
  <reversibility rating="one-way">Migration 024 runs forward-only on unreachable device databases; a wrong durable shape requires another migration and cannot be repaired remotely.</reversibility>
  <read_first>
    - src/db/migrations/022-orrery-systems.ts
    - src/db/migrations/023-orrery-system-selection-revision.ts
    - src/db/database.ts
    - docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
  </read_first>
  <action>Present the exact four-entity schema proposed by 31-RESEARCH: separate named layout/background template tables; one Category-assignment row per Category with nullable layout/background UIDs; one contact-presentation row with explicit layout-template UID or canonical freeform layout JSON, background-template UID, and canonical collapse-override JSON; nullable global template UIDs in app_settings. Confirm FK/cascade/fallthrough and that layout/background identity is UID-based. Do not write schema until approved.</action>
  <decision>Approve the migration-024 table decomposition and reference/fallout policy before irreversible application code is authored.</decision>
  <options>
    <option id="four-entity"><name>Four-entity schema (recommended)</name><pros>Separates reusable definitions, Category assignments, and contact overrides while resolving layout/background independently.</pros><cons>Template deletion needs explicit transactional cleanup of nullable references.</cons></option>
    <option id="owner-alternative"><name>Owner-supplied alternative</name><pros>Allows a different durable shape before it ships.</pros><cons>Requires updating the downstream resolver/DAO contract before execution continues.</cons></option>
  </options>
  <verify><automated>test "$(rg -n 'TARGET_VERSION = 23' src/db/database.ts | wc -l)" -eq 1</automated></verify>
  <acceptance_criteria>Owner chooses the proposed schema or supplies an explicit alternative; execution records the choice before Task 2.</acceptance_criteria>
  <done>The one-way schema shape is authorized.</done>
  <resume-signal>Select `four-entity` or describe the owner-supplied alternative.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Migrate and resolve one factory/global/Category/contact presentation path</name>
  <files>src/db/migrations/024-profile-presentation.ts, src/db/migrations/024-profile-presentation.test.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/backup/backup-schema.ts, src/profile/types.ts, src/profile/module-registry.ts, src/profile/presentation-schema.ts, src/profile/presentation-schema.test.ts, src/profile/resolve-presentation.ts, src/profile/resolve-presentation.test.ts</files>
  <read_first>
    - src/db/database.ts
    - src/db/migrations/022-orrery-systems.ts
    - src/db/migrations/022-orrery-systems.test.ts
    - src/db/app-settings-dao.ts
    - src/db/app-settings-dao.test.ts
    - src/backup/backup-schema.ts
    - src/backup/backup-schema.test.ts
    - src/db/migrations/runner.ts
    - docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
  </read_first>
  <behavior>
    - Fresh 001→024 and existing 023→024 paths both reach user_version 24 without editing shipped migrations.
    - One layout and one background independently resolve factory/theme → global → Category → explicit contact, with missing/deleted references falling through.
    - Factory registry fixes Hero outside customization; body order is Overview, Things to Remember, Methods, History; TTR order is the approved eight-child order.
    - Parser rejects unknown/duplicate IDs, illegal parent moves, third nesting, unsupported Overview sizes, invalid versions, and malformed collapse maps.
  </behavior>
  <action>Recheck migration head and TARGET_VERSION before editing; halt if 024 is no longer head+1. Implement approved additive tables with parameter-safe identifiers, UNIQUE/FK/CHECK constraints and exported SQL. Register migration024 and set TARGET_VERSION 24. Add the two nullable settings through every app-settings mapping/validation point and PORTABLE_SETTINGS_KEYS, but keep current backup emission unchanged per D-03. Define node-pure semantic module/layout/background/source types, the complete registry, versioned closed parser/canonicalizer, and `resolveProfilePresentation`. The resolver must never materialize inherited state into contact rows. Use the current-local-month tagged fallback contract for Unbound Intensity so Phase 32 can reuse it per D-05/D-06.</action>
  <verify><automated>npx vitest run src/db/migrations/024-profile-presentation.test.ts src/db/app-settings-dao.test.ts src/profile/presentation-schema.test.ts src/profile/resolve-presentation.test.ts</automated></verify>
  <acceptance_criteria>All four test files pass; database.ts contains migration024 and TARGET_VERSION 24; tests prove fresh/upgrade chains, closed parsing, independent precedence, contact-override survival, Category deletion fallthrough, and presentation-only reset inputs.</acceptance_criteria>
  <done>The irreversible schema and production resolution skeleton are proven end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete factory registry and source/edge invariants</name>
  <files>src/profile/module-registry.ts, src/profile/presentation-schema.test.ts, src/profile/resolve-presentation.test.ts, src/db/app-settings-dao.test.ts</files>
  <read_first>
    - .planning/phases/31-profile-experience/31-COVERAGE.md
    - .planning/phases/31-profile-experience/31-UI-SPEC.md
    - src/profile/module-registry.ts
    - src/profile/presentation-schema.test.ts
    - src/profile/resolve-presentation.test.ts
    - src/db/app-settings-dao.test.ts
  </read_first>
  <behavior>
    - Every legal module has parentage, factory order/visibility/expanded state, supported size variants, and accessibility label metadata.
    - Equal/touching/empty/order edge cases listed for PROF-02/03/05/07/08 in 31-COVERAGE are asserted or explicitly retained as backstops.
  </behavior>
  <action>Finish registry and invariant matrices against the exact dossier/UI-SPEC. Assert freeform snapshots do not follow later template edits; reusable assignments do; a successful layout switch clears collapse overrides; combined reset targets only the contact presentation row. Keep the seven flagged assumptions in 31-COVERAGE visible to executors rather than silently converting them into product choices.</action>
  <verify><automated>npx vitest run src/profile/presentation-schema.test.ts src/profile/resolve-presentation.test.ts src/db/app-settings-dao.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Targeted tests and check pass; registry contains exactly four top-level modules, six Overview children, and eight TTR children with no Profile AI-draft or customizable Hero entry.</acceptance_criteria>
  <done>The contract is complete enough for independent expansion plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted JSON → parser/resolver | locally stored state may be stale or malformed |
| DAO input → SQLite | names, UIDs, and JSON cross into durable storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-01 | Tampering/DoS | presentation JSON | high | mitigate | total closed parser plus safe factory fallback and adversarial tests |
| T-31-02 | Tampering | migration/FKs | high | mitigate | fresh+upgrade chain and cascade/fallthrough tests before approval |
| T-31-03 | Information Disclosure | backup settings | medium | mitigate | allowlist settings keys without widening Phase 31 backup emission |
</threat_model>

<verification>Run targeted tests, then `npm test && npm run check` at the Wave 1 boundary.</verification>
<success_criteria>The schema, registry, parser, and resolver are durable, closed, independently inherited, and D-01…D-11 compliant.</success_criteria>
<output>Create `.planning/phases/31-profile-experience/31-01-SUMMARY.md` when done.</output>
