---
phase: 31-profile-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/profile/persisted-contract.ts, src/profile/persisted-contract.test.ts, src/db/migrations/profile-presentation.ts, src/db/migrations/profile-presentation.test.ts, src/db/database.ts, src/db/profile-presentation-dao.ts, src/db/profile-presentation-dao.test.ts, src/components/profile/ProfileOverviewTracer.tsx, src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.test.ts, .planning/phases/31-profile-experience/31-VALIDATION.md]
autonomous: false
requirements: [PROF-01, PROF-07, PROF-08, PROF-10]
estimate: { tokens: 49000, raw_tokens: 49000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "D-01/D-02: the dossier and binding planning notes govern without reopening settled decisions."
    - "D-03: execution derives schema head+1 from the live registry, records it as PROFILE_PRESENTATION_SCHEMA_VERSION, and tests fresh plus previous-head upgrades; no shipped migration is edited."
    - "D-04/D-05/D-06: no Profile AI draft exists and truthful Status never fabricates nullable cadence."
    - "D-07/D-08/D-09/D-10/D-11/D-12: presentation is separate from semantic data, backgrounds remain permitted, History stays replaceable, and no Off Limits permission/egress is added."
    - "A user opens Profile, sees the fixed Hero plus truthful Status, taps Relationship Overview, and the collapse choice survives readback and relaunch."
  artifacts: [src/profile/persisted-contract.ts, src/db/migrations/profile-presentation.ts, src/db/profile-presentation-dao.ts, src/components/profile/ProfileOverviewTracer.tsx]
  key_links: ["persisted semantic contract → approved head+1 schema JSON/check constraints", "Profile section toggle → public collapse DAO → profile-presentation row → revision reload/readback"]
  prohibitions:
    - "Never edit migrations 001–023, widen backup/AI egress, customize Hero, or store semantic contact data in presentation tables."
---
<objective>
Define the persisted semantic contract, approve the execution-resolved head+1 migration, and ship one narrow clickable DB-to-Profile tracer.
Purpose: prove real navigation/UI/read/write/relaunch architecture before horizontal expansion.
Output: complete presentation schema, minimal collapse DAO, clickable Profile slice, and tests.
## Artifacts this phase produces
- `PROFILE_LAYOUT_DOCUMENT_VERSION`, closed semantic module IDs, and collapse-map contract before schema approval
- execution-resolved head+1 Profile presentation migration and registration through stable `profilePresentationMigration` identifiers
- `readProfileCollapseOverride` / `setProfileCollapseOverride`
- first production `relationship-overview` header + truthful Status tile under invariant Hero
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@HANDOFF.md
@.planning/phases/31-profile-experience/31-CONTEXT.md
@.planning/phases/31-profile-experience/31-VALIDATION.md
@.planning/phases/31-profile-experience/31-COVERAGE.md
@docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-10-planning-notes.md
@docs/dossier/milestone-2/orbit-ui-ux-working-roadmap-v1.0.md
@docs/dossier/milestone-2/orbit-ui-ux-master-handoff-v1.0.md
@docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
@docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze minimum persisted semantic vocabulary and baseline</name>
  <files>src/profile/persisted-contract.ts, src/profile/persisted-contract.test.ts, .planning/phases/31-profile-experience/31-VALIDATION.md</files>
  <read_first>
    - src/db/migrations/022-orrery-systems.ts
    - src/db/migrations/023-orrery-system-selection-revision.ts
    - src/db/database.ts
    - src/db/migrations/runner.ts
    - docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
    - docs/dossier/milestone-2/planning-notes/phase-10-planning-notes.md
  </read_first>
  <action>Before any schema checkpoint, create a Node-pure persisted contract containing the document-version field, exact closed semantic module-ID union/grammar, parent rules, and collapse-map value shape. Cover canonical serialization and rejection of unknown IDs, illegal parents, duplicate IDs, invalid versions, and non-boolean collapse entries. Run and record the full pre-change test/check baseline in 31-VALIDATION. This addresses the review concern that irreversible JSON columns previously preceded their meaning.</action>
  <verify><automated>npx vitest run src/profile/persisted-contract.test.ts && npm test && npm run check</automated></verify>
  <acceptance_criteria>The minimum persisted vocabulary exists, is test-pinned, and the measured pre-change baseline is recorded before schema authorization.</acceptance_criteria>
  <done>The schema checkpoint can cite an executable semantic contract rather than prose.</done>
</task>
<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve live head+1 presentation schema and no-Category inheritance</name>
  <reversibility rating="one-way">The execution-resolved Profile presentation migration is forward-only on unreachable device databases.</reversibility>
  <read_first>
    - src/profile/persisted-contract.ts
    - src/db/database.ts
    - src/db/migrations/runner.ts
    - src/db/contacts-dao.ts
    - src/screens/EditContactScreen.tsx
    - docs/dossier/milestone-2/planning-notes/phase-10-planning-notes.md
  </read_first>
  <action>Before any resolver, presentation DAO, or Category-clear implementation can encode an outcome, ask the owner for both decisions in this checkpoint and record both selections verbatim in `31-01-SUMMARY.md` for Plan 02. First, at execution time parse the live `MIGRATIONS` versions and `TARGET_VERSION`, assert they are contiguous/equal, compute exactly head+1, and present that resolved number with the four-entity schema, explicit `COLLATE NOCASE` template-name uniqueness, foreign-key behavior, JSON columns constrained by the Task-1 contract, and independent axes. Use stable source path `src/db/migrations/profile-presentation.ts`, export `PROFILE_PRESENTATION_SCHEMA_VERSION` and `profilePresentationMigration`, and require every later plan/doc to use those stable identifiers rather than a baked number. Second, decide what happens to both effective presentation axes when an existing Category is cleared: ordinary fallthrough, or atomic durable retention. Do not author schema or inheritance behavior until both owner choices are recorded. Addresses Cycle-2 HIGH: the owner product decision now gates every behavior-encoding task.</action>
  <decision>Approve the live-head+1 four-entity decomposition before irreversible code is authored.</decision>
  <options><option id="four-entity"><name>Four-entity schema (recommended)</name><pros>Matches locked separation and inheritance.</pros><cons>Deletion needs transactional reference cleanup.</cons></option><option id="owner-alternative"><name>Owner alternative</name><pros>Changes shape before ship.</pros><cons>Requires downstream contract revision.</cons></option></options>
  <decision>Choose the inheritance rule for a contact whose existing Category is cleared.</decision>
  <options><option id="fall-through"><name>Fall through immediately</name><pros>After clearing Category, each inherited axis resolves to global/factory while explicit contact overrides survive.</pros><cons>The Profile presentation can change with the Category edit.</cons></option><option id="retain"><name>Retain effective presentation</name><pros>Preserves the pre-clear appearance by materializing only inherited effective axes as contact-owned assignments.</pros><cons>The Category-clear writer must resolve and persist layout plus background atomically with the contact edit.</cons></option></options>
  <verify><automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/db/database.ts','utf8');const t=Number(s.match(/TARGET_VERSION\s*=\s*(\d+)/)?.[1]);const v=[...s.matchAll(/migration(\d+)/g)].map(x=>Number(x[1]));if(!t||Math.max(...v)!==t||new Set(v).size!==t)process.exit(1);console.log('PROFILE_PRESENTATION_SCHEMA_VERSION='+(t+1))"</automated></verify>
  <acceptance_criteria>Owner approves a schema whose resolved version is current head+1 and whose persisted JSON semantics are already test-pinned, and separately selects fallthrough or durable retention for both axes when Category is cleared.</acceptance_criteria>
  <done>The one-way schema, exact execution-time version, and no-Category inheritance behavior are authorized before dependent implementation.</done>
  <resume-signal>Select the schema option and separately select `fall-through` or `retain` for Category clearing.</resume-signal>
</task>
<task type="tracer" tdd="true">
  <name>Task 3: Clickable persisted Relationship Overview collapse tracer</name>
  <files>src/db/migrations/profile-presentation.ts, src/db/migrations/profile-presentation.test.ts, src/db/database.ts, src/db/profile-presentation-dao.ts, src/db/profile-presentation-dao.test.ts, src/components/profile/ProfileOverviewTracer.tsx, src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.test.ts</files>
  <read_first>
    - src/db/migrations/022-orrery-systems.ts
    - src/db/migrations/022-orrery-systems.test.ts
    - src/db/database.ts
    - src/db/transaction.ts
    - src/db/data-revision-dao.ts
    - src/db/contact-status-read.ts
    - src/screens/ContactProfileScreen.tsx
    - src/screens/contact-profile-logic.ts
    - src/screens/contact-profile-logic.test.ts
    - src/components/control-surface/PopulationPanelContent.tsx
    - docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
    - docs/dossier/milestone-2/planning-notes/phase-10-planning-notes.md
    - docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
    - docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
  </read_first>
  <behavior>
    - Fresh 001→resolved-head+1 and previous-head→resolved-head+1 create the complete approved schema with foreign_keys ON.
    - Profile keeps fixed Hero and shows one truthful Status tile under Relationship Overview.
    - Named accessible header tap writes contact collapse state; failure retains prior state; reload/relaunch reads success back.
    - Unbound/never-contacted Status uses the existing guard, never unsafe cadence.
  </behavior>
  <action>Repeat the live head+1 assertion immediately before editing. Implement/register `profilePresentationMigration` at the approved `PROFILE_PRESENTATION_SCHEMA_VERSION` and set TARGET_VERSION to that exported value. Migration tests must enable `PRAGMA foreign_keys = ON`, exercise malformed JSON fallback plus contact/Category deletion, and derive both upgrade endpoints from the constants. Add the collapse slice through retained `ProfileOverviewTracer` so Plan 05 expands it instead of deleting inline tracer JSX. Wire pending/failure/readback using established publication/error patterns.</action>
  <verify><automated>npx vitest run src/profile/persisted-contract.test.ts src/db/migrations/profile-presentation.test.ts src/db/profile-presentation-dao.test.ts src/screens/contact-profile-logic.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Head+1 registration, FK-on fresh/upgrade/malformed/deletion tests, and tap→write→revision→readback/relaunch/rollback tests pass through the retained component seam.</acceptance_criteria>
  <done>The phase has a real clickable end-to-end tracer.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Profile tap → SQLite | durable presentation write |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-01 | Tampering | migration | high | mitigate | owner checkpoint and chain tests |
| T-31-02 | Tampering/DoS | collapse write | high | mitigate | bound SQL, outer transaction, rollback/readback tests |
</threat_model>
<verification>Baseline full suite, targeted tracer tests/check, then `npm test && npm run check` at Wave 1 boundary.</verification>
<success_criteria>A real user-clickable Profile presentation write works end-to-end before expansion.</success_criteria>
<output>Create `.planning/phases/31-profile-experience/31-01-SUMMARY.md` when done.</output>
