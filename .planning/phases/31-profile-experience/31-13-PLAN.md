---
phase: 31-profile-experience
plan: 13
type: execute
wave: 13
depends_on: [31-12]
files_modified:
  - src/services/photos/background-crop-geometry.ts
  - src/services/photos/background-crop-geometry.test.ts
  - src/services/photos/background-pipeline.ts
  - src/services/photos/background-pipeline.test.ts
  - src/components/profile/ProfileBackgroundManager.tsx
  - src/components/ui/overlay-base.tsx
  - .planning/phases/31-profile-experience/31-UAT.md
  - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
  - .planning/phases/31-profile-experience/evidence/31-13-debug/
autonomous: true
requirements: [PROF-04, PROF-20]
gap_closure: true
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A Profile-background author sees the whole selected source contained in the available crop surface, moves one aspect-locked selection rectangle by dragging, and changes its size by pinching without allowing it outside the source."
    - "The saved crop is the exact clamped source-pixel rectangle, then follows the established one-pass local JPEG derivative, 2048px long-edge cap, UID-derived naming, assignment, cancellation, error, and orphan-reconciliation contracts."
    - "Cancel, Reset, Fine tune, and Use background remain named and reachable at the current font scale without a horizontally scrolling ordinary-action strip; direct touch gestures have a textual state and a physical-Pixel evidence trail."
  artifacts:
    - src/services/photos/background-crop-geometry.ts
    - src/services/photos/background-pipeline.ts
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/components/ui/overlay-base.tsx
    - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
  key_links:
    - "ProfileBackgroundManager selection shared values → clamped source-pixel crop geometry → prepareProfileBackground → crash-safe app-owned derivative storage → existing template/assignment DAO"
    - "BaseOverlay RN Modal root → GestureHandlerRootView → ProfileBackgroundManager GestureDetector on Android"
---

<objective>
Close only UAT Test/Gap 6: make Profile-background cropping a direct, modern Android touch flow and record trustworthy physical-device evidence for PROF-04 and PROF-20.

Purpose: the existing crop model and storage lifecycle are sound, but gestures inside the shared RN Modal are strongly indicated to lack a Gesture Handler native-modal root, and the cover-image/scrolling-control UX does not satisfy the owner's approved crop model.

Output: source-selection crop geometry and regression tests, the corrected modal-root and Profile crop UI, plus debug-Pixel evidence and corrected checklist rows 43–44.

## Artifacts this phase produces

- `BackgroundCropSelection`-style source-rectangle helpers (create, clamp, pan, pinch-resize, source-pixel conversion, and textual selection state) in `background-crop-geometry.ts`.
- A full-source Profile crop workspace in `ProfileBackgroundManager` with direct drag/pinch, a contained source preview, dimmed outside-selection treatment, and a compact Fine tune disclosure.
- A `GestureHandlerRootView` at the direct content root of the shared React Native `Modal` in `BaseOverlay`.
- Physical-Pixel debug screenshots/UI trees and corrected crop evidence in `evidence/31-13-debug/` and the native checklist.

## Source Coverage Audit

| Source | ID | Current-scope item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 31 | Reusable Profile image backgrounds with an understandable direct crop flow | Tasks 1–3 | COVERED |
| REQ | PROF-04 | Custom-image background templates support drag/reposition and pinch crop while retaining assignment hierarchy/readability | Tasks 1–3 | COVERED |
| REQ | PROF-20 | Crop customization remains operable without precise drag and exposes textual state | Task 2–3 | COVERED |
| RESEARCH | Existing image stack | Retain the installed Image Manipulator/local-storage pipeline and bounded derivative rather than add a crop dependency | Task 1 | COVERED |
| CONTEXT | D-09 | Profile backgrounds remain permitted and readable through established theme treatment | Task 2 | COVERED |
| CONTEXT | D-10 | Background assignment hierarchy and contact-only reset boundary remain intact | Tasks 1–2 | COVERED |
| UAT | Test/Gap 6 | Real drag and pinch must work; ordinary controls must fit without horizontal scrolling | Tasks 2–3 | COVERED |

Scope fence: Plans 01–12 remain historical, no Phase 31.1 System-background work enters this plan, and no migration, schema, network, telemetry, backend, external API, assignment-hierarchy change, or release-APK iteration is authorized here.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@HANDOFF.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-profile-experience/31-CONTEXT.md
@.planning/phases/31-profile-experience/31-UAT.md
@.planning/phases/31-profile-experience/31-UI-SPEC.md
@.planning/phases/31-profile-experience/31-REVIEW-FIX.md
@.planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
@.planning/phases/31-profile-experience/31-12-SUMMARY.md
@docs/runbooks/desktop-build-pipeline.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one clamped source-selection crop through the existing local derivative pipeline</name>
  <files>src/services/photos/background-crop-geometry.ts, src/services/photos/background-crop-geometry.test.ts, src/services/photos/background-pipeline.ts, src/services/photos/background-pipeline.test.ts</files>
  <read_first>
    - src/services/photos/background-crop-geometry.ts
    - src/services/photos/background-crop-geometry.test.ts
    - src/services/photos/background-pipeline.ts
    - src/services/photos/background-pipeline.test.ts
    - src/services/photos/profile-background-target.ts
    - src/services/photos/profile-background-target.test.ts
    - src/services/photos/background-storage.ts
    - src/services/photos/background-storage.test.ts
    - src/services/photos/background-reconcile-sweep.ts
    - src/components/profile/ProfileBackgroundManager.tsx
  </read_first>
  <behavior>
    - A portrait, landscape, square, small, and large source each produce an aspect-locked source-pixel rectangle wholly within its bounds.
    - Dragging changes only selection position; pinch changes the locked selection size around its focal point; both clamp identically at every edge and preserve the Profile aspect.
    - The prepared output receives that exact rectangle once, preserves the established target output dimensions, persists once through the app-owned relative path, and releases its intermediate on either success or persistence failure.
  </behavior>
  <action>Start RED by replacing the cover-image transform contract with a source-rectangle selection contract. Export deterministic helpers for initial contained selection, source-bound clamping, drag translation, focal-point pinch resizing, conversion to `expo-image-manipulator` crop coordinates, and concise textual state. Keep Profile aspect and output dimensions derived through `profileBackgroundTarget`; retain the existing one-pass crop/resize/JPEG pipeline, UID-derived relative destination, crash-safe storage handoff, release hook, and error wrapping. Update `prepareProfileBackground` to consume the clamped selection rather than a display-cover transform, so the selected source rectangle is the rectangle sent to the manipulator.</action>
  <verify>
    <automated>npx vitest run src/services/photos/background-crop-geometry.test.ts src/services/photos/background-pipeline.test.ts src/services/photos/profile-background-target.test.ts src/services/photos/background-storage.test.ts src/services/photos/background-reconcile-sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>Deterministic tests cover portrait, landscape, square, small, and large sources; initial, edge, and focal-pinch selections remain source-bounded with the Profile aspect; pipeline assertions receive the expected source-pixel rectangle and retain the existing local persistence/release behavior.</acceptance_criteria>
  <done>One real selection path from source coordinates to a durable Profile-background derivative is tested end-to-end before UI expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the Modal-local gesture root and integrate the full-source accessible crop workspace</name>
  <files>src/components/ui/overlay-base.tsx, src/components/profile/ProfileBackgroundManager.tsx</files>
  <read_first>
    - App.tsx
    - src/components/ui/overlay-base.tsx
    - src/components/ui/Sheet.tsx
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/services/photos/background-crop-geometry.ts
    - src/services/photos/background-pipeline.ts
    - src/services/photos/profile-background-target.ts
    - node_modules/react-native-gesture-handler/src/components/GestureHandlerRootView.android.tsx
    - node_modules/react-native-gesture-handler/src/handlers/gestures/GestureDetector/index.tsx
    - .planning/phases/31-profile-experience/31-UI-SPEC.md
    - .planning/phases/31-profile-experience/31-UAT.md
  </read_first>
  <behavior>
    - Android Modal content has a Gesture Handler root even though the application root already has one, so the crop detector receives native drag and pinch events.
    - The displayed image is fully contained; a tokenized dim mask leaves one Profile-aspect selection rectangle clear, drag moves it, pinch resizes it, and the visual rectangle never leaves image bounds.
    - Cancel, Reset, Fine tune, and Use background remain named controls with textual crop state; named directional/zoom adjustments appear only through Fine tune and ordinary actions fit without horizontal scrolling.
    - Every calculation used during drag or pinch is worklet-safe, has its source/display/selection scalar inputs mirrored in shared values, and runs on the UI runtime; only gesture-end textual-state publication crosses to JS.
  </behavior>
  <action>Before editing, verify the installed `react-native-gesture-handler` version and its Modal requirement against the package source plus the current official installation guidance. Record the source-based conclusion in the implementation notes: the present root cause is strongly indicated by Android RN Modal content living in a separate native root, so App's outer root cannot serve the crop detector inside `BaseOverlay`. Put `GestureHandlerRootView` with `flex: 1` directly inside `RNModal` around the overlay content tree in `BaseOverlay`; retain BaseOverlay's scrim, Android Back, focus, and accessibility-modal contracts, and avoid a manager-only nested wrapper. In `ProfileBackgroundManager`, replace the cover-transform viewport with a contained full-source image and a Profile-aspect selection overlay whose dimmed outside area uses existing theme tokens. Define every helper called by `onStart` or `onUpdate` before the gesture callbacks, mark it worklet-safe, and mirror source dimensions, contained display dimensions, selection origin/size, and aspect/minimum bounds into scalar shared values before activation. Keep every frame calculation and assignment on the UI runtime; use `runOnJS` only after gesture end to commit the textual selection state. Apply Task 1 geometry at initialization, named adjustments, and save. Keep Cancel, Reset, and Use background visible; replace the permanent horizontal Reset/Zoom/arrow strip with one compact secondary Fine tune disclosure containing named, reflowing zoom and directional controls. Preserve the dirty-dismiss guard, picker flow, current template assignment hierarchy per D-10, local-only derivative lifecycle, cancellation, retry/error treatment, and established Profile readability treatment per D-09.</action>
  <verify>
    <automated>npx vitest run src/services/photos/background-crop-geometry.test.ts src/services/photos/background-pipeline.test.ts src/services/photos/profile-background-target.test.ts src/profile/background-manager-model.test.ts src/components/ui/sheet-contract.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>The shared Modal content root is Gesture Handler-enabled; gesture callback helpers are declared before use, are worklet-safe, and consume only shared scalar geometry during active drag/pinch; the crop page visibly contains the whole source behind a bounded aspect selection; source-coordinate save uses Task 1 geometry; compact named primary actions remain in view without a horizontal action scroller; Fine tune provides the non-drag equivalent and reports crop state textually.</acceptance_criteria>
  <done>The Profile crop editor has direct Android gesture wiring and the approved full-source selection interaction without changing unrelated overlay behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Verify physical-Pixel debug gestures and replace unsupported crop PASS claims with evidence</name>
  <precondition>A physical Pixel is connected and authorized; after `emu-connect`, `~/.local/bin/adb devices -l` resolves exactly one device target, and the `orbit` tmux session is available for Metro.</precondition>
  <files>.planning/phases/31-profile-experience/31-UAT.md, .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md, .planning/phases/31-profile-experience/evidence/31-13-debug/</files>
  <read_first>
    - AGENTS.md
    - docs/runbooks/desktop-build-pipeline.md
    - .planning/phases/31-profile-experience/31-UAT.md
    - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
    - .planning/phases/31-profile-experience/31-REVIEW-FIX.md
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/components/ui/overlay-base.tsx
    - src/services/photos/background-crop-geometry.test.ts
    - package.json
  </read_first>
  <action>First revoke rows 43 and 44's previous unsupported PASS wording, replacing it with pending direct-gesture evidence rather than treating geometry or UI tests as physical gesture proof. Follow the debug section of the desktop build runbook: synchronize the current source safely to droid, build/install `assembleDebug` for `com.bwales.orbit`, call `emu-connect`, dynamically resolve exactly one `device` serial, use the existing `orbit` tmux session for Metro, and set the matching adb reverse port. Iterate only with this debug path; this remediation does not build a release APK. On the actual physical Pixel, open a real portrait source and a real landscape source in the Profile background crop editor, then directly observe a one-finger drag and a pinch changing the visible selection rectangle without a Reanimated worklet/runtime exception; capture before/after screenshots and UI trees under `evidence/31-13-debug/`. At the owner's current font scale, inspect the crop workspace to establish that Cancel, Reset, Fine tune, and Use background are reachable without horizontal scrolling; open Fine tune and exercise its named controls plus textual state. Record the concrete observations and evidence links in UAT Test 6 and checklist rows 43–44. Mark a row PASS only after its required direct physical observation is recorded; otherwise leave it as an explicit failed or pending gap.</action>
  <verify>
    <automated>npx vitest run src/services/photos/background-crop-geometry.test.ts src/services/photos/background-pipeline.test.ts src/services/photos/profile-background-target.test.ts src/services/photos/background-storage.test.ts src/services/photos/background-reconcile-sweep.test.ts src/profile/background-manager-model.test.ts src/components/ui/sheet-contract.test.ts && npx tsc --noEmit && npm run check:colors && git diff --check</automated>
    <human-check>On the physical Pixel debug build, directly perform one-finger drag and two-finger pinch on the crop selection for real portrait and landscape sources. Inspect saved before/after screenshots rather than treating an automated test, UI tree, or named button as gesture evidence.</human-check>
  </verify>
  <acceptance_criteria>Rows 43–44 no longer rely on automated geometry/UI coverage as PASS evidence; each has direct physical-Pixel observations and linked debug artifacts for drag, pinch, source orientation, control reachability, textual Fine tune state, and absence of a gesture worklet/runtime exception, or remains an explicit unresolved result.</acceptance_criteria>
  <done>The cropper's native interaction claim is supported by the required physical-debug evidence, with no release build used during this remediation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local picker source → crop pipeline | User-selected image bytes cross into a bounded, app-owned local derivative. |
| RN Modal native root → gesture handler | Modal-local native touch delivery reaches the Profile crop detector. |
| physical Pixel observation → UAT record | Device actions and inspected artifacts become the basis for rows 43–44. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-13-01 | Information Disclosure | local background image pipeline | high | mitigate | Retain picker-to-app-owned local JPEG flow and safe relative paths; add no network, telemetry, backend, or external API path. High severity blocks completion if this boundary broadens. |
| T-31-13-02 | Denial of Service | crop geometry and image preparation | medium | mitigate | Clamp source selection and retain the 2048px long-edge derivative cap, one crop/resize/encode pass, resource release, and retryable failures. |
| T-31-13-03 | Repudiation | Android gesture acceptance | medium | mitigate | Revoke unsupported PASS claims, persist direct Pixel before/after captures and UI trees, and link row-level observations to the debug evidence. |
| T-31-13-04 | Spoofing | Modal gesture root | low | mitigate | Verify installed RNGH behavior before changing BaseOverlay and put the root at the Modal content root where Android touch dispatch occurs. |
</threat_model>

<verification>
Run the focused geometry, pipeline, target, storage/reconciliation, manager-model, and sheet-contract suites; run TypeScript, color-token, and whitespace gates. Then build and use the debug APK with Metro in tmux session `orbit`, dynamically resolve one physical Pixel serial, and require direct touch evidence before checklist PASS status.
</verification>

<success_criteria>
The Profile background cropper presents a contained full source with a source-bounded Profile-aspect selection that moves by drag and resizes by pinch; its exact source-pixel output retains current local safety contracts; named non-drag controls and textual state remain reachable; and rows 43–44 record actual debug-Pixel gesture evidence instead of unsupported PASS claims.
</success_criteria>

<output>Create `.planning/phases/31-profile-experience/31-13-SUMMARY.md` when done.</output>
