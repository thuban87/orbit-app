# Phase 31 Deferred Items

- **Pre-existing regression:** `src/db/orrery-preferences.test.ts:32` hard-codes `TARGET_VERSION === 22`; it already failed against the Phase 30 live registry at version 23 before Phase 31 and now reports 24. Ownership remains with the Phase 30 migration/preferences follow-up.
- **Pre-existing tooling drift:** `npm run check` is named by the phase plans but `package.json` has no `check` script. Phase 31-01 ran `npx tsc --noEmit`, targeted Biome, and `npm run check:colors` as the available direct checks.
- **Plan 31-10 closure blocker:** `src/navigation/widget-linking.ts` still resets `orbit://reach/<id>` to `Profile { contactId, openReachOut: true }`, but the current `ContactProfileScreen` neither consumes/clears that param nor mounts the shared `ReachOutRouter`. This violates the live ADR-074 consumed-once contract and Plan 31-10 Task 1 acceptance; it needs a narrowly scoped regression fix before Plan 10 can complete. The broader physical checklist also remains incomplete (43 of 50 rows are not PASS after crop rows 43–44 were credited with retained Pixel evidence plus explicit owner approval).
