---
phase: 31-profile-experience
plan: 15
type: execute
wave: 15
depends_on: [31-14]
files_modified:
  - src/db/profile-presentation-dao.test.ts
  - src/profile/resolve-presentation.test.ts
  - src/components/profile/ProfileBackgroundManager.tsx
  - src/components/profile/profile-background-manager.contract.test.ts
  - .planning/phases/31-profile-experience/31-UAT.md
  - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
autonomous: false
requirements: [PROF-04, PROF-05, PROF-07]
gap_closure: true
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A global background assignment can be explicitly cleared, returning every Profile without narrower overrides to its theme background after reload."
    - "A Category background assignment can be cleared without erasing that Category's independent layout axis, restoring global/theme inheritance for contacts in the Category."
    - "A contact background can explicitly inherit the current Category/global/theme result while retaining contact > Category > global > theme precedence and persistence."
  artifacts:
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/db/profile-presentation-dao.test.ts
    - src/profile/resolve-presentation.test.ts
  key_links:
    - "Clear global/category/contact UI -> existing nullable presentation assignment writers -> resolver -> BackgroundHost theme fallback"
    - "Category background clear -> preserved category layout UID -> category/global/theme background resolution"
---

<objective>
Close UAT Test 9 only: provide truthful background clear/inherit actions at global, Category, and contact scopes using existing nullable presentation assignments.

Purpose: the local data model already represents absence with nullable background-template UIDs and the resolver already falls through to theme, but the manager exposes setters without the corresponding escape hatches.

Output: covered resolver/DAO regression tests, clear/inherit controls in the Profile background manager, and only UAT Test 9 plus native checklist row 45 updated after the bounded Pixel check.

## Artifacts this phase produces

- Regression coverage for clearing each background scope while preserving the independent layout axis.
- Explicit global and Category clear actions plus truthful contact inherit action in `ProfileBackgroundManager`.
- Physical-Pixel result for the clear-to-theme flow and updated owned UAT/checklist rows.

## Source Coverage Audit

| Source | ID | In-scope item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 31 | Reusable Profile image backgrounds with hierarchy | Tasks 1–3 | COVERED |
| REQ | PROF-04 | Reusable cropped image backgrounds | Task 2 | COVERED |
| REQ | PROF-05 | Category changes affect inheritance rather than explicit overrides | Tasks 1–2 | COVERED |
| REQ | PROF-07 | Switch/reset semantics and durable presentation state | Tasks 1–3 | COVERED |
| RESEARCH | presentation resolver | Preserve contact -> Category -> global -> theme independent-axis resolution | Tasks 1–2 | COVERED |
| CONTEXT | D-09 | Backgrounds remain permitted and readable through existing Profile treatment | Task 2 | COVERED |
| CONTEXT | D-10 | Category fallback and contact-only presentation reset remain intact | Tasks 1–2 | COVERED |
| UAT | Test 9 | Clear global/Category and contact inherit return truthfully through precedence | Tasks 1–3 | COVERED |

Scope fence: no layout drag/template-discovery work, template Preview redesign, image crop changes, migration/schema change, package installation, network/API, telemetry, backend, or color-token redesign belongs in this plan.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@HANDOFF.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-profile-experience/31-CONTEXT.md
@.planning/phases/31-profile-experience/31-RESEARCH.md
@.planning/phases/31-profile-experience/31-UI-SPEC.md
@.planning/phases/31-profile-experience/31-UAT.md
@.planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
@.planning/phases/31-profile-experience/31-09-SUMMARY.md
@.planning/phases/31-profile-experience/31-11-SUMMARY.md
@.planning/phases/31-profile-experience/31-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove a global background clear falls through to the theme without changing the layout axis</name>
  <files>src/db/profile-presentation-dao.test.ts, src/profile/resolve-presentation.test.ts</files>
  <read_first>
    - src/db/profile-presentation-dao.ts
    - src/db/profile-presentation-dao.test.ts
    - src/db/profile-presentation-read.ts
    - src/profile/resolve-presentation.ts
    - src/profile/resolve-presentation.test.ts
    - src/db/migrations/profile-presentation.ts
    - src/components/ui/BackgroundHost.tsx
  </read_first>
  <behavior>
    - Clearing global `backgroundTemplateUid` while retaining global `layoutTemplateUid` resolves background to the configured theme for a contact with no narrower override.
    - Clearing a Category background retains that Category's layout UID and resolves its inheriting contacts to global, then theme when global is clear.
    - Clearing contact background is the existing explicit inherit behavior and retains its layout/freeform/collapse data; explicit narrower backgrounds continue to outrank broader scopes.
  </behavior>
  <action>Start RED with resolver and real SQLite DAO tests that construct global, Category, and contact background assignments, clear each scope through the existing nullable assignment APIs, reload `readProfilePresentationInputs`, and verify contact -> Category -> global -> theme fallthrough. Assert the sibling layout UID remains unchanged for global/Category writes and contact layout/freeform/collapse data remains unchanged for contact-background inheritance. Do not introduce a migration, table, new DAO writer, delete flow, reset widening, or background-byte cleanup: the current nullable axes, assignment writers, resolver, and template-row lifecycle already model these cases. This preserves D-10 and the existing local-only data boundary.</action>
  <verify>
    <automated>npx vitest run src/db/profile-presentation-dao.test.ts src/profile/resolve-presentation.test.ts src/db/migrations/profile-presentation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>Tests prove all three clear/inherit paths persist through a fresh presentation read; global/Category layout IDs survive background-only writes; contact/category/global precedence and final theme fallback remain exact; no migration file changes.</acceptance_criteria>
  <done>One persisted global-background clear reaches the existing theme fallback end-to-end in the local data model before UI expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add truthful clear and inherit actions to the background assignment surface</name>
  <files>src/components/profile/ProfileBackgroundManager.tsx, src/components/profile/profile-background-manager.contract.test.ts</files>
  <read_first>
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/components/profile/profile-background-manager.contract.test.ts
    - src/profile/background-manager-model.ts
    - src/db/profile-presentation-dao.ts
    - src/db/profile-presentation-read.ts
    - src/profile/resolve-presentation.ts
    - src/screens/ContactProfileScreen.tsx
    - src/theme/theme-types.ts
    - .planning/phases/31-profile-experience/31-UI-SPEC.md
  </read_first>
  <behavior>
    - The assignment surface explains and performs a global clear that uses the theme background where no contact/Category background is explicit.
    - Each Category action can clear only that Category's background and labels the resulting global/theme inheritance accurately.
    - The current Profile's contact action explicitly inherits Category/global/theme without clearing its layout, collapse, or Category membership.
    - All controls use existing theme tokens and retain current local create/delete/crop/assignment error handling.
  </behavior>
  <action>Start RED with a manager contract test for explicit global-clear, Category-clear, and contact-inherit labels/actions. Extend the existing assignment page rather than adding a second manager or Settings route: next to the global setter provide a clear action that calls `assignGlobalProfilePresentation` with the current global layout UID and null background UID; for each Category provide a clear action that first reads its independent presentation, preserves its layout UID, and writes null only for background; retain the contact action as the explicit inherit path with wording that names Category/global/theme fallback. Refresh presentation after each successful write and retain the current retry/failure state. Make control availability and confirmation copy truthful to the current value where practical, but never hide the only route to undo a broader assignment. Use existing tokens/components, local SQLite DAO calls, and D-09 readability treatment; add no schema/migration, network/API, image pipeline, or template-preview change.</action>
  <verify>
    <automated>npx vitest run src/components/profile/profile-background-manager.contract.test.ts src/profile/background-manager-model.test.ts src/db/profile-presentation-dao.test.ts src/profile/resolve-presentation.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>The background manager exposes reachable, accurately labelled global-clear and per-Category-clear controls plus a contact-inherit action; each calls the existing nullable writers without overwriting its sibling layout axis; targeted tests and color-token gate pass.</acceptance_criteria>
  <done>A user has an explicit local UI escape hatch for every applicable background scope, and it preserves the established precedence model.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Confirm clear-to-theme behavior on the authorized physical Pixel and record owned UAT evidence</name>
  <files>.planning/phases/31-profile-experience/31-UAT.md, .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/31-profile-experience/31-UAT.md
    - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
    - src/components/profile/ProfileBackgroundManager.tsx
    - src/db/profile-presentation-dao.test.ts
    - src/profile/resolve-presentation.test.ts
  </read_first>
  <what-built>Targeted automated proof of nullable background-axis resolution plus global/category clear and contact-inherit controls in the existing Profile background manager.</what-built>
  <how-to-verify>On a debug build on the authorized physical Pixel, choose an existing saved background and set it as global. Confirm an unrelated Profile without a narrower assignment shows it. Return to Background, use the global clear action, leave/reopen that unrelated Profile, and confirm it shows the active theme background rather than the saved image. Where safely available, set then clear one Category assignment and use the contact inherit action, confirming contact > Category > global > theme precedence after each step. The executor updates only UAT Test 9 and checklist row 45 with the observed result/evidence; use a temporary contact/category or restore prior presentation afterwards.</how-to-verify>
  <resume-signal>Reply `approved` with any failed observation after completing the listed Pixel checks.</resume-signal>
  <verify>
    <automated>npx vitest run src/components/profile/profile-background-manager.contract.test.ts src/profile/background-manager-model.test.ts src/db/profile-presentation-dao.test.ts src/profile/resolve-presentation.test.ts src/db/migrations/profile-presentation.test.ts && npx tsc --noEmit && npm run check:colors && git diff --check</automated>
    <human-check>Physical rendering of the clear action's resulting theme background is the sole checkpoint; resolver/DAO persistence and precedence are already covered by targeted automated tests.</human-check>
  </verify>
  <acceptance_criteria>UAT Test 9 and checklist row 45 record an actual temporary-data Pixel observation of global clear to theme and applicable Category/contact fallback, or explicitly remain unresolved; no release APK is built for this iterative check.</acceptance_criteria>
  <done>The global-background escape hatch is physically verified without expanding beyond the owned assignment hierarchy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| background assignment UI -> SQLite presentation writes | A local control clears or changes one nullable assignment axis. |
| presentation resolver -> Profile renderer | Stored UIDs select contact, Category, global, or theme visual presentation. |
| physical Pixel observation -> UAT record | A temporary visible hierarchy change is recorded as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-15-01 | Tampering | global/category clear writer | high | mitigate | Preserve the sibling layout UID on every background-only nullable write; real DAO/readback tests block completion if it changes. |
| T-31-15-02 | Tampering | contact inherit | high | mitigate | Reuse the existing contact background writer and assert it retains layout/freeform/collapse data. High severity blocks completion if reset scope or unrelated contact data changes. |
| T-31-15-03 | Spoofing | assignment labels | medium | mitigate | State whether the action yields Category, global, or theme inheritance and show no claim that an image remains assigned after clear. |
| T-31-15-04 | Information Disclosure | background manager | high | mitigate | Retain local device media and SQLite-only paths; add no network, telemetry, backend, or external API path. High severity blocks completion if egress is introduced. |
| T-31-15-05 | Repudiation | UAT result | low | mitigate | Record only observed temporary-data Pixel results in UAT Test 9 and checklist row 45. |
</threat_model>

<verification>
Run focused manager, resolver, DAO, migration, TypeScript, token-color, and whitespace gates before the single Pixel clear-to-theme checkpoint. Use a debug build for iteration; a release build is outside this gap closure unless separately required at final handoff.
</verification>

<success_criteria>
Global and Category background assignments each have a truthful clear route, contact has a truthful inherit route, and fresh local reads retain contact > Category > global > theme precedence with every sibling presentation axis and unrelated contact data preserved.
</success_criteria>

<output>Create `.planning/phases/31-profile-experience/31-15-SUMMARY.md` when done.</output>
