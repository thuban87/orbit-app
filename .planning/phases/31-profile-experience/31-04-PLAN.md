---
phase: 31-profile-experience
plan: 04
type: execute
wave: 4
depends_on: [31-03]
files_modified: [src/db/profile-read.ts, src/db/profile-knowledge-read.ts, src/db/profile-knowledge-read.test.ts, src/db/profile-history-read.ts, src/db/profile-history-read.test.ts, src/db/contact-methods-read.test.ts]
autonomous: true
requirements: [PROF-01, PROF-08, PROF-10, PROF-11, PROF-13, PROF-14, PROF-16, PROF-17, PROF-18]
estimate: { tokens: 54000, raw_tokens: 54000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "One read snapshot returns identity, resolved presentation inputs, metrics inputs, complete methods, semantic knowledge groups, and bounded interim history without a network call."
    - "Knowledge stays typed by owner; hidden beats pinned; Off Limits comes from a narrow contact-bound query and remains excluded from ranked/search/AI projections per D-08."
    - "Grouped custom fields preserve field_group and raw invalid values/history identities; History returns at most three latest rows behind the D-11 seam."
  artifacts: [src/db/profile-read.ts, src/db/profile-knowledge-read.ts, src/db/profile-history-read.ts]
  key_links: ["inReadSnapshot → owner-specific readers → renderer-neutral ProfileSnapshot"]
  prohibitions:
    - "Never inline SQL in components or flatten memories, relationships, custom fields, fuel, and current-state into one generic item table."
    - "Never alter RANKED_FUEL_EXCLUSIONS or infer Off Limits AI permission."
---
<objective>
Build the coherent local Profile aggregate and its bounded semantic projections.
Purpose: screen rendering must observe one database revision without leaking storage details into components.
Output: Profile snapshot, knowledge projection, complete method coverage, and interim history reader.
## Artifacts this phase produces
- `ProfileSnapshot`, `readProfileSnapshot`
- `readProfileKnowledge`, `readProfileOffLimits`
- `readProfileHistory` with hard latest-three bound
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/31-profile-experience/31-02-SUMMARY.md
@.planning/phases/31-profile-experience/31-03-SUMMARY.md
@.planning/phases/31-profile-experience/31-COVERAGE.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Semantic knowledge and complete contact-method projections</name>
  <files>src/db/profile-knowledge-read.ts, src/db/profile-knowledge-read.test.ts, src/db/contact-methods-read.test.ts</files>
  <read_first>
    - src/db/memories-read.ts
    - src/db/memories-read.test.ts
    - src/db/relationships-read.ts
    - src/db/relationships-read.test.ts
    - src/db/fuel-read.ts
    - src/db/fuel-read.test.ts
    - src/db/value-history-dao.ts
    - src/db/contact-methods-read.ts
    - src/db/contact-methods-read.test.ts
    - src/db/migrations/018-custom-field-scope-history.ts
    - docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
    - docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md
  </read_first>
  <behavior>
    - Pinned references dedupe; hidden items never render normally; Show hidden is available only when needed.
    - Current-state, relationships, Memories, grouped typed custom fields including invalid raw values/value-history identity, imported notes, and Off Limits retain distinct models.
    - All method rows remain deterministic/readable; only validated actionable rows drive Call/Message.
  </behavior>
  <action>Compose existing ReadOnlyExecutor readers without changing their ownership. Add a dedicated `contact_id = ? AND kind = 'off_limits'` Profile query and preserve RANKED_FUEL_EXCLUSIONS byte-for-byte. Extend custom-field projection with `field_group`, parser/error state, and the `custom_field_value_history` key needed by the Phase 24.2 deferred backlist viewer. Extend method tests for complete groups, malformed values, primaries, and empty sets.</action>
  <verify><automated>npx vitest run src/db/profile-knowledge-read.test.ts src/db/contact-methods-read.test.ts</automated></verify>
  <acceptance_criteria>Tests prove source boundaries, caps/counts, hidden/pinned rules, group order, raw invalid values, history keys, Off Limits isolation, and method actionability.</acceptance_criteria>
  <done>All Profile knowledge and method data is truthful and renderer-neutral.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bounded interim Interaction History projection</name>
  <files>src/db/profile-history-read.ts, src/db/profile-history-read.test.ts</files>
  <read_first>
    - src/db/timeline-read.ts
    - src/db/timeline-read.test.ts
    - src/db/events-dao.ts
    - src/db/recency-dao.ts
    - src/db/memory-registry.ts
    - docs/dossier/milestone-2/phase-10-profile-experience-dossier.md
  </read_first>
  <behavior>
    - Zero rows return a useful summary; populated reads return last-contact summary and at most three latest deterministic entries.
    - Stable semantic `interaction-history` identity remains independent of the current renderer implementation.
  </behavior>
  <action>Create a parameter-bound ReadOnlyExecutor query with deterministic date/id ordering and hard limit three. Return only the interim summary/preview fields and View-all route intent; do not add heatmap bins, date drills, final filters, or editing per D-11.</action>
  <verify><automated>npx vitest run src/db/profile-history-read.test.ts</automated></verify>
  <acceptance_criteria>Tests prove zero/one/many, cap, deterministic tie ordering, full-text exclusion from summary, and stable module identity.</acceptance_criteria>
  <done>Phase 32 can replace the renderer without a persistence migration.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Coherent local Profile snapshot</name>
  <files>src/db/profile-read.ts, src/db/profile-knowledge-read.test.ts, src/db/profile-history-read.test.ts</files>
  <read_first>
    - src/db/transaction.ts
    - src/screens/ContactProfileScreen.tsx
    - src/db/systems-catalog-read.ts
    - src/db/memories-read.ts
    - src/db/timeline-read.ts
    - src/db/impact-read.ts
    - src/db/contact-status-read.ts
    - src/db/contact-methods-read.ts
  </read_first>
  <behavior>
    - One inReadSnapshot observes one coherent revision and passes its ReadOnlyExecutor to every child reader.
    - Missing contact is distinct from section-local empty/error; no network dependency is introduced.
  </behavior>
  <action>Replace the current screen-local Promise.all inventory with `readProfileSnapshot`, but keep it renderer-neutral and do not edit the screen in this plan. Pass the same `ro` to identity, presentation, metrics inputs, methods, knowledge, and history readers. Include independent section error/result types so partial local data does not masquerade as a missing contact.</action>
  <verify><automated>npx vitest run src/db/profile-knowledge-read.test.ts src/db/profile-history-read.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Tests/mocks assert one BEGIN/COMMIT, shared read-only executor use, rollback on error, and no fetch/network import.</acceptance_criteria>
  <done>A single local snapshot powers the entire Profile.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite rows → semantic Profile snapshot | sensitive owner data crosses into a broad read surface |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-09 | Information Disclosure | Off Limits | high | mitigate | narrow bound reader; projection exclusions regression-tested |
| T-31-10 | Tampering | dynamic SQL | high | mitigate | bound contact IDs and closed order fragments only |
| T-31-11 | Information Disclosure | Profile load | high | mitigate | local SQLite only; no fetch/network imports |
</threat_model>
<verification>Targeted Vitest followed by `npm test && npm run check`.</verification>
<success_criteria>A coherent, bounded, local-only snapshot supplies every renderer while preserving data-owner boundaries.</success_criteria>
<output>Create `.planning/phases/31-profile-experience/31-04-SUMMARY.md` when done.</output>
