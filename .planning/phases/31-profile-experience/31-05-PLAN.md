---
phase: 31-profile-experience
plan: 05
type: execute
wave: 5
depends_on: [31-04]
files_modified: [src/profile/pack-overview.ts, src/profile/pack-overview.test.ts, src/profile/module-registry.test.ts, src/components/profile/ProfileHero.tsx, src/components/profile/RelationshipOverview.tsx, src/components/profile/ProfileModuleHost.tsx]
autonomous: true
requirements: [PROF-01, PROF-08, PROF-09, PROF-10, PROF-11, PROF-12, PROF-18, PROF-19, PROF-20]
estimate: { tokens: 52000, raw_tokens: 52000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Fixed Hero always renders identity, Favorite, Message, Call, and one overflow; method failures disable actions with accessible reasons and no geometry shift."
    - "Six Overview modules auto-pack row-major from measured width and declared Compact/Wide spans; text, not color/size/bars alone, communicates state."
    - "ProfileModuleHost renders closed semantic IDs including a replaceable History slot and collapsed useful empty summaries."
  artifacts: [src/components/profile/ProfileHero.tsx, src/components/profile/RelationshipOverview.tsx, src/components/profile/ProfileModuleHost.tsx, src/profile/pack-overview.ts]
  key_links: ["ProfileSnapshot + resolved layout → module registry → renderer host"]
  prohibitions:
    - "Never add raw text glyph controls, hardcoded colors, absolute portrait positions, or per-frame React state animation."
    - "Never add a Profile AI-draft action per D-04/ADR-079."
---
<objective>
Build the invariant Hero, responsive Overview, and semantic module host.
Purpose: establish the presentation spine before knowledge and customization expansion.
Output: production Profile primitives driven only by the aggregate snapshot and resolved presentation.
## Artifacts this phase produces
- `packOverviewModules`, `ProfileHero`, `RelationshipOverview`, `ProfileModuleHost`
- stable `PROFILE_MODULE_RENDERERS` seam including interim History
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/31-profile-experience/31-04-SUMMARY.md
@.planning/phases/31-profile-experience/31-UI-SPEC.md
@.planning/phases/31-profile-experience/31-COVERAGE.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Deterministic responsive Overview packing and registry seam</name>
  <files>src/profile/pack-overview.ts, src/profile/pack-overview.test.ts, src/profile/module-registry.test.ts</files>
  <read_first>
    - src/profile/module-registry.ts
    - src/logic/orrery-reorder-logic.ts
    - src/logic/orrery-reorder-logic.test.ts
    - src/theme/tokens.ts
    - .planning/phases/31-profile-experience/31-UI-SPEC.md
  </read_first>
  <behavior>
    - Stable row-major order, no overlap, no avoidable holes, valid declared spans only, and column reduction before clipping.
    - Renderer lookup covers every top-level/child semantic ID and permits History renderer replacement without changing layout JSON.
  </behavior>
  <action>Implement node-pure width/column packing using measured container width and font-scale-aware minimum tile width. Keep order stable on ties and expose positions only as render results, never persisted x/y. Add a closed renderer-key lookup test; renderer components remain injected so the registry stays node-pure.</action>
  <verify><automated>npx vitest run src/profile/pack-overview.test.ts src/profile/module-registry.test.ts</automated></verify>
  <acceptance_criteria>Tests cover empty/single/mixed Compact/Wide layouts, exact-fit adjacency, narrow/large-text column reduction, and replaceable History lookup.</acceptance_criteria>
  <done>Overview packing and renderer identity are deterministic and accessible-layout-safe.</done>
</task>
<task type="auto">
  <name>Task 2: Fixed Hero and Overview tiles</name>
  <files>src/components/profile/ProfileHero.tsx, src/components/profile/RelationshipOverview.tsx</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx
    - src/screens/contact-profile-logic.ts
    - src/components/ui/Icon.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/GlassSurface.tsx
    - src/components/Avatar.tsx
    - src/services/profile-metrics.ts
    - src/db/contact-methods-read.ts
    - docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
  </read_first>
  <action>Extract a fixed Hero using themed initials/photo, name/Category reflow, Favorite, stable Message/Call controls, and the existing single header overflow. Keep disabled controls focus semantics/reasons discoverable. Implement Status/Gravity/Intensity/Last Interaction/Frequency/Snooze tiles with literal text, declared sizes, pending-only-mutating-tile state, failure retention/Retry, explanation/selector sheet intents, and public Plan 03 writers. Preserve Message → Compose and omit AI draft per D-04. Use semantic icons/tokens and shared press/reduced-motion behavior.</action>
  <verify><automated>npx vitest run src/profile/pack-overview.test.ts src/services/profile-metrics.test.ts src/db/profile-relationship-actions.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Static source assertions find semantic Icon/Button/AppText/token use and no raw glyph control, hardcoded color, interval non-null assertion, or direct Core import; targeted tests/check pass.</acceptance_criteria>
  <done>Hero and Overview provide the release behavior without owning screen loading/navigation.</done>
</task>
<task type="auto">
  <name>Task 3: Semantic module host and empty/history renderers</name>
  <files>src/components/profile/ProfileModuleHost.tsx, src/profile/module-registry.test.ts</files>
  <read_first>
    - src/components/profile/ProfileModuleHost.tsx
    - src/components/ui/GlassSurface.tsx
    - src/components/ui/Sheet.tsx
    - src/profile/module-registry.ts
    - src/db/profile-history-read.ts
    - src/navigation/types.ts
  </read_first>
  <action>Render top-level modules from resolved semantic IDs, respecting visibility and per-contact collapse overrides. Every enabled empty module stays present/collapsed with approved summaries and accessible expanded/collapsed state. Add interim History summary/latest-three/View all renderer under its registry key, with a replaceable component map rather than a switch embedded in persistence.</action>
  <verify><automated>npx vitest run src/profile/module-registry.test.ts src/db/profile-history-read.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Registry test proves all IDs resolve, unknown IDs cannot render, History replacement preserves ID/layout, and enabled-empty summaries exist.</acceptance_criteria>
  <done>The modular body spine is ready for knowledge and methods renderers.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| snapshot → interactive UI | local data becomes actions and accessible descriptions |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-12 | Tampering | tile actions | high | mitigate | public composed writers; publish after success |
| T-31-13 | Denial of Service | responsive grid | medium | mitigate | pure deterministic packer and width/font tests |
| T-31-14 | Spoofing | disabled action affordance | low | mitigate | explicit disabled state and readable reason |
</threat_model>
<verification>Targeted tests and check, then full wave regression.</verification>
<success_criteria>Hero, Overview, and module host satisfy PROF-01/08–12/18–20 without monolithic storage or navigation coupling.</success_criteria>
<output>Create `.planning/phases/31-profile-experience/31-05-SUMMARY.md` when done.</output>
