---
phase: 31-profile-experience
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified: [src/db/profile-presentation-read.ts, src/db/profile-presentation-dao.ts, src/db/profile-presentation-dao.test.ts]
autonomous: true
requirements: [PROF-03, PROF-04, PROF-05, PROF-07]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Template CRUD and global/Category/contact assignments are parameter-bound, UID-addressed, and revisioned once per committed public write."
    - "Layout switch clears contact collapse overrides atomically; Reset Profile Presentation deletes only contact presentation state per D-10."
    - "Deleting an in-use template clears direct references atomically so inherited users fall through; contact data and explicit opposite-axis overrides survive."
  artifacts: [src/db/profile-presentation-read.ts, src/db/profile-presentation-dao.ts]
  key_links: ["profile-presentation-dao writes migration-024 tables → profile-presentation-read supplies resolveProfilePresentation"]
  prohibitions:
    - "Never copy a Category/global effective template into a contact row."
    - "Never nest an inWriteTransaction call or delete app-owned image bytes before DB commit."
---
<objective>
Implement durable presentation reads and atomic mutation/fallout APIs.
Purpose: UI plans need one safe boundary for template, assignment, switch, collapse, delete, and reset behavior.
Output: `profile-presentation-read` and public/core presentation DAO APIs with integration tests.

## Artifacts this phase produces
- `readProfilePresentationInputs`, template/assignment list and usage-count readers
- layout/background template CRUD, assignment, freeform/collapse, switch, deletion-fallout, and reset APIs
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/31-profile-experience/31-01-SUMMARY.md
@.planning/phases/31-profile-experience/31-COVERAGE.md
@docs/dossier/milestone-2/phase-10-profile-experience-dossier.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Presentation read model and usage/fallthrough queries</name>
  <files>src/db/profile-presentation-read.ts, src/db/profile-presentation-dao.test.ts</files>
  <read_first>
    - src/db/profile-presentation-read.ts
    - src/db/profile-presentation-dao.test.ts
    - src/db/systems-catalog-read.ts
    - src/db/transaction.ts
    - src/db/migrations/024-profile-presentation.ts
    - src/profile/resolve-presentation.ts
    - src/db/categories-dao.ts
  </read_first>
  <behavior>
    - Read inputs distinguish inherited versus explicit contact layout/background and return missing refs for pure resolver fallthrough.
    - Usage counts cover global, Category, and contact assignment scopes without counting freeform layout as a template.
  </behavior>
  <action>Implement ReadOnlyExecutor-compatible, parameter-bound readers for definitions, settings assignments, Category assignments, contact presentation/collapse data, and usage counts. Preserve layout/background independence and return renderer-neutral rows. Run coherent consumers inside an existing `inReadSnapshot`; do not open nested snapshots.</action>
  <verify><automated>npx vitest run src/db/profile-presentation-dao.test.ts</automated></verify>
  <acceptance_criteria>Tests prove all four precedence sources, missing-ref fallthrough inputs, inherited/override labeling, and usage counts.</acceptance_criteria>
  <done>Resolvers can consume complete durable inputs without UI-owned SQL.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Atomic template, assignment, switch, collapse, delete, and reset writers</name>
  <files>src/db/profile-presentation-dao.ts, src/db/profile-presentation-dao.test.ts</files>
  <read_first>
    - src/db/profile-presentation-dao.ts
    - src/db/profile-presentation-dao.test.ts
    - src/db/systems-dao.ts
    - src/db/transaction.ts
    - src/db/data-revision-dao.ts
    - src/profile/presentation-schema.ts
    - src/db/contacts-dao.ts
  </read_first>
  <behavior>
    - Repeated creates/assignments respect case-insensitive uniqueness/one assignment; failed multi-row operations roll back.
    - Template edit updates assigned readers; freeform contact snapshots remain unchanged.
    - Category assignment removal/deletion and in-use template deletion fall through without materialization.
    - Reset preserves contacts, Favorite, Snooze, AI permission, memories, custom-field values, relationships, fuel, and interactions.
  </behavior>
  <action>Create non-mutexed `*Core` functions and one public outer-transaction wrapper per operation. Canonicalize layout before writes, bind every runtime value, guard affected-row counts, bump data revision once, and implement D-10 fallout/reset exactly. Return background paths eligible for best-effort deletion after commit; never perform file I/O inside the DB transaction.</action>
  <verify><automated>npx vitest run src/db/profile-presentation-dao.test.ts && npm run check</automated></verify>
  <acceptance_criteria>Integration tests cover commit/rollback, idempotency, assignment hierarchy, Category/template deletion fallout, collapse clearing, presentation-only reset, and non-nested transaction composition.</acceptance_criteria>
  <done>All presentation mutations are safe public APIs for the UI.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI/domain input → DAO | names, UIDs, layouts, and assignment scopes become durable |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-04 | Tampering | reset/delete writers | high | mitigate | narrow rows, one transaction, protected-table assertions |
| T-31-05 | Denial of Service | transaction mutex | high | mitigate | public wrapper + internal core composition only |
| T-31-06 | Tampering | SQL | medium | mitigate | bound values and closed column/order constants |
</threat_model>
<verification>Targeted Vitest, then `npm test && npm run check` at Wave 2 boundary.</verification>
<success_criteria>Every durable mutation is atomic and the resolver receives truthful non-materialized hierarchy inputs.</success_criteria>
<output>Create `.planning/phases/31-profile-experience/31-02-SUMMARY.md` when done.</output>
