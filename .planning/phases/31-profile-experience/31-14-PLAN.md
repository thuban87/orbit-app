---
phase: 31-profile-experience
plan: 14
type: execute
wave: 14
depends_on: [31-13]
files_modified:
  - src/components/profile/ProfileLayoutEditor.tsx
  - src/components/profile/ProfileTemplateManager.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/db/picker-read.ts
  - src/components/profile/profile-layout-editor.contract.test.ts
  - src/components/profile/profile-template-manager.contract.test.ts
  - src/db/profile-presentation-dao.test.ts
  - .planning/phases/31-profile-experience/31-UAT.md
  - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
autonomous: false
requirements: [PROF-02, PROF-03, PROF-06, PROF-20]
gap_closure: true
estimate:
  tokens: 36000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A Profile editor user can press, drag, and release a section to place it at the released position within its owning group; named Move up and Move down controls remain equivalent accessible fallbacks."
    - "A layout template saved while viewing one contact is discoverable from every other contact's Profile Layout entry."
    - "The template assignment page can explicitly choose any eligible individual contact, and the selected contact receives a durable contact-template override without changing its category, background axis, or unrelated contact data."
  artifacts:
    - src/components/profile/ProfileLayoutEditor.tsx
    - src/components/profile/ProfileTemplateManager.tsx
    - src/db/picker-read.ts
    - src/db/profile-presentation-dao.test.ts
  key_links:
    - "Reorderable drag release -> closed layout-editor reducer reorder action -> canonical draft -> existing Save layout transaction"
    - "Profile Layout chooser -> shared ProfileTemplateManager -> local contact picker -> assignContactLayoutTemplate -> contact > Category > global resolver"
---

<objective>
Close UAT Tests 7 and 8 only: restore direct layout drag/release and make the existing shared layout-template library discoverable from every Profile with arbitrary individual-contact assignment.

Purpose: reusable layouts are already durable local entities, but their UI entry and individual-assignment target are incorrectly scoped to the current Profile. Direct drag must use the installed, owner-approved reorder control rather than the non-functional responder shim.

Output: real release-to-reorder wiring; a shared template-library entry in every layout chooser; an existing local contact picker flow for template assignment; targeted tests; and only the owned UAT/checklist records.

## Artifacts this phase produces

- Direct-drag layout reorder integration in `ProfileLayoutEditor` backed by the existing closed reducer.
- Universal template-library access from `Profile Layout`, plus individual-target selection in `ProfileTemplateManager`.
- Tests that prove drag/reducer parity, library reachability, arbitrary contact selection, and axis-safe durable assignment.
- Updated UAT Tests 7–8 and native checklist rows 36 and 39 after their implementation and physical observation.

## Source Coverage Audit

| Source | ID | In-scope item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 31 | Modular Profile body with reusable layout customization | Tasks 1–3 | COVERED |
| REQ | PROF-02 | Constrained top-level and child customization | Task 1 | COVERED |
| REQ | PROF-03 | Reusable layout templates and inheritance | Task 2 | COVERED |
| REQ | PROF-06 | Focused Save/Cancel layout editor | Task 1 | COVERED |
| REQ | PROF-20 | Non-precision reordering and textual state | Tasks 1 and 3 | COVERED |
| RESEARCH | installed reorder control | Use the already-audited `react-native-reorderable-list` release callback instead of a new dependency | Task 1 | COVERED |
| CONTEXT | D-07 | Durable templates, contact overrides, and collapse behavior use existing presentation storage | Task 2 | COVERED |
| CONTEXT | D-10 | Contact overrides outrank inherited Category/global layout; reset boundary remains presentation-only | Task 2 | COVERED |
| UAT | Tests 7–8 | Direct release drag; cross-Profile discovery and arbitrary individual assignment | Tasks 1–3 | COVERED |

Scope fence: UAT Test 9 belongs exclusively to Plan 31-15. The unattractive template Preview is explicitly deferred: do not redesign it unless it is functionally unusable. Do not add a schema/migration, package install, network/API, telemetry, backend, color literal, or unrelated Profile redesign.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@HANDOFF.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-profile-experience/31-CONTEXT.md
@.planning/phases/31-profile-experience/31-RESEARCH.md
@.planning/phases/31-profile-experience/31-PATTERNS.md
@.planning/phases/31-profile-experience/31-UI-SPEC.md
@.planning/phases/31-profile-experience/31-UAT.md
@.planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
@.planning/phases/31-profile-experience/31-07-SUMMARY.md
@.planning/phases/31-profile-experience/31-08-SUMMARY.md
@.planning/phases/31-profile-experience/31-11-SUMMARY.md
@.planning/phases/31-profile-experience/31-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one top-level layout section move by direct drag and release</name>
  <files>src/components/profile/ProfileLayoutEditor.tsx, src/components/profile/profile-layout-editor.contract.test.ts, src/profile/layout-editor-reducer.test.ts</files>
  <read_first>
    - src/components/profile/ProfileLayoutEditor.tsx
    - src/profile/layout-editor-reducer.ts
    - src/profile/layout-editor-reducer.test.ts
    - src/profile/layout-editor-session.ts
    - src/profile/module-registry.ts
    - src/components/ui/Sheet.tsx
    - node_modules/react-native-reorderable-list/README.md
    - node_modules/react-native-reorderable-list/src/types/props.ts
    - .planning/phases/31-profile-experience/31-UAT.md
  </read_first>
  <behavior>
    - Releasing a drag of an item over another legal position dispatches the same `reorder` action and produces the same canonical draft as the existing named Move controls.
    - The three parent buckets remain independent: a release cannot place a module under a different parent or bypass reducer validation.
    - Drag activation does not remove the 44dp labelled Move up/Move down fallback, visibility, default-expansion, size, dirty-dismiss, preview, or Save layout behavior.
  </behavior>
  <action>Start RED with a component-contract test for a release callback wired to the closed `reorder` reducer action and reducer cases that compare direct-drop ordering with named moves. Replace the `PanResponder` handle shim with the installed owner-approved `react-native-reorderable-list` pattern appropriate for the editor's three scrollable buckets: use the package drag handle/hook and `onReorder({from,to})` to dispatch `{type:"reorder", parent, id, toIndex}` only after release. Preserve ScrollView-compatible container and long-press activation as documented by the installed package, so ordinary editor scrolling stays possible. Keep each bucket keyed by semantic module ID and use its declared parent; never mutate draft arrays outside `profileLayoutEditorReducer`. Retain labelled named move buttons as the equivalent non-precision fallback, and retain all existing theme-token styling. This implements D-07 without changing existing presentation schema, migration, persistence DAO, or network behavior.</action>
  <verify>
    <automated>npx vitest run src/profile/layout-editor-reducer.test.ts src/profile/layout-editor-session.test.ts src/components/profile/profile-layout-editor.contract.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>A release callback names the correct bucket, item ID, and release index; reducer tests prove direct release ordering is byte-equivalent to valid named moves and reject parent crossings/out-of-range releases; all named Move controls remain rendered and accessible.</acceptance_criteria>
  <done>One actual Profile section can be directly dragged and released into a legal new position through the same canonical reducer path used by Save layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the shared template library and assign a template to a chosen contact</name>
  <files>src/components/profile/ProfileLayoutEditor.tsx, src/components/profile/ProfileTemplateManager.tsx, src/screens/ContactProfileScreen.tsx, src/db/picker-read.ts, src/components/profile/profile-template-manager.contract.test.ts, src/db/profile-presentation-dao.test.ts</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx
    - src/screens/contact-profile-logic.ts
    - src/components/profile/ProfileLayoutEditor.tsx
    - src/components/profile/ProfileTemplateManager.tsx
    - src/profile/template-manager-model.ts
    - src/db/picker-read.ts
    - src/components/ContactPicker.tsx
    - src/db/profile-presentation-read.ts
    - src/db/profile-presentation-dao.ts
    - src/db/profile-presentation-dao.test.ts
    - src/profile/resolve-presentation.ts
    - src/profile/resolve-presentation.test.ts
  </read_first>
  <behavior>
    - Opening Profile Layout for a contact that merely inherits factory, global, or Category layout exposes the existing reusable template library as well as Edit layout.
    - A template created from David is listed from Kylie's Profile without copying it into either contact's freeform layout.
    - Assignment chooses an arbitrary non-archived local contact and writes only that contact's layout-template axis; the chosen contact's background, category, and unrelated contact fields are retained.
    - Selecting inherited layout for the host contact keeps its current behavior; it is never silently applied to the arbitrary selected contact.
  </behavior>
  <action>Start RED with DAO and component-contract cases for a template created under one Profile becoming visible from another inherited Profile and being applied to a separately selected contact. Add a `ProfileLayoutEditor` callback/button that opens the canonical `ProfileTemplateManager` from every chooser state; wire it in `ContactProfileScreen` without adding a second overflow route. In the manager's assignment page, retain global and Category writes, then add an explicit local contact selector powered by the existing `picker-read`/ContactPicker selection conventions (non-archived contact rows, bound parameters, deterministic display). After a user picks a contact, invoke the existing `assignContactLayoutTemplate` with that selected ID and refresh manager usage and the current screen. Preserve the manager's host-contact-only `Use inherited layout` affordance for that host, with copy that makes the target scope truthful; do not infer an inherited choice for another contact. Keep independent background-axis reads/writes intact per D-10, existing collapse-clearing-on-explicit-layout-switch behavior, category-deletion fallback, SQL parameter binding, local-first reads, and no schema/migration/API additions.</action>
  <verify>
    <automated>npx vitest run src/db/profile-presentation-dao.test.ts src/profile/resolve-presentation.test.ts src/profile/template-manager-model.test.ts src/components/profile/profile-template-manager.contract.test.ts src/screens/contact-profile-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>Every layout chooser has a canonical template-library entry; the template manager lists durable shared rows regardless of host source; selecting a second contact persists only that contact's template UID, clears only its collapse overrides as an explicit layout switch requires, and preserves background/category/contact facts; no migration is altered or introduced.</acceptance_criteria>
  <done>Reusable layout templates are discoverable from every Profile and can be assigned individually to any selected local contact.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Confirm the direct-drag interaction on the physical Pixel and record owned UAT results</name>
  <files>.planning/phases/31-profile-experience/31-UAT.md, .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/31-profile-experience/31-UAT.md
    - .planning/phases/31-profile-experience/31-NATIVE-CHECKLIST.md
    - src/components/profile/ProfileLayoutEditor.tsx
    - src/components/profile/ProfileTemplateManager.tsx
    - src/db/profile-presentation-dao.test.ts
  </read_first>
  <what-built>Direct release drag for layout sections, all-Profile access to the shared layout-template manager, and selected-contact assignment with targeted automated coverage.</what-built>
  <how-to-verify>On the authorized physical Pixel using a debug build, open any Profile → overflow → Profile Layout → Edit layout. Long-press a drag handle, move a section over a different legal position, release, and confirm the order changes before Save; also confirm its named Move buttons still work. Then open another Profile's Profile Layout, enter the template library, find a template created from the first Profile, choose Assign, select a different individual contact, and confirm that target resolves to the template without changing its background. The executor updates UAT Tests 7–8 and checklist rows 36 and 39 only with the observed result and evidence; Preview appearance remains deferred.</how-to-verify>
  <resume-signal>Reply `approved` with any failed observation after completing the listed Pixel checks.</resume-signal>
  <verify>
    <automated>npx vitest run src/profile/layout-editor-reducer.test.ts src/profile/layout-editor-session.test.ts src/components/profile/profile-layout-editor.contract.test.ts src/db/profile-presentation-dao.test.ts src/profile/resolve-presentation.test.ts src/profile/template-manager-model.test.ts src/components/profile/profile-template-manager.contract.test.ts && npx tsc --noEmit && npm run check:colors && git diff --check</automated>
    <human-check>Only direct physical press-drag-release behavior needs this checkpoint; template discoverability and durable arbitrary-contact assignment have targeted automated coverage.</human-check>
  </verify>
  <acceptance_criteria>UAT Tests 7–8 and checklist rows 36 and 39 contain the actual result and evidence after the implementation; the physical check certifies direct drag/release rather than treating reducer or button tests as gesture proof.</acceptance_criteria>
  <done>The release-drag claim is backed by a bounded Pixel observation, while the template library and assignment persistence are backed by targeted automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| drag gesture -> layout reducer | Native touch release becomes a persisted presentation-order change only through validated reducer state. |
| template manager -> selected contact write | A local UI selection changes another contact's presentation axis. |
| local SQLite -> resolved Profile | Stored template IDs are resolved independently from background/category data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-14-01 | Tampering | layout drag reducer | medium | mitigate | Route release index through the existing parent-aware closed reducer; test illegal parent and boundary cases. |
| T-31-14-02 | Tampering | arbitrary contact assignment | high | mitigate | Use the selected, parameter-bound contact ID with `assignContactLayoutTemplate`; assert only the layout axis/collapse state changes. High severity blocks completion if assignment touches category, background, or contact facts. |
| T-31-14-03 | Spoofing | assignment copy | medium | mitigate | Name the selected contact and distinguish host-only inherited action from explicit contact override. |
| T-31-14-04 | Information Disclosure | template/contact selection | high | mitigate | Read only existing on-device contact data; add no network, telemetry, backend, or external API path. High severity blocks completion if data egress is introduced. |
| T-31-14-05 | Repudiation | physical drag acceptance | low | mitigate | Record the bounded Pixel result in only UAT Tests 7–8 and checklist rows 36/39 after observation. |
</threat_model>

<verification>
Run the focused reducer/session, template-manager, resolver, DAO, profile-logic, TypeScript, token-color, and whitespace gates before the one physical-Pixel gesture checkpoint. Do not build a release APK for iteration; use a debug build unless final handoff separately requires one.
</verification>

<success_criteria>
Direct drag/release reorders legal layout sections while accessible Move controls remain; all Profiles reach the same shared layout-template library; and a chosen local contact can receive a template-only override with existing precedence and local-first guarantees intact.
</success_criteria>

<output>Create `.planning/phases/31-profile-experience/31-14-SUMMARY.md` when done.</output>
