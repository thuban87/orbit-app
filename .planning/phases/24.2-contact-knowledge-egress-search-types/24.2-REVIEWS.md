---
phase: 24.2
reviewers: [codex, claude]
reviewed_at: 2026-09-04T16:55:08Z
review_cycle: 4
plans_reviewed: [24.2-01-PLAN.md, 24.2-02-PLAN.md, 24.2-03-PLAN.md, 24.2-04-PLAN.md, 24.2-05-PLAN.md, 24.2-06-PLAN.md, 24.2-07-PLAN.md]
models:
  codex: "gpt-5.6-terra (reasoning=low)"
  claude: "claude read-only subagent"
model_sources:
  codex: "banner"
  claude: "subagent"
---

# Cross-AI Plan Review — Phase 24.2 (Convergence Cycle 4)

## Consensus Summary

This is convergence cycle 4. Cycle 3 closed with 0 HIGH and 4 actionable non-HIGH findings; the plans were revised (commit `177ad2c`) to resolve them. Both independent reviewers (Codex `gpt-5.6-terra`, and a read-only Claude subagent) reviewed the code on disk, not the plan text in isolation, and **both confirm all four cycle-3 findings are genuinely RESOLVED in the named PLAN.md files** — verified against the actual source, not merely claimed.

Neither reviewer raised a HIGH concern. The plan set upholds every non-negotiable: migration 017 is forward-only and non-destructive with per-row proof-of-copy (not just count-equality) before any delete; ADR-001 pair-uniqueness is preserved and tested; no FTS5; the AI-egress boundary is a single default-OFF `allow_ai = 1` SQL predicate at the one `readPromptContext` seam; Off Limits stays a separate SQL exclusion; custom values stay raw TEXT (flag-not-coerce parsers); destructive ops snapshot in-transaction; and the value-history table gets full contact-merge and contact-purge lifecycle wiring across all three registries (tombstones-dao union, purge-dao `PURGE_CHILDREN` + `:318` delete array, merge-dao bespoke reparent).

The reviewers **diverge on one MEDIUM**, which the orchestrator corroborated against disk and holds as a genuine actionable gap (see Divergent Views). Overall risk: **LOW–MEDIUM** — the architecture and one-way-door gating are strong; one destructive-path lifecycle gap in Plan 05 should be closed before execution.

### Agreed Strengths

- **All 4 cycle-3 findings RESOLVED, verified against source (both reviewers, with file:line):**
  1. P01 confirmFuel/FuelEditor confirm-path removal is **deferred to Phase 36, not reversed** — must_haves prohibition (`24.2-01-PLAN.md:54-56`), Task 1 `<decision>` (`:134`), acceptance forbidding edits to fuel-dao.ts/ContactProfileScreen.tsx/FuelEditor.tsx (`:152`), ADR-081 consequences (`:178`), and a ROADMAP Phase 36 breadcrumb on disk (`.planning/ROADMAP.md:525`). The "inert" premise is verified: `confirmFuel` (`fuel-dao.ts:214`/`:282`) only flips `source→'manual'`, and no production `source='ai'` producer exists (only `'share'`/`'manual'` in `src/**/*.tsx`; all `'ai'` hits are tests).
  2. P01 tracer scorer reconciled — tracer ships prefix/substring + bounded ≤1 edit-distance (`24.2-01-PLAN.md:104,:212,:223`); Plan 02 owns the full multi-tier ≥6-char/distance-2 scorer (`24.2-02-PLAN.md:25`). Action/artifact/acceptance now agree internally; the prior contradiction is explicitly called out and resolved.
  3. P07 `24.2-06` added to `depends_on: [24.2-01, 24.2-05, 24.2-06]` (`24.2-07-PLAN.md:6`); matrix acyclic, still 3 waves; Plan 07 touches only `src/backup/*` and is file-disjoint from every wave-2 plan (both reviewers verified the DAG).
  4. P05 pre-018 purge availability guard gates BOTH the `KNOWLEDGE_CHILD_TABLES` map (`purge-dao.ts:96`) AND the `sqlite_master … IN(...)` list (`purge-dao.ts:107-111`), with a dedicated acceptance criterion (`24.2-05-PLAN.md:183,:199`). Orchestrator + both reviewers confirmed the on-disk guard: `isAvailablePurgeChild` (`purge-dao.ts:123-130`) returns `true` for any entity not in the map, which is exactly why a map-only add would leave the SELECT list stale — so both edits are genuinely required.
- HIGH-1 fixed at the real writer: `upsertValue` wrapper has zero non-test callers; the history append is wired into `updateContactFull` (`contacts-dao.ts:342/:412-414`), the production edit path, with a real-path test.
- Local-first upheld throughout: no new network read path; single AI-egress projection; no FTS5; ADR bridge regenerated via `gen:adr-registry` without `graph:build`.

### Agreed Concerns

- None at HIGH or MEDIUM that both reviewers share. The shared non-HIGH observations are all LOW and are either self-correcting or owner-routed (see below).

### Divergent Views

- **[MEDIUM — Codex; not surfaced by Claude] Plan 05 omits `custom_field_value_history` lifecycle on permanent custom-field-DEFINITION deletion.** Plan 05 Task 3 (`24.2-05-PLAN.md:181-184`) exhaustively handles the history table for **contact merge** and **contact purge**, and its must_haves (`:48`) claims "full lifecycle ownership." But permanent *definition* deletion goes through `dropFieldValues` (`field-ddl.ts:120-160`), which tombstones each value row (`:141-147`) and the def uid before deleting — while the new `custom_field_value_history` rows carry `field_def_id … ON DELETE CASCADE` (`24.2-05-PLAN.md:117`) and would CASCADE away **untombstoned**. This is the same bug-class the plan itself calls out for the purge path ("omitting the entity … silently drops history to FK CASCADE", `:173`), on a destructive path the plan's lifecycle enumeration does not cover. **Orchestrator corroboration:** verified against `field-ddl.ts:120-160` and Plan 05's task text — the gap is real. Codex rates MEDIUM; the orchestrator concurs it is actionable. **Fix:** extend Plan 05 Task 3 so `dropFieldValues` (permanent def deletion) tombstones + explicitly deletes `custom_field_value_history` rows for the definition before the def delete, mirroring the value-row handling, with a field-drop test asserting the history rows are tombstoned (not left to FK CASCADE).
  - *Why Claude missed it:* Claude verified the merge/purge (contact-level) lifecycle as complete across all three registries and did not separately examine the definition-deletion path for the history table. Codex's find is the different-blind-spot the cross-AI review exists to catch.

- **Off-limits declassification (Codex LOW, non-actionable):** Codex notes Plan 01 maps a hypothetical `source='ai' AND kind='off_limits'` fuel row to a general AI-off memory (`24.2-01-PLAN.md:134`), losing avoid-topic semantics. Egress safety is preserved (`allow_ai=0` default) and there is no production `source='ai'` producer, so this is a zero-row hypothetical already documented as a decision in the plan — not an unincorporated actionable finding.

- **Claude's 5 LOWs (all explicitly "no action required"):** Plan 01 citation drift (`ContactProfileScreen.tsx:354` writes `source:"manual"`, not `:654`/`'user'` — self-corrects via the re-grep acceptance at `:150`); the tracer→full-scorer coordination seam (managed, both plans acknowledge it); Plan 03 editing `FuelEditor.tsx` for Off-Limits copy only while Plan 01 defers the confirm control (file-disjoint, no conflict); value-history merge-restore being an `insert-if-missing` local-merge (not cross-device) guarantee, consistent with the existing `events` precedent; and the stale "Phase 36 owns the v4 bump" note that Plan 07 correctly routes to the owner rather than resolving. None require a PLAN.md change.

---

## Codex Review

## Summary

The seven plans are unusually thorough and materially improved from the prior cycle. They respect the migration order (017 → 018), keep AI egress SQL-gated and default-off, avoid FTS5, preserve ADR-001’s normalized custom-field uniqueness, and correctly account for the format-4 backup already landing in 24.1. I found one new material lifecycle gap around destructive custom-field definition removal.

## Strengths

- Plan 01 correctly treats migration 017 as a one-way, copy-verify-delete move, while retaining share and legacy AI content. The current schema supports its stated starting point: `memories` exists at v16 but has no `allow_ai` column yet ([016-contact-knowledge.ts](/home/bwales/projects/orbit-app/src/db/migrations/016-contact-knowledge.ts:9), [database.ts](/home/bwales/projects/orbit-app/src/db/database.ts:51)).

- Plan 01 correctly identifies the sole existing prompt-data path: `readPromptContext` reads ranked Fuel then shared fields ([ai-context-read.ts](/home/bwales/projects/orbit-app/src/db/ai-context-read.ts:190), [ai-context-read.ts](/home/bwales/projects/orbit-app/src/db/ai-context-read.ts:214), [ai-context-read.ts](/home/bwales/projects/orbit-app/src/db/ai-context-read.ts:254)). Adding a memory projection there is the right enforcement seam.

- Plan 02’s two-stage corpus/scorer design is sound. The existing custom-value reader binds definition IDs rather than interpolating identifiers ([field-values-dao.ts](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:19)), and the proposed scorer is deliberately TypeScript-only, avoiding an FTS/index reversal.

- Plan 03 preserves parent-owned persistence: `MemoryEditor` should not become a direct DAO caller, consistent with current memory writes being centralized in [memories-dao.ts](/home/bwales/projects/orbit-app/src/db/memories-dao.ts:75). Its global-AI fail-closed UI gate is appropriately distinct from SQL egress enforcement.

- Plan 04 correctly preserves the existing `photo` field type rather than deleting it merely to conform to prose that lists nine product-facing types. The current `FieldType`/parser model is exhaustive and raw-TEXT based; expanding the union and parser record is the appropriate additive route.

- Plan 05 correctly targets both actual scope-seeding writers: `createField` currently fans out to every contact ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:52)), while contact creation also seeds every definition ([contacts-dao.ts](/home/bwales/projects/orbit-app/src/db/contacts-dao.ts:198)). It also correctly avoids putting value-history behavior into `upsertValueCore`, which is used for both initial values and writes ([field-values-dao.ts](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:64)).

- Plan 06 traces the full native bridge rather than assuming a TypeScript-only change. The native module really has separate selected-picker, full-provider, and lookup-key reads ([OrbitContactPickerModule.kt](/home/bwales/projects/orbit-app/modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt:163), [OrbitContactPickerModule.kt](/home/bwales/projects/orbit-app/modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt:224), [OrbitContactPickerModule.kt](/home/bwales/projects/orbit-app/modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt:366)). It also correctly changes the real bridge contract, [index.ts](/home/bwales/projects/orbit-app/modules/orbit-contact-picker/index.ts:11), not a nonexistent types file.

- Plan 07 identifies all the currently separate backup restore registries/maps that need extending. The source confirms `restore-apply` has separate `tableOf`, tombstone, incoming-row, local-row, and replace-all inventories ([restore-apply.ts](/home/bwales/projects/orbit-app/src/backup/restore-apply.ts:50), [restore-apply.ts](/home/bwales/projects/orbit-app/src/backup/restore-apply.ts:54), [restore-apply.ts](/home/bwales/projects/orbit-app/src/backup/restore-apply.ts:243)).

## Concerns

- **MEDIUM — Plan 05 misses value-history lifecycle handling when a custom-field definition is permanently deleted.** The plan adds `custom_field_value_history` and makes it backup/tombstone-visible, but its Task 3 changes neither `field-ddl.ts` nor its destructive `dropFieldValues` core. That core currently snapshots `custom_field_values`, deletes them, then deletes the definition ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:121), [field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:138), [field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:155)). The new history rows would be removed by the planned `ON DELETE CASCADE` with no per-row tombstones, unlike current value rows which are explicitly tombstoned ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:141)). This can leave stale history rows eligible for restoration from another backup/device state and violates the plan’s stated “full lifecycle ownership” claim.

- **LOW — Plan 01 knowingly declassifies a hypothetical `source='ai' AND kind='off_limits'` row.** Its plan text explicitly maps this to a general AI-off memory ([24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:134)). Default `allow_ai=0` preserves egress safety, but the user-visible avoid-topic semantics are lost. The risk is presently limited because no shipped production writer produces `source='ai'`; the current writers shown are `user` and `share` ([ContactProfileScreen.tsx](/home/bwales/projects/orbit-app/src/screens/ContactProfileScreen.tsx:646), [CaptureScreen.tsx](/home/bwales/projects/orbit-app/src/screens/CaptureScreen.tsx:273)).

## Suggestions

- Extend Plan 05 Task 3 to update `dropFieldValues` atomically:

  - enumerate and tombstone `custom_field_value_history.uid` rows for the definition;
  - explicitly delete those rows before deleting `custom_field_defs`;
  - add a field-drop test proving history rows are deleted and their tombstones restore correctly;
  - retain the existing `field_history` snapshot behavior unchanged.

- For Plan 01, either preserve an off-limits marker in the migrated representation or explicitly document a user-visible audit/notice for this zero-expected-row fallback. The current safety guarantee is enough for AI egress, but not semantically lossless.

## Risk Assessment

**MEDIUM.** The architecture, migration sequencing, local-first posture, and testing plans are strong. The remaining concern is confined to a destructive definition-removal path, but it affects a new backup-mergeable entity and therefore should be corrected before implementation.

## Cycle-4 verdict

| Finding | Verdict | Evidence |
|---|---|---|
| P01: legacy confirm-path cleanup explicitly deferred to Phase 36 | **RESOLVED** | Plan 01 has a scope prohibition and verification boundary ([24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:54), [24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:152)), an explicit checkpoint decision ([24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:134)), and required ADR-081 consequences text ([24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:178)). The Phase 36 breadcrumb exists in the roadmap ([ROADMAP.md](/home/bwales/projects/orbit-app/.planning/ROADMAP.md:525)). Source confirms the legacy path exists today: [fuel-dao.ts](/home/bwales/projects/orbit-app/src/db/fuel-dao.ts:214), [ContactProfileScreen.tsx](/home/bwales/projects/orbit-app/src/screens/ContactProfileScreen.tsx:705), [FuelEditor.tsx](/home/bwales/projects/orbit-app/src/components/FuelEditor.tsx:251). |
| P01: tracer scorer scope reconciled | **RESOLVED** | Plan 01 consistently assigns prefix/substring plus bounded ≤1 edit distance to the tracer ([24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:104), [24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:212), [24.2-01-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-PLAN.md:223)); Plan 02 owns the ≥6-character, distance-2 tier ([24.2-02-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-02-PLAN.md:25)). |
| P07: 24.2-06 dependency added; waves remain valid/disjoint | **RESOLVED** | Plan 07 depends on 01, 05, and 06 ([24.2-07-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-07-PLAN.md:6)); its imported-memory test rationale explicitly explains why ([24.2-07-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-07-PLAN.md:145)). No reverse dependency appears, and Plan 07’s backup files do not overlap Wave-2 plans’ listed files. |
| P05: pre-018 purge availability guard covers map and `sqlite_master` query | **RESOLVED** | The plan explicitly requires both edits and both-schema tests ([24.2-05-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-05-PLAN.md:183), [24.2-05-PLAN.md](/home/bwales/projects/orbit-app/.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-05-PLAN.md:199)). Current source verifies these are the exact two necessary seams: map at [purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:96) and table-presence query at [purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:104). The current source appropriately does not yet contain the future table, since migration 018 has not been implemented. |


---

## Claude Review

## 1. Summary

Phase 24.2's seven plans are in strong shape and, verified against the code on disk, all four cycle-3 findings are genuinely resolved in the named PLAN.md files (not merely claimed) — each backed by concrete acceptance criteria and, where I could check the mechanism, by code that matches the plan's description precisely. The migration head (016 / `TARGET_VERSION=16`), `BACKUP_FORMAT_VERSION=4`, the absence of any production `source='ai'` producer, the `purge-dao.ts` availability-guard structure, the `updateContactFull` HIGH-1 append site, the dead-`upsertValue`-wrapper claim, the `merge-dao` bespoke-reparent pattern, and the `memories`/tombstone schemas all match what the plans assert. The plan set correctly upholds the non-negotiable invariants: non-destructive forward-only migrations with proof-of-copy, ADR-001 uniqueness untouched, no FTS5, single SQL egress boundary with an explicit `allow_ai=1` predicate default-OFF, Off Limits kept a separate SQL-level exclusion, raw-TEXT-forever custom values, and one-parser-per-type. Remaining concerns are LOW: minor citation drift in Plan 01, an inherent tracer→full-scorer coordination seam, and one restore-semantics edge worth an owner-aware note. **Overall risk: LOW.**

## 2. Strengths (file:line evidence)

- **Migration 017 is genuinely non-destructive with proof-of-copy, not just count-equality.** Plan 01 Task 2 (`24.2-01-PLAN.md:175-176`) requires a per-row mapping asserting each migrated memory's `value` equals its source fuel `text` before any `DELETE`, catching the "duplicated source masks a dropped one" case. The `memories` schema on disk (`016-contact-knowledge.ts:11-27`) has every column the migration inserts (`value`, `note`, `url`, `custom_label`, `provenance DEFAULT 'user'`, `deleted_at`), and `allow_ai` does **not** already exist — so the additive `ALTER TABLE` is correct (verified: `grep allow_ai src/db/migrations/` returns nothing).
- **The migration test uses a "seeded-at-16" pattern that avoids assuming a starting state** (`24.2-01-PLAN.md:179`) — it runs migrations to 16, seeds the pre-017 fixture into the real `fuel`/`memories` tables, then applies only 017. This is the correct way to test a data-move without editing shipped migrations.
- **The AI-egress boundary is a single SQL predicate, default OFF.** Plan 01 (`24.2-01-PLAN.md:208`, `:219`) exports a reusable `MEMORY_AI_ELIGIBILITY = 'allow_ai = 1 AND deleted_at IS NULL'` fragment and wires it into the sole `readPromptContext` boundary; `MemoryProvenance` on disk (`memories-dao.ts:12`) confirms the provenance values used. Egress eligibility is explicit state, never inferred from type — matching ADR-050/078.
- **HIGH-1 is fixed at the *real* writer.** `field-values-dao.ts:45` `upsertValue` has **zero** non-test callers (verified: grep returns only its definition + two doc-comments), and `updateContactFull` (`contacts-dao.ts:342`) is the production edit path with its custom-value loop at `:412-414` composing `upsertValueCore`. Plan 05 wires the value-history append there (`24.2-05-PLAN.md:179`, `:194`) and adds a real-path test driving `updateContactFull`, not the dead wrapper.
- **HIGH-2 lifecycle is complete across all three files, and the code matches.** `PURGE_CHILDREN` is an exhaustive `Record<TombstoneEntityType,…>` (`purge-dao.ts:80`), the separate hard-coded delete array exists (`purge-dao.ts:318`) guarded by `isAvailablePurgeChild`, and `TombstoneEntityType` is a closed union + exhaustive `switch` (`tombstones-dao.ts:8-21`, `:35-56`). `merge-dao.ts:181` shows the exact `field_history` bespoke-reparent precedent (created_at-only tables can't use the generic `reparent()` at `:180` which sets `modified_at`) that Plan 05 tells the executor to mirror.
- **Local-first is upheld throughout.** No plan adds a network read path; the only egress stays the AI projection; Off Limits keeps its SQL exclusion (`RANKED_FUEL_EXCLUSIONS` left untouched, `24.2-03-PLAN.md:143`, `:156`); no FTS5 (`24.2-02-PLAN.md:104`). Plan 04 keeps custom values raw TEXT with permissive flag-not-coerce parsers (`24.2-04-PLAN.md:82`, `:99`) and leaves `field-sort.ts` unchanged.
- **The ADR bridge is handled correctly.** ADR head on disk is 080 and ADR-030 exists; Plan 01 writes ADR-081 (head+1) and regenerates `adr-registry.ts` via `npm run gen:adr-registry`, explicitly **not** running `graph:build` (`24.2-01-PLAN.md:178`) — honoring build discipline.

## 3. Concerns (severity-tagged, file:line + mechanism)

- **[LOW] Plan 01 mis-cites the non-`ai` fuel producer.** `24.2-01-PLAN.md:134` states the shipped producers write "`source:'user'` (ContactProfileScreen.tsx:654)". On disk, `ContactProfileScreen.tsx:354` writes `source:"manual"` (and the `addFuel` call is at `:646`), not `source:'user'` at `:654`. **Mechanism:** citation drift only — the load-bearing claim ("no shipped producer writes `source='ai'`") is verified true (CaptureScreen writes `source:"share"`, ContactProfileScreen writes `source:"manual"`; no `'ai'` producer in `src/**/*.tsx`). Task 1's acceptance criterion (`:150`) re-greps at execution, so this self-corrects; no action required beyond awareness.
- **[LOW] Tracer→full-scorer coordination seam on a shared file.** Plan 01 creates `knowledge-search.ts`/`knowledge-search-read.ts` with a ≤1 edit-distance matcher and memory-only corpus; Plan 02 (wave 2) *rewrites* the same two files into the full multi-tier scorer + 4-source corpus + shared `tokenize()` contract (`24.2-02-PLAN.md:51`, `:96`). **Mechanism:** Plan 01's tracer tests (`knowledge-search.test.ts`) will be extended/overwritten by Plan 02; if Plan 01 ships a tokenizer shape Plan 02 must replace, there is rework risk. Both plans acknowledge the hand-off explicitly, so this is a managed seam, not a defect — but the executor of Plan 02 must treat Plan 01's scorer as a starting point to *expand*, and re-run the boundary cases (5-char dist-2 reject, 6-char dist-2 accept) that only Plan 02 asserts.
- **[LOW] Plan 03 edits `FuelEditor.tsx` while Plan 01 defers the `FuelEditor` confirm-path.** Plan 01's deferral (`24.2-01-PLAN.md:54`) keeps `files_modified` free of `FuelEditor.tsx`, but Plan 03 *does* modify `FuelEditor.tsx` (`24.2-03-PLAN.md:17`, Task 3) — for the Off-Limits avoid-topic copy only, not the Confirm/Dismiss control. **Mechanism:** no conflict (different code regions, and the two plans are file-disjoint from each other within wave 2 — verified), but the Plan 03 executor will see the dead confirm control in the same file; the plan should (and does, `:143`) leave it untouched. Worth flagging so the executor doesn't "helpfully" remove it and accidentally pull Phase 36's deferred work forward.
- **[LOW / owner-aware] Value-history restore uses `insert-if-missing`, so a post-merge reparent isn't re-applied on merge-restore.** Plan 07 registers `custom_field_value_history` with `writeMode:"insert-if-missing"` (`24.2-07-PLAN.md:116`). If a row was reparented to a survivor (Plan 05 merge) and a *stale* copy of that uid already exists in the restore destination under the old contact, merge-mode restore won't move it. **Mechanism:** this is identical to the existing `events` policy (also `insert-if-missing` with a contact parent, `reconciliation.ts:66`), so it introduces no *new* inconsistency and is consistent with precedent — but it means value-history reparenting is a local-merge guarantee, not a cross-device merge-restore guarantee. Acceptable within the append-only model; noting for completeness, no change required.
- **[LOW] Milestone-ordering staleness is surfaced but unresolved (correctly).** Plan 07 (`24.2-07-PLAN.md:61`, `:181`) flags that "Phase 36 owns the v4 bump" is now factually stale (24.1 pre-bumped to 4) and routes it to the owner rather than resolving it. This is the right call per the batch-decisions/decision-reversal rules — listing it so the aggregator carries it to the owner, not as a plan defect.

I found **no HIGH or MEDIUM concerns.** No migration edits a shipped file; no starting-state assumption; ADR-001 uniqueness is explicitly preserved and tested (`24.2-05-PLAN.md:50-52`, `:127`); no FTS5; no network on a read path; no hardcoded colours (`check:colors` gated in Plans 03/04); no Skia-from-React-state (no animation work in this phase).

## 4. Suggestions

- Fix the Plan 01 citation to `ContactProfileScreen.tsx:354` `source:"manual"` (and `addFuel` at `:646`) so a future reviewer grepping `:654`/`'user'` doesn't flag a phantom mismatch.
- In Plan 02, add one explicit line telling the executor to *diff against* the tracer's `knowledge-search.ts` rather than rewrite from scratch, preserving Plan 01's passing tracer assertions while adding the ≤2 tier — reduces the rework risk noted above.
- Consider a one-line note in Plan 03 Task 3 reminding the executor that the `confirmFuel`/Confirm-Dismiss control in `FuelEditor.tsx` is Phase-36-deferred and must be left intact, mirroring Plan 01's guard — since Plan 03 is the plan that actually opens that file.

## 5. Risk Assessment

**Overall: LOW.** The one-way-door risks (irreversible migration 017, AI egress, backup fidelity) are each gated and tested: 017 is non-destructive copy-then-remove with proof-of-copy behind a `checkpoint:decision`; egress is a single default-OFF SQL predicate with a node test flipping the column; backup round-trips both merge and replace-all plus an old-format-4 parse test. Every non-trivial code claim I checked (migration head, purge guard structure, `updateContactFull` append site, dead `upsertValue`, merge reparent precedent, tombstone union, memories schema, `ai_provider` gate source) matched disk exactly, which raises confidence that the executors' `read_first` groundwork is accurate. Dependency graph is a clean 3-wave DAG, acyclic, with wave-2 plans file-disjoint from each other and from wave-3 Plan 07 (verified programmatically). Residual risk is confined to LOW coordination seams and one owner-routed milestone-ordering note.

## 6. Cycle-4 Verdict

**Finding 1 — P01 confirmFuel/FuelEditor confirm-path removal deferred to Phase 36: RESOLVED.**
All five required surfaces are present and the deferral (not reversal) is explicit: must_haves prohibition with `status: deferred` (`24.2-01-PLAN.md:54-56`); Task 1 `<decision>` naming Phase 36 (`:134`); acceptance criterion forbidding edits to the three files (`:152`, "If the executor finds itself editing any of those three files, STOP"); ADR-081 Consequences deferral instruction (`:178`); threat register `T-24.2-03` accept-with-deferral (`:245`); and the **ROADMAP Phase 36 breadcrumb verified on disk** (`.planning/ROADMAP.md:525`, "Consumes from Phase 24.2 (deferred code cleanup)…remove the now-inert legacy AI-proposed-fuel confirm path"). The "inert" premise is verified: `confirmFuelCore` at `fuel-dao.ts:214` / `confirmFuel` at `:282` only flip `source→'manual'`, and **no production `source='ai'` producer exists** (grep of `src/**/*.tsx` finds only `source:"share"` and `source:"manual"`; all `'ai'` hits are test files). Plan 01 does not silently leave the path unaddressed — `files_modified` deliberately excludes all three files.

**Finding 2 — P01 tracer scorer reconciled: RESOLVED.**
No residual contradiction. The artifact note (`24.2-01-PLAN.md:104`) says the tracer ships "prefix/substring + bounded ≤1 edit-distance" and Plan 02 "COMPLETES … adds the second tier (≤2 …)"; Task 3 action (`:212`) explicitly calls out and resolves the prior contradiction ("the earlier text said 'prefix/substring only'… that internal contradiction is resolved here in favour of implementing the ≤1 primitive in the tracer"); acceptance (`:223`) asserts a distance-1 typo matches "the ≤2-for-≥6-char tier is Plan 02, not asserted here". Action, artifact, and acceptance now agree internally. Plan 02 correctly owns the full multi-tier scorer with the exact D-07 boundary cases (`24.2-02-PLAN.md:25`, `:80-82`, `:102`).

**Finding 3 — P07 24.2-06 added to depends_on: RESOLVED.**
`depends_on: [24.2-01, 24.2-05, 24.2-06]` (`24.2-07-PLAN.md:6`), with the rationale that Task 3 seeds a `type='imported'` memory that only exists after Plan 06 adds it to the registry (`:145`). **Matrix acyclic, wave count still 3:** 01→(02,03,04,05,06)→07; all of 02–06 are wave 2 depending only on 01, and 07 is wave 3 depending on 01/05/06 — verified DAG. **Wave-2 plans file-disjoint from Plan 07:** programmatically confirmed — Plan 07 touches only `src/backup/*`, and no wave-2 plan touches `src/backup/*` (and wave-2 plans are mutually disjoint too, permitting parallel execution).

**Finding 4 — P05 pre-018 purge availability guard gates BOTH map and IN-list: RESOLVED, and the code matches.** Plan 05 requires both edits explicitly (`24.2-05-PLAN.md:183`: "(i) add `custom_field_value_history` to the `KNOWLEDGE_CHILD_TABLES` map (:96), AND (ii) add … to the hard-coded `sqlite_master … name IN (…)` list … (:107-111)") with a dedicated acceptance criterion (`:199`). **Verified against `purge-dao.ts` on disk:** `KNOWLEDGE_CHILD_TABLES` is at `:96-100`; `availableKnowledgeChildren` has the hard-coded `IN ('memories', 'relationships', 'current_state_entries')` literal at `:107-111`; and critically, `isAvailablePurgeChild` at `:123-130` returns `true` for any entity **not** in the map (`!(entityType in KNOWLEDGE_CHILD_TABLES) || …`) — exactly the mechanism the plan cites for why a map-only add leaves the SELECT list stale. The plan's code understanding is precisely accurate, and the guard also correctly gates the `:318` delete array (`if (!isAvailablePurgeChild(...)) continue;` at `:319`).

---

*Note: this Claude lane ran as a read-only Claude subagent (the owner-approved workaround for the `claude -p` headless Write-permission gap), not via `claude -p`. It has full repo read access and its file:line claims were spot-corroborated by the orchestrator against disk.*
