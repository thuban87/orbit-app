---
phase: 24.2
reviewers: [codex, claude]
reviewed_at: 2026-09-04T13:10:00Z
cycle: 7
plans_reviewed: [24.2-01-PLAN.md, 24.2-02-PLAN.md, 24.2-03-PLAN.md, 24.2-04-PLAN.md, 24.2-05-PLAN.md, 24.2-06-PLAN.md, 24.2-07-PLAN.md]
models:
  codex: "gpt-5.6-terra (reasoning=low)"
  claude: "read-only subagent (gsd-review --claude Write-gap workaround)"
model_sources:
  codex: "banner"
  claude: "subagent-workaround"
---

# Cross-AI Plan Review — Phase 24.2 (Convergence Cycle 7 — final owner-authorized cycle)

<!-- Cycle 7. Plans 05 and 06 were revised per two explicit owner decisions (commit 8457209).
     Codex ran through the gsd-review lane machinery (real model, not stubbed). The Claude lane
     was run via a read-only Claude subagent — the documented workaround for the `claude -p`
     Write-permission gap in this repo — with the owner's explicit prior authorization. Both
     lanes are prompt-fed, source-grounded (full repo read access), and cited file:line evidence.
     The orchestrator independently verified the load-bearing claims below against the code on
     disk (not against either reviewer's summary), per CLAUDE.md "Review the code, not the diff." -->

## Consensus Summary

Both reviewers independently VERIFIED the two owner decisions against the code on disk, and both
re-ran the Decision-2 enumeration sweep themselves and found exactly three new-contact-from-import
creators and no fourth. The plan set is assessed as unusually well-converged; every claim about
existing code (migration head, the AI-egress boundary, the custom-field DDL, the import creators,
the tombstone/purge/merge registries, the backup export/restore maps) matched the files on disk at
every point checked. The reviewers **diverge on one point**: Codex raises a single HIGH on Plan 01's
egress tracer (opted-in memories reach `PromptContext` but are never serialized into the actual
outbound provider payload); Claude raises zero HIGH, treating `readPromptContext` as the sufficient
egress-permission boundary. The orchestrator verified this on disk and resolves it below.

**Owner Decision 1 (Plan 05 — `createField` GLOBAL-ONLY): VERIFIED by both reviewers + orchestrator.**
`createField` (`src/db/field-ddl.ts:55`) already inserts a global def and seeds every contact in one
`inWriteTransaction`; Plan 05 adds a runtime guard rejecting `scope='contact'` before any INSERT.
Its only production caller is `CustomFieldsScreen.tsx:156` (global); migration 018 defaults
`scope='global'`; `createContactFullCore` seeding is filtered to global. No 24.2 path can create a
contact-scoped def. `assertContactScopedWriteAllowedCore` is retained as defense-in-depth on the
value-write path, exactly as the decision states.

**Owner Decision 2 (Plan 06 — imported note through all import paths): VERIFIED by both reviewers + orchestrator.**
The exhaustive sweep `grep -rn "createContactFullCore\|importContactRecord" src/services/import src/screens`
yields exactly three production creators — `importRowAsNew` (`import-driver.ts:88`), `commitSingleImport`
(`import-acquire.ts:124`), and `combineCluster` (`source-consolidation.ts:196`) — and **no fourth**.
`combineCluster` runs `createContactFullCore` inside an existing `inWriteTransaction`
(`source-consolidation.ts:195`), so its planned imported Memory write is atomic. The serialization seam
(`acceptPickedContacts` three-field allowlist at `import-acquire.ts:92-96`) is the correct place the
note is currently dropped. `already_linked` creates no contact and is a documented drop; the imported
Memory is `allow_ai=0`, so this path does not widen AI egress.

### Agreed Strengths

- Migration 017 is non-destructive by construction (copy-then-remove with a per-row proof-of-copy
  stronger than count-equality, a named integrity-error class, and a `checkpoint:decision` gate for
  the one-way step). Both reviewers confirmed on disk that no shipped producer writes `source='ai'`,
  so the ADR-030 retirement is a safety-net move, not routine deletion.
- Both owner decisions are implementable exactly as written against the real writers/transactions.
- The value-history deletion lifecycle (Plan 05) is closed across all three destructive paths
  (merge, purge, definition-drop), with the plan correctly warning that the purge registry and the
  backup restore registry are two distinct maps.
- Backup round-trip coverage (Plan 07) is complete, including the real hazard that format 4 is
  already live (24.1 pre-bumped it) so a pre-existing v4 backup lacking `customFieldValueHistory`
  must be normalized to `[]` at parse rather than added to `requiredArrays`.
- The correct `updateContactFull` production writer (not the zero-caller `upsertValue` wrapper) is
  the value-history append test target (Plan 05) — the "test the real writer" rigor the repo demands.

### Agreed Concerns

- **Plan 06 — `commitSingleImport` note-source ordering (both reviewers; orchestrator-confirmed).**
  In `commitSingleImport`, `importContactRecord` (the contact create) runs at `import-acquire.ts:124`,
  but the session `row` carrying `sourcePayload` is resolved only afterward at `:131`. The note is not
  available at the point it must be passed into the create. The executor must be steered to resolve
  `sourcePayload` BEFORE line 124, not reuse the post-import `row` binding. ACTIONABLE — the plan's
  loose "or add a targeted read" clause does not clearly prevent the wrong ordering.

### Divergent Views

- **Plan 01 egress tracer — does the opted-in memory need to reach the provider PAYLOAD in 24.2?**
  - *Codex (HIGH):* Plan 01 adds `sharedMemories` to `PromptContext` and populates it in
    `readPromptContext`, but does not modify `src/ai/prompt-template.ts`. The sole payload builder
    `resolvePrompt` serializes only `rankedFuel` + `sharedFields`, never `sharedMemories`, so an
    opted-in Memory is present in the `PromptContext` object but never reaches AI. Codex would add
    prompt-template.ts serialization + a `resolvePrompt(...).payload` test to Plan 01.
  - *Claude (no concern):* `readPromptContext` is the documented single egress-permission choke point;
    introducing memories there gated `allow_ai=1 AND deleted_at IS NULL`, default OFF, satisfies the
    per-item AI-permission goal from the first commit.
  - *Orchestrator resolution (verified on disk):* Both are partly right. The mechanism Codex cites is
    REAL — `resolvePrompt` (`src/ai/prompt-template.ts:142-290`) does not serialize `sharedMemories`
    (confirmed: only `context.rankedFuel` at :166 and `context.sharedFields` at :197 are rendered into
    `payload` at :288). BUT Plan 01 line 209 EXPLICITLY defers prompt-string formatting to the AI
    phases ("Prompt-string formatting is refined by the AI phases; the DATA boundary is enforced
    here"), and the task's `<done>` (line 226) claims only that the memory is permission-gated in the
    sole egress read and searchable — NOT that it reaches the payload. The security-critical invariant
    (opted-OUT memories never egress) IS enforced; the deferred part is the safe direction
    (opted-IN memories not yet rendered). So this is NOT an unresolved privacy/integrity HIGH — the
    substance is deferred-with-rationale to Phase 36 (AI Configuration & Prompting). What IS unresolved
    is an ACCURACY defect: acceptance-criteria lines 213 and 221 mislabel `readPromptContext()` output
    as "the exact outbound payload," which is false and internally contradicts line 209. Left as-is it
    will mislead the executor — either into a `resolvePrompt(...).payload` test that FAILS
    (sharedMemories not rendered), or a `PromptContext`-object test that ships false "end-to-end egress"
    confidence. This must be corrected in PLAN.md (see Actionable item 1).

- **Plan 03 — global-AI gate strength (Codex MEDIUM, Claude not raised).** Codex notes
  `aiProvider !== 'none'` (`app-settings-dao.ts:381`) is configuration presence, not a true master
  AI-enabled/readiness gate; the plan acknowledges this as interim and fail-closed, with Phase 36
  owning the authoritative toggle. Suggestion only (rename the state for clarity); not blocking.

## Codex Review

*Model: gpt-5.6-terra (reasoning=low), via gsd-review lane (source-grounded, full repo read access).*

## Summary

The seven plans are unusually well-grounded and correctly follow the repository’s real transaction, migration, import, and backup seams. Both final owner decisions are implementable as written. I found one HIGH issue: Plan 01 proves opted-in Memories reach `PromptContext`, but not the actual provider payload; the current prompt renderer ignores the proposed `sharedMemories` field. Overall, resolve that egress gap before execution and the plan set is ready.

## Plan 01 — Migration, egress tracer, ADR-081

### Strengths

- Migration numbering is correct: the current head is migration 016 and `TARGET_VERSION = 16` in [database.ts](/home/bwales/projects/orbit-app/src/db/database.ts:51), so 017 is correctly head+1.
- The proposed copy-verify-delete migration is appropriately conservative for the local-only database. `memories` already has the necessary nullable fields and provenance column in [016-contact-knowledge.ts](/home/bwales/projects/orbit-app/src/db/migrations/016-contact-knowledge.ts:11).
- Leaving the legacy `confirmFuel` UI untouched is genuinely inert after removal of `source='ai'` rows: the existing ranked-fuel SQL excludes those rows in [fuel-read.ts](/home/bwales/projects/orbit-app/src/db/fuel-read.ts:133).

### Concerns

- **HIGH — the claimed “end-to-end AI egress” stops before the actual outbound prompt.** Plan 01 adds `sharedMemories` to `PromptContext` and populates it in `readPromptContext`, but it does not modify `src/ai/prompt-template.ts`. That renderer currently serializes only `context.sharedFields` into the provider-bound payload at [prompt-template.ts](/home/bwales/projects/orbit-app/src/ai/prompt-template.ts:196), and builds the final payload at [prompt-template.ts](/home/bwales/projects/orbit-app/src/ai/prompt-template.ts:280). Therefore an opted-in Memory would be present in an intermediate object but never reach AI.
- **LOW — semantic preservation for `source='ai' AND kind='off_limits'` is intentionally incomplete.** The plan preserves text but deliberately loses the Off Limits classification. The `fuel` safeguard is classification-based (`kind != 'off_limits'`) in [fuel-read.ts](/home/bwales/projects/orbit-app/src/db/fuel-read.ts:121). This is owner-surfaced and safe for egress because migrated rows are AI-off, but it should be retained as an explicit migration-test/documentation caveat.

### Suggestions

- Add `src/ai/prompt-template.ts` and its tests to Plan 01. Serialize only `sharedMemories` selected by `allow_ai = 1`, with the same delimiter, sanitization, per-value bound, total-budget accounting, and truncation disclosure used for `sharedFields`.
- Change the test wording from “resolved `readPromptContext()` output (the exact outbound payload)” to test `resolvePrompt(..., await readPromptContext(...)).payload`.

### Risk Assessment

**HIGH until the provider-payload gap is fixed; MEDIUM afterward.**

## Plan 02 — Full knowledge search corpus

### Strengths

- Correctly retains the two-stage architecture: term-free corpus eligibility in SQL and typo matching in TypeScript.
- Correctly avoids legacy Fuel as a KNOW-10 source. Current dashboard searching does query Fuel at [dashboard-read.ts](/home/bwales/projects/orbit-app/src/db/dashboard-read.ts:237), so keeping that legacy behavior isolated from the new corpus is a clean Phase 25 seam.
- Correctly uses `getValuesForContact`, whose definition-ID placeholders are bound rather than interpolated in [field-values-dao.ts](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:27).

### Concerns

- **LOW — corpus assembly may become N+1 if Phase 25 calls it per contact.** The plan intentionally defines a per-contact result. That is fine as an exposure seam, but Phase 25 should either batch it or cache the already-filtered universe.

### Suggestions

- Export a batch-oriented read now, or explicitly document that Phase 25 owns batching and must not call a per-contact database read for every Dashboard row.

### Risk Assessment

**MEDIUM**, primarily due to future Dashboard-scale performance rather than correctness.

## Plan 03 — Memory AI permission UI and Off Limits

### Strengths

- Correctly honors parent-owned persistence: `MemoryEditor` presently has callback-only persistence props at [MemoryEditor.tsx](/home/bwales/projects/orbit-app/src/components/MemoryEditor.tsx:60), while `ThingsToRememberScreen` owns its render sites at [ThingsToRememberScreen.tsx](/home/bwales/projects/orbit-app/src/screens/ThingsToRememberScreen.tsx:585) and [ThingsToRememberScreen.tsx](/home/bwales/projects/orbit-app/src/screens/ThingsToRememberScreen.tsx:630).
- Correctly uses both `id` and `contact_id` for the planned toggle writer, matching the repository’s ownership-scoping style.
- The sparkle placement is appropriate: `MemoryCard` is actually rendered by `MemoryEditor`, not dead UI.
- Retaining Off Limits separately is correct: the existing SQL exclusion is structural, not merely visual, at [fuel-read.ts](/home/bwales/projects/orbit-app/src/db/fuel-read.ts:133).

### Concerns

- **MEDIUM — `aiProvider !== 'none'` is configuration presence, not a true global AI-enabled gate.** The source exposes `aiProvider` from settings at [app-settings-dao.ts](/home/bwales/projects/orbit-app/src/db/app-settings-dao.ts:381), but it does not establish readiness or a master on/off state. The plan acknowledges this as interim, which is appropriate, but the UI copy should not claim AI is fully available merely because a provider enum is set.

### Suggestions

- Name the state `aiConfiguredForMemoryPermission` or equivalent in code/comments. Keep it fail-closed, as planned, and have Phase 36 replace its source with the authoritative master toggle/readiness composition.

### Risk Assessment

**MEDIUM.**

## Plan 04 — URL, email, and phone field types

### Strengths

- Correctly preserves Photo. The current type system has seven types, including Photo; adding three makes ten rather than deleting an existing field behavior.
- The one-parser-per-type design aligns with the exhaustive `Record<FieldType, ...>` pattern.
- Keyboard-hint plumbing is necessary: existing `TextFieldWidget` cannot receive a keyboard type today.

### Concerns

- No material concern found.

### Suggestions

- Keep the permissive validator rules narrowly documented and byte-preserving, as planned.

### Risk Assessment

**LOW.**

## Plan 05 — Scope-ready custom fields and value history

### Strengths

- Owner Decision 1 is valid against the actual source. `createField` currently inserts a global definition and seeds every contact in one transaction at [field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:55). A runtime rejection for `scope='contact'` before that insert will correctly make contact-scoped creation unreachable in 24.2 while preserving global seeding.
- The plan correctly identifies the real production edit path: `updateContactFull` calls `upsertValueCore` in its live custom-values loop at [contacts-dao.ts](/home/bwales/projects/orbit-app/src/db/contacts-dao.ts:412), while `upsertValue` is only a wrapper at [field-values-dao.ts](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:45).
- The lifecycle inventory is excellent. Current purge has both an exhaustive tombstone disposition map and a separate hard-coded delete inventory at [purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:80) and [purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:318). The plan correctly requires updating both.
- The plan also correctly catches definition-delete cascade loss: `dropFieldValues` currently deletes current pairs and then the definition without any value-history handling at [field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:120).

### Concerns

- **LOW — contact-scoped definitions can still arrive via restore/direct database state, so Phase 31’s scope filtering and ownership bundle must remain a hard prerequisite.** The plan calls this out and provides defense-in-depth. That is sufficient for this phase because public creation is blocked.

### Suggestions

- Add an explicit test that restore-created contact-scoped definitions are rejected by non-owner writes, in addition to the direct-SQL test planned here.

### Risk Assessment

**MEDIUM**, because it changes schema and destruction/restore lifecycle paths, but its safeguards are thorough.

## Plan 06 — Imported phone-Contacts notes

### Strengths

- Owner Decision 2 matches the repository. The independent import creators are exactly:
  - bulk: `importRowAsNew` at [import-driver.ts](/home/bwales/projects/orbit-app/src/services/import/import-driver.ts:68);
  - single/review: `commitSingleImport` at [import-acquire.ts](/home/bwales/projects/orbit-app/src/services/import/import-acquire.ts:120);
  - consolidated clusters: `combineCluster`, which directly calls `createContactFullCore` inside an existing transaction at [source-consolidation.ts](/home/bwales/projects/orbit-app/src/services/import/source-consolidation.ts:195).
- No fourth production new-contact import creator was found by re-running the requested enumeration.
- The plan correctly identified the real serialization loss: `acceptPickedContacts` persists only name, methods, and birthday at [import-acquire.ts](/home/bwales/projects/orbit-app/src/services/import/import-acquire.ts:92), while import replay reconstructs exclusively from `sourcePayload` at [import-driver.ts](/home/bwales/projects/orbit-app/src/services/import/import-driver.ts:54).
- The native coverage is complete in concept. Current picker requests only Phone, Email, Event, and Photo at [OrbitContactPickerModule.kt](/home/bwales/projects/orbit-app/modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt:89), and its three full-read paths are identifiable.
- The cluster Memory write can be atomic because `combineCluster` already owns `inWriteTransaction` at [source-consolidation.ts](/home/bwales/projects/orbit-app/src/services/import/source-consolidation.ts:195).

### Concerns

- **LOW — native build verification is an external build gate, not covered by TypeScript.** The plan already catches this and correctly requires a Gradle/APK build plus device UAT.

### Suggestions

- Parse the session note before opening `importContactRecord` in `commitSingleImport`, so malformed legacy payloads fail before contact creation rather than after an import transaction begins.

### Risk Assessment

**MEDIUM**, due to Android-native implementation and device verification requirements.

## Plan 07 — Backup and restore

### Strengths

- Correctly recognizes format 4 is already live: [types.ts](/home/bwales/projects/orbit-app/src/backup/types.ts:14) has `BACKUP_FORMAT_VERSION = 4`.
- Correctly identifies every restore map that must be extended. They are currently parallel registries at [restore-apply.ts](/home/bwales/projects/orbit-app/src/backup/restore-apply.ts:49).
- Correctly distinguishes backup tombstone validation from purge tombstone typing: the restore schema’s accepted tombstones are separately defined at [backup-schema.ts](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:170), while purge uses `TombstoneEntityType` at [tombstones-dao.ts](/home/bwales/projects/orbit-app/src/db/tombstones-dao.ts:8).
- The old-format-4 compatibility plan is necessary. Existing validation requires every listed array at [backup-schema.ts](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:259), and there is no 4→4 forward migration at [backup-schema.ts](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:618).

### Concerns

- **LOW — modifying a live format-4 wire shape requires disciplined normalization forever.** The plan’s `customFieldValueHistory ?? []` approach handles this particular addition correctly. Future format-4 additions must follow the same compatibility rule or receive a format bump.

### Suggestions

- Add a short compatibility comment beside the optional-array normalization explaining that v4 was already emitted by 24.1 and therefore cannot be treated as a homogeneous shape.

### Risk Assessment

**MEDIUM**, because backup/restore correctness is high-impact, though the planned map inventory and round-trip coverage are strong.

## Overall risk assessment

**MEDIUM after addressing the Plan 01 HIGH issue.** The plans are comprehensive, preserve ADR-001/028/031 constraints, correctly avoid mutex nesting, and properly cover the two owner-authorized decisions. The remaining blocker is ensuring an AI-authorized Memory is not merely present in `PromptContext`, but is deliberately serialized into the exact provider-bound prompt with the existing safety and budgeting controls.

---

## Claude Review

*Read-only Claude subagent lane (the documented gsd-review `--claude` Write-gap workaround, owner-authorized). Source-grounded, full repo read access.*

# Cross-AI Review — Phase 24.2 (Convergence Cycle 7, Claude lane)

## Summary

Phase 24.2's seven plans are, at cycle 7, an unusually rigorous and well-converged set. I verified every load-bearing claim the plans make about existing code against the files on disk (migrations, the AI-egress boundary, the custom-field DDL, the import creators, the tombstone/purge/merge registries, and the backup export/restore maps) and found the file:line references accurate throughout. The two owner decisions this cycle targeted both hold up on disk. The repo's self-declared highest-risk surface — the data layer, where SQL writers cannot be enumerated by the graph — is handled with genuine care: the destructive migration 017 is non-destructive copy-then-remove with a proof-of-copy integrity gate, the new value-history table's deletion lifecycle is closed across all three destructive paths (merge, purge, definition-drop), backup/restore covers every new field and entity under both merge and replace-all with a correct older-backup normalization, and the one new AI-egress path (memories) is introduced already gated default-OFF. I raise **zero HIGH concerns**; the residual findings are LOW/informational and mostly documentation-precision nits.

## Owner Decision Verifications

### OWNER DECISION 1 (Plan 05 — `createField` GLOBAL-ONLY): **VERIFIED**

- The current `createField` (src/db/field-ddl.ts:55-94) wraps its def-INSERT plus the seed loop in one `inWriteTransaction`; the seed loop (lines 79-91) iterates **every** contact (`SELECT id FROM contacts ORDER BY id`) — so "a global create still seeds every contact" is the *existing, preserved* behavior, and the plan does not touch it. ADR-013 is intact.
- The plan's design adds a runtime guard rejecting `def.scope === 'contact'` **before any INSERT** (prompt embed §Plan-05 Task 2; PLAN.md line 1954), with a test asserting the throw leaves both `custom_field_defs` and `custom_field_values` row counts unchanged. Because `createField` is already fully transactional, even a mis-placed throw would roll back — the guard is correct either way.
- "NO 24.2 path can create a `scope='contact'` def" is corroborated on disk: `createField` has exactly **one** production caller, src/screens/CustomFieldsScreen.tsx:156 (grep for `createField\b` across src, excluding tests), which creates global defs; migration 018 defaults `scope='global'`; and the new-contact seeding path in `createContactFullCore` (src/db/contacts-dao.ts:198-211) currently seeds from **all** defs (`listDefs(...includeQuarantined:true)`) and the plan filters it to global. In 24.2 that filter is a no-op (all defs are global), so it is purely defensive for a future Phase-31 restore — no regression, no data loss.
- `assertContactScopedWriteAllowedCore` does not yet exist on disk (grep returns nothing); it is a plan addition retained as defense-in-depth on the value-write path, exactly as the owner decision states. Its deferral of durable ownership / owner-purge to Phase 31 is explicit and internally consistent (PLAN.md lines 186, 189-191).

### OWNER DECISION 2 (Plan 06 — imported note wired through all import creators): **VERIFIED**

- I re-ran the enumeration sweep myself: `grep -rn "createContactFullCore\|importContactRecord" src/services/import src/screens`. Production new-contact-from-import creators on disk are exactly three: `commitSingleImport` (import-acquire.ts:124 → `importContactRecord`), `importRowAsNew` (import-driver.ts:88 → `importContactRecord`), and `combineCluster` (source-consolidation.ts:196 → `createContactFullCore` directly). The other hits are `importContactRecord`'s own internal `createContactFullCore` call (imported-contact-dao.ts:125) and the manual-add wrapper `createContactFull` (contacts-dao.ts:148/156, not an import path). **No 4th new-contact-from-import creator exists.**
- The serialization seam the plan identifies is accurate: `acceptPickedContacts` serializes `sourcePayload` as a three-field allowlist `JSON.stringify({ displayName, methods, birthday })` (import-acquire.ts:92-96), and both replay paths reconstruct solely from `JSON.parse(row.sourcePayload)` (import-driver.ts:55, source-consolidation.ts:46). A type-only `PickedContact.note` addition would indeed be dropped; the plan correctly requires adding `note: contact.note` to that object.
- Atomicity confirmed on disk: `importContactRecord` runs its body inside `inWriteTransaction` (imported-contact-dao.ts:122-186), and `combineCluster` runs `createContactFullCore` + all evidence writes inside `inWriteTransaction` (source-consolidation.ts:195-250). A planned `addMemoryCore(type:'imported')` composed inside either transaction is atomic with the contact create, matching the plan's claim. The `already_linked` outcome creates no new contact (`resolveAlreadyLinked`, import-driver.ts:157-163) and is a correctly-documented drop.
- The imported Memory is `allow_ai=0` (registry `aiDefault=false` + column default from migration 017), so this new import path does **not** widen AI egress — consistent with local-first.

## Strengths

- **Migration 017 is non-destructive by construction** (Plan 01, PLAN.md lines 173-176): share-capture and any `source='ai'` fuel rows are copied to memories and only then removed, gated by a per-row proof-of-copy assertion stronger than count-equality, with a named integrity-error class that rolls the step back on mismatch. The irreversibility is handled by an explicit `checkpoint:decision` gate (Task 1) and a superseding ADR-081 — the correct process for retiring ADR-030 rather than an unauthorized in-line reversal. I confirmed on disk that **no shipped producer writes `source='ai'`** (grep for `source:'ai'` across src returns nothing; the only fuel producers are `source:'user'`/`source:'share'`), so the retirement is genuinely a safety-net move.
- **The AI-egress boundary stays a single choke point.** `readPromptContext` (src/db/ai-context-read.ts:190) is the documented sole projection into the closed `PromptContext`; Plan 01 introduces memories into it via a `sharedMemories` field gated `allow_ai = 1 AND deleted_at IS NULL`, default OFF. This is the first time memories can reach AI at all, and it is opt-in from the first commit.
- **Value-history deletion lifecycle is closed across every destructive path** (Plan 05). I verified each referenced site: the `TombstoneEntityType` union + exhaustive `assertTombstoneEntityType` switch (tombstones-dao.ts:8-21, 35-56) both currently lack the member; the exhaustive `PURGE_CHILDREN` Record (purge-dao.ts:~80), the separate hard-coded explicit-delete array (purge-dao.ts:~318), and the two-part `KNOWLEDGE_CHILD_TABLES` availability guard (map + `IN (...)` literal, purge-dao.ts:96/107); and the `field_history` bespoke reparent that omits `modified_at` (merge-dao.ts:~181). The plan's instruction to touch all of them — and its explicit warning that `custom_field_value_history` (Plan 05, purge) and `TOMBSTONE_ENTITY_TYPES` (Plan 07, restore) are two distinct registries — is exactly right.
- **Backup round-trip coverage is complete** (Plan 07). I verified `TOMBSTONE_ENTITY_TYPES` is a Set at backup-schema.ts:170; the exhaustive `entities`/`tableOf`/`tombstoneEntity` Records and the replace-all delete inventory in restore-apply.ts (lines ~49-57, ~245) that `tsc` will force to be updated; and the real P07-MED hazard — `validate()`'s `requiredArrays` list (backup-schema.ts:260) hard-fails on an absent array and the last forward migration is `3→4` (there is no `4→4`), so a pre-existing format-4 backup lacking `customFieldValueHistory` must be normalized to `[]` at parse rather than added to `requiredArrays`. The plan handles this precisely and adds an end-to-end older-backup test through the real `parseBackupManifest`.
- **HIGH-1 real-path test discipline**: Plan 05 insists the value-history append test drives the production `updateContactFull` writer, not the zero-caller `upsertValue` wrapper — the exact kind of "test the real writer" rigor CLAUDE.md demands for shared tables.

## Concerns

- **LOW — Plan 06, `commitSingleImport` row-fetch ordering guidance is slightly inaccurate.** The plan says to read the note from "the session row already fetched via `listSessionRows`" (PLAN.md line 2201). On disk that fetch happens *after* the `importContactRecord` call (import-acquire.ts:131, whereas the import call is at :124), so the note is not yet available at the point it must be passed in. The plan's alternative clause ("or add a targeted read") covers it, but the executor should be steered to fetch the `sourcePayload` **before** line 124, not reuse the post-import `row`. Mechanism: passing `note` into a call that precedes the only place `row` is currently resolved.
- **LOW — Plan 06, `already_linked` silent note drop is a product choice worth a one-line owner confirmation.** A phone contact carrying a Note that re-imports onto an existing Orbit contact drops the note with no user-visible signal (import-driver.ts:157-163 path). The plan documents this deliberately (KNOW-14 targets new-contact import; grafting notes on every re-import risks duplicates). It is defensible and in the owner's product bucket — flagging only so the drop is a decision, not an accident.
- **LOW — Plan 07, replace-all delete order for `custom_field_value_history`.** The plan says to insert it "before contacts/custom_field_defs" in the delete inventory (PLAN.md line 117). Since the table CASCADE-references *both* `contacts` and `custom_field_defs`, it should be placed with the other per-contact children (near `custom_field_values`), well ahead of both parents, to keep FK order unambiguous. The guidance is not wrong, just less precise than the child-table ordering it should mirror.
- **INFORMATIONAL — verification depth.** My deepest verification went to Plans 01, 05, 06, and 07 (the migration, data-move, import, and backup surfaces where irreversible or egress risk lives) and to the two owner decisions. Plans 02 (TS search scorer), 03 (per-item AI toggle UI/sparkle), and 04 (url/email/phone field types + parsers) received a lighter pass; I saw no red flags, but this review does not assert line-level verification of those three. The search requirement "never searches internal metadata" (KNOW-10) and the two-gate AI permission UI (KNOW-11/12) are the places a later pass should concentrate.

## Suggestions

- In Plan 06, tighten the `commitSingleImport` step to explicitly say "resolve the session row's `sourcePayload` **before** the `importContactRecord` call" so the executor does not attempt to reuse the existing post-import `row` binding.
- In Plan 07, list `custom_field_value_history` in the replace-all delete inventory adjacent to `custom_field_values` (both are per-contact children with parents deleted last), rather than the looser "before contacts/custom_field_defs."
- Consider a one-line SUMMARY note in Plan 06 recording the `already_linked` note-drop as an owner-confirmed product boundary, so a future reader does not read it as an oversight.
- Keep the ADR-030 confirm-code deferral (Plan 01 → Phase 36) explicit in ADR-081's Consequences as the plan already specifies; the inert-but-present path is the one lingering revival surface and its deferral should remain traceable.

## Risk Assessment: **LOW**

Every irreversible or egress-bearing operation in this phase is either non-destructive (migration 017 copy-then-remove with proof-of-copy), opt-in-and-default-off (memories AI egress), or fully round-tripped with forward-compatible defaults (backup/restore). The data-layer lifecycle work that the graph cannot see — table writers, tombstones, merge/purge/def-drop — has been enumerated by hand and closed across all paths, and the plans' claims about existing code matched the files on disk at every point I checked. Both owner decisions are VERIFIED on disk. The remaining findings are LOW documentation-precision items and one product-boundary confirmation, none of which threaten data integrity or the local-first commitment. This plan set is ready to execute.
