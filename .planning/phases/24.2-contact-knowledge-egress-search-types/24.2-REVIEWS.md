---
phase: 24.2
reviewers: [codex, claude]
reviewed_at: 2026-09-04T17:32:12Z
convergence_cycle: 6
plans_reviewed: [24.2-01-PLAN.md, 24.2-02-PLAN.md, 24.2-03-PLAN.md, 24.2-04-PLAN.md, 24.2-05-PLAN.md, 24.2-06-PLAN.md, 24.2-07-PLAN.md]
models:
  codex: "gpt-5.6-terra (reasoning=low)"
  claude: "claude-opus (read-only subagent lane)"
model_sources:
  codex: "banner"
  claude: "subagent-workaround"
lane_notes:
  claude: "The built-in Claude CLI review lane self-skips inside Claude Code (SELF_CLI=claude). Per the owner-approved workaround (MEMORY: claude-reviewer-via-subagent), the Claude lane ran as a read-only general-purpose subagent that read the same review prompt and verified against disk. The lane was NOT silently dropped."
---

# Cross-AI Plan Review — Phase 24.2 (Convergence Cycle 6)

> Owner-authorized extra cycle. Plans 05 & 06 were revised in commit `d8e103a` to fix the cycle-5 HIGH (Plan 06 note serialization seam) + MEDIUM (Plan 05 durable ownership deferral). Reviewed by Codex and Claude, both source-grounded against the code on disk. The aggregator (orbit orchestrator, Opus) independently re-traced the two fixed seams and the surrounding subsystem before writing this consensus.

## Consensus Summary

Both reviewers agree the **cycle-5 HIGH on Plan 06 (imported-note serialization) is FULLY RESOLVED**: the revised plan wires `note` into the `sourcePayload` allowlist in `acceptPickedContacts` (import-acquire.ts:92-96), through the `pickedFromRow` re-parse (import-driver.ts:55) → `mapPickedContact` → `importRowAsNew` → `importContactRecord` bulk path, AND threads it through the `commitSingleImport` single/review path (import-acquire.ts:120). The false "no serialization change needed" claim from the prior cycle is corrected, and the plan mandates an end-to-end test that drives the REAL `acceptPickedContacts` serialization. The aggregator confirmed all of these file:line seams on disk.

The reviewers **diverge on Plan 05**. The **Plan 05 cycle-5 MEDIUM (durable contact-scoped-def ownership + owner-purge)** is explicitly deferred to Phase 31 by name in three places (must_haves prohibition `status: deferred` at :59-61; Task 3(C) forward note at :184-185; threat T-24.2-13b residual at :228). Claude rates this FULLY RESOLVED (deferral is explicit and the in-phase claim holds — `grep` finds no `scope='contact'` writer on disk). **Codex rates it an open HIGH**: Plan 05 Task 2 modifies the *public* `createField` (field-ddl.ts:55) to accept and create `scope='contact'` definitions in THIS phase, so the deferral's "unreachable in 24.2" premise describes callers, not the shipped writer capability — leaving a latent orphan-on-purge / reclaimable-ownership hole in the same phase that introduces the capable writer. **This divergence is an OWNER decision** (risk/security posture, and it challenges an owner-approved deferral by name) — see Divergent Views.

The aggregator adds one **independent MEDIUM finding both lanes missed**: a **third** reachable new-contact import path, `combineCluster` in `source-consolidation.ts:142`, creates a contact via `createContactFullCore` (:196) and never calls `importContactRecord`. Because Plan 06 hangs the note-Memory write inside `importContactRecord`, an imported note on any **consolidated cluster** (2+ picked contacts sharing a canonical phone/email in a bulk import — invoked from `BulkImportSetupScreen.tsx:141`) is silently dropped. Codex flagged this file as a "LOW — another JSON reparse seam" but incorrectly asserted "it may not create contacts"; it does. This is the same missed-sibling-path pattern that stalled cycle 5, and Plan 06 does not list `source-consolidation.ts` in `files_modified`.

### Agreed Strengths

- **Plan 06 note-serialization seam is correctly fixed** across both the bulk and single import paths, with a mandated end-to-end test through the real serialization (Codex, Claude, aggregator — all verified on disk).
- **Tracer-first migration spine (Plan 01)** is well-sequenced; migration 017 is correctly head+1 (disk: migrations 001–016, TARGET_VERSION=16 at database.ts:51) and the one-way data-move is copy-then-verify-then-delete behind a decision checkpoint.
- **`BACKUP_FORMAT_VERSION=4` already live** (types.ts:14); Plan 07 correctly emits within format 4 with no bump and covers soft-deleted memories (export-manifest.ts:58 has no `deleted_at IS NULL` filter).
- **Search corpus (Plan 02)** keeps SQL to eligibility only and never term-`LIKE`s internal metadata; custom values read through the def-id-bound, `?`-parameterized `getValuesForContact`.
- **Plan 04** retains the `photo` FieldType rather than reversing a recorded decision to match dossier prose (both reviewers noted this favorably).

### Agreed Concerns

- **Plan 06 `commitSingleImport` note-fetch ordering** (Codex MEDIUM, Claude LOW): on disk the session-row fetch is at import-acquire.ts:131, AFTER the `importContactRecord` call at :124. The executor must move the fetch/parse BEFORE the import call. The plan's action text already instructs reading the note from `sourcePayload` and passing it INTO `importContactRecord` (which structurally forces the correct order), and offers "or add a targeted read", so it is guidance-adequate — but the "already fetched via listSessionRows" wording is misleading and should be corrected. (Not counted as a residual actionable: the plan's own action text already requires the read-before-import ordering.)
- **Interim/global AI gate is not real global-AI state** (Codex LOW on Plan 03): `aiProvider !== "none"` means "a provider is configured," and Plan 03 already labels this interim, to be replaced in Phase 36. SQL egress enforcement in Plan 01 is the real control.

### Divergent Views

- **Plan 05 ownership deferral — HIGH (Codex) vs LOW/RESOLVED (Claude). OWNER DECISION.** Both reviewers are factually correct about disk: Task 2 makes public `createField` capable of `scope='contact'` creation (verified — current signature at field-ddl.ts:55 takes only `def: NewFieldDef` and seeds every contact; Task 2 adds scope acceptance), AND no 24.2 caller passes `scope='contact'` (the sole caller, CustomFieldsScreen.tsx:156, gets no scope selector in-phase; grep finds no `scope='contact'` on disk). The disagreement is a risk-posture judgment: does shipping a *capable-but-unexercised* public writer while deferring the durable-ownership/owner-purge model to Phase 31 constitute a HIGH in-phase data-integrity risk (Codex), or an acceptable deferral because the edge is unreachable in-phase and a defense-in-depth write guard is present (Claude/Plan)? This challenges an owner-approved deferral by name, so it is escalated rather than closed. Codex's mitigation options: (a) keep `createField` global-only in 24.2 and reserve `scope='contact'` acceptance for Phase 31; (b) add durable ownership (`owner_contact_id`) now; or (c) make scoped creation impossible at runtime, not merely absent from UI. **The aggregator did not unilaterally resolve this.**
- **`combineCluster` note-drop severity — aggregator MEDIUM (reachable, creates contacts) vs Codex LOW ("may not create contacts").** The aggregator's disk trace (source-consolidation.ts:142→196; invoked from BulkImportSetupScreen.tsx:141) shows it DOES create contacts and WILL drop the note. Treat as an actionable MEDIUM for Plan 06.

---

## Codex Review


## Summary

The seven plans are unusually well-grounded in the current codebase and correctly sequence the migration tracer before consumers, custom-field expansion, native import, and backup coverage. The revised Plan 06 now traces the actual import serialization seam correctly. One remaining high-risk design issue remains in Plan 05: it introduces a public contact-scoped-definition creation path while deferring durable ownership and owner-purge semantics, making the claimed “unreachable in 24.2” boundary untrue.

## Plan 01 — Migration, egress tracer, ADR-081

### Strengths

- Correctly starts at migration 017: the current migration head is 016 and `TARGET_VERSION` is 16 in [database.ts](/home/bwales/projects/orbit-app/src/db/database.ts:51).
- The proposed `allow_ai` migration is additive to the existing `memories` table, whose current schema has no such column ([016-contact-knowledge.ts](/home/bwales/projects/orbit-app/src/db/migrations/016-contact-knowledge.ts:10)).
- The plan correctly identifies that `PromptContext` is built centrally in [`readPromptContext`](/home/bwales/projects/orbit-app/src/db/ai-context-read.ts:190), and that current custom-field egress enters through `sharedFields` at [ai-context-read.ts:253](/home/bwales/projects/orbit-app/src/db/ai-context-read.ts:253).
- Copy-then-verify-then-delete is appropriate for the irreplaceable local-data migration.

### Concerns

- **LOW — migration test wording is internally imprecise.** It says both “seeded pre-016-shaped fixture” and migrate to version 16 before seeding. The latter is the viable mechanism because `memories` is only created by migration 016 ([016-contact-knowledge.ts](/home/bwales/projects/orbit-app/src/db/migrations/016-contact-knowledge.ts:76)). The test should consistently call this a pre-017 fixture.

### Suggestions

- State that the source-ai preservation test must use a source row whose `text` is non-null and unique, so the test proves row identity/content preservation rather than only count preservation.
- Add an idempotence-oriented assertion that migration 017 is never reapplied after `user_version=17`, through the standard full-chain harness.

### Risk Assessment

**MEDIUM.** The architecture and data protections are solid, but this is an irreversible data move and warrants the human gate already included.

## Plan 02 — Search corpus and scorer

### Strengths

- The plan respects the present architecture: dashboard search currently only searches name and eligible Fuel via SQL `LIKE` ([dashboard-read.ts](/home/bwales/projects/orbit-app/src/db/dashboard-read.ts:230)).
- It correctly keeps relationship reads live-only; current relationship SQL already filters `deleted_at IS NULL` ([relationships-read.ts](/home/bwales/projects/orbit-app/src/db/relationships-read.ts:21)).
- The structured corpus avoids leaking internal metadata and gives Phase 25 useful source provenance.
- The shared tokenizer and explicit edit-distance boundaries are a good antidote to implementation drift.

### Concerns

- **LOW — bounded input is not the same as bounded result population.** `MAX_TOKENIZE_LEN` bounds individual strings, but the plan should state that Phase 25 must pass the already-filtered eligible contact universe rather than invoking a whole-database corpus scan. This matters because the plan’s public API is framed “per contact,” while the eventual Dashboard use is cross-contact.

### Suggestions

- Export an API shaped around an explicit `contactIds`/eligible-candidates input, or document that the caller owns narrowing before corpus loading.
- Test unicode normalization behavior in both query and candidate text, not merely one diacritic example.

### Risk Assessment

**LOW.** Good separation of SQL eligibility and TypeScript matching; the main residual risk is later misuse by Dashboard consumers.

## Plan 03 — Per-item AI toggle and Off Limits presentation

### Strengths

- The plan correctly preserves parent-owned persistence. `MemoryEditor` should not directly write because the existing DAO’s transaction ownership is explicit in [`memories-dao.ts`](/home/bwales/projects/orbit-app/src/db/memories-dao.ts:90).
- It correctly identifies `getAppSettings`/`ai_provider` as a durable configuration seam rather than using secure credential storage.
- Required props at both editor render sites is a strong compile-time guard.
- Keeping Off Limits separate is aligned with the existing Fuel model rather than falsely treating it as a Memory property.

### Concerns

- **LOW — the interim global gate needs prominent technical debt labeling.** `aiProvider !== "none"` means “a provider is configured,” not “AI globally enabled.” Current `PromptContext` itself has no global-AI state ([prompt-types.ts](/home/bwales/projects/orbit-app/src/ai/prompt-types.ts:99)). The plan acknowledges this, but Phase 36 must replace it rather than merely add another UI gate.

### Suggestions

- Put the interim-gate limitation in a code comment beside the one computed boolean and add a Phase 36 handoff test/issue reference.
- Add a UI test for settings-load failure if the project’s component-test environment can support it; otherwise record device UAT as mandatory.

### Risk Assessment

**MEDIUM.** SQL egress enforcement in Plan 01 is the real protection; the UI gate is intentionally temporary and must not be mistaken for it.

## Plan 04 — URL, email, and phone custom-field types

### Strengths

- Correctly preserves the actual seven existing members plus `photo`; `FieldType` is currently defined in [types.ts](/home/bwales/projects/orbit-app/src/schemas/types.ts:20).
- The plan appropriately preserves TEXT storage and read-time validation rather than coercion.
- Explicit keyboard mappings are warranted because existing widget plumbing currently lacks a keyboard-type field.

### Concerns

- **LOW — completion text says “nine field types” despite the plan correctly retaining Photo and producing ten.** This could confuse future implementers and reviewers.

### Suggestions

- Correct every completion/summary phrase to “ten stored field types; nine product-listed types plus retained Photo.”
- Ensure malformed imported values are visibly flagged in the same existing validation path, not only validated by the parser unit tests.

### Risk Assessment

**LOW.** Additive, strongly typed, and isolated from schema changes.

## Plan 05 — Scope, value history, lifecycle

### Strengths

- It correctly identifies the true production edit path: `updateContactFull` calls `upsertValueCore` in its transaction ([contacts-dao.ts](/home/bwales/projects/orbit-app/src/db/contacts-dao.ts:342), [contacts-dao.ts](/home/bwales/projects/orbit-app/src/db/contacts-dao.ts:414)); placing history only in the wrapper would indeed miss production edits.
- It accurately identifies all three current destructive seams:
  - field drop explicitly deletes values before the definition ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:120));
  - purge is driven by `PURGE_CHILDREN` plus a distinct explicit delete list ([purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:80), [purge-dao.ts](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:316));
  - merge uses a generic reparent helper that requires `modified_at`, while `field_history` already needs bespoke handling ([merge-dao.ts](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:88), [merge-dao.ts](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:180)).
- The plan’s no-cascade-without-tombstone posture is consistent with the repository’s existing explicit deletion model.

### Concerns

- **HIGH — the Plan 05 ownership deferral is not actually unreachable in Phase 24.2.** The plan says no 24.2 writer creates `scope='contact'` definitions, but Task 2 explicitly changes public `createField` to accept and create them. Today `createField` is already a public export ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:55)) and seeds every contact ([field-ddl.ts](/home/bwales/projects/orbit-app/src/db/field-ddl.ts:79)). After the proposed change, any current or future caller can create a contact-scoped definition. The sole value-pair “owner” can then disappear by contact purge because existing value pairs are FK-cascaded from contacts; the planned guard’s “no pair exists” branch would allow later reassignment. Deferring durable ownership to Phase 31 therefore leaves a real data-integrity hole in the same phase that introduces the capable writer.
- **MEDIUM — merge semantics for contact-scoped definitions are deferred but the plan adds merge behavior for their histories.** If a scoped-def owner is absorbed, the plan needs an explicit definition-owner outcome, not merely reparenting child history.

### Suggestions

- Before exposing `scope='contact'` through any public writer, add durable ownership now, e.g. `owner_contact_id` with defined merge/purge/restore behavior; or keep `createField` global-only and reserve the contact-scope creation API for Phase 31.
- If the ownership model truly must wait, make contact-scoped creation impossible at runtime—not merely absent from UI—and validate migration/restore data cannot introduce it prematurely.

### Risk Assessment

**HIGH.** The value-history work is careful, but the proposed scope API invalidates its own “unreachable” rationale and can create orphaned/reclaimable scoped definitions.

## Plan 06 — Native Notes import

### Strengths

- The revised plan correctly fixes the previously missed serialization seam. Currently `acceptPickedContacts` serializes only `displayName`, `methods`, and `birthday` ([import-acquire.ts](/home/bwales/projects/orbit-app/src/services/import/import-acquire.ts:92)); therefore a type-only `note` addition would be lost.
- It correctly traces the bulk path: `pickedFromRow` reparses only `sourcePayload` ([import-driver.ts](/home/bwales/projects/orbit-app/src/services/import/import-driver.ts:54)), then `importRowAsNew` maps it and calls `importContactRecord` ([import-driver.ts](/home/bwales/projects/orbit-app/src/services/import/import-driver.ts:72), [import-driver.ts](/home/bwales/projects/orbit-app/src/services/import/import-driver.ts:88)).
- It correctly identifies the separate single-import hole: `commitSingleImport` currently calls `importContactRecord` before it even reads the session row ([import-acquire.ts](/home/bwales/projects/orbit-app/src/services/import/import-acquire.ts:120), [import-acquire.ts](/home/bwales/projects/orbit-app/src/services/import/import-acquire.ts:131)).
- The native work is rightly not considered verified by TypeScript: the current Kotlin picker has distinct requested-fields, selected, all-contact, and lookup-key read paths.

### Concerns

- **MEDIUM — single-import sequencing must be explicit in implementation.** The plan says to read the note from the session payload, but current code loads the row after `importContactRecord` completes. The executor must move row lookup/parse before that call, because otherwise the note cannot be included in the same import transaction. It should also define handling for a corrupt session payload: fail before creating the contact, rather than silently importing a contact without a note.
- **LOW — source consolidation is another JSON reparse seam.** [`source-consolidation.ts`](/home/bwales/projects/orbit-app/src/services/import/source-consolidation.ts:45) separately parses `sourcePayload`. It may not create contacts, but tests should ensure adding `note` does not break its narrowed shape or accidentally expose notes in its UI/results.

### Suggestions

- Add a test asserting a corrupt single-import `sourcePayload` produces no contact and no Memory.
- In both bulk and single tests, assert transaction rollback means neither contact nor imported Memory remains after a forced Memory insertion failure.
- Confirm Kotlin result mapping includes `note` in all three result maps, not only the MIME dispatches.

### Risk Assessment

**MEDIUM.** The cycle-6 serialization fix is materially correct and covers both intended import paths, provided implementation reads/parses the single-import row before the atomic write.

## Plan 07 — Backup and restore

### Strengths

- Correctly recognizes format 4 is already live in [types.ts](/home/bwales/projects/orbit-app/src/backup/types.ts:14), so no version bump is appropriate.
- It comprehensively identifies restore’s fan-out registry: entity list, table map, tombstone map, incoming rows, local rows, insertion branches, and replace-all deletion inventory all exist in [restore-apply.ts](/home/bwales/projects/orbit-app/src/backup/restore-apply.ts:49).
- The old-format-4 compatibility concern is valid: current validation hard-requires arrays through `requiredArrays` ([backup-schema.ts](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:259)), and format migration only runs when version is below the current version ([backup-schema.ts](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:617)).
- The plan correctly includes soft-deleted Memories: export already serializes `deleted_at` as `deletedAt` ([export-manifest.ts](/home/bwales/projects/orbit-app/src/backup/export-manifest.ts:58)).

### Concerns

- **MEDIUM — Plan 07 inherits Plan 05’s unresolved ownership model.** Scope-aware pair validation prevents valid scoped definitions from being rejected, but it cannot preserve an owner identity that is not represented in the backup schema. If scoped defs are permitted before Phase 31, backup/restore can preserve ambiguous ownership rather than repair it.

### Suggestions

- Resolve the Plan 05 ownership issue before finalizing Plan 07’s schema validation contract.
- Add an explicit restore test for a tombstone racing an incoming history row with the same UID, verifying reconciliation chooses the tombstone consistently.

### Risk Assessment

**MEDIUM.** The backup plan is otherwise thorough; its principal risk is carrying forward the scoped-definition ambiguity.

## Overall Risk Assessment

**HIGH**, driven by Plan 05’s contact-scoped-definition ownership gap. The revised Plan 06 successfully closes the previously identified note-loss path:

`PickedContact.note → acceptPickedContacts sourcePayload → pickedFromRow → mapPickedContact → importRowAsNew → importContactRecord → imported Memory`

and the plan now explicitly covers the separate `commitSingleImport` path. Once Plan 05 either implements durable scope ownership or prevents scoped-definition creation until Phase 31, the remaining phase plan is strong and executable.

---

## Claude Review

_Ran as a read-only general-purpose subagent (the built-in Claude lane self-skips inside Claude Code); owner-approved workaround. Source-grounded against the code on disk._

## Summary

Phase 24.2 (Contact Knowledge — Egress, Search, Types & Data-moves) is a well-architected 7-plan / 3-wave phase built on a tracer-first spine. I read all 7 PLAN.md files in full and verified their load-bearing claims against the actual code on disk (`src/services/import/`, `src/db/`, `src/backup/`), not against the plan text. The two cycle-5 findings that commit `d8e103a` set out to fix are **both fully resolved at the plan level**, and the disk state matches every seam the plans describe. The full-phase pass surfaced no new HIGH; a handful of LOW/informational notes below.

Migration state confirmed on disk: migrations `001–016`, `TARGET_VERSION = 16` (`src/db/database.ts:51`) — so destructive **017** + additive **018** are correctly head+1/+2. `BACKUP_FORMAT_VERSION = 4` (`src/backup/types.ts:14`), matching the "v4 already spent" amendment.

---

## Cycle-5 Findings — Resolution Status

### 1. HIGH (Plan 06 — imported note serialization seam): **FULLY RESOLVED**

The prior cycle's fix stopped at the type layer; the note was dropped at the session serialization boundary. The revised plan now fixes the actual seam, and I traced the entire path on disk:

- **The seam is exactly where the plan says.** `acceptPickedContacts` serializes `sourcePayload` as a three-field allowlist `JSON.stringify({ displayName, methods, birthday })` at `src/services/import/import-acquire.ts:92-96` — `note` is dropped. Plan 06 requires adding `note: contact.note` here (must_haves truth 2, Task 2 action FIRST, acceptance grep gate at line 150). Correct.
- **Re-parse hop confirmed:** `pickedFromRow` does `JSON.parse(row.sourcePayload) as PickedContact` at `import-driver.ts:55`; `mapPickedContact` at `picked-contact-map.ts:79-113` deliberately excludes notes (comment `:77`), and `PickedContactMapResult` (`:12`) has no `note` — both consumer hops the plan wires (FIX 3a/3b).
- **Bulk path** (`importRowAsNew`, `import-driver.ts:88`) passes only `birthday` today; plan adds `mapped.note`. **Single/review path** (`commitSingleImport`, `import-acquire.ts:120-124`) passes no note and its `input` comes from the review screen — plan threads it from the session row's `sourcePayload`. Both covered.
- **No missed hop.** I enumerated every new-contact writer: `importRowAsNew` has a *third* caller the plan doesn't name — `DuplicateReviewScreen.tsx:162` — but it routes through the same `pickedFromRow → mapPickedContact → importRowAsNew` chain, so the plan's FIX 1+3 covers it transitively. `importContactRecord` owns the single write transaction (`imported-contact-dao.ts:122`); composing `addMemoryCore` inside it is sound.
- **AI-off guarantee holds regardless of executor choices:** `addMemoryCore` (`memories-dao.ts:106-127`) does not currently write `allow_ai`; after migration 017 adds the column `INTEGER NOT NULL DEFAULT 0`, an imported memory that omits it defaults to 0. Registry `imported.aiDefault=false` (Plan 06) and the egress predicate `allow_ai = 1` (Plan 01) both agree. The T-24.2-15 default-OFF mitigation is airtight.
- The plan mandates an **end-to-end test driving the REAL `acceptPickedContacts`** (not a hand-built payload) plus a `commitSingleImport` parity test — exactly the gap the prior cycle's unit-only test masked.

### 2. MEDIUM (Plan 05 — contact-scoped-def durable ownership deferral): **FULLY RESOLVED**

Both halves the review asked for are present and the in-phase claim holds on disk:

- **Deferral is explicit and visible in three required places:** must_haves prohibition with `status: deferred` (`24.2-05-PLAN.md:59-61`); Task 3(C) FORWARD NOTE / "P05-MED DEFERRAL" paragraph (`:184-185`); threat **T-24.2-13b** = `mitigate (residual deferred)` naming `owner_contact_id` + owner-purge/merge/restore deferred to Phase 31 (`:228`). The residual is correctly characterized (pair is `ON DELETE CASCADE`, so an owner purge orphans the marker).
- **On-disk unreachability confirmed:** `grep` for `scope='contact'` / `scope: 'contact'` / `seedContactScoped` across `src/` returns **nothing**; `custom_field_defs.scope` does not exist on disk yet (migration 018 unwritten). No 24.2 writer creates a `scope='contact'` def (018 defaults `'global'`; Plan 04's picker adds none). The interim "sole-pair = owner" marker therefore cannot be orphaned in-phase. The defense-in-depth `assertContactScopedWriteAllowedCore` write guard is retained for structure regardless of UI.

---

## Strengths

- **Tracer proves the whole vertical before expansion** (Plan 01): migration 017 + egress gate + one searchable memory, end-to-end with node:sqlite tests, gated by a `checkpoint:decision` for the one-way migration.
- **Non-destructive "destructive" migration.** 017 uses copy-then-remove with per-row proof-of-copy (stronger than count equality) for both share-captures and the ADR-030 `source='ai'` retirement. Verified on disk that **zero production `source='ai'` producers exist** — only `ContactProfileScreen.tsx:654` (`user`) and `CaptureScreen.tsx:277/343/498` (`share`) write fuel; all three share writes are `kind:'topic'`, so the migration's `kind='topic' AND source='share'` SELECT captures every production share row (no orphaned captures — KNOW-16 no-loss holds).
- **`RANKED_FUEL_EXCLUSIONS` (`fuel-read.ts:133-134`) already excludes `source != 'ai'`** — Plan 01's claim that the fuel path needs no code change post-017 is correct.
- **Search corpus is structurally metadata-safe** (Plan 02): two-stage design (SQL eligibility only, no term-`LIKE`; all typo matching in TS), fuel excluded entirely, custom values via the def-id-bound `getValuesForContact` (`field-values-dao.ts:20-37`, `field_def_id IN (${placeholders})`, `?`-bound, `col_name` output-only) — the SQLi and internal-metadata prohibitions are enforceable.
- **Backup soft-delete coverage is real, not asserted:** the memories export SELECT (`export-manifest.ts:58`) already emits `m.deleted_at AS deletedAt` with **no `WHERE deleted_at IS NULL` filter**, so soft-deleted memories export. `FORBIDDEN_KEYS` contains `field_history` and not `custom_field_value_history` (`:13`). Plan 07 correctly makes the value-history a full mergeable+tombstone entity across every restore map incl. the replace-all delete inventory, and normalizes the missing top-level array at parse (avoiding the `array(undefined)` hard-fail on pre-existing format-4 backups).
- **ADR-001 uniqueness untouched:** Plan 05 adds history in a *new* table; Plan 04 keeps `custom_field_values.value` raw TEXT with one permissive parser per type via an exhaustive `Record<FieldType,…>`, `sortExpr()` (`field-sort.ts:29`) unchanged.
- **Plan 04 correctly refuses a decision reversal:** it retains the `photo` FieldType (disk confirms 7 members incl. `photo`, `schemas/types.ts:20-27`) rather than dropping it to match the dossier's "nine user-facing types" prose, and flags the CLAUDE.md "7 parsers" line as a count, not a cap (one-parser-per-type is the real invariant; KNOW-13 is owner-approved).

---

## Concerns

**LOW — Plan 06: `commitSingleImport` note-fetch ordering imprecision.** The plan says to read the note from "the session row's `sourcePayload` (the row is already fetched via `listSessionRows`)". On disk that fetch happens at `import-acquire.ts:131`, **after** the `importContactRecord` call at `:124`. To thread the note in, the executor must move that fetch (or add a targeted read) *before* `:124`. The plan's parenthetical "or add a targeted read" covers it, so this is guidance-adequate, but the "already fetched" framing is slightly misleading — worth an executor eye. Not blocking.

**LOW — Plan 06 read_first overstates current `addMemoryCore` behavior.** It says `addMemoryCore` "writes `allow_ai` seeded from registry `aiDefault`." On disk (`memories-dao.ts:106-127`) the INSERT has **no `allow_ai` column at all** — that seeding is added by *Plan 03* (`24.2-03-PLAN.md:28,45`, seed-on-create). Since Plan 03 and Plan 06 are both Wave 2 and file-disjoint (Plan 03 → `memories-dao.ts`; Plan 06 → `memory-registry.ts`/`imported-contact-dao.ts`), and the column DEFAULT 0 yields `allow_ai=0` for `imported` regardless of order, this is a documentation inaccuracy with **no functional consequence**. Note it so a reviewer of the executed code doesn't expect registry-seeding to already exist when Plan 06 lands.

**LOW — Plan 07 milestone-ordering flag is correctly surfaced, not resolved.** Plan 07's objective note (`:61`) flags that "Phase 36 owns the v4 bump" is now stale (24.1 pre-bumped to 4 in `d677e2c`) and routes it to the owner at verification rather than deciding it. This is the right call per the "reversing a recorded decision is the owner's" rule — listing it here only so the orchestrator ensures the owner actually sees it at wave-3 verification.

**INFORMATIONAL — Off Limits corpus placement is deferred to Phase 25, intentionally.** Plan 02 excludes all fuel (incl. `off_limits`) from the knowledge-search corpus and leaves fuel search in `dashboard-read.ts`; Plan 03 keeps Off Limits a separate caution control with unchanged `RANKED_FUEL_EXCLUSIONS`. This is consistent with KNOW-10/12 (Off Limits is separate) and the "Phase 25 decides combination" boundary — not a gap, but the seam a later reviewer should re-check when Phase 25 wires the Dashboard box.

---

## Suggestions

- Plan 06: reword the `commitSingleImport` step to say "fetch/parse the session row *before* the `importContactRecord` call" so the reordering is explicit rather than implied.
- Plan 06: correct the read_first line to "`addMemoryCore` seeds `allow_ai` from the registry `aiDefault` **after Plan 03** (today it relies on the migration-017 column DEFAULT 0)."
- Plan 03/Plan 06 wave-2 note: since both compose `addMemoryCore` and Plan 03 changes its INSERT, confirm at wave merge that the `imported` write still passes `tsc` and asserts `allow_ai=0` regardless of which plan lands first (both paths converge on 0, so a test is cheap insurance).

---

## Risk Assessment: **LOW**

Both cycle-5 findings are fully resolved at the plan level with disk-verified accuracy of every named seam. The phase's highest-risk artifact — the one-way migration 017 — is non-destructive (copy-then-remove, per-row proof-of-copy, integrity-error rollback, checkpoint gate) and acts on zero production `source='ai'` rows. The egress default-OFF guarantee is enforced in SQL and holds by column DEFAULT even before the registry seed lands. Backup soft-delete/round-trip coverage is structurally sound. The remaining notes are LOW/documentation-level with no functional consequence. No HIGH or MEDIUM concerns remain outstanding.
