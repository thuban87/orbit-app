---
phase: 24.2-contact-knowledge-egress-search-types
plan: 05
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-13]
files_modified:
  - src/db/migrations/018-custom-field-scope-history.ts
  - src/db/migrations/018-custom-field-scope-history.test.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/database.ts
  - src/db/field-types.ts
  - src/db/field-defs-dao.ts
  - src/db/field-ddl.ts
  - src/db/field-ddl.test.ts
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/db/field-values-dao.ts
  - src/db/value-history-dao.ts
  - src/db/value-history-dao.test.ts
  - src/db/merge-dao.ts
  - src/db/merge-dao.test.ts
  - src/db/purge-dao.ts
  - src/db/purge-dao.test.ts
  - src/db/tombstones-dao.ts
  - src/db/tombstones-dao.test.ts
user_setup: []

estimate:
  tokens: 115000
  raw_tokens: 77000
  tasks: 3
  confidence: low

assumption_delta_decision:
  noun: "custom field definition"
  decision: add-alongside
  rationale: "A definition was always global ('one global definition per field, seeded for every contact' — ADR-013/HANDOFF §14). This phase adds a per-contact one-off variant ALONGSIDE the still-default global via an additive `scope` column defaulting to 'global'. Global stays the primary/default; per-contact is the new variant. This is exactly D-12's additive scope and does not reverse ADR-013 (a global def still seeds all contacts). Accepted debt: none material — the invariant test asserts a global def still seeds every contact."

must_haves:
  truths:
    - "Migration 018 adds `custom_field_defs.scope` (default `'global'`), `custom_field_defs.history_retained` (default 0), `custom_field_defs.field_group` (nullable), and a new `custom_field_value_history` table with `uid TEXT UNIQUE`; TARGET_VERSION becomes 18. [KNOW-13]"
    - "A GLOBAL-scope def still seeds a value row for every contact (ADR-013 preserved); a PER-CONTACT def seeds a value row for ONLY its target contact — proven across both seeding writers. [KNOW-13]"
    - "For a `history_retained` def, changing an EXISTING non-null value to a different value appends the PRIOR value to `custom_field_value_history` inside the SAME transaction as the value update — and this is proven by driving the REAL production edit writer `updateContactFull` (contacts-dao.ts), NOT only the zero-production-caller `upsertValue` wrapper (HIGH-1); a first-ever set (prior NULL) appends nothing, an unchanged write appends nothing, and clearing a non-null value to NULL DOES append the prior value; a non-history def never appends; seeding (`upsertValueCore` with NULL) never appends. [KNOW-13]"
    - "A per-contact def can be promoted to global (scope 'contact'→'global') via a single atomic core (never calling the mutex-owning `field-ddl` seeding), after which the remaining contacts receive their seeded value rows. [KNOW-13]"
    - "`custom_field_value_history` has full lifecycle ownership: on contact MERGE its rows reparent to the survivor (never orphaned or CASCADE-lost); on contact PURGE its rows are tombstoned + explicitly deleted alongside the other per-contact children. This REQUIRES `custom_field_value_history` in the `TombstoneEntityType` union + `assertTombstoneEntityType` (tombstones-dao.ts), in the exhaustive `PURGE_CHILDREN` Record, AND in the separate hard-coded :318 delete array in purge-dao.ts — all three, or purge fails to compile / throws at runtime / silently drops history to FK CASCADE (HIGH-2). [KNOW-13]"
  prohibitions:
    - statement: "Migration 018 must NOT relax `UNIQUE(contact_id, field_def_id)` or `custom_field_values.uid UNIQUE`; value history lives in the NEW `custom_field_value_history` table, never in `field_history` and never by relaxing the pair uniqueness (reversing ADR-001 is an owner escalation — D-06 ESCALATE trip-wire)."
      status: enforced
      verification: "018 test asserts the pre-existing UNIQUE constraints still hold (a duplicate `(contact_id, field_def_id)` insert still fails); `field_history` is untouched."
    - statement: "`custom_field_values.value` stays raw TEXT and is never rewritten to satisfy a type; the value-history table also stores raw TEXT."
      status: enforced
      verification: "value-history-dao.test.ts asserts the appended value equals the prior raw stored bytes."
    - statement: "`custom_field_value_history` is backup-INCLUDED (unlike `field_history`) — it must NOT be added to `export-manifest.ts` FORBIDDEN_KEYS; its serialization is Plan 07."
      status: flagged-unverified
      verification: "Plan 07 emits the entity; this plan leaves FORBIDDEN_KEYS unchanged."
    - statement: "Every writer of the custom-field tables (and the new value-history table) was enumerated before altering seeding/lifecycle (D-12): field-defs-dao, field-ddl, field-values-dao (`upsertValue` wrapper AND `upsertValueCore`), field-type-change, contacts-dao (BOTH `createContactFullCore` seeding AND `updateContactFull` — the real edit writer that composes `upsertValueCore`, contacts-dao.ts:414), merge-dao, purge-dao, tombstones-dao (`TombstoneEntityType`), migration 006. The enumeration explicitly records that `updateContactFull`/`upsertValueCore` — NOT the zero-caller `upsertValue` wrapper — is the production edit path (HIGH-1)."
      status: enforced
      verification: "The read_first lists every writer; the SUMMARY records which were changed and which were verified unaffected, including contacts-dao `updateContactFull` (append site), merge-dao (reparent), purge-dao (tombstone+delete via PURGE_CHILDREN + :318 array), and tombstones-dao (union) for the value-history table."
  artifacts:
    - src/db/migrations/018-custom-field-scope-history.ts
    - src/db/value-history-dao.ts
    - src/db/field-ddl.ts (scope-aware seeding)
    - src/db/field-defs-dao.ts (promoteFieldToGlobalCore — atomic promotion)
    - src/db/contacts-dao.ts (updateContactFull — the real edit-path append site, HIGH-1)
    - src/db/merge-dao.ts + src/db/purge-dao.ts + src/db/tombstones-dao.ts (value-history reparent/purge/tombstone lifecycle, HIGH-2)
  key_links:
    - "custom_field_defs.scope → both seeding writers (contacts-dao create-seed + field-ddl def-create) — a per-contact def must not fan out to all contacts"
    - "history_retained flag → value-history append composed inside `updateContactFull`'s edit transaction (contacts-dao.ts:342/:414 — the REAL production edit writer, NOT the dead `upsertValue` wrapper), reading the PRIOR value before `upsertValueCore`'s ON CONFLICT DO UPDATE overwrites it (HIGH-1 fix)"
    - "custom_field_value_history → (a) tombstones-dao `TombstoneEntityType` union + `assertTombstoneEntityType` (or purge's tombstone write + PURGE_CHILDREN Record fail to compile/throw), (b) purge-dao `PURGE_CHILDREN` map AND the separate :318 delete array, (c) merge-dao bespoke reparent (table has no modified_at) — a merged/purged contact's history is not orphaned, silently CASCADE-lost, or an untombstoned delete (HIGH-2 fix)"
---

<objective>
Complete KNOW-13 (part 2): per-contact vs global custom-field definitions (with promotion), optional grouping, and optional value history via a new additive table — without relaxing ADR-001 uniqueness. Ships migration 018 (additive) and updates every seeding writer.

Purpose: History-retained + per-contact custom fields, structurally correct and backup-portable, on the ADR-001 model untouched.
Output: migration 018 (scope/history_retained/field_group columns + `custom_field_value_history` table, TARGET_VERSION 18), scope-aware seeding writers, a value-history DAO, and def promotion.

**Scope boundary (owner-approved 2026-09-04 — deferred UI, from plan-check W1/W2):** This plan ships the value-history and grouping as **data-layer only** — the `custom_field_value_history` table, `value-history-dao.ts` (append + backlist READ), and the `field_group` column/seeding. **Do NOT build a value-history backlist screen or grouped-field rendering here** — both are deferred to the Profile/custom-fields UI work (ROADMAP Phase 31 "Consumes from Phase 24.2"). The DAO read exists so a later phase can consume it, mirroring how the search corpus is exposed here and consumed in Phase 25. Task 3's "backlist read" is the DAO function only, no `.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@docs/dossier/milestone-2/planning-notes/phase-03-planning-notes.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Migration 018 — scope/history/group columns + value-history table</name>
  <files>src/db/migrations/018-custom-field-scope-history.ts, src/db/migrations/018-custom-field-scope-history.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts, src/db/field-types.ts</files>
  <read_first>
    - src/db/migrations/017-knowledge-egress-datamove.ts (the immediately-prior migration; confirm head+1 = 018 on disk before writing)
    - src/db/migrations/007-tombstones.ts (additive `ALTER TABLE … ADD COLUMN` + `CREATE TABLE` idiom; `uid TEXT UNIQUE`)
    - src/db/migrations/006-normalize-custom-field-values.ts (the `custom_field_values` schema + `UNIQUE(contact_id, field_def_id)` + `uid UNIQUE` — DO NOT relax)
    - src/db/database.ts (MIGRATIONS registration + TARGET_VERSION)
    - src/db/field-types.ts (`CustomFieldDef` row type — add `scope`, `history_retained`, `field_group`)
  </read_first>
  <action>
    Create migration `018-custom-field-scope-history.ts`, `version: 18`. In `apply`, one statement per step:
    (1) `ALTER TABLE custom_field_defs ADD COLUMN scope TEXT NOT NULL DEFAULT 'global';`
    (2) `ALTER TABLE custom_field_defs ADD COLUMN history_retained INTEGER NOT NULL DEFAULT 0;`
    (3) `ALTER TABLE custom_field_defs ADD COLUMN field_group TEXT;`
    (4) `CREATE TABLE custom_field_value_history (id INTEGER PRIMARY KEY, uid TEXT UNIQUE NOT NULL, contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE, field_def_id INTEGER NOT NULL REFERENCES custom_field_defs(id) ON DELETE CASCADE, value TEXT, created_at TEXT NOT NULL);`
    (5) an index `CREATE INDEX idx_cf_value_history ON custom_field_value_history(contact_id, field_def_id, created_at);`.
    Do NOT touch the `custom_field_values` pair UNIQUE or `uid UNIQUE`. Register `migration018` after `migration017`; set `TARGET_VERSION = 18`. Add `scope: string`, `history_retained: number`, `field_group: string | null` to the `CustomFieldDef` row type. Write the migration test (empty DB → 18) asserting the columns/table/index exist and that the pre-existing pair UNIQUE still rejects a duplicate; extend `full-chain.test.ts` to 018.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/018-custom-field-scope-history.test.ts src/db/migrations/full-chain.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION = 18" src/db/database.ts` matches; `migration018` follows `migration017` in `MIGRATIONS`.
    - `PRAGMA table_info(custom_field_defs)` shows `scope` (default `'global'`), `history_retained` (default 0), `field_group`.
    - `custom_field_value_history` exists with a `uid` UNIQUE column; a duplicate `custom_field_values (contact_id, field_def_id)` insert still fails.
    - `field_history` is unchanged.
  </acceptance_criteria>
  <done>Additive migration 018 lands the scope/history/group columns and the backup-includable value-history table; ADR-001 constraints intact.</done>
</task>

<task type="auto">
  <name>Task 2: Scope-aware seeding writers + def CRUD + promotion</name>
  <files>src/db/field-defs-dao.ts, src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <read_first>
    - src/db/contacts-dao.ts (create-seeding loop ~line 198-211: `listDefs(...includeQuarantined)` then `upsertValueCore` per def — the FIRST seeding writer)
    - src/db/field-ddl.ts (`createField` ~line 55-94: seeds `upsertValueCore` for every contact — the SECOND seeding writer; also the drop cores)
    - src/db/field-defs-dao.ts (def reads/writes: `listDefs`, `NewFieldDef`, any def-update path — where `scope`/`field_group`/`history_retained` are written and where promotion updates scope)
    - src/db/field-values-dao.ts (`upsertValueCore` signature — the seeding primitive)
    - src/db/field-type-change.ts and src/db/purge-dao.ts (the remaining custom-field writers — verify they need no change for the scope column; record the verification, D-12)
    - HANDOFF.md §14 + docs/dossier/.../phase-03-planning-notes.md R-08 (the seeding trip-wire)
  </read_first>
  <action>
    Thread the `scope` column through def creation and seeding (D-12). In `field-defs-dao.ts` extend `NewFieldDef` (and the def INSERT / reads) with `scope` (`'global' | 'contact'`), `history_retained`, and `field_group`.
    Add promotion as a single ATOMIC core to avoid the non-reentrant-mutex deadlock (transaction.ts:12; each existing field-defs-dao mutator — renameField/reorderFields/etc. — owns its OWN `inWriteTransaction`, and `field-ddl.createField` also owns one): expose `promoteFieldToGlobalCore(exec, { fieldDefId, now })` — a NON-mutexed core that (a) `UPDATE custom_field_defs SET scope='global', modified_at=? WHERE id=?`, (b) SELECTs the contacts that have NO `custom_field_values` pair for this def, (c) `upsertValueCore(exec, contactId, fieldDefId, newUid(), null, now)` for each missing contact, (d) `bumpDataRevisionCore(exec)` — and a thin mutexed wrapper `promoteFieldToGlobal` that runs the core inside exactly ONE `inWriteTransaction`. The core MUST NOT call `field-ddl.createField` or any public mutexed writer (that nests the mutex → deadlock).
    In `field-ddl.ts` `createField`: accept the `scope`; when `scope='global'` seed every contact (unchanged behavior); when `scope='contact'` REQUIRE a target `contactId` argument and seed ONLY that one contact — never fan out a one-off to all contacts. Enforce the ownership invariant in code: a `scope='contact'` create without a target `contactId` throws (a contact-scoped def must have exactly one seeded owner — this is how the owner is recorded, since there is no separate owner column; the single pair row identifies the owner).
    In `contacts-dao.ts` create-seeding: seed the new contact ONLY from GLOBAL-scope defs (`listDefs` filtered to `scope='global'`, still including quarantined per the existing includeQuarantined flag) — a per-contact def owned by another contact must not seed this new contact. Route through the existing `upsertValueCore`; never nest the non-reentrant mutex.
    Verify `field-type-change.ts` needs no scope change and record it in the SUMMARY (value-history merge/purge lifecycle is handled in Task 3). Add node:sqlite tests: a global def seeds all contacts; a per-contact def seeds only its target; a `scope='contact'` create without a target contactId throws; a new contact is seeded only from global defs; promotion (`promoteFieldToGlobal`) seeds exactly the previously-unseeded contacts and leaves the existing owner's row intact.
  </action>
  <verify>
    <automated>npx vitest run src/db/field-ddl.test.ts src/db/contacts-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A test creates a global def with 3 contacts present and asserts 3 seeded value rows; a per-contact def asserts exactly 1 seeded row for the target; a `scope='contact'` create with no target contactId throws.
    - A test creates a new contact AFTER a per-contact def exists and asserts the new contact is NOT seeded that def (only global defs).
    - A promotion test calls `promoteFieldToGlobal`, asserts scope flipped to 'global', the previously-unseeded contacts now have seeded rows, and the original owner's row is unchanged; `grep -n "createField\|inWriteTransaction" src/db/field-defs-dao.ts` confirms `promoteFieldToGlobalCore` calls NO mutexed writer / opens no nested transaction.
    - The SUMMARY lists every custom-field writer read and states which changed vs verified-unaffected.
  </acceptance_criteria>
  <done>Global vs per-contact scope is honored by both seeding writers and promotion, with ADR-013 "global seeds all" preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Value-history DAO (append + read) + merge/purge lifecycle</name>
  <files>src/db/value-history-dao.ts, src/db/value-history-dao.test.ts, src/db/field-values-dao.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/merge-dao.ts, src/db/merge-dao.test.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/tombstones-dao.ts, src/db/tombstones-dao.test.ts</files>
  <read_first>
    - src/db/field-values-dao.ts (VERIFIED: `upsertValueCore` at :64 uses `INSERT ... ON CONFLICT(contact_id, field_def_id) DO UPDATE` — ONE path for both first-set and update, and it does NOT read the prior value. `upsertValue` at :45 is the wrapper — and it has ZERO production callers: a repo-wide `grep "upsertValue\b"` outside `*.test.ts` returns only its definition + two doc-comments (contacts-dao.ts:9, field-defs-dao.ts:12). So wiring the append into `upsertValue` is a DEAD PATH — HIGH-1. The append must be composed where production custom-field edits actually happen: see contacts-dao below.)
    - src/db/contacts-dao.ts (VERIFIED — the REAL edit writer, HIGH-1: `updateContactFull` at :342 opens ONE `inWriteTransaction` at :375 and loops `upsertValueCore` at :412-422 for each submitted custom value; it imports `upsertValueCore` (line 61), NOT the wrapper. `EditContactScreen.tsx:420` calls it. This loop is the append site: read `history_retained` + the CURRENT stored value per pair BEFORE `upsertValueCore` overwrites. Also VERIFIED: `createContactFullCore` seeds NULL values via `upsertValueCore` at :203 and submitted create-values at :217 — both are first-ever sets/NULL, so they must NEVER append; keep `upsertValueCore` a pure primitive with no append inside it.)
    - src/db/fuel-dao.ts (core/wrapper split; compose Cores in ONE transaction — never nest the mutex)
    - src/db/field-defs-dao.ts (`history_retained` flag + `scope` on the def — the gate that decides whether to append, and the owner-scope for the P05-MED write guard)
    - src/db/merge-dao.ts (VERIFIED: reparent at :180 iterates a FIXED table list that OMITS `custom_field_value_history`; `field_history` gets a bespoke `UPDATE ... SET contact_id` at :181 because it has no `modified_at` — the value-history table likewise has `created_at` not `modified_at`, so it needs the SAME bespoke treatment, NOT the generic `reparent()` helper which sets `modified_at`)
    - src/db/tombstones-dao.ts (VERIFIED — HIGH-2: `TombstoneEntityType` is a CLOSED union at :8-21 with NO `custom_field_value_history`; `assertTombstoneEntityType` at :35-56 is an exhaustive `switch` that THROWS on an unknown type; `insertTombstoneCore` at :59 calls it. Both the union AND the switch must gain the new member or purge's tombstone write throws at runtime. This file is NOT the same registry as backup-schema's `TOMBSTONE_ENTITY_TYPES` — that one is Plan 07's.)
    - src/db/purge-dao.ts (VERIFIED — HIGH-2: the map is `PURGE_CHILDREN` at :80, NOT `PURGE_ENTITIES`; it is an EXHAUSTIVE `Record<TombstoneEntityType, PurgeChildSpec | null>` so adding a `custom_field_value_history` entry will NOT COMPILE until the union (tombstones-dao) gains the member; `custom_field_value` at :85 shows the countSql/tombstoneSql/deleteSql shape to mirror. The tombstone write loop at :291 iterates `Object.entries(PURGE_CHILDREN)` and passes the key to `insertTombstoneCore` as a `TombstoneEntityType`. The EXPLICIT per-row deletes are a SEPARATE non-exhaustive hard-coded `as const` array at :318 ("Not relying on FK CASCADE — auditable") — omitting the entity there COMPILES but silently drops history to FK CASCADE. Also note the `KNOWLEDGE_CHILD_TABLES` availability pattern at :96 that skips late-added tables on older-schema fixtures.)
    - src/db/uid.ts (`newUid`) and src/utils/dates.ts (`formatLocalDate` / local timestamp)
    - .planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md (§value-history-dao — additive table, one row per historical value, current stays the pair row)
  </read_first>
  <action>
    **(A) value-history-dao.ts.** Create it with THREE exports: (1) non-mutexed `appendValueHistoryCore(exec, {contactId, fieldDefId, value, now})` inserting one `custom_field_value_history` row (`newUid()`, raw TEXT value, `created_at=now`); (2) non-mutexed `maybeAppendPriorValueHistoryCore(exec, {contactId, fieldDefId, incomingValue, now})` — the decision helper: it reads the def's `history_retained` flag AND the CURRENT stored `custom_field_values.value` for `(contactId, fieldDefId)`, then calls `appendValueHistoryCore` with the PRIOR value IF AND ONLY IF `history_retained = 1` AND the prior stored value is NON-NULL AND the prior value differs from `incomingValue` (first-ever set (prior NULL) → NO append; unchanged (prior == incoming) → NO append; change x→y → append `x`; clear x→NULL → append `x`; non-history def → NO append); (3) read `listValueHistory(exec, contactId, fieldDefId)` returning entries newest-first for the (deferred, Phase 31) drill-in backlist — DAO only, no `.tsx`.
    **(B) HIGH-1 — wire the append into the REAL edit writer, NOT the dead wrapper.** In `contacts-dao.ts` `updateContactFull` (the metadata-edit writer, :342), inside its EXISTING `inWriteTransaction`, in the `customValues` loop (:412-422): call `maybeAppendPriorValueHistoryCore(exec, {contactId: input.id, fieldDefId: customValue.fieldDefId, incomingValue: customValue.value, now: input.now})` BEFORE `upsertValueCore(...)` overwrites the pair (it must read the prior value first). This is the fix for the cycle-2 HIGH: the previously-planned wrapper `upsertValue` (field-values-dao.ts:45) has ZERO production callers, so the real Edit-Contact form (`EditContactScreen.tsx:420` → `updateContactFull` → `upsertValueCore` loop) would have recorded NO history while unit tests passed. Do NOT put the append inside `upsertValueCore` itself — `createContactFullCore` calls it for NULL seeding (:203) and first-set create values (:217), which must never append; keeping the primitive pure preserves seeding. (The `upsertValue` wrapper stays a dead-but-harmless convenience; do not rely on it for history — a comment there should point at `updateContactFull` as the real edit path.)
    **(C) P05-MED — scoped-writer guard (defense-in-depth).** Add a non-mutexed `assertContactScopedWriteAllowedCore(exec, contactId, fieldDefId)` (in field-values-dao.ts, co-located with the writers): for a def whose `scope='contact'`, THROW if a `custom_field_values` pair for this def already exists on a DIFFERENT contact AND none exists for `contactId` (i.e. writing would create a SECOND owner pair for a contact-scoped def on a non-owner). A `scope='global'` def, or the def's existing owner, or the initial owner-seed (no pair anywhere yet), all pass. Call it at the start of `updateContactFull`'s `customValues` loop (before the append + upsert) and in `promoteFieldToGlobalCore`'s seeding is exempt (promotion makes the def global). Task 2 hardened create-SEEDING; this closes the value-WRITE path the review flagged. (Scope-aware EDIT-form filtering — hiding a per-contact def from non-owners in the UI — is the deferred Profile/custom-fields work, Phase 31; this write guard is the structural guarantee regardless of UI. FORWARD NOTE / Phase-31 prerequisite (review Claude-LOW): because `assertContactScopedWriteAllowedCore` THROWS and rolls back `updateContactFull` when a non-owner submits a value — even an empty one — for a `scope='contact'` def, and `defsForEditForm` (field-values-dao.ts:117) today returns EVERY non-quarantined def to EVERY contact, edit-form scope filtering is a HARD prerequisite that MUST land in the same phase that first exposes `scope='contact'` def creation. No in-phase 24.2 UI creates a contact-scoped def (Plan 04's picker adds none; all migration-018 defs default `scope='global'`, which passes the guard), so this is not reachable in 24.2 — but Phase 31 must not ship scoped-def creation UI without also making the edit form skip non-owner contact-scoped defs, or a routine edit throws.)
    **(D) HIGH-2 — tombstone/purge/merge lifecycle across THREE files.**
    In `tombstones-dao.ts`: add `"custom_field_value_history"` to BOTH the `TombstoneEntityType` union (:8) AND the `assertTombstoneEntityType` switch (:35) — without both, purge's tombstone write throws at runtime and `PURGE_CHILDREN` won't compile.
    In `purge-dao.ts`: add a `custom_field_value_history` entry to the exhaustive `PURGE_CHILDREN` map (:80 — the CORRECT symbol name, NOT `PURGE_ENTITIES`), mirroring `custom_field_value` (countSql/tombstoneSql `SELECT uid … WHERE contact_id = ?`, deleteSql `DELETE FROM custom_field_value_history WHERE contact_id = ?`); AND add `"custom_field_value_history"` to the SEPARATE hard-coded per-row delete array at :318 (or history rows silently fall to FK CASCADE with no tombstone, contradicting the auditable posture). For robustness on older-schema test fixtures, mirror the `KNOWLEDGE_CHILD_TABLES` availability pattern (:96) so the tombstone loop + delete loop skip the table when it is absent (pre-018), matching how memories/relationships are guarded. This is a TWO-edit change and BOTH are required (review P05-LOW): (i) add `custom_field_value_history: "custom_field_value_history"` to the `KNOWLEDGE_CHILD_TABLES` map (:96), AND (ii) add `'custom_field_value_history'` to the hard-coded `sqlite_master … WHERE … name IN (…)` list inside `availableKnowledgeChildren` (:107-111). Editing only the map (or neither) is a latent bug: `isAvailablePurgeChild` (:123) returns `true` for any entity NOT in the map, so a map-only add still leaves the SELECT list stale — the availability set never reports the table present, and on a head-migrated DB where the table DOES exist the guard would wrongly skip it (or, mirrored the other way, attempt a SELECT against a missing table on a pre-018 fixture). The map key and the `IN (…)` literal must both name `custom_field_value_history`.
    In `merge-dao.ts`: add a bespoke `UPDATE custom_field_value_history SET contact_id = ? WHERE contact_id = ?` (survivor ← absorbed) alongside the existing `field_history` reparent at :181, BEFORE the absorbed contact is deleted, so the merged contact's history moves to the survivor instead of being CASCADE-lost. (Do NOT add it to the generic `reparent()` list at :180 — that helper sets `modified_at`, which this table lacks.)
    (Cross-plan: `TombstoneEntityType` here (purge) is a DISTINCT registry from Plan 07's `TOMBSTONE_ENTITY_TYPES` in backup-schema.ts (restore validation) — this plan owns the former, Plan 07 owns the latter; both must be updated, do not conflate them.)
    **(E) Tests.** Add node:sqlite tests for the append semantics AND the merge/purge lifecycle AND the scoped-writer guard (see acceptance criteria).
  </action>
  <verify>
    <automated>npx vitest run src/db/value-history-dao.test.ts src/db/contacts-dao.test.ts src/db/merge-dao.test.ts src/db/purge-dao.test.ts src/db/tombstones-dao.test.ts && npx tsc --noEmit</automated>
    <human-check>BELT-AND-SUSPENDERS for HIGH-1 (the automated `updateContactFull` real-path test above is the PRIMARY catch; this device check is the review-requested confirmation that the actual UI edit path records history). On the Pixel (debug build, run-as DB read per the device-UAT run-as pattern): with a `history_retained=1` custom-field def present, edit that field's value on a contact through the Edit-Contact screen, then read `custom_field_value_history` via `run-as` and confirm a row holding the PRIOR value now exists. If no in-phase UI sets `history_retained` (the flag/backlist UI is deferred to Phase 31), seed the def's flag directly for the check and record that in the SUMMARY — the automated real-path test remains the binding gate.</human-check>
  </verify>
  <acceptance_criteria>
    - Append semantics tested at every boundary: first-set (prior NULL) → 0 history rows; unchanged write → 0 rows; change x→y → 1 row holding `x`; clear x→NULL → 1 row holding `x`; a non-history def → 0 rows on any change. Changing a history value twice yields two rows newest-first via `listValueHistory`, each equal to the prior raw bytes.
    - HIGH-1 REAL-PATH PROOF: a test drives the production edit writer `updateContactFull` (contacts-dao.ts) — NOT a direct `upsertValue`/`appendValueHistoryCore` call — editing a `history_retained` custom value from x→y and asserts a `custom_field_value_history` row holding `x` now exists; and an edit through `updateContactFull` on a NON-history def asserts 0 history rows. `grep -n "maybeAppendPriorValueHistoryCore\|appendValueHistoryCore" src/db/contacts-dao.ts` matches inside `updateContactFull` (the append is wired to the real edit path, not only the dead `upsertValue` wrapper).
    - The append and the current-value UPDATE occur in ONE transaction (a forced rollback inside `updateContactFull` leaves neither the new value nor the history row).
    - P05-MED guard: a test asserts writing a value for a `scope='contact'` def on a NON-owner contact throws (no second pair created), while the owner's edit and any global def pass. `grep -n "assertContactScopedWriteAllowedCore" src/db/field-values-dao.ts src/db/contacts-dao.ts` matches.
    - HIGH-2 compile-time: `custom_field_value_history` is in the `TombstoneEntityType` union AND `assertTombstoneEntityType` (tombstones-dao.ts) AND the exhaustive `PURGE_CHILDREN` Record (`grep -n "custom_field_value_history" src/db/tombstones-dao.ts src/db/purge-dao.ts` matches in all three) — `tsc --noEmit` is clean (the exhaustive Record would otherwise fail).
    - HIGH-2 runtime purge: a purge test on a contact with history rows asserts (a) a `custom_field_value_history` TOMBSTONE row is written (not just a delete), and (b) the history rows are gone; the entity appears in BOTH the `PURGE_CHILDREN` map AND the separate :318 delete array (`grep -c "custom_field_value_history" src/db/purge-dao.ts` ≥ 2).
    - P05-LOW availability guard (BOTH edits gated): `custom_field_value_history` appears in the `KNOWLEDGE_CHILD_TABLES` map AND in the `sqlite_master … IN (…)` list inside `availableKnowledgeChildren` — `grep -n "custom_field_value_history" src/db/purge-dao.ts` matches inside the `IN ( … )` SELECT literal (:107-111), not only the map. A pre-018 fixture purge test (a contact on a DB migrated only to 017, where the table does not exist) runs without throwing (the availability guard skips the absent table), while the head-migrated purge test above still tombstones+deletes it — proving the guard reports the table present when it exists and absent when it does not.
    - A merge test: absorbed contact has history rows; after merge they belong to the survivor (0 remain on the absorbed uid, none lost). `grep -n "custom_field_value_history" src/db/merge-dao.ts` matches (the bespoke reparent, not the generic `reparent()` list).
  </acceptance_criteria>
  <done>History-retained custom fields accumulate an append-only, backup-includable value timeline (correct from-NULL/no-op/clear semantics) atomic with the current value — recorded via the REAL edit writer `updateContactFull` (HIGH-1), guarded against non-owner writes on contact-scoped defs (P05-MED), and with full merge/purge/tombstone lifecycle ownership across merge-dao, purge-dao (`PURGE_CHILDREN` + :318 array), and tombstones-dao (`TombstoneEntityType`) (HIGH-2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration 018 → local DB | Additive schema on the user's irreplaceable DB |
| def scope → seeding fan-out | A per-contact def must not leak value rows to every contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-12 | Denial of Service (data) | migration 018 | medium | mitigate | Additive-only, forward-only, no assumed start state, per-step transaction; ADR-001 constraints preserved |
| T-24.2-13 | Tampering (data integrity) | value-history append | medium | mitigate | Composed inside the REAL edit writer `updateContactFull` (HIGH-1 — not the dead `upsertValue` wrapper); atomic with the value UPDATE; prior read before overwrite; from-NULL/no-op/clear rules tested via the real edit path; raw TEXT preserved; new table, `field_history` untouched |
| T-24.2-13b | Tampering / Elevation | contact-scoped value write | medium | mitigate | `assertContactScopedWriteAllowedCore` rejects a second-owner pair for a `scope='contact'` def on a non-owner contact (P05-MED); write-boundary guard, not UI-only |
| T-24.2-14 | Information Disclosure | value-history backup | medium | mitigate | Backup-INCLUDED (not FORBIDDEN_KEYS); serialized + tombstoned in Plan 07 |
| T-24.2-14b | Denial of Service (data loss) | value-history merge/purge | medium | mitigate | Merge reparents history to the survivor (not CASCADE-lost); purge tombstones+deletes it with its siblings — requires the `TombstoneEntityType` union + `PURGE_CHILDREN` map + the :318 delete array all updated (HIGH-2), or the entity is silently CASCADE-dropped or throws; both tested |
</threat_model>

<verification>
- `npx vitest run src/db/migrations/018-custom-field-scope-history.test.ts src/db/migrations/full-chain.test.ts src/db/field-ddl.test.ts src/db/contacts-dao.test.ts src/db/value-history-dao.test.ts src/db/merge-dao.test.ts src/db/purge-dao.test.ts src/db/tombstones-dao.test.ts`
- `npx tsc --noEmit`
- `npm test` at wave merge.
</verification>

<success_criteria>
Global/per-contact custom-field definitions (with promotion), optional grouping, and optional atomic value history via a new backup-includable table — ADR-001 uniqueness untouched.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-05-SUMMARY.md` when done.
</output>
