---
phase: 24.2-contact-knowledge-egress-search-types
plan: 03
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-11, KNOW-12]
files_modified:
  - src/db/memories-dao.ts
  - src/db/memories-dao.test.ts
  - src/components/icons/icon-registry.ts
  - src/components/icons/icon-registry.test.ts
  - src/components/MemoryEditor.tsx
  - src/screens/ThingsToRememberScreen.tsx
  - src/components/MemoryCard.tsx
  - src/components/FuelEditor.tsx
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new Memory is created with `allow_ai` seeded from its type's registry `aiDefault` (all current types = false → 0); changing a type's `aiDefault` affects only newly created items, never existing rows. [KNOW-11]"
    - "`setMemoryAllowAi` toggles one memory's `allow_ai` (0↔1), scoped by both `id` and `contact_id`, asserting exactly one row changed, and marks the DB dirty for backup inside one mutex. [KNOW-11]"
    - "The Memory editor surfaces a per-item 'Allow AI to use this' control defaulting OFF; because `MemoryEditor` owns no DAO (it delegates persistence via callback props), the toggle fires a new `onSetAllowAi(id, allow)` prop that the PARENT `ThingsToRememberScreen` wires to `setMemoryAllowAi` + `load()`. [KNOW-11]"
    - "Gate 1 (global AI enabled) is read from the durable `app_settings.ai_provider` (`!== 'none'`), NOT the secure key store; when `ai_provider === 'none'` the control is disabled with the helper 'Turn on AI in Settings first' (two-gate model). [KNOW-11]"
    - "An AI-enabled item renders the semantic `sparkle` glyph tinted `accentText` with accessible label 'Shared with AI'; an AI-off item shows no marker (absence is the off state). [KNOW-11]"
    - "Off Limits remains a SEPARATE control from AI permission, stays visible by default, is styled as caution (`borderStrong` + neutral pill, never `danger`), and carries the avoid-topic semantic copy 'A topic to avoid bringing up.' where it surfaces. [KNOW-12]"
  prohibitions:
    - statement: "Off Limits must not be merged into the AI-permission control, hidden, or restyled as `danger`; its SQL-level exclusion in fuel-read.ts stays unchanged unless a superseding ADR exists."
      status: enforced
      verification: "grep confirms `RANKED_FUEL_EXCLUSIONS` in fuel-read.ts is unchanged; a grep of docs/decisions for an ADR superseding ADR-036/050/078 is run before any exclusion edit (none expected); FuelEditor keeps a distinct off-limits treatment from the AI toggle."
    - statement: "AI permission is explicit per-item state, default OFF; the sparkle is never a `danger`/negative marker and never appears on an AI-off item."
      status: enforced
      verification: "icon-registry.test.ts asserts the `sparkle` entry exists; MemoryCard renders no sparkle when `allow_ai=0`."
    - statement: "All colours resolve through theme tokens (no hex literals, including any Skia/icon tint)."
      status: enforced
      verification: "`npm run check:colors` clean across changed files."
  artifacts:
    - src/db/memories-dao.ts (setMemoryAllowAi + allow_ai seed-on-create)
    - src/components/icons/icon-registry.ts (sparkle entry)
    - src/components/MemoryEditor.tsx (per-item AI toggle via new `onSetAllowAi` callback prop)
    - src/screens/ThingsToRememberScreen.tsx (parent wiring: setMemoryAllowAi + load(); gate-1 from app_settings.ai_provider)
  key_links:
    - "MemoryEditor `onSetAllowAi` prop → ThingsToRememberScreen `setMemoryAllowAi` → memories.allow_ai → the Plan-01 AI-egress read; the ENFORCEMENT of eligibility is the `allow_ai = 1` SQL predicate in the egress read (Plan 01), not UI exclusivity — the toggle is one write path, the SQL gate is the guarantee"
    - "app_settings.ai_provider (!= 'none') → toggle enabled/disabled state (gate 1 of the two-gate model)"
---

<objective>
Complete KNOW-11 (per-item AI permission model) and KNOW-12 (Off Limits as a separate, visible, avoid-topic control) at the write + UI layers, on top of the tracer's `allow_ai` column and egress read.

Purpose: Make the opt-in real and glanceable — a two-gate, default-OFF per-item permission with the sparkle affordance, while keeping Off Limits a distinct caution control.
Output: `setMemoryAllowAi` writer + registry-seeded create default; the `sparkle` icon-registry entry; the Memory editor AI toggle + sparkle marker; the updated avoid-topic Off Limits copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-UI-SPEC.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: allow_ai writer + registry-seeded create default</name>
  <files>src/db/memories-dao.ts, src/db/memories-dao.test.ts</files>
  <behavior>
    - `addMemory` for a type whose registry `aiDefault` is false inserts `allow_ai = 0`.
    - `setMemoryAllowAi(exec, {id, contactId, allow: true, now})` sets `allow_ai=1`, `modified_at=now`, asserts one row changed.
    - `setMemoryAllowAi` with a mismatched `(id, contactId)` pair changes 0 rows and throws (both-keys scoping).
    - Toggling `allow_ai` bumps the data revision (DB marked dirty for backup).
  </behavior>
  <read_first>
    - src/db/memories-dao.ts (`addMemoryCore` INSERT column list — add `allow_ai`; the core/wrapper split; `assertOneChange`; `bumpDataRevisionCore`)
    - src/db/memory-registry.ts (`MEMORY_TYPE_REGISTRY[type].aiDefault`)
    - src/db/fuel-dao.ts (both-keys scoping + `assertOneChange` idiom, for the setter shape)
  </read_first>
  <action>
    In `memories-dao.ts`: add `allow_ai` to the `addMemoryCore` INSERT, seeding its value from `MEMORY_TYPE_REGISTRY[input.type].aiDefault ? 1 : 0` (type-level default applied at creation only — never re-applied to existing rows). Add a non-mutexed `setMemoryAllowAiCore(exec, {id, contactId, allow, now})` performing a single `UPDATE memories SET allow_ai = ?, modified_at = ? WHERE id = ? AND contact_id = ?` with `assertOneChange`, and a mutexed wrapper `setMemoryAllowAi` that calls the core then `bumpDataRevisionCore` inside one `inWriteTransaction`. Do not touch any other column.
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allow_ai" src/db/memories-dao.ts` shows it in the INSERT column list and the `setMemoryAllowAiCore` UPDATE.
    - Test cases cover each `<behavior>` bullet including the mismatched-pair rollback.
    - The setter never writes `deleted_at`, `type`, `value`, or any column other than `allow_ai` + `modified_at`.
  </acceptance_criteria>
  <done>The per-item permission has a scoped, tested writer and a registry-seeded creation default.</done>
</task>

<task type="auto">
  <name>Task 2: sparkle icon-registry entry</name>
  <files>src/components/icons/icon-registry.ts, src/components/icons/icon-registry.test.ts</files>
  <read_first>
    - src/components/icons/icon-registry.ts (the `ICON_REGISTRY` `{ outline, filled }` map + `IconName` type; D-05 — one icon source)
    - .planning/phases/24.2-contact-knowledge-egress-search-types/24.2-UI-SPEC.md (§New registry entry — `sparkle` = `sparkles-outline`/`sparkles`, tinted `accentText`)
  </read_first>
  <action>
    Add ONE `sparkle: { outline: "sparkles-outline", filled: "sparkles" }` entry to `ICON_REGISTRY` (the single AI-enabled glyph, D-05). Do not add a second icon source and do not reference a raw Ionicons name in any screen. Extend icon-registry.test.ts to assert the `sparkle` name resolves.
  </action>
  <verify>
    <automated>npx vitest run src/components/icons/icon-registry.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sparkle" src/components/icons/icon-registry.ts` matches an `{ outline, filled }` pair.
    - `IconName` now includes `sparkle` (tsc-enforced); a screen can request `sparkle` without a raw glyph string.
  </acceptance_criteria>
  <done>The semantic AI glyph exists in the central registry.</done>
</task>

<task type="auto">
  <name>Task 3: Memory editor AI toggle (parent-wired) + sparkle marker + Off Limits avoid-topic copy</name>
  <files>src/components/MemoryEditor.tsx, src/screens/ThingsToRememberScreen.tsx, src/components/MemoryCard.tsx, src/components/FuelEditor.tsx</files>
  <read_first>
    - src/components/MemoryEditor.tsx (VERIFIED: it imports NO writer DAO and delegates persistence via callback props `onAdd`/`onEdit`/`onDelete`/`onRestore` in `MemoryEditorProps` at :60-68 — the comment at :176 says "parent-owned persistence; it deliberately imports no writer DAO". The toggle MUST follow this pattern via a NEW callback prop, NOT a direct DAO call.)
    - src/screens/ThingsToRememberScreen.tsx (VERIFIED: the parent that renders MemoryEditor and owns `load()` + the DAO calls — this is where `setMemoryAllowAi` is invoked)
    - src/components/MemoryCard.tsx (glanceable memory card — markers render as `AppText` today at :57-78; `MemoryRow` gains `allow_ai` from Plan 01)
    - src/components/icons/Icon.tsx (`Icon` component, `IconProps { name: IconName }` — render the sparkle through this, not a raw glyph)
    - src/components/FuelEditor.tsx (existing off-limits treatment: `isOffLimits`, `borderStrong` outline + pill; the legacy off-limits helper copy to update)
    - .planning/phases/24.2-contact-knowledge-egress-search-types/24.2-UI-SPEC.md (§Copywriting: 'Allow AI to use this' / 'Turn on AI in Settings first' / 'Shared with AI'; Off-Limits marker 'A topic to avoid bringing up.'; §Color rules for sparkle=`accentText`, off-limits=caution never `danger`)
    - src/db/app-settings-dao.ts (VERIFIED gate-1 source: `getAppSettings(exec)` at :381 — the only reader of the FULL `AppSettings` shape (`ai_provider` alone is also read by `getPortableSettingsSnapshot` :468 and `updateAppSettingsCore` :855 — doc-only precision, the fix mechanism is unaffected); returns `aiProvider` at :438 from the `ai_provider` column, default `'none'`; it is async and THROWS if the id=1 row is missing, so the parent must load it into state and fail-closed to `false`. There is NO `getAiSettings` export. Gate 1 = `aiProvider !== 'none'`, a durable/backup-portable flag — NOT the secure key store. NAMING/SEAM (review P03-LOW): treat `aiProvider !== 'none'` as the INTERIM "AI configured/available" gate, not the durable global AI master toggle — the two are not equivalent. Phase 36 (AI Configuration & Prompting) owns the real master-enabled state and MUST replace or compose this interim gate with it; keep gate-1 a single computed boolean so Phase 36 can swap its source without touching the toggle wiring.)
  </read_first>
  <action>
    In `MemoryEditor.tsx`: add a REQUIRED `onSetAllowAi: (id: number, allow: boolean) => void` callback AND a REQUIRED `globalAiEnabled: boolean` prop to `MemoryEditorProps` (mirroring the existing `onDelete`/`onRestore` prop shape). Making BOTH required is deliberate (addresses review concern P03 / Claude-LOW: there are TWO render sites — see below — and a required prop makes `tsc` fail if either site forgets to pass it, so a missed site cannot silently ship an unwired or always-enabled toggle). Add a per-item 'Allow AI to use this' `Switch` control (default OFF, reflecting the row's `allow_ai`) whose `onValueChange` calls `onSetAllowAi(row.id, next)`. Do NOT import a DAO here (the component deliberately has none). When `globalAiEnabled` is false, render the control DISABLED/greyed with helper 'Turn on AI in Settings first' (gate 1) — this is the fail-closed state (see the parent's default). Keep Off Limits (a fuel concept in the FuelEditor) a SEPARATE control — do not merge it into this toggle.
    In `ThingsToRememberScreen.tsx` (the parent): compute `globalAiEnabled` from the durable app settings via the REAL reader — `getAppSettings(exec)` (async; `app-settings-dao.ts:381`), taking `aiProvider !== 'none'` (there is NO `getAiSettings` export — this corrects review concern P03-MED). Because `getAppSettings` is async (and throws if the id=1 row is missing), load it into screen state and DEFAULT `globalAiEnabled = false` until it resolves, and on a settings-read failure set/leave it `false` — FAIL-CLOSED, so a transient or failed settings read never presents a usable AI opt-in switch. Pass `globalAiEnabled` and `onSetAllowAi` to BOTH `MemoryEditor` render sites — the grouped list at `ThingsToRememberScreen.tsx:585` AND the empty-state instance at `:630` (both must receive them; the required props guarantee `tsc` flags a miss). Implement `onSetAllowAi` to call `setMemoryAllowAi(getExecutor(), { id, contactId, allow, now })` then `await load()` (parent-owns-persistence, same posture as its existing `onAdd`/`onEdit` handlers).
    In `MemoryCard.tsx`: when `allow_ai === 1`, render the sparkle via `<Icon name="sparkle" ... />` tinted `useTheme().colors.accentText`, with accessibilityLabel 'Shared with AI'; render nothing when `allow_ai !== 1` (absence is the off state).
    In `FuelEditor.tsx`: update the off-limits helper copy to the avoid-topic semantic 'A topic to avoid bringing up.' (replacing the legacy wording), keep the `borderStrong` + neutral pill caution treatment, keep it visible by default, and keep it a distinct control from any AI toggle. Before changing any exclusion SQL, grep `docs/decisions/` for an ADR superseding ADR-036/050/078; absent one, leave `RANKED_FUEL_EXCLUSIONS` untouched. All colours via theme tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel (debug build): a Memory's AI toggle defaults OFF and shows no sparkle; enabling it persists (survives navigating away and back) and renders the sparkle; with global AI off (ai_provider='none') the toggle is disabled with the helper copy; an Off-Limits fuel item shows the caution pill + 'A topic to avoid bringing up.' and is never red.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "onSetAllowAi" src/components/MemoryEditor.tsx` matches the new prop + the toggle handler; MemoryEditor imports NO writer DAO (`grep -n "dao\|Dao" src/components/MemoryEditor.tsx` shows only type imports, no writer).
    - `grep -n "setMemoryAllowAi" src/screens/ThingsToRememberScreen.tsx` matches — the DAO call lives in the parent, followed by `load()`.
    - `grep -n "getAppSettings" src/screens/ThingsToRememberScreen.tsx` matches (the real async reader) and `grep -n "getAiSettings" src/screens/ThingsToRememberScreen.tsx` returns NOTHING (the non-existent name is not used); gate 1 is `aiProvider !== 'none'` from `app_settings` (not `ai-key-store`).
    - `globalAiEnabled` and `onSetAllowAi` are passed at BOTH render sites (`grep -c "globalAiEnabled" src/screens/ThingsToRememberScreen.tsx` ≥ 2); they are REQUIRED props on `MemoryEditorProps` so `tsc --noEmit` fails if either site omits them.
    - `globalAiEnabled` defaults to `false` until app settings resolve and stays `false` on a settings-read error (fail-closed — a test or the SUMMARY records that the toggle is disabled while settings are unloaded/unavailable).
    - `grep -n "sparkle" src/components/MemoryCard.tsx` matches, rendered via `<Icon name="sparkle"` and guarded by `allow_ai === 1`.
    - FuelEditor's off-limits helper renders the avoid-topic copy and retains a caution (non-`danger`) style; `RANKED_FUEL_EXCLUSIONS` in fuel-read.ts is unchanged.
    - `npm run check:colors` and `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The two-gate per-item AI toggle (callback-delegated to the parent, gated on app_settings.ai_provider), the Icon-rendered sparkle marker, and the separate avoid-topic Off Limits control are all present and token-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user toggle → egress eligibility | The AI toggle is the only user control that flips a memory into the AI-egress set |
| off-limits state → any AI/glance surface | Off Limits must stay excluded structurally, not merged into AI permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-07 | Information Disclosure / Elevation | per-item AI toggle | high | mitigate | Explicit state, default OFF, two gates; disabled when global AI off; setter both-keys-scoped |
| T-24.2-08 | Information Disclosure | Off Limits control | high | mitigate | Kept a separate control; SQL exclusion in fuel-read.ts unchanged; superseding-ADR grep before any exclusion edit (D-09) |
| T-24.2-09 | Spoofing (UI) | sparkle marker | low | accept | Sparkle only on `allow_ai=1`; absence = off; positive `accentText` tone, never `danger` |
</threat_model>

<verification>
- `npx vitest run src/db/memories-dao.test.ts src/components/icons/icon-registry.test.ts`
- `npx tsc --noEmit` + `npm run check:colors`
- Device UAT per Task 3 `<human-check>` on the Pixel (debug build, run-as pattern).
- `npm test` at wave merge.
</verification>

<success_criteria>
Per-item AI permission is default-OFF, two-gated, sparkle-marked, and backed by a scoped writer; Off Limits stays a separate, visible, avoid-topic caution control with its SQL exclusion intact.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-03-SUMMARY.md` when done.
</output>
