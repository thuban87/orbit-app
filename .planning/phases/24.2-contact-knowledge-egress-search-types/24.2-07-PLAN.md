---
phase: 24.2-contact-knowledge-egress-search-types
plan: 07
type: execute
wave: 3
depends_on: [24.2-01, 24.2-05, 24.2-06]
autonomous: true
requirements: [KNOW-15]
files_modified:
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/backup/reconciliation.ts
  - src/backup/reconciliation.test.ts
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/types.ts
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 67000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Export includes `memories.allow_ai`, `custom_field_defs.scope`/`history_retained`/`field_group`, and every `custom_field_value_history` row (uid + contactUid + fieldDefUid join). [KNOW-15]"
    - "`custom_field_value_history` is registered as a full mergeable entity end-to-end — `MergeableEntityType` + `ENTITY_POLICIES` (reconciliation.ts), the restore `entities`/`tableOf`/`tombstoneEntity`/`incomingRows`/`localRows` maps, the INSERT…ON CONFLICT branch, AND the replace-all delete inventory (restore-apply.ts:245) — so merge, replace-all, and clean restore all handle it (not just an INSERT branch). [KNOW-15]"
    - "Restore round-trips those fields under BOTH modes: a full export→restore (merge AND replace-all) preserves allow_ai, scope, history_retained, field_group, the value-history rows, and soft-deleted memories (deleted_at). [KNOW-15]"
    - "`custom_field_value_history` is a tombstone entity type in `TOMBSTONE_ENTITY_TYPES` and in the `tombstoneEntity` map; a tombstone present in a manifest is validated and applied on restore. History is append-only in this phase (rows are only removed by contact PURGE / def CASCADE, both handled in Plan 05), so no new runtime per-row deletion writer is added here. [KNOW-15]"
    - "Manifest validation rejects a value-history row whose `contactUid` OR `fieldDefUid` is not present in the manifest (parent membership on BOTH parents), and `assertCompleteIncomingPairs` is scope-aware: only GLOBAL-scope defs require the full contact×def pairs; a contact-scoped def (Plan 05) legitimately has a partial set and is exempt. [KNOW-15]"
    - "A live format-4 backup object that PREDATES this phase — no `customFieldValueHistory` top-level array and no new per-row columns — PARSES cleanly through `parseBackupManifest` (the missing array is normalized to `[]` at parse, since there is no 4→4 forward migration and `validate()` otherwise hard-fails on an absent array) and RESTORES, defaulting `allow_ai=0`, `scope='global'`, `history_retained=0`, `field_group=null`, and an empty value-history set. [KNOW-15]"
  prohibitions:
    - statement: "`BACKUP_FORMAT_VERSION` is NOT changed by this plan — it is already 4 on disk (bumped by 24.1); these additions are additive within format 4."
      status: enforced
      verification: "grep of src/backup/types.ts shows `BACKUP_FORMAT_VERSION = 4` unchanged; the diff touches no version constant."
    - statement: "`custom_field_value_history` must be backup-INCLUDED (not in export-manifest FORBIDDEN_KEYS); `field_history` stays excluded."
      status: enforced
      verification: "FORBIDDEN_KEYS still contains `field_history` and does NOT contain `custom_field_value_history`; a round-trip test proves the value-history restores."
    - statement: "Restore must not weaken existing invariants (both-keys/uid ON CONFLICT idioms, secret-shaped-key rejection, unknown-contact-uid rejection)."
      status: enforced
      verification: "existing backup tests stay green; new columns are appended to the existing ON CONFLICT statements without altering the guards."
  artifacts:
    - src/backup/export-manifest.ts (new columns + value-history export)
    - src/backup/reconciliation.ts (custom_field_value_history in MergeableEntityType + ENTITY_POLICIES)
    - src/backup/restore-apply.ts (new columns + value-history restore across all maps + replace-all inventory)
    - src/backup/backup-schema.ts (custom_field_value_history tombstone type + scope-aware validation)
  key_links:
    - "export SELECTs ↔ reconciliation MergeableEntityType/ENTITY_POLICIES ↔ restore maps (entities/tableOf/tombstoneEntity/incomingRows/localRows/INSERT/replace-all-delete) ↔ backup-schema validation — ALL must agree on the new columns/entity or merge/replace-all/restore silently drops or strands data"
    - "custom_field_defs.scope ↔ scope-aware assertCompleteIncomingPairs — without scope-awareness, restore of any backup containing a per-contact def (Plan 05) is rejected by the complete-pair invariant"
---

<objective>
Complete KNOW-15: backup/restore preserves the full knowledge model INCLUDING the fields this phase adds — per-item AI permission (`allow_ai`), custom-field `scope`/`history_retained`/`field_group`, the `custom_field_value_history` timeline, and soft-deleted records. Additive within the already-live backup format 4 (no version bump).

Purpose: No new durable state silently misses backup.
Output: export + restore + schema-validation coverage of the new columns/entity, with forward-compatible defaults for older backups.

**⚠ MILESTONE-ORDERING NOTE (flag to owner, do not resolve here):** The milestone plan (ROADMAP + CONTEXT D-03) said 24.2 must NOT bump `BACKUP_FORMAT_VERSION` and that Phase 36 owns the v4 bump. On disk the version is ALREADY 4 — Phase 24.1 bumped it during its review-fix cycle (commit d677e2c "CR-01 preserve contact knowledge backups") and already emits memories/relationships/current_state + their tombstones. This plan therefore EMITS (not merely declares) its additions into the live format 4, and does NOT bump the version. The "Phase 36 owns the v4 bump" milestone instruction is now factually stale — surface this to the owner at verification; it is a milestone-sequencing decision, not a planner call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-05-SUMMARY.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the new columns + value-history entity</name>
  <files>src/backup/export-manifest.ts, src/backup/types.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/backup/export-manifest.ts (the `Promise.all` read block: customFieldDefs SELECT ~line 56 — this is where the `share_with_ai AS shareWithAi` alias template lives, the pattern to mirror for `scope`/`history_retained AS historyRetained`/`field_group AS fieldGroup`; and the memories SELECT ~line 58 — which currently has NO `share_with_ai`/`allow_ai` column, so `m.allow_ai AS allowAi` is a NEW alias added there; the manifest assembly; `assertNoLocalOnlyKeys` + `FORBIDDEN_KEYS`)
    - src/backup/types.ts (`BackupManifest` fields incl. `memories`, `customFieldDefs`, `currentStateEntries`; `BACKUP_FORMAT_VERSION = 4` — DO NOT change)
    - src/db/migrations/018-custom-field-scope-history.ts (the new column names + the `custom_field_value_history` table shape from Plan 05)
  </read_first>
  <action>
    In export-manifest.ts: add `m.allow_ai AS allowAi` to the memories SELECT; add `scope`, `history_retained AS historyRetained`, `field_group AS fieldGroup` to the customFieldDefs SELECT. Add a new read for `custom_field_value_history` selecting `h.uid, c.uid AS contactUid, d.uid AS fieldDefUid, h.value, h.created_at AS createdAt, h.created_at AS modifiedAt` (join contacts + custom_field_defs by id → uid), ordered by uid; add it to the manifest object as `customFieldValueHistory`. NOTE: the table has `created_at` but no `modified_at`, and the reconciliation `Row` shape (restore-apply incomingRows) reads `row.modifiedAt` for tie-breaking — so alias `created_at AS modifiedAt` too, and use `created_at` as the reconciliation timestamp consistently in export + localRows (the entity is append-only/insert-if-missing, so the stamp is only ever a tiebreak). Add `customFieldValueHistory: Record<string, unknown>[]` to `BackupManifest`. Do NOT add `custom_field_value_history` to `FORBIDDEN_KEYS` (it must round-trip). Do NOT change `BACKUP_FORMAT_VERSION`. Extend export-manifest.test.ts to assert the new fields/entity are present in a built manifest.
  </action>
  <verify>
    <automated>npx vitest run src/backup/export-manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The built manifest's `memories[]` rows carry `allowAi`; `customFieldDefs[]` carry `scope`/`historyRetained`/`fieldGroup`; `customFieldValueHistory[]` exists with `uid`/`contactUid`/`fieldDefUid`/`value`.
    - `grep -n "BACKUP_FORMAT_VERSION = 4" src/backup/types.ts` still matches (unchanged).
    - `FORBIDDEN_KEYS` still contains `field_history` and does not contain `custom_field_value_history`.
  </acceptance_criteria>
  <done>The export manifest carries the new columns and the value-history entity within format 4.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliation entity + restore maps + tombstone + scope-aware validation</name>
  <files>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/backup/restore-apply.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <read_first>
    - src/backup/reconciliation.ts (VERIFIED: `MergeableEntityType` closed union at :8; `EntityPolicy` at :33 = `writeMode: "lww" | "insert-if-missing"` + optional `parentFields`; exhaustive `ENTITY_POLICIES` Record at :45. The `events` policy at :66 — `insert-if-missing` + a contact parentField — is the append-only analog to copy.)
    - src/backup/restore-apply.ts (VERIFIED the FULL set of maps that key off MergeableEntityType and MUST gain the new entity: `entities` array :49; `tableOf` :50; `tombstoneEntity` :51; `incomingRows` selector :54; `localRows` SQL map :57-69; the per-entity INSERT…ON CONFLICT branches ~:184 (`custom_field_values` at :184 resolves `defs.get(fieldDefUid)` + `contacts.get(contactUid)` — mirror it); the memories INSERT :185; the custom_field_defs INSERT :164; `assertCompleteIncomingPairs` :154; the REPLACE-ALL delete inventory `for (const table of [...])` at :245 — the new table MUST be added or replace-all leaves history rows)
    - src/backup/backup-schema.ts (VERIFIED: `TOMBSTONE_ENTITY_TYPES` set at :170; the memories validation ~:410; add value-history validation + tombstone type. CRITICAL for P07-MED: `validate()` at :259 extracts each array via `array(manifest[key], key)` which HARD-FAILS (`array(undefined)` → `fail`) on a missing top-level array; `parseBackupManifest` at :618 runs `FORWARD_MIGRATIONS` only for `from < MAX (4)`, and the `3→4` step at :96 is the LAST one — there is NO `4→4` step. So a live format-4 backup (24.1 already emits format 4) that predates this phase and LACKS `customFieldValueHistory` reaches `validate()` with the key ABSENT and, if the key were added to `requiredArrays`, would hard-fail at parse. It must be normalized to `[]` at parse instead.)
    - src/backup/types.ts (`BackupManifest.customFieldValueHistory` from Task 1)
    - src/db/migrations/018-custom-field-scope-history.ts (Plan 05: the `custom_field_value_history` columns + `custom_field_defs.scope` — validation must read `scope`)
  </read_first>
  <action>
    In reconciliation.ts: add `"custom_field_value_history"` to the `MergeableEntityType` union AND an `ENTITY_POLICIES` entry `{ writeMode: "insert-if-missing", parentFields: [{ field: "contact_id", entityType: "contacts" }, { field: "field_def_id", entityType: "custom_field_defs" }] }` (append-only, immutable rows — mirrors `events`). The Record is exhaustive so `tsc` forces every consumer to handle it.
    In restore-apply.ts: add the entity to EVERY map keyed by MergeableEntityType — `entities` (:49), `tableOf` (:50 → `"custom_field_value_history"`), `tombstoneEntity` (:51 → a tombstone type string, e.g. `"custom_field_value_history"`), `incomingRows` (:54 → `manifest.customFieldValueHistory`), `localRows` (:57 → a SELECT joining contacts + custom_field_defs emitting `uid, contactUid, fieldDefUid, value, created_at AS createdAt, created_at AS modified_at` — the table has no `modified_at`, and `localRows` feeds `modified_at` directly into the reconciliation `Row`, so alias `created_at AS modified_at` here to match the manifest's `modifiedAt`). Add the INSERT…ON CONFLICT(uid) branch mirroring the `custom_field_values` branch at :184 (resolve `contacts.get(contactUid)` + `defs.get(fieldDefUid)`; INSERT `uid,contact_id,field_def_id,value,created_at`). Add `"custom_field_value_history"` to the REPLACE-ALL delete inventory at :245 (before `contacts`/`custom_field_defs` so FK order holds). Also add `allow_ai` to the memories read-SELECT + INSERT…ON CONFLICT (`DO UPDATE SET` too), defaulting `allowAi ?? 0`; add `scope`/`history_retained`/`field_group` to the custom_field_defs read-SELECT + INSERT…ON CONFLICT, defaulting `scope ?? 'global'`, `historyRetained ?? 0`, `fieldGroup ?? null`.
    Make `assertCompleteIncomingPairs` (:154) SCOPE-AWARE: iterate defs, and require the full contact×def pairs ONLY for defs where `scope` is `'global'` (or absent, for older backups); SKIP the completeness requirement for `scope='contact'` defs (they legitimately have a partial pair set) — those are still validated for parent membership. Without this, restoring any backup that contains a Plan-05 per-contact def is wrongly rejected.
    In backup-schema.ts: add `custom_field_value_history` to `TOMBSTONE_ENTITY_TYPES` (:170). CROSS-PLAN COORDINATION (review Agreed Concern — two DISTINCT tombstone registries): `TOMBSTONE_ENTITY_TYPES` here drives RESTORE validation and is THIS plan's (07) responsibility; the separate `TombstoneEntityType` union in `tombstones-dao.ts` drives PURGE and is Plan 05's responsibility. Both must gain the member; do not assume "add the tombstone type" is one edit — update ONLY `TOMBSTONE_ENTITY_TYPES` here and rely on Plan 05 for the purge union. Add manifest validation for the new entity — uid uniqueness AND parent membership on BOTH `contactUid` (must be a known contact uid) and `fieldDefUid` (must be a known def uid), failing before restore otherwise; tolerate the new optional columns on memories/customFieldDefs. Preserve every existing guard (secret-shaped-key rejection, unknown-contact-uid rejection). Do NOT write a runtime per-row deletion writer — value-history is append-only in this phase (deletions come only from Plan 05's purge/merge lifecycle), so tombstone support is parse+apply only.
    P07-MED — NORMALIZE THE MISSING ARRAY AT PARSE (not only via `?? 0`/`?? null` at SQL insert): because there is no `4→4` forward migration and `validate()`'s `array(manifest[key])` hard-fails on an absent array, `customFieldValueHistory` must be treated as an OPTIONAL array normalized to `[]` when absent — do NOT add it to the `requiredArrays` list at :260 (that would reject every pre-existing format-4 backup). Instead extract it defensively, e.g. `array(manifest.customFieldValueHistory ?? [], "customFieldValueHistory")`, so an old manifest object lacking the key parses cleanly to an empty history set, while a present-but-non-array value still fails. (The per-row `allowAi ?? 0` / `scope ?? 'global'` defaults handle the per-ROW columns on memories/customFieldDefs; this handles the missing top-level ARRAY, which is the only part that would hard-fail at parse.)
  </action>
  <verify>
    <automated>npx vitest run src/backup/reconciliation.test.ts src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "custom_field_value_history" src/backup/reconciliation.ts` matches in BOTH `MergeableEntityType` and `ENTITY_POLICIES`.
    - `grep -c "custom_field_value_history" src/backup/restore-apply.ts` shows it added across the maps AND the replace-all delete inventory (≥ 6 occurrences: entities, tableOf, tombstoneEntity, incomingRows, localRows, INSERT branch, replace-all list).
    - `grep -n "allow_ai" src/backup/restore-apply.ts` matches in both the memories SELECT and INSERT.
    - `grep -n "custom_field_value_history" src/backup/backup-schema.ts` matches inside `TOMBSTONE_ENTITY_TYPES`.
    - A backup-schema test proves a per-contact (`scope='contact'`) def with only its owner's pair PASSES `assertCompleteIncomingPairs`, while a global def missing a pair still FAILS; and a value-history row with an unknown `fieldDefUid` is rejected.
    - P07-MED: a `parseBackupManifest` test on an OLD format-4 manifest object with NO `customFieldValueHistory` key (and no new per-row columns) PARSES without throwing and yields `customFieldValueHistory: []` — proving `customFieldValueHistory` is NOT in `requiredArrays` and is normalized to `[]` at parse. `grep -n "customFieldValueHistory" src/backup/backup-schema.ts` shows the `?? []` default, not membership in `requiredArrays`.
    - Restoring a manifest with the new fields writes them; restoring a manifest WITHOUT them applies the documented defaults. Existing backup tests remain green.
  </acceptance_criteria>
  <done>The value-history entity is a first-class mergeable+tombstone entity across reconciliation and every restore map (incl. replace-all), the new columns restore with safe old-backup defaults, and pair validation is scope-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Full round-trip fidelity test</name>
  <files>src/backup/restore-apply.test.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/backup/restore-apply.test.ts (the existing export→restore round-trip harness; how a source DB is seeded and a fresh DB restored)
    - src/backup/phase-17-integration.test.ts (the integration-gate round-trip pattern)
  </read_first>
  <action>
    Add round-trip tests covering BOTH restore modes. Seed a source DB with (a) a memory with `allow_ai=1`, a soft-deleted memory (`deleted_at` set), and an `imported`-type memory with `allow_ai=0` (so KNOW-14/KNOW-15 provenance coverage is exercised, not only generic memories — Plan 06's type rides on this generic `allow_ai` path). NOTE — this seeds `type='imported'`, which only exists in `MemoryTypeKey`/`MEMORY_TYPE_REGISTRY` after Plan 06 adds it (memory-registry.ts:14 currently permits only `general | custom`); that is why `24.2-06` is now in this plan's `depends_on`. Also seed (b) a GLOBAL custom_field_def AND a per-contact (`scope='contact'`) def with `history_retained=1` and a `field_group`, (c) two `custom_field_value_history` rows, (d) a `custom_field_value_history` tombstone. Export → restore into a fresh migrated DB in `merge` mode → assert every field and row is preserved (allow_ai incl. the imported-type memory, scope/history_retained/field_group, both history rows, the soft-deleted memory, the tombstone, the per-contact def's single pair not rejected). Add a `replace-all` case asserting the same fidelity AND that pre-existing history rows in the destination are cleared before the manifest's are written (proves the :245 inventory addition). Add a third case (P07-MED, end-to-end): construct an OLD format-4 manifest OBJECT with the new keys OMITTED (no `customFieldValueHistory` top-level array, and memories/customFieldDefs rows lacking `allowAi`/`scope`/`historyRetained`/`fieldGroup`), run it through `parseBackupManifest` END-TO-END (the real parse+validate entry point, NOT a hand-built already-valid manifest) — assert it does NOT throw and `customFieldValueHistory` normalizes to `[]` — then restore the parsed manifest and assert the documented defaults.
  </action>
  <verify>
    <automated>npx vitest run src/backup/restore-apply.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The merge round-trip asserts allow_ai (incl. the `imported`-type memory), scope, history_retained, field_group, both value-history rows, the soft-deleted memory, and the value-history tombstone all survive export→restore.
    - A replace-all round-trip asserts the same fidelity and that a destination history row absent from the manifest is removed (not stranded).
    - The older-backup case runs through `parseBackupManifest` end-to-end (does not throw; `customFieldValueHistory` normalized to `[]`) and restores with `allow_ai=0`, `scope='global'`, `history_retained=0`, `field_group=null` and no value-history.
    - `npm test` (full node suite) green.
  </acceptance_criteria>
  <done>The full knowledge model — including this phase's additions, the imported-type memory, per-contact scoped defs, and soft-deleted records — round-trips through backup/restore under both merge and replace-all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local DB → backup file → restored DB | New durable state (permissions, history, scope) must survive the round-trip without leaking excluded state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-17 | Information Disclosure / data loss | backup of new entities | high | mitigate | Export+restore+validation all cover the new columns/entity; round-trip test; `field_history` stays excluded, value-history included |
| T-24.2-18 | Denial of Service (data) | restore of older backups | medium | mitigate | Missing new fields default safely (allow_ai=0, scope='global'); existing guards preserved |
| T-24.2-19 | Information Disclosure | AI permission on restore | high | mitigate | `allow_ai` round-trips exactly; a restored opted-in item stays opted-in, a default-OFF item stays OFF |
</threat_model>

<verification>
- `npx vitest run src/backup/export-manifest.test.ts src/backup/reconciliation.test.ts src/backup/restore-apply.test.ts src/backup/backup-schema.test.ts`
- `npx tsc --noEmit`
- `npm test` (full node suite) at wave merge.
- Surface the milestone-ordering note (24.1 pre-bumped format to 4) to the owner at verification.
</verification>

<success_criteria>
Backup/restore preserves allow_ai, custom-field scope/history/group, the value-history timeline, and soft-deleted records within the live format 4 — no version bump, older backups restore with safe defaults.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-07-SUMMARY.md` when done.
</output>
