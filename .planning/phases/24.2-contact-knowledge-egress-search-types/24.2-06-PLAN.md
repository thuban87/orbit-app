---
phase: 24.2-contact-knowledge-egress-search-types
plan: 06
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-14]
files_modified:
  - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt
  - modules/orbit-contact-picker/index.ts
  - src/logic/picked-contact-map.ts
  - src/logic/picked-contact-map.test.ts
  - src/services/import/import-driver.ts
  - src/services/import/import-driver.test.ts
  - src/db/memory-registry.ts
  - src/db/memory-registry.test.ts
  - src/db/imported-contact-dao.ts
  - src/db/imported-contact-dao.test.ts
user_setup:
  - service: android-custom-dev-client
    why: "KNOW-14 changes native Kotlin (Note MIME read); the JS-only tests run without it, but on-device verification requires a rebuilt custom dev/release APK"
    dashboard_config:
      - task: "expo prebuild --clean then build + install a debug APK on the Pixel (desktop `ssh droid` build pipeline, run-as UAT)"
        location: "desktop build host (droid) → adb install -r on the Pixel"

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The native contact picker requests and reads the Note MIME type (`ContactsContract.CommonDataKinds.Note.CONTENT_ITEM_TYPE`) in every read path (picked-session, full-provider, and by-lookup-key) and returns a `note` field to JavaScript via the `PickedContact` contract in `modules/orbit-contact-picker/index.ts` (there is NO `OrbitContactPicker.types.ts`). [KNOW-14]"
    - "The note flows end-to-end: `PickedContact.note` → `mapPickedContact` (which today deliberately EXCLUDES notes) now carries it on `PickedContactMapResult` → `importRowAsNew` passes it into `importContactRecord` → an `imported` Memory is written. Serialization round-trips it because `note` lives on `PickedContact` (the `sourcePayload` JSON that import-driver re-parses). [KNOW-14]"
    - "The Memory-type registry includes an `imported` type whose displayName is 'Imported from Contacts App', `aiDefault` is false, and `searchable` is true. [KNOW-14]"
    - "When a picked contact carries a freeform note, the importer writes it as a Memory of type `imported`, provenance `import`, with `allow_ai = 0` (AI-off by default). [KNOW-14]"
  prohibitions:
    - statement: "Imported note memories are AI-off by default (`allow_ai=0`) — never auto-enabled and never AI-classified in this phase (automatic AI classification of imported notes is deferred)."
      status: enforced
      verification: "imported-contact-dao.test.ts asserts the written memory has `allow_ai=0`; no AI call is made in the import path."
    - statement: "The imported-note memory carries the raw note text; it is never coerced, summarized, or truncated in storage."
      status: enforced
      verification: "imported-contact-dao.test.ts asserts the memory `value` equals the raw note string."
  artifacts:
    - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt (Note MIME read)
    - modules/orbit-contact-picker/index.ts (`PickedContact.note`)
    - src/logic/picked-contact-map.ts (`PickedContactMapResult.note`)
    - src/services/import/import-driver.ts (note → `importContactRecord`)
    - src/db/memory-registry.ts ('imported' type)
    - src/db/imported-contact-dao.ts (note → imported Memory)
  key_links:
    - "native Note MIME → PickedContact.note (index.ts) → mapPickedContact/PickedContactMapResult.note → importRowAsNew → importContactRecord → addMemoryCore(type='imported', allow_ai=0) — the whole import-note path (every hop must be wired or the note is silently dropped)"
    - "registry 'imported'.aiDefault=false → allow_ai=0 seed at memory create (the AI-off default is registry-driven)"
---

<objective>
Complete KNOW-14: freeform phone-Contacts notes import as their own AI-off Memory type. Requires a real native change (the picker must read the Note MIME type) plus a registry type and importer write.

Purpose: Preserve imported note content in the knowledge model, AI-off by default.
Output: Note-MIME reading in the native module + TS bridge; the `imported` Memory type; the importer writing an AI-off imported-note Memory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native picker reads the Note MIME type + JS `PickedContact.note`</name>
  <files>modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt, modules/orbit-contact-picker/index.ts</files>
  <precondition>On-device verification requires `expo prebuild --clean` + a rebuilt custom dev/release APK (desktop `ssh droid` build → `adb install -r` on the Pixel). The JS-only tests in Task 2 run without a rebuild.</precondition>
  <read_first>
    - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt (the four read paths: `pickContacts` requested-fields list; `readPickedContacts`; `readAllContacts`; `readContactsByLookupKeys`; the `MutablePickedContact` data class; the `when (mimeType)` dispatch)
    - modules/orbit-contact-picker/index.ts (VERIFIED: the REAL JS contract — `interface PickedContact` at :14; there is NO `OrbitContactPicker.types.ts` and no `*.types.ts` in `modules/orbit-contact-picker/src/`. Extend `PickedContact` here with `note`.)
    - src/components/ContactPicker.tsx / src/logic/contact-picker-chunk.test.ts (consumers of the picked-contact shape — confirm adding an OPTIONAL `note` is non-breaking)
  </read_first>
  <action>
    Add `ContactsContract.CommonDataKinds.Note.CONTENT_ITEM_TYPE` to: the `EXTRA_PICK_CONTACTS_REQUESTED_DATA_FIELDS` list in `pickContacts`, and the MIME-type filter arrays + `when (mimeType)` dispatch in `readPickedContacts`, `readAllContacts`, and `readContactsByLookupKeys`. Add a nullable `note` field to `MutablePickedContact`; populate it from the Note row's `DATA1` (deterministic: FIRST non-blank Note row wins, mirroring `birthday`; a contact may have multiple Note rows — document the first-wins choice in a comment). Return `"note" to contact.note` in every result map. Extend the `PickedContact` interface in `modules/orbit-contact-picker/index.ts` with an OPTIONAL `note?: string | null` (optional keeps existing consumers non-breaking). Keep the failure posture unchanged (a provider error still rejects/returns empty; never surface provider details).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>On the Pixel with a rebuilt APK: pick a phone contact that has a Notes field; the returned picked-contact object includes the note text.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Note.CONTENT_ITEM_TYPE" modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt` is ≥ 4 (requested fields + three read paths).
    - `MutablePickedContact` has a `note` field and every result map returns `"note"`.
    - `grep -n "note" modules/orbit-contact-picker/index.ts` shows `note` on the `PickedContact` interface (optional); `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The native picker reads and returns freeform contact notes across all read paths, exposed on the real `PickedContact` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 'imported' Memory type + note wired through the import pipeline</name>
  <files>src/db/memory-registry.ts, src/db/memory-registry.test.ts, src/logic/picked-contact-map.ts, src/logic/picked-contact-map.test.ts, src/services/import/import-driver.ts, src/services/import/import-driver.test.ts, src/db/imported-contact-dao.ts, src/db/imported-contact-dao.test.ts</files>
  <behavior>
    - `mapPickedContact` on a PickedContact with a non-blank `note` returns a `PickedContactMapResult` whose `note` is the raw note text (today it EXCLUDES notes — this reverses that exclusion for the note field only); a blank/absent note → `note: null`.
    - `importRowAsNew` passes `mapped.note` into `importContactRecord`; the sourcePayload JSON that import-driver re-parses (`JSON.parse(row.sourcePayload) as PickedContact`) already carries `note` because it lives on `PickedContact` — no separate serialization change needed, but a test proves it survives the serialize→parse→import replay.
    - A picked contact with a non-blank note → the importer writes one Memory: type='imported', provenance='import', value=the raw note, allow_ai=0.
    - A picked contact with a null/blank note → no imported-note Memory is written.
    - The imported Memory is written in the SAME import transaction as the contact record (atomic).
  </behavior>
  <read_first>
    - src/db/memory-registry.ts (`MemoryTypeKey` union + `MEMORY_TYPE_REGISTRY`; add `imported`; keep `isMemoryTypeKey` working)
    - src/db/memories-dao.ts (`addMemoryCore` — writes `allow_ai` seeded from registry `aiDefault`; `MemoryProvenance` includes `'import'`)
    - src/logic/picked-contact-map.ts (VERIFIED: `PickedContactMapResult` at :12 has NO `note`; `mapPickedContact` at :81 deliberately excludes notes — comment at :77 "excludes arbitrary Android contact metadata such as addresses and notes". Add `note` to the result + map it through.)
    - src/services/import/import-driver.ts (VERIFIED: `importRowAsNew` at :68 calls `importContactRecord` at :88 passing only `birthday` — add `note`; `JSON.parse(row.sourcePayload) as PickedContact` at :55 is the replay path)
    - src/db/imported-contact-dao.ts (VERIFIED: `importContactRecord` owns `inWriteTransaction` at :122 — `ImportContactRecordInput` + the note-memory write composes as a Core inside it)
    - src/db/transaction.ts (compose Cores in ONE transaction; never nest the mutex)
  </read_first>
  <action>
    Add an `imported` member to `MemoryTypeKey` and `MEMORY_TYPE_REGISTRY` with `displayName: "Imported from Contacts App"` (a single-source constant), `iconSemantic` (reuse an existing semantic), `cardinality: "many"`, `historyAware: false`, `searchable: true`, `aiDefault: false`, `visibilityDefault: "show"`, and a `presentationOrder`.
    Wire the note end-to-end (it is currently dropped): in `picked-contact-map.ts` add `note: string | null` to `PickedContactMapResult` and set it in `mapPickedContact` from the picked contact's `note` (raw text, blank→null — this narrowly reverses the note exclusion for THIS field; addresses and other metadata stay excluded). In `import-driver.ts` `importRowAsNew`, pass `mapped.note` into the `importContactRecord` call. Extend `ImportContactRecordInput` with an optional `note`; when present and non-blank, compose `addMemoryCore(exec, { contactId, type: "imported", provenance: "import", value: note, createdAt: now, now })` inside the existing import `inWriteTransaction` (allow_ai defaults to 0 via the registry `aiDefault=false` seed / the column default). A blank/null note writes nothing.
    Add node:sqlite + logic tests for each `<behavior>` bullet, INCLUDING an end-to-end test: a picker-shaped payload with a note → serialized as a session sourcePayload → `JSON.parse` replay → `importRowAsNew` → the imported Memory exists with the note value.
  </action>
  <verify>
    <automated>npx vitest run src/db/memory-registry.test.ts src/logic/picked-contact-map.test.ts src/services/import/import-driver.test.ts src/db/imported-contact-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "imported" src/db/memory-registry.ts` shows the type with `aiDefault: false` and displayName 'Imported from Contacts App'.
    - `grep -n "note" src/logic/picked-contact-map.ts` shows `note` on `PickedContactMapResult` and mapped in `mapPickedContact`; `grep -n "note" src/services/import/import-driver.ts` shows it passed into `importContactRecord`.
    - imported-contact-dao.test.ts asserts a noted contact yields exactly one type='imported' Memory with `allow_ai=0` and `value` equal to the raw note; a note-less contact yields none.
    - An end-to-end import-driver test proves the note survives serialize→parse→import replay and becomes the imported Memory.
    - The import remains atomic (a forced failure rolls back both the contact and the note memory).
  </acceptance_criteria>
  <done>Imported freeform notes land as AI-off 'Imported from Contacts App' Memories, wired the whole way from the native picker through map + driver, atomic with the import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone ContactsProvider → local model | Imported data enters the model and must default AI-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-15 | Information Disclosure | imported-note AI egress | high | mitigate | 'imported' `aiDefault=false` → `allow_ai=0`; the Plan-01 egress read requires `allow_ai=1`; node test proves default-OFF |
| T-24.2-16 | Information Disclosure | native provider read | medium | accept | Existing module posture: provider errors never surface details to JS; note read reuses the same guarded cursor path |
</threat_model>

<verification>
- `npx vitest run src/db/memory-registry.test.ts src/logic/picked-contact-map.test.ts src/services/import/import-driver.test.ts src/db/imported-contact-dao.test.ts`
- `npx tsc --noEmit`
- Native: `expo prebuild --clean` + APK on the Pixel per Task 1 `<human-check>`.
- `npm test` at wave merge.
</verification>

<success_criteria>
Freeform phone-Contacts notes import as AI-off 'Imported from Contacts App' Memories via a real native Note-MIME read.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-06-SUMMARY.md` when done.
</output>
