---
phase: 24.2-contact-knowledge-egress-search-types
plan: 06
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-14]
files_modified:
  - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt
  - modules/orbit-contact-picker/index.ts
  - src/logic/picked-contact-map.ts
  - src/logic/picked-contact-map.test.ts
  - src/services/import/import-acquire.ts
  - src/services/import/import-acquire.test.ts
  - src/services/import/import-driver.ts
  - src/services/import/import-driver.test.ts
  - src/db/memory-registry.ts
  - src/db/memory-registry.test.ts
  - src/db/imported-contact-dao.ts
  - src/db/imported-contact-dao.test.ts
user_setup:
  - service: android-custom-dev-client
    why: "KNOW-14 changes native Kotlin (Note MIME read); the JS-only tests run without it, but on-device verification requires a rebuilt custom dev/release APK"
    dashboard_config:
      - task: "expo prebuild --clean then build + install a debug APK on the Pixel (desktop `ssh droid` build pipeline, run-as UAT)"
        location: "desktop build host (droid) → adb install -r on the Pixel"

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The native contact picker requests and reads the Note MIME type (`ContactsContract.CommonDataKinds.Note.CONTENT_ITEM_TYPE`) in every read path (picked-session, full-provider, and by-lookup-key) and returns a `note` field to JavaScript via the `PickedContact` contract in `modules/orbit-contact-picker/index.ts` (there is NO `OrbitContactPicker.types.ts`). [KNOW-14]"
    - "The note flows end-to-end THROUGH THE SESSION SERIALIZATION SEAM — a serialization change IS REQUIRED (addresses review HIGH P06-1; the prior cycle's claim that none was needed was FALSE and is corrected here): `PickedContact.note` → serialized into the `sourcePayload` allowlist by `acceptPickedContacts` (import-acquire.ts:92-96, which today serializes ONLY `displayName`/`methods`/`birthday`, so adding `note` to the TYPE alone does NOT survive the round-trip — `note: contact.note` MUST be added to that object) → `pickedFromRow` re-parse (`JSON.parse(row.sourcePayload) as PickedContact`, import-driver.ts:55) → `mapPickedContact` (which today deliberately EXCLUDES notes) now carries it on `PickedContactMapResult.note` → `importRowAsNew` passes `mapped.note` into `importContactRecord` → an `imported` Memory is written. The single/review new-contact path `commitSingleImport` (import-acquire.ts:120) is covered too: its `input` comes from `ImportReviewScreen.makeImportInput` (no note), so it must read the note from the session row's `sourcePayload` and pass it into `importContactRecord`, or the single path silently drops it while the bulk path keeps it. [KNOW-14]"
    - "The Memory-type registry includes an `imported` type whose displayName is 'Imported from Contacts App', `aiDefault` is false, and `searchable` is true. [KNOW-14]"
    - "When a picked contact carries a freeform note, the importer writes it as a Memory of type `imported`, provenance `import`, with `allow_ai = 0` (AI-off by default). [KNOW-14]"
  prohibitions:
    - statement: "Imported note memories are AI-off by default (`allow_ai=0`) — never auto-enabled and never AI-classified in this phase (automatic AI classification of imported notes is deferred)."
      status: enforced
      verification: "imported-contact-dao.test.ts asserts the written memory has `allow_ai=0`; no AI call is made in the import path."
    - statement: "The imported-note memory carries the raw note text; it is never coerced, summarized, or truncated in storage."
      status: enforced
      verification: "imported-contact-dao.test.ts asserts the memory `value` equals the raw note string."
    - statement: "Note import is scoped to the NEW-contact path only; the `already_linked` outcome imports no note (KNOW-14 targets new-contact import; re-import onto existing contacts is out of scope — P06-LOW, an explicit decision not an omission)."
      status: enforced
      verification: "import-driver.test.ts asserts an `already_linked` outcome writes no imported-note Memory."
  artifacts:
    - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt (Note MIME read)
    - modules/orbit-contact-picker/index.ts (`PickedContact.note`)
    - src/logic/picked-contact-map.ts (`PickedContactMapResult.note`)
    - src/services/import/import-acquire.ts (`note` serialized into the `sourcePayload` allowlist in `acceptPickedContacts`; `commitSingleImport` threads the note from the session row into `importContactRecord`)
    - src/services/import/import-driver.ts (note → `importContactRecord`)
    - src/db/memory-registry.ts ('imported' type)
    - src/db/imported-contact-dao.ts (note → imported Memory)
  key_links:
    - "native Note MIME → PickedContact.note (index.ts) → acceptPickedContacts sourcePayload allowlist (import-acquire.ts:92-96 — THE SERIALIZATION SEAM the prior cycle missed; `note` MUST be added here or it is dropped at the session boundary) → pickedFromRow re-parse (import-driver.ts:55) → mapPickedContact/PickedContactMapResult.note → importRowAsNew → importContactRecord → addMemoryCore(type='imported', allow_ai=0); the single/review path commitSingleImport (import-acquire.ts:120) reads note from the session row's sourcePayload → importContactRecord — the whole import-note path (every hop must be wired or the note is silently dropped)"
    - "registry 'imported'.aiDefault=false → allow_ai=0 seed at memory create (the AI-off default is registry-driven)"
---

<objective>
Complete KNOW-14: freeform phone-Contacts notes import as their own AI-off Memory type. Requires a real native change (the picker must read the Note MIME type) plus a registry type and importer write.

Purpose: Preserve imported note content in the knowledge model, AI-off by default.
Output: Note-MIME reading in the native module + TS bridge; the `imported` Memory type; the importer writing an AI-off imported-note Memory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native picker reads the Note MIME type + JS `PickedContact.note`</name>
  <files>modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt, modules/orbit-contact-picker/index.ts</files>
  <precondition>On-device verification requires `expo prebuild --clean` + a rebuilt custom dev/release APK (desktop `ssh droid` build → `adb install -r` on the Pixel). The JS-only tests in Task 2 run without a rebuild.</precondition>
  <read_first>
    - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt (the four read paths: `pickContacts` requested-fields list; `readPickedContacts`; `readAllContacts`; `readContactsByLookupKeys`; the `MutablePickedContact` data class; the `when (mimeType)` dispatch)
    - modules/orbit-contact-picker/index.ts (VERIFIED: the REAL JS contract — `interface PickedContact` at :14; there is NO `OrbitContactPicker.types.ts` and no `*.types.ts` in `modules/orbit-contact-picker/src/`. Extend `PickedContact` here with `note`.)
    - src/components/ContactPicker.tsx / src/logic/contact-picker-chunk.test.ts (consumers of the picked-contact shape — confirm adding an OPTIONAL `note` is non-breaking)
  </read_first>
  <action>
    Add `ContactsContract.CommonDataKinds.Note.CONTENT_ITEM_TYPE` to: the `EXTRA_PICK_CONTACTS_REQUESTED_DATA_FIELDS` list in `pickContacts`, and the MIME-type filter arrays + `when (mimeType)` dispatch in `readPickedContacts`, `readAllContacts`, and `readContactsByLookupKeys`. Add a nullable `note` field to `MutablePickedContact`; populate it from the Note row's `DATA1` (deterministic: FIRST non-blank Note row wins, mirroring `birthday`; a contact may have multiple Note rows — document the first-wins choice in a comment). Return `"note" to contact.note` in every result map. Extend the `PickedContact` interface in `modules/orbit-contact-picker/index.ts` with an OPTIONAL `note?: string | null` (optional keeps existing consumers non-breaking). Keep the failure posture unchanged (a provider error still rejects/returns empty; never surface provider details).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <kotlin-compile-gate>KOTLIN COMPILE GATE (addresses review concern P06-MED — `tsc` compiles ONLY the TS bridge `index.ts`; it CANNOT see a Kotlin/Android/MIME error in `OrbitContactPickerModule.kt:234,392`). The Kotlin change is compiled — and therefore compile-gated — by the desktop build pipeline: `ssh droid` → `expo prebuild --clean` → Gradle `assembleDebug` (the same build that produces the UAT APK). That build MUST succeed; a Kotlin type/MIME/import error fails it here. Treat a green `assembleDebug` as the Kotlin compile gate and the on-device UAT below as the behavioral gate — do NOT mark Task 1 done on `tsc` alone.</kotlin-compile-gate>
    <human-check>On the Pixel with a rebuilt APK: pick a phone contact that has a Notes field; the returned picked-contact object includes the note text.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Note.CONTENT_ITEM_TYPE" modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt` is ≥ 4 (requested fields + three read paths).
    - `MutablePickedContact` has a `note` field and every result map returns `"note"`.
    - `grep -n "note" modules/orbit-contact-picker/index.ts` shows `note` on the `PickedContact` interface (optional); `tsc --noEmit` clean.
    - The Kotlin change is verified by a SUCCESSFUL desktop `assembleDebug` build (the Kotlin compile gate) AND the on-device note-read UAT — recorded in the SUMMARY; `tsc` alone is explicitly NOT sufficient for the native change.
  </acceptance_criteria>
  <done>The native picker reads and returns freeform contact notes across all read paths, exposed on the real `PickedContact` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 'imported' Memory type + note wired through the import pipeline</name>
  <files>src/db/memory-registry.ts, src/db/memory-registry.test.ts, src/logic/picked-contact-map.ts, src/logic/picked-contact-map.test.ts, src/services/import/import-acquire.ts, src/services/import/import-acquire.test.ts, src/services/import/import-driver.ts, src/services/import/import-driver.test.ts, src/db/imported-contact-dao.ts, src/db/imported-contact-dao.test.ts</files>
  <behavior>
    - `mapPickedContact` on a PickedContact with a non-blank `note` returns a `PickedContactMapResult` whose `note` is the raw note text (today it EXCLUDES notes — this reverses that exclusion for the note field only); a blank/absent note → `note: null`.
    - SERIALIZATION SEAM (review HIGH P06-1 — the note is dropped here today): `acceptPickedContacts` (import-acquire.ts:92-96) serializes `sourcePayload` as an explicit THREE-FIELD allowlist (`displayName`, `methods`, `birthday`) and `importRowAsNew` reconstructs the picked contact SOLELY from `JSON.parse(row.sourcePayload)` (via `pickedFromRow`, import-driver.ts:55). So adding `note` to the `PickedContact` type does NOT make it survive — `note: contact.note` must be added to the `sourcePayload` object. A serialization change IS required (the prior cycle's "no separate serialization change needed" claim was FALSE).
    - `importRowAsNew` passes `mapped.note` into `importContactRecord`; after the serialization fix a picker `note` survives serialize→parse→map→import. A test drives the REAL `acceptPickedContacts` serialization (NOT a hand-built payload) then `importRowAsNew` and asserts the imported contact has the note as an `imported` Memory.
    - Single/review path parity (`commitSingleImport`, import-acquire.ts:120): its `input` is built by `ImportReviewScreen.makeImportInput` (no note field) and it passes no note to `importContactRecord`, so today it drops the note. Fix: `commitSingleImport` reads the note from the session row's `sourcePayload` (the row is already fetched via `listSessionRows`) and passes it into `importContactRecord`, so a single-contact import preserves the note exactly as the bulk path does.
    - A picked contact with a non-blank note → the importer writes one Memory: type='imported', provenance='import', value=the raw note, allow_ai=0.
    - A picked contact with a null/blank note → no imported-note Memory is written.
    - The imported Memory is written in the SAME import transaction as the contact record (atomic).
    - SCOPING DECISION (review P06-LOW — explicit, not silent): note import is wired ONLY through the NEW-contact path (`importRowAsNew` → `importContactRecord`). The `already_linked` import outcome creates no new contact record and therefore imports NO note. This is deliberate: KNOW-14 targets new-contact import, and grafting notes onto an already-linked existing contact on every re-import would risk duplicate/backfilled memories. A test asserts an `already_linked` outcome writes no imported-note Memory.
  </behavior>
  <read_first>
    - src/db/memory-registry.ts (`MemoryTypeKey` union + `MEMORY_TYPE_REGISTRY`; add `imported`; keep `isMemoryTypeKey` working)
    - src/db/memories-dao.ts (`addMemoryCore` — writes `allow_ai` seeded from registry `aiDefault`; `MemoryProvenance` includes `'import'`)
    - src/logic/picked-contact-map.ts (VERIFIED: `PickedContactMapResult` at :12 has NO `note`; `mapPickedContact` at :81 deliberately excludes notes — comment at :77 "excludes arbitrary Android contact metadata such as addresses and notes". Add `note` to the result + map it through.)
    - src/services/import/import-acquire.ts (VERIFIED — THE SERIALIZATION SEAM, review HIGH P06-1: `acceptPickedContacts` at :63 builds `sourcePayload` via `JSON.stringify({ displayName, methods, birthday })` at :92-96 — a three-field allowlist that DROPS `note`; add `note: contact.note`. `commitSingleImport` at :120 calls `importContactRecord` at :124 with NO note and its `input` comes from the review screen (no note) — read the note from the session row's `sourcePayload` and pass it in. `ImportSessionRow.sourcePayload` is `string` (import-session-read.ts:28).)
    - src/services/import/import-driver.ts (VERIFIED: `importRowAsNew` at :68 calls `importContactRecord` at :88 passing only `birthday` — add `note`; `pickedFromRow` at :54-56 does `JSON.parse(row.sourcePayload) as PickedContact` at :55 — the replay path that only sees fields serialized by `acceptPickedContacts`)
    - src/screens/ImportReviewScreen.tsx (VERIFIED: `makeImportInput` at :190 builds the single-import `input` with NO note; `importAsNew` at :205 → `commitSingleImport` at :208 — this is why the single path needs the note threaded from the session row, not from screen state)
    - src/db/imported-contact-dao.ts (VERIFIED: `importContactRecord` owns `inWriteTransaction` at :122 — extend `ImportContactRecordInput` (:45) with an optional `note`; the note-memory write composes as a Core inside the existing transaction. BOTH `importRowAsNew` and `commitSingleImport` are its callers, so a single optional `note` param covers both import paths.)
    - src/db/transaction.ts (compose Cores in ONE transaction; never nest the mutex)
  </read_first>
  <action>
    Add an `imported` member to `MemoryTypeKey` and `MEMORY_TYPE_REGISTRY` with `displayName: "Imported from Contacts App"` (a single-source constant), `iconSemantic` (reuse an existing semantic), `cardinality: "many"`, `historyAware: false`, `searchable: true`, `aiDefault: false`, `visibilityDefault: "show"`, and a `presentationOrder`.
    Wire the note end-to-end (it is currently dropped at THREE places — the serialization seam plus the two consumer hops): 
    FIRST, fix the serialization seam (review HIGH P06-1 — WITHOUT this the note never survives the session round-trip): in `import-acquire.ts` `acceptPickedContacts`, add `note: contact.note` to the `sourcePayload` object at :92-96 (the three-field allowlist that today serializes only `displayName`/`methods`/`birthday`). 
    SECOND, cover the single/review path: in `import-acquire.ts` `commitSingleImport` (:120), the screen-built `input` carries no note, so read the note from the session row's `sourcePayload` (parse the row already resolved via `listSessionRows`, or add a targeted read) and pass it into the `importContactRecord` call — a single-contact import must preserve the note exactly as the bulk path does. 
    THIRD, wire the two consumer hops: in `picked-contact-map.ts` add `note: string | null` to `PickedContactMapResult` and set it in `mapPickedContact` from the picked contact's `note` (raw text, blank→null — this narrowly reverses the note exclusion for THIS field; addresses and other metadata stay excluded); in `import-driver.ts` `importRowAsNew`, pass `mapped.note` into the `importContactRecord` call. Extend `ImportContactRecordInput` with an optional `note` (one param serves BOTH callers); when present and non-blank, compose `addMemoryCore(exec, { contactId, type: "imported", provenance: "import", value: note, createdAt: now, now })` inside the existing import `inWriteTransaction` (allow_ai defaults to 0 via the registry `aiDefault=false` seed / the column default). A blank/null note writes nothing.
    Add node:sqlite + logic tests for each `<behavior>` bullet, INCLUDING an end-to-end test that drives the REAL `acceptPickedContacts` serialization (NOT a hand-built sourcePayload string): a picker `PickedContact` carrying a note → `acceptPickedContacts` → the persisted session row's `sourcePayload` → `importRowAsNew` → the imported Memory exists with the note value. Add a matching single-path test through `commitSingleImport`.
  </action>
  <verify>
    <automated>npx vitest run src/db/memory-registry.test.ts src/logic/picked-contact-map.test.ts src/services/import/import-acquire.test.ts src/services/import/import-driver.test.ts src/db/imported-contact-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "imported" src/db/memory-registry.ts` shows the type with `aiDefault: false` and displayName 'Imported from Contacts App'.
    - SERIALIZATION SEAM (review HIGH P06-1): `grep -n "note" src/services/import/import-acquire.ts` shows `note` inside the `acceptPickedContacts` `sourcePayload` object AND the `commitSingleImport` note-threading into `importContactRecord`. (Without the sourcePayload edit, the note is dropped for the primary session-based import path — a type-only change is insufficient.)
    - `grep -n "note" src/logic/picked-contact-map.ts` shows `note` on `PickedContactMapResult` and mapped in `mapPickedContact`; `grep -n "note" src/services/import/import-driver.ts` shows it passed into `importContactRecord`.
    - imported-contact-dao.test.ts asserts a noted contact yields exactly one type='imported' Memory with `allow_ai=0` and `value` equal to the raw note; a note-less contact yields none.
    - END-TO-END (drives the REAL serialization, review HIGH P06-1): an import-acquire/import-driver test builds a `PickedContact` with a note, runs the ACTUAL `acceptPickedContacts` (not a hand-built payload), then `importRowAsNew`, and asserts the imported contact has the note as an `imported` Memory — proving the note survives the true serialize→persist→parse→import round-trip.
    - SINGLE-PATH PARITY: a test drives `commitSingleImport` for a session row whose `sourcePayload` carries a note and asserts the imported contact has the note as an `imported` Memory (the single/review path preserves the note, matching the bulk path).
    - The import remains atomic (a forced failure rolls back both the contact and the note memory).
  </acceptance_criteria>
  <done>Imported freeform notes land as AI-off 'Imported from Contacts App' Memories, wired the whole way from the native picker through map + driver, atomic with the import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone ContactsProvider → local model | Imported data enters the model and must default AI-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-15 | Information Disclosure | imported-note AI egress | high | mitigate | 'imported' `aiDefault=false` → `allow_ai=0`; the Plan-01 egress read requires `allow_ai=1`; node test proves default-OFF |
| T-24.2-16 | Information Disclosure | native provider read | medium | accept | Existing module posture: provider errors never surface details to JS; note read reuses the same guarded cursor path |
</threat_model>

<verification>
- `npx vitest run src/db/memory-registry.test.ts src/logic/picked-contact-map.test.ts src/services/import/import-acquire.test.ts src/services/import/import-driver.test.ts src/db/imported-contact-dao.test.ts`
- `npx tsc --noEmit`
- Native: `expo prebuild --clean` + APK on the Pixel per Task 1 `<human-check>`.
- `npm test` at wave merge.
</verification>

<success_criteria>
Freeform phone-Contacts notes import as AI-off 'Imported from Contacts App' Memories via a real native Note-MIME read.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-06-SUMMARY.md` when done.
</output>
