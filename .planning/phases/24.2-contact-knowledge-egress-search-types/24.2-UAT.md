---
status: passed
phase: 24.2-contact-knowledge-egress-search-types
source: [24.2-VERIFICATION.md]
started: 2026-09-04T20:30:06Z
updated: 2026-09-04T23:05:00Z
---

> **UAT RESULT — 3/3 PASSED (owner-confirmed on-device, 2026-09-04).**
> 1. Memory AI permission + sparkle + Off Limits — PASS (toggle is edit-only on a saved Memory, by design).
> 2. URL/Email/Phone field types + keyboards + tap-to-fix — PASS.
> 3. Imported Contacts-app notes → AI-off "Imported from Contacts App" Memory — PASS.
> Phase goal fully verified; phase marked complete in ROADMAP.

## Current Test

number: 1
name: Memory AI-permission toggle, sparkle, gate-1 disable, and Off Limits caution styling (Plan 03)
expected: |
  Two-gate default-OFF permission behaves as coded; sparkle presence tracks allow_ai; Off Limits is a
  visibly separate caution control.
awaiting: user response

## Tests

### 1. Memory AI-permission toggle + sparkle + Off Limits (Plan 03)
expected: On the Pixel with the current debug build — open a Memory in Things to Remember, toggle "Allow AI to use this" on, confirm the sparkle (Shared with AI) appears on the card and disappears when toggled off; with AI provider set to "none" confirm the control is disabled with "Turn on AI in Settings first"; confirm Off Limits shows "A topic to avoid bringing up." styled as caution (not danger). Two-gate default-OFF permission behaves as coded; sparkle presence tracks allow_ai; Off Limits is a visibly separate caution control.
result: [pending]

### 2. URL/Email/Phone custom-field types — picker, keyboard, tap-to-fix (Plan 04)
expected: On the Pixel — add a custom field of type URL, Email, and Phone; confirm the type picker lists all 10 options (Photo retained), the value inputs raise the appropriate keyboard, and an implausible value shows the tap-to-fix error state without altering stored text. Failing values flag (never coerce/clear). NOTE: adb tap false-negatives on small RN Pressables — verify against code first if a control "looks" broken.
result: [pending]

### 3. Imported Contacts-app Notes → AI-off "imported" Memory across all read paths (Plan 06)
expected: Rebuild the custom dev/release APK via the desktop pipeline (ssh droid → expo prebuild --clean → assembleDebug), install -r on the Pixel, then import a real phone contact that has a populated Notes field (bulk, single/review, and consolidated-cluster paths). Confirm the note lands as an "Imported from Contacts App" Memory, AI-off (no sparkle). The native Note-MIME read returns the note across all read paths; the note is written as a type='imported', provenance='import', allow_ai=0 Memory on every new-contact import path. Use the device-uat-runas debug-build pattern.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## On-device pre-verification (agent-driven, Pixel 3a `943AY0JR4P`, fresh debug build)

A fresh native debug build (`prebuild --clean` + `assembleDebug`, BUILD SUCCESSFUL — the Kotlin
Note-MIME change compiled) was installed with `install -r` and driven via adb. Verified before the
owner's manual pass:

- **Migrations 017 + 018 apply cleanly on real data:** `PRAGMA user_version = 18` after launch,
  against 3 pre-existing contacts, intact. Decisive KNOW-16 (destructive data-move) + KNOW-13
  (additive schema) proof on-device.
- **Test 2 (KNOW-13) core VERIFIED:** the field-def type picker renders all **10** types including
  the three new `url`, `email`, `phone` (screenshot: `item2-type-picker.png`). Creating a URL field
  persisted correctly: `custom_field_defs = (col_name='website', type='url', scope='global',
  history_retained=0)` — confirming the Phase 24.2 global-only `createField` default on-device.
- **Test 1 (KNOW-11) posture confirmed:** AI provider defaults to **None** with the copy
  "Off by default. Orbit only sends anything to a provider you choose, and only when you ask."
- **Test 3 (KNOW-14) fixture staged:** a device contact "Dana NotesTest" (raw_contact 987) with a
  populated Notes field ("Met at the 2024 conference. Allergic to shellfish.") was created for the
  owner's import walkthrough.

**Still needs the owner (UI-interaction / visual / cross-app — not fully driveable via adb):**
- Test 1: the per-memory "Allow AI to use this" toggle disabled-gate + "Turn on AI in Settings first"
  (renders only on a *saved* memory), the sparkle appearing/disappearing (needs a configured AI
  provider key), and Off Limits "caution not danger" styling (subjective colour).
- Test 2: the value-input keyboard hint + tap-to-fix error state (parser behaviour is unit-tested;
  only the on-screen keyboard/visual remains).
- Test 3: the actual import of "Dana NotesTest" through the system contact picker, confirming the
  note lands as an AI-off `imported` Memory (verify via `run-as` DB: type='imported', allow_ai=0).

## Gaps
