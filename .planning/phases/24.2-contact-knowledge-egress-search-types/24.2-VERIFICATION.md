---
phase: 24.2-contact-knowledge-egress-search-types
verified: 2026-09-04T20:25:55Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
next_action: Run deferred on-device (Pixel) UAT for Plans 03/04/06, then close the ADR-030 supersession-pointer warning (one header edit + registry regen). No code gaps.
warnings:
  - id: W1-adr030-reverse-pointer
    severity: warning
    summary: "ADR-081 retires ADR-030 (its header says `Supersedes: ADR-030`) and the regenerated adr-registry.ts carries the `ADR-0081` token, but ADR-030's own file still says `Superseded by: None`. The registry generator derives supersession from each ADR's `Superseded by` field, so ADR-0030 is still tagged `[ACCEPTED]` in the registry — unlike the 7 other superseded ADRs (002/008/013/014/015/018/019) which all carry the reverse pointer. Per CLAUDE.md this means `graph:ask` / the rebuilt graph will confidently report retired ADR-030 as current. No stated must-have literally fails (all three clauses of the Plan-01 ADR truth hold), so this is a WARNING, not a failed must-have."
    fix: "Update docs/decisions/ADR-030-*.md header `Superseded by: None` → `Superseded by: ADR-081`, then `npm run gen:adr-registry` (and `npm run graph:build` when the graph is next rebuilt). This is the repo's sanctioned supersession bookkeeping (the ADR body stays immutable), not a decision reversal."
milestone_notes:
  - "Plan 07 BACKUP_FORMAT_VERSION: confirmed unchanged at 4 (24.1 pre-bumped it, commit d677e2c). The old ROADMAP instruction 'Phase 36 owns the v4 bump' is already reconciled in ROADMAP.md (v4 spent, Phase 36 closing bump now v5) and the owner is aware (MEMORY: backup-format-v4-landed-early). Informational only — no action."
human_verification:
  - test: "On the Pixel with the current debug build: open a Memory in Things to Remember, toggle 'Allow AI to use this' on, confirm the sparkle (Shared with AI) appears on the card and disappears when toggled off; with AI provider set to 'none' confirm the control is disabled with 'Turn on AI in Settings first'; confirm Off Limits shows 'A topic to avoid bringing up.' styled as caution (not danger)."
    expected: "Two-gate default-OFF permission behaves as coded; sparkle presence tracks allow_ai; Off Limits is a visibly separate caution control."
    why_human: "Visual appearance, real-time toggle behavior, and theme-token styling are UI-observable only. Deferred per the phase handoff (Plan 03 device UAT)."
  - test: "On the Pixel: add a custom field of type URL, Email, and Phone; confirm the type picker lists all 10 options (Photo retained), the value inputs raise the appropriate keyboard, and an implausible value shows the tap-to-fix error state without altering stored text."
    expected: "URL/Email/Phone picker options present with correct keyboard hints; failing values flag (never coerce/clear)."
    why_human: "Keyboard hints, picker rendering, and tap-to-fix UX are device/UI-observable only. Deferred per the phase handoff (Plan 04 device UAT). adb tap false-negatives on small RN Pressables — verify against code first (MEMORY)."
  - test: "Rebuild the custom dev/release APK via the desktop pipeline (ssh droid → expo prebuild --clean → assembleDebug), install -r on the Pixel, then import a real phone contact that has a populated Notes field (bulk, single/review, and consolidated-cluster paths). Confirm the note lands as an 'Imported from Contacts App' Memory, AI-off (no sparkle)."
    expected: "The native Note-MIME read returns the note across all read paths; the note is written as a type='imported', provenance='import', allow_ai=0 Memory on every new-contact import path."
    why_human: "The Kotlin native change (Note.CONTENT_ITEM_TYPE across 4 read paths) is NOT compiled by tsc — it is compile-gated only by a successful assembleDebug, and its runtime behavior requires a real device + real contact. Deferred per the phase handoff (Plan 06 device UAT). Use the device-uat-runas debug-build pattern (MEMORY)."
---

# Phase 24.2: Contact Knowledge — Egress, Search, Types & Data-moves — Verification Report

**Phase Goal:** The knowledge model becomes searchable, AI-permissioned, richly typed, and portable — bounded local typo search (no FTS5), opt-in-per-item AI permission with Off Limits kept separate, the expanded custom-field type set with optional value history, imported phone-Contacts notes, migration of existing share-sheet captures, and full backup/restore coverage. Includes the milestone's destructive data-move (migration 017 + the new ADR retiring ADR-030).
**Verified:** 2026-09-04T20:25:55Z
**Status:** human_needed
**Re-verification:** No — initial verification

> Note: the phase directory's stale `.continue-here.md` describes a pause after Plan 06. That is superseded — git shows Plan 07 fully executed (commits `570bd25`→`76ae312`). `phase-plan-index`/ROADMAP show 7/7 plans executed. All verification below is against the actual code on disk at HEAD (`76ae312`).

## Goal Achievement

### Observable Truths

Truths are the 7 requirement-level must-haves (KNOW-10…16), aggregating the per-plan must_haves and the 3 ROADMAP success criteria.

| #   | Truth (Req)                                                                                                                                              | Status     | Evidence |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | **KNOW-10** Search finds names / Memory label+value+note / relationship names / eligible custom-field content regardless of table, typo-tolerant, no FTS5, never internal metadata | ✓ VERIFIED | `knowledge-search-read.ts` corpus (name, registry-`searchable` live memories, live relationship `person_name`, custom values via def-id-bound `getValuesForContact`; no `FROM fuel`, no ids/uid/provenance/timestamps). `knowledge-search.ts` pure scorer: `tokenize` (diacritic-fold, punctuation split), `boundedEditDistance` cap 1 (<6 chars) / 2 (≥6), prefix/substring/exact ranking. Repo-wide FTS5 scan: **no matches**. Tests: `knowledge-search.test.ts` + migration/corpus tests pass. |
| 2   | **KNOW-11** AI opt-in per item, two gates (global AI enabled, then per-item default OFF); sparkle on enabled items; type-defaults affect new items only | ✓ VERIFIED | Migration 017 adds `allow_ai INTEGER NOT NULL DEFAULT 0`. Egress projection `MEMORY_AI_ELIGIBILITY = "allow_ai = 1 AND deleted_at IS NULL"` wired into `readPromptContext` (the sole AI boundary) via `listAiEligibleMemories`. `memories-dao.ts`: `setMemoryAllowAi` (both-keys scoped, `assertOneChange`, dirties backup) + create seeds `allow_ai` from `MEMORY_TYPE_REGISTRY[type].aiDefault`. Gate-1: `getAppSettings().aiProvider !== "none"` in `ThingsToRememberScreen`. Sparkle renders only when `allow_ai === 1` (`MemoryCard.tsx:81`), label "Shared with AI", `tone="accentText"`. Tests: `memories-dao.test.ts`, `017-…test.ts` pass. |
| 3   | **KNOW-12** Off Limits: separate control, visible, caution-styled, avoid-topic semantics; SQL egress exclusion unchanged                                | ✓ VERIFIED | `fuel-read.ts` `RANKED_FUEL_EXCLUSIONS` / in-query `kind != 'off_limits'` exclusions unchanged (no superseding ADR). `FuelEditor.tsx` copy "A topic to avoid bringing up." (:312,:478), kept distinct from the AI toggle. |
| 4   | **KNOW-13** 9 user-facing types incl. url/email/phone (photo retained → 10 `FieldType` members/parsers); global or per-contact defs w/ optional value history; ADR-001 uniqueness untouched | ✓ VERIFIED | `FieldType` = 10 members; `field-parsers.ts` exhaustive `Record<FieldType,…>` with permissive byte-for-byte url/email/phone parsers (flag, never coerce). Picker `FIELD_TYPES` = 10 entries (Photo retained). Migration 018 adds `scope`/`history_retained`/`field_group` + `custom_field_value_history` (uid UNIQUE) + index, additive — 006 uniqueness constraints intact (018 test asserts). `createField` THROWS on `scope='contact'` (`field-ddl.ts:59`). `promoteFieldToGlobalCore` atomic. Value-history append in the real edit writer `updateContactFull` (`contacts-dao.ts:423`, before `upsertValueCore`); `maybeAppendPriorValueHistoryCore` correctly no-ops on first-set/unchanged and appends on change/clear. Full lifecycle: merge reparent (`merge-dao.ts:182`), purge tombstone+delete (`purge-dao.ts` PURGE_CHILDREN :86 + :320 array + tombstone map :101), tombstones union (`tombstones-dao.ts:19,50`), def-drop tombstone before delete (`field-ddl.ts:133,154,166`). Tests: `018-…test.ts`, `value-history-dao.test.ts` pass. |
| 5   | **KNOW-14** Phone-Contacts freeform notes import as a dedicated AI-off "Imported from Contacts App" Memory type on every new-contact path                | ✓ VERIFIED (code path) | Registry `imported` type: displayName "Imported from Contacts App", `aiDefault: false`, `searchable: true`. Note threaded: Kotlin Note MIME (6× `Note.CONTENT_ITEM_TYPE`), `PickedContact.note`, `acceptPickedContacts` sourcePayload allowlist (:96), `commitSingleImport` reads from session row (:128-141), `import-driver` `mapped.note`→`importContactRecord` (:97), `combineCluster` first-non-blank + `addMemoryCore(type='imported')` (:194-224). `imported-contact-dao.ts` writes raw note as type=imported/provenance=import/allow_ai=0. Tests: `imported-contact-dao.test.ts` pass. **Native Note-MIME runtime read + on-device import routed to human verification (device build required).** |
| 6   | **KNOW-15** Backup/restore preserves the full knowledge model incl. history, AI permission, scope/group, soft-deleted, within format 4 (no bump)         | ✓ VERIFIED | `export-manifest.ts`: `allowAi` on memories, `scope`/`historyRetained`/`fieldGroup` on defs, new `customFieldValueHistory` entity; `FORBIDDEN_KEYS` keeps `field_history`, excludes value-history; `BACKUP_FORMAT_VERSION = 4` unchanged. `reconciliation.ts`: value-history in `MergeableEntityType` + `ENTITY_POLICIES`. `restore-apply.ts`: entities/tableOf/tombstoneEntity/incomingRows/INSERT…ON CONFLICT/replace-all-delete inventory all cover value-history; memories restore `allowAi` + `deletedAt`; scope-aware seeding (:157-163). `backup-schema.ts`: `TOMBSTONE_ENTITY_TYPES` incl. value-history; missing array normalized to `[]` at parse (forward-compat); both-parent UID membership + scope-aware `assertCompleteIncomingPairs`. Tests: `restore-apply.test.ts`, `export-manifest.test.ts` pass (merge + replace-all round-trip). |
| 7   | **KNOW-16** Migration 017: share-sheet captures → default-type Memories, no data loss; ADR-030 retired non-destructively via ADR-081                     | ✓ VERIFIED | `017-…ts`: copy-then-prove-then-remove for `kind='topic' AND source='share'` (→ general/share memories, verbatim value, `assertNoRows` after) and for `source='ai'` (→ AI-off memories, preserved before removal). `allow_ai=0` default. Registered in `database.ts` MIGRATIONS, `TARGET_VERSION=18`. ADR-081 exists, Status Accepted, `Supersedes: ADR-030`; regenerated registry carries `ADR-0081` token. No shipped producer writes `source='ai'` (safety-net path). Tests: `017-…test.ts` + `full-chain.test.ts` (001→018) pass. **See W1 warning: ADR-030's reverse pointer was not updated.** |

**Score:** 7/7 truths verified (0 present-behavior-unverified). 1 warning (W1). 3 human-verification items (deferred device UAT).

### Required Artifacts

| Artifact | Provides | Status |
| -------- | -------- | ------ |
| `src/db/migrations/017-knowledge-egress-datamove.ts` | allow_ai column + non-destructive data-move | ✓ VERIFIED (registered, tested) |
| `src/db/migrations/018-custom-field-scope-history.ts` | scope/history/group cols + value-history table | ✓ VERIFIED (registered, tested) |
| `src/db/memories-read.ts` | AI-egress projection (`allow_ai=1 AND deleted_at IS NULL`) | ✓ VERIFIED (wired into `readPromptContext`) |
| `src/services/knowledge-search.ts` | pure bounded typo scorer, no FTS5 | ✓ VERIFIED (node-tested) |
| `src/db/knowledge-search-read.ts` | SQL eligibility corpus (structured, no metadata) | ✓ VERIFIED (exposed for Phase 25) |
| `docs/decisions/ADR-081-…md` + `adr-registry.ts` | retire ADR-030 + graph token | ✓ VERIFIED (⚠ W1: ADR-030 reverse pointer missing) |
| `src/db/memories-dao.ts` | setMemoryAllowAi + registry-seeded default | ✓ VERIFIED |
| `src/components/icons/icon-registry.ts` | `sparkle` entry | ✓ VERIFIED |
| `src/components/MemoryEditor.tsx` / `ThingsToRememberScreen.tsx` | per-item toggle + parent wiring + gate-1 | ✓ VERIFIED (UI render → human) |
| `src/components/FuelEditor.tsx` | Off Limits avoid-topic caution control | ✓ VERIFIED (UI render → human) |
| `src/schemas/types.ts` / `field-parsers.ts` / `FieldDefForm.tsx` | url/email/phone types + parsers + picker | ✓ VERIFIED |
| `src/db/value-history-dao.ts` / `field-defs-dao.ts` / `field-ddl.ts` | history append/read + promotion + global-only create + def-drop tombstone | ✓ VERIFIED |
| `src/db/merge-dao.ts` / `purge-dao.ts` / `tombstones-dao.ts` | value-history lifecycle across 3 destructive paths | ✓ VERIFIED |
| `modules/orbit-contact-picker/*` (Kotlin + index.ts) | Note-MIME read + `PickedContact.note` | ✓ VERIFIED (TS/wiring); native runtime → human |
| `src/db/memory-registry.ts` / `imported-contact-dao.ts` / import chain | `imported` type + note import (all paths) | ✓ VERIFIED (code); device import → human |
| `src/backup/export-manifest.ts` / `reconciliation.ts` / `restore-apply.ts` / `backup-schema.ts` | full-model backup/restore within format 4 | ✓ VERIFIED (round-trip tested) |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| migration 017/018 | `database.ts` MIGRATIONS + `TARGET_VERSION=18` | registration | ✓ WIRED |
| `memories.allow_ai` | `readPromptContext` (sole egress) | `listAiEligibleMemories` / `MEMORY_AI_ELIGIBILITY` | ✓ WIRED |
| corpus SQL | TS scorer | `listKnowledgeSearchCandidates` → `rankCandidates` (shared `tokenize`) | ✓ WIRED |
| `MemoryEditor.onSetAllowAi` | `setMemoryAllowAi` → `allow_ai` → egress read | `ThingsToRememberScreen` wiring | ✓ WIRED |
| `history_retained` flag | value-history append inside `updateContactFull` txn | `maybeAppendPriorValueHistoryCore` before `upsertValueCore` | ✓ WIRED |
| `custom_field_value_history` | merge/purge/def-drop lifecycle + tombstone union | reparent / PURGE_CHILDREN+array / dropFieldValues / TombstoneEntityType | ✓ WIRED |
| native Note MIME | AI-off `imported` Memory | picker→sourcePayload→driver/single/cluster→`importContactRecord`/`addMemoryCore` | ✓ WIRED (native runtime → human) |
| export SELECTs | reconciliation entity | restore maps ↔ backup-schema validation | ✓ WIRED (all agree on new cols/entity) |

### Behavioral Spot-Checks

Ran the phase's node:sqlite / vitest suites (behavioral evidence for the state-transition and round-trip truths):

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Migration 017 data-move + egress + no-loss; 018 schema + uniqueness; scorer typo boundaries; value-history transitions; backup merge+replace-all round-trip; imported note AI-off; setMemoryAllowAi | `npx vitest run` (8 phase test files) | **61 passed (61)** | ✓ PASS |
| Full migration chain 001→018 | `npx vitest run …/full-chain.test.ts` | **1 passed** | ✓ PASS |
| No FTS5 / new index | `grep -riE "fts5|USING fts|VIRTUAL TABLE" src/` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
| ----------- | -------------- | ------ | -------- |
| KNOW-10 | 01, 02 | ✓ SATISFIED | corpus + scorer, no FTS5, no metadata (truth 1) |
| KNOW-11 | 01, 03 | ✓ SATISFIED | allow_ai default OFF, two gates, sparkle (truth 2) |
| KNOW-12 | 03 | ✓ SATISFIED | Off Limits separate/caution/avoid-topic; SQL exclusion intact (truth 3) |
| KNOW-13 | 04, 05 | ✓ SATISFIED | 10 types, value history, ADR-001 untouched (truth 4) |
| KNOW-14 | 06 | ✓ SATISFIED (code) | imported type + note chain (truth 5); device import → human |
| KNOW-15 | 07 | ✓ SATISFIED | full-model backup/restore within format 4 (truth 6) |
| KNOW-16 | 01 | ✓ SATISFIED | migration 017 no-loss + ADR-081 (truth 7); ⚠ W1 reverse pointer |

No orphaned requirements — all seven KNOW-10…16 are claimed by phase plans and evidenced in code. REQUIREMENTS.md checkboxes are justified.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
| ---- | ------- | -------- | ------ |
| (none) | Debt-marker scan (TBD/FIXME/XXX/HACK/PLACEHOLDER/"not implemented") across all non-test files modified this phase | — | Clean — no markers |

Stub scan: search modules are pure functions with no runtime consumer yet — this is intentional (corpus exposed here, consumed by Phase 25), matching the deferred-UI decision in CONTEXT.md, not a stub.

### Human Verification Required

3 items — all UI/native/device-observable and explicitly deferred per the phase handoff (Plans 03/04/06). These render / are wired in verified code but were not device-tested; the Kotlin native Note-MIME read additionally cannot be compile- or behavior-verified without a desktop `assembleDebug` + Pixel. See `human_verification` frontmatter for exact steps/expected/why.

### Warnings (owner review)

**W1 — ADR-030 supersession reverse pointer missing.** ADR-081 correctly declares `Supersedes: ADR-030` and the regenerated `adr-registry.ts` carries the `ADR-0081` token, so the literal Plan-01 must-have passes. But ADR-030's own file still reads `Superseded by: None`, so the registry (and the graph on next build) tag `ADR-0030` as `[ACCEPTED]` rather than superseded — the exact "graph confidently reports a retired ADR as current" failure CLAUDE.md warns about, and inconsistent with the 7 other superseded ADRs which all carry the reverse pointer. Fix is sanctioned bookkeeping (not a decision reversal): set ADR-030 `Superseded by: ADR-081`, then `npm run gen:adr-registry`. Recorded as a warning, not a gap, because no stated must-have literally fails.

### Gaps Summary

No code gaps. All 7 requirement-level must-haves are satisfied in the actual codebase with passing behavioral tests (62 total across the phase's suites + full-chain). Overall status is `human_needed` solely because of three deferred device/UI UAT items (Plans 03/04/06) that cannot be verified programmatically — not because of any implementation deficiency. One documentation-integrity warning (W1) is surfaced for the owner; it is a trivial header edit plus registry regen and does not block Phase 25.

---

_Verified: 2026-09-04T20:25:55Z_
_Verifier: Claude (gsd-verifier) — goal-backward, code-on-disk at HEAD 76ae312, SUMMARY claims not trusted_
