---
phase: 24.2
slug: contact-knowledge-egress-search-types
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-04
---

# Phase 24.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from 24.2-RESEARCH.md §Validation Architecture; per-task map completed by the planner.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (node environment) with `node:sqlite` executor via `src/db/__testkit__` |
| **Config file** | project `vitest` config (repo runs `npm test`) |
| **Quick run command** | `npx vitest run src/db/<new-module>.test.ts` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | full node suite ~ per repo baseline (224 files / 2,174 tests as of Phase 24.1) |

---

## Sampling Rate

- **After every task commit:** `npx vitest run <touched module>.test.ts` + `npm run check:colors` (UI) + `tsc`
- **After every plan wave:** `npm test` (full node suite) + `biome` on changed files
- **Before `/gsd-verify-work`:** Full suite green + on-device UAT (incl. the KNOW-13 native slice on the Pixel)
- **Max feedback latency:** quick module run (seconds)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 24.2-01-01 | 01 | 1 | KNOW-14 | T-24.2-migration | Migration 017 (verify head+1 on disk) copies share-capture rows with zero data loss; forward-only, no assumed start state | migration (node:sqlite) | `npx vitest run src/db/migrations/017-*.test.ts src/db/migrations/full-chain.test.ts` | ❌ W0 | ⬜ pending |

*Planner completes this map for every task. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Migration `017-*.test.ts` (+ any 018) — schema + data-move proofs; extend `full-chain.test.ts`
- [ ] `memories-read.test.ts` — writers + in-SQL AI-egress exclusions + AI-permission default OFF (KNOW-11)
- [ ] `knowledge-search.test.ts` — pure edit-distance scoring (≤1, ≤2 for ≥6 chars), no FTS5 (KNOW-10)
- [ ] `field-parsers.test.ts` extension for url/email/phone + `field-value-history.test.ts` (KNOW-12)
- [ ] seeding-writer tests for the additive per-contact `scope` column
- [ ] `memory-trash-sweep.test.ts` — idempotent launch-sweep expiry (carry-over from KNOW-09)

*Migration number, threat refs, and per-plan rows are authoritative only after the planner writes the observed head+1 on disk.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Imported phone-Contacts notes captured as an AI-off Memory type | KNOW-13 | Native ContactsProvider read path; device-only | On-device UAT on the Pixel — see device-UAT run-as pattern |
| Sparkle AI-enabled affordance renders only for permitted items | KNOW-11 | `.tsx` screen render; token-color + a11y | Device UAT; verify default-OFF (no sparkle until opt-in) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < module-run seconds
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
