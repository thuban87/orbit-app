---
phase: 24.2-contact-knowledge-egress-search-types
plan: 02
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-10]
files_modified:
  - src/services/knowledge-search.ts
  - src/services/knowledge-search.test.ts
  - src/db/knowledge-search-read.ts
  - src/db/knowledge-search-read.test.ts
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 43000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The knowledge-search corpus read returns, per contact, the eligible searchable text spanning: contact name, Memory `custom_label`/`value`/`note` (registry-`searchable` types, live only), relationship `person_name`, and eligible custom-field content — regardless of which table owns the value. [KNOW-10]"
    - "The scorer matches a term by prefix OR substring OR bounded edit distance: ≤ 1 per term, and ≤ 2 for terms of six or more characters — proven with node cases at each boundary (distance 1 accepted, distance 2 rejected for a 5-char term; distance 2 accepted for a 6-char term). [KNOW-10]"
    - "Internal metadata (uid, provenance, timestamps, allow_ai, ids) is never part of the searchable corpus — a term equal to a metadata value returns no contact. [KNOW-10]"
    - "Off Limits fuel and unconfirmed `source='ai'` fuel never enter the search corpus (the shared `RANKED_FUEL_EXCLUSIONS` fragment is reused where fuel is read). [KNOW-10]"
  prohibitions:
    - statement: "No FTS5 and no new search index; typo tolerance is TypeScript scoring over the SQL-eligible set only (reversing ADR-031 is an owner escalation)."
      status: enforced
      verification: "grep of src for `fts5`/`USING fts`/`VIRTUAL TABLE` yields no match; knowledge-search.ts imports nothing from expo/react-native and is node-tested."
    - statement: "Custom-field content reaches the corpus only through the validated `col_name` boundary; no custom identifier is interpolated into SQL, and no ORDER BY over a custom value is constructed outside `sortExpr()`."
      status: enforced
      verification: "knowledge-search-read.ts routes custom-field values via field-values-dao's validated boundary; grep shows no raw identifier interpolation."
    - statement: "This phase EXPOSES the corpus + searchability metadata; it does NOT rewire the Dashboard search box (Phase 25 consumes)."
      status: flagged-unverified
      verification: "dashboard-read.ts is not modified by this plan (files_modified excludes it)."
  artifacts:
    - src/services/knowledge-search.ts
    - src/db/knowledge-search-read.ts
  key_links:
    - "knowledge-search-read corpus SQL (eligibility) → knowledge-search scorer (TS ranking) — the two-stage no-FTS5 pipeline"
    - "RANKED_FUEL_EXCLUSIONS reuse → structural off-limits/AI exclusion in any fuel-derived corpus row"
---

<objective>
Complete KNOW-10: the searchable knowledge corpus and its TypeScript typo scorer, built out from the tracer's proven single-memory path. Widen the corpus read to every eligible source (names, Memories, relationships, custom fields) and complete the bounded edit-distance scorer. This phase exposes the corpus + searchability metadata as reusable units; the Dashboard Data phase (25) consumes them.

Purpose: One forgiving, storage-agnostic search over remembered knowledge, with no FTS5 (ADR-031 preserved).
Output: `knowledge-search-read.ts` (full corpus) + `knowledge-search.ts` (full scorer), node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bounded edit-distance typo scorer</name>
  <files>src/services/knowledge-search.ts, src/services/knowledge-search.test.ts</files>
  <behavior>
    - Term "gift" (4 chars) vs "gift" → match (distance 0).
    - Term "gft" vs "gift" → match (distance 1).
    - Term "gfit" vs "gift" (2 edits, 4-char term) → NO match (≤1 cap for terms < 6 chars).
    - Term "birthday" (8 chars) vs "birthdya" (distance 2) → match (≤2 cap for terms ≥ 6 chars).
    - Term "birthday" vs "birtxxxx" (distance 4) → NO match.
    - Prefix: term "bir" is a prefix of "birthday" → match regardless of distance.
    - Substring: term "day" is a substring of "birthday" → match.
    - Multi-term query "gft bir": every term must match SOME token in the candidate's corpus text (AND across terms, OR across tokens).
    - Empty/whitespace term → no candidates (returns empty).
  </behavior>
  <read_first>
    - src/logic/favourites-reorder-logic.ts (repo convention for a pure, node-tested `-logic`/service module: exported pure functions, header contract, no react-native import)
    - src/db/fuel-read.ts (`escapeLike` — the SQL half's escaper, referenced by Task 2)
    - .planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md (§knowledge-search.ts — the two-stage shape, D-07 thresholds)
  </read_first>
  <action>
    Complete `knowledge-search.ts` as a pure, react-native-free scorer. Export a bounded edit-distance function (hand-rolled — no dependency; a bounded Levenshtein that early-exits once distance exceeds the cap) and a `scoreCandidate(term, corpusText)` / `rankCandidates(query, candidates)` API. Tokenize both the query and each candidate's corpus text (lowercase, split on whitespace). A query term matches a candidate token when it is a prefix, a substring, or within the edit-distance cap (cap = 1 for terms < 6 chars, 2 for terms ≥ 6). A candidate matches the query when EVERY query term matches at least one of its tokens. Return matches ranked best-first (exact/prefix before fuzzy; more terms matched = higher). Never import from expo/react-native so it stays node-testable. Do NOT use FTS5 or any index.
  </action>
  <verify>
    <automated>npx vitest run src/services/knowledge-search.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test cases exist for each `<behavior>` bullet, including the 5-char-term distance-2 rejection and the 6-char-term distance-2 acceptance (the exact D-07 threshold boundary).
    - `grep -riE "fts5|USING fts|VIRTUAL TABLE" src` returns no match introduced by this plan.
    - `knowledge-search.ts` has no `from "react-native"` / `from "expo` import.
  </acceptance_criteria>
  <done>The scorer implements the exact D-07 thresholds and passes every boundary case.</done>
</task>

<task type="auto">
  <name>Task 2: Full knowledge-search corpus read</name>
  <files>src/db/knowledge-search-read.ts, src/db/knowledge-search-read.test.ts</files>
  <read_first>
    - src/db/knowledge-search-read.ts (the tracer's memory-only corpus read — widen it)
    - src/db/memories-read.ts (`MemoryRow`, registry-`searchable` gating; live `deleted_at IS NULL`)
    - src/db/memory-registry.ts (`MEMORY_TYPE_REGISTRY[type].searchable`)
    - src/db/relationships-read.ts (relationship rows: `person_name`, live filter)
    - src/db/field-values-dao.ts (`getValuesForContact`, the validated `col_name` boundary) and src/db/field-defs-dao.ts (`listDefs`)
    - src/db/fuel-read.ts (`RANKED_FUEL_EXCLUSIONS`, `escapeLike` — reuse for any fuel-derived text)
    - src/db/dashboard-read.ts (the existing LIKE-search idiom + snippet subquery — the corpus eligibility mirrors it, but this plan does NOT modify dashboard-read.ts)
  </read_first>
  <action>
    Widen `knowledge-search-read.ts` to assemble, per contact, the full eligible searchable corpus: contact `name`; Memory `custom_label`/`value`/`note` for live rows whose `type` is registry-`searchable`; relationship `person_name` for live rows; and eligible custom-field values read through the validated `col_name` boundary (never interpolate an identifier). Where any fuel-derived text is included, reuse `RANKED_FUEL_EXCLUSIONS` so off-limits and unconfirmed `source='ai'` never enter the corpus. Return a per-contact record of `{ contactId, corpusText }` (or structured tokens) that the scorer ranks — the SQL is the eligibility filter only; ranking is TypeScript (Task 1). Exclude every internal-metadata column (uid, provenance, ids, timestamps, allow_ai) from the corpus text. Provide a way to expose searchability metadata (which fields are searchable) so Phase 25 can consume it. Keep it a pure read (no transaction, all values `?`-bound).
  </action>
  <verify>
    <automated>npx vitest run src/db/knowledge-search-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A node:sqlite test seeds a contact with a Memory, a relationship, and a custom-field value, then asserts each is findable by a matching term through corpus-read + scorer, and that a non-searchable-type Memory is absent from the corpus.
    - A test asserts an off-limits fuel value and a `source='ai'` fuel value never appear in the corpus.
    - A test asserts a term equal to a memory's `provenance` or `uid` returns no contact.
    - `dashboard-read.ts` is unchanged (not in files_modified).
  </acceptance_criteria>
  <done>The corpus read spans all eligible sources with the exclusions structurally enforced in SQL; internal metadata is never searchable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored knowledge → glanceable search results | Search must not surface off-limits/AI-only/internal content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-05 | Information Disclosure | knowledge-search corpus | high | mitigate | Only registry-`searchable` fields; `RANKED_FUEL_EXCLUSIONS` reused; internal metadata never selected; node parity tests |
| T-24.2-06 | Tampering (SQLi) | custom-field content in corpus | medium | mitigate | Values via validated `col_name` boundary; `escapeLike` + `?`-binds; no identifier interpolation |
</threat_model>

<verification>
- `npx vitest run src/services/knowledge-search.test.ts src/db/knowledge-search-read.test.ts`
- `npx tsc --noEmit`; `grep -riE "fts5|USING fts|VIRTUAL TABLE" src` (expect no new match)
- `npm test` at wave merge.
</verification>

<success_criteria>
Forgiving storage-agnostic search over the full knowledge corpus with the exact D-07 thresholds and no FTS5; internal metadata never searched; exposed for Phase 25.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-02-SUMMARY.md` when done.
</output>
