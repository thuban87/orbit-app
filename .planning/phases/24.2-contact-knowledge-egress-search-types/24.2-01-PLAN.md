---
phase: 24.2-contact-knowledge-egress-search-types
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [KNOW-10, KNOW-11, KNOW-16]
files_modified:
  - src/db/migrations/017-knowledge-egress-datamove.ts
  - src/db/migrations/017-knowledge-egress-datamove.test.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/database.ts
  - src/db/memories-read.ts
  - src/db/memories-read.test.ts
  - src/ai/prompt-types.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
  - src/services/knowledge-search.ts
  - src/services/knowledge-search.test.ts
  - src/db/knowledge-search-read.ts
  - src/db/knowledge-search-read.test.ts
  - docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md
  - docs/decisions/adr-registry.ts
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Migration 017 carries every `fuel` row with `kind='topic' AND source='share'` onto a new `memories` row (type='general', provenance='share') with the fuel text preserved verbatim, then REMOVES the copied fuel rows after a verified per-row copy — so post-migration zero `fuel` rows with `kind='topic' AND source='share'` remain (no double-surfacing) and the migrated-memory count equals the pre-migration eligible-share-capture count with each memory value equal to its source fuel text (no data loss). [KNOW-16]"
    - "Migration 017 is NON-DESTRUCTIVE for the ADR-030 retirement: any `fuel` row with `source='ai'` is PRESERVED by copying it onto an AI-off `memories` row (allow_ai=0) with a verified per-row copy before the fuel row is removed — no `source='ai'` content is deleted without being preserved first (zero such rows exist in production, verified: no shipped producer writes `source='ai'`; this is a safety net). [KNOW-16]"
    - "After migration 017, `memories.allow_ai` exists as `INTEGER NOT NULL DEFAULT 0`; a memory created without an explicit permission reads `allow_ai = 0` (AI OFF). [KNOW-11]"
    - "The AI-egress projection returns a memory ONLY when `allow_ai = 1 AND deleted_at IS NULL`; the same memory at `allow_ai = 0` is absent — proven by a node:sqlite test that flips the column and re-reads. [KNOW-11]"
    - "A single 'general' memory whose value contains a search term is returned by the knowledge-search corpus read + scorer for that term; the same read never returns a contact for a term that matches only internal metadata (uid/provenance/timestamps). [KNOW-10]"
    - "ADR-081 exists with Status 'Accepted', names ADR-030 in its supersession header, and `docs/decisions/adr-registry.ts` contains an `ADR-081` token after regeneration. [KNOW-16]"
  prohibitions:
    - statement: "Migration 017 must be forward-only, assume no particular starting state, run each step in the runner's per-step transaction, and never edit a shipped migration (001–016)."
      status: enforced
      verification: "017-knowledge-egress-datamove.test.ts runs the full chain from an empty DB to TARGET_VERSION and asserts the data-move against a seeded pre-016-shaped fixture; full-chain.test.ts extended to 017."
    - statement: "AI eligibility must be explicit per-item state (`allow_ai`), default OFF, never inferred from memory type or storage location; Group Notes are never sent."
      status: enforced
      verification: "memories-read.test.ts asserts the egress projection SQL contains the literal predicate `allow_ai = 1` and excludes a default-OFF row."
    - statement: "No FTS5 and no new search index — typo tolerance is TypeScript scoring over the SQL-eligible set only."
      status: enforced
      verification: "grep of src for `fts5`/`USING fts`/`VIRTUAL TABLE` returns no new match; knowledge-search.ts is a pure react-native-free scorer."
    - statement: "Retiring ADR-030 is an owner-decided retirement (D-10b) executed NON-DESTRUCTIVELY by this migration: `source='ai'` fuel rows are preserved as AI-off memories (copy-then-remove), never blind-deleted; the irreversible migration is gated by a checkpoint:decision before it runs."
      status: enforced
      verification: "Task 1 is checkpoint:decision; the migration test asserts every `source='ai'` fuel row's content survives as an AI-off memory before removal; ADR-081 records the non-destructive retirement rationale and supersedes ADR-030."
    - statement: "This phase's ADR-030 retirement is the DATA + ADR half only (remove `source='ai'` rows non-destructively, write ADR-081). The still-present legacy confirm CODE — `confirmFuel`/`confirmFuelCore` (fuel-dao.ts:214/282), its Profile handler + import (ContactProfileScreen.tsx:70/705), and the FuelEditor AI-unconfirmed render + Confirm/Dismiss control (FuelEditor.tsx) — is DELIBERATELY NOT removed here and is DEFERRED to Phase 36 (AI Configuration & Prompting — the permission-manager phase this milestone designates for consolidating AI enablement, and the same seam Plan 03's interim gate hands off to). Rationale: (1) this phase's charter is the egress boundary + the non-destructive migration, NOT removal of shipped UI — a separately-scoped change; (2) the path is already INERT in production — no shipped producer writes `source='ai'` (only `source:'user'` ContactProfileScreen.tsx:654 and `source:'share'` CaptureScreen.tsx:277/498), `confirmFuel` only ever PROMOTED such rows to `'manual'`, and migration 017 removes the only rows it could act on, so post-017 the Confirm control has nothing to confirm; (3) `files_modified` here intentionally touches none of those files (review P01-MED — deferred, not overlooked)."
      status: deferred
      verification: "Deferral target named (Phase 36) in Task 1's decision + Task 2's ADR-081 content + Task 1 acceptance; `files_modified` contains none of fuel-dao.ts / ContactProfileScreen.tsx / FuelEditor.tsx (removal is Phase 36's scope, not this phase's)."
  artifacts:
    - src/db/migrations/017-knowledge-egress-datamove.ts
    - src/db/memories-read.ts (AI-egress projection)
    - src/services/knowledge-search.ts
    - src/db/knowledge-search-read.ts
    - docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md
  key_links:
    - "migration 017 → database.ts MIGRATIONS registration + TARGET_VERSION=17 (an unregistered migration never runs)"
    - "memories.allow_ai → ai-context-read.readPromptContext (the sole egress boundary) — the permission is only enforced because the egress read reads the column"
    - "knowledge-search-read corpus SQL → knowledge-search.ts scorer (SQL eligibility filter feeds TS scoring)"
---

<objective>
Tracer slice for Phase 24.2. Prove the phase's whole vertical architecture on one path before any expansion: land the destructive data-move migration (017), and prove a single knowledge item is both AI-permission-gated (default OFF, enforced in SQL) and searchable (SQL eligibility → TS scoring), end-to-end, with passing node:sqlite tests. Also write the superseding ADR (ADR-081) that retires ADR-030.

Purpose: The migration is a one-way door (irreversible in production; no server, no remote DB, no backup). Proving the egress boundary and the search corpus on the tracer's best early-context tokens catches an architectural dead-end after one commit instead of after every expansion layer is already built.

Output: migration 017 (+ TARGET_VERSION=17), the `memories.allow_ai` column, the memories AI-egress projection wired into the sole egress boundary (`readPromptContext`), the `knowledge-search-read` corpus SQL + the `knowledge-search` TS scorer proven on one memory, and ADR-081.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@CLAUDE.md

# Ground-truth decision record (read the 2026-09-01 amendments as overriding)
@docs/dossier/milestone-2/phase-03-contact-knowledge-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-03-planning-notes.md
</context>

<artifacts_this_phase_produces>
The convergence source-grounding pass must treat every symbol below as NEWLY CREATED by Phase 24.2 (do not flag as drift from an older source):

**Migrations & schema**
- `src/db/migrations/017-knowledge-egress-datamove.ts` — `memories.allow_ai INTEGER NOT NULL DEFAULT 0` (additive) + a NON-DESTRUCTIVE data-move (share-capture carry-over to memories with copy-then-remove; `source='ai'` fuel preserved as AI-off memories then removed — no content deleted without a verified copy). `TARGET_VERSION = 17`.
- `src/db/migrations/018-custom-field-scope-history.ts` (Plan 05) — `custom_field_defs.scope`, `custom_field_defs.history_retained`, `custom_field_defs.field_group` columns + `custom_field_value_history` table. `TARGET_VERSION = 18`.

**New DB/service modules**
- `src/db/knowledge-search-read.ts` — SQL eligibility corpus read (memory value/label/note, relationship person_name, eligible custom-field content, contact name).
- `src/services/knowledge-search.ts` — pure TS bounded matcher. The TRACER (this plan) ships prefix/substring + bounded ≤1 edit-distance (enough to prove typo tolerance end-to-end); Plan 02 COMPLETES the same file into the full scorer — it adds the second tier (≤2 edit distance for terms ≥6 chars), the shared `tokenize()` contract, and ranking.
- `src/db/value-history-dao.ts` (Plan 05) — append/read for `custom_field_value_history`.

**New columns / registry members**
- `memories.allow_ai` (per-item AI permission, default OFF).
- `custom_field_defs.scope` (`'global'` | `'contact'`), `custom_field_defs.history_retained`, `custom_field_defs.field_group`.
- `MemoryTypeKey` gains `'imported'` (Plan 06) — displayName "Imported from Contacts App", `aiDefault: false`, `searchable: true`.
- `FieldType` gains `'url' | 'email' | 'phone'` (Plan 04); `field-parsers.ts` gains three parsers.

**AI-egress predicate**
- The memories AI-egress projection in `memories-read.ts` (`allow_ai = 1 AND deleted_at IS NULL`) + a new `sharedMemories` field on `PromptContext`.

**Icon / UI**
- `sparkle` entry in `icon-registry.ts` (Plan 03).

**Backup additions (Plan 07, into the already-live format 4 — no version bump)**
- `memories.allow_ai`, `custom_field_defs.scope`/`history_retained`/`field_group`, the `custom_field_value_history` export/restore, and its `custom_field_value_history` tombstone entity type.

**Docs**
- `docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md` (supersedes ADR-030).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize the one-way migration 017 (irreversibility gate)</name>
  <reversibility rating="one-way">Migration 017 rewrites share-captures onto memories and retires the ADR-030 `source='ai'` fuel path. It is NON-DESTRUCTIVE by design (every removed fuel row is copied to a memory first — see Task 2), but a schema migration + data-move is still irreversible in production: there is no server, no remote DB, and no backup for a user's device, so anything this migration gets wrong is permanent for that user.</reversibility>
  <decision>
    Confirm execution of migration 017. The share-capture carry-over and the ADR-030 retirement were pre-decided by the owner (dossier R-01 resolution / CONTEXT D-10b). This gate confirms the irreversible step is authorized to run and that ADR-081 will record the retirement. It is NOT a request to re-open the decision.

    Disposition (planner-chosen, non-destructive — no owner escalation required): the migration PRESERVES all content. Share-capture rows are copied to memories then removed (KNOW-16 no-loss); any `source='ai'` fuel rows are copied to AI-off memories (allow_ai=0) then removed, so nothing is destroyed. On disk, ZERO `source='ai'` rows can exist in a real DB — verified 2026-09-04: the only shipped fuel producers write `source:'user'` (ContactProfileScreen.tsx:654) or `source:'share'` (CaptureScreen.tsx:277,498); the AI-confirmation UI (FuelEditor / confirmFuelCore, fuel-dao.ts:214) exists but no shipped producer INSERTs `'ai'` rows, so this move is a safety net, not a routine deletion. SCOPE OF THIS RETIREMENT (review P01-MED — deliberate deferral, planner-decided, no owner escalation needed): this phase retires ADR-030 at the DATA + ADR level (remove the rows non-destructively, write ADR-081). The legacy confirm CODE PATH — `confirmFuel`/`confirmFuelCore` (fuel-dao.ts:214/282), the Profile import + handler (ContactProfileScreen.tsx:70/705), and the FuelEditor AI-unconfirmed render + Confirm/Dismiss control — is DEFERRED to **Phase 36 (AI Configuration & Prompting)**, which owns the AI permission-manager consolidation this milestone routes AI enablement into (and is the replacement seam for Plan 03's interim gate). This is NOT a decision reversal to re-open at this gate: removing the control COMPLETES the owner-authorized ADR-030 retirement rather than reversing it, but it is a shipped-UI change outside this phase's egress/migration charter, so it is split out — not folded in and not silently dropped. The path is inert post-017 (nothing writes `source='ai'`; `confirmFuel` only promoted such rows; 017 removes them), so deferring the code removal carries no egress or data risk. If the owner instead wants a hard DELETE of `source='ai'` rows (destroying content), that is a separate authorization to request here — the default is preserve. NOTE (review P01-LOW): a copied `source='ai'` row that is also `kind='off_limits'` becomes a plain AI-off (`allow_ai=0`) general memory and does NOT retain an off-limits label (memories have no such concept); the "never reaches AI" outcome is preserved by the `allow_ai=0` default. Zero such rows exist in production (no shipped `source='ai'` producer), so this is a safety-net edge, surfaced here for the owner's awareness at the gate.
  </decision>
  <options>
    - Proceed: land migration 017 (non-destructive data-move + `allow_ai` column) and write ADR-081.
    - Halt: if any assumption below is now false, stop and surface to the owner:
      (a) migration head on disk is still 016 / TARGET_VERSION 16 (re-verify: `ls src/db/migrations/` + `grep TARGET_VERSION src/db/database.ts`);
      (b) a producer of `source='ai'` fuel rows has since shipped (re-verify: `grep -rn "source" src --include=*.tsx --include=*.ts | grep "addFuel" -A3` and confirm none pass `source: "ai"`) — if one has, the safety-net copy still preserves the content, but flag it to the owner so the disposition is re-confirmed.
  </options>
  <read_first>
    - src/db/database.ts (TARGET_VERSION + MIGRATIONS array — verify head+1 on disk before writing 017)
    - docs/decisions/ADR-030-explicit-confirmation-of-ai-proposed-fuel.md (the Accepted ADR being retired)
    - src/db/capture-dao.ts and src/screens/CaptureScreen.tsx (share-capture writer: `kind:'topic'`, `source:'share'` — the rows carried over)
    - src/db/fuel-dao.ts (`confirmFuelCore` at :214 — the shipped confirm path; the FuelSource vocabulary at :45)
  </read_first>
  <acceptance_criteria>
    - `ls src/db/migrations/` shows 016 as the highest numbered migration and `grep TARGET_VERSION src/db/database.ts` shows 16 — confirming 017 is head+1. If either differs, HALT and write the observed head to the SUMMARY.
    - A grep confirms no shipped `addFuel`/`addFuelCore` call passes `source: "ai"` (the "no producer" premise). If one is found, record it in the SUMMARY and re-confirm the disposition with the owner (the copy still preserves content).
    - The owner (or configured auto-advance) has authorized the irreversible step before Task 2 runs.
    - The legacy confirm CODE PATH is DEFERRED, not removed here: `files_modified` for this plan contains none of `src/db/fuel-dao.ts`, `src/screens/ContactProfileScreen.tsx`, or `src/components/FuelEditor.tsx`; the SUMMARY records "confirmFuel path removal deferred to Phase 36 (inert post-017)". If the executor finds itself editing any of those three files, STOP — that removal is Phase 36 scope, not this phase's.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the non-destructive migration 017 (preserve disposition), or reply "hard-delete source='ai'" to instead authorize destroying those rows, or name a failed assumption (head ≠ 016 / a `source='ai'` producer shipped) to HALT.</resume-signal>
  <done>The irreversibility gate is recorded; execution is authorized to write the non-destructive migration 017.</done>
</task>

<task type="tracer">
  <name>Task 2: Migration 017 — allow_ai column + destructive data-move + ADR-081</name>
  <files>src/db/migrations/017-knowledge-egress-datamove.ts, src/db/migrations/017-knowledge-egress-datamove.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts, docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md, docs/decisions/adr-registry.ts</files>
  <read_first>
    - src/db/migrations/016-contact-knowledge.ts (the additive-table analog; `Migration` shape, exported CREATE consts, `apply(exec, deps)`)
    - src/db/migrations/006-normalize-custom-field-values.ts (the data-move + integrity-error-class analog — proof-of-copy pattern)
    - src/db/migrations/007-tombstones.ts (additive `ALTER TABLE … ADD COLUMN` idiom)
    - src/db/database.ts (MIGRATIONS registration + TARGET_VERSION; runner owns per-step transaction)
    - src/db/capture-dao.ts (share rows: `kind='topic'`, `source='share'`, `text`, `url`, `created_at`, `contact_id`, `uid`)
    - src/db/memories-dao.ts (memories column set + `provenance` values `'user'|'import'|'share'`; `MemoryProvenance`)
    - src/db/memory-registry.ts (`DEFAULT_MEMORY_TYPE_KEY = 'general'` — the default type for migrated captures)
    - src/db/uid.ts (`newUid`) and src/utils/dates.ts (`formatLocalDate` — never `toISOString`)
    - docs/decisions/ADR-030-explicit-confirmation-of-ai-proposed-fuel.md and docs/decisions/_archive or existing ADR files for the ADR markdown structure + `adr-registry.ts` generation (`npm run gen:adr-registry`)
  </read_first>
  <action>
    Create migration `017-knowledge-egress-datamove.ts` exporting a `Migration` with `version: 17`. Define a named integrity-error class (mirror `Migration006IntegrityError` — a `code` field + recognizable name) so any mismatch throws and the runner rolls the step back. In `apply(exec, deps)`, in this order, one `execAsync`/`runAsync` per step:
    (1) `ALTER TABLE memories ADD COLUMN allow_ai INTEGER NOT NULL DEFAULT 0;` (additive; per-item AI permission, default OFF — D-08).
    (2) Data-move (share-capture carry-over, KNOW-16 — copy-then-remove, non-destructive): SELECT every `fuel` row where `kind = 'topic' AND source = 'share'` (capture id/uid/text/url/contact_id/created_at). For each, INSERT a `memories` row with `type = 'general'`, `provenance = 'share'`, `value` = the fuel `text`, `url` = the fuel `url`, a freshly minted `uid`, `contact_id` copied, `created_at` copied from the fuel row, `modified_at` = the migration timestamp, `allow_ai = 0`. Build a per-row MAPPING as you go (source fuel uid → new memory uid + value). PROOF-OF-COPY (stronger than count equality — a duplicated source masking a dropped one must be caught): after the loop, assert the mapping has exactly one entry per eligible source row AND each mapped memory's stored `value` equals its source fuel `text`; on any mismatch throw the integrity error. Only THEN `DELETE FROM fuel WHERE id IN (…the copied ids…)` (or delete by the mapped uids). After deletion assert `SELECT COUNT(*) FROM fuel WHERE kind='topic' AND source='share'` is 0 (no double-surfacing).
    (3) ADR-030 retirement (NON-DESTRUCTIVE — copy-then-remove, mirrors step 2): SELECT every `fuel` row where `source = 'ai'` (regardless of `kind`, INCLUDING any `kind='off_limits'` row). For each, INSERT a `memories` row with `type = 'general'`, `provenance = 'user'` (documented: an unconfirmed AI suggestion becomes a plain AI-off memory on the user's contact — zero such rows expected in production per Task 1), `value` = the fuel `text`, `url` = the fuel `url`, fresh `uid`, `contact_id`/`created_at` copied, `modified_at` = migration timestamp, `allow_ai = 0`. OFF-LIMITS TREATMENT (review P01-LOW — explicit, not silent): off_limits is a fuel `kind`, and `memories` has no off-limits concept, so a copied `source='ai' AND kind='off_limits'` row does NOT carry the off-limits classification forward. This is acceptable and deliberate because the memory is written `allow_ai=0`: the off_limits guarantee ("never reaches AI") is preserved by the AI-off default, not by a transferred label — and there is no shipped `source='ai'` producer, so this is a safety-net path with zero expected production rows. Build the same per-row mapping + proof-of-copy, then `DELETE FROM fuel WHERE id IN (…the copied ids…)`, then assert `SELECT COUNT(*) FROM fuel WHERE source='ai'` is 0. Do NOT `DELETE FROM fuel WHERE source='ai'` without the verified copy first — the retirement preserves content (see Task 1 disposition).
    Register `migration017` in `MIGRATIONS` (after `migration016`) and set `TARGET_VERSION = 17` in database.ts. Do NOT set any bootstrap PRAGMA (caller owns WAL/foreign_keys).
    Write `docs/decisions/ADR-081-retire-ai-proposed-fuel-for-explicit-per-item-permission.md`: Status Accepted, a "Supersedes: ADR-030" header, context (the AI-proposed-fuel confirm path shipped a confirm UI — `confirmFuelCore` / `FuelEditor` — but no shipped producer INSERTs `source='ai'` rows; explicit per-item AI permission — this phase's `memories.allow_ai` — replaces the implicit `source='ai'` provenance model), decision (retire the AI-proposed-fuel path; migration 017 PRESERVES any `source='ai'` content as AI-off memories then removes the fuel rows — non-destructive, no data loss), and consequences. In the Consequences section, RECORD THE DEFERRAL explicitly (review P01-MED): Phase 24.2 lands the DATA + ADR half of the retirement only; the still-present confirm CODE — `confirmFuel`/`confirmFuelCore` (fuel-dao.ts:214/282), the Profile handler (ContactProfileScreen.tsx:705), and the FuelEditor AI-unconfirmed render + Confirm/Dismiss control — is left in place but INERT (no producer writes `source='ai'`; 017 removes the only rows it could act on) and its removal is DEFERRED to Phase 36 (AI Configuration & Prompting), which owns the AI permission-manager consolidation. State that until Phase 36 removes it the dead control remains a latent revival surface, mitigated only by the absence of any `source='ai'` producer — so Phase 36 must delete the path rather than leave it. Then regenerate the ADR registry with `npm run gen:adr-registry` so an `ADR-081` token reaches `docs/decisions/adr-registry.ts`. Do NOT run `npm run graph:build` (build discipline — owner runs it).
    Write the migration test using the SEEDED-AT-16 pattern (addresses review concern P01-MED: `fuel` rows cannot be seeded into an empty v0 DB, and `memories` does not exist until migration 016 — so the runner would blow up applying 017 against unseeded tables). Mirror `016-contact-knowledge.test.ts`'s `migrateTo16` helper: (i) `runMigrations(exec, MIGRATIONS, 16, deps)` to bring the DB to version 16 (both `fuel` and `memories` tables now exist in their pre-017 shape), (ii) seed the pre-017 fixture directly (contacts; `fuel` rows with `kind='topic' AND source='share'`; a `fuel` row with `source='ai'`; AND — per P01-LOW below — one `fuel` row with `source='ai' AND kind='off_limits'`), (iii) `runMigrations(exec, MIGRATIONS, 17, deps)` to apply ONLY migration 017. Then assert (a) migrated share-capture memory count == eligible share-fuel count with each value preserved, (b) the `source='ai'` row's content survives as an AI-off memory (allow_ai=0), (c) `allow_ai` column present and default 0, (d) post-migration zero `fuel` rows with `kind='topic' AND source='share'` and zero with `source='ai'`, (e) [P01-LOW] the seeded `source='ai' AND kind='off_limits'` row's TEXT survives as a `memories` row with `allow_ai=0` — documenting the deliberate treatment: the off-limits *kind* does NOT carry onto the general memory (memories have no off-limits concept), but because the copy is written `allow_ai=0` the content is never AI-egress-eligible, so the privacy outcome off_limits guarantees is preserved by the AI-off default (not by a transferred classification). Extend `full-chain.test.ts` to 017.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/017-knowledge-egress-datamove.test.ts src/db/migrations/full-chain.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION = 17" src/db/database.ts` matches and `migration017` appears in the `MIGRATIONS` array immediately after `migration016`.
    - The migration test asserts a per-row mapping (not just a count): the migrated-memory count equals the count of `fuel` rows with `kind='topic' AND source='share'`, AND each migrated memory's `value` equals its source fuel `text` (a duplicated-source-masking-a-drop case fails the assertion).
    - After the chain, a query for `fuel` rows with `kind='topic' AND source='share'` returns zero rows AND a query for `fuel` rows with `source='ai'` returns zero rows (both copied-then-removed).
    - The test asserts the seeded `source='ai'` row's `text` survives as a `memories` row with `allow_ai=0` (content preserved, non-destructive).
    - `PRAGMA table_info(memories)` includes an `allow_ai` column with `notnull=1` and `dflt_value=0`.
    - `docs/decisions/ADR-081-*.md` exists with `**Status:** Accepted` and names ADR-030 as superseded; `grep -c "ADR-081" docs/decisions/adr-registry.ts` is ≥ 1.
    - No `UNIQUE(contact_id, field_def_id)` or `custom_field_values.uid UNIQUE` constraint is altered (this migration does not touch custom-field tables).
  </acceptance_criteria>
  <done>Migration 017 lands the additive `allow_ai` column and the verified, no-loss NON-DESTRUCTIVE data-move (both share-capture and `source='ai'` content copied to memories then removed); TARGET_VERSION is 17; ADR-081 supersedes ADR-030; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end proof — AI-egress gate + searchable, on one memory</name>
  <files>src/db/memories-read.ts, src/db/memories-read.test.ts, src/ai/prompt-types.ts, src/db/ai-context-read.ts, src/db/ai-context-read.test.ts, src/services/knowledge-search.ts, src/services/knowledge-search.test.ts, src/db/knowledge-search-read.ts, src/db/knowledge-search-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (the read-choke-point + in-SQL exclusion pattern: `RANKED_FUEL_EXCLUSIONS`, editor-vs-projection split, `escapeLike`)
    - src/db/memories-read.ts (existing `MemoryRow`, `listMemoriesForContact`, `resolveVisibility`; add `allow_ai` to `MemoryRow` + the AI-egress projection)
    - src/db/ai-context-read.ts (the SOLE outbound projection `readPromptContext`; how `getRankedFuel` + `readSharedFields` feed it — the memories egress read plugs in here)
    - src/ai/prompt-types.ts (`PromptContext`, `SharedFieldValue`, `RankedFuelEntry` — add a `sharedMemories` field mirroring `SharedFieldValue`)
    - src/db/memory-registry.ts (`MEMORY_TYPE_REGISTRY[type].searchable` + `.aiDefault` — the registry gate; `isMemoryTypeKey`)
    - src/db/relationships-read.ts and src/db/field-values-dao.ts (for the corpus read shape; Plan 02 widens these — the tracer proves only the memory path)
  </read_first>
  <action>
    (A) AI-egress projection: add `allow_ai: number` to `MemoryRow` (and the SELECT column lists in memories-read.ts). Add an exported projection `listAiEligibleMemories(exec, contactId)` whose SQL selects only rows where `allow_ai = 1 AND deleted_at IS NULL` (explicit per-item state — never inferred from `type`). Keep this SEPARATE from `listMemoriesForContact` (the editor read, which must still surface all live rows). Export a reusable `MEMORY_AI_ELIGIBILITY` SQL fragment string (`allow_ai = 1 AND deleted_at IS NULL`) so consumers cannot re-derive it. Group Notes are never memories and never enter this path.
    (B) Wire into the sole egress boundary: add a `sharedMemories` field to `PromptContext` (reuse the `SharedFieldValue` element shape `{ label: string; value: string }`). Make it OPTIONAL (`readonly sharedMemories?: ReadonlyArray<SharedFieldValue>`) OR inventory and update every existing `PromptContext` constructor — the type is currently constructed in production (`ai-context-read.readPromptContext`) and in tests (e.g. `src/logic/ai-suggestion-compose-integration.test.ts` builds a context with `sharedFields`); a required field without updating them breaks `tsc`. Grep first: `grep -rn ": PromptContext\|PromptContext =\|sharedFields:" src` and either default each to `[]` or make the field optional. In `readPromptContext`, populate `sharedMemories` from `listAiEligibleMemories`, mapping each row to `{ label: custom_label ?? registry displayName, value }`, skipping null/blank values. This keeps the single-egress-projection invariant intact: opted-in memories cross the boundary only through this one read. (Prompt-string formatting is refined by the AI phases; the DATA boundary is enforced here.)
    Also add a one-line in-file note in `ai-context-read.ts` confirming the pre-existing `getRankedFuel`/`RANKED_FUEL_EXCLUSIONS` fuel path is UNAFFECTED by migration 017: `RANKED_FUEL_EXCLUSIONS` (`fuel-read.ts:133`) still excludes `source='ai'`; migration 017 removed those rows, so the predicate now simply matches none — no code change to the fuel path is needed (states it so a later reviewer does not re-litigate).
    (C) Search corpus read: create `knowledge-search-read.ts` exporting a per-contact eligibility read for MEMORIES only in this tracer — select `memories.value`, `custom_label`, `note` for live (`deleted_at IS NULL`) rows whose `type` is registry-`searchable`. CRITICAL two-stage rule: the SQL is the ELIGIBILITY filter only (which rows are searchable) — it MUST NOT filter by the search term with `LIKE`, because a typo would fail a `LIKE` match and never reach the TS scorer, defeating typo tolerance. Return the eligible corpus text per contact; ALL term matching (prefix/substring/edit-distance) happens in the TS scorer (D). `escapeLike` is only relevant if a later eligibility predicate needs a literal `%`/`_`, not for term matching. Never select internal metadata (uid/provenance/timestamps/allow_ai) into the searchable text.
    (D) Scorer: create `knowledge-search.ts` — a pure, react-native-free module exporting a bounded matcher that does prefix, substring, AND a bounded ≤1 edit-distance match (a small hand-rolled bounded Levenshtein that early-exits once the distance exceeds 1 — no dependency, no FTS5). RECONCILED (review P01-MED — the earlier text said "prefix/substring only, edit-distance is Plan 02" while this plan's artifact + acceptance require a one-edit typo match; that internal contradiction is resolved here in favour of implementing the ≤1 primitive in the tracer): the tracer MUST prove the two-stage typo-tolerance architecture end-to-end, because typo tolerance is the entire reason the SQL stage carries no term-`LIKE` filter — a matcher that could not accept a one-edit typo would not exercise the architecture the tracer exists to prove. So the tracer ships the ≤1 edit-distance primitive; Plan 02 EXPANDS this same file into the full scorer (adds the second tier — ≤2 edit distance for terms ≥6 chars — the shared `tokenize()` contract, ranking, and the widened four-source corpus). It receives the eligible corpus (not a pre-term-filtered set) and decides matches. Prove one 'general' memory is returned for a matching term AND for a ≤1-edit typo of that term.
    Node tests: (a) memories-read.test.ts flips `allow_ai` 0→1 and asserts the egress projection excludes then includes the row; ai-context-read.test.ts asserts `readPromptContext().sharedMemories` is empty at default OFF and, after opt-in, an end-to-end assertion that the opted-in memory VALUE appears in `readPromptContext().sharedMemories` (the DATA egress boundary this phase enforces; the prompt-string serialization into the actual outbound payload by `resolvePrompt` is deferred to Phase 36 per the (B) note above), not only the raw DAO read; (b) knowledge-search-read.test.ts + knowledge-search.test.ts assert the memory is found for its term (including via the TS scorer with NO SQL term-filter) and NOT found for a term appearing only in `provenance`/`uid`.
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-read.test.ts src/db/ai-context-read.test.ts src/db/knowledge-search-read.test.ts src/services/knowledge-search.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allow_ai = 1" src/db/memories-read.ts` matches inside the AI-egress projection / `MEMORY_AI_ELIGIBILITY` fragment.
    - memories-read.test.ts contains a case that inserts a memory, reads `listAiEligibleMemories` (expect empty), sets `allow_ai=1`, re-reads (expect the row).
    - ai-context-read.test.ts asserts `readPromptContext().sharedMemories` is `[]` for a contact whose memories are all default-OFF, and after opt-in the memory's VALUE is present in `readPromptContext().sharedMemories` (the DATA egress boundary enforced here; prompt-string serialization into the actual outbound payload by `resolvePrompt` in `src/ai/prompt-template.ts` is deferred to Phase 36 — see the (B) note), not only the raw DAO read.
    - `grep -rn ": PromptContext\|PromptContext =\|sharedFields:" src` was run and every existing `PromptContext` constructor either compiles unchanged (field made optional) or was updated (recorded in the SUMMARY); `tsc --noEmit` is clean.
    - knowledge-search-read.ts contains NO term-`LIKE` filter on the search term (eligibility SQL only); knowledge-search.test.ts asserts a typo term (edit-distance 1) still matches via the tracer's bounded ≤1 TS matcher (the ≤2-for-≥6-char tier is Plan 02, not asserted here), and a term equal to the memory's `provenance` value does NOT match.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>One memory is provably AI-permission-gated (default OFF, enforced in the sole egress read) and searchable (SQL eligibility → TS match) end-to-end — the phase's architecture is proven before expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device data → AI provider | The only egress of contact-derived knowledge; crosses in `readPromptContext` when the user invokes AI |
| migration → user's irreplaceable local DB | No server, no backup, no remote repair — a bad migration is permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-01 | Information Disclosure | memories AI-egress projection | high | mitigate | Explicit `allow_ai = 1` predicate in SQL, default 0; node test proves default-OFF exclusion; never inferred from type/location (ADR-050/078) |
| T-24.2-02 | Denial of Service (data) | migration 017 destructive data-move | high | mitigate | Forward-only, no assumed start state, per-step transaction, migration-006 proof-of-copy (verify count before delete) + named integrity error; checkpoint:decision gate |
| T-24.2-03 | Tampering / Elevation | ADR-030 retirement (legacy confirm path left inert) | medium | accept | Owner-decided retirement (D-10b); 017 removes all `source='ai'` rows; legacy `confirmFuel`/FuelEditor confirm control stays present-but-inert (no producer feeds it) with CODE removal DEFERRED to Phase 36; recorded in ADR-081 as a latent revival surface for Phase 36 to delete |
| T-24.2-04 | Information Disclosure | knowledge-search corpus read | medium | mitigate | Only registry-`searchable` fields enter the corpus; internal metadata never selected; parity asserted by test |
</threat_model>

<verification>
- `npx vitest run src/db/migrations/017-knowledge-egress-datamove.test.ts src/db/migrations/full-chain.test.ts src/db/memories-read.test.ts src/db/ai-context-read.test.ts src/db/knowledge-search-read.test.ts src/services/knowledge-search.test.ts`
- `npx tsc --noEmit` and `npm run check:colors` (no UI here, but keep the gate green)
- `grep -n "TARGET_VERSION = 17" src/db/database.ts`
- Full node suite `npm test` at wave merge.
</verification>

<success_criteria>
Migration 017 lands (data-move no-loss + `allow_ai` column, TARGET_VERSION 17); one memory is AI-gated (default OFF) and searchable end-to-end; ADR-081 supersedes ADR-030; all node tests green.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-01-SUMMARY.md` when done.
</output>
