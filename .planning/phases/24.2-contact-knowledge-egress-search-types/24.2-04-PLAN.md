---
phase: 24.2-contact-knowledge-egress-search-types
plan: 04
type: execute
wave: 2
depends_on: [24.2-01]
autonomous: true
requirements: [KNOW-13]
files_modified:
  - src/schemas/types.ts
  - src/db/field-parsers.ts
  - src/db/field-parsers.test.ts
  - src/components/field-widgets/types.ts
  - src/components/field-widgets/TextFieldWidget.tsx
  - src/components/FieldValueInput.tsx
  - src/components/CustomFieldValue.tsx
  - src/components/FieldDefForm.tsx
  - src/components/field-def-form-logic.ts
  - src/components/field-def-form-logic.test.ts
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`FieldType` includes `url`, `email`, and `phone` in addition to the existing members, and `field-parsers.ts` provides exactly one parser per member via the exhaustive `Record<FieldType, …>` (a member without a parser fails `tsc`). [KNOW-13]"
    - "Each new parser (url/email/phone) is a permissive read-time validator: a plausible value returns `{ ok: true, value }` with the input string returned byte-for-byte unchanged; an implausible one returns `{ ok: false }` (flag), and the stored bytes are never rewritten or cleared. (The byte-for-byte claim is scoped to these THREE new types — the existing number/date/toggle parsers legitimately canonicalize.) [KNOW-13]"
    - "The custom-field def type picker offers the nine user-facing types (Text, Long Text, Number, Date, Yes/No, URL, Email, Phone, Choice); the value widget for url/email/phone is a single-line text input with the appropriate keyboard hint, plumbed through a new optional `keyboardType` on `FieldWidgetProps` (the existing `TextFieldWidget` forwards no keyboard hint today). [KNOW-13]"
  prohibitions:
    - statement: "One parser per FieldType, never pairwise converters; `custom_field_values.value` stays raw TEXT forever; a failing value is flagged (tap-to-fix error state), never coerced or cleared (ADR-014 / D-12)."
      status: enforced
      verification: "field-parsers.test.ts asserts a failing url/email/phone value returns `{ ok: false }` and the input string is returned unchanged for a passing value; parsers remain a `Record<FieldType,…>`."
    - statement: "Any sort/filter over a custom-field value routes through `sortExpr()`; url/email/phone sort as raw TEXT (no new CAST) and no identifier is interpolated into ORDER BY."
      status: enforced
      verification: "field-sort.ts is unchanged (the three new types fall to the default raw-TEXT branch); not in files_modified."
  artifacts:
    - src/schemas/types.ts (FieldType +url/email/phone)
    - src/db/field-parsers.ts (three new parsers)
    - src/components/FieldDefForm.tsx (nine-type picker)
  key_links:
    - "FieldType union → field-parsers Record exhaustiveness (tsc) → widget map — a new type without a parser or widget fails the build"
---

<objective>
Complete KNOW-13 (part 1): expand the custom-field type set to the nine user-facing types by adding `url`, `email`, and `phone` — one permissive read-time parser each, the type-picker options, and the value widgets. Additive TypeScript only (no migration in this plan).

Purpose: Broaden typed custom fields without weakening the TEXT-forever / flag-never-coerce invariants.
Output: `FieldType` +3 members, +3 parsers, the nine-type def picker, and url/email/phone value inputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-CONTEXT.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-PATTERNS.md
@.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FieldType +url/email/phone and three parsers</name>
  <files>src/schemas/types.ts, src/db/field-parsers.ts, src/db/field-parsers.test.ts</files>
  <behavior>
    - The validation rule for each new type is documented in a top-of-file comment as a single permissive rule, erring PERMISSIVE (ADR-014 "flag, never coerce"; a spurious flag on a legitimate value is a real UX cost — a tap-to-fix error on data that was previously untyped text). Local-dev URLs, `mailto:`, international/extension phone formats, plus-addressed emails should PASS rather than flag.
    - url: "https://example.com" → ok; "example.com" → ok (permissive — scheme optional); "not a url with spaces only" → flag; empty/null → ok with null.
    - email: "a@b.co" → ok; "a+tag@b.co" → ok; "a@b" → flag (documented rule: require exactly one `@` and a `.` in the domain part) — pick ONE rule, state it in the header, and test it explicitly; "plainstring" → flag; empty/null → ok with null.
    - phone: "+1 (555) 123-4567" → ok; "555.123.4567" → ok; "call me maybe" → flag (documented rule: must contain at least N digits and only phone-plausible characters); empty/null → ok with null.
    - Every parser: a passing value returns the input string BYTE-FOR-BYTE unchanged (raw TEXT, no trimming/canonicalization of the three new types); a failing value returns `{ ok: false }`; never throws.
  </behavior>
  <read_first>
    - src/schemas/types.ts (`FieldType` union at line 20; the `validTypes` array inside `isFieldDef` at line 87)
    - src/db/field-parsers.ts (the `Record<FieldType, …>` parsers map; the `ParseResult` type; the permissive/never-coerce contract in the header)
    - src/db/field-sort.ts (`sortExpr()` — confirm the new types fall to the default raw-TEXT branch; do NOT change it)
    - HANDOFF.md §14 (custom-field invariants: TEXT forever, one parser per type, flag-not-coerce)
  </read_first>
  <action>
    Add `"url" | "email" | "phone"` to the `FieldType` union in schemas/types.ts and to the `validTypes` array inside `isFieldDef`. In field-parsers.ts add three permissive read-time parsers to the `Record<FieldType,…>` map (the record's exhaustiveness makes tsc require them): each null/blank → `{ ok: true, value: null }`; otherwise apply a permissive validation and return `{ ok: true, value: r }` (raw, unmodified) on success or `{ ok: false }` on failure. Never throw, never rewrite bytes, never coerce a failing value. Update the parser-module header count/comment to reflect the new total (state the number of parsers as the count after this change). Add node tests for each `<behavior>` bullet.
  </action>
  <verify>
    <automated>npx vitest run src/db/field-parsers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\"url\"|\"email\"|\"phone\"" src/schemas/types.ts` matches in both the `FieldType` union and the `validTypes` array.
    - The `parsers` map has `url`, `email`, `phone` keys; removing any one fails `tsc` (exhaustive Record).
    - field-parsers.test.ts proves a failing value for each new type returns `{ ok: false }` and a passing value is returned byte-for-byte unchanged.
    - src/db/field-sort.ts is unchanged.
  </acceptance_criteria>
  <done>Nine field types with one permissive parser each; storage stays raw TEXT and failing values flag.</done>
</task>

<task type="auto">
  <name>Task 2: Type picker options + url/email/phone widgets with keyboard hints</name>
  <files>src/components/FieldDefForm.tsx, src/components/field-def-form-logic.ts, src/components/field-def-form-logic.test.ts, src/components/field-widgets/types.ts, src/components/field-widgets/TextFieldWidget.tsx, src/components/FieldValueInput.tsx, src/components/CustomFieldValue.tsx</files>
  <read_first>
    - src/components/FieldDefForm.tsx (`FIELD_TYPES` list at line 54; the type option renderer at ~line 221)
    - src/components/field-def-form-logic.ts (the pure def-form logic + its tests — where type validity/labels live)
    - src/components/FieldValueInput.tsx and src/components/CustomFieldValue.tsx (the type→widget mapping; the `FieldWidgetProps` controlled-over-TEXT contract)
    - src/components/field-widgets/types.ts (VERIFIED: `FieldWidgetProps` at :13 has value/onChange/label/options/contactId/colName/testID — it has NO `keyboardType`; add one)
    - src/components/field-widgets/TextFieldWidget.tsx (VERIFIED: at :16 destructures only `FieldWidgetProps` and forwards no `keyboardType` to its `TextInput` — it must forward the new prop)
  </read_first>
  <action>
    First plumb the keyboard hint (the existing widget cannot carry it): in `field-widgets/types.ts` add `keyboardType?: import("react-native").TextInputProps["keyboardType"]` to `FieldWidgetProps` (optional, so the other widgets are unaffected). In `TextFieldWidget.tsx` destructure `keyboardType` and pass it to the `TextInput` (defaulting undefined → RN's default keyboard).
    Add URL, Email, and Phone options to `FIELD_TYPES` in FieldDefForm.tsx (labels per the nine-type set: Text, Long Text, Number, Date, Yes/No, URL, Email, Phone, Choice), and update field-def-form-logic.ts (+ its test) for any type-list/validation the picker depends on. In FieldValueInput.tsx / CustomFieldValue.tsx, map `url`/`email`/`phone` to the single-line `TextFieldWidget`, passing `keyboardType` = `"url"` / `"email-address"` / `"phone-pad"` respectively — reuse the existing widget; do NOT add a new picker/library. Keep widgets controlled-over-TEXT. All colours via theme tokens.
  </action>
  <verify>
    <automated>npx vitest run src/components/field-def-form-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On the Pixel: creating a custom field shows URL/Email/Phone as type options; entering a value for each uses a single-line input with the matching keyboard; a value failing its type renders the tap-to-fix error state (not cleared).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "keyboardType" src/components/field-widgets/types.ts` and `src/components/field-widgets/TextFieldWidget.tsx` both match (prop added + forwarded).
    - `grep -niE "url|email|phone" src/components/FieldDefForm.tsx` shows the three new type options.
    - `url`/`email`/`phone` resolve to a text widget in the value-input map with `keyboardType` set to `url`/`email-address`/`phone-pad` respectively (tsc-checked switch/map is exhaustive).
    - `npm run check:colors` and `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The def picker offers nine types and url/email/phone values render in a text input with the right keyboard, plumbed through the new optional `keyboardType` prop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered custom-field value → storage/sort | Values are raw TEXT; type drives widget + read-time validation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.2-10 | Tampering (data integrity) | new parsers | medium | mitigate | Read-time validators only; never coerce/clear; `{ ok: false }` flags; TEXT storage unchanged |
| T-24.2-11 | Tampering (SQLi) | custom-field ordering | low | mitigate | New types sort via `sortExpr()` default raw-TEXT branch; no identifier interpolation |
</threat_model>

<verification>
- `npx vitest run src/db/field-parsers.test.ts src/components/field-def-form-logic.test.ts`
- `npx tsc --noEmit` + `npm run check:colors`
- Device UAT per Task 2 `<human-check>`.
- `npm test` at wave merge.
</verification>

<success_criteria>
Nine custom-field types with one permissive parser each, a nine-type def picker, and url/email/phone value inputs — TEXT-forever and flag-not-coerce preserved.
</success_criteria>

<output>
Create `.planning/phases/24.2-contact-knowledge-egress-search-types/24.2-04-SUMMARY.md` when done.
</output>
