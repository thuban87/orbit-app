---
phase: 14-ai-message-suggestions
plan: 06
type: execute
wave: 4
depends_on: ["14-05"]
files_modified:
  - .planning/phases/14-ai-message-suggestions/14-VALIDATION.md
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04]
must_haves:
  truths:
    - "Automated evidence covers the schema, secret, provider, privacy-context, and Compose lifecycle boundaries."
    - "A release-build device check confirms native SecureStore and interactive AI Suggest behavior without real personal data."
  artifacts:
    - ".planning/phases/14-ai-message-suggestions/14-VALIDATION.md"
  key_links:
    - "Validation evidence links the exact focused Vitest suites to the release-build UAT checklist."
---

<objective>
Record and run the Phase 14 automated and physical-device validation contract.

Purpose: confirm the native dependency and human-facing privacy workflow work together before release.
Output: reproducible validation evidence and owner-approved Pixel release-build UAT.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-02-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-03-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-04-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 14 validation matrix and capture automated evidence</name>
  <files>.planning/phases/14-ai-message-suggestions/14-VALIDATION.md</files>
  <action>Create the validation matrix before release UAT. Map AI-01 through AI-04 to the precise test files from Plans 01–05 and record command output/status for migration/settings/key isolation, provider/Custom URL/error behavior, outbound-context/prompt identity, field sharing, and Compose lifecycle/navigation. Include the full `npx vitest run`, `npx tsc --noEmit`, and `npm run check:colors` gates. Do not include production credentials, prompts, endpoint URLs, contact data, request bodies, headers, or provider response text in evidence.</action>
  <verify><automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <done>14-VALIDATION.md has a requirement-to-test matrix and sanitized automated pass/fail evidence for every Phase 14 safety boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the release build on Pixel with synthetic data</name>
  <files>.planning/phases/14-ai-message-suggestions/14-VALIDATION.md</files>
  <action>Run this checkpoint only after Task 1 automated gates pass. Record concise, sanitized release-build observations in 14-VALIDATION.md; never copy credentials, prompts, endpoint URLs, request bodies, headers, contact data, or raw responses into evidence.</action>
  <what-built>SecureStore-backed provider configuration, prompt inspection/acknowledgement, and the Compose-only AI Suggest lifecycle.</what-built>
  <how-to-verify>
    1. Run a clean Expo prebuild/release build and install it on a physical Pixel; verify SecureStore setup works after launch and no key is displayed in Settings.
    2. With synthetic data and disposable test credentials only, exercise OpenAI, Anthropic, Gemini, and manual-model fallback; verify Custom rejects cleartext/private/redirect paths.
    3. Inspect the exact prompt before the first request for each provider and again later; verify truncation disclosure and field opt-in behavior, then cancel and navigate away during a request.
    4. Start from a profile and Compose, confirm a non-empty draft is never replaced silently, and verify Copy/SMS remain unchanged and no suggestion records a touchpoint or changes last-contact.
    5. Exercise offline, bad-key, and rate-limit outcomes; verify Retry is deliberate and visible errors/logs omit prompts, keys, headers, endpoints, request bodies, and response bodies.
  </how-to-verify>
  <verify><automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <done>A physical Pixel release build has passed the listed synthetic-data checks and 14-VALIDATION.md records an owner decision with no sensitive payload retained.</done>
  <resume-signal>Type `approved` with build/device notes, or describe the failed case.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test/release evidence → project records | Validation artifacts can accidentally retain private prompt or credential material. |
| native device → human verification | SecureStore and cancellation must be proven in the release environment, not only mocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-18 | Information disclosure | validation evidence | high | mitigate | Matrix records only command/status and sanitized synthetic observations; secret/prompt/raw-wire data is excluded. |
| T-14-19 | Tampering | native release behavior | high | mitigate | Blocking Pixel release-build UAT verifies the observable privacy/lifecycle contract after automated gates pass. |
</threat_model>

<verification>
Automated gates must pass before the blocking device checkpoint. Any egress-boundary, secret, stale-overwrite, or record-write failure blocks release.
</verification>

<success_criteria>
All AI-01…AI-04 automated evidence is recorded, and the owner has verified the release-build workflow on a physical Pixel using synthetic data.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-06-SUMMARY.md` when done.
</output>
