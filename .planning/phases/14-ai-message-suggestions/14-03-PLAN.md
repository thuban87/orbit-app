---
phase: 14-ai-message-suggestions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
  - src/ai/prompt-template.ts
  - src/ai/prompt-template.test.ts
  - src/ai/prompt-types.ts
autonomous: true
requirements: [AI-03, AI-04]
must_haves:
  truths:
    - "Only ranked allowed fuel, aggregate interaction facts, relationship metadata, and approved live fields can enter an AI prompt."
    - "The prompt resolver returns one immutable resolved-prompt object with truncation disclosure."
  artifacts:
    - "src/db/ai-context-read.ts"
    - "src/ai/prompt-template.ts"
  key_links:
    - "The prompt resolver consumes only the dedicated AI context projection."
    - "The exact immutable resolved-prompt object is later passed unchanged to the inspector, acknowledgement, and provider adapter."
---

<objective>
Build the privacy allowlist and deterministic prompt resolver that every AI request must use.

Purpose: make data minimization structural and make the inspected prompt identical to egress.
Output: AI-specific context read, immutable resolved prompt type, resolver limits, and privacy regression tests.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-RESEARCH.md
@src/db/fuel-read.ts
@src/db/impact-read.ts
@src/db/field-values-dao.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a narrow outbound AI context projection</name>
  <files>src/db/ai-context-read.ts, src/db/ai-context-read.test.ts, src/ai/prompt-types.ts</files>
  <behavior>
    - Ranked fuel retains order and age while excluding off-limits, unconfirmed-AI, and blank rows through the existing safe projection.
    - Interaction data supplies only gravity, intensity, quality, cadence/count aggregates, and newest channel with an explicit unspecified value.
    - Only live `share_with_ai=1` field values resolved through validated `col_name` identifiers appear with their display labels; null, blank, unflagged, quarantined, and dropped fields yield less data.
  </behavior>
  <action>Create a dedicated `PromptContext` reader rather than passing a broad contact/editor object. Reuse `getRankedFuel` for its SQL-level exclusion and retain fuel age/rank. Query interaction aggregates without selecting note or detail columns, and model an absent newest channel explicitly. Read opted-in custom values only by validated `col_name`, display label, and current live definition; never interpolate a label as SQL and never add fallback queries that widen the allowlist. Include contact status/gravity/intensity/quality only where the contract permits it.</action>
  <verify><automated>npx vitest run src/db/ai-context-read.test.ts src/db/fuel-read.test.ts src/db/field-values-dao.test.ts src/db/impact-read.test.ts</automated></verify>
  <done>The context read proves by SQLite fixtures that forbidden fuel, interaction prose, and unapproved/malformed field values cannot become prompt input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve a bounded immutable prompt once for every consumer</name>
  <files>src/ai/prompt-template.ts, src/ai/prompt-template.test.ts, src/ai/prompt-types.ts</files>
  <behavior>
    - Template resolution treats all contact-derived values as delimited data, not instructions, and unknown/dropped values become `None available`.
    - Limits preserve ranked order and return explicit category truncation metadata without retaining omitted text.
    - One frozen `ResolvedPrompt` value owns the exact string, inspection display data, acknowledgement identity, and adapter payload; consumers receive the same object reference rather than independently rebuilding text.
  </behavior>
  <action>Implement a pure resolver with static product instruction and delimited serialized `PromptContext` separation. Apply documented deterministic limits (template, total prompt, per-value, ranked fuel count, output length) and return a deeply immutable `ResolvedPrompt` object containing the final prompt plus safe truncation metadata. Make its API the sole construction path and expose its exact payload field for adapters; do not retain omitted values or make an alternate preview string. Add a strict-reference test that feeds the returned object to preview/acknowledgement/adapter consumer stubs and proves they observe the same instance and string.</action>
  <verify><automated>npx vitest run src/ai/prompt-template.test.ts src/db/ai-context-read.test.ts</automated></verify>
  <done>All allowed data is resolved deterministically into one inspectable immutable prompt object; untrusted/ineligible data cannot appear and truncation is visible without storing withheld text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local contact database → PromptContext | Private relationship data is selected for possible cloud egress. |
| PromptContext → provider payload | Untrusted contact-derived text becomes delimited prompt data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-08 | Information disclosure | ai-context-read | critical | mitigate | Dedicated SQL projection, validated dynamic identifiers, and fixtures covering fuel/field/interaction exclusions. |
| T-14-09 | Tampering | prompt-template | high | mitigate | Delimited data, known placeholders only, immutable resolver output, and injection-shaped input tests. |
| T-14-10 | Information disclosure | prompt preview/request split | high | mitigate | One immutable `ResolvedPrompt` reference drives every consumer; identity test prevents divergent reconstructed strings. |
</threat_model>

<verification>
Run the focused SQLite and resolver Vitest files, then `npx tsc --noEmit`.
</verification>

<success_criteria>
AI-03 and AI-04 have a structural outbound allowlist and one exact, bounded prompt that can be inspected before egress.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-03-SUMMARY.md` when done.
</output>
