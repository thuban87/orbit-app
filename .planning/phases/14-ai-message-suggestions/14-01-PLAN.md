---
phase: 14-ai-message-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.config.ts
  - src/db/migrations/004-ai-settings.ts
  - src/db/migrations/004-ai-settings.test.ts
  - src/db/migrations/index.ts
  - src/db/migrations/runner.test.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/services/ai-types.ts
  - src/services/ai-key-store.ts
  - src/services/ai-key-store.test.ts
autonomous: true
requirements: [AI-01]
must_haves:
  truths:
    - "AI is disabled by default and non-secret provider configuration survives an upgrade."
    - "Provider API keys are isolated in device secure storage and cannot be represented by app settings."
  artifacts:
    - "src/db/migrations/004-ai-settings.ts"
    - "src/services/ai-key-store.ts"
    - "src/services/ai-types.ts"
  key_links:
    - "The typed app-settings DAO persists only provider, model, custom endpoint, template, and acknowledgement values."
    - "The provider-scoped key repository is the only app-level key storage boundary."
---

<objective>
Establish the non-secret settings schema and the device-only credential boundary for the optional BYO-key feature.

Purpose: make every later provider and UI flow start from a disabled, typed, export-safe state.
Output: additive migration 004, extended settings DAO, provider types, and a SecureStore-backed key repository.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-RESEARCH.md
@src/db/migrations/002-app-settings.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the forward-only non-secret AI settings migration and DAO contract</name>
  <files>src/db/migrations/004-ai-settings.ts, src/db/migrations/004-ai-settings.test.ts, src/db/migrations/index.ts, src/db/migrations/runner.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <behavior>
    - A fresh and an upgraded database get provider `none`, an empty model/template/custom endpoint, and false acknowledgement defaults.
    - DAO validation accepts only known provider values, validates only non-secret configuration, and preserves the single settings row.
    - The migration and AppSettings type contain no API-key column or property.
  </behavior>
  <action>Create additive migration 004 without modifying migrations 001–003. Extend the typed single-row AppSettings read mapping, column allowlist, patch validation, and test fixtures together for provider, model, prompt template, validated Custom endpoint, and per-provider first-send acknowledgement. Preserve the existing forward-only runner and local timestamp/write conventions. Do not add a secret field, secret default, or credential serialization to SQLite; provider selection defaults to `none` per the locked configuration decision.</action>
  <verify><automated>npx vitest run src/db/migrations/004-ai-settings.test.ts src/db/migrations/runner.test.ts src/db/app-settings-dao.test.ts</automated></verify>
  <done>Migration 004 upgrades and seeds the sole settings row deterministically; the DAO exposes all and only non-secret AI settings with disabled-by-default validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define provider-neutral types and a provider-scoped SecureStore key repository</name>
  <files>package.json, app.config.ts, src/services/ai-types.ts, src/services/ai-key-store.ts, src/services/ai-key-store.test.ts</files>
  <behavior>
    - Saving a key for one provider cannot make it readable through another provider key name.
    - Read, write, and delete are mockable and tolerate a missing key as reconfiguration state.
    - The repository has no API that returns every key or serializes a key into settings/navigation data.
  </behavior>
  <action>Install the first-party Expo Secure Store module with `npx expo install expo-secure-store` and make only its required Expo config change. Define neutral provider/configuration/request types in `ai-types.ts`. Implement a narrow injected-dependency repository that maps one allowed cloud provider to one SecureStore item and exposes get/set/delete for that provider only, without `requireAuthentication`. Keep keys out of TypeScript settings shapes, SQLite, route parameters, logs, analytics, export data, and prompt-inspection state.</action>
  <verify><automated>npx vitest run src/services/ai-key-store.test.ts src/db/app-settings-dao.test.ts</automated></verify>
  <done>Each provider key is independently managed through SecureStore only; losing it after uninstall is a supported missing-key state, while app settings remain credential-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| settings database → app UI/export | Persistent non-secret configuration must remain separate from credentials. |
| key repository → native SecureStore | A provider key crosses only into device-backed secret storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Information disclosure | migration and settings DAO | high | mitigate | Migration/DAO tests prove no key column/property exists and only typed non-secret fields persist. |
| T-14-02 | Information disclosure | ai-key-store | high | mitigate | Provider-scoped SecureStore item names plus a narrow injected repository prevent bulk key serialization. |
| T-14-03 | Tampering | settings writes | medium | mitigate | Typed provider/config validation and existing single-row guarded write path reject malformed non-secret settings. |
</threat_model>

<verification>
Run the focused Vitest migration, DAO, and key-repository tests, then `npm run check:colors`.
</verification>

<success_criteria>
AI-01 starts disabled, with its chosen provider configuration persisted without any credential entering SQLite or an exportable type.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md` when done.
</output>
