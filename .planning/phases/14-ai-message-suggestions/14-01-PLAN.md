---
phase: 14-ai-message-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.config.ts
  - src/db/migrations/004-ai-settings.ts
  - src/db/migrations/004-ai-settings.test.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/services/ai-types.ts
  - src/services/ai-key-store.ts
  - src/services/ai-key-store.test.ts
  - src/ai/custom-endpoint.ts
  - src/ai/custom-endpoint.test.ts
autonomous: true
requirements: [AI-01]
must_haves:
  truths:
    - "AI is disabled by default (provider `none`) and non-secret provider configuration survives a v3→v4 upgrade."
    - "Provider API keys are isolated in device secure storage and cannot be represented by any app-settings type, column, export, or navigation param."
    - "Migration 004 is registered so the runner actually applies it: `TARGET_VERSION` is 4 and the migration list includes migration004."
    - "One shared Custom-endpoint URL validator rejects non-HTTPS, credentialed, and `.local` URLs plus the CANONICAL NON-PUBLIC IP-LITERAL SET (see the shared table below) — the SAME enumeration Plan 07's native predicate enforces for resolved hosts, anchored to the IANA special-purpose registries; the two layers must be identical (C4-H1). An EMPTY endpoint is the valid \"unconfigured\" value (C3-M5) — only non-empty endpoints are validated. The settings DAO validates a non-empty endpoint through it on write."
  artifacts:
    - "src/db/migrations/004-ai-settings.ts"
    - "src/services/ai-key-store.ts"
    - "src/services/ai-types.ts"
    - "src/ai/custom-endpoint.ts"
  key_links:
    - "database.ts imports migration004, adds it to the runMigrations array, and bumps TARGET_VERSION 3→4 (fixes H6)."
    - "app-settings-dao persists only provider, model, custom endpoint, custom model, template — never a key — and validates a non-empty endpoint via src/ai/custom-endpoint.ts (fixes H2 save-time layer). The four acknowledgement columns EXIST in the schema/read shape but are NOT writable through the generic settings patch — only `acknowledgeProvider` (Plan 05) may set them to 1 (C3-H3a); changing the Custom endpoint atomically resets `ai_ack_custom` to 0 (C3-H3b)."
    - "The provider-scoped SecureStore repository is the only app-level key storage boundary."
---

<objective>
Establish the non-secret settings schema, the registered migration, the device-only credential boundary, and the single shared Custom-endpoint URL validator for the optional BYO-key feature.

Purpose: make every later provider and UI flow start from a disabled, typed, export-safe state with the migration actually wired into the runner.
Output: additive migration 004 registered in database.ts, extended settings DAO (incl. per-provider acknowledgement), provider-neutral types, a SecureStore-backed key repository, and the shared HTTPS URL validator.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-RESEARCH.md
@.planning/phases/14-ai-message-suggestions/14-REVIEWS.md
@src/db/migrations/002-app-settings.ts
@src/db/migrations/003-orrery-settings.ts
@src/db/database.ts
@src/db/app-settings-dao.ts
@src/services/ai-types.ts
</context>

## Artifacts this phase produces (Plan 01)

Symbols/paths created or extended here (excluded from drift verification as newly-created):
- `migration004` — `src/db/migrations/004-ai-settings.ts` (adds AI non-secret columns to `app_settings`).
- `TARGET_VERSION = 4` and the extended migration array in `src/db/database.ts`.
- Extended `AppSettings` / `AppSettingsRow` / `COLUMN_OF` in `src/db/app-settings-dao.ts` with: `aiProvider`, `aiModel`, `aiCustomEndpoint`, `aiCustomModel`, `aiPromptTemplate` (all writable via the generic patch), and four READ-ONLY-via-generic-patch per-provider acknowledgement fields (`aiAckOpenai`, `aiAckAnthropic`, `aiAckGoogle`, `aiAckCustom`) — readable and defaulted here, but settable to 1 only through the dedicated `acknowledgeProvider` writer added in Plan 05 (C3-H3a).
- `src/services/ai-key-store.ts` — provider-scoped SecureStore repository (`getKey`/`setKey`/`deleteKey` for one provider at a time).
- Neutral types in `src/services/ai-types.ts` (provider id union, request/config shapes). Legacy `aiApiKey`/`aiApiKeys` remain until Plan 02 removes them (L1).
- `src/ai/custom-endpoint.ts` — `validateCustomEndpoint(raw): { ok: true; url: string } | { ok: false; reason: string }` (URL-literal layer; the connection-time DNS layer is Plan 07's native module).

## C4-H1 — the CANONICAL non-public rejection set (shared, authoritative; Plan 01 JS ⇔ Plan 07 Kotlin)

This is the single source of truth for "non-public / not-globally-reachable." Plan 01's
`validateCustomEndpoint` (IP-literal hosts) and Plan 07's native `isNonPublic` (resolved AND literal
hosts) MUST enforce EXACTLY this set — the two enumerations must be identical, and BOTH are proven by
**table-driven tests over every row** (JS in Plan 01, Kotlin/JVM in Plan 07). Authority: the IANA
IPv4/IPv6 special-purpose registries — reject unless the address is marked *globally reachable*.

| Family | Reject ranges |
|---|---|
| IPv4 | `0.0.0.0/8` (this-network/unspecified), `10.0.0.0/8`, `100.64.0.0/10` (CGNAT), `127.0.0.0/8` (loopback), `169.254.0.0/16` (link-local), `172.16.0.0/12`, `192.0.0.0/24` (IETF protocol), `192.0.2.0/24` + `198.51.100.0/24` + `203.0.113.0/24` (documentation), `192.168.0.0/16`, `198.18.0.0/15` (benchmarking), `224.0.0.0/4` (multicast), `240.0.0.0/4` (reserved), `255.255.255.255/32` (broadcast) |
| IPv6 | `::/128` (unspecified), `::1/128` (loopback), `64:ff9b::/96` (NAT64 — unwrap embedded IPv4 and re-check), `100::/64` (discard), `2001:db8::/32` (documentation), `fc00::/7` (ULA), `fe80::/10` (link-local), `ff00::/8` (multicast) |
| IPv4-mapped | any `::ffff:0:0/96` address — unwrap to its IPv4 form and re-check against the IPv4 rows (do NOT rely on `isSiteLocalAddress`) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install SecureStore, define neutral types + key repository, and the shared Custom-endpoint validator</name>
  <files>package.json, package-lock.json, app.config.ts, src/services/ai-types.ts, src/services/ai-key-store.ts, src/services/ai-key-store.test.ts, src/ai/custom-endpoint.ts, src/ai/custom-endpoint.test.ts</files>
  <read_first>
    - src/services/ai-types.ts (the current AiSettings shape — legacy key fields must be preserved this plan, L1)
    - src/services/AiService.ts (reads settings.aiApiKeys?.[p] ?? settings.aiApiKey at refreshProviders — DO NOT break its compile; L1)
    - app.config.ts (plugin registration idiom; only add an expo-secure-store entry if `npx expo install` instructs one)
    - modules/orbit-share-finish/index.ts (the repo's narrow injected-native-wrapper idiom)
  </read_first>
  <behavior>
    - Saving a key for one provider cannot make it readable through another provider's key name.
    - get/set/delete are mockable and tolerate a missing key as an ordinary reconfiguration state (Android SecureStore clears on uninstall).
    - The repository exposes no API returning all keys and no path that serializes a key into settings or navigation data.
    - validateCustomEndpoint treats an EMPTY string as the valid "unconfigured" value (returns ok without a URL; C3-M5). For a NON-empty input it accepts only `https:` URLs with a non-empty host and no username/password; it rejects `http:`, credentialed URLs, any `.local` mDNS host, AND every row of the CANONICAL non-public IP-literal set above (C4-H1) — returning a sanitized reason string, never echoing the raw URL into a UI error.
  </behavior>
  <action>Install the first-party Expo SecureStore module with `npx expo install expo-secure-store`; commit the resulting `package.json` AND `package-lock.json` changes (addresses M2) and make only the Expo config change `expo install` instructs, if any. Define neutral provider/config/request types in `ai-types.ts` and KEEP the existing legacy `aiApiKey`/`aiApiKeys` fields intact — their removal and the AiService rewrite belong to Plan 02 (addresses L1); this plan must not break `AiService.ts` compile. Implement a narrow injected-dependency repository in `ai-key-store.ts` that maps one allowed cloud provider to one namespaced SecureStore item (e.g. `orbit.ai.key.<provider>`) and exposes get/set/delete for that provider only, without `requireAuthentication`. Create `src/ai/custom-endpoint.ts` exporting a single `validateCustomEndpoint` used by BOTH the settings DAO (Task 2, save-time) and the Plan 02 Custom adapter (request-time) — one module, no duplicate save-time vs request-time validators (addresses H2). Validation returns ok for an EMPTY input (the "unconfigured" state — C3-M5) and validates only non-empty input: parse with `new URL()`, require protocol exactly `https:`, a non-empty host, no credentials, and reject `.local` hosts plus every row of the CANONICAL non-public rejection table above (C4-H1). Drive the rejection with a table constant iterated by a table-driven test, so the JS and Kotlin (Plan 07) layers enforce a provably identical set. Note in a top-of-file comment that this is the URL-LITERAL layer; a public hostname that RESOLVES to a private address is caught at connection time by Plan 07's native module (do not claim DNS resolution here); and that OkHttp does NOT consult the native `Dns` for a numeric-IP-literal URL, so an IP-literal Custom URL is guarded by BOTH this JS validator AND Plan 07's native pre-`Call` literal check — defense in depth, neither being the sole guard (C4-M2).</action>
  <acceptance_criteria>
    - `npx vitest run src/services/ai-key-store.test.ts src/ai/custom-endpoint.test.ts` passes.
    - `ai-key-store.test.ts` proves provider isolation (a key set for `openai` is unreadable as `anthropic`) and missing-key tolerance.
    - `custom-endpoint.test.ts` is TABLE-DRIVEN over the canonical rejection table (C4-H1): empty string accepted as "unconfigured" (C3-M5); valid public `https:` (`1.1.1.1`, `[2606:4700::1111]`, `api.example.com`) accepted; `http:`, `https://user:pass@host`, `https://printer.local`, and a representative literal from EVERY canonical row rejected — incl. `https://0.0.0.1`, `https://10.0.0.5`, `https://100.64.0.1` (CGNAT), `https://127.0.0.1`, `https://169.254.1.1`, `https://192.0.2.1` (doc), `https://198.18.0.1` (benchmark), `https://[::1]`, `https://[64:ff9b::a00:1]` (NAT64), `https://[2001:db8::1]` (doc), `https://[fc00::1]`/`https://[fd00::1]` (ULA), `https://[fe80::1]`, and `https://[::ffff:10.0.0.1]` (IPv4-mapped) — each with a sanitized reason not containing the raw input URL.
    - `npx tsc --noEmit` passes with `AiService.ts` unchanged (legacy key fields retained — L1).
    - `git status --porcelain package-lock.json` shows the lockfile is part of the change (M2).
  </acceptance_criteria>
  <verify><automated>npx vitest run src/services/ai-key-store.test.ts src/ai/custom-endpoint.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Each provider key is managed only through SecureStore; the shared HTTPS validator exists and is import-ready for the DAO and the Plan 02 adapter; app settings and AiService.ts still compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and register the forward-only AI settings migration and extend the settings DAO</name>
  <files>src/db/migrations/004-ai-settings.ts, src/db/migrations/004-ai-settings.test.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <read_first>
    - src/db/database.ts (lines ~24-38 imports + TARGET_VERSION; ~109-114 the runMigrations array — the ACTUAL registration point; there is NO src/db/migrations/index.ts — H6)
    - src/db/migrations/002-app-settings.ts and 003-orrery-settings.ts (additive ALTER TABLE ADD COLUMN forward-only idiom; every seed value bound)
    - src/db/migrations/runner.ts (per-step atomic transaction + user_version bump)
    - src/db/app-settings-dao.ts (AppSettings/AppSettingsRow/COLUMN_OF/validation/read query — extend all in lockstep)
    - src/ai/custom-endpoint.ts (Task 1 — the validator the DAO calls on endpoint write)
  </read_first>
  <behavior>
    - A fresh install and a v0→v4 / v3→v4 upgrade both land on: provider `none`, empty model/custom-endpoint/custom-model/template, and all four per-provider acknowledgement flags `0`.
    - The migration and the AppSettings type contain NO API-key column or property.
    - DAO validation accepts only known provider ids and validates a NON-empty Custom endpoint through `validateCustomEndpoint` before persisting it (an empty endpoint is accepted as "unconfigured" — C3-M5), and preserves the single settings row.
    - The four acknowledgement columns are NOT writable through the generic settings patch — `updateSettings({aiAckCustom:1})` (or any ack field) is rejected/ignored by the generic path; only Plan 05's `acknowledgeProvider` sets them to 1 (C3-H3a).
    - Whenever a settings write changes `ai_custom_endpoint` to a value different from the stored one, `ai_ack_custom` is reset to 0 in the SAME transaction, so a new Custom recipient requires a fresh acknowledgement (C3-H3b).
    - The runner applies migration 004 because it is in the array and `TARGET_VERSION` is 4.
  </behavior>
  <action>Create additive migration 004 as `ALTER TABLE app_settings ADD COLUMN` statements (never modifying migrations 001-003): `ai_provider TEXT NOT NULL DEFAULT 'none'`, `ai_model TEXT NOT NULL DEFAULT ''`, `ai_custom_endpoint TEXT NOT NULL DEFAULT ''`, `ai_custom_model TEXT NOT NULL DEFAULT ''`, `ai_prompt_template TEXT NOT NULL DEFAULT ''`, and four acknowledgement columns `ai_ack_openai`/`ai_ack_anthropic`/`ai_ack_google`/`ai_ack_custom` each `INTEGER NOT NULL DEFAULT 0`. No secret/key column. In `src/db/database.ts` (H6): import `migration004`, add it to the `runMigrations([...])` array, and bump `export const TARGET_VERSION` from 3 to 4 — this is the ONLY registration point (`src/db/migrations/index.ts` does not exist; do not create one). Extend `app-settings-dao.ts` `AppSettings`, `AppSettingsRow`, and the `SELECT` read query to READ all ten new fields (five config + four acks + existing). For WRITES: add ONLY the five config fields (`aiProvider`, `aiModel`, `aiCustomEndpoint`, `aiCustomModel`, `aiPromptTemplate`) to the generic `COLUMN_OF`/patch surface. The four `ai_ack_*` columns MUST be excluded from the generic `COLUMN_OF` write map so a generic `updateSettings` patch cannot set them (reject or drop any ack key that appears in a generic patch) — they are set only by Plan 05's `acknowledgeProvider` (C3-H3a). Validate provider against the known id set; when a patch includes a NON-empty `aiCustomEndpoint` run `validateCustomEndpoint` and throw on failure BEFORE the UPDATE opens (an empty value is accepted as "unconfigured" — C3-M5, H2 save-time). In the SAME write transaction, if the patch sets `aiCustomEndpoint` to a value different from the currently stored one, also set `ai_ack_custom = 0` (C3-H3b). Model the migration test on `003-orrery-settings.test.ts` (node:sqlite): apply on a fresh DB and on a v3 DB, assert defaults and that no credential column exists. Do not add a secret field, secret default, or credential serialization to SQLite; provider defaults to `none`.</action>
  <acceptance_criteria>
    - `npx vitest run src/db/migrations/004-ai-settings.test.ts src/db/app-settings-dao.test.ts` passes.
    - `004-ai-settings.test.ts` asserts: fresh + v3-upgrade both seed provider `none`, empty strings, and all four acks = 0; and asserts the `app_settings` column list contains no name matching a credential/secret (assert via the pragma column list in the test, not a plan-text grep).
    - `app-settings-dao.test.ts` asserts: an invalid Custom endpoint (`http://…`) throws on write and no row is changed; an EMPTY endpoint persists without error and clears a previously-set one (C3-M5); a known-provider patch persists and reloads; a generic patch attempting to set any `aiAck*` field does NOT change the ack column (C3-H3a); and writing a DIFFERENT `aiCustomEndpoint` resets `ai_ack_custom` from 1 to 0 in one transaction while writing the SAME endpoint leaves it untouched (C3-H3b).
    - `grep -n "TARGET_VERSION = 4" src/db/database.ts` returns a line, and `grep -c "migration004" src/db/database.ts` is at least 2 (import + array).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify><automated>npx vitest run src/db/migrations/004-ai-settings.test.ts src/db/app-settings-dao.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Migration 004 is authored AND registered (TARGET_VERSION 4, in the array); the DAO exposes all and only non-secret AI settings with disabled-by-default validation and save-time endpoint validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| settings database → app UI/export | Persistent non-secret configuration must remain separate from credentials. |
| key repository → native SecureStore | A provider key crosses only into device-backed secret storage. |
| Custom URL input → settings persistence | A user-supplied endpoint is validated (URL-literal layer) before it is stored. |
| npm install → build | A new dependency enters the supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Information disclosure | migration + settings DAO | high | mitigate | Migration/DAO tests prove no key column/property exists and only typed non-secret fields persist. |
| T-14-02 | Information disclosure | ai-key-store | high | mitigate | Provider-scoped SecureStore item names plus a narrow injected repository prevent bulk key serialization. |
| T-14-03 | Tampering | settings writes | medium | mitigate | Typed provider/config/ack validation + `validateCustomEndpoint` on the endpoint write + single-row guarded path reject malformed settings. |
| T-14-SC | Tampering | expo-secure-store install | medium | mitigate | First-party Expo SDK module (npmjs.com/package/expo-secure-store, Expo org) installed via `npx expo install`, which pins the SDK-57-compatible version; provenance documented, lockfile committed (M2). Owner has sanctioned the `npx expo install` idiom for this phase. |
</threat_model>

<verification>
Run the focused Vitest migration, DAO, key-repository, and endpoint-validator tests, then `npx tsc --noEmit` (L1/H6 compile gate) and `npm run check:colors`.
</verification>

<success_criteria>
AI-01 starts disabled with its chosen provider configuration persisted; migration 004 is registered and applied; the shared HTTPS validator and the SecureStore boundary exist; no credential enters SQLite or an exportable type.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md` when done.
</output>
