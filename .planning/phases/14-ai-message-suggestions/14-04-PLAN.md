---
phase: 14-ai-message-suggestions
plan: 04
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - src/components/FieldDefForm.tsx
  - src/components/field-def-form-logic.ts
  - src/components/field-def-form-logic.test.ts
  - src/db/field-defs-dao.ts
  - src/db/field-defs-dao.test.ts
  - src/screens/CustomFieldsScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/settings-ai-logic.ts
  - src/screens/settings-ai-logic.test.ts
autonomous: true
requirements: [AI-01, AI-03, AI-04]
must_haves:
  truths:
    - "A field is opt-in for AI sharing by default (share_with_ai=0) and BOTH the create AND the edit path can deliberately change and PERSIST that flag (H7)."
    - "Users can configure provider/model/template/public-Custom endpoint and enter a masked key that goes only to the key repository."
    - "Settings provides pure inspector/acknowledgement helpers that ACCEPT a ResolvedPrompt; the first-send gate/persistence itself lives in Compose (Plan 05), not here (H5)."
  artifacts:
    - "src/components/FieldDefForm.tsx"
    - "src/db/field-defs-dao.ts"
    - "src/screens/settings-ai-logic.ts"
  key_links:
    - "FieldDefDraft carries share_with_ai; updateFieldCuration (or a sibling writer) persists it; CustomFieldsScreen.handleEdit routes the change (fixes H7)."
    - "Settings persists non-secret config through app-settings-dao and stores keys only through ai-key-store; settings-ai-logic helpers never rebuild a prompt."
---

<objective>
Expose intentional per-field AI sharing (with a working edit-persistence path) and provider setup, without owning the first-send prompt gate.

Purpose: make every data-sharing and provider choice visible and reversible before a request; fix the edit-mode flag that currently cannot persist.
Output: end-to-end `share_with_ai` control (create + edit), Settings AI configuration, masked key entry, template editor, and pure inspector/ack helpers.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-REVIEWS.md
@.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-03-SUMMARY.md
@src/components/FieldDefForm.tsx
@src/db/field-defs-dao.ts
@src/screens/CustomFieldsScreen.tsx
@src/screens/SettingsScreen.tsx
</context>

## Artifacts this phase produces (Plan 04)

- `FieldDefDraft` extended with `share_with_ai: SqliteBool` (`src/components/FieldDefForm.tsx`) + a labelled toggle wired to it.
- `updateFieldCuration` extended (or a sibling `updateFieldShareWithAi`) in `src/db/field-defs-dao.ts` to persist `share_with_ai`.
- `CustomFieldsScreen.handleEdit` routes a `share_with_ai` change to the DAO.
- `src/screens/settings-ai-logic.ts` — pure helpers: config validation, model-list request orchestration with free-text fallback, and inspector/acknowledgement view-state builders that ACCEPT a `ResolvedPrompt` (no resolver call here).
- A Settings AI section in `src/screens/SettingsScreen.tsx` (token-only, masked key entry).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the default-off field-sharing control WITH a working edit-persistence path (H7)</name>
  <files>src/components/FieldDefForm.tsx, src/components/field-def-form-logic.ts, src/components/field-def-form-logic.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts, src/screens/CustomFieldsScreen.tsx</files>
  <read_first>
    - src/components/FieldDefForm.tsx (FieldDefDraft at ~53-59 lacks share_with_ai; create hardcodes share_with_ai:0 at ~162; edit onSubmit at ~168-174 omits it — H7)
    - src/db/field-defs-dao.ts (updateFieldCuration at ~117-131 writes only show_on_new + always_show — extend or add a sibling writer)
    - src/screens/CustomFieldsScreen.tsx (handleEdit at ~163-240 diffs the draft and routes each change; it must detect a share_with_ai change and call the DAO)
    - src/db/field-types.ts (NewFieldDef already carries share_with_ai; SqliteBool)
    - existing pure-logic sibling pattern (e.g. src/logic/compose-logic.ts, src/screens/settings-ai-logic.ts) — the codebase tests view-state logic as pure functions because Vitest is Node/render-free and NO RN component-test tooling (`react-test-renderer` / `@testing-library/react-native`) is installed (C2-M4)
  </read_first>
  <behavior>
    - New field definitions start with `share_with_ai` disabled (unchanged create default).
    - Editing a field can deliberately enable OR disable the existing metadata flag and the change PERSISTS across reload, without altering `col_name`, type, quarantine, or dynamic-column safety.
    - A silent drop of a toggled edit-mode flag is impossible (the currently-broken path).
  </behavior>
  <action>Add `share_with_ai: SqliteBool` to `FieldDefDraft`, seed it from `initial?.share_with_ai` on edit hydration, add a labelled themed `Switch` (default off on create) wired to it, and include it in BOTH the create `NewFieldDef` payload and the edit-draft `onSubmit`. **Extract the draft-hydration + payload-construction into pure functions (C2-M4):** create `src/components/field-def-form-logic.ts` exporting `hydrateFieldDefDraft(initial?)` (returns a `FieldDefDraft` with `share_with_ai` defaulting to 0 on create and seeded from `initial.share_with_ai` on edit) and `toNewFieldDefPayload(draft)` / the edit-submit payload builder — so the create-default-off and edit-hydration logic is unit-testable in Node without any renderer. `FieldDefForm.tsx` imports and uses these helpers (the JSX just binds the `Switch` to `draft.share_with_ai`). Extend `updateFieldCuration` in `field-defs-dao.ts` to also write `share_with_ai` (or add a sibling `updateFieldShareWithAi` writer with the same `inWriteTransaction` + `assertOneChange` idiom) — do not create a new dynamic-column path or touch `contact_custom_values`. In `CustomFieldsScreen.handleEdit`, add a `shareChanged` diff and route it through the writer alongside the existing curation write (respecting the all-or-nothing-on-cancel gate ordering). Use theme tokens only.</action>
  <acceptance_criteria>
    - `npx vitest run src/components/field-def-form-logic.test.ts src/db/field-defs-dao.test.ts src/db/field-values-dao.test.ts` passes.
    - A DAO test writes `share_with_ai=1` via the edit path and reads it back = 1, and toggling it back to 0 persists (the H7 regression).
    - A `field-def-form-logic.test.ts` test asserts `hydrateFieldDefDraft()` defaults the flag off on create and hydrates it from the stored value on edit, and that the edit payload includes the toggled flag (pure Node test — no renderer, C2-M4).
    - `grep -c "share_with_ai" src/db/field-defs-dao.ts` >= 1 and `grep -c "share_with_ai" src/components/field-def-form-logic.ts` covers the draft + edit payload.
    - `npm run check:colors` passes and `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify><automated>npx vitest run src/components/field-def-form-logic.test.ts src/db/field-defs-dao.test.ts src/db/field-values-dao.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <manual>The rendered `Switch` toggle itself (create default-off, edit reflects stored value) is confirmed in the Plan 06 Pixel UAT — the render layer has no Node test path (C2-M4).</manual>
  <done>Only a deliberate per-field opt-in marks live field data eligible for the PromptContext reader, and that opt-in persists through BOTH create and edit; dynamic-field safety invariants remain intact. The create-default/edit-hydration logic is proven by a pure Node test; the rendered toggle is a Pixel UAT check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings provider setup + template editor + pure inspector/ack helpers (no first-send gate here — H5)</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/settings-ai-logic.ts, src/screens/settings-ai-logic.test.ts</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (existing focus-reload + guarded persistence idioms; token-only sections)
    - src/db/app-settings-dao.ts (Plan 01 — the AI config fields + acknowledgement fields to read/persist)
    - src/services/ai-key-store.ts (Plan 01 — key entry destination) and src/ai/custom-endpoint.ts (validate the endpoint before save)
    - src/services/AiService.ts (Plan 02 — model-discovery contract + free-text fallback)
    - src/ai/prompt-types.ts (Plan 03 — ResolvedPrompt shape the helpers ACCEPT)
    - 14-REVIEWS.md H5 (the exact-resolved-prompt display + ack persistence belong to Compose/Plan 05, NOT Settings)
  </read_first>
  <behavior>
    - Users can set provider/model/template/public-Custom endpoint, attempt model discovery, and still supply manual model text after discovery failure; Custom is free-text model.
    - API key entry is masked and goes exclusively to `ai-key-store`; settings reload/persistence never render or serialize it.
    - Settings does NOT run the resolver or perform the first-send acknowledgement gate; it exposes pure helpers that ACCEPT a `ResolvedPrompt` to build inspector/ack view-state, reused by Compose (Plan 05).
    - The Custom endpoint is validated via `validateCustomEndpoint` before it is persisted.
  </behavior>
  <action>Add a token-only Settings AI section using the existing focus-reload/guarded-persistence idioms. Persist non-secret provider/model/template/endpoint via `app-settings-dao` (validating the Custom endpoint with `validateCustomEndpoint` before save) and route masked key entry through `ai-key-store`; render no stored key value. Reuse the Plan 02 model-discovery contract with a manual free-text fallback on any discovery failure. Implement PURE helpers in `settings-ai-logic.ts` that accept a `ResolvedPrompt` and return inspector display view-state and per-provider acknowledgement view-state — these MUST NOT call a resolver or build an alternate prompt; they are consumed by Compose in Plan 05. Include a template editor (edit the stored `ai_prompt_template`). Explain in-copy that Orbit does not control a Custom provider's retention/data handling. Do NOT implement the first-send blocking gate or its `app_settings` write here (H5 — that is Plan 05, where the exact contact-specific ResolvedPrompt exists).</action>
  <acceptance_criteria>
    - `npx vitest run src/screens/settings-ai-logic.test.ts src/services/ai-key-store.test.ts src/services/AiService.test.ts` passes.
    - A helper test asserts the inspector/ack builders return the SAME `ResolvedPrompt` string they were given and never construct a new one (no resolver import in settings-ai-logic.ts — grep confirms).
    - A test asserts an invalid Custom endpoint is rejected before persistence, and that a key entered in Settings is not present in any persisted `AppSettings`/serialized value.
    - `npm run check:colors` passes and `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify><automated>npx vitest run src/screens/settings-ai-logic.test.ts src/services/ai-key-store.test.ts src/services/AiService.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Settings offers the four provider paths, secure key entry, dynamic/manual models, and a template editor, and exposes pure ResolvedPrompt-consuming helpers — while the exact-prompt first-send gate is left to Compose (H5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user configuration UI → persistent settings | Intentional selections are persisted as non-secret state. |
| user key entry → SecureStore | Sensitive credential entry must bypass application persistence/rendering. |
| field editor → custom-field defs | An AI-sharing opt-in must persist without touching value columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-11 | Information disclosure | field editor | high | mitigate | Explicit default-off control + edit-persistence writer + DAO test preserve an opt-in-only custom-field boundary (H7). |
| T-14-12 | Information disclosure | Settings key input | high | mitigate | Masked input writes directly to ai-key-store; settings shapes/UI never read or serialize a key. |
| T-14-13 | Spoofing | provider disclosure | medium | mitigate | Pure helpers surface the exact immutable prompt (from Compose) and name Custom privacy limitations accurately; Settings never fabricates a prompt. |
</threat_model>

<verification>
Run the focused field and settings Vitest suites plus `npm run check:colors` and `npx tsc --noEmit`.
</verification>

<success_criteria>
AI-01, AI-03, and AI-04 are user-controlled: no field shares by default (and the opt-in persists on edit), keys remain secret, and Settings prepares — but does not own — the exact-prompt disclosure.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-04-SUMMARY.md` when done.
</output>
