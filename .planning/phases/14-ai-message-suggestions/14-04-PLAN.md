---
phase: 14-ai-message-suggestions
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-03"]
files_modified:
  - src/components/FieldDefForm.tsx
  - src/components/field-def-form.test.tsx
  - src/db/field-defs-dao.test.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/settings-ai-logic.ts
  - src/screens/settings-ai-logic.test.ts
autonomous: true
requirements: [AI-01, AI-03, AI-04]
must_haves:
  truths:
    - "A field is opt-in for AI sharing by default and users can change that explicit setting safely."
    - "Users can configure a provider and inspect the exact resolved prompt before first use, without rendering a key."
  artifacts:
    - "src/components/FieldDefForm.tsx"
    - "src/screens/SettingsScreen.tsx"
    - "src/screens/settings-ai-logic.ts"
  key_links:
    - "Settings persists non-secret configuration through app-settings-dao and stores keys only through ai-key-store."
    - "The settings inspection/acknowledgement flow receives a ResolvedPrompt rather than rebuilding it."
---

<objective>
Expose intentional field sharing and provider setup without weakening the credential or prompt boundaries.

Purpose: make every data-sharing and provider choice visible and reversible before a request.
Output: field opt-in control, Settings AI configuration, key entry/masking, and prompt acknowledgement UI logic.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-03-SUMMARY.md
@src/components/FieldDefForm.tsx
@src/screens/SettingsScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the explicit default-off field sharing control</name>
  <files>src/components/FieldDefForm.tsx, src/components/field-def-form.test.tsx, src/db/field-defs-dao.test.ts</files>
  <behavior>
    - New field definitions start with `share_with_ai` disabled.
    - Editing a field can deliberately enable or disable the existing metadata flag without changing its `col_name`, type, quarantine, or dynamic-column safety behavior.
  </behavior>
  <action>Thread the existing schema-backed `share_with_ai` metadata field through FieldDefForm draft initialization, edit hydration, labelled control, validation, and persistence. Retain explicit default false and existing field-definition metadata rules; the UI must not create a new dynamic-column path or alter quarantine semantics. Use theme tokens and add component/DAO regressions for create/edit/default behavior.</action>
  <verify><automated>npx vitest run src/components/field-def-form.test.tsx src/db/field-defs-dao.test.ts src/db/field-values-dao.test.ts</automated></verify>
  <done>Only a deliberate per-field opt-in marks live field data eligible for the PromptContext reader, while all existing dynamic-field safety invariants remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Settings provider setup, acknowledgement, and prompt inspection</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/settings-ai-logic.ts, src/screens/settings-ai-logic.test.ts</files>
  <behavior>
    - Users can set provider/model/template/public Custom endpoint, attempt model discovery, and still supply manual model text after discovery failure.
    - API key entry is masked and goes exclusively to the key repository; settings reload/persistence never render or serialize it.
    - First use of each provider requires acknowledgement after showing the exact `ResolvedPrompt`; later requests remain inspectable with that same object/string.
  </behavior>
  <action>Add a token-only Settings section using the existing focus reload and guarded persistence idioms. Keep non-secret provider/model/template/endpoint/acknowledgement settings in the DAO and route key entry through `ai-key-store`; render no stored key value. Reuse the provider validator/model-discovery contract and make manual model text available on any discovery failure. Implement pure UI-state helpers that accept `ResolvedPrompt` as input for inspector and per-provider acknowledgement—never call a second resolver or construct an alternate prompt. Explain that Orbit does not control a Custom provider's retention/data handling.</action>
  <verify><automated>npx vitest run src/screens/settings-ai-logic.test.ts src/services/ai-key-store.test.ts src/services/AiService.test.ts</automated></verify>
  <done>Settings offers the four selected provider paths, secure key entry, dynamic/manual models, and first-send disclosure while retaining every-time access to the exact prompt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user configuration UI → persistent settings | Intentional selections are persisted as non-secret state. |
| user key entry → SecureStore | Sensitive credential entry must bypass application persistence/rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-11 | Information disclosure | field editor | high | mitigate | Explicit default-off control and DAO test preserve an opt-in-only custom-field boundary. |
| T-14-12 | Information disclosure | Settings key input | high | mitigate | Masked input writes directly to ai-key-store; settings shapes/UI never read or serialize a key. |
| T-14-13 | Spoofing | provider disclosure | medium | mitigate | Per-provider acknowledgement receives the exact immutable prompt and names Custom privacy limitations accurately. |
</threat_model>

<verification>
Run the focused field and settings Vitest suites plus `npm run check:colors`.
</verification>

<success_criteria>
AI-01, AI-03, and AI-04 are user-controlled: no field shares by default, keys remain secret, and the exact outbound prompt is acknowledged and inspectable.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-04-SUMMARY.md` when done.
</output>
