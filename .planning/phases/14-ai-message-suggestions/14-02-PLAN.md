---
phase: 14-ai-message-suggestions
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/services/AiService.ts
  - src/services/AiService.test.ts
  - src/services/ai-types.ts
  - src/services/ai-key-store.ts
  - src/services/ai-service-guards.test.ts
autonomous: true
requirements: [AI-01]
must_haves:
  truths:
    - "OpenAI, Anthropic, Gemini, and public HTTPS Custom endpoints can produce one validated draft through direct fetch."
    - "Model discovery failure still leaves a user able to enter a model manually."
    - "Provider failures, redirects, aborts, and malformed responses produce sanitized errors without retrying a possibly billed request."
  artifacts:
    - "src/services/AiService.ts"
    - "src/services/ai-service-guards.test.ts"
  key_links:
    - "Every adapter obtains only the selected provider key from ai-key-store immediately before network use."
    - "A single typed request carries the resolved prompt and AbortSignal from Compose to an adapter."
---

<objective>
Replace the dormant AI plumbing with bounded, cancellable, typed direct-fetch adapters.

Purpose: enforce the egress and response boundary before a UI can issue a request.
Output: provider adapters, Custom endpoint validator, normalized errors, and mocked-fetch coverage.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-RESEARCH.md
@.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md
@src/services/AiService.ts
@src/services/AiService.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement typed provider generation and advisory model discovery</name>
  <files>src/services/AiService.ts, src/services/AiService.test.ts, src/services/ai-types.ts, src/services/ai-key-store.ts</files>
  <behavior>
    - OpenAI, Anthropic, Gemini, and Custom requests use their correct headers/body shape and validate a non-empty bounded text result from unknown JSON.
    - Official model discovery is dynamically fetched (Gemini retains generation-capable models) and any discovery error retains manual free-text entry; Custom is always free text.
    - Non-success HTTP status, malformed JSON, empty/oversize draft, timeout, and abort return stable sanitized error codes without provider body/header leakage.
  </behavior>
  <action>Replace the legacy Markdown prompt assembler and all prompt/error logging in `AiService.ts` with a narrow provider adapter contract. Each adapter must obtain exactly the selected provider key from `ai-key-store`, explicitly check `response.ok`, parse `unknown`, validate the short suggestion limit, and return a neutral result/error contract. Implement documented model-list calls for the three official providers with pagination/filtering where applicable and an explicit free-text fallback. Use a fresh AbortController and cleared manual timeout per generation request; do not add streaming, SDKs, framework dependencies, response-body diagnostics, or automatic retry after transport/timeout uncertainty.</action>
  <verify><automated>npx vitest run src/services/AiService.test.ts src/services/ai-service-guards.test.ts</automated></verify>
  <done>All four adapters have validated unary generation and safe model-selection behavior, and no adapter logs prompt text, secrets, headers, bodies, endpoint details, or raw provider failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block unsafe Custom endpoint egress and redirect escapes</name>
  <files>src/services/AiService.ts, src/services/ai-service-guards.test.ts</files>
  <behavior>
    - Malformed, cleartext, credential-bearing, localhost, loopback, private, link-local, and LAN-resolving Custom URLs fail before a request.
    - Custom generation rejects every redirect rather than following an approved URL to another host.
    - Safe public HTTPS URLs can be normalized and used without exposing their full value in UI errors or logs.
  </behavior>
  <action>Centralize Custom URL validation and invoke it both while saving settings and immediately before requesting. Require an HTTPS URL with a host and no username/password; resolve/reject loopback, RFC1918/private, link-local, and LAN addresses so a hostname cannot bypass the policy. Configure fetch to reject redirects and map redirect failures to a sanitized user-facing error. Test the validation and redirect policy at the service boundary without making real network calls.</action>
  <verify><automated>npx vitest run src/services/ai-service-guards.test.ts src/services/AiService.test.ts</automated></verify>
  <done>Custom provider requests can only target a validated public HTTPS endpoint and cannot be redirected into a different or private destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| device → selected provider | Prompt and BYO key cross an outbound network boundary. |
| Custom URL input → network client | User-supplied endpoint controls a potential egress destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-04 | Information disclosure | Custom endpoint | critical | mitigate | Strict public-HTTPS validation, DNS/address checks, no credentials, and redirect rejection before egress. |
| T-14-05 | Information disclosure | error handling/logging | high | mitigate | Normalize status/parse/abort errors; tests assert request body, keys, endpoints, and raw response content never surface. |
| T-14-06 | Denial of service | provider request | medium | mitigate | A caller-controlled AbortSignal and one manually cleared timeout bound each unary request. |
| T-14-07 | Repudiation | provider billing | medium | accept | Uncertain requests are never auto-retried; a later UI presents deliberate Retry. |
</threat_model>

<verification>
Run both mocked-fetch suites and `npx tsc --noEmit` after the service contract compiles.
</verification>

<success_criteria>
AI-01 supports exactly the four selected BYO-key provider paths while making unsafe endpoint, response, and error paths non-disclosing.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-02-SUMMARY.md` when done.
</output>
