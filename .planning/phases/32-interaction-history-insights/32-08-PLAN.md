---
phase: 32-interaction-history-insights
plan: 08
type: execute
wave: 4
depends_on: ["32-04", "32-05", "32-06", "32-07"]
files_modified:
  - src/components/profile/ProfileModuleHost.tsx
  - src/components/history/HistorySection.tsx
  - src/components/history/history-section-logic.ts
  - src/components/history/history-section-logic.test.ts
  - src/navigation/types.ts
  - src/navigation/tabs/OrreryStack.tsx
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [HIST-01, HIST-15, HIST-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Profile History section presents three children — Activity Heatmap, Intensity, and History Browser — in place of the old vertical-timeline stub, wired behind the existing ProfileModuleHost interaction-history renderer seam without touching layout persistence (HIST-01)."
    - "Tapping a heatmap cell opens the context card, whose 'See details' opens the shared Detail Sheet; the browser drawer's 'See details' opens the same sheet; neither the heatmap nor the browser opens the sheet on any path except an explicit action (HIST-01, HIST-07, HIST-09)."
    - "The empty-history predicate is true ONLY when the contact has zero interactions EVER; a lifecycle-only contact (hasLifecycleRecords true, zero interactions) still shows the heatmap/browser zero state, NOT the empty section (HIST-01, uses Plan 03's hasLifecycleRecords signal)."
    - "A contact with no interactions at all shows the empty History state: heading 'No history yet', body 'Log an interaction to start seeing activity here.', action 'Log interaction' (HIST-01)."
    - "An empty historical date/cell 'Log interaction' action navigates via the TYPED LogContact { contactId, prefillDate } route contract with the contact preselected and the date prefilled — never Quick Log. Phase 32 establishes ONLY the typed route contract + an intentionally-unavailable placeholder path; Phase 34 owns the real detailed-log FORM that consumes prefillDate (HIST-15, Phase-34 hand-off)."
    - "The LogContact route RESOLVES from every Profile-hosting stack — it is registered in DashboardStack today and this plan ADDS it to OrreryStack and SettingsStack (Profile is hosted in all three; React Navigation throws on an unregistered route name) — a hard acceptance criterion, not a backstop (HIST-15)."
    - "The History section is fully usable without colour, wheel gestures, or marker iconography, and Reduced Motion simplifies the wheel without removing navigation — the assembled section honors the Accessibility Contract end-to-end (HIST-18)."
    - "The heatmap, intensity, and browser all read the SAME selected window; changing the lens/period re-renders all of them consistently (HIST-06)."
  artifacts:
    - src/components/history/HistorySection.tsx
    - src/components/history/history-section-logic.ts
  key_links:
    - "HistorySection is mounted by ProfileModuleHost.renderHistory(), replacing the stub (channel · occurredAt text + 'View all history'); it owns the lens/window state and wires the child components' callbacks (open sheet, log route, onOpenKnowledgeChange to the profile's existing knowledge nav)."
    - "LogContact route params gain prefillDate; the placeholder screen accepts it today and Phase 34 fills the screen (Phase 32 owns the route/context CONTRACT only, HIST-15)."
  prohibitions:
    - "The empty-date/empty-cell logging route must NOT use Quick Log — it navigates the typed LogContact { contactId, prefillDate } contract (HIST-15)."
    - "This plan must NOT claim Phase 32 delivers 'canonical detailed logging' — the LogContact screen is a placeholder until Phase 34; Phase 32 delivers only the typed route/context contract (Phase-34 hand-off)."
    - "This plan must NOT alter profile layout persistence — it upgrades only the renderer behind the interaction-history seam (HIST-01)."
    - "Assembly must NOT reintroduce colour-only distinctions or a per-frame-setState animation path — it composes the Plan 05/06/07 components as-is."
---

<objective>
Assemble the three History children (Heatmap, Intensity, Rolodex Browser) into the Profile History section behind the existing `ProfileModuleHost` renderer seam — replacing the vertical-timeline stub — and wire the cross-surface behaviors: cell/drawer -> context card/drawer -> shared Detail Sheet, the empty-history state, and the empty-date 'Log interaction' action that navigates the TYPED LogContact { contactId, prefillDate } route contract (contact preselected + date prefilled, never Quick Log). Phase 32 establishes the route/context CONTRACT and registers it in every Profile-hosting stack; Phase 34 owns the real detailed-log form. This is the phase's user-facing payoff: HIST-01, HIST-15, HIST-18.

Purpose: Everything before this built the pieces; this plan makes a real user able to open a contact and explore their history. Keeping the assembly to the renderer seam preserves the profile's layout persistence (PROF contract). Owning the LogContact route/context contract now — and registering it across Dashboard/Orrery/Settings — lets Phase 34 fill the screen without re-deciding the params or the reachability.
Output: HistorySection (+ logic/test), ProfileModuleHost seam replacement, LogContact prefillDate param + registration in all Profile-hosting stacks, empty-state/log routing, and the knowledge-change routing callback wired to the profile's existing knowledge nav.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HistorySection state/orchestration logic + LogContact route contract</name>
  <files>src/components/history/history-section-logic.ts, src/components/history/history-section-logic.test.ts, src/navigation/types.ts</files>
  <read_first>
    - src/components/profile/ProfileModuleHost.tsx (:110-135 the interaction-history summary case; :387-410 renderHistory() the stub to replace; :479-488 where renderHistory is mounted)
    - src/services/history/window.ts, buckets.ts, intensity-window.ts (Plan 03 — the window/aggregation the section drives)
    - src/db/app-settings-dao.ts (Plan 05 — history_lens/history_cycle_count read/write for the persisted lens)
    - src/navigation/types.ts (:39 LogContact: { contactId?: number } | undefined — extend with prefillDate; :13 ProfileRouteParams)
    - src/components/UniversalFab.tsx (:60-61 the LogContact navigate intent — the existing detailed-log entry, contrasted with quick-log)
  </read_first>
  <behavior>
    - selecting a lens updates the window and persists the lens; the same window feeds heatmap buckets, intensity, and the browser.
    - the empty-state predicate is true only when the contact has zero interactions ever; a lifecycle-only contact (Plan 03's hasLifecycleRecords true, zero interactions) is NOT empty — it shows the zero-count heatmap/browser, not the 'No history yet' section.
    - buildLogRoute(contactId, date) yields a typed LogContact navigation payload with contactId preselected and prefillDate set (never a quick-log payload).
  </behavior>
  <action>Create history-section-logic.ts: pure orchestration — resolve the active window from the persisted lens/preset, decide the empty-history predicate (zero interactions ever, distinguishing lifecycle-only via Plan 03's hasLifecycleRecords signal), and build the LogContact navigation payload (contactId + prefillDate, explicitly NOT quick-log). Extend the LogContact route param in navigation/types.ts to `{ contactId?: number; prefillDate?: string } | undefined` in DashboardStackParamList AND add the LogContact route to OrreryStackParamList and SettingsStackParamList (it is absent from those today). Write tests first (RED): lens->window resolution, empty predicate (zero-interaction vs lifecycle-only vs populated), and buildLogRoute producing the preselect+prefill payload.</action>
  <verify>
    <automated>npx vitest run src/components/history/history-section-logic.test.ts && grep -n "prefillDate" src/navigation/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover lens->window, the empty predicate for all three contact states (zero-interaction empty vs lifecycle-only NOT empty vs populated), and buildLogRoute (preselect + prefill, no quick-log).
    - Source assertion: LogContact params include prefillDate; LogContact appears in the Dashboard, Orrery, AND Settings param lists.
  </acceptance_criteria>
  <done>The section's orchestration is pure and tested (lifecycle-only ≠ empty), and the typed LogContact route carries the preselect+prefill contract across all Profile-hosting param lists.</done>
</task>

<task type="auto">
  <name>Task 2: Mount HistorySection in the renderer seam + wire sheet/log/empty behaviors</name>
  <files>src/components/history/HistorySection.tsx, src/components/profile/ProfileModuleHost.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/components/history/ActivityHeatmap.tsx, HeatmapContextCard.tsx, IntensityChart.tsx (Plan 05 — callback props)
    - src/components/history/RolodexBrowser.tsx (Plan 06 — drawer callbacks)
    - src/components/history/DateDetailSheet.tsx (Plan 07 — the sheet these open; its onOpenKnowledgeChange callback)
    - src/components/profile/ProfileModuleHost.tsx (:387-410 renderHistory stub to replace; keep the interaction-history summary case + layout persistence intact)
    - src/screens/ContactProfileScreen.tsx (:393-419 the ProfileModuleHost callbacks incl. onKnowledgeAction/onOpenValueHistory -> ThingsToRemember; the existing knowledge nav the onOpenKnowledgeChange callback reuses)
    - src/navigation/tabs/DashboardStack.tsx (:37 LogContactPlaceholderScreen), src/navigation/tabs/OrreryStack.tsx, src/navigation/tabs/SettingsStack.tsx (LogContact absent from the latter two — register it)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts; Copywriting Contract -> empty section; Accessibility Contract)
  </read_first>
  <action>Create HistorySection.tsx composing ActivityHeatmap + IntensityChart + RolodexBrowser over one shared window (from Task 1), wiring: heatmap cell tap -> HeatmapContextCard -> ('See details' opens DateDetailSheet | 'Log interaction' navigates the typed LogContact route via buildLogRoute); browser drawer -> ('See details' opens the same DateDetailSheet | 'Log interaction' via buildLogRoute); Detail Sheet row -> InteractionDetail (Edit -> EditInteraction route [standalone path — group scope prompt is dormant per Plan 07]; Delete -> hard-delete confirm) and the knowledge-change row's onOpenKnowledgeChange(fieldKey) -> the profile's existing knowledge navigation. Render the empty-history state ('No history yet' / body / 'Log interaction') ONLY when the zero-interactions-ever predicate holds (lifecycle-only shows the zero-count surfaces, not the empty section). Replace ProfileModuleHost.renderHistory() to mount HistorySection instead of the stub, leaving the interaction-history summary case and all layout persistence untouched; wire the knowledge-change callback through ContactProfileScreen to its existing knowledge nav (do not introduce a new cross-stack gap). Register the LogContact placeholder route in OrreryStack and SettingsStack (it exists only in DashboardStack today) so 'Log interaction' resolves from an Orrery- or Settings-originated Profile — a hard requirement, not a backstop (addresses review HIGH). Reframe: Phase 32 delivers the typed route CONTRACT into the placeholder; Phase 34 fills the real detailed-log form (record the Phase-34 hand-off in the SUMMARY).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -n "HistorySection\|renderHistory" src/components/profile/ProfileModuleHost.tsx && grep -rln "LogContact" src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx src/navigation/tabs/SettingsStack.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ProfileModuleHost.renderHistory mounts HistorySection; the old 'View all history'/channel·occurredAt stub is gone; the interaction-history summary case is unchanged.
    - Source assertion: LogContact is registered as a Stack.Screen in Dashboard, Orrery, AND Settings stacks (the grep lists all three); the knowledge-change routing goes through the profile's existing knowledge nav callback (no new hardcoded target).
    - Source assertion: no history surface opens DateDetailSheet except via an explicit context-card/drawer action.
    - Behavior (device UAT, phase gate on the Pixel): a populated contact shows heatmap+intensity+browser; cell->card->sheet and drawer->sheet work; an empty contact shows 'No history yet'; a lifecycle-only contact shows the zero-count surfaces (not the empty section); an empty date's 'Log interaction' resolves the typed LogContact route (contact preselected + date prefilled, not quick log) — including from a Settings-originated Profile; the section is navigable with reduced motion.
  </acceptance_criteria>
  <done>The Profile History section shows the three children behind the renderer seam, all cross-surface actions are wired, LogContact resolves from every Profile-hosting stack, the empty state distinguishes lifecycle-only, and detailed-logging routing carries the typed preselect+prefill contract for Phase 34 to fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile renderer seam -> layout persistence | The seam upgrade must not disturb the persisted profile layout (PROF contract). |
| empty-date action -> logging flow | Routing must reach the typed LogContact route (not Quick Log) with correct context, and resolve from every Profile-hosting stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-23 | Tampering | renderer seam replacement | medium | mitigate | Replace only renderHistory; leave the summary case + layout persistence untouched; source-grep gate. |
| T-32-24 | Tampering | log routing correctness | medium | mitigate | buildLogRoute unit-tested to emit preselect+prefill and never a quick-log payload; LogContact param typed. |
| T-32-25 | Accessibility | assembled section | high | mitigate | Composes a11y-complete components; phase-gate Pixel UAT of colour-free/gesture-free/reduced-motion paths (HIST-18). |
</threat_model>

<verification>
- history-section-logic tests green (window, empty predicate, log route).
- tsc + check:colors green; renderHistory mounts HistorySection; summary case intact.
- Full-section on-device UAT (populated/empty, card->sheet, drawer->sheet, empty-date log, reduced motion) at the Pixel phase gate.
- Full suite `npm test` green as the phase merge gate.
</verification>

<success_criteria>
Opening a contact shows an explorable, accessible History section (Heatmap + Intensity + Rolodex) that replaces the timeline, opens details only on explicit actions, shows a real empty state (lifecycle-only ≠ empty), and routes empty-date logging through the typed LogContact { contactId, prefillDate } contract — reachable from every Profile-hosting stack — for Phase 34 to fill.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/components/history/HistorySection.tsx`, `history-section-logic.ts` (+ test)
- LogContact route param addition: `prefillDate`; LogContact Stack.Screen registration added to OrreryStack and SettingsStack
- ProfileModuleHost.renderHistory now mounts HistorySection (renderer-seam upgrade); knowledge-change routing wired through ContactProfileScreen's existing knowledge nav
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] empty-date logging claims 'canonical detailed logging' but routes into a placeholder** — reframed: Phase 32 delivers ONLY the typed LogContact { contactId, prefillDate } route CONTRACT into the placeholder; Phase 34 owns the real form (Phase-34 hand-off recorded).
- **[HIGH] LogContact not reachable from Settings/Orrery-originated Profile** — LogContact is now registered in Orrery and Settings stacks (it existed only in Dashboard) as a hard acceptance criterion, not a backstop.
- **[HIGH, codex] profile navigation callback is origin-stack-local / routes history to ThingsToRemember** — the renderHistory seam mounts HistorySection and the knowledge-change routing is wired through ContactProfileScreen's existing knowledge nav callback (no new cross-stack gap introduced).
- **[MEDIUM, codex] lifecycle-only contacts should show history, not empty** — the empty predicate uses Plan 03's hasLifecycleRecords signal so lifecycle-only shows the zero-count surfaces, not 'No history yet'.
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-08-SUMMARY.md` when done.
</output>
