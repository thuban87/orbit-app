---
phase: 32-interaction-history-insights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/025-interaction-history-schema.ts
  - src/db/migrations/025-interaction-history-schema.test.ts
  - src/db/interaction-vocabulary.ts
  - src/db/interaction-vocabulary.test.ts
  - src/db/database.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/migrations/runner.test.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
  - src/db/digest-read.ts
  - src/db/digest-read.test.ts
  - src/ai/prompt-types.ts
  - src/db/recency-dao.ts
  - src/db/recency-dao.test.ts
  - src/db/interaction-assist-dao.ts
  - src/db/interaction-assist-dao.test.ts
  - src/components/TouchpointRefineForm.tsx
  - src/components/touchpoint-refine-logic.ts
  - src/components/touchpoint-refine-logic.test.ts
  - src/backup/backup-schema.ts
autonomous: false
requirements: [HIST-11, HIST-14]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After migration 025 applies, every stored interactions.quality value of 'good'/'fine'/'hard' reads back as 'Positive'/'Neutral'/'Negative'; NULL quality stays NULL; any other legacy value passes through unchanged (D-06)."
    - "After migration 025 applies, interactions.channel 'text' and 'email' read back as 'Message', 'call' as 'Call', 'in-person' as 'In Person'; 'other' and 'unspecified' pass through unchanged (D-06)."
    - "A device jumping from schema v1 directly to v25 ends with the migrated vocabulary and the new columns present — the migration assumes no particular starting state (forward-only, order-independent)."
    - "Every migrated interaction row and every newly inserted interaction row has allow_ai = 0 by default; allow_ai only accepts 0 or 1 (CHECK enforced) (D-04)."
    - "interactions.duration is a nullable INTEGER (seconds); rows without a duration read back NULL, never 0 (HIST-14)."
    - "app_settings gains history_lens (default 'cycles') and history_cycle_count (default 10) columns after 025 (D-11)."
    - "readInteractionAggregates and the digest gentle-line tally return correct counts over rows storing the migrated Tone vocabulary — no count silently zeroes (D-06 trip-wire, Pitfall 1)."
    - "markAssistLogged routes the assist channel through the shared remapLegacyChannel at log time so 'call'->'Call', 'text'->'Message', AND 'email'->'Message' — the 014 transport CHECK permits 'email', so an email assist must NOT write a retired 'email' value into a v25 interactions row (review cycle-2 HIGH, T-32-03); interaction_assists.channel keeps its call|text|email transport CHECK (not rebuilt) and a source grep proves the 014 transport CHECK is untouched (D-06)."
    - "The legacy->new vocabulary map lives ONCE in src/db/interaction-vocabulary.ts (remapLegacyQuality/remapLegacyChannel); migration 025's UPDATE CASE literals are FROZEN in the migration file (never generated from the mutable runtime helper — review cycle-2 non-HIGH) and a test pins those frozen CASE outputs equal to the shared helper outputs; Plan 02's restore-remap and markAssistLogged consume the same helpers — no divergent second mapping exists (D-06 trip-wire)."
    - "src/types.ts LastInteractionType is audited: it has no code path populating Contact.lastInteraction from interactions.channel, so it is documented as an unused legacy type intentionally excluded from the migration (not a persisted-channel consumer) (D-06 consumer audit)."
    - "deleteTouchpoint's tombstone-in-txn + recompute + failed-delete-preserves-row behavior is node-tested in recency-dao.test.ts (the DAO-level delete regression Plan 07's UI consumes) (D-05, HIST-13)."
    - "TouchpointRefineForm presents Tone (Positive/Neutral/Negative) and Message/Call/In Person channel labels, an optional Duration control (5m/15m/30m/1h/2h/Custom or none), and an Allow AI toggle defaulting OFF (HIST-11, HIST-14)."
    - "backup-schema PORTABLE_SETTINGS_KEYS gains 'historyLens' and 'historyCycleCount' as declare-only (restore-accept) entries; getPortableSettingsSnapshot does NOT emit them and BACKUP_FORMAT_VERSION is unchanged this phase (D-11, A4)."
    - "Migration 025 adds ONLY duration, allow_ai, history_lens, history_cycle_count and the Tone/channel value remap; it adds NO group_event_id or group-event schema — that column is Phase 33's migration 026+ (D-07, D-12)."
  artifacts:
    - src/db/migrations/025-interaction-history-schema.ts
    - src/db/migrations/025-interaction-history-schema.test.ts
    - src/db/interaction-vocabulary.ts
  key_links:
    - "database.ts MIGRATIONS array registers migration025 after profilePresentationMigration and TARGET_VERSION resolves to 25 — an unregistered migration silently never runs."
    - "The SQL column name stays `quality` (values migrate, name does not) so export-manifest.ts:52 and restore-apply.ts:65,189 keep round-tripping without a backup-format change (Pitfall 2, A1)."
  prohibitions:
    - "The migration must NOT rewrite, coerce, or clear the interactions.note free-text column or any value other than the quality/channel vocabulary literals."
    - "allow_ai must NOT default ON — the durable default is 0 (OFF); shipping default-ON reverses owner decision D-04/E-05."
    - "The SQL column must NOT be renamed quality->tone (that couples this phase to Phase 36's backup-format bump); rename only in TS/UI. Retaining the column name `quality` is a LOCKED invariant (roadmap Schema note + D-06), NOT an open choice."
    - "Migration 025 must NOT add group_event_id or any group-event column/table — that schema is Phase 33's migration 026+ (D-07/D-12). Making any fixture 'constructible' by adding group schema early REVERSES D-07 — STOP and ask the owner, never do it silently."
    - "duration must NOT feed Status, Gravity, or Intensity this milestone — it is descriptive only."
    - "A migration file already shipped (001-023, profile-presentation) must NOT be edited; 025 is additive."
---

<objective>
Ship the irreversible on-device data foundation the whole phase stands on: migration 025 (Tone/channel value remap + nullable `duration` seconds + `allow_ai INTEGER NOT NULL DEFAULT 0` on `interactions`, plus `history_lens`/`history_cycle_count` on `app_settings`), every literal consumer of the old `quality`/channel vocabulary updated in lockstep, and the shared `TouchpointRefineForm` extended with Tone/channel labels, an optional Duration control, and an Allow-AI toggle.

Purpose: This is the highest-risk work in the phase (RESEARCH: "60% data-layer surgery"). A partial vocabulary rename silently corrupts AI context and the weekly digest on real users' unreachable devices; a wrong default on `allow_ai` reverses an owner privacy decision. Getting the migration + consumer lockstep right unblocks the aggregation seam (Plan 03), the Edit route (Plan 04), and every UI surface.
Output: migration 025 + its test, database.ts registration (TARGET_VERSION=25), remapped read/DAO consumers, extended refine form, declare-only backup allowlist.
</objective>

<scope_acceptance>
Plan-checker flagged this plan's size (~90k tokens, confidence: low) at the scope threshold. Splitting is **consciously rejected, not overlooked**: the migration and every live `quality`/channel literal consumer that COMPARES literals (`ai-context-read.ts`, `digest-read.ts`) MUST ship in one commit — the D-06 trip-wire means a partial vocabulary rename silently zeroes AI-context and digest counts on unreachable on-device databases the instant migration 025 lands. Keep this plan atomic. If the executor's context budget degrades mid-plan, the only safe seam is to complete Task 1 (migration + shared vocabulary module) + Task 2 (live-read consumer lockstep) as one landing and take the `TouchpointRefineForm` extension (Task 3) as a follow-up commit — never split Task 1 from Task 2.

**Wave-2 promotion gate (review MEDIUM, claude): Task 3 must land before Wave 2 starts.** Plans 04 (wraps the extended form), 05 (reads `history_lens`/`history_cycle_count`), and 07 (renders the `allow_ai` sparkle) all consume Task 3's outputs (form fields + columns). Gate wave promotion on the FULL Plan-01 file set (Tasks 1–3), not just Tasks 1–2, or Waves 2–3 build against a stale/incomplete contract (tsc breaks / missing readers).

**Restore/export vocabulary is owned by Plan 02, not this plan.** The migration's `UPDATE ... CASE` only rewrites on-device rows at schema-upgrade time; the SEPARATE `restore-apply` writer of `interactions` is remapped in Plan 02 (Wave 2), consuming the SAME `src/db/interaction-vocabulary.ts` helpers this plan creates. Splitting the restore writer out avoids a same-wave file-overlap on `restore-apply.ts`/`restore-apply.test.ts` while keeping ONE canonical vocabulary map (the D-06 single-source-of-truth). The live-read consumers that could miscount on EVERY device the instant 025 lands stay here, in the migration commit.
</scope_acceptance>

<assumption_delta_decision>
The `assumption-delta` detector fired on "optional duration" (a required->optional / derived->chosen signal). Decision: **add-alongside, not promote.** Interactions gain `duration` (nullable) and `allow_ai` (defaulted) as OPTIONAL FIELDS on the existing record. The interaction's identity/anchor does NOT move: it remains `(id, contact_id)` / `uid`, and neither new column participates in identity, uniqueness, the `UNIQUE(uid)` constraint, or the single-writer recency spine's MAX(occurred_at) recompute. No existing consumer's grouping key changes. This is a schema widening, not a re-anchoring of the interaction model.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-CONTEXT.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-11-interaction-history-insights-dossier-v0.2-group-events.md
@docs/dossier/milestone-2/planning-notes/phase-11-planning-notes.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: authorize the irreversible on-device migration 025</name>
  <decision>Migration 025 rewrites stored `quality`/`channel` VALUES on every user's device and is FORWARD-ONLY and IRREVERSIBLE (no backend, no remote DB, no backup to roll back from). Authorize walking through this one-way door: run the value remap + ADD COLUMN as one additive migration (no table rebuild), landing it in the SAME commit as every live literal consumer so no commit ships a half-renamed vocabulary. This gate exists because REVERSIBILITY_GATES flags a one-way-door decision, not because any sub-decision is open.</decision>
  <context>Addresses review divergent finding (codex HIGH / consensus): an EARLIER draft of this gate offered a `quality`->`tone` column rename as an option. That is REMOVED — the roadmap Schema note + D-06 already SETTLE that the SQL column stays `quality` (values migrate, name does not); presenting a rename option reopens a settled decision and risks an executor picking it. Retaining `quality` is now a LOCKED plan invariant (a rename would break export-manifest.ts:52 + restore-apply.ts:65,189 and couple Phase 32 to Phase 36's format bump — Pitfall 2). D-06/D-07 already lock THAT the migration happens and that Phase 32 owns it; this gate only confirms the irreversible mechanics (REVERSIBILITY_GATES on a one-way door). The undo cost is a per-user corrective migration on devices we cannot reach — there is no fix after ship. The only choice here is proceed vs hold — NOT how to migrate.</context>
  <options>
    <option id="approved">
      <name>Approve — run the irreversible migration 025 now (retaining SQL column `quality`)</name>
      <pros>Unblocks the whole phase; additive ALTER+UPDATE, no table rebuild; consumer lockstep in the same commit prevents a half-renamed vocabulary.</pros>
      <cons>Forward-only and irreversible on unreachable devices — but this is the owner-locked plan of record (D-06/D-07).</cons>
    </option>
    <option id="hold">
      <name>Hold — do not migrate yet</name>
      <pros>Defers the one-way door if the owner wants to revisit sequencing.</pros>
      <cons>Blocks all of Phase 32 (every downstream plan depends on 025).</cons>
    </option>
  </options>
  <resume-signal>Reply "approved" to authorize the irreversible migration, or "hold". (Retaining SQL column `quality` is a locked invariant, not a choice — there is no rename option.)</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Migration 025 — schema + Tone/channel value remap, registered end-to-end and proven by test</name>
  <reversibility rating="one-way">Migration 025 rewrites stored interaction vocabulary on every user's device; undoing a mistake requires a corrective per-user migration on devices we cannot reach, and there is no backup to restore from.</reversibility>
  <precondition>src/db/database.ts TARGET_VERSION currently resolves to 24 (PROFILE_PRESENTATION_SCHEMA_VERSION) and no src/db/migrations/025-*.ts exists yet — re-verify head+1 on disk before writing the number (D-03; the number drifts every schema phase).</precondition>
  <files>src/db/interaction-vocabulary.ts, src/db/interaction-vocabulary.test.ts, src/db/migrations/025-interaction-history-schema.ts, src/db/migrations/025-interaction-history-schema.test.ts, src/db/database.ts, src/db/migrations/full-chain.test.ts, src/db/migrations/runner.test.ts</files>
  <read_first>
    - src/db/database.ts (MIGRATIONS array + TARGET_VERSION at :62,88-90 — the registration site; re-verify head)
    - src/db/migrations/019-dashboard-prefs.ts (ADD COLUMN + CHECK + DEFAULT on app_settings — the additive-migration shape to copy)
    - src/db/migrations/017-knowledge-egress-datamove.ts (:35-37 the `allow_ai INTEGER NOT NULL DEFAULT 0` column verbatim precedent + irreversible data-move discipline)
    - src/db/migrations/runner.ts and src/db/migrations/full-chain.test.ts (how the runner sorts/applies and how the snapshot tests assert the chain — they break until TARGET_VERSION updates)
    - src/db/migrations/001-initial.ts (:103,106 the interactions CHECK-less channel/quality columns — confirms ALTER-only, no rebuild)
  </read_first>
  <action>FIRST create `src/db/interaction-vocabulary.ts` — the SINGLE canonical legacy->new vocabulary map (addresses D-06 trip-wire "no divergent rename"): export a frozen `LEGACY_QUALITY_REMAP` ({good:'Positive', fine:'Neutral', hard:'Negative'}), `LEGACY_CHANNEL_REMAP` ({text:'Message', email:'Message', call:'Call', 'in-person':'In Person'}), and pure `remapLegacyQuality(v)` / `remapLegacyChannel(v)` helpers that pass through any value NOT in the map (other/unspecified/NULL/already-new). Migration 025 AND Plan 02's restore-remap both consume THIS module — never a second copy. Write `interaction-vocabulary.test.ts` (RED) pinning every mapping + passthrough. Then write the migration Nyquist test (RED): `025-interaction-history-schema.test.ts` asserts, over a fixture seeded at schema v1 and migrated to v25: good/fine/hard -> Positive/Neutral/Negative, text/email -> Message, call -> Call, in-person -> In Person, other/unspecified/NULL untouched; every interaction row has allow_ai=0 and a new insert defaults allow_ai=0; allow_ai rejects values outside {0,1}; duration is nullable (absent -> NULL); app_settings.history_lens='cycles' and history_cycle_count=10. Then implement `025-interaction-history-schema.ts` exporting `version: 25`: ALTER TABLE interactions ADD COLUMN duration INTEGER; ADD COLUMN allow_ai INTEGER NOT NULL DEFAULT 0 CHECK(allow_ai IN (0,1)); write the `UPDATE interactions SET quality = CASE ... END` and `SET channel = CASE ... END` CASE arms as FROZEN literals INSIDE the migration file (do NOT generate them from `interaction-vocabulary.ts` at runtime — the runner invokes the migration at upgrade time, so a shipped migration must never depend on a mutable helper whose later edit would silently alter shipped behavior; review cycle-2 non-HIGH) and add a test pinning those frozen migration CASE outputs equal to `remapLegacyQuality`/`remapLegacyChannel` for good/fine/hard/text/email/call/in-person and every passthrough, so the migration and the shared map cannot drift (other/unspecified pass through); ALTER TABLE app_settings ADD COLUMN history_lens TEXT NOT NULL DEFAULT 'cycles'; ADD COLUMN history_cycle_count INTEGER NOT NULL DEFAULT 10. Keep the SQL column named `quality` (locked invariant). Register migration025 in `database.ts` MIGRATIONS after profilePresentationMigration and set the exported version constant so TARGET_VERSION resolves to 25. Update `full-chain.test.ts`/`runner.test.ts` expected-version snapshots to 25.</action>
  <verify>
    <automated>npx vitest run src/db/interaction-vocabulary.test.ts src/db/migrations/025-interaction-history-schema.test.ts src/db/migrations/full-chain.test.ts src/db/migrations/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `interaction-vocabulary.test.ts` pins every legacy->new mapping and every passthrough (other/unspecified/NULL/already-new), and this module is the ONLY definition of the mapping (a grep proves migration 025 and restore-apply reference it, not a duplicated table).
    - The migration test passes and asserts the full remap table above, allow_ai default 0 + CHECK, duration nullable, and the two app_settings prefs — over a jump-from-v1 fixture.
    - `TARGET_VERSION` resolves to 25 and the full-chain/runner snapshot tests pass at version 25.
    - Behavior assertion: a row with quality NULL before the migration reads quality NULL after (NULL is not coerced to a Tone value).
    - Source assertion: the migration contains no UPDATE of the `note` column, no DROP/rebuild of `interactions`, and no `group_event_id`/group-event DDL (D-07/D-12).
    - Source assertion (review cycle-2): migration 025's quality/channel CASE arms are frozen literals in the migration file — a grep shows the migration file does NOT import or call `remapLegacyQuality`/`remapLegacyChannel` to build its SQL at runtime — and a test pins those frozen CASE outputs equal to the shared helper outputs for good/fine/hard/text/email/call/in-person and every passthrough (the migration cannot drift from the helper, yet does not depend on it at upgrade time).
  </acceptance_criteria>
  <done>Migration 025 applies cleanly from any prior version, remaps the vocabulary via the single shared map, adds duration/allow_ai/history prefs, is registered, and every migration test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update every quality/channel literal consumer in lockstep with the migration</name>
  <files>src/db/ai-context-read.ts, src/db/ai-context-read.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts, src/ai/prompt-types.ts, src/db/recency-dao.ts, src/db/recency-dao.test.ts, src/db/interaction-assist-dao.ts</files>
  <read_first>
    - src/db/ai-context-read.ts (:101-145 readInteractionAggregates — the good/fine/hard tally + QualityAggregate shape; it deliberately never selects the note)
    - src/db/digest-read.ts (:141-167 readGentleLine — good|fine|hard tally + hard flag; keep the windowModifier() interpolation, change only the compared literals)
    - src/ai/prompt-types.ts (:58,113-114 QualityAggregate — decide field-name handling; see acceptance criteria)
    - src/db/recency-dao.ts (:1-51 header invariant, :189-213 insertInteraction core, :280-309 editTouchpointFull — add duration+allow_ai to the INSERT and UPDATE column lists + params; update doc comments that say good|fine|hard)
    - src/db/interaction-assist-dao.ts (:98-111 markAssistLogged calls insertInteractionCore with `channel: transactionAssist.channel` RAW — the live writer; route the channel through remapLegacyChannel here)
    - src/db/interaction-assist-dao.test.ts (the markAssistLogged test to extend with the email-assist case)
    - src/db/migrations/014-interaction-assists.ts (:12 the call|text|email transport CHECK that stays — note it ALLOWS 'email')
  </read_first>
  <behavior>
    - readInteractionAggregates over rows storing Positive/Neutral/Negative returns the correct positive/neutral/negative counts (not all-zero).
    - readGentleLine counts Positive+Neutral+Negative into total and flags Negative as the effortful case.
    - recordTouchpoint/editTouchpointFull persist and round-trip duration (nullable) and allow_ai (0/1) alongside the existing columns.
    - markAssistLogged writes 'Call' for an assist channel of 'call', 'Message' for 'text', AND 'Message' for 'email' (routed through remapLegacyChannel — the 014 CHECK permits 'email' and an email assist must never persist a retired 'email' channel into a v25 interactions row).
    - deleteTouchpoint writes an interaction tombstone in-txn, recomputes last_contact, and a forced-failure delete leaves the row + derived metrics intact.
  </behavior>
  <action>Remap the value literals in ai-context-read.ts and digest-read.ts from good/fine/hard to Positive/Neutral/Negative. For prompt-types.ts QualityAggregate: resolve the FLAG (review Plan-01 MEDIUM / claude LOW) by KEEPING the internal `{ good, fine, hard }` field names (minimal blast radius, RESEARCH A3) and remapping only the SQL comparison literals — but add an UNMISSABLE in-file doc comment on the QualityAggregate type stating `good = Positive`, `fine = Neutral`, `hard = Negative` so the name/value mismatch is mechanically unambiguous; do NOT ripple a rename into the prompt builder this phase. AUDIT `src/types.ts` LastInteractionType (review Plan-01 MEDIUM): grep confirms it is referenced only at its declaration and as an OPTIONAL `Contact.lastInteraction` field with NO code populating it from `interactions.channel` — mark it in-file as an unused legacy type intentionally excluded from the migration (or deprecate it), do NOT silently leave the divergence unexplained. Add `duration` and `allow_ai` to the INSERT column list in insertInteraction and to the SET list in editTouchpointFull in recency-dao.ts (bound `?` params, never interpolated), preserving the scope-by-id-AND-contact_id + changes===1 + recomputeLastContact contract exactly. In interaction-assist-dao.ts markAssistLogged, route the assist transport channel through the shared `remapLegacyChannel(transactionAssist.channel)` at the insertInteractionCore call (call->Call, text->Message, AND email->Message — the 014 CHECK permits 'email', and cycle-2 HIGH found the prior call/text-only mapping would write a retired 'email' value into a v25 interactions row, re-opening the partial-rename hazard T-32-03); leave the interaction_assists transport CHECK untouched (assert via grep, review suggestion 6). Extend interaction-assist-dao.test.ts with a case per allowed transport channel — call/text/email — asserting the stored interactions.channel round-trips as Call/Message/Message respectively. Extend ai-context-read.test.ts and digest-read.test.ts with cases over the migrated vocabulary (the jump-from-v1 migrate + readInteractionAggregates + digest gentle-line regression is NON-optional — review suggestion 5). Extend recency-dao.test.ts with duration/allow_ai round-trip cases AND the deleteTouchpoint regression (newest + non-newest tombstone/recompute + forced-failure preserves the row — review Plan-07 MEDIUM: this is the DAO-level delete test Plan 07's UI relies on).</action>
  <verify>
    <automated>npx vitest run src/db/ai-context-read.test.ts src/db/digest-read.test.ts src/db/recency-dao.test.ts src/db/interaction-assist-dao.test.ts && grep -n "CHECK" src/db/migrations/014-interaction-assists.ts && grep -n "remapLegacyChannel" src/db/interaction-assist-dao.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: an ai-context-read test over a jump-from-v1-migrated fixture of Positive/Neutral/Negative rows returns non-zero counts matching the fixture (proves no silent zero from a stale literal); the digest gentle-line tally is likewise correct — this regression is non-optional.
    - Source assertion: `grep -nE "'good'|'fine'|'hard'" src/db/ai-context-read.ts src/db/digest-read.ts` returns no value-comparison literal (only, if present, an internal field-name alias documented as such).
    - Source assertion: the interaction_assists channel CHECK (`call|text|email`, 014-interaction-assists.ts) is unchanged (grep shows the transport CHECK intact); markAssistLogged routes the transport channel through `remapLegacyChannel` (grep confirms), and a test asserts an assist of 'call'->'Call', 'text'->'Message', AND 'email'->'Message' in the stored interactions row (no legacy 'email' persists into v25 — review cycle-2 HIGH).
    - Source assertion: src/types.ts LastInteractionType carries a comment documenting it is an unused legacy type (or is deprecated), so its non-migration is deliberate, not an omission.
    - recency-dao.test.ts asserts duration=NULL/allow_ai=0 defaults round-trip, a set duration/allow_ai round-trips, AND deleteTouchpoint writes the tombstone + recomputes + a forced-failure delete preserves the interaction and its recency.
  </acceptance_criteria>
  <done>Every literal consumer of the old vocabulary is updated, duration/allow_ai flow through the single recency writer, the delete regression is proven at the DAO, and all touched tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend TouchpointRefineForm (Tone/channel/duration/Allow-AI) + declare-only backup allowlist</name>
  <files>src/components/TouchpointRefineForm.tsx, src/components/touchpoint-refine-logic.ts, src/components/touchpoint-refine-logic.test.ts, src/backup/backup-schema.ts</files>
  <read_first>
    - src/components/TouchpointRefineForm.tsx (:40-73 TouchpointRefineValue + CHANNEL_OPTIONS + QUALITY_OPTIONS — the controlled value and the two enums to migrate; :242-267 the Quality label region)
    - src/components/touchpoint-refine-logic.ts (the node-tested shaping/validation module — extend it, keep correctness out of the .tsx)
    - src/backup/backup-schema.ts (:133-187 PORTABLE_SETTINGS_KEYS declare-only pattern with Phase-25/29/30/31 comments to mirror)
    - src/db/memories-read.ts (:31 MEMORY_AI_ELIGIBILITY — the allow_ai = 1 gate idiom the Allow-AI toggle mirrors semantically)
  </read_first>
  <action>In TouchpointRefineForm.tsx, relabel Quality -> "Tone" with options Positive/Neutral/Negative (null-by-default, never treated as Neutral when omitted), and present the channel control as Message/Call/In Person while keeping other/unspecified representable as legacy. Extend TouchpointRefineValue with `duration: number | null` (seconds; entry in minute/hour presets 5m/15m/30m/1h/2h plus Custom and a clear/none option) and `allowAi: number` (0/1, default 0) rendered as an Allow-AI toggle beside the note. Move any new validation/shaping (duration parse to bounded seconds; allowAi coercion to 0/1) into touchpoint-refine-logic.ts with node tests; the .tsx stays a controlled widget. In backup-schema.ts add "historyLens" and "historyCycleCount" to PORTABLE_SETTINGS_KEYS with a comment mirroring lines 167-187 ("Phase 32: accepted for restore only; emission + BACKUP_FORMAT_VERSION bump are Phase 36"). Do NOT add them to getPortableSettingsSnapshot and do NOT bump BACKUP_FORMAT_VERSION.</action>
  <verify>
    <automated>npx vitest run src/components/touchpoint-refine-logic.test.ts && npx tsc --noEmit && grep -n "historyLens\|historyCycleCount" src/backup/backup-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - touchpoint-refine-logic.test.ts covers: a duration preset maps to the right seconds; Custom accepts a bounded minute/hour entry; none/clear yields null; allowAi defaults to 0.
    - Source assertion: `grep -c "getPortableSettingsSnapshot" src/backup/backup-schema.ts` shows the two new keys are NOT inside the snapshot emitter (they appear only in PORTABLE_SETTINGS_KEYS), and BACKUP_FORMAT_VERSION is unchanged (git diff shows no change to that constant).
    - Behavior: the form's Tone control offers Positive/Neutral/Negative and starts unset; the Allow-AI toggle starts OFF.
  </acceptance_criteria>
  <done>The shared refine form carries Tone, three-channel, optional Duration and an OFF-by-default Allow-AI toggle; the two history prefs are restore-accepted but not emitted; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration code -> on-device SQLite | Migration 025 rewrites stored values irreversibly on devices we cannot reach or repair. |
| interaction note -> AI egress | The new allow_ai column is the durable gate on whether an interaction note may ever be transmitted (transmission itself is Phase 36). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering/DoS | migration 025 value remap | high | mitigate | Forward-only, order-independent (runner sorts); ALTER+UPDATE only (no rebuild); test over a jump-from-v1 fixture; never edit a shipped migration; irreversible-migration confirmed at checkpoint. |
| T-32-02 | Information disclosure | allow_ai default | high | mitigate | `DEFAULT 0 CHECK(allow_ai IN (0,1))`; migration test asserts every migrated + new row is 0 (OFF); no read path transmits the note this phase. |
| T-32-03 | Tampering | partial vocabulary rename | high | mitigate | Consumer lockstep in the same plan; grep-gate + aggregate test prove no stale literal silently zeroes AI-context/digest counts; the live `markAssistLogged` writer routes call/text/**email** through `remapLegacyChannel` (email→Message) so no assist re-introduces the retired 'email' channel into a v25 row (review cycle-2 HIGH); the migration's CASE literals are frozen in the file and test-pinned to the shared helper so a helper edit cannot silently alter shipped migration behavior. |
| T-32-04 | Tampering | SQL input in DAO writes | low | mitigate | duration/allow_ai bound as `?` params; no interpolation (parameterized-query discipline already enforced in recency-dao). |
</threat_model>

<verification>
- Full migration suite green at version 25; jump-from-v1 fixture proves order-independence.
- AI-context + digest tallies correct over migrated vocabulary (no silent zero).
- `npx tsc --noEmit` passes across the touched files.
- backup-schema declares the two prefs without emitting them; BACKUP_FORMAT_VERSION unchanged.
</verification>

<success_criteria>
Migration 025 is registered, irreversibly-safe, and test-proven; every old-vocabulary consumer is updated in lockstep; the refine form carries Tone/three-channel/optional-duration/OFF-by-default Allow-AI; the history prefs are restore-accepted only.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- File: `src/db/migrations/025-interaction-history-schema.ts` (export `migration025`, `version: 25`)
- File: `src/db/migrations/025-interaction-history-schema.test.ts`
- File: `src/db/interaction-vocabulary.ts` (exports `LEGACY_QUALITY_REMAP`, `LEGACY_CHANNEL_REMAP`, `remapLegacyQuality`, `remapLegacyChannel`) + its `.test.ts` — the single canonical legacy->new vocabulary map, consumed by migration 025 AND Plan 02's restore-remap
- Columns: `interactions.duration` (INTEGER nullable), `interactions.allow_ai` (INTEGER NOT NULL DEFAULT 0 CHECK 0/1), `app_settings.history_lens` (TEXT DEFAULT 'cycles'), `app_settings.history_cycle_count` (INTEGER DEFAULT 10)
- TouchpointRefineValue fields: `duration`, `allowAi`
- backup allowlist keys: `historyLens`, `historyCycleCount` (declare-only)
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 2) findings this plan resolves:
- **[HIGH, codex/claude consensus] `markAssistLogged` omits `email → Message`** — the live assist writer (`interaction-assist-dao.ts:98-111`) now routes `transactionAssist.channel` through the shared `remapLegacyChannel` (call→Call, text→Message, email→Message), and `interaction-assist-dao.test.ts` pins all three allowed transport channels so an `email` assist can never persist the retired `email` value into a v25 `interactions` row. The 014 transport CHECK is left intact (D-06).
- **[non-HIGH, codex] migration CASE derived from a mutable helper** — the safe option is now REQUIRED: migration 025's quality/channel CASE arms are FROZEN literals inside the migration file (not generated from `interaction-vocabulary.ts` at runtime) and a test pins the frozen outputs equal to the shared helper for every input, so a later helper edit cannot silently alter shipped migration behavior.

Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] restore/export vocabulary miscount** — the restore writer of `interactions` is remapped in **Plan 02** (Wave 2) using THIS plan's shared `interaction-vocabulary.ts` (single source of truth). This plan owns the live-read consumers (ai-context-read/digest-read) that would miscount on every device the instant 025 lands; those stay in the migration commit. Restore is a separate writer split to Plan 02 to avoid same-wave file overlap on restore-apply.
- **[divergent, codex HIGH] Plan 01 checkpoint reopens a settled decision** — the `quality`->`tone` rename option is REMOVED; retaining `quality` is now a locked invariant and the checkpoint only authorizes the irreversible migration.
- **[MEDIUM, codex] LastInteractionType still old vocabulary** — audited and documented as an unused legacy type (no code populates Contact.lastInteraction from interactions.channel), intentionally excluded from the migration.
- **[MEDIUM, codex] QualityAggregate good/fine/hard field-name mismatch** — kept internal (RESEARCH A3) with an unmissable in-file good=Positive/fine=Neutral/hard=Negative alias comment.
- **[MEDIUM, claude] Plan 01 large + Task 3 gates downstream** — scope_acceptance now gates Wave-2 promotion on the FULL Plan-01 file set incl. Task 3.
- **[MEDIUM, codex Plan 07] delete has no node test** — the deleteTouchpoint tombstone/recompute/failure-rollback regression lands here in recency-dao.test.ts (the DAO the UI consumes).
- **[suggestion 5, claude] non-optional jump-from-v1 aggregate/digest test** — made non-optional.
- **[suggestion 6, claude] interaction_assists CHECK untouched** — added a source-grep gate.
- **[D-07/D-12] no group schema in migration 025** — prohibition + acceptance criterion assert no group_event_id/group DDL; a fixture-driven attempt to add it is a stop-and-ask.
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-01-SUMMARY.md` when done.
</output>
