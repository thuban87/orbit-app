---
phase: 32-interaction-history-insights
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/services/history/window.ts
  - src/services/history/window.test.ts
  - src/services/history/buckets.ts
  - src/services/history/buckets.test.ts
  - src/services/history/cycles.ts
  - src/services/history/cycles.test.ts
  - src/services/history/intensity-window.ts
  - src/services/history/intensity-window.test.ts
  - src/db/history-read.ts
  - src/db/history-read.test.ts
autonomous: true
requirements: [HIST-02, HIST-04, HIST-05, HIST-06, HIST-10, HIST-16]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The 7 Days lens produces a rolling window of the last 7 local days ending today; Month produces a window preserving real weekday geometry; Year produces a dense daily grid window; all block dates after today (HIST-05)."
    - "The Cycles lens produces one block per currently-configured cadence cycle for the requested preset (5/10/15/20), newest bottom-right, with the current in-progress cycle identifiable structurally (HIST-04)."
    - "For an Unbound / no-cadence contact (interval_days null or tracking disabled) the Cycles lens returns a defined fallback and NEVER divides by a null interval — the fallback is the shared choice with Phase 31 (D-09)."
    - "buckets map a window + interaction rows to per-cell interaction COUNTS only — lifecycle and non-interaction records never contribute to a count (HIST-02, D-10)."
    - "Counts resolve ONLY from `interactions` rows. Group-linked behavior is an INERT SEAM in Phase 32 (D-12): the group-link discriminator (a predicate that will key on `group_event_id` in Phase 33) resolves hard-FALSE for every Phase-32 row because no `group_event_id` column exists yet, so a Group Event parent can never be an `interactions` row and thus never counts — no runtime filter is needed and no constructible parent+child fixture exists (HIST-16, D-10, D-12)."
    - "intensity-window derives Intensity genuinely SCOPED to the selected window: it filters inputs.interactions to the window bounds and computes over that subset with a window-derived period (reusing impact.ts's pure computeIntensity core, NOT computeContactIntensity's fixed cadence-period-over-all-interactions), keeping the nullable-cadence unavailable guard; two different windows over the same contact yield different results (HIST-06, D-09)."
    - "history-read returns date-indexed markers distinguishing interaction-bearing dates from lifecycle-only dates, exposes a distinct `hasLifecycleRecords`/lifecycle-only signal (so Plan 08 can tell zero-interactions-ever from lifecycle-only), and exposes per-date counts, over a ReadOnlyExecutor with no transaction and no network (local-first)."
    - "history-read (or a sibling knowledge-history read) supplies a date-indexed knowledge-change record family sourced from current_state_entries (current-state-history-read.ts, Phase 24.1 history-aware knowledge) with a per-record `fieldKey` so Plan 07 can interleave and route each to its owning-model edit flow (HIST-10)."
    - { statement: "All window/bucket/cycle date math uses formatLocalDate()/localDateTime() and never toISOString().split('T')[0]; a test near a UTC day boundary (evening local time) proves no off-by-one.", verification: backstop }
  artifacts:
    - src/services/history/window.ts
    - src/services/history/buckets.ts
    - src/services/history/cycles.ts
    - src/services/history/intensity-window.ts
    - src/db/history-read.ts
  key_links:
    - "cycles.ts and intensity-window.ts reuse the impact.ts:138-140 nullable-cadence guard verbatim — never a second, divergent guard; intensity-window filters to the window and calls the pure computeIntensity core (impact.ts:141), never computeContactIntensity over all interactions."
    - "history-read supersedes the renderer contract of profile-history-read.ts (its :38-41 header invites this) without changing persistence, and sources the knowledge-change family from current-state-history-read.ts."
    - "The group-link discriminator is an inert code seam (hard-false in Phase 32); Phase 33's migration 026+ adds group_event_id and makes the seam live — nothing in Phase 32 references that column."
  prohibitions:
    - "Heatmap bucket counts must NOT be affected by lifecycle or any non-interaction record — count-only."
    - "This plan must NOT construct a Group-Event parent+child fixture and must NOT add or assume a `group_event_id` column — that column is Phase 33's migration 026+ (D-07/D-12). Making the group seam 'exercisable' by adding group schema early REVERSES D-07 — STOP and ask the owner. The group predicate stays hard-false in Phase 32."
    - "intensity-window must NOT claim window-scoping while computing over ALL of a contact's interactions with a fixed cadence period — it must filter to the window and derive its period from the window (else the 'same selected window' contract is false)."
    - "Cycle/window math must NOT divide by a null interval and must NOT use toISOString()-based date slicing."
    - "These pure services must NOT import a DAO, a store, a component, or open a write transaction — they are pure, node-testable functions (history-read is the one ReadOnlyExecutor read)."
---

<objective>
Build the reusable temporal-aggregation seam the dossier calls for (§M/§AB): pure, node-testable window generation, count-only bucketing, cycle math with the nullable-cadence guard, and intensity over the shared window — plus the canonical single-contact `history-read` DAO that supersedes `profile-history-read`. This is the second architectural tracer of the phase: it proves the query -> window -> aggregate -> (later) render path end-to-end for the heatmap/intensity/browser that Plans 04-08 consume.

Purpose: Separating query / window-gen / aggregation / presentation (dossier §M) lets future Your Week/analytics supply a different query source, and keeps the correctness-critical count/cycle math in `.test.ts`-covered pure modules rather than in `.tsx` the node harness cannot load. Count-only saturation (D-10) lives here, at the source. The group-link behavior is an INERT SEAM (D-12): the discriminator resolves hard-false in Phase 32 (no `group_event_id` column exists — that is Phase 33's migration 026+), so the count invariant holds structurally without any Phase-32 group schema or constructible parent fixture.
Output: `src/services/history/{window,buckets,cycles,intensity-window}.ts` (+ tests) and `src/db/history-read.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
@docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure window generation + count-only bucketing (7 Days / Month / Year / Cycles)</name>
  <files>src/services/history/window.ts, src/services/history/window.test.ts, src/services/history/buckets.ts, src/services/history/buckets.test.ts</files>
  <read_first>
    - src/utils/dates.ts (:17 formatLocalDate + localDateTime — the ONLY date helpers to use)
    - src/services/impact.ts (:120-150 computeContactIntensity + the pure computeIntensity core it delegates to — the pure-derive, tunable-constants-at-top convention)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (New Tokens Required -> Threshold buckets: day lenses 0/1/2/3+; cycle lens 0/1/2/3/4+)
  </read_first>
  <behavior>
    - window('7days', today) returns 7 consecutive local dates ending today; no future date.
    - window('month', ref) returns the month grid with true weekday alignment and flagged leading/trailing non-month positions.
    - window('year', ref) returns a dense daily grid (weeks as columns, weekdays as rows).
    - buckets(window, interactions) returns per-cell interaction counts; an empty date -> 0; a date with 3 interactions -> 3; a lifecycle-only date -> 0.
    - heatmapLevel(count, lens) buckets counts to 0..3 for day lenses and 0..4 for the cycle lens, capping at the top level.
    - prev/next window navigation shifts the window and clamps so no cell is after today.
  </behavior>
  <action>Create window.ts (lens -> ordered date-window generation for '7days'|'month'|'year'|'cycles'; formatLocalDate only; prev/next shifting with a today-max clamp) and buckets.ts (window + interaction rows -> count-only per-cell buckets; a pure `heatmapLevel(count, lens)` helper with the two threshold tables held in ONE tunable constant object at file top). Both are pure functions, no DB/store import. Write the tests first (RED): boundary counts (0, threshold, threshold+1), empty/single/many inputs, stable ordering for same-date rows, an evening-local-time case proving no UTC off-by-one, and prev/next clamping at today.</action>
  <verify>
    <automated>npx vitest run src/services/history/window.test.ts src/services/history/buckets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover each lens' geometry, the two threshold tables at 0/1/2/3(/4) and one step past the cap, empty/single/many, and a UTC-boundary evening case.
    - Source assertion: `grep -n "toISOString" src/services/history/window.ts src/services/history/buckets.ts` returns nothing.
    - Behavior assertion: a lifecycle-only date contributes 0 to buckets.
  </acceptance_criteria>
  <done>Window and bucket math are pure, count-only, local-date-correct, and fully tested including boundaries and empties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cycle math + intensity-over-window with the nullable-cadence guard</name>
  <files>src/services/history/cycles.ts, src/services/history/cycles.test.ts, src/services/history/intensity-window.ts, src/services/history/intensity-window.test.ts</files>
  <read_first>
    - src/services/impact.ts (:113-147 — `computeContactIntensity` computes its period from inputs.intervalDays over ALL inputs.interactions [NOT window-scoped]; the pure `computeIntensity(interactions, periodDays, rarelyResponds, now)` core it delegates to at :141 is the reusable window-aware primitive; copy the tagged { available: false } guard verbatim; never divide by a null interval)
    - src/db/impact-read.ts (:52-69 getImpactInputs — LEFT JOINs and loads ALL of a contact's interactions with no date filter; this is WHY a bare computeContactIntensity wrapper cannot be window-scoped — you must filter its interactions to the window)
    - src/services/history/window.ts (from Task 1 — the window this consumes; use its start/end bounds to filter)
    - docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md (every cadence consumer must guard nullable cadence)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (## UI Considerations -> no-cadence row: fall back to 7 Days / hide Cycles, coordinate with Phase 31)
  </read_first>
  <behavior>
    - cycles(intervalDays, count, now) for a bound contact returns `count` cycle blocks of interval-length each, newest last (bottom-right), the last block flagged current/in-progress.
    - cycles for intervalDays null OR trackingEnabled !== 1 returns a defined fallback signal (not a throw, not a divide-by-zero) that callers map to the 7-Days fallback.
    - intensityWindow(inputs, window, now) filters inputs.interactions to occurred_at within [window.start, window.end], computes intensity over ONLY that subset with a period derived from the window, and returns the tagged unavailable result for a no-cadence contact — so a Month window and a single-cycle window over the same contact yield DIFFERENT results.
  </behavior>
  <action>Create cycles.ts (current-frequency cycle-block math over the configured cadence; reuse the impact.ts nullable-cadence guard verbatim; return a tagged fallback for no-cadence, never divide by null) and intensity-window.ts. intensity-window MUST be genuinely window-scoped (addresses review HIGH, codex Plan 03): filter inputs.interactions to the window bounds, then call the pure `computeIntensity` core (impact.ts:141) over that filtered subset with a period derived from the window span — do NOT call computeContactIntensity directly (it periods over ALL interactions and cannot honor the selected window). Keep the tagged `{ available: false }` unavailable guard for no-cadence/Unbound contacts. Keep cycle/geometry constants in one tunable object at file top. Write tests first (RED): bound contact with each preset (5/10/15/20), current-cycle flagging, the no-cadence fallback path proving no divide-by-null, intensity-window returning available/unavailable correctly, AND a window-sensitivity test proving two different windows over the same contact's interactions produce different intensity results (the load-bearing proof it is scoped, not global). Record the no-cadence fallback decision (default the lens to 7 Days; keep Cycles selectable but rendering the unavailable state) in the SUMMARY as the D-09 choice — CONSISTENT with Phase 31 (COMPLETE), which already treats no-cadence intensity as computeContactIntensity's `{ available: false }` unavailable state (this is a [DERIVED] implementation detail, not a new owner decision; RESEARCH A2 + UI-SPEC recommend it).</action>
  <verify>
    <automated>npx vitest run src/services/history/cycles.test.ts src/services/history/intensity-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover presets 5/10/15/20, current-cycle flag, and the no-cadence fallback (returns the fallback tag, never NaN/Infinity/throw).
    - Behavior assertion: intensityWindow over two different windows of the same contact's interactions returns different results (proves it filters to the window, not the global set).
    - Source assertion: intensity-window filters interactions by the window bounds and calls `computeIntensity` (the pure core), NOT `computeContactIntensity`; `grep -n "available: false" src/services/history/cycles.ts src/services/history/intensity-window.ts` shows the nullable-cadence guard in both.
    - Behavior assertion: intensityWindow for an Unbound contact returns `{ available: false }`.
  </acceptance_criteria>
  <done>Cycle and intensity-window math guard nullable cadence exactly like impact.ts, intensity is provably scoped to the selected window, and the no-cadence fallback matches Phase 31's treatment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Canonical history-read DAO — date-indexed records/markers, group seam inert, knowledge-change family</name>
  <files>src/db/history-read.ts, src/db/history-read.test.ts</files>
  <read_first>
    - src/db/profile-history-read.ts (:38-55 the interim projection being superseded — copy its closed-SELECT, ?-bound, row-shape-typed style; drop the LIMIT 3)
    - src/db/recency-dao.ts (:1-51 the interactions schema contract — occurred_at is a local wall-clock string; NOTE: no group_event_id column exists — verified)
    - src/db/current-state-history-read.ts (the Phase-24.1 current-state history read — the SOURCE for the knowledge-change record family; returns dated current-state value changes per fieldKey)
    - .planning/phases/32-interaction-history-insights/32-CONTEXT.md (D-12 — group behavior is an inert seam; no group_event_id in Phase 32; NEVER add group schema to make a fixture constructible)
  </read_first>
  <behavior>
    - history-read returns, for a contact, date-indexed interaction records (with channel, occurred_at, direction, connected, quality, note, duration, allow_ai) and lifecycle events, plus per-date markers (interaction-bearing vs lifecycle-only vs multiple-with-count) and a distinct hasLifecycleRecords signal.
    - Counts resolve ONLY from `interactions` rows. The group-link discriminator is a predicate present in code but hard-false in Phase 32 (no group_event_id column), so every Phase-32 interaction is standalone and no parent row can exist to double-count.
    - A knowledge-change record family (from current-state-history-read) is exposed date-indexed with a per-record fieldKey for Plan 07 to interleave and route.
    - Same-timestamp records have a specified, stable order.
  </behavior>
  <action>Create history-read.ts exporting a ReadOnlyExecutor read (no transaction) that returns date-indexed interaction records + lifecycle events + a distinct hasLifecycleRecords signal + markers for one contact, resolving counts/markers from `interactions` rows only. Select the new duration/allow_ai columns. Represent the group-link seam as a pure predicate/flag (e.g. `isGroupLinked(row)`) that keys on a group_event_id field which is UNDEFINED in Phase 32 — so it resolves false for every row (INERT SEAM per D-12); do NOT query, join, or assume a group_event_id column, and do NOT construct a Group-Event parent+child fixture (not representable until Phase 33's migration 026+). Additionally source a date-indexed knowledge-change record family from current-state-history-read (current_state_entries changes per fieldKey) so Plan 07 can interleave it and route each row to its owning-model edit flow (HIST-10). Type the row shapes. Write tests first (RED): a fixture with interactions on some dates, lifecycle-only on others (asserting hasLifecycleRecords true with zero interactions), same-timestamp stable ordering, empty-contact returns empty markers, the isGroupLinked predicate returns false for every Phase-32 interaction row (the inert-seam assertion — NOT a constructible parent fixture), and a knowledge-change record surfaces with its fieldKey.</action>
  <verify>
    <automated>npx vitest run src/db/history-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: counts come only from `interactions` rows; the isGroupLinked predicate returns false for every Phase-32 row (inert seam — no group_event_id exists), proving parents cannot double-count without needing a constructible parent fixture.
    - Behavior assertion: a lifecycle-only date produces a lifecycle marker, a zero interaction count, and hasLifecycleRecords=true; a knowledge-change record is returned with its fieldKey.
    - Source assertion: history-read does NOT reference/select/join `group_event_id` (grep returns nothing — D-12), the SELECTs are ?-bound, and `grep -n "inWriteTransaction" src/db/history-read.ts` returns nothing.
  </acceptance_criteria>
  <done>history-read returns correct date-indexed records/markers + hasLifecycleRecords + a knowledge-change family, counts from interactions rows only, with the group seam inert (no group schema referenced).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interaction rows -> derived counts | The aggregation seam is the single source of the count/marker/intensity numbers every History surface renders; a wrong count corrupts the whole feature. |
| read layer -> UI | history-read is a pure local read; no network may enter this path (local-first). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-08 | Tampering | count aggregation | high | mitigate | Count-only from `interactions` rows; group seam inert (predicate hard-false, no group_event_id column) so no parent row can double-count; buckets never sum lifecycle records. |
| T-32-09 | Information disclosure | read path | low | mitigate | ReadOnlyExecutor, no transaction, no network; all SELECTs ?-bound. |
| T-32-10 | Tampering | nullable-cadence divide | medium | mitigate | Reuse impact.ts guard; test proves no divide-by-null / NaN for Unbound contacts; intensity-window filters to the window subset. |
</threat_model>

<verification>
- All five history modules' tests green, including boundary/empty/ordering/no-cadence/group-exclusion cases.
- No toISOString-based date math; no write transaction in the read layer.
</verification>

<success_criteria>
A reusable, node-tested aggregation seam (window/buckets/cycles/window-scoped intensity) and canonical history-read exist — count-only, with the group-link an inert predicate (hard-false in Phase 32, no group schema referenced) and a knowledge-change family + hasLifecycleRecords signal — ready for the heatmap/intensity/browser surfaces.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/services/history/window.ts`, `buckets.ts`, `cycles.ts`, `intensity-window.ts`, `src/db/history-read.ts` (+ their .test.ts)
- Functions: window generation per lens, `heatmapLevel(count, lens)`, count-only `buckets`, `cycles`, `intensityWindow` (window-scoped), the history-read canonical read, the `isGroupLinked` inert-seam predicate, `hasLifecycleRecords` signal, the knowledge-change record family
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] group-linked behavior depends on Phase-33 schema (D-07/D-12 reversal trap)** — the group-parent+child fixture is DROPPED; the group-link discriminator is an inert predicate hard-false in Phase 32 (no group_event_id column). The count invariant holds structurally (parents are not `interactions` rows), no runtime filter or constructible parent needed. A grep gate + prohibition forbid referencing group schema (adding it = stop-and-ask).
- **[HIGH, codex] computeContactIntensity is not window-scoped** — intensity-window now filters inputs.interactions to the window and calls the pure computeIntensity core with a window-derived period; a window-sensitivity test proves two windows yield different results.
- **[MEDIUM, codex] Unbound Cycles fallback chosen despite 'unresolved'** — resolved in-plan as the D-09 [DERIVED] choice (default to 7 Days, Cycles renders unavailable) CONSISTENT with the now-complete Phase 31's `{ available:false }` treatment; not an owner stop.
- **[HIGH, codex Plan 07] knowledge-change family has no source** — history-read sources a date-indexed knowledge-change family from current-state-history-read (fieldKey per record) for Plan 07 to interleave/route (HIST-10).
- **[MEDIUM, codex Plan 08] lifecycle-only vs empty** — history-read exposes a distinct hasLifecycleRecords signal so Plan 08 distinguishes zero-interactions-ever from lifecycle-only.
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-03-SUMMARY.md` when done.
</output>
