---
phase: 32-interaction-history-insights
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/db/events-dao.ts
  - src/db/contact-lifecycle-dao.ts
  - src/db/contact-lifecycle-dao.test.ts
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/backup/export-manifest.test.ts
autonomous: true
requirements: [HIST-10]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The EventType union includes 'bind' and 'unbind' in addition to archive/restore/snooze/unsnooze (D-08)."
    - "bindContact writes exactly one immutable 'bind' event inside its existing write transaction; unbindContact writes exactly one 'unbind' event inside its existing transaction (D-08, ADR-025)."
    - "A bind/unbind event round-trips through backup restore — restore-apply inserts the type string verbatim and no downstream reader throws on the new EventType value (D-08)."
    - "The bind/unbind event producers compose recordEventCore inside the ALREADY-OPEN inWriteTransaction (never a nested inWriteTransaction) so the non-reentrant mutex never hangs (Pitfall 3)."
    - "restore-apply remaps legacy interaction quality/channel values on ingest via Plan 01's shared remapLegacyQuality/remapLegacyChannel — a pre-Phase-32 (format-4) backup carrying good/fine/hard/text/email lands as Positive/Neutral/Negative/Message in a v25 DB, so ai-context-read/digest-read never miscount through the restore backdoor (D-06 trip-wire, review HIGH)."
    - "The export serializer emits the migrated vocabulary automatically because it SELECTs the live (already-migrated) quality/channel column; a guard test over a post-migration DB pins this, and export-manifest source is NOT changed and BACKUP_FORMAT_VERSION is NOT bumped (Phase 36 owns the format bump) (D-06, A4)."
    - "restore is fail-closed for allow_ai on BOTH restore paths: a fresh-UID insert omits allow_ai so it takes the column DEFAULT 0, AND the merge-mode `ON CONFLICT(uid) DO UPDATE` arm forces `allow_ai=0` in its SET list — so restoring a field-less backup row over an existing v25 interaction with allow_ai=1 drives it to 0/OFF, never preserving or raising consent. A restore can NEVER make an interaction more AI-permissive than the backup implied (D-04 fail-closed intent, review cycle-3 HIGH)."
    - "duration and allow_ai are NOT added to the interactions INSERT column list / wire payload this phase (Phase 36 owns serialization + the format bump); the merge-arm `allow_ai=0` is a fail-closed literal, not a serialized field — recorded as a Phase-36 hand-off that must keep the same 'restore never more-permissive' check across both the insert and merge/update paths (D-04, A4)."
  artifacts:
    - src/db/events-dao.ts
  key_links:
    - "recordEventCore is called before bumpDataRevisionCore inside the same bind/unbind transaction — the composition primitive designed for exactly this (events-dao.ts:60-79)."
    - "events.type is CHECK-less TEXT so no migration is needed — bind/unbind is additive at the TS layer only."
    - "restore-apply's interactions INSERT (restore-apply.ts:189) consumes Plan 01's src/db/interaction-vocabulary.ts — the SAME map migration 025 uses — so restore and migration can never diverge (D-06 single source of truth). This is why this plan depends on 32-01."
    - "restore-apply's interactions `ON CONFLICT(uid) DO UPDATE SET` arm (restore-apply.ts:189) is a SEPARATE consent writer from the INSERT column list: SQLite's DEFAULT 0 fires ONLY on fresh INSERT, and reconcileEntity emits an interactions `update` only when the incoming/backup row wins over an existing local row (reconciliation.ts:370-373). So the SET arm must explicitly set `allow_ai=0` or an existing allow_ai=1 row silently survives a field-less merge restore — this is the sole consent-bit fail-closed guarantee on the merge path (review cycle-3 HIGH, D-04)."
  prohibitions:
    - "Bind/Unbind events must NOT be mutable or editable after insert — lifecycle events are immutable and insert-only (ADR-025)."
    - "The producers must NOT open a nested inWriteTransaction — bind/unbind already open one; nesting is a permanent hang."
    - "This plan must NOT add a migration for events.type — the column is CHECK-less and additive at the union only (D-08)."
    - "This plan must NOT bump BACKUP_FORMAT_VERSION or start emitting duration/allow_ai/history prefs — that is Phase 36's closing format bump (A4)."
    - "The restore-remap must NOT re-implement the vocabulary map — it consumes Plan 01's shared helpers; a second copy would reopen the D-06 partial-rename hazard."
---

<objective>
Two backup/restore integrity workstreams on the shared events/interactions tables: (1) add the two missing lifecycle event types — `bind` and `unbind` — as immutable, insert-only events produced inside the existing bind/unbind write transactions, so the shared Detail Sheet (Plan 07) can render a complete lifecycle history (Archive/Restore/Snooze/Unsnooze/Bind/Unbind) per HIST-10; and (2) close the D-06 restore backdoor — remap legacy interaction quality/channel values on restore-apply ingest so a pre-Phase-32 backup restored onto a v25 device does not silently miscount AI-context/digest.

Purpose: Today `bindContact`/`unbindContact` change `tracking_enabled` but write NO event, so the relationship's bind/unbind moments are invisible in history. Separately, migration 025 rewrites on-device rows only at schema-upgrade time; the restore-apply writer of `interactions` is a SEPARATE writer that today inserts backed-up quality/channel verbatim — a legacy backup would reintroduce good/fine/hard onto a migrated device and re-open Pitfall 1 via the backdoor. That same restore writer is also the SEPARATE consent writer: its `ON CONFLICT(uid) DO UPDATE` arm omits `allow_ai`, so in merge mode a field-less backup row winning reconciliation preserves a destination `allow_ai=1` (SQLite's DEFAULT 0 fires only on fresh INSERT) — the merge path is NOT fail-closed. This plan closes both restore backdoors (vocabulary + consent) so restore is fail-closed for AI permission on every path (enforces D-04). All three are backup/restore integrity for the migrated schema; consolidating them here keeps restore-apply single-owned (no same-wave overlap with Plan 01).
Output: extended EventType union, in-transaction bind/unbind producers, a restore round-trip proof, and the restore-side vocabulary remap consuming Plan 01's shared map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-CONTEXT.md
@CLAUDE.md
@docs/decisions/ADR-025-immutable-lifecycle-events-in-a-unified-timeline.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EventType and emit bind/unbind events inside the existing transactions</name>
  <files>src/db/events-dao.ts, src/db/contact-lifecycle-dao.ts, src/db/contact-lifecycle-dao.test.ts</files>
  <read_first>
    - src/db/events-dao.ts (:8-36 header + EventType union; :60-79 recordEventCore the in-txn composition primitive; :86-91 recordEvent wrapper)
    - src/db/contact-lifecycle-dao.ts (:27-106 bindContact and unbindContact — the existing inWriteTransaction blocks that update tracking_enabled but write no event)
    - src/db/interaction-assist-dao.ts (:91-124 the reference for composing non-mutexed cores inside ONE inWriteTransaction)
    - src/db/uid.ts (newUid — Hermes has no crypto global; mint event uid with this)
  </read_first>
  <behavior>
    - After bindContact runs, exactly one events row of type 'bind' exists for that contact with occurred_at === now (the bind moment).
    - After unbindContact runs, exactly one events row of type 'unbind' exists with occurred_at === now (the unbind moment).
    - No second event is written and no nested-transaction hang occurs.
  </behavior>
  <action>Extend the EventType union in events-dao.ts to `"archive" | "restore" | "snooze" | "unsnooze" | "bind" | "unbind"`. Inside bindContact's existing inWriteTransaction, call recordEventCore(exec, { uid: newUid(), contactId, type: "bind", occurredAt: now, now, detail: null }) BEFORE bumpDataRevisionCore — the event's occurredAt is set to `now` (the bind moment), NOT invented from another field (review cycle-2 LOW: name the source so the executor does not guess); do the same with type "unbind" (occurredAt: now) inside unbindContact. Mint uid via newUid(). Do not open a new transaction. Extend contact-lifecycle-dao.test.ts to assert exactly one bind event after bind and one unbind event after unbind, with the correct type string, `occurred_at === now`, and that the event is not editable through any lifecycle path.</action>
  <verify>
    <automated>npx vitest run src/db/contact-lifecycle-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the test shows one 'bind' event after bindContact and one 'unbind' after unbindContact, each with `occurred_at === now` (the bind/unbind moment).
    - Source assertion: `grep -n "inWriteTransaction" src/db/contact-lifecycle-dao.ts` shows the recordEventCore call sits inside the existing transaction block, not a new one.
    - Source assertion: EventType includes 'bind' and 'unbind'.
  </acceptance_criteria>
  <done>Bind and unbind write exactly one immutable event each, inside the existing transaction, with a test proving it.</done>
</task>

<task type="auto">
  <name>Task 2: Verify bind/unbind round-trip through backup restore without an exhaustive-switch throw</name>
  <files>src/backup/restore-apply.test.ts, src/backup/restore-apply.ts</files>
  <read_first>
    - src/backup/restore-apply.ts (:51 event tombstone entity registration; :66,190 the events INSERT that writes type verbatim)
    - src/backup/backup-schema.ts (search for any event-type allowlist / exhaustive validation of EventType)
    - src/backup/restore-apply.test.ts (the restore round-trip test to extend)
  </read_first>
  <action>Grep the codebase for any reader that switches exhaustively on EventType and throws on an unknown value (e.g. a `default:` that throws, or a validator with a fixed set). If one exists, widen it to accept bind/unbind. Extend restore-apply.test.ts with a case that backs up a contact carrying a bind event and an unbind event and asserts they restore with their type strings intact and no throw. If no exhaustive-switch reader exists, record that finding in the SUMMARY and keep the round-trip test as the guard.</action>
  <verify>
    <automated>npx vitest run src/backup/restore-apply.test.ts && grep -rn "EventType" src/ --include=*.ts | grep -iv test</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the restore test round-trips a bind and an unbind event with type strings preserved and no exception.
    - Source assertion: no reader throws on an unknown EventType (the grep output is reviewed; any exhaustive switch is widened or shown to have a safe fallback).
  </acceptance_criteria>
  <done>Bind/unbind events survive backup+restore intact; no downstream reader rejects the new type strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remap legacy interaction vocabulary on restore ingest + confirm export emits migrated vocab (D-06 backdoor)</name>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/backup/restore-apply.ts (:189 the interactions INSERT ... ON CONFLICT that today writes r.quality / r.channel VERBATIM — the second writer of interactions the migration does not cover — AND whose `ON CONFLICT(uid) DO UPDATE SET` arm omits allow_ai, the consent-bit gap this task closes)
    - src/backup/reconciliation.ts (:363-374 — for interactions (writeMode 'lww') an `update` action is emitted ONLY when the incoming/backup row wins over an existing local row; a local-wins case is `retain` and runs no SQL. So forcing allow_ai=0 in the UPDATE arm only touches rows the backup actually overwrote — it never clobbers an untouched local-only consent)
    - src/db/interaction-vocabulary.ts (Plan 01 — the canonical remapLegacyQuality/remapLegacyChannel helpers to consume; do NOT duplicate the map)
    - src/db/migrations/025-interaction-history-schema.ts (Plan 01 — adds `interactions.allow_ai INTEGER NOT NULL DEFAULT 0`; this test file's local `db()` helper currently stops at v24 and MUST be extended to run migration025 at target version 25 so the allow_ai column exists for the merge-mode regression)
    - src/backup/export-manifest.ts (:52 the interactions SELECT — reads the LIVE quality/channel column; post-migration this is already the new vocabulary, so NO source change is needed here)
    - .planning/phases/32-interaction-history-insights/32-RESEARCH.md (§"The quality/channel data migration" §C restore/export consumer rows; Pitfall 1)
  </read_first>
  <behavior>
    - A restore-apply of a backup row with quality='good', channel='text' writes quality='Positive', channel='Message' into the v25 DB (legacy values remapped on ingest via the shared helpers); a row with channel='email' likewise writes channel='Message' (the email path is the same one cycle-2 fixed in markAssistLogged — cover it here too).
    - A restore-apply of a row already carrying new vocabulary (quality='Positive') writes it unchanged (idempotent — passthrough).
    - Export of a post-migration DB serializes 'Positive'/'Message' (it reads the migrated column; no code change, guard test only).
    - Restore is fail-closed for allow_ai on both paths: (a) a fresh-UID interaction insert omits allow_ai and takes the column DEFAULT 0; (b) in merge mode, an existing v25 destination interaction seeded with allow_ai=1, when a winning field-less backup row updates it via `ON CONFLICT(uid) DO UPDATE`, ends with allow_ai=0 (the SET arm forces it) — a restore never preserves or raises an interaction's AI-consent bit.
  </behavior>
  <action>In restore-apply.ts, wrap the `r.quality`/`r.channel` bindings of the interactions INSERT (`:189`) with Plan 01's `remapLegacyQuality(r.quality)` / `remapLegacyChannel(r.channel)` so legacy values are remapped on ingest (passthrough for other/unspecified/NULL/already-new — the helpers guarantee idempotency). Do NOT add duration or allow_ai to the interactions INSERT column list or its VALUES params (Phase 36 owns wire serialization + the format bump; a fresh-UID insert stays fail-closed by taking the column DEFAULT 0). SEPARATELY, close the merge-path consent gap: extend the interactions `ON CONFLICT(uid) DO UPDATE SET …` clause on that same statement with `allow_ai=0` (written in the existing no-space SET style, matching `quality=excluded.quality`), so an existing v25 interaction carrying allow_ai=1 is forced OFF when a field-less backup row wins reconciliation and updates it — SQLite's DEFAULT 0 fires only on fresh INSERT, so without this the merge/update arm silently preserves the destination consent bit (review cycle-3 HIGH; enforces D-04 fail-closed consent). This reverses no recorded decision: it does NOT add allow_ai to the INSERT/wire payload and does NOT bump BACKUP_FORMAT_VERSION. Leave `duration` OUT of the SET arm (the reviewers' `duration = NULL` rider is a deferred, non-blocking merge-policy taste call — see review_resolutions). Do NOT change export-manifest.ts source and do NOT bump BACKUP_FORMAT_VERSION. Extend restore-apply.test.ts (RED): (1) assert a legacy-vocabulary interaction row restores as the migrated vocabulary — cover quality='good'→'Positive', channel='text'→'Message', AND channel='email'→'Message' (review cycle-2: email is the systemic gap) — and an already-migrated row round-trips unchanged; (2) add a merge-mode consent regression that seeds a v25 destination interaction row (same uid) with allow_ai=1, restores in `merge` mode a winning backup row (newer modified_at) that omits allow_ai, and asserts the destination row's allow_ai reads back 0 after restore. Update this test file's local `db()` helper to append Plan 01's migration025 (import from `@/db/migrations/025-interaction-history-schema`) and pass target version 25 so the allow_ai column exists (migration025 is additive; existing fixtures set no good/fine/hard/text/email/call/in-person values, so the remap leaves them untouched). Add export-manifest.test.ts guard asserting export over a migrated DB emits Positive/Message. Record the Phase-36 hand-off in the SUMMARY: duration/allow_ai serialization + the format bump land in Phase 36, and Phase 36 must keep the two-path "restore never falsely appears more-permissive" check (allow_ai fail-closed on BOTH the insert and merge/update arms).</action>
  <verify>
    <automated>npx vitest run src/backup/restore-apply.test.ts src/backup/export-manifest.test.ts && grep -n "remapLegacyQuality\|remapLegacyChannel" src/backup/restore-apply.ts && grep -n "allow_ai=0" src/backup/restore-apply.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: a restored legacy row (quality='good', channel='text') lands as quality='Positive', channel='Message', and a channel='email' row lands as channel='Message'; an already-migrated row round-trips unchanged.
    - Behavior assertion (review cycle-3 HIGH): a merge-mode restore over an existing v25 interaction with allow_ai=1, using a winning backup row that omits allow_ai, leaves the row at allow_ai=0 (fail-closed on the `ON CONFLICT ... DO UPDATE` path, not only on fresh INSERT).
    - Source assertion: restore-apply.ts references remapLegacyQuality/remapLegacyChannel from src/db/interaction-vocabulary.ts (no duplicated map), and its interactions `ON CONFLICT(uid) DO UPDATE SET` arm sets `allow_ai=0` (`grep -n "allow_ai=0" src/backup/restore-apply.ts` matches).
    - Source assertion: the `INSERT INTO interactions (...)` column tuple and its VALUES params are unchanged (git diff shows the ONLY interactions-SQL change is the added `allow_ai=0` inside the ON CONFLICT UPDATE arm — allow_ai/duration are NOT added to the INSERT column list or wire payload), and BACKUP_FORMAT_VERSION is unchanged (git diff shows no change to that constant).
    - Behavior assertion: an export over a post-migration fixture emits the new vocabulary (guard test).
  </acceptance_criteria>
  <done>The restore writer of interactions remaps legacy vocabulary via the shared map, the merge/update path forces allow_ai=0 so restore is fail-closed for AI consent on both the insert and update arms, export emits the migrated vocabulary, and the format bump/serialization stay a documented Phase-36 hand-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle mutation -> events table | Bind/unbind now leave an immutable audit trace; a mis-scoped write could corrupt the shared events history. |
| backup/restore -> events table | A restored backup carrying the new type strings must not crash restore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Tampering | bind/unbind event producer | medium | mitigate | recordEventCore is insert-only and immutable (ADR-025); composed inside the existing transaction; test asserts exactly one event, no nesting. |
| T-32-06 | DoS | non-reentrant write mutex | medium | mitigate | Producers call the non-mutexed core inside the already-open transaction; never a nested inWriteTransaction (Pitfall 3). |
| T-32-07 | Tampering | restore of unknown EventType | low | mitigate | restore-apply inserts type verbatim; round-trip test + exhaustive-switch grep prove no reader throws. |
| T-32-07b | Tampering | legacy-vocabulary restore backdoor | high | mitigate | restore-apply remaps quality/channel on ingest via Plan 01's shared map; test over a legacy format-4 fixture proves ai-context/digest counts are not silently zeroed; single source of truth prevents migration/restore drift. |
| T-32-07c | Information disclosure | restore of allow_ai (consent bit) | high | mitigate | Fail-closed on BOTH restore paths: a fresh-UID insert omits allow_ai and takes the column DEFAULT 0, AND the merge-mode `ON CONFLICT(uid) DO UPDATE SET` arm forces `allow_ai=0` — so a field-less backup winning reconciliation over an existing allow_ai=1 row drives consent OFF, never preserving or raising it (SQLite DEFAULT fires only on INSERT; reconcileEntity emits an interactions `update` only when the backup wins, so the UPDATE arm is the required guard). A merge-mode regression seeds a v25 allow_ai=1 destination row and asserts 0 after a field-less restore. Phase-36 hand-off must keep this two-path check when it serializes allow_ai. |
</threat_model>

<verification>
- contact-lifecycle-dao.test.ts proves one bind + one unbind event, immutable, in-transaction.
- restore-apply.test.ts proves round-trip of the new event types AND legacy interaction-vocabulary remap on ingest.
- restore-apply.test.ts proves the merge/update path is fail-closed for allow_ai: an existing v25 allow_ai=1 interaction overwritten by a field-less backup winner reads back allow_ai=0 (the `ON CONFLICT ... DO UPDATE` arm forces it); the fresh-insert path stays fail-closed via the column DEFAULT 0.
- export-manifest.test.ts guard proves export emits migrated vocabulary; BACKUP_FORMAT_VERSION unchanged.
- No exhaustive EventType switch throws on bind/unbind.
</verification>

<success_criteria>
Bind and unbind produce immutable in-transaction lifecycle events that survive backup/restore, and the restore writer of interactions remaps legacy vocabulary so no restored backup re-opens the D-06 miscount — ready for the Detail Sheet's read-only lifecycle rows and correct AI-context/digest counts. Restore is also fail-closed for the allow_ai consent bit on BOTH the insert and merge/update arms, so no restore can silently keep an interaction AI-permissive (D-04).
</success_criteria>

<artifacts_produced>
New symbols this plan creates (source-grounding exclusion list):
- EventType values: `"bind"`, `"unbind"`
- bind/unbind event producers inside contact-lifecycle-dao (bindContact/unbindContact)
- restore-apply interaction-vocabulary remap-on-ingest (consumes Plan 01's `remapLegacyQuality`/`remapLegacyChannel`)
- restore-apply interactions `ON CONFLICT(uid) DO UPDATE SET` arm gains `allow_ai=0` (merge-path fail-closed consent; INSERT column list / wire payload unchanged)
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 3) findings this plan resolves:
- **[HIGH, codex; orchestrator-confirmed] restore not fail-closed for allow_ai in merge mode** — the interactions `ON CONFLICT(uid) DO UPDATE SET` arm (restore-apply.ts:189) omitted allow_ai, so a merge-mode `update` (which reconcileEntity emits ONLY when the incoming/backup row wins over an existing local row — reconciliation.ts:370-373) preserved a destination allow_ai=1 when the backup carried no permission field, because SQLite's DEFAULT 0 applies only to fresh INSERT. replace-all was already fail-closed (it `DELETE FROM interactions` first, then inserts). Fixed by adding `allow_ai=0` to that SET arm (Task 3) — the INSERT column list / wire payload is unchanged, BACKUP_FORMAT_VERSION is NOT bumped, and no recorded decision is reversed (this ENFORCES D-04 fail-closed consent). A new merge-mode regression seeds a v25 allow_ai=1 destination row and asserts it reads 0 after a field-less restore; the must_have, threat T-32-07c, and success criteria now cover BOTH the replace-all/insert and merge/update paths. Consent withdrawn by a fail-closed restore is re-grantable through Edit Interaction (Plan 04), matching D-04's "a withheld note can later be permitted."
- **[deferred, non-blocking] duration reset on the same merge arm** — the reviewers' optional `duration = NULL` rider on the ON CONFLICT arm is a product/merge-policy taste call, NOT a consent/correctness gap (duration is descriptive-only, never feeds Status/Gravity/Intensity — D-12/A). Deliberately NOT changed this phase; duration on the merge arm stays as-is and is left to Phase 36 or a future explicit owner decision.

Cross-AI review (32-REVIEWS.md, cycle 2) findings this plan resolves:
- **[LOW, claude] `recordEventCore` occurredAt source unspecified** — bind/unbind now explicitly set `occurredAt = now` (the bind/unbind moment) and the round-trip test asserts `occurred_at === now`, so the executor never invents the field.
- **[suggestion, codex] email-assisted restored interaction** — the restore regression now covers a `channel='email'` legacy row remapping to `Message` on ingest (the same email path cycle-2 closed in Plan 01's `markAssistLogged`), via the shared `remapLegacyChannel` helper.

Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] restore/export re-opens the vocabulary miscount** (codex framed as widened-schema, claude as vocabulary miscount) — restore-apply now remaps legacy quality/channel on ingest via Plan 01's shared map (Task 3); export emits the migrated vocab automatically (guard test); BACKUP_FORMAT_VERSION is NOT bumped (Phase 36 owns it — decision preserved).
- **[LOW → superseded by cycle-3 HIGH, claude] duration/allow_ai reset on restore** — the original "allow_ai omitted → defaults 0/OFF" note held only for fresh inserts; cycle 3 corrected the merge/update arm (see the cycle-3 block above), so allow_ai is now fail-closed on BOTH paths. duration serialization remains the Phase-36 boundary.
- **[LOW, codex Plan 02] event immutability not directly testable** — retained the round-trip guard; immutability is enforced structurally (no event-update API).
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-02-SUMMARY.md` when done.
</output>
