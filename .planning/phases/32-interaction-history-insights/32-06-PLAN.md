---
phase: 32-interaction-history-insights
plan: 06
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - src/components/history/RolodexWheel.tsx
  - src/components/history/RolodexBrowser.tsx
  - src/components/history/rolodex-logic.ts
  - src/components/history/rolodex-logic.test.ts
autonomous: true
requirements: [HIST-08, HIST-09, HIST-18]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The History Browser is three synchronized wheels — Month / Day / Year — with Day as the primary axis; scrolling Day rolls Month/Year at boundaries and Month/Year are independently adjustable (HIST-08)."
    - "Invalid dates clamp conventionally (e.g. Aug 31 -> Feb clamps to 28/29) and today is the maximum — future dates are not browsable (HIST-08)."
    - "Eventful dates are marked before selection: filled dot = interaction-bearing, ring/outline = lifecycle-only, filled + numeric count = multiple; silhouette is the primary cue and actual counts/types are exposed to accessibility (HIST-08, HIST-18)."
    - "A drawer beneath the wheels summarizes the selected date (interaction + event counts including lifecycle) and exposes 'See details' / 'Log interaction'; scrolling or selecting NEVER auto-opens the detail sheet (HIST-09)."
    - "Wheel animation uses Reanimated + react-native-gesture-handler only (optional Galaxy Skia glow), never React setState per frame; animation pauses on useIsFocused===false and AppState background (conditionally mounted) (HIST-08)."
    - "Reduced Motion (useReducedMotionShared) simplifies wheel depth/inertia/decorative transitions WITHOUT removing date navigation; the wheels support a non-gesture adjustment path for accessibility (HIST-18)."
    - "Helper worklets are defined ABOVE their callers (no worklet-forward-reference; Hermes crashes undefined-on-device and vitest cannot catch it) — verified by structure and Pixel UAT."
    - { statement: "Wheel scroll, inertia, marker rendering, and reduced-motion simplification behave correctly on the physical Pixel (emulator cannot judge Skia/render-loop perf).", verification: backstop }
  artifacts:
    - src/components/history/RolodexWheel.tsx
    - src/components/history/RolodexBrowser.tsx
    - src/components/history/rolodex-logic.ts
  key_links:
    - "RolodexBrowser consumes Plan 03's date-indexed markers/summaries; the drawer's counts include lifecycle (unlike the heatmap card which is interaction-only)."
    - "The date-clamp/roll math lives in the node-tested rolodex-logic.ts, not the .tsx, since the .tsx cannot load under vitest."
  prohibitions:
    - "Wheel animation must NOT be driven from React setState per frame — write Reanimated shared .value. (React state for the committed selected-date and drawer visibility IS allowed — only per-frame animation state is prohibited.)"
    - "A helper worklet must NOT be defined below its caller (undefined-on-device Hermes crash)."
    - "The wheels must NOT infer focus on their own — RolodexBrowser owns the useIsFocused + AppState lifecycle and conditionally mounts the animated subtree; a wheel left mounted on blur keeps a live render loop."
    - "An optional Skia glow must NOT hardcode a colour — it reads a theme token prop or is dropped."
    - "Selecting/scrolling a wheel must NOT auto-open the detail sheet — only the drawer's explicit action opens it (HIST-09)."
    - "Reduced Motion must NOT remove date navigation — only simplify depth/inertia (HIST-18)."
    - "No colour literal in any View or Skia draw — tokens only."
---

<objective>
Ship the Rolodex History Browser: three synchronized Month/Day/Year wheels (Day primary) with conventional invalid-date clamping and today-as-max, pre-selection eventful-date markers (silhouette-primary, counts to a11y), and a drawer that summarizes the selected date and offers See details / Log interaction without ever auto-opening the detail sheet (HIST-08, HIST-09, HIST-18).

Purpose: This is the phase's ONLY genuinely animated surface. The date-roll/clamp correctness lives in a node-tested logic module; the render-loop discipline (Reanimated/GH, pause-on-blur, reduced-motion, worklet-forward-ref safety) is copied from the proven Orrery. Keeping navigation gesture-free-capable and markers a11y-exposed satisfies the HIST-18 hard requirement.
Output: RolodexWheel, RolodexBrowser (+ drawer), and the node-tested rolodex-logic (clamp/roll/marker mapping).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Node-tested date roll/clamp + marker-mapping logic</name>
  <files>src/components/history/rolodex-logic.ts, src/components/history/rolodex-logic.test.ts</files>
  <read_first>
    - src/utils/dates.ts (formatLocalDate/localDateTime — the only date helpers; never toISOString)
    - src/services/history/window.ts (Plan 03 — reuse its today-max clamp semantics for consistency)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts -> 3. Rolodex History Browser: clamping, today-max, markers)
  </read_first>
  <behavior>
    - rollDate when Day scrolls past a month boundary advances/retreats Month (and Year at Dec/Jan) correctly.
    - clampDate(Aug 31 -> Feb) yields Feb 28 or 29 (leap-aware); a date after today clamps to today.
    - markerFor(date, markers) returns 'interaction' (filled), 'lifecycle' (ring), or 'multiple' with a count, and the count/types for a11y.
  </behavior>
  <action>Create rolodex-logic.ts: pure functions for wheel date rolling across Month/Day/Year boundaries, conventional invalid-date clamping (leap-aware Feb; month-length overflow), a today-max clamp, and marker classification (interaction/lifecycle/multiple + count) from Plan 03's date-indexed markers. Write tests first (RED): month/year boundary rolls, Feb clamp on a leap and non-leap year, today-max clamp, and marker classification for interaction-only / lifecycle-only / multiple / empty dates.</action>
  <verify>
    <automated>npx vitest run src/components/history/rolodex-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover boundary rolls, leap/non-leap Feb clamps, today-max clamp, and all four marker classifications including empty.
    - Source assertion: `grep -n "toISOString" src/components/history/rolodex-logic.ts` returns nothing.
  </acceptance_criteria>
  <done>The wheel's date math and marker mapping are pure, leap-aware, today-clamped, and fully node-tested.</done>
</task>

<task type="auto">
  <name>Task 2: RolodexWheel + RolodexBrowser (wheels, markers, drawer, reduced motion)</name>
  <files>src/components/history/RolodexWheel.tsx, src/components/history/RolodexBrowser.tsx</files>
  <read_first>
    - src/components/orrery/OrreryCanvas.tsx (:1-52 the proven render-loop discipline: Reanimated + GestureDetector/ComposedGesture, useDerivedValue, pause-by-conditional-mount, tokens-as-props; :23-37 imports)
    - src/theme/use-reduced-motion.ts (:106-118 useReducedMotionShared for worklets; :125-135 useReducedMotion for the React tree)
    - src/components/history/rolodex-logic.ts (from Task 1 — the clamp/roll/marker functions the wheels call)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts -> 3; Motion & Animation Contract; Accessibility Contract)
  </read_first>
  <action>Create RolodexWheel.tsx (one wheel: Reanimated shared-value scroll + gesture-handler pan/inertia; ~2-3 neighbor rows fade/scale with distance; selected row strongest; markers drawn per rolodex-logic; Standard flat roller; identical mechanics across themes). Define ALL helper worklets ABOVE their callers. If the OPTIONAL Galaxy Skia glow is implemented, it MUST read a defined theme token passed as a prop (e.g. reuse `accent`/`starPalette`/an existing glow token — NO colour literal in any Skia draw, check:colors-gated per RESEARCH); if no suitable token exists, DROP the glow rather than hardcode a colour (review Plan-06 MEDIUM). Create RolodexBrowser.tsx composing three synchronized wheels (Month/Day/Year, Day primary) via shared values (never setState per frame). Make the pause-on-blur ownership EXPLICIT (review Plan-06 MEDIUM): RolodexBrowser owns the lifecycle — it consumes `useIsFocused()` and an AppState active/background subscription (or receives `isFocused`/`appActive` as props from HistorySection) and conditionally MOUNTS the wheel subtree only when measured AND focused AND foregrounded; neither wheel infers focus on its own. Provide a non-gesture adjustment path for a11y and useReducedMotionShared simplifying depth/inertia without removing navigation. Add the drawer beneath the wheels: selected-date summary ('{n interactions · m events}' incl. lifecycle) with 'See details' / 'Log interaction' actions exposed as callbacks; scrolling/selecting must NOT auto-open the sheet. All colours via tokens.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -n "useSharedValue\|useAnimatedStyle\|useIsFocused\|AppState" src/components/history/RolodexWheel.tsx src/components/history/RolodexBrowser.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Architectural assertion (replaces the over-broad useState grep, review Plan-06 LOW): gesture updates drive Reanimated SHARED VALUES (useSharedValue/useAnimatedStyle), and the committed selection updates React state only AFTER the wheel settles — React state for selected-date/drawer-visibility is ALLOWED; only per-frame animation state is prohibited. The grep shows shared-value/animated-style usage for the scroll and no shared value written from a setState.
    - Source assertion: RolodexBrowser owns the focus/AppState lifecycle (useIsFocused + AppState, or props) and conditionally mounts the wheel subtree; useReducedMotionShared is consumed in a worklet; helper worklets appear above their callers.
    - Source assertion: any Skia glow reads a theme token prop (check:colors passes); the drawer's actions are callbacks and no code path opens the detail sheet on scroll/select.
    - Behavior (device UAT, phase gate): on the Pixel, wheels roll/clamp/mark correctly, animation pauses on blur/background, reduced motion simplifies without removing navigation, and no undefined-on-device worklet crash occurs.
  </acceptance_criteria>
  <done>The Rolodex Browser renders synchronized wheels with markers and a drawer, owns its pause-on-blur lifecycle explicitly, tokens every Skia glow, is animation-safe and reduced-motion-aware, and never auto-opens the sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture/worklet -> UI thread | A worklet-forward-ref or per-frame setState crashes or janks the JS thread on-device. |
| markers/summary -> pixels | Marker classification and drawer counts must faithfully reflect the read layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-17 | DoS (crash/jank) | wheel worklets | high | mitigate | Reanimated shared values (no per-frame setState); helper worklets above callers; pause on blur/background by conditional mount; Pixel UAT. |
| T-32-18 | Accessibility | gesture/colour-only nav | high | mitigate | Non-gesture adjustment path; markers expose counts/types to a11y; reduced-motion keeps navigation; silhouette-primary markers. |
| T-32-19 | Tampering | marker/count fidelity | medium | mitigate | Marker classification unit-tested from Plan 03 markers; drawer counts include lifecycle per HIST-09. |
</threat_model>

<verification>
- rolodex-logic tests green (rolls, leap clamps, today-max, marker classes).
- tsc + check:colors green; no per-frame setState; worklets above callers.
- On-device wheel/marker/reduced-motion behavior verified at the Pixel phase gate.
</verification>

<success_criteria>
A synchronized Month/Day/Year Rolodex with pre-selection markers, a summary drawer that never auto-opens the sheet, and animation that is worklet-safe, pause-on-blur, and reduced-motion-aware.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/components/history/RolodexWheel.tsx`, `RolodexBrowser.tsx`, `rolodex-logic.ts` (+ test)
- Functions: date roll, leap-aware clamp, today-max clamp, marker classification
- RolodexBrowser focus/AppState lifecycle ownership (conditional mount API)
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[MEDIUM, codex] pause-on-blur not achievable from the proposed boundary** — RolodexBrowser now explicitly owns the useIsFocused + AppState lifecycle (or receives it as props) and conditionally mounts the animated subtree; the wheels never infer focus.
- **[MEDIUM, codex] optional Skia glow has no token contract** — the glow, if built, reads a theme token prop (check:colors-gated) or is dropped; no colour literal in a Skia draw.
- **[LOW, codex] useState grep over-broad** — replaced with a precise architectural assertion (gesture→shared values; committed selection→state after settling); React state for selection/drawer is explicitly allowed.
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-06-SUMMARY.md` when done.
</output>
