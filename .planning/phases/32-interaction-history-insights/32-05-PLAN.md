---
phase: 32-interaction-history-insights
plan: 05
type: execute
wave: 3
depends_on: ["32-01", "32-03"]
files_modified:
  - src/theme/theme-types.ts
  - src/theme/theme-presets.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/components/history/ActivityHeatmap.tsx
  - src/components/history/HeatmapContextCard.tsx
  - src/components/history/IntensityChart.tsx
autonomous: true
requirements: [HIST-02, HIST-03, HIST-04, HIST-05, HIST-06, HIST-07]
estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The theme layer gains heatmapScale (readonly string[] length 5, ascending), heatmapCellEmpty, markerInteraction, markerLifecycle tokens seeded per-palette in theme-presets.ts and typed in theme-types.ts; every consumer reads them via useTheme().colors.* with no colour literal in any View or Skia draw (HIST-02)."
    - "ActivityHeatmap renders static RN View cells (no Skia render loop, no animation) coloured by heatmapScale indexed through the pure heatmapLevel helper; it is count-only (HIST-02)."
    - "A SegmentedControl switches lenses Cycles/7 Days/Month/Year; the chosen lens and cycle-count preset persist globally via app_settings.history_lens/history_cycle_count (not per-contact, not AsyncStorage) and are read back on next open (HIST-03, HIST-04)."
    - "Cycles is the default lens with presets 5/10/15/20 (default 10) arranged 5-per-row newest bottom-right; the current in-progress cycle is distinguished by a borderStrong structural outline/corner marker plus a 'Current cycle' accessibility label — NEVER a second hue (HIST-04)."
    - "7 Days is rolling (today rightmost); Month preserves real weekday geometry with leading/trailing blanks rendered from heatmapCellEmpty/transparent (not a real zero-count fill); Year is a dense daily grid; prev/next navigation works and future dates are blocked (HIST-05)."
    - "Intensity renders over the SAME selected window as the heatmap and re-renders when the lens/period changes; it is neutral (textPrimary figure, textSecondary caption), never a warning hue, with no prediction; it shows unavailable for a no-cadence contact (HIST-06)."
    - "Tapping a heatmap cell opens the small anchored HeatmapContextCard FIRST (populated: date/range + '{n} interactions' + 'See details'; empty: date + '0 interactions' + 'Log interaction') — never the big sheet directly; the card is interaction-count-only and never mentions lifecycle events (HIST-07, D-10)."
    - "Every heatmap cell exposes an accessibility label '{date/range}, {n} interactions' so the grid is usable without colour (HIST-02, HIST-18)."
    - { statement: "The Year-lens dense grid and the 20-cycle grid render and remain tappable on the Pixel; dense sub-44px cells are a11y-labelled to compensate (final density/large-text QA is Phase 40).", verification: backstop }
  artifacts:
    - src/components/history/ActivityHeatmap.tsx
    - src/components/history/HeatmapContextCard.tsx
    - src/components/history/IntensityChart.tsx
    - src/theme/theme-types.ts (heatmap/marker tokens)
  key_links:
    - "ActivityHeatmap/HeatmapContextCard/IntensityChart are presentational, parent-owned-state components that consume Plan 03's window/buckets/intensity-window outputs; they import no DAO and open no sheet directly (they expose onSeeDetails/onLog callbacks)."
    - "Lens/preset persistence rides app-settings-dao reading/writing history_lens/history_cycle_count added by migration 025."
  prohibitions:
    - "The heatmap saturation must NOT be affected by lifecycle or non-interaction records — count-only."
    - "The current cycle must NOT be distinguished by a second hue — structure + a11y label only."
    - "Intensity must NOT borrow a warning hue and must NOT imply more-than-intended contact is bad — it stays neutral."
    - "No component may hardcode a colour value (including in any Skia draw) — every colour resolves through theme tokens (npm run check:colors)."
    - "The context card must NOT mention or count lifecycle events (D-10)."
---

<objective>
Ship the Activity Heatmap (static, count-only, lens-switchable, globally persisted), the Intensity chart over the shared window, the anchored cell context card, and the new per-palette heatmap/marker theme tokens they need. These are the presentational surfaces that turn Plan 03's aggregation into the visible heatmap + intensity halves of the Profile History section (HIST-02..07).

Purpose: The heatmap is where D-10's count-only, lifecycle-excluded, no-second-hue invariants become visible; keeping the components presentational (parent-owned state, callback props) lets the profile integration (Plan 08) mount them without any surface reaching into the DB. Static RN Views (no Skia loop) deliberately sidestep the worklet-forward-ref hazard for the heatmap.
Output: heatmap/marker tokens, ActivityHeatmap, HeatmapContextCard, IntensityChart, and lens/preset persistence through app-settings-dao.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: New per-palette heatmap/marker theme tokens + lens/preset persistence</name>
  <files>src/theme/theme-types.ts, src/theme/theme-presets.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <read_first>
    - src/theme/theme-types.ts (:116 avatarSwatches, :170-190 gravityTiers/starPalette — the readonly string[] seed-token pattern to mirror for heatmapScale)
    - src/theme/theme-presets.ts (:93,102,162,165,221,270 the per-palette seed sites — author heatmapScale/marker entries per palette slot, authored to pass the palette contrast tests like gravityTiers)
    - src/db/app-settings-dao.ts (the CLOSED AppSettings model — review flagged that accessors alone are insufficient: the AppSettings interface (:92+), the SQL row type, the SELECT column list, the writable-key union, and the column map ALL must gain the two new keys, plus value validation like updateAppSettings' bounded-value guards)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (New Tokens Required table — exact token contracts; heatmapScale[0] = real zero-count plate vs heatmapCellEmpty = structural blank)
  </read_first>
  <action>Add heatmapScale (readonly string[] length 5, ascending), heatmapCellEmpty, markerInteraction, markerLifecycle to ThemePalette in theme-types.ts, and seed one authored entry per palette slot in theme-presets.ts (Galaxy luminous ramp, Standard flatter neutral ramp; heatmapScale[0] = the real zero-count plate; heatmapCellEmpty = the structural/out-of-month blank, which must read distinctly from a real zero-count day per the UI-SPEC). For app_settings persistence, thread `historyLens` and `historyCycleCount` through EVERY closed seam of app-settings-dao.ts (addresses review HIGH — accessors alone are insufficient): the AppSettings interface, the SQL row type, the SELECT column list, the writable-key union, and the column map, plus a validator (historyLens ∈ {'cycles','7days','month','year'}; historyCycleCount ∈ {5,10,15,20}) mirroring the existing bounded-value guards. Migration 025 already guarantees the 'cycles'/10 column defaults. Extend app-settings-dao.test.ts to assert the getters return the defaults, both round-trip a written lens/preset, and an out-of-range lens/preset is rejected by the validator.</action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: theme-types.ts declares all four new tokens; theme-presets.ts seeds them for every palette (no missing slot); heatmapScale[0] and heatmapCellEmpty are distinct entries.
    - Source assertion: app-settings-dao.ts adds historyLens/historyCycleCount to the AppSettings interface, SQL row type, SELECT, writable-key union, AND column map (not merely a pair of accessors).
    - `npm run check:colors` passes (no new colour literal introduced outside theme-presets.ts).
    - app-settings-dao.test.ts asserts history_lens defaults 'cycles', history_cycle_count defaults 10, both round-trip, and an invalid lens/preset value is rejected.
  </acceptance_criteria>
  <done>Heatmap/marker tokens (with distinct zero-count vs structural-blank) exist per-palette and pass the colour gate; lens/preset persistence is threaded through every app-settings-dao seam with value validation.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityHeatmap (static, count-only, lens-switchable) + cell a11y</name>
  <files>src/components/history/ActivityHeatmap.tsx</files>
  <read_first>
    - src/components/SegmentedControl.tsx (:1-18 the token-driven static control header + the shipped filled-accent active idiom — reuse for the lens switch)
    - src/components/IntensityLine.tsx (:1-24 the presentational, parent-owned, DB-free, token-only contract to follow)
    - src/services/history/window.ts and buckets.ts (from Plan 03 — the window + heatmapLevel the heatmap consumes)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts -> 1. Activity Heatmap; Color -> current-cycle structural rule; Accessibility Contract)
  </read_first>
  <action>Create ActivityHeatmap.tsx: a presentational component that takes the current lens, cycle-count preset, window and per-cell bucket counts (from Plan 03, supplied by the parent) and renders static RN View cells coloured by heatmapScale via heatmapLevel. Render the lens SegmentedControl (Cycles default) and the cycle-count preset selector (5/10/15/20). Cycles: 5-per-row, newest bottom-right, current cycle marked with a borderStrong structural outline/corner marker + a 'Current cycle' a11y label (never a second hue). 7 Days rolling (today rightmost); Month with true weekday geometry and leading/trailing blanks from heatmapCellEmpty/transparent; Year dense grid. prev/next window controls; future cells not shown/blocked. Every cell carries accessibilityLabel '{date/range}, {n} interactions' and a 44px-floor touch target (dense cells a11y-compensated). Expose onLensChange/onPresetChange/onCellPress callbacks; the parent owns persistence + sheet opening. No Skia, no animation, no DB import.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -n "Skia\|useClock\|toISOString" src/components/history/ActivityHeatmap.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ActivityHeatmap imports no DAO/service DB call and uses no Skia/useClock (the grep returns nothing for Skia/useClock/toISOString).
    - Source assertion: current-cycle emphasis uses border/structure + a 'Current cycle' label, not a second colour token.
    - Behavior: a real zero-count day renders heatmapScale[0] while a structural Month leading/trailing blank renders heatmapCellEmpty/transparent (the two are visually distinct, not conflated) — logic-tested at the cell-classification level (review Plan-05 MEDIUM).
    - Behavior (device UAT, phase gate): switching lenses on the Pixel re-renders the grid and persists the choice; every cell is tappable and a11y-labelled.
  </acceptance_criteria>
  <done>The heatmap renders count-only static cells across all four lenses with a distinct real-zero vs structural-blank treatment, structural current-cycle marking, and full cell a11y.</done>
</task>

<task type="auto">
  <name>Task 3: HeatmapContextCard + IntensityChart over the shared window</name>
  <files>src/components/history/HeatmapContextCard.tsx, src/components/history/IntensityChart.tsx</files>
  <read_first>
    - src/components/IntensityLine.tsx (extend/mirror this for IntensityChart; neutral colour contract — textPrimary figure, textSecondary caption, never a warning hue)
    - src/services/history/intensity-window.ts (from Plan 03 — the intensity result over the shared window)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Copywriting Contract -> context card copy; Surface Contracts -> 2. Intensity, 4. Cell/date context card)
  </read_first>
  <action>Create HeatmapContextCard.tsx: a small anchored card (NOT a sheet) with populated copy (line1 date/range, line2 '{n} interactions', action 'See details') and empty copy (line1 date/range, line2 '0 interactions', action 'Log interaction'). It is interaction-count-only and never mentions lifecycle. Expose onSeeDetails/onLog callbacks (the parent mounts the sheet / routes to logging). Create IntensityChart.tsx: a larger presentational bar/time-series extending IntensityLine, consuming Plan 03's WINDOW-SCOPED intensity-window result (the corrected intensityWindow that filters to the selected window — review HIGH), so changing the lens/period re-renders it over the SAME window as the heatmap; neutral colours only, no prediction; render an unavailable state for a no-cadence contact.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: HeatmapContextCard contains the exact populated/empty copy strings and no lifecycle-event mention.
    - Source assertion: IntensityChart uses only textPrimary/textSecondary for its figure/caption (no warning/status hue) and consumes the window-scoped intensity-window result (not a global computeContactIntensity).
    - Behavior: the context card's action label is 'See details' when count>0 and 'Log interaction' when count===0; the intensity figure changes when the parent supplies a different window (proving it re-renders over the selected window).
  </acceptance_criteria>
  <done>The anchored context card (count-only) and the neutral Intensity chart over the shared, window-scoped window are built as presentational, token-driven components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregation output -> pixels | The heatmap/intensity are the visible encoding of the counts; a mis-index would misreport activity. |
| theme tokens -> Skia/Views | All colour must resolve through tokens to keep the four palettes consistent and accessible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-14 | Tampering | heatmap saturation source | high | mitigate | Saturation indexed only from count-only buckets (Plan 03); context card excludes lifecycle; count exposed to a11y as the primary channel. |
| T-32-15 | Information disclosure | read path | low | mitigate | Purely presentational, parent-supplied local data; no network. |
| T-32-16 | Repudiation/accessibility | colour-only encoding | medium | mitigate | Current-cycle structural (not a hue); every cell a11y-labelled; new tokens authored to pass palette contrast tests. |
</threat_model>

<verification>
- check:colors green (no literals); tsc green.
- app-settings-dao lens/preset persistence tested.
- Heatmap/context-card/intensity behaviors asserted; on-device render/tap deferred to the Pixel phase gate.
</verification>

<success_criteria>
A count-only Activity Heatmap with a persisted lens switch, a neutral Intensity chart over the same window, and an anchored count-only context card exist as presentational, token-driven surfaces.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/components/history/ActivityHeatmap.tsx`, `HeatmapContextCard.tsx`, `IntensityChart.tsx`
- Theme tokens: `heatmapScale`, `heatmapCellEmpty`, `markerInteraction`, `markerLifecycle`
- app-settings-dao seams for `historyLens` / `historyCycleCount` (AppSettings interface, SQL row type, SELECT, writable-key union, column map, validator)
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH, codex] app_settings persistence needs more than accessors** — Task 1 now threads historyLens/historyCycleCount through the AppSettings interface, SQL row type, SELECT, writable-key union, column map, plus a bounded-value validator.
- **[MEDIUM, codex] intensity blocked by Plan 03's wrapper** — resolved upstream (Plan 03 intensity-window is now window-scoped); IntensityChart consumes it and a test proves the figure changes with the window.
- **[MEDIUM, codex] heatmapScale conflates empty-cell vs active-level** — heatmapScale[0] (real zero-count) and heatmapCellEmpty (structural Month blank) are distinct tokens with a cell-classification test.
- **[LOW, claude] heatmapScale AA across four palettes** — the check:colors/palette-contrast gate stays in this plan (not deferred to Phase 40).
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-05-SUMMARY.md` when done.
</output>
