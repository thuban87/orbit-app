---
phase: 32-interaction-history-insights
plan: 04
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/screens/EditInteractionScreen.tsx
  - src/screens/edit-interaction-logic.ts
  - src/screens/edit-interaction-logic.test.ts
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
autonomous: true
requirements: [HIST-12, HIST-14]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A canonical Edit Interaction route exists that can change every editable field: date/time, channel, direction, connected, Tone, note, optional duration, and the Allow-AI toggle (HIST-12, HIST-14)."
    - "The Edit route saves through editTouchpointFull (the sole recency writer) inside one transaction — never a set-based UPDATE interactions — so last_contact and derived consumers recompute on save (D-05, HIST-12)."
    - "A future occurred_at is rejected before the write with the inline copy 'Future dates aren't allowed — pick today or earlier.' (HIST-12)."
    - "A failed save preserves the full form state and never shows completion (mirrors the TouchpointRefineForm/CAPT locked idiom)."
    - "On successful save the route returns to the caller and derived consumers (heatmap buckets, Intensity, Last Interaction, Status, Gravity) reflect the change."
    - "The EditInteraction route is registered in every stack that hosts Edit/Profile (Dashboard, Orrery) so it resolves from the profile context."
  artifacts:
    - src/screens/EditInteractionScreen.tsx
    - src/screens/edit-interaction-logic.ts
  key_links:
    - "EditInteractionScreen wraps the existing TouchpointRefineForm value shape + editTouchpointFull; it builds no SQL and re-implements no widget (mirrors EditContactScreen)."
    - "rejectFutureOccurredAt inside editTouchpointFull is the single future-date guard; the route inherits it, never re-implements it."
  prohibitions:
    - "The Edit route must NOT perform a set-based UPDATE interactions that bypasses editTouchpointFull — that silently breaks recency (D-05)."
    - "The Edit route must NOT re-implement the future-date guard or the recompute — it consumes the spine's guarantees."
    - "duration edited here must NOT feed Status/Gravity/Intensity — descriptive only this milestone."
---

<objective>
Ship the one canonical Edit Interaction route (`src/screens/EditInteractionScreen.tsx`) that wraps the extended `TouchpointRefineForm` and saves through `editTouchpointFull`, changing every editable field (date/time, channel, direction, connected, Tone, note, optional duration, Allow-AI toggle), rejecting future dates, and refreshing derived consumers on save (HIST-12, HIST-14).

Purpose: History needs exactly one edit path so recency, tombstones, and the future-date guard stay at their single chokepoint. Modeling this on `EditContactScreen` keeps correctness in a node-tested logic module and the screen a thin RN shell. Registering the route now (Wave 2) lets the Detail Sheet (Plan 07) and profile integration (Plan 08) simply navigate to it.
Output: EditInteractionScreen + edit-interaction-logic (+ test), route params, and stack registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
@docs/decisions/ADR-024-editable-touchpoint-history-and-recomputed-recency.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edit save/validate logic module (node-tested) wrapping the recency contract</name>
  <files>src/screens/edit-interaction-logic.ts, src/screens/edit-interaction-logic.test.ts</files>
  <read_first>
    - src/db/recency-dao.ts (:258-310 editTouchpointFull — the sole edit writer: rejects future occurred_at before the txn, SETs every editable column incl. the new duration/allow_ai from Plan 01, scopes by id AND contact_id, asserts changes===1, then recomputeLastContact)
    - src/components/touchpoint-refine-logic.ts (the shaping/validation convention to follow; duration/allowAi shaping added in Plan 01)
    - src/screens/edit-contact-logic.ts (the analog logic module for an edit screen — canSave gate + input build + failure preservation)
  </read_first>
  <behavior>
    - buildEditInput(formValue, ids) produces the editTouchpointFull input including duration (nullable) and allow_ai (0/1).
    - a future occurred_at is flagged for the inline rejection before any write is attempted.
    - canSave is false while the form is invalid; a failed save returns a state that preserves the form and does not signal completion.
  </behavior>
  <action>Create edit-interaction-logic.ts: a pure module that maps the TouchpointRefineForm value + interaction id/contact id into the editTouchpointFull input, exposes a canSave gate, detects a future occurred_at for the inline rejection, and models the failed-save-preserves-state / never-completes outcome. Write tests first (RED): future-date rejection, duration/allow_ai carried through, canSave transitions, and a simulated write failure preserving state.</action>
  <verify>
    <automated>npx vitest run src/screens/edit-interaction-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover future-date rejection, duration null and set, allow_ai 0/1, canSave gating, and failure-preserves-state.
    - Source assertion: the module contains no SQL and no direct interactions write — it only shapes the input for editTouchpointFull.
  </acceptance_criteria>
  <done>The edit logic module builds a correct editTouchpointFull input, guards future dates, and models failure preservation, fully node-tested.</done>
</task>

<task type="auto">
  <name>Task 2: EditInteractionScreen route + registration</name>
  <files>src/screens/EditInteractionScreen.tsx, src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx</files>
  <read_first>
    - src/screens/EditContactScreen.tsx (:1-60 the header contract + imports: datetimepicker, useFocusEffect, headerShown:false + own Back, Save via Button role primary — the exact shell to mirror)
    - src/navigation/types.ts (:13 ProfileRouteParams model; :39,51,55,131,135,172 route registration across stacks — add EditInteraction params alongside Edit/Profile)
    - src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx (:57 the `Edit` Stack.Screen registration to mirror for EditInteraction)
    - src/components/TouchpointRefineForm.tsx (the controlled form this screen wraps, extended in Plan 01)
  </read_first>
  <action>Create EditInteractionScreen.tsx: a focused route (headerShown:false, own Back chrome, canonical unsaved-change behavior) that loads the interaction, renders the extended TouchpointRefineForm as a controlled value, and on Save calls editTouchpointFull via the logic module. Primary action is a `Save changes` Button (role primary, accent/onAccent). Show the inline `Future dates aren't allowed — pick today or earlier.` on a future pick; on failure preserve state and never navigate away. Add `EditInteraction: { contactId: number; interactionId: number }` to the param lists (mirroring where `Edit` appears) and register `<Stack.Screen name="EditInteraction" component={EditInteractionScreen} />` in DashboardStack and OrreryStack.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -rn "EditInteraction" src/navigation/types.ts src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: EditInteraction is a typed route param and is registered as a Stack.Screen in both Dashboard and Orrery stacks.
    - Source assertion: `grep -n "UPDATE interactions" src/screens/EditInteractionScreen.tsx` returns nothing (no set-based write) and the screen calls editTouchpointFull.
    - Behavior (device UAT, deferred to phase gate): saving an edited Tone/duration/allow_ai on the Pixel updates the interaction and returns to the caller.
  </acceptance_criteria>
  <done>The canonical Edit Interaction route exists, is registered where Profile is reachable, saves only through the recency spine, and typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit UI -> interactions table | The route is a new mutation entry point; it must route only through the single recency writer. |
| user date input -> stored occurred_at | A future or malformed date must be rejected before it can corrupt recency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Tampering | Edit route write path | high | mitigate | Saves only via editTouchpointFull (scope by id AND contact_id, changes===1, recompute); no set-based UPDATE; source-grep gate. |
| T-32-12 | Input validation | future/malformed date | medium | mitigate | rejectFutureOccurredAt inherited from the DAO + inline copy; logic-module test proves rejection before write. |
| T-32-13 | Tampering | partial write on failure | medium | mitigate | Single-transaction write cannot half-land; failed save preserves form and never completes. |
</threat_model>

<verification>
- edit-interaction-logic tests green (future-date, duration/allow_ai, canSave, failure preservation).
- EditInteraction route registered and typechecks; no set-based write in the screen.
- Device UAT of an edit save deferred to the phase gate (Pixel).
</verification>

<success_criteria>
One canonical Edit Interaction route edits every editable field through the recency spine, rejects future dates, preserves state on failure, and is reachable from the profile context.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/screens/EditInteractionScreen.tsx`, `src/screens/edit-interaction-logic.ts` (+ test)
- Route: `EditInteraction` param (`{ contactId, interactionId }`) + Stack.Screen registration in Dashboard/Orrery stacks
</artifacts_produced>

<output>
Create `.planning/phases/32-interaction-history-insights/32-04-SUMMARY.md` when done.
</output>
