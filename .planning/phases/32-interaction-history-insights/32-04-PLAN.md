---
phase: 32-interaction-history-insights
plan: 04
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/screens/EditInteractionScreen.tsx
  - src/screens/edit-interaction-logic.ts
  - src/screens/edit-interaction-logic.test.ts
  - src/db/interaction-edit-read.ts
  - src/db/interaction-edit-read.test.ts
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
  - src/navigation/tabs/SettingsStack.tsx
autonomous: true
requirements: [HIST-12, HIST-14]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A canonical Edit Interaction route exists that can change every editable field: date/time, channel, direction, connected, Tone, note, optional duration, and the Allow-AI toggle (HIST-12, HIST-14)."
    - "The route loads the full interaction to edit via readInteractionForEdit(contactId, interactionId) (a new ReadOnlyExecutor read scoped by id AND contact_id) which SELECTs note + the new duration/allow_ai columns — so the form is seeded with every editable field, not a partial projection (HIST-12)."
    - "The Edit route saves through editTouchpointFull (the sole recency writer) inside one transaction — never a set-based UPDATE interactions — so last_contact and derived consumers recompute on save (D-05, HIST-12)."
    - "A future occurred_at is rejected before the write reusing the SHIPPED FUTURE_DATETIME_MESSAGE constant from TouchpointRefineForm (single copy source, no divergent string); the UI check is UX-only inline feedback and the DAO's rejectFutureOccurredAt remains the authority (HIST-12)."
    - "A failed save preserves the full form state and never shows completion (mirrors the TouchpointRefineForm/CAPT locked idiom)."
    - "On successful save the route returns to the caller and derived consumers (heatmap buckets, Intensity, Last Interaction, Status, Gravity) reflect the change."
    - "The EditInteraction route is registered in EVERY stack that hosts Profile — Dashboard, Orrery, AND Settings (Profile is hosted in all three; Archived Contacts in Settings opens Profile) — so it resolves from every profile origin; React Navigation throws on an unregistered route name (HIST-12)."
  artifacts:
    - src/screens/EditInteractionScreen.tsx
    - src/screens/edit-interaction-logic.ts
    - src/db/interaction-edit-read.ts
  key_links:
    - "EditInteractionScreen wraps the existing TouchpointRefineForm value shape + editTouchpointFull; it builds no SQL and re-implements no widget (mirrors EditContactScreen)."
    - "readInteractionForEdit depends on migration 025's duration/allow_ai columns (Plan 01) — this is why the read lives in this plan (dep 32-01), NOT in Plan 03, avoiding a Plan-04->Plan-03 wave cascade; it is a distinct single-row-by-id read, not history-read's date-indexed projection."
    - "rejectFutureOccurredAt inside editTouchpointFull is the single future-date guard; the route inherits it, never re-implements it; the UI prevalidation only mirrors it for inline feedback."
  prohibitions:
    - "The Edit route must NOT perform a set-based UPDATE interactions that bypasses editTouchpointFull — that silently breaks recency (D-05)."
    - "The Edit route must NOT re-implement the future-date guard or the recompute as a second source of truth — it consumes the spine's guarantees; the UI check is UX-only and must be tested to AGREE with the DAO guard."
    - "The Edit route must NOT introduce a second future-date rejection string — reuse the shipped FUTURE_DATETIME_MESSAGE constant."
    - "duration edited here must NOT feed Status/Gravity/Intensity — descriptive only this milestone."
---

<objective>
Ship the one canonical Edit Interaction route (`src/screens/EditInteractionScreen.tsx`) that wraps the extended `TouchpointRefineForm` and saves through `editTouchpointFull`, changing every editable field (date/time, channel, direction, connected, Tone, note, optional duration, Allow-AI toggle), rejecting future dates, and refreshing derived consumers on save (HIST-12, HIST-14).

Purpose: History needs exactly one edit path so recency, tombstones, and the future-date guard stay at their single chokepoint. Modeling this on `EditContactScreen` keeps correctness in a node-tested logic module and the screen a thin RN shell. Registering the route now (Wave 2) lets the Detail Sheet (Plan 07) and profile integration (Plan 08) simply navigate to it.
Output: EditInteractionScreen + edit-interaction-logic (+ test), route params, and stack registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
@docs/decisions/ADR-024-editable-touchpoint-history-and-recomputed-recency.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interaction-load read + Edit save/validate logic module (node-tested) wrapping the recency contract</name>
  <files>src/db/interaction-edit-read.ts, src/db/interaction-edit-read.test.ts, src/screens/edit-interaction-logic.ts, src/screens/edit-interaction-logic.test.ts</files>
  <read_first>
    - src/db/recency-dao.ts (:258-310 editTouchpointFull — the sole edit writer: rejects future occurred_at before the txn, SETs every editable column incl. the new duration/allow_ai from Plan 01, scopes by id AND contact_id, asserts changes===1, then recomputeLastContact)
    - src/db/profile-history-read.ts (:38-55 the ?-bound, closed-SELECT, row-shape-typed read style to mirror for the single-row read)
    - src/components/touchpoint-refine-logic.ts (the shaping/validation convention to follow; duration/allowAi shaping added in Plan 01)
    - src/components/TouchpointRefineForm.tsx (:55-57 the shipped FUTURE_DATETIME_MESSAGE constant to REUSE — do not author a second string)
    - src/screens/edit-contact-logic.ts (the analog logic module for an edit screen — canSave gate + input build + failure preservation)
  </read_first>
  <behavior>
    - readInteractionForEdit(exec, contactId, interactionId) returns the full editable interaction (occurred_at, channel, direction, connected, quality, note, duration, allow_ai) scoped by BOTH id AND contact_id, or null if not found.
    - buildEditInput(formValue, ids) produces the editTouchpointFull input including duration (nullable) and allow_ai (0/1).
    - a future occurred_at is flagged for the inline rejection (UX-only, using FUTURE_DATETIME_MESSAGE) before any write; the flag AGREES with the DAO's rejectFutureOccurredAt guard.
    - canSave is false while the form is invalid; a failed save returns a state that preserves the form and does not signal completion.
  </behavior>
  <action>Create src/db/interaction-edit-read.ts: a ReadOnlyExecutor (no transaction) read `readInteractionForEdit(exec, contactId, interactionId)` that SELECTs the full editable interaction row — occurred_at, channel, direction, connected, quality, note, AND the new duration/allow_ai columns — scoped by `id = ? AND contact_id = ?` (?-bound), typed row shape, returning null when absent (addresses review HIGH: Plan 04 had no read to load note+new columns). Create edit-interaction-logic.ts: a pure module that maps the TouchpointRefineForm value + interaction id/contact id into the editTouchpointFull input, exposes a canSave gate, detects a future occurred_at for the inline rejection using the imported FUTURE_DATETIME_MESSAGE (no new string), and models the failed-save-preserves-state / never-completes outcome. Write tests first (RED): readInteractionForEdit returns note+duration+allow_ai and is contact-scoped (wrong contactId → null); future-date rejection; the UI prevalidation flag AGREES with the DAO's rejectFutureOccurredAt on the same input (UX-only, not a second source of truth); duration/allow_ai carried through; canSave transitions; a simulated write failure preserving state.</action>
  <verify>
    <automated>npx vitest run src/db/interaction-edit-read.test.ts src/screens/edit-interaction-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - readInteractionForEdit test proves it returns note + duration + allow_ai, is scoped by id AND contact_id (a mismatched contactId returns null), and opens no write transaction.
    - Tests cover future-date rejection (reusing FUTURE_DATETIME_MESSAGE), the UI-guard-agrees-with-DAO-guard assertion, duration null and set, allow_ai 0/1, canSave gating, and failure-preserves-state.
    - Source assertion: the logic module contains no SQL and no direct interactions write; it imports FUTURE_DATETIME_MESSAGE rather than defining a new string.
  </acceptance_criteria>
  <done>A contact-scoped interaction-load read and the edit logic module exist; the form seeds from every editable field, the future-date UX guard reuses the shipped copy and agrees with the DAO, and failure preservation is modeled — all node-tested.</done>
</task>

<task type="auto">
  <name>Task 2: EditInteractionScreen route + registration in ALL three Profile-hosting stacks</name>
  <files>src/screens/EditInteractionScreen.tsx, src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx, src/navigation/tabs/SettingsStack.tsx</files>
  <read_first>
    - src/screens/EditContactScreen.tsx (:1-60 the header contract + imports: datetimepicker, useFocusEffect, headerShown:false + own Back, Save via Button role primary — the exact shell to mirror)
    - src/navigation/types.ts (:13 ProfileRouteParams; :34-124 DashboardStackParamList, :127-149 OrreryStackParamList, :164-195 SettingsStackParamList — Profile is in ALL THREE; RootStackParamList at :209 is a TYPE intersection, NOT a real navigator, so each stack must register the screen itself)
    - src/navigation/tabs/DashboardStack.tsx (:57 `Edit` registration), src/navigation/tabs/OrreryStack.tsx (:57 `Edit`), src/navigation/tabs/SettingsStack.tsx (:55 `Profile` is hosted here but `Edit` is NOT — the SettingsStack gap the review flagged)
    - src/components/TouchpointRefineForm.tsx (the controlled form this screen wraps, extended in Plan 01; and FUTURE_DATETIME_MESSAGE)
  </read_first>
  <action>Create EditInteractionScreen.tsx: a focused route (headerShown:false, own Back chrome, canonical unsaved-change behavior) that loads the interaction via readInteractionForEdit, renders the extended TouchpointRefineForm as a controlled value, and on Save calls editTouchpointFull via the logic module. Primary action is a `Save changes` Button (role primary, accent/onAccent). Show the inline FUTURE_DATETIME_MESSAGE on a future pick; on failure preserve state and never navigate away. Add `EditInteraction: { contactId: number; interactionId: number }` to ALL THREE Profile-hosting param lists (DashboardStackParamList, OrreryStackParamList, SettingsStackParamList) and register `<Stack.Screen name="EditInteraction" component={EditInteractionScreen} />` in DashboardStack, OrreryStack, AND SettingsStack (addresses review HIGH: a Settings-originated Profile would otherwise navigate to an unregistered route and React Navigation throws).</action>
  <verify>
    <automated>npx tsc --noEmit && grep -rln "EditInteraction" src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx src/navigation/tabs/SettingsStack.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: EditInteraction is a typed route param in all three Profile-hosting param lists AND registered as a Stack.Screen in DashboardStack, OrreryStack, and SettingsStack (the grep lists all three files).
    - Source assertion: `grep -n "UPDATE interactions" src/screens/EditInteractionScreen.tsx` returns nothing (no set-based write) and the screen calls editTouchpointFull.
    - Behavior (device UAT, deferred to phase gate): opening a contact Profile from the Settings tab (Archived → Profile) and editing an interaction resolves the EditInteraction route and saves.
  </acceptance_criteria>
  <done>The canonical Edit Interaction route exists, is registered in every stack that hosts Profile (Dashboard/Orrery/Settings), saves only through the recency spine, and typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit UI -> interactions table | The route is a new mutation entry point; it must route only through the single recency writer. |
| user date input -> stored occurred_at | A future or malformed date must be rejected before it can corrupt recency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Tampering | Edit route write path | high | mitigate | Saves only via editTouchpointFull (scope by id AND contact_id, changes===1, recompute); no set-based UPDATE; source-grep gate. |
| T-32-12 | Input validation | future/malformed date | medium | mitigate | rejectFutureOccurredAt inherited from the DAO + inline copy; logic-module test proves rejection before write. |
| T-32-13 | Tampering | partial write on failure | medium | mitigate | Single-transaction write cannot half-land; failed save preserves form and never completes. |
</threat_model>

<verification>
- interaction-edit-read + edit-interaction-logic tests green (contact-scoped load of note/duration/allow_ai, future-date UX guard agrees with DAO, canSave, failure preservation).
- EditInteraction route registered in Dashboard/Orrery/Settings and typechecks; no set-based write in the screen; future-date copy reuses FUTURE_DATETIME_MESSAGE.
- Device UAT of an edit save (incl. from a Settings-originated Profile) deferred to the phase gate (Pixel).
</verification>

<success_criteria>
One canonical Edit Interaction route edits every editable field through the recency spine, rejects future dates, preserves state on failure, and is reachable from the profile context.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/screens/EditInteractionScreen.tsx`, `src/screens/edit-interaction-logic.ts` (+ test), `src/db/interaction-edit-read.ts` (+ test, exports `readInteractionForEdit`)
- Route: `EditInteraction` param (`{ contactId, interactionId }`) + Stack.Screen registration in Dashboard, Orrery, AND Settings stacks
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] no read to load the full interaction for editing** — added `readInteractionForEdit(contactId, interactionId)` (src/db/interaction-edit-read.ts), a contact-scoped single-row read selecting note + duration + allow_ai; kept in this plan (dep 32-01) to avoid a Plan-04->Plan-03 wave cascade.
- **[HIGH] registration omits Settings** — EditInteraction is now registered in all three Profile-hosting stacks (Dashboard, Orrery, Settings) as a hard acceptance criterion, not a backstop.
- **[MEDIUM, codex] duplicates the future-date guard** — the UI check is explicitly UX-only and tested to AGREE with the DAO's rejectFutureOccurredAt (single authority).
- **[MEDIUM, codex] error copy diverges from the shipped form** — the route reuses the shipped FUTURE_DATETIME_MESSAGE constant rather than the UI-SPEC's authored variant (one copy source).
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-04-SUMMARY.md` when done.
</output>
