---
phase: 32-interaction-history-insights
plan: 07
type: execute
wave: 3
depends_on: ["32-02", "32-03", "32-04"]
files_modified:
  - src/components/history/DateDetailSheet.tsx
  - src/components/history/InteractionDetail.tsx
  - src/components/history/GroupScopePrompt.tsx
  - src/components/history/interaction-detail-logic.ts
  - src/components/history/interaction-detail-logic.test.ts
  - src/components/TimelineRow.tsx
autonomous: true
requirements: [HIST-10, HIST-11, HIST-13, HIST-16, HIST-17]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One shared Detail Sheet (Sheet variant 'detail') interleaves three record families chronologically by semantic icon (not separate sections): editable Interactions, read-only lifecycle events (Archive/Restore/Snooze/Unsnooze/Bind/Unbind), and history-aware knowledge changes editable per their owning model (HIST-10)."
    - "Record families are distinguished by semantic icon + text label, never by colour alone; type/date/metadata are exposed textually to assistive tech (HIST-10, HIST-18)."
    - "Interaction Detail shows channel, date/time, direction, connected, Tone, optional duration, and note with NO blank fields (absent fields omitted entirely) plus Edit and Delete (HIST-11)."
    - "A restrained sparkle (accentText, icon-size sm) appears ONLY when that interaction's allow_ai is ON; nothing is rendered when OFF (HIST-11, D-04)."
    - "Deleting an interaction is a hard delete behind the ConfirmDialog destructive variant (danger fill + warning glyph + onDanger, no scrim-dismiss) whose body names the Status/Gravity/Intensity consequences; delete routes through deleteTouchpoint (tombstone in-txn, recompute) — no trash/quarantine subsystem (HIST-13, D-11)."
    - "A Group Event parent never appears as a second row and never increments any count; a group-linked interaction shows restrained Group Event context (badge, title, separate Group Note distinct from the participant note, View Group Event) (HIST-16)."
    - "Editing a group-linked interaction first presents the scope prompt (Edit individual interaction / Edit Group Event) with no hybrid editor; deleting a group-linked child removes only that participant's interaction via deleteTouchpoint (HIST-17, D-05)."
    - "TimelineRow EVENT_LABELS gains bind/unbind labels so lifecycle rows read correctly (HIST-10)."
    - { statement: "A long interaction note reflows in Interaction Detail (never truncated) while the compact detail-sheet row shows a one-line preview clamp — verify reflow + clamp on-device across large-text settings.", verification: backstop }
    - { statement: "A failed deleteTouchpoint leaves the interaction and its derived metrics intact and re-enables the control with an error surface (no optimistic vanish before commit) — verify the delete-failure path on-device.", verification: backstop }
  artifacts:
    - src/components/history/DateDetailSheet.tsx
    - src/components/history/InteractionDetail.tsx
    - src/components/history/GroupScopePrompt.tsx
  key_links:
    - "Delete consumes deleteTouchpoint (recency-dao.ts:344-351) — the tombstone-writing single-writer path; the UI copies nothing and re-implements no DELETE."
    - "Lifecycle rows read bind/unbind from Plan 02's events + TimelineRow EVENT_LABELS; interaction rows carry the sparkle from Plan 01's allow_ai column."
  prohibitions:
    - "Deletion must NOT be a soft/trash-recoverable operation dressed as a hard delete — it is a true hard delete via deleteTouchpoint (D-11)."
    - "The sparkle must NOT appear when Allow AI is OFF (D-04)."
    - "A Group Note must NOT be transmitted to AI and must NOT get an Allow-AI toggle; it must NOT be concatenated into the participant note (ADR-078, D-04)."
    - "A Group Event parent must NOT render as a second interaction row and must NOT increment any count (HIST-16)."
    - "Group-linked edit must NOT open a hybrid editor — it asks scope first (HIST-17)."
    - "No colour-only distinction between record families and no colour literal in any component (tokens only)."
---

<objective>
Ship the shared period/date Detail Sheet, the canonical Interaction Detail (with the Allow-AI sparkle and group context), the hard-delete confirmation, and the group-linked scope routing (HIST-10, HIST-11, HIST-13, HIST-16, HIST-17). This is the inspect/edit/delete heart of History — where a user reads what happened, opens Edit, or deletes.

Purpose: These surfaces enforce several owner-locked invariants at the UI edge: no blank fields, sparkle-only-when-ON (privacy signal), true hard-delete naming consequences (no trash), single Group-child row (no double-count), and scope-first group editing (no hybrid editor). Delete and edit consume the single recency writer, never a bespoke SQL path.
Output: DateDetailSheet, InteractionDetail, GroupScopePrompt, interaction-detail-logic (+ test), and TimelineRow bind/unbind labels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
@docs/decisions/ADR-010-single-writer-interaction-recency-spine.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interaction Detail presentation logic (no blank fields, sparkle gate, group context)</name>
  <files>src/components/history/interaction-detail-logic.ts, src/components/history/interaction-detail-logic.test.ts</files>
  <read_first>
    - src/db/history-read.ts (Plan 03 — the interaction record shape incl. duration/allow_ai and group linkage)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Copywriting Contract; Surface Contracts -> 6. Interaction Detail; Color -> sparkle uses accentText, shown only when ON)
    - src/db/ai-context-read.ts (:101-122 the note-is-never-transmitted contract — context for the sparkle's meaning)
  </read_first>
  <behavior>
    - buildDetailRows(interaction) omits any absent field (no duration -> no duration row; no Tone -> no Tone row; no note -> no note row).
    - showSparkle(interaction) is true only when allow_ai === 1, false when 0/undefined.
    - group context is present only for a group-linked interaction and exposes badge/title/groupNote/participantNote/viewGroupEvent as distinct fields (group note never merged into the participant note).
  </behavior>
  <action>Create interaction-detail-logic.ts: pure helpers that project an interaction record into the ordered set of present-only detail fields, decide sparkle visibility strictly from allow_ai===1, and shape the group context (badge/title/separate group note/participant note/view-group flag) only for group-linked rows. Write tests first (RED): absent-field omission for duration/Tone/note; sparkle ON only at allow_ai===1; group note kept distinct from participant note; standalone interaction has no group context.</action>
  <verify>
    <automated>npx vitest run src/components/history/interaction-detail-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove no blank fields, sparkle strictly gated on allow_ai===1, and group note separated from participant note.
    - Source assertion: the module derives sparkle only from allow_ai (grep shows no other trigger).
  </acceptance_criteria>
  <done>Interaction-detail projection is pure, blank-field-free, privacy-gated, and group-context-correct, fully tested.</done>
</task>

<task type="auto">
  <name>Task 2: DateDetailSheet + InteractionDetail + TimelineRow bind/unbind labels</name>
  <files>src/components/history/DateDetailSheet.tsx, src/components/history/InteractionDetail.tsx, src/components/TimelineRow.tsx</files>
  <read_first>
    - src/components/ui/Sheet.tsx and src/components/ui/sheet-contract.ts (the 'detail' variant, 60% height — the sheet to reuse)
    - src/components/TimelineRow.tsx (:25-30 EVENT_LABELS to extend with bind/unbind; :47-48 the raw-type fallback; the per-record row renderer to reuse)
    - src/components/icons/icon-registry.ts (semantic icon names — use 'sparkle' for Allow-AI, distinct silhouettes per record family)
    - src/components/history/interaction-detail-logic.ts (Task 1)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts -> 5, 6; Copywriting Contract)
  </read_first>
  <action>Create DateDetailSheet.tsx (Sheet variant 'detail') interleaving the three record families chronologically with semantic icons (not separate sections): interaction rows (channel/type icon, time, direction/Tone/duration where present, one-line note preview -> tap opens InteractionDetail), read-only lifecycle rows (concise state-change summary incl. new Bind/Unbind), and history-aware knowledge-change rows (tap routes to that model's edit flow). May expose 'Log interaction' for backfill; NO multi-select/batch edit. Group-linked children appear as ONE ordinary interaction row (optional compact 'Group event' badge); the parent is never a second row. Create InteractionDetail.tsx from the logic module: present-only fields, the restrained sparkle (accentText, icon-size sm) only when ON, Edit (navigates EditInteraction) and Delete actions, and the group block (badge/title/group note/participant note/View Group Event). Extend TimelineRow EVENT_LABELS with bind/unbind. All colours via tokens; families distinguished by icon + label, never colour.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -n "bind\|unbind" src/components/TimelineRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: TimelineRow EVENT_LABELS includes bind and unbind.
    - Source assertion: InteractionDetail renders the sparkle only under the allow_ai===1 branch and omits absent fields (via the logic module).
    - Source assertion: a group-linked interaction renders exactly one row in DateDetailSheet (no parent row) — verified by the row-mapping using history-read's child-only records.
    - Behavior (device UAT, phase gate): opening a date sheet shows interleaved icons; a group note shows separately from the participant note.
  </acceptance_criteria>
  <done>The shared Detail Sheet and Interaction Detail render correctly with sparkle, group context, lifecycle labels, and single group-child rows.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-delete confirmation + group-linked scope prompt</name>
  <reversibility rating="costly">D-11 locks deletion as hard-delete with no trash subsystem; reversing that (adding a trash/undo tier) later would touch the delete path, the confirmation, and the read/restore layers. The runtime data loss is guarded by an explicit irreversible confirmation, and the design decision is owner-locked — flagged, not gated.</reversibility>
  <files>src/components/history/GroupScopePrompt.tsx, src/components/history/InteractionDetail.tsx, src/components/history/DateDetailSheet.tsx</files>
  <read_first>
    - src/components/ui/ConfirmDialog.tsx (:6-39 the destructive variant contract: danger fill + warning glyph + title-names-action + no scrim-dismiss)
    - src/db/recency-dao.ts (:313-351 deleteTouchpoint/deleteInteractionCore — tombstone in-txn, changes===1, recompute; the delete path the UI consumes)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Copywriting Contract -> Destructive confirmation + Group-linked edit scope prompt; Surface Contracts -> 8 + Group-linked routing)
  </read_first>
  <action>Wire the hard-delete: the Interaction Detail Delete action opens ConfirmDialog (destructive variant) with title 'Delete this interaction?' and body 'This can't be undone and may change this contact's Status, Gravity, and Intensity.', actions Cancel / 'Delete interaction'; on confirm, call deleteTouchpoint (never a bespoke DELETE); on failure keep the interaction and re-enable the control (no optimistic vanish). Create GroupScopePrompt.tsx: for a group-linked interaction, Edit first presents title 'Edit interaction' with options 'Edit individual interaction' (routes to the participant override editor — participant-overridable fields only: Channel, Tone, Duration, Direction, Connected, participant note, Allow AI) and 'Edit Group Event' (routes to the Phase 33 Edit Group Event flow — a placeholder navigation target this phase). Deleting a group-linked child removes only that participant via deleteTouchpoint. Standalone interactions skip the scope prompt and go straight to EditInteraction.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "deleteTouchpoint\|DELETE FROM interactions" src/components/history/InteractionDetail.tsx src/components/history/DateDetailSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: delete calls deleteTouchpoint and the components contain no `DELETE FROM interactions`.
    - Source assertion: the confirmation uses the ConfirmDialog destructive variant with the exact title/body/actions copy.
    - Source assertion: a group-linked Edit renders GroupScopePrompt before any editor; a standalone Edit does not.
    - Behavior (device UAT, phase gate): confirming delete removes the interaction and recomputes recency; a group-child delete leaves siblings untouched.
  </acceptance_criteria>
  <done>Hard-delete runs behind the destructive confirmation through deleteTouchpoint, and group-linked edits ask scope before any editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Delete UI -> interactions + derived metrics | An interaction delete destroys the source rows behind Status/Gravity/Intensity/Last Interaction; it must be explicit, confirmed, and routed through the tombstone-writing single writer. |
| allow_ai flag -> sparkle | The sparkle signals egress consent; it must never appear when OFF. |
| group note -> AI | A Group Note must never be transmitted or toggle-able. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-20 | Tampering/DoS | hard-delete of interaction | high | mitigate | Explicit destructive ConfirmDialog naming consequences; routes through deleteTouchpoint (tombstone in-txn, changes===1, recompute); no bespoke DELETE; failure leaves data intact. |
| T-32-21 | Information disclosure | sparkle / group note | high | mitigate | Sparkle strictly gated on allow_ai===1 (unit-tested); group note is a distinct read-only field with no toggle and no AI path (ADR-078). |
| T-32-22 | Tampering | group double-count | medium | mitigate | Rows come from history-read child-only records; parent never rendered/counted; group-child delete removes only the participant. |
</threat_model>

<verification>
- interaction-detail-logic tests green (no blank fields, sparkle gate, group note separation).
- tsc + check:colors green; delete uses deleteTouchpoint; no DELETE FROM interactions in the components.
- On-device delete/edit-scope/group-context behaviors verified at the Pixel phase gate.
</verification>

<success_criteria>
The shared Detail Sheet and Interaction Detail present complete, blank-field-free, privacy-gated records with correct group context; hard-delete runs behind an explicit confirmation through the recency spine; group edits ask scope first.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/components/history/DateDetailSheet.tsx`, `InteractionDetail.tsx`, `GroupScopePrompt.tsx`, `interaction-detail-logic.ts` (+ test)
- TimelineRow EVENT_LABELS entries: `bind`, `unbind`
</artifacts_produced>

<output>
Create `.planning/phases/32-interaction-history-insights/32-07-SUMMARY.md` when done.
</output>
