---
phase: 32-interaction-history-insights
plan: 07
type: execute
wave: 3
depends_on: ["32-02", "32-03", "32-04"]
files_modified:
  - src/components/history/DateDetailSheet.tsx
  - src/components/history/InteractionDetail.tsx
  - src/components/history/GroupScopePrompt.tsx
  - src/components/history/interaction-detail-logic.ts
  - src/components/history/interaction-detail-logic.test.ts
  - src/components/TimelineRow.tsx
autonomous: true
requirements: [HIST-10, HIST-11, HIST-13, HIST-16, HIST-17]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One shared Detail Sheet (Sheet variant 'detail') interleaves three record families chronologically by semantic icon (not separate sections): editable Interactions, read-only lifecycle events (Archive/Restore/Snooze/Unsnooze/Bind/Unbind), and history-aware knowledge changes (sourced from Plan 03's current_state_entries knowledge-change family) each editable per its owning model via a callback route (HIST-10)."
    - "The knowledge-change family's source is Plan 03's date-indexed knowledge-change records (from current-state-history-read, keyed by fieldKey); its edit-routing map is an `onOpenKnowledgeChange(fieldKey)` callback the parent (HistorySection/Profile) wires to the contact's EXISTING knowledge navigation (the owning model's edit flow) — the sheet defines the source + routing contract, not a new nav gap (HIST-10)."
    - "Record families are distinguished by semantic icon + text label, never by colour alone; type/date/metadata are exposed textually to assistive tech (HIST-10, HIST-18)."
    - "Interaction Detail shows channel, date/time, direction, connected, Tone, optional duration, and note with NO blank fields (absent fields omitted entirely) plus Edit and Delete (HIST-11)."
    - "A restrained sparkle (accentText, icon-size sm) appears ONLY when that interaction's allow_ai is ON; nothing is rendered when OFF (HIST-11, D-04)."
    - "Deleting an interaction is a hard delete behind the ConfirmDialog destructive variant (danger fill + warning glyph + onDanger, no scrim-dismiss) whose body names the Status/Gravity/Intensity consequences; delete routes through deleteTouchpoint (tombstone in-txn, recompute) — no trash/quarantine subsystem; the DAO-level tombstone/recompute/failed-delete-preserves-row behavior is node-tested in recency-dao.test.ts (Plan 01) while this plan proves the UI routes through deleteTouchpoint (HIST-13, D-11)."
    - "Group-linked surfaces are an INERT SEAM in Phase 32 (D-12): GroupScopePrompt, group context (badge/title/Group Note/View Group Event), and scope routing are structurally present but gated on a group-link predicate that resolves hard-FALSE for every Phase-32 interaction (no group_event_id column exists until Phase 33). Because a Group Event parent can never be an `interactions` row, it never appears as a second row and never counts — satisfied structurally, with the group branches never exercised this phase (HIST-16, D-12)."
    - "The standalone (non-group) path is the ONLY exercised edit/delete path in Phase 32: Edit → EditInteraction directly; delete → deleteTouchpoint. The scope-prompt / individual-vs-group branch is dormant seam code ready for Phase 33 (HIST-17, D-05, D-12)."
    - "TimelineRow EVENT_LABELS gains bind/unbind labels so lifecycle rows read correctly (HIST-10)."
    - { statement: "A long interaction note reflows in Interaction Detail (never truncated) while the compact detail-sheet row shows a one-line preview clamp — verify reflow + clamp on-device across large-text settings.", verification: backstop }
    - { statement: "A failed deleteTouchpoint leaves the interaction and its derived metrics intact and re-enables the control with an error surface (no optimistic vanish before commit) — verify the delete-failure path on-device.", verification: backstop }
  artifacts:
    - src/components/history/DateDetailSheet.tsx
    - src/components/history/InteractionDetail.tsx
    - src/components/history/GroupScopePrompt.tsx
  key_links:
    - "Delete consumes deleteTouchpoint (recency-dao.ts:344-351) — the tombstone-writing single-writer path; the UI copies nothing and re-implements no DELETE; the DAO regression lives in recency-dao.test.ts (Plan 01)."
    - "Lifecycle rows read bind/unbind from Plan 02's events + TimelineRow EVENT_LABELS; interaction rows carry the sparkle from Plan 01's allow_ai column; the knowledge-change family comes from Plan 03 (current-state-history) and routes via an onOpenKnowledgeChange callback."
    - "The group-link predicate (isGroupLinked, Plan 03) is hard-false in Phase 32; GroupScopePrompt/group context are dormant seam code that Phase 33's group_event_id will make live — nothing here references that column."
  prohibitions:
    - "Deletion must NOT be a soft/trash-recoverable operation dressed as a hard delete — it is a true hard delete via deleteTouchpoint (D-11)."
    - "The sparkle must NOT appear when Allow AI is OFF (D-04)."
    - "A Group Note must NOT be transmitted to AI and must NOT get an Allow-AI toggle; it must NOT be concatenated into the participant note (ADR-078, D-04)."
    - "The group surfaces must NOT reference, query, or assume a `group_event_id` column and must NOT construct a Group-Event parent+child test fixture — none is representable until Phase 33's migration 026+ (D-12). The group-link predicate stays hard-false; making it 'exercisable' by adding group schema REVERSES D-07/D-12 — STOP and ask the owner."
    - "Group-linked edit must NOT open a hybrid editor — the (dormant) scope prompt asks scope first; in Phase 32 every interaction is standalone so the standalone path is the only one taken (HIST-17)."
    - "No colour-only distinction between record families and no colour literal in any component (tokens only)."
---

<objective>
Ship the shared period/date Detail Sheet, the canonical Interaction Detail (with the Allow-AI sparkle and group context), the hard-delete confirmation, and the group-linked scope routing (HIST-10, HIST-11, HIST-13, HIST-16, HIST-17). This is the inspect/edit/delete heart of History — where a user reads what happened, opens Edit, or deletes.

Purpose: These surfaces enforce several owner-locked invariants at the UI edge: no blank fields, sparkle-only-when-ON (privacy signal), true hard-delete naming consequences (no trash), and the group-linked surfaces as an INERT SEAM (D-12) — GroupScopePrompt/group-context/scope-routing are structurally present but gated on a group-link predicate hard-false in Phase 32, so the standalone edit/delete path is the only one exercised until Phase 33 adds group_event_id. Delete and edit consume the single recency writer, never a bespoke SQL path.
Output: DateDetailSheet (interleaving interactions + lifecycle + knowledge-change rows), InteractionDetail, dormant GroupScopePrompt seam, interaction-detail-logic (+ test), and TimelineRow bind/unbind labels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interaction-history-insights/32-RESEARCH.md
@.planning/phases/32-interaction-history-insights/32-PATTERNS.md
@.planning/phases/32-interaction-history-insights/32-UI-SPEC.md
@CLAUDE.md
@docs/decisions/ADR-010-single-writer-interaction-recency-spine.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interaction Detail presentation logic (no blank fields, sparkle gate, group context)</name>
  <files>src/components/history/interaction-detail-logic.ts, src/components/history/interaction-detail-logic.test.ts</files>
  <read_first>
    - src/db/history-read.ts (Plan 03 — the interaction record shape incl. duration/allow_ai; the `isGroupLinked` INERT-SEAM predicate [hard-false in Phase 32]; the knowledge-change record family + fieldKey)
    - .planning/phases/32-interaction-history-insights/32-CONTEXT.md (D-12 — group behavior is an inert seam; no group_event_id in Phase 32; NEVER add group schema to make a fixture constructible)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Copywriting Contract; Surface Contracts -> 6. Interaction Detail; Color -> sparkle uses accentText, shown only when ON)
    - src/db/ai-context-read.ts (:101-122 the note-is-never-transmitted contract — context for the sparkle's meaning)
  </read_first>
  <behavior>
    - buildDetailRows(interaction) omits any absent field (no duration -> no duration row; no Tone -> no Tone row; no note -> no note row).
    - showSparkle(interaction) is true only when allow_ai === 1, false when 0/undefined.
    - The group-context shaper is present as dormant seam code gated on isGroupLinked; in Phase 32 isGroupLinked is hard-false so EVERY interaction resolves to no group context — the shaper is unit-tested to return group context only when the predicate is true (a synthetic true-predicate input exercises the shape) and none for real Phase-32 rows, WITHOUT constructing a group_event_id-bearing DB fixture.
  </behavior>
  <action>Create interaction-detail-logic.ts: pure helpers that project an interaction record into the ordered set of present-only detail fields, decide sparkle visibility strictly from allow_ai===1, and shape the group context (badge/title/separate group note/participant note/view-group flag) ONLY when the isGroupLinked seam predicate is true. Since isGroupLinked is hard-false in Phase 32 (no group_event_id column, D-12), the group-context path is dormant: unit-test it against a SYNTHETIC in-memory record with the predicate forced true (proving the shape keeps the group note distinct from the participant note when Phase 33 arrives) AND against real Phase-32 records proving no group context is produced — NEVER by adding group schema. Write tests first (RED): absent-field omission for duration/Tone/note; sparkle ON only at allow_ai===1; group-context shape correct for a synthetic group-linked input; standalone (every real Phase-32) interaction has no group context.</action>
  <verify>
    <automated>npx vitest run src/components/history/interaction-detail-logic.test.ts && grep -n "group_event_id" src/components/history/interaction-detail-logic.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove no blank fields, sparkle strictly gated on allow_ai===1, group note separated from participant note (synthetic true-predicate input), and no group context for a real Phase-32 record.
    - Source assertion: the module derives sparkle only from allow_ai (grep shows no other trigger); the group-context branch keys on the isGroupLinked predicate and the module does NOT reference/select a `group_event_id` column (the grep returns nothing — D-12 inert seam).
  </acceptance_criteria>
  <done>Interaction-detail projection is pure, blank-field-free, privacy-gated, and carries a dormant group-context seam that never fires in Phase 32, fully tested.</done>
</task>

<task type="auto">
  <name>Task 2: DateDetailSheet + InteractionDetail + TimelineRow bind/unbind labels</name>
  <files>src/components/history/DateDetailSheet.tsx, src/components/history/InteractionDetail.tsx, src/components/TimelineRow.tsx</files>
  <read_first>
    - src/components/ui/Sheet.tsx and src/components/ui/sheet-contract.ts (the 'detail' variant, 60% height — the sheet to reuse)
    - src/components/TimelineRow.tsx (:25-30 EVENT_LABELS to extend with bind/unbind; :47-48 the raw-type fallback; the per-record row renderer to reuse)
    - src/components/icons/icon-registry.ts (semantic icon names — use 'sparkle' for Allow-AI, distinct silhouettes per record family)
    - src/components/history/interaction-detail-logic.ts (Task 1)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Surface Contracts -> 5, 6; Copywriting Contract)
  </read_first>
  <action>Create DateDetailSheet.tsx (Sheet variant 'detail') interleaving the three record families chronologically with semantic icons (not separate sections): interaction rows (channel/type icon, time, direction/Tone/duration where present, one-line note preview -> tap opens InteractionDetail), read-only lifecycle rows (concise state-change summary incl. new Bind/Unbind), and history-aware knowledge-change rows sourced from Plan 03's knowledge-change family (keyed by fieldKey; tap emits an `onOpenKnowledgeChange(fieldKey)` callback the parent wires to the contact's existing knowledge navigation — the sheet does NOT hardcode a nav target). May expose 'Log interaction' for backfill; NO multi-select/batch edit. Rows come from history-read's `interactions`-only records, so a group-linked child (once Phase 33 exists) is naturally ONE ordinary interaction row and the parent is never an `interactions` row — in Phase 32 every interaction is standalone (inert seam). An optional compact 'Group event' badge is dormant seam markup gated on the isGroupLinked predicate. Create InteractionDetail.tsx from the logic module: present-only fields, the restrained sparkle (accentText, icon-size sm) only when ON, Edit (navigates EditInteraction) and Delete actions, and the dormant group block (badge/title/group note/participant note/View Group Event) gated on isGroupLinked. Extend TimelineRow EVENT_LABELS with bind/unbind. All colours via tokens; families distinguished by icon + label, never colour.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && grep -n "bind\|unbind" src/components/TimelineRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: TimelineRow EVENT_LABELS includes bind and unbind.
    - Source assertion: InteractionDetail renders the sparkle only under the allow_ai===1 branch and omits absent fields (via the logic module).
    - Source assertion: the knowledge-change row taps emit `onOpenKnowledgeChange(fieldKey)` (a callback), not a hardcoded navigate; DateDetailSheet renders interaction rows from history-read's `interactions`-only records (no parent row, no group_event_id reference — grep confirms).
    - Behavior (device UAT, phase gate): opening a date sheet shows interleaved interaction / lifecycle / knowledge-change icons; every Phase-32 interaction row is standalone.
  </acceptance_criteria>
  <done>The shared Detail Sheet and Interaction Detail render the three families with sparkle, lifecycle labels, a knowledge-change routing callback, and a dormant group seam that never fires in Phase 32.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-delete confirmation + group-linked scope prompt</name>
  <reversibility rating="costly">D-11 locks deletion as hard-delete with no trash subsystem; reversing that (adding a trash/undo tier) later would touch the delete path, the confirmation, and the read/restore layers. The runtime data loss is guarded by an explicit irreversible confirmation, and the design decision is owner-locked — flagged, not gated.</reversibility>
  <files>src/components/history/GroupScopePrompt.tsx, src/components/history/InteractionDetail.tsx, src/components/history/DateDetailSheet.tsx</files>
  <read_first>
    - src/components/ui/ConfirmDialog.tsx (:6-39 the destructive variant contract: danger fill + warning glyph + title-names-action + no scrim-dismiss)
    - src/db/recency-dao.ts (:313-351 deleteTouchpoint/deleteInteractionCore — tombstone in-txn, changes===1, recompute; the delete path the UI consumes)
    - .planning/phases/32-interaction-history-insights/32-UI-SPEC.md (Copywriting Contract -> Destructive confirmation + Group-linked edit scope prompt; Surface Contracts -> 8 + Group-linked routing)
  </read_first>
  <action>Wire the hard-delete: the Interaction Detail Delete action opens ConfirmDialog (destructive variant) with title 'Delete this interaction?' and body 'This can't be undone and may change this contact's Status, Gravity, and Intensity.', actions Cancel / 'Delete interaction'; on confirm, call deleteTouchpoint (never a bespoke DELETE); on failure keep the interaction and re-enable the control (no optimistic vanish). The DAO-level delete correctness (tombstone in-txn, recompute, failed-delete-preserves-row for newest AND non-newest) is node-tested in recency-dao.test.ts (Plan 01) — this task proves the UI routes through deleteTouchpoint (grep) and models the failure-preservation state, not the DAO internals (addresses review Plan-07 MEDIUM by pointing the UI at the DAO regression). Create GroupScopePrompt.tsx as DORMANT SEAM CODE (D-12): it presents title 'Edit interaction' with options 'Edit individual interaction' (participant override editor — participant-overridable fields only: Channel, Tone, Duration, Direction, Connected, participant note, Allow AI) and 'Edit Group Event' (routes to the Phase 33 Edit Group Event flow — a placeholder target). Route selection is gated on the isGroupLinked predicate: since it is hard-false for every Phase-32 interaction (no group_event_id column), standalone Edit ALWAYS goes straight to EditInteraction and GroupScopePrompt is never reached this phase. Do NOT construct a group-linked interaction to exercise the prompt (not representable until Phase 33); a light render/unit test may mount GroupScopePrompt directly to verify its option copy/callbacks, but the routing decision must be driven by the predicate.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "deleteTouchpoint\|DELETE FROM interactions\|group_event_id" src/components/history/InteractionDetail.tsx src/components/history/DateDetailSheet.tsx src/components/history/GroupScopePrompt.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: delete calls deleteTouchpoint and the components contain no `DELETE FROM interactions`.
    - Source assertion: the confirmation uses the ConfirmDialog destructive variant with the exact title/body/actions copy.
    - Source assertion: the Edit routing is gated on the isGroupLinked predicate (hard-false in Phase 32 → standalone → EditInteraction directly); GroupScopePrompt is dormant seam code and the components do NOT reference a `group_event_id` column (grep returns nothing — D-12).
    - Behavior (device UAT, phase gate): confirming delete removes the interaction and recomputes recency; a failed delete leaves the interaction intact.
  </acceptance_criteria>
  <done>Hard-delete runs behind the destructive confirmation through deleteTouchpoint (DAO regression in Plan 01), and the group scope prompt is dormant predicate-gated seam code that never fires in Phase 32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Delete UI -> interactions + derived metrics | An interaction delete destroys the source rows behind Status/Gravity/Intensity/Last Interaction; it must be explicit, confirmed, and routed through the tombstone-writing single writer. |
| allow_ai flag -> sparkle | The sparkle signals egress consent; it must never appear when OFF. |
| group note -> AI | A Group Note must never be transmitted or toggle-able. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-20 | Tampering/DoS | hard-delete of interaction | high | mitigate | Explicit destructive ConfirmDialog naming consequences; routes through deleteTouchpoint (tombstone in-txn, changes===1, recompute); no bespoke DELETE; failure leaves data intact. |
| T-32-21 | Information disclosure | sparkle / group note | high | mitigate | Sparkle strictly gated on allow_ai===1 (unit-tested); group note is a distinct read-only field with no toggle and no AI path (ADR-078). |
| T-32-22 | Tampering | group double-count | medium | mitigate | Rows come from history-read's `interactions`-only records; the group-link predicate is hard-false in Phase 32 (no group_event_id column), so no parent row can exist to double-count; group surfaces are dormant seam code (D-12). |
</threat_model>

<verification>
- interaction-detail-logic tests green (no blank fields, sparkle gate, group note separation).
- tsc + check:colors green; delete uses deleteTouchpoint; no DELETE FROM interactions in the components.
- On-device delete/edit-scope/group-context behaviors verified at the Pixel phase gate.
</verification>

<success_criteria>
The shared Detail Sheet and Interaction Detail present complete, blank-field-free, privacy-gated records across three families (interactions, lifecycle, knowledge-change); hard-delete runs behind an explicit confirmation through the recency spine; the group-linked surfaces are dormant predicate-gated seam code that never fires in Phase 32 (no group schema referenced) and is ready for Phase 33.
</success_criteria>

<artifacts_produced>
New symbols/files this plan creates (source-grounding exclusion list):
- Files: `src/components/history/DateDetailSheet.tsx`, `InteractionDetail.tsx`, `GroupScopePrompt.tsx` (dormant seam), `interaction-detail-logic.ts` (+ test)
- Callback contract: `onOpenKnowledgeChange(fieldKey)` (knowledge-change edit routing to the owning model)
- TimelineRow EVENT_LABELS entries: `bind`, `unbind`
</artifacts_produced>

<review_resolutions>
Cross-AI review (32-REVIEWS.md, cycle 1) findings this plan resolves:
- **[HIGH] group context/scope routing prematurely implements Phase 33** — GroupScopePrompt, group context, and scope routing are reframed as DORMANT SEAM code gated on the isGroupLinked predicate (hard-false in Phase 32, no group_event_id). No constructible group fixture; a grep gate forbids referencing group schema (adding it = stop-and-ask, D-07/D-12 reversal).
- **[HIGH, codex] knowledge-change family has no source/routing** — the family is sourced from Plan 03's current-state-history knowledge-change records (fieldKey) and routes via an `onOpenKnowledgeChange(fieldKey)` callback the parent wires to the contact's existing knowledge navigation (source + routing contract defined, not deferred).
- **[MEDIUM, codex] delete has no node test** — the DAO-level tombstone/recompute/failed-delete regression lives in recency-dao.test.ts (Plan 01); this plan proves the UI routes through deleteTouchpoint and models failure preservation.
</review_resolutions>

<output>
Create `.planning/phases/32-interaction-history-insights/32-07-SUMMARY.md` when done.
</output>
