---
phase: 07-conversational-fuel
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/db/fuel-read.ts
  - src/db/fuel-read.test.ts
  - src/components/FuelSearchResultRow.tsx
  - src/screens/FuelSearch.tsx
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [FUEL-05]
must_haves:
  truths:
    - "A user can search across all contacts by name OR fuel text (LIKE ? ESCAPE '\\', ?-bound + metacharacter-escaped) and open a matching contact"
    - "off_limits fuel, unconfirmed source='ai' fuel, and archived contacts never appear in search results (excluded in-query, never a UI filter — search aligns with getRankedFuel)"
    - "The search query and the result row ship as reusable units Phase 8 absorbs into the dashboard search box without a rewrite"
  artifacts:
    - searchFuel in src/db/fuel-read.ts
    - src/components/FuelSearchResultRow.tsx
    - src/screens/FuelSearch.tsx
  key_links:
    - "FuelSearch input → searchFuel(term) (name LIKE ? OR fuel text LIKE ?, ESCAPE '\\'; off_limits + unconfirmed source='ai' + archived excluded) → FuelSearchResultRow → tap → Profile"
---

<objective>
## This plan (slice 4 of 4)

Deliver **cross-contact fuel search** (FUEL-05): a user can search across all contacts by **name AND fuel text** (a metacharacter-escaped `LIKE ? ESCAPE '\'` scan; `off_limits` AND unconfirmed `source='ai'` excluded), and tap a result to open that contact. Per the UI-SPEC Search Surface Decision, Phase 7 ships a **minimal self-contained `FuelSearch` screen** reached from Settings, and builds the **query + result-row as reusable units** so Phase 8 absorbs them into the dashboard search box without a rewrite (INDEX `[dashboard → fuel]`). Phase 8 relocating the box is expected and is NOT a reversal of this decision.

Purpose: closes the "I saved this for someone — who was it?" case that capture-without-thinking produces, and proves the search predicate now (node-tested) rather than leaving it un-built until Phase 8.

Output: `searchFuel` added to `fuel-read.ts` (+tests), `FuelSearchResultRow.tsx`, `FuelSearch.tsx`, a `FuelSearch` route, and a Settings entry.

Depends on 07-01 (fuel-read.ts) and 07-02 (fuel-read.ts serialization). Runs in Wave 3 alongside Plan 03 (no shared files). **No new migration; no FTS5** (dossier Cluster F — a `LIKE` scan is free at this scale; FTS5 is deferred to v2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/dossier/03-fuel.md
@.planning/phases/07-conversational-fuel/07-UI-SPEC.md
@.planning/phases/07-conversational-fuel/07-PATTERNS.md

# Extended in this plan (from Plans 01/02)
@src/db/fuel-read.ts
@src/db/fuel-read.test.ts
# The read-SQL/injection + archived-filter conventions, and the screen/route/entry analogs
@src/db/timeline-read.ts
@src/db/queries.ts
@src/navigation/types.ts
@src/navigation/RootNavigator.tsx
@src/screens/SettingsScreen.tsx
@src/screens/CustomFieldsScreen.tsx
@src/components/Avatar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: searchFuel in fuel-read.ts — name OR fuel text, LIKE, ?-bound, off_limits + archived excluded</name>
  <files>src/db/fuel-read.ts, src/db/fuel-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (the choke-point module — add searchFuel next to getRankedFuel/listFuelForEditor)
    - docs/dossier/03-fuel.md (:352-357 LIKE scan, no FTS5, ASCII-only case folding is EXPECTED not a bug; :383-385 off_limits excluded; archived excluded)
    - src/db/queries.ts / src/db/timeline-read.ts (the ?-bound read conventions; archived_at IS NULL predicate)
    - .planning/phases/07-conversational-fuel/07-UI-SPEC.md (:201-209 FuelSearch state contract; result row shows name + matched snippet)
  </read_first>
  <behavior>
    - searchFuel(exec, term) returns, per matching non-archived contact, { contactId, name, snippet } where a contact matches when its name LIKE %term% OR it has a non-off_limits, non-'ai' fuel row whose text LIKE %term%; snippet is one matching non-off_limits, non-'ai' fuel text (null when the match was on name only). Ordered by name.
    - off_limits fuel NEVER contributes a match and NEVER appears as a snippet, for any term.
    - Unconfirmed source='ai' fuel NEVER contributes a match and NEVER appears as a snippet (Addresses review HIGH-1 — search excludes off_limits AND unconfirmed AI, aligning with getRankedFuel; unconfirmed-AI stays a profile-only concept until Plan 03's confirm flips it to 'manual', after which it becomes searchable). source='manual'/'user'/'share' rows DO match.
    - Archived contacts (archived_at IS NOT NULL) NEVER appear.
    - The term is `?`-bound AND its LIKE metacharacters are escaped (Addresses review HIGH-2): in JS escape `\` → `\\`, then `%` → `\%`, then `_` → `\_` (backslash first), wrap in `%…%`, and bind with `LIKE ? ESCAPE '\'`. `?`-binding stops injection but does NOT make `%`/`_` literal — the ESCAPE clause is what makes a term containing a literal `%` or `_` match only rows actually containing that character. An empty/whitespace term returns [] (guarded before querying).
    - Each contact appears at most once (DISTINCT / grouped).
  </behavior>
  <action>
    Extend src/db/fuel-read.ts with searchFuel(exec, term): a pure read (no transaction, no network). First guard: an empty/whitespace term returns [] before querying. Escape the term's LIKE metacharacters in JS — `\` → `\\`, then `%` → `\%`, then `_` → `\_` (backslash FIRST) — then build like = '%' + escapedTerm + '%' and bind it. SELECT c.id AS contactId, c.name, and a correlated snippet subquery (SELECT f.text FROM fuel f WHERE f.contact_id = c.id AND f.kind != 'off_limits' AND f.source != 'ai' AND f.text LIKE ? ESCAPE '\' LIMIT 1) FROM contacts c WHERE c.archived_at IS NULL AND (c.name LIKE ? ESCAPE '\' OR EXISTS(SELECT 1 FROM fuel f2 WHERE f2.contact_id = c.id AND f2.kind != 'off_limits' AND f2.source != 'ai' AND f2.text LIKE ? ESCAPE '\')) ORDER BY c.name. Every `?` bound to the same escaped `like`; `LIKE ? ESCAPE '\'` is MANDATORY (not optional) on ALL THREE predicates (name, snippet subquery, EXISTS) so a literal `%`/`_` in the term matches only rows containing that character — binding alone does NOT make `%`/`_` literal. off_limits AND source != 'ai' are BOTH excluded in the snippet subquery AND the EXISTS predicate; archived excluded. Export a FuelSearchResult interface { contactId: number; name: string; snippet: string | null }. Add a header/function comment recording the coherence decision (Addresses HIGH-1): search excludes off_limits AND unconfirmed source='ai', matching getRankedFuel (Plan 02) — FUEL-05 mandates only off_limits, so the AI exclusion is a safe superset that keeps unconfirmed-AI a profile-only concept until Plan 03's confirm flips it to 'manual'. Also document: ASCII-only case folding is expected (ICU absent) — do not "fix" it.

    Extend src/db/fuel-read.test.ts: a name-only match returns the contact with snippet null; a fuel-text match returns the contact with the matching snippet; an off_limits row whose text contains the term does NOT match and never appears as a snippet; an UNCONFIRMED source='ai' row whose text contains the term does NOT match and never appears as a snippet (Addresses HIGH-1), while a source='manual'/'user'/'share' row with the SAME text DOES match and can surface as a snippet; an archived contact with a matching name/fuel is excluded; a literal-`%` ESCAPE test — seed a fuel text that literally contains `%` (e.g. "got a 50% raise"), search `50%`, and assert it matches ONLY that row and NOT an unrelated row whose text starts `50` (proving ESCAPE, not a wildcard); a literal-`_` ESCAPE test likewise; each contact appears once even with multiple matching fuel rows.
  </action>
  <verify>
    <automated>npx vitest run src/db/fuel-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - searchFuel matches name OR (non-off_limits, non-'ai') fuel text, excludes off_limits + unconfirmed source='ai' + archived, dedups per contact, `?`-binds AND `ESCAPE '\'`-escapes the term; empty term → [].
    - fuel-read.test.ts green including off_limits-never-matches, unconfirmed-ai-never-matches (manual/user/share DO match), archived-excluded, and the literal-`%`/`_` ESCAPE cases (a term with a literal % matches only rows actually containing %).
    - No interpolated search term anywhere; the query is a static string with bound + escaped params; `LIKE ? ESCAPE '\'` on all three predicates (name, snippet, EXISTS).
  </acceptance_criteria>
  <done>searchFuel matches name OR (non-off_limits, non-'ai') fuel text via a ?-bound, `ESCAPE '\'`-escaped LIKE scan, excludes off_limits + unconfirmed AI + archived, dedups per contact; node-tested incl. off_limits/unconfirmed-ai-never-matches and literal-%/_ ESCAPE cases.</done>
</task>

<task type="auto">
  <name>Task 2: FuelSearch screen + reusable result row + route + Settings entry</name>
  <files>src/components/FuelSearchResultRow.tsx, src/screens/FuelSearch.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - .planning/phases/07-conversational-fuel/07-UI-SPEC.md (:201-209 FuelSearch states; :258-261 copy — title "Search", placeholder "Search names and fuel", idle "Search your contacts and everything you've saved to talk about.", no-results "No matches")
    - src/screens/CustomFieldsScreen.tsx (the list-screen chrome: ScrollView/FlatList root, header + Back + title, themed background)
    - src/screens/SettingsScreen.tsx (:108-136 the row pattern — add a "Search" row like custom-fields-row/archived-row, navigate("FuelSearch"))
    - src/navigation/types.ts (:20-40 RootStackParamList — add FuelSearch: undefined) and src/navigation/RootNavigator.tsx (:53-60 Stack.Screen registrations — add one)
    - src/components/Avatar.tsx (result row avatar) ; src/screens/ContactProfileScreen.tsx (navigation target "Profile", { contactId })
  </read_first>
  <action>
    Create src/components/FuelSearchResultRow.tsx as a REUSABLE presentational row: props { contactId, name, snippet, onPress } → Avatar + contact name (Body 15/400 colors.textPrimary) + the snippet or nothing (13/600 colors.textSecondary, numberOfLines={1} ellipsised); minHeight 44; all colours via useTheme().colors.*. Keep it presentational (no DB, no navigation) so Phase 8 can reuse it under the dashboard search box.

    Create src/screens/FuelSearch.tsx (CustomFieldsScreen chrome): a themed TextInput (surface bg, border, 44px, placeholder "Search names and fuel", autoCorrect={false}) whose text drives searchFuel(getExecutor(), term) into a FlatList of FuelSearchResultRow. Idle (empty term): render the idle copy. No matches: "No matches". Tap a row → navigation.navigate("Profile", { contactId }). Debounce is optional; keep it simple (query on change; guard empty term). Header + Back like CustomFieldsScreen.

    Register the route: add FuelSearch: undefined to RootStackParamList (src/navigation/types.ts) and a <Stack.Screen name="FuelSearch" component={FuelSearch} /> in src/navigation/RootNavigator.tsx. Add a "Search" row to src/screens/SettingsScreen.tsx (mirroring settings-custom-fields-row) that navigates to "FuelSearch". Device-UAT.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/components/FuelSearchResultRow.tsx src/screens/FuelSearch.tsx src/navigation/types.ts src/navigation/RootNavigator.tsx src/screens/SettingsScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/FuelSearchResultRow.tsx` is presentational (no DB/navigation import) and reusable; check:colors green.
    - `src/screens/FuelSearch.tsx` queries searchFuel, renders idle/results/no-matches states, and taps into Profile.
    - FuelSearch route added to RootStackParamList + registered in RootNavigator; a "Search" row in Settings reaches it.
    - tsc + biome clean. On-device UAT (phase gate): search a name and a fuel word both return the contact; an off_limits-only term returns no match; an archived contact never appears.
  </acceptance_criteria>
  <done>A minimal FuelSearch screen (reached from Settings) queries searchFuel and renders reusable FuelSearchResultRow results that tap into Profile; route registered; on-device UAT passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search term → SQL LIKE | untrusted typed term crosses into a LIKE scan over names + fuel text |
| fuel table → search results | off_limits fuel must not surface even via a text match |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering (LIKE / wildcard injection) | searchFuel | medium | mitigate | The term is `?`-bound AND its LIKE metacharacters are JS-escaped (`\`,`%`,`_`) with `LIKE ? ESCAPE '\'` on all three predicates, so `%`/`_` are treated as literal data (binding stops injection but does NOT stop glob mis-match); the query is a static string with no interpolation. ASCII-only casing is an accepted limitation, not a vuln. |
| T-07-02 | Information disclosure | searchFuel (off_limits + unconfirmed ai) | high | mitigate | `kind != 'off_limits' AND source != 'ai'` in BOTH the snippet subquery and the EXISTS predicate; node-test proves an off_limits row AND an unconfirmed source='ai' row never match and never surface as a snippet (search aligns with getRankedFuel's exclusions). |
| T-07-01 | Tampering (SQL injection) | searchFuel | medium | mitigate | No identifier or value interpolated; contacts are also filtered `archived_at IS NULL`. |
</threat_model>

<verification>
- `npx vitest run src/db/fuel-read.test.ts` green (name/text match, off_limits + unconfirmed-ai never match, archived excluded, literal-`%`/`_` ESCAPE).
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean on the touched files.
- `git status` shows no change under src/db/migrations/ (no FTS5 table, no migration).
</verification>

<success_criteria>
A user can search across all contacts by name AND fuel text via a `?`-bound, `ESCAPE '\'`-escaped LIKE scan that excludes off_limits fuel, unconfirmed source='ai' fuel, and archived contacts, and tap a result to open the contact. The query + result row ship as reusable units for Phase 8's dashboard search box.
</success_criteria>

<artifacts>
## Artifacts this plan produces

**New files**
- `src/components/FuelSearchResultRow.tsx` — reusable presentational result row (Avatar + name + snippet).
- `src/screens/FuelSearch.tsx` — minimal search screen (input + FlatList of results + idle/no-match states).

**Modified files**
- `src/db/fuel-read.ts` — add searchFuel + FuelSearchResult (name OR fuel text, off_limits + unconfirmed source='ai' + archived excluded, ?-bound + `LIKE ? ESCAPE '\'`-escaped).
- `src/db/fuel-read.test.ts` — search predicate proof.
- `src/navigation/types.ts` — add FuelSearch: undefined to RootStackParamList.
- `src/navigation/RootNavigator.tsx` — register the FuelSearch screen.
- `src/screens/SettingsScreen.tsx` — add a "Search" entry row.

**No schema change** — no FTS5 table, no migration.
</artifacts>

<output>
Create `.planning/phases/07-conversational-fuel/07-04-SUMMARY.md` when done.
</output>
