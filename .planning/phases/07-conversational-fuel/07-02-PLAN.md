---
phase: 07-conversational-fuel
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/services/fuel-ranking.ts
  - src/services/fuel-ranking.test.ts
  - src/services/fuel-age.ts
  - src/services/fuel-age.test.ts
  - src/db/fuel-read.ts
  - src/db/fuel-read.test.ts
  - src/components/RankedFuelLine.tsx
  - src/components/FuelEditor.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [FUEL-02, FUEL-03, FUEL-04, FUEL-06]
must_haves:
  truths:
    - "The profile shows one promoted ranked line = the top item by kind priority (recent > gift > topic > fact) then recency"
    - "off_limits items, unconfirmed source='ai' items, AND blank/NULL-text items never appear in the ranked projection (excluded by SQL WHERE, not the UI) — so the promoted line is always a usable non-empty string"
    - "Each editor row shows fuel age as 'today / N days / N months / N years ago'; age drives ranking recency but never hides or deletes a row"
  artifacts:
    - src/services/fuel-ranking.ts
    - src/services/fuel-age.ts
    - src/components/RankedFuelLine.tsx
  key_links:
    - "getRankedFuel CASE derived from the single FUEL_KIND_PRIORITY tunable; a parity test asserts SQL order == compareFuel order on one fixture"
    - "ContactProfileScreen.load() → getRankedFuel → RankedFuelLine strip (rows[0].text, surface-agnostic plain text)"
---

<objective>
## This plan (slice 2 of 4)

Deliver the **single ranked projection** and **fuel age**. After this plan a user sees, above the editor, one promoted line — the top item by kind priority then recency — and each editor row shows how old the item is. This is FUEL-03 (the one projection card/notification/widget will all reuse) and FUEL-04 (age renders + drives ranking, destroys nothing). It also completes the structural half of FUEL-02 and FUEL-06: the ranked read excludes `off_limits` AND unconfirmed `source='ai'` by a SQL predicate — the load-bearing guarantee that private/unverified notes never reach a glanceable surface.

Purpose: the ranking rule and the off_limits/ai exclusions are the phase's correctness core; they are pure/SQL and fully node-tested (ranking parity, exclusion across every kind-population combo, age boundaries).

Output: `fuel-ranking.ts` (+test), `fuel-age.ts` (+test), `getRankedFuel` added to `fuel-read.ts` (+tests), `RankedFuelLine.tsx`, and the age render + ranked strip on the profile.

Depends on 07-01 (extends `fuel-read.ts`, `FuelEditor.tsx`, `ContactProfileScreen.tsx`; imports FuelItem/FuelKind). **No new migration.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/dossier/03-fuel.md
@.planning/phases/07-conversational-fuel/07-UI-SPEC.md
@.planning/phases/07-conversational-fuel/07-PATTERNS.md

# The pure-logic + tunable + local-date-parse analog
@src/services/gravity-logic.ts
@src/db/__testkit__/node-sqlite.ts

# Extended in this plan (from Plan 01)
@src/db/fuel-read.ts
@src/db/fuel-dao.ts
@src/components/FuelEditor.tsx
@src/screens/ContactProfileScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fuel-ranking.ts + fuel-age.ts — the pure kind-priority comparator and the age formatter</name>
  <files>src/services/fuel-ranking.ts, src/services/fuel-ranking.test.ts, src/services/fuel-age.ts, src/services/fuel-age.test.ts</files>
  <read_first>
    - src/services/gravity-logic.ts (the pure-module posture: no react-native/Skia/expo/DB import, tunables top-of-file, node-tested; parseLocalMs :82-98 — copy the LOCAL-components timestamp parse, never toISOString/UTC)
    - docs/dossier/03-fuel.md (:240-251 the ranking rule: recent > gift > topic > fact, then recency; :199-209 age displays + ranks, nothing destroyed/hidden by age)
    - .planning/phases/07-conversational-fuel/07-UI-SPEC.md (:253 age copy: today · N days ago · N months ago · N years ago)
  </read_first>
  <behavior>
    - FUEL_KIND_PRIORITY = ['recent','gift','topic','fact'] (top-of-file tunable; off_limits is not ranked — it is excluded in-query, so it maps to the ELSE / last bucket).
    - compareFuel(a, b) orders by kind priority FIRST (recent=0 < gift=1 < topic=2 < fact=3), then created_at DESC (newer first), then id DESC. A `recent` from last week outranks a `fact` from yesterday (the explicitly-rejected "newest regardless of kind" must NOT happen).
    - fuel age: given a stored created_at (YYYY-MM-DD HH:MM:SS local wall-clock) and a `now`, returns "today" for same-day, else "N days ago" (< ~30d), "N months ago" (< 12mo), "N years ago" (>= 12mo). Parsed with LOCAL components (parseLocalMs), never toISOString. Pure; no DELETE/UPDATE anywhere (age never mutates data).
  </behavior>
  <action>
    Create src/services/fuel-ranking.ts as a PURE module (no react-native/DB import), mirroring gravity-logic.ts. Export the tunable FUEL_KIND_PRIORITY (the 4 rankable kinds, order = precedence) and compareFuel(a, b) generic over a structural shape { kind: string; created_at: string; id: number } — do NOT import FuelKind from the DB layer (keeps it pure and reusable by the SQL parity test). Rank a kind by its index in FUEL_KIND_PRIORITY, defaulting to FUEL_KIND_PRIORITY.length for any kind not listed (off_limits / unknown); tie-break created_at DESC then id DESC. Add a header noting the SQL CASE in fuel-read.ts (Task 2) is DERIVED FROM this same constant and a parity test asserts they agree (RESEARCH Pitfall 3).

    Create src/services/fuel-age.ts as a PURE module: copy gravity-logic's parseLocalMs, export formatFuelAge(createdAt: string, now: string): string returning the copy above. Compute day/month/year deltas from the parsed local components; guard a clock-skew future createdAt to "today" (never negative). Add a header: age is DISPLAY + RANK only — nothing is ever hidden, archived, or destroyed by age (dossier Cluster C); there is no launch sweep and no age-keyed DELETE/UPDATE.

    Create both sibling .test.ts files under node:sqlite/vitest. fuel-ranking.test.ts: cross-kind date inversions (a week-old recent beats a day-old fact; a gift beats a topic of the same day; within a kind newest-first; id tie-break). fuel-age.test.ts: today boundary, N days, month boundary (~30d → "1 month ago"), year boundary (>=12mo → "N years ago"), and a future createdAt → "today" (no negative), all with fixed `now` strings (local, never UTC).
  </action>
  <verify>
    <automated>npx vitest run src/services/fuel-ranking.test.ts src/services/fuel-age.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/fuel-ranking.ts` exports FUEL_KIND_PRIORITY and compareFuel; imports nothing from src/db or react-native (pure).
    - `src/services/fuel-age.ts` exports formatFuelAge and parses with local components (no toISOString anywhere); contains no DELETE/UPDATE/sweep.
    - Both test files green: ranking honours kind-priority-then-recency with cross-kind date inversions; age boundaries and future-guard hold.
  </acceptance_criteria>
  <done>fuel-ranking.ts (FUEL_KIND_PRIORITY + compareFuel) and fuel-age.ts (formatFuelAge, local parse) exist as pure node-tested modules with no DB/RN import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getRankedFuel in fuel-read.ts — off_limits AND source='ai' excluded in-query, CASE from the tunable</name>
  <files>src/db/fuel-read.ts, src/db/fuel-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (the choke-point module from Plan 01 — extend it, keep listFuelForEditor as the ONLY off_limits-surfacing read)
    - src/services/fuel-ranking.ts (FUEL_KIND_PRIORITY — build the SQL CASE from it; compareFuel — for the parity test)
    - docs/dossier/03-fuel.md (:242 off_limits excluded in the query not the UI; :322-330 unconfirmed source='ai' excluded from prompt-facing reads until confirmed)
    - .planning/phases/07-conversational-fuel/07-UI-SPEC.md (RankedFuelLine note :186-190 — the glanceable projection excludes unconfirmed source='ai' as well as off_limits)
  </read_first>
  <behavior>
    - getRankedFuel(exec, contactId) returns the contact's fuel EXCLUDING kind='off_limits', EXCLUDING source='ai', AND EXCLUDING blank/NULL text (NULLIF(TRIM(text),'') IS NOT NULL), ordered by kind priority (CASE) then created_at DESC then id DESC — so rows[0] is always a USABLE non-empty single glanceable/prompt-facing ranked line.
    - A high-priority row with NULL/blank/whitespace-only text is NOT eligible for the ranked line (fuel.text is NULLable — 001-initial.ts:173-176 — and Plan 01 stores it NULL when blank; without this predicate it would top the ranking and render an empty promoted strip). The next eligible non-blank row becomes rows[0]. (Addresses review MEDIUM-3. The editor's listFuelForEditor still returns ALL rows — a user may be mid-adding; this predicate is on the glanceable projection ONLY.)
    - off_limits rows NEVER appear, for every kind-population combination.
    - source='ai' rows NEVER appear (an ai row is unconfirmed by definition — Plan 03's confirm flips it to 'manual', after which it ranks normally); source='manual'/'user'/'share' all rank.
    - The SQL ordering equals compareFuel's ordering on the same fixture (parity).
  </behavior>
  <action>
    Extend src/db/fuel-read.ts with getRankedFuel(exec, contactId): a pure read (no transaction) SELECTing the same 8 FuelItem columns FROM fuel WHERE contact_id = ? AND kind != 'off_limits' AND source != 'ai' AND NULLIF(TRIM(text), '') IS NOT NULL, ORDER BY <RANK_CASE>, created_at DESC, id DESC. The `NULLIF(TRIM(text),'') IS NOT NULL` predicate keeps a blank/NULL/whitespace-only row from becoming rows[0] and blanking the promoted strip (MEDIUM-3); it is a projection-only predicate — listFuelForEditor still surfaces all rows. Build RANK_CASE once at module load FROM FUEL_KIND_PRIORITY (imported from fuel-ranking.ts): map each kind to its index and append an ELSE = FUEL_KIND_PRIORITY.length. These interpolated values are compile-time CODE CONSTANTS from a closed array — never user input — so there is no injection surface; contact_id remains the sole `?`-bound value. Document this in the function header (the exclusion predicate is the access control; a UI-side .filter() would be the vulnerability). Keep off_limits/'ai' string literals in the SQL as fixed predicates.

    Extend src/db/fuel-read.test.ts: (a) an off_limits-absence sweep — for each combination of populated kinds, assert no returned row has kind 'off_limits'; (b) an ai-exclusion case — a source='ai' row is absent, a source='manual' (confirmed) row is present and ranks; (c) the exact precedence (a week-old recent outranks a day-old fact); (d) a BLANK-TEXT exclusion test — a newest, highest-priority row with NULL/blank text does NOT become rows[0]; the next eligible non-blank row is the ranked line (Addresses MEDIUM-3); (e) a PARITY test over an ELIGIBLE fixture ONLY (Addresses MEDIUM-5): build a mixed fixture, derive `eligibleFixture` by removing off_limits + source='ai' + blank/whitespace-text rows in test setup, sort a copy of eligibleFixture with compareFuel, and assert its id order equals getRankedFuel's output id order — compare the SAME row set on both sides (never the full mixed fixture, whose different length would false-fail). Keep (a)/(b)/(d) as the separate SQL-only exclusion tests.
  </action>
  <verify>
    <automated>npx vitest run src/db/fuel-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - getRankedFuel excludes off_limits, source='ai', AND blank/NULL/whitespace text (NULLIF(TRIM(text),'') IS NOT NULL) via SQL WHERE (verified by the absence sweep + ai case + blank-text test); listFuelForEditor still surfaces off_limits and blank rows (unchanged).
    - The RANK_CASE is built from FUEL_KIND_PRIORITY; the parity test compares getRankedFuel against compareFuel over an ELIGIBLE-only fixture (off_limits/ai/blank removed in setup) and proves SQL order == compareFuel order; exclusion sweeps stay as separate tests.
    - contact_id is the only `?`-bound value; the CASE contains only closed-set code-constant literals.
    - fuel-read.test.ts green.
  </acceptance_criteria>
  <done>getRankedFuel excludes off_limits, source='ai', and blank/NULL text by SQL predicate with the CASE built from FUEL_KIND_PRIORITY; a parity test over an eligible-only fixture proves SQL order == compareFuel order.</done>
</task>

<task type="auto">
  <name>Task 3: RankedFuelLine + age render — the promoted strip and per-row age on the profile</name>
  <files>src/components/RankedFuelLine.tsx, src/components/FuelEditor.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - .planning/phases/07-conversational-fuel/07-UI-SPEC.md (RankedFuelLine contract :179-190 — one line, numberOfLines=1, tail ellipsis, Body 15/400 textPrimary on a surface/borderStrong strip, no kind prefix, surface-agnostic plain text; Fuel age :192-199 — 13/600 textSecondary, right-aligned in the row header)
    - src/screens/ContactProfileScreen.tsx (load() Promise.all + the mutation-then-load convention; the impact strip pattern :430-439 as the model for a promoted strip)
    - src/components/FuelEditor.tsx (Plan 01 — add the age element to each row header)
    - src/services/fuel-age.ts (formatFuelAge)
  </read_first>
  <action>
    Create src/components/RankedFuelLine.tsx: a presentational strip taking a single plain-text string (the ranked top line) and rendering it numberOfLines={1} with tail ellipsis, Body (15/400) colors.textPrimary on a colors.surface card with a colors.borderStrong outline — NO kind prefix (kind drove ranking, not display), keep the string clean and glance-legible. Render nothing when the string is empty/absent. All colours via useTheme().colors.* (no net-new token). Keep it surface-agnostic so the same string is reusable by Phase-8 card / Phase-11 notification / Phase-12 widget.

    Extend src/components/FuelEditor.tsx: render formatFuelAge(row.created_at, now) in each existing persisted row header (13/600, colors.textSecondary, right-aligned); omit it on the transient draft row (no created_at yet — Plan 01's draft/commit boundary).

    Extend src/screens/ContactProfileScreen.tsx: add getRankedFuel into the load() Promise.all and setRankedFuel state; mount <RankedFuelLine> above the FuelEditor showing rankedFuel[0]?.text (nothing when there is no rankable fuel; rankedFuel[0] is guaranteed non-blank by getRankedFuel's text predicate, so the strip never renders empty over a real row). It refreshes with every fuel mutation via the existing `await load()` (no second ranking computed in the UI — UI-SPEC interaction contract #9). Device-UAT.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx biome check src/components/RankedFuelLine.tsx src/components/FuelEditor.tsx src/screens/ContactProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/RankedFuelLine.tsx` renders one ellipsised plain-text line on a surface/borderStrong strip; empty input → renders nothing; check:colors green.
    - FuelEditor rows show fuel age (formatFuelAge), textSecondary, right-aligned; new unsaved rows omit age.
    - ContactProfileScreen.load() reads getRankedFuel and mounts RankedFuelLine above the editor; refreshes on every mutation.
    - tsc + biome clean. On-device UAT (phase gate): the promoted line shows the highest-priority item; adding a newer `recent` changes it; an off_limits item never becomes the line.
  </acceptance_criteria>
  <done>RankedFuelLine strip renders rows[0].text above the editor; each editor row shows fuel age; tsc/biome/check:colors green; on-device UAT passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuel table → glanceable surface | the ranked projection feeds card/notification/widget/AI; off_limits + unconfirmed AI must not cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Information disclosure | getRankedFuel (off_limits) | high | mitigate | `WHERE kind != 'off_limits'` in the shared read; node-test proves an off_limits row is absent from the ranked projection for every kind-population combo. The visual "private" marker is reinforcement only. |
| T-07-03 | Information disclosure / Repudiation | getRankedFuel (source='ai') | high | mitigate | `WHERE source != 'ai'` excludes unconfirmed AI proposals from the glanceable/prompt-facing read, so a hallucination never lands on a glance line as fact or feeds back into a prompt (Plan 03 confirms). |
| T-07-01 | Tampering (SQL injection) | RANK_CASE construction | medium | mitigate | The CASE is built only from FUEL_KIND_PRIORITY (a closed hardcoded array), never user input; contact_id is the sole bound value — no injection surface. |
</threat_model>

<verification>
- `npx vitest run src/services/fuel-ranking.test.ts src/services/fuel-age.test.ts src/db/fuel-read.test.ts` green (ranking parity, exclusion sweeps, age boundaries).
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean on the touched files.
- `git status` shows no change under src/db/migrations/.
</verification>

<success_criteria>
One ranked projection (kind priority then recency), built once and reused, excludes off_limits and unconfirmed source='ai' by SQL predicate; its top line renders as a promoted strip on the profile. Each editor row shows a human age that drives ranking recency and destroys nothing. The SQL order provably equals the pure comparator.
</success_criteria>

<artifacts>
## Artifacts this plan produces

**New files**
- `src/services/fuel-ranking.ts` — const FUEL_KIND_PRIORITY (tunable); function compareFuel.
- `src/services/fuel-ranking.test.ts` — precedence + tie-break proof.
- `src/services/fuel-age.ts` — function formatFuelAge; local parseLocalMs.
- `src/services/fuel-age.test.ts` — age boundary + future-guard proof.
- `src/components/RankedFuelLine.tsx` — surface-agnostic promoted-line strip.

**Modified files**
- `src/db/fuel-read.ts` — add getRankedFuel (off_limits + source='ai' + blank/NULL-text excluded; RANK_CASE from FUEL_KIND_PRIORITY).
- `src/db/fuel-read.test.ts` — exclusion sweeps (off_limits/ai/blank-text) + eligible-fixture ranking parity.
- `src/components/FuelEditor.tsx` — per-row age render.
- `src/screens/ContactProfileScreen.tsx` — getRankedFuel in load(); RankedFuelLine strip mounted.

**No schema change** — no migration created or edited.
</artifacts>

<output>
Create `.planning/phases/07-conversational-fuel/07-02-SUMMARY.md` when done.
</output>
