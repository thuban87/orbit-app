---
phase: 08-dashboard-never-contacted-screen
plan: 06
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/components/BirthdayBanner.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [DASH-05, DASH-06]
must_haves:
  truths:
    - "BirthdayBanner shows upcoming birthdays within a 7-day window, soonest first, tap→profile; it iterates the deliberate predicate (exclude archived ONLY — overrides snooze + never-contacted suppression) and uses the single daysUntilBirthday parser."
    - "The banner renders nothing when no contact has a birthday in the window; it reads offline via an async cancelled-flag guard."
    - "ContactProfileScreen gains a favourite star that toggles favourite_rank via setFavouriteRank/clearFavouriteRank — reversible, non-destructive, no confirmation dialog."
  artifacts:
    - src/components/BirthdayBanner.tsx
  key_links:
    - "BirthdayBanner consumes listBirthdayCandidates (Plan 01) + daysUntilBirthday (Plan 02); the profile star consumes favourites-dao (Plan 03)."
    - "The banner's exclude-archived-only predicate is a SCOPED EXCEPTION to snooze/never-contacted suppression, not a reversal (dossier Cluster E)."
---

<objective>
Deliver the two Wave-2 additive surfaces that consume the Wave-1 cores: the `BirthdayBanner` component (7-day window, soonest-first, exclude-archived-only, using the fixed parser) and the profile favourite star (marks/clears `favourite_rank`). The banner is MOUNTED into the dashboard in Plan 07; the star lives on the existing profile screen.

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

After this plan: a user can mark any contact a favourite from its profile, and the birthday banner component exists (ready to mount).
Output: `BirthdayBanner.tsx`; a favourite star on `ContactProfileScreen.tsx`.

FLAGGED (owner's bucket): OD-2 favourite marker/star — provisional `accent`; the owner may substitute a dedicated favourite token and glyph. The birthday banner's "exclude archived only" predicate is a decided scoped exception (not a reversal).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md
@.planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md
@src/screens/ContactProfileScreen.tsx
@src/screens/FuelSearch.tsx
@src/db/dashboard-read.ts
@src/logic/birthday-logic.ts
@src/db/favourites-dao.ts
@src/db/contact-read.ts
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan: `src/components/BirthdayBanner.tsx` → `BirthdayBanner`; extends `ContactProfileScreen.tsx` with a favourite star.)

<tasks>

<task type="auto">
  <name>Task 1: BirthdayBanner.tsx — 7-day window, soonest-first, exclude-archived-only, tap→profile</name>
  <files>src/components/BirthdayBanner.tsx</files>
  <read_first>
    - src/db/dashboard-read.ts — `listBirthdayCandidates(exec)` (archived_at IS NULL AND birthday IS NOT NULL) → { id, name, birthday }.
    - src/logic/birthday-logic.ts — `daysUntilBirthday(stored, today)` (both bugs fixed; returns 0..N or null).
    - src/screens/FuelSearch.tsx (lines 50-64) — the cancelled-flag async load to reuse.
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Copywriting Contract (birthday banner: "{Name}'s birthday {today / in N days}", soonest first, 7-day) + § Accessibility (`dashboard-birthday-banner`, `dashboard-birthday-item-{contactId}`) + § Interaction States (banner item tap → Profile).
    - .planning/phases/08-dashboard-never-contacted-screen/08-RESEARCH.md § Pattern 3 (banner predicate: exclude archived only; overrides snooze/never-contacted suppression) + dossier Cluster E.
  </read_first>
  <acceptance_criteria>
    - Reads via `listBirthdayCandidates(getExecutor())` inside a cancelled-flag async effect; async only; renders offline.
    - Filters to `daysUntilBirthday(birthday, new Date()) !== null && <= 7`, sorts ascending by days-until (soonest first); renders nothing (null) when the filtered list is empty.
    - Each item carries `dashboard-birthday-item-{contactId}`; the row carries `dashboard-birthday-banner` at the container; tapping an item calls the caller-supplied navigate → `Profile { contactId }` (component takes an `onOpenContact` prop OR uses navigation — keep it presentational-friendly: accept `onPressContact(contactId)`).
    - Copy reads "{Name}'s birthday today" when daysUntil === 0 (the day-of case is reachable thanks to the fixed parser) and "in N days" otherwise.
    - Every colour via `useTheme().colors.*`; `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <action>
    Create `BirthdayBanner`. It loads `listBirthdayCandidates` (exclude-archived-only — the read already does this; do NOT re-filter to the dashboard population, so a snoozed or never-contacted person's birthday still shows) with a cancelled-flag async guard, then in JS computes `daysUntilBirthday(candidate.birthday, new Date())`, keeps 0..7, sorts soonest-first. Render a compact banner (RN View/Text/Pressable rows) using the FuelSearchResultRow row idiom and only theme tokens; each row shows "{Name}'s birthday today"/"in N days" and taps to the profile via an `onPressContact(contactId)` callback (the dashboard, Plan 07, wires it to navigation). Return null when empty. Carry the LOCKED testIDs. Add a header comment stating the exclude-archived-only scoped exception (overrides snooze/never-contacted suppression — decided, not a reversal).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>BirthdayBanner renders the 7-day soonest-first list from the fixed parser, honours the exclude-archived-only predicate, renders nothing when empty, and taps to the profile.</done>
</task>

<task type="auto">
  <name>Task 2: ContactProfileScreen favourite star — toggle favourite_rank</name>
  <files>src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx — the header block (lines 460-496: back, Avatar, name, OverflowMenu) where the star belongs; the `load` useCallback (lines 156-188) and `getContactHeader` usage; the existing `localDateTime()` import from db/database.
    - src/db/contact-read.ts (lines 65-94) — `getContactHeader` returns id/name/rarely_responds/archived_at/photo/modified_at but NOT favourite_rank; the star needs the contact's current favourite state.
    - src/db/favourites-dao.ts — `setFavouriteRank`/`clearFavouriteRank(exec, id, now)`.
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Accessibility (`contact-profile-favourite-star`, accessibilityLabel toggles "Mark favourite"/"Remove favourite") + § Interaction States (Profile star: reversible, no confirmation) + § Owner Decisions (OD-2 favourite token/glyph flagged).
  </read_first>
  <acceptance_criteria>
    - The profile header shows a star toggle `testID="contact-profile-favourite-star"` whose accessibilityLabel is "Mark favourite" when not a favourite and "Remove favourite" when it is.
    - Tapping it calls `setFavouriteRank` (when not favourite) or `clearFavouriteRank` (when favourite) via getExecutor()+localDateTime(), then refreshes the favourite state; NO confirmation dialog (reversible, non-destructive).
    - The screen reads the contact's current `favourite_rank` (extend the header read minimally, or add a light dedicated read) so the star reflects the persisted state on focus.
    - The star uses `colors.accent` (provisional OD-2, flagged in a comment); `npm run check:colors` and `npx tsc --noEmit` clean.
    - No existing profile behaviour regresses (timeline, fuel editor, log, archive, gravity/intensity all still render).
  </acceptance_criteria>
  <action>
    Extend ContactProfileScreen: add local state for `isFavourite` and populate it in `load` — either extend the header read to include `favourite_rank` (preferred: add `favourite_rank` to getContactHeader's SELECT + return type in contact-read.ts, mapping null→false) OR add a small `getFavouriteState(exec, id)` read; pick the minimal-churn option and keep the single unified `load()`. Add a star Pressable in the header row (near the OverflowMenu) with the LOCKED testID + toggling accessibilityLabel; on press, call setFavouriteRank/clearFavouriteRank then re-run `load()` (or optimistically flip then reconcile). No confirmation dialog. Use `colors.accent` for the marked star (comment: OD-2 provisional favourite token, owner decision). Do not disturb any existing profile section.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>The profile carries a reversible favourite star that marks/clears favourite_rank via the guarded DAO with no confirmation, reflecting the persisted state on focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Banner read → screen | The banner reads only id/name/birthday for non-archived contacts; no private fuel is surfaced. |
| Profile star → favourites write | A reversible per-contact write through the guarded DAO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-14 | Information disclosure | BirthdayBanner read path | medium | mitigate | listBirthdayCandidates selects only id/name/birthday for non-archived contacts; async on-device read, no network; renders nothing when empty. |
| T-08-15 | Tampering (wrong-row write) | profile star toggle | low | mitigate | Writes go through setFavouriteRank/clearFavouriteRank (changes===1 guard, transactional, last_contact untouched); reversible, so no confirmation needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run check:colors` clean.
- On-device UAT (Pixel, end-of-phase): star a contact → it shows a favourite marker on the card and in the favourites filter; seed a snoozed + a never-contacted contact with a ≤7-day birthday → both appear in the banner; an archived one does not.
</verification>

<success_criteria>
- The birthday banner component exists (ready to mount) and the profile star marks/clears favourites reversibly through the guarded DAO.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-06-SUMMARY.md` when done.
</output>
