---
phase: 08-dashboard-never-contacted-screen
plan: 07
type: execute
wave: 3
depends_on: ["08-01", "08-04", "08-05", "08-06"]
files_modified:
  - src/logic/dashboard-empty-logic.ts
  - src/logic/dashboard-empty-logic.test.ts
  - src/screens/HomeScreen.tsx
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04, DASH-05, DASH-07]
must_haves:
  truths:
    - "HomeScreen is the dashboard: a flat status-sorted FlatList of ContactCards from listDashboard's default population (never-contacted/archived/currently-snoozed excluded), rendering offline."
    - "Freshness re-queries on useFocusEffect, on an AppState→active listener, and via pull-to-refresh (RefreshControl) — NOT the connection-scoped change listener — with an async cancelled-flag guard and async-only reads."
    - "The birthday banner mounts at the top (tap→profile); a count header shows '{N} contacts'; footer entries reach 'Not yet contacted (N)' (pushes NeverContacted) and a count-less 'Archived'."
    - "The first-run empty state fires ONLY when ALL FOUR populations are empty (countLiveContacts === 0 && countNeverContacted === 0 && countSnoozed === 0 && countArchived === 0); a user with only never-contacted OR only snoozed contacts gets the cause-aware hidden-population pointer, never 'Add your first contact' (HIGH-2 / DASH-07)."
    - "The first-run-vs-hidden-vs-none decision is a pure, node-tested helper (selectDashboardEmptyState) that HomeScreen consumes — not inline count arithmetic."
  artifacts:
    - src/logic/dashboard-empty-logic.ts
    - src/screens/HomeScreen.tsx
  key_links:
    - "Consumes listDashboard + counts + BirthdayBanner + ContactCard; uses dashboard-prefs-store for the default sort/filter; navigates to NeverContacted/Archived/Profile."
    - "Freshness path is focus + AppState + pull-to-refresh — never addDatabaseChangeListener (structurally blind to headless widget/notification writes)."
---

<objective>
Transform `HomeScreen` into the real dashboard CORE — the app's home screen. It renders the default status-sorted `listDashboard` population as `ContactCard`s, mounts the birthday banner, shows the contact-count header and the hidden-population footer entries, wires the reliable freshness path (focus + AppState + pull-to-refresh), and ships cause-aware empty states. The interactive controls (chips, sort control, search box) are added in Plan 09.

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

After this plan: opening Orbit lands on a working dashboard — real cards, birthdays, counts, reachable sibling screens, refreshing on focus/foreground/pull — the core daily surface. This is the phase's primary vertical slice.
Output: `HomeScreen.tsx` transformed into the dashboard core.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md
@.planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md
@src/screens/HomeScreen.tsx
@src/screens/ArchivedContactsScreen.tsx
@src/screens/FuelSearch.tsx
@src/screens/ContactProfileScreen.tsx
@src/db/dashboard-read.ts
@src/components/ContactCard.tsx
@src/components/BirthdayBanner.tsx
@src/stores/dashboard-prefs-store.ts
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan transforms `src/screens/HomeScreen.tsx` into the dashboard core surface.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard-empty-logic.ts — pure cause-aware empty-state gate (HIGH-2)</name>
  <files>src/logic/dashboard-empty-logic.ts, src/logic/dashboard-empty-logic.test.ts</files>
  <read_first>
    - src/logic/birthday-logic.ts + src/logic/favourites-reorder-logic.ts — the `*-logic.ts` pure-module convention (react-native-free, node-tested) this helper follows.
    - src/db/dashboard-read.ts — the four count functions (`countLiveContacts` = `archived_at IS NULL AND last_contact IS NOT NULL`, `countNeverContacted`, `countSnoozed`, `countArchived`) whose integers feed this decision; the first-run gate MUST use countLiveContacts as pinned in 08-01 (HIGH-2).
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Loading/Empty/Error States (DASH-07) — first-run vs hidden-population vs error copy + testIDs.
  </read_first>
  <behavior>
    - selectDashboardEmptyState({ live, neverContacted, snoozed, archived, rowCount }) returns 'firstrun' ONLY when live===0 && neverContacted===0 && snoozed===0 && archived===0 (a genuinely empty app).
    - never-contacted-only user (live===0, neverContacted>0, snoozed===0, archived===0) → 'hidden' (NOT 'firstrun') — this is the DASH-07 regression the gate must prevent.
    - snoozed-only user (live===0, snoozed>0, rest 0) → 'hidden'.
    - archived-only user (live===0, archived>0, rest 0) → 'hidden'.
    - When the visible list is non-empty (rowCount>0) the result is 'none' (no empty state), regardless of hidden populations.
    - When rowCount===0 but any hidden population is non-zero → 'hidden'.
    - Pure: same inputs → same output; no I/O, never throws.
  </behavior>
  <action>
    Implement `selectDashboardEmptyState(counts)` in a react-native-free module returning a small union (`'firstrun' | 'hidden' | 'none'`; the error state is handled separately by the screen's load-throw flag, not by this helper). First-run is gated on ALL FOUR populations being zero — `live === 0 && neverContacted === 0 && snoozed === 0 && archived === 0` — which is robust regardless of how the "{N} contacts" header semantic is read (addresses review HIGH-2: the old `no-live && no-archived` gate mislabelled never-contacted-only and snoozed-only users). When the visible list has rows, return 'none'; when it is empty but a hidden population is non-zero, return 'hidden'. Keep it a pure function so the branch is node-tested off-device. Write `dashboard-empty-logic.test.ts` (Vitest node) covering every behavior bullet, explicitly including the never-contacted-only and snoozed-only cases.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-empty-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/logic/dashboard-empty-logic.test.ts` passes with: all-zero → 'firstrun'; never-contacted-only → 'hidden'; snoozed-only → 'hidden'; archived-only → 'hidden'; rows-present → 'none'; empty+hidden → 'hidden'.
    - `grep -c "react-native\|expo-" src/logic/dashboard-empty-logic.ts` is 0 (pure module).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The cause-aware empty-state gate is a pure, node-tested helper whose first-run branch requires all four populations empty, so never-contacted-only and snoozed-only users get the DASH-07 pointer.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen → dashboard core (list + freshness + banner + count + footer)</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx — the current placeholder shell (testID `home-shell-root`, nav + theme boilerplate) to replace.
    - src/screens/ContactProfileScreen.tsx (lines 21, 156-200) — the shipped `useFocusEffect(useCallback(...))` reload-on-focus idiom to reuse.
    - src/screens/FuelSearch.tsx (lines 50-64) — the cancelled-flag async guard.
    - src/screens/ArchivedContactsScreen.tsx — the FlatList + getExecutor load idiom + Back chrome tokens.
    - src/db/dashboard-read.ts — `listDashboard(exec,{filter,sort,term})`, `countNeverContacted`, `countSnoozed`, `countArchived`, `countLiveContacts` (pinned to `archived_at IS NULL AND last_contact IS NOT NULL`), and the `DashboardRow` shape.
    - src/logic/dashboard-empty-logic.ts — `selectDashboardEmptyState` (Task 1): the pure first-run/hidden/none gate the screen MUST use instead of inline count arithmetic (HIGH-2).
    - src/components/ContactCard.tsx — the card props to pass; src/components/BirthdayBanner.tsx — the `onPressContact` callback.
    - src/stores/dashboard-prefs-store.ts — `useDashboardPrefs` (default sort "status", filter "all").
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Loading/Empty/Error States (DASH-07), § Copywriting Contract (count header "{N} contacts", empty-hidden "Everyone's tucked away" + "{N} not yet contacted →" etc., error "Couldn't load your contacts"), § Accessibility (`dashboard-root`, `dashboard-header-count`, `dashboard-birthday-banner`, `dashboard-not-yet-contacted-entry`, `dashboard-archived-entry`, `dashboard-empty-firstrun`, `dashboard-empty-hidden`, `dashboard-error-state`), § Interaction States (freshness, footer entries).
    - .planning/phases/08-dashboard-never-contacted-screen/08-RESEARCH.md § Pattern 2 (the freshness path + why addDatabaseChangeListener is unusable) — put the literal change-listener name only here/in verify, not in written code comments.
  </read_first>
  <acceptance_criteria>
    - Root carries `testID="dashboard-root"` and `backgroundColor: colors.background`.
    - Renders the default population: `listDashboard(getExecutor(), { filter: prefsFilter, sort: prefsSort })` (from the persisted store; controls to change them land in Plan 09) as a FlatList of `ContactCard` (each row testID `dashboard-card-{contactId}`, onPress → `Profile { contactId }`).
    - Freshness: re-queries on `useFocusEffect`, on an `AppState` change→"active" listener (subscription removed on unmount), and via `RefreshControl` pull-to-refresh (tint `colors.accent`); every load is guarded by a `cancelled` flag; reads are async only.
    - BirthdayBanner mounts at the top with `onPressContact` → `Profile`; the count header (`dashboard-header-count`) shows "{N} contacts" from countLiveContacts.
    - Footer entries: `dashboard-not-yet-contacted-entry` labelled "Not yet contacted ({N})" (from countNeverContacted) → `navigation.navigate("NeverContacted")`; `dashboard-archived-entry` labelled "Archived" (no count) → `navigation.navigate("Archived")`.
    - Cause-aware empty states, selected via `selectDashboardEmptyState` (Task 1): `dashboard-empty-firstrun` ONLY when ALL FOUR populations are zero (`countLiveContacts===0 && countNeverContacted===0 && countSnoozed===0 && countArchived===0`) → "Add your first contact" filled-accent CTA → `Create`; `dashboard-empty-hidden` (visible list empty but a hidden population is non-zero — incl. a never-contacted-only or snoozed-only user, HIGH-2) → show the non-zero "{N} not yet contacted →/{N} snoozed/{N} archived" pointers; `dashboard-error-state` on a load throw ("Couldn't load your contacts — Pull down to try again", never a raw exception).
    - No network on the read path; `expo-image` avatars are local. `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Replace the placeholder HomeScreen with the dashboard core. Use `useNavigation` typed to `RootStackParamList` (or the screen-props form). Read `sort` + `filter` from `useDashboardPrefs`. Implement a single `reload` (useCallback) that runs `listDashboard` + the four counts via `getExecutor()`, guarded by a cancelled flag, setting rows/counts/error state; call it from `useFocusEffect(useCallback(...))`. Add an `AppState.addEventListener("change", s => s === "active" && reload())` effect that removes its subscription on cleanup. Render a `FlatList` with a `RefreshControl` (accent tint) whose `onRefresh` re-runs `reload`; use ListHeaderComponent for the birthday banner + count header, and ListFooterComponent (or below the list) for the Not-yet-contacted + Archived entries. Render each row as a `ContactCard` (map DashboardRow → props; onPress navigates to Profile). Implement the three cause-aware empty states + the error state per UI-SPEC copy and testIDs; decide first-run vs hidden-population by calling `selectDashboardEmptyState` (Task 1) with the four counts + the current row count — do NOT inline the arithmetic. First-run requires ALL FOUR populations empty (`live && never && snoozed && archived` all 0), so a never-contacted-only or snoozed-only user gets the hidden-population pointer, never "Add your first contact" (addresses review HIGH-2); hidden = visible list empty but a hidden population > 0. Every colour via `useTheme().colors.*`. Do NOT use the connection-scoped database change listener — reference it by concept in code comments (freshness is focus + AppState + pull), keeping the literal API name out of the written source.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>HomeScreen is the working dashboard core: real status-sorted cards, birthday banner, count header, footer entries to never-contacted/archived, focus/AppState/pull freshness, and cause-aware empty/error states — all offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard read → home surface | The primary glanceable surface; must not surface private/log-derived data and must never block on a network read. |
| App foreground/focus → re-query | Cross-context freshness must reflect headless widget/notification writes on their own connection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | Information disclosure | dashboard read path | high | mitigate | listDashboard scopes off_limits/ai/archived in-query; the screen performs no `.filter()` on private data; async on-device reads only; expo-image makes zero network calls for file:// avatars (local-first). |
| T-08-17 | Denial of Service (UI stall) | freshness / read path | medium | mitigate | Async `getAllAsync` only (never the sync variants that block the JS thread); cancelled-flag guard prevents stale-result clobber. |
| T-08-18 | Tampering (stale data after headless write) | freshness mechanism | medium | mitigate | Re-query on focus + AppState→active + pull-to-refresh — NOT the connection-scoped change listener, which is structurally blind to the headless widget/notification "mark contacted" write. |
</threat_model>

<verification>
- `npx vitest run src/logic/dashboard-empty-logic.test.ts` green (incl. the never-contacted-only and snoozed-only cases — HIGH-2).
- `npx tsc --noEmit` and `npm run check:colors` clean.
- `grep -c "addDatabaseChangeListener" src/screens/HomeScreen.tsx` is 0 (freshness is focus + AppState + pull-to-refresh).
- `grep -c "getAllSync\|getFirstSync" src/screens/HomeScreen.tsx` is 0.
- On-device UAT (Pixel, end-of-phase): dashboard renders status-sorted cards, banner, counts; background→foreground and pull-to-refresh both re-query; a genuinely empty app shows first-run, while a seed with ONLY never-contacted (and separately ONLY snoozed) contacts shows the hidden-population pointer, NOT "Add your first contact".
</verification>

<success_criteria>
- The dashboard core is the home screen, renders offline, refreshes reliably on focus/AppState/pull, and reaches the sibling screens; cards carry the LOCKED content contract.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-07-SUMMARY.md` when done.
</output>
