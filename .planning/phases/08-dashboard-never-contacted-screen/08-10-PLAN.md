---
phase: 08-dashboard-never-contacted-screen
plan: 10
type: execute
wave: 5
depends_on: ["08-08", "08-09"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/FuelSearch.tsx
autonomous: true
requirements: [DASH-06, DASH-02]
must_haves:
  truths:
    - "The standalone FuelSearch route + Settings search row are retired now that the dashboard owns the search box (expected relocation, NOT a reversal)."
    - "Settings gains a 'Manage favourites' row → ManageFavourites and loses the 'Search' row."
    - "The reusable FuelSearchResultRow component is KEPT (its snippet idiom is reused by the dashboard card); only the standalone screen + route are removed."
  artifacts: []
  key_links:
    - "Depends on the dashboard search box (Plan 09) existing so search capability is not lost, and the ManageFavourites route (Plan 08) existing so the Settings row navigates somewhere."
---

<objective>
Complete the search relocation and the favourites-management entry point: retire the standalone `FuelSearch` route + the Settings "Search" row (the dashboard now owns search, Plan 09), and add a Settings "Manage favourites" row that opens the shared reorder screen. The reusable `FuelSearchResultRow` component stays (the dashboard card reuses its snippet idiom); only the standalone screen and its route are removed.

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

After this plan: search lives only on the dashboard, and Settings offers the Manage-favourites entry — the two navigation seams the phase promised.
Output: nav + Settings edits; the FuelSearch screen removed.

Note (from CONTEXT Deferred Ideas): the 07-photos SDK version-label drift (workpaper pinned to SDK 55 while the project runs SDK 57/RN 0.86) is a documentation-label reconcile that changes no decision — out of scope for this code plan; recorded here so it is not lost.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
@src/screens/SettingsScreen.tsx
@src/screens/FuelSearch.tsx
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan removes the FuelSearch route/screen and edits Settings + the nav types/registration — it introduces no net-new symbol.)

<tasks>

<task type="auto">
  <name>Task 1: Retire the FuelSearch route + screen (keep FuelSearchResultRow)</name>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/screens/FuelSearch.tsx</files>
  <read_first>
    - src/navigation/types.ts (lines 40-46) — the `FuelSearch: undefined` entry to remove.
    - src/navigation/RootNavigator.tsx (lines 8, 62) — the `FuelSearch` import + `<Stack.Screen name="FuelSearch">` registration to remove.
    - src/screens/FuelSearch.tsx — the standalone screen being retired; confirm nothing else imports it (only RootNavigator + the Settings row do).
    - src/components/FuelSearchResultRow.tsx — KEEP (reused by the dashboard card). Do NOT delete this.
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Surfaces Delivered ("Search relocation" — the standalone FuelSearch route + settings-search-row are retired; FuelSearchResultRow + searchFuel are the reusable units) — expected, not a reversal (STATE.md Phase-7 note).
  </read_first>
  <acceptance_criteria>
    - `FuelSearch: undefined` removed from `RootStackParamList`; the `FuelSearch` import + `<Stack.Screen name="FuelSearch">` removed from RootNavigator.
    - `src/screens/FuelSearch.tsx` deleted; `src/components/FuelSearchResultRow.tsx` and `src/db/fuel-read.ts` `searchFuel` remain intact.
    - `grep -rc "FuelSearch\b" src/navigation src/screens` shows no live reference to the removed screen/route (FuelSearchResultRow references are the row component, which stays).
    - `npx tsc --noEmit` clean (no dangling import).
  </acceptance_criteria>
  <action>
    Remove the `FuelSearch` route from `RootStackParamList` (types.ts) and its import + `<Stack.Screen>` registration from RootNavigator.tsx. Delete `src/screens/FuelSearch.tsx`. Leave `FuelSearchResultRow.tsx` and `searchFuel` untouched (the dashboard reuses the row idiom and the query predicates). This is the expected search relocation, not a reversal.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The standalone FuelSearch screen + route are gone; the reusable row component and the search DAO remain.</done>
</task>

<task type="auto">
  <name>Task 2: Settings — remove the Search row, add the Manage favourites row</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (lines 108-121) — the `settings-search-row` Pressable to remove; (lines 123-151) the row idiom to copy for the new Manage-favourites row.
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Accessibility (`settings-manage-favourites-row` added; `settings-search-row` removed) + § Surfaces (Manage favourites shared with widget config).
    - src/navigation/types.ts — the `ManageFavourites` route (added in Plan 08) to navigate to.
  </read_first>
  <acceptance_criteria>
    - The `settings-search-row` Pressable (navigate → FuelSearch) is removed.
    - A new `settings-manage-favourites-row` Pressable (label "Manage favourites", accessibilityLabel "Manage favourites") navigates to `ManageFavourites`.
    - The remaining Settings rows (Your photo, Custom Fields, Archived contacts) are untouched.
    - `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    In SettingsScreen.tsx, delete the `settings-search-row` Pressable (and its `navigate("FuelSearch")`). Add a `settings-manage-favourites-row` Pressable copying the existing row idiom, labelled "Manage favourites", navigating to `ManageFavourites`. Do not disturb the other rows. Every colour via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>Settings no longer hosts the standalone search and offers the Manage-favourites entry to the shared reorder screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route table / Settings entries | Navigation surface change only — no new data or input crosses a boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-22 | Denial of Service (lost capability) | search relocation | low | mitigate | FuelSearch is removed only AFTER the dashboard search box ships (Plan 09 dependency), so search capability is never lost; the searchFuel DAO + FuelSearchResultRow remain. |
| T-08-23 | Tampering (dangling route/import) | nav edits | low | mitigate | tsc verifies no dangling import; grep confirms no live FuelSearch route reference remains. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run check:colors` clean.
- `npx vitest run` (full suite) green after this wave.
- On-device UAT (Pixel, end-of-phase): Settings shows "Manage favourites" (opens the reorder screen) and no "Search" row; dashboard search still works.
</verification>

<success_criteria>
- Search lives only on the dashboard; Settings offers Manage favourites; no dangling FuelSearch route/import; the reusable row + search DAO are intact.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-10-SUMMARY.md` when done.
</output>
