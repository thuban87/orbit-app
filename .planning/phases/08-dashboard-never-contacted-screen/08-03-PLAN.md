---
phase: 08-dashboard-never-contacted-screen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/favourites-dao.ts
  - src/db/favourites-dao.test.ts
  - src/logic/favourites-reorder-logic.ts
  - src/logic/favourites-reorder-logic.test.ts
autonomous: true
requirements: [DASH-06]
must_haves:
  truths:
    - "setFavouriteRank marks a contact a favourite by appending it at the end of the rank order (MAX+1); clearFavouriteRank sets favourite_rank NULL — both single-column UPDATEs with a changes===1 guard, bumping modified_at and never touching last_contact."
    - "rewriteFavouriteRanks writes each id's new rank 0..n-1 inside ONE inWriteTransaction (N raw UPDATEs, never a wrapped single-write DAO in a loop — the non-reentrant mutex would deadlock)."
    - "rewriteFavouriteRanks meets its mismatched-id-count guarantee (MEDIUM-2): in the same transaction it verifies unique ids, verifies the supplied count equals the current non-archived favourite count, and scopes every UPDATE `WHERE id = ? AND favourite_rank IS NOT NULL AND archived_at IS NULL` (changes===1) — a partial, duplicate, or stale list rolls back and never ranks a non-favourite/archived row."
    - "computeReorder(orderedIds, from, to) returns the new id array after a drag move, pure and node-tested."
  artifacts:
    - src/db/favourites-dao.ts
    - src/db/favourites-dao.test.ts
    - src/logic/favourites-reorder-logic.ts
    - src/logic/favourites-reorder-logic.test.ts
  key_links:
    - "favourites-dao mirrors setContactPhoto/clearContactPhoto (contacts-dao.ts:518-563) exactly; it is the ONLY new writer this phase and does NOT write contacts.last_contact (DATA-04 single-writer invariant intact)."
    - "The pure reorder computation lives in favourites-reorder-logic.ts; the transactional rank rewrite is the only DB touch."
---

<objective>
Build the favourites write layer: `src/db/favourites-dao.ts` (the mark/clear toggle + the transactional rank rewrite) and `src/logic/favourites-reorder-logic.ts` (the pure drag→order computation). The DAO is the ONLY new writer Phase 8 introduces; it mirrors the shipped `setContactPhoto`/`clearContactPhoto` single-column-writer pattern and never writes `contacts.last_contact` (the DATA-04 single-writer invariant stays intact — no migration, `favourite_rank` exists in migration 001).

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

Purpose: keep the favourite mark (profile star, Plan 06) and reorder (Manage-favourites, Plan 08) writes transactional, guarded, and node-tested; extract the drag math so it is proven off-device.
Output: `favourites-dao.ts` + `favourites-reorder-logic.ts` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-RESEARCH.md
@.planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md
@src/db/contacts-dao.ts
@src/db/transaction.ts
@src/db/fuel-read.test.ts
@src/db/__testkit__/node-sqlite.ts
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan: `src/db/favourites-dao.ts` → `setFavouriteRank`, `clearFavouriteRank`, `rewriteFavouriteRanks`; `src/logic/favourites-reorder-logic.ts` → `computeReorder`; plus their tests.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: favourites-dao.ts — setFavouriteRank / clearFavouriteRank / rewriteFavouriteRanks</name>
  <files>src/db/favourites-dao.ts, src/db/favourites-dao.test.ts</files>
  <read_first>
    - src/db/contacts-dao.ts (lines 497-563) — `setContactPhoto`/`clearContactPhoto`: the EXACT pattern to mirror (one inWriteTransaction, `?`-bound single-column UPDATE, changes===1 loud-failure guard, modified_at bumped, last_contact untouched; the "updateContactMetadataCore deliberately omits this column" reasoning).
    - src/db/transaction.ts (lines 11-29, 42-57) — `inWriteTransaction` and the NON-REENTRANT rule (nesting = permanent hang). rewriteFavouriteRanks must issue N raw UPDATEs inside ONE transaction, never call a wrapped single-write DAO in a loop.
    - .planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md "src/db/favourites-dao.ts" — the toggle pattern + append-at-end (MAX+1) + rewrite rules; RESEARCH § Pattern 5 + Pitfall 6.
    - src/db/fuel-read.test.ts + src/db/__testkit__/node-sqlite.ts — the node:sqlite harness + seedContact idiom.
  </read_first>
  <behavior>
    - setFavouriteRank on a non-favourite contact sets favourite_rank = MAX(favourite_rank)+1 over contacts (first favourite → 0); a bad id throws (changes !== 1) and rolls back; last_contact is unchanged; modified_at is bumped.
    - clearFavouriteRank sets favourite_rank NULL; a bad id throws; last_contact unchanged; modified_at bumped.
    - rewriteFavouriteRanks(exec, [idA, idB, idC], now) sets rank 0 for idA, 1 for idB, 2 for idC in ONE transaction; reading the three back in favourite_rank ASC yields that order.
    - Inside that same transaction it ENFORCES three invariants (review MEDIUM-2), each a loud failure that rolls back the whole batch: (1) the ids are UNIQUE (a duplicate id → throw); (2) `orderedIds.length` equals the CURRENT count of non-archived favourites (a partial or over-long list → throw); (3) each UPDATE is scoped `WHERE id = ? AND favourite_rank IS NOT NULL AND archived_at IS NULL` with a changes===1 assertion, so a STALE id (a since-unfavourited, archived, or never-favourite contact) can never be assigned a rank — it fails the changes guard and rolls back.
    - Partial list (fewer ids than current favourites), duplicate list (a repeated id), and stale list (an id no longer a live favourite) each roll back with NO rank change persisted and NO rank assigned to a non-favourite/archived row.
    - An empty `orderedIds` (`[]`) is an ACCEPTED no-op, NOT a missing guard (review A-2): the uniqueness check passes (size 0 === length 0) and the count check passes when the current non-archived favourite count is also 0 (0 === 0), so zero UPDATEs commit and nothing changes — harmless, and unreachable from the drag UI, which only reorders an existing non-empty favourites list.
    - No function writes contacts.last_contact.
  </behavior>
  <action>
    Create `favourites-dao.ts` importing `inWriteTransaction` (from transaction.ts) and `SqlExecutor` (from db/types). Implement three functions mirroring setContactPhoto/clearContactPhoto shape:
    - `setFavouriteRank(exec, id, now)`: ONE inWriteTransaction → `UPDATE contacts SET favourite_rank = (SELECT COALESCE(MAX(favourite_rank), -1) + 1 FROM contacts), modified_at = ? WHERE id = ?` with `[now, id]`; assert `result.changes === 1` else throw a descriptive error (rollback). (Append-at-end; ranks need not be gap-free — ordering is favourite_rank ASC.)
    - `clearFavouriteRank(exec, id, now)`: ONE inWriteTransaction → `UPDATE contacts SET favourite_rank = NULL, modified_at = ? WHERE id = ?`; changes===1 guard.
    - `rewriteFavouriteRanks(exec, orderedIds, now)`: ONE inWriteTransaction that, BEFORE writing, enforces two pre-checks so the "mismatched id count" guarantee actually holds (review MEDIUM-2): (a) reject a non-unique `orderedIds` — build a Set and throw if `size !== length`; (b) read the current non-archived favourite count (`SELECT COUNT(*) AS n FROM contacts WHERE favourite_rank IS NOT NULL AND archived_at IS NULL`) and throw unless it equals `orderedIds.length`, so a partial or over-long list can never leave omitted favourites at stale ranks. Then loop N raw `UPDATE contacts SET favourite_rank = ?, modified_at = ? WHERE id = ? AND favourite_rank IS NOT NULL AND archived_at IS NULL` (rank = array index 0..n-1), asserting changes===1 per row — so a stale id (unfavourited / archived / never a favourite) fails loudly and rolls back the batch, and a rank can NEVER be assigned to a non-favourite or archived contact. Do NOT call setFavouriteRank inside the loop (non-reentrant mutex → deadlock — keep this handling exactly as-is). Document the non-reentrancy reason AND the three guards in-file, plus a one-line note that an empty `orderedIds` is an accepted no-op (both guards pass at 0===0, zero UPDATEs) and is unreachable from the drag UI — so it is not mistaken for a missing guard (review A-2).
    Each function is async and `?`-binds every runtime value; only static column names are literal. No last_contact write anywhere.
    Write `favourites-dao.test.ts` (node:sqlite harness) covering every behavior bullet, including a rollback assertion for a bad id and a last_contact-unchanged assertion.
  </action>
  <verify>
    <automated>npx vitest run src/db/favourites-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/favourites-dao.test.ts` passes: append-at-end mark, clear, transactional rewrite order, bad-id rollback for each, last_contact-unchanged, modified_at bumped; and the rewriteFavouriteRanks guards (MEDIUM-2) — a PARTIAL list (fewer ids than current favourites) throws + rolls back; a DUPLICATE id throws + rolls back; a STALE id (an archived or already-unfavourited contact) throws + rolls back, leaving no rank persisted and no non-favourite/archived row ranked.
    - `grep -c "last_contact" src/db/favourites-dao.ts` is 0 (never writes recency).
    - The rewriteFavouriteRanks UPDATE is scoped to live favourites: `grep -c "favourite_rank IS NOT NULL AND archived_at IS NULL" src/db/favourites-dao.ts` is >= 1.
    - rewriteFavouriteRanks contains exactly ONE `inWriteTransaction(` and does NOT call `setFavouriteRank(` (verified by reading — no nested wrapped write).
    - `grep -c "getAllSync\|getFirstSync" src/db/favourites-dao.ts` is 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>favourites-dao.ts provides transactional, guarded mark/clear/reorder writes that never touch last_contact, node-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: favourites-reorder-logic.ts — pure drag→order computation</name>
  <files>src/logic/favourites-reorder-logic.ts, src/logic/favourites-reorder-logic.test.ts</files>
  <read_first>
    - src/screens/edit-contact-logic.ts — the `*-logic.ts` pure-module convention (react-native-free, node-tested).
    - .planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md "src/logic/favourites-reorder-logic.ts" — extract the drag from-index/to-index → new ordered id array as a pure function; the transactional write stays in favourites-dao.
  </read_first>
  <behavior>
    - computeReorder([a,b,c,d], 0, 2) moves a to index 2 → [b,c,a,d].
    - computeReorder([a,b,c,d], 3, 0) moves d to the front → [d,a,b,c].
    - A no-op move (from === to) returns an equal-order array.
    - Out-of-range indices are clamped or rejected deterministically (choose one and test it); the input array is not mutated (returns a new array).
    - The result is always a permutation of the input (same members, no loss/dup).
  </behavior>
  <action>
    Implement `computeReorder(orderedIds: number[], from: number, to: number): number[]` — a pure array move that returns a NEW array (does not mutate the input). Splice the element out of `from` and insert it at `to`. Handle from===to (return a copy) and clamp/guard out-of-range indices deterministically. This is the exact computation the Manage-favourites drag-end (Plan 08) feeds into `rewriteFavouriteRanks`. Keep it react-native-free. Write `favourites-reorder-logic.test.ts` covering every behavior bullet, including a permutation-invariance assertion and an input-not-mutated assertion.
  </action>
  <verify>
    <automated>npx vitest run src/logic/favourites-reorder-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/logic/favourites-reorder-logic.test.ts` passes: forward move, backward move, no-op, out-of-range handling, permutation invariance, input-not-mutated.
    - `grep -c "react-native\|expo-" src/logic/favourites-reorder-logic.ts` is 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>computeReorder is a pure, node-tested drag→order function ready to feed rewriteFavouriteRanks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI star/drag → favourites write | A per-contact device-local write; the risk is transaction integrity, not external attack (no network, no external input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Tampering (wrong-row / partial write) | favourites-dao writes | low | mitigate | Single-column `?`-bound UPDATE with a changes===1 guard; the rewrite runs in ONE inWriteTransaction that verifies unique ids + count-equals-current-favourites and scopes every UPDATE `WHERE id = ? AND favourite_rank IS NOT NULL AND archived_at IS NULL` (changes===1), so a partial/duplicate/stale `orderedIds` cannot leave stale ranks or rank a non-favourite/archived row — it rolls back the whole batch (MEDIUM-2). |
| T-08-08 | Denial of Service (mutex deadlock) | rewriteFavouriteRanks | medium | mitigate | Never nest the non-reentrant mutex — N raw UPDATEs inside one transaction, never a wrapped single-write DAO in a loop (RESEARCH Pitfall 6). |
| T-08-09 | Tampering (recency corruption) | contacts.last_contact | low | accept | The DAO never references last_contact; DATA-04 single-writer invariant is untouched by construction (grep-verified). |
</threat_model>

<verification>
- `npx vitest run src/db/favourites-dao.test.ts src/logic/favourites-reorder-logic.test.ts` green.
- `npx tsc --noEmit` and Biome clean.
- No last_contact write; no nested transaction; no sync read.
</verification>

<success_criteria>
- favourites-dao provides guarded, transactional mark/clear/reorder writes; the pure reorder computation is proven off-device; the single-writer recency invariant is intact.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-03-SUMMARY.md` when done.
</output>
