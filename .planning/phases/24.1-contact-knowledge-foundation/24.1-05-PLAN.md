---
phase: 24.1-contact-knowledge-foundation
plan: 05
type: execute
wave: 3
depends_on: ["24.1-02"]
files_modified:
  - src/db/memories-dao.ts
  - src/db/memories-read.ts
  - src/db/memories-read.test.ts
  - src/services/memory-trash-sweep.ts
  - src/services/memory-trash-sweep.test.ts
  - App.tsx
autonomous: true
requirements: [KNOW-07]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Soft-deleted memories are retrievable: listRecentlyDeleted(contact) returns rows with deleted_at IS NOT NULL, ordered deleted_at DESC, so the Recently Deleted surface can list and restore them (KNOW-07)."
    - "purgeMemoryPermanently physically removes a single soft-deleted (deleted_at IS NOT NULL) memory scoped by id AND contact_id + assertOneChange — it refuses to hard-delete a LIVE memory (the two-stage soft-then-permanent safety, mirroring the archive→purge guard)."
    - "Trash expiry runs as a launch sweep registered via registerSweepHook (App.tsx → installSweepTrigger), NEVER a background timer (D-11); the retention window is a single top-of-file tunable constant."
    - "The empty-state / expiry copy the UI shows is driven by the same retention-window constant the sweep enforces — no promised timer, no drift."
    # --- KNOW-07 edges (specless probe) ---
    - "KNOW-07 idempotency: running the trash sweep twice expires the same rows exactly once — a re-run finds nothing still past-window and permanently deletes nothing further."
    - "KNOW-07 concurrency: the sweep is TOCTOU-safe — a bare candidate scan only narrows the set; the authoritative past-window re-check happens again under the write lock, so a memory RESTORED between the scan and the delete survives."
    - "The sweep uses datetime('now','localtime', ?) with a STRICT '<' boundary so a memory soft-deleted exactly at the window edge is not expired early; a device clock moving backward never expires a row before its true window elapses."
  artifacts:
    - "src/services/memory-trash-sweep.ts — registerMemoryTrashSweep(getExec, now?) launch hook + MEMORY_TRASH_WINDOW_DAYS tunable"
    - "src/db/memories-read.ts — listRecentlyDeleted projection"
    - "src/db/memories-dao.ts — purgeMemoryPermanentlyCore/purgeMemoryPermanently (guarded hard delete)"
    - "App.tsx — one-time registration of the memory-trash sweep"
  key_links:
    - "the sweep loops candidates and calls the mutex-owning purge writer DIRECTLY in its own try/catch — it NEVER re-wraps it in inWriteTransaction (non-reentrant → permanent hang, the field-sweep HIGH-1 lesson)"
    - "App.tsx registers registerMemoryTrashSweep behind a module-level 'registered' guard exactly like registerFieldSweep, after openAndMigrate resolves"
  prohibitions:
    - "The launch sweep must never permanently delete a Memory before its retention window elapses, even across device clock changes."
    - "A soft-deleted Memory must never silently disappear before expiry — it stays restorable from Recently Deleted until the sweep permanently removes it past the window."
---

<objective>
Own the Recently Deleted / Trash lifecycle (assigned to this phase, D-10a): the recently-deleted read, the guarded permanent-delete writer, and the launch-sweep that expires soft-deleted memories past a retention window — TOCTOU-safe, idempotent, and never a timer (KNOW-07, D-11).

Purpose: Complete the soft-delete story so nothing is ever lost silently, while giving expiry a correct, node-tested launch-sweep implementation modeled on field-sweep.

Output: memory-trash-sweep service + App.tsx registration + listRecentlyDeleted + purgeMemoryPermanently, all node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-RESEARCH.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-02-SUMMARY.md
@src/services/field-sweep.ts
@src/services/launch-sweep.ts
@src/db/memories-dao.ts
@src/db/memories-read.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listRecentlyDeleted read + purgeMemoryPermanently (guarded hard delete)</name>
  <files>src/db/memories-read.ts, src/db/memories-read.test.ts, src/db/memories-dao.ts</files>
  <read_first>
    - src/db/memories-read.ts (listMemoriesForContact live projection — the sibling read)
    - src/db/memories-dao.ts (soft delete/restore from Plan 02; the core/wrapper pattern)
    - src/db/purge-dao.ts (the archived-only-before-purge two-stage guard precedent — purgeContact asserts archived_at IS NOT NULL before deleting)
    - src/db/fuel-dao.ts (deleteFuelCore hard-delete + assertOneChange shape)
  </read_first>
  <behavior>
    - listRecentlyDeleted(contact) returns deleted_at IS NOT NULL rows ordered deleted_at DESC, id DESC; [] when the trash is empty for that contact.
    - purgeMemoryPermanently physically DELETEs a memory only when it is soft-deleted (deleted_at IS NOT NULL), scoped by id AND contact_id, assertOneChange.
    - purgeMemoryPermanently on a LIVE (deleted_at IS NULL) memory changes 0 rows and throws — a live item can never be one-tap permanently deleted.
  </behavior>
  <action>
    Add listRecentlyDeleted(exec, contactId) to src/db/memories-read.ts (WHERE contact_id=? AND deleted_at IS NOT NULL ORDER BY deleted_at DESC, id DESC). Add purgeMemoryPermanentlyCore/purgeMemoryPermanently to src/db/memories-dao.ts: DELETE FROM memories WHERE id=? AND contact_id=? AND deleted_at IS NOT NULL, then assertOneChange — this is the guarded hard delete (the second stage after soft-delete). Do NOT insert a tombstone and do NOT touch backup here (backup/merge wiring is Phase 24.2/36). Extend memories-read.test.ts (recently-deleted listing + live exclusion) and add a memories-dao test proving purge refuses a live row and succeeds on a soft-deleted one.
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-read.test.ts src/db/memories-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass: recently-deleted listing excludes live rows; purge refuses a live memory (throws) and succeeds on a soft-deleted one.
    - Grep confirms the purge is guarded: the DELETE in memories-dao.ts includes "deleted_at IS NOT NULL".
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Recently Deleted can be listed and a soft-deleted item permanently purged, but a live item can never be hard-deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memory-trash-sweep launch hook (TOCTOU-safe, idempotent)</name>
  <files>src/services/memory-trash-sweep.ts, src/services/memory-trash-sweep.test.ts</files>
  <read_first>
    - src/services/field-sweep.ts (the exact model: single top-of-file window tunable; bare candidate scan that only narrows; per-item DIRECT call to the mutex-owning writer in its own try/catch — never re-wrapped; strict '<' boundary; datetime('now','localtime', ?))
    - src/services/launch-sweep.ts (registerSweepHook contract; idempotent hooks; __resetSweepForTest)
    - src/db/memories-dao.ts (purgeMemoryPermanently from Task 1 — the writer the sweep calls)
  </read_first>
  <behavior>
    - registerMemoryTrashSweep(getExec, now?) pushes one idempotent hook onto the registry.
    - The hook scans candidates: memories WHERE deleted_at IS NOT NULL AND deleted_at < datetime('now','localtime', '-<N> days') (strict '<').
    - For each candidate it calls purgeMemoryPermanently DIRECTLY in its own try/catch (never wrapped in a second inWriteTransaction) — one failure does not abort the loop.
    - Running the hook twice permanently deletes each past-window row once; a re-run finds nothing still stale.
    - A memory restored (deleted_at→NULL) between the scan and the delete is NOT purged — the purge writer's own deleted_at IS NOT NULL guard is the authoritative under-lock re-check.
    - The retention window is MEMORY_TRASH_WINDOW_DAYS, a single top-of-file tunable.
  </behavior>
  <action>
    Create src/services/memory-trash-sweep.ts modeled on field-sweep.ts. Export MEMORY_TRASH_WINDOW_DAYS (single top-of-file tunable; choose a sensible default such as 30 and document it as the value the empty-state copy must match) and registerMemoryTrashSweep(getExec, now = localNow). Inside the registered hook: build windowModifier = "-<N> days"; run the bare candidate SELECT (deleted_at IS NOT NULL AND deleted_at < datetime('now','localtime', ?), strict '<'); loop candidates calling purgeMemoryPermanently directly in a try/catch that logs via Logger.error and continues. Keep the module node-pure (lazy getExec, injectable now, no react-native import). Add memory-trash-sweep.test.ts using the node:sqlite testkit + __resetSweepForTest: past-window row is purged; within-window row survives; edge (exactly N days) survives; a re-run purges nothing further (idempotent); a row restored after the scan survives (drive by soft-deleting, scanning conceptually, restoring, then running — assert survival via the writer guard).
  </action>
  <verify>
    <automated>npx vitest run src/services/memory-trash-sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/memory-trash-sweep.test.ts` passes past-window / within-window / edge / idempotent-rerun / restore-survives cases.
    - Grep confirms no re-wrapped mutex: the sweep calls purgeMemoryPermanently but contains no inWriteTransaction of its own (`grep -c inWriteTransaction src/services/memory-trash-sweep.ts` filtered of comments is 0).
    - Grep confirms strict boundary: the candidate SQL uses "< datetime(" not "<= datetime(".
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Trash expiry runs as an idempotent, TOCTOU-safe launch sweep with a single tunable window and no timer.</done>
</task>

<task type="auto">
  <name>Task 3: Register the trash sweep in App.tsx</name>
  <files>App.tsx</files>
  <read_first>
    - App.tsx (the registerFieldSweep / registerBackupSweep registration block with its module-level "registered" guards, run after openAndMigrate resolves; installSweepTrigger(AppState))
    - src/services/memory-trash-sweep.ts (registerMemoryTrashSweep signature)
  </read_first>
  <action>
    In App.tsx, add a module-level guard flag (mirroring fieldSweepRegistered) and, in the same effect that registers the other sweeps after migration resolves, call registerMemoryTrashSweep(getExecutor) exactly once behind that guard — before installSweepTrigger(AppState) runs. Do not change the trigger wiring; the sweep must be a foreground SweepHook reached only via the real background→active / cold-start path, never on a headless tap (the launch-sweep contract).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registerMemoryTrashSweep" App.tsx` shows the guarded one-time registration inside the post-migration effect.
    - tsc --noEmit clean.
    - Device UAT (phase gate): soft-delete a memory, confirm it appears in Recently Deleted and is not purged before the window.
  </acceptance_criteria>
  <done>The memory-trash sweep is registered once at launch alongside the existing sweeps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launch (bg→active / cold start) → trash sweep | Time-based permanent deletion runs with no scheduler |
| Recently Deleted UI → purge writer | Permanent deletion of a soft-deleted item |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-07 | Denial of Service / data loss | trash sweep premature/duplicate delete | high | mitigate | Strict '<' window boundary + under-lock deleted_at IS NOT NULL re-check (TOCTOU-safe) + idempotent re-run; single tunable window; no timer |
| T-24.1-08 | Tampering | purgeMemoryPermanently | medium | mitigate | Guarded DELETE (deleted_at IS NOT NULL) + both-keys scope + assertOneChange; refuses live rows |
| T-24.1-09 | Denial of Service | non-reentrant mutex nesting | high | mitigate | Sweep calls the mutex-owning purge writer directly, never re-wrapped (field-sweep HIGH-1 lesson) |
</threat_model>

<verification>
- `npx vitest run src/db/memories-read.test.ts src/db/memories-dao.test.ts src/services/memory-trash-sweep.test.ts` green.
- No-nested-mutex + strict-boundary + guarded-purge greps pass; App.tsx registration present; tsc --noEmit clean.
</verification>

<success_criteria>
Soft-deleted memories are listable and restorable, permanent deletion is guarded and two-stage, and expiry runs as a correct idempotent TOCTOU-safe launch sweep with a single tunable retention window and no timer.
</success_criteria>

## Artifacts this plan produces

- **Service / launch-sweep hook (src/services/memory-trash-sweep.ts):** registerMemoryTrashSweep, MEMORY_TRASH_WINDOW_DAYS.
- **Read (src/db/memories-read.ts):** listRecentlyDeleted.
- **DAO (src/db/memories-dao.ts):** purgeMemoryPermanentlyCore/purgeMemoryPermanently (guarded).
- **Wiring:** App.tsx one-time sweep registration.
- **Tests:** memory-trash-sweep.test.ts + extended memories-read/memories-dao tests.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-05-SUMMARY.md` when done.
</output>
