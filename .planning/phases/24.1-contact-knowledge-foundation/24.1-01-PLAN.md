---
phase: 24.1-contact-knowledge-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/016-contact-knowledge.ts
  - src/db/migrations/016-contact-knowledge.test.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/database.ts
  - src/db/memory-registry.ts
  - src/db/memory-registry.test.ts
  - src/db/memories-dao.ts
  - src/db/memories-dao.test.ts
  - src/db/memories-read.ts
  - src/screens/ThingsToRememberScreen.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
autonomous: false
requirements: [KNOW-01, KNOW-02, KNOW-09]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-01 / KNOW-02 / KNOW-09 goal truths (tracer slice) ---
    - "After adding one typed Memory to a contact, that Memory renders on a reachable Things-to-Remember surface for that contact (end-to-end: migration 016 → memories-dao write → memories-read → ThingsToRememberScreen render)."
    - "The Memory-type set is application-owned: it lives only in the in-code MEMORY_TYPE_REGISTRY constant; there is no table, DAO, or migration that lets a user create a new Memory *type* (ADR-028 / D-05 boundary). User flexibility is the generic 'custom' type carrying a per-item custom_label."
    - "Migration 016 is additive-only: it CREATEs new tables and adds no destructive data-move, drops no table, and does not touch custom_field_values / custom_field_defs (D-03)."
    - "The relationships table carries a DB-level CHECK (linked_contact_id IS NULL OR linked_contact_id != contact_id), so SQLite itself rejects a self-link even from a future writer that bypasses the application assertNotSelfLink — defense-in-depth on the frozen, irreversible migration-016 schema for the HIGH-rated threat T-24.1-14 (review cycle-4 C1)."
    - "A fresh install migrating from ANY prior user_version reaches TARGET_VERSION 16 with the three new tables present and every prior table intact (no assumed starting state) — proven by a staged-upgrade test PARAMETERIZED across every prior version 0..15, not just representative samples, because migration 016 is irreversible and exhaustive coverage here is cheap (review N8)."
    # --- KNOW-02 edges (specless probe) ---
    - "KNOW-02 idempotency: registry seeding is idempotent by construction — MEMORY_TYPE_REGISTRY is an in-code constant, so no rows are minted for types and re-running app launch never duplicates a type."
    - "KNOW-02 concurrency: MEMORY_TYPE_REGISTRY is a read-only static constant with no launch-time write, so concurrent launches never race on it; Memory row writes go through the shared non-reentrant mutex."
    - "KNOW-02 adjacency: two Memories of the same type on one contact are stored and rendered as separate rows and never merged."
    - "KNOW-02 ordering: when two Memories compare equal, order is deterministic — pinned first, then meaningful_date DESC (NULLs last), then created_at DESC, then id DESC."
    - "KNOW-02 empty: a Memory draft with a blank/whitespace-only value AND no custom_label is not persisted (blank→NULL at the single commit boundary, mirroring FuelEditor)."
    - "KNOW-09 empty: a Memory with no explicit provenance defaults to provenance='user'; provenance is surfaced only in detail/edit views, never on the glanceable card."
    - { statement: "KNOW-02 encoding: memories.custom_label and memories.value store raw TEXT and round-trip a multi-byte / emoji / combining-grapheme string byte-for-byte (never truncated to satisfy a type).", verification: backstop }
  artifacts:
    - "src/db/migrations/016-contact-knowledge.ts — the single additive migration for the whole phase (memories, relationships, current_state_entries) + additive read indexes idx_memories_contact_deleted / idx_relationships_contact_deleted / idx_current_state_history (review N10) + a DB-level self-link CHECK (linked_contact_id IS NULL OR linked_contact_id != contact_id) on relationships (review cycle-4 C1)"
    - "src/db/memory-registry.ts — in-code application-owned Memory-type registry + provisional-label constants + the history-aware field-key contract (CURRENT_STATE_FIELD_KEYS / CURRENT_STATE_FIELD_REGISTRY) + RELATIONSHIPS_GROUP meta (review M1, L5)"
    - "src/db/memories-dao.ts — memories writer (addMemoryCore/addMemory to start) with runtime registry validation at the write boundary (review M6)"
    - "src/db/memories-read.ts — memories read choke point (listMemoriesForContact live projection)"
    - "src/screens/ThingsToRememberScreen.tsx — reachable Things-to-Remember surface (tracer render)"
    - "ThingsToRemember route registered in BOTH DashboardStackParamList and OrreryStackParamList (src/navigation/types.ts) and mounted in BOTH src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx (review H3 — Profile is mounted in both stacks)"
  key_links:
    - "database.ts MIGRATIONS array registers migration016 AND TARGET_VERSION is bumped to 16 — omitting either leaves the tables uncreated at launch"
    - "ThingsToRememberScreen reads through memories-read (never inline SQL) and resolves every colour through useTheme().colors.*"
    - "memories-dao writers mint uid via newUid() and call bumpDataRevisionCore inside the single inWriteTransaction (merge identity + backup-dirty signal)"
    - "Plan 04's setCurrentStateValue validates field_key against CURRENT_STATE_FIELD_KEYS and Plans 06/07 derive history-aware headers from CURRENT_STATE_FIELD_REGISTRY — the contract is defined HERE, in wave 1, so the three wave-2 consumers share one source (review M1)"
  prohibitions:
    - "The Memory-type SET must never become user-writable — a user-created system type reverses ADR-028 and is an owner escalation, not an implementation choice (D-05)."
  flagged_assumptions:
    - "The default/general built-in Memory-type NAME is unresolved (D-11) — this plan ships a single-source provisional constant (PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME) and surfaces the final name as human_needed. Do not invent the final name."
    - "Memories is a NEW table coexisting with fuel (not a reshape of fuel). Share-capture carry-over and any fuel↔memory consolidation are Phase 24.2 (assumption-delta: PROMOTE Memory as the primary knowledge noun, ADD alongside fuel; do not retire/reshape fuel here)."
    - "The universal FAB's existing `Memory` placeholder route (src/navigation/tabs/DashboardStack.tsx:42, MemoryPlaceholderScreen) is NOT this phase's surface and stays a placeholder until Phase 34 (Rapid Capture / Update Contact). This phase adds a separate ThingsToRemember route reached from the contact profile; do not wire, rename, or 'fix' the FAB placeholder here (review A5, advisory)."
---

<assumption_delta_decision>
noun: Memory (typed knowledge item) — now the primary knowledge noun of the model; `fuel` (fixed-kind conversational notes, ADR-028) remains as-is.
decision: add-alongside
rationale: Per the 24.1/24.2 split (CONTEXT.md, RESEARCH.md Open Question 3 RESOLVED) `memories` is a NEW table coexisting with `fuel`; promoting Memory to replace fuel (share-capture carry-over, ADR-030 retirement, off_limits/AI exclusions) is Phase 24.2's destructive migration 017, not 24.1. Nothing in 24.1 reads or writes `fuel`.
</assumption_delta_decision>

<objective>
Lead the phase with one production-quality end-to-end tracer: create the phase's single additive migration (016) with the complete new schema, define the in-code application-owned Memory-type registry, write the first memories DAO + read choke point, and render one typed Memory on a reachable Things-to-Remember surface — verified end-to-end before any expansion (KNOW-01/02/09).

Purpose: Prove the whole vertical (SQLite migration → mutexed DAO write → single read projection → themed RN render) works on the phase's best early-context tokens, and freeze the additive schema every later plan in this phase consumes. Migration 016 is one-way (reversibility gate) and additive-only per D-03.

This plan set is authored against the dossier read in full (D-01) and the planning notes as a binding appendix (D-02), and against the code on disk — the Contact Knowledge model is almost entirely unbuilt (D-04: no registry, no relationships table, no current-state history, hard-delete not soft-delete), so every table here is net-new.

Output: migration 016 (frozen contract for the phase), memory-registry.ts, memories-dao.ts + memories-read.ts (memories path), a reachable ThingsToRememberScreen rendering a real typed Memory, and the node:sqlite proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-CONTEXT.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-RESEARCH.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md
@docs/dossier/milestone-2/phase-03-contact-knowledge-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-03-planning-notes.md
@docs/decisions/ADR-028-per-item-conversational-fuel-with-fixed-kinds.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: migration 016 is one-way + registry stays application-owned (ADR-028)</name>
  <reversibility rating="one-way">Migration 016 runs once on every device and cannot be rolled back on an upgraded user database (ADR-009); its shape is frozen the moment it ships.</reversibility>
  <decision>
    Confirm before writing the migration:
    1. Migration 016 is additive-only and forward-only — it CREATEs the three new tables (memories, relationships, current_state_entries) and their columns, performs NO destructive data-move, drops nothing, and does not touch custom_field_values/custom_field_defs (D-03). It is one migration (not 016+017 — Phase 24.2 owns 017).
    2. The Memory-type registry is APPLICATION-OWNED and in-code only. Users get flexibility ONLY through the generic 'custom' type + a per-item custom_label. Building any path that lets a user create a new Memory *type* would reverse ADR-028 (D-05) — STOP and ask the owner if the design ever seems to require it.
    3. The default/general built-in Memory-type NAME is unresolved (D-11) → ship a single-source provisional constant and mark the final name human_needed. Do not invent it.
  </decision>
  <options>
    - **Approve (proceed):** Author migration 016 as a single additive migration with the three tables; registry stays application-owned; default type name shipped as a provisional constant + human_needed. (Recommended — matches D-03/D-05/D-11.)
    - **Adjust schema shape:** Owner amends the table/column set before it is frozen (e.g. splits or merges a table). Re-derive the migration from the amended shape.
    - **Halt:** If the design appears to require a user-writable Memory *type* set (reverses ADR-028) or a destructive data-move (24.2 scope), STOP — do not proceed; escalate to the owner.
  </options>
  <resume-signal>Owner approves the additive-only single-migration + application-owned-registry framing (or supplies an amended schema shape); the default Memory-type name is recorded as human_needed.</resume-signal>
  <read_first>
    - docs/decisions/ADR-028-per-item-conversational-fuel-with-fixed-kinds.md
    - docs/dossier/milestone-2/planning-notes/phase-03-planning-notes.md (R-01 trip-wires)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-CONTEXT.md (D-03, D-05, D-11)
  </read_first>
  <acceptance_criteria>
    - Owner (or pre-approval) confirms the additive-only, single-migration, application-owned-registry framing before the migration file is authored.
    - The default Memory-type name is recorded as human_needed, not chosen.
  </acceptance_criteria>
  <done>Decision recorded in the plan summary; execution proceeds to the migration task.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Migration 016 — the phase's full additive schema (three new tables)</name>
  <files>src/db/migrations/016-contact-knowledge.ts, src/db/migrations/016-contact-knowledge.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts</files>
  <read_first>
    - src/db/migrations/007-tombstones.ts (table-creating migration shape: named CREATE consts, version field, apply(exec, deps), one execAsync per statement)
    - src/db/migrations/006-normalize-custom-field-values.ts (header only — additive vs destructive contrast; do NOT copy the data-move)
    - src/db/database.ts (MIGRATIONS array + TARGET_VERSION=15 on disk; localDateTime)
    - src/db/migrations/full-chain.test.ts (extend to the new TARGET_VERSION)
    - src/db/types.ts (Migration, MigrationDeps, SqlExecutor)
  </read_first>
  <behavior>
    - Applying the full chain reaches user_version 16; the three new tables exist with exactly the specified columns.
    - Staged upgrades succeed with no assumed starting state (review M4, N8): on a fresh node:sqlite DB run `runMigrations(exec, MIGRATIONS, N, deps)` to a GENUINE prior target N — PARAMETERIZED over EVERY prior version N in 0..15 (a cheap loop, preferred over representative samples because migration 016 is irreversible) — assert `PRAGMA user_version` = N, then run `runMigrations(exec, MIGRATIONS, 16, deps)` and assert user_version = 16 with the three new tables present. Never spoof `PRAGMA user_version` on a full-schema DB — the runner selects migrations by user_version only (`runner.ts:38-45`), so a spoofed version does not produce a real old-schema database. (`vi.mock("expo-sqlite", () => ({}))` at the top of the test, exactly as `full-chain.test.ts:3`, because `database.ts` imports expo-sqlite.)
    - memories.uid / relationships.uid / current_state_entries.uid each enforce UNIQUE; a duplicate uid insert throws.
    - The partial unique index on current_state_entries(contact_id, field_key) WHERE is_current=1 rejects a second current row for the same (contact, field).
    - Three additive read indexes exist for the phase's recurring query paths (review N10): idx_memories_contact_deleted on memories(contact_id, deleted_at, id) and idx_relationships_contact_deleted on relationships(contact_id, deleted_at, id) for the live/trash reads, and idx_current_state_history on current_state_entries(contact_id, field_key) for full history reads (all rows for a field, incl. is_current=0). These are baked into 016 now because it is a frozen contract; the custom-field index/UNIQUE ban applies ONLY to the retired dynamic-column table, not to these new tables.
    - ON DELETE CASCADE from contacts removes dependent memories/relationships/current_state_entries rows; relationships.linked_contact_id is ON DELETE SET NULL.
    - The relationships table's DB-level self-link CHECK rejects any INSERT or UPDATE that sets linked_contact_id = contact_id (a would-be self-link) at the SQLite level; a NULL linked_contact_id is always accepted, and ON DELETE SET NULL nulling a link never trips the CHECK (review cycle-4 C1).
    - No prior table (contacts, fuel, custom_field_defs, custom_field_values, field_history, tombstones, app_settings) is dropped or altered by 016.
  </behavior>
  <action>
    Verify head+1 on disk first: confirm TARGET_VERSION is 15 in src/db/database.ts and the newest migration file is 015 — the first new migration is 016 (halt and re-derive if either differs).

    Create src/db/migrations/016-contact-knowledge.ts modelled on 007-tombstones.ts: export a named CREATE constant per table and a migration016 object with version 16 whose apply runs one execAsync per statement in order. Create three tables:

    memories — columns in this order: id INTEGER PRIMARY KEY; uid TEXT UNIQUE NOT NULL; contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE; type TEXT NOT NULL; custom_label TEXT; value TEXT; note TEXT; url TEXT; meaningful_date TEXT; pinned INTEGER NOT NULL DEFAULT 0; outdated INTEGER NOT NULL DEFAULT 0; hidden INTEGER (nullable three-state: NULL inherit type default, 0 show, 1 hide); provenance TEXT NOT NULL DEFAULT 'user'; created_at TEXT NOT NULL; modified_at TEXT NOT NULL; deleted_at TEXT (NULL=live, non-NULL=in Recently Deleted).

    relationships — id INTEGER PRIMARY KEY; uid TEXT UNIQUE NOT NULL; contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE; person_name TEXT NOT NULL; relation_type TEXT; linked_contact_id INTEGER REFERENCES contacts(id) ON DELETE SET NULL; note TEXT; pinned INTEGER NOT NULL DEFAULT 0; hidden INTEGER; created_at TEXT NOT NULL; modified_at TEXT NOT NULL; deleted_at TEXT. Add a table-level `CHECK (linked_contact_id IS NULL OR linked_contact_id != contact_id)` as a DB-level self-link guard — defense-in-depth for the HIGH-rated threat T-24.1-14 (Plan 03) alongside the application `assertNotSelfLink` and the merge bidirectional SET-NULL (review cycle-4 C1). CHECK is an established idiom in this repo — migrations 001/002/009/011/012/013 use it (the closest analog is 011-contact-lifecycle-schema.ts:36-37, a two-column table-level CHECK); read one before writing the DDL. This CHECK is additive on a NEW table and does NOT touch the retired dynamic-column table the CHECK/index ban covers. It never conflicts with ON DELETE SET NULL (a nulled link satisfies `IS NULL`) and never fires during a legitimate merge (Plan 03's pre-reparent bidirectional SET-NULL nulls every would-be self-link before the reparent UPDATE runs).

    current_state_entries — id INTEGER PRIMARY KEY; uid TEXT UNIQUE NOT NULL; contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE; field_key TEXT NOT NULL; value TEXT NOT NULL; is_current INTEGER NOT NULL DEFAULT 0; created_at TEXT NOT NULL; modified_at TEXT NOT NULL. Add a partial unique index idx_current_state_current ON current_state_entries(contact_id, field_key) WHERE is_current = 1 to guarantee at most one current value per (contact, field). Also create three ADDITIVE read indexes for the phase's live/trash/history query paths (review N10 — 016 is a frozen contract authored now, so an index missed here needs a whole new later migration; the custom-field index/UNIQUE ban applies ONLY to the retired dynamic-column table, NOT to memories/relationships/current_state_entries): idx_memories_contact_deleted ON memories(contact_id, deleted_at, id); idx_relationships_contact_deleted ON relationships(contact_id, deleted_at, id); and idx_current_state_history ON current_state_entries(contact_id, field_key) (this one complements idx_current_state_current for full history reads that also scan is_current = 0 rows). In 016-contact-knowledge.test.ts, confirm each index earns its place with an EXPLAIN QUERY PLAN assertion on a realistic fixture (a live/trash read uses idx_*_contact_deleted; a history read uses idx_current_state_history); keep them purely additive — no existing index removed or renamed.

    Register migration016 in the MIGRATIONS array in src/db/database.ts and bump TARGET_VERSION to 16. Never edit a shipped migration 001-015. Extend full-chain.test.ts so the chain asserts through 16, and add 016-contact-knowledge.test.ts covering the behavior block via the node:sqlite testkit. Do NOT add any AI-permission column, custom-field scope column, value-history table, or backup/tombstone declaration — those belong to Phase 24.2.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/016-contact-knowledge.test.ts src/db/migrations/full-chain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/migrations/016-contact-knowledge.test.ts src/db/migrations/full-chain.test.ts` passes.
    - full-chain.test.ts asserts `expect(TARGET_VERSION).toBe(16)` and `MIGRATIONS.filter((m) => m.version === 16)` has length 1 (extend the existing per-version assertions at `full-chain.test.ts:24-45`).
    - 016-contact-knowledge.test.ts contains a staged-upgrade test PARAMETERIZED over every prior version N in 0..15 (e.g. `for (const N of [0,1,2,…,15])` or `it.each`) that calls `runMigrations(exec, MIGRATIONS, N, …)` before migrating to 16 and asserts user_version 16 + the three new tables present (review N8); `grep -n "user_version = \|PRAGMA user_version = " src/db/migrations/016-contact-knowledge.test.ts` (comment-filtered) returns nothing — no spoofed version writes (review M4).
    - Grep confirms additive-only: `grep -nE 'DROP TABLE|ALTER TABLE (custom_field_values|custom_field_defs)|DELETE FROM' src/db/migrations/016-contact-knowledge.ts` returns nothing (filtered of comment lines).
    - `grep -nE 'idx_memories_contact_deleted|idx_relationships_contact_deleted|idx_current_state_history' src/db/migrations/016-contact-knowledge.ts` shows all three additive read indexes created, and 016-contact-knowledge.test.ts contains an EXPLAIN QUERY PLAN assertion that a live/trash read uses the contact index (review N10).
    - `grep -n "linked_contact_id != contact_id" src/db/migrations/016-contact-knowledge.ts` shows the DB-level self-link CHECK on the relationships table, and 016-contact-knowledge.test.ts asserts SQLite rejects both an INSERT and an UPDATE that would set linked_contact_id = contact_id (each throws), while an INSERT with a NULL linked_contact_id succeeds (review cycle-4 C1).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Migration 016 creates the three additive tables, is registered, TARGET_VERSION is 16, the chain applies from any prior version, and all new tests are green.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: In-code Memory-type registry + memories DAO write + read choke point</name>
  <files>src/db/memory-registry.ts, src/db/memory-registry.test.ts, src/db/memories-dao.ts, src/db/memories-dao.test.ts, src/db/memories-read.ts</files>
  <read_first>
    - src/db/fuel-dao.ts (core/wrapper split over the non-reentrant mutex; both-keys scoping; assertOneChange; ?-bound values, static column names only; newUid + bumpDataRevisionCore inside one inWriteTransaction)
    - src/db/fuel-read.ts (single read choke point; editor projection posture; only column names literal, contact_id ?-bound)
    - src/db/uid.ts (newUid), src/db/transaction.ts (inWriteTransaction), src/db/data-revision-dao.ts (bumpDataRevisionCore)
    - src/services/fuel-ranking.ts (FUEL_KIND_PRIORITY as the in-code-constant analog for the registry)
  </read_first>
  <behavior>
    - MEMORY_TYPE_REGISTRY exposes at least the default/general type (key from DEFAULT_MEMORY_TYPE_KEY) and the 'custom' type; each entry carries displayName, iconSemantic, cardinality ('single'|'many'), historyAware, searchable, aiDefault, visibilityDefault ('show'|'hide'), presentationOrder. It is a Record keyed by MemoryTypeKey so an unlisted type fails tsc.
    - The history-aware field-key contract is defined here (review M1): CurrentStateFieldKey = 'last_talked_about' | 'current_location'; CURRENT_STATE_FIELD_KEYS is the readonly array of those keys; CURRENT_STATE_FIELD_REGISTRY is a Record<CurrentStateFieldKey, CurrentStateFieldMeta> carrying displayName, iconSemantic, presentationOrder; isCurrentStateFieldKey(value: string) is a pure type guard. Plan 04 validates field_key against CURRENT_STATE_FIELD_KEYS; Plans 06/07 derive headers from CURRENT_STATE_FIELD_REGISTRY.
    - RELATIONSHIPS_GROUP is an exported constant carrying the relationships group's displayName, visibilityDefault ('show'|'hide'), and presentationOrder, so relationships.hidden has a registry default to resolve against (review L5; dossier §G "types/groups have visibility defaults").
    - addMemory persists a typed Memory row: mints uid via newUid, stores blank optionals (value/note/url/custom_label) as NULL, defaults provenance to 'user' when absent, and bumps data revision inside one inWriteTransaction.
    - addMemory validates at the write boundary (review M6): a `type` not present as a key of MEMORY_TYPE_REGISTRY throws BEFORE any write (assertRegisteredMemoryType); a 'custom'-type Memory with a blank/absent custom_label throws before any write; addMemory of the default type with a NULL custom_label is valid.
    - listMemoriesForContact returns a contact's LIVE (deleted_at IS NULL) memories newest-first with a deterministic tiebreak (pinned DESC, meaningful_date DESC NULLs-last, created_at DESC, id DESC); returns [] for a contact with none; rows for other contacts are excluded (contact_id ?-bound).
  </behavior>
  <action>
    Create src/db/memory-registry.ts (react-native-free, node-testable, import type only). Export: the MemoryTypeKey union (start with 'general' | 'custom' — extensible by a one-line edit, application-owned per D-05); DEFAULT_MEMORY_TYPE_KEY constant; the provisional single-source label constants PROVISIONAL_MEMORY_LABEL and PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME (both flagged human_needed for the final name, D-11); a MemoryTypeMeta interface; MEMORY_TYPE_REGISTRY typed Record<MemoryTypeKey, MemoryTypeMeta>; isMemoryTypeKey(value: string): value is MemoryTypeKey (pure guard over Object.keys(MEMORY_TYPE_REGISTRY)); and KNOWLEDGE_GROUP_ORDER (an ordered list the Things-to-Remember surface uses to place featured/current information first). Note in-file that searchable/aiDefault are declared metadata NOT consumed in 24.1 (search + AI egress are Phase 24.2).

    In the same file define the history-aware field-key contract (review M1 — the wave-2 Plans 04/06/07 all consume it, so it must exist in wave 1): export type CurrentStateFieldKey = 'last_talked_about' | 'current_location'; export const CURRENT_STATE_FIELD_KEYS: readonly CurrentStateFieldKey[]; export interface CurrentStateFieldMeta { displayName; iconSemantic; presentationOrder }; export const CURRENT_STATE_FIELD_REGISTRY: Record<CurrentStateFieldKey, CurrentStateFieldMeta>; export function isCurrentStateFieldKey(value: string): value is CurrentStateFieldKey. Place both keys in KNOWLEDGE_GROUP_ORDER's featured/current position. Also export RELATIONSHIPS_GROUP: { displayName: string; visibilityDefault: 'show' | 'hide'; presentationOrder: number } (review L5) — displayName is a provisional single-source string (dossier defers final section names) defaulting to visibilityDefault 'show'.

    Create src/db/memories-dao.ts following fuel-dao.ts exactly: a non-mutexed addMemoryCore(exec, input) that INSERTs the memories columns in migration order (every value ?-bound, only column names literal), and a wrapper addMemory that owns one inWriteTransaction and calls bumpDataRevisionCore inside it. Before the INSERT, validate at the write boundary (review M6): export assertRegisteredMemoryType(type: string): asserts type is MemoryTypeKey (throws `memories-dao: unregistered memory type` for any key not in MEMORY_TYPE_REGISTRY) and require a non-blank custom_label when type === 'custom' (throw before any write). Plan 02's editMemory reuses the same assertion. Normalize blank optionals to NULL at this boundary. Provenance is a structured column value (documented vocabulary 'user' | 'import' | 'share'), replacing fuel.source's bare-string idiom (KNOW-09) — default 'user'.

    Create src/db/memories-read.ts as the single read choke point: export the MemoryRow interface and listMemoriesForContact(exec, contactId) returning live memories with the deterministic ORDER BY (pinned DESC, COALESCE over meaningful_date for NULLs-last, created_at DESC, id DESC) and a WHERE deleted_at IS NULL AND contact_id = ?. Add memory-registry.test.ts (registry shape + default key present + CURRENT_STATE_FIELD_KEYS covers exactly the registry's keys + isCurrentStateFieldKey rejects 'bogus') and memories-dao.test.ts (add + list round-trip, blank→NULL, provenance default, other-contact isolation, unregistered-type rejection with zero rows written, custom-without-label rejection) via the node:sqlite testkit.
  </action>
  <verify>
    <automated>npx vitest run src/db/memory-registry.test.ts src/db/memories-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/memory-registry.test.ts src/db/memories-dao.test.ts` passes, including the unregistered-type and custom-without-label rejection cases (review M6).
    - `grep -n "CURRENT_STATE_FIELD_KEYS\|CURRENT_STATE_FIELD_REGISTRY\|isCurrentStateFieldKey\|RELATIONSHIPS_GROUP" src/db/memory-registry.ts` shows all four exported (review M1, L5).
    - `grep -n "assertRegisteredMemoryType" src/db/memories-dao.ts` shows the assertion exported and called inside addMemoryCore before the INSERT.
    - Grep confirms no nested mutex: memories-dao cores contain no inWriteTransaction call; only the wrappers do (`grep -c inWriteTransaction src/db/memories-dao.ts` matches the wrapper count).
    - Grep confirms registry is a constant, not a table: `grep -niE 'CREATE TABLE|INSERT INTO .*type' src/db/memory-registry.ts` (comment-filtered) returns nothing.
    - tsc --noEmit + `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The registry constant, memories writer, and read choke point exist, follow the core/wrapper mutex pattern, and pass node:sqlite tests.</done>
</task>

<task type="tracer">
  <name>Task 3: Reachable Things-to-Remember surface rendering one typed Memory</name>
  <files>src/screens/ThingsToRememberScreen.tsx, src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/components/FuelEditor.tsx (parent-owns-persistence posture; token-only colours; the sibling this surface mirrors)
    - src/navigation/types.ts (DashboardStackParamList AND OrreryStackParamList — Profile/Edit/Compose are declared in BOTH param lists; the additive-route idiom used by NeverContacted/ManageFavourites/Compose)
    - src/navigation/tabs/DashboardStack.tsx (Stack.Screen registrations for the Dashboard tab — lines 34-59; this is where a Dashboard-reachable screen registers, NOT RootNavigator.tsx which declares only the four tabs)
    - src/navigation/tabs/OrreryStack.tsx (mounts Profile/Edit/Compose a SECOND time for the Orrery-origin profile — lines 17-24; any screen the Profile navigates to must be registered here too)
    - src/screens/ContactProfileScreen.tsx (how the profile mounts FuelEditor and navigates; the additive entry point)
    - src/db/memories-read.ts (listMemoriesForContact — the read this screen consumes)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (Typography/Color/Copywriting — group header = registry display name; body = Memory value; empty state copy)
  </read_first>
  <action>
    Register a serializable route ThingsToRemember: { contactId: number } in BOTH DashboardStackParamList AND OrreryStackParamList (src/navigation/types.ts) additively, following the existing Compose/Profile/Edit dual-declaration precedent (review H3: ContactProfileScreen is mounted under both the Dashboard and Orrery stacks, so a Dashboard-only route is unreachable from an Orrery-origin profile). Mount the screen with `<Stack.Screen name="ThingsToRemember" component={ThingsToRememberScreen} />` in BOTH src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx. Do NOT touch src/navigation/RootNavigator.tsx — it declares only the four Tab.Screens (lines 176-207) and registers no stack routes. initialRouteName stays Home/Orrery and no existing route changes. Leave the FAB's `Memory` placeholder route (DashboardStack.tsx:42) untouched — it belongs to Phase 34.

    Create src/screens/ThingsToRememberScreen.tsx: read the contact's memories via listMemoriesForContact in a useFocusEffect with a cancelled-flag async guard (the offline read posture from FuelSearch/NeverContacted; no blocking network on the read path), and render each typed Memory as a card showing its registry display name (from MEMORY_TYPE_REGISTRY, or custom_label for the 'custom' type via the PROVISIONAL_MEMORY_LABEL single-source constant) and its value. Use the display/heading/body/label/caption typography roles and resolve every colour through useTheme().colors.* (no hex, no raw Ionicons name). Render the "Nothing to remember yet" empty state when there are no memories.

    Add one additive, low-emphasis entry on ContactProfileScreen that navigates to ThingsToRemember with the contactId (do not restructure the profile — Phase 31 owns the Profile factory order and will absorb this surface). Because the profile is mounted in two stacks, the navigate call must be a plain `navigation.navigate("ThingsToRemember", { contactId })` against the stack the profile is currently in (the same way its existing Edit/Compose entries navigate) — never a cross-tab `navigate("DashboardTab", …)` jump. This is the tracer's reachability path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit clean and `npm run check:colors` reports no raw colour in the new/changed files.
    - `grep -c "ThingsToRemember" src/navigation/types.ts` is >= 2 (declared in both DashboardStackParamList and OrreryStackParamList).
    - `grep -n 'name="ThingsToRemember"' src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx` shows one Stack.Screen registration in EACH file (review H3).
    - `git diff --stat -- src/navigation/RootNavigator.tsx` is empty — RootNavigator.tsx is not modified.
    - `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" src/screens/ThingsToRememberScreen.tsx` (comment-filtered) returns nothing.
    - Device UAT (deferred to phase gate): from a Dashboard-origin profile AND from an Orrery-origin profile, the new entry opens ThingsToRememberScreen and a Memory added via addMemory renders there.
  </acceptance_criteria>
  <done>A typed Memory added through the DAO renders on a reachable, token-clean Things-to-Remember surface — the tracer path is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (ThingsToRememberScreen / profile) → DAO SQL | User-authored Memory text crosses into SQLite via memories-dao |
| App launch → migration runner | Migration 016 rewrites on-device schema irreversibly |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-01 | Tampering | memories-dao INSERT | medium | mitigate | Every value ?-bound; only static column names literal; no user identifier interpolated (fuel-dao precedent) |
| T-24.1-03 | Denial of Service (data) | migration 016 on unreachable devices | high | mitigate | Forward-only, additive-only, no assumed start state, one statement per execAsync, node:sqlite full-chain proof + checkpoint:decision gate before authoring |
| T-24.1-05 | Information Disclosure | provenance / registry metadata | low | accept | No AI egress or search ships in 24.1; registry aiDefault/searchable are declared-only metadata with no consumer |
| T-24.1-15 | Tampering / integrity | memories.type outside the registry | medium | mitigate | assertRegisteredMemoryType at every write boundary (addMemory here, editMemory in Plan 02) rejects a type not in MEMORY_TYPE_REGISTRY before any write; 'custom' requires a non-blank custom_label; readers tolerate an unknown persisted type with a documented fallback (Plan 02) — review M6 |
| T-24.1-SC | Tampering | package installs | low | accept | No new packages this plan (in-repo schema/DAO/UI only) — no package-legitimacy gate needed |
</threat_model>

<verification>
- `npx vitest run src/db/migrations/016-contact-knowledge.test.ts src/db/migrations/full-chain.test.ts src/db/memory-registry.test.ts src/db/memories-dao.test.ts` all green.
- tsc --noEmit + `npm run check:colors` clean across changed files.
- Additive-only + no-custom-field-touch greps (Task 1) return nothing.
- ThingsToRemember route registered in both stacks and reachable from a Dashboard-origin and an Orrery-origin contact profile (device UAT at phase gate).
- Unregistered-type / custom-without-label write rejection tests green (review M6).
</verification>

<success_criteria>
The additive migration 016 is frozen and registered, the application-owned Memory-type registry exists in code, a typed Memory can be written through the mutexed DAO and read through the single choke point, and it renders on a reachable Things-to-Remember surface — the phase's vertical slice is proven before expansion.
</success_criteria>

## Artifacts this plan produces

- **New tables (migration 016):** memories, relationships, current_state_entries (relationships + current_state_entries created here but their DAOs are Plans 03/04).
- **Migration:** src/db/migrations/016-contact-knowledge.ts (version 16); TARGET_VERSION bumped to 16; registered in database.ts MIGRATIONS. Includes the partial unique index idx_current_state_current plus three additive read indexes idx_memories_contact_deleted / idx_relationships_contact_deleted / idx_current_state_history (review N10).
- **Registry symbols (src/db/memory-registry.ts):** MemoryTypeKey, DEFAULT_MEMORY_TYPE_KEY, PROVISIONAL_MEMORY_LABEL, PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME (human_needed), MemoryTypeMeta, MEMORY_TYPE_REGISTRY, isMemoryTypeKey, KNOWLEDGE_GROUP_ORDER; the history-aware field-key contract CurrentStateFieldKey, CURRENT_STATE_FIELD_KEYS, CurrentStateFieldMeta, CURRENT_STATE_FIELD_REGISTRY, isCurrentStateFieldKey (review M1); RELATIONSHIPS_GROUP (review L5).
- **DAO/read:** src/db/memories-dao.ts (addMemoryCore/addMemory, assertRegisteredMemoryType), src/db/memories-read.ts (listMemoriesForContact, MemoryRow).
- **UI/route:** src/screens/ThingsToRememberScreen.tsx; ThingsToRemember route in DashboardStackParamList + OrreryStackParamList (src/navigation/types.ts) and Stack.Screen registrations in src/navigation/tabs/DashboardStack.tsx + src/navigation/tabs/OrreryStack.tsx; additive entry on ContactProfileScreen.
- **Tests:** src/db/migrations/016-contact-knowledge.test.ts (incl. staged-upgrade cases), full-chain.test.ts (extended), memory-registry.test.ts, memories-dao.test.ts.
- **Launch-sweep hooks:** none this plan (Trash sweep is Plan 05).

## Review dispositions (cycle 1)

Consolidated map of every counted cycle-1 finding in `24.1-REVIEWS.md` (3 HIGH + 12 actionable). Each affected plan also carries its own `## Review dispositions (cycle 1)` pointer section naming the IDs it implements.

| ID | Severity | Disposition | Where |
|----|----------|-------------|-------|
| H1 | HIGH | INCORPORATED | Plan 06 Task 2 (inline "Set new value" on each history-aware row → `setCurrentStateValue`) + Plan 07 Task 2 (`MemoryHistoryScreen` "Set new value" primary action → `setCurrentStateValue`); must-have truths + acceptance greps in both |
| H2 | HIGH | INCORPORATED | Plan 03 Task 3 (`src/db/merge-dao.ts` reparent list += memories / relationships / current_state_entries; `relationships.linked_contact_id` re-pointed absorbed→survivor; single-current partial-index collision demotes the absorbed entry; `merge-dao.test.ts` cases) |
| H3 | HIGH | INCORPORATED | Plan 01 Task 3, Plan 06 Task 3, Plan 07 Tasks 1-2: routes declared in BOTH `DashboardStackParamList` and `OrreryStackParamList`; screens mounted in BOTH `tabs/DashboardStack.tsx` and `tabs/OrreryStack.tsx`; `RootNavigator.tsx` removed from every `files_modified` |
| M1 | MEDIUM | INCORPORATED | Plan 01 Task 2 (`CurrentStateFieldKey`, `CURRENT_STATE_FIELD_KEYS`, `CURRENT_STATE_FIELD_REGISTRY`, `isCurrentStateFieldKey` in `memory-registry.ts`); Plan 04 validates against it; Plans 06/07 derive headers from it |
| M2 | MEDIUM | INCORPORATED | Plan 06 Task 2 load contract: `listDefs(exec, { includeQuarantined: false })` → `getValuesForContact(exec, contactId, defs)` → `visibleDefsForProfile(defs, values)`; composition grep includes `listDefs` + `visibleDefsForProfile` |
| M3 | MEDIUM | INCORPORATED (Undo-only, bounded) | Plan 07 Task 3 passes `onRestore` to `RelationshipEditor` + Undo snackbar via `restoreRelationship`; Plan 05 Task 2 sweep also expires soft-deleted relationships — **⚠ SUPERSEDED by cycle-2 N1** (this row's original "via Plan 03's guarded `purgeRelationshipPermanently`" is stale): the sweep now calls Plan 03's `expireRelationshipIfStale` (the under-lock past-window re-check), NOT `purgeRelationshipPermanently`, which stays the user "delete now" action — see § Review dispositions (cycle 2), N1; the dossier (§I) decides Recently-Deleted restore for Memories only, so relationships are Undo-only — recorded in Plan 03/07 `flagged_assumptions` for the owner |
| M4 | MEDIUM | INCORPORATED | Plan 01 Task 1: staged `runMigrations(exec, MIGRATIONS, N, …)` upgrades — **⚠ SUPERSEDED by cycle-3 N8** (this row's original "N = 1, 8, 15" representative sample is stale): the staged-upgrade test is now PARAMETERIZED across every prior version N in 0..15 — see § Review dispositions (cycle 3), N8 — then migrates to 16; spoofed `user_version` writes grep-forbidden |
| M5 | MEDIUM | INCORPORATED | Plan 03 Task 3: explicit purge fan-out + `computeImpact` / `impactSummaryLines` coverage for the three tables in `src/db/purge-dao.ts` via a parallel non-tombstoned `KNOWLEDGE_PURGE_CHILDREN` (tombstone entity types + backup are 24.2 KNOW-15); `restore-apply.ts` reset cascade recorded as a `flagged_assumptions` entry in Plans 03 and 05 |
| M6 | MEDIUM | INCORPORATED | Plan 01 Task 2 (`assertRegisteredMemoryType` in `addMemory`, custom requires label) + Plan 02 Tasks 1/3 (`editMemory` reuses it; `resolveVisibility` tolerates an unknown persisted type with a documented fallback + tests) |
| M8 | MEDIUM | INCORPORATED | Plan 04 Task 3 adds `src/db/first-class-knowledge-read.ts` (`getFirstClassFields`: birthday, social_battery, interval_days, category name — the dossier §A stable set that exists on disk); Plan 06 Task 2 renders them read-only in the featured/current group; Gravity/Closeness (no column on disk) and the exact card appearance recorded as `flagged_assumptions` for the owner / Phase 31 |
| L1 | LOW | INCORPORATED | Plan 06 Task 1 + must_haves: Off Limits marker/styling removed from `MemoryCard` (no off-limits Memory type exists in 24.1; KNOW-12 is 24.2) |
| L2 | LOW | INCORPORATED | Plan 05: clock truth reworded (backward jump only delays; forward jump accepted wall-clock semantics); test technique = fixtures write `deleted_at` via `datetime('now','localtime', ?)` offsets; injectable `now` documented as write-stamp only |
| L3 | LOW | INCORPORATED | Plan 03 Task 1: `linked_contact_id === contact_id` rejected at the commit boundary in add + edit (tested); Plan 07 Task 3: `ContactPicker` gains an optional additive `excludeContactId` prop |
| L4 | LOW | INCORPORATED | Plan 02 Task 3 (out-of-range `hidden` resolves to the type default) + Plan 04 Task 2 (promote the already-current entry is no-op-safe, prior value preserved) |
| L5 | LOW | INCORPORATED (consume) | Plan 01 Task 2 `RELATIONSHIPS_GROUP.visibilityDefault`; Plan 03 Task 2 `resolveRelationshipVisibility(hidden)`; Plan 06 Task 2 applies it to the Key People group; Plan 07 Task 3 editor exposes Hide/Show — `relationships.hidden` stays in migration 016 |

Advisory (not counted, handled cheaply): V11/V13 no-tombstone-on-purge → `flagged_assumptions` line added to Plan 05; V12 UI-SPEC `sparkle` contradiction → Plan 06 dispositions note (doc-sync of the UI-SPEC is an orchestrator item, the plan is correct per CONTEXT D-08/D-09); V16 batched current-state read → Plan 04 Task 3 `getCurrentStateValues`; A5 FAB `Memory` placeholder → Plan 01 `flagged_assumptions`; fact-drift `kind`→`type` in Plan 02 truth fixed; the unrunnable `node -e` acceptance line dropped.

## Review dispositions (cycle 2)

Consolidated map of every current actionable cycle-2 finding in `24.1-REVIEWS.md` (1 HIGH + 2 MEDIUM + 2 LOW). Cycle-1 rows above are unchanged and were NOT re-touched. Each affected plan carries its own `## Review dispositions (cycle 2)` pointer section.

| ID | Severity | Disposition | Where |
|----|----------|-------------|-------|
| N1 | HIGH | INCORPORATED | Plan 05 Task 1 adds `expireMemoryIfStale` + Plan 03 Task 1 adds `expireRelationshipIfStale` — mutex-owning writers that re-verify the FULL past-window predicate (`deleted_at IS NOT NULL AND deleted_at < datetime('now','localtime', ?)`, strict `<`) UNDER the write lock before deleting via the non-mutexed core, mirroring `field-ddl.ts:232-252` `expireFieldIfStale` (verified on disk). Plan 05 Task 2's launch sweep calls THOSE instead of the existence-only `purgeMemoryPermanently`/`purgeRelationshipPermanently` (which stay the user "delete now" action), closing the restore-then-redelete-within-window TOCTOU data-loss hole. The overclaiming line-31 TOCTOU must-have corrected; restore-then-redelete race test added to the sweep test (both tables); T-24.1-07/08/09 mitigations updated |
| N2 | MEDIUM | INCORPORATED | Plan 06 Task 1: `MemoryEditor` renders a read-only provenance caption from `MemoryRow.provenance` for an existing item (KNOW-09 detail/edit rendering path), not editable, absent on create; must-have truths updated + grep acceptance + UAT step. `MemoryCard` still shows no provenance |
| N3 | MEDIUM | INCORPORATED | Plan 05 Task 2 acceptance: a verification line + test asserting `memories.deleted_at` and `relationships.deleted_at` are both `localDateTime()`-format (migration 016) and directly comparable to the sweep's `datetime('now','localtime', ?)` predicate |
| N4 | LOW | INCORPORATED | Plan 04 Task 3: `getCurrentStateValues` malformed/legacy-`field_key` behavior made an explicit contract (excluded from the map, never deleted — KNOW-03 retention) + a test asserting the row is excluded but still present in `current_state_entries` |
| N5 | LOW | INCORPORATED (gate-exclusion) | Plan 06 Task 2: the empty-state gate excludes the near-always-populated Category + Contact Frequency columns so UI-SPEC E1 stays reachable; narrows ONLY the gate — the owner-protected read-only first-class SET (birthday/social_battery/interval_days/category) is unchanged. Recorded in a must-have truth, the action, acceptance grep + UAT, and `flagged_assumptions` |

Protected pending owner decisions (NOT resolved this cycle — remain `flagged_assumptions`): (1) relationships are Undo-only and NOT listed in Recently Deleted (dossier §I, Memories-only) — Plan 03/07; (2) the read-only first-class field set is birthday/social_battery/interval_days/category with Gravity/Closeness excluded (no column) — Plan 04 Task 3 / Plan 06; N5's gate change does NOT alter this set.

## Review dispositions (cycle 3)

Consolidated map of every current actionable cycle-3 finding in `24.1-REVIEWS.md` (1 HIGH + 4 LOW). Cycle-1 and cycle-2 rows above are unchanged and were NOT re-touched. Each affected plan carries its own `## Review dispositions (cycle 3)` pointer section. Both source lanes confirmed all 20 prior-cycle findings remain incorporated — no regressions.

| ID | Severity | Disposition | Where |
|----|----------|-------------|-------|
| N6 | HIGH | INCORPORATED | Plan 03 Task 3: the pre-reparent self-link SET-NULL was one-directional (`WHERE linked_contact_id = absorbed AND contact_id IN (survivor, absorbed)`) and missed the symmetric `contact_id = absorbed AND linked_contact_id = survivor` row, which `reparent()` (merge-dao.ts:89) would flip into a persisted self-link bypassing `assertNotSelfLink`. Fixed: SET-NULL generalized to `WHERE contact_id IN (survivor, absorbed) AND linked_contact_id IN (survivor, absorbed)` (both directions); named "absorbed-owns-row / survivor-is-target" merge-dao.test.ts case added; post-merge invariant `SELECT COUNT(*) FROM relationships WHERE contact_id = linked_contact_id` = 0 asserted; threat-model T-24.1-14 raised to high/mitigate |
| N7 | LOW | INCORPORATED (note) | Plans 03 + 05: one-line note in each that the expiry/purge writers (`expireRelationshipIfStale`/`purgeRelationshipPermanently`, `expireMemoryIfStale`/`purgeMemoryPermanently`) intentionally do NOT bump `data_revision` — mirroring `field-ddl.ts` `expireFieldIfStale` and `fuel-dao.ts` `deleteFuel` (both verified on disk to skip the bump, unlike `addFuel`/`editFuel`); no `data_revision` consumer for these tables until Phase 24.2. Not a 24.1 correctness bug — makes the posture explicit |
| N8 | LOW | INCORPORATED | Plan 01 Task 1: staged-upgrade test PARAMETERIZED across every prior version 0..15 (was representative 1/8/15), so the "any prior version" must-have truth is genuinely proven — preferred over narrowing the wording because migration 016 is irreversible and exhaustive coverage is cheap; the M4 no-spoof structure is preserved |
| N9 | LOW | INCORPORATED | Plan 07 Task 3: relationship soft-delete snackbar copy changed to relationship-specific `Relationship removed` (with Undo), not the Memories recently-deleted copy — relationships are Undo-only and never appear on the Recently Deleted screen. REINFORCES (does NOT resolve) the Undo-only pending owner assumption; the `flagged_assumption` stays |
| N10 | LOW | INCORPORATED (indexes added to 016) | Plan 01 Task 1: three additive read indexes baked into migration 016 now (frozen contract) — `idx_memories_contact_deleted (contact_id, deleted_at, id)`, `idx_relationships_contact_deleted (contact_id, deleted_at, id)`, `idx_current_state_history (contact_id, field_key)` — for the live/trash/history query paths, validated via EXPLAIN QUERY PLAN in the 016 test. Allowed on the new tables: the custom-field index/UNIQUE ban applies only to the retired dynamic-column table |

Uncounted (advisory, no plan change): the shared `VisibilityDefault` type alias nit (`RELATIONSHIPS_GROUP.visibilityDefault` duplicating `MemoryTypeMeta.visibilityDefault` inline) — executor discretion, does not affect correctness.

Protected pending owner decisions carried forward UNCHANGED by cycle 3 (remain `flagged_assumptions`): (1) relationships are Undo-only and NOT listed in Recently Deleted (dossier §I, Memories-only) — Plan 03/05/07 (N9 reinforces, does not resolve); (2) first-class field set = birthday/social_battery/interval_days/category, Gravity/Closeness excluded (no column) — Plan 04/06. Cycle 3 touched neither.

## Review dispositions (cycle 4)

Consolidated map of every current actionable cycle-4 finding in `24.1-REVIEWS.md` (1 MEDIUM + 2 LOW). Cycle-1/2/3 tables above are unchanged EXCEPT the two stale cycle-1 pointer rows (M3 :301, M4 :302) that C3 below annotates as superseded — a doc-sync only; their executable task content was already correct. Both source lanes independently confirmed all 25 prior-cycle findings remain incorporated in executable plan text — no regressions, no HIGH. Each affected plan carries its own `## Review dispositions (cycle 4)` pointer section.

| ID | Severity | Disposition | Where |
|----|----------|-------------|-------|
| C1 | MEDIUM | INCORPORATED | Plan 01 Task 1: migration 016's `relationships` DDL gains a DB-level `CHECK (linked_contact_id IS NULL OR linked_contact_id != contact_id)` — defense-in-depth for the HIGH-rated self-link threat T-24.1-14 on the frozen/irreversible schema, alongside the application `assertNotSelfLink` (Plan 03) and the merge bidirectional SET-NULL (N6). CHECK is an established repo idiom (migrations 001/002/009/011/012/013; closest analog 011-contact-lifecycle-schema.ts:36-37). A test asserts SQLite rejects an INSERT and an UPDATE that would set `linked_contact_id = contact_id` while a NULL link succeeds; acceptance grep added. Plan 03's T-24.1-14 mitigation updated (app guard + merge SET-NULL + DB CHECK). Ordering confirmed: Plan 03's pre-reparent bidirectional SET-NULL runs BEFORE the reparent, so the CHECK sees only valid rows mid-merge and never aborts a legitimate merge. Additive on a NEW table — NOT the retired dynamic-column table the CHECK/index ban covers, NOT a decision reversal |
| C2 | LOW | INCORPORATED | Plan 03 Task 3: the single trailing post-merge invariant `SELECT COUNT(*) FROM relationships WHERE contact_id = linked_contact_id` = 0 is converted to a shared helper `assertNoRelationshipSelfLinks(exec)` asserted after EVERY `mergeContacts`-exercising test case in `merge-dao.test.ts`, so a future test edit cannot slip a self-link past it. Acceptance wording updated |
| C3 | LOW | INCORPORATED (doc-sync) | Plan 01 cycle-1 consolidated table: the stale M3 row (sweep expires relationships "via Plan 03's guarded `purgeRelationshipPermanently`") is annotated **⚠ SUPERSEDED by N1** (`expireRelationshipIfStale`), and the stale M4 row ("N = 1, 8, 15") **⚠ SUPERSEDED by N8** (staged upgrade across 0..15). Executable task content in Plans 01/03/05 was already correct — this only realigns the historical cycle-1 pointer rows so an executor reading them is not misled |

Protected pending owner decisions carried forward UNCHANGED by cycle 4 (remain `flagged_assumptions`): (1) relationships are Undo-only and NOT listed in Recently Deleted (dossier §I, Memories-only) — Plan 03/05/07; (2) first-class field set = birthday/social_battery/interval_days/category, Gravity/Closeness excluded (no column) — Plan 04/06. Cycle 4 touched neither; both remain open for the owner.

## Review dispositions (cycle 5)

Consolidated map of the cycle-5 owner decisions and the three new codex findings (F1 MEDIUM, F2 MEDIUM, F3 LOW) from `24.1-REVIEWS.md`. Cycle-1/2/3/4 tables above are unchanged and were NOT re-touched. Both source lanes independently confirmed all 28 prior-cycle findings remain incorporated in executable plan text and the cycle-4 C1 CHECK edit is defect-free — no regression, no open HIGH. **Both previously protected pending owner decisions are RESOLVED this cycle** (see the note below the table). Each affected plan carries its own `## Review dispositions (cycle 5)` pointer section.

| ID | Severity | Disposition | Where |
|----|----------|-------------|-------|
| Owner: gravity + intensity are first-class | — | INCORPORATED (INCLUDE, reversing the prior omission) | Plan 04 Task 3 adds `getFirstClassDerived(exec, contactId, now)` to `first-class-knowledge-read.ts`, deriving gravity tier + intensity(closeness) at read time from the interaction history via the EXISTING node-pure services (`getImpactInputs` → `computeContactGravity` / `computeContactIntensity`, the ai-context-read.ts:224-261 composition) — DERIVED-NEVER-STORED, no column, migration 016 unchanged/additive; local SQLite, NO network on the read path (verified against impact-read.ts:52 on disk). Plan 06 Task 2 composes it and renders both as READ-ONLY featured rows (gravity by `tierName`, never a raw number; intensity only when available), and excludes the derived pair from the empty-state gate (N5 reasoning). The stale "Gravity/Closeness omitted because no column exists" rationale in Plans 04/06 `flagged_assumptions` is RETRACTED (it was wrong — the values were never column-backed, they are derived, and derived values are first-class). New must-have truths, T-24.1-16 in both plans |
| F1 | MEDIUM | INCORPORATED (owner: include-hidden toggle) | Plan 06 Task 2 adds an include-hidden TOGGLE (default off) that reveals hidden LIVE items inline, greyed (textSecondary + reduced opacity, no new token), each with an Unhide / "Show on Profile" affordance clearing the hidden flag through the EXISTING editor path (editMemory hidden→0) then reload; the surface loads the full live lists and partitions via `resolveVisibility`/`resolveRelationshipVisibility`. Plan 07 Task 3 wires the relationship-side Unhide (editRelationship hidden→0). Makes a hidden live Memory/relationship reachable + reversible per DECIDED dossier §G — a completeness fix, NOT a §G reversal; no new schema (hidden flag already exists). Must-have truths, actions, acceptance greps, hide→toggle-on→unhide UAT (both tables), T-24.1-05 updated |
| F2 | MEDIUM | INCORPORATED | Plan 07 Task 2: MemoryHistoryScreen renders the current value ONCE on top, then `priorEntries = history.filter((row) => row.is_current === 0)` for the backlist — the current row is never duplicated and a current-only field reaches "No earlier entries". Plan 04 exposes `is_current` on `CurrentStateEntryRow` and adds a data-level test (current-only field → exactly one is_current=1 row). Must-have truths (E3 + screen), action, `is_current === 0` acceptance grep, current-only test + UAT |
| F3 | LOW | INCORPORATED | Plan 04 Task 2: `editHistoryEntry(contactId, fieldKey, entryId, value, now)` scopes the UPDATE by `id AND contact_id AND field_key` (the editFuelCore two-key posture), so a caller misroute fails closed via `assertOneChange`; misroute-rollback test + `field_key = ?` acceptance grep. Plan 07 Task 2 (caller) threads `fieldKey` from the MemoryHistory route param |
| Owner: relationships Undo-only | — | CONFIRMED (was pending) | Plans 03/05/07 `flagged_assumptions` updated from "pending, surfaced not decided" to OWNER CONFIRMED — relationships stay Undo-only and NOT in Recently Deleted (dossier §I), including the accepted consequence that a relationship deleted then Undo-dismissed has no recovery UI before the Plan 05 sweep purges it. No behaviour change (the DAOs/sweep already implement exactly this; N9's `Relationship removed` + Undo copy stands) |
| Owner: UI-SPEC sparkle | — | INCORPORATED (doc-sync) | `24.1-UI-SPEC.md` `sparkle` (AI-enabled) entry annotated Phase-24.2-scoped so the UI-SPEC matches the plans (AI affordance is out of 24.1 per CONTEXT D-08/D-09); no `sparkle` added to any 24.1 plan. Retires the cycle-1 V12 doc-sync item |

Both previously protected pending owner decisions are now RESOLVED: (1) relationships Undo-only / not-in-Recently-Deleted → **owner CONFIRMED** (no behaviour change); (2) first-class field set → **owner decided to INCLUDE gravity + intensity** as derived-never-stored values (Plans 04/06), so the set is now birthday / social_battery / interval_days / category + derived gravity + derived intensity + the two current-state fields. No `flagged_assumptions` for these two remain open. No plan content reverses a recorded ADR/HANDOFF/dossier decision — F1 CLOSES a §G under-implementation, and the gravity/intensity inclusion adds no column (migration 016 stays additive). KNOW-01…KNOW-09 remain covered.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md` when done.
</output>
