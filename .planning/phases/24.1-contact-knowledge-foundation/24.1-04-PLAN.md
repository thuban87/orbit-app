---
phase: 24.1-contact-knowledge-foundation
plan: 04
type: execute
wave: 2
depends_on: ["24.1-01"]
files_modified:
  - src/db/current-state-history-dao.ts
  - src/db/current-state-history-dao.test.ts
  - src/db/current-state-history-read.ts
  - src/db/current-state-history-read.test.ts
  - src/db/first-class-knowledge-read.ts
  - src/db/first-class-knowledge-read.test.ts
autonomous: true
requirements: [KNOW-03, KNOW-04, KNOW-01]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-03: history-aware Last Talked About + Current Location ---
    - "Last Talked About and Current Location are history-aware: at most one current entry exists per (contact, field_key) — enforced by the migration-016 partial unique index — and the full backlist is the set of all entries for that (contact, field_key)."
    - "getCurrentStateValue returns the single is_current=1 entry for a (contact, field_key); getCurrentStateValues returns every is_current=1 entry for a contact in ONE query keyed by field_key (the batched read the surface uses); getCurrentStateHistory returns the full backlist ordered created_at DESC, id DESC (most recent first)."
    - "getCurrentStateValues EXCLUDES a row whose field_key is not a registered CURRENT_STATE_FIELD_KEYS member (a malformed/legacy key) from its result map but NEVER deletes it — the row stays in current_state_entries (KNOW-03 full-history retention: excluded-not-lost, the fail-safe counterpart of resolveVisibility's fail-visible posture; review N4)."
    - "KNOW-03 empty: a (contact, field_key) with no entries returns null current and an empty backlist; a field with exactly one entry returns it as current with an empty prior-history remainder."
    - "History behavior is defined centrally (review M1): which field_keys are history-aware and their display come from CURRENT_STATE_FIELD_KEYS / CURRENT_STATE_FIELD_REGISTRY in memory-registry.ts (Plan 01), not hardcoded per screen; every writer validates field_key with isCurrentStateFieldKey and throws on an unknown key before any write."
    # --- KNOW-01: the stable first-class set the surface shows read-only (review M8) ---
    - "getFirstClassFields returns, in one query, the stable first-class contact properties the dossier §A names AND that exist on disk — birthday (contacts.birthday), socialBattery (contacts.social_battery), intervalDays (contacts.interval_days, Contact Frequency), categoryName (categories.name via contacts.category_id LEFT JOIN) — so the Things-to-Remember surface can display them read-only without inline SQL (KNOW-01, review M8)."
    - "getFirstClassDerived(exec, contactId, now) derives the contact's gravity tier and intensity(closeness) at READ TIME from the interaction history — via getImpactInputs → computeContactGravity / computeContactIntensity, exactly as ai-context-read.ts:224-261 does and as ContactProfileScreen already displays — and returns them for READ-ONLY display alongside birthday/social_battery/interval_days/category. Gravity and intensity are DERIVED-NEVER-STORED: no column, no write, migration 016 unchanged (owner decision, cycle-5). The read is local SQLite only — getImpactInputs is a single LEFT JOIN over contacts+interactions, NO network — so this stays on the offline read path (CLAUDE.md local-first)."
    - "CurrentStateEntryRow exposes is_current so a consumer can distinguish the single current entry from prior entries; getCurrentStateHistory returns all entries (current + prior) and a current-only field (exactly one entry) returns one row with is_current=1 — the data precondition MemoryHistoryScreen relies on to show the current value once and reach 'No earlier entries' (review cycle-5 F2)."
    # --- KNOW-04: edit historical + promote-to-current ---
    - "setCurrentStateValue adds a NEW current entry and atomically demotes the prior current entry to history (is_current 1→0) in ONE transaction — the prior value becomes history, it is never overwritten or deleted."
    - "promoteToCurrentValue makes a chosen historical entry current: it sets is_current=0 on the existing current and is_current=1 on the target in one transaction; the prior current value is preserved as history (never destroyed)."
    - "editHistoryEntry updates the value of a chosen entry in place (scoped by id AND contact_id AND field_key, assertOneChange) without changing which entry is current — a caller misroute (an entryId belonging to a DIFFERENT field of the same contact) changes 0 rows and rolls back, matching the editFuelCore two-key write posture (review cycle-5 F3)."
    - "The partial unique index makes it impossible for two entries of the same (contact, field_key) to be is_current=1 simultaneously — a promote/set that would violate it rolls back the whole transaction."
  artifacts:
    - "src/db/current-state-history-dao.ts — setCurrentStateValue, promoteToCurrentValue, editHistoryEntry (core/wrapper split), each validating field_key via isCurrentStateFieldKey"
    - "src/db/current-state-history-read.ts — getCurrentStateValue, getCurrentStateValues (batched), getCurrentStateHistory"
    - "src/db/first-class-knowledge-read.ts — getFirstClassFields (birthday / socialBattery / intervalDays / categoryName), FirstClassFields (review M8) AND getFirstClassDerived (gravity tier + intensity, derived at read time via getImpactInputs → computeContactGravity/computeContactIntensity; DERIVED-NEVER-STORED; local SQLite, no network) with its result type (owner decision, cycle-5)"
  key_links:
    - "set/promote demote-then-promote runs as composed non-mutexed cores inside ONE inWriteTransaction (never nesting the non-reentrant mutex) so the demote and promote are atomic"
    - "field_key values (last_talked_about, current_location) are CURRENT_STATE_FIELD_KEYS registry constants validated by isCurrentStateFieldKey at every write boundary, not free strings interpolated into SQL (review M1)"
  prohibitions:
    - "Promoting a historical value to current must never destroy the prior current value — it becomes history, not lost (KNOW-04)."
    - "Nothing watches a timestamp: current-state history is retained (no expiry) — there is no background timer and no history-retention sweep for it (KNOW-03 full history retained)."
  flagged_assumptions:
    - "First-class set shown read-only in 24.1 (review M8; owner decision cycle-5): dossier §A names Birthday, Current Location, Social Battery, Contact Frequency, Gravity/Closeness, category/group. Birthday, Social Battery, Contact Frequency (interval_days) and category exist as contacts columns and are read by getFirstClassFields; Current Location and Last Talked About are the two current-state fields. Gravity and Intensity(closeness) are DERIVED-NEVER-STORED — computed at read time by computeContactGravity / computeContactIntensity over the interaction history (via getImpactInputs, local SQLite only, no network), exactly as ai-context-read.ts:224-261 does and as ContactProfileScreen already displays. Per the owner's cycle-5 decision BOTH are INCLUDED in the read-only first-class set via getFirstClassDerived (a separate derived read composed on the surface, NOT shoehorned into getFirstClassFields' single SELECT), needing no column — migration 016 stays additive/unchanged. The earlier 'Gravity/Closeness omitted because no column exists' rationale was WRONG and is retracted (they were never column-backed; they are derived, and derived values are first-class here). Editing first-class fields stays on Edit Contact (Phase 34 Update Contact adds quick edit); Phase 31 composes the exact Profile card appearance (dossier defers it)."
---

<objective>
Build the current-state history layer for Last Talked About and Current Location over the frozen 016 schema: one generic table with a single current entry per (contact, field) plus a retained backlist, supporting set-new-current (demoting the prior), edit-historical, and promote-history-to-current (KNOW-03/04).

Purpose: Deliver the history-aware fields the Profile surfaces "most recent, backlist on drill-in," with the data-loss-safety that promotion never destroys the prior value. Registry defines which fields are history-aware.

Output: current-state-history-dao (set/promote/edit) + current-state-history-read (current + backlist), node-tested including the atomic demote-then-promote and the single-current integrity guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-RESEARCH.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md
@src/db/fuel-dao.ts
@src/db/capture-dao.ts
@src/db/memory-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setCurrentStateValue — add current + atomic demote of prior</name>
  <files>src/db/current-state-history-dao.ts, src/db/current-state-history-dao.test.ts</files>
  <read_first>
    - src/db/capture-dao.ts (multi-write fan-out composed as cores inside ONE inWriteTransaction — the atomicity precedent)
    - src/db/fuel-dao.ts (core/wrapper split; assertOneChange; newUid; bumpDataRevisionCore)
    - src/db/migrations/016-contact-knowledge.ts (current_state_entries columns + the partial unique index on is_current=1)
    - src/db/memory-registry.ts (Plan 01's CurrentStateFieldKey, CURRENT_STATE_FIELD_KEYS, isCurrentStateFieldKey — the history-aware field_key contract every writer here validates against; review M1)
  </read_first>
  <behavior>
    - setCurrentStateValue(contact, field_key, value, now): if a current entry exists for (contact, field_key), demote it (is_current 1→0) and insert a new entry with is_current=1 — both in one transaction; if none exists, just insert the new current entry.
    - The demoted entry retains its original value and created_at (it becomes history); the new current entry has a fresh uid and created_at.
    - After set, exactly one is_current=1 entry exists for that (contact, field_key); the prior value is present in the backlist.
    - A blank/whitespace-only value is rejected (current_state_entries.value is NOT NULL and must be meaningful).
    - A field_key that is not a CURRENT_STATE_FIELD_KEYS member (e.g. 'bogus') is rejected by isCurrentStateFieldKey before any write (review M1); the input type is CurrentStateFieldKey so a literal typo also fails tsc.
  </behavior>
  <action>
    Create src/db/current-state-history-dao.ts. Type fieldKey as CurrentStateFieldKey (import type from memory-registry) and, at the top of every core, call isCurrentStateFieldKey(fieldKey) and throw `current-state-history-dao: unknown field_key` when false (review M1 — the registry is the enforcement boundary, mirroring Plan 01's assertRegisteredMemoryType). Implement setCurrentStateValueCore(exec, {contactId, fieldKey, value, now}) that (a) runs UPDATE current_state_entries SET is_current=0, modified_at=? WHERE contact_id=? AND field_key=? AND is_current=1 (demote; 0 or 1 rows), then (b) INSERTs a new row with is_current=1, minted uid, value, created_at=now, modified_at=now. Compose both statements in the core (assumes BEGIN open). The wrapper setCurrentStateValue owns one inWriteTransaction and calls bumpDataRevisionCore. Reject a blank value before writing. Add current-state-history-dao.test.ts: first-set creates one current; second-set demotes the first and creates a new current (assert exactly one is_current, prior value in backlist); unknown field_key rejected with zero rows written.
  </action>
  <verify>
    <automated>npx vitest run src/db/current-state-history-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/current-state-history-dao.test.ts` passes the first-set, demote-on-second-set, and unknown-field_key-rejection cases.
    - A test asserts exactly one is_current=1 row per (contact, field_key) after repeated sets, and that the prior value survives as history.
    - `grep -c "isCurrentStateFieldKey" src/db/current-state-history-dao.ts` is >= 3 (one guard per core: set / promote / edit) — review M1.
    - Grep confirms no nested mutex (core has no inWriteTransaction; wrapper has exactly one).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Setting a new current value atomically demotes the prior to history and preserves it; exactly one current exists per field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: promoteToCurrentValue + editHistoryEntry</name>
  <files>src/db/current-state-history-dao.ts, src/db/current-state-history-dao.test.ts</files>
  <read_first>
    - src/db/current-state-history-dao.ts (setCurrentStateValue from Task 1 — reuse the demote step)
    - src/db/fuel-dao.ts (patch-scoped edit + assertOneChange; both-keys scoping)
  </read_first>
  <behavior>
    - promoteToCurrentValue(contactId, fieldKey, targetId, now): demote the existing current (is_current 1→0) and set the target historical entry is_current=1, in one transaction; the previously-current value remains in the backlist.
    - editHistoryEntry(contactId, fieldKey, entryId, value, now): update that entry's value in place (scoped by id AND contact_id AND field_key, assertOneChange), leaving is_current unchanged. Threading field_key (from the MemoryHistoryScreen route param) makes a caller misroute — an entryId that belongs to a different field of the SAME contact — change 0 rows and fail closed via assertOneChange, matching editFuelCore's two-key posture (review cycle-5 F3).
    - Promoting must never violate the single-current index — if the target does not belong to (contact, field_key) it changes 0 rows and the transaction rolls back (assertOneChange).
    - Promoting the entry that is ALREADY current is no-op-safe (review L4): the unconditional demote flips it to 0 and the promote-by-id flips the same row back to 1 inside the one transaction; afterwards exactly one is_current=1 row exists, it is the same id, and every other entry's value and is_current are unchanged.
    - Neither operation destroys any prior value.
  </behavior>
  <action>
    Add promoteToCurrentValueCore/promoteToCurrentValue: in one transaction demote the current (UPDATE ... SET is_current=0 WHERE contact_id=? AND field_key=? AND is_current=1) then promote the target (UPDATE ... SET is_current=1, modified_at=? WHERE id=? AND contact_id=? AND field_key=?, assertOneChange). Add editHistoryEntryCore/editHistoryEntry (contactId, fieldKey, entryId, value, now): UPDATE ... SET value=?, modified_at=? WHERE id=? AND contact_id=? AND field_key=?, assertOneChange; reject blank value. Bind fieldKey (threaded from the MemoryHistoryScreen route param) so a caller misroute fails closed via assertOneChange (review cycle-5 F3). Both cores validate fieldKey with isCurrentStateFieldKey first (Task 1 idiom). Wrappers own one inWriteTransaction + bumpDataRevisionCore. Extend the test: promote a historical entry back to current (assert the formerly-current value is now history and still present), promote the already-current entry (assert same id still current, single is_current, backlist unchanged — review L4), edit a historical entry's value, a wrong-target promote rolls back, and (review cycle-5 F3) an editHistoryEntry misroute — a valid entryId for field A passed with fieldKey B (same contact) — changes 0 rows and rolls back via assertOneChange, leaving both fields' values unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/db/current-state-history-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/current-state-history-dao.test.ts` passes the promote, promote-already-current (review L4), edit, and edit-misroute-rollback (review cycle-5 F3) cases.
    - A test asserts the prior current value is NOT lost after a promote (it appears in the backlist).
    - A wrong-target promote throws (assertOneChange) and leaves the current unchanged.
    - An editHistoryEntry called with an entryId belonging to a different field_key of the same contact throws (assertOneChange) and leaves both fields' values unchanged (review cycle-5 F3); `grep -n "field_key = ?" src/db/current-state-history-dao.ts` shows editHistoryEntryCore's UPDATE scoped by field_key.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Historical entries can be edited in place and promoted back to current without ever destroying the prior current value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: current-state-history-read (current + batched current + backlist) and the read-only first-class field read</name>
  <files>src/db/current-state-history-read.ts, src/db/current-state-history-read.test.ts, src/db/first-class-knowledge-read.ts, src/db/first-class-knowledge-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (single read choke point; contact_id ?-bound)
    - src/db/current-state-history-dao.ts (the writers to read back)
    - src/db/contact-read.ts:67-108 (getContactHeader — the light Profile-scaffold read; it does NOT return birthday / social_battery / category, which is why a separate first-class read is needed; keep it untouched)
    - src/db/migrations/001-initial.ts:42-70 (categories table; contacts.category_id / interval_days / social_battery / birthday column names)
    - src/db/memory-registry.ts (CURRENT_STATE_FIELD_KEYS — the keys getCurrentStateValues is keyed by)
    - src/db/impact-read.ts (getImpactInputs — the SINGLE local LEFT JOIN over contacts+interactions that feeds both derivations; read-only, NO network, NO transaction — CONFIRM these before wiring) and src/services/impact.ts (computeContactGravity → GravityResult { tierName, tierIndex, tierCount, raw }; computeContactIntensity → ContactIntensity = IntensityResult | { available: false }) — the derived-value orchestrators, both node-pure
    - src/db/ai-context-read.ts:224-261 (the reference composition: getImpactInputs → computeContactGravity(...).tierName + computeContactIntensity(...); copy this shape for getFirstClassDerived, MINUS the egress redaction — this is a LOCAL display read, not an egress path)
  </read_first>
  <behavior>
    - getCurrentStateValue(contact, field_key) returns the single is_current=1 entry or null.
    - getCurrentStateValues(contact) returns a Partial<Record<CurrentStateFieldKey, CurrentStateEntryRow>> from ONE query (WHERE contact_id=? AND is_current=1) — the batched read the surface calls once instead of once per field key (review V16 advisory).
    - getCurrentStateValues drops a row whose field_key fails isCurrentStateFieldKey from the returned map (a malformed/legacy key is not surfaced) but the read performs NO delete — the row remains in current_state_entries (review N4).
    - getCurrentStateHistory(contact, field_key) returns all entries ordered created_at DESC, id DESC (current typically first, then history); each CurrentStateEntryRow carries is_current so a consumer can tell the single current entry from prior entries (review cycle-5 F2).
    - Empty (contact, field_key) → null current and [] history; single-entry (current-only) → getCurrentStateHistory returns exactly one row with is_current=1 (so a consumer filtering is_current=0 gets an empty prior-history list — the "No earlier entries" precondition, review cycle-5 F2).
    - Other contacts/fields are excluded (both ?-bound).
    - getFirstClassFields(contact) returns { birthday: string | null; socialBattery: string | null; intervalDays: number | null; categoryName: string | null } from ONE SELECT over contacts LEFT JOIN categories ON categories.id = contacts.category_id, or null for a missing contact id (review M8). Values are returned RAW (birthday is the stored YYYY-MM-DD text — never passed through new Date()).
    - getFirstClassDerived(exec, contactId, now) loads getImpactInputs once (local SQLite, no network) and returns { gravity: GravityResult | null; intensity: ContactIntensity } — gravity = computeContactGravity(inputs, now) when the contact has interaction history else null (mirroring ContactProfileScreen's hasHistory gate), intensity = computeContactIntensity(inputs, now) (a tagged { available: false } for an Unbound contact). Returns null for a missing contact id (mirrors getImpactInputs). DERIVED-NEVER-STORED — no write. This is the read the Things-to-Remember surface composes to display gravity tier + intensity read-only (owner decision, cycle-5).
  </behavior>
  <action>
    Create src/db/current-state-history-read.ts: export CurrentStateEntryRow (INCLUDE is_current in the projection so consumers can distinguish current from prior — review cycle-5 F2), getCurrentStateValue (SELECT ... WHERE contact_id=? AND field_key=? AND is_current=1 LIMIT 1), getCurrentStateValues(exec, contactId) (SELECT ... WHERE contact_id=? AND is_current=1, folded into a Partial<Record<CurrentStateFieldKey, CurrentStateEntryRow>> keyed by field_key, ignoring any row whose field_key fails isCurrentStateFieldKey), and getCurrentStateHistory (SELECT ... WHERE contact_id=? AND field_key=? ORDER BY created_at DESC, id DESC). Add current-state-history-read.test.ts covering empty, single-entry, multi-entry (current + backlist ordering), the batched read returning both keys when both are set, (review N4) a row inserted directly with an unrecognized field_key (e.g. `legacy_key`, is_current=1) that getCurrentStateValues EXCLUDES from the returned map while a follow-up `SELECT COUNT(*) FROM current_state_entries WHERE field_key='legacy_key'` confirms the row is still present (excluded-not-deleted), and (review cycle-5 F2) a current-only field where getCurrentStateHistory returns exactly one row whose is_current=1 (asserting `history.filter(r => r.is_current === 0)` is empty — the MemoryHistoryScreen "No earlier entries" precondition).

    Create src/db/first-class-knowledge-read.ts (node-pure, read-only, no transaction; NO network on the read path — CLAUDE.md local-first): export interface FirstClassFields and getFirstClassFields(exec, contactId) — `SELECT c.birthday, c.social_battery, c.interval_days, cat.name AS category_name FROM contacts c LEFT JOIN categories cat ON cat.id = c.category_id WHERE c.id = ?` mapped to camelCase; every value ?-bound; returns null when the contact does not exist. ALSO export getFirstClassDerived(exec, contactId, now) and its result interface (e.g. FirstClassDerived { gravity: GravityResult | null; intensity: ContactIntensity }): call getImpactInputs(exec, contactId) once (return null if it returns null), then gravity = the contact has interaction history ? computeContactGravity(inputs, now) : null, intensity = computeContactIntensity(inputs, now) — the exact ai-context-read.ts:224-261 composition, MINUS the C3-M3 egress redaction (this is a LOCAL display read, not egress; showing intensity's fields on-device is what ContactProfileScreen already does). Import getImpactInputs from "@/db/impact-read" and computeContactGravity/computeContactIntensity from "@/services/impact"; both are node-pure so first-class-knowledge-read stays node-testable. These reads are what the Things-to-Remember surface (Plan 06) uses to display the stable first-class set read-only (dossier §A / §G; review M8) plus the derived gravity/intensity rows (owner decision, cycle-5). Add first-class-knowledge-read.test.ts: getFirstClassFields — populated contact (all four present), contact with no category (categoryName null), missing id → null; getFirstClassDerived — a contact WITH interaction history (gravity non-null with a tierName; intensity available for a Bound contact), an Unbound contact (intensity.available === false), a no-history contact (gravity null), missing id → null.
  </action>
  <verify>
    <automated>npx vitest run src/db/current-state-history-read.test.ts src/db/first-class-knowledge-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/current-state-history-read.test.ts src/db/first-class-knowledge-read.test.ts` passes empty / single / multi / batched cases, the malformed-field_key excluded-not-deleted case (review N4), the current-only "No earlier entries" precondition (review cycle-5 F2), the three getFirstClassFields cases, and the four getFirstClassDerived cases (history → gravity+intensity, Unbound → intensity unavailable, no-history → gravity null, missing id → null — owner decision cycle-5).
    - A test asserts getCurrentStateValues excludes an unrecognized-field_key row from the result map but leaves it in current_state_entries (a COUNT confirms the row still exists — review N4).
    - A test asserts a current-only field's getCurrentStateHistory returns exactly one row with is_current=1, so `history.filter(r => r.is_current === 0)` is empty (review cycle-5 F2).
    - `grep -n "export async function getCurrentStateValues\|export function getCurrentStateValues" src/db/current-state-history-read.ts` shows the batched read.
    - `grep -n "LEFT JOIN categories" src/db/first-class-knowledge-read.ts` shows the single-query join; `grep -n "new Date(" src/db/first-class-knowledge-read.ts` (comment-filtered) returns nothing.
    - `grep -n "getImpactInputs\|computeContactGravity\|computeContactIntensity" src/db/first-class-knowledge-read.ts` shows getFirstClassDerived composing the existing derived-value services (owner decision cycle-5); `grep -niE "insert into|delete from" src/db/first-class-knowledge-read.ts` (comment-filtered) returns nothing (read-only, derived-never-stored — no persisting write, no column).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>The reads surface the most-recent current value (singly and batched), the full ordered backlist for a history-aware field, the stable first-class fields, and the derived gravity tier + intensity — all the surface displays read-only, local SQLite, no network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → current-state-history-dao SQL | User sets/edits/promotes location + last-talked-about values |
| current_state_entries integrity | The single-current invariant must hold across concurrent writes |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-01 | Tampering | current_state INSERT/UPDATE | medium | mitigate | All values ?-bound; field_key validated against registry constant; both-keys scoping + assertOneChange; editHistoryEntry additionally scopes by field_key (three-key write) so a caller misroute fails closed (review cycle-5 F3) |
| T-24.1-16 | Information Disclosure | derived gravity/intensity read | low | mitigate | getFirstClassDerived composes the existing node-pure derived-value services over getImpactInputs — a LOCAL SQLite read only, NO network on the read path; DERIVED-NEVER-STORED (no column, no write); display-only on-device, not an egress path (no AI egress in 24.1) — CLAUDE.md local-first |
| T-24.1-04 | Tampering / data loss | promote-to-current | high | mitigate | Promote is atomic demote-then-promote inside one transaction; prior value becomes history and is never overwritten or deleted; single-current partial unique index rolls back any violation |
</threat_model>

<verification>
- `npx vitest run src/db/current-state-history-dao.test.ts src/db/current-state-history-read.test.ts src/db/first-class-knowledge-read.test.ts` green.
- Single-current invariant + prior-value-preserved + promote-already-current + unknown-field_key assertions pass; tsc --noEmit clean.
</verification>

<success_criteria>
Last Talked About and Current Location retain full history behind a single current value; setting, editing, and promoting are atomic, registry-validated, and never destroy a prior value; the stable first-class fields are readable in one query for the surface.
</success_criteria>

## Artifacts this plan produces

- **DAO (src/db/current-state-history-dao.ts):** setCurrentStateValue, promoteToCurrentValue, editHistoryEntry (+ cores), each guarded by isCurrentStateFieldKey.
- **Read (src/db/current-state-history-read.ts):** getCurrentStateValue, getCurrentStateValues (batched), getCurrentStateHistory, CurrentStateEntryRow.
- **Read (src/db/first-class-knowledge-read.ts):** getFirstClassFields, FirstClassFields, AND getFirstClassDerived (gravity tier + intensity, derived at read time via getImpactInputs → computeContactGravity/computeContactIntensity; DERIVED-NEVER-STORED; local SQLite, no network — owner decision cycle-5).
- **Tests:** current-state-history-dao.test.ts, current-state-history-read.test.ts, first-class-knowledge-read.test.ts.
- **Retention:** none — current-state history is retained indefinitely (no expiry sweep; KNOW-03).

## Review dispositions (cycle 1)

- **M1** — INCORPORATED (consumer side): every writer validates `fieldKey` with Plan 01's `isCurrentStateFieldKey` and types it as `CurrentStateFieldKey` (Tasks 1-2; grep-gated). The contract itself is defined in Plan 01 Task 2.
- **L4** (promote half) — INCORPORATED: Task 2 tests promoting the already-current entry (no-op-safe, prior value preserved). The `hidden` half is Plan 02 Task 3.
- **M8** — INCORPORATED (read side): Task 3 `getFirstClassFields` over the dossier §A set that exists on disk; the Gravity/Closeness omission is in `flagged_assumptions`; Plan 06 Task 2 renders it.
- **V16** (advisory) — INCORPORATED: Task 3 `getCurrentStateValues` batched read.
- Full consolidated table: `24.1-01-PLAN.md` § Review dispositions (cycle 1).

## Review dispositions (cycle 2)

- **N4 (LOW)** — INCORPORATED: Task 3 makes the `getCurrentStateValues` malformed/legacy-`field_key` behavior an explicit contract (a must-have truth + a behavior bullet: excluded from the map, NEVER deleted — KNOW-03 full-history retention) and adds a test asserting an unrecognized `field_key` row is excluded from the read yet still present in `current_state_entries` (a COUNT confirms it was not lost).

## Review dispositions (cycle 5)

This plan owns two cycle-5 items: the owner's gravity+intensity inclusion (the read side) and F3 (`editHistoryEntry` field_key scoping). It also adds the F2 data precondition. Full consolidated cycle-5 table: `24.1-01-PLAN.md` § Review dispositions (cycle 5).

- **Owner decision — gravity + intensity are first-class (INCLUDED, not omitted)** — INCORPORATED (read side): Task 3 adds `getFirstClassDerived(exec, contactId, now)` to `first-class-knowledge-read.ts`, deriving gravity tier + intensity(closeness) at read time from the interaction history via the EXISTING node-pure services (`getImpactInputs` → `computeContactGravity` / `computeContactIntensity`, the ai-context-read.ts:224-261 composition). DERIVED-NEVER-STORED — no column, no write, migration 016 unchanged/additive. Verified against the code on disk: `getImpactInputs` (impact-read.ts:52) is a single local `LEFT JOIN` over contacts+interactions — read-only, no transaction, NO network — so this stays on the offline read path (CLAUDE.md local-first). The earlier `flagged_assumptions` claim that Gravity/Closeness was "omitted because no column exists" was WRONG and is retracted; both are now in the read-only first-class set (must-have truth, artifact, behavior, action, acceptance tests, T-24.1-16). Plan 06 composes and displays them.
- **F3 (LOW, codex)** — INCORPORATED: Task 2 threads `fieldKey` through `editHistoryEntry(contactId, fieldKey, entryId, value, now)` and scopes the UPDATE by `id AND contact_id AND field_key` (the editFuelCore two-key posture), so a caller misroute — a valid entryId for field A passed with fieldKey B on the same contact — changes 0 rows and fails closed via `assertOneChange`. Must-have truth, behavior, action, a misroute-rollback test, and an acceptance grep added; the caller (Plan 07 MemoryHistoryScreen) passes `fieldKey` from its route param.
- **F2 (MEDIUM, codex) — data precondition** — INCORPORATED (this plan's half): `CurrentStateEntryRow` now exposes `is_current`, and Task 3 adds a test asserting a current-only field's `getCurrentStateHistory` returns exactly one row with `is_current=1` (so a consumer filtering `is_current === 0` gets an empty prior-history list). The screen-side fix (render the current value once, backlist = `is_current=0` only, reach "No earlier entries") is Plan 07 Task 2.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-04-SUMMARY.md` when done.
</output>
