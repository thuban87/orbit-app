---
phase: 24.1-contact-knowledge-foundation
plan: 02
type: execute
wave: 2
depends_on: ["24.1-01"]
files_modified:
  - src/db/memories-dao.ts
  - src/db/memories-dao.test.ts
  - src/db/memories-read.ts
  - src/db/memories-read.test.ts
autonomous: true
requirements: [KNOW-02, KNOW-06, KNOW-07, KNOW-08, KNOW-09]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-06: notes / links / meaningful date / pin / outdated ---
    - "editMemory patches only the fields present in the input (type/value/note/url/meaningful_date/pinned/outdated/custom_label/hidden/provenance), scoped by BOTH id AND contact_id, and asserts exactly one row changed — a mismatched pair rolls back (no cross-contact corruption)."
    - "editMemory enforces the same write-boundary validation as addMemory (review M6): a patch whose type is not a MEMORY_TYPE_REGISTRY key throws before any write; a patch that leaves the row as type 'custom' with a blank custom_label throws before any write."
    - "A user can set/clear a Memory's optional note, url, and meaningful_date; a user can pin/unpin an item (pinned 1/0) and mark it outdated/inactive without deleting it (outdated 1/0)."
    - "KNOW-06 empty: a blank/whitespace-only note, url, or meaningful_date is stored NULL at the single commit boundary (never empty string)."
    - "KNOW-06 adjacency: two memories with equal meaningful_date are stored separately and ordered by the deterministic tiebreak (created_at DESC, id DESC)."
    - "KNOW-06 ordering: when memories compare equal, output order is deterministic (pinned DESC, meaningful_date DESC NULLs-last, created_at DESC, id DESC)."
    # --- KNOW-07: soft-delete ---
    - "deleteMemory is a SOFT delete: it sets deleted_at to the caller's localDateTime and the row stays in the table; it does not DELETE the row (KNOW-07)."
    - "restoreMemory clears deleted_at back to NULL, returning the item to its contact's live Things-to-Remember surface."
    - "listMemoriesForContact returns only live rows (deleted_at IS NULL); a soft-deleted memory never appears on the live surface but is never physically lost."
    # --- KNOW-08: visibility override, presentation-only ---
    - "A Memory's hidden column is a three-state override: NULL inherits the type's visibilityDefault from the registry, 0 forces show, 1 forces hide; per-item override wins over the type default."
    - "resolveVisibility is defensive (review L4, M6): an out-of-range hidden integer (anything other than 0/1/NULL, e.g. 2) resolves to the type's registry default, and an unknown persisted type (not a MEMORY_TYPE_REGISTRY key) resolves to 'show' — a row is never silently hidden or thrown on because of a bad value; both cases are node-tested."
    - "KNOW-08 empty: with no per-item override set (hidden NULL on every item), the resolved visibility equals each type's registry visibilityDefault."
    - "KNOW-08 ordering: hidden items are excluded from the Profile render set but the relative order of visible items is unchanged by hiding."
    # --- KNOW-09: provenance ---
    - "KNOW-09 adjacency: two memories with the same provenance are separate rows; provenance never merges items."
    - "KNOW-09 ordering: provenance is never an ORDER BY input — it does not affect item order."
  artifacts:
    - "src/db/memories-dao.ts — full writer set: editMemoryCore/editMemory (patch-scoped), deleteMemoryCore/deleteMemory (soft), restoreMemoryCore/restoreMemory"
    - "src/db/memories-read.ts — resolveVisibility helper (registry default + per-item override) and the metadata-complete MemoryRow projection"
  key_links:
    - "editMemory patch-scoping mirrors editFuelCore exactly (SET only present columns; WHERE id=? AND contact_id=?; assertOneChange) so a stale-closure single-field commit never clobbers a concurrently-edited field"
    - "resolveVisibility reads the type's visibilityDefault from MEMORY_TYPE_REGISTRY and applies the item's hidden override — the surface never hardcodes a default"
  prohibitions:
    - "A soft-deleted or outdated Memory must never silently disappear from its owner's reach — soft-deleted stays restorable until sweep expiry; outdated stays present but de-emphasized."
    - "Hidden-from-Profile must never be treated as a privacy or AI-egress control — it is presentation-only (KNOW-08)."
---

<objective>
Complete the memories data layer over the frozen 016 schema: patch-scoped edit, soft-delete + restore, and the metadata-complete read projection with registry-driven visibility resolution (KNOW-02/06/07/08/09).

Purpose: Give the Things-to-Remember UI (Plan 06) and the Recently-Deleted/Trash layer (Plan 05) every memories writer/read they need, following the fuel-dao core/wrapper mutex pattern exactly.

Output: memories-dao editMemory/deleteMemory(soft)/restoreMemory + memories-read visibility resolution and full projection, with node:sqlite proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-RESEARCH.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md
@src/db/memories-dao.ts
@src/db/memories-read.ts
@src/db/memory-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: editMemory (patch-scoped) + metadata writers</name>
  <files>src/db/memories-dao.ts, src/db/memories-dao.test.ts</files>
  <read_first>
    - src/db/fuel-dao.ts (editFuelCore patch idiom: SET only present columns, WHERE id=? AND contact_id=?, assertOneChange; the reason patch-scoping exists — stale-closure single-field commit protection)
    - src/db/memories-dao.ts (the addMemory core/wrapper from Plan 01 to extend)
    - src/db/transaction.ts (inWriteTransaction non-reentrant), src/db/data-revision-dao.ts (bumpDataRevisionCore)
  </read_first>
  <behavior>
    - editMemory patches only fields present (!== undefined): type, custom_label, value, note, url, meaningful_date, pinned, outdated, hidden, provenance; always bumps modified_at; never touches created_at or deleted_at.
    - A blank/whitespace-only note/url/meaningful_date/value/custom_label present in the patch is normalized to NULL.
    - A wrong (id, contact_id) pair changes 0 rows and throws (assertOneChange) → rollback.
    - Pinning (pinned:1) then editing another field does not un-pin; toggling outdated does not delete.
    - Write-boundary validation (review M6): a patch with type not in MEMORY_TYPE_REGISTRY throws via assertRegisteredMemoryType (Plan 01) before any write; when the effective post-patch state is type 'custom' (either the patch sets type 'custom', or the patch sets custom_label on an existing 'custom' row) a blank/NULL custom_label throws before any write — the core reads the current row's type/custom_label (SELECT type, custom_label … WHERE id=? AND contact_id=?) to evaluate the effective state.
  </behavior>
  <action>
    Extend src/db/memories-dao.ts with editMemoryCore(exec, input) and its wrapper editMemory, mirroring editFuelCore: build the SET fragment from only the patch fields that are !== undefined, always append modified_at = ?, and scope WHERE id = ? AND contact_id = ?, then assertOneChange. The SET fragments are compile-time constant column names from the closed memories column set; every value is ?-bound (T-24.1-01). Normalize blank optionals to NULL. Before the UPDATE, reuse Plan 01's assertRegisteredMemoryType on input.type when present, and enforce the custom-requires-label rule against the effective post-patch (type, custom_label) — read the existing row's two columns by (id, contact_id) inside the same transaction to compute it (review M6; T-24.1-15). The wrapper owns one inWriteTransaction and calls bumpDataRevisionCore. Add memories-dao.test.ts cases for patch isolation (editing one field leaves others), blank→NULL, pin/outdated toggles, the wrong-pair rollback, an unregistered-type patch rejection (row unchanged), and a custom-label-blanked-on-custom-row rejection (row unchanged).
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/memories-dao.test.ts` passes including the patch-isolation, wrong-pair-rollback, unregistered-type-patch, and blank-custom-label cases.
    - `grep -c "assertRegisteredMemoryType" src/db/memories-dao.ts` is >= 3 (the export, the addMemoryCore call, the editMemoryCore call).
    - Grep confirms both-keys scoping: every UPDATE/DELETE in memories-dao.ts contains "contact_id = ?" (`grep -nE 'UPDATE memories|DELETE FROM memories' src/db/memories-dao.ts` each followed by a contact_id predicate).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>editMemory patch-scopes and asserts one change; metadata (note/url/date/pin/outdated) is fully editable without collateral writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Soft-delete + restore writers</name>
  <files>src/db/memories-dao.ts, src/db/memories-dao.test.ts</files>
  <read_first>
    - src/db/fuel-dao.ts (deleteFuelCore — note it HARD-deletes + tombstones; the memories soft path is an UPDATE, NOT this)
    - src/db/memories-dao.ts (the writers from Task 1)
  </read_first>
  <behavior>
    - deleteMemory sets deleted_at = now for the (id, contact_id) row, leaving it in the table; assertOneChange.
    - restoreMemory sets deleted_at = NULL for the (id, contact_id) row; assertOneChange.
    - Re-running deleteMemory on an already-soft-deleted row still resolves to exactly one changed row (idempotent-safe) or documents the second-call behavior explicitly.
    - Neither path physically removes the row (no DELETE statement).
  </behavior>
  <action>
    Add deleteMemoryCore/deleteMemory (soft: UPDATE memories SET deleted_at = ?, modified_at = ? WHERE id = ? AND contact_id = ?; assertOneChange) and restoreMemoryCore/restoreMemory (UPDATE ... SET deleted_at = NULL, modified_at = ? WHERE id = ? AND contact_id = ?; assertOneChange). Both cores are non-mutexed; both wrappers own one inWriteTransaction and call bumpDataRevisionCore. Do NOT insert a tombstone here and do NOT touch backup — permanent-delete (with any future tombstone/backup wiring) is Plan 05's sweep and Phase 24.2/36's backup scope. Extend the test with soft-delete-then-restore round-trip and a "row still present after delete" assertion.
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/memories-dao.test.ts` passes the soft-delete/restore round-trip.
    - Grep confirms the soft path is an UPDATE, not a DELETE: `grep -nE 'DELETE FROM memories' src/db/memories-dao.ts` (comment-filtered) returns nothing.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Deleting a Memory soft-deletes (deleted_at set, row retained); restore clears it — nothing is physically lost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: memories-read — visibility resolution + full projection</name>
  <files>src/db/memories-read.ts, src/db/memories-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (editor vs projection split; single read choke point)
    - src/db/memory-registry.ts (MEMORY_TYPE_REGISTRY visibilityDefault per type)
    - src/db/memories-read.ts (listMemoriesForContact from Plan 01 to extend)
  </read_first>
  <behavior>
    - The MemoryRow projection returns every metadata column (type, custom_label, value, note, url, meaningful_date, pinned, outdated, hidden, provenance, created_at, modified_at, deleted_at).
    - resolveVisibility(type, hidden) returns 'show'|'hide': hidden===1 → hide, hidden===0 → show, hidden===null → the type's registry visibilityDefault.
    - Defensive cases (review L4, M6): hidden outside {0, 1, null} (e.g. 2 or -1) resolves exactly as null (the type's registry default); a type string that is not a MEMORY_TYPE_REGISTRY key (isMemoryTypeKey false — a restored/corrupt row) resolves to 'show' and never throws, so a row with a bad value is never silently hidden.
    - A "profile-visible" selection excludes items resolving to 'hide' while preserving the deterministic order of the rest (KNOW-08 ordering).
    - Visibility resolution is presentation-only: there is no AI/search consumer of hidden in 24.1.
  </behavior>
  <action>
    Extend src/db/memories-read.ts with a pure resolveVisibility(type: string, hidden: number | null) helper: guard the type with isMemoryTypeKey (Plan 01) and read MEMORY_TYPE_REGISTRY[type].visibilityDefault for registered types; treat any hidden value other than 0 or 1 as null; document in the JSDoc that an unregistered persisted type resolves to 'show' (fail-visible, never fail-hidden — review M6/L4; the column has no CHECK by project convention, ADR-028 text). Ensure MemoryRow carries all metadata columns. Add a profile-visible read/selector that applies resolveVisibility to filter hidden items while keeping the deterministic ORDER BY (this is the presentation filter the Plan 06 surface consumes). Keep resolveVisibility pure and node-tested. Add memories-read.test.ts covering the three hidden states against a type whose registry default is 'show' and one whose default is 'hide', the out-of-range hidden case (2 → type default), the unknown-type case ('bogus' → 'show', no throw), and order preservation.
  </action>
  <verify>
    <automated>npx vitest run src/db/memories-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/memories-read.test.ts` passes all three hidden-state cases, the out-of-range hidden case, the unknown-type case, and the order-preservation case (review L4, M6).
    - resolveVisibility never returns a hardcoded default — the test asserts it follows a registry-default change.
    - `grep -n "isMemoryTypeKey" src/db/memories-read.ts` shows the guard used inside resolveVisibility.
    - tsc --noEmit + `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The read choke point exposes full memory metadata and resolves visibility from the registry default plus the per-item override, presentation-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → memories-dao SQL | User edits Memory metadata; patch must not clobber concurrent field edits or cross contacts |
| memories-read → Profile render | Visibility resolution decides what surfaces; must be presentation-only, never a privacy gate |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-01 | Tampering | editMemory UPDATE | medium | mitigate | Patch SET is closed-set column names; all values ?-bound; WHERE id=? AND contact_id=? + assertOneChange |
| T-24.1-02 | Information Disclosure / data loss | soft-delete + visibility | medium | mitigate | Soft-delete never physically removes a row; live read filters deleted_at IS NULL; hidden is presentation-only with no AI/search consumer in 24.1 |
| T-24.1-05 | Information Disclosure | hidden mistaken for privacy | low | mitigate | resolveVisibility documented + tested as presentation-only; no egress path reads hidden this phase |
| T-24.1-15 | Tampering / integrity | memories.type outside the registry via editMemory | medium | mitigate | editMemoryCore reuses assertRegisteredMemoryType + custom-requires-label on the effective post-patch state before any write; resolveVisibility fail-visible on an unknown persisted type (review M6) |
</threat_model>

<verification>
- `npx vitest run src/db/memories-dao.test.ts src/db/memories-read.test.ts` green.
- Soft-path-is-UPDATE and both-keys-scoping greps pass.
- tsc --noEmit + `npm run check:colors` clean.
</verification>

<success_criteria>
The memories layer supports patch-scoped edit, notes/links/dates/pin/outdated, soft-delete + restore, structured provenance, and registry-driven presentation-only visibility — all node-tested and following the shared mutex pattern.
</success_criteria>

## Artifacts this plan produces

- **DAO writers (src/db/memories-dao.ts):** editMemoryCore/editMemory (reusing assertRegisteredMemoryType + custom-requires-label), deleteMemoryCore/deleteMemory (soft), restoreMemoryCore/restoreMemory.
- **Read (src/db/memories-read.ts):** resolveVisibility(type, hidden) (defensive: out-of-range hidden → type default; unknown type → 'show'), metadata-complete MemoryRow, profile-visible selector.
- **Tests:** memories-dao.test.ts (edit/soft-delete/restore + M6 rejections), memories-read.test.ts (visibility + order + L4/M6 defensive cases).
- **Launch-sweep hooks:** none (Plan 05).

## Review dispositions (cycle 1)

- **M6** — INCORPORATED: Task 1 (`editMemory` reuses Plan 01's `assertRegisteredMemoryType`; custom-requires-label evaluated on the effective post-patch state; two rejection tests) + Task 3 (`resolveVisibility` tolerates an unknown persisted type → 'show', tested). T-24.1-15 added.
- **L4** (hidden half) — INCORPORATED: Task 3 (out-of-range `hidden` resolves to the type default, tested). The promote-already-current half is Plan 04 Task 2.
- Fact drift (advisory) — the must-have truth previously named the patchable column `kind`; corrected to `type` (the migration-016 column name).
- Full consolidated table: `24.1-01-PLAN.md` § Review dispositions (cycle 1).

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-02-SUMMARY.md` when done.
</output>
