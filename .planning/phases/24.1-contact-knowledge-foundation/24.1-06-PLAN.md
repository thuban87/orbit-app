---
phase: 24.1-contact-knowledge-foundation
plan: 06
type: execute
wave: 3
depends_on: ["24.1-02", "24.1-03", "24.1-04"]
files_modified:
  - src/screens/ThingsToRememberScreen.tsx
  - src/components/MemoryEditor.tsx
  - src/components/MemoryCard.tsx
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [KNOW-01, KNOW-03, KNOW-05, KNOW-06, KNOW-08, KNOW-09]
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-01: one visually grouped surface, featured/current first ---
    - "The Things-to-Remember surface presents first-class current-state values, custom fields, structured relationships, and typed Memories TOGETHER in one visually grouped surface (not a single feed), with featured/current information first, grouped per KNOWLEDGE_GROUP_ORDER from the registry (KNOW-01)."
    - "Group headers use the registry display name (or the Memory's custom_label for the 'custom' type via the single-source PROVISIONAL_MEMORY_LABEL constant), never a per-screen hardcoded string."
    - "The surface honors per-item visibility: items resolving to 'hide' (resolveVisibility) are excluded from the normal render (KNOW-08), which is presentation-only — hiding never removes the item from storage or from Recently Deleted reachability."
    - "History-aware fields (Last Talked About, Current Location) show only the most-recent current value with a drill-in affordance to the full backlist (KNOW-03)."
    - "A Memory card renders provenance only in its detail/edit view, never on the compact card (KNOW-09)."
    # --- KNOW-06: memory editor ---
    - "The Memory editor creates/edits a typed Memory with its type (from the application-owned registry; 'custom' carries a user label), value, optional note, optional link, optional meaningful date, pin, and outdated/inactive marker; it holds NO DB access (parent owns persistence + reload, the FuelEditor posture)."
    - "Deleting a Memory from the surface is a soft-delete: it shows a 'Moved to Recently Deleted' confirmation with Undo (Undo restores via restoreMemory) and is not styled danger."
    # --- KNOW-01 edges (specless probe) ---
    - "KNOW-01 empty: a contact with no knowledge renders the 'Nothing to remember yet' empty state (calm, no error styling)."
    - "KNOW-01 adjacency: two items comparing equal render as separate rows and never merge."
    - "KNOW-01 ordering: within a group, equal-comparing items render in a deterministic order (pinned first, then meaningful_date DESC NULLs-last, then created_at DESC, then id DESC)."
    # --- UI-SPEC UI Considerations (24.1-relevant) ---
    - "UI: E1 empty renders 'Nothing to remember yet' + the documented body copy; E3 backlist empty renders 'No earlier entries' with the current value still shown above."
    - "UI: E4 partial — blank optional metadata (note/date/link/label absent) consumes NO space; the card renders only present fields (no empty rows)."
    - "UI: E8 zero-one-many — a repeatable group shows ~3 items then a 'View all' tertiary link (accentText) into the drill-in backlist; the cap is a declared constant, not per-screen."
    - { statement: "UI: E4 long-text — a long Memory value/note/relationship name truncates on the compact card and shows in full on drill-in/detail; reflows (never clip-shrinks) at max OS text scale.", verification: backstop }
    - { statement: "UI: E4 overflow — off-limits marker + pin + outdated markers co-occurring on one card wrap/reflow, each keeps a >=44px target and an accessible label, and no state is colour-only encoded. (AI sparkle is Phase 24.2 and is not shown here.)", verification: backstop }
    - { statement: "UI: default/general Memory-type NAME copy is human_needed (D-11) — rendered from a single-source provisional constant until the owner reconciles it; surfaced as human_needed at verify time, never a silent guess.", verification: backstop }
  artifacts:
    - "src/screens/ThingsToRememberScreen.tsx — the full grouped surface (extends the Plan 01 tracer)"
    - "src/components/MemoryEditor.tsx — the create/edit Memory editor (FuelEditor sibling)"
    - "src/components/MemoryCard.tsx — the compact Memory card"
    - "RecentlyDeleted + MemoryHistory route TYPES registered (screens are themed placeholders until Plan 07 replaces them)"
  key_links:
    - "the surface composes reads from memories-read (Plan 02), relationships-read (Plan 03), current-state-history-read (Plan 04), and the EXISTING field-values-dao getValuesForContact for read-only custom fields — never inline SQL in the component"
    - "colours resolve through useTheme().colors.*; Off Limits (where it surfaces) is caution (borderStrong + neutral pill), never danger; outdated is de-emphasized via textSecondary + reduced opacity, no new token"
  flagged_assumptions:
    - "UI-SPEC 'unresolved': reduced-motion behavior for Things-to-Remember transitions must respect the Phase-23 reduced-motion hook (THEME-06), and backup/restore round-trip fidelity of soft-deleted/history/pin/visibility rows is a Phase-36 concern — both are cross-phase seams out of scope for this plan's pixel layout. Planner assumption: 24.1 adds no non-essential motion here and declares (not emits) the backup entity list."
    - "Custom-field VALUE widgets, the per-item AI toggle, the sparkle glyph, and dashboard search UI are Phase 24.2 — this surface DISPLAYS existing custom-field values read-only and shows no AI affordance."
---

<objective>
Build the unified "Things to Remember" surface (KNOW-01) — one visually grouped view over current-state values, custom fields (read-only), relationships, and typed Memories, featured/current first — plus the Memory editor for create/edit/pin/outdated/visibility/soft-delete, honoring the approved UI-SPEC contract.

Purpose: Turn the phase's data layers into the one product surface the requirement names, reachable and token-clean, ready for Phase 31 to absorb into the Profile factory order.

Output: the full ThingsToRememberScreen + MemoryEditor + MemoryCard, with RecentlyDeleted/MemoryHistory route types registered for Plan 07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-02-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-03-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-04-SUMMARY.md
@src/components/FuelEditor.tsx
@src/db/memory-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemoryCard + MemoryEditor components (parent-owns-persistence)</name>
  <files>src/components/MemoryCard.tsx, src/components/MemoryEditor.tsx</files>
  <read_first>
    - src/components/FuelEditor.tsx (parent-owns-persistence posture; draft/commit boundary; blank→NULL at commit; zero-dep KindPicker Pressable+Modal+FlatList idiom; token-only colours)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (Typography roles; Color reserved-for list; Copywriting Contract — CTA "Add memory", markers, soft-delete "Moved to Recently Deleted" + Undo, visibility caption "presentation only")
    - src/db/memory-registry.ts (MEMORY_TYPE_REGISTRY, PROVISIONAL_MEMORY_LABEL, MemoryTypeKey)
    - src/components/icons/icon-registry.ts (use only registered semantic names; do NOT add a sparkle entry — AI marker is Phase 24.2)
  </read_first>
  <action>
    Create src/components/MemoryCard.tsx: a compact presentational card rendering a Memory's type display name (registry, or custom_label via PROVISIONAL_MEMORY_LABEL), value, and ONLY the present optional fields (note/link/meaningful-date consume no space when absent — UI-SPEC E4 partial). Long value/note truncate on the card (full on detail). Render pin/outdated markers (and Off Limits caution styling where a Memory represents an off-limits topic) with >=44px targets and accessible labels, wrapping when several co-occur; outdated reads de-emphasized (textSecondary + reduced opacity). No provenance on the card (detail only). No AI/sparkle affordance (Phase 24.2). All colours via useTheme().colors.*.

    Create src/components/MemoryEditor.tsx as a FuelEditor sibling: holds no DB access, imports no DAO; the parent passes items + onAdd/onEdit/onDelete/onRestore callbacks and calls the DAO then reload(). A type picker (default + custom, with a label field shown for 'custom') uses the zero-dependency Pressable+Modal+FlatList idiom. Fields: value, optional note, optional link, optional meaningful date, pin toggle, mark-outdated toggle, and a visibility override (Hide from Profile / Show on Profile) with the caption clarifying it is presentation-only. Blank optionals normalize to NULL at the single commit boundary. Delete triggers the parent's soft-delete and the surface shows "Moved to Recently Deleted" + Undo; delete is not styled danger.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean for both files.
    - `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(|sparkle" src/components/MemoryCard.tsx src/components/MemoryEditor.tsx` (comment-filtered) returns nothing (no hex, no AI sparkle in 24.1).
    - The editor imports no `@/db/` DAO (parent-owns-persistence): `grep -n "@/db/" src/components/MemoryEditor.tsx` shows only type-only imports (memory-registry types), no DAO writer.
  </acceptance_criteria>
  <done>The Memory card and editor render the full metadata contract, token-clean, with soft-delete+Undo and no AI affordance.</done>
</task>

<task type="auto">
  <name>Task 2: ThingsToRememberScreen — the grouped surface</name>
  <files>src/screens/ThingsToRememberScreen.tsx</files>
  <read_first>
    - src/screens/ThingsToRememberScreen.tsx (the Plan 01 tracer to extend)
    - src/db/memories-read.ts, src/db/relationships-read.ts, src/db/current-state-history-read.ts (the reads to compose)
    - src/db/field-values-dao.ts (getValuesForContact — EXISTING custom-field values, read-only display; do NOT modify custom-field storage — that is Phase 24.2)
    - src/db/memory-registry.ts (KNOWLEDGE_GROUP_ORDER for grouping + featured/current ordering)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (grouping, ~3-then-View-all cap, empty state, heading role)
  </read_first>
  <action>
    Extend src/screens/ThingsToRememberScreen.tsx into the full grouped surface. Compose the reads (memories-read profile-visible selector, relationships-read, current-state-history-read current values, and getValuesForContact for read-only custom fields) in a useFocusEffect with a cancelled-flag async guard (offline; no blocking network on the read path). Render featured/current information first (pinned items + current-state current values) then grouped detail ordered by KNOWLEDGE_GROUP_ORDER, each group visually distinct (not one feed), header = registry display name. Apply the ~3-items-then-"View all" cap (a declared constant) per repeatable group; "View all" and each history-aware field's backlist affordance navigate to MemoryHistory. Wire the MemoryEditor: the screen owns persistence (calls addMemory/editMemory/deleteMemory/restoreMemory then reload) and shows the "Moved to Recently Deleted" + Undo confirmation. Add a "Recently Deleted" entry that navigates to RecentlyDeleted. Render the "Nothing to remember yet" empty state when every group is empty. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "getValuesForContact\|listMemoriesForContact\|listRelationshipsForContact\|getCurrentStateValue" src/screens/ThingsToRememberScreen.tsx` shows the surface composing all four read sources (no inline SQL in the screen).
    - The screen contains no INSERT/UPDATE/DELETE of custom_field_* (read-only display): `grep -nE "custom_field_(defs|values)" src/screens/ThingsToRememberScreen.tsx` returns nothing.
    - Device UAT (phase gate): featured/current first, groups visually separated, ~3-then-View-all, empty state, soft-delete+Undo all behave per UI-SPEC on the Pixel.
  </acceptance_criteria>
  <done>The unified grouped surface renders all four knowledge structures, featured/current first, with the editor, caps, drill-ins, and empty state.</done>
</task>

<task type="auto">
  <name>Task 3: Register RecentlyDeleted + MemoryHistory routes (placeholders for Plan 07)</name>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/types.ts (additive-route idiom + the "themed placeholder until a later plan replaces it" pattern already used for placeholder routes)
    - src/navigation/RootNavigator.tsx (placeholder screen registration)
  </read_first>
  <action>
    Add two serializable routes to DashboardStackParamList: RecentlyDeleted: { contactId: number } and MemoryHistory: { contactId: number; fieldKey: string }. Register both in RootNavigator.tsx as themed placeholder screens (the established idiom — Plan 07 replaces the placeholders with the real RecentlyDeletedScreen and MemoryHistoryScreen). initialRouteName stays Home; no existing route changes. This lets Task 2's navigation calls typecheck and be reachable now.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RecentlyDeleted\|MemoryHistory" src/navigation/types.ts src/navigation/RootNavigator.tsx` shows both routes registered in both files.
    - tsc --noEmit clean (Task 2's navigate calls resolve against the new param list).
  </acceptance_criteria>
  <done>RecentlyDeleted and MemoryHistory routes exist as typed, reachable placeholders for Plan 07 to realize.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThingsToRememberScreen → read choke points | Composes four local reads; must not add a network call on the read path |
| MemoryEditor → parent → DAO | Editor holds no DB access; persistence + visibility resolution stay server-of-record correct |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-05 | Information Disclosure | visibility / Off Limits presentation | medium | mitigate | resolveVisibility is presentation-only (Plan 02); Off Limits styled caution not privacy; no AI/egress affordance ships in 24.1 |
| T-24.1-10 | Information Disclosure | read-only custom-field display | low | mitigate | Surface only DISPLAYS getValuesForContact; performs no custom_field_* write (grep-guarded) — ADR-001 storage untouched |
| T-24.1-11 | Tampering | inline SQL in a component | low | mitigate | All data flows through DAO reads; no SQL literal in the screen (CLAUDE.md DAO rule) |
</threat_model>

<verification>
- tsc --noEmit + `npm run check:colors` clean across the changed files.
- Composition + no-custom-field-write + no-hex/no-sparkle greps pass.
- Device UAT at the phase gate on the Pixel (grouping, featured-first, caps, empty, soft-delete+Undo, visibility).
</verification>

<success_criteria>
The Things-to-Remember surface renders all four knowledge structures in one visually grouped view, featured/current first, with a full Memory editor and soft-delete+Undo — matching the approved UI-SPEC and reachable from a contact profile.
</success_criteria>

## Artifacts this plan produces

- **Screens/components:** src/screens/ThingsToRememberScreen.tsx (full), src/components/MemoryEditor.tsx, src/components/MemoryCard.tsx.
- **Routes:** RecentlyDeleted + MemoryHistory route types + placeholder registrations (Plan 07 realizes).
- **No new DAOs/migrations/sweeps** — this plan is UI composition over Plans 01-04.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-06-SUMMARY.md` when done.
</output>
