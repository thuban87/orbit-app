---
phase: 24.1-contact-knowledge-foundation
plan: 06
type: execute
wave: 3
depends_on: ["24.1-02", "24.1-03", "24.1-04"]
files_modified:
  - src/screens/ThingsToRememberScreen.tsx
  - src/components/MemoryEditor.tsx
  - src/components/MemoryCard.tsx
  - src/screens/placeholders/KnowledgePlaceholders.tsx
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
autonomous: true
requirements: [KNOW-01, KNOW-03, KNOW-05, KNOW-06, KNOW-08, KNOW-09]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-01: one visually grouped surface, featured/current first ---
    - "The Things-to-Remember surface presents first-class fields (the read-only stable set from getFirstClassFields — Birthday, Social Battery, Contact Frequency, Category — plus the two current-state fields, plus the DERIVED gravity tier and intensity(closeness) from getFirstClassDerived), custom fields, structured relationships, and typed Memories TOGETHER in one visually grouped surface (not a single feed), with featured/current information first, grouped per KNOWLEDGE_GROUP_ORDER from the registry (KNOW-01; review M8; owner decision cycle-5)."
    - "Gravity and intensity(closeness) render as READ-ONLY first-class rows derived at read time (getFirstClassDerived → computeContactGravity/computeContactIntensity over getImpactInputs) — DERIVED-NEVER-STORED, no column, no write, migration 016 unchanged; the derivation is local SQLite only (no network on the read path). Gravity shows its tier NAME (never a raw number — the gravity coarse-tier decision); intensity renders only for a Bound contact (available), matching how ContactProfileScreen already displays them (owner decision cycle-5)."
    - "Group headers use the registry display name (MEMORY_TYPE_REGISTRY for Memory types, CURRENT_STATE_FIELD_REGISTRY for the history-aware fields, RELATIONSHIPS_GROUP for Key People, or the Memory's custom_label for the 'custom' type via the single-source PROVISIONAL_MEMORY_LABEL constant), never a per-screen hardcoded string (review M1)."
    - "The custom-field group is loaded through the existing contract — listDefs(exec, { includeQuarantined: false }) → getValuesForContact(exec, contactId, defs) → visibleDefsForProfile(defs, values) — so labels, types, order, and which values render come from the definitions, exactly as ContactProfileScreen loads them (review M2)."
    - "The surface honors per-item visibility: Memories resolving to 'hide' (resolveVisibility) and relationships resolving to 'hide' (resolveRelationshipVisibility) are excluded from the normal render (KNOW-08; review L5), which is presentation-only — hiding never removes the item from storage or from Recently Deleted reachability."
    - "An include-hidden TOGGLE (off by default) makes hidden LIVE items reachable per DECIDED dossier §G ('Hidden-from-Profile is presentation-only, not a privacy rule'): with it off the surface shows only profile-visible items; toggled on it reveals the hidden items inline, visually de-emphasized (textSecondary + reduced opacity, no new token), each carrying an Unhide / 'Show on Profile' affordance that clears the hidden flag through the EXISTING editor path (editMemory / editRelationship, hidden→0) then reloads. No new schema — the hidden flag already exists (review cycle-5 F1)."
    - "History-aware fields (Last Talked About, Current Location) show only the most-recent current value with a drill-in affordance to the full backlist (KNOW-03), and each row is rendered even when no value exists yet ('Not set' state) so the field stays reachable."
    - "A user can enter a FIRST Last Talked About / Current Location value from the surface: each history-aware row carries a 'Set new value' affordance wired to setCurrentStateValue (then reload) — success criterion 2 is verifiable on-device without DAO seeding (review H1)."
    - "The Memory editor (the detail/edit view) renders the item's provenance READ-ONLY as a caption/row sourced from MemoryRow.provenance for an EXISTING item — the KNOW-09 rendering path ('shown mainly in detail/edit views'); the compact MemoryCard never shows provenance, and the create form shows none (no provenance yet) — review N2."
    # --- KNOW-06: memory editor ---
    - "The Memory editor creates/edits a typed Memory with its type (from the application-owned registry; 'custom' carries a user label), value, optional note, optional link, optional meaningful date, pin, and outdated/inactive marker, and surfaces the existing item's provenance READ-ONLY (review N2, KNOW-09); it holds NO DB access (parent owns persistence + reload, the FuelEditor posture)."
    - "Deleting a Memory from the surface is a soft-delete: it shows a 'Moved to Recently Deleted' confirmation with Undo (Undo restores via restoreMemory) and is not styled danger."
    # --- KNOW-01 edges (specless probe) ---
    - "KNOW-01 empty: a contact with no Memories / relationships / custom values / current-state history AND no genuinely-optional first-class value (birthday, social battery) renders the 'Nothing to remember yet' empty state (calm, no error styling). The empty-state GATE excludes the near-always-populated Category and Contact Frequency (interval_days) columns so a contact that has only a category or a cadence still reaches E1 — this narrows only the gate; the rendered read-only first-class SET (birthday / social_battery / interval_days / category) is unchanged (review N5)."
    - "KNOW-01 adjacency: two items comparing equal render as separate rows and never merge."
    - "KNOW-01 ordering: within a group, equal-comparing items render in a deterministic order (pinned first, then meaningful_date DESC NULLs-last, then created_at DESC, then id DESC)."
    # --- UI-SPEC UI Considerations (24.1-relevant) ---
    - "UI: E1 empty renders 'Nothing to remember yet' + the documented body copy; E3 backlist empty renders 'No earlier entries' with the current value still shown above."
    - "UI: E4 partial — blank optional metadata (note/date/link/label absent) consumes NO space; the card renders only present fields (no empty rows)."
    - "UI: E8 zero-one-many — a repeatable group shows ~3 items then a 'View all' tertiary link (accentText) into the drill-in backlist; the cap is a declared constant, not per-screen."
    - { statement: "UI: E4 long-text — a long Memory value/note/relationship name truncates on the compact card and shows in full on drill-in/detail; reflows (never clip-shrinks) at max OS text scale.", verification: backstop }
    - { statement: "UI: E4 overflow — pin + outdated markers co-occurring on one card wrap/reflow, each keeps a >=44px target and an accessible label, and no state is colour-only encoded. (Off Limits and the AI sparkle are Phase 24.2 — neither is rendered here; review L1.)", verification: backstop }
    - { statement: "UI: default/general Memory-type NAME copy is human_needed (D-11) — rendered from a single-source provisional constant until the owner reconciles it; surfaced as human_needed at verify time, never a silent guess.", verification: backstop }
  artifacts:
    - "src/screens/ThingsToRememberScreen.tsx — the full grouped surface (extends the Plan 01 tracer)"
    - "src/components/MemoryEditor.tsx — the create/edit Memory editor (FuelEditor sibling)"
    - "src/components/MemoryCard.tsx — the compact Memory card"
    - "src/screens/placeholders/KnowledgePlaceholders.tsx — RecentlyDeletedPlaceholderScreen + MemoryHistoryPlaceholderScreen (FabActionPlaceholders idiom; Plan 07 replaces and deletes them)"
    - "RecentlyDeleted + MemoryHistory route TYPES in BOTH DashboardStackParamList and OrreryStackParamList; placeholder Stack.Screens mounted in BOTH tabs/DashboardStack.tsx and tabs/OrreryStack.tsx (review H3)"
  key_links:
    - "the surface composes reads from memories-read (Plan 02), relationships-read (Plan 03), current-state-history-read + first-class-knowledge-read (Plan 04), and the EXISTING field-defs-dao listDefs + field-values-dao getValuesForContact / visibleDefsForProfile for read-only custom fields — never inline SQL in the component (review M2, M8)"
    - "the surface owns persistence for every write it exposes — addMemory / editMemory / deleteMemory / restoreMemory (Plan 02) and setCurrentStateValue (Plan 04) — then reloads; editors hold no DB access (review H1)"
    - "colours resolve through useTheme().colors.*; outdated is de-emphasized via textSecondary + reduced opacity, no new token; no Off Limits / danger styling on this surface (review L1)"
  flagged_assumptions:
    - "UI-SPEC 'unresolved': reduced-motion behavior for Things-to-Remember transitions must respect the Phase-23 reduced-motion hook (THEME-06), and backup/restore round-trip fidelity of soft-deleted/history/pin/visibility rows is a Phase-36 concern — both are cross-phase seams out of scope for this plan's pixel layout. Planner assumption: 24.1 adds no non-essential motion here and declares (not emits) the backup entity list."
    - "Custom-field VALUE widgets, the per-item AI toggle, the sparkle glyph, Off Limits markers, and dashboard search UI are Phase 24.2 — this surface DISPLAYS existing custom-field values read-only and shows no AI or Off Limits affordance. Doc-sync resolved (owner decision cycle-5): 24.1-UI-SPEC.md's `sparkle` (AI-enabled) entry is now annotated Phase-24.2-scoped so the UI-SPEC matches these plans; per CONTEXT D-08/D-09 that entry ships with the per-item AI permission in 24.2. No `sparkle` is added to any 24.1 plan."
    - "First-class read-only set (review M8; owner decision cycle-5): the surface renders Birthday, Social Battery, Contact Frequency, and Category from getFirstClassFields (Plan 04) plus Current Location and Last Talked About from current-state entries, PLUS the derived Gravity tier and Intensity(closeness) from getFirstClassDerived — the dossier §A properties. Gravity and Intensity are DERIVED-NEVER-STORED (computed at read time from the interaction history via getImpactInputs → computeContactGravity/computeContactIntensity, local SQLite only, no network — exactly as ContactProfileScreen already displays); per the owner's cycle-5 decision BOTH are INCLUDED here (the earlier 'omitted, no column' rationale was wrong and is retracted). These first-class values are DISPLAY-ONLY here: editing stays on Edit Contact (Phase 34 Update Contact adds quick edit); Phase 31 owns the exact Profile card appearance (dossier defers it). Empty-state gate (review N5, cycle-5): to keep UI-SPEC E1 reachable, the near-always-populated Category + Contact Frequency columns AND the derived Gravity/Intensity are EXCLUDED from the 'nothing to remember' gate (which counts only birthday / social battery / current-state / custom / relationships / memories); this narrows only the gate and does NOT change the rendered first-class set (birthday / social_battery / interval_days / category + the derived gravity/intensity rows are all still displayed when present)."
---

<objective>
Build the unified "Things to Remember" surface (KNOW-01) — one visually grouped view over current-state values, custom fields (read-only), relationships, and typed Memories, featured/current first — plus the Memory editor for create/edit/pin/outdated/visibility/soft-delete, honoring the approved UI-SPEC contract.

Purpose: Turn the phase's data layers into the one product surface the requirement names, reachable and token-clean, ready for Phase 31 to absorb into the Profile factory order.

Output: the full ThingsToRememberScreen + MemoryEditor + MemoryCard, with RecentlyDeleted/MemoryHistory route types registered for Plan 07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-02-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-03-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-04-SUMMARY.md
@src/components/FuelEditor.tsx
@src/db/memory-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemoryCard + MemoryEditor components (parent-owns-persistence)</name>
  <files>src/components/MemoryCard.tsx, src/components/MemoryEditor.tsx</files>
  <read_first>
    - src/components/FuelEditor.tsx (parent-owns-persistence posture; draft/commit boundary; blank→NULL at commit; zero-dep KindPicker Pressable+Modal+FlatList idiom; token-only colours)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (Typography roles; Color reserved-for list; Copywriting Contract — CTA "Add memory", markers, soft-delete "Moved to Recently Deleted" + Undo, visibility caption "presentation only")
    - src/db/memory-registry.ts (MEMORY_TYPE_REGISTRY, PROVISIONAL_MEMORY_LABEL, MemoryTypeKey)
    - src/components/icons/icon-registry.ts (use only registered semantic names; do NOT add a sparkle entry — AI marker is Phase 24.2)
  </read_first>
  <action>
    Create src/components/MemoryCard.tsx: a compact presentational card rendering a Memory's type display name (registry, or custom_label via PROVISIONAL_MEMORY_LABEL), value, and ONLY the present optional fields (note/link/meaningful-date consume no space when absent — UI-SPEC E4 partial). Long value/note truncate on the card (full on detail). Render pin and outdated markers with >=44px targets and accessible labels, wrapping when both co-occur; outdated reads de-emphasized (textSecondary + reduced opacity). No Off Limits marker or caution styling on the card (review L1 — no off-limits Memory type exists in 24.1's registry; Off Limits is a fuel.kind state today and its Memory representation is 24.2 KNOW-12; the UI-SPEC's Off-Limits copy applies only when 24.2 lands it). No provenance on the card (detail only). No AI/sparkle affordance (Phase 24.2). All colours via useTheme().colors.*.

    Create src/components/MemoryEditor.tsx as a FuelEditor sibling: holds no DB access, imports no DAO (type-only imports of MemoryRow / registry types are fine); the parent passes items + onAdd/onEdit/onDelete/onRestore callbacks and calls the DAO then reload(). A type picker (default + custom, with a label field shown for 'custom') uses the zero-dependency Pressable+Modal+FlatList idiom. Fields: value, optional note, optional link, optional meaningful date, pin toggle, mark-outdated toggle, and a visibility override (Hide from Profile / Show on Profile) with the caption clarifying it is presentation-only. Blank optionals normalize to NULL at the single commit boundary. For an EXISTING Memory (edit mode), render a READ-ONLY provenance row/caption sourced from the passed item's MemoryRow.provenance — the documented 'user' | 'import' | 'share' vocabulary mapped to a human label (e.g. 'Added by you' / 'Imported from Contacts' / 'From a shared capture') via a single-source screen constant, using the caption typography role and textSecondary; provenance is NOT editable and is NOT wired to any onEdit patch field, and the create form renders no provenance (none exists yet) — this is the KNOW-09 'shown mainly in detail/edit views' rendering path (review N2). Delete triggers the parent's soft-delete and the surface shows "Moved to Recently Deleted" + Undo; delete is not styled danger.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean for both files.
    - `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(|sparkle" src/components/MemoryCard.tsx src/components/MemoryEditor.tsx` (comment-filtered) returns nothing (no hex, no AI sparkle in 24.1).
    - `grep -niE "off.?limits" src/components/MemoryCard.tsx src/components/MemoryEditor.tsx` (comment-filtered) returns nothing (review L1).
    - The editor imports no `@/db/` DAO (parent-owns-persistence): `grep -n "@/db/" src/components/MemoryEditor.tsx` shows only type-only imports (memory-registry types, MemoryRow type), no DAO writer.
    - MemoryEditor renders provenance read-only for an existing item (review N2, KNOW-09): `grep -n "provenance" src/components/MemoryEditor.tsx` shows the read-only provenance caption sourced from the item's MemoryRow.provenance, and provenance is NOT referenced in any onEdit/patch payload (display-only, not an editable field).
  </acceptance_criteria>
  <done>The Memory card and editor render the full metadata contract (including read-only provenance in the editor's detail/edit view — review N2), token-clean, with soft-delete+Undo and no AI or Off Limits affordance.</done>
</task>

<task type="auto">
  <name>Task 2: ThingsToRememberScreen — the grouped surface</name>
  <files>src/screens/ThingsToRememberScreen.tsx</files>
  <read_first>
    - src/screens/ThingsToRememberScreen.tsx (the Plan 01 tracer to extend)
    - src/db/memories-read.ts (listMemoriesForContact full live projection + resolveVisibility — used to PARTITION live items into profile-visible vs hidden for the include-hidden toggle), src/db/relationships-read.ts (listRelationshipsForContact + resolveRelationshipVisibility), src/db/current-state-history-read.ts (getCurrentStateValues — the batched read), src/db/first-class-knowledge-read.ts (getFirstClassFields AND getFirstClassDerived — the derived gravity+intensity read, owner decision cycle-5) — the reads to compose
    - src/screens/ContactProfileScreen.tsx:1208-1230 (how gravity + intensity are already displayed read-only — gravity.tierName is the LABEL never a raw number; intensity rendered only when available; copy this presentation intent, tokens only) and :278-290 (deriving both from one getImpactInputs read)
    - src/db/current-state-history-dao.ts (setCurrentStateValue — the ONE current-state writer this surface wires; review H1)
    - src/db/field-defs-dao.ts:206 (listDefs(exec, { includeQuarantined: false })) and src/db/field-values-dao.ts:20-38 + :126-135 (getValuesForContact(exec, contactId, defs) REQUIRES the defs list; visibleDefsForProfile(defs, values) is the pure profile selector) — the EXISTING custom-field load contract; read-only display; do NOT modify custom-field storage — that is Phase 24.2 (review M2)
    - src/screens/ContactProfileScreen.tsx:236-265 (how the profile already loads listDefs → getValuesForContact — copy this exact composition)
    - src/db/memory-registry.ts (KNOWLEDGE_GROUP_ORDER for grouping + featured/current ordering; CURRENT_STATE_FIELD_KEYS / CURRENT_STATE_FIELD_REGISTRY for history-aware rows + headers; RELATIONSHIPS_GROUP for the Key People header — review M1, L5)
    - src/components/FuelEditor.tsx (the single-transient-draft + commit-boundary idiom the inline "Set new value" input reuses)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (grouping, ~3-then-View-all cap, empty state, heading role, "most recent" caption)
  </read_first>
  <action>
    Extend src/screens/ThingsToRememberScreen.tsx into the full grouped surface. Compose the reads in a useFocusEffect with a cancelled-flag async guard (offline; NO blocking network on the read path — all reads are local SQLite): load the FULL live memory list (listMemoriesForContact, deleted_at IS NULL) and the full live relationship list (listRelationshipsForContact), then PARTITION each into a profile-visible set and a hidden set by applying resolveVisibility (memories) / resolveRelationshipVisibility (relationships) on the surface — the visible set is what renders by default, the hidden set feeds the include-hidden toggle below (review L5, cycle-5 F1); getCurrentStateValues (one batched call, review V16); getFirstClassFields (review M8); getFirstClassDerived(exec, contactId, localDateTime()) for the derived gravity tier + intensity (owner decision cycle-5 — DERIVED-NEVER-STORED, local SQLite, no network); and the custom-field contract in this exact order — `const defs = await listDefs(exec, { includeQuarantined: false })`, `const values = await getValuesForContact(exec, contactId, defs)`, `const visibleDefs = visibleDefsForProfile(defs, values)` — rendering each visible def's label + raw value read-only (review M2). Render featured/current information first: the first-class rows (Birthday, Social Battery, Contact Frequency, Category — labels are single-source constants in the screen module, values read-only, blank rows omitted per E4), the DERIVED gravity + intensity rows (gravity shows `gravity.tierName` as the LABEL — never a raw number; intensity renders only when `intensity.available !== false`, mirroring ContactProfileScreen — both read-only, tokens only, omitted when getFirstClassDerived returns them null/unavailable), and the history-aware rows, then grouped detail ordered by KNOWLEDGE_GROUP_ORDER, each group visually distinct (not one feed), header = registry display name (CURRENT_STATE_FIELD_REGISTRY[key].displayName for history-aware rows; RELATIONSHIPS_GROUP.displayName for Key People; MEMORY_TYPE_REGISTRY for Memory groups — review M1).

    Include-hidden toggle (review cycle-5 F1, DECIDED dossier §G): add a surface-level toggle control (a screen-local useState, default OFF, token-clean, accessible label) that reveals the hidden partitions inline. When OFF, render only the profile-visible sets (current behaviour). When ON, render the hidden Memories and hidden relationships too, visually de-emphasized (textSecondary + reduced opacity — the same idiom outdated uses, NO new token), each hidden row carrying an Unhide / "Show on Profile" affordance (>=44px target, accessible label) that calls the parent's editMemory({ id, contactId, hidden: 0 }) (or, for a relationship, editRelationship({ id, contactId, hidden: 0 }) — the relationship persistence + Unhide wiring lands with the RelationshipEditor in Plan 07 Task 3) then reload — clearing the hidden flag through the EXISTING editor path, no new schema. This makes a hidden LIVE item reachable and reversible from the UI, so "presentation-only" is actually reversible per §G.

    History-aware rows (review H1): iterate CURRENT_STATE_FIELD_KEYS so BOTH rows always render — with the current value + "most recent" caption when set, or a "Not set" caption when absent. Each row carries (a) a "Set new value" affordance opening an inline single-transient-draft TextInput (FuelEditor new-item idiom; blank rejected at the commit boundary) whose commit calls setCurrentStateValue(exec, { contactId, fieldKey, value, now: localDateTime() }) then reload — this is how a user enters the FIRST value; and (b) a drill-in affordance (chevron / "History") navigating to MemoryHistory with { contactId, fieldKey }.

    Apply the ~3-items-then-"View all" cap (a declared constant) per repeatable group; "View all" navigates to MemoryHistory for history-aware fields and expands in place for Memory/relationship groups. Wire the MemoryEditor: the screen owns persistence (calls addMemory/editMemory/deleteMemory/restoreMemory then reload) and shows the "Moved to Recently Deleted" + Undo confirmation via the shell snackbar (snackbarStore.show with an Undo action calling restoreMemory). Add a "Recently Deleted" entry that navigates to RecentlyDeleted. Render the "Nothing to remember yet" empty state (UI-SPEC E1) when every group is empty AND no first-class value is present — where, for the GATE only, "first-class value present" counts ONLY the genuinely-optional first-class columns (birthday, social_battery) plus the two current-state fields, and EXCLUDES the near-always-populated Category (categoryName) and Contact Frequency (intervalDays) columns AND the DERIVED gravity + intensity (near-always-present for a contact with any interaction history / a Bound contact, so counting them would defeat E1 — same reasoning as N5), so a contact that has only a category / a cadence / a derived gravity still reaches the empty state (review N5, cycle-5). This narrows only the empty-state gate — the rendered read-only first-class SET (birthday / social_battery / interval_days / category + the derived gravity/intensity rows) is unchanged (all still displayed when present; the protected first-class field set is NOT redefined). All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "listDefs\|visibleDefsForProfile\|getValuesForContact\|listMemoriesForContact\|listRelationshipsForContact\|getCurrentStateValues\|getFirstClassFields\|getFirstClassDerived" src/screens/ThingsToRememberScreen.tsx` shows all eight read symbols composed (review M2, M8, cycle-5; no inline SQL in the screen).
    - `grep -n "getFirstClassDerived\|tierName" src/screens/ThingsToRememberScreen.tsx` shows the derived gravity+intensity rows rendered read-only, gravity by tier NAME not a raw number (owner decision cycle-5); the screen writes nothing derived — `grep -nE "getImpactInputs|computeContactGravity|computeContactIntensity" src/screens/ThingsToRememberScreen.tsx` returns nothing (the derivation is encapsulated in getFirstClassDerived, not re-done inline).
    - `grep -n "setCurrentStateValue" src/screens/ThingsToRememberScreen.tsx` shows the current-state writer wired to the inline "Set new value" commit (review H1).
    - `grep -n "resolveVisibility\|resolveRelationshipVisibility" src/screens/ThingsToRememberScreen.tsx` shows both partition helpers applied on the surface, and `grep -niE "show on profile|unhide" src/screens/ThingsToRememberScreen.tsx` shows the include-hidden toggle's Unhide affordance wired to editMemory (hidden→0) (review cycle-5 F1). The relationship Unhide path is wired in Plan 07 Task 3.
    - `grep -n "CURRENT_STATE_FIELD_KEYS\|CURRENT_STATE_FIELD_REGISTRY\|RELATIONSHIPS_GROUP" src/screens/ThingsToRememberScreen.tsx` shows headers/rows derived from the registry (review M1, L5).
    - The screen contains no INSERT/UPDATE/DELETE of custom_field_* (read-only display): `grep -nE "custom_field_(defs|values)" src/screens/ThingsToRememberScreen.tsx` returns nothing.
    - The empty-state gate excludes the always-populated + derived first-class values (review N5, cycle-5): the empty-state condition references only birthday/social_battery/current-state/custom/relationships/memories (NOT categoryName, intervalDays, gravity, or intensity), and a contact with only a category / cadence / derived gravity set (no memories/relationships/custom/history/birthday/battery) still renders the "Nothing to remember yet" empty state.
    - Device UAT (phase gate): featured/current first (first-class rows including the derived gravity tier + intensity, both history-aware rows visible even when unset), entering a first Last Talked About value from the surface persists and re-renders as "most recent", groups visually separated, ~3-then-View-all, the "Nothing to remember yet" empty state on a category-only/cadence-only contact (review N5), opening an imported/shared Memory shows its provenance read-only in the editor (review N2), soft-delete+Undo, and (review cycle-5 F1) hide a Memory → toggle include-hidden ON → the hidden Memory shows greyed with Unhide → Unhide returns it to the visible surface — all behave per UI-SPEC on the Pixel.
  </acceptance_criteria>
  <done>The unified grouped surface renders first-class fields (including the derived gravity tier + intensity, read-only), custom fields, relationships, and Memories, featured/current first, with the editor, a working set-new-current path, an include-hidden toggle that makes hidden live items reachable and unhideable, caps, drill-ins, and empty state.</done>
</task>

<task type="auto">
  <name>Task 3: Register RecentlyDeleted + MemoryHistory routes in both stacks (placeholders for Plan 07)</name>
  <files>src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx, src/screens/placeholders/KnowledgePlaceholders.tsx</files>
  <read_first>
    - src/navigation/types.ts (DashboardStackParamList AND OrreryStackParamList — Profile/Edit/Compose/ThingsToRemember are declared in BOTH; the "themed placeholder until a later plan replaces it" comment at lines 16-18)
    - src/navigation/tabs/DashboardStack.tsx (Stack.Screen registrations lines 34-59, incl. the Plan 01 ThingsToRemember entry and the FAB placeholder entries at lines 36-42)
    - src/navigation/tabs/OrreryStack.tsx (lines 17-24 — the Orrery-origin duplicate registrations; review H3)
    - src/screens/placeholders/FabActionPlaceholders.tsx (the FabActionPlaceholder themed idiom — ShellAppBar variant="child" + "Coming soon" body, colours via useTheme)
    - src/db/memory-registry.ts (CurrentStateFieldKey — the fieldKey param type)
  </read_first>
  <action>
    Add two serializable routes to BOTH DashboardStackParamList and OrreryStackParamList in src/navigation/types.ts (the Compose/Profile dual-declaration precedent; review H3): RecentlyDeleted: { contactId: number } and MemoryHistory: { contactId: number; fieldKey: CurrentStateFieldKey } (import type CurrentStateFieldKey from "@/db/memory-registry" — a string-literal union is serializable). Create src/screens/placeholders/KnowledgePlaceholders.tsx exporting RecentlyDeletedPlaceholderScreen and MemoryHistoryPlaceholderScreen built on the same themed placeholder shape as FabActionPlaceholders.tsx (ShellAppBar child variant + body copy; every colour via useTheme().colors.*). Mount both placeholders with Stack.Screen in BOTH src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx (Plan 07 replaces the components with the real RecentlyDeletedScreen / MemoryHistoryScreen and deletes the placeholder file). Do NOT touch src/navigation/RootNavigator.tsx. initialRouteName stays Home / Orrery; no existing route changes. This lets Task 2's navigation calls typecheck and be reachable now from both profile origins.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RecentlyDeleted:" src/navigation/types.ts` is 2 and `grep -c "MemoryHistory:" src/navigation/types.ts` is 2 (declared in both param lists — review H3).
    - `grep -n 'name="RecentlyDeleted"\|name="MemoryHistory"' src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx` shows two Stack.Screen registrations in EACH file.
    - `git diff --stat -- src/navigation/RootNavigator.tsx` is empty.
    - `grep -n "fieldKey: CurrentStateFieldKey" src/navigation/types.ts` matches (typed against the Plan 01 registry contract — review M1).
    - tsc --noEmit + `npm run check:colors` clean (Task 2's navigate calls resolve against the new param lists).
  </acceptance_criteria>
  <done>RecentlyDeleted and MemoryHistory routes exist as typed, reachable placeholders in both stacks for Plan 07 to realize.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThingsToRememberScreen → read choke points | Composes four local reads; must not add a network call on the read path |
| MemoryEditor → parent → DAO | Editor holds no DB access; persistence + visibility resolution stay server-of-record correct |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-05 | Information Disclosure | visibility presentation (memories + relationships) | medium | mitigate | resolveVisibility (Plan 02) and resolveRelationshipVisibility (Plan 03) are presentation-only; no Off Limits or AI/egress affordance ships in 24.1 (review L1, L5). The include-hidden toggle (cycle-5 F1) is presentation-only REACHABILITY — it reveals already-stored hidden LIVE items to their owner on-device and Unhide routes through the existing editMemory/editRelationship path (hidden→0); it adds no egress, no schema, and does not treat hidden as a privacy/AI gate (DECIDED dossier §G) |
| T-24.1-16 | Information Disclosure | derived gravity/intensity display | low | mitigate | getFirstClassDerived composes existing node-pure derived-value services over getImpactInputs — LOCAL SQLite read only, NO network on the read path; display-only on-device, DERIVED-NEVER-STORED, no egress (no AI egress in 24.1) — CLAUDE.md local-first (owner decision cycle-5) |
| T-24.1-10 | Information Disclosure | read-only custom-field display | low | mitigate | Surface only DISPLAYS listDefs → getValuesForContact → visibleDefsForProfile (quarantined defs excluded by the existing selector); performs no custom_field_* write (grep-guarded) — ADR-001 storage untouched (review M2) |
| T-24.1-11 | Tampering | inline SQL in a component | low | mitigate | All data flows through DAO reads; no SQL literal in the screen (CLAUDE.md DAO rule) |
| T-24.1-04 | Tampering / data loss | inline set-new-current write | high | mitigate | The surface calls Plan 04's setCurrentStateValue (atomic demote-then-insert, registry-validated field_key, blank rejected); the prior value becomes history, never overwritten (review H1) |
</threat_model>

<verification>
- tsc --noEmit + `npm run check:colors` clean across the changed files.
- Composition (seven reads) + setCurrentStateValue + registry-header + no-custom-field-write + no-hex/no-sparkle/no-off-limits greps pass; both stacks register both placeholder routes.
- Device UAT at the phase gate on the Pixel (grouping, featured-first incl. first-class rows, first-value entry for a history-aware field, caps, empty, soft-delete+Undo, visibility).
</verification>

<success_criteria>
The Things-to-Remember surface renders first-class fields, custom fields, relationships, and Memories in one visually grouped view, featured/current first, with a full Memory editor, a working set-new-current path for the history-aware fields, and soft-delete+Undo — matching the approved UI-SPEC (as split-adjusted) and reachable from a Dashboard- or Orrery-origin contact profile.
</success_criteria>

## Artifacts this plan produces

- **Screens/components:** src/screens/ThingsToRememberScreen.tsx (full; wires setCurrentStateValue + the seven reads), src/components/MemoryEditor.tsx, src/components/MemoryCard.tsx, src/screens/placeholders/KnowledgePlaceholders.tsx (RecentlyDeletedPlaceholderScreen, MemoryHistoryPlaceholderScreen — deleted by Plan 07).
- **Routes:** RecentlyDeleted + MemoryHistory route types in both DashboardStackParamList and OrreryStackParamList; placeholder Stack.Screens in tabs/DashboardStack.tsx + tabs/OrreryStack.tsx (Plan 07 realizes).
- **Screen-local constants:** the repeatable-group cap constant; first-class row label constants (including the gravity + intensity row labels).
- **Derived display:** the featured group now shows the read-only gravity tier + intensity from getFirstClassDerived (owner decision cycle-5); the include-hidden toggle (default off) reveals hidden live items with an Unhide affordance (cycle-5 F1).
- **No new DAOs/migrations/sweeps** — this plan is UI composition over Plans 01-04; gravity/intensity are DERIVED-NEVER-STORED (no column, no write), migration 016 unchanged.

## Review dispositions (cycle 1)

- **H1** — INCORPORATED: Task 2 inline "Set new value" on each history-aware row → `setCurrentStateValue`; rows render even when unset; must-have truth + grep + UAT step. (Plan 07 Task 2 adds the same action on `MemoryHistoryScreen`.)
- **H3** — INCORPORATED: Task 3 declares both routes in both param lists and mounts placeholders in both stacks; `RootNavigator.tsx` removed from `files_modified`.
- **M1** — INCORPORATED (consumer): headers/rows derived from `CURRENT_STATE_FIELD_KEYS` / `CURRENT_STATE_FIELD_REGISTRY` / `RELATIONSHIPS_GROUP`; `MemoryHistory.fieldKey` typed `CurrentStateFieldKey`.
- **M2** — INCORPORATED: Task 2 load contract `listDefs` → `getValuesForContact(exec, contactId, defs)` → `visibleDefsForProfile`; grep-gated.
- **M8** — INCORPORATED: Task 2 renders the `getFirstClassFields` set read-only in the featured group; the set and its omissions are in `flagged_assumptions`.
- **L1** — INCORPORATED: Off Limits marker/styling removed from `MemoryCard`, the E4 overflow truth, key_links, and T-24.1-05; grep-gated.
- **L5** — INCORPORATED (consumer): Task 2 filters Key People through `resolveRelationshipVisibility`.
- **V12** (advisory) — recorded in `flagged_assumptions` as a UI-SPEC doc-sync item; plan unchanged on `sparkle`.
- Full consolidated table: `24.1-01-PLAN.md` § Review dispositions (cycle 1).

## Review dispositions (cycle 2)

- **N2 (MEDIUM)** — INCORPORATED: Task 1 gives KNOW-09 provenance an actual rendering path — `MemoryEditor` renders a READ-ONLY provenance caption/row (from `MemoryRow.provenance`, the 'user'|'import'|'share' vocabulary mapped to a human label) for an existing item, not editable, absent on the create form; must-have truths (KNOW-06 editor + the KNOW-09 detail/edit truth) updated, a grep acceptance criterion added, and a provenance UAT step added. `MemoryCard` still shows no provenance (glanceable-card contract, cycle-1 L1).
- **N5 (LOW)** — INCORPORATED (gate-exclusion option): Task 2's empty-state gate now excludes the near-always-populated Category and Contact Frequency columns so UI-SPEC E1 stays reachable for a category-only/cadence-only contact. This narrows ONLY the empty-state gate — the owner-protected read-only first-class field SET (birthday / social_battery / interval_days / category) is unchanged and still fully rendered when present. Recorded in the must-have truth, the Task 2 action, an acceptance grep + UAT step, and the first-class `flagged_assumptions` note.

## Review dispositions (cycle 5)

This plan owns two cycle-5 items: the owner's gravity+intensity inclusion (the display side) and F1 (the include-hidden toggle). Full consolidated cycle-5 table: `24.1-01-PLAN.md` § Review dispositions (cycle 5).

- **Owner decision — gravity + intensity are first-class (INCLUDED)** — INCORPORATED (display side): Task 2 composes `getFirstClassDerived` (Plan 04) and renders the derived gravity tier (by `tierName`, never a raw number) + intensity(closeness, only when available) as READ-ONLY featured rows alongside birthday/social_battery/interval_days/category — mirroring how ContactProfileScreen already displays them. DERIVED-NEVER-STORED, local SQLite (no network on the read path). The empty-state gate also excludes the derived pair (same reasoning as N5). The stale "Gravity/Closeness omitted, no column" `flagged_assumptions` text is corrected; must-have truths, the action, acceptance greps + UAT, and T-24.1-16 added.
- **F1 (MEDIUM, codex) — hidden live items reachable per §G** — INCORPORATED: Task 2 adds an include-hidden TOGGLE (default off). Off = profile-visible only (unchanged); on = hidden live items shown inline, greyed (textSecondary + reduced opacity, no new token), each with an Unhide / "Show on Profile" affordance clearing the hidden flag through the EXISTING editMemory path (hidden→0) then reload. The surface loads the FULL live lists and partitions via resolveVisibility/resolveRelationshipVisibility so the hidden set is available to reveal. This makes a hidden live Memory/relationship reachable and reversible from the UI, so "presentation-only" is actually reversible (DECIDED dossier §G) — a completeness fix, not a decision reversal (nothing weakens §G). No new schema. The relationship-side Unhide wiring lands with the RelationshipEditor in Plan 07 Task 3. Must-have truth, action, acceptance greps, a hide→toggle-on→unhide UAT, and T-24.1-05 updated.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-06-SUMMARY.md` when done.
</output>
