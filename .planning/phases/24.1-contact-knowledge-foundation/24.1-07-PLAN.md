---
phase: 24.1-contact-knowledge-foundation
plan: 07
type: execute
wave: 4
depends_on: ["24.1-05", "24.1-06", "24.1-04", "24.1-03"]
files_modified:
  - src/screens/RecentlyDeletedScreen.tsx
  - src/screens/MemoryHistoryScreen.tsx
  - src/components/RelationshipEditor.tsx
  - src/screens/ThingsToRememberScreen.tsx
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [KNOW-03, KNOW-04, KNOW-05, KNOW-07]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-07: Recently Deleted surface ---
    - "The Recently Deleted screen lists a contact's soft-deleted memories (listRecentlyDeleted), offers Restore (restoreMemory → returns to the live surface) as the primary action, and Delete permanently behind an explicit 'Delete permanently?' confirmation — the ONE danger-styled action in the phase (KNOW-07)."
    - "UI: E2 empty — with no soft-deleted items the screen renders 'Recently Deleted is empty'; the surface still exists (owned by this phase)."
    - "The empty-state expiry copy matches the MEMORY_TRASH_WINDOW_DAYS retention window the sweep enforces — no promised timer (D-11)."
    # --- KNOW-03/04: history drill-in backlist ---
    - "The MemoryHistory screen shows a history-aware field's current value plus the full backlist (getCurrentStateHistory), and lets the user Edit a historical entry and Make current (promote-to-current) an older entry (KNOW-03/04)."
    - "UI: E3 empty — a current-only field with no prior values renders 'No earlier entries' with the current value still shown above it."
    - "Promoting a historical value to current via the screen preserves the prior current value as history (it calls promoteToCurrentValue, which is atomic and non-destructive) — the prior value is never lost."
    # --- KNOW-05: relationship editor ---
    - "The relationship editor creates/edits a structured relationship (person_name required, optional relation_type, optional linked Orbit contact via a contact picker) and soft-deletes it; it holds no DB access (parent owns persistence + reload)."
  artifacts:
    - "src/screens/RecentlyDeletedScreen.tsx — restore + permanent-delete surface (realizes the Plan 06 placeholder)"
    - "src/screens/MemoryHistoryScreen.tsx — history-aware field backlist with Edit + Make current (realizes the Plan 06 placeholder)"
    - "src/components/RelationshipEditor.tsx — create/edit/soft-delete a relationship with optional contact link"
  key_links:
    - "RecentlyDeletedScreen / MemoryHistoryScreen / RelationshipEditor are wired to the Plan 05/04/03 DAOs via the parent screen's persistence calls, never inline SQL"
    - "RootNavigator's RecentlyDeleted + MemoryHistory placeholder screens are replaced by the real screens (types unchanged from Plan 06)"
  prohibitions:
    - "Permanent delete must never be reachable in one tap from the live surface — it lives only behind the Recently Deleted screen's explicit confirmation (two-stage soft-then-permanent)."
    - "Making a historical value current must never destroy the prior current value — it becomes history (KNOW-04)."
---

<objective>
Realize the Recently Deleted / Trash screen, the history-aware drill-in backlist (edit + make-current), and the relationship editor — completing the phase's user-facing surfaces over the Plan 03/04/05 data layers and replacing the Plan 06 route placeholders (KNOW-03/04/05/07).

Purpose: Deliver the restore path, the history editing/promotion UX, and structured-relationship editing so every 24.1 requirement has a reachable surface.

Output: RecentlyDeletedScreen, MemoryHistoryScreen, RelationshipEditor, wired into the routes Plan 06 registered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-03-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-04-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-05-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-06-SUMMARY.md
@src/components/FuelEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecentlyDeletedScreen — restore + guarded permanent delete</name>
  <files>src/screens/RecentlyDeletedScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx (chrome + list posture for a low-traffic recovery surface; restore-only-here idiom)
    - src/db/memories-read.ts (listRecentlyDeleted from Plan 05), src/db/memories-dao.ts (restoreMemory, purgeMemoryPermanently)
    - src/services/memory-trash-sweep.ts (MEMORY_TRASH_WINDOW_DAYS — the copy must match it)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (E2 empty copy; "Delete permanently?" destructive confirm — the ONE danger action; Restore primary=accent)
  </read_first>
  <action>
    Create src/screens/RecentlyDeletedScreen.tsx (route param contactId). Load listRecentlyDeleted in a useFocusEffect cancelled-flag async guard (offline). Each row shows the memory + its deleted timestamp; Restore (primary accent → restoreMemory → reload) and Delete permanently (behind a ConfirmDialog "Delete permanently?" / "This memory can't be recovered." with the destructive Button role → danger fill + onDanger + warning glyph → purgeMemoryPermanently → reload). Render "Recently Deleted is empty" when the list is empty, with expiry copy referencing MEMORY_TRASH_WINDOW_DAYS (no promised timer). Replace the Plan-06 RecentlyDeleted placeholder in RootNavigator.tsx with this screen. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "listRecentlyDeleted\|restoreMemory\|purgeMemoryPermanently" src/screens/RecentlyDeletedScreen.tsx` shows all three wired.
    - The permanent-delete path is behind a confirmation (a ConfirmDialog/Alert precedes purgeMemoryPermanently) — verified by reading the handler; no direct purge from a row tap.
    - Device UAT (phase gate): restore returns the item to the live surface; permanent delete requires the confirm.
  </acceptance_criteria>
  <done>Recently Deleted lists soft-deleted memories with Restore and a guarded permanent-delete, empty-state copy matching the sweep window.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryHistoryScreen — backlist with Edit + Make current</name>
  <files>src/screens/MemoryHistoryScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/db/current-state-history-read.ts (getCurrentStateValue, getCurrentStateHistory from Plan 04), src/db/current-state-history-dao.ts (editHistoryEntry, promoteToCurrentValue)
    - src/db/memory-registry.ts (field_key display names for the header)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (E3 empty "No earlier entries"; "most recent" caption; backlist Edit + "Make current")
  </read_first>
  <action>
    Create src/screens/MemoryHistoryScreen.tsx (route params contactId, fieldKey). Load the current value + full backlist. Render the current value on top with a "most recent" caption, then the ordered backlist; each historical entry offers Edit (inline/dialog → editHistoryEntry → reload) and "Make current" (→ promoteToCurrentValue → reload). Render "No earlier entries" when there is no prior history, keeping the current value visible above it. The screen owns persistence (calls the Plan-04 DAO then reloads); no inline SQL. Replace the Plan-06 MemoryHistory placeholder in RootNavigator.tsx. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "getCurrentStateHistory\|editHistoryEntry\|promoteToCurrentValue" src/screens/MemoryHistoryScreen.tsx` shows all three wired.
    - Device UAT (phase gate): making a historical value current preserves the prior value as history (visible in the backlist after the promote).
  </acceptance_criteria>
  <done>The history drill-in shows the current value + backlist and supports editing an entry and promoting one to current non-destructively.</done>
</task>

<task type="auto">
  <name>Task 3: RelationshipEditor + wire into the surface</name>
  <files>src/components/RelationshipEditor.tsx, src/screens/ThingsToRememberScreen.tsx</files>
  <read_first>
    - src/components/FuelEditor.tsx (parent-owns-persistence posture; zero-dep picker idiom)
    - src/db/relationships-dao.ts (addRelationship/editRelationship/deleteRelationship from Plan 03), src/db/relationships-read.ts (RelationshipRow)
    - the shared contact picker used elsewhere (SHELL-10 reusable modal picker) for the optional linked-contact selection — reuse, do not build a new picker
    - src/screens/ThingsToRememberScreen.tsx (the Key People group — where the editor mounts)
  </read_first>
  <action>
    Create src/components/RelationshipEditor.tsx as a FuelEditor sibling (no DB access; parent passes rows + onAdd/onEdit/onDelete and calls the Plan-03 DAO then reload). Fields: person_name (required; blank rejected at the commit boundary), optional relation_type, optional linked Orbit contact chosen via the existing reusable contact picker (clearable), optional note, pin. Delete is a soft-delete with the same "Moved to Recently Deleted"-style reversible affordance. Mount the editor in ThingsToRememberScreen's Key People group and wire its persistence there. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - RelationshipEditor imports no `@/db/` DAO writer (parent-owns-persistence): `grep -n "@/db/" src/components/RelationshipEditor.tsx` shows only type-only imports.
    - `grep -n "addRelationship\|editRelationship\|deleteRelationship" src/screens/ThingsToRememberScreen.tsx` shows the parent wiring persistence.
    - Device UAT (phase gate): add a relationship with and without a linked contact; linked name renders; soft-delete restorable.
  </acceptance_criteria>
  <done>Structured relationships are creatable/editable/soft-deletable from the Key People group with an optional Orbit-contact link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RecentlyDeletedScreen → purge writer | Permanent deletion of a soft-deleted item; must stay two-stage + confirmed |
| MemoryHistoryScreen → promote writer | Making a value current must not destroy the prior value |
| RelationshipEditor → parent → DAO | Editor holds no DB access; persistence stays correct |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-08 | Tampering / data loss | permanent delete | medium | mitigate | Permanent delete only from Recently Deleted, behind an explicit confirm; purge writer refuses live rows (Plan 05 guard) |
| T-24.1-04 | Tampering / data loss | promote-to-current | high | mitigate | promoteToCurrentValue is atomic + non-destructive (Plan 04); prior value preserved as history |
| T-24.1-01 | Tampering | relationship writes via editor | medium | mitigate | Editor holds no DB access; parent routes through the ?-bound, both-keys-scoped Plan-03 DAO |
</threat_model>

<verification>
- tsc --noEmit + `npm run check:colors` clean across the three surfaces.
- DAO-wiring + no-DAO-in-editor + confirmed-permanent-delete greps pass.
- Device UAT at the phase gate on the Pixel (restore, permanent-delete confirm, make-current non-destructive, relationship link).
</verification>

<success_criteria>
Every 24.1 requirement has a reachable surface: Recently Deleted restore + guarded permanent delete, history backlist with edit + non-destructive make-current, and structured-relationship editing with an optional Orbit-contact link.
</success_criteria>

## Artifacts this plan produces

- **Screens:** src/screens/RecentlyDeletedScreen.tsx, src/screens/MemoryHistoryScreen.tsx (both realize Plan 06 route placeholders).
- **Component:** src/components/RelationshipEditor.tsx (mounted in ThingsToRememberScreen's Key People group).
- **No new DAOs/migrations/sweeps** — UI over Plans 03/04/05.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-07-SUMMARY.md` when done.
</output>
