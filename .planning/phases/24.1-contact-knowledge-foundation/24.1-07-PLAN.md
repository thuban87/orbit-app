---
phase: 24.1-contact-knowledge-foundation
plan: 07
type: execute
wave: 4
depends_on: ["24.1-05", "24.1-06", "24.1-04", "24.1-03"]
files_modified:
  - src/screens/RecentlyDeletedScreen.tsx
  - src/screens/MemoryHistoryScreen.tsx
  - src/components/RelationshipEditor.tsx
  - src/components/ContactPicker.tsx
  - src/screens/ThingsToRememberScreen.tsx
  - src/screens/placeholders/KnowledgePlaceholders.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
autonomous: true
requirements: [KNOW-03, KNOW-04, KNOW-05, KNOW-07]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- KNOW-07: Recently Deleted surface ---
    - "The Recently Deleted screen lists a contact's soft-deleted memories (listRecentlyDeleted), offers Restore (restoreMemory → returns to the live surface) as the primary action, and Delete permanently behind an explicit 'Delete permanently?' confirmation — the ONE danger-styled action in the phase (KNOW-07)."
    - "UI: E2 empty — with no soft-deleted items the screen renders 'Recently Deleted is empty'; the surface still exists (owned by this phase)."
    - "The empty-state expiry copy matches the MEMORY_TRASH_WINDOW_DAYS retention window the sweep enforces — no promised timer (D-11)."
    # --- KNOW-03/04: history drill-in backlist ---
    - "The MemoryHistory screen shows a history-aware field's current value plus the full backlist (getCurrentStateHistory), lets the user Set new value (setCurrentStateValue — demotes the prior current to history), Edit a historical entry, and Make current (promote-to-current) an older entry (KNOW-03/04; review H1)."
    - "UI: E3 empty — a current-only field with no prior values renders 'No earlier entries' with the current value still shown above it; a field with NO value at all renders a 'Not set yet' state with 'Set new value' as the primary action, so a first value can be entered here too (review H1)."
    - "The screen header is the field's registry display name (CURRENT_STATE_FIELD_REGISTRY[fieldKey].displayName) — never a per-screen string (review M1)."
    - "Promoting a historical value to current via the screen preserves the prior current value as history (it calls promoteToCurrentValue, which is atomic and non-destructive) — the prior value is never lost."
    # --- KNOW-05: relationship editor ---
    - "The relationship editor creates/edits a structured relationship (person_name required, optional relation_type, optional linked Orbit contact via the shared contact picker with the owning contact excluded, optional note, pin, Hide/Show on Profile) and soft-deletes it; it holds no DB access (parent owns persistence + reload)."
    - "Deleting a relationship is reversible from the surface: the parent shows the shell snackbar with an Undo action wired to restoreRelationship (onRestore) — relationships are Undo-only (not listed in Recently Deleted; dossier §I decides that surface for Memories) and are bounded by the Plan 05 sweep (review M3)."
  artifacts:
    - "src/screens/RecentlyDeletedScreen.tsx — restore + permanent-delete surface (realizes the Plan 06 placeholder)"
    - "src/screens/MemoryHistoryScreen.tsx — history-aware field backlist with Set new value + Edit + Make current (realizes the Plan 06 placeholder)"
    - "src/components/RelationshipEditor.tsx — create/edit/soft-delete(+Undo) a relationship with optional contact link and visibility toggle"
    - "src/components/ContactPicker.tsx — optional additive excludeContactId prop (review L3)"
  key_links:
    - "RecentlyDeletedScreen / MemoryHistoryScreen / RelationshipEditor are wired to the Plan 05/04/03 DAOs via the parent screen's persistence calls, never inline SQL"
    - "The RecentlyDeleted + MemoryHistory placeholder Stack.Screens in BOTH tabs/DashboardStack.tsx and tabs/OrreryStack.tsx are replaced by the real screens (route types unchanged from Plan 06; review H3) and src/screens/placeholders/KnowledgePlaceholders.tsx is deleted"
  prohibitions:
    - "Permanent delete must never be reachable in one tap from the live surface — it lives only behind the Recently Deleted screen's explicit confirmation (two-stage soft-then-permanent)."
    - "Making a historical value current must never destroy the prior current value — it becomes history (KNOW-04)."
  flagged_assumptions:
    - "Relationship restore is Undo-only (review M3): the dossier §I decides Recently-Deleted restore for Memories and is silent on relationships, so this plan does not list relationships in the Recently Deleted screen. Whether relationships should also appear there is a product-surface decision for the owner — surfaced here, not decided; the DAO (restoreRelationship) already supports it if the owner wants it added later."
---

<objective>
Realize the Recently Deleted / Trash screen, the history-aware drill-in backlist (edit + make-current), and the relationship editor — completing the phase's user-facing surfaces over the Plan 03/04/05 data layers and replacing the Plan 06 route placeholders (KNOW-03/04/05/07).

Purpose: Deliver the restore path, the history editing/promotion UX, and structured-relationship editing so every 24.1 requirement has a reachable surface.

Output: RecentlyDeletedScreen, MemoryHistoryScreen, RelationshipEditor, wired into the routes Plan 06 registered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-03-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-04-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-05-SUMMARY.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-06-SUMMARY.md
@src/components/FuelEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecentlyDeletedScreen — restore + guarded permanent delete</name>
  <files>src/screens/RecentlyDeletedScreen.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx</files>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx (chrome + list posture for a low-traffic recovery surface; restore-only-here idiom)
    - src/db/memories-read.ts (listRecentlyDeleted from Plan 05), src/db/memories-dao.ts (restoreMemory, purgeMemoryPermanently)
    - src/services/memory-trash-sweep.ts (MEMORY_TRASH_WINDOW_DAYS — the copy must match it)
    - src/navigation/tabs/DashboardStack.tsx + src/navigation/tabs/OrreryStack.tsx (the Plan 06 placeholder Stack.Screen entries for RecentlyDeleted — one in EACH file; review H3)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (E2 empty copy; "Delete permanently?" destructive confirm — the ONE danger action; Restore primary=accent)
  </read_first>
  <action>
    Create src/screens/RecentlyDeletedScreen.tsx (route param contactId). Load listRecentlyDeleted in a useFocusEffect cancelled-flag async guard (offline). Each row shows the memory + its deleted timestamp; Restore (primary accent → restoreMemory → reload) and Delete permanently (behind a ConfirmDialog "Delete permanently?" / "This memory can't be recovered." with the destructive Button role → danger fill + onDanger + warning glyph → purgeMemoryPermanently → reload). Render "Recently Deleted is empty" when the list is empty, with expiry copy referencing MEMORY_TRASH_WINDOW_DAYS (no promised timer). Replace the Plan-06 RecentlyDeletedPlaceholderScreen component in the `name="RecentlyDeleted"` Stack.Screen of BOTH src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx with this screen (review H3); do not touch RootNavigator.tsx. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "listRecentlyDeleted\|restoreMemory\|purgeMemoryPermanently" src/screens/RecentlyDeletedScreen.tsx` shows all three wired.
    - `grep -n "component={RecentlyDeletedScreen}" src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx` matches once in EACH file, and `grep -n "RecentlyDeletedPlaceholderScreen" src/navigation/tabs/*.tsx` returns nothing (review H3).
    - The permanent-delete path is behind a confirmation (a ConfirmDialog/Alert precedes purgeMemoryPermanently) — verified by reading the handler; no direct purge from a row tap.
    - Device UAT (phase gate): restore returns the item to the live surface; permanent delete requires the confirm; reachable from both a Dashboard-origin and an Orrery-origin profile.
  </acceptance_criteria>
  <done>Recently Deleted lists soft-deleted memories with Restore and a guarded permanent-delete, empty-state copy matching the sweep window.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryHistoryScreen — backlist with Set new value + Edit + Make current</name>
  <files>src/screens/MemoryHistoryScreen.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx</files>
  <read_first>
    - src/db/current-state-history-read.ts (getCurrentStateValue, getCurrentStateHistory from Plan 04), src/db/current-state-history-dao.ts (setCurrentStateValue, editHistoryEntry, promoteToCurrentValue — all three are wired here; review H1)
    - src/db/memory-registry.ts (CURRENT_STATE_FIELD_REGISTRY[fieldKey].displayName for the header; CurrentStateFieldKey is the route param type — review M1)
    - src/components/FuelEditor.tsx (single-transient-draft + commit-boundary idiom for the "Set new value" input)
    - src/navigation/tabs/DashboardStack.tsx + src/navigation/tabs/OrreryStack.tsx (the Plan 06 placeholder Stack.Screen entries for MemoryHistory — one in EACH file; review H3)
    - .planning/phases/24.1-contact-knowledge-foundation/24.1-UI-SPEC.md (E3 empty "No earlier entries"; "most recent" caption; backlist Edit + "Make current")
  </read_first>
  <action>
    Create src/screens/MemoryHistoryScreen.tsx (route params contactId, fieldKey: CurrentStateFieldKey). Header title = CURRENT_STATE_FIELD_REGISTRY[fieldKey].displayName (review M1). Load the current value + full backlist. Render the current value on top with a "most recent" caption, then the ordered backlist; each historical entry offers Edit (inline/dialog → editHistoryEntry → reload) and "Make current" (→ promoteToCurrentValue → reload). Add a "Set new value" primary action (accent Button; the one primary per surface) opening a single-transient-draft input (blank rejected at the commit boundary) whose commit calls setCurrentStateValue(exec, { contactId, fieldKey, value, now: localDateTime() }) then reload — the prior current becomes the newest backlist entry (review H1). Render "No earlier entries" when there is no prior history, keeping the current value visible above it; when there is NO current value at all render a "Not set yet" state with "Set new value" as the only action. The screen owns persistence (calls the Plan-04 DAO then reloads); no inline SQL. Replace the Plan-06 MemoryHistoryPlaceholderScreen component in the `name="MemoryHistory"` Stack.Screen of BOTH src/navigation/tabs/DashboardStack.tsx and src/navigation/tabs/OrreryStack.tsx (review H3); do not touch RootNavigator.tsx. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - `grep -n "getCurrentStateHistory\|setCurrentStateValue\|editHistoryEntry\|promoteToCurrentValue" src/screens/MemoryHistoryScreen.tsx` shows all four wired (review H1).
    - `grep -n "CURRENT_STATE_FIELD_REGISTRY" src/screens/MemoryHistoryScreen.tsx` shows the header derived from the registry (review M1).
    - `grep -n "component={MemoryHistoryScreen}" src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx` matches once in EACH file, and `grep -n "MemoryHistoryPlaceholderScreen" src/navigation/tabs/*.tsx` returns nothing (review H3).
    - Device UAT (phase gate): entering a new value from this screen makes it current and pushes the prior value into the backlist; making a historical value current preserves the prior value as history (visible in the backlist after the promote); the "Not set yet" state offers Set new value.
  </acceptance_criteria>
  <done>The history drill-in shows the current value + backlist and supports setting a new current value, editing an entry, and promoting one to current non-destructively.</done>
</task>

<task type="auto">
  <name>Task 3: RelationshipEditor (+ Undo, visibility, self-link exclusion) + wire into the surface; delete the placeholders</name>
  <files>src/components/RelationshipEditor.tsx, src/components/ContactPicker.tsx, src/screens/ThingsToRememberScreen.tsx, src/screens/placeholders/KnowledgePlaceholders.tsx</files>
  <read_first>
    - src/components/FuelEditor.tsx (parent-owns-persistence posture; zero-dep picker idiom)
    - src/db/relationships-dao.ts (addRelationship/editRelationship/deleteRelationship/restoreRelationship + assertNotSelfLink from Plan 03), src/db/relationships-read.ts (RelationshipRow, resolveRelationshipVisibility)
    - src/components/ContactPicker.tsx IN FULL (the SHELL-10 shared modal picker: ContactPickerProps { visible, onDismiss, onSelect } at lines 22-26; rows filtered by filterPicker(rows, term) at line 80) and src/logic/contact-picker-order.ts:71-76 (filterPicker) — reuse, do not build a new picker
    - src/stores/snackbar-store.ts (snackbarStore.show({ kind, label, action: { label, accessibilityLabel, onPress } }) — the Undo affordance)
    - src/screens/ThingsToRememberScreen.tsx (the Key People group — where the editor mounts; the Undo snackbar pattern Plan 06 used for Memories)
    - src/navigation/tabs/DashboardStack.tsx + src/navigation/tabs/OrreryStack.tsx (confirm no remaining import of KnowledgePlaceholders after Tasks 1-2)
  </read_first>
  <action>
    Add an optional, additive `excludeContactId?: number` prop to src/components/ContactPicker.tsx (review L3): when set, filter that id out of the rows before filterPicker; undefined leaves every existing caller's behaviour unchanged. Create src/components/RelationshipEditor.tsx as a FuelEditor sibling (no DB access; parent passes rows + onAdd/onEdit/onDelete/onRestore and calls the Plan-03 DAO then reload). Fields: person_name (required; blank rejected at the commit boundary), optional relation_type, optional linked Orbit contact chosen via ContactPicker with excludeContactId={contactId} (clearable; the DAO's assertNotSelfLink remains the authoritative guard), optional note, pin, and a "Hide from Profile" / "Show on Profile" visibility override with the same presentation-only caption as the Memory editor (review L5; writes hidden 1 / 0 / null through onEdit). Delete calls the parent's onDelete (soft); the parent then shows the shell snackbar "Moved to Recently Deleted"-style confirmation with an Undo action wired to onRestore → restoreRelationship → reload (review M3 — relationships are Undo-only; see flagged_assumptions). Mount the editor in ThingsToRememberScreen's Key People group and wire add/edit/delete/restore persistence there. Delete src/screens/placeholders/KnowledgePlaceholders.tsx now that Tasks 1-2 replaced both registrations. All colours via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit + `npm run check:colors` clean.
    - RelationshipEditor imports no `@/db/` DAO writer (parent-owns-persistence): `grep -n "@/db/" src/components/RelationshipEditor.tsx` shows only type-only imports.
    - `grep -n "addRelationship\|editRelationship\|deleteRelationship\|restoreRelationship" src/screens/ThingsToRememberScreen.tsx` shows the parent wiring all four (review M3).
    - `grep -n "excludeContactId" src/components/ContactPicker.tsx src/components/RelationshipEditor.tsx` shows the prop declared in the picker and passed by the editor (review L3).
    - `grep -n "Hide from Profile\|Show on Profile" src/components/RelationshipEditor.tsx` shows the visibility toggle (review L5).
    - `ls src/screens/placeholders/KnowledgePlaceholders.tsx` fails (file deleted) and `grep -rn "KnowledgePlaceholders" src/` returns nothing.
    - Device UAT (phase gate): add a relationship with and without a linked contact; the owning contact does not appear in the picker; linked name renders; delete → Undo restores it; Hide from Profile removes it from the Key People group without deleting it.
  </acceptance_criteria>
  <done>Structured relationships are creatable/editable/soft-deletable (with Undo) from the Key People group with an optional Orbit-contact link that cannot be the owner, a visibility override, and no placeholder screens left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RecentlyDeletedScreen → purge writer | Permanent deletion of a soft-deleted item; must stay two-stage + confirmed |
| MemoryHistoryScreen → promote writer | Making a value current must not destroy the prior value |
| RelationshipEditor → parent → DAO | Editor holds no DB access; persistence stays correct |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-08 | Tampering / data loss | permanent delete | medium | mitigate | Permanent delete only from Recently Deleted, behind an explicit confirm; purge writer refuses live rows (Plan 05 guard) |
| T-24.1-04 | Tampering / data loss | set-new-current + promote-to-current | high | mitigate | setCurrentStateValue and promoteToCurrentValue are atomic + non-destructive (Plan 04); prior value preserved as history (review H1) |
| T-24.1-01 | Tampering | relationship writes via editor | medium | mitigate | Editor holds no DB access; parent routes through the ?-bound, both-keys-scoped Plan-03 DAO |
| T-24.1-14 | Tampering / integrity | relationship self-link via the picker | low | mitigate | ContactPicker excludeContactId hides the owner; Plan 03's assertNotSelfLink is the authoritative commit-boundary guard (review L3) |
</threat_model>

<verification>
- tsc --noEmit + `npm run check:colors` clean across the three surfaces + ContactPicker.
- DAO-wiring (incl. setCurrentStateValue + restoreRelationship) + no-DAO-in-editor + confirmed-permanent-delete + both-stacks-registration + placeholder-deleted greps pass.
- Device UAT at the phase gate on the Pixel (restore, permanent-delete confirm, set-new-value + make-current non-destructive, relationship link/Undo/visibility, both profile origins).
</verification>

<success_criteria>
Every 24.1 requirement has a reachable surface from either profile origin: Recently Deleted restore + guarded permanent delete, history backlist with set-new-value + edit + non-destructive make-current, and structured-relationship editing with an optional (non-self) Orbit-contact link, Undo on delete, and a visibility override.
</success_criteria>

## Artifacts this plan produces

- **Screens:** src/screens/RecentlyDeletedScreen.tsx, src/screens/MemoryHistoryScreen.tsx (both replace the Plan 06 placeholder registrations in tabs/DashboardStack.tsx + tabs/OrreryStack.tsx).
- **Component:** src/components/RelationshipEditor.tsx (mounted in ThingsToRememberScreen's Key People group; onAdd/onEdit/onDelete/onRestore).
- **Shared component change:** src/components/ContactPicker.tsx optional `excludeContactId` prop (additive).
- **Deleted:** src/screens/placeholders/KnowledgePlaceholders.tsx.
- **No new DAOs/migrations/sweeps** — UI over Plans 03/04/05.

## Review dispositions (cycle 1)

- **H1** — INCORPORATED: Task 2 "Set new value" → `setCurrentStateValue` on `MemoryHistoryScreen` (+ "Not set yet" state); grep + UAT gated. Plan 06 Task 2 adds the inline surface affordance.
- **H3** — INCORPORATED: Tasks 1-2 replace the placeholder components in BOTH `tabs/DashboardStack.tsx` and `tabs/OrreryStack.tsx`; `RootNavigator.tsx` removed from `files_modified`; Task 3 deletes the placeholder file.
- **M1** — INCORPORATED (consumer): header from `CURRENT_STATE_FIELD_REGISTRY`; route param typed `CurrentStateFieldKey`.
- **M3** — INCORPORATED (Undo-only): Task 3 `onRestore` → `restoreRelationship` via the shell snackbar Undo; Recently Deleted stays Memories-only per dossier §I — recorded in `flagged_assumptions` for the owner.
- **L3** — INCORPORATED: Task 3 `ContactPicker.excludeContactId` (additive) + editor passes the owner id; T-24.1-14 added.
- **L5** — INCORPORATED (consumer): Task 3 Hide/Show on Profile toggle in `RelationshipEditor`.
- Full consolidated table: `24.1-01-PLAN.md` § Review dispositions (cycle 1).

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-07-SUMMARY.md` when done.
</output>
