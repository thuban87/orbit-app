---
phase: 24.1
reviewers: [codex, claude]
reviewed_at: 2026-09-03T22:05:00-05:00
convergence_cycle: 2
plans_reviewed: [24.1-01-PLAN.md, 24.1-02-PLAN.md, 24.1-03-PLAN.md, 24.1-04-PLAN.md, 24.1-05-PLAN.md, 24.1-06-PLAN.md, 24.1-07-PLAN.md]
models:
  codex: "gpt-5.6-terra (reasoning=low)"
  claude: "sonnet (reasoning=low)"
model_sources:
  codex: "banner"
  claude: "pinned"
lane_mechanics:
  codex: "built-in lane; prompt on stdin; no hook-trust bypass flag; ~107 s wall; ok=true stubbed=false; 6059-byte review"
  claude: "built-in lane (owner-authorized; SELF_CLI forced to none so the lane was NOT self-skipped); ~60 s wall; ok=true stubbed=false; 12275-byte review — the read-only fallback was NOT needed"
  aggregator: "Opus 4.8 (this session) — every lane finding re-verified against the code on disk before acceptance; the codex-vs-claude divergence on the trash-sweep TOCTOU was adjudicated by reading field-ddl.ts:238-243, not by vote"
source_grounding:
  enabled: true
  authority: grep
prior_cycle: "cycle 1 preserved in git at commit f5fc43c; this file is overwritten each cycle"
---

# Cross-AI Plan Review — Phase 24.1 (Contact Knowledge Foundation — Model, Storage & UI) — Cycle 2

Both lanes received the same ~287 KB source-grounding prompt (PROJECT.md first 80 lines, ROADMAP §24.1, KNOW-01..09, 24.1-CONTEXT.md, 24.1-RESEARCH.md, REQUIREMENTS.md, all seven PLAN.md files, and an explicit cycle-2 addendum listing the 15 cycle-1 finding IDs and instructing each reviewer to verify the claimed dispositions against executable plan content before re-raising anything). Each lane ran once, produced real source-cited output (`ok=true`, `stubbed=false`), and neither timed out. Each lane's review is reproduced verbatim below, followed by the aggregator's verification against the code on disk, the consensus, and the cycle summary.

**Both lanes independently reached the same top-line verdict: all 15 cycle-1 findings (H1–H3, M1–M6, M8, L1–L5) are adequately incorporated into executable plan content — no cycle-1 regressions to re-raise.** The aggregator confirmed this against the code (see the disposition-verification table below). New this cycle: one HIGH (a trash-sweep TOCTOU gap raised by codex, disputed by claude, adjudicated HERE against the field-sweep precedent) and a small set of MEDIUM/LOW polish items.

---

## Codex Review

## Summary

The cycle-2 plans are substantially stronger: all cycle-1 findings have executable dispositions, the migration is genuinely additive against the current v15 head, and the merge/purge/routing work traces real code seams. One high-risk Trash-sweep TOCTOU gap remains.

## Cycle-1 disposition verification

All 15 cycle-1 findings are adequately incorporated in executable plan content. In particular:

- H2: Plan 03 reparents new FK children before the existing contact delete at [src/db/merge-dao.ts:153](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:153) / [src/db/merge-dao.ts:185](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:185).
- H3: both-stack registration is necessary because the actual Profile route exists in both [DashboardStack](/home/bwales/projects/orbit-app/src/navigation/tabs/DashboardStack.tsx:43) and [OrreryStack](/home/bwales/projects/orbit-app/src/navigation/tabs/OrreryStack.tsx:18).
- M2’s custom-field load composition matches the real API: `getValuesForContact` requires a supplied definitions list at [src/db/field-values-dao.ts:20](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:20), and `visibleDefsForProfile` takes both defs and values at [src/db/field-values-dao.ts:126](/home/bwales/projects/orbit-app/src/db/field-values-dao.ts:126).
- M3, M5, and the tombstone/restore gaps are explicitly deferred with rationale rather than silently ignored.

## Concerns

- [HIGH] NEW — Plan 05’s proposed Trash sweep is not actually TOCTOU-safe for a row that is restored and then soft-deleted again between candidate scan and deletion. Its proposed authoritative guard is only `deleted_at IS NOT NULL`; that protects a simple restore, but not a fresh replacement `deleted_at` timestamp. The existing field sweep explicitly requires the stale predicate to be rechecked under the write lock, not merely an existence/state predicate ([src/services/field-sweep.ts:25](/home/bwales/projects/orbit-app/src/services/field-sweep.ts:25), [src/services/field-sweep.ts:105](/home/bwales/projects/orbit-app/src/services/field-sweep.ts:105)).  
  Change Plan 05 Task 2: add mutex-owning `expireMemoryIfStale` / `expireRelationshipIfStale` writers (or guarded cores) that re-read and require `deleted_at < datetime('now','localtime', ?)` inside their write transaction before deleting. The sweep should call those, while `purgeMemoryPermanently` remains the user-confirmed Trash action that may delete any already-soft-deleted row. Add a restore-then-redelete race test with a newly stamped `deleted_at`.

- [MEDIUM] NEW — KNOW-09’s “provenance shown mainly in detail/edit views” lacks a planned rendering path. The schema and DAO carry provenance, but Plan 06 Task 1’s listed editor fields omit provenance, while `MemoryCard` deliberately omits it. The analogous current Fuel UI does surface source-specific behavior in its editor component ([src/components/FuelEditor.tsx:39](/home/bwales/projects/orbit-app/src/components/FuelEditor.tsx:39)).  
  Change Plan 06 Task 1: require `MemoryEditor` to show a read-only provenance row/caption for existing items, sourced from `MemoryRow.provenance`, with acceptance criteria and UAT coverage. It need not be editable.

## Strengths

- Migration sequencing is grounded in reality: the current head is 15 and registration ends at migration015 ([src/db/database.ts:50](/home/bwales/projects/orbit-app/src/db/database.ts:50), [src/db/database.ts:53](/home/bwales/projects/orbit-app/src/db/database.ts:53)). Plan 01’s staged-upgrade test avoids spoofing `user_version`, correctly matching runner selection behavior ([src/db/migrations/runner.ts:38](/home/bwales/projects/orbit-app/src/db/migrations/runner.ts:38)).
- Plan 03 correctly treats merge as an explicit lifecycle operation. Current code only reparents a closed table list before deleting the absorbed contact ([src/db/merge-dao.ts:153](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:153)), so adding all three knowledge tables before [the delete](/home/bwales/projects/orbit-app/src/db/merge-dao.ts:185) is essential and properly planned.
- Plan 03 also respects purge architecture: current purge deliberately uses explicit fan-out rather than trusting FK cascades ([src/db/purge-dao.ts:242](/home/bwales/projects/orbit-app/src/db/purge-dao.ts:242)).
- The plans consistently honor the non-reentrant transaction model. The repository explicitly forbids nested `inWriteTransaction` calls ([src/db/transaction.ts:12](/home/bwales/projects/orbit-app/src/db/transaction.ts:12)).
- Plan 06’s custom-field composition is compatible with current Profile behavior, which loads definitions then values ([src/screens/ContactProfileScreen.tsx:247](/home/bwales/projects/orbit-app/src/screens/ContactProfileScreen.tsx:247), [src/screens/ContactProfileScreen.tsx:265](/home/bwales/projects/orbit-app/src/screens/ContactProfileScreen.tsx:265)).
- The plans correctly avoid touching backup/tombstone registries in 24.1, which are currently closed allowlists ([src/backup/backup-schema.ts:159](/home/bwales/projects/orbit-app/src/backup/backup-schema.ts:159)); the explicit 24.2 deferral is necessary.

## Suggestions

- Add an explicit error state and retry affordance to the Plan 06/07 `useFocusEffect` loaders; current Profile loading is guarded by `try/catch` ([src/screens/ContactProfileScreen.tsx:236](/home/bwales/projects/orbit-app/src/screens/ContactProfileScreen.tsx:236)).
- Add contact-scoped indexes for the three new tables, particularly `(contact_id, deleted_at)` for Trash and `(contact_id, field_key, is_current)` for history reads. This is not a correctness blocker, but avoids repeatedly scanning an expanding local knowledge store.

## Risk Assessment

**MEDIUM.** The plans now cover migration ordering, two-stack reachability, merge/purge cascades, registry enforcement, and most deletion lifecycle behavior well. The high-severity sweep race must be corrected before implementation; without it, a freshly re-deleted item can be permanently removed by an obsolete sweep candidate.

---

## Claude Review

# Cross-AI Plan Review — Convergence Cycle 2

## Summary

This is a well-engineered plan set that visibly incorporated the cycle-1 findings with specific, verifiable mechanisms (grep gates, explicit test cases, dependency wiring). The core vertical slice (migration 016 → registry → DAO → read → UI) is sound and follows established repo patterns faithfully. The merge/purge coverage for the three new knowledge tables (H2/M5) is the standout fix — it correctly identifies and closes a real cascade-delete data-loss hole. However, several cycle-1 dispositions are only partially load-bearing when checked against actual plan mechanics, and a few new cross-plan wiring gaps emerged from the added complexity (Plan 05's dependency on Plan 03, the RecentlyDeleted-is-Memories-only vs. sweep-also-purges-relationships asymmetry, and a subtle TOCTOU gap in the trash sweep's relationship handling). Overall the plans are executable and converging; remaining issues are medium/low severity polish rather than structural failures.

## Cycle-1 disposition verification

**H1 (inline "Set new value" for history-aware fields)** — ADEQUATELY INCORPORATED. Plan 06 Task 2 wires `setCurrentStateValue` into the surface with an explicit must-have truth and grep gate (`grep -n "setCurrentStateValue" ...ThingsToRememberScreen.tsx`), and Plan 07 Task 2 adds the same on `MemoryHistoryScreen`. Both reference the atomic demote-then-insert from Plan 04 Task 1. Verified sound.

**H2 (merge cascade-deletes knowledge)** — ADEQUATELY INCORPORATED. Plan 03 Task 3 is thorough: it reads `merge-dao.ts` in full first, correctly identifies the ordering hazard (reparent must run before `DELETE FROM contacts WHERE id = ?` at line 185), demotes colliding current-state entries, re-points `linked_contact_id`, and avoids manufacturing self-links. Test list covers the collision case. This is the strongest fix in the set.

**H3 (route only in DashboardStack, not OrreryStack)** — ADEQUATELY INCORPORATED. Plan 01 Task 3, Plan 06 Task 3, Plan 07 Tasks 1-2 all declare routes in both `DashboardStackParamList`/`OrreryStackParamList` and mount Stack.Screens in both stack files, with `git diff --stat -- RootNavigator.tsx` empty-check as an acceptance criterion. Solid, mechanical, verifiable.

**M1 (current-state field-key contract undefined until wave 2)** — ADEQUATELY INCORPORATED. Plan 01 Task 2 now defines `CurrentStateFieldKey`, `CURRENT_STATE_FIELD_KEYS`, `CURRENT_STATE_FIELD_REGISTRY`, `isCurrentStateFieldKey` in wave 1, consumed by Plan 04 (validation) and Plans 06/07 (headers).

**M2 (custom-field load contract undocumented)** — ADEQUATELY INCORPORATED. Plan 06 Task 2 specifies the exact composition `listDefs → getValuesForContact(exec, contactId, defs) → visibleDefsForProfile`, matching `ContactProfileScreen.tsx:236-265`, with a grep gate.

**M3 (relationship soft-delete restore path undecided)** — ADEQUATELY INCORPORATED, with an honest scope decision. Relationships are Undo-only (not in Recently Deleted), bounded by Plan 05's sweep now also purging soft-deleted relationships (Plan 05 depends_on gained `24.1-03`). This is flagged as an owner judgment call in `flagged_assumptions`, which is appropriate — it's disclosed, not silently resolved.

**M4 (migration test doesn't prove staged-upgrade)** — ADEQUATELY INCORPORATED. Plan 01 Task 1 specifies three staged-upgrade cases (N=1, 8, 15) using genuine `runMigrations` calls rather than spoofed `user_version`, with a grep gate forbidding literal version writes.

**M5 (purge doesn't fan out to new tables)** — ADEQUATELY INCORPORATED. Plan 03 Task 3 adds `KNOWLEDGE_PURGE_CHILDREN`, extends `PurgeImpact` and `impactSummaryLines`. Correctly scoped as non-tombstoned (deferred to 24.2) with rationale.

**M6 (unregistered Memory type not rejected at write boundary)** — ADEQUATELY INCORPORATED. `assertRegisteredMemoryType` in Plan 01 Task 2, reused by Plan 02's `editMemory`, plus `resolveVisibility`'s fail-visible fallback for unknown types (Plan 02 Task 3).

**M8 (first-class fields never rendered)** — ADEQUATELY INCORPORATED. Plan 04 Task 3 adds `getFirstClassFields`; Plan 06 Task 2 renders it read-only. Gravity/Closeness omission is honestly flagged rather than fabricated.

**L1–L5** — All show concrete incorporation (Off Limits removed from MemoryCard; sweep clock semantics reworded; self-link guard `assertNotSelfLink`; `hidden` defensive resolution; `RELATIONSHIPS_GROUP.visibilityDefault` consumed by relationships-read/UI). No gaps found on inspection.

**Verdict: all 15 cycle-1 findings show adequate, verifiable incorporation into executable plan content.** No regressions to re-raise as cycle-1 items. Below are genuinely new cycle-2 observations.

## Concerns

- **[MEDIUM] NEW — Plan 05's relationship-sweep candidate scan has no under-lock re-check distinct from the memory path, but the guard it relies on (`purgeRelationshipPermanently`) lives in Plan 03, and Plan 05's `depends_on` was updated to include `24.1-03` — confirm this is consistent.** `.planning/.../24.1-05-PLAN.md` frontmatter: `depends_on: ["24.1-02", "24.1-03"]`. This is correctly wired. However Plan 05 Task 2's behavior block says the sweep scans relationships "with the identical predicate" but the relationships table's `deleted_at` semantics were only just added in Plan 03 Task 1 — worth double-checking Plan 03 Task 1's `deleteRelationshipCore` sets `deleted_at` via the same `datetime('now','localtime', ...)`-comparable format as memories (both use `localDateTime()`/caller-supplied `now`, so this should be consistent, but neither plan explicitly cross-checks the column's format compatibility with the sweep's `datetime('now','localtime', ?)` comparison). Low-risk given both go through the same `localDateTime()` convention repo-wide, but the plan text never states this compatibility explicitly. **Fix:** Plan 05 Task 2 should add one line confirming `relationships.deleted_at` and `memories.deleted_at` share the same timestamp format the sweep's `datetime()` comparison expects (both do, per migration 016's column defs — this is just a documentation/verification gap, not a functional one).

- **[LOW] NEW — RecentlyDeletedScreen (Plan 07 Task 1) surfaces only memories, but the trash sweep (Plan 05) also permanently deletes relationships past the window with no UI to see or intervene on that pending expiry.** Since relationships are Undo-only per M3's resolution, a user who deletes a relationship, dismisses the Undo snackbar (or the app is killed before they see it), has no way to know the relationship exists in a "soft-deleted, about to expire" state, nor to recover it — the sweep will silently purge it after `MEMORY_TRASH_WINDOW_DAYS`. This is disclosed in Plan 03/07 `flagged_assumptions` as an owner decision point, which is the right way to handle it, but it's worth flagging clearly to the owner as a real product gap (a relationship deleted and un-Undo'd is unrecoverable after the window with zero UI surfacing) rather than treating it as fully resolved. **Recommendation:** no plan change required if the owner accepts the Undo-only posture, but this should be called out explicitly at the phase gate, not just buried in a flagged_assumptions block.

- **[LOW] NEW — Plan 04's `getCurrentStateValues` batched read silently drops rows whose `field_key` fails `isCurrentStateFieldKey`** (per the behavior spec: "ignoring any row whose field_key fails isCurrentStateFieldKey"). This is defensible (fail-safe, mirrors M6's resolveVisibility posture) but there's no test asserting a corrupt/legacy field_key doesn't silently vanish without any surfaced signal — unlike `resolveVisibility`'s explicit "fail-visible, never fail-hidden" contract, there's no equivalent "never silently lose a row" contract stated for this read. Since current-state history is supposed to retain full history with no data loss (KNOW-03), a row that fails the guard is invisible on the Profile even though it's still in the DB — acceptable defensively, but not explicitly tested for in Plan 04 Task 3's acceptance criteria (only empty/single/multi/batched cases are listed, not the malformed-field_key case). **Fix:** Plan 04 Task 3 should add one test case for a row with an unrecognized field_key value, asserting it's excluded but not deleted.

- **[LOW] NEW — Plan 06 Task 2's "empty state" truth ("every group is empty AND no first-class value is present") could be perpetually false in practice** since `getFirstClassFields` always returns a `categoryName`/`intervalDays` for any contact with a category or cadence set (which is nearly all contacts post-Phase-34's rapid capture), meaning the calm "Nothing to remember yet" empty state may rarely or never render even for contacts with zero Memories/relationships/history. This is a minor UX-vs-spec tension, not a functional bug — flagging for the owner's awareness since the UI-SPEC's E1 empty-state contract implies a genuinely empty surface, and "first-class fields always populated" may make that contract nearly unreachable in real use. Not blocking.

- **[LOW] cycle-1 residual, downgraded — V16 (batched current-state read)** was marked advisory/incorporated via `getCurrentStateValues`, confirmed present in Plan 04 Task 3 and consumed in Plan 06 Task 2. No further action.

## Strengths

- Merge/purge coverage (H2/M5) is executed with real understanding of the existing `merge-dao.ts`/`purge-dao.ts` internals, not a superficial patch — the ordering constraint (reparent before `DELETE FROM contacts`) and the single-current-index collision handling are both correctly reasoned through.
- The registry-first sequencing (Plan 01 Task 2 defining `CURRENT_STATE_FIELD_KEYS`/`RELATIONSHIPS_GROUP` in wave 1) closes what would otherwise be a real cross-wave dependency-ordering defect (M1) — this is the right fix, not a workaround.
- Soft-delete + guarded permanent-delete (two-stage: `deleted_at` UPDATE, then a separate `deleted_at IS NOT NULL`-gated hard DELETE) is consistently applied across memories and relationships, with explicit grep gates proving the physical DELETE statement count and guard clause.
- TOCTOU-safety in the trash sweep (Plan 05 Task 2) is well-specified: bare candidate scan → authoritative re-check under the guarded writer's own predicate, non-nested mutex, SQL-relative test fixtures rather than fake-clock injection (correctly avoiding the field-sweep HIGH-1 pitfall).
- Every plan's `flagged_assumptions` section is used honestly — genuine open questions (default Memory type name, Gravity/Closeness field, relationship Recently-Deleted surfacing) are disclosed rather than silently decided, consistent with the dossier's ESCALATE posture.

## Suggestions

- Plan 04 Task 3: add an explicit test for a `current_state_entries` row with a malformed/legacy `field_key` to confirm `getCurrentStateValues` excludes-but-doesn't-lose it (see LOW finding above).
- Plan 06 Task 2: consider whether the empty-state condition should exclude `getFirstClassFields`'s always-populated columns (category, cadence) from the "nothing to remember" gate, or accept that the empty state is effectively unreachable for most contacts and adjust the UI-SPEC note accordingly at the phase gate.
- At the phase gate, explicitly surface to the owner (not just in flagged_assumptions) that a deleted-and-un-Undo'd relationship has zero recovery UI before the sweep window expires it — this is a real, if minor, product gap worth an explicit yes/no from the owner rather than an implicit default.

## Risk Assessment

**LOW.** All 15 cycle-1 findings are verifiably incorporated with concrete, grep-gated or test-gated mechanisms rather than prose promises. The one HIGH-severity structural risk from cycle 1 (merge cascade-deleting knowledge silently) is now correctly closed with transaction-ordering awareness. Remaining cycle-2 observations are LOW/MEDIUM polish items (a documentation gap on timestamp-format compatibility, an untested defensive-read edge case, and a UX/product disclosure recommendation) — none block execution or represent a data-loss or security defect. The plan set is ready to proceed to execution with the noted suggestions optionally applied.

---

## Aggregator Verification (Opus 4.8, this session)

Every finding below was re-checked against the actual code on disk and the executable content of the plans (not the dispositions table's self-claims). Line references were opened and confirmed.

### Cycle-1 disposition verification (all 15 — independently confirmed against code)

| ID | Sev (c1) | Disposition claim | Aggregator verdict (code checked) |
|----|----------|-------------------|-----------------------------------|
| H1 | HIGH | Plan 06 Task 2 + Plan 07 Task 2 wire `setCurrentStateValue` (inline "Set new value") | **CONFIRMED** — action + grep gate in `24.1-06-PLAN.md:133,143` and `24.1-07-PLAN.md:117,124`; DAO exists in Plan 04 Task 1 (`24.1-04-PLAN.md:93`) |
| H2 | HIGH | Plan 03 Task 3 extends `merge-dao.ts` reparent list + linked_contact_id re-point + single-current collision demote | **CONFIRMED** — real reparent list is at `src/db/merge-dao.ts:153`, `DELETE FROM contacts` at `:185`; Plan 03 Task 3 action/greps/tests target exactly these (`24.1-03-PLAN.md:166,175,176`) |
| H3 | HIGH | Routes in BOTH DashboardStack + OrreryStack; RootNavigator removed from files_modified | **CONFIRMED** — `ContactProfileScreen` is mounted in both `tabs/DashboardStack.tsx:43` and `tabs/OrreryStack.tsx:18`; Plan 01/06/07 mount + register in both, `git diff --stat RootNavigator.tsx` empty-check present |
| M1 | MED | Plan 01 Task 2 defines `CurrentStateFieldKey`/`CURRENT_STATE_FIELD_KEYS`/`CURRENT_STATE_FIELD_REGISTRY`/`isCurrentStateFieldKey` in wave 1 | **CONFIRMED** — `24.1-01-PLAN.md:183,192,203`; consumed by Plan 04 (`:93`) and Plans 06/07 headers |
| M2 | MED | Plan 06 load contract `listDefs → getValuesForContact(exec, contactId, defs) → visibleDefsForProfile` | **CONFIRMED** — real signatures match: `getValuesForContact(exec, contactId, defs)` at `src/db/field-values-dao.ts:20`, `visibleDefsForProfile` at `:126`, `listDefs` at `src/db/field-defs-dao.ts:206`; grep gate at `24.1-06-PLAN.md:142` |
| M3 | MED | Undo-only relationships, bounded by Plan 05 sweep; Recently-Deleted is Memories-only (dossier §I) — flagged for owner | **CONFIRMED as disposition** — Plan 05 sweep purges relationships (`24.1-05-PLAN.md:27`), Plan 07 wires `onRestore` (`24.1-07-PLAN.md:37`); scope decision disclosed in flagged_assumptions (both lanes agree this is honest) |
| M4 | MED | Staged `runMigrations(exec, MIGRATIONS, N, ...)` for N=1,8,15 then 16; spoofed user_version grep-forbidden | **CONFIRMED** — `24.1-01-PLAN.md:140,165`; matches runner selection-by-user_version (`src/db/migrations/runner.ts:38`) |
| M5 | MED | Plan 03 Task 3 adds `KNOWLEDGE_PURGE_CHILDREN` + `computeImpact`/`impactSummaryLines`; restore-apply cascade flagged for 24.2 | **CONFIRMED** — real anchors `purge-dao.ts` PURGE_CHILDREN:76, computeImpact:121, impactSummaryLines:176, fan-out:245, DELETE:261; Plan 03 Task 3 (`24.1-03-PLAN.md:168,177`); restore-apply.ts reset loop flagged (`24.1-03/05` flagged_assumptions) |
| M6 | MED | `assertRegisteredMemoryType` at write boundary + custom-label rule; `resolveVisibility` fail-visible on unknown type | **CONFIRMED** — `24.1-01-PLAN.md:186,194`; `24.1-02-PLAN.md:91,94,151` (editMemory reuse + defensive resolve) |
| M8 | MED | Plan 04 Task 3 `getFirstClassFields`; Plan 06 renders read-only; Gravity/Closeness flagged | **CONFIRMED** — `24.1-04-PLAN.md:153` (real: `getContactHeader` at `src/db/contact-read.ts:67` omits these, so a separate read is justified); Plan 06 renders (`:131`) |
| L1 | LOW | Off Limits marker removed from `MemoryCard` | **CONFIRMED** — `24.1-06-PLAN.md:101` explicit "No Off Limits marker"; grep gate `:111` |
| L2 | LOW | Clock truth reworded; SQL-relative fixtures; injectable `now` = write-stamp only | **CONFIRMED** — `24.1-05-PLAN.md:123` action + JSDoc requirement |
| L3 | LOW | `assertNotSelfLink` rejects `linked_contact_id === contact_id` on add + edit | **CONFIRMED** — `24.1-03-PLAN.md:102`; `ContactPicker` gains optional `excludeContactId` (real props at `src/components/ContactPicker.tsx:22` have none today — additive) |
| L4 | LOW | Out-of-range `hidden` → type default (Plan 02); promote-already-current no-op-safe (Plan 04) | **CONFIRMED** — `24.1-02-PLAN.md:151,157`; `24.1-04-PLAN.md:119,123` |
| L5 | LOW | `RELATIONSHIPS_GROUP.visibilityDefault` consumed by `resolveRelationshipVisibility` + editor toggle | **CONFIRMED** — `24.1-01-PLAN.md:184`; `24.1-03-PLAN.md:132`; column stays in migration 016 |

**Aggregator verdict: all 15 cycle-1 findings are genuinely incorporated as executable plan content (task action / acceptance_criteria / verify grep / must_have / explicit disclosed deferral), and every code anchor the plans cite matches the file on disk. No cycle-1 finding is re-raised.**

### New cycle-2 findings (verified)

| ID | Lane | Sev | Finding | Code check | Verdict |
|----|------|-----|---------|-----------|---------|
| N1 | codex | **HIGH** | Plan 05 trash-sweep is not TOCTOU-safe for restore-then-redelete: its under-lock authoritative guard is existence-only (`deleted_at IS NOT NULL`), so a row re-deleted with a FRESH within-window `deleted_at` between candidate scan and per-item purge is permanently deleted | **VERIFIED — real divergence.** The field-sweep precedent's under-lock writer `expireFieldIfStale` (`src/db/field-ddl.ts:238-243`) re-checks the FULL staleness window under the lock (`quarantined_at IS NOT NULL AND quarantined_at < datetime('now','localtime', ?)`), so a re-quarantined-with-fresh-stamp def SURVIVES. Plan 05's sweep instead calls `purgeMemoryPermanently`/`purgeRelationshipPermanently`, whose guard is existence-only (`24.1-05-PLAN.md:117` behavior; the guarded DELETE is `deleted_at IS NOT NULL`). Plan 05 Task 2's own TOCTOU must-have (`24.1-05-PLAN.md` behavior "restored ... between the scan and the delete is NOT purged") only covers restore→NULL, **not** restore-then-redelete. claude rated this same sweep a *strength* (line "authoritative re-check under the guarded writer's own predicate") — that is correct for the plain-restore case but misses the fresh-stamp edge field-sweep specifically guards. | **HIGH (unresolved).** Practical window is narrow (launch-time synchronous sweep, concurrent user restore+redelete is hard to interleave), but (a) the plan asserts a TOCTOU guarantee it does not deliver, (b) it diverges from the repo's own tested staleness-recheck pattern, and (c) the consequence is irreversible data loss, which this repo treats at max severity. Fix: add mutex-owning `expireMemoryIfStale`/`expireRelationshipIfStale` writers that re-require `deleted_at < datetime('now','localtime', ?)` under the write lock; the sweep calls those, while `purgeMemoryPermanently` stays the user-confirmed Trash "delete now" action (any soft-deleted row, any age). Add a restore-then-redelete race test with a freshly stamped `deleted_at`. |
| N2 | codex | **MEDIUM** | KNOW-09 provenance has no rendering path: the requirement is "provenance shown mainly in detail/edit views," and Plan 06 carries a must-have truth to that effect, but no task ACTION renders it | **VERIFIED.** KNOW-09 (`REQUIREMENTS.md:61`) = "shown mainly in detail/edit views." `24.1-06-PLAN.md:32` asserts the truth, but the `MemoryCard` action (`:101`) says "No provenance on the card" and the `MemoryEditor` action (`:103`) lists fields value/note/link/meaningful-date/pin/outdated/visibility — **provenance absent**. No grep/acceptance covers it. Invisible to execute-phase. | **ACTIONABLE.** Plan 06 Task 1: require `MemoryEditor` to render a read-only provenance row/caption from `MemoryRow.provenance` (need not be editable), with an acceptance criterion + UAT step. |
| N3 | claude | **MEDIUM** | Plan 05 Task 2 never states that `relationships.deleted_at` and `memories.deleted_at` share the timestamp format the sweep's `datetime('now','localtime', ?)` comparison expects | **VERIFIED as a documentation/verification gap** (functionally both go through `localDateTime()` per repo convention, so behavior is correct; the plan text simply does not assert the compatibility). Not yet stated anywhere in the plan. | **ACTIONABLE (minor).** Plan 05 Task 2: add one verification line asserting both columns' `deleted_at` format is comparable to the sweep's `datetime()` predicate (both written via `localDateTime()`). |
| N4 | claude | **LOW** | `getCurrentStateValues` silently drops rows whose `field_key` fails `isCurrentStateFieldKey`, with no test; KNOW-03 requires full history retention, so a guard-failing row is invisible on Profile though still in the DB | **VERIFIED.** Plan 04 Task 3 acceptance lists empty/single/multi/batched cases only — no malformed-field_key case. The defensive drop is reasonable (fail-safe) but untested and un-contracted. | **ACTIONABLE.** Plan 04 Task 3: add a test asserting a row with an unrecognized `field_key` is excluded from the read but NOT deleted. |
| N5 | claude | **LOW** | Plan 06 "Nothing to remember yet" empty state may be practically unreachable, because `getFirstClassFields` almost always returns a `categoryName`/`intervalDays`, and the empty gate requires "no first-class value present" | **VERIFIED as a spec-vs-reality tension** (the gate is implemented exactly as written; the concern is that it will rarely fire). Not a functional bug. | **ACTIONABLE (minor).** Plan 06 Task 2: either exclude the always-populated first-class columns (category, cadence) from the empty-state gate, or record a `flagged_assumption` / UI-SPEC note that the empty state is effectively unreachable for contacts with a category or cadence. |

### Items NOT counted as unresolved-actionable (already disclosed or not a required plan change)

- **[LOW] claude — relationship deleted-and-un-Undo'd has no recovery UI before the sweep window.** Already recorded in Plan 03/07 `flagged_assumptions` as an owner decision (Undo-only posture, dossier §I). claude itself says "no plan change required if the owner accepts the Undo-only posture." This is an **owner-gate disclosure**, already deferred in-plan — surface it explicitly at the phase gate for a yes/no, but it is not an unincorporated finding.
- **[LOW] codex — suggestions:** add an error/retry state to the Plan 06/07 `useFocusEffect` loaders (parity with `ContactProfileScreen.tsx:236` try/catch), and add contact-scoped indexes `(contact_id, deleted_at)` and `(contact_id, field_key, is_current)` on the three new tables. Both are explicitly "not a correctness blocker" — quality suggestions for the owner/executor, not required plan changes.

## Consensus Summary

Both source-grounded lanes agree the plan set is executable and converging, and both independently verified that **all 15 cycle-1 findings are adequately incorporated** — the merge/purge cascade closure (H2/M5) is called out by both as the standout fix, executed with real understanding of `merge-dao.ts`/`purge-dao.ts` internals rather than a superficial patch.

### Agreed Strengths
- Migration 016 is genuinely additive against the real v15 head (`database.ts:50,53`); the staged-upgrade test avoids spoofing `user_version` and matches runner selection (both lanes).
- Merge/purge knowledge coverage (reparent before `DELETE FROM contacts`; single-current-index collision handling) is correctly reasoned (both lanes).
- Registry-first wave-1 sequencing (M1) closes a real cross-wave dependency-ordering defect (both lanes).
- Soft-delete + guarded permanent-delete two-stage pattern is consistently applied with grep gates (both lanes).
- `flagged_assumptions` are used honestly to disclose genuine owner decisions rather than silently deciding them (both lanes).

### Agreed Concerns
- The trash sweep drew a comment from BOTH lanes, but they DIVERGED on severity (see Divergent Views). No other concern was raised by both lanes.

### Divergent Views
- **Trash-sweep TOCTOU (N1).** Codex rated it **HIGH** (existence-only under-lock guard ≠ field-sweep's staleness re-check; restore-then-redelete permanently deletes a within-window row). Claude rated the same sweep a **strength** (correct non-nested mutex; guard blocks a plain restore). **Adjudicated by the aggregator against `src/db/field-ddl.ts:238-243`: codex is right on the mechanism** — the field-sweep precedent re-checks the full staleness window under the lock, Plan 05's writer checks existence only, and the plan's own TOCTOU must-have overclaims. Recorded as an unresolved **HIGH** for this cycle. Claude's strength rating holds for the plain-restore case it considered but does not cover the fresh-stamp edge.

## Cycle Summary

- **Cycle-1 HIGHs (H1, H2, H3):** all FULLY RESOLVED and verified against code — not counted.
- **Cycle-1 actionable M/L (M1–M6, M8, L1–L5):** all incorporated or explicitly deferred in-plan — not counted.
- **New this cycle:** 1 HIGH (N1, verified), 2 MEDIUM (N2, N3), 2 LOW (N4, N5) actionable; plus disclosed/non-required items not counted.

`CYCLE_SUMMARY: current_high=1 current_actionable=4`

To incorporate this feedback into planning:
  /gsd-plan-phase 24.1 --reviews
