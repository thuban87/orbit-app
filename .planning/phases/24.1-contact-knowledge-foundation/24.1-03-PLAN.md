---
phase: 24.1-contact-knowledge-foundation
plan: 03
type: execute
wave: 2
depends_on: ["24.1-01"]
files_modified:
  - src/db/relationships-dao.ts
  - src/db/relationships-dao.test.ts
  - src/db/relationships-read.ts
  - src/db/relationships-read.test.ts
autonomous: true
requirements: [KNOW-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user can record a structured relationship on a contact with a required person_name, an optional relation_type, and an optional linked_contact_id pointing at another Orbit contact (KNOW-05)."
    - "Linking to an Orbit contact is optional: a relationship with linked_contact_id NULL is fully valid and persists."
    - "addRelationship / editRelationship are scoped by BOTH id AND contact_id and assert exactly one row changed (no cross-contact corruption); editRelationship patches only present fields and never touches created_at."
    - "deleteRelationship is a SOFT delete (sets deleted_at), consistent with memories, so a removed relationship is restorable and never physically lost; listRelationshipsForContact returns only live rows (deleted_at IS NULL)."
    - "When a linked Orbit contact is hard-purged, the relationship survives with linked_contact_id reset to NULL (ON DELETE SET NULL) — the relationship is never destroyed by the other contact's deletion."
    # --- KNOW-05 edges (specless probe) ---
    - "KNOW-05 empty: a relationship with a blank/whitespace-only person_name is rejected at the commit boundary (person_name is required, NOT NULL and non-blank); empty relation_type and absent linked_contact_id are allowed."
    - { statement: "KNOW-05 encoding: person_name and relation_type store raw TEXT and round-trip a multi-byte / emoji / combining-grapheme name byte-for-byte.", verification: backstop }
  artifacts:
    - "src/db/relationships-dao.ts — add/edit(patch)/delete(soft)/restore writers (core/wrapper split)"
    - "src/db/relationships-read.ts — listRelationshipsForContact (live) + the optional linked-contact-name join"
  key_links:
    - "relationships writers mirror fuel-dao core/wrapper + both-keys scoping + newUid + bumpDataRevisionCore inside one inWriteTransaction"
    - "linked_contact_id is a ?-bound value against contacts(id); the optional linked-contact display name is resolved via a LEFT JOIN in the read, never a second query per row"
  prohibitions:
    - "Deleting a relationship must never silently lose it — it soft-deletes and stays restorable, like a Memory."
---

<objective>
Build the structured relationships / Key People data layer over the frozen 016 schema: person/name + relation type + optional link to another Orbit contact, with soft-delete and a read that resolves the optional linked-contact name (KNOW-05).

Purpose: Deliver the relationships model that Phase 8/Orrery and Phase 31 assume exists (verified absent on disk, D-04). The DAO shape is identical to fuel-dao; the tables already exist from migration 016.

Output: relationships-dao (add/edit/soft-delete/restore) + relationships-read (list + linked-contact join), node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-RESEARCH.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-PATTERNS.md
@.planning/phases/24.1-contact-knowledge-foundation/24.1-01-SUMMARY.md
@src/db/fuel-dao.ts
@src/db/fuel-read.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relationships-dao — add / edit / soft-delete / restore</name>
  <files>src/db/relationships-dao.ts, src/db/relationships-dao.test.ts</files>
  <read_first>
    - src/db/fuel-dao.ts (core/wrapper split; both-keys scoping + assertOneChange; patch-scoped edit; newUid + bumpDataRevisionCore in one inWriteTransaction)
    - src/db/uid.ts, src/db/transaction.ts, src/db/data-revision-dao.ts
    - src/db/migrations/016-contact-knowledge.ts (relationships columns — the frozen contract)
  </read_first>
  <behavior>
    - addRelationship persists a row with required person_name (blank rejected), optional relation_type/linked_contact_id/note, pinned default 0, hidden NULL, minted uid, created_at/modified_at.
    - A blank/whitespace-only person_name throws at the commit boundary before any write.
    - editRelationship patches only present fields (person_name/relation_type/linked_contact_id/note/pinned/hidden), scoped by id AND contact_id, assertOneChange, never touches created_at.
    - deleteRelationship soft-deletes (deleted_at set); restoreRelationship clears it; neither physically removes the row.
  </behavior>
  <action>
    Create src/db/relationships-dao.ts following fuel-dao.ts exactly. addRelationshipCore/addRelationship: INSERT the relationships columns in migration order, all values ?-bound, mint uid via newUid, reject a blank person_name (trim length 0) before the INSERT. editRelationshipCore/editRelationship: patch-scoped SET of only present fields + modified_at, WHERE id=? AND contact_id=?, assertOneChange. deleteRelationshipCore/deleteRelationship: soft UPDATE SET deleted_at=?, modified_at=? (never DELETE); restoreRelationshipCore/restoreRelationship: SET deleted_at=NULL. Every wrapper owns one inWriteTransaction + bumpDataRevisionCore; cores are non-mutexed. Add relationships-dao.test.ts covering add (with and without a linked contact), blank-name rejection, patch isolation, soft-delete/restore round-trip.
  </action>
  <verify>
    <automated>npx vitest run src/db/relationships-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/relationships-dao.test.ts` passes, including blank-name rejection and the optional-link case.
    - Grep confirms soft delete: `grep -nE 'DELETE FROM relationships' src/db/relationships-dao.ts` (comment-filtered) returns nothing.
    - Grep confirms both-keys scoping on every UPDATE (contact_id predicate present).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Relationships can be created, patch-edited, soft-deleted, and restored with an optional Orbit-contact link.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relationships-read — list + linked-contact name join</name>
  <files>src/db/relationships-read.ts, src/db/relationships-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.ts (single read choke point; contact_id ?-bound; only column names literal)
    - src/db/migrations/016-contact-knowledge.ts (relationships + contacts columns for the join)
  </read_first>
  <behavior>
    - listRelationshipsForContact returns a contact's live (deleted_at IS NULL) relationships with a deterministic order (pinned DESC, created_at DESC, id DESC) and each row's optional linked-contact display name resolved via LEFT JOIN contacts (NULL when unlinked or the linked contact was purged).
    - A relationship whose linked contact was purged (linked_contact_id NULL after SET NULL) returns with a NULL linked name and does not error.
    - Rows for other contacts are excluded (contact_id ?-bound); [] for none.
  </behavior>
  <action>
    Create src/db/relationships-read.ts: export the RelationshipRow interface (all relationship columns + linked_contact_name string|null) and listRelationshipsForContact(exec, contactId) with WHERE r.contact_id = ? AND r.deleted_at IS NULL, a LEFT JOIN contacts c ON c.id = r.linked_contact_id selecting c.name AS linked_contact_name, ORDER BY r.pinned DESC, r.created_at DESC, r.id DESC. Add relationships-read.test.ts covering: linked-name resolved, unlinked → NULL name, purged-link → NULL name (insert a linked contact, delete it, assert the relationship survives with NULL name), other-contact isolation, empty → [].
  </action>
  <verify>
    <automated>npx vitest run src/db/relationships-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/relationships-read.test.ts` passes, including the purged-link survival case.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>The relationships read choke point lists live relationships with the optional linked-contact name resolved in one query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → relationships-dao SQL | User-authored relationship names/types cross into SQLite |
| relationships-read JOIN | Reads another contact's name via linked_contact_id (same local DB, no egress) |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-01 | Tampering | relationships INSERT/UPDATE | medium | mitigate | All values ?-bound; only static column names literal; both-keys scope + assertOneChange |
| T-24.1-02 | Information Disclosure / data loss | soft-delete + SET NULL link | medium | mitigate | Soft-delete retains the row; purging a linked contact SET-NULLs the link and never destroys the relationship |
</threat_model>

<verification>
- `npx vitest run src/db/relationships-dao.test.ts src/db/relationships-read.test.ts` green.
- Soft-delete + both-keys greps pass; tsc --noEmit clean.
</verification>

<success_criteria>
Structured relationships with an optional Orbit-contact link are fully modeled — add/edit/soft-delete/restore + a read that resolves the linked name — with the relationship surviving the linked contact's deletion.
</success_criteria>

## Artifacts this plan produces

- **DAO (src/db/relationships-dao.ts):** addRelationship, editRelationship (patch), deleteRelationship (soft), restoreRelationship (+ cores).
- **Read (src/db/relationships-read.ts):** listRelationshipsForContact, RelationshipRow (with linked_contact_name).
- **Tests:** relationships-dao.test.ts, relationships-read.test.ts.

<output>
Create `.planning/phases/24.1-contact-knowledge-foundation/24.1-03-SUMMARY.md` when done.
</output>
