---
phase: 24.1-contact-knowledge-foundation
verified: 2026-09-04T17:05:00Z
status: human_needed
score: 3/3 success criteria verified (KNOW-01..09 satisfied)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Confirm the final display NAME for the default/general Memory type (and the general 'Memory' noun), then reconcile the single-source provisional constants before Phase 34 (Rapid Capture) plans."
    expected: "Owner picks the durable name. Code ships PROVISIONAL_DEFAULT_MEMORY_TYPE_NAME='General' and PROVISIONAL_MEMORY_LABEL='Memory' as single-source constants (src/db/memory-registry.ts:10-12), rendered everywhere via the registry — no hardcoded per-screen strings — so the reconciliation is a one-constant edit."
    why_human: "D-11 explicitly defers the final terminology to the owner and mandates surfacing it as human_needed at verify time, never a silent guess. This is a taste/product naming decision (owner's bucket), not a functional gap — the model is fully usable under the provisional names."
  - test: "On the Pixel, add a Memory value / note and a relationship person_name containing a long multi-line string, an emoji/combining-grapheme, and view at max OS font scale."
    expected: "Compact card truncates long text and shows it in full on drill-in/detail; emoji/combining graphemes round-trip byte-for-byte (raw TEXT storage guarantees the data path); layout reflows (never clip-shrinks) at max text scale; co-occurring pin + outdated markers wrap and keep >=44px targets."
    why_human: "Declared backstop truths (UI-SPEC E4). The 2026-09-04 ADB device UAT exercised short ASCII strings only, so max-text-scale reflow and emoji rendering were not visually observed. Data-path round-trip is verified by construction (TEXT columns, no truncation logic); the visual reflow is not."
---

# Phase 24.1: Contact Knowledge Foundation — Model, Storage & UI — Verification Report

**Phase Goal:** The durable contact-knowledge model exists and is usable — typed Memories from one central application-owned registry (plus a user-labelled Custom type), structured relationships, current-state history (Last Talked About, Current Location), and per-item notes/links/dates/pinning/outdated/visibility/provenance — with soft-delete + Recently Deleted (launch-sweep expiry), all surfaced in the unified "Things to Remember" UI. Additive schema only (migration 016).
**Verified:** 2026-09-04T17:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

The goal is **ACHIEVED**. The three tables, the full DAO/read layer, the launch sweep, and the unified surface all exist on disk, are substantively implemented, wired end-to-end, and flow real data. Every success criterion has independent evidence at three levels: code inspection, 71 passing node:sqlite tests I ran myself, and a physical-Pixel UAT covering the user-observable flows. One owner-facing item remains outstanding by design (the provisional Memory-type name, D-11), which is why the status is `human_needed` rather than `passed` — the model is functionally complete regardless.

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | One visually grouped "Things to Remember" surface presents first-class fields, custom fields, structured relationships, and typed Memories together, featured/current first; Memory types come from one central app-owned registry, generic Custom type carries the user's label (KNOW-01/02/05) | ✓ VERIFIED | `ThingsToRememberScreen.tsx` composes reads from memories-read / relationships-read / current-state + first-class reads / field-defs-dao, grouped per `KNOWLEDGE_GROUP_ORDER`, headers from `MEMORY_TYPE_REGISTRY`/`CURRENT_STATE_FIELD_REGISTRY`/`RELATIONSHIPS_GROUP`; no inline SQL; all colors via `useTheme()`. `MEMORY_TYPE_REGISTRY` is an in-code constant with only `general`+`custom` keys — no table/DAO/migration mints types (ADR-028/D-05 held). Reachable from `ContactProfileScreen.tsx:983`. Device UAT flows 2 & 7 PASS. |
| 2 | History-aware Last Talked About / Current Location show most-recent value on Profile with full backlist on drill-in; user can edit a historical entry or promote it back to current (KNOW-03/04) | ✓ VERIFIED | `current-state-history-dao.ts`: `setCurrentStateValue` (add-new + atomic demote in one txn), `promoteToCurrentValue` (demote current + promote target atomically, non-destructive), `editHistoryEntry` (three-key scoped). `MemoryHistoryScreen.tsx` shows current once + prior-only backlist (is_current===0), "No earlier entries", Set/Edit/Make current. Partial unique index `idx_current_state_current` enforces single-current. Device UAT flow 6 PASS (make-current preserved the displaced value). |
| 3 | Optional notes/links/meaningful date, pin, mark outdated w/o deleting, per-item visibility override, provenance in detail views, restore from Recently Deleted (launch-sweep expiry, never a timer) (KNOW-06/07/08/09) | ✓ VERIFIED | `memories-dao.ts` writes/patches all metadata (blank→NULL), soft-delete/restore, guarded permanent purge (deleted_at guard). `resolveVisibility` three-state (NULL=registry default, 0=show, 1=hide; defensive on bad values). Provenance rendered read-only in `MemoryEditor` only. `RecentlyDeletedScreen.tsx` restore + "Delete permanently?" confirm (the one danger action). `memory-trash-sweep.ts` registered via `registerSweepHook` in `App.tsx:217` behind a module guard — launch hook, strict `<` window, `MEMORY_TRASH_WINDOW_DAYS=30` tunable, no timer. Device UAT flows 3/4/5 PASS. |

**Score:** 3/3 success criteria verified. All 71 phase-relevant node tests pass.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/db/migrations/016-contact-knowledge.ts` | Additive migration: memories, relationships, current_state_entries + indexes + self-link CHECK + partial unique index | ✓ VERIFIED | Three CREATE TABLEs, self-link CHECK on relationships, partial unique `is_current=1` index, three read indexes. No DROP, no touch of custom_field tables. Registered in `database.ts` MIGRATIONS; TARGET_VERSION=16. |
| `src/db/memory-registry.ts` | In-code app-owned registry + current-state field contract + group order | ✓ VERIFIED | `MEMORY_TYPE_REGISTRY` (general/custom only), `CURRENT_STATE_FIELD_REGISTRY`, `KNOWLEDGE_GROUP_ORDER`, `RELATIONSHIPS_GROUP`, provisional-name constants, `isMemoryTypeKey`/`isCurrentStateFieldKey` guards. |
| `src/db/memories-dao.ts` | Full writer set with registry validation, both-key scoping, soft-delete, sweep writer | ✓ VERIFIED | add/edit(patch)/delete(soft)/restore/purge cores + wrappers; `expireMemoryIfStale` under-lock re-check; write-boundary registry+custom-label validation; assertOneChange. |
| `src/db/memories-read.ts` | Read choke point + resolveVisibility + deterministic order | ✓ VERIFIED | Live + recently-deleted projections; order `pinned DESC, meaningful_date DESC NULLs-last, created_at DESC, id DESC`; defensive `resolveVisibility`. |
| `src/db/relationships-dao.ts` / `relationships-read.ts` | Add/edit/soft-delete/restore/purge/expire + read w/ linked-name join + visibility | ✓ VERIFIED | Self-link guard in add+edit, person_name required, both-key scoping, `expireRelationshipIfStale` under-lock re-check, tombstone on purge. |
| `src/db/current-state-history-dao.ts` / `-read.ts` | set/promote/edit atomic + batched read | ✓ VERIFIED | Atomic demote-then-promote in one txn; field_key validated; batched `getCurrentStateValues` excludes-not-deletes unknown keys. |
| `src/db/first-class-knowledge-read.ts` | First-class fields + derived gravity/intensity (read-time, no column) | ✓ VERIFIED | `getFirstClassFields` (birthday/socialBattery/intervalDays/category) + `getFirstClassDerived` (derived via getImpactInputs, local SQLite only). |
| `src/db/merge-dao.ts` | Knowledge reparent + linked_contact_id re-point + collision demote + bidirectional self-link SET-NULL | ✓ VERIFIED | Reparent list (line 180) includes memories/relationships/current_state_entries and runs BEFORE `DELETE FROM contacts` (line 212); pre-reparent bidirectional SET-NULL; single-current collision demote. |
| `src/db/purge-dao.ts` | Explicit knowledge fan-out + impact counts + summary lines | ✓ VERIFIED | `KNOWLEDGE_PURGE_CHILDREN` (memory/relationship/current_state_entry) count+tombstone+delete inside the txn; `PurgeImpact` + `impactSummaryLines` render memory/relationship counts. |
| `src/services/memory-trash-sweep.ts` + `App.tsx` | Launch sweep for memories+relationships, tunable window, no timer | ✓ VERIFIED | `registerSweepHook`; expires both tables via the under-lock writers; registered behind `memoryTrashSweepRegistered` guard. |
| `src/screens/ThingsToRememberScreen.tsx` | Full grouped surface | ✓ VERIFIED | See Truth 1. |
| `src/screens/MemoryHistoryScreen.tsx` | History backlist drill-in | ✓ VERIFIED | See Truth 2. |
| `src/screens/RecentlyDeletedScreen.tsx` | Restore + guarded permanent delete | ✓ VERIFIED | See Truth 3. |
| `src/components/MemoryEditor.tsx` / `MemoryCard.tsx` / `RelationshipEditor.tsx` / `ContactPicker.tsx` | Editors (no DB access) + owner-excluding picker | ✓ VERIFIED | `RelationshipEditor` passes `excludeContactId={contactId}` to `ContactPicker`; picker filters owner out (UAT flow 2 PASS). Placeholders `KnowledgePlaceholders.tsx` deleted as planned. |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `database.ts` MIGRATIONS | migration016 | import + array entry + TARGET_VERSION=16 | ✓ WIRED |
| ThingsToRememberScreen | all DAO reads/writes | direct imports, no inline SQL, reload-after-write | ✓ WIRED |
| ContactProfileScreen | ThingsToRemember | `navigation.navigate("ThingsToRemember", { contactId })` (line 983) | ✓ WIRED |
| Routes | both stacks | types in Dashboard+Orrery param lists; mounted in both `DashboardStack.tsx` + `OrreryStack.tsx` | ✓ WIRED |
| memory-trash-sweep | launch hook | `registerSweepHook` + `App.tsx` guarded registration | ✓ WIRED |
| merge/purge | knowledge tables | reparent/fan-out inside the existing txn, before contacts DELETE | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Real Data | Status |
|----------|---------------|--------|-----------|--------|
| ThingsToRememberScreen | knowledge.memories/relationships/currentValues/firstClass/customValues | live SQLite reads in `load()` | Yes | ✓ FLOWING |
| MemoryHistoryScreen | history | `getCurrentStateHistory` | Yes | ✓ FLOWING |
| RecentlyDeletedScreen | rows | `listRecentlyDeleted` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase DAO/migration/sweep suites | `npx vitest run` (7 files) | 7 files / 71 tests pass | ✓ PASS |
| Migration 016 reaches v16 from ANY prior version with 3 tables + constraints | `016-contact-knowledge.test.ts` (`it.each` over 0..15) | pass | ✓ PASS |
| Typecheck | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Color tokens | `npm run check:colors` | exit 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| KNOW-01 | Grouped Things-to-Remember surface, featured first | ✓ SATISFIED | Truth 1 |
| KNOW-02 | Typed memories from central registry + Custom label | ✓ SATISFIED | Truth 1; registry is in-code constant |
| KNOW-03 | History-aware LTA/Current Location, backlist on drill-in | ✓ SATISFIED | Truth 2 |
| KNOW-04 | Edit historical + promote-to-current | ✓ SATISFIED | Truth 2; UAT flow 6 |
| KNOW-05 | Structured relationships w/ optional Orbit link | ✓ SATISFIED | relationships-dao; UAT flows 1/2/3 |
| KNOW-06 | Notes/links/date/pin/outdated | ✓ SATISFIED | Truth 3; memories-dao metadata |
| KNOW-07 | Soft-delete + Recently Deleted, launch-sweep expiry | ✓ SATISFIED | Truth 3; sweep; UAT flow 5 |
| KNOW-08 | Type visibility defaults w/ per-item override, presentation-only | ✓ SATISFIED | resolveVisibility; UAT flows 3/4 |
| KNOW-09 | Lightweight provenance in detail views | ✓ SATISFIED | provenance rendered read-only in MemoryEditor only |

### Prohibitions (judgment-tier — verified NOT violated)

| Prohibition | Status | Evidence |
|-------------|--------|----------|
| Memory-type SET never user-writable (ADR-028/D-05) | ✓ HELD | Registry is in-code constant; no table/DAO/migration to mint types |
| Soft-deleted/outdated never silently disappear | ✓ HELD | Soft-delete keeps row; restorable until sweep; outdated stays de-emphasized |
| Hidden-from-Profile never a privacy/AI control | ✓ HELD | Presentation-only; no Memory sent to AI in 24.1; explicit code comments |
| Sweep never deletes before window elapses | ✓ HELD | Strict `<` boundary + under-lock full re-check (TOCTOU-safe) |
| Merge never relies on FK CASCADE for knowledge tables | ✓ HELD | Reparent (line 180) precedes contacts DELETE (line 212) |
| Permanent delete never one-tap from live surface | ✓ HELD | Two-stage; confirm dialog only in RecentlyDeletedScreen |
| Promote never destroys prior current value | ✓ HELD | Atomic demote-then-promote; UAT flow 6 confirmed |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| (none) | No TODO/FIXME/XXX debt markers in any phase file | — | Clean; all "placeholder" hits are legit TextInput props |
| ThingsToRememberScreen / MemoryEditor / MemoryCard | `color: colors.accent` on link/text (8 sites) vs contract's `accentText` | ℹ️ Info (UI-REVIEW W1) | Non-blocking; `check:colors` passes (exit 0). Phase-23 contrast-gate risk flagged for polish, not goal failure. |
| MemoryEditor | Primary "Add memory"/Save hand-rolled vs `Button role="primary"` | ℹ️ Info (UI-REVIEW W2) | Non-blocking visual inconsistency |
| ContactPicker / Snackbar | Raw `Text` + hardcoded fontSize/spacing off the scale | ℹ️ Info (UI-REVIEW W3) | Non-blocking; colors already tokenized |

Note: `purgeMemoryPermanentlyCore` / `purgeRelationshipPermanentlyCore` / purge-dao now mint tombstones (`memory`/`relationship`/`current_state_entry` are valid `TombstoneEntityType` values; `BACKUP_FORMAT_VERSION=4`). This is STRONGER than the plans' flagged assumption ("no tombstone in 24.1") and reflects the owner-recorded CR-01/v4-early backup-preservation amendment (commit d677e2c, ROADMAP amendment 2026-09-04). Not a deviation from goal — it improves data safety. The v4 bump itself is a recorded-decision deviation already surfaced and accepted at the milestone level (out of scope to re-litigate here).

### Human Verification Required

1. **Final Memory-type name (D-11)** — Owner must confirm the durable display name for the default/general Memory type before Phase 34. Code ships provisional single-source constants (`memory-registry.ts:10-12`); reconciliation is a one-constant edit. This is the phase's contract-mandated human_needed item, not a functional gap.
2. **Long-text / emoji / max-text-scale visual reflow** (declared backstop) — The ADB device UAT used short ASCII strings; visually confirm truncation-on-card / full-on-detail, emoji round-trip, and reflow at max OS font scale on the Pixel. Data-path round-trip is verified by construction (raw TEXT, no truncation).

### Gaps Summary

No blocking gaps. All three ROADMAP success criteria and all nine KNOW-01..09 requirements are satisfied in the codebase, confirmed by code inspection, 71 passing node:sqlite tests, green tsc + check:colors, and a physical-device UAT of the seven user-observable flows. The data layer — the highest-risk surface per project convention — was audited across every writer of the three new tables (DAOs, merge-dao, purge-dao, sweep): scoping, soft-delete/restore, TOCTOU-safe expiry, merge reparent-before-delete ordering, and self-link defense-in-depth (app guards + DB CHECK) are all correct. Status is `human_needed` solely because of the deliberately deferred Memory-type naming decision (owner's bucket) plus one backstop visual spot-check — neither blocks the goal.

---

_Verified: 2026-09-04T17:05:00Z_
_Verifier: Claude (gsd-verifier)_
