---
context: phase
phase: 24.1-contact-knowledge-foundation
task: 4
total_tasks: 6
status: paused
last_updated: 2026-09-04T14:15:29.172Z
---

# Phase 24.1 Pause Handoff — physical-device UAT

<current_state>

All seven implementation plans are complete. The three advisory code-review findings were fixed in `aef0a90`; the Phase 24.1 security verification was added in `b4762eb`. Regression, TypeScript, color-token, and whitespace gates pass (`npm test`: 224 files / 2,177 tests).

The phase is **not complete**. Physical-device UAT is in progress on the installed release app and must be finished before Nyquist/UI/goal verification can truthfully close the phase.

</current_state>

<completed_work>

- Plans `24.1-01` through `24.1-07` executed (`499a7eb`).
- Review fixes completed (`aef0a90`): memory default visibility inheritance, relationship `hidden` boundary validation, and optional relationship backup-field validation.
- Security verification completed (`b4762eb`): `24.1-SECURITY.md`, 16 reviewed threats, zero open threats.
- Automated gates passed: full test suite, `npx tsc --noEmit`, `npm run check:colors`, `git diff --check`.
- Release APK built on `droid`, pulled to `/tmp/orbit-phase24.1-release.apk`, installed with `adb install -r`, and smoke-launched on the Pixel 6 Pro.
- Device observations already passed on contact **UAT Ada**:
  - Things to Remember empty surface and controls render.
  - Memory `PixelUATMemory` was created and renders.
  - Current Last talked about value `PixelUATHistory` was saved and renders.
  - Relationship `PixelUATRelationship` was created and renders.
  - Memory trash/restore lifecycle passed: move to Recently Deleted, open Trash, restore, then Trash reports empty.

</completed_work>

<remaining_work>

Continue actual ADB/device UAT. Do not report a result as passing until it is directly observed.

1. **Verify relationship Undo** — `PixelUATRelationship` was deleted and snackbar Undo tapped; confirm it is present after refreshing/navigation. Relationship deletion is Undo-only; it must never appear in Recently Deleted.
2. **Relationship link picker and owner exclusion** — create or edit a relationship, link the second contact, and verify the owner/contact `UAT Ada` cannot be selected as the related contact.
3. **Relationship hide/unhide** — set Hide from Profile, enable Show hidden items, confirm the hidden relationship appears only there, then unhide with Show on Profile.
4. **Memory hide/unhide** — same visibility flow using a memory, including proof hidden content does not render in the normal surface.
5. **Permanent memory deletion** — move a memory to Recently Deleted; choose Delete permanently; first press Cancel and confirm it remains, then confirm deletion and prove it no longer appears.
6. **History semantics** — set a second Last talked about value, view history, promote a historical value with Make current, and confirm the displaced value remains as prior history.
7. **Orrery origin** — ensure an interaction exists for UAT Ada, use the Skia planet via screenshot-derived coordinate if necessary, and prove navigation into that contact’s Things to Remember surface.
8. Run the remaining configured Nyquist/UI-review/phase-goal verification gates and update tracking only if each is honest and passes.

</remaining_work>

<device_state>

- Re-discover the serial before commands: `/home/bwales/.local/bin/adb devices -l`.
- At pause it was `1A071FDEE002BU`, `Pixel_6_Pro`, product `raven`.
- Do **not** clear app data or uninstall the app: the test values above are deliberately retained.
- If the device locks, `adb shell input keyevent 82` returned to Orbit successfully.
- Use `uiautomator dump` and inspect accessibility text/content descriptions; Orrery uses Skia and may need a screenshot plus coordinate tap.
- Current data: `PixelUATMemory` is live after restore; `PixelUATHistory` is the current Last talked about value; `PixelUATRelationship` needs Undo-restoration confirmation.

</device_state>

<critical_anti_patterns>

| Pattern | Description | Prevention |
|---|---|---|
| Stop before UAT is done | Prior work repeatedly reported interim status while mandatory device checks remained. | Continue the checklist to a definitive pass/fail state, or create a fresh handoff only if genuinely unable to proceed. Never mark the phase done early. |
| Add a new renderer test dependency | `react-test-renderer` was briefly installed while investigating coverage, then completely removed at owner direction. | The owner explicitly chose real ADB-driven device UAT. Do not add it back or introduce a RN test environment for this phase. |
| Destroy UAT state | The installed app has deliberately created lifecycle/history data. | Never clear data or uninstall; use additional uniquely named records when needed. |
| Lose the desktop build transfer | Root-level `app-*.apk` artifacts made the source tar roughly 863 MB and stalled transfer. | If rebuilding, follow `docs/runbooks/desktop-build-pipeline.md` and exclude `./*.apk`, node_modules, android, ios, .git, .expo, and .planning from the tar source transfer. |
| Assume UI behavior from code or selectors | UI verification is the outstanding gate. | Record results only after observing them on the physical device. |

</critical_anti_patterns>

<required_reading>

1. `.planning/HANDOFF.json`
2. This file
3. `docs/runbooks/desktop-build-pipeline.md` (only if a rebuild becomes necessary)
4. `.planning/phases/24.1-contact-knowledge-foundation/24.1-SECURITY.md`
5. `.planning/phases/24.1-contact-knowledge-foundation/24.1-REVIEW.md`
6. `.planning/phases/24.1-contact-knowledge-foundation/24.1-06-PLAN.md` and `24.1-07-PLAN.md`
7. `.planning/STATE.md` and `AGENTS.md`

</required_reading>

<worktree_state>

- Do not stage/revert unrelated modified planning files: `.planning/config.json`, `24.1-REVIEW.md`, and `24.1-REVIEW-FIX.md`.
- Preserve subsequent Phase 24.2 commits already on the branch; they are unrelated to this UAT session.
- No local watcher or async job is running.

</worktree_state>

<next_action>

Start by rediscovering the connected ADB device and verifying whether `PixelUATRelationship` actually returned after Undo. Then complete the remaining checklist without resetting the device/app state.

</next_action>
