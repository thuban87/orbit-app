---
phase: 36-ai-configuration-prompting
plan: 05
type: execute
wave: 3
depends_on: [36-01, 36-02]
files_modified:
  - src/screens/AIConnectionScreen.tsx
  - src/screens/ai-connection-logic.ts
  - src/screens/ai-connection-logic.test.ts
  - src/screens/AIModelPickerScreen.tsx
  - src/screens/ai-model-picker-logic.ts
  - src/screens/ai-model-picker-logic.test.ts
  - src/components/AINeedsAttention.tsx
autonomous: true
requirements: [AICFG-02, AICFG-03, AICFG-05, AICFG-15]
user_setup: []

estimate:
  tokens: 104000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The connection surface presents selectable GlassSurface cards (not literal radio buttons) grouped Recommended (OpenRouter) and Advanced (direct BYOK OpenAI/Anthropic/Gemini; Custom Endpoint), exactly one active with the accent selected-state, others marked 'Saved'; switching never deletes an inactive config (AICFG-02/03)."
    - "Starting setup on a new lane does not deactivate the working connection — the new lane activates only on success; cancel/failure leaves the prior connection active; removal is a separate deliberate destructive action (AICFG-03)."
    - "OpenRouter setup launches the browser-auth flow (no key paste); the curated-first model picker shows real current/cached pricing, a Browse-all path with search, recommendation reasons, and deeper metadata under More Info; renders offline from cache and refreshes daily/manually (AICFG-04 surfacing / AICFG-02)."
    - "Direct BYOK key entry and the Custom Endpoint credential go ONLY to SecureStore (ai-key-store), never into a form-serialized settings patch; the Custom URL is validated through validateCustomEndpoint before persistence and shows the retention caveat (AICFG-15/02)."
    - "AI-on-but-invalid renders a restrained Needs Attention notice (not a danger slab, not OFF) with cause-appropriate repair actions (Reconnect / Replace key / Choose another model / Change AI connection); an unavailable model requires explicit reselection with no silent substitution (AICFG-05)."
    # --- AICFG-15 concurrency edge (explicit) ---
    - "A credential write/delete during an in-flight availability check leaves credentials consistent — the SecureStore write is the single source of truth and app_settings/backup never mirror it (AICFG-15 concurrency)."
    # --- UI-considerations lift (UI-SPEC #2/#3/#5/#6) ---
    - "First run shows all selectable lanes to pick (no 'no connections' void); an empty key/URL field renders its form with placeholder + retention caveat and nothing persists until valid (UI-SPEC E2/E5 empty)."
    - "A configured-but-inactive lane renders a 'Saved' marker with no active indicator; switching to it restores its remembered model (UI-SPEC E2 partial)."
    - "Direct/Custom lanes show token size + 'Cost estimate unavailable for this connection.' (no price source) while OpenRouter shows real pricing (UI-SPEC E5/E9 partial)."
    # --- backstop ---
    - { statement: "Long text (endpoint URL, long model/provider names, custom guidance) wraps or truncates in cards/notices without breaking layout — visual UI-state check at verify time.", verification: backstop }
    - { statement: "In-progress connect/auth shows a loading affordance and the connection becomes active only on success (no optimistic active-state) — verified on the Pixel.", verification: backstop }
  prohibitions:
    - statement: "MUST NOT copy a BYOK API key or Custom Endpoint credential into a form-serialized settings patch, app_settings, the backup, or a log — credentials go only to SecureStore."
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT silently switch to another saved connection or model when the active one is invalid/unavailable — present Needs Attention with explicit repair, never an automatic substitution."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT remove or weaken the custom-endpoint egress guards (HTTPS-only, no credentials-in-URL, no .local, no IP literals) — adding a host allowlist is additive, removing a guard is forbidden."
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT imply an arbitrary HTTP AI API will work — the Custom lane is labelled Advanced and documents OpenAI-compatible HTTPS only."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - src/screens/AIConnectionScreen.tsx
    - src/screens/AIModelPickerScreen.tsx
    - src/components/AINeedsAttention.tsx
  key_links:
    - "connection cards read/write ai-connections-dao + ai-config-store (Plan 01); active pointer in app_settings"
    - "OpenRouter card uses openrouter-oauth + openrouter-catalog (Plan 02); direct lanes use LiteLLM catalog"
    - "BYOK/custom credentials → ai-key-store only; custom URL → validateCustomEndpoint before save"
    - "Needs Attention consumes computeAiAvailability (Plan 01); Compose repair route reuses AINeedsAttention"
---

<objective>
Surface the multi-connection model the tracer proved: the three-lane connection picker (OpenRouter
recommended, BYOK + Custom advanced), the Orbit-owned curated-first model picker with real pricing, and
the Needs-Attention/repair experience — all reusing Orbit primitives, the SecureStore credential
boundary, and the egress guards.

Purpose: Complete AICFG-02/03/05 at the UI layer without re-deriving availability (Plan 01) or the
OpenRouter integration (Plan 02). Safe switching and no-silent-substitution are the load-bearing
behaviors.
Output: `AIConnectionScreen`, `AIModelPickerScreen`, `AINeedsAttention` + their node-pure logic.

**Decisions honored (CONTEXT.md):** D-04 (BYOK/custom credentials only in SecureStore, never a settings patch/backup), D-05 (custom-endpoint egress guards preserved — HTTPS-only/no-creds/no-.local/no-IP; adding a host allowlist is additive, removing a guard is forbidden), D-11 (no silent substitution — an unavailable model/connection → Needs Attention with explicit repair).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-ai-configuration-prompting/36-RESEARCH.md
@.planning/phases/36-ai-configuration-prompting/36-PATTERNS.md
@.planning/phases/36-ai-configuration-prompting/36-UI-SPEC.md

# Analog / source-of-truth files (read before editing)
@src/ai/custom-endpoint.ts
@src/services/ai-key-store.ts
@src/db/ai-connections-dao.ts
@src/logic/ai-availability.ts
@src/ai/model-catalog-filter.ts
@src/screens/settings-ai-logic.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Connection lanes UI + safe switching (one active, saved inactive, deliberate removal)</name>
  <files>src/screens/AIConnectionScreen.tsx, src/screens/ai-connection-logic.ts, src/screens/ai-connection-logic.test.ts</files>
  <read_first>36-UI-SPEC.md Surface #2/#5 + Copywriting (Saved marker, retention caveat, destructive confirms), src/db/ai-connections-dao.ts + src/stores/ai-config-store.ts (Plan 01), src/services/ai-key-store.ts (:26-53), src/ai/custom-endpoint.ts (validateCustomEndpoint), src/screens/settings-ai-logic.ts (:74-92 buildAiSettingsPatch has NO apiKey path — preserve)</read_first>
  <behavior>
    - Exactly one connection active; others render a 'Saved' marker; switching to a saved valid connection restores its remembered model and never deletes the prior.
    - Starting setup on a new lane keeps the prior active until the new one succeeds; cancel/failure keeps the prior.
    - BYOK key + Custom credential are written only via ai-key-store; the settings patch never carries a key.
    - Custom URL is validated (HTTPS-only, no creds, no .local, no IP) before persistence; invalid → danger inline error, nothing persisted.
    - Removal (Disconnect / Remove key) is a separate destructive action that deletes only that lane's credential.
  </behavior>
  <action>Write RED logic tests first, then build the screen. `ai-connection-logic.ts` (node-pure): switching/activation state machine (activate-on-success, keep-prior-on-failure, one-active invariant, restore-remembered-model), credential-routing helpers that call ai-key-store (never a settings patch), and Custom-endpoint save that runs `validateCustomEndpoint` first. `AIConnectionScreen.tsx`: selectable GlassSurface cards grouped Recommended (Connect with OpenRouter → launches Plan 02's browser flow) and Advanced (direct OpenAI/Anthropic/Gemini key entry; Custom Endpoint labelled Advanced, wording "OpenAI-compatible HTTPS", fields base URL/optional credential/model id, show `CUSTOM_RETENTION_CAVEAT`), each expandable (AccordionSection) with inline setup/status; the active card carries the accent selected-state, inactive show "Saved". Destructive removal via `ConfirmDialog`. All colours via theme tokens; icon-only controls declare accessibilityLabel.</action>
  <verify>
    <automated>npx vitest run src/screens/ai-connection-logic.test.ts && npm run check:colors</automated>
  </verify>
  <done>The lane picker enforces one-active/saved-inactive/deliberate-removal and safe switching; credentials route only to SecureStore; custom URLs are guard-validated.</done>
  <acceptance_criteria>
    - Logic test: a failed new-lane setup leaves the prior connection active; a successful one activates the new lane.
    - Grep proves no code path writes a BYOK/custom credential to a settings patch, app_settings, backup, or log.
    - Custom-endpoint save calls `validateCustomEndpoint` before persistence; an invalid URL persists nothing.
    - `npm run check:colors` + `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Curated-first model picker (OpenRouter pricing + direct LiteLLM) </name>
  <files>src/screens/AIModelPickerScreen.tsx, src/screens/ai-model-picker-logic.ts, src/screens/ai-model-picker-logic.test.ts</files>
  <read_first>36-UI-SPEC.md Surface #3/#4 (model cards, pricing, Browse all, freshness), src/ai/openrouter-catalog.ts (Plan 02 — pricing + cache + refresh), src/ai/model-catalog-filter.ts (direct LiteLLM catalog), src/services/model-registry.ts (curated set), dossier §K/§L/§M (curated-first, pricing, refresh)</read_first>
  <action>Build the Orbit-owned model picker. `ai-model-picker-logic.ts` (node-pure): curated-first ordering (curated set in its defined order, then the broader catalog by a stable key — deterministic, no reshuffle), search/filter, and card-model derivation. For OpenRouter, source cards + real input/output pricing from `openrouter-catalog` (decimal-string pricing; never `$/$$/$$$`, never hardcoded ids/prices) with recommendation reasons (Balanced / Cost efficient / Lightweight) and deeper metadata behind More Info; render instantly from cache (offline-safe) and expose Refresh Models + an "Updated {formatLocalDate}" freshness caption. For direct BYOK, source from the LiteLLM catalog + a manual model-id escape hatch; show no monetary estimate ("Cost estimate unavailable for this connection."). Selecting a model writes the per-connection remembered_model via ai-connections-dao. Empty/offline catalog renders cached models or the documented "No models available. Connect and refresh…" state — never blank.</action>
  <verify>
    <automated>npx vitest run src/screens/ai-model-picker-logic.test.ts && npm run check:colors</automated>
  </verify>
  <done>Curated-first picker renders with real OpenRouter pricing and deterministic order; direct lanes show token-size only; selecting a model persists per-connection.</done>
  <acceptance_criteria>
    - Ordering is deterministic (curated first in defined order, then stable-keyed catalog) — a logic test asserts stability across two renders.
    - No hardcoded model id or price literal in the picker; grep proves pricing derives from `openrouter-catalog`.
    - Direct/Custom show "Cost estimate unavailable for this connection."; OpenRouter shows real pricing.
    - Empty catalog renders cached-or-documented-empty, never a thrown error on open.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Needs Attention / repair notice (Settings + Compose consumption)</name>
  <files>src/components/AINeedsAttention.tsx, src/screens/ai-connection-logic.ts, src/screens/ai-connection-logic.test.ts</files>
  <read_first>36-UI-SPEC.md Surface #6 + Color guardrail (restrained tonal notice, NOT a danger slab; status hues not repurposed), src/logic/ai-availability.ts (Plan 01 computeAiAvailability), dossier §F/§N (Needs Attention + model unavailability), MEMORY phase-23 galaxy-dark AA note (contrast)</read_first>
  <action>Build `AINeedsAttention.tsx`: a restrained tonal GlassSurface notice with a `warning` glyph in `danger` tone (NOT a full danger-filled slab; do not repurpose statusStable/Wobble/Decay/rogue hues) and a Primary/Secondary repair button whose action is cause-appropriate — Reconnect (expired OpenRouter), Replace key (invalid BYOK), Choose another model (unavailable model), Change AI connection. Derive the cause→repair mapping in `ai-connection-logic.ts` from `computeAiAvailability` inputs (no silent substitution — an unavailable model requires explicit reselection). Compose consumes the same component so AI-on-but-invalid replaces the AI action with the notice rather than disappearing; AI-off simply hides AI (distinct state). All colours via theme tokens.</action>
  <verify>
    <automated>npx vitest run src/screens/ai-connection-logic.test.ts && npm run check:colors</automated>
  </verify>
  <done>Needs Attention renders as a restrained notice with the correct repair action per cause; no silent substitution; off and needs-attention are distinct.</done>
  <acceptance_criteria>
    - Cause→repair mapping test: expired connection→Reconnect, invalid key→Replace key, unavailable model→Choose another model.
    - `AINeedsAttention` uses no `danger`-filled slab and no status/rogue hue (code review + check:colors).
    - The notice never triggers an automatic model/connection switch.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- **New files:** `src/screens/AIConnectionScreen.tsx` + `ai-connection-logic.ts`; `src/screens/AIModelPickerScreen.tsx` + `ai-model-picker-logic.ts`; `src/components/AINeedsAttention.tsx`.
- **Behavior:** three-lane selectable-card picker, safe switching (activate-on-success), curated-first model picker with real OpenRouter pricing, Needs-Attention/repair (Settings + Compose).
- **Consumes:** ai-connections-dao / ai-config-store / ai-availability (Plan 01); openrouter-oauth / openrouter-catalog (Plan 02); validateCustomEndpoint + ai-key-store.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → credentials | BYOK/custom credential entry must land only in SecureStore |
| app → custom endpoint host | user-entered URL is an egress target (SSRF surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-17 | Information disclosure | BYOK/custom credential | high | mitigate | key routed only through ai-key-store; buildAiSettingsPatch has no key path; grep-asserted |
| T-36-18 | SSRF / info disclosure | custom endpoint URL | high | mitigate | `validateCustomEndpoint` (HTTPS-only, no creds, no .local, no IP) + `secure-fetch` connect-time guard; no guard removed (canon SSRF — also breadcrumb `/gsd-secure-phase`) |
| T-36-19 | Elevation/misconfig | silent substitution on failure | high | mitigate | Needs Attention with explicit repair; computeAiAvailability never auto-picks another connection/model |
| T-36-20 | Spoofing | misleading Custom lane wording | low | mitigate | Advanced label + "OpenAI-compatible HTTPS only" copy |
</threat_model>

<verification>
- `npx vitest run src/screens/ai-connection-logic.test.ts src/screens/ai-model-picker-logic.test.ts` green; `npm run check:colors` + `npx tsc --noEmit` clean.
- Device UAT (Pixel, end-of-phase): connect OpenRouter (no generation), switch lanes without deleting configs, remove a key, model-unavailable → Needs Attention, offline catalog renders from cache.
</verification>

<success_criteria>
- Three lanes selectable with one active, saved-inactive retained, deliberate removal.
- Curated-first picker with real OpenRouter pricing; direct lanes token-size only.
- Needs Attention repairs with no silent substitution; credentials only in SecureStore; guards intact.
</success_criteria>

<output>
Create `.planning/phases/36-ai-configuration-prompting/36-05-SUMMARY.md` when done.
</output>
