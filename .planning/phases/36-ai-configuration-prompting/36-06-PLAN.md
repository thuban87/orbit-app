---
phase: 36-ai-configuration-prompting
plan: 06
type: execute
wave: 3
depends_on: [36-01, 36-02, 36-03]
files_modified:
  - src/db/personalization-dao.ts
  - src/db/personalization-dao.test.ts
  - src/ai/context-estimate.ts
  - src/ai/context-estimate.test.ts
  - src/ai/prompt-template.ts
  - src/ai/prompt-template.test.ts
  - src/screens/AIPersonalizationScreen.tsx
  - src/screens/ai-personalization-logic.ts
  - src/screens/ai-personalization-logic.test.ts
autonomous: true
requirements: [AICFG-06, AICFG-07]
user_setup: []

estimate:
  tokens: 104000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Users customize Writing Style through structured controls — Tone (Casual/Balanced/Polished), Length (Concise/Normal/Detailed), Directness (Gentle/Balanced/Direct), each with a Use-custom-guidance opt-out — plus a freeform guidance field, layered onto Orbit's immutable system/output contract which the user cannot replace (AICFG-06)."
    - "Users create ordered, enableable Personalization Context sections (title + long-form body + enabled/disabled + order), added via paste or .txt/.md import copied into a local editable record (no live file link); enabled = sent, disabled = stored-not-sent, with no second per-section permission flag (AICFG-06)."
    - "Section ordering is organizational only and does not secretly alter semantic weighting; sections are global context, not a second per-contact store (AICFG-06)."
    - "Personalization Context surfaces an estimated context size in tokens (debounced after edits, never per-keystroke network) and, for OpenRouter, an estimated input cost from cached/fresh selected-model pricing; Direct/Custom show token size + 'Cost estimate unavailable for this connection.' (AICFG-07)."
    - "There is no artificial context ceiling and no silent truncation: true model-capacity overflow is surfaced explicitly as a warning requiring deliberate resolution (reduce/disable context, change personalization, or pick a larger-context model) (AICFG-07)."
    # --- UI-considerations lift (UI-SPEC #7/#8/#9) ---
    - "Writing Style always ships defaults (Balanced/Normal/Balanced) — no empty state; the freeform field is optional (UI-SPEC E7 empty)."
    - "Personalization Context empty shows 'No personalization yet.' with an Add/paste next step; with no enabled context the estimate reads ~0 tokens and shows no cost line until a priced model is active (UI-SPEC E8/E9 empty)."
    - "The estimate recomputes debounced after meaningful edits and the stale estimate stays visible during recompute (UI-SPEC E9 loading)."
    - "Personalization section reordering is operable without precise dragging: Move up / Move down controls (or an accessible reorder mode) exist alongside the drag handle, and every icon-only control (drag handle, overflow menu) declares an accessibilityLabel (UI-SPEC E8 accessibility, review MEDIUM)."
    # --- backstop ---
    - { statement: "A long section body / long custom guidance wraps or truncates in its card without breaking layout — visual UI-state check at verify time.", verification: backstop }
    - { statement: "A .txt/.md import copies the file contents into an editable local section and source-file movement/deletion does not break Orbit — device import check.", verification: backstop }
  prohibitions:
    - statement: "MUST NOT silently truncate or discard enabled context to fit an arbitrary product ceiling — over-capacity is surfaced explicitly and requires deliberate user resolution."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT let user personalization replace Orbit's immutable system/output/privacy contract — Writing Style and sections are subordinate content only."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT hit the network per personalization keystroke to refresh pricing — token/context recompute is local and debounced; price metadata is reused per its refresh policy."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT maintain a live link/watch to an imported personalization file — imported content is copied into a local editable record."
      category: privacy
      status: resolved
      verification: test
  artifacts:
    - src/db/personalization-dao.ts
    - src/ai/context-estimate.ts
    - src/screens/AIPersonalizationScreen.tsx
  key_links:
    - "Writing Style reads/writes the migration-029 app_settings columns; sections use the personalization_sections table"
    - "prompt-template renders Writing Style + enabled sections as DATA blocks via the sole resolvePrompt (byte-identity preserved)"
    - "context-estimate imports OpenRouter pricing + model `context_length` from openrouter-catalog (Plan 02); over-capacity is measured against the SELECTED MODEL's real context window (NOT the retired fixed TOTAL_LIMIT) and surfaced via TruncationNotice/warning, never trimmed (AICFG-07, review HIGH #5; same contract as 36-03)"
---

<objective>
Give users the personalization surface — structured Writing Style + arbitrary ordered Personalization
Context sections (paste/import) — layered onto Orbit's immutable prompt contract, with a live token/context
estimate and OpenRouter input-cost, and explicit overflow (never silent truncation).

Purpose: This is the user's authored AI voice. The load-bearing guarantees are: the system prompt stays
immutable, disabled context is stored-not-sent, and no arbitrary ceiling silently drops context.
Output: `personalization-dao.ts`, `context-estimate.ts`, `AIPersonalizationScreen`, and the
prompt-template rendering of Writing Style + enabled sections.

**Decisions honored (CONTEXT.md):** D-03 (Writing Style + personalization durable in app_settings/table, not AsyncStorage), D-11 (no silent truncation — over-capacity surfaced explicitly, no arbitrary ceiling).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-ai-configuration-prompting/36-RESEARCH.md
@.planning/phases/36-ai-configuration-prompting/36-PATTERNS.md
@.planning/phases/36-ai-configuration-prompting/36-UI-SPEC.md

# Analog / source-of-truth files (read before editing)
@src/db/systems-dao.ts
@src/ai/token-budget.ts
@src/ai/prompt-template.ts
@src/ai/openrouter-catalog.ts
@src/screens/settings-ai-logic.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: personalization-dao (Writing Style + sections CRUD + import) </name>
  <files>src/db/personalization-dao.ts, src/db/personalization-dao.test.ts</files>
  <read_first>src/db/systems-dao.ts (durable-defs CRUD + reorder + inWriteTransaction/newUid), src/db/app-settings-dao.ts (updateAppSettingsCore + the migration-029 Writing Style columns), dossier §P/§Q/§R/§T (Writing Style, sections, import, enablement semantics), expo-document-picker/expo-file-system availability (RESEARCH Standard Stack)</read_first>
  <behavior>
    - Writing Style getters/setters over the app_settings columns; each dimension supports a 'custom' opt-out value; freeform stored separately.
    - Section CRUD over personalization_sections: create/rename/edit/enable-disable/reorder/delete/replace; enabled default 1; display_order stable.
    - Import copies .txt/.md contents into a new section (a local editable record); no live file handle retained.
    - Enabled=included flag, Disabled=stored-not-sent; there is no second per-section AI-permission flag.
  </behavior>
  <action>Write RED tests first, then implement `personalization-dao.ts` (node-pure): Writing Style get/set over the migration-029 columns (tone/length/directness with a 'custom' opt-out value + freeform); section CRUD over `personalization_sections` (create with `newUid()`, rename, edit body, toggle enabled, reorder via display_order, delete, replace-by-reimport) inside `inWriteTransaction` with `changes` asserted; and an import helper that takes already-read file text (inject the file-read via expo-document-picker/expo-file-system at the screen layer) and copies it into a new section with no retained file reference. Do not add a per-section permission column (enabled/disabled is the only gate — §T). Keep everything node-testable.</action>
  <verify>
    <automated>npx vitest run src/db/personalization-dao.test.ts</automated>
  </verify>
  <done>Writing Style + section CRUD + import are node-tested; enabled/disabled is the sole section gate; imported content is a detached copy.</done>
  <acceptance_criteria>
    - Section reorder produces a stable, deterministic display_order; a delete removes exactly one section.
    - The import helper stores text with no retained file path/handle (grep-asserted; no live link).
    - No per-section permission column is introduced.
    - Writing Style 'custom' opt-out round-trips through the app_settings columns.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Writing Style + enabled sections in the prompt; context/cost estimate + overflow</name>
  <files>src/ai/prompt-template.ts, src/ai/prompt-template.test.ts, src/ai/context-estimate.ts, src/ai/context-estimate.test.ts</files>
  <read_first>src/ai/prompt-template.ts (DATA-block + PER_VALUE_LIMIT + the fixed `TOTAL_LIMIT`=6000 field-omission loop :298-318 + hard-trim :324-333 + truncations; the USER STYLE NOTE block being reconciled; :175-184 byte-identity), src/ai/token-budget.ts (:60-75 node-pure per-provider sizing), src/ai/openrouter-catalog.ts (Plan 02 pricing + input-cost helper + model `context_length`), src/ai/prompt-types.ts (:157-166 TruncationNotice/warning shape), dossier §O/§U/§V (immutable prompt, no ceiling, input-cost only), AICFG-07 (no artificial context ceiling — the requirement is authority), 36-03 (which renders memories/notes and must state the SAME budget contract)</read_first>
  <behavior>
    - resolvePrompt renders structured Writing Style + each enabled Personalization Context section as its own DATA-delimited block; disabled sections are absent; byte-identity preserved.
    - Orbit's immutable system/output contract is unchanged and cannot be replaced by personalization text.
    - No artificial context ceiling (AICFG-07, review HIGH #5): the fixed `TOTAL_LIMIT`=6000 field-omission governor is RETIRED as the context ceiling — permitted context (Writing Style, enabled sections, permitted memories/notes from 36-03) is preserved, not auto-omitted to fit a fixed product ceiling. A per-single-value abuse bound (`PER_VALUE_LIMIT`, disclosed) may remain.
    - context-estimate (node-pure, provider-aware) estimates the request's token size against the SELECTED MODEL's REAL context window (OpenRouter: catalog `context_length`; direct providers: the model's known window) and, for OpenRouter, input-cost = estimatedInputTokens × Number(pricing.prompt); Direct/Custom return "Cost estimate unavailable for this connection."
    - True over-capacity (estimate > the model's real context window) is surfaced as an EXPLICIT overflow warning (TruncationNotice / the UI-SPEC "Context overflow" copy; matching 36-07's "context" failure category) that requires deliberate user resolution — it NEVER silently trims or auto-omits permitted context.
  </behavior>
  <action>Write RED tests first, then implement. In `resolvePrompt`, render the structured Writing Style and each ENABLED personalization section as its own DATA-delimited, `sanitizeValue`-fenced, `PER_VALUE_LIMIT`-bounded block appended conditionally (absent/disabled = byte-identical scaffold); reconcile the legacy `USER STYLE NOTE` block to the new structured Writing Style (do not leave two personalization mechanisms). **Retire the fixed `TOTAL_LIMIT` field-omission governor (`:298-318`) as the context ceiling (AICFG-07, review HIGH #5): do NOT auto-omit whole permitted items to fit 6,000 code points** — this is the same behavior 36-03 states for its memory/note blocks; the two plans MUST agree. (Decide the disposition of the degenerate hard-trim safety net `:324-333`: keep it ONLY as a last-resort disclosed abuse guard clearly distinct from a product ceiling, or remove it — it must never silently drop permitted context.) Keep the system/output contract immutable and the sole-builder + byte-identity contract. Build `context-estimate.ts` (node-pure, token-budget-style switch): estimate the request's token size and compare against the SELECTED MODEL's real context window (`context_length` from the catalog for OpenRouter; the model's known window for direct providers) — this real capacity, NOT `TOTAL_LIMIT`, is the sole generation blocker; for OpenRouter add input-cost from `openrouter-catalog` pricing; Direct/Custom → the unavailable-cost string. Surface true over-capacity as an explicit `TruncationNotice`/warning requiring deliberate resolution (no artificial ceiling, no silent truncation/auto-omission). The screen debounces recompute; the estimator itself does no network per call (pricing + context_length are passed in / read from cache).</action>
  <verify>
    <automated>npx vitest run src/ai/prompt-template.test.ts src/ai/context-estimate.test.ts</automated>
  </verify>
  <done>Writing Style + enabled sections render byte-identically; the fixed-ceiling auto-omission is retired; the estimate measures against the model's real context window with OpenRouter input-cost; over-capacity is an explicit block; no silent truncation and no second personalization mechanism.</done>
  <acceptance_criteria>
    - A resolver test: a disabled section produces a byte-identical scaffold to its absence; an enabled one adds exactly its block.
    - Byte-identity: prompt === inspectorDisplay === payload after Writing Style + sections.
    - A large-but-under-model-capacity assembly PRESERVES all permitted context (no auto-omission to the retired 6,000 ceiling) — test-asserted.
    - Over-capacity test (estimate > the selected model's real context window) yields an explicit overflow TruncationNotice/warning and does NOT drop/trim context silently.
    - `context-estimate` compares against the model's real `context_length` (not `TOTAL_LIMIT`); OpenRouter cost = Number(pricing.prompt) × tokens; Direct/Custom returns the unavailable-cost string.
    - Only one personalization mechanism remains (legacy raw style-note reconciled).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: AIPersonalizationScreen — Writing Style controls + sections + live estimate</name>
  <files>src/screens/AIPersonalizationScreen.tsx, src/screens/ai-personalization-logic.ts, src/screens/ai-personalization-logic.test.ts</files>
  <read_first>36-UI-SPEC.md Surface #7/#8/#9 + Copywriting (empty states, estimate/cost/overflow copy), src/components/ (SegmentedControl, GlassSurface, AccordionSection, Snackbar), src/db/personalization-dao.ts + src/ai/context-estimate.ts (this plan), expo-document-picker usage</read_first>
  <action>Build `AIPersonalizationScreen.tsx`: Writing Style via `SegmentedControl` rows (Tone/Length/Directness) each with a Use-custom-guidance opt-out + a freeform multi-line field; an ordered list of Personalization Context section cards (title, body, enable toggle on accent track, drag reorder handle at 44px, overflow menu rename/edit/delete/replace) with Add-via-paste and Import-.txt/.md (expo-document-picker → read text → personalization-dao import). **Accessible reorder (review MEDIUM):** a drag handle with only an `accessibilityLabel` is NOT operable without precise dragging — provide operable **Move up / Move down** controls (accessibility actions on the card and/or visible controls, or an accessible reorder mode) so a screen-reader / non-drag user can reorder; the reorder handle + overflow menu still each declare an `accessibilityLabel`. Show the token/context estimate `caption` (debounced recompute; stale value stays during recompute) and, for OpenRouter, the input-cost line; Direct/Custom show the unavailable-cost string; render the explicit overflow warning when over capacity. Empty states: "No personalization yet." with Add/paste next step; Writing Style always shows its defaults. Decision logic (debounce trigger, estimate formatting, overflow detection, move-up/move-down order mutation) in `ai-personalization-logic.ts` (node-pure, tested). All colours via theme tokens; no per-keystroke network.</action>
  <verify>
    <automated>npx vitest run src/screens/ai-personalization-logic.test.ts && npm run check:colors</automated>
  </verify>
  <done>Writing Style + sections + import + a debounced live estimate/cost + explicit overflow render with documented empty states; no per-keystroke network.</done>
  <acceptance_criteria>
    - `npm run check:colors` + `npx tsc --noEmit` clean.
    - Logic test: debounce coalesces rapid edits into a single recompute; overflow detection flips at capacity.
    - Import path reads the file at the screen layer and calls the personalization-dao import (no live link).
    - Reorder handle + overflow menu declare accessibilityLabel AND operable Move up / Move down controls (or an accessible reorder mode) exist — a logic test asserts move-up/move-down produces the correct deterministic display_order (review MEDIUM: drag is not the only reorder path).
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- **New files:** `src/db/personalization-dao.ts`, `src/ai/context-estimate.ts`, `src/screens/AIPersonalizationScreen.tsx` + `ai-personalization-logic.ts`.
- **Modified:** `src/ai/prompt-template.ts` (renders structured Writing Style + enabled sections; legacy style-note reconciled).
- **Consumes:** migration-029 Writing Style columns + `personalization_sections` table (Plan 01); openrouter-catalog pricing (Plan 02).
- **Behavior:** structured Writing Style, ordered enableable sections with paste/import, debounced token/context + OpenRouter input-cost estimate, explicit overflow (no silent truncation).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user personalization → prompt | enabled section text becomes AI egress; disabled must not |
| imported file → local record | imported text is copied, not live-linked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-21 | Information disclosure | disabled section leaking to prompt | high | mitigate | only enabled sections render; a disabled-section resolver test asserts absence |
| T-36-22 | Tampering | personalization overriding system contract | high | mitigate | system/output contract immutable; personalization is subordinate DATA-fenced content (prompt-injection canon — breadcrumb `/gsd-secure-phase`) |
| T-36-23 | Information disclosure | per-keystroke network / price fetch | low | mitigate | recompute is local + debounced; pricing reused from cache per refresh policy |
| T-36-24 | Availability | silent truncation losing user context | medium | mitigate | explicit overflow warning; no arbitrary ceiling; test asserts no silent trim |
</threat_model>

<verification>
- `npx vitest run src/db/personalization-dao.test.ts src/ai/context-estimate.test.ts src/ai/prompt-template.test.ts src/screens/ai-personalization-logic.test.ts` green; `npm run check:colors` + `npx tsc --noEmit` clean.
- Device UAT (Pixel, end-of-phase): paste + import a section, toggle enable, watch the debounced estimate; overflow warning on an oversized context.
</verification>

<success_criteria>
- Structured Writing Style + ordered enableable sections (paste/import) layered on an immutable system prompt.
- Live token/context + OpenRouter input-cost estimate; explicit overflow, never silent truncation.
</success_criteria>

<output>
Create `.planning/phases/36-ai-configuration-prompting/36-06-SUMMARY.md` when done.
</output>
