---
phase: 36-ai-configuration-prompting
plan: 08
type: execute
wave: 5
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05, 36-06, 36-07]
files_modified:
  - src/backup/types.ts
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/export-manifest.ts
  - src/backup/export-manifest.test.ts
  - src/backup/reconciliation.ts
  - src/backup/reconciliation.test.ts
  - src/backup/restore-apply.ts
  - src/backup/restore-apply.test.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  # D-14: Profile-presentation background image bytes ride in v5 via the EXISTING background
  # subsystem — NOT the restore_photo_journal. Migration 008's shipped CHECK
  # (target_kind IN 'contact','profile','customField') forbids a 'background' kind, and a
  # SQLite CHECK cannot be ALTERed — a table-rebuild migration would reverse 36-01's
  # owner-accepted single-migration-029 decision (D-03). Crash-safety mirrors the AVATAR
  # pattern (restore-apply.ts :298 stage-before-txn / :330-331 persist-after-commit): decoded
  # background bytes are STAGED into a background restore-pending namespace BEFORE the write
  # transaction, the profile_background_templates row commits INSIDE the txn, and the durable
  # canonical file is persisted to profile-backgrounds/<uid>.jpg AFTER commit — so canonical
  # is never mutated before the row that references it commits. The retained restore-pending
  # file (keyed by the template's portable uid — NOT a restore_photo_journal row) is the
  # durable evidence that lets the launch sweep re-drive an interrupted post-commit persist;
  # this is why backgrounds need no journal (canonical path is uid-derivable, unlike avatars
  # whose canonical path needs the committed rowid). Reuse background-storage.ts
  # (SAFE_BACKGROUND_RELATIVE, backgroundDerivativeRelPath, persistBackgroundDerivative) +
  # add a base64→restore-pending stage helper and a restore-pending resolver/lister there,
  # and EXTEND background-reconcile-sweep.ts to re-drive (not just log) pending background
  # persists on launch. No second profile-backgrounds/ guard and no second persist: the
  # avatars guards (photo-relative-path.ts / photo-storage.ts) are NOT touched, and the
  # background guard already exists in background-storage.ts.
  - src/services/photos/background-storage.ts
  - src/services/photos/background-storage.test.ts
  - src/services/photos/background-reconcile-sweep.ts
  - src/services/photos/background-reconcile-sweep.test.ts
  - src/services/backup/share-export.ts
  # D-14: retire the "device-local pending" Profile-presentation boundary note now that
  # presentation + background bytes are v5 entities.
  - docs/systems/profile.md
  - docs/systems/backup-restore.md
  # NOTE (D-14): src/db/migrations/profile-presentation.ts, profile-presentation-dao.ts, and
  # profile-presentation-read.ts are the AUTHORITATIVE column/FK shapes for this work but are
  # READ-ONLY here — the backup layer does its own direct SQL and does NOT modify them, so
  # they live in <context>/<read_first>, not files_modified (accuracy per CLAUDE.md).
autonomous: false
requirements: [AICFG-16, AICFG-17]
user_setup: []

# SIZING NOTE (review HIGH #3 + HIGH-C / D-14): this is the milestone-CLOSING backup plan
# and owns the full export↔restore roundtrip, not just export. It is deliberately kept as
# ONE plan (D-03b + D-14: the wire-format bump is this phase's single FINAL plan;
# irreversible-format coherence — the executor must see the whole roundtrip before the
# one-way checkpoint, and the export↔restore↔bump of an irreversible format must NOT be
# split across plans). The restore side adds ~9 previously-unbacked entity tables PLUS the
# two Profile-presentation tables (D-14) to reconciliation, AND rehydrates background-image
# bytes through the EXISTING background-storage subsystem (a base64 persist helper, no new
# migration or journal change), so the estimate is well above the smart-zone. Kept as ONE plan
# per the orchestrator's format-coherence directive. A second blocking checkpoint was
# CONSIDERED and REJECTED: there is still exactly one irreversible decision (the bump), and
# presentation + background-byte completeness is folded into the existing single pre-bump
# gate rather than fragmenting one decision into two human gates. The blocking checkpoint
# sits AFTER export+restore (incl. presentation + background bytes) are built and BEFORE the
# irreversible bump, giving a natural mid-plan pause point.
estimate:
  tokens: 200000
  raw_tokens: 100000
  tasks: 7
  confidence: low

must_haves:
  truths:
    - "The backup wire format bumps to v5 (BACKUP_FORMAT_VERSION = 5 — v4 already landed early in 24.1) with a `4:` FORWARD_MIGRATIONS upgrader that raises an older manifest to 5 and injects the new entity arrays (AICFG-16)."
    - "The v5 backup serializes the WHOLE milestone preference inventory now that emission is Phase 36's boundary: theme package/accent/background, dashboard population/view/sort + right-swipe, orrery density/satellites/last-active System, profile layout+background template uids, history lens+cycle, channel default+remembered, compose mode+remembered, plus this phase's AI Enabled + permission type-defaults + per-interaction Allow-AI default + Writing Style (AICFG-16)."
    - "The v5 backup serializes the still-missing wire entities: orrery Systems tables (systems/system_rules/system_overrides/system_prefs), profile template entities (profile_layout_templates/profile_background_templates), AI connections (ai_connections) and personalization (personalization_sections), plus the interactions columns omitted today (duration, allow_ai, group_event_id) and the group_events table incl group_note (AICFG-16)."
    - "Credentials are excluded from the backup without exception: no API key / OAuth key / custom-endpoint secret is emitted; SECRET_SHAPED_KEY screening still rejects any key-shaped portable key; new portable keys are named plainly (aiEnabled, openrouterModel — never *Key/*Token/*Credential) (AICFG-15/16)."
    - "The RESTORE side is owned, not just export (review HIGH #3): `restore-apply.ts` + `reconciliation.ts` ingest every entity the v5 export now serializes — orrery Systems (systems/system_rules/system_overrides/system_prefs), profile templates (profile_layout_templates/profile_background_templates), ai_connections, personalization_sections, group_events — plus the interaction columns the restore INSERT omits today (duration, allow_ai, group_event_id). The claimed export→import roundtrip actually holds; a serialized entity with no restore writer is a format-completeness DEFECT in an irreversible plan and is not allowed (AICFG-16)."
    - "The interactions restore INSERT (`restore-apply.ts:201`) is extended to carry `duration`, `allow_ai`, and `group_event_id` (it currently omits them and forces `allow_ai=0`); the fail-closed `allow_ai=0` behavior for legacy/older manifests that DON'T carry the column is preserved, but a v5 manifest that DOES carry `allow_ai` restores its real value (AICFG-16)."
    - "The group_event tombstone exclusion is reconciled (review MEDIUM): now that group_events is a serialized+restored entity, the `export-manifest.ts:207-208` `.filter(row => row.entity_type !== 'group_event')` guard is removed so a deleted group_event's tombstone is carried and a restore propagates the deletion; a deleted-group_event roundtrip test proves it (AICFG-16)."
    - "The FK-bearing new entities carry PORTABLE UID aliases, not raw source rowids (review MEDIUM): `system_rules.system_id` (INTEGER FK → `systems.id`) is projected as `systemUid` (from `systems.uid`); `system_overrides.contact_id` (INTEGER FK → `contacts.id`) is projected as `contactUid`; `interactions.group_event_id` (INTEGER FK → `group_events.id`) is projected as `groupEventUid`. On restore each is REBUILT into a local integer id by looking the UID up in the destination DB — a source rowid is NEVER written into a destination FK column. (`system_overrides.system_ref` and `system_prefs.system_ref` are already portable TEXT and need no remap.) (AICFG-16)."
    - "The roundtrip tests restore into a DIFFERENT-rowid destination (review MEDIUM): the restore-side tests seed the destination DB so its `systems`/`contacts`/`group_events` rows have DIFFERENT integer ids than the source, and assert the restored `system_rules`/`system_overrides`/`interactions` FKs resolve to the CORRECT destination rows by UID — a same-fixture roundtrip would hide a wrong integer-FK remap in the irreversible v5 format (AICFG-16)."
    # --- D-14 Profile presentation + background bytes (review HIGH-C) ---
    - "The v5 backup serializes AND restores the two Profile-presentation entities the plan previously omitted: `profile_contact_presentation` (per-contact `layout_template_uid` OR `freeform_layout_json` [mutually exclusive], `background_template_uid`, `collapse_json`) and `profile_category_presentation` (per-category `layout_template_uid`, `background_template_uid`) — columns verified against src/db/migrations/profile-presentation.ts, not memory (AICFG-16/17, D-14)."
    - "Neither presentation table has its OWN `uid`: each is 1:1 with its parent (PRIMARY KEY is `contact_id`/`category_id`), so the portable reconciliation identity IS the parent uid — `profile_contact_presentation` keys on `contactUid`, `profile_category_presentation` on `categoryUid`, and the restore upsert is `ON CONFLICT(contact_id)`/`ON CONFLICT(category_id)` with the integer PK rebuilt from that parent uid against the DESTINATION DB, never a source rowid (D-14)."
    - "The presentation `layout_template_uid`/`background_template_uid` columns are ALREADY portable TEXT UIDs (they reference `profile_layout_templates.uid`/`profile_background_templates.uid`, NOT integer ids) — emitted and restored VERBATIM with NO integer-FK remap; the ONLY integer FK each presentation row carries is its parent PK (D-14)."
    - "Profile background image BYTES ride in the backup, not just `image_path`: each `profile_background_templates` row stages its file bytes as base64 (the contact-photo `photoBase64` idiom) and restore rehydrates them to the app-owned `profile-backgrounds/<uid>.jpg` canonical path (derived from the template's portable uid) via the EXISTING background subsystem (`persistBackgroundDerivative`/`backgroundDerivativeRelPath` in background-storage.ts) — NOT through `restore_photo_journal` (its migration-008 CHECK forbids a `background` target_kind) and with NO new migration, honoring 36-01's single-migration-029 decision (D-03). A restored install shows the real background, never a row pointing at a nonexistent file (D-14, AICFG-16)."
    - "Background-byte restore is CRASH-CONSISTENT on the irreversible v5 surface, mirroring the avatar stage-before-txn / persist-after-commit invariant (restore-apply.ts :298/:330-331): decoded bytes are STAGED into a background restore-pending namespace BEFORE the write transaction, the `profile_background_templates` row commits INSIDE the txn, and the durable `profile-backgrounds/<uid>.jpg` file is persisted AFTER commit — canonical is never mutated before its referencing row commits. The retained restore-pending file (keyed by the template's portable uid, NOT a `restore_photo_journal` row) is durable re-drive evidence: the EXTENDED `background-reconcile-sweep` launch sweep re-drives an interrupted post-commit persist (it no longer merely logs a missing file) and prunes a staged file whose row rolled back. All four crash windows hold — (1) crash before SQL commit → row rolled back, no orphan reference and canonical untouched; (2) crash after commit before FS persist → the retained staged bytes are re-driven to canonical by the sweep; (3) crash mid-swap (after old→bak before tmp→canonical) → the existing `.tmp`/`.bak` sidecar reconcile lands canonical; (4) rollback after a staged replacement → canonical is preserved (never pre-mutated) and the staged artifact is pruned. No new migration, no `restore_photo_journal` `background` kind (D-03/36-01 preserved) (D-14, AICFG-16)."
    - "A DIFFERENT-rowid destination roundtrip proves the presentation FK remap: the destination `contacts`/`categories` are seeded with integer ids that DIFFER from the source, and restored `profile_contact_presentation`/`profile_category_presentation` resolve their `contact_id`/`category_id` to the correct destination rows by uid — a restored install loses NO per-contact/per-category assignment, freeform layout, collapse state, or background image (AICFG-16/17 no-silent-data-loss, D-14)."
    - "At execution the recorded 'device-local pending' Profile-presentation backup boundary is retired: docs/systems/profile.md (:78-82) and docs/systems/backup-restore.md (:123 gotcha 15) are updated to record that presentation entities + background bytes are now v5 entities (D-14 RESOLVES the deferral; it does not reverse it)."
    - "Restore validates and orphan-repairs (carrying Phase 33's Group Event rules for group_events + interactions.group_event_id) and a restored install with no valid local credential never falsely appears Ready — it routes to Needs Attention (AICFG-16)."
    - "Every retired portable key (includeUnboundNeverContacted, any Manage-favourites key) has a decided restore-compat behavior — not a silent drop (AICFG-17)."
    # --- AICFG-16/17 edge-probe derived (explicit) ---
    - "An empty database exports a valid v5 manifest (empty entity arrays, present-but-empty preference set) and re-imports to an equivalent empty state (AICFG-16/17 empty)."
    - "Entity emission is deterministic and stable-ordered (every SELECT `ORDER BY uid`) so two exports of the same DB are byte-stable and equal-keyed rows never reshuffle (AICFG-16/17 ordering)."
    - "Restoring a v4 (or older) manifest upgrades it through the FORWARD_MIGRATIONS chain to v5 exactly once, and a v5 manifest is not re-upgraded; adjacent format versions do not collide or double-apply (AICFG-16/17 adjacency)."
    # --- backstop ---
    - { statement: "A restore that encounters an orphaned group_event member interaction (group_event_id referencing a missing group_event) repairs per Phase 33 rules without dropping the interaction's own contact data — roundtrip fixture check.", verification: backstop }
  prohibitions:
    - statement: "MUST NOT emit any credential (API key, OAuth key, custom-endpoint secret) into the backup manifest, and MUST NOT name a new portable key with a secret-shaped substring that SECRET_SHAPED_KEY would reject."
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT mark a restored install AI-Ready without a valid local credential present in SecureStore."
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT silently drop a retired portable key on restore — each retired key has an explicit decided restore-compat behavior."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT re-bump BACKUP_FORMAT_VERSION to 4 (a no-op that ships an incomplete format) — the target is 5, and a second irreversible bump cannot be given retroactively."
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT restore a `profile_background_templates` row whose `image_path` points at a file that was never rehydrated (a broken background) — the bytes travel in the manifest, are STAGED to a background restore-pending namespace before the write txn, and are persisted to the canonical `profile-backgrounds/<uid>.jpg` path via `persistBackgroundDerivative` (background-storage.ts) AFTER commit; the retained restore-pending file (keyed by uid) is durable re-drive evidence so an interrupted post-commit persist is re-driven by the EXTENDED `background-reconcile-sweep` launch sweep (which re-drives, not merely logs, a missing file), NOT the `restore_photo_journal`. Canonical is never mutated before its referencing row commits, so a transaction rollback cannot orphan a committed reference or clobber the prior background (D-14)."
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT write a source rowid into a restored presentation row's `contact_id`/`category_id`, and MUST NOT invent an integer-FK remap for the already-portable `layout_template_uid`/`background_template_uid` columns (D-14)."
      category: safety
      status: resolved
      verification: test
  artifacts:
    - "src/backup/types.ts (BACKUP_FORMAT_VERSION = 5)"
    - "src/backup/backup-schema.ts (FORWARD_MIGRATIONS 4:, PORTABLE_SETTINGS_KEYS additions, SECRET screen)"
    - "src/backup/export-manifest.ts (new entity SELECTs + interactions gap columns; profile_contact_presentation + profile_category_presentation SELECTs keyed on parent uid; profile_background_templates image bytes staged as base64)"
    - "src/backup/restore-apply.ts (presentation ingest ON CONFLICT(contact_id)/(category_id) with PK rebuilt from parent uid; background byte rehydration)"
    - "src/services/photos/background-storage.ts (base64→restore-pending stage helper + restore-pending resolver/lister, reusing the module's crash-safe .bak swap for the post-commit canonical persist) + src/services/photos/background-reconcile-sweep.ts (EXTENDED to re-drive interrupted post-commit background persists from the retained restore-pending file and prune rolled-back staged artifacts — not merely log a missing file) + src/services/backup/share-export.ts (background-scoped export reader); the profile-backgrounds/ guard ALREADY exists (background-storage.ts SAFE_BACKGROUND_RELATIVE) and is reused, not duplicated — no restore_photo_journal 'background' kind and no new migration (D-03/36-01)"
    - "src/db/app-settings-dao.ts (getPortableSettingsSnapshot emits all portable keys)"
    - "docs/systems/profile.md + docs/systems/backup-restore.md (presentation + background bytes recorded as v5 entities — device-local-pending boundary retired)"
  key_links:
    - "getPortableSettingsSnapshot SELECT/return emits every declare-only key (previously accept-only) + this phase's AI keys"
    - "export-manifest serializes systems/system_* + profile_*_templates + ai_connections + personalization_sections + group_events + interactions(duration,allow_ai,group_event_id); integer FKs are projected as portable UIDs (system_rules.system_id→systemUid, system_overrides.contact_id→contactUid, interactions.group_event_id→groupEventUid); the group_event tombstone filter (:207-208) is removed"
    - "restore-apply.ts + reconciliation.ts INGEST every newly-serialized entity (reconciliation `MergeableEntityType` union + policy registry extended with a `parentFields` UID-remap per FK; restore-apply `entities[]`/`tableOf`/`tombstoneEntity`/`incomingRows`/`localRows`/upsert switch extended) rebuilding each integer FK from its UID against the destination DB, and the interactions INSERT gains duration/allow_ai/group_event_id — the roundtrip is real and tested against a different-rowid destination (review HIGH #3 + MEDIUM)"
    - "FORWARD_MIGRATIONS `4:` upgrader mirrors the existing `3:` entry; restore validation + orphan-repair carry Phase 33 Group Event rules"
    - "this plan runs LAST (wave 5) after every schema-bearing plan; verify BACKUP_FORMAT_VERSION and head+1 on disk at plan-execution time"
    - "export-manifest serializes profile_contact_presentation (portable key contactUid) + profile_category_presentation (portable key categoryUid); template-uid columns emitted verbatim (already portable); profile_background_templates rows carry base64 image bytes (D-14, review HIGH-C)"
    - "restore-apply ingests both presentation tables ON CONFLICT(contact_id)/(category_id) rebuilding the PK from contactUid/categoryUid against the destination DB (reconciliation.ts readParentUid gains a category_id→categoryUid alias), and rehydrates background bytes to profile-backgrounds/<uid>.jpg through the existing background-storage subsystem: STAGE decoded bytes to a background restore-pending namespace before the write txn, then persist to canonical via persistBackgroundDerivative AFTER commit, with the EXTENDED background-reconcile-sweep re-driving an interrupted persist from the retained restore-pending file (keyed by uid) — no restore_photo_journal 'background' kind, no new migration (D-14, D-03)"
    - "FORWARD_MIGRATIONS 4: upgrader injects the two presentation arrays (empty) so an older manifest upgrades cleanly; docs/systems/profile.md + backup-restore.md updated to retire the device-local-pending boundary (D-14)"
---

<objective>
Close the milestone's schema chain: bump the backup wire format to v5, serialize every entity and portable
preference the milestone added (including the interaction/group-event/orrery-systems/template/AI/
personalization gaps verified missing on disk), carry Phase 33's restore-validation + orphan-repair rules,
give every retired key a decided restore-compat behavior, and keep all credentials out — with a restored
install that never falsely appears Ready.

Purpose: This is the FINAL plan and the one irreversible-in-production wire-format change of the phase; it
must be complete because a second bump cannot be given to users retroactively. It runs after all other
Phase-36 schema.
Output: BACKUP_FORMAT_VERSION = 5, the `4:` upgrader, full emission + entity serialization (including the two
Profile-presentation entities and background image bytes per D-14), and a roundtrip test asserting version 5
and full coverage.

D-14 (owner, cycle-3 convergence): Profile presentation IS in the v5 backup. This plan serializes AND
restores `profile_contact_presentation` + `profile_category_presentation` (per-contact/per-category template
+ background assignments, `freeform_layout_json`, `collapse_json`) and stages the background image BYTES (not
just `image_path`), closing cross-AI review finding HIGH-C and discharging the deferral recorded at
docs/systems/profile.md:82 / backup-restore.md:123. This RESOLVES that deferral — it does not reopen it.

**Decisions honored (CONTEXT.md):** D-02 (planning-notes R-09/R-16 — Phase 36 owns the bump, sequenced last; whole preference inventory portable), D-03 (schema head+1 verified on disk; app_settings not AsyncStorage), D-03b (backup wire-format bump is this phase's FINAL plan after all other schema; retired keys get a decided restore-compat behavior; credentials excluded), D-04 (SECRET_SHAPED_KEY screen preserved; restored install never falsely Ready), D-12 (whole milestone preference inventory + all new entities serialized), D-14 (Profile presentation entities + background image bytes serialized AND restored in v5; docs boundary note retired at execution). NOTE: CONTEXT D-03/D-03b/D-12 say "v4" — that text is STALE (v4 already shipped in 24.1); the on-disk closing target is v5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-ai-configuration-prompting/36-RESEARCH.md
@.planning/phases/36-ai-configuration-prompting/36-PATTERNS.md
@docs/dossier/milestone-2/planning-notes/phase-16-planning-notes.md

# Analog / source-of-truth files (read before editing)
@src/backup/types.ts
@src/backup/backup-schema.ts
@src/backup/export-manifest.ts
@src/backup/reconciliation.ts
@src/backup/restore-apply.ts
@src/db/app-settings-dao.ts
@src/db/migrations/025-interaction-history-schema.ts
@src/db/migrations/026-group-events-schema.ts

# D-14 Profile-presentation source of truth (READ-ONLY references — NOT modified by this plan;
# open them for the REAL column names + FK shapes, per CLAUDE.md "review the code, not the diff")
@src/db/migrations/profile-presentation.ts
@src/db/profile-presentation-dao.ts
@src/db/profile-presentation-read.ts
# D-14 background-bytes: the EXISTING background subsystem the restore path reuses (migration-free)
@src/services/photos/background-storage.ts
@src/services/photos/background-reconcile-sweep.ts
@src/services/backup/share-export.ts
@src/db/migrations/008-restore-photo-journal.ts
# (photo-storage.ts / photo-relative-path.ts remain the AVATARS-only guards — NOT touched by
# this plan; the background bytes never route through them)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit the whole milestone preference inventory in getPortableSettingsSnapshot</name>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <read_first>src/db/app-settings-dao.ts (:406-490 the declare-only columns with "DEFERRED to Phase 36 emission" comments; :840-899 getPortableSettingsSnapshot SELECT + Pick + return mapping), src/backup/backup-schema.ts (:137-215 PORTABLE_SETTINGS_KEYS incl the Phase-36-scope markers; :217-243 SECRET_SHAPED_KEY + assertPortableSettings), 36-RESEARCH.md On-Disk #5 (the inventory table) + Pitfall 7 (secret-shaped key names)</read_first>
  <action>Move every declare-only portable key from accept-for-restore-only to EMITTED: add the snake_case column to the `getPortableSettingsSnapshot` SELECT + the `Pick<AppSettingsRow,…>`, and the camelCase mapping in the return — for theme (package/galaxyMode/standardMode/accents/backgrounds), dashboard (viewMode/populations/filters/sort/rightSwipeAction), orrery (density/satellitesEnabled/lastSystem), profile templates (layout/background uid), history (lens/cycleCount), channel (default/remembered — camelCase manifest keys not snake_case), compose (default/remembered message mode), plus this phase's new keys (aiEnabled, active connection, writing-style, permission type-defaults, per-interaction allow-ai default). Ensure every new portable key is in `PORTABLE_SETTINGS_KEYS` and named plainly — no `*Key`/`*Token`/`*Credential` substring (SECRET_SHAPED_KEY would reject it). Do NOT emit any credential or the OAuth key. Extend the app-settings-dao test to assert the snapshot now emits each key with a correct value; extend the backup-schema test to assert `assertPortableSettings` accepts the new keys and still rejects a secret-shaped key.</action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts src/backup/backup-schema.test.ts</automated>
  </verify>
  <done>getPortableSettingsSnapshot emits the full portable inventory + the new AI keys; all are allowlisted and plainly named; secret-shaped keys still rejected.</done>
  <acceptance_criteria>
    - A snapshot test asserts each previously-declare-only key is now present with its value.
    - Every new portable key is in `PORTABLE_SETTINGS_KEYS` and passes SECRET_SHAPED_KEY (no secret-shaped substring).
    - No credential/OAuth key appears in the snapshot (grep + test).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Serialize the missing wire entities + interaction/group-event gap columns + Profile presentation tables (D-14)</name>
  <files>src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/backup-schema.ts</files>
  <read_first>src/backup/export-manifest.ts (:72-146 the entity SELECT array; :110 interactions SELECT missing duration/allow_ai/group_event_id; :208 group_event tombstone filter), src/backup/backup-schema.ts (:182-189 "Phase 36 wire entities" markers for Systems + templates), src/db/migrations/025-interaction-history-schema.ts (duration + allow_ai columns), src/db/migrations/026-group-events-schema.ts (group_events table incl group_note + interactions.group_event_id + UNIQUE INDEX), src/db/migrations/profile-presentation.ts (:40 profile_category_presentation, :51 profile_contact_presentation — the REAL columns: contact_id/category_id INTEGER PK, layout_template_uid/background_template_uid TEXT→template.uid [ALREADY portable], freeform_layout_json, collapse_json; note NEITHER table has its own `uid` column), src/db/profile-presentation-dao.ts + profile-presentation-read.ts (live writers/readers), 36-RESEARCH.md On-Disk #5 + Assumptions A6 (verify events/duration/group-event coverage on disk at execution)</read_first>
  <action>Add the missing entity SELECTs to `export-manifest.ts` (each `ORDER BY uid` for deterministic emission): orrery Systems tables (systems, system_rules, system_overrides, system_prefs), profile template entities (profile_layout_templates, profile_background_templates), AI connections (ai_connections — remembered_model/endpoint/custom_model/lane, NO credential), and personalization (personalization_sections). **Project every integer FK as a PORTABLE UID, never a raw source rowid (review MEDIUM):** `system_rules.system_id` (INTEGER FK → `systems.id`) is emitted as `systemUid` via a JOIN to `systems.uid`; `system_overrides.contact_id` (INTEGER FK → `contacts.id`) is emitted as `contactUid` via a JOIN to `contacts.uid`; and the extended interactions SELECT emits `group_event_id` (INTEGER FK → `group_events.id`) as `groupEventUid` via a JOIN to `group_events.uid`. (`system_overrides.system_ref` and `system_prefs.system_ref` are already portable TEXT — emit as-is, no join.) Extend the interactions SELECT to include the columns omitted today — `duration`, `allow_ai`, and `groupEventUid` (the portable form of `group_event_id`) — and add a group_events SELECT that serializes the group-event shared record INCLUDING `group_note` (group_note is user data preserved in backup; it is separately banned from AI egress — do not conflate). **Reconcile the group_event tombstone exclusion (review MEDIUM):** `export-manifest.ts:207-208` currently drops `group_event` tombstones with `.filter(row => row.entity_type !== "group_event")` behind a "Phase 36 replaces this guard" comment. Now that group_events is a serialized+restored entity, REMOVE that filter so a deleted group_event's tombstone is carried in the manifest (otherwise a restore cannot propagate a group-event deletion — an incomplete restore semantic baked into an irreversible format). Add an explicit deleted-group_event roundtrip test (export with a group_event tombstone → restore drops/repairs it). Verify on disk at execution that `events`/duration/group-event coverage is otherwise complete (RESEARCH A6). **Add the two Profile-presentation entities (D-14, review HIGH-C):** add SELECTs for `profile_contact_presentation` and `profile_category_presentation`. CRITICAL — verify the columns against src/db/migrations/profile-presentation.ts, NOT from memory: `profile_contact_presentation(contact_id PK, layout_template_uid, freeform_layout_json, background_template_uid, collapse_json, created_at, modified_at)` and `profile_category_presentation(category_id PK, layout_template_uid, background_template_uid, created_at, modified_at)`. Neither table has its OWN `uid` — each is 1:1 with its parent, so project the PARENT's uid as the row's portable identity: `SELECT c.uid AS contactUid, p.layout_template_uid AS layoutTemplateUid, p.freeform_layout_json AS freeformLayoutJson, p.background_template_uid AS backgroundTemplateUid, p.collapse_json AS collapseJson, p.created_at AS createdAt, p.modified_at AS modifiedAt FROM profile_contact_presentation p JOIN contacts c ON c.id=p.contact_id ORDER BY c.uid` and the analogous `category.uid AS categoryUid` JOIN for `profile_category_presentation` (`ORDER BY category.uid`). The `layout_template_uid`/`background_template_uid` columns are ALREADY portable TEXT UIDs (they reference the template `uid`, not an integer id) — emit them VERBATIM, do NOT add a JOIN/remap for them; the ONLY integer FK to project is the parent PK (contact_id→contactUid, category_id→categoryUid). (Background image BYTES for `profile_background_templates` are Task 4, not here.) Add the corresponding manifest types/validation in `backup-schema.ts` (including the two presentation arrays). Extend the export-manifest test to assert every new entity + the three interaction gap columns + the group_event tombstone roundtrip + both presentation entities (template-uid assignments, freeform layout, and collapse state present, ordered by parent uid).</action>
  <verify>
    <automated>npx vitest run src/backup/export-manifest.test.ts</automated>
  </verify>
  <done>Orrery Systems, profile templates, AI connections, personalization, group_events (with group_note), interactions duration/allow_ai/group_event_id, and both Profile-presentation tables (keyed on parent uid, template-uid assignments verbatim) are all serialized deterministically; the group_event tombstone filter is removed; no credential emitted.</done>
  <acceptance_criteria>
    - The interactions SELECT includes `duration`, `allow_ai`, and `groupEventUid` (the portable UID form of the `group_event_id` INTEGER FK); a roundtrip test proves they survive export→import.
    - Integer FKs are emitted as portable UIDs, never raw rowids: `system_rules.system_id`→`systemUid`, `system_overrides.contact_id`→`contactUid`, `interactions.group_event_id`→`groupEventUid` (grep/test-asserted no raw source rowid appears in a relationship field).
    - `ai_connections` is serialized without any credential column (only lane/model/endpoint metadata).
    - group_events incl `group_note` roundtrips; systems/system_* and profile_*_templates roundtrip.
    - The `group_event` tombstone filter at `export-manifest.ts:207-208` is removed; a deleted-group_event roundtrip test asserts the tombstone is carried and applied on restore.
    - `profile_contact_presentation` (keyed `contactUid`) and `profile_category_presentation` (keyed `categoryUid`) are serialized; `freeform_layout_json`, `collapse_json`, and both template-uid assignments survive export (columns verified against profile-presentation.ts).
    - The presentation `layout_template_uid`/`background_template_uid` are emitted verbatim as portable TEXT UIDs with NO integer-FK remap; the only projected integer FK is the parent PK (contact_id→contactUid, category_id→categoryUid).
    - Every new SELECT is deterministically ordered — `ORDER BY uid` for own-uid entities, `ORDER BY` the parent uid (c.uid/category.uid) for the two presentation tables.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Own the RESTORE side — ingest every new entity + interaction gap columns + Profile presentation + orphan repair (review HIGH #3 + HIGH-C/D-14)</name>
  <files>src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/backup-schema.ts</files>
  <read_first>src/backup/restore-apply.ts (:50 the `entities[]` array — legacy set only; :51 `tableOf`; :52 `tombstoneEntity`; :54-70 `incomingRows`/`localRows` projections; :180-213 `upsertChildren` incl the interactions INSERT at :201 that omits duration/allow_ai/group_event_id and force-sets allow_ai=0), src/backup/reconciliation.ts (:8-24 `MergeableEntityType` union; :34-45 `EntityPolicy` registry `writeMode` lww/insert-if-missing + parentFields), src/db/migrations/026-group-events-schema.ts (group_events + interactions.group_event_id + the group-event orphan-repair shape), src/db/migrations/022-orrery-systems.ts + the systems/system_rules/system_overrides/system_prefs shapes, the profile_layout_templates/profile_background_templates + ai_connections + personalization_sections table shapes (migration 029), src/db/migrations/profile-presentation.ts (the two presentation tables' REAL shapes — NEITHER has its own `uid`; PK is contact_id/category_id; layout_template_uid/background_template_uid are already-portable TEXT), src/db/profile-presentation-dao.ts (the upsert idiom: `ON CONFLICT(contact_id)`/`ON CONFLICT(category_id)`), dossier §AJ + planning-notes R-09 (Phase 33 validation/orphan-repair; never falsely Ready)</read_first>
  <behavior>
    - Every entity the v5 export serializes has a matching restore writer: systems, system_rules, system_overrides, system_prefs, profile_layout_templates, profile_background_templates, ai_connections, personalization_sections, group_events — added to `reconciliation.ts` (`MergeableEntityType` + policy registry) and `restore-apply.ts` (`entities[]`, `tableOf`, `tombstoneEntity`, `incomingRows`, `localRows`, upsert switch).
    - Integer FKs are REBUILT from portable UIDs against the DESTINATION DB, never written as source rowids (review MEDIUM): `system_rules.systemUid` → destination `systems.id`; `system_overrides.contactUid` → destination `contacts.id`; `interactions.groupEventUid` → destination `group_events.id`. The parent entity restores before its child so the lookup resolves.
    - The interactions restore INSERT carries duration, allow_ai, and the remapped group_event_id (from groupEventUid) from a v5 manifest; a legacy/older manifest that lacks the column still fail-closes allow_ai=0 (preserve the existing D-04 behavior for the no-column path).
    - ai_connections restores WITHOUT any credential column (lane/model/endpoint/custom_model metadata only — credentials live in SecureStore).
    - A group_event tombstone (now carried) applies on restore; an orphaned member interaction (group_event_id → a missing group_event) is repaired per Phase 33 rules WITHOUT dropping the interaction's own contact data.
    - Both merge and replace-all restore modes handle the new entities.
    - The two Profile-presentation entities are restored (D-14): `profile_contact_presentation` and `profile_category_presentation`. Because neither has its own `uid`, the reconciliation identity is the parent uid (contactUid/categoryUid); the restore upsert is `INSERT … ON CONFLICT(contact_id)`/`ON CONFLICT(category_id) DO UPDATE SET …` with the integer PK rebuilt from the parent uid against the DESTINATION DB (never a source rowid). Parent (contacts/categories) restores before the presentation row so the lookup resolves.
    - The presentation `layout_template_uid`/`background_template_uid` are written VERBATIM as portable TEXT UIDs (no remap); the templates (profile_layout_templates/profile_background_templates) are upserted before the assignments so a same-run template uid resolves, but the schema's `ON DELETE SET NULL` FK tolerates a dangling template uid.
    - reconciliation.ts `readParentUid` gains a `category_id → categoryUid` alias (it currently maps only contact_id/field_def_id/method_id/external_contact_link_id; `contact_id → contactUid` already exists).
    - Presentation tables carry NO tombstone (like categories/profile → `tombstoneEntity: null`); a reset/hard-delete is not tombstoned and parent deletion cascades (`ON DELETE CASCADE`), so merge is insert/lww on the parent-uid key and no separate presentation tombstone is needed.
    - Both merge and replace-all handle the presentation entities; replace-all clears them (they cascade from contacts/categories, but add them to the reset/delete set explicitly).
  </behavior>
  <action>Extend the restore machinery so the roundtrip is real (per-entity, following the established reconciliation pattern — the graph cannot see TS→SQL edges, so enumerate by reading the shapes above). In `reconciliation.ts`: add each new entity to the `MergeableEntityType` union and give it an `EntityPolicy` (choose `writeMode` lww vs insert-if-missing per the closest existing precedent — e.g. per-contact merge entities like group_events follow the interactions/memories pattern; user-config tables like systems/templates/ai_connections/personalization_sections may be lww with no parent) plus `parentFields` for EACH portable-UID foreign key so it is remapped to a local id on restore: `system_rules.systemUid`↦`systems` (→ `system_id`), `system_overrides.contactUid`↦`contacts` (→ `contact_id`), `interactions.groupEventUid`↦`group_events` (→ `group_event_id`). In `restore-apply.ts`: add each entity to `entities[]`, `tableOf`, `tombstoneEntity` (null where no tombstone), `incomingRows` (manifest→row projection), `localRows` (SELECT with camelCase aliases mirroring export — including the `systemUid`/`contactUid`/`groupEventUid` joins), and an upsert `INSERT … ON CONFLICT(uid) DO UPDATE` in the correct parent-before-child order; **rebuild each integer FK from its UID by looking the UID up in the destination DB (never write a source rowid into `system_id`/`contact_id`/`group_event_id`).** Extend the interactions INSERT (`:201`) to include `duration`, `allow_ai`, and the remapped `group_event_id` (both INSERT columns and the ON CONFLICT SET arm) — carry the v5 value when present, keep the fail-closed `allow_ai=0` only for the no-column legacy path. Carry Phase 33's orphan-repair for group_events + interactions.group_event_id (a `groupEventUid` with no matching destination group_event repairs per Phase 33 rules without dropping the interaction's own contact data). Add manifest types/validation in `backup-schema.ts` for each new entity if not already added in Task 2. Write RED tests first in `reconciliation.test.ts` + `restore-apply.test.ts`: each new entity roundtrips export→import (merge AND replace-all); the interaction gap columns survive; **the FK-remap tests restore into a DIFFERENT-rowid destination — seed the destination `systems`/`contacts`/`group_events` with integer ids that DIFFER from the source, then assert restored `system_rules`/`system_overrides`/`interactions` resolve their `system_id`/`contact_id`/`group_event_id` to the correct destination rows BY UID (a same-fixture roundtrip is explicitly insufficient and would mask a wrong integer-FK remap in the irreversible v5 format)**; a deleted-group_event tombstone applies; an orphaned member interaction is repaired without contact-data loss; ai_connections restores with no credential column. **Restore the two Profile-presentation entities (D-14, review HIGH-C):** add `profile_contact_presentation` + `profile_category_presentation` to `reconciliation.ts` (`MergeableEntityType` + an `EntityPolicy` — `writeMode: "lww"`, `parentFields: [{ field: "contact_id", entityType: "contacts" }]` / `[{ field: "category_id", entityType: "categories" }]`) and add the missing `category_id → categoryUid` alias to `readParentUid`. In `restore-apply.ts` add each to `entities[]`, `tableOf`, `tombstoneEntity` (null — no tombstone), `incomingRows` (manifest→row projection; set the reconciliation `uid` to `contactUid`/`categoryUid` since the table has none of its own), `localRows` (SELECT with the SAME parent-uid JOIN + camelCase aliases as export), and an upsert `INSERT … ON CONFLICT(contact_id)`/`ON CONFLICT(category_id) DO UPDATE SET …` in parent-before-child order, rebuilding the integer PK from the parent uid via the existing `idMap(exec,"contacts")`/`idMap(exec,"categories")` — NEVER writing a source rowid, and writing the already-portable `layout_template_uid`/`background_template_uid` VERBATIM (no remap). Ensure the presentation upserts run AFTER templates (profile_*_templates) so a same-run template uid resolves. Add manifest types/validation in `backup-schema.ts` if not added in Task 2. Write RED tests first: both presentation entities roundtrip (merge AND replace-all) INTO A DIFFERENT-ROWID DESTINATION — seed the destination `contacts`/`categories` with integer ids that DIFFER from the source and assert restored `profile_contact_presentation.contact_id`/`profile_category_presentation.category_id` resolve to the correct destination rows BY UID, with `freeform_layout_json`, `collapse_json`, and both template assignments preserved (a same-fixture roundtrip is explicitly insufficient — it would mask a wrong PK remap in the irreversible v5 format).</action>
  <verify>
    <automated>npx vitest run src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Every v5-serialized entity + the interaction gap columns + both Profile-presentation tables are ingested by restore-apply/reconciliation; presentation PKs are rebuilt from the parent uid against a different-rowid destination; group_event tombstones apply; orphaned members repair without data loss; ai_connections restores without credentials; the export→import roundtrip actually holds. Project typechecks.</done>
  <acceptance_criteria>
    - `restore-apply.ts` `entities[]` (and `tableOf`/`tombstoneEntity`/`incomingRows`/`localRows`/upsert switch) include systems/system_rules/system_overrides/system_prefs/profile_layout_templates/profile_background_templates/ai_connections/personalization_sections/group_events.
    - The interactions restore INSERT carries duration/allow_ai/group_event_id from a v5 manifest (test); a no-column legacy manifest still fail-closes allow_ai=0 (test).
    - Integer FKs are rebuilt from UIDs against a DIFFERENT-rowid destination (test): `system_rules.systemUid`→destination `systems.id`, `system_overrides.contactUid`→destination `contacts.id`, `interactions.groupEventUid`→destination `group_events.id` all resolve correctly when the destination rows have different integer ids than the source; no source rowid is written into a destination FK column.
    - A per-entity roundtrip test (merge AND replace-all) proves each newly-serialized entity survives export→import.
    - A deleted-group_event tombstone applies on restore; an orphaned member interaction is repaired without losing the interaction's own contact data.
    - `ai_connections` restore writes no credential column; grep-asserted.
    - `profile_contact_presentation` + `profile_category_presentation` are added to `restore-apply.ts` (`entities[]`/`tableOf`/`tombstoneEntity`/`incomingRows`/`localRows`/upsert) and `reconciliation.ts` (`MergeableEntityType` + `EntityPolicy` + the `category_id→categoryUid` alias in `readParentUid`).
    - The presentation PK (`contact_id`/`category_id`) is rebuilt from `contactUid`/`categoryUid` against a DIFFERENT-rowid destination (test); no source rowid is written; `layout_template_uid`/`background_template_uid` restore verbatim.
    - A merge AND a replace-all roundtrip prove per-contact/per-category assignments, `freeform_layout_json`, and `collapse_json` survive export→import.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Stage + rehydrate Profile background image BYTES (D-14, review HIGH-C)</name>
  <files>src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/backup-schema.ts, src/services/photos/background-storage.ts, src/services/photos/background-storage.test.ts, src/services/photos/background-reconcile-sweep.ts, src/services/photos/background-reconcile-sweep.test.ts, src/services/backup/share-export.ts</files>
  <read_first>src/backup/export-manifest.ts (:44-62 `withPhoto` — the photoBase64 export idiom; :157-173 the custom-field photo base64 staging; `ExportManifestDeps.readPhotoBase64`), src/services/backup/share-export.ts (:35-36 `readStoredPhotoBase64` — resolves via `resolvePhotoUri`, the AVATARS-only guard; the export reader that must gain a background-scoped path), src/services/photos/background-storage.ts (THE existing background chokepoint to reuse/extend: `SAFE_BACKGROUND_RELATIVE` :7, `SAFE_BACKGROUND_SIDECAR` :8-9, `backgroundDerivativeRelPath(templateUid)` :43 → the uid-derived `profile-backgrounds/<uid>.jpg`, `persistBackgroundDerivative(sourceUri, relative)` :88 → the crash-safe copy-to-tmp/old-to-bak/tmp-to-dest swap, `resolveBackgroundUri` :62, `enqueueWrite` :66 the per-path write serializer, `listBackgroundStorageEntries` :172), src/services/photos/photo-storage.ts (:245-261 `stageRestorePendingBase64` — the `atob`→`Uint8Array`→`File.write` base64-decode idiom to MIRROR for the new background STAGE helper; `restorePendingRelPath`/`resolveRestorePendingUri`/`listRestorePendingPhotos` — the AVATARS restore-pending namespace shape to MIRROR for a background restore-pending namespace; do NOT edit this avatars file), src/services/photos/background-reconcile-sweep.ts (`registerBackgroundReconcileSweep` :40 the ready-gated launch sweep; `runBackgroundReconciliation` :15-37 — TODAY it reconciles `.tmp`/`.bak` sidecars + prunes orphan canonical + only LOGS a template row whose durable file is missing at :31-35. THIS TASK EXTENDS it: before logging a missing reference, re-drive the persist from the retained restore-pending file if one exists, and prune a staged file whose row rolled back), src/services/photos/background-reconcile-model.ts (`planBackgroundReconciliation` — the PURE planner the sweep delegates to; returns `{actions, missingReferences}`; read to understand the existing missing-reference set the re-drive consumes), src/backup/restore-apply.ts (the AVATAR crash-safe pattern to MIRROR exactly: :232-246 `stageCandidates` stages decoded bytes into the restore-pending namespace, :298 candidates are staged BEFORE `inWriteTransaction`, :324 the reference + journal row commit INSIDE the txn, :330-331 `persist(resolveRestorePendingUri(...), canonical)` runs AFTER commit then deletes the staged file — the journal + retained restore-pending bytes are what make a post-commit crash re-drivable; :28-38 `RestoreApplyDependencies` inject photo deps that DEFAULT to real impls (`stagePhoto ?? stageRestorePendingBase64`, `persistPhoto ?? persistMaster`) — mirror that for new `stageBackground`/`persistBackground` deps), src/db/migrations/008-restore-photo-journal.ts (:8 the CHECK `target_kind IN ('contact','profile','customField')` — WHY the journal CANNOT carry a `background` kind without a table-rebuild migration that would reverse D-03/36-01; do NOT route background bytes here — the uid-keyed retained restore-pending file is the durable evidence instead, since the background canonical path is uid-derivable while an avatar canonical path needs the committed rowid), src/db/migrations/profile-presentation.ts (:27-38 `profile_background_templates` — it HAS its own `uid`; `image_path TEXT NOT NULL CHECK(image_path GLOB 'profile-backgrounds/*')`), docs/systems/profile.md (:76 "Background bytes stay in app-owned `profile-backgrounds/<uid>.jpg` paths")</read_first>
  <action>Carry the background image BYTES in the v5 backup, not just the path (D-14 — a restored install must not point a `profile_background_templates` row at a nonexistent file), routing restore through the EXISTING shipped background subsystem so it needs NO new migration and NO `restore_photo_journal` change. Migration 008's shipped CHECK `target_kind IN ('contact','profile','customField')` cannot carry a `background` kind, and a SQLite CHECK cannot be ALTERed — a table-rebuild migration would reverse 36-01's owner-accepted single-migration-029 decision (D-03). Therefore: do NOT add a `background` target kind to `restore_photo_journal`; do NOT add a second `profile-backgrounds/` guard to `photo-relative-path.ts`; do NOT add a second persist to `photo-storage.ts` — the avatars guards stay untouched and the background guard already exists as `SAFE_BACKGROUND_RELATIVE` in background-storage.ts.

**CRASH-CONSISTENCY (review cycle-5 HIGH) — mirror the AVATAR stage-before-txn / persist-after-commit invariant** (restore-apply.ts :298 stage → :324 commit reference → :330-331 persist after commit) so the durable canonical file is never mutated before the row that references it commits. Persist-before-commit-to-canonical was CONSIDERED AND REJECTED: `persistBackgroundDerivative` deletes its `.bak` at the end of the swap, so a merge UPDATE-in-place (same uid, newer incoming bytes) followed by a transaction rollback would leave the committed OLD row pointing at canonical now holding NEW bytes with the prior bytes gone — an unrecoverable clobber on the irreversible v5 surface (crash window #4). The staged design below preserves the prior background across a rollback and never orphans a committed reference.

(1) EXPORT — when serializing each `profile_background_templates` row (its row + `image_path` join the entity set in Task 2), read the file bytes and attach them as base64 (e.g. `imageBase64`), following the `withPhoto`/`readPhotoBase64` idiom. The injected `readPhotoBase64` (share-export.ts `readStoredPhotoBase64`) resolves through `resolvePhotoUri` — the AVATARS-only guard — so a `profile-backgrounds/<uid>.jpg` path throws there; give it a background-scoped read path that resolves via `resolveBackgroundUri` (which enforces `SAFE_BACKGROUND_RELATIVE`) rather than widening the avatars guard. Recommended low-churn form: a namespace dispatch inside `readStoredPhotoBase64` that routes a `profile-backgrounds/` relative through `resolveBackgroundUri` and keeps `avatars/` on `resolvePhotoUri` — this edits ONLY share-export.ts and leaves the export dep wiring (backup-service.ts / backup-sweep.ts / the screens) unchanged; if instead a dedicated reader dep is threaded, the executor must also thread it through those wiring sites. A missing/unreadable background file raises the same content-free `BackupPhotoUnreadableError` repair cue as contact photos.

(2) RESTORE — STAGE BEFORE THE TRANSACTION: add a base64→restore-pending STAGE helper to `background-storage.ts` (mirror `stageRestorePendingBase64`'s `atob`→`Uint8Array`→`File.write` decode) that writes each incoming WINNING row's `imageBase64` as decoded bytes into a NEW background restore-pending namespace keyed by the template's portable uid — mirror the avatars `restorePendingRelPath`/`resolveRestorePendingUri`/`listRestorePendingPhotos` shape with a `backgroundRestorePendingRelPath(uid[, session])` (e.g. under `profile-backgrounds/_restore_pending/`), a `resolveBackgroundRestorePendingUri`, and a `listBackgroundRestorePendingEntries`, each guarded by a restore-pending-scoped safe-relative check (do NOT loosen `SAFE_BACKGROUND_RELATIVE`; the canonical guard stays `profile-backgrounds/<uid>.jpg`). Stage these BEFORE `inWriteTransaction`, exactly as `stageCandidates` (:298) stages avatar bytes before the txn. INSIDE the txn, Task 3 commits the `profile_background_templates` row with `image_path = backgroundDerivativeRelPath(uid)`. AFTER commit — mirroring the avatars finalize loop (:330-331) — persist each staged file to its canonical `backgroundDerivativeRelPath(uid)` (`profile-backgrounds/<uid>.jpg`, uid-derived and portable) by REUSING `persistBackgroundDerivative` (pass the staged file's `file://` sourceUri; its copy-to-tmp → old-to-bak → tmp-to-dest swap is the crash-safe writer), then delete the staged restore-pending file. Inject `stageBackground`/`persistBackground` into `restore-apply.ts` as optional deps defaulting to the real helpers, exactly as `stagePhoto`/`persistPhoto` default at :28-38, so restore-apply node tests stay pure.

DURABLE RE-DRIVE EVIDENCE (no journal): the retained restore-pending file keyed by uid IS the durable evidence — the background canonical path is uid-derivable (`backgroundDerivativeRelPath(uid)`), so unlike avatars (whose canonical path needs the committed rowid and therefore needs a `restore_photo_journal` row) the sweep can re-derive canonical directly from the staged file's uid. EXTEND `background-reconcile-sweep.ts` `runBackgroundReconciliation`: where it TODAY only logs a template row whose durable file is missing, it now first (a) lists the background restore-pending entries, (b) for each staged uid whose canonical IS referenced by a committed `profile_background_templates` row AND whose canonical file is missing → re-drive `persistBackgroundDerivative(stagedUri, backgroundDerivativeRelPath(uid))` and delete the staged file, (c) for a staged uid with NO committed referencing row (a rolled-back INSERT) → prune the staged file, (d) for a staged uid whose canonical already exists (persist completed, crash before staged cleanup) → delete the staged file (idempotent); only log a truly missing reference when no staged bytes remain to re-drive. The existing `.tmp`/`.bak` sidecar reconcile + orphan-canonical prune stay unchanged (keep `planBackgroundReconciliation` as the pure planner; the re-drive/prune orchestration lives in the sweep). This ALSO discharges the cycle-4 ACTIONABLE MEDIUM for free: the background-aware persist + launch-sweep reconciler the MEDIUM asked for are named here (`persistBackgroundDerivative` + `registerBackgroundReconcileSweep`) rather than built as avatars-scoped duplicates.

Background bytes are user data preserved in backup, NOT AI egress — nothing here transmits them to a provider (do not conflate backup inclusion with egress; credentials still never enter the backup, ADR-049). Add manifest validation for the base64 field in `backup-schema.ts`.

TESTS (RED first; the v5 completeness checkpoint is GATED on these passing): a background image roundtrips export→restore and the restored file exists at `backgroundDerivativeRelPath(uid)` with the row's `image_path` pointing at it (assert the bytes match); a `profile_background_templates` row with an unreadable source raises the content-free `BackupPhotoUnreadableError` on export read through the background-scoped path; the restored canonical path satisfies `SAFE_BACKGROUND_RELATIVE` (no avatars guard involved); AND the FOUR crash windows the review named, each asserting "a committed row always has its durable file AND the prior background is never clobbered": (1) crash BEFORE SQL commit → the row is rolled back, no orphan reference exists, and canonical is untouched (a pre-existing background is intact); (2) crash AFTER commit BEFORE the FS persist → the committed row's durable file is re-driven to canonical from the retained restore-pending file by `runBackgroundReconciliation` (recoverable, not a permanent broken row); (3) crash mid-swap (an interrupted persist leaving a `.tmp`/`.bak` sidecar) → the existing sidecar reconcile lands the canonical file; (4) rollback AFTER a staged replacement → canonical is preserved (never pre-mutated) and the staged restore-pending artifact is pruned by the sweep. Put the sweep re-drive/prune + four-window recovery tests in `background-reconcile-sweep.test.ts`, the stage/persist-helper + swap tests in `background-storage.test.ts`, and the export→restore byte roundtrip in `export-manifest.test.ts`/`restore-apply.test.ts`.</action>
  <verify>
    <automated>npx vitest run src/backup/export-manifest.test.ts src/backup/restore-apply.test.ts src/services/photos/background-storage.test.ts src/services/photos/background-reconcile-sweep.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Profile background image bytes are exported as base64, STAGED to a background restore-pending namespace before the write txn, and persisted to the app-owned `profile-backgrounds/<uid>.jpg` canonical path AFTER commit via `persistBackgroundDerivative`'s crash-safe swap — canonical is never mutated before its referencing row commits. The retained restore-pending file (keyed by uid) is durable re-drive evidence and the EXTENDED `background-reconcile-sweep` re-drives (not merely logs) an interrupted post-commit persist and prunes a rolled-back staged artifact. All four crash windows hold (before-commit / after-commit-before-persist / mid-swap / rollback-after-stage). No `restore_photo_journal` `background` kind and no new migration (D-03/36-01 preserved); a restored install shows the real background, never a broken row and never a clobbered prior background; the existing `SAFE_BACKGROUND_RELATIVE` guard covers the canonical path; no AI egress. Project typechecks.</done>
  <acceptance_criteria>
    - Each `profile_background_templates` row carries its image bytes as base64 in the manifest (not just `image_path`); an unreadable source raises the content-free `BackupPhotoUnreadableError` on export, read through a background-scoped path (never the avatars guard).
    - Restore STAGES decoded bytes to a background restore-pending namespace BEFORE the write transaction, commits the row inside the txn, and persists to `backgroundDerivativeRelPath(uid)` (`profile-backgrounds/<uid>.jpg`, uid-derived) AFTER commit by reusing `persistBackgroundDerivative`'s crash-safe swap; the restored row's `image_path` points at that file (roundtrip test asserts the file exists and bytes match).
    - The restore path adds no `background` target kind to `restore_photo_journal` and introduces no new migration (migration-008 CHECK left intact; D-03/36-01 single-migration-029 preserved).
    - Crash window #1 (crash before SQL commit): the row is rolled back, no orphan reference exists, and a pre-existing background at canonical is intact (test).
    - Crash window #2 (crash after commit before FS persist): `runBackgroundReconciliation` re-drives the persist to canonical from the retained restore-pending file — a committed row is never left pointing at a nonexistent file (test).
    - Crash window #3 (crash mid-swap, `.tmp`/`.bak` sidecar left): the existing sidecar reconcile lands the canonical file (test).
    - Crash window #4 (rollback after a staged replacement): canonical is preserved (never pre-mutated) and the staged restore-pending artifact is pruned by the sweep — the prior background is never clobbered (test).
    - `registerBackgroundReconcileSweep`'s `runBackgroundReconciliation` re-drives from a retained staged file and prunes a rolled-back staged artifact rather than only logging a missing reference; no avatars-scoped duplicate guard or persist is added (`photo-relative-path.ts` / `photo-storage.ts` untouched).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm the v5 format bump + upgrader + entity inventory (one-way / irreversible in production)</name>
  <reversibility rating="one-way">A shipped wire-format bump runs forward-only on users' devices; an incomplete v5 cannot be corrected by a second retroactive bump (planning-notes R-09). Confirm completeness before the bump lands.</reversibility>
  <decision>Approve bumping the backup wire format to v5 with the current emission + entity inventory, given it is forward-only and a second retroactive bump is impossible.</decision>
  <context>On disk `BACKUP_FORMAT_VERSION` is 4; the target is 5 (head+1). Tasks 1–4 should have landed the full milestone preference inventory (emit side), all wire entities (orrery systems, profile templates, ai_connections, personalization_sections, group_events, PLUS the two Profile-presentation tables profile_contact_presentation/profile_category_presentation keyed on parent uid) + the interaction gap columns (duration, allow_ai, group_event_id) + the Profile background image BYTES (staged to a restore-pending namespace before the txn, persisted to profile-backgrounds/<uid>.jpg after commit) on BOTH export AND restore, the group_event tombstone reconciliation, and the orphan-repair. Every "Backup/Restore preserves X" dossier claim (planning-notes lists 8) PLUS D-14's Profile-presentation + background-bytes coverage (review HIGH-C) must have a matching emitter AND a matching restore writer — a serialized-but-not-restored entity, or a background row whose bytes were not staged, is an incomplete v5. **Background-byte crash-consistency is a completeness gate (review cycle-5 HIGH):** Task 4's four crash-window tests (crash before SQL commit; after commit before FS persist; mid-swap `.tmp`/`.bak`; rollback after a staged replacement) MUST be green before approving the bump — each must prove a committed row always has its durable file and the prior background is never clobbered, migration-free (no `restore_photo_journal` `background` kind). The retired-key restore-compat decisions (includeUnboundNeverContacted, any Manage-favourites key) must be settled. Confirm the export↔restore roundtrip is complete before the bump — this is the phase's final irreversible change and a second retroactive bump is impossible.</context>
  <options>
    <option id="bump">
      <name>Approve — bump to v5, add the 4→5 upgrader, apply retired-key compat</name>
      <pros>Closes the milestone schema chain with a complete format; ships portable prefs + all entities</pros>
      <cons>Irreversible on devices; if an entity/pref was missed it cannot be added by a later retroactive bump</cons>
    </option>
    <option id="hold">
      <name>Hold — inventory/roundtrip incomplete, fix emission/restore/entities before bumping</name>
      <pros>Prevents shipping an incomplete v5 (missing emitter OR missing restore writer OR un-staged background bytes OR a dropped Profile-presentation assignment OR a failing background crash-window test)</pros>
      <cons>Blocks phase completion until the gap is closed</cons>
    </option>
  </options>
  <resume-signal>Select "bump" (proceed to the v5 bump + upgrader) or "hold" and name the missing entity/preference.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Bump to v5 + FORWARD_MIGRATIONS 4→5 + restore-never-falsely-Ready + full roundtrip</name>
  <files>src/backup/types.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <read_first>src/backup/types.ts (:14 BACKUP_FORMAT_VERSION — re-verify it is 4 on disk), src/backup/backup-schema.ts (:29-108 FORWARD_MIGRATIONS incl the existing `3:` upgrader; :807-853 the upgrade loop + MAX_SUPPORTED_BACKUP_FORMAT_VERSION; restore validation region), src/backup/restore-apply.ts (the ingest writers Task 3 added), dossier §AJ + planning-notes R-09 (Phase 33 validation/orphan-repair; never falsely Ready)</read_first>
  <action>Set `BACKUP_FORMAT_VERSION = 5` in `types.ts` (re-verify head+1 on disk first — do NOT re-bump to 4). Add a `4:` entry to `FORWARD_MIGRATIONS` mirroring the existing `3:` upgrader: raise `backupFormatVersion` to 5 and inject the new entity arrays (systems/system_*, profile templates, ai_connections, personalization_sections, group_events, AND the two Profile-presentation arrays profile_contact_presentation/profile_category_presentation per D-14) as empty so an older manifest upgrades cleanly and exactly once. The restore-apply/reconciliation ingest + orphan-repair for these entities is built in Tasks 3–4 (SQL entities + presentation in Task 3, background bytes in Task 4) — this task wires the version bump + upgrader + validation and proves the whole chain. Ensure a restored install never appears AI-Ready without a valid local credential (availability requires SecureStore credential — assert it). Apply each retired key's approved restore-compat behavior (no silent drop). Add/extend the roundtrip test asserting `BACKUP_FORMAT_VERSION === 5`, a v4→5 upgrade applies once and a v5 is not re-upgraded, an empty DB roundtrips, and the full portable-key + entity set (incl. the Task-3/Task-4 restore writers, both Profile-presentation entities, and the background image bytes) survives export→import.</action>
  <verify>
    <automated>npx vitest run src/backup/backup-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Format is v5 with a working `4:` upgrader, restore validation/orphan-repair, retired-key compat, credentials excluded, and a restored install never falsely Ready — proven by the roundtrip test.</done>
  <acceptance_criteria>
    - `BACKUP_FORMAT_VERSION === 5` (test-asserted); a v4 manifest upgrades to 5 exactly once; a v5 is not re-upgraded.
    - Empty-DB export→import yields an equivalent empty state.
    - A restore with no SecureStore credential yields Needs Attention, never Ready.
    - Each retired key applies its decided restore-compat behavior (test-asserted; no silent drop).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Retire the "device-local pending" Profile-presentation backup boundary in the docs (D-14)</name>
  <files>docs/systems/profile.md, docs/systems/backup-restore.md</files>
  <read_first>docs/systems/profile.md (:78-82 "Backup and cross-phase boundaries" — currently says format 4 does NOT emit/restore Profile templates/assignments/freeform/collapse/background rows/bytes, "Phase 36 owns the coordinated format decision"), docs/systems/backup-restore.md (:123 gotcha 15 "Profile presentation is not a format-4 entity")</read_first>
  <action>Now that Phase 36 has DISCHARGED the deferred decision (D-14), update the two LIVING system docs to match the shipped v5 format — do this only AFTER Tasks 2–6 have landed the emit+restore+bump. In `docs/systems/profile.md` (:78-82) replace the "not emitted or restored by format 4 / Phase 36 owns the eventual decision" boundary with: format v5 emits AND restores the Profile-presentation entities (`profile_contact_presentation`, `profile_category_presentation` — assignments, `freeform_layout_json`, `collapse_json`) keyed on the parent uid, and the background image bytes (rehydrated to `profile-backgrounds/<uid>.jpg`); the two nullable global Profile preference keys are now emitted (not merely allowlisted). In `docs/systems/backup-restore.md` rewrite gotcha 15 to state that Profile presentation + background bytes ARE v5 entities (was format-4-deferred), and append a Changelog row (Phase 36, "Profile presentation + background bytes added to v5 backup — deferral discharged per D-14"). Do NOT edit any ADR (immutable); these are living system docs. Keep the wording consistent with the actual shipped code — verify column names against the code, not this plan's prose.</action>
  <verify>
    <automated>grep -nE "profile_contact_presentation|profile_category_presentation|profile-backgrounds" docs/systems/profile.md docs/systems/backup-restore.md</automated>
  </verify>
  <done>Both system docs record Profile presentation + background bytes as v5 entities; the "device-local pending" / "not a format-4 entity" boundary notes are retired; a Changelog row is appended. No ADR modified.</done>
  <acceptance_criteria>
    - docs/systems/profile.md no longer says Profile presentation/background bytes are un-emitted/un-restored; it describes them as v5 entities keyed on parent uid.
    - docs/systems/backup-restore.md gotcha 15 states presentation + background bytes ARE v5 entities, with a Changelog row appended.
    - No ADR file is modified.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- **Modified:** `src/backup/types.ts` (BACKUP_FORMAT_VERSION = 5); `src/backup/backup-schema.ts` (FORWARD_MIGRATIONS `4:` upgrader, PORTABLE_SETTINGS_KEYS additions, new entity types/validation incl. the two presentation arrays + background-bytes field, retired-key compat); `src/backup/export-manifest.ts` (new entity SELECTs + interactions duration/allow_ai/group_event_id + group_events + group_event tombstone filter removed + profile_contact_presentation/profile_category_presentation SELECTs + profile_background_templates base64 image bytes); `src/backup/reconciliation.ts` (`MergeableEntityType` union + policy registry extended for the 9 new entities + the 2 presentation entities; `readParentUid` category_id→categoryUid alias); `src/backup/restore-apply.ts` (ingest writers for the new entities + presentation + interactions gap columns + orphan repair + background byte rehydration); `src/services/photos/background-storage.ts` (base64→restore-pending stage helper + restore-pending resolver/lister, reusing the crash-safe swap for the post-commit canonical persist) + `src/services/photos/background-reconcile-sweep.ts` (EXTENDED to re-drive an interrupted post-commit background persist from the retained restore-pending file and prune rolled-back staged artifacts — not merely log a missing file) + `src/services/backup/share-export.ts` (background-scoped export reader) — the profile-backgrounds/ canonical guard already exists (background-storage.ts `SAFE_BACKGROUND_RELATIVE`) and is reused, not duplicated; no restore_photo_journal 'background' kind and no new migration; `src/db/app-settings-dao.ts` (getPortableSettingsSnapshot full emission); `docs/systems/profile.md` + `docs/systems/backup-restore.md` (presentation + background bytes recorded as v5 entities).
- **Behavior:** complete v5 wire format with a REAL export↔restore roundtrip; full milestone preference + entity serialization AND ingest INCLUDING Profile presentation (per-contact/per-category assignments, freeform layouts, collapse state) and background image bytes; presentation FKs rebuilt from parent uid against a different-rowid destination; restore validation/orphan-repair; group_event tombstones carried; credentials excluded; restored install never falsely Ready; the device-local-pending Profile-presentation docs boundary retired.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → backup file | the manifest is portable off-device; credentials must never cross into it |
| backup file → restore | an older/tampered manifest is untrusted input to the upgrade + restore path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-30 | Information disclosure | credential leak into backup | high | mitigate | no credential emitted; SECRET_SHAPED_KEY rejects key-shaped keys; ai_connections serialized without credential columns; grep + test |
| T-36-31 | Tampering | malformed/older manifest on restore | high | mitigate | FORWARD_MIGRATIONS validated upgrade chain; restore validation + orphan-repair (Phase 33 rules); apply-once adjacency test |
| T-36-32 | Elevation/misconfig | restored install falsely Ready | high | mitigate | availability requires a valid local credential; restored-with-no-credential → Needs Attention (test-asserted) |
| T-36-33 | Data loss | incomplete v5 shipped (serialized-but-not-restored entity) | high | mitigate | Task 3 owns the restore side so every serialized entity has an ingest writer; per-entity export↔import roundtrip tests; completeness checkpoint before the bump; a second retroactive bump is impossible so v5 must be complete (planning-notes R-09, review HIGH #3) |
| T-36-33b | Tampering / data corruption | wrong integer-FK remap on restore | high | mitigate | FKs travel as portable UIDs (systemUid/contactUid/groupEventUid) and are rebuilt against the destination DB, never as source rowids; a DIFFERENT-rowid destination restore test proves the remap (a same-fixture roundtrip is insufficient) — corruption baked into the irreversible v5 format is unrecoverable (review MEDIUM) |
| T-36-34 | Data loss | Profile presentation + background bytes dropped from the irreversible v5 | high | mitigate | profile_contact_presentation/profile_category_presentation serialized AND restored (keyed on parent uid, PK rebuilt from uid against a DIFFERENT-rowid destination); background bytes staged like photoBase64 and rehydrated to profile-backgrounds/<uid>.jpg; a serialized-but-not-restored presentation entity is the incomplete-v5 defect this plan forbids (D-14, review HIGH-C) |
| T-36-35 | Tampering / traversal | malicious background image_path on restore | medium | mitigate | the EXISTING profile-backgrounds/-scoped guard (background-storage.ts `SAFE_BACKGROUND_RELATIVE`, reused not duplicated) rejects `..`/backslash/absolute/null-byte; bytes persist only under app-owned profile-backgrounds/ via `persistBackgroundDerivative`; canonical path derived from the portable uid (`backgroundDerivativeRelPath`), never from a manifest-supplied path; no restore_photo_journal 'background' kind and no new migration (D-03/36-01) |
| T-36-36 | Data availability / corruption | crash between background-row commit and file persist on the irreversible v5 surface | high | mitigate | canonical is never mutated before its referencing row commits: decoded bytes are STAGED to a background restore-pending namespace before the write txn, the row commits inside the txn, the canonical file is persisted via `persistBackgroundDerivative` after commit, and the retained restore-pending file (keyed by uid) is durable re-drive evidence the EXTENDED `background-reconcile-sweep` uses to re-drive an interrupted persist (not merely log) and to prune a rolled-back staged artifact; all four crash windows tested and gated at the pre-bump checkpoint; migration-free, no `restore_photo_journal` 'background' kind (D-14, D-03/36-01) |
</threat_model>

<verification>
- `npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts src/db/app-settings-dao.test.ts src/services/photos/background-storage.test.ts src/services/photos/background-reconcile-sweep.test.ts` green (incl. the four background-byte crash-window tests); `npx tsc --noEmit` + `npm run check:colors` clean.
- Device UAT (Pixel, end-of-phase): export→import roundtrip preserves all milestone prefs + entities AND restores them (new entities present after import), INCLUDING per-contact/per-category Profile presentation (template + background assignments, freeform layout, collapse state) and the custom background IMAGE actually rendering after restore (not a broken/blank background); a restore onto a fresh install shows AI Needs Attention (no credential) not Ready; group-event tombstone applied + orphan repaired without contact-data loss.
</verification>

<success_criteria>
- Backup format is v5 with a complete `4:` upgrader and full milestone entity/preference serialization.
- Profile presentation (profile_contact_presentation/profile_category_presentation) and background image bytes are serialized AND restored (D-14/HIGH-C); a restored install loses no assignment, freeform layout, collapse state, or background image; the docs boundary note is retired.
- Credentials excluded; restored install never falsely Ready; retired keys have decided compat.
- This plan ran last, after all other Phase-36 schema.
</success_criteria>

<output>
Create `.planning/phases/36-ai-configuration-prompting/36-08-SUMMARY.md` when done.
</output>
