# Deferred Items

- **Pre-existing Phase 30 test failure:** the optional repository-wide `npm test -- --run` check on 2026-09-14 completed with 3,399 passing tests and one failed suite, `src/components/orrery/orrery-controls-render.test.tsx`, which fails during collection with `SyntaxError: Unexpected token 'typeof'`. Phase 36 Plan 03 does not touch the Orrery subsystem; the existing dirty Phase 30 review files and `tsconfig.json` were preserved unchanged. The Phase 36 targeted suites, TypeScript check, and color check all pass.

- **Plan 36-04 confirmation:** the repository-wide `npm test` check on 2026-09-14 again found the same pre-existing Phase 30 collection failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`). It passed 3,410 tests before the one Plan-36 fixture regression was corrected in `dc71fd8`; the complete focused permission/egress set then passed 114/114. The Orrery suite imports none of Plan 36-04's modules and remains outside this plan; the dirty Phase 30 review files and `tsconfig.json` remain untouched.

- **Plan 36-05 confirmation:** the repository-wide `npm test` check on 2026-09-14 again found only the pre-existing Phase 30 collection failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`), with 3,449 tests passing across 362 suites. Plan 36-05's focused connection/model/generation suites pass 45/45, and TypeScript plus the color gate are clean. The Orrery failure is unrelated to this plan; the dirty Phase 30 review files and `tsconfig.json` remain untouched.

- **Plan 36-06 confirmation:** the repository-wide `npm test -- --run` check on 2026-09-14 again found the same pre-existing Phase 30 collection failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`). After Plan 36-06's directly caused backup-policy regression was fixed in `a066bc4`, the repository suite excluding only that known Orrery file passed 3,465/3,465 tests across 365 suites. Plan 36-06's focused 48 tests, TypeScript, and color gate are clean; the dirty Phase 30 review files and `tsconfig.json` remain untouched.

- **Plan 36-07 confirmation:** the repository-wide `npm test -- --run` check on 2026-09-14 again found only the pre-existing Phase 30 collection failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`), with 3,484 tests passing across 366 suites. Plan 36-07's focused diagnostic, provider, prompt-transparency, lifecycle, and settings-DAO suites pass 193/193; TypeScript and the color gate are clean. The unrelated dirty Phase 30 review files and `tsconfig.json` remain untouched.

- **Plan 36-08 confirmation:** the repository-wide `npm test -- --run` check on 2026-09-14 again found only the pre-existing Phase 30 collection failure in `src/components/orrery/orrery-controls-render.test.tsx` (`SyntaxError: Unexpected token 'typeof'`), with 3,499 tests passing across 366 suites. Plan 36-08's required backup/settings/background suite passes in full; TypeScript and the color gate are clean. The unrelated dirty Phase 30 review files and `tsconfig.json` remain untouched.
