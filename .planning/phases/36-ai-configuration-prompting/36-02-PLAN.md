---
phase: 36-ai-configuration-prompting
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - package.json
  - package-lock.json
  - app.config.ts
  - src/ai/openrouter-oauth.ts
  - src/ai/openrouter-oauth.test.ts
  - src/ai/openrouter-catalog.ts
  - src/ai/openrouter-catalog.test.ts
  - src/services/ai-key-store.ts
autonomous: false
requirements: [AICFG-04]
user_setup: []

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user connects OpenRouter via a browser authorization flow (no manual key paste): Orbit generates a PKCE verifier + S256 challenge, opens the OpenRouter auth URL, captures the redirect `code`, exchanges it at `/api/v1/auth/keys`, and stores the returned key in SecureStore (AICFG-04)."
    - "The OAuth callback is validated before code exchange (review HIGH #6, a SANCTIONED added control): Orbit generates an in-memory anti-CSRF `state`, appends it to the auth URL, and on redirect (a) requires `res.type==='success'`, (b) accepts ONLY an exact `orbit://openrouter-auth` scheme+host (rejecting any other scheme/host/unexpected params), (c) validates the echoed `state` matches when present (confirmed against OpenRouter's echo behavior in the Pixel spike; if `state` is not echoed, exact-callback validation + one-time handling + PKCE binding are the effective controls), and (d) consumes the code once — a second/duplicate redirect is ignored and the verifier+state are cleared after exchange (AICFG-04)."
    - "The OpenRouter model catalog renders instantly from bundled/cached data with no blocking network on open, and refreshes on the first AI/model-settings open of a new LOCAL day (via `formatLocalDate()`) plus an explicit Refresh Models action; a failed refresh keeps the cached catalog and shows an inline failure note (AICFG-04)."
    - "Model cards show real current/cached input+output pricing sourced from OpenRouter metadata — never `$/$$/$$$` and never hardcoded model ids or prices; pricing values are decimal USD-per-token strings parsed with `Number(pricing.prompt)` (AICFG-04)."
    - "Curated-first ordering is reconciled with the no-frozen-list prohibition (review MEDIUM): the CURATION source is `src/ai/model-registry.ts` — a maintained data module of curated model ids + a recommendation-reason category (Balanced / Cost efficient / Lightweight), refreshable via app update, NOT a frozen catalog and NOT baked into UI logic. Curated ids are matched by id against the live/cached catalog, which remains the SOLE source of pricing + availability; a curated id absent from the live catalog is simply not shown. No price or catalog is ever frozen into product logic (AICFG-04)."
    - "OpenRouter is modeled as its own catalog+pricing source and is NOT forced into the LiteLLM `CatalogProvider` union (AICFG-04)."
    # --- Edge-probe derived (explicit) ---
    - "The models catalog is presented in a stable, deterministic order (curated set first in its defined order, then the broader catalog by a stable key) so equal-ranked models do not reshuffle between renders (AICFG-04 ordering)."
    - "An empty/absent cached catalog (first run, never refreshed) renders the bundled curated set or the documented 'No models available. Connect and refresh…' state — never a blank list or a thrown error on a read path (AICFG-04 empty)."
    # --- Edge-probe derived (backstop) ---
    - { statement: "Two catalog entries with identical id/name collapse to one card (no duplicate rows) and a card missing a pricing/context field shows what it has with deeper data behind More Info.", verification: backstop }
    - { statement: "The OpenRouter redirect capture succeeds in the custom dev/release client on the physical Pixel (orbit://openrouter-auth), returning a usable code — device-only, cannot be validated in vitest or on the emulator.", verification: backstop }
  prohibitions:
    - statement: "MUST NOT persist the OpenRouter API key or the intermediate OAuth `code` anywhere except SecureStore — never app_settings, never the backup, never AsyncStorage, never a log line."
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT hardcode OpenRouter model ids, pricing, or a frozen recommendation list into product logic or UI — recommendations and prices are runtime/catalog data."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT fetch the catalog on a read path, screen mount, or app launch — only on explicit Refresh Models or the first settings-open of a new local day (the catalog GET carries no key and no contact data)."
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - src/ai/openrouter-oauth.ts
    - src/ai/openrouter-catalog.ts
    - "SecureStore slot orbit.ai.key.openrouter"
  key_links:
    - "openrouter-oauth stores the resulting key via ai-key-store `keyItemName('openrouter')` → SecureStore only"
    - "openrouter-catalog cache is offline-safe: loadCachedCatalog returns cache-or-null and never throws; refresh throws on failure and leaves prior cache intact"
    - "expo-web-browser / expo-auth-session / expo-crypto installed via `npx expo install` (SDK-57-aligned) — requires a new dev-client build"
---

<objective>
Build the OpenRouter connection lane — the phase's only net-new external integration — as its own
OAuth-PKCE browser-auth flow and its own model catalog + pricing source, reusing Orbit's SecureStore
credential boundary and egress guards. This expands the tracer's connection model with the recommended
lane without touching the LiteLLM catalog that serves direct BYOK.

Purpose: Isolate the highest-risk unknown (browser redirect capture + Hermes crypto) into one plan with
a device spike, and add live pricing so the cost estimate (Plan 06) has a real source.
Output: `openrouter-oauth.ts` (PKCE), `openrouter-catalog.ts` (fetch/cache/pricing), the new SecureStore
key slot, and the three installed Expo auth packages.

**Decisions honored (CONTEXT.md):** D-04 (OpenRouter key lives only in SecureStore, never app_settings/backup), D-05 (OpenRouter adds one new egress host + browser-OAuth path already decided in the dossier — not widened further; egress guards preserved), D-10 (OpenRouter as its own catalog source, not forced into the LiteLLM CatalogProvider union).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-ai-configuration-prompting/36-RESEARCH.md
@.planning/phases/36-ai-configuration-prompting/36-PATTERNS.md
@.planning/phases/36-ai-configuration-prompting/COVERAGE.md

# Analog / safety files (read before editing)
@src/ai/model-catalog-cache.ts
@src/ai/secure-fetch.ts
@src/ai/custom-endpoint.ts
@src/services/ai-key-store.ts
@app.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify the three Expo auth packages before install (package legitimacy)</name>
  <what-built>The plan requires three new native Expo packages — `expo-web-browser` (57.0.3), `expo-auth-session` (57.0.12), `expo-crypto` (57.0.3) — for the OpenRouter browser-auth flow. RESEARCH Package Legitimacy Audit verdict: OK for all three (official `@expo`-published core modules, version-locked to the installed SDK). No install has run yet.</what-built>
  <how-to-verify>
    1. Confirm each package is the official first-party Expo module (publisher `expo`) at `npmjs.com/package/expo-web-browser`, `npmjs.com/package/expo-auth-session`, `npmjs.com/package/expo-crypto`.
    2. Confirm the SDK-57 version line matches (57.0.3 / 57.0.12 / 57.0.3).
    3. On approval, the executor installs via `npx expo install expo-web-browser expo-auth-session expo-crypto` (NOT bare npm install — versions must resolve against SDK 57) and schedules a new dev-client build (native modules do not hot-reload).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the install, or name any package that fails the check.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: OpenRouter OAuth PKCE + redirect + key exchange → SecureStore</name>
  <files>src/ai/openrouter-oauth.ts, src/ai/openrouter-oauth.test.ts, app.config.ts, src/services/ai-key-store.ts</files>
  <read_first>src/services/ai-key-store.ts (:26-53 keyItemName/SecureStore, no bulk accessor; AiCloudProviderId now includes openrouter from Plan 01), app.config.ts (scheme `orbit`), src/ai/secure-fetch.ts (egress boundary), 36-RESEARCH.md Pattern 1 (OAuth field names) + Pitfall 3 (Hermes crypto) + MEMORY hermes-crypto-guard</read_first>
  <behavior>
    - PKCE: `code_verifier` is a guarded-random base64url string; `code_challenge` = base64url(SHA-256(verifier)) via `expo-crypto` `digestStringAsync(SHA256, verifier, {encoding: BASE64})`; method `S256`.
    - Anti-CSRF: an in-memory `state` is a guarded-random string, generated per attempt and appended to the auth URL as `&state=<state>`.
    - Auth URL = `https://openrouter.ai/auth?callback_url=<redirect>&code_challenge=<challenge>&code_challenge_method=S256&state=<state>`.
    - Callback validation BEFORE exchange: require `res.type==='success'`; accept ONLY a URL whose scheme+host is exactly `orbit://openrouter-auth` (reject any other scheme/host); reject unexpected/extra params; if `state` is echoed it MUST equal the generated one; parse `?code=` only after these pass.
    - One-time handling: the code is consumed once; a duplicate/second redirect is ignored; the verifier + state are cleared after the exchange (success OR failure).
    - On a validated redirect, POST `https://openrouter.ai/api/v1/auth/keys` with `{code, code_verifier, code_challenge_method:'S256'}`; on `{key}` store via ai-key-store `orbit.ai.key.openrouter`.
    - The intermediate `code`, verifier, and state are never persisted to durable storage or logged.
  </behavior>
  <action>Write the node-pure PKCE derivation + callback-validation + response-parsing tests first (verifier/challenge shape, S256, `state` generation, exact-callback acceptance/rejection, malformed/foreign-host/duplicate-redirect rejection, code extraction, key-store call), then implement `openrouter-oauth.ts`. Use `expo-crypto` for SHA-256 AND for the guarded-random verifier + `state` (Hermes has no `crypto.subtle`/`randomUUID`; guard any random with the RNQC/expo-crypto fallback per MEMORY hermes-crypto-guard) and `expo-web-browser` `openAuthSessionAsync(authUrl, redirect)` for the browser session. Implement the callback validator as a node-pure function (given the returned URL + expected redirect + expected state → accept/reject) so the malicious/malformed-callback cases are unit-tested. Add the `orbit://openrouter-auth` redirect handling in `app.config.ts` (Android intent-filter / singleTask per Expo docs). Inject `fetch` + the WebBrowser opener + the key-store setter so the module is node-testable with fakes. The resulting key goes ONLY to SecureStore via ai-key-store (no app_settings, no backup, no log). Keep egress on the validated boundary. Mark the on-device redirect capture (incl. confirming whether OpenRouter echoes `state`) as a device-UAT item (Pixel, custom dev client — the emulator cannot validate OAuth); during UAT the OAuth connect + a `/models` fetch are safe but a Draft/Rewrite generation call is NOT without owner clearing (MEMORY no-ai-api-calls-without-clearing).</action>
  <verify>
    <automated>npx vitest run src/ai/openrouter-oauth.test.ts</automated>
  </verify>
  <done>PKCE challenge derivation, `state` generation, exact-callback validation (accepting the valid redirect, rejecting foreign-host/malformed/duplicate), redirect-code extraction, and key-exchange → SecureStore are node-tested green; redirect capture + `state`-echo behavior flagged for on-device UAT on the Pixel.</done>
  <precondition>A new dev-client build including the three native Expo auth modules is installed on the Pixel before the OAuth redirect can be exercised on device (native modules do not hot-reload).</precondition>
  <acceptance_criteria>
    - `code_challenge_method` is `S256` and the challenge is `expo-crypto` SHA-256 of the verifier (not `crypto.subtle`).
    - A generated in-memory `state` is appended to the auth URL and validated on callback when echoed; the verifier + state are cleared after exchange.
    - The callback validator ACCEPTS an exact `orbit://openrouter-auth?code=…` and REJECTS a foreign scheme/host, an unexpected extra param, a mismatched `state`, and a second/duplicate redirect (unit-tested).
    - The OpenRouter key is written only through ai-key-store; grep proves no app_settings/backup/AsyncStorage/log path receives the key, code, verifier, or state.
    - `app.config.ts` declares the `orbit://openrouter-auth` redirect.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: OpenRouter model catalog + pricing (separate source, offline-safe cache)</name>
  <files>src/ai/openrouter-catalog.ts, src/ai/openrouter-catalog.test.ts</files>
  <read_first>src/ai/model-catalog-cache.ts (:1-45 injected fetchImpl+storage, loadCachedCatalog returns cache-or-null and never throws, refresh throws on failure leaving cache intact, privacy contract :5-13), src/ai/model-catalog-filter.ts (:44-51 CatalogProvider — do NOT extend), src/db/ai-context-read.ts (:47 formatLocalDate day-boundary usage), 36-RESEARCH.md Pattern 2</read_first>
  <action>Create `openrouter-catalog.ts` mirroring `model-catalog-cache.ts` structure but as a SEPARATE source (do not extend the LiteLLM `CatalogProvider` union). Fetch `GET https://openrouter.ai/api/v1/models` (public, data-only, NO key, NO contact data) and cache to an offline-safe store: `loadCachedOpenRouterCatalog` returns parsed cache or null and NEVER throws (read path always resolves — local-first); `refreshOpenRouterCatalog` THROWS on network/non-2xx/shape-broken and leaves the prior cache untouched. Parse the model object fields `id`, `name`, `context_length`, `pricing.{prompt,completion,image,request}` (pricing are decimal USD-per-token STRINGS). Expose a helper computing input-cost = `estimatedInputTokens * Number(pricing.prompt)` for a selected model. Gate refresh on the first settings-open of a new local day using `formatLocalDate()` (never `toISOString().split('T')[0]`) plus an explicit refresh entry point; inject `fetchImpl` + `storage` for node-pure testing. Do NOT hardcode model ids/prices; the curated recommendation mapping is data, not constants baked into logic.</action>
  <verify>
    <automated>npx vitest run src/ai/openrouter-catalog.test.ts</automated>
  </verify>
  <done>Catalog fetch/parse/cache/pricing + the daily-refresh local-day boundary are node-tested; a failed refresh preserves the prior cache; read never throws.</done>
  <acceptance_criteria>
    - `loadCachedOpenRouterCatalog` returns null (not throw) when the cache is empty.
    - A simulated non-2xx / malformed `refreshOpenRouterCatalog` throws and the cached catalog is unchanged.
    - The day-boundary check calls `formatLocalDate()`; grep proves no `toISOString().split` in the file.
    - Input-cost uses `Number(pricing.prompt)`; no hardcoded model id or price literal in the module.
    - OpenRouter is NOT added to `model-catalog-filter.ts` `CatalogProvider`.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- **New files:** `src/ai/openrouter-oauth.ts` (PKCE + browser auth + key exchange), `src/ai/openrouter-catalog.ts` (fetch/cache/pricing + input-cost helper).
- **SecureStore slot:** `orbit.ai.key.openrouter` (via existing ai-key-store keyItemName).
- **app.config.ts:** `orbit://openrouter-auth` redirect (Android intent-filter/singleTask).
- **Dependencies:** `expo-web-browser`, `expo-auth-session`, `expo-crypto` (SDK-57) — `package.json` + `package-lock.json` both updated via `npx expo install`.
- **Curation source (clarified):** curated-first ordering consumes `src/ai/model-registry.ts` (curated ids + reason category) matched against the live/cached catalog; no frozen prices/ids in product logic.
- **Functions:** `loadCachedOpenRouterCatalog`, `refreshOpenRouterCatalog`, OpenRouter input-cost estimate helper, OpenRouter connect/exchange functions.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → openrouter.ai | new egress host (auth + catalog + generation) — already owner-decided in the dossier |
| browser redirect → app | the OAuth `code` re-enters the app via the `orbit://` redirect |
| OAuth key → SecureStore | the returned API key must land only in SecureStore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05 | Spoofing | OAuth code interception / login-CSRF | high | mitigate | PKCE S256 binds the code to the verifier; an in-memory anti-CSRF `state` is generated per attempt and validated on callback (when echoed); the intermediate code/verifier/state are never persisted (canon OAuth control — also breadcrumb `/gsd-secure-phase`) |
| T-36-06 | Information disclosure | OpenRouter key at rest | high | mitigate | key → SecureStore only via ai-key-store; no app_settings/backup/AsyncStorage/log path (grep-asserted) |
| T-36-07 | Tampering | malicious/malformed redirect callback | high | mitigate | require `type==='success'`; accept ONLY an exact `orbit://openrouter-auth` scheme+host; reject foreign scheme/host/unexpected params; validate echoed `state`; one-time code consumption (duplicate redirect ignored) — node-unit-tested validator |
| T-36-08 | Information disclosure | catalog fetch | medium | mitigate | catalog GET is public, key-less, contact-data-less; invoked only on explicit refresh / first settings-open of a new local day, never a read path |
| T-36-09 | Cryptography | Hermes crypto undefined | high | mitigate | `expo-crypto` SHA-256 for the S256 challenge; never `crypto.subtle`; guarded random verifier; device-validated on the Pixel |
| T-36-SC | Tampering | expo package installs | high | mitigate | package-legitimacy checkpoint (blocking-human) before install; all three are official @expo modules |
</threat_model>

<verification>
- `npx vitest run src/ai/openrouter-oauth.test.ts src/ai/openrouter-catalog.test.ts` green; `npx tsc --noEmit` clean.
- `package-lock.json` is updated by `npx expo install` and committed with `package.json` (the lockfile is part of the modified-file contract; verify `git status` shows both).
- Device UAT (Pixel, custom dev client, end-of-phase): OpenRouter browser connect returns a key to SecureStore; catalog renders offline from cache; Refresh Models updates it; confirm the callback validator accepts the real redirect and whether `state` is echoed; NO generation call without owner clearing.
- Grep proves no credential/code/verifier/state reaches app_settings, backup, AsyncStorage, or logs.
</verification>

<success_criteria>
- OpenRouter connects via browser auth with the key in SecureStore only.
- Catalog + real pricing render offline-first and refresh on the daily boundary + manual action.
- OpenRouter remains a separate catalog source from LiteLLM.
</success_criteria>

<output>
Create `.planning/phases/36-ai-configuration-prompting/36-02-SUMMARY.md` when done.
</output>
