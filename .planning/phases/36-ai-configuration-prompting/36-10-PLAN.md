---
phase: 36-ai-configuration-prompting
plan: 10
type: execute
wave: 6
depends_on: [36-03, 36-09]
gap_closure: true
autonomous: true
requirements: [AICFG-08]
files_modified:
  - src/ai/prompt-types.ts
  - src/ai/prompt-template.ts
  - src/ai/message-focus.ts
  - src/ai/message-focus.test.ts
  - src/screens/ComposeScreen.tsx
  - src/screens/settings-ai-logic.ts
  - src/screens/settings-ai-logic.test.ts
must_haves:
  truths:
    - "A session Message Focus selection changes the exact resolved provider payload by marking currently permitted information as especially important, while ordinary permitted context remains available."
    - "Message Focus never grants permission: generation revalidates selected identities against a fresh Research projection and excludes revoked, ineligible, off-limits, duplicate, and over-cap selections."
    - "Focused values remain bounded, fence-neutralized DATA and flow through the same immutable resolvePrompt result used by inspectors and providers."
  artifacts:
    - src/ai/message-focus.ts
    - src/ai/prompt-template.ts
    - src/screens/ComposeScreen.tsx
  key_links:
    - "Compose reads current Research eligibility at request time, projects the session selection, and adds only that projection to PromptContext."
    - "resolvePrompt renders PromptContext.messageFocus in a dedicated emphasis DATA block without removing other context."
---

<objective>
Close the Phase 36 verification gap in AICFG-08 by wiring session-only Message Focus into the immutable prompt payload without widening permission.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement permission-preserving Message Focus projection</name>
  <files>src/ai/message-focus.ts, src/ai/message-focus.test.ts, src/ai/prompt-types.ts</files>
  <action>Add a pure projection that intersects the session selection with a fresh Research read by stable identity, uses the fresh label/value, rejects ineligible and off-limits rows, deduplicates, and caps at three. Add the closed PromptContext focus shape.</action>
  <verify><automated>npx vitest run src/ai/message-focus.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render focus emphasis through the sole immutable prompt builder</name>
  <files>src/ai/prompt-template.ts, src/ai/prompt-template.test.ts</files>
  <action>Render current focused items as bounded, fence-neutralized DATA under an Orbit-owned instruction that increases relevance without excluding ordinary permitted context or copying values verbatim into the draft.</action>
  <verify><automated>npx vitest run src/ai/prompt-template.test.ts</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Wire generation to a fresh permission snapshot</name>
  <files>src/screens/ComposeScreen.tsx, src/screens/settings-ai-logic.ts, src/screens/settings-ai-logic.test.ts</files>
  <action>At prompt resolution, read current Research eligibility, project the current session selection, attach it to the complete PromptContext, and continue through the existing resolvePrompt/capacity/provider path unchanged. Include the exact Message Focus DATA block in Compose's contact-specific disclosure.</action>
  <verify><automated>npx vitest run src/ai/message-focus.test.ts src/ai/prompt-template.test.ts src/logic/ai-suggestion-compose-integration.test.ts src/db/compose-research-read.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
</task>

</tasks>
