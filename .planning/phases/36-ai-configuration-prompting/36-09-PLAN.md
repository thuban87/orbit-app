---
phase: 36-ai-configuration-prompting
plan: 09
type: execute
wave: 5
depends_on: [36-01, 36-04, 36-05, 36-06, 36-07]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/settings-ai-hub-logic.ts
  - src/screens/settings-ai-hub-logic.test.ts
autonomous: true
requirements: [AICFG-01]
user_setup: []

# WHY THIS PLAN EXISTS (review HIGH #2): the new AI screens (AIConnection, AIModelPicker,
# AIPersonalization, AIPermissions, AIPreview) were created by 36-04/05/06/07 but NO plan
# registered their routes (SettingsStack.tsx / navigation/types.ts) or added an entry point,
# so they were unreachable. Separately, AICFG-01's master-toggle UI + AI-off simplified state
# were unowned (36-01 builds the store/logic but renders no UI). This plan owns both: it is the
# last UI wave (all screens exist by wave 4), runs parallel to 36-08 (no shared files).

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- AICFG-01 master toggle UI + AI-off simplified state (was unowned) ---
    - "The AI area in Settings renders the real AI Enabled master toggle (accent on-track) bound to ai-config-store (36-01); toggling off disables AI everywhere and toggling on restores immediately when the active connection + model remain valid — the toggle mutates only `ai_enabled`, never connection/model/personalization/permission state (AICFG-01)."
    - "AI OFF collapses the full config hierarchy to the simplified state — the toggle, the preservation note ('AI is off. Your connections, models, personalization, and permissions are saved…'), and a persistent Manage-saved-connections escape hatch that opens connection management WITHOUT re-enabling AI; the full expanded hierarchy is NOT left visible while off (AICFG-01)."
    - "AI ON reveals the six-section hierarchy as routed destinations — Connection, Model, Writing Style + Personalization Context, AI Data Permissions, Preview What Orbit Sends — each reachable from the AI area (AICFG-01)."
    - "`AI off` and `AI misconfigured` are distinct states — off never renders as Needs Attention (AICFG-01)."
    # --- routing / reachability (review HIGH #2) ---
    - "Every new AI screen is registered as a Settings route and reachable from an entry point: `AIConnection` (36-05), `AIModelPicker` (36-05), `AIPersonalization` (36-06), `AIPermissions` (36-04), `AIPreview` (36-07) each appear in `SettingsStackParamList` (navigation/types.ts) and `SettingsStack.tsx`, and each has a navigable entry from the AI area — none compiles-clean-but-unreachable (AICFG-01 reachability, review HIGH #2)."
    - "The legacy inline AI provider/model/key entry UI in SettingsScreen is superseded by the routed connection/model screens so there are NOT two competing provider-config surfaces (the inline UI wrote legacy `AiSettings.aiProvider`/`aiModel`, which 36-01 demoted out of the generation path); shared logic still consumed by the new screens (buildAiSettingsPatch, inspector view-state) is preserved (AICFG-01)."
    - "The first-use disclosure (36-07 component) is mounted in the AI area and shown once on first successful setup, reading the `ai_first_use_disclosed` flag; it never gates generation (AICFG-12 wiring)."
    # --- backstop ---
    - { statement: "Compose's Phase-35 interim needs-attention repair route still lands on a valid AI area/Connection destination after the AI-area rework — device check.", verification: backstop }
    - { statement: "Long provider/endpoint/model names in the AI-area entry rows wrap or truncate without breaking layout — visual UI-state check at verify time.", verification: backstop }
  prohibitions:
    - statement: "MUST NOT leave any new AI screen unreachable — every screen 36-04/05/06/07 created is route-registered AND has an entry point."
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT let the master toggle delete or mutate connection/model/personalization/permission state — off preserves everything and only flips `ai_enabled`."
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT render the full AI config hierarchy while AI is off — off shows the simplified state (toggle + preservation note + Manage-saved-connections escape hatch)."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT keep a second live provider-config surface (the legacy inline AI UI) that writes legacy columns generation no longer reads — it is superseded by the routed screens."
      category: values
      status: resolved
      verification: judgment
  artifacts:
    - src/screens/settings-ai-hub-logic.ts
    - "SettingsScreen AI area (master toggle + AI-off simplified state + six-section routed entries)"
    - "SettingsStackParamList routes: AIConnection, AIModelPicker, AIPersonalization, AIPermissions, AIPreview"
  key_links:
    - "SettingsStack.tsx imports + registers the five AI screens; navigation/types.ts adds them to SettingsStackParamList"
    - "master toggle + AI-off state read/write ai-config-store (36-01); entry points navigate to the routed screens"
    - "first-use disclosure (36-07) mounted in the AI area, gated on ai_first_use_disclosed (36-01 migration-029 column)"
    - "Compose interim repair route (36-01) continues to land on the Settings AI area after the rework"
---

<objective>
Make the phase's AI screens REACHABLE and render the AI Enabled master toggle + AI-off simplified
state — the two integration surfaces the review found unowned (HIGH #2 routing; the AICFG-01 UI that
36-01's store/logic backs but renders nowhere). This is the last UI wave: it registers the five new
Settings routes, adds their entry points, reworks the Settings AI area into the master toggle + AI-off
simplified state + six-section routed hierarchy, retires the legacy inline provider-config UI (superseded
by the routed screens and by 36-01's generation promotion), and mounts the first-use disclosure.

Purpose: Without this plan the AI screens compile clean but are unreachable, and the master toggle has
no UI — the phase would "test green" while the user cannot get to any of it.
Output: navigation registration (types + stack), `settings-ai-hub-logic.ts`, and the reworked
SettingsScreen AI area.

**Decisions honored (CONTEXT.md):** D-01/D-02 (dossier + planning-notes — the six-section AI hierarchy
and AI-off simplified state are the owner-ratified UI-SPEC Surface #1 shape), D-09 (the master toggle is
unbuilt — its UI is built here on 36-01's store), D-11 (off is a distinct state from Needs Attention).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-ai-configuration-prompting/36-PATTERNS.md
@.planning/phases/36-ai-configuration-prompting/36-UI-SPEC.md

# Analog / source-of-truth files (read before editing)
@src/navigation/types.ts
@src/navigation/tabs/SettingsStack.tsx
@src/screens/SettingsScreen.tsx
@src/screens/settings-ai-logic.ts
@src/stores/ai-config-store.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Register the five AI routes + one reachable entry point (thin end-to-end)</name>
  <files>src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>src/navigation/types.ts (:228-269 SettingsStackParamList — the existing route-param pattern incl SystemsManagement/CustomFields), src/navigation/tabs/SettingsStack.tsx (:1-104 the `<Stack.Screen>` registration pattern + screen imports), src/screens/SettingsScreen.tsx (the existing Settings-row navigation pattern, e.g. how SystemsManagement/CustomFields rows call `navigation.navigate`), src/screens/AIConnectionScreen.tsx (36-05 — the first screen to route to), 36-UI-SPEC.md Surface #1/#2</read_first>
  <action>Add the five new routes to `SettingsStackParamList` in `navigation/types.ts` — `AIConnection`, `AIModelPicker` (params: the connection/lane identity it configures), `AIPersonalization`, `AIPermissions`, `AIPreview` — following the existing route-param typing pattern (undefined where no params). Import + register each as a `<Stack.Screen>` in `SettingsStack.tsx` mirroring the existing screens. Then wire ONE end-to-end entry point as the tracer: an AI-area row in `SettingsScreen.tsx` that `navigation.navigate("AIConnection")` and renders the AIConnectionScreen. Prove the thinnest reachable path (Settings → AI Connection) works before the full AI-area rework in Task 3. Do not remove the legacy inline AI UI yet (Task 3).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All five AI routes are typed + registered and AIConnection is reachable from a Settings AI-area row; the project typechecks. Committed.</done>
  <acceptance_criteria>
    - `SettingsStackParamList` includes `AIConnection`, `AIModelPicker`, `AIPersonalization`, `AIPermissions`, `AIPreview` (grep-asserted in navigation/types.ts).
    - `SettingsStack.tsx` registers a `<Stack.Screen>` for each of the five (grep-asserted).
    - A Settings AI-area row navigates to `AIConnection` (one reachable path proven).
    - `npx tsc --noEmit` clean (screen imports resolve — all five screens exist by this wave).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: settings-ai-hub-logic — master toggle + AI-off/on state derivation</name>
  <files>src/screens/settings-ai-hub-logic.ts, src/screens/settings-ai-hub-logic.test.ts</files>
  <read_first>src/stores/ai-config-store.ts (36-01 — aiEnabled + setter), src/logic/ai-availability.ts (36-01 — off vs needs-attention distinction), 36-UI-SPEC.md Surface #1 + Copywriting (AI-off preservation note, escape hatch, six-section list), dossier §B/§F</read_first>
  <behavior>
    - The hub derives a view state from `aiEnabled`: OFF → { showSimplified: true, sections: [] , escapeHatch: true }; ON → { showSimplified: false, sections: [the six routed entries in order] }.
    - Toggling off returns the identical persisted config (only `ai_enabled` changes); toggling twice is idempotent.
    - OFF is distinct from Needs Attention — the derivation never maps OFF to a needs-attention view.
    - The six-section entry list is Connection · Model · Writing Style + Personalization · AI Data Permissions · Preview What Orbit Sends, each mapping to its route.
  </behavior>
  <action>Write RED tests first, then implement `settings-ai-hub-logic.ts` (node-pure): a `deriveAiHubState(aiEnabled, availability)` returning the AI-off simplified descriptor vs the AI-on six-section descriptor (each section carrying its route name + label), and a helper confirming a toggle mutates only `ai_enabled` (assert the descriptor never includes a needs-attention state for OFF). Keep it node-pure so the SettingsScreen consumes it without embedding decision logic. The section order + labels come from UI-SPEC Surface #1 (tunable copy).</action>
  <verify>
    <automated>npx vitest run src/screens/settings-ai-hub-logic.test.ts</automated>
  </verify>
  <done>The hub logic derives the AI-off simplified state and the AI-on six-section list deterministically; OFF is never a needs-attention state; node-tested.</done>
  <acceptance_criteria>
    - `deriveAiHubState(false, …)` yields the simplified descriptor (escape hatch present, no expanded hierarchy) and never a needs-attention state.
    - `deriveAiHubState(true, …)` yields the six routed sections in the UI-SPEC order.
    - A test asserts the toggle descriptor changes only the enabled flag (no config-mutation field).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rework the SettingsScreen AI area — master toggle, AI-off simplified state, routed entries, retire legacy inline UI, mount first-use disclosure</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>src/screens/SettingsScreen.tsx (:321+ the existing "Optional-AI section" inline provider/model/key entry UI + its ~20 state hooks; the Settings-row nav pattern), src/screens/settings-ai-hub-logic.ts (Task 2), src/stores/ai-config-store.ts (36-01), src/components/AIFirstUseDisclosure.tsx (36-07 — component + should-show logic), src/screens/settings-ai-logic.ts (buildAiSettingsPatch + inspector view-state STILL used by the new screens — do NOT delete shared logic), 36-UI-SPEC.md Surface #1 + Copywriting (preservation note, escape hatch, section labels)</read_first>
  <action>Rework the SettingsScreen AI area to the UI-SPEC Surface #1 shape using `deriveAiHubState` (Task 2): render the AI Enabled master toggle (accent on-track, bound to ai-config-store); when OFF show the simplified state (toggle + preservation note "AI is off. Your connections, models, personalization, and permissions are saved…" + a persistent Manage-saved-connections escape hatch that opens connection management WITHOUT re-enabling AI); when ON show the six routed entry rows (Connection → AIConnection, Model → AIModelPicker, Writing Style + Personalization → AIPersonalization, AI Data Permissions → AIPermissions, Preview What Orbit Sends → AIPreview). Retire the legacy inline provider/model/key-entry AI UI (superseded by the routed AIConnection/AIModelPicker screens and by 36-01's generation promotion off the legacy columns) — but FIRST grep every consumer of the shared `settings-ai-logic` symbols and the inline state, and preserve anything still used by the new screens (e.g. buildAiSettingsPatch's no-key patch path, inspector view-state used by 36-07); remove only the now-dead inline widgets, not shared logic. Mount `AIFirstUseDisclosure` (36-07) in the AI area so it shows once on first successful setup (gated on `ai_first_use_disclosed`). Ensure the Compose interim needs-attention repair route (36-01) still lands on this AI area. All colours via theme tokens; icon-only controls declare accessibilityLabel; distinct OFF vs Needs-Attention states.</action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit</automated>
  </verify>
  <done>The Settings AI area renders the master toggle + AI-off simplified state + six routed entries; the legacy inline provider UI is retired without breaking shared logic; the first-use disclosure is mounted; project typechecks + colours clean.</done>
  <acceptance_criteria>
    - AI OFF renders the simplified state (toggle + preservation note + Manage-saved-connections escape hatch) and NOT the expanded hierarchy; AI ON renders the six routed entries.
    - Each of the six entries navigates to its registered route (all five AI screens reachable).
    - The legacy inline provider/model/key entry UI is removed from SettingsScreen; `buildAiSettingsPatch`/inspector view-state still consumed by the new screens are intact (grep-asserted; no orphaned import).
    - `AIFirstUseDisclosure` is mounted and shows once (gated on `ai_first_use_disclosed`).
    - `npm run check:colors` + `npx tsc --noEmit` clean project-wide.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- **New file:** `src/screens/settings-ai-hub-logic.ts` (`deriveAiHubState` + hub view-state helpers).
- **Modified:** `src/navigation/types.ts` (SettingsStackParamList gains AIConnection/AIModelPicker/AIPersonalization/AIPermissions/AIPreview), `src/navigation/tabs/SettingsStack.tsx` (registers those five screens), `src/screens/SettingsScreen.tsx` (AI area reworked: master toggle + AI-off simplified state + six routed entries; legacy inline AI UI retired; first-use disclosure mounted).
- **Behavior:** all AI screens reachable; AI Enabled master toggle + AI-off simplified state; single provider-config surface (routed, not inline).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → AI master toggle | the toggle is a privacy control: OFF must actually stop all AI egress affordances |
| user → AI navigation | routed screens must be reachable but must not bypass the master toggle / availability gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-34 | Elevation/misconfig | master toggle not actually gating AI | high | mitigate | the toggle writes `ai_enabled` (36-01); availability="off" hides every AI affordance and generation is gated on `ai_enabled`; OFF is a distinct state (never Needs Attention) — test-asserted in 36-01 + hub logic |
| T-36-35 | Tampering/data loss | disabling AI deleting saved config | high | mitigate | the toggle mutates only `ai_enabled`; connection/model/personalization/permission state is preserved (36-01 idempotency + hub-logic test) |
| T-36-36 | Repudiation/transparency | an unreachable AI screen (green-but-broken) | medium | mitigate | every AI screen is route-registered AND has an entry point; tsc + a reachability check per route |
</threat_model>

<verification>
- `npx vitest run src/screens/settings-ai-hub-logic.test.ts` green; `npx tsc --noEmit` + `npm run check:colors` clean project-wide.
- Grep gates: all five AI routes present in navigation/types.ts + SettingsStack.tsx; the legacy inline AI UI removed from SettingsScreen; shared settings-ai-logic symbols still consumed by the new screens intact.
- Device UAT (Pixel, end-of-phase): toggle AI off → simplified state + escape hatch, config preserved; toggle on → six sections reachable; open each AI screen from Settings; first-use disclosure shows once; Compose interim repair route lands on the AI area.
</verification>

<success_criteria>
- All five new AI screens are reachable from a reworked Settings AI area (review HIGH #2 closed).
- The AI Enabled master toggle + AI-off simplified state render and preserve config (AICFG-01 UI).
- One provider-config surface (routed), not two; first-use disclosure mounted.
</success_criteria>

<output>
Create `.planning/phases/36-ai-configuration-prompting/36-09-SUMMARY.md` when done.
</output>
