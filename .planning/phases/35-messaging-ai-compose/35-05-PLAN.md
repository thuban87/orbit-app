---
phase: 35-messaging-ai-compose
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/prompt-types.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
  - src/ai/prompt-template.test.ts
  - src/db/fuel-read.test.ts
autonomous: true
requirements: [COMP-10]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-10 / D-14 / ADR-107 (OFF LIMITS EXCLUDED FROM ALL AI EGRESS): Off Limits is NEVER carried toward AI in any form — not as positive context (already excluded) and not as a negative avoidance constraint. D-14/ADR-107 (owner-ratified) reverses ADR-078's off-limits egress widening; plan 35-05 carries NO `avoidanceConstraints` shape. `fuel-read.ts`'s off-limits exclusion stays UNRELAXED and NO `fuel` AI-permission column is added."
    - "COMP-10 / D-13 / ADR-078 (gated-note CARRY ONLY): PromptContext / ai-context-read is extended to CARRY exactly ONE new shape — the gated recent-interaction note (note carried only where that interaction's allow_ai=1). The exact prompt-template rendering/transmission of this NEW addition is Phase 36's job; this phase does not widen actual egress beyond today's authorized projection. This half of ADR-078 is UNCHANGED by D-14 and remains in force."
    - "COMP-10 / HIGH-4 (file scope): the ONE new shape field `gatedRecentInteractionNotes?` is added to `PromptContext` in src/ai/prompt-types.ts (VERIFIED on disk: that is where PromptContext lives — the CLOSED outbound allowlist at prompt-types.ts:111 — NOT ai-context-read.ts). D-13 authorizes extending PromptContext to CARRY this shape; adding the field here is authorized carry-only, not egress widening. It is OPTIONAL (mirroring the existing `sharedMemories?` precedent — the read boundary always supplies an array), so existing PromptContext fixtures/builders do not break."
    - "COMP-10 / HIGH-4 (carry-only proven by test): resolvePrompt in prompt-template.ts explicitly SERIALIZES selected fields (verified on disk: it reads context.rankedFuel etc. field-by-field, never spreads the context) — so adding a field to PromptContext does NOT transmit it. A prompt-template.test.ts regression test proves the new gated-note shape does NOT appear in the resolved provider payload (prompt/inspectorDisplay/payload) until Phase 36 adds explicit rendering."
    - "COMP-10 (egress invariants preserved): the interaction read still selects only channel/quality/connected for the un-gated path; fuel still flows only through getRankedFuel (off_limits / unconfirmed-AI / blank excluded IN SQL); memories only through listAiEligibleMemories (allow_ai=1 AND deleted_at IS NULL); custom fields only where share_with_ai=1; every value ?-bound, only isSafeColName-guarded identifiers interpolated."
    - "COMP-10 / D-14 (off-limits never projected): no AI-facing projection — positive OR negative — ever contains an Off Limits value; a test asserts an off-limits row appears in NEITHER the positive fuel projection NOR any new carried shape."
    - "COMP-10 (empty): with no AI-eligible interaction notes the carried gated-note collection is empty (not null-crashing, not defaulting to including excluded data); deterministic ordering is asserted."
    - "D-08: an interaction note is carried only where interactions.allow_ai=1 (column exists since migration 025); Group Notes are NEVER AI-eligible in Compose context under any state."
    - "D-13 / D-14 (scope boundary): no off-limits text is added as positive OR negative context; no interaction note is added as positive context; fuel-read.ts exclusions are NOT edited; any future re-widening of off-limits egress reverses ADR-107 and is an owner decision."
    - "formatLocalDate() is used for every timestamp in the projection — never toISOString().split('T')[0]."
  artifacts:
    - "prompt-types.ts: PromptContext gains ONE optional gatedRecentInteractionNotes[] shape field (carry-only, closed-allowlist addition per D-13); NO off-limits avoidanceConstraints shape (removed per D-14/ADR-107)"
    - "ai-context-read.ts: readPromptContext populates the gated-note shape from the allow_ai-gated interaction read; carries NO off-limits data in any form"
    - "prompt-template.test.ts: regression proving the gated-note shape never serializes into the resolved payload this phase"
    - "extended ai-context-read.test.ts + fuel-read.test.ts locking the exclusion invariants (incl. off-limits never AI-projected)"
  key_links:
    - "ai-context-read is the SOLE closed egress projection → Phase 36 renders the carried gated-note shape into the prompt template"
    - "off_limits exclusion stays structural in fuel-read SQL (RANKED_FUEL_EXCLUSIONS) AND off-limits is never carried toward AI in any form (ADR-107); allow_ai gate on interaction notes (ADR-078, unchanged)"
  prohibitions:
    - "Must never carry or transmit ANY Off Limits item toward AI — positive OR negative avoidance constraint (D-14/ADR-107 reverses ADR-078's off-limits egress)."
    - "Must never add a `fuel` AI-permission column or otherwise relax fuel-read.ts's off_limits exclusion."
    - "Must never transmit a Group Note to AI (never AI-eligible in Compose context)."
    - "Must never transmit an interaction note whose allow_ai is OFF."
    - "Must never add off-limits text as positive OR negative context, or interaction notes as POSITIVE context, in ai-context-read.ts."
    - "Must never edit resolvePrompt/prompt-template.ts to SERIALIZE the new gated-note shape — carry-only this phase; rendering is Phase 36 (D-13)."
---

<objective>
Extend the SOLE closed egress projection (`ai-context-read.ts`) to CARRY exactly ONE new ADR-078 shape — the allow_ai-gated recent-interaction note — WITHOUT widening actual egress (D-13, owner-resolved). Off Limits is EXCLUDED from all AI egress (D-14/ADR-107, owner-ratified — reverses ADR-078's off-limits widening): this plan carries NO off-limits avoidance-constraint shape, adds NO `fuel` AI-permission column, and leaves `fuel-read.ts`'s off-limits exclusion UNRELAXED. Phase 35 owns the human-facing display (the Research "Avoid" group and never-Message-Focus rule live in plan 35-06/35-09) and the one closed-contract gated-note shape; Phase 36 owns the exact prompt-template rendering/transmission. This plan is the data-minimization boundary work: add the gated-note field and lock the existing exclusion invariants (including off-limits) with tests so no future edit silently widens egress.

Purpose: satisfy the off-limits success criterion (SC-4 off-limits half) as "shown to the human on Research + NEVER sent to AI" (D-14) and carry the gated-note shape, while keeping egress exactly as authorized today (Trip-Wire 3).
Output: the gated-note PromptContext extension + hardened egress-exclusion tests (off-limits never projected).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
@docs/decisions/ADR-107-off-limits-excluded-from-all-ai-egress.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry the ADR-078 gated recent-interaction-note shape; Off Limits excluded from all AI egress (D-14/ADR-107)</name>
  <files>src/ai/prompt-types.ts, src/db/ai-context-read.ts, src/db/ai-context-read.test.ts</files>
  <behavior>
    - PromptContext gains ONE shape field: gated recent-interaction notes (note present only where interactions.allow_ai=1).
    - Off Limits is NEVER carried in any AI-facing shape — no positive context, no negative avoidance constraint (D-14/ADR-107). There is NO avoidanceConstraints field.
    - With a recent interaction whose allow_ai=0, its note is absent; with allow_ai=1, its note is carried in the gated collection.
    - A Group Note is never carried (never AI-eligible).
    - An off-limits row appears in NEITHER the positive fuel projection NOR the gated-note collection NOR any other AI-facing shape.
    - Empty inputs → empty (not null) collection; deterministic ordering.
  </behavior>
  <read_first>
    - src/ai/prompt-types.ts (FULL — VERIFIED this session: `PromptContext` (the CLOSED outbound allowlist) is defined here at :111, with the header contract at :1-23 explaining that adding a field is the compile-time egress-widening mechanism; `sharedMemories?` at :138 is the OPTIONAL-while-Phase-36-owns-serialization precedent to mirror for the ONE new field. This file — NOT ai-context-read.ts — is where the new shape TYPE lives.)
    - src/ai/prompt-template.ts (:142-185 — VERIFIED: resolvePrompt SERIALIZES fields explicitly (context.rankedFuel etc.), never spreads context; this is why adding a PromptContext field does not transmit it. Read to confirm the new field is NOT referenced here this phase.)
    - src/db/ai-context-read.ts (FULL — header egress facts :1-27; interaction aggregate selecting only channel/quality/connected :108-122; shared fields/memories :156-210; the builder :221-302; formatLocalDate :47. This is the SOLE projection that BUILDS a PromptContext — populate the gated-note shape here)
    - src/db/ai-context-read.test.ts (FULL — extend, do not rewrite)
    - src/db/fuel-read.ts (:13-135 — RANKED_FUEL_EXCLUSIONS; getRankedFuel excludes off_limits IN SQL; listFuelForEditor is the ONLY read surfacing off_limits. VERIFIED: `fuel` has NO allow_ai/share_with_ai column (migration 011:92) and 028 is app_settings-only — so no AI-enabled/AI-disabled distinction for off-limits exists; per D-14/ADR-107 off-limits is simply never carried. Read to CONFIRM you do NOT relax any exclusion.)
    - src/db/memories-read.ts (:31 MEMORY_AI_ELIGIBILITY = "allow_ai = 1 AND deleted_at IS NULL")
    - docs/decisions/ADR-107-... (FULL — Off Limits excluded from ALL AI egress; supersedes ADR-078's off-limits portion; no fuel AI-permission column; fuel-read exclusion unrelaxed)
    - docs/decisions/ADR-078-... (the STILL-IN-FORCE half: 3-most-recent-interaction projection with note only when allow_ai=1; Group Notes never sent; keep context bounded/delimited against injection. Its off-limits avoidance-constraint provision is SUPERSEDED by ADR-107 — do NOT implement it.)
    - CLAUDE.md data-layer rules (ADR-107 supersession; egress is an owner decision to widen)
  </read_first>
  <action>
    Add ONE new OPTIONAL shape field to the `PromptContext` type in src/ai/prompt-types.ts — `gatedRecentInteractionNotes?` (recent-interaction note carried ONLY where interactions.allow_ai=1) — mirroring the `sharedMemories?` optional precedent, with a header comment marking it carry-only-until-Phase-36. Do NOT add an `avoidanceConstraints` field or any off-limits shape (D-14/ADR-107). Then extend `readPromptContext` (ai-context-read.ts) to POPULATE `gatedRecentInteractionNotes` from the recent-interaction read gated on allow_ai=1 (the read boundary always supplies an array — empty when nothing qualifies). Do NOT read, project, or carry any Off Limits value in any form. Do NOT render or transmit the gated-note shape into a prompt string, and do NOT touch resolvePrompt/prompt-template.ts (Phase 36 owns rendering — D-13). Do NOT add off-limits text or notes to the existing positive fuel/interaction collections. Do NOT edit fuel-read.ts's exclusions and do NOT add a `fuel` AI-permission column. Keep every value ?-bound, only isSafeColName-guarded identifiers interpolated, and use formatLocalDate for timestamps. Extend ai-context-read.test.ts to cover the full behavior list, asserting the positive projection is unchanged (off_limits/Group Notes/allow_ai=0 notes still excluded from egress) and that off-limits appears in NO AI-facing shape, while the gated-note shape carries exactly the authorized allow_ai=1 notes.
  </action>
  <verify>
    <automated>npx vitest run src/db/ai-context-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gatedRecentInteractionNotes" src/ai/prompt-types.ts` shows the ONE field added to PromptContext (optional, `?`), NOT in ai-context-read.ts's own types.
    - `grep -n "avoidanceConstraints" src/ai/prompt-types.ts src/db/ai-context-read.ts` returns nothing (off-limits avoidance shape removed per D-14/ADR-107).
    - Test: an off-limits row appears in NEITHER the positive fuel projection NOR the gated-note collection NOR any other AI-facing shape.
    - Test: a recent interaction note is carried only when allow_ai=1; absent when allow_ai=0.
    - Test: a Group Note is never carried in any collection.
    - `git diff src/ai/prompt-template.ts` is empty (resolvePrompt not touched); `git diff src/db/fuel-read.ts` is empty (exclusions untouched, no fuel AI column added).
    - `grep -n "toISOString" src/db/ai-context-read.ts` returns nothing; `grep -n "off_limits" src/db/ai-context-read.ts` shows no off-limits branch at all (neither positive nor negative).
  </acceptance_criteria>
  <done>PromptContext (in prompt-types.ts) carries the ONE ADR-078 gated-note shape, readPromptContext populates it, Off Limits is carried in no AI-facing shape (D-14/ADR-107), and the positive-projection exclusions are proven intact — with no egress widening.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the egress-exclusion invariants (regression fence)</name>
  <files>src/db/fuel-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.test.ts (FULL — existing exclusion coverage)
    - src/db/fuel-read.ts (RANKED_FUEL_EXCLUSIONS + the two off_limits guards at :95 and :121)
  </read_first>
  <action>
    Extend fuel-read.test.ts to lock the structural exclusions that Phase 35 must never relax: getRankedFuel excludes off_limits / unconfirmed-AI / blank in SQL, and listFuelForEditor is the ONLY read that surfaces off_limits. These tests are a regression fence so any future accidental widening of the positive projection fails loudly (Trip-Wire 3 / Pitfall 2). No production change to fuel-read.ts.
  </action>
  <verify>
    <automated>npx vitest run src/db/fuel-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts getRankedFuel returns no off_limits row for a contact that has one.
    - Test asserts listFuelForEditor DOES include the off_limits row (the one editor read).
    - `git diff src/db/fuel-read.ts` is empty.
  </acceptance_criteria>
  <done>The off_limits/AI-eligibility exclusion invariants are fenced by tests with no production change.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the new shapes are carry-only (prompt-template regression)</name>
  <files>src/ai/prompt-template.test.ts</files>
  <read_first>
    - src/ai/prompt-template.test.ts (FULL — the existing resolvePrompt suite + the PromptContext builder/fixture it uses; extend, do not rewrite)
    - src/ai/prompt-template.ts (:142-185 — resolvePrompt serializes selected fields explicitly; the new gated-note shape must NOT appear in prompt/inspectorDisplay/payload)
    - src/ai/prompt-types.ts (the ONE new optional field from Task 1)
  </read_first>
  <action>
    Add a regression test that constructs a PromptContext WITH a non-empty `gatedRecentInteractionNotes` (using a recognizable sentinel value), runs resolvePrompt, and asserts the sentinel does NOT appear in the resolved `prompt`, `inspectorDisplay`, or `payload` — proving Phase 35 carries the shape without transmitting it (D-13; Phase 36 owns rendering). This is the fence that fails loudly if a future edit starts serializing the carried shape before Phase 36. Do NOT reference an `avoidanceConstraints`/off-limits shape (it does not exist per D-14/ADR-107). No production change to prompt-template.ts (the Rewrite source-draft rendering that plan 35-08 adds later is a SEPARATE seam — this regression only fences the carried contact-context shape).
  </action>
  <verify>
    <automated>npx vitest run src/ai/prompt-template.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: a PromptContext carrying a sentinel gatedRecentInteractionNotes resolves to a payload that does NOT contain the sentinel.
    - `git diff src/ai/prompt-template.ts` is empty (no rendering added in this plan).
  </acceptance_criteria>
  <done>A regression test proves the carried gated-note shape never serializes into the resolved provider payload this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite contact data → PromptContext (egress projection) | The sole data-minimization boundary; anything added here can reach the AI provider once Phase 36 renders it. |
| Contact-derived free text → prompt | Untrusted, injectable text must stay bounded/delimited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Information disclosure | ai-context-read shape extension | high | mitigate | Off-limits AI egress is CLOSED by D-14/ADR-107 — Off Limits is carried in NO AI-facing shape (positive or negative); no `fuel` AI-permission column added; fuel-read off-limits exclusion unrelaxed. Only the gated recent-interaction note (allow_ai=1) is carried, carry-only (no rendering/transmission this phase); Group Notes / allow_ai=0 notes never carried; owner-gated widening (D-08/D-13/D-14). |
| T-35-02 | Tampering | contact-derived text in shapes | high | mitigate | Values ?-bound; only isSafeColName-guarded identifiers interpolated; the gated-note text stays bounded/delimited (ADR-078) — Phase 36 renders within the closed projection. |
| T-35-14 | Information disclosure | positive projection regression | medium | mitigate | fuel-read.test.ts + ai-context-read.test.ts fence the exclusions so any accidental widening fails CI. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/db/ai-context-read.test.ts src/db/fuel-read.test.ts src/ai/prompt-template.test.ts` green.
- `npx tsc --noEmit` clean.
- `git diff src/ai/prompt-template.ts src/db/fuel-read.ts` empty (resolvePrompt + exclusions untouched).
</verification>

<success_criteria>
PromptContext (in prompt-types.ts) carries the ONE ADR-078 gated-note shape without widening actual egress; Off Limits is carried in no AI-facing shape (D-14/ADR-107) and fuel-read's off-limits exclusion is unrelaxed with no `fuel` AI-permission column added; a prompt-template regression proves the gated-note shape does not serialize; the positive-projection exclusions (including off-limits) are proven intact and fenced against regression.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/ai/prompt-types.ts` — `PromptContext` gains ONE OPTIONAL `gatedRecentInteractionNotes?` shape field (carry-only, closed-allowlist addition per D-13). NO `avoidanceConstraints`/off-limits shape (removed per D-14/ADR-107).
- `src/db/ai-context-read.ts` — `readPromptContext` populates the gated-note shape from the allow_ai-gated interaction read; carries no off-limits data in any form.
- Extended `src/db/ai-context-read.test.ts`, `src/db/fuel-read.test.ts`, `src/ai/prompt-template.test.ts` (carry-only regression for the gated-note shape + off-limits-never-projected assertions).
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-05-SUMMARY.md` when done.
</output>
