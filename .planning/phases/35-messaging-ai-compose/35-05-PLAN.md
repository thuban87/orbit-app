---
phase: 35-messaging-ai-compose
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/prompt-types.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
  - src/ai/prompt-template.test.ts
  - src/db/fuel-read.test.ts
autonomous: true
requirements: [COMP-10]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-10 / D-13 / ADR-078 (DISPLAY + CARRY ONLY): PromptContext / ai-context-read is extended to CARRY the shapes of (a) the avoidance-constraint (off-limits AI-enabled items as negative constraints) and (b) the gated recent-interaction note (note carried only where that interaction's allow_ai=1) — the exact prompt-template rendering/transmission of these NEW additions is Phase 36's job; this phase does not widen actual egress beyond today's authorized projection."
    - "COMP-10 / HIGH-4 (file scope): the two new shape fields are added to `PromptContext` in src/ai/prompt-types.ts (VERIFIED on disk: that is where PromptContext lives — the CLOSED outbound allowlist at prompt-types.ts:111 — NOT ai-context-read.ts). D-13 explicitly authorizes extending PromptContext to CARRY these shapes; adding fields here is authorized carry-only, not egress widening. The fields are OPTIONAL (mirroring the existing `sharedMemories?` precedent — the read boundary always supplies an array), so existing PromptContext fixtures/builders do not break."
    - "COMP-10 / HIGH-4 (carry-only proven by test): resolvePrompt in prompt-template.ts explicitly SERIALIZES selected fields (verified on disk: it reads context.rankedFuel etc. field-by-field, never spreads the context) — so adding fields to PromptContext does NOT transmit them. A prompt-template.test.ts regression test proves the two new shapes do NOT appear in the resolved provider payload (prompt/inspectorDisplay/payload) until Phase 36 adds explicit rendering."
    - "COMP-10 (egress invariants preserved): the interaction read still selects only channel/quality/connected for the un-gated path; fuel still flows only through getRankedFuel (off_limits / unconfirmed-AI / blank excluded IN SQL); memories only through listAiEligibleMemories (allow_ai=1 AND deleted_at IS NULL); custom fields only where share_with_ai=1; every value ?-bound, only isSafeColName-guarded identifiers interpolated."
    - "COMP-10 (adjacency/ordering): off-limits AI-enabled items are carried ONLY as negative avoidance-constraint shapes, never as positive context, never overlapping the ranked-fuel positive projection; a stable, deterministic ordering is asserted."
    - "COMP-10 (empty): with no AI-eligible items the carried avoidance-constraint / gated-note collections are empty (not null-crashing, not defaulting to including excluded data)."
    - "D-08: an interaction note is carried only where interactions.allow_ai=1 (column exists since migration 025); Group Notes are NEVER AI-eligible in Compose context under any state."
    - "D-13 (scope boundary): no off-limits text or interaction note is added as POSITIVE context; no AI-disabled off-limits item is transmitted; fuel-read.ts exclusions are NOT edited; any further egress widening remains an owner decision."
    - "formatLocalDate() is used for every timestamp in the projection — never toISOString().split('T')[0]."
  artifacts:
    - "prompt-types.ts: PromptContext gains optional avoidanceConstraints[] + gatedRecentInteractionNotes[] shape fields (carry-only, closed-allowlist addition per D-13)"
    - "ai-context-read.ts: readPromptContext populates the two new shapes from AI-eligibility-gated reads"
    - "prompt-template.test.ts: regression proving the new shapes never serialize into the resolved payload this phase"
    - "extended ai-context-read.test.ts + fuel-read.test.ts locking the exclusion invariants"
  key_links:
    - "ai-context-read is the SOLE closed egress projection → Phase 36 renders the carried shapes into the prompt template"
    - "off_limits exclusion stays structural in fuel-read SQL (RANKED_FUEL_EXCLUSIONS); allow_ai gate on interaction notes"
  prohibitions:
    - "Must never transmit an AI-disabled Off-Limits item."
    - "Must never transmit a Group Note to AI (never AI-eligible in Compose context)."
    - "Must never transmit an interaction note whose allow_ai is OFF."
    - "Must never add off-limits text or interaction notes as POSITIVE context."
    - "Must never edit fuel-read.ts's off_limits/AI-eligibility exclusions or add a positive-context branch to ai-context-read.ts."
    - "Must never edit resolvePrompt/prompt-template.ts to SERIALIZE the two new shapes — carry-only this phase; rendering is Phase 36 (D-13)."
---

<objective>
Extend the SOLE closed egress projection (`ai-context-read.ts`) to CARRY the two new ADR-078 shapes — the off-limits avoidance-constraint and the allow_ai-gated recent-interaction note — WITHOUT widening actual egress (D-13, owner-resolved). Phase 35 owns the human-facing display (the Research "Avoid" group and never-Message-Focus rule live in plan 35-06/35-09) and the closed-contract shapes; Phase 36 owns the exact prompt-template rendering/transmission. This plan is the data-minimization boundary work: add the shape fields and lock the existing exclusion invariants with tests so no future edit silently widens egress.

Purpose: satisfy SC-11 at the "display + carry the shape" level while keeping egress exactly as authorized today (Trip-Wire 3).
Output: shape-carrying PromptContext extensions + hardened egress-exclusion tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry the ADR-078 shapes in PromptContext (no egress widening)</name>
  <files>src/ai/prompt-types.ts, src/db/ai-context-read.ts, src/db/ai-context-read.test.ts</files>
  <behavior>
    - PromptContext gains a shape field for avoidance constraints (off-limits AI-enabled items) and a shape field for gated recent-interaction notes (note present only where allow_ai=1).
    - With an off-limits AI-enabled item present, the avoidance-constraint collection carries it as a NEGATIVE shape only; it never appears in the positive ranked-fuel collection.
    - With an AI-disabled off-limits item, nothing is carried for it anywhere.
    - With a recent interaction whose allow_ai=0, its note is absent; with allow_ai=1, its note is carried in the gated collection.
    - A Group Note is never carried (never AI-eligible).
    - Empty inputs → empty (not null) collections; deterministic ordering.
  </behavior>
  <read_first>
    - src/ai/prompt-types.ts (FULL — VERIFIED this session: `PromptContext` (the CLOSED outbound allowlist) is defined here at :111, with the header contract at :1-23 explaining that adding a field is the compile-time egress-widening mechanism; `sharedMemories?` at :138 is the OPTIONAL-while-Phase-36-owns-serialization precedent to mirror for the two new fields. This file — NOT ai-context-read.ts — is where the new shape TYPES live.)
    - src/ai/prompt-template.ts (:150-185 — VERIFIED: resolvePrompt SERIALIZES fields explicitly (context.rankedFuel etc.), never spreads context; this is why adding PromptContext fields does not transmit them. Read to confirm the new fields are NOT referenced here this phase.)
    - src/db/ai-context-read.ts (FULL — header egress facts :1-27; interaction aggregate selecting only channel/quality/connected :108-122; shared fields/memories :156-210; the builder :221-302; formatLocalDate :47. This is the SOLE projection that BUILDS a PromptContext — populate the new shapes here)
    - src/db/ai-context-read.test.ts (FULL — extend, do not rewrite)
    - src/db/fuel-read.ts (:13-135 — RANKED_FUEL_EXCLUSIONS; listFuelForEditor is the ONLY read surfacing off_limits; getRankedFuel excludes off_limits IN SQL. Read to confirm where off-limits AI-enabled items must be sourced for the NEGATIVE shape WITHOUT relaxing the positive-projection exclusion)
    - src/db/memories-read.ts (:31 MEMORY_AI_ELIGIBILITY = "allow_ai = 1 AND deleted_at IS NULL")
    - docs/decisions/ADR-078-... (FULL — off-limits AI-enabled → negative constraint; 3-most-recent-interaction projection with note only when allow_ai=1; Group Notes never sent; the note about keeping context bounded/delimited against injection)
    - CLAUDE.md data-layer rules (ADR-078 supersession; egress is an owner decision to widen)
  </read_first>
  <action>
    Add two new OPTIONAL shape fields to the `PromptContext` type in src/ai/prompt-types.ts — `avoidanceConstraints?` (off-limits AI-ENABLED items as negative-constraint shapes) and `gatedRecentInteractionNotes?` (recent-interaction note carried ONLY where interactions.allow_ai=1) — mirroring the `sharedMemories?` optional precedent, with header comments marking them carry-only-until-Phase-36. Then extend `readPromptContext` (ai-context-read.ts) to POPULATE both: `avoidanceConstraints` from an AI-eligibility-gated off-limits read (NOT by relaxing getRankedFuel's exclusion), `gatedRecentInteractionNotes` from the recent-interaction read gated on allow_ai=1 (the read boundary always supplies an array — empty when nothing qualifies). Do NOT render or transmit them into a prompt string, and do NOT touch resolvePrompt/prompt-template.ts (Phase 36 owns rendering — D-13). Do NOT add off-limits text or notes to the existing positive fuel/interaction collections. Do NOT edit fuel-read.ts's exclusions. Keep every value ?-bound, only isSafeColName-guarded identifiers interpolated, and use formatLocalDate for timestamps. Extend ai-context-read.test.ts to cover the full behavior list, asserting the positive projection is unchanged (off_limits/Group Notes/allow_ai=0 notes still excluded from egress) while the new shapes carry exactly the authorized negative/gated items.
  </action>
  <verify>
    <automated>npx vitest run src/db/ai-context-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "avoidanceConstraints\|gatedRecentInteractionNotes" src/ai/prompt-types.ts` shows both fields added to PromptContext (optional, `?`), NOT in ai-context-read.ts's own types.
    - Test: an AI-enabled off-limits item appears ONLY in avoidanceConstraints, never in the positive fuel projection.
    - Test: an AI-disabled off-limits item appears in NEITHER collection.
    - Test: a recent interaction note is carried only when allow_ai=1; absent when allow_ai=0.
    - Test: a Group Note is never carried in any collection.
    - `git diff src/ai/prompt-template.ts` is empty (resolvePrompt not touched); `git diff src/db/fuel-read.ts` is empty (exclusions untouched).
    - `grep -n "toISOString" src/db/ai-context-read.ts` returns nothing; `grep -n "off_limits" src/db/ai-context-read.ts` shows no positive-context branch (only the gated negative shape source).
  </acceptance_criteria>
  <done>PromptContext (in prompt-types.ts) carries the two ADR-078 shapes, readPromptContext populates them, and the positive-projection exclusions are proven intact — with no egress widening.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the egress-exclusion invariants (regression fence)</name>
  <files>src/db/fuel-read.test.ts</files>
  <read_first>
    - src/db/fuel-read.test.ts (FULL — existing exclusion coverage)
    - src/db/fuel-read.ts (RANKED_FUEL_EXCLUSIONS + the two off_limits guards at :95 and :121)
  </read_first>
  <action>
    Extend fuel-read.test.ts to lock the structural exclusions that Phase 35 must never relax: getRankedFuel excludes off_limits / unconfirmed-AI / blank in SQL, and listFuelForEditor is the ONLY read that surfaces off_limits. These tests are a regression fence so any future accidental widening of the positive projection fails loudly (Trip-Wire 3 / Pitfall 2). No production change to fuel-read.ts.
  </action>
  <verify>
    <automated>npx vitest run src/db/fuel-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts getRankedFuel returns no off_limits row for a contact that has one.
    - Test asserts listFuelForEditor DOES include the off_limits row (the one editor read).
    - `git diff src/db/fuel-read.ts` is empty.
  </acceptance_criteria>
  <done>The off_limits/AI-eligibility exclusion invariants are fenced by tests with no production change.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the new shapes are carry-only (prompt-template regression)</name>
  <files>src/ai/prompt-template.test.ts</files>
  <read_first>
    - src/ai/prompt-template.test.ts (FULL — the existing resolvePrompt suite + the PromptContext builder/fixture it uses; extend, do not rewrite)
    - src/ai/prompt-template.ts (:150-185 — resolvePrompt serializes selected fields explicitly; the new shapes must NOT appear in prompt/inspectorDisplay/payload)
    - src/ai/prompt-types.ts (the two new optional fields from Task 1)
  </read_first>
  <action>
    Add a regression test that constructs a PromptContext WITH non-empty `avoidanceConstraints` and `gatedRecentInteractionNotes` (using a recognizable sentinel value), runs resolvePrompt, and asserts the sentinel does NOT appear in the resolved `prompt`, `inspectorDisplay`, or `payload` — proving Phase 35 carries the shapes without transmitting them (D-13; Phase 36 owns rendering). This is the fence that fails loudly if a future edit starts serializing the carried shapes before Phase 36. No production change to prompt-template.ts.
  </action>
  <verify>
    <automated>npx vitest run src/ai/prompt-template.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: a PromptContext carrying sentinel avoidanceConstraints/gatedRecentInteractionNotes resolves to a payload that does NOT contain the sentinel.
    - `git diff src/ai/prompt-template.ts` is empty (no rendering added).
  </acceptance_criteria>
  <done>A regression test proves the two carried shapes never serialize into the resolved provider payload this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite contact data → PromptContext (egress projection) | The sole data-minimization boundary; anything added here can reach the AI provider once Phase 36 renders it. |
| Contact-derived free text → prompt | Untrusted, injectable text must stay bounded/delimited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Information disclosure | ai-context-read shape extension | high | mitigate | Carry-only (no rendering/transmission this phase); off_limits AI-enabled → negative shape ONLY; AI-disabled off_limits / Group Notes / allow_ai=0 notes never carried; fuel-read exclusions untouched; owner-gated widening (D-08/D-13). |
| T-35-02 | Tampering | contact-derived text in shapes | high | mitigate | Values ?-bound; only isSafeColName-guarded identifiers interpolated; negative-constraint text stays as bounded/delimited as positive context (ADR-078) — Phase 36 renders within the closed projection. |
| T-35-14 | Information disclosure | positive projection regression | medium | mitigate | fuel-read.test.ts + ai-context-read.test.ts fence the exclusions so any accidental widening fails CI. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/db/ai-context-read.test.ts src/db/fuel-read.test.ts src/ai/prompt-template.test.ts` green.
- `npx tsc --noEmit` clean.
- `git diff src/ai/prompt-template.ts src/db/fuel-read.ts` empty (resolvePrompt + exclusions untouched).
</verification>

<success_criteria>
PromptContext (in prompt-types.ts) carries the ADR-078 avoidance-constraint and gated-note shapes without widening actual egress; a prompt-template regression proves they do not serialize; the positive-projection exclusions are proven intact and fenced against regression.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/ai/prompt-types.ts` — `PromptContext` gains OPTIONAL `avoidanceConstraints?` and `gatedRecentInteractionNotes?` shape fields (carry-only, closed-allowlist addition per D-13).
- `src/db/ai-context-read.ts` — `readPromptContext` populates the two new shapes from AI-eligibility-gated reads.
- Extended `src/db/ai-context-read.test.ts`, `src/db/fuel-read.test.ts`, `src/ai/prompt-template.test.ts` (new carry-only regression).
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-05-SUMMARY.md` when done.
</output>
