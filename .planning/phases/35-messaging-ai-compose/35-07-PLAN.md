---
phase: 35-messaging-ai-compose
plan: 07
type: execute
wave: 3
depends_on: [35-01, 35-02, 35-03]
files_modified:
  - src/screens/ComposeScreen.tsx
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-02: Compose initializes its mode from the migration-028 default (default_message_mode; 'remember' → remembered_message_mode) via compose-logic.effectiveMode; an ad-hoc 'Make this an email' / 'Make this a text' switch changes the session mode without writing the preference; the remembered mode is written (app-settings setter) ONLY on Transmit or Copy."
    - "COMP-03: Text mode resolves the primary phone and Email the primary email via selectActionablePrimaryMethods; a deliberate destination selection establishes a missing primary via `setContactMethodPrimary()` (the plan-35-03 single-method writer, NOT applyContactMethodDiff); Compose falls back to the usable mode when the preferred one has no destination and stays usable for drafting/Copy when neither does."
    - "COMP-03 (no-destination): with neither phone nor email, Transmit is unavailable with an accessible explanation and Copy is the sole primary — not a blocking error; the smsAvailable===null probe keeps the no-flash discipline in TEXT mode only (HIGH-2: Email stays transmittable while SMS availability is unknown)."
    - "COMP-04: Email mode exposes a Subject field + Body; Transmit preserves recipient/subject/body through the extended handoff email arm; main Copy copies the Body only (feedback 'Message copied') and Subject has its own copy affordance ('Subject copied')."
    - "The channel passed to Transmit derives from the session mode; there is no hardcoded channel literal; capability decisions come from compose-logic, never re-derived in the screen (WR-02)."
    - "subject/body/mode/destination all live in the compose-session-store so they survive nav/background (D-10)."
  artifacts:
    - "ComposeScreen: mode switch + Subject field + no-destination state wired to compose-logic + handoff + app-settings"
  key_links:
    - "app-settings default/remembered → effectiveMode → session mode → resolveComposeControls → Transmit/Copy gate"
    - "Transmit(Email) → performReachOut email arm (subject+body) → OS email composer; remembered mode written on commit"
  prohibitions:
    - "Transmit must never claim Orbit delivered the message."
    - "Must never hardcode channel:'text'."
    - "Must never write the remembered mode on an ad-hoc switch — only on Transmit or Copy."
    - "Must never re-derive send/copy capability in the screen — consume resolveComposeControls."
    - "Must never remove the app-global AssistBanner / dismissal path."
---

<objective>
Wire the Text/Email delivery layer (plans 35-02/35-03) into ComposeScreen: mode initialization from the durable preference, an ad-hoc mode switch, the Email Subject field + separate Subject copy, the no-destination usable state, establish-missing-primary, and remembered-mode-on-commit. This is the first expansion pass over the tracer's ComposeScreen skeleton.

Purpose: complete COMP-02/03/04 at the screen level using the tested pure logic — the screen stays a thin consumer.
Output: ComposeScreen supporting Text and Email end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mode init + ad-hoc switch + remembered-on-commit</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (the tracer skeleton from plan 35-01; the mode field already in the session store)
    - src/logic/compose-logic.ts (effectiveMode / nextRememberedMode from plan 35-03)
    - src/db/app-settings-dao.ts (getAppSettings compose-mode getters + the remembered-mode setter from plan 35-02)
    - src/stores/compose-session-store.ts (setMode)
    - 35-UI-SPEC.md (Mode switch copy 'Make this an email' / 'Make this a text', tertiary accentText link tone)
  </read_first>
  <action>
    On mount, read default_message_mode + remembered_message_mode via getAppSettings and initialize the session mode with compose-logic.effectiveMode. Add the ad-hoc mode-switch control ('Make this an email' / 'Make this a text', Button tertiary accentText) that calls setMode and does NOT write the preference. On a successful Transmit or Copy, advance the remembered mode via nextRememberedMode and persist it with the app-settings remembered-mode setter (commit only). Keep the mode in the session store so it survives nav/background.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "effectiveMode\|nextRememberedMode" src/screens/ComposeScreen.tsx` shows both consumed.
    - The remembered-mode setter is called only in the Transmit and Copy handlers (not in the switch handler) — verify by reading the handlers.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Mode initializes from the preference, switches ad-hoc, and persists the remembered value only on commit.</done>
</task>

<task type="auto">
  <name>Task 2: Destination resolution + no-destination state + establish primary</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (skeleton)
    - src/db/contact-methods-read.ts (:10-19 selectActionablePrimaryMethods — phone AND email)
    - src/db/contact-methods-dao.ts (the NEW setContactMethodPrimary() writer added by plan 35-03 — the single-method primary swap for the picker; do NOT use applyContactMethodDiff for this. NOTE: there was never a set-primary writer at :180-192; that was a stale RESEARCH citation — those lines are the clear-existing-primary step inside applyContactMethodDiffCore.)
    - src/logic/compose-logic.ts (mode-aware resolveComposeControls + no-destination state from plan 35-03 — the SMS probe gates Text only; Email is transmittable whenever a primary email exists regardless of smsAvailable)
    - 35-UI-SPEC.md ("No-destination state" copy; E2 Destination resolution rows; picker/establish-primary; long-value truncation)
  </read_first>
  <action>
    Resolve the primary phone and email via selectActionablePrimaryMethods and feed hasPhone/hasEmail + smsAvailable + mode into resolveComposeControls. Render the resolved controls: Transmit primary when a destination exists; when the preferred mode has none, fall back to the usable mode; when neither exists, render Transmit unavailable with the accessible explanation copy and Copy as sole primary (usable, not an error). IMPORTANT (HIGH-2): the smsAvailable probe (incl. probe-pending) gates TEXT only — in Email mode with a primary email present, Transmit stays available even while smsAvailable is null/false. When a mode has multiple candidate destinations and no primary, present the picker; a deliberate selection sets the canonical primary via `setContactMethodPrimary()` (the plan-35-03 single-method writer), NOT applyContactMethodDiff. Truncate long addresses/numbers with ellipsis in display/picker rows; apply the full value on selection. Store the chosen destination in the session store.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "selectActionablePrimaryMethods\|resolveComposeControls" src/screens/ComposeScreen.tsx` shows both consumed; no bespoke destination store.
    - No-destination path renders Transmit unavailable + Copy primary (read the render branch), not a thrown error.
    - Establish-primary calls `setContactMethodPrimary` (grep the call), not applyContactMethodDiff.
    - Email mode with a primary email renders Transmit even when smsAvailable is null (read the branch — probe gates Text only).
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Destination resolves per mode with fallback, a usable no-destination state, and establish-missing-primary.</done>
</task>

<task type="auto">
  <name>Task 3: Email Subject field + Subject copy + mode-derived Transmit</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (skeleton + tasks 1-2)
    - src/services/reach-out/handoff.ts (email arm carrying subject+body from plan 35-03)
    - src/logic/compose-logic.ts (body-only / subject-only copy-target selectors from plan 35-03)
    - src/stores/compose-session-store.ts (subject/setSubject — owned by plan 35-01; consume via the store action, do NOT redefine the store here; this plan modifies only ComposeScreen.tsx)
    - 35-UI-SPEC.md (Email field label 'Subject' at role label; Copy feedback 'Message copied' / 'Subject copied')
  </read_first>
  <action>
    In Email mode, render a Subject field (AppText role label + input) bound to the session store `subject` via `setSubject` (the field is owned by plan 35-01's compose-session-store — consume it, do not redefine the store), above the Body. Transmit derives channel from the session mode and passes recipient + subject + body to performReachOut (email arm). Main Copy copies the Body only (feedback 'Message copied'); add a separate Subject copy affordance (tertiary accentText) copying the Subject only ('Subject copied'). Keep the 'Copied' feedback as setState+setTimeout (never per-frame animation). Because subject lives in the plan-35-01 session store, it already survives nav/background (COMP-07) — this plan only binds the field, it adds no new persistence.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Subject field renders only in Email mode; bound to the session store.
    - Main Copy target is body only; Subject copy affordance targets subject only (consume the plan-35-03 selectors).
    - Transmit passes subject+body to the email handoff (grep the performReachOut call arguments).
    - `grep -n 'channel: "text"' src/screens/ComposeScreen.tsx` returns nothing.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Email Subject + separate Subject copy work and Transmit carries subject/body via the mode-derived channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose UI → OS SMS/email composer | The composition (recipient/subject/body) crosses the app boundary. |
| Picker selection → set-primary DAO | A deliberate user choice mutates the canonical contact-method primary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Tampering | Transmit → handoff | high | mitigate | subject/body encoded in the handoff (plan 35-03); channel derived from mode; never claims delivery. |
| T-35-17 | Tampering | establish-primary | medium | mitigate | Primary set only on a deliberate user selection via the canonical setContactMethodPrimary DAO (plan 35-03); no silent auto-set; single transaction, partial-unique-index respected. |
| T-35-11 | Denial of service | no-destination gate | low | mitigate | No-destination is a usable degraded state (Copy), never a crash/block. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run check:colors` passes.
- `npm test` (full suite) green at the wave-07 merge (per 35-VALIDATION.md sampling).
- Device backstop (phase gate): Transmit in Text and Email modes on the Pixel; the correct composer opens pre-filled; Copy copies body; Subject copy copies subject; no-destination contact stays usable for Copy.
</verification>

<success_criteria>
ComposeScreen supports Text and Email: mode init from preference, ad-hoc switch, remembered-on-commit, destination resolution with fallback + usable no-destination state, Subject field + separate Subject copy, and mode-derived Transmit.
</success_criteria>

<artifacts_produced>
This plan CREATES no new exported symbols — it wires existing ones into ComposeScreen (source-grounding: the consumed symbols setContactMethodPrimary, effectiveMode, nextRememberedMode, resolveComposeControls are created in plans 35-02/35-03; exclude them from drift checks against this plan).
- `src/screens/ComposeScreen.tsx` — mode switch, Subject field, no-destination state, establish-primary via setContactMethodPrimary, remembered-on-commit wiring (consuming plans 35-02/35-03).
- (session store `subject`/`setSubject` consumed here; the field is owned/created by plan 35-01, not added by this plan.)
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-07-SUMMARY.md` when done.
</output>
