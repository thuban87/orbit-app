---
phase: 35-messaging-ai-compose
plan: 09
type: execute
wave: 5
depends_on: [35-06, 35-08]
files_modified:
  - src/screens/ComposeScreen.tsx
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/tabs/OrreryStack.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/db/lifecycle-consumer-ledger.test.ts
  - src/navigation/ai-suggestion-navigation.ts
  - src/navigation/ai-suggestion-navigation.test.ts
autonomous: true
requirements: [COMP-08, COMP-10, COMP-11, COMP-14]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-08 / HIGH-5 (route registration): a `ComposeResearch` route is REGISTERED in BOTH DashboardStack.tsx and OrreryStack.tsx (VERIFIED on disk: neither currently registers it — both only register `Compose` at DashboardStack.tsx:59 / OrreryStack.tsx:73) and added to BOTH param lists in navigation/types.ts (DashboardStackParamList + OrreryStackParamList), so navigating to ComposeResearchScreen resolves at compile time and runtime rather than throwing on an unregistered route."
    - "COMP-08: Compose links to the Things to Remember Research side (sibling full-screen ComposeResearchScreen from plan 35-06) via a 'Things to Remember · {count}' entry (populated count; tertiary accentText); the same header is kept on both sides for orientation and the session survives the Compose↔Research transition."
    - "COMP-11: selected Message Focus is shown compactly on the Compose side as 'Message focus · {count}' (hidden when empty), reflecting the ≤3 session-only selection; Add to AI grants no permission."
    - "COMP-10 (display): Off Limits never appears as Message Focus on the Compose side (only as the read-only Avoid group on the Research side)."
    - "COMP-14 / HIGH-8: Compose is deep-link-ready and origin-aware — an optional `origin` route param is added to the Compose route type; the Compose ENTRY POINTS are updated to pass it where return semantics differ (VERIFIED on disk: every caller passes only { contactId } today — ContactProfileScreen.tsx:335, HomeScreen.tsx:1287, widget-linking.ts:178, notification-nav.ts:105). ContactProfileScreen passes origin:'profile' (both stacks use it) so a Profile-launched send returns to Profile; dashboard/widget/notification callers keep the default dashboard/deep-link semantics (unchanged reset). Completing a send/log returns toward the origin (pop toward Profile when launched there rather than always resetting to the dashboard root) and leaves no finished Compose route in Back history."
    - "cycle-3 (build-breaker #2 — untouched ledger test): the `origin:'profile'` edit to ContactProfileScreen.tsx changes the Message→Compose call string, which an UNRELATED, currently-passing test asserts verbatim — `src/db/lifecycle-consumer-ledger.test.ts:431` does `expect(screen).toContain('navigation.navigate(\"Compose\", { contactId })')`. That test's INTENT (comment :421-433) is 'Message → Compose is not lifecycle-gated', NOT the exact param shape. Task 2 therefore updates this assertion in the SAME task that edits ContactProfileScreen so `npm test` stays green, PRESERVING the not-lifecycle-gated intent (assert the navigate('Compose', ...) call still carries `contactId` and is not behind any `trackingEnabled`/lifecycle guard — e.g. relax the exact substring to a regex that tolerates the added `origin`)."
    - { statement: "On the Pixel: launching Compose from Profile and confirming a send returns to Profile, and Back does not resurrect the finished Compose draft (origin-aware return + no finished route in Back history)", verification: backstop }
    - "Trip-Wire 4 cleanup: the retired `requestAiSuggestion` route param, the `ai-suggestion-navigation.ts` consume-once intent plumbing, AND its now-orphaned test `ai-suggestion-navigation.test.ts` are removed (VERIFIED the test imports consumeAiSuggestionIntent at :11 and asserts the retired requestAiSuggestion param at :47 — deleting the module without also removing/rewriting the test breaks `npm test`); Profile AI-draft entry was already removed in Phase 31; no code path auto-starts AI from a route param."
    - "D-10: the session survives the in-app Compose↔Research navigation and ordinary backgrounding; it clears on Transmit-confirmed and does not survive relaunch."
  artifacts:
    - "ComposeScreen: Research entry + 'Message focus · N' display + origin-aware return"
    - "navigation/types.ts: Compose route gains optional `origin`, drops `requestAiSuggestion`; NEW `ComposeResearch` route in both param lists"
    - "DashboardStack.tsx + OrreryStack.tsx: `ComposeResearch` <Stack.Screen> registered in both"
    - "ContactProfileScreen.tsx: navigate('Compose', { contactId, origin: 'profile' })"
    - "ai-suggestion-navigation.ts: consume-once intent plumbing retired"
  key_links:
    - "Compose 'Things to Remember · N' → navigate('ComposeResearch', { contactId }) (registered in both stacks) → session preserved → Add to AI → 'Message focus · N' on Compose"
    - "ContactProfileScreen → navigate('Compose', { contactId, origin: 'profile' }) → route.params.origin → return dispatch (pop toward Profile vs dashboard reset) with the finished route removed from Back history"
  prohibitions:
    - "Must never leave the finished Compose draft in Back history after a completed send/log."
    - "Must never retain a route param that auto-starts AI on focus (Trip-Wire 4)."
    - "Must never show Off Limits as Message Focus."
    - "Must never remove the app-global AssistBanner / dismissal path."
---

<objective>
Final ComposeScreen expansion pass: link to the Research side, show the compact 'Message focus · N' summary, make return origin-aware (COMP-14), and retire the leftover `requestAiSuggestion` intent plumbing (Trip-Wire 4). This completes the Compose workflow as the editor-first, sibling-Research, origin-aware surface described by the dossier.

Purpose: close COMP-08 (entry)/COMP-11 (display)/COMP-14 and remove the dead AI-intent nav plumbing cleanly.
Output: the finished Compose surface + navigation cleanup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the ComposeResearch route (both stacks) + Research entry + Message Focus summary</name>
  <files>src/navigation/tabs/DashboardStack.tsx, src/navigation/tabs/OrreryStack.tsx, src/navigation/types.ts, src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/navigation/tabs/DashboardStack.tsx (VERIFIED: registers `Compose` at :59, imports ComposeScreen at :5; NO ComposeResearch route — add one)
    - src/navigation/tabs/OrreryStack.tsx (VERIFIED: registers `Compose` at :73, imports ComposeScreen at :5; NO ComposeResearch route — add one)
    - src/navigation/types.ts (:102 DashboardStackParamList.Compose, :171 OrreryStackParamList.Compose — add a `ComposeResearch: { contactId: number }` entry to BOTH param lists)
    - src/screens/ComposeResearchScreen.tsx (the Research screen from plan 35-06 — the navigation target and its header; the component to register)
    - src/stores/compose-session-store.ts (messageFocus from plan 35-06; body/subject/mode preserved across the transition)
    - 35-UI-SPEC.md (Copywriting 'Things to Remember · {count}', 'Message focus · {count}' hidden when empty; E4 rows; keep the same header on both sides)
  </read_first>
  <action>
    Register `ComposeResearch` (component ComposeResearchScreen) as a <Stack.Screen> in BOTH DashboardStack.tsx and OrreryStack.tsx, and add a `ComposeResearch: { contactId: number }` entry to BOTH param lists in navigation/types.ts (mirroring the existing `Compose` registration in each). Then add a 'Things to Remember · {count}' entry on the Compose side (Button tertiary accentText, populated count) that navigates to ComposeResearch for the same contact, keeping the session store intact so body/subject/mode/Message Focus survive the transition. On the Compose side, render a compact 'Message focus · {count}' summary from the session messageFocus (hidden when empty), reflecting the ≤3 selection; Off Limits never appears here. Keep the same contact-name header (role heading) on both sides for orientation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ComposeResearch" src/navigation/tabs/DashboardStack.tsx src/navigation/tabs/OrreryStack.tsx src/navigation/types.ts` shows the route registered in both stacks and both param lists.
    - The Research entry navigates to ComposeResearch and the session is not reset (read the nav handler — no startSession/clear on the transition).
    - 'Message focus · N' is hidden when empty and shows the count otherwise.
    - No Off Limits item can appear in the Message Focus summary.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>ComposeResearch is a registered route in both stacks; Compose links to it (session preserved) and shows the compact Message Focus summary.</done>
</task>

<task type="auto">
  <name>Task 2: Origin-aware return + caller updates + no finished route in Back history</name>
  <files>src/screens/ComposeScreen.tsx, src/navigation/types.ts, src/screens/ContactProfileScreen.tsx, src/db/lifecycle-consumer-ledger.test.ts</files>
  <read_first>
    - src/navigation/types.ts (:102 + :171 — Compose route params `{ contactId, requestAiSuggestion? }` in BOTH param lists; :141-149 origin-aware Back precedent in the Orrery stack; add an optional `origin` param to both)
    - src/screens/ComposeScreen.tsx (:84 resetToDashboardRoot import; :266-272 the current always-reset-to-dashboard Back; the goHome handler)
    - src/screens/ContactProfileScreen.tsx (:335 — VERIFIED: onMessage navigates to Compose with ONLY { contactId }; this is the caller that must pass origin:'profile' so a Profile-launched send returns to Profile; ContactProfileScreen is used in BOTH stacks so this covers both)
    - src/db/lifecycle-consumer-ledger.test.ts (:421-433 — VERIFIED: an UNRELATED, currently-green architectural-guard test that reads ContactProfileScreen.tsx source and at :431 asserts `expect(screen).toContain('navigation.navigate("Compose", { contactId })')`. Editing :335 to add `origin` breaks this exact substring → `npm test` fails. The test's INTENT (comments :421-426 / :432-433) is 'Message → Compose is not lifecycle-gated', NOT the exact param shape. This is the SAME orphan-test failure class Task 3 catches for ai-suggestion-navigation.test.ts — a second instance the authors initially missed.)
    - src/screens/HomeScreen.tsx (:1287 — navigates with only { contactId }; dashboard origin = default, no change needed) and src/navigation/widget-linking.ts (:178) / src/services/notifications/notification-nav.ts (:105) — reset intents to [Home, Compose]; deep-link/dashboard semantics = default, no change needed (note this in the plan so the executor does not needlessly touch them)
    - src/navigation/reset-intents.ts (resetToDashboardRoot / resetToDashboardWith — the reset primitives)
    - docs/dossier ... (§W origin-aware return: return toward the contact Profile when launched there)
    - 35-VALIDATION.md (COMP-14 is a device-observable Back-stack behavior — manual/device backstop)
  </read_first>
  <action>
    Add an OPTIONAL `origin` param ('profile' | 'dashboard' | 'deep-link') to the Compose route type in BOTH param lists. Update the Profile caller (ContactProfileScreen.tsx:335) to pass `origin: 'profile'` — this is the only entry point whose return semantics differ; HomeScreen, widget-linking, and notification-nav keep the default (dashboard/deep-link) and are intentionally NOT modified (the optional param means they still type-check). BECAUSE this edit changes the Message→Compose call string, in the SAME task update the untouched architectural-guard assertion at `src/db/lifecycle-consumer-ledger.test.ts:431` so `npm test` stays green: replace the exact `toContain('navigation.navigate("Compose", { contactId })')` substring with an assertion that tolerates the added `origin` (e.g. a regex matching `navigation.navigate("Compose", { contactId` / `, origin`) while PRESERVING the test's stated intent (the Message→Compose navigation still carries `contactId` and is NOT placed behind a `trackingEnabled`/lifecycle guard — keep the surrounding widget-guard `trackingEnabled` assertion intact). Do NOT weaken it to a trivially-passing check. On a completed send/log (and on Back), return origin-aware: pop toward the contact Profile when origin==='profile', otherwise use the existing dashboard reset; in all cases ensure the finished Compose route is not left in Back history (do not leave a resurrectable finished draft). Preserve the app-global AssistBanner (do not touch it).
  </action>
  <verify>
    <automated>npx vitest run src/db/lifecycle-consumer-ledger.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "origin" src/navigation/types.ts` shows the optional Compose route param added to both param lists; `requestAiSuggestion` removed (see Task 3).
    - `grep -n "origin" src/screens/ContactProfileScreen.tsx` shows the Profile caller passing origin:'profile'.
    - `src/db/lifecycle-consumer-ledger.test.ts:431`'s assertion is updated to match the new `origin`-bearing Message→Compose call (still asserting the `contactId`-carrying navigate and the not-lifecycle-gated intent); `npx vitest run src/db/lifecycle-consumer-ledger.test.ts` is GREEN.
    - The return handler branches on origin (read it): 'profile' → pop toward Profile; dashboard/deep-link → reset.
    - The origin:'profile' return dispatch resolves within WHICHEVER stack launched Compose — ContactProfileScreen is registered in BOTH DashboardStack and OrreryStack, so the pop-toward-Profile must be stack-agnostic (not a hardcoded stack); verified on-device at the phase gate from both a Dashboard-launched and an Orrery-launched Compose (LOW finding).
    - The finished Compose route is removed from Back history on completion (read the dispatch).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Return is origin-aware — Profile-launched Compose returns to Profile — the caller passes origin, and no finished Compose route is left in Back history; verified on-device at the phase gate.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the requestAiSuggestion consume-once intent plumbing (+ its orphaned test)</name>
  <files>src/navigation/types.ts, src/navigation/ai-suggestion-navigation.ts, src/navigation/ai-suggestion-navigation.test.ts, src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/navigation/types.ts (:88-102 — the `requestAiSuggestion` param + its consume-once doc comment)
    - src/navigation/ai-suggestion-navigation.ts (:27-32 — the consume-once intent helper consumeAiSuggestionIntent)
    - src/navigation/ai-suggestion-navigation.test.ts (VERIFIED: imports consumeAiSuggestionIntent from the deleted module at :11 and asserts the retired `requestAiSuggestion` route param at :47-58 — deleting the module orphans this test and breaks `npm test` unless it is deleted or rewritten)
    - src/screens/ComposeScreen.tsx (plan 35-01's rebuild already removed the focus-effect consumption of route.params.requestAiSuggestion; confirm none survived — this task removes the residual TYPE + nav helper)
    - Confirm no remaining caller: `grep -rn "requestAiSuggestion\|consumeAiSuggestionIntent" src/` before removing (Phase 31 already removed the Profile AI-draft entry)
  </read_first>
  <action>
    Remove the `requestAiSuggestion` route param from the Compose route type (both param lists), delete the consume-once intent plumbing in ai-suggestion-navigation.ts (and the file, since it becomes empty of live use), and DELETE its now-orphaned test `src/navigation/ai-suggestion-navigation.test.ts` (it imports the removed module and asserts the retired param — so `npm test` would fail; delete it, or if a serializable-origin-param contract test is still wanted, rewrite it to test the new `origin` param instead of the removed helper). Remove any residual focus-effect branch that auto-started AI from the param (plan 35-01 already stripped the screen consumption during the rebuild — confirm none survived). Verify no caller remains (grep). This is dead plumbing after Phase 31 removed the Profile AI-draft entry; the only AI entry now is the in-Compose adaptive action (plan 35-08). Do NOT remove the `ai_ack_*` columns (forward-only migration; leave them — plan 35-04 already removed the logic-module writer/reader and left the app-settings-dao acknowledgeProvider writer intact).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "requestAiSuggestion\|consumeAiSuggestionIntent" src/` returns nothing.
    - `src/navigation/ai-suggestion-navigation.test.ts` is deleted (or rewritten to not import the removed module) so no orphaned import survives.
    - No focus/mount effect auto-starts AI (read the effects in ComposeScreen).
    - `npx tsc --noEmit` clean; full suite green (`npm test`).
  </acceptance_criteria>
  <done>The retired AI-intent nav plumbing AND its orphaned test are removed with no remaining callers/imports, the suite stays green, and AI starts only from the in-Compose adaptive action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params (deep link) → Compose | Deep-link/origin params drive navigation; must not resurrect a finished draft or auto-start egress. |
| Compose↔Research navigation → session store | The session must survive the transition without leaking selections into a durable store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-20 | Tampering | origin-aware Back stack | medium | mitigate | Finished Compose route removed from Back history so a completed draft cannot be resurrected/re-sent; device-verified at the phase gate. |
| T-35-24 | Denial of service | ComposeResearch route registration | low | mitigate | Registering the route in both stacks + param lists prevents an unregistered-route throw when Compose navigates to Research; the route carries only { contactId }, no content. |
| T-35-12 | Tampering | retired AI intent plumbing | medium | mitigate | requestAiSuggestion param + consume-once helper removed; grep proves no caller auto-starts AI; no residual ack/intent gate. |
| T-35-19 | Information disclosure | deep-link origin | low | mitigate | Origin param only routes return; carries no contact content and never widens egress. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run check:colors` passes; `npm test` (full suite) green at the phase gate.
- Device backstop (COMP-14, phase gate): on the Pixel, launch Compose from a Profile reached via the DASHBOARD stack, Transmit-confirm, verify return to Profile and that Back does not resurrect the finished draft; repeat from a Profile reached via the ORRERY stack (origin:'profile' must resolve within whichever stack launched Compose — LOW finding); launch from dashboard root and verify the dashboard reset path.

**Behavior coverage split (review MEDIUM #7 — recorded so the device-UAT reliance is a visible decision, not a silent gap):**
- Covered by automated tests: the origin-aware caller shape is locked by the updated `src/db/lifecycle-consumer-ledger.test.ts:431` (Task 2 — still asserts the contactId-carrying, not-lifecycle-gated Message→Compose navigate); the full `npm test` + `tsc --noEmit` (Task 3) prove the dual-stack `ComposeResearch` registration and the retired-intent removal compile and run with no orphaned imports.
- Device-UAT-only (navigation Back-stack behavior — device-observable, no reliable JSDOM/navigator seam; per the project's device-UAT norm and 35-VALIDATION.md): the origin-aware return (pop-toward-Profile from BOTH stacks), the finished-route-not-in-Back-history guarantee, the Compose↔Research session survival, and the 'Message focus · N' summary. Verified on the Pixel at the phase gate (backstop above).
</verification>

<success_criteria>
Compose links to the Research side with the session preserved, shows the compact Message Focus summary, returns origin-aware with no finished route in Back history, and the retired AI-intent nav plumbing is removed.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/navigation/types.ts` — NEW `ComposeResearch` route in both param lists; Compose route gains optional `origin`, drops `requestAiSuggestion`.
- `src/navigation/tabs/DashboardStack.tsx` + `src/navigation/tabs/OrreryStack.tsx` — NEW `ComposeResearch` <Stack.Screen> registered in both.
- `src/screens/ComposeScreen.tsx` — Research entry, 'Message focus · N' display, origin-aware return.
- `src/screens/ContactProfileScreen.tsx` — Compose launch now passes `origin: 'profile'`.
- `src/db/lifecycle-consumer-ledger.test.ts` — the `:431` Message→Compose exact-string assertion updated to tolerate the added `origin` param (intent preserved: still asserts the contactId-carrying, not-lifecycle-gated navigate).
- `src/navigation/ai-suggestion-navigation.ts` — consume-once intent plumbing retired (file deleted, empty of live use).
- `src/navigation/ai-suggestion-navigation.test.ts` — deleted (orphaned once the module is removed; it imported consumeAiSuggestionIntent and asserted the retired requestAiSuggestion param).
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-09-SUMMARY.md` when done.
</output>
