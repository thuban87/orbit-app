---
phase: 35-messaging-ai-compose
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - src/logic/compose-logic.ts
  - src/logic/compose-logic.test.ts
  - src/services/reach-out/handoff.ts
  - src/services/reach-out/handoff.test.ts
  - src/db/contact-methods-dao.ts
  - src/db/contact-methods-dao.test.ts
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "H1 (build-breaker fix): resolveComposeControls's new `mode`/`hasEmail` params are OPTIONAL (defaults 'text'/false) and appended AFTER `(hasPhone, smsAvailable)`, so the wave-1 ComposeScreen 2-arg call (ComposeScreen.tsx:798) and the existing 2-arg cases in compose-logic.test.ts keep type-checking AND returning the current Text-mode result through wave 2; this plan's OWN `tsc --noEmit` gate depends on that 2-arg call still compiling. The screen rewire to the full 4-arg gate stays in plan 35-07 (wave 3) — ComposeScreen.tsx is NOT pulled into this plan."
    - "COMP-03: compose-logic resolves the primary phone for Text and the primary email for Email via selectActionablePrimaryMethods; the screen consumes this, never invents a destination store."
    - "COMP-03 (empty/no-destination): when the preferred mode has no destination, resolution falls back to the other usable mode; when NEITHER phone nor email exists, Transmit is unavailable (accessible explanation) and Copy stays the sole primary — drafting and Copy remain usable, not a blocking error."
    - "COMP-03 / HIGH-2 (probe-pending is TEXT-ONLY): the smsAvailable===null probe-pending state (Transmit hidden, Copy primary, no helper, no-flash) gates TEXT mode ONLY — verified on disk, resolveComposeControls's null gate is phone/SMS-scoped (compose-logic.ts:64,77) and email handoff uses Linking.openURL('mailto:') with NO expo-sms dependency. In EMAIL mode with an actionable primary email, Transmit stays available while SMS availability is unknown; Email has no probe-pending state (it resolves synchronously — mailto via Linking is assumed available). 'missing destination wins first' precedence is preserved within each mode."
    - "COMP-02 (empty/init + remembered): the effective mode initializes from default_message_mode — 'remember' resolves to remembered_message_mode; the remembered value is written ONLY on Transmit or Copy (never on an ad-hoc in-session switch)."
    - "COMP-02 (equality/encoding): mode equality is over the fixed lowercase token set {'text','email'} (and 'remember' sentinel for the default) — never a free-text or locale-sensitive comparison."
    - "COMP-04: the email handoff arm carries recipient + Subject + Body via a mailto query string with encodeURIComponent on subject and body (no-dependency Linking path); Text arm unchanged (SMS.sendSMSAsync); the assist row is still created before handoff and markAssistFailed still runs on catch; the plan-35-01 return contract `{ handoffStarted, assistUid }` is PRESERVED (the email arm returns the same structured outcome — do NOT revert it to Promise<void>)."
    - "COMP-04: main Copy copies the Body only; the Subject has its own separate copy affordance (logic exposes body-only vs subject-only copy targets)."
    - "COMP-03 / HIGH-7 (set-primary DAO): a purpose-built, transaction-safe `setContactMethodPrimary()` is ADDED to contact-methods-dao.ts for the lightweight Compose 'establish missing primary' picker — VERIFIED on disk there is NO existing set-primary writer (only applyContactMethodDiff/applyContactMethodDiffCore, which require the whole seeded/current method set and are inappropriate for a quick picker). It validates the chosen (contact_id, method_type) target, clears the prior primary of that type, then promotes the target in one transaction (pre-read→clear→promote — see the A3 hardening truth), respecting the partial-unique-primary index and bumping data_revision like the diff path."
    - "COMP-03 / cycle-3 + A3 (set-primary integrity hardening — review MEDIUM #5, ordering de-contradicted): setContactMethodPrimary runs ONE transaction in the strict order PRE-READ/VALIDATE → CLEAR → PROMOTE. (1) It PRE-READS the target by `WHERE id = ? AND contact_id = ? AND method_type = ?`; a foreign id, unknown id, or a `methodType` that disagrees with the row's stored `method_type` (e.g. an email method id with methodType:'phone') matches no row → NO write, throw/rollback, BEFORE anything is cleared, so no prior primary is ever orphaned. (2) It CLEARS the prior `is_primary=1` of that exact `(contactId, methodType)` BEFORE (3) PROMOTING the validated target to `is_primary=1`, asserting EXACTLY ONE row changed and rolling back on zero. Clear-before-promote never transiently holds two primaries of a type (statement-immediate partial-unique index at 009-contact-method-normalization.ts:235); a promote-before-clear would trip that index. This resolves the earlier promote-first vs clear-first ambiguity."
    - "The channel passed to performReachOut derives from mode ('text'→text SMS, 'email'→email mailto), never a hardcoded literal."
    - "COMP-02/03/04 (email transmit availability, HIGH-2): with Email mode selected and an actionable primary email present, the resolved controls expose Transmit even when smsAvailable is null/false — Email transmittability is independent of the SMS probe."
  artifacts:
    - "compose-logic: resolveComposeControls extended for mode + no-destination (probe-pending scoped to Text) — new `mode`/`hasEmail` params OPTIONAL with defaults ('text'/false), appended after `(hasPhone, smsAvailable)` so the 2-arg call is preserved for wave 2 (H1); effectiveMode / remembered-mode selection helpers"
    - "handoff.ts: email arm carries mailto subject+body (encoded); return contract { handoffStarted, assistUid } preserved from plan 35-01"
    - "contact-methods-dao.ts: NEW setContactMethodPrimary() single-method primary writer"
  key_links:
    - "app-settings default/remembered mode → compose-logic effective-mode selection → ComposeScreen mode init (later plan)"
    - "compose-logic destination resolve → performReachOut channel/endpoint → OS composer"
    - "establish-missing-primary picker → setContactMethodPrimary() → contact_methods (single-method primary swap)"
  prohibitions:
    - "Transmit must never claim Orbit delivered the message."
    - "Must never hardcode channel:'text'; channel derives from the resolved mode."
    - "Must never write the remembered mode on an ad-hoc in-session switch — only on Transmit or Copy."
    - "Must never move destination resolution off selectActionablePrimaryMethods into a bespoke store."
    - "Must never revert performReachOut's return type to Promise<void> (plan 35-01 widened it to { handoffStarted, assistUid })."
    - "Must never apply the SMS probe-pending gate to Email mode — Email has no expo-sms dependency."
    - "setContactMethodPrimary must never set two primaries for one method type — clear the prior primary in the same transaction (partial-unique index)."
---

<assumption_delta_decision>
Detector fired: text-only → Text/Email is a singular→plural (pluralization) transition, and today `channel: "text"` is hardcoded (ComposeScreen.tsx:456). Decision = **promote**: the primary noun becomes "message mode" (Text | Email), a first-class session + preference concept, not "text with an email bolt-on". Rationale: mode drives destination resolution, handoff transport, Subject visibility, and the remembered preference — treating it as a first-class enum (backed by migration 028's CHECK vocabulary) avoids scattering `if email` special-cases. Advisory, non-blocking.
</assumption_delta_decision>

<objective>
Build the pure delivery logic for Text/Email: extend `resolveComposeControls` to handle mode + a no-destination Transmit-unavailable state, add effective-mode/remembered-mode selection (reading migration-028 preferences), and extend the `handoff.ts` email arm to carry Subject + Body via an encoded `mailto` query string (no new dependency). All decision logic stays in the node-pure `compose-logic` module and its tests — the screen consumes it (WR-02: never re-derive capability in the screen).

Purpose: make Text/Email delivery a tested, off-device decision layer before the ComposeScreen integration plan wires it in.
Output: extended compose-logic + handoff with full test matrices.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend compose-logic for Text/Email mode + no-destination + remembered mode</name>
  <files>src/logic/compose-logic.ts, src/logic/compose-logic.test.ts</files>
  <behavior>
    - The extended signature is `resolveComposeControls(hasPhone, smsAvailable, mode='text', hasEmail=false)`: the two NEW params (`mode`, `hasEmail`) are OPTIONAL with defaults, appended AFTER the existing `(hasPhone, smsAvailable)`. Backward-compat is load-bearing (H1): the existing 2-arg call `resolveComposeControls(hasPhone, smsAvailable)` still type-checks AND returns the current Text-mode result (identical to `resolveComposeControls(hasPhone, smsAvailable, 'text', false)`), so ComposeScreen.tsx:798 and every existing 2-arg case in compose-logic.test.ts stay green through wave 2.
    - Text mode + primary phone present + SMS available → Transmit primary, Copy secondary.
    - Text mode + no phone → Transmit unavailable; Copy sole primary; establish-primary prompt available; not an error.
    - Email mode + primary email present → Transmit primary (email), Copy secondary.
    - Email mode + no email but phone exists → fall back to Text as the usable mode.
    - Neither phone nor email → Transmit unavailable in both modes; Copy sole primary; accessible explanation.
    - Text mode + smsAvailable===null (probe pending) → Transmit hidden, Copy primary, no helper (no-flash). Probe-pending gates TEXT only.
    - Email mode + primary email + smsAvailable===null → Transmit (email) STILL available; the SMS probe does not gate Email (no expo-sms dependency; mailto via Linking assumed available). Email has no probe-pending state.
    - effectiveMode(default='remember', remembered='text') → 'text'; effectiveMode(default='email') → 'email'.
    - remembered mode updates on Transmit/Copy only: a helper `nextRememberedMode(current, adHocMode, committed:boolean)` returns adHocMode only when committed is true.
  </behavior>
  <read_first>
    - src/logic/compose-logic.ts (FULL — resolveComposeControls :71-116 with the explicit-precedence + null=probe-pending idiom; ComposeControls interface :35; actionablePrimaryPhoneDestination :51)
    - src/logic/compose-logic.test.ts (FULL — existing matrix to extend, not rewrite)
    - src/db/contact-methods-read.ts (:10-19, :41-48 — selectActionablePrimaryMethods resolves BOTH phone and email; consume for both modes)
    - src/db/app-settings-dao.ts (the DefaultMessageMode/RememberedMessageMode types from plan 35-02 — import, do not redefine)
    - 35-UI-SPEC.md ("No-destination state" copy; the copyEmphasis resolver reference)
  </read_first>
  <action>
    Extend the pure gate signature to `resolveComposeControls(hasPhone, smsAvailable, mode?: 'text'|'email' = 'text', hasEmail?: boolean = false)` — the TWO NEW params (`mode`, `hasEmail`) are OPTIONAL with safe defaults (`mode` → 'text', `hasEmail` → false) and are APPENDED AFTER the existing `(hasPhone, smsAvailable)` positional params (H1 build-breaker fix). This is load-bearing: the existing TWO-argument call at ComposeScreen.tsx:798 (and every existing 2-arg case in compose-logic.test.ts) MUST keep type-checking AND keep returning the current Text-mode result through wave 2 — with `mode='text'`/`hasEmail=false` the resolver output is byte-identical to today's 2-arg Text gate. The screen rewire to the full 4-arg call stays in plan 35-07 (wave 3). Do NOT pull ComposeScreen.tsx into this plan, and do NOT make the new params required — required params would break THIS plan's own `npx tsc --noEmit` gate on the stale wave-1 2-arg call. IMPORTANT (HIGH-2): the `smsAvailable` probe (including the `=== null` probe-pending no-flash branch) governs TEXT mode ONLY. In Email mode the gate decides on hasEmail alone — Email is transmittable whenever an actionable primary email exists, regardless of smsAvailable, and Email has no probe-pending branch (mailto via Linking is assumed available). Preserve the existing Text precedence (probe-pending first, then missing-destination, then transport-unavailable, then full controls) and extend it to: fall back to the alternate mode when the preferred one has no destination; when neither exists, return a Transmit-unavailable + Copy-sole-primary state with a helper reason (the screen renders the accessible explanation copy). Add `effectiveMode(default, remembered)` resolving the 'remember' sentinel, and `nextRememberedMode(...)` that only advances the remembered value when a commit (Transmit/Copy) happened. Keep every branch pure and node-testable; do not import RN/expo. Extend the test file to cover the full behavior matrix above including empty (no-destination), the Email-transmittable-while-SMS-unknown case, and the remember-sentinel resolution.
  </action>
  <verify>
    <automated>npx vitest run src/logic/compose-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior row above has a passing test.
    - Test (H1 backward-compat): the 2-arg call `resolveComposeControls(hasPhone, smsAvailable)` returns the SAME object as the explicit `resolveComposeControls(hasPhone, smsAvailable, 'text', false)` for every (hasPhone, smsAvailable) row — the new params are optional with defaults; the existing 2-arg test cases stay green.
    - `npx tsc --noEmit` passes with ComposeScreen.tsx's existing 2-arg call UNCHANGED (this plan does not touch the screen; the 4-arg rewire is plan 35-07). This is the concrete proof H1 (the plan's own tsc gate on the stale 2-arg call) is closed.
    - Test: Email mode + primary email + smsAvailable===null → Transmit available (the SMS probe does not gate Email).
    - Test: Text mode + smsAvailable===null → Transmit hidden (probe-pending gates Text).
    - `grep -n "expo\|react-native\|from \"@/db" src/logic/compose-logic.ts` shows no RN/expo import (types-only import from app-settings-dao is acceptable).
    - No-destination case returns Transmit unavailable + Copy primary (asserted), not a thrown error.
  </acceptance_criteria>
  <done>compose-logic resolves mode, destination fallback, no-destination usability, and remembered-mode advancement; test matrix green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the handoff email arm to carry Subject + Body</name>
  <files>src/services/reach-out/handoff.ts, src/services/reach-out/handoff.test.ts</files>
  <read_first>
    - src/services/reach-out/handoff.ts (FULL — AFTER plan 35-01 this returns `{ handoffStarted: boolean; assistUid: string | null }`, NOT Promise<void>; createPendingAssist BEFORE handoff; Text arm SMS.sendSMSAsync; bare mailto/tel arm via Linking; markAssistFailed on catch; HANDOFF_ERROR_COPY. Build on the 35-01 return contract — do not revert it.)
    - src/services/reach-out/handoff.test.ts (FULL — existing coverage to extend, incl. the 35-01 return-shape tests)
    - src/db/interaction-assist-dao.ts (:27-67 — createPendingAssist contract; do not change it)
    - 35-RESEARCH.md §External Handoff + Package Legitimacy Audit (no-dependency mailto path is the recommended default; expo-mail-composer is an owner opt-in, NOT installed here)
  </read_first>
  <action>
    Extend the email branch of performReachOut to accept optional `subject` and `body` and build the URL as `mailto:${endpoint}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}` (omit empty params). Keep the Text arm (SMS.sendSMSAsync) and tel arm unchanged. Preserve the existing order: createPendingAssist (row stamped handoff_at=now) BEFORE the OS handoff; markAssistFailed on catch. PRESERVE the plan-35-01 return contract `{ handoffStarted, assistUid }` on every arm (the email arm returns the same structured outcome). Do NOT install expo-mail-composer or any package (the no-dependency Linking path is the chosen default; the richer composer remains a deferred owner opt-in). Extend handoff.test.ts to assert the email URL contains the encoded subject and body, that the assist row is created before the openURL call, and that the email arm returns the structured outcome.
  </action>
  <verify>
    <automated>npx vitest run src/services/reach-out/handoff.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts an email handoff with subject 'Hi & bye' + body 'line1\nline2' produces a mailto URL with encodeURIComponent-escaped subject and body.
    - Test asserts createPendingAssist is invoked before the handoff and markAssistFailed on a thrown handoff.
    - `grep -rn "expo-mail-composer" package.json src/` returns nothing (no dependency added).
    - Text arm behavior byte-unchanged for the SMS path.
  </acceptance_criteria>
  <done>Email handoff carries encoded subject+body via mailto with the assist row + failure handling intact; no new dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Copy targets — body-only vs subject-only</name>
  <files>src/logic/compose-logic.ts, src/logic/compose-logic.test.ts</files>
  <read_first>
    - src/logic/compose-logic.ts (the module extended in Task 1)
    - 35-UI-SPEC.md (Copywriting: main Copy copies the Body only → "Message copied"; Subject has its own copy affordance → "Subject copied")
  </read_first>
  <action>
    Add pure helpers that expose the two Copy targets so the screen never inlines the rule: the main Copy target is always the Body text; the Subject copy affordance target is the Subject text (Email mode only). Keep it a trivial, tested selector so the ComposeScreen integration wires deterministic strings. No RN/expo import.
  </action>
  <verify>
    <automated>npx vitest run src/logic/compose-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: main Copy target === body regardless of mode.
    - Test: Subject copy target === subject, and is only offered in Email mode.
  </acceptance_criteria>
  <done>Body-only and subject-only copy targets are pure, tested selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add setContactMethodPrimary() single-method primary writer (HIGH-7)</name>
  <files>src/db/contact-methods-dao.ts, src/db/contact-methods-dao.test.ts</files>
  <behavior>
    - Setting a contact's phone/email primary to a specific method id sets is_primary=1 for it and clears the prior primary of that method_type in the SAME transaction.
    - A no-op (already primary) leaves state unchanged and does not spuriously bump data_revision beyond the established pattern.
    - Only the targeted method_type is affected — the other type's primary is untouched.
    - The write is transaction-safe (single mutex/BEGIN) and rejects a method id that does not belong to the contact.
    - Deterministic single-transaction ordering (A3): (1) PRE-READ/VALIDATE the target by `WHERE id = ? AND contact_id = ? AND method_type = ?` — if it returns no row (foreign id, unknown id, or a `methodType` that disagrees with the row's stored `method_type`, e.g. an email method id passed with methodType:'phone'), make NO write and throw/rollback (so nothing is cleared or orphaned); (2) CLEAR the prior `is_primary=1` of that exact `(contactId, methodType)`; (3) PROMOTE the validated target to `is_primary=1` and assert EXACTLY ONE row changed, rolling back on zero. Clear precedes promote so no moment has two primaries of a type (statement-immediate partial-unique index); a promote-before-clear would trip that index.
  </behavior>
  <read_first>
    - src/db/contact-methods-dao.ts (FULL — VERIFIED this session: the ONLY writers are applyContactMethodDiffCore (:99) and applyContactMethodDiff (:298); there is NO standalone set-primary function. The primary-swap discipline lives inside applyContactMethodDiffCore at :159-197: a selected draft wins, and an existing primary of a type is cleared BEFORE the new one is set because SQLite's partial unique index is statement-immediate. Reuse that ordering. Note: RESEARCH.md's claim of a set-primary writer at :180-192 was WRONG — those lines are the clear-existing-primary step inside the diff core, not a standalone API.)
    - src/db/contact-methods-dao.test.ts (FULL — extend, do not rewrite)
    - src/db/transaction.ts (inWriteTransaction — the shared write mutex; compose the core inside one BEGIN)
    - src/db/contact-methods-read.ts (:10-19 selectActionablePrimaryMethods — the read the Compose picker uses to know which methods exist)
  </read_first>
  <action>
    Add a purpose-built, transaction-safe `setContactMethodPrimary(exec, { contactId, methodId, methodType, now })` to contact-methods-dao.ts for the lightweight Compose 'establish missing primary' picker. Do the whole thing in ONE write transaction with this DETERMINISTIC ordering that never transiently violates the statement-immediate partial-unique index `idx_contact_methods_primary_type ON contact_methods (contact_id, method_type) WHERE is_primary = 1` (verified at 009-contact-method-normalization.ts:235):
    (1) PRE-READ / VALIDATE: SELECT the target row by `WHERE id = ? AND contact_id = ? AND method_type = ?`. If it returns NO row (wrong contact, unknown id, or a `methodType` that disagrees with the row's stored method_type — e.g. an email method id passed with methodType:'phone'), make NO write and throw/rollback. Because this validation runs BEFORE any UPDATE, a mismatch can never clear or orphan the prior primary of any type.
    (2) CLEAR: UPDATE the prior `is_primary=1` row of that exact `(contactId, methodType)` to `is_primary=0`. This runs BEFORE the promote so no moment has two primaries of a type — matching the diff core's clear-before-set ordering at contact-methods-dao.ts:183-197.
    (3) PROMOTE: UPDATE the validated target (`WHERE id = ? AND contact_id = ? AND method_type = ?`) to `is_primary=1` and assert EXACTLY ONE row changed; if zero rows changed (e.g. a concurrent delete between the pre-read and the promote), throw/rollback so the transaction leaves the prior primary intact rather than a type with no primary.
    Bump data_revision consistently with the diff path (reuse the same revision-bump primitive rather than a bespoke UPDATE). Do NOT route the picker through applyContactMethodDiff — it demands the entire seeded/current method set and handles tombstones/normalization/ordering, which is wrong for a single-field primary swap. Keep the existing diff writers byte-unchanged. Extend contact-methods-dao.test.ts to cover: a valid primary swap clears the prior primary and promotes the target (exactly one row changed) WITHOUT a UNIQUE-constraint violation (proving clear precedes promote); the other method_type is untouched; a foreign method id is rejected in the pre-read (no write); a method id whose stored method_type MISMATCHES the passed methodType (email id + methodType:'phone') makes NO write and leaves BOTH the phone and email primaries unchanged; and the data_revision behavior matches the diff path.
  </action>
  <verify>
    <automated>npx vitest run src/db/contact-methods-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setContactMethodPrimary" src/db/contact-methods-dao.ts` shows the new exported writer; the promote UPDATE predicate includes `method_type` (grep the WHERE clause shows id + contact_id + method_type).
    - Test (A3 ordering): a valid swap succeeds with NO UNIQUE-constraint violation — proving CLEAR runs before PROMOTE (a promote-before-clear would trip the statement-immediate partial-unique index and throw); the prior primary of that type ends is_primary=0 and the target ends is_primary=1.
    - Test: setting a new phone primary clears the old phone primary and does not touch the email primary.
    - Test: a method id not belonging to the contact is rejected in the pre-read (no write — neither type's primary changes).
    - Test (integrity hardening #5): a method id whose stored method_type is 'email' passed with methodType:'phone' makes NO write — both the phone and email primaries are unchanged (no orphaned phone primary), and the call throws/rolls back rather than silently clearing.
    - Test: the promote asserts exactly one row changed (a matching promote updates exactly 1 row).
    - Test: primary swap bumps data_revision the same way the diff path does.
    - `git diff` shows applyContactMethodDiff / applyContactMethodDiffCore are unchanged.
  </acceptance_criteria>
  <done>A transaction-safe single-method setContactMethodPrimary exists for the Compose picker, respecting the partial-unique-primary index and the data-revision contract, with the diff writers untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose composition → OS email/SMS composer | User-authored subject/body cross the app boundary in a URL/intent. |
| Contact record → destination resolution | Contact-provided phone/email endpoints are used verbatim in the handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Tampering | handoff.ts email arm | high | mitigate | subject/body run through encodeURIComponent before entering the mailto query string (prevents query-param injection / breakage on `&`, `#`, newlines); assist row created before handoff; markAssistFailed on catch. |
| T-35-07 | Spoofing/Repudiation | Transmit | medium | mitigate | Handoff never claims delivery; honest confirmation is the sole truth (D-05). |
| T-35-11 | Denial of service | no-destination gate | low | mitigate | No-destination is a usable degraded state (Copy), never a crash/blocking error. |
| T-35-17 | Tampering | setContactMethodPrimary | medium | mitigate | Single transaction; clears the prior primary before setting the new one (partial-unique index); method id guarded to the contact; only a deliberate picker selection triggers it (no silent auto-set); data_revision bumped like the diff path. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (no-dependency mailto path); expo-mail-composer deferred as owner opt-in, not installed. |
</threat_model>

<verification>
- `npx vitest run src/logic/compose-logic.test.ts src/services/reach-out/handoff.test.ts src/db/contact-methods-dao.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Text/Email mode (probe-pending scoped to Text, Email transmittable while SMS unknown), destination fallback, no-destination usability, remembered-mode advancement, encoded email subject/body handoff (return contract preserved), and a single-method setContactMethodPrimary writer are all implemented as tested pure logic + service/DAO code with no new dependency.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/logic/compose-logic.ts` — mode-aware `resolveComposeControls(hasPhone, smsAvailable, mode='text', hasEmail=false)` (new `mode`/`hasEmail` params OPTIONAL with defaults so the wave-1 2-arg call is preserved — H1; probe-pending Text-only); `effectiveMode`; `nextRememberedMode`; body/subject copy-target selectors.
- `src/services/reach-out/handoff.ts` — email arm carries encoded `mailto` subject+body; return contract `{ handoffStarted, assistUid }` preserved from plan 35-01.
- `src/db/contact-methods-dao.ts` — NEW `setContactMethodPrimary()` single-method primary writer (single transaction ordered pre-read/validate → clear-prior → promote; method_type-scoped predicates + exactly-one-row assertion on the promote; no-write on foreign id or type mismatch; clear-before-promote respects the statement-immediate partial-unique index).
- Extended `src/logic/compose-logic.test.ts`, `src/services/reach-out/handoff.test.ts`, `src/db/contact-methods-dao.test.ts`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-03-SUMMARY.md` when done.
</output>
