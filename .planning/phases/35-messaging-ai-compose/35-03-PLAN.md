---
phase: 35-messaging-ai-compose
plan: 03
type: execute
wave: 2
depends_on: [35-02]
files_modified:
  - src/logic/compose-logic.ts
  - src/logic/compose-logic.test.ts
  - src/services/reach-out/handoff.ts
  - src/services/reach-out/handoff.test.ts
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-03: compose-logic resolves the primary phone for Text and the primary email for Email via selectActionablePrimaryMethods; the screen consumes this, never invents a destination store."
    - "COMP-03 (empty/no-destination): when the preferred mode has no destination, resolution falls back to the other usable mode; when NEITHER phone nor email exists, Transmit is unavailable (accessible explanation) and Copy stays the sole primary — drafting and Copy remain usable, not a blocking error."
    - "COMP-03: the smsAvailable===null probe-pending state keeps its no-flash discipline (Transmit hidden, Copy primary, no helper) and 'missing destination wins first' precedence is preserved."
    - "COMP-02 (empty/init + remembered): the effective mode initializes from default_message_mode — 'remember' resolves to remembered_message_mode; the remembered value is written ONLY on Transmit or Copy (never on an ad-hoc in-session switch)."
    - "COMP-02 (equality/encoding): mode equality is over the fixed lowercase token set {'text','email'} (and 'remember' sentinel for the default) — never a free-text or locale-sensitive comparison."
    - "COMP-04: the email handoff arm carries recipient + Subject + Body via a mailto query string with encodeURIComponent on subject and body (no-dependency Linking path); Text arm unchanged (SMS.sendSMSAsync); the assist row is still created before handoff and markAssistFailed still runs on catch."
    - "COMP-04: main Copy copies the Body only; the Subject has its own separate copy affordance (logic exposes body-only vs subject-only copy targets)."
    - "The channel passed to performReachOut derives from mode ('text'→text SMS, 'email'→email mailto), never a hardcoded literal."
  artifacts:
    - "compose-logic: resolveComposeControls extended for mode + no-destination; effectiveMode / remembered-mode selection helpers"
    - "handoff.ts: email arm carries mailto subject+body (encoded)"
  key_links:
    - "app-settings default/remembered mode → compose-logic effective-mode selection → ComposeScreen mode init (later plan)"
    - "compose-logic destination resolve → performReachOut channel/endpoint → OS composer"
  prohibitions:
    - "Transmit must never claim Orbit delivered the message."
    - "Must never hardcode channel:'text'; channel derives from the resolved mode."
    - "Must never write the remembered mode on an ad-hoc in-session switch — only on Transmit or Copy."
    - "Must never move destination resolution off selectActionablePrimaryMethods into a bespoke store."
---

<assumption_delta_decision>
Detector fired: text-only → Text/Email is a singular→plural (pluralization) transition, and today `channel: "text"` is hardcoded (ComposeScreen.tsx:456). Decision = **promote**: the primary noun becomes "message mode" (Text | Email), a first-class session + preference concept, not "text with an email bolt-on". Rationale: mode drives destination resolution, handoff transport, Subject visibility, and the remembered preference — treating it as a first-class enum (backed by migration 028's CHECK vocabulary) avoids scattering `if email` special-cases. Advisory, non-blocking.
</assumption_delta_decision>

<objective>
Build the pure delivery logic for Text/Email: extend `resolveComposeControls` to handle mode + a no-destination Transmit-unavailable state, add effective-mode/remembered-mode selection (reading migration-028 preferences), and extend the `handoff.ts` email arm to carry Subject + Body via an encoded `mailto` query string (no new dependency). All decision logic stays in the node-pure `compose-logic` module and its tests — the screen consumes it (WR-02: never re-derive capability in the screen).

Purpose: make Text/Email delivery a tested, off-device decision layer before the ComposeScreen integration plan wires it in.
Output: extended compose-logic + handoff with full test matrices.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend compose-logic for Text/Email mode + no-destination + remembered mode</name>
  <files>src/logic/compose-logic.ts, src/logic/compose-logic.test.ts</files>
  <behavior>
    - Text mode + primary phone present + SMS available → Transmit primary, Copy secondary.
    - Text mode + no phone → Transmit unavailable; Copy sole primary; establish-primary prompt available; not an error.
    - Email mode + primary email present → Transmit primary (email), Copy secondary.
    - Email mode + no email but phone exists → fall back to Text as the usable mode.
    - Neither phone nor email → Transmit unavailable in both modes; Copy sole primary; accessible explanation.
    - smsAvailable===null (probe pending) → Transmit hidden, Copy primary, no helper (no-flash), regardless of mode.
    - effectiveMode(default='remember', remembered='text') → 'text'; effectiveMode(default='email') → 'email'.
    - remembered mode updates on Transmit/Copy only: a helper `nextRememberedMode(current, adHocMode, committed:boolean)` returns adHocMode only when committed is true.
  </behavior>
  <read_first>
    - src/logic/compose-logic.ts (FULL — resolveComposeControls :71-116 with the explicit-precedence + null=probe-pending idiom; ComposeControls interface :35; actionablePrimaryPhoneDestination :51)
    - src/logic/compose-logic.test.ts (FULL — existing matrix to extend, not rewrite)
    - src/db/contact-methods-read.ts (:10-19, :41-48 — selectActionablePrimaryMethods resolves BOTH phone and email; consume for both modes)
    - src/db/app-settings-dao.ts (the DefaultMessageMode/RememberedMessageMode types from plan 35-02 — import, do not redefine)
    - 35-UI-SPEC.md ("No-destination state" copy; the copyEmphasis resolver reference)
  </read_first>
  <action>
    Extend the pure gate to take a `mode` ('text'|'email') and both destination presences (hasPhone, hasEmail) plus smsAvailable. Preserve the existing precedence (probe-pending first, then missing-destination, then transport-unavailable, then full controls) and extend it to: fall back to the alternate mode when the preferred one has no destination; when neither exists, return a Transmit-unavailable + Copy-sole-primary state with a helper reason (the screen renders the accessible explanation copy). Add `effectiveMode(default, remembered)` resolving the 'remember' sentinel, and `nextRememberedMode(...)` that only advances the remembered value when a commit (Transmit/Copy) happened. Keep every branch pure and node-testable; do not import RN/expo. Extend the test file to cover the full behavior matrix above including empty (no-destination) and the remember-sentinel resolution.
  </action>
  <verify>
    <automated>npx vitest run src/logic/compose-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior row above has a passing test.
    - `grep -n "expo\|react-native\|from \"@/db" src/logic/compose-logic.ts` shows no RN/expo import (types-only import from app-settings-dao is acceptable).
    - No-destination case returns Transmit unavailable + Copy primary (asserted), not a thrown error.
  </acceptance_criteria>
  <done>compose-logic resolves mode, destination fallback, no-destination usability, and remembered-mode advancement; test matrix green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the handoff email arm to carry Subject + Body</name>
  <files>src/services/reach-out/handoff.ts, src/services/reach-out/handoff.test.ts</files>
  <read_first>
    - src/services/reach-out/handoff.ts (FULL — performReachOut :39-73; createPendingAssist BEFORE handoff :50-57; Text arm SMS.sendSMSAsync :61; bare mailto/tel arm :63-65; markAssistFailed on catch :69; HANDOFF_ERROR_COPY)
    - src/services/reach-out/handoff.test.ts (FULL — existing coverage to extend)
    - src/db/interaction-assist-dao.ts (:27-67 — createPendingAssist contract; do not change it)
    - 35-RESEARCH.md §External Handoff + Package Legitimacy Audit (no-dependency mailto path is the recommended default; expo-mail-composer is an owner opt-in, NOT installed here)
  </read_first>
  <action>
    Extend the email branch of performReachOut to accept optional `subject` and `body` and build the URL as `mailto:${endpoint}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}` (omit empty params). Keep the Text arm (SMS.sendSMSAsync) and tel arm unchanged. Preserve the existing order: createPendingAssist (row stamped handoff_at=now) BEFORE the OS handoff; markAssistFailed on catch. Do NOT install expo-mail-composer or any package (the no-dependency Linking path is the chosen default; the richer composer remains a deferred owner opt-in). Extend handoff.test.ts to assert the email URL contains the encoded subject and body and that the assist row is created before the openURL call.
  </action>
  <verify>
    <automated>npx vitest run src/services/reach-out/handoff.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts an email handoff with subject 'Hi & bye' + body 'line1\nline2' produces a mailto URL with encodeURIComponent-escaped subject and body.
    - Test asserts createPendingAssist is invoked before the handoff and markAssistFailed on a thrown handoff.
    - `grep -rn "expo-mail-composer" package.json src/` returns nothing (no dependency added).
    - Text arm behavior byte-unchanged for the SMS path.
  </acceptance_criteria>
  <done>Email handoff carries encoded subject+body via mailto with the assist row + failure handling intact; no new dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Copy targets — body-only vs subject-only</name>
  <files>src/logic/compose-logic.ts, src/logic/compose-logic.test.ts</files>
  <read_first>
    - src/logic/compose-logic.ts (the module extended in Task 1)
    - 35-UI-SPEC.md (Copywriting: main Copy copies the Body only → "Message copied"; Subject has its own copy affordance → "Subject copied")
  </read_first>
  <action>
    Add pure helpers that expose the two Copy targets so the screen never inlines the rule: the main Copy target is always the Body text; the Subject copy affordance target is the Subject text (Email mode only). Keep it a trivial, tested selector so the ComposeScreen integration wires deterministic strings. No RN/expo import.
  </action>
  <verify>
    <automated>npx vitest run src/logic/compose-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: main Copy target === body regardless of mode.
    - Test: Subject copy target === subject, and is only offered in Email mode.
  </acceptance_criteria>
  <done>Body-only and subject-only copy targets are pure, tested selectors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose composition → OS email/SMS composer | User-authored subject/body cross the app boundary in a URL/intent. |
| Contact record → destination resolution | Contact-provided phone/email endpoints are used verbatim in the handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Tampering | handoff.ts email arm | high | mitigate | subject/body run through encodeURIComponent before entering the mailto query string (prevents query-param injection / breakage on `&`, `#`, newlines); assist row created before handoff; markAssistFailed on catch. |
| T-35-07 | Spoofing/Repudiation | Transmit | medium | mitigate | Handoff never claims delivery; honest confirmation is the sole truth (D-05). |
| T-35-11 | Denial of service | no-destination gate | low | mitigate | No-destination is a usable degraded state (Copy), never a crash/blocking error. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (no-dependency mailto path); expo-mail-composer deferred as owner opt-in, not installed. |
</threat_model>

<verification>
- `npx vitest run src/logic/compose-logic.test.ts src/services/reach-out/handoff.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Text/Email mode, destination fallback, no-destination usability, remembered-mode advancement, and encoded email subject/body handoff are all implemented as tested pure logic + service code with no new dependency.
</success_criteria>

<artifacts_produced>
- `src/logic/compose-logic.ts` — mode-aware `resolveComposeControls`; `effectiveMode`; `nextRememberedMode`; body/subject copy-target selectors.
- `src/services/reach-out/handoff.ts` — email arm carries encoded `mailto` subject+body.
- Extended `src/logic/compose-logic.test.ts`, `src/services/reach-out/handoff.test.ts`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-03-SUMMARY.md` when done.
</output>
