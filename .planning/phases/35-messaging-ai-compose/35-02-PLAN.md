---
phase: 35-messaging-ai-compose
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/028-compose-message-mode.ts
  - src/db/migrations/028-compose-message-mode.test.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/db/migrations/full-chain.test.ts
  - src/backup/backup-schema.ts
autonomous: false
requirements: [COMP-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-02 (durable half) / D-03: a new forward-only migration 028 adds two app_settings columns — default_message_mode ('remember'|'text'|'email', NOT NULL DEFAULT 'remember', CHECK-constrained) and remembered_message_mode ('text'|'email', NOT NULL DEFAULT 'text') — modeled exactly on migration 027's remember-sentinel pattern."
    - "COMP-02: app-settings-dao exposes typed getters/setters for the two compose-mode columns (defaultMessageMode, rememberedMessageMode), validated on WRITE by an assertMessageMode guard that rejects any value outside the CHECK vocabulary before the UPDATE opens."
    - "D-03: both keys are added to PORTABLE_SETTINGS_KEYS but are allowlisted-NOT-emitted — getPortableSettingsSnapshot does not emit them and BACKUP_FORMAT_VERSION is NOT bumped this phase (Phase 36 owns emission + the bump)."
    - "D-03: the migration touches app_settings ONLY — it never issues a second UPDATE against the shared interactions table (whose vocabulary migrated at 025; a second migration would be a double-migration hazard)."
    - "Migration 028 is registered in MIGRATIONS and TARGET_VERSION is bumped to 28 in src/db/database.ts; the full-chain test proves a v27→v28 jump applies additively with the singleton row gaining both NOT NULL defaults."
    - "Migration head was re-verified on disk at plan time: highest registered migration is 027 (TARGET_VERSION=27), so head+1 = 028; the executor re-verifies head+1=028 before writing."
    - "D-03 (one-way / irreversible): migration 028 is forward-only and irreversible in production (no remote repair); its irreversibility is acknowledged at a decision checkpoint before the migration is written."
  artifacts:
    - "src/db/migrations/028-compose-message-mode.ts — migration028 + COMPOSE_MESSAGE_MODE_SCHEMA_VERSION=28"
    - "src/db/migrations/028-compose-message-mode.test.ts — new migration test"
    - "app-settings-dao getters/setters + assertMessageMode validator + DefaultMessageMode/RememberedMessageMode types"
    - "backup-schema.ts PORTABLE_SETTINGS_KEYS additions (allowlisted-not-emitted)"
  key_links:
    - "database.ts MIGRATIONS registry + TARGET_VERSION → runMigrations applies 028 on launch"
    - "app-settings-dao SETTINGS_COLUMN_MAP → getAppSettings SELECT/return → typed compose-mode read"
    - "backup-schema PORTABLE_SETTINGS_KEYS gate (allowlist) — no emission this phase"
  prohibitions:
    - "Must never edit an already-shipped migration (001–027); add only the new 028."
    - "Must never bump BACKUP_FORMAT_VERSION or add the compose-mode keys to getPortableSettingsSnapshot this phase."
    - "Must never write a second UPDATE against the interactions table from this migration."
    - "Must never store the compose-mode preference in AsyncStorage — it is an app_settings column."
---

<objective>
Ship the durable half of COMP-02: the SQLite schema and DAO for the Compose default message mode. Add migration 028 (two additive `app_settings` columns modeled on migration 027's remember-sentinel), typed getters/setters with a write-time validator in `app-settings-dao.ts`, and allowlist the two keys in `PORTABLE_SETTINGS_KEYS` WITHOUT emitting them or bumping the backup format (Phase 36 owns emission + bump).

Purpose: give the compose-logic and ComposeScreen plans a real, backed-up-later preference to read/write, following the exact established `app_settings` idiom so the phase adds no schema debt.
Output: migration 028 + registry/TARGET_VERSION bump + DAO accessors + backup allowlist, all under Vitest + full-chain coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <reversibility rating="one-way">Migration 028 is a forward-only SQLite schema change; once shipped it is irreversible on unreachable user devices (no remote repair, no down-migration). The exact column names, CHECK vocabulary, and NOT NULL defaults become permanent for every user who runs it.</reversibility>
  <decision>Confirm the exact, irreversible migration 028 schema shape (column names, CHECK vocabulary, NOT NULL defaults) before it is written.</decision>
  <context>Migrations are forward-only and irreversible in production (CLAUDE.md — no remote DB access, no down-migration). A wrong CHECK constraint or default is permanent for every user who jumps to v28. This gate is blocking-human so an unattended run cannot silently commit an irreversible schema choice. Head+1 was verified on disk at plan time as 028 (highest registered migration 027 / TARGET_VERSION 27); the executor re-verifies before writing. Per D-03 the keys are allowlisted-not-emitted with NO BACKUP_FORMAT_VERSION bump this phase (Phase 36 owns emission + bump).</context>
  <options>
    <option id="as-designed">
      <name>Ship the researched shape (recommended)</name>
      <pros>Exactly mirrors migration 027's remember-sentinel template; `default_message_mode TEXT NOT NULL DEFAULT 'remember' CHECK(default_message_mode IN ('remember','text','email'))` + `remembered_message_mode TEXT NOT NULL DEFAULT 'text'`; additive, app_settings-only, no interactions-table touch.</pros>
      <cons>The 'text'/'email' vocabulary is locked once shipped.</cons>
    </option>
    <option id="revise-vocab">
      <name>Owner revises column names or CHECK vocabulary before writing</name>
      <pros>Owner can rename or extend the mode vocabulary now, while it is still free to change.</pros>
      <cons>Diverges from the 027 template; requires re-confirming against COMP-02 copy and the DAO validator.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" to ship the researched shape, or specify the revised column names / CHECK vocabulary.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write migration 028 + register + bump TARGET_VERSION</name>
  <files>src/db/migrations/028-compose-message-mode.ts, src/db/migrations/028-compose-message-mode.test.ts, src/db/database.ts, src/db/migrations/full-chain.test.ts</files>
  <read_first>
    - src/db/migrations/027-default-interaction-channel.ts (FULL — the EXACT template: version constant, `apply(exec, _deps)`, two `ALTER TABLE app_settings ADD COLUMN … NOT NULL DEFAULT …` with a CHECK on the sentinel column; header rules on app_settings-only/forward-only/additive)
    - src/db/database.ts (:62-99 — TARGET_VERSION = DEFAULT_INTERACTION_CHANNEL_SCHEMA_VERSION at :68; MIGRATIONS array at :71; runMigrations wiring at :175)
    - src/db/migrations/full-chain.test.ts (FULL — how the chain asserts each version's columns; extend for v28)
    - src/db/migrations/runner.ts (the runMigrations contract — version bump interpolated as an integer literal, guarded)
    - CLAUDE.md "SQLite migrations" (forward-only, strict order, never edit a shipped migration, irreversible in production)
  </read_first>
  <action>
    Re-verify on disk that the highest registered migration is 027 and TARGET_VERSION is 27 before proceeding (if a later migration has landed, use head+1 instead of 028 and stop to reconcile). Create `028-compose-message-mode.ts` exporting `COMPOSE_MESSAGE_MODE_SCHEMA_VERSION = 28` and a `migration028: Migration` whose `apply` runs a single `execAsync` adding the two columns per the checkpoint-confirmed DDL. Register `migration028` in the `MIGRATIONS` array in `database.ts` and set `TARGET_VERSION` to 28 (following the existing named-constant idiom). Add `028-compose-message-mode.test.ts` asserting the two columns exist with the correct defaults on a freshly-migrated DB and that the CHECK rejects an out-of-vocabulary default_message_mode. Update `full-chain.test.ts` so a v27→v28 chain applies additively (no data loss on the singleton row; both columns present with defaults). Touch `app_settings` ONLY — never the interactions table.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/028-compose-message-mode.test.ts src/db/migrations/full-chain.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION\|COMPOSE_MESSAGE_MODE_SCHEMA_VERSION" src/db/database.ts src/db/migrations/028-compose-message-mode.ts` shows TARGET_VERSION resolving to 28 and the migration registered.
    - Migration test proves both columns exist with defaults 'remember' / 'text' and the CHECK rejects e.g. 'sms'.
    - full-chain test green for the v27→v28 jump.
    - `grep -n "interactions" src/db/migrations/028-compose-message-mode.ts` returns nothing (app_settings only).
    - Migrations 001–027 are byte-unchanged (`git diff --name-only src/db/migrations/` lists only the new 028 file + full-chain.test.ts).
  </acceptance_criteria>
  <done>Migration 028 applies additively on a v27→v28 jump, is registered, TARGET_VERSION=28, and no shipped migration was edited.</done>
</task>

<task type="auto">
  <name>Task 3: DAO getters/setters + write validator + backup allowlist</name>
  <files>src/db/app-settings-dao.ts, src/backup/backup-schema.ts</files>
  <read_first>
    - src/db/app-settings-dao.ts (FULL — VERIFIED this session: the coupled shapes the compose-mode keys MUST be threaded through are `AppSettings` (interface :187), `AppSettingsPatch` (:420), `WritableSettingsKey` (:446), `AppSettingsRow` (row shape :491), and `COLUMN_OF` (the generic-patch column map :580, consumed by the `updateAppSettings` loop ~:1228). The `default_interaction_channel` / `remembered_interaction_channel` pair is the exact template through all of these; `assertOrreryDensity` / `assertDefaultInteractionChannel` are the validator templates; write posture header :12-20. NOTE: the ai_ack_* flags are DELIBERATELY absent from COLUMN_OF (:1341) and set only by `acknowledgeProvider` (:1357) — do not touch that.)
    - src/backup/backup-schema.ts (:135-232 — PORTABLE_SETTINGS_KEYS Set, the Phase-23/25 allowlist-not-emit comment idiom :158-191, the emission gate at :232; getPortableSettingsSnapshot around :780)
    - src/db/transaction.ts (inWriteTransaction contract)
    - 35-PATTERNS.md (app-settings-dao section — column-name map + read projection + write posture)
  </read_first>
  <action>
    Add `DefaultMessageMode` ('remember'|'text'|'email') and `RememberedMessageMode` ('text'|'email') types plus an `assertMessageMode` validator (mirroring assertDefaultInteractionChannel/assertOrreryDensity) that throws on any value outside the vocabulary. Thread both keys through ALL the coupled shapes so the generic patch path stays consistent (review LOW finding — do NOT add standalone setters that bypass it): extend `AppSettings` (typed read shape), `AppSettingsRow` (raw row), `AppSettingsPatch`, `WritableSettingsKey`, and `COLUMN_OF` (`defaultMessageMode → default_message_mode`, `rememberedMessageMode → remembered_message_mode`) so `updateAppSettings` can persist them, and add both columns to the getAppSettings SELECT list + typed return (cast on read as a read-shape convenience — validation is on write). The remembered-mode write goes through the generic validated patch path with `assertMessageMode` applied BEFORE the UPDATE opens, keeping the existing `changes===1` loud-failure guard. In backup-schema, add `"defaultMessageMode"` and `"rememberedMessageMode"` to PORTABLE_SETTINGS_KEYS with a comment matching the Phase-25 idiom stating they are allowlisted-NOT-emitted this phase (no emission, no BACKUP_FORMAT_VERSION bump — Phase 36 scope). Do NOT add them to getPortableSettingsSnapshot.
  </action>
  <verify>
    <automated>npx vitest run src/db/app-settings-dao.test.ts src/backup && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getAppSettings returns typed `defaultMessageMode` and `rememberedMessageMode`.
    - Both keys appear in `AppSettings`, `AppSettingsRow`, `AppSettingsPatch`, `WritableSettingsKey`, and `COLUMN_OF` (grep each) — the generic `updateAppSettings` patch path persists them; no standalone setter bypasses COLUMN_OF.
    - A patch with an out-of-vocabulary value throws via assertMessageMode before any UPDATE (test asserts no row change on reject).
    - `grep -n "defaultMessageMode\|rememberedMessageMode" src/backup/backup-schema.ts` shows both in PORTABLE_SETTINGS_KEYS.
    - `grep -c "BACKUP_FORMAT_VERSION =" src/backup/backup-schema.ts` unchanged from pre-plan value (no bump); the two keys are absent from getPortableSettingsSnapshot.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Typed, write-validated compose-mode accessors exist and both keys are allowlisted-not-emitted with no format bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App launch → migration runner | An irreversible schema mutation runs on every user's device with no remote repair path. |
| DAO setter → app_settings singleton | Untrusted mode values must be constrained before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06 | Tampering | migration 028 | high | mitigate | Forward-only, additive, app_settings-only; NOT NULL CHECK-constrained columns; decision checkpoint acknowledges irreversibility; full-chain test proves the v27→v28 jump; no shipped migration edited. |
| T-35-09 | Tampering | app-settings-dao setter | medium | mitigate | assertMessageMode rejects out-of-vocabulary values before the UPDATE opens; CHECK constraint is the defense-in-depth backstop. |
| T-35-10 | Information disclosure | backup allowlist | low | mitigate | Keys allowlisted but NOT emitted this phase; no new data leaves the device via backup until Phase 36 emission + format bump. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/db/migrations/028-compose-message-mode.test.ts src/db/migrations/full-chain.test.ts src/db/app-settings-dao.test.ts src/backup` green.
- `npx tsc --noEmit` clean.
- `git diff --name-only src/db/migrations/` shows only the new 028 file + full-chain.test.ts (no shipped migration edited).
</verification>

<success_criteria>
Migration 028 exists, is registered, applies additively on a v27→v28 jump; the DAO exposes write-validated compose-mode accessors; both keys are allowlisted-not-emitted with no BACKUP_FORMAT_VERSION bump.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks — they are new this phase):
- `src/db/migrations/028-compose-message-mode.ts` — `migration028`, `COMPOSE_MESSAGE_MODE_SCHEMA_VERSION = 28`; columns `default_message_mode`, `remembered_message_mode`.
- `src/db/migrations/028-compose-message-mode.test.ts` — new.
- `src/db/database.ts` — `MIGRATIONS` includes `migration028`; `TARGET_VERSION = 28`.
- `src/db/app-settings-dao.ts` — types `DefaultMessageMode`, `RememberedMessageMode`; `assertMessageMode`; `defaultMessageMode`/`rememberedMessageMode` threaded through `AppSettings`, `AppSettingsRow`, `AppSettingsPatch`, `WritableSettingsKey`, `COLUMN_OF`, and the getAppSettings read projection.
- `src/backup/backup-schema.ts` — `PORTABLE_SETTINGS_KEYS` gains `defaultMessageMode`, `rememberedMessageMode` (allowlisted-not-emitted).
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-02-SUMMARY.md` when done.
</output>
