---
phase: 35-messaging-ai-compose
plan: 06
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/db/compose-research-read.ts
  - src/db/compose-research-read.test.ts
  - src/screens/ComposeResearchScreen.tsx
  - src/stores/compose-session-store.ts
  - src/stores/compose-session-store.test.ts
autonomous: true
requirements: [COMP-08, COMP-11]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "COMP-08: Things to Remember Research is a sibling full-screen read-only projection over the Contact-Knowledge READ modules — showing ONLY populated conversation-relevant groups, excluding operational metadata, with NO add/edit/delete actions (it does NOT reuse ThingsToRememberScreen's editor UI)."
    - "COMP-08 (empty): nothing populated → a compact 'nothing to remember yet' caption (not Profile empty-state scaffolding); absent groups are simply not shown (no empty-section prompts, no data-completeness admin)."
    - "COMP-10 / D-13 / D-14 (display half — UNCHANGED): Off Limits renders as a distinct 'Avoid' group, visible to the human, never exposing Add to AI and never eligible for Message Focus. Off Limits is NEVER sent to AI in any form (D-14/ADR-107, reverses ADR-078's off-limits egress); this Research display is the human-facing surface for it."
    - "COMP-11: up to three AI-authorized research items can be toggled Add to AI ('Added ✓'), held as session-only Message Focus in the compose-session-store; Add to AI grants NO AI permission and runs no network op."
    - "COMP-11 (adjacency/cap/dedupe): the cap is a hard ceiling of three; re-adding an already-added item is a no-op toggle-off (dedupe by item identity); a fourth add is rejected while three are selected."
    - "COMP-11 (ordering): Message Focus preserves a stable, deterministic order (selection order); it is capped, not sorted-on-read."
    - "COMP-11 (eligibility): Add-to-AI eligibility reads the same structural allow_ai=1 truth as the SQL projection — never a component-side guess; Off Limits and non-AI-eligible items expose no Add to AI."
    - "COMP-08/11 / HIGH-6 (normalized Research-item view model): Add-to-AI eligibility is NOT one common shape across sources — VERIFIED on disk: memories expose `allow_ai` (memories-read.ts:24,31), custom fields expose `share_with_ai` (profile-knowledge-read.ts:88), first-class fields (birthday/socialBattery/interval/category) have NO permission field (first-class-knowledge-read.ts:11-16), and structured relationships (Key People) have NO permission field either (relationships-read.ts RelationshipRow has no allow_ai/share_with_ai — VERIFIED). A NEW read/projection module `compose-research-read.ts` builds a NORMALIZED `ResearchItem` view model at the read boundary — each item carries a stable identity, its group, display label/value, an explicit `aiEligible` boolean derived from the SOURCE-SPECIFIC permission (memory→allow_ai=1; customField→share_with_ai=1; firstClass→false, not Add-to-AI-selectable; relationship→false, not Add-to-AI-selectable; currentState→per its own rule, else false), and an `isOffLimits` flag. The session store's addToFocus consumes this validated shape; it NEVER infers eligibility from raw source rows."
    - "COMP-08 (Key People included): structured relationships (listRelationshipsForContact) ARE part of the conversation-relevant Research projection (Key People is a dossier-listed Research group, factory order PROF-14) — populated relationships appear as read-only ResearchItems with aiEligible=false (no Add to AI, since there is no per-relationship AI-permission), honoring resolveRelationshipVisibility for show/hide."
    - "D-10: Message Focus is session-only (in the compose-session-store), survives nav/background, clears on relaunch/confirm, and adds no durable draft/backup contract."
    - "COMP-08 (getFirstClassDerived export verification, review LOW): VERIFIED on disk — getFirstClassDerived AND getFirstClassFields both exist and are exported (first-class-knowledge-read.ts:27,59); the Research projection consumes them read-only; first-class fields render WITHOUT an Add-to-AI control (they have no permission field, so aiEligible=false)."
  artifacts:
    - "src/db/compose-research-read.ts — normalized ResearchItem read/projection (identity, group, label/value, aiEligible, isOffLimits) over the knowledge read modules"
    - "src/screens/ComposeResearchScreen.tsx — read-only Research projection consuming the normalized ResearchItem view model"
    - "compose-session-store: messageFocus[] (≤3) + addToFocus/removeFromFocus/isInFocus actions (consume the validated ResearchItem shape)"
  key_links:
    - "compose-research-read (normalized ResearchItem[] w/ aiEligible + isOffLimits) ← first-class-knowledge-read / current-state-history-read / memories-read / profile-knowledge-read / relationships-read (READ only)"
    - "ComposeResearchScreen ← compose-research-read (never raw source rows for eligibility)"
    - "Add to AI toggle (aiEligible ResearchItem only) → compose-session-store.messageFocus (session-only) → Compose-side 'Message focus · N' (wired in plan 35-09)"
  prohibitions:
    - "Must never reuse ThingsToRememberScreen's editor UI (MemoryEditor/RelationshipEditor/Switch/setCurrentStateValue) or expose any add/edit/delete on the Research side."
    - "Must never expose Add to AI on an Off Limits item or make it Message Focus."
    - "Must never use a UI-side .filter() for off_limits or AI-eligibility — the exclusion is structural in SQL; Add-to-AI eligibility comes from the normalized ResearchItem.aiEligible built at the read boundary (per-source permission), never a component/store guess over raw rows."
    - "Add to AI must never grant AI permission or perform a network operation."
    - "Must never exceed three Message Focus items or persist them durably."
---

<objective>
Build the read-only Things to Remember Research side and the session-only Message Focus state. `ComposeResearchScreen` is a compact projection over the Contact-Knowledge READ modules (NOT the full `ThingsToRememberScreen` editor) showing only populated conversation-relevant groups, with Off Limits as a distinct read-only "Avoid" group. Add up-to-three Add to AI / "Added ✓" toggling that stores selections as session-only Message Focus in the compose-session-store — granting no AI permission.

Purpose: deliver the human-facing knowledge display + selection without touching the ComposeScreen file (that wiring is plan 35-09), keeping this a self-contained new-screen + store-extension unit.
Output: the Research screen + Message Focus session state with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message Focus session state (≤3, dedupe, no permission)</name>
  <files>src/stores/compose-session-store.ts, src/stores/compose-session-store.test.ts</files>
  <read_first>
    - src/stores/compose-session-store.ts (the store created in plan 35-01 — extend, do not rewrite; preserve the session-survival/clear-on-relaunch behavior)
    - src/stores/compose-session-store.test.ts (extend)
    - src/db/memories-read.ts (:31 MEMORY_AI_ELIGIBILITY — the allow_ai=1 truth Add-to-AI eligibility must mirror)
    - 35-UI-SPEC.md (E4 Message Focus rows; dossier §O restrained selected highlight; cap of three)
  </read_first>
  <action>
    Extend the compose-session-store with a `messageFocus` array (session-only, ≤3) keyed by contact, plus actions `addToFocus(item)`, `removeFromFocus(itemId)`, and `isInFocus(itemId)`. `addToFocus` accepts the VALIDATED normalized ResearchItem shape from plan Task 2 (`compose-research-read.ts` — carries a stable identity + `aiEligible` + `isOffLimits`) and rejects any item where `aiEligible !== true` or `isOffLimits === true` (the store never infers eligibility from raw rows — HIGH-6). Enforce: hard cap of three (a fourth add while three are selected is a no-op / rejected); dedupe by item identity (adding an already-present item toggles it off — "tap again removes"); stable selection order (append; do not sort). Message Focus clears with the session (on clearSession / relaunch). Add to AI grants NO permission — the store holds a reference only and performs no network/DAO op. Extend the test file for cap, dedupe, order, clear-with-session, and the eligibility/off-limits rejection.
  </action>
  <verify>
    <automated>npx vitest run src/stores/compose-session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: adding a 4th item while 3 are selected leaves the set at 3.
    - Test: re-adding an existing item removes it (toggle) — no duplicate.
    - Test: selection order is preserved (append order), not sorted.
    - Test: clearSession empties messageFocus.
    - No DAO/network import added to the store.
  </acceptance_criteria>
  <done>Message Focus is a capped, deduped, ordered, session-only selection that grants no permission.</done>
</task>

<task type="auto">
  <name>Task 2: compose-research-read — normalized ResearchItem view model (HIGH-6)</name>
  <files>src/db/compose-research-read.ts, src/db/compose-research-read.test.ts</files>
  <read_first>
    - src/db/memories-read.ts (:24 allow_ai on MemoryRow; :31 MEMORY_AI_ELIGIBILITY = "allow_ai = 1 AND deleted_at IS NULL"; listMemoriesForContact / resolveVisibility)
    - src/db/profile-knowledge-read.ts (:88 share_with_ai on the custom-field row; :160-182 the projection incl. shareWithAi + readProfileOffLimits)
    - src/db/first-class-knowledge-read.ts (:11-53 — FirstClassFields have NO allow_ai/share_with_ai; getFirstClassFields / getFirstClassDerived)
    - src/db/relationships-read.ts (:21-38 — listRelationshipsForContact returns RelationshipRow with person_name / relation_type / linked_contact_name / note; VERIFIED RelationshipRow has NO allow_ai/share_with_ai column → aiEligible=false; :41-47 resolveRelationshipVisibility for show/hide)
    - src/db/current-state-history-read.ts (getCurrentStateValues — check whether current-state entries carry any AI-permission; if none, they are not Add-to-AI-selectable)
    - src/db/memory-registry.ts (KNOWLEDGE_GROUP_ORDER / MEMORY_TYPE_REGISTRY / RELATIONSHIPS_GROUP — group identity + order)
    - 35-UI-SPEC.md (E3/E4 rows; Off Limits 'Avoid' group; Add to AI only on AI-authorized items)
  </read_first>
  <action>
    Create `compose-research-read.ts` exporting a `ResearchItem` type — `{ id: string; group: string; label: string; value: string; aiEligible: boolean; isOffLimits: boolean }` — and a `readComposeResearch(exec, contactId)` that composes the existing knowledge READ modules into a normalized, populated-only list in KNOWLEDGE_GROUP_ORDER. Derive `aiEligible` from the SOURCE-SPECIFIC permission at this boundary: memories → allow_ai=1; custom fields → share_with_ai=1; first-class fields → false (no permission field, not Add-to-AI-selectable); structured relationships (Key People, via listRelationshipsForContact, honoring resolveRelationshipVisibility) → false (RelationshipRow has no permission field — VERIFIED, not Add-to-AI-selectable); current-state → false unless it carries its own AI-permission truth. Set `isOffLimits` for off-limits items (which are always aiEligible=false for Add-to-AI — Off Limits can never be Message Focus). Give each item a STABLE identity (source-type + source id) so the session store can dedupe/toggle by identity. Do NOT relax any SQL exclusion; do NOT read secrets; use formatLocalDate for any timestamp. Write compose-research-read.test.ts asserting the per-source aiEligible derivation (memory allow_ai, custom-field share_with_ai, first-class false, relationship false), isOffLimits marking, that a populated relationship appears as a read-only ResearchItem, and that only populated groups appear.
  </action>
  <verify>
    <automated>npx vitest run src/db/compose-research-read.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: a memory with allow_ai=1 → ResearchItem.aiEligible true; allow_ai=0 → false.
    - Test: a custom field with share_with_ai=1 → aiEligible true; =0 → false.
    - Test: a first-class field → aiEligible false (no Add to AI).
    - Test: a structured relationship (Key People) → ResearchItem present with aiEligible false (no Add to AI); a populated relationship appears in the projection and honors resolveRelationshipVisibility.
    - Test: an off-limits item → isOffLimits true AND aiEligible false.
    - Test: only populated groups are returned (empty groups omitted).
    - `grep -n "expo\|react-native" src/db/compose-research-read.ts` returns nothing (read module, node-testable).
  </acceptance_criteria>
  <done>A normalized ResearchItem read boundary derives per-source Add-to-AI eligibility and off-limits marking, tested for every source type.</done>
</task>

<task type="auto">
  <name>Task 3: ComposeResearchScreen — read-only knowledge projection</name>
  <files>src/screens/ComposeResearchScreen.tsx</files>
  <read_first>
    - src/screens/ThingsToRememberScreen.tsx (:19-58 imports — REUSE ONLY the READ modules; do NOT reuse MemoryEditor/RelationshipEditor/Switch/setCurrentStateValue or any add/edit/delete DAO)
    - src/db/compose-research-read.ts (Task 2 — the normalized ResearchItem[] the screen renders; the screen consumes aiEligible/isOffLimits, never re-derives them)
    - src/db/memory-registry.ts (KNOWLEDGE_GROUP_ORDER — group order for display)
    - src/components/ui/AppText.tsx + button-roles.ts (roles: heading/label/caption; tertiary accentText for the Add to AI link tone)
    - 35-UI-SPEC.md (E3 rows; Copywriting "Empty state — Research" / "Off Limits presentation"; role assignments)
  </read_first>
  <action>
    Build a new full-screen read-only projection that renders the normalized ResearchItem[] from compose-research-read (Task 2): only populated conversation-relevant groups in KNOWLEDGE_GROUP_ORDER as a compact scannable list (no cards-heavy Profile scaffolding); exclude operational metadata; expose NO add/edit/delete. Off Limits (isOffLimits) renders as a distinct 'Avoid' group (read-only, no Add to AI, never Message Focus). For items where ResearchItem.aiEligible is true, render an Add to AI / 'Added ✓' toggle bound to the Message Focus store from Task 1 with the restrained selected-highlight treatment (accent-tinted surface/border, not full fill). Empty → compact 'nothing to remember yet' caption. All text in AppText roles, all colours via theme tokens, controls pad to MIN_TOUCH_TARGET. Do NOT use a UI-side .filter() for off_limits/AI-eligibility and do NOT re-derive eligibility in the screen — consume ResearchItem.aiEligible/isOffLimits.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MemoryEditor\|RelationshipEditor\|setCurrentStateValue\|deleteMemory\|upsert" src/screens/ComposeResearchScreen.tsx` returns nothing (no editor/mutation).
    - `grep -n "allow_ai\|share_with_ai" src/screens/ComposeResearchScreen.tsx` returns nothing (eligibility comes from ResearchItem.aiEligible, not re-derived).
    - `grep -n "fontSize" src/screens/ComposeResearchScreen.tsx` shows no raw fontSize (roles only); `npm run check:colors` passes.
    - Off Limits group renders without an Add to AI control; Add to AI toggle appears only where aiEligible is true.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A read-only Research projection renders the normalized items — populated groups + an Avoid group + Add-to-AI on aiEligible items only — with no editor UI and no re-derived eligibility.</done>
</task>

<task type="auto">
  <name>Task 4: Add-to-AI eligibility guard test (validated ResearchItem shape)</name>
  <files>src/stores/compose-session-store.test.ts</files>
  <read_first>
    - src/stores/compose-session-store.test.ts (Task 1 output)
    - src/db/compose-research-read.ts (the normalized ResearchItem shape with aiEligible/isOffLimits — the store guards on THIS, not raw rows)
    - 35-UI-SPEC.md (E4/E3 — Add to AI only on AI-authorized items; Off Limits never Message Focus)
  </read_first>
  <action>
    Add tests locking that only `aiEligible === true` ResearchItems can enter Message Focus and that an `isOffLimits` item can never be added — the store's addToFocus rejects a non-eligible / off-limits item (the guard reads the validated ResearchItem fields, not raw source rows). This fences COMP-11's "AI-authorized only" and the never-Message-Focus rule for Off Limits at the store boundary (HIGH-6).
  </action>
  <verify>
    <automated>npx vitest run src/stores/compose-session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: a ResearchItem with aiEligible=false cannot become Message Focus.
    - Test: a ResearchItem with isOffLimits=true cannot become Message Focus.
    - Test: an aiEligible=true item can be added (up to the cap of three).
  </acceptance_criteria>
  <done>Add-to-AI eligibility and the Off-Limits-never-focus rule are fenced by tests over the validated ResearchItem shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite knowledge reads → read-only Research UI | Only populated, non-operational, AI-authorized-where-relevant data is projected. |
| Add to AI toggle → session Message Focus | A selection reference only; grants no permission, performs no egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-15 | Elevation of privilege | Add to AI eligibility | high | mitigate | Eligibility is derived once at the read boundary (compose-research-read → ResearchItem.aiEligible from each source's own permission: memory allow_ai, custom-field share_with_ai, first-class none) — never a UI/store guess over raw rows; Off Limits (isOffLimits) exposes no Add to AI and can never be Message Focus; the store rejects non-eligible/off-limits items; Add to AI grants no permission. |
| T-35-16 | Information disclosure | Off Limits Avoid group | medium | mitigate | Off Limits is a human-visible read-only Avoid group only; it is NEVER carried to AI in any form — positive OR negative avoidance constraint (D-14/ADR-107, reverses ADR-078's off-limits egress) — and can never be Message Focus. |
| T-35-08 | Denial of state | Message Focus session store | low | accept | Session-only; loss on relaunch is by design (D-10). |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/db/compose-research-read.test.ts src/stores/compose-session-store.test.ts` green.
- `npx tsc --noEmit` clean; `npm run check:colors` passes.
- Device backstop (phase gate): on the Pixel, open Research, confirm only populated groups + an Avoid group render, add three items (fourth rejected), toggle one off, background+return (selection survives).
</verification>

<success_criteria>
A read-only Research projection over a normalized ResearchItem view model (per-source Add-to-AI eligibility) with an Avoid group and capped/deduped session-only Message Focus exists, granting no AI permission, inferring no eligibility in UI/store, and reusing no editor UI.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/db/compose-research-read.ts` — NEW `ResearchItem` type + `readComposeResearch(exec, contactId)` normalized projection (identity, group, label, value, aiEligible, isOffLimits) over memories, custom fields, first-class fields, Key People relationships, current state, and off-limits.
- `src/screens/ComposeResearchScreen.tsx` — new read-only Research projection consuming ResearchItem[].
- `src/stores/compose-session-store.ts` — `messageFocus` (≤3) + `addToFocus`/`removeFromFocus`/`isInFocus` (guard on ResearchItem.aiEligible/isOffLimits).
- New test `src/db/compose-research-read.test.ts`; extended `src/stores/compose-session-store.test.ts`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-06-SUMMARY.md` when done.
</output>
