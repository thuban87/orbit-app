---
phase: 35-messaging-ai-compose
plan: 06
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/screens/ComposeResearchScreen.tsx
  - src/stores/compose-session-store.ts
  - src/stores/compose-session-store.test.ts
autonomous: true
requirements: [COMP-08, COMP-11]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-08: Things to Remember Research is a sibling full-screen read-only projection over the Contact-Knowledge READ modules — showing ONLY populated conversation-relevant groups, excluding operational metadata, with NO add/edit/delete actions (it does NOT reuse ThingsToRememberScreen's editor UI)."
    - "COMP-08 (empty): nothing populated → a compact 'nothing to remember yet' caption (not Profile empty-state scaffolding); absent groups are simply not shown (no empty-section prompts, no data-completeness admin)."
    - "COMP-10/D-13 (display half): Off Limits renders as a distinct 'Avoid' group, visible to the human, never exposing Add to AI and never eligible for Message Focus."
    - "COMP-11: up to three AI-authorized research items can be toggled Add to AI ('Added ✓'), held as session-only Message Focus in the compose-session-store; Add to AI grants NO AI permission and runs no network op."
    - "COMP-11 (adjacency/cap/dedupe): the cap is a hard ceiling of three; re-adding an already-added item is a no-op toggle-off (dedupe by item identity); a fourth add is rejected while three are selected."
    - "COMP-11 (ordering): Message Focus preserves a stable, deterministic order (selection order); it is capped, not sorted-on-read."
    - "COMP-11 (eligibility): Add-to-AI eligibility reads the same structural allow_ai=1 truth as the SQL projection — never a component-side guess; Off Limits and non-AI-eligible items expose no Add to AI."
    - "D-10: Message Focus is session-only (in the compose-session-store), survives nav/background, clears on relaunch/confirm, and adds no durable draft/backup contract."
  artifacts:
    - "src/screens/ComposeResearchScreen.tsx — read-only Research projection over the knowledge read modules"
    - "compose-session-store: messageFocus[] (≤3) + addToFocus/removeFromFocus/isInFocus actions"
  key_links:
    - "ComposeResearchScreen ← first-class-knowledge-read / current-state-history-read / memories-read / profile-knowledge-read (READ only)"
    - "Add to AI toggle → compose-session-store.messageFocus (session-only) → Compose-side 'Message focus · N' (wired in plan 35-09)"
  prohibitions:
    - "Must never reuse ThingsToRememberScreen's editor UI (MemoryEditor/RelationshipEditor/Switch/setCurrentStateValue) or expose any add/edit/delete on the Research side."
    - "Must never expose Add to AI on an Off Limits item or make it Message Focus."
    - "Must never use a UI-side .filter() for off_limits or AI-eligibility — the exclusion is structural in SQL; Add-to-AI eligibility reads allow_ai=1."
    - "Add to AI must never grant AI permission or perform a network operation."
    - "Must never exceed three Message Focus items or persist them durably."
---

<objective>
Build the read-only Things to Remember Research side and the session-only Message Focus state. `ComposeResearchScreen` is a compact projection over the Contact-Knowledge READ modules (NOT the full `ThingsToRememberScreen` editor) showing only populated conversation-relevant groups, with Off Limits as a distinct read-only "Avoid" group. Add up-to-three Add to AI / "Added ✓" toggling that stores selections as session-only Message Focus in the compose-session-store — granting no AI permission.

Purpose: deliver the human-facing knowledge display + selection without touching the ComposeScreen file (that wiring is plan 35-09), keeping this a self-contained new-screen + store-extension unit.
Output: the Research screen + Message Focus session state with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/decisions/ADR-078-negative-constraint-off-limits-and-gated-recent-interaction-ai-context.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message Focus session state (≤3, dedupe, no permission)</name>
  <files>src/stores/compose-session-store.ts, src/stores/compose-session-store.test.ts</files>
  <read_first>
    - src/stores/compose-session-store.ts (the store created in plan 35-01 — extend, do not rewrite; preserve the session-survival/clear-on-relaunch behavior)
    - src/stores/compose-session-store.test.ts (extend)
    - src/db/memories-read.ts (:31 MEMORY_AI_ELIGIBILITY — the allow_ai=1 truth Add-to-AI eligibility must mirror)
    - 35-UI-SPEC.md (E4 Message Focus rows; dossier §O restrained selected highlight; cap of three)
  </read_first>
  <action>
    Extend the compose-session-store with a `messageFocus` array (session-only, ≤3) keyed by contact, plus actions `addToFocus(item)`, `removeFromFocus(itemId)`, and `isInFocus(itemId)`. Enforce: hard cap of three (a fourth add while three are selected is a no-op / rejected); dedupe by item identity (adding an already-present item toggles it off or is a no-op — pick toggle-off to match "tap again removes"); stable selection order (append; do not sort). Message Focus clears with the session (on clearSession / relaunch). Add to AI grants NO permission — the store holds a reference only and performs no network/DAO op. Extend the test file for cap, dedupe, order, and clear-with-session.
  </action>
  <verify>
    <automated>npx vitest run src/stores/compose-session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: adding a 4th item while 3 are selected leaves the set at 3.
    - Test: re-adding an existing item removes it (toggle) — no duplicate.
    - Test: selection order is preserved (append order), not sorted.
    - Test: clearSession empties messageFocus.
    - No DAO/network import added to the store.
  </acceptance_criteria>
  <done>Message Focus is a capped, deduped, ordered, session-only selection that grants no permission.</done>
</task>

<task type="auto">
  <name>Task 2: ComposeResearchScreen — read-only knowledge projection</name>
  <files>src/screens/ComposeResearchScreen.tsx</files>
  <read_first>
    - src/screens/ThingsToRememberScreen.tsx (:19-58 imports — REUSE ONLY the READ modules; do NOT reuse MemoryEditor/RelationshipEditor/Switch/setCurrentStateValue or any add/edit/delete DAO)
    - src/db/first-class-knowledge-read.ts (getFirstClassDerived / getFirstClassFields)
    - src/db/current-state-history-read.ts (getCurrentStateValues)
    - src/db/memories-read.ts (listMemoriesForContact / resolveVisibility / MEMORY_AI_ELIGIBILITY)
    - src/db/memory-registry.ts (KNOWLEDGE_GROUP_ORDER / MEMORY_TYPE_REGISTRY)
    - src/db/profile-knowledge-read.ts
    - src/components/ui/AppText.tsx + button-roles.ts (roles: heading/label/caption; tertiary accentText for the Add to AI link tone)
    - 35-UI-SPEC.md (E3 rows; Copywriting "Empty state — Research" / "Off Limits presentation"; role assignments)
  </read_first>
  <action>
    Build a new full-screen read-only projection over the knowledge READ modules: render ONLY populated conversation-relevant groups in KNOWLEDGE_GROUP_ORDER as a compact scannable list (no cards-heavy Profile scaffolding); exclude operational metadata; expose NO add/edit/delete. Off Limits renders as a distinct 'Avoid' group (read-only, no Add to AI, never Message Focus). For AI-eligible items (allow_ai=1 truth from the read module, never a UI guess), render an Add to AI / 'Added ✓' toggle bound to the Message Focus store from Task 1 with the restrained selected-highlight treatment (accent-tinted surface/border, not full fill). Empty → compact 'nothing to remember yet' caption. All text in AppText roles, all colours via theme tokens, controls pad to MIN_TOUCH_TARGET. Do not use a UI-side .filter() for off_limits/AI-eligibility — consume the structural read outputs.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MemoryEditor\|RelationshipEditor\|setCurrentStateValue\|deleteMemory\|upsert" src/screens/ComposeResearchScreen.tsx` returns nothing (no editor/mutation).
    - `grep -n "fontSize" src/screens/ComposeResearchScreen.tsx` shows no raw fontSize (roles only); `npm run check:colors` passes.
    - Off Limits group renders without an Add to AI control (asserted by code structure / a lightweight render check).
    - Add to AI toggle is present only on allow_ai=1 items.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A read-only Research projection renders populated groups + an Avoid group + AI-eligible Add-to-AI toggles, with no editor UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add-to-AI eligibility guard test</name>
  <files>src/stores/compose-session-store.test.ts</files>
  <read_first>
    - src/stores/compose-session-store.test.ts (Task 1 output)
    - src/db/memories-read.ts (:31 the eligibility predicate)
    - 35-UI-SPEC.md (E4/E3 — Add to AI only on AI-authorized items; Off Limits never Message Focus)
  </read_first>
  <action>
    Add tests locking that only allow_ai-eligible items can enter Message Focus and that an Off Limits item can never be added (the store's addToFocus should reject a non-eligible/off-limits item, or the eligibility is asserted at the call boundary — document the chosen guard in the test). This fences COMP-11's "AI-authorized only" and the never-Message-Focus rule for Off Limits.
  </action>
  <verify>
    <automated>npx vitest run src/stores/compose-session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: a non-AI-eligible / off-limits item cannot become Message Focus.
    - Test: an allow_ai=1 item can be added (up to the cap).
  </acceptance_criteria>
  <done>Add-to-AI eligibility and the Off-Limits-never-focus rule are fenced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite knowledge reads → read-only Research UI | Only populated, non-operational, AI-authorized-where-relevant data is projected. |
| Add to AI toggle → session Message Focus | A selection reference only; grants no permission, performs no egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-15 | Elevation of privilege | Add to AI eligibility | high | mitigate | Eligibility mirrors the structural allow_ai=1 SQL truth (never a UI guess); Off Limits exposes no Add to AI and can never be Message Focus; Add to AI grants no permission. |
| T-35-16 | Information disclosure | Off Limits Avoid group | medium | mitigate | Off Limits is a human-visible read-only Avoid group only; carried to AI solely as a negative constraint via plan 35-05, never as positive context. |
| T-35-08 | Denial of state | Message Focus session store | low | accept | Session-only; loss on relaunch is by design (D-10). |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/stores/compose-session-store.test.ts` green.
- `npx tsc --noEmit` clean; `npm run check:colors` passes.
- Device backstop (phase gate): on the Pixel, open Research, confirm only populated groups + an Avoid group render, add three items (fourth rejected), toggle one off, background+return (selection survives).
</verification>

<success_criteria>
A read-only Research projection with an Avoid group and capped/deduped session-only Message Focus exists, granting no AI permission and reusing no editor UI.
</success_criteria>

<artifacts_produced>
- `src/screens/ComposeResearchScreen.tsx` — new read-only Research projection.
- `src/stores/compose-session-store.ts` — `messageFocus` (≤3) + `addToFocus`/`removeFromFocus`/`isInFocus`.
- Extended `src/stores/compose-session-store.test.ts`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-06-SUMMARY.md` when done.
</output>
