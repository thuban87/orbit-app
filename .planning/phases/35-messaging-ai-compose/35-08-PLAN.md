---
phase: 35-messaging-ai-compose
plan: 08
type: execute
wave: 4
depends_on: [35-04, 35-05, 35-07]
files_modified:
  - src/ai/prompt-template.ts
  - src/ai/prompt-template.test.ts
  - src/screens/ComposeScreen.tsx
autonomous: true
requirements: [COMP-09, COMP-12, COMP-13]
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "COMP-12 / D-09: one adaptive AI action reads 'Draft with AI' on an empty editor and 'Rewrite with AI' when the editor holds meaningful text; it never auto-writes on open; invoking it returns exactly three unlabeled varied suggestions on a non-destructive review surface (editor untouched until 'Choose this'); 'Try Again' replaces the set; Rewrite also shows the original with a keep-original path."
    - "COMP-12: 'Choose this' is the ONLY action that mutates the editor (applies one suggestion); suggestion text renders at body size 16/24 (no shrink for AI panels)."
    - "COMP-12 / HIGH-3 (three-variant fan-out wiring): the lifecycle's injected `generate` dep is wired to `generateVariants(provider.generate, prompt, signal, 3)` (plan 35-04 helper) so exactly three variants are produced by three cancellable provider.generate calls under the lifecycle's ONE AbortController signal; AiService is not modified. Cancel/timeout aborts all three; any single failure surfaces the sanitized error with Try Again."
    - "COMP-12 / HIGH-3 (Rewrite body → bounded prompt path): this plan EXTENDS `resolvePrompt(template, context, sourceDraft?)` in prompt-template.ts so the Rewrite path has a REAL bounded/delimited path into the prompt — VERIFIED the current resolver has no source-draft param (prompt-template.ts:142) and ResolvedPrompt has no such field (prompt-types.ts:159), so without this the draft could not enter the prompt except by raw concatenation. For Rewrite, ComposeScreen passes the current editor body as `sourceDraft`, rendered as a fenced `MESSAGE TO REWRITE` DATA block (sanitized, code-point-bounded) with MINIMAL §P-411 rewrite framing (preserve the user's message rather than an unrelated starter) — never raw string-concatenated. It is the user's OWN composition, not contact-data egress; PromptContext (the contact-data allowlist) is unchanged and sourceDraft is a resolvePrompt PARAM, not a PromptContext field. Draft (empty editor) passes no sourceDraft and the Draft prompt is byte-identical to today. Sophisticated generation-context construction (channel-awareness, personalization weighting, provider-specific prompting, exact variation instructions) remains Phase 36 (§U/§Q/D-13)."
    - "COMP-13: generation is cancellable ('Cancel') and failure-safe — a timeout/error/cancel preserves the manual draft, offers 'Try Again'/'Cancel', maps a sanitized code to a short line, and never becomes a provider-troubleshooting surface."
    - "COMP-09 / D-07 / D-12: the screen consumes the ai-availability adapter (computes none itself); AI Off removes every AI affordance; AI On+Ready exposes them; AI On+Needs Attention shows the restrained 'AI needs attention' repair notice that REPLACES (never silently hides) the AI actions and routes to the existing AI settings surface (interim), without restoring Draft/Rewrite/Add-to-AI."
    - "COMP-09: manual composition and the Research side work fully in every AI state."
    - "The review surface resolves from the same prompt construction path and never displays credentials or raw provider text (ADR-079)."
  artifacts:
    - "prompt-template.ts: resolvePrompt gains an OPTIONAL bounded/delimited `sourceDraft` param for the Rewrite path (HIGH-3, §P-411 minimal framing); Draft output unchanged; sourceDraft is NOT a PromptContext field"
    - "ComposeScreen: adaptive AI action + three-suggestion review surface + availability-state rendering wired to AiSuggestionLifecycle + ai-availability"
  key_links:
    - "adaptive action → AiSuggestionLifecycle.begin() → three suggestions → review surface → chooseSuggestion applies one to the editor"
    - "ai-availability.computeAiAvailability → affordance posture (off/ready/needs-attention) → AI control rendering"
  prohibitions:
    - "Must never auto-start a generation on open."
    - "Must never mutate the editor before an explicit 'Choose this'."
    - "Must never silently hide AI in Needs-Attention — a restrained repair notice replaces the actions."
    - "Must never display credentials or raw provider text in the review/error surface."
    - "Must never reintroduce the ADR-052 exact-prompt acknowledgement gate."
---

<objective>
Extend `resolvePrompt` with a bounded/delimited Rewrite source-draft (HIGH-3), then wire the reshaped AI lifecycle (plan 35-04) and the shape-carrying egress (plan 35-05) into ComposeScreen: the single adaptive Draft/Rewrite action, the non-destructive three-suggestion review surface (Choose this / Try Again / Cancel), the Rewrite keep-original path, and the three AI-availability states consumed from the adapter. Second expansion pass over ComposeScreen.

Purpose: complete COMP-09/12/13 at the screen level with the tested lifecycle + availability adapter — no ack gate, no auto-write, failure-safe — and make Rewrite actually rewrite the user's message (§P-411) via a minimal, in-boundary prompt-template extension (sophisticated generation-context construction stays Phase 36 per §U/§Q/D-13).
Output: extended resolvePrompt (source-draft) + ComposeScreen with the full AI review experience across all three availability states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend resolvePrompt with a bounded, delimited Rewrite source-draft (HIGH-3 / §P-411)</name>
  <files>src/ai/prompt-template.ts, src/ai/prompt-template.test.ts</files>
  <behavior>
    - resolvePrompt(template, context, sourceDraft?) gains an OPTIONAL third param. When sourceDraft is present and non-blank (Rewrite), the prompt includes a single distinct fenced `===== DATA: MESSAGE TO REWRITE =====` … `===== END DATA: MESSAGE TO REWRITE =====` block carrying the user's own draft, sanitized (fence-neutralized via the existing sanitizeValue) and trimmed to a code-point limit, and the instruction is framed so the model REWRITES that message preserving its core intent rather than inventing an unrelated starter (§P-411).
    - When sourceDraft is absent/blank (Draft), the resolved prompt is byte-identical to today's Draft prompt (no rewrite block, no rewrite instruction line).
    - The source-draft is DATA, never instructions (same injection controls as all contact-derived values); it counts against the existing TOTAL_LIMIT budget and any truncation is disclosed by category only.
    - prompt/inspectorDisplay/payload remain the SAME frozen string instance (the existing byte-identity invariant holds with the rewrite block included).
  </behavior>
  <read_first>
    - src/ai/prompt-template.ts (FULL — VERIFIED this session: resolvePrompt(template, context) at :142; STATIC_INSTRUCTION Draft-shaped at :64-75 ("Write only the message body ... specific only to the reference data below"); the DATA-fence discipline + sanitizeValue at :95-104; trimToCodePoints / TOTAL_LIMIT / PER_VALUE_LIMIT at :36-113; the frozen prompt/inspectorDisplay/payload same-instance return at :280-289. This is the SOLE construction path.)
    - src/ai/prompt-types.ts (ResolvedPrompt at :159 — UNCHANGED; sourceDraft is a resolvePrompt PARAM, not a PromptContext field — do NOT add it to the contact-data allowlist)
    - src/ai/prompt-template.test.ts (extend the existing byte-identity + truncation suite; do not rewrite)
    - docs/dossier/milestone-2/phase-14-messaging-ai-compose-dossier.md (§P line 411 [DECIDED]: Rewrite preserves the user's existing message, not an unrelated starter; §Q line 427 + §U line 500: channel-awareness, personalization weighting, provider-specific prompting, and exact variation instructions are Phase 36 — keep the rewrite framing MINIMAL)
    - docs/decisions/ADR-079-... (the resolved prompt is the byte-identical inspect/transmit object)
  </read_first>
  <action>
    Extend `resolvePrompt` to accept an OPTIONAL third `sourceDraft?: string`. When present and non-blank, render it as a single distinct fenced `===== DATA: MESSAGE TO REWRITE =====` … `===== END DATA: MESSAGE TO REWRITE =====` block placed alongside the other DATA blocks, applying sanitizeValue (fence-neutralize) and trimToCodePoints (PER_VALUE_LIMIT), counted against TOTAL_LIMIT with a category-only truncation notice. Add the MINIMAL rewrite framing needed to honor §P-411: when a MESSAGE TO REWRITE block is present, instruct the model to rewrite THAT message preserving its core intent rather than writing an unrelated new message (a short conditional line) — do NOT build channel-aware, personalization-weighted, or provider-specific prompting, and do NOT add variation instructions (Phase 36 §U/§Q). When sourceDraft is absent/blank, produce the exact current Draft prompt unchanged. Preserve the prompt/inspectorDisplay/payload same-instance freeze. This is user-authored data — apply the same injection controls as contact-derived values; it is NOT contact-data egress and does NOT touch PromptContext. Extend prompt-template.test.ts to the behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/ai/prompt-template.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: resolvePrompt with no sourceDraft produces a prompt byte-identical to the pre-change Draft prompt (no MESSAGE TO REWRITE block, no rewrite instruction line).
    - Test: resolvePrompt WITH a sourceDraft includes exactly one fenced MESSAGE TO REWRITE block containing the (sanitized) draft plus the minimal rewrite-framing instruction.
    - Test: a source-draft containing a forged `=====` fence is neutralized by sanitizeValue and cannot break out of its DATA block.
    - Test: an over-length source-draft is trimmed to the code-point limit with a category-only truncation notice; prompt/inspectorDisplay/payload remain the same frozen instance.
    - `grep -n "sourceDraft" src/ai/prompt-types.ts` returns nothing (it is a resolvePrompt param, not a PromptContext field).
  </acceptance_criteria>
  <done>resolvePrompt carries the user's Rewrite draft as a bounded, delimited, injection-safe MESSAGE TO REWRITE block with minimal §P-411 rewrite framing; Draft output is unchanged; the byte-identity invariant holds.</done>
</task>

<task type="auto">
  <name>Task 2: Adaptive Draft/Rewrite action + lifecycle wiring</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (post plan 35-07 state; the old single-suggestion/ack wiring was already removed in the tracer; the existing lifecycle wiring built the generate dep at :215 as provider.generate(prompt,signal)→Promise<string> and resolvePrompt at :190 — this is what now becomes a three-variant fan-out)
    - src/logic/ai-suggestion-logic.ts (the reshaped lifecycle from plan 35-04 — begin() calls the injected resolvePrompt dep `(sourceDraft?)` WITH the body for Rewrite / undefined for Draft, chooseSuggestion(index), retry(), the injected deps incl. isEditorEmpty and generate→Promise<readonly string[]>, dispose/onConfigChange)
    - src/logic/ai-generate-variants.ts (plan 35-04 — generateVariants(generateOne, prompt, signal, count); wire generateOne = provider.generate)
    - src/ai/prompt-template.ts (the Task-1 extended `resolvePrompt(template, context, sourceDraft?)` — the closed/delimited construction path the Rewrite body rides within) + src/ai/prompt-types.ts (ResolvedPrompt)
    - src/db/ai-context-read.ts (readPromptContext — the closed egress projection the screen passes to generate)
    - 35-UI-SPEC.md (AI adaptive action copy 'Draft with AI'/'Rewrite with AI'; body size 16/24; E5 review rows)
  </read_first>
  <action>
    Add the single adaptive AI action: label 'Draft with AI' when the editor is empty, 'Rewrite with AI' when it holds meaningful text (drive the injected isEditorEmpty from the session body). On tap, build the closed prompt context via readPromptContext and call the lifecycle begin() (never on open). Build the lifecycle's injected `resolvePrompt` dep as `(sourceDraft) => resolvePrompt(template, context, sourceDraft)` (the Task-1 extended resolver); begin() passes the current editor body as sourceDraft for Rewrite and undefined for Draft, so the Rewrite body enters the bounded/delimited MESSAGE TO REWRITE block — never raw-concatenated onto the prompt string. Wire the lifecycle's `generate` dep to `generateVariants(provider.generate, prompt, signal, 3)` so three variants are produced by three cancellable provider.generate calls under the lifecycle's one signal (do NOT modify AiService). Provide the injected deps (isActive/dispose on blur/unmount so a resolving/loading state is never stranded). Render the action as a Button primary. Do NOT auto-start; do NOT reintroduce any acknowledgement gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Adaptive label toggles Draft/Rewrite on editor emptiness (read the label logic).
    - The generate dep is wired via generateVariants (grep the call) with provider.generate as generateOne; `git diff src/services/AiService.ts` is empty.
    - Rewrite passes the editor body as sourceDraft into the Task-1 extended resolvePrompt (read the resolvePrompt dep + the begin() call); no raw `prompt + body` concatenation anywhere in the screen.
    - `grep -n "needs-acknowledgement\|acknowledgeProvider" src/screens/ComposeScreen.tsx` returns nothing.
    - No generation is started in a focus/mount effect (read the effects — begin() is only in the action handler).
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>The adaptive action drives the reshaped lifecycle on explicit invocation only, with the Rewrite body flowing through the extended resolvePrompt source-draft path.</done>
</task>

<task type="auto">
  <name>Task 3: Non-destructive three-suggestion review surface</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (tasks 1-2)
    - src/logic/ai-suggestion-logic.ts (the three-suggestion success state + chooseSuggestion + retry + error/idle states)
    - src/components/ui/ (GlassSurface/Sheet/Modal for the review surface; Button roles; AppText body role)
    - 35-UI-SPEC.md (E5 rows: three unlabeled varied suggestions, each 'Choose this'; 'Try Again' replaces; 'Cancel'; Rewrite shows original + keep-original; long text scrolls at body 16/24; loading placeholder is a device-tuned backstop)
  </read_first>
  <action>
    Render the review surface for the success state: exactly three unlabeled suggestions at AppText role body (16/24 — no shrink), each with a 'Choose this' primary that calls chooseSuggestion(index) (the ONLY editor mutation). 'Try Again' (tertiary) calls retry() to replace the set; 'Cancel' (secondary) dismisses without mutating. For Rewrite, show the current/original text with a clear keep-the-original path. The editor stays untouched until Choose this. Long suggestions scroll within the surface; each stays individually selectable. Show a non-destructive pending placeholder while generating (editor untouched, Cancel available).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Review surface renders three suggestions; only 'Choose this' mutates the editor (read the handlers — chooseSuggestion is the sole editor write).
    - 'Try Again' calls retry(); 'Cancel' dismisses with no mutation.
    - Rewrite path shows the original + a keep-original action.
    - Suggestion text uses AppText role body (no raw fontSize); `npm run check:colors` passes.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A non-destructive three-suggestion review surface with Choose this / Try Again / Cancel and a Rewrite keep-original path is wired.</done>
</task>

<task type="auto">
  <name>Task 4: Three availability states + failure-safe error surface</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (tasks 1-3)
    - src/logic/ai-availability.ts (computeAiAvailability + the affordance-posture selector from plan 35-04)
    - src/screens/ComposeScreen.tsx (:488-506 — the existing sanitized error→short-line mapping to preserve)
    - 35-UI-SPEC.md (E6 rows; Copywriting 'AI Needs Attention' restrained notice — replaces, never hides, routes to existing AI settings interim; 'AI error / failure' concise recovery; E5 error row)
    - 35-CONTEXT.md (D-07 + D-12 — repair notice routes to the EXISTING AI settings surface as interim; keep minimal)
  </read_first>
  <action>
    Consume the ai-availability adapter to drive AI rendering (the screen computes no availability state itself): Off → no AI affordances (manual + Research fully usable); Ready → adaptive action + review available; Needs-Attention → a restrained 'AI needs attention' notice (caption/label on surface, no accent CTA styling) that REPLACES the AI actions and routes to the existing AI settings surface (interim per D-12), without restoring Draft/Rewrite/Add-to-AI. Ensure the error/failure state preserves the manual draft, offers Try Again/Cancel, and maps sanitized error codes to short lines via the existing mapping — never raw provider text, never a provider-troubleshooting screen. Confirm manual composition + Research work in all three states.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "computeAiAvailability" src/screens/ComposeScreen.tsx` shows the adapter consumed; no inline `aiProvider !== "none"` availability computation remains for the AI affordance gate.
    - Needs-Attention renders the restrained notice replacing (not hiding) AI actions and does not restore Draft/Rewrite/Add-to-AI (read the branch).
    - Error surface maps sanitized codes to short lines; `grep` shows no raw provider-text render.
    - Manual editor + Research entry remain rendered in the Off branch.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>All three availability states render correctly with manual/Research always usable and a failure-safe, non-troubleshooting error surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose → user-configured AI provider | The sole AI egress; only on explicit adaptive-action invocation. |
| Provider response/error → review + error UI | Must not leak credentials or raw provider payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | Information disclosure | review/error surface | high | mitigate | Review resolves from the closed prompt construction; error path maps sanitized codes to short lines, never raw provider text or credentials (ADR-079). |
| T-35-18 | Tampering | non-destructive review | high | mitigate | Editor mutates ONLY on explicit Choose this; no auto-write on open; Try Again replaces without history; stale-guard in the lifecycle prevents superseded completions mutating state. |
| T-35-19 | Information disclosure | Needs-Attention notice | medium | mitigate | Restrained repair route to existing AI settings; never surfaces provider config/credentials; never silently hides AI. |
| T-35-02 | Tampering | prompt context egress (+ Rewrite source-draft) | medium | mitigate | Prompt built via the closed readPromptContext projection (plan 35-05); the Rewrite source-draft (the user's own body) enters through resolvePrompt's new `sourceDraft` param (Task 1) — sanitized (fence-neutralized) and code-point-bounded, rendered as a fenced MESSAGE TO REWRITE DATA block, never raw-concatenated; PromptContext (contact-data allowlist) unchanged (sourceDraft is a param, not a field); bounded/delimited context resists injection. |
| T-35-23 | Information disclosure | three-call fan-out egress | medium | mitigate | The three provider.generate calls share the lifecycle's one signal so cancel/timeout aborts all three (no orphaned egress); no new provider/credential surface (AiService unchanged); only fires on explicit invocation. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/ai/prompt-template.test.ts` green (Task 1 source-draft rendering); `npx tsc --noEmit` clean; `npm run check:colors` passes; `npm test` green at the wave-08 merge.
- Device backstop (phase gate): on the Pixel, Draft on empty + Rewrite on filled editor return three suggestions; Rewrite suggestions clearly rework the user's own text (not an unrelated starter — §P-411); Choose this applies one; Try Again replaces; Cancel/timeout preserve the draft; toggle provider config to exercise Off/Ready/Needs-Attention.
</verification>

<success_criteria>
resolvePrompt carries a bounded/delimited Rewrite source-draft; the adaptive Draft/Rewrite action, non-destructive three-suggestion review, Rewrite keep-original path, failure-safe error surface, and all three AI-availability states are wired with manual/Research usable everywhere and no ack gate.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks; generateVariants, computeAiAvailability, the reshaped AiSuggestionLifecycle, and readPromptContext's carried shapes are created in plans 35-04/35-05):
- `src/ai/prompt-template.ts` — `resolvePrompt` gains an OPTIONAL third `sourceDraft?: string` param rendering a bounded/delimited MESSAGE TO REWRITE block with minimal §P-411 framing; Draft output unchanged; ResolvedPrompt/PromptContext unchanged.
- `src/screens/ComposeScreen.tsx` — adaptive AI action (Draft/Rewrite w/ source-draft via the extended resolvePrompt), three-variant fan-out wiring (generateVariants + provider.generate), non-destructive three-suggestion review surface, availability-state rendering (consuming plans 35-04/35-05).
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-08-SUMMARY.md` when done.
</output>
