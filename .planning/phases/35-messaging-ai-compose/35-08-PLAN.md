---
phase: 35-messaging-ai-compose
plan: 08
type: execute
wave: 4
depends_on: [35-04, 35-05, 35-07]
files_modified:
  - src/ai/prompt-template.ts
  - src/ai/prompt-template.test.ts
  - src/screens/ComposeScreen.tsx
  - src/logic/ai-availability.ts
  - src/logic/ai-availability.test.ts
autonomous: true
requirements: [COMP-09, COMP-12, COMP-13]
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "COMP-12 / D-09: one adaptive AI action reads 'Draft with AI' on an empty editor and 'Rewrite with AI' when the editor holds meaningful text; it never auto-writes on open; invoking it returns exactly three unlabeled varied suggestions on a non-destructive review surface (editor untouched until 'Choose this'); 'Try Again' replaces the set; Rewrite also shows the original with a keep-original path."
    - "COMP-12: 'Choose this' is the ONLY action that mutates the editor (applies one suggestion); suggestion text renders at body size 16/24 (no shrink for AI panels)."
    - "COMP-12 / HIGH-3 (three-variant fan-out wiring — corrected per review MEDIUM #3): the lifecycle's injected `generate` dep is wired to `generateVariants(generateOne, prompt, signal, 3)` (plan 35-04 helper) where `generateOne` is an ADAPTER over `provider.generate`, NOT `provider.generate` passed directly. VERIFIED on disk `provider.generate(input: GenerationInput): Promise<string>` (AiService.ts:92) takes a GenerationInput, not `(prompt, signal)` — so the adapter is `(prompt, signal, variantIndex) => provider.generate({ resolvedPrompt: prompt, model, temperature: variantTemperature(base, variantIndex, 3), maxOutputTokens, signal })`, carrying the lifecycle's shared AbortSignal into `GenerationInput.signal` and applying a DISTINCT per-call temperature (via the plan-35-04 variantTemperature helper) so the three suggestions are meaningfully varied by a deliberate lever rather than by chance (COMP-12; D-12 minimal). Three cancellable calls run under the lifecycle's ONE AbortController signal; AiService is not modified. Cancel/timeout aborts all three; any single failure surfaces the sanitized error with Try Again."
    - "COMP-12 / HIGH-3 (Rewrite body → bounded prompt path): this plan EXTENDS `resolvePrompt(template, context, sourceDraft?)` in prompt-template.ts so the Rewrite path has a REAL bounded/delimited path into the prompt — VERIFIED the current resolver has no source-draft param (prompt-template.ts:142) and ResolvedPrompt has no such field (prompt-types.ts:159), so without this the draft could not enter the prompt except by raw concatenation. For Rewrite, ComposeScreen passes the current editor body as `sourceDraft`, rendered as a fenced `MESSAGE TO REWRITE` DATA block (sanitized, code-point-bounded) with MINIMAL §P-411 rewrite framing (preserve the user's message rather than an unrelated starter) — never raw string-concatenated. It is the user's OWN composition, not contact-data egress; PromptContext (the contact-data allowlist) is unchanged and sourceDraft is a resolvePrompt PARAM, not a PromptContext field. Draft (empty editor) passes no sourceDraft and the Draft prompt is byte-identical to today. Sophisticated generation-context construction (channel-awareness, personalization weighting, provider-specific prompting, exact variation instructions) remains Phase 36 (§U/§Q/D-13)."
    - "COMP-13: generation is cancellable ('Cancel') and failure-safe — a timeout/error/cancel preserves the manual draft, offers 'Try Again'/'Cancel', maps a sanitized code to a short line, and never becomes a provider-troubleshooting surface."
    - "COMP-09 / D-07 / D-12: the screen consumes the ai-availability adapter (computes none itself); AI Off removes every AI affordance; AI On+Ready exposes them; AI On+Needs Attention shows the restrained 'AI needs attention' repair notice that REPLACES (never silently hides) the AI actions and routes to the existing AI settings surface (interim), without restoring Draft/Rewrite/Add-to-AI."
    - "COMP-09 / D-12 / HIGH (availability SOURCING — corrected per review MEDIUM #2): plan 35-04 built the pure `computeAiAvailability(providerId, credentialPresent)` but explicitly left credential-sourcing to a later plan — THIS plan owns it. On focus the screen ASYNC-reads the ACTIVE provider's key PRESENCE into a boolean (never the key value, never logged/exported/nav-param'd) and feeds it to the adapter. A4: it does so through a node-pure `readCredentialPresence(provider, getKey)` helper (added to ai-availability.ts) that RETURNS false for `provider === 'none'` WITHOUT calling getKey — because `aiKeyStore.getKey(provider: AiCloudProviderId)` (ai-key-store.ts:48/:86) EXCLUDES 'none' (`AiCloudProviderId = Exclude<AiProviderId,'none'>`, ai-types.ts:48) yet `aiProvider` can be 'none', so passing 'none' to getKey would both fail `tsc` and be semantically wrong; only a cloud provider id reaches getKey (`(await getKey(provider)) !== null`). An observed `unauthorized` generation result flips a session-local `credentialFailed` flag (via the node-pure `isCredentialFailure(code)` predicate added to ai-availability.ts — true for `unauthorized`, false for transient codes like timeout/rate_limited so a rate-limit is never mistaken for a bad key) so `effectiveCredentialPresent = credentialPresent && !credentialFailed` moves availability to 'needs-attention' WITHOUT exposing any key material; a later success clears it. Missing-key and observed-unauthorized transitions are unit-tested in ai-availability.test.ts."
    - "COMP-09: manual composition and the Research side work fully in every AI state."
    - "The review surface resolves from the same prompt construction path and never displays credentials or raw provider text (ADR-079)."
  artifacts:
    - "prompt-template.ts: resolvePrompt gains an OPTIONAL bounded/delimited `sourceDraft` param for the Rewrite path (HIGH-3, §P-411 minimal framing); Draft output unchanged; sourceDraft is NOT a PromptContext field"
    - "ComposeScreen: adaptive AI action + three-suggestion review surface + availability-state rendering wired to AiSuggestionLifecycle + ai-availability; RE-CREATES the provider-wiring generate adapter 35-01 removed (service.refreshProviders/getActiveProvider/model-selection/resolveMaxOutputTokens+catalog) and the sanitized error-code→line mapping 35-01 removed; SOURCES credential presence (async getKey on focus) for the availability adapter"
    - "ai-availability.ts: pure `isCredentialFailure(code)` predicate + `readCredentialPresence(provider, getKey)` helper added (the latter narrows 'none' out before any getKey call — A4) atop the plan-35-04 computeAiAvailability; missing-key + observed-unauthorized + 'none'-zero-read transitions tested"
  key_links:
    - "adaptive action → AiSuggestionLifecycle.begin() → three suggestions → review surface → chooseSuggestion applies one to the editor"
    - "ai-availability.computeAiAvailability → affordance posture (off/ready/needs-attention) → AI control rendering"
  prohibitions:
    - "Must never auto-start a generation on open."
    - "Must never mutate the editor before an explicit 'Choose this'."
    - "Must never silently hide AI in Needs-Attention — a restrained repair notice replaces the actions."
    - "Must never display credentials or raw provider text in the review/error surface."
    - "Must never reintroduce the ADR-052 exact-prompt acknowledgement gate."
---

<objective>
Extend `resolvePrompt` with a bounded/delimited Rewrite source-draft (HIGH-3), then wire the reshaped AI lifecycle (plan 35-04) and the shape-carrying egress (plan 35-05) into ComposeScreen: the single adaptive Draft/Rewrite action, the non-destructive three-suggestion review surface (Choose this / Try Again / Cancel), the Rewrite keep-original path, and the three AI-availability states consumed from the adapter. Second expansion pass over ComposeScreen.

Purpose: complete COMP-09/12/13 at the screen level with the tested lifecycle + availability adapter — no ack gate, no auto-write, failure-safe — and make Rewrite actually rewrite the user's message (§P-411) via a minimal, in-boundary prompt-template extension (sophisticated generation-context construction stays Phase 36 per §U/§Q/D-13).
Output: extended resolvePrompt (source-draft) + ComposeScreen with the full AI review experience across all three availability states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend resolvePrompt with a bounded, delimited Rewrite source-draft (HIGH-3 / §P-411)</name>
  <files>src/ai/prompt-template.ts, src/ai/prompt-template.test.ts</files>
  <behavior>
    - resolvePrompt(template, context, sourceDraft?) gains an OPTIONAL third param. When sourceDraft is present and non-blank (Rewrite), the prompt includes a single distinct fenced `===== DATA: MESSAGE TO REWRITE =====` … `===== END DATA: MESSAGE TO REWRITE =====` block carrying the user's own draft, sanitized (fence-neutralized via the existing sanitizeValue) and trimmed to a code-point limit, and the instruction is framed so the model REWRITES that message preserving its core intent rather than inventing an unrelated starter (§P-411).
    - When sourceDraft is absent/blank (Draft), the resolved prompt is byte-identical to today's Draft prompt (no rewrite block, no rewrite instruction line).
    - The source-draft is DATA, never instructions (same injection controls as all contact-derived values); it counts against the existing TOTAL_LIMIT budget and any truncation is disclosed by category only.
    - CONSTRUCTION ORDER (review MEDIUM #6): the MESSAGE TO REWRITE block AND its conditional rewrite-instruction line enter the SCAFFOLD — the `baseCount` region measured BEFORE the shared-field budgeting loop (VERIFIED prompt-template.ts:230-243) — so their cost is RESERVED against TOTAL_LIMIT up front, NOT appended after the loop where the final safety-net hard-trim (:269-278) could sever the block's closing fence or drop the rewrite instruction. Reserve room for the block's own category-only truncation notice as well. The shared-field loop then budgets against whatever the scaffold-with-rewrite-block leaves under TOTAL_LIMIT.
    - FENCE INTEGRITY: at an exact-`TOTAL_LIMIT` input (and an over-limit input) every DATA block — CONTACT CONTEXT, USER STYLE NOTE, and MESSAGE TO REWRITE — still has BOTH its opening and its closing fence; the hard-trim can never leave a dangling/severed fence, and the rewrite-instruction line survives.
    - prompt/inspectorDisplay/payload remain the SAME frozen string instance (the existing byte-identity invariant holds with the rewrite block included).
  </behavior>
  <read_first>
    - src/ai/prompt-template.ts (FULL — VERIFIED this session: resolvePrompt(template, context) at :142; STATIC_INSTRUCTION Draft-shaped at :64-75 ("Write only the message body ... specific only to the reference data below"); the DATA-fence discipline + sanitizeValue at :95-104; trimToCodePoints / TOTAL_LIMIT / PER_VALUE_LIMIT at :36-113; the frozen prompt/inspectorDisplay/payload same-instance return at :280-289. This is the SOLE construction path. A6 (grep caveat): this file embeds a NUL `FIELD_MARKER` sentinel (`"\0FIELDS\0"`, verified this session), so GNU grep/ripgrep treat it as BINARY — plain `grep -n resolvePrompt src/ai/prompt-template.ts` returns "binary file matches" with NO line info even though the symbol exists at :142. ANY grep you run against this file (reading OR verifying) MUST use `grep -a`; `tsc`/`vitest` remain the real gates.)
    - src/ai/prompt-types.ts (ResolvedPrompt at :159 — UNCHANGED; sourceDraft is a resolvePrompt PARAM, not a PromptContext field — do NOT add it to the contact-data allowlist)
    - src/ai/prompt-template.test.ts (extend the existing byte-identity + truncation suite; do not rewrite)
    - docs/dossier/milestone-2/phase-14-messaging-ai-compose-dossier.md (§P line 411 [DECIDED]: Rewrite preserves the user's existing message, not an unrelated starter; §Q line 427 + §U line 500: channel-awareness, personalization weighting, provider-specific prompting, and exact variation instructions are Phase 36 — keep the rewrite framing MINIMAL)
    - docs/decisions/ADR-079-... (the resolved prompt is the byte-identical inspect/transmit object)
  </read_first>
  <action>
    Extend `resolvePrompt` to accept an OPTIONAL third `sourceDraft?: string`. When present and non-blank, render it as a single distinct fenced `===== DATA: MESSAGE TO REWRITE =====` … `===== END DATA: MESSAGE TO REWRITE =====` block that JOINS THE SCAFFOLD — add the block AND its conditional rewrite-instruction line into the scaffold string so they land in the `baseCount` region measured BEFORE the shared-field budgeting loop (prompt-template.ts:230-243), reserving their cost against TOTAL_LIMIT up front rather than appending them after the loop where the :269-278 safety-net hard-trim could sever the closing fence or drop the instruction. Apply sanitizeValue (fence-neutralize) and trimToCodePoints (PER_VALUE_LIMIT) to the draft, reserve room for its category-only truncation notice, and let the shared-field loop budget against the scaffold-with-rewrite-block remainder. Add the MINIMAL rewrite framing needed to honor §P-411: when a MESSAGE TO REWRITE block is present, instruct the model to rewrite THAT message preserving its core intent rather than writing an unrelated new message (a short conditional line) — do NOT build channel-aware, personalization-weighted, or provider-specific prompting, and do NOT add variation instructions (Phase 36 §U/§Q). When sourceDraft is absent/blank, produce the exact current Draft prompt unchanged. Preserve the prompt/inspectorDisplay/payload same-instance freeze. This is user-authored data — apply the same injection controls as contact-derived values; it is NOT contact-data egress and does NOT touch PromptContext. Extend prompt-template.test.ts to the behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/ai/prompt-template.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: resolvePrompt with no sourceDraft produces a prompt byte-identical to the pre-change Draft prompt (no MESSAGE TO REWRITE block, no rewrite instruction line).
    - Test: resolvePrompt WITH a sourceDraft includes exactly one fenced MESSAGE TO REWRITE block containing the (sanitized) draft plus the minimal rewrite-framing instruction.
    - Test: a source-draft containing a forged `=====` fence is neutralized by sanitizeValue and cannot break out of its DATA block.
    - Test: an over-length source-draft is trimmed to the code-point limit with a category-only truncation notice; prompt/inspectorDisplay/payload remain the same frozen instance.
    - Test (construction order / fence integrity #6): with a Rewrite input sized so the assembled prompt is at EXACTLY TOTAL_LIMIT (and one sized OVER it), every DATA block — CONTACT CONTEXT, USER STYLE NOTE, and MESSAGE TO REWRITE — has BOTH its opening and closing fence in the resolved prompt (count opening vs closing fences: balanced), and the rewrite-instruction line is still present; assert the hard-trim never severs a closing fence.
    - `grep -n "sourceDraft" src/ai/prompt-types.ts` returns nothing (it is a resolvePrompt param, not a PromptContext field). NOTE (A6): prompt-types.ts is plain text (grep -n is fine here); prompt-template.ts is NOT — see next line.
    - A6 (grep -a mandatory for prompt-template.ts): the file embeds a NUL `FIELD_MARKER` sentinel so plain grep reports "binary file matches" with no line numbers — ANY acceptance/verification grep against `src/ai/prompt-template.ts` MUST use `grep -a`. Concretely, confirm the new block landed with `grep -a "MESSAGE TO REWRITE" src/ai/prompt-template.ts` (a plain `grep -n` here misleadingly returns no line info); `tsc`/`vitest` remain the real gates.
  </acceptance_criteria>
  <done>resolvePrompt carries the user's Rewrite draft as a bounded, delimited, injection-safe MESSAGE TO REWRITE block with minimal §P-411 rewrite framing; Draft output is unchanged; the byte-identity invariant holds.</done>
</task>

<task type="auto">
  <name>Task 2: Adaptive Draft/Rewrite action + lifecycle wiring</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (post plan 35-07 state — READ IT ON DISK: plan 35-01 Task 3 in wave 1 already REMOVED the ENTIRE `new AiSuggestionLifecycle({...})` construction and EVERY injected dep, including the `generate` adapter, the `resolvePrompt` dep, the `AiService`/`serviceRef` instance, the `settingsRef`/catalog `catalogRef` refs, and the on-focus AI-settings/catalog load. So at wave 4 there is NO generate adapter at any line to "reuse" — this task RE-CREATES it (and its supporting provider-wiring) against the reshaped lifecycle. The pre-35-01 shape lives in git history — inspect it via `git show <pre-35-01-commit>:src/screens/ComposeScreen.tsx` (the block that was at ~:163-240) if you want the exact prior body — but the concrete identifiers to reconstruct are inlined in this task's action so you never chase a deleted line-anchor.)
    - src/logic/ai-suggestion-logic.ts (the reshaped lifecycle from plan 35-04 — begin() calls the injected resolvePrompt dep `(sourceDraft?)` WITH the body for Rewrite / undefined for Draft, chooseSuggestion(index), retry(), the injected deps incl. isEditorEmpty and generate→Promise<readonly string[]>, dispose/onConfigChange)
    - src/logic/ai-generate-variants.ts (plan 35-04 — generateVariants(generateOne, prompt, signal, count) passes each call a 0-based variantIndex; variantTemperature(base, variantIndex, count) yields the distinct per-call temperature. generateOne is an ADAPTER, NOT provider.generate directly)
    - src/services/AiService.ts (:45-61 GenerationInput shape + :92 `generate(input: GenerationInput): Promise<string>` — VERIFIED generate takes a GenerationInput, not (prompt, signal); the adapter must BUILD the GenerationInput and carry the signal into it)
    - PROVIDER-WIRING TO RE-CREATE (removed by 35-01 Task 3; verified against the pre-35-01 source this session — these are the concrete identifiers, not a line-anchor): the generate adapter must (1) `service.refreshProviders(s)` on the current AI settings `s`, (2) `provider = service.getActiveProvider(s)` (throw `AiError('not_configured')` if null), (3) select the model as `s.aiProvider === 'custom' ? s.aiCustomModel : s.aiModel`, (4) `maxOutputTokens = resolveMaxOutputTokens(s.aiProvider, model, catalogRef.current)` (the model catalog supplies Anthropic's REQUIRED max_tokens; OpenAI/Gemini omit it), then (5) call `provider.generate({ resolvedPrompt: prompt, model, temperature, maxOutputTokens, signal })`. The ONLY change vs the pre-35-01 body is temperature: use `variantTemperature(base, variantIndex, 3)` (base = the configured/default temperature, formerly the fixed `AI_TEMPERATURE`) per fan-out call instead of one fixed value. Also re-create the SUPPORTING wiring the old block owned: the `AiService` instance (`serviceRef`), the AI-settings ref (`settingsRef`) + its on-focus load, and the model-catalog ref (`catalogRef`) + its best-effort cache load (loadCachedCatalog/resolveActiveCatalog/SEED_CATALOG) — plus the imports `resolveMaxOutputTokens` (@/ai/token-budget), `AiService` (@/services/AiService), `readPromptContext` (@/db/ai-context-read), `resolvePrompt` (@/ai/prompt-template), and the catalog helpers. Do NOT modify AiService.
    - src/ai/prompt-template.ts (the Task-1 extended `resolvePrompt(template, context, sourceDraft?)` — the closed/delimited construction path the Rewrite body rides within) + src/ai/prompt-types.ts (ResolvedPrompt)
    - src/db/ai-context-read.ts (readPromptContext — the closed egress projection the screen passes to generate)
    - 35-UI-SPEC.md (AI adaptive action copy 'Draft with AI'/'Rewrite with AI'; body size 16/24; E5 review rows)
  </read_first>
  <action>
    Add the single adaptive AI action: label 'Draft with AI' when the editor is empty, 'Rewrite with AI' when it holds meaningful text (drive the injected isEditorEmpty from the session body). On tap, build the closed prompt context via readPromptContext and call the lifecycle begin() (never on open). Build the lifecycle's injected `resolvePrompt` dep as `(sourceDraft) => resolvePrompt(template, context, sourceDraft)` (the Task-1 extended resolver); begin() passes the current editor body as sourceDraft for Rewrite and undefined for Draft, so the Rewrite body enters the bounded/delimited MESSAGE TO REWRITE block — never raw-concatenated onto the prompt string. Wire the lifecycle's `generate` dep to `generateVariants(generateOne, prompt, signal, 3)` (plan-35-04 helper) so three variants are produced by three cancellable calls under the lifecycle's one signal. `generateOne` is an ADAPTER over `provider.generate` — NOT `provider.generate` passed directly (VERIFIED provider.generate takes a `GenerationInput`, not `(prompt, signal)`): build it as `(prompt, signal, variantIndex) => provider.generate({ resolvedPrompt: prompt, model, temperature: variantTemperature(base, variantIndex, 3), maxOutputTokens, signal })`. RE-CREATE this adapter from the provider-wiring inventory in this task's read_first — 35-01 Task 3 deleted the entire `AiSuggestionLifecycle` construction (including this `generate` adapter and its `service.refreshProviders`/`getActiveProvider`/model-selection/`resolveMaxOutputTokens(...,catalogRef.current)` wiring) in wave 1, so there is NO adapter body left to "reuse"; reconstruct it, do not chase a line-anchor. The ONLY difference from the pre-35-01 body is the temperature: swap the formerly-fixed `AI_TEMPERATURE` for a DISTINCT per-call `variantTemperature(base, variantIndex, 3)` (base = the configured/default temperature) so the three suggestions are meaningfully varied by a deliberate lever, satisfying COMP-12 (D-12 minimal; the sophisticated variation-INSTRUCTION prompting stays Phase 36). Also reconstruct the supporting scaffolding 35-01 removed — the `AiService` instance ref, the AI-settings ref + its on-focus load, and the model-catalog ref + best-effort cache load — so `getActiveProvider`/model-selection/`resolveMaxOutputTokens` have the live settings + catalog they need (dropping `maxOutputTokens`/catalog/model-selection would silently regress e.g. Anthropic's required max_tokens, a functional break `tsc` cannot catch). Carry the lifecycle's shared AbortSignal into `GenerationInput.signal`. Do NOT modify AiService. Provide the injected deps (isActive/dispose on blur/unmount so a resolving/loading state is never stranded). Render the action as a Button primary. Do NOT auto-start; do NOT reintroduce any acknowledgement gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Adaptive label toggles Draft/Rewrite on editor emptiness (read the label logic).
    - The generate dep is wired via generateVariants (grep the call); generateOne is an ADAPTER that builds a GenerationInput and calls provider.generate — NOT provider.generate passed directly; `git diff src/services/AiService.ts` is empty.
    - The generateOne adapter consumes `variantTemperature(...)` for the per-call temperature (grep shows variantTemperature in the adapter; the fan-out path no longer sends a single fixed temperature to all three calls) — the deliberate COMP-12 'meaningfully varied' lever, not provider chance.
    - The re-created generate adapter reconstructs the provider-wiring 35-01 removed: `grep -n "resolveMaxOutputTokens\|getActiveProvider" src/screens/ComposeScreen.tsx` matches BOTH (proves model-selection + per-provider token budgeting were rebuilt, not dropped in favour of the GenerationInput type alone — dropping maxOutputTokens/catalog would silently break Anthropic's required max_tokens). The model-catalog ref that feeds `resolveMaxOutputTokens` and the `refreshProviders`/settings scaffolding are present (read the reconstructed wiring).
    - Rewrite passes the editor body as sourceDraft into the Task-1 extended resolvePrompt (read the resolvePrompt dep + the begin() call); no raw `prompt + body` concatenation anywhere in the screen.
    - `grep -n "needs-acknowledgement\|acknowledgeProvider" src/screens/ComposeScreen.tsx` returns nothing.
    - No generation is started in a focus/mount effect (read the effects — begin() is only in the action handler).
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>The adaptive action drives the reshaped lifecycle on explicit invocation only, with the Rewrite body flowing through the extended resolvePrompt source-draft path.</done>
</task>

<task type="auto">
  <name>Task 3: Non-destructive three-suggestion review surface</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (tasks 1-2)
    - src/logic/ai-suggestion-logic.ts (the three-suggestion success state + chooseSuggestion + retry + error/idle states)
    - src/components/ui/ (GlassSurface/Sheet/Modal for the review surface; Button roles; AppText body role)
    - 35-UI-SPEC.md (E5 rows: three unlabeled varied suggestions, each 'Choose this'; 'Try Again' replaces; 'Cancel'; Rewrite shows original + keep-original; long text scrolls at body 16/24; loading placeholder is a device-tuned backstop)
  </read_first>
  <action>
    Render the review surface for the success state: exactly three unlabeled suggestions at AppText role body (16/24 — no shrink), each with a 'Choose this' primary that calls chooseSuggestion(index) (the ONLY editor mutation). 'Try Again' (tertiary) calls retry() to replace the set; 'Cancel' (secondary) dismisses without mutating. For Rewrite, show the current/original text with a clear keep-the-original path. The editor stays untouched until Choose this. Long suggestions scroll within the surface; each stays individually selectable. Show a non-destructive pending placeholder while generating (editor untouched, Cancel available).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Review surface renders three suggestions; only 'Choose this' mutates the editor (read the handlers — chooseSuggestion is the sole editor write).
    - 'Try Again' calls retry(); 'Cancel' dismisses with no mutation.
    - Rewrite path shows the original + a keep-original action.
    - Suggestion text uses AppText role body (no raw fontSize); `npm run check:colors` passes.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A non-destructive three-suggestion review surface with Choose this / Try Again / Cancel and a Rewrite keep-original path is wired.</done>
</task>

<task type="auto">
  <name>Task 4: Availability sourcing (credential presence) + three states + failure-safe error surface</name>
  <files>src/screens/ComposeScreen.tsx, src/logic/ai-availability.ts, src/logic/ai-availability.test.ts</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (tasks 1-3)
    - src/logic/ai-availability.ts (plan-35-04 `computeAiAvailability(providerId, credentialPresent)` → 'off'|'ready'|'needs-attention' + the affordance-posture selector; this task EXTENDS the file with a pure `isCredentialFailure(code)` predicate — see action)
    - src/services/ai-key-store.ts (VERIFIED this session: `aiKeyStore.getKey(provider: AiCloudProviderId): Promise<string | null>` at :48/:86 — the ASYNC credential-presence source; a missing/cleared key returns `null` (never throws). Read PRESENCE only: `(await getKey(provider)) !== null` → boolean; NEVER store, log, or expose the key value. A4: `getKey`'s param type EXCLUDES 'none' (`AiCloudProviderId = Exclude<AiProviderId,'none'>`, ai-types.ts:48), but `aiProvider` CAN be 'none' — so the screen MUST narrow 'none' out (via readCredentialPresence, see action) BEFORE any getKey call; passing 'none' to getKey fails tsc and is meaningless.)
    - src/services/ai-types.ts (:23 AiProviderId includes 'none'; :48 AiCloudProviderId = Exclude<AiProviderId,'none'> — the type gap A4 narrows; import both as TYPES only to keep ai-availability.ts node-pure)
    - src/services/AiService.ts (:98-109 the `AiErrorCode` union — `unauthorized` is the observed credential-failure signal; `isCredentialFailure` imports this as a TYPE only, keeping ai-availability.ts node-pure)
    - src/screens/ComposeScreen.tsx (35-01 REMOVED the `aiErrorText(code)` error-code→short-line mapping helper — do NOT expect it at any line; this task RE-CREATES it, see action)
    - 35-UI-SPEC.md (E6 rows; Copywriting 'AI Needs Attention' restrained notice — replaces, never hides, routes to existing AI settings interim; 'AI error / failure' concise recovery; E5 error row)
    - 35-CONTEXT.md (D-07 + D-12 — three-state availability; repair notice routes to the EXISTING AI settings surface as interim; keep minimal)
  </read_first>
  <action>
    OWN the three-state availability SOURCING (35-04 built the pure adapter but explicitly left credential-sourcing to "a later plan" — this is it; addresses review MEDIUM #2). A4 (narrow 'none' before getKey — VERIFIED `aiKeyStore.getKey(provider: AiCloudProviderId)` at ai-key-store.ts:48/:86, and `AiCloudProviderId = Exclude<AiProviderId,'none'>` at ai-types.ts:48 EXCLUDES 'none', while `aiProvider` CAN be 'none'): do NOT pass the active provider straight into getKey — that fails `tsc` (a 'none' provider is not assignable to AiCloudProviderId) and is semantically wrong (there is no key for 'none'). Add a node-pure helper to ai-availability.ts — `export async function readCredentialPresence(provider: AiProviderId, getKey: (p: AiCloudProviderId) => Promise<string | null>): Promise<boolean>` — that RETURNS false for `provider === 'none'` WITHOUT calling getKey (AI Off has no credential concept; the key store is never touched), and otherwise (provider narrowed to a cloud id) returns `(await getKey(provider)) !== null`. On focus, source presence via `credentialPresent = await readCredentialPresence(activeProvider, (p) => aiKeyStore.getKey(p))` into a boolean state — presence ONLY, never the key value, never logged, never put in a nav param or exportable shape; guard the post-await setState with the focus effect's existing `cancelled` flag. Feed the pure `computeAiAvailability(provider, effectiveCredentialPresent)` (the screen computes no availability state itself; `computeAiAvailability('none', false)` → 'off' per D-07). Define the observed-unauthorized → 'needs attention' transition: EXTEND `src/logic/ai-availability.ts` with a node-pure `isCredentialFailure(code: AiErrorCode): boolean` (true for `unauthorized`; false for transient codes like timeout/rate_limited/network so a rate-limit never masquerades as a bad key — import `AiErrorCode` as a TYPE only so the module stays node-pure). When a generation result surfaces a code where `isCredentialFailure` is true, set a session-local `credentialFailed` flag; compute `effectiveCredentialPresent = credentialPresent && !credentialFailed` so an observed unauthorized flips availability to 'needs-attention' WITHOUT reading or exposing any key material. Clear `credentialFailed` on a subsequent successful generation. Add unit coverage in `ai-availability.test.ts` for: missing-key (`computeAiAvailability(provider, false)` → 'needs-attention'), credential present (`… true` → 'ready'), `isCredentialFailure('unauthorized') === true`, `isCredentialFailure('timeout'|'rate_limited') === false`, and (A4) `readCredentialPresence('none', spy)` → false with the injected getKey spy called ZERO times (provider 'none' never touches the key store), while `readCredentialPresence('openai', spy→'k')` → true calls the spy once and `readCredentialPresence('openai', spy→null)` → false.

    Then drive AI rendering from the resulting availability: Off → no AI affordances (manual + Research fully usable); Ready → adaptive action + review available; Needs-Attention → a restrained 'AI needs attention' notice (caption/label on surface, no accent CTA styling) that REPLACES the AI actions and routes to the existing AI settings surface (interim per D-12), without restoring Draft/Rewrite/Add-to-AI. Ensure the error/failure state preserves the manual draft, offers Try Again/Cancel, and maps sanitized error codes to short lines. RE-CREATE the sanitized error-code→short-line mapping (35-01 Task 3 REMOVED `aiErrorText` with the AI block — do NOT "preserve" it; rebuild it) covering the codes `timeout | cancelled | not_configured | unauthorized | rate_limited | blocked | invalid_endpoint | default` — never raw provider text, never a provider-troubleshooting screen. Confirm manual composition + Research work in all three states.
  </action>
  <verify>
    <automated>npx vitest run src/logic/ai-availability.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "computeAiAvailability" src/screens/ComposeScreen.tsx` shows the adapter consumed; no inline `aiProvider !== "none"` availability computation remains for the AI affordance gate.
    - `grep -n "readCredentialPresence" src/screens/ComposeScreen.tsx src/logic/ai-availability.ts` shows the helper defined in ai-availability.ts and consumed by the screen on focus (A4 — the screen narrows 'none' through it, never passing 'none' to getKey); the screen holds a boolean presence state only — `grep -n "apiKey\|secret" src/screens/ComposeScreen.tsx` shows no stored/rendered key material.
    - Test (A4 zero-read): `readCredentialPresence('none', spy)` → false with the injected getKey spy called ZERO times (provider 'none' never reads the key store); `readCredentialPresence('openai', spy→'k')` → true (spy called once); `readCredentialPresence('openai', spy→null)` → false.
    - Test: `computeAiAvailability(provider, false)` → 'needs-attention' (missing key) and `… true` → 'ready'; `computeAiAvailability('none', false)` → 'off'; `isCredentialFailure('unauthorized')` true; `isCredentialFailure('timeout')` and `isCredentialFailure('rate_limited')` false (a transient error never masquerades as a bad credential).
    - The observed-unauthorized path sets a session-local flag that flips `effectiveCredentialPresent` to false → 'needs-attention' (read the handler); no key material is read or exposed.
    - Needs-Attention renders the restrained notice replacing (not hiding) AI actions and does not restore Draft/Rewrite/Add-to-AI (read the branch).
    - Error surface maps the re-created sanitized codes (timeout/cancelled/not_configured/unauthorized/rate_limited/blocked/invalid_endpoint/default) to short lines; `grep` shows no raw provider-text render.
    - Manual editor + Research entry remain rendered in the Off branch.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Availability is SOURCED (async credential presence on focus + observed-unauthorized → needs-attention, no key material exposed) and rendered across all three states with manual/Research always usable, the sanitized error mapping re-created, and a failure-safe, non-troubleshooting error surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose → user-configured AI provider | The sole AI egress; only on explicit adaptive-action invocation. |
| Provider response/error → review + error UI | Must not leak credentials or raw provider payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | Information disclosure | review/error surface | high | mitigate | Review resolves from the closed prompt construction; error path maps sanitized codes to short lines, never raw provider text or credentials (ADR-079). |
| T-35-18 | Tampering | non-destructive review | high | mitigate | Editor mutates ONLY on explicit Choose this; no auto-write on open; Try Again replaces without history; stale-guard in the lifecycle prevents superseded completions mutating state. |
| T-35-19 | Information disclosure | Needs-Attention notice | medium | mitigate | Restrained repair route to existing AI settings; never surfaces provider config/credentials; never silently hides AI. |
| T-35-25 | Information disclosure | availability credential-presence sourcing | medium | mitigate | The on-focus availability read calls `aiKeyStore.getKey(provider)` for PRESENCE only (`!== null` → boolean); the key VALUE is never retained, logged, rendered, put in a nav param, or serialized. Observed `unauthorized` flips a boolean flag (via the pure `isCredentialFailure`), never captures the credential. `getKey` degrades a missing/cleared key to `null` (never throws), so absence is an ordinary needs-attention state. |
| T-35-02 | Tampering | prompt context egress (+ Rewrite source-draft) | medium | mitigate | Prompt built via the closed readPromptContext projection (plan 35-05); the Rewrite source-draft (the user's own body) enters through resolvePrompt's new `sourceDraft` param (Task 1) — sanitized (fence-neutralized) and code-point-bounded, rendered as a fenced MESSAGE TO REWRITE DATA block, never raw-concatenated; PromptContext (contact-data allowlist) unchanged (sourceDraft is a param, not a field); bounded/delimited context resists injection. |
| T-35-23 | Information disclosure | three-call fan-out egress | medium | mitigate | The three provider.generate calls share the lifecycle's one signal so cancel/timeout aborts all three (no orphaned egress); no new provider/credential surface (AiService unchanged); only fires on explicit invocation. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/ai/prompt-template.test.ts src/logic/ai-availability.test.ts` green (Task 1 source-draft rendering + fence-integrity at exact TOTAL_LIMIT; Task 4 missing-key + observed-unauthorized `isCredentialFailure` transitions); `npx tsc --noEmit` clean; `npm run check:colors` passes; `npm test` green at the wave-08 merge.
- Device backstop (phase gate): on the Pixel, Draft on empty + Rewrite on filled editor return three suggestions; the three suggestions are visibly varied (per-call temperature lever), not identical; Rewrite suggestions clearly rework the user's own text (not an unrelated starter — §P-411); Choose this applies one; Try Again replaces; Cancel/timeout preserve the draft; toggle provider config to exercise Off/Ready/Needs-Attention.

**Behavior coverage split (review MEDIUM #7 — recorded so the device-UAT reliance is a visible decision, not a silent gap):**
- Covered by pure-logic unit tests: the three-suggestion contract, abort-on-non-stale-failure, stale-guard, and Draft/Rewrite predicate → `src/logic/ai-suggestion-logic.test.ts` (plan 35-04); the fan-out, per-call variantIndex, and distinct `variantTemperature` → `src/logic/ai-generate-variants.test.ts` (plan 35-04); availability derivation + affordance posture, including Needs-Attention 'replace not hide' → `src/logic/ai-availability.test.ts` (plan 35-04), EXTENDED here (Task 4) with the missing-key derivation and the observed-unauthorized `isCredentialFailure` transition (unauthorized → needs-attention; transient codes → not); the bounded/delimited Rewrite source-draft rendering + fence integrity → `src/ai/prompt-template.test.ts` (Task 1 here). These prove the availability-states and the varied-fan-out MECHANISM off-device.
- Device-UAT-only (no clean automated seam — RN screen render): the three-suggestion review-surface rendering, the on-focus async `aiKeyStore.getKey` credential-presence read + its wiring into the adapter, the Needs-Attention repair-notice rendering + route, the sanitized error-code→short-line surface, and the Choose-this/Try-Again/Cancel interactions. Verified on the Pixel at the phase gate (backstop above).
</verification>

<success_criteria>
resolvePrompt carries a bounded/delimited Rewrite source-draft; the adaptive Draft/Rewrite action, non-destructive three-suggestion review, Rewrite keep-original path, failure-safe error surface, and all three AI-availability states are wired with manual/Research usable everywhere and no ack gate.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks; generateVariants, computeAiAvailability, the reshaped AiSuggestionLifecycle, and readPromptContext's carried shapes are created in plans 35-04/35-05):
- `src/ai/prompt-template.ts` — `resolvePrompt` gains an OPTIONAL third `sourceDraft?: string` param rendering a bounded/delimited MESSAGE TO REWRITE block with minimal §P-411 framing; Draft output unchanged; ResolvedPrompt/PromptContext unchanged.
- `src/screens/ComposeScreen.tsx` — adaptive AI action (Draft/Rewrite w/ source-draft via the extended resolvePrompt), three-variant fan-out wiring (generateVariants + a `generateOne` adapter over provider.generate that builds the GenerationInput and applies per-call `variantTemperature` for COMP-12 variation); RE-CREATES the provider-wiring 35-01 removed (`AiService`/`serviceRef`, `settingsRef` + on-focus load, `catalogRef` + cache load, `service.refreshProviders`/`getActiveProvider`/model-selection/`resolveMaxOutputTokens`); non-destructive three-suggestion review surface; three-state availability rendering sourced from an on-focus async credential-presence read fed to `computeAiAvailability`; RE-CREATES the sanitized error-code→short-line mapping 35-01 removed (consuming plans 35-04/35-05).
- `src/logic/ai-availability.ts` — EXTENDED here: a pure `isCredentialFailure(code)` predicate (observed-unauthorized → needs-attention lever) AND a node-pure `readCredentialPresence(provider, getKey)` helper that narrows 'none' out before any getKey call (A4 — 'none' returns false with zero key-store reads), layered on the plan-35-04 `computeAiAvailability`; `src/logic/ai-availability.test.ts` extended for the missing-key, observed-unauthorized, and 'none'-zero-read transitions.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-08-SUMMARY.md` when done.
</output>
