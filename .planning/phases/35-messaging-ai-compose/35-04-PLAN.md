---
phase: 35-messaging-ai-compose
plan: 04
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/logic/ai-suggestion-logic.ts
  - src/logic/ai-suggestion-logic.test.ts
  - src/logic/ai-suggestion-compose-integration.test.ts
  - src/logic/ai-generate-variants.ts
  - src/logic/ai-generate-variants.test.ts
  - src/logic/ai-availability.ts
  - src/logic/ai-availability.test.ts
autonomous: true
requirements: [COMP-09, COMP-12, COMP-13]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "COMP-12 / D-09 (ADR-079): the AI lifecycle returns exactly three unlabeled suggestions on a non-destructive review surface; the editor is untouched until an explicit Choose-this; Try Again replaces the whole set via a fresh begin(); there is no generation-history stack."
    - "COMP-12 / HIGH-3 (three-suggestion MECHANISM — the decided implementation path): the three suggestions are produced by THREE independently-cancellable provider calls sharing ONE request token + ONE AbortController signal, via a node-pure fan-out helper `generateVariants(generateOne, prompt, signal, count=3)`. The lifecycle's injected `generate` dep signature changes from `(prompt, signal) => Promise<string>` to returning `Promise<readonly string[]>` (three). AiService's `AiProvider.generate(input): Promise<string>` single-draft contract and parseSuggestionOutput are UNCHANGED (verified on disk: AiService.ts:92 returns one string; provider/model/prompt-template ownership is Phase 36 — D-13). Rationale for three-calls over one-call-parse-three: the latter would change parseSuggestionOutput + all four provider adapters + the provider prompt, which is Phase 36's lane; three calls keep the change inside the lifecycle+screen. Cost note (surfaced, not blocking): three calls ≈ 3× tokens/latency per generation; Phase 36 may consolidate to a single-call multi-variant provider strategy when it owns the provider layer."
    - "COMP-12 / cycle-3 (per-call variation + generateOne adapter shape — addresses review MEDIUM 'meaningfully varied' + the provider.generate wiring bug): VERIFIED on disk `provider.generate(input: GenerationInput): Promise<string>` takes a GenerationInput (AiService.ts:92; GenerationInput at :45-61 carries `signal` and `temperature`), NOT `(prompt, signal)`. So `generateOne` is an ADAPTER the screen supplies in plan 35-08 — `(prompt, signal, variantIndex) => provider.generate({ resolvedPrompt: prompt, model, temperature, maxOutputTokens, signal })` (the AbortSignal is carried into `GenerationInput.signal`) — NEVER `provider.generate` passed directly (that would not type-check). To make COMP-12's three 'meaningfully varied' suggestions a DELIBERATE mechanism rather than incidental provider nondeterminism (today all three calls would carry the same `AI_TEMPERATURE` — ComposeScreen.tsx:236), `generateVariants` fires each call with its 0-based `variantIndex` and the adapter derives a DISTINCT, in-range per-call `temperature` via the node-pure `variantTemperature(base, variantIndex, count)` helper (Task 2). This is the MINIMAL in-phase lever per D-12 (keep it functional now; the sophisticated variation-INSTRUCTION prompting stays Phase 36 §U/§Q)."
    - "cycle-3 (build-breaker ordering — addresses review MEDIUM/build-breaker #1): ComposeScreen is the SOLE consumer of AiSuggestionLifecycle/AiSuggestionState (VERIFIED: ComposeScreen.tsx imports them at :74-77 and uses isProviderAcknowledged :196, acknowledgeProvider :203, `needs-acknowledgement` :568, `confirm-replace` :650, confirmReplace() :697 — every member this plan removes/narrows). This plan therefore depends_on [35-01] and runs in WAVE 2: plan 35-01 (wave 1) strips the entire old ack/confirm-replace lifecycle wiring from ComposeScreen FIRST, so this reshape's own `tsc --noEmit` gate compiles cleanly instead of failing on the pre-35-01 screen. AI is re-wired against the reshaped lifecycle in plan 35-08 (wave 4)."
    - "COMP-12 / HIGH-2 (fan-out cancellation — the DECIDED mechanism): generateVariants fires the three calls under the ONE shared signal; all three must resolve for a success (Promise.all-style). generateVariants receives a READ-ONLY AbortSignal and therefore CANNOT itself abort — on the first rejection it fast-fails (Promise.all rejects) and propagates the sanitized error (Try Again). The LIFECYCLE (the SOLE AbortController owner, per its H4 invariant) then aborts its controller in the egress-failure path BEFORE nulling it, so the in-flight sibling provider.generate calls observe signal.aborted and are cancelled (no orphaned in-flight egress). VERIFIED gap on disk: the current egress catch (ai-suggestion-logic.ts:304-310) nulls this.controller WITHOUT calling abort() — this plan fixes that. Cancel/timeout/invalidate already abort all three; the monotonic stale-guard still drops a superseded batch."
    - "COMP-12 (empty→Draft/Rewrite boundary): the adaptive action is Draft with AI when the editor is empty and Rewrite with AI when it holds meaningful text; this boundary is a pure predicate on the editor content, not a mode toggle."
    - "COMP-12 / HIGH-3 (Rewrite source-draft carry contract): the lifecycle's injected `resolvePrompt` dep signature changes from `() => Promise<ResolvedPrompt>` to `(sourceDraft?: string) => Promise<ResolvedPrompt>`; begin() decides Draft vs Rewrite via the injected isEditorEmpty predicate and calls the dep WITH the current editor body for Rewrite and WITHOUT it (undefined) for Draft. The lifecycle only PASSES the source-draft to the injected dep (node-pure, testable here); the bounded/delimited RENDERING of that source-draft into the prompt (a `resolvePrompt(template, context, sourceDraft)` extension in prompt-template.ts) is delivered by plan 35-08 (wave 4) — see 35-08 for the §P-411 rewrite-framing that keeps Rewrite from producing an unrelated starter. The source-draft is the user's OWN composition — not contact-data egress; PromptContext stays the closed contact-data allowlist unchanged."
    - "COMP-12 (concurrency/stale-guard): the monotonic gen token + AbortController + 20s timeout are PRESERVED — a slow or superseded completion can never mutate the draft or the suggestion set (if token !== this.gen return)."
    - "COMP-13: generation is cancellable and failure-safe — Cancel and timeout end in an error/idle state with the manual draft preserved; sanitizeError yields a code only, never raw provider text; Compose never becomes a provider-troubleshooting surface."
    - "D-09 / ADR-079 (Trip-Wire 4): the ADR-052 needs-acknowledgement exact-prompt gate and the entire ack path (isProviderAcknowledged / acknowledgeProvider / acknowledge() / decline()) are removed CLEANLY — generation is never gated on an ai_ack_* value that nothing sets; the ai_ack_* columns are left in place (forward-only), only the writer/reader are removed."
    - "COMP-09 / D-07 / D-12: a stable three-state AI-availability adapter returns Off | Ready | NeedsAttention with a provisional implementation deriving it from today's state — Off = aiProvider==='none'; Ready = provider set + credential present; NeedsAttention = provider set but credential missing/invalid; Phase 36 later swaps the internals without touching Compose."
    - "COMP-09: manual composition and Research work fully in every AI state; Off removes AI affordances; NeedsAttention surfaces a restrained repair notice (does not silently hide AI, does not restore Draft/Rewrite/Add-to-AI)."
    - "The adapter is a stable interface (swap-internals-later, per app-settings-dao read-shape/validate-on-write discipline); Compose consumes it and computes no availability state itself."
    - "ai-suggestion-logic.ts reshaped: three-suggestion success shape, ack path removed, generate dep returns 3 variants, resolvePrompt dep gains an optional sourceDraft param (Rewrite carry), and the egress-failure path aborts the controller before nulling it (HIGH-2)"
    - "cycle-4 (build-breaker #3 — orphaned test consumer of the reshaped contract): src/logic/ai-suggestion-compose-integration.test.ts (VERIFIED on disk) imports AiSuggestionLifecycle/AiSuggestionDeps/AiSuggestionState and asserts the EXACT contract this plan retires (acknowledgeProvider dep, isProviderAcknowledged, `needs-acknowledgement` status, generate → Promise<string>). tsconfig includes **/*.ts, so this plan's project-wide `tsc --noEmit` gate (Task 1) FAILS on it unless it is reshaped/deleted IN Task 1. It is the same orphaned-consumer class as the ledger test (35-09) and ai-suggestion-navigation.test.ts (35-09) — this plan owns THIS one because its own gate breaks on it."
    - "cycle-4 (settings-ai-logic ack-view disposition — recorded, not silent): src/screens/settings-ai-logic.ts exports buildProviderAckViewState (per-provider ack view-state) with its own test. It does NOT import AiSuggestionState and does not reference any removed union member (VERIFIED: still compiles after this reshape — NOT a build-breaker). It is DELIBERATELY LEFT untouched for Phase 36 (per-provider ack in the AI settings screen is Phase-36 provider-config territory, like the forward-only ai_ack_* columns and the DAO acknowledgeProvider writer). Do not delete it in this phase."
    - "ai-generate-variants.ts: node-pure generateVariants(generateOne, prompt, signal, count) fan-out helper (passes each call its 0-based variantIndex) + node-pure variantTemperature(base, variantIndex, count) per-call variation helper"
    - "ai-availability.ts: computeAiAvailability() → 'off'|'ready'|'needs-attention' + AiAvailability type"
  key_links:
    - "AiSuggestionLifecycle.begin() → generate dep (Promise<readonly string[]>) → generateVariants fan-out (3× provider.generate under one signal, wired in plan 35-08) → three variants → review surface → Choose this applies one"
    - "ai-availability adapter ← aiProvider + credential presence (ai-key-store); consumed by ComposeScreen (later plan)"
  prohibitions:
    - "Must never leave generation gated on an ai_ack_* value that nothing sets (half-wired ack gate — ADR-079 flags this exact trap)."
    - "Must never change AiService's AiProvider.generate single-draft contract or parseSuggestionOutput — the three-variant fan-out lives at the lifecycle boundary (provider layer is Phase 36)."
    - "Must never re-add a call to the now-orphaned DAO writer acknowledgeProvider in app-settings-dao.ts — it is DELIBERATELY LEFT (D-09/ADR-079: ai_ack_* columns are forward-only; only the logic-module ack path is removed here)."
    - "Must never surface raw provider error text or credentials in the error/review surface — code-only via sanitizeError."
    - "Must never remove the AbortController / timeout / stale-guard machinery."
    - "Must never auto-start a generation on open — AI fires only on the explicit adaptive action."
---

<assumption_delta_decision>
Detector fired: single AI suggestion → three suggestions is a singular→plural (generalization) transition. Today the lifecycle success shape carries ONE `suggestion` string (ai-suggestion-logic.ts:97 `confirm-replace`) and the provider returns one string (AiService.ts:92). Decision = **PROMOTE**: the three-suggestion set becomes the primary success representation (a `readonly string[]` of exactly three), replacing the single-suggestion shape — not added alongside it. The `confirm-replace` single-suggestion state is retired. Rationale: ADR-079 makes three-suggestion the product contract; keeping a single-suggestion branch alongside would leave a dead shape and a half-migrated union. The provider-layer `generate(): Promise<string>` stays single-draft (fan-out at the lifecycle boundary); that is an implementation seam, not a competing representation. Advisory, non-blocking.
</assumption_delta_decision>

<objective>
Reshape the node-pure AI lifecycle, add the three-variant fan-out helper, and add the three-state availability adapter — the client-logic modules the AI review surface will consume. Reshape `AiSuggestionLifecycle` from single-suggestion-with-ack-gate to the ADR-079 three-suggestion contract while PRESERVING its AbortController + 20s timeout + monotonic stale-guard (these are security/privacy controls). Define the three-suggestion MECHANISM: three independently-cancellable provider calls sharing one request token + signal, via a node-pure `generateVariants` helper — AiService's single-draft provider contract stays unchanged. Make that cancellation REAL (cycle-2 HIGH-2): the lifecycle aborts its sole AbortController on a non-stale generation failure so a partial fan-out rejection cancels the in-flight sibling calls (the current egress catch nulls the controller without aborting). Carry the Rewrite source-draft through the injected resolvePrompt dep so plan 35-08 can render it (cycle-2 HIGH-3). Remove the ADR-052 ack path cleanly (Trip-Wire 4) so nothing gates generation on an unset `ai_ack_*`. Add `ai-availability.ts`: a stable Off/Ready/NeedsAttention adapter with a provisional implementation (D-12) that Phase 36 later re-implements without touching Compose.

Purpose: get the AI decision/lifecycle logic correct and fully tested off-device before it is wired into the screen.
Output: reshaped lifecycle + fan-out helper + new availability adapter, all under extended/new Vitest coverage.

**Wave-ordering note (review build-breaker #1):** this plan narrows the exported `AiSuggestionState` union and `AiSuggestionDeps` that ComposeScreen (its SOLE consumer) still references via the old ack/confirm-replace wiring. It therefore now runs in WAVE 2 with `depends_on: [35-01]` so plan 35-01 strips that wiring from ComposeScreen FIRST — otherwise this plan's own `tsc --noEmit` gate fails on the pre-35-01 screen. No file overlap with the other wave-2 plans (35-03, 35-06); the AI re-wiring against the reshaped lifecycle lands in plan 35-08 (wave 4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@docs/decisions/ADR-079-on-demand-ai-transparency-and-compose-only-three-suggestion-invocation.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape AiSuggestionLifecycle — three suggestions, drop the ack gate</name>
  <files>src/logic/ai-suggestion-logic.ts, src/logic/ai-suggestion-logic.test.ts, src/logic/ai-suggestion-compose-integration.test.ts</files>
  <behavior>
    - begin() on an empty editor is a Draft request; on a non-empty editor a Rewrite request (predicate via the injected isEditorEmpty dep). For Rewrite, begin() calls the injected resolvePrompt dep WITH the current editor body as `sourceDraft`; for Draft it calls it with no sourceDraft (undefined).
    - The injected `generate` dep resolves `Promise<readonly string[]>` (exactly three variants), not a single string.
    - A successful generation resolves to a state carrying exactly three suggestion strings (unlabeled) — not one; the retired single-suggestion `confirm-replace` shape is gone.
    - The editor/draft is NOT mutated by generation; only an explicit chooseSuggestion(index) applies one.
    - retry() (Try Again) starts a fresh begin() that replaces the three; no history is retained.
    - A superseded completion (token !== this.gen) is dropped and mutates nothing.
    - On a non-stale generation FAILURE (the injected generate/fan-out rejects while token === this.gen), the lifecycle aborts its sole AbortController BEFORE nulling it, so any in-flight sibling provider calls are cancelled; then it surfaces the sanitized (code-only) error. (HIGH-2 — the current catch nulls without aborting.)
    - Cancel/timeout resolve to an error/idle state; the manual draft is preserved; sanitizeError returns a code only.
    - No needs-acknowledgement state exists; begin() goes straight to egress (no ack await, no ack gate).
  </behavior>
  <read_first>
    - src/logic/ai-suggestion-logic.ts (FULL — PRESERVE: AI_REQUEST_TIMEOUT_MS=20_000 :61, the AbortController+timeout created/cleared together in egress :292-297, the monotonic gen token + stale-guard :192/:207/:216/:305/:312/:348, node-pure injected AiSuggestionDeps :110-144, dispose()/onConfigChange() :363-382, sanitizeError code-only :98-99/:308. FIX (HIGH-2): the egress failure catch at :304-310 currently does `this.stopTimer(); this.controller = null;` WITHOUT `controller.abort()` — on a non-stale failure it MUST abort the controller before nulling so a fan-out rejection cancels the in-flight siblings (cancel()/onTimeout() already abort — mirror that). REMOVE: needs-acknowledgement state :87-96, isProviderAcknowledged/acknowledgeProvider deps :114-120, acknowledge()/decline()/the ack path :223-281, the single confirm-replace success shape :97. CHANGE: the injected `resolvePrompt` dep from `() => Promise<ResolvedPrompt>` :113 to `(sourceDraft?: string) => Promise<ResolvedPrompt>`)
    - src/logic/ai-suggestion-logic.test.ts (FULL — reshape the suite: three-suggestion success, no-ack, Try-Again-replaces-set, stale-guard, cancel/timeout)
    - src/logic/ai-suggestion-compose-integration.test.ts (FULL — build-breaker #3: this test asserts the RETIRED contract — acknowledgeProvider dep :135-137, `acknowledgeProvider?` prop :94, `needs-acknowledgement` status :176-177, generate → Promise<string> :121/:140. It must be reshaped or deleted in THIS task or the project-wide tsc gate below fails. Keep any still-valid byte-identity / egress-ordering / stale-guard assertions, rewritten to the three-suggestion contract; drop the ADR-052 ack-gate assertions entirely.)
    - src/screens/ComposeScreen.tsx (:196-212, :568-648 — the current ack wiring, so the reshaped dep signature matches what the later integration plan will provide; do NOT edit the screen here)
  </read_first>
  <action>
    Rewrite the `AiSuggestionState` union so the success shape carries exactly three suggestion strings (`readonly string[]`) and there is NO `needs-acknowledgement` variant and NO single-suggestion `confirm-replace` variant. Change the injected `generate` dep signature from `(prompt, signal) => Promise<string>` to `(prompt, signal) => Promise<readonly string[]>` (keep it injected, node-pure, signal-driven — the REAL three-call fan-out is wired in plan 35-08 via the Task 2 helper; here the lifecycle just awaits the injected array). Change the injected `resolvePrompt` dep signature from `() => Promise<ResolvedPrompt>` to `(sourceDraft?: string) => Promise<ResolvedPrompt>`; in begin(), use the injected isEditorEmpty predicate to decide Draft vs Rewrite and call `resolvePrompt(sourceDraft)` WITH the current editor body for Rewrite and WITH no argument (undefined) for Draft — the lifecycle only passes it through; the delimited RENDERING of the sourceDraft into the prompt is delivered by plan 35-08 (prompt-template.ts extension). FIX HIGH-2: in the egress failure catch, on a non-stale rejection (`token === this.gen`) call `this.controller?.abort()` BEFORE `this.controller = null` (mirroring cancel()/onTimeout()) so a fan-out rejection aborts the shared signal and cancels the in-flight sibling provider calls — no orphaned in-flight egress. Delete the ack-path deps (`isProviderAcknowledged`, `acknowledgeProvider`) and methods (`acknowledge`, `decline`) so `begin()` proceeds straight to `egress` — reshape CLEANLY with no residual `ai_ack_*` reference (Pitfall 3 / Trip-Wire 4). The DAO-level `acknowledgeProvider` writer in app-settings-dao.ts stays in place (D-09/ADR-079 — do NOT re-add a call to it, do NOT delete it). Add an injected `isEditorEmpty` (or reuse the existing empty-editor dep) to drive the Draft vs Rewrite predicate. Add `chooseSuggestion(index)` that applies exactly one suggestion (the ONLY mutation of the editor). Keep `retry()` as the Try-Again fresh-begin. PRESERVE the AbortController/timeout/stale-guard/dispose machinery verbatim in intent (extend the egress catch's abort only). Reshape the test suite to the behavior list above; remove ack tests. ALSO (build-breaker #3) reshape or delete `src/logic/ai-suggestion-compose-integration.test.ts` in THIS task — it asserts the retired ack contract (`acknowledgeProvider` dep/prop, `needs-acknowledgement` status, `generate → Promise<string>`) and would fail this task's project-wide `tsc --noEmit`. Retire the ADR-052 ack-gate assertions; rewrite its still-valid egress-ordering / stale-guard / byte-identity assertions to the three-suggestion contract (`generate → Promise<readonly string[]>`, no `needs-acknowledgement`), or delete the file if fully superseded by the reshaped `ai-suggestion-logic.test.ts`. Do NOT touch `src/screens/settings-ai-logic.ts` / `buildProviderAckViewState` — it still compiles and is deliberately left for Phase 36 (recorded in must_haves).
  </action>
  <verify>
    <automated>npx vitest run src/logic/ai-suggestion-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "needs-acknowledgement\|isProviderAcknowledged\|acknowledgeProvider\|ai_ack\|confirm-replace" src/logic/ai-suggestion-logic.ts` returns nothing.
    - `grep -n "needs-acknowledgement\|acknowledgeProvider\|isProviderAcknowledged" src/logic/ai-suggestion-compose-integration.test.ts` returns nothing (retired-contract assertions removed); `npx tsc --noEmit` passes project-wide (this file no longer references the removed union members / dep — build-breaker #3 closed).
    - The injected `generate` dep type is `Promise<readonly string[]>` (grep the dep interface); no `Promise<string>` single-draft generate remains in the lifecycle.
    - `grep -n "AI_REQUEST_TIMEOUT_MS\|AbortController\|this.gen" src/logic/ai-suggestion-logic.ts` still present (machinery preserved).
    - Test: a successful generation (fake generate returning three strings) exposes exactly three suggestions and does not mutate the draft until chooseSuggestion.
    - Test: a Rewrite begin() calls the injected resolvePrompt dep WITH the current editor body as sourceDraft; a Draft begin() calls it with no sourceDraft (undefined) — assert via a spy on the injected resolvePrompt.
    - Test (HIGH-2, unit): when the injected generate rejects on a non-stale request, the lifecycle aborts its AbortController — a recording generate that captures its `signal` observes `signal.aborted === true` after the rejection is handled; the manual draft is preserved and a code-only error is surfaced.
    - Test (HIGH-2, end-to-end with the Task 2 helper): with `generate` wired to `generateVariants(recordingGenerateOne, prompt, signal, 3)` where the 2nd generateOne rejects, the signals captured by the 1st and 3rd generateOne calls become aborted (siblings actually cancelled, no orphaned in-flight egress).
    - Test: retry() replaces the three and retains no history; a stale (superseded) completion mutates nothing.
    - Test: cancel + timeout preserve the manual draft and yield a code-only error.
  </acceptance_criteria>
  <done>The lifecycle is the ADR-079 three-suggestion contract (generate → three variants; resolvePrompt dep carries the Rewrite sourceDraft) with the ack gate cleanly removed, a non-stale generation failure aborts the controller (siblings cancelled — HIGH-2), and all cancellation/stale machinery preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateVariants fan-out helper (three cancellable calls, one signal)</name>
  <files>src/logic/ai-generate-variants.ts, src/logic/ai-generate-variants.test.ts</files>
  <behavior>
    - generateVariants(generateOne, prompt, signal, count=3) fires `count` calls to the injected `generateOne(prompt, signal, variantIndex)` under the ONE shared signal, passing each call its 0-based `variantIndex` (0,1,2 for count=3), and resolves a `readonly string[]` of exactly `count` drafts in deterministic order when all succeed.
    - variantTemperature(base, variantIndex, count) is a node-pure helper returning a DISTINCT temperature per index, all within the provider-safe [0,1] range: the `count` values are pairwise distinct and deterministic given the same inputs, even when `base` sits at an extreme (0 or 1) where a naive add would collapse them. This is the minimal COMP-12 'meaningfully varied' lever (D-12); it lives here (node-pure/testable) and the screen adapter (plan 35-08) applies it inside generateOne. It carries NO contact data and adds no variation-INSTRUCTION prompting (Phase 36 §U/§Q).
    - The helper receives a READ-ONLY AbortSignal and does NOT own a controller: on the first rejection it fast-fails (Promise.all-style) and propagates that (sanitized) error. It does NOT call abort() (it cannot — the signal is read-only). Actual sibling cancellation is performed by the CALLER: the lifecycle aborts its sole AbortController on the propagated rejection (Task 1 HIGH-2 fix), which the still-in-flight sibling generateOne calls observe via the shared signal.
    - An already-aborted signal short-circuits (no calls fired / immediate cancelled error).
    - The helper is node-pure: injected generateOne, no RN/expo, no direct provider/AiService import. It never itself constructs a GenerationInput — building the GenerationInput (and carrying the signal into it) is the screen adapter's job in plan 35-08.
  </behavior>
  <read_first>
    - src/logic/ai-suggestion-logic.ts (the generate dep contract from Task 1 — the helper produces the `readonly string[]` that dep resolves)
    - src/services/AiService.ts (:45-61 GenerationInput — `resolvedPrompt`, `model`, `temperature`, optional `maxOutputTokens`, `signal`; :85-92 the real single-draft `AiProvider.generate(input: GenerationInput): Promise<string>`. VERIFIED: generate takes a GenerationInput, NOT `(prompt, signal)` — so plan 35-08's `generateOne` is an ADAPTER over this, not this function passed directly. Do NOT modify AiService here.)
    - src/screens/ComposeScreen.tsx (:215-240 — the CURRENT generate-dep adapter that already builds the GenerationInput and passes `temperature: AI_TEMPERATURE` on every call; this is the shape plan 35-08 evolves into a per-variant-temperature adapter using variantTemperature)
    - docs/decisions/ADR-079-... (three suggestions; cancellable/failure-safe)
  </read_first>
  <action>
    Create `ai-generate-variants.ts` exporting a node-pure `generateVariants(generateOne, prompt, signal, count=3): Promise<readonly string[]>` that fans out `count` concurrent `generateOne(prompt, signal, variantIndex)` calls sharing the one read-only AbortSignal — passing each call its 0-based `variantIndex` — awaits all (Promise.all-style) and returns the drafts in deterministic order; on the first rejection it propagates that error promptly (Promise.all fast-fail). It does NOT call abort() — the signal is read-only and the lifecycle that OWNS the controller performs the abort on the propagated rejection (Task 1 HIGH-2). Handle an already-aborted signal by short-circuiting to a cancelled error with zero calls. Keep it injected and node-pure (it does NOT build a GenerationInput — that is the plan-35-08 adapter's job). ALSO export a node-pure `variantTemperature(base, variantIndex, count)` returning a distinct, in-range ([0,1]) temperature per index that stays pairwise-distinct and deterministic even when `base` is 0 or 1 (choose the ladder — Claude's discretion — e.g. an evenly-spread set anchored on `base`, clamped and de-collapsed at the extremes); this is the minimal COMP-12 variation lever (D-12) the screen adapter applies inside generateOne. Write ai-generate-variants.test.ts covering: three successes → three drafts in deterministic order; each generateOne receives its expected 0-based variantIndex (spy on the calls); variantTemperature yields three pairwise-distinct in-range values for base=0, base=1, and a mid base; one rejection → generateVariants rejects with that error (the siblings' ACTUAL abort is asserted at the lifecycle level in Task 1, since abort is the caller's responsibility); pre-aborted signal → zero generateOne calls.
  </action>
  <verify>
    <automated>npx vitest run src/logic/ai-generate-variants.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test: injected generateOne resolving 'a'/'b'/'c' → generateVariants returns exactly three drafts in order.
    - Test: each of the three generateOne calls receives its expected 0-based variantIndex (0,1,2) — spy on the injected generateOne.
    - Test: variantTemperature returns three pairwise-DISTINCT in-range ([0,1]) values for base=0, for base=1, and for a mid base (proves the per-call variation lever never collapses to identical temperatures).
    - Test: one injected rejection → generateVariants rejects with that error (read-only signal propagated to all calls; the actual sibling-abort is asserted at the lifecycle level in Task 1).
    - Test: a pre-aborted signal fires zero generateOne calls.
    - `grep -n "expo\|react-native\|AiService\|provider\|GenerationInput" src/logic/ai-generate-variants.ts` returns nothing (node-pure, injected; the GenerationInput is built by the plan-35-08 screen adapter, not here).
  </acceptance_criteria>
  <done>A node-pure three-call fan-out helper passes each call its 0-based variantIndex, produces exactly three variants under one shared cancellation signal, fast-fails on the first rejection (the lifecycle performs the abort), and ships variantTemperature so the screen adapter can vary each call's temperature; tested for success/index/variation/failure/pre-abort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ai-availability three-state adapter (D-12 provisional impl)</name>
  <files>src/logic/ai-availability.ts, src/logic/ai-availability.test.ts</files>
  <behavior>
    - aiProvider === 'none' → 'off'.
    - provider set + credential present → 'ready'.
    - provider set + credential missing/invalid → 'needs-attention'.
    - The function is pure over its inputs (provider id + a credential-present boolean/probe), so Phase 36 can swap the credential source without changing Compose.
  </behavior>
  <read_first>
    - src/screens/ComposeScreen.tsx (:159, :198, :318 — the current binary `aiProvider !== 'none'` check that this adapter replaces; :90 ai-key-store import for the credential-presence source)
    - src/db/app-settings-dao.ts (the aiProvider field type on settings)
    - 35-CONTEXT.md (D-12 verbatim — the provisional derivation + the interim NeedsAttention repair route to the EXISTING AI settings surface; keep minimal, Phase 36 replaces internals)
    - 35-PATTERNS.md ("No Analog Found" note — stable-interface / swap-internals-later discipline mirrors app-settings-dao)
  </read_first>
  <action>
    Create `ai-availability.ts` exporting an `AiAvailability = 'off' | 'ready' | 'needs-attention'` type and a pure `computeAiAvailability(input)` deriving the state per the behavior list from the provider id and a credential-present signal (the caller supplies credential presence from the existing ai-key-store; the adapter does not itself read secrets). Keep the surface small and stable so Phase 36 replaces only the derivation. Add a brief file-header comment noting the provisional D-12 implementation and that the NeedsAttention repair route targets the existing AI settings surface as an interim (no new provider-troubleshooting UI here). Write ai-availability.test.ts covering all three states.
  </action>
  <verify>
    <automated>npx vitest run src/logic/ai-availability.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Three tests: off / ready / needs-attention per the behavior list.
    - The module reads no secret directly (`grep -n "ai-key-store\|getKey\|apiKey" src/logic/ai-availability.ts` shows no direct secret read — credential presence is an input).
    - Pure: `grep -n "expo\|react-native" src/logic/ai-availability.ts` returns nothing.
  </acceptance_criteria>
  <done>A stable, pure, three-state availability adapter with a provisional implementation and full test coverage exists.</done>
</task>

<task type="auto">
  <name>Task 4: Availability → affordance mapping (every-state usability)</name>
  <files>src/logic/ai-availability.ts, src/logic/ai-availability.test.ts</files>
  <read_first>
    - src/logic/ai-availability.ts (Task 2 output)
    - 35-UI-SPEC.md (E6 AI availability control rows; Copywriting "AI Needs Attention" restrained repair notice — replaces, never hides; does not restore Draft/Rewrite/Add-to-AI)
  </read_first>
  <action>
    Add a pure selector mapping AiAvailability → the affordance posture the screen renders: 'off' → no AI affordances (manual + Research still fully available); 'ready' → AI actions exposed; 'needs-attention' → a restrained repair notice REPLACES the AI actions (does not restore Draft/Rewrite/Add-to-AI). Return a small descriptor the screen consumes; keep manual-composition/Research availability true in all three. Test the mapping for each state.
  </action>
  <verify>
    <automated>npx vitest run src/logic/ai-availability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts manual + Research usable in all three states.
    - Test asserts 'needs-attention' returns "repair notice replaces AI actions" (not "hide AI", not "restore AI actions").
    - Test asserts 'off' exposes no AI affordance.
  </acceptance_criteria>
  <done>A tested selector maps each availability state to its screen affordance posture with manual/Research always usable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose → user-configured AI provider | The single AI egress path; only fires on explicit invocation. |
| Provider response / error → review + error UI | Provider text and error details must not leak credentials or raw provider payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | Information disclosure | error/review surface | high | mitigate | sanitizeError returns a code only; review/error surfaces render sanitized codes, never raw provider text or credentials (ADR-079). |
| T-35-12 | Tampering | reshaped lifecycle (half-wired ack) | high | mitigate | Ack path removed cleanly; grep-gate proves no residual ai_ack_* reference gates generation (ADR-079 flags this exact trap). |
| T-35-02 | Tampering | generate egress (three-call fan-out) | medium | mitigate | AbortController + 20s timeout + monotonic stale-guard preserved so a superseded/slow completion cannot mutate state; the three fan-out calls share the one signal so cancel/timeout aborts all three, AND a non-stale generate rejection makes the lifecycle abort its controller before nulling it so the in-flight siblings are cancelled on a partial fan-out failure (HIGH-2 fix — no orphaned in-flight egress); no auto-start on open; AiService single-draft contract unchanged (no new egress surface). |
| T-35-22 | Information disclosure | Rewrite source-draft carry | low | mitigate | The carried source-draft is the user's OWN composition (the text they are about to send), delimited within the closed prompt-construction path, never raw-concatenated; PromptContext (the contact-data allowlist) is not widened by it. |
| T-35-13 | Information disclosure | ai-availability adapter | low | mitigate | Adapter takes credential-presence as an input and reads no secret directly; NeedsAttention exposes a repair route, never credential values. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/logic/ai-suggestion-logic.test.ts src/logic/ai-generate-variants.test.ts src/logic/ai-availability.test.ts` green.
- `npx tsc --noEmit` clean.
- `git diff src/services/AiService.ts` empty (provider single-draft contract untouched).
</verification>

<success_criteria>
The AI lifecycle is the three-suggestion ADR-079 contract (generate → three variants via the fan-out helper, Rewrite source-draft carried, ack gate cleanly removed, machinery + AiService contract preserved); the three-state availability adapter derives Off/Ready/NeedsAttention provisionally and maps each to an every-state-usable affordance posture.
</success_criteria>

<artifacts_produced>
Symbols this plan CREATES or changes the contract of (source-grounding: exclude these from drift checks):
- `src/logic/ai-suggestion-logic.ts` — reshaped `AiSuggestionState` (three-suggestion success), `chooseSuggestion(index)`, Draft/Rewrite predicate; injected `resolvePrompt` dep now `(sourceDraft?: string) => Promise<ResolvedPrompt>` (Rewrite carry); egress-failure path now aborts the controller before nulling (HIGH-2); ack path removed; `generate` dep now returns `Promise<readonly string[]>`.
- `src/logic/ai-generate-variants.ts` — NEW node-pure `generateVariants(generateOne, prompt, signal, count=3)` fan-out helper (passes each call its 0-based variantIndex) + NEW node-pure `variantTemperature(base, variantIndex, count)` per-call variation helper (COMP-12 lever, consumed by the plan-35-08 screen adapter).
- `src/logic/ai-availability.ts` — `AiAvailability` type, `computeAiAvailability`, availability→affordance selector.
- New tests `src/logic/ai-generate-variants.test.ts`, `src/logic/ai-availability.test.ts`; reshaped `src/logic/ai-suggestion-logic.test.ts` and `src/logic/ai-suggestion-compose-integration.test.ts` (retired-ack assertions removed / rewritten to the three-suggestion contract, or deleted if superseded — build-breaker #3).
- (unchanged, noted for grounding) `src/services/AiService.ts` `AiProvider.generate` stays `Promise<string>`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-04-SUMMARY.md` when done.
</output>
