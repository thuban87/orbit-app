---
phase: 35-messaging-ai-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/ComposeScreen.tsx
  - src/stores/compose-session-store.ts
  - src/stores/compose-session-store.test.ts
autonomous: true
requirements: [COMP-01, COMP-05, COMP-06, COMP-07]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "COMP-01: Compose opens directly on a blank editor (placeholder 'Write your message…'); no greeting, AI prose, remembered context, or auto-started AI is inserted on open."
    - "COMP-05: Transmit hands the Text composition to the OS SMS composer via performReachOut and never claims Orbit delivered it; on return a compact 'Did you send it?' panel offers 'Yes, log interaction' (writes the canonical Message interaction) and 'Not yet' (keeps the session, writes nothing)."
    - "COMP-05: Copy copies the body to the clipboard and never triggers the 'Did you send it?' panel."
    - "COMP-06 / D-04 (ESCALATE trip-wire): the Compose 'Did you send it?' panel is ADDITIVE — the app-global AssistBanner + PendingConfirmationsSheet (App.tsx:393) remain mounted and functional, a dismissal path exists, and the confirmed interaction stamps at handoff_at via markAssistLogged, never at confirmation time."
    - "COMP-07 / D-10: session body + subject + mode + destination live in a Zustand compose-session-store keyed by contactId; the draft survives in-app navigation and ordinary backgrounding but is cleared on Transmit-confirmed and is NOT restored across app relaunch (no drafts table, no backup contract)."
    - "D-05: 'Not yet' is not 'Don't log' — it dismisses the panel and preserves the session; the pending assist row lingers until explicit logging or 24h expiry."
    - "D-06 [informational, already shipped]: the confirmation reuses markAssistLogged UNCHANGED, which already routes the assist channel through remapLegacyChannel (text→Message) at interaction-assist-dao.ts:112 — no channel-translation work is planned or added here."
    - "D-11 [informational, flag-not-fix]: ADR-035/036 text 'Send/Copy never write to SQLite' is stale (superseded in practice by ADR-072's assist row); the assist write is PRESERVED, not removed — the overdue superseding note is flagged to the owner, never 'fixed' by deleting the write."
    - "D-01 / D-02: the tracer respects the dossier + planning-notes as ground truth; no [DECIDED]/[REJECTED] item or Accepted ADR is reversed."
    - "The transmit call derives its channel from the session-store mode field (default 'text'), not a hardcoded channel literal — Email is an additive branch in a later plan, not an architectural change."
  artifacts:
    - "src/stores/compose-session-store.ts — Zustand store (body, subject, mode, destination) keyed by contactId with clear-on-confirm"
    - "src/stores/compose-session-store.test.ts — session-survival + clear-on-relaunch/confirm tests"
    - "src/screens/ComposeScreen.tsx — rebuilt editor-first Text slice with additive 'Did you send it?' panel in AppText/Button roles"
  key_links:
    - "ComposeScreen Transmit → performReachOut (handoff.ts) → createPendingAssist (row @ handoff_at) → OS SMS composer"
    - "'Did you send it?' → Yes → markAssistLogged (interaction @ handoff_at, sole recency writer); Not yet → dismiss panel, session preserved"
    - "compose-session-store ↔ AppState subscribe (assist-store pattern) so backgrounding does not lose the draft"
  prohibitions:
    - "Transmit must never assert that Orbit delivered or sent the message."
    - "The logged interaction must never be stamped at confirmation time; it stamps at the assist row's handoff_at."
    - "Must never remove the app-global AssistBanner, the PendingConfirmationsSheet, or the assist dismissal path (reverses ADR-070/071 — owner escalation)."
    - "Must never persist the draft to any durable store (no drafts table, no backup, no AsyncStorage)."
---

<objective>
Ship the leading production-quality tracer for Phase 35: the thinnest honest end-to-end reach-out — blank editor → resolve the primary phone (Text mode) → Transmit hands off to the OS SMS composer → returning shows an ADDITIVE "Did you send it?" panel → "Yes, log interaction" logs the canonical Message interaction through the existing assist path. All new/rebuilt UI speaks in `AppText` and `Button` roles. Session state moves into a new Zustand `compose-session-store` so the draft survives navigation and backgrounding but never relaunch.

This proves the full vertical: UI (editor-first, roles) → session store → capability gate (reuse) → OS handoff (reuse) → durable assist row → confirmation write (reuse) — before Email, Subject, Research, AI review, Message Focus, three-state availability, and origin-aware return are expanded in later plans.

Purpose: catch any architectural dead-end (assist coexistence, session persistence, role-based rebuild) after one commit rather than after ten layers. Honors D-04's ESCALATE coexistence trip-wire and D-05/D-06/D-11 by REUSING the assist plumbing unchanged.
Output: a working Text-only Compose reach-out with the confirmation panel, and the session store the rest of the phase builds on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-messaging-ai-compose/35-CONTEXT.md
@.planning/phases/35-messaging-ai-compose/35-RESEARCH.md
@.planning/phases/35-messaging-ai-compose/35-PATTERNS.md
@.planning/phases/35-messaging-ai-compose/35-UI-SPEC.md
@.planning/phases/35-messaging-ai-compose/35-VALIDATION.md

# Decision ground truth (read the sections cited by RESEARCH.md before touching the assist path)
@docs/dossier/milestone-2/phase-14-messaging-ai-compose-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-14-planning-notes.md
@docs/decisions/ADR-070-durable-pending-interaction-assist-lifecycle-and-portable-opt-out.md
@docs/decisions/ADR-071-user-attested-handoff-time-interaction-logging-through-the-sole-recency-writer.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: compose-session-store (session-only draft state)</name>
  <files>src/stores/compose-session-store.ts, src/stores/compose-session-store.test.ts</files>
  <read_first>
    - src/stores/assist-store.ts (FULL — the analog: Zustand `create<T>()((set) => …)` shape + `subscribeAppState` AppState-change pattern; localDateTime import at :2)
    - src/screens/ComposeScreen.tsx (FULL — current draft is local `useState("")` at :143; the focus effect reloads on every focus :279-401; understand what is lost on unmount today)
    - src/db/database.ts (locate `localDateTime` export — timestamps use local wall-clock, never toISOString)
    - CLAUDE.md "Zustand stores in src/stores/" + "never toISOString().split('T')[0]"
  </read_first>
  <action>
    Create a Zustand store `useComposeSession` with per-contact session state: fields `contactId`, `body` (string), `subject` (string, default ''; consumed by Email mode's Subject field in a later plan, but the field lives in the store now so it survives nav/background per COMP-07), `mode` ('text' | 'email', default 'text' this plan — 'email' branch added later), and `destination` (the resolved endpoint value, nullable). Provide actions `startSession(contactId)` (initializes blank state for a contact, preserving an existing in-memory session for the SAME contactId across nav/background), `setBody(body)`, `setSubject(subject)`, `setMode(mode)`, `setDestination(value)`, and `clearSession(contactId)` (called on Transmit-confirmed, clears subject too). Model the AppState-survival on `assist-store.ts` — do NOT persist to SQLite, AsyncStorage, or any durable store (D-10: session-only, no drafts table, no backup). The store is plain in-memory module state, so it is naturally lost on relaunch; assert that in the test rather than adding relaunch persistence. Use `localDateTime()` for any timestamp, never `toISOString()`.
  </action>
  <verify>
    <automated>npx vitest run src/stores/compose-session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: after `setBody('draft')` and `setSubject('re: lunch')`, a simulated in-app navigation away and back (store not re-created) still reads `body === 'draft'` and `subject === 're: lunch'`.
    - Test: `clearSession(contactId)` empties body/subject/destination for that contact.
    - Test: a fresh module import (simulating relaunch) yields empty session state — no durable restore.
    - `grep -n "toISOString" src/stores/compose-session-store.ts` returns nothing.
    - No import of `expo-sqlite`, `@react-native-async-storage`, or any DAO in the store file.
  </acceptance_criteria>
  <done>The store holds session draft state that survives nav/background, clears on confirm, and does not survive relaunch; tests green.</done>
</task>

<task type="tracer">
  <name>Task 2: Rebuild ComposeScreen as the editor-first Text reach-out slice (roles)</name>
  <files>src/screens/ComposeScreen.tsx</files>
  <read_first>
    - src/screens/ComposeScreen.tsx (FULL, 1202 lines — the current fuel-first, text-only, single-suggestion-with-ack-gate screen; note blank editor at :143/:894-914, hardcoded `channel: "text"` at :456, `resetToDashboardRoot` import :84, "Copied" feedback via setState+setTimeout :469-479, SMS.isAvailableAsync no-flash pattern :376-390)
    - src/components/ui/AppText.tsx (FULL — role map: heading/body/label/caption; never raw fontSize; never allowFontScaling={false})
    - src/components/ui/button-roles.ts + src/components/ui/Button.tsx (FULL — primary/secondary/tertiary/destructive roles, MIN_TOUCH_TARGET=44; accent FILL reserved for the single primary per surface)
    - src/logic/compose-logic.ts (FULL — reuse resolveComposeControls for the Text destination gate and the smsAvailable===null no-flash discipline; do NOT re-derive capability in the screen)
    - src/services/reach-out/handoff.ts (FULL — performReachOut creates the pending assist BEFORE handoff, stamped handoff_at=now; markAssistFailed on catch; Text arm uses SMS.sendSMSAsync)
    - src/db/interaction-assist-dao.ts (:74-153 — markAssistLogged inserts one interaction @ transactionAssist.handoff_at via the sole recency writer and already remaps channel at :112; markAssistDismissed at :141-153)
    - src/stores/compose-session-store.ts (Task 1 output)
    - src/components/AssistBanner.tsx + App.tsx (:393 mount — confirm the app-global banner + pending sheet are NOT touched)
    - 35-UI-SPEC.md (Copywriting Contract rows: Transmit, Copy → "Message copied", "Did you send it?" / "Yes, log interaction" / "Not yet"; Typography role assignment)
  </read_first>
  <action>
    Rebuild the Compose surface as editor-first (NOT fuel-first): a blank multiline body editor at `AppText role="body"` sizing with placeholder "Write your message…", the contact name at `role="heading"`, and completion actions (Transmit primary, Copy). This tracer supports Text mode only — resolve the primary phone via `selectActionablePrimaryMethods` and gate Transmit/Copy through the existing `resolveComposeControls` (keep the `smsAvailable===null` probe-pending no-flash behavior). Wire the body to the compose-session-store (`setBody`/`startSession(contactId)` on mount) instead of local `useState`. Transmit calls `performReachOut` with `channel` derived from the store `mode` field (currently 'text'), NOT a hardcoded `"text"` literal. On return from handoff, show a compact ADDITIVE "Did you send it?" panel (`role="heading"`) with `Yes, log interaction` (primary → `markAssistLogged`, disable re-tap during the write to prevent a double-log) and `Not yet` (secondary → dismiss panel, keep session, leave the assist row). Copy uses `expo-clipboard`, shows the "Message copied" feedback via the existing setState+setTimeout pattern (never per-frame animation), and never opens the confirmation panel. REMOVE the old fuel-first cards, the single-suggestion AI block, and the ADR-052 `needs-acknowledgement` ack gate from this screen (AI is re-added in a later plan; its temporary absence is an acceptable functionality gap). Keep `resetToDashboardRoot` Back behavior for now (origin-aware return is a later plan). Do NOT remove or alter the app-global AssistBanner/PendingConfirmationsSheet (App.tsx) — the Compose panel is additive. Speak only in `AppText`/`Button` roles; no hand-rolled `Pressable`/raw `fontSize`; no `accent` fill on text.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fontSize' src/screens/ComposeScreen.tsx` returns no raw numeric fontSize on text (roles only).
    - `grep -n 'channel: "text"' src/screens/ComposeScreen.tsx` returns nothing (channel derives from the store mode field).
    - `grep -n 'needs-acknowledgement\|acknowledgedRef\|acknowledgeProvider' src/screens/ComposeScreen.tsx` returns nothing (ack gate removed).
    - ComposeScreen still imports and does NOT unmount/hide the app-global banner; `grep -n "AssistBanner\|PendingConfirmationsSheet" App.tsx` still shows the mount at App.tsx.
    - `npx tsc --noEmit` clean; `npm run check:colors` passes (no hex, incl. Skia).
  </acceptance_criteria>
  <done>Compose opens blank in Text mode, Transmit hands off and shows the additive "Did you send it?" panel, Copy copies without confirmation, and the screen uses AppText/Button roles.</done>
</task>

<task type="auto">
  <name>Task 3: Confirmation-coexistence assertion (assist path reused unchanged)</name>
  <files>src/db/interaction-assist-dao.test.ts</files>
  <read_first>
    - src/db/interaction-assist-dao.test.ts (FULL — existing coverage of markAssistLogged/markAssistDismissed; extend, do not rewrite)
    - src/db/interaction-assist-dao.ts (:74-153 — the write under test; stamp @ handoff_at; remap @ :112)
    - Every writer of the `interactions` table that this confirmation path drives: src/db/interaction-assist-dao.ts (markAssistLogged → insertInteractionCore + recomputeLastContactCore). Confirm insertInteractionCore + recomputeLastContactCore remain the sole recency writer (grep `insertInteractionCore`, `recomputeLastContactCore` across src/db/ before asserting the invariant).
  </read_first>
  <action>
    Extend the existing assist-dao test to lock the Phase-35 coexistence invariants (no production change to the DAO — it is reused unchanged): (a) markAssistLogged stamps the inserted interaction's occurredAt at the assist's `handoff_at`, NOT the `now` passed in; (b) the written channel is the remapped vocabulary value ('Message' for a 'text' assist) via remapLegacyChannel; (c) markAssistDismissed is the "Not yet"/"Don't log" dismissal path and creates no interaction row. These tests exist to fail loudly if a future edit tries to restamp at confirmation time or strip the remap (Trip-Wire 1 & 2). Do not add any new translation or restamp logic to production code.
  </action>
  <verify>
    <automated>npx vitest run src/db/interaction-assist-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts inserted occurredAt === assist.handoff_at (not the confirmation `now`).
    - Test asserts a 'text' assist logs channel 'Message'.
    - Test asserts markAssistDismissed inserts zero interaction rows.
    - No diff to src/db/interaction-assist-dao.ts (production DAO unchanged).
  </acceptance_criteria>
  <done>The assist-coexistence invariants are locked by tests; the DAO is confirmed reused unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose UI → OS SMS composer | The composition leaves the Orbit app boundary via an OS intent; Orbit cannot observe delivery. |
| Confirmation UI → SQLite recency writer | User-attested "Yes" creates the sole canonical interaction row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Repudiation/Tampering | "Did you send it?" → markAssistLogged | high | mitigate | Stamp at handoff_at (never confirmation time); disable re-tap on "Yes" during the write to prevent a double-log (ADR-071, D-04); locked by interaction-assist-dao.test.ts. |
| T-35-07 | Spoofing/Repudiation | Transmit copy | medium | mitigate | Transmit never claims Orbit delivered the message; the honest "Did you send it?" prompt is the only truth source (D-05). |
| T-35-08 | Denial of state | compose-session-store | low | accept | Session-only store; loss on relaunch is by design (D-10) and carries no user-content durability promise. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan (no-dependency path); nothing to verify. |
</threat_model>

<verification>
- `npx vitest run src/stores/compose-session-store.test.ts src/db/interaction-assist-dao.test.ts` green.
- `npx tsc --noEmit` clean; `npm run check:colors` passes.
- Device backstop (phase-gate UAT, not this plan): on the Pixel, open Compose, type a draft, background+foreground (draft survives), Transmit in Text mode (SMS composer opens), return, "Yes, log interaction" logs a Message; confirm the app-global banner still surfaces a pending assist when the session is gone.
</verification>

<success_criteria>
Blank editor-first Text reach-out works end-to-end with an additive confirmation panel, session state survives nav/background but not relaunch, and the app-global assist lifecycle is preserved unchanged.
</success_criteria>

<artifacts_produced>
- `src/stores/compose-session-store.ts` — `useComposeSession` Zustand store; state `{ contactId, body, subject, mode: 'text'|'email', destination }`; actions `startSession`, `setBody`, `setSubject`, `setMode`, `setDestination`, `clearSession`.
- `src/stores/compose-session-store.test.ts` — new test file.
- `src/screens/ComposeScreen.tsx` — rebuilt editor-first Text slice; additive "Did you send it?" panel; AppText/Button roles; ack gate + fuel-first + single-suggestion AI removed.
- (test-only) extended assertions in `src/db/interaction-assist-dao.test.ts`.
</artifacts_produced>

<output>
Create `.planning/phases/35-messaging-ai-compose/35-01-SUMMARY.md` when done.
</output>
