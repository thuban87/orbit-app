---
phase: 11-actionable-notifications
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/notifications/decay-suppression.ts
  - src/db/notification-read.ts
  - src/db/notification-read.test.ts
autonomous: true
requirements: [NOTIF-03, NOTIF-04]
user_setup: []

must_haves:
  truths:
    - "The decay-due read returns overdue-but-not-rogue contacts and EXCLUDES never-contacted, snoozed, rogue (>=ROGUE_K), rarely_responds, and reminders_off — reusing status.ts fragments, never recomputing rogue."
    - "The birthday read returns non-archived contacts whose daysUntilBirthday()===0, ignoring EVERY decay suppressor (mirrors the dashboard birthday banner)."
    - "The decay read returns each candidate's last_contact + interval_days so the scheduler can derive the due date and weekly re-nag cadence."
  artifacts:
    - src/services/notifications/decay-suppression.ts
    - src/db/notification-read.ts
    - src/db/notification-read.test.ts
  key_links:
    - "Imports PROGRESS_SQL/WOBBLE_MAX/ROGUE_K from status.ts and daysUntilBirthday from birthday-logic.ts — the single shared cutoffs/parser, never redefined."
---

<objective>
Create the notification read layer: a `decay-suppression` SQL fragment (the "eligible for a decay nudge" predicate, composed from status.ts) and `notification-read.ts` exposing `listDecayDueCandidates` (overdue, non-suppressed) and `listBirthdayNotificationCandidates` (non-archived, day-of). The suppression predicate encodes NOTIF-03 entirely; the birthday read encodes NOTIF-04's different, decay-suppressor-ignoring rule.

Purpose: the scheduler asks "who should be notified?" here — deriving nothing itself. Rogue is read from status.ts, the birthday parser from birthday-logic.ts; neither is re-implemented.
Output: `decay-suppression.ts`, `notification-read.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/db/status.ts
@src/db/dashboard-read.ts
@src/logic/birthday-logic.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decay-suppression fragment + notification-read read module</name>
  <files>src/services/notifications/decay-suppression.ts, src/db/notification-read.ts</files>
  <read_first>src/db/status.ts:40-73 (PROGRESS_SQL / WOBBLE_MAX / ROGUE_K / STATUS_SQL + the TIMEZONE note: only `now` gets 'localtime', a stored column is bare date()), src/db/dashboard-read.ts:137-144 (BASE_WHERE snooze clause) + :335-344 (listBirthdayCandidates: archived_at IS NULL AND birthday IS NOT NULL), src/logic/birthday-logic.ts:136 (daysUntilBirthday signature + FEB_29_OBSERVED_DAY), 11-PATTERNS.md §notification-read.</read_first>
  <behavior>
    - `listDecayDueCandidates(exec)` returns rows `{ id, name, last_contact, interval_days }` for every contact where ALL hold: last_contact IS NOT NULL; (snooze_until IS NULL OR date(snooze_until) <= date('now','localtime')); rarely_responds = 0; reminders_off = 0; archived_at IS NULL; and PROGRESS_SQL is in [WOBBLE_MAX, ROGUE_K).
    - A snoozed (future snooze_until), never-contacted, rarely_responds, muted (reminders_off=1), archived, stable/wobble (progress < WOBBLE_MAX), or rogue (progress >= ROGUE_K) contact is EXCLUDED.
    - `listBirthdayNotificationCandidates(exec, today)` reads archived_at IS NULL AND birthday IS NOT NULL, then JS-filters to daysUntilBirthday(birthday, today) === 0 — and does NOT apply any decay suppressor (a muted/snoozed/rogue contact STILL gets a birthday nudge).
  </behavior>
  <action>Create `decay-suppression.ts` mirroring status.ts's string-constant posture: export a `DECAY_ELIGIBLE_WHERE` SQL fragment built by interpolating ONLY the imported code-constants `PROGRESS_SQL`, `WOBBLE_MAX`, `ROGUE_K` from `@/db/status` — the predicate is `last_contact IS NOT NULL AND (snooze_until IS NULL OR date(snooze_until) <= date('now','localtime')) AND rarely_responds = 0 AND reminders_off = 0 AND archived_at IS NULL AND (PROGRESS_SQL) >= WOBBLE_MAX AND (PROGRESS_SQL) < ROGUE_K`. Interpolate no user input; every runtime value in the eventual query is `?`-bound (there are none here — the predicate is closed constants). Follow the status.ts TIMEZONE rule: only `now` gets `'localtime'`; the stored `snooze_until`/`last_contact` are truncated with a bare `date()`. Create `notification-read.ts` (pure async read, dashboard-read posture): `listDecayDueCandidates(exec)` selects `id, name, last_contact, interval_days` FROM contacts WHERE `DECAY_ELIGIBLE_WHERE`; `listBirthdayNotificationCandidates(exec, today)` reuses the `listBirthdayCandidates` shape (SELECT id, name, birthday WHERE archived_at IS NULL AND birthday IS NOT NULL) then filters in JS with `daysUntilBirthday(row.birthday, today) === 0` (import the single parser — do NOT re-implement it, both Obsidian bugs are already fixed). Import `WOBBLE_MAX`/`ROGUE_K`/`PROGRESS_SQL` — never re-type a rogue cutoff (CLAUDE.md / RESEARCH anti-pattern).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - decay-suppression.ts interpolates only status.ts constants; no user input, no second rogue cutoff.
    - notification-read exposes the two reads with the field shapes above; birthday read ignores decay suppressors.
    - biome clean.
  </acceptance_criteria>
  <done>The scheduler has a single read source for decay-due and birthday candidates, reusing status.ts + birthday-logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: notification-read.test.ts — suppression + birthday coverage (in-memory sqlite)</name>
  <files>src/db/notification-read.test.ts</files>
  <read_first>src/db/dashboard-read.test.ts (the in-memory node:sqlite fixture + SQL-parity approach), src/db/status.ts (thresholds to compute fixture progress values).</read_first>
  <behavior>
    - A contact overdue but not rogue (progress in [1.0, 3)) is returned by listDecayDueCandidates with its last_contact + interval_days.
    - Each excluded case is asserted absent: never-contacted (last_contact NULL), future snooze_until, rarely_responds=1, reminders_off=1, archived, stable/wobble (progress < 1.0), rogue (progress >= 3).
    - A past snooze_until does NOT exclude (still eligible).
    - listBirthdayNotificationCandidates returns non-archived day-of contacts even when they are muted/snoozed/rogue; excludes archived and non-today birthdays; a Feb-29 contact in a non-leap "today" is handled by the parser (no crash).
    - SQL-parity: the fixture's expected progress buckets match status.ts thresholds.
  </behavior>
  <action>Write the co-located test (RED before Task 1 implementation is finalized, then GREEN) using the same in-memory node:sqlite executor + seeding helpers as dashboard-read.test.ts. Seed contacts spanning every suppression case using `date('now','localtime', '-N days')` for last_contact so progress lands in a known bucket for a fixed interval_days. Cover the birthday read with a day-of contact plus a muted/snoozed day-of contact (still returned) and an archived day-of contact (excluded). Assert exact candidate id sets.</action>
  <verify>
    <automated>npx vitest run src/db/notification-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every NOTIF-03 exclusion and the overdue-eligible case are asserted.
    - NOTIF-04 birthday day-of returns non-archived contacts ignoring decay suppressors.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>Suppression and birthday-candidate behavior is proven against an in-memory DB with status.ts-parity fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored contact columns → notification eligibility | the read decides who is exposed via a scheduled notification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SUP | Information disclosure | decay-suppression predicate | high | mitigate | A muted (reminders_off=1) or snoozed contact must never be scheduled — the predicate excludes them in-query; proven by notification-read.test.ts. Birthday intentionally ignores these (decided: birthdays fire for all non-archived). |
| T-11-INJ | Injection | notification-read queries | medium | mitigate | Only closed status.ts code-constants are interpolated; the `today` for the birthday parser is app-supplied, not user free-text; no string interpolation of runtime values. |
</threat_model>

## Artifacts this plan produces
- **File:** `src/services/notifications/decay-suppression.ts` — exports `DECAY_ELIGIBLE_WHERE`.
- **File:** `src/db/notification-read.ts` — exports `listDecayDueCandidates` (`DecayDueCandidate { id, name, last_contact, interval_days }`) and `listBirthdayNotificationCandidates` (`BirthdayCandidate { id, name, birthday }`).
- **File:** `src/db/notification-read.test.ts`.

<verification>
- `npx vitest run src/db/notification-read.test.ts` green.
- tsc + biome clean; no re-typed rogue/threshold constant (imports from status.ts only).
</verification>

<success_criteria>
The decay-eligibility and birthday-candidate reads exist, exclude the right contacts (NOTIF-03) / include the right ones (NOTIF-04), and reuse the shared status + birthday primitives.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-04-SUMMARY.md` when done.
</output>
