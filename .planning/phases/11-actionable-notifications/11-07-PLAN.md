---
phase: 11-actionable-notifications
plan: 07
type: execute
wave: 2
depends_on: [11-01, 11-03]
files_modified:
  - src/services/notifications/notification-actions.ts
  - src/services/notifications/notification-actions.test.ts
  - src/services/notifications/headless-task.ts
autonomous: true
requirements: [NOTIF-02]
user_setup: []

must_haves:
  truths:
    - "One shared handler routes a mark-contacted action through recordTouchpoint (source='notification', outbound, unspecified, connected=1) and a snooze action through snooze-dao (+1 week), then cancels the scheduled notification for that contact."
    - "The action category registers mark + snooze buttons with opensAppToForeground:false so a killed-app tap reaches the headless path."
    - "The headless task is defined + registered at module scope so it survives process death and funnels to the same shared handler."
  artifacts:
    - src/services/notifications/notification-actions.ts
    - src/services/notifications/headless-task.ts
  key_links:
    - "Both wirings (foreground listener in 11-12, headless task here) funnel to handleNotificationAction; writes go only through the existing mutexed DAOs."
---

<objective>
Create the write side of the headless actions: `notification-actions.ts` (the mark/snooze action category registration + the single shared `handleNotificationAction(data)` that both the foreground listener and the headless task call) and `headless-task.ts` (the module-scope `registerTaskAsync` task). Mark routes through the existing single-writer `recordTouchpoint`; snooze routes through the 11-03 `snooze-dao`. Neither ever does a raw DB write.

Purpose: make the notification's one-tap actions write correctly and identically whether the app is alive (foreground listener) or killed (headless task) — the 04-log double-write requirement, now covering snooze.
Output: `notification-actions.ts` (+ test), `headless-task.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/db/recency-dao.ts
@src/db/snooze-dao.ts
@src/services/notifications/notification-ids.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notification-actions — category registration + the shared handler</name>
  <files>src/services/notifications/notification-actions.ts, src/services/notifications/notification-actions.test.ts</files>
  <read_first>11-RESEARCH.md §Pattern 4 (double-wired handler; custom buttons set opensAppToForeground:false; both wirings funnel to one handler then cancel) + §Assumption A6 (the 04-log canonical one-tap values), src/db/recency-dao.ts:56-76 + :215-240 (recordTouchpoint input shape + source field), src/db/snooze-dao.ts (snoozeContact signature), src/services/notifications/notification-ids.ts (DECAY_CATEGORY / ACTION_MARK / ACTION_SNOOZE / decayIdentifier / NotificationData).</read_first>
  <behavior>
    - `ensureNotificationCategories()` registers the DECAY_CATEGORY category with two action buttons (ACTION_MARK, ACTION_SNOOZE), both with opensAppToForeground:false.
    - `handleNotificationAction(data, actionIdentifier)` with actionIdentifier ACTION_MARK calls recordTouchpoint(exec, { contactId, uid, occurredAt, now, source:"notification", direction:"outbound", channel:"unspecified", connected:1, quality:null }) then cancels decayIdentifier(contactId).
    - With actionIdentifier ACTION_SNOOZE it calls snoozeContact(exec, { contactId, uid, preset:"1w", now }) then cancels decayIdentifier(contactId).
    - The handler mints uid via newUid() and local now/occurredAt via localDateTime(); it obtains the executor via getExecutor().
    - An unknown actionIdentifier does nothing (no write, no cancel).
  </behavior>
  <action>Create `notification-actions.ts` exporting `ensureNotificationCategories()` (one `Notifications.setNotificationCategoryAsync(DECAY_CATEGORY, [mark button, snooze button])`, both buttons `opensAppToForeground:false` so the killed-app path reaches the headless task) and the shared `handleNotificationAction(data, actionIdentifier)`. In the handler, obtain `getExecutor()`, mint `newUid()` + `localDateTime()`, and branch on actionIdentifier: mark → `recordTouchpoint` with the 04-log canonical values (source "notification", direction "outbound", channel "unspecified", connected 1, quality null — A6); snooze → `snoozeContact` preset "1w" (the fixed +1-week headless snooze). After either write, `cancelScheduledNotificationAsync(decayIdentifier(data.contactId))`. Never write last_contact/snooze_until directly — only through the DAOs (mutex intact). Write the co-located test with `vi.mock("expo-notifications")` + a spy/mock on recordTouchpoint and snoozeContact (or an in-memory sqlite executor): assert mark passes the exact canonical values and cancels; snooze calls snooze-dao "1w" and cancels; an unknown action is a no-op; the category registration passes opensAppToForeground:false for both buttons.</action>
  <verify>
    <automated>npx vitest run src/services/notifications/notification-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - mark routes through recordTouchpoint with the canonical one-tap values; snooze through snooze-dao "1w"; both cancel the decay identifier.
    - Category buttons set opensAppToForeground:false.
    - No raw DB writes; unknown action is a no-op.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>The shared handler + action category exist and write only through the mutexed DAOs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: headless-task — module-scope registerTaskAsync funneling to the shared handler</name>
  <files>src/services/notifications/headless-task.ts</files>
  <read_first>11-RESEARCH.md §Pattern 4 (registerTaskAsync at MODULE scope; the JS listener is a no-op when killed; A2 device spike) + §Pitfall 5 (the headless path must do ONLY the write + the single cancel — never reach the launch reconcile), src/services/launch-sweep.ts:10-24 (the negative rules the headless path must respect).</read_first>
  <action>Create `headless-task.ts` that, at MODULE scope, defines a TaskManager task (a stable task name constant) whose body extracts the notification data + actionIdentifier and calls `handleNotificationAction` from notification-actions, and calls `Notifications.registerTaskAsync(TASK_NAME)`. This module is imported for its side effect (registration) from App.tsx (11-13) — importing it registers the task; it runs NOTHING else at import (no reconcile, no sweep — Pitfall 5). Keep the task body minimal: resolve the executor, funnel to the shared handler, done. Add a top-of-file note that this path is the A2 device spike — its FCM-less bring-up on the release APK is proven by Pixel-UAT (it cannot be exercised in the JS harness). Guard the task body in try/catch + Logger so a failure does not crash the headless context.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Pixel-UAT (phase gate, A2 device spike): on the FCM-less release APK, kill the app, tap mark/snooze from the shade, and verify the DB write via `run-as com.bwales.orbit` reading SQLite; confirm the reconcile did NOT run on the headless tap.</manual>
  </verify>
  <acceptance_criteria>
    - The task is defined + registered at module scope and funnels to handleNotificationAction.
    - The module runs nothing beyond registration at import (no reconcile/sweep reachable).
    - tsc + biome clean.
  </acceptance_criteria>
  <done>A module-scope headless task exists that writes killed-app actions through the shared handler, pending on-device FCM-less proof (A2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification action tap → DB write | headless/foreground taps write to contacts/interactions/events |
| notification data payload → handler | data.contactId drives which contact is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-MUTEX | Tampering | headless/foreground action writes | high | mitigate | Mark → recordTouchpoint (single-writer mutex); snooze → snooze-dao (mutexed). No raw UPDATE anywhere in the action path; the headless task cannot reach the launch reconcile (Pitfall 5). |
| T-11-SPOOF | Spoofing | data.contactId | medium | mitigate | data.contactId is app-minted at schedule time (not user-supplied); recordTouchpoint/snooze-dao assert the row exists (changes===1) so a stale/missing id fails loudly rather than corrupting another contact. |
</threat_model>

## Artifacts this plan produces
- **File:** `src/services/notifications/notification-actions.ts` — exports `ensureNotificationCategories`, `handleNotificationAction` (and the action-button config).
- **File:** `src/services/notifications/headless-task.ts` — module-scope TaskManager task + `registerTaskAsync`, exports the task-name constant.
- **File:** `src/services/notifications/notification-actions.test.ts`.

<verification>
- `npx vitest run src/services/notifications/notification-actions.test.ts` green.
- Pixel-UAT (phase gate): killed-app FCM-less mark/snooze write proven on-device (A2).
- tsc + biome clean.
</verification>

<success_criteria>
Mark and snooze actions write identically via the shared handler through the mutexed DAOs, with the category set for the headless path and a module-scope headless task registered.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-07-SUMMARY.md` when done.
</output>
