---
phase: 11-actionable-notifications
plan: 09
type: execute
wave: 2
depends_on: [11-03]
files_modified:
  - src/screens/EditContactScreen.tsx
  - src/db/contact-read.ts
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [NOTIF-03]
user_setup: []

must_haves:
  truths:
    - "The existing edit-form reminders_off toggle reads 'Mute reminders' with a helper line; its testID, position, and on/off contract are unchanged (copy-only)."
    - "The profile shows a Snooze reminders block with 3-day / 1-week / 1-month presets that write snooze_until via snooze-dao, plus a 'Snoozed until {date}' status and a Clear affordance when a snooze is active."
    - "getContactHeader additively returns snooze_until so the profile can render the snooze status without a second read."
  artifacts:
    - src/screens/EditContactScreen.tsx (relabel)
    - src/db/contact-read.ts (getContactHeader widened with snooze_until)
    - src/screens/ContactProfileScreen.tsx (snooze presets block)
  key_links:
    - "The presets call snooze-dao (11-03), then the profile's unified load() reconciles the header — reversible, no confirmation."
---

<!-- REVIEW-CYCLE-1 (11-REVIEWS.md) — item 7: the snooze status must render the stored YYYY-MM-DD
     directly (or parse with local components); passing the string to formatLocalDate (Date-typed,
     dates.ts:17) / new Date("YYYY-MM-DD") reintroduces the forbidden UTC evening off-by-one. Also
     item 10: clearSnooze now requires a uid and always writes an unsnooze event. -->

<objective>
Deliver the two in-app NOTIF-03 surfaces: relabel the already-wired edit-form `reminders_off` toggle to "Mute reminders" (copy-only), and add the profile in-app snooze presets (3d/1wk/1mo + status + clear) that write through the 11-03 snooze-dao. Widen `getContactHeader` additively with `snooze_until` so the profile can show the active-snooze status.

Purpose: give the user the mute and the in-app snooze controls the suppression predicate consumes — mute is a copy change (the real wiring is the suppression predicate, already built in 11-04); snooze is the first UI writer of snooze_until.
Output: relabelled edit toggle, widened `getContactHeader`, profile snooze block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-UI-SPEC.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/screens/EditContactScreen.tsx
@src/db/contact-read.ts
@src/screens/ContactProfileScreen.tsx
@src/db/snooze-dao.ts
@src/components/FilterChipRow.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Relabel the edit-form mute toggle (copy-only)</name>
  <files>src/screens/EditContactScreen.tsx</files>
  <read_first>src/screens/EditContactScreen.tsx:679-693 (the existing reminders_off Switch: testID edit-contact-reminders-off, wired to form.remindersOff), 11-UI-SPEC.md §2 (copy-only relabel + helper), 11-PATTERNS.md §Edit-form mute — COPY-ONLY.</read_first>
  <action>Change the visible label Text and the Switch's accessibilityLabel from "Turn off reminders" to "Mute reminders", and add a helper Text line below the toggle row: "Keep them in Orbit, but never get reminders about them." (13/400 textSecondary, matching the "Rarely responds" helper idiom directly above). Keep the testID `edit-contact-reminders-off`, the position (immediately after the "Rarely responds" field), the token styling (trackColor false=border/true=accent, thumbColor surfaceElevated), and the on/off `form.remindersOff` contract EXACTLY. Do NOT rebuild the control or touch edit-contact-logic.ts (the mapping already exists at edit-contact-logic.ts:148). This is the only edit-form change.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <manual>Pixel-UAT (phase gate): the toggle reads "Mute reminders" + helper; toggling still persists reminders_off.</manual>
  </verify>
  <acceptance_criteria>
    - Label + accessibilityLabel read "Mute reminders"; helper line present; testID/position/contract unchanged.
    - No change to edit-contact-logic.ts; tsc + check:colors clean.
  </acceptance_criteria>
  <done>The edit-form mute toggle is relabelled per the UI-SPEC without rebuilding the control.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Widen getContactHeader with snooze_until (additive)</name>
  <files>src/db/contact-read.ts</files>
  <read_first>src/db/contact-read.ts:65-114 (getContactHeader — the additive-widening idiom already used for favourite_rank and phone: append to the SELECT + both type literals; the two other callers read field-wise so widening is safe).</read_first>
  <action>Add `snooze_until: string | null` to `getContactHeader`'s return type (both the function's declared return type and the internal `getFirstAsync<...>` type literal) and append `snooze_until` to the SELECT column list. This is PURELY ADDITIVE (same idiom as favourite_rank/phone): the EditContactScreen refreshPhoto caller and contact-read.test.ts read fields individually, so nothing breaks. Document the widening in a short comment mirroring the favourite_rank/phone comments. No new query, no join.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/db/contact-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - getContactHeader returns snooze_until additively; existing callers + contact-read.test.ts stay green.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>The profile can read the active snooze date from the existing header seek.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Profile Snooze-reminders block (presets + status + clear)</name>
  <files>src/screens/ContactProfileScreen.tsx</files>
  <read_first>src/screens/ContactProfileScreen.tsx:101-120 (the local Header type + header state) + :586-627 (the Message / Log contact action block + styles.logContact) + the unified load() (around :164-205), src/components/FilterChipRow.tsx:51-84 (filled-accent chip idiom: active accent fill + background label; inactive surface+border+textSecondary), 11-UI-SPEC.md §3 (the exact copy + testIDs), src/db/snooze-dao.ts (snoozeContact / clearSnooze — clearSnooze now REQUIRES a uid, item 10), src/utils/dates.ts:17 (formatLocalDate takes a Date, NOT a string — review item 7).</read_first>
  <action>Add `snooze_until: string | null` to the screen's local `Header` type and seed it from the widened getContactHeader in the existing load(). Render a "Snooze reminders" block (container testID `contact-profile-snooze`, section-heading label in textSecondary) placed with the action buttons near Message/Log contact. Three preset Pressables in a row using the FilterChipRow filled-accent idiom with testIDs `contact-profile-snooze-3d` / `-1w` / `-1m` and labels "3 days" / "1 week" / "1 month". On press, call `snoozeContact(getExecutor(), { contactId, uid: newUid(), preset, now: localDateTime() })` for preset "3d"/"1w"/"1m", then re-run the unified load() so the header (and thus the status line) reconciles — reversible, non-destructive, NO confirmation dialog. When `header.snooze_until` is a FUTURE date, show a status Text (testID `contact-profile-snooze-status`) and a Clear Pressable (testID `contact-profile-snooze-clear`, label "Clear snooze") that calls `clearSnooze(getExecutor(), { contactId, uid: newUid(), now: localDateTime() })` then re-runs load(). REVIEW ITEM 7 — DO NOT call `formatLocalDate(snooze_until)`: `snooze_until` is a stored `YYYY-MM-DD` STRING and `formatLocalDate` takes a `Date` (dates.ts:17); coercing via `new Date("YYYY-MM-DD")` reintroduces the exact UTC evening off-by-one CLAUDE.md forbids (already fixed once in the plugin). Instead render the stored string DIRECTLY in the status copy ("Snoozed until {snooze_until}") — it is already the correct local date — or, if a prettier format is wanted, parse it with LOCAL components (`const [y,m,d] = snooze_until.split('-').map(Number); new Date(y, m-1, d)`) before formatting, never `new Date(snooze_until)`. For the FUTURE-date comparison, compare the stored `YYYY-MM-DD` string against today's `formatLocalDate(new Date())` (both bare local YYYY-MM-DD → lexicographic string compare is correct); do not construct a Date from the stored string. All colours via useTheme().colors.* (no literals — check:colors). Use only the 8/12/16/24 spacing values for the new block (UI-SPEC §Spacing). Do not add a danger token (snooze is reversible).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <manual>Pixel-UAT (phase gate): tap 3 days/1 week/1 month → status shows "Snoozed until {date}"; Clear removes it; the dashboard 'snoozed' segment/count reflects it.</manual>
  </verify>
  <acceptance_criteria>
    - Snooze block renders with the three preset chips, the active-snooze status, and the clear affordance (only when snoozed), all with the UI-SPEC testIDs + copy.
    - Presets write via snooze-dao; clear NULLs it (always leaving an unsnooze event, item 10); both reconcile via load(). No confirmation dialog, no danger token.
    - The snooze status renders the stored `YYYY-MM-DD` directly (or via a local-component parse) — NEVER `formatLocalDate(snooze_until)` or `new Date(snooze_until)` (item 7, no UTC off-by-one).
    - tsc + check:colors clean.
  </acceptance_criteria>
  <done>The profile exposes the in-app snooze presets writing snooze_until through the DAO, with a local-date-safe status + clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile UI → snooze_until | user-initiated snooze/clear writes cross into the contacts row via the mutexed DAO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-MUTEX | Tampering | profile snooze/clear | high | mitigate | Writes go only through snooze-dao (mutexed, changes===1); the UI never issues a raw UPDATE and never touches last_contact. |
| T-11-COLOR | (quality gate) | new profile block | low | mitigate | All colours via theme tokens; check:colors enforces (no status/danger tokens on the snooze block — it is state-neutral per UI-SPEC). |
</threat_model>

## Artifacts this plan produces
- **Change:** `src/screens/EditContactScreen.tsx` — mute toggle relabelled "Mute reminders" + helper (testID `edit-contact-reminders-off` unchanged).
- **Change:** `src/db/contact-read.ts` — `getContactHeader` returns `snooze_until` (additive).
- **Change:** `src/screens/ContactProfileScreen.tsx` — Snooze-reminders block; testIDs `contact-profile-snooze`, `contact-profile-snooze-3d`, `contact-profile-snooze-1w`, `contact-profile-snooze-1m`, `contact-profile-snooze-status`, `contact-profile-snooze-clear`.

<verification>
- tsc + check:colors + biome clean; contact-read.test.ts green.
- Pixel-UAT (phase gate): mute relabel + snooze presets/status/clear behave and reflect on the dashboard snoozed segment.
</verification>

<success_criteria>
The mute toggle is relabelled copy-only and the profile snooze presets write snooze_until through the DAO with a status + clear, feeding the suppression predicate and the dashboard snoozed segment.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-09-SUMMARY.md` when done.
</output>
