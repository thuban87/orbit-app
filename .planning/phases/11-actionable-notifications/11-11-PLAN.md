---
phase: 11-actionable-notifications
plan: 11
type: execute
wave: 4
depends_on: [11-02, 11-06, 11-10]
files_modified:
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [NOTIF-05]
user_setup: []

must_haves:
  truths:
    - "Settings shows a Notifications section: a master toggle, decay + birthday per-type toggles, a lock-screen visibility toggle (private by default), and tappable reminder-time / quiet-start / quiet-end controls."
    - "Flipping the master toggle on requests POST_NOTIFICATIONS at that value moment; a denial reverts master to off and shows the degraded note once, non-nagging."
    - "Sub-controls are disabled + dimmed when master is off; every control reads/writes app_settings via the DAO and a change triggers an immediate reconcile."
  artifacts:
    - src/screens/SettingsScreen.tsx (Notifications section)
  key_links:
    - "Reads/writes app-settings-dao (11-02); requests permission via 11-06; calls reconcileSchedule (11-10) after a settings change so the schedule updates without waiting for the next launch."
---

<objective>
Add the Settings → Notifications section: master + decay + birthday + lock-screen toggles, the delivery-hour and quiet-window time controls (native time picker), the value-moment permission request, and the denial-degraded note. Every control is backed by the app_settings DAO and triggers an immediate reconcile.

Purpose: the in-app control surface for NOTIF-05 — the master toggle IS the value-moment permission affordance, the per-type/lock-screen toggles gate scheduling, and the owner's user-tunable delivery hour + quiet window (the reversal) get their controls.
Output: the Notifications section in `SettingsScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-UI-SPEC.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/screens/SettingsScreen.tsx
@src/db/app-settings-dao.ts
@src/services/notifications/permission.ts
@src/services/notifications/notification-schedule.ts
@src/screens/EditContactScreen.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Notifications section — toggles, permission value moment, degraded note</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>src/screens/SettingsScreen.tsx:65-186 (the ScrollView content + styles.row idiom + useFocusEffect reload pattern), 11-UI-SPEC.md §1 + §4 + §Copywriting Contract (exact labels/helpers/testIDs + disabled/dimmed convention), src/screens/EditContactScreen.tsx:665-691 (the Switch contract: trackColor false=border/true=accent, thumbColor surfaceElevated), src/db/app-settings-dao.ts (getAppSettings/updateAppSettings), src/services/notifications/permission.ts (requestNotificationPermission), src/services/notifications/notification-schedule.ts (reconcileSchedule).</read_first>
  <action>Add a Notifications section container (testID `settings-notifications-section`) at the top of the SettingsScreen scroll content, above the existing rows, each control a `styles.row`. Load app_settings + the current permission status in the existing `useFocusEffect` reload (extend it) and hold them in local state (mirror the reloadProfile pattern; no new store). Render: master Switch (testID `settings-notifications-master`, label "Allow notifications", helper when off/undetermined per UI-SPEC); decay Switch (`settings-notifications-decay`, "Decay reminders" + helper); birthday Switch (`settings-notifications-birthday`, "Birthday alerts" + helper); lock-screen Switch (`settings-notifications-lockscreen`, "Show names on lock screen" + helper, default off = private). Use the EditContactScreen Switch token contract verbatim. Sub-controls (decay/birthday/lockscreen + the time rows in Task 2) are `disabled` + dimmed (surface bg, label textSecondary, no accent track) when master is off. Wiring: flipping master ON calls `requestNotificationPermission()` at that value moment — on granted, persist notifications_enabled=1 via updateAppSettings and call `ensureChannels()` is handled at app init (11-13), so here just persist + reconcile; on denied, keep master off and render the degraded note ONCE (testID `settings-notifications-degraded`, "Notifications are off" copy) — no re-prompt, no danger styling (orchestrator pick 6). Each per-type / lock-screen change persists via updateAppSettings then calls `reconcileSchedule(getExecutor())` (fire-and-forget, best-effort) so the schedule updates immediately. All colours via useTheme().colors.* (check:colors); every control carries accessibilityRole + accessibilityLabel + accessibilityState (disabled/checked) per the UI-SPEC register.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <manual>Pixel-UAT (phase gate): master on → system POST_NOTIFICATIONS dialog; deny → degraded note once + master off; grant → sub-controls enable; toggles gate scheduling.</manual>
  </verify>
  <acceptance_criteria>
    - The four toggles render with the UI-SPEC copy/testIDs; master triggers the permission request; denial reverts master + shows the degraded note once.
    - Sub-controls disabled+dimmed when master off; each change persists + reconciles.
    - tsc + check:colors clean.
  </acceptance_criteria>
  <done>The Notifications toggles + permission value moment + degraded state are wired to app_settings and reconcile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delivery-hour + quiet-window time controls (native time picker)</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>11-UI-SPEC.md §1 (the three time rows: Reminder time / Quiet-hours start / Quiet-hours end, values + testIDs + disabled-when-master-off), the Phase-4 datetimepicker usage (registered in app.config.ts; `mode="time"`), src/db/app-settings-dao.ts (delivery_hour / quiet_start_hour / quiet_end_hour + the 0–23 write validation).</read_first>
  <action>Add three tappable `Pressable` rows below the toggles: Reminder time (testID `settings-notifications-time`, shows the formatted delivery_hour e.g. "9:00 AM"), Quiet-hours start (`settings-notifications-quiet-start`, shows quiet_start_hour e.g. "9:00 PM"), Quiet-hours end (`settings-notifications-quiet-end`, shows quiet_end_hour e.g. "8:00 AM"), plus the quiet-hours helper line. Tapping a row opens `@react-native-community/datetimepicker` in `mode="time"`; on pick, extract the hour (0–23), persist the matching field via `updateAppSettings` (the DAO validates bounds), refresh local state, and call `reconcileSchedule(getExecutor())`. These are the owner's user-tunable controls (the reversal): the 9am / 9pm–8am seed values are the defaults, now editable. Disable + dim the rows when master is off. Format the display via a small local hour→"h:MM AM/PM" helper (token text, textPrimary label + accent value per the UI-SPEC "accent text on the tappable value"). accessibilityRole="button" with the label naming the current value (e.g. "Reminder time, 9:00 AM"). No new dep (datetimepicker already installed + registered); all colours via tokens.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <manual>Pixel-UAT (phase gate): tapping each row opens the time picker; changing the hour persists + reschedules; disabled when master off.</manual>
  </verify>
  <acceptance_criteria>
    - Three time rows render the formatted values with the UI-SPEC testIDs; picking a time persists via the validated DAO + reconciles.
    - Rows disabled+dimmed when master off; no new dependency.
    - tsc + check:colors clean.
  </acceptance_criteria>
  <done>The user-tunable delivery hour + quiet-window controls persist and re-drive the schedule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings UI input → app_settings | user hour/quiet/toggle values cross into persisted scheduling inputs |
| master toggle → OS permission request | flipping master triggers the system dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering / DoS | time controls | high | mitigate | The time picker yields an hour in 0–23; the DAO re-validates bounds on write and fire-instant clamps on read — a malformed value cannot corrupt scheduling. |
| T-11-PERM | Denial of service | permission value moment | low | mitigate | A denied permission reverts master and shows the degraded note once; the app never re-prompts (orchestrator pick 6). |
| T-11-LOCK | Information disclosure | lock-screen toggle | high | mitigate | The toggle defaults off (private); turning it on is an explicit opt-in mapped to the public channel at reconcile time (no channel mutation). |
</threat_model>

## Artifacts this plan produces
- **Change:** `src/screens/SettingsScreen.tsx` — Notifications section. testIDs: `settings-notifications-section`, `settings-notifications-master`, `settings-notifications-decay`, `settings-notifications-birthday`, `settings-notifications-lockscreen`, `settings-notifications-time`, `settings-notifications-quiet-start`, `settings-notifications-quiet-end`, `settings-notifications-degraded`.

<verification>
- tsc + check:colors + biome clean.
- Pixel-UAT (phase gate): permission dialog, grant/deny degrade, toggle gating, time-picker persistence + reschedule.
</verification>

<success_criteria>
Settings exposes the master + per-type + lock-screen toggles and the user-tunable delivery-hour / quiet-window controls, all backed by app_settings and triggering an immediate reconcile, with a value-moment permission request and a non-nagging degraded state.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-11-SUMMARY.md` when done.
</output>
