---
phase: 11-actionable-notifications
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/snooze-dao.ts
  - src/db/snooze-dao.test.ts
autonomous: true
requirements: [NOTIF-03]
user_setup: []

must_haves:
  truths:
    - "Snoozing a contact writes a local YYYY-MM-DD snooze_until comparable via bare date() AND an immutable events snooze row, both in one transaction."
    - "Clearing a snooze NULLs snooze_until AND ALWAYS writes an immutable unsnooze event in the same transaction; last_contact is never touched by either path (single-writer DATA-04 intact)."
    - "Both the in-app profile presets (3d/1wk/1mo) and the headless +1-week snooze go through this one DAO — one writer, one contract."
  artifacts:
    - src/db/snooze-dao.ts
    - src/db/snooze-dao.test.ts
  key_links:
    - "snooze_until written here is exactly the dashboard-read.ts:33 SNOOZE STORAGE CONTRACT the snoozed filter/count already expect (they go live once this ships)."
---

<!-- REVIEW-CYCLE-1 (11-REVIEWS.md) — item 10: clearSnooze now ALWAYS records an unsnooze event
     (uid is required, insert is unconditional) so the events audit trail — the only recovery
     mechanism — stays consistent. -->

<objective>
Create `snooze-dao` — the FIRST writer of `contacts.snooze_until` and the first producer of the reserved `snooze`/`unsnooze` events. It writes a local `YYYY-MM-DD` snooze date (comparable by bare `date()` per the dashboard-read storage contract) plus an immutable events row in ONE transaction, and clears via NULL. Both the profile snooze presets and the headless notification snooze call it.

Purpose: give both the in-app and headless snooze surfaces a single, mutexed, contract-correct writer that never bypasses the recency single-writer invariant.
Output: `src/db/snooze-dao.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/db/favourites-dao.ts
@src/db/events-dao.ts
@src/db/recency-dao.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snooze-dao — snooze (set) + clear, with the events row in one transaction</name>
  <files>src/db/snooze-dao.ts, src/db/snooze-dao.test.ts</files>
  <read_first>src/db/favourites-dao.ts:30-49 (single-column ?-bound UPDATE + changes===1 guard, never touches recency), src/db/events-dao.ts:38 + :62-81 (EventType reserves snooze/unsnooze; recordEventCore is the NON-mutexed composition primitive), src/db/recency-dao.ts:148-156 (non-reentrant mutex — never nest inWriteTransaction), src/db/dashboard-read.ts:29-36 + :144 (the snooze_until storage contract + bare date() comparison), 11-PATTERNS.md §snooze-dao.</read_first>
  <behavior>
    - `snoozeContact(exec, { contactId, uid, preset, now })` sets snooze_until to the local date `date('now','localtime', <modifier>)` where preset "3d"→"+3 days", "1w"→"+7 days", "1m"→"+1 month"; and inserts one immutable events row type "snooze".
    - The resulting snooze_until is a bare `YYYY-MM-DD` (or comparable) local string; comparing it via `date(snooze_until) <= date('now','localtime')` behaves correctly (a future snooze is "still snoozed").
    - `clearSnooze(exec, { contactId, uid, now })` sets snooze_until = NULL and ALWAYS inserts an "unsnooze" event (review item 10: the events log is the only recovery mechanism, so the audit trail must be consistent — `uid` is now REQUIRED, not optional).
    - Both paths assert changes===1 on the contacts UPDATE (bad id → throw → rollback).
    - Neither path writes or references last_contact.
    - The set path and the event insert commit in the SAME transaction (both or neither); the clear path's NULL update and unsnooze insert likewise commit atomically.
  </behavior>
  <action>Create `snooze-dao.ts` exporting `snoozeContact` and `clearSnooze`. `snoozeContact` runs inside ONE `inWriteTransaction`: first compute the target date with `SELECT date('now','localtime', ?)` binding the preset→modifier string (this keeps "+1 month" calendar-correct AND local, avoiding JS month-length math and avoiding `toISOString`), then `UPDATE contacts SET snooze_until = ?, modified_at = ? WHERE id = ?` with the computed date, asserting `changes===1` (favourites-dao guard), then compose `recordEventCore(exec, { contactId, uid, type: "snooze", occurredAt: now, now, detail: null })` — the NON-mutexed core, NEVER the mutexed `recordEvent` wrapper (nesting the non-reentrant mutex is a permanent hang). `clearSnooze` runs inside ONE `inWriteTransaction`: `UPDATE contacts SET snooze_until = NULL, modified_at = ? WHERE id = ?` (changes===1), then ALWAYS `recordEventCore(exec, { contactId, uid, type: "unsnooze", occurredAt: now, now, detail: null })` in the SAME transaction (review item 10 — not conditional). Make the `clearSnooze` input `uid` REQUIRED (callers already mint `newUid()`); the profile Clear affordance (11-09) and any future caller therefore always leave an audit row. Export a small `PRESET_MODIFIERS` map (`{ "3d": "+3 days", "1w": "+7 days", "1m": "+1 month" }`) as the top-of-file tunable so the preset lengths are single-edit. The headless +1-week snooze (11-07) calls `snoozeContact` with preset "1w". This DAO MUST NOT touch `last_contact` — recency-dao stays its sole writer (grep the finished file: it references no recency column). Write the test first (RED) against the node:sqlite executor + the shared mutex (mirror recency-dao.test.ts / snooze-dao analog): seed a contact, snooze "1w", assert snooze_until equals `date('now','localtime','+7 days')`, assert one events row type "snooze", assert last_contact unchanged; snooze "1m" then clear, assert NULL + exactly one unsnooze event written; assert a bad contactId throws and writes nothing (neither the UPDATE nor the event).</action>
  <verify>
    <automated>npx vitest run src/db/snooze-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - snoozeContact writes the correct local snooze_until per preset + one immutable snooze event in one transaction; clearSnooze NULLs it AND always writes one unsnooze event (item 10).
    - last_contact is never written/referenced (behavioral assert: unchanged across snooze/clear).
    - changes===1 loud-failure guard on a bad id; whole transaction rolls back.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>snooze-dao is the single mutexed writer of snooze_until + the snooze/unsnooze events, consumed by both the profile presets and the headless action; every clear always leaves an unsnooze audit row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless action / UI → snooze_until + events | writes cross into the shared contacts/events tables under the single-writer mutex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-MUTEX | Tampering | `snoozeContact` / `clearSnooze` writes | high | mitigate | All writes go through `inWriteTransaction` (the one shared non-reentrant mutex); the events row uses `recordEventCore` (non-mutexed core) so the mutex is never nested; last_contact is never written (recency-dao stays sole writer). |
| T-11-DATE | Tampering | snooze_until format | medium | mitigate | snooze_until is computed via `date('now','localtime', ?)` (bound modifier) so it is always a local bare date comparable via `date()`; never `toISOString()` (Pitfall 8). |
</threat_model>

## Artifacts this plan produces
- **File:** `src/db/snooze-dao.ts` — exports `snoozeContact`, `clearSnooze`, `PRESET_MODIFIERS` (and its input types). First writer of `contacts.snooze_until`; first producer of `snooze`/`unsnooze` events.
- **File:** `src/db/snooze-dao.test.ts`.

<verification>
- `npx vitest run src/db/snooze-dao.test.ts` green.
- Full suite + tsc + biome clean at wave merge (`npm test`).
</verification>

<success_criteria>
snooze_until has a single mutexed writer honoring the dashboard-read storage contract, writing an immutable snooze event in the same transaction and never touching last_contact.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-03-SUMMARY.md` when done.
</output>
