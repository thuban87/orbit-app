---
context: phase
phase: 22-app-shell-navigation
task: 6
total_tasks: 6
status: paused
last_updated: 2026-09-03T04:45:26.483Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. A resuming agent must acknowledge them before proceeding.

- [ ] CONSTRAINT: Device UAT is a required execution gate — build/install/UAT must happen before code review or verification claims. Use the documented desktop-build-to-Pixel workflow and record the result.
- [ ] CONSTRAINT: Preserve the Pixel's normal reverse port — inspect `adb reverse --list`; use `8081 → 8082` only during Orbit debug UAT and restore `8081 → 8081` immediately afterward.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Claiming UAT without an installed tested build | Phase 22 was initially summarized without the required Android build/UAT. | blocking | Before review or verification, record successful debug/release build, install, and concrete device observations. |
| Leaving the Pixel aimed at Orbit Metro | The Pixel normally uses port 8081 for another project; a stale mapping served the wrong bundle and showed missing native-module errors. | blocking | Audit mapping before UAT, use a narrow temporary 8082 window, stop Metro, and restore `8081 → 8081` before any pause. |
| Letting Expo rewrite configuration unnoticed | Starting local Metro rewrote `tsconfig.json`; it was restored before commit. | advisory | Check `git status` after Metro and revert only the known incidental diff with `apply_patch`. |
| Treating a hardware-Back test outside Orbit as valid | A Back event resumed Chrome in one attempt, invalidating the rapid Undo sequence. | advisory | Confirm `mCurrentFocus` is Orbit immediately before scripted device actions; avoid unverified Back sequences. |

<current_state>

All six Phase 22 implementation plans are complete. Debug and release Android builds succeeded, and the final release APK remains on the Windows build host at `C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\release\app-release.apk` (SHA-256 `62ea58ee10d5f466b1c14a91d466c8bb6ec9eb1761c60388dbf965b96c8b1b63`).

Device UAT covered the correct Orbit bundle, four tabs, shell/FAB placement, picker search/no-result, archived-via-search, snoozed marker/selectability, Quick Log commit plus prompt Undo, archived restore, and gesture/three-button clearance. UAT-only interaction rows were removed through the app. The Pixel is back to gesture navigation and `adb reverse tcp:8081 tcp:8081`; the temporary Orbit Metro session is stopped.

Fresh deep review of the UAT fixes completed in `22-REVIEW.md` and was committed as `ea70b3a`. It has no critical findings and one warning, WR-01. The user explicitly asked to pause here: do not fix it, rerun review, or start phase verification until the user says to continue.

</current_state>

<completed_work>

- Plans 22-01 through 22-06 completed and committed.
- First review's notification/widget/input warnings were fixed and regression-tested (`ddbf1ba`).
- Post-UAT source fix `a011470` preserves picker input through keyboard visibility and gives the tab shell a top safe area.
- Automated checks after that source fix passed: `npm test` (197 files / 1,876 tests), `npx tsc --noEmit --pretty false`, and `npm run check:colors`.
- Debug build installed on the Pixel; release build was rebuilt after the UAT source fix and retained on the Windows desktop build host path above.
- Device UAT found and validated fixes for the unsafe shell header and picker input unmounting. It also verified Quick Log Undo by observing the fresh interaction disappear and a tombstone be written.
- Fresh deep post-UAT review committed as `ea70b3a`.

</completed_work>

<remaining_work>

- Resolve or explicitly disposition `WR-01` in `22-REVIEW.md`: a component-wide `undoPending` can discard the second Quick Log Undo while an earlier deletion is in flight. If fixed, add an overlapping-Undo regression test, run focused/full checks, rebuild only if native-affecting changes require it, and perform a fresh review of the changed source.
- Run the Phase 22 verifier to create `22-VERIFICATION.md`; do not run it before the warning is addressed or explicitly accepted.
- Follow verifier findings, rerun relevant checks, then update roadmap/state and mark the phase complete only if verification passes.
- Human-sensory UAT not asserted: live TalkBack auditory traversal and visual widget/haptic observation. The device accessibility tree verified modal semantics and labels without altering the user's configured accessibility service.

</remaining_work>

<decisions_made>

- Device UAT is executor-owned whenever safely automatable; do not stop for routine test checkpoints.
- The Pixel's normal reverse mapping belongs to the other project. Orbit uses local Metro port 8082 only during a deliberate, temporary test window.
- Three-button navigation was enabled only long enough for the clearance UAT, then gesture mode was restored.
- Do not discard or manually modify the device database. UAT rows were deleted only through Orbit's own confirmed delete/Undo paths.
- The user requested a pause after this review report. The remaining warning/fixes and verifier are deliberately not started.

</decisions_made>

<blockers>

- No technical blocker. The current stop is an intentional user-requested approval gate.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state and exact next action.
2. This `.continue-here.md` — the device/UAT constraints and pause boundary.
3. `.planning/phases/22-app-shell-navigation/22-REVIEW.md` — WR-01 details.
4. `.planning/phases/22-app-shell-navigation/22-REVIEW-FIX.md` — prior review resolution.
5. `.planning/phases/22-app-shell-navigation/22-01-SUMMARY.md` through `22-06-SUMMARY.md` — implementation evidence.
6. `docs/runbooks/desktop-build-pipeline.md` — required Windows-build/Pixel workflow if a rebuild is needed.

## Infrastructure State

- Branch: `main`; working tree clean at pause.
- Pixel serial: `1A071FDEE002BU`.
- Pixel navigation: gesture mode restored (`navigation_mode=2`).
- Pixel reverse mapping: `UsbFfs tcp:8081 tcp:8081` restored.
- No temporary Orbit Metro, Gradle, or async jobs are running. Existing `orbit` and `questboard` tmux sessions are unrelated and must not be stopped.
- Final release APK: `C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\release\app-release.apk`, SHA-256 `62ea58ee10d5f466b1c14a91d466c8bb6ec9eb1761c60388dbf965b96c8b1b63`.

<next_action>

Wait for the user to say go ahead. Then start with WR-01 in `22-REVIEW.md`; after its outcome is tested and reviewed, dispatch the Phase 22 verifier.

</next_action>
