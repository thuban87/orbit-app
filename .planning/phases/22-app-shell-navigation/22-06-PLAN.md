---
phase: 22-app-shell-navigation
plan: 06
type: execute
wave: 5
depends_on: [22-02, 22-05]
files_modified:
  - src/logic/contact-picker-order.ts
  - src/logic/contact-picker-order.test.ts
  - src/db/picker-read.ts
  - src/components/ContactPicker.tsx
  - src/components/Snackbar.tsx
  - src/stores/snackbar-store.ts
  - src/stores/shell-refresh-store.ts
  - src/components/UniversalFab.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/OrreryScreen.tsx
  - src/screens/ContactProfileScreen.tsx
  - App.tsx
autonomous: true
requirements: [SHELL-09, SHELL-10, SHELL-11, SHELL-14]
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-10 (ADR-075-compliant): the shared modal picker lists contacts ordered by favourite MEMBERSHIP band (a favourite = `favourite_rank IS NOT NULL`, surfaced first as a group) -> recently relevant (interaction recency, `last_contact DESC`) -> alphabetical (`name COLLATE NOCASE`). The order NEVER uses `favourite_rank ASC` — rank does not decide user-visible order (ADR-075's named risk in a NEW read path); within the favourites band, recency then alpha decides order. Deterministic, stable SQL ORDER BY (ordering edge: contacts equal on membership+recency fall back to alpha)."
    - "SHELL-10: archived contacts are HIDDEN from the default list and surface ONLY via explicit search, each with an Archived badge; snoozed contacts stay SELECTABLE with a visible Snoozed marker (adjacency edge: archived and non-archived are distinct sets merged only under a search term; snoozed rows are marked but not excluded)."
    - "SHELL-10: a zero-match search shows \"No matching contacts\"; a user with zero contacts shows \"No contacts yet\" + a pointer to Add Contact (empty / zero-one-many edges: zero -> empty copy; one and many render with consistent row spacing, no singular/plural branch)."
    - "SHELL-10: the picker opens immediately with a calm loading state; ordering reads are on-device SQLite with no blocking network on the read path (local-first); an unexpected query exception renders the empty state rather than a broken sheet (loading/error UI-considerations)."
    - "SHELL-10: rows virtualize via FlatList and never clip; the row NAME is always present and the secondary line (recency / Snoozed marker / Archived badge) is optional and simply omitted when absent — no blank-row artifact (populated/partial/overflow UI-considerations)."
    - "SHELL-11: Quick Log writes immediately at the current time once the target is known via `recordTouchpoint()` (inside inWriteTransaction; rejects a future occurred_at before opening the txn) and shows the success snackbar (\"Logged\" + Undo + SUCCESS haptic) ONLY in the resolved `.then(({interactionId}))`, and the error snackbar (\"Couldn't log\" + Retry, NO haptic) ONLY in `.catch()` — never optimistically (loading UI-consideration: there is no in-flight snackbar). The failure path fires NO haptic (finding G): dossier §M's taxonomy is Light=FAB-open, Success=successful fast action, Warning=DESTRUCTIVE confirmation, ordinary taps=none — a write error is none of these, and there is no ratified error haptic, so the earlier 'warning haptic on failure' is corrected to no haptic."
    - { statement: "SHELL-11 write contract: the Quick Log call mirrors the canonical one-tap analog `doLogContact` (ContactProfileScreen.tsx:339-349) EXACTLY — it mints `uid: newUid()` (RecordTouchpointInput.uid is REQUIRED with no DAO default, recency-dao.ts:59-61) and passes `direction: \"outbound\"`, `channel: \"unspecified\"`, `connected: 1`, `quality: null`, `source: \"manual\"`, with a single captured `stamp = localDateTime()` used for BOTH `occurredAt` and `now` (never toISOString — DATA-05 local wall-clock).", verification: explicit }
    - { statement: "SHELL-11 freshness (finding B — CONCRETE mechanism, not 'refresh the focused surface' as prose): after a committed Quick Log AND after Undo, the shell (1) calls `notifyWidgetDataChanged()` for the Android widget AND (2) calls `bumpShellRefresh()` on a small in-memory Zustand `shell-refresh-store` (a monotonic revision). Browse surfaces subscribe via a `useShellRefresh(reload)` hook that re-invokes their existing reload when the revision changes: HomeScreen (its `reload()`), OrreryScreen (its data re-query — verified at execution), and ContactProfileScreen (its `load()`, since the shell FAB is visible on a Profile and can Quick Log the viewed contact). This is REQUIRED because the shell-mounted Quick Log does NOT sit in any screen's `useFocusEffect`, `deleteTouchpoint` (Undo's path) does NOT call `bumpDataRevisionCore` (recency-dao.ts:313-345, unlike recordTouchpoint at :240), and HomeScreen deliberately does NOT subscribe to the connection-scoped SQLite change notification (HomeScreen.tsx:9-15 / DASH-07) — so without this explicit in-process shell event, status/recency/widget stay stale until a later focus. NOTE: `shell-refresh-store` is an app-level shell event fired in-process, NOT the connection-scoped SQLite notification DASH-07 deliberately avoids — this does NOT reverse DASH-07.", verification: explicit }
    - "SHELL-11: Undo silently deletes exactly the just-written interaction via the EXISTING `deleteTouchpoint(exec, {interactionId, contactId, now})` (recency-dao.ts:313 — delete-by-both-keys + tombstone + recomputeLastContact in one inWriteTransaction) and recomputes recency; no second snackbar (idempotency edge: Undo targets one interactionId so a repeated Quick Log + Undo affects only its own write; concurrency edge: each Quick Log is its own committed transaction)."
    - "SHELL-09: Quick Log from a Profile preselects that contact and writes immediately; from a global context it opens the shared picker first, then writes (the Plan-05 quick-log/pick-then seam completed here)."
    - "SHELL-14: the picker sheet sets `accessibilityViewIsModal`, rows have >=44px targets + a color-free accessible description (name + markers), and the snackbar Undo/Retry actions carry semantic labels + >=44px targets; haptics are semantic only — a SUCCESS haptic on commit and NO haptic on failure (dossier §M reserves the warning haptic for destructive confirmation, not a write error — finding G)."
    - { statement: "contact-picker long-text backstop: long contact display names wrap or truncate gracefully within the row without breaking layout — on-device visual on the Pixel.", verification: backstop }
  artifacts:
    - "src/logic/contact-picker-order.ts (+ .test.ts) — pure ordering + search-filter (archived-via-search) + marker (snoozed/archived) logic, node-tested"
    - "src/db/picker-read.ts — thin static-SQL read (favourites -> last_contact recency -> alpha; snooze_until + archived_at surfaced)"
    - "src/components/ContactPicker.tsx — reusable modal picker (Modal + search + FlatList + a11y-modal)"
    - "src/components/Snackbar.tsx + src/stores/snackbar-store.ts — commit-truthful Quick Log feedback host (Undo/Retry)"
    - "src/stores/shell-refresh-store.ts — in-memory Zustand monotonic-revision store + `useShellRefresh(reload)` hook (finding B); bumped after a committed Quick Log AND after Undo; Home/Orrery/Profile subscribe to re-query"
  key_links:
    - "UniversalFab Quick Log / contact-specific-global -> ContactPicker (selection) -> recordTouchpoint -> Snackbar"
    - "Snackbar Undo -> existing deleteTouchpoint (recency-dao.ts:313) — REUSED, not a new DAO"
    - "ContactPicker open-state -> shell-transient-store (Plan 02) so retap/Back dismiss it first"
    - "picker-read (on-device SQLite) -> contact-picker-order pure functions -> ContactPicker rows"
    - "Quick Log commit + Undo -> shell-refresh-store.bumpShellRefresh() -> useShellRefresh(reload) on HomeScreen/OrreryScreen/ContactProfileScreen (finding B — the browse-surface freshness mechanism)"
  prohibitions:
    - "Quick Log must NEVER show the success snackbar before `recordTouchpoint()`'s transaction resolves — no optimistic success (SHELL-11: never success unless the write committed). Success is shown only in `.then(({interactionId}))`."
    - "Undo must NOT invent a new lightweight delete path — it routes through the existing guarded `deleteTouchpoint` (canonical writer, single-writer invariant intact)."
    - "The picker read must NOT put any network call on the read path (local-first) and must NOT surface archived contacts in the default (non-search) list."
    - "The picker read must NOT ORDER BY `favourite_rank ASC` (ADR-075): favourites surface as a MEMBERSHIP band (`favourite_rank IS NOT NULL`), then Default ordering (recency) then alpha. Copying `capture-read.ts:65-66`'s `favourite_rank ASC` idiom verbatim would reintroduce the retired user-facing rank order in a new read path — the exact ADR-075 named risk (see 22-01-PLAN.md ADR-075 compliance block). Do NOT `favourite_rank ASC`."
    - "The picker must NOT re-derive relationship status or convey it via a status hue — it shows identity + Snoozed/Archived markers only."
  notes:
    - "interactions-table writer audit (CLAUDE.md 'read every writer of a shared table' — verified on disk, carry into 22-06-SUMMARY). Quick Log uses the SINGLE-WRITER path only: `recordTouchpoint` (INSERT, src/db/recency-dao.ts:195) on commit and `deleteTouchpoint` (DELETE, recency-dao.ts:336; the edit UPDATE lives at recency-dao.ts:281) on Undo — the canonical guarded writers this plan REUSES and adds nothing to. The OTHER production writers of `interactions` are DISTINCT and are NOT Quick Log paths: `src/backup/restore-apply.ts:170` (`upsertChildren`) is the sanctioned BULK writer — a whole-backup restore `INSERT … ON CONFLICT(uid) DO UPDATE`, legitimately upserting many rows, NOT a per-tap log — and `src/db/purge-dao.ts:78` is the hard-purge deleter (ADR-018). (Note: this path is `src/backup/restore-apply.ts`, not `src/services/backup/`.) This plan introduces NO new writer of `interactions`; the single-writer invariant for the Quick Log path stays intact and the bulk-restore/purge writers are untouched and unaffected by the Quick Log wiring."
---

<objective>
Complete the shell's capture loop: the one reusable modal contact picker (Favorites -> recency -> alphabetical, archived-via-search-only with a badge, snoozed selectable-and-marked) and the commit-truthful Quick Log snackbar (success + Undo only on a committed write, error + Retry otherwise), wiring the FAB's Quick Log and global contact-specific actions through them.

Purpose: SHELL-10 (picker) and SHELL-11 (commit-truthful Quick Log) are the payoff of the shell — the fastest honest path from intent to a logged interaction. Undo reuses the EXISTING `deleteTouchpoint` (an on-disk correction to RESEARCH A5, which claimed no delete path exists).
Output: contact-picker-order (pure + tested) + picker-read, ContactPicker, Snackbar + host, and the Quick Log write wiring.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification — full phase list in 22-01-PLAN.md phase-anchor):
- `src/logic/contact-picker-order.ts` (+ `.test.ts`) — pure membership-band ordering + search-filter (archived-via-search) + snoozed/archived markers (favourite = boolean membership, NEVER rank — ADR-075).
- `src/db/picker-read.ts` — thin static-SQL read; ORDER BY membership band -> `last_contact DESC` -> alpha (no `favourite_rank ASC`).
- `src/components/ContactPicker.tsx` — reusable modal picker.
- `src/components/Snackbar.tsx` + `src/stores/snackbar-store.ts` — commit-truthful Quick Log feedback host (Undo/Retry).
- `src/stores/shell-refresh-store.ts` — in-memory Zustand monotonic-revision store + `useShellRefresh(onRefresh)` hook (finding B — the concrete post-Quick-Log browse-surface refresh mechanism).
This plan completes the Quick Log seam in `UniversalFab.tsx` (Plan 05), mounts the Snackbar host in App.tsx, and subscribes HomeScreen/OrreryScreen/ContactProfileScreen to `useShellRefresh`; Undo REUSES the existing `deleteTouchpoint` (recency-dao.ts:313) — no new delete DAO. `depends_on: [22-02, 22-05]` — 22-05 (UniversalFab + universal-fab-logic it completes) and 22-02 (shell-transient-store the picker registers on) are both direct imports; wave 5 (after 22-05 in wave 4).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-app-shell-navigation/22-01-PLAN.md
@.planning/phases/22-app-shell-navigation/22-05-PLAN.md
@.planning/phases/22-app-shell-navigation/22-UI-SPEC.md
@.planning/phases/22-app-shell-navigation/22-PATTERNS.md
@src/db/recency-dao.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Picker ordering + search-filter + markers (pure) and the picker read (SHELL-10)</name>
  <files>src/logic/contact-picker-order.ts, src/logic/contact-picker-order.test.ts, src/db/picker-read.ts</files>
  <read_first>
    - src/db/capture-read.ts (the ordering idiom: favourites -> recency -> alpha as static SQL ORDER BY, WHERE archived_at IS NULL, async getAllAsync, no txn/mutex/migration, no interpolation) + src/db/capture-read.test.ts (the ordering test idiom)
    - src/db/contact-read.ts lines 100-125 (existing row shapes incl. snooze_until — confirm the column names last_contact / favourite_rank / snooze_until / archived_at on disk)
    - src/db/migrations/001-initial.ts (confirm snooze_until + last_contact + favourite_rank + archived_at columns exist — they do)
    - 22-UI-SPEC.md UI Considerations (contact-picker rows) + docs/dossier section G (ordering favourites -> recent -> alpha; archived via explicit search with Archived badge; snoozed selectable + marked)
    - 22-PATTERNS.md "contact-picker-order.ts" note (name-distinct from the import-flow contact-picker-* logic; recency source is interaction recency not fuel.created_at)
  </read_first>
  <reversibility rating="reversible">Pure logic + a thin read; no schema, no migration (D-03).</reversibility>
  <action>
    Create `src/db/picker-read.ts`: a thin static-SQL read `listPickerContacts(exec, { includeArchived: boolean })` mirroring `capture-read.ts` POSTURE (single async `getAllAsync`, no transaction, no mutex, NO migration, static string, no interpolation) but NOT its ORDER BY. Select id, name, photo, modified_at, favourite_rank, last_contact, snooze_until, archived_at, ORDER BY `(favourite_rank IS NULL), (last_contact IS NULL), last_contact DESC, name COLLATE NOCASE ASC` — favourite MEMBERSHIP band (the `(favourite_rank IS NULL)` 0/1 key groups favourites first) -> interaction recency (`last_contact DESC`, NOT `fuel.created_at`) -> alpha. **Do NOT include a `favourite_rank ASC` term** (ADR-075: rank must never decide user-visible order; copying capture-read.ts:65-66 verbatim would reintroduce it in a new read path). WHERE `archived_at IS NULL` when `includeArchived` is false (default list); when true (search active) include archived rows too. Name it distinctly from the existing import-flow `contact-picker-*` modules (Wave-0 note).

    Create `src/logic/contact-picker-order.ts` (pure, react-native/db-free, node-testable): treat favourite as BOOLEAN membership (`favourite_rank IS NOT NULL`), never a rank. `matchPickerRow(row, term)` (case-insensitive name match; empty/whitespace term matches nothing special — the default list is shown), `pickerRowMarkers(row)` returning `{ snoozed: boolean; archived: boolean }` from `snooze_until`/`archived_at` (snoozed = snooze_until present and in the future; archived = archived_at present), and `filterPicker(rows, term)` composing them so archived rows appear ONLY when a non-empty term matches (SHELL-10). Add `contact-picker-order.test.ts`: ordering by favourite-membership band -> recency -> alpha; a test that two favourites are ordered by RECENCY not rank (a favourite with a LOWER favourite_rank but OLDER last_contact sorts AFTER a favourite with a HIGHER rank but NEWER last_contact — proving rank does not decide order, ADR-075); a snoozed row is marked but retained; an archived row is excluded with no term and included (badge) with a matching term; zero-match term yields empty.
  </action>
  <verify>
    <automated>npx vitest run src/logic/contact-picker-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/logic/contact-picker-order.test.ts` green: order by favourite-membership band -> recency(last_contact) -> alpha; the rank-does-not-decide-order test passes (two favourites ordered by recency, not favourite_rank); snoozed marked+retained; archived excluded without a term, included (badge) with a matching term; zero-match -> empty.
    - Source assertion (ADR-075): `picker-read.ts` ORDER BY contains `(favourite_rank IS NULL)` (membership band) and `last_contact DESC` and `name COLLATE NOCASE`, and the rank-ASC ordering construct is absent from non-comment lines (`grep -vE '^[[:space:]]*(//|\*|/\*)' src/db/picker-read.ts | grep -c 'favourite_rank ASC'` returns 0 — comment-filtered so an explanatory comment cannot self-invalidate the gate); the read is a single `getAllAsync` with no interpolation and no `inWriteTransaction`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The picker read orders favourites -> interaction-recency -> alpha with conditional archived inclusion, and the pure filter/marker logic (archived-via-search, snoozed-marked) is node-tested green.</done>
</task>

<task type="auto">
  <name>Task 2: ContactPicker modal component (search, virtualized list, markers, a11y-modal) (SHELL-10/14)</name>
  <files>src/components/ContactPicker.tsx</files>
  <read_first>
    - src/screens/LegacyContactPickerScreen.tsx (the list/filter/search body idiom: query useState, filteredRows useMemo, TextInput + FlatList keyExtractor/renderItem)
    - src/components/OverflowMenu.tsx (the Modal transparent + scrim + accessibilityLabel="Dismiss actions" + accessibilityViewIsModal sheet idiom to reuse — do NOT add @gorhom/bottom-sheet)
    - src/logic/contact-picker-order.ts + src/db/picker-read.ts (Task 1)
    - src/stores/shell-transient-store.ts (Plan 02 — register open-state so retap/Back dismiss the picker first)
    - 22-UI-SPEC.md Copywriting (empty-state copy) + UI Considerations (picker loading/error/populated/partial/overflow/zero-one-many)
  </read_first>
  <reversibility rating="reversible">A new presentational modal component.</reversibility>
  <action>
    Create `src/components/ContactPicker.tsx`: a reusable modal picker (built-in `Modal transparent animationType="fade"` + `absoluteFill` Pressable scrim over a `surfaceElevated` sheet, `accessibilityViewIsModal` on the open sheet, scrim `accessibilityLabel="Dismiss picker"`). A live search `TextInput` (`accessibilityLabel`, placeholder) drives `includeArchived = term.trim().length > 0` for the read and `filterPicker(rows, term)` for display. Rows via `FlatList` (virtualized) with `keyExtractor` on contact id; each row: Avatar + name (always present) + an optional secondary line rendering the Snoozed marker and/or Archived badge from `pickerRowMarkers` (omitted when absent — no blank row); snoozed rows stay selectable; `accessibilityRole="button"`, >=44px, a color-free accessible label (name + markers). Empty states: zero-match term -> "No matching contacts"; zero contacts total -> "No contacts yet" + a pointer to Add Contact; an unexpected read exception -> the empty state (never a broken sheet). Opens immediately with a calm loading state; reads are on-device SQLite only (no network on the read path). Register `openTransient("contact-picker", onDismiss)` on show — passing a REAL close callback (`onDismiss = () => setOpen(false)`, or the prop that hides the sheet) that matches Plan 02's `openTransient(id, dismiss)` contract so `dismissTop()` (system Back / active-tab retap) actually closes the Modal, NOT just clears an id from the store (finding A — an id alone cannot close a component whose visibility is local React state). Call `closeTransient("contact-picker")` when the sheet closes itself (selection or scrim tap). Selection returns the chosen contactId to the caller via an `onSelect(contactId)` prop (the FAB/Quick-Log wiring in Task 3 consumes it). All colours via `useTheme().colors.*` (no status hue, no literal).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass (no colour literal, no status hue in ContactPicker).
    - Source assertion: ContactPicker uses `Modal` + `accessibilityViewIsModal` + `FlatList`; `grep -c "No matching contacts" src/components/ContactPicker.tsx` returns 1 and `grep -c "No contacts yet" src/components/ContactPicker.tsx` returns 1; `closeTransient` is called.
    - Source assertion (finding A): `openTransient` is called with TWO arguments — the id AND a dismiss callback (`grep -E 'openTransient\("contact-picker",' src/components/ContactPicker.tsx` matches, i.e. the callback arg is present, not a bare `openTransient("contact-picker")`), so `dismissTop()` invokes a real close and Back/retap dismiss the Modal.
    - Source assertion: the read toggles `includeArchived` on a non-empty term (grep the term-length gate); no `@gorhom/bottom-sheet` import.
    - On-device (Pixel, end-of-phase UAT): the picker opens as a sheet; typing filters; archived contacts appear only when searched (with a badge); snoozed contacts show a marker and remain selectable; zero-match shows "No matching contacts"; TalkBack treats the sheet as modal.
  </acceptance_criteria>
  <done>The reusable modal ContactPicker lists favourites-first with search, archived-via-search + snoozed markers, virtualized rows, empty states, and a11y-modal focus; tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit-truthful Quick Log — Snackbar host + write wiring + Undo/Retry + shell-refresh freshness (SHELL-11/09)</name>
  <files>src/components/Snackbar.tsx, src/stores/snackbar-store.ts, src/stores/shell-refresh-store.ts, src/components/UniversalFab.tsx, src/screens/HomeScreen.tsx, src/screens/OrreryScreen.tsx, src/screens/ContactProfileScreen.tsx, App.tsx</files>
  <read_first>
    - src/db/recency-dao.ts lines 50-70 (RecordTouchpointInput — `uid` is REQUIRED, no DAO default) + 210-346 (recordTouchpoint returns {interactionId} inside inWriteTransaction, rejects a future occurred_at, and calls bumpDataRevisionCore at :240; deleteTouchpoint at :313-345 is the EXISTING Undo path — delete-by-both-keys + tombstone + recomputeLastContact but does NOT call bumpDataRevisionCore)
    - src/screens/ContactProfileScreen.tsx lines 330-360 (`doLogContact` — the CANONICAL one-tap write to mirror EXACTLY: `uid: newUid()`, `occurredAt/now = stamp = localDateTime()`, `channel: "unspecified"`, `direction: "outbound"`, `connected: 1`, `quality: null`, `source: "manual"`, then `await load()` + `notifyWidgetDataChanged()`) + the `newUid` import + the screen's `load()` reload (the reload `useShellRefresh` re-invokes)
    - src/screens/HomeScreen.tsx lines 1-15 (the FRESHNESS design comment — it re-queries on focus/AppState/pull and DELIBERATELY does NOT subscribe to the connection-scoped SQLite notification; the shell-refresh tick is an app-level in-process event, NOT that notification, so it does not reverse DASH-07) + its `reload()`/`useFocusEffect` (~:178-190, the reload `useShellRefresh` re-invokes)
    - src/screens/OrreryScreen.tsx (VERIFY its data source + re-query path on disk; wire `useShellRefresh` to whatever re-reads the recency-derived orbit positions — do NOT drive the Skia loop from React state; if Orrery already re-derives from a live store, note that instead of adding a subscription)
    - src/screens/CaptureScreen.tsx lines 840-895 (the capture-confirmation-toast surface — the closest transient-confirmation idiom; setState+setTimeout auto-dismiss, themed tokens)
    - src/components/universal-fab-logic.ts (the quick-log / pick-then intents from Plan 05 — the seam this task completes)
    - src/components/ContactPicker.tsx (Task 2 — invoked for the global/pick-then Quick Log path)
    - src/stores/dashboard-prefs-store.ts (the plain-Zustand store idiom to mirror for `shell-refresh-store`)
    - docs/dossier/milestone-2/phase-01-app-shell-navigation-dossier-amended-group-events.md §M (haptic taxonomy: Light=FAB-open, Success=successful fast action, Warning=DESTRUCTIVE confirmation, taps=none — a write error gets NO haptic, finding G)
    - 22-UI-SPEC.md Copywriting (Quick Log success "Logged"+Undo; error "Couldn't log"+Retry; Undo silently removes, no second snackbar) + 22-RESEARCH.md "Quick Log commit-truthful write" (key off the resolved promise; NO optimistic success)
  </read_first>
  <reversibility rating="reversible">New host + wiring; Undo reuses an existing DAO.</reversibility>
  <action>
    Create `src/stores/snackbar-store.ts` (small store: `show({kind, label, action})`, current snackbar, auto-dismiss timer) and `src/components/Snackbar.tsx` (a themed transient surface mirroring the CaptureScreen toast idiom — `surface`/`border`/`textPrimary`, Undo/Retry action in `accent`, >=44px, `accessibilityRole="button"` labels). Mount one `<Snackbar/>` host at shell level (App.tsx, sibling of the FAB) so it shows over any browse surface. Optional Reanimated slide-in must use the shared-value idiom (never per-frame setState); a setState+setTimeout surface is acceptable for a transient (CaptureScreen precedent).

    **Create the shell-refresh freshness mechanism (finding B — CONCRETE, not "refresh the focused surface" as prose):** `src/stores/shell-refresh-store.ts` — a plain Zustand store (mirror `dashboard-prefs-store` idiom, NOT persisted) with a monotonic `revision: number` and `bumpShellRefresh()` that increments it, plus a `useShellRefresh(onRefresh: () => void)` hook that subscribes to `revision` and calls `onRefresh` whenever it changes (skipping the initial mount value). This is an in-process app-level event, explicitly NOT the connection-scoped SQLite change notification HomeScreen deliberately avoids (HomeScreen.tsx:9-15 / DASH-07) — so it does not reverse DASH-07. Wire `useShellRefresh` into the browse surfaces so a shell Quick Log/Undo re-queries them without a manual refocus: HomeScreen (`useShellRefresh(reload)` alongside its existing focus/AppState/pull paths), ContactProfileScreen (`useShellRefresh(load)` — the FAB is visible on a Profile and can Quick Log the viewed contact), and OrreryScreen (wire it to whatever re-reads the recency-derived orbit data — verify Orrery's data path on disk; if Orrery already re-derives from a live store, note that and skip). Do NOT drive the Skia loop from React state.

    Complete the Plan-05 Quick Log seam in UniversalFab: on the Quick Log action, resolve the target — Profile origin -> preselected contactId (via `getFocusedContactContext`); global -> open `ContactPicker`, use its `onSelect` contactId. Then call `recordTouchpoint` mirroring the canonical `doLogContact` (ContactProfileScreen.tsx:339-349) EXACTLY: capture one `stamp = localDateTime()` and pass `{ contactId, uid: newUid(), occurredAt: stamp, now: stamp, channel: "unspecified", direction: "outbound", connected: 1, quality: null, source: "manual" }` (import `newUid`; `uid` is REQUIRED with no DAO default — omitting it fails at runtime/tsc). In `.then(({ interactionId }) => ...)` show the success snackbar "Logged" + Undo, fire the SUCCESS haptic, AND call `notifyWidgetDataChanged()` + `bumpShellRefresh()` (so Home/Orrery/Profile re-query); in `.catch(() => ...)` show the error snackbar "Couldn't log" + Retry with NO haptic (finding G — dossier §M reserves the warning haptic for destructive confirmation, not an ordinary write error, and there is no ratified error haptic; if the owner later ratifies a distinct error haptic that is a taxonomy addition the owner owns). NEVER show success before the promise resolves. GUARD against double-fire: gate the Quick Log dispatch behind an in-flight latch (a `quickLogPending` ref/flag set before `recordTouchpoint` and cleared in BOTH terminal paths — `.then` AND `.catch`), mirroring the canonical `logging` guard at `ContactProfileScreen.tsx:332-338`, so a fast double-tap cannot commit two `interactions` rows; apply the SAME single-flight guard to Undo (and Retry) so a double-tapped Undo cannot double-dispatch `deleteTouchpoint`. Undo AWAITS the EXISTING `deleteTouchpoint(getExecutor(), { interactionId, contactId, now: localDateTime() })` — which REJECTS on an absent/mismatched target (recency-dao.ts:326/:340) — and ONLY on resolve calls `notifyWidgetDataChanged()` + `bumpShellRefresh()` (deleteTouchpoint does NOT bump the data revision, so without this the widget/status/browse surfaces stay stale after Undo) and silently dismisses (no second snackbar); on REJECT it does NOT dismiss-as-success — it surfaces a retryable error snackbar ("Couldn't undo" + Retry) so a failed Undo never implies success. Retry re-invokes the same recordTouchpoint path. Also complete the global pick-then wiring for the other contact-specific actions (Log Contact/Update Contact/Memory) so a global invocation opens the picker then routes to the placeholder (nested DashboardTab target, per Plan 05) with the picked contactId (SHELL-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors &amp;&amp; npx vitest run src/db/recency-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass; `npx vitest run src/db/recency-dao.test.ts` green (recordTouchpoint + deleteTouchpoint behavior unchanged — Undo reuses the existing DAO).
    - Source assertion: in UniversalFab the success snackbar is shown ONLY inside a `.then(` on recordTouchpoint (grep shows no showSnackbar success call outside the resolved branch); the recordTouchpoint call passes `uid: newUid()` and `direction: "outbound"` (grep confirms — mirrors doLogContact); Undo calls `deleteTouchpoint` (grep confirms; no new delete SQL is introduced).
    - Source assertion: `notifyWidgetDataChanged` is called after BOTH the commit `.then` and the Undo path (`grep -c "notifyWidgetDataChanged" src/components/UniversalFab.tsx` returns ≥2).
    - Source assertion (finding B): `grep -c "bumpShellRefresh" src/components/UniversalFab.tsx` returns ≥2 (bumped after commit AND after Undo); `shell-refresh-store.ts` exports `bumpShellRefresh` + `useShellRefresh` and is a plain (non-persisted) Zustand store (`grep -c "persist(" src/stores/shell-refresh-store.ts` returns 0); `grep -l "useShellRefresh" src/screens/HomeScreen.tsx src/screens/ContactProfileScreen.tsx` lists both (OrreryScreen either uses it or the SUMMARY documents why its existing live data path suffices).
    - Source assertion (finding G): no warning/notification haptic fires on the Quick Log failure path — the `.catch` branch shows the error snackbar but calls no haptic (`grep -nE "notificationAsync|Warning|impactAsync" src/components/UniversalFab.tsx` shows no haptic inside the error branch; a SUCCESS haptic only in the `.then`).
    - Source assertion: `grep -c "Logged" src/components/Snackbar.tsx src/components/UniversalFab.tsx` >=1 and `grep -c "Couldn't log" ...` >=1; the Snackbar host is mounted once in App.tsx.
    - On-device (Pixel, end-of-phase UAT): Quick Log from a Profile writes immediately and shows "Logged" + Undo with a SUCCESS haptic, and the contact's status/recency (and widget) update without a manual refocus; staying on the Dashboard/Orrery after a global Quick Log, the affected contact's status/recency updates without a manual refocus (shell-refresh tick); Undo removes the interaction (the row disappears, recency recomputed, widget + browse surfaces refreshed); a forced failure shows "Couldn't log" + Retry with NO haptic; success never appears before the write commits; global Quick Log opens the picker first.
  </acceptance_criteria>
  <done>Quick Log writes through recordTouchpoint and reports truthfully — success + Undo only on a committed write (Undo via the existing deleteTouchpoint), error + Retry (no haptic) otherwise — with the Snackbar host mounted at shell level, the picker wired for global targets, and Home/Orrery/Profile re-queried via the shell-refresh tick after commit and Undo; tsc/check:colors/recency-dao tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quick Log UI → SQLite write | The UI must reflect only committed writes; a "success" that outran the transaction is a truthfulness failure |
| picker read → screen | On-device SQLite read; no network may enter the read path (local-first) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-15 | Repudiation/Integrity | Quick Log success signal | high | mitigate | Success is keyed strictly to the resolved `recordTouchpoint` transaction ({interactionId}); no optimistic snackbar — the app never claims a write that did not commit (SHELL-11). |
| T-22-16 | Integrity | Undo delete | medium | mitigate | Undo routes through the existing `deleteTouchpoint` (delete-by-both-keys + tombstone + recomputeLastContact in one txn) — the canonical single-writer path; recency stays correct. |
| T-22-17 | Information disclosure | picker read | low | mitigate | Read is local-only static SQL (no network, no interpolation); archived contacts are excluded from the default list and surface only under an explicit search term. |
</threat_model>

<verification>
- `npx vitest run src/logic/contact-picker-order.test.ts src/db/recency-dao.test.ts` green; `npx tsc --noEmit` + `npm run check:colors` green; `npm test` green.
- On-device (Pixel): picker ordering/archived-via-search/snoozed-marker; Quick Log commit-truth (success only on commit); Undo removes via deleteTouchpoint; error + Retry; global Quick Log opens picker first.
</verification>

<success_criteria>
The shared picker (favourites -> recency -> alpha, archived-via-search, snoozed-marked, a11y-modal) and the commit-truthful Quick Log snackbar (success+Undo on commit via the existing deleteTouchpoint, error+Retry otherwise) complete the shell's honest capture loop.
</success_criteria>

<output>
Create `.planning/phases/22-app-shell-navigation/22-06-SUMMARY.md` when done.
</output>
