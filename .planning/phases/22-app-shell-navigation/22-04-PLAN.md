---
phase: 22-app-shell-navigation
plan: 04
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/ShellAppBar.tsx
  - src/components/OverflowMenu.tsx
  - src/navigation/use-bottom-clearance.ts
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/screens/GroupEventsScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/OrreryScreen.tsx
  - src/screens/BackupScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [SHELL-12, SHELL-13, SHELL-14]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-13: root tab screens show a branded title with NO Back control (Dashboard = Orbit branded identity; Orrery/Backup/Settings = explicit destination titles); child/focused screens show Back + a clear title — delivered via the reusable ShellAppBar primitive, with `headerShown:false` kept so screens own their chrome."
    - "SHELL-13: scrollable browse content clears the bottom tab bar + FAB region via a `useBottomClearance()` hook built on `useBottomTabBarHeight()` + safe-area insets — never a hardcoded bottom padding (dossier §J forbids ad-hoc inset math); final list items scroll fully above the nav/FAB."
    - "SHELL-13: the Android status bar integrates with the active theme (light icons over the dark palette) and interactive content stays safely inset; full per-theme status-bar treatment is deferred to Phase 23 (Theme), which this primitive routes through tokens."
    - "SHELL-12: the Dashboard exposes Group Events as a PROMINENT header icon+label destination AND a redundant Dashboard-overflow entry; both route to the GroupEvents placeholder route (Phase 33 owns the real screen) — the header and overflow entries are distinct redundant paths, never merged (adjacency edge)."
    - "SHELL-12: Archived Contacts is reachable from the Dashboard overflow, routing to the DashboardStack-registered ArchivedContactsScreen instance (dual-registered in Plan 01) so Back returns to Dashboard; the Settings row remains as the second entry point — one screen, one restore/purge surface (ADR-018/ADR-080)."
    - "SHELL-14: every ShellAppBar control (Back, overflow, header destinations) carries a semantic accessibilityLabel matching its visible label, a ≥44px touch target (hitSlop where the glyph is smaller), and logical focus order; the overflow menu manages focus — OverflowMenu is UPGRADED this phase to set `accessibilityViewIsModal` on its open Modal and restore focus to the trigger on close (its Modal currently lacks both, OverflowMenu.tsx:47), so the SHELL-14 modal-focus guarantee is real and not asserted against a component that cannot deliver it."
    - "SHELL-12/ADR-075: the retired Manage-favourites user-facing entries are REMOVED from the new shell — the Dashboard favourites-filter 'Manage' entry (HomeScreen.tsx:319) and the Settings 'Manage favourites' row (SettingsScreen.tsx:2008) are gone (ADR-075 retires them 'with nothing put in their place'). No new entry to ManageFavourites is added; the route itself is retained-but-unexposed per the Plan 01 ADR-075 flag (the shipped orbit://favourites widget deep-link still needs it)."
    - "SHELL-01/13: the Dashboard header's cross-tab shortcuts are reconciled with the new bottom nav — the Backup/Orrery/Settings header entries (HomeScreen.tsx:536/:554/:572) become tab switches (navigate('BackupTab'|'OrreryTab'|'SettingsTab')) rather than bare cross-tab navigates that would throw route-not-found; the footer Archived entry (HomeScreen.tsx:407) is removed so the Dashboard reaches Archived via the D-09 overflow only (overflow + Settings = the two D-09 entry points, not three). Whether to further remove the now-redundant header shortcuts is an owner visual decision, flagged not silently taken."
    - "SHELL-12: Group Events header/overflow labels render single-line; a long localized label truncates with ellipsis rather than clipping the icon (encoding edge: label length measured for the row, icon never dropped)."
    - "SHELL-13: app-bar secondary actions collapse into the ⋯ overflow when the header row is full; the title is single-line and truncates with ellipsis (overflow edge)."
    - { statement: "SHELL-13 app-bar long-text backstop: under large OS font scale, long screen/child titles reflow or truncate without clipping the Back or overflow affordances — on-device visual on the Pixel.", verification: backstop }
    - "GroupEvents placeholder route renders the themed \"Coming soon\" heading + one feature line (\"Group logging arrives with Group Events.\") and is registered as a real semantic route (deep-link-ready) per the types.ts placeholder pattern (empty edge: no group-events data yet → a calm placeholder, never a broken/blank screen)."
  artifacts:
    - "src/components/ShellAppBar.tsx — reusable root/child app-bar primitive (branded-title/no-back vs back+title, overflow slot, a11y)"
    - "src/components/OverflowMenu.tsx — UPGRADED with accessibilityViewIsModal + focus restore (SHELL-14 modal-focus guarantee)"
    - "src/navigation/use-bottom-clearance.ts — useBottomTabBarHeight()+inset content-clearance hook"
    - "src/screens/GroupEventsScreen.tsx — themed placeholder route (Phase 33 replaces it)"
    - "GroupEvents route added to DashboardStackParamList + registered in DashboardStack"
  key_links:
    - "ShellAppBar Back -> back-intent resolver (Plan 02) so visible Back == system Back"
    - "useBottomClearance -> useBottomTabBarHeight() -> scrollable browse screens' contentContainer padding"
    - "HomeScreen header + overflow -> GroupEvents placeholder route (Phase 33 seam) and Archived (DashboardStack instance)"
  prohibitions:
    - "Group Events must NOT become a fifth bottom-nav tab and the Dashboard tab must NOT become a radial/menu launcher (both [REJECTED FOR THIS MILESTONE] — dossier). Group Events is a Dashboard-owned header+overflow destination only."
    - "The shell app bar and header entries must NOT convey relationship status via any chrome token — status hues (statusStable/statusWobble/statusDecay/rogue) belong to contact cards/orrery, never to nav/app-bar/header affordances."
    - "Content clearance must NOT be a hardcoded bottom padding value — it must derive from useBottomTabBarHeight()+insets."
    - "Do NOT introduce an icon dependency — continue the unicode glyph-token approach; the semantic icon registry is Phase 23's job."
---

<objective>
Build the shell's chrome layer: a reusable ShellAppBar primitive (root branded-title/no-back vs child back+title with an overflow slot and full a11y), a `useBottomClearance()` hook so scrollable browse content clears the tab bar + FAB, status-bar integration, and the Dashboard's Group Events header icon+label + overflow (Group Events + Archived) entries with the GroupEvents placeholder route.

Purpose: SHELL-13 (app bars, insets, content clearance) and SHELL-12 (Group Events + Archived Dashboard reachability) are the "shell looks and routes like an app" layer. The app-bar + clearance primitives are inherited by every later UI phase (dossier cross-phase export).
Output: ShellAppBar, useBottomClearance, GroupEventsScreen placeholder, the Dashboard header/overflow entries, the OverflowMenu a11y upgrade, the ADR-075 entry removals, and the header cross-tab reconciliation.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification — full phase list in 22-01-PLAN.md phase-anchor):
- `src/components/ShellAppBar.tsx` — reusable root/child app-bar primitive.
- `src/navigation/use-bottom-clearance.ts` — `useBottomClearance(extraForFab?)` over `useBottomTabBarHeight()`+insets (single-sources the FAB clearance geometry Plan 05 consumes).
- `src/screens/GroupEventsScreen.tsx` — themed "Coming soon" placeholder; `GroupEvents` route added to `DashboardStackParamList`.
This plan also MODIFIES existing `src/components/OverflowMenu.tsx` (a11y upgrade) and removes/reshapes existing HomeScreen/Settings entries — those are not new symbols.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-app-shell-navigation/22-01-PLAN.md
@.planning/phases/22-app-shell-navigation/22-UI-SPEC.md
@.planning/phases/22-app-shell-navigation/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable ShellAppBar primitive + OverflowMenu a11y upgrade + status-bar integration (SHELL-13/14)</name>
  <files>src/components/ShellAppBar.tsx, src/components/OverflowMenu.tsx</files>
  <read_first>
    - src/components/OverflowMenu.tsx (the Modal + scrim + a11y overflow idiom to compose AND upgrade; ⋯ trigger, hitSlop 16, accessibilityLabel="Dismiss actions"; note the Modal at :47 has NO `accessibilityViewIsModal` and no focus restore — the SHELL-14 gap to close)
    - src/screens/CustomFieldsScreen.tsx or another screen's existing header/Back/title block (the chrome pattern screens own today)
    - 22-UI-SPEC.md Typography (app-bar title 18-22px, 600-700) + Color (accent reserved list; status tokens NOT for chrome) + UI Considerations (app-bar overflow explicit; app-bar long-text backstop)
    - docs/dossier/milestone-2/phase-01-app-shell-navigation-dossier-amended-group-events.md §I (app bars: root = title/branded identity/no Back; child = Back+title+optional overflow) + §J (status bar integrates with theme) + §N (a11y: labels match visible, ≥44px, focus order)
    - src/navigation/back-intent.ts (Plan 02 — the Back the app-bar wires to)
  </read_first>
  <reversibility rating="reversible">A new presentational primitive; screens opt in.</reversibility>
  <action>
    First, UPGRADE `src/components/OverflowMenu.tsx` for SHELL-14 modal focus (Plan 04 relies on it for the overflow a11y guarantee): add `accessibilityViewIsModal={true}` to its open `Modal` content container (:47) and restore accessibility focus to the ⋯ trigger on close (capture a ref to the trigger and call `AccessibilityInfo.setAccessibilityFocus` / a focus ref on dismiss). Keep its existing themed 44px trigger, `hitSlop`, and `accessibilityLabel="Dismiss actions"` scrim untouched. No colour literal.

    Then create `src/components/ShellAppBar.tsx`: a presentational app bar with a `variant: "root" | "child"` (root = branded/destination title, NO Back; child = a Back control + title), an optional `overflow?: OverflowAction[]` slot (reuse the upgraded `OverflowMenu`), and an optional trailing-utilities slot. The child Back control wires to the shell back-intent path (Plan 02) so visible Back == system Back. All colours via `useTheme().colors.*` (title `textPrimary`, inactive utilities `textSecondary`, active/primary `accent`; NEVER a status hue — status is not shell chrome). Title is single-line and truncates with ellipsis; do not lock a height that clips scaled text (reflow, SHELL-14). Every control: `accessibilityRole="button"`, an `accessibilityLabel` matching the visible label, ≥44px target (hitSlop where the glyph is smaller). Status-bar: keep `StatusBar style="light"` over the dark palette (App.tsx) and document that Phase 23 owns per-theme status-bar treatment through tokens — do not hardcode a colour here. Apply ShellAppBar to the four tab roots as the reference (Home = root branded "Orbit"; Orrery/Backup/Settings = root titled), leaving existing child-screen chrome as-is (full migration is a later Theme/Release-Hardening pass — note the boundary; the deferral is SAFE per Plan 02's system-Back scope note since shell transients are hidden on child screens).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass (no colour literal in ShellAppBar or the OverflowMenu upgrade).
    - Source assertion: `grep -c "accessibilityLabel" src/components/ShellAppBar.tsx` returns ≥1 per interactive control; the `variant` prop distinguishes root (no Back) from child (Back).
    - Source assertion: no status-hue token (`statusStable|statusWobble|statusDecay|rogue`) appears in ShellAppBar.tsx (`grep -E "status(Stable|Wobble|Decay)|rogue" src/components/ShellAppBar.tsx` returns none).
    - Source assertion: `grep -c "accessibilityViewIsModal" src/components/OverflowMenu.tsx` returns ≥1 and the file references a focus-restore call on close (grep `setAccessibilityFocus` or a focus ref).
    - On-device (Pixel, end-of-phase UAT): the four tab roots show correct titles (Dashboard branded, others destination-titled) with no Back on roots; child screens show Back+title; overflow opens as a modal, traps + restores TalkBack focus to the ⋯ trigger on close; TalkBack reads control labels matching the visible labels.
  </acceptance_criteria>
  <done>ShellAppBar renders root branded-title/no-back and child back+title with an a11y-correct overflow and no status-hue chrome; the four tab roots use it; tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 2: Bottom content-clearance hook for scrollable browse surfaces (SHELL-13)</name>
  <files>src/navigation/use-bottom-clearance.ts, src/screens/HomeScreen.tsx, src/screens/BackupScreen.tsx, src/screens/SettingsScreen.tsx, src/screens/OrreryScreen.tsx</files>
  <read_first>
    - 22-RESEARCH.md "Don't Hand-Roll" (content-clears-nav-bar → useBottomTabBarHeight(); manual inset guessing forbidden) + Pitfall (manual inset guessing)
    - src/screens/HomeScreen.tsx (the dashboard FlatList/ScrollView contentContainer — where bottom padding applies; note AddSpeedDialFab is still mounted here until Plan 05 removes it)
    - react-native safe-area-context useSafeAreaInsets + @react-navigation/bottom-tabs useBottomTabBarHeight (the two inputs to the clearance)
  </read_first>
  <reversibility rating="reversible">A hook + padding application; removable.</reversibility>
  <action>
    Create `src/navigation/use-bottom-clearance.ts`: a hook `useBottomClearance(extraForFab = true): number` returning `useBottomTabBarHeight()` plus (when `extraForFab`) the FAB clearance (FAB base 56 + its 28 bottom offset + a small gap) so scrollable content clears BOTH the tab bar and the FAB region. Read the tab-bar height from `useBottomTabBarHeight()` (bottom-tabs) — never hardcode. Apply the returned value as the `contentContainerStyle` bottom padding on the scrollable browse surfaces (HomeScreen dashboard list; BackupScreen; SettingsScreen; OrreryScreen if it scrolls — otherwise skip and note it). Do not alter list data or ordering; only bottom padding. Keep every style value theme-token-free numbers (padding is not a colour) and no colour literal introduced.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - Source assertion: `grep -c "useBottomTabBarHeight" src/navigation/use-bottom-clearance.ts` returns 1; no hardcoded bottom-padding literal is used for clearance in the touched screens (the padding reads from `useBottomClearance()`).
    - On-device (Pixel, end-of-phase UAT): the last dashboard row/list item scrolls fully above the bottom tab bar and the FAB; nothing is permanently hidden behind the bar/FAB.
  </acceptance_criteria>
  <done>useBottomClearance derives clearance from the tab-bar height + insets + FAB region and the browse surfaces clear the nav/FAB; tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 3: GroupEvents placeholder + Dashboard Group Events header/overflow + Archived overflow + ADR-075 entry removal + header cross-tab reconciliation (SHELL-12/ADR-075)</name>
  <files>src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/screens/GroupEventsScreen.tsx, src/screens/HomeScreen.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (the header topBar :505-582 with the Your week :517 / Backup :536 / ◎ Orrery :554 / ⚙ Settings :572 shortcuts; the favourites-filter "Manage" entry block :314-326 → :319 `navigate("ManageFavourites")`; the footer Archived entry :403-416 → :407 `navigate("Archived")`)
    - src/screens/SettingsScreen.tsx around :2008 (the `navigate("ManageFavourites")` Manage-favourites row to remove)
    - src/navigation/types.ts (the placeholder-route doc pattern at lines 13-16 — the mechanism for a route whose screen lands later)
    - src/components/OverflowMenu.tsx (the upgraded overflow menu the Dashboard uses for Group Events + Archived redundant entries)
    - 22-UI-SPEC.md Copywriting "Placeholder routes" ("Coming soon" heading + one feature line) + docs/dossier §D-09 (Group Events = header icon+label + redundant overflow; Archived from overflow; NOT a fifth tab)
    - 22-01-PLAN.md phase-anchor "ADR-075 compliance" block + the D-08 inventory rows N11-N17
    - src/navigation/tabs/DashboardStack.tsx (Plan 01 — where Archived is dual-registered; add GroupEvents here)
  </read_first>
  <reversibility rating="reversible">Additive route + header entries; removals of retired/off-spec entries; call-shape conversions.</reversibility>
  <action>
    **GroupEvents placeholder (SHELL-12):** Add `GroupEvents: undefined` to `DashboardStackParamList` (types.ts) with the placeholder-route doc comment (Phase 33 replaces it). Create `src/screens/GroupEventsScreen.tsx` as a themed "Coming soon" placeholder: heading "Coming soon" + line "Group logging arrives with Group Events." (all colours via `useTheme().colors.*`; centered two-line static content). Register `<Stack.Screen name="GroupEvents" component={GroupEventsScreen} />` in DashboardStack.

    **Dashboard Group Events + Archived (SHELL-12/D-09):** In HomeScreen add a PROMINENT Group Events header destination (icon + label, the glyph-token approach, `accessibilityLabel="Group Events"`, ≥44px) → `navigation.navigate("GroupEvents")`. Add a Dashboard overflow (reuse the upgraded OverflowMenu) with a REDUNDANT "Group Events" entry (→ GroupEvents) and an "Archived Contacts" entry → `navigation.navigate("Archived")` (resolves to the DashboardStack Archived instance, origin-aware Back to Dashboard). Do NOT add Group Events as a tab and do NOT convert the Dashboard into a launcher.

    **ADR-075 entry removal (N11/N17):** Remove the Dashboard favourites-filter "Manage" entry (HomeScreen.tsx:314-326 block, the `navigate("ManageFavourites")` at :319) and the Settings "Manage favourites" row (SettingsScreen.tsx:2008). ADR-075 retires these "with nothing put in their place" — do NOT replace them with any other favourites-order affordance. (The ManageFavourites route stays registered in DashboardStack per the Plan 01 ADR-075 flag; nothing here routes to it.)

    **Archived footer removal (N12):** Remove the footer Archived entry (HomeScreen.tsx:403-416) so the Dashboard reaches Archived via the D-09 overflow only — overflow + the Settings row are the two D-09 entry points (three would be off-spec).

    **Header cross-tab reconciliation (N14/N15/N16):** Convert the Dashboard header shortcuts from bare cross-tab navigates (which throw route-not-found post-split) to tab switches: `navigate("Backup")` (:536) → `navigate("BackupTab")`; `navigate("Orrery")` (:554) → `navigate("OrreryTab")`; `navigate("Settings")` (:572) → `navigate("SettingsTab")`. Keep `navigate("Digest")` (:517, Your week) unchanged — Digest is intra-DashboardStack. Do NOT silently DELETE these header affordances (the Backup/Orrery/Settings shortcuts now duplicate the bottom tabs — whether to remove them is an owner visual decision; note that as a flagged follow-up, do not remove them here).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - Source assertion: `grep -c "GroupEvents" src/navigation/tabs/DashboardStack.tsx` returns ≥1 (registered); `grep -rn "navigate(\"GroupEvents\")" src/screens/HomeScreen.tsx` returns ≥2 (header + overflow); GroupEventsScreen contains the "Coming soon" heading + the Group Events feature line, no colour literal.
    - Source assertion (ADR-075, comment-filtered): `grep -vE '^[[:space:]]*(//|\*|/\*)' src/screens/HomeScreen.tsx | grep -c 'ManageFavourites'` returns 0 and the same over `src/screens/SettingsScreen.tsx` returns 0 (both user-facing entries removed; comment mentions do not count).
    - Source assertion (header reconciliation, comment-filtered): `grep -vE '^[[:space:]]*(//|\*|/\*)' src/screens/HomeScreen.tsx | grep -Ec 'navigate\("Backup"\)|navigate\("Orrery"\)|navigate\("Settings"\)'` returns 0 (converted to `navigate("BackupTab"|"OrreryTab"|"SettingsTab")`); the footer `dashboard-archived-entry` block is removed (`grep -c 'dashboard-archived-entry' src/screens/HomeScreen.tsx` returns 0 — a testID not used in prose comments) while the overflow Archived entry exists.
    - On-device (Pixel, end-of-phase UAT): the Dashboard header shows Group Events icon+label → placeholder; the overflow lists Group Events (redundant) + Archived Contacts; Archived from overflow opens the list, Back → Dashboard; the header Backup/Orrery/Settings shortcuts switch tabs; no "Manage favourites" entry appears on the Dashboard favourites filter or in Settings.
  </acceptance_criteria>
  <done>Group Events (header + redundant overflow) and Archived (overflow) route correctly with one destructive surface; the retired Manage-favourites entries are removed (ADR-075); the header cross-tab shortcuts are converted to tab switches and the redundant footer Archived is removed; tsc/check:colors green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard overflow → Archived (destructive surface) | Reaching the single restore/purge surface from a second entry point must not create a second destructive surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Tampering | dual Archived entry points | medium | mitigate | Both entries route to ONE ArchivedContactsScreen component + one restore/purge DAO; ADR-018's archive-before-purge gate and single-destructive-surface rule are untouched (Pitfall 6). |
| T-22-11 | Information disclosure | app-bar/header chrome | low | accept | Chrome renders route titles + static labels only; no contact data or status is conveyed by chrome (status-hue prohibition enforced). |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run check:colors` green; `npm test` green.
- On-device (Pixel): tab-root titles + child back+title; content clears nav/FAB; Group Events header+overflow → placeholder; Archived from overflow → single screen, origin-aware Back.
</verification>

<success_criteria>
The shell has a reusable app-bar primitive and content-clearance hook, status-bar integration, and the Dashboard exposes Group Events (header + redundant overflow) and Archived (overflow) with a single destructive surface — no fifth tab, no launcher, no status-hue chrome.
</success_criteria>

<output>
Create `.planning/phases/22-app-shell-navigation/22-04-SUMMARY.md` when done.
</output>
