---
phase: 22-app-shell-navigation
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/ResumeImportPrompt.tsx
  - src/components/ResumeReconcilePrompt.tsx
  - src/screens/ImportCompleteScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/components/MergeImpactSummary.tsx
  - src/screens/ReconcileCompleteScreen.tsx
  - src/screens/RestorePreviewScreen.tsx
  - src/screens/RestoreResultScreen.tsx
  - src/screens/EditContactScreen.tsx
  - src/navigation/discard-keep-guard.ts
autonomous: true
requirements: [SHELL-03, SHELL-04, SHELL-05, SHELL-07]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-05: the two container-level completion resets — import Done (ImportCompleteScreen.tsx:300) and reconcile Done (ReconcileCompleteScreen.tsx:47) — land the Dashboard tab root via resetToDashboardRoot() (a nested reset), so Back returns to Dashboard, never a blank tab."
    - "SHELL-05: the App-level Resume-prompt navigate sites resolve to the correct nested tab target — ResumeImportPrompt → SettingsTab/(ImportReview|BulkImportSetup|ImportProgress|DuplicateReview|ImportComplete); ResumeReconcilePrompt → SettingsTab/ReconcileGrid — never a root-level navigate to a now-nested route name (which would throw route-not-found). (The ShareIntent N1/N2 sites in linking.ts are owned by Plan 01 with the navigationRef retype.)"
    - "SHELL-04/05: the cross-tab component navigates that the tab split strands are all reshaped — ImportCompleteScreen → DashboardTab/UnboundContacts (N8, :267) and DashboardTab/Profile (N9, :283); ContactProfileScreen → SettingsTab/ReconcileDetail (N6, :827, the reconcile flow lands in its home tab); each via the composite/tab navigation typing so a bare cross-stack navigate cannot silently type-check."
    - "SHELL-04/05 (crash fix, reviews cycle-3 HIGH #1): the two ImportReviewScreen `navigation.replace(\"Profile\",{contactId})` completion sites (N18 :215 single-import commit, N19 :289 duplicate-link) — which crash post-split because ImportReview is a SettingsStack screen and Profile lives only in DashboardStack/OrreryStack — are reshaped to `navigationRef.current?.reset(resetToDashboardWith({name:\"Profile\",params:{contactId}}))` (Plan 01's reset owner), landing the imported/linked contact's Profile on the Dashboard tab (Back=Dashboard, a completion like import Done). `ImportReviewScreen.tsx:239 replace(\"ImportComplete\")` is intra-SettingsStack and stays UNCHANGED."
    - "SHELL-04 (crash fix, HIGH #3): merge COMPLETION no longer calls `navigation.replace(\"Profile\")` — which crashes from the reconcile (SettingsStack) origin because Profile is not registered there (MergeImpactSummary.tsx:17). On merge success it routes `navigationRef.current?.reset(resetToDashboardWith({name:\"Profile\",params:{contactId:survivorId}}))` (Plan 01's single reset owner), landing the survivor Profile in the Dashboard tab with Back=Dashboard, identically from the Profile-origin and the reconcile-origin (a completion, like import/reconcile Done). Typed against the retyped navigationRef so the stale route cannot silently type-check."
    - "SHELL-04: the Backup-tab-local completion resets (RestorePreviewScreen.tsx:99/:123, RestoreResultScreen.tsx:18) stay WITHIN the Backup stack — verified that `Backup` is BackupStack's root route name, so `reset({routes:[{name:\"Backup\"}...]})` keeps the same flat shape and lands the Backup tab root, not a cross-tab jump (adjacency edge: a tab-local reset and an external reset are distinct and must not be conflated)."
    - "SHELL-03: Back after a completed edit never replays the finished Edit screen — EditContactScreen's save-completion removes the Edit route from the stack (pop/replace to Profile), so `Profile → Edit → Save → Profile → Back` returns past Edit to the true origin, never re-showing Edit."
    - "SHELL-07: leaving a focused workflow with meaningful unsaved changes prompts a Discard changes / Keep editing dialog (title \"Discard changes?\", Keep editing = non-destructive accent, Discard = destructive danger); an unchanged form exits without a prompt (empty/ordering edge: no unsaved delta → no dialog; the guard fires only on a real delta)."
    - { statement: "SHELL-07: a SUCCESSFUL save is NOT intercepted by the guard — EditContactScreen navigates to Profile immediately after the write while the form is still dirty (EditContactScreen.tsx:451), so the guard honors a confirmed-save BYPASS ref (set true just before the post-save navigate) and lets that navigation through with no 'Discard changes?' prompt. The dirty delta covers the UNSAVED editable surface — form fields + links draft + custom-field values (which live outside EditFormState but are committed only on Save) — so the guard neither misses a real unsaved change nor fires spuriously.", verification: explicit }
    - { statement: "SHELL-07 photo truthfulness (reviews cycle-3 HIGH #2, verified EditContactScreen.tsx:170-176/:227-247): the photo is committed IMMEDIATELY by its own setContactPhoto/clearContactPhoto DAO (CropPhoto/picker write it, then the focus effect re-reads it) — it is NEVER part of the form Save. Therefore the photo is EXCLUDED from the Discard dirty delta and the guard/acceptance MUST NOT claim Discard reverts a photo change: Discard reverts the other unsaved fields; a photo change is already persisted by existing shipped behavior and survives Discard. If the ONLY change is the photo, Back exits with NO 'Discard changes?' prompt (nothing unsaved). The larger option 'stage the photo until Save so Discard CAN revert it' is a product/behavior change flagged for the owner (see the owner-flagged note in Task 3) — not implemented silently.", verification: explicit }
    - "SHELL-04: cross-tab in-app flows remain origin-aware because the merge cluster and Archived are dual-registered in their launching stacks (Plan 01) — this plan adds NO forced-Dashboard override for ordinary in-app back (unclassified edge resolved: origin = the launching stack, preserved)."
    - { statement: "SHELL-05 encoding edge: session-id params threaded through the nested Resume navigates (sessionId, batchCategoryId) are passed unchanged as the existing typed integers — the nested-navigate reshaping never reinterprets or truncates them.", verification: explicit }
  artifacts:
    - "src/navigation/discard-keep-guard.ts — reusable beforeRemove-based Discard/Keep guard hook/helper (SHELL-07 shell primitive for later-phase forms)"
  key_links:
    - "container-level Resume prompts / ShareIntent (navigationRef) -> nested navigate onto the owning tab stack"
    - "import/reconcile completion -> reset-intents.resetToDashboardRoot() (Plan 01 owner)"
    - "EditContactScreen save -> pop/replace so the completed Edit leaves the stack (no-replay)"
    - "discard-keep-guard -> beforeRemove + Alert; wired into EditContactScreen as the reference focused workflow"
  prohibitions:
    - "Do NOT convert the Backup-tab-local resets (RestorePreview/RestoreResult) into external Dashboard resets — they are completion resets WITHIN the Backup stack and must stay there (conflating them would strand the user off the Backup tab)."
    - "Do NOT remove or weaken the import/reconcile completion → Dashboard behavior; re-express it as a nested reset, never delete it."
    - "The Discard/Keep guard must NOT fire on an unchanged form (no spurious confirmation) and must NOT auto-save or partially save (the shell does not assume forms can safely autosave — dossier §B)."
---

<objective>
Reconcile every in-app navigation site that the flat→nested tab-tree split would otherwise strand: the container-level Resume prompts and ShareIntent gate (which navigate to now-nested route names), the import/reconcile completion resets, and the Backup-tab-local resets. Add the two remaining Back-correctness behaviors: completed-edit no-replay and the reusable Discard/Keep unsaved-changes guard.

Purpose: a "just add a tab navigator" plan silently strands these — container-level `navigate("ReconcileGrid")` throws route-not-found once ReconcileGrid is nested, and merge-completion `replace("Profile")` crashes from the reconcile origin. The reviews cycle-3 sweep also caught two ImportReviewScreen `replace("Profile")` sites (N18/N19) the prior "COMPLETE" inventory missed — the same crash class on real import paths. This plan closes those gaps (running a LITERAL grep re-audit rather than restating completeness) and delivers SHELL-03 (no-replay) + SHELL-04 (origin-aware + the merge-completion + ImportReview crash fixes) + SHELL-05 (completion fallbacks) + SHELL-07 (Discard/Keep with a confirmed-save bypass and a truthful photo exclusion).
Output: nested-navigate component sites (Resume prompts, ImportComplete, Profile→ReconcileDetail), the merge-completion + ImportReview resets via navigationRef+reset-intents, resetToDashboardRoot() completion resets, no-replay Edit save, and the discard-keep-guard primitive.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification — full phase list in 22-01-PLAN.md phase-anchor):
- `src/navigation/discard-keep-guard.ts` — `useDiscardKeepGuard({ hasUnsavedChanges, bypassRef })` reusable beforeRemove Discard/Keep guard (SHELL-07 shell primitive), with a confirmed-save bypass ref.
This plan otherwise only RESHAPES existing call sites (Resume prompts, ImportComplete, ContactProfile:827, MergeImpactSummary, completion resets, Edit save) — it creates no other new symbols. It consumes `resetToDashboardRoot`/`resetToDashboardWith` and the retyped `navigationRef` from Plan 01.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-app-shell-navigation/22-01-PLAN.md
@.planning/phases/22-app-shell-navigation/22-RESEARCH.md
@.planning/phases/22-app-shell-navigation/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape the stranded component-nav cross-tab sites — Resume prompts (N3/N4), ImportComplete (N8/N9), ImportReview (N18/N19), Profile→ReconcileDetail (N6), merge-completion crash (N10) + EXHAUSTIVE cross-tab route-not-found re-audit across all of src/ (closes the class: N20/N21)</name>
  <files>src/components/ResumeImportPrompt.tsx, src/components/ResumeReconcilePrompt.tsx, src/screens/ImportCompleteScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/ContactProfileScreen.tsx, src/components/MergeImpactSummary.tsx</files>
  <read_first>
    - src/components/ResumeImportPrompt.tsx (its `navigate("ImportReview"|"BulkImportSetup"|"ImportProgress"|"DuplicateReview"|"ImportComplete")` + `useNavigation<RootNavigation>()`)
    - src/components/ResumeReconcilePrompt.tsx (its `navigate("ReconcileGrid", {sessionId})`)
    - src/screens/ImportCompleteScreen.tsx lines 260-305 (:267 `navigate("UnboundContacts")` = N8; :283 `navigate("Profile",{contactId})` = N9; :300 Done reset = C1, handled in Task 2)
    - src/screens/ImportReviewScreen.tsx lines 71-74 (typed `RootStackScreenProps<"ImportReview">`, a SettingsStack screen), :203-216 (`importAsNew` → :215 `navigation.replace("Profile",{contactId})` = N18), :269-296 (`linkToExisting` → :289 `navigation.replace("Profile",{contactId:choice.contactId})` = N19), :239 `navigation.replace("ImportComplete",…)` (intra-SettingsStack — NO change)
    - src/screens/ContactProfileScreen.tsx lines 810-830 (:817 `navigate("SurvivorSelect")` = N5 — NO change, merge cluster is tri-registered by Plan 01; :827 `navigate("ReconcileDetail")` = N6 — becomes cross-tab to SettingsTab)
    - src/components/MergeImpactSummary.tsx line 17 (the merge-success `navigation.replace("Profile",{contactId:survivorId})` = N10; typed `RootStackScreenProps<"MergeImpactSummary">`) + src/navigation/reset-intents.ts (Plan 01's `resetToDashboardWith`) + src/navigation/linking.ts (Plan 01's retyped `navigationRef`)
    - 22-01-PLAN.md phase-anchor D-08 inventory (rows N3/N4/N5/N6/N8/N9/N10/N18/N19/N20/N21) + "Route → tab assignment" (the route→stack registration map the exhaustive re-audit checks against, incl. CropPhoto now in SettingsStack)
    - src/navigation/tabs/DashboardStack.tsx, OrreryStack.tsx, BackupStack.tsx, SettingsStack.tsx (Plan 01 — the ACTUAL route→stack registrations to grep for Step 2 of the exhaustive re-audit)
    - the shared navigating components for the Step-3 mount enumeration: src/components/PhotoSourcePicker.tsx (mounted in SettingsScreen:1351 + EditContactScreen:583 + PhotoFieldWidget), src/components/ResumeImportPrompt.tsx, src/components/ResumeReconcilePrompt.tsx, src/components/ContactCard.tsx (no navigate), and src/components/AddSpeedDialFab.tsx (still present this wave; deleted by Plan 05 — its startContactImport→import-acquire navigate is the N21 Dashboard-origin caller)
    - src/services/import/import-acquire.ts:150-176 + src/services/import/start-contact-import.ts (the service-layer navigate pass-through the wider sweep must cover — N21)
    - App.tsx (the Resume prompts render as siblings of the navigator; their navigation is container-level, now targeting the tab navigator)
  </read_first>
  <reversibility rating="reversible">Call-site reshaping; no data change. (Merge-completion changes only the post-commit navigation, not the merge write.)</reversibility>
  <action>
    Reshape each stranded cross-tab site so it resolves under the tab tree (do not change which flow is launched, only the navigation shape/params-unchanged):
    - `ResumeImportPrompt.tsx` (N3): each `navigate("ImportReview"|"BulkImportSetup"|"ImportProgress"|"DuplicateReview"|"ImportComplete", params)` → `navigate("SettingsTab", { screen: "<Route>", params })`, threading the SAME `{ sessionId }` / `{ sessionId, batchCategoryId: null }` params unchanged.
    - `ResumeReconcilePrompt.tsx` (N4): `navigate("ReconcileGrid", { sessionId })` → `navigate("SettingsTab", { screen: "ReconcileGrid", params: { sessionId } })`.
    - `ImportCompleteScreen.tsx` (N8/N9): `navigate("UnboundContacts")` → `navigate("DashboardTab", { screen: "UnboundContacts" })`; `navigate("Profile", { contactId: alreadyLinkedContactId })` → `navigate("DashboardTab", { screen: "Profile", params: { contactId: alreadyLinkedContactId } })`.
    - `ContactProfileScreen.tsx` (N6): `navigate("ReconcileDetail", { contactId })` (:827) → `navigate("SettingsTab", { screen: "ReconcileDetail", params: { contactId } })` (reconcile flow lands in its home tab; :817 SurvivorSelect is UNCHANGED — the merge cluster is tri-registered so it resolves intra-stack from Dashboard- or Orrery-origin).
    - `MergeImpactSummary.tsx` (N10, the crash fix): replace `navigation.replace("Profile", { contactId: survivorId })` in the merge-success `.then(...)` with `navigationRef.current?.reset(resetToDashboardWith({ name: "Profile", params: { contactId: survivorId } }))` (import `navigationRef` from `@/navigation/linking` + `resetToDashboardWith` from `@/navigation/reset-intents`). This lands the survivor Profile in the Dashboard tab identically from the Profile-origin AND the reconcile-origin (where `replace("Profile")` crashed — Profile is not in SettingsStack). Leave the merge write (`mergeContacts`) and its `.catch(() => setFailed(true))` untouched.
    - `ImportReviewScreen.tsx` (N18/N19, reviews cycle-3 HIGH #1 crash fix — the SAME class as N9/N10, on two real import paths): replace `navigation.replace("Profile", { contactId })` at :215 (in `importAsNew`) and `navigation.replace("Profile", { contactId: choice.contactId })` at :289 (in `linkToExisting`) with `navigationRef.current?.reset(resetToDashboardWith({ name: "Profile", params: { contactId } }))` (import `navigationRef` from `@/navigation/linking` + `resetToDashboardWith` from `@/navigation/reset-intents`). ImportReview is a SettingsStack screen so `navigation.replace("Profile")` is route-not-found post-split; the reset lands the imported/linked contact's Profile on the Dashboard tab (a completion, Back=Dashboard). Leave the import/link DAO writes (`commitSingleImport`/`linkExistingContactToRow`/`finalizeSessionIfTerminal`) and `navigation.replace("ImportComplete",…)` at :239 (intra-SettingsStack) UNCHANGED.
    Update each component's `useNavigation<...>()` / screen-props typing to the composite/tab type (or use the retyped `navigationRef`) so the nested `{ screen, params }` form type-checks and a bare cross-stack route name would NOT.

    **EXHAUSTIVE, EXECUTABLE cross-tab route-not-found re-audit (reviews cycle-4 — close the CLASS, not the instance).** Three consecutive cycles each declared the D-08 inventory "COMPLETE" and each still missed a NEW crash of the same class (N10, N18/N19, N20), every time a SHARED component mounted in more than one stack. So this is a concrete EXECUTOR PROCEDURE with an acceptance grep, not a restated completeness claim. Run all four steps and record the full result matrix in 22-03-SUMMARY.md:

    - **Step 1 — enumerate EVERY route-navigating call site across ALL of `src/` (not just screens/components):** run `grep -rnE 'navigation(Ref)?(\.current)?\??\.(navigate|replace|push)\(\s*"[A-Za-z]' src/screens src/components src/services src/navigation`. This deliberately includes `src/services/` (service-layer navigate pass-throughs — e.g. `src/services/import/import-acquire.ts:172/:174` `navigate("ImportReview"|"BulkImportSetup")`, threaded via `start-contact-import.ts`) and `src/navigation/` (container-level `navigationRef` sites, Plan-01-owned). Also capture any site that threads a `navigate`/`navigation` FUNCTION as an argument (service helpers) — trace it to its callers.
    - **Step 2 — build the route→stack registration map** from `grep -rnE 'name="[A-Za-z]+"' src/navigation/tabs/*.tsx`: for each route name, the exact set of stacks it is registered in (note the multi-registered routes: the Profile family in Dashboard+Orrery, `CropPhoto` in Dashboard+Orrery+Settings per N20, the merge cluster tri-registered, Archived in Dashboard+Settings).
    - **Step 3 — for EACH call site, determine every STACK the calling module can be MOUNTED in.** A SCREEN is mounted in exactly the stack(s) it is registered in (Step 2). A SHARED COMPONENT (a `useNavigation()` consumer that is NOT a `Stack.Screen` — enumerate them with `grep -rln "useNavigation" src/components`: currently ContactCard [no navigate], AddSpeedDialFab [deleted by Plan 05], ResumeImportPrompt, ResumeReconcilePrompt, PhotoSourcePicker) inherits the UNION of the stacks of EVERY screen that mounts it — find those with `grep -rn "<ComponentName" src/` and map each mount screen to its stack. A SERVICE that receives a `navigate` fn inherits the stack of whichever caller passed it. This is the exact discipline that would have caught PhotoSourcePicker (mounted in SettingsScreen [Settings] AND EditContactScreen [Dashboard/Orrery]) — enumerate ALL mount screens, never just the first/safe one.
    - **Step 4 — FLAG a crash for every (call-site stack × target route) pair where the target route is NOT registered in that stack.** For each flagged pair choose the fix: reshape an in-flight navigate to the nested `{screen,params}` form; reshape a COMPLETION to `navigationRef.reset(resetToDashboardWith(...))`; or (for a shared component whose crashing mount cannot be reshaped, like PhotoSourcePicker→CropPhoto) confirm the target route is MULTI-REGISTERED in the mounting stack (the N20 fix Plan 01 applies — verify `CropPhoto` is now in SettingsStack). Container-level `navigationRef` sites are exempt ONLY if they use the nested form under the TabParamList-typed ref (verify).

    Record in the SUMMARY: the Step-1 output, the per-shared-component mount→stack enumeration (Step 3), and every (call-site × target × disposition) row (Step 4). Confirm explicitly: the orchestrator-verified intra-DashboardStack `navigate("Profile")` sites (CreateContactScreen:187, DigestScreen:120, UnboundContactsScreen:113, NeverContactedScreen:167/216, HomeScreen:221, ContactProfileScreen:312) stay unchanged; `import-acquire.ts:172/:174` is intra-SettingsStack from its two Settings callers and its former Dashboard caller (AddSpeedDialFab) is deleted by Plan 05 (N21); and `CropPhoto` is registered in SettingsStack (N20). This task OWNS reshaping any NEWLY-flagged component/service crash it finds within its file set; a flagged site in a Plan-01-owned file (container-level/registration) is recorded and routed to Plan 01.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (all nested navigates type-check against TabParamList + the per-tab screen params; the retyped navigationRef accepts `resetToDashboardWith(...)`).
    - Source assertion (comment-filtered): for each of src/components/ResumeImportPrompt.tsx, src/components/ResumeReconcilePrompt.tsx, src/screens/ImportCompleteScreen.tsx, src/screens/ContactProfileScreen.tsx, `grep -vE '^[[:space:]]*(//|\*|/\*)' FILE | grep -Ec 'navigate\("ReconcileGrid"|navigate\("ImportReview"|navigate\("UnboundContacts"\)|navigate\("ReconcileDetail"'` returns 0 for the bare cross-tab forms (all are `navigate("<Tab>Tab", { screen: ... })`).
    - Source assertion (comment-filtered): `grep -vE '^[[:space:]]*(//|\*|/\*)' src/components/MergeImpactSummary.tsx | grep -c 'replace("Profile"'` returns 0; `grep -c 'resetToDashboardWith' src/components/MergeImpactSummary.tsx` returns 1.
    - Source assertion (ImportReview N18/N19, comment-filtered): `grep -vE '^[[:space:]]*(//|\*|/\*)' src/screens/ImportReviewScreen.tsx | grep -c 'replace("Profile"'` returns 0; `grep -c 'resetToDashboardWith' src/screens/ImportReviewScreen.tsx` returns 2 (both completion sites); the `replace("ImportComplete"` site remains (intra-SettingsStack, unchanged).
    - Re-audit assertion (EXHAUSTIVE, reviews cycle-4): the SUMMARY records all four steps — (1) the `grep -rnE 'navigation(Ref)?(\.current)?\??\.(navigate|replace|push)\(\s*"[A-Za-z]' src/screens src/components src/services src/navigation` sweep output (the wider scope INCLUDING `src/services/` and `src/navigation/`); (2) the route→stack map from `src/navigation/tabs/*.tsx`; (3) the per-shared-component mount→stack enumeration for every `useNavigation` consumer in `src/components` (ContactCard, ResumeImportPrompt, ResumeReconcilePrompt, PhotoSourcePicker; AddSpeedDialFab confirmed deleted by Plan 05); (4) the (call-site stack × target route) crash matrix with each disposition. No remaining (call-site stack × target route) pair is unregistered.
    - Re-audit falsifiable checks: `grep -c 'name="CropPhoto"' src/navigation/tabs/SettingsStack.tsx` returns 1 (N20 — every stack that mounts a PhotoSourcePicker now registers CropPhoto: Dashboard, Orrery, Settings); the six orchestrator-verified intra-DashboardStack `navigate("Profile")` sites are unchanged. For N21 (the `import-acquire.ts:172/:174` service pass-through): its two SettingsStack callers (SettingsScreen:160, LegacyContactPickerScreen:161) are intra-stack SAFE, but its Dashboard-origin caller `AddSpeedDialFab.tsx:55` is STILL PRESENT at this wave (Plan 03 is wave 2; Plan 05 deletes AddSpeedDialFab at wave 4) — so the SUMMARY records this site as "flagged cross-tab, disposition = removed by Plan 05 (UniversalFab has no import action); NOT reshaped here (out of this plan's file set)", and the END-OF-PHASE verification (below) asserts `ls src/components/AddSpeedDialFab.tsx` fails once Plan 05 has run. Do NOT reshape AddSpeedDialFab in this plan.
    - On-device (Pixel, end-of-phase UAT): a pending import/reconcile Resume prompt resumes into the Settings tab; ImportComplete "View Unbound"/"View contact" land the Dashboard tab; **importing a single Android contact → commit, and linking a duplicate during import, no longer crash and land that contact's Profile on the Dashboard tab (N18/N19)**; a merge completed FROM THE RECONCILE FLOW no longer crashes and lands the survivor's Profile on the Dashboard tab; a merge completed from a Profile also lands the survivor Profile.
  </acceptance_criteria>
  <done>Every stranded cross-tab component navigate (Resume prompts, ImportComplete, ImportReview N18/N19, Profile→ReconcileDetail) resolves through the nested tab form or reset-intents, the merge-completion `replace("Profile")` crash is fixed via navigationRef+reset-intents, and the literal grep re-audit is recorded; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Completion resets → Dashboard (C1/C2), Backup-local resets verified (B1–B3), completed-edit no-replay (SHELL-03/05)</name>
  <files>src/screens/ImportCompleteScreen.tsx, src/screens/ReconcileCompleteScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/EditContactScreen.tsx</files>
  <read_first>
    - src/screens/ImportCompleteScreen.tsx:300 + src/screens/ReconcileCompleteScreen.tsx:47 (the C1/C2 `reset({routes:[{name:"Home"}]})` completion resets)
    - src/screens/RestorePreviewScreen.tsx lines 90-125 (B1/B2 resets + the existing `beforeRemove` applying-guard) + src/screens/RestoreResultScreen.tsx:18 (B3)
    - src/navigation/reset-intents.ts (Plan 01 — resetToDashboardRoot)
    - src/screens/EditContactScreen.tsx (how save completes — read the save handler's navigation call to determine push vs pop/replace)
    - src/screens/ContactProfileScreen.tsx (how it navigates to Edit — to confirm the pop target)
  </read_first>
  <reversibility rating="reversible">Call-site reshaping; no data change.</reversibility>
  <action>
    C1/C2: change `ImportCompleteScreen.tsx:300` and `ReconcileCompleteScreen.tsx:47` `reset({ index: 0, routes: [{ name: "Home" }] })` → `reset(resetToDashboardRoot())` (import/reconcile are launched from Settings but their completion returns to Dashboard, matching the historical Home reset and the external-entry fallback).

    B1/B2/B3: VERIFY on disk that `Backup` is the root route name of BackupStack (Plan 01). If so, the RestorePreviewScreen.tsx:99/:123 and RestoreResultScreen.tsx:18 resets keep their existing flat `{routes:[{name:"Backup"}...]}` shape — they are tab-LOCAL (the `navigation` prop is the Backup stack's own navigator) and land the Backup tab root. Make NO shape change if Backup is the stack root; if Plan 01 named it differently, align the reset to that root name. Keep the RestorePreviewScreen `beforeRemove` applying-guard untouched.

    No-replay (SHELL-03): read EditContactScreen's save-completion navigation. If it PUSHES Profile (leaving Edit in the stack so Back replays Edit), change it to pop/replace back to the existing Profile (`navigation.goBack()` or `popTo("Profile")`) so the completed Edit leaves the stack. Verify the `Profile → Edit → Save → Profile → Back` path no longer re-shows Edit. If it already pops/replaces, assert that in the SUMMARY and make no change.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - Source assertion (positive): `grep -rn "resetToDashboardRoot" src/screens/ImportCompleteScreen.tsx src/screens/ReconcileCompleteScreen.tsx` returns 1 each — the two completion resets now route through reset-intents rather than a flat root reset.
    - Source assertion: the RestorePreview/RestoreResult resets reference `Backup` (the confirmed BackupStack root name) and are unchanged in shape.
    - On-device (Pixel, end-of-phase UAT): import Done and reconcile Done land the Dashboard tab; restore cancel/finish lands the Backup tab root; Profile→Edit→Save→Profile→Back does NOT re-show Edit.
  </acceptance_criteria>
  <done>Import/reconcile completion resets land Dashboard via resetToDashboardRoot(); Backup-local resets stay within the Backup stack; the completed Edit no longer replays on Back; tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 3: Reusable Discard/Keep unsaved-changes guard (SHELL-07)</name>
  <files>src/navigation/discard-keep-guard.ts, src/screens/EditContactScreen.tsx</files>
  <read_first>
    - src/screens/RestorePreviewScreen.tsx lines 94-96 (the existing `beforeRemove` + `preventDefault()` guard — the codebase idiom to generalize)
    - src/screens/EditContactScreen.tsx: its `EditFormState`/`seedEditState`/`buildEditInput`; the seeded links baseline `seededLinks` (:156) + `linksDraft`; the `committedValuesRef` custom-field baseline (:179-185); the IMMEDIATE-photo-commit comment + state (:170-178) and the focus-effect photo re-read (:227-247) — proving the photo is NOT part of Save and must be EXCLUDED from the dirty delta; the save handler at :420-457 that does `navigation.navigate("Profile",{contactId})` at :451 while the form is still dirty; and the partial-save `reseedMetadataAfterPartialSave()` path (:422-430) that keeps `linksDraft` for retry and stays on the form (the baseline-reset point)
    - 22-UI-SPEC.md Copywriting Contract "Destructive confirmation (unsaved work)" (title "Discard changes?", Keep editing = accent non-destructive, Discard = danger destructive)
    - docs/dossier/milestone-2/phase-01-app-shell-navigation-dossier-amended-group-events.md §B (Discard changes / Keep editing; no autosave assumption)
  </read_first>
  <reversibility rating="reversible">A guard hook + one consumer; removable.</reversibility>
  <action>
    Create `src/navigation/discard-keep-guard.ts`: a reusable hook `useDiscardKeepGuard({ hasUnsavedChanges: boolean; bypassRef: RefObject<boolean> })` that registers a `navigation.addListener("beforeRemove", ...)` which, when `bypassRef.current` is true, does NOTHING (lets the removal through — the confirmed-save escape hatch) ; else when `hasUnsavedChanges` is true, `preventDefault()`s the removal and shows a native `Alert` titled "Discard changes?" with "Keep editing" (cancel/non-destructive) and "Discard" (`style: "destructive"`, dispatches the pending remove action); when both are false it does nothing (unchanged forms exit without a prompt). Native `Alert` `style:"destructive"` needs no theme token; any non-Alert affordance uses `useTheme().colors.{accent,danger}` (Keep=accent, Discard=danger) — no literal.

    Wire `useDiscardKeepGuard` into EditContactScreen as the reference focused workflow.

    **`hasUnsavedChanges` covers only the UNSAVED editable surface — EXPLICIT baselines + reset points (finding F), and the photo is EXCLUDED (reviews cycle-3 HIGH #2):**
    - **Form fields:** compare current `buildEditInput(form)` against the input built from the on-load `seedEditState(result)` snapshot captured in a `seedInputRef` on load (this ref IS the baseline).
    - **Links:** compare `buildLinksForDiff(linksDraft)` against `buildLinksForDiff` of the seeded `seededLinks` baseline (already captured in state at :156).
    - **Custom-field values:** compare the current editor values against `committedValuesRef.current` (the values actually committed to the DB, seeded pre-edit and updated on a successful Save at :185). If a precise per-field diff is impractical this cycle, track a coarse `customFieldsDirty` boolean toggled by each custom-field editor's onChange — never silently drop custom fields/links from the delta.
    - **PHOTO IS EXCLUDED.** Verified EditContactScreen.tsx:170-176/:227-247: the photo is written IMMEDIATELY by its own `setContactPhoto`/`clearContactPhoto` DAO (CropPhoto/picker write it, then the focus effect re-reads it) — it is NEVER part of the form Save, so a "Discard" CANNOT revert it. Do NOT include a photo-changed flag in `hasUnsavedChanges`, and do NOT claim Discard reverts the photo. If the ONLY change is the photo, Back exits with no prompt (nothing unsaved).
    - **Reset points (so a coarse flag cannot get stuck):** after the partial-save path (`reseedMetadataAfterPartialSave` at :428, which re-commits metadata but KEEPS linksDraft for retry and stays on the form), RESET `seedInputRef` and `committedValuesRef` to the newly committed metadata/values so the guard compares against what is actually committed now (not the stale pre-edit seed); links remain dirty (kept for retry) so backing out still prompts. On a successful full Save the screen navigates via the bypass (below), so no baseline reset is needed there.

    **Confirmed-save bypass:** add a `bypassRef` (`useRef(false)`); set `bypassRef.current = true` synchronously immediately BEFORE the successful-save `navigation.navigate("Profile", { contactId })` at :451 so the guard does not present "Discard changes?" after a confirmed save (EditContactScreen navigates while still dirty).

    **OWNER-FLAGGED ALTERNATIVE (do NOT implement silently — CLAUDE.md: reversing/changing shipped behavior is the owner's call):** the larger option "stage the photo in screen state until the form Save and only then commit it via the photo DAO" WOULD let Discard revert a photo change (and would make the guard fire on a photo-only change). It reverses the shipped immediate-commit photo behavior (EditContactScreen.tsx:171-176 + the CropPhoto `setContactPhoto`-then-`goBack` contract + the orphan-cleanup invariant at :249-257), touches the CropPhoto/picker flow, and is a product/behavior decision — flag it for the owner to schedule as its own task; this plan ships the truthful-minimal exclusion above.

    This is the shell primitive later-phase forms (Rapid Capture, etc.) consume.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - Source assertion: `grep -c "beforeRemove" src/navigation/discard-keep-guard.ts` returns 1; the Alert title string "Discard changes?" is present; the guard early-returns when `bypassRef.current` is true AND when `hasUnsavedChanges` is false.
    - Source assertion: in EditContactScreen the `bypassRef` is set true immediately before the post-save `navigate("Profile"` (grep the ordering); the dirty computation references links + custom-field state and the seed/committed baselines (`seedInputRef`/`seededLinks`/`committedValuesRef`), and does NOT include a photo-changed flag (the photo is excluded — grep confirms no `photo`/`setPhoto` term inside the `hasUnsavedChanges` computation).
    - On-device (Pixel, end-of-phase UAT): editing a contact's NAME/PHONE/a custom field/a link then pressing Back shows "Discard changes?" with Keep editing / Discard; Keep editing stays; Discard leaves and the unsaved field edits are gone; a SUCCESSFUL Save navigates to Profile with NO "Discard changes?" prompt; editing nothing then Back exits with no prompt; **changing ONLY the photo then Back exits with NO prompt and the photo change PERSISTS (it was already committed by existing behavior — Discard does not and cannot revert it)**; after a partial link-save failure (metadata committed, links kept), Back still prompts for the unsaved links.
  </acceptance_criteria>
  <done>A reusable beforeRemove Discard/Keep guard with a confirmed-save bypass exists and is wired into Edit Contact; the dirty delta covers the UNSAVED surface (form/links/custom fields with explicit baselines + partial-save reset) and EXCLUDES the already-persisted photo; a successful save is never intercepted; unchanged forms exit silently; tsc/check:colors green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed workflow → navigation stack | A finished edit or import must not remain replayable; stale stack entries are a correctness (not confidentiality) hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Tampering (state) | completed Edit replay | low | mitigate | Edit save pops/replaces so a finished edit cannot be re-entered and re-submitted from Back (SHELL-03 no-replay). |
| T-22-08 | DoS (UX trap) | Discard/Keep guard | low | mitigate | Guard only fires on a real unsaved delta and never autosaves; Keep editing always returns to the form so the user is never trapped or silently loses work. |
| T-22-09 | Tampering | container-level nested navigate params | low | accept | sessionId/batchCategoryId are app-internal typed integers threaded unchanged; not external input. |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run check:colors` green.
- `npm test` green (no test regressions from the call-site reshaping).
- On-device (Pixel): Resume prompts resume into the right tab; import/reconcile Done → Dashboard; restore → Backup root; no Edit replay; Discard/Keep on Edit.
</verification>

<success_criteria>
Every container-level and completion navigation resolves correctly under the tab tree, the completed Edit no longer replays, and a reusable Discard/Keep guard protects unsaved focused-workflow changes.
</success_criteria>

<output>
Create `.planning/phases/22-app-shell-navigation/22-03-SUMMARY.md` when done.
</output>
