---
phase: 22-app-shell-navigation
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/stores/shell-transient-store.ts
  - src/stores/shell-transient-store.test.ts
  - src/navigation/back-intent.ts
  - src/navigation/back-intent.test.ts
  - src/navigation/focused-route-classification.ts
  - src/navigation/focused-route-classification.test.ts
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [SHELL-02, SHELL-03, SHELL-04, SHELL-06]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-02: tapping the ALREADY-ACTIVE tab first dismisses any open transient overlay (speed dial / picker / modal) and stops; a SECOND active-tab tap returns that tab's stack to its root (popToTop). Tapping a NON-active tab is an ordinary tab switch (no dismiss/pop). Ordering is: focused? -> transient-open? dismiss+stop : canGoBack? popToTop."
    - "SHELL-03: Android system Back and the visible app Back produce identical logical results — both route through the single back-intent resolver, which dismisses the topmost open transient layer FIRST (returns handled) and otherwise falls through to ordinary stack back."
    - "SHELL-03: when a transient layer (speed dial / picker / modal) is open, Back dismisses it and does NOT pop the stack or exit the app (adjacency edge: an open overlay and the stack are distinct layers dismissed in order, never merged/collided)."
    - { statement: "SHELL-03: with no transient layer open and an empty stack (tab root), Back yields the platform default (leaves/backgrounds the app) — the resolver returns not-handled so react-navigation's nested back handles it (empty-stack edge).", verification: backstop }
    - "SHELL-03: when multiple transient layers could be open, back-intent dismisses exactly the TOPMOST one per resolution (ordering edge: layered transients dismiss most-recent-first, one Back per layer)."
    - { statement: "SHELL-02/03 dismissal actually CLOSES the overlay: the shell-transient-store registers a real dismiss CALLBACK per overlay (not just an id), so `dismissTop()` invokes the topmost overlay's own close (e.g. the FAB's setExpanded(false), the picker's onDismiss) and returns true — removing an id from a set alone could not close a component whose visibility is local React state (AddSpeedDialFab pattern, HIGH #5a). The FAB (Plan 05) and picker (Plan 06) register their close function on open and unregister on close.", verification: explicit }
    - { statement: "SHELL-03 scope + deferral: the shell BackHandler + back-intent deliver system-Back == visible-Back for SHELL-OWNED transients (speed dial, picker). Existing child-screen visible Back controls that call navigation.goBack() directly (DigestScreen:128, ArchivedContactsScreen:181, NeverContactedScreen:87, UnboundContactsScreen:55, BackupScreen:288) and the focused-workflow screens that own their own hardwareBackPress handler (ComposeScreen:404, CaptureScreen:216) are NOT migrated to route through back-intent this phase (deferred child-chrome pass, Plan 04 boundary). This is SAFE for a VERIFIED reason (reviews cycle-2 correction — the earlier 'FAB/picker are hidden on child screens' rationale was FALSE: the FAB is VISIBLE on browse child surfaces Archived/NeverContacted/UnboundContacts/Profile). The real reason: every shell transient's scrim is `StyleSheet.absoluteFill` and `speedDialScrimPointerEvents(open)` returns `\"auto\"` when open (verified AddSpeedDialFab.tsx:75/:133 + add-speed-dial-fab-logic.ts:23), so WHILE a transient is open its full-screen scrim physically intercepts the tap and the child's visible Back cannot fire; and when NO transient is open, the child's goBack() already equals the back-intent 'default' branch. CAVEAT recorded for the future maintainer: this safety depends on full-screen scrim coverage — a later change that shrinks the scrim below full-screen would reopen a real system-Back ≠ visible-Back divergence and MUST re-route those child Back controls through back-intent. Documented deferral with the correct rationale, not a silent gap (HIGH #5b / finding E).", verification: explicit }
    - "SHELL-04: origin-aware Back holds by construction (per-tab stacks) — this plan's back-intent resolver never overrides it: dismiss-transient is the only interception; ordinary back still pops the focused tab's stack to its true origin (unclassified edge resolved: 'origin-aware' = the focused tab's own stack history, never a forced Dashboard)."
    - "SHELL-06: bottom nav is HIDDEN on focused workflows (Create/Edit/Compose/Capture/CropPhoto and the import/reconcile/merge forms) and while the keyboard is open (`tabBarHideOnKeyboard`), and VISIBLE on browse/read surfaces (tab roots, Profile, Archived, NeverContacted, UnboundContacts) — driven by the pure `isFocusedWorkflow(routeName)` predicate (unclassified edge resolved: the focused/browse split is an explicit route allow-list, not a guess)."
    - { statement: "SHELL-02/03 interaction backstop: on the Pixel, with the speed dial open, both system Back and the visible Back dismiss the dial and do NOT exit the app or pop the stack; active-tab retap with the dial open dismisses it, and a second retap returns the tab to root.", verification: backstop }
    - { statement: "tab-shell long-text backstop: under large OS font scale, tab captions reflow/wrap or hide-label rather than shrink or ellipsize the icon away (SHELL-14 foreshadow) — on-device visual.", verification: backstop }
  artifacts:
    - "src/stores/shell-transient-store.ts — Zustand registry of open transient overlays holding {id, dismiss} entries (openTransient(id, dismiss)/closeTransient(id)/dismissTop()/isAnyOpen)"
    - "src/navigation/back-intent.ts (+ .test.ts) — dismiss-transient-then-fall-through resolver"
    - "src/navigation/focused-route-classification.ts (+ .test.ts) — isFocusedWorkflow(routeName) predicate"
  key_links:
    - "shell-transient-store <- FAB/picker/modal register open-state; consulted by back-intent AND tabPress from one place"
    - "RootNavigator Tab.Screen tabPress listener -> shell-transient-store.dismissTop() then stack.popToTop()"
    - "RootNavigator Tab.Screen options(getFocusedRouteNameFromRoute) -> isFocusedWorkflow -> tabBarStyle display none/flex"
    - "shell-level BackHandler + app-bar Back (Plan 04) -> back-intent resolver (one function = system==visible Back)"
  prohibitions:
    - "back-intent must NOT override origin-aware Back: its ONLY interception is dismissing an open transient layer; ordinary Back always pops the focused tab's own stack, never a forced Dashboard (that would reverse the SHELL-04 origin-aware decision)."
    - "The active-tab retap must NOT pop to root while a transient layer is open — dismiss the transient first, pop only on the subsequent tap."
---

<objective>
Give the four-tab shell its behavior contract: one shell transient-overlay registry, one back-intent resolver so Android Back and the visible Back agree (dismiss the topmost transient first, then ordinary origin-aware back), active-tab retap that dismisses transient UI before returning a tab to root, and focused-workflow/keyboard-driven bottom-nav visibility.

Purpose: SHELL-02/03/04/06 are the shell's "does Back behave" core. Centralizing transient-open state in one store means tabPress, the shell BackHandler, and the visible Back all consult the same source — the only way "system Back == visible Back" is literally one function.
Output: shell-transient-store, back-intent + focused-route-classification pure modules with node tests, and the RootNavigator wired for retap + nav visibility.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification — see the full phase list in 22-01-PLAN.md phase-anchor):
- `src/stores/shell-transient-store.ts` (+ `.test.ts`) — Zustand registry of `{id, dismiss}` transient-overlay entries; `openTransient(id, dismiss)`, `closeTransient(id)`, `dismissTop()`, `isAnyOpen()`.
- `src/navigation/back-intent.ts` (+ `.test.ts`) — `resolveBackIntent({anyTransientOpen})` pure resolver.
- `src/navigation/focused-route-classification.ts` (+ `.test.ts`) — `isFocusedWorkflow(routeName)` predicate over an explicit allow-list.
- RootNavigator wiring (this phase): per-`Tab.Screen` `tabPress` retap listener + `getFocusedRouteNameFromRoute`-driven `tabBarStyle` visibility + the single shell `hardwareBackPress` handler.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-app-shell-navigation/22-01-PLAN.md
@.planning/phases/22-app-shell-navigation/22-RESEARCH.md
@.planning/phases/22-app-shell-navigation/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell transient-overlay store + back-intent resolver + focused-route classification (pure)</name>
  <files>src/stores/shell-transient-store.ts, src/navigation/back-intent.ts, src/navigation/back-intent.test.ts, src/navigation/focused-route-classification.ts, src/navigation/focused-route-classification.test.ts</files>
  <read_first>
    - src/stores/dashboard-prefs-store.ts (the existing Zustand store idiom to mirror — a device-local UI store, NOT a SQLite row)
    - src/components/add-speed-dial-fab-logic.ts (the pure-resolver + node-test idiom the new pure modules follow)
    - 22-RESEARCH.md Pattern 2 (active-tab retap: dismiss transient then pop root) + Pitfall 4 (hardware Back vs transient layers) + "Validation Architecture" (node/render-free vitest — pure modules only)
    - docs/dossier/milestone-2/phase-01-app-shell-navigation-dossier-amended-group-events.md §B (focused-workflow list: Add/Edit/Log/Update/Compose/onboarding/destructive) + §C (Back dismisses topmost transient before navigating)
  </read_first>
  <reversibility rating="reversible">Pure logic modules + a small store; no data or schema.</reversibility>
  <action>
    Create `src/stores/shell-transient-store.ts` (Zustand, device-local, NOT persisted — a transient runtime registry): an ORDERED stack of open transient-overlay entries, each `{ id: string; dismiss: () => void }`. `openTransient(id, dismiss)` pushes an entry (registering the overlay's OWN close function); `closeTransient(id)` removes it (the overlay calling this when it closes itself); `dismissTop(): boolean` pops the topmost entry, INVOKES its `dismiss()` (so the actual component closes — an id in a set cannot close a component whose visibility is local React state, e.g. AddSpeedDialFab's `setExpanded`, HIGH #5a), and returns true, or returns false when none is open; `isAnyOpen(): boolean`. Keep it a plain store (no persist middleware — ephemeral UI state, distinct from `dashboard-prefs-store`). The FAB (Plan 05) registers `openTransient("fab-speed-dial", () => setExpanded(false))`; the picker (Plan 06) registers `openTransient("contact-picker", onDismiss)`. Guard against double-registration (re-registering the same id replaces its dismiss).

    Create `src/navigation/back-intent.ts` (pure): `resolveBackIntent({ anyTransientOpen }: { anyTransientOpen: boolean }): "dismiss-transient" | "default"` — returns `"dismiss-transient"` when a transient layer is open, else `"default"`. Keep it a pure decision function (no react-native import) so both the shell BackHandler and the visible Back wire to the same logic. Document in-file that ordinary Back is origin-aware by construction and this resolver only intercepts transient dismissal (never a forced Dashboard).

    Create `src/navigation/focused-route-classification.ts` (pure): `isFocusedWorkflow(routeName: string): boolean` — an explicit ALLOW-LIST returning true for the dossier §B focused-workflow routes (Create, Edit, Compose, Capture, CropPhoto, and the import/reconcile/merge form routes: LegacyContactPicker, ImportReview, BulkImportSetup, ImportProgress, DuplicateReview, ImportComplete, BulkReview, SurvivorSelect, MergeConflicts, MergeImpactSummary, ReconcileDetail, ReconcileGrid, ReconcileComplete; plus the four FAB placeholder form routes LogContact/GroupLog/UpdateContact/Memory) and false for browse/read surfaces (Home, Orrery, Backup, BackupSettings, Settings, CustomFields, Profile, Archived, NeverContacted, UnboundContacts, ManageFavourites, Digest, GroupEvents, RestorePreview/RestoreResult). Put the classification in a top-of-file constant set so tuning is a one-line edit.

    Add `back-intent.test.ts` (both branches), `focused-route-classification.test.ts` (asserts the focused set → true, the browse set → false, and an unknown route → a documented default), and a `shell-transient-store` test asserting that `dismissTop()` INVOKES the topmost entry's registered `dismiss()` callback (spy) and pops it — proving dismissal actually closes the overlay, not just clears an id.
  </action>
  <verify>
    <automated>npx vitest run src/navigation/back-intent.test.ts src/navigation/focused-route-classification.test.ts src/stores/shell-transient-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/navigation/back-intent.test.ts` green: transient-open → "dismiss-transient", none → "default".
    - `npx vitest run src/navigation/focused-route-classification.test.ts` green: every focused route → true, every browse route → false, unknown → documented default. The test EXPLICITLY asserts `CropPhoto` AND the four FAB placeholder routes (LogContact/GroupLog/UpdateContact/Memory) → true (reviews cycle-4 minor — these are the routes most likely to be forgotten from the allow-list later; CropPhoto is now tri-registered incl. Settings per N20 and must hide the nav bar consistently across all three mounts).
    - `npx vitest run src/stores/shell-transient-store.test.ts` green: `dismissTop()` calls the topmost registered `dismiss` callback (spy asserted called once) and returns true; with none open it returns false; entries dismiss most-recent-first.
    - Source assertion: `shell-transient-store.ts` uses `create` from zustand and is NOT wrapped in `persist` (grep shows no `persist(` in the file — it is ephemeral); `openTransient` accepts a `dismiss` function argument (grep the signature).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The transient store, back-intent resolver, and focused-route predicate exist as pure/node-testable modules with green tests; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Active-tab retap (dismiss transient then pop root) + nav-bar focused/keyboard visibility (SHELL-02/06)</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/RootNavigator.tsx (the Tab.Navigator from Plan 01)
    - src/navigation/focused-route-classification.ts + src/stores/shell-transient-store.ts (from Task 1)
    - 22-RESEARCH.md Pattern 2 (tabPress listener code) + Standard Stack (`tabBarHideOnKeyboard` already set in Plan 01) + Pitfall 5 (keyboard)
  </read_first>
  <reversibility rating="reversible">Navigator config only.</reversibility>
  <action>
    On each `<Tab.Screen>` add a `listeners={({ navigation, route }) => ({ tabPress: (e) => {...} })}` that implements SHELL-02: if the tab is NOT focused, return (ordinary switch); if focused and `shellTransientStore.getState().dismissTop()` returned true, `e.preventDefault()` and stop (first tap dismisses transient); otherwise get the focused tab's stack navigator and if it `canGoBack()`, `e.preventDefault()` and `popToTop()` (second tap → root). Use `getFocusedStackNav`/the nested navigation state per RESEARCH Pattern 2.

    Add per-tab focused-workflow nav-bar visibility: in each Tab.Screen `options`, derive the focused child route via `getFocusedRouteNameFromRoute(route)` and set `tabBarStyle: { display: isFocusedWorkflow(focusedChild) ? "none" : "flex" }` so the bottom bar hides on focused-workflow screens within a tab (SHELL-06 nav half) and shows on browse/read surfaces. `tabBarHideOnKeyboard: true` (already set in Plan 01) covers the keyboard half. Keep all colours via `useTheme().colors.*` for the tab bar surface/active/inactive tokens (no literal). Leave the D-10 predictive-back comment from Plan 01 in place.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run check:colors` pass.
    - Source assertion: `grep -c "tabPress" src/navigation/RootNavigator.tsx` returns 4 (one listener per tab); `grep -c "getFocusedRouteNameFromRoute" src/navigation/RootNavigator.tsx` returns ≥1.
    - On-device (Pixel, end-of-phase UAT): opening a focused workflow (e.g. Edit Contact) hides the bottom nav; returning shows it; opening the keyboard on a text field hides the bar; active-tab retap with a transient overlay open dismisses it, a second retap returns the tab to root; retapping a non-active tab just switches.
  </acceptance_criteria>
  <done>Active-tab retap dismisses transient UI then pops to root, and the bottom nav hides on focused workflows and while the keyboard is open; tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 3: Shell-level BackHandler so system Back == visible Back (dismiss transient first) (SHELL-03)</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/back-intent.ts + src/stores/shell-transient-store.ts (Task 1)
    - 22-RESEARCH.md Pitfall 4 (hardware Back vs nested navigators + transient layers) + "Don't Hand-Roll" (RN's built-in nested back handles ordinary pops; only override for transient dismissal)
    - React Navigation v7 BackHandler integration (do NOT hand-roll a global stack walker — only intercept transient dismissal, then let default nested back proceed)
  </read_first>
  <reversibility rating="reversible">A single BackHandler subscription; removable without data impact.</reversibility>
  <action>
    Add ONE shell-level `BackHandler.addEventListener("hardwareBackPress", ...)` (inside the RootNavigator component, cleaned up on unmount) that consults `resolveBackIntent({ anyTransientOpen: shellTransientStore.getState().isAnyOpen() })`: if `"dismiss-transient"`, call `shellTransientStore.getState().dismissTop()` (which invokes the topmost overlay's own close callback) and return `true` (handled — do NOT pop the stack or exit); if `"default"`, return `false` so react-navigation's built-in nested back pops the focused tab's stack (origin-aware) and, at a tab root, yields the platform default. Do NOT walk a custom global stack (RESEARCH Don't-Hand-Roll). Document in-file that the visible Back control (the Plan-04 app-bar primitive) wires to the SAME `resolveBackIntent`/`dismissTop` path so "system Back == visible Back" is one function. Leave predictive-back disabled (D-10).

    **Document the SCOPE + DEFERRAL in-file (HIGH #5b / finding E — use the VERIFIED rationale, NOT the false one)**: this shell BackHandler delivers system-Back == visible-Back for SHELL-OWNED transients (speed dial, picker). Existing child-screen visible Back controls that call `navigation.goBack()` directly (DigestScreen:128, ArchivedContactsScreen:181, NeverContactedScreen:87, UnboundContactsScreen:55, BackupScreen:288) and the focused-workflow screens that own their own `hardwareBackPress` handler (ComposeScreen:404, CaptureScreen:216, which consume Back first) are NOT migrated to route through back-intent this phase (deferred child-chrome pass — Plan 04 boundary note). Do NOT write the earlier (FALSE) rationale that "the FAB/picker are hidden on child screens" — the FAB is VISIBLE on browse child surfaces (Archived/NeverContacted/UnboundContacts/Profile). The CORRECT, verified reason the deferral is safe: every shell transient's scrim is `StyleSheet.absoluteFill` and `speedDialScrimPointerEvents(open)` returns `"auto"` when open (AddSpeedDialFab.tsx:75/:133, add-speed-dial-fab-logic.ts:23), so while a transient is open its full-screen scrim intercepts the tap and the child's visible Back physically cannot fire; and when nothing is open, the child's `goBack()` already equals the back-intent "default" branch. State this scrim-interception reasoning in the comment AND add the caveat that the safety depends on full-screen scrim coverage — if a future change shrinks the scrim, those child Back controls MUST be re-routed through back-intent to avoid a real divergence.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Source assertion: `grep -c "hardwareBackPress" src/navigation/RootNavigator.tsx` returns 1; the handler returns `true` only on the dismiss-transient branch (grep the branch returns).
    - On-device (Pixel, end-of-phase UAT): with the speed dial open, system Back dismisses the dial and does NOT exit the app; with nothing open, system Back pops the focused tab's stack to its true origin; at a tab root with nothing open, system Back backgrounds the app.
  </acceptance_criteria>
  <done>A single shell BackHandler routes through back-intent so system Back dismisses the topmost transient first and otherwise defers to origin-aware nested back — identical to the visible Back path; tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS Back button → shell | The hardware Back event is OS input the shell interprets; must not be coerced into exiting mid-workflow or skipping a transient dismissal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | DoS (UX trap) | shell BackHandler | medium | mitigate | Handler returns `true` ONLY to dismiss a transient; every other case returns `false` so the user can never be trapped and normal origin-aware back always works. |
| T-22-06 | Tampering | focused-route classification | low | accept | The route allow-list is app-owned compile-time data, not external input; a mis-listed route is a UX bug caught in UAT, not a security threat. |
</threat_model>

<verification>
- `npx vitest run` green for back-intent + focused-route-classification tests; `npx tsc --noEmit` + `npm run check:colors` green.
- On-device (Pixel): retap dismiss-then-root; nav hides on focused workflow + keyboard; system Back == visible Back with a transient open (dismiss, don't exit); origin-aware back otherwise.
</verification>

<success_criteria>
One transient-overlay store and one back-intent resolver drive tabPress retap, nav-bar visibility, and the shell BackHandler; system Back and visible Back agree; nav hides on focused workflows and keyboard; origin-aware back is never overridden.
</success_criteria>

<output>
Create `.planning/phases/22-app-shell-navigation/22-02-SUMMARY.md` when done.
</output>
