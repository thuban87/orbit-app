---
phase: 22-app-shell-navigation
plan: 05
type: execute
wave: 4
depends_on: [22-02, 22-04]
files_modified:
  - package.json
  - src/components/universal-fab-logic.ts
  - src/components/universal-fab-logic.test.ts
  - src/components/UniversalFab.tsx
  - src/navigation/types.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/screens/placeholders/FabActionPlaceholders.tsx
  - src/screens/HomeScreen.tsx
  - App.tsx
autonomous: true
requirements: [SHELL-06, SHELL-08, SHELL-09, SHELL-14]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-08: the FAB expands into a labeled speed dial of EXACTLY SIX actions in fixed order — Add Contact, Quick Log, Log Contact, Group Log, Update Contact, Memory — above a translucent scrim; the set never grows, so the dial renders as a single non-scrolling column that never scrolls or clips (overflow edge)."
    - "SHELL-08: Add Contact routes to the existing Create screen; Group Log (position 4) routes directly to its GroupLog placeholder with NO shell-level pre-picker; Log Contact / Update Contact / Memory route to their themed placeholder routes (Phases 24/33/34 replace them)."
    - { statement: "SHELL-08/09 shell-level routing: because the FAB is mounted ONCE at shell level (outside any tab stack), `resolveFabTarget` returns a NESTED `{ tab, screen, params }` target (e.g. `navigate(\"DashboardTab\",{screen:\"Create\"})`), NOT a bare `navigate(\"Create\")` — a bare screen name cannot resolve from a shell-level ref / a non-Dashboard tab (the same stranded-navigate class Plan 03 fixes, reintroduced by the new shell-level consumer). `originContactId` is resolved by a PURE, node-tested `getFocusedContactContext(navigationState)` helper (finding D) that walks the nested navigation state and returns the contactId ONLY when the focused route is a Profile in DashboardTab or OrreryTab (else null), so a Profile-origin action preselects and a global-origin/root-tab/stale-state action opens the picker — no untested inline route walk.", verification: explicit }
    - "SHELL-09: from a Contact Profile the contact-specific actions (Quick Log, Log Contact, Update Contact, Memory) preselect that contact (contactId threaded to the target); from global contexts they open the shared picker (Plan 06 seam); Group Log always opens directly regardless of origin (adjacency/ordering edge: the Profile-origin vs global-origin branch is explicit, Group Log is exempt)."
    - "SHELL-08: opening then closing the dial repeatedly is idempotent — the shared-value open/close leaves no residual scrim capturing touches; a rapid double-tap does not desync the `open` state from the `expanded` shared value (idempotency/concurrency edges)."
    - "SHELL-06/13: the FAB is HIDDEN on focused workflows (via the Plan-02 `isFocusedWorkflow` predicate on the current route) and while the software keyboard is open (a `Keyboard` addListener toggles it), and VISIBLE and consistently positioned (bottom-right, 56x56) on browse/read surfaces across tabs. **Its bottom offset is DERIVED from `useMeasuredTabBarHeight()` (Plan 04's measured tab-bar-height store) + the shared `FAB_EDGE_GAP` — NOT from the context-bound `useBottomTabBarHeight()` (reviews cycle-4 HIGH #2: the FAB mounts as a NavigationContainer SIBLING of the tab navigator, and `useBottomTabBarHeight()` throws outside a tab-screen descendant, which would crash the shell's main capture control at mount) and NOT the legacy fixed `bottom:28` (sized for a full-screen Dashboard with no tab bar; it would collide with the new bottom nav).** The measured height already includes the bottom safe-area inset, so no extra inset is added. The FAB imports the SAME `FAB_SIZE`/`FAB_EDGE_GAP` constants AND the SAME `useMeasuredTabBarHeight()` source that `useBottomClearance` reads (Plan 04, finding C), so content clearance and FAB placement are single-sourced and never drift."
    - "SHELL-14: the FAB and every speed-dial action carry semantic accessibilityLabels (FAB collapsed = \"Add / capture\", announced open/closed), >=44px targets, the open dial sets `accessibilityViewIsModal` and restores focus to the invoking FAB on close; opening the dial fires a LIGHT haptic (dossier section M) — haptics are semantic only."
    - "SHELL-08: the scrim + collapsed action rows use `speedDialScrimPointerEvents(open)` -> 'none' when collapsed so dashboard touches pass through, 'auto' when open (Pitfall 3, the retained helper); an opacity-0 scrim never blankets the tab content."
    - { statement: "fab-speed-dial long-text backstop: under large OS font scale, the six action labels reflow rather than clip within the speed-dial rows — on-device visual on the Pixel.", verification: backstop }
    - "the FAB registers its open-state in the shell-transient-store (Plan 02) via `openTransient(\"fab-speed-dial\", () => setExpanded(false))` — passing its REAL close callback so `dismissTop()` actually collapses the dial (an id alone cannot close a component whose visibility is local React state); it calls `closeTransient(\"fab-speed-dial\")` when it closes itself (empty edge: with the dial closed, the store reports nothing open and retap/Back act normally)."
  artifacts:
    - "src/components/universal-fab-logic.ts (+ .test.ts) — six-action set + fixed order + context->target route map"
    - "src/components/UniversalFab.tsx — the universal six-action speed dial (supersedes AddSpeedDialFab), mounted at shell level"
    - "src/screens/placeholders/FabActionPlaceholders.tsx — themed LogContact/GroupLog/UpdateContact/Memory placeholder screens"
    - "LogContact/GroupLog/UpdateContact/Memory routes added to DashboardStackParamList + registered"
    - "expo-haptics dependency (first-party Expo)"
  key_links:
    - "UniversalFab (shell-level mount, App.tsx) -> reads current route -> isFocusedWorkflow (Plan 02) for visibility"
    - "UniversalFab bottom offset -> useMeasuredTabBarHeight() (Plan 04 measured store) + FAB_EDGE_GAP — NOT the context-bound useBottomTabBarHeight() (throws at the shell-sibling mount, HIGH #2); single-sourced with useBottomClearance"
    - "UniversalFab open-state -> shell-transient-store (Plan 02) so retap/Back dismiss it first"
    - "universal-fab-logic context->NESTED target map -> DashboardTab/Create / DashboardTab/GroupLog(direct) / DashboardTab/placeholders / Quick-Log seam (Plan 06)"
    - "speedDialScrimPointerEvents (retained from add-speed-dial-fab-logic) -> scrim + collapsed rows pointerEvents"
  prohibitions:
    - "The FAB action set must be EXACTLY the six fixed-order actions Add Contact, Quick Log, Log Contact, Group Log, Update Contact, Memory. Any five-action set (or a different order) is stale (D-05/AF-03) and must not ship."
    - "Group Log must NOT be routed through a shell-level contact pre-picker — it opens its canonical workflow directly (D-05)."
    - "The FAB must NOT be animated from React state per frame (CLAUDE.md) — open/close is a Reanimated shared value; React state mirrors open/closed ONLY for pointerEvents."
    - "The FAB must NOT convey relationship status via any chrome token — its fill is `accent`, never a status hue."
---

<objective>
Replace the two-action dashboard `AddSpeedDialFab` with the universal six-action speed dial mounted at shell level: fixed-order labeled actions over a translucent scrim, contact-preselect from Profile vs shared-picker from global (Group Log always direct), hidden on focused workflows and while the keyboard is open, with modal focus management and a light open haptic. Register the four unbuilt actions as themed placeholder routes.

Purpose: SHELL-08/09 are the shell's capture spine. The FAB reuses every Reanimated + scrim-pointer-events pattern from AddSpeedDialFab (CLAUDE.md compliance) and consumes the Plan-02 transient store + focused-route predicate so it participates in retap/Back dismissal and visibility.
Output: universal-fab-logic (pure + tested), UniversalFab (shell-mounted), the four placeholder routes, and expo-haptics.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification — full phase list in 22-01-PLAN.md phase-anchor):
- `src/components/universal-fab-logic.ts` (+ `.test.ts`) — six-action fixed-order set + `resolveFabTarget(actionId, {originContactId})` returning NESTED `{tab,screen,params}` / quick-log intents + the pure `getFocusedContactContext(navigationState)` route-walker (finding D).
- `src/components/UniversalFab.tsx` — shell-mounted six-action speed dial (supersedes AddSpeedDialFab; helper `add-speed-dial-fab-logic.ts` retained).
- `src/screens/placeholders/FabActionPlaceholders.tsx` — themed LogContact/GroupLog/UpdateContact/Memory placeholder screens; those four routes added to `DashboardStackParamList`.
- `expo-haptics` dependency (first-party Expo).
This plan deletes `AddSpeedDialFab.tsx` and consumes Plan 02's `shell-transient-store`/`isFocusedWorkflow` and Plan 04's clearance geometry constants (`FAB_SIZE`/`FAB_EDGE_GAP`) AND Plan 04's `useMeasuredTabBarHeight()` (reviews cycle-4 HIGH #2 — the shell-sibling FAB reads the measured tab-bar height from the store, never the context-bound `useBottomTabBarHeight()` which throws outside the tab tree).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-app-shell-navigation/22-01-PLAN.md
@.planning/phases/22-app-shell-navigation/22-02-PLAN.md
@.planning/phases/22-app-shell-navigation/22-UI-SPEC.md
@.planning/phases/22-app-shell-navigation/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: universal-fab-logic — six-action set/order + context to target map (pure)</name>
  <files>src/components/universal-fab-logic.ts, src/components/universal-fab-logic.test.ts</files>
  <read_first>
    - src/components/add-speed-dial-fab-logic.ts (the pure-logic + node-test idiom to mirror; the retained `speedDialScrimPointerEvents` helper lives here)
    - 22-UI-SPEC.md Copywriting (the six locked labels + fixed order) + docs/dossier section F (six actions; Group Log direct; contact-context preselect) + sections E/G (global -> shared picker)
    - 22-01-PLAN.md phase-anchor (FAB placeholder routes list; Add Contact -> Create)
  </read_first>
  <reversibility rating="reversible">Pure logic + test.</reversibility>
  <action>
    Create `src/components/universal-fab-logic.ts` (pure, react-native-free, node-testable): export the ordered six-action descriptor list [AddContact, QuickLog, LogContact, GroupLog, UpdateContact, Memory] with each action's stable id, visible label (the six locked strings verbatim), and a `resolveFabTarget(actionId, context: { originContactId: number | null })` that returns a serializable routing intent. **Every navigate intent is a NESTED tab target `{ kind: "navigate"; tab: "DashboardTab"; screen: string; params?: object }`** (the FAB is shell-mounted, so a bare `navigate("Create")` would not resolve from a non-Dashboard tab / shell-level ref — return the nested form): AddContact -> DashboardTab/Create; GroupLog -> DashboardTab/GroupLog (ALWAYS direct, ignores originContactId — no picker); QuickLog -> a `{ kind: "quick-log"; contactId: number | null }` intent (preselect when non-null, else a pick-then that opens the picker); LogContact/UpdateContact/Memory -> DashboardTab/<placeholder> with `{ contactId }` when originContactId is non-null, else a pick-then intent (open picker, then route with the picked id). Keep the six-in-fixed-order as a single frozen array so the set cannot drift.

    **Also export a PURE `getFocusedContactContext(navigationState): { originContactId: number | null }` helper (finding D — replaces the untested inline route walk):** it walks the nested React Navigation state (the container/tab state → the focused tab's route → that tab stack's focused route) and returns the `contactId` param ONLY when the deepest focused route is `Profile` inside `DashboardTab` or `OrreryTab`; for any root-tab focus, a non-Profile route, or a malformed/stale/partial state (missing `routes`/`index`/`params`) it returns `{ originContactId: null }` (defensive — never throws). Keep it react-native-free by typing its argument structurally (`{ index?: number; routes?: Array<{ name: string; params?: any; state?: ... }> }`) so it is node-testable without a live navigator. The FAB (Task 2) calls it with the current navigation state to derive `originContactId`.

    Add `universal-fab-logic.test.ts` asserting: exactly six actions in fixed order with the exact labels; every navigate intent names a TAB (`tab: "DashboardTab"`) not a bare screen; Group Log is direct regardless of originContactId; the Profile-origin (preselect) vs global (pick-then) branch for each contact-specific action; AND `getFocusedContactContext` for four cases — (a) DashboardTab→…→Profile{contactId:N} returns N, (b) OrreryTab→…→Profile{contactId:N} returns N, (c) a root tab (Home/Orrery/Backup/Settings, no Profile) returns null, (d) a stale/partial/malformed nested state (missing routes or index, or a Profile route with no contactId) returns null without throwing.
  </action>
  <verify>
    <automated>npx vitest run src/components/universal-fab-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/universal-fab-logic.test.ts` green: exactly six actions, fixed order, exact labels; every navigate intent names `tab: "DashboardTab"` (not a bare screen); Group Log direct for any origin; contact-specific actions preselect on Profile origin and pick-then on global origin.
    - Source assertion (finding D): `grep -c "getFocusedContactContext" src/components/universal-fab-logic.ts` returns ≥1 and `universal-fab-logic.test.ts` covers all four cases — DashboardTab/Profile → contactId, OrreryTab/Profile → contactId, a root tab → null, a stale/partial/malformed state → null without throwing.
    - Source assertion: the action array length is asserted equal to 6 in the test; the test asserts a navigate intent's `tab` field; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>universal-fab-logic exposes the six fixed-order actions, a context→target map with Group-Log-direct and Profile-preselect-vs-global-picker branches, and the pure `getFocusedContactContext` route-walker — all node-tested.</done>
</task>

<task type="auto">
  <name>Task 2: UniversalFab component (shell-mounted, keyboard/focused-hide, a11y, haptic) — supersede AddSpeedDialFab</name>
  <files>package.json, src/components/UniversalFab.tsx, src/screens/HomeScreen.tsx, App.tsx</files>
  <read_first>
    - src/components/AddSpeedDialFab.tsx (the full component — Reanimated open/close, scrim pointerEvents, 56x56 geometry, theme tokens, dashboard-create-fab testID; the two-action logic to expand to six)
    - src/components/add-speed-dial-fab-logic.ts (speedDialScrimPointerEvents — REUSE, do not reinvent)
    - src/navigation/use-bottom-clearance.ts (Plan 04 — import the exported `FAB_SIZE` + `FAB_EDGE_GAP` named constants for the FAB bottom offset; finding C, single-sourced with content clearance)
    - src/stores/tab-bar-layout-store.ts (Plan 04 — import `useMeasuredTabBarHeight()` for the FAB's tab-bar offset; reviews cycle-4 HIGH #2 — the shell-sibling FAB CANNOT call the context-bound `useBottomTabBarHeight()`, which throws outside a tab-screen descendant)
    - src/components/universal-fab-logic.ts (Task 1 — `resolveFabTarget` + the pure `getFocusedContactContext` route-walker to derive `originContactId`)
    - src/navigation/focused-route-classification.ts + src/stores/shell-transient-store.ts (Plan 02 — visibility + transient registration)
    - App.tsx lines 309-340 (NavigationContainer children where a shell-level FAB mounts; the FAB reads the focused route via navigation state) + src/screens/HomeScreen.tsx:44,653 (the AddSpeedDialFab import + mount to remove)
    - 22-RESEARCH.md Pitfall 5 (keyboard hide) + Pitfall 7 (a11y focus) + Supporting stack (expo-haptics via `npx expo install`)
  </read_first>
  <reversibility rating="reversible">Supersedes a component; retains the reused helper.</reversibility>
  <action>
    Install `expo-haptics` via `npx expo install expo-haptics` (first-party Expo, SDK-57 pin; no config plugin entry). Create `src/components/UniversalFab.tsx` by expanding `AddSpeedDialFab` to six actions: keep the `useSharedValue`/`withTiming` open/close with React `open` state mirrored ONLY for pointerEvents (CLAUDE.md), reuse `speedDialScrimPointerEvents(open)` for the scrim + all six collapsed rows, size the FAB from the shared `FAB_SIZE` (right:20 + borderRadius:`FAB_SIZE/2`), and DERIVE the bottom offset from `useMeasuredTabBarHeight()` (Plan 04's measured tab-bar-height store) + the shared `FAB_EDGE_GAP` — importing `FAB_SIZE`/`FAB_EDGE_GAP` from Plan 04's `use-bottom-clearance.ts` and `useMeasuredTabBarHeight` from `@/stores/tab-bar-layout-store`. **Do NOT call `useBottomTabBarHeight()` here (reviews cycle-4 HIGH #2): the FAB is a NavigationContainer sibling, outside the tab-screen tree, so that context hook throws at mount and would crash the FAB.** The measured height already includes the bottom safe-area inset (no extra `useSafeAreaInsets().bottom` needed). This is finding C: NOT the legacy fixed `bottom:28` (which assumed a full-screen Dashboard with no tab bar and now collides with the bottom nav); reading the SAME `useMeasuredTabBarHeight()` source AND the same `FAB_SIZE`/`FAB_EDGE_GAP` constants that `useBottomClearance` uses single-sources placement and content clearance so they never drift. Drive each row's translateY offset off the shared value. Each action row: label (the six locked strings), minHeight 44, `accessibilityRole="button"` + `accessibilityLabel`, onPress -> dispatch `resolveFabTarget(actionId, { originContactId })` and apply the NESTED result: a `navigate` intent → `navigation.navigate(intent.tab, { screen: intent.screen, params: intent.params })`; a `quick-log`/`pick-then` intent → the Plan-06 seam. `originContactId` comes from `getFocusedContactContext(navigationRef.current?.getRootState())` (the pure Task-1 helper, finding D; GUARD the ref — `navigationRef.current` is null before the container mounts, so fall back to `originContactId: null` and never throw) — resolving it from a shell-level mount requires walking the navigation state, not assuming Profile is in the current tab; the helper returns the contactId only for a DashboardTab/OrreryTab Profile and null otherwise. Collapsed FAB `accessibilityLabel="Add / capture"`, announce open/closed. On open: fire the light impact haptic (dossier section M), call `openTransient("fab-speed-dial", () => setExpanded(false))` (pass the REAL close callback so `dismissTop()` collapses the dial), and set `accessibilityViewIsModal` on the open dial; on close: `closeTransient("fab-speed-dial")` + restore accessibility focus to the FAB. All colours via `useTheme().colors.*` (FAB fill `accent`, rows `surfaceElevated`/`border`/`textPrimary`; scrim `background` at opacity — NEVER a status hue, never a literal).

    Mount `<UniversalFab/>` ONCE at shell level (inside NavigationContainer, as a sibling of the tab navigator) so it is consistently positioned across browse surfaces; hide it when `isFocusedWorkflow(currentRoute)` is true OR the keyboard is open (a `Keyboard.addListener('keyboardDidShow'/'keyboardDidHide')` toggling a local flag — MEMORY prefers this over KeyboardAvoidingView). Derive `currentRoute` for visibility from a REACTIVE subscription — `useNavigationState(s => …)` (or a `navigationRef.addListener('state', …)` that setStates) — NOT a one-shot `navigationRef.getRootState()` snapshot, which does not re-render on navigation and would freeze FAB visibility on the route it first read. Because it dispatches through `navigation` from a shell-level mount, use the container/tab navigation (the retyped navigationRef or a tab-typed navigation) so the nested targets type-check. Remove `<AddSpeedDialFab/>` + its import from HomeScreen (lines 44, 653). Migrate the `dashboard-create-fab` testID onto the new FAB (keep it so existing callers/tests resolve). Retire `AddSpeedDialFab.tsx` (delete the component) but KEEP `add-speed-dial-fab-logic.ts` (the `speedDialScrimPointerEvents` helper is still used).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass (no colour literal, no status hue in UniversalFab).
    - Source assertion: `grep -c "accessibilityLabel" src/components/UniversalFab.tsx` returns >=7 (FAB + six rows); the six locked labels appear in fixed order; `speedDialScrimPointerEvents` is imported and applied to the scrim + rows.
    - Source assertion: the superseded component file is deleted (`ls src/components/AddSpeedDialFab.tsx` fails) and the retained helper remains (`ls src/components/add-speed-dial-fab-logic.ts` succeeds); a passing `tsc` confirms HomeScreen has no dangling import of the deleted component; `grep -c "dashboard-create-fab" src/components/UniversalFab.tsx` returns 1 (testID migrated).
    - Source assertion: the FAB open/close animates a `useSharedValue`; `open` React state is used ONLY for pointerEvents (grep shows no per-frame setState in an animation callback).
    - Source assertion (finding C + reviews cycle-4 HIGH #2): the context hook is absent from non-comment lines — `grep -vE '^[[:space:]]*(//|\*|/\*)' src/components/UniversalFab.tsx | grep -c "useBottomTabBarHeight"` returns 0 (comment-filtered so a prose note cannot self-invalidate the gate; the shell-sibling FAB must NOT call the context hook that throws outside the tab tree) and `grep -c "useMeasuredTabBarHeight" src/components/UniversalFab.tsx` returns ≥1 (it reads Plan 04's measured store instead); `grep -Ec "FAB_SIZE|FAB_EDGE_GAP" src/components/UniversalFab.tsx` returns ≥1 (imported from `use-bottom-clearance.ts`); there is NO fixed `bottom: 28` (or bare `56` for diameter) literal for the FAB position — the offset derives from `useMeasuredTabBarHeight()` + the shared `FAB_EDGE_GAP`. `grep -c "openTransient(\"fab-speed-dial\"" src/components/UniversalFab.tsx` returns 1 with a close-callback argument; the action dispatch uses `navigation.navigate(intent.tab, { screen: ... })` (nested), not a bare `navigate("Create")`.
    - Source assertion (finding D): `grep -c "getFocusedContactContext" src/components/UniversalFab.tsx` returns ≥1 (the FAB derives `originContactId` via the pure helper, not an inline route walk).
    - On-device (Pixel, end-of-phase UAT): the FAB shows on browse surfaces across tabs at the same position, CLEARING the new bottom tab bar (not overlapping/under it); tapping it opens the six labeled actions over a scrim with a light haptic; each action routes correctly from ANY tab (e.g. Add Contact from the Orrery tab still opens Create); the dial hides on focused workflows and while the keyboard is open; system Back / active-tab retap collapse the dial (via the registered close callback); TalkBack treats the open dial as modal and restores focus to the FAB on close.
  </acceptance_criteria>
  <done>The universal six-action FAB is mounted at shell level, hides on focused/keyboard, manages modal focus with a light open haptic, registers its open-state in the transient store, and supersedes AddSpeedDialFab (helper retained); tsc/check:colors green.</done>
</task>

<task type="auto">
  <name>Task 3: FAB-action placeholder routes (Log Contact, Group Log, Update Contact, Memory)</name>
  <files>src/navigation/types.ts, src/navigation/tabs/DashboardStack.tsx, src/screens/placeholders/FabActionPlaceholders.tsx</files>
  <read_first>
    - src/navigation/types.ts (the placeholder-route doc pattern at lines 13-16 — real semantic routes registered before their screens land)
    - src/screens/GroupEventsScreen.tsx (Plan 04 — the "Coming soon" placeholder idiom to mirror)
    - 22-UI-SPEC.md Copywriting "Placeholder routes" (Coming soon heading + one owning-phase feature line) + Dismissed rows (placeholder-route overflow/long-text: fixed short static content)
    - 22-01-PLAN.md phase-anchor (the four FAB placeholder routes + which carry a contactId param for Profile-preselect)
  </read_first>
  <reversibility rating="reversible">Additive routes; later phases replace them.</reversibility>
  <action>
    Add `LogContact`, `GroupLog`, `UpdateContact`, `Memory` to `DashboardStackParamList` (types.ts) with the placeholder-route doc comment. LogContact/UpdateContact/Memory carry an optional `{ contactId?: number }` (Profile-preselect threads it; global opens the picker in Plan 06). GroupLog carries no params (direct, no pre-picker). Create `src/screens/placeholders/FabActionPlaceholders.tsx` exporting four themed "Coming soon" screens, each a centered heading "Coming soon" + one owning-phase feature line ("Detailed logging arrives with Rapid Capture." / "Group logging arrives with Group Events." / "Update Contact arrives with Rapid Capture." / "Memories arrive with Contact Knowledge."), all colours via `useTheme().colors.*`. Register the four `<Stack.Screen>`s in DashboardStack. These are real semantic, deep-link-ready routes that Phases 24/33/34 swap with zero shell change.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - Source assertion: `grep -Ec "LogContact|GroupLog|UpdateContact|Memory" src/navigation/tabs/DashboardStack.tsx` returns >=4 (all four registered); each placeholder screen renders "Coming soon" + its feature line with no colour literal.
    - Source assertion: `GroupLog` has NO contactId param (direct); LogContact/UpdateContact/Memory carry an optional `contactId`.
    - On-device (Pixel, end-of-phase UAT): each of the four FAB actions opens its themed "Coming soon" placeholder; from a Profile the contact-specific placeholders receive the contact context.
  </acceptance_criteria>
  <done>The four unbuilt FAB actions route to themed, deep-link-ready placeholder screens with the correct preselect params; tsc/check:colors green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FAB action dispatch → route | The FAB routes to internal semantic routes only; action ids and route names are compile-time, never external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-12 | Tampering (UX) | speed-dial scrim pointer-events | medium | mitigate | Reuse `speedDialScrimPointerEvents` so a collapsed opacity-0 scrim is inert ('none') and never swallows dashboard touches (Pitfall 3). |
| T-22-13 | Spoofing | FAB target routing | low | accept | `resolveFabTarget` maps a fixed action id to a compile-time route; no free-text or external input reaches routing. |
| T-22-14 | DoS (a11y trap) | open speed dial | low | mitigate | Open dial sets `accessibilityViewIsModal` and restores focus to the FAB on close so a screen-reader user is never stranded behind the scrim (Pitfall 7). |
</threat_model>

<verification>
- `npx vitest run src/components/universal-fab-logic.test.ts` green; `npx tsc --noEmit` + `npm run check:colors` green; `npm test` green.
- On-device (Pixel): six fixed-order actions over a scrim; light open haptic; hide on focused/keyboard; consistent position across tabs; Group Log direct; Profile-preselect vs global; a11y modal focus.
</verification>

<success_criteria>
The universal six-action FAB is the shell's single consistently-positioned capture control — fixed order, contact-aware, hidden on focused/keyboard, a11y-modal with a light haptic — superseding AddSpeedDialFab and routing the four unbuilt actions to themed placeholder routes.
</success_criteria>

<output>
Create `.planning/phases/22-app-shell-navigation/22-05-SUMMARY.md` when done.
</output>
