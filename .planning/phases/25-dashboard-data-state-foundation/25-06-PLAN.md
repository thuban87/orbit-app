---
phase: 25-dashboard-data-state-foundation
plan: 06
type: execute
wave: 3
depends_on: [25-02]
autonomous: true
requirements: [DASHQ-04]
files_modified:
  - src/services/widget/widget-data.ts
  - src/services/widget/widget-data.test.ts
  - src/db/favourites-dao.ts
  - src/db/favourites-dao.test.ts
  - src/logic/favourites-reorder-logic.ts
  - src/screens/ManageFavouritesScreen.tsx
  - src/navigation/widget-linking.ts
  - src/navigation/widget-linking.test.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/types.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/HomeScreen.tsx
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The favourites home-screen widget renders the Favorites population in its Default ordering (the shared Dashboard relationship-health order), NOT by `favourite_rank` (ADR-075 / ADR-043 consequence). [DASHQ-04]"
    - "The Manage-favourites drag-reorder screen and its rank-rewrite write are retired: `ManageFavouritesScreen.tsx` and `favourites-reorder-logic.ts` are removed, `favourites-dao` keeps only the binary mark/clear writes, and the `orbit://favourites` widget deep-link no longer routes to a Manage-favourites screen. [DASHQ-04]"
    - "The `favourite_rank` COLUMN is KEPT (not dropped): the capture / sun-picker / merge-candidate / picker reads that internally ORDER BY `favourite_rank` continue to compile and function; only the Dashboard/widget stop treating rank as a user-facing order (D-04 trip-wire — the column's fate is documented, not blind-dropped). [DASHQ-04]"
    - "Favourites remains binary membership everywhere; toggling membership (Profile/List/Card star) still works via the retained mark/clear writes, with no user-visible ordering surface anywhere. [DASHQ-04]"
  prohibitions:
    - statement: "Do NOT drop the `favourite_rank` column — it is still read (ORDER BY) by capture-read, sun-picker-read, merge-candidate-read, and picker-read; dropping it without a decided fate for those reads is a D-04 trip-wire. The column is kept; the Dashboard/widget simply stop sorting by it."
      status: enforced
      verification: "No migration is added; `grep -rn 'favourite_rank' src/db/capture-read.ts src/db/sun-picker-read.ts src/db/merge-candidate-read.ts src/db/picker-read.ts` still shows their internal ORDER BY intact; the widget no longer orders by rank."
    - statement: "Ranked Favorites is a retired product concept (ADR-075, supersedes ADR-033) — no drag-reorder surface, no rank-rewrite transaction, and no Dashboard/widget order-by-rank anywhere. Repurposing Manage favourites as a binary list was already rejected."
      status: enforced
      verification: "ManageFavouritesScreen.tsx + favourites-reorder-logic.ts are removed; favourites-dao no longer exports rewriteFavouriteRanks; the ManageFavourites route is removed from navigation; widget-data.test.ts asserts Default order."
    - statement: "This is a consumer-repointing retirement, not a blind delete: every consumer of the retired screen/route/rank-order (widget deep-link, Settings entry, HomeScreen Manage link) is re-pointed or removed atomically so no navigation targets a deleted route."
      status: enforced
      verification: "widget-linking.test.ts asserts orbit://favourites resolves to a live target (Home/Favorites population), not a deleted route; tsc is clean (no reference to the removed screen)."
  artifacts:
    - "widget-data.ts: Favorites-population Default-order projection (replaces rank-ordered read)"
    - "favourites-dao.ts: mark/clear binary writes retained; rewriteFavouriteRanks removed"
    - "Removed: ManageFavouritesScreen.tsx, favourites-reorder-logic.ts, the ManageFavourites route"
  key_links:
    - "widget-data → the Plan-02 Favorites-population Default-order read (the new ordering source, ADR-043 consequence)"
    - "orbit://favourites deep-link → re-pointed target (no Manage-favourites screen)"
    - "favourite_rank column KEPT → capture/sun/merge/picker internal reads (unbroken)"
---

<objective>
Retire ranked Favourites per ADR-075: remove the Manage-favourites drag-reorder screen + its rank-rewrite write, re-point the widget from `favourite_rank` order to the Favorites-population Default order, and re-point/remove every consumer of the retired screen and deep-link — while KEEPING the `favourite_rank` column (its picker/sun/merge/capture reads still use it internally).

Purpose: DASHQ-04 (favourites binary, Dashboard never sorts by rank). This is a deletion-with-consumers (D-04 trip-wire): re-point, do not blind-delete, and do not drop the column.

Output: widget projects Favorites in Default order; Manage-favourites screen + reorder logic + rank-rewrite write + route + deep-link removed/re-pointed; column preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record + governing ADR
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/decisions/ADR-075-binary-favourite-membership-without-a-user-facing-order.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the widget to the Favorites-population Default order</name>
  <files>src/services/widget/widget-data.ts, src/services/widget/widget-data.test.ts</files>
  <read_first>
    - src/services/widget/widget-data.ts (READ IN FULL: the current `listDashboard({filter:'favourites'})` read truncated by `favourite_rank ASC` at 52/75/83–86 — the ordering source being changed)
    - src/db/dashboard-read.ts (the Plan-02 Favorites-population Default-order read the widget re-points to; the legacy listDashboard favourites branch)
    - docs/decisions/ADR-075-...md (Implementation: "widget-data.ts — projects the Favorites population in Default ordering instead of manual rank")
    - docs/decisions/ADR-043 reference in RESEARCH (the widget-ordering consequence)
  </read_first>
  <action>
    Change `widget-data.ts` to source the favourites widget tiles from the Favorites-population Default-order read (the Plan-02 population entry, or the legacy favourites read re-pointed to Default order) instead of `favourite_rank ASC`. The membership set is unchanged (favourite_rank IS NOT NULL); only the ORDER changes to the shared Dashboard Default (relationship-health) order, truncated to the widget's tile count. Do NOT read or order by `favourite_rank`. Update widget-data.test.ts to assert Default ordering (not rank order) and that membership is still the favourites set.
  </action>
  <verify>
    <automated>npx vitest run src/services/widget/widget-data.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - widget-data.test.ts asserts the favourites tiles are ordered by the Default relationship-health order, not `favourite_rank`.
    - `grep -n 'favourite_rank' src/services/widget/widget-data.ts` returns no ordering use (membership may be expressed via the read, but no rank ORDER BY remains).
    - Membership is still the favourites set; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The widget renders the Favorites population in Default order; no rank ordering remains in widget-data.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the rank-rewrite write + the Manage-favourites screen/logic (keep the column + mark/clear)</name>
  <files>src/db/favourites-dao.ts, src/db/favourites-dao.test.ts, src/logic/favourites-reorder-logic.ts, src/screens/ManageFavouritesScreen.tsx</files>
  <read_first>
    - src/db/favourites-dao.ts (READ IN FULL: setFavouriteRank/clearFavouriteRank — the binary mark/clear writes to KEEP; rewriteFavouriteRanks — the rank-rewrite transaction to REMOVE; the changes===1 guard idiom)
    - src/logic/favourites-reorder-logic.ts (computeReorder — the drag→order permutation, REMOVED per ADR-075)
    - src/screens/ManageFavouritesScreen.tsx (the drag-reorder surface, REMOVED)
    - docs/decisions/ADR-075-...md (Implementation: favourites-dao keeps mark/clear, the rank-rewrite write is retired; ManageFavouritesScreen + favourites-reorder-logic retired)
    - CLAUDE.md (favourites binary-membership invariants; do NOT drop the column)
  </read_first>
  <action>
    Remove `rewriteFavouriteRanks` from `favourites-dao.ts` (and its tests), KEEPING `setFavouriteRank`/`clearFavouriteRank` (binary mark/clear) unchanged — membership toggling stays. Delete `src/logic/favourites-reorder-logic.ts` and `src/screens/ManageFavouritesScreen.tsx`. Do NOT add a migration and do NOT drop the `favourite_rank` column — its capture/sun/merge/picker internal reads still use it (Task 3 leaves those intact). In favourites-dao's in-file docs, record that rank-as-order is retired (ADR-075) but the column persists as vestigial storage for the internal picker reads. Update/prune favourites-dao.test.ts to drop the rewrite-path cases while keeping mark/clear coverage. (Route + navigation removal is Task 3.)
  </action>
  <verify>
    <automated>npx vitest run src/db/favourites-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'rewriteFavouriteRanks' src` returns no match; `favourites-reorder-logic.ts` and `ManageFavouritesScreen.tsx` no longer exist.
    - `setFavouriteRank`/`clearFavouriteRank` remain and their tests pass; no migration was added and the `favourite_rank` column still exists (`grep -rn 'ADD COLUMN\|DROP COLUMN' src/db/migrations` shows no new favourite change).
    - `npx tsc --noEmit` — note it may report the not-yet-removed route/consumers; those are removed in Task 3 of THIS plan, so run the plan's tasks in order and gate tsc at Task 3.
  </acceptance_criteria>
  <done>The rank-rewrite write + drag-reorder screen + reorder logic are removed; binary mark/clear and the column are kept.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point/remove the retired-screen consumers (route, deep-link, Settings, HomeScreen)</name>
  <files>src/navigation/widget-linking.ts, src/navigation/widget-linking.test.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/types.ts, src/screens/SettingsScreen.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/navigation/widget-linking.ts (READ IN FULL: FAVOURITES_URI = 'orbit://favourites' at 107 and the reset intents routing to ManageFavourites at 92/148 — the deep-link to re-point)
    - src/navigation/tabs/DashboardStack.tsx (the ManageFavourites Stack.Screen registration to remove)
    - src/navigation/types.ts (the ManageFavourites route param entry to remove)
    - src/screens/SettingsScreen.tsx (grep for the Manage-favourites row → ManageFavourites; remove it)
    - src/screens/HomeScreen.tsx (grep for the favourites-chip Manage link → ManageFavourites; remove it — the legacy dashboard's Manage affordance)
  </read_first>
  <action>
    Remove the `ManageFavourites` route from `DashboardStack.tsx` and its param entry from `navigation/types.ts`. Re-point `orbit://favourites` in `widget-linking.ts`: since the Manage-favourites screen is retired, resolve the deep-link to a LIVE target — navigate to Home (the dashboard) rather than a deleted route (the Favorites population is the intended destination; the render phases add its explicit chip — until then Home is the safe live target). Update the reset-intent `routes` arrays (92/148) to drop `ManageFavourites`. Remove the Settings Manage-favourites row and the HomeScreen favourites-chip Manage link (grep-locate both; delete the entry + any now-unused import). Update widget-linking.test.ts to assert `orbit://favourites` resolves to the live Home target (no deleted route). This task is where `tsc` must go fully clean (all references to the removed screen gone).
  </action>
  <verify>
    <automated>npx vitest run src/navigation/widget-linking.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'ManageFavourites' src` returns no match (route, deep-link target, Settings entry, HomeScreen link all removed/re-pointed).
    - widget-linking.test.ts asserts `orbit://favourites` resolves to a live target (Home), never a deleted route.
    - Full `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>Every retired-screen consumer is re-pointed or removed atomically; no navigation targets a deleted route; the column and binary favourites survive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget deep-link (orbit://) → navigation | An OS-delivered deep link must resolve to a live route, not a deleted one |
| retired screen consumers → navigation | Removing a route must not strand a navigate() call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.6-01 | Denial of Service (crash) | orbit://favourites deep-link to deleted route | medium | mitigate | Deep-link re-pointed to a live target (Home) atomically with the route removal; widget-linking.test.ts asserts a live resolution |
| T-25.6-02 | Tampering (data integrity) | dropping favourite_rank column | high | mitigate | Column is KEPT (no migration); capture/sun/merge/picker internal reads unbroken; the D-04 trip-wire honoured |
| T-25.6-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
| T-25.6-03 | Network / Auth | — | n/a | accept | No network, no auth (local-first) — no applicable threat |
</threat_model>

<verification>
- `npx vitest run src/services/widget/widget-data.test.ts src/db/favourites-dao.test.ts src/navigation/widget-linking.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
- `grep -rn 'ManageFavourites\|rewriteFavouriteRanks' src` (must be empty)
</verification>

<success_criteria>
The widget renders Favorites in Default order; the Manage-favourites screen + reorder logic + rank-rewrite write + route + deep-link are removed/re-pointed; the `favourite_rank` column and its internal picker reads survive; binary favourites membership is intact.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-06-SUMMARY.md` when done.
</output>