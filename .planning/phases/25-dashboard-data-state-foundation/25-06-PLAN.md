---
phase: 25-dashboard-data-state-foundation
plan: 06
type: execute
wave: 3
depends_on: [25-02]
autonomous: true
requirements: [DASHQ-04]
files_modified:
  - src/services/widget/widget-data.ts
  - src/services/widget/widget-data.test.ts
  - src/db/favourites-dao.ts
  - src/db/favourites-dao.test.ts
  - src/db/data-revision-dao.test.ts
  - src/db/ring-seq-dao.ts
  - src/db/ring-seq-dao.test.ts
  - src/logic/favourites-reorder-logic.ts
  - src/logic/favourites-reorder-logic.test.ts
  - src/screens/ManageFavouritesScreen.tsx
  - src/services/widget/widget-quick-action-guard.ts
  - src/services/widget/widget-quick-action-guard.test.ts
  - src/navigation/widget-linking.ts
  - src/navigation/widget-linking.test.ts
  - src/navigation/focused-route-classification.test.ts
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/types.ts
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The favourites home-screen widget renders the Favorites population in its Default ordering (the shared Dashboard relationship-health order), NOT by `favourite_rank` (ADR-075 / ADR-043 consequence). [DASHQ-04]"
    - "The Manage-favourites drag-reorder screen and its rank-rewrite write are retired: `ManageFavouritesScreen.tsx` and `favourites-reorder-logic.ts` are removed, `favourites-dao` keeps only the binary mark/clear writes, and the `orbit://favourites` widget deep-link no longer routes to a Manage-favourites screen. [DASHQ-04]"
    - "Re-pointing `orbit://favourites` compiles AND runs: a NEW Home-only `WidgetNavIntent` variant (`type:'reset', index:0, routes:[{name:'Home'}]`) is added — because the existing union permits only `index:1` two-route tuples and BOTH `widget-quick-action-guard.ts` and the `WidgetLinkingGate` flush read `intent.routes[1]` unconditionally, so a bare `[{name:'Home'}]` would fail `tsc` and dereference `undefined` at runtime. `guardWidgetIntent` accepts the Home-only variant BEFORE reading `routes[1]`, and the gate resets to `resetToDashboardRoot()` for it. [DASHQ-04]"
    - "The `favourite_rank` COLUMN is KEPT (not dropped): the capture / sun-picker / merge-candidate / picker reads that internally ORDER BY `favourite_rank` continue to compile and function; only the Dashboard/widget stop treating rank as a user-facing order (D-04 trip-wire — the column's fate is documented, not blind-dropped). [DASHQ-04]"
    - "Favourites remains binary membership everywhere; toggling membership (Profile/List/Card star) still works via the retained mark/clear writes, with no user-visible ordering surface anywhere. [DASHQ-04]"
  prohibitions:
    - statement: "Do NOT drop the `favourite_rank` column — it is still read (ORDER BY) by capture-read, sun-picker-read, merge-candidate-read, and picker-read; dropping it without a decided fate for those reads is a D-04 trip-wire. The column is kept; the Dashboard/widget simply stop sorting by it."
      status: enforced
      verification: "No migration is added; `grep -rn 'favourite_rank' src/db/capture-read.ts src/db/sun-picker-read.ts src/db/merge-candidate-read.ts src/db/picker-read.ts` still shows their internal ORDER BY intact; the widget no longer orders by rank."
    - statement: "Ranked Favorites is a retired product concept (ADR-075, supersedes ADR-033) — no drag-reorder surface, no rank-rewrite transaction, and no Dashboard/widget order-by-rank anywhere. Repurposing Manage favourites as a binary list was already rejected."
      status: enforced
      verification: "ManageFavouritesScreen.tsx + favourites-reorder-logic.ts are removed; favourites-dao no longer exports rewriteFavouriteRanks; the ManageFavourites route is removed from navigation; widget-data.test.ts asserts Default order."
    - statement: "This is a consumer-repointing retirement, not a blind delete, and the sweep is COMPLETE against the real on-disk consumers (the review found the prior list both incomplete and pointing at phantoms). The REAL consumers of the `ManageFavourites` route name / `rewriteFavouriteRanks` export are: `widget-linking.ts` (deep-link + reset-intent `routes` + the WidgetNavIntent union + the gate's `routes[1]` reads), `widget-quick-action-guard.ts:33` (`if (target.name === \"ManageFavourites\")` functional guard) + its test, `DashboardStack.tsx` (Stack.Screen + import), `navigation/types.ts` (route param + the `:80` doc-comment mention), `focused-route-classification.test.ts:40` (browseRoutes array literal), `data-revision-dao.test.ts:13` (imports + calls `rewriteFavouriteRanks`), `favourites-reorder-logic.test.ts` (tests the deleted module), AND — VERIFIED on disk, the cycle-2 grep-gate gap — two DOC-COMMENT references to `rewriteFavouriteRanks` that keep the grep gate from ever passing: `ring-seq-dao.ts:4` (\"A near-VERBATIM clone of `rewriteFavouriteRanks` (favourites-dao.ts:105-143)\" — the parenthetical is ALSO a dangling line ref once the function is deleted) and `ring-seq-dao.test.ts:6` (\"a near-verbatim clone of rewriteFavouriteRanks\"). Both comments are updated (drop the symbol name / fix the dangling `favourites-dao.ts:105-143` ref) so `grep -rn 'rewriteFavouriteRanks' src` returns empty. There is NO `ManageFavourites` reference in `SettingsScreen.tsx` or `HomeScreen.tsx` (grep=0 — the earlier plan's Settings/HomeScreen 'Manage link' targets were phantoms and are dropped). Every real consumer is re-pointed or removed atomically so no navigation targets a deleted route and no test/import references a removed symbol."
      status: enforced
      verification: "widget-linking.test.ts asserts orbit://favourites resolves to a live target (Home), not a deleted route; `grep -rn 'ManageFavourites' src` and `grep -rn 'rewriteFavouriteRanks' src` both return empty AFTER the sweep (test files AND ring-seq-dao comments included); `npx tsc --noEmit` and full `npx vitest run` clean."
  artifacts:
    - "widget-data.ts: Favorites-population Default-order projection (replaces rank-ordered read)"
    - "favourites-dao.ts: mark/clear binary writes retained; rewriteFavouriteRanks removed"
    - "Removed: ManageFavouritesScreen.tsx, favourites-reorder-logic.ts, favourites-reorder-logic.test.ts, the ManageFavourites route"
    - "widget-linking.ts: NEW Home-only `WidgetNavIntent` variant (`type:'reset', index:0, routes:[{name:'Home'}]`); `resolveWidgetUri('orbit://favourites')` returns it; the `WidgetLinkingGate` flush branches on it → `resetToDashboardRoot()`"
    - "Updated consumers: widget-quick-action-guard.ts (+ its test — accept the Home-only intent before reading routes[1], drop ManageFavourites branch), focused-route-classification.test.ts (drop ManageFavourites), data-revision-dao.test.ts (drop rewriteFavouriteRanks usage), ring-seq-dao.ts + ring-seq-dao.test.ts (doc-comment sweep of the `rewriteFavouriteRanks` name + dangling line ref), navigation/types.ts:80 (stale doc-comment mention), and widget-linking.ts's own header comments (line 13 `orbit://favourites → ManageFavourites` + the `WidgetNavIntent` type doc — stale once the Home-only variant lands)"
  key_links:
    - "widget-data → the Plan-02 Favorites-population Default-order read (the new ordering source, ADR-043 consequence)"
    - "orbit://favourites deep-link reset → re-pointed to [Home] (no Manage-favourites screen)"
    - "widget-quick-action-guard + focused-route-classification.test + data-revision-dao.test → real consumers of the retired route/export (review HIGH-5), swept atomically"
    - "favourite_rank column KEPT → capture/sun/merge/picker internal reads (unbroken)"
---

<objective>
Retire ranked Favourites per ADR-075: remove the Manage-favourites drag-reorder screen + its rank-rewrite write, re-point the widget from `favourite_rank` order to the Favorites-population Default order, and re-point/remove every consumer of the retired screen and deep-link — while KEEPING the `favourite_rank` column (its picker/sun/merge/capture reads still use it internally).

Purpose: DASHQ-04 (favourites binary, Dashboard never sorts by rank). This is a deletion-with-consumers (D-04 trip-wire): re-point, do not blind-delete, and do not drop the column.

Output: widget projects Favorites in Default order; Manage-favourites screen + reorder logic + rank-rewrite write + route + deep-link removed/re-pointed; column preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record + governing ADR
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/decisions/ADR-075-binary-favourite-membership-without-a-user-facing-order.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the widget to the Favorites-population Default order</name>
  <files>src/services/widget/widget-data.ts, src/services/widget/widget-data.test.ts</files>
  <read_first>
    - src/services/widget/widget-data.ts (READ IN FULL: the current `listDashboard({filter:'favourites'})` read truncated by `favourite_rank ASC` at 52/75/83–86 — the ordering source being changed)
    - src/db/dashboard-read.ts (the Plan-02 Favorites-population Default-order read the widget re-points to; the legacy listDashboard favourites branch)
    - docs/decisions/ADR-075-...md (Implementation: "widget-data.ts — projects the Favorites population in Default ordering instead of manual rank")
    - docs/decisions/ADR-043 reference in RESEARCH (the widget-ordering consequence)
  </read_first>
  <action>
    Change `widget-data.ts` to source the favourites widget tiles from the Plan-02 Favorites-population Default-order read — the REAL signature is `listDashboardPopulation(exec, { populations: ['favourites'], filters: {}, sort: 'default' }, now)` (`(exec, query, now)`, NOT `listDashboardPopulation(['favourites'])`) — instead of `favourite_rank ASC`. The 3-arg `(exec, query, now)` form is introduced by **Plan 02** — the wave-2 plan this plan `depends_on: [25-02]` — so this call compiles against a signature that exists by wave 3; `widget-data.ts` (a service, not a node-pure read module) supplies `now` (pin a fixed `now` in the widget-data test so the Default relationship-health ordering is deterministic). Do NOT re-point the frozen legacy `listDashboard`/`listFavourites` branch (Plan 02 freezes it; that fallback is dropped). The membership set is unchanged (favourite_rank IS NOT NULL); only the ORDER changes to the shared Dashboard Default (relationship-health) order, truncated to the widget's tile count. Do NOT read or order by `favourite_rank`. Update widget-data.test.ts to assert Default ordering (not rank order) and that membership is still the favourites set, pinning a fixed `now` in the test so the Default (relationship-health) ordering is deterministic.
  </action>
  <verify>
    <automated>npx vitest run src/services/widget/widget-data.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - widget-data.test.ts asserts the favourites tiles are ordered by the Default relationship-health order, not `favourite_rank`.
    - `grep -n 'favourite_rank' src/services/widget/widget-data.ts` returns no ordering use (membership may be expressed via the read, but no rank ORDER BY remains).
    - Membership is still the favourites set; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The widget renders the Favorites population in Default order; no rank ordering remains in widget-data.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the rank-rewrite write + the Manage-favourites screen/logic (keep the column + mark/clear)</name>
  <files>src/db/favourites-dao.ts, src/db/favourites-dao.test.ts, src/db/data-revision-dao.test.ts, src/db/ring-seq-dao.ts, src/db/ring-seq-dao.test.ts, src/logic/favourites-reorder-logic.ts, src/logic/favourites-reorder-logic.test.ts, src/screens/ManageFavouritesScreen.tsx</files>
  <read_first>
    - src/db/favourites-dao.ts (READ IN FULL: setFavouriteRank/clearFavouriteRank — the binary mark/clear writes to KEEP; rewriteFavouriteRanks — the rank-rewrite transaction to REMOVE; the changes===1 guard idiom; note lines 105–143 are the range `ring-seq-dao.ts:4` cites — that ref goes dangling once the function is gone)
    - src/db/ring-seq-dao.ts (line 4 doc comment: "A near-VERBATIM clone of `rewriteFavouriteRanks` (favourites-dao.ts:105-143)" — a doc reference to the SYMBOL being deleted, NOT a call site; the clone is `rewriteRingSeq` and is UNCHANGED, only the comment is updated)
    - src/db/ring-seq-dao.test.ts (line 6 doc comment: "rewriteRingSeq is a near-verbatim clone of rewriteFavouriteRanks" — same, comment-only)
    - src/db/data-revision-dao.test.ts (READ IN FULL: line 13 imports `setFavouriteRank, rewriteFavouriteRanks`; lines 117–128 use `rewriteFavouriteRanks` to prove the data-revision counter advances on a real reorder and no-ops on an empty one — this consumer breaks when the export is removed)
    - src/logic/favourites-reorder-logic.ts (computeReorder — the drag→order permutation, REMOVED per ADR-075)
    - src/logic/favourites-reorder-logic.test.ts (tests computeReorder — the deleted module's test, REMOVED with it)
    - src/screens/ManageFavouritesScreen.tsx (the drag-reorder surface, REMOVED)
    - docs/decisions/ADR-075-...md (Implementation: favourites-dao keeps mark/clear, the rank-rewrite write is retired; ManageFavouritesScreen + favourites-reorder-logic retired)
    - CLAUDE.md (favourites binary-membership invariants; do NOT drop the column)
  </read_first>
  <action>
    Remove `rewriteFavouriteRanks` from `favourites-dao.ts` (and its dedicated cases in favourites-dao.test.ts), KEEPING `setFavouriteRank`/`clearFavouriteRank` (binary mark/clear) unchanged — membership toggling stays. Delete `src/logic/favourites-reorder-logic.ts` AND its test `src/logic/favourites-reorder-logic.test.ts`, and `src/screens/ManageFavouritesScreen.tsx`. Update `src/db/data-revision-dao.test.ts`: drop the `rewriteFavouriteRanks` import and rewrite the "empty reorder is a no-op / real reorder advances once" case to exercise the data-revision counter through a STILL-EXISTING mutating writer (e.g. `setFavouriteRank`/`clearFavouriteRank`, or another data-revision-bumping DAO already covered) so the counter-advances-on-write and no-op semantics stay tested without the retired function. Do NOT add a migration and do NOT drop the `favourite_rank` column — its capture/sun/merge/picker internal reads still use it (Task 3 leaves those intact). In favourites-dao's in-file docs, record that rank-as-order is retired (ADR-075) but the column persists as vestigial storage for the internal picker reads. (Route + navigation removal is Task 3.)

    Sweep the two DOC-COMMENT references that otherwise keep the `grep -rn 'rewriteFavouriteRanks' src` gate from passing (VERIFIED on disk; comment-only, no behaviour change): in `src/db/ring-seq-dao.ts:4` and `src/db/ring-seq-dao.test.ts:6`, reword the header comments so they no longer contain the literal `rewriteFavouriteRanks` (e.g. "the retired favourites rank-rewrite writer") and DROP the dangling `favourites-dao.ts:105-143` line reference in `ring-seq-dao.ts:4` (that range no longer exists once the function is deleted). Do NOT touch `rewriteRingSeq` itself or any ring-seq behaviour — this is a documentation edit only.
  </action>
  <verify>
    <automated>npx vitest run src/db/favourites-dao.test.ts src/db/data-revision-dao.test.ts src/db/ring-seq-dao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'rewriteFavouriteRanks' src` returns no match (the `ring-seq-dao.ts:4` + `ring-seq-dao.test.ts:6` doc comments are reworded and the dangling `favourites-dao.ts:105-143` ref dropped); `favourites-reorder-logic.ts`, `favourites-reorder-logic.test.ts`, and `ManageFavouritesScreen.tsx` no longer exist; `rewriteRingSeq` and the ring-seq tests are unchanged and green.
    - `setFavouriteRank`/`clearFavouriteRank` remain and their tests pass; `data-revision-dao.test.ts` no longer imports `rewriteFavouriteRanks` and still asserts the counter advances on a real write and no-ops appropriately.
    - No migration was added and the `favourite_rank` column still exists (`grep -rn 'ADD COLUMN\|DROP COLUMN' src/db/migrations` shows no new favourite change).
    - `npx tsc --noEmit` — note it may still report the not-yet-removed route consumers; those are removed in Task 3 of THIS plan, so run the plan's tasks in order and gate the full tsc at Task 3.
  </acceptance_criteria>
  <done>The rank-rewrite write + drag-reorder screen + reorder logic are removed; binary mark/clear and the column are kept.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point/remove the retired-route consumers (route, deep-link, quick-action guard, route-classification test)</name>
  <files>src/navigation/widget-linking.ts, src/navigation/widget-linking.test.ts, src/services/widget/widget-quick-action-guard.ts, src/services/widget/widget-quick-action-guard.test.ts, src/navigation/focused-route-classification.test.ts, src/navigation/tabs/DashboardStack.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/navigation/widget-linking.ts (READ IN FULL — VERIFIED cycle-2 break: the `WidgetNavIntent` union at 61–93 permits ONLY `index:1` two-route tuples (the last variant, ~89–93, is `[{name:'Home'},{name:'ManageFavourites'}]`); `resolveWidgetUri`'s favourites branch returns it at 144–150; the `WidgetLinkingGate` flush reads `pending.routes[1]` at 301 and `guarded.intent.routes[1]` at 326 UNCONDITIONALLY, and derives `isReachIntent` from `target.name` at 302–305. A bare `routes:[{name:'Home'}]` therefore fails `tsc` AND dereferences `undefined` at runtime — a Home-only variant + gate branch is required.)
    - src/navigation/reset-intents.ts (READ IN FULL: `resetToDashboardRoot()` is the Home-only reset shape (index 0, `[{name:'Home'}]`); `resetToDashboardWith(target)` is the two-route shape — the gate uses the FORMER for the Home-only intent)
    - src/navigation/widget-linking.test.ts (line 57/62: asserts orbit://favourites RESETs onto [Home, ManageFavourites] — update to assert the Home-only intent shape)
    - src/services/widget/widget-quick-action-guard.ts (READ IN FULL: line 32 `const target = intent.routes[1]` is read UNCONDITIONALLY, then line 33 `if (target.name === "ManageFavourites")`. A Home-only intent (one route) makes `target` undefined → `target.name` throws; the Home-only case MUST be handled BEFORE line 32)
    - src/services/widget/widget-quick-action-guard.test.ts (READ IN FULL: the existing guard cases — add a Home-only-intent case asserting `{ok:true}` with no contact lookup; update/remove the ManageFavourites case)
    - src/navigation/focused-route-classification.test.ts (line 40: the `browseRoutes` array literal lists `"ManageFavourites"` — remove that element; note line 38 `"NeverContacted"` is retired by Plan 07, not here — leave it for Plan 07)
    - src/navigation/tabs/DashboardStack.tsx (line 13 import + lines 68–69 the ManageFavourites Stack.Screen registration to remove)
    - src/navigation/types.ts (line 75 the ManageFavourites route param entry + the `:80` doc-comment mention of `ManageFavourites`/`NeverContacted` to remove/adjust)
  </read_first>
  <action>
    Remove the `ManageFavourites` route from `DashboardStack.tsx` (Stack.Screen + import) and its param entry from `navigation/types.ts` (and the stale `:80` doc-comment mention of `ManageFavourites`).

    Re-point `orbit://favourites` PROPERLY (the cycle-2 HIGH — a bare array edit does NOT compile or run): (1) in `widget-linking.ts`, ADD a Home-only variant to the `WidgetNavIntent` union — `{ type:"reset"; index:0; routes:[{ name:"Home" }] }` — and REMOVE the `[{name:'Home'},{name:'ManageFavourites'}]` variant. (2) Make `resolveWidgetUri`'s `FAVOURITES_URI` branch return the new Home-only intent (`index:0`, one route). (3) In `guardWidgetIntent` (`widget-quick-action-guard.ts`), handle the Home-only variant FIRST — branch on `intent.index === 0` (or `intent.routes.length === 1`) and `return { ok:true, intent }` BEFORE the `const target = intent.routes[1]` read at line 32, and delete the now-dead `if (target.name === "ManageFavourites")` branch. (4) In the `WidgetLinkingGate` flush effect, branch on the Home-only intent BEFORE reading `pending.routes[1]` (301) / deriving `isReachIntent` (302–305): on a successful guard of a Home-only intent, `navigationRef.current?.reset(resetToDashboardRoot())` and `setPending(null)`, skipping the `resetToDashboardWith(guarded.intent.routes[1])` path (326). (The Favorites population is the intended destination; the render phases add its explicit chip + wire the deep-link to activate that population — until then Home is a live nav target, no crash.)

    Remove `"ManageFavourites"` from the `browseRoutes` array in `focused-route-classification.test.ts` (do NOT touch `"NeverContacted"` — Plan 07 owns that). Update `widget-linking.test.ts` (57/62) to assert `orbit://favourites` resolves to the Home-only intent (`type:'reset', index:0, routes:[{name:'Home'}]`). Update `widget-quick-action-guard.test.ts`: add a case asserting the Home-only intent guards to `{ok:true}` WITHOUT any contact lookup, and remove/replace the ManageFavourites case. Do NOT edit `SettingsScreen.tsx` or `HomeScreen.tsx` — neither references `ManageFavourites` (grep=0; the earlier phantom targets are dropped). This task is where the FULL `tsc` + `vitest` gate must go green (all references to the removed screen/route/export gone, and the Home-only intent typechecks + guards + flushes).
  </action>
  <verify>
    <automated>npx vitest run src/navigation/widget-linking.test.ts src/services/widget/widget-quick-action-guard.test.ts src/navigation/focused-route-classification.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'ManageFavourites' src` returns no match (route registration, deep-link reset targets, the WidgetNavIntent variant, quick-action-guard branch, types param + `:80` comment, and the route-classification test literal all removed/re-pointed) — the gate passes because the real consumers (guard + test + intent union) are handled, not just the screen.
    - `WidgetNavIntent` gains the Home-only variant (`index:0`, one route); `resolveWidgetUri('orbit://favourites')` returns it; `guardWidgetIntent` returns `{ok:true}` for it WITHOUT a contact lookup (branch precedes the `routes[1]` read); the gate flush resets to `resetToDashboardRoot()` for it. `npx tsc --noEmit` is clean (the one-route intent typechecks — no `routes[1]` on an `index:0` intent is dereferenced).
    - widget-linking.test.ts asserts `orbit://favourites` resolves to the Home-only intent (`index:0`, `routes:[{name:'Home'}]`), never a deleted route; widget-quick-action-guard.test.ts asserts the Home-only guard case.
    - Full `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>Every retired-screen consumer is re-pointed or removed atomically; no navigation targets a deleted route; the column and binary favourites survive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget deep-link (orbit://) → navigation | An OS-delivered deep link must resolve to a live route, not a deleted one |
| retired screen consumers → navigation | Removing a route must not strand a navigate() call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.6-01 | Denial of Service (crash) | orbit://favourites deep-link to deleted route | medium | mitigate | Deep-link re-pointed to a live target via a NEW Home-only `WidgetNavIntent` variant (index:0) atomically with the route removal; `guardWidgetIntent` + the gate flush handle the one-route intent BEFORE any `routes[1]` read (no `undefined` deref); widget-linking + guard tests assert live resolution + safe guarding |
| T-25.6-02 | Tampering (data integrity) | dropping favourite_rank column | high | mitigate | Column is KEPT (no migration); capture/sun/merge/picker internal reads unbroken; the D-04 trip-wire honoured |
| T-25.6-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
| T-25.6-03 | Network / Auth | — | n/a | accept | No network, no auth (local-first) — no applicable threat |
</threat_model>

<verification>
- `npx vitest run src/services/widget/widget-data.test.ts src/db/favourites-dao.test.ts src/navigation/widget-linking.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
- `grep -rn 'ManageFavourites\|rewriteFavouriteRanks' src` (must be empty)
</verification>

<success_criteria>
The widget renders Favorites in Default order; the Manage-favourites screen + reorder logic + rank-rewrite write + route + deep-link are removed/re-pointed; the `favourite_rank` column and its internal picker reads survive; binary favourites membership is intact.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-06-SUMMARY.md` when done.
</output>