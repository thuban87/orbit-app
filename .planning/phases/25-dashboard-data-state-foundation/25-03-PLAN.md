---
phase: 25-dashboard-data-state-foundation
plan: 03
type: execute
wave: 3
depends_on: [25-02]
autonomous: true
requirements: [DASHQ-06, DASHQ-07]
files_modified:
  - src/logic/dashboard-query-logic.ts
  - src/logic/dashboard-query-logic.test.ts
  - src/logic/dashboard-gravity-filter.ts
  - src/logic/dashboard-gravity-filter.test.ts
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 53000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The five filter families — Category, Social Battery, Needs Attention, Gravity/Closeness, Contact Frequency — combine OR-within-a-family and AND-across-families, proven by a test asserting `(Family OR Friends) AND (Charger OR Neutral) AND Needs-Attention` membership. [DASHQ-06]"
    - "Filters survive a population change: changing the selected population does not clear the active filters. [DASHQ-06]"
    - "Category is filtered on `c.category_id`, Social Battery on `c.social_battery`, Contact Frequency on `c.interval_days`, and Needs Attention on `(PROGRESS_SQL) >= STABLE_MAX` — all SQL-expressible over existing columns/fragments, every runtime value `?`-bound. [DASHQ-06]"
    - "Gravity/Closeness is resolved as a POST-QUERY TypeScript filter over the candidate id set (reusing `computeContactGravity` + `GRAVITY_TIERS`), NOT a SQL WHERE clause and NOT a new cached column — there is no gravity column and a WHERE clause would silently match nothing (Pitfall 3). [DASHQ-06]"
    - "Sort exposes Default, Name A–Z, Name Z–A, Least Recently Contacted, Most Recently Contacted, and Relationship Status; Default is population-aware and an explicit sort survives until the user resets to Default. [DASHQ-07]"
    - "The literal `'default'` sentinel is persisted (never a resolved population-specific ordering); Default resolves to a concrete ORDER BY at query-build time per population. [DASHQ-07]"
  prohibitions:
    - statement: "Gravity is NEVER a SQL WHERE clause and NEVER a new cached/stored column — a cached gravity column is a one-way schema door reserved for the owner; the reversible post-query TS filter is used (RESEARCH A2 / Open Q1)."
      status: enforced
      verification: "dashboard-gravity-filter.ts is a pure TS post-query filter over candidate ids reusing computeContactGravity; no migration is added; `grep -i 'gravity' src/db/migrations` returns no new column; the choice is recorded as an <assumption> in this plan."
    - statement: "Arbitrary custom-field filtering is out of scope — only the five settled filter families ship; no custom-field predicate is added, and any future custom-field sort/filter would route through the single `sortExpr()` helper (never an interpolated identifier)."
      status: enforced
      verification: "dashboard-read.ts adds no custom-field filter predicate; the filter families are the closed set {category, social-battery, needs-attention, gravity, contact-frequency}."
    - statement: "Advanced filter families beyond the initial five (contact-method presence, remembered-info presence, AI-enabled data, recently-updated) are deferred, not built."
      status: enforced
      verification: "The DashboardFilterFamily union is exactly the five families; no additional predicate is present."
    - statement: "The Sort option list is NOT dynamically rewritten per population — `Default` stays a single semantic option resolved per population at query-build time (dossier Notes for GSD)."
      status: enforced
      verification: "dashboard-query-logic exposes one fixed DashboardSortMode union including 'default'; resolveDefaultSort maps 'default' to a concrete ORDER BY without changing the option set."
  artifacts:
    - "dashboard-query-logic.ts: AND-across/OR-within filter composer for the four SQL-expressible families + the sort model"
    - "dashboard-gravity-filter.ts: pure post-query TS Gravity/Closeness filter over candidate ids (reuses computeContactGravity + GRAVITY_TIERS)"
    - "dashboard-read.ts: filter predicates ANDed onto the population WHERE + full sort ORDER BY + Gravity post-query pass"
  key_links:
    - "dashboard-query-logic filter composer → dashboard-read WHERE (AND-across families, OR-within a family)"
    - "impact.ts computeContactGravity + GRAVITY_TIERS → dashboard-gravity-filter (reuse the owner-approved tunables; single tuning surface)"
    - "resolveDefaultSort (population-aware) → dashboard-read ORDER BY (Default resolved at query build, explicit sort passthrough)"
---

<objective>
Add the filter and sort halves of the shared query contract: the five filter families (OR-within, AND-across, surviving population change), the six sort modes with population-aware Default, and the Gravity/Closeness filter resolved as a reversible post-query TypeScript pass over the candidate id set — never a SQL clause and never a new stored column.

Purpose: With populations (Plan 02) this completes the browse-side eligible-set contract the render phases (26–28) consume. Gravity's non-column nature is the phase's one genuine architecture fork (Open Q1); this plan takes the reversible path and records it.

Output: `dashboard-query-logic.ts` gains the filter composer + sort model; `dashboard-gravity-filter.ts` (new) is the post-query Gravity pass; `dashboard-read.ts`'s population read gains the AND-ed filter predicates + full ORDER BY + the Gravity id-scoping pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-04-planning-notes.md
</context>

<assumption>
Gravity/Closeness (DASHQ-06) is implemented as a POST-QUERY TypeScript filter over the candidate id set, reusing `computeContactGravity` + `GRAVITY_TIERS` from `services/impact.ts` — NOT a new cached `contacts` column. This is the reversible path (RESEARCH A2 / Open Q1); a cached gravity column is a one-way schema door in the owner's bucket. Chosen without an owner checkpoint per the planning-context guidance ("PREFER the reversible path … A cached Gravity column is a one-way schema door — do NOT add it without an owner checkpoint"). Perf is assessable only on the physical Pixel (Manual-Only verification in 25-VALIDATION.md); if it profiles as expensive on-device, escalating to a cached column is a separate owner decision.
</assumption>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter composer (four SQL families) + full sort model</name>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts</files>
  <behavior>
    - `buildFilterWhere(filters)` composes OR-within each family and AND-across families into a `{ sql, params }` fragment, with every runtime value `?`-bound.
    - Category → `c.category_id IN (?, …)`; Social Battery → `c.social_battery IN (?, …)`; Contact Frequency → an `interval_days` band predicate per selected frequency bucket; Needs Attention → `(PROGRESS_SQL) >= STABLE_MAX`.
    - Gravity family is recognized by the composer but produces NO SQL (returns an empty fragment) — it is handled post-query.
    - Sort model: the fixed `DashboardSortMode` union {default, name-asc, name-desc, least-recent, most-recent, status}; `resolveDefaultSort` already population-aware; an explicit sort passes through.
    - Filters are independent of populations (a filters object composes the same regardless of the population set).
  </behavior>
  <read_first>
    - src/logic/dashboard-query-logic.ts (Plan 01/02 surfaces: DashboardFilterFamily, DashboardFilters, buildPopulationWhere, resolveDefaultSort)
    - src/db/dashboard-read.ts (the needs-attention branch `(PROGRESS_SQL) >= STABLE_MAX` at 253–255; the category/battery `?`-bound predicates at 256–262; the SORT map 160–170)
    - src/types.ts (SocialBattery = 'Charger'|'Neutral'|'Drain')
    - src/db/status.ts (PROGRESS_SQL/STATUS_SQL/STABLE_MAX)
    - docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md §G (filter semantics: OR-within/AND-across; the `(Family OR Friends) AND (Charger OR Neutral) AND Needs-Attention` example) and §H (sort)
  </read_first>
  <action>
    Add `buildFilterWhere(filters)` to `dashboard-query-logic.ts`: for each present family build an OR-of-values group and AND the groups. Category values are numeric ids bound as `c.category_id IN (…?)`; Social Battery values bound as `c.social_battery IN (…?)`; Contact Frequency maps each selected frequency-bucket token to an `interval_days` band (define the closed bucket→band mapping as a code-constant, e.g. weekly/monthly/quarterly/yearly bands over `c.interval_days`, bounds `?`-bound); Needs Attention appends the code-constant `(PROGRESS_SQL) >= STABLE_MAX`. The `gravity` family is enumerated but `buildFilterWhere` returns no SQL for it (documented in-file: gravity is post-query). Never interpolate a runtime value into the SQL — bind everything; the family keys and column names are the only interpolated tokens and they are closed code-constants. Confirm the `DashboardSortMode` union and `resolveDefaultSort` cover all six sort options. Add tests for OR-within/AND-across composition, the worked dossier example, gravity-produces-no-SQL, and filters-independent-of-population.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-query-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A test builds `{category:[1,2], social-battery:['Charger','Neutral'], needs-attention:['on']}` and asserts the SQL ANDs three OR-groups with 4 bound params + the needs-attention fragment.
    - The `gravity` family yields an empty SQL fragment from `buildFilterWhere`.
    - The DashboardSortMode union is exactly the six options; `resolveDefaultSort` maps each population's Default to a concrete token.
    - No runtime value is interpolated (params array carries every id/band bound); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The four SQL-expressible filter families compose OR-within/AND-across, gravity is deferred to post-query, and the six-option sort model resolves Default per population.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gravity/Closeness post-query TS filter (reversible, no column)</name>
  <files>src/logic/dashboard-gravity-filter.ts, src/logic/dashboard-gravity-filter.test.ts</files>
  <behavior>
    - `filterByGravity(candidateIds, selectedTiers, loadInputs, now)` returns the subset of candidate ids whose computed gravity tier is in `selectedTiers`.
    - Tiers come from `GRAVITY_TIERS` (thin/building/solid/deep); computation reuses `computeContactGravity` over each candidate's impact inputs.
    - An empty `selectedTiers` (gravity family not filtered) returns all candidate ids unchanged.
    - A never-contacted candidate (raw 0) lands on the `thin` tier (not dropped/garbage).
  </behavior>
  <read_first>
    - src/services/impact.ts (READ IN FULL: computeContactGravity, GRAVITY_TIERS thin/building/solid/deep, the derived-never-stored comment naming the cached-column fallback as owner-bucket, getImpactInputs/loadContactGravity)
    - src/services/gravity-logic.ts (computeGravity, GravityTier, GravityResult shapes)
    - src/db/impact-read.ts (getImpactInputs — the batch read of interaction rows per contact)
    - src/logic/dashboard-query-logic.ts (DashboardFilters — the gravity family's selected tier tokens)
  </read_first>
  <action>
    Create `src/logic/dashboard-gravity-filter.ts` as a pure orchestration module (node-testable; it may import `impact.ts`/`gravity-logic.ts` which are node-pure). Export `filterByGravity(candidateIds, selectedTiers, loadInputs, now)` where `loadInputs` is an injected reader (`(id) => Promise<ImpactInputs | null>`) so the filter stays node-testable without a live DB. For each candidate id, load its impact inputs, call `computeContactGravity`, map to its tier name, and keep the id iff its tier is in `selectedTiers`. Return ids in the input order (the post-query pass preserves the SQL ORDER BY). Add NO migration and NO `contacts` column. Document in-file that this is the reversible path (RESEARCH A2) and that a cached column is an owner decision. Tests: tier membership filtering, empty-tiers passthrough, never-contacted→thin, and input-order preservation.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-gravity-filter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `filterByGravity` returns only candidates whose computed tier is selected; empty selection returns all; order preserved.
    - The module adds no migration and no `contacts` column (`grep -i 'ALTER TABLE\|ADD COLUMN' src/logic/dashboard-gravity-filter.ts` returns nothing).
    - A never-contacted candidate maps to the `thin` tier.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Gravity/Closeness is a reversible post-query TS filter reusing the owner-approved tunables, with no new stored derived state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire filters + sort + gravity pass into the dashboard-read population read</name>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts</files>
  <behavior>
    - `listDashboardPopulation` ANDs `buildFilterWhere(query.filters)` onto the population WHERE and orders by `SORT[resolveDefaultSort(query.sort, query.populations)]`.
    - When the gravity family has selected tiers, the read applies `filterByGravity` over the returned candidate ids (post-query), preserving the SQL order.
    - Filters survive a population change (the same filters object composes across populations).
  </behavior>
  <read_first>
    - src/db/dashboard-read.ts (READ IN FULL: the Plan-02 listDashboardPopulation, buildPopulationWhere usage, CARD_STATUS, SORT, the injection posture)
    - src/logic/dashboard-query-logic.ts (buildFilterWhere, resolveDefaultSort from Task 1)
    - src/logic/dashboard-gravity-filter.ts (filterByGravity from Task 2)
    - src/db/impact-read.ts (getImpactInputs — the loader passed into filterByGravity for the live read)
  </read_first>
  <action>
    In `listDashboardPopulation`, AND `buildFilterWhere(query.filters).sql` onto the population WHERE (concatenating params in order), and set ORDER BY from `resolveDefaultSort`. After the SQL read returns rows, if `query.filters.gravity` has tiers, pass the row ids through `filterByGravity(ids, tiers, id => getImpactInputs(exec, id), now)` and keep only surviving rows in their existing order (the post-query pass, per the Gravity assumption). Keep the injection posture: static SQL string, `?`-bound values only. Do NOT touch the legacy `listDashboard`. Extend dashboard-read.test.ts: the worked `(Family OR Friends) AND (Charger OR Neutral) AND Needs-Attention` example; filters-survive-population-change; a gravity-tier filter drops a low-gravity contact while keeping a high-gravity one; each sort option orders correctly; Default is population-aware (Birthdays soonest-first vs Active status).
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts src/logic/dashboard-query-logic.test.ts src/logic/dashboard-gravity-filter.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-read.test.ts asserts the worked `(Family OR Friends) AND (Charger OR Neutral) AND Needs-Attention` membership; filters persist across a population change.
    - A gravity-tier filter drops a low-gravity contact and keeps a high-gravity one (via the post-query pass); the SQL order is preserved among survivors.
    - Each of the six sort options orders correctly; Default resolves to status for Active and soonest-first for Birthdays.
    - Full `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>Filters (five families, OR-within/AND-across, surviving population change), the six-option sort model, and the reversible Gravity post-query pass are wired into the shared population read; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filter/sort selection → SQL | Selected filter values and sort mode drive the narrowing WHERE + ORDER BY |
| candidate ids → gravity TS pass | Post-query gravity reads each candidate's interaction log in TS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.3-01 | Tampering | filter value → SQL injection | high | mitigate | Every category id / battery value / frequency band bound as `?`; family keys + column names are closed code-constants; no interpolated identifier (the shipped dashboard-read posture preserved) |
| T-25.3-02 | Denial of Service (perf) | gravity post-query pass | medium | accept | O(interactions) per filtered candidate; reversible-by-design, assessed on the physical Pixel (25-VALIDATION Manual-Only); escalation to a cached column is a separate owner decision |
| T-25.3-03 | Access Control (data-scope) | filtered result | low | mitigate | Filters only narrow within the population's already archived/unbound-excluded universe; no relaxation of the exclusion |
| T-25.3-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
</threat_model>

<verification>
- `npx vitest run src/db/dashboard-read.test.ts src/logic/dashboard-query-logic.test.ts src/logic/dashboard-gravity-filter.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
</verification>

<success_criteria>
The five filter families combine OR-within/AND-across and survive population change; Gravity is a reversible post-query TS filter (no column); the six-option sort model resolves population-aware Default and persists the 'default' sentinel; full suite green.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-03-SUMMARY.md` when done.
</output>