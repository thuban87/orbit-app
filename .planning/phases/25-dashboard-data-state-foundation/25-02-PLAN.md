---
phase: 25-dashboard-data-state-foundation
plan: 02
type: execute
wave: 2
depends_on: [25-01]
autonomous: true
requirements: [DASHQ-02, DASHQ-03, DASHQ-04, DASHQ-05, DASHQ-14]
files_modified:
  - src/logic/dashboard-query-logic.ts
  - src/logic/dashboard-query-logic.test.ts
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Special populations Favourites, Birthdays, Not Contacted, Snoozed, and All Contacts are multi-selectable and combine as an OR-union where each contact appears exactly once (deduped), proven by a node:sqlite test that selects two overlapping populations and asserts one row per contact. [DASHQ-02]"
    - "Deselecting the last explicit population returns immediately to the Active universe (empty populations set → BASE_WHERE). [DASHQ-02]"
    - "All Contacts resolves to Active ∪ Not Contacted as an explicit union predicate (`BASE_WHERE` OR the never-contacted predicate); archived and unbound stay structurally excluded in EVERY population (`c.archived_at IS NULL`, `tracking_enabled = 1`) — All Contacts does not relax this. [DASHQ-03]"
    - "The Favourites population is binary membership (`favourite_rank IS NOT NULL`) and renders in the population Default order — the Dashboard NEVER adds `favourite_rank` to its ORDER BY. [DASHQ-04]"
    - "The Birthdays population covers contacts with a birthday in the next 30 days, computed via the shared `birthday-logic` parser (local-midnight, explicit Feb-29 handling), and under Sort=Default orders soonest-birthday-first. [DASHQ-04][DASHQ-14]"
    - "Snoozed contacts remain inside the Active universe but a never-contacted or snoozed row projects `status`/`progress` as NULL (CASE-wrapped, never bucketed 'stable'); the Snoozed population reveals the currently-future-snoozed contacts. [DASHQ-05]"
    - "A deduped contact retains all its population-match reasons in the result projection (single WHERE with OR'd population predicates, not a UNION of SELECTs that discards reasons). [DASHQ-02]"
  prohibitions:
    - statement: "Ranked Favorites is a retired product concept — the Dashboard exposes no user-visible favourite order and never sorts by `favourite_rank` (ADR-075)."
      status: enforced
      verification: "dashboard-read.test.ts asserts the Favourites-population ORDER BY contains no `favourite_rank` token; the population uses the resolved Default ordering."
    - statement: "`BASE_WHERE` is preserved verbatim as the Active predicate; All Contacts is built as an explicit `Active ∪ Not Contacted` union, never by weakening `BASE_WHERE` (D-05 trip-wire)."
      status: enforced
      verification: "dashboard-read.test.ts asserts All Contacts includes never-contacted but still excludes archived + unbound; the BASE_WHERE constant is byte-unchanged."
    - statement: "Never-contacted / snoozed rows must never be bucketed as 'stable' — status/progress project NULL via the CASE wrap (the HIGH-1 guard); STATUS_SQL is never evaluated over a NULL last_contact."
      status: enforced
      verification: "dashboard-read.test.ts asserts a never-contacted row in the All Contacts / Not Contacted population reads status=null, progress=null."
    - statement: "Richer imminent/upcoming birthday presentation is out of scope — the Dashboard owns only the 30-day Birthdays population; no birthday banner, module, or bespoke birthday query is added (banner removal itself is Plan 07 / ADR-076)."
      status: enforced
      verification: "The birthdays population reuses `birthday-logic`/`listBirthdayCandidates`; no new birthday-only presentation surface is created in this plan (grep shows no new banner/module)."
    - statement: "Deduplication uses a single WHERE with OR'd population predicates, not a `UNION` of per-population SELECTs (which would discard the retained population-match reasons — Pitfall 4)."
      status: enforced
      verification: "dashboard-read.ts population read is a single SELECT with an OR'd population WHERE; dashboard-read.test.ts asserts one row per contact across overlapping populations."
  artifacts:
    - "dashboard-query-logic.ts: population predicate model for all six universes (Active, Favourites, Birthdays, Not Contacted, Snoozed, All Contacts) + OR-union composition + per-population Default sort resolution"
    - "dashboard-read.ts: listDashboardPopulation expanded to OR-union populations with dedupe + population-match-reason projection"
  key_links:
    - "dashboard-query-logic population enum (Plan 01) → dashboard-read OR-union WHERE builder (the eligible universe)"
    - "birthday-logic parser → Birthdays population (30-day window, soonest-first Default) — reuse, never re-implement"
    - "BASE_WHERE (Active) + never-contacted predicate → All Contacts union"
---

<objective>
Expand the tracer's Active-only universe into the full OR-union population engine: Favourites, Birthdays (30-day), Not Contacted, Snoozed, and All Contacts (= Active ∪ Not Contacted), multi-selectable, deduped to one row per contact while retaining population-match reasons, with per-population Default sort resolution. Archived and Unbound stay structurally excluded everywhere.

Purpose: This is the eligible-universe half of the shared query contract the Control Surface (26), List (27), and Card (28) bind to. It is the highest-value expansion off the proven schema→store→query slice.

Output: `dashboard-query-logic.ts` grows the population predicate model + OR-union composition + per-population Default resolution; `dashboard-read.ts`'s population read grows from Active-only to the full deduped OR-union.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-04-planning-notes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Population predicate model — all six universes + OR-union + Default resolution</name>
  <files>src/logic/dashboard-query-logic.ts, src/logic/dashboard-query-logic.test.ts</files>
  <behavior>
    - Empty populations → Active (BASE_WHERE reference).
    - Each population token maps to its predicate reference: Favourites → `favourite_rank IS NOT NULL` (+ bound + non-archived), Birthdays → birthday-in-30-days, Not Contacted → never-contacted predicate, Snoozed → currently-future-snoozed, All Contacts → Active OR never-contacted.
    - Selecting ≥1 populations composes their predicates with OR; archived + unbound exclusion is always ANDed on top.
    - resolveDefaultSort('default', populations): Active/Favourites/Snoozed/All Contacts → 'status'; Birthdays → soonest-birthday ordering token; explicit sort passes through unchanged.
    - Deselecting the last population returns to Active.
  </behavior>
  <read_first>
    - src/logic/dashboard-query-logic.ts (the Plan-01 seed: DashboardPopulation enum, resolveDefaultSort, resetDashboardView, the Active predicate helper)
    - src/db/dashboard-read.ts (BASE_WHERE 155–158, the never-contacted predicate at 313–319, the snoozed reveal at 248, CARD_STATUS 131–132, SORT map 160–170 — the fragments the predicate references must name)
    - src/logic/birthday-logic.ts (READ IN FULL: the shared 30-day/next-occurrence parser + Feb-29 handling — the Birthdays population reuses it, never re-implements)
    - docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md §D–E, §H (population rules + population-aware Default examples)
  </read_first>
  <action>
    In `dashboard-query-logic.ts` add a pure `buildPopulationWhere(populations)` returning a `{ sql: string; params: unknown[] }` OR-union of the selected population predicate fragments, with the always-on archived+unbound exclusion ANDed. Every fragment is a closed code-constant (referencing the dashboard-read fragment names or re-declaring them as shared consts imported from dashboard-read to avoid drift); the only bound values are the birthday window bound and any date bound — never an interpolated identifier. Populations enumerated: `favourites` → `c.favourite_rank IS NOT NULL`; `not-contacted` → `c.last_contact IS NULL`; `snoozed` → `c.snooze_until IS NOT NULL AND date(c.snooze_until) > date('now','localtime')`; `all-contacts` → `(BASE_WHERE) OR (never-contacted)`; `birthdays` → a birthday-window predicate (see Task 2 for the 30-day mechanism — the logic layer exposes the token, the read layer supplies the SQL/JS window). Empty set → `BASE_WHERE`. Extend `resolveDefaultSort` to cover every population per dossier §H (Birthdays → soonest-first; all others → status). Keep `resetDashboardView` unchanged. Add tests for OR-union composition, deselect-last→Active, and per-population Default resolution.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-query-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-query-logic.test.ts asserts empty populations → the Active/BASE_WHERE reference; two overlapping populations compose with OR; deselecting the last returns Active.
    - `resolveDefaultSort('default', ['birthdays'])` resolves to the soonest-birthday token; `resolveDefaultSort('default', [])` resolves to 'status'; an explicit sort passes through.
    - No population predicate interpolates a runtime identifier; the archived+unbound exclusion is present on every non-empty union.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The pure population model resolves all six universes as an OR-union with dedupe-friendly single-WHERE composition and population-aware Default sort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dashboard-read OR-union population read + Birthdays 30-day window</name>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts</files>
  <behavior>
    - `listDashboardPopulation(exec, query)` returns the deduped OR-union of the selected populations, one row per contact, with the CARD_STATUS CASE-wrap so never-contacted/snoozed rows read status=null/progress=null.
    - All Contacts includes never-contacted contacts but excludes archived + unbound.
    - Favourites population returns favourite members ordered by the resolved Default order (no favourite_rank in ORDER BY).
    - Birthdays population returns only contacts with a birthday within the next 30 days; under Default it is soonest-first.
    - The projection carries population-match reasons (e.g. boolean membership flags per selected population) so a deduped row records why it qualified.
  </behavior>
  <read_first>
    - src/db/dashboard-read.ts (READ IN FULL: the Plan-01 listDashboardPopulation Active entry, CARD_FROM, CARD_STATUS, FUEL_LINE, BASE_WHERE, SORT, the injection posture 43–51, listBirthdayCandidates 391–399, listNeverContacted 296–322)
    - src/logic/dashboard-query-logic.ts (buildPopulationWhere + resolveDefaultSort from Task 1)
    - src/logic/birthday-logic.ts (the 30-day next-occurrence computation — the Birthdays window is computed with this, never a raw date arithmetic in SQL that reintroduces the UTC off-by-one)
    - src/db/status.ts (PROGRESS_SQL/STATUS_SQL/STABLE_MAX — import, never re-type)
  </read_first>
  <action>
    Expand `listDashboardPopulation` to consume `buildPopulationWhere(query.populations)`: SELECT the existing CARD_FROM + CARD_STATUS + FUEL_LINE projection, add per-selected-population membership-flag columns (each a CASE over the population's predicate, e.g. `CASE WHEN <fav-predicate> THEN 1 ELSE 0 END AS isFavourite`) so the deduped row retains its match reasons, WHERE = the OR-union from `buildPopulationWhere`, ORDER BY = `SORT[resolveDefaultSort(query.sort, query.populations)]`. Keep it a single SELECT (never a UNION — Pitfall 4). Birthdays: because the 30-day next-occurrence needs `birthday-logic`'s local-midnight/Feb-29 rules, resolve the eligible birthday-contact id set (or the soonest-first ordering) in JS via the shared parser rather than a bare `date()` window in SQL — either prefilter candidate ids (like the search/gravity id-scoping pattern) or carry a computed days-until value the ORDER BY consumes; do NOT reintroduce `toISOString().split`. Reuse `listBirthdayCandidates` as the candidate source (keep it — `notification-read.ts` consumes it). Preserve the injection posture: static string, `?`-bound runtime values only, closed code-constants for every fragment. Do NOT modify the legacy `listDashboard`/`listNeverContacted`/`listFavourites` (they stay for the current HomeScreen until the render phases). Extend dashboard-read.test.ts with OR-union dedupe, All Contacts union membership, Favourites Default-order (no rank), never-contacted null-status, and Birthdays 30-day-window cases.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-read.test.ts: selecting Favourites + Not Contacted returns one row per contact (dedupe) with correct membership flags; All Contacts includes a never-contacted contact and excludes an archived + an unbound contact.
    - The Favourites-population ORDER BY string contains no `favourite_rank` token (`grep` the built ORDER BY in the test).
    - A never-contacted row in All Contacts / Not Contacted reads status=null, progress=null.
    - A contact whose birthday is 40 days out is absent from the Birthdays population; one 10 days out is present and precedes a 25-days-out contact under Default.
    - `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The dashboard-read population entry returns the full deduped OR-union of all six universes with population-match reasons, Favourites in Default order, and a 30-day Birthdays window via the shared parser.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the population axis into the query store + full-suite guard</name>
  <files>src/logic/dashboard-query-logic.test.ts, src/db/dashboard-read.test.ts</files>
  <read_first>
    - src/stores/dashboard-query-store.ts (the Plan-01 store — setPopulations already persists; confirm it serializes the population array to the `dashboard_populations` JSON column)
    - src/logic/dashboard-query-logic.ts + src/db/dashboard-read.ts (the Task 1/2 surfaces this integration test exercises)
  </read_first>
  <action>
    Add an integration test proving the population axis flows end-to-end: set populations via the store → persist to `app_settings` → rehydrate → `buildPopulationWhere` → `listDashboardPopulation` returns the expected OR-union. Assert the "deselect last → Active" transition round-trips through the store (persists `[]`, rehydrates to Active). This task adds NO new production code beyond any tiny wiring the integration reveals; it is the wave's confidence gate. Run the full node suite at the end.
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-query-logic.test.ts src/db/dashboard-read.test.ts src/stores/dashboard-query-store.test.ts && npx vitest run && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - An integration test drives populations through the store into the read and asserts the OR-union result; deselect-last round-trips to Active.
    - Full `npx vitest run` is green; `npm run check:colors` green.
  </acceptance_criteria>
  <done>The population axis is proven end-to-end through the store; the full suite is green at wave merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client population selection → SQL WHERE | The selected population set drives the eligible-universe query |
| stored contact rows → deduped result | Archived/unbound must be excluded in-query, never by a UI `.filter()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.2-01 | Access Control (data-scope) | OR-union population WHERE | high | mitigate | Archived + unbound excluded in EVERY population predicate (`c.archived_at IS NULL`, `tracking_enabled = 1`); never a component-side filter; test asserts exclusion |
| T-25.2-02 | Tampering | population token → SQL | medium | mitigate | Population tokens map to closed code-constant predicates; no runtime identifier interpolated; only date/window values `?`-bound |
| T-25.2-03 | Information Disclosure | never-contacted status leak | low | mitigate | CARD_STATUS CASE-wrap projects NULL for never-contacted/snoozed; never re-derived as 'stable' |
| T-25.2-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
</threat_model>

<verification>
- `npx vitest run src/logic/dashboard-query-logic.test.ts src/db/dashboard-read.test.ts src/stores/dashboard-query-store.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
</verification>

<success_criteria>
All six populations resolve as a deduped OR-union with retained match reasons; All Contacts = Active ∪ Not Contacted with archived/unbound excluded; Favourites in Default order (no rank sort); Birthdays 30-day via the shared parser; never-contacted rows read null status; the population axis persists and round-trips through the store.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-02-SUMMARY.md` when done.
</output>
