---
phase: 25-dashboard-data-state-foundation
plan: 07
type: execute
wave: 4
depends_on: [25-03, 25-04, 25-06]
autonomous: true
requirements: [DASHQ-03, DASHQ-08, DASHQ-14]
files_modified:
  - src/components/BirthdayBanner.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/NeverContactedScreen.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/DigestScreen.tsx
  - src/screens/UnboundContactsScreen.tsx
  - src/db/unbound-read.ts
  - src/db/unbound-read.test.ts
  - src/screens/dashboard-search-row-logic.ts
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 57000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Dashboard birthday banner is removed: `BirthdayBanner.tsx` is retired and `HomeScreen` no longer mounts it; birthdays are reached only through the Birthdays population (30-day, Plan 02). ADR-034's re-query freshness (focus / foreground / pull-to-refresh) is retained. [DASHQ-14]"
    - "The standalone Never Contacted screen is retired and its route removed; `HomeScreen`'s footer entry and `DigestScreen`'s backlog navigation are re-pointed to a live target (never a deleted route). `countNeverContacted` is KEPT so Digest's backlog count still reads. [DASHQ-03]"
    - "The `include_unbound_never_contacted` Settings toggle is removed from `SettingsScreen`; its `app_settings` column AND its `PORTABLE_SETTINGS_KEYS` entry are KEPT (removal coordinated with the Phase 36 backup format bump — not dropped unilaterally, D-05 trip-wire). [DASHQ-03]"
    - "Unbound contacts get a replacement retrieval path: `UnboundContactsScreen` gains its own name search over `unbound-read`, so removing Unbound from Dashboard search does not remove their only name-lookup path (ADR-062 'retrieval stays available' holds). [DASHQ-08]"
    - "The Dashboard-search Unbound retrieval rows (`dashboard-search-row-logic.ts`) are retired from the HomeScreen search surface, consistent with search being scoped to Bound eligible contacts (D-07). [DASHQ-08]"
  prohibitions:
    - statement: "Do NOT drop the `include_unbound_never_contacted` portable settings key or its app_settings column unilaterally — its removal is coordinated with the Phase 36 backup format bump (D-05 deletion-with-consumers). Keep the allowlist entry + column; only the Settings toggle UI is removed."
      status: enforced
      verification: "`grep -n 'includeUnboundNeverContacted' src/backup/backup-schema.ts src/db/app-settings-dao.ts` still shows the key/column present; only the SettingsScreen toggle row is removed."
    - statement: "Removing Unbound from Dashboard search WITHOUT a replacement retrieval path weakens ADR-062 (R-11 trip-wire). A replacement (UnboundContactsScreen own search) ships in the SAME plan as the removal."
      status: enforced
      verification: "unbound-read.test.ts covers the new name search; UnboundContactsScreen wires it; the dashboard-search-row-logic retirement and the replacement land together (same plan)."
    - statement: "Removing the birthday banner while Your Week is unplanned leaves a coverage gap (Digest has no birthday read today) — this is NOTED, not silently dropped (D-06). The banner is not folded into Digest."
      status: flagged-unverified
      verification: "The SUMMARY records the coverage gap (upcoming birthdays reachable only via the Birthdays population until the deferred Your Week phase); no birthday read is added to Digest."
    - statement: "Retirements are consumer-repointing, not blind deletes: every navigate() to a removed route (Never Contacted) is re-pointed to a live target atomically; `countNeverContacted` survives for Digest (RESEARCH A3)."
      status: enforced
      verification: "`grep -rn 'NeverContacted\"' src` shows no navigate target to the deleted route; `countNeverContacted` still exported and consumed by DigestScreen; tsc clean."
    - statement: "Permanent Dashboard summary modules are out of scope — the banner removal does not introduce any replacement permanent module; the Dashboard stays lean (dossier §A)."
      status: enforced
      verification: "No new banner/summary component is added; HomeScreen loses the banner mount with nothing permanent put in its place."
  artifacts:
    - "Removed: BirthdayBanner.tsx, NeverContactedScreen.tsx, the NeverContacted route, the include-Unbound Settings toggle UI"
    - "unbound-read.ts: new name search over Unbound contacts (the ADR-062 replacement retrieval path)"
    - "Retired from HomeScreen: dashboard-search-row-logic Unbound retrieval rows"
    - "Kept: countNeverContacted, include_unbound_never_contacted column + portable key (Phase 36 coordinates removal)"
  key_links:
    - "HomeScreen banner mount removed → Birthdays population (Plan 02) is the only birthday surface (ADR-076)"
    - "DigestScreen backlog + HomeScreen footer → re-pointed live target; countNeverContacted KEPT for Digest (A3)"
    - "UnboundContactsScreen search → unbound-read name search (ADR-062 replacement, coordinated with Phase 26 R-11)"
---

<objective>
Complete the phase's surface retirements as consumer-repointing deletions: remove the Dashboard birthday banner (ADR-076/DASHQ-14), retire the standalone Never Contacted screen + the include-Unbound Settings toggle (ADR-011/DASHQ-03), and provide the ADR-062 replacement retrieval path (Unbound child-route search) so removing Unbound from Dashboard search does not strand their only name lookup (D-07/DASHQ-08).

Purpose: These are the four trip-wire retirements the dossier requires in Phase 25. Each has live consumers — re-point them, keep the count read + the portable key/column (Phase 36 coordinates their removal), and note the birthday coverage gap.

Output: banner + Never-Contacted screen + include-Unbound toggle removed with all consumers re-pointed; Unbound gets its own search; the count read + portable key/column survive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record + governing ADRs
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-04-planning-notes.md
@docs/decisions/ADR-076-population-reached-birthdays-without-a-dashboard-banner.md
@docs/decisions/ADR-011-query-time-status-and-never-contacted-segregation.md
@docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Dashboard birthday banner (retain freshness); note the coverage gap</name>
  <files>src/components/BirthdayBanner.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/components/BirthdayBanner.tsx (READ IN FULL: the permanent banner to remove; its listBirthdayCandidates read — note listBirthdayCandidates is KEPT, notification-read.ts consumes it)
    - src/screens/HomeScreen.tsx (the `<BirthdayBanner onPressContact=... />` mount to remove; the focus / AppState→active / pull-to-refresh freshness triad to RETAIN — do not remove the refresh handlers)
    - docs/decisions/ADR-076-...md (banner retired; freshness model retained unchanged; Your Week owns richer presentation; the coverage-gap acceptance)
    - src/db/notification-read.ts (confirms listBirthdayCandidates has a live consumer — do NOT delete the DAO read)
  </read_first>
  <action>
    Delete `src/components/BirthdayBanner.tsx` and remove its mount (and now-unused import) from `HomeScreen.tsx`. RETAIN HomeScreen's freshness handlers (useFocusEffect, AppState→active, pull-to-refresh) exactly (ADR-076 keeps them). Do NOT delete `listBirthdayCandidates` from dashboard-read (notification-read.ts consumes it; the Plan-02 Birthdays population also builds on it). Put NO permanent replacement module in the banner's place — the Dashboard stays lean; birthdays are reached via the Birthdays population. In the SUMMARY, record the D-06 coverage gap: upcoming birthdays lose their at-a-glance surface until the deferred Your Week phase; Digest gets no birthday read here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BirthdayBanner.tsx` no longer exists; `grep -n 'BirthdayBanner' src/screens/HomeScreen.tsx` returns no match.
    - HomeScreen's freshness handlers (useFocusEffect / AppState / pull-to-refresh) remain (`grep -n 'useFocusEffect\|AppState\|RefreshControl' src/screens/HomeScreen.tsx` still matches).
    - `grep -rn 'listBirthdayCandidates' src` still shows it defined + consumed by notification-read (not deleted).
    - No new permanent banner/module component added; `npx tsc --noEmit` and `npm run check:colors` clean.
    - The SUMMARY records the D-06 birthday coverage gap.
  </acceptance_criteria>
  <done>The Dashboard birthday banner is removed with freshness retained and the coverage gap noted; birthdays are population-only.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the Never Contacted screen + include-Unbound toggle (keep count + column + key)</name>
  <files>src/screens/NeverContactedScreen.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts, src/screens/SettingsScreen.tsx, src/screens/DigestScreen.tsx</files>
  <read_first>
    - src/screens/NeverContactedScreen.tsx (the standalone screen to remove)
    - src/navigation/tabs/DashboardStack.tsx + src/navigation/RootNavigator.tsx + src/navigation/types.ts (the NeverContacted route registration + param entry to remove — grep which navigator owns it)
    - src/screens/HomeScreen.tsx (the Not-yet-contacted footer entry → NeverContacted, re-point/remove — NOTE HomeScreen is also touched in Task 1/3; make this a single coherent edit set)
    - src/screens/DigestScreen.tsx (onPressBacklog → navigation.navigate('NeverContacted') at 163; countNeverContacted read at 100 — re-point the nav to a live target, KEEP the count read)
    - src/screens/SettingsScreen.tsx (the include-Unbound toggle at ~1170 — remove the toggle ROW; do NOT remove the DAO field/column)
    - src/db/dashboard-read.ts (countNeverContacted 346–360 — KEEP; RESEARCH A3 Digest still consumes it)
    - src/backup/backup-schema.ts + src/db/app-settings-dao.ts (the includeUnboundNeverContacted portable key + column — KEEP both; Phase 36 coordinates removal)
  </read_first>
  <action>
    Delete `src/screens/NeverContactedScreen.tsx` and remove its route from `DashboardStack.tsx`/`RootNavigator.tsx` (whichever registers it) + its param entry from `navigation/types.ts`. Re-point every `navigate('NeverContacted')` to a LIVE target: DigestScreen's `onPressBacklog` and HomeScreen's Not-yet-contacted footer entry → navigate to Home (the dashboard; the Not Contacted population is the intended destination, its explicit chip lands in Phase 26 — Home is the safe live target until then). KEEP `countNeverContacted` (DigestScreen still displays the backlog count — A3). Remove the include-Unbound toggle ROW from `SettingsScreen.tsx` (delete the row UI + any now-unused local handler), but do NOT touch `includeUnboundNeverContacted` in app-settings-dao (column/field) or backup-schema (portable key) — their removal is coordinated with Phase 36's format bump (D-05). In the SUMMARY, record that the column + portable key are intentionally retained pending Phase 36. Run tsc clean here (all NeverContacted route references gone).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/db/dashboard-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/NeverContactedScreen.tsx` no longer exists; `grep -rn "navigate(\"NeverContacted\")\|'NeverContacted'" src` shows no navigation to the deleted route.
    - `countNeverContacted` is still exported and still consumed by DigestScreen (`grep -n 'countNeverContacted' src/screens/DigestScreen.tsx` matches); DigestScreen's backlog nav resolves to a live target.
    - The Settings include-Unbound toggle row is gone, but `grep -n 'includeUnboundNeverContacted' src/db/app-settings-dao.ts src/backup/backup-schema.ts` still shows the column/field + portable key present.
    - `npx tsc --noEmit` and `npm run check:colors` clean; dashboard-read tests green.
  </acceptance_criteria>
  <done>The Never Contacted screen + include-Unbound toggle UI are retired with consumers re-pointed; the count read + column + portable key survive for Phase 36.</done>
</task>

<task type="auto">
  <name>Task 3: Unbound replacement retrieval (own search) + retire Dashboard-search Unbound rows</name>
  <files>src/db/unbound-read.ts, src/db/unbound-read.test.ts, src/screens/UnboundContactsScreen.tsx, src/screens/dashboard-search-row-logic.ts, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/db/unbound-read.ts (READ IN FULL: listUnbound SELECT/ORDER BY at 24–37, countUnbound — the base read the new name search extends)
    - src/screens/UnboundContactsScreen.tsx (currently listUnbound only, no search — the child route that gains the replacement search)
    - src/screens/dashboard-search-row-logic.ts (the Unbound-in-Dashboard-search retrieval rows to retire, per D-07)
    - src/screens/HomeScreen.tsx (where dashboard-search-row-logic Unbound rows are rendered in the search surface — remove that rendering; NOTE HomeScreen is edited across Tasks 1–3, keep the edits coherent)
    - docs/decisions/ADR-062-...md (retrieval stays available; unbound lifecycle) ; RESEARCH R-11 (coordinate with Phase 26 R-11 — the shared picker path is the alternative; the child-route search is the chosen replacement)
  </read_first>
  <action>
    Add a name search to `unbound-read.ts`: `searchUnbound(exec, term)` — a `?`-bound `LIKE ? ESCAPE '\'` scan over `contacts.name` restricted to the Unbound universe (mirror `listUnbound`'s WHERE), escaping the term with the existing `escapeLike` idiom; empty/whitespace term → []; ordered by name. Wire it into `UnboundContactsScreen.tsx` as an in-screen search box (reuse the existing screen's list rendering; add a controlled search input filtering via `searchUnbound`). This is the ADR-062 replacement retrieval path. Then retire the Unbound retrieval rows from the Dashboard search surface: remove the `dashboard-search-row-logic.ts` Unbound-row rendering from `HomeScreen.tsx` (the legacy dashboard search no longer returns Unbound rows — consistent with the new scoped search returning only Bound eligible contacts). Keep the change minimal on the legacy HomeScreen (it is replaced by the render phases); just stop surfacing Unbound rows there. Add unbound-read.test.ts cases for searchUnbound (name match, escape correctness, empty term → [], Unbound-only scope). Record in the SUMMARY that the shared-picker explicit-search alternative is deferred to Phase 26's R-11 (the child-route search satisfies ADR-062 now).
  </action>
  <verify>
    <automated>npx vitest run src/db/unbound-read.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - unbound-read.test.ts asserts `searchUnbound` matches by name within the Unbound scope, escapes `%`/`_` literally, and returns [] for an empty/whitespace term.
    - `UnboundContactsScreen.tsx` renders a working in-screen search wired to `searchUnbound`.
    - `grep -n 'dashboard-search-row-logic' src/screens/HomeScreen.tsx` returns no match (Unbound rows retired from the Dashboard search surface).
    - Full `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>Unbound retrieval survives via the child-route search (ADR-062 held); Dashboard-search Unbound rows are retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retired routes → navigation | Removing a screen must not strand a navigate() call (crash) |
| Unbound search term → SQL | The new name search takes user input over the Unbound universe |
| portable key/column removal → backup format | Dropping a portable key unilaterally breaks backup compatibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.7-01 | Denial of Service (crash) | navigate() to deleted NeverContacted route | medium | mitigate | Every navigate target re-pointed to a live route atomically with the removal; tsc + grep gate confirm no dangling target |
| T-25.7-02 | Tampering | searchUnbound term → SQL injection | high | mitigate | `?`-bound `LIKE ? ESCAPE '\'` with escapeLike; no interpolated term; Unbound-scope WHERE is a code-constant |
| T-25.7-03 | Information Disclosure | Unbound retrieval removed w/o replacement | medium | mitigate | Replacement child-route search ships in the same plan (ADR-062 retrieval preserved); test covers it |
| T-25.7-04 | Tampering (backup compat) | include_unbound key/column removal | high | mitigate | Key + column KEPT; only the toggle UI removed; removal coordinated with Phase 36 format bump (D-05) |
| T-25.7-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
</threat_model>

<verification>
- `npx vitest run src/db/unbound-read.test.ts src/db/dashboard-read.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
- `grep -rn 'BirthdayBanner\|NeverContactedScreen\|dashboard-search-row-logic' src` (must show removals); `grep -n 'includeUnboundNeverContacted' src/backup/backup-schema.ts` (must still be present)
- On-device UAT (Pixel): birthday-banner gone, Not-Contacted/backlog nav lands live, Unbound child-route search finds a contact, no archived/unbound leak in Dashboard search (25-VALIDATION Manual-Only).
</verification>

<success_criteria>
The birthday banner is removed (freshness retained, gap noted); the Never Contacted screen + include-Unbound toggle are retired with consumers re-pointed and the count read + column + portable key kept for Phase 36; Unbound retrieval survives via the child-route search; the Dashboard-search Unbound rows are retired; full suite green.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-07-SUMMARY.md` when done.
</output>