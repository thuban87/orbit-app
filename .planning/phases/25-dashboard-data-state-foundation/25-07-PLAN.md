---
phase: 25-dashboard-data-state-foundation
plan: 07
type: execute
wave: 4
depends_on: [25-03, 25-04, 25-06]
autonomous: true
requirements: [DASHQ-03, DASHQ-08, DASHQ-14]
files_modified:
  - src/components/BirthdayBanner.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/HomeScreen.test.tsx
  - src/screens/NeverContactedScreen.tsx
  - src/screens/NeverContactedScreen.test.tsx
  - src/navigation/tabs/DashboardStack.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
  - src/navigation/focused-route-classification.test.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/DigestScreen.tsx
  - src/screens/dashboard-search-row-logic.ts
  - src/db/dashboard-read.ts
  - src/db/dashboard-read.test.ts
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 57000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Dashboard birthday banner is removed: `BirthdayBanner.tsx` is retired and `HomeScreen` no longer mounts it; birthdays are reached only through the Birthdays population (30-day, Plan 02). ADR-034's re-query freshness (focus / foreground / pull-to-refresh) is retained. [DASHQ-14]"
    - "The standalone Never Contacted screen is retired and its route removed; `HomeScreen`'s footer entry and `DigestScreen`'s backlog navigation are re-pointed to a live target (never a deleted route). `countNeverContacted` is KEPT so Digest's backlog count still reads. [DASHQ-03]"
    - "The `include_unbound_never_contacted` Settings toggle is removed from `SettingsScreen`; its `app_settings` column AND its `PORTABLE_SETTINGS_KEYS` entry are KEPT (removal coordinated with the Phase 36 backup format bump — not dropped unilaterally, D-05 trip-wire). [DASHQ-03]"
    - "`UnboundContactsScreen` and `unbound-read` (`listUnbound`/`countUnbound`) are NOT modified this phase — the Unbound browse surface stays exactly as-is; no `searchUnbound` is added (D-13 defers the Unbound name-lookup replacement to Phase 26). [DASHQ-08] (D-13)"
    - "The Dashboard-search Unbound retrieval rows (`dashboard-search-row-logic.ts`) are retired from the HomeScreen search surface, consistent with search being scoped to Bound eligible contacts (D-07). [DASHQ-08]"
    - "The legacy Home Dashboard-search term branch is made BOUND-ONLY now: `dashboard-read.ts`'s `listDashboard` term branch (Branch 1, ~230–238) gains `AND ${DASHBOARD_BOUND_WHERE}` (`c.tracking_enabled = 1`) so an Unbound (`tracking_enabled = 0`) contact no longer surfaces in legacy Home search — VERIFIED on disk that today it filters only `c.archived_at IS NULL`, so merely removing the neutral-row presentation would make Unbound render as normal cards (worse). This is the D-13 owner resolution of the D-07 trip-wire; the legacy `BASE_WHERE` constant (155–158) and every other branch stay byte-unchanged — only Branch 1's inline WHERE gains the bound predicate. [DASHQ-08] (D-13)"
    - "Owner-ACCEPTED transient gap (D-13): Unbound contacts have NO name-lookup (search) path between Phase 25 and Phase 26's D-07/ADR-062/R-11 replacement. No Unbound replacement search is added in Phase 25 — the previously-planned `searchUnbound` child-route search is DROPPED per D-13 (the replacement is coordinated with Phase 26; this is NOT a D-07/ADR-062 reversal, it is the owner answering the D-07 trip-wire by accepting the transient gap). The existing `UnboundContactsScreen` browse list (`listUnbound`) is untouched and still lets a user reach Unbound contacts by browsing; only typed search is deferred. [DASHQ-08] (D-13)"
  prohibitions:
    - statement: "Do NOT drop the `include_unbound_never_contacted` portable settings key or its app_settings column unilaterally — its removal is coordinated with the Phase 36 backup format bump (D-05 deletion-with-consumers). Keep the allowlist entry + column; only the Settings toggle UI is removed."
      status: enforced
      verification: "`grep -n 'includeUnboundNeverContacted' src/backup/backup-schema.ts src/db/app-settings-dao.ts` still shows the key/column present; only the SettingsScreen toggle row is removed."
    - statement: "The D-07/R-11/ADR-062 trip-wire (removing Unbound from search needs a replacement) is OWNER-RESOLVED by D-13: the owner ACCEPTED the one-phase Unbound name-lookup gap, with the replacement coordinated with Phase 26 (R-11). Phase 25 therefore (a) makes the legacy Home term branch BOUND-ONLY so Unbound stops leaking into search (DASHQ-08 holds at the data layer), and (b) does NOT add any Unbound replacement search. This is NOT an ADR-062 reversal — retrieval-by-browse (`listUnbound`) stays available and the search replacement is deferred, not abandoned. Do NOT re-add a `searchUnbound` path in Phase 25."
      status: enforced
      verification: "`grep -rn 'searchUnbound' src` returns no match (the replacement is not built this phase); `unbound-read.ts` still exports only `listUnbound`/`countUnbound` (unchanged); dashboard-read.test.ts asserts the legacy term branch excludes an Unbound contact (D-13); the SUMMARY records the owner-accepted transient gap citing D-13."
    - statement: "Removing the birthday banner while Your Week is unplanned leaves a coverage gap (Digest has no birthday read today) — this is NOTED, not silently dropped (D-06). The banner is not folded into Digest."
      status: flagged-unverified
      verification: "The SUMMARY records the coverage gap (upcoming birthdays reachable only via the Birthdays population until the deferred Your Week phase); no birthday read is added to Digest."
    - statement: "Retirements are consumer-repointing, not blind deletes, and the NeverContacted sweep is COMPLETE against on-disk consumers (review HIGH-5): the route param (`types.ts:65`), the Stack.Screen (`DashboardStack.tsx:65`), the two navigate() call sites (`DigestScreen.tsx:163`, `HomeScreen.tsx:417`), the `browseRoutes` literal (`focused-route-classification.test.ts:38`), and the screen's own test (`NeverContactedScreen.test.tsx`) are all removed/re-pointed atomically. (`queries.test.ts:135` uses `\"NeverContacted\"` only as a seeded contact NAME, not a route — left untouched.) `countNeverContacted` survives for Digest (RESEARCH A3)."
      status: enforced
      verification: "`grep -rn 'name=\"NeverContacted\"\\|navigate(\"NeverContacted\")\\|\"NeverContacted\",' src` shows no route registration / navigate target / browseRoutes literal remaining; `NeverContactedScreen.test.tsx` no longer exists; `countNeverContacted` still exported and consumed by DigestScreen; tsc + full vitest clean."
    - statement: "TRANSIENT NOT-CONTACTED CONTROL GAP — OWNER-ACCEPTED (D-14, resolved, no longer merely flagged): retiring the standalone Never Contacted screen in Phase 25 while its replacement CONTROL (the Not-Contacted population chip) lands in Phase 26 leaves a one-phase window where the legacy Home screen (still on legacy `listDashboard`) offers no user-facing chip to select the Not-Contacted population. The DATA surface (the Not-Contacted population + All Contacts, Plan 02) EXISTS after Phase 25; only its visible chip is Phase 26. `navigate('NeverContacted')` call sites re-point to Home (a live target — no crash). The owner has ACCEPTED this one-phase gap (D-14): the dossier E-02 retirement proceeds NOW; this is a confirmation of the retirement intent, NOT a reversal. `countNeverContacted` stays for Digest. This is the owner's answered sequencing decision — an executor does NOT keep the screen or re-escalate."
      status: enforced
      verification: "The SUMMARY records the owner-accepted transient gap citing D-14 (data surface present in Phase 25; chip in Phase 26; Home is the live nav target; no crash); the dossier E-02 retirement is not reversed; `countNeverContacted` remains exported."
    - statement: "Permanent Dashboard summary modules are out of scope — the banner removal does not introduce any replacement permanent module; the Dashboard stays lean (dossier §A)."
      status: enforced
      verification: "No new banner/summary component is added; HomeScreen loses the banner mount with nothing permanent put in its place."
  artifacts:
    - "Removed: BirthdayBanner.tsx, NeverContactedScreen.tsx, NeverContactedScreen.test.tsx, the NeverContacted route (+ its browseRoutes literal), dashboard-search-row-logic.ts + HomeScreen.test.tsx (retired Unbound-row helper + test), the include-Unbound Settings toggle UI"
    - "dashboard-read.ts: legacy `listDashboard` term branch made BOUND-ONLY (`AND ${DASHBOARD_BOUND_WHERE}`) so Unbound stops surfacing in legacy Home search (D-13); BASE_WHERE + all other branches byte-unchanged"
    - "Retired from HomeScreen: dashboard-search-row-logic Unbound retrieval rows"
    - "NOT built (D-13, deferred to Phase 26): the `searchUnbound` Unbound name-lookup replacement; `unbound-read.ts` + `UnboundContactsScreen.tsx` are untouched"
    - "Kept: countNeverContacted (Digest), listNeverContacted (@deprecated, retired with legacy listDashboard in render phases), include_unbound_never_contacted column + portable key (Phase 36 coordinates removal)"
  key_links:
    - "HomeScreen banner mount removed → Birthdays population (Plan 02) is the only birthday surface (ADR-076)"
    - "DigestScreen backlog + HomeScreen footer → re-pointed live target; countNeverContacted KEPT for Digest (A3)"
    - "legacy `listDashboard` term branch + `AND ${DASHBOARD_BOUND_WHERE}` (D-13) → Unbound excluded from legacy Home search; the Unbound name-lookup replacement is coordinated with Phase 26 R-11 (owner-accepted gap, no `searchUnbound` this phase)"
---

<objective>
Complete the phase's surface retirements as consumer-repointing deletions: remove the Dashboard birthday banner (ADR-076/DASHQ-14), retire the standalone Never Contacted screen + the include-Unbound Settings toggle (ADR-011/DASHQ-03), and close the Unbound-in-search leak per D-13 — make the legacy Home term-search branch BOUND-ONLY and retire the Dashboard-search Unbound rows, accepting the owner-approved one-phase Unbound name-lookup gap (D-07/DASHQ-08, replacement coordinated with Phase 26).

Purpose: These are the trip-wire retirements the dossier requires in Phase 25. Each has live consumers — re-point them, keep the count read + the portable key/column (Phase 36 coordinates their removal), and note the birthday coverage gap. The Unbound name-lookup replacement is DEFERRED to Phase 26 by owner decision D-13 (no `searchUnbound` this phase).

Output: banner + Never-Contacted screen + include-Unbound toggle removed with all consumers re-pointed; the legacy term branch is bound-only + the Dashboard-search Unbound rows retired (D-13); the count read + portable key/column survive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@CLAUDE.md

# Ground-truth decision record + governing ADRs
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-04-planning-notes.md
@docs/decisions/ADR-076-population-reached-birthdays-without-a-dashboard-banner.md
@docs/decisions/ADR-011-query-time-status-and-never-contacted-segregation.md
@docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Dashboard birthday banner (retain freshness); note the coverage gap</name>
  <files>src/components/BirthdayBanner.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/components/BirthdayBanner.tsx (READ IN FULL: the permanent banner to remove; its listBirthdayCandidates read — note listBirthdayCandidates is KEPT, notification-read.ts consumes it)
    - src/screens/HomeScreen.tsx (the `<BirthdayBanner onPressContact=... />` mount to remove; the focus / AppState→active / pull-to-refresh freshness triad to RETAIN — do not remove the refresh handlers)
    - docs/decisions/ADR-076-...md (banner retired; freshness model retained unchanged; Your Week owns richer presentation; the coverage-gap acceptance)
    - src/db/notification-read.ts (confirms listBirthdayCandidates has a live consumer — do NOT delete the DAO read)
  </read_first>
  <action>
    Delete `src/components/BirthdayBanner.tsx` and remove its mount (and now-unused import) from `HomeScreen.tsx`. RETAIN HomeScreen's freshness handlers (useFocusEffect, AppState→active, pull-to-refresh) exactly (ADR-076 keeps them). Do NOT delete `listBirthdayCandidates` from dashboard-read (notification-read.ts consumes it; the Plan-02 Birthdays population also builds on it). Put NO permanent replacement module in the banner's place — the Dashboard stays lean; birthdays are reached via the Birthdays population. In the SUMMARY, record the D-06 coverage gap: upcoming birthdays lose their at-a-glance surface until the deferred Your Week phase; Digest gets no birthday read here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BirthdayBanner.tsx` no longer exists; `grep -n 'BirthdayBanner' src/screens/HomeScreen.tsx` returns no match.
    - HomeScreen's freshness handlers (useFocusEffect / AppState / pull-to-refresh) remain (`grep -n 'useFocusEffect\|AppState\|RefreshControl' src/screens/HomeScreen.tsx` still matches).
    - `grep -rn 'listBirthdayCandidates' src` still shows it defined + consumed by notification-read (not deleted).
    - No new permanent banner/module component added; `npx tsc --noEmit` and `npm run check:colors` clean.
    - The SUMMARY records the D-06 birthday coverage gap.
  </acceptance_criteria>
  <done>The Dashboard birthday banner is removed with freshness retained and the coverage gap noted; birthdays are population-only.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the Never Contacted screen + include-Unbound toggle (keep count + column + key)</name>
  <files>src/screens/NeverContactedScreen.tsx, src/screens/NeverContactedScreen.test.tsx, src/screens/HomeScreen.tsx, src/navigation/tabs/DashboardStack.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts, src/navigation/focused-route-classification.test.ts, src/screens/SettingsScreen.tsx, src/screens/DigestScreen.tsx</files>
  <read_first>
    - src/screens/NeverContactedScreen.tsx (the standalone screen to remove) + src/screens/NeverContactedScreen.test.tsx (its dedicated test, removed with it)
    - src/navigation/tabs/DashboardStack.tsx (line 65: `<Stack.Screen name="NeverContacted" …>` — the route registration to remove; grep confirms DashboardStack owns it, NOT RootNavigator) + src/navigation/RootNavigator.tsx (does NOT register the route — but its doc comment at lines 121 & 125 NAMES `NeverContactedScreen`/`NeverContacted` as an example child screen; reword those stale mentions since the screen is gone) + src/navigation/types.ts (line 65: the `NeverContacted: undefined` param entry to remove; and the `:80` doc-comment mention of `NeverContacted` — Plan 06 already dropped the `ManageFavourites` half in the prior wave, so clean only the remaining `NeverContacted` reference)
    - src/navigation/focused-route-classification.test.ts (line 38: the `browseRoutes` literal lists `"NeverContacted"` — remove that element; do NOT touch `"ManageFavourites"` on line 40, Plan 06 owns it)
    - src/screens/HomeScreen.tsx (line 417: the Not-yet-contacted footer entry `navigation.navigate("NeverContacted")`, re-point — NOTE HomeScreen is also touched in Task 1/3; make this a single coherent edit set)
    - src/screens/DigestScreen.tsx (onPressBacklog → navigation.navigate('NeverContacted') at 163; countNeverContacted read at 100 — re-point the nav to a live target, KEEP the count read; ALSO the doc comment at line 17 "Load discipline mirrors NeverContactedScreen" goes stale — reword it, the screen is gone)
    - src/screens/SettingsScreen.tsx (the include-Unbound toggle at ~1170 — remove the toggle ROW; do NOT remove the DAO field/column)
    - src/db/dashboard-read.ts (countNeverContacted 346–360 — KEEP; RESEARCH A3 Digest still consumes it. `listNeverContacted` 278–322 — see the dead-code decision in Task 2 action)
    - src/backup/backup-schema.ts + src/db/app-settings-dao.ts (the includeUnboundNeverContacted portable key + column — KEEP both; Phase 36 coordinates removal)
  </read_first>
  <action>
    Delete `src/screens/NeverContactedScreen.tsx` AND its test `src/screens/NeverContactedScreen.test.tsx`. Remove the `NeverContacted` route from `DashboardStack.tsx` (line 65 registration + its import) — grep confirms DashboardStack owns it (RootNavigator does not; leave RootNavigator untouched if it has no reference) — and remove the `NeverContacted: undefined` param entry from `navigation/types.ts`. Remove `"NeverContacted"` from the `browseRoutes` array in `focused-route-classification.test.ts` (leave `"ManageFavourites"` for Plan 06). Re-point every `navigate('NeverContacted')` to a LIVE target: DigestScreen's `onPressBacklog` and HomeScreen's Not-yet-contacted footer entry (417) → navigate to Home (the dashboard; the Not Contacted population is the intended destination, its explicit chip lands in Phase 26 — Home is a live nav target, no crash; see the TRANSIENT RETRIEVAL GAP flagged assumption). KEEP `countNeverContacted` (DigestScreen still displays the backlog count — A3).

    `listNeverContacted` dead-code decision (review LOW): KEEP `listNeverContacted` in dashboard-read.ts but add an in-file `@deprecated` note — it becomes unreferenced once the screen is gone, and it is retired TOGETHER WITH the legacy `listDashboard`/legacy Home in the render phases (26–28), not surgically pulled here (removing it now would touch dashboard-read.ts which Plans 02/03 heavily edit, and the legacy Home still exists this phase). Record this in the SUMMARY.

    Remove the include-Unbound toggle ROW from `SettingsScreen.tsx` (delete the row UI + any now-unused local handler), but do NOT touch `includeUnboundNeverContacted` in app-settings-dao (column/field) or backup-schema (portable key) — their removal is coordinated with Phase 36's format bump (D-05). In the SUMMARY, record that the column + portable key are intentionally retained pending Phase 36.

    Clean the stale doc-comment references left by the retirement (review actionable — non-blocking, comment-only): reword `RootNavigator.tsx:121,125` (drops the `NeverContactedScreen`/`NeverContacted` example mentions), `DigestScreen.tsx:17` ("Load discipline mirrors NeverContactedScreen" → a live reference or a neutral phrasing), and the remaining `NeverContacted` mention in the `navigation/types.ts:80` comment (Plan 06 handled the `ManageFavourites` half a wave earlier). These are documentation edits only — no behaviour change.

    Run tsc clean here (all NeverContacted route references gone).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors && npx vitest run src/db/dashboard-read.test.ts src/navigation/focused-route-classification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/NeverContactedScreen.tsx` and `src/screens/NeverContactedScreen.test.tsx` no longer exist; `grep -rn 'name="NeverContacted"\|navigate("NeverContacted")\|"NeverContacted",' src` shows no route registration / navigate target / browseRoutes literal remaining (the `queries.test.ts` seed-name string is not a route match).
    - `countNeverContacted` is still exported and still consumed by DigestScreen (`grep -n 'countNeverContacted' src/screens/DigestScreen.tsx` matches); DigestScreen's backlog nav resolves to a live target; `listNeverContacted` remains with an `@deprecated` note.
    - The Settings include-Unbound toggle row is gone, but `grep -n 'includeUnboundNeverContacted' src/db/app-settings-dao.ts src/backup/backup-schema.ts` still shows the column/field + portable key present.
    - `npx tsc --noEmit` and `npm run check:colors` clean; dashboard-read + focused-route-classification tests green.
  </acceptance_criteria>
  <done>The Never Contacted screen + include-Unbound toggle UI are retired with consumers re-pointed; the count read + column + portable key survive for Phase 36.</done>
</task>

<task type="auto">
  <name>Task 3: Make legacy Home search BOUND-ONLY (D-13) + retire Dashboard-search Unbound rows</name>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/screens/dashboard-search-row-logic.ts, src/screens/HomeScreen.tsx, src/screens/HomeScreen.test.tsx</files>
  <read_first>
    - src/db/dashboard-read.ts (READ IN FULL, focus 139–268: `DASHBOARD_BOUND_WHERE = "c.tracking_enabled = 1"` at `:139`; `BASE_WHERE` at 155–158 (leave byte-unchanged); `listDashboard`'s term Branch 1 at 230–240 — its `where` is `c.archived_at IS NULL AND (name LIKE … OR EXISTS fuel …)` and OMITS the bound predicate, VERIFIED — this is the Unbound leak D-13 fixes; the favourites/snoozed/default branches already carry `${DASHBOARD_BOUND_WHERE}`)
    - .planning/phases/25-dashboard-data-state-foundation/25-CONTEXT.md (D-13 — the owner resolution authorizing this ONE change to legacy `listDashboard` and accepting the transient Unbound name-lookup gap; D-07/ADR-062 replacement is Phase 26)
    - src/screens/dashboard-search-row-logic.ts (READ IN FULL: `dashboardSearchRowPresentation` — the Unbound-in-Dashboard-search retrieval rows to retire, per D-07; used ONLY by HomeScreen.tsx + HomeScreen.test.tsx — grep-verified, no other consumer)
    - src/screens/HomeScreen.test.tsx (READ IN FULL: imports `dashboardSearchRowPresentation` — its sole `describe("HomeScreen Unbound search presentation")` exercises the retired helper; removed with it)
    - src/screens/HomeScreen.tsx (where dashboard-search-row-logic Unbound rows are rendered in the search surface — remove that rendering + the import; NOTE HomeScreen is edited across Tasks 1–3, keep the edits coherent)
  </read_first>
  <action>
    (1) **D-13 — make the legacy Home term branch bound-only.** In `src/db/dashboard-read.ts`, `listDashboard`'s Branch 1 (the `if (hasTerm)` branch, ~230–240), add `AND ${DASHBOARD_BOUND_WHERE}` to the branch's inline `where` so it reads `c.archived_at IS NULL AND ${DASHBOARD_BOUND_WHERE} AND ( c.name LIKE … OR EXISTS ( … fuel … ) )` — an Unbound (`tracking_enabled = 0`) contact no longer surfaces in legacy Home search. Change ONLY Branch 1's inline `where`: do NOT touch the `BASE_WHERE` constant (155–158, must stay byte-identical), the favourites/snoozed/default branches, the snippet subquery, or the param order. This is the single authorized edit to the otherwise-frozen legacy `listDashboard` (D-13). Extend `dashboard-read.test.ts`: seed a matching-name Unbound (`tracking_enabled = 0`) contact and assert it is ABSENT from `listDashboard({term})`, while a matching Bound contact is present; assert the `BASE_WHERE` constant string is unchanged from its pre-plan value.

    (2) **Retire the Unbound retrieval rows from the Dashboard search surface (D-07).** Remove the `dashboardSearchRowPresentation` rendering + its import from `HomeScreen.tsx`, and DELETE `src/screens/dashboard-search-row-logic.ts` (now dead — grep confirms HomeScreen.tsx + HomeScreen.test.tsx are its only consumers) and its dedicated test `src/screens/HomeScreen.test.tsx` (its sole describe exercises the retired helper — verify no unrelated tests live there before deleting; if any do, remove only the Unbound-presentation block + import instead). Keep the change minimal on the legacy HomeScreen (it is replaced by the render phases); just stop surfacing Unbound rows there. With Branch 1 now bound-only (step 1), removing the neutral-row presentation no longer leaves Unbound rendering as normal cards — the data layer excludes them.

    (3) **Do NOT build the Unbound name-lookup replacement (D-13).** Do NOT add `searchUnbound` to `unbound-read.ts` and do NOT add a search box to `UnboundContactsScreen.tsx` — the owner accepted the one-phase Unbound name-lookup gap (D-13); the D-07/ADR-062/R-11 replacement is coordinated with Phase 26. `unbound-read.ts` and `UnboundContactsScreen.tsx` stay untouched (browse via `listUnbound` remains).

    Record in the SUMMARY (citing D-13): the legacy Home term branch is now bound-only so DASHQ-08's "search never surfaces unbound" holds at the data layer for BOTH the legacy Home search (this change) and the new scoped search (Plan 04); the Unbound name-lookup replacement is an owner-accepted transient gap deferred to Phase 26; `BASE_WHERE` and every other `listDashboard` branch are byte-unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/db/dashboard-read.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-read.test.ts asserts a matching-name Unbound (`tracking_enabled = 0`) contact is ABSENT from `listDashboard({term})` while a matching Bound contact is present (D-13); the `BASE_WHERE` constant string is byte-unchanged; only Branch 1's inline `where` gained `${DASHBOARD_BOUND_WHERE}` (`grep -n 'DASHBOARD_BOUND_WHERE' src/db/dashboard-read.ts` shows it in the term branch now).
    - `src/screens/dashboard-search-row-logic.ts` and `src/screens/HomeScreen.test.tsx` no longer exist (or HomeScreen.test.tsx retains only unrelated tests with the Unbound block + import gone); `grep -rn 'dashboard-search-row-logic\|dashboardSearchRowPresentation' src` returns no match.
    - `grep -rn 'searchUnbound' src` returns no match (the replacement is NOT built this phase — D-13); `unbound-read.ts` still exports only `listUnbound`/`countUnbound`; `UnboundContactsScreen.tsx` is unchanged.
    - Full `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The legacy Home term branch is bound-only (D-13, BASE_WHERE untouched) so Unbound no longer surfaces in search; the Dashboard-search Unbound rows are retired; no `searchUnbound` is added (owner-accepted gap, deferred to Phase 26).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retired routes → navigation | Removing a screen must not strand a navigate() call (crash) |
| Unbound search term → SQL | The new name search takes user input over the Unbound universe |
| portable key/column removal → backup format | Dropping a portable key unilaterally breaks backup compatibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.7-01 | Denial of Service (crash) | navigate() to deleted NeverContacted route | medium | mitigate | Every navigate target re-pointed to a live route atomically with the removal; tsc + grep gate confirm no dangling target |
| T-25.7-02 | Information Disclosure | Unbound leaking into legacy Home search (DASHQ-08) | high | mitigate | Legacy term Branch 1 made bound-only via the `${DASHBOARD_BOUND_WHERE}` code-constant (D-13); the term stays `?`-bound; test asserts an Unbound contact is absent from `listDashboard({term})` |
| T-25.7-03 | Availability (retrieval) | Unbound name-lookup removed, replacement deferred to Phase 26 | medium | accept | OWNER-ACCEPTED one-phase gap (D-13): browse via `listUnbound` (`UnboundContactsScreen`) stays available; the D-07/ADR-062/R-11 search replacement is coordinated with Phase 26; NOT an ADR-062 reversal |
| T-25.7-04 | Tampering (backup compat) | include_unbound key/column removal | high | mitigate | Key + column KEPT; only the toggle UI removed; removal coordinated with Phase 36 format bump (D-05) |
| T-25.7-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
</threat_model>

<verification>
- `npx vitest run src/db/dashboard-read.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
- `grep -rn 'BirthdayBanner\|NeverContactedScreen\|dashboard-search-row-logic\|dashboardSearchRowPresentation' src` (must show removals); `grep -rn 'name="NeverContacted"\|navigate("NeverContacted")\|"NeverContacted",' src` (no route registration / navigate / browseRoutes literal); `grep -rn 'searchUnbound' src` (must be empty — replacement deferred to Phase 26 per D-13); `grep -n 'includeUnboundNeverContacted' src/backup/backup-schema.ts` and `grep -n 'countNeverContacted' src/db/dashboard-read.ts` (must still be present)
- On-device UAT (Pixel): birthday-banner gone, Not-Contacted/backlog nav lands live, an Unbound contact does NOT surface in legacy Home search (bound-only per D-13) while the Unbound browse list still lists it, no archived/unbound leak in Dashboard search (25-VALIDATION Manual-Only).
</verification>

<success_criteria>
The birthday banner is removed (freshness retained, gap noted); the Never Contacted screen + include-Unbound toggle are retired with consumers re-pointed and the count read + column + portable key kept for Phase 36; the legacy Home term branch is bound-only (D-13, BASE_WHERE untouched) and the Dashboard-search Unbound rows are retired; the Unbound name-lookup replacement is an owner-accepted transient gap deferred to Phase 26 (no `searchUnbound` this phase); full suite green.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-07-SUMMARY.md` when done.
</output>