---
phase: 25-dashboard-data-state-foundation
plan: 05
type: execute
wave: 2
depends_on: [25-01]
autonomous: true
requirements: [DASHQ-12]
files_modified:
  - src/stores/dashboard-session-store.ts
  - src/stores/dashboard-session-store.test.ts
  - src/logic/dashboard-empty-logic.ts
  - src/logic/dashboard-empty-logic.test.ts
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An in-memory (non-persisted) session store holds the ephemeral Dashboard working state — search text and scroll offset — restored on Dashboard → Profile → Back and cleared on a fresh launch. [DASHQ-12]"
    - "Search text and scroll position are NOT written to `app_settings` / AsyncStorage; they live only in memory and reset naturally on a fresh process launch (dossier §M). [DASHQ-12]"
    - "The empty-state gate resolves ONE cause by explicit precedence (rowCount>0 → none → hasTerm → search-empty → activeFilter≠default → filter-empty → population fallback firstrun|hidden), extended from the old filter enum to the population model, with per-population empty causes folding into the same single gate (no parallel arithmetic in render screens). [DASHQ-12]"
    - "The gate's signature change is ADDITIVE, not a breaking cutover: the legacy filter-enum input shape still resolves (the sole current caller `src/screens/HomeScreen.tsx:308` keeps compiling in Wave 2 with NO edit to HomeScreen), and the new population-model input is accepted alongside it. This avoids a Wave-2 `tsc` break and the Plan-05/Plan-07 HomeScreen contention (Plan 07 edits HomeScreen in Wave 4). [DASHQ-12]"
    - "The store + gate are the DASHQ-12 state-shape foundation; the actual Dashboard→Profile→Back restoration wiring (search input binding, FlatList scroll capture/restore, cold-launch hydration) is bound and verified in the render phases (26–28), not here. Fresh launch restores the durable axes (view/populations/filters/sort) at the top of the collection with search cleared; Profile → Back restores everything including scroll — as the render phases wire it. [DASHQ-12]"
  prohibitions:
    - statement: "Search text and scroll position must NOT persist across a fresh launch — they are explicitly ephemeral (DASHQ-11/12); persisting them to app_settings or AsyncStorage is forbidden."
      status: enforced
      verification: "dashboard-session-store.ts imports no AsyncStorage and no DAO write; dashboard-session-store.test.ts asserts a new store instance starts with empty search + zero scroll."
    - statement: "The empty-state gate keeps ONE cause-aware decision with the locked precedence; no inline count arithmetic leaks into the render screens (the HIGH-2 / MEDIUM-4 fixes are preserved)."
      status: enforced
      verification: "dashboard-empty-logic.test.ts asserts the precedence: search-empty and filter-empty win before the population fallback; firstrun requires all populations empty."
  artifacts:
    - "dashboard-session-store.ts: in-memory Zustand store (search text + scroll offset; restore-on-Back, clear-on-fresh-launch)"
    - "dashboard-empty-logic.ts: cause-aware gate extended from the filter enum to the population model"
  key_links:
    - "dashboard-session-store (ephemeral) vs dashboard-query-store (durable) — the dossier §M durable/ephemeral split"
    - "population counts + activePopulations + hasTerm → selectDashboardEmptyState (one gate, render screens read the decision not the arithmetic)"
---

<objective>
Add the ephemeral half of the state split and extend the empty-state gate to the population model. The session store holds search text + scroll offset in memory (restored on Dashboard→Profile→Back, cleared on fresh launch); the empty-state gate grows from the old filter enum to the population model while keeping its single cause-aware precedence.

Purpose: DASHQ-12 requires the working session to survive navigation but reset on relaunch — the durable/ephemeral split the dossier §M mandates. The empty-state gate is the shared state-shape contract the render phases (26–28) bind to.

Output: `dashboard-session-store.ts` (new, in-memory Zustand); `dashboard-empty-logic.ts` extended to the population model.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-data-state-foundation/25-RESEARCH.md
@.planning/phases/25-dashboard-data-state-foundation/25-UI-SPEC.md
@CLAUDE.md

# Ground-truth decision record
@docs/dossier/milestone-2/phase-04-dashboard-data-state-foundation-dossier.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-memory ephemeral session store (search text + scroll)</name>
  <files>src/stores/dashboard-session-store.ts, src/stores/dashboard-session-store.test.ts</files>
  <behavior>
    - The store holds `{ searchText: string, scrollOffset: number }` with setters, plus a `clearSession()` that returns to the empty defaults.
    - It is a plain in-memory Zustand store (no `persist`, no AsyncStorage, no DAO write) — a fresh store instance starts with empty search + zero scroll.
    - The store is the restore surface for Dashboard→Profile→Back (the values survive navigation because the store outlives the screen) and resets naturally on a fresh process launch.
  </behavior>
  <read_first>
    - src/stores/shell-transient-store.ts (READ IN FULL: the shipped non-persisted in-memory Zustand idiom — the precedent RESEARCH A4 cites; mirror its plain-`create` shape)
    - src/stores/dashboard-query-store.ts (the DURABLE counterpart from Plan 01 — this store is deliberately its ephemeral opposite; do not persist here)
    - src/stores/dashboard-prefs-store.ts (the AsyncStorage store being superseded — the anti-pattern to NOT copy for ephemeral state)
  </read_first>
  <action>
    Create `src/stores/dashboard-session-store.ts` as a plain `create(...)` Zustand store (NO `persist`, NO `createJSONStorage`, NO AsyncStorage import) holding `searchText` (default '') and `scrollOffset` (default 0) with `setSearchText`, `setScrollOffset`, and `clearSession` (reset to defaults). Document in-file that this is the EPHEMERAL session state (dossier §M): it survives Dashboard→Profile→Back because the store outlives the screen, and it resets on a fresh launch because it is never persisted. Tests: default empty state; setters update; clearSession resets; the module imports no AsyncStorage.
  </action>
  <verify>
    <automated>npx vitest run src/stores/dashboard-session-store.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-session-store.test.ts asserts a fresh store starts with `searchText === ''` and `scrollOffset === 0`; setters update; `clearSession` resets.
    - `grep -n 'async-storage\|persist\|createJSONStorage' src/stores/dashboard-session-store.ts` returns no match (ephemeral by construction).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The ephemeral session store holds search text + scroll offset in memory only, restored on Back and cleared on fresh launch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the empty-state gate from the filter enum to the population model</name>
  <files>src/logic/dashboard-empty-logic.ts, src/logic/dashboard-empty-logic.test.ts</files>
  <behavior>
    - `selectDashboardEmptyState` accepts the active population set (not just the filter enum) and resolves ONE cause by the locked precedence: rowCount>0 → none → hasTerm → search-empty → (active filter present) → filter-empty → population fallback (firstrun|hidden), with per-population empty causes (Birthdays / Not Contacted / Snoozed) folding into the same gate.
    - The signature is extended ADDITIVELY: the existing filter-enum `DashboardEmptyInput` shape keeps resolving (so `HomeScreen.tsx:308`, the sole caller, compiles unchanged), and the new population-model fields are OPTIONAL additions the render phases pass. No breaking cutover of the input type in this wave.
    - firstrun still requires all populations empty; a hidden-bucket-only user gets 'hidden', never 'firstrun'.
    - No inline count arithmetic leaks to the render screens — the gate owns the decision, the screen owns the copy (per the UI-SPEC Copywriting Contract).
  </behavior>
  <read_first>
    - src/logic/dashboard-empty-logic.ts (READ IN FULL: DashboardEmptyState, DashboardEmptyInput, selectDashboardEmptyState — the current filter-enum gate with its HIGH-2/MEDIUM-4 precedence)
    - src/logic/dashboard-query-logic.ts (DashboardPopulation, DashboardFilters — the population/filter model the extended input references)
    - .planning/phases/25-dashboard-data-state-foundation/25-UI-SPEC.md (§Empty states — the cause list incl. per-population empties; copy lives there, not in this gate)
  </read_first>
  <action>
    Extend `dashboard-empty-logic.ts` ADDITIVELY: grow the input to accept the population model — the active populations set + the active filters + `hasTerm` + `rowCount` + the population counts — WITHOUT breaking the existing filter-enum input shape the sole caller `HomeScreen.tsx:308` passes. Make the new fields optional (or accept a union of the old + new input) so `npx tsc --noEmit` stays green in Wave 2 with HomeScreen UNCHANGED (do NOT add HomeScreen to this plan's `files_modified` — Plan 07 owns the HomeScreen edits in Wave 4, and an additive signature removes the cross-wave contention the review flagged). Preserve the EXACT precedence (rowCount>0 → none → hasTerm → search-empty → active-filter → filter-empty → population fallback). Add the per-population empty causes the UI-SPEC lists (Birthdays / Not Contacted / Snoozed empties) as sub-cases resolved within the same gate (return a cause the screen maps to copy; do NOT put copy here). Keep firstrun = all-populations-empty (the HIGH-2 fix) and search/filter empties winning before the population fallback (the MEDIUM-4 fix). Keep the module pure. Verify with a grep that the additive change compiles for every existing caller (`grep -rn 'selectDashboardEmptyState\|DashboardEmptyInput' src` — confirm `HomeScreen.tsx:308` still type-checks unedited). When Gravity is active, the caller passes `rowCount` = the post-Gravity survivors' length (Plan 03 seam), not a SQL count. Tests: precedence ordering; per-population empties; firstrun requires all empty; a zero-result filter over a non-empty population resolves filter-empty (not hidden); the legacy filter-enum call shape still resolves (additive-compatibility test).
  </action>
  <verify>
    <automated>npx vitest run src/logic/dashboard-empty-logic.test.ts && npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-empty-logic.test.ts asserts: rowCount>0→none; hasTerm+0 rows→search-empty (wins over filter/population); active filter+0 rows→filter-empty; all-populations-empty→firstrun; hidden-bucket-only→hidden.
    - Per-population empty causes (Birthdays/Not Contacted/Snoozed) resolve within the single gate; no copy strings live in the logic module.
    - The legacy filter-enum input shape still resolves (an additive-compatibility test) so `HomeScreen.tsx:308` compiles UNEDITED; `HomeScreen.tsx` is NOT in this plan's `files_modified`.
    - `grep -rn 'selectDashboardEmptyState' src` shows every existing caller still compiles against the additive input; full `npx vitest run` green.
    - `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>The cause-aware empty-state gate is extended to the population model with its locked precedence intact; render screens read the decision, not the arithmetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ephemeral session state → memory only | Search text / scroll must never reach durable storage or a backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.5-01 | Information Disclosure | session store persistence | low | mitigate | The store is in-memory only (no persist/AsyncStorage/DAO); search text + scroll never reach durable storage or the backup wire; test asserts a fresh instance is empty |
| T-25.5-02 | Tampering | empty-state input | low | accept | Pure function over counts/flags; no I/O, no SQL; cannot be tampered via a data path |
| T-25.5-SC | Tampering | npm/pip/cargo installs | low | accept | No external package installed this plan |
| T-25.5-03 | Spoofing / Auth / Network | — | n/a | accept | No accounts, no network — no applicable threat (local-first) |
</threat_model>

<verification>
- `npx vitest run src/stores/dashboard-session-store.test.ts src/logic/dashboard-empty-logic.test.ts`
- `npx vitest run` (full suite) + `npm run check:colors` at wave merge
- `npx tsc --noEmit`
</verification>

<success_criteria>
The ephemeral session store holds search text + scroll in memory (restore-on-Back, clear-on-launch, never persisted); the empty-state gate is extended to the population model with its single cause-aware precedence intact.
</success_criteria>

<output>
Create `.planning/phases/25-dashboard-data-state-foundation/25-05-SUMMARY.md` when done.
</output>