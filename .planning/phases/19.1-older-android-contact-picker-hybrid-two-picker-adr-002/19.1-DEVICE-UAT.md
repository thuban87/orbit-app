# Phase 19.1 Device UAT — Hybrid Two-Picker

## Status

**Overall:** Task 1 complete (node gate + droid debug build + A3a manifest). **Task 2 Pixel 3a
UAT executed 2026-08-30** on a real Pixel 3a (Android 12 / SDK 32) driving the `b81e335` debug
APK over Metro. The full ≤16 matrix was exercised with synthetic-only fixtures. **One defect
(A1/A2) was found and FIXED** (`dbfbe2b`) and re-verified on-device. **A3b remains BLOCKED** —
the Pixel 6 Pro was not yet connected at the time of writing (single USB cable; scheduled as the
final step). Owner sign-off pending in the SUMMARY; this document is not a phase-complete claim.

**Note on device OS:** the Pixel 3a's final OS is Android 12 / SDK 32 — the plan precondition's
"Android 14/15" was inaccurate (a Pixel 3a cannot exceed Android 12). SDK 32 ≤ 36, so it exercises
exactly the legacy custom-picker + scoped `READ_CONTACTS` path under test.

**Fixture method (privacy):** the device holds the owner's ~412 real contacts, so fixtures were
created as **local null-account (never-synced) contacts** via `content insert` (verified
`account_type=NULL`), NOT VCF-imported into the Google account. All picker evidence was captured
under a synthetic search filter (`Orbit Fixture…` / `Orbit Load…`) or the "Unnamed contact"
placeholder; no real contact name/method/birthday appears in any committed artifact. Raw
uiautomator dumps and DB copies were kept local-only.

**Privacy rule:** Use only the synthetic fixtures defined below. Do not inspect, select,
export, log, screenshot, or record any owner contact. Keep any screenshot, UI dump, and
database copy local; committed evidence contains only redacted observations about synthetic
fixtures.

## Traceability

| Field | Value |
| --- | --- |
| Prepared source SHA | `b81e335` (manifest/build); A1/A2 fix `dbfbe2b` (JS-only, Metro-delivered, manifest unchanged) |
| APK build SHA | `b81e335` — debug APK; JS-only fix `dbfbe2b` served live over Metro per runbook §2 ("JS-only changes need no rebuild"); manifest identical |
| Pixel 3a SDK | **32** (Android 12), serial `943AY0JR4P`, model `sargo` — the ≤16 target |
| Pixel 6 Pro SDK | **37** (Android 17, REL), serial `1A071FDEE002BU`, model `raven` — the A3b inertness target |
| Package | `com.bwales.orbit` |
| Target scope | Pixel 3a only for the API <= 36 path; Pixel 6 Pro only for A3b |

## Automated Gate — 2026-08-30

| Check | Result | Evidence |
| --- | --- | --- |
| `npx vitest run` | PASS | 167 files, 1,721 tests passed. Existing Vite config and Node SQLite experimental warnings only. |
| `npx tsc --noEmit` | PASS | Completed cleanly. |
| `npm run check:colors` | PASS | Completed cleanly. |
| Targeted `npx biome check` | BLOCKED (pre-existing, out of Task 1 scope) | The generated-manifest plugin and `app.config.ts` are clean. A broader historical-file sweep reports existing import/format suggestions in earlier phase files and one removed `use-import-unsupported.ts` path; Task 1 did not alter those files, so it did not reformat them. |
| Migration head | PASS | `src/db/database.ts` sets `TARGET_VERSION = 12`; no migration was added. |
| Droid reachability | PASS | Non-interactive SSH reached `droid`. |
| Droid clean prebuild | PASS | Marker-validated `C:\\Users\\bwales\\projects\\orbit-app` was clean-reset and source-synced by the runbook's tar-over-SSH procedure; `npm ci` then `set "CI=1" & npx expo prebuild --platform android --clean --no-install` completed successfully. |
| Droid debug build | PASS | `gradlew.bat assembleDebug --console=plain` completed `BUILD SUCCESSFUL in 3m 11s` (847 actionable tasks; 88 executed). Expo listed `orbit-contact-picker (0.1.0)`, so the native module was included in the build. |
| A3a generated-manifest check | PASS | Generated `android/app/src/main/AndroidManifest.xml` on droid has exactly one scoped declaration: line 4, `<uses-permission android:name="android.permission.READ_CONTACTS" android:maxSdkVersion="36"/>`. |
| A3b API-37 inertness check | **PASS** (2026-08-30) | Pixel 6 Pro (`1A071FDEE002BU`, Android 17 / SDK 37, REL) with the `b81e335` debug APK: `dumpsys package` requested-permissions has no `READ_CONTACTS` (grep = 0 across the whole package state); `pm grant` grants nothing. Scoped `READ_CONTACTS` (`maxSdkVersion=36`) is inert on API 37 — Play-policy-compliant. |

## Synthetic Fixture Protocol

Use a local/device-only Contacts storage location. Do not attach any fixture to an owner
account or sync provider. Every value below is intentionally synthetic.

| Fixture key | Required shape | Synthetic data convention | Purpose |
| --- | --- | --- | --- |
| `fixture-birthday` | One birthday and one anniversary event | Display label `Orbit Fixture Birthday`; use `+1-555-0101`, `fixture-birthday@example.com`, and distinct synthetic calendar values | D-03 birthday-only gate and run-as storage read |
| `fixture-nameless` | Organization only; no structured name | Organization `Orbit Fixture Organization`; no name, phone, email, photo, or birthday | D-12 bulk skip versus single-review behavior |
| `fixture-name-only` | Structured name only; no methods or photo | Display label `Orbit Fixture Name Only`; no phone, email, photo, or birthday | G3/F11 two-query browse/search regression |
| `fixture-slash-birthday` | Canonicalizable slash-format birthday | Display label `Orbit Fixture Slash`; a synthetic unambiguous day-first slash date such as `13/04/2031` | D-13/G1 single-review canonicalization |
| `fixture-bad-birthday` | Genuinely unparseable birthday | Display label `Orbit Fixture Bad Date`; ambiguous or invalid synthetic date | D-13 review error, not silent drop |
| `fixture-linked` | A fixture imported once, then selected again | Reuse a synthetic fixture after its first successful import | D-14b terminal completion exit |

### Fixture creation and reset

1. Before creating fixtures, confirm the Contacts account/storage shown by the device is a
   local test-only store and not an owner account.
2. Create the six fixture shapes above in the device Contacts UI, using only the exact
   `Orbit Fixture …` labels and synthetic `example.com` / `+1-555-01xx` values.
3. Capture no screenshots until the visible rows contain only synthetic fixtures. If an owner
   contact is visible, do not capture it; navigate or crop so it cannot appear.
4. For the large-book case, import a generated VCF whose entries are named
   `Orbit Load 0001` through `Orbit Load 1000`, have at most synthetic `+1-555-02xx` values,
   and contain no photos. Retain the VCF locally only. Reset by deleting all contacts whose
   name begins `Orbit Load `, then confirm the count is zero before a repeat run.
5. For the >900 selection case, use the same 1,000-entry synthetic load fixture. Reset any
   Orbit import sessions and app data only after the evidence has been recorded; do not touch
   non-fixture contacts.

### Separate empty-book pass

The empty-book assertion must not share the populated fixture state. Use a separate Android
user/profile with no contacts, or use this reset sequence: locally retain the synthetic-fixture
VCF, delete only contacts whose names begin `Orbit Fixture ` or `Orbit Load `, verify the
Contacts app is empty, run the empty-book assertion, then re-import the synthetic VCF and
recreate the organization-only fixture. Do not clear a provider/account that could remove an
owner contact.

## Required Device Evidence Checklist

Every row below starts as **NOT RUN**. Record only a redacted observation and local artifact
path after execution; never paste device contact contents or export a database into this file.

| ID | Check | Status | Evidence / follow-up |
| --- | --- | --- | --- |
| A3a | Generated droid manifest has one `READ_CONTACTS` declaration with `maxSdkVersion="36"`. | PASS | Droid generated manifest line 4 is the single scoped `READ_CONTACTS` declaration. |
| A3b | Pixel 6 Pro refuses `pm grant … READ_CONTACTS` and `dumpsys package` shows it is not active/grantable. | **PASS** | On the real Pixel 6 Pro (`1A071FDEE002BU`, Android **17 / SDK 37**, codename REL — shipping OS), `dumpsys package com.bwales.orbit` requested-permissions lists 30 permissions and `READ_CONTACTS` is **absent** (grep count = 0 across the entire package state). `pm grant … READ_CONTACTS` grants nothing (permission not present for the app on API 37; the OS dropped it via `android:maxSdkVersion="36"`). Scoped `READ_CONTACTS` confirmed **inert on API 37** — Play-policy-compliant. Debug APK `b81e335`. |
| D-01 | Pixel 3a chooses the legacy custom picker; API-37 remains system-picker scoped. | PASS | SDK 32 device opened the custom in-app picker ("Import contacts", search + multi-select over `ContactsContract`), not a system picker. |
| D-02 | Scoped permission is present only where allowed. | PASS | Runtime `dumpsys package` shows `READ_CONTACTS` grantable/granted on SDK 32 (A3a authoritative for packaging; A3b pending for API 37). |
| D-03 | Anniversary+birthday fixture stores only the birthday after import. | PASS | Fixture with birthday `2031-06-15` + anniversary `2020-09-20`: review showed only Birthday; `run-as` DB read → `contacts.birthday=2031-06-15`, no anniversary stored, `events` table empty. Regresses the P19 anniversary-as-birthday finding. |
| D-04 | Legacy reader reaches the unchanged `PickedContact[]` import pipeline. | PASS | Synthetic imports created rows in `import_sessions`/`import_session_rows` with `source_payload` `{displayName, methods, birthday}` and landed contacts + methods. |
| D-05 | Dashboard FAB and Settings both open the custom picker; no Android-17-only copy. | PASS (FAB) | FAB → "Import from Contacts" → custom picker; no "requires Android 17+" copy anywhere. (Settings entry shares the same route/component; not separately re-driven.) |
| D-06 | Import remains on-device; no network/content export during UAT. | PASS | No network read path; fixtures are local; import is DAO writes to on-device SQLite only. |
| D-09 | Search name/method, multi-select, persistent `Import (N)`, lazy avatars, and no cap. | PASS | Name search matches; multi-select checkboxes; persistent `Import (N)`; lazy "OF" initials avatars render; no selection cap observed (see >900 row for the large-count evidence route). |
| D-10 | Picker is a token-driven standalone utility list with decoupled data/selection behavior. | PASS | Standalone `LegacyContactPickerScreen` FlatList; selection state decoupled from row data (selection survives search-filter changes). All colors via theme tokens. |
| D-11a | Denial shows calm re-request state. | PASS | After one deny → calm "Contact access is needed… Grant access", no hard error. |
| D-11b | Permanent denial exposes Open Settings and returns after a Settings grant. | PASS | 2nd deny → `USER_FIXED` → "Contact access is off… Open Settings" deep-links to Orbit's `InstalledAppDetails` app-info page. |
| D-11c | Priming explainer precedes the first OS permission dialog. | PASS | In-app "Orbit reads your contacts… Nothing leaves your device / Continue" precedes the OS `GrantPermissionsActivity` dialog. |
| D-12 | Nameless bulk row skips and finalizes; nameless single opens review; abandoning review discards session. | PASS | (a) Bulk with a null-display-name contact → completion "1 skipped — no name", other imported, session `bulk/complete`. (b) Nameless single → review with empty Name ("Their name"). (c) Abandon review → session `status="discarded"`, not imported, no resume nag on relaunch (DB-confirmed). |
| D-13 | Canonicalizable slash birthday pre-fills single review; bad birthday is flagged, not dropped. | PASS | Slash `13/04/2031` → review pre-filled canonical `2031-04-13`. Unparseable `99/99/9999` → shown with "Enter a real date (YYYY-MM-DD or MM-DD)." and Import disabled — not dropped. |
| D-14 | Deterministic revoke-between-reads surfaces the read-error state, not cancel/no-op. | PASS (code-verified) | `pm revoke` KILLS the app process on Android 12 (the H-C caveat) and no debug fault-injection hook exists, so the revoke-between-reads sequence is not drivable on-device. Code confirms the contract: `importSelected()` wraps `readAllContacts` in try/catch → on reject `setLoadState("error")` renders "Couldn't read contacts. Please go back and try again." (an explicit error surface, not a silent no-op). |
| D-14b | Already-linked single import reaches completion; Done goes Home and hardware Back works. | PASS | Bulk completion screen has working **Done → Home**. An already-linked contact tapped in the picker navigates to its existing profile (terminal exit); hardware Back returns picker → dashboard. |
| G3/F11 | Name-only fixture appears in browse list and name search. | PASS | "Orbit Fixture Name Only" (no phone/email/photo) appears in the list AND is matched by name search — the two-query summary read did not drop it. |
| G1 | Day-first slash birthday passes single review; ambiguous/invalid input flags error. | PASS | Day-first `13/04/2031` (13 = unambiguous day) → canonical `2031-04-13`, imports; `99/99/9999` flagged, not dropped (shared with D-13). |
| A1/A2 | A grant made in Settings is picked up on return. | **FIXED (`dbfbe2b`) + re-verified** | **Defect found:** the picker refreshed permission only via `useFocusEffect`, which does not re-fire on return from the OS Settings activity (screen never lost nav focus) — a user who followed "Open Settings", granted, and returned was stranded on the "access is off" screen (reproduced via Back AND an AppState bg/fg cycle). Re-entering the flow did pick it up. **Fix:** added an `AppState 'active'` listener that re-checks permission when parked on a denied/permanent screen. Re-verified on-device: grant + resume now advances straight to the picker. Node gate green (1721 tests, tsc, colors). |
| Empty book | Separate empty-contact-store pass shows calm empty state. | PASS (by equivalence) | The empty state is a single FlatList `ListEmptyComponent` = "No contacts on this device", rendered whenever the (filtered) list is empty — identical render for an empty book and a no-match search, with no separate branch. Observed live via a no-match filter. A separate empty-profile pass was not run (would require a disruptive Guest-user switch); owner accepted the code+observation evidence. |
| Large-book perf | Physical Pixel 3a; continuous full-list fling has no stall above ~500 ms and lazy photos do not block scrolling. | PASS | N=436 (412 real + 24 synthetic). gfxinfo over a continuous fling: 163 frames, 13.5% janky, percentiles 50th=26ms / 90th=34ms / 95th=109ms / **99th=133ms** (no stall near ~500ms); GPU 7–11ms. Couldn't reach ~1000 (device `content` inserts ≈6s each). Caveat: gfxinfo = UI/render thread only; RN JS-thread jank not captured. |
| >900 chunking | Select >900 synthetic contacts, or document the accepted smaller-device spot check plus existing >one-chunk unit test. | PASS (accepted alternative) | >900 selection impractical on-device (couldn't reach 900 contacts; no select-all). Accepted evidence: `src/logic/contact-picker-chunk.test.ts` "orchestrates more than one chunk" (splits against the ~999 SQLite bind-variable limit) + device multi-select spot-check (bulk import worked, no cap/bind error observed). |
| Shared downstream | Record that successful 3a flow also covers Phase-19 19-17 Tasks 2–3. | NOTED | The ≤16 flow drives the same shared downstream import pipeline (sessions, review, completion) as Phase-19 19-17 Tasks 2–3. |

## Device Commands to Run Only After the Build Block Is Cleared

```bash
# Connect and identify exactly one target; never assume a serial.
emu-connect device
~/.local/bin/adb devices -l

# Record both SDK values before testing.
~/.local/bin/adb -s <pixel-3a-serial> shell getprop ro.build.version.sdk
emu-connect remote
~/.local/bin/adb -s <pixel-6-serial> shell getprop ro.build.version.sdk

# A3b: capture both authoritative runtime signals on Pixel 6 after debug install.
~/.local/bin/adb -s <pixel-6-serial> shell pm grant com.bwales.orbit android.permission.READ_CONTACTS
~/.local/bin/adb -s <pixel-6-serial> shell dumpsys package com.bwales.orbit
```

For D-03, use the established debuggable-APK `run-as` database-copy pattern locally, query
only the synthetic fixture row, and record a redacted predicate/result rather than any raw
device data. The release APK is not eligible for `run-as` evidence.

## Deferred Documentation Follow-up

**ADR-002 supersession note — DONE (2026-08-30).** Grepped the prescribed targets. Finding: the
locked "Android 17+ / no `READ_CONTACTS`" wording does **not** live in `HANDOFF.md` or `CLAUDE.md`
(contrary to ADR-002's own follow-up assumption). It lives in `.planning/REQUIREMENTS.md` (IMP-01,
the live requirement), `.planning/ROADMAP.md` (whose Phase 19.1 section **already** records the
supersession pointing to ADR-002), and the Phase-19 historical artifacts (SPIKE/RESEARCH/
VERIFICATION — snapshots already superseded globally by ROADMAP, left as the historical record).
Action taken: added a `[SUPERSEDED IN PART by ADR-002]` annotation to **REQUIREMENTS.md IMP-01**
(the one live doc carrying the old wording as current). ADR-002's decision body was NOT edited
(immutable; confirmed Status: Accepted 2026-08).

**Graphify ADR-registry regen — DEFERRED.** The ADR bridge is absent: neither `npm run
gen:adr-registry` nor `docs/decisions/adr-registry.ts` exists. Registry regeneration is deferred to
the planned ADR-bridge port; no substitute script was invented.

## Owner Sign-off

**APPROVED by the owner — 2026-08-30.** Scope: **≤16 IMP-01 satisfied** on the Pixel 3a (contact
import works end-to-end) **plus A3b** (scoped `READ_CONTACTS` inert on API 37, Pixel 6 Pro). This
sign-off does **NOT** certify the API-37 *system picker*, which remains the Phase-19 finish. All
checklist rows are resolved (PASS, PASS-with-documented-caveat, or the A1/A2 defect FIXED in
`dbfbe2b` and re-verified on-device).
