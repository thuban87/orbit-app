---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
doc: convergence-log
---

# Phase 19.1 — Plan Convergence Log

This log tracks cross-AI review findings against the five Phase-19.1 PLAN.md files and how each
plan was revised to address them. Reviewer attributions come from `19.1-REVIEWS.md` (codex + cursor,
verified against on-disk source) plus the aggregated consensus labelling; findings not present in the
codex/cursor sections are attributed to the third (claude) reviewer per the review brief.

---

## Cycle 1

**Nature of this revision:** SCALPEL. The five plans were **hand-edited in place** — only the specific
task `<action>`/`<acceptance_criteria>`/`<behavior>`/`must_haves`/frontmatter sections named by each
finding were changed. The plans were **not re-cut** (no task renumbering, no wave/dependency
reordering, no re-splitting of the hand-tuned task/finding assignments). Every edit implements a
recorded decision (ADR-002 / D-01..D-14b) **more correctly**; none reverses or weakens one.

**Re-review:** these cycle-1 edits are expected to be re-reviewed in cycle 2.

### Owner decision incorporated

- **D-14b → Option A (preserve the View-contact shortcut).** The already-linked single import must not
  dead-end on a single-button Alert, but the one-tap "View contact" jump-to-profile it offered must be
  PRESERVED, not dropped. Disposition: a net-new "View contact" action is ADDED to `ImportCompleteScreen`
  for the single already-linked case (navigating to `Profile` with the row's `matched_contact_id`), and the
  already-linked single import is routed to `ImportComplete`. Dead-end removed AND shortcut kept. (Plan 02, Task 3.)

### Findings F1–F18

| ID | Severity | Reviewer(s) | Plan(s) | Disposition (how the plan now addresses it) |
|----|----------|-------------|---------|----------------------------------------------|
| **F1** | HIGH | codex + cursor + claude (3-reviewer consensus) | 02 (Task 1) | Single nameless pick now terminalizes 'skipped'/'name-required', finalizes the session, and routes to **ImportComplete** (never ImportReview) — no resume nag, no leave-guard-defeat dangling session; end-to-end integration (DAO+navigation) test required. |
| **F2** | (owner-approved) | codex + cursor (D-14b verified) | 02 (Task 3) | Stopped claiming ImportComplete already has "View contact" (it does not). Plan now ADDS that action to ImportComplete for the single already-linked case (→ Profile via `matched_contact_id`), then routes the already-linked single import there — dead-end removed, shortcut preserved (owner Option A). |
| **F3** | HIGH (codex) / MED (cursor) | codex + cursor | 03 (Tasks 1–2) | `ContactPickerRow` gains `searchMethods: string[]` (ALL phone/email); `matchesQuery` searches displayName + every method (primaryMethod is subtitle-only); native `listContactsSummary` projects all methods per contact. Acceptance: matches a SECONDARY method. |
| **F4** | HIGH (codex) / MED (cursor) | codex + cursor | 03 (Task 2) | Uncapped selected-key read now CHUNKS lookupKeys under a conservative bind limit (≤900/query) as the PRIMARY mechanism, merges by key, preserves selected order; test for a selection larger than one chunk; reject-on-error kept per chunk. |
| **F5** | HIGH (codex) / MED (cursor) | codex + cursor | 04 (Task 1) | `classifyPermissionResult` reworked: NEVER_ASK_AGAIN → permanent; plain DENIED disambiguated by `shouldShowRequestPermissionRationale` sampled AFTER the request (rationale=false & not granted → permanent; else denied); signature takes the real rationale signal, not a bare "requested before"; ambiguous → denied (never falsely permanent). A1 device confirmation kept. |
| **F5b** | LOW | codex (derived from F5) | 04 (Task 1) | Documented where any "requested before" / prior-attempt state lives (in-memory per screen session vs a persisted flag) and that it affects permanent-denial detection across app restarts. |
| **F6** | HIGH (codex) / LOW (cursor) | codex + cursor | 05 (Task 1) | Split the permission evidence: (a) generated-manifest `maxSdkVersion="36"` grep on droid = PACKAGING (A3a); (b) NEW check on the Pixel 6 Pro (API 37) that the effective manifest carries NO active READ_CONTACTS = inert-on-API-37 (A3b). If (b) can't run it is recorded BLOCKED, never passed off the ≤16 Pixel 3a. |
| **F7** | MED | cursor (emphasis) + codex | 01 (Task 3), 03 (Task 3), 04 (Task 3) | Read/failure error surfacing made end-to-end: legacy ≤16 screen shows "Couldn't import contacts" on `readAllContacts()` rejection (plan 01); `listContactsSummary` failure gets a calm error state alongside loading/empty (plan 03); Settings system-path failure surfaces the SAME Alert as the FAB, not just `Logger.error` (plan 04). |
| **F8** | MED | claude (D-13 broadening) | 02 (Task 2) | Slash-format birthday parsing accepts only UNAMBIGUOUS order: a field >12 fixes day-vs-month; `YYYY/MM/DD` (4-digit leading) always parses; both fields ≤12 (e.g. `03/04/1990`) → treated UNPARSEABLE (flag on single review / count on bulk), never coerced to a guessed date. Strict calendar gate unchanged. Test table updated. |
| **F9** | MED | cursor | 02 (Task 1) | The distinct "N skipped — no name" count is EXCLUDED from the existing `failedOrSkipped` bucket so name-required rows are counted once; partition test asserts `failedOrSkipped`, "no name", and "birthday unreadable" do not overlap. |
| **F10** | MED | codex (cursor light) | 03 (Task 3) | Picker photo rows render the external `content://`/`file://` thumbnail via a dedicated `expo-image` `Image` fed the URI directly; `Avatar` (which expects a durable `resolvePhotoUri` path) is used ONLY for the initials fallback. No external URI reaches the persisted-photo Avatar API. |
| **F11** | MED | codex | 03 (Task 2) | `listContactsSummary` enumerates from a source that INCLUDES name-only contacts (Contacts/RawContacts or StructuredName), not a `MIMETYPE IN (phone,email,event,photo)` filter that drops them — a name-only contact is still pickable (also matters for the D-12 nameless edge). |
| **F12** | MED | codex | 02 (Task 1) | A nameless contact is classified BEFORE its photo is staged in `acceptPickedContacts` (or the staged photo is explicitly cleaned up) so a terminal 'skipped'/'name-required' row leaves no orphaned staged contact-photo PII. Acceptance added. |
| **F13** | MED | codex | 01 (Task 3 + assumption_delta) | Shared-sink test claim corrected to match the real path: split into (i) DISPATCH — `startContactImport` calls `routePickedImport` for "system" and `navigate("LegacyContactPicker")` for "legacy" (not the sink); (ii) LEGACY HANDOFF — the picker's Import action calls `routePickedImport` with the same sink. |
| **F14** | HIGH | cursor | 01 (must_haves) | Narrowed the D-05 seam claim: plan 01's guarantee is FAB-scoped (the seam exists and the FAB dispatches through it); Settings is routed through the same seam in plan 04 — plan 01 no longer claims both entry points are done. |
| **F15** | LOW | codex | 05 (frontmatter) | Added `HANDOFF.md` and `CLAUDE.md` to Task 2's `files_modified` (the ADR-002 supersession note edits both, but they were missing from the header). |
| **F16** | (dead surface) | claude | 01 (action/acceptance/artifacts) | Removed `isLegacyContactReadRequired` (native Function, JS wrapper, `OrbitContactPickerModule.ts` decl, artifacts + acceptance) — it was specified but never consumed (`contactImportMode()` uses `isContactPickerAvailable()`; no plan calls it). `contactImportMode()` left unchanged. Stale references also dropped from plans 03 (read_first) and 05 (build-confirm) for coherence. |
| **F17** | LOW | cursor | 01 (Task 3) | Made explicit that the `LegacyContactPicker` route takes params `{}` and the screen re-fetches `getAppSettings`/`getDeviceRegion`/`localDateTime()` (mirroring `AddSpeedDialFab.tsx:63-74`) to build the `{effectivePhoneRegion, now}` it passes to `routePickedImport`. |
| **F18** | LOW | cursor | 02 (deferred note) | Added a note that the Android-17 (system-picker) reader's read-exception→`resolve(emptyList())` swallow (`OrbitContactPickerModule.kt:118-120`) remains UNFIXED here (docketed to the Phase-19 finish per ADR-002) so it is not mistaken as done. |

### Additional LOW/MED items from the brief (not F-numbered)

| Item | Severity | Plan | Disposition |
|------|----------|------|-------------|
| Repo-wide unsupported grep | LOW | 04 (Task 3) | Acceptance adds `grep -rl "use-import-unsupported" src/` returns nothing after deletion (stronger than tsc-only). |
| Reproducible D-14 read-failure recipe (F5-UAT) | MED | 05 (Task 2) | Replaced "simulate/force a read failure" with a concrete method (revoke READ_CONTACTS mid-read, or a debug-only fault-injection hook absent in production), recorded in the UAT doc. |
| Split sign-off scope | LOW | 05 (Task 2) | Sign-off explicitly scopes success as "≤16 IMP-01 satisfied" (+ A3b), NOT "API-37 system picker" (Phase-19 finish). |
| Fixture setup steps | MED | 05 (Task 2) | UAT precondition now requires the doc to record the address-book fixture setup (anniversary+birthday, nameless/org-only, OEM-format birthday) so the matrix is reproducible. |

### No decision reversed

Every edit implements ADR-002 / D-01..D-14b more correctly. D-14b was resolved per the owner's Option A
(preserve the View-contact shortcut). No `[DECIDED]`/`[REJECTED]` item in `HANDOFF.md` or any ADR was
deleted, weakened, or inverted.

---

## Cycle 2

**Nature of this revision:** SCALPEL, same discipline as cycle 1 — the five plans were **hand-edited in
place** (only the named `<action>`/`<acceptance_criteria>`/`<behavior>`/`must_haves`/frontmatter sections),
**not re-cut** (no task renumbering, no wave/dependency reordering, no re-splitting of assignments).
Reviewers this cycle: **codex + cursor + a sonnet-5 reviewer**, re-reviewing the cycle-1 edits.

Every edit implements a recorded decision more correctly **except the one explicit owner-approved scoping
change (G2)**, which REVERTS a cycle-1 edit (see below).

### Owner decision incorporated (REVERSES a cycle-1 edit)

- **G2 — D-12 single-pick = KEEP NAMING.** Cycle 1 routed a nameless SINGLE pick straight to
  `ImportComplete` (auto-terminalize at accept time, ImportReview never mounts, widen
  `PickedImportNavigator` to `"ImportComplete"`, leave-guard-defeat treated as moot). The owner has ruled
  that a nameless SINGLE pick must **still open `ImportReview`** so the user can type a name and import;
  if the review is abandoned, the EXISTING leave-guard (`use-import-leave-guard.ts`) discards the
  unresolved session. Only **BULK** rows auto-skip to 'skipped'/'name-required'. Disposition (Plan 02,
  Task 1): the cycle-1 single→ImportComplete language, the "ImportReview never opens" claim, the
  `PickedImportNavigator` widening, and the leave-guard-defeat-is-moot rationale are all REMOVED; the
  single-nameless test now asserts the pick routes to `ImportReview` (not `ImportComplete`) and can be
  named + imported. This is the sole intentional decision reversal in this cycle, and it is owner-approved.
  Coherent knock-on scoping applied to Plan 05's D-12 UAT rows and truths (nameless→skipped is BULK-only;
  single nameless keeps naming).

### Findings G1–G13 (+ minor items)

| ID | Severity | Reviewer(s) | Plan(s) | Disposition |
|----|----------|-------------|---------|-------------|
| **G1** | HIGH | codex + sonnet | 02 (Task 2) | **Birthday seed fix.** Cycle-1 directed seeding `birthdayInput` from the RAW `snapshot.birthday`, which BREAKS import of a valid OEM slash birthday: `normalizeEditedBirthday` accepts only hyphenated `MM-DD`/`YYYY-MM-DD`, so a raw `13/04/1990` trips `birthdayInvalid=true` and blocks the whole contact. Now seeds `mapped.birthday ?? snapshot.birthday ?? ""` — canonical (hyphenated) value when `mapBirthdayForStorage` succeeds (pre-fills + imports), raw only when canonicalization fails (correctly flags). Positive+negative acceptance test added. **This was itself a cycle-1 codex HIGH that the cycle-1 agent summary had DROPPED** (the committed REVIEWS.md flagged the codex-vs-cursor "cannot both be right" contradiction); it is now fixed. |
| **G2** | OWNER | owner ruling (via sonnet flag) | 02 (Task 1), 05 | **Keep naming for single picks** — see the owner-decision block above. REVERSES the cycle-1 single→ImportComplete routing; BULK-only auto-skip retained. |
| **G3** | HIGH | codex | 03 (Task 2) | **Two-query summary read.** A single `MIMETYPE IN (phone,email,event,photo)` Data-table pass cannot both include name-only contacts AND collect all method rows. Replaced the "single cursor pass" wording with a TWO-QUERY read: (1) enumerate ALL contacts from `Contacts.CONTENT_URI` (browse spine, includes name-only), (2) query the Data table for all phone/email values, merge by lookupKey. **Arose from the cycle-1 F3+F11 fixes** (which had left F3's all-methods projection and F11's name-only inclusion as a self-contradictory single pass). |
| **G4** | HIGH (typecheck) | codex | 04 (Task 1) | **Drop the untyped rationale API.** `PermissionsAndroid.shouldShowRequestPermissionRationale` exists at runtime but is ABSENT from the installed RN typings, so the cycle-1 F5 design (sample it after the request) breaks `npx tsc --noEmit` (a plan gate). Reworked the pure classifier to key ONLY on the typed `request()` result: `NEVER_ASK_AGAIN`→permanent, `GRANTED`→granted, `DENIED`→denied (recoverable). Ambiguous denials stay recoverable; permanence never inferred from a repeat-DENIED heuristic. A typed native bridge for a rationale signal is noted as an out-of-scope follow-up. **Arose from the cycle-1 F5 fix.** |
| **G4b** | LOW | codex (derived from G4) | 04 (Task 1) | **F5b note updated.** Since permanence now keys on `NEVER_ASK_AGAIN`, a persisted "requested before" flag is no longer needed for permanence detection; the lifetime note now says any auxiliary prior-attempt state does NOT drive permanence. |
| **G5** | (parity) | codex | 01 (Task 2), 03 (Task 2) | **Web-shim parity.** `OrbitContactPickerModule.web.ts` stubs only `isContactPickerAvailable`/`pickContacts`. Added the web stub for `readAllContacts` (plan 01) and `listContactsSummary` + the optional `lookupKeys?` on `readAllContacts` (plan 03); added the file to `files_modified`/`<files>` and an acceptance grep in both plans so web/test parity + tsc do not break. |
| **G5b** | (nit) | brief | 01 (frontmatter) | **List the test file.** `src/screens/use-contact-import-mode.test.ts` was verified in Task 2 but missing from `files_modified`; added. |
| **G6** | MED | codex | 03 (Task 2) | **Chunking test realism.** A Vitest/JS test cannot prove native Kotlin chunk execution/merge/order. Directed factoring the chunk-boundary FORMATION into a pure JS seam (JS forms ordered boundaries; native executes) covered by a >1-chunk unit test; native execution + merge + selected-order preservation is verified in device UAT (plan 05), not claimed as JS coverage. |
| **G7** | MED | codex/cursor | 05 (Task 1) | **Precise API-37 assertion (refines cycle-1 F6/A3b).** `maxSdkVersion` may still appear in package metadata, so a "not in the requested-permissions list" check can false-fail. Defined the EXACT successful observation: the runtime/install PERMISSION STATE via `dumpsys package <pkg>` flags / `pm` permission state showing READ_CONTACTS is not granted/grantable on the Pixel 6 (API 37) — kept DISTINCT from the (a) generated-manifest packaging grep on droid. |
| **G8** | (scope) | cursor (refines F14) | 01 (`<success_criteria>`) | **Finish the D-05 scope narrowing.** Cycle-1 fixed the must_have but `<success_criteria>` still said "both entry points routed through one shared seam"; plan 01 rewires only the FAB (Settings is plan 04). Reworded to scope plan-01's guarantee to the FAB entry point. |
| **G9** | (overclaim) | codex (refines F13) | 01 (Task 3 + assumption_delta) | **Shared-sink test overclaim.** The cycle-1 split still implied a unit test proves the legacy picker component's Import action calls `routePickedImport`. The picker is a React component wiring an executor + navigation — not unit-provable — so the LEGACY HANDOFF is now verified in device UAT (plan 05), and only the DISPATCH claim is unit-tested. No unit coverage claimed that the shape can't provide. |
| **G10** | LOW | codex | 02 (Task 3) | **Pin D-14b View-contact scope.** The net-new `ImportComplete` "View contact" action renders ONLY for the single already-linked case (`session.mode === 'single'`); a bulk session containing one already-linked row does not gain/lose it. |
| **G11** | LOW | brief | 05 (Task 2) | **Blocking checkpoint.** Task 2 depends on physical-device fixtures + owner sign-off but read as autonomous; stated explicitly that it is a BLOCKING human checkpoint — the executor must not mark the phase complete without recorded owner approval (stated in the task; frontmatter not silently flipped). |
| **G12** | LOW | codex | 02 (Task 2) | **Keep the birthday-unreadable count out of the failure UI.** Added a caution + acceptance that the informational "N birthdays couldn't be read" count must NOT feed `ImportCompleteScreen`'s `hasFailures`/retry block (`:179-196`) — those contacts imported successfully. |
| **G13** | LOW | codex | 03 (Task 2) | **Chunk-limit is an assumption.** Noted the ≤900-keys-per-`IN` chunk is a defensive choice against the classic ~999 SQLite bind-var limit (`SQLITE_MAX_VARIABLE_NUMBER`), recorded as a tunable constant with that rationale — NOT a verified provider contract. |

### Additional LOW items from the brief (not G-numbered)

| Item | Severity | Reviewer | Plan | Disposition |
|------|----------|----------|------|-------------|
| Ephemeral thumbnail URIs | LOW | cursor | 03 (prohibitions) | A summary-tier `photoThumbUri` must never be passed to `stageImportPhoto`/durable paths — list-tier URIs are ephemeral; full-photo staging happens only for the selection. |
| Focus re-check must only CHECK | LOW | cursor | 04 (Task 2) | The on-focus recovery after Open-Settings must re-run the fresh `check` ONLY (never `request`) to avoid a permission-request loop; stated in action + acceptance. |
| UAT row: name-only contact | (regression) | cursor | 05 (Task 2) | Added a matrix row (+ fixture + acceptance): a name-only contact (no phone/email/photo) appears in the picker and is name-searchable — the on-device regression catch for G3/F11. |
| UAT row: single-review slash birthday | (regression) | sonnet | 05 (Task 2) | Added a matrix row (+ fixture + acceptance): a slash-format OEM birthday driven through the SINGLE-import REVIEW screen pre-fills the canonical birthday and imports (an unparseable one is flagged) — the on-device regression catch for the G1 class, invisible to a bulk-only birthday check. |

### Provenance notes (called out explicitly)

- **G1 was a cycle-1 codex HIGH that the cycle-1 agent summary had DROPPED.** The committed `19.1-REVIEWS.md`
  flagged the codex-vs-cursor birthday-seed contradiction as "cannot both be right"; it did not make the
  cycle-1 edit set. It is fixed here.
- **G3 and G4 arose from cycle-1 fixes.** G3 corrects the self-contradiction the cycle-1 F3+F11 fixes left
  (all-methods projection + name-only inclusion described as one filtered pass). G4 corrects the cycle-1 F5
  fix, whose `shouldShowRequestPermissionRationale` dependency breaks the tsc gate against the installed
  RN typings.

### One decision reversed — and only one, owner-approved

Unlike cycle 1 (no reversals), cycle 2 contains exactly ONE intentional reversal: **G2**, the owner's ruling
that a nameless SINGLE pick keeps its ImportReview + naming path (only BULK auto-skips), reverting the
cycle-1 single→ImportComplete routing. Every other cycle-2 edit implements a recorded decision (ADR-002 /
D-01..D-14b) more correctly. No other `[DECIDED]`/`[REJECTED]` item in `HANDOFF.md` or any ADR was deleted,
weakened, or inverted.

---

## Cycle 3

**Nature of this revision:** SCALPEL, same discipline as cycles 1–2 — the five plans plus `19.1-CONTEXT.md`
were **hand-edited in place** (only the named `<action>`/`<acceptance_criteria>`/`<behavior>`/`must_haves`/
frontmatter sections), **not re-cut** (no task renumbering, no wave/dependency reordering, no re-splitting of
the hand-tuned assignments). Reviewers this cycle: **codex + cursor + a sonnet-5 reviewer**, re-reviewing the
cycle-2 edits. Every edit implements a recorded decision (ADR-002 / D-01..D-14b) **more correctly** or hardens
verification — **no decision was reversed** this cycle (see the closing note).

**Re-review:** the owner directed ONE VALIDATION review (**cycle 4**) after this pass — so unlike a normal
convergence cap, these cycle-3 edits WILL be re-reviewed.

### Several cycle-3 items REFINE earlier fixes (called out explicitly)

- **H-B refines G6** — the cycle-2 chunking design was left ambiguous (it mixed "native chunks" with "JS
  forms boundaries, native reads per boundary"). H-B makes it SINGULAR: JS owns chunking + merge + order;
  native reads exactly one bounded chunk per call.
- **C3-H1 refines G11** — the cycle-2 "blocking human checkpoint" was PROSE-ONLY on a `type="auto"`/
  `autonomous:true` task, so it was structurally unenforced. C3-H1 makes it a real `checkpoint:human-verify`.
- **M-E refines G4** — G4 correctly keyed permanence on the typed `NEVER_ASK_AGAIN` result; M-E closes the
  reopen/persistence gap that left (durable permanent verdict) + the OEM plain-DENIED loop (deny-count fallback).
- **H-A hardens the D-12 bulk fix (G2)** — the status-string change alone does not stop the resume nag,
  because nothing finalizes the session until a screen mounts; H-A finalizes in the driver.

### Findings (H-A/B/C, M-D..M-H, tracer/parity/wording/fixture/perf/files/supersession, M-H CONTEXT, C3-*)

| ID | Severity | Reviewer(s) | Plan(s) | Disposition |
|----|----------|-------------|---------|-------------|
| **H-A** | HIGH | codex | 02 (Task 1) | **Finalize the bulk session in the batch path.** `runImportBatch` (import-driver.ts:123-198) ends with `return sessionSummaryCounts(...)` (:198) and never calls `finalizeSessionIfTerminal` — finalize runs only on a screen mount, so a bulk session stays `pending` (getResumableSession keys on `status='pending'`, not rows) and re-nags if the app exits before ImportComplete loads. Directed a `finalizeSessionIfTerminal(exec, sessionId, now)` call at the end of the driver; added a process-restart acceptance test (driver completes incl. a nameless→'skipped' row → getResumableSession null WITHOUT ImportComplete mounting). Hardens the D-12/G2 bulk fix. |
| **M-D** (F12) | MED | codex + cursor | 02 (Task 1) | **Single mandated F12 approach — BULK-ONLY.** Replaced "classify-before-staging OR clean up (executor's choice)" with a HARD requirement: skip `stageImportPhoto` for a nameless row when `options.mode==="bulk"` only (preferred: driver-side, since `importRowAsNew`'s only callers — runImportBatch + DuplicateReviewScreen — are bulk-only). Gated to BULK so it cannot break the keep-naming SINGLE path (which stages/discards through ImportReview + leave-guard). Acceptance: zero staged files for a nameless BULK row AND a nameless SINGLE pick still stages. |
| **partition** | LOW/MED | codex + cursor | 02 (Task 1) | **Strengthened partition test.** Asserts imported / already-linked / user-skipped / name-required-skipped / failed / birthday-unreadable are mutually exclusive (no double-count) and that birthday-unreadable does NOT feed `hasFailures` (ImportCompleteScreen.tsx:179-196). Extends G9/G12. |
| **G10-refine** | LOW | cursor | 02 (Task 3) | **Pin View-contact scope precisely.** `ImportComplete.load()` (:33-47) now also reads `getSessionById` (`session.mode==='single'`, migration-012 CHECK) + `listSessionRows` (the lone `match_outcome='already_linked'` row's `matched_contact_id`) as the exact render condition — a bulk session with one already-linked row does not gain it. |
| **H-B** | HIGH | codex + cursor | 03 (Task 2) | **Singular chunking design.** Removed the two-design ambiguity. Now: a pure JS `chunkLookupKeys(keys, limit)` + a JS merge/reorder (dedupe by lookupKey, restore order) in new `src/logic/contact-picker-chunk.ts` (node-tested, >1-chunk); the JS `readAllContacts(lookupKeys?)` wrapper orchestrates chunk→per-chunk native read→merge; native `readContactsByLookupKeys(chunk)` reads exactly ONE bounded chunk (no native loop/chunk/merge). Web-shim + artifacts updated. **Refines G6.** |
| **M-F** | MED | codex | 03 (Task 2/3) | **Source-disappearance semantics.** A contact that vanishes between the summary and full read is OMITTED (absent from the chunk result — not a rejection; only a read exception rejects) and surfaced as a calm count on the screen; not a silent selection-vs-imported divergence, not a whole-import error. |
| **M-G** | MED | codex | 03 (Task 3) | **List-load cancellation guard.** The `listContactsSummary()` effect (+ selected-key read) must use a mounted/active guard mirroring `ImportProgressScreen.tsx:22`, so a late resolution after navigation-away / permission change does not setState on an unmounted/!granted screen. |
| **tracer-supersession** | (note) | codex + cursor | 03 (Task 2/3) | Stated explicitly that plan 03's two-tier read REPLACES plan 01's temporary full-book `readAllContacts()` + full-photo staging; the browse source becomes `listContactsSummary`, and full-book photo staging is removed before phase completion. |
| **tracer-is-temporary** | MED/HIGH | codex/cursor | 01 (Task 2) | Documented that plan 01's full-book `readAllContacts()` + per-contact photo staging is INTENTIONALLY TEMPORARY (tracer only), replaced by plan 03; interim large-book testing may be slow — do not treat the tracer as the perf target or harden it. |
| **region/time parity** | MED | codex | 01 (Task 3) | Documented that the legacy screen re-fetching `effectivePhoneRegion`/`now` at IMPORT time (F17) is intentional (the picker can be open a while, so current settings are correct) — a deliberate, documented divergence from the system path that snapshots once at acquisition. |
| **M-E** | MED | codex + cursor | 04 (Tasks 1–2) | **Durable permanent-denial + deny-count fallback.** (a) PERSIST the permanent verdict derived from NEVER_ASK_AGAIN durably (mirror notifications ask-once) so a reopen restores Open-Settings without re-priming/re-request; a fresh grant still clears it. (b) After N plain-DENIED results some OEM 14/15 builds never return NEVER_ASK_AGAIN, so the SCREEN (not the classifier) offers the Open-Settings escape hatch while still `denied`. Classifier still never infers permanence from a count (G4 preserved). **Refines G4.** |
| **wording** | LOW | codex | 04 (prohibitions/Task 1) | Narrowed "ask once, no nag loop" to "no AUTOMATIC re-prompt" — D-11a explicitly allows a user-initiated "Grant access" retry; the ban is on automatic re-prompting, not deliberate retries. |
| **H-C** | HIGH | codex | 05 (Task 2) | **Deterministic read-failure UAT.** Replaced the race-dependent "revoke mid-read" with a deterministic sequence: load summary (granted) → select one → revoke READ_CONTACTS while idle → press Import (triggers the full read) → assert "Couldn't import contacts", not a silent no-op. (Debug fault-injection kept as an alternative.) |
| **fixture-lifecycle** | MED | codex | 05 (precondition) | The empty-book case cannot coexist with the populated fixtures — specified a separate pass (second profile/device or an explicit reset→run→restore procedure) with creation + cleanup steps recorded in the UAT doc. |
| **measurable-perf** | MED | codex | 05 (Task 2) | "Scrolls smoothly" made measurable: an approximate fixture size, a scroll protocol, a stated threshold (no dropped frames / stalls > ~500 ms), the JS-thread caveat, physical-3a only. |
| **>900-select** | (regression) | cursor | 05 (Task 2) | Added a UAT row exercising the H-B chunk boundary: select >the chunk limit (e.g. >900) and import with no bind overflow / no cap — OR document why the JS `chunkLookupKeys` unit test + a smaller multi-select device spot-check is sufficient device evidence for the uncapped D-09 claim. |
| **files_modified** | LOW | cursor | 05 (frontmatter) | ADR-002 removed from `files_modified` — it is READ-ONLY here (status-confirmed, body immutable); the supersession note is written to HANDOFF.md/CLAUDE.md. Annotated with a comment. |
| **supersession-search** | LOW | cursor | 05 (Task 2) | Directed searching for the locked "Android 17+ / no READ_CONTACTS" wording across `.planning/phases/19-system-contact-import/` and CLAUDE.md import references too — not only HANDOFF.md — since it may not live verbatim in HANDOFF. |
| **M-H** | MED | cursor | CONTEXT | **CONTEXT.md drift fix.** Line ~25 "COMPLETE + verified on the Pixel 6" corrected: the Android-17 path's CODE is complete but the system picker is NON-FUNCTIONAL on the Pixel 6 (OS-level dead-end per device UAT / ADR-002); still out of 19.1 scope (LOCKED / do-not-touch). Factual doc sync, NOT a decision change. |
| **C3-H1** | HIGH | sonnet | 05 (Task 2 + frontmatter) | **Structural owner sign-off.** Plan 05 was `autonomous:true` with Task 2 `type="auto"`, so the emphatic G11 "blocking checkpoint" prose was UNENFORCED (execute-plan.md:221 auto-commits; :223 STOPs only on `checkpoint:*`). Changed Task 2 to `type="checkpoint:human-verify"` and the plan to `autonomous:false` (mirroring Plan 01 Task 1) so the executor STOPs for recorded owner approval and cannot self-certify. **Refines G11.** |
| **C3-M1** | MED | sonnet | 02 (Task 1) | **Second writer of `importRowAsNew`.** It is called from `runImportBatch` (:164) AND `DuplicateReviewScreen.tsx` (:162). After D-12, a nameless bulk row a user taps "import-new" for is silently converted to a skip — `resolveAction` (:148-207) never inspects `{skipped:'name-required'}`. Added the file to Task 1 read_first + files_modified and an acceptance criterion: surface distinct feedback (an Alert matching :134/:179/:207), not a silent no-op. |
| **C3-M2** | MED | sonnet | 01 (3 sites) | **`navigate("LegacyContactPicker", {})` breaks tsc.** The route is `undefined`-typed; `{}` is not assignable to `undefined` under strict, failing `npx tsc --noEmit` (a Task-3 gate); every existing undefined-typed route is navigated with no second arg. Changed all three sites (assumption_delta, Task 3 action, Task 3 acceptance) to `navigate("LegacyContactPicker")` and fixed the adjacent "params `{}`" prose. |
| **C3-L1** | LOW | sonnet | 02 (Task 2) | day==month is NOT ambiguous — `04/04/1990` parses (both readings agree). Flag as unparseable only when the two candidate day/month values DIFFER and both ≤12. Table test updated. |
| **C3-L2** | LOW | sonnet | 02 (Task 2) | The slash branch of `mapBirthdayForStorage` must itself build a fully-formed hyphenated `YYYY-MM-DD`/`--MM-DD` intermediate — `buildBirthdayForStorage` (edit-contact-logic.ts:121-130) positionally slices an already-hyphenated shape and does not parse slashes; don't compose the slash case through the hyphen-detection flow. |
| **C3-L3** | LOW | sonnet | 02 (Task 3) | `ImportCompleteScreen.load()` (:33-47) currently only calls `sessionSummaryCounts`/`sessionRowCounts`; named the extra `getSessionById` + `listSessionRows` fetches the G10 View-contact scoping needs, so the executor doesn't substitute a heuristic. |
| **C3-L4** | LOW | sonnet | 05 (Task 2) | Added the Graphify ADR-registry regen (`npm run gen:adr-registry`) to the supersession task since ADR-002 is a NEW ADR — CONDITIONAL: the `gen:adr-registry` script + `docs/decisions/adr-registry.ts` DO NOT EXIST in-repo yet (per CLAUDE.md the ADR bridge must be ported first), so if absent the regen is recorded DEFERRED to the bridge port, never invented. |
| **C3-L5** | LOW | sonnet | 01 (Task 3) | When rewiring `AddSpeedDialFab.importContacts()` to `startContactImport`, explicitly preserve the existing `busy`/`setBusy` re-entry guard (`AddSpeedDialFab.tsx:55-83`, verified on disk) so a double-tap can't launch two concurrent imports. |

### No decision reversed

Every cycle-3 edit implements a recorded decision (ADR-002 / D-01..D-14b) more correctly or hardens its
verification. No `[DECIDED]`/`[REJECTED]` item in `HANDOFF.md` or any ADR was deleted, weakened, or inverted;
the owner's cycle-2 G2 keep-naming ruling is preserved and reinforced (H-A/M-D are BULK-scoped so they cannot
touch the SINGLE keep-naming path). The M-H edit is a factual CONTEXT.md doc sync, not a scope or decision
change. A cycle-4 validation review follows per the owner's direction.

---

## Cycle 4 (final)

**Nature of this revision:** SCALPEL, same discipline as cycles 1–3 — the five plans were **hand-edited in
place** (only the named `<action>`/`<acceptance_criteria>`/`<behavior>`/`must_haves`/frontmatter sections),
**not re-cut** (no task renumbering, no wave/dependency reordering, no re-splitting of the hand-tuned
assignments). This was the owner-directed ONE VALIDATION review of the cycle-3 edits. Every edit implements a
recorded decision (ADR-002 / D-01..D-14b) **more correctly** or hardens verification — **no decision was
reversed** this cycle (see the closing note).

**This is the FINAL pass — these cycle-4 edits are NOT re-reviewed (see CONVERGENCE CLOSED below).**

### The 4 genuine fixes — each was INCOMPLETENESS in a cycle-3 fix (the diminishing-returns floor)

The validation review found no NEW defect class — all four genuine items are places where a cycle-3 fix was
directionally right but under-specified: **Fix 1 ← M-D**, **Fix 2 ← G3/H-B**, **Fix 3 ← C3-H1**,
**Fix 4 ← M-E**. That every remaining finding is a residual of a prior fix (not a fresh problem) is the
signal the convergence has reached its diminishing-returns floor.

| ID | Severity | Reviewer(s) | Plan(s) | Disposition |
|----|----------|-------------|---------|-------------|
| **Fix 1** (photo-stage gate) — refines **M-D** | (correctness) | codex + cursor | 02 (Task 1) | The cycle-3 M-D text offered a "PREFERRED driver-side skip in `importRowAsNew`" — impossible, because `acceptPickedContacts` (import-acquire.ts:69-96) stages the photo (:76-85) BEFORE any row status is known and BEFORE the driver runs, so the driver-side skip is too late. REMOVED that option; MANDATED the single mechanism: skip `stageImportPhoto` inside `acceptPickedContacts` gated `options.mode==="bulk" && mapped.nameRequired` (or an explicit post-mark cleanup of the staged file + `photo_rel_path`). `<done>` + acceptance de-reference "driver-side". |
| **Fix 1a** (test ownership) | MED | codex | 02 | `src/services/import/import-driver.test.ts` added to `files_modified`/`<files>`/verify; the H-A "batch finalizes before ImportComplete mounts" process-restart test is placed THERE (where `runImportBatch` tests live), not in `import-session-dao.test.ts`. |
| **Fix 1b** (defensive parse) | LOW | codex | 02 (Task 2) | The bulk unreadable-birthday count parses each row's `source_payload` through a guarded parser (try/catch idiom, cf. `parseCandidates` import-session-read.ts:68-76), never a raw `JSON.parse`; a malformed payload counts as NEITHER birthday-present NOR birthday-unreadable. |
| **Fix 2** (selected name-only read) — refines **G3/H-B** | HIGH | codex | 03 (Task 2) | The cycle-3 two-query fix was applied to the BROWSE summary only; the SELECTED-key full reader (`readContactsByLookupKeys`) was still a `Data`-only `LOOKUP_KEY IN (...)` read, which cannot return a name-only contact (no `Data` row) — so a selected name-only contact appeared in the list but was silently OMITTED from the import. Made the selected read two-query too (Contacts spine + Data merge) so it emits `{ displayName, methods: [], birthday: null, photoTempUri: null }` and is actually imported. |
| **Fix 2a** (return type) | MED | codex | 03 | The M-F "found + omitted count" now has a DEFINED shape `{ contacts: PickedContact[]; omittedCount: number }`; the wrapper passes `result.contacts` to `routePickedImport`, which still receives a plain `PickedContact[]` (its input contract preserved). |
| **Fix 2b** (chunk-failure PII) | MED | codex | 03 | On a multi-chunk failure the JS orchestrator DELETES cache photos staged by already-completed chunks before surfacing the error (JS test with a mock reader); no orphaned staged PII on a partial-then-rejected read. |
| **Fix 2c** (summary thumbnail source) | LOW | cursor | 03 | The summary thumbnail is `PHOTO_THUMBNAIL_URI` (null when absent) — no `openContactPhotoInputStream`/full-photo fallback in the summary tier. |
| **Fix 2d** (empty-selection guard) | LOW | cursor | 03 | `readAllContacts([])` → `{ contacts: [], omittedCount: 0 }` and `readContactsByLookupKeys([])` → `[]`, each WITHOUT a native call (guards against an accidental full-book read). |
| **Fix 3** (blocking-human gates) — refines **C3-H1** | HIGH | codex + sonnet (validation) | 01 (Task 1), 05 (Task 2) | C3-H1 changed the `type` but per `checkpoints.md` a `checkpoint:human-verify`/`checkpoint:decision` at the DEFAULT `gate="blocking"` is AUTO-APPROVED under `--auto`/`--chain`; ONLY `gate="blocking-human"` is never bypassed. Added `gate="blocking-human"` to Plan 01 Task 1 (ADR ratification) AND Plan 05 Task 2 (owner sign-off + A3b), with prose stating the gate — not the type — enforces the stop in every mode. |
| **Fix 4** (real persistence idiom) — refines **M-E** | HIGH | codex + sonnet | 04 (Tasks 1–2) | M-E said "mirror the notifications ask-once persistence pattern" — but `src/services/notifications/permission.ts` has NO persistence (fresh OS read every call). Replaced every such reference with the repo's ACTUAL idiom: `@react-native-async-storage/async-storage` (theme-store.ts pattern). Concrete: a namespaced boolean `contacts_permission_permanent_v1` written ONLY after a `NEVER_ASK_AGAIN` result, cleared when a fresh `check()` returns granted; the pure classifier still never infers permanence from a count. |
| **Fix 4a** (normal-denied reopen) | MED | codex | 04 | A SEPARATE presentation-only persisted flag `hasRequestedReadContacts` (NOT permanence) lets a reopen distinguish first-entry (prime) from a prior plain deny (calm re-request), instead of re-priming every open. |
| **Fix 4b** (M-E(b) persistence) | MED | sonnet | 04 | The OEM deny-count fallback that surfaces "Open Settings" after N plain denials must PERSIST durably (AsyncStorage, e.g. `contacts_deny_count`) — the repeated-back-out user it exists for would never reach an in-memory count that resets on reopen. |
| **Fix 4c** (openSettings guard) | LOW | codex | 04 | `openContactsSettings()`/`Linking.openSettings()` wrapped in try/catch → a calm "Open Settings manually" fallback on rejection, not an inert button. |

### Hygiene tail (cycle 4)

| Item | Plan | Disposition |
|------|------|-------------|
| Transition-table doc sync | 02 | Directed updating the canonical row-status transition-table comment (`import-session-dao.ts:11-22`, "single source of truth for plans 06/07/08/11") to record the nameless-BULK-row → `skipped` path as a THIRD path into `skipped` (distinct from `user Skip` and `import failure → failed`). `import-session-dao.ts` added to `files_modified`. |
| Pin `navigate` type | 01 | `startContactImport`'s injected `navigate` pinned WIDER than `PickedImportNavigator` (e.g. `NativeStackNavigationProp<RootStackParamList>["navigate"]`, which `AddSpeedDialFab.tsx:75` already wraps), since it calls both `routePickedImport` (narrow) and `navigate("LegacyContactPicker")`. |
| A3b executable assertion | 05 | Named the exact API-37 check: `adb shell pm grant <pkg> android.permission.READ_CONTACTS` is refused + the exact `dumpsys package <pkg>` permission-state field, alongside (not instead of) the generated-manifest grep. |
| Synthetic fixtures + redacted evidence | 05 | Fixtures must use synthetic identifiers; all committed `19.1-DEVICE-UAT.md` evidence redacted/cropped — no real contact names/methods/birthdays. |
| Pre-populated checklist + traceability | 05 | `19.1-DEVICE-UAT.md` pre-populated with a D-01..D-14b checklist table; records the APK build git SHA + `adb shell getprop ro.build.version.sdk` for BOTH the Pixel 3a and the Pixel 6. |
| Reproducible fixture lifecycle | 05 | The large-book and >900-select fixtures need a reproducible creation/reset procedure (generator/import script preferred), recorded in the UAT doc. |

### No decision reversed

Every cycle-4 edit implements a recorded decision (ADR-002 / D-01..D-14b) more correctly or hardens its
verification. No `[DECIDED]`/`[REJECTED]` item in `HANDOFF.md` or any ADR was deleted, weakened, or inverted;
the owner's cycle-2 G2 keep-naming ruling is preserved (Fix 1's photo-stage skip is BULK-gated and cannot
touch the SINGLE keep-naming path).

### CONVERGENCE CLOSED

The owner elected to APPLY the cycle-4 fixes and STOP here — there is no cycle-5 review. All four genuine
findings were incompleteness in cycle-3 fixes rather than new defect classes (Fix 1 ← M-D, Fix 2 ← G3/H-B,
Fix 3 ← C3-H1, Fix 4 ← M-E), i.e. the diminishing-returns floor. **These cycle-4 edits are NOT
re-reviewed, by design.** The five plans are considered converged and ready for execution.
