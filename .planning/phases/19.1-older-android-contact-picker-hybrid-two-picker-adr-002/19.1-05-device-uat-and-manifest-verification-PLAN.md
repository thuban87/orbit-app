---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
plan: 05
type: execute
wave: 4
depends_on: [19.1-01, 19.1-02, 19.1-03, 19.1-04]
files_modified:
  - docs/decisions/ADR-002-hybrid-two-picker-contact-import.md
  - .planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md
  - HANDOFF.md
  - CLAUDE.md
autonomous: true
requirements: [IMP-01]
user_setup:
  - service: pixel-3a-device
    why: "The Android ≤16 (Pixel 3a, Android 14/15) is the only real target for the ≤16 path; the emulator cannot run on this box (hardware) and cannot assess Skia/RN perf."
    dashboard_config:
      - task: "Pixel 3a reachable via emu-connect (USB) and the droid desktop reachable for the Expo prebuild + Gradle build"
        location: "docs/runbooks/desktop-build-pipeline.md"

estimate:
  tokens: 45000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - { statement: "PACKAGING (A3a): the generated android/app/src/main/AndroidManifest.xml on a droid prebuild carries READ_CONTACTS with android:maxSdkVersion=\"36\" — proving the permission is correctly PACKAGED and scoped, on the ≤16 build.", verification: backstop }
    - { statement: "INERT ON API-37 (A3b): on the owner's Pixel 6 Pro (API 37), READ_CONTACTS is NOT grantable/active for the installed app (the maxSdkVersion=36 drop actually takes effect at runtime) — the Play-policy claim checked on real API-37 hardware. The EXACT successful observation is the runtime/install PERMISSION STATE, via `adb shell dumpsys package <pkg>` (the runtime/install permission flags) and/or `adb shell pm` permission state, showing READ_CONTACTS is not a granted/grantable runtime permission — NOT a check that the permission is merely absent from the requested-permissions list. `maxSdkVersion` may still appear in package metadata / a requested-permissions listing on API 37, so a requested-list presence check can produce a FALSE failure; the permission-state observation is what proves inertness. This is kept DISTINCT from the (a) generated-manifest packaging grep on droid. A generated manifest with maxSdkVersion=\"36\" proves packaging, NOT runtime absence on API 37 — the ≤16 Pixel 3a cannot validate this. If the Pixel 6 check cannot run, this is RECORDED as BLOCKED, never marked passed off the ≤16 device.", verification: backstop }
    - { statement: "On the Pixel 3a: opening import from BOTH the dashboard FAB and Settings opens the custom picker (no 'requires Android 17+' copy); the priming explainer precedes the OS dialog; deny → calm re-request; Don't-ask-again → Open Settings deep-links to Orbit's app-info page (D-05, D-11a/b/c).", verification: backstop }
    - { statement: "On the Pixel 3a: a contact with an anniversary AND a birthday imports only the birthday (D-03), verified by a run-as read of contacts.birthday; a picker read failure surfaces 'Couldn't import contacts' and is not a silent no-op (D-14).", verification: backstop }
    - { statement: "On the Pixel 3a: an empty address book shows the calm empty state; a large book scrolls smoothly with lazy photos and no selection cap (D-09); a nameless contact in a BULK (multi-select) import auto-terminalizes as skipped and the session finalizes without re-nagging on relaunch, while a nameless SINGLE pick instead opens ImportReview so it can be named and imported (owner: keep naming) — abandoning that review discards the session via the leave-guard (D-12).", verification: backstop }
    - { statement: "On the Pixel 3a: an already-linked single import lands on ImportComplete with a working Done (→Home) and hardware Back (D-14b); an OEM-format birthday (e.g. MM/DD/YYYY) imports, and an unparseable one is flagged on review not dropped (D-13).", verification: backstop }
    - "The full node suite (npx vitest run) is green and migrations full-chain.test.ts still asserts TARGET_VERSION===12 (no new migration was added this phase)."
  artifacts:
    - .planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md
  key_links:
    - "droid prebuild → generated AndroidManifest.xml (maxSdkVersion=36) → Pixel 3a install → the whole ≤16 acquisition path"
  prohibitions:
    - "No contact content may be logged or transmitted during UAT capture — screenshots/uiautomator dumps and run-as DB reads stay local; local-first is the property under test, not a thing to violate while testing it."
---

<objective>
Prove the ≤16 hybrid picker on real hardware and gate the phase: build the app on droid, verify the generated manifest scopes READ_CONTACTS to `maxSdkVersion="36"` (A3), then run the Pixel 3a UAT matrix over every locked decision (D-01..D-14b) plus the specless edges (empty book, large book, permission race, read-error, birthday encoding/precision). This device pass also satisfies Phase-19's 19-17 Tasks 2–3 (the shared downstream flows) per ADR-002.

Purpose: The IMP-01 acceptance evidence — contact import works on Android ≤16 — and the release/policy check that the permission is inert on API-37.
Output: `19.1-DEVICE-UAT.md` findings + owner sign-off; the ADR-002 HANDOFF/CLAUDE.md supersession note actioned; the full node suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/decisions/ADR-002-hybrid-two-picker-contact-import.md
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-01-SUMMARY.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-02-SUMMARY.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-03-SUMMARY.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full node gate + droid prebuild + manifest-scoping verification (A3a packaging + A3b inert-on-API-37)</name>
  <files>.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md</files>
  <precondition>The droid desktop is reachable for an Expo prebuild + Gradle build (docs/runbooks/desktop-build-pipeline.md); this is a native-surface phase (new Kotlin + a new manifest permission) so a rebuild is required — Metro fast-refresh is insufficient. For the A3b inert-on-API-37 check, the owner's Pixel 6 Pro (API 37) is reachable via emu-connect (per CLAUDE.md); if it is not, the A3b check is recorded BLOCKED rather than passed off the ≤16 device.</precondition>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (the build-on-droid → install-on-Pixel procedure)
    - modules/orbit-contact-picker/android/src/main/AndroidManifest.xml (the source of the scoped permission that must merge)
    - src/db/database.ts (TARGET_VERSION===12 :47 — the migration-head assertion; no new migration this phase)
  </read_first>
  <action>
    Run the full node gate: `npx vitest run` (all suites green, including migrations full-chain.test.ts asserting TARGET_VERSION===12), `npx tsc --noEmit`, `npm run check:colors`, and biome on the touched files.
    Prebuild + build the ≤16 (debug, run-as-able) APK on droid per the runbook. Confirm the whole build compiles the new Kotlin (`readAllContacts`/`listContactsSummary`).
    Split the permission evidence into TWO distinct checks (F6 — a generated manifest carrying `maxSdkVersion="36"` proves the permission is PACKAGED, NOT that it is absent at runtime on API 37):
    - (a) PACKAGING (A3a): grep the GENERATED `android/app/src/main/AndroidManifest.xml` on droid for `READ_CONTACTS` with `android:maxSdkVersion="36"` (the module manifest merged) — packaging correct.
    - (b) INERT ON API-37 (A3b): on the owner's **Pixel 6 Pro (API 37)**, verify READ_CONTACTS is NOT grantable/active for the installed app (the `maxSdkVersion=36` drop actually takes effect at runtime) — the Play-policy claim checked on real API-37 hardware. The EXACT observation is the runtime/install PERMISSION STATE: `adb shell dumpsys package <pkg>` (inspect the runtime/install permission flags) and/or `adb shell pm` permission state, confirming READ_CONTACTS is not a granted/grantable runtime permission. Do NOT assert this by checking merely that the permission is ABSENT from the requested-permissions list — `maxSdkVersion` may still surface in package metadata / a requested-list on API 37, so a presence check there can give a FALSE failure; the permission-state flags are the authoritative signal. A working system picker is NOT required for this check. The Pixel 6 is available per CLAUDE.md. If the Pixel 6 check cannot run for any reason, RECORD the API-37 runtime check as **BLOCKED** in `19.1-DEVICE-UAT.md` — never mark it passed off the ≤16 Pixel 3a.
    Record BOTH the (a) droid manifest grep and the (b) Pixel 6 API-37 permission-state result (pass or BLOCKED) + the build outcome in `19.1-DEVICE-UAT.md`.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run check:colors</automated>
    <human-check>On droid, the generated android/app/src/main/AndroidManifest.xml shows a single READ_CONTACTS uses-permission with android:maxSdkVersion="36" (packaging), and the app builds with the new native reader compiled; AND on the Pixel 6 Pro (API 37) the effective manifest / permission state shows READ_CONTACTS is NOT active (inert on API-37) — or that check is recorded as BLOCKED, never passed off the ≤16 device.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` green; `npx tsc --noEmit` and `npm run check:colors` clean; migrations full-chain test still asserts TARGET_VERSION===12.
    - (a, packaging) Grep of the GENERATED manifest on droid confirms `android:maxSdkVersion="36"` on the READ_CONTACTS line (recorded in 19.1-DEVICE-UAT.md).
    - (b, inert on API-37) On the Pixel 6 Pro (API 37), the runtime/install PERMISSION STATE (via `dumpsys package <pkg>` permission flags / `pm` permission state) shows READ_CONTACTS is NOT granted/grantable for the installed app (recorded in 19.1-DEVICE-UAT.md) — the permission-state observation, NOT a "not in the requested-permissions list" check, which can false-fail because maxSdkVersion metadata may still appear on API 37 (G7). OR the check is explicitly recorded as BLOCKED; it is NEVER marked passed off the ≤16 Pixel 3a. Packaging (a) alone does not satisfy this.
    - The debug APK builds on droid with the new Kotlin (no compile error in OrbitContactPickerModule).
  </acceptance_criteria>
  <done>The node suite is green, the app builds on droid, and the scoped READ_CONTACTS is confirmed in the generated manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Pixel 3a UAT matrix (D-01..D-14b + specless edges) + ADR supersession note + owner sign-off</name>
  <files>.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md, HANDOFF.md, CLAUDE.md</files>
  <precondition>The Pixel 3a (Android 14/15) is connected and reachable via emu-connect (USB); it is the only real ≤16 target (no emulator on this box). Its address book has at least: a contact with both an anniversary and a birthday, a nameless (organization-only) contact, a NAME-ONLY contact (a structured name but NO phone/email/photo — for the G3/F11 two-query regression row), and an OEM-format (slash) birthday contact. The `19.1-DEVICE-UAT.md` doc MUST record the exact address-book FIXTURE SETUP steps (how each of these contacts was created — the anniversary+birthday contact, the nameless/org-only contact, the name-only contact, and the OEM-format-birthday contact) so the whole matrix is reproducible by a later reader on a fresh device, not dependent on the current state of one phone.</precondition>
  <read_first>
    - .planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md (Phase Requirements → Test Map; Assumptions A1–A6; Validation Architecture)
    - docs/decisions/ADR-002-hybrid-two-picker-contact-import.md (Consequences — the HANDOFF.md / CLAUDE.md supersession note to action)
    - .planning/phases/19-system-contact-import/19-DEVICE-UAT-FINDINGS.md (the run-as DB-read pattern for verifying contacts.birthday on device)
  </read_first>
  <action>
    Install the debug APK on the Pixel 3a and drive it via adb/uiautomator per the runbook + the device-UAT run-as pattern. Execute and record in `19.1-DEVICE-UAT.md` the full matrix:
    - **Routing/D-05:** both entry points (FAB + Settings) open the custom picker; no "requires Android 17+" copy anywhere.
    - **Permission D-11a/b/c:** priming explainer precedes the OS dialog; deny → calm "Grant access" re-request; deny→deny→Don't-ask-again → "Open Settings" deep-links to Orbit's app-info page; a grant made in Settings is picked up on return (A1/A2).
    - **Picker D-09/D-10:** search matches name + phone/email; multi-select with a persistent "Import (N)"; no cap (select many); avatars render; large-book scroll is smooth on the physical device (perf claim is legitimate here — state the JS-thread caveat); empty book → calm empty state.
    - **Name-only contact (G3/F11 regression, cursor):** a contact that has ONLY a structured name (no phone/email/photo) APPEARS in the picker list AND is found by a name search — the two-query summary read must not drop it. Uses the name-only fixture in the address book (record it in the fixture setup). This is the on-device regression catch for the two-query merge that replaced the single MIMETYPE-filtered pass.
    - **Reader D-03/D-14:** import a contact with an anniversary + birthday → only the birthday lands (run-as read of contacts.birthday). D-14 read-failure must use a CONCRETE, REPRODUCIBLE trigger — not a vague "simulate a failure" — so the check can't be marked pass superficially: e.g. revoke READ_CONTACTS mid-read (`adb shell pm revoke <pkg> android.permission.READ_CONTACTS` while the picker read is in flight), OR a debug-only fault-injection hook that forces `readAllContacts`/`listContactsSummary` to reject (compiled only into the debug build, ABSENT in production). Record the exact method used in `19.1-DEVICE-UAT.md`. Expected: the ≤16 screen shows "Couldn't import contacts" (the D-14 reject-on-error contract), NOT a silent no-op that reads as a user cancel.
    - **Pipeline D-12/D-13/D-14b:** a nameless contact selected in a BULK (multi-select) import auto-terminalizes as skipped, the session finalizes, and relaunch does NOT re-offer resume; a nameless SINGLE pick instead opens ImportReview so it can be named and imported (owner: keep naming) — and abandoning that review discards the session (no resume nag on relaunch). An OEM-format birthday (MM/DD/YYYY) imports and an unparseable one is flagged on review; an already-linked single import lands on ImportComplete with working Done (→Home) + hardware Back.
    - **Single-review slash birthday (G1 regression, sonnet):** drive a slash-format OEM birthday (e.g. `13/04/1990`) through the SINGLE-import REVIEW screen (not only the bulk path — the G1-class regression is invisible to a bulk-only birthday check). Assert the review pre-fills the CANONICAL stored birthday (birthdayInvalid=false) and the contact imports; a genuinely-unparseable birthday (e.g. ambiguous `03/04/1990`, or garbage) is FLAGGED on the review screen (birthdayInvalid=true), not silently dropped. Use the OEM-format-birthday fixture; record it in the fixture setup.
    - **Shared downstream (ADR-002 §47):** note that this pass also covers Phase-19 19-17 Tasks 2–3.
    Action the ADR-002 Consequences follow-up: add the supersession note to HANDOFF.md / CLAUDE.md references (the "Android 17+ / no READ_CONTACTS" locked decision is superseded by ADR-002). Do NOT edit ADR-002's decision body (immutable) beyond confirming it is Accepted.
    Present the findings to the owner for sign-off. This task is a BLOCKING HUMAN CHECKPOINT (G11): although the plan frontmatter runs the automated steps autonomously, Task 2 depends on physical-device fixtures and owner review, so the executor MUST NOT mark the phase complete without recorded owner approval in the SUMMARY — stop and wait for it rather than self-certifying. Scope the sign-off EXPLICITLY: success here means "≤16 IMP-01 satisfied" (contact import works end-to-end on the Pixel 3a) plus the A3b packaging-inert-on-API-37 permission check — it does NOT mean "the API-37 system picker works", which remains the Phase-19 finish. Phase completion must not be read as full IMP-01 on API 37.
  </action>
  <verify>
    <human-check>On the Pixel 3a, every matrix row above passes (or each failure is logged in 19.1-DEVICE-UAT.md with a follow-up), the run-as DB read confirms only the birthday landed for the anniversary+birthday contact, and the owner signs off.</human-check>
  </verify>
  <acceptance_criteria>
    - `19.1-DEVICE-UAT.md` records a pass/fail line for every matrix item with device evidence (screenshot / uiautomator / run-as DB read), no contact content leaked into any log or committed artifact.
    - The D-03 birthday gate is confirmed by a run-as read of contacts.birthday (anniversary excluded); the D-14 read-error path shows the error toast, not a silent no-op.
    - The name-only contact (G3/F11) appears in the picker and is found by name search (recorded with device evidence) — the two-query summary read did not drop it.
    - The single-review slash birthday (G1) row is recorded: a canonicalizable slash birthday driven through the SINGLE-import review pre-fills the canonical stored value and imports (birthdayInvalid=false); an unparseable one is flagged on review (birthdayInvalid=true), not dropped.
    - The HANDOFF.md / CLAUDE.md supersession note for ADR-002 is added (grep confirms the reference).
    - Owner sign-off is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The ≤16 hybrid picker is proven on the Pixel 3a across every locked decision and specless edge, the ADR supersession note is actioned, and the owner has signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Effective manifest on API-37 vs ≤16 | The maxSdkVersion scoping must actually drop the permission above API 36 — the Play-policy boundary under test. |
| UAT capture (screenshots, DB reads) → local artifacts | Device evidence must stay local and carry no exported contact content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-15 | Elevation/Policy | Scoped permission not actually dropped on API-37 | high | mitigate | Two-part release gate for the Play Contacts policy (D-02): (a) verify the GENERATED manifest carries maxSdkVersion="36" on droid — PACKAGING (A3a); (b) verify on the Pixel 6 Pro (API 37) that the effective manifest / permission state carries NO active READ_CONTACTS — actual RUNTIME drop (A3b). Packaging alone is insufficient; if (b) can't run it is recorded BLOCKED, never assumed. |
| T-19.1-16 | Information disclosure | Contact PII leaking via UAT logs/artifacts | medium | mitigate | Keep screenshots/dumps/run-as reads local; no contact content in committed artifacts or logs (local-first is the property under test). |

No package installs; no supply-chain gate applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)
- `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md` (findings log).
- A HANDOFF.md / CLAUDE.md supersession note referencing ADR-002 (documentation edit; no code symbol).
No new code symbols.
</artifacts_produced>

<verification>
- Full node suite green (`npx vitest run`), tsc + check:colors clean.
- Generated manifest carries `maxSdkVersion="36"` on READ_CONTACTS (droid).
- Pixel 3a UAT matrix passes with device evidence; owner sign-off recorded.
</verification>

<success_criteria>
IMP-01 is demonstrably met: contact import works end-to-end on the Pixel 3a (Android ≤16) across every locked decision and specless edge, the scoped permission is confirmed inert on API-37, the node suite is green, and the owner has signed off.
</success_criteria>

<output>
Create `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-05-SUMMARY.md` when done.
</output>
