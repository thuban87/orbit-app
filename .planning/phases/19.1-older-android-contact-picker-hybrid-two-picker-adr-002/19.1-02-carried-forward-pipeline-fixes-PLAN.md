---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/import/import-driver.ts
  - src/services/import/import-acquire.ts
  - src/db/import-session-read.ts
  - src/screens/ImportCompleteScreen.tsx
  - src/logic/picked-contact-map.ts
  - src/screens/ImportReviewScreen.tsx
  - src/db/import-session-dao.test.ts
  - src/logic/picked-contact-map.test.ts
autonomous: true
requirements: [IMP-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A picked contact with no usable name auto-terminalizes as row_status 'skipped' (not 'failed'), so the import session finalizes and getResumableSession stops re-offering it every launch (D-12)."
    - "The completion summary reports the nameless count distinctly — 'N skipped — no name' — keyed on failure_reason='name-required', without a new migration (D-12)."
    - "Common Android Event birthday formats — YYYY-MM-DD, year-less --MM-DD / MM-DD, YYYY/MM/DD, MM/DD/YYYY — canonicalize to a stored shape; a genuinely unparseable value is FLAGGED on the single review screen (birthdayInvalid), never silently dropped or coerced (D-13)."
    - "The strict calendar gate (parseStoredBirthday) still rejects impossible dates like 02-30; D-13 broadens only the input canonicalizer, never loosens the gate."
    - "An already-linked single import routes to the ImportComplete screen with a working Done (reset→Home) and hardware Back, instead of dead-ending on a single-button Alert (D-14b)."
  artifacts:
    - src/services/import/import-driver.ts
    - src/logic/picked-contact-map.ts
    - src/screens/ImportReviewScreen.tsx
    - src/db/import-session-read.ts
  key_links:
    - "importRowAsNew nameless branch → markRowStatus 'skipped' → finalizeSessionIfTerminal terminal set (import-session-dao.ts:418-420) → getResumableSession null"
    - "mapBirthdayForStorage canonicalizer → buildBirthdayForStorage → isValidStoredBirthday (strict gate) → stored birthday OR flagged on review"
    - "ImportReviewScreen already-linked → navigation.replace('ImportComplete') → ImportComplete Done (reset→Home)"
  prohibitions:
    - "The unresolved-row status sets must never be widened or inverted — finalizeSessionIfTerminal (import-session-dao.ts:418-420) and UNRESOLVED_ROW_STATUSES (import-leave-guard-logic.ts:7-11) are the shared terminalization invariant, kept in sync by design; the fix moves rows to the already-terminal 'skipped', it does not change what 'terminal' means."
    - "Birthday values must never be coerced or silently dropped when unparseable — flag on the review screen (custom-fields 'flag, don't coerce' philosophy, D-13)."
    - "No new SQLite migration — 'skipped' already exists in the row-status enum and is already terminal; adding a status/migration is a needless irreversible change."
---

<objective>
Apply the four carried-forward Phase-19 import-subsystem fixes that live in the SHARED pipeline (so they benefit both the 17+ and the new ≤16 reader), each with node coverage and NO new migration: D-12 (nameless rows auto-terminalize as 'skipped' + a distinct completion count), D-13 (broaden birthday parsing + flag-don't-drop the unparseable), and D-14b (the already-linked single import gets a clean exit). (D-14 — read-exception ≠ cancel — is the native reader's contract, owned by plan 01.)

Purpose: Stop the resume-prompt nag, stop silently dropping OEM birthday formats, and remove the single-import dead-end — the review/finalize invariants stay intact.
Output: Edited driver/acceptance seam, birthday canonicalizer + review load, completion summary count, and the already-linked routing — all node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/decisions/ADR-002-hybrid-two-picker-contact-import.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-CONTEXT.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-12 — nameless rows auto-terminalize as 'skipped' + a distinct completion count</name>
  <files>src/services/import/import-driver.ts, src/services/import/import-acquire.ts, src/db/import-session-read.ts, src/screens/ImportCompleteScreen.tsx, src/db/import-session-dao.test.ts</files>
  <behavior>
    - A bulk row whose mapped name is empty → markRowStatus 'skipped' with failure_reason 'name-required'; finalizeSessionIfTerminal returns true (session finalizes) and getResumableSession returns null afterward.
    - A single-import nameless row is terminalized the same way at the acceptance seam so it cannot dead-end review.
    - sessionSummaryCounts (or a sibling read-only count) surfaces the 'name-required' skipped count separately from other failed/skipped rows; the completion screen renders it (default copy "N skipped — no name").
    - A 'skipped' row that is `already_linked` is still bucketed as already-in-Orbit, unchanged.
  </behavior>
  <read_first>
    - src/services/import/import-driver.ts (importRowAsNew nameless branch :76-85 — the `'failed'` to change to `'skipped'`; keep the `"name-required"` reason)
    - src/db/import-session-dao.ts (ImportSessionRowStatus enum :27-33; finalizeSessionIfTerminal unresolved query :418-420; resolveAlreadyLinkedCore :301-314; markRowStatus)
    - src/screens/import-leave-guard-logic.ts (UNRESOLVED_ROW_STATUSES :7-11 — must stay in sync; do NOT widen)
    - src/db/import-session-read.ts (sessionSummaryCounts :212-246 — failedOrSkipped bucketing at :238-243; getResumableSession :97-115)
    - src/services/import/import-acquire.ts (acceptPickedContacts :63-111 — mapped.nameRequired computed at :71 via mapPickedContact but currently discarded; the single/bulk acceptance seam)
    - src/screens/ImportCompleteScreen.tsx (the four count cards :124-177; "Failed / skipped" at :170-176)
    - src/db/import-session-dao.test.ts (extend — the D-12 Wave-0 gap)
  </read_first>
  <action>
    Change the nameless branch in `importRowAsNew` (import-driver.ts:79) from status `"failed"` to `"skipped"`, keeping `failure_reason = "name-required"`. Because `'skipped'` is already a valid status and is NOT in the unresolved set of either finalizeSessionIfTerminal (import-session-dao.ts:418-420) or UNRESOLVED_ROW_STATUSES, the session now finalizes and the resume nag stops — no migration. Do NOT widen either unresolved set.
    Terminalize nameless rows for the SINGLE path too: at the acceptance seam in `acceptPickedContacts` (import-acquire.ts), where `mapped.nameRequired` is already computed, mark the just-inserted row `'skipped'`/`'name-required'` when nameless (planner delegates exact placement — accept-time mark or an immediately-following markRowStatus — to the executor; either needs no schema change and must cover single + bulk uniformly).
    Add a read-only nameless count: extend `sessionSummaryCounts` (or add a sibling grouped read) to count rows with `row_status='skipped' AND failure_reason='name-required'` distinctly; render it on ImportCompleteScreen as its own line (default "N skipped — no name"). Exact owner-facing copy is taste — use the D-12 default and note it is owner-tunable; the DECISION to surface a distinct count is locked by D-12, do not fold it silently back into "Failed / skipped".
  </action>
  <verify>
    <automated>npx vitest run src/db/import-session-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A DAO/driver test inserts a nameless row, runs the driver, and asserts: row_status becomes 'skipped', finalizeSessionIfTerminal returns true, and getResumableSession returns null (no re-nag).
    - The nameless-count read returns the count of `skipped`+`name-required` rows separate from other failed/skipped rows; a test with one nameless + one user-skipped row distinguishes them.
    - `grep -n "\"skipped\"" src/services/import/import-driver.ts` shows the nameless branch now uses 'skipped'; `import-leave-guard-logic.ts` UNRESOLVED_ROW_STATUSES is unchanged (still exactly pending/needs_review/failed).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Nameless rows finalize the session as 'skipped' (single + bulk), the nag is gone, and the completion summary reports them distinctly — with no migration and the terminalization invariant untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-13 — broaden birthday canonicalization; flag (don't drop) the unparseable</name>
  <files>src/logic/picked-contact-map.ts, src/screens/ImportReviewScreen.tsx, src/logic/picked-contact-map.test.ts</files>
  <behavior>
    - mapBirthdayForStorage normalizes YYYY-MM-DD (unchanged), year-less --MM-DD / MM-DD (unchanged), plus YYYY/MM/DD and MM/DD/YYYY, to the two stored shapes (MM-DD or YYYY-MM-DD).
    - An impossible date (e.g. 2021-02-29, 02-30) still returns null via the unchanged strict parseStoredBirthday gate — the canonicalizer never loosens calendar validity.
    - The single review screen loads the RAW snapshot birthday into birthdayInput, so a genuinely-unparseable value trips the existing birthdayInvalid error state instead of showing empty.
  </behavior>
  <read_first>
    - src/logic/picked-contact-map.ts (mapBirthdayForStorage :28-38 — the canonicalizer to broaden; yearUnknown handling; buildBirthdayForStorage usage)
    - src/logic/birthday-logic.ts (MM_DD/YYYY_MM_DD regexes :33-34; parseStoredBirthday strict gate :79-105 — KEEP strict; normalizeEditedBirthday :121-134; isValidStoredBirthday :108-114)
    - src/screens/edit-contact-logic.ts (buildBirthdayForStorage :121-130 — strips year on save)
    - src/screens/ImportReviewScreen.tsx (load path :126-146 — birthdayInput currently seeded from mapped.birthday (pre-nulled) at :145; the raw snapshot.birthday is on the parsed snapshot; birthdayInvalid at :174-175 and the error text at :516-520)
    - src/logic/picked-contact-map.test.ts (add/extend — the D-13 Wave-0 gap)
  </read_first>
  <action>
    Broaden `mapBirthdayForStorage` (picked-contact-map.ts:28-38) to also recognise `YYYY/MM/DD` and `MM/DD/YYYY` and normalise them (with the existing `--MM-DD`/`MM-DD`/`YYYY-MM-DD` handling) into the two stored shapes before it calls `buildBirthdayForStorage`. Route ALL validity through the unchanged strict `parseStoredBirthday`/`isValidStoredBirthday` — never `new Date(y,m,d)`, which silently normalises impossible dates. Applies to both readers via this shared module; the ≤16 reader inherits it.
    In `ImportReviewScreen.tsx`, seed `birthdayInput` from the RAW `snapshot.birthday` (the parsed source payload) rather than the pre-nulled `mapped.birthday`, so an unparseable value hits the existing `birthdayInvalid` state (:174-175) and its error line (:516-520) — flag, don't drop. (On the bulk path there is no per-row birthday review; an unparseable birthday is dropped there, which is acceptable since birthday is optional — the flag applies only where a review UI exists.)
  </action>
  <verify>
    <automated>npx vitest run src/logic/picked-contact-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Table test: `MM/DD/YYYY` and `YYYY/MM/DD` inputs canonicalize to the correct stored shape; `--MM-DD`/`MM-DD`/`YYYY-MM-DD` still work; `2021-02-29` and `02-30` return null (strict gate preserved).
    - A test (or a documented review-screen assertion) shows the review load uses the raw snapshot birthday, so an unparseable value yields birthdayInvalid=true rather than an empty field.
    - `grep -n "parseStoredBirthday" src/logic/birthday-logic.ts` confirms the strict gate is unchanged (broadening lives in picked-contact-map.ts only).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Common OEM birthday formats parse to a valid stored shape, impossible dates still reject, and an unparseable value is flagged on review rather than vanishing.</done>
</task>

<task type="auto">
  <name>Task 3: D-14b — already-linked single import gets a clean, back-safe exit</name>
  <files>src/screens/ImportReviewScreen.tsx</files>
  <read_first>
    - src/screens/ImportReviewScreen.tsx (the already-linked branch :231-248 — the bare single-button Alert to replace; the reload matcher `rows.find(candidate => candidate.contactId === null)` at :117; the `skipDuplicate` precedent that already does `navigation.replace("ImportComplete", { sessionId })` at :316-318)
    - src/db/import-session-dao.ts (resolveAlreadyLinkedCore :301-314 — sets row_status='skipped', match_outcome='already_linked', matched_contact_id, leaves contact_id NULL)
    - src/screens/ImportCompleteScreen.tsx (the terminal screen's "Done" reset→Home exit :234-244; it renders "Already in Orbit (N)")
  </read_first>
  <action>
    In `ImportReviewScreen.tsx`'s already-linked branch (:231-248), after `resolveAlreadyLinked` + `finalizeSessionIfTerminal`, replace the bare single-button Alert with `navigation.replace("ImportComplete", { sessionId: route.params.sessionId })` — the same terminal route `skipDuplicate` already uses (:316-318). The session is already finalized and the row terminal, so ImportComplete renders "Already in Orbit (1)" and offers the back-safe "Done" (reset→Home) plus the optional "View contact" path. This removes the dead-end where dismissing the Alert reloads the screen and re-matches the contact_id-NULL already-linked row (:117). Do not otherwise change the duplicate/link flow. No schema change.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The already-linked branch navigates to `ImportComplete` (assert `grep -n "ImportComplete" src/screens/ImportReviewScreen.tsx` shows a replace call in the already-linked path, matching the skipDuplicate idiom).
    - The bare single-"View contact"-button dead-end Alert is gone from the already-linked path.
    - `npx tsc --noEmit` clean; device confirmation (Done→Home, hardware Back works) is scheduled in plan 19.1-05.
  </acceptance_criteria>
  <done>An already-linked single import lands on ImportComplete with a working Done and hardware Back — no dead-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source payload (untrusted contact snapshot) → session rows / review | Contact name + birthday from the picker is untrusted input parsed into durable rows and review state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-06 | Tampering | Malformed birthday poisoning storage | medium | mitigate | Broaden only the input canonicalizer; all validity still routes through the strict parseStoredBirthday gate (rejects 02-30/2021-02-29); unparseable is flagged, never coerced (D-13). |
| T-19.1-07 | Denial of service (self-inflicted) | Nameless row wedging the session unresolved forever | medium | mitigate | Move nameless rows to the already-terminal 'skipped' status; do not alter the terminalization invariant (D-12). |
| T-19.1-08 | Repudiation / UX | Silent birthday data loss | low | mitigate | Flag-don't-drop on the single review screen so the user sees and can fix an unparseable value (D-13). |

No external API, no network, and no new package installs on this path (pure shared-pipeline TS); no supply-chain gate applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
No net-new public symbols — all edits are in-place to existing shared-pipeline modules (import-driver.ts, import-acquire.ts, import-session-read.ts, picked-contact-map.ts, ImportReviewScreen.tsx, ImportCompleteScreen.tsx). If a sibling read count is added to import-session-read.ts, it is a new exported function (e.g. `namelessSkippedCount`) — name it explicitly in the SUMMARY so the convergence source-grounding pass can exclude it from drift verification.
</artifacts_produced>

<verification>
- `npx vitest run src/db/import-session-dao.test.ts src/logic/picked-contact-map.test.ts` green.
- `npx tsc --noEmit` clean.
- Full suite `npx vitest run` green at the wave merge (migrations full-chain.test.ts still asserts TARGET_VERSION===12 — this plan adds no migration).
</verification>

<success_criteria>
The resume-prompt nag is gone (nameless rows finalize as 'skipped', reported distinctly), OEM birthday formats parse while impossible dates are flagged not dropped, and the already-linked single import has a clean back-safe exit — all with no new migration and the finalize/leave-guard invariants unchanged.
</success_criteria>

<output>
Create `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-02-SUMMARY.md` when done.
</output>
