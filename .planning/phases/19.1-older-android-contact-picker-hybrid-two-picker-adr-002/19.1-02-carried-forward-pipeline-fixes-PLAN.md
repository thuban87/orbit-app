---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/import/import-driver.ts
  - src/services/import/import-acquire.ts
  - src/db/import-session-read.ts
  - src/screens/ImportCompleteScreen.tsx
  - src/logic/picked-contact-map.ts
  - src/screens/ImportReviewScreen.tsx
  - src/db/import-session-dao.test.ts
  - src/db/import-session-read.test.ts
  - src/logic/picked-contact-map.test.ts
autonomous: true
requirements: [IMP-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A picked contact with no usable name auto-terminalizes as row_status 'skipped' (not 'failed'), so the import session finalizes and getResumableSession stops re-offering it every launch (D-12)."
    - "The completion summary reports the nameless count distinctly — 'N skipped — no name' — keyed on failure_reason='name-required', without a new migration (D-12)."
    - "Common Android Event birthday formats — YYYY-MM-DD, year-less --MM-DD / MM-DD, and unambiguous slash forms (YYYY/MM/DD always; MM/DD/YYYY or DD/MM/YYYY only when a field >12 fixes the day-vs-month order) — canonicalize to a stored shape; an ambiguous slash value (both day/month fields ≤12, e.g. 03/04/1990) is treated as unparseable, never guessed; a genuinely unparseable value is never silently dropped or coerced on EITHER path — the single-import review screen FLAGS it (birthdayInvalid), and the bulk path imports the contact without the birthday (never coerced) while surfacing it in a distinct completion count (D-13)."
    - "On bulk import, a contact whose source birthday is present but unparseable still imports (birthday omitted), and the count of such contacts is reported distinctly on the completion summary — default copy 'N birthdays couldn't be read' — computed read-only over the raw source_payload already stored, with NO new migration (D-13; owner ratification of checker Warning 1)."
    - "The strict calendar gate (parseStoredBirthday) still rejects impossible dates like 02-30; D-13 broadens only the input canonicalizer, never loosens the gate."
    - "An already-linked single import routes to the ImportComplete screen — with a working Done (reset→Home), hardware Back, AND a preserved one-tap 'View contact' action (newly ADDED to ImportComplete) that jumps to the already-linked contact's Profile — instead of dead-ending on a single-button Alert (D-14b, owner Option A: preserve the View-contact shortcut)."
  artifacts:
    - src/services/import/import-driver.ts
    - src/logic/picked-contact-map.ts
    - src/screens/ImportReviewScreen.tsx
    - src/db/import-session-read.ts
  key_links:
    - "importRowAsNew nameless branch → markRowStatus 'skipped' → finalizeSessionIfTerminal terminal set (import-session-dao.ts:418-420) → getResumableSession null"
    - "mapBirthdayForStorage canonicalizer → buildBirthdayForStorage → isValidStoredBirthday (strict gate) → stored birthday OR (single-import: flagged on the review screen / bulk: contact imports without birthday, then the SAME canonicalizer re-run read-only over each imported row's source_payload counts the drop → distinct 'N birthdays couldn't be read' line on ImportCompleteScreen)"
    - "ImportReviewScreen already-linked → navigation.replace('ImportComplete') → ImportComplete Done (reset→Home) + newly-added 'View contact' → Profile(matched_contact_id)"
  prohibitions:
    - "The unresolved-row status sets must never be widened or inverted — finalizeSessionIfTerminal (import-session-dao.ts:418-420) and UNRESOLVED_ROW_STATUSES (import-leave-guard-logic.ts:7-11) are the shared terminalization invariant, kept in sync by design; the fix moves rows to the already-terminal 'skipped', it does not change what 'terminal' means."
    - "An unparseable birthday is never silently dropped on EITHER path — on the single-import review path it is FLAGGED (birthdayInvalid); on the bulk path the contact still imports (birthday omitted, never coerced) AND the drop is surfaced as a distinct 'N birthdays couldn't be read' completion count. The birthday value is never coerced to satisfy a type (custom-fields 'flag, don't coerce' philosophy, D-13)."
    - "No new SQLite migration — 'skipped' already exists in the row-status enum and is already terminal, and the bulk unparseable-birthday count is read-only (re-runs the pure canonicalizer over each imported row's already-stored source_payload); neither fix adds a column or a migration — a status/migration would be a needless irreversible change."
---

<objective>
Apply the four carried-forward Phase-19 import-subsystem fixes that live in the SHARED pipeline (so they benefit both the 17+ and the new ≤16 reader), each with node coverage and NO new migration: D-12 (nameless rows auto-terminalize as 'skipped' + a distinct completion count), D-13 (broaden birthday parsing; on the single-import review path flag-don't-drop the unparseable, and on the bulk path import the contact without the birthday — never coerced — while surfacing a distinct "N birthdays couldn't be read" completion count so nothing is silently dropped), and D-14b (the already-linked single import gets a clean exit). (D-14 — read-exception ≠ cancel — is the native reader's contract, owned by plan 01.)

Purpose: Stop the resume-prompt nag, stop silently dropping OEM birthday formats (single-import flags it; bulk import counts it), and remove the single-import dead-end — the review/finalize invariants stay intact.
Output: Edited driver/acceptance seam, birthday canonicalizer + single-import review load + a read-only bulk unparseable-birthday count line, the nameless completion count, and the already-linked routing — all node-tested.

Deferred to Phase 20 (recorded, owner-approved — NOT a silent gap): 19.1 surfaces the bulk unparseable-birthday *count* only. A durable home for each raw unparseable value plus a bulk review surface to resolve them per-item (alongside other review cases) needs a new migration + bulk review UI, which is Phase-20 (Contact Reconciliation & Merge) scope. See the &lt;deferred&gt; note below — this is an owner ratification of the checker's Warning 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/decisions/ADR-002-hybrid-two-picker-contact-import.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-CONTEXT.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-12 — nameless rows auto-terminalize as 'skipped' + a distinct completion count</name>
  <files>src/services/import/import-driver.ts, src/services/import/import-acquire.ts, src/db/import-session-read.ts, src/screens/ImportCompleteScreen.tsx, src/db/import-session-dao.test.ts</files>
  <behavior>
    - A bulk row whose mapped name is empty → markRowStatus 'skipped' with failure_reason 'name-required'; finalizeSessionIfTerminal returns true (session finalizes) and getResumableSession returns null afterward.
    - A single-import nameless pick is terminalized 'skipped'/'name-required', the session is finalized (finalizeSessionIfTerminal), and the single all-terminal session routes to **ImportComplete** (NOT ImportReview): getResumableSession returns null (no resume nag) and ImportReview never mounts. Because the session never opens ImportReview and is already finalized, the leave-guard-defeat concern (a pre-'skipped' nameless row not being discarded on back-out → a dangling session) is moot for this path — there is nothing to back out of.
    - sessionSummaryCounts (or a sibling read-only count) surfaces the 'name-required' skipped count separately from other failed/skipped rows, and those rows are EXCLUDED from the failedOrSkipped bucket so each is counted once (never in both); the completion screen renders it (default copy "N skipped — no name").
    - A 'skipped' row that is `already_linked` is still bucketed as already-in-Orbit, unchanged.
  </behavior>
  <read_first>
    - src/services/import/import-driver.ts (importRowAsNew nameless branch :76-85 — the `'failed'` to change to `'skipped'`; keep the `"name-required"` reason)
    - src/db/import-session-dao.ts (ImportSessionRowStatus enum :27-33; finalizeSessionIfTerminal unresolved query :418-420; resolveAlreadyLinkedCore :301-314; markRowStatus)
    - src/screens/import-leave-guard-logic.ts (UNRESOLVED_ROW_STATUSES :7-11 — must stay in sync; do NOT widen)
    - src/db/import-session-read.ts (sessionSummaryCounts :212-246 — failedOrSkipped bucketing at :238-243; getResumableSession :97-115)
    - src/services/import/import-acquire.ts (acceptPickedContacts :63-111 — mapped.nameRequired computed at :71 via mapPickedContact but currently discarded; the single/bulk acceptance seam)
    - src/screens/ImportCompleteScreen.tsx (the four count cards :124-177; "Failed / skipped" at :170-176)
    - src/db/import-session-dao.test.ts (extend — the D-12 Wave-0 gap)
  </read_first>
  <action>
    Change the nameless branch in `importRowAsNew` (import-driver.ts:79) from status `"failed"` to `"skipped"`, keeping `failure_reason = "name-required"`. Because `'skipped'` is already a valid status and is NOT in the unresolved set of either finalizeSessionIfTerminal (import-session-dao.ts:418-420) or UNRESOLVED_ROW_STATUSES, the session now finalizes and the resume nag stops — no migration. Do NOT widen either unresolved set.
    Terminalize the SINGLE nameless path END-TO-END so it never lands on a screen that dead-ends or nags: at the acceptance seam in `acceptPickedContacts` (import-acquire.ts), where `mapped.nameRequired` is already computed, mark the just-inserted row `'skipped'`/`'name-required'` when nameless, call `finalizeSessionIfTerminal`, and route the single all-terminal session to **`ImportComplete`**, NOT `ImportReview`. Today `routePickedImport` (import-acquire.ts:156-172) sends a `picked.length===1` import to `ImportReview` regardless of row status (marking a row 'skipped' does NOT by itself change that navigation), so the routing MUST detect the single all-terminal case (acceptPickedContacts already knows `mapped.nameRequired`) and navigate to `ImportComplete` instead. Exact placement is the executor's choice (inside `acceptPickedContacts` or `routePickedImport`), but the plan REQUIRES: single nameless pick → session finalized (status complete) → `getResumableSession` null → `ImportReview` never opens. Because the single nameless case now routes to `ImportComplete` and the session is already finalized, the leave-guard-defeat concern for this path is moot (no unresolved session left to dangle on back-out). This must cover single + bulk uniformly with no schema change; do NOT widen either unresolved set.
    PII hygiene for a doomed nameless row (F12): a nameless contact must be CLASSIFIED (nameRequired) BEFORE its photo is staged in `acceptPickedContacts` (`import-acquire.ts:77-80` currently stages the photo via `stageImportPhoto` around the row insert, i.e. before any post-insert terminalization), OR any already-staged photo for a terminal 'skipped'/'name-required' row must be explicitly cleaned up — so a doomed nameless row never leaves an orphaned staged contact-photo on disk. Executor's choice which, but the plan REQUIRES no orphaned staged PII for a nameless-skipped row.
    Add a read-only nameless count: extend `sessionSummaryCounts` (or add a sibling grouped read) to count rows with `row_status='skipped' AND failure_reason='name-required'` distinctly; render it on ImportCompleteScreen as its own line (default "N skipped — no name"). Exact owner-facing copy is taste — use the D-12 default and note it is owner-tunable; the DECISION to surface a distinct count is locked by D-12, do not fold it silently back into "Failed / skipped".
    Resolve the double-count (F9): the existing `failedOrSkipped` bucket (`import-session-read.ts:238-243`) currently ALSO counts these `skipped`+`name-required` rows, so surfacing a distinct "N skipped — no name" line would double-count them. Modify that grouping (or the completion rendering) so a name-required row is counted ONCE — in the "no name" line, EXCLUDED from `failedOrSkipped` — and never in both.
  </action>
  <verify>
    <automated>npx vitest run src/db/import-session-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A DAO/driver test inserts a nameless row, runs the driver, and asserts: row_status becomes 'skipped', finalizeSessionIfTerminal returns true, and getResumableSession returns null (no re-nag).
    - An integration test for the SINGLE nameless path (DAO + navigation) asserts end-to-end: a single nameless pick finalizes the session (status complete), `getResumableSession` returns null (no resume nag), and navigation targets `ImportComplete` — `ImportReview` is NOT opened.
    - No orphaned staged PII: a test (or documented assertion) shows a single nameless-skipped row leaves no staged contact photo on disk — the nameless classification precedes photo staging, or the staged photo is cleaned up on terminalization.
    - The nameless-count read returns the count of `skipped`+`name-required` rows separate from other failed/skipped rows; a test with one nameless + one user-skipped row distinguishes them.
    - Partition test: `failedOrSkipped`, the "no name" (name-required-skipped) count, and (from Task 2) the "birthday unreadable" count do NOT overlap — a name-required row is counted in "no name" only and is excluded from `failedOrSkipped`, so no row is counted twice.
    - `grep -n "\"skipped\"" src/services/import/import-driver.ts` shows the nameless branch now uses 'skipped'; `import-leave-guard-logic.ts` UNRESOLVED_ROW_STATUSES is unchanged (still exactly pending/needs_review/failed).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Nameless rows finalize the session as 'skipped' (single + bulk), the nag is gone, and the completion summary reports them distinctly — with no migration and the terminalization invariant untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-13 — broaden birthday canonicalization; flag the unparseable on single review, count it on bulk (neither path drops silently)</name>
  <files>src/logic/picked-contact-map.ts, src/screens/ImportReviewScreen.tsx, src/db/import-session-read.ts, src/screens/ImportCompleteScreen.tsx, src/logic/picked-contact-map.test.ts, src/db/import-session-read.test.ts</files>
  <behavior>
    - mapBirthdayForStorage normalizes YYYY-MM-DD (unchanged), year-less --MM-DD / MM-DD (unchanged), and `YYYY/MM/DD` (4-digit leading field ⇒ unambiguously year-first) to the two stored shapes (MM-DD or YYYY-MM-DD).
    - Slash-delimited day/month order is accepted ONLY when unambiguous, never guessed: a field `>12` forces the day-vs-month assignment (e.g. `13/04/1990` → Apr-13; `04/13/1990` → Apr-13). When BOTH candidate day/month fields are `≤12` the order is genuinely ambiguous (e.g. `03/04/1990` could be Mar-4 or 3-Apr) — treat the value as UNPARSEABLE rather than coerce it to the wrong date. (Both readings would pass the strict calendar gate, so the gate cannot catch this; the ambiguity must be rejected at the canonicalizer.) `YYYY/MM/DD` with a 4-digit leading field is unambiguous and always parses.
    - An impossible date (e.g. 2021-02-29, 02-30) still returns null via the unchanged strict parseStoredBirthday gate — the canonicalizer never loosens calendar validity.
    - An ambiguous slash value that is treated as unparseable follows the same no-silent-drop rule as any unparseable birthday: single-import review FLAGS it (birthdayInvalid); bulk imports the contact without the birthday and counts it — never coerced to a guessed date.
    - The single review screen loads the RAW snapshot birthday into birthdayInput, so a genuinely-unparseable value trips the existing birthdayInvalid error state instead of showing empty.
    - Bulk path: a contact whose RAW source birthday is present-but-unparseable still imports (birthday omitted, never coerced), and a read-only count of such imported rows is surfaced on the completion summary as its own line (default copy "N birthdays couldn't be read"). The count re-runs the SAME canonicalizer over each imported row's stored source_payload — it never re-parses independently, so the count can never disagree with what was actually dropped.
    - A row whose source birthday is ABSENT (null/empty) is NOT counted — "couldn't be read" means present-but-unparseable, not "had none". These contacts still land in the normal Imported bucket; the birthday line is informational and never reduces the Imported count nor folds into Failed / skipped.
  </behavior>
  <read_first>
    - src/logic/picked-contact-map.ts (mapBirthdayForStorage :28-38 — the canonicalizer to broaden AND to export for reuse; yearUnknown handling; buildBirthdayForStorage usage; mapPickedContact returns birthday:null when the raw value fails to canonicalize :76)
    - src/logic/birthday-logic.ts (MM_DD/YYYY_MM_DD regexes :33-34; parseStoredBirthday strict gate :79-105 — KEEP strict; normalizeEditedBirthday :121-134; isValidStoredBirthday :108-114)
    - src/screens/edit-contact-logic.ts (buildBirthdayForStorage :121-130 — strips year on save)
    - src/screens/ImportReviewScreen.tsx (load path :126-146 — birthdayInput currently seeded from mapped.birthday (pre-nulled) at :145; the raw snapshot.birthday is on the parsed snapshot; birthdayInvalid at :174-175 and the error text at :516-520)
    - src/services/import/import-driver.ts (importRowAsNew :87-98 — the bulk create; `birthday: mapped.birthday` at :95 is where an unparseable birthday is currently dropped silently — the drop stays, it just gets counted)
    - src/services/import/import-acquire.ts (acceptImportSessionWithRows staging :86-95 — source_payload persists the RAW `birthday: contact.birthday` at :92, so the raw value survives on every row for the read-only count)
    - src/db/import-session-read.ts (sessionSummaryCounts :212-246 and its SessionSummaryCounts shape :200-205 — the completion buckets; listSessionRows :136-164 already returns sourcePayload, the input for the count; mirror the Task-1 nameless-count read pattern)
    - src/screens/ImportCompleteScreen.tsx (the count cards :124-177; load() reads sessionSummaryCounts+sessionRowCounts in a Promise.all :40-44 — add the birthday count alongside; render a new card next to "Failed / skipped" :170-176)
    - src/logic/picked-contact-map.test.ts (add/extend — the D-13 Wave-0 gap: the canonicalizer table + the exported unparseable predicate)
    - src/db/import-session-read.test.ts (DB-backed; already stages rows with source_payload JSON and exercises sessionSummaryCounts — extend for the bulk unparseable-birthday count)
  </read_first>
  <action>
    Broaden `mapBirthdayForStorage` (picked-contact-map.ts:28-38) to also recognise slash-delimited values (with the existing `--MM-DD`/`MM-DD`/`YYYY-MM-DD` handling) and normalise them into the two stored shapes before it calls `buildBirthdayForStorage` — but ONLY when the day/month order is unambiguous, never guessing: `YYYY/MM/DD` (4-digit leading field ⇒ year-first) always parses; a slash value where one of the two candidate day/month fields is `>12` uses that field to fix the assignment; a slash value where BOTH candidate fields are `≤12` (e.g. `03/04/1990`) is genuinely ambiguous and MUST be treated as UNPARSEABLE (return null) rather than coerced to a guessed date — both readings pass the strict calendar gate, so the ambiguity has to be rejected here, at the canonicalizer, to honour "flag, don't coerce" (D-13). Route ALL validity through the unchanged strict `parseStoredBirthday`/`isValidStoredBirthday` — never `new Date(y,m,d)`, which silently normalises impossible dates. Applies to both readers via this shared module; the ≤16 reader inherits it. Also EXPORT the canonicalizer (or a thin `isBirthdayUnreadable(raw: string | null): boolean` predicate = raw present-and-non-empty AND `mapBirthdayForStorage(raw) === null`) from this module so the bulk count below reuses the exact same drop logic — a single source of truth, so the count can never drift from what import actually dropped. Do NOT reimplement the parse in the count.
    Single-import (review) path: in `ImportReviewScreen.tsx`, seed `birthdayInput` from the RAW `snapshot.birthday` (the parsed source payload) rather than the pre-nulled `mapped.birthday`, so an unparseable value hits the existing `birthdayInvalid` state (:174-175) and its error line (:516-520) — flag, don't drop.
    Bulk path (D-13 owner ratification of checker Warning 1 — do NOT leave a silent drop): the bulk create in `importRowAsNew` still imports the contact WITHOUT the birthday when it fails to canonicalize (`birthday: mapped.birthday` stays null — never coerced). Surface the drop instead of hiding it: add a read-only count in import-session-read.ts (extend `sessionSummaryCounts`/`SessionSummaryCounts` with a `birthdayUnreadable` field, OR add a sibling grouped read — executor's choice; either is read-only, NO new column, NO migration) that walks the session's imported rows (row_status 'imported'), parses each row's already-stored `source_payload.birthday`, and counts those the exported predicate marks unreadable (present-but-unparseable). Because the raw birthday is durably persisted in `source_payload` (import-acquire.ts:92) and the predicate is the same one that dropped it, the count is exact with no schema change. Render it on `ImportCompleteScreen.tsx` as its OWN line next to the existing cards (default copy "N birthdays couldn't be read", theme-tokened, owner-tunable at build — same treatment as the D-12 "N skipped — no name" line; mirror that plumbing). It is informational: it must NOT be folded into "Failed / skipped" (those contacts imported successfully) and must NOT reduce the Imported count. If the executor finds this genuinely cannot be counted without new schema (it can — the raw value is in source_payload), STOP and flag rather than silently dropping.
  </action>
  <verify>
    <automated>npx vitest run src/logic/picked-contact-map.test.ts src/db/import-session-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Table test (slash ambiguity, F8): `2019/03/04` (YYYY/MM/DD) canonicalizes to the correct stored shape; `13/04/1990` (a field >12 forces the order) → Apr-13; `03/04/1990` (both fields ≤12, ambiguous) returns null / is flagged as unparseable — NOT coerced to a guessed date; `--MM-DD`/`MM-DD`/`YYYY-MM-DD` still work; `2021-02-29` and `02-30` return null (strict gate preserved).
    - A test (or a documented review-screen assertion) shows the review load uses the raw snapshot birthday, so an unparseable value yields birthdayInvalid=true rather than an empty field.
    - Exported-predicate test: the exported unreadable predicate returns true for a present-but-unparseable raw value, false for a value that canonicalizes, and false for null/empty (absent ≠ unreadable).
    - Bulk-count test (DB-backed, in import-session-read.test.ts): a session with one imported row whose source birthday is present-but-unparseable, one imported row with a valid birthday, and one imported row with no birthday returns a birthday-unreadable count of exactly 1 — separate from `failedOrSkipped` and without lowering `imported`.
    - The bulk count reuses the exported picked-contact-map predicate (grep shows import-session-read.ts imports it), so the count logic is not a second, drift-prone copy of the parser.
    - ImportCompleteScreen renders the birthday-unreadable count as its own card (grep shows the new count field referenced in the screen), distinct from the "Failed / skipped" and "N skipped — no name" lines.
    - `grep -n "parseStoredBirthday" src/logic/birthday-logic.ts` confirms the strict gate is unchanged (broadening lives in picked-contact-map.ts only).
    - No new migration: `full-chain.test.ts` still asserts the existing TARGET_VERSION (this task adds no migration); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Common OEM birthday formats parse to a valid stored shape, impossible dates still reject, an unparseable value is flagged on the single-import review rather than vanishing, and on bulk import the contact still imports (birthday omitted) with the drop reported as a distinct "N birthdays couldn't be read" completion count — no migration, no coercion, nothing silently dropped.</done>
</task>

<task type="auto">
  <name>Task 3: D-14b — already-linked single import gets a clean, back-safe exit (with the View-contact shortcut preserved)</name>
  <files>src/screens/ImportReviewScreen.tsx, src/screens/ImportCompleteScreen.tsx</files>
  <read_first>
    - src/screens/ImportReviewScreen.tsx (the already-linked branch :231-248 — the bare single-button Alert to replace; the reload matcher `rows.find(candidate => candidate.contactId === null)` at :117; the `skipDuplicate` precedent that already does `navigation.replace("ImportComplete", { sessionId })` at :316-318)
    - src/db/import-session-dao.ts (resolveAlreadyLinkedCore :301-314 — sets row_status='skipped', match_outcome='already_linked', matched_contact_id, leaves contact_id NULL)
    - src/screens/ImportCompleteScreen.tsx (the terminal screen's "Done" reset→Home exit :234-244; it renders "Already in Orbit (N)". NOTE: its only actions today are retry (:189), DuplicateReview (:205), UnboundContacts (:224), Done→Home reset (:238) — there is NO "View contact" affordance; this task ADDS one for the single already-linked case. resolveAlreadyLinkedCore sets match_outcome='already_linked' + matched_contact_id on the row, so ImportComplete can read the matched contact id from the session rows for the sessionId)
  </read_first>
  <action>
    First, ADD the shortcut to the terminal screen: in `ImportCompleteScreen.tsx`, for the single already-linked case, render a token-driven "View contact" action that navigates to `Profile` with the already-linked contact id — read from the session rows for `sessionId` (the row's `matched_contact_id`, which resolveAlreadyLinkedCore set; contact_id stays NULL). This does NOT exist today (ImportComplete's only actions are retry/DuplicateReview/UnboundContacts/Done), so it is a net-new action, fully theme-tokened, shown alongside the existing back-safe "Done" (reset→Home) + hardware Back.
    Then, in `ImportReviewScreen.tsx`'s already-linked branch (:231-248), after `resolveAlreadyLinked` + `finalizeSessionIfTerminal`, replace the bare single-button Alert — whose ONLY affordance today is "View contact" → Profile, and which dead-ends when the Alert is dismissed — with `navigation.replace("ImportComplete", { sessionId: route.params.sessionId })`, the same terminal route `skipDuplicate` already uses (:316-318). The session is already finalized and the row terminal, so ImportComplete renders "Already in Orbit (1)" with the back-safe "Done" AND the newly-added "View contact" action. Net effect (D-14b, owner Option A): the dead-end is removed (dismissing the Alert used to reload the screen and re-match the contact_id-NULL already-linked row at :117) AND the one-tap jump-to-profile is preserved — not dropped. Do not otherwise change the duplicate/link flow. No schema change.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The already-linked branch navigates to `ImportComplete` (assert `grep -n "ImportComplete" src/screens/ImportReviewScreen.tsx` shows a replace call in the already-linked path, matching the skipDuplicate idiom).
    - `ImportCompleteScreen` exposes a "View contact" action for the single already-linked case that navigates to `Profile` with the row's matched contact id (assert via grep for the `Profile` navigation + the matched_contact_id read in ImportCompleteScreen.tsx, plus a description), in addition to the existing back-safe Done.
    - The bare single-"View contact"-button dead-end Alert is gone from the already-linked path (the shortcut now lives on ImportComplete, not a dead-ending Alert).
    - `npx tsc --noEmit` clean; device confirmation (Done→Home, hardware Back works, View contact opens the Profile) is scheduled in plan 19.1-05.
  </acceptance_criteria>
  <done>An already-linked single import lands on ImportComplete with a working Done, hardware Back, and the preserved "View contact" shortcut to the Profile — no dead-end, no lost affordance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source payload (untrusted contact snapshot) → session rows / review | Contact name + birthday from the picker is untrusted input parsed into durable rows and review state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-06 | Tampering | Malformed birthday poisoning storage | medium | mitigate | Broaden only the input canonicalizer; all validity still routes through the strict parseStoredBirthday gate (rejects 02-30/2021-02-29); unparseable is flagged, never coerced (D-13). |
| T-19.1-07 | Denial of service (self-inflicted) | Nameless row wedging the session unresolved forever | medium | mitigate | Move nameless rows to the already-terminal 'skipped' status; do not alter the terminalization invariant (D-12). |
| T-19.1-08 | Repudiation / UX | Silent birthday data loss | low | mitigate | Neither path silently drops: single-import flags the unparseable value on the review screen (birthdayInvalid) so the user can fix it; bulk import keeps the contact (birthday omitted, never coerced) and reports the drop as a distinct "N birthdays couldn't be read" completion count computed read-only over source_payload. Durable per-item bulk review is carried forward to Phase 20 (D-13, owner ratification of checker Warning 1). |

No external API, no network, and no new package installs on this path (pure shared-pipeline TS); no supply-chain gate applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
Mostly in-place edits to existing shared-pipeline modules (import-driver.ts, import-acquire.ts, import-session-read.ts, picked-contact-map.ts, ImportReviewScreen.tsx, ImportCompleteScreen.tsx). Task 2 also EXPORTS a previously-private symbol from picked-contact-map.ts — the birthday canonicalizer or a thin `isBirthdayUnreadable` predicate — reused by the bulk count. If a sibling read count is added to import-session-read.ts, it is a new exported function (e.g. `namelessSkippedCount` for D-12 and a birthday-unreadable count for D-13 — or a `birthdayUnreadable` field extending `SessionSummaryCounts`) — name each explicitly in the SUMMARY so the convergence source-grounding pass can exclude them from drift verification.
</artifacts_produced>

<deferred>
## Carried forward to Phase 20 (recorded, owner-approved — NOT a silent gap)

Owner ratification of the checker's **Warning 1** (D-13 bulk silent-drop):

- **In 19.1 (this plan, Task 2):** the bulk path surfaces the unparseable-birthday **count only** — a read-only "N birthdays couldn't be read" line on the completion summary. The contact still imports (birthday omitted, never coerced); no raw value is durably retained beyond what already lives in `source_payload`; no new migration.
- **Deferred to Phase 20 (Contact Reconciliation & Merge):** a **durable home for each raw unparseable birthday value** (new migration) plus a **bulk review surface** that shows these unparseable birthdays as review items alongside other review cases, so the user can resolve them per-item. That retention + UI is explicitly out of 19.1 scope and must NOT be built here.
- **Why deferred, not done now:** per-item bulk review needs schema (durable raw-value retention) and a new bulk review UI — both Phase-20 scope. The count is the correct, migration-free 19.1 increment; it removes the silent drop the checker flagged without pulling Phase-20 scope forward.
- This deferral is an **owner ruling**, recorded so a later reader does not mistake the count-only surface for a missed requirement.

**Not fixed here (F18, LOW — recorded so it is not mistaken as done):** the Android-17 (system-picker) reader's read-exception→`resolve(emptyList())` swallow (`OrbitContactPickerModule.kt:118-120`) remains UNFIXED in this phase. The D-14 reject-on-error contract is applied to the NEW ≤16 reader (plan 01), but the existing 17+ path's swallow is docketed to the Phase-19 finish per ADR-002 and is out of 19.1 scope. Do not read this plan's D-14 work as having also fixed the 17+ swallow.
</deferred>

<verification>
- `npx vitest run src/db/import-session-dao.test.ts src/logic/picked-contact-map.test.ts src/db/import-session-read.test.ts` green.
- `npx tsc --noEmit` clean.
- Full suite `npx vitest run` green at the wave merge (migrations full-chain.test.ts still asserts TARGET_VERSION===12 — this plan adds no migration).
</verification>

<success_criteria>
The resume-prompt nag is gone (nameless rows finalize as 'skipped', reported distinctly), OEM birthday formats parse while impossible dates are rejected, and an unparseable birthday is never silently dropped — the single-import review flags it, the bulk path imports the contact without it and reports the drop as a distinct completion count (durable per-item bulk review carried forward to Phase 20). The already-linked single import has a clean back-safe exit — all with no new migration and the finalize/leave-guard invariants unchanged.
</success_criteria>

<output>
Create `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-02-SUMMARY.md` when done.
</output>
