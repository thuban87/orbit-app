---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
plan: 04
type: execute
wave: 3
depends_on: [19.1-01, 19.1-03]
files_modified:
  - src/services/contacts/use-read-contacts-permission.ts
  - src/services/contacts/read-contacts-permission-logic.ts
  - src/services/contacts/read-contacts-permission-logic.test.ts
  - src/screens/LegacyContactPickerScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/components/AddSpeedDialFab.tsx
  - src/screens/use-import-unsupported.ts
  - src/services/import/start-contact-import.ts
autonomous: true
requirements: [IMP-01]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On first request, an in-app pre-permission explainer primes the user before the OS dialog (plain, privacy-forward copy: contacts are read on-device so you can pick which to import, nothing leaves the device), 'Continue' triggers the OS READ_CONTACTS dialog (D-11c)."
    - "On a plain deny, the screen shows a calm, non-blocking explainer with a 'Grant access' button that re-requests READ_CONTACTS — no single-field ACTION_PICK fallback (D-11a, the rejected alternative stays rejected)."
    - "On permanent denial (Don't ask again / NEVER_ASK_AGAIN), the action switches to 'Open Settings' deep-linking to Orbit's app-info permissions page, and a grant made there is picked up on next focus via a fresh check (D-11b)."
    - "The 'Contact import requires Android 17+' unsupported state is fully retired: both entry points (dashboard FAB and Settings) dispatch through contactImportMode()/startContactImport, and there is no disabled/greyed import row on any device (D-05)."
  artifacts:
    - src/services/contacts/use-read-contacts-permission.ts
    - src/services/contacts/read-contacts-permission-logic.ts
    - src/screens/LegacyContactPickerScreen.tsx
    - src/screens/SettingsScreen.tsx
  key_links:
    - "requestContactsPermission → PermissionsAndroid.request → classifyPermissionResult (granted | denied | permanent) → screen state (list | re-request | Open Settings)"
    - "SettingsScreen import row → startContactImport(contactImportMode()) → legacy picker OR system picker (single shared handler)"
  prohibitions:
    - "No single-field ACTION_PICK fallback — it was considered and rejected in D-11a; do not reintroduce a thin name+one-phone path as a 'degraded' second code path."
    - "The permission must never re-prompt in a nag loop — it is requested at the value moment and asked once; a denial degrades to a calm in-app state (mirrors the notifications ask-once posture)."
    - "Contact data must never leave the device — the permission only reads contacts on-device; no egress is added on this path (D-06)."
---

<objective>
Complete the permission experience and finish the routing cutover: the D-11c priming explainer before the first OS dialog, the D-11a calm-deny + re-request state (no ACTION_PICK fallback), the D-11b permanent-denial → "Open Settings" deep-link with a fresh re-check on focus, and the D-05 retirement of the "requires Android 17+" unsupported state on BOTH entry points so every device can import.

Purpose: D-11a/b/c permission UX and D-05 routing completion on top of plan 01's minimal request and plan 03's rich list.
Output: The full `use-read-contacts-permission` hook + a pure classifier, the screen's permission states, the Settings dispatch through the shared seam, and the deletion of `use-import-unsupported.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/decisions/ADR-002-hybrid-two-picker-contact-import.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-CONTEXT.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-PATTERNS.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-01-SUMMARY.md
@.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Permission hook + pure classifier (grant | deny | permanent denial)</name>
  <files>src/services/contacts/use-read-contacts-permission.ts, src/services/contacts/read-contacts-permission-logic.ts, src/services/contacts/read-contacts-permission-logic.test.ts</files>
  <behavior>
    - classifyPermissionResult maps the TYPED `PermissionsAndroid.request()` result to one of: "granted" | "denied" | "permanent". `RESULTS.GRANTED` → "granted". `RESULTS.NEVER_ASK_AGAIN` → "permanent". `RESULTS.DENIED` → "denied" (recoverable — keep the "Grant access" re-request). It keys ONLY on the typed request result: `request()` returns `RESULTS.GRANTED | DENIED | NEVER_ASK_AGAIN`, and NEVER_ASK_AGAIN is the single authoritative permanent signal. The classifier NEVER infers "permanent" from a bare repeat-DENIED count and NEVER calls `shouldShowRequestPermissionRationale` — that API is absent from the installed RN typings (it exposes only check/request/deprecated checkPermission/requestPermission) so calling it breaks `npx tsc --noEmit` (a plan gate). Any ambiguous denial stays "denied" (recoverable).
    - The hook: fresh-read check + request wrappers (extend plan 01's), each guarded (rejection → not-granted), returning the classified state from the typed request result; plus an openContactsSettings() calling Linking.openSettings().
  </behavior>
  <read_first>
    - src/services/contacts/use-read-contacts-permission.ts (plan 01's minimal getContactsPermission/requestContactsPermission to extend)
    - src/services/notifications/permission.ts (the fresh-read, guarded, ask-once shape — the design contract to preserve, not the API)
    - .planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-RESEARCH.md (Permission & Manifest D-11a/b/c; the NEVER_ASK_AGAIN vs DENIED cross-version nuance and the "requested-before" heuristic; Assumptions A1/A2)
  </read_first>
  <action>
    Create `src/services/contacts/read-contacts-permission-logic.ts` — a PURE classifier returning `"granted" | "denied" | "permanent"` that keys ONLY on the TYPED `PermissionsAndroid.request()` result: `classifyPermissionResult(result)` where `result` is a `PermissionStatus` (`RESULTS.GRANTED | DENIED | NEVER_ASK_AGAIN`). Rules: `GRANTED` → "granted"; `NEVER_ASK_AGAIN` → "permanent"; `DENIED` → "denied" (recoverable). Do NOT accept or sample a rationale signal, and do NOT classify "permanent" off a repeat-DENIED count — NEVER_ASK_AGAIN is the sole permanent signal, and any ambiguous denial stays "denied" (a false permanent wrongly strands the user in Open-Settings). Do NOT call `PermissionsAndroid.shouldShowRequestPermissionRationale`: it is ABSENT from the installed RN typings, so referencing it breaks `npx tsc --noEmit` (a plan gate) even though it exists at runtime. Node-test it across GRANTED / DENIED / NEVER_ASK_AGAIN (Wave-0 gap; the exact device behavior is confirmed on the 3a in plan 19.1-05, A1, which keeps the 3a confirmation step A1 as already scheduled).
    Extend `use-read-contacts-permission.ts`: keep the fresh-read guarded `check`/`request`; feed the typed `request()` result directly into `classifyPermissionResult` (no rationale sampling); add `openContactsSettings()` = `Linking.openSettings()` (D-11b; net-new, verified on the 3a in plan 05, A2). No caching of OS state in app state; ask once, no nag loop.
    (Out-of-scope follow-up, not this phase): a rationale-based signal (to distinguish a first plain-DENIED from a later one) would need a TYPED native bridge exposing `shouldShowRequestPermissionRationale`; that is a separate follow-up, NOT built here — this phase relies solely on the typed NEVER_ASK_AGAIN result.
    (F5b/G4b, LOW — lifetime note): since permanence now keys on the `NEVER_ASK_AGAIN` request result, a persisted "have we requested before" flag is NO LONGER needed for permanent-denial detection. If any auxiliary prior-attempt state is kept (e.g. to decide whether to show the D-11c priming explainer), note in the hook/SUMMARY whether it lives in-memory per screen session vs a persisted flag and that it does NOT drive permanence — permanence comes from NEVER_ASK_AGAIN alone.
  </action>
  <verify>
    <automated>npx vitest run src/services/contacts/read-contacts-permission-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classifyPermissionResult` returns "granted" for `RESULTS.GRANTED`, "permanent" for `RESULTS.NEVER_ASK_AGAIN`, and "denied" for `RESULTS.DENIED` — all test-proven. It does NOT return "permanent" off a repeat-DENIED count and treats any ambiguous denial as recoverable "denied".
    - The classifier signature keys ONLY on the typed `PermissionsAndroid.request()` result (a `PermissionStatus`); it does NOT accept or sample a rationale signal. Neither the classifier nor the hook references `shouldShowRequestPermissionRationale` (assert `grep -r 'shouldShowRequestPermissionRationale' src/` returns nothing) — so `npx tsc --noEmit` stays clean against the installed RN typings.
    - `read-contacts-permission-logic.ts` is pure (no react-native import; assert via grep).
    - The hook exposes an `openContactsSettings()` calling `Linking.openSettings()` (grep).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Permission classification is a tested pure function and the hook exposes grant/deny/permanent + Open-Settings, fresh-read and ask-once.</done>
</task>

<task type="auto">
  <name>Task 2: Screen permission states — prime, calm-deny + re-request, permanent → Open Settings</name>
  <files>src/screens/LegacyContactPickerScreen.tsx</files>
  <read_first>
    - src/screens/LegacyContactPickerScreen.tsx (plan 03's screen — the list body to gate behind permission states; the minimal request from plan 01)
    - src/services/contacts/use-read-contacts-permission.ts (the extended hook — check/request/classify/openContactsSettings)
    - src/theme/theme-types.ts (the token palette :29-158 — token-only state views, row min-height 44)
  </read_first>
  <action>
    Gate the picker list behind the permission states in LegacyContactPickerScreen.tsx, all token-driven:
    (1) First-run PRIMING (D-11c): before requesting, show an in-app pre-permission explainer with plain privacy-forward copy — Orbit reads your contacts on-device so you can pick which to import, nothing leaves your device — and a "Continue" button that triggers `requestContactsPermission()`. (Final wording is owner-tunable taste; use plain, privacy-forward copy per D-11c.)
    (2) DENIED (D-11a): a calm, non-blocking explainer with a "Grant access" button that re-calls `requestContactsPermission()`. NO ACTION_PICK fallback.
    (3) PERMANENT (D-11b): switch the action to "Open Settings" → `openContactsSettings()`; on screen focus (after returning from system Settings), re-run the fresh `check` ONLY — never re-`request()` — so a grant made in Settings is picked up and the list renders. The on-focus recovery must only CHECK, never request, to avoid a permission-request loop (cursor LOW).
    (4) GRANTED: render the plan-03 list.
    Keep the whole screen token-only; do not import dashboard/orrery components.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - The screen renders four distinct states driven by the classified permission (priming / denied / permanent / granted) — grep shows it consumes the hook's classify + openContactsSettings.
    - The screen's only import action calls `readAllContacts` → `routePickedImport` (the scoped full-provider reader); no single-field fallback read path exists (the deny state re-requests the same scoped permission, per D-11a).
    - The permanent-state on-focus recovery re-runs the fresh `check` ONLY (never `request`), so returning from system Settings does not trigger a permission-request loop (assert via grep/description that the focus handler calls the check, not the request).
    - `npm run check:colors` clean (token-only state views); `npx tsc --noEmit` clean. The deny→deny-again→don't-ask-again sequence and the Open-Settings deep-link are confirmed on the Pixel 3a in plan 19.1-05.
  </acceptance_criteria>
  <done>The picker primes on first run, re-requests calmly on deny, and deep-links to Settings on permanent denial — no rejected fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the unsupported state (D-05) + route Settings through the shared seam</name>
  <files>src/screens/SettingsScreen.tsx, src/components/AddSpeedDialFab.tsx, src/screens/use-import-unsupported.ts, src/services/import/start-contact-import.ts</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (importUnsupported at :134; onImportContacts :155-175 — the pickContacts + routePickedImport + getAppSettings/getDeviceRegion/localDateTime plumbing to keep; the disabled/greyed "requires Android 17+" import row branch :755-785)
    - src/components/AddSpeedDialFab.tsx (plan 01 already routes the FAB through startContactImport; confirm no residual unsupported reference)
    - src/screens/use-import-unsupported.ts (the whole file to delete — IMPORT_UNSUPPORTED_TITLE/COPY + useImportUnsupported)
    - src/services/import/start-contact-import.ts (plan 01's shared handler both entry points call)
  </read_first>
  <action>
    Route SettingsScreen's `onImportContacts` through `startContactImport({ mode: contactImportMode(), … })` (the shared handler), keeping its getAppSettings/effectivePhoneRegion/localDateTime plumbing. Remove the `importUnsupported` gate (:134) and DELETE the disabled/greyed "requires Android 17+" branch of the import row (:755-785) so the row is always enabled and always dispatches (D-05: no device is unsupported now). Delete `src/screens/use-import-unsupported.ts` entirely (IMPORT_UNSUPPORTED_TITLE/COPY + useImportUnsupported) and remove its imports from SettingsScreen (and confirm AddSpeedDialFab has no residual reference from plan 01). Both entry points now dispatch identically through the single seam.
    Error parity (F7): a system-path (API-37) import failure from Settings must surface the SAME "Couldn't import contacts" Alert the FAB shows — today `SettingsScreen`'s `onImportContacts` only `Logger.error`s on failure, so a Settings-initiated read failure is a silent no-op. Add the parity either inside `startContactImport` (so every caller inherits it) or in the Settings caller's catch — matching the FAB's D-14 surfacing. (The legacy ≤16 read error is surfaced on the picker screen itself per plan 01/03; this bullet covers the Settings system-path.)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/screens/use-import-unsupported.ts` (the file is deleted).
    - No file imports the deleted module repo-wide: `grep -rl "use-import-unsupported" src/` returns nothing (stronger than tsc-only, which could miss a dynamic/string reference).
    - Both `src/screens/SettingsScreen.tsx` and `src/components/AddSpeedDialFab.tsx` call `startContactImport` / `contactImportMode` (assert `grep -c 'startContactImport' <file>` ≥ 1 each).
    - A system-path import failure initiated from Settings surfaces the SAME "Couldn't import contacts" Alert as the FAB (F7) — not just a `Logger.error` (assert the Alert path via grep/description in `startContactImport` or the Settings caller).
    - The Settings import row is always enabled (its `Pressable` has no unsupported-gated `disabled` branch) and dispatches through the shared handler; `npx tsc --noEmit` is clean, which fails if any file still imports the deleted unsupported module.
    - `npm run check:colors` clean.
  </acceptance_criteria>
  <done>Both entry points import on every supported device through one shared seam; the "requires Android 17+" state and its module are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User ↔ OS permission dialog | The runtime READ_CONTACTS grant is the access-control boundary; the app must request minimally and honor denial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-12 | Elevation/Policy | Over-eager or nagging permission request | medium | mitigate | Requested only at the value moment (opening import); primed once; a denial degrades to a calm in-app state with no re-prompt loop (mirrors notifications ask-once). |
| T-19.1-13 | Denial of service (UX) | Permanent-denial mis-detection strands the user | medium | mitigate | Pure classifier + fresh re-check on focus; "Open Settings" deep-link recovers; device-confirmed on the 3a (A1/A2, plan 19.1-05). |
| T-19.1-14 | Information disclosure | Reintroducing a degraded single-field path widening/altering the read | low | mitigate | No ACTION_PICK fallback (D-11a rejected); the only read path is the scoped full-provider reader; no egress added (D-06). |

No external API, no network egress, no new package installs (PermissionsAndroid + Linking are react-native built-ins); no supply-chain gate applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
Newly created symbols:
- `src/services/contacts/read-contacts-permission-logic.ts`: `classifyPermissionResult()` and its result-state type.
- `src/services/contacts/use-read-contacts-permission.ts`: extended with the classified state + `openContactsSettings()` (built on plan 01's getContactsPermission/requestContactsPermission).
- `src/screens/LegacyContactPickerScreen.tsx`: permission-state views (priming/denied/permanent/granted) — same component symbol.
Removed: `src/screens/use-import-unsupported.ts` (IMPORT_UNSUPPORTED_TITLE, IMPORT_UNSUPPORTED_COPY, useImportUnsupported) — deletion; note in SUMMARY.
</artifacts_produced>

<verification>
- `npx vitest run src/services/contacts/read-contacts-permission-logic.test.ts` green.
- `npx tsc --noEmit` clean (no dangling import of the deleted unsupported module).
- `npm run check:colors` clean.
- Device confirmation of the deny/permanent/Open-Settings sequence on the Pixel 3a is scheduled in plan 19.1-05.
</verification>

<success_criteria>
Contact import primes-then-requests on first run, re-requests calmly on deny (no ACTION_PICK fallback), deep-links to Settings on permanent denial, and is reachable on every device from both the FAB and Settings through one shared seam — the "requires Android 17+" state fully retired.
</success_criteria>

<output>
Create `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-04-SUMMARY.md` when done.
</output>
