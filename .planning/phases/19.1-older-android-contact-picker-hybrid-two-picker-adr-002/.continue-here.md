---
context: phase
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-30T11:48:27.053Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through an actual failed build attempt. A resuming agent must acknowledge and apply them before acting.

- [ ] CONSTRAINT: Do not overlap a clean Expo prebuild with Gradle — `expo prebuild --clean` deletes `android/`, while `assembleDebug` can hold generated resources open. Follow the runbook serially: marker validation → clean reset → tar-over-SSH sync → `npm ci` → clean prebuild → manifest check → Gradle build. Wait for each command to finish.

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Concurrent native regeneration and Gradle build | The first Plan 05 attempt ran an `assembleDebug` build while `expo prebuild --clean` attempted to remove `android/app/src/main/res/drawable-xxhdpi`, producing `EPERM`. | blocking | Use the exact serial droid pipeline in `docs/runbooks/desktop-build-pipeline.md`; check that the Gradle wrapper has exited before starting a clean prebuild. |
| Undocumented source transfer | The first transfer used `git archive` into the existing droid folder instead of the runbook's clean tar-over-SSH source sync. | advisory | Use the marker-validated clean reset and the documented tar-over-SSH command; never push and never treat droid as a Git checkout. |

<current_state>

Phase 19.1 is paused in Plan 05, Task 2, a `checkpoint:human-verify` with `gate=blocking-human`. Plans 01-04 are complete and summarized. Plan 05 Task 1 is complete; Task 2 has not accessed the Pixel 3a UI or any contacts because the user paused immediately after authorizing it.

The owner is switching accounts. Do not resume device work until they return and explicitly authorize continuation.
</current_state>

<completed_work>

Completed plans:

- Plan 01: legacy Android <=36 contact-acquisition tracer; summary `19.1-01-SUMMARY.md`.
- Plan 02: carried-forward shared import-pipeline fixes; summary `19.1-02-SUMMARY.md`.
- Plan 03: custom picker UI and two-tier native reads; summary `19.1-03-SUMMARY.md`.
- Plan 04: permission UX and both-entry-point routing; summary `19.1-04-SUMMARY.md`.

Plan 05, Task 1 — complete:

- Full Node gate passed: 167 test files / 1,721 tests, TypeScript, and color checks.
- Droid build pipeline completed after correction: marker-validated clean reset, tar-over-SSH source sync, `npm ci`, clean Android prebuild, and `assembleDebug`.
- A3a passed: generated Android manifest has exactly one `READ_CONTACTS` declaration with `android:maxSdkVersion="36"`.
- The package-scoping fix is committed in `b81e335`; build/manifest evidence is committed in `0b53840`.
- `19.1-DEVICE-UAT.md` is prepared with synthetic-fixture lifecycle instructions, redacted evidence rules, a traceability section, and a complete matrix of NOT RUN rows.
</completed_work>

<remaining_work>

- Plan 05, Task 2: install/use the debug APK on the connected Pixel 3a and execute the full UAT matrix using synthetic contacts only. Do not inspect, select, export, log, or commit the owner's real contacts.
- Capture only redacted/synthetic evidence. Record every result honestly; do not fabricate a pass.
- Run the separate empty-book pass and the reproducible large-book / >900-selection procedure described in `19.1-DEVICE-UAT.md`.
- Complete A3b only with the Pixel 6 Pro/API 37: record both `pm grant` refusal and runtime permission-state evidence. It is currently BLOCKED, not failed or passed.
- Apply the ADR-002 supersession note only after grepping the actual targets prescribed by the plan; do not edit ADR-002. Record the conditional Graphify registry result.
- Present recorded findings to the owner and obtain explicit final sign-off. Only then may the executor write Plan 05's SUMMARY and complete the phase gates.
</remaining_work>

<decisions_made>

- Use synthetic fixtures only. This follows the Plan 05 hygiene requirement and honors the owner's PII-minimization preference.
- The connected device is Pixel 3a (sargo), the sole <=16 UAT target. It is not evidence for the Pixel 6/API-37 A3b policy check.
- A3a and A3b are distinct: a scoped generated manifest proves packaging, while Pixel 6 runtime behavior proves inertness on API 37.
- Droid is a source-synced Windows build folder (`C:\Users\bwales\projects\orbit-app`), not a checkout. Use tar-over-SSH, never Git push.
</decisions_made>

<blockers>

- Owner account switch: device work is paused until they return and authorize continuation.
- Pixel 6 Pro was not connected. A3b remains BLOCKED until its real-device permission-state check can be run.
- Existing broad Biome findings are documented as out of Task 1 scope. The generated-manifest plugin and `app.config.ts` are clean; do not reformat unrelated historical files to close this plan.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured current status and resume target.
1. `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-05-device-uat-and-manifest-verification-PLAN.md` — exact UAT requirements and blocking-human contract.
1. `.planning/phases/19.1-older-android-contact-picker-hybrid-two-picker-adr-002/19.1-DEVICE-UAT.md` — current results, synthetic fixture protocol, and checklist.
1. `docs/runbooks/desktop-build-pipeline.md` — droid transport/build order; do not improvise a different pipeline.
1. `CLAUDE.md` — physical-device and adb rules.
1. `docs/decisions/ADR-002-hybrid-two-picker-contact-import.md` — immutable accepted decision and documentation follow-up scope.

## Infrastructure State

- Pixel 3a (`sargo`) is connected over USB and was the only adb-visible device when paused. No app/device UAT action was taken after the final device-executor was interrupted.
- Droid is reachable over SSH. The debug APK exists at `C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\debug\app-debug.apk` (347,233,588 bytes, built 2026-08-30 06:27 local droid time). It was also transferred locally during Task 1.
- No known async jobs or active Gradle wrapper build remain.
- Unrelated untracked files: `.gsd/` and `docs/dossier/milestone-2/`. Preserve them.

<context>

The implementation phase is in good shape: all code plans are committed and the full test suite is green. The remaining work is intentionally human-gated hardware verification. Keep the PII boundary strict even though the owner previously approved local contacts access: synthetic fixtures are required by the plan and avoid exposure altogether. The phase must not be marked complete until the UAT evidence and explicit owner sign-off exist.
</context>

<next_action>

Start with `$gsd-resume-work`. After the owner returns, acknowledge the blocking prebuild/Gradle constraint, then resume Plan 19.1-05 Task 2 at its blocking-human checkpoint. Verify device selection before every adb action and use synthetic fixtures only.
</next_action>
