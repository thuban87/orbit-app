---
phase: 23-theme-visual-system
plan: 04
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/theme/use-reduced-motion.ts
  - src/theme/use-reduced-motion.test.ts
  - src/theme/tokens/motion.ts
  - src/theme/tokens/motion.test.ts
autonomous: true
requirements: [THEME-06]
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "THEME-06 / D-05 / D-07 (highest-risk item): a reusable reduced-motion hook reads the OS preference via AccessibilityInfo.isReduceMotionEnabled() (seed) + addEventListener('reduceMotionChanged') (live) and exposes it as a Reanimated SharedValue<boolean> readable from the Skia render loop through useDerivedValue WITHOUT per-frame setState — animation is never driven from React state."
    - "THEME-06 (zero-one-many, UI-SPEC UI Considerations 'reduced-motion ON vs OFF', covered): when reduced motion is ON, nonessential motion stops or simplifies app-wide; when OFF, ambient motion runs. The value updates LIVE mid-session (via the reduceMotionChanged subscription), not only at boot."
    - "D-07 / RESEARCH Pitfall 1: the hook does NOT use react-native-reanimated's useReducedMotion() — that is boot-time-only and never re-updates on a live toggle. The AccessibilityInfo subscription bridge is the only correct source for the live requirement."
    - "A separate state-backed boolean twin is provided for NON-Skia (React-tree) consumers that need to re-render on change (e.g. choose a crossfade vs an instant swap); the SharedValue is for worklets, the boolean for React — Skia is never driven from the boolean."
    - "The subscribe/seed/cleanup logic is EXTRACTED into a plain non-React controller with ONE pinned signature (REVIEWS 23-04 MEDIUM): `createReducedMotionController(accessibilityInfo, emit): { dispose(): void }` — `emit` is the SECOND ARGUMENT (a `(value: boolean) => void` callback the controller calls once with the seeded value and again on every reduceMotionChanged), NOT a `start(emit)` method or a subscription-return API; the returned object exposes exactly `dispose()` (removes the listener + guards a post-dispose seed resolve). It is fully node-testable by mocking `accessibilityInfo` and passing a spy `emit` (the repo has Vitest but no react-test-renderer/testing-library, so a hook cannot be mounted/unmounted in a node test). The two hooks are thin wrappers: each instantiates its OWN controller in a `useEffect` (its own listener — no shared subscription), passing an `emit` that writes the SharedValue `.value` (useReducedMotionShared) or setState (useReducedMotion), and calls `dispose()` on unmount; the controller carries the tested seed/single-subscribe/live-toggle/unmount-cleanup invariants and one device-level assertion covers the hook wiring."
    - "motion tokens are pure data: motion.fast(120)/base(200)/slow(320)/ambient + easing.standard/decelerate; RN-free where possible and carrying no colour literal; ambient timings are tunable top-of-file single-number edits. The `ambient` token exposes the exact shape Plan 06's Orrery worklet multiplies into its useDerivedValue — a per-second angular/drift SPEED constant (not a duration) — so the Plan 04↔Plan 06 seam cannot mismatch (REVIEWS 23-04 LOW)."
    - { statement: "THEME-06 (unclassified edge — flagged planner assumption, not auto-dismissed): 'nonessential' motion = ambient/decorative (Galaxy drift/twinkle, Orrery drift/inertia/twinkle, decorative transitions); 'essential' feedback (focus, explicit state-change acknowledgement) may remain when reduced motion is ON. The exact partition per surface is applied by each consuming surface (Orrery/background in Plan 06); this hook only exposes the signal. Flagged for verify.", verification: backstop }
    - { statement: "THEME-06 subscription lifecycle: the effect seeds once, subscribes once, and removes the listener on unmount (no leak, no double-subscribe under StrictMode remounts); writing sharedValue.value is not a setState and does not re-render.", verification: explicit }
    - "src/theme/use-reduced-motion.ts (+ .test.ts) — createReducedMotionController(accessibilityInfo, emit): { dispose(): void } (plain, node-testable; emit is the 2nd arg) + useReducedMotionShared(): SharedValue<boolean> (worklet/Skia) + useReducedMotion(): boolean (React-tree twin), the hooks being thin wrappers over the controller"
    - "src/theme/tokens/motion.ts (+ .test.ts) — MOTION durations (fast/base/slow) + ambient SPEED constant + EASING tokens"
    - "Consumers import the direct modules `@/theme/use-reduced-motion` and `@/theme/tokens/motion` (no `src/theme/index.ts` barrel edit — matches Plan 06's read_first which imports the module directly; REVIEWS 23-04 LOW)"
  key_links:
    - "OS reduceMotionChanged event -> AccessibilityInfo listener -> useSharedValue<boolean> -> useDerivedValue in the Skia loop (Plan 06 Orrery/background consumers)"
    - "MOTION/EASING tokens -> animated transitions across Plans 06/07"
  prohibitions:
    - "Do NOT use react-native-reanimated's useReducedMotion() for this — it is boot-time-only and silently satisfies the letter not the spirit (RESEARCH Pitfall 1)."
    - "Do NOT drive any Skia/worklet animation from a React boolean or per-frame setState (D-07) — the SharedValue is the only worklet-readable source."
    - "Do NOT poll on an interval for the reduced-motion value — use the event subscription (no battery cost)."
    - "Do NOT put a colour literal in the motion token file (check:colors)."
  downstream_consumers:
    - "Plan 06 consumes useReducedMotionShared() in OrreryCanvas (twinkle/drift) and the Galaxy animated background; the boolean twin gates crossfade-vs-instant decisions."
---

<objective>
Deliver the reusable OS reduced-motion signal — the phase's highest-risk integration — as a live Reanimated SharedValue readable from the Skia render loop without per-frame setState, plus a React-tree boolean twin and the semantic motion tokens.

Purpose: satisfy THEME-06/D-05/D-07 with the correct AccessibilityInfo-subscription bridge (never Reanimated's boot-time hook), so later Skia surfaces can gate ambient motion live.
Output: use-reduced-motion.ts (SharedValue + boolean twin) and motion tokens, node-tested with AccessibilityInfo mocked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduced-motion hook (SharedValue bridge + boolean twin)</name>
  <files>src/theme/use-reduced-motion.ts, src/theme/use-reduced-motion.test.ts</files>
  <read_first>
    - 23-RESEARCH.md Pattern 1 (lines ~179-225) — the AccessibilityInfo -> SharedValue bridge code, and Pitfall 1 (why Reanimated's useReducedMotion is wrong)
    - src/components/orrery/OrreryCanvas.tsx:93-101 — the proven useClock + useDerivedValue Skia render-loop pattern the SharedValue is read through
    - node_modules/react-native/Libraries/Components/AccessibilityInfo/AccessibilityInfo.d.ts (isReduceMotionEnabled + reduceMotionChanged signatures)
    - any existing test that mocks a react-native module (mocking idiom for AccessibilityInfo)
  </read_first>
  <behavior>
    - `createReducedMotionController(accessibilityInfo, emit): { dispose(): void }` is a plain (non-React) controller with a PINNED signature (REVIEWS 23-04 MEDIUM): `accessibilityInfo` is the injected AccessibilityInfo-shaped dependency `{ isReduceMotionEnabled(): Promise<boolean>, addEventListener(event, handler): { remove(): void } }`; `emit` is the SECOND ARGUMENT, a `(value: boolean) => void` callback the controller calls once with the seeded value and again on every reduceMotionChanged (emit is an ARGUMENT, not a `start(emit)` method or a subscription-return API). It seeds from `isReduceMotionEnabled()`, subscribes ONCE to reduceMotionChanged, returns exactly `{ dispose() }` (removes the listener + guards a post-dispose seed resolve). Fully node-testable by passing a mock accessibilityInfo and a spy emit — no React renderer needed.
    - useReducedMotionShared() wraps the controller: a Reanimated useSharedValue<boolean> whose .value the controller writes (never setState); useReducedMotion() wraps it as a state-backed boolean twin that DOES re-render.
    - The controller test asserts seed, live toggle, single-subscribe, dispose-cleanup, and post-dispose-seed guard; one device-level assertion covers the hook wiring.
  </behavior>
  <action>
    Create `src/theme/use-reduced-motion.ts` with three exports. First, `createReducedMotionController(accessibilityInfo, emit): { dispose(): void }` — a plain controller with this EXACT pinned signature (REVIEWS 23-04 MEDIUM): `accessibilityInfo` is the injected dependency `{ isReduceMotionEnabled, addEventListener }` and `emit` is the SECOND ARGUMENT, a `(value: boolean) => void` callback (NOT a `start(emit)` method, NOT a subscription-return API); it seeds from `isReduceMotionEnabled()` and calls `emit(seed)`, subscribes ONCE to `reduceMotionChanged` forwarding each change through `emit(value)`, returns `{ dispose() }` removing the listener, and guards against a post-dispose seed resolve. Then `useReducedMotionShared()` — a Reanimated `useSharedValue<boolean>` where a `useEffect` instantiates its OWN controller with an `emit` that writes `.value` (never setState); and `useReducedMotion()` — a state-backed boolean twin for React-tree consumers whose `useEffect` instantiates its own controller with an `emit` that calls setState. Each hook owns one controller instance (one subscription per mounted hook — no shared subscription) and calls `dispose()` on unmount. Write `use-reduced-motion.test.ts` NODE-testing the controller with a mock AccessibilityInfo (seed, live toggle, single-subscribe, dispose-cleanup, post-dispose-seed guard); do not attempt to mount the hooks in node.
  </action>
  <verify>
    <automated>npx vitest run src/theme/use-reduced-motion.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/use-reduced-motion.test.ts` passes covering the controller's seed (`emit(seed)`), live reduceMotionChanged update (`emit(value)`), single subscription, `dispose()` cleanup, and post-dispose-seed guard — all in node with a mock accessibilityInfo and a spy `emit` (no react-test-renderer dependency).
    - `createReducedMotionController` has the pinned signature `(accessibilityInfo, emit): { dispose(): void }` (emit as the 2nd argument, returns exactly `dispose()`) — tsc + the test's call site confirm the shape.
    - grep confirms the module imports AccessibilityInfo and useSharedValue and does NOT import react-native-reanimated's useReducedMotion.
    - The controller is exported and testable independent of the hooks; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A node-tested reduced-motion controller plus its two thin hook wrappers (worklet-readable SharedValue, no setState; React boolean twin) exist, tested without a React renderer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motion tokens</name>
  <files>src/theme/tokens/motion.ts, src/theme/tokens/motion.test.ts</files>
  <read_first>
    - 23-UI-SPEC.md Motion Tokens table (fast/base/slow/ambient + easing.standard/decelerate)
    - src/theme/tokens/spacing.ts (pure-data token idiom, if present from Plan 02) or src/components/contact-card-ring.ts (RN-free pure-module idiom)
  </read_first>
  <behavior>
    - MOTION exposes fast(120), base(200), slow(320) durations, an `ambient` SPEED constant (a per-second drift/angular rate the Orrery worklet multiplies into useDerivedValue — NOT a duration, so Plan 06 consumes it without a unit mismatch), and EASING standard/decelerate; ambient constants are top-of-file tunable.
    - No colour literal; node-importable.
  </behavior>
  <action>
    Create `src/theme/tokens/motion.ts` with the semantic `MOTION` durations (fast/base/slow) plus an `ambient` SPEED constant (per-second rate, the exact shape Plan 06's Orrery worklet multiplies in) and `EASING` tokens as pure data (ambient constants tunable top-of-file); consumers import `@/theme/tokens/motion` directly (no barrel edit). Write `motion.test.ts` asserting the token set, value ordering (fast < base < slow), and that `ambient` is a positive rate constant.
  </action>
  <verify>
    <automated>npx vitest run src/theme/tokens/motion.test.ts</automated>
    <automated>npm run check:colors src/theme/tokens/motion.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/tokens/motion.test.ts` passes asserting fast/base/slow + the ambient SPEED constant (positive rate, not a duration) + easing tokens and fast<base<slow.
    - `npm run check:colors src/theme/tokens/motion.ts` exits 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Semantic motion + easing tokens exist as pure, node-tested data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS accessibility API -> app | reduceMotionChanged events + isReduceMotionEnabled read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Denial of Service | leaked/duplicate AccessibilityInfo subscription | low | mitigate | Effect subscribes once and removes the listener on unmount (tested); a post-unmount seed resolve is guarded |
| T-23-S/T/R/I/E | Spoofing/Tampering/Repudiation/Info/Elevation | — | n/a | accept | Reading an OS accessibility flag is local, read-only, non-sensitive; no network, no persistence, no auth surface |
</threat_model>

<verification>
- `npx vitest run src/theme/use-reduced-motion.test.ts src/theme/tokens/motion.test.ts` green; check:colors + tsc clean.
- Device UAT (end-of-phase Pixel): toggle OS reduced motion mid-session and confirm ambient motion (Plan 06 consumers) stops live without an app restart.
</verification>

<success_criteria>
A reusable, live reduced-motion signal is readable from the Skia loop as a SharedValue (no per-frame setState) with a React boolean twin, and the semantic motion tokens exist — satisfying THEME-06/D-07 and unblocking the Plan 06 Skia consumers.
</success_criteria>

## Artifacts this phase produces

New files: `src/theme/use-reduced-motion.ts` (+ `.test.ts`), `src/theme/tokens/motion.ts` (+ `.test.ts`).
New exported symbols: `createReducedMotionController(accessibilityInfo, emit): { dispose(): void }` (plain controller; emit is the 2nd arg), `useReducedMotionShared()` (SharedValue<boolean>), `useReducedMotion()` (boolean twin); `MOTION` (fast/base/slow durations + `ambient` speed constant), `EASING` (standard/decelerate).

<output>
Create `.planning/phases/23-theme-visual-system/23-04-SUMMARY.md` when done.
</output>
