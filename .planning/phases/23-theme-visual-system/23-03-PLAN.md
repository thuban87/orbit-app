---
phase: 23-theme-visual-system
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/theme/theme-presets.ts
  - src/theme/theme-presets.test.ts
  - src/theme/theme-types.ts
  - src/theme/theme-provider.tsx
  - src/theme/accents.ts
  - src/theme/accents.test.ts
  - src/theme/contrast.ts
  - src/theme/contrast.test.ts
autonomous: true
requirements: [THEME-01, THEME-02, THEME-11]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "THEME-01 (partial, UI-SPEC UI Considerations 'all four theme/mode combinations', covered): all four palettes are authored — Galaxy Dark, Galaxy Light, Standard Dark, Standard Light — each defining the full ThemePalette token set; ThemePreset.light becomes REQUIRED so the resolvePalette dark-fallback stops being load-bearing. Galaxy is glass-forward/deep-space; Standard is calmer/flatter (dossier §B); both share layout/IA/density."
    - "THEME-02 / dossier §C (per-mode accent tones — REVIEWS 23-03 HIGH resolution): a curated accent set of ~8 choices lives in src/theme/accents.ts, but each accent resolves to a PER-MODE tone triple `{ fill, onAccent, text }` — NOT a single hex. `fill` = the filled-accent background (Primary button / active state); `onAccent` = the foreground drawn on `fill`; `text` = the accent-as-text/link tone for THAT mode's background. This is required because one hex cannot be AA-4.5:1 as a text-link on BOTH a Galaxy-dark background and a near-white light surface (theme-provider.tsx:33-39 exposes one palette.accent). Tones vary by resolved mode (dark|light) at minimum; per-package variation is allowed at discretion. accent stays separate from the owner/star colour (starPalette). Each package ships a default accent (Galaxy -> Nebula Blue, Standard -> Slate Indigo) applied when the stored per-package accent is NULL."
    - "THEME-02 / D-06: accent hexes live ONLY under a /theme/ path (accents.ts). The DAO stores an accent-ID (or NULL), never a hex; the provider resolves the active package's accent-id + resolved mode -> the `{fill, onAccent, text}` tone from accents.ts and overlays `palette.accent`=fill, `palette.onAccent`, `palette.accentText`=text at render."
    - "THEME-03 / D-11 (accent portion): each package remembers its own accent — switching package restores that package's stored accent-id (or its default when NULL); an accent change previews live because the provider re-renders from store state. Owner/star colour is untouched."
    - "THEME-11: contrast.ts is a pure, node-tested WCAG relative-luminance + ratio module (no dependency, RN-free); AA_NORMAL=4.5, AA_LARGE=3.0 — NEVER weakened to make a token pass (prohibited reversal)."
    - "THEME-11 (SPLIT gate — REVIEWS 23-03 HIGH resolution): the per-combo AA test has two dispositions. (a) CURATED ACCENTS + NEWLY-AUTHORED palette tokens (galaxy-light, standard dark+light) => HARD-FAIL: an accent tone (`text` vs background/surface, `onAccent` vs `fill`) or a new-palette required token that misses AA in any of the four combos fails the build; the fix is retuning the DISCRETION tone or dropping the accent — never lowering AA. (b) LEGACY pre-existing GALAXY-DARK (=space-dark) required tokens => FLAG-FOR-OWNER (stop-and-ask): if one misses AA the executor does NOT edit the hue; it records the token + measured ratio in the SUMMARY as an owner decision needed and halts. The correct set is ALL pre-existing galaxy-dark required tokens regardless of whether each carries an inline owner-approval date — statusStable/statusWobble/statusDecay ('owner-approved 2026-08-16', theme-types.ts:77-108), rogue ('owner-approved 2026-08-15', :66-75), AND textPrimary/textSecondary (:34-35, which carry NO inline date but are still pre-existing space-dark tokens). An undated pre-existing hue is NOT license to auto-retune it (REVIEWS 23-03 LOW citation fix). Retuning any pre-existing galaxy-dark hue is an owner taste decision, not a planner/executor call."
    - "THEME-11 (real compositing pairs — REVIEWS 23-03 MEDIUM): the gate tests ACTUAL fg/bg pairs, not a palette-only approximation. Opaque pairs validated here: textPrimary/textSecondary vs {background, surface, surfaceElevated}; each status hue vs the surface it is drawn on ({background, surface}); accent `text` vs {background, surface}; accent `onAccent` vs `fill`. Translucent GLASS-composited pairs (Plan 06's surfaces) are validated in Plan 06 against the guaranteed semi-opaque fallback tint token where the composite colour is defined — Plan 03 explicitly does NOT claim its opaque-pair check is the complete accessibility gate."
    - { statement: "THEME-11 (long-text, UI-SPEC UI Considerations 'AA-equivalent contrast across all 4 combos', backstop): every accent + status + text + surface pair meets AA-equivalent contrast in Galaxy Dark, Galaxy Light, Standard Dark, and Standard Light; verified by the per-combo contrast test at verify time.", verification: backstop }
    - { statement: "THEME-11 (adjacency edge): contrastRatio treats an equal or near-equal foreground/background pair (ratio approaching 1.0) as a FAIL against the AA threshold — a token that merges into its background is rejected, never rounded up to passing.", verification: explicit }
    - { statement: "THEME-11 (empty edge): contrastRatio over a single colour against itself returns 1.0 (fails AA); a malformed/empty hex input is rejected by the parser rather than silently yielding a passing ratio.", verification: explicit }
    - { statement: "THEME-11 (ordering edge): contrastRatio is symmetric — the larger luminance is always the numerator regardless of argument order — so (a,b) and (b,a) return the identical ratio and pass/fail identically.", verification: explicit }
    - { statement: "THEME-01 (adjacency edge): resolvePalette maps each (package, mode) pair to exactly one distinct palette; galaxy and standard at the same mode never resolve to the same palette object once all four are authored.", verification: explicit }
    - { statement: "THEME-01 (empty edge): with light now required, resolvePalette(package,'light') returns the authored light palette (never the dark fallback); a resolver call for any of the four combos is total (no undefined).", verification: explicit }
    - { statement: "THEME-01 (ordering edge): palette resolution is independent of the order package vs mode were selected; the same (package,mode) yields the same palette every time.", verification: explicit }
    - "PLANNER ASSUMPTION (edge-probe unclassified, THEME-02 — flagged, not auto-dismissed): the accent set is a FIXED curated list (~8 ids) with a deterministic per-package default; there is no unrestricted/user-entered accent (out of scope). An accent failing the AA gate is dropped from the list, never silently retained. Flagged for verify."
  artifacts:
    - "src/theme/theme-presets.ts — four authored palettes (galaxy dark+light, standard dark+light); Galaxy glass-forward, Standard flatter; one of the sanctioned /theme/ hex-literal files (accents.ts also carries hexes — check:colors exempts the whole /theme/ subtree, not only theme-presets.ts)"
    - "src/theme/theme-types.ts — ThemePreset.light made REQUIRED; AccentId + per-mode tone `{fill, onAccent, text}` types; ResolvedTheme/ThemePalette gain onAccent + accentText (overlay-provided)"
    - "src/theme/theme-provider.tsx — overlays the resolved active-package accent-id + resolved mode -> `{fill, onAccent, text}` tone from accents.ts onto palette.accent(=fill)/onAccent/accentText at render; per-package accent memory"
    - "src/theme/accents.ts (+ .test.ts) — ACCENTS curated set (each id -> per-resolved-mode `{fill, onAccent, text}` tones), package defaults, resolveAccent(id|null, package, resolvedMode) -> tone triple"
    - "src/theme/contrast.ts (+ .test.ts) — contrastRatio/luminance + AA thresholds; per-combo AA gate over all four palettes"
  key_links:
    - "app_settings galaxy_accent/standard_accent (id|NULL) -> theme-store -> theme-provider -> resolveAccent(accents.ts) -> palette.accent at render"
    - "accents.ts + theme-presets.ts palettes -> contrast.ts AA gate (drops any accent failing AA in any of the four combos)"
    - "ThemePreset.light required -> resolvePalette(package,'light') returns real light palette (dark fallback retired)"
  prohibitions:
    - "Do NOT place any accent/palette hex outside src/**/theme/** — accents.ts and theme-presets.ts are the only sanctioned locations (check:colors)."
    - "Do NOT store or return an accent as a hex from the DAO/migration; the DAO holds an accent-id or NULL, resolved to a hex only at render (self_sun_colour precedent)."
    - "Do NOT conflate the UI accent with the owner/star colour (starPalette) — they are separate settings (dossier §C, THEME-02)."
    - "Do NOT keep an accent in the curated set that fails AA in any of the four combos — retune its DISCRETION tone or drop it (THEME-11 gate); do NOT relax AA_NORMAL/AA_LARGE to make one pass (prohibited reversal)."
    - "Do NOT auto-edit a pre-existing GALAXY-DARK required hue to pass the gate (OWNER-BUCKET FIREWALL, REVIEWS 23-03 HIGH). The protected set is EVERY pre-existing galaxy-dark required token regardless of inline date: statusStable/statusWobble/statusDecay ('owner-approved 2026-08-16', theme-types.ts:77-108), rogue ('owner-approved 2026-08-15', :66-75), and textPrimary/textSecondary (:34-35, UNDATED — an undated pre-existing hue is still owner-bucket, REVIEWS 23-03 LOW). If any misses AA, STOP and escalate to the owner (record token + ratio in the SUMMARY) — retuning that hue is an owner taste decision. New palettes (galaxy-light, standard dark+light) MAY be tuned to pass (agent discretion)."
    - "Do NOT collapse the accent to a single hex — use the per-mode `{fill, onAccent, text}` tone triple so accent-as-text can meet 4.5:1 on both dark and light backgrounds (REVIEWS 23-03 HIGH)."
    - "Do NOT add an unrestricted accent picker or any user-entered accent value (out of scope, dossier deferred list)."
  downstream_consumers:
    - "Plan 06 (backgrounds/surface) resolves per-package background over these palettes AND extends the AA gate to the translucent glass-composited pairs against the semi-opaque fallback tint token; Plan 05 (status glyphs) reads status hue tokens from these palettes; Plan 07 (buttons) uses palette.accent(=fill) + palette.onAccent for Primary and palette.accentText for text/link (Tertiary) roles."
---

<objective>
Author the four real palettes (Galaxy/Standard × Light/Dark), build the curated accent system separate from the star colour, and gate every accent/status/text token against AA-equivalent contrast in all four combinations.

Purpose: fill the palette + accent DATA the tracer's package axis resolves, satisfying THEME-01 (four combos), THEME-02 (curated accent), and THEME-11 (AA contrast) without re-opening the migration/DAO/boot spine.
Output: four palettes, accents.ts, contrast.ts, and a passing per-combo AA gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
@.planning/phases/23-theme-visual-system/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure WCAG contrast module</name>
  <files>src/theme/contrast.ts, src/theme/contrast.test.ts</files>
  <read_first>
    - 23-RESEARCH.md Contrast validation code example (lines ~356-370) — the WCAG relative-luminance formula
    - src/components/contact-card-ring.ts (RN-free node-testable pure-module header idiom to mirror)
    - 23-UI-SPEC.md Color -> Validation gate (AA-equivalent thresholds)
  </read_first>
  <behavior>
    - contrastRatio(a,b) is symmetric and returns 1.0 for identical colours; a malformed/empty hex is rejected (throws), never silently passes.
    - AA_NORMAL=4.5, AA_LARGE=3.0 exported.
    - A pair approaching ratio 1.0 fails the AA threshold.
  </behavior>
  <action>
    Create `src/theme/contrast.ts` — a pure, RN-free module implementing the WCAG relative-luminance channel + luminance + `contrastRatio(a,b)` (larger luminance as numerator, so it is argument-order-symmetric) plus exported `AA_NORMAL`/`AA_LARGE` thresholds. Validate the hex input shape and reject malformed/empty strings. Write `contrast.test.ts` covering symmetry, identity=1.0, malformed-input rejection, and a known reference ratio.
  </action>
  <verify>
    <automated>npx vitest run src/theme/contrast.test.ts</automated>
    <automated>npm run check:colors src/theme/contrast.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/contrast.test.ts` passes including symmetry, identity=1.0 (fails AA), and malformed-input rejection.
    - contrast.ts imports nothing from react-native.
    - `npm run check:colors src/theme/contrast.ts` exits 0 (test-only reference hexes live under /theme/).
  </acceptance_criteria>
  <done>A pure, node-tested WCAG contrast module with AA thresholds exists under src/theme/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the four palettes and make light required</name>
  <files>src/theme/theme-presets.ts, src/theme/theme-presets.test.ts, src/theme/theme-types.ts</files>
  <read_first>
    - src/theme/theme-presets.ts (galaxy = the former space-dark dark palette from Plan 01; the full ThemePalette token set incl. status/gravity/star/muted tokens)
    - src/theme/theme-types.ts (ThemePackage axis + ThemePreset.light optional from Plan 01)
    - src/theme/theme-presets.test.ts (package-axis tests from Plan 01)
    - 23-UI-SPEC.md Color + Surface & Glass (Galaxy glass-forward vs Standard flatter; the 60/30/10 role structure repeated per palette with retuned luminance/saturation)
    - docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md §B, §F, §L (identities + status hue families stable across themes)
  </read_first>
  <behavior>
    - THEME_PRESETS.galaxy and .standard each define dark AND light full ThemePalette token sets (all existing tokens: background/surface/.../status*/gravityTiers/starPalette/muted*/rogueExtinguished).
    - Status hue FAMILIES stay recognizable across all four (Stable/Wobble/Decay/Rogue) with per-palette luminance/saturation retuned for contrast.
    - resolvePalette(package,mode) returns four distinct palettes; ThemePreset.light is now required (dark fallback path removed).
  </behavior>
  <action>
    In `src/theme/theme-presets.ts` author the light palette for galaxy and the full dark+light palettes for standard, keeping the galaxy dark palette (from Plan 01) as the reference; Galaxy stays deep-space/glass-forward, Standard calmer/flatter, both preserving the full token set and stable status hue families. In `theme-types.ts` make `ThemePreset.light` REQUIRED. Extend `theme-presets.test.ts` to assert all four combos resolve to distinct, complete palettes and that light is no longer a fallback. Keep every hex inside theme-presets.ts.
  </action>
  <verify>
    <automated>npx vitest run src/theme/theme-presets.test.ts</automated>
    <automated>npm run check:colors src/theme/theme-presets.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/theme-presets.test.ts` passes asserting four distinct complete palettes and required light.
    - ThemePreset.light is required (tsc fails if a preset omits light).
    - Every ThemePalette token present in all four palettes; no missing token.
    - `npm run check:colors src/theme/theme-presets.ts` exits 0.
  </acceptance_criteria>
  <done>Four complete palettes are authored, light is required, and the resolver returns four distinct palettes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Curated accent system (per-mode tones) + SPLIT AA gate + provider overlay</name>
  <files>src/theme/accents.ts, src/theme/accents.test.ts, src/theme/theme-provider.tsx</files>
  <read_first>
    - 23-UI-SPEC.md Curated accent palette table (~8 ids) + the validation gate
    - src/db/app-settings-dao.ts (galaxy_accent/standard_accent nullable accent-id columns from Plan 01; assertAccentId)
    - src/theme/theme-provider.tsx:28-59 (Plan 01 reads the accent column; the useMemo overlay wires here; palette.accent is a single string today)
    - src/theme/theme-types.ts — the pre-existing galaxy-dark required hues the gate may only FLAG, never auto-edit, with their ACTUAL provenance (REVIEWS 23-03 LOW citation fix): statusStable/statusWobble/statusDecay at :77-108 ('owner-approved 2026-08-16'), rogue at :66-75 ('owner-approved 2026-08-15'), and textPrimary/textSecondary at :34-35 (UNDATED — no inline owner-approval date, but still pre-existing space-dark tokens and therefore still flag-for-owner). Do NOT treat the undated pair as new/tunable just because :34-35 lacks a date.
    - src/theme/contrast.ts (Task 1 gate; AA_NORMAL/AA_LARGE)
    - src/db/app-settings-dao.ts self_sun_colour NULL-resolve-at-render precedent
  </read_first>
  <behavior>
    - ACCENTS is a fixed curated set; each accent id resolves per RESOLVED MODE to a tone triple `{fill, onAccent, text}`. resolveAccent(id|null, package, resolvedMode) returns the id's tone (or the package default's tone when null); an unknown/tampered id falls back to the package default tone (asserted).
    - SPLIT AA gate test: (a) HARD-FAIL for every curated accent in all four (package,mode) combos — `onAccent` vs `fill` >= AA_NORMAL AND `text` vs {background, surface} >= AA_NORMAL — plus every REQUIRED token in the three NEWLY-authored palettes (galaxy-light, standard dark+light) over the real opaque compositing pairs; (b) for the LEGACY galaxy-dark owner-approved required tokens the test reports any AA miss as an OWNER-ESCALATION diagnostic (token + measured ratio) and does not auto-pass by editing the hue — it fails loudly with a stop-and-ask message, never a silent hue edit.
    - The gate tests the real opaque compositing pairs (textPrimary/textSecondary vs background/surface/surfaceElevated; each status hue vs background/surface; accent text vs background/surface; accent onAccent vs fill) — not a palette-only approximation. Glass-composited pairs are deferred to Plan 06.
    - The provider overlays the active package's mode-resolved tone: palette.accent=fill, palette.onAccent, palette.accentText=text; switching package restores that package's stored accent (or default).
  </behavior>
  <action>
    Create `src/theme/accents.ts` — the curated ACCENTS set where each id maps to per-resolved-mode `{fill, onAccent, text}` tones, the per-package default accent ids (Galaxy Nebula Blue, Standard Slate Indigo), and `resolveAccent(id | null, package, resolvedMode)` returning the tone triple (NULL/unknown -> package default). Wire `theme-provider.tsx` to overlay the active package's mode-resolved tone onto `palette.accent`(=fill)/`palette.onAccent`/`palette.accentText` at render (reading the accent-id column via the store; resolved mode from resolveMode). Write `accents.test.ts` running the SPLIT gate via contrast.ts: HARD-FAIL for curated accents + new-palette required tokens over the real opaque compositing pairs across all four combos (fix a miss by retuning the discretion tone or dropping the accent — never by lowering AA); and for the legacy galaxy-dark owner-approved required tokens, assert AA and, on any miss, emit an explicit owner-escalation failure naming the token + ratio (do NOT edit the owner hue). Keep all accent hexes inside accents.ts.
  </action>
  <verify>
    <automated>npx vitest run src/theme/accents.test.ts</automated>
    <automated>npm run check:colors src/theme/accents.ts src/theme/theme-provider.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/accents.test.ts` passes; the split gate covers all curated accents (per-mode `text` vs background/surface AND `onAccent` vs `fill`) across all four combos and every new-palette required token over the real opaque pairs; every shipped accent meets AA (a failing accent is retuned or removed, never kept).
    - The test partitions legacy galaxy-dark owner-approved tokens into a FLAG-FOR-OWNER path: a simulated legacy-token AA miss produces an explicit owner-escalation failure (token + ratio), and the test does NOT mutate the owner hue to pass. AA_NORMAL/AA_LARGE are unchanged (grep confirms 4.5/3.0).
    - resolveAccent(null, package, mode) returns the package default tone; a known id returns its per-mode tone; an unknown id falls back to the default (asserted).
    - Provider overlays the mode-resolved tone onto palette.accent/onAccent/accentText (grep/tsc confirms accent-id -> tone resolution in the provider, not a hex in the DAO).
    - `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A curated accent set (per-mode fill/onAccent/text tones, separate from the star colour) resolves per package (NULL->default); the split gate hard-fails accents/new-palette tokens that miss AA and flags-for-owner any legacy owner-approved galaxy-dark token that misses AA — without weakening AA or editing an owner hue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored accent-id -> render | An invalid/unknown accent-id must resolve to a safe default, not crash |
| palette data -> contrast gate | AA gate is a build/test-time check, no runtime cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Tampering | an unknown/tampered accent-id in app_settings | low | mitigate | assertAccentId (Plan 01) rejects unknown ids on write; resolveAccent falls back to the package default for a null/unknown id at render |
| T-23-11 | (accessibility invariant) | an accent that fails AA slipping into the shipped set | medium | mitigate | Split AA gate hard-fails the build for any curated accent (per-mode text/onAccent) or new-palette required token missing AA in any of the four combos over the real opaque compositing pairs |
| T-23-11c | (owner-decision integrity) | the AA gate silently retuning an owner-approved galaxy-dark hue to pass | medium | mitigate | Legacy owner-approved tokens are FLAG-FOR-OWNER: an AA miss halts with a stop-and-ask escalation (token + ratio) and the executor never edits the hue (REVIEWS 23-03 HIGH firewall) |
| T-23-S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | n/a | accept | Pure data + pure functions; local-first, no network, no auth surface |
</threat_model>

<verification>
- `npx vitest run src/theme` green (contrast, presets, accents); `npm run check:colors` and `npx tsc --noEmit` clean.
- Device UAT (end-of-phase): visually confirm all four combos and the accent picker preview live-restyle.
</verification>

<success_criteria>
Four complete palettes are authored, a curated accent set (per-mode fill/onAccent/text tones, separate from the star colour) resolves per package with NULL->default, and the split AA gate passes for every shipped accent + new-palette token while flagging (never editing) any legacy owner-approved galaxy-dark token that misses AA.
</success_criteria>

<escalations>
## REVIEWS 23-03 HIGH — accent AA gate (resolved in-scope; hue retuning escalated)

**Resolved in scope (reverses nothing):** accent representation changed to per-mode `{fill, onAccent, text}` tones so accent-as-text can meet 4.5:1 on both dark and light backgrounds; the gate is split into hard-fail (curated accents + newly-authored palettes, fixed by discretion retune/drop) vs flag-for-owner (legacy owner-approved galaxy-dark required tokens); the gate tests real opaque compositing pairs and hands glass-composited pairs to Plan 06.

**Escalated to owner (do NOT do here):** retuning any owner-approved galaxy-dark status/text hue. If the gate finds a legacy galaxy-dark required token below AA, the executor STOPS and records the token + measured ratio for an owner decision — it must not edit the hue and must not weaken AA_NORMAL/AA_LARGE. This is the sanctioned resolution under the review_incorporation_contract; the HIGH is otherwise fully mitigated.
</escalations>

## Artifacts this phase produces

New files: `src/theme/contrast.ts` (+ `.test.ts`), `src/theme/accents.ts` (+ `.test.ts`).
New exported symbols: `contrastRatio`, `relativeLuminance` (or channel helper), `AA_NORMAL` (4.5), `AA_LARGE` (3.0); `ACCENTS` curated set, `AccentId` type, per-mode tone type `{fill, onAccent, text}`, `resolveAccent(id|null, package, resolvedMode)`, per-package default accent ids.
Changed symbols: `ThemePreset.light` made REQUIRED; the four authored palettes in `theme-presets.ts` (galaxy light + standard dark/light added); provider overlay adds `palette.accent`(=fill)/`palette.onAccent`/`palette.accentText`; `ThemePalette`/`ResolvedTheme` gain `onAccent`/`accentText`.

<output>
Create `.planning/phases/23-theme-visual-system/23-03-SUMMARY.md` when done.
</output>
