---
phase: 23-theme-visual-system
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/theme/theme-presets.ts
  - src/theme/theme-presets.test.ts
  - src/theme/theme-types.ts
  - src/theme/theme-provider.tsx
  - src/theme/accents.ts
  - src/theme/accents.test.ts
  - src/theme/contrast.ts
  - src/theme/contrast.test.ts
autonomous: true
requirements: [THEME-01, THEME-02, THEME-11]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "THEME-01 (partial, UI-SPEC UI Considerations 'all four theme/mode combinations', covered): all four palettes are authored — Galaxy Dark, Galaxy Light, Standard Dark, Standard Light — each defining the full ThemePalette token set; ThemePreset.light becomes REQUIRED so the resolvePalette dark-fallback stops being load-bearing. Galaxy is glass-forward/deep-space; Standard is calmer/flatter (dossier §B); both share layout/IA/density."
    - "THEME-02 / dossier §C: a curated accent palette of ~8 choices lives in src/theme/accents.ts, each entry an {id, hex, onAccent} resolved at render; accent is separate from the owner/star colour (starPalette stays its own token). Each package ships a default accent (Galaxy -> Nebula Blue, Standard -> Slate Indigo) applied when the stored per-package accent is NULL."
    - "THEME-02 / D-06: accent hexes live ONLY under a /theme/ path (accents.ts). The DAO stores an accent-ID (or NULL), never a hex; the provider resolves the active package's accent-id -> hex from accents.ts and overlays it onto palette.accent at render."
    - "THEME-03 / D-11 (accent portion): each package remembers its own accent — switching package restores that package's stored accent-id (or its default when NULL); an accent change previews live because the provider re-renders from store state. Owner/star colour is untouched."
    - "THEME-11: contrast.ts is a pure, node-tested WCAG relative-luminance + ratio module (no dependency, RN-free); AA_NORMAL=4.5, AA_LARGE=3.0. A per-combo test runs every curated accent + status + text token against its background across all four palettes and FAILS any accent that misses AA in any combo (that accent is dropped from the curated set)."
    - { statement: "THEME-11 (long-text, UI-SPEC UI Considerations 'AA-equivalent contrast across all 4 combos', backstop): every accent + status + text + surface pair meets AA-equivalent contrast in Galaxy Dark, Galaxy Light, Standard Dark, and Standard Light; verified by the per-combo contrast test at verify time.", verification: backstop }
    - { statement: "THEME-11 (adjacency edge): contrastRatio treats an equal or near-equal foreground/background pair (ratio approaching 1.0) as a FAIL against the AA threshold — a token that merges into its background is rejected, never rounded up to passing.", verification: explicit }
    - { statement: "THEME-11 (empty edge): contrastRatio over a single colour against itself returns 1.0 (fails AA); a malformed/empty hex input is rejected by the parser rather than silently yielding a passing ratio.", verification: explicit }
    - { statement: "THEME-11 (ordering edge): contrastRatio is symmetric — the larger luminance is always the numerator regardless of argument order — so (a,b) and (b,a) return the identical ratio and pass/fail identically.", verification: explicit }
    - { statement: "THEME-01 (adjacency edge): resolvePalette maps each (package, mode) pair to exactly one distinct palette; galaxy and standard at the same mode never resolve to the same palette object once all four are authored.", verification: explicit }
    - { statement: "THEME-01 (empty edge): with light now required, resolvePalette(package,'light') returns the authored light palette (never the dark fallback); a resolver call for any of the four combos is total (no undefined).", verification: explicit }
    - { statement: "THEME-01 (ordering edge): palette resolution is independent of the order package vs mode were selected; the same (package,mode) yields the same palette every time.", verification: explicit }
    - "PLANNER ASSUMPTION (edge-probe unclassified, THEME-02 — flagged, not auto-dismissed): the accent set is a FIXED curated list (~8 ids) with a deterministic per-package default; there is no unrestricted/user-entered accent (out of scope). An accent failing the AA gate is dropped from the list, never silently retained. Flagged for verify."
  artifacts:
    - "src/theme/theme-presets.ts — four authored palettes (galaxy dark+light, standard dark+light); Galaxy glass-forward, Standard flatter; the sole hex-literal file"
    - "src/theme/theme-types.ts — ThemePreset.light made REQUIRED; accent-id + on-accent types as needed"
    - "src/theme/theme-provider.tsx — overlays the resolved active-package accent-id -> hex from accents.ts onto palette.accent at render; per-package accent memory"
    - "src/theme/accents.ts (+ .test.ts) — ACCENTS curated set [{id, hex, onAccent}], package defaults, resolveAccent(id|null, package) -> hex"
    - "src/theme/contrast.ts (+ .test.ts) — contrastRatio/luminance + AA thresholds; per-combo AA gate over all four palettes"
  key_links:
    - "app_settings galaxy_accent/standard_accent (id|NULL) -> theme-store -> theme-provider -> resolveAccent(accents.ts) -> palette.accent at render"
    - "accents.ts + theme-presets.ts palettes -> contrast.ts AA gate (drops any accent failing AA in any of the four combos)"
    - "ThemePreset.light required -> resolvePalette(package,'light') returns real light palette (dark fallback retired)"
  prohibitions:
    - "Do NOT place any accent/palette hex outside src/**/theme/** — accents.ts and theme-presets.ts are the only sanctioned locations (check:colors)."
    - "Do NOT store or return an accent as a hex from the DAO/migration; the DAO holds an accent-id or NULL, resolved to a hex only at render (self_sun_colour precedent)."
    - "Do NOT conflate the UI accent with the owner/star colour (starPalette) — they are separate settings (dossier §C, THEME-02)."
    - "Do NOT keep an accent in the curated set that fails AA in any of the four combos — drop it (THEME-11 gate); do NOT relax AA_NORMAL/AA_LARGE to make one pass."
    - "Do NOT add an unrestricted accent picker or any user-entered accent value (out of scope, dossier deferred list)."
  downstream_consumers:
    - "Plan 06 (backgrounds/surface) resolves per-package background over these palettes; Plan 05 (status glyphs) reads status hue tokens from these palettes; Plan 07 (buttons) uses palette.accent + onAccent for Primary."
---

<objective>
Author the four real palettes (Galaxy/Standard × Light/Dark), build the curated accent system separate from the star colour, and gate every accent/status/text token against AA-equivalent contrast in all four combinations.

Purpose: fill the palette + accent DATA the tracer's package axis resolves, satisfying THEME-01 (four combos), THEME-02 (curated accent), and THEME-11 (AA contrast) without re-opening the migration/DAO/boot spine.
Output: four palettes, accents.ts, contrast.ts, and a passing per-combo AA gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
@.planning/phases/23-theme-visual-system/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure WCAG contrast module</name>
  <files>src/theme/contrast.ts, src/theme/contrast.test.ts</files>
  <read_first>
    - 23-RESEARCH.md Contrast validation code example (lines ~356-370) — the WCAG relative-luminance formula
    - src/components/contact-card-ring.ts (RN-free node-testable pure-module header idiom to mirror)
    - 23-UI-SPEC.md Color -> Validation gate (AA-equivalent thresholds)
  </read_first>
  <behavior>
    - contrastRatio(a,b) is symmetric and returns 1.0 for identical colours; a malformed/empty hex is rejected (throws), never silently passes.
    - AA_NORMAL=4.5, AA_LARGE=3.0 exported.
    - A pair approaching ratio 1.0 fails the AA threshold.
  </behavior>
  <action>
    Create `src/theme/contrast.ts` — a pure, RN-free module implementing the WCAG relative-luminance channel + luminance + `contrastRatio(a,b)` (larger luminance as numerator, so it is argument-order-symmetric) plus exported `AA_NORMAL`/`AA_LARGE` thresholds. Validate the hex input shape and reject malformed/empty strings. Write `contrast.test.ts` covering symmetry, identity=1.0, malformed-input rejection, and a known reference ratio.
  </action>
  <verify>
    <automated>npx vitest run src/theme/contrast.test.ts</automated>
    <automated>npm run check:colors src/theme/contrast.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/contrast.test.ts` passes including symmetry, identity=1.0 (fails AA), and malformed-input rejection.
    - contrast.ts imports nothing from react-native.
    - `npm run check:colors src/theme/contrast.ts` exits 0 (test-only reference hexes live under /theme/).
  </acceptance_criteria>
  <done>A pure, node-tested WCAG contrast module with AA thresholds exists under src/theme/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the four palettes and make light required</name>
  <files>src/theme/theme-presets.ts, src/theme/theme-presets.test.ts, src/theme/theme-types.ts</files>
  <read_first>
    - src/theme/theme-presets.ts (galaxy = the former space-dark dark palette from Plan 01; the full ThemePalette token set incl. status/gravity/star/muted tokens)
    - src/theme/theme-types.ts (ThemePackage axis + ThemePreset.light optional from Plan 01)
    - src/theme/theme-presets.test.ts (package-axis tests from Plan 01)
    - 23-UI-SPEC.md Color + Surface & Glass (Galaxy glass-forward vs Standard flatter; the 60/30/10 role structure repeated per palette with retuned luminance/saturation)
    - docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md §B, §F, §L (identities + status hue families stable across themes)
  </read_first>
  <behavior>
    - THEME_PRESETS.galaxy and .standard each define dark AND light full ThemePalette token sets (all existing tokens: background/surface/.../status*/gravityTiers/starPalette/muted*/rogueExtinguished).
    - Status hue FAMILIES stay recognizable across all four (Stable/Wobble/Decay/Rogue) with per-palette luminance/saturation retuned for contrast.
    - resolvePalette(package,mode) returns four distinct palettes; ThemePreset.light is now required (dark fallback path removed).
  </behavior>
  <action>
    In `src/theme/theme-presets.ts` author the light palette for galaxy and the full dark+light palettes for standard, keeping the galaxy dark palette (from Plan 01) as the reference; Galaxy stays deep-space/glass-forward, Standard calmer/flatter, both preserving the full token set and stable status hue families. In `theme-types.ts` make `ThemePreset.light` REQUIRED. Extend `theme-presets.test.ts` to assert all four combos resolve to distinct, complete palettes and that light is no longer a fallback. Keep every hex inside theme-presets.ts.
  </action>
  <verify>
    <automated>npx vitest run src/theme/theme-presets.test.ts</automated>
    <automated>npm run check:colors src/theme/theme-presets.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/theme-presets.test.ts` passes asserting four distinct complete palettes and required light.
    - ThemePreset.light is required (tsc fails if a preset omits light).
    - Every ThemePalette token present in all four palettes; no missing token.
    - `npm run check:colors src/theme/theme-presets.ts` exits 0.
  </acceptance_criteria>
  <done>Four complete palettes are authored, light is required, and the resolver returns four distinct palettes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Curated accent system + per-combo AA gate + provider overlay</name>
  <files>src/theme/accents.ts, src/theme/accents.test.ts, src/theme/theme-provider.tsx</files>
  <read_first>
    - 23-UI-SPEC.md Curated accent palette table (~8 ids/hexes) + the validation gate
    - src/db/app-settings-dao.ts (galaxy_accent/standard_accent nullable accent-id columns from Plan 01; assertAccentId)
    - src/theme/theme-provider.tsx (Plan 01 reads the accent column; overlay resolution wires here)
    - src/theme/contrast.ts (Task 1 gate)
    - src/db/app-settings-dao.ts self_sun_colour NULL-resolve-at-render precedent
  </read_first>
  <behavior>
    - ACCENTS is a fixed curated set of {id, hex, onAccent}; resolveAccent(id|null, package) returns the id's hex or the package default when null.
    - The AA gate test runs every curated accent (as text-link on each background AND its onAccent foreground) plus status + text tokens across all four palettes and fails any accent missing AA in any combo.
    - The provider overlays the resolved active-package accent hex onto palette.accent; switching package restores that package's stored accent (or default).
  </behavior>
  <action>
    Create `src/theme/accents.ts` — the curated ACCENTS set ({id, hex, onAccent}), the per-package default accent ids (Galaxy Nebula Blue, Standard Slate Indigo), and `resolveAccent(id | null, package)`. Wire `theme-provider.tsx` to overlay the active package's resolved accent hex onto `palette.accent` at render (reading the accent-id column via the store; NULL -> package default). Write `accents.test.ts` running the per-combo AA gate over all four palettes (accents as link + onAccent foreground, plus status/text on surface) using contrast.ts, failing/dropping any accent that misses AA in any combo. Keep all accent hexes inside accents.ts.
  </action>
  <verify>
    <automated>npx vitest run src/theme/accents.test.ts</automated>
    <automated>npm run check:colors src/theme/accents.ts src/theme/theme-provider.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/accents.test.ts` passes; the AA gate covers all curated accents across all four palettes and asserts each shipped accent meets AA (a failing accent is removed from the set, not kept).
    - resolveAccent(null, package) returns the package default; a known id returns its hex.
    - Provider overlays the resolved accent onto palette.accent (grep/tsc confirms accent-id -> hex resolution in the provider, not a hex in the DAO).
    - `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A curated accent set separate from the star colour resolves per package (NULL->default) and every shipped accent passes AA in all four combos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored accent-id -> render | An invalid/unknown accent-id must resolve to a safe default, not crash |
| palette data -> contrast gate | AA gate is a build/test-time check, no runtime cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Tampering | an unknown/tampered accent-id in app_settings | low | mitigate | assertAccentId (Plan 01) rejects unknown ids on write; resolveAccent falls back to the package default for a null/unknown id at render |
| T-23-11 | (accessibility invariant) | an accent that fails AA slipping into the shipped set | medium | mitigate | Per-combo AA gate test fails the build for any accent missing AA in any of the four palettes |
| T-23-S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | n/a | accept | Pure data + pure functions; local-first, no network, no auth surface |
</threat_model>

<verification>
- `npx vitest run src/theme` green (contrast, presets, accents); `npm run check:colors` and `npx tsc --noEmit` clean.
- Device UAT (end-of-phase): visually confirm all four combos and the accent picker preview live-restyle.
</verification>

<success_criteria>
Four complete palettes are authored, a curated accent set (separate from the star colour) resolves per package with NULL->default, and every shipped accent passes AA-equivalent contrast in all four theme/mode combos.
</success_criteria>

<output>
Create `.planning/phases/23-theme-visual-system/23-03-SUMMARY.md` when done.
</output>
