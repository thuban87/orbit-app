---
phase: 23-theme-visual-system
plan: 05
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - src/components/icons/icon-registry.ts
  - src/components/icons/icon-registry.test.ts
  - src/components/icons/Icon.tsx
  - src/components/icons/StatusGlyph.tsx
  - src/components/contact-card-ring.ts
  - src/components/contact-card-ring.test.ts
  - src/theme/tokens/icon-size.ts
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [THEME-08, THEME-09]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "THEME-09 / D-05 / dossier §M: all icons draw through ONE centralized semantic registry (icon-registry.ts) mapping semantic names (close, settings, favorite, search, back, add, message, call, edit, ...) to a base family (Ionicons via @expo/vector-icons) with outline/filled state variants; screens import semantic names only, never a third-party icon name directly. There is never a second icon source (D-05)."
    - "THEME-09 / D-06: the Icon component resolves colour ONLY through useTheme().colors[tone] and size through an iconSize token set (16/20/24/28) — no hardcoded colour, no hardcoded pixel size. The base family swaps at the registry mapping only (deferred custom Orbit family; do NOT build it now)."
    - "THEME-09: the ad-hoc RootNavigator TAB_GLYPHS map (⌂/◎/↥/⚙) is retired — the tab bar renders through the registry (highest-visibility fork retired). In-form raw ✕ conversions are left to consuming phases (planner scope call)."
    - "THEME-09 registry completeness (REVIEWS 23-05 MEDIUM + LOW): ICON_REGISTRY explicitly reserves and tests (a) a semantic name for each of the FOUR tab identities with the EXPLICIT semantic-name -> real route-key mapping (REVIEWS 23-05 LOW — verified on disk, RootNavigator.tsx:42-47,172-197 + types.ts:187-192): dashboard -> DashboardTab, orrery -> OrreryTab, backup -> BackupTab, settings -> SettingsTab (the TabParamList keys are the `*Tab`-suffixed forms, NOT the bare semantic words) so retiring TAB_GLYPHS cannot leave a tab route without an icon; (b) the semantic glyph names StatusGlyph needs for all six display states; and (c) a `warning` glyph name required by Plan 07's Destructive button/ConfirmDialog. A test asserts every TabParamList key (DashboardTab/OrreryTab/BackupTab/SettingsTab) resolves — via that mapping — to a registered outline+filled entry, and likewise every StatusGlyph state and the warning name."
    - "THEME-08 / D-05 / dossier §L: status carries a DISTINCT silhouette glyph per state, not a recoloured copy of one icon — extend the existing single source (ringVisual in contact-card-ring.ts) with a parallel pure statusGlyph(state) -> semantic glyph id in the SAME module so card/profile/orrery/widget read ONE glyph+hue source. Never fork a second status source."
    - "THEME-08 status type (REVIEWS 23-05 MEDIUM — verified): the query-time `ProfileStatus` union is only stable|wobble|decay|rogue (contact-status-read.ts:19-20) and has NO `snoozed` member; snooze is an INDEPENDENT condition (contacts.snooze_until, a filter/UI state, not a query-time status). So statusGlyph does NOT take `ProfileStatus | null` — it takes a NEW display union `StatusDisplayState = ProfileStatus | 'snoozed' | null` that the consumer composes from the computed status + the independent snooze state. This keeps ringVisual's `ProfileStatus | null` contract unchanged while giving the six-state glyph set a correct input type."
    - "THEME-08/09 adoption is deferred by design (REVIEWS scope note, planning-notes:78-81): this plan delivers the icon registry + statusGlyph SOURCE + the StatusGlyph/Icon primitives and retires the TAB_GLYPHS fork (a real in-phase consumer). Wiring StatusGlyph into ContactCard/Profile/Orrery and equivalent widget glyph semantics is a renderer-phase/Phase-15 adoption task, not a gap — THEME-08 is delivered this phase as the single source + primitive, verified at the primitive level (node tests + greyscale device check of the primitive), not via full screen adoption."
    - "THEME-08 / D-06: StatusGlyph renders the silhouette with colour via theme tokens (statusStable/statusWobble/statusDecay/rogue/border), never a hex; state stays readable WITHOUT colour via the distinct glyph + the existing border weight + a text/accessibility label (redundant CVD channels)."
    - { statement: "THEME-08 (partial, UI-SPEC UI Considerations 'status readable without colour', backstop): Stable/Wobble/Decay/Rogue/Neutral/Snoozed each carry a distinct silhouette glyph + border weight + text label; verified the glyphs are distinguishable with colour removed.", verification: backstop }
    - { statement: "THEME-08 (empty edge): statusGlyph(null) -> the neutral 'no status yet' glyph (matching ringVisual's null neutral ring); a never-evaluated contact never gets a real-status glyph.", verification: explicit }
    - { statement: "THEME-08 (adjacency edge): each of the six display states (stable/wobble/decay/rogue/null-neutral/snoozed, i.e. StatusDisplayState) maps to a DISTINCT glyph id — no two states share a silhouette, so states never visually merge.", verification: explicit }
    - { statement: "THEME-08 (ordering edge): statusGlyph is a pure total function of the StatusDisplayState value alone (order-independent, deterministic); the same state always yields the same glyph id.", verification: explicit }
    - { statement: "THEME-09 (empty edge): the Icon component requires a registered semantic name (typed IconName union), so an unregistered/empty name fails tsc rather than rendering a blank; every semantic name in the registry maps to a defined Ionicons glyph for both outline and filled.", verification: explicit }
    - { statement: "THEME-09 (adjacency edge): distinct semantic names may legitimately map to the same base glyph (e.g. close outline==filled) without collision — the registry key is the semantic identity, not the glyph.", verification: explicit }
    - { statement: "THEME-09 (ordering edge): registry lookup is a deterministic keyed map (no order dependence); resolving name+state always yields the same glyph.", verification: explicit }
  artifacts:
    - "src/components/icons/icon-registry.ts (+ .test.ts) — ICON_REGISTRY (semantic name -> {outline, filled}) + IconName type; reserves the four tab names, the six status-glyph names, and a `warning` name (Plan 07)"
    - "src/components/icons/Icon.tsx — <Icon name state size tone/> resolving colour via useTheme() and size via iconSize tokens"
    - "src/components/icons/StatusGlyph.tsx — silhouette per StatusDisplayState, colour via status tokens, accessibilityLabel naming the state"
    - "src/components/contact-card-ring.ts (+ .test.ts) — StatusDisplayState union (ProfileStatus | 'snoozed' | null) + parallel pure statusGlyph(state) -> semantic glyph id beside ringVisual (extend, not fork; ringVisual's ProfileStatus|null contract unchanged)"
    - "src/theme/tokens/icon-size.ts — ICON_SIZE token set (16/20/24/28)"
    - "src/navigation/RootNavigator.tsx — tab bar routed through the registry (TAB_GLYPHS retired)"
  key_links:
    - "screens -> Icon (semantic name) -> ICON_REGISTRY -> Ionicons; colour via useTheme(), size via ICON_SIZE"
    - "StatusDisplayState (computed status + independent snooze) -> statusGlyph() (same module as ringVisual) -> StatusGlyph -> card/profile/orrery one glyph+hue source"
    - "RootNavigator tab bar -> Icon registry (TAB_GLYPHS fork retired)"
  prohibitions:
    - "Do NOT let any screen import a third-party icon name directly (dossier §M exported constraint [theme -> icon usage]) — semantic names only, through the registry."
    - "Do NOT create a second icon source or a second status glyph/hue source (D-05) — extend ringVisual's module for glyphs; one registry for icons."
    - "Do NOT hardcode a colour or a pixel size in Icon/StatusGlyph — colour via useTheme() tokens (D-06), size via iconSize tokens."
    - "Do NOT build the custom Orbit icon FAMILY (deferred/out of scope) — only the registry seam that enables a later swap."
    - "Do NOT convey status by colour alone — the distinct glyph + border weight + text/accessibility label are the required redundant channels."
    - "Do NOT wire the icon/status system into the headless widget bitmap (widget-colors.ts) — the widget has no theme provider (contact-card-ring.ts:15-17 note)."
---

<objective>
Deliver the two centralized visual seams every later renderer phase depends on: a semantic icon registry (state variants, token colour, swappable base family) and distinct status glyphs that make relationship state readable without colour — both extending single sources, never forking.

Purpose: satisfy THEME-09 (icon registry) and THEME-08 (status glyphs) as first-class deliverables (D-05) before renderer phases consume them.
Output: icon-registry + Icon + iconSize tokens (tab bar routed through it) and statusGlyph + StatusGlyph extending contact-card-ring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
@.planning/phases/23-theme-visual-system/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic icon registry + Icon component + iconSize tokens; retire TAB_GLYPHS</name>
  <files>src/components/icons/icon-registry.ts, src/components/icons/icon-registry.test.ts, src/components/icons/Icon.tsx, src/theme/tokens/icon-size.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - 23-RESEARCH.md Pattern 4 (lines ~237-265) — ICON_REGISTRY + Icon.tsx code and the semantic-name constraint
    - src/navigation/RootNavigator.tsx:42-47 (the TAB_GLYPHS map to retire — keyed by the REAL route keys DashboardTab/OrreryTab/BackupTab/SettingsTab, NOT the bare words) and :172-197 (the four `<Tab.Screen name="DashboardTab"...>` registrations). The real TabParamList keys are `DashboardTab`/`OrreryTab`/`BackupTab`/`SettingsTab` (verified in src/navigation/types.ts:187-192). Registry semantic names map to route keys as: dashboard->DashboardTab, orrery->OrreryTab, backup->BackupTab, settings->SettingsTab (REVIEWS 23-05 LOW)
    - src/theme/theme-provider.tsx:50 — useTheme() (the only sanctioned colour source)
    - package.json — confirm @expo/vector-icons installed (Plan 02)
    - src/theme/tokens/ (token-file idiom from Plan 02)
  </read_first>
  <behavior>
    - ICON_REGISTRY maps each semantic name to {outline, filled} Ionicons glyphs; IconName is the keyof union so an unregistered name fails tsc.
    - The registry explicitly includes: a name for each of the four tab identities with the explicit semantic-name -> route-key mapping (dashboard->DashboardTab, orrery->OrreryTab, backup->BackupTab, settings->SettingsTab — the real TabParamList keys are `*Tab`-suffixed, REVIEWS 23-05 LOW), the six status-glyph names StatusGlyph needs (Task 2), and a `warning` name (Plan 07).
    - Icon resolves colour from useTheme().colors[tone] and size from ICON_SIZE (16/20/24/28); state 'active' -> filled, else outline.
    - The tab bar renders its four tabs through Icon (registry), with the previous TAB_GLYPHS map removed.
  </behavior>
  <action>
    Create `src/theme/tokens/icon-size.ts` (`ICON_SIZE` 16/20/24/28). Create `src/components/icons/icon-registry.ts` (`ICON_REGISTRY` semantic name -> {outline, filled} Ionicons pairs; export `IconName`) reserving the four tab names, the six status-glyph names (Task 2), and a `warning` name (Plan 07). Create `src/components/icons/Icon.tsx` (`<Icon name state size tone/>` resolving colour via `useTheme().colors[tone]` and size via `ICON_SIZE`, mapping state active->filled else outline). Route `RootNavigator.tsx` tab icons through `<Icon>` — resolve each `route.name` (the real key DashboardTab/OrreryTab/BackupTab/SettingsTab) to its registry semantic name via the explicit mapping (dashboard->DashboardTab, orrery->OrreryTab, backup->BackupTab, settings->SettingsTab) so no route is left without an icon — and delete the `TAB_GLYPHS` map. Write `icon-registry.test.ts` asserting every semantic name has a defined outline+filled glyph, the IconName union shape, that EACH of the four TabParamList keys (DashboardTab/OrreryTab/BackupTab/SettingsTab) resolves through that mapping to a registered icon (REVIEWS 23-05 LOW), and that the six status-glyph names + `warning` are present (REVIEWS 23-05 MEDIUM). Leave in-form raw ✕ conversions to consuming phases.
  </action>
  <verify>
    <automated>npx vitest run src/components/icons/icon-registry.test.ts</automated>
    <automated>npm run check:colors src/components/icons src/navigation/RootNavigator.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/icons/icon-registry.test.ts` passes; every registry name resolves a defined outline and filled glyph; each of the four TabParamList keys (DashboardTab/OrreryTab/BackupTab/SettingsTab) resolves — via the explicit dashboard->DashboardTab / orrery->OrreryTab / backup->BackupTab / settings->SettingsTab mapping — to a registered `<Icon>` name; the six status-glyph names and the `warning` name are registered.
    - `grep -n "TAB_GLYPHS" src/navigation/RootNavigator.tsx` returns nothing (fork retired); the tab bar renders through Icon.
    - Icon resolves colour via useTheme() and size via ICON_SIZE (grep confirms no hardcoded colour/size).
    - `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A semantic icon registry with state variants + token colour/size exists, the tab bar renders through it, and the ad-hoc TAB_GLYPHS fork is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Status glyphs extending ringVisual (one glyph+hue source)</name>
  <files>src/components/contact-card-ring.ts, src/components/contact-card-ring.test.ts, src/components/icons/StatusGlyph.tsx</files>
  <read_first>
    - src/components/contact-card-ring.ts:44-61 — ringVisual (extend, do NOT fork); its `ProfileStatus | null` input (unchanged) and the border-weight channel
    - src/db/contact-status-read.ts:19-23 — `ProfileStatus = stable|wobble|decay|rogue` (NO snoozed member) — the reason a new StatusDisplayState union is required
    - src/db/dashboard-read.ts (how snooze is applied as an independent condition, e.g. snooze_until — NOT a query-time status)
    - src/components/contact-card-ring.test.ts — ringVisual test to extend
    - 23-UI-SPEC.md Status Glyphs table (silhouette per status + snoozed) and the readable-without-colour rule
    - src/components/icons/Icon.tsx + icon-registry.ts (Task 1 — StatusGlyph renders silhouettes via the registry; the six status-glyph names reserved there)
    - src/theme/theme-types.ts — status colour tokens (statusStable/statusWobble/statusDecay/rogue/border)
  </read_first>
  <behavior>
    - A new display union `StatusDisplayState = ProfileStatus | 'snoozed' | null` is exported from the ringVisual module; snooze is composed by the consumer from the independent snooze condition, NOT read from ProfileStatus (REVIEWS 23-05 MEDIUM). ringVisual keeps its `ProfileStatus | null` signature unchanged.
    - statusGlyph(state: StatusDisplayState) is a pure total function returning a distinct semantic glyph id per state: stable/wobble/decay/rogue/null-neutral/snoozed — no two states share a silhouette; null -> neutral; the six ids are registered names from Task 1.
    - StatusGlyph renders the silhouette with colour via the status theme token and carries an accessibilityLabel naming the state (Stable/Wobbling/Decaying/Rogue/Not yet contacted/Snoozed).
    - ringVisual is unchanged (colour+opacity+width contract intact).
  </behavior>
  <action>
    In `src/components/contact-card-ring.ts` (the SAME module as `ringVisual`, extend not fork) export a `StatusDisplayState = ProfileStatus | 'snoozed' | null` union and a pure `statusGlyph(state: StatusDisplayState)` mapping to six distinct registered glyph ids (stable/wobble/decay/rogue/null-neutral/snoozed); leave ringVisual's `ProfileStatus | null` signature and behavior untouched. Create `src/components/icons/StatusGlyph.tsx` rendering the silhouette through the icon registry with colour resolved from the status theme token (never a hex) and an accessibilityLabel naming the state; its prop is a StatusDisplayState. Extend `contact-card-ring.test.ts` to assert statusGlyph is total over StatusDisplayState, distinct-per-state, null->neutral, and that snoozed maps to its own glyph distinct from all four ProfileStatus glyphs and neutral. Do not touch the widget bitmap path.
  </action>
  <verify>
    <automated>npx vitest run src/components/contact-card-ring.test.ts</automated>
    <automated>npm run check:colors src/components/icons/StatusGlyph.tsx src/components/contact-card-ring.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On a Pixel with a colour filter / greyscale applied (device-UAT, end-of-phase): the six status states remain distinguishable by silhouette + border weight + label alone.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/contact-card-ring.test.ts` passes asserting statusGlyph totality over StatusDisplayState, six distinct glyph ids, null->neutral, and snoozed distinct from every ProfileStatus glyph.
    - `StatusDisplayState` is exported and `statusGlyph` takes it (not `ProfileStatus`); ringVisual's existing `ProfileStatus | null` contract/test is unchanged.
    - StatusGlyph colour comes only from status theme tokens (grep confirms no hex); accessibilityLabel names the state.
    - `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Distinct per-status silhouette glyphs extend ringVisual's single source, render with token colour, and keep status readable without colour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status value -> glyph/hue render | An unknown status must resolve to a safe neutral, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08 | Tampering | an unexpected status value reaching statusGlyph | low | mitigate | statusGlyph is a total function over StatusDisplayState with a null/default -> neutral branch; IconName union makes an unregistered icon name a tsc error; the six status names + warning + four tab names are reserved and tested so a missing registry entry fails at test/tsc, not at runtime |
| T-23-11b | (accessibility invariant) | status conveyed by colour alone | medium | mitigate | Distinct silhouette + border weight + accessibilityLabel are redundant non-colour channels (verified by the greyscale device check) |
| T-23-S/R/I/D/E | Spoofing/Repudiation/Info/DoS/Elevation | — | n/a | accept | Pure lookup + presentational render; local-first, no network, no auth surface |
</threat_model>

<verification>
- `npx vitest run src/components/icons src/components/contact-card-ring.test.ts` green; check:colors + tsc clean.
- Device UAT (end-of-phase): tab bar icons render through the registry; status glyphs distinguishable under greyscale.
</verification>

<success_criteria>
One semantic icon registry (state variants, token colour/size, swappable base family, four tab names + six status names + a warning name reserved) powers the tab bar, and distinct status glyphs over StatusDisplayState extend ringVisual to make relationship state readable without colour — THEME-09 and THEME-08 delivered as first-class seams (screen adoption of StatusGlyph deferred to renderer/Phase-15).
</success_criteria>

## Artifacts this phase produces

New files: `src/components/icons/icon-registry.ts` (+ `.test.ts`), `src/components/icons/Icon.tsx`, `src/components/icons/StatusGlyph.tsx`, `src/theme/tokens/icon-size.ts`.
New exported symbols: `ICON_REGISTRY`, `IconName` type, `Icon` component, `ICON_SIZE` (16/20/24/28), `StatusGlyph` component; and in `contact-card-ring.ts`: `StatusDisplayState` union (`ProfileStatus | 'snoozed' | null`) + `statusGlyph(state)` (added beside the unchanged `ringVisual`).
Changed files: `src/navigation/RootNavigator.tsx` (TAB_GLYPHS removed; tabs render through `<Icon>`).

<output>
Create `.planning/phases/23-theme-visual-system/23-05-SUMMARY.md` when done.
</output>
