---
phase: 23-theme-visual-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/015-theme-settings.ts
  - src/db/migrations/015-theme-settings.test.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/database.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/backup/backup-schema.ts
  - src/backup/export-manifest.test.ts
  - src/services/widget/widget-colors.test.ts
  - src/theme/theme-types.ts
  - src/theme/theme-presets.ts
  - src/theme/theme-presets.test.ts
  - src/theme/theme-provider.tsx
  - src/theme/orbit-theme-migration.ts
  - src/theme/orbit-theme-migration.test.ts
  - src/stores/theme-store.ts
  - App.tsx
autonomous: false
requirements: [THEME-01, THEME-03, THEME-13]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "THEME-13 / D-08 / D-03: theme package, per-package appearance mode, per-package accent, and per-package background are durable columns on the single `app_settings` row (migration 015, head+1 VERIFIED on disk this plan per D-03: TARGET_VERSION=14, latest migration 014, so 015 — never assumed) — never AsyncStorage. The old AsyncStorage `orbit-theme` key stops being the source of truth. Per D-01/D-02 the dossier and planning-notes are ground truth for these decisions (do not reverse a [DECIDED]/[REJECTED] item)."
    - "THEME-03 / D-11: switching theme package restores THAT package's last-used mode/accent/background — per-package memory columns (galaxy_* / standard_*) are the storage; this plan lands the columns + store selectors + provider plumbing (mode proven end-to-end here; accent/background DATA in Plans 03/06)."
    - "THEME-13 / D-09 (allowlist NOW, serialize LATER — forward-safe, REVIEWS 23-01 HIGH resolution): the 7 new theme keys are added to the `PORTABLE_SETTINGS_KEYS` allowlist in `src/backup/backup-schema.ts` NOW (D-09) so a future format-4 backup that carries them is accepted by `assertPortableSettings`; AND the 7 keys are declared **OPTIONAL (`?:`)** in the `PortableSettingsSnapshot` TYPE (so they are writable via `AppSettingsPatch` and restorable, while a `getPortableSettingsSnapshot` return object that omits them still satisfies `tsc --noEmit` — declaring them REQUIRED and not emitting them would fail typecheck) — but their EMISSION in the `getPortableSettingsSnapshot` SELECT/return is DEFERRED to Phase 36's format-4 plan. NOTE: `phoneRegionOverride` is NO LONGER the live precedent — 18.1-04 made it REQUIRED and fully emitted (app-settings-dao.ts:154 required, :444 emitted); the deferred-emission form to copy is the OPTIONAL declaration it carried at the deferred-emission commit (69bb048), not its current shape. Net effect: this build's exports stay byte-identical to the current format-3 wire shape, so a pre-Phase-23 build can still restore them — no silent wire-shape change."
    - "THEME-13 (backup forward-safety mechanism, REVIEWS 23-01 HIGH): `export-manifest.ts:63,74` spreads `...portable` from `getPortableSettingsSnapshot` UNCONDITIONALLY into `manifest.appSettings`, so the ONLY safe way to keep the 7 columns off the format-3 wire is to not emit them from that snapshot function this phase. A regression test asserts a format-3 export's `appSettings` contains none of the 7 theme keys. Because they ARE in the allowlist + `AppSettingsPatch`, restore-apply's settings-patch path (restore-apply.ts:272) can WRITE them the moment Phase 36 begins emitting them — no data model change needed then."
    - "THEME-01 / D-10: theme package (galaxy|standard) and appearance mode (light|dark|system) are independent axes; `resolvePalette(package, resolvedMode)` selects one of the four palette slots deterministically, and `system` resolves live at the provider boundary through the existing `useColorScheme()` -> pure `resolveMode` path (no manual Appearance listener)."
    - "THEME-01 (empty edge): on a fresh install with no persisted value, migration 015 seeds theme_package='galaxy' and galaxy_mode/standard_mode='system' as NOT NULL DEFAULTs, so first launch is Galaxy + Follow System with no code branch required; `orbit-theme-migration` maps an ABSENT AsyncStorage `orbit-theme` key to no-op (columns keep their seeded defaults)."
    - "THEME-01 (adjacency edge): Follow-System at the exact OS light/dark boundary is deterministic — `resolveMode('system', scheme)` maps ANY scheme other than literal 'light' (including 'dark', 'unspecified', null, undefined) to 'dark'; there is no ambiguous/merge state at the boundary (existing verified contract, theme-presets.ts:114-123)."
    - "THEME-01 (ordering edge): the two axes are orthogonal and order-independent — setting package then mode, or mode then package, resolves to the same palette; per-package memory means switching package restores that package's own stored mode/accent/background regardless of the order changes were made in."
    - "THEME-03 / D-10: theme-critical prefs are read from `app_settings` INSIDE App.tsx's existing `openAndMigrate` `ready` gate (before the navigator mounts), then used to hydrate the theme store, so the first painted frame already carries the persisted palette — no wrong-theme flash. A short neutral/default-palette splash before `ready` is acceptable; a flash of the WRONG saved palette is not."
    - "THEME-03: the theme store's source of truth becomes the `app_settings` read (hydrated at boot); the provider re-renders the tree from store state so a later live change restyles the whole app (live-preview plumbing established here; accent/background DATA authored in Plans 03/06)."
    - { statement: "THEME-13 (idempotency edge): migration 015 is purely additive (ALTER TABLE app_settings ADD COLUMN ... NOT NULL DEFAULT / nullable), forward-only, and re-runnable-safe under the PRAGMA user_version runner — a device jumping v0->v15 in one launch lands fully seeded; migrations 001-014 are never edited.", verification: explicit }
    - { statement: "THEME-13 (concurrency/interrupted-launch edge): the boot sequence stays crash-safe if interrupted — the theme read is folded into the SAME openAndMigrate ready gate that already guarantees no read path sees a half-built DB, and the `orbit-theme` one-time import runs after openAndMigrate resolves; an interrupted launch either has the columns (migration committed) or does not (rolled back), never a partial theme column set.", verification: explicit }
    - { statement: "THEME-13 (idempotent-import edge, REVIEWS 23-01 HIGH — compare-before-write): the one-time orbit-theme import is idempotent because it DIFFS the mapped legacy values against the current committed settings (getAppSettings) and calls updateAppSettings ONLY with the non-empty diff — NOT merely 'if the mapped patch is non-empty'. `updateAppSettings` unconditionally bumps modified_at AND data_revision inside its transaction (app-settings-dao.ts:606-617; docstring :598-604 confirms even an empty patch bumps modified_at), so gating on the mapped-blob emptiness alone would spuriously advance the settings revision every boot after a FAILED AsyncStorage clear (the mapped legacy blob is always non-empty). With compare-before-write, a repeat import when the DB already equals the mapped values diffs to EMPTY, performs ZERO writes, and leaves modified_at/data_revision unchanged — so a failed clear can never corrupt backup last-writer-wins.", verification: explicit }
    - { statement: "THEME-03 (loading, UI-SPEC UI Considerations 'theme restore before first paint', covered): theme-critical prefs resolve from app_settings before the main UI renders; first paint is gated on that read so there is no wrong-theme flash. Device-observable on a Pixel cold start.", verification: backstop }
    - { statement: "the migrated orbit-theme AsyncStorage payload is treated as untrusted AND is a zustand persist ENVELOPE, not a flat object (REVIEWS 23-01 MEDIUM — verified on disk: theme-store.ts persists via zustand `persist` + createJSONStorage(() => AsyncStorage) under name:'orbit-theme', version:1, partialize -> {mode,presetId}, so the AsyncStorage value is the envelope {\"state\":{\"mode\":\"dark\",\"presetId\":\"space-dark\"},\"version\":1}). orbit-theme-migration unwraps parsed.state (tolerating the version field and any unknown version; a missing/absent .state is a no-op; a bare flat {mode,presetId} blob is tolerated defensively), reads state.mode / state.presetId, validates them against the known enums, and maps presetId 'space-dark' -> package 'galaxy'; any unknown/malformed/absent value falls back to the seeded defaults rather than writing a bad column value. Reading the envelope's TOP-LEVEL .mode/.presetId (the pre-fix specification) yields undefined on both and silently no-ops on every real upgraded device while a flat-shape unit test passes false-green.", verification: explicit }
  artifacts:
    - "src/db/migrations/015-theme-settings.ts (+ .test.ts) — additive app_settings columns: theme_package (NOT NULL DEFAULT 'galaxy' CHECK galaxy|standard), galaxy_mode/standard_mode (NOT NULL DEFAULT 'system' CHECK light|dark|system), galaxy_accent/standard_accent (nullable TEXT accent-id, NULL=package default at render), galaxy_background/standard_background (nullable TEXT slot-id, NULL=package default at render)"
    - "src/db/database.ts — TARGET_VERSION bumped 14 -> 15; migration015 registered in MIGRATIONS"
    - "src/db/app-settings-dao.ts — AppSettings + AppSettingsRow gain themePackage/galaxyMode/standardMode/galaxyAccent/standardAccent/galaxyBackground/standardBackground; the 7 keys are ALSO declared in the PortableSettingsSnapshot TYPE (so they enter AppSettingsPatch → writable via updateAppSettings and restorable) but their EMISSION in getPortableSettingsSnapshot's SELECT/return is DEFERRED (phoneRegionOverride precedent, :153-154 — NOT added to the SELECT this phase); COLUMN_OF, WritableSettingsKey, getAppSettings mapping, validators (assertThemePackage/assertThemeMode/assertAccentId/assertBackgroundId) + *_FIELDS arrays + patch-loop wiring"
    - "src/backup/backup-schema.ts — the 7 new theme keys added to the PORTABLE_SETTINGS_KEYS ALLOWLIST only (accept-if-present); no BACKUP_FORMAT_VERSION bump, no FORWARD_MIGRATIONS entry (both are Phase 36 / owner scope)"
    - "src/backup/export-manifest.test.ts — regression test: a format-3 export's appSettings omits all 7 theme keys (the deferral guard)"
    - "src/services/widget/widget-colors.test.ts — stale 'space-dark' description/assertion updated for the space-dark→galaxy re-key; widget-colors.ts (ADR-042 headless consumer of DEFAULT_PRESET_ID/resolvePalette) compiles unchanged and still resolves the galaxy(=former space-dark) dark palette"
    - "src/theme/theme-types.ts — ThemePackage axis ('galaxy'|'standard'); store/provider types for package/mode/accent/background; ResolvedTheme carries the active package"
    - "src/theme/theme-presets.ts — THEME_PRESETS re-keyed by ThemePackage (galaxy = former space-dark dark palette; standard placeholder dark palette to prove the package axis); resolvePalette(package, mode)"
    - "src/theme/theme-provider.tsx — resolves (package, resolvedMode) -> palette; reads accent/background columns (applied by Plans 03/06)"
    - "src/theme/orbit-theme-migration.ts (+ .test.ts) — pure mapper from the zustand persist ENVELOPE {state:{mode,presetId},version} (unwrap parsed.state; tolerate version + a bare flat blob; absent/malformed -> no-op) to a column patch (one-time import); presetId 'space-dark' -> 'galaxy' (REVIEWS 23-01 MEDIUM — the payload is the envelope, NOT a flat {mode,presetId})"
    - "src/stores/theme-store.ts — source of truth reworked to app_settings-hydrated selection (package/mode/accent/background + setters); no new AsyncStorage prefs"
    - "New app_settings columns: theme_package, galaxy_mode, standard_mode, galaxy_accent, standard_accent, galaxy_background, standard_background"
    - "Exported: resolvePalette (package,mode signature), orbit-theme import mapper, ThemePackage type"
  key_links:
    - "App.tsx openAndMigrate ready gate -> getAppSettings() theme read -> theme store hydrate -> ThemeProvider first paint (restore-before-paint, no flash)"
    - "migration 015 columns -> app-settings-dao writer contract (COLUMN_OF/validators/patch loop) -> PORTABLE_SETTINGS_KEYS -> Phase 36 backup"
    - "AsyncStorage orbit-theme (device-local blob) -> orbit-theme-migration one-time import at boot -> app_settings columns -> AsyncStorage key cleared"
    - "theme-store selection -> ThemeProvider resolveMode + resolvePalette(package,mode) -> useTheme().colors across the tree"
  prohibitions:
    - "Do NOT store any new theme preference in AsyncStorage (D-08/D-09). New durable prefs are app_settings columns; the old orbit-theme AsyncStorage key is migrated once then cleared, never extended."
    - "Do NOT write a raw #RRGGBB hex as a migration-015 column DEFAULT. scripts/check-colors.sh scans src recursively and migrations live at src/db/migrations/ (NOT under a /theme/ path), so a hex default trips the gate. Store an accent-id or NULL (resolved to a palette hex at render, the self_sun_colour precedent), never a hex in the column."
    - "Do NOT edit any shipped migration 001-014. Migration 015 is additive-only; it is irreversible on users' devices, so it must land a v0->v15 device fully seeded."
    - "Do NOT omit any new theme key from the PORTABLE_SETTINGS_KEYS allowlist (D-09) — an omitted key would be rejected by assertPortableSettings when a future format-4 backup carries it."
    - "Do NOT emit the 7 theme keys from getPortableSettingsSnapshot this phase, and do NOT bump BACKUP_FORMAT_VERSION or add a FORWARD_MIGRATIONS entry (REVIEWS 23-01 HIGH / OWNER-BUCKET FIREWALL). Emitting them now changes the format-3 wire shape silently and makes a format-3 backup from this build hard-rejected by a pre-Phase-23 build (assertPortableSettings, backup-schema.ts:129-135) instead of getting the friendly update-first message. The format-4 bump is a DECIDED milestone-sequencing item owned by Phase 36 (D-03, planning-notes:73-74) — reversing that sequencing is stop-and-ask, not a planner/executor call."
    - "Do NOT reintroduce toISOString().split('T')[0] for any timestamp write; use formatLocalDate()/the injected deps.now (migration runner supplies it), matching 002-app-settings.ts."
    - "Do NOT reverse D-08/D-10 or any [DECIDED] dossier item as a 'simplification' — the app_settings storage model and independent-axes design are settled owner decisions; changing them is stop-and-ask."
---

<objective>
Prove the theme architecture end-to-end with the thinnest real vertical slice: a persisted theme selection in `app_settings` resolves through the provider to the app's on-screen background across the package × appearance-mode axes, restored BEFORE first paint so there is no wrong-theme flash — and the existing AsyncStorage `orbit-theme` value is migrated once into the new durable columns.

This is the phase's tracer. It authors the full, irreversible migration-015 schema (all seven theme columns) in ONE gated decision, threads those columns through the entire `app_settings` writer contract and the backup allowlist, re-keys the theme layer onto a package axis, and folds the theme read into `App.tsx`'s boot `ready` gate. Only the package + mode path is wired all the way to render here; accent/background DATA and the independent primitive subsystems expand from this proven slice in later plans without re-opening the migration, DAO, provider, store, or boot gate.

Purpose: front-load every shared-file and irreversible-schema edit into one plan so the architecture is proven on the agent's best early-context tokens and downstream plans stay parallel and additive.
Output: migration 015 + DAO/backup plumbing + package-axis theme layer + orbit-theme import + restore-before-paint gate, all green under node tests and check:colors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-PATTERNS.md
@docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-02-planning-notes.md
</context>

<assumption_delta_decision>
This phase FIRES the singular-theme assumption. Primary noun: the **theme identity** — theme package × appearance mode, with **per-package preference memory** (each package remembers its own mode, accent, background). Prior state assumed ONE dark-only preset (`space-dark`) and ONE AsyncStorage `orbit-theme` key holding only {mode, presetId}. This plan generalizes to TWO independent axes (package × mode = four palette slots) with per-package remembered accent/background/mode.

Decision: **PROMOTE.** The singular `orbit-theme` AsyncStorage key is retired to durable `app_settings` columns (theme_package + per-package galaxy_*/standard_* mode/accent/background) that are backup-portable; the old value is imported once at boot then the AsyncStorage key is cleared. Rationale: D-08 (theme prefs are durable settings that must live in app_settings and survive backup) and D-10 (independent axes, per-package memory, restore-before-paint). Companion invariant test: every package round-trips its own remembered accent/background/mode through the DAO write -> read path.
</assumption_delta_decision>

<deferrals_and_escalations>
## REVIEWS 23-01 HIGH — backup wire-shape (resolved in-scope; format-4 bump escalated)

**Finding (both reviewers, verified on disk):** `export-manifest.ts:63,74` spreads `...portable` from `getPortableSettingsSnapshot` unconditionally into `manifest.appSettings`. Adding the 7 theme fields to that snapshot would emit them on every export while `BACKUP_FORMAT_VERSION` stays `3` (`types.ts:14`) — a wire-shape change with no format bump, no `FORWARD_MIGRATIONS` entry, and a hard-reject (`backup-schema.ts:129-135`) when a format-3 backup from this build is restored on a pre-Phase-23 build.

**Resolution (in-scope, reverses NO decision):** land the columns + DAO validators + `COLUMN_OF` + `PortableSettingsSnapshot` TYPE fields + the `PORTABLE_SETTINGS_KEYS` allowlist entry NOW, but DEFER emitting the theme fields from `getPortableSettingsSnapshot` until Phase 36's format-4 plan owns them. This is the repo's own shipped pattern (`phoneRegionOverride`, `app-settings-dao.ts:153-154`). A regression test asserts a format-3 export omits the theme keys. Forward-safe both directions: old apps still read this build's format-3 backups; Phase 36 can begin emitting with the allowlist + patch path already in place.

**Escalated to owner / Phase 36 (do NOT do here):** bumping `BACKUP_FORMAT_VERSION` to 4 or adding a `FORWARD_MIGRATIONS[3]` entry. The milestone order schema→consumers→backup and the format-4 ownership by Phase 36's final plan are DECIDED (D-03, planning-notes:73-74). Reversing that sequencing to "fix" the HIGH early is a decision-reversal and is stop-and-ask. The HIGH is fully mitigated without it.
</deferrals_and_escalations>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the one-way migration-015 schema shape</name>
  <decision>Lock the migration-015 column shape and the orbit-theme import strategy — an irreversible on-device schema decision the rest of the phase inherits.</decision>
  <context>
    Migration 015 is forward-only and irreversible on users' devices (no remote repair). Every later plan (accents, backgrounds, per-package memory, backup v4 in Phase 36) reads these columns, so the shape is a one-way door. Two sub-decisions carry real cost if wrong: (a) how the accent is stored — a raw hex would trip `check:colors` (migrations are scanned, and are not under a /theme/ path) and would couple the palette to the schema; (b) how the existing AsyncStorage `orbit-theme` value is carried over. RESEARCH Open Q1/Q2 and the PATTERNS accent-storage trap both flag this for the planner. Head+1 verified on disk this plan: TARGET_VERSION=14, latest migration 014 -> new migration is 015.
  </context>
  <options>
    <option id="recommended">
      <name>Nullable accent-id + NOT-NULL-default enums; one-time import then clear</name>
      <pros>galaxy_accent/standard_accent are nullable TEXT holding an accent-ID (NULL -> package default resolved at render, the proven self_sun_colour idiom) so the accent palette stays swappable with no future data migration and no hex ever enters the migration; galaxy_background/standard_background nullable TEXT slot-id (NULL -> package default; 'none' explicit); theme_package + galaxy_mode/standard_mode are NOT NULL DEFAULT with CHECK enums so a v0->v15 device is fully seeded; orbit-theme is imported ONCE at boot (presetId 'space-dark' -> package 'galaxy', mode carried onto galaxy_mode) then the AsyncStorage key is cleared so a second import can never occur.</pros>
      <cons>Accent/background resolution must happen at render (never in the DAO), matching but slightly more indirect than a stored hex.</cons>
    </option>
    <option id="hex-default">
      <name>Store accent as a hex column default</name>
      <pros>Accent value readable directly from the row with no render-time resolve.</pros>
      <cons>A #RRGGBB DEFAULT in migration 015 TRIPS check:colors (migrations are scanned, not under /theme/); couples the palette to the schema so retuning an accent needs a data migration; contradicts the self_sun_colour precedent. Rejected on the merits — do not choose without owner override.</cons>
    </option>
  </options>
  <resume-signal>Select "recommended" (default) or "hex-default", or describe a different column shape.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end persisted theme resolves package × mode with restore-before-paint</name>
  <reversibility rating="one-way">Migration 015 adds columns every later plan and the Phase 36 backup read; changing the shape after it ships requires an on-device data migration that cannot be pushed to users.</reversibility>
  <files>src/db/migrations/015-theme-settings.ts, src/db/migrations/015-theme-settings.test.ts, src/db/migrations/full-chain.test.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/backup/backup-schema.ts, src/theme/theme-types.ts, src/theme/theme-presets.ts, src/theme/theme-presets.test.ts, src/theme/theme-provider.tsx, src/theme/orbit-theme-migration.ts, src/theme/orbit-theme-migration.test.ts, src/stores/theme-store.ts, App.tsx</files>
  <read_first>
    - src/db/migrations/014-interaction-assists.ts (additive ALTER idiom to copy exactly)
    - src/db/migrations/002-app-settings.ts (bound seed via injected deps.now)
    - src/db/migrations/runner.ts and src/db/database.ts:49 (TARGET_VERSION; MIGRATIONS registration; PRAGMA user_version contract)
    - src/db/migrations/full-chain.test.ts (chain test that must extend to 015)
    - src/db/app-settings-dao.ts:37-247 (AppSettings/PortableSettingsSnapshot/AppSettingsRow), :278-300 (COLUMN_OF), :316-349 (getAppSettings + NULL-passthrough), :479-596 (validators, *_FIELDS, patch loop), and the self_sun_colour precedent (nullable/validated/NULL-resolved-at-render)
    - src/backup/backup-schema.ts:106-139 (PORTABLE_SETTINGS_KEYS allowlist + assertPortableSettings + SECRET_SHAPED_KEY; note FORWARD_MIGRATIONS:19-79 and BACKUP_FORMAT_VERSION are NOT touched here)
    - src/backup/export-manifest.ts:41-84 (readManifest destructures `getPortableSettingsSnapshot` and spreads `...portable` into manifest.appSettings unconditionally — WHY the snapshot must not emit theme keys yet; add a negative test in export-manifest.test.ts)
    - src/backup/restore-apply.ts:249,261-272 (local getPortableSettingsSnapshot read for last-writer-wins modifiedAt; the appSettings patch loop that will WRITE theme keys once Phase 36 emits them — confirms the deferral is emit-only)
    - src/db/app-settings-dao.ts:134-163 (PortableSettingsSnapshot TYPE + AppSettingsPatch = Partial<Omit<PortableSettingsSnapshot,'modifiedAt'>>) and :387-456 (getPortableSettingsSnapshot SELECT/return). CAUTION: `phoneRegionOverride` is now REQUIRED (:154) and EMITTED (:444) — 18.1-04 landed its emission, so it is NOT a live deferred-emission example. The pattern to copy for the 7 theme keys is OPTIONAL type field + NO SELECT/return entry (the shape phoneRegionOverride had at deferred-emission commit 69bb048), which keeps `tsc --noEmit` green while the keys stay off the format-3 wire.
    - src/services/widget/widget-colors.ts:18,31-33 and src/services/widget/widget-colors.test.ts:9,47 (ADR-042 headless consumer: resolvePalette(DEFAULT_PRESET_ID,'dark'); the re-key must keep DEFAULT_PRESET_ID exported and value-safe; only the stale 'space-dark' test string changes)
    - src/theme/theme-types.ts (ThemePresetId, ThemePreset, ThemePalette, resolvers' type contract)
    - src/theme/theme-presets.ts:18-138 (THEME_PRESETS, resolveMode, resolvePalette)
    - src/theme/theme-provider.tsx:28-59 (useMemo resolution; useColorScheme live path; useTheme fallback)
    - src/stores/theme-store.ts (the orbit-theme AsyncStorage persist being retired) — CONFIRM ON DISK: zustand `persist` + createJSONStorage(() => AsyncStorage), name:'orbit-theme', version:1, partialize -> {mode,presetId} (theme-store.ts:25-48). Zustand v5 writes an ENVELOPE to AsyncStorage: {"state":{"mode":"dark","presetId":"space-dark"},"version":1} — NOT a flat {mode,presetId}. The one-time import mapper MUST read parsed.state.mode / parsed.state.presetId, not the top level (REVIEWS 23-01 MEDIUM)
    - App.tsx:129-161 (AppShell ready gate) and :292-298 (themed splash while pending)
    - scripts/check-colors.sh (exempts only ^[^:]*/theme/ ; migrations are scanned)
  </read_first>
  <behavior>
    - Migration 015 test: applying 015 on a v14 DB adds all seven columns with the seeded defaults; a fresh v0->v15 run lands theme_package='galaxy', galaxy_mode/standard_mode='system', accents/backgrounds NULL; full-chain test reaches version 15.
    - DAO round-trip test: updateAppSettings can set themePackage/galaxyMode/standardMode/galaxyAccent/standardAccent/galaxyBackground/standardBackground; getAppSettings reads them back typed; each package round-trips its OWN remembered mode/accent/background (assumption-delta invariant); an invalid enum/accent/background value throws before the UPDATE opens.
    - Backup deferral test (REVIEWS 23-01 HIGH): the 7 theme keys ARE present in PORTABLE_SETTINGS_KEYS (allowlist), and a built format-3 export manifest's `appSettings` contains NONE of the 7 theme keys (getPortableSettingsSnapshot does not emit them); a manifest that DOES carry a theme key parses without the "local-only or secret member" rejection (allowlist accepts it for the future format-4 path).
    - Boot import-order test (REVIEWS 23-01 MEDIUM): the one-time orbit-theme import runs AFTER openAndMigrate resolves, reads the current committed settings via getAppSettings, unwraps the zustand persist envelope (parsed.state) and validates/maps the legacy blob, computes the DIFF between the mapped legacy values and the current stored values (compare-before-write), calls updateAppSettings ONLY with that diff and ONLY when it is non-empty, then clears the AsyncStorage key ONLY after the DB write resolves.
    - Boot idempotency test (REVIEWS 23-01 HIGH — compare-before-write is the mechanism, not "patch non-empty"): because updateAppSettings ALWAYS bumps modified_at + data_revision (app-settings-dao.ts:606-617; docstring :598-604 — even an empty patch bumps modified_at), the guard MUST be the value diff, since the mapped legacy blob is always non-empty. Test: seed the seven columns to the mapped legacy values, leave the orbit-theme key PRESENT (simulating a failed clear), run the boot import again, and assert updateAppSettings is NOT called AND data_revision + modified_at are byte-identical before vs after (a second import performs ZERO writes).
    - Boot error-isolation test (REVIEWS 23-01 MEDIUM): the legacy getItem, JSON.parse, and removeItem are EACH individually non-fatal — a rejected/unavailable getItem, a corrupt/unparseable blob, or a failed removeItem is caught locally, logs, and yields "no import this launch" while keeping the seeded/committed DB settings; none of the three can reach AppShell's boot catch or block the ready gate. A write-failure at updateAppSettings leaves the AsyncStorage key intact for retry and does not partially write.
    - orbit-theme-migration test (REVIEWS 23-01 MEDIUM — REAL envelope fixture, NOT the flat shape): the fixture is the real zustand persist envelope {"state":{"mode":"dark","presetId":"space-dark"},"version":1} -> patch {themePackage:'galaxy', galaxyMode:'dark'} (mapping reads parsed.state, not the top level); an envelope carrying an UNKNOWN `version` still maps from `.state` (version is tolerated, not gated on); a malformed/unparseable or absent/null blob -> no-op/defaults; an envelope whose state has an unknown presetId/mode -> defaults, never a bad column value; a defensively-tolerated bare flat {mode,presetId} blob (no envelope) also maps. The test MUST feed the envelope shape — a flat-shape-only fixture is false-green because it never exercises the real device payload.
    - Resolver test: resolvePalette('galaxy','dark') and resolvePalette('standard','dark') return distinct package palettes; resolveMode unchanged (boundary contract intact).
    - Widget re-key test: widget-colors.ts compiles unchanged; widgetPalette() resolves the galaxy(=former space-dark) dark palette with identical status tokens; widget-colors.test.ts's stale 'space-dark' wording updated.
  </behavior>
  <action>
    Author migration 015 (`src/db/migrations/015-theme-settings.ts`, `version: 15`) copying the additive idiom from 014: add the seven theme columns to `app_settings` with the shape confirmed in Task 1 — `theme_package` NOT NULL DEFAULT 'galaxy' with a CHECK on galaxy|standard; `galaxy_mode`/`standard_mode` NOT NULL DEFAULT 'system' with a CHECK on light|dark|system; `galaxy_accent`/`standard_accent` nullable TEXT (accent-id, NULL = package default at render); `galaxy_background`/`standard_background` nullable TEXT (slot-id, NULL = package default at render). Register migration015 in `MIGRATIONS` and bump `TARGET_VERSION` to 15 in `src/db/database.ts`. Extend `full-chain.test.ts` to 015. Write `015-theme-settings.test.ts`.
    Thread the columns through the writer contract in `src/db/app-settings-dao.ts`: add fields to `AppSettings` and `AppSettingsRow`; declare the 7 keys as OPTIONAL (`theme_package?: ...` etc.) in the `PortableSettingsSnapshot` TYPE so they enter `AppSettingsPatch` (writable via `updateAppSettings`, restorable via restore-apply) AND so a `getPortableSettingsSnapshot` return that omits them still typechecks (`tsc --noEmit`) — do NOT copy the CURRENT `phoneRegionOverride` form (:154), which is required-and-emitted since 18.1-04; instead add a comment that portable-snapshot EMISSION of these keys is owned by Phase 36's format-4 plan (deferred-emission = optional field + no SELECT/return, per commit 69bb048); add keys to `WritableSettingsKey` and `COLUMN_OF`; map the row in `getAppSettings` (NULL passthrough for accent/background exactly like `selfSunColour`); add validators `assertThemePackage`, `assertThemeMode`, `assertAccentId` (null-or-known-id), `assertBackgroundId` (null-or-known-slot) with exported constants, plus `*_FIELDS` arrays wired into the patch-validation loop before the UPDATE opens. **Do NOT add the 7 keys to the `getPortableSettingsSnapshot` SELECT or its returned object** (emission deferred — REVIEWS 23-01 HIGH). Extend `app-settings-dao.test.ts`.
    Add the seven new keys to the `PORTABLE_SETTINGS_KEYS` allowlist in `src/backup/backup-schema.ts` (allowlist only — do NOT bump `BACKUP_FORMAT_VERSION` in `src/backup/types.ts` and do NOT add a `FORWARD_MIGRATIONS` entry). Add a regression test to `src/backup/export-manifest.test.ts` asserting a built format-3 export's `appSettings` contains none of the 7 theme keys.
    Confirm the `space-dark`->`galaxy` re-key keeps the headless widget path intact: `src/services/widget/widget-colors.ts` imports `DEFAULT_PRESET_ID`/`resolvePalette` by name and must compile unchanged (ADR-042); update only the stale `'space-dark'` wording in `src/services/widget/widget-colors.test.ts` and confirm the test still passes (galaxy dark == former space-dark values).
    Re-key the theme layer onto the package axis: in `theme-types.ts` introduce `ThemePackage` = 'galaxy'|'standard', repoint the preset identifier to the package, and add the store/provider types for package/mode/accent/background (ResolvedTheme carries the active package). In `theme-presets.ts` re-key `THEME_PRESETS` by `ThemePackage` (galaxy = the former space-dark dark palette; add a standard package with a placeholder dark palette so the package axis resolves to two DISTINCT palettes — real four-palette authoring is Plan 03) and give `resolvePalette` a `(package, mode)` signature; keep `resolveMode` unchanged. Update `theme-presets.test.ts` for the package axis. Leave `ThemePreset.light` optional here (Plan 03 makes it required). The `@/theme` barrel already re-exports theme-presets/theme-types via `export *`, so new symbols there surface without editing index.ts.
    Write `src/theme/orbit-theme-migration.ts` — a pure (RN-free, node-testable) mapper from the legacy zustand persist ENVELOPE to a column patch. The real AsyncStorage payload is the envelope {"state":{"mode":"dark","presetId":"space-dark"},"version":1} (theme-store.ts persists via zustand `persist`/partialize -> {mode,presetId}), so the mapper MUST unwrap `parsed.state` and read `state.mode`/`state.presetId` (tolerating the `version` field and any unknown version; also tolerate a bare flat {mode,presetId} blob defensively; a missing/absent `.state` -> no-op). Map presetId 'space-dark' -> package 'galaxy', treat absent/unknown/malformed input as a no-op/defaults, and NEVER read the envelope's top-level .mode/.presetId (that is the defect — it is undefined on the real payload). Write its test with the REAL envelope fixture (envelope present; envelope with unknown version; malformed/absent -> no-op) — do NOT use a flat-only fixture (REVIEWS 23-01 MEDIUM).
    Rework `src/stores/theme-store.ts` so its source of truth is the boot-hydrated `app_settings` selection (package/mode/accent/background + setters) rather than the AsyncStorage persist; the provider re-renders from store state.
    In `App.tsx`, fold the theme read into the existing `openAndMigrate` `ready` gate in this EXACT order (REVIEWS 23-01 MEDIUM/HIGH — import ordering must not hydrate stale values AND the import must be idempotent): (1) await `openAndMigrate` (migration 015 committed); (2) read the current committed settings ONCE via `getAppSettings()` — the pre-import snapshot, used both to diff against and to hydrate; (3) read the legacy `orbit-theme` AsyncStorage blob via `getItem` inside a local try/catch — a rejected/unavailable read is non-fatal ("no import this launch"); (4) `JSON.parse` the blob (which is the zustand persist ENVELOPE {"state":{...},"version":1}, NOT a flat object) then validate/map it to a column patch via `orbit-theme-migration`, which unwraps `parsed.state.mode`/`parsed.state.presetId` (REVIEWS 23-01 MEDIUM), inside a local try/catch — a corrupt/unparseable blob is non-fatal (falls back to defaults / no import); (5) **compare-before-write** — compute the DIFF: the subset of mapped legacy fields whose value DIFFERS from the corresponding field in the pre-import snapshot, and call `updateAppSettings(diff)` (awaiting the DB commit) ONLY when that diff is NON-EMPTY. Do NOT gate on "the mapped patch is non-empty": `updateAppSettings` ALWAYS bumps `modified_at` + `data_revision` (app-settings-dao.ts:606-617; docstring :598-604 says even an empty patch bumps `modified_at`), and the mapped legacy blob is always non-empty, so writing an already-matching blob would spuriously advance the settings revision and corrupt backup last-writer-wins. The empty-diff path performs ZERO writes and no bump, so a repeat import after a failed clear is a TRUE no-op; (6) clear the AsyncStorage `orbit-theme` key via `removeItem` (in a local try/catch) after step (5) resolves — a clear-failure is non-fatal precisely because the next launch will diff to empty and write nothing; (7) hydrate the theme store from the post-import settings (the pre-import snapshot with the written diff merged over it, or a re-read via `getAppSettings()`) so the store never hydrates pre-import values; (8) only then flip `ready`. A write-failure at (5) must leave the AsyncStorage key intact and not partially write. `ThemeProvider` stays outside `AppShell` so the pre-`ready` splash still resolves tokens. Wire the persisted `theme_package` (+ resolved mode) all the way to the app background surface so the first painted frame carries the saved palette.
    Run all writes through `formatLocalDate()`/the injected `deps.now`. Keep every colour literal under a `src/**/theme/**` path (theme-presets.ts here; accents.ts arrives in Plan 03) — `check:colors` exempts the whole `/theme/` subtree, not only theme-presets.ts.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/015-theme-settings.test.ts src/db/migrations/full-chain.test.ts src/db/app-settings-dao.test.ts src/theme/orbit-theme-migration.test.ts src/theme/theme-presets.test.ts</automated>
    <automated>npm run check:colors</automated>
    <human-check>On a Pixel debug build (device-UAT, end-of-phase): cold-start the app with a persisted non-default theme_package and confirm the first painted frame is the saved package's palette with NO dark->other snap (restore-before-paint); then confirm an existing device that had an AsyncStorage orbit-theme value carries its mode across after one launch.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION = 15" src/db/database.ts` matches and migration015 is registered in MIGRATIONS.
    - `npx vitest run src/db/migrations/015-theme-settings.test.ts src/db/migrations/full-chain.test.ts` passes; full-chain reaches version 15.
    - `npx vitest run src/db/app-settings-dao.test.ts` passes including a per-package round-trip of mode/accent/background and a reject test for an invalid enum/accent/background value.
    - `npx vitest run src/theme/orbit-theme-migration.test.ts` passes with the REAL zustand persist envelope fixture {"state":{"mode":"dark","presetId":"space-dark"},"version":1} -> {themePackage:'galaxy', galaxyMode:'dark'} (reading parsed.state, not the top level), an unknown-`version` envelope still mapping, and an absent/malformed blob -> no-op (REVIEWS 23-01 MEDIUM); a flat-shape-only fixture is rejected as false-green.
    - `npx vitest run src/theme/theme-presets.test.ts` passes with resolvePalette('galaxy',*) and resolvePalette('standard',*) returning distinct palettes.
    - All seven new keys appear in PORTABLE_SETTINGS_KEYS (assert in a backup-schema or dao test); `grep -n "BACKUP_FORMAT_VERSION = 3" src/backup/types.ts` still matches (no bump this phase) and `git diff` shows no new FORWARD_MIGRATIONS entry.
    - `npx vitest run src/backup/export-manifest.test.ts` passes including the assertion that a format-3 export's appSettings contains none of the 7 theme keys (deferral guard).
    - Boot import-order/idempotency tests pass: a SECOND import when the DB already equals the mapped legacy values performs ZERO writes — `updateAppSettings` is not called and `data_revision`/`modified_at` are unchanged before vs after (compare-before-write, REVIEWS 23-01 HIGH); a clear-failure leaves the columns correct and the next-launch import is that zero-write no-op; `getItem`/`JSON.parse`/`removeItem` failures are each individually non-fatal and do not block `ready` (REVIEWS 23-01 MEDIUM); a write-failure leaves the AsyncStorage key intact; the store never hydrates pre-import values.
    - `npx vitest run src/services/widget/widget-colors.test.ts` passes after the re-key; widget-colors.ts is unchanged (only its test's stale 'space-dark' string updated).
    - `npm run check:colors` exits 0 (no hex outside src/**/theme/**; none in migration 015).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A persisted theme_package + per-package mode is read from app_settings inside the boot ready gate and drives the first-frame palette across the package × mode axes; the legacy orbit-theme value is imported once then cleared; all seven columns are DAO-writable/readable/validated and in the backup allowlist; node tests and check:colors are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device-local AsyncStorage `orbit-theme` blob -> app | Legacy stored value imported at boot; treat as untrusted data |
| backup file -> restore -> app_settings | Restored settings row may be tampered/partial |
| app_settings row -> render | A missing/corrupt column must not crash boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | orbit-theme-migration reading the legacy AsyncStorage payload | low | mitigate | Unwrap the zustand persist envelope (parsed.state, tolerating `version`); validate state.mode/state.presetId against known enums; unknown/malformed/absent -> seeded defaults; never write an unvalidated column value (REVIEWS 23-01 MEDIUM — the payload is {"state":{...},"version":1}, not flat) |
| T-23-02 | Tampering | migration 015 / getAppSettings on a partial row | low | mitigate | NOT NULL DEFAULT + CHECK on package/mode columns seed a v0->v15 device; getAppSettings throws on a missing id=1 row rather than returning garbage |
| T-23-03 | Tampering | theme values arriving via a restored backup | medium | mitigate | assertPortableSettings allowlist + DAO assertThemePackage/assertThemeMode/assertAccentId/assertBackgroundId before UPDATE + migration CHECK constraints |
| T-23-04 | Info Disclosure | new keys leaking secrets into backup | low | accept | New keys are plain enums/ids; SECRET_SHAPED_KEY regex + allowlist already reject secret-shaped keys — no secret can ride these columns |
| T-23-14 | Tampering / compatibility | silent format-3 wire-shape change breaking cross-version restore | high | mitigate | Theme keys are allowlisted but NOT emitted by getPortableSettingsSnapshot this phase (phoneRegionOverride precedent); format-3 exports stay byte-compatible, so a pre-Phase-23 build restores them and gets no spurious hard-reject; regression test guards the omission; BACKUP_FORMAT_VERSION bump deferred to Phase 36 (owner-sequenced) |
| T-23-S/R/E | Spoofing/Repudiation/Elevation | — | n/a | accept | Local-first, single-device, no auth, no network on any read path — these STRIDE categories do not apply |
</threat_model>

<verification>
- `npx vitest run src/db src/theme` green for the changed areas; `npm test` green at wave merge.
- `npm run check:colors` and `npx tsc --noEmit` clean.
- Device UAT (end-of-phase Pixel pass): no wrong-theme flash on cold start; orbit-theme value carried across on an upgrade install.
</verification>

<success_criteria>
A durable, backup-portable theme selection resolves through the provider to the on-screen palette across the package × mode axes, restored before first paint, with the legacy AsyncStorage value migrated once — proven by node tests, check:colors, and tsc.
</success_criteria>

## Artifacts this phase produces

New files: `src/db/migrations/015-theme-settings.ts` (+ `.test.ts`), `src/theme/orbit-theme-migration.ts` (+ `.test.ts`).
New app_settings columns: `theme_package`, `galaxy_mode`, `standard_mode`, `galaxy_accent`, `standard_accent`, `galaxy_background`, `standard_background`.
New/changed exported symbols: `ThemePackage` type; `resolvePalette(package, mode)` signature; `orbit-theme-migration` mapper (e.g. `mapLegacyThemeBlob`); DAO validators `assertThemePackage`, `assertThemeMode`, `assertAccentId`, `assertBackgroundId` and their `*_FIELDS` arrays; new `AppSettings`/`AppSettingsRow`/`PortableSettingsSnapshot`/`AppSettingsPatch` fields `themePackage`/`galaxyMode`/`standardMode`/`galaxyAccent`/`standardAccent`/`galaxyBackground`/`standardBackground`; new `PORTABLE_SETTINGS_KEYS` allowlist entries; theme-store package/mode/accent/background selectors + setters.
Not produced here (deferred): theme-key emission in `getPortableSettingsSnapshot`; any `BACKUP_FORMAT_VERSION`/`FORWARD_MIGRATIONS` change (Phase 36).

<output>
Create `.planning/phases/23-theme-visual-system/23-01-SUMMARY.md` when done.
</output>
