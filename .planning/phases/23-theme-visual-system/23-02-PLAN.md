---
phase: 23-theme-visual-system
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - package.json
  - package-lock.json
  - assets/Inter-Regular.ttf
  - assets/SpaceGrotesk-SemiBold.ttf
  - assets/README.md
  - src/theme/fonts.ts
  - src/theme/fonts.test.ts
  - src/theme/tokens/typography.ts
  - src/theme/tokens/typography.test.ts
  - src/theme/tokens/spacing.ts
  - src/theme/tokens/radii.ts
  - src/components/ui/AppText.tsx
  - App.tsx
autonomous: true
requirements: [THEME-07]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "THEME-07: OS text scaling is honored broadly — text reflows (wraps / grows height / stacks) rather than truncating or shrinking. The AppText role primitive does NOT set allowFontScaling={false} and does NOT pin fixed heights or numberOfLines on reflowable content; utility/forms may be tighter, presentation screens more expressive (dossier §H)."
    - "THEME-07 / dossier §H: five semantic typography ROLES (display, heading, body, label, caption) over exactly four sizes (28/20/16/14) and exactly two weights (Regular 400, SemiBold 600); label and caption share the 14 step and differ by weight + colour token, so every size step stays distinct with no 1px near-collision."
    - "Fonts load through expo-font for RN <Text> (Inter Regular + SemiBold, Space Grotesk SemiBold), bundled locally from assets/ with no network; the font load is folded into App.tsx's existing ready gate alongside the theme read so first paint has the real fonts (Pitfall 5 — the Skia useFonts pipeline in the Orrery is separate and untouched here)."
    - "THEME-07 font-load failure is NON-FATAL (REVIEWS 23-02 MEDIUM): fonts.ts's load helper CATCHES a rejected/failed load internally, logs, and RESOLVES (degrading to the system font) so a font error can never reach AppShell's boot catch and block the app in its startup-error state; the ready gate proceeds and still hydrates theme + database. Asserted by a node test, not device-UAT alone."
    - "Adoption is deferred by design (REVIEWS scope note, planning-notes:78-81): this plan ships the AppText PRIMITIVE and the token vocabulary; migrating existing raw <Text>/clamped surfaces (e.g. UnboundContactsScreen.tsx:128-134) to AppText and the Appearance settings UI are Phase 15. THEME-07 is verified this phase at the primitive level (AppText reflow node/props checks + a primitive device render), not via screen-wide adoption — a recorded scope boundary, not a gap."
    - "Runtime deps @expo/vector-icons, expo-font, and expo-blur are installed via npx expo install (SDK-57-pinned, first-party Expo, no postinstall); a native-dep change requires a new dev-client build (device UAT), not just a Metro reload."
    - "spacing tokens are multiples of 4 (xs4/sm8/md12/base16/lg24/xl32/2xl48) and radii tokens (sm8/md12/lg16/xl24/pill999/full9999) follow the very-rounded language; the 44x44 minimum touch target is a floor, not a spacing token; user-controlled density is out of scope (fixed moderate)."
    - "typography/spacing/radii token values resolve as pure data (node-testable, no react-native import) and carry no colour literals; text colour comes from theme tokens via useTheme()."
    - { statement: "THEME-07 (overflow, UI-SPEC UI Considerations 'OS text scaling (large text)', covered): at large OS text sizes layouts reflow rather than truncate or shrink. Device-observable on a Pixel with large font scale.", verification: backstop }
    - { statement: "THEME-07 (encoding edge): text length/reflow is defined by the platform <Text> layout over grapheme clusters — multibyte/combining-character and long-word content wraps or grows height rather than clipping; AppText never measures bytes or imposes a code-unit cap that would clip a multibyte string.", verification: backstop }
    - { statement: "THEME-07 (empty edge): an empty or whitespace-only string renders an empty AppText that consumes no unexpected space and does not crash; a role with no children is valid.", verification: explicit }
  artifacts:
    - "package.json + package-lock.json — @expo/vector-icons, expo-font, expo-blur added via npx expo install; the lockfile change is committed with package.json and validated reproducible via npm ci (REVIEWS 23-02 LOW), never an unreviewed drift"
    - "assets/Inter-Regular.ttf, assets/SpaceGrotesk-SemiBold.ttf (SIL OFL 1.1) bundled locally"
    - "assets/README.md — in-project provenance/license record for every bundled font (source, version, SIL OFL 1.1), placed at the assets ROOT beside the .ttf files it documents so the README and the fonts it covers live together (REVIEWS 23-02 LOW — path made consistent; fonts stay at root matching the existing assets/Inter-SemiBold.ttf)"
    - "src/theme/fonts.ts (+ .test.ts) — expo-font font map + a load helper that catches/logs a failed load and RESOLVES (degrade to system font, never rejects), consumed by the App.tsx ready gate"
    - "src/theme/tokens/typography.ts (+ .test.ts) — TYPOGRAPHY roles: {family, size, weight, lineHeight, colorToken} for display/heading/body/label/caption"
    - "src/theme/tokens/spacing.ts — SPACING scale (multiples of 4)"
    - "src/theme/tokens/radii.ts — RADII scale (sm..full)"
    - "src/components/ui/AppText.tsx — role-driven <Text> primitive resolving family/size/weight from TYPOGRAPHY and colour from useTheme(); reflow-preserving"
    - "App.tsx — expo-font load folded into the ready gate alongside the theme read"
  key_links:
    - "assets/*.ttf -> src/theme/fonts.ts (expo-font map) -> App.tsx ready gate -> first paint with real fonts"
    - "TYPOGRAPHY roles -> AppText -> every screen's text (semantic role, not raw fontSize)"
    - "SPACING/RADII tokens -> consumed by Plans 05 (icon), 06 (surface), 07 (modal/button)"
  prohibitions:
    - "Do NOT set allowFontScaling={false} or pin fixed heights/line clamps on reflowable text — that reintroduces the truncate/shrink behavior THEME-07 forbids."
    - "Do NOT introduce a network/CDN font path. Fonts are bundled locally from assets/ (local-first, no network on any read path)."
    - "Do NOT add a config-plugin entry to app.config.ts for expo-font/@expo/vector-icons/expo-blur unless the package actually ships one — a bogus plugins entry is a prebuild error (01-01 deduped-plugins lesson)."
    - "Do NOT put a colour literal in a token file; typography carries a colour TOKEN name, resolved through useTheme() at render (check:colors)."
    - "Do NOT wire these RN fonts into the Skia useFonts map (Orrery); that is a separate font pipeline (Pitfall 5) owned elsewhere."
---

<objective>
Establish the typographic and layout foundation the rest of the phase builds on: install the phase's runtime dependencies, bundle the fonts locally, define the semantic typography/spacing/radii tokens, and deliver an OS-text-scaling-respecting text primitive that reflows rather than truncates.

Purpose: give downstream plans (icons, backgrounds/surface, modals/buttons) a single token vocabulary and the installed deps they need, and satisfy THEME-07 (text scaling) with a reusable primitive.
Output: installed deps + bundled fonts + typography/spacing/radii tokens + AppText role primitive, with the font load folded into the boot gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runtime deps and bundle fonts locally</name>
  <files>package.json, package-lock.json, assets/Inter-Regular.ttf, assets/SpaceGrotesk-SemiBold.ttf, assets/README.md, src/theme/fonts.ts, src/theme/fonts.test.ts</files>
  <read_first>
    - package.json (current deps; SDK 57 pin ~57.0.1)
    - app.config.ts (expo config plugins array; the 01-01 dedupe lesson)
    - src/screens/OrreryScreen.tsx:175-176 and src/components/orrery/OrbitBody.tsx:138 (existing Skia useFonts pipeline + the bundled Inter-SemiBold.ttf precedent)
    - assets/ (existing Inter-SemiBold.ttf location + how fonts are referenced)
    - 23-RESEARCH.md Package Legitimacy Audit + Provenance note (all four first-party Expo, no postinstall)
  </read_first>
  <action>
    Run `npx expo install @expo/vector-icons expo-font expo-blur` so each is pinned to the SDK-57-matched version; commit the resulting `package-lock.json` change alongside `package.json` (REVIEWS 23-02 LOW — the lockfile is part of the reviewed diff, not an unreviewed drift). Add `Inter-Regular.ttf` and `SpaceGrotesk-SemiBold.ttf` (SIL OFL 1.1) to `assets/` (root), mirroring the existing `assets/Inter-SemiBold.ttf` bundling. Create `assets/README.md` (assets ROOT, beside the .ttf files it documents — REVIEWS 23-02 LOW path fix) recording per-font provenance/license (source, version, SIL OFL 1.1) for every bundled font (dossier §E). Create `src/theme/fonts.ts` exposing the expo-font font map (Inter Regular + SemiBold, Space Grotesk SemiBold) and a `loadAppFonts()` helper that internally try/catches the expo-font load, logs on failure, and RESOLVES rather than rejecting (degrade to the system font) so the App.tsx ready gate can never hang or crash on a font error. Write `src/theme/fonts.test.ts` asserting `loadAppFonts()` resolves even when the underlying font load rejects. Do not add an app.config.ts plugins entry for any of these unless the package actually ships a config plugin. Do NOT claim "no postinstall scripts ran" — this repo declares `"postinstall": "patch-package"` (package.json); instead confirm the ONLY postinstall that runs is the known `patch-package` root script and that the three new packages themselves declare no install/postinstall scripts (REVIEWS 23-02 MEDIUM).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');for(const d of ['@expo/vector-icons','expo-font','expo-blur']){if(!(d in (p.dependencies||{}))) {console.error('missing dep '+d);process.exit(1)}}console.log('deps ok')"</automated>
    <automated>test -f assets/Inter-Regular.ttf && test -f assets/SpaceGrotesk-SemiBold.ttf && test -f assets/README.md && test -f package-lock.json && echo fonts-ok</automated>
    <automated>npx vitest run src/theme/fonts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `@expo/vector-icons`, `expo-font`, `expo-blur` present in package.json dependencies at SDK-57-pinned versions.
    - `assets/Inter-Regular.ttf` and `assets/SpaceGrotesk-SemiBold.ttf` exist; `assets/README.md` (assets root) records provenance/license for each bundled font.
    - `package-lock.json` is updated by `npx expo install` and committed with `package.json`; the lockfile is reproducible (validated via `npm ci` or an equivalent clean-install check), not an unreviewed drift (REVIEWS 23-02 LOW).
    - `src/theme/fonts.ts` exports a font map + `loadAppFonts()` helper; `npx vitest run src/theme/fonts.test.ts` proves the helper resolves on a font-load rejection (non-fatal degrade).
    - The known root `patch-package` postinstall is the only postinstall in play; the three new packages declare no own install scripts (verified, not assumed-none).
    - No app.config.ts plugin entry added for a package that ships none; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The three deps are installed (SDK-pinned; only the known patch-package postinstall runs), both fonts are bundled locally with a provenance README, and a fonts module exposes the expo-font map plus a non-fatal load helper proven by a node test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typography, spacing, and radii tokens</name>
  <files>src/theme/tokens/typography.ts, src/theme/tokens/typography.test.ts, src/theme/tokens/spacing.ts, src/theme/tokens/radii.ts</files>
  <read_first>
    - 23-UI-SPEC.md sections Spacing Scale, Radii & Geometry, Typography (the role/size/weight table + the four-sizes-five-roles rule)
    - src/theme/theme-presets.ts and src/theme/theme-types.ts (token/purity idiom; colour tokens live here, not in these new files)
    - src/components/contact-card-ring.ts (RN-free node-testable pure-data header idiom to mirror)
  </read_first>
  <behavior>
    - TYPOGRAPHY has exactly five roles (display, heading, body, label, caption) over exactly four sizes (28,20,16,14) and exactly two weights (400,600); label vs caption share size 14 and differ by weight (600 vs 400) and colour token (textPrimary vs textSecondary).
    - SPACING values are all multiples of 4; RADII include pill (999) and full (9999).
    - The token modules import nothing from react-native (node-testable) and contain no hex literal.
  </behavior>
  <action>
    Create `src/theme/tokens/typography.ts` defining the five semantic roles as pure data — each role carries family (Space Grotesk for display/heading, Inter for body/label/caption), size, weight, lineHeight, and a colour TOKEN name (not a hex). Create `src/theme/tokens/spacing.ts` (xs4/sm8/md12/base16/lg24/xl32/2xl48) and `src/theme/tokens/radii.ts` (sm8/md12/lg16/xl24/pill999/full9999). Keep all three RN-free and colour-literal-free; consumers import the concrete `@/theme/tokens/*` paths (no barrel edit). Write `typography.test.ts` asserting the role/size/weight/colour-token invariants.
  </action>
  <verify>
    <automated>npx vitest run src/theme/tokens/typography.test.ts</automated>
    <automated>npm run check:colors src/theme/tokens</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/tokens/typography.test.ts` passes and asserts exactly five roles, four sizes, two weights, and the label/caption weight+colour-token divergence.
    - spacing values are all `% 4 === 0`; radii include pill 999 and full 9999 (assert in test or by inspection).
    - `npm run check:colors src/theme/tokens` exits 0 (no hex in token files).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Typography, spacing, and radii tokens exist as pure, node-tested, colour-literal-free data exported from the theme index.</done>
</task>

<task type="auto">
  <name>Task 3: AppText role primitive + font gate in boot</name>
  <files>src/components/ui/AppText.tsx, App.tsx</files>
  <read_first>
    - src/theme/tokens/typography.ts (roles from Task 2)
    - src/theme/theme-provider.tsx:50 (useTheme() — the only sanctioned colour source)
    - App.tsx:129-161 (the ready gate the theme read was folded into by Plan 01)
    - src/theme/fonts.ts (Task 1 load helper)
    - any existing <Text> usage for the current styling convention
  </read_first>
  <action>
    Create `src/components/ui/AppText.tsx` — a `<Text>` wrapper taking a semantic `role` prop (display|heading|body|label|caption), resolving family/size/weight/lineHeight from `TYPOGRAPHY` and colour from `useTheme().colors[colorToken]`. It must NOT disable font scaling and must NOT impose fixed heights or default line clamps on reflowable content, so OS text scaling reflows. In `App.tsx`, extend the existing `ready` gate so `loadAppFonts()` (from `src/theme/fonts.ts`) is awaited alongside the theme read before `ready` flips — because `loadAppFonts()` resolves even on failure, a font error degrades to the system font without reaching the boot catch or blocking the app (REVIEWS 23-02 MEDIUM).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors src/components/ui/AppText.tsx App.tsx</automated>
    <human-check>On a Pixel with OS font size set large (device-UAT, end-of-phase): AppText content wraps/grows rather than truncating or shrinking; long multibyte and long-word strings reflow without clipping; the correct fonts render on first paint.</human-check>
  </verify>
  <acceptance_criteria>
    - AppText resolves role -> family/size/weight from TYPOGRAPHY and colour from useTheme(); grep confirms no `allowFontScaling={false}` and no fixed height/`numberOfLines` on reflowable text.
    - App.tsx awaits the expo-font load within the ready gate (grep for the fonts load helper inside the boot effect).
    - `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <done>AppText renders the five roles with theme-token colour and OS-scaling reflow, and fonts are gated into first paint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> package.json | New runtime deps introduced this plan |
| bundled font asset -> render | A missing/failed font must degrade, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-SC | Tampering | npm installs (@expo/vector-icons, expo-font, expo-blur) | medium | mitigate | First-party Expo packages pinned via `npx expo install`; no postinstall scripts; RESEARCH Package Legitimacy Audit verdict OK — no [ASSUMED]/[SUS]/[SLOP] package, so no blocking-human legitimacy checkpoint is required (Phase 9 first-party-Expo precedent) |
| T-23-05 | Denial of Service | missing/failed font load | low | mitigate | `loadAppFonts()` try/catches the expo-font load and RESOLVES (never rejects); the ready gate proceeds and a failed font degrades to the system font rather than reaching AppShell's boot catch — proven by src/theme/fonts.test.ts, not device-UAT alone (REVIEWS 23-02 MEDIUM) |
| T-23-S/T/R/I/D/E (network) | Info Disclosure | fonts/assets | n/a | accept | Fonts bundled locally, no network on any read path — no CDN/remote font delivery |
</threat_model>

<verification>
- `npx vitest run src/theme/tokens` green; `npm run check:colors` and `npx tsc --noEmit` clean.
- Device UAT (end-of-phase): large-text reflow + correct fonts on first paint.
</verification>

<success_criteria>
The phase's deps are installed, fonts are bundled locally, the semantic typography/spacing/radii tokens exist as pure data, and text reflows under OS scaling via AppText — satisfying THEME-07 (at the primitive level; screen adoption is Phase 15) and giving downstream plans their token vocabulary.
</success_criteria>

## Artifacts this phase produces

New files: `src/theme/fonts.ts` (+ `.test.ts`), `src/theme/tokens/typography.ts` (+ `.test.ts`), `src/theme/tokens/spacing.ts`, `src/theme/tokens/radii.ts`, `src/components/ui/AppText.tsx`, `assets/README.md` (font provenance, assets root), `assets/Inter-Regular.ttf`, `assets/SpaceGrotesk-SemiBold.ttf`.
Changed files: `package.json` + `package-lock.json` (three runtime deps added via `npx expo install`; lockfile committed and `npm ci`-reproducible).
New exported symbols: `loadAppFonts()` + the expo-font font map; `TYPOGRAPHY` (five roles); `SPACING` (xs4/sm8/md12/base16/lg24/xl32/2xl48); `RADII` (sm8/md12/lg16/xl24/pill999/full9999); `AppText` (role prop display|heading|body|label|caption).
New runtime deps: `@expo/vector-icons`, `expo-font`, `expo-blur`.

<output>
Create `.planning/phases/23-theme-visual-system/23-02-SUMMARY.md` when done.
</output>
