---
phase: 23-theme-visual-system
plan: 06
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified:
  - src/theme/backgrounds.ts
  - src/theme/backgrounds.test.ts
  - src/theme/tokens/surface.ts
  - src/theme/tokens/surface.test.ts
  - src/components/ui/BackgroundHost.tsx
  - src/components/ui/GlassSurface.tsx
  - src/components/ui/__dev__/ThemePreviewScreen.tsx
  - src/components/orrery/OrreryCanvas.tsx
  - src/components/orrery/SunBody.tsx
  - assets/backgrounds/README.md
  - assets/backgrounds/galaxy-nebula.webp
  - assets/backgrounds/galaxy-deep-space.webp
  - assets/backgrounds/galaxy-aurora.webp
  - assets/backgrounds/galaxy-starfield.webp
  - assets/backgrounds/standard-dawn.webp
  - assets/backgrounds/standard-dusk.webp
  - assets/backgrounds/standard-mesh.webp
  - assets/backgrounds/standard-paper.webp
autonomous: true
requirements: [THEME-04, THEME-05, THEME-12]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "THEME-04 / D-04 / dossier §E: each package ships ~4-5 curated bundled backgrounds PLUS None/Solid, all local require() assets (no network/CDN). A BackgroundHost renders the active package's selected background fixed while content scrolls; per D-04 (supersedes HANDOFF §7) this applies app-wide behind text-heavy screens too — ADR-048 tap-to-freeze / live creeping motion stays superseded and is NOT reinstated."
    - "THEME-04: surface opacity increases with content density — dense forms/settings use more opaque readable surfaces, presentation screens may show more background; readability wins. Backgrounds stay fixed (not scrolling) via a fixed-position host behind scrollable content."
    - "THEME-04 (single canonical slot-id source — REVIEWS 23-01 cycle-4 MEDIUM): backgrounds.ts IMPORTS the accepted background slot ids from Plan 01's single source `src/theme/theme-option-ids.ts` (`BACKGROUND_SLOT_IDS`) and keys its slot manifest by exactly those ids — it does NOT re-declare a parallel list. A drift test in backgrounds.test.ts asserts the manifest's declared slot-id set (across both packages, including None/Solid) equals `BACKGROUND_SLOT_IDS` exactly (no id in the DAO-accepted set without a manifest slot, no manifest slot outside the accepted set), so the DAO validator (`assertBackgroundId`) and this resolver cannot drift in either direction."
    - "THEME-05 / dossier §F: Galaxy renders glass-forward (translucency, luminous borders, subtle glow) via GlassSurface; Standard renders flatter/quieter (opaque surfaces, plain borders). Both use ONE semantic surface API keyed by package (no parallel component families)."
    - "THEME-05: blur degrades gracefully — GlassSurface uses expo-blur only where affordable and falls back to a semi-opaque tinted surface TOKEN when blur is unavailable; blur carries no required meaning (dossier §F [DERIVED], [theme -> performance])."
    - "THEME-05 surface-token-only (cycle-3 LOW, finding #4): GlassSurface (and its peer surface components) derive their style from a PURE `resolveSurfaceStyle(package, blurAvailable)` selector that returns ONLY declared SURFACE token-field values; a node test asserts no value the selector returns is outside the declared SURFACE token set for that package. This closes the check:colors /theme/-exemption escape hatch — the colour gate is a LOCATION policy that cannot catch an ad-hoc literal inside a theme module OR a component-local opacity/colour that bypasses a named token, so the semantic 'reads only named tokens' assurance is a unit test, not the colour gate. (BackgroundHost's density opacity is likewise token-sourced and covered by the density->opacity test.)"
    - "THEME-12 / dossier §R: the Orrery follows the active theme PACKAGE (still resolving colour through shared tokens, D-06) but may use a specialized immersive treatment; its ambient motion honors the reduced-motion SharedValue (Plan 04) read via useDerivedValue with no per-frame setState."
    - "THEME-06 consumption — ALL Orrery clock consumers gate, not just OrreryCanvas (REVIEWS 23-06 HIGH, verified on disk): OrreryCanvas.tsx:93-101 twinkle/drift AND SunBody.tsx:82-100 (the sun glow pulse derived from the shared clock via useOrreryClock()) both gate on the reduced-motion SharedValue (collapse to a constant when reduced motion is ON), read in the Skia loop through useDerivedValue — never from a React boolean. Gating only OrreryCanvas would leave the sun pulsing under reduced motion, so SunBody.tsx is in scope."
    - "THEME-04 animated-background contract (REVIEWS 23-06 MEDIUM): backgrounds are STATIC bundled assets by default. IF a Galaxy background animates (discretion), its motion is a Skia/Reanimated worklet in BackgroundHost that multiplies the Plan 04 `ambient` SPEED token and gates on the same reduced-motion SharedValue via useDerivedValue (collapse to a static frame when ON) — the exact worklet-consumer contract, not an unspecified assertion. A non-animated background ships with no worklet at all; there is no undefined ambient-motion claim."
    - { statement: "THEME-04 (empty, UI-SPEC UI Considerations 'None/Solid background option', covered): each package offers None/Solid as a valid explicit background; a stored NULL background resolves to the package default, 'none' resolves to the solid theme background.", verification: explicit }
    - { statement: "error (UI-SPEC UI Considerations 'background asset load failure / blur unaffordable', covered): the catchable RUNTIME failure is an image RENDER failure (decode error / null image via the Image/Skia onError path), NOT a bundle-missing require() (which fails at Metro bundle-resolution time, not as a recoverable component error — REVIEWS 23-06 MEDIUM). Asset PRESENCE is validated at bundle time (every slot resolves to a real require()); at runtime a render failure silently falls back to None/Solid, and unaffordable blur falls back to a semi-opaque tinted surface token — no user-facing error.", verification: explicit }
    - { statement: "THEME-11 glass-composite AA (REVIEWS 23-03 MEDIUM handoff): Plan 03's AA gate deferred the translucent glass-composited pairs to here; this plan asserts text/status foregrounds over the semi-opaque tinted fallback surface token meet AA-equivalent contrast (the guaranteed-readable composite), so glass never drops a foreground below AA.", verification: backstop }
    - { statement: "THEME-11 live-glass AA invariant (REVIEWS 23-06 MEDIUM + cycle-3 MEDIUM): the AA gate bounds the RENDERED live glass composite over the real bundled imagery, not only the blur-unavailable fallback token. TWO layered guards. (1) COMPOSITED per-asset check (cycle-3 — the primary, strictly-safer proof): backgrounds.ts declares a worst-case (brightest representative) pixel per bundled asset, and surface.test.ts asserts every text/status foreground, composited over alphaComposite(live glass tint @ liveGlassTintOpacity, that brightest pixel), meets AA-equivalent contrast via contrast.ts — so a foreground drawn on live glass over the brightest region of each asset still passes AA. The declared brightest pixel is a design CONSTRAINT the shipped asset must not exceed (recorded beside its provenance row). (2) OPACITY-ORDERING regression guard (kept): surface.ts encodes BOTH the fallback tint token opacity AND the live glass tint opacity per package, and surface.test.ts asserts liveGlassTintOpacity >= fallbackTintTokenOpacity for each package, so the live tint is never MORE translucent than the fallback surface the AA check validated. GlassSurface applies a live tint opacity >= this fallback-token opacity at render. Opacity ordering ALONE does NOT prove per-asset AA (contrast depends on tint colour, foreground colour, AND the actual background pixels) — hence guard (1) is required, not optional. DECLARED-VS-DECODED LIMITATION (REVIEWS 23-06 cycle-4 MEDIUM — recorded explicitly): guard (1) proves AA only under the DECLARED worst-case pixel in backgrounds.ts; nothing in the node test decodes the committed `.webp` bytes, so a shipped asset whose ACTUAL brightest region exceeds its declared pixel could drop text below AA on-device while every test stays green (THEME-11 is a hard a11y requirement). This gap is CLOSED at execution by a per-asset DEVICE-UAT on the Pixel (body+caption text over glass on each Galaxy asset's brightest region confirmed legible — added to 23-VALIDATION.md's Manual-Only table so /gsd-verify-work exercises it); an optional build-time image-luminance gate that decodes each `.webp` and asserts its brightest region <= the declared pixel is a candidate future enhancement (Phase 15). The composited unit test stays the fast regression guard.", verification: explicit }
    - { statement: "THEME-04 (adjacency edge): selecting a background equal to the current selection is a no-op (no flicker/reload); switching between two presets swaps cleanly with the background fixed behind scrolling content.", verification: backstop }
    - { statement: "THEME-04 (ordering edge): the background slot manifest has a stable defined order per package; the resolved background is a deterministic function of (package, stored slot-id) independent of selection order.", verification: explicit }
    - { statement: "THEME-05 (unclassified edge — flagged planner assumption, not auto-dismissed): 'glass-forward' vs 'flatter' is a per-package surface treatment applied through GlassSurface/surface tokens, sharing layout/IA/density (dossier §B); the exact blur radius/opacity/glow values are discretion defaults, owner-tunable. Flagged for verify.", verification: backstop }
    - { statement: "THEME-12 (unclassified edge — flagged planner assumption, not auto-dismissed): the Orrery immersive treatment (specialized background, stronger glow, edge-to-edge, more motion) is an intentional exception but stays tokenized and honors contrast + reduced motion; it does not adopt the ordinary selected background. Flagged for verify.", verification: backstop }
    - "Maintain in-project provenance/license info for every shipped background asset (dossier §E [DERIVED])."
    - "Adoption is deferred by design (REVIEWS scope note, planning-notes:78-81): this plan delivers the BackgroundHost / GlassSurface PRIMITIVES and the fixed-behind-scroll + density-opacity + fallback behaviors. MOUNTING BackgroundHost at the app-shell root across existing text-heavy screens is a renderer-phase/Phase-15 adoption task (the Appearance settings UI that drives it is Phase 37). THEME-04/05 are delivered this phase as the primitives + behaviors, verified at the primitive level (node tests + a primitive device render), not via app-wide mounting — a recorded scope boundary, not a gap. In-phase, OrreryCanvas + SunBody ARE wired (real consumers of the reduced-motion signal)."
  artifacts:
    - "src/theme/backgrounds.ts (+ .test.ts) — BACKGROUND slot manifest per package (~4-5 + None/Solid) -> local require() map + provenance + a declared worst-case (brightest representative) pixel per bundled asset (cycle-3 MEDIUM — the composited-AA design bound the shipped asset must not exceed); resolveBackground(package, slotId|null); AND resolveRenderableBackground(package, slotId|null, renderFailed) — a PURE, node-testable reducer (REVIEWS 23-06 LOW) that returns None/Solid when renderFailed=true and resolveBackground's result otherwise, so BackgroundHost's onError->None/Solid fallback is exercised by a unit test rather than manifest-only (the repo has no react-test-renderer to mount BackgroundHost)"
    - "src/theme/tokens/surface.ts (+ .test.ts) — per-package surface treatment tokens (opacity-by-density, glass vs flat, semi-opaque tinted blur-fallback token, live glass tint colour + live glass tint opacity) PLUS a pure `resolveSurfaceStyle(package, blurAvailable)` selector returning the glass/flat surface style drawn ONLY from declared SURFACE token fields (cycle-3 LOW, finding #4 — the token-only escape-hatch guard GlassSurface consumes)"
    - "src/components/ui/BackgroundHost.tsx — fixed-behind-scroll host rendering the active background with density-driven surface opacity + asset-fail fallback to None/Solid"
    - "src/components/ui/GlassSurface.tsx — one semantic surface API; expo-blur where affordable, semi-opaque tinted token fallback"
    - "src/components/orrery/OrreryCanvas.tsx — twinkle/drift ambient motion gated on the reduced-motion SharedValue; follows active package tokens"
    - "src/components/orrery/SunBody.tsx — the sun glow pulse (pulseGlowRadius/pulseGlowOpacity off useOrreryClock) gated on the same reduced-motion SharedValue so the sun stops pulsing under reduced motion (REVIEWS 23-06 HIGH)"
    - "New background assets under assets/backgrounds/ (bundled locally, one committed image file per non-None/Solid slot: galaxy-nebula/galaxy-deep-space/galaxy-aurora/galaxy-starfield + standard-dawn/standard-dusk/standard-mesh/standard-paper, .webp) PLUS assets/backgrounds/README.md — an in-project provenance/license manifest with a row per background asset (source, licence, author). The backgrounds.ts slot manifest is the canonical list (~4-5 per package); every slot it declares MUST have a corresponding committed asset AND a README provenance row (REVIEWS 23-06 MEDIUM — asset paths + provenance owned in files_modified so they cannot drop out of execution scope)"
  key_links:
    - "app_settings galaxy_background/standard_background (slot-id|NULL) -> theme-store -> BackgroundHost -> resolveBackground(backgrounds.ts) -> local require() asset"
    - "package -> tokens/surface.ts -> GlassSurface (glass vs flat) with expo-blur -> tinted-token fallback"
    - "Plan 04 useReducedMotionShared() -> useDerivedValue in OrreryCanvas AND SunBody AND (if animated) the Galaxy background worklet -> ALL ambient motion gated (no setState)"
  prohibitions:
    - "Do NOT add a network/CDN/remote/downloadable background path — preset library only, bundled locally (D-04, dossier §E deferred list)."
    - "Do NOT reinstate HANDOFF §7 tap-to-freeze or live creeping-motion behavior (ADR-048 superseded); do NOT re-scope backgrounds to dashboard/orrery-only (D-04 supersedes §7). Reversing D-04 is stop-and-ask."
    - "Do NOT drive Orrery/background ambient motion from React state or per-frame setState (D-07) — read the reduced-motion SharedValue via useDerivedValue in the Skia loop."
    - "Do NOT gate only OrreryCanvas and leave SunBody's glow pulse (or any other useOrreryClock consumer) unguarded (REVIEWS 23-06 HIGH) — audit every clock consumer; the sun must stop pulsing under reduced motion."
    - "Do NOT place a background-tint/surface colour literal outside src/**/theme/** (backgrounds.ts / tokens/surface.ts / theme-presets.ts are the sanctioned locations) (check:colors)."
    - "Do NOT make blur a hard requirement — it must degrade to a semi-opaque tinted surface token when unaffordable (dossier §F)."
    - "Do NOT support user-uploaded/custom backgrounds (out of scope, dossier deferred list)."
---

<objective>
Deliver the background system, per-package surface/glass treatment, and the Orrery visual exception — and wire the reduced-motion signal into the app's Skia ambient motion.

Purpose: satisfy THEME-04 (bundled backgrounds + None/Solid + opacity-by-density, fixed while scrolling), THEME-05 (Galaxy glass-forward / Standard flatter with graceful blur fallback), and THEME-12 (Orrery follows the package immersively but stays tokenized + honors reduced motion), consuming the Plan 04 hook.
Output: backgrounds.ts + surface tokens + BackgroundHost + GlassSurface + reduced-motion-gated OrreryCanvas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-RESEARCH.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
@.planning/phases/23-theme-visual-system/23-04-SUMMARY.md
@docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Background slot manifest + surface tokens (pure data)</name>
  <files>src/theme/backgrounds.ts, src/theme/backgrounds.test.ts, src/theme/tokens/surface.ts, src/theme/tokens/surface.test.ts</files>
  <read_first>
    - 23-UI-SPEC.md Background System (slot manifest per package + None/Solid) and Surface & Glass Treatment
    - docs/dossier/milestone-2/phase-02-theme-visual-system-dossier.md §E, §F (backgrounds bundled locally; surface opacity by density; blur degrades)
    - src/db/app-settings-dao.ts (galaxy_background/standard_background nullable slot-id columns from Plan 01; assertBackgroundId)
    - src/theme/theme-option-ids.ts (Plan 01 single source — `BACKGROUND_SLOT_IDS`; IMPORT this and key the slot manifest by it, do NOT re-declare a parallel slot-id list — REVIEWS 23-01 cycle-4 MEDIUM)
    - src/theme/theme-presets.ts (solid theme background token for None/Solid; the four palettes' text/status tokens for the glass-composite AA check)
    - src/theme/contrast.ts (Plan 03 — the AA gate reused for the glass-composited pairs handed off from Plan 03)
    - src/theme/tokens/ (pure-data token idiom)
  </read_first>
  <behavior>
    - BACKGROUND manifest lists ~4-5 slots per package + a None/Solid slot, each mapping to a local require() asset (or the solid token for None); resolveBackground(package, slotId|null) returns the asset for a slot, the solid token for 'none', and the package default for NULL. The manifest is keyed by the `BACKGROUND_SLOT_IDS` imported from Plan 01's `theme-option-ids.ts` (single source) — NOT a re-declared parallel list; a drift test asserts the manifest's slot-id set equals `BACKGROUND_SLOT_IDS` exactly (REVIEWS 23-01 cycle-4 MEDIUM).
    - resolveRenderableBackground(package, slotId|null, renderFailed) is a PURE reducer (node-testable, REVIEWS 23-06 LOW): renderFailed=true returns the None/Solid effective background regardless of the stored slot; renderFailed=false returns resolveBackground's result. BackgroundHost's onError handler drives this reducer so the onError -> None/Solid runtime fallback is exercised by a node test, not manifest-only (no react-test-renderer needed to mount the component).
    - surface tokens expose per-package opacity-by-density values (denser -> more opaque) and a glass-vs-flat flag, plus the semi-opaque tinted FALLBACK surface token used when blur is unaffordable, AND the LIVE glass tint opacity per package (REVIEWS 23-06 MEDIUM).
    - Glass-composite AA (Plan 03 handoff): text/status foregrounds over the semi-opaque tinted fallback surface token meet AA-equivalent contrast via contrast.ts across all four palettes.
    - Live-glass AA invariant (REVIEWS 23-06 MEDIUM + cycle-3 MEDIUM): TWO guards. (1) COMPOSITED per-asset AA (primary, strictly-safer): backgrounds.ts declares a worst-case (brightest representative) pixel per bundled asset; surface.test.ts asserts every text/status foreground composited over alphaComposite(live glass tint @ live tint opacity, that brightest pixel) meets AA-equivalent contrast via contrast.ts, for each asset/package/mode. (2) OPACITY-ORDERING regression guard (kept): surface.test.ts asserts live glass tint opacity >= fallback tint token opacity per package, so the rendered tint is never more translucent than the AA-checked fallback. Opacity ordering alone is NOT sufficient (contrast depends on tint colour + foreground + actual pixels) — the composited check is the real proof. LIMITATION (REVIEWS 23-06 cycle-4 MEDIUM): guard (1) validates the DECLARED worst-case pixel only; it does NOT decode the committed `.webp`, so the on-device per-asset DEVICE-UAT (Task 2 / 23-VALIDATION.md Manual-Only) is the check against the SHIPPED bytes. The composited unit test remains the fast regression guard; a build-time `.webp`-luminance gate is an optional future enhancement (Phase 15).
    - Surface-token-only selector (cycle-3 LOW, finding #4): surface.ts exposes a pure `resolveSurfaceStyle(package, blurAvailable)` returning the glass/flat surface style drawn only from declared SURFACE token fields; surface.test.ts asserts every value it returns is a member of the declared SURFACE token set (no ad-hoc literal), and GlassSurface consumes it so the /theme/ colour-gate exemption cannot become an escape hatch.
    - Both modules are node-testable; any colour lives under /theme/.
  </behavior>
  <action>
    Create `src/theme/backgrounds.ts` — the per-package background slot manifest (~4-5 + None/Solid) keyed by the `BACKGROUND_SLOT_IDS` IMPORTED from Plan 01's `src/theme/theme-option-ids.ts` (single source — do NOT re-declare a parallel slot-id list, REVIEWS 23-01 cycle-4 MEDIUM), mapping slot ids to local `require()` assets, a declared worst-case (brightest representative) pixel per bundled asset (cycle-3 MEDIUM — the composited-AA bound the shipped asset must not exceed; recorded beside its README provenance row, with a comment that this DECLARED pixel is not decoded from the committed `.webp` and is enforced against the shipped bytes by the per-asset device-UAT — REVIEWS 23-06 cycle-4 MEDIUM), and `resolveBackground(package, slotId | null)` (NULL -> package default; 'none' -> solid theme background). ALSO export `resolveRenderableBackground(package, slotId | null, renderFailed)` — a pure reducer that returns None/Solid when `renderFailed` is true and otherwise `resolveBackground`'s result (REVIEWS 23-06 LOW test seam). Bundle the background assets locally under `assets/backgrounds/` (one committed image file per non-None/Solid slot the manifest declares — the files listed in files_modified) and create `assets/backgrounds/README.md`, an in-project provenance/license manifest with a row per asset (source, licence, author, AND its declared worst-case brightest pixel) covering every slot the manifest resolves via `require()` (REVIEWS 23-06 MEDIUM). Create `src/theme/tokens/surface.ts` — per-package surface treatment tokens (opacity-by-density steps + glass/flat flag + the semi-opaque tinted blur-fallback surface token AND the live glass tint colour + live glass tint opacity per package) PLUS a pure, node-testable `resolveSurfaceStyle(package, blurAvailable)` selector that returns the glass/flat surface style (tint colour, live tint opacity, border, glow) drawn ONLY from declared SURFACE token fields — so GlassSurface consumes the selector instead of inventing component-local values, closing the /theme/-exemption escape hatch (cycle-3 LOW, finding #4). Write `backgrounds.test.ts` (slot ordering stable, NULL->default, 'none'->solid, adjacency no-op, AND resolveRenderableBackground: renderFailed=true -> None/Solid, renderFailed=false -> the resolved slot, AND the DRIFT test: the manifest's declared slot-id set equals the imported `BACKGROUND_SLOT_IDS` exactly — no accepted id without a manifest slot, no manifest slot outside the accepted set, REVIEWS 23-01 cycle-4 MEDIUM) and `surface.test.ts` covering: denser density -> higher opacity; the Plan 03 handoff (text/status foregrounds over the tinted fallback surface token pass AA via contrast.ts in all four palettes); the OPACITY-ORDERING regression guard (live glass tint opacity >= fallback tint token opacity per package — REVIEWS 23-06 MEDIUM); the COMPOSITED per-asset AA proof (cycle-3 MEDIUM — every text/status foreground over alphaComposite(live glass tint @ live tint opacity, each asset's declared brightest pixel) meets AA-equivalent contrast via contrast.ts, per asset/package/mode); and the surface-token-only selector guard (cycle-3 LOW — every value `resolveSurfaceStyle` returns is a member of the declared SURFACE token set for that package, so no ad-hoc literal can leak through a theme component). Keep all colour under /theme/.
  </action>
  <verify>
    <automated>npx vitest run src/theme/backgrounds.test.ts src/theme/tokens/surface.test.ts</automated>
    <automated>npm run check:colors src/theme/backgrounds.ts src/theme/tokens/surface.ts</automated>
    <automated>test -f assets/backgrounds/README.md && echo bg-provenance-ok</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/backgrounds.test.ts src/theme/tokens/surface.test.ts` passes covering stable slot order, NULL->default, 'none'->solid, same-selection no-op, resolveRenderableBackground (renderFailed=true -> None/Solid, false -> resolved slot), the slot-id drift test (manifest slot-id set === imported `BACKGROUND_SLOT_IDS`, no re-declared parallel list — REVIEWS 23-01 cycle-4 MEDIUM), density->opacity monotonicity, the glass-composite AA check over the tinted fallback surface token in all four palettes, the live-glass opacity-ordering guard (live glass tint opacity >= fallback tint token opacity per package — REVIEWS 23-06 MEDIUM), the COMPOSITED per-asset AA proof (every text/status foreground over the live glass tint composited on each bundled asset's declared brightest pixel meets AA — cycle-3 MEDIUM), and the surface-token-only selector guard (`resolveSurfaceStyle` returns only declared SURFACE token values — cycle-3 LOW, finding #4).
    - Every background slot resolves to a local require() asset or the solid token (no remote URL); each non-None/Solid slot has a committed asset under `assets/backgrounds/`, a declared worst-case brightest pixel in backgrounds.ts, and a provenance row (incl. that pixel) in `assets/backgrounds/README.md` (REVIEWS 23-06 MEDIUM + cycle-3 MEDIUM).
    - `npm run check:colors` exits 0 for both files.
  </acceptance_criteria>
  <done>The background slot manifest (bundled, None/Solid, per-package defaults) and per-package surface tokens exist as pure, node-tested data.</done>
</task>

<task type="auto">
  <name>Task 2: BackgroundHost + GlassSurface with graceful fallbacks</name>
  <files>src/components/ui/BackgroundHost.tsx, src/components/ui/GlassSurface.tsx</files>
  <read_first>
    - src/theme/backgrounds.ts + tokens/surface.ts (Task 1)
    - src/theme/theme-provider.tsx / src/stores/theme-store.ts (active package + stored background slot-id from Plan 01)
    - 23-UI-SPEC.md Surface & Glass + Copywriting Contract (asset-fail -> None/Solid; blur unaffordable -> tinted token; both silent)
    - package.json (expo-blur from Plan 02)
    - an existing themed screen root using useTheme() for the fixed-background layering approach
  </read_first>
  <action>
    Create `src/components/ui/BackgroundHost.tsx` — renders the active package's resolved background fixed behind scrollable content (never scrolling with it), applying density-driven surface opacity so dense screens stay readable. Asset PRESENCE is guaranteed at bundle time (every slot resolves to a real require()); the runtime fallback catches an image RENDER failure (Image/Skia onError, decode error, null image) — NOT a bundle-missing module (which fails at Metro resolution, not recoverably at runtime, REVIEWS 23-06 MEDIUM) — by holding an onError boolean in component state and feeding it into `resolveRenderableBackground(package, slotId, renderFailed)` (Task 1), so the effective background silently becomes None/Solid on render failure and the fallback logic is the node-tested pure reducer rather than untested inline component branching (REVIEWS 23-06 LOW). If a Galaxy background animates, its motion is a Skia/Reanimated worklet multiplying the Plan 04 `ambient` SPEED token and gating on the reduced-motion SharedValue via useDerivedValue (collapse to a static frame when ON); a static background ships with no worklet. Create `src/components/ui/GlassSurface.tsx` — one semantic surface component that renders glass-forward for Galaxy (expo-blur where affordable, luminous border/glow via tokens) and flatter/opaque for Standard, falling back to a semi-opaque tinted surface token when blur is unavailable. GlassSurface derives ALL of its surface style from the pure `resolveSurfaceStyle(package, blurAvailable)` selector (Task 1) and reads colour only via useTheme()/theme tokens — it never introduces a component-local colour or opacity literal, so the /theme/ colour-gate exemption cannot become an escape hatch (cycle-3 LOW, finding #4; the selector's token-only output is node-tested in Task 1). The live translucent tint opacity GlassSurface applies (from resolveSurfaceStyle) MUST be >= the fallback tint token's opacity (the AA-checked bound from surface.ts), so the rendered glass composite is never more translucent than the surface the AA gate validated (REVIEWS 23-06 MEDIUM live-glass invariant). No network path.
    Create `src/components/ui/__dev__/ThemePreviewScreen.tsx` — a DEV-ONLY preview harness that is the in-scope MOUNT POINT for this plan's device-UATs (REVIEWS 23-06 cycle-5 MEDIUM: app-wide BackgroundHost mounting is deferred to Phase 15/37, so without this harness the human-checks below have nowhere to run in-phase). It mounts `BackgroundHost` with a long `ScrollView` of representative body + caption text over `GlassSurface`, with dev controls to cycle package (galaxy|standard), mode (light|dark), and every background slot (incl. None/Solid) and to force the BackgroundHost onError fallback — so the fixed-behind-scroll, density-opacity, glass-vs-flat, onError→None/Solid, blur-fallback, and per-asset brightest-region AA checks are all exercisable on the Pixel. Reachable via a dev-only route/entry (not shipped in a release build, no new user-facing nav); it introduces no production mount and does not reverse the deferred-adoption scope boundary. All colour via tokens; no network path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors src/components/ui/BackgroundHost.tsx src/components/ui/GlassSurface.tsx</automated>
    <human-check>On a Pixel (device-UAT, end-of-phase), mounted via the dev-only `ThemePreviewScreen` harness (the in-scope mount point, since app-wide adoption is deferred to Phase 15/37): background stays fixed while content scrolls in all four combos; dense forms stay readable (higher opacity); Galaxy shows glass, Standard shows flat; a removed asset falls back to None/Solid with no error; None/Solid renders the plain themed background. PER-ASSET BRIGHTEST-REGION AA (REVIEWS 23-06 cycle-4 MEDIUM — the ONLY check against the SHIPPED `.webp` bytes; the composited unit test validates only the declared worst-case pixel, not the decoded image): for EACH Galaxy background asset, place body + caption text over glass on the asset's visibly brightest region and confirm it stays legible; a failure means the shipped asset exceeds its declared worst-case pixel and must be re-mastered/darkened or its declared pixel + tint opacity retuned (never weaken AA).</human-check>
  </verify>
  <acceptance_criteria>
    - BackgroundHost resolves the active background via backgrounds.ts, renders it fixed (grep confirms a fixed/absolute layer behind scroll content), applies density opacity, and drives its onError state through `resolveRenderableBackground` so it falls back to None/Solid on an image RENDER failure (grep confirms the reducer call fed by onError; the transition itself is covered by the Task 1 node test), with asset presence validated at bundle time.
    - GlassSurface renders glass (Galaxy) vs flat (Standard) via one API and falls back to a tinted token when blur is unavailable; it derives its surface style from the `resolveSurfaceStyle` selector and reads colour only via useTheme() (grep confirms it consumes resolveSurfaceStyle / surface tokens and carries no component-local colour or opacity literal — cycle-3 LOW, finding #4); the live tint opacity it applies is >= the fallback tint token opacity (grep/tsc confirms it reads the surface.ts live-tint token, not an ad-hoc lower opacity — REVIEWS 23-06 MEDIUM).
    - Any animated Galaxy background gates on the reduced-motion SharedValue via useDerivedValue (grep confirms the SharedValue read in the worklet; no per-frame setState); a static background has no animation worklet.
    - No hardcoded colour and no network/CDN reference (grep); `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Backgrounds render fixed-behind-scroll with density opacity and None/Solid + asset-fail fallback, and the glass/flat surface API degrades blur gracefully.</done>
</task>

<task type="auto">
  <name>Task 3: Gate ALL Orrery clock consumers on reduced motion (THEME-12/THEME-06)</name>
  <files>src/components/orrery/OrreryCanvas.tsx, src/components/orrery/SunBody.tsx</files>
  <read_first>
    - src/components/orrery/OrreryCanvas.tsx:93-101 — the existing useClock + useDerivedValue twinkle/drift loop to gate; how the ambient clock is provided to children
    - src/components/orrery/SunBody.tsx:82-100 — pulseGlowRadius/pulseGlowOpacity derived from useOrreryClock() (the SECOND clock consumer that must also gate — REVIEWS 23-06 HIGH)
    - src/theme/use-reduced-motion.ts (Plan 04 useReducedMotionShared) and src/theme/tokens/motion.ts (the `ambient` SPEED token the worklet multiplies)
    - 23-UI-SPEC.md Motion Tokens & Reduced Motion + Orrery Visual Exception
    - src/screens/OrreryScreen.tsx (how OrreryCanvas is mounted/paused on blur; the pause-on-blur unmount pattern)
    - CLAUDE.md animation rules (never drive Skia from React state; all colour via tokens incl Skia)
  </read_first>
  <action>
    Audit EVERY Orrery clock consumer and gate each on `useReducedMotionShared()`. In `src/components/orrery/OrreryCanvas.tsx`, gate the ambient twinkle/drift inside the existing `useDerivedValue` worklets so motion collapses to a constant when reduced motion is ON. In `src/components/orrery/SunBody.tsx`, gate the sun glow pulse (pulseGlowRadius/pulseGlowOpacity) the same way so the glow holds at its static mid-point when reduced motion is ON — SunBody.tsx calls `useReducedMotionShared()` DIRECTLY and reads it inside its worklet via `useDerivedValue`, never a React boolean or per-frame setState. Do NOT thread the SharedValue via a prop or the OrreryClock context: SunBody is instantiated before it is handed to OrreryCanvas as children (OrreryScreen.tsx:685-700), so the prop route is architecturally impossible, and orrery-clock-context.ts carries only the clock (:20-27) — routing through it would edit orrery-clock-context.ts/OrreryScreen.tsx, which are NOT in this plan's files_modified. The direct hook call keeps the fix inside SunBody.tsx (already in files_modified). (REVIEWS 23-06 cycle-4) Confirm the Orrery continues to resolve every colour through tokens (D-06) and follows the active theme package. Do not change the pause-on-blur unmount behavior.
  </action>
  <verify>
    <automated>npm run check:colors src/components/orrery/OrreryCanvas.tsx src/components/orrery/SunBody.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On a Pixel (device-UAT, end-of-phase — perf/Skia claims are physical-device only, not the emulator): with OS reduced motion ON, Orrery drift/twinkle AND the sun glow pulse both stop (and any Galaxy background motion) while manual camera control still works; toggling reduced motion mid-session updates live without an app restart; Galaxy(glass) vs Standard(flat) surfaces compared on-device; Orrery colours follow the active package.</human-check>
  </verify>
  <acceptance_criteria>
    - Both OrreryCanvas AND SunBody read the reduced-motion SharedValue via useDerivedValue and gate their ambient motion (grep confirms the SharedValue read inside each loop; no new setState in either animation path).
    - Orrery colours all resolve through tokens (`npm run check:colors` clean on both files).
    - `npx tsc --noEmit` clean. (Full `npm test` runs at wave merge — not re-run in this task.)
  </acceptance_criteria>
  <done>Every Orrery clock consumer (OrreryCanvas twinkle/drift AND SunBody glow pulse) honors the live reduced-motion signal from the Skia loop without setState, still tokenized and package-following — the sun no longer pulses under reduced motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored background slot-id -> render | An unknown/missing background must fall back, not crash |
| bundled asset -> render | A failed asset load must degrade to None/Solid |
| OS reduced-motion signal -> Skia loop | Live gating without a re-render storm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05b | Denial of Service | missing/failed background asset | low | mitigate | Asset presence validated at bundle time; a runtime image RENDER failure (onError) silently falls back to None/Solid; assertBackgroundId (Plan 01) rejects unknown slot ids on write |
| T-23-06b | (accessibility invariant) | the sun glow pulse ignoring reduced motion | low | mitigate | SunBody.tsx gated on the same reduced-motion SharedValue as OrreryCanvas (all clock consumers audited) so no ambient motion continues under reduced motion (REVIEWS 23-06 HIGH) |
| T-23-09 | Denial of Service | expensive blur on a weak device | low | mitigate | expo-blur degrades to a semi-opaque tinted surface token; blur carries no required meaning |
| T-23-11d | (accessibility invariant) | text dropping below AA on live glass over a bright bundled background | medium | mitigate | TWO guards: (1) COMPOSITED per-asset AA test (cycle-3 MEDIUM) — backgrounds.ts declares each asset's worst-case brightest pixel and surface.test.ts asserts every text/status foreground over alphaComposite(live glass tint, that pixel) meets AA; (2) opacity-ordering regression guard — surface.test.ts asserts live glass tint opacity >= fallback tint token opacity per package and GlassSurface applies the >= tint at render, so the AA-checked fallback also bounds the composite (REVIEWS 23-06 MEDIUM); (3) per-asset DEVICE-UAT against the SHIPPED `.webp` bytes (REVIEWS 23-06 cycle-4 MEDIUM) — guards (1)/(2) validate only the DECLARED worst-case pixel, so the on-device brightest-region text-over-glass check (23-VALIDATION.md Manual-Only) is the enforcement against the decoded image; an optional build-time `.webp`-luminance gate is a Phase-15 candidate. Opacity ordering alone is insufficient; the composited check is the real regression proof and the device-UAT is the shipped-bytes proof |
| T-23-15 | Tampering | a theme surface component slipping an ad-hoc colour/opacity literal past the check:colors /theme/ exemption | low | mitigate | GlassSurface derives style only from the pure resolveSurfaceStyle selector; a node test asserts the selector returns only declared SURFACE token values, so token-only assurance comes from the test, not the location-based colour gate (cycle-3 LOW, finding #4) |
| T-23-10 | (local-first invariant) | a network/CDN reference sneaking into a background path | medium | mitigate | Backgrounds are local require() assets only; grep + check:colors confirm no remote URL |
| T-23-S/T/R/I/E | Spoofing/Tampering/Repudiation/Info/Elevation | — | n/a | accept | Presentational rendering of local assets; local-first, no network on any read path, no auth surface |
</threat_model>

<verification>
- `npx vitest run src/theme/backgrounds.test.ts src/theme/tokens/surface.test.ts` green; `npm test` + `npm run check:colors` + `npx tsc --noEmit` clean at wave merge.
- Device UAT (end-of-phase Pixel): fixed backgrounds + density opacity + glass/flat per package + None/Solid + asset-fail fallback; reduced motion stops Orrery/background ambient motion live.
</verification>

<success_criteria>
Curated bundled backgrounds (+ None/Solid, fixed while scrolling, opacity by density, render-failure fallback), per-package glass/flat surfaces with graceful blur fallback and AA-checked composite, and an Orrery where EVERY clock consumer (canvas twinkle/drift + sun glow pulse) honors live reduced motion — satisfying THEME-04/05/12 and consuming the Plan 04 hook.
</success_criteria>

## Artifacts this phase produces

New files: `src/theme/backgrounds.ts` (+ `.test.ts`), `src/theme/tokens/surface.ts` (+ `.test.ts`), `src/components/ui/BackgroundHost.tsx`, `src/components/ui/GlassSurface.tsx`; new background assets under `assets/backgrounds/` (one image file per non-None/Solid slot: galaxy-nebula/galaxy-deep-space/galaxy-aurora/galaxy-starfield + standard-dawn/standard-dusk/standard-mesh/standard-paper) + `assets/backgrounds/README.md` provenance manifest.
New exported symbols: `BACKGROUND` slot manifest (keyed by the imported `BACKGROUND_SLOT_IDS` single source from Plan 01's theme-option-ids.ts, not a re-declared list; + per-asset worst-case brightest pixel, declared-not-decoded and enforced against shipped bytes by the per-asset device-UAT) + `resolveBackground(package, slotId|null)` + `resolveRenderableBackground(package, slotId|null, renderFailed)` (pure onError->None/Solid reducer); per-package `SURFACE` tokens (opacity-by-density + glass/flat flag + tinted blur-fallback token + live glass tint colour + live glass tint opacity) + `resolveSurfaceStyle(package, blurAvailable)` pure selector (token-only surface style); `BackgroundHost`, `GlassSurface` components; `ThemePreviewScreen` (dev-only preview harness — the in-scope mount point for the device-UATs; not shipped in release, no production mount, does not reverse the deferred-adoption boundary).
Changed files: `src/components/orrery/OrreryCanvas.tsx` (twinkle/drift gated), `src/components/orrery/SunBody.tsx` (glow pulse gated) on the reduced-motion SharedValue.

<output>
Create `.planning/phases/23-theme-visual-system/23-06-SUMMARY.md` when done.
</output>
