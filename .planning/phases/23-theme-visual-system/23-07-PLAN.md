---
phase: 23-theme-visual-system
plan: 07
type: execute
wave: 4
depends_on: ["23-02", "23-05"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Button.test.ts
  - src/components/ui/Modal.tsx
  - src/components/ui/Sheet.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [THEME-10]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "THEME-10 / dossier §P: buttons follow a formal hierarchy — Primary (filled accent + onAccent text, one per surface), Secondary (tonal/outline on surface, textPrimary), Tertiary/text (accent or textPrimary), Destructive, Icon-only (44px min target, semantic registry glyph, accessibilityLabel required). One shared Button API, colour via useTheme() tokens."
    - "THEME-10 / dossier §P: destructive actions are distinct BEYOND colour — the Destructive role pairs the danger token with a non-colour cue (warning glyph via the icon registry) AND is confirmed through an explicit ConfirmDialog naming the action; colour alone is never the only destructive cue."
    - "THEME-10 / dossier §O: modals/sheets come from shared variants — Compact sheet, Detail/half-height sheet, Full-screen modal, Confirmation dialog — sharing radius language (radius.xl top corners), scrim (colors.background at opacity, never a colour literal), safe-area, spacing, typography, and drag-handle conventions."
    - "Icon-only buttons meet the 44x44 minimum touch target (a11y floor) and render their glyph through the semantic icon registry (Plan 05) with a required accessibilityLabel."
    - { statement: "THEME-10 (unclassified edge — flagged planner assumption, not auto-dismissed): exactly one Primary per surface is a convention the Button API expresses via the role prop (Primary/Secondary/Tertiary/Destructive/IconOnly), not an enforced runtime singleton; the sheet variant set is the fixed four (compact/detail/full/confirm). Flagged for verify.", verification: backstop }
    - { statement: "THEME-10 (empty edge): a Button with no label but a role requires either children or (for IconOnly) an icon + accessibilityLabel; IconOnly without an accessibilityLabel fails the a11y contract (asserted).", verification: explicit }
    - { statement: "THEME-10 (a11y edge): every button variant meets the 44x44 minimum touch target and every icon-only button carries an accessibilityLabel; the scrim resolves from colors.background at opacity, never a hex.", verification: explicit }
  artifacts:
    - "src/components/ui/Button.tsx (+ .test.ts) — Button with role variants (Primary/Secondary/Tertiary/Destructive/IconOnly); token colour; 44px min target; registry glyph for icon-only"
    - "src/components/ui/Modal.tsx — full-screen modal variant (radius.xl top corners, scrim, safe-area)"
    - "src/components/ui/Sheet.tsx — compact + detail/half-height sheet variants (drag handle, shared radius/scrim)"
    - "src/components/ui/ConfirmDialog.tsx — confirmation dialog variant; destructive confirmations name the action + warning glyph"
    - "src/components/ui/index.ts — barrel exports for the UI primitives"
  key_links:
    - "useTheme().colors.accent/onAccent/danger + radii/spacing tokens (Plan 02) -> Button/Modal/Sheet variants"
    - "Destructive Button -> ConfirmDialog (explicit confirmation) + warning glyph (icon registry, Plan 05) -> distinct-beyond-colour"
    - "Icon-only Button -> icon registry semantic name (Plan 05) + accessibilityLabel + 44px target"
  prohibitions:
    - "Do NOT convey a destructive action by colour alone — pair the danger token with a warning glyph AND an explicit confirmation dialog naming the action (THEME-10, dossier §P)."
    - "Do NOT use a colour literal for a scrim or any surface — scrim is colors.background at opacity; all colour via useTheme() tokens (check:colors)."
    - "Do NOT import a third-party icon name in a button — icon-only buttons use the semantic registry (Plan 05); one icon source (D-05)."
    - "Do NOT reintroduce pervasive card-boxing — buttons/sheets are meaningful conceptual units, not a card wrapper for everything (dossier §K, [theme -> Dashboard/Profile/Settings])."
    - "Do NOT ship an icon-only button without an accessibilityLabel or below the 44x44 touch target."
---

<objective>
Deliver the shared modal/sheet variants and the formal button hierarchy so consuming phases inherit consistent, accessible action and overlay primitives with destructive actions distinct beyond colour.

Purpose: satisfy THEME-10 (button hierarchy + modal/sheet variants; destructive beyond colour) as reusable primitives, consuming the tokens (Plan 02) and the icon registry (Plan 05).
Output: Button (role variants), Modal, Sheet, ConfirmDialog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-theme-visual-system/23-CONTEXT.md
@.planning/phases/23-theme-visual-system/23-UI-SPEC.md
@.planning/phases/23-theme-visual-system/23-02-SUMMARY.md
@.planning/phases/23-theme-visual-system/23-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Button hierarchy with destructive-beyond-colour</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Button.test.ts, src/components/ui/index.ts</files>
  <read_first>
    - 23-UI-SPEC.md Modal / Sheet & Button Hierarchy (the five roles + destructive treatment) and Radii & Geometry
    - src/theme/tokens/radii.ts + spacing.ts (Plan 02) and theme-provider useTheme() colour tokens (accent/onAccent/danger/textPrimary)
    - src/components/icons/Icon.tsx + icon-registry.ts (Plan 05 — icon-only glyph + a warning glyph for destructive)
    - an existing themed pressable/button usage for the touch-target + AppText integration idiom
  </read_first>
  <behavior>
    - Button role in {Primary, Secondary, Tertiary, Destructive, IconOnly}; Primary = filled accent + onAccent text; Secondary = tonal/outline; Tertiary = text-only; Destructive = danger + warning glyph; IconOnly = registry glyph + accessibilityLabel + 44px target.
    - IconOnly without an accessibilityLabel is rejected by the a11y contract (test); every variant meets 44x44.
    - All colour comes from useTheme() tokens.
  </behavior>
  <action>
    Create `src/components/ui/Button.tsx` — one Button API with a `role` prop covering Primary/Secondary/Tertiary/Destructive/IconOnly, resolving colour from `useTheme()` (accent/onAccent/danger/textPrimary), radii/spacing from Plan 02 tokens, label text via AppText, and (for IconOnly and Destructive) glyphs via the Plan 05 icon registry with a required accessibilityLabel and a 44x44 minimum target. Destructive pairs the danger token with a warning glyph. Write `Button.test.ts` asserting the role->treatment mapping, the IconOnly-requires-accessibilityLabel contract, and the 44px floor.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/Button.test.ts</automated>
    <automated>npm run check:colors src/components/ui/Button.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/Button.test.ts` passes covering all five roles, the IconOnly accessibilityLabel requirement, and the 44x44 floor.
    - Destructive role pairs danger + warning glyph (grep confirms a registry warning glyph, not colour alone).
    - Colour only via useTheme() (grep confirms no hex); `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A single Button API expresses the five-role hierarchy with token colour, 44px targets, and destructive-beyond-colour.</done>
</task>

<task type="auto">
  <name>Task 2: Modal / Sheet / ConfirmDialog variants</name>
  <files>src/components/ui/Modal.tsx, src/components/ui/Sheet.tsx, src/components/ui/ConfirmDialog.tsx, src/components/ui/index.ts</files>
  <read_first>
    - 23-UI-SPEC.md Modal / Sheet variants table (compact/detail/full/confirm; radius.xl top corners; scrim = colors.background at opacity; drag handles)
    - src/theme/tokens/radii.ts (radius.xl) + theme-provider useTheme() (scrim from colors.background at opacity — the existing DropdownFieldWidget scrim idiom)
    - src/components/ui/Button.tsx (Task 1 — ConfirmDialog uses Destructive/Secondary buttons)
    - an existing Modal/FlatList picker in the repo (the zero-dependency Modal + scrim pattern, e.g. SettingsScreen sun picker) for the scrim/safe-area convention
  </read_first>
  <action>
    Create `src/components/ui/Modal.tsx` (full-screen modal variant: radius.xl top corners, themed scrim = colors.background at opacity, safe-area), `src/components/ui/Sheet.tsx` (compact + detail/half-height sheet variants sharing the radius/scrim/drag-handle conventions), and `src/components/ui/ConfirmDialog.tsx` (confirmation dialog variant whose destructive path uses the Destructive Button + names the action). Share radius/scrim/spacing/typography across all variants; colour via useTheme() tokens only. Barrel-export the UI primitives from `src/components/ui/index.ts`.
  </action>
  <verify>
    <automated>npm run check:colors src/components/ui/Modal.tsx src/components/ui/Sheet.tsx src/components/ui/ConfirmDialog.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On a Pixel (device-UAT, end-of-phase): each sheet/modal variant renders with the shared radius/scrim/safe-area; a destructive confirmation names the action, shows the warning glyph, and requires explicit confirmation; scrim dims the background without a colour literal.</human-check>
  </verify>
  <acceptance_criteria>
    - Modal/Sheet/ConfirmDialog share radius.xl corners + a scrim resolved from colors.background at opacity (grep confirms no hex scrim).
    - ConfirmDialog's destructive path uses the Destructive Button and names the action.
    - `npm run check:colors` and `npx tsc --noEmit` clean; UI primitives barrel-exported.
  </acceptance_criteria>
  <done>Shared compact/detail/full/confirm overlay variants exist with consistent radius/scrim/safe-area and destructive-confirmation semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| destructive action -> confirmation | An irreversible action must require explicit non-colour-only confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-12 | (safety / accidental-action invariant) | destructive action fired without clear intent | medium | mitigate | Destructive role pairs danger + warning glyph + an explicit ConfirmDialog naming the action (distinct beyond colour); accessibilityLabel required on icon-only |
| T-23-13 | (accessibility invariant) | icon-only button below touch target / unlabeled | low | mitigate | 44x44 minimum enforced; accessibilityLabel required (tested) |
| T-23-S/T/R/I/D/E | Spoofing/Tampering/Repudiation/Info/DoS/Elevation | — | n/a | accept | Presentational primitives; local-first, no network, no auth/data surface |
</threat_model>

<verification>
- `npx vitest run src/components/ui/Button.test.ts` green; `npm run check:colors` + `npx tsc --noEmit` clean; `npm test` green at wave merge.
- Device UAT (end-of-phase Pixel): button hierarchy + modal/sheet variants + destructive-beyond-colour confirmation.
</verification>

<success_criteria>
A formal five-role button hierarchy (destructive distinct beyond colour) and shared compact/detail/full/confirm overlay variants exist as accessible, token-driven primitives — satisfying THEME-10.
</success_criteria>

<output>
Create `.planning/phases/23-theme-visual-system/23-07-SUMMARY.md` when done.
</output>
