---
phase: 30-orrery-systems
plan: 06
type: execute
wave: 5
depends_on: [30-03, 30-08, 30-10]
files_modified:
  - src/screens/SystemsManagementScreen.tsx
  - src/screens/SystemsManagementScreen.test.tsx
  - src/navigation/types.ts
  - src/navigation/tabs/OrreryStack.tsx
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [ORRS-03, ORRS-04, ORRS-09, ORRS-10]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A flat Systems Management screen lists all Systems in management order with All Contacts pinned first, visible, non-deletable and non-renamable (ORRS-09)"
    - "The screen is reachable from a SystemsManagement route registered in both the Orrery stack (from the switcher's Manage Systems entry) and the Settings stack (a Settings row), resolving to the same screen (ORRS-09)"
    - "Create New System and custom-row Edit navigate to the SystemBuilder route, which plan 08 registers in BOTH the Orrery AND Settings stacks — so Create/Edit resolve whether management was opened from the Orrery or from Settings (Codex review 30-06 HIGH, orchestrator-verified real: SystemBuilder was OrreryStack-only). This plan depends on 30-08, so the builder route exists when these actions are wired — no dead TODO seam ships (Codex review 30-06 HIGH: wave ordering exposed dead actions)"
    - "Management supports create, edit, rename, delete (custom), duplicate (all kinds), reorder, hide/show (built-in & Category), and reset membership overrides — every write routed through systems-dao (ORRS-03/09)"
    - "Deleting a custom System shows a simple confirmation, then a short-lived Undo snackbar that restores the System; delete never touches contact data and falls back to All Contacts if the deleted System was active (ORRS-10)"
    - "Active-System fallback and Undo re-select go through the APP-SCOPED selection channel, NOT a direct store.select: SystemsManagement is reachable from the Settings stack and CANNOT reach the OrreryScreen-LOCAL useSystemStore factory instance (OrreryScreen.tsx:214 — verified no provider/context/global handle). So deleting the ACTIVE custom System writes app_settings.orrery_last_system = builtin:all-contacts via the GLOBAL useOrreryPreferencesStore.getState().save(exec,{lastSystem}); OrreryScreen's cross-route observer (30-10) makes that a LIVE fallback while it is mounted AND it persists so a relaunch never restores the dead custom:<uid> ref. Undo restores the System (uid-preserving restoreDeletedSystem) and re-writes lastSystem=custom:<uid> via the same channel — no in-memory store.select from this route (cycle-3 HIGH #1; Codex review 30-06 MEDIUM)"
    - "The active-delete fallback and Undo re-select selection writes are HYDRATE-GUARDED: useOrreryPreferencesStore.save() no-ops while !hydrated (orrery-preferences-store.ts:135) and the ONLY hydration site is OrreryScreen's focus effect (:314), yet SystemsManagement is Settings-stack-reachable — so deleting the active custom System (or Undo-restoring it) BEFORE the Orrery has ever mounted would silently drop the fallback write, leaving a dead custom:<uid> as the persisted last-active with no live/persisted repair (only the 30-01/30-10 relaunch missing-custom fallback would eventually catch it). Therefore each such writer FIRST hydrates: `const prefs = useOrreryPreferencesStore.getState(); if (!prefs.hydrated) await prefs.hydrate(getExecutor());` (idempotent) THEN save({lastSystem}). This guarantees the fallback persists whether or not the Orrery is mounted. Reverses no decision — last-active is a D-05 app_settings preference (cycle-4 HIGH #1)"
    - "The Undo handler catches a FAILED restoreDeletedSystem (e.g. the name was re-taken during the Undo window — systems.name COLLATE NOCASE is a hard unique index and restore preserves the original name) and surfaces a non-destructive message rather than throwing an unhandled error (cycle-3 actionable #8)"
    - "Rename/create enforce case-insensitive uniqueness with the §T error inline and reject an empty name (ORRS-09)"
    - "Built-in and Category rows are non-renamable/non-deletable, show a neutral overrides indicator when customized, and offer Duplicate, Hide/Show, Manage Members, and Reset Overrides; renaming a Category renames its generated System (name is read live) and deleting a Category removes that System while referencing custom rules stay needs-attention (ORRS-03/04)"
    - "Built-in/Category rows expose a Manage Members action (completing [DECIDED] ORRS-03 'override-able with visible indication', dossier §363 'edit … built-in/category-derived membership overrides') that navigates to the SystemBuilder in OVERRIDE-ONLY mode for that ref (navigation.navigate('SystemBuilder', { systemRef }) — resolves in whichever stack hosts this screen, since 30-08 registers SystemBuilder in both). This makes the surfaced overrides indicator + Reset Overrides authorable rather than inert; the read-path application of those overrides is 30-02 (cycle-3 HIGH #2)"
    - { statement: "Reorder is long-press drag with an accent drag preview and a 44-min handle; a failed reorder restores the committed order with feedback", verification: backstop }
    - { statement: "Management rows and titles reflow/scale at the largest supported text size without clipping", verification: backstop }
  artifacts:
    - src/screens/SystemsManagementScreen.tsx
    - SystemsManagement route in OrreryStackParamList and SettingsStackParamList
    - a Systems row in SettingsScreen
  key_links:
    - "SystemsManagementScreen → systems-dao (rename/delete/duplicate/reorder/hide-show/reset) + snackbar-store (Undo) + resolver counts"
    - "switcher Manage Systems entry (plan 05) → navigate('SystemsManagement') → this screen; Settings row → same route"
    - "active-delete/Undo → hydrate-if-needed (idempotent, cycle-4 HIGH #1) → useOrreryPreferencesStore.save({lastSystem}) (app-scoped selection channel; NOT no-op'd because hydrated first) → OrreryScreen observer (plan 10) = live fallback + relaunch persist (NOT the unreachable OrreryScreen-local store.select)"
    - "built-in/Category row Manage Members → navigate('SystemBuilder', { systemRef }) (override-only mode, plan 08); overrides applied at read by plan 02; Reset Overrides → resetSystemOverrides (plan 03)"
  prohibitions:
    - "Never write Systems SQL in the screen — call systems-dao only"
    - "Never let delete touch contact data or use a 30-day quarantine — simple confirm + short-lived Undo (ORRS-10)"
    - "Never allow rename/delete of All Contacts or of built-in/Category base definitions (ORRS-03/09)"
    - "Never hardcode a colour — accent for selected/drag-preview, danger for delete, neutral chrome otherwise, all via useTheme().colors"
    - "Never silently rewrite a broken rule surfaced on a row — offer repair through the editor only (D-07)"
---

<objective>
Build the flat Systems Management screen and its routes: the canonical CRUD/duplicate/reorder/hide-show/override-reset surface, reachable from both the switcher and Settings, with contact-safe delete + Undo and case-insensitive name uniqueness.

Purpose: The dossier separates management (a flat screen) from switching (a dropdown). This screen owns administration; the switcher only switches. Centralizing every mutation here through systems-dao keeps the switcher lean and the invariants enforced.

Output: SystemsManagementScreen, route registration in the Orrery + Settings stacks, and a Settings entry — all writes through systems-dao, Undo through snackbar-store.

## Artifacts this plan produces
- `src/screens/SystemsManagementScreen.tsx`
- `SystemsManagement` route in `OrreryStackParamList` + `SettingsStackParamList`; Stack.Screen registration in both stacks
- a "Systems" row in `SettingsScreen`

## Review Feedback Incorporated (cycle 4)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-4 HIGH #1 (Settings-origin active-delete selection dropped when prefs store unhydrated):** the active-delete fallback (and the Undo re-select) now hydrate the global preferences store before writing lastSystem (`if (!prefs.hydrated) await prefs.hydrate(getExecutor())`, idempotent), so deleting the active custom System from the Settings stack BEFORE the Orrery mounts still persists the All-Contacts fallback and emits the observer commit — a dead custom:<uid> is never left as the persisted last-active. `save()` no-ops while `!hydrated` (orrery-preferences-store.ts:135) and only OrreryScreen hydrates (:314). No decision reversed — last-active stays a D-05 app_settings preference (Task 3).
- **Cycle-4 LOW (depends_on):** `30-10` is added to `depends_on` so the observer-lands-before-writer ordering (the live fallback relies on it) is encoded in the dependency graph, not incidental to wave arithmetic.

## Review Feedback Incorporated (cycle 3)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-3 HIGH #1 (cross-route active-System selection ownership):** the active-delete fallback and Undo re-select no longer call the unreachable OrreryScreen-local store.select. They write the selection intent via the GLOBAL useOrreryPreferencesStore.save({lastSystem}); OrreryScreen's observer (30-10, wave 3) makes it a live fallback while mounted and it persists for relaunch (Task 3).
- **Cycle-3 HIGH #2 (built-in/Category override authoring entry — completing [DECIDED] ORRS-03):** built-in/Category rows gain a Manage Members action that navigates to SystemBuilder override-only mode ({ systemRef }); this makes the already-surfaced overrides indicator + Reset Overrides authorable instead of inert (the dossier §153/§173/§363 do not defer built-in/Category override authoring). Read-path application is 30-02 (Task 2/3).
- **Cycle-3 actionable #8 (Undo failed-restore):** the Undo handler catches a failed restoreDeletedSystem (name re-taken during the Undo window) and surfaces a non-destructive message rather than throwing (Task 3).

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Codex 30-06 HIGH (Settings-stack builder unreachable, orchestrator-verified real):** plan 08 now registers `SystemBuilder` in BOTH stacks; this plan depends on 30-08 (wave 5), so Create/Edit navigate to a route that resolves from Settings AND Orrery. Verified on disk: OrreryStack.tsx and SettingsStack.tsx are separate navigators.
- **Codex 30-06 HIGH (wave ordering exposed dead actions):** by depending on 30-08, this plan wires Create/Edit against a live route — no dead TODO seam ships (Task 2/3).
- **Codex 30-06 MEDIUM (deletion fallback UI-local):** deleting the active custom System PERSISTS the All-Contacts preference (not just an in-memory select), so a relaunch cannot restore a dead ref; the plan-10 launch missing-custom fallback is the backstop (Task 3).

## Review Feedback Incorporated (cycle 2)
- **Cycle-2 LOW #8 (unclosed objective block):** the objective block is now closed before the execution-context block (all other plans close it; structured-plan tooling relies on the closing tag).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-03-SUMMARY.md
@.planning/phases/30-orrery-systems/30-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the SystemsManagement route + Settings entry</name>
  <files>src/navigation/types.ts, src/navigation/tabs/OrreryStack.tsx, src/navigation/tabs/SettingsStack.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/navigation/types.ts lines 28-150 (OrreryStackParamList 121-142, SettingsStackParamList — the route-name + params conventions; serializable-primitives-only rule)
    - src/navigation/tabs/OrreryStack.tsx and src/navigation/tabs/SettingsStack.tsx (Stack.Screen registration idiom; headerShown:false — each screen owns its Back chrome)
    - src/screens/SettingsScreen.tsx lines 736-1000 (ShellAppBar variant="root"; the settings-row pattern with navigation.navigate; the CustomFields/Archived rows as the analog)
    - src/screens/ArchivedContactsScreen.tsx lines 41, 99 (a child-route screen with a goBack Back control — the chrome analog)
  </read_first>
  <action>
Add `SystemsManagement: undefined` to both `OrreryStackParamList` and `SettingsStackParamList` in navigation/types.ts. Register `<Stack.Screen name="SystemsManagement" component={SystemsManagementScreen} />` in both OrreryStack.tsx and SettingsStack.tsx. Add a "Systems" row to SettingsScreen (mirroring the CustomFields/Archived row pattern) that calls `navigation.navigate("SystemsManagement")`, using the semantic Icon registry and theme tokens only.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "SystemsManagement" || echo "no route type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SystemsManagement" src/navigation/types.ts` shows it in both param lists
    - `grep -rn "name=\"SystemsManagement\"" src/navigation/tabs/` shows registration in both OrreryStack and SettingsStack
    - `grep -n "SystemsManagement" src/screens/SettingsScreen.tsx` shows the navigate call
  </acceptance_criteria>
  <done>The SystemsManagement route resolves from both the Orrery and Settings stacks; Settings has a Systems entry.</done>
</task>

<task type="auto">
  <name>Task 2: The flat management list — order, All Contacts pinned, rows, overrides/broken indicators</name>
  <files>src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx</files>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx (a flat list screen with ShellAppBar child chrome + goBack — structural analog)
    - src/db/systems-dao.ts (listCustomSystems, listSystemPrefs, listSystemOverrides — the read surface; §S/§Q semantics from the SUMMARY)
    - src/logic/orrery-system-logic.ts (BUILTIN_SYSTEMS, systemRefId) and src/components/orrery/orrery-controls-logic.ts (buildSystemChoices — the ordered, cross-kind list to reuse)
    - src/logic/system-rule-resolver.ts (resolveCustomSystemMembers → live counts + brokenRules for a row's severity)
    - .planning/phases/30-orrery-systems/30-UI-SPEC.md (§Q/§R/§S/§T row: All Contacts pinned first non-deletable/non-renamable; built-in/Category immutable base + overrides indicator + Hide/Show + Duplicate; custom Create/Edit/Rename/Delete/Duplicate; system-overrides neutral indicator; Copywriting Contract for every label/confirm)
    - src/theme (colors: accent, danger, statusWobble, neutral chrome; SPACING; ICON_SIZE; 44-min targets)
  </read_first>
  <action>
Build SystemsManagementScreen as a flat, scrollable list (ShellAppBar child chrome + goBack, title "Systems"). Compose the ordered row set from buildSystemChoices + listSystemPrefs (management order, All Contacts pinned first). Each row shows the System name, a live member count, and — where applicable — the neutral `system-overrides` indicator (customized built-in/Category), the `system-empty` (statusWobble) empty indicator, or the `system-broken` (danger) needs-attention indicator, always icon shape + text never hue alone. Row actions per kind: All Contacts → none destructive (pinned, no rename/delete/hide) BUT it DOES offer Manage Members (override-only) since it is a built-in that is override-able (only Hide is barred for it); built-in/Category → Duplicate, Hide/Show, Manage Members, Reset Overrides (when overrides exist); custom → Edit (navigate to the SystemBuilder route with the System uid), Rename, Duplicate, Delete (Manage Members is inside the custom Edit builder). The built-in/Category Manage Members action navigates `navigation.navigate("SystemBuilder", { systemRef })` to enter the builder's OVERRIDE-ONLY mode for that ref (cycle-3 HIGH #2, completing [DECIDED] ORRS-03 §363) — so the surfaced overrides indicator + Reset Overrides are authorable, not inert. "Create New System" affordance navigates to SystemBuilder (new). Because this plan now depends on 30-08 (which registers SystemBuilder in BOTH the Orrery and Settings stacks), navigate DIRECTLY — no route-exists guard or TODO seam (the earlier seam is removed; Codex review 30-06 HIGH). Use `navigation.navigate("SystemBuilder", …)` so it resolves in whichever stack (Orrery or Settings) hosts this screen. All colours via theme tokens; 44-min touch targets. Write a component test asserting All Contacts is first + non-deletable, that built-in rows offer no Rename/Delete but DO offer Manage Members (→ SystemBuilder navigate with a systemRef), and that Create/Edit issue a SystemBuilder navigate.
  </action>
  <verify>
    <automated>npx vitest run src/screens/SystemsManagementScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: All Contacts renders first and exposes no Delete/Rename/Hide action
    - Test: a built-in/Category row exposes Duplicate + Hide/Show + Manage Members but not Rename/Delete; its Manage Members issues navigate("SystemBuilder", { systemRef }) (override-only mode)
    - Test: a custom row exposes Rename + Delete + Duplicate + Edit
    - `npm run check:colors` passes (no colour literals)
  </acceptance_criteria>
  <done>The management list renders every System kind in order with correct per-kind actions and distinct overrides/empty/broken indicators.</done>
</task>

<task type="auto">
  <name>Task 3: Mutations wired — rename/delete+Undo/duplicate/reorder/hide-show/reset</name>
  <files>src/screens/SystemsManagementScreen.tsx, src/screens/SystemsManagementScreen.test.tsx</files>
  <read_first>
    - src/db/systems-dao.ts (renameSystem, deleteSystem→DeletedSystemSnapshot, restoreDeletedSystem, duplicateSystem, reorderSystems, setSystemHidden, resetSystemOverrides — signatures from the plan-03 SUMMARY)
    - src/stores/snackbar-store.ts (showSnackbar({kind,label,action:{label,accessibilityLabel,onPress}}); 6s auto-dismiss — the Undo host)
    - src/stores/orrery-system-store.ts (the OrreryScreen-LOCAL system store — its createOrrerySystemStore instance is a useMemo at OrreryScreen.tsx:214 and is UNREACHABLE from this Settings-stack-reachable screen; do NOT call its select from here — cycle-3 HIGH #1)
    - src/stores/orrery-preferences-store.ts (the GLOBAL singleton useOrreryPreferencesStore at :166; save(exec,{lastSystem}) at :133-157 — the APP-SCOPED selection channel: writing lastSystem both persists the fallback AND, via OrreryScreen's observer (plan 10), drives a live switch/fallback. This is the ONLY selection mechanism this screen uses. CRITICAL: save() early-returns Promise.resolve() while !hydrated at :135, and the ONLY hydration call site is OrreryScreen.tsx:314 — since this screen is Settings-stack-reachable, the active-delete/Undo writers MUST call hydrate(getExecutor()) at :108-132 first (idempotent; guards concurrent reads via `reading` at :109) or a cold-path fallback write is dropped — cycle-4 HIGH #1)
    - .planning/phases/30-orrery-systems/30-UI-SPEC.md Copywriting Contract (Delete confirmation "Delete {systemName}? · This only removes the System. Your contacts are not affected. — Delete / Cancel"; "System deleted — Undo"; Reset overrides confirm; Duplicate default name; §T duplicate-name error; empty-name error)
  </read_first>
  <behavior>
    - Delete (custom): confirmation with the exact copy → deleteSystem → showSnackbar "System deleted — Undo"; Undo calls restoreDeletedSystem from the (uid-preserving) snapshot; if the deleted System was active, the active-System fallback runs through the selection channel below.
    - Active-System fallback via the selection channel (cycle-3 HIGH #1): deleting the ACTIVE custom System writes app_settings.orrery_last_system = builtin:all-contacts through the GLOBAL useOrreryPreferencesStore.save({lastSystem}), AFTER hydrating the store if needed (`if (!prefs.hydrated) await prefs.hydrate(getExecutor())` — cycle-4 HIGH #1, else the cold Settings-stack write is a no-op) — this is BOTH the persisted fallback (a relaunch cannot restore the dead custom:<uid> ref) AND, via OrreryScreen's observer (plan 10), the LIVE fallback while the Orrery is mounted. Do NOT call the OrreryScreen-local store.select (unreachable from this route). Undo re-writes lastSystem=custom:<uid> via the same hydrate-then-save channel after a successful restore.
    - Undo failed-restore (cycle-3 actionable #8): if restoreDeletedSystem THROWS (e.g. the original name was re-taken during the Undo window — systems.name COLLATE NOCASE is a hard unique index and restore reuses the original name), the Undo handler CATCHES it and shows a non-destructive snackbar (e.g. "Couldn't undo — that name is in use again") instead of letting the error propagate; no selection write happens on a failed restore.
    - Rename: inline field; CI-collision (cross-catalog) surfaces the §T error, empty name surfaces the empty-name error, both without leaving the row.
    - Duplicate: creates a custom System with the deterministic name and (for custom Edit) opens the builder.
    - Manage Members (built-in/Category): navigates to SystemBuilder override-only mode ({ systemRef }); on return the list re-reads so the overrides indicator reflects any new overrides.
    - Reorder: reorderSystems persists order; a failure restores the committed order with feedback.
    - Hide/Show + Reset Overrides call the DAO and refresh the list.
  </behavior>
  <action>
Wire each row action to its systems-dao call. Delete: render the confirmation copy, call deleteSystem, capture the (portable, uid-preserving) DeletedSystemSnapshot, showSnackbar with an Undo action that calls restoreDeletedSystem inside a try/catch — on a failed restore (name re-taken during the Undo window) show a non-destructive snackbar and take no selection write (cycle-3 actionable #8). If the deleted ref equals the active System, run the fallback ONLY through the selection channel: first hydrate if needed (`const prefs = useOrreryPreferencesStore.getState(); if (!prefs.hydrated) await prefs.hydrate(getExecutor());` — idempotent; mandatory because save() no-ops while !hydrated and only OrreryScreen hydrates, cycle-4 HIGH #1) THEN useOrreryPreferencesStore.getState().save(getExecutor(), { lastSystem: "builtin:all-contacts" }) — do NOT call the unreachable OrreryScreen-local store.select (cycle-3 HIGH #1); OrreryScreen's observer (plan 10) applies it live and it persists so a relaunch does not restore the dead ref. On a successful Undo of the active System, re-write lastSystem=custom:<uid> via the same hydrate-then-save channel. Rename: controlled field validating against the DAO's cross-catalog CI-uniqueness (surface §T + empty-name copy inline). Manage Members (built-in/Category rows): navigate("SystemBuilder", { systemRef }) for override-only authoring; re-read the list on return. Duplicate/Hide-Show/Reset: call the DAO and re-read the list. Reorder: long-press drag committing via reorderSystems, restoring committed order on failure. Keep every mutation off the render path of contact data. Extend the test to assert delete shows the contact-safe confirmation copy and an Undo snackbar wired to restoreDeletedSystem, that deleting the active System writes lastSystem=builtin:all-contacts via the preferences store (the selection channel — mock the preferences store; assert NO OrreryScreen-local store.select), that deleting the active System with the preferences store UNHYDRATED (hydrated=false, Orrery never mounted) calls hydrate BEFORE the fallback save so the write is not dropped (cycle-4 HIGH #1), that a failed restoreDeletedSystem surfaces a non-destructive message and does not throw, and that a CI-colliding rename surfaces the §T error inline.
  </action>
  <verify>
    <automated>npx vitest run src/screens/SystemsManagementScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: delete renders the exact contact-safe confirmation copy and fires an Undo snackbar wired to restoreDeletedSystem
    - Test: deleting the active System writes orrery_last_system = builtin:all-contacts via the preferences-store selection channel and does NOT call an OrreryScreen-local store.select (cycle-3 HIGH #1)
    - Test: deleting the active System with the preferences store UNHYDRATED (hydrated=false) calls hydrate BEFORE the fallback save, so the All-Contacts fallback persists and is not dropped by the !hydrated no-op (cycle-4 HIGH #1)
    - Test: a failed restoreDeletedSystem (name re-taken) surfaces a non-destructive message instead of throwing (cycle-3 actionable #8)
    - Test: a built-in/Category Manage Members action navigates to SystemBuilder with a systemRef (override-only authoring, cycle-3 HIGH #2)
    - Test: a cross-catalog CI-colliding rename surfaces the §T error inline
    - `npm test` green
  </acceptance_criteria>
  <done>Every management mutation routes through systems-dao with contact-safe delete + Undo and the fallback behavior; the screen is fully operable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| management UI → systems-dao | names/refs/order cross into SQL through the DAO (no inline SQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-15 | Tampering | rename/create input | medium | mitigate | Validation delegated to systems-dao (CI-unique + empty-name); no inline SQL; values ?-bound in the DAO |
| T-30-16 | Repudiation/data loss | delete losing a System without recourse | medium | mitigate | Simple confirm + short-lived Undo via restoreDeletedSystem from the DeletedSystemSnapshot; delete never touches contacts |
| T-30-17 | Elevation of privilege | editing an immutable base | high | mitigate | Built-in/Category/All Contacts rows expose no Rename/Delete; the DAO also rejects such writes (defense in depth) |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install in this phase (RESEARCH: zero new dependencies); if any surfaces, gate behind checkpoint:human-verify |
</threat_model>

<verification>
- `npm test` green; the screen suite covers order/pinning, per-kind actions, contact-safe delete+Undo, active-delete fallback, CI-rename error.
- `grep -rn "INSERT INTO system\|UPDATE system\|DELETE FROM system" src/screens/SystemsManagementScreen.tsx` returns nothing.
- `npm run check:colors` and Biome pass.
- Device-verify (end-of-phase): reorder drag handle + largest-text row layout.
</verification>

<success_criteria>
The flat management screen delivers ORRS-03/04/09/10 CRUD/duplicate/reorder/hide-show/reset with All Contacts pinned, contact-safe delete + Undo, and CI-unique names — reachable from both entry points.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-06-SUMMARY.md` — record the route name, the Edit→builder navigation seam plan 08 fills, the Undo/restore wiring, and the hydrate-before-save guard on the active-delete/Undo selection writes (cycle-4 HIGH #1).
</output>
