---
phase: 30-orrery-systems
plan: 08
type: execute
wave: 4
depends_on: [30-02, 30-03, 30-07, 30-10]
files_modified:
  - src/screens/SystemBuilderScreen.tsx
  - src/screens/SystemBuilderScreen.test.tsx
  - src/components/orrery/SystemRuleAccordion.tsx
  - src/components/orrery/system-builder-logic.ts
  - src/components/orrery/system-builder-logic.test.ts
  - src/navigation/types.ts
  - src/navigation/tabs/OrreryStack.tsx
  - src/navigation/tabs/SettingsStack.tsx
autonomous: true
requirements: [ORRS-01, ORRS-02, ORRS-03, ORRS-05, ORRS-08]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "System authoring is a multi-page floating Orrery HUD (Definition → Manage Members → Preview) fully operable without touching the canvas. Per the recorded presentation model (dossier §K [DECIDED] '`The builder appears over a fresh/canonical Orrery canvas`', §N [DECIDED] '`the Orrery canvas displays the provisional System`' in Preview, and UI-SPEC §K), SystemBuilderScreen OWNS its own background Orrery canvas — it is NOT a transparent modal over the live OrreryScreen (the stacks contain no transparentModal; SystemBuilder is a normal opaque Stack.Screen, and OrrerySystemSelector — the only 'HUD over the live Orrery' precedent — is a component inside OrreryScreen, not a pushed route, so that model does not transfer). There is therefore no production Orrery behind the builder to make inert; the a11y-inert target is the builder's OWN background canvas: while a HUD page (Definition/Manage Members) is open that canvas is decorative, non-interactive, and removed from accessibility focus (accessibilityElementsHidden / importantForAccessibility='no-hide-descendants'), and entering Preview (plan 09) renders the provisional System into that same canvas and moves focus to the Preview controls (Codex review cycle-2 HIGH #2; §Y) (ORRS-05)"
    - "The SystemBuilder route is registered in BOTH OrreryStackParamList/OrreryStack AND SettingsStackParamList/SettingsStack (with serializable-only params { systemUid?: string }), so management's Create/Edit resolves from either stack (Codex review 30-06 HIGH — SystemBuilder was OrreryStack-only)"
    - "The Definition page has Back/Cancel, a System name field, accordion rule families over the eight axes (Category, Favorite, Status/Needs-Attention, Gravity, Social Battery, Contact Frequency, Not Contacted, Snoozed; Birthday omitted), a live match count, Manage Members, and Save (ORRS-05)"
    - "Accordion headers summarize active values by meaningful label not only counts (e.g. 'Category · Friends, Community'); rules combine OR within a family, AND across families (ORRS-01/05)"
    - "Manage Members is available even with no rules selected, enabling manual-only Systems (ORRS-01/05)"
    - "Rules reuse the canonical shared predicate logic and do NOT share Dashboard live UI/query state (ORRS-01)"
    - "The live match count on the Definition page is computed from the db-facing draft wrapper resolveDraftMembership(exec, { rules, overrides, now }) (30-02, in src/db/orrery-system-read.ts) over the CURRENT unsaved draft — a stored-System resolver cannot count a new/changed draft that has no persisted rows yet (Codex review 30-08 HIGH), and the builder must NOT call the logic-layer resolveMembershipFromDefinition directly (its gravityInputsFor loader is a REQUIRED 4th param; resolveDraftMembership constructs it from readOrreryImpactInputsCore so a gravity-rule draft's count actually resolves — cycle-4 HIGH #3). The call is debounced and does not rebuild the builder's background canvas"
    - "The embedded Manage Members grid is seeded with resolveDraftMembership(exec, draft).candidateIds (the pre-override rule-candidate set returned by 30-02's draft wrapper) — not memberIds and not a re-derived match set — so exclude-in-place renders correctly for the current draft, including a gravity-rule draft whose candidate set depends on the constructed loader (Codex review cycle-2 HIGH #1; cycle-4 HIGH #3)"
    - "Save is ATOMIC: it calls the single saveSystemDefinition (one inWriteTransaction composing create/rename + rules + override replacement + stale-exclusion prune) — never composing multiple public non-reentrant DAO writes (Codex review 30-08 HIGH, orchestrator-verified real)"
    - "The switch on save-new is performed through the APP-SCOPED selection channel, NOT a direct store.select: SystemBuilder is reachable from BOTH stacks (incl. Settings) and CANNOT reach the OrreryScreen-LOCAL useSystemStore factory instance (createOrrerySystemStore via useMemo at OrreryScreen.tsx:214 — verified no provider/context/global handle). So after the transaction commits, save-new writes the selection intent via the GLOBAL useOrreryPreferencesStore.getState().save(exec,{lastSystem:newRefId}); OrreryScreen's cross-route observer (30-10) turns that committed change into a live switch AND it persists for relaunch — one mechanism (cycle-3 HIGH #1)"
    - "The selection write is HYDRATE-GUARDED so it is not silently dropped on a cold Settings path: useOrreryPreferencesStore.save() early-returns Promise.resolve() while !hydrated (orrery-preferences-store.ts:135) and the ONLY hydration site is OrreryScreen's focus effect (OrreryScreen.tsx:314). SystemBuilder is reachable from the Settings stack, so a System created BEFORE the Orrery has ever mounted would hit an unhydrated store and the save-new selection write would no-op — the row would commit but neither the durable preference nor the observer-triggering commit would be emitted (ORRS-08 broken, no backstop). Therefore save-new FIRST hydrates: `const prefs = useOrreryPreferencesStore.getState(); if (!prefs.hydrated) await prefs.hydrate(getExecutor());` (hydrate is idempotent — it guards concurrent reads via its own `reading` promise and is a no-op-equivalent when already hydrated) THEN calls useOrreryPreferencesStore.getState().save(getExecutor(),{lastSystem:newRefId}). This persists the last-active preference AND emits the committed change whether or not the Orrery is mounted; when the Orrery later mounts, its focus-effect hydrate reads the persisted value and the 30-10 reconcile-with-committed effect selects it. Reverses no decision — last-active remains a D-05 app_settings preference (cycle-4 HIGH #1)"
    - "Saving a new System switches the Orrery to it (via the selection channel above); editing the ACTIVE System keeps it active and refreshes membership via the data-revision reload OrreryScreen already runs (saveSystemDefinition bumps the revision) — no selection write needed; editing a NON-active System returns to management via goBack without any selection write, so the active System is unchanged (ORRS-08)"
    - "Meaningful unsaved changes route through the shell's useDiscardKeepGuard contract (Discard changes? / Keep editing / Discard) (ORRS-05)"
    - "Manual includes/excludes captured in Manage Members persist through systems-dao (setSystemOverride replacement op) inside the atomic save; stale exclusions are pruned on save via pruneSystemExclusionsCore; Reset Membership Overrides clears both and keeps rules (ORRS-02)"
    - "The builder supports an OVERRIDE-ONLY mode for a built-in/Category ref (entered from 30-06's Manage-Members action on those rows), completing ORRS-03's [DECIDED] 'override-able' half (dossier §153 built-ins, §173 Category-derived, §363 'edit/reset built-in/category-derived membership overrides'): the route accepts a systemRef param (serializable string) as well as systemUid; in override-only mode the System name is READ-ONLY (immutable base name) and the rule accordion is READ-ONLY/hidden (the base predicate is immutable — no create/rename/rule write), while Manage Members + Save + Reset Overrides are active. The embedded grid is seeded with the ref's BASE member id set (the pre-override candidateIds from the shared base-member read 30-02 exposes for built-in/Category — the SAME base predicate the render uses, so builder and canvas cannot diverge), and the live count is |base ∪ eligible-includes ∖ excludes| via the shared applyMembershipOverrides. (ORRS-03)"
    - "Save in override-only mode persists ONLY overrides atomically via the 30-03 override-only composite (setSystemOverride deltas + pruneSystemExclusions in one inWriteTransaction), NEVER saveSystemDefinition (which would create/rename/write rules against an immutable base). Editing overrides on the ACTIVE built-in/Category System refreshes it via the data-revision reload (no selection write); editing a NON-active one returns without switching — so override-only mode never triggers a System switch, consistent with ORRS-08 edit semantics (ORRS-03)"
    - { statement: "Definition page titles, name field, and accordion summaries reflow/scale at the largest supported text size", verification: backstop }
    - { statement: "The builder's own background canvas (a fresh/canonical Orrery per §K) does not rebuild on every rule toggle (§O)", verification: backstop }
  artifacts:
    - src/screens/SystemBuilderScreen.tsx (custom-System authoring + a built-in/Category override-only mode)
    - src/components/orrery/SystemRuleAccordion.tsx
    - src/components/orrery/system-builder-logic.ts
    - SystemBuilder route ({ systemUid?: string; systemRef?: string } | undefined) in OrreryStackParamList AND SettingsStackParamList
  key_links:
    - "SystemRuleAccordion selections → system-builder-logic draft → resolveDraftMembership (30-02 db wrapper, builds the gravity loader) (live count + candidateIds) + systems-dao (save rules/overrides) + ManageMembersGrid (embed)"
    - "Save-new → hydrate-if-needed (orrery-preferences-store.ts:314-analog; idempotent) → useOrreryPreferencesStore.save({lastSystem:newRefId}) (the app-scoped selection channel; NOT no-op'd because hydrated first — cycle-4 HIGH #1) → OrreryScreen cross-route observer (plan 10) = live switch + relaunch persist; edit-active → data-revision reload; edit-non-active → goBack, no selection write → SystemsManagement Edit entry (plan 06)"
    - "built-in/Category override-only mode ← 30-06 Manage-Members action (systemRef param); base candidateIds ← 30-02 shared base-member read; Save → 30-03 override-only composite (setSystemOverride deltas + prune)"
  prohibitions:
    - "Never share Dashboard live UI/query state — reuse the predicate LOGIC only (§C)"
    - "Never expose an arbitrary boolean-query builder or System nesting (deferred)"
    - "Never include Birthday as a rule family (omitted, §D)"
    - "Never hand-roll the unsaved-changes dialog — reuse useDiscardKeepGuard (§P)"
    - "Never hardcode a colour — accent for selected rule values/Save, neutral chrome otherwise, via useTheme().colors"
---

<objective>
Build the floating HUD wizard: the Definition page (name, accordion rules over the eight axes with summarizing headers, live match count, Manage Members, Save), the embedded Manage Members page, and the save/unsaved-changes behavior — fully operable without touching the canvas.

Purpose: This is the authoring heart of the phase (ORRS-05). It composes the resolver (live count), the Manage Members grid (plan 07), and systems-dao (save), and it enforces the save-switch semantics and the shell's discard/keep contract. The rule-draft-to-count logic is unit-testable; the HUD feel is device-verified.

Output: SystemBuilderScreen (Definition + Manage Members pages + save), the rule accordion component, the pure builder draft/count logic, and the SystemBuilder route.

## Artifacts this plan produces
- `src/screens/SystemBuilderScreen.tsx` (Definition + Manage Members pages, save behavior via the app-scoped selection channel, discard guard, AND a built-in/Category override-only mode)
- `src/components/orrery/SystemRuleAccordion.tsx`
- `src/components/orrery/system-builder-logic.ts`: `RuleDraft`, `summarizeFamily`, `draftToRules`, `isMeaningfulChange`
- `SystemBuilder` route (`{ systemUid?: string; systemRef?: string } | undefined`) in `OrreryStackParamList` AND `SettingsStackParamList`; Stack.Screen registration in BOTH OrreryStack.tsx and SettingsStack.tsx

## Review Feedback Incorporated (cycle 4)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-4 HIGH #1 (Settings-origin save-new selection dropped when prefs store unhydrated):** save-new now hydrates the global preferences store before writing lastSystem (`if (!prefs.hydrated) await prefs.hydrate(getExecutor())`, idempotent), so a System created from the Settings stack BEFORE the Orrery mounts still persists the last-active preference and emits the observer-triggering commit. `save()` no-ops while `!hydrated` (orrery-preferences-store.ts:135) and only OrreryScreen hydrates (:314); the hydrate-guard closes that gap. No decision reversed — last-active stays a D-05 app_settings preference (Task 3).
- **Cycle-4 HIGH #3 (draft gravity-loader unconstructed):** the live count and embedded grid now call 30-02's db-facing `resolveDraftMembership(exec, { rules, overrides, now })` (which constructs `gravityInputsFor` from `readOrreryImpactInputsCore`), NOT the logic-layer `resolveMembershipFromDefinition` directly (whose loader is a required 4th param). A gravity-rule draft's count/grid now resolves (Tasks 2 + 3).
- **Cycle-4 LOW (depends_on):** `30-10` is added to `depends_on` so the observer-lands-before-writer ordering (HIGH #1's live switch) is encoded in the dependency graph, not incidental to wave arithmetic.

## Review Feedback Incorporated (cycle 3)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-3 HIGH #1 (cross-route active-System selection ownership):** the save-new switch no longer calls the unreachable OrreryScreen-local `store.select` (the store is a useMemo factory instance at OrreryScreen.tsx:214 with no cross-route handle). After commit it writes the selection intent via the GLOBAL useOrreryPreferencesStore.save({lastSystem:newRefId}); OrreryScreen's observer (30-10, wave 3, lands first) makes it a live switch. Edit-active refreshes via the existing data-revision reload; edit-non-active returns via goBack with no selection write (Task 3). Tests updated to assert the preferences-store selection write, not a store.select mock.
- **Cycle-3 HIGH #2 (built-in/Category override authoring — completing [DECIDED] ORRS-03, not deferring):** the dossier does NOT defer built-in/Category override authoring (§153/§173/§363 all [DECIDED], in-scope), so this plan ADDS an override-only builder mode for a built-in/Category ref (name + rules read-only; Manage Members + Save-overrides-only active), reached from 30-06's new Manage-Members action on those rows. Save persists ONLY overrides via the 30-03 override-only composite; the read path applying those overrides is 30-02 (Task 2/3).

## Review Feedback Incorporated (cycle 2)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-2 HIGH #2 (builder HUD presentation model — plan-fidelity fix, NOT an owner reversal):** the cycle-1 text mixed "own canvas" with "make the production Orrery behind the HUD inert", which is self-contradictory — `SystemBuilder` is a normal opaque `Stack.Screen` (no `transparentModal` anywhere in the stacks) and `OrrerySystemSelector` is a component inside `OrreryScreen`, not a route, so there is no live Orrery behind a pushed builder route. Reconciled to the recorded decision: dossier §K:236 [DECIDED] "The builder appears over a **fresh/canonical Orrery canvas**", §N:296 [DECIDED] Preview shows "the provisional System" on that canvas, and UI-SPEC §K. The builder OWNS its own background canvas; the a11y-inert requirement re-targets to that own canvas. No decision is reversed (the presentation model was already recorded; this fixes the plan to match it).
- **Cycle-2 HIGH #1 (embedded grid candidateIds):** the embedded Manage Members grid is seeded from the draft's `candidateIds` (now returned by plan 02; sourced via the `resolveDraftMembership` db wrapper per cycle-4 HIGH #3), closing the missing pre-override candidate set (Task 3).

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Codex 30-08 HIGH (draft live-count, no stored-System resolver for a draft):** the live count uses plan-02's draft engine over the in-memory draft (Task 2), not `resolveCustomSystemMembers`. (Cycle-4 HIGH #3 refines this to call the db-facing `resolveDraftMembership` wrapper, which constructs the required gravity loader — the builder never calls the 3-arg engine directly.)
- **Codex 30-08 HIGH (save atomicity, orchestrator-verified real):** Save calls plan-03's single `saveSystemDefinition` (one inWriteTransaction) — never composing public non-reentrant DAO wrappers (Task 3). Verified on disk: transaction.ts is explicitly non-reentrant. (The cycle-1 "then store.select after commit" step is SUPERSEDED by cycle-3 HIGH #1: the OrreryScreen-local store is unreachable from this route, so the save-new switch now writes the selection intent via the global useOrreryPreferencesStore instead.)
- **Codex 30-06 HIGH (Settings-stack reachability):** this plan registers `SystemBuilder` in BOTH stacks so management's Create/Edit resolves from Settings and Orrery (Task 2).
- **Codex 30-08 MEDIUM (overlay a11y):** the builder makes its OWN background canvas inert + a11y-hidden while a HUD page is open (superseded/clarified by cycle-2 HIGH #2 above — the a11y target is the builder's own canvas, since there is no live production Orrery behind an opaque route) (Task 2).
- **Codex 30-07 MEDIUM (override replacement):** overrides persist via `setSystemOverride(ref, contactId, mode|null)` inside the atomic save (Task 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-02-SUMMARY.md
@.planning/phases/30-orrery-systems/30-03-SUMMARY.md
@.planning/phases/30-orrery-systems/30-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure builder draft logic (rule draft, family summaries, live-count inputs, meaningful-change)</name>
  <files>src/components/orrery/system-builder-logic.ts, src/components/orrery/system-builder-logic.test.ts</files>
  <read_first>
    - src/logic/dashboard-query-logic.ts (DASHBOARD_FILTER_FAMILIES, SOCIAL_BATTERY_VALUES, CONTACT_FREQUENCY_BANDS, NEEDS_ATTENTION_VALUE) and src/services/impact.ts (GRAVITY_TIERS) — the closed value vocabularies per family
    - src/logic/system-rule-resolver.ts (SystemRule shape; resolveMembershipFromDefinition — the DRAFT membership ENGINE, but the builder must NOT call it directly: its gravityInputsFor loader is a required 4th param) AND src/db/orrery-system-read.ts (resolveDraftMembership(exec,{rules,overrides,now}) — the db-facing draft wrapper 30-02 adds that builds the loader; this is what the builder calls for the live count/grid — cycle-4 HIGH #3)
    - src/db/systems-dao.ts (saveSystemDefinition atomic composite / setSystemRules / the rule row shape)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §C/§D/§L (eight axes; OR within/AND across; Birthday omitted; accordion summaries by label; manual-only valid) and .planning/phases/30-orrery-systems/30-UI-SPEC.md accordion summary examples + Copywriting Contract
  </read_first>
  <behavior>
    - RuleDraft models per-family selected values over the eight axes (Category, Favorite, Status/Needs-Attention, Gravity, Social Battery, Contact Frequency, Not Contacted, Snoozed). Birthday is not a family.
    - summarizeFamily(family, selectedValues, labels) returns a header string like "Category · Friends, Community" / "Gravity · Close +1" / "Social Battery · Chargers" (labels, not counts).
    - draftToRules(draft) produces the SystemRule rows systems-dao persists (family, value), with boolean axes encoded as a sentinel value.
    - isMeaningfulChange(before, after) is true when rules/name/overrides differ (drives the discard guard).
  </behavior>
  <action>
Create system-builder-logic.ts with RuleDraft, summarizeFamily, draftToRules, and isMeaningfulChange. Keep it pure/framework-free for vitest. Draw every value from the closed family vocabularies (no free text). Encode Favorite/Not-Contacted/Snoozed as single-value boolean axes. Unit-test summaries (label not count), draftToRules round-trip, the manual-only (no rules) case, and meaningful-change detection.
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/system-builder-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: summarizeFamily renders "Category · Friends, Community" style labels, never a bare count
    - Test: draftToRules yields OR-within/AND-across-compatible SystemRule rows; a no-rule draft yields zero rules (manual-only valid)
    - Test: isMeaningfulChange flips on a rule/name/override delta and not on a no-op
  </acceptance_criteria>
  <done>The pure builder draft/summary/count-input logic is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Rule accordion + Definition page (draft live count) + SystemBuilder route in BOTH stacks + own background canvas a11y-inert</name>
  <files>src/components/orrery/SystemRuleAccordion.tsx, src/screens/SystemBuilderScreen.tsx, src/navigation/types.ts, src/navigation/tabs/OrreryStack.tsx, src/navigation/tabs/SettingsStack.tsx</files>
  <read_first>
    - src/components/orrery/system-builder-logic.ts (Task 1) and src/db/orrery-system-read.ts (resolveDraftMembership — the db-facing DRAFT count wrapper from plan 02 that builds the gravity loader; the builder calls this, not the logic-layer resolveMembershipFromDefinition — cycle-4 HIGH #3)
    - src/components/orrery/OrrerySystemSelector.tsx (GlassSurface overlay + OrreryObstacle + focus/AppState lifecycle — the HUD visual-primitive pattern to reuse; NOTE it is a COMPONENT inside OrreryScreen, not a route, and only makes its own panel modal — the builder does NOT inherit its "over the live Orrery" model; the builder is a pushed opaque screen that renders its OWN canvas)
    - src/screens/OrreryScreen.tsx (~800 the OrreryWorld canvas + ~818 the OrrerySystemSelector component — confirm the selector is NOT a route, so the builder cannot sit "over" it; the builder renders a fresh/canonical background canvas itself)
    - src/navigation/tabs/OrreryStack.tsx AND src/navigation/tabs/SettingsStack.tsx (route registration idiom in BOTH — these are separate native stacks; confirm there is NO `presentation: 'transparentModal'` — SystemBuilder is a normal opaque Stack.Screen, correct for the own-canvas model) + src/navigation/types.ts (OrreryStackParamList AND SettingsStackParamList); the SystemBuilder route params are serializable-only ({ systemUid?: string } for edit vs new)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §K:236 [DECIDED] ("appears over a fresh/canonical Orrery canvas") and §N:296 [DECIDED] ("the Orrery canvas displays the provisional System") — the RECORDED presentation model this task implements
    - .planning/phases/30-orrery-systems/30-UI-SPEC.md K/L (HUD wizard "over a fresh/canonical Orrery canvas"; Definition page elements; accordion accessible expanded/collapsed + selected state; background-orrery removed from focus) + Copywriting Contract (page titles, name placeholder, empty-name error, §T duplicate error) + Color/Typography/Spacing tables
    - src/theme (SPACING, RADII, ICON_SIZE, accent/neutral tokens; 44-min targets)
  </read_first>
  <action>
Add `SystemBuilder: { systemUid?: string; systemRef?: string } | undefined` to BOTH OrreryStackParamList AND SettingsStackParamList (serializable-only; `systemUid` = custom edit, `systemRef` = a built-in/Category ref token for override-only mode, neither = create-new), and register `<Stack.Screen name="SystemBuilder" component={SystemBuilderScreen} />` in BOTH OrreryStack.tsx AND SettingsStack.tsx (so management's Create/Edit AND the built-in/Category Manage-Members action resolve from either stack — Codex review 30-06 HIGH; verified on disk the stacks are separate). When `systemRef` is a built-in/Category ref, enter OVERRIDE-ONLY mode (cycle-3 HIGH #2, completing [DECIDED] ORRS-03): render the System name READ-ONLY (the immutable base label) and the rule accordion READ-ONLY/collapsed (the base predicate is immutable — no rule/name/create write is offered), while Manage Members, the live count, Save, and Reset Overrides stay active. Seed the embedded grid with the ref's BASE member id set (the pre-override candidateIds from the shared base-member read 30-02 exposes for built-in/Category — the SAME base predicate the canvas renders, so the grid and the live Orrery cannot diverge) and compute the live count as |base ∪ eligible-includes ∖ excludes| via the shared applyMembershipOverrides. Build SystemRuleAccordion: vertically stacked collapsible sections over the eight axes, each header showing summarizeFamily output, exposing accessible expanded/collapsed + selected state, selected values tinted accent. Build the SystemBuilderScreen Definition page as a floating HUD (GlassSurface + OrreryObstacle) over the builder's OWN background Orrery canvas — a fresh/canonical Orrery the screen renders itself (dossier §K:236 [DECIDED]; UI-SPEC §K), NOT a transparent modal over the live OrreryScreen (there is no live Orrery behind an opaque pushed route; do not add a transparentModal). Reuse the focus/AppState lifecycle pattern from OrrerySystemSelector for dismiss/pause behaviour. Header: Back/Cancel (neutral), a controlled System name field (placeholder "Name this System"), the accordion set, a live match count computed from the db-facing draft wrapper resolveDraftMembership(getExecutor(), { rules, overrides, now }) over the CURRENT unsaved draft rules+override intent (debounced; a stored-System resolver cannot count an unsaved draft — Codex review 30-08 HIGH; resolveDraftMembership — NOT the logic-layer resolveMembershipFromDefinition — because it constructs the required gravityInputsFor loader so a gravity-rule draft's count resolves, cycle-4 HIGH #3; do NOT rebuild the builder's own background canvas on each toggle — §O), a Manage Members button, and Save (accent). Make the builder's OWN background canvas decorative and removed from accessibility focus while a HUD page is open (accessibilityElementsHidden / importantForAccessibility="no-hide-descendants" on that canvas, so the HUD is fully operable without touching it — Codex review cycle-2 HIGH #2; the earlier "make the production Orrery inert" framing is void because there is no production Orrery behind an opaque route). All colours via theme tokens.
  </action>
  <verify>
    <automated>npx vitest run src/screens/SystemBuilderScreen.test.tsx && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SystemBuilder" src/navigation/types.ts` shows it in BOTH param lists; `grep -rn "name=\"SystemBuilder\"" src/navigation/tabs/` shows registration in BOTH OrreryStack and SettingsStack
    - `grep -rn "transparentModal" src/navigation/tabs/OrreryStack.tsx src/navigation/tabs/SettingsStack.tsx` returns nothing (SystemBuilder is a normal opaque screen owning its own canvas; the a11y-inert target is that own canvas, not a nonexistent Orrery behind a transparent route)
    - `grep -n "accessibilityElementsHidden\|importantForAccessibility" src/screens/SystemBuilderScreen.tsx` shows the builder's own background canvas removed from a11y focus while a HUD page is open
    - `grep -n "resolveDraftMembership" src/screens/SystemBuilderScreen.tsx` shows the DRAFT count path via the db-facing wrapper (which builds the gravity loader); code-line (comment-stripped) negative gate `grep -vE '^\s*(//|\*|/\*)' src/screens/SystemBuilderScreen.tsx | grep -cE "resolveMembershipFromDefinition\(|readOrreryImpactInputsCore"` returns 0 — the builder never CALLS the 3-arg engine directly nor references the loader source in code (comments excluded so an explanatory comment does not self-invalidate — cycle-4 HIGH #3)
    - Test: a gravity-rule draft (e.g. Gravity=deep) yields a correct live count through resolveDraftMembership — a deep-gravity contact is counted and a thin one is not (proves the draft path resolves gravity via the constructed loader, cycle-4 HIGH #3)
    - Test: the Definition page renders the eight accordion families (no Birthday) with label summaries and a live count over an unsaved draft
    - Test: Manage Members is reachable with zero rules selected (manual-only path)
    - Test: opened with a built-in/Category `systemRef` (override-only mode), the name field and rule accordion are read-only/disabled while Manage Members + Save + Reset Overrides are active, and the grid is seeded from the ref's base member set (cycle-3 HIGH #2)
    - `grep -n "systemRef" src/navigation/types.ts src/screens/SystemBuilderScreen.tsx` shows the override-only route param and its handling
    - `npm run check:colors` passes
  </acceptance_criteria>
  <done>The Definition page + rule accordions render with a draft live match count over the builder's own fresh/canonical background canvas, the SystemBuilder route resolves from both stacks as an opaque screen, and the builder's own background canvas is inert/a11y-hidden while a HUD page is open.</done>
</task>

<task type="auto">
  <name>Task 3: Embed Manage Members, save behavior, and the discard/keep guard</name>
  <files>src/screens/SystemBuilderScreen.tsx, src/screens/SystemBuilderScreen.test.tsx</files>
  <read_first>
    - src/components/orrery/ManageMembersGrid.tsx (plan 07 — the props/override-intent onChange contract from the plan-07 SUMMARY; OverrideIntent deltas keyed to setSystemOverride)
    - src/db/systems-dao.ts (saveSystemDefinition — the SINGLE atomic composite from plan 03; resetSystemOverrides; setSystemOverride; and the non-mutexed cores it composes)
    - src/db/transaction.ts (the NON-REENTRANT inWriteTransaction note — why the save must be ONE transaction, never composed public wrappers)
    - src/stores/orrery-preferences-store.ts (the GLOBAL singleton useOrreryPreferencesStore at :166; save(exec,{lastSystem}) at :133-157 — the app-scoped selection channel the builder writes on save-new; the OrreryScreen-local system store is UNREACHABLE from here, cycle-3 HIGH #1. CRITICAL: save() early-returns Promise.resolve() while !hydrated at :135, and hydrate(exec) is at :108-132 — the ONLY hydration call site elsewhere is OrreryScreen.tsx:314, so a Settings-stack save-new before the Orrery mounts MUST hydrate first or the write is dropped — cycle-4 HIGH #1; hydrate guards concurrent reads via `reading` at :109 so calling it is idempotent)
    - src/db/systems-dao.ts override-only composite from the plan-03 SUMMARY (saveMembershipOverrides — setSystemOverride deltas + pruneSystemExclusions in one transaction — for built-in/Category override-only Save)
    - src/navigation/discard-keep-guard.ts (useDiscardKeepGuard signature — the shell contract to reuse verbatim)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §P (save behavior) + .planning/phases/30-orrery-systems/30-UI-SPEC.md Save behavior table + Copywriting Contract (Discard changes? / Keep editing / Discard; Reset Membership Overrides + confirm; empty-name; §T)
  </read_first>
  <behavior>
    - Manage Members embeds the plan-07 grid seeded with the draft's pre-override candidate set + the current override intent; its onChange updates the builder draft's override intent (setSystemOverride deltas). For a CUSTOM draft the candidate set is resolveDraftMembership(exec, draft).candidateIds (30-02's db-facing draft wrapper — it builds the gravity loader so a gravity-rule draft's candidate set is correct, cycle-4 HIGH #3); for a built-in/Category override-only ref it is the shared base member id set (30-02). The grid classifies rule-match vs added vs excluded from that candidate set — it does not re-derive matches (cycle-2 HIGH #1).
    - Save (custom) is a SINGLE atomic call to saveSystemDefinition (plan 03) — one inWriteTransaction composing create/rename + setSystemRules + setSystemOverride replacements + pruneSystemExclusionsCore — so a partial failure leaves NO half-created System. Public non-reentrant DAO wrappers are NEVER composed together (Codex review 30-08 HIGH).
    - Save (override-only, built-in/Category) calls the 30-03 override-only composite (setSystemOverride deltas + pruneSystemExclusions in ONE inWriteTransaction) — NEVER saveSystemDefinition (no create/rename/rule write against the immutable base).
    - The switch on save-new goes through the APP-SCOPED selection channel, NOT the OrreryScreen-local store (which the builder cannot reach): after the transaction commits, save-new HYDRATES the preferences store if needed (`const prefs = useOrreryPreferencesStore.getState(); if (!prefs.hydrated) await prefs.hydrate(getExecutor())`) THEN calls useOrreryPreferencesStore.getState().save(getExecutor(),{lastSystem:newRefId}); OrreryScreen's observer (30-10) turns that into a live switch and it persists for relaunch (cycle-3 HIGH #1). The hydrate is REQUIRED because save() no-ops while !hydrated (orrery-preferences-store.ts:135) and only OrreryScreen hydrates (:314) — on a cold Settings path (System created before the Orrery ever mounts) the unhydrated write would otherwise be silently dropped (cycle-4 HIGH #1). hydrate is idempotent (guards concurrent reads via its own `reading` promise).
    - Save (new custom): saveSystemDefinition(new draft) → on commit, hydrate-if-needed → write selection intent lastSystem=newRef via the preferences store — the Orrery switches to it (live if mounted, on next mount otherwise), and the last-active preference persists regardless of whether the Orrery is mounted.
    - Save (edit active): saveSystemDefinition(edit)/override-only save → the active System stays active and refreshes via the data-revision reload (saveSystemDefinition bumps the revision); no selection write.
    - Save (edit non-active): saveSystemDefinition(edit)/override-only save → return to management via goBack with no selection write, so the active System is unchanged.
    - Reset Membership Overrides clears include+exclude (resetSystemOverrides) and keeps rules, behind the confirm copy (available in both custom and override-only modes).
    - Meaningful unsaved changes (isMeaningfulChange) trigger useDiscardKeepGuard on Back/Cancel; empty name and §T collision surface their errors inline and block save (name/collision checks are custom-mode only — override-only mode has no name write).
  </behavior>
  <action>
Embed ManageMembersGrid in the builder, seeding it with the draft's pre-override candidate set (custom → resolveDraftMembership(getExecutor(), draft).candidateIds, 30-02's db-facing draft wrapper that builds the gravity loader — cycle-4 HIGH #3; built-in/Category override-only → the shared base member id set, 30-02) + the current include/exclude intent and wiring its override-intent onChange (setSystemOverride deltas) into the draft. Implement Save routing per §P: for a CUSTOM System, one SINGLE saveSystemDefinition transaction (plan 03); for a built-in/Category override-only ref, the 30-03 override-only composite (setSystemOverride deltas + pruneSystemExclusions in ONE inWriteTransaction), never saveSystemDefinition. On save-NEW → after commit, HYDRATE the preferences store if needed (`const prefs = useOrreryPreferencesStore.getState(); if (!prefs.hydrated) await prefs.hydrate(getExecutor());` — idempotent) THEN call useOrreryPreferencesStore.getState().save(getExecutor(),{lastSystem:newRefId}) (the app-scoped selection channel — OrreryScreen's observer, plan 10, makes it a live switch). The hydrate is mandatory: save() no-ops while !hydrated (:135) and only OrreryScreen hydrates (:314), so a cold Settings-stack save-new would otherwise drop the write (cycle-4 HIGH #1). Do NOT call the OrreryScreen-local store.select (it is unreachable from this route — cycle-3 HIGH #1). On edit-active → save + rely on the data-revision reload (no selection write). On edit-non-active (and override-only) → save + navigation.goBack() with NO selection write (NOT navigate("SystemsManagement") — that route is registered by plan 06 in a later wave; goBack has no route-name dependency). Do NOT compose createCustomSystem/setSystemRules/addSystemOverride public wrappers in sequence (transaction.ts is non-reentrant and that path is non-atomic — Codex review 30-08 HIGH). Wire Reset Membership Overrides to resetSystemOverrides behind the confirm copy (custom AND override-only modes). Gate Back/Cancel through useDiscardKeepGuard when isMeaningfulChange; surface empty-name and §T errors inline and block Save (custom mode only). Add tests asserting save-new calls the save composite ONCE then writes lastSystem=newRef via the preferences store (the selection channel — assert the preferences-store save, not a store.select mock); save-new with the preferences store UNHYDRATED (Orrery never mounted — simulate hydrated=false) calls hydrate BEFORE save so the write is not dropped and lastSystem persists (cycle-4 HIGH #1); a gravity-rule draft's live count resolves via resolveDraftMembership (cycle-4 HIGH #3); edit-non-active saves once without writing lastSystem; a forced save failure does not write lastSystem (no switch to a half-saved System); an override-only save calls the override-only composite (not saveSystemDefinition) and writes no lastSystem; and a meaningful edit triggers the discard guard (mock the DAO + preferences store + guard).
  </action>
  <verify>
    <automated>npx vitest run src/screens/SystemBuilderScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: save-new calls saveSystemDefinition once then writes lastSystem=newRef via useOrreryPreferencesStore.save (the app-scoped selection channel — NOT a store.select mock); edit-non-active saves once and writes NO lastSystem (cycle-3 HIGH #1)
    - Test: save-new with the preferences store UNHYDRATED (hydrated=false, Orrery never mounted) calls hydrate BEFORE save, so lastSystem is persisted and not silently dropped by the !hydrated no-op (cycle-4 HIGH #1)
    - Test: a failed saveSystemDefinition does NOT write lastSystem (no switch to a partially-saved System)
    - Test: an override-only Save (built-in/Category ref) calls the 30-03 override-only composite, NOT saveSystemDefinition, and writes no lastSystem (cycle-3 HIGH #2)
    - `grep -n "hydrate" src/screens/SystemBuilderScreen.tsx` shows the save-new path hydrates the preferences store before the selection write (cycle-4 HIGH #1)
    - `grep -n "saveSystemDefinition\|useOrreryPreferencesStore" src/screens/SystemBuilderScreen.tsx` matches (custom Save routes through the single atomic composite; the switch is a hydrate-guarded preferences-store selection write; manual review confirms no OrreryScreen-local store.select and no sequence of composed public DAO write wrappers on the save path)
    - Test: a meaningful unsaved edit invokes useDiscardKeepGuard on Back; Reset Membership Overrides calls resetSystemOverrides and leaves rules intact
    - `npm test` green; `npm run check:colors` passes
  </acceptance_criteria>
  <done>The builder saves atomically (custom via saveSystemDefinition; built-in/Category via the override-only composite), routes the save-new switch through the app-scoped selection channel (never the unreachable OrreryScreen-local store.select), persists overrides via the replacement op, prunes stale exclusions, resets overrides, and honors the discard/keep contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder input (name/rules/overrides) → systems-dao | authored System definition crosses into SQL via the DAO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-27 | Tampering | rule/override values → SQL | high | mitigate | draftToRules emits closed-vocabulary values only; systems-dao binds every value; no free-text rule value reaches SQL as an identifier |
| T-30-28 | Tampering | duplicate/empty System name | medium | mitigate | Save routes through renameSystem/createCustomSystem CI-uniqueness + empty-name guards; errors surfaced inline, save blocked |
| T-30-29 | Repudiation | lost unsaved work | low | mitigate | useDiscardKeepGuard reused verbatim for meaningful changes (§P) |
</threat_model>

<verification>
- `npm test` green; builder-logic + screen suites cover summaries, save routing, discard guard, reset.
- `npm run check:colors` and Biome pass.
- Device-verify (end-of-phase): HUD operable without the canvas; largest-text Definition layout; production Orrery does not rebuild per toggle.
</verification>

<success_criteria>
ORRS-01/02/05/08 delivered: the multi-page floating HUD authors rules + manual members with live count, correct save-switch semantics, override reset, and the discard/keep contract.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-08-SUMMARY.md` — record the SystemBuilder route params ({ systemUid?; systemRef? }), the built-in/Category override-only mode contract (base candidateIds source + override-only save), the own-canvas presentation model (opaque screen renders its own fresh/canonical background canvas; Preview renders the provisional System into it per §N), the save→select wiring (save-new writes lastSystem via useOrreryPreferencesStore — the app-scoped selection channel consumed by plan 10's OrreryScreen observer, NOT the unreachable local store.select), the hydrate-before-save guard on the save-new selection write (cycle-4 HIGH #1); and the seam the Preview page (plan 09) extends: the provisional draft's resolved memberIds (from resolveDraftMembership) that plan 09's readProvisionalOrreryScene consumes.
</output>
