---
phase: 30-orrery-systems
plan: 12
type: execute
wave: 8
depends_on: [30-11]
files_modified:
  - src/logic/orrery-switch-choreography.ts
  - src/logic/orrery-switch-choreography.test.ts
  - src/logic/orrery-frame.ts
  - src/logic/orrery-frame.test.ts
  - src/components/orrery/orrery-switch-animation.ts
  - src/components/orrery/orrery-switch-animation.test.ts
  - src/components/orrery/use-orrery-switch-runtime.ts
  - src/components/orrery/orrery-switch-runtime.test.ts
  - src/components/orrery/OrreryWorld.tsx
  - src/screens/OrreryScreen.tsx
  - src/logic/orrery-camera-logic.ts
  - src/logic/orrery-camera-logic.test.ts
  - src/components/orrery/use-orrery-camera.ts
  - src/logic/orrery-session-logic.ts
  - src/logic/orrery-session-logic.test.ts
  - src/components/orrery/orrery-frame-mapper.test.ts
  - src/stores/orrery-system-store.ts
  - src/stores/orrery-system-store.test.ts
  - .planning/phases/30-orrery-systems/30-12-DEVICE.md
autonomous: false
requirements: [ORRS-13]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-11, a normal System switch visibly accelerates the current world, peels leaving contacts outward, keeps retained contacts continuous, streams entering contacts inward, and decelerates into exact canonical destination geometry."
    - "The choreography is fluid on the physical Pixel and no longer presents as two or three React publication steps when switching between materially different membership sizes."
    - "The existing Skia/Reanimated render loop owns every animation frame; React publishes only discrete scene, lifecycle, and System-selection state."
    - "The switch runtime is owned above the conditionally mounted OrreryWorld, so blur/background can unmount the canvas while preserving the exact displayed world, progress, camera sample, roles, and retained resources; resume begins on that identical sample with no hidden wall-clock advance."
    - "Canonical Home is reached through a camera transition coordinated with the body choreography: the first projected frame equals the pre-switch frame, the last equals destination Home, and arbitrary prior camera state is not retained at completion."
    - "Android Reduced Motion alone replaces normal choreography with a short non-rotational crossfade/reposition, verified live on the Pixel."
    - "A debug APK is built on droid and installed on the physical Pixel with its reverse temporarily changed from 8081 to 8082; after verification the 8081 reverse is restored."
    - "Only after the complete owner-visible choreography is ready, a release APK is rebuilt on droid and left at the documented release artifact path for the owner."
    - "ORRS-13 and Phase 30 remain incomplete until the owner explicitly approves the normal-motion choreography on the physical Pixel."
  artifacts:
    - "src/components/orrery/use-orrery-switch-runtime.ts — OrreryScreen-owned UI-thread transition/progress/camera/resource session that survives OrreryWorld unmount"
    - "src/components/orrery/OrreryWorld.tsx — pure runtime consumer and single authoritative projected frame"
    - "src/screens/OrreryScreen.tsx — lifecycle-surviving runtime owner, switch token publication, and Home/focus coordination"
    - ".planning/phases/30-orrery-systems/30-12-DEVICE.md — repeatable build, install, reverse-port, test matrix, recordings, timing, Reduced Motion, and APK evidence"
    - "droid:C:/Users/bwales/projects/orbit-app/android/app/build/outputs/apk/release/app-release.apk — owner-retrievable release APK"
  key_links:
    - "ready->loading source world retained by orrery-system-store -> ready destination world -> begin/re-target choreography before registry pruning"
    - "membership delta + System-switch generation -> shared choreography profile/progress -> OrreryWorld -> one projected frame -> rings/bodies/labels/hits"
    - "useIsFocused + AppState -> OrreryScreen-owned runtime pause before canvas unmount -> held progress/world/camera/resources -> identical first frame on resume"
    - "captured pre-switch camera + derived canonical destination Home + choreography progress -> continuous projected camera sample -> exact Home commit at completion"
    - "useReducedMotionShared -> same renderer's simple crossfade/reposition branch"
    - "droid source transport/build -> debug APK -> Pixel with adb reverse 8082 -> evidence/tuning -> release APK left on droid -> owner approval checkpoint"
  prohibitions:
    - "Do not claim ORRS-13 from automated wiring tests, grep output, screenshots, or desktop-emulator behavior."
    - "Do not use React state as a frame clock or publish body positions per frame."
    - "Do not substitute the former 260 ms fade/grow plus reversible 0.22-radian rotation."
    - "Do not push, create a worktree, or build Android locally on this Ivy Bridge host."
---

<objective>
Integrate and tune the full D-11 choreography in the live Orrery, prove it on the physical Pixel, produce the requested Android artifacts, and hold completion behind the owner's explicit visual approval.

Purpose: Convert the deterministic Plan-11 engine into the fluid System-switch experience originally approved, without regressing Home framing, focus continuity, input, lifecycle, or Reduced Motion.
Output: Live Skia/Reanimated integration, physical-Pixel recordings and evidence, installed debug APK, release APK left on droid, and an owner-approval checkpoint.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-CONTEXT.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@.planning/phases/30-orrery-systems/30-VALIDATION.md
@.planning/phases/30-orrery-systems/30-UAT.md
@.planning/phases/30-orrery-systems/30-VERIFICATION.md
@.planning/phases/30-orrery-systems/30-11-SUMMARY.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@docs/runbooks/orrery-visual-layer.md
@docs/runbooks/desktop-build-pipeline.md
</context>

## Artifacts this phase produces

- Live normal and Reduced Motion System-switch paths in `OrreryWorld` using the Plan-11 model.
- Repeatable physical-device evidence in `30-12-DEVICE.md`, with recordings referenced by absolute temporary/evidence path and exact tested fixtures.
- Fresh debug APK installed on the Pixel and fresh release APK retained on droid for the owner.
- Explicit owner approval evidence before any verifier may pass ORRS-13 or Phase 30.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one real System switch through the live UI-thread choreography</name>
  <files>src/components/orrery/use-orrery-switch-runtime.ts, src/components/orrery/orrery-switch-runtime.test.ts, src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx, src/components/orrery/orrery-frame-mapper.test.ts</files>
  <read_first>
    - src/components/orrery/OrreryWorld.tsx (full file; owns keyed resources, transition/progress SharedValues, projection, rings/bodies/labels/hits, pruning, and Reduced Motion)
    - src/screens/OrreryScreen.tsx (full file; owns source-id stash, switch generation, Home/focus, SharedValue publication, isFocused/AppState canvas mount gate, and OrreryWorld props)
    - src/stores/orrery-system-store.ts and src/stores/orrery-system-store.test.ts (full files; outgoing snapshot must remain mounted while destination loads)
    - src/components/orrery/orrery-frame-mapper.test.ts (compiled Reanimated closure harness; update every captured worklet dependency)
    - src/logic/orrery-switch-choreography.ts and test (Plan-11 canonical roles/phases/sampler)
    - src/logic/orrery-frame.ts and test (one authoritative sampled/projected frame)
    - src/components/orrery/OrbitBody.tsx, ProjectedOrbitRing.tsx, OrreryLabel.tsx, SatelliteBody.tsx, SunBody.tsx, and OrreryCanvas.tsx (full consumers sharing frame/resource/lifecycle state; preserve unconditional hooks and theme-token colors)
    - src/services/orrery-scene.ts, src/logic/orrery-world-logic.ts, src/logic/orrery-camera-logic.ts, and src/components/orrery/use-orrery-camera.ts (scene generation, canonical geometry, Home/projection, and lifecycle patterns)
    - src/logic/orrery-session-logic.ts, src/stores/orrery-session-store.ts, and their tests (background/profile camera capture semantics; switch runtime is separate memory-only state and must not leak into persisted/session restore)
  </read_first>
  <behavior>
    - "A real in-session switch from a ready source to a ready destination initializes one choreography using both immutable worlds and the computed membership delta before any departing resources are pruned."
    - "The UI-thread progress drives accelerate -> shed -> capture -> settle continuously; React does not receive per-frame positions, progress, or body-role updates."
    - "Shared focus remains selected only when present in both Systems, the camera lands at canonical Home, and departing bodies are immediately excluded from hit authority."
    - "A second System selection during motion re-targets from the currently displayed sample without unmount/snap."
    - "If blur or background occurs mid-transition, a UI-thread pause cancels the progress driver while retaining the exact transition state, held progress, currently displayed world/camera sample, role state, and source/destination resources above OrreryWorld; repeated samples while absent are identical, and the first remounted frame equals the departure frame before progress resumes."
    - "The switch begins with the exact pre-switch projected world/camera frame, continuously coordinates the camera from its captured arbitrary pose toward the destination's canonical Home pose, and completes at exact Home rather than preserving the old camera or assigning Home before the first animation frame."
  </behavior>
  <action>Replace the current scalar ramp plus `spinSwitchWorld` modulation with the Plan-11 transition state and sampler. Add `useOrrerySwitchRuntime` and call it from `OrreryScreen`, above the `{visible && sessionReady && scene ? <OrreryWorld/> : null}` mount gate. This screen-owned runtime—not OrreryWorld—must own the transition/progress SharedValues, immutable source/destination scenes, role-bearing state, merged keyed resource registry, captured source camera pose, derived canonical destination Home pose, and begin/pause/resume/re-target/prune operations. `OrreryScreen` publishes only discrete scene/System/lifecycle events into it; `OrreryWorld` becomes a pure consumer of the retained runtime cells/resources, so unmounting the canvas cannot reset them.

On `AppState` departure, invoke the runtime's UI-thread `pause` before `setAppActive(false)`; on navigation blur, invoke the same `pause` in the focus cleanup before clearing `sessionReady`. `pause` must `cancelAnimation(progress)` on the UI thread and retain the held progress and transition state without rebasing to wall-clock time. While absent, no callback may advance or prune the transition. Once focused, active, measured, and session-ready again, remount OrreryWorld against the held runtime first, prove its first derived frame is the exact held world/camera sample, then resume `withTiming` for only the remaining normalized progress/duration. Preserve this runtime across OrreryWorld cleanup; prune source resources only after a genuine completed transition. Re-target A->B->C from the runtime's current sampled world and camera, including while paused.

Coordinate canonical Home through that same normalized choreography progress: capture the currently displayed camera pose at switch start, derive the destination Home from the destination world/viewport, and sample a continuous camera path alongside the body sample. The projected frame at progress 0 must equal the last pre-switch projection; at progress 1 it must equal destination canonical Home exactly, at which boundary the canonical Home pose may be committed to `pose` without a visual change. Remove/bypass the switch path's direct pre-frame `pose.value = home` assignment; do not preserve arbitrary prior pan/zoom/tilt at completion. A re-target captures the currently displayed camera sample as its new source. Keep camera gestures disabled until the engine's stable threshold so gesture math never reads a pose different from the displayed projection, then hand off from the sampled pose to the canonical `pose` cell continuously. Keep one `useDerivedValue` frame path so `ProjectedOrbitRing`, `ProjectedBody`, labels, culling, satellites, and hit testing all consume the same sampled world and sampled camera. Retain source/destination resources until the choreography reports their exit complete, then prune once. Preserve the existing D-11 lifecycle blocker regression: the store keeps the source snapshot mounted while loading. Keep focus membership policy independent from body choreography. Use `useReducedMotionShared()` live and route it into the Plan-11 simple branch while preserving the same lifecycle and camera boundaries. Define every worklet helper above its caller and update the compiled mapper harness for all closure captures.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts src/components/orrery/orrery-switch-runtime.test.ts src/components/orrery/orrery-frame-mapper.test.ts src/components/orrery/orrery-switch-animation.test.ts src/logic/orrery-camera-logic.test.ts src/logic/orrery-session-logic.test.ts src/stores/orrery-system-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A renderer-level test drives ready A -> loading B with A still mounted -> ready B and proves one transition owns both source and destination worlds.
    - The compiled frame-mapper test evaluates samples in each named phase and all worklet captures resolve under the real mapper harness.
    - A rapid A->B->C test proves C starts at B's displayed sample, not settled A/B geometry.
    - A mid-transition focus-blur and AppState-background test records the departure frame/progress, destroys and recreates the OrreryWorld consumer, samples repeatedly while absent, and proves the first resumed frame is exactly equal and progress did not advance before the remaining animation continues.
    - A renderer boundary test starts from a non-Home pan/zoom/tilt, proves progress-0 projected body/ring coordinates equal the pre-switch frame, proves intermediate camera samples are continuous, and proves progress-1 projection/pose equals destination canonical Home with no completion snap.
    - Tests prove no per-frame React state publication, exact canonical destination settle, inert departures, and stable destination interaction threshold.
    - Focus/Home and same-System reload/session-restore regressions remain green.
  </acceptance_criteria>
  <done>A real System-selection path drives the complete staged choreography through one live UI-thread frame, including a screen-owned runtime that survives OrreryWorld unmount, exact pause/resume and re-target continuity, coordinated canonical Home projection, focus rules, and Reduced Motion routing.</done>
</task>

<task type="auto">
  <name>Task 2: Build debug on droid, install and tune on the physical Pixel, then leave a fresh release APK on droid</name>
  <files>src/logic/orrery-switch-choreography.ts, src/components/orrery/OrreryWorld.tsx, .planning/phases/30-orrery-systems/30-12-DEVICE.md</files>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (follow the droid tar-over-SSH, cmd.exe, Gradle, APK, and adb procedure; never substitute git push or a local Android build)
    - docs/runbooks/orrery-visual-layer.md (physical Pixel is required for Skia performance and lifecycle claims)
    - .planning/phases/30-orrery-systems/30-CONTEXT.md D-11 (owner-visible stages and completion gate)
    - src/logic/orrery-switch-choreography.ts (top-level tunable phase, duration, rotation, displacement, and stability constants)
    - src/components/orrery/OrreryWorld.tsx (live progress/lifecycle driver; no React frame loop)
  </read_first>
  <precondition>The physical Pixel is USB-authorized, exactly one adb device is in `device` state, droid is reachable by SSH, and the Orbit Metro instance responds on local port 8082.</precondition>
  <action>Run all focused suites, TypeScript, Biome on touched files, colors, and the full test suite before transport. Follow `desktop-build-pipeline.md`: marker-check `C:\Users\bwales\projects\orbit-app`, incrementally tar the current source to droid excluding `.git`, `.planning`, `node_modules`, `android`, `ios`, and `.expo`, then run the documented droid build commands (no push/worktree/local Android build). Build `assembleDebug`, copy its APK to a temporary local path, run `emu-connect device`, resolve the one physical serial from `~/.local/bin/adb devices -l`, and install with `-r`. Temporarily remove the existing `tcp:8081` reverse and set `tcp:8082 tcp:8082`; verify `adb reverse --list` and that local port 8082 is the Orbit Metro before launch. Exercise repeatable fixtures covering: high-overlap subtle switch; 3-contact to 9-contact expansion; 9-to-3 narrowing; near/disjoint high-turnover switch; retained focused contact; a switch from a visibly non-Home pan/zoom/tilt; rapid A->B->C re-target; mid-transition blur/remount/resume; mid-transition background/remount/resume; and Android animator scale 0 Reduced Motion. Capture `adb shell screenrecord` videos for normal expansion, normal narrowing, high-overlap comparison, non-Home-to-Home continuity, rapid re-target, both lifecycle pause/resume paths, and Reduced Motion; screenshots alone are not motion evidence. Inspect recordings at normal playback speed and use frame/timestamp analysis only as supporting evidence. For observations that are exclusively taste/timing, tune top-level choreography constants/easing based on the physical Pixel until all five D-11 stages read as one fluid motion, entering/leaving trajectories are unmistakable, high delta is materially stronger than high overlap, the final settle is exact, and input/lifecycle behavior remains sound. If evidence instead exposes a semantic, state-ownership, projection, re-target, resource, store, or test defect, treat it as an implementation failure and repair the owning seam through the Task-3 rejection loop rather than masking it with constants. Re-run deterministic tests after every change and rebuild/reinstall debug when the running Metro bundle cannot safely establish the tested artifact. Record commands, git commit, serial/model (not a guessed serial), fixture memberships/deltas, recording paths, durations, observations, tuning values, Reduced Motion result, exact departure/first-resumed-frame lifecycle result, Home projection result, and automated outputs in `30-12-DEVICE.md`. Once the complete owner-visible choreography is ready for the owner's final taste check, re-sync final source to droid and run `gradlew.bat assembleRelease --console=plain`; assert the fresh artifact exists at `C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\release\app-release.apk` and leave it there. Restore the phone reverse to `tcp:8081 tcp:8081` after debug testing and record the final reverse list. Do not mark ORRS-13 passed; Task 3 owns owner approval.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts src/components/orrery/orrery-switch-runtime.test.ts src/components/orrery/orrery-frame-mapper.test.ts src/components/orrery/orrery-switch-animation.test.ts src/logic/orrery-camera-logic.test.ts src/logic/orrery-session-logic.test.ts src/stores/orrery-system-store.test.ts && npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Executor inspects the captured physical-Pixel recordings at normal speed and records whether every D-11 stage, delta contrast, re-target, pause/resume, and Reduced Motion replacement is plainly observable; uncertainty is a failed tuning pass, not acceptance.</human-check>
  </verify>
  <acceptance_criteria>
    - `30-12-DEVICE.md` identifies the physical Pixel selected from live adb output and contains commands/results for every fixture and recording; emulator evidence is absent from performance claims.
    - Normal 3->9 and disjoint recordings visibly accelerate, shed/peel, preserve retained bodies, stream/capture, then decelerate/settle as one continuous animation rather than staged React publications.
    - Normal 9->3 visibly peels leaving bodies outward; high-overlap motion is observably more restrained than high-turnover motion.
    - Rapid re-target, mid-transition blur/remount/resume, mid-transition background/remount/resume, focus/Home, and destination input checks pass without snap, hidden advance, stale hit targets, or unnecessary lockout; recordings show the first resumed frame matching the departure frame.
    - A switch begun from non-Home pan/zoom/tilt has no first-frame projection snap and continuously lands at canonical destination Home rather than preserving the arbitrary source camera.
    - Reduced Motion recording contains crossfade/reposition and no rotational, peel, or capture sweep.
    - Debug APK was built on droid and installed on the Pixel; the temporary `8082>8082` reverse was verified and final `8081>8081` reverse restored.
    - A fresh release APK exists and remains on droid at the documented release path, built only after the executor's complete visual/device readiness gate passed.
  </acceptance_criteria>
  <done>The fully staged choreography is tuned and evidenced on the physical Pixel, debug is installed, the reverse-port change has been restored, and the final release APK is waiting on droid for owner testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Owner approves the normal-motion choreography on the physical Pixel</name>
  <files>src/logic/orrery-switch-choreography.ts, src/logic/orrery-switch-choreography.test.ts, src/logic/orrery-frame.ts, src/logic/orrery-frame.test.ts, src/components/orrery/orrery-switch-animation.ts, src/components/orrery/orrery-switch-animation.test.ts, src/components/orrery/use-orrery-switch-runtime.ts, src/components/orrery/orrery-switch-runtime.test.ts, src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx, src/logic/orrery-camera-logic.ts, src/logic/orrery-camera-logic.test.ts, src/components/orrery/use-orrery-camera.ts, src/logic/orrery-session-logic.ts, src/logic/orrery-session-logic.test.ts, src/components/orrery/orrery-frame-mapper.test.ts, src/stores/orrery-system-store.ts, src/stores/orrery-system-store.test.ts, .planning/phases/30-orrery-systems/30-12-DEVICE.md</files>
  <what-built>A fresh release APK containing the physical-Pixel-tuned D-11 System-switch choreography is waiting on droid; `30-12-DEVICE.md` contains the tested fixture matrix and normal/Reduced Motion recordings.</what-built>
  <how-to-verify>
    On the physical Pixel, use the release build to switch 3-contact -> 9-contact, 9-contact -> 3-contact, high-overlap, and near-disjoint Systems. Confirm normal motion visibly accelerates, peels leaving contacts outward, retains shared contacts continuously, streams entering contacts inward, and decelerates into exact settled orbits as one fluid animation. Confirm high-turnover drama is materially stronger than high-overlap motion. Enable Android Reduced Motion and confirm the same switches use only short crossfade/reposition with no rotation, peel, or capture sweep.
  </how-to-verify>
  <resume-signal>Reply "approved" only if the complete normal and Reduced Motion contracts pass, or describe every stage/timing/fluidity problem observed so Task 2 can be resumed and retuned.</resume-signal>
  <action>Present the release APK location and the concise fixture matrix from `30-12-DEVICE.md`. The owner installs/grabs the release build and judges the actual visual result. Approval must cover the complete normal-motion choreography, not merely that something moves: acceleration/rotational impulse; outward peel/shedding; retained continuity; inward stream/capture; deceleration/exact settle; clearly different drama for high-overlap versus high-turnover switches; continuous non-Home-to-canonical-Home projection; and exact lifecycle pause/resume. Also have the owner confirm Reduced Motion is the simple non-rotational alternative.

If approval is withheld, keep this checkpoint open and run one bounded correction iteration for the owner's concrete observations: (1) reproduce and record the failing fixture/video plus expected-versus-observed behavior in `30-12-DEVICE.md`; (2) diagnose which owning seam is responsible—Plan-11 choreography model/frame/tests, Plan-12 runtime/renderer/screen/camera/session/store/worklet-mapper integration, or top-level visual tuning; (3) change the smallest correct implementation and add/update a deterministic regression in that seam, with permission to modify any source/test path enumerated by this task rather than forcing structural feedback into constants; (4) rerun the affected focused tests, all choreography/frame/runtime/mapper/camera/session/store tests, TypeScript, touched-file Biome, colors, and the full suite; (5) re-sync to droid, rebuild debug, reinstall on the physical Pixel, temporarily verify `8082>8082`, rerun and record the failed fixture plus the core matrix, then restore `8081>8081`; (6) only when that candidate passes executor device inspection, rebuild the release APK on droid and leave it at the documented path; and (7) return to this same owner checkpoint with the new evidence. Repeat one such diagnose/change/test/build/device/checkpoint iteration for every rejection, with no fixed attempt limit and no authority to override rejection. Do not negotiate a weaker interpretation of D-11 and do not pass on executor-only evidence.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts src/components/orrery/orrery-switch-runtime.test.ts src/components/orrery/orrery-frame-mapper.test.ts src/components/orrery/orrery-switch-animation.test.ts src/logic/orrery-camera-logic.test.ts src/logic/orrery-session-logic.test.ts src/stores/orrery-system-store.test.ts && test -f .planning/phases/30-orrery-systems/30-12-DEVICE.md && ssh -o BatchMode=yes droid 'if exist "C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\release\app-release.apk" (echo RELEASE_READY) else (exit /b 1)'</automated>
    <human-check>Owner explicitly says the physical-Pixel normal-motion choreography matches D-11 and approves it; silence, “something happens,” or approval of only the Reduced Motion path does not pass.</human-check>
  </verify>
  <acceptance_criteria>
    - Owner explicitly approves all five normal-motion stages and delta-adaptive drama on the physical Pixel.
    - Owner confirms Reduced Motion is a simple non-rotational crossfade/reposition.
    - If approval is withheld, this plan remains at the checkpoint and ORRS-13/Phase 30 remain incomplete.
    - Every rejection produces a recorded expected-versus-observed diagnosis, a regression test at the owning seam, complete automated validation, a fresh debug install/device rerun with the temporary 8082 reverse, a restored 8081 reverse, and—only for an executor-ready candidate—a fresh release APK before the checkpoint is presented again.
    - Structural model/runtime/renderer/screen/camera/session/store/worklet defects may be repaired across the enumerated Plan-11/12 seams; rejection is never constrained to tuning constants and is never overridden after any number of iterations.
    - Automated or executor visual evidence is never represented as owner approval.
  </acceptance_criteria>
  <done>Only an explicit owner approval closes this checkpoint. After approval, ORRS-13 may be submitted to phase verification; the executor still does not independently mark Phase 30 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JS/System store -> UI-thread renderer | A discrete scene generation and profile become continuously sampled visual/hit state. |
| Local source -> droid build host | Source is transported over the established SSH pipeline without git push; droid emits installable APKs. |
| Linux adb -> owner's physical work phone | Debug APK install, port reverse, settings changes, recordings, and app interaction affect the attached Pixel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-12-01 | Denial of Service | UI-thread animation / Pixel responsiveness | high | mitigate | One bounded Skia/Reanimated frame path, no per-frame React state, physical-Pixel recordings, input checks, and lifecycle pause/unmount verification before release. |
| T-30-12-02 | Tampering | stale scene/re-target publication | high | mitigate | Generation ownership, outgoing-world retention, displayed-sample re-target tests, and exact destination assertions in renderer/store coverage. |
| T-30-12-03 | Tampering | wrong source or stale APK on droid | medium | mitigate | Marker-check destination, source-only tar transport, record commit/build timestamps, and assert both APK paths after successful Gradle output. |
| T-30-12-04 | Denial of Service | shared adb topology / reverse port | medium | mitigate | Use `emu-connect device`, require exactly one authorized serial from live output, use SDK adb only, temporarily replace only the scoped 8081 reverse with 8082, then restore and record 8081. |
| T-30-12-05 | Information Disclosure | device recordings | low | accept | Record only the owner-authorized Orrery test surface locally; no upload, telemetry, backend, or network read path is introduced. |
| T-30-12-06 | Spoofing | non-Pixel/emulator evidence | medium | mitigate | Record live adb serial/model and reject emulator performance/visual evidence for completion. |
</threat_model>

<verification>
- Focused choreography/frame/mapper/store tests, `npx tsc --noEmit --pretty false`, `npm run check:colors`, touched-file Biome, and full `npm test` are green or any pre-existing unrelated harness failure is named with before/after evidence and not concealed.
- `30-12-DEVICE.md` contains physical-Pixel normal, comparison, re-target, lifecycle, and Reduced Motion evidence plus debug/install/reverse/release commands.
- Release APK remains on droid at `C:\Users\bwales\projects\orbit-app\android\app\build\outputs\apk\release\app-release.apk`.
- ORRS-13 is not complete until Task 3 records explicit owner approval.
</verification>

<success_criteria>
- The physical Pixel shows one fluid staged accelerate -> shed/capture -> decelerate/settle choreography with real outward/inward paths and retained continuity.
- High membership turnover is visibly more dramatic than high overlap, without raw-size dependence.
- Rapid switch, Home/focus, hit authority, interaction readiness, blur/background pause, and Reduced Motion all behave as contracted.
- Debug is installed; the 8082 reverse was temporary and 8081 restored; the fresh release APK is left on droid.
- The owner explicitly approves the animation before ORRS-13 or Phase 30 can pass.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 30 SC5 | Delta-proportional switch presentation and Reduced Motion alternative | 30-11 + 30-12 | COVERED | Renderer, Pixel proof, artifacts, approval here. |
| REQ | ORRS-13 | Normal spin + shedding/capture; Reduced Motion crossfade/reposition | 30-11 + 30-12 | COVERED | Every observable is device-tested and owner-gated. |
| RESEARCH | Render/lifecycle | Shared-value Skia loop, live reduced motion, focus/AppState pause, Pixel-only perf | 30-12 | COVERED | Integration and test matrix explicitly exercise each seam. |
| CONTEXT | D-11 | Complete staged choreography and owner Pixel approval | 30-11 + 30-12 | COVERED | Human checkpoint is blocking. |
| CONTEXT | D-10 | Consume Phase-29 camera/projection/focus rather than redefining it | 30-12 | COVERED | One shared frame; Home/focus semantics preserved. |
| UI-SPEC | E8 / V-W-X | Home/focus, adaptive spin/shedding/capture, lifecycle, interaction, Reduced Motion | 30-11 + 30-12 | COVERED | Automated properties plus physical evidence and approval. |

<output>
After owner approval, create `.planning/phases/30-orrery-systems/30-12-SUMMARY.md` recording the exact tuning constants, device/build evidence, release APK path, and the owner's explicit approval. Until then, stop at the checkpoint and do not list ORRS-13 under `requirements-completed` or mark Phase 30 complete.
</output>
