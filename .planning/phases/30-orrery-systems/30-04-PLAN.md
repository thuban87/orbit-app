---
phase: 30-orrery-systems
plan: 04
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/backup/backup-schema.ts
  - src/backup/backup-schema.test.ts
  - src/backup/export-manifest.test.ts
  - docs/systems/orrery-systems-backup-contract.md
autonomous: true
requirements: [ORRS-14]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "assertPortableSettings accepts orreryLastSystem = custom:<uid> on restore (it already routes through assertOrreryLastSystem, widened in plan 01) and rejects a malformed token rather than coercing it (ORRS-14)"
    - "BACKUP_FORMAT_VERSION is NOT changed by this phase (it is already 4); the export wire shape is byte-identical to before (ORRS-14)"
    - "getPortableSettingsSnapshot emits NO Systems data and no orrery-last-system change beyond what already ships; FORWARD_MIGRATIONS is untouched (declare-only, Phase 36 owns emission)"
    - "A written Systems backup contract documents every entity Phase 36 must serialize: custom System definitions, rules, manual inclusions/exclusions, ordering, built-in/Category visibility + overrides, and the last-active preference, plus the restore validation and orphan-repair expectations (ORRS-14)"
    - { statement: "A restored backup carrying a custom:<uid> last-active whose System no longer exists resolves safely (the System read already treats a stale ref as resolvable/fallback); orphan-repair of that case is documented for Phase 36", verification: backstop }
  artifacts:
    - src/backup/backup-schema.ts (Systems restore-acceptance + contract, declare-only)
    - docs/systems/orrery-systems-backup-contract.md
  key_links:
    - "assertOrreryLastSystem (widened plan 01) ↔ assertPortableSettings restore path — custom:<uid> flows through automatically"
    - "backup contract doc → Phase 36 serialization/orphan-repair consumer"
  prohibitions:
    - "Never emit Systems into the backup wire this phase — no edit to getPortableSettingsSnapshot's SELECT, no export-manifest.ts change (D-06 trip-wire)"
    - "Never bump BACKUP_FORMAT_VERSION or add a FORWARD_MIGRATIONS step this phase (Phase 36 owns the bump — now v5)"
    - "Never silently coerce a malformed restored System token — validate and reject"
---

<objective>
Declare — do not emit — the Systems backup contract: make restore accept the widened last-active grammar, document the full Systems entity shape plus validation and orphan-repair expectations for Phase 36, and prove the wire format is unchanged this phase.

Purpose: ORRS-14 requires backup/restore to eventually preserve all Systems state, but the milestone sequencing (D-06) puts the format bump in Phase 36's final plan. This phase follows the verbatim Phase 23/25/29 precedent: allowlist/accept on restore, document the contract, emit nothing, bump nothing. Emitting Systems into the live format-4 wire would hard-reject backups on older builds — a trip-wire.

Output: Restore-acceptance for custom:<uid> (already routed via the widened validator), a durable Systems backup-contract doc, and tests proving no wire/format change.

## Artifacts this plan produces
- `src/backup/backup-schema.ts`: Systems restore-acceptance comment block + assertion coverage (declare-only)
- `docs/systems/orrery-systems-backup-contract.md`: the Phase 36 serialization + orphan-repair contract
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore-accept custom:<uid>; prove no wire/format change</name>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/export-manifest.test.ts</files>
  <read_first>
    - src/backup/backup-schema.ts lines 148-220 (PORTABLE_SETTINGS_KEYS with the "allowlisted NOW … NOT emitted this phase" precedent for Phase 23/25/29; assertPortableSettings 200-220 already calls assertOrreryLastSystem for orreryLastSystem; TOMBSTONE_ENTITY_TYPES set)
    - src/backup/export-manifest.ts lines 1, 75, 97-104 (BACKUP_FORMAT_VERSION import + usage; FORWARD_MIGRATIONS — DO NOT edit)
    - src/db/app-settings-dao.ts lines 74-84 (assertOrreryLastSystem — widened in plan 01 to accept custom:<uid>)
    - src/backup/export-manifest.test.ts AND src/backup/backup-schema.test.ts (existing structure to extend)
    - EVERY writer/reader of the app_settings backup path (D-08 hand-grep): grep `getPortableSettingsSnapshot` and `assertPortableSettings` across src/ to confirm the emit vs accept split before asserting the no-emit invariant
  </read_first>
  <behavior>
    - assertPortableSettings accepts a manifest whose orreryLastSystem is 'custom:abc123' (via the widened assertOrreryLastSystem) and rejects a malformed token (whitespace/control char) with the existing "invalid Orrery preference" failure.
    - BACKUP_FORMAT_VERSION is unchanged (test asserts its current value === the value before this phase).
    - getPortableSettingsSnapshot emits the same keys as before — no Systems key, no new emission (test snapshots the emitted key set).
  </behavior>
  <action>
Confirm assertPortableSettings already routes orreryLastSystem through assertOrreryLastSystem (widened in plan 01) — no code change needed there beyond a comment noting custom:<uid> is now accepted on restore per the Phase 23/25/29 declare-only precedent. Add a `// Phase 30 (declare-only)` comment block near PORTABLE_SETTINGS_KEYS documenting that Systems TABLES (not settings) are serialized by Phase 36, not here. Add tests: (a) restore accepts custom:<uid> and rejects a malformed token; (b) an assertion pinning BACKUP_FORMAT_VERSION to its current value so any future bump this phase fails CI; (c) an assertion that the emitted portable-settings key set contains no Systems entity key and is unchanged. Do NOT edit export-manifest.ts or FORWARD_MIGRATIONS.
  </action>
  <verify>
    <automated>npx vitest run src/backup/backup-schema.test.ts src/backup/export-manifest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test proves restore accepts orreryLastSystem custom:<uid> and rejects a malformed one
    - Test pins BACKUP_FORMAT_VERSION (fails if bumped this phase)
    - `git diff --stat src/backup/export-manifest.ts` shows no change to export-manifest.ts
    - `grep -n "FORWARD_MIGRATIONS" src/backup/export-manifest.ts` unchanged (no new step added)
  </acceptance_criteria>
  <done>Restore accepts the widened last-active grammar; the wire format and emission are provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Systems backup contract for Phase 36</name>
  <files>docs/systems/orrery-systems-backup-contract.md</files>
  <read_first>
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §AA (Backup/Restore: preserves custom defs, rules, manual inclusions, persisted overrides, ordering, built-in/Category visibility + overrides, last-active; built-in definitions themselves need not be serialized — only user customization) and §AB (category-deletion orphan handling)
    - src/db/migrations/022-orrery-systems.ts (the four tables + columns — the exact shapes Phase 36 serializes)
    - src/backup/backup-schema.ts (TOMBSTONE_ENTITY_TYPES and the entity-list idiom Phase 36 extends)
    - docs/systems/ (an existing system doc for the house format/changelog convention)
  </read_first>
  <action>
Write docs/systems/orrery-systems-backup-contract.md documenting, for Phase 36 to serialize: (1) the entity list — `systems`, `system_rules`, `system_overrides` (include/exclude, ref-keyed across builtin/category/custom), `system_prefs` (ordering + visibility, ref-keyed), and the `app_settings.orrery_last_system` preference; (2) that built-in/Category BASE definitions are NOT serialized (they regenerate) — only their user customization (visibility + overrides) is; (3) restore validation expectations — ?-bound closed-token grammar, CI-unique names, uid uniqueness; (4) orphan-repair expectations — a system_overrides/system_rules row referencing a contact/category that no longer exists after restore, and a last-active custom:<uid> whose System is absent (fall back to All Contacts), and the category-deletion fallout (broken rules stay needs-attention, never rewritten). Note it is a declare-only contract: no wire emission or format bump lands until Phase 36. Add a Changelog entry.
  </action>
  <verify>
    <automated>test -f docs/systems/orrery-systems-backup-contract.md && grep -c "system_overrides\|system_prefs\|system_rules\|orrery_last_system" docs/systems/orrery-systems-backup-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - The doc names all four Systems tables + orrery_last_system and states each is Phase-36-serialized, declare-only now
    - The doc lists restore-validation and orphan-repair expectations including the category-deletion fallout and stale last-active fallback
    - `grep -c "system_overrides\|system_prefs\|system_rules\|orrery_last_system" docs/systems/orrery-systems-backup-contract.md` >= 4
  </acceptance_criteria>
  <done>The Systems backup contract is documented as the Phase 36 consumer specification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restored backup file → assertPortableSettings | an attacker-editable or corrupt backup carries a last-active token / (future) Systems entities here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-12 | Tampering | malformed restored last-active token | medium | mitigate | assertOrreryLastSystem (widened) validates the token on restore and rejects rather than coercing |
| T-30-13 | Tampering | premature Systems wire emission | high | mitigate | Declare-only: no export-manifest/FORWARD_MIGRATIONS edit; a test pins BACKUP_FORMAT_VERSION so a bump this phase fails CI |
| T-30-14 | Denial of Service | orphaned Systems rows on restore | low | accept (this phase) | Orphan-repair is documented for Phase 36; the System read already tolerates a stale ref (fallback) — no destructive action taken now |
</threat_model>

<verification>
- `npm test` green; backup suites prove restore-acceptance and the pinned format version.
- `git diff --stat` shows export-manifest.ts unchanged.
- Biome lint passes.
</verification>

<success_criteria>
Restore accepts custom:<uid>; the wire format and emission are unchanged; the Phase 36 Systems backup contract is documented.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-04-SUMMARY.md` — record the contract-doc path and the exact entity list Phase 36 will serialize.
</output>
