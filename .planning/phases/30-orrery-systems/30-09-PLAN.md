---
phase: 30-orrery-systems
plan: 09
type: execute
wave: 5
depends_on: [30-08, 30-10]
files_modified:
  - src/components/orrery/SystemPreviewCanvas.tsx
  - src/components/orrery/system-preview-logic.ts
  - src/components/orrery/system-preview-logic.test.ts
  - src/services/orrery-scene.ts
  - src/screens/SystemBuilderScreen.tsx
  - src/screens/SystemBuilderScreen.test.tsx
autonomous: true
requirements: [ORRS-07]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Preview is full-canvas (not a tiny in-HUD box): entering Preview collapses the HUD into a small floating Preview/Edit bar while the canvas shows the provisional System (ORRS-07)"
    - "Preview reuses the real Phase 29 layout/scale model with SIMPLIFIED rendering (sun, orbital rails, simple body markers, approximate real sizing) — no final photos/labels/decorative effects (ORRS-07)"
    - "Preview supports pan and zoom; tilt/yaw are not exposed; tapping a body may focus/identify it but never opens a Profile (unsaved focused workflow) (ORRS-07)"
    - "Save is available from Preview AND from the HUD; Edit restores the full HUD at the prior builder state (ORRS-07)"
    - "Preview provides a textual membership summary so visual Orrery interpretation is never mandatory (a11y, §N/§Y)"
    - "Unsaved changes from Preview route through the shell's useDiscardKeepGuard (ORRS-07)"
    - "Preview derives its geometry from a SHARED exported provisional-scene/world function that consumes the same inputs the real scene uses — ordered member records, contact attributes, density, gravity, and sun identity — NOT a marker mapper from member ids alone (member ids are insufficient layout input; a mapper-from-ids would silently reimplement geometry and diverge from Phase 29) (Codex review 30-09 MEDIUM)"
    - "A new read/service seam readProvisionalOrreryScene(exec, memberIds) (in src/services/orrery-scene.ts, owned by this plan) PRODUCES those provisional scene inputs for an unsaved draft's id set: it batches full OrrerySystemMember records for the ids (same projection as readOrrerySystemMembersCore — id/uid/name/photo/ring_seq/status/progress/last_contact/rarely_responds/favourite_rank/created_at), impact inputs via readOrreryImpactInputsCore (for the gravity map), settings (density) + profile + sun identity/occupant, then orders via orderOrreryMembers, computes gravity via computeContactGravity, and invokes the shared deriveOrreryWorld — reproducing loadOrreryScene's derivation for an arbitrary member id set. Neither the resolver (returns ids) nor systems-members-read (returns {id,name,photo,searchMethods}) is sufficient on its own (Codex review cycle-2 HIGH #3)"
    - "readProvisionalOrreryScene wraps ALL its dependent reads (members, impact inputs, settings, profile, sun identity/occupant) in a single inReadSnapshot(exec, ro => …) coherent-read boundary — matching the production read at orrery-system-read.ts:160 — so the provisional geometry is never assembled from mismatched mid-write inputs (cycle-3 actionable #7)"
    - "A contact resolved as the SUN is excluded from the orbiting bodies of the provisional scene, exactly as the production read does (orrery-system-read.ts:113 filters the resolvedSunIdentity out of `orbiting`) — a resolved non-member sun does not appear as an orbiting body in the preview (cycle-3 actionable #7)"
    - "The Preview canvas pauses/unmounts its ambient Skia loop while unfocused (useIsFocused===false) or AppState backgrounded — the same lifecycle guarantee the real OrreryCanvas gives by unmounting — so no orphaned render loop runs behind the builder (Codex review 30-09 MEDIUM)"
    - "Previewing a zero-member System shows the empty treatment; a broken definition still previews whatever valid membership remains"
    - { statement: "The simplified full-canvas Preview renders and stays responsive at large membership on the physical Pixel", verification: backstop }
    - { statement: "Preview body markers and the Preview/Edit bar reflow at the largest supported text size", verification: backstop }
  artifacts:
    - src/components/orrery/SystemPreviewCanvas.tsx
    - src/components/orrery/system-preview-logic.ts
    - src/services/orrery-scene.ts: `readProvisionalOrreryScene(exec, memberIds)` (batches member records + impact inputs + settings + sun, then invokes the shared deriveOrreryWorld) — the Preview's provisional-scene input source
    - SystemBuilder Preview/Edit bar wiring
  key_links:
    - "builder provisional draft (plan 08) → resolveDraftMembership(exec, draft) memberIds (30-02's db-facing draft wrapper, which builds the gravity loader so a gravity-rule draft's members resolve — cycle-4 HIGH #3) → readProvisionalOrreryScene(exec, memberIds) (member records + impact inputs + density + sun/settings) → shared deriveOrreryWorld → buildPreviewMarkers → simplified Skia markers"
    - "Preview/Edit bar Save → same atomic saveSystemDefinition routing as the HUD (plan 08); Edit → restore HUD state"
  prohibitions:
    - "Never open a Profile from a Preview body tap — the user is in an unsaved focused workflow (§N)"
    - "Never render final photos/labels/decorative effects in Preview — simplified markers only (§N/§Z)"
    - "Never expose tilt/yaw in Preview (§N)"
    - "Never redefine Phase 29 layout/projection — reuse it (D-10)"
    - "Never hardcode a colour in a Skia draw — resolve through useTheme().colors"
    - "Never define a worklet helper below its caller (Hermes forward-ref hazard)"
---

<objective>
Add the full-canvas Preview: a simplified Skia rendering of the provisional System over the REAL Phase 29 layout/scale model, entered by collapsing the HUD into a Preview/Edit bar, supporting pan/zoom (no tilt/yaw, no Profile navigation), Save-from-Preview, and a textual membership summary — under the shell's discard/keep contract.

Purpose: Preview answers both membership AND scale questions before Save (§O) without paying full production rendering cost. It must reuse Phase 29's layout so the answer is honest, but render simplified markers so even large Systems communicate geometry. The provisional-membership mapping is unit-testable; the Skia render is device-verified.

Output: SystemPreviewCanvas (simplified render over reused layout), the pure preview-projection logic, and the Preview/Edit bar wiring in the builder.

## Artifacts this plan produces
- `src/components/orrery/SystemPreviewCanvas.tsx`
- `src/components/orrery/system-preview-logic.ts`: `buildPreviewMarkers` (over the shared provisional-scene derivation), `previewMembershipSummary`
- SystemBuilderScreen: Preview/Edit bar + enter/exit Preview state

## Review Feedback Incorporated (cycle 4)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-4 HIGH #3 (draft gravity-loader unconstructed by preview caller):** the preview's provisional memberIds are sourced from 30-02's db-facing `resolveDraftMembership(exec, draft)` wrapper (which constructs `gravityInputsFor` from `readOrreryImpactInputsCore`), NOT the logic-layer `resolveMembershipFromDefinition` (whose loader is a required 4th param) — so a gravity-rule draft's preview membership resolves. `readProvisionalOrreryScene` still owns the SCENE-input production (member records/impact/density/sun) for that id set; the two seams compose (the id set from resolveDraftMembership, the scene inputs from readProvisionalOrreryScene).

## Review Feedback Incorporated (cycle 3)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-3 actionable #7 (atomic read boundary):** readProvisionalOrreryScene now explicitly wraps its members/impact-inputs/settings/sun reads in ONE inReadSnapshot boundary (matching orrery-system-read.ts:160), and excludes a resolved non-member sun from the orbiting bodies (mirroring orrery-system-read.ts:113); both are tested (Task 1).

## Review Feedback Incorporated (cycle 2)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-2 HIGH #3 (provisional-scene inputs produced by no plan):** verified `loadOrreryScene` (`orrery-scene.ts:52-115`) derives world geometry via `deriveOrreryWorld(orderOrreryMembers(snapshot.orbiting), settings.orreryDensity, gravity, sun)` where `gravity` comes from `snapshot.impactInputs` — full member records + impact inputs + settings/sun, none of which the resolver (ids) or `systems-members-read` ({id,name,photo,searchMethods}) supplies. This plan now OWNS a `readProvisionalOrreryScene(exec, memberIds)` read/service seam in `orrery-scene.ts` that batches those inputs and invokes the shared `deriveOrreryWorld` (Task 1).
- **Cycle-2 LOW #11 (pin the exact derivation symbol):** Task 1 pins `deriveOrreryWorld` / `orderOrreryMembers` / `computeContactGravity` and `loadOrreryScene` (`orrery-scene.ts:52-115`) as the reference to factor the provisional entry point from, replacing the guess-set grep (`generateScene|deriveScene`).

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Codex 30-09 MEDIUM (member ids insufficient layout input):** buildPreviewMarkers consumes a SHARED exported provisional-scene/world derivation (the same ordered-member/attribute/density/gravity/sun inputs the real `orrery-scene` derivation uses), not a marker mapper from ids — so Preview geometry cannot silently diverge from Phase 29 (Task 1).
- **Codex 30-09 MEDIUM (preview lifecycle pause):** SystemPreviewCanvas gates/unmounts its Skia loop on useIsFocused===false / AppState background, matching OrreryCanvas's unmount-while-unfocused guarantee (Task 2).
- **Consensus (renderer boundary):** unlike plan 10, Preview OWNS its own Skia surface (SystemPreviewCanvas), so it can implement its simplified render without editing OrreryWorld; the scale/perf items stay device-verify backstops.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: readProvisionalOrreryScene seam + pure preview-projection logic over the reused Phase 29 layout</name>
  <files>src/services/orrery-scene.ts, src/components/orrery/system-preview-logic.ts, src/components/orrery/system-preview-logic.test.ts</files>
  <read_first>
    - src/services/orrery-scene.ts lines 52-115 (`loadOrreryScene` — the REAL derivation to factor a provisional entry point FROM, verified on disk): it reads `readOrrerySystemSnapshot`, then `orderOrreryMembers(snapshot.orbiting)` (@/logic/orrery-world-logic), computes `gravity = new Map(snapshot.impactInputs → computeContactGravity(...))` (@/services/impact), and `deriveOrreryWorld(contacts, settings.orreryDensity, gravity, sun)` (@/logic/orrery-world-logic) → `{ world, extent }`. These are the EXACT symbols to reuse — not a `generateScene`/`deriveScene` guess.
    - src/db/orrery-system-read.ts lines 36-46 (the `OrrerySystemMember` shape readProvisionalOrreryScene must produce per id) and lines 66-74 (the member SELECT projection to mirror), and src/db/orrery-impact-read.ts:23 (`readOrreryImpactInputsCore` — the impact-inputs batch for gravity)
    - src/screens/OrreryScreen.tsx (how the real scene is assembled from the snapshot before rendering — the inputs a provisional preview must supply)
    - src/db/orrery-system-read.ts (resolveDraftMembership(exec, { rules, overrides, now }) → the provisional draft's resolved memberIds — the db-facing draft wrapper 30-02 adds that builds the gravity loader; this is the id set readProvisionalOrreryScene expands into full records. Preview consumes the memberIds the builder already resolved via resolveDraftMembership; it does NOT call the logic-layer resolveMembershipFromDefinition directly, whose gravityInputsFor loader is a required 4th param — cycle-4 HIGH #3)
    - src/db/systems-members-read.ts (plan 07's {id,name,photo,searchMethods} row — NOTE it is insufficient for layout; readProvisionalOrreryScene reads the fuller OrrerySystemMember projection instead)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §N/§O/§Z (simplified rendering: sun, rails, simple markers, approximate sizing; communicates membership + scale; large membership simplified) and .planning/phases/30-orrery-systems/30-UI-SPEC.md E4/Preview contract
  </read_first>
  <behavior>
    - readProvisionalOrreryScene(exec, memberIds) batches, for the draft's resolved member id set: the full OrrerySystemMember records (a `WHERE c.id IN (?…)` SELECT mirroring readOrrerySystemMembersCore's projection, ordered by the same COALESCE(c.ring_seq,1e9),c.created_at,c.id clause), the impact inputs via readOrreryImpactInputsCore (→ gravity map via computeContactGravity), and settings (orreryDensity) + profile + sun identity/occupant; it then calls orderOrreryMembers + deriveOrreryWorld exactly as loadOrreryScene does, returning a provisional scene ({ world, extent, contacts, gravity, sun, preferences }) shaped like the real OrrerySceneSnapshot's geometry-bearing fields. It does NOT persist anything.
    - ALL of readProvisionalOrreryScene's dependent reads run inside ONE inReadSnapshot(exec, ro => …) boundary (the same coherent-read primitive the production read uses at orrery-system-read.ts:160) so the members/impact-inputs/settings/sun are a consistent snapshot, not a torn read (cycle-3 actionable #7).
    - A contact resolved as the sun is EXCLUDED from the provisional scene's orbiting bodies (mirroring orrery-system-read.ts:113 which filters resolvedSunIdentity out of `orbiting`) — a resolved non-member sun never renders as an orbiting body (cycle-3 actionable #7).
    - buildPreviewMarkers consumes that provisional SCENE (its world/geometry), producing simple marker descriptors (position + approximate size) — NOT a mapper from member ids alone (ids are insufficient layout input and would silently reimplement/diverge from Phase 29 geometry — Codex review 30-09 MEDIUM). No photo/label payload.
    - A zero-member set yields an empty-preview descriptor (drives the empty treatment).
    - previewMembershipSummary(memberRecords) returns the textual a11y summary ("{count} members" per the copy contract).
    - The geometry is the SHARED Phase 29 scene derivation (deriveOrreryWorld reused via readProvisionalOrreryScene), never a reimplemented ring/scale math.
  </behavior>
  <action>
In src/services/orrery-scene.ts add `readProvisionalOrreryScene(exec, memberIds)` that factors the provisional entry point OUT of loadOrreryScene: inside ONE inReadSnapshot(exec, ro => …) boundary (matching the production read at orrery-system-read.ts:160 so the inputs are a coherent snapshot — cycle-3 actionable #7), read the full OrrerySystemMember records for `memberIds` (the same projection as readOrrerySystemMembersCore, ordered COALESCE(c.ring_seq,1e9),c.created_at,c.id; all ids ?-bound), batch impact inputs via readOrreryImpactInputsCore, read settings/profile/sun identity/occupant, EXCLUDE any resolved-sun contact from the orbiting bodies (mirroring orrery-system-read.ts:113), then call orderOrreryMembers + computeContactGravity + deriveOrreryWorld exactly as loadOrreryScene does, and return the provisional scene's geometry-bearing fields. Refactor loadOrreryScene to share this derivation where practical (do not duplicate deriveOrreryWorld). Create system-preview-logic.ts with buildPreviewMarkers (provisional scene → simple marker descriptors: position + approximate size) and previewMembershipSummary (textual membership summary). Keep system-preview-logic.ts free of Skia imports so the mapping is node-testable; consume the provisional scene from readProvisionalOrreryScene rather than duplicating any ring/scale math. Unit-test the empty case, the summary text, that marker descriptors carry position+size but no photo/label payload, and that markers derive from the shared scene function (e.g. two members with different attributes/order produce distinct positions, proving attributes flow through — not id-only mapping).
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/system-preview-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: readProvisionalOrreryScene(exec, memberIds) returns a world derived from full member records + gravity (from impact inputs) + density + sun — two members with different attributes/order yield distinct world positions (proves records/attributes flow through, not id-only)
    - Test: a resolved non-member sun is EXCLUDED from the provisional scene's orbiting bodies (cycle-3 actionable #7)
    - `grep -n "inReadSnapshot" src/services/orrery-scene.ts` shows readProvisionalOrreryScene wraps its dependent reads in the coherent-read boundary (cycle-3 actionable #7)
    - Test: buildPreviewMarkers over the provisional scene yields N simple markers (position + size); zero members → empty-preview descriptor
    - Test: previewMembershipSummary returns the "{count} members" textual summary
    - `grep -n "deriveOrreryWorld\|readProvisionalOrreryScene" src/services/orrery-scene.ts` shows the provisional seam reuses the exact real derivation symbol (not a reimplemented `generateScene`/`deriveScene`)
    - `grep -n "readProvisionalOrreryScene" src/components/orrery/system-preview-logic.ts` shows the shared provisional scene is consumed, not reimplemented
    - `grep -n "photo\|label" src/components/orrery/system-preview-logic.ts` shows marker descriptors carry no photo/label payload
  </acceptance_criteria>
  <done>readProvisionalOrreryScene supplies the draft's provisional scene inputs (full member records + impact inputs + density + sun) through the shared deriveOrreryWorld, and the pure preview projection reuses that layout to produce simplified markers + a textual summary.</done>
</task>

<task type="auto">
  <name>Task 2: SystemPreviewCanvas + Preview/Edit bar wiring</name>
  <files>src/components/orrery/SystemPreviewCanvas.tsx, src/screens/SystemBuilderScreen.tsx, src/screens/SystemBuilderScreen.test.tsx</files>
  <read_first>
    - src/components/orrery/system-preview-logic.ts (Task 1)
    - src/components/orrery/OrreryCanvas.tsx (the ambient-loop lifecycle: it UNMOUNTS while unfocused/backgrounded to stop the Skia loop — the guarantee the preview canvas must match, verified 1-21)
    - src/screens/OrreryScreen.tsx (the Skia canvas + pan/zoom gesture pattern to reuse; useReducedMotionShared; how bodies are drawn — copy the simplified subset)
    - src/screens/SystemBuilderScreen.tsx (plan 08 — the HUD state; the atomic saveSystemDefinition routing to reuse for Save-from-Preview; the discard guard)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §N + .planning/phases/30-orrery-systems/30-UI-SPEC.md Preview contract + Copywriting Contract (Preview, Edit, Save System)
    - MEMORY worklet-forward-ref-hazard (define worklet helpers above callers); CLAUDE.md Skia colour-token rule
  </read_first>
  <behavior>
    - Entering Preview collapses the HUD to a small floating Preview/Edit bar; the canvas renders the provisional System via SystemPreviewCanvas (simplified markers over the shared scene derivation).
    - SystemPreviewCanvas gates/unmounts its ambient Skia loop on useIsFocused()===false and AppState background — matching OrreryCanvas's unmount-while-unfocused guarantee so no orphaned render loop runs behind the builder (Codex review 30-09 MEDIUM).
    - Pan and zoom work; tilt/yaw are not exposed; a body tap may focus/identify but never navigates to a Profile.
    - Save from the Preview/Edit bar reuses the plan-08 atomic saveSystemDefinition routing (same switch semantics); Edit restores the full HUD at the prior builder state.
    - A textual membership summary is present (a11y). A zero-member provisional System shows the empty treatment.
    - Unsaved changes still route through useDiscardKeepGuard.
  </behavior>
  <action>
Build SystemPreviewCanvas: a Skia canvas rendering buildPreviewMarkers over the shared scene derivation with pan/zoom gestures (Reanimated shared values; no tilt/yaw), simple markers only (all colours via theme tokens), a body tap that focuses/identifies but never navigates. Gate the canvas's ambient loop on useIsFocused()/AppState — unmount or stop the loop while unfocused/backgrounded, mirroring OrreryCanvas (Codex review 30-09 MEDIUM). Add a Preview/Edit bar and enter/exit-Preview state to SystemBuilderScreen: Preview collapses the HUD; Edit restores it; Save (from the bar) calls the same plan-08 atomic saveSystemDefinition routing. Render the textual membership summary for a11y and the empty treatment for a zero-member provisional System. Keep the discard guard active. Define any worklet helper above its caller. Add a test asserting a Preview body tap does not navigate to Profile and that Save-from-Preview invokes the same save path as the HUD Save.
  </action>
  <verify>
    <automated>npx vitest run src/screens/SystemBuilderScreen.test.tsx src/components/orrery/system-preview-logic.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Test: a Preview body tap does not trigger Profile navigation
    - Test: Save-from-Preview calls the same save routing as the HUD Save (switch on save-new)
    - `grep -n "useIsFocused\|AppState" src/components/orrery/SystemPreviewCanvas.tsx` shows the focus/background lifecycle gating (loop paused/unmounted while unfocused)
    - `grep -n "tilt\|yaw" src/components/orrery/SystemPreviewCanvas.tsx` shows no tilt/yaw gesture exposed
    - `npm run check:colors` passes; `npm test` green
  </acceptance_criteria>
  <done>Full-canvas simplified Preview with pan/zoom, no-Profile body tap, Save-from-Preview/Edit, and a textual summary — under the discard guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (render-only preview) | Preview reads the provisional draft; no new write/network/auth surface; Save reuses the plan-08 DAO path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-30 | Denial of Service | eager full render at large membership | medium | mitigate | Simplified markers over reused layout with culling/LOD; no photos/labels; device-verified at scale (§Z) |
| T-30-31 | Elevation of privilege | Profile navigation from an unsaved workflow | low | mitigate | Body tap focuses/identifies only; Profile navigation is blocked in Preview (§N) |
| T-30-32 | Tampering | worklet forward-reference crash | high | mitigate | Worklet helpers defined above callers; pure projection unit-tested |
</threat_model>

<verification>
- `npm test` green; preview-logic + builder suites pass.
- `npm run check:colors` and Biome pass.
- Device-verify (end-of-phase, Pixel): simplified full-canvas Preview render + responsiveness at large membership; largest-text Preview/Edit bar. Emulator cannot assess Skia perf.
</verification>

<success_criteria>
ORRS-07 delivered: full-canvas simplified Preview over the reused Phase 29 layout with pan/zoom, no-Profile body tap, Save-from-Preview/Edit, textual summary, and the discard/keep contract.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-09-SUMMARY.md` — record the readProvisionalOrreryScene(exec, memberIds) signature + return shape, the reused derivation symbol (deriveOrreryWorld via loadOrreryScene's factored entry point), the marker-descriptor shape, and the device-UAT items carried to end-of-phase.
</output>
