---
phase: 30-orrery-systems
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/db/systems-dao.ts
  - src/db/systems-dao.test.ts
autonomous: true
requirements: [ORRS-02, ORRS-03, ORRS-09, ORRS-10]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "systems-dao is the ONLY writer of the Systems tables; no inline Systems SQL exists in any component (CLAUDE.md DAO rule)"
    - "System names are unique case-insensitively; create AND rename reject a colliding name before insert with the §T error, and empty names reject with the empty-name error (ORRS-09)"
    - "Deleting a custom System removes its systems row, its system_rules (FK cascade), and its system_overrides + system_prefs rows (ref-keyed, deleted in the same transaction) — and NEVER deletes or modifies any contact row (ORRS-10)"
    - "Duplicating any System kind produces a new editable custom System with a deterministic uniqueness-aware name (Copy, Copy 2, Copy 3 …); duplicating a built-in/Category System converts its base predicate into editable custom rules (ORRS-03/duplicate)"
    - "Built-in and Category base definitions are immutable: the DAO rejects rename/delete/rule-edit targeting a builtin:* or category:* ref; only visibility and membership overrides may be written for them (ORRS-03)"
    - "Reset Membership Overrides deletes the include AND exclude rows for a System ref in one transaction and leaves system_rules untouched (ORRS-02)"
    - "Reorder writes display_order in system_prefs for the visible Systems; every UPDATE/DELETE asserts changes===1 scoped by id so a stale target rolls back (ORRS-09)"
    - "Hide/show writes hidden on system_prefs for a builtin/category/custom ref; All Contacts can never be hidden (ORRS-03/09)"
    - "reorderSystems and setSystemHidden VALIDATE each submitted ref against the actual complete catalog (built-in ids ∪ live category uids ∪ existing custom uids) and reject a duplicate or nonexistent ref, so a bare system_ref string can never create prefs for a phantom System (Codex review 30-03 MEDIUM)"
    - "saveSystemDefinitionCore composes create/rename + setSystemRules + override replacement inside ONE inWriteTransaction using non-mutexed cores — never composing public wrappers (transaction.ts is explicitly non-reentrant), so a partial failure leaves NO half-created System (Codex review 30-08 HIGH, orchestrator-verified real)"
    - "setSystemOverride(ref, contactId, mode | null) is the single override persistence op: it upserts an include/exclude row or deletes it on null, honoring UNIQUE(system_ref, contact_id) so toggling include↔exclude replaces the one row (Codex review 30-07 MEDIUM)"
    - "pruneSystemExclusionsCore(ref, prunableContactIds) deletes stale exclusion rows in a caller's transaction — the write owner for the resolver's prunableExclusionContactIds; a read never triggers it (Codex review 30-02 HIGH)"
    - "restoreDeletedSystem re-inserts the definition preserving the ORIGINAL uid (so the custom:<uid> ref is stable and ref-keyed overrides/prefs match), obtains/uses the row id, and re-inserts system_rules against that id in one transaction — snapshot values are portable, never replayed against a stale system_id (Codex review 30-03 HIGH)"
    - "Duplicating the All Contacts built-in produces a custom System carrying a single system-generated scope:population rule (not 'no rules'), so its membership equals All Contacts exactly (Codex review 30-03 HIGH)"
  artifacts:
    - src/db/systems-dao.ts (full CRUD/duplicate/reorder/visibility/overrides/reset + atomic save + prune)
    - src/db/systems-dao.test.ts
  key_links:
    - "systems-dao writers ↔ system-rule-resolver reads (rules/overrides) ↔ system_prefs ordering+visibility consumed by buildSystemChoices"
    - "duplicate built-in/Category → mapBuiltinPredicateToRules → new custom systems + system_rules rows (All Contacts → scope:population)"
    - "saveSystemDefinitionCore (one txn) ← builder Save (plan 08); pruneSystemExclusionsCore ← builder Save prunes stale exclusions"
    - "restoreDeletedSystem (preserve uid) ← management Undo (plan 06)"
  prohibitions:
    - "Never write Systems SQL outside systems-dao — components call the DAO"
    - "Never let a System delete touch contact data — deletion is metadata-only (ORRS-10)"
    - "Never permit a rename/delete/rule write against a builtin:* or category:* ref — those base definitions are immutable (ORRS-03)"
    - "Never allow All Contacts to be hidden or deleted (pinned first, nondeletable)"
    - "Never interpolate a name/value/id into SQL — every value is ?-bound"
---

<objective>
Complete the Systems DAO: the single sanctioned writer of the Systems tables, covering create/rename/delete/duplicate/reorder/hide-show, membership overrides (include/exclude/reset), rule writes for custom Systems, case-insensitive name uniqueness, and built-in/Category immutability — each mutation guarded by changes===1 and composed through the shared write transaction.

Purpose: The management screen (plan 06), builder (plan 08), and Manage Members (plan 07) all call this DAO. Centralizing every write here (with the immutability, uniqueness, and never-touch-contacts guarantees enforced at the write boundary, not just the UI) is what keeps the invariants true regardless of which surface calls in.

Output: A full systems-dao with core+mutex pairs for every mutation, deterministic duplicate naming, override reset, and immutability guards, fully unit-tested.

## Artifacts this plan produces
- `src/db/systems-dao.ts`: `renameSystemCore`/`renameSystem`, `deleteSystemCore`/`deleteSystem`, `restoreDeletedSystemCore`/`restoreDeletedSystem`, `duplicateSystemCore`/`duplicateSystem`, `reorderSystemsCore`/`reorderSystems`, `setSystemHiddenCore`/`setSystemHidden`, `setSystemRulesCore`/`setSystemRules`, `resetSystemOverridesCore`/`resetSystemOverrides`, `setSystemOverrideCore`/`setSystemOverride` (upsert/replace/delete), `pruneSystemExclusionsCore`, `saveSystemDefinitionCore`/`saveSystemDefinition` (atomic composite), `removeSystemOverride`, `nextDuplicateName`, `mapBuiltinPredicateToRules`, `assertMutableCustomRef`, `assertKnownSystemRef` (catalog validation), `DeletedSystemSnapshot` (portable, uid-preserving)

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Codex 30-08 HIGH (save atomicity, orchestrator-verified real):** `saveSystemDefinitionCore` composes the non-mutexed cores (createCustomSystemCore/renameSystemCore + setSystemRulesCore + override cores) in ONE `inWriteTransaction`; public wrappers are never nested (Task 2). `transaction.ts` is documented non-reentrant.
- **Codex 30-03 HIGH (All Contacts duplication):** `mapBuiltinPredicateToRules('builtin:all-contacts')` emits a single `scope`/`population` rule (Task 2), so the duplicate's membership matches All Contacts exactly (resolver base-mode from plan 02).
- **Codex 30-03 HIGH (undo ID remapping):** `restoreDeletedSystem` preserves the original uid and re-inserts rules against the new row id; the snapshot stores portable values, never a stale system_id (Task 1).
- **Codex 30-07 MEDIUM (single override replacement):** `setSystemOverride(ref, contactId, mode | null)` upsert/delete (Task 1).
- **Codex 30-02 HIGH (stale-exclusion prune owner):** `pruneSystemExclusionsCore` is the write path for the resolver's prunable ids, invoked on save by plan 08 (Task 1).
- **Codex 30-03 MEDIUM (reorder/hide catalog validation):** `assertKnownSystemRef` rejects unknown/duplicate refs (Task 3).
- **Claude 30-01 LOW (ref-keyed orphan integrity):** a delete test asserts ref-keyed overrides/prefs are gone in the same transaction (Task 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename, delete-with-fallback, override reset, immutability guards</name>
  <files>src/db/systems-dao.ts, src/db/systems-dao.test.ts</files>
  <read_first>
    - src/db/systems-dao.ts (the create/add reads from plan 01 — extend, don't fork)
    - src/db/memories-dao.ts lines 77-88 (assertOneChange changes===1 guard), 296-305 (public wrapper: inWriteTransaction + bumpDataRevisionCore), 184-235 (partial-update builder — only present fields written)
    - src/logic/orrery-system-logic.ts (systemRefId / the {kind:'custom'|'builtin'|'category'} refs — to gate immutability)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §R (delete: simple confirm, never touches contact data, fall back to All Contacts if active, short-lived Undo, no 30-day quarantine), §T (unique case-insensitive names; built-in names protected), §Q (All Contacts pinned first, nondeletable, nonrenamable), §E (Reset Membership Overrides)
  </read_first>
  <behavior>
    - renameSystem on a custom System with a free name succeeds; a case-different collision (against custom names, built-in labels, OR live Category names — cross-catalog assertUniqueSystemName from plan 01) throws the §T copy; an empty/whitespace name throws the empty-name copy; rename on a builtin:*/category:* ref throws an immutability error.
    - deleteSystem on a custom System deletes its systems row + cascaded rules + its ref-keyed overrides + prefs in one transaction, asserts a contact-row count unchanged, and returns a PORTABLE DeletedSystemSnapshot for Undo; deleteSystem on a builtin/category ref throws.
    - The DeletedSystemSnapshot stores portable values only: the original `uid`, `name`, the rule rows as `{family, value}` (NOT their old system_id/rule id), the override rows as `{contactId, mode}`, and the prefs as `{displayOrder, hidden}`. restoreDeletedSystem re-inserts the systems row PRESERVING the original uid (so systemRefId stays `custom:<uid>` and the ref-keyed overrides/prefs line up), takes the resulting row id, and inserts system_rules against THAT new id — never replaying a stale system_id (Codex review 30-03 HIGH).
    - setSystemOverride(ref, contactId, mode | null): mode 'include'/'exclude' upserts the single UNIQUE(system_ref, contact_id) row (replacing the opposite mode in place); mode null deletes it. This is the sole override write op.
    - pruneSystemExclusionsCore(exec, ref, prunableContactIds): deletes the named stale exclusion rows for the ref (a caller's intentional write, e.g. builder Save) — never called from a read.
    - resetSystemOverrides deletes include+exclude rows for a ref in one transaction and leaves system_rules untouched.
    - assertMutableCustomRef(ref) throws for builtin/category refs (used by rename/delete/setSystemRules).
  </behavior>
  <action>
Extend systems-dao.ts. Add `assertMutableCustomRef` throwing an immutability error unless the ref is `{kind:'custom'}`. Add `renameSystemCore`/`renameSystem`: trim the name, throw the empty-name copy if blank, call assertUniqueSystemName (cross-catalog, plan 01) with excludeId, then UPDATE systems SET name=?, modified_at=? WHERE id=? with assertOneChange. Add `deleteSystemCore`/`deleteSystem` returning a PORTABLE `DeletedSystemSnapshot` { uid, name, rules:[{family,value}], overrides:[{contactId,mode}], prefs:{displayOrder,hidden} }: assert the ref is custom, capture the snapshot, DELETE FROM system_overrides WHERE system_ref=?, DELETE FROM system_prefs WHERE system_ref=?, DELETE FROM systems WHERE id=? (system_rules cascade via FK), each guarded; bump revision. Add `restoreDeletedSystemCore`/`restoreDeletedSystem(snapshot)`: re-INSERT the systems row with the snapshot's ORIGINAL uid (assertUniqueSystemName may be skipped only for the exact restored uid/name), read back the new id, INSERT the rules against that id, and re-insert the ref-keyed overrides/prefs (ref recomputed from the preserved uid) — all in one transaction. Add `setSystemOverrideCore`/`setSystemOverride(systemRef, contactId, mode|null)` doing INSERT … ON CONFLICT(system_ref, contact_id) DO UPDATE SET mode=? for a mode, or DELETE for null. Add `pruneSystemExclusionsCore(exec, systemRef, prunableContactIds)` deleting the listed exclude rows (?-bound IN(…)). Add `resetSystemOverridesCore`/`resetSystemOverrides(systemRef)` deleting include+exclude rows for the ref, leaving rules intact. Every value ?-bound.
  </action>
  <verify>
    <automated>npx vitest run src/db/systems-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: rename to a case-different existing name (custom, built-in label, or Category name) throws the §T copy; empty name throws the empty-name copy; rename of a builtin ref throws
    - Test: delete of a custom System removes rules+overrides+prefs and leaves the contacts table row-count unchanged; a follow-up read confirms zero ref-keyed system_overrides/system_prefs rows remain (Option-A cleanup, Claude 30-01 LOW)
    - Test: delete then restoreDeletedSystem reproduces the rules against the NEW row id and the same custom:<uid> ref; the restored System resolves identical membership (undo ID-remapping, Codex 30-03 HIGH)
    - Test: setSystemOverride toggling include→exclude leaves exactly one row with mode 'exclude' (UNIQUE replacement); mode null deletes it
    - Test: pruneSystemExclusionsCore removes only the named exclusion rows
    - Test: delete of a builtin/category ref throws (immutable); resetSystemOverrides removes include+exclude rows and keeps system_rules
    - `grep -rn "INSERT INTO system\|UPDATE system\|DELETE FROM system" src/components src/screens` returns nothing (DAO-only)
  </acceptance_criteria>
  <done>Rename, contact-safe delete, uid-preserving Undo restore, single-op override replacement, exclusion prune, override reset, and immutability guards are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Duplicate (deterministic naming) + built-in/Category predicate → editable rules</name>
  <files>src/db/systems-dao.ts, src/db/systems-dao.test.ts</files>
  <read_first>
    - src/logic/orrery-system-logic.ts lines 82-113 (the builtin predicate map: favorites→FAVOURITES, needs-attention→needs-attention 'on', chargers→social-battery 'Charger', not-contacted, snoozed, all-contacts; category→category uid) — the source of truth for converting a base predicate into rules
    - src/db/systems-dao.ts (createCustomSystemCore + setSystemRules seam)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §S (duplicate: all kinds duplicable; built-in/category base predicate becomes editable custom rules; deterministic uniqueness-aware naming Copy/Copy 2/Copy 3; generated name immediately editable)
  </read_first>
  <behavior>
    - nextDuplicateName("Inner Circle") → "Inner Circle Copy"; if taken → "Inner Circle Copy 2"; case-insensitive, deterministic, and cross-catalog-aware (never collides with a built-in label or Category name).
    - duplicateSystem of a custom System copies its rules + manual overrides into a new custom System with the duplicate name.
    - duplicateSystem of a built-in (e.g. Chargers) creates a custom System whose system_rules encode the equivalent predicate (social-battery = Charger), editable thereafter.
    - duplicateSystem of the All Contacts built-in creates a custom System carrying a single `scope`/`population` rule (NOT zero rules) so its membership equals All Contacts exactly — an empty-rule custom would resolve from ACTIVE_SEGREGATION and drop never-contacted contacts (Codex review 30-03 HIGH).
    - duplicateSystem of a Category System creates a custom System with a category rule for that category uid.
    - saveSystemDefinitionCore(exec, draft) creates-or-renames the System, replaces its rules, and applies override intent (setSystemOverrideCore per changed contact) + prunes stale exclusions — all composed from non-mutexed cores inside ONE inWriteTransaction, so a mid-sequence failure rolls the whole save back (no half-created System).
  </behavior>
  <action>
Add `nextDuplicateName(baseName, existingNamesLowercased)` implementing the Copy / Copy N deterministic pattern with CI uniqueness. Add `mapBuiltinPredicateToRules(ref)` returning the SystemRule `{family,value}` rows equivalent to a built-in/Category base predicate (drawn from the buildOrrerySystemWhere mapping — favorites→FAVOURITES, needs-attention→needs-attention 'on', chargers→social-battery 'Charger', not-contacted, snoozed, category→category uid; ALL-CONTACTS → a single `{family:'scope', value:'population'}` rule so the resolver's population base-mode reproduces its membership — plan 02). Add `duplicateSystemCore`/`duplicateSystem(ref)`: compute the source name (built-in label / category name / custom name), createCustomSystemCore with nextDuplicateName, then setSystemRulesCore with either the copied custom rules (+ copied overrides) or the mapped built-in/category predicate rows. Add `saveSystemDefinitionCore(exec, draft)` and public `saveSystemDefinition`: the SINGLE atomic composite used by the builder (plan 08) — inside one inWriteTransaction, call createCustomSystemCore (new) or renameSystemCore (edit), setSystemRulesCore, apply the override intent via setSystemOverrideCore per changed contact, pruneSystemExclusionsCore for stale exclusions, then bumpDataRevisionCore. NEVER compose the public wrappers (transaction.ts non-reentrant, Codex review 30-08 HIGH). All ?-bound.
  </action>
  <verify>
    <automated>npx vitest run src/db/systems-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: nextDuplicateName returns Copy, then Copy 2, then Copy 3 as collisions accrue (case-insensitive)
    - Test: duplicating the Chargers built-in yields a custom System with a social-battery=Charger rule
    - Test: duplicating All Contacts yields a custom System with a scope:population rule and identical membership to the built-in All Contacts
    - Test: duplicating a custom System copies its rules and manual overrides
    - Test: saveSystemDefinition creates a System + rules + overrides atomically; a forced failure mid-save (e.g. a duplicate name at the rule step) leaves NO partial systems/rules/overrides rows (single-transaction rollback)
    - `grep -n "saveSystemDefinitionCore" src/db/systems-dao.ts` shows it calls only `…Core` helpers (never the public inWriteTransaction wrappers)
  </acceptance_criteria>
  <done>Duplication produces editable custom Systems (All Contacts via scope:population); saveSystemDefinition is the atomic single-transaction composite for the builder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reorder + hide/show over system_prefs (All Contacts protected)</name>
  <files>src/db/systems-dao.ts, src/db/systems-dao.test.ts</files>
  <read_first>
    - src/db/systems-dao.ts (listSystemPrefs read from plan 01)
    - src/db/memories-dao.ts lines 184-235 (partial-update builder / upsert idiom)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §U (dropdown order follows management order; All Contacts pinned first; hidden omitted), §R (built-in/category hideable except All Contacts; custom deletable not hideable)
  </read_first>
  <behavior>
    - setSystemHidden('builtin:all-contacts', true) throws (never hideable).
    - setSystemHidden for any other KNOWN ref upserts hidden into system_prefs; an unknown ref (no matching built-in id / live category uid / existing custom uid) throws.
    - reorderSystems(orderedRefs) validates the whole set against the actual complete catalog (assertKnownSystemRef per ref) and rejects a duplicate or nonexistent ref, then upserts display_order per ref; All Contacts always resolves first regardless of the supplied order.
    - A prefs upsert asserts a single-row effect; a duplicate ref is rejected by the UNIQUE(system_ref) constraint.
  </behavior>
  <action>
Add `assertKnownSystemRef(exec, ref)` validating a ref against the complete catalog: built-in ids (BUILTIN_SYSTEM_LABELS keys), live category uids (`SELECT uid FROM categories`), and existing custom uids (`SELECT uid FROM systems`); throw on an unknown ref — parseSystemRef validates grammar only, not existence (Codex review 30-03 MEDIUM). Add `setSystemHiddenCore`/`setSystemHidden(systemRef, hidden)`: reject `builtin:all-contacts`; call assertKnownSystemRef; upsert (INSERT … ON CONFLICT(system_ref) DO UPDATE) hidden + modified_at into system_prefs (uid via newUid on insert). Add `reorderSystemsCore`/`reorderSystems(orderedRefs)`: assertKnownSystemRef for each ref and reject duplicates in the submitted set, then upsert display_order in the given order, forcing All Contacts to order 0; every value ?-bound; bump revision. Ensure listSystemPrefs returns the rows the switcher/management consume.
  </action>
  <verify>
    <automated>npx vitest run src/db/systems-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: setSystemHidden('builtin:all-contacts', true) throws
    - Test: setSystemHidden persists hidden for a category ref and listSystemPrefs reads it back
    - Test: reorderSystems persists display_order and All Contacts resolves to order 0
    - Test: reorderSystems/setSystemHidden with a nonexistent custom:<uid> or duplicate ref throws (catalog validation)
    - `npx vitest run src/db/systems-dao.test.ts` passes; `npm test` green
  </acceptance_criteria>
  <done>Reorder and hide/show persist to system_prefs with All Contacts protected and refs validated against the complete catalog; the DAO surface the management/switcher plans need is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| management/builder UI → systems-dao | names, rule values, contact ids, order cross into SQL here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-08 | Tampering | DAO INSERT/UPDATE/DELETE | high | mitigate | Every value ?-bound; mode CHECK-constrained; assertOneChange scopes each mutation by id and rolls back stale targets |
| T-30-09 | Elevation of privilege | write to an immutable base | high | mitigate | assertMutableCustomRef rejects rename/delete/rule writes to builtin:*/category:*; All Contacts hide/delete blocked |
| T-30-10 | Tampering | duplicate CI name | medium | mitigate | name COLLATE NOCASE uniqueness enforced on create AND rename before insert |
| T-30-11 | Repudiation/data loss | delete touching contacts | high | mitigate | delete is metadata-only over Systems tables; a test asserts the contacts row-count is unchanged; DeletedSystemSnapshot supports Undo |
</threat_model>

<verification>
- `npm test` green; the systems-dao suite covers uniqueness, immutability, contact-safe delete, duplicate naming, reset, reorder, hide/show.
- `grep -rn "INSERT INTO system\|UPDATE system\|DELETE FROM system" src/components src/screens` returns nothing.
- Biome lint passes.
</verification>

<success_criteria>
systems-dao is the sole Systems writer with every ORRS-03/09/10 write path implemented, guarded, and unit-proven; contact data is provably untouched by any System operation.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-03-SUMMARY.md` — record the DeletedSystemSnapshot shape and the exact DAO function signatures the management (plan 06), builder (plan 08), and Manage Members (plan 07) plans call.
</output>
