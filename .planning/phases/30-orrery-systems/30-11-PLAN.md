---
phase: 30-orrery-systems
plan: 11
type: execute
wave: 7
depends_on: [30-05, 30-06, 30-08, 30-09, 30-10]
files_modified:
  - src/logic/orrery-switch-choreography.ts
  - src/logic/orrery-switch-choreography.test.ts
  - src/logic/orrery-frame.ts
  - src/logic/orrery-frame.test.ts
  - src/components/orrery/orrery-switch-animation.ts
  - src/components/orrery/orrery-switch-animation.test.ts
autonomous: true
requirements: [ORRS-13]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-11 and ORRS-13, the normal-motion model has explicit acceleration, shedding, capture, and settling phases; it is not the Phase-29 fade/shrink/grow transition under a different name."
    - "Each contact is classified by stable key as retained, leaving, or entering, and deterministic samples prove leaving contacts peel/spin outward, entering contacts stream/spin inward, and retained contacts remain continuously visible."
    - "The transition retains both source and destination geometry, preserves the currently displayed sample when interrupted or re-targeted, and ends exactly at canonical destination geometry without a snap."
    - "Membership turnover scales angular impulse and trajectory displacement: high-overlap switches are restrained and high-turnover switches are materially more dramatic."
    - "Normal motion uses a non-reversing rotational velocity envelope that visibly accelerates and later decelerates; Reduced Motion alone uses direct crossfade/reposition with no peel, stream, or rotational sweep."
    - "Equal-step samples mathematically prove positive non-reversing angular velocity that increases during acceleration, decreases during deceleration, and reaches zero/near-zero at settle; multi-sample radial deltas likewise prove outward shedding and inward capture rather than endpoint teleportation."
    - "The engine is deterministic, worklet-safe, elapsed-progress driven, and pausable without advancing while the Orrery is blurred or the app is backgrounded."
  artifacts:
    - "src/logic/orrery-switch-choreography.ts — role classification, staged timeline, motion envelopes, trajectory sampling, interruption/re-target state, and Reduced Motion branch"
    - "src/logic/orrery-switch-choreography.test.ts — deterministic trajectory, phase-order, continuity, delta-scaling, interruption, pause, and Reduced Motion assertions"
    - "src/logic/orrery-frame.ts — one frame pipeline extended to consume the complete choreography sample"
  key_links:
    - "source world + destination world + membership delta -> beginSwitchChoreography -> role-bearing transition state"
    - "progress + transition state -> sampleSwitchChoreography -> one displayed world used by projection, rings, bodies, labels, and hits"
    - "interrupted displayed sample -> re-targeted transition source -> position/radius/opacity continuity at the interruption boundary"
    - "Reduced Motion signal -> non-rotational crossfade/reposition branch only"
  prohibitions:
    - "Do not equate opacity/radius fade/grow at final coordinates with shed/capture."
    - "Do not implement the timeline with per-frame React state."
    - "Do not create separate competing body and ring transition pipelines."
    - "Do not make a normal transition rotate out and reverse back; acceleration and deceleration govern forward accumulated angular travel."
---

<objective>
Replace the mislabeled Phase-29 continuity effect with a deterministic choreography engine that makes every observable part of D-11 executable and testable before renderer tuning.

Purpose: ORRS-13 remains incomplete. This plan restores the owner-approved semantic contract at the motion-model boundary so later integration cannot satisfy it with vocabulary, symbol presence, or merely nonzero movement.
Output: A worklet-safe staged choreography model, one extended world-frame path, and property-level tests for roles, trajectories, phase ordering, continuity, interruption, scaling, pause, and Reduced Motion.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-CONTEXT.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@.planning/phases/30-orrery-systems/30-VALIDATION.md
@.planning/phases/30-orrery-systems/30-10-SUMMARY.md
@.planning/forensics/report-20260909T030240Z.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@docs/runbooks/orrery-visual-layer.md
</context>

## Artifacts this phase produces

- `src/logic/orrery-switch-choreography.ts`: the canonical D-11/ORRS-13 animation state and sampler.
- `src/logic/orrery-switch-choreography.test.ts`: behavioral proof of visible trajectory properties rather than token/wiring checks.
- Extended `orrery-frame` integration that keeps one sampled world authoritative for every visual and interaction consumer.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one complete disjoint-System choreography through the projected frame</name>
  <files>src/logic/orrery-switch-choreography.ts, src/logic/orrery-switch-choreography.test.ts, src/logic/orrery-frame.ts, src/logic/orrery-frame.test.ts</files>
  <read_first>
    - src/logic/orrery-frame.ts (full file; existing transition starts entries at final coordinates and only fades/shrinks, which D-11 explicitly rejects)
    - src/logic/orrery-frame.test.ts (full file; preserve projection, hit, culling, stable-order, and interrupted-size coverage while replacing the insufficient switch assertion)
    - src/logic/orrery-camera-logic.ts (WorldBody, projectFrame, and ringPath behavior; one sampled world must remain authoritative)
    - src/logic/orrery-world-logic.ts (canonical contact x/y/ringRadius/angle geometry and deterministic identity)
    - .planning/phases/30-orrery-systems/30-CONTEXT.md D-11 and docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md sections V/W/X
  </read_first>
  <behavior>
    - "RED: a source containing one leaving contact and a disjoint destination containing one entering contact produces a staged transition with explicit leaving/entering roles."
    - "At an acceleration sample the current world has accumulated forward angular displacement; at shedding the leaving contact has greater radial distance than at source; during capture the entering contact is outside its destination radius and moving inward; at completion the entering contact equals its exact destination geometry."
    - "Projection consumes that same sampled world, so body, ring, visibility, hit authority, and stable key order cannot disagree."
    - "At equal progress steps wholly inside `accelerate`, successive signed angular increments are positive/non-reversing and strictly increase; at equal steps wholly inside `settle`, they remain positive/non-reversing and strictly decrease, with the final finite-difference velocity zero or within an explicitly small epsilon. A constant-rate or reverse-to-origin curve must fail these assertions."
    - "At three or more ordered samples inside `shed`, a leaving contact's radius from the sun strictly increases at every step; at three or more ordered samples inside `capture`, an entering contact's radial distance to its exact destination strictly decreases at every step. Endpoint-only displacement or publication in batches must fail."
  </behavior>
  <action>Start with failing deterministic tests, then add a pure worklet-safe `orrery-switch-choreography` module and extend the existing frame pipeline to use it. Define stable roles (`retained`, `leaving`, `entering`, plus a non-contact anchor role where needed), explicit normalized phases (`accelerate`, `shed`, `capture`, `settle`), and a transition state retaining both source and destination keyed geometry. Choose concrete initial phase boundaries and easing constants at module top for later Pixel tuning. The rotational envelope must accumulate angle in one direction while angular velocity rises and later falls; do not reuse the reversible `sin(progress*pi)*0.22` displacement as the main motion. Test angular travel at equal normalized progress intervals that lie wholly inside acceleration and wholly inside deceleration/settle: finite differences must stay the same nonzero sign, grow during acceleration, shrink during deceleration, and finish at zero/near-zero velocity under an explicit numeric tolerance, so monotonic constant-rate travel cannot pass. A leaving contact receives a deterministic tangential component plus monotonically increasing radial displacement during shed before fading; an entering contact begins visibly away from/outside its destination orbit, follows a deterministic spinning inward path during capture, and reaches the destination exactly. Prove those direction claims with at least three ordered in-phase samples and strict consecutive radial differences, not only phase endpoints. Retained contacts stay opaque and continuously interpolate source to destination while sharing the system impulse. Feed the sampled bodies through `projectAnimatedFrame` so rings, bodies, labels, culling, and hits still derive from one world. Keep all worklet helpers above callers.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert role identity and actual x/y radial trajectory properties at named phase samples, not only that a symbol exists or a coordinate is nonzero.
    - The leaving sample moves outward from its source orbit before opacity reaches zero; the entering sample starts away from its destination and moves inward before settling exactly on it.
    - Accumulated rotation is forward through acceleration/cruise/deceleration and is not a small rotation that reverses to its starting angle.
    - Equal-step finite differences strictly increase during rotational acceleration, strictly decrease during rotational deceleration, and end with zero/near-zero terminal velocity; a constant-speed curve fails.
    - Three-or-more-sample radial sequences prove every consecutive shedding step moves outward and every consecutive capture step moves inward toward exact destination geometry.
    - `projectAnimatedFrame` receives the choreography sample as its sole world input and existing culling/hit tests remain green.
  </acceptance_criteria>
  <done>A production-quality disjoint switch can be sampled end-to-end from source/destination worlds through one projected frame, visibly expressing accelerate, shed, capture, and exact settle in deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand role continuity, membership-delta drama, and timing envelopes</name>
  <files>src/logic/orrery-switch-choreography.ts, src/logic/orrery-switch-choreography.test.ts, src/components/orrery/orrery-switch-animation.ts, src/components/orrery/orrery-switch-animation.test.ts</files>
  <read_first>
    - src/components/orrery/orrery-switch-animation.ts (full file; preserve membership set math, focus preservation, and switch-vs-reload discrimination)
    - src/components/orrery/orrery-switch-animation.test.ts (full file; existing scalar tests are necessary but insufficient)
    - src/logic/orrery-switch-choreography.ts (Task 1 output and its top-level tuning constants)
    - src/logic/orrery-world-logic.ts (canonical destination geometry and stable UID/id-derived nudge precedent)
  </read_first>
  <behavior>
    - "A retained contact never disappears, never becomes non-interactive merely because the System changed, and has no positional/opacity discontinuity at any adjacent phase boundary."
    - "For the same source geometry, a full-turnover delta produces greater peak radial displacement and accumulated spin than a high-overlap delta; raw population size alone does not determine drama."
    - "Leaving, retained, and entering bodies begin their role-specific motion in the documented order, while capture may overlap late shedding without allowing settle to begin before capture is stable."
    - "The destination is exact at progress 1: keyed destination membership, x/y, ringRadius, radius, and opacity 1; all departures are absent/invisible and inert."
  </behavior>
  <action>Extend the Task-1 model across mixed memberships and count-independent delta scaling. Keep `computeMembershipDelta` as the source of turnover, but convert it into a choreography profile that scales meaningful observable quantities: accumulated angular travel/peak velocity and radial/tangential displacement, with bounded duration variation only if useful on device. Deterministically stagger individual entering/leaving contacts by stable body key within narrow phase windows so a 3-to-9 switch reads as flowing motion rather than React publication batches; all contacts still complete within the single transition. Preserve retained contacts at opacity 1 throughout and interpolate their source/destination ring geometry continuously. Add boundary tests immediately before/at/after every phase boundary and compare high-overlap versus high-turnover samples using displacement and rotation measurements. Keep sun/anchor behavior stable and keep departing bodies non-interactive from transition start.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/components/orrery/orrery-switch-animation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover mixed retained/leaving/entering membership and assert retained opacity/position continuity across all phase boundaries.
    - Tests prove high turnover causes strictly larger peak displacement and accumulated rotation than high overlap for equivalent geometry.
    - Per-contact staggering is deterministic and continuous, never dependent on array publication timing or frame count.
    - No test treats fade/shrink/grow at final coordinates as evidence of shedding/capture.
  </acceptance_criteria>
  <done>The choreography scales visibly with membership turnover and preserves retained-contact continuity across explicit, boundary-tested phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make interruption, re-targeting, pause, and Reduced Motion semantics deterministic</name>
  <files>src/logic/orrery-switch-choreography.ts, src/logic/orrery-switch-choreography.test.ts, src/logic/orrery-frame.ts, src/logic/orrery-frame.test.ts</files>
  <read_first>
    - src/logic/orrery-switch-choreography.ts (Tasks 1-2 output)
    - src/logic/orrery-frame.ts (full current transition/re-target path; preserve displayed geometry rather than the stale original source)
    - src/stores/orrery-system-store.ts (full file; outgoing snapshot remains mounted through destination load and selection generations own publication)
    - src/components/orrery/use-orrery-camera.ts (enabled/shared-value lifecycle and cancellation precedent)
    - src/theme/use-reduced-motion.ts (live OS/shared-app source; no System-specific preference)
  </read_first>
  <behavior>
    - "Re-targeting A->B midway into A->C begins from the exact displayed x/y/radius/opacity for every surviving key; contacts may change roles without a one-frame snap."
    - "A paused progress cell samples identical geometry until resumed; lifecycle pause never advances from wall-clock time while the canvas is not live."
    - "Reduced Motion produces only direct crossfade/reposition: no accumulated rotation, no outward peel, no inward stream, and exact destination settle."
    - "Same-System reload/session restore remains outside the switch choreography and cannot inherit nonzero transition drama."
  </behavior>
  <action>Add a re-target constructor that consumes the currently displayed choreography sample, not the original source snapshot. Preserve keyed body resources and current opacity/radius/position when a contact changes role across rapid A->B->C selection. Make progress the only animation time input so lifecycle integration can pause by cancelling/holding its SharedValue and resume without elapsed wall-clock jumps. Define the Reduced Motion branch inside the same state/sampler contract: short direct position/ring/radius interpolation and crossfade, with angular and radial choreography terms exactly zero. Preserve the existing same-System reload/session-restore gate from `switchTransitionIntensity`; a zero/non-switch profile must use ordinary frame continuity rather than a latent prior switch. Add exact boundary and re-target tests, including an entering body re-targeted to leaving and a retained body preserved across all three Systems.</action>
  <verify>
    <automated>npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts src/components/orrery/orrery-switch-animation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Re-target tests compare the old sample and new transition at progress 0 for exact keyed x/y/radius/opacity equality.
    - Pause tests prove repeated samples at an unchanged progress value are byte-stable.
    - Reduced Motion tests assert zero rotational/radial deviation while preserving crossfade/reposition and exact final geometry.
    - Same-System reload and session restore tests retain zero choreography intensity.
  </acceptance_criteria>
  <done>Rapid re-targets, lifecycle pauses, and live Reduced Motion changes have explicit deterministic semantics with no snap, hidden time advance, or leaked spectacle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JS scene publication -> Reanimated/Skia worklet | Immutable source/destination worlds and a discrete generation cross onto the UI-thread animation path. |
| Rendered transition -> user input | Departing decorative bodies must not remain actionable, while stable destination bodies must not be unnecessarily locked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-11-01 | Denial of Service | worklet choreography sampler | high | mitigate | Keep the sampler pure and bounded O(n), define worklet helpers above callers, run compiled frame-mapper/device coverage in Plan 12, and introduce no per-frame React state or allocation-growing cache. |
| T-30-11-02 | Tampering | stale or mismatched transition generations | medium | mitigate | Key by stable body identity, retain generation in transition state, and start re-targets from the displayed sample with deterministic tests. |
| T-30-11-03 | Elevation of Privilege | decorative departing hit target | medium | mitigate | Mark departures inert from transition start and retain the shared projected frame as the only hit authority. |
| T-30-11-04 | Information Disclosure | animation state | low | accept | The state contains only on-device geometry/ids already rendered locally; no network, telemetry, log export, or new persistence is introduced. |
</threat_model>

<verification>
- `npx vitest run src/logic/orrery-switch-choreography.test.ts src/logic/orrery-frame.test.ts src/components/orrery/orrery-switch-animation.test.ts`
- `npx tsc --noEmit --pretty false`
- `npm run check:colors`
- This plan proves model semantics only. It must not mark ORRS-13 complete or claim visual smoothness; physical Pixel integration and owner approval remain Plan 30-12.
</verification>

<success_criteria>
- Every D-11 normal-motion stage and role has an explicit state/trajectory contract with deterministic property assertions.
- Delta scaling changes visible trajectory and angular quantities, not merely a scalar passed through wiring.
- Interrupt/re-target, pause, exact final settle, and Reduced Motion replacement are tested.
- ORRS-13 remains incomplete pending Plan 30-12 renderer evidence and owner approval.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 30 SC5 | Membership deltas animate proportionally with Reduced Motion alternative | 30-11 + 30-12 | COVERED | Engine here; visible device delivery in 30-12. |
| REQ | ORRS-13 | Normal spin + shedding/capture; Reduced Motion crossfade/reposition | 30-11 + 30-12 | COVERED | No substitution with Phase-29 continuity. |
| RESEARCH | Switch animation | Skia/Reanimated shared values, focus/AppState pause, Pixel-only perf proof | 30-11 + 30-12 | COVERED | Pure model here; runtime/device proof next. |
| CONTEXT | D-11 | Five-stage owner-approved choreography, delta drama, owner Pixel approval | 30-11 + 30-12 | COVERED | Model stages here; checkpoint next. |
| CONTEXT | D-01/D-02/D-10 | Preserve canonical dossier and consume Phase 29 without redefining it | 30-11 + 30-12 | COVERED | Existing world/projection pipeline extended, not forked. |
| CONTEXT | D-09 | Reduced Motion alone gets simple crossfade/reposition | 30-11 + 30-12 | COVERED | Pure zero-spectacle semantics here; live hook next. |
| CONTEXT | D-03..D-08 | Persistence/schema/backup/category/SQL constraints | existing 30-01..30-10 | EXCLUDED | Already delivered and outside this corrective ORRS-13 slice. |

<output>
Create `.planning/phases/30-orrery-systems/30-11-SUMMARY.md`. Do not list ORRS-13 under `requirements-completed`; record it as implemented-model/pending renderer, Pixel evidence, and owner approval.
</output>
