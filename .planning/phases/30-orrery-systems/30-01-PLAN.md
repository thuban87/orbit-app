---
phase: 30-orrery-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/022-orrery-systems.ts
  - src/db/migrations/022-orrery-systems.test.ts
  - src/db/database.ts
  - src/db/systems-dao.ts
  - src/db/systems-dao.test.ts
  - src/logic/orrery-system-logic.ts
  - src/logic/orrery-system-logic.test.ts
  - src/db/app-settings-dao.ts
  - src/db/app-settings-dao.test.ts
  - src/logic/system-rule-resolver.ts
  - src/logic/system-rule-resolver.test.ts
  - src/db/orrery-system-read.ts
  - src/components/orrery/orrery-controls-logic.ts
autonomous: false
requirements: [ORRS-01, ORRS-02, ORRS-12]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Migration 022 runs forward-only from any prior user_version and creates the Systems table set (systems, system_rules, system_overrides, system_prefs) with uid UNIQUE, case-insensitive unique System name, and contact_id ON DELETE CASCADE (ORRS-01/02)"
    - "TARGET_VERSION in src/db/database.ts is bumped to 22 and migration022 is registered in the MIGRATIONS array"
    - "Migration 022 does NOT touch app_settings — orrery_last_system already exists (migration 021); no ALTER TABLE app_settings appears in the 022 body (Pitfall 1)"
    - "OrrerySystemRef gains a third { kind: 'custom'; uid } member; systemRefId emits custom:<uid> and parseSystemRef round-trips it (ORRS-12)"
    - "assertOrreryLastSystem and the OrrerySystemId type accept custom:<uid>; existing builtin:*/category:* tokens still validate and no stored value becomes invalid (ORRS-12)"
    - "A manual-only custom System created via systems-dao resolves its members end-to-end through readOrrerySystemMembersCore and appears in buildSystemChoices, selectable through the existing store select() (ORRS-01/02)"
    - "Creating a second System whose name matches an existing name case-insensitively is rejected before insert with the §T collision error (ORRS-01)"
    - { statement: "Creating the same manual-only System twice does not produce two rows: the case-insensitive unique-name enforcement rejects the duplicate", verification: backstop }
  artifacts:
    - src/db/migrations/022-orrery-systems.ts
    - src/db/systems-dao.ts
    - src/logic/system-rule-resolver.ts
    - src/logic/orrery-system-logic.ts (extended union)
    - src/db/app-settings-dao.ts (widened grammar)
  key_links:
    - "systems-dao createCustomSystem/addSystemOverride writes → system-rule-resolver reads rules/overrides → readOrrerySystemMembersCore routes custom → store select → buildSystemChoices lists it"
    - "orrery_last_system grammar (app-settings-dao) ↔ parseSystemRef/systemRefId (orrery-system-logic) must agree on the custom:<uid> token"
  prohibitions:
    - "Never re-add app_settings.orrery_last_system in migration 022 — it already exists (migration 021)"
    - "Never interpolate a System/category identifier into SQL — every runtime value is ?-bound and family/kind tokens come from closed constants"
    - "Never fork the OrrerySystemRef union or the Phase 29 read pipeline — extend the closed union so custom Systems ride the unchanged pipeline"
    - "Never edit an already-shipped migration (001–021) — migration 022 is additive and forward-only"
---

<objective>
Establish the Systems persistence foundation and prove the whole Phase 30 architecture with one thin, production-quality vertical slice: a manual-only custom System created in the DAO, resolved through the UNCHANGED Phase 29 read pipeline, and selectable in the switcher.

Purpose: Migration 022 is a one-way door (irreversible on unreachable devices). Proving the custom-System identity, persistence, resolution, and switcher wiring end-to-end on one manual-only path catches any architectural dead-end after one commit instead of after the whole authoring UI is built. All later plans expand outward from this proven slice.

Output: Migration 022 + registration + schema-invariant tests; a minimal systems-dao (create + override write + the reads the resolver needs); the OrrerySystemRef `custom` kind + widened `orrery_last_system` grammar; a minimal manual-only resolver; custom-kind routing in the read layer; and custom Systems surfaced by buildSystemChoices.

## Artifacts this plan produces
- `src/db/migrations/022-orrery-systems.ts` (`migration022`) and tables `systems`, `system_rules`, `system_overrides`, `system_prefs`; `TARGET_VERSION = 22`
- `src/db/systems-dao.ts`: `createCustomSystemCore`/`createCustomSystem`, `addSystemOverrideCore`/`addSystemOverride`, `assertUniqueSystemName`, and reads `listCustomSystems`, `getSystem`, `listSystemRules`, `listSystemOverrides`, `listSystemPrefs`
- `src/logic/orrery-system-logic.ts`: `{ kind: "custom"; uid }` union member + `systemRefId`/`parseSystemRef` custom branch
- `src/db/app-settings-dao.ts`: `OrrerySystemId` `custom:${string}` + `assertOrreryLastSystem` custom branch
- `src/logic/system-rule-resolver.ts`: `resolveCustomSystemMembers` (manual-only path in this plan), `SystemRule`/`SystemOverride` types
- `src/db/orrery-system-read.ts`: custom-kind routing inside `readOrrerySystemMembersCore`
- `src/components/orrery/orrery-controls-logic.ts`: custom Systems added to `buildSystemChoices`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md

# Key seams (read the actual code, not just these references)
@src/db/migrations/016-contact-knowledge.ts
@src/db/migrations/021-orrery-preferences.ts
@src/db/memories-dao.ts
@src/logic/orrery-system-logic.ts
@src/db/orrery-system-read.ts
@src/db/app-settings-dao.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Sign off the migration 022 Systems table shape (one-way door)</name>
  <reversibility rating="one-way">Migration 022 is forward-only and irreversible in production — there is no remote DB access to repair a wrong shape on a user's device. The exact table decomposition is [DERIVED]/Claude's discretion (dossier §B: rules, manual inclusions, and exclusions are separately represented; D-04: ordering + visibility are definition-level).</reversibility>
  <decision>Confirm the Systems table decomposition for migration 022 before the irreversible schema is written.</decision>
  <context>
Migration 022 ships as application code and runs forward-only on every device from any prior version; a wrong shape cannot be repaired remotely (there is no server, no remote DB access — CLAUDE.md). The exact decomposition is [DERIVED]/Claude's discretion, so a human should sign off the shape before it is frozen. The recommended shape (Option A) keys manual overrides and ordering/visibility by the `system_ref` TEXT token so built-in, Category, and custom Systems share one representation — built-in/Category Systems are NOT rows in `systems` (they are derived) yet must carry visibility + overrides (§G/§H).
  </context>
  <options>
    <option id="ref-keyed-four-table">
      <name>Option A (recommended): four tables, overrides + prefs keyed by system_ref</name>
      <pros>`systems` (id, uid UNIQUE, name, created_at, modified_at) + CI-unique name index; `system_rules` (system_id FK CASCADE, family, value, UNIQUE(system_id,family,value)); `system_overrides` (system_ref TEXT, contact_id FK CASCADE, mode CHECK include/exclude, UNIQUE(system_ref,contact_id)); `system_prefs` (system_ref UNIQUE, display_order, hidden). One override/pref representation shared across all three System kinds; custom-delete cleans ref-keyed rows in the DAO transaction; rules cascade via FK.</pros>
      <cons>Overrides/prefs are not FK-bound to `systems` (custom-delete cleanup is DAO-explicit, not DDL cascade); D-04's "ordering/visibility as definition columns" is satisfied via a ref-keyed table rather than columns on `systems`.</cons>
    </option>
    <option id="columns-on-systems">
      <name>Option B: ordering/visibility as columns on `systems`, a separate table only for built-in/Category customization</name>
      <pros>Matches D-04's literal "definition columns" wording for custom Systems.</pros>
      <cons>Splits ordering/visibility across two sources (custom on `systems`, built-in/Category elsewhere), complicating the single cross-kind switcher order; more join logic; two override representations.</cons>
    </option>
  </options>
  <resume-signal>Select: ref-keyed-four-table (Option A) or columns-on-systems (Option B), or describe an alternative shape.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Migration 022 + registration + schema-invariant tests</name>
  <files>src/db/migrations/022-orrery-systems.ts, src/db/migrations/022-orrery-systems.test.ts, src/db/database.ts</files>
  <read_first>
    - src/db/migrations/016-contact-knowledge.ts (the multi-table additive analog: named CREATE_* consts, Migration object, no BEGIN in body — the runner owns the transaction)
    - src/db/migrations/021-orrery-preferences.ts (confirms orrery_last_system + orrery_density + orrery_satellites_enabled already exist on app_settings — do NOT re-add)
    - src/db/migrations/020-dashboard-swipe-pref.test.ts AND src/db/migrations/019-dashboard-prefs.test.ts (the node:sqlite schema-invariant test analog)
    - src/db/database.ts lines 50-80 (TARGET_VERSION = 21; the MIGRATIONS array with migration001…migration021)
    - src/db/migrations/runner.ts (atomic per-step transaction; forward-only)
    - src/db/migrations/001-initial.ts lines 41-49 (categories table shape: id, uid TEXT UNIQUE, name, display_order — the contact_id/uid/cascade conventions to mirror)
  </read_first>
  <behavior>
    - Re-verify head+1 on disk first: `ls src/db/migrations/` head is 021 and `TARGET_VERSION` is 21, so this migration is 022. If disk shows otherwise, STOP and use the observed number (D-03).
    - Test: applying migrations 001→022 from a fresh DB yields user_version = 22.
    - Test: applying 022 onto a DB already at 021 succeeds and leaves the four new tables present.
    - Test: `systems.uid` and every table's `uid` reject a duplicate (UNIQUE).
    - Test: two `systems` rows whose names differ only by case are rejected by the CI-unique index (`name COLLATE NOCASE`).
    - Test: `UNIQUE(system_id, family, value)` on system_rules and `UNIQUE(system_ref, contact_id)` on system_overrides both reject duplicates.
    - Test: deleting a contact cascades away its system_overrides rows (contact_id FK ON DELETE CASCADE).
    - Test: deleting a `systems` row cascades away its system_rules rows (system_id FK ON DELETE CASCADE).
    - Test: the 022 body contains no `app_settings` ALTER (grep the migration source in the test or by inspection).
  </behavior>
  <action>
Create migration 022 mirroring 016's structure: named exported `CREATE_SYSTEMS`, `CREATE_SYSTEM_RULES`, `CREATE_SYSTEM_OVERRIDES`, `CREATE_SYSTEM_PREFS` SQL constants plus separate `CREATE UNIQUE INDEX`/`CREATE INDEX` statements, and a `migration022: Migration = { version: 22, async apply(exec, _deps) { … } }` that calls `exec.execAsync` per statement. NO `BEGIN` in the body — the runner owns the transaction. Follow the confirmed four-table shape from Task 1: `systems` (id, uid TEXT UNIQUE NOT NULL, name TEXT NOT NULL, created_at, modified_at) with `CREATE UNIQUE INDEX idx_systems_name_nocase ON systems(name COLLATE NOCASE)`; `system_rules` (id, uid TEXT UNIQUE NOT NULL, system_id INTEGER NOT NULL REFERENCES systems(id) ON DELETE CASCADE, family TEXT NOT NULL, value TEXT NOT NULL, created_at) with `UNIQUE(system_id, family, value)`; `system_overrides` (id, uid TEXT UNIQUE NOT NULL, system_ref TEXT NOT NULL, contact_id INTEGER NOT NULL REFERENCES contacts(id) ON DELETE CASCADE, mode TEXT NOT NULL CHECK(mode IN ('include','exclude')), created_at) with `UNIQUE(system_ref, contact_id)` and an index on system_ref; `system_prefs` (id, uid TEXT UNIQUE NOT NULL, system_ref TEXT NOT NULL UNIQUE, display_order INTEGER, hidden INTEGER NOT NULL DEFAULT 0 CHECK(hidden IN (0,1)), created_at, modified_at). Do NOT touch app_settings. Register `migration022` (import + append to MIGRATIONS array) in database.ts and bump `TARGET_VERSION` to 22. Write the node:sqlite schema-invariant test covering every behavior above using the SqlExecutor/runner substrate from the neighbor test.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations/022-orrery-systems.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION = 22" src/db/database.ts` matches
    - `grep -n "migration022" src/db/database.ts` shows both the import and the array entry
    - `grep -c "app_settings" src/db/migrations/022-orrery-systems.ts` returns 0
    - `grep -n "COLLATE NOCASE" src/db/migrations/022-orrery-systems.ts` matches (CI unique name)
    - `npx vitest run src/db/migrations/022-orrery-systems.test.ts` passes
  </acceptance_criteria>
  <done>Migration 022 creates the four Systems tables with the integrity constraints; it is registered; TARGET_VERSION is 22; the schema-invariant suite is green.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Thin end-to-end manual-only custom System (DAO + union + grammar + resolver + read + switcher)</name>
  <files>src/db/systems-dao.ts, src/db/systems-dao.test.ts, src/logic/orrery-system-logic.ts, src/logic/orrery-system-logic.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts, src/db/orrery-system-read.ts, src/components/orrery/orrery-controls-logic.ts</files>
  <read_first>
    - src/db/memories-dao.ts lines 1-130, 296-305 (the DAO idiom: imports newUid/inWriteTransaction/bumpDataRevisionCore; non-mutexed `…Core` + public wrapper; assertOneChange changes===1 guard; every value ?-bound)
    - src/logic/orrery-system-logic.ts (the FULL file — the closed union at 24-26, parseSystemRef 44-54, systemRefId 55-62, buildOrrerySystemWhere 73-115)
    - src/db/app-settings-dao.ts lines 48-84 (ORRERY_BUILTIN_SYSTEM_IDS, OrrerySystemId type, assertOrreryLastSystem regex grammar)
    - src/db/orrery-system-read.ts (readOrrerySystemMembersCore 54-75 calls buildOrrerySystemWhere then one member SELECT; the missing-category domain-result pattern 59-65 + MissingOrreryCategoryError 166-171)
    - src/components/orrery/orrery-controls-logic.ts (buildSystemChoices 7-26 currently maps BUILTIN_SYSTEMS + categories)
    - src/stores/orrery-system-store.ts (select() 66-124 — how a ref becomes the active snapshot; the store already handles fallback and MissingOrreryCategoryError)
    - src/db/uid.ts, src/db/data-revision-dao.ts, src/db/transaction.ts (newUid, bumpDataRevisionCore, inWriteTransaction — confirm signatures)
  </read_first>
  <behavior>
    - Grammar (orrery-system-logic.test.ts NEW + app-settings-dao.test.ts): systemRefId({kind:'custom',uid:'abc'}) === 'custom:abc'; parseSystemRef('custom:abc') deep-equals {kind:'custom',uid:'abc'}; assertOrreryLastSystem accepts 'custom:abc' and still accepts 'builtin:all-contacts' and 'category:xyz'; a whitespace/control-char custom token is rejected.
    - DAO (systems-dao.test.ts, node:sqlite): createCustomSystem inserts one systems row with a uid and returns it; a second create with a case-different same name throws the §T collision ("A System named … already exists"); addSystemOverride writes one system_overrides row keyed by the custom:<uid> ref with mode 'include'; listSystemOverrides/listSystemRules/listCustomSystems/getSystem/listSystemPrefs read back correctly.
    - Resolver (system-rule-resolver.test.ts, node:sqlite): resolveCustomSystemMembers for a System with NO rules returns exactly its manual-include contact ids (base scope = ACTIVE_SEGREGATION_WHERE applied to the include set), and never returns an excluded contact.
    - Read (exercised via resolver + orrery-system-read): readOrrerySystemMembersCore for a {kind:'custom',uid} routes to the resolver and returns member rows via a `WHERE c.id IN (?…)` SELECT, ORDER BY the existing ring_seq clause.
  </behavior>
  <action>
Extend the closed union in orrery-system-logic.ts with `| { kind: "custom"; uid: string }`; add the `token.startsWith("custom:")` branch to parseSystemRef (slice 7) and the `custom:${system.uid}` branch to systemRefId; leave buildOrrerySystemWhere's static builtin/category paths unchanged (custom is routed to the resolver, never a WHERE). In app-settings-dao.ts widen `OrrerySystemId` with `` | `custom:${string}` `` and add a parallel `custom:` alternative to the assertOrreryLastSystem regex (same bounded, control-char-free pattern used for category). In systems-dao.ts follow the memories-dao core+mutex idiom: `createCustomSystemCore(exec, {name, now})` (assertUniqueSystemName first — SELECT id FROM systems WHERE name = ? COLLATE NOCASE; throw the §T copy on hit; then INSERT uid via newUid) + public `createCustomSystem` wrapping inWriteTransaction + bumpDataRevisionCore; `addSystemOverrideCore`/`addSystemOverride({systemRef, contactId, mode, now})` INSERT into system_overrides (all ?-bound); reads `listCustomSystems`, `getSystem`, `listSystemRules(systemId)`, `listSystemOverrides(systemRef)`, `listSystemPrefs()`. In system-rule-resolver.ts add `resolveCustomSystemMembers(exec, ref)` that (for this plan's manual-only path) reads system_overrides for the ref, applies ACTIVE_SEGREGATION_WHERE eligibility to the include ids, and returns `{ memberIds, brokenRules: [] }` — the rules/gravity/exclusion-pruning/broken paths are stubbed to empty and filled by plan 02 (leave a clear seam, no architectural gap). In orrery-system-read.ts route `system.kind === "custom"` inside readOrrerySystemMembersCore: call the resolver for the id set, then run the existing member SELECT with `WHERE c.id IN (${placeholders})` bound to the ids (empty set → return `{status:"ready", members:[]}`); keep the builtin/category buildOrrerySystemWhere path exactly as-is. In orrery-controls-logic.ts extend buildSystemChoices to append custom Systems (from listCustomSystems, provided by the caller) as `{ id: 'custom:'+uid, ref:{kind:'custom',uid}, name }` after builtins/categories (full ordering/visibility/counts are plan 05).
  </action>
  <verify>
    <automated>npx vitest run src/logic/orrery-system-logic.test.ts src/db/app-settings-dao.test.ts src/db/systems-dao.test.ts src/logic/system-rule-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind: \"custom\"" src/logic/orrery-system-logic.ts` matches the union member
    - `grep -n "custom:" src/db/app-settings-dao.ts` shows the widened grammar
    - `grep -n "COLLATE NOCASE" src/db/systems-dao.ts` shows the pre-insert uniqueness check
    - resolver test proves a manual-only System returns exactly its include ids and never an excluded id
    - `npx vitest run src/logic/orrery-system-logic.test.ts src/db/app-settings-dao.test.ts src/db/systems-dao.test.ts src/logic/system-rule-resolver.test.ts` passes
    - `npm run check:colors` passes (no colour literals introduced)
  </acceptance_criteria>
  <done>A manual-only custom System created in the DAO round-trips its ref through the grammar, resolves its members through readOrrerySystemMembersCore, and appears in buildSystemChoices — the whole architecture is proven on one path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/caller → systems-dao | System name and rule/override values cross into SQL here |
| migration runner → SQLite | schema DDL applied forward-only, irreversibly, on unreachable devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | systems-dao INSERTs | high | mitigate | Every runtime value is `?`-bound (newUid, name, contactId, mode); mode is CHECK-constrained; family/kind tokens come from closed constants — no identifier is interpolated into SQL |
| T-30-02 | Denial of Service | migration 022 half-apply | high | mitigate | Runner applies each step in an atomic per-step transaction (DATA-01); migration is additive and starting-state-independent, so a v1→v22 jump is safe |
| T-30-03 | Tampering | duplicate/CI System names | medium | mitigate | `name COLLATE NOCASE` unique index + a pre-insert DAO uniqueness check throwing the §T collision |
| T-30-04 | Tampering | malformed orrery_last_system token | medium | mitigate | assertOrreryLastSystem grammar accepts only builtin/category/custom bounded tokens; widening is additive and rejects control chars |
</threat_model>

<verification>
- `npm test` green after the wave (the new suites plus the existing app-settings/backup suites still pass).
- Re-confirm on disk that migration head was 021 and TARGET_VERSION was 21 before the bump (record the observed values in the SUMMARY per D-03).
- `npm run check:colors` and Biome lint pass.
</verification>

<success_criteria>
Migration 022 exists, is registered, bumps TARGET_VERSION to 22, and passes its schema-invariant suite; the OrrerySystemRef `custom` kind and the widened `orrery_last_system` grammar round-trip; a manual-only custom System resolves end-to-end and is switcher-visible.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-01-SUMMARY.md` when done. Record the observed migration head + TARGET_VERSION at plan time, the confirmed table shape, and the seams left for plan 02 (rules/gravity/exclusion-pruning/broken-rule) and plan 05 (switcher ordering/visibility/counts).
</output>
