---
phase: 30-orrery-systems
plan: 10
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - src/components/orrery/orrery-switch-animation.ts
  - src/components/orrery/orrery-switch-animation.test.ts
  - src/screens/OrreryScreen.tsx
  - src/components/orrery/OrreryWorld.tsx
  - src/logic/orrery-frame.ts
  - src/services/orrery-scene.ts
  - src/stores/orrery-system-store.ts
autonomous: true
requirements: [ORRS-12, ORRS-13]
estimate:
  tokens: 72000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The last active System (including custom:<uid>) persists across relaunch via app_settings.orrery_last_system and is restored on launch (ORRS-12)"
    - "Switching sends the destination to canonical Phase 29 Home framing computed as deriveHomePose(destination.world, viewport) — the same canonical framing OrreryScreen already uses at :714/:755 — NOT a raw assignment of HOME_CAMERA (which is only the initial pose seed at :212 and would misfit differently-sized Systems); prior pan/zoom/tilt is not preserved (ORRS-12; Codex review cycle-2 MED #3)"
    - "A focus present in BOTH source and destination Systems is preserved through the switch; a focus absent from the destination is cleared (ORRS-12)"
    - "OrreryWorld ALREADY sheds departing bodies and captures entering ones on every scene-generation change via the existing orrery-frame pipeline — beginWorldTransition(from, world, generation) marks bodies in `from` but not `world` as leaving (opacity→0, radius*0.75, interactive:false) and bodies in `world` but not `from` as entering (start opacity:0, grow to 1), sampleWorldTransition interpolates, and OrreryWorld drives it at :245/:259/:278/:292 (verified on disk). A System switch already mints a new generation → this exact shed/capture. So ORRS-13 does NOT add a parallel entering/leaving id-set prop (that would re-derive what beginWorldTransition already tracks and risk double-animating opacity/radius). What ORRS-13 adds and OrreryWorld consumes is (a) a membership-delta switchIntensity SharedValue that MODULATES the existing transition's magnitude/easing and (b) a per-body spin term — both integrated into sampleWorldTransition/projectAnimatedFrame, never standing beside them (Claude review cycle-2 MED #2)"
    - "The switch animation is driven by the Skia render loop / Reanimated shared values, never per-frame React state, and pauses on useIsFocused===false and AppState background (ORRS-13)"
    - "The membership-delta source ids are STASHED at the loading edge and diffed at the ready edge: the store sets snapshot=null during loading (verified orrery-system-store.ts:93), so a naive subscribe((next,previous)) never sees old-ready and new-ready members in one transition — the wiring captures the previous ready snapshot's member ids when status flips to loading and diffs against the next ready snapshot (ORRS-13; both reviewers MEDIUM)"
    - "The existing UNCONDITIONAL focus clear is REPLACED, not supplemented: OrreryScreen.tsx:281-288 currently calls setFocusTargets([]) on any requested.id change; this plan replaces it with preservedFocus-aware logic and reconciles the :639-652 re-validation so a focus present in BOTH source and destination survives the switch (ORRS-12; Codex HIGH / Claude MEDIUM — verified on disk)"
    - "At startup, a persisted custom:<uid> last-active whose System no longer exists falls back to All Contacts via the plan-01 missing-custom read result (readOrrerySystemMembersCore → status:'missing-custom'), not an assumed pre-existing fallback (ORRS-12; ties Codex 30-04 HIGH)"
    - "OS Reduced Motion replaces spin+shedding/capture with a simple crossfade/reposition, sourced from the existing useReducedMotion()/useReducedMotionShared() hook (ADR-085), not a System-specific setting (ORRS-13) — satisfies D-09 (the reduced-motion switch path); D-09's 'hook does not exist yet' premise is disk-corrected — the Phase 23 hook already exists (ADR-085), so this consumes it rather than depending on it landing first"
    - "Membership-delta intensity is computed from the overlap between source and destination member sets (large overlap → subtle; large delta → dramatic), not from raw counts (ORRS-13)"
    - { statement: "The spin + shedding/capture animation renders smoothly on the physical Pixel and its intensity visibly scales with the membership delta", verification: backstop }
    - { statement: "Under OS Reduced Motion the switch is a simple crossfade/reposition with no rotational sweep, and manual camera control still works", verification: backstop }
    - { statement: "Switching lands at canonical Home framing and preserves a shared focus without disorientation (device-tune; simplify only if disorienting)", verification: backstop }
    - "The active broken-canvas affordance has a REAL data path (not just a device-verify hope): plan 01 attaches the resolver's brokenRules to the custom OrreryMembersResult, readOrrerySystemSnapshotCore spreads it into the snapshot, loadOrreryScene carries it on OrrerySceneSnapshot.systemSnapshot, and OrreryScreen reads state.snapshot.systemSnapshot.brokenRules to drive the non-blocking needs-attention affordance for the ACTIVE System (Codex review cycle-2 MED #4)"
    - { statement: "Canvas empty/broken states (§J / UI-SPEC E7): an empty-but-valid ACTIVE System shows the empty-state copy + a restrained comet (removed/replaced under Reduced Motion via useReducedMotionShared); a broken ACTIVE System shows a small non-blocking needs-attention affordance rendered from the snapshot's brokenRules signal (data path above). The visual treatment is carried as an explicit device-verify item so E7 is not silently dropped", verification: backstop }
  artifacts:
    - src/components/orrery/orrery-switch-animation.ts: `computeMembershipDelta`, `switchIntensity`, `preservedFocus`, the shared-value driver
    - src/components/orrery/OrreryWorld.tsx: a switch-intensity SharedValue + a per-body spin term that MODULATE the existing beginWorldTransition/sampleWorldTransition pipeline (NOT a parallel entering/leaving id-set prop — the existing pipeline already derives shed/capture)
    - OrreryScreen: switch-animation wiring, source-id stash-at-loading/diff-at-ready delta capture, focus-clear replacement, deriveHomePose Home framing, startup missing-custom fallback, active broken-canvas affordance from snapshot brokenRules

## Review Feedback Incorporated (cycle 2)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-2 MED #2 (reconcile with the existing orrery-frame pipeline):** verified `beginWorldTransition`/`sampleWorldTransition`/`projectAnimatedFrame` (`orrery-frame.ts:39-124`) already shed/capture on every generation change and are driven by OrreryWorld (`:245/:259/:278/:292`). Task 3 is rewritten to MODULATE that pipeline with a switchIntensity SharedValue + a per-body spin term, dropping the redundant entering/leaving id-set prop; `orrery-frame.ts` added to Task 3 read_first.
- **Cycle-2 MED #3 (Home framing):** Task 2 uses `deriveHomePose(destination.world, viewport)` (the canonical framing at OrreryScreen `:714/:755`), keeping `HOME_CAMERA` only as the initial seed (`:212`).
- **Cycle-2 MED #4 (active broken-canvas data path):** brokenRules now flow from plan-01's OrreryMembersResult through the snapshot to OrreryScreen, giving the active needs-attention affordance a real source (Task 2), not just a device-verify hope.
- **Cycle-2 LOW #9 (re-entrant missing-custom re-select):** Task 2 notes the generation/`same`-guard interaction so the silent All-Contacts re-select from inside select()'s catch cannot loop.

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Both reviewers HIGH (renderer scope):** OrreryWorld.tsx is IN SCOPE. Cycle-1 framed this as a new entering/leaving id-set transition prop; cycle-2 (MED #2 above) corrects that to MODULATING the EXISTING orrery-frame shed/capture pipeline with intensity + spin. Verified: OrreryWorld owns the body draw (rendered at OrreryScreen ~800) and drives beginWorldTransition/sampleWorldTransition (Task 3).
- **Codex HIGH / Claude MEDIUM (focus-clear conflict):** the unconditional setFocusTargets([]) at OrreryScreen:281-288 is REPLACED with preservedFocus-aware logic, reconciled with the :639-652 re-validation (Task 2).
- **Both reviewers MEDIUM (delta source ids):** source member ids are stashed at the loading edge (previous ready snapshot) and diffed at the ready edge, working around the store's snapshot=null-during-loading (verified :93) (Task 2).
- **Codex 30-04 HIGH tie-in (startup fallback):** a persisted stale custom:<uid> falls back to All Contacts via plan-01's missing-custom read result (Task 2).
- **Claude 30-05 LOW (E7 active canvas states):** the empty comet + active broken affordance are carried as explicit device-verify backstops (not silently dropped); brokenRules must reach the active snapshot.
  key_links:
    - "store status→loading edge: stash previous ready snapshot member ids → status→ready edge: diff vs next snapshot member ids → membership delta → switchIntensity SharedValue → MODULATES OrreryWorld's existing beginWorldTransition/sampleWorldTransition shed/capture + a per-body spin term (no separate entering/leaving prop)"
    - "preservedFocus(currentFocusId, sourceIds, destIds) replaces the :285 unconditional clear; reconciled with :641-648 re-validation"
    - "plan-01 OrreryMembersResult.brokenRules → readOrrerySystemSnapshotCore spread → OrrerySceneSnapshot.systemSnapshot → OrreryScreen active broken-canvas affordance"
    - "useReducedMotionShared() → reduced-motion branch; useIsFocused()/AppState → pause; startup parseSystemRef(custom:<uid>) → missing-custom read result → select(All Contacts)"
  prohibitions:
    - "Never drive the switch animation from per-frame React state (setState per frame) — Skia render loop / Reanimated shared values only (CLAUDE.md)"
    - "Never define a worklet helper below its caller in the same file — Hermes forward-ref crash vitest cannot catch (MEMORY)"
    - "Never hardcode a colour in a Skia draw — resolve through useTheme().colors (npm run check:colors)"
    - "Never persist camera state — camera is ephemeral (Phase 29 D-08-064); only the last-active System persists"
    - "Never make a perf claim from the desktop emulator — the orrery is a Skia render-loop feature; measure on the Pixel"
---

<objective>
Deliver the System-switch experience: persist and restore the last-active System, land every switch at canonical Home framing with shared-focus preservation, and animate the transition as a membership-delta-adaptive spin + shedding/capture on the Skia render loop — with the OS Reduced Motion path reduced to a simple crossfade/reposition.

Purpose: Switching is the payoff of Systems. The animation must be render-loop-driven (never per-frame React state), reduced-motion-aware through the existing hook, and honest about camera framing and focus. The pure membership-delta math is unit-testable; the render/feel is device-verified.

Output: A pure switch-transition module (delta → intensity, focus preservation) + its wiring into the OrreryScreen render loop, with the reduced-motion branch.

## Artifacts this plan produces
- `src/components/orrery/orrery-switch-animation.ts`: `computeMembershipDelta`, `switchIntensity`, `preservedFocus`, `captureSourceMemberIds` (stash-at-loading helper), the shared-value driver
- `src/components/orrery/OrreryWorld.tsx`: a transition contract prop (switch-intensity SharedValue + entering/leaving member-id sets) consumed by the per-body Skia draw for shed/capture
- OrreryScreen: switch-animation wiring, stash-at-loading/diff-at-ready delta capture, focus-clear REPLACEMENT (preservedFocus), Home framing, reduced-motion branch, startup missing-custom fallback
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure switch-transition math (delta → intensity, focus preservation)</name>
  <files>src/components/orrery/orrery-switch-animation.ts, src/components/orrery/orrery-switch-animation.test.ts</files>
  <read_first>
    - src/theme/use-reduced-motion.ts lines 106-135 (useReducedMotion()/useReducedMotionShared() — ADR-085; the SharedValue<boolean> the Skia loop reads and the React boolean twin)
    - src/screens/OrreryScreen.tsx (the existing Skia render loop, useSharedValue<CameraPose> pose, HOME_CAMERA, useReducedMotionShared usage, the store snapshot shape with member ids)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §V (Home framing; preserve focus present in both, else clear), §W (spin+shedding/capture; intensity adaptive to membership DELTA not raw counts; must not lock out interaction; render-loop driven; pause on unfocus/background), §X (Reduced Motion → crossfade/reposition)
    - MEMORY worklet-forward-ref-hazard note (define worklet helpers ABOVE callers)
  </read_first>
  <behavior>
    - computeMembershipDelta(sourceIds, destIds) returns { overlap, entering, leaving, total } counts over the two member sets.
    - switchIntensity(delta) maps a large overlap ratio to a subtle intensity and a large entering/leaving ratio to a dramatic one — bounded [0,1], monotonic in the delta fraction, independent of absolute counts (e.g. 100→100 identical sets → ~0; 100→2 tiny overlap → ~1).
    - preservedFocus(focusId, sourceIds, destIds) returns focusId if present in both, else null.
    - The module is pure (no React/Skia imports) so it runs in vitest; any worklet-facing helper is defined above its caller.
  </behavior>
  <action>
Create orrery-switch-animation.ts with pure computeMembershipDelta, switchIntensity (delta fraction → bounded intensity, count-independent), and preservedFocus. Keep the file free of React/Skia imports so the math is node-testable; if a helper will be called from a worklet in Task 2, define it above its caller (MEMORY hazard). Unit-test the intensity mapping at the identical-set, full-turnover, and partial-overlap boundaries plus the focus-preservation cases.
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/orrery-switch-animation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: identical source/dest sets → intensity ≈ 0; near-complete turnover → intensity ≈ 1; intensity for 10→10 (5 overlap) equals 1000→1000 (500 overlap) — count-independent
    - Test: preservedFocus returns the id only when present in both sets, else null
    - `grep -n "react\|skia" src/components/orrery/orrery-switch-animation.ts` returns nothing (pure module)
  </acceptance_criteria>
  <done>The membership-delta intensity and focus-preservation math is pure and unit-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Wire delta capture, focus-clear replacement, Home framing, reduced motion, and startup fallback into OrreryScreen</name>
  <files>src/screens/OrreryScreen.tsx, src/components/orrery/orrery-switch-animation.test.ts</files>
  <read_first>
    - src/screens/OrreryScreen.tsx — the FULL relevant regions: the createOrrerySystemStore adapter ~214-234; the store.subscribe on generation change ~630-637; the pose useSharedValue seeded with HOME_CAMERA (line 212); deriveHomePose(scene.world, viewport) at :714 and :755 — the CANONICAL Home framing to reuse on switch (NOT a raw HOME_CAMERA assignment); reducedMotion = useReducedMotionShared() (213); the UNCONDITIONAL focus clear at :285 (this is what must be REPLACED); the focus re-validation at :641-648; the launch restoration at 329-338 (parseSystemRef(pref.committed.lastSystem) → select); the active snapshot's systemSnapshot.brokenRules (plan-01 field) for the broken-canvas affordance
    - src/stores/orrery-system-store.ts (select 66-124 — snapshot is set to null during loading at :93; status transitions initial→loading→ready; the subscribe (next, previous) sees previous=ready at the loading edge and previous=loading at the ready edge; the catch at 113-123 already special-cases MissingOrreryCategoryError — add a parallel MissingOrreryCustomSystemError branch that SILENTLY re-selects All Contacts rather than surfacing a needs-attention state)
    - src/services/orrery-scene.ts (loadOrreryScene:52-59 throws MissingOrreryCategoryError on a missing-category snapshot — add the parallel `if (snapshot.status === "missing-custom") throw new MissingOrreryCustomSystemError(snapshot)`; the class is defined by plan 01 in orrery-system-read.ts)
    - src/components/orrery/orrery-switch-animation.ts (Task 1)
    - src/theme/use-reduced-motion.ts (useReducedMotionShared for the Skia branch)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §V/§W/§X and .planning/phases/30-orrery-systems/30-UI-SPEC.md §V/§W/§X (E8 row)
    - MEMORY: worklet-forward-ref-hazard; never per-frame React state; emu perf caveat
  </read_first>
  <behavior>
    - Delta capture: stash the SOURCE member ids from the previous READY snapshot at the loading edge (subscribe fires with previous.status==='ready' when select flips to loading and snapshot→null), then at the ready edge diff the stashed source ids against next.snapshot member ids to compute the membership delta. A naive read of previous.snapshot.members at the ready edge is null (store :93) and must NOT be used.
    - Focus clear replacement: REMOVE the unconditional setFocusTargets([]) at 281-288 and instead, on a requested-System change, preserve the focus via preservedFocus(currentFocusId, sourceIds, destIds) once destination membership resolves — clearing only when the focus is absent from the destination; reconcile with the :639-652 re-validation so focus is not double-handled (either always-clear or double-flag).
    - Home framing: set the destination camera via deriveHomePose(destination.world, viewport) — the canonical framing already used at OrreryScreen :714/:755 — NOT a raw HOME_CAMERA assignment (HOME_CAMERA stays only the initial pose seed at :212); no prior pan/zoom/tilt preserved.
    - Reduced motion: branch on useReducedMotionShared() — a simple crossfade/reposition to Home instead of the rotational sweep.
    - Startup fallback: loadOrreryScene throws MissingOrreryCustomSystemError on a missing-custom snapshot; the store's select catch handles it by SILENTLY re-selecting All Contacts (distinct from the missing-category needs-attention state). So a persisted stale custom:<uid> at launch lands on All Contacts, not a dead ref or an error screen. Because this re-select is RE-ENTRANT (it calls select(ALL_CONTACTS_SYSTEM) from inside select()'s own catch — unlike the terminal missing-category branch), the branch must respect the store's generation/`same`-guard: the re-select mints a fresh generation and its own status transitions, and the failed generation is abandoned, so the guard prevents any select→catch→select loop. Re-select All Contacts exactly once per stale-ref failure (Claude review cycle-2 LOW #9).
    - Active broken-canvas affordance: read the active snapshot's brokenRules (carried on OrrerySceneSnapshot.systemSnapshot via plan-01's OrreryMembersResult.brokenRules) and render a small non-blocking needs-attention affordance for the ACTIVE System; the visual is device-verified but the data path is concrete (cycle-2 MED #4).
    - Pause on useIsFocused()===false / AppState background; interaction resumes once destination geometry is stable (no hard input lock).
  </behavior>
  <action>
In OrreryScreen: (1) add source-id stash-at-loading/diff-at-ready via the store.subscribe — capture the previous ready snapshot's member ids when status flips to loading, and at the ready edge compute computeMembershipDelta(stashedSourceIds, nextMemberIds) and drive a Reanimated shared value for switchIntensity (never per-frame setState). (2) REPLACE the 281-288 unconditional setFocusTargets([]) with preservedFocus(currentFocusId, sourceIds, destIds)-aware logic applied when destination membership resolves, and reconcile with the 639-652 re-validation so a both-present focus survives and an absent one clears exactly once. (3) Set the destination camera via deriveHomePose(state.snapshot.world, viewport) (the canonical framing at :714/:755), NOT a raw HOME_CAMERA assignment; keep HOME_CAMERA only as the pose seed. (4) Branch on useReducedMotionShared() for the crossfade/reposition path. (5) Add the missing-custom fallback: in loadOrreryScene (orrery-scene.ts) throw MissingOrreryCustomSystemError on a missing-custom snapshot (parallel to the existing missing-category throw), and in the store's select catch (orrery-system-store.ts) add a MissingOrreryCustomSystemError branch that silently re-selects ALL_CONTACTS_SYSTEM — so the launch restoration at 329-338 lands on All Contacts when the persisted custom:<uid> no longer exists. Because this fallback re-enters select() from inside select()'s catch, rely on the store's existing generation/`same`-guard so the re-select runs exactly once and cannot loop (note it explicitly in a comment). (6) Render the active broken-canvas affordance from state.snapshot.systemSnapshot.brokenRules (plan-01 data path). Pause on useIsFocused()===false / AppState≠active; do not hard-lock input. All colours via theme tokens. Define any worklet helper above its caller. Add a test exercising the pure delta/focus module with the same source/dest id sets OrreryScreen stashes/diffs.
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/orrery-switch-animation.test.ts && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useReducedMotionShared\|deriveHomePose\|switchIntensity\|preservedFocus" src/screens/OrreryScreen.tsx` matches the wiring (Home framing via deriveHomePose, not a raw HOME_CAMERA assignment on switch)
    - `grep -n "MissingOrreryCustomSystemError\|missing-custom" src/services/orrery-scene.ts src/stores/orrery-system-store.ts` shows the throw + the silent All-Contacts fallback branch
    - The unconditional `setFocusTargets(\[\])` at the old 281-288 requested-id effect is gone/replaced (manual review: focus is now preservedFocus-driven, not always-cleared)
    - No per-frame React state drives the animation: `grep -n "setState\|useState" src/screens/OrreryScreen.tsx` shows no per-frame setter inside the render loop / animation path (intensity is a shared value)
    - `npm run check:colors` passes; `npm test` green
  </acceptance_criteria>
  <done>OrreryScreen stashes source ids at the loading edge, diffs at ready, preserves shared focus (replacing the unconditional clear), lands at Home, branches reduced motion, and falls back to All Contacts on a stale custom last-active.</done>
</task>

<task type="auto">
  <name>Task 3: Modulate the existing orrery-frame shed/capture with switch intensity + per-body spin</name>
  <files>src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx, src/logic/orrery-frame.ts</files>
  <read_first>
    - src/logic/orrery-frame.ts (the FULL module — the EXISTING world-transition pipeline this task integrates with: beginWorldTransition 39-73 already marks LEAVING bodies — in `from` not `world` → end opacity:0, radius*0.75, interactive:false — and ENTERING bodies — in `world` not `from` → start opacity:0, grow to 1; sampleWorldTransition 74-95 interpolates by a `fraction`; projectAnimatedFrame 96-124 projects; all are worklets. Do NOT re-derive entering/leaving — this module already does it per generation.)
    - src/components/orrery/OrreryWorld.tsx (the FULL component — it OWNS the per-body Skia draw and DRIVES orrery-frame at :245 (beginWorldTransition initial), :278/:317 (sampleWorldTransition), :292 (projectAnimatedFrame), :322 (beginWorldTransition on generation change); reads useReducedMotionShared() at :228; receives scene/camera/pose/viewport/colors/focusedIds/satellites via props at 191-226 but NO intensity/spin input today; verified on disk)
    - src/screens/OrreryScreen.tsx (where OrreryWorld is rendered ~800 with its current prop set; the Task-2 switchIntensity SharedValue to pass down)
    - src/components/orrery/orrery-switch-animation.ts (Task 1 — the intensity + delta helpers)
    - MEMORY: worklet-forward-ref-hazard (define worklet helpers ABOVE callers); never per-frame React state; CLAUDE.md Skia colour-token rule
  </read_first>
  <behavior>
    - OrreryWorld accepts ONE new render-loop input from the switch: a switch-intensity SharedValue (0..1). It does NOT accept an entering/leaving member-id-set prop — beginWorldTransition already computes shed (leaving) and capture (entering) from `from` vs `world` on the generation change a System switch produces.
    - switchIntensity MODULATES the existing transition: it scales the drama/easing of sampleWorldTransition's progress (e.g. how far shed bodies scatter / the transition's ease curve) and drives an added per-body SPIN term — read via useDerivedValue on the render loop (never per-frame React state). It must not double-animate opacity/radius already handled by beginWorldTransition/sampleWorldTransition — it augments, it does not duplicate.
    - Under Reduced Motion (useReducedMotionShared already read at OrreryWorld:228) the spin/intensity modulation is suppressed and the transition degrades to the plain crossfade/reposition (no rotational sweep).
    - When no switch is in flight (intensity ~0) the body draw is byte-for-byte the steady-state behaviour that exists today (the modulation is inert at 0).
  </behavior>
  <action>
Extend OrreryWorld with a single `switchIntensity` SharedValue input (from Task 2) and integrate it into the EXISTING orrery-frame path — do NOT add a parallel entering/leaving id-set prop or a second transition (beginWorldTransition already tracks shed/capture from `from` vs `world`; a parallel path would double-animate opacity/radius and fight the sampled frame — Claude review cycle-2 MED #2). Consume switchIntensity via useDerivedValue to (a) scale the magnitude/easing of the sampled transition (modulating sampleWorldTransition's fraction/curve or an amplitude applied to the shed scatter) and (b) drive an added per-body spin term in the draw. Retained bodies keep continuity via the existing keying. Suppress the spin/modulation under the already-read useReducedMotionShared (crossfade/reposition only). Keep the modulation inert when intensity is ~0 so the steady state is unchanged. In OrreryScreen, pass ONLY the Task-2 switchIntensity SharedValue into OrreryWorld at ~800. All colours (including these draws) via useTheme().colors. Define any worklet helper ABOVE its caller (Hermes forward-ref hazard).
  </action>
  <verify>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "beginWorldTransition\|sampleWorldTransition\|switchIntensity\|spin" src/components/orrery/OrreryWorld.tsx` shows the intensity/spin MODULATE the existing orrery-frame pipeline (not a new entering/leaving transition)
    - `grep -n "entering\|leaving" src/components/orrery/OrreryWorld.tsx` shows NO new entering/leaving id-set prop was introduced (shed/capture stays inside beginWorldTransition)
    - `grep -n "OrreryWorld" src/screens/OrreryScreen.tsx` and the render site (~800) show ONLY the switchIntensity SharedValue passed (no id-set prop)
    - `npm run check:colors` passes (no colour literals in the modulated draws)
    - Device-verify (end-of-phase, Pixel): per-body shedding/capture (already from orrery-frame) plays with a spin whose intensity scales with the membership delta; reduced-motion path shows no rotational sweep
  </acceptance_criteria>
  <done>OrreryWorld's existing orrery-frame shed/capture is modulated by the switchIntensity SharedValue + a per-body spin (no redundant entering/leaving prop), reduced-motion-aware — the ORRS-13 signature animation is buildable as scoped without a parallel transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (render-loop UI) | No new network/auth/storage surface; last-active persistence reuses the existing app_settings writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-21 | Denial of Service | render-loop jank / input lockout | medium | mitigate | Animation runs on the Skia loop / shared values (never per-frame React state), pauses on unfocus/background, and does not hard-lock input; perf verified on the Pixel (not the emulator) |
| T-30-22 | Tampering | worklet forward-reference crash | high | mitigate | Worklet helpers defined above callers (MEMORY hazard); the pure math is unit-tested |
| T-30-23 | Denial of Service | stale last-active on restore | low | accept | The System read tolerates a stale ref (fallback to All Contacts); no destructive action |
</threat_model>

<verification>
- `npm test` green; the pure switch-math suite covers intensity + focus preservation.
- `npm run check:colors` and Biome pass.
- Device-verify (end-of-phase, Pixel): spin+shedding/capture feel + delta-scaled intensity; reduced-motion crossfade; Home framing + shared-focus preservation. Emulator cannot assess Skia perf.
</verification>

<success_criteria>
ORRS-12/13 delivered: last-active persists, switches land at Home with shared-focus preservation, and the transition is a render-loop-driven, membership-delta-adaptive animation with a reduced-motion crossfade.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-10-SUMMARY.md` — record the intensity mapping, the focus-preservation rule, and the device-UAT items carried to end-of-phase.
</output>
