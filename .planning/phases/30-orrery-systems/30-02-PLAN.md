---
phase: 30-orrery-systems
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/logic/system-rule-resolver.ts
  - src/logic/system-rule-resolver.test.ts
  - src/db/orrery-system-read.ts
  - src/db/orrery-system-read.test.ts
autonomous: true
requirements: [ORRS-01, ORRS-02, ORRS-03, ORRS-04]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Custom-System rules compose the canonical dashboard predicates: OR within a rule family, AND across families over the eight axes (Category, Favorite, Status/Needs-Attention, Gravity, Social Battery, Contact Frequency, Not Contacted, Snoozed) (ORRS-01)"
    - "Gravity is resolved as a post-query filterByGravity TypeScript pass over the SQL candidate set, never as a SQL WHERE clause (ORRS-01)"
    - "The base scope is ACTIVE_SEGREGATION_WHERE unless a Not-Contacted rule is present, in which case the resolver widens to DASHBOARD_POPULATION_SCOPE_WHERE for that path so never-contacted bodies surface only in Systems that explicitly ask for them (E-02 preserved)"
    - "Membership = (rule candidates ∪ manual inclusions) ∖ manual exclusions (ORRS-02)"
    - "ResolvedMembership exposes candidateIds — the pre-override rule-candidate id set (the resolveCandidateIds output, before overrides are unioned/subtracted) — alongside memberIds, so Manage Members (30-07) and the builder's embedded grid (30-08) can classify each card as rule-match vs manual-add vs exclude-in-place; the member editor cannot render 'Excluded in place' without it (Codex review cycle-2 HIGH #1, ORRS-06)"
    - "A manual exclusion for a contact that is no longer in the rule candidate set is pruned at resolve time — dynamic buckets, not ledgers; no stale exclusion accumulates (ORRS-02)"
    - "A durable manual inclusion persists in membership until removed, subject to active-contact eligibility (ORRS-02)"
    - "A rule whose category:<uid> value no longer joins categories is flagged needs-attention at resolve time and is EXCLUDED from the WHERE while every other valid rule keeps resolving — never silently deleted or rewritten (ORRS-04)"
    - "Every closed-vocabulary axis has an exported sentinel/value constant that the mapper validates against — including the boolean/system axes Favorite, Not-Contacted, Snoozed, and scope:population, not only category/battery/frequency/needs-attention/gravity; a malformed value on ANY axis (including a boolean axis) becomes a BrokenRule and is omitted, never coerced or interpolated (Codex review cycle-2 MED #7)"
    - "The candidate SELECT carries an explicit deterministic ORDER BY compatible with Orrery ring ordering (COALESCE(c.ring_seq,1e9), c.created_at, c.id) so membership/count output is stable across reads (Codex review 30-02 MEDIUM)"
    - "Each BrokenRule carries a stable ruleUid (system_rules.uid) alongside {family,value,reason} so the builder/management can target a precise malformed row for repair, even among duplicate-looking rows (Codex review 30-02 MEDIUM)"
    - "A boolean-only rule set (favorite/snoozed/not-contacted only, no category/battery/frequency/needs-attention) never ANDs an empty buildFilterWhere.sql ('') into the candidate WHERE (Claude review 30-02 LOW)"
    - "A resolveMembershipFromDefinition(exec, { rules, overrides, now }) resolves a PROVISIONAL (unsaved) draft's membership from in-memory rules + override intent and returns { memberIds, candidateIds, brokenRules, prunableExclusionContactIds }, and resolveCustomSystemMembers is a thin wrapper that loads persisted rules/overrides and delegates to it — so the builder's live count AND its embedded member grid (which needs candidateIds) work before a System is saved (Codex review 30-08 HIGH; cycle-2 HIGH #1)"
    - "A base-scope rule (family 'scope', value 'population') widens the candidate base to DASHBOARD_POPULATION_SCOPE_WHERE (the full active + never-contacted population), so a custom System can represent All Contacts exactly when duplicated — honoring ORRS-03 'duplicable' without changing membership (Codex review 30-03 HIGH; system-generated, not a user-authorable accordion axis)"
    - "The injected gravity-inputs loader is FULLY WIRED, not just named: resolveMembershipFromDefinition/resolveCustomSystemMembers each take a loader parameter (e.g. gravityInputsFor(ids) or {now, gravityInputsFor}), and the REAL render caller — the custom branch of readOrrerySystemMembersCore in src/db/orrery-system-read.ts (routed there by 30-01) — CONSTRUCTS the loader from readOrreryImpactInputsCore (orrery-impact-read.ts:23) and passes it in. The logic module stays db-free (no static @/db import); the db read layer owns loader construction. A gravity-rule custom System resolves correctly on the real read path, not only in a unit test with a hand-built loader (Codex review 30-02 HIGH / cycle-3 actionable #1)"
    - "Built-in and Category Systems apply manual membership overrides at the read path, completing [DECIDED] ORRS-03 'override-able with visible indication' (dossier §153 built-ins, §173 Category-derived): a shared applyMembershipOverrides(candidateIds, includeIds, excludeIds, eligibility) computes (base ∪ eligible-includes ∖ excludes) and is used by BOTH the custom resolver AND the builtin/category branch of readOrrerySystemMembersCore. For builtin/category the BASE candidate set is buildOrrerySystemWhere's id set (the base predicate is unchanged — the override step is purely additive and a no-op when no override rows exist, so existing Phase-29 built-in behavior is preserved), then listSystemOverrides(systemRefId(ref)) is layered on. Includes respect active-contact eligibility; stale excludes (not in the base) are reported as prunable, never written by the read (cycle-3 HIGH #2)"
    - "The pre-override BASE member id set for any ref is obtainable for the builder's override-only mode (30-08): custom → resolveMembershipFromDefinition(...).candidateIds; builtin/category → the same buildOrrerySystemWhere base id set the read path uses — ONE base-truth so the builder grid and the live canvas cannot diverge (cycle-3 HIGH #2)"
    - "Stale-exclusion policy is stated, not implied: an exclusion whose contact left the base is SEMANTICALLY DISCARDED at read (never counted as a member) but is only PHYSICALLY pruned at the next definition save (30-03 pruneSystemExclusionsCore via 30-08); a nominal read never writes (dossier §E:110 'exclusions apply to contacts currently produced by the rules'). This persisted-but-ignored window is an accepted policy and is relevant to Phase-36 backup contents (a stale row may still be present in a backup taken before the next save) (cycle-3 actionable #11)"
  artifacts:
    - src/logic/system-rule-resolver.ts (rules + gravity + inc/exc pruning + broken-rule detection + draft-vs-persisted resolvers + scope base-mode; ResolvedMembership return now carries candidateIds; exported closed-value sentinel constants for every axis; the injected gravity-loader parameter; the shared applyMembershipOverrides helper reused by the read path)
    - src/logic/system-rule-resolver.test.ts
    - src/db/orrery-system-read.ts (custom-branch gravity-loader construction + injection; builtin/category override application via applyMembershipOverrides + listSystemOverrides; base-member-id exposure)
    - src/db/orrery-system-read.test.ts
  key_links:
    - "system_rules rows → mapRulesToFilters → buildFilterWhere + FAVOURITES/NOT_CONTACTED/SNOOZED fragments (SQL) → candidateIds (stable ORDER BY) → filterByGravity (TS, INJECTED loader from the db caller) → union/minus overrides (shared applyMembershipOverrides) → memberIds; candidateIds AND memberIds are both returned"
    - "ResolvedMembership { memberIds, candidateIds, brokenRules, prunableExclusionContactIds } → 30-07 deriveMemberRows(candidateIds, includeIds, excludeIds, rows) + 30-08 embedded grid seeding (candidateIds classifies rule-match vs added vs excluded)"
    - "resolveMembershipFromDefinition(draft) ← resolveCustomSystemMembers (persisted wrapper) — one shared membership engine for saved Systems and unsaved builder drafts"
    - "applyMembershipOverrides ← BOTH the custom resolver AND readOrrerySystemMembersCore's builtin/category branch (buildOrrerySystemWhere base ∪ includes ∖ excludes) — the [DECIDED] ORRS-03 read-path override application (cycle-3 HIGH #2)"
    - "orrery-system-read custom branch → constructs gravity loader from readOrreryImpactInputsCore → injects into the resolver (cycle-3 actionable #1)"
    - "category rule value (uid) → EXISTS categories join; missing join → BrokenRule descriptor (with ruleUid), not a WHERE term"
    - "scope:population rule → DASHBOARD_POPULATION_SCOPE_WHERE base (All Contacts duplication equivalence)"
  prohibitions:
    - "Never express Gravity as a SQL WHERE clause — it is a reversible post-query TypeScript pass (Pitfall 2)"
    - "Never silently delete or rewrite a rule referencing a deleted Category — flag it needs-attention and keep resolving the rest (D-07 decision-reversal risk)"
    - "Never interpolate a category/rule value into SQL — buildFilterWhere binds every value and uses closed family/token constants"
    - "Never reimplement a rule predicate that dashboard-query-logic already provides (ADR-093)"
    - "Never accumulate invisible stale exclusions for contacts no longer produced by the rules"
---

<objective>
Build the custom-System membership resolver: the read-side transform that turns stored rules + manual overrides into a resolved member id set by composing the canonical Dashboard predicates, the post-query Gravity pass, and the include/exclude override rows — with dynamic-bucket exclusion pruning and non-destructive broken-rule detection.

Purpose: This is the one genuinely new piece of logic in the phase. Getting OR/AND semantics, the Gravity TS pass, the E-02 never-contacted scope rule, exclusion pruning, and broken-rule handling right (and unit-proven) is what makes every downstream UI honest. The tracer (plan 01) left this as a manual-only stub; this plan fills it.

Output: A complete `resolveCustomSystemMembers` composing verified modules, with a broken-rule descriptor set, fully unit-tested on the node:sqlite substrate.

## Artifacts this plan produces
- `src/logic/system-rule-resolver.ts`: full `resolveCustomSystemMembers` (persisted wrapper) + `resolveMembershipFromDefinition` (draft engine) — both taking an injected gravity loader; `applyMembershipOverrides` (the shared union/subtract/eligibility/prune helper reused by the read path); `mapRulesToFilters`, `resolveCandidateIds`, `SystemRule` (incl. `uid`), `BrokenRule` (incl. `ruleUid`), `ResolvedMembership` (now `{ memberIds, candidateIds, brokenRules, prunableExclusionContactIds }`), the exported closed-value sentinel constants for every axis (category/battery/frequency/needs-attention/gravity AND the boolean/system axes Favorite/Not-Contacted/Snoozed/`scope:population`), and the `scope`/`population` base-mode handling
- `src/db/orrery-system-read.ts`: the custom-branch gravity-loader construction/injection (from `readOrreryImpactInputsCore`); the builtin/category override application (`applyMembershipOverrides` over the `buildOrrerySystemWhere` base id set + `listSystemOverrides`), completing [DECIDED] ORRS-03 at the read path; and the pre-override base-id exposure for the builder's override-only mode (30-08)

## Review Feedback Incorporated (cycle 3)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-3 actionable #1 (gravity-loader injection incomplete):** the resolver entry points now DECLARE a gravity-loader parameter, and this plan now edits src/db/orrery-system-read.ts so the custom-branch caller (routed there by 30-01) CONSTRUCTS the loader from readOrreryImpactInputsCore and injects it — closing the "named but not threaded/owned" gap. A gravity-rule custom System is tested on the real read path (Task 2/3).
- **Cycle-3 HIGH #2 (built-in/Category override read-path — completing [DECIDED] ORRS-03):** a shared applyMembershipOverrides helper is used by both the custom resolver and readOrrerySystemMembersCore's builtin/category branch, so built-in/Category Systems honor manual overrides at render (dossier §153/§173). The base predicate (buildOrrerySystemWhere) is unchanged; the override step is additive and a no-op with no override rows (Task 3). The authoring entry is 30-06 → 30-08 override-only mode.
- **Cycle-3 actionable #11 (stale-exclusion policy):** a must-have + a code comment now state the semantically-discarded-at-read / physically-pruned-at-next-save window explicitly (dossier §E:110), so the persisted-but-ignored window is an accepted, stated policy relevant to Phase-36 backup contents (Task 3).

## Review Feedback Incorporated (cycle 2)
This plan (verified against source on disk this cycle) now also addresses:
- **Cycle-2 HIGH #1 (resolver contract omits candidateIds):** `ResolvedMembership` and both resolvers now return `candidateIds` (the `resolveCandidateIds` output, pre-override) so 30-07/30-08 can render exclude-in-place (ORRS-06). Verified: `resolveMembershipFromDefinition` previously returned only `{memberIds,brokenRules,prunableExclusionContactIds}` while `deriveMemberRows` (30-07) already consumes `candidateIds` — the producer gap is closed here (Task 3).
- **Cycle-2 MED #7 (closed-token sentinels for boolean/system axes):** Task 1 now exports and validates sentinel constants for Favorite/Not-Contacted/Snoozed/`scope:population`, so a malformed boolean-axis value becomes a `BrokenRule` (previously only category/battery/frequency/needs-attention/gravity were validated).
- **Cycle-2 LOW #10 (impact-read citation + loader injection):** the read_first citation is corrected (`readOrreryImpactInputsCore` is DEFINED at `orrery-impact-read.ts:23`, INVOKED at `orrery-system-read.ts:109`), and Task 2 states the gravity impact-inputs loader is INJECTED into the resolver (matching `dashboard-gravity-filter.ts`'s loader convention) rather than statically imported from `src/db`, keeping `src/logic/system-rule-resolver.ts` free of a logic→db import.

## Review Feedback Incorporated (cycle 1)
This plan (verified against source on disk) now addresses:
- **Codex 30-08 HIGH (draft live-count):** `resolveMembershipFromDefinition(exec, {rules, overrides, now})` resolves an unsaved draft; `resolveCustomSystemMembers` wraps it after loading persisted rows (Task 3). The builder (plan 08) counts a draft through this, not the persisted resolver.
- **Codex 30-03 HIGH (All Contacts duplication equivalence):** a system-generated `scope`/`population` rule widens the base to `DASHBOARD_POPULATION_SCOPE_WHERE`, letting a duplicated All Contacts reproduce membership exactly (Task 2). Not a user-authorable axis; plan 03 emits it when duplicating All Contacts.
- **Codex 30-02 MEDIUM (candidate ordering):** explicit stable `ORDER BY` on the candidate SELECT (Task 2).
- **Codex 30-02 MEDIUM (broken-rule identity):** `BrokenRule.ruleUid` (Task 1).
- **Claude 30-02 LOW (empty buildFilterWhere):** boolean-only rule sets never AND an empty `sql` fragment (Task 2 + test).
- **Stale-exclusion pruning:** this plan still only SURFACES `prunableExclusionContactIds` (a read must never write); the write owner is plan 03's `pruneSystemExclusionsCore`, invoked on save (plan 08). See Codex 30-02 HIGH, resolved in plans 03/08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@docs/dossier/milestone-2/planning-notes/phase-08-planning-notes.md
@.planning/phases/30-orrery-systems/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map stored rules to the canonical Dashboard filter shape + broken-rule detection</name>
  <files>src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts</files>
  <read_first>
    - src/logic/system-rule-resolver.ts (the manual-only seam left by plan 01)
    - src/logic/dashboard-query-logic.ts lines 1-160 (DASHBOARD_FILTER_FAMILIES 14-20; buildFilterWhere 75-116 — category uses category_id integers; SOCIAL_BATTERY_VALUES 38; NEEDS_ATTENTION_VALUE 39; CONTACT_FREQUENCY_BANDS 29-34; ACTIVE_SEGREGATION_WHERE 149-151; DASHBOARD_POPULATION_SCOPE_WHERE 154-155; NOT_CONTACTED_WHERE 157; FAVOURITES_WHERE 158; SNOOZED_WHERE 159-160)
    - src/logic/orrery-system-logic.ts lines 73-115 (how buildOrrerySystemWhere composes the same fragments for builtin/category — the category EXISTS join at 79, the not-contacted population-scope path at 85-89)
    - src/db/orrery-system-read.ts lines 59-65, 166-171 (the existing missing-category domain result + MissingOrreryCategoryError — the broken-rule analog to mirror)
    - EVERY reader of the categories table (D-08 hand-grep, the graph cannot enumerate SQL writers): grep `FROM categories` / `categories cat` across src/ — confirm the category uid→id join shape before asserting the broken-rule invariant
  </read_first>
  <behavior>
    - mapRulesToFilters converts system_rules rows (family, value) into: (a) a DashboardFilters object for buildFilterWhere covering category (resolved uids→ids), social-battery, needs-attention, contact-frequency; (b) boolean flags for favorite/not-contacted/snoozed; (c) the gravity tier list.
    - A category rule value that resolves to a live category id is included; a value whose uid does not join categories produces a BrokenRule descriptor and is omitted from the filter (not interpolated, not deleted).
    - Unknown/invalid family or value tokens produce a BrokenRule descriptor and are omitted (closed-token discipline).
    - Every axis validates against an exported closed-value constant, INCLUDING the boolean/system axes: Favorite, Not-Contacted, and Snoozed each accept only their single sentinel value, and `scope`/`population` accepts only its sentinel; a boolean/system rule carrying any other value becomes a BrokenRule and is omitted (not just category/battery/frequency/needs-attention/gravity — Codex review cycle-2 MED #7).
    - Values within one family OR together (multiple category rules → category_id IN (…)); families AND together.
  </behavior>
  <action>
Add `mapRulesToFilters(exec, rules)` returning `{ filters: DashboardFilters, favorite: boolean, notContacted: boolean, populationScope: boolean, snoozed: boolean, gravityTiers: string[], broken: BrokenRule[] }`. Resolve each `category` rule value (a category uid) to its integer id via a single `SELECT id, uid FROM categories WHERE uid IN (?…)` and map surviving uids to the DashboardFilters `category` array (buildFilterWhere expects `/^\d+$/` id strings); a uid with no row becomes a `BrokenRule { ruleUid, family:'category', value:uid, reason:'missing-category' }`. Validate social-battery values against SOCIAL_BATTERY_VALUES, contact-frequency against CONTACT_FREQUENCY_BANDS keys, needs-attention against NEEDS_ATTENTION_VALUE, gravity against the GRAVITY_TIERS names — any out-of-set value becomes a BrokenRule (carrying its ruleUid) and is omitted. Recognize the system-generated `scope`/`population` rule as a `populationScope` flag (not a user-facing family). Define and export the closed-value sentinel constants for the boolean/system axes — e.g. `FAVORITE_RULE_VALUE`, `NOT_CONTACTED_RULE_VALUE`, `SNOOZED_RULE_VALUE` (each a single "on"-style sentinel) and `SCOPE_POPULATION_FAMILY`/`SCOPE_POPULATION_VALUE` — and validate a boolean/system rule's value against them: any other value on those axes becomes a `BrokenRule` and is omitted (Codex review cycle-2 MED #7). Define exported `SystemRule` (fields: `uid`, `family`, `value`) and `BrokenRule` (fields: `ruleUid`, `family`, `value`, `reason`) types — `ruleUid` is `system_rules.uid`, carried so the builder/management can target a precise malformed row even among duplicate-looking rows (Codex review 30-02 MEDIUM). Do not build any SQL string here — only shape the closed inputs buildFilterWhere/the fragments consume.
  </action>
  <verify>
    <automated>npx vitest run src/logic/system-rule-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: two category rules produce a single category_id IN(…) group via buildFilterWhere (OR within family)
    - Test: a category rule with a nonexistent uid yields a BrokenRule carrying its ruleUid and is omitted from filters (others still map)
    - Test: an out-of-set social-battery/gravity/frequency value yields a BrokenRule (with ruleUid) and is omitted
    - Test: a Favorite/Not-Contacted/Snoozed/scope rule carrying a value other than its exported sentinel yields a BrokenRule and is omitted (boolean/system-axis sentinel validation)
    - Test: two malformed rows with the same {family,value} yield two BrokenRule descriptors distinguished by ruleUid
    - `grep -n "gravity" src/logic/system-rule-resolver.ts` shows gravity handled as a tier LIST, never appended to any SQL string
  </acceptance_criteria>
  <done>mapRulesToFilters produces closed, injection-safe inputs and a non-destructive broken-rule set from stored rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose candidate SQL + Gravity pass + E-02 scope rule</name>
  <files>src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts</files>
  <read_first>
    - src/logic/dashboard-gravity-filter.ts (filterByGravity 19-38: candidateIds → survivors, preserves candidate order; GRAVITY_TIER_NAMES; note it takes an INJECTED loader — the convention to follow)
    - src/db/orrery-impact-read.ts line 23 (readOrreryImpactInputsCore is DEFINED here; it batches impact inputs for a set of ids — the gravity loader source), invoked from src/db/orrery-system-read.ts:109 (the read layer wires it; the LOGIC resolver must NOT statically import it — inject it instead, see action)
    - src/services/impact.ts (GRAVITY_TIERS names "thin"/"building"/"solid"/"deep"; computeContactGravity)
    - src/logic/dashboard-query-logic.ts lines 149-160 (ACTIVE_SEGREGATION_WHERE requires last_contact IS NOT NULL; DASHBOARD_POPULATION_SCOPE_WHERE does not)
    - docs/dossier/milestone-2/planning-notes/phase-08-planning-notes.md (E-02: never-contacted bodies only in Systems that explicitly include them; do not remove the default segregation wholesale)
  </read_first>
  <behavior>
    - With no Not-Contacted rule and no scope:population rule, base scope = ACTIVE_SEGREGATION_WHERE (excludes never-contacted) — a Favorite-only rule never returns never-contacted contacts.
    - With a Not-Contacted rule present, base scope widens to DASHBOARD_POPULATION_SCOPE_WHERE AND NOT_CONTACTED_WHERE for that family, so never-contacted contacts surface only there (E-02).
    - With a scope:population rule present (the All-Contacts base-mode), base scope = DASHBOARD_POPULATION_SCOPE_WHERE with NO not-contacted narrowing — the custom System spans the full active + never-contacted population, exactly matching the built-in All Contacts (Codex review 30-03 HIGH). This is the explicit "asks for never-contacted" path E-02 requires.
    - Favorite/Snoozed rules AND their fragments (FAVOURITES_WHERE / SNOOZED_WHERE) into the WHERE.
    - A boolean-only rule set (favorite/snoozed/not-contacted only, no category/battery/frequency/needs-attention) yields buildFilterWhere.sql === '' and must NOT AND an empty `()` group into the WHERE — the composition skips an empty fragment (Claude review 30-02 LOW).
    - Gravity rules do NOT enter the WHERE; the candidate ids are narrowed by filterByGravity using batched impact inputs.
    - The candidate SELECT is `SELECT c.id FROM contacts c WHERE <scope> [AND <families>]` with buildFilterWhere params bound, ending with an explicit deterministic `ORDER BY COALESCE(c.ring_seq,1e9), c.created_at, c.id` (matches readOrrerySystemMembersCore ordering so membership/count is stable and ring-order-compatible; Codex review 30-02 MEDIUM).
  </behavior>
  <action>
Add `resolveCandidateIds(exec, mapped, now)` that assembles the base scope per the rules above (populationScope OR notContacted → DASHBOARD_POPULATION_SCOPE_WHERE base, else ACTIVE_SEGREGATION_WHERE), conditionally ANDs in buildFilterWhere(mapped.filters).sql ONLY when it is non-empty (guard against the '' fragment), FAVOURITES_WHERE (if favorite), SNOOZED_WHERE (if snoozed), and NOT_CONTACTED_WHERE (if notContacted) — every runtime value ?-bound, mirroring buildOrrerySystemWhere. Append an explicit `ORDER BY COALESCE(c.ring_seq,1e9), c.created_at, c.id` to the candidate SELECT. Run the candidate `SELECT c.id`, then if gravityTiers is non-empty pass the candidate ids through filterByGravity with the supplied `now` and an INJECTED gravity-inputs loader. The loader is provided by the resolver's caller (the db read layer / builder) and is backed by `readOrreryImpactInputsCore` (defined at `orrery-impact-read.ts:23`, batch-load once, look up per id) — the logic module `system-rule-resolver.ts` receives the loader as a parameter rather than statically importing the `src/db` read, matching the `dashboard-gravity-filter.ts` loader convention and keeping the logic layer db-free (Codex/Claude review cycle-2 LOW #10; discretion on the exact parameter name). The read-layer CONSTRUCTION and injection of this loader (in `orrery-system-read.ts`, built from `readOrreryImpactInputsCore`) lands in Task 3 — this task only DECLARES the parameter so the signature is stable (cycle-3 actionable #1). Return the surviving id list in the stable candidate order (filterByGravity preserves input order).
  </action>
  <verify>
    <automated>npx vitest run src/logic/system-rule-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: a Favorite-only System excludes a never-contacted favorite (base scope = active) and does NOT AND an empty () group (boolean-only rule set)
    - Test: a Not-Contacted System includes never-contacted contacts (widened scope) and no active contact
    - Test: a scope:population System returns the same membership as the built-in All Contacts (active + never-contacted)
    - Test: a Gravity=deep rule narrows candidates via filterByGravity, and no gravity token appears in any SQL string
    - Test: multi-family (e.g. Category OR Category, AND Social-Battery) resolves as OR-within/AND-across
    - `grep -n "ORDER BY" src/logic/system-rule-resolver.ts` shows the deterministic ring-compatible candidate ordering
  </acceptance_criteria>
  <done>Candidate resolution composes the canonical fragments with the correct E-02 scope and applies Gravity as a post-query pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Overrides (shared applyMembershipOverrides) — draft + persisted resolvers; wire the read path (gravity-loader injection + built-in/Category override application)</name>
  <files>src/logic/system-rule-resolver.ts, src/logic/system-rule-resolver.test.ts, src/db/orrery-system-read.ts, src/db/orrery-system-read.test.ts</files>
  <read_first>
    - src/db/systems-dao.ts (listSystemOverrides(systemRef) read created by plan 01 — the include/exclude rows keyed by ref)
    - src/logic/orrery-system-logic.ts (systemRefId — to compute the ref token for any System; buildOrrerySystemWhere — the builtin/category BASE predicate whose id set overrides layer onto)
    - src/db/orrery-system-read.ts (readOrrerySystemMembersCore: the custom branch 30-01 added routes to the resolver; the builtin/category path runs buildOrrerySystemWhere then the member SELECT at :66-74; readOrreryImpactInputsCore is invoked at :109 — the gravity-inputs batch source to build the injected loader from)
    - src/db/orrery-impact-read.ts:23 (readOrreryImpactInputsCore — the loader source the db read layer constructs and injects into the resolver; the logic module must NOT import it)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §E (manual inclusion/exclusion semantics; dynamic buckets not ledgers; exclusion discarded when the contact stops matching — §E:110 the discard-at-read / prune-at-save policy) and §153/§173 (built-in and Category Systems may carry manual membership overrides — [DECIDED])
  </read_first>
  <behavior>
    - members = (candidateIds ∪ includeIds) ∖ excludeIds, subject to active-contact eligibility for includes (an Archived manual include is retained in the definition but not returned as a member — §F). This union/subtract/eligibility/prune math is factored into a single shared `applyMembershipOverrides` so the custom resolver AND the built-in/category read path use ONE implementation.
    - An exclusion whose contact_id is NOT in candidateIds is SEMANTICALLY DISCARDED at read (returned as prunable, never counted as a member) but is only PHYSICALLY pruned by an INTENTIONAL write path (plan 03 pruneSystemExclusionsCore, invoked on save in plan 08); the resolver/read itself NEVER writes (a nominal read must not mutate — Codex review 30-02 HIGH; the discard-at-read/prune-at-save window is an accepted stated policy, dossier §E:110, cycle-3 actionable #11). It never suppresses a manual include.
    - `resolveMembershipFromDefinition(exec, { rules, overrides, now }, gravityInputsFor)` is the pure membership ENGINE over an in-memory definition (draft or persisted): it maps rules, resolves candidates (using the INJECTED gravity loader for the gravity pass), applies overrides via applyMembershipOverrides, and returns `{ memberIds, candidateIds, brokenRules, prunableExclusionContactIds }`. It works for an UNSAVED builder draft that has no persisted rows yet (Codex review 30-08 HIGH; candidateIds added for cycle-2 HIGH #1). The gravity loader is a PARAMETER — the logic module never imports @/db.
    - `resolveCustomSystemMembers(exec, ref, now, gravityInputsFor)` is the thin PERSISTED wrapper: load listSystemRules(systemId) + listSystemOverrides(systemRefId(ref)) and delegate to resolveMembershipFromDefinition, threading the injected loader.
    - Read-path wiring (cycle-3 actionable #1 + HIGH #2): the custom branch of readOrrerySystemMembersCore CONSTRUCTS the gravity loader from readOrreryImpactInputsCore and passes it to resolveCustomSystemMembers (so a gravity-rule custom System resolves on the real render path). The builtin/category branch runs buildOrrerySystemWhere for the BASE id set (unchanged), loads listSystemOverrides(systemRefId(ref)), and applies the SAME applyMembershipOverrides — so built-in/Category Systems honor manual overrides ([DECIDED] ORRS-03). With no override rows the result is byte-identical to today (additive, no-op). The pre-override base id set is exposed so the builder's override-only mode (30-08) seeds its grid from the same base-truth.
    - A manual-only definition (no rules) returns exactly (eligible includes − excludes).
  </behavior>
  <action>
Define a shared `applyMembershipOverrides({ candidateIds, includeIds, excludeIds, eligibleIncludeIds })` returning `{ memberIds, prunableExclusionContactIds }` (candidates ∪ eligible-includes ∖ excludes; prunable = excludes not in candidates). Add `resolveMembershipFromDefinition(exec, { rules, overrides, now }, gravityInputsFor)`: call mapRulesToFilters(exec, rules) + resolveCandidateIds (using the INJECTED gravityInputsFor loader for the gravity pass) → the `candidateIds` set, split overrides into include/exclude id sets, resolve include eligibility via a bound id lookup (archived/tracking-disabled excluded from the returned members but not from the stored inclusion), and call applyMembershipOverrides. Return `{ memberIds, candidateIds, brokenRules, prunableExclusionContactIds }` — `candidateIds` is the pre-override rule-candidate set surfaced verbatim so 30-07/30-08 can classify each card (rule-match / added / excluded-in-place) (cycle-2 HIGH #1). Define `resolveCustomSystemMembers(exec, ref, now, gravityInputsFor)` as a wrapper that reads the persisted rules + overrides for `ref` and calls resolveMembershipFromDefinition, threading the loader. Preserve the manual-only fast path (empty rules → memberIds = eligible includes − excludes). Emphasize in a code comment that neither function writes, AND that a stale exclusion is discarded at read but only physically pruned at the next definition save (dossier §E:110 policy — cycle-3 actionable #11). THEN wire the read path in src/db/orrery-system-read.ts: (a) in the custom branch (added by 30-01), construct `gravityInputsFor` from readOrreryImpactInputsCore and pass it into resolveCustomSystemMembers — the logic module never imports @/db (cycle-3 actionable #1); (b) in the builtin/category branch, after the buildOrrerySystemWhere base member read, load listSystemOverrides(systemRefId(system)) and apply the SAME applyMembershipOverrides (with active-contact eligibility for includes) so built-in/Category Systems honor overrides ([DECIDED] ORRS-03; a no-op when there are no override rows) — expose the pre-override base id set for the builder's override-only mode (cycle-3 HIGH #2). Keep the base predicate (buildOrrerySystemWhere) itself unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/logic/system-rule-resolver.test.ts src/db/orrery-system-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: an include for a non-matching contact appears in members; an exclude for a matching contact does not
    - Test: an exclusion for a contact that stopped matching is reported in prunableExclusionContactIds and does not affect a re-added include
    - Test: an Archived manual-include is absent from memberIds but its override row is unaffected
    - Test: resolveMembershipFromDefinition over an in-memory draft (rules + overrides passed directly, nothing persisted) returns the same membership as resolveCustomSystemMembers would after persisting those rows
    - Test: candidateIds is the pre-override rule-candidate set — it contains a rule-match that was later excluded (so the grid can render it Excluded in place) and it excludes a manual-only add (which appears in memberIds but not candidateIds)
    - Test (read path, cycle-3 actionable #1): a gravity-rule custom System resolves correctly through readOrrerySystemMembersCore with the loader built from readOrreryImpactInputsCore — a deep-gravity member survives, a thin one is dropped
    - Test (read path, cycle-3 HIGH #2): a built-in (e.g. Favorites) OR category ref with a manual include of a non-matching contact returns that contact as a member, and with a manual exclude of a matching contact drops it — while a ref with NO override rows returns the same members as buildOrrerySystemWhere alone (additive/no-op preservation)
    - Code-line (comment-stripped) negative gate: `grep -vE '^\s*(//|\*|/\*)' src/logic/system-rule-resolver.ts | grep -cE "INSERT |UPDATE |DELETE FROM"` returns 0 — no write statement in resolver code (comments excluded so header prose does not self-invalidate)
    - `grep -n "readOrreryImpactInputsCore" src/db/orrery-system-read.ts` shows the loader constructed in the read layer (not imported by src/logic/system-rule-resolver.ts — `grep -c "@/db" src/logic/system-rule-resolver.ts` returns 0)
    - `npx vitest run src/logic/system-rule-resolver.test.ts src/db/orrery-system-read.test.ts` passes; `npm test` green
  </acceptance_criteria>
  <done>applyMembershipOverrides is shared by the custom resolver and the builtin/category read path; resolveMembershipFromDefinition/resolveCustomSystemMembers take an injected gravity loader wired from the db read layer; built-in/Category Systems honor overrides at read (no-op when none); nothing writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored rules/overrides → resolver → SQL | rule family/value tokens and category values become SQL predicates here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | rule → SQL composition | high | mitigate | Reuse buildFilterWhere/closed fragments only; every value ?-bound; family/value/tier tokens validated against closed constants before use; category uids resolved via a bound IN(…) join, never interpolated |
| T-30-06 | Tampering | broken/unknown rule value | medium | mitigate | Out-of-set or unresolved values become BrokenRule descriptors and are omitted from SQL, never coerced or interpolated |
| T-30-07 | Information disclosure | never-contacted leakage | medium | mitigate | E-02 scope rule keeps ACTIVE_SEGREGATION_WHERE as the default; never-contacted bodies surface only when a Not-Contacted rule explicitly widens the scope |
</threat_model>

<verification>
- `npm test` green; the resolver suite covers OR/AND, gravity pass, E-02 scope, inc/exc, pruning, and broken rules.
- `grep -n "gravity" src/logic/system-rule-resolver.ts` confirms gravity never enters a SQL string.
- Biome lint passes.
</verification>

<success_criteria>
resolveCustomSystemMembers composes the canonical predicates + Gravity TS pass + override rows correctly, prunes stale exclusions, and flags broken rules without ever rewriting them.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-02-SUMMARY.md` — record the resolved-membership return shape `{ memberIds, candidateIds, brokenRules, prunableExclusionContactIds }` (candidateIds is consumed by 30-07's deriveMemberRows and 30-08's embedded grid), the injected gravity-loader parameter shape + where the db read layer constructs it, the shared `applyMembershipOverrides` signature, the builtin/category read-path override application + how the pre-override base id set is exposed for 30-08's override-only mode, the exported per-axis sentinel constants, the stated stale-exclusion discard-at-read/prune-at-save policy, and the broken-rule descriptor shape that the switcher (plan 05), builder (plan 08), and management (plan 06) consume.
</output>
