---
phase: 30-orrery-systems
plan: 07
type: execute
wave: 3
depends_on: [30-02, 30-03]
files_modified:
  - src/components/orrery/ManageMembersGrid.tsx
  - src/components/orrery/manage-members-logic.ts
  - src/components/orrery/manage-members-logic.test.ts
  - src/db/systems-members-read.ts
  - src/db/systems-members-read.test.ts
autonomous: true
requirements: [ORRS-02, ORRS-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Manage Members is a searchable virtualized multi-select avatar grid built on the contact-picker-selection LOGIC modules (toggleSelection/matchesQuery/filterRows), NOT on ContactPicker.tsx's single-select modal (dossier §M; A3)"
    - "Rule-derived matching contacts enter preselected; deselecting a rule-derived member marks it Excluded in place (kept visible, de-emphasized) rather than removing the card (ORRS-06)"
    - "Add People manually includes otherwise non-matching eligible ACTIVE contacts (never Archived/Unbound), tagged Added and legible on reopen (ORRS-02/06/§F)"
    - "Counts read total members · {added} added · {excluded} excluded (ORRS-06)"
    - "A manually included contact that is Archived is shown as unavailable/Archived and retained in the definition, not silently dropped (§F)"
    - "The grid virtualizes so hundreds of member cards are inspectable without eager mount; missing photo falls back to initials (§Z)"
    - { statement: "Large-membership grid rendering stays responsive without eager-mounting every card", verification: backstop }
    - { statement: "Cards, tags, and counts reflow at the largest supported text size; 44x44 minimum touch targets hold", verification: backstop }
  artifacts:
    - src/components/orrery/ManageMembersGrid.tsx
    - src/components/orrery/manage-members-logic.ts
    - src/db/systems-members-read.ts
  key_links:
    - "resolver rule candidates (plan 02) → preselected rows; toggle → include/exclude override intent → systems-dao (persisted by the builder, plan 08)"
    - "systems-members-read (active-contact rows) → ManageMembersGrid rows; contact-picker-selection logic → selection/search"
  prohibitions:
    - "Never build a second contact-selection system — reuse contact-picker-selection/-source logic (dossier §M)"
    - "Never include Archived or Unbound contacts in Add People results (§F)"
    - "Never remove a deselected rule-match card — mark it Excluded in place (ORRS-06)"
    - "Never eager-mount the full member list — virtualize (§Z)"
    - "Never hardcode a colour — Excluded uses textSecondary on surface at reduced opacity via theme tokens"
---

<objective>
Build the Manage Members surface: a searchable, virtualized, multi-select avatar grid over the contact-picker selection LOGIC, where rule matches enter preselected, deselection expresses an in-place Exclusion, Add People adds eligible active contacts as manual inclusions, and counts report total/added/excluded.

Purpose: This is the direct-manipulation half of membership authoring. The dossier is explicit: reuse the existing selection foundation (the logic modules), not the single-select ContactPicker modal, and never invent a second selection system. Building the grid as a self-contained component now lets plan 08 embed it as a builder page.

Output: ManageMembersGrid + its pure selection/override logic + an active-contact read source, unit-tested for the exclude-in-place, added, counts, and eligibility semantics.

## Artifacts this plan produces
- `src/components/orrery/ManageMembersGrid.tsx`
- `src/components/orrery/manage-members-logic.ts`: `deriveMemberRows`, `applyDeselect`, `applyAdd`, `overrideCounts`, `MemberCardState`
- `src/db/systems-members-read.ts`: active-contact row source for Add People + card rendering
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orrery-systems/30-RESEARCH.md
@.planning/phases/30-orrery-systems/30-PATTERNS.md
@.planning/phases/30-orrery-systems/30-UI-SPEC.md
@docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md
@.planning/phases/30-orrery-systems/30-02-SUMMARY.md
@.planning/phases/30-orrery-systems/30-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure member/override logic (preselect, exclude-in-place, add, counts)</name>
  <files>src/components/orrery/manage-members-logic.ts, src/components/orrery/manage-members-logic.test.ts</files>
  <read_first>
    - src/logic/contact-picker-selection.ts (toggleSelection/selectionCount/matchesQuery/filterRows — the Set-based selection + search to reuse verbatim)
    - src/logic/contact-picker-source.ts (ContactPickerRow shape: lookupKey/displayName/searchMethods/photoThumbUri; toPickerRows + "Unnamed contact" fallback)
    - src/logic/system-rule-resolver.ts (resolveCustomSystemMembers → the rule-candidate id set that seeds preselection)
    - docs/dossier/milestone-2/phase-09-orrery-systems-dossier.md §M, §E, §F (preselect rule matches; deselect = explicit exclusion kept visible; Add People = manual inclusion of eligible active; counts total·added·excluded; dynamic buckets not ledgers)
    - .planning/phases/30-orrery-systems/30-UI-SPEC.md Copywriting Contract ("Excluded", "Added", "{count} members · {added} added · {excluded} excluded", "{name} — Archived (unavailable)")
  </read_first>
  <behavior>
    - deriveMemberRows(candidateIds, includeIds, excludeIds, contactRows) yields rows each with a MemberCardState of: 'member' (matches, selected), 'excluded' (matches but deselected), 'added' (manual include, non-matching), or 'archived-added' (manual include, ineligible).
    - A rule match starts selected; toggling it off produces state 'excluded' and keeps the card present.
    - applyAdd adds an eligible active contact as 'added'; applyDeselect on a rule match records an exclusion; applyDeselect on an 'added' contact removes the inclusion.
    - overrideCounts returns { total, added, excluded } matching the copy contract.
    - An excluded contact that is not in candidateIds is not surfaced (dynamic bucket) — consistent with the resolver's pruning.
  </behavior>
  <action>
Create manage-members-logic.ts consuming ContactPickerRow + the contact-picker-selection helpers. Implement deriveMemberRows/applyDeselect/applyAdd/overrideCounts and a MemberCardState union. Keep it pure and framework-free so it runs under the node/vitest harness. Reflect §E dynamic-bucket semantics (an exclusion only means something while the contact matches). Do not touch SQL here.
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/manage-members-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: a rule match toggled off becomes 'excluded' and stays in the row list
    - Test: an added non-matching active contact becomes 'added'; counts read total·added·excluded correctly
    - Test: a manual include that is Archived resolves to 'archived-added' and is retained
    - Test: an exclusion for a contact absent from candidates is not surfaced (dynamic bucket)
  </acceptance_criteria>
  <done>The pure member/override logic implements preselect, exclude-in-place, add, and counts with dynamic-bucket semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Active-contact read source for the grid</name>
  <files>src/db/systems-members-read.ts, src/db/systems-members-read.test.ts</files>
  <read_first>
    - src/db/contact-read.ts and src/db/orrery-system-read.ts (the contacts SELECT idioms: name/photo/uid; the eligibility columns archived_at/tracking_enabled/last_contact)
    - src/logic/dashboard-query-logic.ts (ACTIVE_SEGREGATION_WHERE / DASHBOARD_POPULATION_SCOPE_WHERE — Add People "searches your active contacts", §F)
    - EVERY reader of the contacts table used for member rows (D-08 hand-grep): confirm the active-vs-archived-vs-unbound columns before asserting the eligibility invariant
    - src/logic/contact-picker-source.ts (ContactPickerRow shape to produce)
  </read_first>
  <behavior>
    - listActiveMemberRows returns ContactPickerRow-shaped rows for eligible ACTIVE contacts (archived_at IS NULL, tracking_enabled=1), lookupKey = the contact id as a string (Orbit contact, not a native lookupKey), searchMethods including the name for the picker search.
    - A separate read returns the display rows for a given set of contact ids (including an Archived manual-include so the editor can render it as unavailable).
  </behavior>
  <action>
Create systems-members-read.ts with `listActiveMemberRows(exec)` (eligible active contacts as ContactPickerRow rows with id-string lookupKey + name/photo) and `readMemberRowsByIds(exec, ids)` (rows for arbitrary ids including Archived, flagged, so an Archived manual-include renders as "{name} — Archived (unavailable)"). All values ?-bound; reuse ACTIVE_SEGREGATION_WHERE / population scope constants — never a hand-rolled predicate.
  </action>
  <verify>
    <automated>npx vitest run src/db/systems-members-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: listActiveMemberRows excludes archived and tracking-disabled contacts
    - Test: readMemberRowsByIds returns an Archived contact flagged unavailable (for retained manual includes)
    - `grep -n "ACTIVE_SEGREGATION_WHERE\|DASHBOARD_POPULATION_SCOPE_WHERE" src/db/systems-members-read.ts` matches (shared predicate reuse)
  </acceptance_criteria>
  <done>The grid has an active-contact source and an id-set read that honors §F eligibility.</done>
</task>

<task type="auto">
  <name>Task 3: The virtualized multi-select grid component</name>
  <files>src/components/orrery/ManageMembersGrid.tsx, src/components/orrery/manage-members-logic.test.ts</files>
  <read_first>
    - src/components/orrery/manage-members-logic.ts (Task 1) and src/db/systems-members-read.ts (Task 2)
    - src/logic/contact-picker-selection.ts (filterRows/matchesQuery for the search box)
    - src/components/ui (AppText, Avatar, GlassSurface, Button) and src/components/icons (Icon) + icon-registry (system-overrides for the add/exclude affordances if used)
    - .planning/phases/30-orrery-systems/30-UI-SPEC.md §M row (avatar + name + selection state; Excluded = textSecondary on surface reduced opacity; Added tag; counts; 44-min targets; virtualization) and the Color/Spacing/Typography tables
    - a FlatList/virtualization usage in the repo (e.g. an existing grid screen) for the virtualization idiom + numColumns
  </read_first>
  <action>
Build ManageMembersGrid as a controlled component: props = the resolved candidate ids + current include/exclude override sets + an onChange emitting the updated override intent (the builder persists via systems-dao). Render a virtualized (FlatList numColumns) avatar grid using deriveMemberRows; each card shows Avatar (initials fallback), name, and selection state; a deselected rule-match renders greyed with an "Excluded" tag; a manual add renders with an "Added" tag; an Archived include renders "{name} — Archived (unavailable)". A search box filters via filterRows. An "Add People" affordance opens the active-contact source (scope note "Searches your active contacts."). A header shows "{count} members · {added} added · {excluded} excluded". All colours via theme tokens (Excluded = textSecondary on surface at reduced opacity), 44-min touch targets, accessible multi-select semantics (accessibilityState checked). Add a component test (or extend the logic test with a render smoke test) asserting Excluded/Added tags render and counts match.
  </action>
  <verify>
    <automated>npx vitest run src/components/orrery/manage-members-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The grid uses a virtualized list (numColumns) — `grep -n "FlatList\|numColumns" src/components/orrery/ManageMembersGrid.tsx` matches
    - Deselecting a rule match keeps the card and shows the Excluded tag; Add People shows the "Searches your active contacts." scope note
    - `npm run check:colors` passes; no colour literals; 44-min touch targets present
  </acceptance_criteria>
  <done>The searchable virtualized multi-select grid renders preselected matches, exclude-in-place, added, counts, and Archived-include handling — ready for the builder to embed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid search/add input → systems-members-read | contact search term crosses into SQL here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-18 | Tampering | member read query | medium | mitigate | Reuse shared ACTIVE_SEGREGATION_WHERE constants; search is a client-side filterRows pass over already-bound reads; ids ?-bound |
| T-30-19 | Information disclosure | Archived/Unbound leaking into Add People | medium | mitigate | listActiveMemberRows filters to eligible active contacts only (§F) |
| T-30-20 | Denial of Service | eager-mounting a huge member set | medium | mitigate | Virtualized FlatList; no full-list mount (§Z) — device-verified at scale |
</threat_model>

<verification>
- `npm test` green; logic + read suites cover preselect/exclude/add/counts/eligibility.
- `npm run check:colors` and Biome pass.
- Device-verify (end-of-phase): large-membership grid responsiveness + largest-text card layout.
</verification>

<success_criteria>
Manage Members delivers ORRS-06 (searchable virtualized grid, preselected matches, exclude-in-place, counts) and the ORRS-02/§F inclusion semantics on the reused selection logic.
</success_criteria>

<output>
Create `.planning/phases/30-orrery-systems/30-07-SUMMARY.md` — record the ManageMembersGrid props contract (override intent onChange shape) the builder (plan 08) wires to systems-dao.
</output>
