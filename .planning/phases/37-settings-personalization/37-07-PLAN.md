---
phase: 37-settings-personalization
plan: 07
type: execute
wave: 7
depends_on: ["37-06"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/RestoreResultScreen.tsx
  - src/screens/BackupScreen.tsx
  - src/screens/backup-dualhome-logic.ts
  - src/screens/backup-dualhome-logic.test.ts
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Data & Backup (D-08 / §I) opens the SAME canonical Backup screen tree as the Backup tab — the four existing screens (`BackupScreen`/`BackupSettingsScreen`/`RestorePreviewScreen`/`RestoreResultScreen`) are registered in `SettingsStackParamList` + `SettingsStack.tsx` and reused unchanged (one canonical tree, two entry points), not a second copy. The Backup bottom tab stays (removal is DEFERRED §R — this plan only adds the entry point)."
    - "After a Settings → Data & Backup → Restore flow, the post-restore return is origin-aware: the Backup-TAB restore flow still resets to Backup (UNCHANGED shipped behaviour), while the Settings-entry flow returns to the Settings hub instead of stranding the user on a lone Backup route inside the Settings stack (RESEARCH Pitfall 3 hazard 1 — `RestoreResultScreen.tsx:18` hard `navigation.reset` to \"Backup\")."
    - "The native shared-backup singleton is consumed by the BACKUP-TAB copy only: the Settings-mounted `BackupScreen` does NOT drain `consumeSharedBackup` on focus (RESEARCH Pitfall 3 hazard 2 — the share-intent gate routes shared backups to `BackupTab › Backup` at linking.ts:67; a Settings copy that also consumed on focus would double-drain)."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06); no new backup functionality is invented merely because ownership is moving (§I)."
  artifacts:
    - src/screens/backup-dualhome-logic.ts
  key_links:
    - "Hub `Data & Backup` row → navigation.navigate('Backup') within the Settings stack → the same BackupScreen tree the tab hosts."
    - "RestoreResultScreen reads its host (tab vs settings) → restoreReturnRouteName → reset target ('Backup' | 'Settings'); BackupScreen focus effect gates consumeSharedBackup on shouldConsumeSharedBackup(host)."
---

<objective>
Give **Data & Backup** a second, canonical entry point from Settings (**D-08 / §I**) by registering the existing four Backup screens in the Settings stack — one tree, two entry points — and handle the two dual-home hazards so the Backup TAB flow is preserved unchanged while the Settings flow behaves correctly: origin-aware post-restore return, and tab-scoped consumption of the native shared-backup singleton.

Implements **D-08** (Backup dual-home; tab removal deferred). Honors **§I** (one canonical tree, no new backup functionality) and the deferrals in **§R**. Cites **D-01**.

Purpose: prepare Settings as the long-term Backup home without breaking the shipped Backup-tab restore/share flows.
Output: the four Backup route registrations + a hub Data & Backup row, plus `backup-dualhome-logic.ts` driving origin-aware return and tab-scoped consume, with a dual-home test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the four Backup screens in the Settings stack (D-08) + hub Data & Backup row</name>
  <files>src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts</files>
  <read_first>
    - src/navigation/tabs/BackupStack.tsx (the source of truth — the four imports + `<Stack.Screen>` registrations to copy VERBATIM into SettingsStack; the screens self-fetch and take only `navigation`/`route`, so they mount unchanged in a second stack)
    - src/navigation/types.ts:216-227 (BackupStackParamList — the exact param shapes for `Backup`/`BackupSettings`/`RestorePreview`/`RestoreResult` to add to SettingsStackParamList; RootStackParamList is a TYPE intersection so each hosting stack must list the routes it reaches — types.ts:314-317, and the file already does this for Profile-hosted routes at 245-299)
    - src/screens/backup-restore-logic.ts:5-52 (the `RestorePreviewRoute = { token; preview }` param is serializable; the heavy manifest lives in a module-singleton `Map` keyed by token — dual-home safe, one shared cache)
    - src/screens/settings-hub-model.ts (insert the `Data & Backup` row at §A index 5, route `Backup`)
  </read_first>
  <action>
Register the four existing Backup routes in the Settings stack, mirroring `BackupStack.tsx:1-18`: add `Backup`, `BackupSettings`, `RestorePreview` (`RestorePreviewRoute`), and `RestoreResult` (its existing param shape) to `SettingsStackParamList` in `src/navigation/types.ts`, and add the four `<Stack.Screen>` registrations importing the same `BackupScreen`/`BackupSettingsScreen`/`RestorePreviewScreen`/`RestoreResultScreen` into `src/navigation/tabs/SettingsStack.tsx`. Reuse the screens unchanged — do NOT create a second Backup tree (§I). Insert the `Data & Backup` hub row (route `Backup`, §A index 5) into `settings-hub-model.ts`. Do NOT remove the Backup bottom tab (§R deferred). Do NOT invent new backup functionality (§I).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsStackParamList` contains `Backup`, `BackupSettings`, `RestorePreview`, `RestoreResult` with the same param shapes as `BackupStackParamList`; `SettingsStack.tsx` registers all four screens.
    - source: `settings-hub-model.ts` has the `Data & Backup` row at §A index 5 targeting `Backup` (hub-model test green); the Backup tab registration in `BackupStack.tsx` is unchanged (tab stays, §R).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): Settings → Data & Backup opens the same Backup screen tree the tab opens.
  </acceptance_criteria>
  <done>Data & Backup reachable from Settings via the one canonical tree (D-08); tab preserved; hub row in place; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-home hazards — origin-aware post-restore return + tab-scoped shared-backup consume</name>
  <files>src/screens/RestoreResultScreen.tsx, src/screens/BackupScreen.tsx, src/screens/backup-dualhome-logic.ts, src/screens/backup-dualhome-logic.test.ts</files>
  <read_first>
    - src/screens/RestoreResultScreen.tsx (READ IN FULL: line 18 hard-resets `navigation.reset({ index: 0, routes: [{ name: "Backup" }] })` — this strands the user inside the Settings stack; it is typed `RootStackScreenProps<"RestoreResult">`)
    - src/screens/BackupScreen.tsx (READ IN FULL: the `useFocusEffect` at ~259-273 calling `consumeSharedBackup()` — drains the native singleton on focus in whichever stack gains focus)
    - src/navigation/linking.ts:56-74 (the share-intent gate routes a shared backup to `navigationRef.current?.navigate("BackupTab", { screen: "Backup" })` — the TAB copy is the intended consumer)
    - src/navigation/tabs/BackupStack.tsx (BOTH screens are ALSO consumed by the tab — per CLAUDE.md read every consumer: the tab flow MUST stay unchanged)
    - src/screens/backup-restore-logic.ts (the restore flow that navigates BackupScreen → RestorePreview → RestoreResult; how the host tab can be determined via navigation state / getParent)
  </read_first>
  <behavior>
    - restoreReturnRouteName: given the host tab identity, returns the reset target — `"Backup"` when hosted under the Backup tab (UNCHANGED shipped behaviour), `"Settings"` (the hub) when hosted under the Settings tab. Test both hosts.
    - shouldConsumeSharedBackup: returns `true` only when the host is the Backup tab; `false` for the Settings-mounted copy (so the singleton is drained once, by the tab). Test both hosts.
    - Neither helper touches the DB or the singleton — pure functions over the host identity (unit-testable without a device).
  </behavior>
  <action>
Create `src/screens/backup-dualhome-logic.ts` with two pure helpers: `restoreReturnRouteName(host)` and `shouldConsumeSharedBackup(host)`, where `host` is the resolved hosting-tab identity ("backup-tab" | "settings"). Write `backup-dualhome-logic.test.ts` first (RED) asserting the behavior block, then implement (GREEN).

In `RestoreResultScreen.tsx`, replace the hard `navigation.reset({ routes: [{ name: "Backup" }] })` with an origin-aware reset that uses `restoreReturnRouteName(host)` — resolving `host` from navigation state (e.g. the parent tab route name via `getParent`/`getState`, reading the code first). The Backup-tab flow MUST still reset to `Backup` (no regression); the Settings flow resets to `Settings` (the hub).

In `BackupScreen.tsx`, gate the `useFocusEffect` `consumeSharedBackup()` call on `shouldConsumeSharedBackup(host)` so only the Backup-tab copy drains the native singleton; the Settings-mounted copy never consumes it. Keep the tab copy's behaviour identical to today (linking.ts still routes shared backups to `BackupTab › Backup`).

This enforces the existing share-intent contract (does not reverse it); it only prevents the new Settings entry point from double-draining. Reversibility: reversible (pure code under git).
  </action>
  <verify>
    <automated>npx vitest run src/screens/backup-dualhome-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: `backup-dualhome-logic.test.ts` asserts `restoreReturnRouteName("backup-tab") === "Backup"`, `restoreReturnRouteName("settings") === "Settings"`, `shouldConsumeSharedBackup("backup-tab") === true`, `shouldConsumeSharedBackup("settings") === false`.
    - source: `RestoreResultScreen.tsx` resets via `restoreReturnRouteName(host)` (Backup-tab path unchanged); `BackupScreen.tsx` gates `consumeSharedBackup` on `shouldConsumeSharedBackup(host)`.
    - `npx vitest run src/screens/backup-dualhome-logic.test.ts` green; `npx tsc --noEmit` passes.
    - behavior (device UAT): Settings → Data & Backup → Restore returns to the Settings hub; the Backup tab restore still returns to Backup; a shared backup opens once (no double-drain) and lands on the Backup tab copy.
  </acceptance_criteria>
  <done>Both dual-home hazards handled with tested pure helpers, tab flow preserved; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native shared-backup singleton (`consumeSharedBackup`) → restore pipeline | A single native resource drained on focus; dual-mounting the consumer risks a double-drain / wrong-copy consume. |
| restored backup document → local SQLite (via the existing restore pipeline) | Unchanged — the restore logic is reused, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering / DoS | `consumeSharedBackup` native singleton double-drained across two mounted `BackupScreen` copies | medium | mitigate | Tab-scope the consume via `shouldConsumeSharedBackup(host)` — only the Backup-tab copy drains it; unit-tested; the share-intent gate (linking.ts:67) already targets the tab. |
| T-37-07 | Denial of navigation (stranded user) | `RestoreResultScreen` hard-reset to "Backup" inside the Settings stack | low | mitigate | Origin-aware `restoreReturnRouteName(host)`; the Settings flow returns to the hub, the tab flow is unchanged; unit-tested. |
| T-37-01 | Tampering | restore applying to local SQLite | low | accept | Restore pipeline reused unchanged; no new write path introduced. |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress (backup export/restore is on-device / SAF, unchanged).
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/backup-dualhome-logic.ts` — `restoreReturnRouteName` + `shouldConsumeSharedBackup` pure helpers + `backup-dualhome-logic.test.ts`.
- New `SettingsStackParamList` route names (re-registrations of the existing Backup screens): `Backup`, `BackupSettings`, `RestorePreview`, `RestoreResult`.
- Modified (behaviour-preserving) shared screens: `RestoreResultScreen.tsx` (origin-aware reset), `BackupScreen.tsx` (tab-scoped consume).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/backup-dualhome-logic.test.ts src/screens/settings-hub-model.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06); no new backup functionality (§I).
- Device UAT (per memory saf-grant-reconnect-cycle + verify-ui-on-pixel-yourself): reach Backup from BOTH entry points; confirm one canonical tree, origin-aware return, and no double-drain of a shared backup.
</verification>

<success_criteria>
Data & Backup dual-homed into Settings on one canonical tree (D-08), both dual-home hazards handled with tested helpers, the shipped Backup-tab flow preserved, tab removal deferred (§R) — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-07-SUMMARY.md` when done.
</output>
