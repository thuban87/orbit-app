---
phase: 37-settings-personalization
plan: 07
type: execute
wave: 7
depends_on: ["37-06"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/tabs/BackupStack.tsx
  - src/navigation/settings-routes.ts
  - src/screens/settings-hub-model.ts
  - src/screens/RestoreResultScreen.tsx
  - src/screens/RestorePreviewScreen.tsx
  - src/screens/BackupScreen.tsx
  - src/screens/backup-dualhome-logic.ts
  - src/screens/backup-dualhome-logic.test.ts
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Data & Backup (D-08 / §I) opens the SAME canonical Backup screen tree as the Backup tab — the four existing screens (`BackupScreen`/`BackupSettingsScreen`/`RestorePreviewScreen`/`RestoreResultScreen`) are registered in `SettingsStackParamList` + `SettingsStack.tsx` and reused unchanged (one canonical tree, two entry points), not a second copy. The Backup bottom tab stays (removal is DEFERRED §R — this plan only adds the entry point)."
    - "Host identity is resolved by an EXPLICIT `host` prop threaded through per-stack wrapper components — `host=\"backup-tab\"` in `BackupStack`, `host=\"settings\"` in `SettingsStack` — NOT by inferring the parent tab from nested navigation state via `getParent`/`getState` (review HIGH: nav-state inference had no parent ID, no fail-closed default, no test). The `host` prop DEFAULTS to `\"backup-tab\"` (fail-closed to the shipped single-consumer/reset-to-Backup behaviour) so any un-anticipated future mount degrades safely."
    - "The post-restore return is origin-aware at EVERY reset site across the four screens (review MEDIUM — origin handling was only partially enumerated): the Backup-TAB flow still resets to Backup (UNCHANGED shipped behaviour) while the Settings-entry flow returns to the Settings hub. This covers `RestoreResultScreen.tsx:18` (the 'Return to Backup & Restore' reset) AND `RestorePreviewScreen.tsx:123` (the success reset base) — both routed through `restoreReturnRouteName(host)`. (`RestorePreviewScreen.tsx:99` `returnToSelection` re-picks a file and targets `Backup`, which resolves within whichever stack hosts it.)"
    - "The native shared-backup singleton is consumed by the BACKUP-TAB copy only: the Settings-mounted `BackupScreen` does NOT drain `consumeSharedBackup` on focus (RESEARCH Pitfall 3 hazard 2 — the share-intent gate routes shared backups to `BackupTab › Backup` at linking.ts:67; a Settings copy that also consumed on focus would double-drain). Gated on `shouldConsumeSharedBackup(host)`."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06); no new backup functionality is invented merely because ownership is moving (§I)."
  artifacts:
    - src/screens/backup-dualhome-logic.ts
  key_links:
    - "Hub `Data & Backup` row → navigation.navigate('Backup') within the Settings stack → the same BackupScreen tree the tab hosts."
    - "Per-stack wrapper passes explicit host → RestoreResultScreen/RestorePreviewScreen reset via restoreReturnRouteName(host) ('Backup' | 'Settings'); BackupScreen focus effect gates consumeSharedBackup on shouldConsumeSharedBackup(host); host defaults to 'backup-tab' (fail-closed)."
---

<objective>
Give **Data & Backup** a second, canonical entry point from Settings (**D-08 / §I**) by registering the existing four Backup screens in the Settings stack — one tree, two entry points — and handle the two dual-home hazards so the Backup TAB flow is preserved unchanged while the Settings flow behaves correctly: origin-aware post-restore return, and tab-scoped consumption of the native shared-backup singleton.

Implements **D-08** (Backup dual-home; tab removal deferred). Honors **§I** (one canonical tree, no new backup functionality) and the deferrals in **§R**. Cites **D-01**.

Purpose: prepare Settings as the long-term Backup home without breaking the shipped Backup-tab restore/share flows.
Output: the four Backup route registrations (via per-stack wrappers passing an explicit `host`) + a hub Data & Backup row, plus `backup-dualhome-logic.ts` driving origin-aware return and tab-scoped consume, with a dual-home test.
</objective>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan:
- **HIGH — native shared-backup singleton safety hinges on unspecified `host` resolution**: RESOLVED by the reviewers' preferred fix — an EXPLICIT `host` prop threaded through per-stack wrapper components (`BackupStack` → `host="backup-tab"`, `SettingsStack` → `host="settings"`), NOT `getParent`/`getState` nav-state inference. The prop defaults to `"backup-tab"` (fail-closed: preserves the shipped single-consumer/reset-to-Backup behaviour). This removes the fragile, untested nested-state derivation entirely.
- **MEDIUM — origin handling spread across shared screens but only partially enumerated (`RestorePreviewScreen.tsx:98,123`)**: RESOLVED — Task 2 now enumerates and handles ALL backup-tree reset-to-Backup sites: `RestoreResultScreen.tsx:18`, `RestorePreviewScreen.tsx:123` (success reset base → `restoreReturnRouteName(host)`), and documents `RestorePreviewScreen.tsx:99` (`returnToSelection` re-pick → stays `Backup`, resolving within the current stack). The original plan touched only RestoreResultScreen + BackupScreen.
- **MEDIUM — pure helper tests do not test host resolution**: RESOLVED — with an explicit prop, host resolution is a wiring assertion (each stack's wrapper passes its host; default is `backup-tab`) plus the pure-helper tests (`restoreReturnRouteName`/`shouldConsumeSharedBackup`) and a single-drain-per-host assertion; there is no nested nav-state to test because inference is gone.
- **LOW — Settings hub row uses generic route name `Backup`**: ACCEPTED as-is — reusing the existing `Backup` route name is required for "one canonical tree" (§I); a Settings-specific alias would fork the tree. Noted, not changed.
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the four Backup screens in the Settings stack (D-08) via per-stack host wrappers + hub Data & Backup row</name>
  <files>src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/tabs/BackupStack.tsx, src/navigation/settings-routes.ts, src/screens/backup-dualhome-logic.ts, src/screens/BackupScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/settings-hub-model.ts</files>
  <read_first>
    - src/navigation/tabs/BackupStack.tsx (READ IN FULL — the four registrations to convert to host-passing wrappers; the screens self-fetch and take only `navigation`/`route`, so they mount unchanged in a second stack)
    - src/navigation/types.ts:216-227 (BackupStackParamList — the exact param shapes for `Backup`/`BackupSettings`/`RestorePreview`/`RestoreResult` to add to SettingsStackParamList; RootStackParamList is a TYPE intersection so each hosting stack must list the routes it reaches — types.ts:314-317, already done for Profile-hosted routes at 245-299)
    - src/screens/BackupScreen.tsx (READ IN FULL — typed `RootStackScreenProps<"Backup">`; the `host` prop is added here, defaulting to `DEFAULT_BACKUP_HOST`)
    - src/screens/RestorePreviewScreen.tsx (READ IN FULL — typed `RootStackScreenProps<"RestorePreview">`; add the `host` prop) and src/screens/RestoreResultScreen.tsx (add the `host` prop)
    - src/screens/backup-restore-logic.ts:5-52 (the `RestorePreviewRoute = { token; preview }` param is serializable; the heavy manifest lives in a module-singleton `Map` keyed by token — dual-home safe, one shared cache)
    - src/navigation/settings-routes.ts (append `"Backup"`, `"BackupSettings"`, `"RestorePreview"`, `"RestoreResult"` to `SETTINGS_REGISTERED_ROUTES`)
    - src/screens/settings-hub-model.ts (insert the `Data & Backup` `kind:"route"` row at §A index 5, route `Backup`)
  </read_first>
  <action>
Define the host plumbing in `src/screens/backup-dualhome-logic.ts`: export `type BackupHost = "backup-tab" | "settings"` and `const DEFAULT_BACKUP_HOST: BackupHost = "backup-tab"` (fail-closed to the shipped behaviour). Add an optional `host?: BackupHost` prop (defaulting to `DEFAULT_BACKUP_HOST`) to `BackupScreen`, `RestorePreviewScreen`, and `RestoreResultScreen` (extend their props to `RootStackScreenProps<"…"> & { host?: BackupHost }`). This task only wires the prop through; Task 2 consumes it for the reset/consume behaviour.

Add `Backup`, `BackupSettings`, `RestorePreview` (`RestorePreviewRoute`), and `RestoreResult` (its existing param shape) to `SettingsStackParamList` in `src/navigation/types.ts`. In `src/navigation/tabs/SettingsStack.tsx`, register the four routes via wrapper components that pass `host="settings"` for `Backup`/`RestorePreview`/`RestoreResult` (`BackupSettings` needs no host — it neither resets to Backup nor consumes). In `src/navigation/tabs/BackupStack.tsx`, convert the `Backup`/`RestorePreview`/`RestoreResult` registrations to wrappers passing `host="backup-tab"` (explicit, equals the default). Reuse the screens unchanged — do NOT create a second Backup tree (§I). Append the four names to `SETTINGS_REGISTERED_ROUTES`. Insert the `Data & Backup` hub row (route `Backup`, §A index 5) into `settings-hub-model.ts`. Do NOT remove the Backup bottom tab (§R deferred). Do NOT invent new backup functionality (§I).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsStackParamList` contains `Backup`, `BackupSettings`, `RestorePreview`, `RestoreResult` with the same param shapes as `BackupStackParamList`; all four are in `SETTINGS_REGISTERED_ROUTES`; `SettingsStack.tsx` registers all four, with `Backup`/`RestorePreview`/`RestoreResult` via wrappers passing `host="settings"`.
    - source: `BackupStack.tsx` registers `Backup`/`RestorePreview`/`RestoreResult` via wrappers passing `host="backup-tab"`; the three screens accept an optional `host?: BackupHost` defaulting to `DEFAULT_BACKUP_HOST = "backup-tab"`.
    - source: `settings-hub-model.ts` has the `Data & Backup` row at §A index 5 targeting `Backup`; the Backup bottom tab still exists (§R).
    - `npx tsc --noEmit` + `npm run check:colors` pass; hub-model + settings-routes source-scan tests green.
    - behavior (device UAT): Settings → Data & Backup opens the same Backup screen tree the tab opens.
  </acceptance_criteria>
  <done>Data & Backup reachable from Settings via the one canonical tree with an explicit `host` threaded through per-stack wrappers (D-08); tab preserved; hub row in place; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-home hazards — origin-aware resets at EVERY site (via host prop) + tab-scoped shared-backup consume + tests</name>
  <files>src/screens/RestoreResultScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/BackupScreen.tsx, src/screens/backup-dualhome-logic.ts, src/screens/backup-dualhome-logic.test.ts</files>
  <read_first>
    - src/screens/RestoreResultScreen.tsx (READ IN FULL: line 18 hard-resets `navigation.reset({ index: 0, routes: [{ name: "Backup" }] })`; it now receives the `host` prop added in Task 1)
    - src/screens/RestorePreviewScreen.tsx (READ IN FULL — the review's partially-enumerated finding: `returnToSelection` at line 99 resets `[{ name: "Backup" }]`; the SUCCESS path at lines 123-129 resets `[{ name: "Backup" }, { name: "RestoreResult", params }]`. The success-reset BASE must be origin-aware; `returnToSelection` re-picks a file and targets `Backup`, which resolves within the current stack. It now receives the `host` prop.)
    - src/screens/BackupScreen.tsx (READ IN FULL: the `useFocusEffect` at ~259-273 calling `consumeSharedBackup()` — drains the native singleton on focus in whichever stack gains focus; it now receives the `host` prop)
    - src/navigation/linking.ts:56-74 (the share-intent gate routes a shared backup to `navigationRef.current?.navigate("BackupTab", { screen: "Backup" })` — the TAB copy is the intended consumer)
    - src/navigation/tabs/BackupStack.tsx (per CLAUDE.md read every consumer: the tab flow MUST stay unchanged — its wrappers pass `host="backup-tab"`)
    - src/screens/backup-restore-logic.ts (the restore flow BackupScreen → RestorePreview → RestoreResult; `toRestoreResultParams`)
  </read_first>
  <behavior>
    - restoreReturnRouteName(host): `"Backup"` for `"backup-tab"` (UNCHANGED shipped behaviour), `"Settings"` for `"settings"`. Test both hosts.
    - shouldConsumeSharedBackup(host): `true` only for `"backup-tab"`; `false` for `"settings"` (single drain, by the tab). Test both hosts.
    - DEFAULT_BACKUP_HOST is `"backup-tab"` (fail-closed) — assert the default so an un-anticipated mount preserves shipped behaviour.
    - Single-drain-per-host: assert `shouldConsumeSharedBackup` is true for exactly the tab host, so the singleton is consumed once across the two mounts.
    - The helpers are pure over the host identity (no DB, no nav, no singleton) — unit-testable without a device.
  </behavior>
  <action>
Implement the two pure helpers in `src/screens/backup-dualhome-logic.ts`: `restoreReturnRouteName(host: BackupHost)` and `shouldConsumeSharedBackup(host: BackupHost)` (alongside the `BackupHost` type + `DEFAULT_BACKUP_HOST` added in Task 1). Write `backup-dualhome-logic.test.ts` FIRST (RED) asserting the behavior block, then implement (GREEN). Host is the EXPLICIT prop from Task 1 — do NOT infer it from `getParent`/`getState` (the review HIGH: nav-state inference is removed entirely).

Make every reset-to-Backup site origin-aware via `restoreReturnRouteName(host)`:
  - `RestoreResultScreen.tsx:18` — the "Return to Backup & Restore" reset → `navigation.reset({ index: 0, routes: [{ name: restoreReturnRouteName(host) }] })`.
  - `RestorePreviewScreen.tsx:123` — the SUCCESS reset → base route becomes `restoreReturnRouteName(host)`: `reset({ index: 1, routes: [{ name: restoreReturnRouteName(host) }, { name: "RestoreResult", params: toRestoreResultParams(result) }] })`. (The RestoreResult that lands is the same-stack copy — its own `host` drives its return.)
  - `RestorePreviewScreen.tsx:99` — `returnToSelection` re-picks a file; keep it targeting `Backup` (present in both stacks, resolves within the current one). Document why it is not hub-routed.
The Backup-tab flow MUST still reset to `Backup` (no regression) since its wrappers pass `host="backup-tab"`.

In `BackupScreen.tsx`, gate the `useFocusEffect` `consumeSharedBackup()` call on `shouldConsumeSharedBackup(host)` so only the Backup-tab copy drains the native singleton; the Settings-mounted copy never consumes it. Keep the tab copy identical to today (linking.ts still routes shared backups to `BackupTab › Backup`).

This enforces the existing share-intent contract (does not reverse it); it only prevents the new Settings entry point from double-draining. Reversibility: reversible (pure code under git).
  </action>
  <verify>
    <automated>npx vitest run src/screens/backup-dualhome-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: `backup-dualhome-logic.test.ts` asserts `restoreReturnRouteName("backup-tab") === "Backup"`, `restoreReturnRouteName("settings") === "Settings"`, `shouldConsumeSharedBackup("backup-tab") === true`, `shouldConsumeSharedBackup("settings") === false`, `DEFAULT_BACKUP_HOST === "backup-tab"`, and the single-drain-per-host invariant.
    - source: BOTH `RestoreResultScreen.tsx` (line 18) and `RestorePreviewScreen.tsx` (success reset base, line 123) reset via `restoreReturnRouteName(host)`; `RestorePreviewScreen.tsx:99` returnToSelection stays `Backup` (documented). No `getParent`/`getState` host inference anywhere.
    - source: `BackupScreen.tsx` gates `consumeSharedBackup` on `shouldConsumeSharedBackup(host)`.
    - `npx vitest run src/screens/backup-dualhome-logic.test.ts` green; `npx tsc --noEmit` passes.
    - behavior (device UAT, per memory saf-grant-reconnect-cycle): Settings → Data & Backup → Restore returns to the Settings hub; the Backup tab restore still returns to Backup; a shared backup opens once (no double-drain) and lands on the Backup tab copy.
  </acceptance_criteria>
  <done>All backup-tree reset sites are origin-aware via the explicit host prop, the shared-backup singleton is drained once by the tab, tab flow preserved, tested; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native shared-backup singleton (`consumeSharedBackup`) → restore pipeline | A single native resource drained on focus; dual-mounting the consumer risks a double-drain / wrong-copy consume. |
| restored backup document → local SQLite (via the existing restore pipeline) | Unchanged — the restore logic is reused, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering / DoS | `consumeSharedBackup` native singleton double-drained across two mounted `BackupScreen` copies | medium | mitigate | Tab-scope the consume via `shouldConsumeSharedBackup(host)` — only the Backup-tab copy drains it; unit-tested; the share-intent gate (linking.ts:67) already targets the tab. |
| T-37-07 | Denial of navigation (stranded user) | `RestoreResultScreen` hard-reset to "Backup" inside the Settings stack | low | mitigate | Origin-aware `restoreReturnRouteName(host)`; the Settings flow returns to the hub, the tab flow is unchanged; unit-tested. |
| T-37-01 | Tampering | restore applying to local SQLite | low | accept | Restore pipeline reused unchanged; no new write path introduced. |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress (backup export/restore is on-device / SAF, unchanged).
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/backup-dualhome-logic.ts` — `BackupHost` type, `DEFAULT_BACKUP_HOST` constant, and `restoreReturnRouteName` + `shouldConsumeSharedBackup` pure helpers + `backup-dualhome-logic.test.ts`.
- New optional `host?: BackupHost` prop on `BackupScreen`, `RestorePreviewScreen`, `RestoreResultScreen` (default `"backup-tab"`).
- New `SettingsStackParamList` route names (re-registrations of the existing Backup screens, also appended to `SETTINGS_REGISTERED_ROUTES`): `Backup`, `BackupSettings`, `RestorePreview`, `RestoreResult`.
- Modified (behaviour-preserving) shared surfaces: `SettingsStack.tsx`/`BackupStack.tsx` per-stack host wrappers; `RestoreResultScreen.tsx` + `RestorePreviewScreen.tsx` (origin-aware resets), `BackupScreen.tsx` (tab-scoped consume).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/backup-dualhome-logic.test.ts src/screens/settings-hub-model.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06); no new backup functionality (§I).
- Device UAT (per memory saf-grant-reconnect-cycle + verify-ui-on-pixel-yourself): reach Backup from BOTH entry points; confirm one canonical tree, origin-aware return, and no double-drain of a shared backup.
</verification>

<success_criteria>
Data & Backup dual-homed into Settings on one canonical tree (D-08), both dual-home hazards handled with tested helpers, the shipped Backup-tab flow preserved, tab removal deferred (§R) — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-07-SUMMARY.md` when done.
</output>
