---
phase: 37-settings-personalization
plan: 06
type: execute
wave: 6
depends_on: ["37-05"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/settings-routes.ts
  - src/screens/settings-hub-model.ts
  - src/screens/SettingsOrreryScreen.tsx
  - src/stores/orrery-preferences-store.cross-surface.test.ts
  - src/screens/SettingsAIScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → Orrery is a category screen (§H) reached from the hub: a Display section exposing the stateless user-configurable Orrery preferences (density, satellites) bound to the CANONICAL shared preference source — the `useOrreryPreferencesStore` Zustand store (the deliberate single serialized writer whose `save()` writes SQLite AND publishes `committed`) — read via `committed` and written via `save()`, EXACTLY as `OrreryViewOptions` does. Settings does NOT call `updateAppSettings` for `orrery_density`/`orrery_satellites_enabled` directly (review HIGH: a second unsynchronized writer is the duplicate preference model §H forbids). Changes from Settings and from the Orrery immediately agree through the one store."
    - "The Settings Orrery Display controls surface the store's `saving`/`saveError`/`hydration` states and a `retry` (mirroring OrreryViewOptions), hydrating the store on mount; a Systems section routes to the canonical Systems Management screen."
    - "Remembered/session Orrery state (last active System, `lastSystem`) is NOT surfaced as a Setting (§C/§H — persistence alone does not make it a Setting); Settings never writes `lastSystem`."
    - "Settings → AI is a top-level category (§J) after Data & Backup that routes into the canonical Phase 36 AI hierarchy (the existing `AIConnection`/`AIModelPicker`/`AIPersonalization`/`AIPermissions`/`AIPreview` leaf routes) via `settings-ai-hub-logic.ts`, including the AI-Off credential-management escape hatch; it only flips `ai_enabled` and does NOT widen AI egress (`AiService.ts` untouched)."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsOrreryScreen.tsx
    - src/screens/SettingsAIScreen.tsx
    - src/stores/orrery-preferences-store.cross-surface.test.ts
  key_links:
    - "Orrery Display control → useOrreryPreferencesStore.save(getExecutor(), { density | satellitesEnabled }) → SQLite commit + committed publication → both Settings and OrreryScreen render the same `committed` (single preference source, §H)."
    - "AI category → settings-ai-hub-logic deriveAiHubState → existing AI leaf routes; AI Enabled toggle → buildAiEnabledPatch → app_settings.ai_enabled only."
---

<objective>
Build the **Orrery** category (§H — Display prefs bound to the shared preference source + Systems Management routing) and the **AI** category (§J — routes into the canonical Phase 36 AI hierarchy, flips `ai_enabled` only), migrating both groups out of the monolith.

Implements dossier **§H** (Orrery Display + Systems; shared preference source; last-System excluded) and **§J** (AI category reusing the Phase 36 hub + escape hatch, no egress widening). Cites **D-01**.

Purpose: relocate the Orrery preferences and the AI hub with no duplicate preference model and no new AI egress.
Output: SettingsOrreryScreen (bound to `useOrreryPreferencesStore`) + a cross-surface store test + SettingsAIScreen; monolith Orrery/Systems + AI groups removed.
</objective>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan:
- **HIGH — Plan 06 forks the Orrery's canonical preference source (§H DECIDED)**: RESOLVED. The original plan wrote `orrery_density`/`orrery_satellites_enabled` directly via `updateAppSettings`, but the Orrery renders from `useOrreryPreferencesStore((s)=>s.committed)` (OrreryScreen.tsx:218) — a deliberately single-serialized-writer Zustand store whose `save()` writes SQLite then publishes `committed` (orrery-preferences-store.ts). Task 1 now routes the Settings Orrery Display controls through the SAME store (`committed` for read, `save`/`hydrate`/`retry` for write), exactly like `OrreryViewOptions.tsx`, so §H's "changes agree immediately; no duplicate preference model" holds by construction (not via an undocumented focus side-effect, and with no race against the store's in-flight drain). A cross-surface store test is added (the prior DAO round-trip test could not observe cross-surface agreement). `lastSystem` stays out of Settings (§C/§H). The AI half (Task 2) was already correct.
- **The existing DAO test cannot catch this defect** (Codex MEDIUM): RESOLVED — a store-level cross-surface test replaces reliance on the DAO round-trip alone.
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsOrreryScreen — Display bound to useOrreryPreferencesStore (shared source, §H) + Systems routing + cross-surface store test</name>
  <files>src/screens/SettingsOrreryScreen.tsx, src/stores/orrery-preferences-store.cross-surface.test.ts, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/settings-routes.ts, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/stores/orrery-preferences-store.ts (READ IN FULL — the canonical shared source: `useOrreryPreferencesStore` singleton; `committed` (density/satellitesEnabled/lastSystem); `hydrate`/`save(exec, intent, origin?, forceLastSystemCommit?)`/`retry`; `saving`/`saveError`/`hydration`; the single serialized `drain` writer; `createOrreryPreferencesStore(io)` factory for injecting adapters in a test)
    - src/components/orrery/OrreryViewOptions.tsx (READ IN FULL — the exact consumer pattern to mirror: `const preferences = useOrreryPreferencesStore()`; read `preferences.committed.density` / `preferences.committed.satellitesEnabled`; write `preferences.save(getExecutor(), { density })` / `{ satellitesEnabled }`; `busy`/`disabled` from `saving`/`hydration`/`hydrated`; error → `preferences.retry(getExecutor())`)
    - src/screens/OrreryScreen.tsx:200-230 + 405-420 (VERIFY the Orrery renders from `useOrreryPreferencesStore((s)=>s.committed)` at ~218 and hydrates on focus — the surface Settings must agree with)
    - src/db/app-settings-dao.ts (`ORRERY_DENSITIES`; the store's adapter writes `orrery_density`/`orrery_satellites_enabled` — Settings must NOT write these directly)
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Systems row at ~1945-1958 → `navigation.navigate("SystemsManagement")`; density/satellites are NOT rendered in the monolith today — they live only on the Orrery, so Settings SURFACES them for the first time via the store)
    - src/navigation/settings-routes.ts (append `"SettingsOrrery"` to `SETTINGS_REGISTERED_ROUTES`)
    - src/screens/settings-hub-model.ts (insert the `Orrery` `kind:"route"` row at §A index 4)
  </read_first>
  <behavior>
    - Cross-surface store test (`orrery-preferences-store.cross-surface.test.ts`): build a store via `createOrreryPreferencesStore(io)` with a mock adapter; `hydrate`; then `save(exec, { density })` (a Settings-style write) publishes `committed.density` that ANY consumer reading `committed` observes, and the mock `write` was called exactly once (single serialized writer) — proving Settings and the Orrery share one source. Also assert a Settings write never sets `lastSystem`.
  </behavior>
  <action>
Create `src/screens/SettingsOrreryScreen.tsx` (leaf-shell analog). Its **Display** section binds to the CANONICAL `useOrreryPreferencesStore` EXACTLY as `OrreryViewOptions.tsx` does — NOT via a direct `updateAppSettings` call:
  - read density/satellites from `preferences.committed`;
  - write via `preferences.save(getExecutor(), { density })` and `preferences.save(getExecutor(), { satellitesEnabled: enabled ? 1 : 0 })`;
  - `hydrate(getExecutor())` on mount (so the store is ready before edits);
  - surface `saving`/`hydration` as busy/disabled and `saveError`/`hydration === "error"` with a `retry(getExecutor())` affordance.
This makes a change here and a change on the Orrery agree immediately through the single serialized writer (§H — no duplicate preference model; the direct-`updateAppSettings` write the review flagged is removed). Do NOT surface `lastSystem` (§C/§H) and never pass it to `save`.

A **Systems** section routes to the canonical `SystemsManagement` (reuse by navigation; it also stays reachable from the Orrery per §H).

Write `orrery-preferences-store.cross-surface.test.ts` per the behavior block. Register `SettingsOrrery: undefined;` in types.ts + the SettingsStack wrapper, append `"SettingsOrrery"` to `SETTINGS_REGISTERED_ROUTES`; insert the hub row (§A index 4). Remove the migrated Systems row (1945-1958) from `SettingsScreen.tsx`. Theme tokens + §O a11y.
  </action>
  <verify>
    <automated>npx vitest run src/stores/orrery-preferences-store.cross-surface.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts src/db/app-settings-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsOrreryScreen.tsx` imports `useOrreryPreferencesStore`, reads `committed`, and writes via `preferences.save(...)`; it does NOT call `updateAppSettings` for `orrery_density`/`orrery_satellites_enabled` (no forked writer, review HIGH), and never writes `lastSystem`.
    - source: the screen hydrates the store and surfaces `saving`/`saveError`/`hydration` + `retry`, mirroring `OrreryViewOptions`; navigates to `SystemsManagement`.
    - test: `orrery-preferences-store.cross-surface.test.ts` proves a Settings-style `save` publishes `committed` observed cross-surface, with the adapter `write` called once and `lastSystem` untouched.
    - source: `SettingsOrrery` is in `SETTINGS_REGISTERED_ROUTES` + `SettingsStackParamList`, SettingsStack registers it, hub model has the row at §A index 4 (hub-model + settings-routes source-scan tests green).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): changing density/satellites in Settings is reflected on the Orrery and vice versa via the one store (single source).
  </acceptance_criteria>
  <done>Orrery category ships Display bound to the canonical `useOrreryPreferencesStore` (§H, no forked writer) + Systems routing, last-System excluded, with a cross-surface store test; monolith Systems row removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsAIScreen — route into the canonical Phase 36 AI hub (§J), flip ai_enabled only</name>
  <files>src/screens/SettingsAIScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/settings-routes.ts, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the AI group at ~1730-1874 — AI Enabled + hub rows + first-use disclosure)
    - src/screens/settings-ai-hub-logic.ts (the canonical AI hub row/route model: `deriveAiHubState`, `AI_HUB_SECTIONS`, `buildAiEnabledPatch`, the escape-hatch + simplified-off states — reuse this verbatim, do not re-derive)
    - src/navigation/tabs/SettingsStack.tsx (the AI leaf routes `AIConnection`/`AIModelPicker`/`AIPersonalization`/`AIPermissions`/`AIPreview` are ALREADY registered — reuse by navigation)
    - src/navigation/settings-routes.ts (append `"SettingsAI"` to `SETTINGS_REGISTERED_ROUTES`)
    - src/screens/settings-hub-model.ts (insert the `AI` `kind:"route"` row at §A index 6)
  </read_first>
  <action>
Create `src/screens/SettingsAIScreen.tsx` (leaf-shell analog) that renders the AI hub using `settings-ai-hub-logic.ts`: the AI Enabled master toggle (writing `buildAiEnabledPatch` → `ai_enabled` ONLY), the `deriveAiHubState` sections navigating to the existing AI leaf routes, the first-use disclosure, and the AI-Off credential-management escape hatch (§J). It must NOT widen AI egress — `AiService.ts` is untouched; the master toggle cannot mutate saved AI config (per the logic module's contract).

Register `SettingsAI: undefined;` in types.ts + the SettingsStack wrapper, append `"SettingsAI"` to `SETTINGS_REGISTERED_ROUTES`; insert the hub row (§A index 6). Remove the migrated AI group (1730-1874) from `SettingsScreen.tsx`. Theme tokens + §O a11y.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/navigation/settings-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAIScreen.tsx` imports `settings-ai-hub-logic.ts` (`deriveAiHubState`/`buildAiEnabledPatch`) and navigates to the existing AI leaf routes; the master toggle writes only `ai_enabled`.
    - source: `AiService.ts` is NOT modified this plan (no egress widening); the AI group is removed from `SettingsScreen.tsx`.
    - source: `SettingsAI` is in `SETTINGS_REGISTERED_ROUTES` + `SettingsStackParamList`, SettingsStack registers it, hub model has the row at §A index 6 (hub-model + settings-routes source-scan tests green).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT — do NOT trigger a real AI API call per memory no-ai-api-calls-without-clearing): AI category routes into Connection/Model/Personalization/Permissions/Preview; master toggle flips AI on/off.
  </acceptance_criteria>
  <done>AI category routes into the canonical Phase 36 hierarchy + escape hatch, flips only `ai_enabled`, no egress widened; monolith AI group removed; committed.</done>
</task>

<task type="auto">
  <name>Task 3: hub-model test extension — Orrery + AI rows at §A indices; shared-pref-source note</name>
  <files>src/screens/settings-hub-model.test.ts</files>
  <read_first>
    - src/screens/settings-hub-model.ts (rows added in Tasks 1-2)
    - src/screens/settings-hub-model.test.ts (extend the existing Wave-0 test)
  </read_first>
  <action>
Extend `src/screens/settings-hub-model.test.ts` to assert the `Orrery` (index 4) and `AI` (index 6) `kind:"route"` rows are present in §A order and target `SettingsOrrery` / `SettingsAI` (both now in `SETTINGS_REGISTERED_ROUTES`). This keeps the growing hub model's ordering invariant enforced as categories land; the source-scan `settings-routes.test.ts` (from Plan 01) proves the two routes are actually registered `<Stack.Screen>`s.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: asserts Orrery + AI rows present, in §A order, targeting routes in `SETTINGS_REGISTERED_ROUTES`; settings-routes source-scan proves both are registered.
    - `npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts` green.
  </acceptance_criteria>
  <done>Hub-model ordering coverage extended for Orrery + AI; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (Orrery prefs / AI toggle) → DAO write → SQLite | Orrery Display prefs + `ai_enabled` cross here; the AI leaf routes own credential flows (untouched). |
| AI egress (existing, user-configured) | Settings only flips `ai_enabled`; the egress path (`AiService.ts`) is not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Orrery pref writes to `app_settings` | low | mitigate | Route through `useOrreryPreferencesStore.save` — the single serialized writer whose adapter uses validated `updateAppSettings`; no forked writer, no inline SQL, one shared source (§H). |
| T-37-03 | Information disclosure | AI egress via a Settings toggle | low | mitigate | AI category flips `ai_enabled` only; `AiService.ts` untouched; credentials stay SecureStore-only (never surfaced in Settings/backup). |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsOrreryScreen.tsx` — Orrery category screen (Display bound to `useOrreryPreferencesStore` + Systems).
- `src/stores/orrery-preferences-store.cross-surface.test.ts` — cross-surface shared-source test (Settings ↔ Orrery agree through one store).
- `src/screens/SettingsAIScreen.tsx` — AI category screen (reuses `settings-ai-hub-logic.ts`).
- New `SettingsStackParamList` route names: `SettingsOrrery`, `SettingsAI` (also appended to `SETTINGS_REGISTERED_ROUTES`).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts src/db/app-settings-dao.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06); `AiService.ts` untouched.
- Device UAT: Orrery prefs share one source with the Orrery; AI category routes into the Phase 36 hub (no real AI call triggered).
</verification>

<success_criteria>
Orrery (§H, shared pref source, last-System excluded) and AI (§J, reused Phase 36 hierarchy, `ai_enabled`-only, no egress widening) categories migrated with zero behaviour lost — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-06-SUMMARY.md` when done.
</output>
