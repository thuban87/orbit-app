---
phase: 37-settings-personalization
plan: 06
type: execute
wave: 6
depends_on: ["37-05"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/SettingsOrreryScreen.tsx
  - src/screens/SettingsAIScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → Orrery is a category screen (§H) reached from the hub: a Display section exposing the stateless user-configurable Orrery preferences (`orrery_density`, `orrery_satellites_enabled`) bound to the SAME underlying `app_settings` preference source the Orrery itself uses (changes from either surface immediately agree — no duplicate preference model, §H), and a Systems section routing to the canonical Systems Management screen."
    - "Remembered/session Orrery state (last active System) is NOT surfaced as a Setting (§C/§H — persistence alone does not make it a Setting)."
    - "Settings → AI is a top-level category (§J) after Data & Backup that routes into the canonical Phase 36 AI hierarchy (the existing `AIConnection`/`AIModelPicker`/`AIPersonalization`/`AIPermissions`/`AIPreview` leaf routes) via `settings-ai-hub-logic.ts`, including the AI-Off credential-management escape hatch; it only flips `ai_enabled` and does NOT widen AI egress (`AiService.ts` untouched)."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsOrreryScreen.tsx
    - src/screens/SettingsAIScreen.tsx
  key_links:
    - "Orrery Display control → updateAppSettings → the same app_settings columns the Orrery reads (single preference source, §H)."
    - "AI category → settings-ai-hub-logic deriveAiHubState → existing AI leaf routes; AI Enabled toggle → buildAiEnabledPatch → app_settings.ai_enabled only."
---

<objective>
Build the **Orrery** category (§H — Display prefs bound to the shared preference source + Systems Management routing) and the **AI** category (§J — routes into the canonical Phase 36 AI hierarchy, flips `ai_enabled` only), migrating both groups out of the monolith.

Implements dossier **§H** (Orrery Display + Systems; shared preference source; last-System excluded) and **§J** (AI category reusing the Phase 36 hub + escape hatch, no egress widening). Cites **D-01**.

Purpose: relocate the Orrery preferences and the AI hub with no duplicate preference model and no new AI egress.
Output: SettingsOrreryScreen + SettingsAIScreen; monolith Orrery/Systems + AI groups removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsOrreryScreen — Display (density/satellites, shared pref source) + Systems routing</name>
  <files>src/screens/SettingsOrreryScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Systems row at ~1945-1958 → `navigation.navigate("SystemsManagement")`; confirm whether density/satellites are currently rendered in the monolith or only on the Orrery — RESEARCH lists `orrery_density`/`orrery_satellites_enabled` as persisted prefs to surface here)
    - src/db/app-settings-dao.ts (VERIFY `orrery_density` + `orrery_satellites_enabled` columns + validators; the write path)
    - the Orrery screen's density/satellites control (find the Orrery-side consumer to confirm it reads the SAME `app_settings` columns — §H requires one shared preference source, changes agree immediately)
    - src/screens/settings-hub-model.ts (insert the `Orrery` row at §A index 4)
  </read_first>
  <action>
Create `src/screens/SettingsOrreryScreen.tsx` (leaf-shell analog): a **Display** section exposing `orrery_density` and `orrery_satellites_enabled` via `updateAppSettings` — bound to the SAME `app_settings` columns the Orrery reads, so a change here and a change on the Orrery agree immediately (§H — no duplicate preference model). A **Systems** section routes to the canonical `SystemsManagement` (reuse by navigation; it also stays reachable from the Orrery per §H). Do NOT surface remembered/session state such as the last active System (§C/§H).

Register `SettingsOrrery: undefined;` in types.ts + the SettingsStack wrapper; insert the hub row (§A index 4). Remove the migrated Systems row (1945-1958) from `SettingsScreen.tsx`. Theme tokens + §O a11y.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/db/app-settings-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsOrreryScreen.tsx` writes `orrery_density`/`orrery_satellites_enabled` via `updateAppSettings` (same columns the Orrery reads) and navigates to `SystemsManagement`; it does NOT surface last-active-System.
    - source: `SettingsStackParamList` has `SettingsOrrery`, SettingsStack registers it, hub model has the row at §A index 4 (hub-model test green).
    - test: `app-settings-dao.test.ts` green (density/satellites round-trip unchanged; D-06 no schema change).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): changing density/satellites in Settings is reflected on the Orrery and vice versa (single source).
  </acceptance_criteria>
  <done>Orrery category ships Display (shared pref source) + Systems routing, last-System excluded; monolith Systems row removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsAIScreen — route into the canonical Phase 36 AI hub (§J), flip ai_enabled only</name>
  <files>src/screens/SettingsAIScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the AI group at ~1730-1874 — AI Enabled + hub rows + first-use disclosure)
    - src/screens/settings-ai-hub-logic.ts (the canonical AI hub row/route model: `deriveAiHubState`, `AI_HUB_SECTIONS`, `buildAiEnabledPatch`, the escape-hatch + simplified-off states — reuse this verbatim, do not re-derive)
    - src/navigation/tabs/SettingsStack.tsx (the AI leaf routes `AIConnection`/`AIModelPicker`/`AIPersonalization`/`AIPermissions`/`AIPreview` are ALREADY registered — reuse by navigation)
    - src/screens/settings-hub-model.ts (insert the `AI` row at §A index 6)
  </read_first>
  <action>
Create `src/screens/SettingsAIScreen.tsx` (leaf-shell analog) that renders the AI hub using `settings-ai-hub-logic.ts`: the AI Enabled master toggle (writing `buildAiEnabledPatch` → `ai_enabled` ONLY), the `deriveAiHubState` sections navigating to the existing AI leaf routes, the first-use disclosure, and the AI-Off credential-management escape hatch (§J). It must NOT widen AI egress — `AiService.ts` is untouched; the master toggle cannot mutate saved AI config (per the logic module's contract).

Register `SettingsAI: undefined;` in types.ts + the SettingsStack wrapper; insert the hub row (§A index 6). Remove the migrated AI group (1730-1874) from `SettingsScreen.tsx`. Theme tokens + §O a11y.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAIScreen.tsx` imports `settings-ai-hub-logic.ts` (`deriveAiHubState`/`buildAiEnabledPatch`) and navigates to the existing AI leaf routes; the master toggle writes only `ai_enabled`.
    - source: `AiService.ts` is NOT modified this plan (no egress widening); the AI group is removed from `SettingsScreen.tsx`.
    - source: `SettingsStackParamList` has `SettingsAI`, SettingsStack registers it, hub model has the row at §A index 6 (hub-model test green).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT — do NOT trigger a real AI API call per memory no-ai-api-calls-without-clearing): AI category routes into Connection/Model/Personalization/Permissions/Preview; master toggle flips AI on/off.
  </acceptance_criteria>
  <done>AI category routes into the canonical Phase 36 hierarchy + escape hatch, flips only `ai_enabled`, no egress widened; monolith AI group removed; committed.</done>
</task>

<task type="auto">
  <name>Task 3: hub-model test extension — Orrery + AI rows at §A indices; shared-pref-source note</name>
  <files>src/screens/settings-hub-model.test.ts</files>
  <read_first>
    - src/screens/settings-hub-model.ts (rows added in Tasks 1-2)
    - src/screens/settings-hub-model.test.ts (extend the existing Wave-0 test)
  </read_first>
  <action>
Extend `src/screens/settings-hub-model.test.ts` to assert the `Orrery` (index 4) and `AI` (index 6) rows are present in §A order and target `SettingsOrrery` / `SettingsAI`. This keeps the growing hub model's ordering invariant enforced as categories land.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: asserts Orrery + AI rows present, in §A order, targeting registered routes.
    - `npx vitest run src/screens/settings-hub-model.test.ts` green.
  </acceptance_criteria>
  <done>Hub-model ordering coverage extended for Orrery + AI; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (Orrery prefs / AI toggle) → DAO write → SQLite | Orrery Display prefs + `ai_enabled` cross here; the AI leaf routes own credential flows (untouched). |
| AI egress (existing, user-configured) | Settings only flips `ai_enabled`; the egress path (`AiService.ts`) is not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Orrery pref writes to `app_settings` | low | mitigate | Reuse validated `updateAppSettings`; same columns as the Orrery (single source); no inline SQL. |
| T-37-03 | Information disclosure | AI egress via a Settings toggle | low | mitigate | AI category flips `ai_enabled` only; `AiService.ts` untouched; credentials stay SecureStore-only (never surfaced in Settings/backup). |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsOrreryScreen.tsx` — Orrery category screen (Display + Systems).
- `src/screens/SettingsAIScreen.tsx` — AI category screen (reuses `settings-ai-hub-logic.ts`).
- New `SettingsStackParamList` route names: `SettingsOrrery`, `SettingsAI`.
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts src/db/app-settings-dao.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06); `AiService.ts` untouched.
- Device UAT: Orrery prefs share one source with the Orrery; AI category routes into the Phase 36 hub (no real AI call triggered).
</verification>

<success_criteria>
Orrery (§H, shared pref source, last-System excluded) and AI (§J, reused Phase 36 hierarchy, `ai_enabled`-only, no egress widening) categories migrated with zero behaviour lost — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-06-SUMMARY.md` when done.
</output>
