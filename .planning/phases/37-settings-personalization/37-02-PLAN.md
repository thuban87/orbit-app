---
phase: 37-settings-personalization
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/settings-routes.ts
  - src/screens/settings-hub-model.ts
  - src/screens/SettingsAppearanceScreen.tsx
  - src/screens/settings-appearance-background.ts
  - src/screens/settings-appearance-background.test.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Appearance is a category screen (§D) reached from the hub, carrying the Theme section: theme package, appearance mode, curated accent, and background — each driving the live `theme-store` setter (instant restyle) AND persisting its durable `app_settings` column through the same `persist()` write path, exactly as the monolith does today (no behaviour lost)."
    - "The Galaxy-only background control is conditionally shown only while Galaxy is the active package — a one-line `themePackage === \"galaxy\"` guard on the reactive selector (D-07), with NO theme-store or schema change."
    - "Changing the active theme profile restyles the Appearance screen itself — every colour resolves through theme tokens (no hardcoded colours)."
    - "The Background section renders ONLY the active package's choices — `BACKGROUND_ORDER[themePackage]` plus None — not both packages' subgroups (review MEDIUM: the monolith renders both at SettingsScreen.tsx:922/935-962; the migrated screen gates on the reactive `themePackage`). Selecting a background persists the package-SENSITIVE key — `galaxyBackground` under Galaxy, `standardBackground` under Standard (mirroring SettingsScreen.tsx:522-531) — via a tested pure `backgroundPatchForPackage(pkg, slot)` helper (review cycle-2: the patch-key assertion, not just the choices list)."
    - "A FAILED durable persist is surfaced honestly, not swallowed: the monolith's `persist()` updates the live store first and fires the DAO write without awaiting/rolling back (SettingsScreen.tsx:501), so the migrated screen adds an inline save-error notice (or a reload of the durable value) rather than leaving the live store silently diverged from what was persisted (review MEDIUM — 'durable' must not imply 'failure-safe')."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06); Appearance builds on the CURRENT per-package theme model and accepts the later PARKED theme-merge rework (does NOT start it)."
  artifacts:
    - src/screens/SettingsAppearanceScreen.tsx
  key_links:
    - "Hub `Appearance` row → navigation.navigate('SettingsAppearance') → SettingsAppearanceScreen → theme-store setters (live) + persist() → app_settings theme columns."
---

<objective>
Build the **Appearance** category screen (§D) and migrate the Theme controls (package / mode / accent / background) out of the monolith into it, preserving the live-restyle + durable-persist behaviour verbatim. Implement the Galaxy-conditional control as the trivial one-line guard **D-07** confirms it is — no theme rewrite.

Implements **D-07** (Galaxy-conditional appearance control) and dossier **§D** Theme section; cites **D-01** (dossier ground truth). Builds on the CURRENT per-package theme model and explicitly accepts the later PARKED theme-merge (STATE `carried_forward`) — Deferred Idea, do NOT start it here.

Purpose: relocate the single largest control cluster without regressing instant restyle or durability.
Output: SettingsAppearanceScreen (Theme section); Orbit Center / self / global-profile-default controls land in Plan 03.
</objective>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan (both MEDIUM):
- **Live vs durable divergence on a failed write carried silently** (Codex+Claude): the migrated screen surfaces a failed persist inline (save-error notice / reload of the durable value), not just a caught log — see Task 1.
- **Background-grid conditional wording** (Codex LOW / Claude MEDIUM): the Background section renders only `BACKGROUND_ORDER[themePackage]` + None; the D-07 guard is applied to the Background grid (the one control that renders both packages today) — see Task 2.

Cross-AI review (37-REVIEWS.md, cycle 2) findings owned by this plan:
- **LOW/MEDIUM — Plan 02 patch-key test is absent (partial resolution of cycle-1 #3)** (codex, orchestrator-confirmed): cycle 1 added `backgroundChoicesForPackage(pkg)` + a choices test, but not the requested assertion that the correct per-package key is persisted (`galaxyBackground` vs `standardBackground`, `SettingsScreen.tsx:522-531`). RESOLVED — Task 2 factors the package-sensitive patch decision into a pure `backgroundPatchForPackage(pkg, slot)` helper (mirroring the monolith's `onSelectBackground` at 522-531), consumes it from the grid, and `settings-appearance-background.test.ts` asserts it yields `{ galaxyBackground: slot }` for `galaxy` and `{ standardBackground: slot }` for `standard`.
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsAppearanceScreen — Theme section (package/mode/accent/background) migrated verbatim + hub row + route</name>
  <files>src/screens/SettingsAppearanceScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Appearance/Theme JSX at ~701-981, the reactive selectors at 217-238 — `useThemeStore((s) => s.package)` etc. and the store setters `setPackage`/`setModeForActivePackage`/`setAccentForActivePackage`/`setBackgroundForActivePackage`, the `activeBackground`/`selectedBackgroundSlot` derivation at 263-267, and `onSelectBackground` at 522-532 which pairs a store setter with `persist()`)
    - src/stores/theme-store.ts (the store contract; NO AsyncStorage/persist middleware — durability is `app_settings` only)
    - src/navigation/tabs/SettingsStack.tsx (register `SettingsAppearance` with a `SettingsAppearanceRoute` wrapper, as Plan 01 did for Interactions)
    - src/navigation/settings-routes.ts (append `"SettingsAppearance"` to `SETTINGS_REGISTERED_ROUTES` — the Plan-01 runtime registration contract; the source-scan test then requires a matching `<Stack.Screen>`)
    - src/screens/settings-hub-model.ts (insert the `Appearance` `kind:"route"` row at §A index 0)
    - src/screens/AIPermissionsScreen.tsx:1-70 (screen shell)
  </read_first>
  <action>
Create `src/screens/SettingsAppearanceScreen.tsx` (leaf-shell analog; `onBack`; `useTheme()` colours only). MOVE the Theme JSX from SettingsScreen.tsx:701-981 into it verbatim in behaviour: package selector, mode selector, accent selector, background grid — each reading the reactive `useThemeStore` selector and calling the live store setter, then persisting through a `persist()` copied from SettingsScreen.tsx:501-520. Keep the `__DEV__` theme-preview row here (moved from the hub) targeting `__ThemePreview`.

Failed-write honesty (review MEDIUM): the copied `persist()` updates the live store first and fires the DAO write without awaiting/rolling back — a caught error is only logged (SettingsScreen.tsx:501). Do NOT leave that divergence silent on the migrated screen: on a persist failure, surface an inline, non-nagging save-error notice (theme-token colours, `accessibilityLiveRegion="polite"`) AND reconcile the visible state with the durable value (either reload `getAppSettings` into the store on failure or show that the change was not saved). Keep the live-restyle-first ordering (instant apply is the shipped behaviour); the only change is making a failed durable write observable rather than swallowed. Do not invent a new write path — reuse `updateAppSettings`/the store setters.

Register `SettingsAppearance: undefined;` in `src/navigation/types.ts` `SettingsStackParamList` and a `<Stack.Screen name="SettingsAppearance" component={SettingsAppearanceRoute} />` in SettingsStack. Insert the `Appearance` row (route `SettingsAppearance`, §A index 0) into `settings-hub-model.ts`.

Remove the migrated Theme JSX (701-981) and the `__DEV__` preview row from `src/screens/SettingsScreen.tsx` (now `SettingsMore`) so there is no duplicate write surface; leave the not-yet-migrated groups in place.

Accessibility (§O): every control carries an `accessibilityLabel` + role/state; section headings are screen-reader readable.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAppearanceScreen.tsx` reads `useThemeStore` selectors and calls the store setters, with each change also routed through a `persist()` mirroring SettingsScreen.tsx:501-520 (live restyle + durable write both preserved).
    - source: on a persist failure the screen renders an inline save-error notice (theme-token colours) and reconciles visible state with the durable value — a failed durable write is NOT silently swallowed (review MEDIUM).
    - source: `SettingsAppearance` is appended to `SETTINGS_REGISTERED_ROUTES` (src/navigation/settings-routes.ts) and `SettingsStackParamList` contains `SettingsAppearance: undefined;` and SettingsStack registers the screen; `settings-hub-model.ts` has the `Appearance` row at §A index 0 (hub-model + settings-routes source-scan tests still green).
    - source: the Theme JSX (package/mode/accent/background) is present in `SettingsAppearanceScreen.tsx` and removed from `SettingsScreen.tsx`.
    - `npm run check:colors` passes (no hardcoded colours); `npx tsc --noEmit` passes.
    - behavior (device UAT): changing package/mode/accent/background restyles the app instantly AND survives relaunch (durable), identical to the old monolith.
  </acceptance_criteria>
  <done>Appearance › Theme is a category screen reached from the hub, preserving live restyle + durability; monolith Theme JSX removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Galaxy-conditional background control (D-07) — active-package background choices + package-switch test + per-package patch-key test</name>
  <files>src/screens/SettingsAppearanceScreen.tsx, src/screens/settings-appearance-background.ts, src/screens/settings-appearance-background.test.ts</files>
  <read_first>
    - src/screens/SettingsAppearanceScreen.tsx (the background grid moved in Task 1)
    - src/screens/SettingsScreen.tsx (VERIFY the D-07 facts on disk: `themePackage` reactive selector at 222; Mode/Accent already read the active package at ~789 and ~845-846; the Background grid renders BOTH package subgroups at ~935-962 — this is the only control that differs; AND the package-SENSITIVE patch key in `onSelectBackground` at 522-531 — `themePackage === "galaxy" ? { galaxyBackground: slot } : { standardBackground: slot }` — the behaviour the new `backgroundPatchForPackage` helper + its test must mirror)
    - the `BACKGROUND_ORDER` source (grep `BACKGROUND_ORDER` — the per-package ordered background-slot list the grid maps) to confirm the active-package projection
  </read_first>
  <action>
Apply the D-07 guard in `SettingsAppearanceScreen.tsx` so the Background section renders ONLY the active package's choices — `BACKGROUND_ORDER[themePackage]` plus None — instead of both packages' subgroups (review: the monolith renders both at ~935-962; correct wording is "renders the active package's background choices"). Gate via the reactive `themePackage` selector (Mode and Accent already read the active package, so they need no change). Factor the "which background choices are offered for the active package" decision into a tiny pure helper (e.g. `backgroundChoicesForPackage(pkg)` returning `BACKGROUND_ORDER[pkg]` + None) so the package-switch behaviour is unit-testable in the vitest-node env, and consume it from the grid. This is a rendering guard + a pure selector — NO `theme-store` change, NO schema change, NO theme rewrite (D-07 supersedes §D's "more involved than expected" caution). Do not reverse ADR-083/084/087; do not begin the PARKED theme-merge.

Also factor the package-SENSITIVE persistence key into a pure helper `backgroundPatchForPackage(pkg, slot)` (review cycle-2 finding #5) returning `{ galaxyBackground: slot }` for `galaxy` and `{ standardBackground: slot }` for `standard`, mirroring the monolith's `onSelectBackground` at SettingsScreen.tsx:522-531, and consume it from the grid's select handler (still routed through the Task-1 `persist()`). Add a patch-key test to `settings-appearance-background.test.ts` asserting the helper produces the correct key per active package — this is the still-missing half of cycle-1 finding #3 (the choices list alone did not prove the right durable key is written).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-appearance-background.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: the Background section renders only `BACKGROUND_ORDER[themePackage]` + None via the reactive `themePackage` selector (D-07); no edit to `src/stores/theme-store.ts` and no migration file added.
    - test: a package-switch unit test asserts the pure helper returns the Standard slots + None for `standard` and the Galaxy slots + None for `galaxy` (the Galaxy-only choices are not offered under Standard).
    - test (review cycle-2 finding #5): a patch-key unit test asserts `backgroundPatchForPackage("galaxy", slot)` yields `{ galaxyBackground: slot }` and `backgroundPatchForPackage("standard", slot)` yields `{ standardBackground: slot }`; the grid's select handler consumes this helper.
    - behavior (device UAT): with Standard active the Galaxy-only background choices are not offered; switching to Galaxy reveals them live; a background selection persists to the active package's key and survives relaunch.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
  </acceptance_criteria>
  <done>Galaxy-conditional background control ships as a one-line active-package guard (D-07) with a tested per-package patch-key helper (`galaxyBackground`/`standardBackground`), no store/schema/theme change; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (theme control) → theme-store setter + DAO write → SQLite | Theme selections cross here into `app_settings`; the store drives instant restyle, the DAO persists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Appearance preference writes reaching `app_settings` | low | mitigate | Reuse the monolith's `persist()` + existing DAO validators; no new write path, no inline SQL; store setters are the shipped, validated ones. |
| T-37-04 | Tampering (scope-reversal) | ADR-083/084/087 theme model | low | mitigate | D-07 is a rendering guard only; enforce (never reverse) the per-package model; the PARKED theme-merge stays deferred (owner decision). |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsAppearanceScreen.tsx` — Appearance category screen (Theme section this plan; Orbit/self/profile-default added by Plan 03).
- `src/screens/settings-appearance-background.ts` — `backgroundChoicesForPackage(pkg)` + `backgroundPatchForPackage(pkg, slot)` pure helpers + `settings-appearance-background.test.ts` (choices + per-package patch-key tests).
- New `SettingsStackParamList` route name: `SettingsAppearance` (also appended to `SETTINGS_REGISTERED_ROUTES`).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts` all green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06).
- Device UAT: Theme controls restyle live + survive relaunch; Galaxy-only control conditional on active package.
</verification>

<success_criteria>
Appearance › Theme migrated with zero behaviour lost (live restyle + durability), Galaxy-conditional control shipped as the trivial D-07 guard, no schema/format change, PARKED theme-merge untouched.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-02-SUMMARY.md` when done.
</output>
