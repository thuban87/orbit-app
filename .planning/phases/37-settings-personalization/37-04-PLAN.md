---
phase: 37-settings-personalization
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/SettingsContactsScreen.tsx
  - src/screens/settings-contacts-model.ts
  - src/screens/settings-contacts-model.test.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → Contacts & Relationships is a category screen (§E) reached from the hub, sectioned into Contact Sources (device Contacts permission/status, Import, linked-contact reconciliation, phone-region override ordered lower), Relationship Structure (Custom Fields), and Contact Management (Archived) — each row migrated from the monolith preserving its exact navigation/write behaviour."
    - "The Categories IA slot is RESERVED under Relationship Structure per D-03: a stable internal route name `CategoryManagement` exists in `SettingsStackParamList` for the future Category Management phase, but NO functional or inert Categories row is rendered anywhere (§K no-dead-placeholders) and NO `<Stack.Screen>` component is wired for it — Category CRUD stays OUT of Phase 37."
    - "The `categories` table stays read-only — this plan introduces no category create/rename/delete writer (D-03; verified zero runtime CRUD)."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsContactsScreen.tsx
    - src/screens/settings-contacts-model.ts
  key_links:
    - "Hub `Contacts & Relationships` row → SettingsContacts → navigate to CustomFields / Archived / reconcile / import (existing canonical managers, reused not reimplemented)."
    - "CategoryManagement route NAME reserved (types.ts) with no screen and no rendered row (D-03 / §K)."
---

<objective>
Build the **Contacts & Relationships** category (§E) by migrating the contact-methods, contacts-integration, Custom Fields, and Archived rows out of the monolith into a sectioned category screen that reuses the existing canonical managers by navigation. Reserve the Categories IA slot as a route name only (**D-03**), shipping no dead placeholder and no Category CRUD.

Implements **D-03** (reserve the Categories route/IA slot, no CRUD, no dead row). Cites **D-01**.

Purpose: relocate contact-domain admin destinations and lock in the Categories reservation without building the deferred Category Management manager.
Output: SettingsContactsScreen + settings-contacts-model + the `CategoryManagement` route-name reservation; monolith contact groups removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsContactsScreen — Contact Sources + Relationship Structure + Contact Management, migrated</name>
  <files>src/screens/SettingsContactsScreen.tsx, src/screens/settings-contacts-model.ts, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Contact methods group at ~983-1054 (phone region, check-linked-contacts/reconcile, review-flagged), Contacts Integration/Import at ~1056-1083 with `onImportContacts` at 269-284 and `onCheckLinkedContacts` at 286+, the Custom Fields row at ~1930-1943 → `navigation.navigate("CustomFields")`, the Archived row at ~1960-1973 → `navigation.navigate("Archived")`)
    - src/navigation/tabs/SettingsStack.tsx (CustomFields, Archived, ReconcileGrid, import routes are ALREADY registered here — reuse by navigation, do not re-register or reimplement)
    - src/screens/settings-hub-model.ts (insert the `Contacts & Relationships` row at §A index 1)
    - src/screens/AIPermissionsScreen.tsx:1-70 (screen shell)
  </read_first>
  <action>
Create `src/screens/settings-contacts-model.ts`: a frozen section model (§E) — Contact Sources (device Contacts permission/status, Import, reconcile, phone-region override "ordered lower" per §E), Relationship Structure (Custom Fields), Contact Management (Archived) — each entry naming the destination route or the migrated action. Include the phone-region override control's placement lower within Contact Sources.

Create `src/screens/SettingsContactsScreen.tsx` (leaf-shell analog): render the sections from the model, migrating each row's exact behaviour from the monolith — phone-region override write path, `onImportContacts`, `onCheckLinkedContacts` (resumable reconcile), review-flagged, and the `navigation.navigate("CustomFields")` / `navigation.navigate("Archived")` rows. Reuse the canonical managers by navigation — never reimplement. Register `SettingsContacts: undefined;` in types.ts + a `<Stack.Screen>` wrapper, and insert the hub row (§A index 1).

Remove the migrated groups (983-1054, 1056-1083, 1930-1943, 1960-1973) from `src/screens/SettingsScreen.tsx`. Theme tokens + §O a11y labels throughout.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsContactsScreen.tsx` navigates to `CustomFields` and `Archived` (existing routes) and carries the phone-region / import / reconcile / review-flagged behaviours; no manager is reimplemented.
    - source: `SettingsStackParamList` has `SettingsContacts: undefined;`, SettingsStack registers it, and the hub model has the `Contacts & Relationships` row at §A index 1 (hub-model test green).
    - source: the migrated groups are removed from `SettingsScreen.tsx`.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): each Contacts row reaches the same destination/behaviour as the old monolith.
  </acceptance_criteria>
  <done>Contacts & Relationships category ships with all contact-admin rows reused-by-navigation; monolith groups removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Categories IA reservation (D-03) — route name only, NO rendered row, NO CRUD</name>
  <files>src/navigation/types.ts, src/screens/settings-contacts-model.ts</files>
  <read_first>
    - .planning/phases/37-settings-personalization/37-RESEARCH.md ("Categories IA reservation (D-03) — verified read-only": zero runtime CRUD writers; the reservation is route-name + section-order slot with NO visible placeholder)
    - src/navigation/types.ts (add the reserved name to `SettingsStackParamList`)
    - src/screens/settings-contacts-model.ts (the Relationship Structure section where the slot is reserved)
    - src/db/migrations/001-initial.ts (VERIFY `categories` is seeded read-only — 4 rows; no runtime CRUD)
  </read_first>
  <action>
Reserve the Categories IA slot per D-03 / §K: add `CategoryManagement: undefined;` to `SettingsStackParamList` in `src/navigation/types.ts` as a stable internal address for the FUTURE Category Management phase, and hold its section-order position under Relationship Structure in `settings-contacts-model.ts` (documented as reserved / feature-flag-off). Do NOT render any Categories row (functional or inert), do NOT add a `<Stack.Screen>` component for it (no screen exists), and do NOT add any category create/rename/delete writer — the `categories` table stays read-only. This is a route-name + position reservation only; the deletion-cascade manager is a separate future phase.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run src/screens/settings-contacts-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsStackParamList` contains `CategoryManagement: undefined;` and SettingsStack registers NO `<Stack.Screen>` for it.
    - source/behavior: no rendered row anywhere navigates to `CategoryManagement` (the model marks the slot reserved/flag-off); the `settings-contacts-model.test.ts` guard asserts no active row targets `CategoryManagement`.
    - source: no category CRUD writer is added (grep of the change shows no INSERT/UPDATE/DELETE against `categories`).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Categories reservation is a route name + held IA slot with no dead row and no CRUD (D-03 / §K); committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 contacts-model test (section shape + Categories-reservation guard)</name>
  <files>src/screens/settings-contacts-model.test.ts</files>
  <read_first>
    - src/screens/settings-contacts-model.ts
    - src/navigation/types.ts (registered route names)
    - src/screens/settings-hub-model.test.ts (test-shape precedent)
  </read_first>
  <behavior>
    - Every active (rendered) row in `settings-contacts-model` targets a registered `SettingsStackParamList` route.
    - No active row targets `CategoryManagement` — the reservation stays inert (D-03 / §K regression guard).
    - The section order is Contact Sources → Relationship Structure → Contact Management (§E), with the phone-region control ordered lower within Contact Sources.
  </behavior>
  <action>
Create `src/screens/settings-contacts-model.test.ts` asserting the section shape (§E order), that every active row targets a registered route, and — as the key D-03 regression guard — that the reserved `CategoryManagement` slot produces no active/rendered row. This keeps the "no dead Categories placeholder" invariant enforced automatically.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-contacts-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: fails if any active contacts-model row targets an unregistered route, if section order deviates from §E, or if the `CategoryManagement` reservation yields a rendered row.
    - `npx vitest run src/screens/settings-contacts-model.test.ts` green.
  </acceptance_criteria>
  <done>Automated coverage of the Contacts section shape + the Categories-reservation inertness guard; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (phone-region override) → DAO write → SQLite | The one preference write this plan carries; the rest are navigation to existing managers. |
| OS Contacts permission → device state | Contacts permission status is read + surfaced; no new grant path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | phone-region override write to `app_settings` | low | mitigate | Reuse the monolith's validated write path; no inline SQL. |
| T-37-05 | Elevation of privilege (unintended CRUD) | `categories` table | low | mitigate | Reservation adds a route name only; automated guard asserts no rendered Categories row and no category CRUD writer (D-03). |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsContactsScreen.tsx` — Contacts & Relationships category screen.
- `src/screens/settings-contacts-model.ts` — section model (§E) + `settings-contacts-model.test.ts`.
- New `SettingsStackParamList` route names: `SettingsContacts`, and the RESERVED `CategoryManagement` (name only — no screen, no rendered row).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-contacts-model.test.ts src/screens/settings-hub-model.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged; `categories` read-only (no CRUD writer).
- Device UAT: every Contacts row reaches the same destination as before; no Categories row visible.
</verification>

<success_criteria>
Contacts & Relationships category migrated with zero behaviour lost, canonical managers reused-by-navigation, and the Categories IA slot reserved as a route name with no dead placeholder and no CRUD (D-03 / §K).
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-04-SUMMARY.md` when done.
</output>
