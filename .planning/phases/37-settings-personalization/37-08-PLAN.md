---
phase: 37-settings-personalization
plan: 08
type: execute
wave: 8
depends_on: ["37-07"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/settings-routes.ts
  - src/navigation/settings-routes.test.ts
  - src/screens/settings-hub-model.ts
  - src/screens/settings-hub-model.test.ts
  - src/screens/settings-about-model.ts
  - src/screens/SettingsAboutScreen.tsx
  - src/screens/SettingsHubScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → About Orbit (§K) contains ONLY what genuinely has a runtime source, verified on disk (review HIGH): the product display name (a real name — NOT the `app.json` scaffold value `\"orbit-scaffold\"`) and the semantic version (from `expo-constants` `Constants.expoConfig?.version` → `\"1.0.0\"`, with a module-constant fallback; NO new package install). The build number is OMITTED (no `android.versionCode` on disk). Dependency licenses/acknowledgements are OMITTED for now (no runtime source/generator exists and no package is installed this phase; §K 'omit unavailable rows') — flagged to the owner as a possible Phase-40/legal follow-up. Support/feedback and Privacy/Terms rows stay omitted (no real destinations). No dead placeholders."
    - "The Home Screen Widget access is a hub `kind:\"action\"` utility row (the discriminated-union entry type from Plan 01) near the bottom, AFTER the primary category/About hierarchy (§L), reusing the existing `requestPinWidget` path — it is modeled as an action, not a navigation route, and does NOT get a dedicated category."
    - "After this plan the monolith is fully retired: the transitional `SettingsMore` route + its hub row + its `SETTINGS_REGISTERED_ROUTES` entry are removed, `src/screens/SettingsScreen.tsx` is deleted (every user-facing group having migrated to its category across Plans 01–07 — verified against the phase-wide Monolith Migration Matrix in 37-01-PLAN.md, with Plan 08 failing on any unaccounted entry), and the hub renders the full §A top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) plus the widget action row — no dead placeholder anywhere (§A/§K)."
    - "No SQLite migration was added across Phase 37 and `BACKUP_FORMAT_VERSION` is still 5 (D-06 confirmed at phase close): every surfaced preference reused an existing column or the existing `profile` record, so no format-6 bump was needed or performed."
  artifacts:
    - src/screens/SettingsAboutScreen.tsx
    - src/screens/settings-about-model.ts
  key_links:
    - "Hub `About Orbit` route row → SettingsAbout; hub widget `kind:\"action\"` row → requestPinWidget; the deleted monolith no longer mounts at any route (SettingsMore removed from types + SettingsStack + SETTINGS_REGISTERED_ROUTES)."
---

<objective>
Close Phase 37: ship the basic **About Orbit** surface (§K), move the Home Screen Widget access to a hub utility row (§L), and **retire the monolith** — remove the transitional `SettingsMore` route/row and delete `SettingsScreen.tsx` now that every group has migrated — leaving the hub in the full §A order with no dead placeholders. Confirm at phase close that Phase 37 added no schema and no backup-format bump (**D-06**).

Implements dossier **§K** (About basics, omit unavailable rows), **§L** (widget utility row), **§A** (final top-level order, clean root), and confirms **D-06**. Cites **D-01**.

Purpose: finish the navigation-first directory and remove the migration scaffold, leaving Settings as the coherent configuration directory the phase promised.
Output: SettingsAboutScreen (name + version only, from a real source) + the widget action row; the monolith and transitional route deleted; the hub-model test asserting the complete §A order + Monolith Migration Matrix coverage.
</objective>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan:
- **HIGH — About promises content with no runtime source (contradicts §K)**: RESOLVED. Verified on disk: `app.json` name/slug = `"orbit-scaffold"`, version `"1.0.0"`, NO `android.versionCode`; `expo-application`/`expo-constants` are not direct deps; no license asset/generator. Task 1 now: (a) displays a REAL product name via a module constant (`"Orbit"`) rather than the scaffold `app.json` name; (b) reads the semantic version from `expo-constants` `Constants.expoConfig?.version` (a core Expo module already resolvable — no install) with a module-constant fallback; (c) OMITS the build number (no versionCode) and OMITS licenses/acknowledgements (no runtime source; §K omit-unavailable) — no dead placeholders. See the OWNER FLAGS below.
- **MEDIUM — monolith parity largely manual**: RESOLVED — Task 3 verifies every entry in the phase-wide Monolith Migration Matrix (37-01-PLAN.md `<monolith_migration_matrix>`) has migrated and FAILS on any unaccounted control/action/modal/focus-refresh/error-state, backed by the full `npm test` gate.
- **LOW — widget row is not naturally a navigation row**: RESOLVED — the widget row uses the `kind:"action"` arm of the Plan-01 discriminated-union hub model (§L), not a route.
- **Cross-cutting route-registration**: the final hub test asserts registration against `SETTINGS_REGISTERED_ROUTES` + the source-scan test; `SettingsMore` is removed from the constant, the param list, and the stack.

## OWNER FLAGS (surfaced, not silently decided — see return summary)
- **F-1 (product/build identity)**: `app.json` name/slug stay `"orbit-scaffold"`. About displays a product-name CONSTANT (`"Orbit"`) to avoid showing the scaffold name; RENAMING `app.json` name/slug (touches Android package/build identity) is an owner + Phase-40 (release-hardening) decision, NOT taken here.
- **F-2 (OSS license attribution)**: licenses/acknowledgements are omitted this phase (no generator; no install per RESEARCH). If OSS attribution is a real distribution/legal obligation, it needs a dedicated Phase-40 task (license-generation tooling) — owner decision. Neither flag reverses a recorded decision; both defer product/legal calls to the owner.
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsAboutScreen (§K) — real product name + semantic version only, unavailable rows omitted</name>
  <files>src/screens/SettingsAboutScreen.tsx, src/screens/settings-about-model.ts, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/settings-routes.ts, src/screens/settings-hub-model.ts</files>
  <read_first>
    - src/screens/AIPermissionsScreen.tsx:1-70 (leaf-shell container to reuse)
    - app.json (VERIFY on disk: name/slug = "orbit-scaffold", version "1.0.0", NO android.versionCode — so the scaffold name must NOT be displayed and there is no build number to show)
    - node_modules/expo-constants (present via the Expo SDK — `Constants.expoConfig?.version` returns the app.json version "1.0.0"; a core Expo module, no install required)
    - .planning/phases/37-settings-personalization/37-RESEARCH.md (Assumption A3 — but note the review correction: no license generator/asset exists either, so licenses are also omitted, not shipped)
    - src/screens/settings-hub-model.ts (insert the `About Orbit` `kind:"route"` row at §A index 7)
    - src/navigation/settings-routes.ts (append `"SettingsAbout"` to `SETTINGS_REGISTERED_ROUTES`)
  </read_first>
  <action>
Create `src/screens/settings-about-model.ts` exporting the About display data from a REAL source: `ABOUT_APP_NAME = "Orbit"` (a product-name constant — do NOT display `app.json`'s scaffold name `"orbit-scaffold"`; see OWNER FLAG F-1) and a version resolver reading `expo-constants` `Constants.expoConfig?.version` with a module-constant fallback (`"1.0.0"`) — NO new package install. Include ONLY fields with a genuine runtime source.

Create `src/screens/SettingsAboutScreen.tsx` (leaf-shell analog, `useTheme()` colours only) rendering `ABOUT_APP_NAME`, the app icon, and the semantic version from the model. OMIT the build number (no `android.versionCode` exists), OMIT dependency licenses/acknowledgements (no runtime source/generator, no install this phase — §K omit-unavailable; OWNER FLAG F-2), and OMIT support/feedback + Privacy/Terms (no real destinations). Do NOT ship dead placeholders (Phase 40 may add release/legal destinations later).

Register `SettingsAbout: undefined;` in types.ts + the SettingsStack wrapper, append `"SettingsAbout"` to `SETTINGS_REGISTERED_ROUTES`; insert the hub `About Orbit` row (§A index 7). §O a11y labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAboutScreen.tsx` renders the product-name constant (`"Orbit"`, NOT `app.json`'s `"orbit-scaffold"`) + the semantic version from `expo-constants` (fallback constant); it renders NO build-number row, NO licenses row, and NO support/privacy/legal row.
    - source: `SettingsAbout` is in `SETTINGS_REGISTERED_ROUTES` + `SettingsStackParamList`, SettingsStack registers it, hub model has the `About Orbit` row at §A index 7.
    - `npx tsc --noEmit` + `npm run check:colors` pass; hub-model + settings-routes source-scan tests green.
  </acceptance_criteria>
  <done>About Orbit ships a real product name + semantic version only, every unavailable row omitted (§K), no scaffold name shown; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Home Screen Widget utility row on the hub (§L)</name>
  <files>src/screens/settings-hub-model.ts, src/screens/SettingsHubScreen.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Home-screen widget group at ~1876-1928 and `requestPinWidget` at ~647 — this is the LAST user-facing group still in the monolith after Plans 01–07)
    - src/screens/settings-add-widget.ts + src/services/widget/* + src/navigation/widget-linking.ts (§L — the existing widget-pin path to reuse)
    - src/screens/settings-hub-model.ts (add a utility section AFTER the category rows)
    - src/screens/SettingsHubScreen.tsx (render the utility section)
  </read_first>
  <action>
Add the Home Screen Widget access as a hub `kind:"action"` row (the discriminated-union arm defined in Plan 01 — NOT a `kind:"route"` entry) near the bottom, AFTER the category + About hierarchy (§L): add the action entry to the utility section in `settings-hub-model.ts` and render it in `SettingsHubScreen.tsx` so an action row invokes its handler (the existing `requestPinWidget` path via `settings-add-widget.ts`) rather than `navigation.navigate`. Carry the monolith's `addWidgetCopy` fallback behaviour (SettingsScreen.tsx:647 / 1876-1928). It does NOT get a dedicated category. Remove the widget group (1876-1928) from `SettingsScreen.tsx` — after this the monolith holds no user-facing groups.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: the hub renders an "Add Orbit widget" `kind:"action"` row after the category/About rows, invoking `requestPinWidget` (reused, not reimplemented) — not modeled as a navigation route; the widget group is removed from `SettingsScreen.tsx`.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): the widget row still pins the widget from the hub.
  </acceptance_criteria>
  <done>Widget access is a hub utility row (§L); the last monolith group is migrated; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the monolith + transitional route; assert Migration-Matrix coverage + final §A order + D-06 no-schema/format-change</name>
  <files>src/screens/SettingsScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/settings-routes.ts, src/screens/settings-hub-model.ts, src/screens/settings-hub-model.test.ts, src/navigation/settings-routes.test.ts</files>
  <read_first>
    - .planning/phases/37-settings-personalization/37-01-PLAN.md (the `<monolith_migration_matrix>` — the authoritative behavior/error-state inventory; every entry must be accounted for before deletion)
    - src/screens/SettingsScreen.tsx (VERIFY it now holds no user-facing groups; walk the matrix entry-by-entry against the code; identify any shared helper/logic still imported by a category screen so nothing breaks on deletion — tsc catches a stranded import, the matrix catches dropped behavior)
    - src/navigation/tabs/SettingsStack.tsx (the `SettingsMore` registration + the `SettingsScreen` import to remove)
    - src/navigation/types.ts (the `SettingsMore` entry to remove)
    - src/navigation/settings-routes.ts (remove `"SettingsMore"` from `SETTINGS_REGISTERED_ROUTES`)
    - src/screens/settings-hub-model.ts + settings-hub-model.test.ts (remove the transitional row; the test's "SettingsMore present" guard from Plan 01 must be inverted to "SettingsMore absent")
    - src/backup/types.ts (VERIFY `BACKUP_FORMAT_VERSION === 5`) + src/db/database.ts (VERIFY `TARGET_VERSION === 29`)
  </read_first>
  <action>
Before deleting anything, walk the phase-wide Monolith Migration Matrix (37-01-PLAN.md `<monolith_migration_matrix>`) entry by entry against the current `SettingsScreen.tsx` and confirm every group + its listed behaviors/error-states/focus-effects has migrated to its destination category (async refresh, reconcile prompts, permission handling, AI hydration, modals, `addWidgetCopy` fallback). If ANY un-inventoried user-facing group or behavior remains, MIGRATE it to its category rather than dropping it — never lose behaviour (tsc only catches stranded imports, not dropped behavior). Record the matrix walk in the SUMMARY.

Retire the migration scaffold: remove the transitional `More settings` row from `settings-hub-model.ts`, remove `"SettingsMore"` from `SETTINGS_REGISTERED_ROUTES`, remove the `SettingsMore` entry from `SettingsStackParamList`, remove the `<Stack.Screen name="SettingsMore">` registration and the `SettingsScreen` import from `SettingsStack.tsx`, and DELETE `src/screens/SettingsScreen.tsx`. Relocate any helper defined only in the monolith but used by a category screen to a shared module first.

Update `settings-hub-model.test.ts`: invert the Plan-01 transitional-row guard to assert `SettingsMore` is ABSENT (also from `SETTINGS_REGISTERED_ROUTES` via the source-scan test), and assert the COMPLETE §A top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) followed by the widget `kind:"action"` row — with every route row targeting a registered route and no dead/reserved row rendered (the `CategoryManagement` reservation stays inert).

Confirm D-06 at phase close: no migration file was added in Phase 37 (`TARGET_VERSION === 29` unchanged) and `BACKUP_FORMAT_VERSION === 5` unchanged — the existing format/version assertions still pass. Reversibility: reversible (monolith deletion is recoverable via git).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - process: every entry in the 37-01 Monolith Migration Matrix is confirmed migrated (behavior/error-state accounted for), recorded in the SUMMARY; no un-inventoried group is dropped.
    - source: `src/screens/SettingsScreen.tsx` no longer exists; `SettingsMore` is absent from `types.ts`, `SettingsStack.tsx`, and `SETTINGS_REGISTERED_ROUTES`; no import of the deleted monolith remains (tsc clean).
    - test: `settings-hub-model.test.ts` asserts the full §A order + widget action row, `SettingsMore` absent, and no rendered row targets the reserved `CategoryManagement`; `settings-routes.test.ts` still passes (no `SettingsMore`).
    - source: `src/backup/types.ts` `BACKUP_FORMAT_VERSION === 5` and `src/db/database.ts` `TARGET_VERSION === 29` unchanged (no Phase-37 migration) — D-06 confirmed.
    - `npx tsc --noEmit`, `npm run check:colors`, and the full `npm test` suite pass.
  </acceptance_criteria>
  <done>Monolith + transitional route retired with full Migration-Matrix coverage, hub in full §A order, D-06 confirmed (no schema/format change), full suite green; committed.</done>
  <reversibility rating="reversible">Deleting the monolith and the transitional route is pure code removal, recoverable via git; no data/schema/format change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-config version strings → About display | Read-only display of app name/version/licenses; no input crosses back into the app. |
| widget pin request → OS (RemoteViews) | The existing `requestPinWidget` OS handoff, reused unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-08 | DoS (lost behaviour on deletion) | deleting `SettingsScreen.tsx` while a category screen still depends on it | low | mitigate | Confirm no stranded import before deletion (tsc gate); migrate any remaining group rather than drop it; full `npm test` at phase close. |
| T-37-01 | Tampering | About/widget rows | low | accept | About is read-only display; the widget row reuses the existing validated pin path; no new write surface. |

Package legitimacy (T-37-SC): N/A — no package installs across Phase 37. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsAboutScreen.tsx` — About Orbit category screen (name + version only).
- `src/screens/settings-about-model.ts` — `ABOUT_APP_NAME` product-name constant + semantic-version resolver (expo-constants + fallback).
- New `SettingsStackParamList` route name: `SettingsAbout` (also appended to `SETTINGS_REGISTERED_ROUTES`).
- Hub `kind:"action"` widget row in `settings-hub-model.ts` / `SettingsHubScreen.tsx`.
- Removals: transitional `SettingsMore` route + row + `SETTINGS_REGISTERED_ROUTES` entry; deleted `src/screens/SettingsScreen.tsx` (the 2,168-line monolith).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts`, and full `npm test` all green.
- D-06 confirmed at phase close: `TARGET_VERSION === 29` and `BACKUP_FORMAT_VERSION === 5` unchanged — Phase 37 added no migration and no format bump.
- Device UAT (Pixel, full pass per memory verify-ui-on-pixel-yourself): drive Settings → every category (§A order) → Data & Backup from both entry points → About; confirm no behaviour lost vs. the retired monolith and no dead placeholder.
</verification>

<success_criteria>
Settings is the coherent navigation-first directory (§A) with About (§K) and the widget utility row (§L); the monolith and migration scaffold are gone; no schema/format change across the phase (D-06). Phase goal achieved.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-08-SUMMARY.md` when done.
</output>
