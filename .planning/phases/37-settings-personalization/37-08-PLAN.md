---
phase: 37-settings-personalization
plan: 08
type: execute
wave: 8
depends_on: ["37-07"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/settings-hub-model.test.ts
  - src/screens/SettingsAboutScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → About Orbit is a basic About surface (§K) containing only what genuinely exists — app name/icon, semantic version/build number, and dependency licenses/acknowledgements — with unavailable support/privacy/legal rows OMITTED rather than shipped as dead placeholders (§K / Assumption A3)."
    - "The Home Screen Widget access is a utility/action row on the hub near the bottom, AFTER the primary category/About hierarchy (§L), reusing the existing `requestPinWidget` path — it does NOT get a dedicated category."
    - "After this plan the monolith is fully retired: the transitional `SettingsMore` route + its hub row are removed, `src/screens/SettingsScreen.tsx` is deleted (every user-facing group having migrated to its category across Plans 01–07), and the hub renders the full §A top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) plus the widget utility row — no dead placeholder anywhere (§A/§K)."
    - "No SQLite migration was added across Phase 37 and `BACKUP_FORMAT_VERSION` is still 5 (D-06 confirmed at phase close): every surfaced preference reused an existing column or the existing `profile` record, so no format-6 bump was needed or performed."
  artifacts:
    - src/screens/SettingsAboutScreen.tsx
  key_links:
    - "Hub `About Orbit` row → SettingsAbout; hub widget utility row → requestPinWidget; the deleted monolith no longer mounts at any route (SettingsMore removed)."
---

<objective>
Close Phase 37: ship the basic **About Orbit** surface (§K), move the Home Screen Widget access to a hub utility row (§L), and **retire the monolith** — remove the transitional `SettingsMore` route/row and delete `SettingsScreen.tsx` now that every group has migrated — leaving the hub in the full §A order with no dead placeholders. Confirm at phase close that Phase 37 added no schema and no backup-format bump (**D-06**).

Implements dossier **§K** (About basics, omit unavailable rows), **§L** (widget utility row), **§A** (final top-level order, clean root), and confirms **D-06**. Cites **D-01**.

Purpose: finish the navigation-first directory and remove the migration scaffold, leaving Settings as the coherent configuration directory the phase promised.
Output: SettingsAboutScreen + the widget utility row; the monolith and transitional route deleted; the hub-model test asserting the complete §A order.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsAboutScreen (§K) — app name/version/licenses only, no dead rows</name>
  <files>src/screens/SettingsAboutScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts</files>
  <read_first>
    - src/screens/AIPermissionsScreen.tsx:1-70 (leaf-shell container to reuse)
    - app.json / app.config.ts (the source of the app name + semantic version/build number to display)
    - .planning/phases/37-settings-personalization/37-RESEARCH.md (Assumption A3: no real support/privacy/legal destinations exist yet → ship app name/version/licenses only, omit dead rows)
    - src/screens/settings-hub-model.ts (insert the `About Orbit` row at §A index 7)
  </read_first>
  <action>
Create `src/screens/SettingsAboutScreen.tsx` (leaf-shell analog, `useTheme()` colours only): app name/icon, semantic version + build number (read from the app config, not hardcoded), and dependency licenses/acknowledgements. OMIT support/feedback and Privacy Policy/Terms rows — no real destinations exist yet (§K / Assumption A3); do NOT ship dead placeholders (Phase 40 may add release/legal destinations later). Register `SettingsAbout: undefined;` in types.ts + the SettingsStack wrapper; insert the hub `About Orbit` row (§A index 7). §O a11y labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAboutScreen.tsx` renders app name, version/build (from app config), and licenses; it renders NO support/privacy/legal row.
    - source: `SettingsStackParamList` has `SettingsAbout`, SettingsStack registers it, hub model has the `About Orbit` row at §A index 7.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
  </acceptance_criteria>
  <done>About Orbit ships real info only, no dead rows (§K); committed.</done>
</task>

<task type="auto">
  <name>Task 2: Home Screen Widget utility row on the hub (§L)</name>
  <files>src/screens/settings-hub-model.ts, src/screens/SettingsHubScreen.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Home-screen widget group at ~1876-1928 and `requestPinWidget` at ~647 — this is the LAST user-facing group still in the monolith after Plans 01–07)
    - src/screens/settings-add-widget.ts + src/services/widget/* + src/navigation/widget-linking.ts (§L — the existing widget-pin path to reuse)
    - src/screens/settings-hub-model.ts (add a utility section AFTER the category rows)
    - src/screens/SettingsHubScreen.tsx (render the utility section)
  </read_first>
  <action>
Add the Home Screen Widget access as a hub utility/action row near the bottom, AFTER the category + About hierarchy (§L) — a utility section in `settings-hub-model.ts` and its render in `SettingsHubScreen.tsx`, invoking the existing `requestPinWidget` path (reuse `settings-add-widget.ts`; do not reimplement). It does NOT get a dedicated category. Remove the widget group (1876-1928) from `SettingsScreen.tsx` — after this the monolith holds no user-facing groups.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: the hub renders an "Add Orbit widget" utility row after the category/About rows, invoking `requestPinWidget` (reused, not reimplemented); the widget group is removed from `SettingsScreen.tsx`.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): the widget row still pins the widget from the hub.
  </acceptance_criteria>
  <done>Widget access is a hub utility row (§L); the last monolith group is migrated; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the monolith + transitional route; assert final §A order + D-06 no-schema/format-change</name>
  <files>src/screens/SettingsScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/settings-hub-model.test.ts</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (VERIFY it now holds no user-facing groups; identify any shared helper/logic still imported by a category screen so nothing breaks on deletion — tsc will catch a stranded import)
    - src/navigation/tabs/SettingsStack.tsx (the `SettingsMore` registration + the `SettingsScreen` import to remove)
    - src/navigation/types.ts (the `SettingsMore` entry to remove)
    - src/screens/settings-hub-model.ts + settings-hub-model.test.ts (remove the transitional row; the test's "SettingsMore present" guard from Plan 01 must be inverted to "SettingsMore absent")
    - src/backup/types.ts (VERIFY `BACKUP_FORMAT_VERSION === 5`) + src/db/database.ts (VERIFY `TARGET_VERSION === 29`)
  </read_first>
  <action>
Retire the migration scaffold: remove the transitional `More settings` row from `settings-hub-model.ts`, remove the `SettingsMore` entry from `SettingsStackParamList`, remove the `<Stack.Screen name="SettingsMore">` registration and the `SettingsScreen` import from `SettingsStack.tsx`, and DELETE `src/screens/SettingsScreen.tsx`. Before deleting, confirm no category screen depends on a helper defined only in the monolith (relocate any such shared helper to a shared module; if an un-inventoried user-facing group somehow remains, migrate it to its category rather than dropping it — never lose behaviour).

Update `settings-hub-model.test.ts`: invert the Plan-01 transitional-row guard to assert `SettingsMore` is ABSENT, and assert the COMPLETE §A top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) followed by the widget utility row — with every row targeting a registered route and no dead/reserved row rendered (the `CategoryManagement` reservation stays inert).

Confirm D-06 at phase close: no migration file was added in Phase 37 (`TARGET_VERSION === 29` unchanged) and `BACKUP_FORMAT_VERSION === 5` unchanged — the existing format/version assertions still pass. Reversibility: reversible (monolith deletion is recoverable via git).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/screens/SettingsScreen.tsx` no longer exists; `SettingsMore` is absent from `types.ts` and `SettingsStack.tsx`; no import of the deleted monolith remains (tsc clean).
    - test: `settings-hub-model.test.ts` asserts the full §A order + widget utility row, `SettingsMore` absent, and no rendered row targets the reserved `CategoryManagement`.
    - source: `src/backup/types.ts` `BACKUP_FORMAT_VERSION === 5` and `src/db/database.ts` `TARGET_VERSION === 29` unchanged (no Phase-37 migration) — D-06 confirmed.
    - `npx tsc --noEmit`, `npm run check:colors`, and the full `npm test` suite pass.
  </acceptance_criteria>
  <done>Monolith + transitional route retired, hub in full §A order, D-06 confirmed (no schema/format change), full suite green; committed.</done>
  <reversibility rating="reversible">Deleting the monolith and the transitional route is pure code removal, recoverable via git; no data/schema/format change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-config version strings → About display | Read-only display of app name/version/licenses; no input crosses back into the app. |
| widget pin request → OS (RemoteViews) | The existing `requestPinWidget` OS handoff, reused unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-08 | DoS (lost behaviour on deletion) | deleting `SettingsScreen.tsx` while a category screen still depends on it | low | mitigate | Confirm no stranded import before deletion (tsc gate); migrate any remaining group rather than drop it; full `npm test` at phase close. |
| T-37-01 | Tampering | About/widget rows | low | accept | About is read-only display; the widget row reuses the existing validated pin path; no new write surface. |

Package legitimacy (T-37-SC): N/A — no package installs across Phase 37. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsAboutScreen.tsx` — About Orbit category screen.
- New `SettingsStackParamList` route name: `SettingsAbout`.
- Hub utility section (widget row) in `settings-hub-model.ts` / `SettingsHubScreen.tsx`.
- Removals: transitional `SettingsMore` route + row; deleted `src/screens/SettingsScreen.tsx` (the 2,168-line monolith).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts`, and full `npm test` all green.
- D-06 confirmed at phase close: `TARGET_VERSION === 29` and `BACKUP_FORMAT_VERSION === 5` unchanged — Phase 37 added no migration and no format bump.
- Device UAT (Pixel, full pass per memory verify-ui-on-pixel-yourself): drive Settings → every category (§A order) → Data & Backup from both entry points → About; confirm no behaviour lost vs. the retired monolith and no dead placeholder.
</verification>

<success_criteria>
Settings is the coherent navigation-first directory (§A) with About (§K) and the widget utility row (§L); the monolith and migration scaffold are gone; no schema/format change across the phase (D-06). Phase goal achieved.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-08-SUMMARY.md` when done.
</output>
