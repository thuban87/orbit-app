---
phase: 37-settings-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/SettingsHubScreen.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/settings-hub-model.test.ts
  - src/screens/SettingsInteractionsScreen.tsx
  - src/screens/settings-interactions-logic.ts
  - src/screens/settings-interactions-logic.test.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # Phase 37 carries NO REQ-IDs (deferred-planning phase; success derived from GOAL + D-01..D-09 + dossier §A–§S). Spec-less probe fallback SKIPPED (recorded).
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low          # calibration sample_count=0, factor=1

must_haves:
  truths:
    - "Opening the Settings tab shows a navigation-first directory (icon + title + subtitle rows, NO live setting values per §A/D-09), mounted at the preserved `Settings` route name (§M)."
    - "Every Settings control that has not yet been migrated into a category screen remains reachable during the migration (nothing is lost) via a functional transitional row."
    - "The Interactions category screen surfaces the Compose default message mode (D-04a, `default_message_mode`) and the Dashboard right-swipe action (D-04c, `dashboard_right_swipe_action`) plus the migrated Interaction Assist and Default Interaction Channel controls, each writing its existing `app_settings` column through the DAO."
    - "A preference toggled in the Interactions screen round-trips: write via `updateAppSettings` → `getAppSettings` returns the new value (no schema change, D-06)."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06); the tracer proves the hub+category+DAO architecture end-to-end before the remaining 13 row-groups are split (D-09)."
  artifacts:
    - src/screens/SettingsHubScreen.tsx
    - src/screens/settings-hub-model.ts
    - src/screens/SettingsInteractionsScreen.tsx
    - src/screens/settings-interactions-logic.ts
  key_links:
    - "SettingsStack `Settings` route now mounts SettingsHubScreen; the monolith is re-registered at the transitional internal route `SettingsMore` so the hub can front un-migrated controls until Plan 08 retires it."
    - "Hub row → navigation.navigate('SettingsInteractions') → SettingsInteractionsScreen → updateAppSettings → app_settings (id=1)."
---

<objective>
TRACER for Phase 37. Prove the whole Settings decomposition architecture end-to-end on ONE real vertical slice before splitting the remaining row-groups: a navigation-first hub screen mounted at the preserved `Settings` route name, ONE category sub-route (Interactions) carrying real `app_settings` writes, and a transitional route that keeps every not-yet-migrated control reachable so the app stays whole at every commit.

This directly implements **D-09** (decompose the 2,168-line monolith into a hub + category routes — the phase's core structural work), **D-04a** (surface `default_message_mode`) and **D-04c** (surface `dashboard_right_swipe_action`), and honors **D-01** (dossier §A/§F/§M are ground truth: navigation-first directory, Interactions owns interaction defaults, stable internal routes, preserve the shell/back-stack).

Purpose: de-risk the architecture (hub-at-`Settings` + category-route + DAO write) on the agent's best early-context tokens, so an architectural dead-end surfaces after one commit instead of after ten migrated categories.
Output: SettingsHubScreen + settings-hub-model + SettingsInteractionsScreen + settings-interactions-logic, the transitional `SettingsMore` registration, and Wave-0 tests for the hub model and interactions options.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@docs/dossier/milestone-2/phase-37-settings-personalization-dossier.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Hub at `Settings` + transitional `SettingsMore` + one navigable Interactions route (thin end-to-end slice)</name>
  <files>src/screens/SettingsHubScreen.tsx, src/screens/settings-hub-model.ts, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (the 2,168-line monolith — READ IN FULL per CLAUDE.md "review the code, not the diff": the render tree starts at the `testID="settings-screen"` ScrollView ~line 671, `ShellAppBar variant="root" title="Settings"`, the `__DEV__` theme-preview row, and the `persist()` write path at 501–520)
    - src/navigation/tabs/SettingsStack.tsx (the ONLY sub-route precedent: `*Route` wrappers passing `onBack={() => navigation.goBack()}`, and the additive `<Stack.Screen>` block under `initialRouteName="Settings"` / `headerShown:false`)
    - src/navigation/types.ts:229-300 (SettingsStackParamList — additive `RouteName: undefined` entries; RootStackParamList is a TYPE intersection, types.ts:314-317)
    - src/screens/AIPermissionsScreen.tsx:1-70 (leaf-screen shell: `useTheme()`, `@/components/ui` primitives, module-level LOG_SCOPE, `onBack` prop; the screen does NOT register its own route)
    - src/screens/settings-ai-hub-logic.ts (the hub-model logic-module precedent: a frozen `ReadonlyArray` of `{ label, route, params }` + pure functions, unit-testable in vitest)
  </read_first>
  <action>
Create `src/screens/settings-hub-model.ts` mirroring the shape of `settings-ai-hub-logic.ts`: export a frozen `SETTINGS_HUB_ROWS` array of `{ key, title, subtitle, icon, route }` entries authored in the §A canonical top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) plus a bottom utility section — but include ONLY the rows whose destination screen exists after this plan: the real `Interactions` row (route `SettingsInteractions`) and a transitional `More settings` row (route `SettingsMore`). Later plans insert their category rows at the correct §A index. Per §A the rows carry NO live setting values (title + subtitle only). Per §K do NOT emit any row whose screen does not exist yet.

Create `src/screens/SettingsHubScreen.tsx` as a navigation-first directory (leaf-shell analog): `useTheme()` for every colour (no hardcoded colours), a `ScrollView` with `ShellAppBar variant="root" title="Settings"`, mapping `SETTINGS_HUB_ROWS` to `Pressable` rows that `navigation.navigate(row.route)`, each with an `accessibilityRole="button"` and an `accessibilityLabel` (§O). Keep the `__DEV__` theme-preview row target for now (it moves to Appearance in Plan 02). Type navigation via `useNavigation<NativeStackNavigationProp<RootStackParamList>>()` exactly as the monolith does (SettingsScreen.tsx:240-241).

In `src/navigation/tabs/SettingsStack.tsx`: change the `Settings` `<Stack.Screen>` to `component={SettingsHubScreen}` (PRESERVE the `Settings` route name per §M — deep-link + back-stack safe), and ADD a `<Stack.Screen name="SettingsMore" component={SettingsScreen} />` re-registering the untouched monolith at the transitional internal route. In `src/navigation/types.ts` add `SettingsMore: undefined;` and `SettingsInteractions: undefined;` to `SettingsStackParamList`. Register `SettingsInteractions` in SettingsStack with a `SettingsInteractionsRoute` wrapper (Task 2 supplies the screen — for this tracer slice the screen may surface only `default_message_mode`; Task 2 expands it).

The transitional `More settings` hub row is a FUNCTIONAL row (it opens the real, fully-working monolith) — not a dead placeholder; Plan 08 removes it once every group has migrated. Reversibility: reversible (pure nav/code under git).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/navigation/tabs/SettingsStack.tsx` mounts `component={SettingsHubScreen}` for `<Stack.Screen name="Settings" ...>` and registers a `<Stack.Screen name="SettingsMore" component={SettingsScreen} />`.
    - source: `src/navigation/types.ts` `SettingsStackParamList` contains `SettingsMore: undefined;` and `SettingsInteractions: undefined;`.
    - source: `src/screens/settings-hub-model.ts` exports a frozen array whose only rows target `SettingsInteractions` and `SettingsMore` (no row for an unbuilt category, §K).
    - behavior (device UAT): opening the Settings tab shows the hub directory; the transitional row opens the full original monolith (nothing lost); the Interactions row opens the new category screen.
    - `npx tsc --noEmit` passes (route registration type-checks) and `npm run check:colors` passes (no hardcoded colours in the new hub).
  </acceptance_criteria>
  <done>The hub renders at `Settings`, the monolith is reachable via `SettingsMore`, and `SettingsInteractions` navigates — proven end-to-end (tsc + colours green; device-UAT nav confirmed), committed.</done>
  <reversibility rating="reversible">Route rewiring and a transitional route are pure code under version control; no data or schema change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interactions category — surface message-mode (D-04a) + right-swipe (D-04c), migrate Interaction Assist + Default Channel (§F)</name>
  <files>src/screens/SettingsInteractionsScreen.tsx, src/screens/settings-interactions-logic.ts, src/screens/settings-interactions-logic.test.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Interaction Assist toggle group at ~1531-1565 that migrates here, and the `persist()` write path at 501-520 to carry verbatim)
    - src/db/app-settings-dao.ts (the `AppSettings` interface + `AppSettingsPatch`; the validators `assertMessageMode` (~200-209) and `MESSAGE_MODES` (~185); `dashboardRightSwipeAction`/`RIGHT_SWIPE_ACTIONS` (~309, imported from `@/logic/dashboard-query-logic`); `defaultInteractionChannel`; `updateAppSettings`/`getAppSettings`)
    - src/screens/settings-ai-hub-logic.ts (options-model precedent for the testable logic module)
    - src/screens/AIPermissionsScreen.tsx:1-70 (screen shell + `onBack` prop)
  </read_first>
  <behavior>
    - settings-interactions-logic: message-mode option set equals `MESSAGE_MODES` (`remember` / `text` / `email`) with human labels; right-swipe option set equals `RIGHT_SWIPE_ACTIONS`; each option maps to the exact `AppSettingsPatch` key it writes. Test: building a patch for a chosen message mode yields `{ defaultMessageMode: <mode> }` and is accepted by `assertMessageMode`.
    - settings-interactions-logic: default-interaction-channel option set matches the existing ordinary-channel contract (Remember Last Choice / Message / Call / In Person; Group Log exempt per §F).
    - Round-trip: writing `defaultMessageMode` and `dashboardRightSwipeAction` via `updateAppSettings` then `getAppSettings` returns the written values (existing DAO test suite covers the DAO; add the options-model unit test).
  </behavior>
  <action>
Create `src/screens/settings-interactions-logic.ts`: pure, frozen option models for `default_message_mode` (D-04a), `dashboard_right_swipe_action` (D-04c), and `default_interaction_channel` (§F / RESEARCH A1 — persisted but not currently rendered), each entry pairing a display label to the `AppSettingsPatch` it produces. Reuse the DAO's exported enums (`MESSAGE_MODES`, `RIGHT_SWIPE_ACTIONS`) — do not re-declare literals; the DB CHECK + `assertMessageMode` remain the source of truth.

Create `src/screens/SettingsInteractionsScreen.tsx` (leaf-shell analog, `onBack` prop, `useTheme()` colours only): an inline segmented/chip selector (2-4 options → inline per §B) for message mode, right-swipe action, and default channel, plus the migrated Interaction Assist toggle. Every write goes through a local `persist()` copied verbatim from SettingsScreen.tsx:501-520 (`updateAppSettings(exec, patch, localDateTime())` → re-read `getAppSettings` → the two idempotent reconcilers) — never inline SQL. Wire the `SettingsInteractionsRoute` wrapper registered in Task 1.

In `src/screens/SettingsScreen.tsx`, remove the Interaction Assist inline group (~1531-1565) now that it lives in the Interactions category (no duplicate write surface). Interaction Assist is the only group this plan migrates out of the monolith; all other groups stay in `SettingsMore` until their plans.

Category placement follows D-04 planner guidance: (a) message mode and (c) right-swipe belong in Interactions.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-interactions-logic.test.ts</automated>
    <automated>npx vitest run src/db/app-settings-dao.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/screens/SettingsInteractionsScreen.tsx` imports `updateAppSettings`/`getAppSettings` and writes via a `persist()` mirroring SettingsScreen.tsx:501-520 (no inline SQL).
    - test: `settings-interactions-logic.test.ts` asserts the message-mode options equal `MESSAGE_MODES` and each builds the correct `{ defaultMessageMode }` patch; right-swipe options equal `RIGHT_SWIPE_ACTIONS`.
    - test: the app-settings-dao suite still passes (round-trip of `defaultMessageMode` + `dashboardRightSwipeAction` unchanged; D-06 no schema change).
    - source: the Interaction Assist group is rendered by `SettingsInteractionsScreen` and no longer in `SettingsScreen.tsx`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Interactions category surfaces message mode (D-04a), right-swipe (D-04c), default channel, and Interaction Assist; options-model + DAO tests green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 hub-model test (route targets + §K no-orphan-row guard)</name>
  <files>src/screens/settings-hub-model.test.ts</files>
  <read_first>
    - src/screens/settings-hub-model.ts (the array authored in Task 1)
    - src/navigation/types.ts:229-300 (the registered `SettingsStackParamList` route names to assert every hub row targets a real route)
    - src/screens/settings-ai-hub-logic.ts (test-shape precedent)
  </read_first>
  <behavior>
    - Every `route` in `SETTINGS_HUB_ROWS` is a key of `SettingsStackParamList` (no row points at an unregistered route).
    - The array preserves §A canonical order for whatever category rows are present (the ordering invariant the later plans insert into).
    - The transitional `SettingsMore` row is present now and is the last non-utility row (documents the migration scaffold that Plan 08 removes).
  </behavior>
  <action>
Create `src/screens/settings-hub-model.test.ts`: assert every hub row's `route` is a registered `SettingsStackParamList` name (import the type / a route-name allowlist), assert the §A relative ordering of present category rows, and assert the transitional `SettingsMore` row exists (a regression guard so a future plan does not accidentally strand un-migrated controls before Plan 08 removes it). This is the Wave-0 scaffold covering the RESEARCH "screen/logic tests for the hub navigation" gap; on-device rendering stays a device-UAT step (VALIDATION Manual-Only).
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: `settings-hub-model.test.ts` fails if any hub row targets a route not in `SettingsStackParamList`.
    - test: asserts §A ordering of present rows and the presence of the transitional `SettingsMore` row.
    - `npx vitest run src/screens/settings-hub-model.test.ts` green; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Hub-model has automated coverage of route validity + ordering; the Wave-0 hub-nav gap is closed; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (Settings control) → DAO write → SQLite | The only untrusted input this plan adds; every preference value crosses here into `app_settings`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering / DoS | Interactions preference writes reaching the scheduler/DB | low | mitigate | Route every write through `updateAppSettings`; reuse the existing DAO validators (`assertMessageMode`, `RIGHT_SWIPE_ACTIONS` `.includes()`, channel enum) — no new unvalidated write path, no inline SQL. |
| T-37-03 | Information disclosure | AI egress via a Settings toggle | low | accept | Out of this plan's scope; Interactions touches no AI egress. `AiService.ts` untouched (do not widen). |

Package legitimacy (T-37-SC): N/A — this phase installs no npm/pip/cargo packages (RESEARCH Standard Stack: "Installation: none").
ASVS L1, block-on high. Local-first, on-device SQLite; no new network egress on any read path.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols — excluded from source-grounding drift checks)
- `src/screens/SettingsHubScreen.tsx` — navigation-first Settings directory (mounts at the preserved `Settings` route name).
- `src/screens/settings-hub-model.ts` — `SETTINGS_HUB_ROWS` frozen row model (§A order) + `settings-hub-model.test.ts`.
- `src/screens/SettingsInteractionsScreen.tsx` — Interactions category screen.
- `src/screens/settings-interactions-logic.ts` — option models for message mode / right-swipe / default channel + `settings-interactions-logic.test.ts`.
- New `SettingsStackParamList` route names: `SettingsInteractions`, and the transitional `SettingsMore` (monolith re-registration; removed in Plan 08).
</artifacts>

<verification>
- `npx tsc --noEmit` clean, `npm run check:colors` clean, `npx vitest run src/screens/settings-hub-model.test.ts src/screens/settings-interactions-logic.test.ts src/db/app-settings-dao.test.ts` green.
- No migration added (head stays `029`, `TARGET_VERSION === 29`); `BACKUP_FORMAT_VERSION === 5` unchanged (D-06) — existing format-version assertions still pass.
- Device UAT (Pixel, per memory verify-ui-on-pixel-yourself): Settings tab → hub renders; Interactions row → message-mode/right-swipe/channel/assist all persist; transitional row still reaches every original control.
</verification>

<success_criteria>
Hub-at-`Settings` + one category route + a real DAO write proven end-to-end (D-09, D-04a, D-04c), the app stays whole via the transitional route, no schema/format change (D-06), and the architecture is de-risked before the remaining 13 row-groups split.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-01-SUMMARY.md` when done.
</output>
