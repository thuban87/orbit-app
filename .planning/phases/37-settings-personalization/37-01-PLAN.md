---
phase: 37-settings-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/settings-routes.ts
  - src/navigation/settings-routes.test.ts
  - src/screens/SettingsHubScreen.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/settings-hub-model.test.ts
  - src/screens/SettingsInteractionsScreen.tsx
  - src/screens/settings-interactions-logic.ts
  - src/screens/settings-interactions-logic.test.ts
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # Phase 37 carries NO REQ-IDs (deferred-planning phase; success derived from GOAL + D-01..D-09 + dossier §A–§S). Spec-less probe fallback SKIPPED (recorded).
user_setup: []
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low          # calibration sample_count=0, factor=1

must_haves:
  truths:
    - "Opening the Settings tab shows a navigation-first directory (icon + title + subtitle rows, NO live setting values per §A/D-09), mounted at the preserved `Settings` route name (§M)."
    - "Every Settings control that has not yet been migrated into a category screen remains reachable during the migration (nothing is lost) via a functional transitional row."
    - "The Interactions category screen surfaces the Compose default message mode (D-04a, `default_message_mode`), the Dashboard right-swipe action (D-04c, `dashboard_right_swipe_action`), and the Default Interaction Channel — each writing its existing `app_settings` column through the generic `updateAppSettings`/`persist()` path (they have no specialized writer) — plus the migrated Interaction Assist toggle, which writes through its CANONICAL specialized writer (see the ADR-070 truth below), NOT the generic path."
    - "The migrated Interaction Assist toggle PRESERVES ADR-070 / D-10 ('off means off, clear at once' — Accepted, one-way; leaving pending assists to keep prompting is a REJECTED alternative): it writes through `setInteractionAssistEnabled(exec, on ? 1 : 0, localDateTime())` (`src/db/app-settings-dao.ts:1443`), whose single transaction expires every `pending` `interaction_assists` row on opt-out, then re-reads via `getAppSettings` and calls `await useAssistBanner.getState().refresh()` (`@/stores/assist-store`) — mirroring the shipped monolith at `SettingsScreen.tsx:596-606`. It NEVER routes through the generic `persist()`/`updateAppSettings` path. This is decision ENFORCEMENT (planner bucket), not a reversal."
    - "A preference toggled in the Interactions screen round-trips: message mode / right-swipe / default channel write via `updateAppSettings` → `getAppSettings` returns the new value; Interaction Assist opt-off additionally expires all `pending` assists via `setInteractionAssistEnabled` and refreshes the banner store (no schema change, D-06)."
    - "Route registration is proven at runtime, not merely at the type level (review cross-cutting MEDIUM): a runtime `SETTINGS_REGISTERED_ROUTES` constant is the single source both `SettingsStack` and the tests read, and a source-scan test asserts every registered route has a real `<Stack.Screen>` in `SettingsStack.tsx` and that the deliberately typed-but-unregistered `CategoryManagement` (D-03) is NOT registered — closing the erased-`SettingsStackParamList` false positive."
    - "The tracer proves the hub+category+DAO architecture end-to-end (hub → navigate → SettingsInteractions → DAO round-trip) on ONE atomic vertical slice, independently green, before the remaining 13 row-groups are split (D-09). No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsHubScreen.tsx
    - src/screens/settings-hub-model.ts
    - src/navigation/settings-routes.ts
    - src/screens/SettingsInteractionsScreen.tsx
    - src/screens/settings-interactions-logic.ts
  key_links:
    - "SettingsStack `Settings` route now mounts SettingsHubScreen; the monolith is re-registered at the transitional internal route `SettingsMore` so the hub can front un-migrated controls until Plan 08 retires it."
    - "Hub row → navigation.navigate('SettingsInteractions') → SettingsInteractionsScreen → updateAppSettings → app_settings (id=1)."
    - "SETTINGS_REGISTERED_ROUTES (runtime) ← SettingsStack registrations + hub-model route rows both reference it; the source-scan test binds it to the actual `<Stack.Screen>` list on disk."
---

<objective>
TRACER for Phase 37. Prove the whole Settings decomposition architecture end-to-end on ONE atomic, independently-green vertical slice before splitting the remaining row-groups: a navigation-first hub screen mounted at the preserved `Settings` route name, ONE fully-wired category sub-route (Interactions) carrying a real `app_settings` write that round-trips through the DAO, and a transitional route that keeps every not-yet-migrated control reachable so the app stays whole at every commit. The tracer also lands the phase-wide route-registration contract (`SETTINGS_REGISTERED_ROUTES` + source-scan test) that every later plan extends.

This directly implements **D-09** (decompose the 2,168-line monolith into a hub + category routes — the phase's core structural work), **D-04a** (surface `default_message_mode`) and **D-04c** (surface `dashboard_right_swipe_action`), enforces **D-10 / ADR-070** (the migrated Interaction Assist toggle writes through the canonical `setInteractionAssistEnabled` + banner refresh, preserving the atomic "off means off" pending-queue clear — never the generic `persist()` path), and honors **D-01** (dossier §A/§F/§M are ground truth: navigation-first directory, Interactions owns interaction defaults, stable internal routes, preserve the shell/back-stack).

Purpose: de-risk the architecture (hub-at-`Settings` + category-route + DAO write + runtime-provable route registration) on the agent's best early-context tokens, so an architectural dead-end surfaces after one commit instead of after ten migrated categories.
Output: SettingsHubScreen + settings-hub-model (discriminated-union entries) + SettingsInteractionsScreen (a minimal-but-real slice) + settings-interactions-logic, the transitional `SettingsMore` registration, the `SETTINGS_REGISTERED_ROUTES` runtime contract + source-scan test, and Wave-0 tests for the hub model and interactions options.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-REVIEWS.md
@docs/dossier/milestone-2/phase-37-settings-personalization-dossier.md
</context>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan:
- **Cross-cutting MEDIUM — route-registration tests can't prove runtime registration** (Plans 01/04/08): RESOLVED here by introducing the runtime `SETTINGS_REGISTERED_ROUTES` constant + a source-scan test that reads `SettingsStack.tsx` on disk and asserts each registered route resolves to a real `<Stack.Screen>` and that `CategoryManagement` is NOT registered (the D-03 false-positive case). Plans 04 and 08 extend this same contract.
- **Plan 01 tracer atomicity** (Codex+Claude MEDIUM): RESOLVED — Task 1 now ships a minimal-but-real `SettingsInteractionsScreen` (message-mode write + DAO round-trip) wired end-to-end, so the tracer is independently green; Task 2 expands it. Task 1 no longer registers a route whose screen only arrives in Task 2.
- **Hub entry discriminated union** (Claude/Codex LOW, §L): RESOLVED — `settings-hub-model` entries are a discriminated union `{ kind: "route" } | { kind: "action" }` from the outset, so the widget utility row (Plan 08) is not mis-modeled as a navigation route.
- **Phase-wide monolith migration matrix** (Codex+Claude MEDIUM): the matrix lives in this plan (see `<monolith_migration_matrix>`) so it exists before decomposition begins; Plan 08 fails if any entry is unaccounted.
- **Device-UAT prerequisite** (Codex+Claude): a `<precondition>` requires confirming the Orbit app package name + `emu-connect` target with the owner before any device command (CLAUDE.md Android section is quest-board-derived).

Cross-AI review (37-REVIEWS.md, cycle 2) findings owned by this plan:
- **HIGH (OWNER-ESCALATION) — Plan 01 reversed accepted, one-way ADR-070 by name** (codex, orchestrator-confirmed on disk): the cycle-1 replan routed the migrated Interaction Assist toggle through the generic `persist()`/`updateAppSettings` path, dropping the atomic pending-assist expiry and the banner refresh — reintroducing ADR-070's explicitly REJECTED "leave already-pending assists prompting" alternative. RESOLVED here as decision ENFORCEMENT (planner bucket, NOT a reversal): the migration matrix row + Task 2 now specify the canonical `setInteractionAssistEnabled(exec, on ? 1 : 0, localDateTime())` writer (`src/db/app-settings-dao.ts:1443`) followed by a `getAppSettings` re-read and `await useAssistBanner.getState().refresh()` (`@/stores/assist-store`), mirroring `SettingsScreen.tsx:596-606` verbatim; the generic path is explicitly forbidden for this one toggle. The DAO's queue-clear-on-opt-out is already proven by `src/db/app-settings-dao.test.ts` (~645-675: seed pending → opt-off expires all → opt-on does not resurrect); Task 2 adds a handler-level test proving the migrated toggle routes through the specialized writer + banner refresh, not the generic persist. D-10 is cited across the objective, must_haves.truths, Task 2 read_first/behavior/action/acceptance_criteria so it survives Plan 08's migration-matrix gate. (Message mode, right-swipe, and default channel legitimately keep `updateAppSettings`/`persist()` — they have no specialized writer; only the assist toggle was over-corrected and is fixed here.)
</review_feedback_addressed>

<monolith_migration_matrix>
## Phase-wide monolith migration matrix (authoritative behavior/error-state inventory)

Every user-facing group + its load-bearing behaviors/error-states in `src/screens/SettingsScreen.tsx`, mapped to the destination category and owning plan. Plan 08 Task 3 asserts every row here has migrated (fail on any unaccounted entry — `tsc` catches stranded imports, this catches dropped behavior).

| Monolith group (source lines) | Behaviors / error-states / focus effects to carry | Destination category | Owning plan |
|---|---|---|---|
| `__DEV__` theme-preview row (679–694) | `__DEV__`-gated; targets `__ThemePreview` | Appearance (dev-only) | 02 |
| Appearance: Theme/Mode/Accent/Background (701–981) | live `theme-store` setters + `persist()`; `activeBackground`/`selectedBackgroundSlot` derivation (263–267); `onSelectBackground` (522–532); **failed-write honesty** (persist catches/logs) | Appearance › Theme | 02 |
| Contact methods: phone region, check-linked (reconcile), review flagged (983–1054) | phone-region override write; `onCheckLinkedContacts` resumable-reconcile prompt (286+); review-flagged nav | Contacts › Contact Sources | 04 |
| Contacts Integration: Import (1056–1083) | `onImportContacts` (269–284) incl. effective phone region + import-error Alert | Contacts › Contact Sources | 04 |
| **Contacts permission/status** (read where surfaced) | fresh-on-focus READ_CONTACTS status + Open-system-settings handoff (reuse `use-read-contacts-permission.ts`) | Contacts › Contact Sources | 04 |
| Notifications: 10 controls (1173–1529) | `persist()` → `reconcileSchedule` + `reconcileDigestSchedule` on EVERY write; fresh-on-focus permission read + `degraded` note; native time pickers | Notifications | 05 |
| Notification permission surface | raw OS status (separate from `degraded`) + Open-system-settings handoff | Notifications | 05 |
| Interaction Assist toggle (1531–1565) | canonical `setInteractionAssistEnabled(exec, 0/1, localDateTime())` (ADR-070 / D-10 — the one-transaction opt-out that expires every `pending` `interaction_assists` row) → `setSettings(await getAppSettings(exec))` re-read → `await useAssistBanner.getState().refresh()`; NEVER the generic `persist()`/`updateAppSettings` path (which would silently drop the atomic queue-clear + banner refresh) | Interactions | 01 (Task 2) |
| Default message mode / right-swipe / default channel (persisted, not yet rendered) | `updateAppSettings` writes; enum validators | Interactions | 01 |
| Your photo row (1567–1584) | `PhotoSourcePicker target={{kind:'profile'}}` → `profile-dao`; reload-on-focus | Appearance / owner-profile | 03 |
| Your orbit: self-star colour + Orbit Center picker (1586–1728) | `selfSunColour` write; `sunOptions`/`sunCandidates` derivation (493–496); "Me" special option; ADR-047 enforcement | Appearance › Orbit Appearance | 03 |
| Self-name editor (NEW, D-04b) | `setProfileName` on `profile`; trim/empty→NULL/length validation | Appearance / owner-profile | 03 |
| Global default profile layout/background (NEW, D-05) | `updateAppSettings` PREFs; template list load + loading/error | Appearance | 03 |
| AI: enabled + hub rows + first-use disclosure (1730–1874) | `deriveAiHubState`/`buildAiEnabledPatch`; `ai_enabled` only; AI hub hydration; escape hatch | AI | 06 |
| Home screen: Add Orbit widget (1876–1928) | `requestPinWidget` (647); `addWidgetCopy` fallback copy | hub utility/action row | 08 |
| Custom Fields row (1930–1943) | nav `CustomFields` | Contacts › Relationship Structure | 04 |
| Systems row (1945–1958) | nav `SystemsManagement` | Orrery › Systems | 06 |
| Orrery Display prefs (density/satellites — persisted, rendered only on the Orrery today) | bound to `useOrreryPreferencesStore` (shared source, §H) | Orrery › Display | 06 |
| Archived contacts row (1960–1973) | nav `Archived` | Contacts › Contact Management | 04 |
| Data & Backup (NEW entry point, D-08) | register 4 Backup routes; dual-home hazards | Data & Backup | 07 |
| About Orbit (NEW, §K) | app name/version; omit unavailable rows | About | 08 |
</monolith_migration_matrix>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — hub at `Settings` + transitional `SettingsMore` + a real end-to-end Interactions slice + runtime route-registration contract</name>
  <precondition>Before ANY device/adb/emu-connect command in this phase, confirm the Orbit app package name and the `emu-connect` target with the owner — the CLAUDE.md Android section is carried over from quest-board and its package name + tmux session are placeholders (user_setup). Automated verification (tsc/vitest/check:colors) needs no device.</precondition>
  <files>src/screens/SettingsHubScreen.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsInteractionsScreen.tsx, src/screens/settings-interactions-logic.ts, src/screens/settings-interactions-logic.test.ts, src/navigation/settings-routes.ts, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (the 2,168-line monolith — READ IN FULL per CLAUDE.md "review the code, not the diff": the render tree starts at the `testID="settings-screen"` ScrollView ~line 671, `ShellAppBar variant="root" title="Settings"`, the `__DEV__` theme-preview row, and the `persist()` write path at 501–520)
    - src/navigation/tabs/SettingsStack.tsx (the ONLY sub-route precedent, READ IN FULL: `*Route` wrappers passing `onBack={() => navigation.goBack()}`, and the additive `<Stack.Screen>` block under `initialRouteName="Settings"` / `headerShown:false`; note the AI leaf routes and the already-hosted Profile/import/reconcile routes)
    - src/navigation/types.ts:229-300 (SettingsStackParamList — additive `RouteName: undefined` entries; RootStackParamList is a TYPE intersection, types.ts:314-317; note `CategoryManagement` is added typed-but-unregistered in Plan 04)
    - src/screens/AIPermissionsScreen.tsx:1-70 (leaf-screen shell: `useTheme()`, `@/components/ui` primitives, module-level LOG_SCOPE, `onBack` prop; the screen does NOT register its own route)
    - src/screens/settings-ai-hub-logic.ts (the hub-model logic-module precedent: a frozen `ReadonlyArray` of typed entries + pure functions, unit-testable in vitest)
    - src/db/app-settings-dao.ts (for the tracer write: `updateAppSettings`/`getAppSettings`, `MESSAGE_MODES` (~185) + `assertMessageMode` (~200-209), `defaultMessageMode` on `AppSettings`)
  </read_first>
  <action>
Create `src/navigation/settings-routes.ts` exporting `SETTINGS_REGISTERED_ROUTES` — a runtime `as const` array that is the SINGLE source of truth for the Settings-stack route names the hub can address. Seed it with the routes registered by this plan: `"Settings"`, the transitional `"SettingsMore"`, and `"SettingsInteractions"`. Export `type SettingsRegisteredRoute = (typeof SETTINGS_REGISTERED_ROUTES)[number]`. Document that later plans APPEND their category/Backup/About route names here as they register the corresponding `<Stack.Screen>`, and that `CategoryManagement` is DELIBERATELY excluded (D-03 typed-but-unregistered reservation). Addresses the cross-cutting review finding that a test over the erased `SettingsStackParamList` proves nothing.

Create `src/screens/settings-hub-model.ts` mirroring the shape of `settings-ai-hub-logic.ts`: export a frozen `SETTINGS_HUB_ROWS` array of a DISCRIMINATED UNION entry type — `{ kind: "route"; key; title; subtitle; icon; route: SettingsRegisteredRoute } | { kind: "action"; key; title; subtitle; icon; action }` — so a navigation row and an action row (the Plan 08 widget) are distinct at the type level (§L). Author rows in the §A canonical top-level order (Appearance, Contacts & Relationships, Interactions, Notifications, Orrery, Data & Backup, AI, About) plus a bottom utility section — but include ONLY the rows whose destination screen exists after THIS plan: the real `Interactions` route row (route `SettingsInteractions`) and a transitional `More settings` route row (route `SettingsMore`). Later plans insert their category rows at the correct §A index. Per §A the rows carry NO live setting values (title + subtitle only). Per §K do NOT emit any row whose screen does not exist yet. Because a route row's `route` is typed `SettingsRegisteredRoute`, a row targeting an unregistered/reserved name fails to type-check.

Create `src/screens/settings-interactions-logic.ts`: a pure, frozen option model for `default_message_mode` (D-04a) reusing the DAO's exported `MESSAGE_MODES` (do not re-declare literals), each entry pairing a display label to the `AppSettingsPatch` it produces (`{ defaultMessageMode: <mode> }`). Create `src/screens/settings-interactions-logic.test.ts` with the message-mode case (options equal `MESSAGE_MODES`; each builds the correct patch) so the tracer's logic is covered and independently green. Task 2 extends this module + test with right-swipe + default-channel models.

Create `src/screens/SettingsInteractionsScreen.tsx` as a REAL (minimal) category screen (leaf-shell analog; `onBack`; `useTheme()` colours only): an inline segmented/chip selector (§B) for the message mode, writing through a local `persist()` copied verbatim from SettingsScreen.tsx:501-520 (`updateAppSettings(exec, patch, localDateTime())` → re-read `getAppSettings` → the two idempotent reconcilers) — never inline SQL. This is the far end of the tracer's vertical slice; Task 2 expands it with right-swipe, default channel, and the migrated Interaction Assist toggle.

Create `src/screens/SettingsHubScreen.tsx` as a navigation-first directory (leaf-shell analog): `useTheme()` for every colour (no hardcoded colours), a `ScrollView` with `ShellAppBar variant="root" title="Settings"`, mapping `SETTINGS_HUB_ROWS` to `Pressable` rows — a `kind:"route"` row calls `navigation.navigate(row.route)`; a `kind:"action"` row invokes its action handler (none this plan) — each with `accessibilityRole="button"` and an `accessibilityLabel` (§O). Keep the `__DEV__` theme-preview row target for now (it moves to Appearance in Plan 02). Type navigation via `useNavigation<NativeStackNavigationProp<RootStackParamList>>()` exactly as the monolith does (SettingsScreen.tsx:240-241).

In `src/navigation/tabs/SettingsStack.tsx`: change the `Settings` `<Stack.Screen>` to `component={SettingsHubScreen}` (PRESERVE the `Settings` route name per §M — deep-link + back-stack safe), ADD a `<Stack.Screen name="SettingsMore" component={SettingsScreen} />` re-registering the untouched monolith at the transitional internal route, and register `SettingsInteractions` with a `SettingsInteractionsRoute` wrapper that renders the real screen from this task. In `src/navigation/types.ts` add `SettingsMore: undefined;` and `SettingsInteractions: undefined;` to `SettingsStackParamList`.

The transitional `More settings` hub row is a FUNCTIONAL row (it opens the real, fully-working monolith) — not a dead placeholder; Plan 08 removes it once every group has migrated.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-interactions-logic.test.ts src/db/app-settings-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/navigation/settings-routes.ts` exports a runtime `SETTINGS_REGISTERED_ROUTES` `as const` array containing `Settings`, `SettingsMore`, `SettingsInteractions` and a `SettingsRegisteredRoute` type; `CategoryManagement` is NOT present.
    - source: `src/navigation/tabs/SettingsStack.tsx` mounts `component={SettingsHubScreen}` for `<Stack.Screen name="Settings" ...>`, registers `<Stack.Screen name="SettingsMore" component={SettingsScreen} />`, and registers `SettingsInteractions` with a wrapper rendering the real `SettingsInteractionsScreen`.
    - source: `src/navigation/types.ts` `SettingsStackParamList` contains `SettingsMore: undefined;` and `SettingsInteractions: undefined;`.
    - source: `settings-hub-model.ts` entries are a discriminated union (`kind:"route" | "action"`); a route row's `route` is typed `SettingsRegisteredRoute`; the only rows target `SettingsInteractions` and `SettingsMore` (no row for an unbuilt category, §K).
    - source: `SettingsInteractionsScreen.tsx` imports `updateAppSettings`/`getAppSettings` and writes message mode via a `persist()` mirroring SettingsScreen.tsx:501-520 (no inline SQL) — the tracer's slice is real, not a stub.
    - behavior (device UAT, precondition met): Settings tab shows the hub; the Interactions row opens the new screen and a message-mode change persists; the transitional row opens the full original monolith (nothing lost).
    - `npx tsc --noEmit` and `npm run check:colors` pass.
  </acceptance_criteria>
  <done>The hub renders at `Settings`, the monolith is reachable via `SettingsMore`, and `SettingsInteractions` navigates to a REAL screen whose message-mode write round-trips — proven end-to-end and independently green (tsc + colours + DAO round-trip), committed.</done>
  <reversibility rating="reversible">Route rewiring, a transitional route, and a runtime route-name constant are pure code under version control; no data or schema change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interactions expansion — right-swipe (D-04c) + default channel (§F) + migrate Interaction Assist</name>
  <files>src/screens/SettingsInteractionsScreen.tsx, src/screens/settings-interactions-logic.ts, src/screens/settings-interactions-logic.test.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Interaction Assist toggle group at ~1531-1565 that migrates here; the CANONICAL `onToggleInteractionAssist` handler at 596-606 — `setInteractionAssistEnabled(exec, on ? 1 : 0, localDateTime())` → `setSettings(await getAppSettings(exec))` → `await useAssistBanner.getState().refresh()` with a try/catch `Alert` — this is the exact behaviour to preserve for the assist toggle (D-10/ADR-070); the generic `persist()` write path at 501-520 is carried verbatim ONLY for message mode / right-swipe / default channel, NOT for the assist toggle)
    - src/db/app-settings-dao.ts (READ the assist writer at ~1437-1465: `setInteractionAssistEnabled(exec, enabled: 0|1, now)` — one `inWriteTransaction` that updates the preference AND, on `enabled === 0`, runs `UPDATE interaction_assists SET status='expired' … WHERE status='pending'` (the ADR-070 atomic opt-out) — the migrated toggle MUST call this, never the generic `updateAppSettings`; also the `AppSettings` interface + `AppSettingsPatch`; `dashboardRightSwipeAction`/`RIGHT_SWIPE_ACTIONS` (~309, imported from `@/logic/dashboard-query-logic`); `defaultInteractionChannel` + its enum contract)
    - src/stores/assist-store.ts (READ: `useAssistBanner` Zustand store + its `refresh(): Promise<void>` action — the migrated assist toggle calls `useAssistBanner.getState().refresh()` after the write so the app-global banner drops the just-expired pending assists, mirroring SettingsScreen.tsx:601)
    - src/screens/settings-interactions-logic.ts (the message-mode model created in Task 1 — extend it; also add the injectable `persistInteractionAssistEnabled` helper here)
    - src/db/app-settings-dao.test.ts (the EXISTING queue-clear proof at ~645-675: seed pending assists → `setInteractionAssistEnabled(exec, 0, …)` expires all pending → opt-on does NOT resurrect — reference it as the DAO-level ADR-070 test, do not duplicate)
    - src/screens/AIPermissionsScreen.tsx:1-70 (screen shell + `onBack` prop)
  </read_first>
  <behavior>
    - settings-interactions-logic: right-swipe option set equals `RIGHT_SWIPE_ACTIONS`; default-interaction-channel option set matches the existing ordinary-channel contract (Remember Last Choice / Message / Call / In Person; Group Log exempt per §F). Each option maps to the exact `AppSettingsPatch` key it writes.
    - Test (already-red message-mode case from Task 1 stays green): building a patch for a chosen right-swipe action yields `{ dashboardRightSwipeAction: <action> }` and equals a member of `RIGHT_SWIPE_ACTIONS`; the channel model equals the ordinary-channel contract.
    - Round-trip: writing `dashboardRightSwipeAction` (and message mode) via `updateAppSettings` then `getAppSettings` returns the written values (existing DAO suite covers the DAO).
    - Interaction Assist (D-10/ADR-070): an injectable `persistInteractionAssistEnabled(exec, enabled: 0|1, deps)` helper (deps = `{ setInteractionAssistEnabled, getAppSettings, refreshBanner, now }`, mirroring Plan 05's `persistNotificationSettings` shape) calls `setInteractionAssistEnabled(exec, enabled, now())`, re-reads via `getAppSettings`, then calls `refreshBanner()`. It NEVER calls `updateAppSettings`/the generic `persist()`.
    - Test (RED first): with mocked deps, opt-OFF (`enabled = 0`) invokes `setInteractionAssistEnabled(exec, 0, now())` and THEN `refreshBanner()` (proving the migrated toggle routes through the specialized queue-clearing writer + banner refresh, not the generic path); opt-ON (`enabled = 1`) invokes `setInteractionAssistEnabled(exec, 1, …)` + `refreshBanner()`. The DAO-level queue-clear (all `pending` assists expired on opt-off, none resurrected on opt-on) is already proven by `app-settings-dao.test.ts` (~645-675) — reference it, do not duplicate.
  </behavior>
  <action>
Extend `src/screens/settings-interactions-logic.ts` with frozen option models for `dashboard_right_swipe_action` (D-04c) and `default_interaction_channel` (§F / RESEARCH A1 — persisted but not currently rendered), reusing the DAO's exported enums (`RIGHT_SWIPE_ACTIONS`) — do not re-declare literals; the DB CHECK + validators remain the source of truth. ALSO add the injectable `persistInteractionAssistEnabled(exec, enabled, deps)` helper described in the behavior block (deps default to the real `setInteractionAssistEnabled`/`getAppSettings`/`useAssistBanner.getState().refresh`/`localDateTime`) so the assist toggle's write path is unit-testable in vitest-node.

Extend `src/screens/SettingsInteractionsScreen.tsx`: add inline segmented/chip selectors (2-4 options → inline per §B) for right-swipe action and default channel — these write through the same local generic `persist()` copied from SettingsScreen.tsx:501-520. The migrated Interaction Assist toggle is DIFFERENT: it MUST write through the canonical `setInteractionAssistEnabled(exec, on ? 1 : 0, localDateTime())` writer (via `persistInteractionAssistEnabled`), then re-read with `getAppSettings`, then `await useAssistBanner.getState().refresh()` (import `useAssistBanner` from `@/stores/assist-store`), inside a try/catch that surfaces a failure `Alert` — mirroring `SettingsScreen.tsx:596-606` exactly (D-10 / ADR-070: "off means off, clear at once"; the atomic pending-queue expiry + banner refresh are load-bearing privacy behaviour). Do NOT route the assist toggle through the generic `persist()`/`updateAppSettings` path — that would silently drop the queue-clear and the banner refresh, reversing an Accepted, one-way ADR (an owner-only call). This is decision enforcement, not a reversal.

In `src/screens/SettingsScreen.tsx`, remove the Interaction Assist inline group (~1531-1565) AND its `onToggleInteractionAssist` handler (596-606) now that the toggle lives in the Interactions category (no duplicate write surface). Interaction Assist is the only group this plan migrates out of the monolith; all other groups stay in `SettingsMore` until their plans. (Leave the generic `persist` at 501-520 in place — later plans still use it.)

Category placement follows D-04 planner guidance: (a) message mode and (c) right-swipe belong in Interactions.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-interactions-logic.test.ts</automated>
    <automated>npx vitest run src/db/app-settings-dao.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: `settings-interactions-logic.test.ts` asserts message-mode options equal `MESSAGE_MODES` (each builds `{ defaultMessageMode }`), right-swipe options equal `RIGHT_SWIPE_ACTIONS` (each builds `{ dashboardRightSwipeAction }`), and the default-channel model equals the ordinary-channel contract.
    - test (D-10 / ADR-070): `settings-interactions-logic.test.ts` asserts `persistInteractionAssistEnabled` on opt-OFF (`enabled = 0`) calls `setInteractionAssistEnabled(exec, 0, now())` and THEN `refreshBanner()`, and on opt-ON (`enabled = 1`) calls `setInteractionAssistEnabled(exec, 1, …)` + `refreshBanner()` — and NEVER calls `updateAppSettings`/the generic persist. The DAO-level queue-clear (opt-off expires all `pending` assists; opt-on does not resurrect) is covered by the existing `app-settings-dao.test.ts` (~645-675) and remains green.
    - source (D-10 / ADR-070): the migrated Interaction Assist toggle in `SettingsInteractionsScreen.tsx` writes through `setInteractionAssistEnabled` (via `persistInteractionAssistEnabled`) + `getAppSettings` re-read + `useAssistBanner.getState().refresh()` (imported from `@/stores/assist-store`); it does NOT call `updateAppSettings`/`persist()`. The migration-matrix row for Interaction Assist names this specialized writer + banner refresh (so Plan 08's matrix gate approves the correct behaviour, not the generic one).
    - test: the app-settings-dao suite still passes (round-trip of `defaultMessageMode` + `dashboardRightSwipeAction` unchanged; the assist queue-clear test still green; D-06 no schema change).
    - source: the Interaction Assist group + its handler are rendered/owned by `SettingsInteractionsScreen` and no longer in `SettingsScreen.tsx`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Interactions category surfaces message mode (D-04a), right-swipe (D-04c), default channel, and the migrated Interaction Assist toggle routed through the canonical `setInteractionAssistEnabled` + banner refresh (D-10/ADR-070 enforced — atomic pending-queue clear preserved, generic persist NOT used); options-model + assist-handler + DAO tests green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 route-registration contract test + hub-model test (§K no-orphan-row guard)</name>
  <files>src/navigation/settings-routes.test.ts, src/screens/settings-hub-model.test.ts</files>
  <read_first>
    - src/navigation/settings-routes.ts (the `SETTINGS_REGISTERED_ROUTES` constant from Task 1)
    - src/navigation/tabs/SettingsStack.tsx (the actual `<Stack.Screen name="...">` registrations the source-scan test reads on disk)
    - src/screens/settings-hub-model.ts (the array authored in Task 1)
    - src/screens/settings-ai-hub-logic.ts (test-shape precedent)
  </read_first>
  <behavior>
    - settings-routes.test: reading `src/navigation/tabs/SettingsStack.tsx` from disk, every name in `SETTINGS_REGISTERED_ROUTES` appears as a `<Stack.Screen name="X"` registration; and `CategoryManagement` does NOT appear as a registered `<Stack.Screen>` (the D-03 typed-but-unregistered guard — the exact false-positive the old type-only test missed).
    - settings-hub-model.test: every `kind:"route"` hub row's `route` is a member of `SETTINGS_REGISTERED_ROUTES`; the §A relative ordering of present category rows holds; the transitional `SettingsMore` row is present now and is the last non-utility row (documents the migration scaffold Plan 08 removes).
  </behavior>
  <action>
Create `src/navigation/settings-routes.test.ts`: read `SettingsStack.tsx` via `node:fs`, assert each `SETTINGS_REGISTERED_ROUTES` entry has a matching `<Stack.Screen name="…">` registration (a genuine runtime-registration proof, feasible in the vitest-node env without RN rendering), and assert `CategoryManagement` is absent from the registrations. This is the phase-wide route-registration contract every later plan relies on.

Create `src/screens/settings-hub-model.test.ts`: assert every `kind:"route"` hub row's `route` is in `SETTINGS_REGISTERED_ROUTES`, assert the §A relative ordering of present rows, and assert the transitional `SettingsMore` row exists (a regression guard so a future plan does not accidentally strand un-migrated controls before Plan 08 removes it). This closes the RESEARCH "screen/logic tests for the hub navigation" Wave-0 gap; on-device rendering stays a device-UAT step.
  </action>
  <verify>
    <automated>npx vitest run src/navigation/settings-routes.test.ts src/screens/settings-hub-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test: `settings-routes.test.ts` fails if any `SETTINGS_REGISTERED_ROUTES` entry lacks a `<Stack.Screen>` in `SettingsStack.tsx`, and fails if `CategoryManagement` is registered.
    - test: `settings-hub-model.test.ts` fails if any `kind:"route"` hub row targets a route not in `SETTINGS_REGISTERED_ROUTES`; asserts §A ordering + presence of the transitional `SettingsMore` row.
    - `npx vitest run src/navigation/settings-routes.test.ts src/screens/settings-hub-model.test.ts` green; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Runtime route-registration contract + hub-model coverage (route validity, ordering, §K no-orphan, D-03 unregistered guard) are automated; the Wave-0 hub-nav gap is closed; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (Settings control) → DAO write → SQLite | The only untrusted input this plan adds; every preference value crosses here into `app_settings`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering / DoS | Interactions preference writes reaching the scheduler/DB | low | mitigate | Route message-mode / right-swipe / default-channel writes through `updateAppSettings` and the Interaction Assist toggle through the canonical `setInteractionAssistEnabled`; reuse the existing DAO validators (`assertMessageMode`, `RIGHT_SWIPE_ACTIONS` `.includes()`, channel enum, `validateAppSettingsPatch`) — no new unvalidated write path, no inline SQL. |
| T-37-04 | Tampering (scope-reversal) | ADR-070 "off means off" opt-out (`interaction_assists` pending queue) | high | mitigate | ENFORCE ADR-070 / D-10 by writing the migrated assist toggle through `setInteractionAssistEnabled` (atomic pending-queue expiry) + `useAssistBanner.refresh()`; forbid the generic `persist()` path for this toggle; a handler test asserts the specialized writer + banner refresh are used. Reversing ADR-070 (leaving pending assists prompting) is an owner-only decision, never a silent side effect. |
| T-37-03 | Information disclosure | AI egress via a Settings toggle | low | accept | Out of this plan's scope; Interactions touches no AI egress. `AiService.ts` untouched (do not widen). |

Package legitimacy (T-37-SC): N/A — this phase installs no npm/pip/cargo packages (RESEARCH Standard Stack: "Installation: none").
ASVS L1, block-on high. Local-first, on-device SQLite; no new network egress on any read path.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols — excluded from source-grounding drift checks)
- `src/screens/SettingsHubScreen.tsx` — navigation-first Settings directory (mounts at the preserved `Settings` route name).
- `src/screens/settings-hub-model.ts` — `SETTINGS_HUB_ROWS` frozen discriminated-union model (§A order; `{kind:"route"} | {kind:"action"}`) + `settings-hub-model.test.ts`.
- `src/navigation/settings-routes.ts` — `SETTINGS_REGISTERED_ROUTES` runtime constant + `SettingsRegisteredRoute` type + `settings-routes.test.ts` (source-scan registration contract).
- `src/screens/SettingsInteractionsScreen.tsx` — Interactions category screen.
- `src/screens/settings-interactions-logic.ts` — option models for message mode / right-swipe / default channel + the injectable `persistInteractionAssistEnabled(exec, enabled, deps)` helper (canonical `setInteractionAssistEnabled` + banner refresh, D-10/ADR-070) + `settings-interactions-logic.test.ts`.
- New `SettingsStackParamList` route names: `SettingsInteractions`, and the transitional `SettingsMore` (monolith re-registration; removed in Plan 08).
</artifacts>

<verification>
- `npx tsc --noEmit` clean, `npm run check:colors` clean, `npx vitest run src/navigation/settings-routes.test.ts src/screens/settings-hub-model.test.ts src/screens/settings-interactions-logic.test.ts src/db/app-settings-dao.test.ts` green.
- No migration added (head stays `029`, `TARGET_VERSION === 29`); `BACKUP_FORMAT_VERSION === 5` unchanged (D-06) — existing format-version assertions still pass.
- Device UAT (Pixel, per memory verify-ui-on-pixel-yourself; precondition: owner-confirmed package name + emu-connect target): Settings tab → hub renders; Interactions row → message-mode/right-swipe/channel/assist all persist; transitional row still reaches every original control.
</verification>

<success_criteria>
Hub-at-`Settings` + one REAL category route + a DAO round-trip proven end-to-end and independently green (D-09, D-04a, D-04c), the migrated Interaction Assist toggle enforcing D-10/ADR-070 via the canonical `setInteractionAssistEnabled` + banner refresh (atomic pending-queue clear preserved, generic persist forbidden), the runtime route-registration contract landed (SETTINGS_REGISTERED_ROUTES + source-scan test), the app stays whole via the transitional route, no schema/format change (D-06), and the architecture is de-risked before the remaining 13 row-groups split.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
