---
phase: 37-settings-personalization
plan: 05
type: execute
wave: 5
depends_on: ["37-04"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/navigation/settings-routes.ts
  - src/screens/settings-hub-model.ts
  - src/screens/settings-notifications-logic.ts
  - src/screens/settings-notifications-logic.test.ts
  - src/screens/SettingsNotificationsScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Notifications is a category screen (§G) reached from the hub, carrying all 10 existing notification controls (master, degraded note, Decay, Birthday, Birthday-unbound, Weekly digest, Lock-screen, Reminder time, Quiet start, Quiet end) organized by user-facing notification type, migrated from the monolith."
    - "Every notification/digest write goes through a SHARED, TESTED `persistNotificationSettings(exec, patch, deps)` helper — `updateAppSettings` → re-read → `reconcileSchedule` + `reconcileDigestSchedule` — extracted from the monolith's inline `persist()` so it can be unit-tested with mocked DAO + reconcilers (ordering + failure), and reused by the screen. Both reconcilers are fired immediately (fire-and-forget via `void`, matching the shipped monolith at SettingsScreen.tsx:507) so the OS schedule re-arms on change (RESEARCH Pitfall 5: a plain `updateAppSettings` without the reconcilers leaves the schedule stale until next launch). NOTE: 'synchronously' is inaccurate — the reconcilers are not awaited; the correct wording is 'immediately triggered'."
    - "The RAW OS notification permission status is tracked SEPARATELY from the master-on-but-blocked `degraded` note: the monolith reads `getNotificationPermission()` (→ `{ granted, status }`) only when the master is enabled (SettingsScreen.tsx:349), which cannot power a general permission row. The category screen reads the raw OS status fresh on focus regardless of the master, renders a permission row from it, and offers an actionable Open-system-settings handoff (`Linking.openSettings()`) when denied — never a silent re-prompt; permission-dependent controls stay visible with explanation rather than hidden (§G)."
    - "Unbound birthday-reminder behaviour lives under Notifications (§G); birthday *presentation* stays deferred to Your Week (ADR-076) — Phase 37 owns only the birthday notification settings seam."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsNotificationsScreen.tsx
    - src/screens/settings-notifications-logic.ts
  key_links:
    - "Notifications control → persistNotificationSettings() (updateAppSettings + reconcileSchedule + reconcileDigestSchedule, both void-fired) → app_settings + OS schedule re-armed immediately."
    - "Fresh-on-focus getNotificationPermission() → raw {granted,status} → permission row + Linking.openSettings() handoff (separate from the master-on-but-blocked `degraded` note)."
---

<objective>
Build the **Notifications** category (§G) by migrating the 10 notification controls out of the monolith, carrying the reconcile-on-write path verbatim so the OS schedule keeps re-arming immediately, and surfacing notification-permission state with a system-settings handoff.

Implements dossier **§G** (notifications organized by user-facing type; permission handoff; Unbound birthday-reminder behaviour here) and honors **ADR-041** (notification settings) and **ADR-076** (birthday presentation stays in Your Week — §G owns only the notification-settings seam). Cites **D-01**.

Purpose: relocate the most reconcile-sensitive control group without leaving the OS schedule stale (RESEARCH Pitfall 5).
Output: SettingsNotificationsScreen carrying all 10 controls + a tested shared `persistNotificationSettings` helper + the raw-permission surface/handoff; monolith notification group removed.
</objective>

<review_feedback_addressed>
Cross-AI review (37-REVIEWS.md, cycle 1) findings owned by this plan (both MEDIUM):
- **"Synchronously" inaccurate; tests don't prove reconciliation**: RESOLVED — extract a shared `persistNotificationSettings(exec, patch, deps)` logic helper (updateAppSettings then both reconcilers, injectable) and unit-test ordering + both-fire + failure; the word "synchronously" is replaced with "immediately triggered (fire-and-forget via `void`)". A source-text acceptance that both names appear is NOT sufficient (Codex) — the helper is tested through the actual write path.
- **Permission "degraded" isn't a general permission signal**: RESOLVED — Task 2 reads the raw OS status via `getNotificationPermission()` fresh on focus, independent of the master toggle (the monolith only reads it when the master is on, SettingsScreen.tsx:349), and drives the permission row + handoff from it, keeping `degraded` (master-on-but-blocked) as a separate concern.
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract + test `persistNotificationSettings`; SettingsNotificationsScreen with all 10 controls (Pitfall 5)</name>
  <files>src/screens/settings-notifications-logic.ts, src/screens/settings-notifications-logic.test.ts, src/screens/SettingsNotificationsScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/navigation/settings-routes.ts, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Notifications group at ~1173-1529 — master, degraded note, Decay, Birthday, Birthday-unbound, Weekly digest, Lock-screen, Reminder-time native picker, Quiet start, Quiet end; the `persist()` at 501-520 that folds BOTH `reconcileSchedule` and `reconcileDigestSchedule` into every write; the fresh-on-focus permission read + `degraded` state at 251-256 and reloadNotifications at ~347-360)
    - src/services/notifications/permission.ts (`getNotificationPermission()` → `{ granted, status }`)
    - the `reconcileSchedule` / `reconcileDigestSchedule` sources (grep — the two reconcilers folded into persist) to confirm their signatures for the injectable deps
    - src/navigation/tabs/SettingsStack.tsx (register `SettingsNotifications` with a wrapper)
    - src/navigation/settings-routes.ts (append `"SettingsNotifications"` to `SETTINGS_REGISTERED_ROUTES`)
    - src/screens/settings-hub-model.ts (insert the `Notifications` `kind:"route"` row at §A index 3)
    - docs/systems/notifications.md (notification settings + reconcile behaviour, §G)
  </read_first>
  <behavior>
    - `persistNotificationSettings(exec, patch, deps)` (deps = `{ updateAppSettings, getAppSettings, reconcileSchedule, reconcileDigestSchedule, now }`, all injectable): calls `updateAppSettings(exec, patch, now())`, re-reads via `getAppSettings`, then triggers BOTH `reconcileSchedule(exec)` and `reconcileDigestSchedule(exec)` (fire-and-forget via `void`, matching the shipped monolith — NOT awaited).
    - Test (RED first): with mocked deps, asserts updateAppSettings is called with the patch BEFORE the re-read, and BOTH reconcilers are invoked after a successful write; a failing `updateAppSettings` is caught/logged and does NOT invoke the reconcilers (ordering + failure). This proves reconciliation through the actual shared write path — a source-text check that both names appear is insufficient.
  </behavior>
  <action>
Create `src/screens/settings-notifications-logic.ts` exporting `persistNotificationSettings` with the injectable deps above, extracted from the monolith's inline `persist()` (SettingsScreen.tsx:501-520) so the reconcile-on-write path is unit-testable. Write `settings-notifications-logic.test.ts` first (RED), then implement (GREEN).

Create `src/screens/SettingsNotificationsScreen.tsx` (leaf-shell analog): migrate all 10 controls from SettingsScreen.tsx:1173-1529, organized by user-facing notification type (relationship reminders, birthdays incl. Unbound birthday behaviour, weekly digest, system notification behaviour) per §G. Route every write through the new `persistNotificationSettings` (so `reconcileSchedule` AND `reconcileDigestSchedule` fire immediately — Pitfall 5), never a bare `updateAppSettings`. Preserve the master-on-but-blocked `degraded` note (permission denied while master on → text only, never a re-prompt). Keep the native reminder-time / quiet-window time pickers. (The raw-permission surface is Task 2.)

Register `SettingsNotifications: undefined;` in types.ts + the SettingsStack wrapper, append `"SettingsNotifications"` to `SETTINGS_REGISTERED_ROUTES`; insert the hub row (§A index 3). Remove the migrated group (1173-1529) from `SettingsScreen.tsx`. Theme tokens + §O a11y labels.
  </action>
  <verify>
    <automated>npx vitest run src/screens/settings-notifications-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts src/navigation/settings-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test: `settings-notifications-logic.test.ts` proves `persistNotificationSettings` calls `updateAppSettings` then fires BOTH reconcilers on success, and skips the reconcilers on a failed write (ordering + failure), with mocked deps.
    - source: `SettingsNotificationsScreen.tsx` routes writes through `persistNotificationSettings` (not a bare `updateAppSettings`); all 10 controls render here and the group is removed from `SettingsScreen.tsx`; the master-on `degraded` note is carried.
    - source: `SettingsNotifications` is in `SETTINGS_REGISTERED_ROUTES` + `SettingsStackParamList`, SettingsStack registers it, hub model has the row at §A index 3 (hub-model + settings-routes source-scan tests green).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): toggling any notification setting re-arms the OS schedule immediately (not at next launch).
  </acceptance_criteria>
  <done>Notifications category ships all 10 controls routed through a tested shared reconcile-on-write helper; monolith group removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Notification-permission surface + Open-system-settings handoff (§G)</name>
  <files>src/screens/SettingsNotificationsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsNotificationsScreen.tsx (the screen from Task 1; the master-on `degraded` handling)
    - src/screens/SettingsScreen.tsx (VERIFY reloadNotifications at ~347-360: `getNotificationPermission()` is read ONLY when `notificationsEnabled === 1`, so `degraded` is master-on-but-blocked, NOT a general permission signal — the review MEDIUM)
    - src/services/notifications/permission.ts (`getNotificationPermission()` → `{ granted, status }`; fresh OS read every call)
    - src/services/contacts/use-read-contacts-permission.ts (`openContactsSettings` shows the `Linking.openSettings()` handoff idiom to mirror for notifications)
    - docs/decisions/ADR-041-notification-settings-privacy-channels-and-birthday-alerts.md (§G governing decision)
  </read_first>
  <action>
In `SettingsNotificationsScreen.tsx`, add a RAW OS notification-permission signal read fresh on focus via `getNotificationPermission()` (→ `{ granted, status }`), tracked in its OWN state SEPARATE from the master-on `degraded` note — so the permission row reflects true OS status regardless of the master toggle (fixing the review finding that `degraded` clears when the master is off). Render the permission row where the user conceptually encounters it (§G: permission under Notifications, no global Permissions category). When permission is denied and Orbit cannot change it itself, provide an actionable "Open system settings" handoff via `Linking.openSettings()` (mirror `openContactsSettings`'s pattern, incl. its calm OEM fallback). Keep permission-dependent capabilities visible with contextual explanation rather than hiding them (§G). Do not add a re-prompt loop.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: the raw OS permission status is read via `getNotificationPermission()` fresh on focus and stored SEPARATELY from `degraded` (not gated on the master toggle).
    - source: `SettingsNotificationsScreen.tsx` renders a permission row with an Open-system-settings handoff (`Linking.openSettings()`) active when permission is denied (§G).
    - source: permission-dependent controls stay rendered (with explanation) rather than hidden when blocked.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): with notifications denied, the permission row reflects it even with the master off, and the handoff opens Android app settings; controls remain visible with an explanation.
  </acceptance_criteria>
  <done>Notification permission state + system-settings handoff live under Notifications (§G); committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (notification toggles / times) → DAO write → OS scheduler | Notification settings cross here into `app_settings` and then re-arm the OS schedule via the reconcilers. |
| OS notification permission → device state | Permission is read + surfaced; the handoff opens system settings (no in-app grant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering / DoS | notification/digest writes reaching the scheduler | low | mitigate | Reuse the monolith's validated `persist()` (hours 0–23, toggle 0/1 validators); the reconcilers are idempotent + defer-guarded; no inline SQL. |
| T-37-06 | DoS (stale schedule) | OS schedule left stale by a write that skips the reconcilers | medium | mitigate | Carry `reconcileSchedule` + `reconcileDigestSchedule` on EVERY notification write (Pitfall 5); acceptance asserts both fire. |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsNotificationsScreen.tsx` — Notifications category screen (10 controls + raw-permission surface/handoff).
- `src/screens/settings-notifications-logic.ts` — `persistNotificationSettings(exec, patch, deps)` shared reconcile-on-write helper + `settings-notifications-logic.test.ts`.
- New `SettingsStackParamList` route name: `SettingsNotifications` (also appended to `SETTINGS_REGISTERED_ROUTES`).
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06).
- Device UAT: every notification write re-arms the schedule immediately; denied-permission handoff opens system settings.
</verification>

<success_criteria>
Notifications category migrated with zero behaviour lost, the reconcile-on-write path preserved (Pitfall 5), and a permission surface + system-settings handoff (§G) — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-05-SUMMARY.md` when done.
</output>
