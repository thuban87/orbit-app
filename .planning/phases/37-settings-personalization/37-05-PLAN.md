---
phase: 37-settings-personalization
plan: 05
type: execute
wave: 5
depends_on: ["37-04"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/tabs/SettingsStack.tsx
  - src/screens/settings-hub-model.ts
  - src/screens/SettingsNotificationsScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings → Notifications is a category screen (§G) reached from the hub, carrying all 10 existing notification controls (master, degraded note, Decay, Birthday, Birthday-unbound, Weekly digest, Lock-screen, Reminder time, Quiet start, Quiet end) organized by user-facing notification type, migrated from the monolith."
    - "Every notification/digest write goes through the SAME `persist()` path as the monolith — `updateAppSettings` → re-read → `reconcileSchedule` + `reconcileDigestSchedule` — so the OS schedule re-arms IMMEDIATELY on change (RESEARCH Pitfall 5: a plain `updateAppSettings` without the reconcilers would leave the schedule stale until next launch)."
    - "OS notification permission state appears under Notifications with an actionable Open-system-settings handoff when denied (§G); a denied permission renders a non-nagging degraded note, never a silent re-prompt, and permission-dependent controls stay visible with explanation rather than hidden."
    - "Unbound birthday-reminder behaviour lives under Notifications (§G); birthday *presentation* stays deferred to Your Week (ADR-076) — Phase 37 owns only the birthday notification settings seam."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06)."
  artifacts:
    - src/screens/SettingsNotificationsScreen.tsx
  key_links:
    - "Notifications control → persist() (updateAppSettings + reconcileSchedule + reconcileDigestSchedule) → app_settings + OS schedule re-armed synchronously."
---

<objective>
Build the **Notifications** category (§G) by migrating the 10 notification controls out of the monolith, carrying the reconcile-on-write path verbatim so the OS schedule keeps re-arming immediately, and surfacing notification-permission state with a system-settings handoff.

Implements dossier **§G** (notifications organized by user-facing type; permission handoff; Unbound birthday-reminder behaviour here) and honors **ADR-041** (notification settings) and **ADR-076** (birthday presentation stays in Your Week — §G owns only the notification-settings seam). Cites **D-01**.

Purpose: relocate the most reconcile-sensitive control group without leaving the OS schedule stale (RESEARCH Pitfall 5).
Output: SettingsNotificationsScreen carrying all 10 controls + the permission handoff; monolith notification group removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsNotificationsScreen — 10 controls migrated WITH the reconcile-on-write path (Pitfall 5)</name>
  <files>src/screens/SettingsNotificationsScreen.tsx, src/navigation/types.ts, src/navigation/tabs/SettingsStack.tsx, src/screens/settings-hub-model.ts, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the Notifications group at ~1173-1529 — master, degraded note, Decay, Birthday, Birthday-unbound, Weekly digest, Lock-screen, Reminder-time native picker, Quiet start, Quiet end; the `persist()` at 501-520 that folds BOTH `reconcileSchedule` and `reconcileDigestSchedule` into every write; the fresh-on-focus permission read + `degraded` state at 251-256)
    - src/navigation/tabs/SettingsStack.tsx (register `SettingsNotifications` with a wrapper)
    - src/screens/settings-hub-model.ts (insert the `Notifications` row at §A index 3)
    - docs/systems/notifications.md (notification settings + reconcile behaviour, §G)
  </read_first>
  <action>
Create `src/screens/SettingsNotificationsScreen.tsx` (leaf-shell analog): migrate all 10 controls from SettingsScreen.tsx:1173-1529, organized by user-facing notification type (relationship reminders, birthdays incl. Unbound birthday behaviour, weekly digest, system notification behaviour) per §G. Carry the `persist()` from SettingsScreen.tsx:501-520 VERBATIM — every write must fire `reconcileSchedule` AND `reconcileDigestSchedule` so the OS schedule and the weekly-digest trigger re-arm synchronously (Pitfall 5). Preserve the fresh-on-focus permission read and the non-nagging `degraded` note (permission denied → text only, never a re-prompt). Keep the native reminder-time / quiet-window time pickers.

Register `SettingsNotifications: undefined;` in types.ts + the SettingsStack wrapper; insert the hub row (§A index 3). Remove the migrated group (1173-1529) from `SettingsScreen.tsx`. Theme tokens + §O a11y labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/screens/settings-hub-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsNotificationsScreen.tsx`'s write callback fires `reconcileSchedule` AND `reconcileDigestSchedule` after `updateAppSettings` (the Pitfall-5 reconcile-on-write path preserved), not a bare `updateAppSettings`.
    - source: all 10 controls render in `SettingsNotificationsScreen.tsx` and the group is removed from `SettingsScreen.tsx`; the fresh-on-focus permission read + `degraded` note are carried.
    - source: `SettingsStackParamList` has `SettingsNotifications`, SettingsStack registers it, hub model has the row at §A index 3 (hub-model test green).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): toggling any notification setting re-arms the OS schedule immediately (not at next launch); denying notification permission shows the degraded note without a re-prompt.
  </acceptance_criteria>
  <done>Notifications category ships all 10 controls with the reconcile-on-write path intact; monolith group removed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Notification-permission surface + Open-system-settings handoff (§G)</name>
  <files>src/screens/SettingsNotificationsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsNotificationsScreen.tsx (the screen from Task 1; the existing degraded/permission handling)
    - src/screens/SettingsScreen.tsx (VERIFY how the fresh-on-focus permission is read today — the existing permission API usage — before adding the handoff)
    - docs/decisions/ADR-041-notification-settings-privacy-channels-and-birthday-alerts.md (§G governing decision)
  </read_first>
  <action>
In `SettingsNotificationsScreen.tsx`, surface OS notification-permission state where the user conceptually encounters it (§G: permission under Notifications, no global Permissions category). When permission is denied and Orbit cannot change it itself, provide an actionable "Open system settings" handoff (the RN/Expo Linking handoff already used elsewhere in the app — confirm the existing pattern before wiring). Keep permission-dependent capabilities visible with contextual explanation rather than hiding them (§G). Do not add a re-prompt loop.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsNotificationsScreen.tsx` renders a permission row with an Open-system-settings handoff active only when permission is denied (§G).
    - source: permission-dependent controls stay rendered (with explanation) rather than hidden when blocked.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): with notifications denied, the handoff opens Android app settings; controls remain visible with an explanation.
  </acceptance_criteria>
  <done>Notification permission state + system-settings handoff live under Notifications (§G); committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (notification toggles / times) → DAO write → OS scheduler | Notification settings cross here into `app_settings` and then re-arm the OS schedule via the reconcilers. |
| OS notification permission → device state | Permission is read + surfaced; the handoff opens system settings (no in-app grant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering / DoS | notification/digest writes reaching the scheduler | low | mitigate | Reuse the monolith's validated `persist()` (hours 0–23, toggle 0/1 validators); the reconcilers are idempotent + defer-guarded; no inline SQL. |
| T-37-06 | DoS (stale schedule) | OS schedule left stale by a write that skips the reconcilers | medium | mitigate | Carry `reconcileSchedule` + `reconcileDigestSchedule` on EVERY notification write (Pitfall 5); acceptance asserts both fire. |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress.
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `src/screens/SettingsNotificationsScreen.tsx` — Notifications category screen (10 controls + permission handoff).
- New `SettingsStackParamList` route name: `SettingsNotifications`.
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/screens/settings-hub-model.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06).
- Device UAT: every notification write re-arms the schedule immediately; denied-permission handoff opens system settings.
</verification>

<success_criteria>
Notifications category migrated with zero behaviour lost, the reconcile-on-write path preserved (Pitfall 5), and a permission surface + system-settings handoff (§G) — no schema/format change.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-05-SUMMARY.md` when done.
</output>
