---
phase: 37-settings-personalization
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - src/db/profile-dao.ts
  - src/db/profile-dao.test.ts
  - src/screens/SettingsAppearanceScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: []          # No REQ-IDs (deferred-planning phase).
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Appearance carries an Orbit Appearance section (§D): the self-star colour control (`self_sun_colour`, D-02 — only the self-star is user-configurable; a contact at centre keeps its status-derived glow, no ADR-047 reversal) and Orbit Center — a searchable contact picker with a special 'You' option — migrated from the monolith's 'Your orbit' group with the 'Your star' control staying available regardless of the current centre."
    - "Appearance exposes the GLOBAL default profile layout and background as controls (D-05: `profile_layout_template_uid`, `profile_background_template_uid` — the global `app_settings` PREFs), distinct from the per-contact managers which stay contact-scoped and are NOT linked from Settings."
    - "A new `setProfileName(exec, name, now)` DAO writer persists the owner's self name to the single-row `profile` table (id=1) — D-04b — mirroring `setProfilePhoto` exactly (one `inWriteTransaction`, `?`-bound `UPDATE profile SET name = ?, modified_at = ? WHERE id = 1`, `changes===1` loud-fail guard, `bumpDataRevisionCore`); it is NOT a `contacts` edit, and it bounds/validates the name before the UPDATE (V5)."
    - "The owner's photo control (`PhotoSourcePicker target={{kind:'profile'}}` → `profile-dao`) is migrated into Appearance/owner-profile unchanged."
    - "No SQLite migration is added and `BACKUP_FORMAT_VERSION` stays 5 (D-06); `profile.name` already round-trips backup in format 5 — no new column, no format bump."
  artifacts:
    - "setProfileName (src/db/profile-dao.ts)"
  key_links:
    - "Self-name editor → setProfileName → profile table (id=1) → getProfile read-back; global-default controls → updateAppSettings → app_settings PREFs."
---

<objective>
Complete the **Appearance** category by migrating the Orbit Appearance group (self-star colour + Orbit Center picker) and the owner photo control, exposing the **global default** profile layout/background (D-05), and adding the self-name editor (D-04b) backed by a new `setProfileName` writer on the single-row `profile` table.

Implements **D-02** (self-star colour only; no ADR-047 reversal), **D-04b** (self-name editor on `profile`, not `contacts` — RESEARCH Pitfall 1), **D-05** (global profile layout/background default in Appearance; per-contact managers stay contact-scoped). Cites **D-01**.

Purpose: finish Appearance and add the one new DAO writer the phase needs, without touching schema or the backup format.
Output: `setProfileName` + its test; SettingsAppearanceScreen extended with Orbit Appearance + owner-profile + global-default controls; the corresponding monolith groups removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-settings-personalization/37-CONTEXT.md
@.planning/phases/37-settings-personalization/37-RESEARCH.md
@.planning/phases/37-settings-personalization/37-PATTERNS.md
@.planning/phases/37-settings-personalization/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `setProfileName` DAO writer (D-04b) on the single-row `profile` table + name test case</name>
  <files>src/db/profile-dao.ts, src/db/profile-dao.test.ts</files>
  <read_first>
    - src/db/profile-dao.ts (READ IN FULL: `setProfilePhoto` at 38-61 is the exact analog — one `inWriteTransaction`, `?`-bound `UPDATE ... WHERE id = 1`, `changes===1` guard, `bumpDataRevisionCore`; `getProfile` at 96-102 already returns `{ name, photo, modified_at }`; the file header documents `name` nullable until a self-name editor lands)
    - src/db/profile-dao.test.ts (add the name round-trip case here)
    - src/db/migrations/001-initial.ts (VERIFY the `profile` DDL: `id INTEGER PRIMARY KEY CHECK (id = 1)`, `name TEXT`; the seed omits name → NULL. `contacts` is a SEPARATE table — RESEARCH Pitfall 1)
  </read_first>
  <behavior>
    - After `setProfileName(exec, "Ada", now)`, `getProfile(exec)` returns `name === "Ada"` and a bumped `modified_at`.
    - `setProfileName` on the id=1 seed row reports `changes===1`; a hypothetical mismatch throws → rollback (loud-fail, like `setProfilePhoto`).
    - Input validation (V5): a name exceeding the bound length or containing control characters is rejected BEFORE the UPDATE opens (fail-fast, mirroring `assertSafeRelative`'s posture in `setProfilePhoto`).
    - The writer targets `profile`, never `contacts` (no `contacts` name UPDATE anywhere in the change).
  </behavior>
  <action>
Add `export async function setProfileName(exec, name, now)` to `src/db/profile-dao.ts` mirroring `setProfilePhoto` exactly: a fail-fast validation of `name` (bound length + reject control chars) before the transaction, then a single `inWriteTransaction` running a `?`-bound `UPDATE profile SET name = ?, modified_at = ? WHERE id = 1`, a `changes !== 1` throw, and `bumpDataRevisionCore`. Do NOT touch `contacts`. Do NOT add a column — `profile.name` already exists (migration 001) and already emits in backup format 5 (export-manifest.ts:106), so there is no schema change and no format bump (D-06).

Extend `src/db/profile-dao.test.ts` with the name round-trip + validation cases described in the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/db/profile-dao.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/db/profile-dao.ts` contains `export async function setProfileName(` with a `?`-bound `UPDATE profile SET name = ?, modified_at = ? WHERE id = 1` and a `changes !== 1` guard; no `contacts` write is introduced.
    - test: `profile-dao.test.ts` asserts `getProfile` returns the name after a `setProfileName` round-trip and that an over-length / control-char name is rejected before the write.
    - `npx vitest run src/db/profile-dao.test.ts` green; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>`setProfileName` persists the self name to `profile` (id=1) with validation + loud-fail, proven by the name test case; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Orbit Appearance (self-star colour D-02 + Orbit Center picker) + owner photo, migrated into Appearance</name>
  <files>src/screens/SettingsAppearanceScreen.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (READ IN FULL: the 'Your orbit' group at ~1586-1728 — self sun colour + sun/centre occupant picker, and the `sunOptions` derivation at 493-496 with the 'Me' special option; the 'Your photo' row at ~1567-1584 using `PhotoSourcePicker target={{kind:'profile'}}`)
    - docs/decisions/ADR-047-app-level-assignable-sun-and-themed-self-identity.md (D-02 governing decision — only the self-star colour is configurable; a contact at centre keeps its status-derived glow; NO reversal)
    - src/screens/SettingsAppearanceScreen.tsx (extend the screen built in Plan 02)
  </read_first>
  <action>
Migrate the 'Your orbit' group into `SettingsAppearanceScreen.tsx` as an **Orbit Appearance** section, reframed as **Orbit Center** per §D: the self-star colour control (`self_sun_colour`) and a searchable contact-occupant picker with the special 'You' option (reuse the existing picker / `sunOptions` shape). Per D-02, keep the 'Your star' colour control available regardless of the current centre selection, and do NOT introduce a user-chosen colour for a contact at centre (a contact keeps its status-derived glow) — enforce ADR-047, do not reverse it. Migrate the 'Your photo' row (`PhotoSourcePicker target={{kind:'profile'}}` → `profile-dao`) into the owner-profile grouping unchanged.

Remove the migrated 'Your orbit' (1586-1728) and 'Your photo' (1567-1584) groups from `src/screens/SettingsScreen.tsx`. All colours via theme tokens (§O a11y labels on every control).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAppearanceScreen.tsx` renders the self-star colour control and an Orbit Center picker with a 'You' special option; the self-star control is not conditioned on the current centre (D-02).
    - source: the self-star colour control writes `self_sun_colour` and introduces no user-chosen colour for a contact centre (ADR-047 enforced, not reversed).
    - source: the 'Your orbit' + 'Your photo' groups are removed from `SettingsScreen.tsx` and present in `SettingsAppearanceScreen.tsx`.
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT): star colour persists; Orbit Center picker sets the centre; picking a contact centre does not offer a colour control.
  </acceptance_criteria>
  <done>Orbit Appearance (self-star D-02 + Orbit Center) and owner photo migrated into Appearance; monolith groups removed; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Global default profile layout/background (D-05) + self-name editor (D-04b) in Appearance</name>
  <files>src/screens/SettingsAppearanceScreen.tsx</files>
  <read_first>
    - src/db/app-settings-dao.ts (VERIFY `profile_layout_template_uid` / `profile_background_template_uid` are global `app_settings` PREFs with validators; the `updateAppSettings` write path)
    - src/db/profile-dao.ts (the `setProfileName` writer added in Task 1 + `getProfile` for the read-back)
    - src/components/profile/ProfileTemplateManager.tsx, src/components/profile/ProfileBackgroundManager.tsx (the PER-CONTACT managers — D-05 says these stay contact-scoped and are NOT linked from Settings; confirm the distinction before wiring the global control)
    - src/screens/SettingsAppearanceScreen.tsx (extend)
    - src/screens/SettingsScreen.tsx (VERIFY at 242-249 the self `name` is nullable with a 'You' fallback and 'carries no name until a self-name editor ships')
  </read_first>
  <action>
Add to `SettingsAppearanceScreen.tsx` a control for the GLOBAL default profile layout and background (`profile_layout_template_uid`, `profile_background_template_uid`) written through `updateAppSettings` (D-05). This is the global default only — do NOT link the per-contact `ProfileTemplateManager` / `ProfileBackgroundManager` from Settings (they stay reachable only from a contact's profile).

Add the self-name editor (D-04b) in the owner-profile grouping: a bounded text input seeding from `getProfile().name` (with the existing 'You' fallback for display) that persists via `setProfileName(getExecutor(), name, localDateTime())`. This writes the `profile` table, not `contacts`.

No migration, no format bump — all keys are existing columns / the existing `profile.name` (D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/db/profile-dao.test.ts src/db/app-settings-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `SettingsAppearanceScreen.tsx` writes `profile_layout_template_uid` + `profile_background_template_uid` via `updateAppSettings` (global default, D-05) and does NOT import/link the per-contact managers.
    - source: the self-name editor calls `setProfileName` (profile table) — no `contacts` name write.
    - test: `profile-dao.test.ts` + `app-settings-dao.test.ts` green (round-trips unchanged; D-06 no schema change).
    - `npx tsc --noEmit` + `npm run check:colors` pass.
    - behavior (device UAT, DB-invariant per memory device-uat-runas-pattern): edited self name persists in `profile` (id=1); global default layout/background applies to new profile views.
  </acceptance_criteria>
  <done>Global profile default (D-05) + self-name editor (D-04b) ship in Appearance over existing columns / the `profile` table; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (self name, star colour, template uids) → DAO write → SQLite | The self name is free-text (the only new free-text input this phase adds); the others are enum/colour values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering / DoS | `setProfileName` free-text + template/colour writes reaching SQLite | low | mitigate | `setProfileName` bounds length + rejects control chars BEFORE the UPDATE (V5, mirroring `assertSafeRelative`); template/colour writes reuse the existing `app_settings` validators (`SELF_SUN_COLOUR_RE`, enum guards). No inline SQL; `?`-bound only. |
| T-37-04 | Tampering (scope-reversal) | ADR-047 self/sun identity | low | mitigate | Only the self-star colour is configurable; enforce (never reverse) ADR-047 — no user-chosen colour for a contact centre. |

Package legitimacy (T-37-SC): N/A — no package installs. ASVS L1, block-on high; local-first, no new egress (self name never leaves the device; not sent to AI).
</threat_model>

<artifacts>
## Artifacts this plan produces (NEW symbols)
- `setProfileName(exec, name, now)` in `src/db/profile-dao.ts` — self-name writer on the single-row `profile` table.
- New controls on `SettingsAppearanceScreen.tsx`: Orbit Appearance (self-star + Orbit Center), owner photo (migrated), global profile layout/background default, self-name editor.
</artifacts>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, `npx vitest run src/db/profile-dao.test.ts src/db/app-settings-dao.test.ts` green.
- No migration added; `BACKUP_FORMAT_VERSION === 5` unchanged (D-06); `profile.name` already emits in format 5.
- Device UAT: self name persists in `profile` id=1 (run-as DB read); star colour + global default persist.
</verification>

<success_criteria>
Appearance complete (§D): Orbit Center + self-star (D-02), owner photo, global profile default (D-05), and a validated self-name editor (D-04b) on `profile` — all over existing storage, no schema/format change, ADR-047 enforced.
</success_criteria>

<output>
Create `.planning/phases/37-settings-personalization/37-03-SUMMARY.md` when done.
</output>
