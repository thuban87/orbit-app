---
phase: 05-photos
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - src/services/photos/url-image.ts
  - src/services/photos/url-image.test.ts
  - src/components/PhotoSourcePicker.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements: [PHOTO-02, PHOTO-01]
must_haves:
  truths:
    - "A user pastes an https image URL and it downloads once to the same local 512px master pipeline"
    - "Only https URLs are accepted; http/file/other schemes and non-image content are rejected with SPEC copy — and a redirect that resolves to a non-https/non-image final URL is rejected too (the FINAL resolved URL is validated, not just the submitted scheme)"
    - "Reads never hit the network — the URL fetch is a one-time write-path event at paste time only"
    - "A user can set their own (self record) photo from Settings via the same pipeline, with a deterministic self initials avatar (getProfile name or a stable 'You' fallback — never a permanently blank swatch)"
    - "The self photo can be removed from Settings via Plan 05's target-kind-aware profile Remove (clearProfilePhoto + delete file), with no contactId crash"
  artifacts:
    - path: "src/services/photos/url-image.ts"
      provides: "https-only isImageUrl (submitted + final-redirect URL) + content-type→ext + fetch-based download-once-to-cache with content-type/size validation"
      exports: ["isImageUrl", "extFromContentType", "downloadImageToCache"]
    - path: "src/screens/SettingsScreen.tsx"
      provides: "Your photo entry → self-record photo via the pipeline"
      contains: "PhotoSourcePicker"
  key_links:
    - from: "src/components/PhotoSourcePicker.tsx"
      to: "src/services/photos/url-image.ts"
      via: "Add from URL → downloadImageToCache → navigate CropPhoto"
      pattern: "downloadImageToCache"
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/db/profile-dao.ts"
      via: "self photo target → setProfilePhoto through the crop pipeline"
      pattern: "profile"
---

<objective>
Add the two remaining capture surfaces that reuse the same pipeline: the pasted-URL path and the user's own (self record) photo.

Phase Goal (user story): **As a** user, **I want to** paste an image URL or set my own photo **so that** I can add a picture even when it isn't in my gallery, and personalise my own record — all landing as the same local master.

`url-image.ts` re-ports ONLY the plugin `ImageScraper`'s download + content-type→extension logic (as `fetch`/`downloadFileAsync`, dropping the wikilink/vault/conflict logic — no vault on mobile). It is a one-time WRITE path: it downloads once to cache, then feeds the identical crop → manipulate → persist pipeline. Reads are never network (local-first §3 intact). Security is the load-bearing part: only `https` URLs are accepted (reusing the Phase-4 positive-allowlist posture) — `http://`, `file://`, `intent://`, `javascript://` and non-image content are rejected with the SPEC error copy, closing the SSRF/cleartext surface. The self-record photo adds a "Your photo" entry in Settings that drives the same `PhotoSourcePicker`/crop flow against the `profile` target. Setting AND removing the self photo both route through Plan 05's fully target-kind-aware picker — the `profile` Remove branch (clearProfilePhoto + delete) already exists there, so Settings does not need (and must not add) its own Remove logic.

Purpose: PHOTO-02 (URL download-once) + the self-record half of PHOTO-01.
Output: `url-image.ts` (+ test), the URL submit wired into `PhotoSourcePicker`, and the Settings self-photo entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-photos/05-RESEARCH.md
@.planning/phases/05-photos/05-PATTERNS.md
@.planning/phases/05-photos/05-UI-SPEC.md
@src/screens/SettingsScreen.tsx
@src/components/PhotoSourcePicker.tsx
</context>

## Artifacts this plan produces

- `url-image.ts`: `isImageUrl(url)` (https-only + extension/shape check), `extFromContentType(ct)`, `downloadImageToCache(url)` → raw cache uri; ported `CONTENT_TYPE_MAP` / `VALID_IMAGE_EXTENSIONS` / `DEFAULT_EXTENSION`.
- URL submit path in `PhotoSourcePicker` (Add from URL → download → `navigate('CropPhoto', ...)`).
- Settings "Your photo" entry driving `PhotoSourcePicker` with the `profile` target.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: url-image.ts — https-only validate + content-type→ext + download-once</name>
  <files>src/services/photos/url-image.ts, src/services/photos/url-image.test.ts</files>
  <read_first>
    - .planning/phases/05-photos/05-RESEARCH.md (Architecture Pattern 3 URL download; Security Domain — V5 input validation, SSRF-ish / DoS mitigations; State of the Art — requestUrl→fetch/downloadFileAsync)
    - .planning/phases/05-photos/05-PATTERNS.md ("url-image.ts" — re-port ONLY CONTENT_TYPE_MAP/getExtensionFromContentType/getExtensionFromUrl/VALID_IMAGE_EXTENSIONS/DEFAULT_EXTENSION; DROP requestUrl/wikilink/vault/conflict logic)
    - ~/projects/Orbit/src/utils/ImageScraper.ts (reference only — the extension mapping to re-port)
    - STATE.md decision "04-07: link-open uses a POSITIVE https?:// allowlist" — reuse the https-only posture here (this phase is stricter: https only, no http)
  </read_first>
  <behavior>
    - `isImageUrl("https://x.com/a.jpg")` → true; `isImageUrl("http://x.com/a.jpg")` → false (no cleartext); `isImageUrl("file:///etc/passwd")`, `"intent://…"`, `"javascript:…"`, `"ftp://…"` → false; a non-URL string → false.
    - `extFromContentType("image/png")` → `"png"`; `"image/jpeg"` → `"jpg"`; `"image/webp"` → `"webp"`; unknown/absent → the default extension.
    - `downloadImageToCache` only proceeds for an `isImageUrl`-passing https URL, re-validates the FINAL resolved URL after redirects is still https + image (a redirect to `http://`/`file://`/another scheme is rejected), validates the response `content-type` is `image/*`, and writes into a cache subdir (never the document dir directly).
  </behavior>
  <action>
    Create `url-image.ts`. Re-port the pure pieces from `ImageScraper.ts`: the `CONTENT_TYPE_MAP`, `extFromContentType` (content-type→extension with the `.webp`/default fallback), an extension-from-url helper, and `VALID_IMAGE_EXTENSIONS`/`DEFAULT_EXTENSION`. Add `isImageUrl(url)` as an https-ONLY positive allowlist: parse the URL, require `protocol === 'https:'` (reject http/file/intent/javascript/ftp and unparseable input), and require an image-ish extension OR let content-type validation at download time decide.
    Implement `downloadImageToCache(url)` (addresses review concern [codex/MED 05-06 content-type + redirect policy]): DO NOT use `File.downloadFileAsync` for the transfer — verified against SDK-57 docs, its return value exposes neither the response headers nor the final resolved URL after redirects, so it cannot enforce a content-type or a redirect policy. Instead use `fetch(url)` (which follows redirects and exposes both): (1) `isImageUrl(url)` on the submitted URL first; (2) `await fetch(url)`; (3) re-validate the REDIRECT-RESOLVED final URL with `isImageUrl(response.url)` — this closes the SSRF/cleartext gap where a redirect lands on `http://`/`file://`/another host-scheme (the submitted-scheme check alone does not); (4) require `response.ok`; (5) require the `content-type` header to be `image/*` (map via `extFromContentType` for the extension); (6) bound DoS by rejecting an oversized `content-length` (a sane cap, e.g. a few MB) before reading the body; (7) write the validated bytes into `new Directory(Paths.cache, 'photo-dl')` via the SDK-57 `File` write API (confirm the exact `File.create`/`File.write(Uint8Array)` signature against the installed `.d.ts`). Reject any failure with a thrown error the UI maps to "That doesn't look like an image URL." (invalid/redirect-rejected) / "Couldn't fetch that image." (network) / "That image couldn't be used." (content-type/decode). It returns the raw cache uri — from there the flow is identical to a picked image.
    Write `url-image.test.ts` covering the PURE parts: `isImageUrl` accepts https image URLs and rejects http/file/intent/javascript/ftp/garbage, and `extFromContentType` maps jpeg/png/webp/unknown. Do NOT unit-test the native download (device UAT).
  </action>
  <verify>
    <automated>npm test -- src/services/photos/url-image.test.ts && npm run check:colors src/services/photos/url-image.ts</automated>
  </verify>
  <acceptance_criteria>
    - `isImageUrl` is https-only and rejects http/file/intent/javascript/ftp/non-URL input.
    - `extFromContentType` maps the common image types with a default fallback.
    - `downloadImageToCache` uses `fetch` (not `downloadFileAsync`), re-validates the final redirect-resolved `response.url` is still https via `isImageUrl`, requires an `image/*` content-type + `response.ok`, bounds size by `content-length`, and writes to a cache subdir (not the document dir).
    - `url-image.test.ts` passes for all accept/reject and mapping cases.
    - `npm run check:colors src/services/photos/url-image.ts` green.
  </acceptance_criteria>
  <done>The ported URL logic downloads once on the write path, accepts only https image URLs, and is node-tested for the accept/reject allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: wire Add-from-URL into PhotoSourcePicker + Settings "Your photo"</name>
  <files>src/components/PhotoSourcePicker.tsx, src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/components/PhotoSourcePicker.tsx (from Plan 05 — the "Paste image URL" entry point placeholder to complete; the target-kind-aware navigate pattern incl. the customField-only requestId = customFieldPhotoRelPath; and the existing Remove switch — do NOT re-touch it)
    - .planning/phases/05-photos/05-UI-SPEC.md (URL entry state: themed input `surface`/`border`, `https://…` placeholder, "Add from URL" 44px; error copy for invalid/failed/decode)
    - src/screens/SettingsScreen.tsx (the existing rows — Custom Fields, Archived — and the row/navigate pattern to add a "Your photo" entry; it is a STATIC screen today, so add `useState` + a `useFocusEffect` reload; see the Plan 04-01 two-row note)
    - src/screens/ContactProfileScreen.tsx (lines 21, 73-77 — the `useFocusEffect` reload-on-focus pattern to mirror)
    - src/db/profile-dao.ts (from Plan 02 — `getProfile(exec)` → `{ name, photo, modified_at }` to seed the self avatar name + photo + cache-bust) + src/components/Avatar.tsx
  </read_first>
  <action>
    In `PhotoSourcePicker.tsx`, complete the "Paste image URL" affordance (the entry-point placeholder from Plan 05): a themed `TextInput` (`surface` bg, `border`, `https://…` placeholder) + an "Add from URL" submit (44px, accent). On submit, `isImageUrl` first (invalid → "That doesn't look like an image URL."), then `downloadImageToCache` (network failure → "Couldn't fetch that image."; decode/content-type failure → "That image couldn't be used."), then navigate to the crop screen reusing Plan 05's requestId convention — thread `requestId` only for a customField target: `navigation.navigate('CropPhoto', { rawUri, target, requestId: target.kind === 'customField' ? customFieldPhotoRelPath(target.contactId, target.colName) : undefined })` — identical crop→pipeline flow. This task only ADDS the URL affordance to the already-target-kind-aware picker from Plan 05; it does NOT re-touch the Remove switch or the library-launch path. Copy verbatim from the UI-SPEC Copywriting Contract; all colours tokenised.
    In `SettingsScreen.tsx` (currently a static screen — no state/effects), add a "Your photo" row/section. Seed via `getProfile(getExecutor())` → `{ name, photo, modified_at }` into local state (`selfPhoto`, `selfName`, `selfModifiedAt`), and reload on focus with `useFocusEffect` (mirroring `ContactProfileScreen`) so a self-photo set/remove refreshes when the crop screen `goBack()`s. Render `<PhotoSourcePicker target={{kind:'profile'}} photo={selfPhoto} name={selfName ?? 'You'} cacheBust={selfModifiedAt} onChanged={reloadProfile} />`. Addresses review concern [codex/MED 05-06 self name]: the id=1 profile seed row has a NULL `name` (no self-name editor ships until a later phase), so `getProfilePhoto` alone would always yield a blank swatch — use `getProfile` for a real name source and a stable `'You'` fallback so the self initials avatar is deterministic ("Y" on a stable swatch) rather than always blank. Addresses review concern [claude/MED stale cache]: pass `cacheBust={selfModifiedAt}` so replacing the self photo (fixed `avatars/profile.jpg` path) visibly refreshes. The self record photo uses the exact same picker/URL/crop pipeline and `setProfilePhoto` write; the profile-target Remove (clearProfilePhoto + deletePhoto(profilePhotoRelPath())) is already provided by Plan 05's target-kind-aware picker, so the Settings "Remove photo" works without a contactId and without any change to the picker here (Remove's `onChanged` → `reloadProfile` refreshes immediately). Keep the existing Custom Fields / Archived rows intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/components/PhotoSourcePicker.tsx src/screens/SettingsScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "Add from URL" validates https-only, downloads once, and routes to the crop screen; the three error states use the SPEC copy.
    - Settings shows "Your photo" seeded from `getProfile` (name+photo+modified_at) with a stable `'You'` name fallback (never a permanently blank swatch) and `cacheBust={selfModifiedAt}`; it reloads on focus; setting uses the profile-target pipeline and removing uses Plan 05's profile Remove branch (no contactId crash).
    - Copy verbatim from the UI-SPEC; all colours tokenised; 44px targets.
    - `npx tsc --noEmit` green; `check:colors` green on both files.
    - On-device (manual UAT): pasting an https image URL downloads → crop → saves the master; an http/invalid URL shows the invalid-URL copy; the self photo sets/removes from Settings.
  </acceptance_criteria>
  <done>The URL path and the self-record photo both reuse the one pipeline, https-only, with SPEC error copy and tokenised UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted URL → network fetch | user-supplied URL is fetched once on the write path |
| remote bytes → filesystem | downloaded content is validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Information disclosure | pasted-URL fetch (SSRF / cleartext) | high | mitigate | `isImageUrl` is an https-ONLY positive allowlist (rejects http/file/intent/javascript/ftp) applied to BOTH the submitted URL AND the redirect-resolved final `response.url` (a redirect to http/file/other scheme is rejected — not just the submitted scheme); content-type must be `image/*`; `content-length` capped; fetch is user-initiated, one-time, write-path only; NO fetch on any read path (local-first §3 preserved) |
| T-05-03 | Denial of service | malicious/huge remote image, decode bomb | medium | mitigate | download to evictable cache (not document) and validate content-type before use; the 512px re-encode bounds output; decode failure → SPEC copy, no crash |
</threat_model>

<verification>
- `npm test -- src/services/photos/url-image.test.ts` green.
- `npx tsc --noEmit` green.
- `npm run check:colors` green on url-image, PhotoSourcePicker, SettingsScreen.
- On-device (manual UAT): https URL → crop → save; http/invalid → invalid-URL copy; self photo set/remove from Settings.
</verification>

<success_criteria>
Pasting an https image URL downloads once to the same local 512px master (http/file/other rejected with SPEC copy, reads never network); the self record photo is settable/removable from Settings through the identical pipeline.
</success_criteria>

<output>
Create `.planning/phases/05-photos/05-06-SUMMARY.md` when done.
</output>
