---
phase: 18.2-bound-unbound-lifecycle
plan: 10
type: execute
wave: 6
depends_on: [18.2-04, 18.2-05, 18.2-06, 18.2-07, 18.2-08, 18.2-09]
files_modified: [.planning/phases/18.2-bound-unbound-lifecycle/18.2-VALIDATION.md, src/db/lifecycle-consumer-ledger.test.ts, src/db/contact-lifecycle-dao.test.ts, src/db/ai-context-read.test.ts, src/db/digest-read.test.ts, src/db/contact-status-read.test.ts, src/services/impact.test.ts]
autonomous: false
requirements: [CDN-02, CDN-03, CDN-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Automated tests cover every 18.2 requirement's lifecycle, query-owner, nullable-cadence consumer, and lifecycle-restore invariants."
    - "The lifecycle UAT (bind/unbind/rebind, Unbound surfaces, Bound-only Orrery/widget/notifications, lifecycle backup round-trip) is completed on a real upgraded Android build."
    - "No PII method data reaches AI context or routine logs; explicit person-level AI remains available for Unbound contacts, and all OS handoff is actionability-gated."
  artifacts: [.planning/phases/18.2-bound-unbound-lifecycle/18.2-VALIDATION.md, src/db/ai-context-read.test.ts]
  key_links: ["The final 18.2 ledger maps every nullable-cadence and orbiting/favourite-set consumer to its owner test; the release build applies v11 and exercises lifecycle DAO/UI/widget/notification/Orrery refresh and lifecycle backup round-trip against the completed model."]
  prohibitions:
    - "Phase 18 must not ship picker/import/reconciliation, generic sync, rich merge management, passive listener detection, reorder polish, Interaction Assist, endpoint history, or provider analytics."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-08-PLAN.md` — **Task 2** (owner-led Android UAT, scoped to lifecycle) plus a **18.2 automated-verification task** covering the lifecycle/query consumers.

**In 18.1-06:** the migration + method-read + method-backup automated ledger and the owner-led Android **migration** UAT. This plan covers **lifecycle behavior end-to-end**: the nullable-cadence/orbiting/favourite-set consumer ledger, explicit-Unbound AI / status / digest / impact coverage, and the on-device Bind/Unbind, Unbound-surface, Orrery/widget/notification, and lifecycle-backup UAT.

**Streamlined Export is deferred out of Phase 18 entirely** (per the split plan, an owner decision) and is NOT part of this UAT.

<objective>Prove the 18.2 lifecycle half on its complete automated suite and a real Android lifecycle UAT.
Purpose: backstop state-space and device-only behavior that a node SQLite or RN logic test cannot establish for the Bound/Unbound lifecycle.
Output: completed lifecycle edge audit and owner-approved lifecycle UAT record.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-VALIDATION.md
@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/18.2-bound-unbound-lifecycle/18.2-01-SUMMARY.md
@.planning/phases/18.2-bound-unbound-lifecycle/18.2-05-SUMMARY.md</context>

## Artifacts this phase produces

- Requirement-mapped regression coverage and lifecycle/nullable-cadence consumer audit.
- Owner-reviewed Android lifecycle UAT evidence.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove lifecycle behavior across query owners, nullable cadence, AI, and lifecycle restore</name>
  <read_first>18-VALIDATION.md; 18-RESEARCH.md Validation Architecture; src/db/contact-lifecycle-dao.test.ts; src/backup/restore-apply.test.ts</read_first>
  <files>.planning/phases/18.2-bound-unbound-lifecycle/18.2-VALIDATION.md, src/db/lifecycle-consumer-ledger.test.ts, src/db/contact-lifecycle-dao.test.ts, src/db/ai-context-read.test.ts, src/db/digest-read.test.ts, src/db/contact-status-read.test.ts, src/services/impact.test.ts</files>
  <behavior>
    - Requirement test matrix includes CDN-02 boundary/adjacency/empty/ordering/precision; CDN-03 boundary/precision including the shared Orrery sun predicate; CDN-04 lifecycle-restore boundary/precision/idempotency/concurrency.
    - An evidence-backed rg ledger — implemented as a committed CI-enforceable test (`src/db/lifecycle-consumer-ledger.test.ts`), not a Markdown table — maps every nullable `interval_days` consumer and orbiting/favourite-set predicate consumer to its owning task/test, records benchmark.ts as an audited positive-cadence fragment consumer, and fails on an unassigned reference outside explicitly listed fixtures.
    - `ai-context-read.test.ts` proves an explicitly requested Unbound contact can produce normal permitted context with neither raw nor canonical methods in the payload and the neutral intensity fallback.
    - Unbound status/digest/impact boundary assertions prove no Unbound-but-contacted contact is labeled stable or included in active-orbit summaries.
  </behavior>
  <action>Fill the Phase 18.2 validation matrix with final plan/task/wave ownership and add an evidence-backed rg ledger. The ledger must enumerate every nullable `interval_days` consumer and orbiting/favourite-set predicate consumer found in production, name its migrated owner task/test, and record benchmark.ts as audited because its writer supplies a positive cadence and its STATUS_SCAN use inherits the updated fragment precondition. The orbiting/favourite section must include dashboard/default and Never Contacted projections, `orrery-read`, `ring-seq-dao` count/update guards, `sun-picker-read`, digest, notification, widget, and all favourite projections, documenting their exact Bound predicates or neutral-read exception. The nullable-`interval_days` section must additionally name the shared status/`PROGRESS_SQL` fragment consumers explicitly — `contact-status-read.ts`, `decay-suppression.ts`, and `digest-read.ts` — as ledger owners, rather than relying on a broad `interval_days` ripgrep that could miss the fragment reached via `@/db/status` import. Implement this audit as a committed, CI-enforceable test (`src/db/lifecycle-consumer-ledger.test.ts`) that ripgreps the production source for every nullable-`interval_days` and orbiting/favourite-set predicate consumer and asserts each maps to an owner task/test or an explicit allowlist entry, failing on any unmapped reference — a Markdown table alone cannot fail CI. Keep the human-readable ledger in 18.2-VALIDATION.md in sync with that test as documentation, not as the enforcement mechanism. Narrowly scope the ripgrep to production source files (exclude migrations, comments, and tests) with an explicit syntax inventory of the fragments it matches, so the gate is not brittle. Add a nullable-cadence TypeScript-contract sweep asserting every cadence-bearing type agrees with the v11 DDL: `ImpactInputs.intervalDays`, `ContactEditRow.interval_days`, `CreateContactFullInput`/`UpdateContactFullInput.intervalDays`, and `DashboardRow`'s cadence field are all nullable. Add a backstop test asserting profile-initiated Compose stays reachable for a live Unbound contact (explicit person-level action is permitted for Unbound, per dossier Cluster N), distinct from the widget Compose fail-closed guard. Extend the named AI-context, status, digest, and impact test owners: seed explicit Unbound AI with raw/canonical methods, assert the permitted prompt context remains available without either value and uses the neutral intensity fallback; add boundary assertions for Unbound status/digest and dormant/never-assigned impact. As immediate feedback, run the named targeted tests first; only after they pass, run the full suite, TypeScript, and colors gate. Treat the spec-less edge probe as a coverage ledger: write defensible automated predicates, retain device-only items as explicit backstops, and flag no unresolved material assumptions. Preserve D-02 through D-04 and scope fences. This addresses cycle-2 review M5 and Cycle 3 C3-H1. The scalar-method consumer ledger and method-backup coverage are owned by 18.1-06.</action>
  <verify><automated>First run `npm test -- src/db/lifecycle-consumer-ledger.test.ts src/db/ai-context-read.test.ts src/db/contact-lifecycle-dao.test.ts src/db/digest-read.test.ts src/db/contact-status-read.test.ts src/services/impact.test.ts`; only after it passes, run `npm test && npx tsc --noEmit && npm run check:colors`.</automated></verify>
  <acceptance_criteria>CDN-02, CDN-03, and CDN-04 lifecycle behavior has executable evidence; the named targeted suite passes before the full regression gate; the committed ledger test (`src/db/lifecycle-consumer-ledger.test.ts`) proves every nullable-cadence and orbiting/favourite-set consumer is migrated or explicitly fixture-allowlisted and fails CI on an unmapped reference; explicit Unbound AI remains available without raw/canonical method egress or nullable-cadence arithmetic; and all requirement-specific quick commands pass.</acceptance_criteria>
  <done>The completed lifecycle codebase has one traceable automated verification path for every Phase 18.2 truth.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perform owner-led Android lifecycle UAT</name>
  <read_first>18-VALIDATION.md Manual-Only Verifications; 18-UI-SPEC.md UI Considerations; docs/runbooks/desktop-build-pipeline.md</read_first>
  <action>Build the release APK through the documented desktop pipeline over the already-migrated v10 method+label build, apply the v11 lifecycle migration, and prepare the named test contacts/settings. Preflight-verify (do not assume) that the device is at the v10 method+label baseline before applying v11 by reading and recording the observed `user_version` as a UAT artifact. This validates D-02 and D-03 on device: the Bound/Unbound lifecycle end to end, Unbound surfaces, Bound-only proactive surfaces, and lifecycle backup round-trip. The migration + method + method-backup UAT is owned by 18.1-06; Streamlined Export is deferred out of Phase 18.</action>
  <what-built>Full Bound/Unbound lifecycle, Unbound screens/settings, proactive-surface gating, and lifecycle backup/restore behavior are automated and ready on a release build.</what-built>
  <how-to-verify>On the Pixel: apply the v11 migration over migrated v10 data; create/edit contacts choosing Bound and Unbound; unbind and rebind contacts with dormant and never-assigned cadence; verify Unbound dashboard footer/list/search/profile and Bound-only Orrery/favourites/widget/decay/notifications; exercise lifecycle settings (Never Contacted opt-in, birthday-Unbound, phone region); confirm notifications/widgets refresh after Unbind and stale widget quick actions fail closed; run full backup export then restore and confirm lifecycle (tracking_enabled/nullable cadence) round-trips; inspect 320dp overflow, long text, keyboard scroll, loading/error retry, and no raw canonical/source fields. Approve only if no history/cadence/rank loss and no deferred import/reconciliation UI appears.</how-to-verify>
  <resume-signal>Reply approved, or provide the failing behavior and device evidence.</resume-signal>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Owner records the release-build result for every lifecycle manual-only validation item and UI backstop.</acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release build to real device database | The v11 migration and lifecycle UI execute against user-like persisted relationship data. |
| actionability read to Android SMS | Device handoff can expose or use a contact endpoint. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-24 | Tampering/DoS | release migration | high | mitigate | Real v10-to-v11 upgrade UAT after fixture/rollback tests and backup verification. |
| T-18-25 | Information disclosure | UI/AI/logs | high | mitigate | Device check plus automated absence/actionability assertions; no canonical/raw source display. |
| T-18-27 | Tampering | source evidence | medium | mitigate | UAT confirms no source-based delete/overwrite control is present. |
| T-18-SC | Tampering | package provenance | high | mitigate | Confirm the 18.1-01 blocking human approval remains recorded. |
</threat_model>
<verification>Run the named targeted Task 1 suite as immediate feedback, then run the full suite before release UAT; final human checkpoint records device-only predicates and unresolved defects for the lifecycle half.</verification>
<success_criteria>Every 18.2 requirement is code-proven and the owner has seen the real Android lifecycle behavior.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-10-SUMMARY.md` when done</output>
