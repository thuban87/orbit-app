---
phase: 18.2-bound-unbound-lifecycle
doc: cross-ai-convergence-log
reviewers: [codex, cursor]
max_cycles: 3
started: 2026-08-28
note: Custom convergence run emulating gsd-plan-review-convergence with codex + cursor via gsd-review (the convergence skill errors on --cursor). Mechanism per cycle chosen by finding volume — scalpel (hand-edit plans) for localized findings, gsd-plan-phase --reviews hammer only if a structural re-cut is warranted.
---

# Phase 18.2 — Cross-AI Convergence Log

## Cycle 1 — 2026-08-28

- **Reviewers:** codex (`gpt-5.6-terra`, reasoning=low), cursor (model unrecorded). Both source-grounded against on-disk repo.
- **Review artifact:** `18.2-REVIEWS.md` (commit `f61f3cc`).
- **Raised:** 6 HIGH, 14 actionable non-HIGH.
- **Mechanism:** scalpel — every finding was a localized edit to an individual PLAN.md (renumber, ownership, enumeration, executable ledger), none required re-deriving the wave structure. Hand-edited the affected plans to preserve the careful 18-split finding assignments (C3-*, H3, "moves no data").

### HIGH disposition

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | Migration `010`/user_version 10 collision (18.1 shipped `010-contact-method-label`) | **Incorporated** — renumbered lifecycle migration to `011-contact-lifecycle-schema` / v11 and rewrote every v9→v10 chain reference to v10→v11 across plans 01/02/06/07/09/10 + CONTEXT. |
| 2 | Contacts-rebuild child-FK inventory incomplete (post-009 `contact_methods`/`external_contact_links`/`contact_method_provenance` missing) | **Incorporated** — Plan 01 now enumerates the full v11 child set, ports/expands migration 009's `assertExpectedChildren`, and requires per-child FK/index/trigger/FK-action proofs (not `foreign_key_check` alone). |
| 3 | Aggregate create/edit DAO lifecycle ownership unassigned | **Incorporated** — Plan 08 Task 1 now owns `contacts-dao.ts`/`.test.ts`, claims the `createContactFull`/`updateContactFull` lifecycle-parameter extension, and adds the four-lifecycle-cell tests. |
| 4 | Plan 08 missing `depends_on: [18.2-04]` | **Incorporated** — added 18.2-04 to Plan 08 `depends_on`. |
| 5 | Backup wire-format not versioned (adds fields to v2 without a bump) | **Owner decision → Incorporated** — bump to `BACKUP_FORMAT_VERSION = 3` + new v2→v3 forward migration (Plan 09). Enforces the `types.ts:13` contract; resolves the codex/cursor divergence in codex's favour. |
| 6 | Backup merge trigger-violation can't be rejected at parse time (needs local row) | **Incorporated** — Plan 09 Task 2 split into manifest-only parse validation vs a distinct pre-transaction merge-planning phase that reads the local row and resolves conflicts deterministically. |

### Actionable non-HIGH disposition

| Finding | Disposition |
|---------|-------------|
| `reserved-columns.ts` drift (retired scalar phone/email; test chained to v6) | **Incorporated** — Plan 01: add `tracking_enabled`, drop retired columns, chain test to v11. |
| Consumer ledger must be executable, not a Markdown table | **Incorporated** — Plan 10: committed CI-enforceable `src/db/lifecycle-consumer-ledger.test.ts`. |
| Plan 08 Task 1 verify is `tsc --noEmit` only | **Incorporated** — Plan 08: DAO + create/edit-logic tests added to verify. |
| Plan 06 "fail closed" scope ambiguous | **Owner decision → Incorporated** — active-cadence actions fail closed; Profile-open still navigates for a live Unbound contact (Plan 06 Task 3). |
| Plan 03 `countLiveContacts`/favourites need Bound predicates + parity tests | **Incorporated** — Plan 03: per-owner `tracking_enabled = 1` SQL-fragment parity tests. |
| Plan 03 search must return Unbound rows with NULL status/favourite | **Incorporated** — Plan 03: asserted in tests. |
| Plan 05 must not import dashboard snooze exclusion into Orrery predicate | **Incorporated** — Plan 05 Task 2 action. |
| Plan 06 widget favourites path depends on 18.2-03 | **Already satisfied** — Plan 06 `depends_on` already includes 18.2-03 (cursor self-corrected). |
| Plan 07 Task 2 too large — split DAO/policy from Settings UI | **Deferred (not actioned)** — plan-sizing is explicitly OUT of the review loop per `18-SPLIT-PLAN.md` anti-spiral rules; not a correctness/safety/ownership finding. |
| Plan 09 `export-manifest.ts`/`restore-apply.ts` lack `tracking_enabled`; sequence shared files | **Covered** — those files are Plan 09-owned; the v3 rework carries them, sequenced after the committed 18.1-04 method graph. |
| Plan 03 digest `readRetrospective`/`readGentleLine` policy | **Incorporated** — pinned as all-relationship-history with a test (Plan 03). |
| Plan 07 `birthday_unbound_enabled` changes only notification eligibility | **Incorporated** — asserted in Plan 06 Task 1 acceptance (dashboard birthday population stays independent). |
| Plan 02 Capture should persist `tracking_enabled=1` via DDL default | **Incorporated** — Plan 02 Task 2 acceptance. |
| Plan 10 UAT text "v10 over v9" → "v11 over v10" | **Incorporated** — via the renumber. |

**Owner-bucket items surfaced this cycle:** backup format version (→ v3), widget fail-closed scope (→ Profile view allowed). Both recorded as LOCKED in `18.2-CONTEXT.md`.

## Cycle 2 — 2026-08-28

- **Reviewers:** codex (`gpt-5.6-terra`, reasoning=low), cursor. Both source-grounded; both confirmed the cycle-1 re-cut correctly implements the two locked owner decisions and the v11 numbering, and neither reopened them.
- **Review artifact:** `18.2-REVIEWS.md` (commit `d351bd4`).
- **Raised (remaining after cycle 1):** 3 HIGH, 10 actionable — down from 6/20; unresolved total 13 vs 20, converging, not stalled.
- **Mechanism:** scalpel — all localized plan edits.
- **Notable:** cycle 2 caught that a cycle-1 edit was itself wrong — Plan 01 had lumped `contact_method_provenance` in with the direct contacts-FK children, but it references `contact_methods`, not `contacts`. Corrected. (Convergence catching an introduced regression.)

### HIGH disposition

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | Plan 01 FK inventory conflates direct vs transitive children (`contact_method_provenance` targets `contact_methods`, not `contacts`) | **Incorporated (corrected cycle-1 error)** — Plan 01 now enumerates the 8 direct contacts-FK children (six 009 children + `contact_methods` + `external_contact_links`) with per-child FK-target/action proofs and verifies the transitive `contact_method_provenance` undisturbed. |
| 2 | Plan 09 restore merge-conflict resolution rule left unspecified | **Owner decision → Incorporated** — retain-dormant-cadence rule: restore never nulls an assigned cadence (`interval_days = COALESCE(incoming, local)`), applies the modified-at winner's `tracking_enabled`, never trips the v11 trigger. Honors the recorded cadence-once-assigned-never-cleared invariant. |
| 3 | Bind lacks symmetric post-commit lifecycle effects (Plan 06/08) | **Incorporated** — added `bindWithLifecycleEffects` (Plan 06) re-arming decay + widget refresh, and routed Plan 08's Bind dispatch through it. |

### Actionable non-HIGH disposition

| Finding | Disposition |
|---------|-------------|
| Plan 02 — no explicit `modified_at`/`data_revision` bump on Bind/Unbind | **Incorporated** — each successful Bind/Unbind bumps `modified_at` + increments `data_revision` exactly once. |
| Plan 03 — projection test not exhaustive across branches | **Incorporated** — exhaustive DashboardRow projection test across default/favourites/snoozed/search/Never-Contacted. |
| Plan 06 — DB guard not provable through WidgetLinkingGate (no DI seam) | **Incorporated** — extract async `guardWidgetIntent(intent, lookup)` with injected lookup, unit-tested directly. |
| Plan 08 — aggregate update writes `interval_days` unconditionally (Bound→Unbound edit nulls cadence, trips trigger) | **Incorporated** — distinguish transition vs edit; retain dormant cadence on Bound→Unbound; regression test. |
| Nullable-cadence TypeScript-contract sweep | **Incorporated** — added to Plan 10 (ImpactInputs/ContactEditRow/Create+UpdateContactFullInput/DashboardRow all nullable). |
| Plan 01 reserved-columns still lists retired phone/email; test chained to v6 | **Already incorporated (cycle 1)** — Plan 01 already adds `tracking_enabled`, drops retired columns, chains test to v11. |
| Plan 10 — ledger ripgrep scope brittle; baseline assumed; no profile-Compose backstop | **Incorporated** — narrow production-file scope + syntax inventory; v10 baseline as UAT preflight; backstop that profile-initiated Compose stays reachable for a live Unbound contact (distinct from widget fail-closed). |
| Plan 07 — Never Contacted subquery should read single `id=1` settings row explicitly | **Incorporated** — reads the guaranteed single row, fails consistently with getAppSettings. |
| Plan 01 reversibility copy typo ("Migration 010") | **Incorporated** — corrected to 011. |
| Doc/flag cleanups (Plan 09 stale fold flag; CONTEXT:13 "v10"; Plan 08 early repo-wide tsc) | **Incorporated** — fold flag retired; CONTEXT:13 → v11; Plan 08 verify now runs targeted tests only (repo-wide gate stays 18.2-10). |

**Owner-bucket items surfaced this cycle:** restore merge-conflict rule (→ retain dormant cadence). Recorded in CONTEXT.

## Cycle 3 (final) — 2026-08-28

- **Reviewers:** codex (`gpt-5.6-terra`, reasoning=low), cursor. Both source-grounded; both confirmed all three locked owner decisions are correctly implemented and every cycle-1/2 finding is incorporated. No decisions reopened.
- **Review artifact:** `18.2-REVIEWS.md` (commit `c0be95e`).
- **Raised (remaining):** 1 HIGH, 4 actionable — down from 3/10; unresolved total 5. Trend across cycles: **20 → 13 → 5** (monotonic, no stall).
- **Mechanism:** scalpel. **No owner decisions this cycle** — all findings were my-bucket, localized fixes.

### Disposition

| Sev | Finding | Disposition |
|-----|---------|-------------|
| HIGH | Plan 09 v3 export not lossless — `export-manifest.ts:48` selects `interval_days` but not `tracking_enabled`, and no plan owned that file → v3 export silently drops Bound/Unbound state (fails CDN-04) | **Incorporated** — added `export-manifest.ts`(+test) to Plan 09 `files_modified`/`<files>`/verify and specified `tracking_enabled AS trackingEnabled` in the export projection with a lossless round-trip test. (This is the cycle-1 "export/restore tracking_enabled" item I wrongly marked "covered" — cycle 3 correctly escalated it.) |
| MED | Plan 09 settings-snapshot wave ordering — snapshot fields owned by Plan 07 (wave 4), Plan 09 was wave 3 | **Incorporated** — Plan 09 `depends_on` gains 18.2-07; wave 3 → 5. |
| MED | Plan 09 verification omits the export suite | **Incorporated** — `export-manifest.test.ts` added to Plan 09 verify. |
| LOW | Plan 10 ledger under-names status/`PROGRESS_SQL` fragment consumers (embedded via import in `decay-suppression`/`digest-read`/`contact-status-read`) | **Incorporated** — enumerated explicitly as ledger owners. |
| LOW | Plan 01 full-chain migration test not declared — `full-chain.test.ts` derives from `TARGET_VERSION` | **Incorporated** — added to Plan 01 `files_modified`/`<files>`/action (assert `TARGET_VERSION === 11`). |

---

## Cycles 1–3 recap — 2026-08-28

Three review→re-cut cycles with codex + cursor. **Review-verdict counts (findings raised per review): 20 → 13 → 5.** Each cycle's findings were incorporated. **Correction to an earlier framing:** there was no "→ 0" review round at cycle 3 — the 0 was only the count of cycle-3 findings left unincorporated *after* fixes, never a reviewer verdict. Cycle-3's fixes were not independently re-reviewed at that point.

**Owner decisions locked (in `18.2-CONTEXT.md`):** backup format v3 + v2→v3 migration; widget fail-closed scope; restore merge rule (retain dormant cadence).

The owner then requested a genuine 4th round to validate cycle-3's fixes and earn a real reviewed-0.

## Cycle 4 — 2026-08-28

- **Reviewers:** codex + cursor, source-grounded (deeper pass, ~13 min). Both confirmed the locked decisions are correctly implemented; neither reopened them.
- **Review artifact:** `18.2-REVIEWS.md` (commit `9abc531`).
- **Raised:** 1 HIGH, 6 actionable = 7. **The count ROSE from 5** — cycle 4 probed the notification-ingress and intensity-contract surfaces earlier cycles never reached, surfacing pre-existing gaps (not regressions from cycle-3 fixes). **Honest review trend: 20 → 13 → 5 → 7 (not monotonic).** A fixed number of cycles does not guarantee a reviewed-0.
- **Adjudicated as not-counted (verified against source):** cursor's `readOverlooked` HIGH is already owned by Plan 03 Task 2; two cursor MEDIUMs (PROGRESS_SQL fragment consumers, ContactEditRow nullable sweep) already covered by Plan 10.
- **Mechanism:** scalpel.

### Disposition

| Sev | Finding | Disposition |
|-----|---------|-------------|
| HIGH | Notification ingress unowned — `notification-nav.ts:90-97` routes a decay tap to Compose unconditionally; `notification-actions.ts` ACTION_MARK writes a touchpoint with no lifecycle check; no plan owned either file | **Owner decision → Incorporated** — fail closed (mirror widget). New Plan 06 Task 4 owns `notification-nav`/`notification-actions`(+tests): decay tap fails closed for now-Unbound, birthday still opens Profile, Mark records history but doesn't re-arm cadence. Plan 10 UAT step + ledger entry added. |
| MED | Plan 06 birthday-eligibility read underspecified (getAppSettings lacks lifecycle fields; notification-read ignores settings) | **Incorporated** — read `birthday_unbound_enabled` from the singleton `id=1` row explicitly, loud-fail on missing/corrupt. |
| MED | Plan 04/08 "intensity unavailable" contract not pinned (`ai-context-read` fabricates `intervalDays:1`) | **Incorporated** — explicit tagged `{available:false}` result (never null→0, never fabricated cadence), consumed by profile (08) and ai-context-read. |
| MED | Plan 08 Edit-path lifecycle effects use a different seam than Profile | **Incorporated** — acceptance asserts Edit Bound→Unbound/Unbound→Bind fires the same reconcile+widget-refresh effects, post-commit, non-rollback. |
| MED/LOW | Plan 07 portable-settings round-trip not asserted (Plan 09 depends on it) | **Incorporated** — round-trip `includeUnboundNeverContacted`/`birthdayUnboundEnabled` through getPortableSettingsSnapshot/AppSettingsPatch. |
| LOW/MED | Plan 01 v11 cadence CHECK permits REAL `1.5` | **Incorporated** — `typeof(interval_days)='integer'` added to the CHECK + fractional-rejection test. Defense-in-depth (unreachable by app code today). |
| LOW | Plan 03 "add method projections" wording risks reshaping the 18.1 method read | **Incorporated** — narrowed to "preserve existing grouped method projections + add lifecycle fields," with a no-scalar-regression assertion. |

**Owner-bucket items surfaced this cycle:** notification-ingress policy (→ fail closed). Recorded in CONTEXT.

**Caveat:** cycle-4 fixes are applied but not yet re-reviewed — **cycle 5 is running** (owner chose apply-then-re-review). The honest expectation: cycle 5 may itself surface new pre-existing gaps; a reviewed-0 is the goal but not guaranteed on any particular cycle.

## Cycle 5 — 2026-08-28

- **Reviewers:** codex + cursor, source-grounded. Both confirmed the four locked decisions correctly targeted; neither reopened them.
- **Review artifact:** `18.2-REVIEWS.md` (commit `90f01a3`).
- **Raised:** 1 HIGH, 4 actionable = 5. **Honest review trend: 20 → 13 → 5 → 7 → 5** — oscillating around 5, not trending to 0.
- **Notable:** 4 of the 5 findings were incompleteness in *cycle-4's own edits* (guard placed in the pure resolver instead of the async gate; Edit effect-path in acceptance but not action; `tasks:` metadata; a missing negative test). Hand-editing plan text to add a requirement creates new surface (file ownership, action/acceptance consistency) that the next review then checks — a real driver of the non-convergence.
- **Mechanism:** scalpel. **No owner decisions** (the decay→Profile fallback is an implementation of locked decision #4, consistent with the widget Profile-open rule).

### Disposition

| Sev | Finding | Disposition |
|-----|---------|-------------|
| HIGH | Plan 06 Task 4 notification body-tap guard not implementable as declared — `resolveNotificationNav` is pure/sync (no DB); the async live-Bound check must live in `notification-gate.tsx` `applyBodyNav`, which Task 4 didn't own | **Incorporated (fixes cycle-4 error)** — Task 4 now owns `notification-gate.tsx`(+test) for the async body-tap guard (warm-flush + cold-start), keeps `resolveNotificationNav` pure, routes decay→Unbound to Profile; ACTION_MARK half stays in `notification-actions.ts`. |
| MED | Plan 08 Task 1 Edit-effects action/acceptance mismatch — acceptance required the shared effect path, action didn't | **Incorporated** — action now requires routing Edit's post-commit effects through `bind/unbindWithLifecycleEffects`; added `contact-lifecycle-effects.ts` to files/read_first. |
| LOW | Plan 06 frontmatter `tasks: 3` but body has four | **Incorporated** — `tasks: 4`. |
| LOW | Plan 05 Task 1 OrreryScreen `trackingEnabled` pass-through untested (compiles silently through widening) | **Incorporated** — extract the sun-input mapping into a pure, unit-tested function. |
| LOW | Plan 06 Task 1 missing loud-fail negative test for `birthday_unbound_enabled` read | **Incorporated** — added a missing/corrupt-`id=1` negative test to acceptance. |

**No owner-bucket items this cycle.** Cycle-5 fixes are applied but **not yet re-reviewed** — continuation is the owner's call (the review count is not cleanly converging to 0).

