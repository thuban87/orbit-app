---
phase: 18.2-bound-unbound-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/migrations/010-contact-lifecycle-schema.ts, src/db/migrations/010-contact-lifecycle-schema.test.ts, src/db/database.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts]
autonomous: false
requirements: [CDN-02, CDN-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A v9 database migrates to v10 as a pure shape change: `interval_days` becomes nullable, `tracking_enabled` is added defaulting to Bound, and the two Unbound settings columns are added — with row-count-in = row-count-out and every value identical."
    - "The v10 migration owns the positive-or-null cadence CHECK, the Bound-requires-cadence CHECK, and the one-way assigned-cadence trigger before lifecycle DAOs depend on them."
    - "app_settings retains every pre-v10 value; only the two new v10 lifecycle settings use their declared defaults."
  artifacts: [src/db/migrations/010-contact-lifecycle-schema.ts, src/db/migrations/010-contact-lifecycle-schema.test.ts]
  key_links:
    - "database.ts registers migration 010 so the real v9-to-v10 chain executes."
    - "Migration 010 owns the cadence CHECKs and the one-way trigger before the lifecycle DAOs in 18.2-02 depend on them."
    - "reserved-columns.ts records tracking_enabled so backup/portable-column logic stays exhaustive."
  prohibitions:
    - "The v10 migration moves no contact data; it must not re-run any scalar-to-method transform or manufacture a cadence."
---

## RE-CUT NOTE

**New plan, extracted from `.planning/phases/18-contact-data-normalization/18-01-PLAN.md` (Task 3's lifecycle DDL).** This is the Bound/Unbound lifecycle schema that was **removed from 18.1-01**. It is a **pure shape change** on the v9 contacts table — it moves no data (row-count-in = row-count-out, values identical) and ships alongside the lifecycle code (18.2-02 onward) that gives its constraints meaning, rather than locking constraint semantics a phase early.

**Owns the straddling findings that moved here:** the **app_settings-lifecycle-column half of C3-H2** (preservation proof for the two Unbound settings columns) and **H3** (the cadence CHECK/trigger DDL and its direct-SQL rollback tests).

The DDL, constraint, and trigger details are drawn verbatim from 18-01 Task 3's body. `phone_region_override` is NOT added here — it is a method-parsing setting owned by the v9 migration in 18.1-01.

<objective>
## Phase Goal

Establish the Bound/Unbound lifecycle shape on the normalized v9 contacts table as a pure v10 migration.

Purpose: give the lifecycle code that follows a durable schema whose constraints have meaning, without re-running a data transform.
Output: migration 010, updated reserved columns, and constraint/trigger/preservation proofs.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-contact-data-normalization/18-CONTEXT.md
@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@docs/dossier/18-contact-data-normalization.md
@src/db/migrations/001-initial.ts
@src/db/migrations/008-restore-photo-journal.ts
@src/db/migrations/runner.ts
</context>

## Artifacts this phase produces

- A v10 contacts-table shape change (nullable cadence + tracking_enabled + CHECKs + one-way trigger) and app_settings lifecycle columns.
- Constraint, trigger-rollback, and row-count/value preservation proofs.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Apply the v10 lifecycle shape change with constraint and preservation proofs</name>
  <reversibility rating="one-way">Migration 010 changes shipped SQLite table shape and its constraint contract; changing it later requires a forward migration.</reversibility>
  <read_first>src/db/migrations/001-initial.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/008-restore-photo-journal.ts; src/db/migrations/009-contact-method-normalization.ts; src/db/database.ts; src/db/app-settings-dao.ts; src/db/reserved-columns.ts; 18-PATTERNS.md Database migration</read_first>
  <files>src/db/migrations/010-contact-lifecycle-schema.ts, src/db/migrations/010-contact-lifecycle-schema.test.ts, src/db/database.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts</files>
  <behavior>
    - Running the real migration chain from v9 to v10 changes only table shape: row-count-in = row-count-out and every retained column value is identical after the migration; no contact data is transformed and no cadence is manufactured.
    - After v10 the contacts table allows a NULL `interval_days`, carries `tracking_enabled` defaulting to 1 (Bound), and every previously Bound row retains its positive cadence.
    - Direct SQL rejects Bound contacts with NULL cadence, rejects zero or negative cadence, and rolls back an attempt to clear a previously positive cadence through the one-way trigger.
    - The v10 app_settings row contains include_unbound_never_contacted 0 and birthday_unbound_enabled 1.
    - A v9 fixture seeds every pre-v10 app_settings column with a distinctive non-default value; after v10, every pre-existing value is equal and only the two new v10 lifecycle settings use their declared defaults.
    - Schema assertions compare PRAGMA table_info and sqlite_master table/index SQL before and after migration for contacts and app_settings, proving each retained column, NOT NULL/default, index, and UNIQUE constraint survives.
  </behavior>
  <action>Implement migration 010 as the runner-owned transaction. Because SQLite cannot drop the `interval_days` NOT NULL constraint or add table-level CHECKs in place, rebuild the contacts table with the new shape while preserving every row and its SQLite ID: rename the old contacts parent, create the new contacts table, copy every retained parent row and value unchanged, then either prove the legacy-alter route preserved each child FK or retarget every child REFERENCES before dropping the renamed parent; recreate indexes/triggers. In the rebuilt contacts DDL make `interval_days` nullable, add `tracking_enabled INTEGER NOT NULL DEFAULT 1`, and add `CHECK (interval_days IS NULL OR interval_days > 0)` and `CHECK (tracking_enabled = 0 OR interval_days IS NOT NULL)`; create a `BEFORE UPDATE OF interval_days` trigger that aborts when a non-null old cadence would become NULL. Add `include_unbound_never_contacted INTEGER NOT NULL DEFAULT 0` and `birthday_unbound_enabled INTEGER NOT NULL DEFAULT 1` to app_settings in this migration. Update src/db/reserved-columns.ts and src/db/reserved-columns.test.ts for tracking_enabled. Use bound SQL parameters, run row-count/value-equality assertions plus `PRAGMA foreign_key_check`, and register v10 in database.ts. Add direct-SQL migration tests for each CHECK plus the trigger rollback, including an existing Bound fixture. Seed every pre-v10 app_settings column with a distinctive non-default value and assert full pre-existing-row equality after v10. For contacts and app_settings, compare pre/post `PRAGMA table_info` metadata and sqlite_master table/index SQL, asserting retained columns, defaults, NOT NULL, indexes, and UNIQUE constraints are complete. Implement D-02, address the app_settings-lifecycle-column half of Cycle 3 C3-H2 and finding H3, and move no data.</action>
  <verify><automated>npm test -- src/db/migrations/010-contact-lifecycle-schema.test.ts src/db/reserved-columns.test.ts</automated></verify>
  <acceptance_criteria>Real v9 fixtures prove the shape change moves no data (row-count-in = row-count-out, identical values), makes `interval_days` nullable, adds `tracking_enabled` defaulting to Bound, and preserves every pre-v10 app_settings column while the two new lifecycle settings use their declared defaults, with schema preservation proven through table_info/sqlite_master comparisons for contacts and app_settings including indexes and UNIQUE constraints. Migration tests directly prove the positive-or-null CHECK, the Bound-with-cadence CHECK, and the old-positive-to-NULL trigger rollback.</acceptance_criteria>
  <done>The v9 table gains the durable Bound/Unbound lifecycle shape without transforming any contact data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| v9 SQLite data to migration 010 | Existing contact rows are re-shaped without value changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-01 | Tampering/DoS | migration 010 | high | mitigate | Transaction-owned rebuild, `?` binding, row-count/value-equality proofs, FK check, v9 fixture rollback tests, and backup-first device UAT. |
| T-18-06 | Tampering | cadence constraints | high | mitigate | Table CHECKs plus the one-way trigger reject illegal lifecycle cells; direct-SQL rollback tests prove enforcement. |
</threat_model>

<verification>Run the named migration and reserved-columns suites only; retain the v9 fixture for the phase-end Android upgrade review. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.2-10 after every lifecycle consumer migration.</verification>
<success_criteria>The lifecycle schema is durable, test-proven, and moves no data, ready for transactional lifecycle writes.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-01-SUMMARY.md` when done</output>
