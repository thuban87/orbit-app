---
phase: 18.2-bound-unbound-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/migrations/011-contact-lifecycle-schema.ts, src/db/migrations/011-contact-lifecycle-schema.test.ts, src/db/database.ts, src/db/migrations/full-chain.test.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts]
autonomous: false
requirements: [CDN-02, CDN-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A v10 database migrates to v11 as a pure shape change: `interval_days` becomes nullable, `tracking_enabled` is added defaulting to Bound, and the two Unbound settings columns are added — with row-count-in = row-count-out and every value identical."
    - "The v11 migration owns the positive-or-null cadence CHECK, the Bound-requires-cadence CHECK, and the one-way assigned-cadence trigger before lifecycle DAOs depend on them."
    - "app_settings retains every pre-v11 value; only the two new v11 lifecycle settings use their declared defaults."
    - "The contacts rebuild preserves every DIRECT contacts-FK child (the six in migration 009's `EXPECTED_CONTACT_CHILDREN` plus `contact_methods` and `external_contact_links`) with per-child FK-target/action, index, and trigger proofs — not `PRAGMA foreign_key_check` alone — and verifies the transitive child `contact_method_provenance` (whose FK targets `contact_methods`, not `contacts`) is left undisturbed."
  artifacts: [src/db/migrations/011-contact-lifecycle-schema.ts, src/db/migrations/011-contact-lifecycle-schema.test.ts]
  key_links:
    - "database.ts registers migration 011 so the real v10-to-v11 chain executes."
    - "Migration 011 owns the cadence CHECKs and the one-way trigger before the lifecycle DAOs in 18.2-02 depend on them."
    - "reserved-columns.ts records tracking_enabled so backup/portable-column logic stays exhaustive."
  prohibitions:
    - "The v11 migration moves no contact data; it must not re-run any scalar-to-method transform or manufacture a cadence."
---

## RE-CUT NOTE

**New plan, extracted from `.planning/phases/18-contact-data-normalization/18-01-PLAN.md` (Task 3's lifecycle DDL).** This is the Bound/Unbound lifecycle schema that was **removed from 18.1-01**. It is a **pure shape change** on the v9 contacts table — it moves no data (row-count-in = row-count-out, values identical) and ships alongside the lifecycle code (18.2-02 onward) that gives its constraints meaning, rather than locking constraint semantics a phase early.

**Owns the straddling findings that moved here:** the **app_settings-lifecycle-column half of C3-H2** (preservation proof for the two Unbound settings columns) and **H3** (the cadence CHECK/trigger DDL and its direct-SQL rollback tests).

The DDL, constraint, and trigger details are drawn verbatim from 18-01 Task 3's body. `phone_region_override` is NOT added here — it is a method-parsing setting owned by the v9 migration in 18.1-01.

<objective>
## Phase Goal

Establish the Bound/Unbound lifecycle shape on the normalized contacts table (unchanged since migration 009) as a pure v11 migration.

Purpose: give the lifecycle code that follows a durable schema whose constraints have meaning, without re-running a data transform.
Output: migration 011, updated reserved columns, and constraint/trigger/preservation proofs.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-contact-data-normalization/18-CONTEXT.md
@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@docs/dossier/18-contact-data-normalization.md
@src/db/migrations/001-initial.ts
@src/db/migrations/008-restore-photo-journal.ts
@src/db/migrations/runner.ts
</context>

## Artifacts this phase produces

- A v11 contacts-table shape change (nullable cadence + tracking_enabled + CHECKs + one-way trigger) and app_settings lifecycle columns.
- Constraint, trigger-rollback, and row-count/value preservation proofs.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Apply the v11 lifecycle shape change with constraint and preservation proofs</name>
  <reversibility rating="one-way">Migration 011 changes shipped SQLite table shape and its constraint contract; changing it later requires a forward migration.</reversibility>
  <read_first>src/db/migrations/001-initial.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/008-restore-photo-journal.ts; src/db/migrations/009-contact-method-normalization.ts; src/db/migrations/010-contact-method-label.ts; src/db/database.ts; src/db/app-settings-dao.ts; src/db/reserved-columns.ts; 18-PATTERNS.md Database migration</read_first>
  <files>src/db/migrations/011-contact-lifecycle-schema.ts, src/db/migrations/011-contact-lifecycle-schema.test.ts, src/db/database.ts, src/db/migrations/full-chain.test.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts</files>
  <behavior>
    - Running the real migration chain from v10 to v11 changes only table shape: row-count-in = row-count-out and every retained column value is identical after the migration; no contact data is transformed and no cadence is manufactured.
    - After v11 the contacts table allows a NULL `interval_days`, carries `tracking_enabled` defaulting to 1 (Bound), and every previously Bound row retains its positive cadence.
    - Direct SQL rejects Bound contacts with NULL cadence, rejects zero or negative cadence, and rolls back an attempt to clear a previously positive cadence through the one-way trigger.
    - The v11 app_settings row contains include_unbound_never_contacted 0 and birthday_unbound_enabled 1.
    - A v10 fixture (chained through the migration 010 label change) seeds every pre-v11 app_settings column with a distinctive non-default value; after v11, every pre-existing value is equal and only the two new v11 lifecycle settings use their declared defaults.
    - Schema assertions compare PRAGMA table_info and sqlite_master table/index SQL before and after migration for contacts and app_settings, proving each retained column, NOT NULL/default, index, and UNIQUE constraint survives.
    - Every DIRECT contacts-FK child — the six in migration 009's `EXPECTED_CONTACT_CHILDREN` plus `contact_methods` and `external_contact_links` (both `REFERENCES contacts(id)`) — retains its FK target, indexes, triggers, and FK actions after the rebuild, proven per-child rather than by `PRAGMA foreign_key_check` alone; the transitive child `contact_method_provenance` (`REFERENCES contact_methods(id)`) is unaffected by the contacts-only rebuild and is verified undisturbed.
  </behavior>
  <action>Implement migration 011 as the runner-owned transaction. Because SQLite cannot drop the `interval_days` NOT NULL constraint or add table-level CHECKs in place, rebuild the contacts table with the new shape while preserving every row and its SQLite ID: rename the old contacts parent, create the new contacts table, copy every retained parent row and value unchanged, then retarget or defer every DIRECT contacts-FK child before dropping the renamed parent, enumerating the full direct-child set — the six tables in migration 009's `EXPECTED_CONTACT_CHILDREN` PLUS `contact_methods` and `external_contact_links` (both `REFERENCES contacts(id)`) — and port/expand migration 009's `assertExpectedChildren` with that inventory; do NOT retarget `contact_method_provenance`, whose FK targets `contact_methods` (not `contacts`) and is therefore untouched by a contacts-only rebuild — instead verify it is undisturbed. Recreate indexes/triggers and prove each direct child's `PRAGMA foreign_key_list` target/action, row preservation, indexes, and triggers survived, not `PRAGMA foreign_key_check` alone. In the rebuilt contacts DDL make `interval_days` nullable, add `tracking_enabled INTEGER NOT NULL DEFAULT 1`, and add `CHECK (interval_days IS NULL OR interval_days > 0)` and `CHECK (tracking_enabled = 0 OR interval_days IS NOT NULL)`; create a `BEFORE UPDATE OF interval_days` trigger that aborts when a non-null old cadence would become NULL. Add `include_unbound_never_contacted INTEGER NOT NULL DEFAULT 0` and `birthday_unbound_enabled INTEGER NOT NULL DEFAULT 1` to app_settings in this migration. Update src/db/reserved-columns.ts to add `tracking_enabled` AND remove the retired scalar `phone`/`email` entries from the whitelist, and update src/db/reserved-columns.test.ts to chain migrations through v11 (not v6) and cover tracking_enabled. Use bound SQL parameters, run row-count/value-equality assertions plus `PRAGMA foreign_key_check`, and register v11 in database.ts (bump `TARGET_VERSION` to 11) — then update `src/db/migrations/full-chain.test.ts`, which derives from `database.ts`'s `TARGET_VERSION`, to assert `TARGET_VERSION === 11` and that the full chain reaches v11. Add direct-SQL migration tests for each CHECK plus the trigger rollback, including an existing Bound fixture. Seed every pre-v11 app_settings column with a distinctive non-default value and assert full pre-existing-row equality after v11. For contacts and app_settings, compare pre/post `PRAGMA table_info` metadata and sqlite_master table/index SQL, asserting retained columns, defaults, NOT NULL, indexes, and UNIQUE constraints are complete. Implement D-02, address the app_settings-lifecycle-column half of Cycle 3 C3-H2 and finding H3, and move no data.</action>
  <verify><automated>npm test -- src/db/migrations/011-contact-lifecycle-schema.test.ts src/db/reserved-columns.test.ts</automated></verify>
  <acceptance_criteria>Real v10 fixtures (chained through the migration 010 label change) prove the shape change moves no data (row-count-in = row-count-out, identical values), makes `interval_days` nullable, adds `tracking_enabled` defaulting to Bound, and preserves every pre-v11 app_settings column while the two new lifecycle settings use their declared defaults, with schema preservation proven through table_info/sqlite_master comparisons for contacts and app_settings including indexes and UNIQUE constraints, and per-child FK-target/action, index, and trigger preservation proven for every direct contacts-FK child (the six 009 children plus `contact_methods` and `external_contact_links`), with `contact_method_provenance` verified undisturbed as a transitive child of `contact_methods`. Migration tests directly prove the positive-or-null CHECK, the Bound-with-cadence CHECK, and the old-positive-to-NULL trigger rollback.</acceptance_criteria>
  <done>The normalized contacts table gains the durable Bound/Unbound lifecycle shape at v11 without transforming any contact data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| v10 SQLite data to migration 011 | Existing contact rows are re-shaped without value changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-01 | Tampering/DoS | migration 011 | high | mitigate | Transaction-owned rebuild, `?` binding, row-count/value-equality proofs, FK check, v10 fixture rollback tests, and backup-first device UAT. |
| T-18-06 | Tampering | cadence constraints | high | mitigate | Table CHECKs plus the one-way trigger reject illegal lifecycle cells; direct-SQL rollback tests prove enforcement. |
</threat_model>

<verification>Run the named migration and reserved-columns suites only; retain the v10 fixture for the phase-end Android upgrade review. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.2-10 after every lifecycle consumer migration.</verification>
<success_criteria>The lifecycle schema is durable, test-proven, and moves no data, ready for transactional lifecycle writes.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-01-SUMMARY.md` when done</output>
