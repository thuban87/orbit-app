---
phase: 18.2-bound-unbound-lifecycle
plan: 08
type: execute
wave: 5
depends_on: [18.2-02, 18.2-04, 18.2-06, 18.2-07]
files_modified: [src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/services/contact-lifecycle-effects.ts, src/services/contact-lifecycle-effects.test.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Create/edit forms present a Bound/Unbound choice, default Bound, and enforce a positive cadence before Save for Bound while never-assigned Unbound may save with no methods/cadence."
    - "Unbound profile hides status/intensity/favourite/snooze cadence treatment but retains details, history, gravity, archive, birthday, and explicit AI."
    - "Unbind confirmation preserves history/cadence/dormant favourite rank; Bind restores lifecycle eligibility when cadence is positive."
  artifacts: [src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/services/contact-lifecycle-effects.ts]
  key_links: ["Profile invokes the 18.2-06 lifecycle-effects owners so Bind/Unbind reconcile schedules and refresh widget targets; create/edit forms compose the lifecycle choice with method drafts from 18.1-03 into one aggregate save."]
  prohibitions:
    - "Contact methods must not create endpoint-specific interaction history, a method chooser, or a general endpoint router in Phase 18."
---

## RE-CUT NOTE

Re-cut by pulling the **lifecycle halves** out of two source plans:
- from `.planning/phases/18-contact-data-normalization/18-04-PLAN.md` — ONLY the **Bound/Unbound lifecycle choice** control in the create/edit form;
- from `.planning/phases/18-contact-data-normalization/18-06-PLAN.md` — **Task 2** (render Bound/Unbound lifecycle states on Profile) and **Task 3** (execute confirmed Bind/Unbind with reload-safe failure handling), carried in as Tasks 2 and 3.

**Screen METHOD wiring is owned by 18.1-03, NOT here (cycle-1 review H2 resolution):** the method wiring of `CreateContactScreen.tsx`/`EditContactScreen.tsx` — removing scalar phone/email state, mounting the `ContactMethodsEditor`, and passing method drafts into `createContactFull`/`updateContactFull` — lands in 18.1-03 so Phase 18.1 typechecks against the retired scalar contract. This plan ADDS only the Bound/Unbound lifecycle **choice** control onto those already-method-wired screens; it does not perform the method wiring.

**Stayed in 18.1:** the method-editor half of 18-04 (controlled `ContactMethodsEditor` + create/edit method-draft logic AND the screen method wiring → 18.1-03) and the profile-method-display half of 18-06 (Task 1 → 18.1-05).

**Shared-file note for the re-plan:** `CreateContactScreen.tsx`/`EditContactScreen.tsx` are owned by 18.1-03 for the method wiring (the pure `*-contact-logic.ts` method shaping plus the screen wiring); this plan re-enters them only to add the lifecycle choice control. `ContactProfileScreen.tsx`/`contact-profile-logic.ts` are also touched by 18.1-05 (method display). The aggregate `createContactFull`/`updateContactFull` lifecycle-parameter extension lives in `contacts-dao.ts`, which is owned by 18.1-02 — the re-plan must reconcile that shared-file ownership so the form's lifecycle choice reaches the aggregate save. `contact-lifecycle-effects.ts` owner is created in 18.2-06; this plan integrates and tests it from the profile side.

<objective>Complete the lifecycle choice in create/edit forms and Bind/Unbind execution on the profile.
Purpose: a user can choose Orbit participation at create/edit and make intentional lifecycle transitions without data loss or stale status UI.
Output: form lifecycle choice, profile lifecycle UI, and reload-safe Bind/Unbind execution.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/screens/CreateContactScreen.tsx
@src/screens/EditContactScreen.tsx
@src/screens/ContactProfileScreen.tsx
@src/db/contact-lifecycle-dao.ts
@src/services/contact-lifecycle-effects.ts</context>

## Artifacts this phase produces

- Create/edit lifecycle choice wired into the aggregate save alongside method drafts.
- Profile Bound/Unbound state UI and explicit Bind/Unbind lifecycle actions.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the Bound/Unbound choice to create/edit forms</name>
  <read_first>18-UI-SPEC.md Create contact and Edit contact; src/screens/CreateContactScreen.tsx; src/screens/EditContactScreen.tsx; src/screens/create-contact-logic.ts; src/screens/edit-contact-logic.ts; src/db/contact-lifecycle-dao.ts</read_first>
  <files>src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <behavior>
    - Bound is default and needs a positive cadence before Save; never-assigned Unbound may save zero methods/cadence.
    - The lifecycle choice composes into the aggregate create/edit save alongside the method drafts shaped by 18.1-03.
    - CreateContactScreen and EditContactScreen wire the ContactMethodsEditor (18.1-03) and the Bound/Unbound choice into one aggregate save.
  </behavior>
  <action>Add the Bound/Unbound lifecycle choice and cadence gating to the create/edit forms per the UI-SPEC, and wire CreateContactScreen/EditContactScreen to compose the method drafts from 18.1-03's create/edit logic with the lifecycle choice through one aggregate createContactFull/updateContactFull save. Bound requires a positive cadence; never-assigned Unbound may save with no methods/cadence. Implement D-02 and the exact UI-SPEC lifecycle copy; use no raw colors, DB calls in the component, or parser details. This plan explicitly OWNS the aggregate lifecycle extension (18.1-02 already shipped `createContactFull`/`updateContactFull` with method arrays but no lifecycle field, so this is a sequential build-on, not a conflict): add a lifecycle parameter (`trackingEnabled` + nullable `intervalDays`) to `createContactFull`/`updateContactFull` in contacts-dao.ts so a Bound save writes a positive cadence and a never-assigned Unbound save writes NULL cadence with `tracking_enabled=0`; relax the current positive-`intervalDays` precondition to admit Unbound+NULL without violating the v11 CHECK/trigger. Add contacts-dao.test.ts cases for the four lifecycle cells — Bound+positive, Unbound+dormant-positive, Unbound+never-assigned NULL, and rollback when a method/custom-value/lifecycle write fails — plus create-contact-logic/edit-contact-logic Save-gating tests (Bound requires a positive cadence; never-assigned Unbound may save empty). The aggregate `updateContactFull` must distinguish a lifecycle transition from an ordinary metadata edit: a Bound→Unbound edit sets `tracking_enabled=0` while RETAINING the existing positive cadence as dormant (never writing NULL over an assigned cadence, which would trip the v11 trigger) rather than writing `interval_days` unconditionally; add a regression test for the exact Bound→Unbound edit transition proving cadence is retained dormant. See the RE-CUT NOTE for the shared-file reconciliation.</action>
  <verify><automated>npm test -- src/db/contacts-dao.test.ts src/screens/create-contact-logic.test.ts src/screens/edit-contact-logic.test.ts</automated></verify>
  <acceptance_criteria>The create/edit screens present a Bound/Unbound choice defaulting Bound, gate Save on a positive cadence for Bound, and compose lifecycle plus method drafts into one aggregate save; device behavior is backstopped in 18.2-10. The aggregate createContactFull/updateContactFull writes the lifecycle parameter, with contacts-dao tests proving all four lifecycle cells (Bound+positive, Unbound+dormant-positive, Unbound+never-assigned NULL, write-failure rollback) and logic-layer tests proving Save gating rather than tsc alone.</acceptance_criteria>
  <done>Users can choose Orbit participation at create/edit without losing draft data.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render Bound and Unbound lifecycle states correctly</name>
  <read_first>18-UI-SPEC.md Profile; src/db/impact-read.ts; src/db/contact-lifecycle-dao.ts; src/screens/ContactProfileScreen.tsx</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts</files>
  <behavior>
    - Bound preserves current hierarchy; Unbound shows quiet panel, retains gravity/details/history, and has no status/intensity/favourite/snooze treatment.
    - Dormant cadence enables Bind immediately; never-assigned cadence reveals FrequencyPicker and blocks Bind until positive.
    - Method section omits entirely when both groups are empty and renders both groups in stored order otherwise.
  </behavior>
  <action>Apply the exact UI-SPEC lifecycle panel, tokens, headings, 44px controls, accessibility state, and load/error posture through a pure profile view model. Implement D-02 and D-03; pass nullable cadence to UI only as lifecycle state and avoid inventing paused/reset status language.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts</automated></verify>
  <acceptance_criteria>View-model tests cover Bound, dormant Unbound, never-assigned Unbound, no methods, one invalid method, and both ordered type groups.</acceptance_criteria>
  <done>Profile accurately explains participation state while retaining non-cadence relationship information.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Execute confirmed Bind and Unbind with reload-safe failure handling</name>
  <read_first>18-UI-SPEC.md Profile; src/screens/ContactProfileScreen.tsx existing action callbacks; src/services/contact-lifecycle-effects.ts; src/db/contact-lifecycle-dao.ts</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/services/contact-lifecycle-effects.ts, src/services/contact-lifecycle-effects.test.ts</files>
  <action>Add the exact native Unbind Alert and non-optimistic Bind/Unbind dispatch/reload/error Alert flow. Disable duplicate transitions while write is pending, call the 18.2-06 `bindWithLifecycleEffects`/`unbindWithLifecycleEffects` owners rather than the raw DAO, and reload confirmed profile state after success. Implement D-02 and D-03: cadence/history/rank stay stored, rank visibility follows Bound read paths, source data is untouched, and the post-commit notification/widget effects have their dedicated owner tests.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts src/services/contact-lifecycle-effects.test.ts</automated></verify>
  <acceptance_criteria>Tests prove confirmation copy/options, single-flight actions, retry-safe failure state, dormant cadence preservation, reload-derived rebound state, and dispatch through the lifecycle-effects seam.</acceptance_criteria>
  <done>Users can make intentional lifecycle transitions without optimistic data loss or stale status UI.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| React inputs to aggregate DAO | Lifecycle choices become durable state. |
| profile action to lifecycle DAO | User confirmation changes proactive eligibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-12 | Tampering | form save | high | mitigate | Typed shaping delegates normalization/validation to DAO and uses atomic aggregate write. |
| T-18-19 | Tampering | lifecycle action | medium | mitigate | Native confirmation, single-flight DAO call, reload-on-success, generic error alert. |
| T-18-20 | Information disclosure | profile/UI logs | medium | mitigate | Formatted visual display and no canonical/raw data in diagnostic paths. |
| T-18-SC | Tampering | package install | high | accept | No dependency changes in this plan. |
</threat_model>
<verification>Run the named profile and lifecycle-effects suites only; complete device handoff proof in 18.2-10. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.2-10.</verification>
<success_criteria>Lifecycle choice and transitions remain intentional, safe, and lifecycle-aware while relationship data stays whole.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-08-SUMMARY.md` when done</output>
