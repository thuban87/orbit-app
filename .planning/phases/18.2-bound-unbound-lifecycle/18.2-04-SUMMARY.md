---
phase: 18.2-bound-unbound-lifecycle
plan: 04
subsystem: database-ai
tags: [sqlite, lifecycle, impact, ai-context, privacy, vitest]
requires:
  - phase: 18.2-03
    provides: "Lifecycle-bearing direct reads and Bound-only proactive population predicates"
provides:
  - "Nullable-cadence impact inputs with a tagged Unbound intensity-unavailable contract"
  - "Explicit AI context with exact neutral intensity for both Unbound cadence states"
  - "Normalized method PII regression coverage at the prompt egress boundary"
affects: [18.2-08, profile-view-model, explicit-ai, lifecycle]
actuals:
  tokens: 5375.25
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [tagged unavailable derived value, closed normalized-method PII projection]
key-files:
  created: [src/services/impact.test.ts]
  modified: [src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/impact.ts, src/db/ai-context-read.ts, src/db/ai-context-read.test.ts]
key-decisions:
  - "Unbound intensity is represented as the explicit tagged result `{ available: false }`, not a nullable cadence calculation or a fabricated interval."
  - "Explicit AI maps unavailable intensity to the fixed neutral aggregate while preserving gravity, relationship history, and the closed PromptContext projection."
patterns-established:
  - "Lifecycle-sensitive derived services must inspect lifecycle before cadence arithmetic and force consumers to handle unavailable results."
requirements-completed: [CDN-02, CDN-03]
coverage:
  - id: D1
    description: "Impact inputs retain Unbound relationship history and gravity while intensity is unavailable for dormant and never-assigned cadence states."
    requirement: CDN-02
    verification:
      - kind: integration
        ref: "npm test -- src/db/impact-read.test.ts src/services/impact.test.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "Explicit AI remains available for an Unbound contact with exact neutral intensity and no normalized method PII in PromptContext."
    requirement: CDN-03
    verification:
      - kind: integration
        ref: "npm test -- src/db/ai-context-read.test.ts src/db/impact-read.test.ts src/services/impact.test.ts"
        status: pass
    human_judgment: false
duration: 4min
completed: 2026-08-29
status: complete
---

# Phase 18.2 Plan 04: Bound-Aware Impact and Explicit AI Summary

**Unbound contacts now retain relationship gravity and user-requested AI context while cadence intensity is explicitly unavailable and normalized method PII remains outside prompts.**

## Performance

- **Duration:** 4min
- **Started:** 2026-08-29T00:23:11Z
- **Completed:** 2026-08-29T00:27:09Z
- **Tasks:** 2/2
- **Files modified:** 6

## Accomplishments

- Exposed lifecycle state and nullable cadence in the single-snapshot impact read without dropping historical interactions.
- Made impact intensity a tagged unavailable result for every Unbound contact, while preserving existing Bound intensity behavior and gravity calculations.
- Kept explicit AI available for dormant- and never-assigned-cadence Unbound contacts with the exact neutral intensity aggregate.
- Migrated the prompt-context privacy proof to normalized contact methods and asserted raw, formatted, and canonical values never cross the closed projection.

## Task Commits

1. **Task 1 (RED): Bound-aware impact coverage** — `73e9b52` (`test`)
2. **Task 1 (GREEN): lifecycle-gated impact intensity** — `f871d55` (`feat`)
3. **Task 2 (RED): Unbound explicit-AI coverage** — `b51e83e` (`test`)
4. **Task 2 (GREEN): neutral Unbound AI intensity** — `2025873` (`feat`)

## Files Created/Modified

- `src/db/impact-read.ts` — returns lifecycle state plus nullable cadence alongside relationship history.
- `src/services/impact.ts` — returns `{ available: false }` before cadence arithmetic for Unbound inputs.
- `src/db/ai-context-read.ts` — maps unavailable intensity to the required neutral prompt aggregate without an invented interval.
- `src/db/impact-read.test.ts`, `src/services/impact.test.ts`, and `src/db/ai-context-read.test.ts` — prove both Unbound cadence states, Bound regression behavior, and method-PII exclusion.

## Verification

- `npm test -- src/db/ai-context-read.test.ts src/db/impact-read.test.ts src/services/impact.test.ts` — passed (24 tests).
- `npx biome check src/db/ai-context-read.ts src/db/impact-read.ts src/db/impact-read.test.ts src/services/impact.ts src/services/impact.test.ts` — passed.

## Decisions Made

- Intensity has no paused or reset reinterpretation: its unavailable branch is an explicit tagged service result.
- Prompt context consumes the tag and emits a fixed neutral aggregate, retaining user-initiated relationship support without exposing cadence inputs or contact methods.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected Unbound test touchpoint timestamps.**

- **Found during:** Task 2 RED verification
- **Issue:** The initial fixture used a touchpoint after the deterministic test clock, which the real write guard correctly rejected before exercising the AI-context assertion.
- **Fix:** Set both fixture touchpoints before the fixed test clock.
- **Files modified:** `src/db/ai-context-read.test.ts`
- **Verification:** The RED test then failed only on the expected missing neutral aggregate; the GREEN suite passed.
- **Committed in:** `b51e83e`

**Total deviations:** 1 auto-fixed (Rule 1).

## Known Stubs

None.

## Next Phase Readiness

Plan 18.2-08 can consume the same tagged intensity contract in the profile view-model, while explicit AI already handles both Unbound cadence states safely.

## Self-Check: PASSED

- Summary exists on disk.
- Task commits `73e9b52`, `f871d55`, `b51e83e`, and `2025873` exist in git history.
