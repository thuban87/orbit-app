---
phase: 18.2-bound-unbound-lifecycle
plan: 01
subsystem: database
tags: [sqlite, migration, lifecycle, schema, vitest]
requires:
  - phase: 18.1
    provides: normalized v10 contacts, contact methods, and provenance tables
provides:
  - v11 Bound/Unbound lifecycle schema with cadence constraints
  - lossless v10 contacts and child-table rebuild proof
affects: [18.2-02, lifecycle-daos, backup, dashboard]
actuals:
  tokens: 8733
  tasks: 1
  commits: 2
tech-stack:
  added: []
  patterns: [runner-owned SQLite table rebuild, direct-child FK preservation audit]
key-files:
  created: [src/db/migrations/011-contact-lifecycle-schema.ts, src/db/migrations/011-contact-lifecycle-schema.test.ts]
  modified: [src/db/database.ts, src/db/reserved-columns.ts]
key-decisions:
  - "Migration 011 preserves row IDs and all retained values while rebuilding contacts and every direct contacts-FK child."
  - "contact_method_provenance remains logically transitive and is copied only to survive SQLite parent-table rename side effects."
patterns-established:
  - "For a contacts rebuild, inventory direct FK children from PRAGMA and prove their rows, FK actions, indexes, and triggers individually."
requirements-completed: [CDN-02, CDN-04]
coverage:
  - id: D1
    description: "v10-to-v11 lifecycle schema rebuild preserves contacts, settings, direct children, and transitive provenance."
    requirement: CDN-02
    verification:
      - kind: integration
        ref: "npm test -- src/db/migrations/011-contact-lifecycle-schema.test.ts src/db/reserved-columns.test.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "v11 cadence CHECKs and one-way cadence-clear trigger reject invalid direct SQL."
    requirement: CDN-02
    verification:
      - kind: integration
        ref: "src/db/migrations/011-contact-lifecycle-schema.test.ts#enforces lifecycle cadence constraints and rolls back a cadence clear"
        status: pass
    human_judgment: false
duration: 9min
completed: 2026-08-28
status: complete
---

# Phase 18.2 Plan 01: Lifecycle Schema Migration Summary

**SQLite v11 lifecycle shape with nullable never-assigned cadence, Bound defaulting, durable cadence guards, and lossless normalized-contact rebuilds.**

## Performance

- **Duration:** 9min
- **Started:** 2026-08-28T18:45:00-05:00
- **Completed:** 2026-08-28T18:53:12Z
- **Tasks:** 1/1
- **Files modified:** 6

## Accomplishments

- Added migration 011 and registered it as the database target version.
- Rebuilt contacts and its eight direct FK children without changing retained rows, IDs, constraints, indexes, or FK actions.
- Added the nullable positive-integer cadence contract, Bound-requires-cadence CHECK, one-way cadence-clear trigger, and lifecycle settings defaults.
- Updated the reserved-column guard for `tracking_enabled` and the real chain through v11.

## Task Commits

1. **Task 1 (RED): lifecycle migration coverage** — `349c6c4` (`test`)
2. **Task 1 (GREEN): lifecycle schema migration** — `904410d` (`feat`)

## Verification

- `npm test -- src/db/migrations/011-contact-lifecycle-schema.test.ts src/db/reserved-columns.test.ts` — passed (4 tests).
- `npm test -- src/db/migrations/full-chain.test.ts` — passed.
- `npx biome check` across all six touched files — passed.

## Decisions Made

- Existing contacts enter v11 as Bound (`tracking_enabled = 1`) with their original positive cadence.
- The contacts rebuild treats the eight contacts-FK tables as direct children; provenance remains a contact-method child and retains its unchanged shape and values.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved transitive method provenance during the direct-child rebuild.**

- **Found during:** Task 1 verification
- **Issue:** Dropping the old `contact_methods` or `external_contact_links` parent before copying provenance invoked its foreign-key action and removed provenance rows.
- **Fix:** Retained both old direct parents until the unchanged `contact_method_provenance` rows had been copied against the new parents, then dropped the old tables.
- **Files modified:** `src/db/migrations/011-contact-lifecycle-schema.ts`, `src/db/migrations/011-contact-lifecycle-schema.test.ts`
- **Verification:** The v10 fixture now proves exact provenance row/FK/index/trigger preservation and `PRAGMA foreign_key_check` is empty.
- **Committed in:** `904410d`

**Total deviations:** 1 auto-fixed (Rule 1).

## Known Stubs

None.

## Next Phase Readiness

Plan 18.2-02 can build transactional Bind/Unbind writes on the durable v11 cadence and lifecycle contract.

## Self-Check: PASSED

- Created migration and migration test exist on disk.
- Task commits `349c6c4` and `904410d` exist in git history.
