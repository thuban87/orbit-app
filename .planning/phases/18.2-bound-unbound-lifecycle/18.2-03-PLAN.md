---
phase: 18.2-bound-unbound-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [18.2-01]
files_modified: [src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts, src/db/contact-status-read.ts, src/db/contact-status-read.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts, src/db/status.ts, src/db/status.test.ts, src/db/queries.ts, src/db/queries.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct profile/search retrieval retains both states, while dashboard and favourite projections select only Bound contacts."
    - "Unbound has a dedicated alphabetical read and is included in Never Contacted only through its persisted opt-in."
    - "Status, digest, and header counts remain Bound-only even when direct retrieval retains an Unbound contact."
    - "CDN-03 boundary and precision cases are covered at their owning SQL reads."
  artifacts: [src/db/contact-read.ts, src/db/unbound-read.ts, src/db/dashboard-read.ts, src/db/contact-status-read.ts, src/db/digest-read.ts]
  key_links: ["getContactHeader supplies trackingEnabled to the sequenced 18.2-05 shared sun policy; the dependency-ordered nullable-cadence impact and explicit-AI work is isolated in 18.2-04, while notification and widget side effects remain isolated in 18.2-06.", "Dashboard search preserves direct two-state retrieval while query-owned proactive/status/digest/favourite projections use the Bound predicate."]
---
<objective>
Put lifecycle eligibility at the direct and population-query owners, retaining relationship retrieval while removing Unbound contacts from proactive cadence surfaces.
Purpose: prevent profile/search, dashboard, status, digest, widget, and notification consumers from disagreeing about Bound state.
Output: lifecycle-bearing direct reads, Bound-aware dashboard/status/digest predicates, a dedicated Unbound read, and owner-level regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/contact-read.ts
@src/db/dashboard-read.ts
@src/db/contact-status-read.ts
@src/db/digest-read.ts
@src/db/status.ts
@src/db/queries.ts</context>

## Artifacts this phase produces

- Direct method/lifecycle profile projections and dedicated Unbound retrieval.
- SQL-owned Bound eligibility for dashboard/search presentation, favourite rank, header counts, status, and digest projections.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Read one Unbound contact through profile and dedicated population paths</name>
  <read_first>src/db/contact-read.ts:65-208; src/db/dashboard-read.ts:257-343; src/screens/NeverContactedScreen.tsx; 18-PATTERNS.md Read owners and downstream consumers</read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts</files>
  <behavior>
    - Contact header/edit assembly returns ordered methods and lifecycle state, including `trackingEnabled` for the sequenced 18.2-05 sun-policy consumer, without scalar phone/email columns.
    - listUnbound returns non-archived Unbound contacts alphabetically with neutral metadata.
    - A profile may retain Unbound history while cadence-dependent presentation receives nullable/disabled inputs.
  </behavior>
  <action>Extend the existing contact-read aggregate with method projections and `tracking_enabled`, exposing it as `trackingEnabled` on the header and edit contracts; create listUnbound from the dashboard-read query conventions. Implement D-01 and D-02 at the named SQL owners rather than screen filters, preserving both-state direct/profile retrieval and no scalar compatibility read. This is the direct-header contract consumed by 18.2-05’s single Orrery sun predicate and addresses cycle-2 review H2.</action>
  <verify><automated>npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts</automated></verify>
  <acceptance_criteria>Tests prove a real Unbound contact keeps details/history through the profile read, supplies `trackingEnabled: false`, and appears only in the dedicated alphabetical population.</acceptance_criteria>
  <done>A user-facing read path can retrieve one Unbound contact without reintroducing retired endpoint columns.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Constrain dashboard, status, digest, and favourites at their SQL owners</name>
  <read_first>src/db/dashboard-read.ts; src/db/contact-status-read.ts; src/db/digest-read.ts; src/db/status.ts; src/db/queries.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/contact-status-read.ts, src/db/contact-status-read.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts, src/db/status.ts, src/db/status.test.ts, src/db/queries.ts, src/db/queries.test.ts</files>
  <behavior>
    - Bound dashboard population excludes Unbound; dashboard search and opt-in Never Contacted reads may return both with `trackingEnabled` and neutral status/rank metadata.
    - Favourite rank persists in storage but Bound-only favourite reads hide dormant ranks.
    - Status and digest projections cannot label an Unbound-but-contacted contact stable or include it in active-orbit summaries, and Bound-only header counts match list population.
  </behavior>
  <action>Add `tracking_enabled=1` to the named proactive/favourite SQL branches, keeping search explicitly retrieval-oriented. Make CARD_STATUS return NULL status/progress for an Unbound row, return `trackingEnabled` in DashboardRow, and project NULL `favourite_rank` for every Unbound retrieval row that can reach a status-bearing card, including dashboard search and `listNeverContacted`; `listNeverContacted` must explicitly select `tracking_enabled AS trackingEnabled` rather than relying on the unchecked DashboardRow cast. Update countLiveContacts and related dashboard/header totals to use the same Bound-only predicate as their list population. Extend contact-status-read so Unbound contacts return no cadence status/reason/progress even if contacted; update the shared status.ts and queries.ts contract so every STATUS_SCAN caller excludes nullable cadence, and audit benchmark.ts in 18.2-10’s ledger as a known positive-cadence writer/fragment consumer. Add the Bound predicate to digest-read's active-orbit/overlooked projections. Implement D-02 and D-03 and address cycle-2 reviews M4 and M5 plus Cycle 3 C3-M3. Add one SQL-fragment parity test per Bound-predicate owner (dashboard population, countLiveContacts, favourites read) asserting the emitted SQL contains `tracking_enabled = 1` (mirroring the orrery-read STATUS_SQL parity pattern), assert dashboard search returns an Unbound row with NULL status and NULL favourite_rank, and lock-and-test whether digest `readRetrospective`/`readGentleLine` stay all-relationship-history (they currently include every non-archived contact) — pin them as all-relationship-history and record that decision in the test. The Orrery-specific query and sun policy are deliberately owned by 18.2-05; the widget inherits this favourite projection in 18.2-06 rather than introducing a second filter.</action>
  <verify><automated>npm test -- src/db/dashboard-read.test.ts src/db/contact-status-read.test.ts src/db/digest-read.test.ts src/db/status.test.ts src/db/queries.test.ts</automated></verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision tests prove no Unbound row reaches status, digest, rank, or header count while direct retrieval stays available. Unbound dashboard-search and opt-in Never Contacted results carry `trackingEnabled: false` with `status` and `favourite_rank` NULL for neutral rendering; shared fragments document and test the nullable-cadence precondition. Per-owner SQL-fragment parity tests assert `tracking_enabled = 1` in dashboard population, countLiveContacts, and favourites reads; a search test asserts an Unbound row with NULL status and NULL favourite_rank; and the digest retrospective/gentle-line policy is pinned as all-relationship-history with a test.</acceptance_criteria>
  <done>Dashboard-derived proactive and favourite populations honor dormant lifecycle state from their query origins.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite query owners to UI | Lifecycle state determines profile, dashboard, status, digest, and favourite treatment. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-09 | Elevation/Tampering | proactive reads | high | mitigate | SQL predicates and owner tests block Unbound from cadence/status routes. |
| T-18-10 | Information disclosure | profile/read projections | medium | mitigate | Return presentation-safe method and lifecycle data only; AI/log boundary tests remain in 18.2-10. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18.1-01 owns the approved dependency gate. |
</threat_model>
<verification>Run the named source-adjacent direct/population query suites only and keep consumer changes at their declared SQL owners. Scalar-contract retirement is intentionally staged through later consumers, so the repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.2-10.</verification>
<success_criteria>Profile/search and lifecycle query owners apply the policy while explicit relationship retrieval stays intact.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-03-SUMMARY.md` when done</output>
