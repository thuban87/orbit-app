---
phase: 18.2-bound-unbound-lifecycle
plan: 06
type: execute
wave: 4
depends_on: [18.2-02, 18.2-03]
files_modified: [src/db/notification-read.ts, src/db/notification-read.test.ts, src/services/notifications/decay-suppression.ts, src/services/notifications/notification-schedule.test.ts, src/navigation/notification-gate.tsx, src/navigation/notification-gate.test.tsx, src/services/notifications/notification-actions.ts, src/services/notifications/notification-actions.test.ts, src/services/contact-lifecycle-effects.ts, src/services/contact-lifecycle-effects.test.ts, src/services/widget/widget-data.ts, src/services/widget/widget-data.test.ts, src/services/widget/widget-quick-action-guard.ts, src/services/widget/widget-quick-action-guard.test.ts, src/navigation/widget-linking.ts, src/navigation/widget-linking.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Decay scheduling never retains an Unbound contact, while factual birthday data remains available and birthday notification policy follows its persisted default-on Unbound setting."
    - "Unbind commits lifecycle data, then reconciles/cancels obsolete scheduled notifications and requests a widget refresh without rolling back the completed lifecycle transition; Bind is symmetric — it commits, then re-arms the decay schedule and requests a widget refresh, also without rollback."
    - "Widget tiles drop Unbound favourites; a stale or now-Unbound target fails closed for an active-cadence action (Compose/quick-action) while a plain Profile-open still navigates to a live Unbound contact."
    - "A delivered decay notification whose contact is now Unbound fails closed on tap (no Compose route) and does not re-arm cadence on Mark, while a birthday notification still opens Profile and Mark still records the real touchpoint."
  artifacts: [src/db/notification-read.ts, src/services/contact-lifecycle-effects.ts, src/services/widget/widget-quick-action-guard.ts]
  key_links: ["Profile’s Bind/Unbind actions in 18.2-08 call bindWithLifecycleEffects/unbindWithLifecycleEffects; the effects use Bound-aware notification reads and widget guards supplied here."]
---

## RE-CUT NOTE

Carried whole from `.planning/phases/18-contact-data-normalization/18-09-PLAN.md` (notification/widget lifecycle policy + post-Unbind side effects + stale quick-action fail-closed).

**Dependency severed:** the original dep on old 18-05 (the Unbound settings SCREEN) is **removed**. The `birthday_unbound_enabled` column this plan reads comes from the **v11 migration (18.2-01)**, not the settings screen. Depends only on the lifecycle write DAO (18.2-02) and the Bound-aware query owners (18.2-03).

<objective>
Apply the lifecycle policy to headless notification and widget behavior, including post-Unbind side effects.
Purpose: an Unbound transition must immediately remove proactive work and prevent stale widget actions without losing owned relationship data.
Output: notification policy reads, lifecycle-effect owner, widget guard, and automated side-effect tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/db/notification-read.ts
@src/services/notifications/notification-schedule.ts
@src/services/widget/widget-data.ts
@src/navigation/widget-linking.ts</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Reconcile one newly Unbound contact out of notifications</name>
  <read_first>src/db/notification-read.ts; src/db/notification-read.test.ts; src/services/notifications/decay-suppression.ts; src/services/notifications/notification-schedule.test.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/notification-read.ts, src/db/notification-read.test.ts, src/services/notifications/decay-suppression.ts, src/services/notifications/notification-schedule.test.ts</files>
  <behavior>
    - Decay candidates require Bound independently of reminders_off.
    - Factual birthday data remains both-state; birthday notification eligibility uses the persisted default-on Unbound policy.
    - A reconciliation after an Unbind cancels the contact’s obsolete scheduled decay request instead of leaving it pending.
  </behavior>
  <action>Add `tracking_enabled=1` to the closed decay candidate predicate and read the persisted `birthday_unbound_enabled` from the singleton `id=1` app_settings row explicitly (failing loud on a missing/corrupt row, consistent with getAppSettings) and thread it to notification eligibility only. Extend the real notification-schedule integration test with a contact that becomes Unbound after an existing schedule, proving reconciliation cancels its stale request. Implement D-02 and D-03 without changing contact-specific notification decisions or adding proactive AI selection.</action>
  <verify><automated>npm test -- src/db/notification-read.test.ts src/services/notifications/notification-schedule.test.ts</automated></verify>
  <acceptance_criteria>Tests prove decay is Bound-only, birthday policy defaults correctly and changes only notification eligibility (leaving factual/dashboard birthday population independent), and a scheduled decay notification is cancelled after a committed Unbind. The `birthday_unbound_enabled` read fails loud on a missing or corrupt `id=1` app_settings row (negative test).</acceptance_criteria>
  <done>Headless notification reconciliation reflects the current lifecycle rather than stale cadence eligibility.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Own post-Unbind notification and widget side effects</name>
  <read_first>src/db/contact-lifecycle-dao.ts; src/services/notifications/notification-schedule.ts; src/services/widget/widget-refresh.ts; src/services/widget/widget-data.ts; 18-UI-SPEC.md Copywriting Contract</read_first>
  <files>src/services/contact-lifecycle-effects.ts, src/services/contact-lifecycle-effects.test.ts, src/services/widget/widget-data.ts, src/services/widget/widget-data.test.ts</files>
  <behavior>
    - `unbindWithLifecycleEffects` calls the lifecycle DAO once, then reconciles the notification schedule and publishes a widget refresh after its durable success.
    - `bindWithLifecycleEffects` is symmetric: it calls the Bind DAO once, then re-arms the decay schedule and publishes a widget refresh after durable success, so a rebound contact's proactive surfaces do not lag until the next foreground sweep.
    - `applyLifecycleTransitionEffects(contactId, direction, deps)` is the DAO-free effects-only unit (schedule reconcile + widget refresh, no DAO call) shared by both owners and called directly by the 18.2-08 edit-form save, so an already-transitioned edit dispatches effects once without a second DAO transition.
    - A reconciliation or widget refresh failure is recorded through an error boundary and cannot undo the committed Unbind or re-run it.
    - Widget tiles inherit the Bound-only favourite projection and no longer include a dormant favourite after refresh.
  </behavior>
  <action>Create the foreground `unbindWithLifecycleEffects` AND the symmetric `bindWithLifecycleEffects` owners around the existing DAO, `reconcileSchedule`, and `notifyWidgetDataChanged`; preserve the DAO transaction boundary and make effects post-commit for both (Unbind cancels the stale decay request; Bind re-arms it). Factor the post-commit reconcile+refresh into a DAO-free `applyLifecycleTransitionEffects(contactId, direction, deps)` that performs ONLY the schedule reconcile + widget refresh (no DAO call); `bindWithLifecycleEffects`/`unbindWithLifecycleEffects` are the DAO-wrapping variants (DAO transition + `applyLifecycleTransitionEffects`) for callers that have not yet transitioned (the Profile Bind/Unbind buttons), while a caller that already wrote the transition in its own transaction (the 18.2-08 edit-form aggregate save) calls `applyLifecycleTransitionEffects` directly, exactly once. Add owner tests with injected dependencies for successful sequencing, schedule cancellation (Unbind) and re-arm (Bind), widget refresh, and non-rollback effect failure for each direction. Extend widget-data tests to prove a Bound-filtered favourites projection produces no Unbound tile. Implement D-02 and D-03 without clearing dormant favourite rank or adding widget polling.</action>
  <verify><automated>npm test -- src/services/contact-lifecycle-effects.test.ts src/services/widget/widget-data.test.ts</automated></verify>
  <acceptance_criteria>Owner tests prove both Unbind and Bind cause notification reconciliation (cancel on Unbind, re-arm on Bind) and widget refresh exactly once after DAO success, with failure isolation; widget projection tests exclude Unbound favourites. A test proves `applyLifecycleTransitionEffects` runs the effects with NO DAO call, so the edit-form caller cannot double-transition.</acceptance_criteria>
  <done>Lifecycle transitions refresh external proactive surfaces without compromising saved relationship state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Fail closed for stale widget quick-action contact targets</name>
  <read_first>src/navigation/widget-linking.ts; src/navigation/widget-linking.test.ts; src/db/contact-read.ts; src/services/widget/widget-refresh.ts; docs/dossier/18-contact-data-normalization.md Cluster S</read_first>
  <files>src/services/widget/widget-quick-action-guard.ts, src/services/widget/widget-quick-action-guard.test.ts, src/navigation/widget-linking.ts, src/navigation/widget-linking.test.ts</files>
  <behavior>
    - Compose and other active-cadence quick-action intents are allowed only for a live Bound contact; for a stale, archived, missing, or now-Unbound target they fail closed (no active action presented).
    - A plain contact-open (Profile view) intent still navigates for any live contact regardless of Bound state — an Unbound contact is a complete, viewable relationship record (dossier Cluster M) — and only a genuinely missing or archived target resolves to no navigation.
    - The strict URI allow-list remains unchanged for malformed/untrusted URLs.
    - The favourites intent remains valid and refresh repopulates it from the Bound-only projection.
  </behavior>
  <action>Add a narrow database-backed widget quick-action guard and call it in WidgetLinkingGate before dispatching an intent. Extract the guard as an async `guardWidgetIntent(intent, lookup)` that takes an INJECTED contact-lookup dependency, so it is unit-tested directly rather than through WidgetLinkingGate's dynamic RN/navigation imports; WidgetLinkingGate calls it with the real lookup. Keep `resolveWidgetUri` pure and its strict URI validation intact; only the post-parse live-state guard decides dispatch. Scope the guard by intent kind: an active-cadence intent (Compose / quick-action) fails closed for a stale, archived, missing, or now-Unbound target, while a plain Profile-open intent still navigates for any live contact including Unbound and only dead-ends on a missing/archived target. Implement D-02 and D-03 with node tests covering Bound Compose (allowed), Unbound Compose (fails closed), Unbound Profile-open (navigates), archived/missing (no navigation for either intent), and malformed URL (rejected by the allow-list); do not expose a widget lifecycle-management UI.</action>
  <verify><automated>npm test -- src/services/widget/widget-quick-action-guard.test.ts src/navigation/widget-linking.test.ts</automated></verify>
  <acceptance_criteria>Tests prove stale/Unbound active-cadence quick-action targets fail closed while a plain Profile-open still navigates to a live Unbound contact, valid Bound targets retain their navigation, archived/missing targets dead-end for either intent, and malformed URLs stay rejected by the allow-list.</acceptance_criteria>
  <done>Widget update and navigation behavior cannot continue to present an Unbound contact as active cadence work.</done>
</task>
<task type="tracer" tdd="true">
  <name>Task 4: Fail closed on stale delivered-notification ingress for now-Unbound contacts</name>
  <read_first>src/services/notifications/notification-nav.ts (pure resolver — stays unchanged); src/navigation/notification-gate.tsx; src/services/notifications/notification-actions.ts; src/services/notifications/notification-actions.test.ts; src/db/contact-read.ts; docs/dossier/18-contact-data-normalization.md Clusters L and S</read_first>
  <files>src/navigation/notification-gate.tsx, src/navigation/notification-gate.test.tsx, src/services/notifications/notification-actions.ts, src/services/notifications/notification-actions.test.ts</files>
  <behavior>
    - A delivered decay notification whose contact is now Unbound does NOT route to Compose on tap; the async live-Bound guard in the notification gate routes it to the contact's Profile (view) instead — mirroring the widget Profile-open-allowed rule — never presenting the active-cadence Compose action.
    - A birthday notification still routes to Profile regardless of lifecycle — an Unbound contact is a complete, viewable record.
    - `ACTION_MARK` ("Mark contacted") still records the real touchpoint for a now-Unbound contact (Unbound preserves history) but the guard prevents re-arming cadence/decay for it; snooze on a stale decay for a now-Unbound contact is a no-op.
    - The strict notification-data validation and the killed-app headless path are unchanged; malformed/unknown notification data behaves exactly as today.
  </behavior>
  <action>Add the live-Bound-state guard at the delivered-notification ingress seam, in the two places that can do an async DB read: keep `resolveNotificationNav` PURE (unchanged) and add the guard in `notification-gate.tsx`'s `applyBodyNav` — covering both the warm-tap flush (`isReady && pendingBodyData`) and the cold-start async path — so before applying a decay intent it does an async live-Bound read and, when the contact is now Unbound, routes to its Profile instead of Compose (birthday routing to Profile is unchanged); and in notification-actions, `ACTION_MARK` still records the touchpoint via recordTouchpoint but the guard prevents any cadence/decay re-arm for a now-Unbound contact, and the snooze branch is a no-op for it. Read live lifecycle state through the existing Bound-aware read (contact-read), keep the guard headless-safe for the killed-app task, and do not change actionability for live Bound contacts. Implement D-02 and D-03 with node tests for: Bound decay tap (routes to Compose), Unbound decay tap (routes to Profile, not Compose), birthday tap for an Unbound contact (routes to Profile), `ACTION_MARK` on a now-Unbound contact (records the touchpoint, no cadence re-arm), snooze on a now-Unbound contact (no-op), and unchanged malformed-data handling. This extends the owner's widget fail-closed decision to the notification surface — a distinct, previously-unowned proactive surface named in the phase goal.</action>
  <verify><automated>npm test -- src/navigation/notification-gate.test.tsx src/services/notifications/notification-actions.test.ts</automated></verify>
  <acceptance_criteria>Tests prove a stale decay notification for a now-Unbound contact routes to Profile instead of Compose on tap and does not re-arm cadence on Mark (while still recording the real touchpoint), a birthday notification still opens Profile, live Bound targets keep their existing routing/actions, and the killed-app headless path is preserved.</acceptance_criteria>
  <done>Delivered-notification ingress cannot present or re-arm active cadence work for an Unbound contact.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| lifecycle transition to OS scheduler | A durable state change modifies notification eligibility. |
| widget deep link to navigation | An OS-delivered action targets a mutable local contact state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-28 | Elevation/Tampering | notification reconciliation | high | mitigate | Bound-aware candidate SQL, post-Unbind reconciliation, and cancellation integration tests. |
| T-18-29 | Elevation | widget quick actions | high | mitigate | Bound/archived/missing database guard after strict URI parsing, with stale-target tests. |
| T-18-30 | DoS/Tampering | post-commit effects | medium | mitigate | Single DAO transition with injected side-effect isolation; failed refresh cannot repeat or roll back durable writes. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan. |
</threat_model>
<verification>Run the named notification, lifecycle-effects, widget projection/guard, and linking suites only, keeping effects at their declared service/read owners. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.2-10.</verification>
<success_criteria>Unbinding deterministically removes proactive notification/widget behavior and stale quick actions fail safely.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-06-SUMMARY.md` when done</output>
