---
phase: 18.2-bound-unbound-lifecycle
plan: 09
type: execute
wave: 3
depends_on: [18.2-01, 18.2-02]
files_modified: [src/backup/types.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts]
autonomous: true
requirements: [CDN-04]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The v3 backup wire format carries `tracking_enabled` and nullable/dormant cadence; `BACKUP_FORMAT_VERSION` is bumped 2→3 per the types.ts shape-change contract, and a v2→v3 forward migration defaults existing contacts to Bound with their existing positive cadence."
    - "Parse rejects self-contained illegal lifecycle/cadence combinations (Bound with null cadence, non-0/1 trackingEnabled, non-positive/non-integer cadence) with BackupSchemaError, and a separate pre-transaction merge-planning phase resolves a trigger-conflicting merge cadence (which needs the local target row) before restore opens its transaction."
  artifacts: [src/backup/backup-schema.ts, src/backup/restore-apply.ts]
  key_links: ["The method backup graph (18.1-04) is extended here with the lifecycle fields that only exist after the v11 migration (18.2-01); validation enforces the same lifecycle cells the v11 CHECKs/trigger and lifecycle DAO (18.2-02) enforce."]
  prohibitions:
    - "Backup/restore must not reinterpret a missing or stale system source as authorization to delete or overwrite Orbit-owned data."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-07-PLAN.md` — the **lifecycle parts only** — plus finding **C3-M4**.

**The method backup graph is 18.1-04** (UID method/link/provenance arrays, tombstones, purge, reconciliation, v1→v2 wire migration, `phone_region_override` default). This plan **adds the lifecycle fields as a new backup format version v3** (owner decision 2026-08-28 — the lifecycle fields change the portable JSON shape, and `types.ts:13`'s bump-on-shape-change contract therefore requires a v3, not an additive v2 extension): `tracking_enabled` + nullable cadence in the v3 wire format, a new `v2→v3` forward migration defaulting existing contacts to Bound with their existing positive cadence (the two Unbound settings defaults included), and **C3-M4** (reject illegal lifecycle/cadence combos → `BackupSchemaError` before apply). These fields cannot exist until the v11 migration (18.2-01), which is why they were held out of 18.1-04.

**Flag for the re-plan:** this plan may be small — consider whether to **fold it into 18.2-02**. It also **shares `types.ts`, `backup-schema.ts`, and `restore-apply.ts` with 18.1-04**; the re-plan must reconcile shared-file ownership (18.1-04 lands first, this extends its v2 format).

<objective>Extend the normalized-method backup graph to carry Bound/Unbound lifecycle and validate its combinations.
Purpose: keep the sync-ready model recoverable once lifecycle exists, rejecting impossible lifecycle/cadence states before apply.
Output: lifecycle wire fields, forward-migration defaults, and C3-M4 validation.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/backup/types.ts
@src/backup/backup-schema.ts
@src/backup/restore-apply.ts</context>

## Artifacts this phase produces

- Lifecycle wire fields (`tracking_enabled`, nullable cadence) layered onto the v2 backup graph.
- Pre-apply validation of illegal lifecycle/cadence combinations (C3-M4).

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Carry lifecycle fields through export/restore and forward-migration defaults</name>
  <read_first>src/backup/types.ts; src/backup/backup-schema.ts; src/backup/restore-apply.ts; src/db/migrations/011-contact-lifecycle-schema.ts; 18-PATTERNS.md Backup, restore, and tombstones</read_first>
  <files>src/backup/types.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts</files>
  <reversibility rating="costly">Adding durable lifecycle fields to the portable recovery graph requires version compatibility support; existing-version parsing remains supported.</reversibility>
  <behavior>
    - `BACKUP_FORMAT_VERSION` is bumped 2→3; the v3 contact record carries `trackingEnabled` (0 or 1) and nullable/dormant `intervalDays`.
    - A new v2→v3 forward migration sets `trackingEnabled=1`, retains positive legacy cadence, and adds the absent `includeUnboundNeverContacted=0` and `birthdayUnboundEnabled=1` settings; the completed v1→v2 method migration (18.1-04, which adds `phoneRegionOverride=null`) is left intact.
    - Restore writes tracking_enabled and nullable cadence into the v11 contacts table without violating its CHECKs or one-way trigger.
  </behavior>
  <action>Bump `BACKUP_FORMAT_VERSION` from 2 to 3 in types.ts and extend the manifest contact type with `trackingEnabled` and nullable `intervalDays`; extend backup-schema/export/restore to serialize and reconcile them on top of the method graph owned by 18.1-04. Add a NEW `v2→v3` FORWARD_MIGRATIONS entry that sets `trackingEnabled=1`, retains positive legacy cadence, and adds the absent `includeUnboundNeverContacted=0` and `birthdayUnboundEnabled=1` settings — leave the completed v1→v2 method migration (which already adds `phoneRegionOverride=null`) untouched so a v1 backup still chains v1→v2→v3. In restore-apply, write tracking_enabled and nullable cadence into the v11 contacts table without violating its CHECKs or one-way trigger. Implement D-02 and D-04; do not add a system-contact refresh or delete interpretation.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts</automated></verify>
  <acceptance_criteria>Export→parse→restore tests prove `BACKUP_FORMAT_VERSION` is 3, the v3 contact record round-trips `trackingEnabled` and nullable cadence, a v2 fixture upgrades through v2→v3 to `trackingEnabled=1` with the two lifecycle setting defaults (and a v1 fixture still chains v1→v2→v3), and restore writes lifecycle state into the v11 table without constraint violations.</acceptance_criteria>
  <done>The portable backup graph carries the full Bound/Unbound lifecycle once v11 exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reject illegal lifecycle/cadence combinations before apply</name>
  <read_first>src/backup/backup-schema.ts; src/backup/restore-apply.test.ts; src/db/migrations/011-contact-lifecycle-schema.ts</read_first>
  <files>src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.test.ts</files>
  <behavior>
    - Parse (manifest-only, no local DB read) requires each contact `intervalDays` null or a positive integer, `trackingEnabled` exactly 0 or 1, and a Bound (`trackingEnabled === 1`) contact to carry non-null cadence; it rejects these self-contained illegal combinations with BackupSchemaError.
    - A separate pre-transaction merge-planning phase — which alone reads the local target row — detects a merge input whose newer cadence would violate the durable v11 one-way trigger and resolves it deterministically (reject vs skip vs modified-at rule), distinguishing a malformed backup (BackupSchemaError at parse) from a valid backup that loses a merge conflict.
    - Both the parse rejections and the merge-planning resolution complete before restore-apply opens its transaction or issues SQL.
  </behavior>
  <action>In validate, require each contact `intervalDays` to be null or a positive integer, `trackingEnabled` to be exactly 0 or 1, and a Bound (`trackingEnabled === 1`) contact to carry non-null cadence; reject each such self-contained illegal combination with BackupSchemaError (manifest-only, no local DB read). Then add a distinct pre-transaction merge-planning step that reads the local target row and detects a merge input whose newer cadence would violate the durable v11 one-way trigger; define its resolution explicitly (reject, skip, or modified-at rule) and prove it distinguishes a malformed backup from a valid-but-conflicting merge. Add targeted BackupSchemaError cases for intervalDays zero/negative/non-integer, Bound with null cadence, and non-0/1 trackingEnabled at parse; add merge-planning cases for the trigger-conflicting-cadence input against a seeded local row. Assert every invalid graph and every unresolved conflict is caught before restore-apply opens its transaction or issues SQL. Preserve existing backup format-version handling, parameterized writes, transaction boundaries, and Phase 17 data-revision rules. This addresses Cycle 3 C3-M4.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts</automated></verify>
  <acceptance_criteria>Negative tests prove each self-contained malformed lifecycle/cadence graph returns BackupSchemaError at parse, the merge-planning step resolves a trigger-conflicting-cadence input against a seeded local row by its defined rule (distinguishing malformed from valid-but-conflicting), and neither parse rejection nor conflict resolution can partially mutate local data before the restore transaction opens.</acceptance_criteria>
  <done>Impossible lifecycle/cadence states are rejected before any restore transaction opens.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Parsed manifest to transactional restore | Untrusted backup content proposes durable lifecycle changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-21 | Tampering | manifest/restore lifecycle graph | high | mitigate | Strict lifecycle/cadence validation before a single restore transaction and integration tests. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in backup work. |
</threat_model>
<verification>Run the named schema and restore integration suites at each task, keeping lifecycle graph work within the declared backup owners. The repository-wide test, TypeScript, and color compatibility gate is deferred to 18.2-10.</verification>
<success_criteria>Backup and restore carry the whole Bound/Unbound lifecycle and reject impossible lifecycle/cadence states before apply.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-09-SUMMARY.md` when done</output>
