---
phase: 10-share-sheet-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.config.ts
  - patches/expo-share-intent+8.0.1.patch
  - modules/orbit-share-finish/**
autonomous: true
requirements: [CAP-01, CAP-03, CAP-04]
user_setup: []

must_haves:
  truths:
    - "The Android build registers Orbit as a text/plain-ONLY share target (never text/* or image/*)."
    - "A Chrome link share carries its page title to JS via EXTRA_SUBJECT (patched), with a bare-URL fallback when no extra is set."
    - "A native finish() bridge returns the user to the sharing app (plain finish(), never finishAndRemoveTask())."
  artifacts:
    - "patches/expo-share-intent+8.0.1.patch (committed; postinstall-applied)"
    - "modules/orbit-share-finish local Expo module exposing finishActivity()"
    - "app.config.ts share-intent plugin tuple + scheme: orbit"
  key_links:
    - "app.config.ts plugin tuple appended AFTER the name-dedupe filter (01-01 lesson) — a duplicate plugin entry is a prebuild error."
    - "postinstall runs patch-package on `npm ci` on the desktop build host (droid), so the EXTRA_SUBJECT patch travels with the tar transfer."
  prohibitions:
    - "MUST NOT register text/* or image/* (attack-surface minimization + text/html error branch)."
    - "MUST NOT set androidMultiIntentFilters (keeps ACTION_SEND_MULTIPLE out of v1)."
    - "MUST NOT use BackHandler.exitApp() for return-to-source (RN-version-dependent; may land on home)."
    - "MUST NOT introduce a .ts import into app.config.ts (breaks Expo's native TS loader — header hazard)."
---

<objective>
Lay the native + build foundation for share-sheet capture: install `expo-share-intent@8.0.1` and `patch-package`, patch the library's Kotlin module to read `EXTRA_SUBJECT` (so Chrome page titles reach JS), register the `text/plain`-only intent filter + a `scheme` through the config plugin, and add a tiny local Expo native module that exposes a deterministic `finishActivity()` (plain `Activity.finish()`) for return-to-source.

Purpose: Everything else in Phase 10 (the picker query, the payload logic, the screen, the JS wiring) is pure TS composed over settled foundations — this plan is the only genuinely new native-risk surface, and it must land first so the share intent can reach JS and the app can return to the source app.
Output: A committed patch, a local finish module, and an `app.config.ts` that makes the release build a share target — all invisible to a Metro reload, so verification is Pixel-UAT via the desktop pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-share-sheet-capture/10-RESEARCH.md
@.planning/phases/10-share-sheet-capture/10-CONTEXT.md
@.planning/phases/10-share-sheet-capture/10-PATTERNS.md
@app.config.ts
@docs/runbooks/desktop-build-pipeline.md

# RESEARCH Q1 (patch mechanism), Q2 (filter/scheme + dedupe), Q3 (finish bridge) hold the exact
# verified facts and the conceptual Kotlin diff. app.config.ts:54-77 is the tuple-dedupe analog
# (the expo-image-picker tuple). Do NOT re-derive — mirror.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-share-intent + patch-package and author the EXTRA_SUBJECT Kotlin patch</name>
  <files>package.json, patches/expo-share-intent+8.0.1.patch</files>
  <read_first>
    - `.planning/phases/10-share-sheet-capture/10-RESEARCH.md` §Q1 (patch mechanism + conceptual Kotlin diff + pipeline note) and §Standard Stack / §Package Legitimacy Audit.
    - `app.config.ts:2-13` header — the native-TS-loader hazard (patch-package's postinstall is a plain diff-applier and does NOT touch it; do not add a .ts import).
  </read_first>
  <action>
    Install `expo-share-intent` with `npx expo install expo-share-intent` — this pins the SDK-57-aligned 8.0.1 and pulls the `expo-constants` / `expo-linking` peers (currently absent) at SDK-compatible versions (do NOT hand-pick versions; let expo install resolve). Add `patch-package` as a devDependency (`npm install --save-dev patch-package`) and add a `"postinstall": "patch-package"` script to package.json (if a postinstall already exists, chain patch-package onto it). Both packages passed the RESEARCH Package Legitimacy Audit (OK/Approved, no [SUS]/[SLOP]); `expo-share-intent` is owner-locked in the dossier (single-maintainer risk owner-accepted) — no legitimacy checkpoint. Then author the patch: locate `node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt` (the text/plain branch reads `intent.getCharSequenceExtra(Intent.EXTRA_TITLE)` near line 132), and change the title read so `EXTRA_SUBJECT` is preferred with `EXTRA_TITLE` as the fallback — the meta "title" becomes `intent.getStringExtra(Intent.EXTRA_SUBJECT) ?: intent.getCharSequenceExtra(Intent.EXTRA_TITLE)` (per D-CAP EXTRA_SUBJECT decision, CAP-03; Chrome puts the page title in EXTRA_SUBJECT). Run `npx patch-package expo-share-intent` to generate `patches/expo-share-intent+8.0.1.patch`, and COMMIT the patch file (it is the source of truth — the node_modules edit is disposable). Keep the bare-URL fallback intact in JS (the payload logic, plan 10-02, already handles no-title). The patch is loud-on-drift: on any library version bump the `+8.0.1` filename no longer matches and install errors visibly — it can never silently no-op.
  </action>
  <verify>
    <automated>test -f patches/expo-share-intent+8.0.1.patch && grep -q 'EXTRA_SUBJECT' patches/expo-share-intent+8.0.1.patch && node -e "const p=require('./package.json');if(!p.dependencies['expo-share-intent'])throw 'no dep';if(!p.devDependencies['patch-package'])throw 'no patch-package';if(!/patch-package/.test(p.scripts.postinstall||''))throw 'no postinstall'"</automated>
    <human-check>On the desktop pipeline (`npm ci` on droid), patch-package applies the patch cleanly during install (no fuzz/error). Sharing a Chrome link into the release APK surfaces the page title as the display text (not the bare URL). — deferred to end-of-phase Pixel UAT (release APK required).</human-check>
  </verify>
  <done>expo-share-intent + patch-package installed; the committed EXTRA_SUBJECT patch applies via postinstall.</done>
  <acceptance_criteria>
    - `expo-share-intent@8.0.1` is in dependencies; `patch-package` is a devDependency; `postinstall` runs `patch-package`.
    - `patches/expo-share-intent+8.0.1.patch` exists, is committed, and its diff adds the `EXTRA_SUBJECT ?: EXTRA_TITLE` title read to `ExpoShareIntentModule.kt`.
    - No app.config.ts `.ts` import was introduced by the install.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the text/plain-only intent filter + scheme in app.config.ts</name>
  <files>app.config.ts</files>
  <read_first>
    - `app.config.ts:36-77` — the plugin dedupe block; `pickerPlugin` (lines 68-76) is the tuple-append analog you MUST mirror.
    - `.planning/phases/10-share-sheet-capture/10-RESEARCH.md` §Q2 (exact config shape + dedupe mechanics + scheme requirement).
  </read_first>
  <action>
    Add the share-intent config plugin as a `[name, options]` TUPLE `["expo-share-intent", { androidIntentFilters: ["text/plain"] }]` (per the D-CAP text/plain-only decision, CAP-01). Because a tuple is a distinct Set member from a bare string, it CANNOT be Set-deduped — mirror the existing `pickerPlugin` handling EXACTLY: build the string-plugin Set as today, then in the returned array filter the share-intent name out of the string plugins and append the tuple once, alongside the existing `pickerPlugin` append (a duplicate plugin entry is a prebuild error, 01-01 lesson). Add a top-level `scheme: "orbit"` to the returned ExpoConfig (the config plugin REQUIRES a scheme and none is currently set). Do NOT set `androidMultiIntentFilters` (leaving it unset keeps `ACTION_SEND_MULTIPLE` unregistered — multi-item share is out of v1). Do NOT register `text/*` or `image/*`. Leave the `launchMode` alone — the plugin's default `{"android:launchMode":"singleTask"}` on `.MainActivity` is intentional and app-wide (a side effect Phases 11/12 inherit); do not override it.
  </action>
  <verify>
    <automated>grep -q 'expo-share-intent' app.config.ts && grep -q 'text/plain' app.config.ts && grep -q 'scheme' app.config.ts && ! grep -q 'androidMultiIntentFilters' app.config.ts && ! grep -q 'text/\*' app.config.ts && npx biome check app.config.ts</automated>
    <human-check>Release APK appears in the Android share sheet for a text/plain share (Chrome link, a text selection) and does NOT appear for an image share. `expo prebuild --clean` succeeds with no duplicate-plugin error. — end-of-phase Pixel UAT.</human-check>
  </verify>
  <done>app.config.ts registers the text/plain-only share target with scheme:"orbit", no duplicate plugin, no multi/broad filters.</done>
  <acceptance_criteria>
    - `app.config.ts` returns the share-intent tuple exactly once, appended after the name-dedupe filter (no duplicate plugin entry).
    - A top-level `scheme: "orbit"` is present.
    - `androidMultiIntentFilters` is not set; `text/*` and `image/*` are not registered.
    - biome clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add a local Expo native module exposing a deterministic finishActivity()</name>
  <files>modules/orbit-share-finish/** , package.json</files>
  <read_first>
    - `.planning/phases/10-share-sheet-capture/10-RESEARCH.md` §Q3 (finish bridge rationale + conceptual module) and §Assumptions Log A1 (why NOT BackHandler.exitApp()).
    - Expo local-module docs (create-expo-module `--local`) for the SDK-57 scaffolding shape; verify the current API via Context7 if unsure.
  </read_first>
  <action>
    Create a local Expo native module (e.g. `npx create-expo-module@latest --local` → `modules/orbit-share-finish`) that exposes a single synchronous native function `finish` returning void. The Kotlin definition calls `appContext.currentActivity?.finish()` — a PLAIN `Activity.finish()`, never `finishAndRemoveTask()` and never `finishAffinity()` (per the D-CAP return-to-source decision, CAP-04: plain finish() returns to the sharing app; finishAndRemoveTask lands on home). Export a typed JS wrapper `finishActivity(): void` from the module's `index.ts` so screens import `finishActivity` without touching the native object directly. Strip the scaffold's example iOS/web surface down to the Android `finish` (this is an Android-first, Android-only capability; a no-op or absent web/iOS impl is acceptable — the app is Android-first). Do NOT use `BackHandler.exitApp()` (its RN-0.86 native mapping is uncertain and may translate to home). Keep the module tiny — one function.
  </action>
  <verify>
    <automated>test -d modules/orbit-share-finish && grep -rq 'finish' modules/orbit-share-finish && ! grep -rq 'finishAndRemoveTask\|exitApp' modules/orbit-share-finish && npx tsc --noEmit</automated>
    <human-check>After a capture commit + the auto-return timer, the release APK calls finishActivity() and the device returns to the SOURCE app (not the launcher/home). Also verify on an API-36 Pixel that the library's intent re-launch survives Android 16 intent-redirection hardening. — end-of-phase Pixel UAT (requires prebuild --clean + release APK).</human-check>
  </verify>
  <done>A local finishActivity() native module exists (plain finish(), never finishAndRemoveTask/exitApp).</done>
  <acceptance_criteria>
    - `modules/orbit-share-finish` exists, exposes a Kotlin `finish` calling `currentActivity?.finish()`, and exports a JS `finishActivity()`.
    - No `finishAndRemoveTask`, `finishAffinity`, or `BackHandler.exitApp()` appears in the module.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
Symbols/files this plan produces (for source-grounding):
- `patches/expo-share-intent+8.0.1.patch`
- `modules/orbit-share-finish/` local Expo module → JS export `finishActivity(): void`; Kotlin `finish` function
- `package.json`: `expo-share-intent` dependency, `patch-package` devDependency, `postinstall` script, `expo-constants`/`expo-linking` peers
- `app.config.ts`: `scheme: "orbit"`, `["expo-share-intent", { androidIntentFilters: ["text/plain"] }]` plugin tuple
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external app → Orbit share intent | Untrusted `EXTRA_TEXT`/`EXTRA_SUBJECT` from a third-party app crosses into Orbit here. |
| Android OS → task/activity re-launch | The library re-launches capture as the task root; Android 15/16 finish + intent-redirection rules apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering/Spoofing | intent filter (config plugin) | medium | mitigate | Register `text/plain` ONLY; never `text/*`/`image/*`; `androidMultiIntentFilters` unset (attack-surface minimization; avoids the text/html error branch). |
| T-10-03 | Elevation of Privilege | library intent re-launch (Android 16) | medium | mitigate | The re-launch carries a resolved component so it should pass API-36 hardening; verify on an API-36 Pixel (on-device check, Task 3 human-check). |
| T-10-SC | Tampering (supply chain) | npm installs (expo-share-intent, patch-package) | high | mitigate | Both passed the RESEARCH Package Legitimacy Audit (OK/Approved, no [SUS]/[SLOP]); `expo-share-intent` owner-locked in the dossier (single-maintainer bus-factor risk owner-accepted); `patch-package` is standard tooling; the committed patch is loud-on-drift; re-verify `expo-share-intent` is still 8.0.1 latest at build time. No blocking-human checkpoint (neither [SUS]/[SLOP]; owner-locked) — mitigated, does not block. |

Capture opens NO new network path and requests NO new runtime permission — the privacy posture is strengthened, not weakened.
</threat_model>

<verification>
- Wave-merge gate: `npx tsc --noEmit && npx biome check . && npm run check:colors` clean.
- Patch present + committed; `postinstall` wired; scheme + text/plain tuple present; no `androidMultiIntentFilters`/`text/*`.
- On-device (end-of-phase Pixel UAT, release APK via desktop pipeline): share sheet registration (text/plain only), EXTRA_SUBJECT labelling, finish()→return-to-source, API-36 intent-redirection survival.
</verification>

<success_criteria>
- `expo-share-intent@8.0.1` + `patch-package` installed; EXTRA_SUBJECT patch committed and postinstall-applied.
- `app.config.ts` registers the text/plain-only share target with a scheme, no duplicate plugin, no multi/broad filters.
- A local `finishActivity()` native module exists (plain finish(), never finishAndRemoveTask/exitApp).
</success_criteria>

<output>
Create `.planning/phases/10-share-sheet-capture/10-01-SUMMARY.md` when done.
</output>
