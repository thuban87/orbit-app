---
phase: 10-share-sheet-capture
plan: 06
type: execute
wave: 4
depends_on: ["10-05"]
files_modified:
  - src/screens/CaptureScreen.tsx
autonomous: true
requirements: [CAP-02, CAP-04]
user_setup: []

must_haves:
  truths:
    - "Long-press a face enters multi-select (corner checkmark badge + a persistent 'Done · N' bar); tap toggles faces; Done writes N independent fuel rows in ONE transaction."
    - "The optional note recomposes the just-written row(s)' display text to `note — label` (via editFuel) with the url column untouched; for multi-attach the note applies to all N rows."
    - "The ＋ New contact tile inline-creates a name-only contact (lands never-contacted), then writes the fuel row to it, then confirms and returns to source — NO 'add detail now?' prompt."
    - "A demoted tap-to-reveal search live-filters the grid by name; it is not autofocused until revealed."
  artifacts:
    - "src/screens/CaptureScreen.tsx → multi-select state + Done·N bar, note recompose, inline-create surface, tap-to-reveal search"
  key_links:
    - "Multi-attach uses captureMultiAttach (10-03) — N addFuelCore in ONE inWriteTransaction; the note applies the same composed text to all N via editFuel."
    - "Inline-create composes createContactFull name-only (omit firstInteraction → last_contact NULL) then addFuel to the returned id (two transactions, RESEARCH Q5)."
  prohibitions:
    - "MUST NOT nest inWriteTransaction (compose captureMultiAttach once; never call addFuel inside a multi loop)."
    - "MUST NOT prompt for contact detail after inline-create (name-only; refined later on the profile)."
    - "MUST NOT overwrite the url column with the note; MUST NOT mark a touchpoint anywhere."
    - "No hardcoded colours; the note/toast surface is setState + setTimeout, never per-frame animation."
---

<objective>
Enrich the CaptureScreen with the three remaining flows: long-press multi-select (badges + "Done · N" bar → `captureMultiAttach` writing N independent rows), the optional-note surface (recomposing display text to `note — label` via `editFuel`, url untouched, applied to all N on multi-attach), inline name-only create (`createContactFull` name-only → fuel row → confirm → return), and the demoted tap-to-reveal search. After this plan the phase's full CAP-02/CAP-04 surface is complete.

Purpose: These are the enrichments layered on the single-tap slice (10-05); they share `CaptureScreen.tsx`, so they land after it.
Output: The complete capture picker + note + multi-attach + inline-create.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-share-sheet-capture/10-UI-SPEC.md
@.planning/phases/10-share-sheet-capture/10-RESEARCH.md
@.planning/phases/10-share-sheet-capture/10-CONTEXT.md
@src/screens/CaptureScreen.tsx
@src/db/capture-dao.ts
@src/logic/capture-logic.ts
@src/db/contacts-dao.ts
@src/db/fuel-dao.ts

# CaptureScreen.tsx (from 10-05) is the base. captureMultiAttach (10-03) is the fan-out; resolveCapturePayload
# (10-02) already handles the note→`note — label` recompose (pass note into it). createContactFull name-only
# (contacts-dao.ts:106-184, omit firstInteraction) is the inline-create path. UI-SPEC "Long-press → multi-select",
# "＋ New contact → inline name-only create", "Confirmation + optional-note surface", and the locked testIDs
# are the contract.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Long-press multi-select + "Done · N" → captureMultiAttach</name>
  <files>src/screens/CaptureScreen.tsx</files>
  <read_first>
    - `.planning/phases/10-share-sheet-capture/10-UI-SPEC.md` §"Long-press → multi-select" (badge geometry, Done·N bar, exit rules) + the `capture-face-check-{id}` / `capture-multiselect-bar` / `capture-multiselect-done` testIDs.
    - `src/db/capture-dao.ts` (`captureMultiAttach`) + `src/db/fuel-dao.ts:52-70` (`NewFuelItem`).
  </read_first>
  <action>
    Add a multi-select mode to CaptureScreen: `onLongPress` on a face enters multi-select with that face selected; tapping a face TOGGLES it in/out of the selection set (in normal mode a tap still single-commits per 10-05). Selected faces show a filled-`accent` corner checkmark badge (18x18, borderRadius 9, top-right inset -2, check glyph in `colors.background`, testID `capture-face-check-{id}`) plus a `borderStrong` tile outline. A persistent bottom bar overlays the grid while in multi-select: filled-accent, label "Done · {N}" (16/700, `colors.background`), testID `capture-multiselect-bar` / `capture-multiselect-done`, `minHeight:44`. Back / Close exits multi-select WITHOUT writing (if a selection exists, first Back clears selection/exits mode; a second Back cancels capture). On Done: build N `NewFuelItem` rows (one per selected id, each `newUid()`, kind 'topic', source 'share', the resolved displayText + url, `localDateTime()` stamps) and call `captureMultiAttach(getExecutor(), rows)` (per D-CAP multi-attach decision, CAP-02 — N independent rows, ONE transaction; never nest the mutex). Then show the confirmation "Saved to {N} contacts" (reuse the confirmation surface + auto-return from 10-05). All colours via tokens; the payload survives multi-select because the user stays in-app.
  </action>
  <verify>
    <automated>grep -q "captureMultiAttach" src/screens/CaptureScreen.tsx && grep -q "capture-multiselect-done" src/screens/CaptureScreen.tsx && grep -q "onLongPress" src/screens/CaptureScreen.tsx && npx tsc --noEmit && npm run check:colors && npx biome check src/screens/CaptureScreen.tsx</automated>
    <human-check>Release APK: long-press a face enters multi-select (badge shown); tapping toggles others; "Done · N" writes N independent fuel rows (one per contact); confirmation "Saved to N contacts"; Back exits multi-select without writing. — end-of-phase Pixel UAT.</human-check>
  </verify>
  <done>Long-press multi-select writes N independent fuel rows via captureMultiAttach in one transaction; Back exits without writing.</done>
  <acceptance_criteria>
    - Long-press enters multi-select; tap toggles; the Done·N bar shows the live count; Done writes N independent rows via captureMultiAttach.
    - Back/Close exits multi-select without writing; a supplied note (Task 2) applies to all N.
    - tsc + check:colors + biome clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Optional-note surface — recompose display text (`note — label`), url untouched</name>
  <files>src/screens/CaptureScreen.tsx</files>
  <read_first>
    - `.planning/phases/10-share-sheet-capture/10-UI-SPEC.md` §"Confirmation + optional-note surface" (note field, Done/Skip, `capture-note-input`/`-done`/`-skip`) + §"Payload → display text".
    - `src/logic/capture-logic.ts` (pass `note` into `resolveCapturePayload` to get the recomposed displayText) + `src/db/fuel-dao.ts:81-97, 163-195, 247-253` (`EditFuelInput`, `editFuelCore`, `editFuel` — patch-scoped UPDATE of `text` only).
  </read_first>
  <action>
    Complete the "Add a note" affordance in the confirmation surface (from 10-05): tapping it cancels the auto-return timer (the note is never rushed) and reveals a multiline note `TextInput` (`surface` bg, `border`, radius 8, 15/400, `minHeight >= 88`, keyboard closed until engaged, testID `capture-note-input`) with a note "Done" (accent, testID `capture-note-done`) and a subtle "Skip" (`textSecondary`, testID `capture-note-skip`). On note Done: recompose the display text by calling `resolveCapturePayload({ text, webUrl, title, note })` (10-02 already implements `note — label`, url untouched), then UPDATE the just-written row(s)' `text` via `editFuel` (patch-scoped — sets `text` only, leaves `url`/`created_at` untouched). For multi-attach, apply the SAME composed text to ALL N rows (loop `editFuel` over the written ids, or wrap N `editFuelCore` in one `inWriteTransaction` — never nest the mutex). Then show the toast + arm the auto-return (reuse 10-05). Skip dismisses the note and arms the auto-return with the unchanged single-tap/multi display text. The `url` column is NEVER touched by the note (F-CAP-6/F-CAP-15).
  </action>
  <verify>
    <automated>grep -q "capture-note-done" src/screens/CaptureScreen.tsx && grep -q "editFuel" src/screens/CaptureScreen.tsx && grep -q "resolveCapturePayload" src/screens/CaptureScreen.tsx && npx tsc --noEmit && npm run check:colors && npx biome check src/screens/CaptureScreen.tsx</automated>
    <human-check>Release APK: after a single or multi commit, "Add a note" cancels auto-return, opens the note field; Done recomposes the display text to `note — label` on the profile (url still separate/intact); Skip keeps the original text. — end-of-phase Pixel UAT.</human-check>
  </verify>
  <done>The optional note recomposes display text to `note — label` via editFuel (text-only), url untouched, applied to all N on multi-attach; Skip keeps the original.</done>
  <acceptance_criteria>
    - The note surface recomposes display text to `note — label` via resolveCapturePayload + editFuel (text-only patch); the url column is never changed.
    - On multi-attach the note applies to all N rows; Skip leaves the original text.
    - tsc + check:colors + biome clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Inline name-only create + tap-to-reveal search</name>
  <files>src/screens/CaptureScreen.tsx</files>
  <read_first>
    - `.planning/phases/10-share-sheet-capture/10-UI-SPEC.md` §"＋ New contact → inline name-only create" + §"Revealed search field" + the `capture-inline-*` / `capture-search-*` testIDs.
    - `src/db/contacts-dao.ts:75-184` (`createContactFull` — omit `firstInteraction` for the name-only never-contacted path; `intervalDays` is required + must be a positive integer) + `src/screens/create-contact-logic.ts:28-31` (the app default interval is Monthly = 30).
    - `src/db/fuel-dao.ts:237-245` (`addFuel`) + `src/db/database.ts` (`getExecutor`, `localDateTime`) + `src/db/uid.ts` (`newUid`).
  </read_first>
  <action>
    Wire the ＋ New contact tile (`capture-new-contact-tile`) to open an inline-create surface (`capture-inline-create`): a single name `TextInput` (`surface`, `border`, radius 8, 15/400, AUTOFOCUSED — the one place typing is the point, testID `capture-inline-name-input`, placeholder "Name") + a filled-accent "Create & save" primary (`minHeight:44`, testID `capture-inline-submit`). On submit: create the contact NAME-ONLY via `createContactFull(getExecutor(), { uid: newUid(), name: name.trim(), intervalDays: 30, now: localDateTime() })` — OMIT `firstInteraction` so `last_contact` stays NULL (never-contacted, per D-CAP inline-create decision, CAP-04; the 30-day Monthly interval is the app default the create form uses, refined later on the profile), then write the fuel row to the returned `contactId` via `addFuel` (topic/share, the resolved displayText + url) — two transactions is acceptable (RESEARCH Q5). Then the standard confirmation surface (with the optional-note affordance) → auto-return. NO "add detail now?" prompt (CONTEXT #5 — detail is refined later on the profile). Also add the demoted tap-to-reveal search: `capture-search-reveal` toggles a `TextInput` (`capture-search-input`, `surface`, autofocus ONLY when revealed, placeholder "Search people") that live-filters the loaded grid by name (case-insensitive substring on the in-memory array — the set is small, Claude's discretion) + a `capture-search-clear` (✕) that restores the full grid. The keyboard stays closed on the fast path (search is never autofocused on entry).
  </action>
  <verify>
    <automated>grep -q "createContactFull" src/screens/CaptureScreen.tsx && grep -q "capture-inline-submit" src/screens/CaptureScreen.tsx && grep -q "capture-search-input" src/screens/CaptureScreen.tsx && npx tsc --noEmit && npm run check:colors && npx biome check src/screens/CaptureScreen.tsx</automated>
    <human-check>Release APK: ＋ New contact → type a name → Create & save → a never-contacted contact is created (last_contact NULL) with the captured fuel (visible on the never-contacted screen), then return to source, no detail prompt. Search reveal filters the grid by name; keyboard stays closed until reveal. — end-of-phase Pixel UAT.</human-check>
  </verify>
  <done>The ＋ tile inline-creates a name-only never-contacted contact then captures fuel to it (no detail prompt); tap-to-reveal search filters by name, keyboard closed until revealed.</done>
  <acceptance_criteria>
    - The ＋ tile inline-creates a name-only never-contacted contact (last_contact NULL, interval 30) then writes the captured fuel row to it, confirms, and returns — no detail prompt.
    - Tap-to-reveal search live-filters the grid by name and is not autofocused until revealed.
    - tsc + check:colors + biome clean.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
Symbols/files this plan produces (for source-grounding):
- `src/screens/CaptureScreen.tsx` (extended): multi-select state + Done·N bar, note recompose, inline-create surface, tap-to-reveal search
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared payload → N writes / inline-created contact | Untrusted payload fans out to N rows and can create a contact. |
| capture flow → status engine | Inline-create + multi-attach must not mark a touchpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Info-disclosure | note recompose + inline-create name | medium | mitigate | All writes are `?`-bound via `editFuel`/`addFuel`/`createContactFull`; the note recompose keeps the url canonical/separate (never overwritten). The name is stored as data. |
| T-10-06 | Tampering (status corruption) | inline-create + multi-attach | high | mitigate | Inline-create omits `firstInteraction` (last_contact stays NULL, never-contacted); multi-attach uses only `addFuelCore`; neither writes a touchpoint — DATA-04 single-writer invariant intact. |
| T-10-04 | Denial (data loss) | multi-select / note timing | medium | mitigate | The row(s) are written on pick (single) or on Done (multi) before the note; the note is a text-only patch afterward; the flow stays in-app so a background can't lose a committed row. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run check:colors && npx biome check .` clean; full node suite green.
- On-device (end-of-phase Pixel UAT): multi-select N-write, note recompose (url intact), inline name-only create (never-contacted), tap-to-reveal search, keyboard closed on the fast path.
</verification>

<success_criteria>
- Multi-select writes N independent rows in one transaction; the optional note recomposes `note — label` without touching url; inline name-only create lands never-contacted and captures fuel; search is demoted/tap-to-reveal — completing CAP-02/CAP-04.
</success_criteria>

<output>
Create `.planning/phases/10-share-sheet-capture/10-06-SUMMARY.md` when done.
</output>
