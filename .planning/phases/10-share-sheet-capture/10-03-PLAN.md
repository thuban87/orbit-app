---
phase: 10-share-sheet-capture
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/db/capture-read.ts
  - src/db/capture-read.test.ts
  - src/db/capture-dao.ts
  - src/db/capture-dao.test.ts
autonomous: true
requirements: [CAP-01, CAP-02, CAP-04]

user_setup: []

must_haves:
  truths:
    - "The picker read orders contacts favourites → capture-MRU → rest, INCLUDES never-contacted, and EXCLUDES archived."
    - "Capture-MRU is derived from existing fuel.created_at + contact_id — no new column, no migration."
    - "Multi-attach writes N independent fuel rows in ONE inWriteTransaction (compose addFuelCore N times); a throw mid-loop rolls back all N."
    - "Every captured row is kind='topic', source='share'; each contact owns its own row (its own uid)."
    - "Capture writes NOTHING to contacts.last_contact and NO interaction row — a capture onto a never-contacted contact leaves last_contact NULL."
  artifacts:
    - "src/db/capture-read.ts → CapturePickRow interface + listCapturePickContacts()"
    - "src/db/capture-dao.ts → captureMultiAttach()"
    - "src/db/capture-read.test.ts + src/db/capture-dao.test.ts (Wave 0 node tests)"
  key_links:
    - "capture-read uses `archived_at IS NULL` ONLY — NEVER dashboard-read's BASE_WHERE (which carries last_contact IS NOT NULL and would hide never-contacted people)."
    - "captureMultiAttach composes the NON-mutexed addFuelCore inside ONE inWriteTransaction — never nests the non-reentrant mutex."
  prohibitions:
    - "MUST NOT call recomputeLastContactCore / insertInteractionCore / any recency writer in the capture path (capture is not a touchpoint — DATA-04 single-writer stays intact)."
    - "MUST NOT add an index/UNIQUE/migration; MUST NOT inherit `last_contact IS NOT NULL`."
    - "MUST NOT call addFuel / inWriteTransaction from inside the multi-attach loop (permanent hang)."
---

<objective>
Build the two node-tested DB modules capture needs: `capture-read.ts` (the favourites → capture-MRU → rest picker query, archived-excluded, never-contacted-included, no new column) and `capture-dao.ts` (the multi-attach fan-out composing `addFuelCore` N times in ONE transaction, plus the no-touchpoint invariant). These are the last two Wave-0 node-test files.

Purpose: Keep the correctness-critical ordering and write-atomicity out of the `.tsx` screen and in tested DAO/read modules, exactly like the rest of the data layer.
Output: `listCapturePickContacts()`, `captureMultiAttach()`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-share-sheet-capture/10-RESEARCH.md
@.planning/phases/10-share-sheet-capture/10-PATTERNS.md
@src/db/dashboard-read.ts
@src/db/fuel-dao.ts
@src/db/transaction.ts
@src/db/contacts-dao.ts
@src/db/fuel-dao.test.ts

# dashboard-read.listFavourites (288-295) / listNeverContacted (264-282) are the read-chokepoint idiom
# (async getAllAsync only, archived-only filter). RESEARCH Pattern 1 holds the exact verified picker SQL.
# fuel-dao.addFuelCore (129-150) + transaction.inWriteTransaction (42-57) are the multi-attach primitives;
# fuel-dao.ts:16-19 names THIS phase's fan-out. fuel-dao.test.ts:42-78 is the node-sqlite harness to reuse.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capture-read — favourites → capture-MRU → rest picker query</name>
  <files>src/db/capture-read.ts, src/db/capture-read.test.ts</files>
  <behavior>
    - A favourite (favourite_rank NOT NULL) sorts before a recently-captured non-favourite.
    - Among non-favourites, a contact with more recent fuel.created_at sorts before one with older/no fuel (capture-MRU DESC; NULL last_captured sorts last).
    - A never-contacted contact (last_contact NULL) is INCLUDED in the result.
    - An archived contact (archived_at NOT NULL) is EXCLUDED.
    - Ties within a band break by name COLLATE NOCASE ASC (deterministic).
    - Each row carries id, name, photo, modified_at, favourite_rank, last_captured (for Avatar cacheBust + ordering).
  </behavior>
  <read_first>
    - `src/db/dashboard-read.ts:1-56, 264-295` — the header contract (async, read-only, no mutex, no migration) + `listFavourites` / `listNeverContacted` shape + `FavouriteRow`.
    - `.planning/phases/10-share-sheet-capture/10-RESEARCH.md` §"Pattern 1" (the exact verified SQL) and §Q4 (mutex-free read is safe concurrent with the launch sweep).
    - `src/db/fuel-dao.test.ts:42-58` — `openTestDb` + `nodeSqliteExecutor` + `runMigrations([migration001])` + `seedContact` harness.
  </read_first>
  <action>
    Author `src/db/capture-read.ts` mirroring the dashboard-read chokepoint idiom: a pure `export function listCapturePickContacts(exec: SqlExecutor): Promise<CapturePickRow[]>` that runs a single `exec.getAllAsync` (async ONLY — never sync, no transaction, no mutex; RESEARCH Q4 confirms this is safe alongside the concurrent launch sweep because it reads `contacts`/`fuel`, not `contact_custom_values`). Declare `export interface CapturePickRow { id: number; name: string; photo: string | null; modified_at: string; favourite_rank: number | null; last_captured: string | null; }`. Use the RESEARCH Pattern-1 SQL verbatim: LEFT JOIN a `(SELECT contact_id, MAX(created_at) AS last_captured FROM fuel GROUP BY contact_id)` subquery, `WHERE c.archived_at IS NULL` ONLY (per D-CAP picker-scope decision, CAP-01 — INCLUDES never-contacted; do NOT add `last_contact IS NOT NULL`, and do NOT import dashboard-read's BASE_WHERE), ordered `(favourite_rank IS NULL), favourite_rank ASC, (last_captured IS NULL), last_captured DESC, name COLLATE NOCASE ASC`. Capture-MRU is derived from existing fuel columns — NO new column, NO migration, NO index. Write `capture-read.test.ts` using the fuel-dao.test.ts harness (seed favourites, a recently-captured contact via a fuel row, a never-contacted contact, an archived contact) asserting the ordering + inclusion/exclusion in the behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/db/capture-read.test.ts</automated>
  </verify>
  <done>listCapturePickContacts orders favourites → MRU → rest, includes never-contacted, excludes archived, no new column — node-proven.</done>
  <acceptance_criteria>
    - All Task-1 behavior cases pass; favourites → MRU → rest ordering is deterministic.
    - Never-contacted rows appear; archived rows do not.
    - No migration/index added; the query is a single async getAllAsync with no transaction/mutex.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture-dao — multi-attach fan-out + no-touchpoint invariant</name>
  <files>src/db/capture-dao.ts, src/db/capture-dao.test.ts</files>
  <behavior>
    - captureMultiAttach(exec, rows) with N NewFuelItem rows writes exactly N fuel rows in ONE transaction, each with its own uid, kind='topic', source='share', the given text/url/createdAt.
    - A throw mid-loop (e.g. a bad row on iteration 2 of 3) rolls back ALL rows (0 fuel rows persist) — atomic fan-out.
    - After a capture onto a never-contacted contact, `SELECT last_contact FROM contacts` for that contact stays NULL, and the interactions table has zero rows for it — capture is not a touchpoint.
    - A single-contact capture (N=1) writes exactly one fuel row (the standalone addFuel wrapper is the single-tap path).
  </behavior>
  <read_first>
    - `src/db/fuel-dao.ts:52-70, 121-150, 237-245` — `NewFuelItem`, `addFuelCore` (the non-mutexed insert primitive), `addFuel` (the single-row wrapper); the header (16-19) names this fan-out.
    - `src/db/transaction.ts:11-57` — the NON-REENTRANCY rule (compose cores, wrap once; never nest).
    - `src/db/contacts-dao.ts:106-184` — `createContactFull` as the canonical "compose non-mutexed cores in ONE inWriteTransaction" precedent and the name-only inline-create path (omit `firstInteraction` → last_contact NULL).
  </read_first>
  <action>
    Author `src/db/capture-dao.ts` exporting `captureMultiAttach(exec: SqlExecutor, rows: NewFuelItem[]): Promise<void>` that opens ONE `inWriteTransaction` and, inside it, `for (const row of rows) await addFuelCore(exec, row)` (per D-CAP multi-attach decision, CAP-02 — N independent rows, one atomic transaction; RESEARCH Q5). The mutex is NON-REENTRANT: compose `addFuelCore` (the non-mutexed core), wrap ONCE — NEVER call `addFuel`/`inWriteTransaction` inside the loop (documented permanent hang). Do NOT import or call `recomputeLastContactCore`, `insertInteractionCore`, or any recency writer — capture is NOT a touchpoint (DATA-04 single-writer invariant stays intact); rows carry `kind:'topic'`, `source:'share'`, minted by the caller. Reuse the standalone `addFuel` wrapper for the single-tap path (do not add a redundant single-row composer). For inline-create-then-capture, the CONSUMER (screen, 10-06) composes the existing `createContactFull` (name-only, omit `firstInteraction`) then `addFuel` to the returned id — two transactions is acceptable (RESEARCH Q5); do NOT add a fused create+fuel core here. Write `capture-dao.test.ts` with the fuel-dao harness asserting: N-rows-one-transaction, mid-loop rollback (mirror contacts-dao's mid-composition ROLLBACK test), each row's own uid + topic/share, and the no-touchpoint invariant (capture onto a never-contacted contact → last_contact stays NULL, zero interaction rows).
  </action>
  <verify>
    <automated>npx vitest run src/db/capture-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>captureMultiAttach writes N atomic rows (topic/share) via the fuel writer with no last_contact/interaction side effect — node-proven.</done>
  <acceptance_criteria>
    - All Task-2 behavior cases pass; the full suite + tsc + check:colors are clean.
    - captureMultiAttach writes N rows atomically; a mid-loop throw rolls back all N.
    - The no-touchpoint test proves last_contact stays NULL and no interaction row is written.
    - capture-dao imports no recency/interaction writer.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
Symbols/files this plan produces (for source-grounding):
- `src/db/capture-read.ts` → `interface CapturePickRow`, `function listCapturePickContacts(exec)`
- `src/db/capture-dao.ts` → `function captureMultiAttach(exec, rows)`
- `src/db/capture-read.test.ts`, `src/db/capture-dao.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared payload → fuel INSERT | Attacker-influenceable text is written to the fuel table. |
| capture write → status engine | Capture must not become a recency/status signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Info-disclosure | captureMultiAttach INSERT | medium | mitigate | Every value is `?`-bound via `addFuelCore` (fuel has FIXED columns — no identifier interpolation); the payload is stored as data only. `RANKED_FUEL_EXCLUSIONS` keeps blank/off_limits out of glance surfaces downstream. |
| T-10-06 | Tampering (status corruption) | recency/status engine | high | mitigate | The capture path uses ONLY `addFuelCore` — no `last_contact` write, no interaction row; the no-touchpoint node test proves it. This preserves the DATA-04 single-writer invariant and keeps the status engine uncorrupted by sharing habits (the LinkListener hazard). |
</threat_model>

<verification>
- `npx vitest run src/db/capture-read.test.ts src/db/capture-dao.test.ts` green.
- Wave-merge: `npx vitest run && npx tsc --noEmit && npm run check:colors` clean.
</verification>

<success_criteria>
- The picker read orders favourites → MRU → rest (archived-excluded, never-contacted-included) with no new column.
- Multi-attach writes N atomic rows through the fuel writer with no touchpoint side effect — all node-proven.
</success_criteria>

<output>
Create `.planning/phases/10-share-sheet-capture/10-03-SUMMARY.md` when done.
</output>
