---
phase: 18-contact-data-normalization
plan: 06
type: execute
wave: 5
depends_on: [18-02, 18-03, 18-04, 18-09]
files_modified: [src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/screens/ComposeScreen.tsx, src/screens/compose-logic.ts, src/screens/compose-logic.test.ts]
autonomous: true
requirements: [CDN-01, CDN-02, CDN-03]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Profile shows ordered non-empty method groups, formats them for people, and never offers a handoff for a non-actionable primary."
    - "Unbound profile hides status/intensity/favourite/snooze cadence treatment but retains details, history, gravity, archive, birthday, and explicit AI."
    - "Unbind confirmation preserves history/cadence/dormant favourite rank; Bind restores lifecycle eligibility when cadence is positive."
  artifacts: [src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/ComposeScreen.tsx]
  key_links: ["Compose reads one actionable primary phone from contact-methods-read before calling expo-sms; Profile invokes the 18-09 lifecycle-effects owner so Unbind reconciles schedules and refreshes widget targets."]
  prohibitions:
    - "Contact methods must not create endpoint-specific interaction history, a method chooser, or a general endpoint router in Phase 18."
---
<objective>Complete the profile and existing Compose handoff slice for normalized methods and lifecycle transitions.
Purpose: a user can understand/manage one relationship state without exposing inactive cadence treatment or unsafe handoff targets.
Output: profile method/lifecycle UI and primary-actionable Compose integration.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/screens/ContactProfileScreen.tsx
@src/screens/ComposeScreen.tsx
@src/db/contact-read.ts
@src/db/contact-lifecycle-dao.ts
@src/services/contact-lifecycle-effects.ts</context>

## Artifacts this phase produces

- Profile method groups plus explicit Bind/Unbind lifecycle panel/actions.
- Compose SMS handoff sourced solely from a primary actionable phone read.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Display one primary actionable phone on Profile and compose from it</name>
  <read_first>18-UI-SPEC.md Profile methods and lifecycle; src/screens/ContactProfileScreen.tsx; src/screens/ComposeScreen.tsx:426-440; src/db/contact-methods-read.ts</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/screens/ComposeScreen.tsx, src/screens/compose-logic.ts, src/screens/compose-logic.test.ts</files>
  <behavior>
    - Profile renders phone first with label, formatted value, quiet Primary marker, extension, and complete accessibility label.
    - Compose receives exactly the selected actionable primary phone; no actionable phone retains Copy-first/add-number fallback.
    - Invalid rows remain readable with the exact helper and are never supplied to SMS.sendSMSAsync.
  </behavior>
  <action>Test profile/compose view-model logic first, then wire the existing profile and Compose shells through contact-methods-read. Implement D-01's sole-authority/actionability rules with format-only display values. Preserve the existing coarse message/history taxonomy and do not add Call/Email routing, a chooser, or raw method logging.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts src/screens/compose-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>One primary actionable phone is rendered and passed to the existing SMS path end to end; invalid/missing primary cases prove no handoff occurs.</acceptance_criteria>
  <done>Profile and Compose now consume normalized method data safely.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render Bound and Unbound lifecycle states correctly</name>
  <read_first>18-UI-SPEC.md Profile; src/db/impact-read.ts; src/db/contact-lifecycle-dao.ts; src/screens/ContactProfileScreen.tsx</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts</files>
  <behavior>
    - Bound preserves current hierarchy; Unbound shows quiet panel, retains gravity/details/history, and has no status/intensity/favourite/snooze treatment.
    - Dormant cadence enables Bind immediately; never-assigned cadence reveals FrequencyPicker and blocks Bind until positive.
    - Method section omits entirely when both groups are empty and renders both groups in stored order otherwise.
  </behavior>
  <action>Apply the exact UI-SPEC lifecycle panel, tokens, headings, 44px controls, accessibility state, and load/error posture through a pure profile view model. Implement D-02 and D-03; pass nullable cadence to UI only as lifecycle state and avoid inventing paused/reset status language.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>View-model tests cover Bound, dormant Unbound, never-assigned Unbound, no methods, one invalid method, and both ordered type groups.</acceptance_criteria>
  <done>Profile accurately explains participation state while retaining non-cadence relationship information.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Execute confirmed Bind and Unbind with reload-safe failure handling</name>
  <read_first>18-UI-SPEC.md Profile; src/screens/ContactProfileScreen.tsx existing action callbacks; src/services/contact-lifecycle-effects.ts; src/db/contact-lifecycle-dao.ts</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts</files>
  <action>Add the exact native Unbind Alert and non-optimistic Bind/Unbind dispatch/reload/error Alert flow. Disable duplicate transitions while write is pending, call the 18-09 `unbindWithLifecycleEffects` owner rather than the raw DAO, and reload confirmed profile state after success. Implement D-02 and D-03: cadence/history/rank stay stored, rank visibility follows Bound read paths, source data is untouched, and the post-commit notification/widget effects have their dedicated owner tests.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts src/services/contact-lifecycle-effects.test.ts src/db/contact-lifecycle-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Tests prove confirmation copy/options, single-flight actions, retry-safe failure state, dormant cadence preservation, reload-derived rebound state, and dispatch through the lifecycle-effects seam.</acceptance_criteria>
  <done>Users can make intentional lifecycle transitions without optimistic data loss or stale status UI.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| method read to Compose launcher | Contact endpoint data becomes an OS SMS handoff input. |
| profile action to lifecycle DAO | User confirmation changes proactive eligibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-18 | Elevation/Tampering | Compose handoff | high | mitigate | Consume only selected actionable primary read; tests cover invalid/null suppression. |
| T-18-19 | Tampering | lifecycle action | medium | mitigate | Native confirmation, single-flight DAO call, reload-on-success, generic error alert. |
| T-18-20 | Information disclosure | profile/UI logs | medium | mitigate | Formatted visual display and no canonical/raw data in diagnostic paths. |
| T-18-SC | Tampering | package install | high | accept | No dependency changes in this plan. |
</threat_model>
<verification>Run profile/Compose logic and DAO tests, tsc/colors; complete device handoff proof in 18-08.</verification>
<success_criteria>Actions remain intentional, safe, and lifecycle-aware while relationship data stays whole.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-06-SUMMARY.md` when done</output>
