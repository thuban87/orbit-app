---
phase: 18-contact-data-normalization
plan: 12
type: execute
wave: 3
depends_on: [18-03]
files_modified: [src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/sun-picker-read.ts, src/db/sun-picker-read.test.ts, src/logic/sun-occupant-logic.ts, src/logic/sun-occupant-logic.test.ts, src/screens/OrreryScreen.tsx]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A saved Unbound sun renders self without changing app_settings.sun_contact_id, and rebinding restores the saved contact sun."
    - "Orrery active population and sun candidates are Bound-only, while never-contacted Bound users remain candidate-eligible."
    - "One shared sun predicate receives trackingEnabled from 18-03 header data; Orrery and Settings do not fork lifecycle policy."
  artifacts: [src/db/orrery-read.ts, src/db/sun-picker-read.ts, src/logic/sun-occupant-logic.ts, src/screens/OrreryScreen.tsx]
  key_links: ["OrreryScreen passes getContactHeader().trackingEnabled from 18-03 into sunOccupantIsSelf, which alone selects the self fallback without mutating app_settings.sun_contact_id.", "Settings consumes the same predicate only after this plan, and listSunCandidates offers only non-archived Bound contacts."]
---
<objective>
Apply the completed lifecycle-header contract to the Orrery and its sun candidate policy.
Purpose: an Unbound saved sun must preserve identity and relationship history without appearing as an active Orrery contact or silently changing the saved choice.
Output: Bound-aware Orrery and picker reads, one shared sun occupant predicate, OrreryScreen wiring, and regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-03-SUMMARY.md
@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@src/db/contact-read.ts
@src/db/orrery-read.ts
@src/db/sun-picker-read.ts
@src/logic/sun-occupant-logic.ts
@src/screens/OrreryScreen.tsx</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Render self for one saved Unbound sun and restore it on Bind</name>
  <read_first>src/db/contact-read.ts; src/logic/sun-occupant-logic.ts; src/logic/sun-occupant-logic.test.ts; src/screens/OrreryScreen.tsx; docs/dossier/18-contact-data-normalization.md Clusters G and K</read_first>
  <files>src/logic/sun-occupant-logic.ts, src/logic/sun-occupant-logic.test.ts, src/screens/OrreryScreen.tsx</files>
  <behavior>
    - A stored `sun_contact_id` for a live Unbound contact renders self but leaves that setting unchanged.
    - Rebinding that same saved contact renders it as the sun again without re-selection.
    - The sole predicate accepts `trackingEnabled` from getContactHeader; OrreryScreen cannot derive or duplicate lifecycle fallback policy.
  </behavior>
  <action>Extend SunOccupantLookup and sunOccupantIsSelf with `trackingEnabled`, then wire OrreryScreen to pass getContactHeader’s lifecycle field supplied by 18-03. Implement D-02 and D-03 with the self fallback exclusively in the shared predicate: do not mutate `app_settings.sun_contact_id`, put a lifecycle branch in orrery-read, or create a second Settings policy. Add tests for Bound, Unbound, missing, and rebinding states, including the unchanged saved reference.</action>
  <verify><automated>npm test -- src/logic/sun-occupant-logic.test.ts</automated></verify>
  <acceptance_criteria>Tests prove the saved Unbound reference becomes a self rendering only, preserves its database setting, and returns to the selected contact after Bind.</acceptance_criteria>
  <done>The one end-to-end Orrery sun path respects lifecycle without losing the user’s prior selection.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Restrict Orrery population and sun candidates to Bound contacts</name>
  <read_first>src/db/orrery-read.ts; src/db/orrery-read.test.ts; src/db/sun-picker-read.ts; src/db/sun-picker-read.test.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/sun-picker-read.ts, src/db/sun-picker-read.test.ts</files>
  <behavior>
    - Orrery active population excludes Unbound contacts without changing saved sun identity.
    - Sun candidates include non-archived Bound contacts regardless of whether they have ever been contacted.
    - An Unbound contact is neither rendered as an active Orrery body nor offered by the picker.
  </behavior>
  <action>Add `tracking_enabled=1` to the Orrery population owner and pin listSunCandidates to non-archived Bound contacts while retaining its favourites-first/name ordering. Preserve never-contacted Bound candidates and leave candidate selection as a read, not a setting mutation. Implement D-02 and D-03 and address cycle-2 reviews H2 and L14 with source-adjacent Bound/Unbound, never-contacted, archived, ordering, and saved-reference regression cases.</action>
  <verify><automated>npm test -- src/db/orrery-read.test.ts src/db/sun-picker-read.test.ts</automated></verify>
  <acceptance_criteria>Read tests prove Unbound users are absent from active Orrery/picker populations, never-contacted Bound users remain available, and candidate ordering remains deterministic.</acceptance_criteria>
  <done>Orrery data and its selector use the same Bound lifecycle boundary as the shared rendering policy.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| lifecycle header/query state to Orrery rendering | A mutable lifecycle state selects self versus a saved contact and determines active population eligibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-35 | Elevation/Tampering | Orrery sun policy | high | mitigate | One shared trackingEnabled predicate, unchanged saved-reference tests, and no query/UI policy duplicate. |
| T-18-36 | Tampering | Orrery and picker reads | high | mitigate | Bound-only owner predicates with never-contacted and archived boundary tests. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18-01 owns the approved dependency gate. |
</threat_model>
<verification>Run the named sun-policy, Orrery-read, and picker-read suites only. The repository-wide test, TypeScript, and color compatibility gate remains solely in 18-08 after all consumer migrations complete.</verification>
<success_criteria>Saved sun identity survives lifecycle transitions, while active Orrery and picker populations remain Bound-only.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-12-SUMMARY.md` when done</output>
