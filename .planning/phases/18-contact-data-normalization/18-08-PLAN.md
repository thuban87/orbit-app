---
phase: 18-contact-data-normalization
plan: 08
type: execute
wave: 5
depends_on: [18-04, 18-05, 18-06, 18-07]
files_modified: [src/db/migrations/009-contact-data-normalization.test.ts, src/db/contact-methods-dao.test.ts, src/db/contact-lifecycle-dao.test.ts, src/db/dashboard-read.test.ts, src/db/orrery-read.test.ts, src/db/notification-read.test.ts, src/db/impact-read.test.ts, src/backup/restore-apply.test.ts]
autonomous: false
requirements: [CDN-01, CDN-02, CDN-03, CDN-04]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Automated tests cover every requirement’s migration, lifecycle, query-owner, and restore invariants."
    - "CDN-01 manual parser/migration edge review and the UI-SPEC device backstops are completed on a real upgraded Android build."
    - "No PII method data reaches AI context or routine logs; all OS handoff is actionability-gated."
  artifacts: [src/db/migrations/009-contact-data-normalization.test.ts, src/db/contact-methods-dao.test.ts, src/db/contact-lifecycle-dao.test.ts]
  key_links: ["The final release build upgrades real v8 data, then exercises DAO/UI/widget/notification refresh against the completed v9 model."]
  prohibitions:
    - "Phase 18 must not ship picker/import/reconciliation, generic sync, rich merge management, passive listener detection, reorder polish, Interaction Assist, endpoint history, or provider analytics."
---
<objective>Prove the whole phase on its complete automated suite and a real Android migration/UI handoff.
Purpose: backstop state-space and device-only behavior that a node SQLite or RN logic test cannot establish.
Output: completed edge audit and owner-approved device UAT record.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-VALIDATION.md
@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/18-contact-data-normalization/18-01-SUMMARY.md
@.planning/phases/18-contact-data-normalization/18-07-SUMMARY.md</context>

## Artifacts this phase produces

- Requirement-mapped regression coverage and source-retirement audit.
- Owner-reviewed Android v8 upgrade/UAT evidence.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove one upgraded contact across migration, lifecycle, UI reads, and backup round-trip</name>
  <read_first>18-VALIDATION.md; 18-RESEARCH.md Validation Architecture; src/db/migrations/009-contact-data-normalization.test.ts; src/backup/restore-apply.test.ts</read_first>
  <files>src/db/migrations/009-contact-data-normalization.test.ts, src/db/contact-methods-dao.test.ts, src/db/contact-lifecycle-dao.test.ts, src/db/dashboard-read.test.ts, src/db/orrery-read.test.ts, src/db/notification-read.test.ts, src/db/impact-read.test.ts, src/backup/restore-apply.test.ts</files>
  <behavior>
    - Requirement test matrix includes CDN-01 manual/probe marker; CDN-02 boundary/adjacency/empty/ordering/precision; CDN-03 boundary/precision; CDN-04 boundary/precision/idempotency/concurrency.
    - A retired scalar-column audit fails if production references remain outside migration fixtures.
    - Actionability, SQL binding, PII/AI/log exclusion, and source-non-destructive behavior remain covered by their owning suites.
  </behavior>
  <action>Fill the Phase 18 validation matrix with source-adjacent cases and a scoped source-retirement audit that excludes migration fixture literals. Run the complete suite, TypeScript, and colors check. Treat the spec-less edge probe as a coverage ledger: write defensible automated predicates, retain device-only items as explicit backstops, and flag no unresolved material assumptions. Preserve D-01 through D-04 and scope fences rather than broadening into deferred functionality.</action>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>All four requirements have executable evidence; source audit proves production code has no legacy scalar authority and all requirement-specific quick commands pass.</acceptance_criteria>
  <done>The completed codebase has one traceable automated verification path for every Phase 18 truth.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perform owner-led Android migration and interaction UAT</name>
  <read_first>18-VALIDATION.md Manual-Only Verifications; 18-UI-SPEC.md UI Considerations; docs/runbooks/desktop-build-pipeline.md</read_first>
  <action>Build the release APK through the documented desktop pipeline, install it over an upgraded v8 fixture, and prepare the named test contacts/settings. This validates D-01 through D-04 on device, including parser display/region behavior, bind/unbind/rebind, widgets/notifications refresh, backup restore, and all UI-SPEC backstop states.</action>
  <what-built>Full normalized-model migration, lifecycle, screens, settings, and backup/restore behavior are automated and ready on a release build.</what-built>
  <how-to-verify>On the Pixel: upgrade v8 data; inspect migrated primary methods; create/edit invalid/extension/multiple methods; unbind and rebind contacts with dormant and never-assigned cadence; verify Unbound dashboard/list/search/profile and Bound-only Orrery/favourites/widget/decay; exercise settings; confirm Compose only offers actionable primary SMS; export then restore; inspect 320dp overflow, long text, keyboard scroll, loading/error retry, and no raw canonical/source fields. Approve only if no history/cadence/rank loss and no deferred import/reconciliation UI appears.</how-to-verify>
  <resume-signal>Reply approved, or provide the failing behavior and device evidence.</resume-signal>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Owner records the release-build result for every manual-only validation item and UI backstop, including CDN-01 parser provenance confirmation.</acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release build to real device database | Migration and UI execute against user-like persisted relationship data. |
| actionability read to Android SMS | Device handoff can expose or use a contact endpoint. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-24 | Tampering/DoS | release migration | high | mitigate | Real v8 upgrade UAT after fixture/rollback tests and backup verification. |
| T-18-25 | Information disclosure | UI/AI/logs | high | mitigate | Device check plus automated absence/actionability assertions; no canonical/raw source display. |
| T-18-26 | Elevation | Android handoff | high | mitigate | Exercise only actionable-primary Compose path and invalid fallback. |
| T-18-27 | Tampering | source evidence | medium | mitigate | UAT confirms no source-based delete/overwrite control is present. |
| T-18-SC | Tampering | package provenance | high | mitigate | Confirm the 18-01 blocking human approval remains recorded. |
</threat_model>
<verification>Automated full suite is required before release UAT; final human checkpoint records device-only predicates and unresolved defects.</verification>
<success_criteria>Every requirement is code-proven and the owner has seen the real Android migration and lifecycle behavior.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-08-SUMMARY.md` when done</output>
