---
phase: 18-contact-data-normalization
plan: 07
type: execute
wave: 5
depends_on: [18-01, 18-02, 18-05]
files_modified: [src/backup/types.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts]
autonomous: true
requirements: [CDN-01, CDN-02, CDN-04]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Lossless manifest carries methods, extensions, primary/order, lifecycle/cadence, external links, provenance, and their stable UIDs/tombstones."
    - "Merge restore does not clear a locally stored dormant favourite rank, while portable backup deliberately does not make favourite rank a new wire field; cross-contact shared canonical methods remain intact without scalar compatibility fields."
    - "Stale source evidence survives restore unless an explicit local tombstone authorizes removal."
    - "Pre-Phase-18 v1 backups deterministically forward-migrate to the v2 normalized graph with current lifecycle and portable-settings defaults."
    - "CDN-04 boundary, precision, idempotency, and concurrency cases are verified on the reconciliation graph."
  artifacts: [src/backup/types.ts, src/backup/export-manifest.ts, src/backup/backup-schema.ts, src/backup/reconciliation.ts, src/backup/restore-apply.ts]
  key_links: ["The closed restore entity map reconciles normalized children by UID after live parent survivor validation, and backup-schema forwards v1 snapshots before validation.", "One typed contact-purge registry drives child preview counts, tombstone emission, and explicit fan-out deletes, so adding a tombstone entity cannot silently skip purge evidence."]
  prohibitions:
    - "Backup/restore must not reinterpret a missing or stale system source as authorization to delete or overwrite Orbit-owned data."
---
<objective>Carry the normalized contact model through lossless export validation and transactional restore.
Purpose: ensure the sync-ready local model remains recoverable without making provenance authoritative.
Output: versioned manifest graph, strict validation, and UID reconciliation support.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/backup/export-manifest.ts
@src/backup/backup-schema.ts
@src/backup/restore-apply.ts
@src/backup/reconciliation.ts</context>

## Artifacts this phase produces

- Backup manifest arrays and strict graph validation for normalized children.
- Restore entity registration that preserves local/provenance semantics and tombstones.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Export and parse one normalized contact losslessly</name>
  <read_first>src/backup/types.ts; src/backup/export-manifest.ts:39-83; src/backup/backup-schema.ts:77-175; src/backup/export-manifest.test.ts; 18-PATTERNS.md Backup, restore, and tombstones</read_first>
  <files>src/backup/types.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts</files>
  <behavior>
    - Manifest uses first-class UID-bearing arrays for methods, links, and method provenance.
    - Contact record carries tracking_enabled and nullable/dormant cadence but no scalar phone/email compatibility fields.
    - Parse rejects orphan children, duplicate UIDs, invalid parent/tombstone relationships, unsupported entity types, illegal lifecycle/cadence combinations, and two surviving primary methods of one type before restore opens its transaction.
    - A v1 scalar-method fixture advances deterministically to v2 child rows, `tracking_enabled=1`, the three v9 portable-setting defaults, and every v2 required array before strict validation.
  </behavior>
  <reversibility rating="costly">Adding child entities and tombstone types to an already portable recovery graph requires durable schema/version compatibility support, but existing-version parsing remains supported.</reversibility>
  <action>Extend the existing manifest types/export snapshot/strict parser rather than introducing a second format. Bump BACKUP_FORMAT_VERSION to 2 and register the one deterministic v1-to-v2 FORWARD_MIGRATIONS entry before validation: convert each v1 scalar phone/email into a stable UID formatted `legacy-method:${contactUid}:${type}` (deterministic by parent and type, disjoint from newUid’s UUID shape), preserve the legacy presentation value, set lifecycle `tracking_enabled=1`, retain positive legacy cadence, remove the retired scalar `phone` and `email` keys from each migrated contact record, and add absent `phoneRegionOverride=null`, `includeUnboundNeverContacted=0`, and `birthdayUnboundEnabled=1` settings. Before validate runs, materialize every v2 required array, including empty external-link, method-provenance, and tombstone arrays where the v1 input has none. Add tests for derived UID determinism and disjointness from newUid output. In validate, require each contact `intervalDays` to be null or a positive integer, `trackingEnabled` to be exactly 0 or 1, and a Bound (`trackingEnabled === 1`) contact to carry non-null cadence; scan surviving contact-method rows by `(contactUid, type)` and reject more than one primary with BackupSchemaError before restore planning. Include D-01, D-02, and D-04 fields exactly, parent-reference and same-file survivor checks, allowed new tombstone entity types, and the closed portable-settings allowlist. Retain only formatted/structured backup data; do not add a system-contact refresh or delete interpretation. This addresses cycle-2 reviews L11 and L12 plus Cycle 3 C3-M4/C3-M6.</action>
  <verify><automated>npm test -- src/backup/export-manifest.test.ts src/backup/backup-schema.test.ts</automated></verify>
  <acceptance_criteria>Export→parse tests prove a single normalized contact retains all method/lifecycle/provenance fields, a v1 fixture upgrades with its phone/email methods intact, strips scalar compatibility keys, and has deterministic lifecycle/settings defaults. Negative parser tests return BackupSchemaError before restore planning for malformed child graphs, illegal cadence/lifecycle cells, and duplicate surviving primaries by contact/type.</acceptance_criteria>
  <done>One complete normalized relationship record has a lossless, validated backup representation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconcile normalized child entities through restore</name>
  <read_first>src/backup/restore-apply.ts:35-175; src/backup/reconciliation.ts; src/backup/restore-apply.test.ts; src/backup/reconciliation.test.ts; src/db/tombstones-dao.ts; src/db/purge-dao.ts; src/db/purge-dao.test.ts</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/backup-schema.test.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts</files>
  <behavior>
    - Restore reconciles each UID-bearing child atomically after parent survivor validation.
    - Reapplying the same manifest is idempotent and concurrent/conflicting plan cases fail atomically rather than aliasing data.
    - Stale link/provenance rows remain unless an explicit tombstone is present; no source absence deletes a method/contact.
    - Contact purge previews, tombstones, and explicitly deletes every UID-bearing normalized child; a typed registry makes every TombstoneEntityType disposition explicit.
    - Merge restore leaves an existing local dormant favourite_rank untouched because favourite rank is not promoted into the portable v2 contract.
    - Replace-all removes non-mergeable owned residual rows, including seeded field_history, in an explicit FK-safe order.
  </behavior>
  <action>Extend the exhaustive MergeableEntityType/ENTITY_POLICIES contract in reconciliation.ts and its owner tests before registering contact methods, external contact links, and method provenance in restore-apply's closed entity map. Give every new child an explicit UID write mode and parent survivor policy; make the compiler/test reject an unregistered entity. Derive replaceAllReset’s tombstone and mergeable delete sequences from required typed maps over MergeableEntityType, then pair them with an explicit exhaustively typed residual owned-table list for non-mergeable tables, currently `field_history`; preserve the complete FK-safe delete order (children before contacts/custom_field_defs). Test replace-all tombstones for a method, external link, and provenance UID and seed field_history to prove Replace-all removes it. In purge-dao, derive preview counts, tombstone sources, and explicit child fan-out deletes from one `Record<TombstoneEntityType, PurgeChildSpec | null>` plus the same explicit exhaustively typed non-mergeable residual owned-table list, so every current and future tombstone type and owned residual has an explicit purge disposition; include contact_methods, external_contact_links, and contact_method_provenance in their appropriate child specs. Extend purge tests to prove the preview counts, explicit deletes, tombstone evidence, and residual-table deletion ordering. Keep a single outer transaction and respect existing data_revision conventions. For merge restore only, prove an existing local dormant favourite_rank is never cleared because the contact upsert does not write that non-portable field; replace-all remains the intentional portable-state reset and makes no rank-preservation claim. Implement D-04’s non-destructive provenance and D-01/D-02 data retention without auto-merge, source refresh, or generic sync logic. This addresses cycle-2 reviews H1, M7, M8, and L10 plus Cycle 3 C3-M1.</action>
  <verify><automated>npm test -- src/backup/restore-apply.test.ts src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts src/db/purge-dao.test.ts</automated></verify>
  <acceptance_criteria>Integration tests prove export→parse→restore preserves methods/extensions/primary/order, stale evidence, nullable cadence, shared canonical values, and history; merge restore does not clobber a local dormant rank; replace-all emits tombstones for every new child and removes seeded field_history through the explicit residual list; purge previews/deletes/tombstones every normalized child and handles residual owned tables in the declared FK-safe order; and repeat/concurrent conflicts meet the existing reconciliation contract.</acceptance_criteria>
  <done>The normalized graph restores safely without inventing source authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Cover explicit-delete tombstones and backup wire compatibility</name>
  <read_first>src/db/tombstones-dao.ts; src/backup/backup-schema.ts; src/backup/restore-apply.test.ts; Phase 17 summaries for reconciliation contracts</read_first>
  <files>src/db/tombstones-dao.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.test.ts</files>
  <reversibility rating="costly">New tombstone entity types change the durable reconciliation graph; the established UID/version rules make the forward-compatible change recoverable without deleting existing backup data.</reversibility>
  <action>Add targeted BackupSchemaError cases for method/link/provenance tombstones, malformed same-UID/survivor combinations, intervalDays zero/negative/non-integer, Bound with null cadence, non-0/1 trackingEnabled, duplicate surviving primary methods by `(contactUid, type)`, and a merge input whose newer cadence would otherwise violate the durable trigger. Assert each invalid graph is rejected by parse before restore-apply opens its transaction or issues SQL. Preserve existing backup format-version handling, parameterized writes, transaction boundaries, and Phase 17 data-revision rules. Implement D-04 while keeping source disappearance separate from an explicit local delete. This addresses Cycle 3 C3-M4/C3-M6.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts</automated></verify>
  <acceptance_criteria>Negative and idempotency tests demonstrate only a valid explicit tombstone removes a normalized child; malformed lifecycle/primary graphs return BackupSchemaError before apply; and failed validation cannot partially mutate local data.</acceptance_criteria>
  <done>Deletion evidence is precise, sync-ready, and never inferred from a source’s current contents.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite export to portable manifest | Local PII and relationship graph become a recovery artifact. |
| Parsed manifest to transactional restore | Untrusted backup content proposes durable changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-21 | Tampering | manifest/restore graph | high | mitigate | Strict UID/parent/tombstone validation before a single restore transaction and integration tests. |
| T-18-22 | Information disclosure | export/logging | high | mitigate | Keep full data inside existing encrypted backup path; avoid raw record logging and route/privacy changes. |
| T-18-23 | Tampering | provenance reconciliation | high | mitigate | Explicit tombstones only; source absence has no destructive behavior. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in backup work. |
</threat_model>
<verification>Run the named manifest, schema, reconciliation, and restore integration suites at each task, keeping normalized graph work within the declared backup owners. The repository-wide test, TypeScript, and color compatibility gate is deferred to 18-08 after every scalar and nullable-cadence consumer has been migrated.</verification>
<success_criteria>Backup and restore preserve the whole normalized relationship graph with safe non-destructive provenance semantics.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-07-SUMMARY.md` when done</output>
