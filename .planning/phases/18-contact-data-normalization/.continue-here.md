---
context: phase
phase: 18-contact-data-normalization
task: pre-cycle-3-internal-revision-gate
total_tasks: 8
status: paused
last_updated: 2026-08-26T00:00:00-05:00
---

# Phase 18 Convergence Handoff

## Current stop point — supersedes the instructions below

Cycle 3 has been aggregated and incorporated: `ba740f2` records the two Claude HIGH plus six actionable MEDIUM findings, and `fa20e62` incorporates them into Plans 01, 03, 05, 07, 08, 10, and 12 plus the UI/review contracts. A fresh internal plan checker confirmed that every Cycle-3 finding has executable ownership, then raised a scope-size blocker for 18-05 and warnings for 18-02, 18-03, 18-07, and 18-09.

Do not respond by splitting plans. A scope-split attempt was interrupted before commit and its only edits (18-02 and 18-03) were fully restored. No new PLAN file exists. No fourth external cycle is permitted.

The owner has paused because the Phase 18 dossier explicitly bundles a broad Bound/Unbound product rollout (dashboard, Orrery, Never Contacted, notifications, widgets, AI, CRUD/Compose, backup/export, and a new Streamlined Export) into a phase whose intended purpose may instead be data normalization plus minimal compatibility. Before any further replan, execution, or status update, obtain an owner decision resolving that product boundary. Do not mark Phase 18 planned, converged, or executed.

## Current update — supersedes every historical narrative below

Cycle 3, the owner-capped final external review cycle, is complete. Do not launch cycle 4. The exact direct read-only Claude gate succeeded with a coherent 23,143-byte Markdown review at `/tmp/orbit-phase18-cycle3-20260826/claude.out`; its paired flagless Codex review succeeded at `/tmp/orbit-phase18-cycle3-20260826/codex-run/gsd-review-codex.md` with `gpt-5.6-terra (reasoning=low)`. No reviewer process is active.

The next session must finish this final cycle without another external review: (1) read and aggregate both artifacts into `18-REVIEWS.md`; (2) replan all actionable findings; (3) run the final independent internal plan checker; and (4) report the convergence result, including any residual issues, to the owner. Current findings are intentionally unincorporated: Claude has 2 HIGH plus 5 MEDIUM (Bound-aware `ring-seq-dao`; full `app_settings`/rebuilt-child migration preservation proof; `field_history` residual delete; dashboard empty-state Unbound count; neutral Never Contacted Unbound rows; strict backup lifecycle validation; distinct Streamlined Export artifact/UI/UAT); Codex adds one MEDIUM (per-contact/per-type primary partial unique constraint plus restore validation). Do not repeat Claude or Codex and do not mark the phase planned/converged before that work completes.

Two of the owner-capped three external review cycles are complete. Cycle 2 used the exact direct read-only Claude gate successfully (24,105-byte coherent Markdown output; only the harmless `MultiEdit` stderr warning), followed by the required flagless Codex lane using `gpt-5.6-terra (reasoning=low)`. The aggregate and full incorporation are committed as `d423ae3` (`docs(18): incorporate cycle-2 plan reviews`); `18-REVIEWS.md` is current.

Do not start cycle 3 yet. The latest independent checker requires two preconditions: (1) split `18-03-PLAN.md`, whose 21 files / 17-file Task 2 exceed its execution scope, into dependency-correct query/status/digest and Orrery sun/picker slices; and (2) obtain the owner's already-designed 18-01 Task 1 phone-actionability choice (`isPossible()`, `isValid()`, or named alternative) with representative-country and extension cases. Re-run the checker after those steps. If it clears, run the third and final external cycle: exact Claude-first command, then flagless Codex, aggregate the result, and stop whether or not convergence is achieved.

`HANDOFF.json` is the authoritative machine-readable state. Preserve the pre-existing uncommitted `.planning/ROADMAP.md` change and unrelated untracked files. Never widen or alter the Claude command; if its completed stdout is empty, stop and ask the owner.

<current_state>
Phase 18 is in plan-review convergence. The initial external review ran successfully with Claude first and Codex second. Review feedback was committed, incorporated into the plans, and then independently checked. The first checker revision is committed. A resumed final plan-checker run found one remaining blocker in 18-02. No external cycle-2 review has started and Phase 18 must not be marked planned/converged yet.
</current_state>

<completed_work>

- Claude gate test succeeded using the read-only direct headless invocation below; it produced a 24.5 KB source-grounded Markdown review.
- Flagless Codex reviewer succeeded through the declared review lane; it used `gpt-5.6-terra (reasoning=low)`.
- Consolidated two-reviewer feedback committed as `a311090` (`docs: cross-AI review for phase 18`) in `18-REVIEWS.md`.
- Review-incorporation replan committed as `185e89f` (`docs(18): revise phase plan from cross-AI reviews`). It added FK-safe migration and relationship fixture coverage, settings defaults, raw-only legacy-region policy, malformed-cadence rollback, v1→v2 backup migration, all previously missed consumers, validation gates, and an 18-10 owner checkpoint.
- First internal checker found 2 blockers + 2 warnings; the planner revision committed as `2a2a270` (`docs(18): address plan verification findings`). It corrected `custom_field_values`, split nullable-cadence impact/AI work into new 18-11, completed task `<files>` lists, and standardized block-style estimates.
- `.planning/ROADMAP.md` has an uncommitted but correct phase-18 metadata update: 11 plans, 18-11 in Wave 3, 18-07/18-09 in Wave 4, and 18-10 in Wave 5.
</completed_work>

<remaining_work>

1. Fix the sole current internal checker blocker in `18-02-PLAN.md`: Task 3 removes scalar `createContactFull`/`updateContactFull` inputs in Wave 2, but `CreateContactScreen.tsx` and `EditContactScreen.tsx` do not move to aggregate inputs until 18-04 (Wave 4). Its `npx tsc --noEmit` gate therefore cannot pass. Remove that repository-wide TypeScript gate from Task 3 and the Wave-2 plan gate, retaining named targeted suites; `18-VALIDATION.md` already reserves full `npx tsc --noEmit` for 18-08. Commit the targeted plan change.
2. Re-run the independent `gsd-plan-checker`. If it passes, run external convergence cycle 2: Claude first as the gate, then flagless Codex, aggregate the two reviews into `18-REVIEWS.md`, and assess current unresolved HIGH/actionable non-HIGH concerns.
3. If cycle 2 still has actionable findings, replan with `--reviews`, run plan-checker, and externally re-review (cycle 3 max). If it clears, update STATE/ROADMAP via the normal convergence completion step and commit generated planning metadata.
</remaining_work>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state.
2. `.planning/phases/18-contact-data-normalization/18-REVIEWS.md` — all current actionable external-review feedback and its disposition.
3. `.planning/phases/18-contact-data-normalization/18-02-PLAN.md` and `18-VALIDATION.md` — the only remaining internal check blocker.
4. `.planning/phases/18-contact-data-normalization/18-01-PLAN.md` through `18-11-PLAN.md` — current executable plan set.
5. `.planning/handoffs/running-claude-headless-reviews.md` — authoritative Claude invocation instructions.
6. `docs/dossier/18-contact-data-normalization.md`, `18-CONTEXT.md`, `18-RESEARCH.md`, `18-UI-SPEC.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` — Phase 18 source of truth.

## Claude Review Gate — Required Invocation

Run Claude once before starting Codex. Build a self-contained prompt that names the Phase 18 plan directory and tells Claude to read the plans and cited source with `Read`, `Grep`, and `Glob`. It must contain: **“Output the full review to stdout as markdown. Write NO files. Do not modify STATE.md, .planning/, or anything on disk.”**

Use this command exactly (from repository root), with unique output names per cycle:

```bash
timeout 900 claude -p \
  --model claude-opus-5 \
  --allowedTools Read Grep Glob \
  --disallowedTools Write Edit MultiEdit NotebookEdit Bash \
  < review-prompt.md \
  > /tmp/claude-review-18-cycle-N.out \
  2> /tmp/claude-review-18-cycle-N.err
```

This worked in this session. Claude 2.1.247 printed a harmless stderr warning that `MultiEdit` was not a recognized tool, but it still ran read-only and returned a 24.5 KB review on stdout. Do not alter flags, widen permissions, add write access, use `--dangerously-bypass-hook-trust`, route through gsd-review's Claude lane, or try multiple fallback variants. If stdout is empty after completion, stop the convergence run and ask the owner as instructed. A source-grounded run can take 5–9 minutes; do not treat buffered empty stdout while the process is active as failure.

After a non-empty Claude review, run the Codex review flaglessly (no hook bypass). The successful initial run used the declared lane:

```bash
node /home/bwales/.codex/gsd-core/bin/gsd-tools.cjs query review-lane invoke \
  --slug codex --run-dir <run-dir> --repo-root "$PWD" \
  --prompt-file <review-prompt.md> --explicit --json
```

## Critical Anti-Patterns (do NOT repeat these)

- Do not spend Codex reviewer usage before the Claude gate succeeds.
- Do not call Claude through gsd-review's Claude lane or retry Claude with invented flag variants. The owner explicitly prohibited this after earlier usage-limit damage.
- Do not interpret an active Claude process with buffered zero-byte stdout as failed; inspect stderr only after it exits or times out.
- Do not mark Phase 18 planned/converged while the checker blocker remains or before cycle-2 external review.
- Do not include unrelated untracked user files in commits. Existing unrelated items include `.planning/debug/replace-apply-failure.md`, `.planning/handoffs/`, `.planning/milestone.lock`, `orbit-home.png`, and `ui.xml`.

## Infrastructure State

- No reviewer processes or agents are running.
- Feature gate `workflow.plan_review_convergence` is enabled.
- Local review scratch from successful cycle 1: `/tmp/orbit-phase18-claude-gate-QefQLg/` (not required to resume; the consolidated artifact is committed).
- Current uncommitted tracked change: `.planning/ROADMAP.md` Phase 18 plan/wave metadata only.

<decisions_made>

- Claude first is a hard gate. This preserves other usage if Claude fails.
- Plan 18-10 requires an explicit owner checkpoint for Streamlined Export PII egress; do not silently decide it during planning.
- New 18-11 isolates nullable-cadence impact and explicit-AI work after 18-03, so the query plan stays within the plan-size threshold.
</decisions_made>

<next_action>
Start with the narrow 18-02 TypeScript-gate correction, commit it, and rerun the plan checker. Only after a pass should you start cycle 2 with the required Claude gate.
</next_action>
