---
phase: 18-contact-data-normalization
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [src/db/contact-methods-dao.ts, src/db/contact-methods-dao.test.ts, src/db/contact-methods-read.ts, src/db/contact-lifecycle-dao.ts, src/db/contact-lifecycle-dao.test.ts, src/db/migrations/009-contact-data-normalization.test.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/screens/CaptureScreen.tsx, src/screens/CaptureScreen.test.tsx]
autonomous: true
requirements: [CDN-01, CDN-02, CDN-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per-contact method drafts atomically collapse same-type canonical duplicates, return typed collision feedback to the caller, retain cross-contact sharing, preserve order, and promote a replacement primary."
    - "Bind/Unbind only changes lifecycle state; existing cadence, favourite rank, and relationship history survive transitions."
    - "The four lifecycle cells are explicit: Bound+positive, Unbound+positive, Unbound+NULL, and forbidden Bound+NULL."
  artifacts: [src/db/contact-methods-dao.ts, src/db/contact-methods-read.ts, src/db/contact-lifecycle-dao.ts]
  key_links: ["Create/edit aggregate writers resolve the effective region at their caller, pass it into the shared normalizer through method cores, and receive a typed same-contact collision result in their existing outer transaction."]
---

<objective>
Make normalized methods and Bound/Unbound transactional write capabilities rather than one-time migration data.
Purpose: preserve data and lifecycle invariants for every later form, profile, backup, and sync operation.
Output: tested method/lifecycle DAOs and updated aggregate contact writers.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/contact-links-dao.ts
@src/db/contacts-dao.ts
@src/db/transaction.ts</context>

## Artifacts this phase produces

- Transactional method diff/read owners with UID, ordering, primary, and tombstone semantics.
- Transactional lifecycle transitions and aggregate create/edit integration.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist and read ordered contact-method drafts</name>
  <read_first>src/db/contact-links-dao.ts:29-284; src/db/contact-links-dao.test.ts:35-314; src/logic/contact-method-normalization.ts; 18-PATTERNS.md Ordered contact methods and provenance</read_first>
  <files>src/db/contact-methods-dao.ts, src/db/contact-methods-dao.test.ts, src/db/contact-methods-read.ts</files>
  <behavior>
    - Removing a current primary promotes the next stored row of the same type.
    - Same-contact canonical duplicates converge to one ordered row and return a typed `canonicalDuplicate` result containing the method type and surviving draft identity; shared cross-contact canonical values persist independently and return no collision.
    - A failed later statement rolls back the complete seeded-vs-current diff and its tombstones.
  </behavior>
  <action>Create method cores and public transaction-owning wrappers from the contact-links DAO analog: stable UID, per-contact display order, parameterized SQL, exact row-count guards, method tombstones for explicit local deletes, and one revision bump for a changed aggregate. Define a typed normalization context whose `effectivePhoneRegion` is supplied by the caller and pass it unchanged into the shared pure normalizer before comparison. Persist incomplete nonblank values as non-actionable. Return a typed aggregate-save collision result only for a same-contact, same-type canonical collapse; never query or signal a cross-contact match. Expose ordered groups and primary actionable selection through a read owner. This implements D-01 and D-04 while retaining stale external evidence rather than treating it as a delete signal.</action>
  <verify><automated>npm test -- src/db/contact-methods-dao.test.ts</automated></verify>
  <acceptance_criteria>Method DAO tests prove effective-region input reaches the shared normalizer, ordering, primary promotion, typed same-contact collision, absence of cross-contact collision, invalid storage, source provenance survival, parameter binding, and atomic rollback.</acceptance_criteria>
  <done>Every contact can durably own zero-to-many normalized phone/email rows with deterministic per-type primary behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce Bind and Unbind state transitions</name>
  <read_first>src/db/contacts-dao.ts:101-178,281-330; src/db/migrations/009-contact-data-normalization.ts; docs/dossier/18-contact-data-normalization.md Clusters G-I; 18-PATTERNS.md Bound/Unbound lifecycle writes</read_first>
  <files>src/db/contact-lifecycle-dao.ts, src/db/contact-lifecycle-dao.test.ts, src/db/migrations/009-contact-data-normalization.test.ts</files>
  <behavior>
    - Unbind leaves last_contact, interactions, cadence, and favourite_rank unchanged while toggling tracking state.
    - Bind accepts a dormant positive cadence or requires a supplied positive cadence when none was assigned.
    - SQL guard rejects Bound+NULL and any positive-to-NULL update, including a direct SQL attempt that rolls back.
  </behavior>
  <action>Create the lifecycle DAO with pre-transaction validation, `inWriteTransaction`, exact affected-row checking, and data-revision behavior matching contacts-dao. Bind and Unbind are named operations, never a nullability shortcut. Exercise D-02 and D-03: retain dormant favourite rank without making it visible here, preserve all relationship data, and never clear an assigned cadence.</action>
  <verify><automated>npm test -- src/db/contact-lifecycle-dao.test.ts src/db/migrations/009-contact-data-normalization.test.ts</automated></verify>
  <acceptance_criteria>Lifecycle tests cover boundary values, adjacent transitions, empty-history contacts, cadence precision, and direct SQL trigger rejection.</acceptance_criteria>
  <done>Lifecycle writes make Bound/Unbound independent from cadence and history at both DAO and SQL layers.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Compose methods and lifecycle into contact create/edit saves</name>
  <read_first>src/db/contacts-dao.ts; src/db/contact-links-dao.ts; src/db/contact-methods-dao.ts; 18-UI-SPEC.md Create contact and Edit contact</read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/contact-methods-dao.test.ts, src/db/contact-lifecycle-dao.test.ts, src/screens/CaptureScreen.tsx, src/screens/CaptureScreen.test.tsx</files>
  <behavior>
    - A Bound create/edit requires a positive cadence; never-assigned Unbound may save with no methods or cadence.
    - A saved positive cadence cannot become NULL through aggregate update input.
    - Aggregate writes apply method differences in the same outer transaction as contact fields and return the DAO-owned collision result without doing UI normalization.
    - Capture's inline name-only create supplies the updated Bound lifecycle/method contract without scalar phone/email inputs.
  </behavior>
  <action>Replace the old scalar phone/email input contract in createContactFull and updateContactFull with typed method drafts, lifecycle data, and the explicit effective-region normalization context. Call method/lifecycle cores inside the existing aggregate transaction rather than nesting public wrappers, and surface their typed same-contact collision result to the form caller together with the saved ordered methods. Update CaptureScreen's inline name-only create and its owner test to pass an explicit Bound positive cadence plus empty method drafts through that aggregate contract; it must not retain scalar compatibility inputs. Implement D-01 and D-02; preserve the stable coarse interaction model and omit endpoint/provider interaction changes.</action>
  <verify><automated>npm test -- src/db/contacts-dao.test.ts src/db/contact-methods-dao.test.ts src/db/contact-lifecycle-dao.test.ts src/screens/CaptureScreen.test.tsx</automated></verify>
  <acceptance_criteria>Aggregate and Capture tests prove a create/edit either commits contact, methods, lifecycle, and collision result together or leaves all unchanged; a region override can affect later normalization while existing canonical identity is never rewritten; and inline capture creation remains Bound with no scalar contract.</acceptance_criteria>
  <done>Create/edit persistence supports the full normalized model without a legacy scalar writer.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Form/restore draft to DAO | User-provided phone, email, labels, and cadence cross into durable SQL. |
| lifecycle command to contacts row | A state change affects proactive eligibility without changing owned relationship data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-05 | Tampering | method/lifecycle DAOs | high | mitigate | `?`-bound SQL, limited typed fields, transaction cores, exact row counts, and rollback tests. |
| T-18-06 | Tampering | cadence transition | high | mitigate | SQL checks/trigger plus DAO validation reject illegal lifecycle cells. |
| T-18-07 | Information disclosure | errors/logs | medium | mitigate | Map save errors to generic UI paths; do not Logger-print methods or canonical values. |
| T-18-08 | Tampering | provenance rows | medium | mitigate | Explicit local-delete tombstones only; no source-link cascade. |
| T-18-SC | Tampering | package install | high | mitigate | Parser package was approved by the blocking checkpoint in 18-01. |
</threat_model>
<verification>Run the named targeted DAO suites after each task and keep execution within each task's declared owner files. The aggregate task intentionally retires scalar contracts before later form consumers migrate in 18-04, so the repository-wide test, TypeScript, and color compatibility gate is deferred to 18-08.</verification>
<success_criteria>All normalized writes preserve atomicity, PII boundaries, lifecycle invariants, and future sync provenance.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-02-SUMMARY.md` when done</output>
