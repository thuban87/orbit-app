---
phase: 18-contact-data-normalization
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified: [src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/contact-status-read.ts, src/db/contact-status-read.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct profile/search retrieval retains both states, while dashboard, Orrery, and favourite projections select only Bound contacts."
    - "Unbound has a dedicated alphabetical read and is included in Never Contacted only through its persisted opt-in."
    - "Status, digest, and header counts remain Bound-only even when direct retrieval retains an Unbound contact."
    - "CDN-03 boundary and precision cases are covered at their owning SQL reads."
  artifacts: [src/db/unbound-read.ts, src/db/dashboard-read.ts, src/db/contact-status-read.ts, src/db/digest-read.ts]
  key_links: ["The lifecycle/read projection seam feeds profile, search, dashboard, Orrery, status, and digest; the dependency-ordered nullable-cadence impact and explicit-AI work is isolated in 18-11, while notification and widget side effects remain isolated in 18-09."]
---
<objective>
Put lifecycle eligibility at the population query owners, retaining relationship retrieval while removing Unbound contacts from proactive cadence surfaces.
Purpose: prevent UI, widget, and headless notification paths from disagreeing about Bound state.
Output: Bound-aware profile/dashboard/Orrery/status/digest predicates, Unbound population read, and owner-level regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/dashboard-read.ts
@src/db/orrery-read.ts
@src/db/impact-read.ts</context>

## Artifacts this phase produces

- SQL-owned lifecycle eligibility across profile, dashboard, Orrery, favourite, status, and digest projections.
- Direct method/lifecycle profile projections and dedicated Unbound retrieval.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Read one Unbound contact through profile and dedicated population paths</name>
  <read_first>src/db/contact-read.ts:65-208; src/db/dashboard-read.ts:257-343; src/screens/NeverContactedScreen.tsx; 18-PATTERNS.md Read owners and downstream consumers</read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts</files>
  <behavior>
    - Contact header/edit assembly returns ordered methods and lifecycle state without scalar phone/email columns.
    - listUnbound returns non-archived Unbound contacts alphabetically with neutral metadata.
    - A profile may retain Unbound history while cadence-dependent presentation receives nullable/disabled inputs.
  </behavior>
  <action>Extend the existing contact-read aggregate with method projections and `tracking_enabled`; create listUnbound from the dashboard-read query conventions. Implement D-01 and D-02 at the named SQL owners rather than screen filters, preserving both-state direct/profile retrieval and no scalar compatibility read.</action>
  <verify><automated>npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts</automated></verify>
  <acceptance_criteria>Tests prove a real Unbound contact keeps details/history through the profile read and appears only in the dedicated alphabetical population.</acceptance_criteria>
  <done>A user-facing read path can retrieve one Unbound contact without reintroducing retired endpoint columns.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Constrain dashboard, Orrery, and favourites at their SQL owners</name>
  <read_first>src/db/dashboard-read.ts; src/db/orrery-read.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/contact-status-read.ts, src/db/contact-status-read.test.ts, src/db/digest-read.ts, src/db/digest-read.test.ts</files>
  <behavior>
    - Bound dashboard and Orrery population excludes Unbound; dashboard search may return both with neutral metadata.
    - Favourite rank persists in storage but Bound-only favourite reads hide dormant ranks.
    - Status and digest projections cannot label an Unbound-but-contacted contact stable or include it in active-orbit summaries, and Bound-only header counts match the list population.
    - An Unbound Orrery sun selection is rendered as self without mutating the saved sun reference; rebinding restores the existing selection.
  </behavior>
  <action>Add `tracking_enabled=1` to the named proactive/favourite SQL branches, keeping search explicitly retrieval-oriented. Update countLiveContacts and related dashboard/header totals to use the same Bound-only predicate as their list population. Extend contact-status-read so Unbound contacts return no cadence status/reason/progress even if contacted; add the Bound predicate to digest-read's active-orbit/overlooked projections. Resolve the Orrery sun policy at the read owner: retain the persisted sun reference, but render self when its target is Unbound and restore it on rebind. Implement D-02 and D-03; do not rewrite favourite rank during unbind. The widget inherits this favourite projection in 18-09 rather than introducing a second filter.</action>
  <verify><automated>npm test -- src/db/dashboard-read.test.ts src/db/orrery-read.test.ts src/db/contact-status-read.test.ts src/db/digest-read.test.ts</automated></verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision tests prove no Unbound row reaches status, digest, rank, header count, or Orrery logic while direct retrieval stays available; an Unbound sun falls back to self non-destructively.</acceptance_criteria>
  <done>Dashboard-derived proactive and favourite populations honor dormant lifecycle state from their query origins.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite query owners to UI | Lifecycle state determines profile, dashboard, Orrery, and favourite treatment. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-09 | Elevation/Tampering | proactive reads | high | mitigate | SQL predicates and owner tests block Unbound from cadence/status routes. |
| T-18-10 | Information disclosure | profile/read projections | medium | mitigate | Return presentation-safe method and lifecycle data only; AI/log boundary tests remain in 18-08. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18-01 owns the approved dependency gate. |
</threat_model>
<verification>Run the named source-adjacent population query suites only and keep consumer changes at their declared SQL owners. Scalar-contract retirement is intentionally staged through later consumers, so the repository-wide test, TypeScript, and color compatibility gate belongs solely to 18-08.</verification>
<success_criteria>Profile, dashboard, Orrery, favourite, status, and digest owners apply the lifecycle policy while explicit relationship retrieval stays intact.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-03-SUMMARY.md` when done</output>
