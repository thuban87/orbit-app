---
phase: 18-contact-data-normalization
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified: [src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/impact-read.ts, src/db/impact-read.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Direct profile/search retrieval retains both states, while dashboard, Orrery, and favourite projections select only Bound contacts."
    - "Unbound has a dedicated alphabetical read and is included in Never Contacted only through its persisted opt-in."
    - "Gravity remains available for Unbound; intensity is unavailable instead of calculating against NULL cadence."
    - "CDN-03 boundary and precision cases are covered at their owning SQL reads."
  artifacts: [src/db/unbound-read.ts, src/db/dashboard-read.ts, src/db/impact-read.ts]
  key_links: ["The lifecycle/read projection seam feeds profile, search, dashboard, and Orrery; notification and widget side effects are isolated in 18-09."]
---
<objective>
Put lifecycle eligibility at every query owner, retaining relationship retrieval while removing Unbound contacts from proactive cadence surfaces.
Purpose: prevent UI, widget, and headless notification paths from disagreeing about Bound state.
Output: Bound-aware profile/dashboard/Orrery predicates, Unbound population read, and owner-level regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/dashboard-read.ts
@src/db/orrery-read.ts
@src/db/impact-read.ts</context>

## Artifacts this phase produces

- SQL-owned lifecycle eligibility across profile, dashboard, Orrery, and favourites projections.
- Direct method/lifecycle profile projections and dedicated Unbound retrieval.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Read one Unbound contact through profile and dedicated population paths</name>
  <read_first>src/db/contact-read.ts:65-208; src/db/dashboard-read.ts:257-343; src/screens/NeverContactedScreen.tsx; 18-PATTERNS.md Read owners and downstream consumers</read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts</files>
  <behavior>
    - Contact header/edit assembly returns ordered methods and lifecycle state without scalar phone/email columns.
    - listUnbound returns non-archived Unbound contacts alphabetically with neutral metadata.
    - A profile may retain Unbound history while cadence-dependent presentation receives nullable/disabled inputs.
  </behavior>
  <action>Extend the existing contact-read aggregate with method projections and `tracking_enabled`; create listUnbound from the dashboard-read query conventions. Implement D-01 and D-02 at the named SQL owners rather than screen filters, preserving both-state direct/profile retrieval and no scalar compatibility read.</action>
  <verify><automated>npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tests prove a real Unbound contact keeps details/history through the profile read and appears only in the dedicated alphabetical population.</acceptance_criteria>
  <done>A user-facing read path can retrieve one Unbound contact without reintroducing retired endpoint columns.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Constrain dashboard, Orrery, and favourites at their SQL owners</name>
  <read_first>src/db/dashboard-read.ts; src/db/orrery-read.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts</files>
  <behavior>
    - Bound dashboard and Orrery population excludes Unbound; dashboard search may return both with neutral metadata.
    - Favourite rank persists in storage but Bound-only favourite reads hide dormant ranks.
  </behavior>
  <action>Add `tracking_enabled=1` to the named proactive/favourite SQL branches, keeping search explicitly retrieval-oriented. Implement D-02 and D-03; do not rewrite favourite rank during unbind. The widget inherits this favourite projection in 18-09 rather than introducing a second filter.</action>
  <verify><automated>npm test -- src/db/dashboard-read.test.ts src/db/orrery-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision tests prove no Unbound row reaches status, rank, or Orrery logic while direct retrieval stays available.</acceptance_criteria>
  <done>Dashboard-derived proactive and favourite populations honor dormant lifecycle state from their query origins.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Preserve gravity while making Unbound cadence intensity unavailable</name>
  <read_first>src/db/impact-read.ts; src/db/impact-read.test.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/impact-read.ts, src/db/impact-read.test.ts</files>
  <behavior>
    - Unbound gravity and relationship history remain readable.
    - Cadence intensity is unavailable for Unbound rather than calculated with nullable cadence.
  </behavior>
  <action>Expose `tracking_enabled` and nullable cadence through the impact read so its caller can retain gravity and history without applying progress arithmetic. Implement D-02 and D-03 with SQL-owned data shape rather than an invented paused/reset status.</action>
  <verify><automated>npm test -- src/db/impact-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Impact tests prove Bound and both Unbound cadence states retain gravity/history but emit no cadence intensity for Unbound.</acceptance_criteria>
  <done>Impact data preserves the relationship while communicating that cadence is inactive.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite query owners to UI | Lifecycle state determines profile, dashboard, Orrery, and favourite treatment. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-09 | Elevation/Tampering | proactive reads | high | mitigate | SQL predicates and owner tests block Unbound from cadence/status routes. |
| T-18-10 | Information disclosure | profile/read projections | medium | mitigate | Return presentation-safe method and lifecycle data only; AI/log boundary tests remain in 18-08. |
| T-18-11 | Tampering | lifecycle/impact projection | medium | mitigate | Bound parameters and nullable-cadence behavior tests prevent progress arithmetic on Unbound data. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18-01 owns the approved dependency gate. |
</threat_model>
<verification>Run each source-adjacent query suite; full test/tsc/colors is the Wave 2 merge gate.</verification>
<success_criteria>Profile, dashboard, Orrery, favourite, and impact owners apply the lifecycle policy while explicit relationship retrieval stays intact.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-03-SUMMARY.md` when done</output>
