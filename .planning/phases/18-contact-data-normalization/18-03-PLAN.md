---
phase: 18-contact-data-normalization
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified: [src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/notification-read.ts, src/db/notification-read.test.ts, src/services/notifications/decay-suppression.ts, src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/widget/widget-data.ts, src/db/app-settings-dao.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Direct profile/search retrieval retains both states, while cadence/status/reminder/favourite/widget projections select only Bound contacts."
    - "Unbound has a dedicated alphabetical read and is included in Never Contacted only through its persisted opt-in."
    - "Gravity remains available for Unbound; intensity is unavailable instead of calculating against NULL cadence."
    - "CDN-03 boundary and precision cases are covered at their owning SQL reads."
  artifacts: [src/db/unbound-read.ts, src/db/dashboard-read.ts, src/db/notification-read.ts]
  key_links: ["Widget favourites inherit the Bound constraint from listDashboard rather than adding a divergent widget filter."]
---
<objective>
Put lifecycle eligibility at every query owner, retaining relationship retrieval while removing Unbound contacts from proactive cadence surfaces.
Purpose: prevent UI, widget, and headless notification paths from disagreeing about Bound state.
Output: Bound-aware read predicates, unbound population read, and owner-level regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/dashboard-read.ts
@src/services/notifications/decay-suppression.ts
@src/services/widget/widget-data.ts</context>

## Artifacts this phase produces

- SQL-owned lifecycle eligibility across proactive projections.
- Direct method/lifecycle profile projections and dedicated Unbound retrieval.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Read one Unbound contact through profile and dedicated population paths</name>
  <read_first>src/db/contact-read.ts:65-208; src/db/dashboard-read.ts:257-343; src/screens/NeverContactedScreen.tsx; 18-PATTERNS.md Read owners and downstream consumers</read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts, src/db/unbound-read.ts, src/db/unbound-read.test.ts</files>
  <behavior>
    - Contact header/edit assembly returns ordered methods and lifecycle state without scalar phone/email columns.
    - listUnbound returns non-archived Unbound contacts alphabetically with neutral metadata.
    - A profile may retain Unbound history while cadence-dependent presentation receives nullable/disabled inputs.
  </behavior>
  <action>Extend the existing contact-read aggregate with method projections and `tracking_enabled`; create listUnbound from the dashboard-read query conventions. Implement D-01 and D-02 at the named SQL owners rather than screen filters, preserving both-state direct/profile retrieval and no scalar compatibility read.</action>
  <verify><automated>npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Tests prove a real Unbound contact keeps details/history through the profile read and appears only in the dedicated alphabetical population.</acceptance_criteria>
  <done>A user-facing read path can retrieve one Unbound contact without reintroducing retired endpoint columns.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Constrain dashboard, Orrery, favourites, and impact at their SQL owners</name>
  <read_first>src/db/dashboard-read.ts; src/db/orrery-read.ts; src/db/impact-read.ts; src/services/widget/widget-data.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/orrery-read.ts, src/db/orrery-read.test.ts, src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/widget/widget-data.ts</files>
  <behavior>
    - Bound dashboard and Orrery population excludes Unbound; dashboard search may return both with neutral metadata.
    - Favourite rank persists in storage but Bound-only favourite and widget reads hide dormant ranks.
    - Impact preserves Unbound gravity/history and represents cadence intensity as unavailable.
  </behavior>
  <action>Add `tracking_enabled=1` to the named proactive/favourite SQL branches, keeping search explicitly retrieval-oriented. Make impact expose lifecycle/null cadence safely rather than applying progress arithmetic. Implement D-02 and D-03; do not rewrite favourite rank during unbind and do not add widget-specific filtering.</action>
  <verify><automated>npm test -- src/db/dashboard-read.test.ts src/db/orrery-read.test.ts src/db/impact-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision tests prove no Unbound row reaches status/rank/Orrery logic while its gravity/history remains readable.</acceptance_criteria>
  <done>All dashboard-derived proactive and favourite populations honor dormant lifecycle state from their query origins.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Gate decay and Never Contacted while retaining birthday behavior</name>
  <read_first>src/db/notification-read.ts; src/services/notifications/decay-suppression.ts; src/db/app-settings-dao.ts; 18-UI-SPEC.md Settings</read_first>
  <files>src/db/notification-read.ts, src/db/notification-read.test.ts, src/services/notifications/decay-suppression.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/db/app-settings-dao.ts</files>
  <behavior>
    - Decay candidates never include Unbound; reminders_off remains independent.
    - Never Contacted defaults to Bound and widens only with persisted include-Unbound setting.
    - Birthday data/banner stays both-state; birthday notification eligibility follows its new default-on Unbound setting.
  </behavior>
  <action>Update the closed decay predicate once, implement the two portable settings in existing allowlists/mapping/validation, and thread them to their exact read owners. Implement D-02 and D-03 while preserving coarse explicit AI behavior and keeping phone/email absent from AI context/logging.</action>
  <verify><automated>npm test -- src/db/notification-read.test.ts src/db/dashboard-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Query tests prove deterministic Bound/Unbound policy for decay, Never Contacted, and birthday notification settings without changing factual birthday reads.</acceptance_criteria>
  <done>Headless and dashboard eligibility match the lifecycle contract instead of relying on client filtering.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite query owners to UI/headless services | Lifecycle state determines whether a contact can enter proactive work. |
| Settings row to notification eligibility | Persisted user policy narrows populations. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-09 | Elevation/Tampering | proactive reads | high | mitigate | SQL predicates and owner tests block Unbound from cadence/status routes. |
| T-18-10 | Information disclosure | AI/log consumers | high | mitigate | Keep normalized methods out of AI context and raw-value logs. |
| T-18-11 | Tampering | settings/queries | medium | mitigate | Bound parameters, allowlisted AppSettingsPatch fields, typed defaults, and deterministic tests. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18-01 owns the approved dependency gate. |
</threat_model>
<verification>Run each source-adjacent query suite; full test/tsc/colors is the Wave 2 merge gate.</verification>
<success_criteria>Every autonomous consumer applies the same Bound policy while explicit relationship retrieval stays intact.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-03-SUMMARY.md` when done</output>
