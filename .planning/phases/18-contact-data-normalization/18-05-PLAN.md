---
phase: 18-contact-data-normalization
plan: 05
type: execute
wave: 3
depends_on: [18-03]
files_modified: [src/screens/UnboundContactsScreen.tsx, src/screens/UnboundContactsScreen.test.tsx, src/screens/HomeScreen.tsx, src/screens/HomeScreen.test.tsx, src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.test.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Unbound contacts are browseable through an always-visible counted dashboard entry and neutral dedicated list."
    - "Settings persist device-region override, Never Contacted opt-in, and default-on Unbound birthday policy; Never Contacted's list and dashboard count are Bound-only by default and include Unbound together only when that persisted opt-in is enabled."
    - "No Phase 18 UI exposes system links, provenance, import, reconciliation, or sync operations."
  artifacts: [src/screens/UnboundContactsScreen.tsx, src/navigation/RootNavigator.tsx, src/screens/SettingsScreen.tsx, src/db/app-settings-dao.ts]
  key_links: ["Home footer count and Unbound route consume listUnbound; the persisted Include-Unbound setting reaches the dashboard read owner so Never Contacted list and count share one eligibility policy; settings owns the effective-region seam consumed by 18-04 aggregate saves."]
  prohibitions:
    - "Phase 18 navigation and settings must not become a system-contact picker, importer, reconciliation workspace, or generic-sync control surface."
---
<objective>Create the visible, non-stigmatizing Unbound browsing and settings surfaces.
Purpose: keep the active orbit focused while preserving explicit access to all relationship data.
Output: dedicated screen/route, dashboard footer, and settings controls per approved UI contract.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/screens/NeverContactedScreen.tsx
@src/screens/HomeScreen.tsx
@src/screens/SettingsScreen.tsx
@src/navigation/RootNavigator.tsx</context>

## Artifacts this phase produces

- Dashboard-to-Unbound population navigation with exact empty/loading/error behavior.
- Durable preference controls for phone region and lifecycle-related lists/notifications.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Open one dedicated Unbound contact from the dashboard</name>
  <read_first>18-UI-SPEC.md Dedicated Unbound population and search; src/screens/NeverContactedScreen.tsx:50-211; src/navigation/types.ts; src/navigation/RootNavigator.tsx</read_first>
  <files>src/screens/UnboundContactsScreen.tsx, src/screens/UnboundContactsScreen.test.tsx, src/screens/HomeScreen.tsx, src/db/unbound-read.test.ts, src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <behavior>
    - Footer always displays `Unbound contacts (N)` after Not yet contacted and routes to UnboundContacts.
    - The screen shows alphabetical neutral rows, exact empty/error copy, count grammar, and taps Profile.
    - Loading cannot flash an empty list and focus retry recovers load failure.
  </behavior>
  <action>Use NeverContactedScreen’s cancelled focus-load/FlatList chrome and listUnbound; add the additive no-param route and Home counted footer. Follow D-02 and D-03 plus every UI-SPEC token, 44px, accessibility, and neutral-row rule. ContactCard remains reserved for Bound status-bearing rows.</action>
  <verify><automated>npm test -- src/screens/UnboundContactsScreen.test.tsx src/db/unbound-read.test.ts</automated></verify>
  <acceptance_criteria>A user can open one Unbound profile through the dashboard and observes the documented empty/loading/error/count variants without a status ring or favourite control.</acceptance_criteria>
  <done>The dedicated population makes Unbound contacts discoverable without treating them as active-orbit candidates.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Persist lifecycle-related settings and apply Never Contacted eligibility</name>
  <read_first>18-UI-SPEC.md Settings; src/db/migrations/009-contact-data-normalization.ts; src/db/app-settings-dao.ts:31-196; src/db/dashboard-read.ts; src/db/dashboard-read.test.ts; src/screens/SettingsScreen.tsx; src/components/DropdownFieldWidget.tsx</read_first>
  <files>src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/dashboard-read.ts, src/db/dashboard-read.test.ts, src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.test.tsx</files>
  <behavior>
    - The exported effective-region policy returns the saved override when present and otherwise the device region; it performs no canonical rewrite of saved method identity.
    - The `Phone number region` modal renders the exact title, searchable region list, and top `Use device region` option; a confirmed choice survives modal/load/error state.
    - Include-Unbound Never Contacted is off by default and birthday-Unbound notifications are on by default.
    - With that default, the Never Contacted list and its dashboard count both exclude Unbound contacts; enabling the persisted option includes the same Unbound no-history contacts in both projections.
    - Modal selection/search preserves confirmed value through load/error state and switches use existing token mapping.
  </behavior>
  <action>Consume the v9-owned app_settings columns from 18-01: extend AppSettings, PortableSettingsSnapshot, AppSettingsPatch, select mapping, write allowlists, and tests; do not create a second settings migration here. Then export the pure `resolveEffectivePhoneRegion(savedOverride, deviceRegion)` policy from this settings owner and add the exact Settings section/row/modal/Switch UI from the contract. In `dashboard-read`, consume the persisted Include-Unbound preference at the Never Contacted read owner, and have the list and its dashboard count derive from the same Bound-versus-opted-in eligibility rule rather than separate UI filters. Implement D-01, D-02, and D-03. Reuse DropdownFieldWidget/Settings shells and expo-localization only as a device fallback. Test the modal selection/search and `Use device region` transition, all three persisted defaults, fallback precedence, and preservation of an existing canonical method identity after a later region choice. Seed Bound and Unbound contacts with no history, then prove the default excludes the Unbound row from both Never Contacted outputs and the persisted opt-in adds it to both. Do not expose canonical values, external IDs, provenance, source refresh, or import controls.</action>
  <verify><automated>npm test -- src/db/app-settings-dao.test.ts src/db/dashboard-read.test.ts src/screens/SettingsScreen.test.tsx</automated></verify>
  <acceptance_criteria>Portable settings round-trip all three fields with correct defaults; settings UI and policy tests prove modal selection, device fallback, and non-retroactive method identity behavior; dashboard-read tests prove the persisted Never Contacted option changes both the list and count from Bound-only to Bound-plus-Unbound together.</acceptance_criteria>
  <done>Users can configure the approved lifecycle preferences without mutating existing method identity, and the Never Contacted list/count consistently honor the saved eligibility preference.</done>
</task>
<task type="auto">
  <name>Task 3: Render neutral Unbound search results and footer edge states</name>
  <read_first>src/screens/HomeScreen.tsx; src/db/dashboard-read.ts; 18-UI-SPEC.md Dedicated Unbound population and search</read_first>
  <files>src/screens/HomeScreen.tsx, src/screens/HomeScreen.test.tsx, src/screens/UnboundContactsScreen.tsx, src/screens/UnboundContactsScreen.test.tsx</files>
  <action>Adapt Home’s search-result presentation so Unbound matches use the neutral label and omit status/favourite treatment, while normal population filters stay query-owned Bound-only. Add Home presentation tests for zero, one, and many Unbound matches plus the descriptive accessibility label; retain width, long-name, retry, and dedicated-list coverage from the UI-SPEC state table. Implement D-03 without adding a general all-contact management or import workflow.</action>
  <verify><automated>npm test -- src/screens/HomeScreen.test.tsx src/screens/UnboundContactsScreen.test.tsx</automated></verify>
  <acceptance_criteria>Search tests prove zero/one/many Unbound results are neutral and accessibility-labeled, without making them active dashboard population or furnishing cadence actions.</acceptance_criteria>
  <done>Retrieval and active-management visual vocabulary remain deliberately distinct.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Read models to React Native UI | Lifecycle state determines visual/action treatment. |
| Settings controls to persisted policy | User choices change future parsing and population eligibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-15 | Tampering | settings writes | medium | mitigate | AppSettingsPatch allowlist, typed defaults, bound DAO writes, and persistence tests. |
| T-18-16 | Information disclosure | Unbound UI | medium | mitigate | Show formatted user-facing methods only and no source/canonical/schema fields. |
| T-18-17 | Elevation | dashboard treatment | high | mitigate | Neutral components and query-owned Bound filters prevent proactive action leaks. |
| T-18-SC | Tampering | package install | high | accept | No package install occurs in this plan. |
</threat_model>
<verification>Run the named source-adjacent settings and list suites only, keeping each change within its declared screen/read owner; record narrow-layout backstops for 18-08 device UAT. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18-08.</verification>
<success_criteria>Unbound is visible, browseable, and configurable without becoming a disguised import or active-management surface.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-05-SUMMARY.md` when done</output>
