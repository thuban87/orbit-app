---
phase: 18-contact-data-normalization
plan: 10
type: execute
wave: 5
depends_on: [18-02, 18-07]
files_modified: [src/db/contact-methods-read.ts, src/backup/export-manifest.test.ts, src/services/backup/share-export.ts, src/services/backup/share-export.test.ts, src/services/backup/streamlined-contact-export.ts, src/services/backup/streamlined-contact-export.test.ts, src/screens/BackupScreen.tsx, src/screens/BackupScreen.test.tsx]
autonomous: false
requirements: [CDN-01, CDN-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Streamlined Export uses only the approved primary formatted phone/email values and does not expose canonical, secondary, external-link, or provenance data."
    - "The existing full export/backup remains the lossless recovery artifact for the complete normalized graph."
  artifacts: [src/services/backup/streamlined-contact-export.ts, src/services/backup/share-export.ts, src/screens/BackupScreen.tsx]
  key_links: ["The Backup screen invokes the narrowed human share builder, while the existing manifest/export path retains every normalized method and provenance row."]
---
<objective>
Add the explicitly approved human-oriented Streamlined Export beside, not in place of, lossless backup.
Purpose: let users share concise contact details without weakening the recovery artifact or exposing internal identity/provenance fields.
Output: formatted-primary export read/builder, native share integration, screen affordance, and tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/dossier/18-contact-data-normalization.md Cluster R
@src/db/contact-methods-read.ts
@src/services/backup/share-export.ts
@src/screens/BackupScreen.tsx
@.planning/phases/18-contact-data-normalization/18-07-SUMMARY.md</context>
<tasks>
<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve the Streamlined Export PII egress boundary</name>
  <read_first>docs/dossier/18-contact-data-normalization.md Cluster R; src/services/backup/share-export.ts; src/screens/BackupScreen.tsx</read_first>
  <action>Ask the owner to approve this intentional local-share egress before implementation: Streamlined Export is a separately labeled, user-initiated artifact that may contain only name, category, and formatted actionable primary phone/email values. It excludes raw/canonical values, extensions, secondary methods, source links, provenance, tombstones, and recovery semantics. Record approval or a decision to defer the entire Streamlined Export slice in the summary. This checkpoint owns the review-identified product decision; it does not add share recipients, provider routing, or automatic export.</action>
  <decision>Approve the bounded human-oriented PII share payload, or defer Streamlined Export from Phase 18.</decision>
  <context>This is a deliberate egress path beyond the phase requirement list, even though the dossier describes it as an approved product direction.</context>
  <options><option id="approve"><name>Approve bounded export</name><pros>Delivers the dossier’s distinct streamlined export alongside lossless recovery.</pros><cons>Allows the approved minimal contact fields to leave through the user’s chosen native share target.</cons></option><option id="defer"><name>Defer export</name><pros>Removes the new share egress from this phase.</pros><cons>Leaves only lossless backup/export behavior until a future scoped phase.</cons></option></options>
  <resume-signal>Reply `approve` or `defer`; a defer marks Tasks 2 and 3 intentionally out of scope and preserves existing backup behavior.</resume-signal>
  <verify><automated>node -e "process.exit(0)"</automated></verify>
  <acceptance_criteria>The owner has recorded an explicit approve/defer decision before any new native share egress is written.</acceptance_criteria>
</task>
<task type="tracer" tdd="true">
  <name>Task 2: Build and share one formatted-primary streamlined contact record</name>
  <read_first>src/db/contact-methods-read.ts; src/services/backup/share-export.ts; src/backup/export-manifest.ts; docs/dossier/18-contact-data-normalization.md Cluster R</read_first>
  <files>src/db/contact-methods-read.ts, src/backup/export-manifest.test.ts, src/services/backup/share-export.ts, src/services/backup/share-export.test.ts, src/services/backup/streamlined-contact-export.ts, src/services/backup/streamlined-contact-export.test.ts</files>
  <behavior>
    - A streamlined row contains only name/category and formatted primary phone/email values approved for human sharing.
    - Missing, non-actionable, secondary, canonical, raw, extension, external-link, and provenance values are absent from this artifact.
    - The full manifest exporter remains unchanged as the lossless graph used for recovery.
  </behavior>
  <action>Add a narrowed formatted-primary contact read and a separate `Streamlined Export` builder that uses the existing native file/share adapter. It may include one approved primary formatted phone and one approved primary formatted email per contact; do not source it from scalar compatibility fields or the lossless manifest’s internal rows. Add unit tests that seed multiple, invalid, shared, and extension-bearing methods and prove the share payload is concise while the full manifest still contains all normalized data. Implement D-01 and D-04 without adding source refresh, import, or a second backup format.</action>
  <verify><automated>npm test -- src/services/backup/share-export.test.ts src/services/backup/streamlined-contact-export.test.ts src/backup/export-manifest.test.ts</automated></verify>
  <acceptance_criteria>Tests prove the human share payload contains only formatted actionable primaries and that lossless export continues to preserve secondary/invalid/provenance rows.</acceptance_criteria>
  <done>One normalized contact can be shared in a concise human-readable form without changing recovery fidelity.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Surface Streamlined Export without relabeling lossless backup</name>
  <read_first>src/screens/BackupScreen.tsx; 18-UI-SPEC.md Copywriting Contract; docs/dossier/18-contact-data-normalization.md Cluster R</read_first>
  <files>src/screens/BackupScreen.tsx, src/screens/BackupScreen.test.tsx, src/services/backup/streamlined-contact-export.test.ts</files>
  <behavior>
    - The screen labels the new action `Streamlined Export` and keeps existing full backup/export wording and recovery path distinct.
    - Share failure uses the established retry-safe error posture and does not affect backup health or full export state.
  </behavior>
  <action>Add the `Streamlined Export` action to the existing Backup screen using its loading, accessibility, themed-row, and generic error conventions. Route only this action to the narrowed builder from Task 1; preserve the existing full manual export/backup call and labels as the recovery artifact. Implement D-01 and D-04 without displaying canonical methods, source IDs, or provenance.</action>
  <verify><automated>npm test -- src/screens/BackupScreen.test.tsx src/services/backup/streamlined-contact-export.test.ts</automated></verify>
  <acceptance_criteria>Screen tests prove the action invokes the streamlined builder, presents its label/accessibility state, retains retry-safe failures, and leaves the lossless backup action unchanged.</acceptance_criteria>
  <done>Users can deliberately choose a human-oriented export without confusing it with full recovery backup.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| normalized contact read to native share file | Local contact data leaves the app through an intentional human-share action. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-31 | Information disclosure | Streamlined Export | high | mitigate | Narrow formatted-primary read, payload allowlist tests, and no canonical/provenance fields. |
| T-18-32 | Tampering | recovery/export semantics | medium | mitigate | Separate builder and screen action preserve the full manifest/backup pathway. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan. |
</threat_model>
<verification>Run the named streamlined-share, full-manifest, and Backup-screen suites only. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18-08.</verification>
<success_criteria>Human sharing is concise and safe, while recovery backup remains lossless and unmistakably separate.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-10-SUMMARY.md` when done</output>
