---
phase: 18-contact-data-normalization
plan: 04
type: execute
wave: 4
depends_on: [18-02, 18-03, 18-05]
files_modified: [src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx]
autonomous: true
requirements: [CDN-01, CDN-02]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Create/edit forms own reversible method drafts until Save, permit zero methods for Unbound, and enforce Bound cadence before Save."
    - "Method cards obey stable ordering, primary selection/promotion, labels/extensions, invalid helper copy, and 44px accessible controls."
    - "Canonical duplicates collapse on save without warning about other contacts."
  artifacts: [src/components/ContactMethodsEditor.tsx, src/screens/create-contact-logic.ts, src/screens/edit-contact-logic.ts]
  key_links: ["After 18-05 resolves saved region override or device fallback, form logic passes that effective region to the aggregate DAO and renders its typed same-contact collision result without UI-side normalization."]
---
<objective>Deliver the create/edit slice for real normalized methods and lifecycle choice.
Purpose: users can maintain ordered contact methods and choose Orbit participation without losing draft data.
Output: controlled method editor, create/edit shaping, and screens matching the UI contract.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/components/LinksEditor.tsx
@src/screens/CreateContactScreen.tsx
@src/screens/EditContactScreen.tsx
@src/screens/create-contact-logic.ts
@src/db/app-settings-dao.ts
@src/db/contact-methods-dao.ts</context>

## Artifacts this phase produces

- Reusable controlled Phone numbers and Email addresses editor.
- Create/edit lifecycle controls and atomic method-draft save adapters.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Save one created Unbound contact with an editable phone draft</name>
  <read_first>18-UI-SPEC.md Create contact and Edit contact; src/components/LinksEditor.tsx:42-205; src/screens/create-contact-logic.ts; src/db/contacts-dao.ts</read_first>
  <files>src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/CreateContactScreen.tsx</files>
  <behavior>
    - Bound is default and needs positive cadence; never-assigned Unbound may save zero methods/cadence.
    - The initial empty create-phone row disappears when blank, while a nonblank invalid value remains a visible stored draft.
    - One phone group uses controlled stable-UID rows, resolves saved override else device region through the 18-05 policy seam, and yields the aggregate DAO draft contract.
  </behavior>
  <action>Build the controlled ContactMethodsEditor from LinksEditor’s draft/card model, theme and accessibility conventions. Wire one real Create path through lifecycle choice, method draft shaping, the 18-05 region-policy seam (saved override else device region), and createContactFull. Pass the effective region as aggregate-write input; do not invoke the normalizer in UI code. Implement D-01 and D-02 and the exact UI-SPEC copy/scan order; use no raw colors, DB calls in the component, parser details, or reorder affordance.</action>
  <verify><automated>npm test -- src/components/ContactMethodsEditor.test.tsx src/screens/create-contact-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>A user can save a named Unbound contact with a phone draft end to end; form tests prove cadence gating, blank-row disposal, saved-region override, device fallback, and that later region choices do not rewrite an existing method identity.</acceptance_criteria>
  <done>The first UI-to-DAO method/lifecycle path is usable and production-quality.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expand editor groups, labels, primaries, and invalid-state feedback</name>
  <read_first>18-UI-SPEC.md Edit contact and Copywriting Contract; src/components/LinksEditor.tsx; src/logic/contact-method-normalization.ts</read_first>
  <files>src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/db/contact-methods-dao.test.ts</files>
  <behavior>
    - Phone/email groups expose labels/custom labels, phone extension, stored order, Primary controls, and draft-only Remove.
    - Removing a selected primary selects the next draft row; after Save, the typed aggregate-save `canonicalDuplicate` result collapses a same-contact duplicate and displays exactly `This matches an existing {phone number/email address}; only one will be kept.`.
    - Invalid methods remain editable with exact helper copy and actionability stays DAO-derived.
  </behavior>
  <action>Expand the tracer editor to both type groups and implement draft reducer/shaping tests for ordering, primary selection, extension, label, and duplicate feedback. Consume only the typed `canonicalDuplicate` result returned by aggregate save: collapse the local draft to the returned surviving row and render the exact UI-SPEC helper after a same-contact collision. Follow D-01 exactly: a canonical match across a different contact receives no warning, helper, or client-side lookup. Keep removal reversible before Save and omit endpoint action/router/history features.</action>
  <verify><automated>npm test -- src/components/ContactMethodsEditor.test.tsx src/screens/edit-contact-logic.test.ts src/db/contact-methods-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Controlled-editor and aggregate-save tests cover empty, multiple, primary promotion, invalid, extension, 44px/a11y state behavior, exact same-contact helper/collapse on Save, and absence of cross-contact feedback.</acceptance_criteria>
  <done>Both type groups satisfy the ordered first-class method interaction contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Wire full edit save and recoverable drafts</name>
  <read_first>src/screens/EditContactScreen.tsx; src/screens/edit-contact-logic.ts; src/db/contact-read.ts; 18-UI-SPEC.md Edit contact</read_first>
  <files>src/screens/EditContactScreen.tsx, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/db/contacts-dao.test.ts</files>
  <behavior>
    - Edit assembles seeded ordered methods and lifecycle from the new read boundary.
    - Back discards local edits; a save error preserves every draft for retry.
    - Save uses one aggregate write, receives the effective-region context and typed collision result, and never references legacy scalar fields.
  </behavior>
  <action>Replace EditContact’s scalar field state with seeded method/lifecycle drafts and route its Save through the aggregate DAO with the region-policy seam and returned typed collision result. Preserve existing non-optimistic loading/error posture and exact UI-SPEC error copy. Implement D-01 and D-02 without adding a source/provenance settings surface.</action>
  <verify><automated>npm test -- src/screens/edit-contact-logic.test.ts src/db/contacts-dao.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Logic tests prove saved drafts round-trip through ordered reads, typed same-contact collision feedback preserves the surviving draft row, cross-contact matches remain silent, and failed saves retain the same draft state.</acceptance_criteria>
  <done>Users can edit existing contacts without scalar endpoint storage or destructive draft loss.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| React inputs to aggregate DAO | PII and lifecycle choices become durable state. |
| UI feedback to contact actions | Non-actionable values require safe presentation without use as handoff input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-12 | Tampering | form save | high | mitigate | Typed shaping delegates normalization/validation to DAO and uses atomic aggregate write. |
| T-18-13 | Information disclosure | UI/logging | medium | mitigate | Display formatted values only; no canonical/parser data in UI diagnostics or Logger. |
| T-18-14 | Elevation | invalid methods | high | mitigate | UI uses DAO-provided actionability and introduces no launcher/router. |
| T-18-SC | Tampering | dependency graph | high | accept | No package change; parser provenance was gated in 18-01. |
</threat_model>
<verification>Run component/logic tests, TypeScript, colors; device layout cases are carried to 18-08.</verification>
<success_criteria>Create/edit ownership of data is atomic, accessible, and non-destructive.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-04-SUMMARY.md` when done</output>
