---
phase: 18-contact-data-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, src/logic/contact-method-normalization.ts, src/logic/contact-method-normalization.test.ts, src/db/migrations/009-contact-data-normalization.ts, src/db/migrations/009-contact-data-normalization.test.ts, src/db/database.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts]
autonomous: false
requirements: [CDN-01, CDN-02, CDN-04]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A v8 database migrates atomically to v9: legacy contacts become Bound, legacy scalar methods become first-class primary rows, and the old scalar columns no longer exist."
    - "The normalization boundary stores nonblank malformed input as non-actionable and permits shared canonical values across contacts."
    - "The migration schema makes local Orbit identity independent of system-contact identifiers and retains stale source evidence."
    - "The v9 migration owns durable defaults for phone-region override, Never Contacted Unbound eligibility, and Unbound birthday-notification eligibility."
    - "CDN-01 manual edge review remains required for installed parser provenance and Android migration behavior."
  artifacts: [src/db/migrations/009-contact-data-normalization.ts, src/logic/contact-method-normalization.ts, src/db/migrations/009-contact-data-normalization.test.ts]
  key_links:
    - "Migration 009 invokes the same pure normalizer that later DAO writes use."
    - "database.ts registers migration 009 so the real v8-to-v9 chain executes."
    - "The contact-parent rebuild preserves every child and the Orrery sun reference before foreign-key validation succeeds."
    - "Migration 009 owns the positive-or-null cadence checks and the one-way assigned-cadence trigger before lifecycle DAOs depend on them."
  prohibitions:
    - "No Phase 18 artifact may make a system-contact identifier authoritative over Orbit identity; source evidence remains local provenance."
---

<objective>
## Phase Goal

Normalize phone/email into first-class, sync-ready contact methods and establish Bound/Unbound lifecycle without losing relationship data or making system contacts authoritative.

Purpose: establish the durable, migration-safe data path that every later Phase 18 slice consumes.
Output: parser seam, migration 009, normalized child schema, and real-chain migration tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-contact-data-normalization/18-CONTEXT.md
@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@docs/dossier/18-contact-data-normalization.md
@src/db/migrations/001-initial.ts
@src/db/migrations/008-restore-photo-journal.ts
@src/db/migrations/runner.ts
</context>

## Artifacts this phase produces

- A v9 contacts-table rebuild, mergeable method/link/provenance entities, and migration proofs.
- One pure phone/email normalizer used at migration and DAO boundaries.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Ratify the v9 storage and phone-actionability contracts</name>
  <read_first>18-CONTEXT.md Locked Invariants; 18-RESEARCH.md Patterns 1, 3, and 5; docs/dossier/18-contact-data-normalization.md Clusters C, I, O, and P</read_first>
  <action>Present two decisions for confirmation before any normalizer or migration implementation. First, the v9 contract: rebuilt contacts table retires scalar phone/email, `tracking_enabled` is the Bound flag, `interval_days` is nullable only before first assignment, child methods/links/provenance carry UIDs and tombstone types, a partial active-link unique index enforces `(provider, external_contact_id)`, and migration 009 owns three durable cadence rules: `interval_days IS NULL OR interval_days > 0`, Bound requires a non-null cadence, and an update trigger rejects clearing a previously positive cadence. Second, choose the parser threshold that makes a parsed phone actionable: `isPossible()`, `isValid()`, or a named alternative with its metadata policy. Record the selected threshold and representative-country plus extension test cases in the SUMMARY before continuing. This implements D-01, D-02, and D-04 and addresses cycle-2 review H3; it leaves picker/import/reconciliation UI outside Phase 18.</action>
  <decision>Approve the v9 on-disk contract and select the actionability threshold for normalized phones.</decision>
  <context>Changing shipped table shape, child UID/tombstone types, cadence constraints, or the user-visible actionability threshold later requires a forward migration or a behavior-policy change.</context>
  <options><option id="possible"><name>Approve v9 + use isPossible()</name><pros>Accepts realistically dialable numbers with the library's default metadata.</pros><cons>May accept numbers that are possible but not fully valid.</cons></option><option id="valid"><name>Approve v9 + use isValid()</name><pros>Uses stricter metadata validation before an action becomes available.</pros><cons>May classify legitimate numbers unavailable when metadata is incomplete.</cons></option><option id="alternative"><name>Approve v9 + specify another threshold</name><pros>Allows an explicit product policy and test matrix.</pros><cons>Requires naming the parser metadata and representative cases now.</cons></option><option id="revise-storage"><name>Revise storage before approval</name><pros>Changes the durable contract before user data is transformed.</pros><cons>Delays the migration tracer.</cons></option></options>
  <resume-signal>Reply with the storage approval and `possible`, `valid`, or a named alternative plus representative countries.</resume-signal>
  <verify><automated>node -e "process.exit(0)"</automated></verify>
  <acceptance_criteria>A human has explicitly approved the v9 storage contract and the parser threshold before normalizer or migration code is written.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify libphonenumber-js release provenance</name>
  <read_first>18-RESEARCH.md Package Legitimacy Audit and Environment Availability; package.json</read_first>
  <files>package.json</files>
  <action>Ask the human to verify `libphonenumber-js@1.13.11` on npmjs.com against its GitLab project and release metadata before installation. This trust gate supports D-01's canonical phone equality and is mandatory because research marked the current release SUS solely for recency.</action>
  <what-built>No package has been installed; this gate protects the approved parser dependency.</what-built>
  <how-to-verify>Open npmjs.com/package/libphonenumber-js/v/1.13.11, confirm the package owner/repository and version are expected, then approve installation.</how-to-verify>
  <resume-signal>Reply approved to permit the exact package installation, or report the concern.</resume-signal>
  <verify><automated>node -e "const p=require('./package.json'); if (p.dependencies?.['libphonenumber-js']) throw new Error('libphonenumber-js is already installed; record its provenance approval before continuing')"</automated></verify>
  <acceptance_criteria>The human records approval before package.json or package-lock.json receives libphonenumber-js@1.13.11.</acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Migrate one legacy contact end to end into normalized, Bound data</name>
  <reversibility rating="one-way">Migration 009 transforms shipped SQLite data and changing its child/constraint contract later requires a forward migration.</reversibility>
  <read_first>src/db/migrations/001-initial.ts; src/db/migrations/003-orrery-settings.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/008-restore-photo-journal.ts; src/db/database.ts; src/db/app-settings-dao.ts; src/db/contact-links-dao.test.ts; 18-PATTERNS.md Database migration and Normalization boundary</read_first>
  <files>package.json, package-lock.json, src/logic/contact-method-normalization.ts, src/logic/contact-method-normalization.test.ts, src/db/migrations/009-contact-data-normalization.ts, src/db/migrations/009-contact-data-normalization.test.ts, src/db/database.ts, src/db/reserved-columns.ts, src/db/reserved-columns.test.ts</files>
  <behavior>
    - Representative national and international phone inputs plus extensions follow the human-selected actionability threshold, become E.164 canonical data when actionable, and retain a separate extension and derived display; a valid email uses trimmed ASCII case-fold canonical equality.
    - A nonblank unparsable method remains stored with no canonical/actionable value.
    - Running the real migration chain on v8 copies legacy phone/email once as per-type primary rows, marks contacts Bound, preserves every retained relationship field, and retires the scalar columns.
    - A source link made stale remains a provenance row and cannot cascade-delete its Orbit contact or method.
    - National-format legacy phone values migrate raw-only with no assumed country: only explicitly international values may receive a canonical identity during migration; legacy national values remain storable/non-actionable until a later user-region-aware edit.
    - A v8 fixture with zero, negative, or non-integer cadence rejects atomically and leaves user_version at v8; it never manufactures a cadence.
    - A v8 fixture containing every contacts FK child, including custom_field_values rows with their IDs and values, and app_settings.sun_contact_id retains all child rows, IDs, values, and sun reference after the parent-table rebuild, and PRAGMA foreign_key_check returns no rows.
    - The v9 app_settings row contains phone_region_override NULL, include_unbound_never_contacted 0, and birthday_unbound_enabled 1.
    - A v8 fixture seeds every pre-v9 app_settings column with a distinctive non-default value; after v9, every pre-existing value is equal and only the three new v9 settings use their declared defaults.
    - Schema assertions compare PRAGMA table_info and sqlite_master table/index SQL before and after migration for app_settings and every rebuilt child, proving each retained column, NOT NULL/default, index, and UNIQUE constraint survives.
    - Direct SQL rejects two primary methods of the same method_type for one contact through the v9 partial unique index while allowing one primary per different method type.
    - Direct SQL rejects Bound contacts with NULL cadence, rejects zero or negative cadence, and rolls back an attempt to clear a previously positive cadence.
  </behavior>
  <action>After Tasks 1 and 2 approval, install exactly `libphonenumber-js@1.13.11` and `expo-localization` with Expo-compatible install tooling. Add the pure normalization module and RED-to-GREEN tests; it owns trimming, conservative email equality, parser-backed phone canonicalization/extension/display, and the threshold selected in Task 1 so migration/UI/DAO code cannot fork semantics. Migration-time region policy is deliberately raw-only: do not assume a default country from unavailable MigrationDeps; normalize only explicitly international legacy phones and retain national/ambiguous values as stored non-actionable rows for a later edit using the persisted effective region. Implement migration 009 as the runner-owned transaction. Evaluate `PRAGMA legacy_alter_table = ON` around the parent rename as an optional route to avoid retargeting child REFERENCES clauses and reduce the rebuild blast radius; whichever route is selected, retain the complete preservation proof below. With foreign keys already enabled, rename the old contacts parent, create the new contacts table, copy retained parent IDs, then either prove the legacy-alter route preserved each child FK or rebuild every retargeted child table (contact_links, interactions, events, fuel, custom_field_values, and app_settings' sun-contact reference) so replacements point at the new contacts IDs before dropping the renamed parent. Preserve every custom_field_values ID, UID, contact_id, field_def_id, value, and timestamp as well as all other child rows and SQLite IDs; recreate indexes/triggers, then create UID-bearing ordered method/external-link/provenance tables, active-link partial uniqueness, and a partial unique primary-method index on `(contact_id, method_type) WHERE is_primary = 1`. Add direct-SQL migration tests for both a duplicate same-type primary rejection and a distinct-type primary success. In the rebuilt contacts DDL add `CHECK (interval_days IS NULL OR interval_days > 0)` and `CHECK (tracking_enabled = 0 OR interval_days IS NOT NULL)`; create a `BEFORE UPDATE OF interval_days` trigger that aborts when a non-null old cadence would become NULL. Add direct-SQL migration tests for each CHECK plus the trigger rollback, including an existing Bound fixture. Add `tracking_enabled INTEGER NOT NULL DEFAULT 1` to contacts and add `phone_region_override TEXT`, `include_unbound_never_contacted INTEGER NOT NULL DEFAULT 0`, and `birthday_unbound_enabled INTEGER NOT NULL DEFAULT 1` to app_settings in this migration; update src/db/reserved-columns.ts and src/db/reserved-columns.test.ts for tracking_enabled. Copy scalar values into primary methods, reject malformed legacy cadence (zero, negative, or non-integer) before any committed mutation, use bound SQL parameters, run row-count/copy assertions plus `PRAGMA foreign_key_check`, and register v9. Seed every pre-v9 app_settings column with a distinctive non-default value and assert full pre-existing-row equality after v9. For app_settings and each rebuilt child, compare pre/post `PRAGMA table_info` metadata and sqlite_master table/index SQL, asserting retained columns, defaults, NOT NULL, indexes, and UNIQUE constraints are complete. Prove the safe rebuild with a v8 fixture containing every cascaded child, populated custom_field_values rows, and an Orrery sun reference. Implement D-01, D-02, and D-04 and address cycle-2 review H3 and Cycle 3 C3-H2/C3-M6 without adding import, refresh, or reconciliation behavior.</action>
  <verify><automated>npm test -- src/logic/contact-method-normalization.test.ts src/db/migrations/009-contact-data-normalization.test.ts</automated></verify>
  <acceptance_criteria>Real v8 fixtures prove the FK-safe atomic parent/child rebuild, scalar retirement, primary rows, Bound migration, raw-only legacy regional policy, invalid-value retention, canonical precision, explicit settings defaults, preserved child rows/Orrery sun reference, `foreign_key_check` integrity, and rollback on malformed cadence. The fixture proves complete app_settings data preservation across every pre-v9 column and schema preservation through table_info/sqlite_master comparisons for app_settings and every rebuilt child, including indexes and UNIQUE constraints. Migration tests directly prove the positive-or-null CHECK, Bound-with-cadence CHECK, old-positive-to-NULL trigger rollback, and the partial same-contact/type primary uniqueness constraint.</acceptance_criteria>
  <done>One existing user’s contact data reaches the new durable model through the actual migration runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| v8 SQLite data to migration 009 | Existing relationship data and PII are transformed into durable v9 rows. |
| npm registry to local dependency graph | A recently published parser package enters the application build. |
| system-contact evidence to Orbit rows | Opaque source identifiers are retained as non-authoritative provenance. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-01 | Tampering/DoS | migration 009 | high | mitigate | Transaction-owned rebuild, `?` binding, FK/copy proofs, v8 fixture rollback tests, and backup-first device UAT. |
| T-18-02 | Information disclosure | normalizer/migration logs | high | mitigate | Keep values out of Logger/AI context and assert test failures without printing raw PII. |
| T-18-03 | Tampering | external links/provenance | medium | mitigate | Partial active-link uniqueness; no cascade from evidence to Orbit-owned method/contact. |
| T-18-04 | Elevation | actionable phone handoff | high | mitigate | Store actionability separately; later callers may consume only approved actionable primary reads. |
| T-18-SC | Tampering | npm install | high | mitigate | Blocking human provenance checkpoint precedes exact libphonenumber-js installation. |
</threat_model>

<verification>Run the named normalizer and migration suite only; retain the v8 fixture for the phase-end Android upgrade review. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18-08 after every consumer migration.</verification>
<success_criteria>The storage contract is durable, test-proven, source-non-authoritative, and ready for transactional writes.</success_criteria>
<output>Create `.planning/phases/18-contact-data-normalization/18-01-SUMMARY.md` when done</output>
