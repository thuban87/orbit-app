---
phase: 01-project-scaffold-portable-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.config.ts
  - tsconfig.json
  - biome.json
  - babel.config.js
  - vitest.config.ts
  - .gitignore
  - scripts/check-colors.sh
  - src/db/.gitkeep
  - src/components/.gitkeep
  - src/screens/.gitkeep
  - src/theme/.gitkeep
  - src/stores/.gitkeep
  - src/services/.gitkeep
  - src/schemas/.gitkeep
  - src/utils/.gitkeep
autonomous: true
requirements: [FND-06]
user_setup: []

must_haves:
  truths:
    - "A flat (non-monorepo) Expo SDK 57 app scaffolds into the existing repo without clobbering HANDOFF.md, docs/, .planning/, .claude/, or .git"
    - "tsc --noEmit, biome check, and vitest run all execute with exit code 0 on the fresh scaffold"
    - "The app is portrait-locked and ALL eight CLAUDE.md folders (src/theme, src/stores, src/services, src/schemas, src/utils, src/db, src/components, src/screens) exist as tracked dirs at the end of THIS plan — FND-06's full layout is satisfied here, not deferred to later plans"
    - "A shared, broad no-hardcoded-colour gate (npm run check:colors) exists in package.json for every later phase to reuse (catches 3-8 digit hex, rgb()/hsl(), and quoted named colours outside src/theme)"
  artifacts:
    - path: "package.json"
      provides: "Pinned Expo SDK 57 stack (expo, expo-sqlite, zustand, async-storage, biome, vitest, tsx) + shared check:colors script"
      contains: "expo"
    - path: "app.config.ts"
      provides: "Portrait lock + expo-sqlite plugin + placeholder Android package id; loads via tsx/cjs and merges the template app.json"
      contains: "orientation"
    - path: "scripts/check-colors.sh"
      provides: "Shared broad colour-literal gate reused by 01-03 and every later phase"
      contains: "theme"
    - path: "biome.json"
      provides: "Lint/format config (schema 2.5.8, monorepo bits dropped, !android) + overrides for src/utils/logger.ts (static-class rules) and src/**/*.test.ts (noNonNullAssertion)"
      contains: "biomejs.dev/schemas/2.5.8"
    - path: "tsconfig.json"
      provides: "strict TS + @/* -> ./src/* path alias"
      contains: "@/*"
    - path: "vitest.config.ts"
      provides: "Vitest runner for pure-logic tests (src/**/*.test.ts)"
      contains: "src/**/*.test.ts"
  key_links:
    - from: "tsconfig.json"
      to: "src/*"
      via: "paths alias @/* -> ./src/*"
      pattern: "@/\\*"
    - from: "app.config.ts"
      to: "expo-sqlite"
      via: "plugins array pre-registers the Phase 2 native dep so first prebuild covers it"
      pattern: "expo-sqlite"
---

<!-- planner-discipline-allow: newArchEnabled -->
<!-- planner-discipline-allow: withSentry -->
<!-- planner-discipline-allow: @quest-board -->
<!-- planner-discipline-allow: obsidian -->

<objective>
Scaffold a flat (single-app, NOT monorepo) Expo SDK 57 project into the existing `orbit-app`
repo and configure the toolchain: portrait lock, Biome, strict TypeScript with the `@/*` alias,
Vitest, and the CLAUDE.md `src/` folder layout. This is the foundation every other Phase 1 plan
builds on — it produces no user-facing behaviour itself, but after it the repo typechecks, lints,
and runs tests cleanly.

Purpose: FND-06 (Biome + portrait-lock + folder layout configured) and the scaffold half of the
Walking Skeleton. Establishes the pinned stack and green-baseline gates the ports and theme plans
depend on.
Output: `package.json`, `app.config.ts`, `tsconfig.json`, `biome.json`, `babel.config.js`,
`vitest.config.ts`, an extended `.gitignore`, and the empty-but-tracked `src/` folder skeleton.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-portable-code/01-RESEARCH.md
@.planning/phases/01-project-scaffold-portable-code/01-PATTERNS.md
@CLAUDE.md
@HANDOFF.md

Analog files (READ IN PLACE — do not copy monorepo machinery):
@~/projects/quest-board-app/biome.json
@~/projects/quest-board-app/apps/mobile/app.config.ts
@~/projects/quest-board-app/apps/mobile/tsconfig.json
@~/projects/quest-board-app/apps/mobile/babel.config.js

Review the code, not the diff: open the actual quest-board analog files listed above before
writing config. quest-board is an npm-workspaces MONOREPO; Orbit is a FLAT single app. Extract
the config *patterns*, never the monorepo build hacks (patch-build-gradle.js, Metro watchFolders,
packages/* aliases, expo-cli-wrapper, withSentry). Those produce a broken-but-configured build here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold a flat Expo SDK 57 app into the existing repo without clobbering it</name>
  <files>package.json, babel.config.js</files>
  <read_first>
    - `.gitignore` (current repo root — will be extended, must NOT be overwritten)
    - `.planning/phases/01-project-scaffold-portable-code/01-RESEARCH.md` (Pitfall 6: non-empty dir; Installation block; Don't Hand-Roll table)
    - `~/projects/quest-board-app/apps/mobile/babel.config.js` (analog — preset only, no reanimated)
  </read_first>
  <action>
    The repo root already contains `CLAUDE.md`, `HANDOFF.md`, `docs/`, `.planning/`, `.claude/`,
    `.git`, and `.gitignore` — `create-expo-app` refuses a non-empty target, so scaffold into a
    temp dir and move the generated files in. Steps: (1) run
    `npx create-expo-app@latest /tmp/orbit-scaffold --template blank-typescript` (blank-typescript,
    NOT the default router template — a single home shell needs no expo-router; this decision is
    recorded in SKELETON.md). (2) Copy the generated files into the repo root, preserving every
    existing file — copy `package.json`, `App.tsx`, `babel.config.js`, `metro.config.js` (if
    present), `index.ts`/`AppEntry`, `assets/`, `expo-env.d.ts`; NEVER overwrite `CLAUDE.md`,
    `HANDOFF.md`, `docs/`, `.planning/`, `.claude/`, `.git`, or the existing `.gitignore`. (3)
    Verify the template resolved Expo SDK 57 — run `node -e "console.log(require('./package.json').dependencies.expo)"`;
    if it is not `^57`/`57.x`, run `npx expo install expo@^57` then `npx expo install --fix` to
    re-pin the WHOLE stack (RN/React/etc.) to the SDK-57 matrix — `expo install expo@^57` alone
    pins only `expo` and leaves RN/React wherever the template left them (review finding). (4)
    Install the pinned native stack via `npx expo install expo-sqlite expo-status-bar react-native-safe-area-context @react-native-async-storage/async-storage`
    (expo install resolves the SDK-57-correct RN 0.86 pin — NEVER bare `npm install` a native pkg,
    it pulls RN 0.87 and breaks the pin). (5) Install pure-JS + dev deps:
    `npm install zustand` then `npm install -D @biomejs/biome@2.5.8 vitest tsx` (tsx is required so
    Expo can evaluate the TypeScript `app.config.ts` at prebuild — see Task 2). (6) Set `babel.config.js`
    to `presets: ["babel-preset-expo"]` only — do NOT add the reanimated plugin (that is a Phase 13
    dep). (7) Extend `.gitignore` by APPENDING Expo/native ignores (`/android`, `/ios`, `*.jks`,
    `*.keystore`, `.expo/`, `dist/`, `web-build/`) — append only, keep the existing graphify and
    settings.local.json entries intact.
  </action>
  <verify>
    <automated>test -f package.json && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; for (const k of ['expo','expo-sqlite','zustand','@biomejs/biome','vitest','@react-native-async-storage/async-storage']) { if(!d[k]) { console.error('MISSING '+k); process.exit(1);} } console.log('deps ok', d.expo)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f HANDOFF.md && test -d .planning && test -d docs && test -d .claude` all succeed (nothing clobbered)
    - `package.json` `dependencies.expo` starts with `57` or `^57`
    - `package.json` contains `expo-sqlite`, `zustand`, `@react-native-async-storage/async-storage`, and devDeps `@biomejs/biome` (2.5.8), `vitest`, and `tsx`
    - `babel.config.js` references `babel-preset-expo` and contains NO `react-native-reanimated/plugin`
    - `grep -c 'node_modules' .gitignore` is >= 1 AND `.gitignore` still contains the graphify `graph.html` entry (append-only preserved)
    - `git status --porcelain HANDOFF.md .planning docs` prints nothing (existing tracked docs untouched)
  </acceptance_criteria>
  <done>Expo SDK 57 flat app scaffolded in place, pinned stack installed, existing repo docs preserved, .gitignore extended additively.</done>
</task>

<task type="auto">
  <name>Task 2: Configure portrait lock, TypeScript alias, and Biome</name>
  <files>app.config.ts, tsconfig.json, biome.json</files>
  <read_first>
    - `~/projects/quest-board-app/apps/mobile/app.config.ts` (portrait, android.package, predictiveBackGestureEnabled — analog)
    - `~/projects/quest-board-app/apps/mobile/tsconfig.json` (@/* alias, strict — drop @quest-board/* aliases)
    - `~/projects/quest-board-app/biome.json` (schema, vcs, files.includes, formatter — drop monorepo/haptics/grit)
    - `.planning/phases/01-project-scaffold-portable-code/01-PATTERNS.md` (Patterns 3 & 4; Anti-Patterns list)
  </read_first>
  <action>
    Create `app.config.ts`. Line 1 MUST be the side-effect import `import "tsx/cjs";` (the TS
    transpiler hook Expo needs to evaluate a TypeScript app config — quest-board's analog opens with
    exactly this; without it prebuild fails to load the config in wave 4, the most expensive place to
    discover it). Use the FUNCTIONAL form `export default ({ config }: ConfigContext): ExpoConfig =>
    ({ ...config, ... })` (import `ConfigContext`, `ExpoConfig` from `expo/config`) so the config
    MERGES the template's `app.json` (preserving its icon/splash/adaptive-icon references — do NOT
    delete app.json; this reconciles the app.json-vs-app.config.ts overlap the review flagged) and
    layers Orbit's fields on top: `name: "Orbit"`, `slug: "orbit"`, `orientation: "portrait"` (the
    config-layer portrait lock — CLAUDE.md), `android: { ...config.android, package:
    "com.placeholder.orbit", predictiveBackGestureEnabled: false }` with an adjacent comment stating
    the real id is confirmed by the owner at the FND-01 checkpoint (plan 01-05) before the first
    build, and `plugins: [...(config.plugins ?? []), "expo-sqlite"]` (pre-register the Phase 2 native
    dep so the first prebuild covers it). Do NOT add `newArchEnabled` (New Architecture is always-on
    since SDK 55). Do NOT add withSentry, expo-router, splash, or secure-store plugins (later phases /
    rejected by local-first). Create `tsconfig.json` extending `expo/tsconfig.base` with
    `strict: true`, `forceConsistentCasingInFileNames: true`, and `paths: { "@/*": ["./src/*"] }` —
    keep ONLY the `@/*` alias, drop every `@quest-board/*` alias. Create `biome.json` with `$schema`
    pointing at the `2.5.8` schema, `vcs` git + useIgnoreFile, `files.includes` = `["**", "!.planning",
    "!android", "!ios", "!**/dist"]` (exclude the generated native folders so they are never linted),
    `linter.rules.recommended: true`, formatter with 2-space indent. Drop quest-board's
    `noRestrictedImports` haptics rule and the `.grit` plugin. Add an `overrides` array carrying the
    two rule exemptions later ports depend on (they target files that do not exist yet — harmless, and
    lets 01-02 stay lint-clean without redesigning ported code): (a) an override matching
    `src/utils/logger.ts` that DISABLES the static-class complexity rules that fire on an intentional
    static gated logger — `noStaticOnlyClass` AND `noThisInStatic` — because `Logger` is a static
    singleton whose call sites are `Logger.warn/error/debug`; the `Logger` class itself is ported verbatim by **01-02** (01-04 only *calls* `Logger.*`), and it must NOT
    be refactored into free functions; (b) an override matching `src/**/*.test.ts` that DISABLES
    `noNonNullAssertion` so the ported Vitest suites (which use `result!.getFullYear()` idioms) lint
    clean. Confirm the exact rule ids against `npx biome explain`/`biome check` output.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check . && npx expo config --type public > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 app.config.ts | grep -c 'tsx/cjs'` == 1 (the transpiler hook is line 1)
    - `grep -c 'orientation' app.config.ts` >= 1 and the value is `"portrait"`
    - `grep -c 'expo-sqlite' app.config.ts` >= 1
    - `grep -c 'newArchEnabled' app.config.ts` == 0
    - `grep -c 'withSentry' app.config.ts` == 0
    - `test -f app.json` (the template's app.json is preserved, merged via the `{ config }` form — not deleted)
    - `grep -c '@/\*' tsconfig.json` >= 1 AND `grep -c '@quest-board' tsconfig.json` == 0
    - `grep -c '2.5.8' biome.json` >= 1 AND `grep -c '!android' biome.json` >= 1
    - `grep -c 'logger.ts' biome.json` >= 1 AND `grep -c 'noStaticOnlyClass' biome.json` >= 1 (the ported-Logger override is present)
    - `grep -c 'noNonNullAssertion' biome.json` >= 1 (the ported-test-file override is present)
    - `npx tsc --noEmit` exits 0, `npx biome check .` exits 0, and `npx expo config --type public` exits 0 (proves app.config.ts actually loads via tsx — the wave-1 catch for the wave-4 prebuild failure)
  </acceptance_criteria>
  <done>Portrait lock, strict tsconfig with @/* alias, an app.json-merging tsx-loaded app.config.ts, and a clean flat-repo Biome config (with the two ported-file rule overrides) are in place; tsc, biome, and `expo config` all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Stand up Vitest, the FULL folder skeleton, the shared colour gate, and prove the green baseline</name>
  <files>vitest.config.ts, package.json, scripts/check-colors.sh, src/db/.gitkeep, src/components/.gitkeep, src/screens/.gitkeep, src/theme/.gitkeep, src/stores/.gitkeep, src/services/.gitkeep, src/schemas/.gitkeep, src/utils/.gitkeep</files>
  <read_first>
    - `~/projects/Orbit/vitest.config.ts` (analog runner config — but this app has NO obsidian alias and uses node env, not jsdom, for pure logic)
    - `CLAUDE.md` (Repo layout table — the required src/ subfolders; the no-hardcoded-colour rule)
  </read_first>
  <action>
    Create `vitest.config.ts` (defineConfig from `vitest/config`) with `test.environment: "node"`
    (pure-logic tests need no DOM), `test.globals: true`, `test.include: ["src/**/*.test.ts"]`
    (co-located tests), `test.passWithNoTests: true` set IN THE CONFIG (not only on the CLI — so a
    later plan's bare `npx vitest run` gate never fails on execution ordering when no test file has
    landed yet; review finding), and a resolve alias `@` -> the ABSOLUTE `./src` path via
    `fileURLToPath(new URL("./src", import.meta.url))` (an absolute resolved path, so 01-02's
    `@/types` imports resolve deterministically). Do NOT add an `obsidian` alias — this app has zero
    Obsidian references. Create the FULL CLAUDE.md folder layout NOW so FND-06's layout claim is
    satisfied at the end of THIS plan (review finding — do not defer half the layout): add tracked
    `.gitkeep` files for ALL eight dirs — `src/db`, `src/components`, `src/screens`, `src/theme`,
    `src/stores`, `src/services`, `src/schemas`, `src/utils`. Later plans drop real files alongside
    these (the `.gitkeep` may remain harmlessly). Create the SHARED colour gate `scripts/check-colors.sh`
    and wire it as the `check:colors` npm script in `package.json` (`"check:colors": "bash
    scripts/check-colors.sh"`). The script scans the targets in `$@` (defaulting to `src App.tsx`; each target may be a FILE
    or a DIRECTORY — implement with `grep -r` so both work, which lets the negative-control probes
    in the acceptance criteria below, and later phases, point it at a single file)
    for forbidden colour literals — case-insensitive extended-regex `#[0-9a-fA-F]{3,8}\b` (3/4/6/8-digit
    hex, catching `#fff` and 8-digit `#RRGGBBAA`), `rgba?\(`, `hsla?\(`, and quoted named colours
    (`['"](white|black|transparent|silver|gray|grey|red|green|blue|yellow|orange|purple|pink|cyan|magenta|brown)['"]`)
    — over `*.ts`/`*.tsx`, EXCLUDING any path under `/theme/` (the sole sanctioned hex location), and
    exits NON-zero if any match remains (use `! grep -q ...` semantics so a clean tree exits 0). This
    is the one shared implementation every later phase reuses instead of re-greping. Do NOT run
    `check:colors` as part of THIS plan's baseline — the scaffold's default `App.tsx` still carries the
    template's `#fff` until plan 01-03 replaces it with the themed shell; 01-03 is the first enforcement
    point. Then prove the green baseline: `npx tsc --noEmit && npx biome check . && npx vitest run`
    (passWithNoTests now lives in the config, so no CLI flag is needed).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check . && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'src/\*\*/\*.test.ts' vitest.config.ts` >= 1 AND `grep -c 'passWithNoTests' vitest.config.ts` >= 1 AND `grep -c 'fileURLToPath' vitest.config.ts` >= 1
    - `grep -c 'obsidian' vitest.config.ts` == 0
    - `test -f src/db/.gitkeep && test -f src/components/.gitkeep && test -f src/screens/.gitkeep && test -f src/theme/.gitkeep && test -f src/stores/.gitkeep && test -f src/services/.gitkeep && test -f src/schemas/.gitkeep && test -f src/utils/.gitkeep` (all eight folders present — FND-06 layout complete)
    - `test -f scripts/check-colors.sh` AND `grep -c 'check:colors' package.json` >= 1
    - `bash scripts/check-colors.sh src/theme` exits 0 (no forbidden literal flagged inside the sanctioned dir when scanned as a target)
    - NEGATIVE + POSITIVE CONTROL (proves the gate is a real gate, not a decoration — the previous `printf … | grep` self-test only exercised a regex string typed into the criterion, never the script) — run as ONE compound command so `$d` persists: `d=$(mktemp -d) && printf 'const c = "#ff0000";\n' > "$d/hex.ts" && printf 'const c = rgba(0,0,0,1);\n' > "$d/rgba.ts" && printf 'const c = "white";\n' > "$d/named.ts" && printf 'export const x = 1;\n' > "$d/clean.ts" && ! bash scripts/check-colors.sh "$d/hex.ts" && ! bash scripts/check-colors.sh "$d/rgba.ts" && ! bash scripts/check-colors.sh "$d/named.ts" && bash scripts/check-colors.sh "$d/clean.ts" && rm -rf "$d"` — each of the three forbidden literals (`#ff0000`, `rgba(`, `"white"`) must make the script EXIT NON-ZERO (flagged), and the clean file must exit 0; the chain succeeds only if the gate flags bad input AND passes clean input (an inverted exit code, wrong `--include`, or a missing `-r` fails this)
    - The full gate `npx tsc --noEmit && npx biome check . && npx vitest run` exits 0
  </acceptance_criteria>
  <done>Vitest configured (absolute @ alias, passWithNoTests in config), all eight CLAUDE.md folders tracked, the shared check:colors gate created (enforced from 01-03 on), and the tsc+biome+vitest baseline green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Scaffold + dependency installs pull third-party code that runs during install and build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs (expo, expo-sqlite, zustand, biome, async-storage, safe-area-context) | high | mitigate | RESEARCH.md ran the Package Legitimacy Audit — all six are mainstream, official source repos, no `postinstall`, not deprecated; the only flag was `too-new` (frequent-legit-release false positive). Install exact pinned versions and use `npx expo install` for SDK resolution. No `[SLOP]`/`[SUS]`-with-real-signal packages — no human legitimacy checkpoint required. |

No new runtime trust boundary is introduced this plan (no network read path, no secrets). This is
pure scaffold + config.
</threat_model>

<artifacts_produced>
New files/config this plan creates (exclude from source-grounding "unknown symbol" checks):
`package.json`, `app.config.ts`, `tsconfig.json`, `biome.json`, `babel.config.js`,
`vitest.config.ts`, extended `.gitignore`, `scripts/check-colors.sh`, and the eight `.gitkeep`
folders (`src/{db,components,screens,theme,stores,services,schemas,utils}`). Config keys introduced:
`@/*` tsconfig path alias, `orientation: "portrait"`, `plugins: ["expo-sqlite"]`, placeholder
`android.package = com.placeholder.orbit`, biome `overrides` for `src/utils/logger.ts` and
`src/**/*.test.ts`, `check:colors` npm script, `test.passWithNoTests` in vitest config. The template
`app.json` is preserved and merged via the `({ config })` form.
</artifacts_produced>

<deferred_scope>
## Explicitly deferred (NOT in Phase 1 FND-01…06 scope) — recorded so it is not silently dropped

The **ADR bridge** and the first **KB manifest** are a separate *graphify-enablement foundation task*,
NOT part of this phase's requirements, and are deferred on purpose (review finding: "the ADR bridge is
unowned"):

- `docs/decisions/` + `docs/decisions/adr-registry.ts` (the generated ADR-node source for graphify),
  and `scripts/normalize-graph-docrefs.ts` — porting these from quest-board is the prerequisite for
  ever running `graph:build`. **Graphify is `enabled:false`** in `.planning/config.json` and STATE.md
  records the block explicitly; no Phase-1 plan builds the graph (correct). Deferring the bridge is
  therefore legitimate, not a gap.
- `01-KB-MANIFEST.md` (the phase KB manifest CLAUDE.md wants before the first phase completes) is
  produced by the KB-extraction step at phase close, not by an execute plan. The project-specific
  `qb-extract-phase-kb` skill does not exist in this repo yet (CLAUDE.md flags this); until it does,
  extraction is manual and happens at phase completion — it is NOT an FND-01…06 deliverable.

STATE.md is the tracking home for the graphify-enablement task; this note makes the deferral visible in
the plan the executor actually reads. Do NOT invent these files inside this phase's plans.
</deferred_scope>

<verification>
- `npx tsc --noEmit` exits 0
- `npx biome check .` exits 0
- `npx vitest run` exits 0 (passWithNoTests configured)
- `npx expo config --type public` exits 0 (app.config.ts loads via tsx)
- All eight `src/` folders present; `scripts/check-colors.sh` + `check:colors` script exist
- `git status` shows only additive changes; HANDOFF.md, docs/, .planning/, .claude/ untouched
</verification>

<success_criteria>
FND-06 satisfied: Biome lint/format configured and clean (with the ported-Logger and test-file rule
overrides so 01-02 stays green), portrait lock set at the config layer, the FULL CLAUDE.md eight-folder
layout present, a tsx-loaded app.config.ts that actually evaluates, and the shared `check:colors` gate
in place. The scaffold typechecks, lints, and runs the (empty) test suite green — the foundation for
plans 01-02, 01-03, 01-04, and 01-05.
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-portable-code/01-01-SUMMARY.md` when done.
</output>
