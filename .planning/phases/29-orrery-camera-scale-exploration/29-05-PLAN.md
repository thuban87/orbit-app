---
phase: 29-orrery-camera-scale-exploration
plan: "05"
type: execute
wave: 5
depends_on:
  - "29-04"
files_modified:
  - "src/components/orrery/OrbitBody.tsx"
  - "src/components/orrery/SunBody.tsx"
  - "src/components/orrery/ProjectedOrbitRing.tsx"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/logic/orrery-frame.test.ts"
  - "src/logic/orrery-label-logic.ts"
  - "src/logic/orrery-label-logic.test.ts"
  - "src/components/orrery/OrreryLabel.tsx"
autonomous: true
requirements:
  - "ORRC-03"
  - "ORRC-06"
  - "ORRC-07"
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Sun and contact billboards are direct siblings under one native depth group, so nearer bodies may occlude the sun with stable equal-depth ties."
    - "Photos stay circular and labels horizontal; perspective is noticeable but bounded, and ring paths use the same projection as body centers."
    - "E1: Empty/sun-only/loading/failure use approved copy; stale same-System content remains with notice; missing photos use initials, missing progress stays neutral and missing optional context is omitted; one remains readable, many grow; viewport clipping never drops companion members; ellipsized canvas names retain full accessible and wrapping companion identities."
    - "EDGE-05/14: UTF-8/combining/emoji names preserve original full identity; truncation uses native paragraph/grapheme-aware measurement, never byte or UTF-16-unit slicing."
    - "EDGE-05 (ORRC-03/encoding): Names containing emoji, combining marks and non-Latin scripts preserve complete original accessible identity and use measured native text truncation."
    - "EDGE-12 (ORRC-06/boundary): Semantic zoom thresholds are tested on both sides with hysteresis preventing boundary flicker."
    - "EDGE-13 (ORRC-06/empty): Zero labels is valid at overview and absent optional context creates no placeholder row."
    - "EDGE-14 (ORRC-06/encoding): Label truncation does not split grapheme identity; companion and accessibility retain full Unicode text."
    - "UI-E1 World; all 8 considerations (empty, loading, error, populated, partial, overflow, zero-one-many, long-text): Use Copywriting Contract for true empty, sun-only, initial loading and local failure. Keep last successful same-System data on refresh failure with its stale notice. Missing photo uses initials; missing progress stays neutral, missing optional context is omitted. Typical population uses bounded readable Home; one contact keeps useful scale, many grow the world. Clip world to viewport, preserve camera access and complete companion membership. Canvas names use one-line ellipsis when needed, with full accessible name and full wrapping name in the companion sheet."
  artifacts:
    - path: "src/components/orrery/ProjectedOrbitRing.tsx"
      provides: "ProjectedOrbitRing shared-frame path rendering"
    - path: "src/components/orrery/OrreryLabel.tsx"
      provides: "screen-facing semantic label with bundled fonts"
    - path: "src/logic/orrery-label-logic.ts"
      provides: "semantic level hysteresis and allocateLabels priority/exclusion policy"
    - path: "src/logic/orrery-frame.test.ts"
      provides: "intermediate transition geometry/hit/depth tests"
  key_links:
    - from: "src/components/orrery/OrreryWorld.tsx"
      to: "src/components/orrery/OrbitBody.tsx"
      via: "derived projected pose/depth without React sorting"
    - from: "src/logic/orrery-label-logic.ts"
      to: "src/components/orrery/OrreryLabel.tsx"
      via: "measured label rectangles and approved text roles"
---

<objective>
Render billboard bodies, projected rings and semantic labels from one animated frame.
Purpose: Keep depth and touch geometry accurate throughout interpolation without per-frame React work.
Output: ProjectedOrbitRing shared-frame path rendering; screen-facing semantic label with bundled fonts; semantic level hysteresis and allocateLabels priority/exclusion policy; intermediate transition geometry/hit/depth tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-05-01: Project rings and naturally depth-sort billboards including the sun</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrbitBody.tsx, src/components/orrery/SunBody.tsx, src/components/orrery/ProjectedOrbitRing.tsx, src/components/orrery/OrreryWorld.tsx, src/logic/orrery-frame.test.ts</files>
<read_first>src/components/orrery/OrbitBody.tsx, src/components/orrery/SunBody.tsx, src/components/orrery/OrreryCanvas.tsx, node_modules/@shopify/react-native-skia/src/dom/types/Common.ts, node_modules/@shopify/react-native-skia/cpp/api/recorder/RNRecorder.h; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/components/orrery/ProjectedOrbitRing.tsx (read current contents if present); src/components/orrery/OrreryWorld.tsx (read current contents if present); src/logic/orrery-frame.test.ts (read current contents if present)</read_first>
<behavior>At multiple animation fractions, ring/body/hit positions coincide. Equal depth sorts deterministically. Contact and sun can reverse relative depth. Exiting decorative bodies have no live action; entering bodies hit at their current displayed center.</behavior>
<action>Move contact and sun projected transforms/zIndex onto their actual keyed root Group so all comparable bodies are direct siblings within a single shared body layer. Avoid separate parent wrappers that prevent cross-body/sun sorting. Native shared zIndex drives depth, with deterministic source order for ties; no React child reorder per frame. Rings render behind bodies as sampled projected paths from the same frame transform, retaining status line style. Interpolate membership/density world state once then project once; drawing/hit/label visibility all consume that frame, including during transitions interrupted by a newer snapshot. Photos and avatar initials use local prepared resources and circular screen-facing billboard geometry; semantic name text is not scaled with avatars. Include viewport culling/detail suppression while retaining all members in snapshot/companion and all relevant interactive targets. Preserve clock reduced-motion gating.</action>
<acceptance_criteria>Shared current-frame rings/billboards/hits agree throughout animation; native sibling zIndex includes the sun and preserves keyed resource lifetimes.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-frame.test.ts && npm test -- src/logic/orrery-frame.test.ts src/services/orrery-scene.test.ts</automated><human-check>Later native check: tilt/yaw and mid-recovery touches confirm circular billboards and near-body occlusion of sun; Node results do not establish native rendering.</human-check></verify>
<done>Shared current-frame rings/billboards/hits agree throughout animation; native sibling zIndex includes the sun and preserves keyed resource lifetimes.</done>
</task>

<task type="auto" tdd="true">
<name>29-05-02: Reveal prioritized labels progressively without a text wall</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-label-logic.ts, src/logic/orrery-label-logic.test.ts, src/components/orrery/OrreryLabel.tsx, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/theme/tokens/typography.ts, src/components/orrery/OrbitBody.tsx, src/logic/orrery-camera-logic.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/logic/orrery-label-logic.ts (read current contents if present); src/logic/orrery-label-logic.test.ts (read current contents if present); src/components/orrery/OrreryLabel.tsx (read current contents if present); src/components/orrery/OrreryWorld.tsx (read current contents if present)</read_first>
<behavior>Far/identity/deeper levels with hysteresis; label priority focused→cluster→Favorites→isolated fit→others; touching rectangles follow one documented inclusive collision rule; empty labels valid; emoji/combining names and OS scaling preserve full content outside ellipsis.</behavior>
<action>Implement three semantic levels with stable threshold hysteresis. Allocate measured label rectangles by the approved priority order with stable identity tie breaks; respect measured controls/panel exclusion bounds. Overview largely hides names, identity exposes useful names, deeper adds existing lightweight health/Gravity/focused relation context only. Use bundled Inter/Space Grotesk matching approved roles and OS font scale, fixed readable screen-space text size with one-line canvas ellipsis. Do not split code units or normalize identity names. Reuse native paragraph measurement; keep resources off the frame loop. Explicitly implement Paragraph visibility/fade using supported paint/layer behavior rather than assuming parent opacity works. Wire active/focus/cluster priorities into frame and preserve complete names for conventional rows.</action>
<acceptance_criteria>Semantic labels remain horizontal/readable and prioritized through zoom, with deterministic fitting and complete accessible identities.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-label-logic.test.ts && npm test -- orrery-label-logic orrery-frame</automated></verify>
<done>Semantic labels remain horizontal/readable and prioritized through zoom, with deterministic fitting and complete accessible identities.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| interpolated frame → native drawing/hits | Both use identical live transform and membership generation. |
| text/photo → presentation | Local fallbacks and full accessible identities. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-05-01 | Tampering | animated hit targets | high | mitigate | Task1 samples endpoints and 0.25/0.5/0.75 transitions; exiting targets cease actions immediately. |
| T-29-05-02 | Denial of service | labels/depth | medium | mitigate | UI-thread shared props, culling and bounded labels prevent text walls/per-frame React churn. |
</threat_model>

<verification>
npm test -- src/logic/orrery-frame.test.ts src/services/orrery-scene.test.ts
npm test -- orrery-label-logic orrery-frame
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
Sun and contact billboards are direct siblings under one native depth group, so nearer bodies may occlude the sun with stable equal-depth ties.
Photos stay circular and labels horizontal; perspective is noticeable but bounded, and ring paths use the same projection as body centers.
E1: Empty/sun-only/loading/failure use approved copy; stale same-System content remains with notice; missing photos use initials, missing progress stays neutral and missing optional context is omitted; one remains readable, many grow; viewport clipping never drops companion members; ellipsized canvas names retain full accessible and wrapping companion identities.
EDGE-05/14: UTF-8/combining/emoji names preserve original full identity; truncation uses native paragraph/grapheme-aware measurement, never byte or UTF-16-unit slicing.
</success_criteria>

## Artifacts this phase produces

- src/components/orrery/ProjectedOrbitRing.tsx: ProjectedOrbitRing shared-frame path rendering
- src/components/orrery/OrreryLabel.tsx: screen-facing semantic label with bundled fonts
- src/logic/orrery-label-logic.ts: semantic level hysteresis and allocateLabels priority/exclusion policy
- src/logic/orrery-frame.test.ts: intermediate transition geometry/hit/depth tests

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-05-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
