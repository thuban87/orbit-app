---
phase: 29-orrery-camera-scale-exploration
plan: "02"
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - "src/db/migrations/021-orrery-preferences.ts"
  - "src/db/database.ts"
  - "src/db/app-settings-dao.ts"
  - "src/db/orrery-preferences.test.ts"
  - "src/screens/OrreryScreen.tsx"
  - "src/stores/orrery-preferences-store.ts"
  - "src/stores/orrery-preferences-store.test.ts"
  - "src/components/orrery/OrreryViewOptions.tsx"
  - "src/backup/backup-schema.ts"
  - "src/backup/orrery-preferences-portability.test.ts"
autonomous: true
requirements:
  - "ORRC-05"
  - "ORRC-13"
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-03/D-06: A separate forward-only Phase 29 migration adds density Balanced, satellites Off and last System All Contacts defaults without altering prior settings or ranks."
    - "D-07: No camera or focus is serialized; portable preference keys remain optional and current wire export omits them."
    - "E3: Fresh settings use Balanced/Off; hydration and saves mark affected controls busy, serialize conflicts, preserve saved choice on failure and retain a retryable intent; labels/help wrap and grow at large text."
    - "EDGE-29/30/31: Re-selecting saved options is a no-op; absent optional keys use defaults while read failure preserves durable intent; preset order is Spacious, Balanced, Compact."
    - "EDGE-11 (ORRC-05/unclassified): ASSUMPTION: ORRC-05 was unclassified; no new edge semantics are inferred. The approved three spacing presets preserve membership and use Balanced factory default."
    - "EDGE-29 (ORRC-13/adjacency): Selecting the already saved option is a no-op; duplicate/conflicting saves serialize and only a successful save publishes durable choice."
    - "EDGE-30 (ORRC-13/empty): Missing optional preference values use factory defaults; a failed read exposes retry and never writes defaults over unread durable state."
    - "EDGE-31 (ORRC-13/ordering): Density controls order Spacious, Balanced, Compact; conflicting async saves preserve the last successful committed value and retry intent."
    - "UI-E3 View options; all 5 considerations (empty, loading, error, partial, long-text): Fresh settings resolve Balanced/Off. Hydrating or saving indicates busy on the affected controls and prevents duplicate/conflicting writes. Optional missing preference values use documented factory defaults; read failure exposes retry rather than overwriting durable state with defaults. Failed writes restore the prior saved selection and retain a retryable change. Labels/help wrap and rows grow under text scaling."
  artifacts:
    - path: "src/db/migrations/021-orrery-preferences.ts"
      provides: "migration021, reallocate head+1 if changed before execution"
    - path: "src/stores/orrery-preferences-store.ts"
      provides: "useOrreryPreferencesStore with hydration/write/retry state"
    - path: "src/components/orrery/OrreryViewOptions.tsx"
      provides: "density and satellite control panel"
    - path: "src/db/orrery-preferences.test.ts"
      provides: "migration/default/rollback and settings integration"
    - path: "src/backup/orrery-preferences-portability.test.ts"
      provides: "optional acceptance and unchanged export omission"
  key_links:
    - from: "src/components/orrery/OrreryViewOptions.tsx"
      to: "src/stores/orrery-preferences-store.ts"
      via: "commit-before-publish typed actions"
    - from: "src/db/app-settings-dao.ts"
      to: "src/backup/backup-schema.ts"
      via: "optional portable closed-key validation"
---

<objective>
Persist Orrery density, satellites and last-System preferences through SQLite.
Purpose: Make approved view choices survive launches without persisting camera state.
Output: migration021, reallocate head+1 if changed before execution; useOrreryPreferencesStore with hydration/write/retry state; density and satellite control panel; migration/default/rollback and settings integration; optional acceptance and unchanged export omission.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-02-01: Ship the additive preference migration and a reachable saved density choice</name>
<reversibility rating="one-way">Additive on-device columns require forward migrations to reverse; D-03/D-06 already approve this door, so do not re-ask.</reversibility>
<precondition>Verify src/db/database.ts TARGET_VERSION and registered migration directory agree; current plan-time head20 means new step21, otherwise renumber this new step and its test references before editing.</precondition>
<files>src/db/migrations/021-orrery-preferences.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/db/orrery-preferences.test.ts, src/screens/OrreryScreen.tsx</files>
<read_first>src/db/migrations/020-dashboard-swipe-pref.ts, src/db/migrations/runner.ts, src/db/database.ts, src/db/app-settings-dao.ts, src/backup/restore-apply.ts, src/db/__testkit__/node-sqlite.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/db/migrations/021-orrery-preferences.ts (read current contents if present); src/db/orrery-preferences.test.ts (read current contents if present); src/screens/OrreryScreen.tsx (read current contents if present)</read_first>
<behavior>Fresh full-chain and head20 upgrade preserve sun/ranks/settings; defaults balanced/0/builtin:all-contacts. Invalid enum/toggle/System identity is rejected before SQL and by core restore calls. Forced migration failure preserves user_version and preexisting data.</behavior>
<action>Per D-03/D-06 add exactly one independent preference migration with orrery_density TEXT NOT NULL DEFAULT 'balanced' checked against spacious/balanced/compact; orrery_satellites_enabled INTEGER NOT NULL DEFAULT 0 checked 0/1; orrery_last_system TEXT NOT NULL DEFAULT 'builtin:all-contacts'. System identity uses a closed builtin token or category:<UID>, with a bounded nonempty UID payload; existence is resolved later, not enforced as a foreign key. Register head+1; migration runner owns transaction. Extend AppSettings, row/select/map, optional PortableSettingsSnapshot, writable keys, COLUMN_OF and validation including updateAppSettingsCore. Wire an accessible density choice into OrreryScreen using existing primitives and updateAppSettings, await commit and reload settings before publishing choice. Do not persist camera, modify shipped migrations, or touch backup emission. Task2 replaces inline control ownership with the dedicated preference store/panel, retaining the same live action.</action>
<acceptance_criteria>Density is selectable and survives reread; all three approved durable columns exist through the production chain and old data is preserved.</acceptance_criteria>
<verify><automated>test -f src/db/orrery-preferences.test.ts && npm test -- src/db/orrery-preferences.test.ts</automated></verify>
<done>Density is selectable and survives reread; all three approved durable columns exist through the production chain and old data is preserved.</done>
</task>

<task type="auto" tdd="true">
<name>29-02-02: Expose view options with truthful hydration, save and retry state</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/stores/orrery-preferences-store.ts, src/stores/orrery-preferences-store.test.ts, src/components/orrery/OrreryViewOptions.tsx, src/screens/OrreryScreen.tsx</files>
<read_first>src/stores/dashboard-query-store.ts, src/components/control-surface/AnchoredPanel.tsx, src/components/ui/GlassSurface.tsx, src/components/ui/AppText.tsx; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/stores/orrery-preferences-store.ts (read current contents if present); src/stores/orrery-preferences-store.test.ts (read current contents if present); src/components/orrery/OrreryViewOptions.tsx (read current contents if present); src/screens/OrreryScreen.tsx (read current contents if present)</read_first>
<behavior>Read rejection is distinct from omitted optional values; conflicting rapid updates serialize; late hydration cannot replace a newer successful write; duplicate pending or unchanged selection makes no second write; rejected save keeps saved selection and retry intent.</behavior>
<action>Per D-06 implement a dedicated Zustand preference store, with committed values, hydration phase, pending intent, save/read error and retry. Serialize conflicting writes and generation-guard hydration; only publish committed values. Replace screen-inline preference ownership with OrreryViewOptions top-right panel: Spacious/Balanced/Compact explicit selected state, Relationship Satellites switch default Off, exact approved help and failure/retry copy. Do not copy Dashboard's centered anchoring or transient ID. Register orrery-view-options with latest callback, focus panel on open and restore trigger on close; wrap labels/help and allow scroll/reflow. Keep loaded view available during failures. Wire density to scene input and satellite setting to scene presentation input; render behavior expands in its owning plans.</action>
<acceptance_criteria>View options is reachable with real persistence, guarded busy/error/retry state and no stale hydration overwrite.</acceptance_criteria>
<verify><automated>test -f src/stores/orrery-preferences-store.test.ts && npm test -- orrery-preferences</automated></verify>
<done>View options is reachable with real persistence, guarded busy/error/retry state and no stale hydration overwrite.</done>
</task>

<task type="auto" tdd="true">
<name>29-02-03: Protect restore acceptance and the current export boundary</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/backup/backup-schema.ts, src/backup/orrery-preferences-portability.test.ts</files>
<read_first>src/backup/backup-schema.ts, src/backup/types.ts, src/backup/export-manifest.ts, src/backup/restore-apply.ts, src/db/app-settings-dao.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/backup/orrery-preferences-portability.test.ts (read current contents if present)</read_first>
<behavior>Optional valid preference keys validate and apply through the existing restore core; omitted keys preserve existing choices. Current snapshot/export omits all three new keys, camera/focus and secrets; current BACKUP_FORMAT_VERSION stays4.</behavior>
<action>Per D-03 extend PORTABLE_SETTINGS_KEYS and validators for optional orreryDensity/orrerySatellitesEnabled/orreryLastSystem only, matching DAO names and closed validators. Exercise validation and existing applyRestore with production migration chain and missing-key merge/replace fixtures; optional omission must not invent resets. Pin current getPortableSettingsSnapshot/export omission and current format4, leaving coordinated emission/versioning to Phase36. Category UID token is stable identity; no device-local numeric category ID becomes a portable token. Do not change restore behavior beyond the accepted keys.</action>
<acceptance_criteria>New optional keys round-trip through accepted restore input, while current exported wire content/version remains unchanged.</acceptance_criteria>
<verify><automated>test -f src/backup/orrery-preferences-portability.test.ts && npm test -- src/backup/orrery-preferences-portability.test.ts</automated></verify>
<done>New optional keys round-trip through accepted restore input, while current exported wire content/version remains unchanged.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| control/restore → app_settings | Validated durable preference input. |
| settings → portable wire | Ordinary preferences only; phase36 owns emission. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-02-01 | Tampering | app_settings DAO/core | high | mitigate | Validate density/toggle/System token before public and core writes; bound parameters. |
| T-29-02-02 | Information disclosure | portable settings | high | mitigate | Omission and unknown/secret-key rejection tests retain allowlist. |
</threat_model>

<verification>
npm test -- src/db/orrery-preferences.test.ts
npm test -- orrery-preferences
npm test -- src/backup/orrery-preferences-portability.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
D-03/D-06: A separate forward-only Phase 29 migration adds density Balanced, satellites Off and last System All Contacts defaults without altering prior settings or ranks.
D-07: No camera or focus is serialized; portable preference keys remain optional and current wire export omits them.
E3: Fresh settings use Balanced/Off; hydration and saves mark affected controls busy, serialize conflicts, preserve saved choice on failure and retain a retryable intent; labels/help wrap and grow at large text.
EDGE-29/30/31: Re-selecting saved options is a no-op; absent optional keys use defaults while read failure preserves durable intent; preset order is Spacious, Balanced, Compact.
</success_criteria>

## Artifacts this phase produces

- src/db/migrations/021-orrery-preferences.ts: migration021, reallocate head+1 if changed before execution
- src/stores/orrery-preferences-store.ts: useOrreryPreferencesStore with hydration/write/retry state
- src/components/orrery/OrreryViewOptions.tsx: density and satellite control panel
- src/db/orrery-preferences.test.ts: migration/default/rollback and settings integration
- src/backup/orrery-preferences-portability.test.ts: optional acceptance and unchanged export omission

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-02-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
