---
phase: 29-orrery-camera-scale-exploration
plan: "09"
type: execute
wave: 9
depends_on:
  - "29-08"
files_modified:
  - "src/db/__testkit__/sqlite-local-day.ts"
  - "src/logic/ring-reorder-logic.ts"
  - "src/logic/ring-reorder-logic.test.ts"
  - "src/db/ring-seq-dao.ts"
  - "src/db/ring-seq-dao.test.ts"
  - "src/components/orrery/use-orrery-camera.ts"
  - "src/logic/orrery-reorder-logic.ts"
  - "src/logic/orrery-reorder-logic.test.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/screens/OrreryScreen.tsx"
autonomous: true
requirements:
  - "ORRC-10"
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Prolonged stationary hold gives visible/haptic activation; motion before activation pans; neutral and ambiguous targets do not arm persisted reorder."
    - "Filtered reorder moves selected contacted IDs only within their complete contacted order slots, preserving unselected order."
    - "Current saved sun, expected complete population/order, ID/UID fingerprints, and expected eligible contacted membership of the identified System are revalidated under one write lock using current local-day predicates before any update; stale/duplicate/omitted IDs roll back all writes and existing uniqueness/count/scoped-update guards remain."
    - "Cancel/second-pointer/blur/switch/finalize never persists; no-op release writes nothing; failure restores committed layout with exact retry/reload feedback and does not alter recency/Gravity/status."
    - "EDGE-22 (ORRC-10/unclassified): ASSUMPTION: ORRC-10 was unclassified; no widening of rank eligibility is inferred. Contacted-only persisted reorder uses the existing guards plus expected order and saved-sun checks."
  artifacts:
    - path: "src/db/__testkit__/sqlite-local-day.ts"
      provides: "openSqliteLocalDayFixture: per-connection exact SQLite local-day override with native delegation and teardown"
    - path: "src/logic/ring-reorder-logic.ts"
      provides: "mergeVisibleRingOrder and unchanged permutation helper"
    - path: "src/db/ring-seq-dao.ts"
      provides: "RingReorderRequest with System identity, expected full order/saved sun/eligible visible IDs and ID/UID fingerprints plus reordered visible IDs; lock-time predicate validation"
    - path: "src/logic/orrery-reorder-logic.ts"
      provides: "hold/drag preview, inverse world-radius rank and commit intent"
  key_links:
    - from: "src/components/orrery/use-orrery-camera.ts"
      to: "src/db/ring-seq-dao.ts"
      via: "success-only generation-scoped complete reorder intent"
    - from: "src/logic/orrery-reorder-logic.ts"
      to: "src/logic/orrery-camera-logic.ts"
      via: "inverse projection subtracts actual outward drift"
    - from: "src/db/ring-seq-dao.ts"
      to: "src/db/orrery-system-read.ts"
      via: "plan29-03 readOrrerySystemMembersCore on the already-locked executor; no nested snapshot/mutex"
---

<objective>
Reorder contacted rings deliberately without corrupting filtered or hidden order.
Purpose: Preserve complete rank/lifecycle/sun guards while enabling prolonged-hold reorder within any eligible System.
Output: mergeVisibleRingOrder and unchanged permutation helper; RingReorderRequest with System identity and expected full order/saved sun/eligible visible IDs validated under the write lock; hold/drag preview, inverse world-radius rank and commit intent.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-09-01: Commit filtered contacted permutations under order, sun and live System guards</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/ring-reorder-logic.ts, src/logic/ring-reorder-logic.test.ts, src/db/ring-seq-dao.ts, src/db/ring-seq-dao.test.ts, src/db/__testkit__/sqlite-local-day.ts</files>
<read_first>src/db/__testkit__/sqlite-local-day.ts (if present), src/db/__testkit__/node-sqlite.ts, src/db/mutex.ts, src/db/orrery-system-read.ts, src/logic/orrery-system-logic.ts, src/logic/dashboard-query-logic.ts, src/db/status.ts, src/db/transaction.ts, src/db/bulk-actions-dao.ts, src/db/favourites-dao.ts, src/db/snooze-dao.ts, src/db/ring-seq-dao.ts, src/db/orrery-read.ts, src/db/contacts-dao.ts, src/db/contact-lifecycle-dao.ts, src/db/recency-dao.ts, src/db/merge-dao.ts, src/db/imported-contact-dao.ts, src/services/import/source-consolidation.ts, src/db/purge-dao.ts, src/backup/restore-apply.ts, src/db/app-settings-dao.ts, src/db/migrations/011-contact-lifecycle-schema.ts, src/logic/ring-reorder-logic.ts (read current contents if present; new paths are created by this task), src/logic/ring-reorder-logic.test.ts (read current contents if present; new paths are created by this task), src/db/ring-seq-dao.ts (read current contents if present; new paths are created by this task), src/db/ring-seq-dao.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Full A,B,C,D,E with visible B,D moved D,B becomes A,D,C,B,E. Duplicate/partial/nonmember lists fail atomically. Same count but changed IDs/order, sun changed between hold/release, archived/unbound/deleted/merged row all reject without writes. With complete order and saved sun unchanged, reject Favorites removal/addition or same-count replacement, Category reassignment/removal, Charger change, snooze/unsnooze, cadence and qualifying recency changes that alter eligible visible membership before release or while queued for the mutex. Test local midnight crossing for Snoozed expiry and Needs Attention entry without a dataRevision change, executing real predicates at the new local day. Category rename with stable UID and unchanged members succeeds. A real-SQL purge/create or restore fixture reuses a numeric ID for a different UID with the same full numeric order/sun/membership and must reject atomically. Unchanged valid order is no-op; rejection changes no rank, modified_at or revision.</behavior>
<action>Preserve current code's contacted Bound nonarchived sun-excluded rank eligibility; ADR-046 governs complete guarded rendered scope but does not explicitly mandate contacted-only population. Neutral display does not authorize a write extension. Add mergeVisibleRingOrder(fullExpectedOrder, expectedVisibleIds, reorderedVisibleIds), validating uniqueness and exact visible permutation before substituting only visible slots. Define RingReorderRequest with system: OrrerySystemRef, expectedFullOrderedIds, expectedSavedSunContactId, expectedEligibleVisibleIds, expectedContactIdentities and reorderedVisibleIds. expectedContactIdentities carries exact ID/UID pairs for the full contacted order and any saved-sun row, including a neutral saved sun outside that order. Capture expected values from plan29-03's single coherent snapshot. Inside the existing inWriteTransaction read current singleton sun and current complete contacted order using exact DAO sorting, compare both to expected and compare their current ID/UID pairs with expectedContactIdentities before accepting the numeric IDs (migration011 permits rowid reuse), then call readOrrerySystemMembersCore(exec, request.system) on the SAME locked executor. Reject missing/invalid System or Category identity; derive current eligible contacted visible IDs from current members intersected with the complete contacted sun-excluded order and compare ordered IDs exactly with expectedEligibleVisibleIds. Reevaluate exact shared Favorites/category/Charger/Needs Attention/Snoozed predicates at this read's current SQLite local day, including snooze suppression and status thresholds; caller timestamps, cached generations and dataRevision do not authorize time-sensitive membership. Only after all checks pass compute the new complete order with mergeVisibleRingOrder inside the lock; never accept caller-supplied arbitrary new complete order or alter hidden slots. Retain uniqueness/count/scoped changes===1 guards and current-sun validation. No nested inReadSnapshot, write transaction or mutex. Preserve valid no-op behavior and one revision bump on actual successful change only. Plan29-09-02 wires the camera commit seam to this request, retaining controller generation cancellation as an early guard and recoverable DAO rejection as final authority. Addresses review concern: create openSqliteLocalDayFixture(initialLocalDay) in src/db/__testkit__/sqlite-local-day.ts, a test-only factory returning {db, exec, setLocalDay, close}. Use openTestDb/nodeSqliteExecutor and a second isolated DatabaseSync connection for native date delegation. Register db.function('date', {varargs:true}, callback) only on the fixture connection. Intercept exactly two arguments equal to 'now' and 'localtime', returning the controlled YYYY-MM-DD local day. Delegate EVERY other date invocation, including stored date(value), NULL, and modifiers, through a parameter-bound SELECT date(...) on the untouched second connection; julianday and all production query text stay native and unchanged. Validate setter input with native SQLite date normalization; close both connections in finally/afterEach, idempotently, without global mocks, prototype changes, application clock parameters or production imports of the testkit. First prove stored-date/modifier/NULL parity and that only the exact clock call changes. Then load the real scene snapshot at dayD, retain its complete order/sun/UID/membership request, acquire the shared mutex using a deferred test blocker, enqueue the actual rank DAO, advance only the fixture local day toD+1 while queued, and release the blocker. Use a Snoozed parent expiring atD+1 and a Needs Attention parent crossing its threshold or suppression boundary atD+1; positive controls at unchanged day succeed. Assert the membership query executes only after lock acquisition with the advanced day and rejects the old request; full stored rows, rank, modified_at and dataRevision are unchanged by clock advancement and rejection. Do not refresh the expected request or mutate contacts to simulate midnight. Keep unmodified real-clock tests as parity coverage: fixture dates relative to SELECT date('now','localtime') must match the shared predicates. This helper is newly implemented by this task, not a capability claimed of the existing adapter. A runnable planning feasibility probe is /tmp/orbit-phase29-sqlite-day-probe.cjs; the committed task specification above is sufficient without that temporary file. Read every current table writer from COVERAGE audit before declaring a new invariant; this change strengthens the reorder entry point and does not rewrite other lifecycle/merge/restore policies.</action>
<acceptance_criteria>Actual guarded DAO rejects stale System membership even with unchanged full order/sun and no refresh or revision signal; valid filtered permutations preserve hidden slots and every existing guard under one lock.</acceptance_criteria>
<verify><automated>test -f src/logic/ring-reorder-logic.test.ts && test -f src/db/ring-seq-dao.test.ts && test -f src/db/orrery-system-read.test.ts && npm test -- src/logic/ring-reorder-logic.test.ts src/db/ring-seq-dao.test.ts src/db/orrery-system-read.test.ts</automated></verify>
<done>Actual guarded DAO rejects stale System membership even with unchanged full order/sun and no refresh or revision signal; valid filtered permutations preserve hidden slots and every existing guard under one lock.</done>
</task>

<task type="auto" tdd="true">
<name>29-09-02: Arm prolonged-hold reorder with inverse-projected preview and safe release</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-reorder-logic.ts, src/logic/orrery-reorder-logic.test.ts, src/components/orrery/use-orrery-camera.ts, src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx</files>
<read_first>node_modules/react-native-gesture-handler/src/handlers/gestures/panGesture.ts, src/logic/orrery-gesture-logic.ts, src/logic/orrery-camera-logic.ts, src/logic/ring-reorder-logic.ts, src/logic/orrery-reorder-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-reorder-logic.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Hold arms while stationary before movement; prehold move pans; no-motion release maps back to same rank even for drifted bodies under tilt/yaw; midpoint rank ties use documented nearest-integer/clamp; cancellation, second pointer and stale generation emit no write.</behavior>
<action>Restore reachable ring reordering through this new recognizer; plan29-01 already retired the legacy radial writer, so do not revive its callbacks or bypass these guards. Wire the camera commit seam to plan29-09-01's RingReorderRequest. Use activateAfterLongPress with one-pointer guard and centralized deliberately prolonged duration/slop. Hit-test current frame at touch-down and require one eligible contacted target; capture system identity, expectedFullOrderedIds, expectedSavedSunContactId, expectedEligibleVisibleIds, expectedContactIdentities and System generation from the same committed snapshot before arm. Keep that captured request unchanged through release and supply only the reordered visible permutation; never refresh expected values to bless an older drag. At activation show highlight/ghost ring and one haptic acknowledgement, independent of movement. After arm convert touch through unprojectToWorldPlane and subtract the body's actual outward drift before nearest ring/rank selection. Derive target among eligible contacted slots; neutral bodies never become write targets. Preview only shared transient layout; persist once on successful onEnd with changed order and still-current generation; DAO membership revalidation remains mandatory even when generation has not refreshed. onFinalize clears only. New System snapshot, active sun change, second pointer, blur/background, unmount or recognizer failure cancels. On DAO error restore committed snapshot/preview and exact approved failure plus Reload System, retaining failure if reload also rejects.</action>
<acceptance_criteria>Ordinary panning cannot reorder; deliberate held drags have visible feedback and atomic guarded success/failure behavior.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-reorder-logic.test.ts && npm test -- src/logic/orrery-reorder-logic.test.ts src/logic/orrery-gesture-logic.test.ts src/db/ring-seq-dao.test.ts</automated><human-check>Native gesture check: stationary hold activation before drag, preactivation pan, multi-pointer cancellation and haptic/ghost-ring acknowledgment.</human-check></verify>
<done>Ordinary panning cannot reorder; deliberate held drags have visible feedback and atomic guarded success/failure behavior.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| filtered visible order → complete DB population | No guard weakening or neutral eligibility widening. |
| delayed gesture → write lock | Expected order, current saved sun and identified System eligible membership revalidated under lock with current local-day predicates. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-09-01 | Tampering | rank write | high | mitigate | Full order/sun, ID/UID fingerprints and eligible System membership equality at current local day, hidden-slot merge and old three guards under one mutex transaction; test queued mutation and clock-only invalidation. |
| T-29-09-02 | Tampering | cancelled recognizer | high | mitigate | Only successful active onEnd can dispatch; finalize cleanup cannot persist. |
</threat_model>

<verification>
npm test -- src/logic/ring-reorder-logic.test.ts src/db/ring-seq-dao.test.ts src/db/orrery-system-read.test.ts
npm test -- src/logic/orrery-reorder-logic.test.ts src/logic/orrery-gesture-logic.test.ts src/db/ring-seq-dao.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
Prolonged stationary hold gives visible/haptic activation; motion before activation pans; neutral and ambiguous targets do not arm persisted reorder.
Filtered reorder moves selected contacted IDs only within their complete contacted order slots, preserving unselected order.
Current saved sun, expected complete population/order ID/UID fingerprints and eligible membership of the identified System are revalidated under one write lock at current local day; every existing uniqueness/count/scoped-update guard and hidden-slot rule remains. Stale membership rejects even when complete order/sun and dataRevision are unchanged.
Cancel/second-pointer/blur/switch/finalize never persists; no-op release writes nothing; failure restores committed layout with exact retry/reload feedback and does not alter recency/Gravity/status.
</success_criteria>

## Artifacts this phase produces

- src/db/__testkit__/sqlite-local-day.ts: openSqliteLocalDayFixture test-only helper, consumed by ring-seq-dao tests and plan12 integration; no app import or production clock injection

- src/logic/ring-reorder-logic.ts: mergeVisibleRingOrder and unchanged permutation helper
- src/db/ring-seq-dao.ts: RingReorderRequest { system, expectedFullOrderedIds, expectedSavedSunContactId, expectedEligibleVisibleIds, expectedContactIdentities, reorderedVisibleIds }; current System predicates rechecked through plan29-03 readOrrerySystemMembersCore inside the write lock before computing the complete permutation
- src/logic/orrery-reorder-logic.ts: hold/drag preview, inverse world-radius rank and commit intent

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-09-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
