---
phase: 29-orrery-camera-scale-exploration
plan: "07"
type: execute
wave: 7
depends_on:
  - "29-06"
files_modified:
  - "src/components/orrery/use-orrery-camera.ts"
  - "src/logic/orrery-gesture-logic.ts"
  - "src/logic/orrery-gesture-logic.test.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/logic/orrery-recovery-logic.ts"
  - "src/logic/orrery-recovery-logic.test.ts"
  - "src/components/orrery/Polaris.tsx"
autonomous: true
requirements:
  - "ORRC-02"
  - "ORRC-09"
  - "ORRC-10"
  - "ORRC-16"
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One-finger movement pans; pinch preserves focal world anchor; deliberate two-finger translation tilts and rotation changes yaw without contact mutation."
    - "Recenter clears focus/cluster and restores all axes with continuous distance-adaptive bounded recovery; Polaris resets yaw only."
    - "Pan/yaw coast only a tiny bounded amount; zoom has no continuation and tilt none; Reduced Motion disables continuation and uses short direct recovery."
    - "Camera/input cancellation never emits navigation or rank persistence from finalize."
    - "EDGE-03 (ORRC-02/unclassified): ASSUMPTION: ORRC-02 was unclassified by the probe; no extra behavior is inferred. Bounded camera, frame authority and native gesture recognition use the explicit canonical contracts; device proof remains pending."
    - "EDGE-21 (ORRC-09/unclassified): ASSUMPTION: ORRC-09 was unclassified; no alternate recovery behavior is inferred. Recenter resets all axes, Polaris yaw only, with bounded recovery."
  artifacts:
    - path: "src/components/orrery/use-orrery-camera.ts"
      provides: "useOrreryCamera shared pose/frame/gesture controller"
    - path: "src/logic/orrery-gesture-logic.ts"
      provides: "gesture ownership and camera intent transition rules"
    - path: "src/logic/orrery-recovery-logic.ts"
      provides: "bounded distance-adaptive recovery and north-only target"
    - path: "src/components/orrery/Polaris.tsx"
      provides: "world-projected theme landmark"
  key_links:
    - from: "src/components/orrery/use-orrery-camera.ts"
      to: "src/logic/orrery-camera-logic.ts"
      via: "bounded pose and inverse focal anchoring"
    - from: "src/components/orrery/Polaris.tsx"
      to: "src/components/orrery/use-orrery-camera.ts"
      via: "successful current-frame yaw-only reset"
---

<objective>
Manipulate the bounded camera and recover Home/north continuously.
Purpose: Deliver deliberate multitouch navigation with restrained inertia and interruption-safe recovery.
Output: useOrreryCamera shared pose/frame/gesture controller; gesture ownership and camera intent transition rules; bounded distance-adaptive recovery and north-only target; world-projected theme landmark.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-07-01: Navigate with pan, pinch, deliberate tilt and yaw</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/use-orrery-camera.ts, src/logic/orrery-gesture-logic.ts, src/logic/orrery-gesture-logic.test.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>node_modules/react-native-gesture-handler/src/handlers/gestures/panGesture.ts, src/components/orrery/OrreryWorld.tsx, src/logic/orrery-camera-logic.ts, src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task), src/logic/orrery-gesture-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-gesture-logic.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Motion before prolonged hold belongs to pan; pinch preserves focal world position; two-finger vertical centroid motion after deliberate threshold tilts within limits; rotation yaws; handoff/rebase does not jump; failed tap/second-pointer/cancel/blur never opens Profile or writes ranks.</behavior>
<action>Extract existing tracer camera into useOrreryCamera, retaining shared authoritative frame. Use installed Gesture Handler2 builder APIs: one-pointer pan, pinch with focal initialization at active/start, and deliberate two-finger tilt plus rotation/yaw with documented centralized activation thresholds. Compose recognizers and ownership so ordinary motion wins pan before stationary hold, successful held reorder can claim one pointer later, and a second pointer cancels rank intent before camera multitouch. Both simultaneous zoom/yaw and tilt respect current shared bounded pose and rebase gesture anchors at ownership change. Test pure arbitration with native callbacks as thin adapters. successful onEnd gates tap/actions; onFinalize performs cleanup only. Cancel old recovery/inertia immediately on new manual input. Bridge discrete IDs/intents, never touch-coordinate frames, to JS.</action>
<acceptance_criteria>All four camera gestures are wired to the reachable world with bounded math, focal anchoring and cancellation-safe ownership.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-gesture-logic.test.ts && npm test -- orrery-gesture-logic orrery-camera-logic orrery-frame</automated></verify>
<done>All four camera gestures are wired to the reachable world with bounded math, focal anchoring and cancellation-safe ownership.</done>
</task>

<task type="auto" tdd="true">
<name>29-07-02: Add world Polaris and distance-adaptive Recenter with restrained inertia</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-recovery-logic.ts, src/logic/orrery-recovery-logic.test.ts, src/components/orrery/Polaris.tsx, src/components/orrery/use-orrery-camera.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/theme/use-reduced-motion.ts, src/components/orrery/SunBody.tsx, src/components/orrery/OrreryControls.tsx, src/logic/orrery-recovery-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-recovery-logic.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/Polaris.tsx (read current contents if present; new paths are created by this task), src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>At zero/small/large displacement recovery is finite, continuous and duration-bounded, ending at exact Home. North reset changes yaw only, including wrap boundary. New gesture/Reduced Motion/blur invalidates prior completion. Manual gestures work while motion reduction is on.</behavior>
<action>Draw Polaris from theme primitives at a canonical world north point included in finite projection bounds. It follows yaw rather than being fixed chrome; successful tap or accessible Reset north targets yaw0 using shortest angular distance, preserving pan/zoom/tilt/focus. Implement continuous Acquire/Approach/Dock-style recenter curve with bounded adaptive duration; target derives from current measured Home and clears focus/group. Tiny clamped pan/yaw inertia only; zoom/tilt stop on release. Consume useReducedMotionShared inside animation worklets, immediately cancel existing inertia/recovery when it turns on, then short direct transition to valid target. Track animation generation so interrupted completion cannot publish stale pose/focus. Wire Recenter/Reset north callbacks from plan6 to controller and orientation accessibility value. Centralize tuning constants; defer phone feel/performance claims.</action>
<acceptance_criteria>World Polaris and conventional recovery controls function with correct reset scope, bounded continuous recovery and live reduced-motion cancellation.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-recovery-logic.test.ts && npm test -- orrery-recovery-logic orrery-gesture-logic</automated></verify>
<done>World Polaris and conventional recovery controls function with correct reset scope, bounded continuous recovery and live reduced-motion cancellation.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| native gesture → pose/intent | One active input owner and generation. |
| motion preference → existing animation | Cancellation precedes replacement. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-07-01 | Tampering | gesture cancellation | high | mitigate | Success-only end actions; finalize only clears transient state; second pointer/blur invalidates drag. |
| T-29-07-02 | Denial of service | camera inertia | medium | mitigate | Bounded velocities/duration and cancellation generation guard. |
</threat_model>

<verification>
npm test -- orrery-gesture-logic orrery-camera-logic orrery-frame
npm test -- orrery-recovery-logic orrery-gesture-logic
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
One-finger movement pans; pinch preserves focal world anchor; deliberate two-finger translation tilts and rotation changes yaw without contact mutation.
Recenter clears focus/cluster and restores all axes with continuous distance-adaptive bounded recovery; Polaris resets yaw only.
Pan/yaw coast only a tiny bounded amount; zoom has no continuation and tilt none; Reduced Motion disables continuation and uses short direct recovery.
Camera/input cancellation never emits navigation or rank persistence from finalize.
</success_criteria>

## Artifacts this phase produces

- src/components/orrery/use-orrery-camera.ts: useOrreryCamera shared pose/frame/gesture controller
- src/logic/orrery-gesture-logic.ts: gesture ownership and camera intent transition rules
- src/logic/orrery-recovery-logic.ts: bounded distance-adaptive recovery and north-only target
- src/components/orrery/Polaris.tsx: world-projected theme landmark

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-07-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
