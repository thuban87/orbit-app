---
phase: 29-orrery-camera-scale-exploration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/services/orrery-scene.ts"
  - "src/logic/orrery-camera-logic.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/screens/OrreryScreen.tsx"
  - "src/services/orrery-scene.test.ts"
  - "src/components/orrery/OrbitBody.tsx"
  - "src/logic/orrery-geometry-logic.ts"
  - "src/logic/orrery-geometry-logic.test.ts"
autonomous: true
requirements:
  - "ORRC-01"
  - "ORRC-02"
  - "ORRC-07"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: The Orrery has one unnamed timestamp-based health world and no Status/Relationship toggle."
    - "A real contacted row read through the existing DAO is drawn and hit-tested from the same projected frame after pan; identity-level successful tap opens its Profile."
    - "EDGE-01/02: Identical scene input gives identical placement; stale or cancelled read generations cannot replace newer members."
    - "EDGE-01 (ORRC-01/idempotency): The same input snapshot and camera pose yields the same placement and intent; repeated focus of the same live ID does not duplicate navigation."
  artifacts:
    - path: "src/services/orrery-scene.ts"
      provides: "loadOrreryScene and OrrerySceneSnapshot"
    - path: "src/logic/orrery-camera-logic.ts"
      provides: "CameraPose, WorldPoint, ProjectedFrame, projectWorldPoint, unprojectToWorldPlane, projectFrame, collectHitCandidates"
    - path: "src/components/orrery/OrreryWorld.tsx"
      provides: "OrreryWorld UI-thread scene adapter"
    - path: "src/services/orrery-scene.test.ts"
      provides: "real-SQL read → projection → hit → focus/Profile tracer"
  key_links:
    - from: "src/screens/OrreryScreen.tsx"
      to: "src/services/orrery-scene.ts"
      via: "real SQLite focus refresh"
    - from: "src/components/orrery/OrreryWorld.tsx"
      to: "src/logic/orrery-camera-logic.ts"
      via: "drawing and gesture callbacks share one projected frame"
---

<objective>
Prove canonical world → bounded camera → current-frame contact interaction.
Purpose: Deliver a reachable production tracer over real local data before expanding controls.
Output: loadOrreryScene and OrrerySceneSnapshot; CameraPose, WorldPoint, ProjectedFrame, projectWorldPoint, unprojectToWorldPlane, projectFrame, collectHitCandidates; OrreryWorld UI-thread scene adapter; real-SQL read → projection → hit → focus/Profile tracer.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<!-- Review cycle 1: the camera and legacy radial writer must never compete. -->

<task type="tracer" tdd="true">
<name>29-01-01: Pan and focus a real local contact through the full scene path</name>
<reversibility rating="costly">All later render, hit and camera consumers depend on this shared frame contract.</reversibility>
<files>src/services/orrery-scene.ts, src/logic/orrery-camera-logic.ts, src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx, src/services/orrery-scene.test.ts</files>
<read_first>docs/dossier/milestone-2/phase-08-orrery-camera-scale-exploration-dossier.md, docs/dossier/milestone-2/planning-notes/phase-08-planning-notes.md, src/screens/OrreryScreen.tsx, src/components/orrery/OrreryCanvas.tsx, src/components/orrery/OrbitBody.tsx, src/components/orrery/SunBody.tsx, src/db/orrery-read.ts, src/db/transaction.ts, src/logic/sun-occupant-logic.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/services/orrery-scene.ts (read current contents if present); src/logic/orrery-camera-logic.ts (read current contents if present); src/components/orrery/OrreryWorld.tsx (read current contents if present); src/services/orrery-scene.test.ts (read current contents if present)</read_first>
<behavior>Create a contacted Bound row with production migrations and nodeSqliteExecutor. Invoke the actual loader, frame projection, hit collection and intent dispatcher used by the screen. At Home, after pan and halfway through focus animation the drawn location hits the same ID; failed/stale taps never navigate. Drive the actual registered camera-pan callbacks over a contacted body: pose changes while ring_seq, modified_at and dataRevision remain byte-identical and no rank DAO call occurs, including unsuccessful end/finalize. Keep this regression through the widened All/Not and filtered Systems in plans03/12.</behavior>
<action>Read the full dossier and appendix per D-01/D-02. Extract loadOrreryScene using existing local settings/sun/header/status/profile/contact reads, grouped under inReadSnapshot without nesting mutexes. Retain default contacted-only listOrbitingContacts for this tracer. Introduce a worklet-safe finite sun-centered CameraPose and invertible project/unproject functions; one shared ProjectedFrame owns current centers, hit bounds and stable identities. Wire OrreryWorld into measured/focus/AppState-gated OrreryScreen, remove its mode toggle and endpoint hit refs per D-04, and add real one-finger pan. Addresses review concern (HIGH): in this same task retire the existing radial-reorder Gesture.Pan registration, commitRingSeq/commitRef/commitFromWorklet bridge, bodiesShared/dragMetrics mirroring, activeDragId/dragRadius/draggedPush state and ghostRingRadius/ghostRingOpacity preview from OrreryScreen. Remove imports/constants used only by that path, including its rank-writer call; preserve shared ring-seq DAO guards and reusable reorder logic for plan29-09. Do not register both ordinary-drag recognizers. Reordering becomes reachable again through plan29-09's prolonged stationary hold; all twelve waves are one unreleased implementation sequence and the final phase must deliver reorder before completion. A successful unambiguous touch focuses to name-visible inspection; at that scale it opens the existing Profile route after generation revalidation. Multiple candidates produce a group intent without arbitrary selection. Keep all currently loaded contacts visible/reachable. Existing body resources may use keyed projected wrappers with canonical zero morph until task 2 removes that obsolete prop. Photo/font hooks remain keyed and unconditional. Read/publish failure states stay explicit, never catch into an empty array. Bridge discrete IDs/intents from UI thread only; do not mirror frames to React state or JS refs. The test mocks only native drawing/navigation adapters and calls production loader/math/dispatch; it proves the architectural chain before expansions.</action>
<acceptance_criteria>Reachable screen reads real SQLite through loadOrreryScene and renders/interacts through one frame. The camera pan is the sole ordinary-drag owner; the legacy radial gesture, commit bridge and ghost preview are disconnected. Automated tracer passes before expansion begins; native tracer remains a separately tracked end-of-phase obligation when unavailable.</acceptance_criteria>
<verify><automated>test -f src/services/orrery-scene.test.ts && npm test -- src/services/orrery-scene.test.ts</automated><human-check>Before expansion, inspect production route wiring and pass the automated tracer. Schedule the native tracer smoke within the existing end-of-phase human verification when the device session is available: open Orrery with one real contacted Bound person, pan, tap its visibly moved body to focus, then tap the inspected body to open that person's Profile. Record native verification pending if unavailable; the Node adapter test alone does not prove Skia or navigation reachability. Confirm package and Metro session with the owner only before actual device use.</human-check></verify>
<done>Reachable screen reads real SQLite through loadOrreryScene and renders/interacts through one frame. Automated tracer passes before expansion begins; pending native evidence does not become an automated pass or phase-completion claim.</done>
</task>

<task type="auto" tdd="true">
<name>29-01-02: Complete canonical status placement and retire the body mode contract</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrbitBody.tsx, src/components/orrery/OrreryWorld.tsx, src/logic/orrery-geometry-logic.ts, src/logic/orrery-geometry-logic.test.ts</files>
<read_first>src/components/orrery/OrbitBody.tsx, src/logic/orrery-geometry-logic.ts, src/logic/orrery-ring-logic.ts, docs/decisions/ADR-077-single-canonical-orrery-with-a-constrained-inspection-camera.md; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/components/orrery/OrreryWorld.tsx (read current contents if present); src/logic/orrery-geometry-logic.test.ts (read current contents if present)</read_first>
<behavior>Progress 0/0.5/1 preserves clockwise timestamp placement. Repeat refresh never starts ambient contact revolution. Rogue/decay styling and neutral sun remain unchanged.</behavior>
<action>Per D-04 remove relationship angle/color morph props, exclusively mode-specific geometry exports and their obsolete tests. Keep progressToAngle/polarToXY and generic angular utilities that camera recovery uses. OrbitBody consumes canonical style under its projected wrapper and does not calculate another location. Preserve rogue cold body fill, status rings, local photo fallback and all unsuperseded ADR-048 static behavior. Update comments to the resulting single-world contract.</action>
<acceptance_criteria>No production mode-morph consumer remains; canonical progress/ring/sun behavior passes its regressions.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-geometry-logic.test.ts && npm test -- orrery-geometry orrery-ring sun-occupant orrery-scene</automated></verify>
<done>No production mode-morph consumer remains; canonical progress/ring/sun behavior passes its regressions.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| SQLite → scene | Async identity and snapshot consistency. |
| gesture → navigation | Successful live targets only. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-01-01 | Tampering | projected frame | high | mitigate | Task 1 pins hit/draw identity and stale-intent rejection. |
| T-29-01-02 | Denial of service | camera math | medium | mitigate | Finite input validation and measurement gates. |
</threat_model>

<verification>
npm test -- src/services/orrery-scene.test.ts
npm test -- orrery-geometry orrery-ring sun-occupant orrery-scene
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
D-01/D-02/D-04: The Orrery has one unnamed timestamp-based health world and no Status/Relationship toggle.
A real contacted row read through the existing DAO is drawn and hit-tested from the same projected frame after pan; identity-level successful tap opens its Profile.
EDGE-01/02: Identical scene input gives identical placement; stale or cancelled read generations cannot replace newer members.
</success_criteria>

## Artifacts this phase produces

- src/services/orrery-scene.ts: loadOrreryScene and OrrerySceneSnapshot
- src/logic/orrery-camera-logic.ts: CameraPose, WorldPoint, ProjectedFrame, projectWorldPoint, unprojectToWorldPlane, projectFrame, collectHitCandidates
- src/components/orrery/OrreryWorld.tsx: OrreryWorld UI-thread scene adapter
- src/services/orrery-scene.test.ts: real-SQL read → projection → hit → focus/Profile tracer

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-01-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
