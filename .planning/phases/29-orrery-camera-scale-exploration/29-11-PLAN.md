---
phase: 29-orrery-camera-scale-exploration
plan: "11"
type: execute
wave: 11
depends_on:
  - "29-10"
files_modified:
  - "src/stores/orrery-session-store.ts"
  - "src/logic/orrery-session-logic.ts"
  - "src/logic/orrery-session-logic.test.ts"
  - "src/navigation/tabs/OrreryStack.tsx"
  - "src/screens/OrreryScreen.tsx"
  - "src/theme/use-reduced-motion.ts"
  - "src/theme/use-reduced-motion.test.ts"
  - "src/components/orrery/use-orrery-camera.ts"
  - "src/components/orrery/OrreryCanvas.tsx"
  - "src/components/orrery/OrreryFeedback.tsx"
  - "src/components/orrery/orrery-feedback-logic.test.ts"
  - "src/stores/orrery-system-store.ts"
  - "src/components/orrery/OrreryWorld.tsx"
autonomous: true
requirements:
  - "ORRC-08"
  - "ORRC-11"
  - "ORRC-13"
  - "ORRC-16"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-07: Orrery→Profile→Back restores settled pose and valid single focus, not dismissed cluster; fresh launch or fresh tab visit starts Home. Background alone preserves session, and camera is never durable."
    - "D-08: Live Reduced Motion event outranks a later stale async seed; existing inertia is cancelled immediately; manual controls remain; blur/background unmounts clock canvas and cancels animation callbacks."
    - "Same-System membership changes retain bodies with smooth settle and entering/departing fade/scale; removed IDs stop being interactive immediately and switch transitions remain functional, not theatrical."
    - "E9: Named loading/error/retry copy is surface-specific; retry busy suppresses duplicate operation and can repeat after failure; errors preserve valid content/intent; messages wrap/scroll with reachable recovery and contain no stack trace or network setup prompt."
    - "EDGE-20 (ORRC-08/unclassified): ASSUMPTION: ORRC-08 was unclassified; no extra durable camera state is inferred. Profile Back restores the live session; fresh visit resets and background alone does not mean fresh visit."
    - "EDGE-37 (ORRC-16/unclassified): ASSUMPTION: ORRC-16 was unclassified; no new motion exception is inferred. Live Reduced Motion cancels existing inertia and stale seed publication while manual control remains."
    - "UI-E9 Feedback; all 4 considerations (loading, error, overflow, long-text): Use the named copy rows for each affected surface. Loading is distinct from empty and failure. Retry is busy while its operation is pending and can be attempted again after failure. Errors preserve valid local content and view intent where possible. Messages wrap in a bounded scrollable surface if needed; recovery remains reachable at large font sizes. No technical stack trace or network setup prompt appears."
  artifacts:
    - path: "src/stores/orrery-session-store.ts"
      provides: "session-only settled pose/focus and departure reason"
    - path: "src/logic/orrery-session-logic.ts"
      provides: "Profile return vs fresh-visit/background state transitions"
    - path: "src/components/orrery/OrreryFeedback.tsx"
      provides: "shared exact-copy loading/error/empty/retry rendering"
    - path: "src/theme/use-reduced-motion.ts"
      provides: "live-event-before-seed ordering guard"
  key_links:
    - from: "src/navigation/tabs/OrreryStack.tsx"
      to: "src/stores/orrery-session-store.ts"
      via: "explicit origin/departure/return state"
    - from: "src/theme/use-reduced-motion.ts"
      to: "src/components/orrery/use-orrery-camera.ts"
      via: "live shared signal cancels existing continuation"
---

<objective>
Restore Profile-return context and make motion/error lifecycles race safe.
Purpose: Keep camera transient while preserving meaningful navigation return and truthful failure recovery.
Output: session-only settled pose/focus and departure reason; Profile return vs fresh-visit/background state transitions; shared exact-copy loading/error/empty/retry rendering; live-event-before-seed ordering guard.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-11-01: Restore only genuine Profile Back camera/focus sessions</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/stores/orrery-session-store.ts, src/logic/orrery-session-logic.ts, src/logic/orrery-session-logic.test.ts, src/navigation/tabs/OrreryStack.tsx, src/screens/OrreryScreen.tsx</files>
<read_first>src/navigation/RootNavigator.tsx, src/navigation/tabs/OrreryStack.tsx, src/navigation/types.ts, src/stores/dashboard-session-store.ts, src/components/orrery/use-orrery-camera.ts, src/stores/orrery-session-store.ts (read current contents if present; new paths are created by this task), src/logic/orrery-session-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-session-logic.test.ts (read current contents if present; new paths are created by this task), src/navigation/tabs/OrreryStack.tsx (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Direct Profile Back restores pose/current single focus; cluster was closed before navigation and stays closed. Tab leave/reentry and fresh launch use Home. Background/foreground without route departure preserves pose. Removed/merged focused ID clears after reload; restored bounds clamp to changed measurement.</behavior>
<action>Per D-07 add explicit session departure reasons and route event integration, not useIsFocused-only inference. Capture settled pose/focus through a discrete UI→JS event when navigating to Profile or becoming inactive; no per-frame store writes. Orrery stack/navigation state distinguishes same-session Profile pop from new tab visit, other navigation and launch. On return reload/revalidate active System before restoring eligible single focus and clamped pose; preserve framed camera but do not resurrect dismissed cluster. A normal background/foreground cycle keeps the session. Fresh visit resets all axes to current Home after valid measure. Clear disposed callback generations; keep store memory-only with no persistence middleware, route serialization of pose, settings columns or backup keys.</action>
<acceptance_criteria>Profile return restores meaningful current context while fresh visits start Home and backgrounding alone does not reset it.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-session-logic.test.ts && npm test -- src/logic/orrery-session-logic.test.ts</automated></verify>
<done>Profile return restores meaningful current context while fresh visits start Home and backgrounding alone does not reset it.</done>
</task>

<task type="auto" tdd="true">
<name>29-11-02: Honor live Reduced Motion and dispose all inactive animation</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/theme/use-reduced-motion.ts, src/theme/use-reduced-motion.test.ts, src/components/orrery/use-orrery-camera.ts, src/components/orrery/OrreryCanvas.tsx, src/logic/orrery-session-logic.test.ts</files>
<read_first>src/components/orrery/SunBody.tsx, src/theme/use-reduced-motion.ts, src/components/orrery/use-orrery-camera.ts, docs/decisions/ADR-085-live-reduced-motion-signal-for-skia-ambient-animation.md, src/theme/use-reduced-motion.ts (read current contents if present; new paths are created by this task), src/theme/use-reduced-motion.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryCanvas.tsx (read current contents if present; new paths are created by this task), src/logic/orrery-session-logic.test.ts (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Emit live true before async false seed resolves: final state stays true. Multiple live changes win, disposed seed emits nothing, repeated cleanup is harmless. Toggle mid-inertia/recovery cancels continuation while manual pose updates remain active; blur/background removes clock consumer.</behavior>
<action>Per D-08 fix createReducedMotionController with an event-generation/seed guard so stale seed cannot overwrite a live event; retain the existing shared-value/state-hook APIs and default OS inheritance. Make dispose idempotent. Camera shared reaction cancels running inertia/recovery before applying reduced-motion short transitions, with invalidated completion generation. Retain both OrreryCanvas twinkle and SunBody pulse live shared gates. Unmount the sole clock-owning canvas subtree on blur/background and explicitly cancel camera animations, pending hold/reorder and optional context callbacks, while preserving session pose for a background-only pause. Do not implement animation by React state or merely hide a running clock.</action>
<acceptance_criteria>Live OS changes cannot be undone by stale seed; inactive Orrery has no active clock/camera continuation and user-controlled movement remains available.</acceptance_criteria>
<verify><automated>test -f src/theme/use-reduced-motion.test.ts && test -f src/logic/orrery-session-logic.test.ts && npm test -- src/theme/use-reduced-motion.test.ts src/logic/orrery-session-logic.test.ts src/logic/orrery-recovery-logic.test.ts</automated></verify>
<done>Live OS changes cannot be undone by stale seed; inactive Orrery has no active clock/camera continuation and user-controlled movement remains available.</done>
</task>

<task type="auto" tdd="true">
<name>29-11-03: Finish continuity and surface-specific error recovery under races</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrreryFeedback.tsx, src/components/orrery/orrery-feedback-logic.test.ts, src/stores/orrery-system-store.ts, src/components/orrery/OrreryWorld.tsx, src/screens/OrreryScreen.tsx</files>
<read_first>src/stores/orrery-preferences-store.ts, src/components/orrery/OrreryContactsSheet.tsx, src/components/orrery/OrreryClusterPanel.tsx, src/db/orrery-satellites-read.ts, src/components/orrery/OrreryFeedback.tsx (read current contents if present; new paths are created by this task), src/components/orrery/orrery-feedback-logic.test.ts (read current contents if present; new paths are created by this task), src/stores/orrery-system-store.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Initial empty vs read error vs sun-only distinct; same-System refresh retains content with stale notice; switch failure has no old-members/new-label mix. Retry double tap invokes once, can repeat after rejection; preference/reorder/satellite failures preserve other content. Membership delta during camera recovery uses current shared frame.</behavior>
<action>Centralize exact UI-SPEC empty/loading/failure/retry messages and action routing in OrreryFeedback, using named per-surface states instead of a generic empty catch. Preserve successful same-System snapshot on refresh failure; failed destination switch must not reveal old rows as new members. Keep save failure's retryable intent, reorder committed layout, satellite parent identity and removed-focus notice. Retry is busy only for its operation and resets after success/failure; suppress duplicate requests. Finish retained/entering/departing membership transitions with a single interpolated world frame, continuity fade/shrink/settle and direct restrained Reduced Motion fallback. Interrupt transition into new snapshot without endpoint snapping or stale targets. Bound wrapping/scrollable feedback surface so recovery remains reachable at large text.</action>
<acceptance_criteria>Every approved failure/empty/loading case has truthful reachable recovery and membership continuity shares the current-frame rendering/hit authority.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-feedback-logic.test.ts && npm test -- src/components/orrery/orrery-feedback-logic.test.ts src/stores/orrery-system-store.test.ts src/stores/orrery-preferences-store.test.ts src/logic/orrery-frame.test.ts</automated></verify>
<done>Every approved failure/empty/loading case has truthful reachable recovery and membership continuity shares the current-frame rendering/hit authority.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| route/AppState → ephemeral camera | Departure reason must not leak durable pose. |
| async event/seed → animation | Latest live OS signal is authoritative. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-11-01 | Tampering | stale session/async callback | high | mitigate | Generation guards, current membership revalidation and reason-specific lifecycle tests. |
| T-29-11-02 | Denial of service | unfocused render loop | medium | mitigate | Unmount clock subtree and cancel every in-flight controller animation on inactivity. |
</threat_model>

<verification>
npm test -- src/logic/orrery-session-logic.test.ts
npm test -- src/theme/use-reduced-motion.test.ts src/logic/orrery-session-logic.test.ts src/logic/orrery-recovery-logic.test.ts
npm test -- src/components/orrery/orrery-feedback-logic.test.ts src/stores/orrery-system-store.test.ts src/stores/orrery-preferences-store.test.ts src/logic/orrery-frame.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
D-07: Orrery→Profile→Back restores settled pose and valid single focus, not dismissed cluster; fresh launch or fresh tab visit starts Home. Background alone preserves session, and camera is never durable.
D-08: Live Reduced Motion event outranks a later stale async seed; existing inertia is cancelled immediately; manual controls remain; blur/background unmounts clock canvas and cancels animation callbacks.
Same-System membership changes retain bodies with smooth settle and entering/departing fade/scale; removed IDs stop being interactive immediately and switch transitions remain functional, not theatrical.
E9: Named loading/error/retry copy is surface-specific; retry busy suppresses duplicate operation and can repeat after failure; errors preserve valid content/intent; messages wrap/scroll with reachable recovery and contain no stack trace or network setup prompt.
</success_criteria>

## Artifacts this phase produces

- src/stores/orrery-session-store.ts: session-only settled pose/focus and departure reason
- src/logic/orrery-session-logic.ts: Profile return vs fresh-visit/background state transitions
- src/components/orrery/OrreryFeedback.tsx: shared exact-copy loading/error/empty/retry rendering
- src/theme/use-reduced-motion.ts: live-event-before-seed ordering guard

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-11-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
