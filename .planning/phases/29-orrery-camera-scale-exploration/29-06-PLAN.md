---
phase: 29-orrery-camera-scale-exploration
plan: "06"
type: execute
wave: 6
depends_on:
  - "29-05"
files_modified:
  - "src/stores/shell-obstacle-store.ts"
  - "src/navigation/RootNavigator.tsx"
  - "src/components/UniversalFab.tsx"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/components/orrery/orrery-obstacle-logic.test.ts"
  - "src/components/orrery/OrreryControls.tsx"
  - "src/components/orrery/orrery-obstacle-logic.ts"
  - "src/components/icons/icon-registry.ts"
  - "src/screens/OrreryScreen.tsx"
autonomous: true
requirements:
  - "ORRC-04"
  - "ORRC-09"
  - "ORRC-13"
  - "ORRC-15"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Shell tab/FAB rectangles and canvas window origin are measured and translated into one coordinate space; layout changes recompute usable bounds without relocating shell capture."
    - "E4: Contacts opens the companion even empty/loading; Recenter is disabled until valid measured bounds, has no persistence spinner and remains available after read failures; controls stay clear of shell bounds with full accessible names and wrapping labels."
    - "E8: Polaris is theme-drawn and meaningful with zero contacts; no remote asset load/error exists; action waits for valid measurement; world marker moves with yaw and accessible Reset north remains reachable at edges without pinning the star to HUD."
    - "UI-E4 Contacts/Recenter; all 4 considerations (loading, error, overflow, long-text): Contacts opens the sheet even for an empty/loading System so that state is readable there. Recenter requires valid measured camera bounds; before those exist it is disabled with accessible state. Direct camera action has no network/save spinner. A failed member load does not disable navigation recovery; show the affected System's error. Keep target bounds above shell controls; accessible names stay complete and any visible labels wrap."
    - "UI-E8 Polaris; all 6 considerations (empty, loading, error, populated, overflow, long-text): Generate the landmark from theme-resolved drawing primitives, with no remote image loading/error path. It remains meaningful with zero contacts. Until camera measurement is ready, disable its action and expose orientation only when valid. At valid bounds use the restrained starburst and canonical yaw value. At viewport edges preserve orientation access through an accessible Reset north action; do not pin the star into fixed HUD chrome. Orientation text/accessibility label remains complete."
  artifacts:
    - path: "src/stores/shell-obstacle-store.ts"
      provides: "measured window-space rect registry with remove/update lifecycle"
    - path: "src/components/orrery/OrreryControls.tsx"
      provides: "separate Contacts/Recenter actions and accessible Reset north"
    - path: "src/components/orrery/orrery-obstacle-logic.ts"
      provides: "translate/intersect obstacles and derive usable framing region"
  key_links:
    - from: "src/components/UniversalFab.tsx"
      to: "src/stores/shell-obstacle-store.ts"
      via: "measureInWindow registration/removal"
    - from: "src/components/orrery/OrreryWorld.tsx"
      to: "src/logic/orrery-camera-logic.ts"
      via: "canvas-local obstacle-aware Home/framing"
---

<objective>
Measure shell obstacles and place reachable Orrery controls around them.
Purpose: Ensure Home/focus/recovery and accessible controls use the actual unobscured canvas.
Output: measured window-space rect registry with remove/update lifecycle; separate Contacts/Recenter actions and accessible Reset north; translate/intersect obstacles and derive usable framing region.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-06-01: Frame the existing world against measured tab and capture obstacles</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/stores/shell-obstacle-store.ts, src/navigation/RootNavigator.tsx, src/components/UniversalFab.tsx, src/components/orrery/OrreryWorld.tsx, src/components/orrery/orrery-obstacle-logic.test.ts</files>
<read_first>src/navigation/RootNavigator.tsx, src/components/UniversalFab.tsx, src/stores/tab-bar-layout-store.ts, src/navigation/use-bottom-clearance.ts, src/components/ShellAppBar.tsx, src/stores/shell-obstacle-store.ts (read current contents if present; new paths are created by this task), src/navigation/RootNavigator.tsx (read current contents if present; new paths are created by this task), src/components/UniversalFab.tsx (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task), src/components/orrery/orrery-obstacle-logic.test.ts (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Window rects translate by actual canvas origin; tab/FAB relayout, hiding, unmount and zero-measurement leave no stale obstruction. Home and focused bodies do not frame underneath measured obstacles.</behavior>
<action>Introduce a small stable keyed window-space obstacle registry. Extend measured tab wrapper and existing FAB ref to publish measureInWindow bounds on layout and relevant visibility/layout changes; remove on hide/unmount. Measure the canvas window origin and bounds in OrreryWorld. Feed translated/intersected obstacles into existing Home/frameBodies immediately; ShellAppBar is excluded by actual canvas origin/size rather than another hardcoded top offset. Keep updates discrete on layout, never animation-frame React state. Preserve universal capture routes, existing FAB position, tab height consumers and Back behavior. Add tests of registry lifecycle and coordinate conversions using the actual helpers consumed by world.</action>
<acceptance_criteria>Reachable Home camera and existing controls respond to measured shell geometry; no stale/hardcoded duplicate shell clearance remains in the new camera path.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-obstacle-logic.test.ts && npm test -- src/components/orrery/orrery-obstacle-logic.test.ts</automated></verify>
<done>Reachable Home camera and existing controls respond to measured shell geometry; no stale/hardcoded duplicate shell clearance remains in the new camera path.</done>
</task>

<task type="auto" tdd="true">
<name>29-06-02: Place separate Contacts and Recenter controls and accessible north recovery</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrreryControls.tsx, src/components/orrery/orrery-obstacle-logic.ts, src/components/orrery/orrery-obstacle-logic.test.ts, src/components/icons/icon-registry.ts, src/screens/OrreryScreen.tsx</files>
<read_first>src/components/icons/icon-registry.ts, src/components/ui/Button.tsx, src/stores/shell-transient-store.ts, src/components/orrery/OrreryControls.tsx (read current contents if present; new paths are created by this task), src/components/orrery/orrery-obstacle-logic.ts (read current contents if present; new paths are created by this task), src/components/orrery/orrery-obstacle-logic.test.ts (read current contents if present; new paths are created by this task), src/components/icons/icon-registry.ts (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Controls have at least44 logical-unit targets, Contacts above Recenter with8 gap, clear of FAB/tabs. Invalid camera bounds disable Recenter/Reset north only; contact read failure does not remove recovery. All labels remain complete under scaling.</behavior>
<action>Build the approved bottom-right vertical stack with Contacts in this System above Recenter, actual measured exclusions, 16 edge inset and8 gap. Centralize any new semantic icon keys in registry, use existing tokens/AppText/Button/GlassSurface. Expose callbacks from scene/camera for all-axis Recenter and yaw-only Reset north; actual animated controller lands next plan. Contacts opens a reachable detail Sheet containing current shared System state and conventional basic member identity/actions; the full row/accessibility expansion is plan8, so this action must work now. Pass relevant panel rectangles back to world framing. Publish orientation accessibility text only when valid; expose Reset north conventional action even if in-world Polaris is clipped. Keep top System/View panels as their own transients, prevent covered hidden controls accepting input, and restore trigger focus on dismissal.</action>
<acceptance_criteria>Separate reachable controls use measured available geometry and meaningful disabled states; Contacts opens the current System's detail surface.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-obstacle-logic.test.ts && npm test -- orrery-obstacle-logic orrery-controls-logic</automated></verify>
<done>Separate reachable controls use measured available geometry and meaningful disabled states; Contacts opens the current System's detail surface.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| shell/window layout → camera | Coordinate conversion must prevent obscured actions. |
| overlay → shell navigation | Transient ownership and focus restoration. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-06-01 | Denial of service | invalid obstacles | medium | mitigate | Reject nonfinite/zero bounds; preserve last valid frame and disable unavailable actions. |
| T-29-06-02 | Tampering | overlay input ownership | medium | mitigate | Modal blocks world, nonmodal cluster preserves visible canvas and reachable controls. |
</threat_model>

<verification>
npm test -- src/components/orrery/orrery-obstacle-logic.test.ts
npm test -- orrery-obstacle-logic orrery-controls-logic
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
Shell tab/FAB rectangles and canvas window origin are measured and translated into one coordinate space; layout changes recompute usable bounds without relocating shell capture.
E4: Contacts opens the companion even empty/loading; Recenter is disabled until valid measured bounds, has no persistence spinner and remains available after read failures; controls stay clear of shell bounds with full accessible names and wrapping labels.
E8: Polaris is theme-drawn and meaningful with zero contacts; no remote asset load/error exists; action waits for valid measurement; world marker moves with yaw and accessible Reset north remains reachable at edges without pinning the star to HUD.
</success_criteria>

## Artifacts this phase produces

- src/stores/shell-obstacle-store.ts: measured window-space rect registry with remove/update lifecycle
- src/components/orrery/OrreryControls.tsx: separate Contacts/Recenter actions and accessible Reset north
- src/components/orrery/orrery-obstacle-logic.ts: translate/intersect obstacles and derive usable framing region

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-06-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
