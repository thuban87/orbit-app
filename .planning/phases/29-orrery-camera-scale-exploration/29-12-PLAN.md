---
phase: 29-orrery-camera-scale-exploration
plan: "12"
type: execute
wave: 12
depends_on:
  - "29-11"
files_modified:
  - "src/screens/OrreryScreen.tsx"
  - "src/components/orrery/OrbitBody.tsx"
  - "docs/decisions/adr-registry.ts"
  - "src/services/orrery-exploration.integration.test.ts"
  - "src/services/orrery-scene.test.ts"
  - "docs/systems/orrery.md"
  - ".planning/phases/29-orrery-camera-scale-exploration/29-NATIVE-CHECKLIST.md"
autonomous: true
requirements:
  - "ORRC-01"
  - "ORRC-02"
  - "ORRC-03"
  - "ORRC-04"
  - "ORRC-05"
  - "ORRC-06"
  - "ORRC-07"
  - "ORRC-08"
  - "ORRC-09"
  - "ORRC-10"
  - "ORRC-11"
  - "ORRC-12"
  - "ORRC-13"
  - "ORRC-14"
  - "ORRC-15"
  - "ORRC-16"
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  prohibitions:
    - statement: "MUST NOT frame Gravity as a persons worth or present never-contacted people as decaying."
      status: resolved
      verification: judgment
      reason: "Approved neutral-health and derived-Gravity framing requires human review; no mechanical descriptor is claimed."
    - statement: "MUST NOT turn relationship satellites into independent contacts or imply contact health, logging, or Profile semantics."
      status: resolved
      verification: judgment
      reason: "Review satellite copy and actions against D-09; automated typed-action tests support but do not replace presentation review."
  truths:
    - "All16 requirements, D-01..D-11, 55 approved UI-state considerations and all explicit edge predicates have linked implementation tests or honest native human-check evidence."
    - "Automated integration preserves local-only reads, current backup format/emission, contacted-only guarded reorder and derived health/Gravity across lifecycle changes."
    - "D-10: Native visual/a11y/gesture verification is recorded separately; no desktop-emulator performance claim is made and release-hardening owns final density/neighbor/large-System calibration."
  artifacts:
    - path: "src/services/orrery-exploration.integration.test.ts"
      provides: "production real-SQL scene/actions/session regression matrix"
    - path: "docs/systems/orrery.md"
      provides: "updated canonical world/camera/Systems/preferences/session architecture and Changelog"
    - path: ".planning/phases/29-orrery-camera-scale-exploration/29-NATIVE-CHECKLIST.md"
      provides: "all E1–E9 visual, accessibility, gesture and lifecycle checks with evidence fields"
  key_links:
    - from: "src/services/orrery-exploration.integration.test.ts"
      to: "src/services/orrery-scene.ts"
      via: "real production loader/read/controller/mutation integration"
    - from: "docs/systems/orrery.md"
      to: ".planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md"
      via: "verified implementation and phase handoff traceability"
---

<objective>
Verify integrated exploration and document its durable/session boundaries.
Purpose: Close cross-feature regressions and leave executable native acceptance checks without claiming unobserved results.
Output: production real-SQL scene/actions/session regression matrix; updated canonical world/camera/Systems/preferences/session architecture and Changelog; all E1–E9 visual, accessibility, gesture and lifecycle checks with evidence fields.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-12-01: Exercise complete exploration and data-lifecycle workflows against real SQLite</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/services/orrery-exploration.integration.test.ts, src/services/orrery-scene.test.ts</files>
<read_first>src/db/__testkit__/sqlite-local-day.ts, src/db/orrery-action-read.ts, src/db/orrery-impact-read.ts, src/db/orrery-system-read.ts, src/db/orrery-satellites-read.ts, src/services/orrery-scene.ts, src/db/ring-seq-dao.ts, src/db/favourites-dao.ts, src/db/snooze-dao.ts, src/db/bulk-actions-dao.ts, src/db/recency-dao.ts, src/logic/orrery-focus-logic.ts, src/db/app-settings-dao.ts, src/db/merge-dao.ts, src/db/purge-dao.ts, src/backup/restore-apply.ts, src/stores/orrery-system-store.ts, src/logic/orrery-session-logic.ts, src/services/orrery-exploration.integration.test.ts (read current contents if present; new paths are created by this task), src/services/orrery-scene.test.ts (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>All/Not and filtered systems preserve boundaries before/after first contact, archive/unbind/merge/purge/restore; qualifying contact sun appears once in members. A current-frame member action needs live System membership; a contact-sun action needs current globally resolved sun ID/UID even when Favorites/category excludes it. Filtered reorder rolls back on concurrent sun/order or eligible-member changes, including unchanged complete order/sun, local-day transition without revision and reused numeric contact ID with a different UID. Preferences survive restore omission and camera never appears in export.</behavior>
<action>Build non-vacuous integration fixtures through production migration chain/DAO writers and run actual scene, frame, action, settings and rank functions. Mock native adapters only, not the population/transaction/controller behavior being asserted. Include Unicode/duplicate display names, zero/one/sun-only/many, missing optional photo/context, all System predicates and Category UID rename/removal, satellite hidden/link/delete/restore/merge changes, A→B→A reads, Profile Back versus fresh/background, retry failures, interrupted recovery and no-op/cancel reorder. Add production action-path cases for a global sun excluded by Favorites/category: isolated focus, identity-level Profile, ambiguous group including that sun once, current-sun change rejecting the stale action, and companion exclusion. Per D-11 also exercise satellites On at inspection with excluded global sun: no moon, relationship context or stale satellite action, sun contact actions remain valid; requalification restores eligibility exactly once under existing On/semantic rules. Assert production pan callbacks never dispatch rank writes and batch/probe query counts remain bounded as the scene grows. Addresses cycle2 contention concern: use the real buildExportManifest with an injected deferred readPhotoBase64 to hold its existing snapshot, then queue the production scene loader and target action probe on the shared mutex. Assert neither overtakes export; cancel their request/action generations while queued, release the photo read in finally, and assert no stale publication, preference persistence, focus or Profile navigation. Repeat with a current uncancelled action to prove fresh validation and one dispatch after release. Separately pause an instrumented scene ro read inside its snapshot, queue a production recency write, prove that writer remains pending until the coherent read releases, then assert it commits and a subsequent scene reflects it. Use explicit deferred barriers, not elapsed-time sleeps or latency assertions. Keep snapshot/mutex code unchanged and do not mock the transaction owner. This proves ordering/cancellation, not native responsiveness. Add rank-write cases for changed filtered membership with unchanged complete order/sun, a queued write observing newer membership after mutex acquisition, clock-only local-midnight invalidation without dataRevision, and reused numeric ID with a different UID. Use plan29-09's newly owned openSqliteLocalDayFixture test helper: override only the fixture connection's exact date('now','localtime') call, delegate every other date call to its separate unmodified native SQLite connection, and close both in finally. Capture the production request at dayD, queue the rank DAO behind a deferred shared-mutex blocker, advance only setLocalDay toD+1, release the blocker, and assert lock-time membership rejection with complete order/sun/UIDs and database rows/revision unchanged. Keep unchanged-day success and unmodified real-SQLite-clock predicate parity controls. Do not inject a caller day into production APIs or alter production status SQL. Assert stored recency/rank/sun/settings independently after operations, not merely returned UI shape. Run the full suite and static checks once targeted behavior passes; record preexisting unrelated failures separately without representing them as phase passes.</action>
<acceptance_criteria>Real production integration covers the critical data/render/action/session seams with every asserted fixture exercised, including real export→scene/action queueing, cancellation after enqueue, and scene→recency-write contention; required broader checks are recorded.</acceptance_criteria>
<verify><automated>test -f src/services/orrery-exploration.integration.test.ts && test -f src/services/orrery-scene.test.ts && npm test -- src/services/orrery-exploration.integration.test.ts src/services/orrery-scene.test.ts</automated></verify>
<done>Real production integration covers the critical data/render/action/session seams with every asserted fixture exercised; required broader checks are recorded.</done>
</task>

<task type="auto">
<name>29-12-02: Record native acceptance and the verified Orrery system handoff</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>docs/systems/orrery.md, .planning/phases/29-orrery-camera-scale-exploration/29-NATIVE-CHECKLIST.md, src/screens/OrreryScreen.tsx, src/components/orrery/OrbitBody.tsx, docs/decisions/adr-registry.ts</files>
<read_first>src/screens/OrreryScreen.tsx, src/components/orrery/OrbitBody.tsx, docs/decisions/adr-registry.ts, scripts/gen-adr-registry.ts, docs/decisions/ADR-077-single-canonical-orrery-with-a-constrained-inspection-camera.md, docs/systems/orrery.md, .planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md, .planning/phases/29-orrery-camera-scale-exploration/29-VALIDATION.md, docs/systems/orrery.md (read current contents if present; new paths are created by this task), .planning/phases/29-orrery-camera-scale-exploration/29-NATIVE-CHECKLIST.md (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Documentation distinguishes implemented/tested/native-pending behavior and contains all9 UI surfaces with55 state checks, camera/motion/session and gesture interactions, plus global nonmember-sun actions and stale filtered-reorder recovery.</behavior>
<action>Update the living Orrery system doc from actual completed code with Changelog: canonical view supersession, world/projected-frame ownership, bounded camera, native sibling depth, System/member/sun rules, app_settings preference columns/validators, optional portable keys without current emission, ephemeral return session, guard-preserving filtered rank writes, relationships-only moons, and Reduced Motion/clock shutdown. Addresses review concern: update the actual code-comment ADR tokens in OrreryScreen.tsx and OrbitBody.tsx to cite ADR-077 for the single canonical world and preserve ADR-048 citations only for still-live static/status/keyed-render behavior, explicitly describing its partial supersession. Do not claim the retired dual-mode/morph still governs current rendering. Run npm run gen:adr-registry and inspect its generated diff; never edit registry entries manually or mutate immutable ADR bodies. An unchanged generated registry is valid when ADR source facts are already current. No graph rebuild is needed for these planning edits; any later required graph refresh must use only npm run graph:build and obey its generated-file ownership. Keep source-citation edits comment-only; verify that no executable tokens change. Document the actual new symbols/ownership: readOrreryImpactInputsCore inside scene snapshot, buildOrrerySystemWhere shared by members/action probe, readOrreryContactTargetValidation on focus/Profile, and the test-only local-day fixture separately from production SQLite authority. Addresses cycle2 ADR-027 traceability: in docs/systems/orrery.md add a clearly named KB extraction handoff for the precise partial supersession. ADR-027's profile-only presentation and rejected Orrery encoding clauses are superseded in this consumer by the owner-approved canonical phase08 dossier §E (Gravity as Visual Mass), §Z (accessible companion Gravity context), and ORRC-03/15; cite those exact sources. Derived-never-stored Gravity, full-history/floor/rarely-responds scope, no displayed raw score or human-worth framing, and all intensity/cadence policies remain live. Cite ADR-093 only as corroborating prior Dashboard Gravity use, not as the authority for Orrery. Formalize this already-authorized partial reversal through the required later extract-phase-kb workflow, which allocates the new ADR identity; do not guess an ADR number, change existing immutable ADR bodies, or claim gen:adr-registry alone records a supersession absent from ADR source. Carry the handoff and exact authority into29-12-SUMMARY.md so milestone-close extraction sees it; until then document that the graph may still report ADR-027 Accepted without the display exception. Also note in the living doc that ADR-077's historical SegmentedControl-only-consumer claim is stale: HomeScreen now uses the shared Dashboard view control, so retain that component. Creation-task-owned citations in camera/system/impact/world/action files must explain their actual governing constraints; verify them from the completed source without adding those files to this final task's modification ownership. Keep ADRs immutable. Create native checklist covering every approved E1–E9 treatment, long/Unicode/scaled text, TalkBack/switch navigation and trigger focus, modal companion vs nonmodal cluster, native stationary-hold arbitration/haptics, midpoint hits/depth, Polaris/recenter, background and live motion toggle. Include native Favorites/category exclusion of the global contact sun: focus, Profile and Back, mixed sun/member ambiguity, and no extra companion row; per D-11, satellites On must still hide that nonmember sun's moons/context, then requalification restores eligible moons without changing sun actions. Include stale filtered-hold rejection/reload and restored committed layout when membership changes before release. Add the native scenario "Tap a contact during automatic backup": using normal enabled automatic-backup behavior and a disposable representative photo/history fixture, observe tap-to-focus and identity-level tap-to-Profile completion while backup runs; repeat with Clear focus/Recenter, System switch and background before validation settles to check no delayed action after cancellation. Record whether overlap with the backup snapshot was actually observed; if not, retain the contention case pending rather than inferring success. Also observe a scene refresh concurrent with a normal Quick Log/write and record completion, retained loading/refresh treatment and any visible wait. Record contact/history/photo scale, target type, observable backup/scene/write timing and evidence limits without adding telemetry or a new production diagnostic surface. Do not equate wall-clock tap delay with JS/Skia frame time; physical-phone measurements only substantiate performance. Retain FIFO ordering, snapshot coherence and required fresh probes regardless of observed delay. Per D-10 carry measured long-history/photo-library contention and optimization to Phase40 with evidence or explicit unmeasured status; no timeout, priority, busy UI or control bypass is authorized here. Mark native checks pending until observed. Automated tracer success permits sequential implementation expansion, but does not complete native verification. The existing human_verify_mode=end-of-phase workflow must carry unresolved native items as pending UAT and cannot report phase verification complete until its native obligations are resolved under that workflow; this adds no new approval checkpoint or device requirement during automated plan execution. Before any actual device use confirm app package and Metro session with owner per AGENTS.md, discover target/serial with approved tooling, and make performance claims only on physical phone. Do not run device actions as part of documentation. Record Phase30/36/37/40 seams without implementing their deferred scope.</action>
<acceptance_criteria>Living system doc and29-12-SUMMARY.md carry the precise ADR-027 partial-supersession authority and required future KB extraction, retaining derivation/raw-score/intensity policies and immutable ADR bodies. All five new authoritative source files have their creation-task-owned governing comments. The doc records the SegmentedControl consumer correction and native checklist contains automatic-backup tap/cancellation plus scene/write contention with observed-or-pending evidence. No unobserved native/performance result is marked passing.</acceptance_criteria>
<verify><automated>npm run gen:adr-registry && npm test -- src/services/orrery-exploration.integration.test.ts</automated><human-check>Run the complete native checklist at end-of-phase; owner package/session confirmation is a prerequisite only to actual device use. Retain pending items explicitly until evidence exists.</human-check></verify>
<done>Living system doc matches actual code and native checklist is concrete with honest evidence status; no unobserved native/performance result is marked passing.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| automated proof → native claims | Separate evidence types. |
| render/reorder/preferences → existing domain | Cross-feature regressions retain governing decisions. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-12-01 | Tampering | cross-feature stale mutation | high | mitigate | Real SQL regression under active sun/merge/archive/restore and cancelled gestures. |
| T-29-12-02 | Information disclosure | local-only/portable boundary | high | mitigate | No network invocation and export omission integration checks. |
</threat_model>

<verification>
npm test -- src/services/orrery-exploration.integration.test.ts src/services/orrery-scene.test.ts
npm test -- src/services/orrery-exploration.integration.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
All16 requirements, D-01..D-11, 55 approved UI-state considerations and all explicit edge predicates have linked implementation tests or honest native human-check evidence.
Automated integration preserves local-only reads, current backup format/emission, contacted-only guarded reorder and derived health/Gravity across lifecycle changes.
D-10: Native visual/a11y/gesture verification is recorded separately; no desktop-emulator performance claim is made and release-hardening owns final density/neighbor/large-System calibration.
</success_criteria>

## Artifacts this phase produces

- src/services/orrery-exploration.integration.test.ts: production real-SQL scene/actions/session regression matrix
- docs/systems/orrery.md: updated canonical world/camera/Systems/preferences/session architecture and Changelog
- .planning/phases/29-orrery-camera-scale-exploration/29-NATIVE-CHECKLIST.md: all E1–E9 visual, accessibility, gesture and lifecycle checks with evidence fields

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-12-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures. Include a KB extraction handoff requiring later formal ADR-027 partial supersession from the owner-approved dossier §E/§Z and ORRC-03/15, preserving its remaining policies, plus the Phase40 contention measurement/optimization handoff with observed or unmeasured status.
</output>
