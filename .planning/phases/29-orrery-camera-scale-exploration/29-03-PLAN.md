---
phase: 29-orrery-camera-scale-exploration
plan: "03"
type: execute
wave: 3
depends_on:
  - "29-02"
files_modified:
  - "src/logic/orrery-system-logic.ts"
  - "src/db/orrery-system-read.ts"
  - "src/db/orrery-system-read.test.ts"
  - "src/services/orrery-scene.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/stores/orrery-system-store.ts"
  - "src/stores/orrery-system-store.test.ts"
  - "src/screens/OrreryScreen.tsx"
  - "src/components/orrery/OrrerySystemSelector.tsx"
  - "src/components/orrery/orrery-controls-logic.ts"
  - "src/components/orrery/orrery-controls-logic.test.ts"
autonomous: true
requirements:
  - "ORRC-11"
  - "ORRC-12"
  - "ORRC-15"
estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-05/D-10: Only explicit All Contacts/Not Contacted widen the default never-contacted exclusion; all Systems exclude archived and Unbound contacts and Category identities use UID."
    - "A qualifying contact sun appears once in the companion member set and never twice as an orbiting body; a nonqualifying configured sun remains governed by existing sun presentation without becoming a System member."
    - "E2: Built-ins remain selectable even empty; zero Categories omits only Category entries; destination loading is busy without relabelling old bodies; failure uses Reload System without automatic switch; successful selection persists; lists scroll, trigger ellipsizes with full accessible name and rows wrap."
    - "EDGE-23/24/25/26/27/28: Duplicate names do not merge identities; no members and sun-only have explicit results; System rows have stable builtin then category display_order/UID ordering; null progress/status stay null and neutral members retain deterministic dense read order."
    - "EDGE-02 (ORRC-01/concurrency): A stale or cancelled scene/read generation cannot replace a newer System snapshot, including A→B→A completion order."
    - "EDGE-23 (ORRC-11/adjacency): Duplicate Category names remain separate UID-identified Systems; overlapping memberships do not merge Systems."
    - "EDGE-24 (ORRC-11/empty): No Categories omits only Category rows; empty built-ins remain selectable and sun-only scene membership stays explicit."
    - "EDGE-25 (ORRC-11/ordering): Built-ins have canonical fixed ordering, Categories display_order then UID; out-of-order read completions cannot reorder selection."
    - "EDGE-26 (ORRC-12/adjacency): All Contacts/Not Contacted membership overlaps remain separate queries; null progress never equates to zero progress."
    - "EDGE-27 (ORRC-12/empty): Null last-contact rows exist only under explicit All/Not widening, with null progress/status and fixed neutral resting angle."
    - "EDGE-28 (ORRC-12/ordering): Neutral members have deterministic dense scene ordering without inventing durable rank eligibility."
    - "UI-E2 System selector; all 8 considerations (empty, loading, error, populated, partial, overflow, zero-one-many, long-text): Built-in choices remain listed even when empty; no Categories is valid and omits only Category entries. During System loading mark the selected destination busy and do not relabel old System bodies as new members. Failures use Reload System without an automatic switch. Current selection remains explicit; persist success truthfully. Scroll long dropdown lists; selected trigger truncates to one line with full accessible name, rows wrap. No zero-member System is disabled."
  artifacts:
    - path: "src/logic/orrery-system-logic.ts"
      provides: "OrrerySystemRef, SystemDescriptor, parseSystemRef and closed predicate resolver"
    - path: "src/db/orrery-system-read.ts"
      provides: "readOrrerySystemMembersCore plus coherent snapshot of members/orbiting/global sun, complete contacted order, ID/UID fingerprints and eligible visible IDs"
    - path: "src/stores/orrery-system-store.ts"
      provides: "generation-guarded request/status/snapshot state"
    - path: "src/components/orrery/OrrerySystemSelector.tsx"
      provides: "top-left built-in/category dropdown"
  key_links:
    - from: "src/services/orrery-scene.ts"
      to: "src/db/orrery-system-read.ts"
      via: "coherent read-only snapshot"
    - from: "src/components/orrery/OrrerySystemSelector.tsx"
      to: "src/stores/orrery-system-store.ts"
      via: "selected request vs successful snapshot identity"
---

<objective>
Switch live built-in and Category Systems with coherent membership.
Purpose: Make every approved System accessible without Dashboard state leakage or neutral-status fabrication.
Output: OrrerySystemRef, SystemDescriptor, parseSystemRef and closed predicate resolver; readOrrerySystemMembersCore and readOrrerySystemSnapshot with members/orbiting/global sun partition, category catalog and coherent reorder inputs; generation-guarded request/status/snapshot state; top-left built-in/category dropdown.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-03-01: Read and display All Contacts and Not Contacted without fabricated health</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-system-logic.ts, src/db/orrery-system-read.ts, src/db/orrery-system-read.test.ts, src/services/orrery-scene.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/db/orrery-read.ts, src/db/status.ts, src/logic/dashboard-query-logic.ts, src/db/contact-read.ts, src/db/transaction.ts, src/logic/sun-occupant-logic.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/logic/orrery-system-logic.ts (read current contents if present); src/db/orrery-system-read.ts (read current contents if present); src/db/orrery-system-read.test.ts (read current contents if present); src/services/orrery-scene.ts (read current contents if present); src/components/orrery/OrreryWorld.tsx (read current contents if present)</read_first>
<behavior>Cross contacted/never × Bound/Unbound × archived/live, with sun inside/outside membership. All/Not explicitly show neutral rows; default listOrbitingContacts stays contacted-only. Every qualifying contact appears once; no fake stable/decay for null progress. Favorites/category can exclude the globally resolved contact sun without removing its stable identity or adding it to members. The same System member core returns identical eligible contacted IDs from a read snapshot and an already-open write transaction, including Needs Attention threshold and Snoozed local-date boundaries.</behavior>
<action>Implement closed builtin IDs all-contacts/favorites/needs-attention/not-contacted/snoozed/chargers and category UID refs, reusing matching pure Dashboard SQL predicates while importing no Dashboard store state. Use one inReadSnapshot over settings, sun/profile, category catalog, members and impact inputs; adapt read-only signatures rather than nesting transactions. Default All Contacts includes active plus never-contacted Bound rows. Every other builtin/category remains contacted-only except Not Contacted; Needs Attention reuses exact snooze suppression and threshold semantics, Chargers binds the stored Charger value. Use CASE null health/progress for neutral rows, not STATUS_SQL's stable fallback. Order dense members by existing COALESCE(ring_seq,1e9),created_at,id. Resolve configured sun with the shared occupant helper and partition only qualifying membership out of orbiting rows; keep the complete member set. Wire scene to All Contacts default and the renderer's fixed neutral resting-angle/style branch. Return an explicit missing-category result rather than confusing deletion with SQL failure.
Also export readOrrerySystemMembersCore(exec: ReadOnlyExecutor, system: OrrerySystemRef), a transaction-composable read with no mutex or BEGIN. It owns the closed current System predicates and returns members in canonical order plus the explicit missing-category result. Both readOrrerySystemSnapshot and plan29-09's rank writer call this exact core inside their own single outer transaction; evaluate SQLite local-day/status/snooze expressions at the current read, never at gesture-start time or from cached revision alone. Snapshot includes savedSunContactId, globally resolved sun identity (contact ID and UID when applicable; read UID directly in this DAO since the existing header omits it), complete contacted sun-excluded order, contactIdentities (ID/UID pairs for every complete-order contact plus any saved-sun row), and eligibleContactedVisibleIds (ordered contacted intersection of members minus the resolved contact sun) from the same read lock. This defines the source of the later guarded reorder request, including empty eligibility for Not Contacted. Keep the globally resolved contact sun available separately from members even when a Favorites/category predicate excludes it; action validation may resolve that sun independently, while companion rows consume members only.</action>
<acceptance_criteria>Reachable All Contacts world includes neutral members and retains Bound/archive/default-read guards; coherent snapshot partitions sun exactly once and exports one reusable transaction-composable membership authority plus the complete reorder snapshot.</acceptance_criteria>
<verify><automated>test -f src/db/orrery-system-read.test.ts && npm test -- orrery-system-read orrery-read</automated></verify>
<done>Reachable All Contacts world includes neutral members and retains Bound/archive/default-read guards; coherent snapshot partitions sun exactly once and exports the transaction-composable membership authority with complete order/identity/reorder inputs.</done>
</task>

<task type="auto" tdd="true">
<name>29-03-02: Publish System switches and refreshes with race-safe state</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/stores/orrery-system-store.ts, src/stores/orrery-system-store.test.ts, src/screens/OrreryScreen.tsx, src/services/orrery-scene.ts</files>
<read_first>src/stores/shell-refresh-store.ts, src/stores/dashboard-query-store.ts, src/stores/orrery-preferences-store.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/stores/orrery-system-store.ts (read current contents if present); src/stores/orrery-system-store.test.ts (read current contents if present); src/screens/OrreryScreen.tsx (read current contents if present); src/services/orrery-scene.ts (read current contents if present)</read_first>
<behavior>A→B→A reads resolve out of order; only latest generation publishes. Same-System refresh failure retains its snapshot with stale notice; failed switch cannot label prior members as destination. Disposed requests cannot publish. Missing category differs from database failure.</behavior>
<action>Define request identity/generation at request start, successful snapshot identity, initial/loading/ready/stale/error/missing-category states, and reload. Scene snapshots publish atomically. Wire focus/shell refresh and explicit selection into this store; cancel/invalidate on disposal. During a switch show destination busy and clear/inert previous scene for destination presentation; never reuse old members under new label. After read success persist last-System through preference store; if persistence fails keep active successful System usable but retain truthful unsaved intent/retry. A confirmed removed category preserves a recoverable named missing-System state and Show All Contacts action; do not silently select another System on SQL errors. Revalidation gates downstream focus/Profile/reorder intents.</action>
<acceptance_criteria>System loading and refresh errors are distinct; stale generations and post-dispose reads cannot publish or persist a different selection.</acceptance_criteria>
<verify><automated>test -f src/stores/orrery-system-store.test.ts && npm test -- src/stores/orrery-system-store.test.ts</automated></verify>
<done>System loading and refresh errors are distinct; stale generations and post-dispose reads cannot publish or persist a different selection.</done>
</task>

<task type="auto" tdd="true">
<name>29-03-03: Make all built-in and Category Systems selectable</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrrerySystemSelector.tsx, src/components/orrery/orrery-controls-logic.ts, src/components/orrery/orrery-controls-logic.test.ts, src/screens/OrreryScreen.tsx</files>
<read_first>src/components/control-surface/AnchoredPanel.tsx, src/stores/shell-transient-store.ts, src/components/ui/AppText.tsx; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/components/orrery/OrrerySystemSelector.tsx (read current contents if present); src/components/orrery/orrery-controls-logic.ts (read current contents if present); src/components/orrery/orrery-controls-logic.test.ts (read current contents if present); src/screens/OrreryScreen.tsx (read current contents if present)</read_first>
<behavior>Builtin ordering is fixed, categories use display_order and stable UID tie break, duplicate names remain distinct. Empty choices stay enabled; long selector names keep full accessible labels; stale callback dismissal acts on the current dropdown.</behavior>
<action>Per D-10 build top-left 16-inset compact current-name/chevron selector with scrollable anchored floating list, exact UI-SPEC labels, explicit single selected state and busy destination. Consume real category rows and UID refs, never seeded names. Register unique orrery-system-selector transient, restore trigger accessibility focus after close, and use current callback refs. Route every choice through the System store. Empty built-ins stay selectable; missing category is recoverable and category CRUD remains Phase37. Keep no conventional sort/search or System-authoring actions.</action>
<acceptance_criteria>All six built-ins and live Category entries are reachable with explicit busy/selected/error handling and correct long-list behavior.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-controls-logic.test.ts && npm test -- orrery-controls-logic orrery-system-store orrery-system-read</automated></verify>
<done>All six built-ins and live Category entries are reachable with explicit busy/selected/error handling and correct long-list behavior.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| System/category input → SQL | Closed query dispatch and parameter binding. |
| async read → published membership | Atomic successful snapshot and generation guard. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-03-01 | Tampering | System resolver | high | mitigate | Bound/archive/default-read matrix and parameterized category UID tests. |
| T-29-03-02 | Information disclosure | stale member state | high | mitigate | Reject out-of-order generations and revalidate contact identity before actions. |
</threat_model>

<verification>
npm test -- orrery-system-read orrery-read
npm test -- src/stores/orrery-system-store.test.ts
npm test -- orrery-controls-logic orrery-system-store orrery-system-read
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
D-05/D-10: Only explicit All Contacts/Not Contacted widen the default never-contacted exclusion; all Systems exclude archived and Unbound contacts and Category identities use UID.
A qualifying contact sun appears once in the companion member set and never twice as an orbiting body; a nonqualifying configured sun remains governed by existing sun presentation without becoming a System member.
E2: Built-ins remain selectable even empty; zero Categories omits only Category entries; destination loading is busy without relabelling old bodies; failure uses Reload System without automatic switch; successful selection persists; lists scroll, trigger ellipsizes with full accessible name and rows wrap.
EDGE-23/24/25/26/27/28: Duplicate names do not merge identities; no members and sun-only have explicit results; System rows have stable builtin then category display_order/UID ordering; null progress/status stay null and neutral members retain deterministic dense read order.
</success_criteria>

## Artifacts this phase produces

- src/logic/orrery-system-logic.ts: OrrerySystemRef, SystemDescriptor, parseSystemRef and closed predicate resolver
- src/db/orrery-system-read.ts: readOrrerySystemSnapshot with members/orbiting/sun partition and category catalog
- src/db/orrery-system-read.ts: readOrrerySystemMembersCore(ReadOnlyExecutor, OrrerySystemRef), reused under read/write owners without acquiring a nested mutex; snapshot savedSunContactId, resolved sun ID/UID, complete contacted order, contactIdentities and eligibleContactedVisibleIds
- src/stores/orrery-system-store.ts: generation-guarded request/status/snapshot state
- src/components/orrery/OrrerySystemSelector.tsx: top-left built-in/category dropdown

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-03-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
