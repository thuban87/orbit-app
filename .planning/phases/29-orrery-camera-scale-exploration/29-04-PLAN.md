---
phase: 29-orrery-camera-scale-exploration
plan: "04"
type: execute
wave: 4
depends_on:
  - "29-03"
files_modified:
  - "src/db/orrery-impact-read.ts"
  - "src/db/orrery-impact-read.test.ts"
  - "src/db/orrery-system-read.ts"
  - "src/logic/orrery-world-logic.ts"
  - "src/logic/orrery-world-logic.test.ts"
  - "src/services/orrery-scene.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/logic/orrery-camera-logic.ts"
  - "src/logic/orrery-camera-logic.test.ts"
autonomous: true
requirements:
  - "ORRC-02"
  - "ORRC-03"
  - "ORRC-04"
  - "ORRC-05"
  - "ORRC-06"
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Canonical Gravity inputs are read in bounded batches through ReadOnlyExecutor within the member snapshot; exact single-reader parity and no per-contact query loop are verified before rendered mass expands."
    - "Meaningful minimum ring spacing persists as count grows; Home stops shrinking at the readability floor and All Contacts stays available."
    - "Density changes spacing/framing only; same IDs, health and timestamps produce the same deterministic bounded nudges."
    - "EDGE-04/06/07/08/09/10: Empty/sun-only/one scenes remain finite; exact spacing equality remains distinct; tie ranks use created_at/id; Home thresholds are tested on both sides; all legal camera poses retain a positive perspective denominator and project/inverse error within 1e-6 world units."
    - "EDGE-12/13/15: Semantic thresholds use hysteresis, zero labels is valid and nudges are deterministic with a documented angular/world displacement budget."
    - "EDGE-04 (ORRC-03/empty): Empty, sun-only and one-contact worlds produce finite geometry and a readable scale."
    - "EDGE-06 (ORRC-04/boundary): Test each legal zoom/tilt/perspective and Home-readability threshold exactly, just below and just above; clamp to finite legal camera bounds."
    - "EDGE-07 (ORRC-04/adjacency): Two adjacent rings exactly at minimum spacing remain separate rings; numerical equality never merges identities."
    - "EDGE-08 (ORRC-04/empty): Zero/one/many members have defined Home framing and never disable All Contacts."
    - "EDGE-09 (ORRC-04/ordering): Equal ring ranks order by created_at then id; UID-based nudges remain stable across refresh."
    - "EDGE-10 (ORRC-04/precision): At every legal pose the projection denominator stays positive and project/inverse error is at most 1e-6 world units."
    - "EDGE-15 (ORRC-06/precision): Collision nudges obey a documented small angular/world displacement budget and are deterministic across identical inputs."
  artifacts:
    - path: "src/db/orrery-impact-read.ts"
      provides: "readOrreryImpactInputsCore(ReadOnlyExecutor, readonly number[]) batched canonical ImpactInputs"
    - path: "src/db/orrery-impact-read.test.ts"
      provides: "real-SQL parity, query-count and coherent-snapshot tests"
    - path: "src/logic/orrery-world-logic.ts"
      provides: "deriveOrreryWorld, DENSITY_PRESETS, neutral placement and bounded deterministic nudge functions"
    - path: "src/logic/orrery-camera-logic.ts"
      provides: "deriveHomePose, frameBodies, clampCameraPose, project/unproject bounds"
    - path: "src/logic/orrery-world-logic.test.ts"
      provides: "spacing/count/density/Gravity/neutral fixtures"
    - path: "src/logic/orrery-camera-logic.test.ts"
      provides: "projection round trips and Home/readability boundary tests"
  key_links:
    - from: "src/services/orrery-scene.ts"
      to: "src/services/impact.ts"
      via: "computeContactGravity on canonical current inputs"
    - from: "src/components/orrery/OrreryWorld.tsx"
      to: "src/logic/orrery-world-logic.ts"
      via: "one canonical world supplied to frame projection"
---

<objective>
Grow the world while keeping Home and inspection readable.
Purpose: Replace viewport compression with stable layout, modest derived Gravity mass and bounded invertible projection.
Output: deriveOrreryWorld, DENSITY_PRESETS, neutral placement and bounded deterministic nudge functions; deriveHomePose, frameBodies, clampCameraPose, project/unproject bounds; spacing/count/density/Gravity/neutral fixtures; projection round trips and Home/readability boundary tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-04-01: Derive and display Gravity mass from one batched read snapshot</name>
<reversibility rating="reversible">Read-only batching preserves current Gravity semantics and stored data.</reversibility>
<files>src/db/orrery-impact-read.ts, src/db/orrery-impact-read.test.ts, src/db/orrery-system-read.ts, src/services/orrery-scene.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/db/impact-read.ts, src/services/impact.ts, src/services/gravity-logic.ts, src/db/transaction.ts, src/db/recency-dao.ts, src/db/__testkit__/node-sqlite.ts, src/db/orrery-impact-read.ts (if present), src/db/orrery-impact-read.test.ts (if present), src/db/orrery-system-read.ts, src/services/orrery-scene.ts, src/components/orrery/OrreryWorld.tsx</read_first>
<behavior>Real-SQL batch inputs and computeContactGravity results exactly match getImpactInputs at the same now for zero history, tied timestamps, ancient/future rows, mixed connected/direction, rarelyResponds 0/1 and missing IDs. Empty IDs issue zero statements; duplicates deduplicate; one through100 IDs issue one bound SELECT; larger lists use bounded chunks. A queued recency/policy writer cannot produce mixed member/policy/history state. Production scene feeds derived mass into rendered and hit-tested body bounds without storing scores.</behavior>
<action>Addresses review concern: own readOrreryImpactInputsCore(exec: ReadOnlyExecutor, contactIds: readonly number[]) returning a Map from contact ID to canonical ImpactInputs in the new DAO. Deduplicate valid IDs, short-circuit empty input and use a static contacts LEFT JOIN interactions projection scoped by bound IN values with centralized chunk size256. Select c.id and the exact policy/history fields getImpactInputs consumes; order c.id, i.occurred_at DESC, i.id DESC and omit only null interaction fillers. Missing contacts have no map entry; existing no-history contacts have an empty array. Preserve complete history: computeContactGravity has a nonzero ancient-history floor, so no time cutoff, latest-N limit or raw-count approximation. Neither this core nor its helpers acquires a mutex/BEGIN or closes over an outer writable executor. Extend readOrrerySystemSnapshot's existing inReadSnapshot callback to call this core with its ro executor for unique System members plus globally resolved contact sun and publish inputs in the same snapshot. Scene computes Gravity once per contact with computeContactGravity and one shared now, then feeds modest bounded mass into OrreryWorld's existing projected body/touch bounds immediately. No getImpactInputs or loadContactGravity loop is permitted in scene loading. Keep Profile's single-contact reader unchanged. Test actual snapshot assembly through an instrumented read-only executor for query counts, no nested transaction and no writable capability use. Later world-layout expansion consumes this working mass result.</action>
<verify><automated>test -f src/db/orrery-impact-read.test.ts && npm test -- src/db/orrery-impact-read.test.ts src/services/orrery-scene.test.ts</automated></verify>
<done>Scene owns coherent batched canonical inputs, rendered bodies use derived mass, and real-SQL parity/query-count/coherence checks pass.</done>
</task>

<task type="auto" tdd="true">
<name>29-04-02: Display a growing density-aware world with derived Gravity mass</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-world-logic.ts, src/logic/orrery-world-logic.test.ts, src/services/orrery-scene.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/services/impact.ts, src/db/impact-read.ts, src/logic/orrery-geometry-logic.ts, src/db/status.ts, src/db/orrery-system-read.ts; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/logic/orrery-world-logic.ts (read current contents if present); src/logic/orrery-world-logic.test.ts (read current contents if present); src/services/orrery-scene.ts (read current contents if present); src/components/orrery/OrreryWorld.tsx (read current contents if present)</read_first>
<behavior>0/1/6/10/100 members; sparse/equal/null ranks; same-day progress; never-contacted neutral values; all densities preserve identities. Minimum radial gaps survive 100 members. Camera yaw/density cannot change status or stored order. Gravity connected-only scope for rarely-responding contacts matches computeContactGravity.</behavior>
<action>Create deriveOrreryWorld independent of viewport size. Ring radius uses dense display index and centralized density mapping with meaningful minimum world spacing. Keep timestamp progressToAngle and bounded decay/rogue outward drift, removing the viewport clamp from this production path. Neutral bodies keep null progress/status and one fixed neutral angle; small nudges must not simulate progress. Add stable UID/neighborhood-based collision correction with explicit small maximum angular and world displacement, no random/camera-dependent global packing. Consume the computeContactGravity result and batched canonical inputs integrated by29-04-01; modest bounded mass modifier remains secondary to zoom/perspective and is never stored. Wire density from saved settings and the world output into the renderer immediately. Preserve separate world/layout from camera so subordinate bodies later share arbitrary framing.</action>
<acceptance_criteria>Density changes real world spacing, large worlds extend beyond viewport, neutral/timestamp semantics and canonical Gravity are preserved.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-world-logic.test.ts && npm test -- orrery-world-logic orrery-scene impact</automated></verify>
<done>Density changes real world spacing, large worlds extend beyond viewport, neutral/timestamp semantics and canonical Gravity are preserved.</done>
</task>

<task type="auto" tdd="true">
<name>29-04-03: Frame Home and arbitrary bodies with bounded project/inverse math</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-camera-logic.ts, src/logic/orrery-camera-logic.test.ts, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/logic/orrery-world-logic.ts, src/logic/orrery-camera-logic.ts, src/components/orrery/OrreryWorld.tsx; .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; src/logic/orrery-camera-logic.test.ts (read current contents if present)</read_first>
<behavior>Zero/invalid measurement disables framing. Home fits if projected size is readable, otherwise retains floor; counts are not hard caps. Test max tilt, full yaw wrap, pan/zoom endpoints, arbitrary body sets, exact bounds/epsilon and project→inverse tolerance 1e-6.</behavior>
<action>Implement clampCameraPose, deriveHomePose and frameBodies over explicit usable canvas rectangles/obstacles and finite world/body bounds. Derive a positive focal distance from maximum world extent and bounded tilt (never below plane/edge-on); use the same invertible perspective for rings, centers, Polaris and inverse drag coordinates, not a discontinuous denominator clamp. Centralize zoom/tilt/pan/perspective/readable-radius tunables. Home is sun centered, north up and top down, fitting until body readability floor then allowing offscreen extent; Balanced six/toward-ten remains visual calibration only. Provide arbitrary-set framing accounting for projected body and touch bounds; do not merely center their average. Preserve pinch world anchor across clamping. Wire initial Home and density reframe to these helpers now. Measurement invalidity preserves prior valid pose and disables dependent recovery until valid.</action>
<acceptance_criteria>The reachable world has readable count-independent Home and finite invertible projection; arbitrary framing API is ready and tested.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-camera-logic.test.ts && npm test -- orrery-camera-logic orrery-world-logic orrery-scene</automated></verify>
<done>The reachable world has readable count-independent Home and finite invertible projection; arbitrary framing API is ready and tested.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| domain → world | Gravity remains derived; camera cannot mutate domain. |
| measurement/input → projection | Finite bounds prevent invalid drawing and inverse math. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-04-03 | Denial of service | Gravity read | medium | mitigate | Task1 batches bound IDs and tests query count/coherence; no per-person read loop. |
| T-29-04-01 | Tampering | world semantics | high | mitigate | Tests preserve rank/progress/member identity across all camera/density changes. |
| T-29-04-02 | Denial of service | projection | medium | mitigate | Positive denominator, finite clamped axes and degenerate bounds tests. |
</threat_model>

<verification>
test -f src/db/orrery-impact-read.test.ts && npm test -- src/db/orrery-impact-read.test.ts src/services/orrery-scene.test.ts
npm test -- orrery-world-logic orrery-scene impact
npm test -- orrery-camera-logic orrery-world-logic orrery-scene
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
Meaningful minimum ring spacing persists as count grows; Home stops shrinking at the readability floor and All Contacts stays available.
Density changes spacing/framing only; same IDs, health and timestamps produce the same deterministic bounded nudges.
EDGE-04/06/07/08/09/10: Empty/sun-only/one scenes remain finite; exact spacing equality remains distinct; tie ranks use created_at/id; Home thresholds are tested on both sides; all legal camera poses retain a positive perspective denominator and project/inverse error within 1e-6 world units.
EDGE-12/13/15: Semantic thresholds use hysteresis, zero labels is valid and nudges are deterministic with a documented angular/world displacement budget.
</success_criteria>

## Artifacts this phase produces

- src/db/orrery-impact-read.ts: readOrreryImpactInputsCore over ReadOnlyExecutor, called inside readOrrerySystemSnapshot
- src/db/orrery-impact-read.test.ts: complete-history parity, bounded query count and coherent snapshot fixtures

- src/logic/orrery-world-logic.ts: deriveOrreryWorld, DENSITY_PRESETS, neutral placement and bounded deterministic nudge functions
- src/logic/orrery-camera-logic.ts: deriveHomePose, frameBodies, clampCameraPose, project/unproject bounds
- src/logic/orrery-world-logic.test.ts: spacing/count/density/Gravity/neutral fixtures
- src/logic/orrery-camera-logic.test.ts: projection round trips and Home/readability boundary tests

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-04-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
