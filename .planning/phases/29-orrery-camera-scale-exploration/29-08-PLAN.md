---
phase: 29-orrery-camera-scale-exploration
plan: "08"
type: execute
wave: 8
depends_on:
  - "29-07"
files_modified:
  - "src/db/orrery-action-read.ts"
  - "src/logic/orrery-focus-logic.ts"
  - "src/logic/orrery-focus-logic.test.ts"
  - "src/components/orrery/use-orrery-camera.ts"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/components/orrery/OrreryFocusContext.tsx"
  - "src/components/orrery/OrreryClusterPanel.tsx"
  - "src/components/orrery/orrery-overlay-logic.ts"
  - "src/components/orrery/orrery-overlay-logic.test.ts"
  - "src/screens/OrreryScreen.tsx"
  - "src/components/orrery/OrreryContactsSheet.tsx"
  - "src/components/orrery/orrery-companion-logic.ts"
  - "src/components/orrery/orrery-companion-logic.test.ts"
autonomous: true
requirements:
  - "ORRC-06"
  - "ORRC-07"
  - "ORRC-15"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each focus/Profile action uses a narrow current ID/UID, membership and global-sun probe with at most three SELECTs and no Gravity/history/full-scene read; current generation is checked after awaiting it."
    - "Distant isolated tap focuses to identity-level name visibility; an already inspected tap opens Profile; overlapping interactive targets always offer the whole plausible group."
    - "A globally resolved contact sun excluded by Favorites/category remains focusable and Profile-actionable after current sun/identity validation; it participates once in ambiguous groups and never enters the companion without qualifying membership."
    - "E5: Companion shares active loading/error/empty state; qualifying sun appears once; full names wrap with health/Gravity and separate Focus/Open Profile actions; missing photo uses initials; optional data omission is local; many rows scroll and actions reflow; either action closes sheet before framing/navigation."
    - "E6: Group derives from loaded current targets without fake loading; every action revalidates ID; zero members dismiss, one stays actionable, many scroll; count grammar is correct; optional metadata omitted; measured height reserves bodies and dismiss/Recenter; stale same-System content retains shared notice."
    - "E7: Loaded identity remains during optional context loading; removed contact clears focus with documented notice; manual camera movement preserves focus until explicitly dismissed/effectively offscreen; wrapped context stays inside usable bounds and yields to controls."
    - "EDGE-16/17/18/19/34/35/36: Inclusive exact target-boundary overlap includes both candidates; none clears focus; stable contact UID/id defines equality, never name; clusters and companion preserve deterministic System order and sun-once membership."
    - "EDGE-16 (ORRC-07/adjacency): A touch on an inclusive shared target boundary includes both plausible targets and opens the group."
    - "EDGE-17 (ORRC-07/empty): No hit clears/dismisses focus; zero surviving cluster rows closes it, one stays actionable and many scroll."
    - "EDGE-18 (ORRC-07/encoding): Contact identity comparisons use stable UID/id, never normalized display-name equality."
    - "EDGE-19 (ORRC-07/ordering): Ambiguous candidates retain deterministic System order with stable identity tie-breaking."
    - "EDGE-34 (ORRC-15/adjacency): Sun and ordinary member identities are deduplicated before companion rows; shell/overlay obstacle intersections do not create duplicate actions."
    - "EDGE-35 (ORRC-15/empty): Companion opens for empty/loading/error states; zero has approved copy, one a normal row, many a scrolling list."
    - "EDGE-36 (ORRC-15/ordering): Companion order equals the active System member order and every qualifying contact, including the contact sun, appears once."
    - "UI-E5 Companion sheet; all 8 considerations (empty, loading, error, populated, partial, overflow, zero-one-many, long-text): Share the active System's loading/error/empty truth and copy with E1. Each qualifying contact appears once, including a qualifying contact sun. Rows expose full wrapping name, health, available Gravity, Focus in Orrery and Open Profile. Photo failure uses initials and absent optional context omits only that field. Zero uses empty treatment, one stays a normal row, many scroll within the detail sheet. Actions reflow vertically when necessary. Focus closes the sheet and frames the selected contact; Profile closes the sheet before navigating."
    - "UI-E6 Cluster panel; all 8 considerations (empty, loading, error, populated, partial, overflow, zero-one-many, long-text): Derive the group from loaded projected interactive targets, with no independent artificial loading delay. Profile/focus actions revalidate the target. Zero surviving members dismisses the group; one keeps an actionable single row until dismissed/selected; many scroll in the floating panel. Use singular/plural count copy. Missing optional metadata is omitted. Names wrap; panel height is bounded by measured available space and reserves visible framed bodies and dismiss/recenter controls. Read failure preserves last valid same-System content with the shared failure notice."
    - "UI-E7 Focus labels; all 4 considerations (loading, error, overflow, long-text): Identity comes from the loaded body; optional context may load without blocking that identity. Satellite failure uses its targeted reload copy while the parent stays available. A removed target clears focus and uses the documented notice. Single focus persists through camera moves until dismissed or effectively lost offscreen; camera clipping must not strand hidden focus. Focus names/context wrap within usable bounds; suppress subordinate context before covering controls. Full identity remains in the companion's accessible parent context."
  artifacts:
    - path: "src/db/orrery-action-read.ts"
      provides: "readOrreryContactTargetValidation: narrow identity, membership and global sun probe"
    - path: "src/logic/orrery-focus-logic.ts"
      provides: "OrreryContactTarget member/contact-sun discriminant, validateOrreryContactTarget, resolveOrreryTap, reconcileFocus and deterministic cluster intents"
    - path: "src/components/orrery/OrreryClusterPanel.tsx"
      provides: "nonmodal Contacts here group panel"
    - path: "src/components/orrery/OrreryContactsSheet.tsx"
      provides: "modal Contacts in this System companion"
    - path: "src/components/orrery/OrreryFocusContext.tsx"
      provides: "minimal wrapped focused identity/context and Clear focus"
  key_links:
    - from: "src/components/orrery/use-orrery-camera.ts"
      to: "src/logic/orrery-focus-logic.ts"
      via: "current-frame target set → discrete intent"
    - from: "src/components/orrery/OrreryContactsSheet.tsx"
      to: "src/stores/orrery-system-store.ts"
      via: "same committed members and state as world"
---

<objective>
Focus contacts and ambiguous groups, with a complete accessible companion.
Purpose: Make every System member reachable through both spatial and conventional interactions.
Output: OrreryContactTarget, validateOrreryContactTarget, resolveOrreryTap, reconcileFocus, deterministic cluster membership and action intents; nonmodal Contacts here group panel; modal Contacts in this System companion; minimal wrapped focused identity/context and Clear focus.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-08-01: Focus exact current targets and frame ambiguous groups</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-focus-logic.ts, src/logic/orrery-focus-logic.test.ts, src/components/orrery/use-orrery-camera.ts, src/components/orrery/OrreryWorld.tsx, src/db/orrery-action-read.ts</files>
<read_first>src/db/orrery-action-read.ts (if present), src/logic/orrery-system-logic.ts, src/db/transaction.ts, src/db/__testkit__/node-sqlite.ts, src/db/orrery-system-read.ts, src/logic/sun-occupant-logic.ts, src/navigation/tabs/OrreryStack.tsx, src/logic/orrery-camera-logic.ts, src/logic/orrery-label-logic.ts, src/stores/orrery-system-store.ts, src/logic/orrery-focus-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-focus-logic.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/use-orrery-camera.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task), src/components/orrery/OrreryFocusContext.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Real-SQL target-probe results match membership/global-sun truth from the scene snapshot for all six builtins and Category Systems, including never-contacted eligibility, category rename/deletion, Favorites changes and snooze/status day boundaries. Instrument SQL to prove at most three SELECTs independent of scene size and zero interaction/Gravity reads; test ro-only capability and no nested mutex. Touch at target edge uses inclusive containment; two effective targets at same point yields both regardless of depth. No target clears selection. Distant tap stops at identity level; inspected tap opens Profile. Sun/contact/satellite kinds cannot accidentally share ID namespaces. Removed focus clears, valid moved focus persists. With contact sun excluded by Favorites or category: distant sun focuses, inspected sun opens Profile, overlapping sun/member yields both once in a deterministic group, and no sun companion row is introduced. Sun reassignment, archive, Unbinding, purge/merge or ID reuse with changed UID invalidates the former sun action; an unchanged current sun does not require System membership.</behavior>
<action>Replace tracer intent handling with resolveOrreryTap over the authoritative interpolated ProjectedFrame and current semantic name visibility. Collect all effective at-least44 target bounds at the touch, not nearest-center/topmost winner. Translate frame targets into OrreryContactTarget with explicit member/contact-sun discriminant and contact ID/UID; satellites retain their own identity namespace and self sun has no contact action. Deduplicate contact identities by ID/UID across the member/sun roles, retaining contact-sun role when both qualify. Define validateOrreryContactTarget against freshly read narrow DAO validation and current request generation: member targets require continued live System membership; contact-sun targets require the same live ID/UID as the CURRENT globally resolved contact sun using the shared occupant policy, regardless of active System predicate. Addresses review concern: implement readOrreryContactTargetValidation(exec: SqlExecutor, system: OrrerySystemRef, target: OrreryContactTarget) in src/db/orrery-action-read.ts and inject it before focus/Profile dispatch. Add source comments in orrery-action-read.ts after reading the full ADRs: ADR-011 for truthful status eligibility with D-05's explicit All/Not widening, ADR-047 for current global sun fallback, ADR-093 for the reused population predicate semantics only, and ADR-077 for inspection/Profile intent over the canonical world. It owns one short inReadSnapshot, using only its ro argument. Read the requested ID/UID and lifecycle, the singleton saved sun with its current occupant lifecycle/ID/UID, and existence of the selected category when applicable; evaluate target membership using plan29-03 buildOrrerySystemWhere with bound target ID and unchanged SQLite local-day/status predicates. Use sunOccupantIsSelf for the shared global-sun fallback policy rather than duplicating it or resolving theme/photo resources. Return ID/UID, isMember, current resolved contact-sun identity and missing-category status, without returning other people. Keep the probe bounded to at most three SELECT statements for all System sizes, with no category catalog, full-member/order read, interaction/Gravity read or call to readOrrerySystemSnapshot. Missing/invalid System and database failure reject the action distinctly. The member versus contact-sun rule remains unchanged; after it resolves recheck request generation and route activity, rejecting stale reads or failures without dispatch. Do not validate sun solely against the rendered/cached sun. One valid distant contact invokes frameBodies and identity zoom/highlight; one already inspected contact routes Profile. For ambiguous contacts preserve System member order with stable identity tie-break and append a globally resolved nonmember sun after members; never rank it through a missing member index. This extends deterministic order without inserting the sun into System membership. Frame the complete candidate group. Maintain light single-focus context with Clear focus; use the same discriminated validation result in reconcileFocus and subsequent group/focused actions. Reconcile removal/merge/category switch and meaningful offscreen loss using explicit tested visibility threshold; manual camera movement alone does not clear focus. Expose optional-context loading/error independently.
Addresses cycle2 contention concern: this narrow probe still queues behind the same FIFO mutex as automatic backup snapshots, scene reads and app writes. Its three-statement bound is not a latency guarantee. Preserve the mandatory fresh identity/membership/sun validation and wait for it before focus/Profile dispatch; never fall back to cached identity while waiting. Keep the existing loaded identity/canvas and approved controls usable, with no new busy UI. The existing action/request generation must invalidate a pending intent on superseding action, Clear focus, empty/outside dismissal, Recenter, System change, route blur, background or disposal; after awaiting the probe check both that action generation and current route/System request before any focus/navigation side effect. This is logical cancellation: do not claim the existing mutex dequeues SQL or aborts another transaction. In the owning test file queue production validation behind a deferred withMutex holder, cancel through each production lifecycle/dismissal path, release it and assert no late focus/navigation; retain an uncancelled control that validates and dispatches once after release. Include a rejected holder control proving the chain continues and probe errors remain handled. Phase40's D-10 hardening owns measured contention optimization; record backup/tap behavior and unknown latency in29-12 without changing mutex priority, timeout policy or atomicity.</action>
<acceptance_criteria>Single and ambiguous touches resolve from actual animated targets with correct focus depth, stable identity and no arbitrary winner, including globally configured nonmember contact suns with fresh eligibility validation. Queued cancelled/superseded intents never focus or navigate after lock release; the uncancelled current action dispatches once after its fresh probe, and failed holders do not poison later work. The action DAO contains the scoped ADR-011/047/093/077 source comments.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-focus-logic.test.ts && npm test -- src/logic/orrery-focus-logic.test.ts</automated></verify>
<done>Single and ambiguous touches resolve from actual animated targets with correct focus depth, stable identity and no arbitrary winner.</done>
</task>

<task type="auto" tdd="true">
<name>29-08-02: Resolve crowded groups through a nonmodal floating panel</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrreryClusterPanel.tsx, src/components/orrery/orrery-overlay-logic.ts, src/components/orrery/orrery-overlay-logic.test.ts, src/screens/OrreryScreen.tsx, src/components/orrery/OrreryWorld.tsx</files>
<read_first>src/stores/shell-transient-store.ts, src/components/ui/GlassSurface.tsx, src/components/orrery/orrery-obstacle-logic.ts, src/components/orrery/OrreryClusterPanel.tsx (read current contents if present; new paths are created by this task), src/components/orrery/orrery-overlay-logic.ts (read current contents if present; new paths are created by this task), src/components/orrery/orrery-overlay-logic.test.ts (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Zero surviving group members closes it; one keeps a normal actionable row; many scroll within measured bound. Back/outside/Recenter closes once. Profile closes group before navigation, never reopens dismissed group on return. Canvas remains interactive outside panel.</behavior>
<action>Render Contacts here floating above measured shell exclusion region with wrapping names, correct singular/plural count, separate identify/focus/open actions and Close contact group. Group rows come only from currently loaded candidate IDs, no separate loading query or invented delay. Revalidate each action through validateOrreryContactTarget and its narrow current-target probe, preserving member versus contact-sun eligibility and the deterministic nonmember-sun append order. Register orrery-cluster transient with stable latest callback; dismiss on shell Back/active-tab retap, empty/outside touch, Recenter and before navigation. Reserve measured panel height in frameBodies, retaining largest useful visible body region and reachable Recenter/dismiss. Keep it nonmodal and avoid a full-screen blocking scrim; modal companion remains a different surface. Preserve stale same-System group content only while still valid, with shared refresh notice.</action>
<acceptance_criteria>Ambiguous groups are conventionally selectable without blocking world exploration, and dismissal/navigation ordering is tested.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-overlay-logic.test.ts && npm test -- src/components/orrery/orrery-overlay-logic.test.ts src/logic/orrery-focus-logic.test.ts</automated></verify>
<done>Ambiguous groups are conventionally selectable without blocking world exploration, and dismissal/navigation ordering is tested.</done>
</task>

<task type="auto" tdd="true">
<name>29-08-03: Expose every System member in the accessible companion sheet</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/components/orrery/OrreryContactsSheet.tsx, src/components/orrery/orrery-companion-logic.ts, src/components/orrery/orrery-companion-logic.test.ts, src/screens/OrreryScreen.tsx, src/components/orrery/OrreryFocusContext.tsx</files>
<read_first>src/components/orrery/OrreryFocusContext.tsx (if present), src/components/ui/Sheet.tsx, src/components/ui/overlay-base.tsx, src/stores/shell-transient-store.ts, src/db/orrery-system-read.ts, src/components/orrery/OrreryContactsSheet.tsx (read current contents if present; new paths are created by this task), src/components/orrery/orrery-companion-logic.ts (read current contents if present; new paths are created by this task), src/components/orrery/orrery-companion-logic.test.ts (read current contents if present; new paths are created by this task), src/screens/OrreryScreen.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>0/1/many and sun-only member sets preserve exact snapshot IDs; duplicate display names remain separate. Focus closes sheet then frames exact selected member; Profile closes sheet then routes. Initial failure/stale refresh/copy matches world; modal blocks underlying touches and restores trigger focus.</behavior>
<action>Replace plan6's detail surface content with OrreryContactsSheet consuming the exact successful System snapshot and loading/error state. Each qualifying contact including sun appears once, with full wrapping name, local photo/initials, readable health (Not contacted yet for null) and available derived Gravity. Give distinct Focus in Orrery and Open Profile actions; close before framing/navigation and revalidate the current member before action; a qualifying sun row still requires companion membership, while canvas/cluster sun targets use their separate eligibility. Never append a nonmember sun to this sheet. Scroll many rows within detail Sheet, reflow action rows vertically under large text. Register unique orrery-contacts transient and restore Contacts trigger focus on close; isolate underlying controls while modal is open. Empty/loading surfaces still open and share exact approved recovery copy. Create OrreryFocusContext and wire the light single-focus display already tracked by the camera, with Clear focus and wrapping identity. Provide its and the companion's parent relationship-context slots for satellite plan10 without querying broader contacts. Per D-11, a visible nonmember sun keeps identity/focus/Profile but receives no satellite relationship context.</action>
<acceptance_criteria>Every active System member has accessible identity and separate focus/Profile actions with correct shared state and sun accounting.</acceptance_criteria>
<verify><automated>test -f src/components/orrery/orrery-companion-logic.test.ts && npm test -- src/components/orrery/orrery-companion-logic.test.ts</automated><human-check>End-of-phase TalkBack: full names, action ordering, trigger focus restoration, modal isolation, large-font wrapping and group/world coexistence.</human-check></verify>
<done>Every active System member has accessible identity and separate focus/Profile actions with correct shared state and sun accounting.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| current projected targets → navigation | Revalidate live ID and generation. |
| overlay → accessible focus | Modal isolation vs nonmodal world access. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-08-01 | Tampering | stale focus/Profile action | high | mitigate | Narrow SQLite target probe plus generation check: members require current membership; contact-sun requires current globally resolved sun and live ID/UID. Revalidate group/focused actions and reject changed occupant/identity. Task1 also invalidates pending action generations while queued behind the shared mutex and tests late-completion rejection without bypassing fresh validation. |
| T-29-08-02 | Information disclosure | companion membership | high | mitigate | Use same successful snapshot; never a separate broad contact query. |
</threat_model>

<verification>
npm test -- src/logic/orrery-focus-logic.test.ts
npm test -- src/components/orrery/orrery-overlay-logic.test.ts src/logic/orrery-focus-logic.test.ts
npm test -- src/components/orrery/orrery-companion-logic.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
Distant isolated tap focuses to identity-level name visibility; an already inspected tap opens Profile; overlapping interactive targets always offer the whole plausible group.
A globally resolved contact sun outside Favorites/category keeps focus, Profile and ambiguity handling after fresh sun/identity validation. Companion remains the exact active System member set; a nonmember sun is appended only to ambiguous candidate ordering.
E5: Companion shares active loading/error/empty state; qualifying sun appears once; full names wrap with health/Gravity and separate Focus/Open Profile actions; missing photo uses initials; optional data omission is local; many rows scroll and actions reflow; either action closes sheet before framing/navigation.
E6: Group derives from loaded current targets without fake loading; every action revalidates ID; zero members dismiss, one stays actionable, many scroll; count grammar is correct; optional metadata omitted; measured height reserves bodies and dismiss/Recenter; stale same-System content retains shared notice.
E7: Loaded identity remains during optional context loading; removed contact clears focus with documented notice; manual camera movement preserves focus until explicitly dismissed/effectively offscreen; wrapped context stays inside usable bounds and yields to controls.
EDGE-16/17/18/19/34/35/36: Inclusive exact target-boundary overlap includes both candidates; none clears focus; stable contact UID/id defines equality, never name; clusters and companion preserve deterministic System order and sun-once membership.
</success_criteria>

## Artifacts this phase produces

- src/db/orrery-action-read.ts: readOrreryContactTargetValidation bounded ID/UID/member/sun validation, sharing buildOrrerySystemWhere and sunOccupantIsSelf; real-SQL tests owned by src/logic/orrery-focus-logic.test.ts

- src/logic/orrery-focus-logic.ts: OrreryContactTarget member/contact-sun discriminant with ID/UID; validateOrreryContactTarget, resolveOrreryTap, reconcileFocus and deterministic candidate order including global nonmember sun
- src/components/orrery/OrreryClusterPanel.tsx: nonmodal Contacts here group panel
- src/components/orrery/OrreryContactsSheet.tsx: modal Contacts in this System companion
- src/components/orrery/OrreryFocusContext.tsx: minimal wrapped focused identity/context and Clear focus

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-08-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
