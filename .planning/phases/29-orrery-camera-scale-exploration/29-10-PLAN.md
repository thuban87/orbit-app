---
phase: 29-orrery-camera-scale-exploration
plan: "10"
type: execute
wave: 10
depends_on:
  - "29-09"
files_modified:
  - "src/db/orrery-satellites-read.ts"
  - "src/db/orrery-satellites-read.test.ts"
  - "src/services/orrery-scene.ts"
  - "src/components/orrery/orrery-satellite-context.ts"
  - "src/components/orrery/OrreryContactsSheet.tsx"
  - "src/logic/orrery-satellite-logic.ts"
  - "src/logic/orrery-satellite-logic.test.ts"
  - "src/components/orrery/SatelliteBody.tsx"
  - "src/components/orrery/OrreryWorld.tsx"
  - "src/components/orrery/OrreryFocusContext.tsx"
autonomous: true
requirements:
  - "ORRC-14"
  - "ORRC-15"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09: Only live, visible, unlinked structured relationship rows for current parent members are satellites; linking, hiding, deletion, merge or parent exclusion invalidates them."
    - "Satellites Off hides every moon; overview hides them and readable parent inspection reveals them. Tap shows person name/relation only, with parent accessible context available without a precision tap."
    - "Satellites have no status, Gravity, cadence, rail, membership, logging, Profile, or children; identity is relationship UID plus parent context."
    - "EDGE-32/33: Zero eligible relationships produces no moons and no read-failure notice; one/many have deterministic identity order. Unicode person/relation text round-trips unchanged; absent relation uses approved key-person fallback."
    - "EDGE-32 (ORRC-14/empty): Zero eligible relationship rows yields no moons, one/many deterministic moons; a failed read remains distinguishable from an empty result."
    - "EDGE-33 (ORRC-14/encoding): Person and relationship text round-trips unchanged in Unicode; absent relation uses the approved key-person fallback."
  artifacts:
    - path: "src/db/orrery-satellites-read.ts"
      provides: "readOrrerySatellites batched eligible relationship projection"
    - path: "src/logic/orrery-satellite-logic.ts"
      provides: "deriveSatelliteBodies and semantic parent-context visibility"
    - path: "src/components/orrery/SatelliteBody.tsx"
      provides: "subordinate projected moon billboard"
    - path: "src/components/orrery/orrery-satellite-context.ts"
      provides: "parent accessible name/relation descriptors"
  key_links:
    - from: "src/db/orrery-satellites-read.ts"
      to: "src/db/relationships-read.ts"
      via: "shared resolveRelationshipVisibility semantics"
    - from: "src/components/orrery/SatelliteBody.tsx"
      to: "src/logic/orrery-camera-logic.ts"
      via: "same frame projection and typed target dispatch"
---

<objective>
Reveal lightweight relationship satellites around their parent contacts.
Purpose: Present existing unlinked person relationships as optional contextual moons with no contact semantics.
Output: readOrrerySatellites batched eligible relationship projection; deriveSatelliteBodies and semantic parent-context visibility; subordinate projected moon billboard; parent accessible name/relation descriptors.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@HANDOFF.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md
@.planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md
@.planning/phases/29-orrery-camera-scale-exploration/29-RESEARCH.md
@.planning/phases/29-orrery-camera-scale-exploration/29-PATTERNS.md
@.planning/phases/29-orrery-camera-scale-exploration/COVERAGE.md

</context>

<execution_rules>
Read actual full affected files and same-subsystem consumers, not a diff. Read every shared-table writer listed in COVERAGE.md before modifying data invariants. Preserve others' edits. Work in place: no push, worktree, branch creation or switching. No new dependency, network read, service, device action or package install is required for automated execution. Follow exact approved UI copy and theme tokens. Each task starts with its behavioral test where code changes behavior; keep native visual/gesture claims pending until human-check evidence.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
<name>29-10-01: Read eligible relationships and expose parent context when enabled</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/db/orrery-satellites-read.ts, src/db/orrery-satellites-read.test.ts, src/services/orrery-scene.ts, src/components/orrery/orrery-satellite-context.ts, src/components/orrery/OrreryContactsSheet.tsx</files>
<read_first>src/db/relationships-read.ts, src/db/relationships-dao.ts, src/db/memory-registry.ts, src/db/migrations/016-contact-knowledge.ts, src/db/merge-dao.ts, src/db/purge-dao.ts, src/backup/restore-apply.ts, src/db/orrery-satellites-read.ts (read current contents if present; new paths are created by this task), src/db/orrery-satellites-read.test.ts (read current contents if present; new paths are created by this task), src/services/orrery-scene.ts (read current contents if present; new paths are created by this task), src/components/orrery/orrery-satellite-context.ts (read current contents if present; new paths are created by this task), src/components/orrery/OrreryContactsSheet.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>hidden null respects shared default; hidden1/deleted/linked rows excluded; empty parents short-circuit; eligible parent sun is included once. Linking/hiding/delete/restore and merge clearing/reparenting are reflected by next read. Missing relation text uses approved fallback.</behavior>
<action>Per D-09 verify migration016 and actual relationship model before use. Batch current System parent IDs through a read-only query, bound in safe chunks if needed and joined to live Bound parents. Select existing relationship UID/name/relation/link/visibility/deletion fields only; use resolveRelationshipVisibility, retaining only unlinked live visible rows. Do not invent person-type inference from free text or graph storage. Include a qualifying contact sun's parent context. Load optional satellite context under a separate request generation so failure cannot erase core contacts; only published successful System parents may receive context. Wire enabled preference to loading/context display and expose names/relations beneath each companion parent without precise moon interaction. Show exact Reload satellites error/retry while retaining identity; turn-off/switch/link invalidates stale results.</action>
<acceptance_criteria>Enabled companion parent context reflects current structured eligible relationships with correct failures and no new contact membership.</acceptance_criteria>
<verify><automated>test -f src/db/orrery-satellites-read.test.ts && npm test -- src/db/orrery-satellites-read.test.ts</automated></verify>
<done>Enabled companion parent context reflects current structured eligible relationships with correct failures and no new contact membership.</done>
</task>

<task type="auto" tdd="true">
<name>29-10-02: Render semantic moons through the existing projected-frame pipeline</name>
<reversibility rating="reversible">Local implementation behind the phase's existing domain contracts; no stored data format changes.</reversibility>
<files>src/logic/orrery-satellite-logic.ts, src/logic/orrery-satellite-logic.test.ts, src/components/orrery/SatelliteBody.tsx, src/components/orrery/OrreryWorld.tsx, src/components/orrery/OrreryFocusContext.tsx</files>
<read_first>src/logic/orrery-camera-logic.ts, src/logic/orrery-label-logic.ts, src/logic/orrery-focus-logic.ts, src/components/orrery/OrbitBody.tsx, src/logic/orrery-satellite-logic.ts (read current contents if present; new paths are created by this task), src/logic/orrery-satellite-logic.test.ts (read current contents if present; new paths are created by this task), src/components/orrery/SatelliteBody.tsx (read current contents if present; new paths are created by this task), src/components/orrery/OrreryWorld.tsx (read current contents if present; new paths are created by this task), src/components/orrery/OrreryFocusContext.tsx (read current contents if present; new paths are created by this task); .planning/phases/29-orrery-camera-scale-exploration/29-UI-SPEC.md; .planning/phases/29-orrery-camera-scale-exploration/29-CONTEXT.md; </read_first>
<behavior>Off/overview yields no interactive moon; inspected enabled parent gets small stable subordinate bodies. Projection and hits match at intermediate camera frames. Linked/removed moon immediately loses focus/target; moon tap cannot create Profile/log/reorder intent.</behavior>
<action>Derive stable subordinate world positions from parent and relationship UID; keep domain layout separate from projection to preserve arbitrary-body framing extensibility. Draw small tokenized moons only when enabled and parent semantic scale permits; use same camera/current-frame/depth path and finite bounds. Satellite targets are typed distinctly from contacts, cannot arm reorder or enter contact membership, and reveal only name/relation with restrained focus. Mixed satellite/contact target ambiguity must retain the contact-group path and parent context access without coercing relationship ID into a contact ID. Add wrapped lightweight relationship focus text, missing-relation fallback and Clear focus, prioritizing controls/identity above subordinate context. Reconcile hidden/linked/deleted/parent-excluded rows immediately on successful refresh; no synthetic status/Gravity/cadence/rail/actions/children or relationship writes.</action>
<acceptance_criteria>Enabled inspected parents show lightweight tappable moons whose context is also accessible from the companion; lifecycle changes remove invalid moon actions.</acceptance_criteria>
<verify><automated>test -f src/logic/orrery-satellite-logic.test.ts && npm test -- src/logic/orrery-satellite-logic.test.ts src/db/orrery-satellites-read.test.ts</automated></verify>
<done>Enabled inspected parents show lightweight tappable moons whose context is also accessible from the companion; lifecycle changes remove invalid moon actions.</done>
</task>

</tasks>

<threat_model>
Review level: ASVS 1 adapted to local mobile data; high or critical findings block completion. No remote auth boundary is introduced.

| Boundary | Description |
|---|---|
| relationship lifecycle → render | Current live eligibility only. |
| satellite target → contact actions | Entity kind prevents fabricated contact navigation/logging. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-10-01 | Information disclosure | hidden/linked/deleted relationships | high | mitigate | Batch live eligibility with generation invalidation and merge/link/purge/restore fixtures. |
| T-29-10-02 | Tampering | satellite actions | high | mitigate | Discriminated target kind exposes name/relation only; no contact command capability. |
</threat_model>

<verification>
npm test -- src/db/orrery-satellites-read.test.ts
npm test -- src/logic/orrery-satellite-logic.test.ts src/db/orrery-satellites-read.test.ts
For changed TypeScript contracts run npx tsc --noEmit; for rendering run npm run check:colors and targeted Biome. Automated tests prove pure/controller/SQLite behavior; native Skia, TalkBack and gesture arbitration require the final human-check.
</verification>

<success_criteria>
D-09: Only live, visible, unlinked structured relationship rows for current parent members are satellites; linking, hiding, deletion, merge or parent exclusion invalidates them.
Satellites Off hides every moon; overview hides them and readable parent inspection reveals them. Tap shows person name/relation only, with parent accessible context available without a precision tap.
Satellites have no status, Gravity, cadence, rail, membership, logging, Profile, or children; identity is relationship UID plus parent context.
EDGE-32/33: Zero eligible relationships produces no moons and no read-failure notice; one/many have deterministic identity order. Unicode person/relation text round-trips unchanged; absent relation uses approved key-person fallback.
</success_criteria>

## Artifacts this phase produces

- src/db/orrery-satellites-read.ts: readOrrerySatellites batched eligible relationship projection
- src/logic/orrery-satellite-logic.ts: deriveSatelliteBodies and semantic parent-context visibility
- src/components/orrery/SatelliteBody.tsx: subordinate projected moon billboard
- src/components/orrery/orrery-satellite-context.ts: parent accessible name/relation descriptors

<output>
Create .planning/phases/29-orrery-camera-scale-exploration/29-10-SUMMARY.md with executed checks, remaining native checks and concrete exported signatures.
</output>
