---
phase: 02-data-foundation-status-engine
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/services/launch-sweep.ts
  - src/services/launch-sweep.test.ts
  - App.tsx
autonomous: true
requirements:
  - DATA-06
must_haves:
  truths:
    - "runLaunchSweep executes registered hooks in registration order and is idempotent within a launch"
    - "Importing launch-sweep.ts runs NOTHING — there is no module-scope side effect"
    - "installSweepTrigger fires the sweep on cold-start and on a real background→active AppState transition, and not on any non-active transition"
    - "The app gates first render on openAndMigrate() completing and installs the sweep trigger from an effect (not module scope), cleaning up on unmount"
    - "The Phase-2 hook registry is empty; later phases register quarantine expiry, purge, reconcile, rotation"
  artifacts:
    - path: "src/services/launch-sweep.ts"
      provides: "AppState-gated sweep runner + hook registry (dependency-injected AppState)"
      exports: ["registerSweepHook", "runLaunchSweep", "installSweepTrigger"]
    - path: "src/services/launch-sweep.test.ts"
      provides: "Hook-order + idempotency + no-import-side-effect + trigger-gating assertions"
    - path: "App.tsx"
      provides: "Migrate-gated first render + sweep-trigger install"
  key_links:
    - from: "App.tsx"
      to: "src/db/database.ts"
      via: "first render is gated on openAndMigrate() resolving"
      pattern: "openAndMigrate"
    - from: "App.tsx"
      to: "src/services/launch-sweep.ts"
      via: "an effect calls installSweepTrigger(AppState) and returns its remover"
      pattern: "installSweepTrigger"
---

<objective>
Build the launch-sweep skeleton (DATA-06): a `runLaunchSweep()` + `registerSweepHook()` registry that
runs once per REAL foreground launch — cold start and every RN `AppState` background→active transition
— and NEVER on module import or a headless widget/notification tap (which load the JS bundle inside a
30-second budget). Phase 2 ships the runner and an EMPTY registry; Phases 3/4/11/15/16 register their
responsibilities later. Then wire `App.tsx` to gate first render on `openAndMigrate()` and install the
sweep trigger from an effect.

The pure sweep logic takes `AppState` by dependency injection so the whole module is node-testable
(and so `App.tsx` owns the one react-native import). The load-bearing rule is negative: importing the
module must do nothing, and a headless tap must never reach the sweep.

Purpose: one sweep entry point, correctly gated, that later phases hang responsibilities on.
Output: `src/services/launch-sweep.ts`, its test, and the App.tsx wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-data-foundation-status-engine/02-RESEARCH.md
@.planning/phases/02-data-foundation-status-engine/02-PATTERNS.md
@CLAUDE.md
@App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppState-gated sweep runner + hook registry</name>
  <files>src/services/launch-sweep.ts, src/services/launch-sweep.test.ts</files>
  <read_first>
    - .planning/phases/02-data-foundation-status-engine/02-RESEARCH.md §Code Example 4 (lines 515-539) + Pattern 4 + Pitfall P5 (sweep on every headless tap, lines 248-252)
    - .planning/phases/02-data-foundation-status-engine/02-PATTERNS.md ("launch-sweep.ts"; dormant-module doc convention; AppState shape from quest-board useIdleTimer)
    - src/services/AiService.ts lines 1-16 (dormant-module doc-comment convention)
  </read_first>
  <behavior>
    - registerSweepHook then runLaunchSweep runs the hooks in registration order (assert via an order log).
    - runLaunchSweep is idempotent within a launch: a re-entrant call while one is running does not double-run the hooks.
    - Merely importing launch-sweep.ts runs no hook and touches no AppState — no module-scope side effect.
    - installSweepTrigger(fakeAppState) runs the sweep once immediately (cold start), again on a 'change' event with state 'active' (and on a background→active sequence), and NOT on a 'change' to 'background'/'inactive'; it returns the subscription's remover.
    - The hook registry is empty by default in Phase 2.
  </behavior>
  <action>
    Create `src/services/launch-sweep.ts` (repo dormant-module doc header, DATA-06 — state the registry
    is EMPTY in Phase 2 and later phases register quarantine expiry / archived-purge / schedule
    reconcile / digest re-register / backup rotation). Implement RESEARCH §Code Example 4:
    `const hooks: SweepHook[] = []`; `registerSweepHook(fn)`; a module-level `running` guard;
    `runLaunchSweep()` returns early if running, else sets running, awaits each hook in order in a try,
    resets running in finally. For testability, do NOT statically `import { AppState } from
    'react-native'` — instead `installSweepTrigger(appState)` takes an AppState-like object
    (`{ addEventListener(type, cb): { remove(): void } }`), runs the sweep once for cold-start, then
    subscribes to 'change' and runs the sweep when the next state is 'active', returning the
    subscription remover. Expose a `__resetSweepForTest()` helper (clears hooks + running) so tests are
    isolated. Never trigger at module top-level (P5).

    Write `src/services/launch-sweep.test.ts` first (RED) covering every behavior case with a hand-rolled
    fake AppState (records the callback, lets the test fire 'active'/'background'). Prove the
    no-import-side-effect case by asserting a fresh import ran no hook.
  </action>
  <verify>
    <automated>npx vitest run src/services/launch-sweep.test.ts</automated>
    <automated>npx tsc --noEmit && npx biome check src/services/launch-sweep.ts src/services/launch-sweep.test.ts</automated>
  </verify>
  <done>launch-sweep.test.ts green: hooks run in order, runLaunchSweep is idempotent, importing the module runs nothing, installSweepTrigger fires only on active/background→active and returns a remover. launch-sweep.ts has no static react-native import.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx — migrate-gated render + sweep install</name>
  <files>App.tsx</files>
  <read_first>
    - .planning/phases/02-data-foundation-status-engine/02-RESEARCH.md §Code Example 4 installSweepTrigger (lines 534-539) + §Code Example 1 openAndMigrate
    - .planning/phases/02-data-foundation-status-engine/02-PATTERNS.md ("App.tsx (modify)")
    - App.tsx (current thin shell, lines 1-21) + src/db/database.ts (openAndMigrate) + src/services/launch-sweep.ts (installSweepTrigger)
  </read_first>
  <action>
    Modify `App.tsx` (keep the existing doc-comment convention). Before rendering HomeScreen, gate on
    `openAndMigrate()` from src/db/database.ts: run it in an effect on mount, hold a loading/ready state
    (render a minimal themed loading view while pending — resolve colours through the theme tokens, no
    hardcoded colour), and render HomeScreen once migration resolves. In a SEPARATE effect (or the same,
    after ready), call `installSweepTrigger(AppState)` — importing `AppState` from 'react-native' HERE
    (App.tsx owns the react-native binding; launch-sweep.ts stays pure) — and return its remover for
    cleanup. Do NOT call migration or the sweep at module scope. Keep it minimal; there is no CRUD/UI
    this phase.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check App.tsx && npm run check:colors</automated>
    <human-check>On the Pixel (Plan 06 build), the app launches to the home shell after migration resolves; no crash on cold start; relaunch is stable. Verified at the end-of-phase device run.</human-check>
  </verify>
  <done>App.tsx gates first render on openAndMigrate(), installs the sweep trigger from an effect with cleanup, uses no hardcoded colour, and typechecks + lints. Neither migration nor the sweep runs at module scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN lifecycle (foreground vs headless tap) → sweep runner | The sweep must fire on a real foreground launch only; headless widget/notification taps share the runtime but must not trigger it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Denial of service (headless 30s budget) | sweep on a headless tap | medium | mitigate | Sweep triggers only on a real AppState background→active transition + cold-start via installSweepTrigger, never at module top-level (P5) — a headless bundle load runs no hook. Proven by the no-import-side-effect + gating tests. |
| T-02-12 | Availability | migration on the launch path | low | accept | openAndMigrate gates first render behind a loading state; the runner's crash-safety (Plan 01) is the real mitigation — a failed migration surfaces cleanly rather than a half-built DB. |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No installs. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx biome check . && npx vitest run && npm run check:colors` green.
- launch-sweep.ts has no static react-native import — match real import lines only: `grep -cE "^import .*from ['\"]react-native['\"]" src/services/launch-sweep.ts` returns 0.
- App.tsx references openAndMigrate and installSweepTrigger (grep) and calls neither at module scope (both inside effects — reviewed).
</verification>

<success_criteria>
DATA-06 is complete: the launch sweep runs once per real foreground launch (not on a headless tap or
module import), exposes an empty hook registry for later phases, and App.tsx gates first render on
migration and installs the trigger from an effect. Device-side render is confirmed at the end-of-phase
run (Plan 06).
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation-status-engine/02-05-SUMMARY.md` when done.
</output>
