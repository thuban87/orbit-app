---
phase: 02-data-foundation-status-engine
plan: 06
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-04
  - 02-05
files_modified:
  - src/db/benchmark.ts
  - src/db/benchmark.test.ts
  - app.config.ts
  - App.tsx
autonomous: true
requirements:
  - DATA-07
must_haves:
  truths:
    - "The benchmark seeds N contacts × M interactions and times STATUS_SCAN and NEWEST_PER_CONTACT"
    - "On the physical Pixel both queries complete under the ACCEPTABLE_MS tunable threshold"
    - "date('now','localtime') on the device matches the device wall-clock local date (the owed Android/bionic probe, P6)"
    - "Android allowBackup is false in the generated release AndroidManifest (third-party PII at rest, dossier cluster G)"
  artifacts:
    - path: "src/db/benchmark.ts"
      provides: "Seed + timing harness for STATUS_SCAN / NEWEST_PER_CONTACT + a localtime probe"
      exports: ["seedBenchmarkData", "runBenchmark"]
    - path: "src/db/benchmark.test.ts"
      provides: "Node-side correctness of the seed/measure helpers"
    - path: "app.config.ts"
      provides: "android.allowBackup=false (PII-at-rest mitigation)"
  key_links:
    - from: "src/db/benchmark.ts"
      to: "src/db/queries.ts"
      via: "times the real STATUS_SCAN and NEWEST_PER_CONTACT constants"
      pattern: "STATUS_SCAN|NEWEST_PER_CONTACT"
    - from: "the release build"
      to: "the Pixel"
      via: "docs/runbooks/desktop-build-pipeline.md → adb -s 1A071FDEE002BU install com.bwales.orbit"
      pattern: "assembleRelease"
---

<objective>
Prove DATA-07 on real hardware and close the two device-only items the node-side suite cannot: the
newest-interaction-per-contact query and the status scan are acceptably fast on the Pixel, and
`date('now','localtime')` behaves on Android/bionic (the owed P6 probe). Also land the load-bearing
PII-at-rest mitigation for the datastore this phase created: `android:allowBackup="false"` in the
generated release manifest (dossier cluster G) — so the sandbox DB of third-party notes cannot be
pulled off the device via OS auto-backup / `adb backup`.

The build/test pipeline is PROVEN (STATE.md FND-01): `ssh droid` over Tailscale, tar/scp transport
(NOT git push), `gradlew assembleRelease`, `adb -s 1A071FDEE002BU install`, package `com.bwales.orbit`.
No owner bring-up is needed; follow `docs/runbooks/desktop-build-pipeline.md`.

Purpose: confirm the irreversible foundation is fast and correct on the device it ships to.
Output: `src/db/benchmark.ts` + its node-side test, the allowBackup config, and a recorded device run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-foundation-status-engine/02-RESEARCH.md
@.planning/phases/02-data-foundation-status-engine/02-VALIDATION.md
@CLAUDE.md
@docs/runbooks/desktop-build-pipeline.md
@src/db/queries.ts
@src/db/status.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Benchmark harness (seed + measure + localtime probe)</name>
  <files>src/db/benchmark.ts, src/db/benchmark.test.ts</files>
  <read_first>
    - .planning/phases/02-data-foundation-status-engine/02-RESEARCH.md §Validation Architecture (Tier B, DATA-07 row) + Pitfall P6 (localtime probe) + §Code Example 3 (the queries being timed)
    - .planning/phases/02-data-foundation-status-engine/02-VALIDATION.md (Manual-Only Verifications row)
    - src/db/queries.ts (STATUS_SCAN, NEWEST_PER_CONTACT) + src/db/status.ts + src/db/types.ts (SqlExecutor) + src/db/__testkit__/node-sqlite.ts + src/utils/logger.ts (Logger)
  </read_first>
  <behavior>
    - seedBenchmarkData(exec, {contacts, perContact, now, newUid}) inserts exactly contacts rows and contacts×perContact interaction rows (parameterized), each contact given a non-null last_contact so it is in-scope for STATUS_SCAN.
    - runBenchmark(exec) executes STATUS_SCAN and NEWEST_PER_CONTACT and returns a result object with a millisecond duration per query plus the localtime probe value (SELECT date('now','localtime')).
    - Node-side, over a modest seed, STATUS_SCAN returns one row per non-archived contact and NEWEST_PER_CONTACT returns exactly `contacts` rows (correctness, not perf).
  </behavior>
  <action>
    Create `src/db/benchmark.ts` (repo doc header, DATA-07) with top-of-file tunable constants
    `BENCH_CONTACTS`, `BENCH_PER_CONTACT`, and `ACCEPTABLE_MS` (an "acceptable" threshold — Claude's
    discretion per CONTEXT; pick a conservative value for tens-to-low-hundreds of contacts). Export
    `seedBenchmarkData(exec, opts)` that bulk-inserts contacts + interactions via parameterized INSERTs
    (reuse newUid/now injection; give each contact a last_contact so STATUS_SCAN counts it), and
    `runBenchmark(exec)` that times STATUS_SCAN and NEWEST_PER_CONTACT (wall-clock around getAllAsync),
    runs the `SELECT date('now','localtime')` probe, and logs a single structured line via Logger with
    both durations, row counts, and the probe value. It operates on the injected SqlExecutor so it runs
    both node-side (tests) and on-device (the expo db).

    Write `src/db/benchmark.test.ts` first (RED) asserting the seed row counts and that runBenchmark
    returns sane shapes/counts against the testkit + migration001 fixture (correctness only — perf is
    device-only per CLAUDE.md).
  </action>
  <verify>
    <automated>npx vitest run src/db/benchmark.test.ts</automated>
    <automated>npx tsc --noEmit && npx biome check src/db</automated>
  </verify>
  <done>benchmark.test.ts green: seedBenchmarkData produces the expected row counts and runBenchmark returns per-query durations + a localtime probe value with correct row counts. Tunables sit at top-of-file.</done>
</task>

<task type="auto">
  <name>Task 2: Android allowBackup=false (PII at rest)</name>
  <files>app.config.ts</files>
  <read_first>
    - .planning/phases/02-data-foundation-status-engine/02-RESEARCH.md §Security Domain (Third-party PII at rest row: android:allowBackup="false", dossier cluster G — "verify it is set and never fixed back")
    - CLAUDE.md "Local-first is a product commitment" (contact data never leaves the device)
    - app.config.ts lines 20-27 (current android block: package, predictiveBackGestureEnabled)
  </read_first>
  <action>
    Set Android `allowBackup=false` so the app-private SQLite file of third-party notes cannot be
    extracted via OS auto-backup / `adb backup`. Prefer the zero-dependency path: add
    `allowBackup: false` to the `android` block in `app.config.ts`. If the SDK-57 prebuild does not
    honor that key on the generated manifest (confirm during Task 3's prebuild), fall back to a small
    INLINE config plugin using `withAndroidManifest` from `@expo/config-plugins` (bundled with `expo`,
    no new dependency) to set `android:allowBackup="false"` on the `<application>` element. Do NOT
    install `expo-build-properties` or any new package. This enforces a recorded [DECIDED] mitigation
    (dossier cluster G); it is not a posture reversal.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "allowBackup" app.config.ts</automated>
    <human-check>Manifest verification is folded into Task 3 (the release prebuild generates AndroidManifest.xml on droid — assert android:allowBackup="false" there).</human-check>
  </verify>
  <done>app.config.ts (or a bundled inline config plugin) declares allowBackup=false with no new dependency; tsc clean. Manifest assertion happens in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: On-device benchmark run + localtime probe + manifest check</name>
  <files>App.tsx</files>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (the proven ssh droid / tar-scp / gradlew assembleRelease / adb install loop)
    - CLAUDE.md "Android device" section (emu-connect, adb -s 1A071FDEE002BU, reading console.log via a logcat DELTA on ReactNativeJS, never apt install adb; release build required for a legitimate perf claim)
    - .planning/STATE.md Blockers/Concerns (pipeline RESOLVED; package com.bwales.orbit; transport is scp/tar NOT git push)
    - src/db/benchmark.ts (runBenchmark) + src/db/database.ts (openAndMigrate/getDb) + App.tsx
  </read_first>
  <action>
    Run the benchmark on the physical Pixel via the proven pipeline (this is device-observable; the
    node suite already proved correctness). Steps:
    1. TEMPORARILY wire the benchmark for this build only: in App.tsx, after openAndMigrate resolves,
       call `runBenchmark(getDb())` once and let it log via Logger (release builds have __DEV__ false,
       so a flag/dev-gate will not fire — wire it unconditionally for the benchmark build, then revert).
    2. Build a RELEASE APK on droid following the runbook: commit locally (NEVER git push), tar/scp the
       tree to `droid` over Tailscale, `npm ci` + `expo prebuild --clean` (CI=1) + `gradlew.bat
       assembleRelease`, scp the APK back.
    3. While the prebuild output is on droid, assert the generated
       `android/app/src/main/AndroidManifest.xml` contains `android:allowBackup="false"` (Task 2). If it
       does not, apply the Task-2 inline config-plugin fallback and rebuild.
    4. `emu-connect` to confirm the Pixel target, `adb -s 1A071FDEE002BU install -r <apk>` (package
       com.bwales.orbit), launch, and read the benchmark log line from the ReactNativeJS channel using a
       DELTA capture (fire a probe, diff — a Metro pane can hold stale LOG lines; use whichever channel
       moved), per CLAUDE.md.
    5. Record STATUS_SCAN + NEWEST_PER_CONTACT durations, the seed size, and the `date('now','localtime')`
       probe vs the device wall-clock local date in the SUMMARY.
    6. REVERT the temporary App.tsx benchmark wiring (net-zero diff to App.tsx; benchmark.ts stays as a
       reusable harness).
    If the timings exceed ACCEPTABLE_MS, record the numbers and flag for owner review rather than
    silently loosening the threshold (perf posture is the owner's call).
  </action>
  <verify>
    <automated>grep -rn "android:allowBackup=\"false\"" android/app/src/main/AndroidManifest.xml 2>/dev/null || echo "manifest generated on droid during the release prebuild — assert there"</automated>
    <human-check>On the Pixel: STATUS_SCAN and NEWEST_PER_CONTACT durations are under ACCEPTABLE_MS at the seeded size; date('now','localtime') matches the device wall-clock local date; the generated release manifest has android:allowBackup="false". Timings + probe recorded in 02-06-SUMMARY.md. (end-of-phase human verification)</human-check>
  </verify>
  <done>A release APK built via the proven pipeline runs on the Pixel; the benchmark logs both query durations (under ACCEPTABLE_MS, or flagged to the owner) and the localtime probe matches wall clock; the generated manifest has allowBackup=false; the temporary App.tsx wiring is reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS backup / adb → app-private DB file | Third-party PII (notes about others) at rest in the SQLite sandbox |
| build config → generated AndroidManifest | The manifest attribute that governs whether the DB is backup-extractable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Information disclosure | third-party PII at rest extractable via OS auto-backup / adb backup | medium | mitigate | `android:allowBackup="false"` in the generated release manifest (dossier cluster G); DB stays in the private sandbox; no network egress on any read path (local-first). Asserted on the built manifest in Task 3. |
| T-02-14 | Tampering | benchmark seed SQL | low | accept | Seeds via parameterized INSERTs; the harness is a dev tool, not shipped wired (App.tsx wiring is reverted). |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No new package — allowBackup uses app.config or `@expo/config-plugins` (bundled with expo). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx biome check . && npx vitest run` green (benchmark harness correctness).
- The generated release AndroidManifest.xml contains `android:allowBackup="false"` (asserted on droid during the prebuild).
- Device run recorded: STATUS_SCAN + NEWEST_PER_CONTACT durations under ACCEPTABLE_MS (or flagged), and date('now','localtime') matches the Pixel wall clock.
- App.tsx benchmark wiring reverted (net-zero diff; benchmark.ts remains).
</verification>

<success_criteria>
DATA-07 is complete: the newest-interaction-per-contact query and the status scan benchmark acceptably
on the Pixel, the Android/bionic localtime behaviour is confirmed, and the datastore is protected at
rest with allowBackup=false — the irreversible foundation is verified fast and correct on the device it
ships to.
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation-status-engine/02-06-SUMMARY.md` when done, recording the
device timings, the localtime probe result, and the manifest assertion.
</output>
