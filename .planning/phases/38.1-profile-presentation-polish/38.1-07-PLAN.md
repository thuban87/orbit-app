---
phase: 38.1-profile-presentation-polish
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/profile/pack-overview.ts
  - src/profile/pack-overview.test.ts
  - src/components/profile/RelationshipOverview.tsx
autonomous: true
requirements: [R-10, R-11]
estimate:
  tokens: 46000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The user's configured module order is preserved; full-width modules stay full-width; half-width modules pair in configured sequence (§I)."
    - "A half-width module that would be orphaned on its row stretches its OCCUPIED width to fill the row instead of leaving an empty half-row (§I)."
    - "Orphan-stretch is a pure computed rendered span; it never reorders modules and never pulls later half-width modules upward across intervening full-width modules (§I / D-05 — no lookahead reordering)."
    - "Orphan-stretch is visual-only: it never mutates a card's semantic size and never overrides the user's explicit compact/wide (set-size) choice (D-05)."
    - "The existing compact/wide (set-size) toggle in ProfileLayoutEditor.tsx:149-164 is KEPT — no NEW manual width controls are added (D-05 reconciles §I/§P 'no manual width controls')."
    - "All Relationship Overview / Relationship Status card contents are centered at half AND full width, at all times (§I)."
    - { statement: "Centered content and orphan-stretch do not clip at large OS font scale (columns collapse to 1 at large scale — no orphan case there).", verification: backstop }
  artifacts:
    - src/profile/pack-overview.ts
    - src/profile/pack-overview.test.ts
    - src/components/profile/RelationshipOverview.tsx
  key_links:
    - "packOverviewModules columnSpan (with orphan-stretch) -> RelationshipOverview tileWidth = columnWidth * columnSpan + gap (existing consumer at :113-115)."
---

<objective>
Implement Relationship Overview orphan-stretch and centered card content (§I) as a pure span computation plus a render change — preserving configured order, never reordering, and never touching a card's semantic size. Planned against the authoritative dossier §I (D-01), grouped as the "Relationship Overview packing" ownership group per §Q (D-03).

This plan also RECONCILES the dossier §I/§P "no manual width controls" wording per D-05: the already-shipped compact/wide (`set-size`) toggle in `ProfileLayoutEditor.tsx:149-164` is KEPT (that file is not modified here); §I/§P mean no NEW controls. Orphan-stretch is visual-only and never overrides the user's explicit compact/wide choice.

Purpose: today a trailing 1×1 module that cannot pair leaves a blank half-row, and tiles render left-aligned. §I wants the orphan to fill the row and all card content centered.
Output: orphan-stretch in `pack-overview.ts` (+ tests); centered tile content in `RelationshipOverview.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@src/profile/pack-overview.ts
@src/profile/pack-overview.test.ts
@src/components/profile/RelationshipOverview.tsx
</context>

<artifacts_produced>
New behavior/symbols this plan introduces (source-grounding: exclude from drift verification — net-new here):
- Orphan-stretch span computation inside `packOverviewModules` (pack-overview.ts) — a computed `columnSpan` bump for a trailing/alone 1×1 when `columns >= 2`; no new exported symbol required, but a new internal branch + new test cases in pack-overview.test.ts.
- Centering styles on the Overview tile in RelationshipOverview.tsx (e.g. `alignItems: "center"` on the tile/text at half and full width).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orphan-stretch as a pure computed span (§I / D-05)</name>
  <files>src/profile/pack-overview.ts, src/profile/pack-overview.test.ts</files>
  <read_first>
    - src/profile/pack-overview.ts:42-84 — the single forward-pass packer; `declaredSpan = module.size === "2x1" ? 2 : 1`, `columnSpan = Math.min(declaredSpan, columns)` (:67-68), row-wrap logic (:69-79), and `columns = Math.max(1, Math.floor((width + gap) / (minimumWidth + gap)))` (:57) with `minimumWidth` scaling by `fontScale` (:52-56 — at large scale columns collapses to 1).
    - src/profile/pack-overview.test.ts — existing packing cases to extend.
    - RESEARCH.md §"§I packing + orphan-stretch" and PATTERNS.md §"§I" — orphan-stretch = stretch the trailing orphan only; NO lookahead reorder; never touch `module.size`.
  </read_first>
  <behavior>
    - A trailing 1×1 that ends up alone on its row (occupies column 0, span 1, and no following module pairs beside it) gets `columnSpan` widened to fill the row when `columns >= 2`.
    - Configured order is preserved exactly; no module is pulled upward across a full-width module to fill a hole (no lookahead reordering).
    - `module.size` is never mutated (visual span only).
    - When `columns === 1` (large font scale) every module is already full-width — no orphan case, no stretch applied.
    - A 1×1 that DOES pair with an adjacent 1×1 keeps span 1 (no spurious stretch).
    - New test cases cover: trailing orphan → stretched; paired 1×1s → unchanged; full-width-then-orphan → orphan stretched, full-width untouched, order preserved; single-column layout → no stretch.
  </behavior>
  <action>
    Add orphan-stretch to `packOverviewModules` as a pure computed rendered span. Detect a 1×1 placement that is alone on its row (no other placement shares the row and it cannot pair with a following module without reordering) and, when `columns >= 2`, set its `columnSpan` to fill the remaining row. Keep the single forward pass; do NOT add lookahead that REORDERS modules to fill holes (§I / D-05) — only the trailing orphan stretches. Never mutate `module.size`. Add the test cases described in <behavior> to pack-overview.test.ts. (Prefer a small post-pass over the computed placements that widens a lone trailing 1×1, so the forward-pass ordering is untouched.)
  </action>
  <reversibility rating="reversible">Pure layout math; node-tested; no data, no schema (D-02).</reversibility>
  <verify>
    <automated>npx vitest run src/profile/pack-overview.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/profile/pack-overview.test.ts` exits 0 with the new orphan-stretch cases.
    - A test asserts a trailing lone 1×1 gets `columnSpan === columns` when `columns >= 2`, and that a paired 1×1 stays `columnSpan === 1`.
    - A test asserts configured order is unchanged and no module is reordered/pulled upward.
    - A test asserts `columns === 1` produces no stretch and `module.size` is never mutated.
  </acceptance_criteria>
  <done>Orphan half-width modules stretch to fill the row as a pure span, with no reordering and no semantic-size change.</done>
</task>

<task type="auto">
  <name>Task 2: Center all Relationship Overview card content (§I)</name>
  <files>src/components/profile/RelationshipOverview.tsx</files>
  <read_first>
    - src/components/profile/RelationshipOverview.tsx:113-115 (`tileWidth = columnWidth * placement.columnSpan + SPACING.sm * (columnSpan - 1)` — consumes the span from Task 1), :125-127 (the tile `GlassSurface density="presentation"` with currently left-default text).
    - UI-SPEC §"Relationship Overview width toggle (§I / D-05)" and §"Color"/"Typography" (tiles use existing tokens; no new spacing values).
  </read_first>
  <action>
    Center the contents of every Relationship Overview / Relationship Status tile at BOTH half and full width (align the tile's content and text to center). Compose only from existing spacing/typography tokens (no new spacing values — UI-SPEC). Do not change tile data or the width-toggle behavior; centering applies whether the tile occupies a half or a stretched full width. Confirm the last-interaction tile (which shows the reformatted timestamp string produced by Plan 04's read-layer change) still lays out correctly when centered.
  </action>
  <reversibility rating="reversible">Style-only render change; no data, no schema.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <human-check>On the Pixel, construct Overview arrangements covering multiple full/half combinations INCLUDING an orphan card: the orphan fills the row, and every card's content is centered at both half and full width. Then raise the OS font scale to the largest setting and confirm centered content + stretching do not clip (§R item 11). (Batched into Plan 09 end-of-phase device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - Reading RelationshipOverview confirms tile content is center-aligned for both half and full (stretched) widths, using existing tokens only.
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - Device UAT (Plan 09) confirms no clipping at large font scale and centered content across orphan/paired arrangements (backstop).
  </acceptance_criteria>
  <done>All Overview card content is centered at half and full width; the large-font/clip check is queued for device UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout math → rendered tiles | Incorrect span/centering is a visual defect (clipping/misalignment), not a data or trust breach. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-07-01 | Tampering | pack-overview span math | low | mitigate | Pure, node-tested; orphan-stretch never mutates `module.size` or reorders (D-05); unit cases cover paired/orphan/single-column. |
| T-38.1-07-02 | Denial of service (UX) | large-font clipping | low | mitigate | Columns collapse to 1 at large font scale (no orphan case); device UAT backstop verifies no clipping (§R-11). |
| T-38.1-07-03 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); layout + style only. |

No high-severity threats.
</threat_model>

<verification>
- `npx vitest run src/profile/pack-overview.test.ts` green (orphan-stretch cases).
- `npx tsc --noEmit` and `npm run check:colors` clean.
- Centering + large-font clip check queued for Plan 09 device UAT (backstop).
</verification>

<success_criteria>
- Orphan half-width modules stretch to fill their row (pure span, no reorder, no semantic-size change), the existing compact/wide toggle is kept, and all Overview card content is centered at half and full width.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-07-SUMMARY.md` when done. Explicitly record the D-05 reconciliation: the compact/wide toggle is retained and orphan-stretch never overrides it.
</output>
