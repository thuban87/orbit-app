---
phase: 38.1-profile-presentation-polish
verified: 2026-09-20T23:28:26Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 8
  total: 8
  not_honored: []
owner_waivers:
  - check: "Profile no-data physical-UAT fixture"
    disposition: OWNER-WAIVED
    accepted_by: owner
    accepted_at: 2026-09-20
    reason: "No eligible no-data fixture was exercised. This is not a passing test."
  - check: "Off Limits on-device SQLite preservation backstop"
    disposition: OWNER-WAIVED
    accepted_by: owner
    accepted_at: 2026-09-20
    reason: "The debug-only run-as procedure lacked a supported four-kind fixture path. This is not a passing test."
---

# Phase 38.1: Profile & Presentation Polish Verification Report

**Phase Goal:** A bounded presentation-polish pass across Orrery, Contact Profile, Interaction History, and Contacts before Phase 39, with no new relationship-domain concepts or schema/backup-format change.

**Verified:** 2026-09-20T23:28:26Z  
**Status:** passed — by code evidence, current automated checks, owner release-APK acceptance, and the two explicit owner waivers below.  
**Re-verification:** No — initial formal verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Orrery overlays have a dedicated translucent, AA-tested surface treatment rather than ordinary card opacity. | VERIFIED | `surface.ts` defines package-specific `orreryOverlayTintOpacity`; `GlassSurface` selects it only for `treatment="orrery-overlay"`; all named Orrery panels use that treatment. `surface.test.ts` is in the current focused suite. |
| 2 | Orrery navigation is three uniform icon-only accessible controls, grouped in the obstacle-aware right column. | VERIFIED | `OrreryControls.tsx` renders three 55x55 `GlassSurface` controls with the required labels, 4px gap, bounded `ScrollView`, and `alignItems: "flex-end"`; obstacle logic enforces a 55px minimum. Current render/logic tests passed. |
| 3 | Profile headings are identifiers; state is in bodies; the top-level Things to Remember edit dead route is absent. | VERIFIED | `ProfileModuleHost.tsx` emits normal captions only for pending/error, renders child `bodySummary` in `ThingsToRemember.tsx`, and has no top-level Things-to-Remember header action. Current render contract exercises the routed child actions. |
| 4 | Temporal knowledge has non-duplicated Most Recent/Previous presentation, a five-item preview, and owned edit destinations. | VERIFIED | `profile-knowledge-read.ts` selects current rows separately and caps prior rows with `is_current = 0 ... LIMIT 5`; `knowledge-presentation.ts` presents Most Recent then Previous; `ContactProfileScreen.tsx` routes memory/current-state, Off Limits, relationship, and custom-field actions to their owners. Custom Fields currently routes to per-contact `Edit`, not Settings. |
| 5 | Off Limits editing remains collection-scoped and does not code-path-clobber other fuel kinds. | VERIFIED | `OffLimitsEditorScreen.tsx` wires a controller whose read filter, forced write kind, allowed-id check, and DAO calls are all in `off-limits-editor-controller.ts`. The current mixed-kind controller test exercises add/edit/delete and asserts preservation of recent/topic/fact/gift rows. |
| 6 | Unbind moved from the Profile body to bound-only overflow without changing lifecycle confirmation semantics; Unbound keeps Bind. | VERIFIED | `profileOverflowEntries()` gates `unbind` on `bound`; `ContactProfileScreen.tsx` reuses `unbindConfirmation` and `transitionLifecycle("unbind")`; current logic tests cover bound/unbound entry sets. |
| 7 | Relationship Overview preserves semantic order/size, stretches row-local compact orphans, and centers contents. | VERIFIED | `pack-overview.ts` derives `columnSpan` after forward packing without mutating `size`; its focused tests cover trailing, mid-row, paired, and responsive cases; `RelationshipOverview.tsx` provides centered tile content. |
| 8 | The Year heatmap remains interactive but uses vertical rows of two adjacent whole weeks, with no horizontal heatmap scroller. | VERIFIED | `ActivityHeatmap.tsx` builds `yearWeekPairs` and reuses `renderDayCell`; the current render test asserts two weeks in a vertical row and no horizontal `ScrollView`. |
| 9 | Explicit timestamp displays use one minute-precision, local-wall-clock formatter and never expose raw invalid storage text. | VERIFIED | `dates.ts` uses `parseLocalMs`, exported pure 12h/24h clock formatting, and `Unknown time` fallback; the three audited consumers call it. Current date and presentation tests passed. |
| 10 | Normal Contacts Grid omits its routine third line while List and search-context behavior remain available. | VERIFIED | `GridCard.tsx` renders the third line only in search mode; `HomeScreen.tsx` computes routine `line3` only for List mode. Current Grid render tests cover normal Grid, search Grid, and List behavior. |
| 11 | The bounded §O audit and release deliverable are complete without unrelated data/model changes. | VERIFIED | `38.1-UAT.md` records all four audit classes as pass and the release-APK owner results. Read-only inspection of the retained Droid artifact found 202,241,490 bytes and SHA-256 `24D6B0BE7B5A384B615A1C06ABBA60FB1D9A1D1F5C0976CAF28A47AD8766F821`, matching the recorded release evidence. |

**Score:** 11/11 truths verified (0 present-but-behavior-unverified).

### Required Artifacts and Key Links

The phase’s PLAN frontmatter uses shorthand artifact/key-link entries, which the generic artifact query does not parse; these were checked directly rather than treated as an empty automatic pass. All are present, substantive, and wired:

| Area | Artifact / link | Status |
| --- | --- | --- |
| Theme → Orrery | `GlassSurface treatment` → `orreryOverlayTintOpacity` → named panel call sites | VERIFIED |
| Profile → navigation | child header action → `profileKnowledgeDestination` → `ContactProfileScreen` route | VERIFIED |
| Off Limits → SQLite | screen callbacks → injected controller → kind-forced DAO inputs | VERIFIED |
| Current state → Profile | `is_current` queries → presentation model → rendered temporal groups | VERIFIED |
| History → display | three timestamp consumers → shared safe formatter | VERIFIED |
| Dashboard → Grid | host list-only routine selection → Grid search-only third line | VERIFIED |

### Data-Flow Trace

| Rendered value / behavior | Source | Status |
| --- | --- | --- |
| Temporal Most Recent / Previous | `current_state_entries` queries in `profile-knowledge-read.ts` | FLOWING |
| Off Limits list and mutations | `listFuelForEditor` plus `fuel-dao` through the controller | FLOWING |
| History timestamps | stored timestamp → `parseLocalMs` → minute display formatter | FLOWING |
| Grid search explanation | `searchResultsByContactId` / `searchSnippet` props | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Phase-owned render, route, state, and controller contracts | `npx vitest run` with 12 phase-focused files | 12 files / 140 tests passed | PASS |
| Type correctness | `npx tsc --noEmit` | exit 0 | PASS |
| Theme-token colour guard | `npm run check:colors` | exit 0 | PASS |

The Plan 09 record also documents the completed full-suite run (411 files / 3,840 tests), but this report does not rely on that narrative in place of the focused current-code checks above.

### Requirements Coverage

`ROADMAP.md` intentionally defers formal requirement IDs for Phase 38.1 to the dossier’s 15-item checklist. The phase plans use local `R-01` through `R-15` labels that are not entries in the legacy `REQUIREMENTS.md`; there are therefore no orphaned formal requirements to map. The dossier contract is covered by the eleven observable truths above and the owner UAT record.

### Test Quality and Anti-Patterns

| Check | Result |
| --- | --- |
| Disabled/skipped tests in the phase-linked test set | None found |
| `TBD` / `FIXME` / `XXX` debt markers in the phase-owned production files | None found |
| Mixed-kind Off Limits assertion strength | Behavioral: add, edit, delete, forced kind, scoped ID/contact, refresh, and preservation are asserted against a five-kind fixture |
| Decision coverage gate | 8/8 trackable CONTEXT decisions honored |

### Owner-Accepted Waivers — Not Test Passes

Two physical-UAT cells remain intentionally **not run/passed**:

| Cell | Disposition | What it does and does not establish |
| --- | --- | --- |
| Profile no-data fixture | OWNER-WAIVED | No eligible fixture was exercised; no claim is made that this cell passed. |
| Off Limits on-device DB preservation backstop | OWNER-WAIVED | The debug-only `run-as` SQLite procedure was not run because the shipped UI cannot create the needed four-kind fixture; the controller regression test remains evidence for the code path, but not a substitute for that device backstop. |

On 2026-09-20 the owner explicitly authorized closure with these exact waivers. All other release-APK UI items were owner-accepted after the in-scope fixes in `44c6e32`, `02c643c`, and `fc284d0`.

## Verification Conclusion

No unwaived implementation, wiring, test-quality, or scope gap blocks the stated Phase 38.1 goal. The bounded audit found no residual in-class defect, the latest regressions are present in the live code and covered by current focused tests, and the retained release APK matches the recorded artifact hash. Phase 38.1 is ready to close under the owner’s explicit waiver decision.

_Verifier: gsd-verifier_
