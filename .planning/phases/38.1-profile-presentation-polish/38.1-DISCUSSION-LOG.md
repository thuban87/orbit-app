# Phase 38.1: Profile & Presentation Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 38.1-profile-presentation-polish — Profile & Presentation Polish (INSERTED)
**Areas discussed:** Orrery overlay legibility, Layout width toggle conflict, Profile editing routes, Timestamp display format (+ time-format setting sequencing)

> The dossier locks the bulk of the phase (§A–§R `[DECIDED]`). Discussion covered only the genuine
> open items surfaced by a subsystem-level grounding audit of the post-Phase-38 repo.

---

## Orrery overlay surface (§B/§C)

| Option | Description | Selected |
|--------|-------------|----------|
| Stay translucent always | One Orrery overlay treatment reads consistently in every theme; the orrery canvas is a controlled render surface, not mismatched wallpaper. AA-proven against the backdrop. | ✓ |
| Fall back to solid in mismatched mode | Mirror ordinary-card behavior (opaque when package art tone ≠ resolved mode). | |

**User's choice:** Stay translucent always.
**Notes:** Must be a NEW named token in `surface.ts`, AA-proven per `surface.test.ts`, decoupled from ordinary content-card opacity — not a `GlassSurface` fork, not a change to `CARD_GLASS_OPACITY`. Rides on ADR-115, not a reversal. → CONTEXT D-04.

---

## Relationship Overview width toggle (§I/§P)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it | The compact/wide toggle is already shipped; §I "don't add" = no NEW controls. Orphan-stretch stays visual-only (line 167) + centering. | ✓ |
| Remove it | Fully-automatic width; users lose the compact/wide choice; added scope (remove UI + reducer + tests). | |

**User's choice:** Keep it.
**Notes:** Planner reconciles §I/§P wording to record the existing toggle is retained; orphan-stretch never overrides an explicit compact choice's semantic size. → CONTEXT D-05.

---

## Profile editing routes (§G/§O)

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse existing FuelEditor | Route the Off Limits edit icon to the existing `FuelEditor` — no new editor/model. | ✓ |
| Dedicated Off Limits screen | Build a new focused editor screen (more work; scope risk). | |
| You decide in planning | Delegate the routing mechanism. | |

**User's choice:** Reuse existing FuelEditor.
**Notes:** Same "reuse existing editor for the whole collection" rule applies to the other thin §G routes. → CONTEXT D-06.

---

## Timestamp display format (§K)

| Option | Description | Selected |
|--------|-------------|----------|
| 12-hour: `Sep 19, 2026, 7:14 PM` | Matches the dossier example; friendliest for a consumer app. | ✓ (as default) |
| 24-hour: `Sep 19, 2026, 19:14` | More compact, no AM/PM. | |

**User's choice:** "Can we make this user-choosable in settings? Default to the 12-hour standard though."
**Notes:** Follow-up decision below on how to sequence the user-choosable setting.

### Follow-up — time-format setting sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Split: fix now, setting next | 38.1 ships the app-wide fix defaulting to 12-hour (formatter reads format from one source); user-choosable toggle becomes its own phase (38.2) / Settings pass. Keeps 38.1 no-schema. | ✓ |
| Include the setting in 38.1 | Accept a migration + backup-format bump + Settings row inside 38.1 (reverses the phase guardrail). | |
| Choosable but local-only | Non-portable pref; won't survive backup/restore. | |

**User's choice:** Split: fix now, setting next.
**Notes:** The migration + `BACKUP_FORMAT_VERSION` bump + Settings row cost was surfaced explicitly; owner chose to keep 38.1 a clean no-schema polish phase and defer the toggle. → CONTEXT D-08 + Deferred.

---

## Claude's Discretion
- Exact Orrery overlay tint value (D-04) — I pick + AA-prove, tuned on-device.
- Off Limits edit-route reuse mechanism (D-06) — lowest-risk reuse of `FuelEditor`.
- Plan decomposition/grouping (D-03) — within dossier §Q guidance.

## Deferred Ideas
- User-choosable 12/24-hour time-format setting → own phase (candidate 38.2) / Settings pass (needs migration + backup bump).
- Dossier §P explicit deferrals (unchanged).
- Reviewed-not-folded todo: `2026-08-26-validate-restore-progress-with-imported-photo-library.md` (unrelated; generic keyword match).
