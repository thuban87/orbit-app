---
phase: 38.1-profile-presentation-polish
reviewers: [codex, claude]
reviewed_at: 2026-09-19
plans_reviewed: [38.1-01-PLAN.md, 38.1-02-PLAN.md, 38.1-03-PLAN.md, 38.1-04-PLAN.md, 38.1-05-PLAN.md, 38.1-06-PLAN.md, 38.1-07-PLAN.md, 38.1-08-PLAN.md, 38.1-09-PLAN.md]
models:
  codex: "gpt-5.6-sol (reasoning=low)"
  claude: "claude-opus-4-8"
model_sources:
  codex: "banner"
  claude: "self"
cycle_summary:
  current_high: 5
  current_actionable: 13
---

# Cross-AI Plan Review — Phase 38.1

Two independent source-grounded lanes: **codex** (gpt-5.6-sol) via gsd-review, and **claude** (opus-4-8) reviewing the code on disk directly. Both lanes opened the referenced files and verified claimed file:line references and behavior. The two lanes converged strongly.

## Consensus Summary

The plan set is **architecturally sound and fully respects the phase's non-negotiable boundaries** — no migration/schema/backup-format change (D-02), no network on any read path (local-first), colours stay token-owned, animation stays on Reanimated (no React-state driving), and Plan 05→06 correctly serialize the shared Profile files. **No plan deletes, weakens, or inverts an ADR, HANDOFF, or D-NN decision** — every HIGH finding below is an *executable-correctness* defect caught pre-execution, not a decision reversal. That is the good news.

The bad news: several plans describe mechanisms that **do not match the code on disk**, and would fail at execution as written. Overall risk is **HIGH until the five blockers are corrected.**

### Agreed Strengths (both lanes)
- D-02 respected everywhere: no migration, no schema, no `BACKUP_FORMAT_VERSION` bump. Plan 06's temporal read is a bounded SELECT against the existing `current_state_entries` table (verified: it already carries `is_current` + `created_at`, `current-state-history-read.ts:9-18,67-79`).
- The D-07 false empty-state bug is real and correctly located: `ProfileModuleHost.tsx:113-116` returns `PROFILE_MODULE_EMPTY_SUMMARIES` unconditionally, unlike the conditional `contact-methods` sibling at `:117-120`.
- The Off Limits data-loss guard being ported is real and load-bearing (`EditContactScreen.tsx:289-296` forced kind, `:520` seed filter, `:804-808` defensive-re-filter comment).
- Timestamp foundation is correct: `parseLocalMs` (`dates.ts:25-72`) is UTC-safe; the three display offenders are accurately located (`profile-history-read.ts:72,81`; `interaction-detail-logic.ts:96`; `MemoryHistoryScreen.tsx:172`).
- Orrery surface seam (Plan 01/03) is correct: `GlassSurface.tsx:66` obtains opacity via `cardTintOpacity`; the treatment prop preserves the single ADR-087 component.

### Agreed Concerns (2+ lanes — highest priority)
1. **Plan 06 (HIGH):** the new `OffLimitsEditorScreen` route is added only to `navigation/types.ts` (typed), never registered as a `<Stack.Screen>` in the navigators that host Profile → route unnavigable, runtime throw.
2. **Plan 06 (HIGH):** the off-limits preservation test is placed at `fuel-dao.test.ts`, but the DAO is kind-agnostic; the data-loss control lives in the *screen*, so a DAO test cannot prove it.
3. **Plan 08 (HIGH):** `selectLine3` is the **List** selector; the Grid uses `selectCardLine3`, and `GridCard` consumes a precomputed `line3` prop (never calls a selector). The plan targets the wrong function and omits the host that computes `line3`.
4. **Plan 07 (HIGH):** orphan-stretch is scoped to the "trailing" orphan in a 2-column mental model, but `pack-overview` yields up to 3 columns and the dossier requires *any* row-local orphan (incl. before an intervening full-width card) to stretch.
5. **Plan 05 (HIGH):** the plan's verification names a nonexistent test file and proposes to unit-test private functions.

### Divergent Views
- **Plan 09 autonomy:** codex rated "cannot be reliably autonomous" (needs a physical Pixel + owner USB-auth) as HIGH. In *this repo's* context that is only MEDIUM — the owner has a documented agent-driven device pipeline (MEMORY: verify-ui-on-pixel-yourself, device-UAT run-as pattern). The residual actionable part is the `files_modified: []` vs declared outputs conflict, and framing the device leg as a checkpoint.
- **Plan 09 Metro port:** codex flagged the "8082 remap" as an unverified assumption. This is a **codex false-positive** — the port remap is a documented repo fact (MEMORY: orbit-metro-port-8082-remap; quest-board owns :8081). No plan change needed for the port.

---

## Codex Review

*(Lane A — gpt-5.6-sol reasoning=low, source-grounded. `graph:ask` could not run because tsx was denied `/tmp/tsx-1000/...pipe`; codex fell back to direct source + ADR/dossier inspection. Not a timeout — full review returned.)*

### Overall assessment
The plan set covers the dossier comprehensively and respects the phase's no-schema, local-first, token-only boundaries. Decomposition and dependency waves are sound. However the plans are not execution-ready: Plan 06 has a blocking navigation-registration gap and test/file-ownership omissions; Plans 05, 07, and 08 describe mechanisms that do not match the current code structure. Overall risk is HIGH until corrected.

### Plan 01 — Orrery surface token foundation — Risk LOW–MEDIUM
- Strengths: coupling is exactly as described (`GlassSurface.tsx:60-66` → `cardTintOpacity`); constants left alone (galaxy `0.05`/standard `0.5` at `surface.ts:151-156`); extends the existing composite-AA test (`surface.test.ts:177-205`).
- MEDIUM — opacity contract underspecified for blur: `GlassSurface` can render a real `BlurView` beneath the tint (`GlassSurface.tsx:87-107`); the proof models tint over the raw brightest pixel (conservative for averaging blur) but should state that assumption.
- LOW — "strictly less than the opaque density band" proves numeric translucency, not perceptual visibility (`surface.ts:197-225`). Add a visibility-floor/backdrop-contribution assertion.

### Plan 02 — Shared timestamp formatter — Risk LOW
- Strengths: `parseLocalMs` validates local components without UTC (`dates.ts:24-71`); avoids `toISOString()`; pure/node-safe.
- LOW — the 24h branch is not meaningfully testable if the format source stays a private compile-time constant; put formatting behind an internal helper taking `"12h"|"24h"` and expose only `formatDateTimeMinute()`.
- LOW — locale-independence should be explicit: define English month abbreviations in-module / pass explicit `Intl` options rather than relying on ambient defaults.

### Plan 03 — Orrery overlay application and controls — Risk MEDIUM
- Strengths: combined card really holds all three controls (`OrreryControls.tsx:89-142`); labels/disabled/`animatedProps` present (`:94-139`); 44×44 met (`:150-157`); `OrreryContactsSheet` exclusion reasonable.
- MEDIUM — removing the enclosing `ScrollView` needs overflow validation: controls are scrollable within the measured obstacle region (`OrreryControls.tsx:89-93`); three independent surfaces may overflow at constrained height / display scaling. Preserve a constrained/scroll group container.
- LOW — wrapping a `Pressable` in each `GlassSurface` doesn't auto-make the outer visual square; apply square dims to the correct layer. Verify focus restoration targets Contacts after restructuring.

### Plan 04 — Heatmap orientation and timestamps — Risk LOW–MEDIUM
- Strengths: Year uses horizontal `ScrollView` + week columns (`ActivityHeatmap.tsx:345-364`); Month supplies the vertical week-row pattern (`:365-382`); cell behavior centralized in `renderDayCell` (`:196-232`); `MemoryHistoryScreen` renders raw `created_at` (`:169-172`).
- MEDIUM — the automated heatmap check doesn't test orientation (`heatmap-cell.test.ts` proves classification only); add a source/render assertion that the Year branch no longer contains a horizontal scroll.
- LOW — raw fallback conflicts with D-08: falling back to the raw stored timestamp on parse failure re-exposes seconds; use a neutral "Unknown time" and centralize as `formatDateTimeMinuteOrFallback`. Keep malformed values in logs, not user-facing output.

### Plan 05 — Profile headings and Unbind relocation — Risk HIGH (until test/model design corrected)
- Strengths: false heading metadata confirmed (`ProfileModuleHost.tsx:113-116`); parent heading+caption (`:165-170`) vs child heading+summary (`ThingsToRemember.tsx:180-188`); Unbind body action + confirmation exactly at `ContactProfileScreen.tsx:378-396`; confirmation copy centralized (`contact-profile-logic.ts:151-159`).
- HIGH — proposed tests cannot exercise the named summary functions: `topLevelSummary`/`knowledgeChildSummary` are private in `ProfileModuleHost.tsx:62-128`; the plan says `module-host-model` tests will assert them, but they are not in that module.
- MEDIUM — the named test file does not exist: there is no `src/profile/module-host-model.test.ts`; the real consumer is `module-registry.test.ts:1-44`, which imports and asserts `PROFILE_MODULE_EMPTY_SUMMARIES`. The plan's files/verify commands omit this.
- MEDIUM — removing `knowledge-presentation.summary()` wholesale risks removing legitimate body state (that summary belongs to the nested body at `ThingsToRemember.tsx:184-188`). D-07 requires moving state into the body, not deleting all counts/empty states. Separate `headerSummary` (remove) from `bodySummary` (keep where useful).
- LOW — error handling is conflated with heading metadata: an error rendered as a header caption is still content-state metadata; relocate loading/error/save states into explicit body/status regions.

### Plan 06 — Temporal history and functional editors — Risk HIGH
- Strengths: current Profile read truly selects only `is_current=1` (`profile-knowledge-read.ts:108-128`); full history exists deterministically in the same table (`current-state-history-read.ts:67-78`); `MemoryHistoryScreen:75-78` already excludes current from previous; Off Limits guard is real (forced kind `EditContactScreen.tsx:284-296`, seed filter `:500-530`, guarded mutator `:804-819`); no migration required.
- HIGH — the new route is added only to `navigation/types.ts`, not any navigator. A typed route is not navigable until registered. Profile is hosted in Dashboard/Orrery/Digest/Events/Settings: `DashboardStack.tsx:45-52`, `OrreryStack.tsx:56-66`, `DigestStack.tsx:26-44`, `EventsStack.tsx:26-44`, `SettingsStack.tsx:235-248`. None is in Plan 06's `files_modified`.
- HIGH — `SettingsStack` does not register `Edit`: it hosts Profile (`:235-236`) but its edit destinations are only ThingsToRemember + MemoryHistory (`:240-248`). Routing key-people/custom-fields to `"Edit"` will fail from a Settings-originated Profile.
- HIGH — the claimed preservation test doesn't test the new screen guard: `fuel-dao.test.ts` proves DAO scoping, but the DAO already accepts arbitrary kinds; a DAO test cannot prove `OffLimitsEditorScreen` ports all three guards.
- MEDIUM — declared files omit `knowledge-presentation.test.ts` and `fuel-dao.test.ts` though the tasks modify them.
- MEDIUM — route infra tests need updates: `shell-contract.ts:11-49` and `focused-route-classification.test.ts:90-109` enumerate destinations/presentation screens.
- MEDIUM — "PREVIOUS+1" is unexplained; querying six only makes sense for a truncation/"more" check the plan doesn't define. Use `LIMIT 5` unless a sixth row feeds a named affordance.
- MEDIUM — destructive delete confirmation missing from the new editor plan (§G requires normal confirmation where appropriate).

### Plan 07 — Relationship Overview packing — Risk HIGH
- Strengths: packing is pure/deterministic (`pack-overview.ts:42-84`); renderer consumes `columnSpan` (`RelationshipOverview.tsx:123-125`); `module.size` copied not mutated; single-column collapse already tested (`pack-overview.test.ts:57-73`).
- HIGH — "trailing orphan only" is narrower than the dossier: a half-width card can be orphaned before an intervening full-width card (`[1x1, 2x1, 1x1]`), not only as the final placement (dossier `:154-167`).
- MEDIUM — code supports three columns but the plan reasons in half/full rows (`pack-overview.test.ts:29-54` produces `columns===3`); define whether "full width" means two semantic units or all computed columns.
- MEDIUM — a post-pass must identify every row-local orphan, not merely the final placement.

### Plan 08 — Contacts Grid density — Risk HIGH (LOW after aligning to the real boundary)
- Strengths: Grid normal renders the third line (`GridCard.tsx:341-358`); Grid search has a separate branch (`:305-340`); List independently retains its third line (`ListRow.tsx:234-248`).
- HIGH — `selectLine3()` does not know Grid/List/search context (signature is candidates/id/name/date at `list-row-selection.ts:77-82`).
- HIGH — `GridCard` does not call `selectLine3()`; it receives a precomputed `line3` prop (`:128-140`), and search bypasses that prop entirely (`:151-165,305-340`).
- MEDIUM — changing `selectLine3()` can affect List (selection happens upstream, contract shared); the plan doesn't modify the upstream caller that would supply context.
- LOW — over-engineered: the dossier is satisfied by GridCard omitting `line3` in non-search mode while ListRow keeps consuming it. Prefer the minimal design + component render tests (Grid-normal, Grid-search, List-normal).

### Plan 09 — Audit and physical-device UAT — Risk MEDIUM–HIGH
- Strengths: audit correctly bounded to the four defect classes; full tests/tsc/colors form a suitable gate; on-device DB check for Off Limits is appropriate; package confirmed `com.bwales.orbit` (`app.config.ts:70-75`).
- HIGH — cannot be reliably autonomous: needs a physical Pixel, owner USB auth, a debug build, device setup. *(See Divergent Views — treated as MEDIUM in this repo's agent-device context.)*
- MEDIUM — assumes Metro port 8082 without source-backed confirmation. *(Codex false-positive — repo memory confirms the 8082 remap.)*
- MEDIUM — `files_modified: []` conflicts with its outputs (`38.1-UAT.md`, `38.1-09-SUMMARY.md`) and authorized fixes.
- MEDIUM — "fix any residual offender" is too open for a verification plan; prefer a gap-closure plan unless trivial, and re-run targeted tests after any audit fix.
- LOW — screenshot/UI-tree evidence should use synthetic contacts / stay uncommitted (third-party PII).

### Codex required corrections
1. Fix Plan 06 navigation registration across every stack hosting Profile. 2. Add a test at the Off Limits screen/adapter boundary, not merely the DAO. 3. Rewrite Plan 08 around the actual GridCard/ListRow prop boundary. 4. Expand Plan 07's orphan algorithm to row-local orphans + resolve 3-column semantics. 5. Correct Plan 05's test ownership and preserve body-level state independently from header metadata. 6. Make Plan 09 device-dependent rather than autonomous; drop the unverified Metro-port assumption. 7. Reconcile every plan's `files_modified` with the files its tasks/tests actually change.

---

## Claude Review

*(Lane B — claude-opus-4-8, read the actual code on disk per the repo's "review the code, not the diff" mandate. Independently verified every codex file:line claim below against the files; independently discovered the Plan 08 selector mismatch before reading codex.)*

### Verification of codex's HIGH claims (all confirmed against code on disk)
- **Plan 06 navigator gap — CONFIRMED.** `DashboardStack.tsx:36-65` registers every screen via `<Stack.Screen name=... component=.../>` (Profile:45, ThingsToRemember:47, MemoryHistory:51, Edit:52). A new `OffLimitsEditorScreen` requires an explicit `<Stack.Screen>` in **each** stack hosting Profile. Plan 06 `files_modified` lists only `src/navigation/types.ts` — adding to the type map alone leaves the route unnavigable (React Navigation throws on an unregistered `navigate`). `SettingsStack.tsx:235-248` registers Profile/ThingsToRemember/MemoryHistory but **no `Edit` screen** — so Plan 06's key-people/custom-field routing to `"Edit"` will throw from a Settings-originated Profile.
- **Plan 06 preservation test at wrong layer — CONFIRMED.** The guard triad lives in the screen (`EditContactScreen.tsx:289-296` forced kind, `:520` seed filter, `:804-808` defensive re-filter). `fuel-dao.addFuel/editFuel/deleteFuel` are kind-agnostic, so `fuel-dao.test.ts` cannot prove `OffLimitsEditorScreen` scopes writes. The T-38.1-06-01 HIGH data-loss control is asserted at a layer that cannot fail the way the threat describes. A screen/adapter-level test is required. (Best fix: extract a pure `offLimitsDraftAdapter` shared by both screens and unit-test filter/forced-kind/preservation directly.)
- **Plan 05 private-function tests — CONFIRMED.** `topLevelSummary`/`knowledgeChildSummary` are module-local in `ProfileModuleHost.tsx:62-128` (not exported, not in `module-host-model.ts`). `src/profile/module-host-model.test.ts` **does not exist** (`ls` confirms); the real consumer of `PROFILE_MODULE_EMPTY_SUMMARIES` is `module-registry.test.ts`. Plan 05's `verify` command `npx vitest run src/profile/module-host-model.test.ts` targets a nonexistent file and asserts unimportable functions.
- **Plan 07 orphan/columns — CONFIRMED.** `pack-overview.ts:42-84` is a single forward pass; `pack-overview.test.ts:34` asserts `columns).toBe(3)`. Dossier §I (`:154-167`) requires any orphaned half-width card to stretch and forbids pulling later cards across full-width modules — so `[1x1, 2x1, 1x1]` orphans the FIRST half, not only a trailing one. Plan 07's "trailing 1×1" + half/full framing under-covers mid-sequence orphans and the 3-column reality.
- **Plan 08 selector mismatch — INDEPENDENTLY CONFIRMED.** `HomeScreen.tsx:772` selects `query.viewMode === "card" ? selectCardLine3 : selectLine3`. The Grid (card) view uses **`selectCardLine3`** (`src/logic/card-line3-selection.ts:93`); `selectLine3` (`list-row-selection.ts:77`, doc-labelled "Dashboard **List** line-3 selection") feeds the List. `GridCard` consumes a precomputed `line3` prop (`GridCard.tsx:128,342-357`) built by the host in `HomeScreen.tsx:1821` / `CardGrid.tsx:148` — it never calls a selector. Plan 08 modifies only `list-row-selection.ts` + `GridCard.tsx`, so gating `selectLine3` would (a) miss the Grid entirely and (b) risk the List the dossier says to keep. The host wiring is absent from `files_modified`.

### Lane-B-specific notes
- **Plan 04 (LOW):** reformatting `latest` at `profile-history-read.ts:72` mutates both `summary.text` (`:81`) and the structured `summary.lastContact` field (`:80`). I traced consumers: `RelationshipOverview.tsx:77` displays `summary.text`; `ProfileHistorySummary.lastContact` has **no other non-test consumer** (the `lastContact` hits in `src/types.ts` are an unrelated `Date`-based recency helper). So the risk is LOW, but the plan should reformat only the display `text` and keep `summary.lastContact` raw to avoid semantic drift and test churn.
- **No decision reversals anywhere.** I checked each plan against D-02/D-04/D-05/D-06/D-07/D-08 and the cited ADRs (087/115/062/118/108/109/123/120/121). All are respected: D-05 explicitly keeps the `set-size` toggle (`ProfileLayoutEditor.tsx:149-164`, verified present); D-02 holds (no migration needed — the temporal read is a bounded SELECT on the existing table). The HIGH findings are plan/code mismatches, not owner-bucket reversals.

---

## Aggregated Findings by Severity

### HIGH (5 — all unresolved, none a decision reversal)
- **H1 · Plan 06** — new `OffLimitsEditor` route added only to `navigation/types.ts`, not registered as `<Stack.Screen>` in the 5 stacks hosting Profile (+ `SettingsStack` lacks an `Edit` screen for key-people/custom-field routing). Routes typed but unnavigable → runtime throw.
- **H2 · Plan 06** — off-limits preservation test placed at the kind-agnostic `fuel-dao.test.ts`; the data-loss guard lives in the screen. Needs a screen/adapter-level test (ideally a shared pure `offLimitsDraftAdapter`).
- **H3 · Plan 08** — targets `selectLine3` (List selector) and omits the host; Grid uses `selectCardLine3` and `GridCard` consumes a precomputed `line3` prop. Mechanism doesn't match the data flow.
- **H4 · Plan 07** — orphan-stretch under-specified: handles only the trailing orphan in a 2-col model, but columns can be 3 and the dossier requires every row-local orphan (incl. before an intervening full-width card) to stretch.
- **H5 · Plan 05** — verification names a nonexistent test file (`module-host-model.test.ts`) and proposes to unit-test private functions (`topLevelSummary`/`knowledgeChildSummary`). Wrong test target; real consumer is `module-registry.test.ts`.

### MEDIUM
- Plan 05 — separate `headerSummary` (remove) from `bodySummary` (keep counts/empty-state in body); don't delete `knowledge-presentation.summary()` wholesale (D-07 = move-to-body, not delete-all).
- Plan 06 — `files_modified` omits `knowledge-presentation.test.ts` + `fuel-dao.test.ts`; route-contract (`shell-contract.ts`) + `focused-route-classification.test.ts` need the new route.
- Plan 06 — clarify "PREVIOUS+1" (why 6 for a 5-cap) or use `LIMIT 5`.
- Plan 06 — add destructive-delete confirmation to the Off Limits editor (§G).
- Plan 04 — add a source/render assertion that the vertical Year branch has no horizontal `ScrollView` (classification test doesn't cover orientation).
- Plan 04 — neutral fallback on parse failure instead of the raw string (raw re-exposes seconds, violates §K); centralize as `formatDateTimeMinuteOrFallback`.
- Plan 03 — preserve a constrained/scroll group container around the three controls (avoid overflow at large font/constrained height); apply square dims to the correct GlassSurface layer.
- Plan 01 — document the conservative blur assumption in the AA proof; add a visibility-floor assertion beyond `< densityOpacity`.
- Plan 07 — decide the 3-column "full width" semantics (two semantic units vs all computed columns).
- Plan 08 — prefer the minimal design (GridCard omits `line3` in non-search mode; ListRow keeps consuming) and add component render tests.
- Plan 09 — `files_modified: []` conflicts with declared outputs + authorized in-class fixes; frame the device leg as a checkpoint (autonomy caveat).

### LOW
- Plan 02 — 24h branch testable via an internal `"12h"|"24h"` helper; define month/AM-PM locale-independently.
- Plan 04 — reformat only the display `text`; keep `summary.lastContact` raw.
- Plan 09 — use synthetic UAT contacts / keep screenshots uncommitted (third-party PII). Metro-port "assumption" is a false positive (repo memory confirms the 8082 remap) — no change needed.

### Cross-plan strengths (both lanes)
No migration/schema/format change; no network dependency; theme stays token-owned; animation stays on Reanimated; Plan 05→06 serialize shared Profile files; Plans 01→03 and 02→04 separate foundations from consumers; Plan 09 depends on all implementation plans.
