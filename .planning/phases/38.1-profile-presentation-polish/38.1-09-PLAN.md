---
phase: 38.1-profile-presentation-polish
plan: 09
type: execute
wave: 3
depends_on: [38.1-01, 38.1-02, 38.1-03, 38.1-04, 38.1-05, 38.1-06, 38.1-07, 38.1-08]
files_modified:
  - .planning/phases/38.1-profile-presentation-polish/38.1-UAT.md
  - .planning/phases/38.1-profile-presentation-polish/38.1-09-SUMMARY.md
autonomous: true
requirements: [R-15]
user_setup:
  - service: android-device
    why: "§N/§R-15 require the data-density/lifecycle/theme UAT on physical Android hardware; Skia orrery legibility/perf is only assessable on the phone, not the desktop emulator."
    dashboard_config:
      - task: "Connect the Pixel over USB (emu-connect) and install a DEBUG build of the phase (run-as DB reads need debug; install -r preserves data). Run Orbit Metro on the remapped port per the device runbook."
        location: "Physical Pixel 6 Pro + desktop build pipeline (memory: verify-ui-on-pixel-yourself, device-UAT run-as pattern, orbit Metro port 8082 remap)"
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The bounded §O audit sweeps for the SAME defect classes established this phase — stray seconds-exposing user-facing timestamp formatters, Profile layers repeating semantic headings/values, applicable Orrery overlays still coupled to ordinary content-card opacity, and targeted Profile edit affordances still leading to bare/non-functional destinations — and does not authorize unrelated cleanup, navigation changes, new features, or repo-wide refactoring (§O)."
    - "Any residual offender found by the audit is either fixed within the established defect class or recorded as a scoped follow-up; no storage builder was reformatted and no relative-time formatter changed (D-08)."
    - "The §N UAT matrix is executed on physical Android hardware across: no data / exactly one / multiple / history exceeding the five-item preview / Bound / Unbound / multiple full-half Relationship Overview arrangements incl. orphan / Standard and Galaxy."
    - "Device UAT verifies no duplicate headings or repeated latest metadata; no false 'nothing added' heading copy; Most Recent excluded from Previous; Previous caps at five on Profile; edit icons reach genuinely editable screens; Unbind absent from body and present in overflow; Relationship Overview content centered; Year heatmap needs no horizontal scroll; visible explicit timestamps omit seconds; normal Grid cards omit excerpts while List retains them; search Grid cards may still explain matches; Orrery controls readable, uniform, grouped, accessible."
    - { statement: "The Off Limits editor add/edit/delete leaves other fuel kinds intact when verified against the on-device DB (run-as), not just via UI observation.", verification: backstop }
    - { statement: "Orrery overlay tint is legible AND still reveals the orrery behind it on the real backdrop across theme/mode; any marginal mismatched combo is flagged to the owner rather than silently accepted.", verification: backstop }
  artifacts:
    - .planning/phases/38.1-profile-presentation-polish/38.1-UAT.md
  key_links:
    - "This plan consumes the artifacts of Plans 01-08; it produces no source changes except any in-class fixes for residual audit offenders."
---

<objective>
Close Phase 38.1 with the bounded cross-app audit (§O) and the physical-device UAT matrix (§N/§R-15). Planned against the authoritative dossier §N/§O (D-01), the closing half of the §Q "Contacts Grid density + bounded regression/UAT" group (D-03). Depends on all prior plans (it verifies their combined result).

This is the end-of-phase verification pass (workflow.human_verify_mode = end-of-phase — device confirmations are `<human-check>` items batched here, not per-plan checkpoints). It is bounded: it audits only the defect classes this phase established and does not open unrelated cleanup.

Purpose: catch any residual instance of the four defect classes before Phase 39 Onboarding is designed against these surfaces, and prove the changes on real hardware (the emulator cannot assess the Skia orrery).
Output: any in-class fixes for residual offenders + a `38.1-UAT.md` recording the matrix results and any owner flags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@.planning/phases/38.1-profile-presentation-polish/38.1-VALIDATION.md
@.planning/phases/38.1-profile-presentation-polish/38.1-04-SUMMARY.md
@.planning/phases/38.1-profile-presentation-polish/38.1-06-SUMMARY.md
</context>

<artifacts_produced>
Net-new here (source-grounding: exclude from drift verification):
- `.planning/phases/38.1-profile-presentation-polish/38.1-UAT.md` — the §N matrix results + audit findings + any owner flags.
- `.planning/phases/38.1-profile-presentation-polish/38.1-09-SUMMARY.md` — the phase-close summary.
- Any small in-class fix commits for residual §O offenders (review MEDIUM: `files_modified` therefore is NOT empty — it declares the two artifacts above; a residual fix stays strictly inside the four established defect classes — timestamps / Profile heading ownership / Orrery overlay treatment / edit-affordance routing — and is recorded at execution; anything larger is a scoped follow-up, not an edit here).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bounded §O cross-app audit for the four established defect classes</name>
  <files>.planning/phases/38.1-profile-presentation-polish/38.1-UAT.md</files>
  <read_first>
    - dossier §O (the four defect classes and the explicit "does not authorize unrelated cleanup" bound) and §K/D-08 storage-builder exclusions.
    - 38.1-04-SUMMARY.md (the three timestamp display sites already fixed) and PATTERNS.md "Storage builders — DO NOT TOUCH" (the 8 builders that must stay unchanged).
    - RESEARCH.md §"§O bounded audit" and the Orrery overlay inventory (the 6 overlays + OrreryContactsSheet exclusion decision from 38.1-03-SUMMARY.md).
  </read_first>
  <action>
    Run the bounded §O sweep for the SAME defect classes this phase established, staying strictly within scope (no unrelated cleanup, no navigation changes, no new features, no repo-wide refactor — §O):
    (a) Timestamps: enumerate any remaining user-facing display site that interpolates a raw wall-clock `YYYY-MM-DD HH:MM:SS` string (search for render-context uses of `occurredAt` / `created_at` / `last_contact` reaching `AppText`), confirm the three fixed sites are the only display offenders, and confirm none of the 8 storage builders was reformatted.
    (b) Profile hierarchy: scan Profile presentation for any layer still repeating a semantic heading or emitting content-state as heading/caption metadata.
    (c) Orrery overlays: confirm no applicable floating overlay still sources opacity from `cardTintOpacity`/`CARD_GLASS_OPACITY` (i.e. all named overlays carry `treatment="orrery-overlay"`).
    (d) Edit affordances: confirm no targeted Profile edit affordance still routes to a bare/non-functional destination (blanket `ThingsToRemember` navigate is gone for the fuel/current-state/relationship/custom-field owners).
    Fix any residual offender WITHIN its established defect class (small, in-scope); record anything larger as a scoped follow-up rather than expanding this phase. Write findings to 38.1-UAT.md. Use grep hygiene when counting (pipe through `grep -v '^#'` before `grep -c` so comments do not skew counts); do not gate on bare unfiltered counts.
  </action>
  <reversibility rating="reversible">Read/audit + small in-class fixes; no schema, no migration (D-02). Any fix that would need a migration or reverse a recorded decision STOPS and escalates to the owner.</reversibility>
  <verify>
    <automated>npx vitest run</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite (`npx vitest run`), `npx tsc --noEmit`, and `npm run check:colors` all exit 0.
    - 38.1-UAT.md records the audit result for each of the four defect classes (clean, or the in-class fix / scoped follow-up made).
    - The audit confirms the 8 storage builders are unchanged and relative-time formatters are untouched.
  </acceptance_criteria>
  <done>The four defect classes are swept, residual in-class offenders fixed or scoped, and the audit is recorded — bounded, with no unrelated cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the §N UAT matrix on physical Android hardware</name>
  <files>.planning/phases/38.1-profile-presentation-polish/38.1-UAT.md</files>
  <read_first>
    - dossier §N (UAT matrix + verification list) and §M (Standard/Galaxy parity).
    - MEMORY: verify-ui-on-pixel-yourself, device-UAT run-as pattern (debug build, install -r, Metro port remap, WAL-aware DB reads), adb input source taxonomy, LogBox toast blocks tab taps.
    - 38.1-03-SUMMARY.md (final orrery tint scalar + any owner flag) and 38.1-06-SUMMARY.md (off-limits guard).
  </read_first>
  <precondition>A physical Pixel is connected via emu-connect with a DEBUG build of the phase installed and Orbit Metro reachable on the remapped port (run-as DB reads require a debug build; install -r preserves data). Assert the device is attached (`adb devices` shows exactly one) before starting.</precondition>
  <checkpoint>The device leg is a hardware-dependent CHECKPOINT (autonomy caveat — this is the one part that cannot be self-served). Drive the Pixel directly per repo memory (verify-ui-on-pixel-yourself) — do NOT default the UI flows to human-needed. BUT if no physical Pixel is attached, or it shows `unauthorized` (only the owner can accept the on-device USB-debugging prompt), PAUSE and flag the owner rather than skipping the device leg, faking it, or marking the phase verified. The desktop emulator cannot substitute (Skia orrery legibility/perf is not assessable there).</checkpoint>
  <action>
    Drive the app on the Pixel yourself (do not default UI flows to human-needed — MEMORY) and execute the §N matrix across all data-density/lifecycle/theme states: no relevant data; exactly one item; multiple items; history exceeding the five-item Profile preview; Bound contact; Unbound contact; multiple full/half Relationship Overview arrangements including an orphan card (cover a MID-SEQUENCE orphan, not only a trailing one — per Plan 07); and Standard and Galaxy where relevant. Use SYNTHETIC/fixture contacts for the matrix (device-local contact fixtures per MEMORY), never real third-party PII; keep screenshots / uiautomator dumps as uncommitted local evidence (do not commit third-party contact data). Verify each §N assertion: no duplicate headings or repeated latest metadata; no false "nothing added" heading copy; Most Recent excluded from Previous; Previous caps at five on Profile; edit icons reach genuinely editable screens; Unbind absent from body / present in overflow; Relationship Overview content centered (incl. large font — no clipping); Year heatmap needs no horizontal scroll; visible explicit timestamps omit seconds; normal Grid cards omit excerpts while List retains them; search Grid cards may still explain matches; Orrery controls readable, uniform, grouped, accessible. For the Off Limits path, verify via the on-device DB (run-as) that other fuel kinds survive an add/edit/delete — not UI observation alone. Do NOT trigger any real AI suggestion API call (MEMORY: no-ai-api-calls-without-clearing). Record pass/fail per matrix cell in 38.1-UAT.md; if the orrery overlay tint is marginal in any mismatched combo, flag it to the owner (Phase-23 precedent) rather than silently retuning the owner's hue.
  </action>
  <reversibility rating="reversible">Verification pass; produces evidence + a UAT record. No source change except any in-class fix already covered by Task 1.</reversibility>
  <verify>
    <human-check>The §N matrix is executed on the physical Pixel with pass/fail recorded per cell in 38.1-UAT.md; the two backstop items (on-device off-limits DB preservation; orrery legibility-vs-reveal across theme/mode) are confirmed or flagged to the owner. Screenshots / uiautomator dumps captured for the key states.</human-check>
  </verify>
  <acceptance_criteria>
    - 38.1-UAT.md contains a pass/fail result for every §N matrix state and every §N verification assertion, captured on the physical device.
    - The on-device off-limits DB-preservation backstop and the orrery legibility backstop are each marked confirmed or flagged-to-owner (not silently passed).
    - Any failure is recorded with enough detail to route to a gap-closure plan (`/gsd-plan-phase --gaps`) rather than being closed silently.
  </acceptance_criteria>
  <done>The §N matrix is executed on real hardware, results recorded, and backstops confirmed or flagged — Phase 38.1 is verified against its authoritative contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented change → user-visible behavior | UAT is where UI-observable regressions surface; but UI observation alone cannot prove data invariants (off-limits preservation needs the on-device DB). |
| audit scope → phase scope | An unbounded "audit" could smuggle in unrelated refactors or a decision reversal; §O bounds it to the four established defect classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-09-01 | Tampering | off-limits fuel preservation (verification) | high | mitigate | On-device DB check (run-as), not UI-only, per the device-UAT run-as pattern; ties back to the Plan-06 preservation test + ported guard. A confirmed clobber blocks phase close and routes to gap closure. |
| T-38.1-09-02 | Elevation of scope | bounded audit | medium | mitigate | §O explicitly forbids unrelated cleanup/navigation/features/refactor; any larger finding is recorded as a scoped follow-up, and any decision-reversal candidate is escalated to the owner (not fixed under the "audit" label). |
| T-38.1-09-03 | Information disclosure | AI egress during UAT | medium | mitigate | Do not trigger a real AI suggestion API call during app-driving without owner clearance (MEMORY); AiService untouched this phase. |
| T-38.1-09-04 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); verification + at most in-class presentation fixes. |

One HIGH-severity item (off-limits preservation) is verified on-device; a confirmed regression blocks close.
</threat_model>

<verification>
- `npx vitest run`, `npx tsc --noEmit`, `npm run check:colors` all green (full-suite phase gate).
- §O audit recorded for all four defect classes in 38.1-UAT.md.
- §N matrix executed on the physical Pixel; backstops (off-limits DB preservation, orrery legibility) confirmed or flagged to the owner.
</verification>

<success_criteria>
- The four established defect classes are swept clean (or residuals fixed in-class / scoped), and the §N UAT matrix passes on physical Android hardware with the two device-only backstops confirmed or flagged — Phase 38.1 is complete and Phase 39 can be designed against the cleaned-up product.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-UAT.md` (matrix results + audit findings + owner flags) and `.planning/phases/38.1-profile-presentation-polish/38.1-09-SUMMARY.md` when done.
</output>
