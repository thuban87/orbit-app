---
phase: 38.1-profile-presentation-polish
plan: 06
type: execute
wave: 2
depends_on: [38.1-05]
files_modified:
  - src/db/profile-knowledge-read.ts
  - src/db/profile-knowledge-read.test.ts
  - src/profile/knowledge-presentation.ts
  - src/components/profile/ThingsToRemember.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/screens/OffLimitsEditorScreen.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [R-04, R-05, R-06, R-07, R-08]
estimate:
  tokens: 72000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Temporal knowledge modules present an explicit Most Recent + Previous hierarchy; Most Recent is the is_current=1 row and Previous is the is_current=0 rows ordered created_at DESC — Most Recent is structurally excluded from Previous (§F / §R item 5)."
    - "The Profile Previous preview is capped at exactly five items; the complete history lives on the dedicated MemoryHistory screen (§F)."
    - "Zero records -> neutral in-body empty state (no MOST RECENT / PREVIOUS shells); one record -> Most Recent only, Previous omitted; many -> Most Recent + up to five Previous (§F)."
    - "Most Recent/Previous is applied only to temporal current_state fields (last_talked_about, current_location) using the existing table — no new fields, no schema (§E / D-02); a temporal field with no prior rows simply shows Most Recent."
    - "Applicable full-width/row-level Manage affordances are replaced by a heading-level edit icon (accent, >=44x44 hit target); tapping content still uses its normal view/detail behavior and tapping the edit icon enters editing; the edit icon manages the WHOLE collection (§G)."
    - "Editing stays on dedicated screens (no inline full-editor expansion on Profile); routes reach genuinely editable screens using existing editors and the existing model — no new editor, no new model (D-06 / §G)."
    - "Off Limits' edit affordance reuses FuelEditor via a dedicated off-limits-scoped screen that ports the EditContactScreen kind-scope guard verbatim (filter kind===off_limits, force draft kind, defensive re-filter on write) so editing Off Limits cannot create/edit/clobber any other fuel kind (D-06 — DATA-LOSS guard)."
    - "A preservation test proves a contact's other fuel kinds survive an Off Limits add/edit/delete (D-06)."
    - "Temporal edit affordances route to MemoryHistory with the known fieldKey (derived from the child id); memories/imported-notes/pinned-featured keep the working ThingsToRemember route (§G)."
    - { statement: "key-people (relationship) and custom-fields edit affordances route to their existing EditContactScreen editors — the exact destination + CRUD verified against code at execution before wiring (Assumption A3).", verification: backstop }
  artifacts:
    - src/db/profile-knowledge-read.ts
    - src/profile/knowledge-presentation.ts
    - src/components/profile/ThingsToRemember.tsx
    - src/screens/ContactProfileScreen.tsx
    - src/screens/OffLimitsEditorScreen.tsx
    - src/navigation/types.ts
  key_links:
    - "profile-knowledge-read capped prior-entries read (is_current=0, DESC, PREVIOUS+1) -> knowledge-presentation formatCurrentState (Most Recent + Previous[0..5]) -> ThingsToRemember render."
    - "ContactProfileScreen owner-switched router: off-limits -> OffLimitsEditorScreen(FuelEditor + ported guard); current-state -> MemoryHistory(fieldKey); memory -> ThingsToRemember; relationship/custom-field -> EditContactScreen editors."
    - "OffLimitsEditorScreen route registered in navigation/types.ts."
---

<objective>
Deliver the §F Most Recent/Previous temporal knowledge hierarchy (five-item Profile preview, dedup, empty/single states) and the §G heading-level edit affordances that reach genuinely editable screens — all using the existing data model, no new fields, no schema (D-02). Planned against the authoritative dossier §E/§F/§G (D-01), the "Profile knowledge presentation + editing" ownership group per §Q (D-03).

Depends on Plan 05 (heading ownership + identifier-only captions established there): this plan adds Most Recent/Previous BODY content and heading-level edit icons on the cleaned hierarchy. Shares knowledge-presentation.ts / ThingsToRemember.tsx / ContactProfileScreen.tsx with Plan 05, hence sequential (05 -> 06).

D-06 decision recorded: Off Limits editing uses reuse-mechanism (B) — a dedicated `OffLimitsEditorScreen` that mounts the existing `FuelEditor` scoped to off_limits, porting `EditContactScreen`'s kind-scope DATA-LOSS guard verbatim (the §G-faithful "dedicated screen managing the whole off-limits collection"). Mechanism (A) navigate-to-EditContactScreen was the zero-risk alternative but opens the whole-contact editor, a poor match for a heading-level "Edit Off Limits" affordance. The guard + a preservation test make (B) safe.

Purpose: today the Profile snapshot reads current-value-only and every knowledge edit intent blanket-navigates to ThingsToRemember, discarding the owner discriminator; and Off Limits mis-routes to a screen with zero off-limits handling.
Output: capped prior-entries read; Most Recent/Previous presentation + render; owner-switched edit router; OffLimitsEditorScreen with the ported guard + preservation test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@.planning/phases/38.1-profile-presentation-polish/38.1-05-SUMMARY.md
@docs/decisions/ADR-108-durable-independent-axis-profile-presentation-and-inheritance.md
@docs/decisions/ADR-109-fixed-hero-semantic-profile-composition-and-focused-accessible-editors.md
@docs/decisions/ADR-123-profile-history-section-replacing-the-vertical-timeline.md
@src/db/profile-knowledge-read.ts
@src/db/current-state-history-read.ts
@src/profile/knowledge-presentation.ts
@src/components/profile/ThingsToRemember.tsx
@src/screens/ContactProfileScreen.tsx
@src/screens/EditContactScreen.tsx
@src/components/FuelEditor.tsx
@src/db/fuel-dao.ts
@src/screens/MemoryHistoryScreen.tsx
@src/navigation/types.ts
</context>

<artifacts_produced>
Net-new here (source-grounding: exclude these from drift verification — created this phase):
- A capped prior-entries read for temporal fields in profile-knowledge-read.ts (is_current=0, created_at DESC, limit PREVIOUS+1) reusing the existing `current_state_entries` table; extended `ProfileKnowledge` shape carrying previous entries.
- Most Recent/Previous emission in knowledge-presentation.ts `formatCurrentState` (dedup via is_current; five-item cap; empty/single-state handling).
- MOST RECENT / PREVIOUS subsection rendering + heading-level edit icons in ThingsToRemember.tsx.
- An owner-switched edit router in ContactProfileScreen (replacing the blanket ThingsToRemember navigate).
- A new screen `src/screens/OffLimitsEditorScreen.tsx` (mounts FuelEditor scoped to off_limits with the ported kind-scope guard) and its route in navigation/types.ts.
- New test cases: profile-knowledge-read.test.ts (capped prior read), knowledge-presentation.test.ts (Most Recent/Previous dedup + cap + states), and a fuel kind-preservation test for the off-limits path.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Most Recent/Previous data + presentation (§F) — capped read, dedup, states</name>
  <files>src/db/profile-knowledge-read.ts, src/db/profile-knowledge-read.test.ts, src/profile/knowledge-presentation.ts</files>
  <read_first>
    - src/db/profile-knowledge-read.ts:66-74 (`ProfileKnowledge.currentState` shape) and :108-129 (`readCurrentState` — current `WHERE contact_id=? AND is_current=1`).
    - src/db/current-state-history-read.ts:67-79 (`getCurrentStateHistory` — full history from the SAME table, ORDER BY created_at DESC, id DESC).
    - src/screens/MemoryHistoryScreen.tsx:75-78 (`priorEntries = history.filter(row => row.is_current === 0)` — the shipped dedup/split pattern to copy).
    - src/profile/knowledge-presentation.ts:199-230 (`formatCurrentState` — currently emits a single current item) and :116-122 (`summary()` — already reduced to identifier-only by Plan 05; do not re-add content-state).
    - RESEARCH.md §"§F — Most Recent & Previous" and PATTERNS.md §"§F".
  </read_first>
  <behavior>
    - For the temporal current_state fields (last_talked_about, current_location) the read returns the current row (is_current=1) plus up to five prior rows (is_current=0, created_at DESC) from the existing table — a bounded read, no schema change.
    - formatCurrentState emits Most Recent = current row; Previous = prior rows capped at 5; Most Recent never appears in Previous.
    - Zero rows -> neutral empty state, no MOST RECENT/PREVIOUS shells. One row -> Most Recent only, Previous omitted. Many -> Most Recent + up to 5 Previous.
    - A field with no prior rows shows Most Recent only (recency not imposed where absent — §E).
    - Unit tests cover: dedup (Most Recent excluded from Previous), five-item cap (6+ prior rows -> exactly 5 shown), and the 0/1/many states.
  </behavior>
  <action>
    Extend the Profile knowledge read to fetch, for the temporal fields only, up to PREVIOUS+1 prior rows keyed on is_current=0 (created_at DESC) from `current_state_entries` — a bounded READ against the existing table (no schema, D-02). Reuse the `is_current` discriminator so Most Recent (is_current=1) is structurally excluded from Previous. Carry the prior entries on the knowledge shape. In knowledge-presentation.ts, rewrite `formatCurrentState` to emit Most Recent + Previous[0..5] with the exact empty/single/many behavior above; do not re-introduce heading content-state (Plan 05 removed it). Apply to both temporal current_state fields uniformly (a field with no prior rows renders Most Recent only). Add cases to profile-knowledge-read.test.ts (capped prior read) and knowledge-presentation.test.ts (dedup, cap, states).
  </action>
  <precondition>The existing `current_state_entries` table carries `is_current` and `created_at` for prior rows (verified: current-state-history-read.ts:67-79). No migration is created or required (D-02).</precondition>
  <reversibility rating="reversible">Read-only query extension + presentation; no writes, no schema, no new fields (D-02). NOTE: if this appears to need a new column/table, STOP and flag as a checkpoint:decision — do not add a migration (D-02 one-way guard).</reversibility>
  <verify>
    <automated>npx vitest run src/db/profile-knowledge-read.test.ts</automated>
    <automated>npx vitest run src/profile/knowledge-presentation.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert Most Recent (is_current=1) is never in Previous, Previous is capped at exactly 5 when 6+ prior rows exist, and the 0/1/many states match §F.
    - The read is a bounded SELECT against `current_state_entries` (no new table/column) — verified by reading the query.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Temporal modules carry Most Recent + up-to-five Previous from the existing table, deduped and state-correct, with no schema change.</done>
</task>

<task type="auto">
  <name>Task 2: Render Most Recent/Previous + heading-level edit icons (§F/§G)</name>
  <files>src/components/profile/ThingsToRemember.tsx</files>
  <read_first>
    - src/components/profile/ThingsToRemember.tsx:184-188 (the child heading+caption/helper render — Plan 05 already suppresses the duplicate nested heading; build the subsections + edit icon around the single owned heading) and the KnowledgeActionIntent / target shape (:19-27).
    - UI-SPEC §"Typography" (MOST RECENT/PREVIOUS = Label role; values = Body; dates = Caption), §"Color" (edit icon = accent; >=44x44), §"Copywriting Contract" (edit icon accessible name `Edit {section}`; MOST RECENT/PREVIOUS shown only when records exist; single-record omits PREVIOUS; empty -> body neutral state).
    - RESEARCH.md §"§F" (labels align to `Most Recent`/`Previous` on Profile) and §"D-06/§G".
  </read_first>
  <action>
    Render the Most Recent/Previous subsections for temporal modules: a `MOST RECENT` Label + the current value (Body) + its date (Caption), then a `PREVIOUS` Label + up to five prior items (Body + Caption each), separated per the spacing scale. Show MOST RECENT/PREVIOUS only when records exist; with one record show MOST RECENT only (omit PREVIOUS); with zero records show the neutral in-body empty state (no empty subsection shells). Add a heading-level edit icon beside the relevant subsection/row heading: accent-tinted, >=44x44 hit target (hitSlop if the glyph is smaller), accessible name `Edit {section name}`. Separate view from edit — tapping content keeps its normal view/detail behavior; tapping the edit icon fires the edit intent for the WHOLE collection (not just the latest item). Do not expand a full editor inline (editing happens on the dedicated screen the router opens). Use existing tokens only (no new spacing/type).
  </action>
  <reversibility rating="reversible">Render change; no data, no schema.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/profile/knowledge-presentation.test.ts</automated>
    <human-check>On the Pixel, a temporal module shows MOST RECENT (+ up to five PREVIOUS) with dates; a single-record module shows MOST RECENT only; an empty module shows a neutral body state (no empty shells); the heading edit icon is tappable at >=44x44 with a correct TalkBack name and is distinct from tapping the content. (Batched into Plan 09 device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - Reading ThingsToRemember confirms the MOST RECENT/PREVIOUS subsections (Label/Body/Caption roles), the five-item preview, the single/empty-state handling, and the accent heading-level edit icon with a >=44x44 target and `Edit {section}` accessible name.
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
  </acceptance_criteria>
  <done>Most Recent/Previous renders with the correct states and a heading-level edit icon that manages the whole collection, distinct from view.</done>
</task>

<task type="auto">
  <name>Task 3: Owner-switched edit router + OffLimitsEditorScreen with the ported kind-scope guard (§G / D-06)</name>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/OffLimitsEditorScreen.tsx, src/navigation/types.ts</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx:408-424 — the blanket `navigate("ThingsToRemember", {contactId})` for onKnowledgeAction/onKnowledgeViewAll/onOpenKnowledgeChange/onOpenValueHistory; the intent target owner discriminator (`KnowledgeActionIntent.target.owner`).
    - src/screens/EditContactScreen.tsx:32-36 (the DATA-LOSS guard rationale comment), :285-291 (`fuelItemToDraftRow` FORCES kind to off_limits), :520 (`offLimitsRows = fuelRows.filter(row => row.kind === "off_limits")`), :806-808 (defensive re-filter so the section can never create/edit a non-off_limits row). THIS GUARD MUST BE PORTED VERBATIM.
    - src/components/FuelEditor.tsx:59-68 (`KIND_OPTIONS`; `DEFAULT_KIND` is never off_limits — the hazard), :494-501 (controlled-component contract `{items, now, onAdd, onEdit, onDelete, testID}`).
    - src/db/fuel-dao.ts:232,244,255 (`addFuel`/`editFuel`/`deleteFuel` — standalone, transaction-wrapped) and fuel-read.ts:61 (`listFuelForEditor`).
    - src/screens/MemoryHistoryScreen.tsx (route target for temporal edits; route `MemoryHistory: {contactId, fieldKey}` at navigation/types.ts:75) and the deterministic KnowledgeChildId -> fieldKey mapping (last-talked-about -> last_talked_about, current-location -> current_location).
    - RESEARCH.md §"D-06 / §G — edit-affordance routing map" (incl. Assumption A3 for key-people/custom-fields), Pitfall 1, and PATTERNS.md §"D-06 / §G".
  </read_first>
  <behavior>
    - Editing Off Limits from Profile opens a dedicated off-limits-scoped screen (FuelEditor) that shows only off_limits fuel and can add/edit/delete only off_limits rows.
    - After any Off Limits add/edit/delete, a contact's other fuel kinds (recent/topic/fact/gift) are unchanged (preservation test proves this).
    - The temporal edit affordance opens MemoryHistory for the correct fieldKey (full add/edit/promote surface).
    - memories / imported-notes / pinned-featured editing opens ThingsToRemember (the currently-working route).
    - key-people (relationship) and custom-fields editing open their existing EditContactScreen editors (destination verified against code at execution — A3).
  </behavior>
  <action>
    Replace the blanket knowledge navigate in ContactProfileScreen with an owner-switched router keyed on the intent target owner (and, for temporal modules, on the child id -> fieldKey mapping, bypassing the intent id->fieldKey gap): fuel/off-limits -> the new OffLimitsEditorScreen; current-state -> MemoryHistory with the known fieldKey; memory -> ThingsToRemember (unchanged, it works); relationship (key-people) and custom-field -> their existing EditContactScreen editors — OPEN EditContactScreen.tsx at execution and verify the exact relationship/custom-field editor destination + CRUD before wiring (A3); if a destination is not genuinely editable, record it and route to the closest existing editable screen rather than a bare page. Create `src/screens/OffLimitsEditorScreen.tsx` that mounts `FuelEditor` scoped to off_limits and PORTS the EditContactScreen guard verbatim: filter items to `kind === "off_limits"`, force the draft kind to `off_limits`, and keep the defensive re-filter on write so it can never create/edit a non-off_limits row; wire add/edit/delete to the standalone `fuel-dao` operations. Register the new route in navigation/types.ts (all four route-map declarations). Add a preservation test asserting other fuel kinds survive an off-limits add/edit/delete. Do NOT invent new fields/tags/importance/source metadata (D-06/§P). Do NOT add a migration (D-02).
  </action>
  <precondition>`FuelEditor`, the `fuel-dao` add/edit/delete operations, and the `MemoryHistory` route already exist (verified). No new editor component or data model is created — only a screen wrapper + routing (D-06).</precondition>
  <reversibility rating="reversible">New screen + routing over existing editors/DAOs; no schema, no new model (D-02/D-06). The ported guard is the data-integrity control; do not ship the screen without it.</reversibility>
  <verify>
    <automated>npx vitest run src/db/fuel-dao.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On the Pixel: tapping the Off Limits edit icon opens an off-limits-only editor; adding/editing/deleting an off-limits item leaves the contact's other fuel kinds intact (confirm via the app and, per the device-UAT run-as pattern, the on-device DB); the temporal edit icon opens Memory History for the right field; key-people/custom-fields edit icons reach genuinely editable screens. (Batched into Plan 09 device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - A fuel preservation test asserts other kinds (recent/topic/fact/gift) are unchanged after an off_limits add, edit, and delete — `npx vitest run src/db/fuel-dao.test.ts` exits 0 with that case.
    - Reading OffLimitsEditorScreen confirms the ported guard triad (filter kind===off_limits, forced draft kind, defensive re-filter on write) is present.
    - Reading ContactProfileScreen confirms the blanket navigate is replaced by an owner-switched router; the OffLimitsEditorScreen route is registered in navigation/types.ts.
    - `npx tsc --noEmit` exits 0.
    - The A3 relationship/custom-field destinations are verified against code and recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Edit affordances reach genuinely editable dedicated screens via an owner-switched router; Off Limits edits are kind-scoped and provably non-destructive to other fuel kinds; no new model/migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Off Limits editor → fuel table | The single highest-risk boundary: a general 5-kind FuelEditor writing to the shared `fuel` table can clobber other kinds if not scoped. |
| Profile edit intent → editor screen | Mis-routing sends the user to a screen that cannot edit the intended collection (bare/non-functional destination). |
| knowledge read → current_state_entries | A read that mixed is_current rows could duplicate Most Recent into Previous (data-presentation integrity). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-06-01 | Tampering | OffLimitsEditorScreen / fuel table (DATA LOSS) | high | mitigate | Port EditContactScreen's kind-scope guard verbatim (filter + forced draft kind + defensive re-filter on write); a preservation test asserts other fuel kinds survive add/edit/delete; FuelEditor's DEFAULT_KIND-never-off_limits hazard is neutralized by the forced kind. Blocks release if the preservation test is absent/failing. |
| T-38.1-06-02 | Tampering | SQLite schema / migrations | high | mitigate | §F is a bounded READ against the existing `current_state_entries` table; NO new column/table/migration and NO format bump (D-02, one-way). If the work appears to need a migration, STOP and flag a checkpoint:decision — do not add one. |
| T-38.1-06-03 | Repudiation | Most Recent duplicated in Previous | medium | mitigate | is_current discriminator (Most Recent = is_current=1, Previous = is_current=0) structurally prevents overlap; unit-tested. |
| T-38.1-06-04 | Information disclosure | read path / network | low | accept | Local-first synchronous SQLite reads; no network, no new fetch (CLAUDE.md); no AI egress touched (no AI suggestion call in this phase). |

Two HIGH-severity threats (data-loss + migration) are mitigated by mandatory controls (ported guard + preservation test; no migration). If either control cannot be satisfied, STOP and escalate to the owner (security_block_on: high) — do not ship a weaker version.
</threat_model>

<verification>
- `npx vitest run src/db/profile-knowledge-read.test.ts src/profile/knowledge-presentation.test.ts src/db/fuel-dao.test.ts` all green (incl. dedup/cap/states + fuel preservation).
- `npx tsc --noEmit` and `npm run check:colors` clean (verify no orphaned consumers after the router change — memory: orphaned test consumers break tsc).
- Device edit-routing + off-limits-preservation (on-device DB via run-as) checks queued for Plan 09 (end-of-phase UAT).
</verification>

<success_criteria>
- Temporal modules show Most Recent + up-to-five Previous (deduped, state-correct, no schema); heading-level edit icons reach genuinely editable dedicated screens; Off Limits editing is kind-scoped and provably non-destructive; no new editor, model, or migration.
</success_criteria>

<assumption_delta_decision>
No identity-model transition introduced: Most Recent/Previous is a read-only preview over existing `current_state_entries` rows (is_current discriminator) — not a singular→plural, required→optional, or derived→chosen change to any stored entity. Advisory delta detector: no action.
</assumption_delta_decision>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-06-SUMMARY.md` when done. Record: the A3 relationship/custom-field editor destinations as verified; the OffLimitsEditorScreen route name; and confirmation the ported guard + preservation test are in place. List the new symbols under "Artifacts this phase produces" for the convergence source-grounding pass.
</output>
