---
phase: 38.1-profile-presentation-polish
plan: 04
type: execute
wave: 2
depends_on: [38.1-02]
files_modified:
  - src/components/history/ActivityHeatmap.tsx
  - src/components/history/activity-heatmap-render.test.tsx
  - src/db/profile-history-read.ts
  - src/components/history/interaction-detail-logic.ts
  - src/screens/MemoryHistoryScreen.tsx
autonomous: true
requirements: [R-12, R-13]
estimate:
  tokens: 54000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Year heatmap progresses VERTICALLY (weeks flow down the page) with no horizontal heatmap scrolling; the week/day cell concept is preserved, not replaced (§J). An automated render/source assertion proves the Year branch no longer emits a horizontal `ScrollView` (the classification test does not cover orientation — review MEDIUM)."
    - "Year cell tap/day-selection, intensity classification (lens=\"year\"), and accessibility labels are unchanged after the orientation flip — renderDayCell stays orientation-agnostic and the 44-floor hitSlop math is preserved (§J)."
    - "The three verified user-facing timestamp display offenders are reformatted through the shared formatter to minute precision, 12-hour (no seconds): profile-history-read.ts (Last Interaction), interaction-detail-logic.ts (When row), MemoryHistoryScreen.tsx (prior-entry caption) (§K / D-08)."
    - "On parse failure the display sites fall back to a NEUTRAL label via `formatDateTimeMinuteOrFallback` (Plan 02), never the raw stored string (raw re-exposes seconds, violating §K — review MEDIUM)."
    - "In profile-history-read.ts only the display `summary.text` is reformatted; the structured `summary.lastContact` field stays RAW (it has no other non-test consumer; keeping it raw avoids semantic drift + test churn — review LOW)."
    - "The 8 storage builders are untouched and no 9th ad-hoc HH:MM:SS builder is added; relative-time strings are unaffected; stored precision is unchanged (D-08)."
  artifacts:
    - src/components/history/ActivityHeatmap.tsx
    - src/db/profile-history-read.ts
    - src/components/history/interaction-detail-logic.ts
    - src/screens/MemoryHistoryScreen.tsx
  key_links:
    - "formatDateTimeMinuteOrFallback (Plan 02) <- profile-history-read.ts (summary.text only) / interaction-detail-logic.ts / MemoryHistoryScreen.tsx (the three display offenders); neutral fallback on parse failure, never raw."
    - "ActivityHeatmap Month dayRow stack pattern -> reused for the Year branch with yearCellEdge; renderDayCell reused unchanged."
---

<objective>
Make the Year heatmap flow vertically (§J) and reroute the three verified user-facing timestamp display offenders through the Plan-02 shared formatter (§K/D-08). Planned against the authoritative dossier §J/§K (D-01), the "History + timestamp presentation" ownership group per §Q (D-03). Depends on Plan 02 (the shared formatter must exist).

Purpose: today the Year heatmap uses a horizontal ScrollView (weeks-as-columns), diverging from the Month convention and requiring horizontal scrolling; and three display sites render a raw 24-hour, seconds-exposing `YYYY-MM-DD HH:MM:SS` string.
Output: vertical Year heatmap (render-branch swap reusing the Month stack) + an orientation render assertion; three offenders reformatted via `formatDateTimeMinuteOrFallback` (neutral fallback, never raw).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@.planning/phases/38.1-profile-presentation-polish/38.1-02-SUMMARY.md
@docs/decisions/ADR-120-shared-window-heatmap-and-intensity-with-persisted-lenses.md
@docs/decisions/ADR-121-rolodex-month-day-year-history-browser.md
@src/components/history/ActivityHeatmap.tsx
@src/db/profile-history-read.ts
@src/components/history/interaction-detail-logic.ts
@src/screens/MemoryHistoryScreen.tsx
@src/utils/dates.ts
</context>

<artifacts_produced>
Net-new here (source-grounding: exclude from drift verification):
- A vertical Year render branch in ActivityHeatmap (reusing the Month `dayRow` stack with `HEATMAP_GEOMETRY.yearCellEdge`); the old `yearColumns`/`yearColumn` horizontal styles become unused and are removed.
- A new `activity-heatmap-render.test.tsx` asserting the Year-lens render has no horizontal `ScrollView` (orientation coverage the classification test lacks).
- Three call sites now import and call `formatDateTimeMinuteOrFallback` (Plan 02): profile-history-read.ts (in `summary.text` only, `summary.lastContact` stays raw), interaction-detail-logic.ts, MemoryHistoryScreen.tsx.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Vertical Year heatmap — render-branch swap (§J)</name>
  <files>src/components/history/ActivityHeatmap.tsx</files>
  <read_first>
    - src/components/history/ActivityHeatmap.tsx:345-364 — the Year branch (horizontal `ScrollView` + `styles.yearColumns`/`yearColumn`, weeks-as-columns), :413-420 (those styles), and the stale "weeks as COLUMNS" comment (~:346).
    - :365-382 — the Month branch's vertical `dayRow` stack (the pattern to copy), fed by `chunkWeeks(window.cells)` (:145-151,182-185) — each chunk is one calendar week (7 cells).
    - :197-233 (`renderDayCell` — orientation-agnostic: owns tap `onCellPress`, `classifyHeatmapCell(cell,count,lens)`, a11y label, placeholder/future blanks, `hitSlop`), :65-68 (`hitSlopFor(edge)` 44-floor), and `HEATMAP_GEOMETRY` (`yearCellEdge` 13 vs `dayCellEdge` 38).
    - RESEARCH.md §"§J — Vertical Year heatmap" and PATTERNS.md §"§J".
  </read_first>
  <action>
    Replace the Year branch's horizontal `ScrollView` + `yearColumns`/`yearColumn` layout with the SAME vertical `dayRow` stack the Month branch uses, passing `HEATMAP_GEOMETRY.yearCellEdge` to `renderDayCell` instead of `dayCellEdge`. Because each `chunkWeeks` chunk is one calendar week, rendering chunks as rows yields weekday-aligned columns automatically and the year flows down the page with no horizontal scroll. Reuse `renderDayCell` unchanged (do not alter tap/classification/a11y). Keep `lens="year"` passed to `classifyHeatmapCell` so intensity is unchanged, and keep the `hitSlopFor(edge)` 44-floor. Remove the now-unused `yearColumns`/`yearColumn` styles and fix the stale "weeks as COLUMNS" comment. No new spacing values (UI-SPEC). Add an automated orientation assertion in `src/components/history/activity-heatmap-render.test.tsx` (following the mocked-`react-native` convention of `src/components/orrery/orrery-controls-render.test.tsx`): render the heatmap in the Year lens and assert the rendered tree contains NO `ScrollView` with a `horizontal` prop (the vertical `dayRow` stack has replaced it) — `heatmap-cell.test.ts` only proves classification, not orientation (review MEDIUM).
  </action>
  <reversibility rating="reversible">Render-branch swap reusing an in-file pattern; no data, no schema (D-02).</reversibility>
  <verify>
    <automated>npx vitest run src/components/history/heatmap-cell.test.ts src/components/history/activity-heatmap-render.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <human-check>On the Pixel, the Year heatmap scrolls only vertically (no horizontal heatmap scroll), weekday columns stay aligned, day cells remain tappable with correct a11y labels, and lens switching + intensity look unchanged. (Batched into Plan 09 end-of-phase device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/history/heatmap-cell.test.ts` exits 0 (classification/selection semantics unchanged — the render swap does not touch them).
    - `activity-heatmap-render.test.tsx` asserts the Year-lens render emits no horizontal `ScrollView` (orientation is flipped, not just classified).
    - Reading ActivityHeatmap confirms the Year branch reuses the vertical `dayRow` stack with `yearCellEdge`, `renderDayCell` unchanged, `lens="year"` preserved, and the old horizontal styles removed.
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - Device UAT (Plan 09): no horizontal Year scroll; day selection works.
  </acceptance_criteria>
  <done>Year heatmap flows vertically with preserved intensity/selection/a11y and no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Reformat the three timestamp display offenders (§K / D-08)</name>
  <files>src/db/profile-history-read.ts, src/components/history/interaction-detail-logic.ts, src/screens/MemoryHistoryScreen.tsx</files>
  <read_first>
    - src/db/profile-history-read.ts:72 (`latest = rows[0]?.last_contact ?? entries[0]?.occurredAt ?? null`) and :81 (the `Last interaction ${latest}` render string) — `latest` is a raw `YYYY-MM-DD HH:MM:SS`; surfaces in RelationshipOverview last-interaction tile (:73-78) and the ProfileModuleHost interaction-history caption (:121-124). (CONTEXT cited :73; the render string is :81, value at :72.)
    - src/components/history/interaction-detail-logic.ts:96 (`rows.push({ key:"datetime", label:"When", value: interaction.occurredAt })` — raw string → InteractionDetail When row).
    - src/screens/MemoryHistoryScreen.tsx:172 (`{entry.created_at}` raw prior-entry caption).
    - src/utils/dates.ts — `formatDateTimeMinuteOrFallback` (Plan 02; the read-path-safe wrapper that returns a neutral label, never raw, on parse failure). PATTERNS.md "Storage builders — DO NOT TOUCH" list (8 builders) — do NOT reformat any of those.
  </read_first>
  <action>
    Reroute exactly these three DISPLAY sites through `formatDateTimeMinuteOrFallback` from Plan 02 (the wrapper — never the throwing `formatDateTimeMinute` directly on a read path): in profile-history-read.ts, reformat ONLY the display `summary.text` (`` `Last interaction ${formatDateTimeMinuteOrFallback(latest)}` ``) and keep the structured `summary.lastContact` field RAW — it has no other non-test consumer and keeping it raw avoids semantic drift (review LOW); reformat the `When` row value in interaction-detail-logic.ts; reformat the prior-entry caption in MemoryHistoryScreen.tsx:172. Do NOT touch any of the 8 storage builders (they write to SQLite / build keys — stored precision unchanged, D-08) and do NOT add a 9th `HH:MM:SS` builder. Relative-time strings ("Yesterday", "5d ago") stay as-is. On a malformed/empty stored value the wrapper yields a NEUTRAL label (e.g. "Unknown time") — NEVER the raw seconds-bearing string (raw re-exposes seconds, violating §K — review MEDIUM); log the malformed value if useful, do not surface it.
  </action>
  <reversibility rating="reversible">Display reformatting via the shared formatter; stored data and schema untouched (D-02/D-08).</reversibility>
  <verify>
    <automated>npx vitest run src/components/history/interaction-detail-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On the Pixel, the Profile Last-Interaction tile, the Interaction Detail When row, and the Memory History prior-entry captions all show minute-precision 12-hour timestamps (e.g. Sep 19, 2026, 7:14 PM) with no seconds; relative-time strings elsewhere are unchanged. (Batched into Plan 09 end-of-phase device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/history/interaction-detail-logic.test.ts` exits 0 (the When row uses the shared formatter output).
    - Reading the three files confirms each display site calls `formatDateTimeMinuteOrFallback`, that profile-history-read reformats only `summary.text` while `summary.lastContact` stays raw, and none of the 8 storage builders is modified.
    - A test covers the parse-failure path yielding a neutral label (never the raw seconds-bearing string).
    - `npx tsc --noEmit` exits 0.
    - Device UAT (Plan 09): the three surfaces show no seconds; relative-time unaffected.
  </acceptance_criteria>
  <done>The three verified display offenders render minute-precision 12-hour timestamps via the shared formatter; storage builders and relative-time are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored timestamp → display formatter | A formatter applied to a write path, or an unhandled parse failure on a read path, would corrupt stored precision or crash a local-first read. |
| heatmap render branch → day selection | An orientation swap that broke `renderDayCell` semantics would silently drop day-tap/intensity/a11y. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-04-01 | Tampering | storage builders / stored precision | medium | mitigate | Only 3 DISPLAY sites reformatted; the 8 storage builders are excluded by name (D-08); no 9th builder; stored precision unchanged. |
| T-38.1-04-02 | Denial of service | read path (formatter throws) | medium | mitigate | Use `formatDateTimeMinuteOrFallback` (never the throwing formatter directly) so unparseable input yields a NEUTRAL label — never raw (raw re-exposes seconds, §K) and never a crashed local-first read path (CLAUDE.md). |
| T-38.1-04-03 | Tampering | heatmap day-selection semantics | medium | mitigate | Reuse `renderDayCell` unchanged; `heatmap-cell.test.ts` proves classification/selection intact; device UAT confirms day-tap. |
| T-38.1-04-04 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); render + display reformat only. |

No high-severity threats.
</threat_model>

<verification>
- `npx vitest run src/components/history/heatmap-cell.test.ts src/components/history/activity-heatmap-render.test.tsx src/components/history/interaction-detail-logic.test.ts` green.
- `npx tsc --noEmit` and `npm run check:colors` clean.
- Device Year-orientation + no-seconds checks queued for Plan 09 (end-of-phase UAT).
</verification>

<success_criteria>
- The Year heatmap flows vertically with preserved semantics, and the three verified display offenders show minute-precision 12-hour timestamps via the shared formatter with storage builders and relative-time untouched.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-04-SUMMARY.md` when done. List the exact three display sites reformatted (for the Plan 09 §O audit to confirm no others remain).
</output>
