---
phase: 38.1-profile-presentation-polish
plan: "04"
subsystem: history-presentation
tags: [react-native, heatmap, local-timestamps, vitest]
requires:
  - phase: 38.1-02
    provides: Shared minute-precision timestamp formatter with neutral parse fallback
provides:
  - Vertically flowing Year heatmap with weekday-aligned week rows and no horizontal scroll container
  - Minute-precision timestamp presentation at the three verified history display sites
affects: [38.1-09, history-device-uat, timestamp-presentation]
actuals:
  tokens: 5943
  tasks: 2
  commits: 2
tech-stack:
  added: []
  patterns:
    - Year heatmap reuses the Month week-row layout and orientation-agnostic day-cell renderer.
    - Stored timestamp read paths use formatDateTimeMinuteOrFallback for neutral malformed-data presentation.
key-files:
  created:
    - src/components/history/activity-heatmap-render.test.tsx
  modified:
    - src/components/history/ActivityHeatmap.tsx
    - src/db/profile-history-read.ts
    - src/components/history/interaction-detail-logic.ts
    - src/screens/MemoryHistoryScreen.tsx
key-decisions:
  - "Keep profile summary.lastContact raw while formatting only its display text, preventing semantic drift for the structured value."
  - "Reuse renderDayCell unchanged so Year orientation changes preserve classification, tap targets, and accessibility labels."
patterns-established:
  - "Use a source-level Year render contract in addition to pure heatmap classification tests when layout orientation matters."
requirements-completed: [R-12, R-13]
coverage:
  - id: D1
    description: Year heatmap uses vertically stacked week rows with no horizontal ScrollView while retaining its existing cell behavior.
    requirement: R-12
    verification:
      - kind: unit
        ref: src/components/history/activity-heatmap-render.test.tsx#uses a vertical week-row stack instead of a horizontal ScrollView
        status: pass
      - kind: unit
        ref: src/components/history/heatmap-cell.test.ts
        status: pass
    human_judgment: false
  - id: D2
    description: Profile Last Interaction, Interaction Detail When, and Memory History prior-entry timestamps show minute precision through the shared safe formatter.
    requirement: R-13
    verification:
      - kind: integration
        ref: src/db/profile-history-read.test.ts#uses a neutral label rather than exposing a malformed stored timestamp
        status: pass
      - kind: unit
        ref: src/components/history/interaction-detail-logic.test.ts#uses a neutral label rather than the raw timestamp when parsing fails
        status: pass
      - kind: unit
        ref: src/utils/dates.test.ts
        status: pass
    human_judgment: false
  - id: D3
    description: Year heatmap orientation, cell tapping, and timestamp rendering hold on the physical Pixel.
    verification: []
    human_judgment: true
    rationale: Native scroll behavior, touch targets, and visual layout require the end-of-phase physical-device UAT batched into Plan 09.
duration: 5min
completed: 2026-09-20
status: complete
---

# Phase 38.1 Plan 04: History Presentation Summary

**The Year heatmap now flows vertically, and the three audited History timestamp surfaces render safe 12-hour minute precision without changing stored timestamps.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-20T05:20:00Z
- **Completed:** 2026-09-20T05:25:26Z
- **Tasks:** 2/2
- **Files modified:** 7

## Accomplishments

- Replaced the Year lens horizontal week-column `ScrollView` with the Month branch's vertical weekday-aligned week rows, preserving the unchanged cell renderer and 44px hit-slop floor.
- Added a render-tree regression test proving the Year lens contains no horizontal `ScrollView`.
- Routed exactly three display sites through `formatDateTimeMinuteOrFallback`: Profile Last Interaction summary text, Interaction Detail's When row, and Memory History prior-entry captions.
- Preserved raw `summary.lastContact`, SQLite storage builders, structured data, and relative-time strings; malformed display inputs resolve to `Unknown time`.

## Task Commits

1. **Task 1: Vertical Year heatmap — render-branch swap (§J)** — `bec28f3` (feat)
2. **Task 2: Reformat the three timestamp display offenders (§K / D-08)** — `a62d3b4` (feat)

## Files Created/Modified

- `src/components/history/ActivityHeatmap.tsx` — uses vertical week rows for the Year lens and removes retired horizontal styles.
- `src/components/history/activity-heatmap-render.test.tsx` — locks the Year branch against horizontal scrolling.
- `src/db/profile-history-read.ts` — formats only the visible Last Interaction summary text.
- `src/db/profile-history-read.test.ts` — verifies formatted and malformed summary output while retaining the raw structured timestamp.
- `src/components/history/interaction-detail-logic.ts` — formats the visible When detail row through the fallback wrapper.
- `src/components/history/interaction-detail-logic.test.ts` — covers formatted and neutral parse-failure detail output.
- `src/screens/MemoryHistoryScreen.tsx` — formats prior-entry captions through the same fallback wrapper.

## Decisions Made

- Kept `ProfileHistorySummary.lastContact` raw and changed only `summary.text`; it has no production display consumer and must retain its structured storage value.
- Reused `renderDayCell` exactly as it stood, keeping Year classification, day selection, accessibility labels, and hit slop independent of orientation.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Vitest emitted its existing Vite native-config warning and Node emitted its experimental SQLite warning; all requested checks passed.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 09 owns physical-Pixel validation of vertical-only Year scrolling, cell tapping/accessibility, and the three no-seconds timestamp surfaces. No schema, network, or stored-data behavior changed.

## Self-Check: PASSED

- Confirmed the seven changed source/test files and this summary exist.
- Confirmed task commits `bec28f3` and `a62d3b4` exist in git history.

---
*Phase: 38.1-profile-presentation-polish*
*Completed: 2026-09-20*
