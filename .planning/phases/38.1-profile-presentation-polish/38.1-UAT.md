# Phase 38.1 UAT — Profile & Presentation Polish

**Device target:** physical Pixel 6 Pro (serial resolved at execution; never assumed)

## Task 1 — §O bounded cross-app audit

**Scope:** only the four defect classes named in dossier §O. No migration, schema, navigation redesign, feature work, or general cleanup was performed.

| Audit class | Result | Evidence |
| --- | --- | --- |
| Explicit timestamps exposing seconds | PASS | The only three known display offenders remain routed through `formatDateTimeMinuteOrFallback`: Profile last-interaction text, Interaction Detail’s When row, and Memory History captions. Render-context search found no direct raw `occurredAt` / `createdAt` / `lastContact` interpolation in `AppText`. Relative-time formatting remains untouched. |
| Storage timestamp builders | PASS — unchanged | Reviewed the eight D-08 exclusions: `database.ts`, `memory-trash-sweep.ts`, `orrery-scene.ts`, `widget-data.ts`, `touchpoint-refine-logic.ts`, `field-sweep.ts`, `ai-context-read.ts`, and `picked-contact-map.ts`. Their phase history contains no 38.1 change; no storage precision was reformatted. |
| Profile heading / state metadata duplication | PASS | Profile section ownership is in `ProfileModuleHost`; section headings are identifiers, while temporal/state content renders in bodies. The targeted source scan found no `Nothing added yet` heading copy and no retired row-level `Manage` label. |
| Orrery overlay opacity coupling | PASS | `rg` found no `cardTintOpacity` or `CARD_GLASS_OPACITY` use in the Orrery overlay subsystem. The six applicable overlay surfaces use `treatment="orrery-overlay"`: Controls (three independent cards), View Options trigger/panel, System Selector trigger/panel, Focus Context, and Cluster Panel. `OrreryContactsSheet` remains a sheet, per Plan 03’s explicit exclusion. |
| Targeted Profile edit affordances | PASS | The former blanket `ThingsToRemember` route is absent from the fuel/current-state/relationship/custom-field owners. The owner switch sends Fuel to `OffLimitsEditor`, temporal state to `MemoryHistory`, and relationship/custom-field editing to registered `Edit` destinations; each Profile host registers the destination. |

### Automated verification

| Command | Result |
| --- | --- |
| `npx vitest run` | PASS |
| `npx tsc --noEmit` | PASS |
| `npm run check:colors` | PASS |

The Vitest run emitted the existing Vite native-config and Node experimental SQLite warnings only; it exited successfully.

### Audit disposition

No residual in-class offender was found. No source change was warranted or made. Task 2 appends physical-device UAT evidence below.

## Task 2 — §N physical Pixel UAT

**Status: IN PROGRESS — the original Profile edit-affordance blocker is fixed and retested; the remaining §N matrix and Off Limits DB backstop remain open. Do not treat this phase as device-verified.**

### Hardware / debug-build precondition

| Check | Result |
| --- | --- |
| Target | PASS — physical Pixel 6 Pro, USB-authorized; `adb devices` cardinality was exactly one before the workflow. |
| Debuggable package / DB access | PASS — `com.bwales.orbit` installed and `run-as com.bwales.orbit pwd` succeeded. |
| Metro / launch | Recovered — the initial debug launch showed the expected missing-bundle RedBox because its reverse tunnel was absent. `adb reverse tcp:8081 tcp:8081` restored the existing Metro route; the themed app then rendered on the phone. |
| Fixture safety | PASS — only existing device-local synthetic `UAT-*` / `ZZ-UAT-*` contacts were inspected. No real AI request was invoked. |

### Evidence completed before the blocker

| Matrix / assertion | Result | Evidence |
| --- | --- | --- |
| Normal Contacts Grid density | PASS (observed) | Physical Grid cards for synthetic contacts show name + recency only; normal routine excerpts are absent. |
| Single temporal current value | PASS (observed) | `UAT Ada` Profile renders one `MOST RECENT` value at minute precision and no duplicated latest value in Previous. |
| Explicit timestamp precision | PASS (observed) | The Profile rendered `Sep 4, 2026, 9:40 AM` for a stored `2026-09-04 09:40:08` value; seconds are not visible. |
| Neutral section copy | PASS (observed) | `Pinned / Featured` renders `Nothing added yet` inside its body; the `Things to Remember` heading itself has no state suffix. |
| On-device DB inspection setup | PASS (observed) | WAL-aware copies of `files/SQLite/orbit.db`, `-wal`, and `-shm` were read locally after force-stopping the debug app. The synthetic fixture database contains `UAT Ada`, `UAT Grace`, and `UAT-*` rows only for this pass. |

### Resolved failure — targeted Profile edit affordance

**Reproduction (physical Pixel, no UI-only inference):**

1. Open synthetic `UAT Ada` from Contacts.
2. Expand `Things to Remember` using its separately exposed header target.
3. Dump the accessibility tree: `Edit Things to Remember` is an enabled, clickable Button at `[1171,1486][1325,1640]`.
4. Tap its center (`1248,1560`) once through the physical-device adb transport.
5. The screen remains `contact-profile-screen`; the edit destination does not appear. The section remains expanded, proving this isolated tap did not merely trigger the adjacent collapse action.

Source inspection confirms the intended owner route exists (`ThingsToRemember` for memories; `MemoryHistory`, `OffLimitsEditor`, and `Edit` for the other targeted owners), but the real Pixel tap does not reach the expected destination. Two reversible, uncommitted in-class attempts — elevating the existing action and substituting the shared icon-only Button — did not alter the result and were reverted. No speculative source change remains.

**Diagnosis and fix:** The header action passed the top-level `"things-to-remember"` layout ID into the child-only knowledge router. It was a valid accessible target but matched no child route, so its press was a no-op. Commit `39ab1c7` maps the header action to the `memories` child owner and centralizes the child-to-editor destination contract; regression tests invoke the rendered press and prove it resolves to `ThingsToRemember`.

**Physical retest:** PASS — after force-stopping and relaunching the debug build, I opened `UAT Ada`, tapped the same enabled `Edit Things to Remember` target at `[1171,1486][1325,1640]`, and observed the full `Things to Remember` editor with its Back action. The resulting UI dump is retained locally at `/tmp/orbit-381-profile-edit-pass.xml`.

**Disposition:** RESOLVED / PASS for the targeted affordance. The phase remains unverified until the remaining matrix and backstops below are completed.

### Remaining required device work

The following required cells are deliberately **not passed or inferred**: no-data, long-history (> five preview), Bound/Unbound lifecycle, multiple full/half/orphan Relationship Overview arrangements including large font, Standard/Galaxy parity, Year heatmap scroll interaction, List/search Grid behavior, and Orrery tint/control legibility.

The Pixel did confirm a Bound Profile keeps `Unbind contact` out of the body and exposes it only through the overflow menu; the exact fresh overflow dump is `/tmp/orbit-381-uat-profile-overflow.xml`.

**Off Limits DB backstop: NOT CONFIRMED / investigate before phase close.** On `UAT Ada`, I opened the existing Edit Contact → Off Limits accordion, entered synthetic `PixelUAT-OffLimits` into the scoped draft, and attempted the visible `Add fuel item` action. After force-stopping, WAL-aware `run-as` copies (`orbit.db`, `orbit.db-wal`, `orbit.db-shm`) at `/tmp/orbit-381-after-brxqiM` showed no `fuel` row for contact 1, while its pre-existing `memories.general = PixelUATMemory` row remained. The copy procedure itself is valid (debuggable package and `run-as` succeeded); this does **not** establish preservation of other fuel kinds and must be reproduced/determined before claiming the high-severity backstop. No real AI request was made.

Local screenshots and UI dumps are retained under `/tmp/orbit-381-*` as uncommitted evidence.
