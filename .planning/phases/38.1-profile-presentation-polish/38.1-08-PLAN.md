---
phase: 38.1-profile-presentation-polish
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logic/list-row-selection.ts
  - src/logic/list-row-selection.test.ts
  - src/components/GridCard.tsx
autonomous: true
requirements: [R-14]
estimate:
  tokens: 34000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A normal Contacts Grid card shows exactly two textual rows: contact name and last-contact/recency information — the ordinary profile excerpt/third line is removed (§L)."
    - "Contacts List view retains its existing third-line excerpt behavior (§L)."
    - "During search, a Grid card MAY show a third line that explains why the contact matched (search-match context is semantically distinct from the routine excerpt) (§L)."
    - "Grid vs List vs search line-3 selection stays centralised through the existing selectLine3() selector — no per-component divergence (§L)."
  artifacts:
    - src/logic/list-row-selection.ts
    - src/logic/list-row-selection.test.ts
    - src/components/GridCard.tsx
  key_links:
    - "selectLine3(context) -> GridCard renders line-3 only for the search-match context; ListRow keeps its excerpt line-3."
---

<objective>
Reduce normal Contacts Grid cards to two rows (name + recency) by removing the routine profile excerpt, while preserving the List view's third-line excerpt and the Grid's search-match third line — routed through the existing centralised `selectLine3()` selector so Grid/List/search behavior stays in one place (§L). Planned against the authoritative dossier §L (D-01); this is the code half of the §Q "Contacts Grid density" group (D-03) (the bounded audit + device UAT half is Plan 09).

Purpose: today normal Grid cards render a truncated third-line excerpt that adds noise; §L keeps it out of the normal Grid but keeps it in List and keeps a distinct search-match line in Grid.
Output: `selectLine3()` gating (+ tests); GridCard renders line-3 only for the search-match context.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@src/logic/list-row-selection.ts
@src/logic/list-row-selection.test.ts
@src/components/GridCard.tsx
</context>

<artifacts_produced>
Net-new behavior this plan introduces (source-grounding: exclude from drift verification — net-new here):
- A gating branch in `selectLine3()` (list-row-selection.ts) that suppresses the routine excerpt for the normal Grid context while keeping it for List and for the Grid search-match context.
- New/updated cases in list-row-selection.test.ts for Grid-normal (no line-3), List (line-3 kept), Grid-search (match line-3 shown).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate line-3 selection by context (§L)</name>
  <files>src/logic/list-row-selection.ts, src/logic/list-row-selection.test.ts</files>
  <read_first>
    - src/logic/list-row-selection.ts — the existing `selectLine3()` selector (the central decider for Grid/List line-3).
    - src/logic/list-row-selection.test.ts — existing cases to extend.
    - RESEARCH.md §"§L — Contacts Grid density" and PATTERNS.md §"§L" — normal Grid = name + recency only; List retains line-3; Grid MAY keep line-3 during search (semantically distinct).
    - src/components/ListRow.tsx:234-248 (reference: List keeps its line-3 — do NOT change List).
  </read_first>
  <behavior>
    - selectLine3 returns no routine excerpt line for the normal (non-search) Grid context.
    - selectLine3 returns the existing excerpt line for the List context (unchanged).
    - selectLine3 returns a search-match explanation line for the Grid search context (distinct from the routine excerpt).
    - Tests cover all three contexts.
  </behavior>
  <action>
    Extend `selectLine3()` to gate on the render context so the normal Grid context yields no routine excerpt, the List context keeps its excerpt behavior unchanged, and the Grid search context yields the search-match explanation line. Keep the decision centralised in this selector (do not push the branch into the components). Add the three-context cases to list-row-selection.test.ts. Do not alter List behavior.
  </action>
  <reversibility rating="reversible">Pure selector logic; no data, no schema (D-02).</reversibility>
  <verify>
    <automated>npx vitest run src/logic/list-row-selection.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/logic/list-row-selection.test.ts` exits 0 with cases for Grid-normal (no line-3), List (line-3 kept), Grid-search (match line-3).
    - List behavior is provably unchanged (its case still returns the excerpt).
  </acceptance_criteria>
  <done>Line-3 selection is context-gated centrally: normal Grid has none, List keeps its excerpt, Grid-search shows the match line.</done>
</task>

<task type="auto">
  <name>Task 2: GridCard renders line-3 only for the search-match context (§L)</name>
  <files>src/components/GridCard.tsx</files>
  <read_first>
    - src/components/GridCard.tsx — the line-3 render fed by `selectLine3()`, and the search snippet block (~:324-340).
    - The updated `selectLine3()` contract from Task 1.
  </read_first>
  <action>
    Make GridCard render its third row only when `selectLine3()` yields a line for the current context — so a normal Grid card shows only name + recency, and a Grid card during search shows the search-match line. Do not hardcode the excerpt; consume the selector. Keep the name row (Label role) and recency row (Caption role) using existing typography tokens (UI-SPEC).
  </action>
  <reversibility rating="reversible">Render change consuming the selector; no data, no schema.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run src/logic/list-row-selection.test.ts</automated>
    <human-check>On the Pixel, the normal Contacts Grid shows two rows per card (name + recency, no excerpt); the List view still shows its third-line excerpt; typing a search that matches on profile text shows a Grid third line explaining the match. (Batched into Plan 09 end-of-phase device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - Reading GridCard confirms line-3 renders only when the selector yields one (no hardcoded excerpt).
    - `npx tsc --noEmit` exits 0.
    - Device UAT (Plan 09): normal Grid = 2 rows; List = 3 rows; Grid-search = match line present.
  </acceptance_criteria>
  <done>Normal Grid cards drop the excerpt; List retains it; Grid search shows the match line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contact data → Grid/List card text | Card text is read-only presentation of local contact data; the change removes a line, it does not expand what is shown or transmitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-08-01 | Information disclosure | Grid/List card text / network | low | accept | Local-first read path; no network; the change reduces shown text. No new fetch (CLAUDE.md). |
| T-38.1-08-02 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); selector + render only. |

No high-severity threats.
</threat_model>

<verification>
- `npx vitest run src/logic/list-row-selection.test.ts` green (three contexts).
- `npx tsc --noEmit` clean.
- Device Grid/List/search checks queued for Plan 09 (end-of-phase UAT).
</verification>

<success_criteria>
- Normal Contacts Grid cards show name + recency only; List keeps its excerpt; Grid-during-search shows a distinct match line; all routed through the centralised selector.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-08-SUMMARY.md` when done.
</output>
