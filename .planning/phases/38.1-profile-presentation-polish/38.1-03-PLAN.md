---
phase: 38.1-profile-presentation-polish
plan: 03
type: execute
wave: 2
depends_on: [38.1-01]
files_modified:
  - src/components/orrery/OrreryControls.tsx
  - src/components/orrery/OrrerySystemSelector.tsx
  - src/components/orrery/OrreryClusterPanel.tsx
  - src/components/orrery/OrreryFocusContext.tsx
  - src/components/orrery/OrreryViewOptions.tsx
autonomous: true
requirements: [R-01, R-02]
estimate:
  tokens: 56000
  raw_tokens: 37000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The named Orrery floating overlays (OrreryControls, OrrerySystemSelector, OrreryFocusContext, OrreryClusterPanel, OrreryViewOptions) render with treatment=\"orrery-overlay\" so they are translucent-yet-legible and no longer inherit ordinary content-card opacity (§B / D-04)."
    - "The single combined nav card is replaced by three independent, equally-sized SQUARE icon-only controls (Contacts, Recenter, Center North), tightly grouped with a consistent xs (4px) gap (§C)."
    - "The three separate controls stay inside a constrained/scroll GROUP container (the existing ScrollView bounded by `maxHeight: region.height - 2` at OrreryControls.tsx:90-91 is preserved, not dropped) so they cannot overflow the measured obstacle region at large OS font scale or constrained height (review MEDIUM)."
    - "Square dimensions are applied to the layer that renders the glass box — each control's `GlassSurface` (equal width===height) — not merely to the inner `Pressable`; wrapping a Pressable in a GlassSurface does not by itself make the visible glass box square (review MEDIUM)."
    - "Visible button text is removed; the current icons are preserved (§C)."
    - "Each control's size is icon + padding reaching a >=44x44 touch target (hitSlop where the visual is smaller), and each keeps its explicit accessibility label (Contacts in this System / Recenter Orrery / Reset north) (§C / §M)."
    - "The Reanimated animatedProps / orientation wiring on the north control is preserved and helper definitions stay above their callers (no worklet forward-ref hazard) (§C)."
    - "OrreryContactsSheet is excluded from the treatment (it uses Sheet, not GlassSurface, and does not reveal the orrery behind it) unless plan-time inspection shows it needs it (§R item 1, Assumption A2)."
    - { statement: "The overlay tint is readable AND still reveals the orrery behind it on the real orrery backdrop across theme/mode, tuned on-device (Phase-23-style AA flag-for-owner is acceptable if a mismatched combo is marginal).", verification: backstop }
  artifacts:
    - src/components/orrery/OrreryControls.tsx
    - src/components/orrery/OrrerySystemSelector.tsx
    - src/components/orrery/OrreryClusterPanel.tsx
    - src/components/orrery/OrreryFocusContext.tsx
    - src/components/orrery/OrreryViewOptions.tsx
  key_links:
    - "GlassSurface treatment=\"orrery-overlay\" (from Plan 01) -> each overlay call site -> orreryOverlayTintOpacity token."
    - "Three separate GlassSurface controls grouped with SPACING.xs -> preserved accessibilityLabels + >=44x44 targets."
---

<objective>
Apply the Plan-01 Orrery overlay treatment across the named floating overlays and split the combined nav card into three uniform icon-only glass controls (§B/§C). Planned against the authoritative dossier §B/§C (D-01), the second half of the §Q "Orrery surface semantics + navigation controls" ownership group (D-03). Depends on Plan 01 (the `treatment` prop + AA-proven token must exist first).

Purpose: overlays currently inherit `cardTintOpacity` (galaxy 0.05 — near-transparent, the §B complaint); routing them onto the dedicated treatment makes them legible without retuning card opacity. The combined Contacts/Recenter/Center-North card becomes three tightly-grouped square icon-only controls (§C).
Output: `treatment="orrery-overlay"` applied at the overlay call sites; three square icon-only nav controls with preserved a11y + touch targets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@.planning/phases/38.1-profile-presentation-polish/38.1-01-SUMMARY.md
@src/components/orrery/OrreryControls.tsx
@src/components/orrery/OrrerySystemSelector.tsx
@src/components/orrery/OrreryClusterPanel.tsx
@src/components/orrery/OrreryFocusContext.tsx
@src/components/orrery/OrreryViewOptions.tsx
@src/components/orrery/OrreryContactsSheet.tsx
@src/components/ui/GlassSurface.tsx
</context>

<artifacts_produced>
Net-new here (source-grounding: exclude from drift verification):
- Three independent square icon-only control elements inside OrreryControls (replacing the single combined card), each a `GlassSurface treatment="orrery-overlay"` with preserved accessibilityLabel + >=44x44 target.
- `treatment="orrery-overlay"` prop usage at the OrrerySystemSelector / OrreryFocusContext / OrreryClusterPanel / OrreryViewOptions GlassSurface call sites.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Apply treatment="orrery-overlay" to the named floating overlays (§B / D-04)</name>
  <files>src/components/orrery/OrrerySystemSelector.tsx, src/components/orrery/OrreryClusterPanel.tsx, src/components/orrery/OrreryFocusContext.tsx, src/components/orrery/OrreryViewOptions.tsx</files>
  <read_first>
    - The `GlassSurface` call sites in each file: OrrerySystemSelector.tsx:142,170; OrreryClusterPanel.tsx:49; OrreryFocusContext.tsx:87; OrreryViewOptions.tsx:104,123 (per RESEARCH.md §"D-04 Orrery overlay treatment").
    - src/components/ui/GlassSurface.tsx — the `treatment` prop added by Plan 01 (default `"card"`).
    - src/components/orrery/OrreryContactsSheet.tsx:12,54 — uses `Sheet variant="detail"`, NOT GlassSurface (Assumption A2: exclude unless plan-time inspection shows it needs the treatment).
  </read_first>
  <action>
    Add `treatment="orrery-overlay"` to each named floating-overlay `GlassSurface` call site so they source opacity from the Plan-01 token instead of `cardTintOpacity`. Do not change any other props (density, style, border/glow). Inspect OrreryContactsSheet: it uses `Sheet`, not `GlassSurface`, and is a modal bottom sheet rather than a floating orrery control — exclude it unless the inspection shows it visually behaves as a floating overlay that reveals the orrery (record the decision in the SUMMARY, §R item 1).
  </action>
  <reversibility rating="reversible">Opt-in prop at call sites; trivially removable; no data, no schema (D-02).</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <automated>npx vitest run src/theme/tokens/surface.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each named overlay `GlassSurface` carries `treatment="orrery-overlay"` (verified by reading each call site).
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - The OrreryContactsSheet inclusion/exclusion decision is recorded (§R item 1).
  </acceptance_criteria>
  <done>The named floating overlays render on the dedicated translucent treatment; the ordinary card opacity is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Split the combined nav card into three square icon-only controls (§C)</name>
  <files>src/components/orrery/OrreryControls.tsx</files>
  <read_first>
    - src/components/orrery/OrreryControls.tsx:89-142 — the single combined `GlassSurface` (:89) wrapping a `ScrollView` (:90) constrained by `maxHeight: region.height - 2` (:91) with `contentContainerStyle={styles.stack}` (:92); `styles.stack` gap (:149), `styles.control` (minWidth/minHeight 44, row layout, padding — :150-157); Contacts (:98,105-108), Recenter (:113,120-123), Reset-north (:128,136-139) with its Animated `useAnimatedProps`/orientation wiring (:49-53). The `maxHeight`-bounded scroll group is the overflow guard to PRESERVE when splitting the card.
    - RESEARCH.md §"Orrery nav controls (§C)" and PATTERNS.md §"§J … worklet hazard" note; MEMORY worklet-forward-ref-hazard (define helpers above callers).
    - UI-SPEC §"Interaction Contracts … Orrery navigation controls (§C)" — three equal squares, xs gap, drop labels, keep icons + a11y labels, 44x44 floor.
  </read_first>
  <action>
    Replace the single combined card with THREE independent, equally-sized square controls (Contacts, Recenter, Center North), each wrapped in its own `GlassSurface treatment="orrery-overlay"`, tightly grouped with a consistent `SPACING.xs` (4px) gap. Keep the three controls inside a CONSTRAINED GROUP container: preserve the `maxHeight: region.height - 2` scroll bound (OrreryControls.tsx:90-91) around the group so three separate surfaces cannot overflow the measured obstacle region at constrained height or large OS font scale (review MEDIUM) — the group scrolls if it must, exactly as today. Apply the square dimensions (equal width===height) to each control's `GlassSurface` layer (the visible glass box), NOT just the inner `Pressable` — wrapping a Pressable in a GlassSurface does not auto-square the outer visual (review MEDIUM). Remove the three visible `AppText role="label"` captions but KEEP each control's existing `accessibilityLabel` verbatim (Contacts in this System / Recenter Orrery / Reset north) and keep the current `Icon` glyphs. Each square = icon + padding reaching the 44x44 touch floor (retain minWidth/minHeight:44; add `hitSlop` if the visual square is smaller). Preserve the `disabled`/`accessibilityState` and the north control's Reanimated `useAnimatedProps`/orientation `accessibilityValue` wiring untouched — keep any worklet/helper definitions ABOVE their callers so the restructure cannot introduce a Hermes forward-ref crash (MEMORY: worklet-forward-ref-hazard; vitest will not catch it — verify on the Pixel). Verify focus restoration still targets Contacts after the restructure. Remove now-unused style entries.
  </action>
  <reversibility rating="reversible">Component layout change; no data, no schema.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
    <human-check>On the Pixel (physical device — Skia orrery perf/legibility is only assessable on device, not the emulator): the three nav controls render as tightly-grouped equal squares with icons only, each tappable at >=44x44, with correct TalkBack labels; the north control still animates/reflects orientation with no crash; overlays are readable yet still reveal the orrery behind them across theme/mode. Tune the Plan-01 tint scalar here if a mismatched combo is marginal (flag-for-owner if it cannot be both legible and revealing — Phase-23 precedent). (Batched into Plan 09 end-of-phase device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - Reading OrreryControls confirms three separate square `GlassSurface treatment="orrery-overlay"` controls, `SPACING.xs` gap, no visible labels, icons retained, accessibilityLabels retained, >=44x44 targets, north-control animation wiring intact with helpers above callers.
    - Reading OrreryControls confirms the constrained/scroll group container (the `maxHeight: region.height - 2` bound) is preserved around the three controls, and the square dimensions are applied to each control's `GlassSurface` layer (not just the inner Pressable).
    - `npx tsc --noEmit` and `npm run check:colors` exit 0.
    - Device UAT (Plan 09) confirms legibility + reveal + no worklet crash + no overflow at large font (backstop).
  </acceptance_criteria>
  <done>Three uniform square icon-only nav controls, grouped with a small gap, accessible, on the overlay treatment, with the animation wiring preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme token → orrery overlay legibility | An overlay too transparent (or too opaque) degrades control readability (accessibility), not data integrity. |
| Reanimated worklet (north control) | A forward-ref/worklet ordering mistake crashes on-device (Hermes) though tests pass — a runtime-availability risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-03-01 | Denial of service | OrreryControls north-control worklet | medium | mitigate | Preserve animatedProps wiring; keep helpers above callers (MEMORY worklet-forward-ref-hazard); verify on the Pixel (vitest cannot catch it). |
| T-38.1-03-02 | Denial of service (accessibility) | overlay tint legibility | medium | mitigate | Plan-01 AA proof (all 4 combos) + on-device tuning; flag-for-owner if a mismatched combo cannot be both legible and revealing (backstop). |
| T-38.1-03-03 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); component/token application only. |
| T-38.1-03-04 | Information disclosure | read path / network | low | accept | No network; local-first render (CLAUDE.md). |

No high-severity threats.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run check:colors`, and `npx vitest run src/theme/tokens/surface.test.ts` all green.
- Device legibility + three-control + worklet checks queued for Plan 09 (end-of-phase UAT) — physical Pixel required (emulator cannot assess Skia orrery).
</verification>

<success_criteria>
- The named overlays are legible on the dedicated treatment (card opacity untouched) and the combined nav card is three uniform, accessible, icon-only square controls with the north-control animation intact.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-03-SUMMARY.md` when done. Record the OrreryContactsSheet inclusion/exclusion decision and the final on-device tint scalar (and any owner flag).
</output>
