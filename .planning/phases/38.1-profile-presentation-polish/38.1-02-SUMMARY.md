---
phase: 38.1-profile-presentation-polish
plan: "02"
subsystem: date-formatting
tags: [timestamps, local-time, vitest, typescript]
requires:
  - phase: existing-date-utilities
    provides: UTC-safe local wall-clock parsing through parseLocalMs
provides:
  - A shared, locale-independent minute-precision timestamp display formatter
  - A neutral invalid-timestamp fallback safe for future display consumers
affects: [38.1-04, profile-history, interaction-detail, memory-history]
actuals:
  tokens: 897
  tasks: 1
  commits: 2
tech-stack:
  added: []
  patterns:
    - Display-only timestamp formatting parses stored local wall-clock values with parseLocalMs.
    - Time-format branches stay pure and directly testable without mutable module state.
key-files:
  created: []
  modified:
    - src/utils/dates.ts
    - src/utils/dates.test.ts
key-decisions:
  - "Keep TIME_FORMAT private and defaulted to 12h; future settings need only replace that single source."
  - "Use a fixed English month table and manual AM/PM clock construction so display output is independent of ambient locale."
patterns-established:
  - "Display call sites use formatDateTimeMinuteOrFallback rather than exposing raw invalid storage text."
requirements-completed: [R-13]
coverage:
  - id: D1
    description: "Shared formatter renders local stored timestamps at minute precision with a 12-hour default and a directly tested 24-hour branch."
    requirement: R-13
    verification:
      - kind: unit
        ref: "src/utils/dates.test.ts#minute-precision display timestamps"
        status: pass
      - kind: other
        ref: "npx tsc --noEmit; npm run check:colors"
        status: pass
    human_judgment: false
  - id: D2
    description: "Display consumers can use a neutral fallback that never leaks raw seconds-bearing storage text after a parse failure."
    requirement: R-13
    verification:
      - kind: unit
        ref: "src/utils/dates.test.ts#returns a neutral display label rather than the raw timestamp when parsing fails"
        status: pass
    human_judgment: false
duration: 1 min
completed: 2026-09-20
status: complete
---

# Phase 38.1 Plan 02: Shared Minute-Precision Timestamp Formatter Summary

**A locale-independent local-wall-clock formatter now presents stored timestamps as `Sep 19, 2026, 7:14 PM`, with a neutral malformed-value fallback for Plan 04 display consumers.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-09-20T04:43:58Z
- **Completed:** 2026-09-20T04:45:40Z
- **Tasks:** 1/1
- **Files modified:** 2

## Accomplishments

- Added `formatDateTimeMinute()`, which parses local stored timestamps through `parseLocalMs`, preserves the stored value, and omits seconds in display output.
- Added private `TIME_FORMAT` as the single 12-hour/24-hour source and exported the pure `formatMinuteClock()` primitive so both clock branches are unit-tested without mutable test state.
- Added `formatDateTimeMinuteOrFallback()`, returning `Unknown time` instead of raw malformed storage text.

## Task Commits

1. **Task 1: Write formatter tests (RED) then implement formatDateTimeMinute (GREEN)** — `0acf89a` (test), `bb6f3d5` (feat)

## Files Created/Modified

- `src/utils/dates.ts` — shared display-only formatter, pure clock primitive, fixed English month abbreviations, and safe fallback.
- `src/utils/dates.test.ts` — boundary, 24-hour, default-format, malformed-input, and regression coverage.

## Decisions Made

- Kept `TIME_FORMAT` as the one private module-level source, preserving the deferred user-toggle boundary while making the default explicit.
- Used an in-module English abbreviation table and AM/PM calculation rather than ambient-locale formatting.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The planned RED test failed before implementation; the targeted suite, TypeScript check, and color guard passed after implementation. `state.advance-plan` could not parse the pre-existing `STATE.md` current-plan format, and `state.update-progress` skipped because the phase scope is truncated; the other required state, metric, session, and roadmap updates completed. This plan did not alter that pre-existing planning-metadata shape.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 04 can route the three confirmed explicit-timestamp offenders through `formatDateTimeMinuteOrFallback()` without exposing seconds or raw malformed values. Pixel appearance checks remain batched in Plan 09.

## Self-Check: PASSED

- Confirmed `src/utils/dates.ts`, `src/utils/dates.test.ts`, and this SUMMARY exist.
- Confirmed task commits `0acf89a` and `bb6f3d5` exist in git history.
- Confirmed coverage metadata classifies both deliverables as automatically covered.

---
*Phase: 38.1-profile-presentation-polish*
*Completed: 2026-09-20*
