---
phase: 38.1-profile-presentation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/tokens/surface.ts
  - src/theme/tokens/surface.test.ts
  - src/components/ui/GlassSurface.tsx
autonomous: true
requirements: [R-01]
estimate:
  tokens: 44000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A new named surface treatment for Orrery floating overlays exists in src/theme/tokens/surface.ts, decoupled from CARD_GLASS_OPACITY/cardTintOpacity (D-04)."
    - "The new treatment returns a translucent opacity in ALL FOUR package×mode combos (galaxy-dark, galaxy-light, standard-dark, standard-light) — it never falls through to the opaque density band, even in the mismatched combos (D-04)."
    - "surface.test.ts proves text/status foregrounds over the overlay tint composited on the brightest orrery pixel meet AA in all 4 combos; the mismatched combos are the binding constraint. The proof DOCUMENTS its conservative assumption: it models the tint over the RAW brightest pixel with no BlurView contribution — a real `BlurView` (GlassSurface.tsx:87-107) averages/dims the backdrop, so the on-device composite is never brighter (better contrast) than the modeled one (review MEDIUM)."
    - "Beyond the `< densityOpacity` numeric-translucency check, surface.test.ts asserts a VISIBILITY FLOOR — the overlay tint opacity stays at or below a backdrop-visibility ceiling so the orrery demonstrably shows through (translucency is perceptual, not just numeric) (review LOW)."
    - "CARD_GLASS_OPACITY (galaxy 0.05 / standard 0.5) and cardTintOpacity are byte-for-byte unchanged; every pre-existing card AA test still passes (D-04)."
    - "GlassSurface gained a treatment prop (default preserves current card behavior); no fork, no parallel component family (ADR-087, D-04)."
    - { statement: "The chosen overlay tint opacity reads as translucent (orrery visible behind) yet legible on the real orrery backdrop.", verification: backstop }
  artifacts:
    - src/theme/tokens/surface.ts
    - src/theme/tokens/surface.test.ts
    - src/components/ui/GlassSurface.tsx
  key_links:
    - "GlassSurface treatment prop -> orreryOverlayTintOpacity(pkg, mode) -> surface.ts token (consumed by Plan 03 at the six Orrery overlay call sites)."
    - "surface.test.ts brightestOrreryPixel derived from {textPrimary, ...starPalette} per OrreryWorld.tsx:272-273 / Polaris.tsx:30."
---

<objective>
Land the D-04 shared foundation for Orrery overlay legibility: a NEW named surface treatment in the theme-token layer, AA-proven against the actual orrery backdrop, and a `treatment` prop on the existing `GlassSurface` that routes to it — WITHOUT forking GlassSurface (ADR-087) and WITHOUT changing ordinary content-card opacity. Planned against the authoritative dossier §B (D-01), grouped as a cross-cutting foundation per dossier §Q (D-03).

This is a Wave-1 foundation (the TRACER-reconciled early slice for the Orrery surface): the token + AA proof + prop must land and be proven green before Plan 03 applies `treatment="orrery-overlay"` at the six overlay call sites and splits the nav controls.

Purpose: overlays today render via `GlassSurface density="dense"` → `cardTintOpacity()` → galaxy `0.05` (near-transparent — the §B complaint). Decoupling them onto a dedicated translucent-everywhere treatment fixes legibility without retuning the shared card constant.
Output: `ORRERY_OVERLAY_TINT_OPACITY` + `orreryOverlayTintOpacity()` in surface.ts; a `treatment` prop on GlassSurface; a new orrery-backdrop AA block in surface.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@docs/decisions/ADR-115-visible-mode-aware-background-surface-composition.md
@docs/decisions/ADR-087-bundled-background-presets-and-package-specific-surface-treatment.md
@src/theme/tokens/surface.ts
@src/theme/tokens/surface.test.ts
@src/components/ui/GlassSurface.tsx
@src/theme/contrast.ts
</context>

<artifacts_produced>
New symbols/blocks this plan introduces (source-grounding: exclude these from drift verification — they are net-new here):
- `ORRERY_OVERLAY_TINT_OPACITY: Record<ThemePackage, number>` (surface.ts) — new named treatment constant, distinct from `CARD_GLASS_OPACITY`.
- `orreryOverlayTintOpacity(themePackage, mode): number` (surface.ts) — new pure helper; translucent in all 4 combos; never returns the opaque density band.
- `treatment?: "card" | "orrery-overlay"` prop on `GlassSurface` (GlassSurface.tsx), default `"card"`.
- New test block in surface.test.ts asserting foregrounds-over-overlay-tint AA over the brightest orrery pixel across all 4 pkg×mode combos (plus a guard test that `CARD_GLASS_OPACITY` values are unchanged).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the orrery-backdrop AA proof (RED) then the decoupled token treatment (GREEN)</name>
  <files>src/theme/tokens/surface.test.ts, src/theme/tokens/surface.ts</files>
  <read_first>
    - src/theme/tokens/surface.ts:90-181 — SURFACE token set (`tintTokenKey: "surface"`), `densityOpacity` bands (the OPAQUE fallback the new treatment must NOT use), `CARD_GLASS_OPACITY` (galaxy 0.05 / standard 0.5), `cardMatchesMode`, `cardTintOpacity`. MIRROR this shape; do NOT mutate it.
    - src/theme/tokens/surface.test.ts:186-206 — the existing composite-over-brightest-pixel AA loop (the exact pattern to copy for the orrery block); also the monotonic/ordering guards ~135-151.
    - src/theme/contrast.ts:20,22,67 — `AA_NORMAL=4.5`, `AA_LARGE=3.0`, `contrastRatio()`; and surface.ts `alphaComposite(fg,bg,alpha)` (~:324).
    - src/components/orrery/OrreryWorld.tsx:272-273,521 and src/components/orrery/Polaris.tsx:30 — the orrery draws stars from `{colors.textSecondary, colors.textPrimary, ...colors.starPalette}` over `colors.background`; sun/Polaris uses `starPalette[0] ?? textPrimary`. The worst-case (brightest) orrery pixel per palette is the max-luminance member of `{textPrimary, ...starPalette}`.
  </read_first>
  <behavior>
    - New test block iterates the 4 pkg×mode palettes. For each: brightestOrreryPixel = the max-luminance colour of {palette.textPrimary, ...palette.starPalette}; composite = alphaComposite(palette.surface, brightestOrreryPixel, orreryOverlayTintOpacity(pkg, mode)); assert every text/status foreground over `composite` clears AA (assertForegroundsAA analog), including the two MISMATCHED combos (galaxy-light, standard-dark) which are the binding constraint.
    - The AA block carries an explicit comment documenting the conservative modeling assumption: tint over the RAW brightest orrery pixel, no BlurView averaging — the real on-device composite (with BlurView) can only be dimmer/higher-contrast, so passing here is a lower bound (review MEDIUM).
    - Guard test: `CARD_GLASS_OPACITY.galaxy === 0.05` and `CARD_GLASS_OPACITY.standard === 0.5` (proves the card constant was not retuned).
    - orreryOverlayTintOpacity returns a value strictly less than the opaque `densityOpacity` band in every combo (proves "still reveals the orrery behind it") while clearing AA over the brightest star.
    - Visibility-floor assertion: `orreryOverlayTintOpacity(pkg, mode)` is <= a named backdrop-visibility ceiling in every combo (so at least that fraction of the orrery shows through), a perceptual-visibility check beyond the numeric `< densityOpacity` bound (review LOW).
  </behavior>
  <action>
    Write the orrery-backdrop AA block in surface.test.ts FIRST (RED — it references `orreryOverlayTintOpacity`, which does not yet exist). Mirror the loop at surface.test.ts:186-206 exactly, substituting brightestOrreryPixel (max-luminance over `{textPrimary, ...starPalette}`) for `slot.brightestPixel` and `orreryOverlayTintOpacity(pkg, mode)` for `cardTintOpacity(...)`. Then add to surface.ts a new `ORRERY_OVERLAY_TINT_OPACITY: Record<ThemePackage, number>` constant and a pure `orreryOverlayTintOpacity(themePackage, mode)` helper returning a single translucent scalar per package in ALL four combos — its key divergence from `cardTintOpacity` is it MUST NOT fall through to `SURFACE[pkg].densityOpacity` in the mismatched regime (the orrery canvas is a controlled dark render surface, not mismatched wallpaper — D-04). Tint colour stays the `surface` token (reuse `tintTokenKey`). Tune the scalar to the LOWEST value that clears AA over the brightest star in the adverse palettes (final on-device tuning happens in Plan 03 UAT — this is the AA floor), and confirm it also sits at/below the named backdrop-visibility ceiling so the orrery still shows through (assert both bounds). Add an explicit comment in the AA block documenting the conservative no-BlurView modeling assumption (tint over the raw brightest pixel; a real BlurView only improves contrast). Leave `CARD_GLASS_OPACITY`, `cardTintOpacity`, `cardMatchesMode`, and the density bands byte-for-byte unchanged (D-04). No hardcoded colour literals outside the sanctioned token files (CLAUDE.md; check:colors enforces location).
  </action>
  <reversibility rating="reversible">Additive token + test; no persisted data, no schema (D-02 untouched — this is theme-token/test only).</reversibility>
  <verify>
    <automated>npx vitest run src/theme/tokens/surface.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/theme/tokens/surface.test.ts` exits 0 with the new orrery block AND all pre-existing card AA tests still passing.
    - A test asserts `orreryOverlayTintOpacity(pkg, mode)` is translucent (< the opaque `densityOpacity` band) in all 4 combos and clears AA over the brightest orrery pixel in the 2 mismatched combos.
    - A visibility-floor test asserts the tint opacity is <= the named backdrop-visibility ceiling in all 4 combos (perceptual reveal, beyond the `< densityOpacity` bound), and the AA block documents the conservative no-BlurView assumption.
    - A guard test asserts `CARD_GLASS_OPACITY` = { galaxy: 0.05, standard: 0.5 } (unchanged).
    - `npx tsc --noEmit` and `npm run check:colors` both exit 0.
  </acceptance_criteria>
  <done>The orrery overlay treatment token exists, is proven AA in all 4 combos over the brightest star, is decoupled from the card constant, and the card constant is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the treatment prop to GlassSurface (no fork)</name>
  <files>src/components/ui/GlassSurface.tsx</files>
  <read_first>
    - src/components/ui/GlassSurface.tsx:66 — `const tintOpacity = cardTintOpacity(themePackage, mode, density);` (the single line that decides overlay opacity today) and the surrounding prop/type declaration.
    - src/theme/tokens/surface.ts — the `orreryOverlayTintOpacity` helper added in Task 1.
  </read_first>
  <action>
    Add a `treatment?: "card" | "orrery-overlay"` prop to GlassSurface, defaulting to `"card"`. When `treatment === "orrery-overlay"`, source the tint opacity from `orreryOverlayTintOpacity(themePackage, mode)` instead of `cardTintOpacity(themePackage, mode, density)`; otherwise preserve the existing `cardTintOpacity` path exactly. This is ONE component with ONE new token treatment — do NOT create a second component, a subclass, or a parallel family (ADR-087 / D-04). Do not change the default rendering of any current caller (the prop is opt-in). Keep the border/glow token wiring untouched.
  </action>
  <reversibility rating="reversible">Opt-in prop with a default that preserves current behavior; trivially removable.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run src/theme/tokens/surface.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GlassSurface accepts `treatment="orrery-overlay"` and, in that mode, its tint opacity comes from `orreryOverlayTintOpacity` (verified by reading the prop-branch in the file).
    - Default (`treatment` omitted or `"card"`) is byte-for-byte the prior `cardTintOpacity` path — no existing caller changes behavior.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>GlassSurface can render the orrery-overlay treatment via an opt-in prop, with the card path unchanged and no component fork.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme tokens → rendered UI | Token opacity values decide text legibility; a wrong value silently degrades readability (accessibility integrity), not a data leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-01-01 | Tampering | src/theme/tokens/surface.ts CARD_GLASS_OPACITY | medium | mitigate | Guard test asserts the card constant is unchanged; the new treatment is a separate constant/helper (D-04 anti-pattern: retuning the shared card constant). |
| T-38.1-01-02 | Information disclosure | read path / network | low | accept | No network, no data read on this path; theme-token + test only. Local-first read path unchanged (CLAUDE.md). |
| T-38.1-01-03 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no BACKUP_FORMAT_VERSION bump (D-02); this plan touches theme tokens + a component prop only. |

No high-severity threats. If AA cannot be met in a mismatched combo without going effectively opaque, that surfaces as a legibility/product tension to the owner (Plan 03 on-device), not a security block.
</threat_model>

<verification>
- `npx vitest run src/theme/tokens/surface.test.ts` — new orrery AA block + card-unchanged guard both green.
- `npx tsc --noEmit` clean (memory: tsc is not in a commit hook; run it explicitly).
- `npm run check:colors` clean (no hex literal outside the sanctioned token files).
</verification>

<success_criteria>
- A decoupled, AA-proven (all 4 combos) Orrery overlay treatment exists in the token layer and is reachable via an opt-in GlassSurface prop, with the ordinary card constant provably unchanged and no component fork. Plan 03 can now apply it.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-01-SUMMARY.md` when done. Record the chosen `ORRERY_OVERLAY_TINT_OPACITY` scalar per package and note that final on-device tuning is Plan 03's UAT responsibility.
</output>
