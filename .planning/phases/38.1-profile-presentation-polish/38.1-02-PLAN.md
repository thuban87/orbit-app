---
phase: 38.1-profile-presentation-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/dates.ts
  - src/utils/dates.test.ts
autonomous: true
requirements: [R-13]
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A single shared minute-precision display formatter exists in src/utils/dates.ts and returns the 12-hour form `Sep 19, 2026, 7:14 PM` for a stored `YYYY-MM-DD HH:MM:SS` timestamp (D-08)."
    - "The formatter reads the 12h/24h choice from ONE module-level source, so a future user toggle is a one-line add (the toggle itself is deferred out of 38.1 — D-08)."
    - "The formatter parses via the existing UTC-safe parseLocalMs — no toISOString().split('T')[0] (CLAUDE.md off-by-one hazard)."
    - "The new formatter is DISPLAY-only; it is not a 9th storage builder and none of the 8 storage builders are touched (D-08)."
    - "Relative-time formatters (Yesterday, 5d ago, 2 hours ago) are untouched (D-08 / §K)."
  artifacts:
    - src/utils/dates.ts
    - src/utils/dates.test.ts
  key_links:
    - "formatDateTimeMinute() in dates.ts is the single source consumed by Plan 04's three display offenders (profile-history-read.ts, interaction-detail-logic.ts, MemoryHistoryScreen.tsx)."
    - "Single module-level TIME_FORMAT source -> future 12/24h toggle is a one-line change."
---

<objective>
Land the D-08 shared foundation for app-wide minute-precision timestamps: ONE new display formatter in `src/utils/dates.ts` that renders `Sep 19, 2026, 7:14 PM`, reading 12h/24h from a single source so the deferred user toggle is a one-line add later. Planned against the authoritative dossier §K (D-01), grouped as a cross-cutting foundation per §Q (D-03).

This is a Wave-1 foundation (the TRACER-reconciled early slice for timestamps): the formatter + its tests must land and be proven green before Plan 04 reroutes the three verified display offenders through it.

Purpose: today's user-facing offenders render a raw `YYYY-MM-DD HH:MM:SS` string (a 24-hour, seconds-exposing format). A single shared formatter avoids adding a 9th ad-hoc builder and keeps stored precision unchanged.
Output: `formatDateTimeMinute()` + a single `TIME_FORMAT` source in dates.ts; new cases in dates.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@src/utils/dates.ts
@src/utils/dates.test.ts
</context>

<artifacts_produced>
New symbols this plan introduces (source-grounding: exclude from drift verification — net-new here):
- `formatDateTimeMinute(stored: string): string` (dates.ts) — display-only minute-precision formatter, 12-hour default.
- A single module-level format source in dates.ts (e.g. `const TIME_FORMAT: "12h" | "24h" = "12h"`) read by the formatter.
- New dates.test.ts cases for the formatter (12h output shape; midnight/noon boundaries; that relative-time helpers are unaffected).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write formatter tests (RED) then implement formatDateTimeMinute (GREEN)</name>
  <files>src/utils/dates.test.ts, src/utils/dates.ts</files>
  <read_first>
    - src/utils/dates.ts:17-22 (`formatLocalDate` style to mirror), :25-72 (`parseLocalMs` — the UTC-safe parse to reuse), and the whole pure module (no react-native import — the formatter must stay node-safe so logic files can call it).
    - src/utils/dates.test.ts — existing test structure to extend.
    - RESEARCH.md §"§K / D-08 — timestamp reformat" and PATTERNS.md §"§K / D-08 — timestamp formatter" — target format and single-source rule.
  </read_first>
  <behavior>
    - formatDateTimeMinute("2026-09-19 19:14:37") returns the 12-hour minute-precision form for Sep 19, 2026 at 7:14 PM (no seconds).
    - Noon and midnight render with correct 12-hour AM/PM (e.g. 00:xx -> 12:xx AM, 12:xx -> 12:xx PM).
    - Minute is zero-padded to two digits; the seconds component of the input is ignored (not shown).
    - Parsing goes through parseLocalMs (local wall-clock), so an evening timestamp does not shift a day (no UTC off-by-one).
    - A test documents that flipping the single TIME_FORMAT source would switch to 24-hour (assert current default is 12-hour without adding a public toggle).
    - Relative-time helpers already in dates.ts remain unchanged (their existing tests still pass).
  </behavior>
  <action>
    Add the failing cases to dates.test.ts first (RED), asserting the target rendered string for a known input plus the noon/midnight boundaries. Then add to dates.ts a single module-level format source and a pure `formatDateTimeMinute(stored)` that parses via `parseLocalMs`, builds the month-name + day + year + 12-hour hour:minute + AM/PM display string, and reads AM/PM-vs-24h from that single source. Do NOT route through `toISOString().split('T')[0]` (UTC off-by-one, CLAUDE.md). This formatter is DISPLAY-only: it must never be called from a write/storage path, and it must not become a 9th `HH:MM:SS` builder. Keep it a pure function (no react-native import) so `interaction-detail-logic.ts` (node env) can consume it in Plan 04.
  </action>
  <reversibility rating="reversible">Additive pure function + tests; no persisted data, no schema (D-02 untouched).</reversibility>
  <verify>
    <automated>npx vitest run src/utils/dates.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/dates.test.ts` exits 0 with the new formatter cases and the pre-existing relative-time tests still passing.
    - `formatDateTimeMinute("2026-09-19 19:14:37")` output contains the month name, day, year, and `7:14 PM`, and contains no seconds component.
    - The 12h/24h behavior is governed by exactly one module-level constant (verified by reading the source — a single point of change).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A single shared, UTC-safe, minute-precision 12-hour display formatter exists and is proven by tests; relative-time and storage builders are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored timestamp string → display | A display formatter that accidentally rewrote stored precision, or an off-by-one parse, would corrupt what the user sees (and, if misused on a write path, stored data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-02-01 | Tampering | src/utils/dates.ts storage builders | medium | mitigate | Formatter is display-only and separate from the 8 storage builders; D-08 anti-pattern (9th builder / reformatting a storage builder) is explicitly excluded and no storage builder file is in files_modified. |
| T-38.1-02-02 | Tampering | date parsing (UTC off-by-one) | medium | mitigate | Reuse `parseLocalMs`; forbid `toISOString().split('T')[0]` (CLAUDE.md); midnight/evening boundary tests. |
| T-38.1-02-03 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); pure utility + test only. |

No high-severity threats.
</threat_model>

<verification>
- `npx vitest run src/utils/dates.test.ts` green (formatter + boundaries; relative-time unaffected).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- The single shared minute-precision formatter exists, is 12-hour by default from one source, is UTC-safe, and is proven by tests. Plan 04 can now reroute the display offenders through it with no seconds exposure.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-02-SUMMARY.md` when done. Name the formatter symbol and the single format-source constant for Plan 04's consumers.
</output>
