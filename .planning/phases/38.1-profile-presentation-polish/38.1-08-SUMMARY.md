---
phase: 38.1-profile-presentation-polish
plan: "08"
subsystem: contacts-dashboard
tags: [grid, list, search, presentation]
dependency_graph:
  requires: [ADR-101, dossier-38.1-L]
  provides: [normal-grid-two-row-contract]
  affects: [GridCard, HomeScreen, CardGrid, ListRow]
tech_stack:
  added: []
  patterns: [mocked-react-native-render-tree-tests, host-owned-list-line3-selection]
key_files:
  created:
    - src/components/grid-card-render.test.tsx
  modified:
    - src/components/GridCard.tsx
    - src/screens/HomeScreen.tsx
decisions:
  - "Normal Grid cards omit routine line three at GridCard while CardGrid retains its shared prop chain."
  - "HomeScreen computes routine line three only for List mode; search context remains GridCard-owned and independent."
metrics:
  duration: 12m
  completed_date: 2026-09-20
status: complete
actuals:
  tokens: 23881
  tasks: 2
  commits: 2
---

# Phase 38.1 Plan 08: Contacts Grid Density Summary

Normal Contacts Grid cards now render only their name and recency in ordinary mode, while search explanations and List excerpts retain their independent presentation paths.

## Tasks Completed

1. **GridCard omits routine line three; host stops card-mode selection**
   - Removed the ordinary Grid excerpt render branch without changing the search explanation/snippet branch.
   - Limited HomeScreen's candidate read and `selectLine3` computation to normal List mode.
   - Retained CardGrid's shared `line3ByContactId` forwarding as specified; it is intentionally dead for ordinary Grid cards.
   - Commit: `90dfb64`

2. **Render coverage for Grid and List line-three contracts**
   - Added node-safe mocked React Native render tests for normal Grid, search Grid, and normal List behavior.
   - Commit: `a01d123`

## Verification

- `npx vitest run src/components/grid-card-render.test.tsx src/logic/card-line3-selection.test.ts src/logic/list-row-selection.test.ts` — 3 files, 14 tests passed.
- `npx tsc --noEmit` — passed.
- `npm run check:colors` — passed.
- `npx biome check src/components/grid-card-render.test.tsx` — passed.
- Pixel Grid/List/search verification remains intentionally batched in Plan 09.

## Decisions Made

- The routine Grid excerpt is removed at `GridCard`; `CardGrid.tsx` remains untouched so its shared prop chain is preserved.
- `selectLine3` and `ListRow` remain the normal List excerpt path; `selectCardLine3` is no longer invoked by HomeScreen.

## Deviations from Plan

None — implementation scope matched the plan. Biome's focused three-file check reported pre-existing formatting/lint findings in `GridCard.tsx` and `HomeScreen.tsx`; only the newly created test was formatted, and no unrelated cleanup was included.

## TDD Gate Compliance

Task 2's render test was necessarily introduced after Task 1's implementation because the plan orders implementation first. Its first valid run was green against the just-committed behavior; no artificial failing assertion or separate RED commit was created.

## Known Stubs

None.

## Self-Check: PASSED

- Confirmed `src/components/grid-card-render.test.tsx` exists.
- Confirmed commits `90dfb64` and `a01d123` exist in git history.
