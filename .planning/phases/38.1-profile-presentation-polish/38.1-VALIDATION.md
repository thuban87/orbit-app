---
phase: 38.1
slug: profile-presentation-polish
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-19
---

# Phase 38.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by plan-phase from `38.1-RESEARCH.md` §Validation Architecture. Per-task rows are filled by the planner/validate-phase once PLAN.md task IDs exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^4.1.10 |
| **Config file** | in-repo vitest config (node env for pure logic; `surface.ts` / logic files are react-native-free) |
| **Quick run command** | `npx vitest run <file>` |
| **Full suite command** | `npm test` (`vitest run`) + `npx tsc --noEmit` + `npm run check:colors` |
| **Estimated runtime** | ~30–60 seconds (unit); device UAT separate |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <touched file>` **plus** `npx tsc --noEmit` (memory: tsc is NOT in a commit hook — a TS2339 shipped past 3263 passing tests once).
- **After every plan wave:** Run `npm test` + `npx tsc --noEmit` + `npm run check:colors`.
- **Before `/gsd-verify-work`:** Full suite green **and** dossier §N device UAT executed on physical Android hardware.
- **Max feedback latency:** ~60 seconds (unit tier).

---

## Per-Task Verification Map

*Seeded from research §Requirements → Test Map; task IDs bound at planning time.*

| Behavior | Requirement | Threat Ref | Test Type | Automated Command | File Exists | Status |
|----------|-------------|------------|-----------|-------------------|-------------|--------|
| D-04 orrery overlay AA over brightest orrery pixel, all 4 theme/mode combos | §B/D-04 | T-tamper (surface) | unit | `npx vitest run src/theme/tokens/surface.test.ts` | ✅ extend existing | ⬜ pending |
| D-04 no change to `CARD_GLASS_OPACITY` / `cardTintOpacity` (existing card AA still passes) | D-04 | — | unit | `npx vitest run src/theme/tokens/surface.test.ts` | ✅ | ⬜ pending |
| §F Most Recent excluded from Previous; 5-item cap; 0/1/many states | §F | — | unit | `npx vitest run src/profile/knowledge-presentation.test.ts` (+ read test) | ⚠️ add cases | ⬜ pending |
| D-07 caption is identifier-only; no baked "Nothing added yet" | §D/D-07 | — | unit | `npx vitest run` on module-host-model / knowledge-presentation | ⚠️ add cases | ⬜ pending |
| §I orphan-stretch (trailing 1×1 → full row; no reorder) | §I/D-05 | — | unit | `npx vitest run` on `pack-overview` test | ⚠️ add cases | ⬜ pending |
| §J year classification unchanged after orientation flip | §J | — | unit | `npx vitest run src/components/history/heatmap-cell.test.ts` | ✅ (classification untouched) | ⬜ pending |
| §K formatter output `Sep 19, 2026, 7:14 PM`; relative-time untouched | §K/D-08 | — | unit | `npx vitest run src/utils/dates.test.ts` (add) | ⚠️ add | ⬜ pending |
| D-06 Off Limits edit preserves other fuel kinds (kind-scope guard) | §G/D-06 | T-tamper (fuel) | unit | fuel-dao / editor preservation test | ⚠️ add guard test | ⬜ pending |
| §C/§G/§H/§J/§I/§L visual + a11y + lifecycle | multiple | — | manual | Pixel device UAT (dossier §N matrix) | device | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/utils/dates.test.ts` — new minute-precision formatter (12h output `Sep 19, 2026, 7:14 PM` + relative-time untouched)
- [ ] Extend `src/theme/tokens/surface.test.ts` — orrery-backdrop AA block (worst-case brightest star/sun pixel; all 4 theme/mode combos, mismatched combos binding)
- [ ] Add orphan-stretch cases to the `pack-overview` test (trailing 1×1 → full row; no reorder)
- [ ] Add a fuel kind-preservation test for the Off Limits reuse path (ports the `EditContactScreen` DATA-LOSS kind-scope guard)
- [ ] Add §F Most Recent/Previous dedup + 5-item cap cases to `knowledge-presentation` (and the new capped prior-rows read)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Orrery overlay legibility over live Skia backdrop | §B/D-04 | Real render surface; AA proof is worst-case, on-device tuning of the scalar is visual | Dossier §N: open Orrery in each theme/mode, confirm overlays readable and orrery still visible behind |
| Vertical Year heatmap layout + tap/selection | §J | Skia/scroll layout not observable in unit tests | Dossier §N: scroll Year view, tap day cells, confirm classification + a11y labels |
| Unbind moved to overflow; lifecycle unchanged | §H | Navigation + lifecycle event flow | Dossier §N: open overflow menu, exercise Unbind, confirm ADR-062/118 semantics |
| Off Limits / heading-level edits reach functional editors | §G/D-06 | Cross-screen navigation | Dossier §N: exercise each heading-level edit affordance, confirm correct editor + no data loss |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s (unit tier)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
