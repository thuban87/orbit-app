---
phase: 38.1-profile-presentation-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileModuleHost.tsx
  - src/profile/module-host-model.ts
  - src/profile/knowledge-presentation.ts
  - src/components/profile/ThingsToRemember.tsx
  - src/screens/ContactProfileScreen.tsx
  - src/screens/contact-profile-logic.ts
autonomous: true
requirements: [R-03, R-04, R-09]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Profile section headings are identifiers only — no appended current value, count, inferred state, or empty-state copy (§D / D-07)."
    - "The false `things-to-remember` empty-state metadata bug is removed by deleting the heading-metadata class, NOT by making the empty-state inference smarter (D-07)."
    - "The heading-metadata class is removed from all three sources: topLevelSummary/knowledgeChildSummary (ProfileModuleHost), the PROFILE_MODULE_EMPTY_SUMMARIES constant class (module-host-model), and summary() (knowledge-presentation) (§D / D-07)."
    - "Every semantic section/subsection has at most one visible heading; a nested renderer does not repeat a heading its parent container already supplied; genuinely distinct subsection headings are preserved (§D — do not flatten)."
    - "Real empty-state text lives in the section BODY, not the heading; the transient `Saving…` pending state is preserved (it is not content metadata)."
    - "Unbind is absent from the Profile body and present in the Profile overflow menu, offered only when the contact is bound, reusing the existing unbind confirmation + transitionLifecycle(\"unbind\") verbatim (§H; ADR-062/118 lifecycle unchanged)."
    - "The Unbound-state Bind experience remains in the Profile body (§H)."
    - "Profile modules are classified so outer headings carry no duplicate content metadata; values/state belong inside content/subsection presentation (§E)."
  artifacts:
    - src/components/profile/ProfileModuleHost.tsx
    - src/profile/module-host-model.ts
    - src/profile/knowledge-presentation.ts
    - src/components/profile/ThingsToRemember.tsx
    - src/screens/ContactProfileScreen.tsx
    - src/screens/contact-profile-logic.ts
  key_links:
    - "ProfileModuleHost parent ProfileSection owns the section heading; ThingsToRemember nested renderer suppresses the duplicate role=heading when hosted as its own section."
    - "contact-profile-logic profileOverflowEntries(bound) -> ContactProfileScreen overflow switch -> unbindConfirmation + transitionLifecycle('unbind')."
---

<objective>
Establish strict Profile heading ownership and fix the false empty-state metadata (§D/§E, D-07), and relocate Unbind from the Profile body into the overflow menu (§H) — all against the existing data model, no new fields (D-02). Planned against the authoritative dossier §D/§E/§H (D-01), grouped as the "Profile hierarchy + lifecycle cleanup" ownership group per §Q (D-03).

This is a Wave-1 plan and a hard prerequisite for Plan 06 (knowledge presentation + editing): captions become identifiers-only and single heading ownership is established here so Plan 06 can add Most Recent/Previous body content and heading-level edit icons on a clean hierarchy. The two plans share `knowledge-presentation.ts`, `ThingsToRemember.tsx`, and `ContactProfileScreen.tsx`, so they run sequentially (05 → 06).

Purpose: today `topLevelSummary` returns a baked empty-summary for `things-to-remember` unconditionally (the confirmed D-07 bug at ProfileModuleHost.tsx:113-116), unlike the correctly-conditional `contact-methods` sibling; and nested renderers can repeat a heading the parent already owns.
Output: identifier-only section captions; single heading ownership; empty-state moved to body; Unbind in overflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/dossier/milestone-2/phase-38.1-profile-presentation-polish-dossier.md
@.planning/phases/38.1-profile-presentation-polish/38.1-RESEARCH.md
@.planning/phases/38.1-profile-presentation-polish/38.1-PATTERNS.md
@.planning/phases/38.1-profile-presentation-polish/38.1-UI-SPEC.md
@docs/decisions/ADR-062-bound-unbound-lifecycle-and-one-way-cadence-assignment.md
@docs/decisions/ADR-118-bind-unbind-immutable-lifecycle-events-without-a-migration.md
@src/components/profile/ProfileModuleHost.tsx
@src/profile/module-host-model.ts
@src/profile/knowledge-presentation.ts
@src/components/profile/ThingsToRemember.tsx
@src/screens/ContactProfileScreen.tsx
@src/screens/contact-profile-logic.ts
</context>

<artifacts_produced>
This plan REMOVES a class of behavior (heading-metadata) and relocates an action; net-new symbols are minimal:
- A new `"unbind"` member of the `ProfileOverflowEntry` union + its inclusion path in `profileOverflowEntries()` (contact-profile-logic.ts), gated on a new `bound` input.
- A new overflow `switch` case in ContactProfileScreen dispatching Unbind (reusing existing `unbindConfirmation` + `transitionLifecycle`).
- New/updated unit-test cases in knowledge-presentation.test.ts and contact-profile-logic.test.ts, and a new module-host-model test asserting identifier-only summaries.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the heading-metadata class — section captions become identifiers only (D-07)</name>
  <files>src/components/profile/ProfileModuleHost.tsx, src/profile/module-host-model.ts, src/profile/knowledge-presentation.ts</files>
  <read_first>
    - src/components/profile/ProfileModuleHost.tsx:62-104 (`knowledgeChildSummary` — returns content-state strings), :106-128 (`topLevelSummary` — the buggy `things-to-remember` case at :113-116 vs the correctly-conditional `contact-methods` sibling at :117-120), :166-169 (the heading + caption render; keep the `Saving…` pending branch).
    - src/profile/module-host-model.ts:11-15 (`PROFILE_MODULE_EMPTY_SUMMARIES` — the baked metadata class to remove).
    - src/profile/knowledge-presentation.ts:116-122 (`summary()` — the second independent content-state summary source).
    - RESEARCH.md §"D-07 — heading/caption ownership" and PATTERNS.md §"D-07" — the fix DIRECTION is remove-the-class, not smarter inference.
  </read_first>
  <behavior>
    - topLevelSummary / knowledgeChildSummary return an identifier (or empty) for every module — no counts, current values, or empty-state phrasing. (Keep the error-message branch and the pending `Saving…` branch, which are transient state, not content metadata.)
    - The PROFILE_MODULE_EMPTY_SUMMARIES metadata class no longer supplies heading/caption copy.
    - knowledge-presentation summary() no longer emits content-state as a heading-adjacent summary.
    - A section with zero content renders its neutral empty-state text in the section BODY, not the caption.
    - Unit tests assert the summary functions are identifier-only for both the empty and populated cases of `things-to-remember` and `contact-methods` (the previously-divergent pair).
  </behavior>
  <action>
    Remove the content-state summary class per D-07 (not a smarter inference). In ProfileModuleHost, reduce `topLevelSummary` and `knowledgeChildSummary` to identifier-only outputs (drop the count/value/empty-state branches; keep the error and `Saving…` transient branches). Delete or empty the `PROFILE_MODULE_EMPTY_SUMMARIES` metadata class in module-host-model.ts and its consumers. In knowledge-presentation.ts, strip the content-state emission from `summary()` so it no longer feeds heading-adjacent metadata. Move any genuinely needed neutral empty-state text into the section body rendering (body role), not the header caption. Add/adjust unit cases in knowledge-presentation.test.ts and add a module-host-model test asserting identifier-only summaries for the empty and populated states of the previously-divergent modules. This is §E's "outer headings contain no duplicate content metadata; values belong inside content" in the same edit.
  </action>
  <reversibility rating="reversible">Presentation-logic change; no persisted data, no schema (D-02).</reversibility>
  <verify>
    <automated>npx vitest run src/profile/knowledge-presentation.test.ts</automated>
    <automated>npx vitest run src/profile/module-host-model.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests assert `topLevelSummary`/`knowledgeChildSummary`/`summary()` return identifier-only output (no counts, values, or empty-state copy) for both empty and populated `things-to-remember` and `contact-methods` states.
    - A test confirms the previously-unconditional `things-to-remember` caption no longer differs from the general identifier behavior (the D-07 divergence is gone).
    - `npx tsc --noEmit` exits 0 (no orphaned references to the removed metadata class — memory: orphaned test consumers break the tsc gate; update all consumers).
  </acceptance_criteria>
  <done>Section headings are identifiers only from every source; the false empty-state metadata class is removed (not made smarter); empty-state text lives in the body.</done>
</task>

<task type="auto">
  <name>Task 2: Single heading ownership — suppress duplicate nested headings (§D)</name>
  <files>src/components/profile/ThingsToRemember.tsx</files>
  <read_first>
    - src/components/profile/ThingsToRemember.tsx:184-188 — the nested per-child `role="heading"` + `role="caption"` emit that can duplicate a heading the parent ProfileSection already owns.
    - src/components/profile/ProfileModuleHost.tsx:130-189 (`ProfileSection`) — the parent that owns the section heading (`AppText role="heading"` at :166-168).
    - RESEARCH.md §"D-07 … The duplicate-heading pair" — when a child is hosted as its own ProfileSection, the parent heading and nested child heading render the same string.
  </read_first>
  <action>
    Establish single ownership: the parent `ProfileSection` owns the visible section heading, so the nested `ThingsToRemember` renderer must not re-emit a `role="heading"` for a child that its container already supplies as a section heading. Suppress the duplicate nested heading in exactly that case; preserve genuinely distinct subsection headings that the container does NOT supply (§D "do not flatten Profile hierarchy"). Do not remove the child's body/summary content — only the redundant heading. Keep accessibility semantics intact (the remaining single heading keeps `accessibilityRole="header"`).
  </action>
  <reversibility rating="reversible">Render-tree change; no data, no schema.</reversibility>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run src/profile/knowledge-presentation.test.ts</automated>
    <human-check>On the Pixel, open a Contact Profile where a temporal module (e.g. Last Talked About) is hosted as its own section: exactly one heading renders for that section (no duplicate title); a genuinely distinct subsection still shows its own heading. (Batched into the end-of-phase device UAT — Plan 09.)</human-check>
  </verify>
  <acceptance_criteria>
    - Reading ThingsToRemember confirms the nested `role="heading"` is suppressed when the child is hosted as its own section (parent owns the heading), and retained otherwise.
    - `npx tsc --noEmit` exits 0.
    - Device UAT (Plan 09) shows no duplicate section headings.
  </acceptance_criteria>
  <done>At most one visible heading per semantic section/subsection; distinct subsections keep their headings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move Unbind to the Profile overflow menu (§H)</name>
  <files>src/screens/contact-profile-logic.ts, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/screens/contact-profile-logic.ts:43-52 (`ProfileOverflowEntry` union), :63-78 (`profileOverflowEntries()`), :151-160 (`unbindConfirmation(name)` — the exact title/message to preserve verbatim).
    - src/screens/ContactProfileScreen.tsx:379-396 (the body Unbind button branch to remove), :346-377 (the Unbound-state Bind body block to KEEP), :445-525 (overflow list render + switch dispatch), :408-424 (existing knowledge nav — do not touch here; Plan 06 owns it).
    - src/screens/contact-profile-logic.test.ts — existing tests to extend.
    - ADR-062 / ADR-118 — lifecycle semantics are unchanged; this is placement-only.
  </read_first>
  <behavior>
    - profileOverflowEntries() includes an `"unbind"` entry ONLY when the contact is bound (add a `bound` input); it is absent for an unbound contact.
    - The overflow Unbind action reuses `unbindConfirmation(name)` and `transitionLifecycle("unbind")` unchanged.
    - The prominent body Unbind button is gone; the Unbound-state Bind body block remains.
    - Unit tests assert the overflow entry set includes `unbind` when bound and excludes it when unbound.
  </behavior>
  <action>
    Add `"unbind"` to the `ProfileOverflowEntry` union and include it in `profileOverflowEntries()` only when a new `bound` argument is true; present it as a lifecycle/destructive-style action (not equated with deletion). In ContactProfileScreen, remove the body Unbind button branch (:379-396) and add an overflow switch case that reuses `unbindConfirmation` + `transitionLifecycle("unbind")` verbatim — placement-only, lifecycle DAO untouched (ADR-062/118). Keep the Unbound-state Bind body block (:346-377) exactly as is. Extend contact-profile-logic.test.ts to cover the bound/unbound overflow entry set.
  </action>
  <reversibility rating="reversible">Placement-only UI move; lifecycle DAO and confirmation copy unchanged; no schema (D-02).</reversibility>
  <verify>
    <automated>npx vitest run src/screens/contact-profile-logic.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>On the Pixel, a bound contact shows Unbind only in the overflow menu (not the body) and its confirmation dialog is unchanged; an unbound contact shows the Bind body experience and no Unbind overflow entry. (Batched into Plan 09 device UAT.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/screens/contact-profile-logic.test.ts` exits 0, asserting `unbind` present-when-bound / absent-when-unbound.
    - Reading ContactProfileScreen confirms the body Unbind button branch is removed and the overflow case reuses the existing confirmation + `transitionLifecycle("unbind")`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Unbind lives in overflow (bound only), body button removed, Bind body block kept, lifecycle semantics identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile presentation logic → rendered UI | Wrong caption/heading logic misinforms the user about saved content (a correctness/UX-integrity issue, not a data leak). |
| overflow action → lifecycle DAO | Unbind is a lifecycle transition; relocating its trigger must not alter or bypass the existing confirmation/lifecycle guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-05-01 | Tampering | ContactProfileScreen unbind path | medium | mitigate | Reuse `unbindConfirmation` + `transitionLifecycle("unbind")` verbatim (placement-only); unit test on the overflow entry set; ADR-062/118 lifecycle DAO untouched. |
| T-38.1-05-02 | Repudiation | false empty-state metadata | low | mitigate | Removing the heading-metadata class (D-07) removes the misleading "nothing saved" claim when content exists; unit tests assert identifier-only summaries. |
| T-38.1-05-03 | Tampering | SQLite schema / migrations | low | accept | No schema, no migration, no format bump (D-02); presentation + placement only. |
| T-38.1-05-04 | Information disclosure | read path / network | low | accept | No network on read path; local-first (CLAUDE.md). No loading/error surface added (UI-SPEC: synchronous SQLite reads). |

No high-severity threats.
</threat_model>

<verification>
- `npx vitest run src/profile/knowledge-presentation.test.ts src/profile/module-host-model.test.ts src/screens/contact-profile-logic.test.ts` all green.
- `npx tsc --noEmit` clean (verify no orphaned consumers of the removed metadata class).
- `npm run check:colors` clean.
- Device duplicate-heading + Unbind-placement checks deferred to Plan 09 (end-of-phase UAT).
</verification>

<success_criteria>
- Section headings are identifiers only; the false empty-state metadata class is removed; each section/subsection has one visible heading; Unbind is in overflow (bound only) with unchanged lifecycle; the Bind body experience remains for unbound contacts. Plan 06 can now build knowledge presentation + editing on this clean hierarchy.
</success_criteria>

<output>
Create `.planning/phases/38.1-profile-presentation-polish/38.1-05-SUMMARY.md` when done. Record the §E module classification used (which modules are current-value vs temporal-history vs collection) so Plan 06 applies Most Recent/Previous only where recency is meaningful.
</output>
