---
phase: 16-custom-field-value-normalization
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/db/field-type-change.ts
  - src/db/field-type-change.test.ts
  - src/db/field-sort.ts
  - src/db/field-sort.test.ts
autonomous: true
requirements: [CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Retype/options preflight continues to use all seven existing parsers over raw normalized TEXT and leaves stored value bytes and value uids untouched."
    - "sortExpr emits a static expression over the normalized value column (number → CAST REAL, toggle → CAST INTEGER, else raw TEXT) and never accepts col_name as SQL; its documented contract now requires the caller to constrain field_def_id in the join."
  artifacts:
    - src/db/field-type-change.ts
    - src/db/field-sort.ts
  key_links:
    - "field_history type-change snapshots retain the definition col_name compatibility key."
    - "sortExpr remains a latent forward-looking helper (zero runtime consumers in this repo); it preserves its raw-TEXT ordering contract for its first future consumer."
---

<objective>
Preserve type-change repair semantics on the normalized TEXT store without parser logic or identifiers
leaking into SQL, and re-key the (currently consumer-less) sort/filter helper to a static normalized-value
expression that preserves its raw-TEXT ordering contract for a future consumer.

Purpose: the type-change contract protects unusual legacy values and blast-radius-zero storage; sortExpr is
the app-wide permanent sort/filter interpolation site and must never accept a user-derived identifier after
dynamic columns are retired.
Output: normalized preflight/history queries and a static, hostile-key-proof sort expression with an
updated documented contract.

Addresses review concerns: MEDIUM "sortExpr has zero runtime consumers" — Task 2 is re-scoped as
forward-looking (no parity claims), and the field_def_id join constraint is added to the helper's contract;
Claude suggestion #12 — depends_on corrected to [16-01, 16-02] (field-type-change/field-sort have no
dependency on field-ddl / Plan 03).
</objective>

## Artifacts this phase produces (owned by this plan)

- **`field-type-change.ts`:** `preflightTypeChange` / `preflightOptionsChange` / `applyTypeChange` reading normalized rows by bound `field_def_id`; value bytes and value uids never rewritten; metadata-only def update + field_history snapshot under `col_name`.
- **`field-sort.ts`:** `sortExpr(field)` emitting a static expression over the normalized `value` column; its JSDoc contract updated to (a) state it is a latent forward-looking helper with no runtime consumer in this repo and (b) require the caller to constrain `field_def_id` in the join.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move type/options preflight and audit snapshots to normalized values</name>
  <reversibility rating="costly">D-10 and D-11 preserve raw-TEXT behavior and the audit compatibility key relied on by current repair/recovery paths.</reversibility>
  <read_first>
    - src/db/field-type-change.ts and src/db/field-type-change.test.ts (preflight partitions; applyTypeChange snapshot + metadata-only update)
    - src/db/field-parsers.ts (seven parser contract; option-membership helper)
    - src/db/field-values-dao.ts and src/db/migrations/006-normalize-custom-field-values.ts (normalized table + read)
    - src/components/CustomFieldValue.tsx (Tap to fix behavior)
  </read_first>
  <files>src/db/field-type-change.ts, src/db/field-type-change.test.ts</files>
  <behavior>
    - Preflight yields the same contact partitions for every type/options path from non-NULL normalized raw values, read by bound field_def_id.
    - Type change snapshots raw values under field_col_name, updates definition metadata only, and leaves value uid/value bytes unchanged.
  </behavior>
  <action>Start RED tests for all seven types, unusual raw number/toggle text, and out-of-list dropdown values. Replace dynamic-column reads with literal normalized pair queries filtered by bound field_def_id. Reuse the existing parser map and option-membership helper in TypeScript; do not parse or canonicalize in SQL. In the existing transaction, snapshot each non-NULL raw value into field_history with unchanged col_name and operation text, then update definition metadata only. This implements D-10 and D-11 and preserves the UI-SPEC invalid-value repair without UI redesign.</action>
  <verify><automated>npm test -- src/db/field-type-change.test.ts src/db/field-parsers.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - No type/options path deletes, rewrites, or rekeys a normalized value row (value uid and bytes unchanged).
    - History contains contact_id, raw value, field_col_name, operation, and timestamp.
    - Invalid raw values remain available to the existing repair flow.
  </acceptance_criteria>
  <done>Retype and options changes remain parser-driven, raw-TEXT preserving, and auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-key sortExpr to a static normalized-value expression (forward-looking; no parity claim)</name>
  <read_first>
    - src/db/field-sort.ts and src/db/field-sort.test.ts (current guarded col_name interpolation; the header note that sortExpr has NO runtime consumer yet, lines ~44-53)
    - src/db/field-values-dao.ts (normalized value column + literal alias)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-09 and D-11)
  </read_first>
  <files>src/db/field-sort.ts, src/db/field-sort.test.ts</files>
  <behavior>
    - Number stays CAST(value AS REAL), toggle stays CAST(value AS INTEGER), and other field types retain raw TEXT ordering over the normalized value column.
    - Hostile col_name data cannot alter the emitted SQL expression (no col_name reaches SQL at all).
    - The emitted expression references a fixed normalized value column, not a per-field dynamic column.
  </behavior>
  <action>Write RED cases across all field types and a hostile col_name. Replace the guarded dynamic-column quoting with the fixed normalized `value` expression over a literal alias. Re-scope this task HONESTLY per the review: sortExpr has ZERO runtime consumers in this repo today (dashboard/orrery sort maps do not branch on custom fields), so make NO "same user-visible ordering" or "sort/filter parity" claim — this task PRESERVES the helper's raw-TEXT ordering contract for its first future consumer, it does not migrate a live sort. Update the JSDoc contract to: (a) state plainly that the helper is latent/forward-looking with no runtime consumer in this repo; (b) require any future consumer to constrain field_def_id in the join (otherwise ORDER BY CAST(value AS REAL) would sort by an arbitrary field's value or multiply rows) — this is the part a future consumer will get wrong; (c) describe col_name solely as compatibility metadata, never SQL. Do not duplicate parser logic in SQL. This implements D-09 and D-11.</action>
  <verify><automated>npm test -- src/db/field-sort.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Type-to-expression behavior is preserved (number/toggle casts, raw text otherwise) with no user-derived identifier in the output.
    - A malformed/hostile col_name has no effect on expression generation (col_name is not used to build the expression).
    - The JSDoc contract states the helper is latent/forward-looking AND requires the caller to constrain field_def_id in the join; no parity claim is made.
  </acceptance_criteria>
  <done>sortExpr emits a static, injection-proof normalized-value expression and documents the field_def_id join requirement for its first consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Definition type/metadata → stored value | Metadata changes must not mutate historic raw user content. |
| Definition compatibility key → query | col_name is metadata and may not become SQL syntax. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-08 | Tampering | type change | medium | mitigate | Reuse parsers, snapshot raw rows, metadata-only mutation. |
| T-16-09 | Tampering | sort expression | high | mitigate | Fixed expression over the normalized value column; no col_name in SQL; hostile-key regressions. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2); node:sqlite is available.
- Run named unit/DAO tests and `npx tsc --noEmit` (project green from Plan 02).
- Fixtures seed defs + normalized value rows via migration 006 or direct INSERTs into custom_field_defs/custom_field_values — NOT via createField or another sibling wave-3 DAO — so the four wave-3 plans (03/04/05/06, all depending only on 16-01/16-02) are order-independent within the wave.
</verification>

<success_criteria>
CFN-02 retains parser and repair behavior for raw normalized values; CFN-03's no-identifier-in-SQL guarantee
holds for the sort/filter helper, whose contract now honestly reflects its latent status and the field_def_id
join requirement for its first consumer.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-04-SUMMARY.md` when done.
</output>
