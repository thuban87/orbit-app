---
phase: 16-custom-field-value-normalization
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - src/db/field-type-change.ts
  - src/db/field-type-change.test.ts
  - src/db/field-sort.ts
  - src/db/field-sort.test.ts
autonomous: true
requirements: [CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Retype/options preflight continues to use all seven existing parsers over raw normalized TEXT and leaves stored bytes untouched."
    - "Sort/filter expressions use a static normalized value reference, preserving numeric/toggle casts and raw ordering without accepting col_name as SQL."
  artifacts:
    - src/db/field-type-change.ts
    - src/db/field-sort.ts
  key_links:
    - "field_history type-change snapshots retain the definition col_name compatibility key."
    - "field-sort emits a fixed alias/value expression selected by the DAO query boundary."
---

<objective>
Preserve type-change repair semantics and user-visible custom sort/filter behavior on the normalized TEXT
store, without parser logic or identifiers leaking into SQL.

Purpose: these two established read-time contracts protect unusual legacy values and avoid SQL injection
after dynamic columns are retired.
Output: normalized preflight/history queries and static sort expression regressions.

## Artifacts
- Raw-TEXT-preserving normalized type/options preflight and history operations.
- A static normalized custom sort/filter expression and hostile-key coverage.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move type/options preflight and audit snapshots to normalized values</name>
  <reversibility rating="costly">D-10 and D-11 preserve raw-TEXT behavior and the audit compatibility key relied on by current repair/recovery paths.</reversibility>
  <read_first>
    - src/db/field-type-change.ts and src/db/field-type-change.test.ts
    - src/db/field-parsers.ts (seven parser contract)
    - src/db/field-values-dao.ts and src/db/migrations/006-normalize-custom-field-values.ts
    - src/components/CustomFieldValue.tsx (Tap to fix behavior)
  </read_first>
  <files>src/db/field-type-change.ts, src/db/field-type-change.test.ts</files>
  <behavior>
    - Preflight yields the same contact partitions for every type/options path from non-NULL normalized raw values.
    - Type change snapshots raw values under field_col_name, updates definition metadata only, and leaves value uid/value bytes unchanged.
  </behavior>
  <action>Start RED tests for all seven types, unusual raw number/toggle text, and out-of-list dropdown values. Replace dynamic-column reads with literal normalized pair queries by bound field_def_id. Reuse the existing parser map and option-membership helper in TypeScript; do not parse or canonicalize in SQL. In the existing transaction, snapshot each non-NULL raw value into field_history with unchanged col_name and operation text, then update definition metadata only. This implements D-10 and D-11 and preserves the UI-SPEC invalid-value repair without UI redesign.</action>
  <verify><automated>Node 22+: npm test -- src/db/field-type-change.test.ts src/db/field-parsers.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - No type/options path deletes, rewrites, or rekeys a normalized value row.
    - History contains contact_id, raw value, field_col_name, operation, and timestamp.
    - Invalid raw values remain available to the existing repair flow.
  </acceptance_criteria>
  <done>Retype and options changes remain parser-driven, raw-TEXT preserving, and auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make custom sort/filter expressions static normalized value expressions</name>
  <read_first>
    - src/db/field-sort.ts and src/db/field-sort.test.ts
    - src/db/field-values-dao.ts (literal normalized query alias)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-09 and D-11)
  </read_first>
  <files>src/db/field-sort.ts, src/db/field-sort.test.ts</files>
  <behavior>
    - Number stays CAST(value AS REAL), toggle stays CAST(value AS INTEGER), and other field types retain raw TEXT ordering.
    - Hostile col_name data cannot alter the emitted SQL expression.
  </behavior>
  <action>Write RED cases across all field types and a hostile col_name. Replace guarded dynamic column quoting with the fixed normalized `value` expression using the literal DAO alias selected at the query boundary. Preserve number/toggle casts and all raw text behavior; update comments/tests so col_name is described solely as compatibility metadata. Do not duplicate parser logic in SQL. This implements D-09 and D-11.</action>
  <verify><automated>Node 22+: npm test -- src/db/field-sort.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Type-to-expression behavior is preserved except no user-derived identifier appears.
    - Malformed col_name has no effect on expression generation.
  </acceptance_criteria>
  <done>All custom sort/filter callers can use a static normalized-row expression safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Definition type/metadata → stored value | Metadata changes must not mutate historic raw user content. |
| Definition compatibility key → query | col_name is metadata and may not become SQL syntax. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-08 | Tampering | type change | medium | mitigate | Reuse parsers, snapshot raw rows, metadata-only mutation. |
| T-16-09 | Tampering | sort expression | high | mitigate | Fixed expression/alias plus hostile-key regressions. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Run named Node 22+ unit/DAO tests and `npx tsc --noEmit`.
</verification>

<success_criteria>
CFN-02/03 retain parser, repair, sort, and filter behavior for raw normalized values without SQL identifiers
derived from custom-field metadata.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-04-SUMMARY.md` when done.
</output>
