---
phase: 16-custom-field-value-normalization
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/screens/create-contact-logic.ts
  - src/screens/create-contact-logic.test.ts
  - src/screens/edit-contact-logic.ts
  - src/screens/edit-contact-logic.test.ts
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
autonomous: true
requirements: [CFN-01, CFN-02, CFN-04]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Create and edit flows write normalized contact-definition rows atomically and no longer carry a contact-wide custom-values rowUid."
    - "A new contact receives a durable uid-bearing blank row for EVERY definition — including quarantined ones — before submitted custom values are applied, so D-03 holds for contacts created while a field is quarantined."
    - "Edit and clear UPSERT existing pair rows on the (contact_id, field_def_id) constraint (never a bare UPDATE), so a value is never silently discarded."
    - "The contact uid and optional interaction uid remain separately generated and passed through their real paths."
    - "After this plan the project typechecks (every TYPED direct caller of the changed value-write API migrated); the remaining string-literal legacy-table SQL writers migrate in wave 3, and the contact_custom_values grep completeness gate is asserted at wave-3 exit (Plan 07)."
  artifacts:
    - src/db/contacts-dao.ts
    - src/screens/CreateContactScreen.tsx
    - src/screens/EditContactScreen.tsx
  key_links:
    - "The contact DAO composes the normalized field-values *Core inside its existing outer transaction without nesting the write mutex."
    - "Create-contact queries listDefs(includeQuarantined: true) so the complete pair matrix is seeded."
    - "Both React Native screens stop minting/passing rowUid while retaining contactUid and interactionUid generation where required."
---

<objective>
Perform the direct, compile-checked caller migration for create and edit: contacts and forms use per-value
normalized identity, the complete pair matrix (including quarantined definitions) is seeded on create, and
the obsolete contact-wide rowUid disappears end-to-end. This is the plan that restores the project to a
typechecking state (the first project-wide tsc).

Purpose: the migration core cannot safely ship with stale form inputs; these are the production write callers
whose TypeScript contract must change together, and this plan owns the first tsc gate.
Output: normalized create/edit DAO composition, pure-builder tests, screen caller cleanup, and the first
phase-wide typecheck. (The legacy-table grep completeness gate is a wave-3-exit criterion — the string-literal
SQL writers in field-ddl/field-defs-dao/field-type-change/purge-dao migrate in wave 3, not here.)

Addresses review concerns: HIGH pair-row invariant across create (all defs incl. quarantined + cross-plan
quarantine→create→restore regression), Claude-HIGH bare-UPDATE (UPSERT), HIGH wave-ordering vs tsc (this
plan is the sole first tsc gate; 03/04/05/06 follow it), MEDIUM grep-vs-tsc completeness gate.
</objective>

## Artifacts this phase produces (owned by this plan)

- **DAO (`contacts-dao.ts`):** `CreateContactFullInput` / `UpdateContactFullInput` with `rowUid?` REMOVED and the normalized value representation the Plan 01 DAO requires; `createContactFull` / `updateContactFull` compose the normalized `*Core` writer across the full pair matrix inside their one outer transaction.
- **Pure builders (`create-contact-logic.ts`, `edit-contact-logic.ts`):** no `rowUid` in their dependency interfaces or outputs; `contactUid` (create) and `interactionUid` (create + conditional edit) retained.
- **Screens:** `CreateContactScreen.tsx` (retains `newUid` for contactUid + interactionUid), `EditContactScreen.tsx` (retains `newUid` for interactionUid only).
- **Gate:** the first project-wide `npx tsc --noEmit` and the `contact_custom_values` grep completeness assertion.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose the complete normalized pair matrix into atomic contact create and edit writes</name>
  <reversibility rating="costly">D-01 through D-03 make pair rows and immutable value uid the write invariant used by every contact form.</reversibility>
  <read_first>
    - src/db/contacts-dao.ts and src/db/contacts-dao.test.ts (outer transaction, contact/interaction composition, rollback contract, current input.rowUid ?? newUid() usage)
    - src/db/field-values-dao.ts (Plan 01 normalized UPSERT-on-pair *Core writer and defs-filtered read)
    - src/db/field-defs-dao.ts (listDefs incl. quarantined; visibility selectors)
    - src/db/transaction.ts (non-reentrant mutex rule; core/wrapper split)
  </read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <behavior>
    - Create inserts one value row per definition returned by listDefs(includeQuarantined: true) — EVERY definition, including quarantined — each with a newly generated immutable uid and a NULL blank; supplied values then UPSERT their matching pairs in that same transaction.
    - Edit and clear UPSERT existing pair rows on (contact_id, field_def_id) without deleting or re-keying them; clear sets value NULL.
    - A failed value write rolls back contact, interaction, and all pair-row work together.
    - Cross-plan regression (co-owned with Plan 01/03): quarantine a field → create a contact → restore the field → the pair row for that contact/field EXISTS and an edit round-trips through it (no hole).
  </behavior>
  <action>Start with RED contacts-dao regressions against the normalized fixture. Remove rowUid from CreateContactFullInput and UpdateContactFullInput and replace it with the normalized per-pair value representation the Plan 01 DAO consumes; do not retain a deprecated or ignored rowUid field. Inside the existing one outer transaction, query listDefs(includeQuarantined: true), create the COMPLETE pair matrix with one fresh value uid per contact/definition (blank NULL by default), and apply submitted values by UPSERTing their matching pairs through the non-mutexed *Core writer (never nesting inWriteTransaction). Preserve contact uid, the optional first-interaction uid, recency ordering, and rollback behavior. Add the quarantine→create→restore regression that asserts the pair row exists after restore and an edit round-trips. This implements D-01, D-02, and D-03 and resolves the HIGH pair-row invariant.</action>
  <verify><automated>npm test -- src/db/contacts-dao.test.ts src/db/field-values-dao.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove new contact creation produces one distinct uid-bearing row for EVERY definition including quarantined ones, before form values are applied.
    - Tests prove clear-to-NULL preserves the same pair row/uid via UPSERT and a failure rolls back all contact, interaction, and value writes.
    - The quarantine→create→restore regression proves the pair row exists after restore and an edit round-trips (no missing-row hole).
    - Source and input types contain no contact-wide custom-values rowUid.
  </acceptance_criteria>
  <done>The DAO writes the durable, complete normalized pair matrix transactionally for both contact forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove obsolete rowUid from both form builders and RN callers; run the first tsc + grep gate</name>
  <read_first>
    - src/screens/create-contact-logic.ts and src/screens/create-contact-logic.test.ts
    - src/screens/edit-contact-logic.ts and src/screens/edit-contact-logic.test.ts
    - src/screens/CreateContactScreen.tsx and src/screens/EditContactScreen.tsx (newUid call sites)
    - src/db/contacts-dao.ts (Task 1 inputs)
  </read_first>
  <files>src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx</files>
  <behavior>
    - Pure input builders retain form values, contact ids, and first-interaction construction but expose no rowUid dependency/output.
    - Create continues to mint contactUid and interactionUid; edit continues to mint interactionUid only for its real optional first-interaction path.
    - Screens compile and call the normalized DAO inputs without changing visual behavior.
  </behavior>
  <action>Write RED pure-builder cases that assert rowUid is neither requested nor emitted while the existing value maps, trimmed fields, contact uid, and interaction uid behavior remain exact. Remove rowUid from both dependency interfaces, builder outputs, tests, and screen calls. In CreateContactScreen retain newUid for contactUid and interactionUid; in EditContactScreen retain newUid only for interactionUid. Do not generate a contact-wide replacement uid, alter widgets/navigation/layout/labels/accessibility, or add a compatibility overload. Then run the first project-wide typecheck. This implements D-01, D-02, D-08, D-12 and resolves the wave-ordering-vs-tsc finding (this plan is the sole first tsc gate; the tsc-gated plans 03/04/05/06 all depend on it). NOTE: the legacy-table grep completeness gate (no executable contact_custom_values SQL outside the migrations) is NOT yet satisfiable here — field-ddl, field-defs-dao (isFieldEmpty), field-type-change, and purge-dao still hold legacy-table string-literal SQL until their wave-3 plans migrate them; that gate is asserted at wave-3 exit in Plan 07 Task 1.</action>
  <verify><automated>npm test -- src/screens/create-contact-logic.test.ts src/screens/edit-contact-logic.test.ts src/db/contacts-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - `rg "rowUid" src/db/contacts-dao.ts src/screens/create-contact-logic.ts src/screens/edit-contact-logic.ts src/screens/CreateContactScreen.tsx src/screens/EditContactScreen.tsx` returns no executable custom-value plumbing.
    - The named unit tests preserve form value mapping and contact/interaction identity behavior.
    - The full TypeScript project compiles (`npx tsc --noEmit`) without a legacy API shim — the first phase-wide typecheck.
    - The legacy-table grep completeness gate is DEFERRED to wave-3 exit (Plan 07 Task 1): field-ddl, field-defs-dao, field-type-change, and purge-dao still hold executable contact_custom_values SQL until their wave-3 plans migrate them, so the "comment-lines-only" grep cannot pass at Plan 02.
  </acceptance_criteria>
  <done>All production create/edit callers use the normalized contract directly and the project typechecks; the string-literal SQL writers migrate in wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Form state → contact transaction | User-entered values must become only bound normalized pair writes. |
| Contact create → value identity | Each definition value needs an independent immutable uid, not a contact-wide reuse; the complete matrix (incl. quarantined defs) is seeded. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-04 | Tampering | contact write composition | high | mitigate | Complete pair matrix across all defs, one outer transaction, UPSERT-on-pair, and rollback tests. |
| T-16-05 | Tampering | form caller migration | medium | mitigate | Remove stale rowUid source-to-screen contract and typecheck all call sites. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2 in this shell); node:sqlite is available. No separate Node-provisioning step is required.
- `npx tsc --noEmit` is the first compile gate proving every TYPED direct caller migrated; no legacy compatibility shim is permitted. It passes here because the only typed caller of the changed value-write API was contacts-dao (migrated in this plan) and getValuesForContact's signature is preserved.
- The `contact_custom_values` grep completeness gate proves the string-literal SQL writers — invisible to tsc — all migrated. It is a WAVE-3-EXIT criterion (asserted in Plan 07 Task 1), NOT satisfiable at Plan 02: field-ddl, field-defs-dao, field-type-change, and purge-dao still reference the legacy table until wave 3.
</verification>

<success_criteria>
CFN-01/02 write paths are normalized and testable: both forms preserve their behavior, the complete pair
matrix (incl. quarantined defs) is seeded on create, and the obsolete contact-wide value-row uid plumbing is
fully removed. The project typechecks; the legacy table's remaining string-literal SQL writers are migrated
in wave 3 (grep completeness gate asserted in Plan 07 Task 1).
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-02-SUMMARY.md` when done.
</output>