---
phase: 16-custom-field-value-normalization
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/screens/create-contact-logic.ts
  - src/screens/create-contact-logic.test.ts
  - src/screens/edit-contact-logic.ts
  - src/screens/edit-contact-logic.test.ts
  - src/screens/CreateContactScreen.tsx
  - src/screens/EditContactScreen.tsx
autonomous: true
requirements: [CFN-01, CFN-02, CFN-04]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Create and edit flows write normalized contact-definition rows atomically and no longer carry a contact-wide custom-values rowUid."
    - "New contacts receive a durable uid-bearing blank row for every live definition before submitted custom values are applied."
    - "The contact uid and optional interaction uid remain separately generated and passed through their real paths."
  artifacts:
    - src/db/contacts-dao.ts
    - src/screens/CreateContactScreen.tsx
    - src/screens/EditContactScreen.tsx
  key_links:
    - "The contact DAO composes the normalized field-values core inside its existing outer transaction without nesting the write mutex."
    - "Both React Native screens stop minting/passing rowUid while retaining contactUid and interactionUid generation where required."
---

<objective>
Perform the direct, compile-checked caller migration for create and edit: contacts and forms use per-value
normalized identity, while the obsolete contact-wide rowUid disappears end-to-end.

Purpose: migration core cannot safely ship with stale form inputs or test-only conversion; these are the
production write callers whose TypeScript contract must change together.
Output: normalized create/edit DAO composition, pure-builder tests, screen caller cleanup, and the first
phase-wide typecheck.

## Artifacts
- Direct normalized create/edit inputs and transaction regressions.
- Both form builders and screens with obsolete custom-value rowUid removed.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose normalized value pairs into atomic contact create and edit writes</name>
  <reversibility rating="costly">D-01 through D-03 make pair rows and immutable value uid the write invariant used by every contact form.</reversibility>
  <read_first>
    - src/db/contacts-dao.ts and src/db/contacts-dao.test.ts (outer transaction, contact/interaction composition, rollback contract)
    - src/db/field-values-dao.ts (Plan 01 normalized core and its definition-local API)
    - src/db/field-defs-dao.ts (live definition query/visibility contract)
    - src/db/transaction.ts (non-reentrant mutex rule)
  </read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts</files>
  <behavior>
    - Create inserts one value row per live definition, each with a newly generated immutable uid, including NULL blanks; supplied values then update their matching rows in that same transaction.
    - Edit and clear update existing pair rows without deleting/re-keying them.
    - A failed value write rolls back contact, interaction, and all pair-row work together.
  </behavior>
  <action>Start with RED contacts-dao regressions against the normalized fixture from Plan 01. Replace rowUid in CreateContactFullInput and UpdateContactFullInput with the normalized value representation required by the Plan 01 DAO; do not retain a deprecated or ignored rowUid field. Query every live definition inside the existing one outer transaction, create the complete pair matrix with one fresh value uid per contact/definition, and apply submitted values only to matching pairs. Preserve contact uid and the optional first-interaction uid behavior, recency ordering, rollback behavior, and no-nested-inWriteTransaction rule. This implements D-01, D-02, and D-03.</action>
  <verify><automated>Node 22+: npm test -- src/db/contacts-dao.test.ts src/db/field-values-dao.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove new contact creation produces one distinct uid-bearing row for each live definition before form values are applied.
    - Tests prove clear-to-NULL preserves the same pair row/uid and a failure rolls back all contact, interaction, and value writes.
    - Source and input types contain no contact-wide custom-values rowUid.</acceptance_criteria>
  <done>The DAO writes durable normalized pairs transactionally for both contact forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove obsolete rowUid from both form builders and React Native callers</name>
  <read_first>
    - src/screens/create-contact-logic.ts and src/screens/create-contact-logic.test.ts
    - src/screens/edit-contact-logic.ts and src/screens/edit-contact-logic.test.ts
    - src/screens/CreateContactScreen.tsx and src/screens/EditContactScreen.tsx (newUid call sites)
    - src/db/contacts-dao.ts (Task 1 inputs)
  </read_first>
  <files>src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx</files>
  <behavior>
    - Pure input builders retain form values, contact ids, and first-interaction construction but expose no rowUid dependency/output.
    - Create continues to mint contactUid and interactionUid; edit continues to mint interactionUid only for its real optional first-interaction path.
    - Screens compile and call the normalized DAO inputs without changing visual behavior.
  </behavior>
  <action>Write RED pure-builder cases that assert rowUid is neither requested nor emitted while the existing value maps, trimmed fields, contact uid, and interaction uid behavior remain exact. Remove rowUid from both dependency interfaces, builder outputs, tests, and screen calls. In CreateContactScreen retain newUid for contactUid and interactionUid; in EditContactScreen retain newUid only for interactionUid as its conditional interaction path requires. Do not generate a contact-wide replacement uid, alter widgets/navigation/layout/labels/accessibility, or add a compatibility overload. This directly resolves the obsolete screen plumbing noted by the checker and implements D-01, D-02, D-08, and D-12.</action>
  <verify><automated>Node 22+: npm test -- src/screens/create-contact-logic.test.ts src/screens/edit-contact-logic.test.ts src/db/contacts-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - `rg "rowUid" src/db/contacts-dao.ts src/screens/create-contact-logic.ts src/screens/edit-contact-logic.ts src/screens/CreateContactScreen.tsx src/screens/EditContactScreen.tsx` returns no executable custom-value plumbing.
    - The named unit tests preserve form value mapping and contact/interaction identity behavior.
    - The full TypeScript project compiles without a legacy API shim.
  </acceptance_criteria>
  <done>All production create/edit callers use the normalized contract directly, and the project typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Form state → contact transaction | User-entered values must become only bound normalized pair writes. |
| Contact create → value identity | Each definition value needs an independent immutable uid, not a contact-wide reuse. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-04 | Tampering | contact write composition | high | mitigate | Complete pair matrix, one outer transaction, and rollback tests. |
| T-16-05 | Tampering | form caller migration | medium | mitigate | Remove stale rowUid source-to-screen contract and typecheck all call sites. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Run named node:sqlite tests with Node 22+.
- `npx tsc --noEmit` is the compile gate proving every direct caller migrated; no legacy compatibility shim is permitted.
</verification>

<success_criteria>
CFN-01/02 write paths are normalized and testable: both forms preserve their behavior while their obsolete
contact-wide value-row uid plumbing is fully removed.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-02-SUMMARY.md` when done.
</output>
