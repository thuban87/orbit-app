---
phase: 16-custom-field-value-normalization
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/db/contact-read.ts
  - src/db/contact-read.test.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
autonomous: true
requirements: [CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edit/profile reads retain the established col_name-keyed compatibility map, NULL/empty behavior, invalid-value repair availability, and custom-photo relative paths."
    - "The edit/profile read is filtered to defsForEditForm — a quarantined definition's now-durable pair row never appears in the returned map."
    - "AI still includes only live share_with_ai nonblank values by visible label, excluding quarantined, opted-out, NULL, and whitespace-only values; the normalized read passed sharedDefs cannot widen egress."
  artifacts:
    - src/db/contact-read.ts
    - src/db/ai-context-read.ts
  key_links:
    - "Both read modules remain callers of listDefs/getValuesForContact rather than constructing custom-value SQL."
    - "getValuesForContact's defs-filtering (Plan 01) is what keeps quarantined/non-shared values out of these maps now that pair rows exist for every definition."
    - "The AI projection retains its closed local-to-provider privacy allowlist."
---

<objective>
Lock profile/edit and AI read consumers to the normalized DAO compatibility map with production behavior
regressions, keeping value storage details entirely below their boundaries. Plan 02 already performed the
COMPILE-level conversion of these two files' retired-`upsertValue` call sites (so the wave-2 tsc gate is
green — HIGH-1); this plan completes their normalized v6 fixture construction and adds the
privacy/quarantine-exclusion assertions. (The wholesale move out of Plan 01 removed the earlier
shared-file overlap; the compile-fix lives in Plan 02 to keep the wave-2 gate honest — see Plan 02 HIGH-1.)

Purpose: normalized rows are only a safe replacement if existing visible and private projections cannot
change or widen during the conversion — and now that a durable pair row exists for EVERY definition
(including quarantined), the defs-filtering in the read is what preserves privacy.
Output: migrated projection fixture coverage and only necessary production type/assumption cleanup.

Addresses review concerns: MEDIUM defs-filtered privacy read (quarantined + non-share_with_ai must not
leak) proven at both consumers; HIGH-1 wave-2 tsc gate (Plan 02 does the compile-level call-site conversion
of these two files; this plan owns their v6 fixture construction and the new assertions).
</objective>

## Artifacts this phase produces (owned by this plan)

- **`contact-read.ts` / `contact-read.test.ts`:** edit/profile projection proven over normalized rows; the two test files' v6 fixture setup COMPLETED on the normalized DAO write contract (Plan 02 already re-pointed their retired-`upsertValue` call sites at the normalized writer at compile level; this plan finishes the fixtures — migrate through 001..006 and seed via the Plan 01 writer — and adds the assertions).
- **`ai-context-read.ts` / `ai-context-read.test.ts`:** the share_with_ai privacy projection proven closed over normalized rows.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove edit/profile projection compatibility over normalized value rows</name>
  <reversibility rating="costly">D-11 preserves the compatibility map used by existing form/profile consumers; changing it later would alter visible field behavior.</reversibility>
  <read_first>
    - src/db/contact-read.ts and src/db/contact-read.test.ts (getContactForEdit; how it passes defsForEditForm to getValuesForContact; the current fixture seeding to convert)
    - src/db/field-values-dao.ts (Plan 01 defs-filtered map contract)
    - src/components/CustomFieldValue.tsx and src/components/field-widgets/photo-field-logic.ts
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
  </read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts</files>
  <behavior>
    - A populated upgraded contact has the same edit/profile map, including NULL/empty distinction, invalid/retyped raw value, and custom-photo relative string.
    - The read is filtered to defsForEditForm: a QUARANTINED definition's durable pair row does NOT appear in the returned map.
    - No read consumer sees uid, field_def_id, table names, or migration metadata.
  </behavior>
  <action>Building on Plan 02's compile-level call-site conversion, COMPLETE the contact-read.test.ts fixture setup on the normalized v6 DAO write contract (migrate the fixture through migrations 001..006 and seed via the Plan 01 normalized writer), then add focused RED assertions for populated values across types, NULL and empty text, an invalid/retyped raw value, active versus quarantined definitions (asserting the quarantined def's value is ABSENT from the returned map), and a custom-photo relative path. Keep contact-read a consumer of listDefs/getValuesForContact and change production source only when its types or assumptions need cleanup for that map. Do not add custom SQL, RN screen changes, widgets, navigation, labels, accessibility, or layout changes. This implements D-08, D-11, D-12 and the defs-filtered privacy requirement.</action>
  <verify><automated>npm test -- src/db/contact-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove normalized rows preserve edit/profile map behavior and photo-path compatibility.
    - A test proves a quarantined definition's durable pair row is excluded from the getContactForEdit map (defs-filtering works).
    - The production read boundary does not consume normalized internal identity/schema details.
  </acceptance_criteria>
  <done>Profile and edit reads preserve their public compatibility map over normalized storage and exclude quarantined values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the AI custom-field privacy projection over normalized rows</name>
  <reversibility rating="costly">The established share_with_ai allowlist is a privacy boundary; changing it changes data egress behavior.</reversibility>
  <read_first>
    - src/db/ai-context-read.ts and src/db/ai-context-read.test.ts (readSharedFields; how it passes sharedDefs to getValuesForContact; the current fixture seeding to convert)
    - src/db/field-values-dao.ts and src/db/field-defs-dao.ts
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-11 and D-12)
  </read_first>
  <files>src/db/ai-context-read.ts, src/db/ai-context-read.test.ts</files>
  <behavior>
    - Prompt context contains only live share_with_ai definitions with nonblank values and user-visible labels.
    - Unshared, quarantined, NULL, empty/whitespace-only values do not cross the prompt boundary — proven both via the sharedDefs iteration AND the defs-filtered read (a non-shared def's durable pair row is not in the map).
  </behavior>
  <action>Building on Plan 02's compile-level call-site conversion, COMPLETE the ai-context-read.test.ts fixture setup on the normalized v6 DAO write contract (migrate through 001..006 and seed via the Plan 01 writer) — this includes converting the existing `createField` fixture calls at ai-context-read.test.ts:150,207 into direct defs INSERTs (or the Plan 01 writer), since seeding via `createField` is disallowed here (per this plan's fixture note) and would throw against the wave-3 DAO churn — then extend the regressions to cover every privacy exclusion and a positive labeled value. Add an explicit test that a non-share_with_ai definition's value and a quarantined definition's value are ABSENT from the map returned to readSharedFields (the defs-filtered read is the guard now that pair rows exist for every definition). Keep ai-context-read a caller of live definitions plus the DAO map, without custom-value SQL. Modify production code only if needed to retain the closed allowlist. Do not expose uid, field_def_id, col_name, table names, backup/sync state, or a new UI. This implements D-08, D-11, D-12 and the defs-filtered privacy requirement.</action>
  <verify><automated>npm test -- src/db/ai-context-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove normalization cannot expand AI egress for unshared/quarantined/blank values, including an explicit "absent from the returned map" assertion.
    - Existing visible-label output survives for allowed values.
  </acceptance_criteria>
  <done>The AI privacy projection remains closed and behaviorally identical on normalized rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DAO compatibility map → profile/edit | Storage details must not leak into user-facing consumers; quarantined values must not appear. |
| Local read projection → AI prompt | Only explicitly shared nonblank values may cross provider egress. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-10 | Information disclosure | AI custom-field projection | high | mitigate | Closed allowlist, defs-filtered read, and exclusion regressions. |
| T-16-11 | Tampering | profile/edit projection | medium | mitigate | DAO map boundary plus populated/raw/photo + quarantine-exclusion coverage. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2); node:sqlite is available.
- Run named read-projection tests and `npx tsc --noEmit` (project green from Plan 02).
- Fixtures seed defs + normalized value rows via migration 006 or direct INSERTs / the Plan 01 value writer — NOT via createField or another sibling wave-3 DAO — so the four wave-3 plans (03/04/05/06) are order-independent within the wave.
</verification>

<success_criteria>
CFN-02/03/04 read paths retain every visible projection and AI privacy exclusion without exposing normalized
implementation details, and the defs-filtered read keeps quarantined/non-shared values out of both maps now
that a durable pair row exists for every definition.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-05-SUMMARY.md` when done.
</output>
