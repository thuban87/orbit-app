---
phase: 16-custom-field-value-normalization
plan: 05
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/db/contact-read.ts
  - src/db/contact-read.test.ts
  - src/db/ai-context-read.ts
  - src/db/ai-context-read.test.ts
autonomous: true
requirements: [CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edit/profile reads retain the established col_name-keyed compatibility map, NULL/empty behavior, invalid-value repair availability, and custom-photo relative paths."
    - "AI still includes only live share_with_ai nonblank values by visible label, excluding quarantined, opted-out, NULL, and whitespace-only values."
  artifacts:
    - src/db/contact-read.ts
    - src/db/ai-context-read.ts
  key_links:
    - "Both read modules remain callers of listDefs/getValuesForContact rather than constructing custom-value SQL."
    - "The AI projection retains its closed local-to-provider privacy allowlist."
---

<objective>
Lock profile/edit and AI read consumers to the normalized DAO compatibility map with production behavior
regressions, keeping value storage details entirely below their boundaries.

Purpose: normalized rows are only a safe replacement if existing visible and private projections cannot
change or widen during the conversion.
Output: migrated projection fixture coverage and only necessary production type/assumption cleanup.

## Artifacts
- Normalized edit/profile projection regressions.
- Normalized AI privacy-projection regressions.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove edit/profile projection compatibility over normalized value rows</name>
  <reversibility rating="costly">D-11 preserves the compatibility map used by existing form/profile consumers; changing it later would alter visible field behavior.</reversibility>
  <read_first>
    - src/db/contact-read.ts and src/db/contact-read.test.ts
    - src/db/field-values-dao.ts (Plan 01 map contract)
    - src/components/CustomFieldValue.tsx and src/components/field-widgets/photo-field-logic.ts
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
  </read_first>
  <files>src/db/contact-read.ts, src/db/contact-read.test.ts</files>
  <behavior>
    - A populated upgraded contact has the same edit/profile map, including NULL/empty distinction, invalid/retyped raw value, and custom-photo relative string.
    - No read consumer sees uid, field_def_id, table names, or migration metadata.
  </behavior>
  <action>Build on the Plan 01 direct normalized fixture conversion with focused RED assertions for populated values across types, NULL and empty text, an invalid/retyped raw value, active versus quarantined definitions, and a custom-photo relative path. Keep contact-read as a consumer of listDefs/getValuesForContact and change production source only when its types or assumptions need cleanup for that map. Do not add custom SQL, React Native screen changes, widgets, navigation, labels, accessibility, or layout changes. This implements D-08, D-11, and D-12.</action>
  <verify><automated>Node 22+: npm test -- src/db/contact-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove normalized rows preserve edit/profile map behavior and photo-path compatibility.
    - The production read boundary does not consume normalized internal identity/schema details.
  </acceptance_criteria>
  <done>Profile and edit reads preserve their public compatibility map over normalized storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the AI custom-field privacy projection over normalized rows</name>
  <reversibility rating="costly">The established share_with_ai allowlist is a privacy boundary; changing it changes data egress behavior.</reversibility>
  <read_first>
    - src/db/ai-context-read.ts and src/db/ai-context-read.test.ts
    - src/db/field-values-dao.ts and src/db/field-defs-dao.ts
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-11 and D-12)
  </read_first>
  <files>src/db/ai-context-read.ts, src/db/ai-context-read.test.ts</files>
  <behavior>
    - Prompt context contains only live share_with_ai definitions with nonblank values and user-visible labels.
    - Unshared, quarantined, NULL, empty/whitespace-only values do not cross the prompt boundary.
  </behavior>
  <action>Extend the normalized fixture regressions to cover every privacy exclusion and a positive labeled value. Keep ai-context-read a caller of live definitions plus the DAO map, without custom-value SQL. Modify production code only if needed to retain the closed allowlist after normalized map conversion. Do not expose uid, field_def_id, col_name, table names, backup/sync state, or a new UI. This implements D-08, D-11, and D-12.</action>
  <verify><automated>Node 22+: npm test -- src/db/ai-context-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove normalization cannot expand AI egress for unshared/quarantined/blank values.
    - Existing visible-label output survives for allowed values.
  </acceptance_criteria>
  <done>The AI privacy projection remains closed and behaviorally identical on normalized rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DAO compatibility map → profile/edit | Storage details must not leak into user-facing consumers. |
| Local read projection → AI prompt | Only explicitly shared nonblank values may cross provider egress. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-10 | Information disclosure | AI custom-field projection | high | mitigate | Closed allowlist and exclusion regressions. |
| T-16-11 | Tampering | profile/edit projection | medium | mitigate | DAO map boundary plus populated/raw/photo coverage. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Run named Node 22+ read-projection tests and `npx tsc --noEmit`.
</verification>

<success_criteria>
CFN-02/03/04 read paths retain every visible projection and AI privacy exclusion without exposing
normalized implementation details.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-05-SUMMARY.md` when done.
</output>
