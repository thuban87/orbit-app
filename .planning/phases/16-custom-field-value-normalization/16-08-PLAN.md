---
phase: 16-custom-field-value-normalization
plan: 08
type: execute
wave: 4
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05, 16-06]
files_modified:
  - docs/decisions/ADR-001-normalized-custom-field-values.md
  - CLAUDE.md
  - HANDOFF.md
autonomous: true
requirements: [CFN-01, CFN-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "docs/decisions/ADR-001-normalized-custom-field-values.md records the decision, the rejected alternatives (dual-read/dual-write mode, JSON value column, keeping ALTER TABLE/DROP COLUMN DDL), and the consequences — matching migration 006 as implemented."
    - "CLAUDE.md §'Custom fields — invariants' no longer states the four now-false storage-model invariants as live rules; each is marked superseded by migration 006 / ADR-001, while the still-true invariants (same-transaction field_history snapshot, sweeps-not-timers, type-changes-never-destroy-data, route sort/filter through sortExpr) are preserved."
    - "HANDOFF §14 carries an APPENDED superseding note (the decision record itself is not edited)."
  artifacts:
    - docs/decisions/ADR-001-normalized-custom-field-values.md
  key_links:
    - "migration 006 already cites ADR-001 in a comment (Plan 01) so the code→ADR edge will exist when the ADR-bridge tooling is later ported."
    - "The normalized table's UNIQUE(contact_id, field_def_id) and uid UNIQUE constraints are documented as correct (the value-column index/UNIQUE ban applied to the retired dynamic-column table, whose DROP-COLUMN hazard is gone)."
---

<objective>
Bring the project's own non-negotiable documentation into agreement with the implemented normalized model,
so a future agent does not "enforce" a stale storage invariant against the new schema.

Purpose: after migration 006, CLAUDE.md's custom-field invariants and HANDOFF §14 are actively wrong on the
storage-model specifics; leaving them is how the NEXT phase gets this wrong.
Output: an immutable ADR recording the reversal and its rejected alternatives, a synced CLAUDE.md invariants
block, and an appended HANDOFF §14 supersession note.

Addresses review concern: MEDIUM "no plan updates CLAUDE.md / HANDOFF §14 / writes an ADR for the single
largest reversal in the project." This is documentation SYNC to an ALREADY-owner-decided supersession
(CONTEXT D-01..D-12, REQUIREMENTS CFN-01..04, ROADMAP §58-60) — not a new reversal.
</objective>

## Artifacts this phase produces (owned by this plan)

- **`docs/decisions/ADR-001-normalized-custom-field-values.md`** — the first global ADR (the `docs/decisions/` directory is created here; no prior ADR exists in this repo).
- **`CLAUDE.md`** §"Custom fields — invariants" updated (storage-model bullets marked superseded; still-applicable invariants preserved).
- **`HANDOFF.md`** §14 with an appended supersession note.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@CLAUDE.md
@HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-001 recording the normalized-value decision and rejected alternatives</name>
  <read_first>
    - CLAUDE.md (§"Custom fields — invariants" and §"Knowledge base" — ADRs numbered globally, immutable; the ADR-bridge notes)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-01..D-12, D-06a)
    - .planning/phases/16-custom-field-value-normalization/16-RESEARCH.md (State of the Art; Anti-Patterns to Avoid — the rejected alternatives)
    - src/db/migrations/006-normalize-custom-field-values.ts (the implemented decision + its ADR-001 comment)
  </read_first>
  <files>docs/decisions/ADR-001-normalized-custom-field-values.md</files>
  <action>Create docs/decisions/ (absent today) and write ADR-001-normalized-custom-field-values.md (the first global ADR — no prior ADR exists). Structure it as: Status (Accepted, 2026-08); Context (the shipped dynamic-column-per-field model and why it blocks Phase 17 backup/sync — replicated-DDL risk, no portable value identity); Decision (the normalized custom_field_values table with id/uid/contact_id/field_def_id/value/created_at/modified_at, UNIQUE(contact_id, field_def_id), one durable row per contact×definition including blanks and quarantined defs, per-value immutable uid, UPSERT-on-pair writes, forward-only migration 006, fail-closed on loss-bearing inconsistency, and the D-06a orphan-column snapshot-and-proceed refinement, and the documented modified_at/created_at provenance policy). State D-03 HONESTLY: the database enforces AT-MOST-ONE row per pair via UNIQUE(contact_id, field_def_id); COMPLETENESS — a row for every contact×definition pair — is maintained by migration seeding plus seeding at every contact/field-creation point plus UPSERT self-heal, NOT by a lower-bound SQL constraint (SQLite cannot enforce lower-bound cardinality). Do not claim the completeness half is "database-enforced"; Rejected Alternatives (dual-read/dual-write compatibility mode — D-05; a JSON value column; keeping ALTER TABLE/DROP COLUMN DDL — replicated-DDL sync risk; each with why it was rejected); Consequences (CLAUDE.md invariants updated per Task 2; the value-column index/UNIQUE ban no longer applies since the retired table's DROP-COLUMN hazard is gone; D-03 holds as at-most-one-per-pair by UNIQUE(contact_id, field_def_id) while COMPLETENESS is maintained by seeding + UPSERT self-heal, not a lower-bound SQL constraint; createContactFull is the ONLY contact-creation path that seeds the pair matrix — the exported createContactWithInteraction (recency-dao.ts) writes no custom values and must not become a production contact-creation path without replicating the seeding (LOW-7); the permanent-delete photo-orphan is a bounded LOCAL orphan that is NOT reclaimed by contact purge — purge-photo-cleanup enumerates only surviving photo defs, so a deleted def's files remain until a future cleanup mechanism (do NOT claim purge removes them); the D-06a orphan-column field_history snapshot is a bounded 30-day local audit trace with no read surface and no backup path — it is NOT a recovery mechanism; Phase 17 consumes normalized rows and must read the timestamp provenance; multi-device conflict policy remains explicitly open). Keep it immutable and self-contained. This documents CFN-01/CFN-02 and D-01..D-12.</action>
  <verify><automated>test -f docs/decisions/ADR-001-normalized-custom-field-values.md && rg -q "Rejected Alternatives" docs/decisions/ADR-001-normalized-custom-field-values.md && rg -q "dual-read|dual-write" docs/decisions/ADR-001-normalized-custom-field-values.md</automated></verify>
  <acceptance_criteria>
    - The ADR exists, is numbered ADR-001, and contains Status/Context/Decision/Rejected Alternatives/Consequences.
    - Rejected Alternatives names dual-read/dual-write mode, a JSON value column, and keeping ALTER TABLE/DROP COLUMN DDL.
    - The Decision matches migration 006 as implemented (UNIQUE pair, per-value uid, UPSERT, fail-closed + D-06a orphan).
    - Consequences states, honestly: D-03 completeness is maintained by seeding + UPSERT self-heal (not a lower-bound SQL constraint) (item 7); the D-06a orphan field_history snapshot is a bounded 30-day local trace, NOT a recovery mechanism (MEDIUM-4); the permanent-delete photo-orphan is NOT reclaimed by contact purge (item 6); createContactFull is the only pair-matrix-seeding contact-creation path (LOW-7).
  </acceptance_criteria>
  <done>ADR-001 records the reversal, its rejected alternatives, and its consequences, matching the shipped migration.</done>
</task>

<task type="auto">
  <name>Task 2: Sync CLAUDE.md invariants and append the HANDOFF §14 supersession note</name>
  <read_first>
    - CLAUDE.md (§"Custom fields — invariants" — the nine bullets)
    - HANDOFF.md (§14 custom-field design — the decision record to append to, not edit)
    - docs/decisions/ADR-001-normalized-custom-field-values.md (Task 1)
  </read_first>
  <files>CLAUDE.md, HANDOFF.md</files>
  <action>In CLAUDE.md §"Custom fields — invariants", mark the four now-false STORAGE-MODEL bullets as superseded by migration 006 / ADR-001, updating each to the normalized reality: (1) the same field is no longer "a row in one table and a column in the other" — there is one custom_field_values row per (contact×definition); (2) "Every column in contact_custom_values is TEXT forever" — that table is retired; the single normalized value column is raw TEXT and is still never rewritten (the TEXT-forever principle survives for the value, the dynamic-column rationale does not); (4) "route every sort or filter through sortExpr()" stays true in NAME but the mechanism is now a static expression over the normalized value column, and note sortExpr still has no runtime consumer; (5) "Never add an index or a UNIQUE constraint to a column in contact_custom_values" — the ban applied to the retired dynamic-column table (DROP COLUMN failed on indexed columns); the normalized table legitimately carries uid UNIQUE and UNIQUE(contact_id, field_def_id), which is correct, not a violation; (8) "Deleting a defs row does not drop a column" — deleting a definition now removes its dependent value rows (cascade/explicit) in one transaction. PRESERVE unchanged the still-true invariants: field type drives the UI widget only; type changes never destroy data (7 parsers, flag-don't-coerce); every destructive op snapshots to field_history in the same transaction; quarantine expiry/history run as a launch sweep, never a timer; formatLocalDate usage. Cite ADR-001 and migration 006 in the updated bullets. Then in HANDOFF.md §14, APPEND a dated supersession note (do not edit the existing §14 decision text) stating that Phase 16 / migration 006 / ADR-001 replaced the dynamic-column storage model with normalized value rows, preserving all §14 user-facing behavior. This documents CFN-02. Do NOT regenerate adr-registry.ts or run npm run graph:build (see DEFERRED below).</action>
  <verify><automated>rg -q "ADR-001|migration 006|superseded" CLAUDE.md && rg -q "supersed|migration 006|ADR-001" HANDOFF.md</automated></verify>
  <acceptance_criteria>
    - CLAUDE.md's four storage-model invariants are marked superseded/updated with an ADR-001 citation; the still-true invariants are unchanged.
    - HANDOFF §14 gains an appended supersession note; the original §14 text is not deleted.
    - No colour/code behavior changes; this is documentation only.
  </acceptance_criteria>
  <done>The project's non-negotiable docs agree with the normalized model and cite ADR-001.</done>
</task>

</tasks>

<deferred>
## Explicitly deferred (documented, not dropped)

- **Regenerate `docs/decisions/adr-registry.ts` and run `npm run graph:build`** (review suggestion #7). REJECTED for this phase because the ADR-bridge tooling does not exist in this repo yet: there is no `docs/decisions/adr-registry.ts`, no `scripts/normalize-graph-docrefs.ts`, and no `gen:adr-registry` / `graph:build` npm scripts. CLAUDE.md (§"Graphify and feature intelligence" / "The ADR bridge") states this tooling must be PORTED from quest-board before the first graph build; porting it is separate infrastructure work outside Phase 16's data-migration scope. The ADR-001 citation comment is written into migration 006 now (Plan 01) so the code→ADR edge materializes automatically once the bridge is ported. This deferral is surfaced to the owner in the planning return.
</deferred>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Decision record → future agents | Stale invariants must not be enforceable against the new schema. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-18 | Tampering | stale project invariants | medium | mitigate | ADR-001 + synced CLAUDE.md/HANDOFF so a future agent does not fight the new schema. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Documentation-only plan; no test/tsc/color gate applies beyond the file-content assertions above.
- Do not run graph:build (blocked at the harness layer per CLAUDE.md; and the ADR bridge is not ported).
</verification>

<success_criteria>
CFN-01/CFN-02 documentation is complete: the largest storage reversal in the project has an immutable ADR,
CLAUDE.md's invariants match the shipped schema, and HANDOFF §14 records the supersession without erasing the
original decision.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-08-SUMMARY.md` when done.
</output>
