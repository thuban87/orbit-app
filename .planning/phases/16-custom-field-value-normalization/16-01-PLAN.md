---
phase: 16-custom-field-value-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/006-normalize-custom-field-values.ts
  - src/db/migrations/006-normalize-custom-field-values.test.ts
  - src/db/database.ts
  - src/db/field-values-dao.ts
  - src/db/field-values-dao.test.ts
  - App.tsx
autonomous: false
requirements: [CFN-01, CFN-03, CFN-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A populated v5 database atomically upgrades to v6 with one immutable uid-bearing custom_field_values row for EVERY contact × EVERY definition pair — including quarantined definitions and blank records. The database enforces AT-MOST-ONE row per pair via UNIQUE(contact_id, field_def_id); COMPLETENESS (a row for every pair) is maintained by migration seeding plus seeding at every contact/field-creation point plus UPSERT self-heal — that is HOW D-03 holds (no SQL constraint can enforce lower-bound cardinality)."
    - "Legacy NULL, empty TEXT, unusual raw TEXT, and custom-photo paths survive byte-for-byte; each migrated value row's modified_at/created_at carry the documented provenance (legacy row's modified_at for source-derived rows; deps.now for synthesized blanks)."
    - "A LOSS-bearing legacy inconsistency (a definition whose backing value column is missing) fails closed and leaves the original database unchanged; a NON-loss-bearing orphan dynamic column (no matching definition) is snapshotted to field_history and dropped in the same transaction, then the upgrade PROCEEDS (D-06a). That orphan snapshot is a bounded 30-day local audit trace with no read surface and no backup path (D-10) — it is NOT a recovery mechanism."
    - "The normalized read returns keys ONLY for the definitions passed by the caller — a quarantined or non-share_with_ai definition's value never appears in the map returned to getContactForEdit/readSharedFields (privacy)."
    - "The normalized value write is an UPSERT on the (contact_id, field_def_id) pair constraint, so an edit/clear can never silently discard a value against a missing row; a bare UPDATE is forbidden."
    - "A classified migration-integrity failure shows the approved plain-language copy; any OTHER bootstrap failure shows the generic 'Couldn't start Orbit' state whose body is CORRECTED per D-06b to drop the false support-channel promise (keeping the 'Your data is safe and unchanged' reassurance); both keep the navigator unmounted (classification changes the copy only, not the gating)."
  artifacts:
    - src/db/migrations/006-normalize-custom-field-values.ts
    - src/db/migrations/006-normalize-custom-field-values.test.ts
    - src/db/field-values-dao.ts
  key_links:
    - "database.ts registers migration006 at TARGET_VERSION 6 through the runner-owned transaction."
    - "field-values-dao joins literal custom_field_values/custom_field_defs tables and returns the existing col_name-keyed compatibility map, filtered by the passed defs, without interpolating col_name."
    - "The classified migration error reaches AppShell before the navigator mounts; migration006 cites ADR-001 in a code comment so the future code→ADR edge exists."
---

<objective>
Build the production-quality tracer: a real v5 database upgrades through migration 006, exposes its
unchanged value map through the normalized DAO, and fails closed at app bootstrap when a loss-bearing
correspondence proof fails (while a non-loss orphan column is snapshotted and the upgrade proceeds, per
D-06a).

Purpose: establish the one-way, testable persistence boundary before callers and lifecycle operations
expand onto it.
Output: migration 006, normalized DAO contract, version registration, and classified bootstrap-failure
presentation.

Addresses review concerns: HIGH pair-row invariant (create rows for EVERY definition incl. quarantined),
Claude-HIGH bare-UPDATE silent loss (mandate UPSERT), OWNER D-06a (orphan-column snapshot-and-proceed),
MEDIUM timestamp provenance, MEDIUM defs-filtered privacy read, MEDIUM legacy-identifier negative test.
</objective>

## Artifacts this phase produces (owned by this plan)

- **Table:** `custom_field_values` (`id` INTEGER PK, `uid` TEXT NOT NULL UNIQUE, `contact_id` INTEGER NOT NULL FK→contacts(id) ON DELETE CASCADE, `field_def_id` INTEGER NOT NULL FK→custom_field_defs(id) ON DELETE CASCADE, `value` TEXT nullable, `created_at` TEXT NOT NULL, `modified_at` TEXT NOT NULL, `UNIQUE(contact_id, field_def_id)`).
- **Migration:** `src/db/migrations/006-normalize-custom-field-values.ts` (exported `migration006`), registered in `database.ts` at `TARGET_VERSION = 6`.
- **DAO (reshaped, `field-values-dao.ts`):** `getValuesForContact(exec, contactId, defs)` (normalized, defs-filtered map — signature preserved); a normalized per-pair value writer that UPSERTs on `(contact_id, field_def_id)` and a non-mutexed `*Core` variant for transaction composition; the three pure placement selectors preserved unchanged. The legacy `upsertValue(exec, contactId, uid, colName, value, now)` / `upsertValueCore` (col_name-keyed, per-contact-row-uid) contract is RETIRED.
- **Test:** `src/db/migrations/006-normalize-custom-field-values.test.ts` (Wave 0 — success, rollback, orphan-column, and negative-identifier fixtures).
- ADR reference `ADR-001` cited in a `006-*.ts` comment (the ADR file itself is authored by Plan 16-08).

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
@.planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
@.planning/phases/16-custom-field-value-normalization/16-REVIEWS.md
@HANDOFF.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the migration-006 one-way-door execution boundary, including the D-06a refinement</name>
  <decision>Authorize the locked forward-only normalized representation and the D-06a orphan-column snapshot-and-proceed refinement after reviewing the fail-closed fixture contract.</decision>
  <context>Changing a released representation requires a further migration on unreachable device databases; Phase 17 depends on the durable uid-bearing row model. D-06a is itself one-way: the orphan-column path writes to field_history and advances user_version.</context>
  <options><option id="proceed">Proceed with the locked atomic conversion, the loss-bearing fail-closed proof, and the D-06a non-loss orphan-column snapshot-and-proceed path.</option><option id="pause">Pause before representation-changing code.</option></options>
  <read_first>
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-01, D-02, D-05 through D-08, and D-06a in full)
    - src/db/migrations/runner.ts (per-version transaction contract)
    - .planning/phases/16-custom-field-value-normalization/16-RESEARCH.md (Migration proof protocol)
    - .planning/phases/16-custom-field-value-normalization/16-REVIEWS.md (Owner Resolution banner)
  </read_first>
  <files>None — execution checkpoint only</files>
  <action>Present the already-locked decision without reopening it: migration 006 atomically converts to immutable uid-bearing rows, retires the dynamic store only after proof, and aborts on every LOSS-bearing inconsistency (missing value column, wrong legacy type, orphan legacy value row, unreadable values). State the D-06a refinement precisely: a NON-loss-bearing orphan dynamic column (present in the table with no matching definition — nothing to lose) is NOT a brick; its non-NULL data is snapshotted to field_history and it is dropped inside the same transaction, then the upgrade proceeds. State that dual read/write, skipped records, invented definitions, a post-migration DDL fallback, and reversing the loss-bearing fail-closed path are all excluded by D-05, D-06, and D-06a.</action>
  <verify><automated>MISSING — blocking owner confirmation is required before changing persisted representation.</automated></verify>
  <acceptance_criteria><criterion>The checkpoint names D-01, D-02, D-05, D-06, D-06a, D-07, D-08 and the forward-migration undo cost, and distinguishes the loss-bearing fail-closed path from the non-loss orphan-column snapshot-and-proceed path.</criterion></acceptance_criteria>
  <done>The owner authorizes the migration boundary and the D-06a refinement.</done>
  <resume-signal>Confirm that migration 006 may proceed.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Write migration 006 — atomic legacy→normalized conversion with fail-closed and D-06a orphan handling</name>
  <reversibility rating="one-way">D-01, D-02, D-05, and D-06a establish durable value identity and a forward-only persisted representation; the orphan path writes field_history and advances user_version.</reversibility>
  <read_first>
    - src/db/migrations/001-initial.ts (legacy custom_field_defs / contact_custom_values / field_history schema; the shipped fixed columns are contact_id, uid, modified_at)
    - src/db/migrations/runner.ts (per-version BEGIN/COMMIT ownership; migration.apply runs inside the already-open transaction)
    - src/db/database.ts (migration registry and TARGET_VERSION)
    - src/db/field-defs-dao.ts (listDefs incl. quarantined; the col_name compatibility key)
    - src/db/uid.ts (deps.newUid)
    - src/db/__testkit__/node-sqlite.ts (node:sqlite harness — Node 22 runtime)
    - .planning/phases/16-custom-field-value-normalization/16-RESEARCH.md (Migration proof protocol; Pattern 2)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-06a orphan-column resolution)
  </read_first>
  <files>src/db/migrations/006-normalize-custom-field-values.ts, src/db/migrations/006-normalize-custom-field-values.test.ts, src/db/database.ts</files>
  <behavior>
    - RED first: fixtures build a genuine v5 legacy database through runMigrations(001..005), then apply 006.
    - Success fixture covers all seven definition types, populated and blank values, NULL vs empty text, unusual number/toggle text, a custom-photo relative path, an archived contact, a contact with NO legacy contact_custom_values row, AND at least one quarantined definition.
    - The resulting custom_field_values holds exactly one row per (every contact × every definition, INCLUDING quarantined definitions), each with a distinct immutable uid, raw nullable TEXT value, created_at/modified_at, foreign keys, and the database UNIQUE(contact_id, field_def_id) constraint.
    - Timestamp provenance is asserted: a value row derived from a contact that HAD a legacy contact_custom_values row carries that legacy row's modified_at as BOTH modified_at and created_at; a synthesized blank for a contact with NO legacy row carries deps.now for both.
    - Loss-bearing rollback fixture: a definition whose backing value column is missing → migration throws → runner rolls back → user_version stays 5, contact_custom_values and its data are byte-unchanged, custom_field_values does not exist.
    - D-06a orphan fixture: an extra dynamic column with NO matching definition → its non-NULL values are snapshotted into field_history (field_col_name = the orphan column name, operation naming the orphan drop) → the legacy table is retired → user_version advances to 6 → the app proceeds. No custom_field_values row is invented for the orphan (there is no definition to attach it to).
    - Negative-identifier fixture: a persisted col_name/column pair that fails the safe-identifier guard throws BEFORE any write and rolls back.
  </behavior>
  <action>Implement migration006 ONLY through raw SqlExecutor calls inside the runner-owned transaction — never inWriteTransaction (a nested BEGIN + non-reentrant mutex would permanently hang; see transaction.ts). Follow the proof protocol: (1) read ALL definitions including quarantined via a direct SELECT; (2) read PRAGMA table_info(contact_custom_values) and validate every definition col_name against the safe-identifier guard before any controlled identifier access; (3) classify each structural discrepancy — a definition whose value column is MISSING, a wrong legacy field type, an orphan legacy value row (contact_id with no contacts row), or an unreadable value are LOSS-bearing and MUST throw (fail closed, D-06); an unexpected dynamic column with NO matching definition is NON-loss-bearing and MUST be handled per D-06a — snapshot its non-NULL values into field_history under the orphan column name with a distinct operation label and deps.now, and let it disappear when the legacy table is dropped, without inventing a definition or a value row for it. (4) Create custom_field_values with the schema in the Artifacts section, then insert one row for the COMPLETE contact × definition matrix across EVERY definition including quarantined ones — including NULL and empty-string values exactly as stored, and blank (NULL) rows for contacts with no legacy row and for the missing-value cells of contacts that do have one. Mint exactly one deps.newUid() per pair. Set modified_at AND created_at from the legacy row's modified_at when the contact has a legacy contact_custom_values row; set both to deps.now for a contact with no legacy row. Do not reuse the old per-contact contact_custom_values.uid. (5) Prove post-copy: row count equals contacts × definitions, the unique pair count matches, and every source cell's raw bytes (including custom-photo relative paths and the NULL/empty distinction) round-trip; throw on any mismatch so the runner rolls back. (6) DROP the legacy contact_custom_values table only after all proofs pass. Register migration006 in database.ts and set TARGET_VERSION = 6. Add a top-of-file comment in 006-*.ts citing ADR-001 (docs/decisions/ADR-001-normalized-custom-field-values.md, authored by Plan 16-08) so the code→ADR edge exists, and a header note stating the modified_at/created_at provenance policy above and that Phase 17 merge semantics are intentionally NOT decided here. The header note MUST also state that the D-06a orphan-column field_history snapshot is a bounded 30-day local audit trace with no read surface and no backup path — it is NOT a recovery mechanism (so a Phase 17 reader does not treat "snapshotted to field_history" as recoverable). This implements D-01, D-02, D-03, D-05, D-06, D-06a, D-09, D-10, and D-11.</action>
  <verify><automated>npm test -- src/db/migrations/006-normalize-custom-field-values.test.ts</automated></verify>
  <acceptance_criteria>
    - Success migration proves raw NULL differs from empty text, custom-photo strings and unusual raw TEXT are byte-identical, archived/no-row contacts and quarantined definitions all receive durable pairs, exactly one uid per pair, and v6 is set only after proof.
    - Timestamp provenance is asserted for both a source-derived row and a synthesized blank.
    - The loss-bearing rollback fixture proves no user_version advance, no partial normalized table, and no data/schema mutation of the old database.
    - The D-06a orphan fixture proves the orphan column's non-NULL values reach field_history, the upgrade advances to v6, and no bogus definition/value row is created.
    - The negative-identifier fixture throws before any write.
    - The 006-*.ts header cites ADR-001 and states the timestamp provenance policy.
  </acceptance_criteria>
  <done>A real v5 profile migrates losslessly to uid-bearing normalized rows; a loss-bearing inconsistency rolls back unchanged, and a non-loss orphan column is snapshotted and the upgrade proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite the value DAO to normalized rows with UPSERT-on-pair and defs-filtered reads</name>
  <reversibility rating="reversible">The DAO refactor is behind the migrated table; it can be corrected by code change (the irreversible artifact is the migration in Task 2).</reversibility>
  <read_first>
    - src/db/field-values-dao.ts and src/db/field-values-dao.test.ts (getValuesForContact map contract; upsertValue/upsertValueCore UID + interpolation contract; the three pure placement selectors)
    - src/db/migrations/006-normalize-custom-field-values.ts (Task 2 table shape)
    - src/db/transaction.ts (shared non-reentrant mutex; core/wrapper split)
    - src/db/contact-read.ts and src/db/ai-context-read.ts (the callers that pass defsForEditForm / sharedDefs — the defs the read must be filtered by)
  </read_first>
  <files>src/db/field-values-dao.ts, src/db/field-values-dao.test.ts</files>
  <behavior>
    - getValuesForContact keeps its (exec, contactId, defs) signature and returns a col_name-keyed map containing keys ONLY for the passed defs — a quarantined or non-shared def's row is never included because the caller does not pass it. The JOIN binds field_def_id values from the passed defs (or filters the result to them); it never interpolates col_name.
    - The normalized value write is an UPSERT keyed on the (contact_id, field_def_id) pair constraint: INSERT the pair with a fresh uid AND created_at = now on first write; ON CONFLICT(contact_id, field_def_id) DO UPDATE SET value = excluded.value, modified_at = excluded.modified_at (uid AND created_at on the INSERT branch ONLY — DO UPDATE never rewrites them). Because created_at is declared NOT NULL, the self-healing INSERT branch (a missing-row first write) MUST supply it or it throws. Clearing a value sets value = NULL via the same UPSERT and never deletes the row. A bare UPDATE is forbidden.
    - A non-mutexed *Core variant exists for composition inside an already-open transaction (create-contact in Plan 02), mirroring the retired upsertValueCore's role, without nesting the mutex.
    - The three pure placement selectors (defsForCreateForm / defsForEditForm / visibleDefsForProfile) are preserved byte-for-byte.
  </behavior>
  <action>Write RED field-values-dao regressions first against the Task 2 normalized table. Replace the dynamic-column SELECT in getValuesForContact with a literal join of custom_field_values and custom_field_defs, binding runtime values, filtered to the passed defs' field_def_ids, returning the same col_name→value map (still {} for an empty defs array). Replace upsertValue/upsertValueCore's per-contact-row-uid, ON CONFLICT(contact_id) contract with a per-PAIR write: an UPSERT on the (contact_id, field_def_id) unique constraint that sets value/modified_at from excluded and writes uid AND created_at (= now) on the INSERT branch ONLY (DO UPDATE never rewrites uid or created_at); keep the standalone mutexed entry and the non-mutexed core. Do not quote or interpolate col_name as SQL anywhere. Preserve the three pure selectors unchanged. Add explicit tests: (a) a defs-filtered read omits a quarantined def's value and a non-share_with_ai def's value; (b) an UPSERT against an existing pair updates in place and preserves uid; (c) clearing to NULL keeps the row and its uid; (d) a first write to a pair with no row INSERTs rather than silently losing the value; (e) a self-healed INSERT (missing-row first write) carries BOTH created_at and modified_at, satisfying the NOT NULL created_at. This implements D-01, D-02, D-03, D-11, and the Claude-HIGH UPSERT/privacy findings. NOTE: this DAO rewrite and the Task 2 migration together make the project temporarily non-compiling — production callers still use the retired write contract until Plan 02 migrates them; that is intentional (D-05 forbids a compatibility overload). This plan therefore runs NO project-wide tsc and NO full suite; those gates land at Plan 02 (tsc + grep) and wave-3 exit (full suite).</action>
  <verify><automated>npm test -- src/db/field-values-dao.test.ts src/db/migrations/006-normalize-custom-field-values.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove the read is filtered to the passed defs (quarantined / non-shared values excluded) and returns the col_name-keyed map.
    - Tests prove the write UPSERTs on the pair constraint: update-in-place preserves uid, clear-to-NULL keeps the row, and a first write against a missing pair INSERTs rather than discarding the value; the self-healed INSERT carries both created_at and modified_at (NOT NULL created_at satisfied).
    - `rg "ON CONFLICT\(contact_id\)" src/db/field-values-dao.ts` returns nothing (the per-contact-row conflict target is gone); the only ON CONFLICT target is the (contact_id, field_def_id) pair.
    - No col_name is interpolated/quoted into SQL in field-values-dao.ts.
  </acceptance_criteria>
  <done>The value DAO reads and writes normalized pair rows safely; edits and clears can never silently drop a value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Thread only the classified conversion error into the bootstrap gate</name>
  <read_first>
    - App.tsx (AppShell bootstrap: openAndMigrate effect, error/ready gating, the generic "Couldn't start Orbit" branch at ~205-217)
    - src/db/migrations/006-normalize-custom-field-values.ts (Task 2 classified error type/shape)
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md (Copywriting Contract — the REVISED classified migration-failure copy per D-06a AND the generic-bootstrap-failure body revised per D-06b)
  </read_first>
  <files>App.tsx</files>
  <behavior>
    - A classified migration-006 integrity failure renders the approved plain-language classified copy (see UI-SPEC, revised per D-06a — no "contact support" promise).
    - ANY other bootstrap failure shows the generic "Couldn't start Orbit" state whose BODY is corrected per D-06b to drop the false support-channel promise while keeping "Your data is safe and unchanged. Please reopen the app." Both keep the navigator unmounted; classification changes which copy shows, never the gating.
    - Successful migration is silent: no status, banner, or identity UI; the existing ActivityIndicator shows while pending.
  </behavior>
  <action>Distinguish the classified conversion error (thrown by migration006 with a recognizable marker) from any other bootstrap rejection in AppShell's catch, and render the revised classified heading/body from the UI-SPEC for the classified case. For every other bootstrap failure, render the generic "Couldn't start Orbit" state with its body corrected per D-06b: remove the false support-channel promise clause from App.tsx ~211-213 while keeping "Your data is safe and unchanged. Please reopen the app." (the project has no support channel to honor for any deterministic startup failure). Do not change the loading state, silent-success behavior, navigator gating, theme tokens, or any existing custom-field UI. Resolve all copy through theme tokens (check:colors must stay green). This implements D-06a (honest classified copy), D-06b (honest generic copy), D-08, and D-12.</action>
  <verify><automated>npm run check:colors</automated></verify>
  <acceptance_criteria>
    - The classified branch renders the revised UI-SPEC copy (asserted to contain no "contact support" text).
    - The generic branch body is corrected per D-06b (asserted to contain no "contact support" text) while keeping the "Your data is safe and unchanged" reassurance.
    - Navigator stays unmounted on either failure; successful migration adds no UI.
    - check:colors passes (no colour literal introduced).
  </acceptance_criteria>
  <done>A classified integrity failure surfaces honest, support-channel-free copy; every other bootstrap failure gets the generic copy corrected per D-06b (no false support promise); success is silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Legacy SQLite → migration006 | Historical schema/data must prove exact correspondence or (loss-bearing) remain untouched; a non-loss orphan column is snapshotted then dropped. |
| DAO caller → SQLite | Runtime values cross only as bound parameters; tables and query aliases are literal; col_name is never SQL. |
| Bootstrap → navigator | A failed irreversible conversion cannot mount database consumers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-01 | Tampering | migration006 | high | mitigate | Validate, copy/prove before retirement, throw inside the runner transaction; loss-bearing fails closed, non-loss orphan snapshots to field_history then drops (D-06a) — that snapshot is a bounded 30-day local audit trace with no read surface and no backup path, NOT a recovery mechanism. |
| T-16-02 | Tampering | value rows | high | mitigate | UNIQUE pair constraint, immutable uid policy, UPSERT-on-pair (no bare UPDATE), clear-retention regressions. |
| T-16-17 | Information disclosure | normalized read | medium | mitigate | getValuesForContact filters to the passed defs so quarantined/non-shared values never enter the returned map. |
| T-16-03 | Information disclosure | bootstrap error | medium | mitigate | Classify only integrity failures; render data-free approved copy with no false support channel. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2 in this shell); node:sqlite is available — the existing DB suite runs green. No separate Node-provisioning step is required.
- Run the task migration/DAO tests. This plan intentionally runs NO project-wide `npx tsc --noEmit` and NO full suite: the retired write contract leaves production callers non-compiling until Plan 02. The first tsc + the `contact_custom_values` grep completeness gate land in Plan 02; the full suite goes green at wave-3 exit.
- Run `npm run check:colors` after the App change.
</verification>

<success_criteria>
CFN-01's migration tracer is real: a populated v5 database converts losslessly to uid-bearing normalized rows,
the defs-filtered map is immediately readable without dynamic SQL, a loss-bearing conversion cannot partially
start the app, and a non-loss orphan column is snapshotted and the upgrade proceeds.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-01-SUMMARY.md` when done. Record the
modified_at/created_at provenance policy in the summary (Phase 17 will read it).
</output>
