---
phase: 16-custom-field-value-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/006-normalize-custom-field-values.ts
  - src/db/migrations/006-normalize-custom-field-values.test.ts
  - src/db/database.ts
  - src/db/field-values-dao.ts
  - src/db/field-values-dao.test.ts
  - src/db/contact-read.test.ts
  - src/db/ai-context-read.test.ts
  - App.tsx
autonomous: false
requirements: [CFN-01, CFN-03, CFN-04]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A populated v5 database atomically upgrades to v6 with one immutable uid-bearing custom_field_values row for every contact × definition pair, including blank records."
    - "Legacy NULL, empty TEXT, unusual raw TEXT, custom-photo paths, and modified timestamps survive byte-for-byte; an inconsistent legacy state leaves its original database unchanged."
    - "The normalized DAO has no dynamic custom-value SQL identifier path and its directly converted node fixtures compile against the new value-write contract."
    - "Only a classified migration-integrity failure blocks navigation with the approved plain-language copy; a successful upgrade is silent."
  artifacts:
    - src/db/migrations/006-normalize-custom-field-values.ts
    - src/db/migrations/006-normalize-custom-field-values.test.ts
    - src/db/field-values-dao.ts
  key_links:
    - "database.ts registers migration006 at TARGET_VERSION 6 through the runner-owned transaction."
    - "field-values-dao joins literal custom_field_values/custom_field_defs tables and returns the existing col_name-keyed compatibility map without interpolating col_name."
    - "The classified migration error reaches AppShell before the navigator mounts."
---

<objective>
Build the production-quality tracer: a real v5 database upgrades through migration 006, exposes its
unchanged value map through the normalized DAO, and fails closed at app bootstrap when correspondence
proof fails.

Purpose: establish the one-way, testable persistence boundary before callers and lifecycle operations
expand onto it.
Output: migration 006, normalized DAO contract, direct normalized test fixtures, version registration,
and classified bootstrap-failure presentation.

## Artifacts
- `migration006`, its Node 22+ rollback fixture, and the normalized value DAO.
- Version-six registration and the classified bootstrap integrity-failure branch.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
@.planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
@HANDOFF.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the already-approved migration-006 one-way-door execution boundary</name>
  <decision>Authorize the locked forward-only normalized representation after reviewing its fail-closed fixture contract.</decision>
  <context>Changing a released representation requires a further migration on unreachable device databases; Phase 17 depends on the durable uid-bearing row model.</context>
  <options><option id="proceed">Proceed with the locked atomic conversion and rollback proof.</option><option id="pause">Pause before representation-changing code.</option></options>
  <read_first>
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-01, D-02, D-05 through D-08)
    - src/db/migrations/runner.ts (per-version transaction contract)
    - .planning/phases/16-custom-field-value-normalization/16-RESEARCH.md (Migration proof protocol)
  </read_first>
  <files>None — execution checkpoint only</files>
  <action>Present the already locked decision without reopening it: migration 006 atomically converts to immutable uid-bearing rows, retires the dynamic store only after proof, and aborts on every inconsistency. State that dual read/write, skipped records, invented definitions, and a post-migration DDL fallback are excluded by D-05 and D-06.</action>
  <verify><automated>MISSING — blocking owner confirmation is required before changing persisted representation.</automated></verify>
  <acceptance_criteria><criterion>The checkpoint names D-01, D-02, D-05, D-06, D-07, and D-08 and the forward-migration undo cost.</criterion></acceptance_criteria>
  <done>The owner authorizes the already-approved migration boundary.</done>
  <resume-signal>Confirm that migration 006 may proceed.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Upgrade a populated v5 value map through normalized rows and the bootstrap gate</name>
  <reversibility rating="one-way">D-01, D-02, and D-05 establish durable value identity and a forward-only persisted representation.</reversibility>
  <read_first>
    - src/db/migrations/001-initial.ts and src/db/migrations/runner.ts (legacy schema and transaction ownership)
    - src/db/database.ts (registry and target version)
    - src/db/field-values-dao.ts and src/db/field-values-dao.test.ts (public map/write API and selectors)
    - src/db/contact-read.test.ts and src/db/ai-context-read.test.ts (direct old upsertValue fixtures to convert)
    - src/db/__testkit__/node-sqlite.ts (Node 22+ harness)
    - App.tsx and .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md (bootstrap preservation/copy contract)
  </read_first>
  <files>src/db/migrations/006-normalize-custom-field-values.ts, src/db/migrations/006-normalize-custom-field-values.test.ts, src/db/database.ts, src/db/field-values-dao.ts, src/db/field-values-dao.test.ts, src/db/contact-read.test.ts, src/db/ai-context-read.test.ts, App.tsx</files>
  <behavior>
    - A v5 fixture covers all seven definition types, populated and blank values, NULL, empty text, unusual number/toggle text, a custom-photo relative path, archived contacts, and a contact with no legacy values row.
    - The resulting table has local id, immutable globally unique uid, raw nullable TEXT value, timestamps, foreign keys, and a database UNIQUE(contact_id, field_def_id) constraint.
    - The public DAO reads the same col_name-keyed map with literal joins and bound values, while direct contact-read/AI node fixtures use its normalized write contract rather than a legacy signature or dynamic-column setup.
  </behavior>
  <action>Write RED migration regressions first with the Node 22+ node:sqlite executor. Implement migration006 only through raw executor calls inside the runner-owned transaction: validate the exact legacy fixed columns and definition keys before controlled source-column access, reject missing definitions, unexpected columns, orphan rows, and post-copy mismatch, then create custom_field_values and copy the complete contact × definition matrix. Mint exactly one deps.newUid() per pair; preserve source bytes and modified_at where present, create blank records for missing legacy rows, prove counts/pairs/value correspondence, and only then retire contact_custom_values. Register version six.

Replace field-values-dao dynamic SELECT/UPSERT with literal normalized-table queries, bound runtime values, and a definition-local write API that creates or updates one contact/definition pair without changing its uid; clearing sets value NULL without deleting the row. Preserve pure placement selectors and the col_name-keyed compatibility map, but do not quote/interpolate col_name as SQL. Convert the direct upsertValue calls and fixture helpers in contact-read.test.ts and ai-context-read.test.ts to seed definitions/normalized rows or call that new contract; this is a direct fixture conversion, not a compatibility overload. Thread only the classified conversion error into the existing App bootstrap error branch using the exact UI-SPEC copy, keeping pending loading, generic startup failure, silent success, navigator gating, theme tokens, and all existing custom-field UI unchanged. This implements D-01 through D-03, D-05 through D-09, D-11, and D-12.</action>
  <verify><automated>Node 22+: npm test -- src/db/migrations/006-normalize-custom-field-values.test.ts src/db/field-values-dao.test.ts src/db/contact-read.test.ts src/db/ai-context-read.test.ts</automated></verify>
  <acceptance_criteria>
    - Migration success proves raw NULL differs from empty text, custom-photo strings and unusual raw TEXT are byte-identical, archived/no-row contacts receive durable pairs, and v6 is set only after proof.
    - Each failure regression proves no user_version advance, no partial normalized representation, and no data/schema mutation of the old database.
    - The converted read-test fixtures contain no ALTER TABLE custom-value setup and no obsolete upsertValue argument shape.
    - A classified integrity failure leaves navigation unmounted with approved plain-language copy; successful migration adds no status or identity UI.
  </acceptance_criteria>
  <done>A real v5 profile can migrate and be read through normalized rows; any integrity failure rolls back before app consumers mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Legacy SQLite → migration006 | Historical schema/data must prove exact correspondence or remain untouched. |
| DAO caller → SQLite | Runtime values cross only as bound parameters; tables and query aliases are literal. |
| Bootstrap → navigator | A failed irreversible conversion cannot mount database consumers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-01 | Tampering | migration006 | high | mitigate | Validate, copy/prove before retirement, and throw inside the runner transaction. |
| T-16-02 | Tampering | value rows | high | mitigate | UNIQUE pair constraint, immutable uid policy, and clear-retention regressions. |
| T-16-03 | Information disclosure | bootstrap error | medium | mitigate | Classify only integrity failures and render data-free approved copy. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Use Node 22+ for every node:sqlite command; Node 18 is not a substitute.
- Run the task's named migration/DAO/read tests. Plan 02 completes the atomic TypeScript caller migration and owns the first phase-wide `npx tsc --noEmit` gate.
- Run `npm run check:colors` after the App change.
</verification>

<success_criteria>
CFN-01's migration tracer is real: a populated v5 database converts losslessly to uid-bearing normalized rows,
the map is immediately readable without dynamic SQL, and a failed conversion cannot partially start the app.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-01-SUMMARY.md` when done.
</output>
