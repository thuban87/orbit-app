---
phase: 16-custom-field-value-normalization
plan: 07
type: execute
wave: 4
depends_on: [16-02, 16-03, 16-04, 16-05, 16-06]
files_modified:
  - src/db/migrations/006-normalize-custom-field-values.test.ts
  - .planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md
autonomous: false
requirements: [CFN-01, CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A real populated v5 fixture proves migration, normalized lifecycle, sort/filter, profile/edit, AI, custom-photo, and history behavior together."
    - "A physical Pixel upgrade of populated test-profile data opens silently and preserves custom-field behavior; a controlled bad fixture fails closed without mounting navigation."
    - "Final evidence explicitly leaves backup/export/restore, sync transport, conflict policy, tombstones, identity UI, and custom-field redesign out of Phase 16."
  artifacts:
    - src/db/migrations/006-normalize-custom-field-values.test.ts
    - .planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md
  key_links:
    - "The all-path fixture runs migrations 001–006 through the real runner, then exercises the DAOs consumed by forms/profile/AI."
    - "The blocking physical-device procedure uses the established desktop release-build and Pixel runbook rather than treating node:sqlite as a substitute."
---

<objective>
Complete the cross-cutting proof matrix and the required physical Pixel upgrade UAT after every
normalized producer and consumer is implemented.

Purpose: unit paths cannot alone prove that real pre-006 device data survives a forward-only SQLite
migration with its established field experience intact.
Output: one rich automated upgrade regression and a signed owner-facing device UAT record.

## Artifacts
- An all-path populated v5-to-v6 automated migration proof.
- `16-UPGRADE-UAT.md` with physical Pixel evidence and phase scope confirmation.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
@.planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
@docs/runbooks/desktop-build-pipeline.md
@HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the populated-profile migration and lifecycle proof matrix</name>
  <reversibility rating="reversible">This task expands automated evidence only; it does not change the migrated representation or user data contract.</reversibility>
  <read_first>
    - src/db/migrations/006-normalize-custom-field-values.test.ts (Plan 01 migration/rollback fixture)
    - src/db/field-values-dao.test.ts, src/db/contacts-dao.test.ts, src/db/field-ddl.test.ts, src/db/field-type-change.test.ts, src/db/field-sort.test.ts
    - src/services/field-sweep.test.ts, src/db/contact-read.test.ts, src/db/ai-context-read.test.ts
    - .planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
  </read_first>
  <files>src/db/migrations/006-normalize-custom-field-values.test.ts</files>
  <action>Expand the real v5 fixture rather than duplicating focused tests. Seed all seven definitions, populated and blank values, NULL and empty text, unusual raw number/toggle text, a custom-photo relative path, an archived contact, and an existing profile with no legacy row. Migrate through runMigrations and exercise normalized DAOs to prove create/edit/clear, pair uid uniqueness, sort/filter raw-TEXT behavior, type-change preflight/history, quarantine/restore/strict expiry/permanent deletion, profile/edit compatibility, AI privacy projection, and field_history key preservation. Keep isolated structural/value correspondence rollback cases. Assertions must prove no source byte is rewritten and must state that Phase 17 backup/tombstone/reconciliation and later conflict policy are not decided here. This completes D-07, D-10, and D-11.</action>
  <verify><automated>Node 22+: npm test -- src/db/migrations/006-normalize-custom-field-values.test.ts src/db/field-values-dao.test.ts src/db/contacts-dao.test.ts src/db/field-ddl.test.ts src/db/field-type-change.test.ts src/db/field-sort.test.ts src/services/field-sweep.test.ts src/db/contact-read.test.ts src/db/ai-context-read.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - The fixture proves every required type/value/lifecycle/profile/AI/photo case after a real upgrade.
    - Failure cases prove rollback with unchanged version and no partially retired legacy store.
    - The suite contains no dynamic post-migration custom-field DDL fixture setup.
  </acceptance_criteria>
  <done>Automated CFN-01 through CFN-04 evidence covers a realistic upgrade and its retained behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Perform the physical Pixel populated-database upgrade UAT</name>
  <what-built>Migration 006, lifecycle, read projections, sorting/filtering, and the automated Node 22+ suite have passed.</what-built>
  <how-to-verify>Install the release build over populated pre-006 test-profile data with the approved desktop-to-Pixel pipeline, execute every required observation, and sign the UAT record.</how-to-verify>
  <precondition>A Node 22+ full suite is green, populated pre-006 test-profile data is available, and docs/runbooks/desktop-build-pipeline.md is operational.</precondition>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
    - .planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-07 and D-08)
  </read_first>
  <files>.planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md</files>
  <action>Create the UAT record before testing. Install migration-006 release build over populated test-profile data and record device/build identifiers, pre-upgrade state, evidence, and PASS/FAIL for silent launch; all seeded values/blanks; custom photo; create/edit/clear; invalid retype Tap to fix; quarantine/restore/expiry permanent delete; sort/filter parity; AI sharing exclusions; and long label/option/value wrapping without clipped recovery/action text. Verify there is no uid, field_def_id, col_name, table, backup, sync, or conflict UI. In a disposable test profile only, exercise a deliberate malformed fixture and confirm approved classified failure copy, unmounted navigator, and unchanged database after rollback. Do not use personal data and do not add backup, sync, conflict policy, tombstones, identity display, or UI redesign.</action>
  <verify><automated>Node 22+: npm test && npx tsc --noEmit && npm run check:colors</automated><human-check>Owner or executor signs every UAT scenario with evidence.</human-check></verify>
  <acceptance_criteria>
    - The UAT record records PASS/FAIL evidence for each listed successful-upgrade, lifecycle, visual-preservation, and classified-failure observation.
    - Safe success is silent; integrity failure does not mount partial UI.
    - The record explicitly confirms the phase did not decide Phase 17 backup/sync conflict work or redesign the custom-field UI.
  </acceptance_criteria>
  <done>A physical Pixel has safely upgraded populated data and its evidence is ready for phase verification.</done>
  <resume-signal>Record PASS with evidence, or record the failing scenario and stop for remediation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Node fixture → migration proof | Fixture must model real v5 edge cases rather than fresh/empty state. |
| Release build → device database | Forward-only schema migration crosses into persistent on-device data. |
| Malformed fixture → user data | Failure-path testing must run only against disposable test content. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-14 | Tampering | cross-cutting migration proof | high | mitigate | Rich runner fixture, rollback matrix, and full targeted suite. |
| T-16-15 | Tampering | physical device upgrade | high | mitigate | Blocking populated-data Pixel UAT before verification. |
| T-16-16 | Information disclosure | failure-path UAT | medium | mitigate | Isolated test profile and data-free classified copy. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Node 22+ is mandatory for node:sqlite; run the named all-path command before the checkpoint.
- Before physical UAT, run `npm test`, `npx tsc --noEmit`, and `npm run check:colors`.
- The signed UAT record must PASS before `$gsd-verify-work`.
</verification>

<success_criteria>
CFN-01 through CFN-04 are complete only when automated proof and a populated-data physical Pixel upgrade
both demonstrate lossless, fail-closed normalization with unchanged custom-field behavior.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-07-SUMMARY.md` when done.
</output>
