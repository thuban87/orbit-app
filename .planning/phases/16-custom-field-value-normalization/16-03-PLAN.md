---
phase: 16-custom-field-value-normalization
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/db/field-ddl.ts
  - src/db/field-ddl.test.ts
  - src/db/field-defs-dao.ts
  - src/db/field-defs-dao.test.ts
  - src/services/field-sweep.ts
  - src/services/field-sweep.test.ts
autonomous: true
requirements: [CFN-02, CFN-04]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Creating a field creates a durable blank normalized record for every existing contact without custom-field DDL."
    - "A populated field quarantines non-destructively; permanent deletion snapshots non-NULL values under field_col_name and removes definitions/pairs atomically."
    - "The existing strict 30-day foreground sweep and restore-race recheck remain intact."
  artifacts:
    - src/db/field-ddl.ts
    - src/db/field-defs-dao.ts
    - src/services/field-sweep.ts
  key_links:
    - "Lifecycle writers use Plan 01's normalized pair core inside the shared serialized transaction boundary."
    - "field_history continues to use the immutable definition col_name compatibility key."
---

<objective>
Move definition creation, quarantine, permanent deletion, and the foreground expiry sweep from dynamic
columns to normalized pair-row lifecycle operations.

Purpose: after migration, every field lifecycle action must preserve the existing recovery behavior
without future ALTER TABLE or DROP COLUMN work.
Output: row-based lifecycle DAOs and focused migration-006 fixture regressions.

## Artifacts
- Normalized definition lifecycle/emptiness operations and their regressions.
- The race-safe 30-day normalized field-sweep path.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert field creation and delete-or-quarantine decisions to normalized pairs</name>
  <reversibility rating="costly">D-03, D-04, D-09, and D-10 make pair lifecycle and history compatibility shared persisted behavior.</reversibility>
  <read_first>
    - src/db/field-ddl.ts and src/db/field-ddl.test.ts (core/wrapper split and lifecycle decisions)
    - src/db/field-defs-dao.ts and src/db/field-defs-dao.test.ts (definition metadata and emptiness)
    - src/db/field-values-dao.ts (Plan 01 normalized pair core)
    - src/db/transaction.ts and src/db/migrations/001-initial.ts (transaction/history contracts)
  </read_first>
  <files>src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts</files>
  <behavior>
    - A new definition adds a NULL pair record for every existing contact in its creation transaction.
    - Field emptiness preserves existing non-NULL semantics; populated deletion quarantines, while deletion snapshots eligible values then removes pairs and definition together.
    - New runtime custom-field operations do not issue custom-field ALTER TABLE or DROP COLUMN SQL.
  </behavior>
  <action>Write RED lifecycle regressions using the normalized fixture from Plan 01. Replace dynamic table alteration and dynamic value-column queries in field-ddl and field-defs-dao with literal custom_field_values queries filtered by bound field_def_id. Preserve the one outer inWriteTransaction pattern: create definition plus its blank contact pairs is atomic; delete-or-quarantine uses the existing population decision; permanent delete inserts field_history snapshots from non-NULL values with def.col_name as field_col_name before deleting dependent pairs then the definition. Retain NULL-versus-empty storage behavior, quarantine as non-destructive metadata, and no mutable uid policy. This implements D-01, D-03, D-04, D-09, and D-10.</action>
  <verify><automated>Node 22+: npm test -- src/db/field-ddl.test.ts src/db/field-defs-dao.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove creation inserts durable blanks for every existing contact and permanent delete is atomic with snapshots.
    - Tests prove populated fields quarantine and the snapshot retains raw value plus field_col_name.
    - Lifecycle source has no executable custom-field ALTER/DROP operation after migration006.
  </acceptance_criteria>
  <done>Definition lifecycle works entirely through normalized pair rows and preserves recovery history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain strict expiry and restore-race protection in the field sweep</name>
  <read_first>
    - src/services/field-sweep.ts and src/services/field-sweep.test.ts (candidate scan, localtime cutoff, non-reentrant transaction discipline)
    - src/db/field-ddl.ts (Task 1 normalized expiry core)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-04 and D-10)
  </read_first>
  <files>src/services/field-sweep.ts, src/services/field-sweep.test.ts</files>
  <behavior>
    - Candidate discovery remains outside the mutex and the stale-state recheck occurs under it, so a restored field is not expired.
    - Strict older-than-30-day expiry and existing history pruning remain exactly local/foreground behavior.
  </behavior>
  <action>Convert sweep tests to the normalized lifecycle fixtures and retain the existing candidate scan followed by an under-lock expiry recheck. Call the Task 1 destructive core rather than recreating SQL or nesting inWriteTransaction. Preserve the fixed localtime 30-day cutoff and history pruning policy, and cover a restore committed between scan and lock. This implements D-04 and D-10 without adding notifications, backup, sync, or UI.</action>
  <verify><automated>Node 22+: npm test -- src/services/field-sweep.test.ts src/db/field-ddl.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - A stale quarantined field expires with normalized value snapshots; a concurrently restored one survives.
    - The sweep retains 30-day history pruning and issues no custom-field DDL.
  </acceptance_criteria>
  <done>The foreground sweep remains race-safe and preserves the established bounded local recovery trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Field-management UI → lifecycle DAO | User-originated metadata reaches only bound relation values. |
| Sweep candidate → destructive transaction | Time-based candidate discovery must not override a concurrent restore. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-06 | Tampering | field lifecycle | high | mitigate | Atomic snapshot/delete core and bound ids. |
| T-16-07 | Denial of service | sweep restore race | medium | mitigate | Under-lock recheck; no nested mutex. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Run all named node:sqlite tests under Node 22+, then `npx tsc --noEmit`.
</verification>

<success_criteria>
CFN-02/04 lifecycle behavior persists: fields create blank rows, retain quarantine/restore/expiry safeguards,
and record bounded field_history snapshots without any dynamic DDL.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-03-SUMMARY.md` when done.
</output>
