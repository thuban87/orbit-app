---
phase: 19-system-contact-import
plan: 11
type: execute
wave: 5
depends_on: [19-05, 19-06, 19-07]
files_modified:
  - src/services/import/source-consolidation.ts
  - src/services/import/source-consolidation.test.ts
  - src/components/ConsolidationPrompt.tsx
  - src/screens/DuplicateReviewScreen.tsx
autonomous: true
requirements: [IMP-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When multiple SELECTED source records show strong evidence of being the same real person, Orbit may offer to import them as ONE new Orbit contact with multiple external links — and this consolidation is never silent: the user must explicitly choose Combine into one or Keep separate (IMP-03, Cluster K)."
    - "Consolidation is conservative: it detects same-person source clusters using the same strong-evidence rules as duplicate scoring (deterministic/canonical-method), never name-only, and defaults to Keep separate when in doubt (Cluster K)."
    - "Combining creates one Unbound contact via the composed importer with one external_contact_links row per source record (the importer already accepts an array of links), merging normalized methods; it performs NO generic Orbit-to-Orbit merge (Cluster J stays deferred)."
    - "[ASSUMPTION | Cluster K conservative-first] The first implementation only surfaces high-confidence source clusters; low-confidence clusters are left as separate rows for normal per-record review — no silent grouping."
  artifacts:
    - "src/services/import/source-consolidation.ts — detectSourceClusters(): groups a session's selected source records into same-person clusters by strong evidence"
    - "src/components/ConsolidationPrompt.tsx — explicit Combine into one / Keep separate choice (never silent)"
    - "consolidation offer wired into DuplicateReviewScreen"
  key_links:
    - "detectSourceClusters reuses the plan-05 strong-evidence signals (deterministic + canonical phone/email), never name-only, so a cluster is conservative"
    - "Combine calls importContactRecord with externalLinks = [one per source record] (the plan-03 array parameter) — one contact, multiple links"
  prohibitions:
    - "Do not consolidate silently; the user explicitly chooses Combine into one or Keep separate (Cluster K)."
    - "Do not cluster on name-only similarity; require strong (deterministic/canonical-method) evidence."
    - "Do not perform generic Orbit-to-Orbit merge of existing contacts (Cluster J — deferred to Phase 20)."
    - "Do not hardcode any colour; theme tokens only."
---

<objective>
Deliver the conservative multi-source consolidation Phase 19 explicitly owns: detecting that several selected system-contact records represent one person and offering an explicit Combine-into-one (with multiple external links) vs Keep-separate choice.

Purpose: The phase boundary lists "conservative multi-source consolidation" as owned by Phase 19; research sequenced it last (after the tracer + single/bulk land). This is the minimal, never-silent, strong-evidence-only version — distinct from the generic Orbit-to-Orbit merge that stays deferred.
Output: source-consolidation detector + ConsolidationPrompt + the DuplicateReview wiring.
</objective>

<assumption_delta_decision>
Detector fired (pluralization): a contact may now carry MULTIPLE external links (one → many) via consolidation.
- Primary noun: the Orbit contact id (contacts.id) + its normalized methods remains the identity primary.
- Decision: no-change. `external_contact_links` is already a plural, non-authoritative provenance table (schema permits N active links per contact; the deterministic-identity check keys on a single (provider, external_contact_id) row, which stays unique). Multiple links per contact is provenance breadth, not a new identity model — System Contacts is source-only, never Orbit's authority (Cross-Domain Invariant 2).
- Invariant guard (suggested test): a consolidated contact's deterministic-bypass lookup resolves each of its source external ids back to the same single contact — encoded in source-consolidation.test.ts.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@src/services/import/duplicate-evidence.ts
@src/db/imported-contact-dao.ts
@src/db/import-session-read.ts
@src/screens/DuplicateReviewScreen.tsx
@src/components/CandidateCardGrid.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: source-consolidation — detect same-person source clusters + combine writer</name>
  <files>src/services/import/source-consolidation.ts, src/services/import/source-consolidation.test.ts</files>
  <behavior>
    - two selected source rows sharing a canonical phone/email → one cluster (strong evidence)
    - two rows matching on name only → NOT clustered (conservative)
    - combineCluster imports ONE Unbound contact with one external_contact_links row per source row, merged methods
    - after combine, findActiveExternalLink resolves each source external id to the SAME contact id
    - a mixed session yields high-confidence clusters + ungrouped singletons
  </behavior>
  <read_first>src/services/import/duplicate-evidence.ts (strong-evidence signals — plan 05; reuse canonical-method matching, exclude name-only), src/db/imported-contact-dao.ts (importContactRecord externalLinks array + findActiveExternalLink — plan 03), src/db/import-session-read.ts (listSessionRows — plan 01)</read_first>
  <action>Write `detectSourceClusters(session rows)` that groups selected source records into same-person clusters using ONLY strong evidence (shared canonical phone/email via the plan-05 signals) — never name-only; a singleton stays ungrouped. Write `combineCluster(exec, { rows, batchCategoryId, now })` that maps the cluster's merged methods into one CreateContactFullInput and calls importContactRecord with `externalLinks` = one {provider:'android', externalContactId} per source row (the plan-03 array parameter), producing ONE Unbound contact with multiple links, then setRowContact('linked'/'imported') for each row. No generic Orbit-to-Orbit merge. Test the strong-only clustering, the name-only exclusion, and the post-combine deterministic-resolution invariant (each source external id → the same contact).</action>
  <verify>
    <automated>npx vitest run src/services/import/source-consolidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "shared canonical phone clusters two source rows; name-only does not" passes.
    - Test "combineCluster yields one contact with one external link per source row" passes.
    - Test "each source external id resolves to the same contact after combine" passes.
    - `npx vitest run src/services/import/source-consolidation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Strong-evidence source clusters are detected conservatively and can be combined into one contact with multiple links.</done>
</task>

<task type="auto">
  <name>Task 2: ConsolidationPrompt + DuplicateReview wiring (never silent)</name>
  <files>src/components/ConsolidationPrompt.tsx, src/screens/DuplicateReviewScreen.tsx</files>
  <read_first>src/screens/DuplicateReviewScreen.tsx (plan 07 workspace), src/services/import/source-consolidation.ts (Task 1), .planning/phases/19-system-contact-import/19-UI-SPEC.md (colourless idiom; ≥44px; Modal sheet idiom), src/components/OverflowMenu.tsx</read_first>
  <action>Create ConsolidationPrompt: a Modal sheet that, for a detected high-confidence source cluster, shows the clustered source records and two explicit choices — `Combine into one` (→ combineCluster) and `Keep separate` (→ leave the rows for normal per-record review). Never auto-combine. In DuplicateReviewScreen, run detectSourceClusters on load; when a high-confidence cluster exists, surface the ConsolidationPrompt (or an in-grid affordance) before/alongside normal review. Colours via theme tokens; the consolidation offer is neutral (no semantic hue).</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: select two system-contact records for the same person (shared phone) → Orbit offers Combine into one / Keep separate; Combine creates one Unbound contact with two external links; Keep separate leaves them as separate rows.</human-check>
  </verify>
  <acceptance_criteria>
    - The consolidation offer requires an explicit tap (no auto-combine path exists).
    - Combine routes to combineCluster; Keep separate leaves rows untouched for normal review.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Multiple same-person source records can be explicitly combined into one contact with multiple links, or kept separate — never silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clustered source records → one contact | Combining must be user-authorized and strong-evidence only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Spoofing (mistaken identity) | source clustering | high | mitigate | Strong-evidence only (canonical method), never name-only; explicit Combine/Keep-separate choice; default Keep separate (Cluster K) |
| T-19-19 | Repudiation (silent action) | consolidation | high | mitigate | Never silent; the user explicitly chooses; conservative-first (Cluster K) |
| T-19-01 | Tampering | combine writer SQL | high | mitigate | Reuses `?`-bound importContactRecord + session DAO (V5) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/services/import/source-consolidation.ts`: `detectSourceClusters()`, `combineCluster()`.
- `src/components/ConsolidationPrompt.tsx` (explicit Combine/Keep-separate modal).
- DuplicateReviewScreen consolidation wiring.
- Test: `source-consolidation.test.ts`.

<verification>
- `npx vitest run src/services/import/source-consolidation.test.ts` green (strong-only clustering + deterministic-resolution invariant).
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: explicit Combine/Keep-separate flow confirmed.
</verification>

<success_criteria>
Multiple selected source records that clearly represent one person can be explicitly combined into one Orbit contact with multiple external links — conservative, strong-evidence-only, never silent, and never a generic Orbit-to-Orbit merge.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-11-SUMMARY.md` when done.
</output>
