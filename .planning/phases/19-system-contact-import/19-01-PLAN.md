---
phase: 19-system-contact-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/012-import-sessions.ts
  - src/db/migrations/012-import-sessions.test.ts
  - src/db/database.ts
  - src/db/migrations/full-chain.test.ts
  - src/db/import-session-dao.ts
  - src/db/import-session-dao.test.ts
  - src/db/import-session-read.ts
  - src/db/import-session-read.test.ts
autonomous: false
requirements: [IMP-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Applying MIGRATIONS from v11 to v12 creates import_sessions + import_session_rows, preserves every existing row, and PRAGMA foreign_key_check is empty (IMP-04)."
    - "A durable session snapshot (name/methods/birthday JSON + document-dir staged photo path + phone_region) survives a simulated process death: after re-opening the same DB the pending session and its rows read back identically with no picker re-query (IMP-04)."
    - "acceptImportSessionWithRows writes the session AND all its rows AND total_rows in ONE transaction, so a crash never leaves a valid-looking session with a partial/mismatched row set (IMP-04; addresses review HIGH 19-01 non-atomic snapshot + MEDIUM total_rows consistency)."
    - "discardSession sets status='discarded' and DELETEs every row WITHOUT a contact_id (pending/needs_review/failed/user-skipped) while rows carrying a contact_id (imported/linked) are preserved; it returns the deleted rows' photo_rel_path list so the caller can delete staged files after commit (IMP-04; corrects review MEDIUM 19-01 discard-scope wording)."
    - "sessionSummaryCounts returns the four completion-summary buckets { imported, alreadyInOrbit, needReview, failedOrSkipped } computed by a single grouped query, where alreadyInOrbit keys off match_outcome='already_linked' (NOT row_status) so deterministic already-linked rows are counted distinctly from user Skips (IMP-02/IMP-04; addresses review HIGH cross-plan count-semantics collision 01/06/07/08)."
    - "The session DAO exposes BOTH mutexed standalone writers (each its own inWriteTransaction) AND non-mutexed *Core row writers (setRowContactCore, markRowStatusCore, setRowMatchOutcomeCore, markRowPhotoFailedCore) that assume an open transaction, so plan 03's composed importer can resolve a session row in the SAME transaction as the contact create (addresses review HIGH atomic contact+session-row write)."
    - "getResumableSession returns the single most-recent status='pending' session; any OLDER pending sessions are swept to status='discarded' in the same call so at most one import session is ever resumable (addresses review MEDIUM 19-09 multiple-pending-sessions unspecified)."
    - "[ASSUMPTION | spec-less edge IMP-04/unclassified] The durable session snapshot is the SOLE source of truth after acceptance; the expired picker grant is never re-queried, and match outcomes are stored on the row (match_outcome, matched_contact_id) rather than recomputed on read."
    - "[ASSUMPTION | spec-less edge IMP-02/concurrency] Each imported row transitions pending → imported/linked/skipped/failed/needs_review independently; an interrupted run leaves committed rows committed and the rest pending in the session."
    - "[ASSUMPTION | spec-less edge IMP-04/backup — review MEDIUM 19-backup policy] import_sessions and import_session_rows are LOCAL-ONLY runtime state (like restore-pending photo staging): they are never exported and a v12-database restore/Replace-all neither reads nor writes them; a test asserts the export manifest table set omits both tables."
  artifacts:
    - "src/db/migrations/012-import-sessions.ts — migration012 (version 12): CREATE TABLE import_sessions (incl. phone_region TEXT) + import_session_rows + indexes; additive, no contacts rebuild"
    - "src/db/database.ts — TARGET_VERSION = 12, migration012 appended to MIGRATIONS"
    - "src/db/import-session-dao.ts — createImportSession, insertSessionRow, acceptImportSessionWithRows (atomic), markRowStatus, setRowMatchOutcome, setRowContact, markRowPhotoFailed, completeSession, discardSession (returns staged photo paths); PLUS non-mutexed cores setRowContactCore/markRowStatusCore/setRowMatchOutcomeCore/markRowPhotoFailedCore"
    - "src/db/import-session-read.ts — getResumableSession (one pending, sweeps older), listSessionRows, sessionRowCounts (6 raw buckets), sessionSummaryCounts (4 completion buckets)"
    - "The canonical (event → row_status, match_outcome) state-transition table, documented in import-session-dao.ts as the single source of truth consumed by plans 06/07/08/11"
  key_links:
    - "import_sessions.batch_category_id REFERENCES categories(id) with NULL meaning Uncategorized — migration 012 MUST NOT seed or touch categories (planning anchor: Uncategorized == category_id NULL)"
    - "import_session_rows.source_payload JSON snapshot + photo_rel_path (document-dir staged) + import_sessions.phone_region captured at acceptance are the durability spine for IMP-04 resume and for correct canonical method normalization on resume"
    - "the non-mutexed *Core row writers are what let imported-contact-dao (plan 03) make contact-create + session-row-resolution ONE atomic transaction — the load-bearing fix for the resumable/no-duplicate-on-resume guarantee"
  prohibitions:
    - "Do not edit any already-shipped migration (001–011); migration 012 is forward-only and irreversible in production."
    - "Do not seed an 'Uncategorized' categories row; NULL category_id renders as no-category."
    - "Do not rebuild the contacts table; migration 012 is purely additive (two new tables)."
    - "Do not store or reference the picker session content:// URI in any column; only opaque external_contact_id text and app-owned document-dir staged photo paths."
    - "Do not add import_sessions/import_session_rows to the backup export manifest or restore table set; they are local-only runtime state."
---

<objective>
Ship migration 012 (the durable import-session schema), the session DAO/read chokepoint, and the canonical row state-transition contract that make an accepted picker result survive process death — the load-bearing durability spine for IMP-04 and the shared vocabulary every later plan writes against.

Purpose: Every later plan drives review/import from Orbit's own rows, never from the temporary picker grant. Without durable session tables, an atomic accept, a single-source-of-truth state-transition table, and non-mutexed row cores first, resume is impossible and the completion summary is uncomputable.
Output: `import_sessions` + `import_session_rows` at v12, a full session write DAO (mutexed writers + non-mutexed cores), a node-tested read chokepoint (raw counts + four summary buckets), and the documented transition table.

Schema (authoritative — additive only, no contacts rebuild):
- `import_sessions`: id, uid UNIQUE, mode CHECK('single','bulk'), status CHECK('pending','complete','discarded') DEFAULT 'pending', batch_category_id → categories(id) (NULL = Uncategorized), batch_tracking_enabled INTEGER DEFAULT 0 (bulk = Unbound), phone_region TEXT (durable effective phone region captured at acceptance for canonical normalization on resume), total_rows, created_at, modified_at.
- `import_session_rows`: id, uid UNIQUE, session_id → import_sessions(id) ON DELETE CASCADE, external_contact_id TEXT (opaque lookup key), source_payload TEXT (JSON: name, methods[], birthday), photo_rel_path TEXT nullable (app-owned DOCUMENT-DIR staged file — never a cache path), row_status CHECK('pending','imported','linked','skipped','failed','needs_review') DEFAULT 'pending', match_outcome TEXT nullable ('already_linked'|'probable'|'possible'|'new'|'needs_review'), matched_contact_id → contacts(id) ON DELETE SET NULL, contact_id → contacts(id) ON DELETE SET NULL, failure_reason TEXT, photo_failed INTEGER DEFAULT 0 CHECK(0,1), created_at, modified_at, UNIQUE(session_id, external_contact_id).
- Indexes: `idx_import_session_rows_session` ON (session_id, row_status); `idx_import_sessions_status` ON (status).

## Canonical row state-transition table (single source of truth — document verbatim in import-session-dao.ts)
| Event (who fires it) | row_status | match_outcome | contact_id |
|----------------------|-----------|---------------|-----------|
| accepted (acquire, plan 04) | pending | NULL | NULL |
| deterministic already-linked (driver, plan 06) | skipped | already_linked | NULL (matched_contact_id set) |
| import as new (importer, plans 06/07) | imported | new | set (ATOMIC w/ contact create) |
| link to existing (user, plans 07/11) | linked | probable\|possible | set (ATOMIC w/ link write) |
| ambiguous deferred (driver, plan 06) | needs_review | probable\|possible\|needs_review | NULL |
| user Skip (review, plan 07) | skipped | (prior outcome or NULL) — NEVER already_linked | NULL |
| import failure (driver, plan 06) | failed | (prior) | NULL |
| photo-only failure (plan 10) | (unchanged: stays imported) | (unchanged) | (unchanged) + photo_failed=1 |

The four completion-summary buckets derive from this table:
- imported = row_status IN ('imported','linked')  (photo_failed rows stay here — Cluster F)
- alreadyInOrbit = match_outcome='already_linked'
- needReview = row_status='needs_review'
- failedOrSkipped = row_status='failed' OR (row_status='skipped' AND (match_outcome IS NULL OR match_outcome <> 'already_linked'))
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/db/database.ts
@src/db/migrations/011-contact-lifecycle-schema.ts
@src/db/contacts-dao.ts
@src/db/transaction.ts
@src/db/types.ts
@src/backup/export-manifest.ts
</context>

## Review Feedback Incorporated
This plan is the origin of the shared cross-plan contract several review HIGH findings demanded. Specifically:
- **HIGH (Codex/Cursor/Claude) — count semantics collide across 01/06/07/08.** Fixed here by adding `sessionSummaryCounts` with `alreadyInOrbit` keyed on `match_outcome='already_linked'` (not `row_status`) and by publishing the canonical state-transition table (above) that plans 06/07/08 must write against. Claude's sharpening (no plan ever writes `match_outcome='already_linked'`) is closed by mandating the driver (plan 06) set it — this plan supplies the column + the summary query that reads it.
- **HIGH (Codex/Cursor/Claude) — non-atomic contact + session-row write.** Fixed here by exporting non-mutexed `*Core` row writers so plan 03 can resolve the session row inside the same `inWriteTransaction` as the contact create.
- **HIGH (Codex 19-01) — non-atomic snapshot / MEDIUM total_rows.** Fixed by `acceptImportSessionWithRows` (session + all rows + total_rows in one transaction).
- **MEDIUM (Codex 19-01) — discard scope vs task text conflict.** Reconciled: discard clears ALL rows without a contact_id (the DAO's `contact_id IS NULL` predicate is correct); the truth wording now matches the code and it returns staged-photo paths for post-commit cleanup (plan 09).
- **MEDIUM (Codex — backup policy) + MEDIUM (Cursor 19-01 count API).** Import-session tables declared local-only + a manifest-omission test; `sessionSummaryCounts` added.
- **MEDIUM (Codex 19-09) — multiple pending sessions unspecified.** `getResumableSession` now sweeps older pending sessions so at most one is resumable.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm the irreversible migration-012 schema before writing it</name>
  <decision>Approve the final v12 durable-session schema (import_sessions + import_session_rows, incl. the phone_region column and the row_status/match_outcome enumerations) before it is written — it is forward-only and can never be edited once shipped.</decision>
  <context>Migration 012 ships as application code and is irreversible in production (no remote DB access; any mistake is permanent for every user — CLAUDE.md data-layer rule). This is an owner-bucket risk decision, not an implementation detail. Surface specifically: (a) batch_category_id is nullable and NULL means Uncategorized (no categories seed — planning anchor); (b) the row_status and match_outcome enumerations and the canonical transition table in this plan's objective; (c) source_payload is a JSON snapshot, never a URI; (d) NEW vs the pre-review plan: a `phone_region TEXT` column is added so imported national-format numbers normalize to a canonical value consistently with manual create and survive resume (review MEDIUM — dropped phone region silently degrades duplicate matching).</context>
  <options>
    <option name="approve">Write migration 012 exactly as specified in this plan's objective (both tables incl. phone_region, columns, CHECKs, indexes; no contacts rebuild; no categories seed).</option>
    <option name="revise">Owner supplies column/constraint edits; Task 1 writes the amended schema instead.</option>
  </options>
  <reversibility rating="one-way">Migration 012 can never be edited once released; a later fix requires a new migration 013. Owner must confirm the durable shape.</reversibility>
  <resume-signal>Type "approved" to write the schema as specified, or describe the edits.</resume-signal>
  <done>Owner has approved the v12 schema (or supplied edits); the approved column set is what Task 1 writes.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 012 — import_sessions (incl. phone_region) + import_session_rows, registered and version-bumped</name>
  <files>src/db/migrations/012-import-sessions.ts, src/db/migrations/012-import-sessions.test.ts, src/db/database.ts, src/db/migrations/full-chain.test.ts</files>
  <read_first>src/db/migrations/011-contact-lifecycle-schema.ts (Migration shape: `export const migrationNNN: Migration = { version, async apply(exec, deps) }`; external_contact_links + contact_method_provenance shapes), src/db/database.ts (TARGET_VERSION and MIGRATIONS array at lines 46–61), src/db/types.ts (Migration/SqlExecutor/MigrationDeps), src/db/migrations/full-chain.test.ts (asserts TARGET_VERSION and per-version presence)</read_first>
  <action>Create `migration012` (version 12) that runs the additive DDL from the objective: CREATE TABLE import_sessions (with the `phone_region TEXT` column), CREATE TABLE import_session_rows, and the two indexes. It performs NO contacts rebuild and touches NO existing table. Import migration012 into src/db/database.ts, append it to the MIGRATIONS array (after migration011), and set `TARGET_VERSION = 12`. Update full-chain.test.ts to assert `TARGET_VERSION` toBe 12 and that exactly one migration has version 12. The migration test proves: v11→v12 creates both tables with the exact columns/CHECKs/indexes (including phone_region); a fixture row in contacts survives; PRAGMA foreign_key_check returns empty; inserting an import_session_rows row referencing a bad session_id fails the FK; the UNIQUE(session_id, external_contact_id) rejects a duplicate lookup key in one session; batch_category_id accepts NULL. Use `?`-bound params in every test insert.</action>
  <verify>
    <automated>npx vitest run src/db/migrations/012-import-sessions.test.ts src/db/migrations/full-chain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_VERSION = 12" src/db/database.ts` matches.
    - `grep -c "migration012" src/db/database.ts` >= 2 (import + array entry).
    - `grep -n "phone_region" src/db/migrations/012-import-sessions.ts` matches (the region column exists).
    - `npx vitest run src/db/migrations/012-import-sessions.test.ts` exits 0 and includes a passing "foreign_key_check empty after v11→v12" assertion.
    - Migration file contains no `ALTER TABLE contacts` and no `INSERT INTO categories`.
  </acceptance_criteria>
  <done>DB migrates cleanly to v12 with both durable tables (incl. phone_region) and their integrity constraints; the registered chain test is green at TARGET_VERSION 12.</done>
</task>

<task type="auto">
  <name>Task 2: import-session-dao — atomic accept, standalone writers, and non-mutexed row cores</name>
  <files>src/db/import-session-dao.ts, src/db/import-session-dao.test.ts</files>
  <read_first>src/db/contacts-dao.ts (inWriteTransaction usage + `changes===1` loud-failure guard idiom; the createContactFull/updateContactMetadataCore mutexed-writer-plus-non-mutexed-core pattern to mirror; the mutex is NON-REENTRANT — never nest), src/db/migrations/012-import-sessions.ts (column set), src/db/transaction.ts (non-reentrancy contract)</read_first>
  <action>Write the session write surface following the codebase's mutexed-writer + non-mutexed-core idiom (mirror createContactFull vs updateContactMetadataCore).

MUTEXED standalone writers (each opens exactly one `inWriteTransaction`):
- `createImportSession({uid, mode, batchCategoryId, batchTrackingEnabled, phoneRegion, now})` returns the session id.
- `insertSessionRow({sessionId, uid, externalContactId, sourcePayload, photoRelPath, now})` returns row id and bumps import_sessions.total_rows in the same transaction.
- `acceptImportSessionWithRows({session:{uid,mode,batchCategoryId,batchTrackingEnabled,phoneRegion,now}, rows:[{uid,externalContactId,sourcePayload,photoRelPath}]})` — creates the session AND every row AND sets total_rows to rows.length, ALL in ONE transaction; returns { sessionId, rowIds }. This is the atomic acceptance writer (review HIGH 19-01).
- `setRowMatchOutcome(rowId, outcome, matchedContactId, now)`, `setRowContact(rowId, contactId, status, now)` (status one of 'imported'|'linked'), `markRowStatus(rowId, status, failureReason, now)`, `markRowPhotoFailed(rowId, now)`, `completeSession(sessionId, now)`.
- `discardSession(sessionId, now)`: FIRST SELECT the photo_rel_path of every row whose contact_id IS NULL (into a list), THEN set status='discarded' and DELETE those rows (contact_id IS NULL — pending/needs_review/failed/user-skipped); rows with a contact_id (imported/linked) stay. Return the collected photo_rel_path list so the caller (plan 09) deletes the staged files AFTER commit.

NON-MUTEXED cores (assume an open transaction; take no mutex; used by plan 03's composed importer):
- `setRowContactCore(exec, rowId, contactId, status, now)`, `markRowStatusCore(exec, rowId, status, failureReason, now)`, `setRowMatchOutcomeCore(exec, rowId, outcome, matchedContactId, now)`, `markRowPhotoFailedCore(exec, rowId, now)`. Each is a `?`-bound UPDATE with a `changes===1` guard. The mutexed writers above are thin wrappers that `inWriteTransaction(exec, () => <core>)`.

Document the canonical (event → row_status, match_outcome) transition table (from this plan's objective) as a header comment — it is the single source of truth for plans 06/07/08/11. Every value `?`-bound; opaque externalContactId stored, never interpolated. Tests: run migrations to v12 then exercise each writer, asserting (a) acceptImportSessionWithRows is atomic (a forced failure on row N leaves zero session + zero rows), (b) discard preserves already-committed rows and returns the discarded rows' photo paths, (c) a *Core writer composes correctly inside a caller-opened transaction (call inWriteTransaction directly around two cores and assert both applied atomically).</action>
  <verify>
    <automated>npx vitest run src/db/import-session-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "acceptImportSessionWithRows writes session + all rows + total_rows atomically; a mid-insert failure persists nothing" passes.
    - Test "discardSession preserves rows with a contact_id, removes only contact_id-null rows, and returns their photo_rel_path list" passes.
    - Test "setRowContactCore + setRowMatchOutcomeCore compose inside one caller-opened transaction" passes.
    - `grep -n "setRowContactCore\|markRowStatusCore\|setRowMatchOutcomeCore\|markRowPhotoFailedCore" src/db/import-session-dao.ts` shows all four non-mutexed cores exported.
    - `npx vitest run src/db/import-session-dao.test.ts` exits 0.
  </acceptance_criteria>
  <done>The session write surface exists with an atomic accept, standalone mutexed writers, AND non-mutexed cores; discard clears only unresolved state and reports staged photos.</done>
</task>

<task type="auto">
  <name>Task 3: import-session-read — resume/count chokepoint (raw + summary buckets) + export-omission guard</name>
  <files>src/db/import-session-read.ts, src/db/import-session-read.test.ts</files>
  <read_first>src/db/import-session-dao.ts (row/session shape + the transition table), src/db/migrations/012-import-sessions.ts, src/backup/export-manifest.ts (the explicit exported-table set that must NOT include import-session tables)</read_first>
  <action>Write node-pure reads:
- `getResumableSession(exec, now)` returns the single most-recent session with status='pending' (or null). If more than one pending session exists, it discards the older ones (status='discarded' via the dao, in their own transactions) so exactly one is resumable (review MEDIUM 19-09). Because this mutates, it may open transactions for the sweep of stale sessions; the primary read stays a simple SELECT.
- `listSessionRows(exec, sessionId)` returns rows ordered by id (stable order — spec-less edge IMP-03/ordering assumption).
- `sessionRowCounts(exec, sessionId)` returns the six raw buckets `{ pending, imported, linked, needs_review, failed, skipped }` (kept for internal driver use).
- `sessionSummaryCounts(exec, sessionId)` returns the FOUR completion-summary buckets `{ imported, alreadyInOrbit, needReview, failedOrSkipped }` computed by a single grouped query per the derivations in this plan's objective — alreadyInOrbit keys off `match_outcome='already_linked'`, failedOrSkipped excludes already-linked skips.
No writes on the count reads. Tests: (1) a snapshot round-trip that simulates process death (build a session + rows via acceptImportSessionWithRows, close/re-open the same node:sqlite DB, assert getResumableSession + listSessionRows read the identical JSON payload, photo_rel_path, and phone_region back with no external re-query); (2) a mixed-fixture test proving sessionSummaryCounts separates a deterministic already-linked row (skipped + match_outcome='already_linked') from a user-Skip row (skipped + match_outcome NULL) — the former counts under alreadyInOrbit, the latter under failedOrSkipped; (3) getResumableSession discards an older pending session and returns only the newest; (4) an assertion that the backup export manifest's exported-table set (imported from export-manifest.ts) contains neither 'import_sessions' nor 'import_session_rows'.</action>
  <verify>
    <automated>npx vitest run src/db/import-session-read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "session snapshot (payload + photo_rel_path + phone_region) survives DB re-open (simulated process death)" passes.
    - Test "sessionSummaryCounts counts already-linked distinctly from user Skip" passes.
    - Test "getResumableSession keeps only the newest pending session" passes.
    - Test "export manifest omits import_sessions and import_session_rows" passes.
    - `npx vitest run src/db/import-session-read.test.ts` exits 0.
  </acceptance_criteria>
  <done>Resume enumerates one pending session and its four summary buckets purely from durable rows; import-session tables are proven excluded from export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker payload → SQLite | Untrusted external contact data (name/methods/birthday/lookup key/region) is snapshotted into import_session_rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | import_session_rows inserts | high | mitigate | Every value `?`-bound (SQL-injection canon control); external_contact_id stored as opaque TEXT, never interpolated into SQL |
| T-19-02 | Info disclosure | source_payload / photo_rel_path columns | medium | mitigate | Store only app-owned document-dir staged file paths and JSON field data; never the picker content:// URI or a provider path; tables excluded from export |
| T-19-05 | Tampering (data integrity) | migration 012 forward-only | high | mitigate | Additive migration, checkpoint:decision gate, foreign_key_check assertion, no edit to shipped migrations |
| T-19-20 | Repudiation (false report) | sessionSummaryCounts | medium | mitigate | Counts derive from the durable rows via the canonical transition table; alreadyInOrbit keys on match_outcome so it cannot be conflated with user Skip |
| T-19-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

## Artifacts this plan produces
- Migration `migration012` (version 12) → tables `import_sessions` (incl. `phone_region`), `import_session_rows`; indexes `idx_import_session_rows_session`, `idx_import_sessions_status`.
- `src/db/database.ts`: `TARGET_VERSION = 12`, `migration012` registered in `MIGRATIONS`.
- `src/db/import-session-dao.ts`: mutexed writers `createImportSession`, `insertSessionRow`, `acceptImportSessionWithRows`, `setRowMatchOutcome`, `setRowContact`, `markRowStatus`, `markRowPhotoFailed`, `completeSession`, `discardSession`; non-mutexed cores `setRowContactCore`, `markRowStatusCore`, `setRowMatchOutcomeCore`, `markRowPhotoFailedCore`; the documented state-transition table.
- `src/db/import-session-read.ts`: `getResumableSession`, `listSessionRows`, `sessionRowCounts`, `sessionSummaryCounts`.
- Tests: `012-import-sessions.test.ts`, `import-session-dao.test.ts`, `import-session-read.test.ts`; updated `full-chain.test.ts`.

<verification>
- `npx vitest run src/db/migrations/012-import-sessions.test.ts src/db/import-session-dao.test.ts src/db/import-session-read.test.ts src/db/migrations/full-chain.test.ts` all green.
- `npx tsc --noEmit --pretty false` passes; `npx biome check` on touched files passes.
</verification>

<success_criteria>
DB reaches v12 with the durable session tables (incl. phone_region); the session write DAO offers an atomic accept, standalone writers, and non-mutexed cores; the read chokepoint computes four summary buckets that separate already-linked from user Skip; and import-session tables are proven local-only (excluded from export).
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-01-SUMMARY.md` when done.
</output>
