---
phase: 19
slug: system-contact-import
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-28
reviewed_at: 2026-08-28
---

# Phase 19 — System Contact Import — UI Design Contract

> Visual and interaction contract for Phase 19's net-new import surfaces. Generated by
> gsd-ui-researcher, verified by gsd-ui-checker. This app is a mature React Native / Expo,
> Android-first, portrait-locked application (18 phases shipped). This spec fits the EXISTING
> `src/theme` token system and reuses shipped `src/components` / `src/screens` patterns — it invents
> no new design system. Every value below was read out of shipped code, not assumed.

---

## Scope of this contract

Phase 19's net-new user-facing surfaces (numbered per the orchestrator scope brief):

1. Entry points — a multi-option Add affordance on the dashboard, plus a "Contacts Integration" Settings section.
2. Single-contact import-review screen (reviewed before any write).
3. Bulk import setup (shared batch defaults; batch category override; **no** per-person checkbox list).
4. Bulk duplicate-review **card-grid workspace** — designed as a **reusable contract** (Phase 20 reuses it).
5. Confidence-ladder presentation (advisory outcomes; **no raw numeric scores**).
6. Single duplicate-resolution interrupt.
7. Chunked large-import progress screen.
8. Import completion summary/report screen (bridges to the Unbound-contacts screen).
9. Resume / Discard prompt for a durable resumable session.
10. Unsupported-platform state.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (React Native / Expo; shadcn is web-only and does not apply) |
| Preset | not applicable |
| Component library | none — bespoke RN primitives in `src/components`, all colours via `useTheme().colors.*` |
| Icon library | none — the app uses **Unicode text glyphs** (`+`, `◎`, `⚙`, `⋯`, `★`); do **not** introduce an icon package this phase |
| Font | system default (RN default font stack). The only bundled font is the Skia orrery `.ttf`, irrelevant here |
| Theme | single `space-dark` preset, **dark-first** (`theme-presets.ts` — the ONLY file allowed a colour literal) |
| Platform | Android-first, **portrait-locked** — design one phone-width column; no landscape, tablet, or web layout |

**Registry safety gate:** not applicable (no third-party UI registry). See Registry Safety below.

---

## Existing-system reuse map (read before planning any screen)

The planner/executor MUST reuse these shipped primitives rather than build new ones. Each was opened and confirmed on disk.

| Need in Phase 19 | Reuse this shipped asset | File |
|------------------|--------------------------|------|
| Contact tile / row (avatar + name + secondary line) | `ContactCard` (dashboard card) or the Unbound row idiom | `src/components/ContactCard.tsx`, `src/screens/UnboundContactsScreen.tsx` |
| Avatar with initials fallback + cache-bust | `Avatar` | `src/components/Avatar.tsx` |
| Two-segment / equal-width toggle (e.g. Bound / Unbound) | `SegmentedControl` (generic, filled-accent idiom) | `src/components/SegmentedControl.tsx` |
| Bulk-action sheet (grid multi-select actions) | `OverflowMenu`'s Modal-sheet-over-scrim pattern | `src/components/OverflowMenu.tsx` |
| Add-menu **speed-dial FAB** (owner-decided) | net-new animated FAB — Reanimated shared values only, no new library; the existing single `+` FAB is the visual base | `src/screens/HomeScreen.tsx` (FAB at 652–663) + Reanimated |
| Bound/Unbound + cadence-if-Bound + category + methods on the single-import review | The **exact** create-form block | `src/screens/CreateContactScreen.tsx` (lines ~243–349) |
| Cadence picker (Bound only) | `FrequencyPicker` | `src/components/FrequencyPicker.tsx` |
| Category select | `@react-native-picker/picker` in a `pickerShell` (as create form does) | `src/screens/CreateContactScreen.tsx` |
| Phone/email method selection + primary | `ContactMethodsEditor` | `src/components/ContactMethodsEditor.tsx` |
| Photo pipeline (Orbit-owned 512px master) | existing photo-storage chokepoint (Phase 5) | `src/db/photo-storage.*` |
| Completion-summary bridge target | navigate to the `UnboundContacts` route | `src/screens/UnboundContactsScreen.tsx` |
| Screen header (Back + title) | Unbound / Restore screen header idiom | `src/screens/UnboundContactsScreen.tsx` (lines 54–70) |

**Card-grid workspace (#4) is genuinely net-new** — no grid component exists (all lists are single-column `FlatList`). Build it as a shared, route-reusable component (`FlatList numColumns={2}`) so Phase 20 consumes the same contract. Do not fork a Phase-20-specific copy.

---

## ⚠ Code-grounded corrections the planner must honor

1. **There is no multi-option FAB today.** `HomeScreen.tsx` (lines 652–663) renders a **single** `+` FAB whose `onPress` is `navigation.navigate("Create")` — it goes straight to create with no menu. Dossier Cluster A's phrasing "the dashboard already uses a multi-option FAB" is **not** true against shipped code. Phase 19 therefore **adds** the multi-option behavior. **[OWNER-DECIDED 2026-08-28: speed-dial FAB.]** Tapping `+` expands into stacked mini-FABs that animate outward (`Import from Contacts` / `Create manually`) and the `+` glyph rotates to a `×`/close affordance; tapping the scrim or `×` collapses it. Animate with **Reanimated shared values only** (already an app dependency for gestures) — **never** drive the expand/collapse from React state (per CLAUDE.md's animation rule). No new FAB/animation library is added. Portrait single column; each mini-FAB is ≥44px. (This supersedes the earlier action-sheet recommendation.)
2. **No central spacing or typography token module exists.** Spacing and font sizes are inline per-`StyleSheet`. The scales below are the **de-facto shipped** values distilled from real screens — match them; do not introduce a divergent scale.
3. **No dedicated "caution/warning" colour token exists.** `rogue` (amber) and `statusWobble` (gold) are *orbit-status* tokens, `danger` (red) is *destructive-action only*. The confidence ladder must NOT borrow any of these to color-code match outcomes (that would conflate identity confidence with orbit status / destruction). See Color + Confidence Ladder below.

---

## Spacing Scale

De-facto scale distilled from shipped screens (`HomeScreen`, `ContactCard`, `UnboundContactsScreen`, `CreateContactScreen`). Canonical steps are multiples of 4; the shipped exceptions are documented, not to be "cleaned up" this phase.

| Token (conceptual) | Value | Usage (as shipped) |
|-------|-------|-------|
| xs | 4px | Micro gaps in a text stack (`rowText` gap, trailing gap) |
| sm | 8px | Compact control/list gaps (`sortControl` gap, list `gap: 8`) |
| md | 12px | Card/row inner padding, avatar↔text gap (`card padding: 12`, `gap: 12`) |
| lg | 16px | Screen padding, section spacing (`root padding: 16`, `content padding: 16`) |
| xl | 24px | Screen title size / large section breaks / empty-state top margin (`marginTop: 24`) |
| 2xl | 32px | Reserved for major layout breaks (FAB glyph size) |

**Documented shipped exceptions (allowed, do not refactor):** `2` (name↔fuel micro gap), `10` (dashboard list row gap, card/input `borderRadius`), `14` (footer/row vertical padding, sheet-option padding), `28` (FAB bottom offset / circular radius). Corner radius convention: **10** on cards/inputs/chips/rows, **12** on modal sheets, circular for the FAB/avatars.

**Touch targets:** every interactive element is **≥44px** in the tapped dimension (`minHeight: 44` / `minWidth: 44`). This is non-negotiable and already universal in shipped code — the card grid's cards, bulk-action buttons, and resume/discard buttons all inherit it.

---

## Typography

De-facto roles distilled from shipped `StyleSheet`s. No custom font; system default. Only these sizes/weights appear in the app — stay within them.

| Role | Size | Weight | Line Height | Shipped precedent |
|------|------|--------|-------------|-------------------|
| Screen title | 24px | 600 | default | `UnboundContactsScreen.title` |
| Section / empty heading | 18px | 700 | default | `HomeScreen.emptyHeading` (600 also used on Unbound — prefer 700 for emphasis) |
| Row title / primary CTA / segment | 16px | 600 | default | `footerText`, `primaryCtaText`, `SegmentedControl.label`, `rowName` |
| Body | 15px | 400 | 21px (~1.4) | `emptyBody`, `create-form helper` |
| Label / secondary / count | 13px | 600 | default | `countHeader`, `count`, `unboundLabel`, `categoryText` |

Notes: contact **name** on a card is `15px / 400` (intentionally light — see `ContactCard.name`); interactive control labels are `600`; headings `700`. Do not introduce new sizes (e.g. 20, 28) — they do not exist in this app.

---

## Color

All values are `space-dark` dark-palette tokens (`theme-presets.ts`). Consume via `useTheme().colors.*`. **Never** write a hex literal in a screen/component/Skia call (`check:colors` gate + CLAUDE.md).

| Role | Token → value | Usage |
|------|---------------|-------|
| Dominant (60%) | `background` `#0B0E1A` | Screen roots, FAB glyph-on-accent text, active-segment label text |
| Secondary (30%) | `surface` `#141828`, `surfaceElevated` `#1D2235` | Cards, rows, inputs, picker shells (`surface`); modal sheets + category chips (`surfaceElevated`) |
| Accent (10%) | `accent` `#6C8CFF` | See reserved list below |
| Destructive | `danger` `#E5484D` | Destructive-action emphasis ONLY (see Copywriting: Discard) |
| Borders | `border` `#2A3048`, `borderStrong` `#3C4568` | Default 1px borders; `borderStrong` on active/selected outlines |
| Text | `textPrimary` `#E6E9F5`, `textSecondary` `#8B93B0` | Primary content; labels/helpers/secondary lines |

**Accent (`#6C8CFF`) is reserved for, and ONLY for:**
- primary CTA fills (`Import`, `Resume import`, `Link to Existing` when it is the recommended primary, `Done`),
- the selected/active state of a `SegmentedControl` or choice-pair (e.g. Bound/Unbound, batch category chip),
- the favourite star (existing `★`),
- pressed-state link text (`Your week` / `Backup`-style text links),
- the pull-to-refresh / progress spinner tint.

Accent is **never** applied to every interactive element, never to advisory/ambiguous match outcomes, and never to the overflow `⋯` trigger.

**Confidence-ladder colour rule (locked):** match outcomes are **advisory**, not identity or destruction, so they are rendered **without a semantic hue** — a `surfaceElevated` chip with a leading `13px/600 textSecondary` outcome word (see Confidence Ladder). Do NOT color-code outcomes with `danger`, `rogue`, `statusWobble`, or `accent`. If the owner later wants a caution hue for "Manual review required", that is a **new token addition** and an owner decision — flag it, do not inline a hex.

---

## Confidence Ladder — user-facing presentation (Cluster H)

Five outcomes map from the internal weighted evidence model. **Raw scores are never shown.** Each outcome renders as a labeled chip (`surfaceElevated` bg, `textSecondary` label, radius 10) plus an advisory recommendation line (`15px/400 textSecondary`). Colour carries no outcome meaning.

| Internal outcome | User-facing label | Advisory recommendation copy | Default action offered |
|------------------|-------------------|------------------------------|------------------------|
| deterministic / already linked | `Already in Orbit` | "Already linked to this contact." | (skip in bulk; not offered as new) |
| probable match | `Probable match` | "Looks like someone you already have." | Recommend **Link to Existing** |
| possible match | `Possible match` | "Might be someone you already have." | Recommend **Review** (no auto-pick) |
| no meaningful match | `New person` | "No match found — import as new." | Recommend **Import as New** |
| manual review required | `Needs review` | "Needs a closer look before linking." | **Manual Review Required** (never auto-accepted in bulk) |

Locked behavior: recommendations are advisory (never an automatic identity decision); `Needs review` candidates are **excluded** from any "apply recommendation in bulk" action.

---

## Screen-by-screen interaction contract

### 1. Entry points
- **Dashboard `+` (speed-dial FAB — owner-decided 2026-08-28):** tapping `+` expands into two stacked mini-FABs animating outward — `Import from Contacts` → import entry; `Create manually` → `Create` route — with the `+` rotating to a `×` close affordance and a tap-scrim to collapse. (Replaces today's direct-to-Create navigation.) Animation via Reanimated shared values only, never React state; no new library.
- **Settings:** add a `Contacts Integration` section (reuse `settings.section` / `sectionHeading` idiom, `13px/600 textSecondary` heading) with one row now: `Import contacts`. Phase 20 maintenance rows land in this same section later — leave room, add nothing else this phase.

### 2. Single-contact import-review screen
- Header idiom (Back + `24px/600` title "Import contact"). Reuses the create-form field stack **verbatim in order**: name (editable; required if source lacks a usable name — Cluster O), Orbit participation (Bound/Unbound choice-pair), Category (Picker; defaults to Uncategorized), Frequency (`FrequencyPicker`, shown only when Bound), method selection + primary (`ContactMethodsEditor`), birthday, photo preview.
- Primary CTA: `Import` (accent fill). Nothing is written until this tap (Cluster D).
- If a strong single candidate exists, this screen defers to the duplicate interrupt (#6) before create.

### 3. Bulk import setup
- After the system picker returns N selections, show a lightweight setup — **not** a per-person list. Show: selection count ("N contacts selected"), the two locked defaults as read-only affordances (`Unbound`, `Uncategorized`), and a single batch **Category override** control (Picker or a chip set in the accent-selected idiom). One primary CTA: `Import N contacts`.
- Explicitly **no** per-person checkbox/exclusion list (Cluster E).

### 4. Bulk duplicate-review card grid (REUSABLE — Phase 20 too)
- `FlatList numColumns={2}`, single portrait column of pairs. Each candidate **card**: `Avatar` + name (`15px`) + the confidence-ladder chip + a one-line evidence hint (`13px textSecondary`). Card is `surface` bg, `border`, radius 10, `≥44` tap height.
- **Tap** a card → detail inspector (candidate + its Orbit match evidence, still no raw score). **Long-press** → enter multi-select mode (selected cards get a `borderStrong`/accent outline, matching the shipped selected-idiom). In multi-select, a bottom action bar offers `Link to Existing`, `Import as New`, `Skip`, and `Apply recommendation` (the last excludes `Needs review` cards).
- Ambiguous candidates never block: safe imports proceed; unresolved cards remain in the grid.
- Build as a standalone component with a generic item/action contract so Phase 20's reconciliation reuses it unchanged.

### 5. (covered by Confidence Ladder above)

### 6. Single duplicate-resolution interrupt
- One strong candidate: a focused card showing incoming vs. existing, two buttons — `Link to Existing` (recommended → accent), `Import as New` (secondary → `surface`/`border`).
- Multiple credible candidates: list the candidate cards; actions `Choose this one` (per card), `Import as New`, `Skip`. Orbit does not pre-pick (Cluster I).

### 7. Chunked large-import progress screen
- One logical import with overall progress. A determinate progress line/bar (accent tint) + `"Importing… X of N"` (`15px`), plus an incremental sub-status ("Photos may finish after contacts"). No spinner-only screen. Never expose chunk internals as separate imports (Cluster S).

### 8. Import completion summary
- Header "Import complete". Four count rows (reuse `footerEntry` idiom): `Imported (n)`, `Already in Orbit (n)`, `Need review (n)`, `Failed / skipped (n)`. Actions: `Review possible matches` (→ card grid, shown only when need-review > 0), `View Unbound contacts` (→ `UnboundContacts` route — the locked bridge), `Done` (accent). 

### 9. Resume / Discard prompt
- On app return with a durable pending session, present a non-dismissable-by-tap-outside prompt (Modal sheet idiom): heading "Resume your import?", body explaining unresolved items are saved. Buttons: `Resume import` (accent), `Discard import` (**danger** — this discards pending review state; already-committed contacts stay committed, state this in the body).

### 10. Unsupported-platform state
- Where import would launch, show the calm empty/disabled state: heading `Contact import unavailable`, body `Contact import requires Android 17 or later.` (verbatim, Cluster B). The rest of Orbit stays fully usable; do not disable the Add flow's `Create manually` row.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Add-menu row (import) | `Import from Contacts` |
| Add-menu row (manual) | `Create manually` |
| Settings section heading | `Contacts Integration` |
| Settings row | `Import contacts` |
| Single-review primary CTA | `Import` |
| Bulk setup primary CTA | `Import N contacts` (interpolate the count) |
| Progress line | `Importing… X of N` |
| Completion heading | `Import complete` |
| Completion count rows | `Imported (n)` · `Already in Orbit (n)` · `Need review (n)` · `Failed / skipped (n)` |
| Completion actions | `Review possible matches` · `View Unbound contacts` · `Done` |
| Resume prompt heading | `Resume your import?` |
| Resume prompt actions | `Resume import` · `Discard import` |
| Empty state heading (unsupported) | `Contact import unavailable` |
| Empty state body (unsupported) | `Contact import requires Android 17 or later.` |
| Empty state (grid, nothing to review) | heading `Nothing to review` / body `All picked contacts were imported.` |
| Error state (import batch) | `Some contacts couldn't be imported.` + a `Failed / skipped (n)` count with a `Retry` affordance on the summary; safe contacts remain imported. |
| Error state (name required, Cluster O) | `This contact needs a name before you can import it.` |
| Duplicate interrupt (single) | `We found someone who might already be in Orbit.` |
| Confidence advisory lines | per the Confidence Ladder table |
| Destructive confirmation — Discard import | `Discard import`: "Unresolved items will be discarded. Contacts already imported stay in Orbit." |
| Destructive confirmation — leave mid-review | `Leave import?`: "You've made changes. Leaving keeps anything already imported and discards the rest." (only after meaningful review progress — Cluster Q) |

Voice: calm, plain, non-nagging (consistent with shipped Unbound/empty-state copy). No exclamation marks, no gamified counts.

---

## UI Considerations

State coverage for the net-new import surfaces, computed by the `ui-consideration-probe` engine over
the 11 described surfaces (E1 Add-menu FAB · E2 Settings section · E3 single-review · E4 bulk setup ·
E5 card-grid workspace · E6 confidence chip · E7 duplicate interrupt · E8 progress · E9 completion
summary · E10 resume/discard · E11 unsupported platform). Empty/error COPY lives in the Copywriting
Contract above; this section covers shape-rooted STATE coverage and references those rows rather than
restating them.

**Probe result:** 69 applicable considerations across 9 state categories. **Resolved:** 62 explicit
(the spec states a concrete truth) · 6 backstop (needs a held-out UI-state test at implementation) ·
1 unresolved (planner assumption). Resolution is per-category below; dismissals are noted with reason.

| Category | Elements | Status | Resolution / Reason |
|----------|----------|--------|---------------------|
| **empty** | E5 grid · E9 summary · E11 platform | ✅ explicit | Grid → `Nothing to review` / `All picked contacts were imported.`; summary renders `(0)` counts; unsupported → locked `Contact import requires Android 17 or later.` (Copywriting). Menus (E1/E2) and the conditional resume prompt (E10) have no data-empty state — dismissed (static action lists / shown only when a session exists). |
| **loading** | E8 progress · E5 grid | ✅ explicit (E8) · 🧪 backstop (E5) | E8 progress screen **is** the loading surface: determinate `Importing… X of N`, never a bare spinner. E3 fields populate synchronously from the picker result (explicit). **Backstop:** while duplicate scoring is in flight, the E5 grid must show a determinate/in-progress state, not a frozen blank. Menus (E1/E2) load nothing — dismissed. |
| **error** | E3 name · E9 batch · E5 card action · E10 session | ✅ explicit (E3,E9) · 🧪 backstop (E5,E10) | E3 → name-required copy blocks import (Cluster O); E9 → `Failed / skipped (n)` + `Retry`, safe contacts stay committed (Cluster P). **Backstop:** a single E5 card action (e.g. link write) failing surfaces per-card without aborting the batch; an unreadable/corrupt E10 session offers `Discard` rather than crashing. |
| **populated** | E1–E10 | ✅ explicit | Happy-path layout fully specified in the Screen-by-screen contract at typical volume. |
| **partial** | E3 sparse source · E5 partly-resolved grid · E9 partial counts · E10 session | ✅ explicit | Sparse source imports (name required); unresolved grid cards remain while safe imports proceed (Cluster L); partial completion counts render; resumable session survives to Resume/Discard (Cluster R). |
| **long-text** | E5/E7 card names · single-column screens | ✅ explicit · 🧪 backstop (E5) | Single-column screens wrap/ellipsize per shipped idiom (explicit). **Backstop:** confirm a long name + many methods hold the 2-up card width in portrait (`numberOfLines={1}` + ellipsize, ContactCard precedent). |
| **overflow** | E5 grid at thousands · E7 many candidates | ✅ explicit (E7) · ⚠ unresolved (E5) | E7 candidate list scrolls; E9 fixed 4-row summary can't overflow — dismissed. ⚠ **Unresolved — planner must treat as assumption:** `FlatList` is virtualized by default, but the paging/window strategy and whether the on-screen review batch is capped for a multi-thousand review set is a **planner** decision (delegated; not an owner call). Chunked *processing* is already decided (Cluster S). |
| **zero-one-many** | E7 candidates (0/1/many) · E4/E9 counts | 🧪 backstop | E7: 0 → straight to review; 1 strong → Link/Import-as-New interrupt; many → choose-one/import-new/skip (Cluster I). **Backstop:** verify each branch renders and that E4 `Import N contacts` / E9 count rows pluralize (`1 contact` vs `N contacts`). |
| **unclassified** | E6 confidence chip | ✅ explicit | Classified on review: E6 is a **display badge** with 5 enumerated variants (`Already in Orbit`/`Probable match`/`Possible match`/`New person`/`Needs review`), colourless (owner-confirmed no caution hue), never a raw numeric score. Not a gap. |

**Backstop inventory (6) — held-out UI-state tests the executor/verifier must wire:** (1) E5 grid shows progress, not a frozen blank, while scoring runs; (2) per-card E5 action failure isolates without aborting the batch; (3) unreadable E10 session → Discard path, no crash; (4) long name/many methods hold the 2-up grid card in portrait; (5) E7 0/1/many branches each render; (6) singular/plural count copy (E4/E9).

**Unresolved (1) — planner assumption:** E5 grid virtualization/paging strategy at thousands of candidates.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (React Native app; no shadcn/registry) | none | not applicable |

No third-party UI registry, block, or component-generator is used or introduced. All UI is bespoke RN composed from existing `src/components`.

---

## Open dependencies — RESOLVED by owner (2026-08-28)

All three were owner-bucket calls (risk/scope/product/taste). Decided in one batched round after both reviews:

1. **iOS picker scope conflict → RESOLVED: Android-only for Phase 19.** Dossier 19 Cluster B's `[DECIDED]` "iOS uses the native privacy-preserving contact picker" is **deferred**, consistent with the app-wide Android-first posture (PROJECT.md / REQUIREMENTS.md line 200 / HANDOFF §11 iOS deferral). The dossier's iOS decision is honored later, when iOS is un-deferred — it is **not reversed**, only sequenced after v1. Phase 19 designs and plans **Android-first only**: no iOS entry-point variant, no iOS unsupported-state copy, and the picker layer targets the Android 17+ Contact Picker exclusively. Planner: do not build a cross-platform picker abstraction this phase.
2. **Add-menu introduction → RESOLVED: speed-dial FAB.** The `+` FAB (today a direct-to-Create navigation) becomes an expanding speed-dial (`Import from Contacts` / `Create manually`), Reanimated shared values only, no new library. See Code-grounded corrections #1 and Screen contract #1. (The earlier action-sheet recommendation is superseded.)
3. **Caution hue for "Needs review" → RESOLVED: no hue.** The advisory confidence ladder stays **colourless** per the locked Color rule — neutral `surfaceElevated` chips, no borrowing of `rogue`/`statusWobble`/`danger`/`accent`, and **no new caution token** is added this phase. Advisory match-confidence remains visually distinct from orbit-status and destructive semantics.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS (FLAG — single-review CTA `Import` is nounless; non-blocking, header supplies the noun)
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS (FLAG — 5 shipped sizes exceed the 4-size guideline; pre-existing, app-wide cleanup not this phase)
- [x] Dimension 5 Spacing: PASS (FLAG — shipped exceptions 2/10/14/28 are not multiples of 4; pre-existing, do not refactor this phase)
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED 2026-08-28 — 6/6 dimensions pass, 3 non-blocking FLAGs (all pre-existing shipped-system conditions, owner-aware). Load-bearing code claims verified against disk by the checker (no multi-option FAB today; no caution token; all 8 referenced primitives exist). Three open dependencies resolved by owner (see above).
