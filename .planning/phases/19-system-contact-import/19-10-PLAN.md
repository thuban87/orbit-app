---
phase: 19-system-contact-import
plan: 10
type: execute
wave: 5
depends_on: [19-03, 19-04, 19-06]
files_modified:
  - src/services/import/import-photo.ts
  - src/services/import/import-photo.test.ts
  - src/services/import/import-acquire.ts
  - src/services/import/import-driver.ts
autonomous: true
requirements: [IMP-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Imported contact photos become Orbit-owned 512px local masters via the existing photo pipeline (persistMaster/contactPhotoRelPath + setContactPhoto), persisted POST-COMMIT so a photo failure never rolls back or invalidates the contact (IMP-02, Cluster F)."
    - "The photo bytes are read from the durable DOCUMENT-DIR staged path in import_session_rows.photo_rel_path (staged at acceptance by plan 04), NEVER from the OS-evictable picker cache or the expired grant (IMP-04; addresses review HIGH photo-staging durability)."
    - "A photo decode/resize/copy failure marks the row photo_failed and leaves the contact imported (counted under Imported, not Failed); the batch continues (Cluster F/P)."
    - "Imported birthdays are already written inside importContactRecord's transaction in the app's shape (MM-DD year-unknown / YYYY-MM-DD), validated by the exported isValidStoredBirthday gate (02-30 → not stored) — plan 03 owns that write; this plan only confirms it end-to-end on device and never re-implements it (IMP-02, Pitfall 6; addresses review HIGH — a strict birthday validator now exists as an exported symbol)."
    - "[ASSUMPTION | A1/A2 device-verified] Whether the Android 17 picker actually returns birthday/photo is confirmed on-device; when it does not, contacts import without an avatar (initials fallback) or birthday, and the import stays valid (best-effort, guaranteed fields already proven in the tracer)."
  artifacts:
    - "src/services/import/import-photo.ts — persistImportedPhotoPostCommit(): DOCUMENT-DIR staged file → 512px master → setContactPhoto, idempotent, failure-isolating"
    - "post-commit photo hook wired into single (inside commitSingleImport in import-acquire.ts — ImportReviewScreen.tsx NOT touched) and bulk (import-driver) import paths"
  key_links:
    - "persistImportedPhotoPostCommit runs AFTER the contact transaction commits, rebuilding the filename from contactId (mirror the purge-cleanup post-commit idiom); a throw is caught and recorded, never propagated into the write transaction"
    - "the staged photo bytes come from the durable session row's document-dir photo_rel_path (staged at acceptance, plan 04), not the expired picker grant or cache; birthday is already applied by plan 03's importContactRecord"
  prohibitions:
    - "Do not persist the photo inside the contact transaction; photo is post-commit and independent (Cluster F)."
    - "Do not fail or roll back a contact because its photo failed; mark photo_failed and continue."
    - "Do not hand-roll decode/resize; reuse persistCroppedMaster/persistMaster + contactPhotoRelPath."
    - "Do not use toISOString().split() for the birthday; reuse buildBirthdayForStorage + the strict parser."
---

<objective>
Add the photo and birthday expansion to the proven import path: Orbit-owned photo masters persisted post-commit (failure-isolated) and birthdays stored in the app's canonical shape, verified on an Android 17 device.

Purpose: The owner sequenced photo/birthday AFTER the verified single-contact tracer because the Android 17 picker's granular birthday/photo support is unproven (A1/A2). This plan wires them best-effort into both single and bulk paths, keeping the Cluster F guarantee that a photo failure never invalidates a contact.
Output: import-photo post-commit service + birthday write-through, wired into single + bulk, node-tested for the failure-isolation contract and device-verified for actual picker support.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/services/photos/photo-storage.ts
@src/db/contacts-dao.ts
@src/db/imported-contact-dao.ts
@src/services/import/import-acquire.ts
@src/services/import/import-driver.ts
@src/logic/picked-contact-map.ts
@src/screens/edit-contact-logic.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor) — cache-staging durability.** This plan now explicitly reads the DOCUMENT-DIR staged path (`photo_rel_path`), which plan 04 populates at acceptance — never the cache/grant.
- **HIGH (Codex) — assumed a birthday validator that didn't exist.** Plan 03 now exports `isValidStoredBirthday` and writes the validated birthday inside `importContactRecord`; this plan no longer edits `imported-contact-dao.ts` for birthday (removed from files_modified) and only device-confirms the end-to-end behavior.
- **MEDIUM (Codex/Cursor) — when are staged files deleted.** Success: this plan leaves the staged file for plan 09's discard/orphan sweep to reconcile; it does not delete a live row's staged photo (Retry may need it). The staged file for a committed contact becomes an orphan reconciled by plan 09.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: import-photo — post-commit Orbit-owned master, failure-isolated</name>
  <files>src/services/import/import-photo.ts, src/services/import/import-photo.test.ts</files>
  <behavior>
    - given a committed contactId + a staged photo temp file → produces a 512px master and setContactPhoto(contactId, relPath)
    - a decode/resize/copy throw → returns { ok:false } and does NOT throw to the caller; caller marks photo_failed
    - missing/null staged photo → no-op { ok:true, skipped:true }
    - idempotent: rebuilding the filename from contactId is stable across retries
  </behavior>
  <read_first>src/services/photos/photo-storage.ts (persistCroppedMaster/persistMaster crash-safe swap lines 83–118, 227–262; contactPhotoRelPath line 83), src/db/contacts-dao.ts (setContactPhoto lines 605–629), .planning/phases/19-system-contact-import/19-RESEARCH.md (Pitfall 3 post-commit photo; buildPhotoPurgeCleanup idiom)</read_first>
  <action>Write `persistImportedPhotoPostCommit(exec, fs, { contactId, stagedPhotoPath, now })` returning `{ ok, skipped? }`. Resize the staged temp file to a 512px JPEG master via the existing persistCroppedMaster/persistMaster pipeline, then setContactPhoto(contactId, contactPhotoRelPath(contactId), now). Wrap the whole thing in try/catch so any failure returns `{ ok:false }` without throwing. A null/missing stagedPhotoPath is a `{ ok:true, skipped:true }` no-op. Rebuild the filename from contactId (idempotent across retries). This function is ONLY ever called after the contact transaction has committed.</action>
  <verify>
    <automated>npx vitest run src/services/import/import-photo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "photo failure returns ok:false and does not throw" passes; the caller can mark photo_failed.
    - Test "null staged photo is a skipped no-op" passes.
    - `grep -n "inWriteTransaction\|BEGIN" src/services/import/import-photo.ts` shows no transaction wrapping the resize (photo work is outside/after the contact txn).
    - `npx vitest run src/services/import/import-photo.test.ts` exits 0.
  </acceptance_criteria>
  <done>An idempotent, failure-isolating post-commit photo persister exists, never throwing into the contact write.</done>
</task>

<task type="auto">
  <name>Task 2: Wire photo post-commit into single + bulk paths (birthday already handled by plan 03)</name>
  <files>src/services/import/import-acquire.ts, src/services/import/import-driver.ts</files>
  <read_first>src/services/import/import-photo.ts (Task 1), src/services/import/import-driver.ts (per-row loop + markRowPhotoFailed — plan 06), src/services/import/import-acquire.ts (commitSingleImport, the single create seam — plan 04; enrich its body here, do NOT change its signature), src/db/imported-contact-dao.ts (importContactRecord already writes the validated birthday inside its transaction — plan 03; READ ONLY, do not edit), src/db/import-session-read.ts (reading the row's document-dir photo_rel_path by sessionId)</read_first>
  <action>Birthday is ALREADY written inside importContactRecord's transaction by plan 03 (validated via isValidStoredBirthday) — do NOT re-implement it and do NOT edit imported-contact-dao.ts here. This task wires ONLY the post-commit photo. In the bulk driver's per-row loop, after a successful importContactRecord commit, call persistImportedPhotoPostCommit reading the row's DOCUMENT-DIR photo_rel_path; on `{ ok:false }` call markRowPhotoFailed(row) and keep the row 'imported' (Cluster F). For the single path, add the identical post-commit photo step INSIDE `commitSingleImport` in import-acquire.ts (the create seam plan 04 established) — enrich its BODY only, keeping its signature `commitSingleImport(exec, { sessionId, rowId, input, externalLinks, birthday, now })` unchanged so plan 07's call site is untouched. Do NOT edit ImportReviewScreen.tsx: that file is owned by plan 07 in the same wave, so touching it here would be a merge race; the screen already reaches the photo step transitively by calling commitSingleImport. The staged photo path for the single path is read from the session row (via sessionId) inside commitSingleImport, so no new parameter is needed. Staged photo bytes come from the durable document-dir photo_rel_path (plan 04 staging), never the cache. Do not change the transaction boundaries of importContactRecord; photo persistence stays strictly post-commit.</action>
  <verify>
    <automated>npx vitest run src/services/import/import-driver.test.ts src/services/import/import-photo.test.ts</automated>
    <human-check>Android 17 device: import a contact WITH a photo → the Orbit contact shows the imported avatar; simulate a bad photo (or a contact without one) → the contact still imports (initials fallback), row marked photo_failed only when a photo existed but failed. Confirm an imported birthday (written by plan 03) shows correctly with no off-by-one.</human-check>
  </verify>
  <acceptance_criteria>
    - The single-path photo step lives inside commitSingleImport in import-acquire.ts; this plan does NOT modify src/screens/ImportReviewScreen.tsx or src/db/imported-contact-dao.ts (neither is in files_modified — no same-wave collision with plan 07, no re-edit of plan 03's birthday write). `grep -n "persistImportedPhotoPostCommit" src/services/import/import-acquire.ts` matches; the plan's files_modified contains no `*Screen.tsx` and no `imported-contact-dao.ts`.
    - The photo step reads the row's document-dir photo_rel_path; `grep -rn "cacheDir\|cache/" src/services/import/import-photo.ts` returns nothing (bytes come from document-dir staging).
    - The bulk driver calls persistImportedPhotoPostCommit after commit and markRowPhotoFailed on failure, keeping the row imported.
    - A photo-failed row is counted under Imported (verified against plan 08 counts) not Failed.
    - `grep -rn "toISOString" src/services/import/` returns nothing.
    - `npx vitest run src/services/import/import-driver.test.ts src/services/import/import-photo.test.ts` exits 0.
  </acceptance_criteria>
  <done>Photos import as Orbit-owned masters post-commit and birthdays store in the app's shape, both best-effort and failure-isolated, across single and bulk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged photo bytes → Orbit-owned master | Untrusted image is resized in app-private storage before it becomes a contact asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-06 | Tampering/DoS | malicious/oversized photo blob | high | mitigate | Resize through the existing expo-image-manipulator pipeline caps; failure caught post-commit, never aborts the contact (Cluster F, V12) |
| T-19-07 | Tampering (data integrity) | birthday off-by-one | medium | mitigate | buildBirthdayForStorage + strict parser; no toISOString() (Pitfall 6, V5) |
| T-19-02 | Info disclosure | staged photo file | medium | mitigate | App-private staging; swept for discarded/failed sessions (plan 09) (V8) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs (expo-image-manipulator already a dependency) |
</threat_model>

## Artifacts this plan produces

- `src/services/import/import-photo.ts`: `persistImportedPhotoPostCommit()` (reads document-dir staged path).
- Post-commit photo wiring in `import-acquire.ts` (single, inside commitSingleImport) + `import-driver.ts` (bulk).
- Test: `import-photo.test.ts`.
- (Birthday write-through is NOT produced here — it is owned by plan 03's importContactRecord.)

<verification>
- `npx vitest run src/services/import/import-photo.test.ts src/services/import/import-driver.test.ts src/db/imported-contact-dao.test.ts` green.
- `npx tsc --noEmit --pretty false`, `npx biome check` pass; `grep -rn toISOString src/services/import/` empty.
- Device (Android 17): photo imports when the picker returns one; contact imports without a photo/birthday when it does not (A1/A2 outcome recorded).
</verification>

<success_criteria>
Photos and birthdays import best-effort into both single and bulk paths — photos as post-commit Orbit-owned masters that never invalidate a contact, birthdays in the app's canonical shape — verified on an Android 17 device.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-10-SUMMARY.md` when done.
</output>
