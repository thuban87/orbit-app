---
phase: 19-system-contact-import
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - src/services/import/import-acquire.ts
  - src/services/import/import-acquire.test.ts
  - src/screens/ImportReviewScreen.tsx
  - src/screens/use-import-leave-guard.ts
  - src/components/AddSpeedDialFab.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [IMP-01, IMP-02, IMP-04]
user_setup:
  - service: android-17-device
    why: "The single-contact happy path exercises the real Android 17 Contact Picker; full end-to-end UAT needs an Android 17 device"
    dashboard_config:
      - task: "Confirm the Pixel is on Android 17 (else only the unsupported-state path is verifiable on a pre-17 device)"
        location: "Device: Settings ▸ About ▸ Android version"
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TRACER: from the dashboard speed-dial FAB, tapping 'Import from Contacts' launches the picker, and picking ONE contact opens ImportReviewScreen with name + methods pre-filled from a durable session snapshot; nothing is written until Import is tapped, then a new Unbound contact with normalized methods appears (and is visible in the Unbound contacts list) (IMP-01/IMP-02/IMP-04)."
    - "The speed-dial FAB expands the single '+' into 'Import from Contacts' / 'Create manually' and rotates '+'→'×', animated with Reanimated shared values only (never React state); tapping the scrim or '×' collapses it."
    - "Both entry points (dashboard FAB and Settings ▸ Contacts Integration ▸ Import contacts) gate on isContactPickerAvailable(): on SDK<37 the import affordance shows the unsupported state 'Contact import requires Android 17 or later.' and the rest of Orbit — including 'Create manually' — stays usable (IMP-01)."
    - "Cancelling the native picker before Orbit owns the selection writes nothing; backing out of an untouched review writes nothing; leaving after meaningful review edits prompts 'Leave import?' (Cluster Q)."
    - "[ASSUMPTION | spec-less edge IMP-02/empty] pickContacts returning [] (cancel) creates no session and writes nothing; a picked row with no usable name blocks Import with the name-required copy until a name is supplied (Cluster O)."
    - { statement: "The ImportReviewScreen field stack (name / Bound-Unbound / category / Frequency-if-Bound / methods+primary / birthday / photo preview) populates synchronously from the session snapshot, not a spinner", verification: backstop }
  artifacts:
    - "src/services/import/import-acquire.ts — acceptPickedContacts(): pickContacts result → createImportSession + insertSessionRow snapshot (single mode), returns session id; commitSingleImport(exec, { sessionId, input, externalLinks, birthday, now }): the single create seam (importContactRecord + completeSession → new contactId) that plan 10 later enriches with the post-commit photo step"
    - "src/screens/ImportReviewScreen.tsx — single-import review (reuses CreateContactScreen field block + ContactMethodsEditor + FrequencyPicker), Import CTA"
    - "src/components/AddSpeedDialFab.tsx — Reanimated speed-dial replacing the single '+' FAB"
    - "src/screens/use-import-leave-guard.ts — shared 'meaningful edits → confirm on leave' hook"
    - "ImportReview route registered in RootNavigator + RootStackParamList"
  key_links:
    - "import-acquire snapshots the picker result into the durable session IMMEDIATELY (Pattern 1) — the review screen reads Orbit rows, never re-queries the expired grant"
    - "the FAB routes single vs bulk by pickContacts({multiple}) result length; the tracer path is the N==1 branch"
  prohibitions:
    - "Do not drive the FAB expand/collapse (or any animation) from React state; Reanimated shared values only (CLAUDE.md)."
    - "Do not write any Orbit row before the Import tap; picker cancel / untouched back-out writes nothing."
    - "Do not hardcode any colour; every colour resolves through useTheme().colors.* (check:colors gate)."
    - "Do not add an iOS entry point or unsupported-state copy; Android-only."
    - "Do not seed or select an 'Uncategorized' category row; the category defaults to null."
---

<objective>
Deliver the phase's leading vertical slice: a thin, real, end-to-end single-contact import — pick one contact → durable session snapshot → review → create one Unbound contact with normalized methods. This is the owner-directed tracer that must be proven end-to-end BEFORE any bulk / photo / birthday / duplicate expansion.

User story: **As a** person curating my inner circle, **I want to** pick one phone contact and review it before it becomes an Orbit contact, **so that** I import deliberately, not by dumping my address book.

Purpose: Prove the whole architecture — native acquisition, snapshot-then-drive durability, the composed importer, and the entry-point/unsupported gating — on one happy path, on the agent's best early-context tokens, so an architectural dead-end surfaces after one slice instead of ten.
Output: Speed-dial FAB + Settings entry + ImportReviewScreen + acquire orchestration, wired to the plan 01–03 primitives, device-checked on Android 17.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@src/screens/HomeScreen.tsx
@src/screens/CreateContactScreen.tsx
@src/screens/SettingsScreen.tsx
@src/screens/UnboundContactsScreen.tsx
@src/components/ContactMethodsEditor.tsx
@src/components/FrequencyPicker.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "import one contact" — one path only</name>
  <files>src/services/import/import-acquire.ts, src/services/import/import-acquire.test.ts, src/screens/ImportReviewScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>modules/orbit-contact-picker/index.ts (pickContacts/PickedContact — plan 02), src/logic/picked-contact-map.ts (mapPickedContact — plan 03), src/db/imported-contact-dao.ts (importContactRecord — plan 03), src/db/import-session-dao.ts + import-session-read.ts (plan 01), src/screens/CreateContactScreen.tsx (the field block ~243–349 to reuse verbatim in order), src/components/ContactMethodsEditor.tsx, src/navigation/RootNavigator.tsx (Stack.Screen registration idiom lines 64–85), src/navigation/types.ts (RootStackParamList lines 21–125)</read_first>
  <action>Wire ONE path through every layer, single-select only. In import-acquire.ts, `acceptPickedContacts(exec, picked, { mode:'single', batchCategoryId:null, now })` maps each PickedContact via mapPickedContact, creates one import_session (mode 'single') and one import_session_row snapshot (source_payload JSON + photoTempUri staged path), and returns the session id — this is the acceptance moment (Pattern 1). Register an `ImportReview` route (param: sessionId) in RootNavigator + RootStackParamList. ImportReviewScreen reads the session row via import-session-read, seeds the CreateContactScreen field stack in order (name editable/required, Bound-Unbound SegmentedControl defaulting Unbound, Category Picker defaulting null/Uncategorized, FrequencyPicker only when Bound, ContactMethodsEditor with primary, birthday, photo preview via Avatar), and on the `Import` CTA calls `commitSingleImport(exec, { sessionId, input, externalLinks:[{provider:'android', externalContactId}], birthday, now })` — a new import-acquire service function that runs importContactRecord + completeSession in that order and returns the new contactId — then navigates to the new contact / Unbound list. The screen NEVER orchestrates the DB write inline (per the DAO/service convention, CLAUDE.md): commitSingleImport is the single create seam, and its signature is fixed here so plan 10 can enrich its BODY with the post-commit photo step (and plan 07's duplicate-interrupt "Import as New" can reuse it) without touching this screen. No write happens before the Import tap. import-acquire.test.ts (node) proves acceptPickedContacts creates exactly one session + one row from a fake PickedContact and that commitSingleImport produces exactly one Unbound contact — the data spine of the tracer, harness-verified without a device.</action>
  <verify>
    <automated>npx vitest run src/services/import/import-acquire.test.ts && npx tsc --noEmit --pretty false</automated>
    <human-check>Android 17 device (desktop-build APK, debug build for run-as): dashboard FAB → 'Import from Contacts' → pick one contact → review shows its name + a phone/email → tap Import → confirm a new Unbound contact exists with the normalized method (verify the contacts row via run-as sqlite: tracking_enabled=0). Cancel the picker once and confirm no contact/session is created.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/import/import-acquire.test.ts` exits 0 with a passing "single pick → one session + one row → one Unbound contact" test.
    - RootStackParamList contains `ImportReview: { sessionId: number }` and RootNavigator registers the screen.
    - ImportReviewScreen writes nothing on mount or on back; the create happens ONLY via the Import handler calling commitSingleImport (import-acquire), never orchestrated inline in the screen. `grep -n "commitSingleImport" src/services/import/import-acquire.ts src/screens/ImportReviewScreen.tsx` shows the function defined in import-acquire and called from the screen.
    - Device: one picked contact yields exactly one new Unbound contact with its method.
  </acceptance_criteria>
  <done>The single happy path works end-to-end and is committed; the data spine is node-green and the device path is confirmed on Android 17.</done>
</task>

<task type="auto">
  <name>Task 2: Speed-dial FAB (Reanimated) replacing the dashboard '+' with Import / Create</name>
  <files>src/components/AddSpeedDialFab.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>src/screens/HomeScreen.tsx (current single '+' FAB lines 652–663, styles), src/theme (useTheme colors), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #1; Color reserved-accent rules; ≥44px targets)</read_first>
  <action>Create AddSpeedDialFab: the base '+' FAB expands into two stacked mini-FABs animating outward — 'Import from Contacts' (→ launch acquisition: pickContacts({multiple:false}), then acceptPickedContacts, then navigate ImportReview) and 'Create manually' (→ navigate 'Create') — with '+' rotating to '×' and a tap-scrim to collapse. Drive expand/collapse and rotation with Reanimated shared values + useAnimatedStyle ONLY (no React state per frame). Each mini-FAB ≥44px, colours via theme tokens (accent fill for the base FAB per the reserved-accent list). Replace HomeScreen's direct-to-Create FAB with this component. Gate the 'Import from Contacts' mini-FAB on isContactPickerAvailable(): when false, it shows the unsupported state instead of launching (Task 3 owns the shared unsupported surface).</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: FAB expands to two options, '+' rotates to '×', scrim collapses; 'Create manually' still reaches the create screen.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "useSharedValue\|useAnimatedStyle" src/components/AddSpeedDialFab.tsx` matches; no `useState` drives the animation.
    - `npm run check:colors` passes (no hex literals).
    - HomeScreen renders AddSpeedDialFab instead of the old direct-navigate FAB.
  </acceptance_criteria>
  <done>The dashboard Add affordance is a Reanimated speed-dial offering Import / Create.</done>
</task>

<task type="auto">
  <name>Task 3: Settings 'Contacts Integration' entry + shared unsupported-state gating + leave-guard</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/use-import-leave-guard.ts</files>
  <read_first>src/screens/SettingsScreen.tsx (section/sectionHeading idiom lines 676–835), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #2 + #10 unsupported copy; Copywriting Contract), modules/orbit-contact-picker/index.ts (isContactPickerAvailable)</read_first>
  <action>Add a `Contacts Integration` section to SettingsScreen (reuse settings.section / sectionHeading, 13px/600 textSecondary) with one row: `Import contacts`. When isContactPickerAvailable() is false, the row renders the calm unsupported state (heading `Contact import unavailable`, body `Contact import requires Android 17 or later.` — verbatim) and does not launch; the rest of Settings stays usable. Extract a tiny shared `useImportUnsupported()` helper (or const) so both the FAB and this row read the same gate. Create `use-import-leave-guard.ts`: a hook that tracks whether meaningful review edits were made and, on a navigation-away/back with edits, shows the `Leave import?` confirm ("You've made changes. Leaving keeps anything already imported and discards the rest."); wire it into ImportReviewScreen so an untouched back writes/discards nothing and an edited back confirms (Cluster Q).</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Pre-17 device (or forced availability=false): both entry points show the unsupported copy and Orbit stays usable; edited review back-out prompts 'Leave import?'.</human-check>
  </verify>
  <acceptance_criteria>
    - SettingsScreen contains a `Contacts Integration` section with an `Import contacts` row.
    - The exact string `Contact import requires Android 17 or later.` appears once in the unsupported surface source.
    - use-import-leave-guard exports a hook consumed by ImportReviewScreen; an untouched review triggers no confirm.
  </acceptance_criteria>
  <done>Import is reachable from Settings and the Add flow, both gated to the calm unsupported state on old Android, with leave-confirmation on edited reviews.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker → acquisition → review | Untrusted snapshot becomes a durable session then a contact only on explicit Import |
| navigation | Back/cancel must not leave partial writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Info disclosure | acquisition passing picker data | high | mitigate | import-acquire consumes only the plan-02 snapshot (no content:// URI); staged photo path is app-owned (V8/V12) |
| T-19-09 | Tampering | review-screen writes before confirm | medium | mitigate | No DB write on mount/back; only the Import handler writes; picker cancel writes nothing (Cluster Q) |
| T-19-01 | Tampering | name/method values into DAO | high | mitigate | Reuses plan-03 `?`-bound importContactRecord; name required before create (V5) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs (Reanimated already a dependency) |
</threat_model>

## Artifacts this plan produces

- `src/services/import/import-acquire.ts`: `acceptPickedContacts()`.
- `src/screens/ImportReviewScreen.tsx` (single review), `src/screens/use-import-leave-guard.ts` (shared leave-confirm hook).
- `src/components/AddSpeedDialFab.tsx` (Reanimated speed-dial).
- `src/screens/HomeScreen.tsx` (renders speed-dial), `src/screens/SettingsScreen.tsx` (Contacts Integration section + unsupported state).
- `src/navigation/RootNavigator.tsx` + `types.ts`: `ImportReview` route.

<verification>
- `npx vitest run src/services/import/import-acquire.test.ts` green; `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device (Android 17): single-contact import happy path confirmed; picker cancel writes nothing; unsupported state confirmed on a pre-17 device (or forced availability=false).
</verification>

<success_criteria>
The single-contact import works end-to-end on device before any expansion: pick one → review → one Unbound contact with normalized methods, reachable from both entry points, gated to the unsupported state on old Android.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-04-SUMMARY.md` when done.
</output>
