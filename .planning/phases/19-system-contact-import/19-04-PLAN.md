---
phase: 19-system-contact-import
plan: 04
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - src/services/import/import-acquire.ts
  - src/services/import/import-acquire.test.ts
  - src/services/photos/photo-storage.ts
  - src/db/photo-relative-path.ts
  - src/screens/ImportReviewScreen.tsx
  - src/screens/use-import-leave-guard.ts
  - src/components/AddSpeedDialFab.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [IMP-01, IMP-02, IMP-04]
user_setup:
  - service: android-17-device
    why: "The single-contact happy path exercises the real Android 17 Contact Picker; full end-to-end UAT needs an Android 17 device"
    dashboard_config:
      - task: "Confirm the Pixel is on Android 17 (else only the unsupported-state path is verifiable on a pre-17 device)"
        location: "Device: Settings ▸ About ▸ Android version"
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TRACER: from the dashboard speed-dial FAB, tapping 'Import from Contacts' launches the picker, and picking ONE contact opens ImportReviewScreen with name + methods pre-filled from a durable session snapshot; nothing is written until Import is tapped, then a new Unbound contact with normalized methods appears (and is visible in the Unbound contacts list) (IMP-01/IMP-02/IMP-04)."
    - "acceptPickedContacts stages each picker CACHE photo into DOCUMENT-DIR session staging (via the new stageImportPhoto/importStagingRelPath helpers mirroring restore-pending) BEFORE inserting the session row, and stores only the validated document-dir relative path in photo_rel_path — so a process death or cache eviction between acceptance and photo persistence never orphans the reference (IMP-04; addresses review HIGH photo-staging durability)."
    - "The import-staging path shape is enforced at the SINGLE canonical chokepoint src/db/photo-relative-path.ts via a new `SAFE_IMPORT_STAGING_RELATIVE` regex + `assertSafeImportStagingRelative` export (mirroring SAFE_RESTORE_PENDING_RELATIVE), imported by the photo-storage staging helpers — no ad-hoc inline regex (addresses cycle-2 LOW — the staging guard was never added to the chokepoint file)."
    - "If acceptPickedContacts throws AFTER a photo is staged but before/at acceptImportSessionWithRows, the staged document-dir file is left orphaned (no owning session row) and is reconciled — deleted — by plan 09's REQUIRED launch-sweep orphan reconciliation (a staged file with no owning live-session row), so a failed acceptance never permanently leaks staged photos (IMP-04; addresses cycle-2 LOW — orphan-if-DB-fails had no coverage)."
    - "acceptPickedContacts writes the session + all rows in ONE transaction via acceptImportSessionWithRows and captures the effective phone region into import_sessions.phone_region (resolveEffectivePhoneRegion over the settings override + device region), so imported numbers normalize canonically and survive resume (IMP-04; addresses review MEDIUM phone-region + review HIGH atomic snapshot)."
    - "commitSingleImport calls importContactRecord with resolveRow so the new contact AND the session row's imported/contact_id transition commit in ONE transaction, then completeSession; the session never ends 'complete' with an unresolved row (IMP-04; addresses review HIGH — commitSingleImport never called setRowContact)."
    - "The speed-dial FAB's 'Import from Contacts' calls pickContacts({multiple:true}) and routes by result length: 0 → no-op, 1 → acceptPickedContacts(single) → ImportReview, N>1 → acceptPickedContacts(bulk) → BulkImportSetup (screen registered by plan 06) — so IMP-02's bulk entry point is wired from the Add flow (addresses review HIGH bulk entry never wired)."
    - "The speed-dial FAB expands the single '+' into 'Import from Contacts' / 'Create manually' and rotates '+'→'×', animated with Reanimated shared values only (never React state); tapping the scrim or '×' collapses it."
    - "Both entry points (dashboard FAB and Settings ▸ Contacts Integration ▸ Import contacts) gate on isContactPickerAvailable(): on SDK<37 the import affordance shows the unsupported state 'Contact import requires Android 17 or later.' and the rest of Orbit — including 'Create manually' — stays usable (IMP-01)."
    - "Cancelling the native picker before Orbit owns the selection writes nothing; backing out of an UNTOUCHED review explicitly DISCARDS the just-accepted session and its staged photos (no confirm) so no orphan pending session prompts on next launch; leaving after meaningful review edits prompts 'Leave import?' then discards on confirm (Cluster Q; addresses review HIGH orphaned-resumable-session on untouched back)."
    - "[ASSUMPTION | spec-less edge IMP-02/empty] pickContacts returning [] (cancel) creates no session and writes nothing; a picked row with no usable name blocks Import with the name-required copy until a name is supplied (Cluster O)."
    - { statement: "The ImportReviewScreen field stack (name / Bound-Unbound / category / Frequency-if-Bound / methods+primary / birthday / photo preview) populates synchronously from the session snapshot, not a spinner", verification: backstop }
  artifacts:
    - "src/services/import/import-acquire.ts — acceptPickedContacts(exec, picked, { mode, batchCategoryId, effectivePhoneRegion, now }): stages photos to document-dir, then acceptImportSessionWithRows snapshot (single OR bulk), returns session id; commitSingleImport(exec, { sessionId, rowId, input, externalLinks, birthday, now }): the single create seam calling importContactRecord({resolveRow}) + completeSession; routePickedImport(picked, nav): length-based single/bulk routing"
    - "src/services/photos/photo-storage.ts — importStagingRelPath(sessionToken, rowToken), stageImportPhoto(cacheUri, relative), deleteImportStaging(relative), listImportStagingPhotos() — an import-staging namespace mirroring the shipped restore-pending idiom, asserting via assertSafeImportStagingRelative"
    - "src/db/photo-relative-path.ts — SAFE_IMPORT_STAGING_RELATIVE (regex over `import-staging/<sessionToken>/<rowToken>.<ext>` with optional `.stage-tmp`) + assertSafeImportStagingRelative(relative), mirroring SAFE_RESTORE_PENDING_RELATIVE/assertSafeRestorePendingRelative"
    - "src/screens/ImportReviewScreen.tsx — single-import review (reuses CreateContactScreen field block + ContactMethodsEditor + FrequencyPicker), Import CTA"
    - "src/components/AddSpeedDialFab.tsx — Reanimated speed-dial replacing the single '+' FAB, multi-pick + length routing"
    - "src/screens/use-import-leave-guard.ts — shared 'untouched back → discard session; edited back → confirm then discard' hook"
    - "ImportReview route registered in RootNavigator + RootStackParamList; BulkImportSetup added to the param list (screen registered by plan 06)"
  key_links:
    - "import-acquire snapshots the picker result into the durable session IMMEDIATELY (Pattern 1), staging photos to the document dir first — the review screen reads Orbit rows, never re-queries the expired grant"
    - "the FAB routes single vs bulk by pickContacts({multiple:true}) result length; the tracer path is the N==1 branch; the N>1 branch reaches BulkImportSetup which plan 06 registers + verifies end-to-end"
    - "commitSingleImport is the single create seam whose BODY plan 10 enriches with the post-commit photo step and plan 07's Import-as-New reuses — its signature is fixed here"
  prohibitions:
    - "Do not drive the FAB expand/collapse (or any animation) from React state; Reanimated shared values only (CLAUDE.md)."
    - "Do not store a picker CACHE path in photo_rel_path; stage to the document dir first (review HIGH)."
    - "Do not write any Orbit contact row before the Import tap; picker cancel / untouched back-out writes no contact — but untouched back-out MUST discard the accepted session + staged photos (it is not 'do nothing')."
    - "Do not hardcode any colour; every colour resolves through useTheme().colors.* (check:colors gate)."
    - "Do not add an iOS entry point or unsupported-state copy; Android-only."
    - "Do not seed or select an 'Uncategorized' category row; the category defaults to null."
---

<objective>
Deliver the phase's leading vertical slice: a thin, real, end-to-end single-contact import — pick one contact → durable document-dir-staged session snapshot → review → atomically create one Unbound contact with normalized methods and resolve its session row. This is the owner-directed tracer proven end-to-end BEFORE any bulk / photo / birthday / duplicate expansion, and it establishes the durable-staging, region-capture, atomic-commit, and length-based-routing contracts the rest of the phase depends on.

User story: **As a** person curating my inner circle, **I want to** pick one phone contact and review it before it becomes an Orbit contact, **so that** I import deliberately, not by dumping my address book.

Purpose: Prove the whole architecture — native acquisition, snapshot-then-drive durability WITH document-dir photo staging, the atomic composed importer + session-row resolution, phone-region capture, and the entry-point/unsupported gating + length routing — on one happy path, so an architectural dead-end surfaces after one slice instead of ten.
Output: Speed-dial FAB (multi-pick + routing) + Settings entry + ImportReviewScreen + acquire orchestration + import-staging helpers, wired to the plan 01–03 primitives, device-checked on Android 17.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/screens/HomeScreen.tsx
@src/screens/CreateContactScreen.tsx
@src/screens/create-contact-logic.ts
@src/screens/SettingsScreen.tsx
@src/screens/UnboundContactsScreen.tsx
@src/components/ContactMethodsEditor.tsx
@src/components/FrequencyPicker.tsx
@src/services/photos/photo-storage.ts
@src/db/app-settings-dao.ts
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Claude) — commitSingleImport never resolved the session row.** It now calls `importContactRecord({ resolveRow })` so contact-create + row resolution are atomic; then `completeSession`.
- **HIGH (Codex/Cursor) — photo staging on OS-evictable cache.** `acceptPickedContacts` stages the cache photo into document-dir session staging (new `stageImportPhoto` helpers mirroring `stageRestorePending`) before the row insert; only the document-dir rel path is stored.
- **HIGH (Codex/Cursor/Claude) — bulk entry point never wired.** The FAB now `pickContacts({multiple:true})` + `routePickedImport` length routing (0/1/N); N>1 → BulkImportSetup.
- **HIGH (Codex) — untouched Back leaves an orphan resumable session.** The leave-guard now discards the accepted session + staged photos on untouched back (no confirm); edited back confirms then discards.
- **HIGH (Codex 19-01) — non-atomic snapshot.** `acceptPickedContacts` uses `acceptImportSessionWithRows` (session + all rows in one transaction).
- **MEDIUM (Cursor/Claude) — phone region dropped.** `acceptPickedContacts` resolves + stores `phone_region`; the mapper threads it.
- **MEDIUM (Codex) — resume routing target for a single already-imported/linked session** is defined in the leave-guard/resume contract (plan 09 owns the resume prompt; this plan's single session resolves to ImportReview when its row is still pending).

### Cycle-2 review incorporated
- **LOW (import-staging guard not at the chokepoint).** Added `SAFE_IMPORT_STAGING_RELATIVE` + `assertSafeImportStagingRelative` to `src/db/photo-relative-path.ts` (added to files_modified) and route the staging helpers through it — no inline regex in photo-storage.ts.
- **LOW (orphan-if-DB-fails uncovered).** Added a truth + an import-acquire.test.ts assertion that a photo staged before a failed accept is a reconcilable orphan (deleted by plan 09's REQUIRED orphan reconciliation).

<tasks>

<task type="tracer">
  <name>End-to-end "import one contact" — one path only (durable staging + atomic commit)</name>
  <files>src/services/import/import-acquire.ts, src/services/import/import-acquire.test.ts, src/services/photos/photo-storage.ts, src/db/photo-relative-path.ts, src/screens/ImportReviewScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>modules/orbit-contact-picker/index.ts (pickContacts/PickedContact — plan 02; photoTempUri is a CACHE path), src/logic/picked-contact-map.ts (mapPickedContact(opts:{categoryId,effectivePhoneRegion}) — plan 03), src/db/imported-contact-dao.ts (importContactRecord({resolveRow}) — plan 03), src/db/import-session-dao.ts (acceptImportSessionWithRows, completeSession, discardSession, setRow* — plan 01), src/db/import-session-read.ts (listSessionRows — plan 01), src/db/photo-relative-path.ts (SAFE_RESTORE_PENDING_RELATIVE + assertSafeRestorePendingRelative lines 22–56 — the SINGLE chokepoint to mirror; add the import-staging guard HERE, not inline), src/services/photos/photo-storage.ts (restorePendingRelPath/stageRestorePending/deleteRestorePending idiom lines 120–188 to mirror for an import-staging namespace; how it imports assertSafeRestorePendingRelative from photo-relative-path.ts), src/db/app-settings-dao.ts (resolveEffectivePhoneRegion), src/screens/create-contact-logic.ts (methodNormalization construction), src/screens/CreateContactScreen.tsx (the field block ~243–349 to reuse verbatim in order; how effectivePhoneRegion is resolved from settings + device), src/navigation/RootNavigator.tsx (Stack.Screen registration idiom), src/navigation/types.ts (RootStackParamList)</read_first>
  <action>First add the import-staging path guard to the canonical chokepoint src/db/photo-relative-path.ts (additive; do not touch SAFE_RELATIVE/SAFE_RESTORE_PENDING_RELATIVE): export `SAFE_IMPORT_STAGING_RELATIVE` (a regex over `import-staging/<sessionToken>/<rowToken>.<ext>` where each token is `[A-Za-z0-9_-]+`, `<ext>` one of jpg|jpeg|png|webp, with an optional trailing `.stage-tmp`) and `assertSafeImportStagingRelative(relative)` (throws on non-string, null byte, or non-match), mirroring assertSafeRestorePendingRelative EXACTLY. Then add an import-staging namespace to photo-storage.ts mirroring restore-pending EXACTLY (additive; do not touch restore-pending): `importStagingRelPath(sessionToken, rowToken)` → an `import-staging/` document-dir relative path validated by assertSafeImportStagingRelative; `stageImportPhoto(cacheUri, relative)` (copy cache file → tmp → atomic move, like stageRestorePending, asserting the guard on both the tmp and final relative); `deleteImportStaging(relative)`; `listImportStagingPhotos()`. Then wire ONE path through every layer, single-select for the tracer. In import-acquire.ts, `acceptPickedContacts(exec, picked, { mode, batchCategoryId, effectivePhoneRegion, now })`: for each PickedContact, stage its photoTempUri (cache) into a document-dir import-staging path via stageImportPhoto (best-effort — on failure photo_rel_path is null, the contact still imports), map it via mapPickedContact({ categoryId: batchCategoryId, effectivePhoneRegion }), and build a row payload {externalContactId, sourcePayload JSON, photoRelPath}. Then call acceptImportSessionWithRows({ session: { uid, mode, batchCategoryId, batchTrackingEnabled: 0, phoneRegion: effectivePhoneRegion, now }, rows }) in ONE transaction (batchTrackingEnabled=0 = the Unbound batch default; single-mode review still lets the user choose Bound before Import) and return the session id — the acceptance moment (Pattern 1). Register an `ImportReview` route (param: sessionId) in RootNavigator + RootStackParamList, and ADD `BulkImportSetup: { sessionId: number }` to the param list (type only — plan 06 registers the screen). ImportReviewScreen reads the session row via import-session-read, seeds the CreateContactScreen field stack in order (name editable/required, Bound-Unbound SegmentedControl defaulting Unbound, Category Picker defaulting null/Uncategorized, FrequencyPicker only when Bound, ContactMethodsEditor with primary, birthday, photo preview via Avatar reading the staged path), and on the `Import` CTA calls `commitSingleImport(exec, { sessionId, rowId, input, externalLinks:[{provider:'android', externalContactId}], birthday, now })` — a new import-acquire function that calls `importContactRecord({ input, externalLinks, birthday, now, resolveRow:{ rowId, matchOutcome:'new' } })` (atomic contact + row resolution) then `completeSession(sessionId, now)`, returning the new contactId — then navigates to the new contact / Unbound list. The screen NEVER orchestrates the DB write inline: commitSingleImport is the single create seam, signature fixed here so plan 10 enriches its BODY with the post-commit photo step and plan 07's Import-as-New reuses it. No write happens before the Import tap. import-acquire.test.ts (node) proves acceptPickedContacts creates exactly one session + one row (photo_rel_path is the document-dir staged path) and that commitSingleImport produces exactly one Unbound contact AND resolves the row to imported/contact_id AND marks the session complete — the atomic data spine of the tracer, harness-verified without a device.</action>
  <verify>
    <automated>npx vitest run src/services/import/import-acquire.test.ts && npx tsc --noEmit --pretty false</automated>
    <human-check>Android 17 device (desktop-build APK, debug build for run-as): dashboard FAB → 'Import from Contacts' → pick one contact → review shows its name + a phone/email → tap Import → confirm a new Unbound contact exists with the normalized method (verify the contacts row via run-as sqlite: tracking_enabled=0; verify the import_session_rows row is imported with contact_id set). Cancel the picker once and confirm no contact/session is created.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/import/import-acquire.test.ts` exits 0 with a passing "single pick → one session + one row (document-dir staged photo) → one Unbound contact + row resolved imported + session complete" test.
    - `grep -n "importStagingRelPath\|stageImportPhoto" src/services/photos/photo-storage.ts` matches; the row's photo_rel_path is under `import-staging/`, never a cache path.
    - `grep -n "SAFE_IMPORT_STAGING_RELATIVE\|assertSafeImportStagingRelative" src/db/photo-relative-path.ts` matches (the guard lives at the canonical chokepoint), and photo-storage.ts imports assertSafeImportStagingRelative from it (no inline `import-staging` regex in photo-storage.ts).
    - import-acquire.test.ts includes a "staging-before-accept orphan is reconcilable" assertion: after acceptPickedContacts stages a photo then acceptImportSessionWithRows is forced to throw, the staged file exists with no owning session row (so it matches plan 09's orphan-reconciliation predicate: a listImportStagingPhotos entry not referenced by any live session row).
    - RootStackParamList contains `ImportReview: { sessionId: number }` and `BulkImportSetup: { sessionId: number }`; RootNavigator registers ImportReview.
    - ImportReviewScreen writes nothing on mount; the create happens ONLY via commitSingleImport (import-acquire), which calls importContactRecord with resolveRow. `grep -n "resolveRow" src/services/import/import-acquire.ts` matches.
    - Device: one picked contact yields exactly one new Unbound contact with its method and a resolved (imported) session row.
  </acceptance_criteria>
  <done>The single happy path works end-to-end and is committed; the atomic data spine (durable staging + contact+row resolution) is node-green and the device path is confirmed on Android 17.</done>
</task>

<task type="auto">
  <name>Task 2: Speed-dial FAB (Reanimated) with multi-pick + length routing</name>
  <files>src/components/AddSpeedDialFab.tsx, src/screens/HomeScreen.tsx, src/services/import/import-acquire.ts</files>
  <read_first>src/screens/HomeScreen.tsx (current single '+' FAB lines 652–663, styles), src/theme (useTheme colors), src/db/app-settings-dao.ts (resolveEffectivePhoneRegion), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #1; Color reserved-accent rules; ≥44px targets)</read_first>
  <action>Add `routePickedImport(exec, picked, { effectivePhoneRegion, now }, nav)` to import-acquire.ts: if picked.length===0 → no-op; if ===1 → acceptPickedContacts(single) then nav.navigate('ImportReview',{sessionId}); if >1 → acceptPickedContacts(bulk) then nav.navigate('BulkImportSetup',{sessionId}) (screen registered by plan 06 — until then the N>1 branch is exercised at plan 06's device UAT). Create AddSpeedDialFab: the base '+' FAB expands into two stacked mini-FABs animating outward — 'Import from Contacts' (→ resolve effectivePhoneRegion from settings+device, call pickContacts({multiple:true}), then routePickedImport) and 'Create manually' (→ navigate 'Create') — with '+' rotating to '×' and a tap-scrim to collapse. Drive expand/collapse and rotation with Reanimated shared values + useAnimatedStyle ONLY (no React state per frame). Each mini-FAB ≥44px, colours via theme tokens (accent fill for the base FAB per the reserved-accent list). Replace HomeScreen's direct-to-Create FAB with this component. Gate the 'Import from Contacts' mini-FAB on isContactPickerAvailable(): when false, it shows the unsupported state instead of launching (Task 3 owns the shared unsupported surface).</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: FAB expands to two options, '+' rotates to '×', scrim collapses; 'Create manually' still reaches the create screen; picking two contacts routes toward the bulk setup entry (verified fully in plan 06).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "useSharedValue\|useAnimatedStyle" src/components/AddSpeedDialFab.tsx` matches; no `useState` drives the animation.
    - `grep -n "multiple: true\|multiple:true" src/components/AddSpeedDialFab.tsx src/services/import/import-acquire.ts` matches (multi-pick), and routePickedImport branches on length (0/1/N).
    - `npm run check:colors` passes (no hex literals).
    - HomeScreen renders AddSpeedDialFab instead of the old direct-navigate FAB.
  </acceptance_criteria>
  <done>The dashboard Add affordance is a Reanimated speed-dial that multi-picks and routes single vs bulk by result length.</done>
</task>

<task type="auto">
  <name>Task 3: Settings 'Contacts Integration' entry + shared unsupported gating + leave-guard (discards accepted session)</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/ImportReviewScreen.tsx, src/screens/use-import-leave-guard.ts</files>
  <read_first>src/screens/SettingsScreen.tsx (section/sectionHeading idiom lines 676–835), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #2 + #10 unsupported copy; Copywriting Contract), modules/orbit-contact-picker/index.ts (isContactPickerAvailable), src/db/import-session-dao.ts (discardSession — plan 01), src/services/photos/photo-storage.ts (deleteImportStaging — Task 1)</read_first>
  <action>Add a `Contacts Integration` section to SettingsScreen (reuse settings.section / sectionHeading, 13px/600 textSecondary) with one row: `Import contacts` (→ the same resolve-region + pickContacts({multiple:true}) + routePickedImport path as the FAB). When isContactPickerAvailable() is false, the row renders the calm unsupported state (heading `Contact import unavailable`, body `Contact import requires Android 17 or later.` — verbatim) and does not launch; the rest of Settings stays usable. Extract a tiny shared `useImportUnsupported()` helper so both the FAB and this row read the same gate. Create `use-import-leave-guard.ts`: a hook that tracks whether meaningful review edits were made. On a navigation-away/back from ImportReview: if the session row is still unresolved (no contact_id) AND no meaningful edits were made, DISCARD the accepted session (discardSession) and its staged photos (deleteImportStaging over the returned paths) with NO confirm — acceptance made Orbit the owner, so back-out must resolve the session, not orphan it (Cluster Q / review HIGH). If meaningful edits were made, show the `Leave import?` confirm ("You've made changes. Leaving keeps anything already imported and discards the rest.") and on confirm discard the session + staged photos. Wire it into ImportReviewScreen. (A session whose row is already imported/linked is left as committed — nothing to discard.)</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Pre-17 device (or forced availability=false): both entry points show the unsupported copy and Orbit stays usable. On Android 17: accept a single pick, back out WITHOUT editing → relaunch shows NO resume prompt (session was discarded); accept, edit a field, back out → 'Leave import?' confirm, and after confirming, relaunch shows no orphan session.</human-check>
  </verify>
  <acceptance_criteria>
    - SettingsScreen contains a `Contacts Integration` section with an `Import contacts` row.
    - The exact string `Contact import requires Android 17 or later.` appears once in the unsupported surface source.
    - use-import-leave-guard discards the accepted session + staged photos on untouched back (no confirm) and confirms then discards on edited back; `grep -n "discardSession\|deleteImportStaging" src/screens/use-import-leave-guard.ts` matches.
  </acceptance_criteria>
  <done>Import is reachable from Settings and the Add flow, both gated to the calm unsupported state on old Android; back-out of an accepted-but-unresolved review discards the session (no orphan), with a confirm only after meaningful edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker → acquisition → review | Untrusted snapshot becomes a durable (document-dir-staged) session then a contact only on explicit Import |
| navigation | Back/cancel must not leave partial writes; back after acceptance must resolve (discard) the session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Info disclosure | acquisition passing picker data | high | mitigate | import-acquire consumes only the plan-02 snapshot (no content:// URI); staged photo path is app-owned document-dir (V8/V12) |
| T-19-22 | DoS (feature break) | non-durable cache photo staging | high | mitigate | acceptPickedContacts stages cache → document-dir before the row insert; only the document-dir rel path is stored (review HIGH) |
| T-19-23 | Repudiation (orphan session) | untouched back-out | medium | mitigate | Untouched back discards the accepted session + staged photos; no orphan pending session prompts later |
| T-19-09 | Tampering | review-screen writes before confirm | medium | mitigate | No contact write on mount; only the Import handler writes via commitSingleImport; picker cancel writes nothing |
| T-19-01 | Tampering | name/method values into DAO | high | mitigate | Reuses plan-03 `?`-bound importContactRecord; name required before create (V5) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs (Reanimated already a dependency) |
</threat_model>

## Artifacts this plan produces
- `src/services/import/import-acquire.ts`: `acceptPickedContacts()`, `commitSingleImport()`, `routePickedImport()`.
- `src/services/photos/photo-storage.ts`: import-staging namespace `importStagingRelPath()`, `stageImportPhoto()`, `deleteImportStaging()`, `listImportStagingPhotos()`.
- `src/db/photo-relative-path.ts`: `SAFE_IMPORT_STAGING_RELATIVE`, `assertSafeImportStagingRelative()`.
- `src/screens/ImportReviewScreen.tsx` (single review), `src/screens/use-import-leave-guard.ts` (discard-on-back hook).
- `src/components/AddSpeedDialFab.tsx` (Reanimated speed-dial, multi-pick + length routing).
- `src/screens/HomeScreen.tsx` (renders speed-dial), `src/screens/SettingsScreen.tsx` (Contacts Integration section + unsupported state).
- `src/navigation/RootNavigator.tsx` + `types.ts`: `ImportReview` route; `BulkImportSetup` param-list entry.

<verification>
- `npx vitest run src/services/import/import-acquire.test.ts` green; `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device (Android 17): single-contact import happy path confirmed (contact + resolved row + complete session); picker cancel writes nothing; untouched back discards the session; unsupported state confirmed on a pre-17 device (or forced availability=false).
</verification>

<success_criteria>
The single-contact import works end-to-end on device before any expansion: pick one → durable document-dir-staged review → atomically create one Unbound contact with normalized methods AND resolve its session row → complete session; reachable from both entry points with multi-pick length routing, gated to the unsupported state on old Android, and orphan-free on back-out.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-04-SUMMARY.md` when done.
</output>
