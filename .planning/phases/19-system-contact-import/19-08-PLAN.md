---
phase: 19-system-contact-import
plan: 08
type: execute
wave: 6
depends_on: [19-06, 19-07]
files_modified:
  - src/screens/ImportCompleteScreen.tsx
  - src/screens/ImportProgressScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [IMP-02, IMP-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Bulk import ends on an explicit summary screen with four count rows — `Imported (n)`, `Already in Orbit (n)`, `Need review (n)`, `Failed / skipped (n)` — computed from the durable session rows via sessionSummaryCounts (plan 01), where `Already in Orbit` reads match_outcome='already_linked' (NOT row_status='skipped'), so it is counted distinctly from a user Skip (IMP-02/IMP-04, Cluster T; addresses review HIGH count-semantics collision)."
    - "The summary offers `Review possible matches` (→ DuplicateReview, shown only when need-review > 0), `View Unbound contacts` (→ the UnboundContacts route — the locked bridge), and `Done` (accent) (Cluster T)."
    - "A partial batch reports `Failed / skipped (n)` with a `Retry` affordance that re-runs runImportBatch with eligibleStatuses ['pending','failed'] over the still-failed/pending rows (idempotency guard prevents double-import) while safe contacts remain committed; a photo-only failure keeps its contact in `Imported` (Cluster F/P; addresses review HIGH retry-cannot-reach-failed-rows)."
    - "The session is marked complete ONLY when no needs_review or pending rows remain; while needs_review > 0 the session stays status='pending' (resumable), the summary is still shown, and `Review possible matches` resolves the remaining rows — so a mid-review app kill still offers Resume (plan 09) rather than losing the review path (addresses review MEDIUM complete-while-needs_review removes resume)."
    - { statement: "Completion count rows pluralize correctly and render (0) counts (1 contact vs N contacts; empty categories show (0))", verification: backstop }
    - "[ASSUMPTION | spec-less edge IMP-04/unclassified] Completion counts derive from import_session_rows via sessionSummaryCounts (durable), not from an in-memory tally, so the summary is correct even after a resumed session finishes."
  artifacts:
    - "src/screens/ImportCompleteScreen.tsx — four-count summary + Review/View-Unbound/Done + Retry"
    - "ImportProgress → ImportComplete wiring (progress lands on the summary)"
    - "ImportComplete route registered"
  key_links:
    - "the summary reads sessionSummaryCounts (plan 01, four buckets) and marks the session complete ONLY when needs_review + pending == 0; `View Unbound contacts` navigates to the existing UnboundContacts route (the locked completion bridge)"
    - "`Review possible matches` navigates to DuplicateReview (plan 07) for the same session; Retry calls runImportBatch (plan 06) with eligibleStatuses ['pending','failed']"
  prohibitions:
    - "Do not fabricate counts from a transient tally; read the durable session rows via sessionSummaryCounts."
    - "Do not compute `Already in Orbit` from row_status; it MUST read match_outcome='already_linked' so a user Skip is not miscounted (review HIGH)."
    - "Do not mark the session complete while needs_review or pending rows remain; keep it resumable."
    - "Do not report a photo-only failure as a contact failure; a photo failure keeps the contact imported (Cluster F)."
    - "Do not hardcode any colour; theme tokens only."
---

<objective>
Deliver the import completion summary and its bridge to the Unbound-contacts screen — the closing surface of IMP-02/IMP-04 that reports what happened and routes the user onward.

Purpose: A bulk import must end on an explicit, honest report (imported / already in Orbit / need review / failed-skipped) sourced from durable rows, with a Retry path for failures and a direct bridge to the dedicated Unbound screen — the locked completion behavior.
Output: ImportCompleteScreen + the progress→complete wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/db/import-session-read.ts
@src/db/import-session-dao.ts
@src/services/import/import-driver.ts
@src/screens/ImportProgressScreen.tsx
@src/screens/UnboundContactsScreen.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor/Claude) — count-semantics collision.** The summary reads `sessionSummaryCounts` (plan 01); `Already in Orbit` keys on `match_outcome='already_linked'`, so it never conflates a deterministic already-linked row with a user Skip.
- **HIGH (Codex/Cursor) — Retry incompatible with the pending-only driver.** Retry calls `runImportBatch` with `eligibleStatuses: ['pending','failed']` (plan 06), so failed rows are actually re-processed; the driver's idempotency guard prevents double-import.
- **MEDIUM (Codex/Cursor) — completeSession while needs_review>0 removes the resume path.** The summary marks the session complete ONLY when needs_review + pending == 0; otherwise it stays resumable and `Review possible matches` resolves the rest.
- **MEDIUM (Codex) — photo-only failure must not count as failed.** `sessionSummaryCounts` keeps photo_failed rows under Imported (they retain row_status='imported').

<tasks>

<task type="auto">
  <name>Task 1: ImportCompleteScreen — four-count summary + actions + Retry</name>
  <files>src/screens/ImportCompleteScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #8; Copywriting count rows + actions + `Some contacts couldn't be imported.`), src/db/import-session-read.ts (sessionSummaryCounts — plan 01, four buckets), src/db/import-session-dao.ts (completeSession — plan 01), src/services/import/import-driver.ts (runImportBatch with eligibleStatuses — plan 06), src/screens/UnboundContactsScreen.tsx (route target), src/screens/ArchivedContactsScreen.tsx (footerEntry idiom for count rows)</read_first>
  <action>Register an `ImportComplete` route (param: sessionId). On mount, read sessionSummaryCounts and render header `Import complete` with four count rows (footerEntry idiom): `Imported (n)`, `Already in Orbit (n)`, `Need review (n)`, `Failed / skipped (n)` — each pluralized and rendering (0). `Already in Orbit` is the `alreadyInOrbit` bucket (match_outcome='already_linked'), NOT a row_status count. Actions: `Review possible matches` (shown only when needReview > 0 → navigate DuplicateReview with the sessionId), `View Unbound contacts` (→ navigate `UnboundContacts`), `Done` (accent → dashboard). When failedOrSkipped includes failures, show `Some contacts couldn't be imported.` and a `Retry` affordance that calls runImportBatch(exec, { sessionId, batchCategoryId, now, onProgress, eligibleStatuses: ['pending','failed'] }) — re-processing failed/pending rows only, safe contacts stay committed (idempotency guard in the driver). Mark the session complete via completeSession ONLY when needReview == 0 AND no pending rows remain; otherwise leave it status='pending' (resumable) so a mid-review kill still offers Resume. A photo-only failure row counts under Imported, not Failed. Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: after a mixed batch, the four counts match reality; `Review possible matches` appears only with need-review>0; `View Unbound contacts` opens the Unbound screen; Retry re-runs failed rows without touching committed contacts.</human-check>
  </verify>
  <acceptance_criteria>
    - ImportComplete route registered; four count rows present with pluralized + (0)-capable labels; `grep -n "sessionSummaryCounts" src/screens/ImportCompleteScreen.tsx` matches (no ad-hoc row_status count for Already in Orbit).
    - Retry calls runImportBatch with `eligibleStatuses` including 'failed'.
    - completeSession is called only when needReview == 0 and no pending rows remain.
    - `View Unbound contacts` navigates to the `UnboundContacts` route; `Review possible matches` is conditional on needReview > 0.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>The summary reports durable counts, bridges to Unbound, and offers Retry for failures without disturbing committed contacts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ImportProgress → ImportComplete</name>
  <files>src/screens/ImportProgressScreen.tsx</files>
  <read_first>src/screens/ImportProgressScreen.tsx (plan 06 temporary landing target), src/screens/ImportCompleteScreen.tsx (Task 1)</read_first>
  <action>Replace the plan-06 temporary post-import landing so that when runImportBatch completes, ImportProgress navigates to `ImportComplete` with the sessionId (using navigation.replace so back does not return to the progress screen). No other behavior of the progress screen changes.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false</automated>
    <human-check>Device: a bulk import advances through progress and lands on the completion summary (not the temporary dashboard landing); back does not return to progress.</human-check>
  </verify>
  <acceptance_criteria>
    - ImportProgressScreen navigates to `ImportComplete` on batch completion via navigation.replace.
    - `npx tsc --noEmit --pretty false` exits 0.
  </acceptance_criteria>
  <done>The bulk flow ends on the completion summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable session rows → reported counts | The report must reflect committed state, not a transient tally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15 | Repudiation (false report) | count computation | medium | mitigate | Counts read sessionRowCounts from durable rows; photo-only failure stays under Imported (Cluster F/T) |
| T-19-16 | Tampering (retry double-import) | Retry affordance | medium | mitigate | Retry re-runs only failed/pending rows; deterministic bypass prevents re-importing an already-linked/imported row |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/screens/ImportCompleteScreen.tsx` (summary + Retry + bridge).
- `src/screens/ImportProgressScreen.tsx`: progress→complete wiring.
- Route: `ImportComplete`.

<verification>
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: mixed-batch summary counts correct; Unbound bridge works; Retry isolates.
</verification>

<success_criteria>
Bulk import ends on an explicit durable-count summary that bridges to Unbound contacts and offers Retry without disturbing committed contacts.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-08-SUMMARY.md` when done.
</output>
