---
phase: 19-system-contact-import
plan: 07
type: execute
wave: 6
depends_on: [19-04, 19-05, 19-06, 19-10]
files_modified:
  - src/components/CandidateCardGrid.tsx
  - src/components/ConfidenceChip.tsx
  - src/screens/DuplicateReviewScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [IMP-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The bulk duplicate-review workspace is a `FlatList numColumns={2}` card grid: each card shows the incoming person's photo (rendered from a pre-resolved `photoUri`, NOT `Avatar`, when the row has a staged photo) or an initials `Avatar photo={null}` fallback, plus name + a colourless confidence chip + a one-line evidence hint; tap inspects, long-press enters multi-select, and a bottom action bar offers Link to Existing / Import as New / Skip / Apply recommendation (IMP-03, Cluster L)."
    - "A needs_review row's OWN incoming photo is a staged `import-staging/…` path (deferNeedsReviewCore never clears photo_rel_path — 19-01-PLAN.md:189). DuplicateReviewScreen resolves it via `resolveImportStagingUri` into the grid's `photoUri` and NEVER passes the raw staged path to `Avatar` (whose avatars-only `resolvePhotoUri`/`assertSafeRelative` throws SYNCHRONOUSLY on render). So the reusable bulk review grid does not crash for any ambiguous row whose picked contact has a photo — the cycle-3 single-surface fix, GENERALIZED to this sibling surface per plan 04's phase-wide staged-preview rule (addresses cycle-4 HIGH 3)."
    - "The confidence chip renders one of five enumerated advisory labels (Already in Orbit / Probable match / Possible match / New person / Needs review) on a neutral surfaceElevated background with NO semantic hue and NO raw score (Cluster H; owner: colourless)."
    - "`Apply recommendation` in bulk acts ONLY on `New person` cards (→ Import as New) and skips `Already in Orbit`; it does NOT silently bulk-link `Probable`/`Possible` cards (each Link requires an explicit per-card confirmation) and it excludes `Needs review` entirely; ambiguous cards never block already-safe imports, which remain committed while unresolved cards stay in the grid (Cluster L; addresses review MEDIUM — 'apply recommendation' must not silently link probable/possible)."
    - "Single-import interrupt: one strong candidate offers Link to Existing (recommended) / Import as New; multiple credible candidates list cards with Choose this one / Import as New / Skip and Orbit never pre-picks (Cluster I)."
    - "The bulk grid and the single interrupt render the multi-candidate set from the row's durable candidates_json (parsed by listSessionRows), not just matched_contact_id: a needs_review row with 2+ credible candidates shows every candidate for the user to choose among, and a candidate contactId that no longer resolves (contact deleted since accept) is silently skipped rather than crashing (addresses cycle-2 HIGH 2)."
    - "Every contact-creating action in this plan reaches plan 10's post-commit photo step: bulk 'Import as New' calls the shared `importRowAsNew` seam (plan 06, photo enriched by plan 10) and single-interrupt 'Import as New' calls `commitSingleImport` (plan 04, photo enriched by plan 10) — this plan never calls importContactRecord directly, so no review-grid import orphans its staged photo (addresses cycle-2 HIGH 3)."
    - "Every row-resolving action calls finalizeSessionIfTerminal(exec, sessionId, now) (plan 01) after it commits (it completes the session ONLY when no pending, needs_review, AND failed rows remain). NAVIGATION back to ImportComplete is DECOUPLED from that completion boolean: the bulk flow routes back to ImportComplete when no needs_review/pending rows remain to review (the review workspace is empty), regardless of whether the session was completed — so a session with residual `failed` rows still reaches the summary + Retry and stays resumable (revision WARNING). A single-import Link/Skip/already-linked resolution likewise finalizes its one-row session (single-import rows never enter the `failed` state, so its session completes) — so a session resolved entirely through duplicate review does not stay resumable forever unless genuine failed rows remain for Retry (addresses cycle-2 HIGH 4 + revision WARNING)."
    - "The single-import deterministic already_linked branch writes the row terminally: resolveAlreadyLinked(row.id, matchedContactId, now) (row_status='skipped' + match_outcome='already_linked' + matched_contact_id in one transaction) then finalizeSessionIfTerminal, so the one-row session completes and is counted under Already in Orbit — never left pending; the user then LANDS on the matched contact's Profile (contactId = deterministicContactId), a concrete destination like its Link/Import-as-New siblings (addresses cycle-2 LOW — deterministic single already_linked left unspecified — and cycle-3 LOW — no navigation terminus)."
    - "Bulk 'Import as New' in DuplicateReview sources its batchCategoryId (and phoneRegion) from the session row via getSessionById(sessionId) (plan 01), NOT a nav param — so the user's chosen batch category (durably persisted at setup, plan 06) survives into duplicate-review Import-as-New instead of reverting to Uncategorized (IMP-02/IMP-03; addresses cycle-3 HIGH batch-category durability)."
    - "The card grid is a standalone, route-reusable component with a generic item/action contract so Phase 20 reconciliation consumes it unchanged (UI-SPEC #4)."
    - { statement: "While duplicate scoring is in flight, the grid shows a determinate/in-progress state, not a frozen blank", verification: backstop }
    - { statement: "A single card action failure (e.g. a link write) surfaces per-card without aborting the rest of the batch", verification: backstop }
    - { statement: "A long name + many methods hold the 2-up card width in portrait (numberOfLines=1 + ellipsize, ContactCard precedent)", verification: backstop }
    - { statement: "The single-interrupt 0/1/many candidate branches each render (0 → straight to review; 1 → Link/Import-as-New; many → choose-one/import-new/skip)", verification: backstop }
    - "[ASSUMPTION | UI-SPEC unresolved E5] The review set is capped to one picker's return (≤100), so FlatList's default virtualization suffices; no additional paging/windowing is built this phase (planner decision, delegated)."
  artifacts:
    - "src/components/CandidateCardGrid.tsx — reusable 2-up grid (tap / long-press multi-select / bulk action bar) with a generic item+action contract"
    - "src/components/ConfidenceChip.tsx — colourless five-variant advisory badge"
    - "src/screens/DuplicateReviewScreen.tsx — bulk review workspace consuming the grid over a session's ambiguous rows"
    - "single-import duplicate interrupt wired into ImportReviewScreen"
    - "DuplicateReview route registered"
    - "the grid + interrupt read match_outcome / matched_contact_id / candidates_json from import_session_rows (durable, set by plan 06 driver as row_status='needs_review', or the single-import pre-create check) and resolve ATOMICALLY via importRowAsNew (bulk Import as New — contact+row+photo one seam) / commitSingleImport (single Import as New — contact+row+photo one seam) or linkExistingContactToRow (Link to Existing — link+row one txn); after each resolution the screen calls finalizeSessionIfTerminal"
    - "Link to Existing calls the plan-03 named DAO linkExistingContactToRow (NOT an inline external_contact_links INSERT, NOT a nonexistent setRowStatus); it writes the link on the chosen existing contact + resolves the row to 'linked' in ONE transaction and never overwrites contacts.name"
  prohibitions:
    - "Do not colour-code outcomes with danger/rogue/statusWobble/accent and do not add a new caution token; chips are colourless surfaceElevated (owner-locked)."
    - "Do not show raw numeric scores anywhere in the UI."
    - "Do not auto-accept `Needs review` cards in any bulk 'Apply recommendation' action."
    - "Do not pre-pick among multiple credible candidates; the user chooses (Cluster I)."
    - "Do not silently overwrite a name when linking to an existing contact (Cluster M)."
    - "Do not fork a Phase-20-specific grid; build one reusable component."
    - "Do not call importContactRecord directly for a bulk 'Import as New'; call importRowAsNew (plan 06) so the post-commit photo step (plan 10) runs — a direct importContactRecord orphans the staged photo (cycle-2 HIGH 3)."
    - "Do not skip finalizeSessionIfTerminal after a row-resolving action; call it every time (it completes the session only when no pending/needs_review/failed rows remain — a residual `failed` row legitimately keeps the session resumable for Retry, which is NOT a leak, revision WARNING) (cycle-2 HIGH 4)."
    - "Do not gate the navigation back to ImportComplete on finalize's completion boolean; route when the review workspace is empty (no needs_review/pending rows) so a session with residual failed rows still reaches its summary + Retry (revision WARNING)."
    - "Do not pass a needs_review row's staged `import-staging/…` photo_rel_path to `Avatar` (or `resolvePhotoUri`) in CandidateCardGrid/DuplicateReviewScreen — it throws SYNCHRONOUSLY on render (avatars-only). Resolve it via `resolveImportStagingUri` into the grid's pre-resolved `photoUri`; Avatar is only for `photo={null}` initials or a candidate's canonical `avatars/` master (cycle-4 HIGH 3)."
---

<objective>
Build the duplicate-resolution UI: the reusable bulk card-grid workspace, the colourless confidence chip, and the single-import interrupt — the user-facing half of IMP-03's advisory model.

Purpose: The evidence engine (plan 05) is invisible; this is where its five outcomes become tap-to-resolve cards and Link/Import/Skip choices, without ever exposing a score, borrowing a semantic hue, or auto-linking an ambiguous match. The grid is deliberately reusable for Phase 20.
Output: CandidateCardGrid + ConfidenceChip + DuplicateReviewScreen + the single-import interrupt path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/components/ContactCard.tsx
@src/components/Avatar.tsx
@src/components/OverflowMenu.tsx
@src/screens/ImportReviewScreen.tsx
@src/services/import/duplicate-evidence.ts
@src/db/import-session-read.ts
@src/db/imported-contact-dao.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor) — relies on needs_review rows plan 06 didn't produce.** Reconciled upstream: plan 06 now sets `row_status='needs_review'` on ambiguous rows; this plan's grid loads them by that status.
- **HIGH (Codex) — Link-to-Existing was two separate writes (link + row) with a crash window.** Link now calls the atomic `linkExistingContactToRow` (plan 03): link + row resolution in one transaction.
- **MEDIUM (Cursor/Claude) — unnamed link DAO + nonexistent `setRowStatus`.** Fixed to `linkExistingContactToRow`; `setRowStatus` never referenced (plan 01's surface is `setRowContact`/`markRowStatus` + cores).
- **MEDIUM (Codex) — 'Apply recommendation' policy for probable/possible.** Apply-recommendation only imports `new` and skips `already_linked`; probable/possible require explicit per-card confirmation (no silent bulk link); needs_review excluded.
- **BACKSTOP — per-card action failure isolation** and the scoring-in-progress grid state remain wired (unchanged from the original plan).

### Cycle-2 review incorporated
- **HIGH 2 (multi-candidate unrenderable).** The grid and single interrupt now read the row's durable `candidates` list (parsed from candidates_json by listSessionRows) and render every candidate, tolerating a deleted candidate contactId.
- **HIGH 3 (bulk Import-as-New bypassed the photo hook).** Bulk 'Import as New' now calls the shared `importRowAsNew` seam (plan 06, photo enriched by plan 10), never importContactRecord directly.
- **HIGH 4 (session never completed after review).** Every resolving action calls `finalizeSessionIfTerminal`; the bulk flow routes back to ImportComplete when terminal, and single-mode Link/Skip complete the one-row session.
- **LOW (single deterministic already_linked unspecified).** That branch now writes the row via `resolveAlreadyLinked` and finalizes — never left pending.

### Revision (checker WARNING — failed-row resumability)
- **finalizeSessionIfTerminal now also requires failed==0 (plan 01).** DuplicateReview's navigation back to ImportComplete is DECOUPLED from finalize's completion boolean: it routes when the review workspace is empty (no needs_review/pending rows), so a session left resumable by residual `failed` rows still reaches its summary + Retry (Cluster R durability + Cluster P partial-failure). Nav-registration also moved to the placeholder-replace model (param declared up front by plan 04).

### Cycle-3 review incorporated
- **HIGH (Cursor/Claude) — bulk category override not durable.** DuplicateReview's bulk 'Import as New' now sources `batchCategoryId` (+ phoneRegion) from `getSessionById(sessionId)` (plan 01), reading the durable value BulkImportSetup persisted (plan 06) — no reliance on a `{sessionId}`-only route param that never carried the category.
- **MEDIUM (Cursor/Claude) — wave-5 dependency gap: plan 07 called importRowAsNew whose photo body plan 10 adds, without depending on it.** Added `19-10` to `depends_on` and moved this plan to wave 6 (a plan cannot depend on a same-wave plan; plan 10 is wave 5), so bulk Import-as-New device UAT never runs before the photo seam exists. Wave graph re-verified: only plan 08 depends on plan 07, so plan 08 moves to wave 7; zero intra-wave file collisions remain.
- **LOW (Cursor/Claude) — single-import already_linked branch had no navigation terminus.** It now navigates to the matched contact's Profile after resolving the row.

### Cycle-4 review incorporated
- **HIGH 3 (Claude, new this cycle) — Avatar render crash RECURS in the bulk review grid.** `CandidateCardGrid`/`DuplicateReviewScreen` rendered a needs_review row's OWN incoming (staged `import-staging/…`) photo through `Avatar` — the exact avatars-only chokepoint the cycle-3 HIGH proved throws SYNCHRONOUSLY on render. The cycle-3 fix (`resolveImportStagingUri`) had been scoped only to ImportReviewScreen/import-acquire and never carried into the reusable grid. FIXED + GENERALIZED: `CandidateCardGrid`'s `CandidateItem` now takes a pre-resolved `photoUri` (rendered via a direct `Image`, Avatar only for `photo={null}` initials); `DuplicateReviewScreen` resolves the incoming row's staged path via `resolveImportStagingUri` before passing it in; and plan 04 now states the phase-wide rule once (no `import-staging/…` path ever reaches `Avatar`) so the third surface (plan 11's ConsolidationPrompt) is covered too. The grid stays namespace-agnostic (a Phase-20 caller resolves canonical `avatars/` masters itself), so no future consumer can reintroduce the crash by feeding a raw path to `Avatar`.

<tasks>

<task type="auto">
  <name>Task 1: ConfidenceChip + CandidateCardGrid (reusable, colourless, multi-select)</name>
  <files>src/components/ConfidenceChip.tsx, src/components/CandidateCardGrid.tsx</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Confidence Ladder table; Screen #4 grid contract; Color reserved-accent + colourless-chip rule; spacing/typography scales), src/components/ContactCard.tsx (card idiom, numberOfLines ellipsize), src/components/Avatar.tsx (line 73: renders a non-null `photo` via `resolvePhotoUri`→`assertSafeRelative`, avatars-only, throwing SYNCHRONOUSLY on render — so a staged `import-staging/…` path must NEVER reach Avatar; the grid takes a pre-resolved `photoUri` and uses Avatar only with `photo={null}`), 19-04-PLAN.md (the phase-wide staged-photo-preview rule + `resolveImportStagingUri`), src/components/OverflowMenu.tsx (Modal-sheet-over-scrim bulk-action pattern)</read_first>
  <action>ConfidenceChip: a badge taking one of the five outcomes and rendering its user-facing label (Already in Orbit / Probable match / Possible match / New person / Needs review) on `surfaceElevated` with a 13px/600 `textSecondary` label, radius 10 — NO semantic hue, NO score. CandidateCardGrid: a standalone `FlatList numColumns={2}` with a generic contract `{ items: CandidateItem[]; onInspect(item); onBulkAction(action, items); recommendationExcludes: 'needs_review' }`, where each `CandidateItem` carries a PRE-RESOLVED `photoUri: string | null` (an already-`file://`-resolved absolute URI, NOT a raw stored `photo_rel_path`) plus `name`, `outcome`, and the evidence hint. Each card renders its photo as: when `photoUri != null`, a direct `expo-image` `Image` with `source={{ uri: item.photoUri }}` and an `onError` fall-back to the initials state; when `photoUri == null`, `Avatar photo={null} name={item.name}` for the initials swatch. The card NEVER passes a non-null stored path to `Avatar` — `Avatar` (Avatar.tsx:73) calls `resolvePhotoUri`→`assertSafeRelative` (avatars-only) and throws SYNCHRONOUSLY on render for an `import-staging/…` path (cycle-4 HIGH 3). Resolving the raw path into `photoUri` is the CALLER's job (DuplicateReviewScreen uses `resolveImportStagingUri` for a staged incoming photo; a future Phase-20 caller can use `resolvePhotoUri` for a canonical `avatars/` master) — so the grid stays namespace-agnostic and reusable. Card = photo (per above) + name (15px, numberOfLines=1 ellipsize) + ConfidenceChip + one-line evidence hint (13px textSecondary), `surface` bg, `border`, radius 10, ≥44px. Long-press toggles multi-select (selected → borderStrong/accent outline per shipped selected idiom); in multi-select a bottom action bar (OverflowMenu sheet idiom) offers Link to Existing / Import as New / Skip / Apply recommendation, with Apply recommendation excluding needs_review items. The grid renders a determinate in-progress state when passed a `scoring` flag (backstop 1) and isolates a per-item action failure by surfacing an item-level error without dropping the others (backstop 2). All colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: grid shows 2-up cards with colourless chips; long-press → multi-select → bulk actions; Apply recommendation skips Needs-review cards; a long name holds the card width.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "numColumns" src/components/CandidateCardGrid.tsx` shows `numColumns={2}`.
    - The `CandidateItem` contract carries a pre-resolved `photoUri: string | null`; the card renders `photoUri` through a direct expo-image `Image` (with `onError` → initials) and uses `Avatar` only with `photo={null}` — a stored `photo_rel_path` is NEVER passed to `Avatar`. `grep -n "photoUri" src/components/CandidateCardGrid.tsx` matches; the grid resolves no raw stored paths itself (the caller supplies the already-resolved URI), so a staged path cannot reach the avatars-only resolver and crash the render (cycle-4 HIGH 3).
    - ConfidenceChip source contains no `danger`/`rogue`/`statusWobble`/`accent` token reference for the chip background and no numeric score.
    - Apply-recommendation logic excludes `needs_review` items (unit-visible via the recommendationExcludes contract).
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>A reusable, colourless, multi-select card grid + confidence chip exist with the scoring/in-progress and per-item-failure states.</done>
</task>

<task type="auto">
  <name>Task 2: DuplicateReviewScreen — bulk workspace over a session's ambiguous rows</name>
  <files>src/screens/DuplicateReviewScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>src/db/import-session-read.ts (listSessionRows parses candidates_json / sessionRowCounts — plan 01; getSessionById — the session's phone_region AND the durable batch_category_id, plan 01, the source for importRowAsNew's category), src/services/import/import-driver.ts (importRowAsNew — the shared per-row import-as-new seam with the plan-10 photo step, plan 06), src/db/imported-contact-dao.ts (linkExistingContactToRow — plan 03), src/db/import-session-dao.ts (markRowStatus + finalizeSessionIfTerminal — plan 01), src/components/CandidateCardGrid.tsx (Task 1 — its `CandidateItem.photoUri` is a pre-resolved URI, never a raw path), src/services/photos/photo-storage.ts (resolveImportStagingUri — plan 04; resolve the incoming row's staged photo HERE before passing to the grid; the phase-wide staged-photo-preview rule is in 19-04-PLAN.md), src/navigation/RootNavigator.tsx (the `DuplicateReview` ImportRoutePlaceholder registration to REPLACE; `ImportComplete` is already a registered placeholder from plan 04), src/navigation/types.ts (read-only: `DuplicateReview` param already declared by plan 04 — do not edit this file), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #4; empty state `Nothing to review` / `All picked contacts were imported.`)</read_first>
  <action>REPLACE plan 04's `DuplicateReview` themed-placeholder screen registration in RootNavigator with the real DuplicateReviewScreen (swap `component={ImportRoutePlaceholder}` for `component={DuplicateReviewScreen}` + its import). The param `DuplicateReview: { sessionId: number }` is already declared by plan 04 — add no new param key and do NOT edit navigation/types.ts. Load the session's `row_status='needs_review'` rows via listSessionRows (each row carries its parsed `candidates` list from candidates_json), feed them to CandidateCardGrid as CandidateItems. For each item, RESOLVE the top-level card photo HERE (the caller's job): the top-level card is the INCOMING picked person, whose `photo_rel_path` is a staged `import-staging/…` path (deferNeedsReviewCore never clears it — 19-01-PLAN.md:189), so set `photoUri = row.photoRelPath ? resolveImportStagingUri(row.photoRelPath) : null` and pass that to the grid — NEVER pass the raw staged path to `Avatar`, whose avatars-only `resolvePhotoUri`/`assertSafeRelative` chain throws SYNCHRONOUSLY on render for an `import-staging/…` path and would crash the review workspace for any ambiguous row whose picked contact has a photo (cycle-4 HIGH 3, the same crash class cycle 3 fixed for ImportReviewScreen). Also pass name, outcome, evidence hint, and the full candidate list. A card with 2+ candidates surfaces all of them for the user to choose among on Link (resolving each candidate's contactId to a contact for display — a candidate is an EXISTING Orbit contact whose canonical `avatars/…` photo is legal through `Avatar`/`resolvePhotoUri` — and SKIPPING any candidate whose contact no longer exists — never crash). Read the session once via `getSessionById(exec, sessionId)` (plan 01) for its phone_region AND the DURABLE `batch_category_id`, and source BOTH `batchCategoryId` and `phoneRegion` for importRowAsNew from that session row — NOT from a nav param (DuplicateReview's route param is `{sessionId}` only, and the setup override is persisted on the session, so this is where the user's chosen category comes from; cycle-3 HIGH). Wire actions: Import as New → `importRowAsNew(exec, { row, batchCategoryId: session.batchCategoryId, phoneRegion: session.phoneRegion, now })` (the plan-06 seam: contact + row resolution one txn PLUS the plan-10 post-commit photo step — do NOT bypass it with a direct create-DAO call, which would orphan the staged photo); Link to Existing → linkExistingContactToRow(exec, { rowId, contactId: chosenCandidateContactId, provider:'android', externalContactId, matchOutcome, now }) (link + linked-row resolution in one txn; NEVER updates contacts.name — Cluster M); Skip → markRowStatus(row,'skipped', null) (a USER skip: leave match_outcome as-is, which is NOT 'already_linked', so the completion summary counts it under Failed/skipped). AFTER each of these resolving actions commits, call `finalizeSessionIfTerminal(exec, sessionId, now)` (it completes the session only when no pending, needs_review, AND failed rows remain — plan 01). Then, DECOUPLED from that boolean, when no needs_review/pending rows remain to review (the review workspace is now empty), navigate back to `ImportComplete` regardless of whether the session was completed — if genuine `failed` rows remain, finalize left the session status='pending' (resumable) and ImportComplete shows the Retry affordance for them (revision WARNING — do NOT gate this navigation on the completion boolean, or a session with residual failed rows would never reach its summary + Retry). `Apply recommendation` (bulk multi-select) imports only `New person` cards via importRowAsNew and skips `Already in Orbit`; it never bulk-links probable/possible (those need an explicit per-card Link tap) and excludes needs_review; call finalizeSessionIfTerminal once after the batch of applied actions. Safe/already-imported rows are untouched (they stay committed). Empty grid → `Nothing to review` / `All picked contacts were imported.`. Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: resolve a probable card via Link to Existing (existing contact gains the external link, no name change); Import as New creates an Unbound contact; Skip leaves it out; empty state shows the copy.</human-check>
  </verify>
  <acceptance_criteria>
    - DuplicateReview REAL screen replaces plan 04's `ImportRoutePlaceholder` registration in RootNavigator (param pre-declared by plan 04; this plan adds no param key and does not edit navigation/types.ts); the grid loads rows via `row_status='needs_review'` and reads each row's parsed `candidates` list; a multi-candidate card surfaces all candidates and tolerates a deleted candidate contactId without crashing.
    - The incoming card's staged photo is resolved via `resolveImportStagingUri(row.photoRelPath)` into the grid's `photoUri`; no `import-staging/…` path reaches `Avatar`. `grep -n "resolveImportStagingUri" src/screens/DuplicateReviewScreen.tsx` matches (cycle-4 HIGH 3 — the bulk review grid does not crash on a needs_review row whose picked contact has a photo).
    - Import as New calls `importRowAsNew` (plan 06 seam, includes the photo step); `grep -n "importRowAsNew" src/screens/DuplicateReviewScreen.tsx` matches; `grep -n "importContactRecord" src/screens/DuplicateReviewScreen.tsx` returns nothing (no direct call that bypasses the photo step).
    - Import as New sources `batchCategoryId` (and phoneRegion) from `getSessionById(sessionId)`, not a nav param; `grep -n "getSessionById" src/screens/DuplicateReviewScreen.tsx` matches (cycle-3 HIGH — the user's chosen batch category survives into duplicate-review Import-as-New).
    - Link to Existing calls `linkExistingContactToRow` (atomic link+row); it does NOT UPDATE contacts.name and does NOT reference `setRowStatus`. `grep -n "linkExistingContactToRow" src/screens/DuplicateReviewScreen.tsx` matches; `grep -n "setRowStatus" src/screens/DuplicateReviewScreen.tsx` returns nothing.
    - After each resolving action the screen calls `finalizeSessionIfTerminal`; `grep -n "finalizeSessionIfTerminal" src/screens/DuplicateReviewScreen.tsx` matches, and when no needs_review/pending rows remain it navigates to `ImportComplete` — the navigation is NOT gated on finalize's completion boolean, so a session left resumable by residual `failed` rows still reaches the summary + Retry.
    - `Apply recommendation` acts only on `New person` cards; it does not bulk-link probable/possible and excludes needs_review.
    - Empty grid renders `All picked contacts were imported.`.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>The bulk workspace resolves ambiguous rows via Import-as-New (with photos) / Link / Skip without blocking safe imports or overwriting names, renders every durable candidate, and completes the session when the last row resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Single-import duplicate interrupt in ImportReviewScreen</name>
  <files>src/screens/ImportReviewScreen.tsx</files>
  <read_first>src/screens/ImportReviewScreen.tsx (Import handler from plan 04), src/services/import/duplicate-evidence.ts (scoreImportCandidate returning {outcome, candidates[], deterministicContactId} — plan 05), src/services/import/import-acquire.ts (commitSingleImport — plan 04/10, includes the photo step), src/db/imported-contact-dao.ts (linkExistingContactToRow — plan 03), src/db/import-session-dao.ts (resolveAlreadyLinked + markRowStatus + finalizeSessionIfTerminal — plan 01), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #6; Copywriting `We found someone who might already be in Orbit.`)</read_first>
  <action>Before creating on the single-import `Import` tap, run scoreImportCandidate (passing the session's phone_region). Branch on the result:
- deterministic already_linked → do not offer as new; surface 'Already in Orbit', then WRITE the row terminally via `resolveAlreadyLinked(exec, rowId, result.deterministicContactId, now)` (row_status='skipped' + match_outcome='already_linked' + matched_contact_id in one txn) and call `finalizeSessionIfTerminal(exec, sessionId, now)` so the one-row session completes and is counted under Already in Orbit (cycle-2 LOW — this branch previously left the row unspecified/pending). After the write, NAVIGATE the user to the matched existing contact's Profile (`navigation.navigate('Profile', { contactId: result.deterministicContactId })`) — surfacing the contact that is already in Orbit — so this branch has a concrete destination like its Link/Import-as-New siblings, not a dead end (cycle-3 LOW — already_linked branch had no navigation terminus).
- exactly one strong candidate → present a focused interrupt (incoming vs existing) with Link to Existing (recommended, accent) / Import as New (secondary).
- multiple credible candidates (from result.candidates) → list candidate cards with Choose this one (per card) / Import as New / Skip, with NO pre-pick.
- zero candidates → proceed straight to create (the plan-04 happy path, which already calls commitSingleImport → completeSession).
Link to Existing calls the plan-03 atomic `linkExistingContactToRow(exec, { rowId, contactId: chosenContactId, provider:'android', externalContactId, matchOutcome, now })` (link + row 'linked' one txn; no name overwrite) THEN `finalizeSessionIfTerminal(exec, sessionId, now)` (single-mode Link now completes the session — cycle-2 HIGH 4). Import as New calls `commitSingleImport` (the shared import-acquire create seam from plan 04, which persists the photo post-commit AND completes the session) — never a direct create-DAO call. Skip calls markRowStatus(row,'skipped', null) THEN finalizeSessionIfTerminal. This edits only ImportReviewScreen.tsx (owned by this plan); it calls commitSingleImport / linkExistingContactToRow / resolveAlreadyLinked / finalizeSessionIfTerminal by their fixed signatures and does not modify import-acquire.ts. Each of the 0/1/many branches renders (backstop). Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: importing a contact matching an existing one triggers the interrupt; Link vs Import-as-New behave per Cluster I; a non-matching contact imports directly.</human-check>
  </verify>
  <acceptance_criteria>
    - The Import handler calls scoreImportCandidate before writing.
    - Multiple-candidate branch offers no pre-selected/default candidate and lists candidates from result.candidates.
    - The deterministic already_linked branch writes the row via `resolveAlreadyLinked`, calls `finalizeSessionIfTerminal`, and navigates to the matched contact's `Profile` (`contactId: result.deterministicContactId`) so the branch has a destination; the Link branch calls `linkExistingContactToRow` then `finalizeSessionIfTerminal`; `grep -n "resolveAlreadyLinked\|finalizeSessionIfTerminal" src/screens/ImportReviewScreen.tsx` matches.
    - Import as New calls `commitSingleImport` (not importContactRecord directly); `grep -n "importContactRecord" src/screens/ImportReviewScreen.tsx` returns nothing.
    - Zero-candidate path preserves the plan-04 direct-create behavior.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Single import interrupts on a strong match with Link/Import choices, never pre-picks among multiples, and terminally resolves + completes the one-row session on every branch (Link / already-linked / Skip / Import-as-New).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory outcome → user link/import action | The user, not the system, authorizes any link |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Spoofing (mistaken identity) | link/import UI | high | mitigate | No auto-link; Needs-review excluded from bulk apply; no pre-pick; advisory only (Cluster I/L) |
| T-19-13 | Tampering (data loss) | Link to Existing name overwrite | high | mitigate | Link writes only an external_contact_links row; never UPDATE contacts.name (Cluster M) |
| T-19-14 | Info disclosure | raw score leakage | low | mitigate | Chip shows an enumerated label only; no numeric score in the UI |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/components/ConfidenceChip.tsx` (five colourless variants).
- `src/components/CandidateCardGrid.tsx` (reusable 2-up grid + multi-select + bulk actions; `CandidateItem.photoUri` is a pre-resolved URI rendered via a direct `Image`, never a raw path through `Avatar`).
- `src/screens/DuplicateReviewScreen.tsx` (bulk workspace; resolves each incoming row's staged photo via `resolveImportStagingUri` into the grid's `photoUri`).
- ImportReviewScreen single-interrupt path.
- Screen swap in `RootNavigator.tsx`: `DuplicateReview` real screen replaces its plan-04 placeholder (param pre-declared by plan 04; no navigation/types.ts edit).

<verification>
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: bulk grid resolution + single interrupt behave per Clusters H/I/L/M; chips colourless; no scores.
</verification>

<success_criteria>
Advisory outcomes become tap-to-resolve cards and single-import interrupts with Link/Import/Skip, colourless and score-free, never auto-linking or overwriting names, on a reusable grid.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-07-SUMMARY.md` when done.
</output>
