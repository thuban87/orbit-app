---
phase: 19-system-contact-import
plan: 07
type: execute
wave: 5
depends_on: [19-04, 19-05, 19-06]
files_modified:
  - src/components/CandidateCardGrid.tsx
  - src/components/ConfidenceChip.tsx
  - src/screens/DuplicateReviewScreen.tsx
  - src/screens/ImportReviewScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [IMP-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The bulk duplicate-review workspace is a `FlatList numColumns={2}` card grid: each card shows Avatar + name + a colourless confidence chip + a one-line evidence hint; tap inspects, long-press enters multi-select, and a bottom action bar offers Link to Existing / Import as New / Skip / Apply recommendation (IMP-03, Cluster L)."
    - "The confidence chip renders one of five enumerated advisory labels (Already in Orbit / Probable match / Possible match / New person / Needs review) on a neutral surfaceElevated background with NO semantic hue and NO raw score (Cluster H; owner: colourless)."
    - "`Apply recommendation` in bulk acts ONLY on `New person` cards (→ Import as New) and skips `Already in Orbit`; it does NOT silently bulk-link `Probable`/`Possible` cards (each Link requires an explicit per-card confirmation) and it excludes `Needs review` entirely; ambiguous cards never block already-safe imports, which remain committed while unresolved cards stay in the grid (Cluster L; addresses review MEDIUM — 'apply recommendation' must not silently link probable/possible)."
    - "Single-import interrupt: one strong candidate offers Link to Existing (recommended) / Import as New; multiple credible candidates list cards with Choose this one / Import as New / Skip and Orbit never pre-picks (Cluster I)."
    - "The card grid is a standalone, route-reusable component with a generic item/action contract so Phase 20 reconciliation consumes it unchanged (UI-SPEC #4)."
    - { statement: "While duplicate scoring is in flight, the grid shows a determinate/in-progress state, not a frozen blank", verification: backstop }
    - { statement: "A single card action failure (e.g. a link write) surfaces per-card without aborting the rest of the batch", verification: backstop }
    - { statement: "A long name + many methods hold the 2-up card width in portrait (numberOfLines=1 + ellipsize, ContactCard precedent)", verification: backstop }
    - { statement: "The single-interrupt 0/1/many candidate branches each render (0 → straight to review; 1 → Link/Import-as-New; many → choose-one/import-new/skip)", verification: backstop }
    - "[ASSUMPTION | UI-SPEC unresolved E5] The review set is capped to one picker's return (≤100), so FlatList's default virtualization suffices; no additional paging/windowing is built this phase (planner decision, delegated)."
  artifacts:
    - "src/components/CandidateCardGrid.tsx — reusable 2-up grid (tap / long-press multi-select / bulk action bar) with a generic item+action contract"
    - "src/components/ConfidenceChip.tsx — colourless five-variant advisory badge"
    - "src/screens/DuplicateReviewScreen.tsx — bulk review workspace consuming the grid over a session's ambiguous rows"
    - "single-import duplicate interrupt wired into ImportReviewScreen"
    - "DuplicateReview route registered"
    - "the grid + interrupt read match_outcome / matched_contact_id from import_session_rows (durable, set by plan 06 driver as row_status='needs_review', or the single-import pre-create check) and resolve ATOMICALLY via commitSingleImport/importContactRecord (Import as New — contact+row one txn) or linkExistingContactToRow (Link to Existing — link+row one txn)"
    - "Link to Existing calls the plan-03 named DAO linkExistingContactToRow (NOT an inline external_contact_links INSERT, NOT a nonexistent setRowStatus); it writes the link on the chosen existing contact + resolves the row to 'linked' in ONE transaction and never overwrites contacts.name"
  prohibitions:
    - "Do not colour-code outcomes with danger/rogue/statusWobble/accent and do not add a new caution token; chips are colourless surfaceElevated (owner-locked)."
    - "Do not show raw numeric scores anywhere in the UI."
    - "Do not auto-accept `Needs review` cards in any bulk 'Apply recommendation' action."
    - "Do not pre-pick among multiple credible candidates; the user chooses (Cluster I)."
    - "Do not silently overwrite a name when linking to an existing contact (Cluster M)."
    - "Do not fork a Phase-20-specific grid; build one reusable component."
---

<objective>
Build the duplicate-resolution UI: the reusable bulk card-grid workspace, the colourless confidence chip, and the single-import interrupt — the user-facing half of IMP-03's advisory model.

Purpose: The evidence engine (plan 05) is invisible; this is where its five outcomes become tap-to-resolve cards and Link/Import/Skip choices, without ever exposing a score, borrowing a semantic hue, or auto-linking an ambiguous match. The grid is deliberately reusable for Phase 20.
Output: CandidateCardGrid + ConfidenceChip + DuplicateReviewScreen + the single-import interrupt path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/components/ContactCard.tsx
@src/components/Avatar.tsx
@src/components/OverflowMenu.tsx
@src/screens/ImportReviewScreen.tsx
@src/services/import/duplicate-evidence.ts
@src/db/import-session-read.ts
@src/db/imported-contact-dao.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor) — relies on needs_review rows plan 06 didn't produce.** Reconciled upstream: plan 06 now sets `row_status='needs_review'` on ambiguous rows; this plan's grid loads them by that status.
- **HIGH (Codex) — Link-to-Existing was two separate writes (link + row) with a crash window.** Link now calls the atomic `linkExistingContactToRow` (plan 03): link + row resolution in one transaction.
- **MEDIUM (Cursor/Claude) — unnamed link DAO + nonexistent `setRowStatus`.** Fixed to `linkExistingContactToRow`; `setRowStatus` never referenced (plan 01's surface is `setRowContact`/`markRowStatus` + cores).
- **MEDIUM (Codex) — 'Apply recommendation' policy for probable/possible.** Apply-recommendation only imports `new` and skips `already_linked`; probable/possible require explicit per-card confirmation (no silent bulk link); needs_review excluded.
- **BACKSTOP — per-card action failure isolation** and the scoring-in-progress grid state remain wired (unchanged from the original plan).

<tasks>

<task type="auto">
  <name>Task 1: ConfidenceChip + CandidateCardGrid (reusable, colourless, multi-select)</name>
  <files>src/components/ConfidenceChip.tsx, src/components/CandidateCardGrid.tsx</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Confidence Ladder table; Screen #4 grid contract; Color reserved-accent + colourless-chip rule; spacing/typography scales), src/components/ContactCard.tsx (card idiom, numberOfLines ellipsize), src/components/Avatar.tsx, src/components/OverflowMenu.tsx (Modal-sheet-over-scrim bulk-action pattern)</read_first>
  <action>ConfidenceChip: a badge taking one of the five outcomes and rendering its user-facing label (Already in Orbit / Probable match / Possible match / New person / Needs review) on `surfaceElevated` with a 13px/600 `textSecondary` label, radius 10 — NO semantic hue, NO score. CandidateCardGrid: a standalone `FlatList numColumns={2}` with a generic contract `{ items: CandidateItem[]; onInspect(item); onBulkAction(action, items); recommendationExcludes: 'needs_review' }`. Each card = Avatar + name (15px, numberOfLines=1 ellipsize) + ConfidenceChip + one-line evidence hint (13px textSecondary), `surface` bg, `border`, radius 10, ≥44px. Long-press toggles multi-select (selected → borderStrong/accent outline per shipped selected idiom); in multi-select a bottom action bar (OverflowMenu sheet idiom) offers Link to Existing / Import as New / Skip / Apply recommendation, with Apply recommendation excluding needs_review items. The grid renders a determinate in-progress state when passed a `scoring` flag (backstop 1) and isolates a per-item action failure by surfacing an item-level error without dropping the others (backstop 2). All colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: grid shows 2-up cards with colourless chips; long-press → multi-select → bulk actions; Apply recommendation skips Needs-review cards; a long name holds the card width.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "numColumns" src/components/CandidateCardGrid.tsx` shows `numColumns={2}`.
    - ConfidenceChip source contains no `danger`/`rogue`/`statusWobble`/`accent` token reference for the chip background and no numeric score.
    - Apply-recommendation logic excludes `needs_review` items (unit-visible via the recommendationExcludes contract).
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>A reusable, colourless, multi-select card grid + confidence chip exist with the scoring/in-progress and per-item-failure states.</done>
</task>

<task type="auto">
  <name>Task 2: DuplicateReviewScreen — bulk workspace over a session's ambiguous rows</name>
  <files>src/screens/DuplicateReviewScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>src/db/import-session-read.ts (listSessionRows/sessionRowCounts — plan 01; the session's phone_region for scoring), src/db/imported-contact-dao.ts (importContactRecord({resolveRow}) + linkExistingContactToRow — plan 03), src/db/import-session-dao.ts (markRowStatus — plan 01), src/components/CandidateCardGrid.tsx (Task 1), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #4; empty state `Nothing to review` / `All picked contacts were imported.`)</read_first>
  <action>Register a `DuplicateReview` route (param: sessionId). Load the session's `row_status='needs_review'` rows via listSessionRows, feed them to CandidateCardGrid as CandidateItems (name, avatar, outcome, evidence hint, matched contact). Wire actions to the ATOMIC plan-03 writers: Import as New → importContactRecord({ input, externalLinks, birthday, now, resolveRow:{ rowId, matchOutcome:'new' } }) (contact + row resolution one txn); Link to Existing → linkExistingContactToRow(exec, { rowId, contactId: matchedContactId, provider:'android', externalContactId, matchOutcome, now }) (link + row 'linked' one txn; NEVER updates contacts.name — Cluster M); Skip → markRowStatus(row,'skipped', null) (a USER skip: leave match_outcome as-is, which is NOT 'already_linked', so the completion summary counts it under Failed/skipped, not Already in Orbit). `Apply recommendation` (bulk multi-select) imports only `New person` cards via Import as New and skips `Already in Orbit`; it never bulk-links probable/possible (those need an explicit per-card Link tap) and excludes needs_review. Safe/already-imported rows are untouched (they stay committed). Empty grid → `Nothing to review` / `All picked contacts were imported.`. Colours via theme tokens. Resolve every row through the plan-01/03 named writers (linkExistingContactToRow / importContactRecord / markRowStatus) — the plan-01 DAO surface exposes setRowContact + markRowStatus (+ cores), and no status-only helper by any other name exists.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: resolve a probable card via Link to Existing (existing contact gains the external link, no name change); Import as New creates an Unbound contact; Skip leaves it out; empty state shows the copy.</human-check>
  </verify>
  <acceptance_criteria>
    - DuplicateReview route registered; the grid loads rows via `row_status='needs_review'`.
    - Link to Existing calls `linkExistingContactToRow` (atomic link+row); it does NOT UPDATE contacts.name and does NOT reference `setRowStatus`. `grep -n "linkExistingContactToRow" src/screens/DuplicateReviewScreen.tsx` matches; `grep -n "setRowStatus" src/screens/DuplicateReviewScreen.tsx` returns nothing.
    - `Apply recommendation` acts only on `New person` cards; it does not bulk-link probable/possible and excludes needs_review.
    - Empty grid renders `All picked contacts were imported.`.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>The bulk workspace resolves ambiguous rows via Link/Import/Skip without blocking safe imports or overwriting names.</done>
</task>

<task type="auto">
  <name>Task 3: Single-import duplicate interrupt in ImportReviewScreen</name>
  <files>src/screens/ImportReviewScreen.tsx</files>
  <read_first>src/screens/ImportReviewScreen.tsx (Import handler from plan 04), src/services/import/duplicate-evidence.ts (scoreImportCandidate — plan 05), src/db/imported-contact-dao.ts (importContactRecord + external-link write), .planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #6; Copywriting `We found someone who might already be in Orbit.`)</read_first>
  <action>Before creating on the single-import `Import` tap, run scoreImportCandidate (passing the session's phone_region). If deterministic already_linked → do not offer as new (surface 'Already in Orbit'). If exactly one strong candidate → present a focused interrupt (incoming vs existing) with Link to Existing (recommended, accent) / Import as New (secondary). If multiple credible candidates → list candidate cards with Choose this one (per card) / Import as New / Skip, with NO pre-pick. If zero candidates → proceed straight to create (the plan-04 happy path). Link to Existing calls the plan-03 atomic `linkExistingContactToRow(exec, { rowId, contactId: chosenContactId, provider:'android', externalContactId, matchOutcome, now })` (link + row 'linked' one txn; no name overwrite); Import as New calls `commitSingleImport` (the shared import-acquire create seam from plan 04) — NOT importContactRecord directly — so the post-commit photo step plan 10 wires into commitSingleImport applies to the interrupt→Import-as-New path too. This edits only ImportReviewScreen.tsx (owned by this plan); it calls commitSingleImport / linkExistingContactToRow by their fixed signatures and does not modify import-acquire.ts. Each of the 0/1/many branches renders (backstop). Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: importing a contact matching an existing one triggers the interrupt; Link vs Import-as-New behave per Cluster I; a non-matching contact imports directly.</human-check>
  </verify>
  <acceptance_criteria>
    - The Import handler calls scoreImportCandidate before writing.
    - Multiple-candidate branch offers no pre-selected/default candidate.
    - Zero-candidate path preserves the plan-04 direct-create behavior.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Single import interrupts on a strong match with Link/Import choices and never pre-picks among multiples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory outcome → user link/import action | The user, not the system, authorizes any link |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Spoofing (mistaken identity) | link/import UI | high | mitigate | No auto-link; Needs-review excluded from bulk apply; no pre-pick; advisory only (Cluster I/L) |
| T-19-13 | Tampering (data loss) | Link to Existing name overwrite | high | mitigate | Link writes only an external_contact_links row; never UPDATE contacts.name (Cluster M) |
| T-19-14 | Info disclosure | raw score leakage | low | mitigate | Chip shows an enumerated label only; no numeric score in the UI |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/components/ConfidenceChip.tsx` (five colourless variants).
- `src/components/CandidateCardGrid.tsx` (reusable 2-up grid + multi-select + bulk actions).
- `src/screens/DuplicateReviewScreen.tsx` (bulk workspace).
- ImportReviewScreen single-interrupt path.
- Route: `DuplicateReview`.

<verification>
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: bulk grid resolution + single interrupt behave per Clusters H/I/L/M; chips colourless; no scores.
</verification>

<success_criteria>
Advisory outcomes become tap-to-resolve cards and single-import interrupts with Link/Import/Skip, colourless and score-free, never auto-linking or overwriting names, on a reusable grid.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-07-SUMMARY.md` when done.
</output>
