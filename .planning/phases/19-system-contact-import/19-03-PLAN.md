---
phase: 19-system-contact-import
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - src/logic/picked-contact-map.ts
  - src/logic/picked-contact-map.test.ts
  - src/logic/birthday-logic.ts
  - src/logic/birthday-logic.test.ts
  - src/db/contacts-dao.ts
  - src/db/imported-contact-dao.ts
  - src/db/imported-contact-dao.test.ts
autonomous: true
requirements: [IMP-02]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "picked-contact-map converts a PickedContact into a CreateContactFullInput carrying name, normalized method drafts, methodNormalization={effectivePhoneRegion}, and a birthday in stored shape; it imports only name/phones/emails/birthday/photo and drops all other OS fields (IMP-02)."
    - "A source record with no usable name yields a name-required signal (the map flags it) rather than silently creating a nameless contact (Cluster O, IMP-02)."
    - "imported-contact-dao creates an Unbound contact (trackingEnabled=false, intervalDays=null) via the EXTRACTED createContactFullCore AND inserts external_contact_links + contact_method_provenance AND (when a session row is supplied) resolves that import_session_row (contact_id + row_status) — ALL in ONE transaction; a throw rolls back the contact, links, provenance, AND the row-state transition together (IMP-02, IMP-03, IMP-04; addresses review HIGH atomic contact+session-row write)."
    - "createContactFullCore is a non-mutexed extraction of createContactFull's exact body — it seeds the full custom-field-definition pair matrix (listDefs + upsertValueCore per definition) before submitted values; both createContactFull and importContactRecord call it, so an imported contact can never be missing the def-pair matrix (addresses review MEDIUM/LOW — hand-duplicating the composition body would violate the Phase-3 custom-fields invariant)."
    - "linkExistingContactToRow writes ONE external_contact_links row on a chosen EXISTING contact + provenance + setRowContactCore(rowId, existingContactId, 'linked') in ONE transaction, and NEVER updates contacts.name (Cluster M); it is the single atomic Link-to-Existing writer plan 07 calls (addresses review MEDIUM — unnamed link DAO / nonexistent setRowStatus)."
    - "Imported birthdays route through the exported isValidStoredBirthday gate (new public wrapper over the previously-private parseStoredBirthday in birthday-logic.ts) so a calendar-invalid value (e.g. 02-30) maps to null, never a coerced date (addresses review HIGH — no exported strict birthday validator existed)."
    - "[ASSUMPTION | spec-less edge IMP-02/encoding] Method equality/canonicalization reuses contact_methods.canonical_value via normalizeContactMethod with the threaded effectivePhoneRegion — the import never invents its own equality; names compared case/whitespace-normalized downstream."
    - "[ASSUMPTION | spec-less edge IMP-03/provenance — review MEDIUM] contact_method_provenance.source_method_id is stored NULL for Phase 19 imports (the Android picker exposes no stable per-Data-row method id we persist); provenance attribution to a source RECORD is carried by external_contact_link_id, which IS populated per imported method — sufficient for plan 11 consolidation. source_method_id NULL is a documented artifact decision, not a gap."
    - "[ASSUMPTION | spec-less edge IMP-02/idempotency] Importing a source contact already deterministically linked is skippable by callers via plan 05's findActiveExternalLink; imported-contact-dao is a single-shot writer and does not create a duplicate when the caller honours the bypass."
  artifacts:
    - "src/db/contacts-dao.ts — createContactFullCore(exec, input): non-mutexed extraction of createContactFull's body (assumes open transaction); createContactFull becomes a thin inWriteTransaction wrapper around it"
    - "src/logic/birthday-logic.ts — isValidStoredBirthday(stored): boolean, an exported wrapper over the (still-private) parseStoredBirthday validity gate"
    - "src/logic/picked-contact-map.ts — mapPickedContact(picked, { categoryId, effectivePhoneRegion }): { input: CreateContactFullInput, nameRequired, externalContactId, photoTempUri }"
    - "src/db/imported-contact-dao.ts — importContactRecord() (composed Unbound create + external link(s) + provenance + optional atomic session-row resolution in one inWriteTransaction) and linkExistingContactToRow() (atomic link-existing + row resolution)"
  key_links:
    - "imported-contact-dao enters inWriteTransaction EXACTLY ONCE and composes createContactFullCore (plan-03 extract) + external_contact_links INSERTs + contact_method_provenance INSERTs + plan-01 non-mutexed row cores (setRowContactCore/setRowMatchOutcomeCore) — never nesting the non-reentrant mutex (permanent hang otherwise)"
    - "externalLinks accepts an array so plan 11 (multi-source consolidation) reuses the same writer to attach multiple links to one contact; each imported method's provenance row points at its source record's external_contact_link_id"
    - "the effectivePhoneRegion threaded here is the SAME resolveEffectivePhoneRegion value the create form uses (create-contact-logic.ts:108 wraps methodNormalization={effectivePhoneRegion}); the caller (plan 04/06) reads it from import_sessions.phone_region"
  prohibitions:
    - "Do not import postal address / employer / job title / notes / websites / social profiles / arbitrary OS metadata (dossier Cluster C — only name/phones/emails/birthday/photo)."
    - "Do not hand-copy createContactFull's body into imported-contact-dao; call the extracted createContactFullCore so the custom-value def-pair matrix seeding cannot drift (review MEDIUM/LOW)."
    - "Do not follow RESEARCH.md Pattern 2's pseudocode literally: it wraps createContactFull INSIDE inWriteTransaction, which nests the non-reentrant mutex — a permanent hang. Enter the mutex once and compose cores (review LOW anti-pattern warning)."
    - "Do not hand-roll phone canonicalization or a pairwise birthday converter; reuse normalizeContactMethod and buildBirthdayForStorage + isValidStoredBirthday."
    - "Do not use toISOString().split() for any date; timestamps are localDateTime(), birthdays keep MM-DD / YYYY-MM-DD."
    - "Do not UPDATE contacts.name in linkExistingContactToRow; linking never overwrites an existing Orbit name (Cluster M)."
---

<objective>
Build the transform + write primitives that turn an acquired snapshot into a real Unbound Orbit contact — atomically resolving the durable session row in the same transaction — plus the atomic Link-to-Existing writer, the extracted non-mutexed create core, and the exported birthday validator every later plan needs.

Purpose: These are the node-verifiable heart of every import path (single, bulk, link, consolidation). Getting the one-transaction composition (contact + methods + links + provenance + session-row resolution) right here is what makes IMP-04's resumable/no-duplicate-on-resume guarantee true, and extracting the create core is what keeps the Phase-3 custom-fields invariant intact for imported contacts.
Output: `createContactFullCore` (extract), `isValidStoredBirthday` (export), `picked-contact-map` (pure), and `imported-contact-dao` (`importContactRecord` + `linkExistingContactToRow`) — all node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/db/contacts-dao.ts
@src/db/contact-methods-dao.ts
@src/db/import-session-dao.ts
@src/db/migrations/011-contact-lifecycle-schema.ts
@src/screens/edit-contact-logic.ts
@src/screens/create-contact-logic.ts
@src/logic/birthday-logic.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor/Claude) — non-atomic contact + session-row write.** `importContactRecord` now takes an optional `resolveRow` and, when supplied, calls plan 01's non-mutexed `setRowContactCore`/`setRowMatchOutcomeCore` INSIDE its single transaction, so the contact and its session-row transition commit or roll back together. `linkExistingContactToRow` does the same for the Link-to-Existing path.
- **HIGH (Codex/Claude) — no exported strict birthday validator; `buildBirthdayForStorage` only formats.** This plan EXPORTS `isValidStoredBirthday` from birthday-logic.ts (wrapping the private `parseStoredBirthday`) and the mapper routes through it, so 02-30 → null.
- **MEDIUM (Codex/Claude) — importContactRecord hand-duplicates createContactFull's body (custom-field matrix drift risk).** This plan EXTRACTS `createContactFullCore` from `createContactFull`; both call it, so the def-pair matrix seeding cannot be dropped.
- **MEDIUM (Cursor/Claude) — Link-to-Existing has no named DAO / references a nonexistent `setRowStatus`.** Added `linkExistingContactToRow` (atomic); plan 07 calls it by name.
- **MEDIUM (Cursor/Claude) — phone region never threaded.** `mapPickedContact` accepts `effectivePhoneRegion` and sets `input.methodNormalization`.
- **MEDIUM (Codex/Cursor) — source_method_id has no source in PickedMethod.** Decided: `source_method_id` stored NULL; provenance attribution is via `external_contact_link_id`. Documented as an artifact decision (see must_haves).
- **LOW (Cursor/Codex) — RESEARCH Pattern 2 pseudocode nests the mutex.** Called out as an anti-pattern prohibition so an executor skimming RESEARCH.md doesn't copy it.

<tasks>

<task type="auto">
  <name>Task 1: Extract createContactFullCore + export isValidStoredBirthday</name>
  <files>src/db/contacts-dao.ts, src/logic/birthday-logic.ts, src/logic/birthday-logic.test.ts</files>
  <read_first>src/db/contacts-dao.ts (createContactFull body lines 112–231 — the single inWriteTransaction wrapping contact INSERT + optional first-interaction + FULL def-pair matrix seed lines 184–208 + method diff; the pre-transaction guards lines 121–146), src/db/transaction.ts (non-reentrancy), src/logic/birthday-logic.ts (private parseStoredBirthday lines 79–105 — the strict calendar validator; only daysUntilBirthday is exported today)</read_first>
  <action>In contacts-dao.ts, extract the BODY of createContactFull (everything inside its `inWriteTransaction` callback — the contact INSERT, optional first-interaction via recompute, the full definition-pair matrix seed, submitted custom values, and the method diff) into a NON-MUTEXED `createContactFullCore(exec, input)` that assumes BEGIN is already open and returns the same `{ contactId, interactionId, methods, methodSaveResult }`. Keep the two pre-transaction guards (positive intervalDays; non-future firstInteraction) in the mutexed `createContactFull`, which now becomes a thin `inWriteTransaction(exec, () => createContactFullCore(exec, input))`. Behavior and the existing createContactFull tests must be unchanged (this is a pure extraction). In birthday-logic.ts, add `export function isValidStoredBirthday(stored: string | null): boolean` that returns `parseStoredBirthday(stored) !== null` for a non-empty string and `false` for null/empty — exposing the existing strict calendar validity WITHOUT changing parseStoredBirthday. Add tests for isValidStoredBirthday covering YYYY-MM-DD valid, MM-DD valid, 02-29 year-unknown valid, 2021-02-29 invalid, 02-30 invalid, null/empty false.</action>
  <verify>
    <automated>npx vitest run src/logic/birthday-logic.test.ts src/db/contacts-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "createContactFullCore" src/db/contacts-dao.ts` shows the exported non-mutexed core; createContactFull calls it inside one inWriteTransaction.
    - The existing createContactFull test suite still passes unchanged (pure extraction — the full def-pair matrix seed is inside the core).
    - `grep -n "export function isValidStoredBirthday" src/logic/birthday-logic.ts` matches.
    - isValidStoredBirthday tests pass incl. 02-30 → false and 2021-02-29 → false.
    - `npx vitest run src/logic/birthday-logic.test.ts src/db/contacts-dao.test.ts` exits 0.
  </acceptance_criteria>
  <done>createContactFull's body is a reusable non-mutexed core; a public strict birthday validator exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: picked-contact-map — PickedContact → CreateContactFullInput (with region + birthday gate)</name>
  <files>src/logic/picked-contact-map.ts, src/logic/picked-contact-map.test.ts</files>
  <behavior>
    - name present → input.name set; methods mapped to ContactMethodDraft[] (each with a fresh uid, type, value)
    - effectivePhoneRegion passed in opts → input.methodNormalization = { effectivePhoneRegion }
    - name blank/whitespace/null → nameRequired: true and input.name is empty (caller must collect a name before create)
    - birthday "1990-05-14" → stored "1990-05-14"; year-unknown OS "--05-14" or "05-14" → stored "05-14"; "02-30" → null (via isValidStoredBirthday)
    - only phone/email methods survive; a hypothetical non-phone/email entry is dropped
    - externalContactId echoes lookupKey; photoTempUri passed through untouched
  </behavior>
  <read_first>modules/orbit-contact-picker/index.ts (PickedContact/PickedMethod — from plan 02), src/db/contacts-dao.ts (CreateContactFullInput shape lines 83–106, incl. methodNormalization), src/db/contact-methods-dao.ts (ContactMethodDraft lines 30–41), src/screens/edit-contact-logic.ts (buildBirthdayForStorage lines 121–130; parseBirthdayForForm for the --MM-DD/YYYY-MM-DD split shape), src/screens/create-contact-logic.ts (how methodNormalization={effectivePhoneRegion} is constructed lines 108–109), src/logic/birthday-logic.ts (isValidStoredBirthday — Task 1)</read_first>
  <action>Write a pure `mapPickedContact(picked, opts: { categoryId: number | null; effectivePhoneRegion: string | null })` returning `{ input: CreateContactFullInput, nameRequired: boolean, externalContactId: string, photoTempUri: string | null }`. Build `input` with `uid` (newUid), trimmed `name`, `intervalDays: null`, `trackingEnabled: false` (Unbound default), `categoryId` from opts (null = Uncategorized), `methodDrafts` mapped from picked.methods filtered to type in {'phone','email'} with a fresh uid each, and `methodNormalization: { effectivePhoneRegion: opts.effectivePhoneRegion }`. Derive the stored birthday: normalize the OS birthday (which may be `--MM-DD` for year-unknown or `YYYY-MM-DD`) into buildBirthdayForStorage's form-input + yearUnknown shape, call buildBirthdayForStorage, then GATE with isValidStoredBirthday — if invalid, birthday is null. Do NOT write birthday into contacts here (the write DAO applies it) — expose it on the returned object for the caller. Set nameRequired when the trimmed name is empty. No date uses toISOString().</action>
  <verify>
    <automated>npx vitest run src/logic/picked-contact-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover: named contact maps methods; methodNormalization carries the passed region; nameless source → nameRequired true; year-known vs year-unknown (--MM-DD) birthday; invalid birthday (02-30) → null via the exported validator; non-phone/email dropped.
    - `grep -n "toISOString" src/logic/picked-contact-map.ts` returns nothing.
    - `grep -n "isValidStoredBirthday\|methodNormalization" src/logic/picked-contact-map.ts` matches.
    - `npx vitest run src/logic/picked-contact-map.test.ts` exits 0.
  </acceptance_criteria>
  <done>A pure, tested mapper produces a create-ready Unbound input honouring the field allowlist, phone-region threading, and strict birthday storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: imported-contact-dao — atomic composed create/link + session-row resolution</name>
  <files>src/db/imported-contact-dao.ts, src/db/imported-contact-dao.test.ts</files>
  <behavior>
    - importContactRecord creates exactly one contacts row (tracking_enabled=0, interval_days NULL) via createContactFullCore, plus its normalized methods, one external_contact_links row per supplied link (provider 'android', is_active=1, fresh uid each), and one contact_method_provenance row per imported method (external_contact_link_id set, source_method_id NULL)
    - when resolveRow={rowId, matchOutcome} is supplied, the SAME transaction also calls setRowContactCore(rowId, contactId, 'imported') + setRowMatchOutcomeCore(rowId, matchOutcome, null) — one atomic unit
    - a throw mid-composition (e.g. a bad method draft) rolls back the contact AND the link(s) AND the row transition (0 rows persisted, row stays pending)
    - linkExistingContactToRow inserts one external_contact_links row on an EXISTING contact + provenance + setRowContactCore(rowId, existingContactId, 'linked') in one transaction; contacts.name is never UPDATEd
  </behavior>
  <read_first>src/db/contacts-dao.ts (createContactFullCore — Task 1; NON-REENTRANT mutex warning lines 5–15), src/db/import-session-dao.ts (non-mutexed cores setRowContactCore/setRowMatchOutcomeCore — plan 01), src/db/migrations/011-contact-lifecycle-schema.ts (external_contact_links schema lines 117–124 + active partial-unique index line 180; contact_method_provenance lines 228–233, source_method_id nullable), src/db/transaction.ts (non-reentrancy), src/db/contact-methods-dao.ts (the methodSaveResult.methods returned so provenance can map each method id)</read_first>
  <action>Write `importContactRecord(exec, { input, externalLinks, birthday, now, resolveRow })` returning `{ contactId }`. Enter inWriteTransaction EXACTLY ONCE. Inside: (1) call createContactFullCore(exec, input) to create the Unbound contact + methods + the full custom-value def matrix; (2) if birthday is non-null, UPDATE contacts.birthday within this transaction; (3) for each entry in externalLinks (default a single {provider:'android', externalContactId}), INSERT external_contact_links(uid, contact_id, provider, external_contact_id, is_active=1, created_at, modified_at) `?`-bound; (4) for each created method row, INSERT contact_method_provenance(uid, method_id, external_contact_link_id=<the link for that method's source record>, source_method_id=NULL, created_at, modified_at) — attributing each method to its source record's link; (5) if resolveRow is supplied, call setRowContactCore(exec, resolveRow.rowId, contactId, 'imported', now) and, when resolveRow.matchOutcome is given, setRowMatchOutcomeCore(exec, resolveRow.rowId, resolveRow.matchOutcome, null, now). Also write `linkExistingContactToRow(exec, { rowId, contactId, provider:'android', externalContactId, matchOutcome, now })`: ONE inWriteTransaction that INSERTs an external_contact_links row on the EXISTING contact (+ optional provenance for any newly-observed methods, still source_method_id NULL) and calls setRowContactCore(rowId, contactId, 'linked', now) + setRowMatchOutcomeCore(rowId, matchOutcome, null, now); it NEVER UPDATEs contacts.name and never creates a contact. Every value `?`-bound; externalContactId opaque. (findActiveExternalLink is owned by plan 05 — do NOT re-declare it here.)</action>
  <verify>
    <automated>npx vitest run src/db/imported-contact-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Post-import assertion: the new contacts row has tracking_enabled=0 and interval_days IS NULL; its methods + the full def-pair matrix exist; exactly one active external_contact_links row references it; each provenance row carries external_contact_link_id and source_method_id NULL.
    - Atomic-resolve test: importContactRecord with resolveRow sets the session row to imported+contact_id IN THE SAME transaction; a forced failure leaves zero contacts, zero links, AND the row still pending.
    - linkExistingContactToRow test: an existing contact gains one external link, the row becomes 'linked' with the existing contact_id, and contacts.name is unchanged.
    - `grep -n "createContactFullCore" src/db/imported-contact-dao.ts` matches (composes the core, does not hand-copy the body); `grep -c "inWriteTransaction" src/db/imported-contact-dao.ts` equals the number of exported writers (one entry each; no nesting).
    - `npx vitest run src/db/imported-contact-dao.test.ts` exits 0.
  </acceptance_criteria>
  <done>Two atomic writers produce (a) an Unbound contact with methods/links/provenance AND its resolved session row, and (b) a link on an existing contact AND its resolved row — both one transaction, no mutex nesting, no name overwrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapped snapshot → contacts/methods/links/session-row | Untrusted names/methods/birthday become durable Orbit rows and resolve the session row atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | imported-contact-dao INSERTs | high | mitigate | `?`-bound params only; opaque external_contact_id; composes createContactFullCore's validated body (V5) |
| T-19-07 | Tampering (data integrity) | birthday parse off-by-one/overflow | medium | mitigate | Reuse buildBirthdayForStorage + exported isValidStoredBirthday strict validity (rejects 02-30); no toISOString() (V5, Pitfall 6) |
| T-19-08 | DoS (feature break) | mutex non-reentrancy | high | mitigate | Enter inWriteTransaction exactly once; compose createContactFullCore + row cores; never nest (RESEARCH Pattern 2 anti-pattern flagged) |
| T-19-21 | Tampering (data loss) | crash between contact create and row resolution | high | mitigate | resolveRow updates the session row inside the SAME transaction as the contact create — no partial-commit window |
| T-19-13 | Tampering (data loss) | linkExistingContactToRow name overwrite | high | mitigate | Link writes only an external_contact_links row + row resolution; never UPDATE contacts.name (Cluster M) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces
- `src/db/contacts-dao.ts`: `createContactFullCore()` (non-mutexed extraction); `createContactFull()` now wraps it.
- `src/logic/birthday-logic.ts`: `isValidStoredBirthday()` (exported strict validator over the private parseStoredBirthday).
- `src/logic/picked-contact-map.ts`: `mapPickedContact()` (pure, region + birthday gate).
- `src/db/imported-contact-dao.ts`: `importContactRecord()` (composed create + links + provenance + optional atomic session-row resolution), `linkExistingContactToRow()` (atomic link-existing + row resolution).
- Tests: `birthday-logic.test.ts` (isValidStoredBirthday cases), `picked-contact-map.test.ts`, `imported-contact-dao.test.ts`.

<verification>
- `npx vitest run src/logic/birthday-logic.test.ts src/logic/picked-contact-map.test.ts src/db/imported-contact-dao.test.ts src/db/contacts-dao.test.ts` green.
- `npx tsc --noEmit --pretty false` and `npx biome check` on touched files pass.
</verification>

<success_criteria>
An acquired snapshot maps to a create-ready Unbound input (with region + validated birthday) and writes an atomic contact + methods + external link + provenance + resolved session row, plus an atomic Link-to-Existing writer — all node-verified, composing the extracted create core with no mutex nesting.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-03-SUMMARY.md` when done.
</output>
