---
phase: 19-system-contact-import
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logic/picked-contact-map.ts
  - src/logic/picked-contact-map.test.ts
  - src/db/imported-contact-dao.ts
  - src/db/imported-contact-dao.test.ts
autonomous: true
requirements: [IMP-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "picked-contact-map converts a PickedContact into a CreateContactFullInput carrying name, normalized method drafts, and a birthday in stored shape; it imports only name/phones/emails/birthday/photo and drops all other OS fields (IMP-02)."
    - "A source record with no usable name yields a name-required signal (the map flags it) rather than silently creating a nameless contact (Cluster O, IMP-02)."
    - "imported-contact-dao creates an Unbound contact (trackingEnabled=false, intervalDays=null) via createContactFull AND inserts external_contact_links + contact_method_provenance in ONE transaction; a throw rolls back all three together (IMP-02, IMP-03)."
    - "[ASSUMPTION | spec-less edge IMP-02/encoding] Method equality/canonicalization reuses contact_methods.canonical_value (phone via libphonenumber-js E.164, conservative email) — the import never invents its own equality; names compared case/whitespace-normalized downstream."
    - "[ASSUMPTION | spec-less edge IMP-02/precision] Birthday is stored MM-DD when the year is unknown and YYYY-MM-DD when known via buildBirthdayForStorage; a calendar-invalid birthday (e.g. 02-30) maps to null, never a coerced value (Pitfall 6)."
    - "[ASSUMPTION | spec-less edge IMP-02/idempotency] Importing a source contact already deterministically linked is skippable by callers via a provided active-link lookup; imported-contact-dao itself is a single-shot writer and does not create a duplicate when the caller honours the bypass."
  artifacts:
    - "src/logic/picked-contact-map.ts — mapPickedContact(): PickedContact → { input: CreateContactFullInput, nameRequired: boolean, externalContactId, photoTempUri }"
    - "src/db/imported-contact-dao.ts — importContactRecord(): composed Unbound create + external link(s) + provenance in one inWriteTransaction"
  key_links:
    - "imported-contact-dao must NOT nest inWriteTransaction: it calls createContactFull ONCE (one mutex entry) then adds the link/provenance INSERTs, or composes the non-mutexed cores — never both (mutex is non-reentrant, permanent hang otherwise)"
    - "externalLinks accepts an array so plan 11 (multi-source consolidation) reuses the same writer to attach multiple links to one contact"
  prohibitions:
    - "Do not import postal address / employer / job title / notes / websites / social profiles / arbitrary OS metadata (dossier Cluster C — only name/phones/emails/birthday/photo)."
    - "Do not hand-roll phone canonicalization or a pairwise birthday converter; reuse normalizeContactMethod and buildBirthdayForStorage / the daysUntilBirthday-compatible shape."
    - "Do not use toISOString().split() for any date; timestamps are localDateTime(), birthdays keep MM-DD / YYYY-MM-DD."
    - "Do not call the mutexed createContactWithInteraction/upsertValue wrappers from inside another transaction; compose the *Core variants or enter the mutex exactly once."
---

<objective>
Build the two pure/data primitives that turn an acquired snapshot into a real Unbound Orbit contact: the field mapper and the composed importer DAO.

Purpose: These are the transform + write halves of the single-contact tracer, node-verifiable without a device. Getting the one-transaction composition and the field allowlist right here is what makes every later import (single, bulk, consolidation) safe and partial-failure-friendly.
Output: `picked-contact-map` (pure) and `imported-contact-dao` (one-transaction composed writer), both node-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@src/db/contacts-dao.ts
@src/db/contact-methods-dao.ts
@src/db/migrations/011-contact-lifecycle-schema.ts
@src/screens/edit-contact-logic.ts
@src/logic/birthday-logic.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: picked-contact-map — PickedContact → CreateContactFullInput</name>
  <files>src/logic/picked-contact-map.ts, src/logic/picked-contact-map.test.ts</files>
  <behavior>
    - name present → input.name set; methods mapped to ContactMethodDraft[] (each with a fresh uid, type, value)
    - name blank/whitespace/null → nameRequired: true and input.name is empty (caller must collect a name before create)
    - birthday "1990-05-14" → stored "1990-05-14"; year-unknown "--05-14"/"05-14" → stored "05-14"; "02-30" → null
    - only phone/email methods survive; a hypothetical non-phone/email entry is dropped
    - externalContactId echoes lookupKey; photoTempUri passed through untouched
  </behavior>
  <read_first>modules/orbit-contact-picker/index.ts (PickedContact/PickedMethod — from plan 02; if not yet merged, model against RESEARCH Code Example 1), src/db/contacts-dao.ts (CreateContactFullInput shape lines 83–106), src/db/contact-methods-dao.ts (ContactMethodDraft lines 30–41), src/screens/edit-contact-logic.ts (buildBirthdayForStorage lines 121–130), src/logic/birthday-logic.ts (parseStoredBirthday validity rules)</read_first>
  <action>Write a pure `mapPickedContact(picked, opts)` returning `{ input: CreateContactFullInput, nameRequired: boolean, externalContactId: string, photoTempUri: string | null }`. Build `input` with `uid` (newUid), trimmed `name`, `intervalDays: null`, `trackingEnabled: false` (Unbound default), `categoryId` from opts (null = Uncategorized), and `methodDrafts` mapped from picked.methods filtered to type in {'phone','email'} with a fresh uid each. Derive the stored birthday by reusing buildBirthdayForStorage plus the parseStoredBirthday validity gate (reject calendar-invalid → null); do NOT write birthday into contacts here (the write DAO applies it) — expose it on the input for the caller. Set nameRequired when the trimmed name is empty. No date uses toISOString().</action>
  <verify>
    <automated>npx vitest run src/logic/picked-contact-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover: named contact maps methods; nameless source → nameRequired true; year-known vs year-unknown birthday; invalid birthday → null; non-phone/email dropped.
    - `grep -n "toISOString" src/logic/picked-contact-map.ts` returns nothing.
    - `npx vitest run src/logic/picked-contact-map.test.ts` exits 0.
  </acceptance_criteria>
  <done>A pure, tested mapper produces a create-ready Unbound input honouring the field allowlist and birthday storage rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: imported-contact-dao — composed Unbound create + external link(s) + provenance, one transaction</name>
  <files>src/db/imported-contact-dao.ts, src/db/imported-contact-dao.test.ts</files>
  <behavior>
    - importContactRecord creates exactly one contacts row with tracking_enabled=0, interval_days NULL, plus its normalized methods
    - one external_contact_links row per supplied link (provider 'android', is_active=1), each with a fresh uid
    - a throw mid-composition (e.g. a bad method draft) rolls back the contact AND the link (0 rows persisted)
    - findActiveExternalLink returns the linked contact_id for an existing active (provider, external_contact_id) or null
  </behavior>
  <read_first>src/db/contacts-dao.ts (createContactFull composition contract lines 5–48, 112–231; NON-REENTRANT mutex warning), src/db/migrations/011-contact-lifecycle-schema.ts (external_contact_links schema lines 117–124 and the active partial-unique index line 180; contact_method_provenance lines 228–233), src/db/transaction.ts</read_first>
  <action>Write `importContactRecord(exec, { input, externalLinks, birthday, now })` returning `{ contactId }`. Enter the mutex EXACTLY ONCE. Inside the single transaction, call the contacts+methods create path via the existing createContactFull composition (Unbound: trackingEnabled=false, intervalDays=null, methodDrafts from input) — but because createContactFull opens its own inWriteTransaction, either (a) call createContactFull directly and then attach links/provenance requires a second entry (forbidden), so instead (b) mirror createContactFull's transaction body by composing its underlying cores plus the INSERTs so contact + methods + links + provenance land in ONE transaction. For each entry in externalLinks (default a single {provider:'android', externalContactId}), INSERT external_contact_links(uid, contact_id, provider, external_contact_id, is_active=1, created_at, modified_at) with `?`-bound params, then thread contact_method_provenance rows linking each imported method to its link. Apply the birthday to the contact within the same transaction (an UPDATE of contacts.birthday, or via the create path). Also export `findActiveExternalLink(exec, provider, externalContactId)` returning the active-link contact_id or null (the deterministic bypass lookup). Every value `?`-bound; externalContactId opaque.</action>
  <verify>
    <automated>npx vitest run src/db/imported-contact-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Post-import assertion: the new contacts row has tracking_enabled=0 and interval_days IS NULL; its methods exist; exactly one active external_contact_links row references it.
    - Rollback test: a forced failure leaves zero contacts and zero external_contact_links rows for that import.
    - `findActiveExternalLink` returns the contact_id for a seeded active link and null otherwise.
    - `npx vitest run src/db/imported-contact-dao.test.ts` exits 0.
  </acceptance_criteria>
  <done>One atomic writer produces an Unbound contact with normalized methods, external link(s), and provenance; the deterministic-bypass lookup is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapped snapshot → contacts/methods/links | Untrusted names/methods/birthday become durable Orbit rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | imported-contact-dao INSERTs | high | mitigate | `?`-bound params only; opaque external_contact_id; reuse createContactFull's validated composition (V5) |
| T-19-07 | Tampering (data integrity) | birthday parse off-by-one/overflow | medium | mitigate | Reuse buildBirthdayForStorage + parseStoredBirthday strict validity (rejects 02-30); no toISOString() (V5, Pitfall 6) |
| T-19-08 | DoS (feature break) | mutex non-reentrancy | high | mitigate | Enter inWriteTransaction exactly once; never nest createContactFull (RESEARCH Pattern 2 / hang risk) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/logic/picked-contact-map.ts`: `mapPickedContact()` (pure).
- `src/db/imported-contact-dao.ts`: `importContactRecord()`, `findActiveExternalLink()`.
- Tests: `picked-contact-map.test.ts`, `imported-contact-dao.test.ts`.

<verification>
- `npx vitest run src/logic/picked-contact-map.test.ts src/db/imported-contact-dao.test.ts` green.
- `npx tsc --noEmit --pretty false` and `npx biome check` on touched files pass.
</verification>

<success_criteria>
An acquired snapshot maps to a create-ready Unbound input and writes an atomic contact + methods + external link + provenance, with a deterministic-bypass lookup — all node-verified.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-03-SUMMARY.md` when done.
</output>
