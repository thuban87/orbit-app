---
phase: 19-system-contact-import
plan: 05
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/services/import/duplicate-evidence.ts
  - src/services/import/duplicate-evidence.test.ts
autonomous: true
requirements: [IMP-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An active external_contact_links row matching (provider='android', external_contact_id) short-circuits to the deterministic 'already_linked' outcome carrying the matched contact_id — no advisory scoring runs (IMP-03, Cluster G)."
    - "With no deterministic hit, the weighted advisory engine maps evidence (canonical phone match, normalized email match, name similarity, birthday) to exactly one of five outcomes: already_linked | probable | possible | new | needs_review — raw scores never surface (IMP-03, Cluster H)."
    - "Name-only similarity never reaches a Link-recommending outcome; birthday is supporting evidence only and never sufficient alone; correlated evidence from the same source record is not double-counted (Cluster G, Pitfall 4)."
    - "[ASSUMPTION | spec-less edge IMP-03/adjacency] Exactly-equal canonical phone/email is a strong match signal; a bare name touch is not; weights/thresholds are top-of-file tunable constants (dossier marks them [OPEN])."
    - "[ASSUMPTION | spec-less edge IMP-03/empty] Zero candidate contacts → outcome 'new' (import as new); an input with no methods and no name defers to the caller's name-required gate, not a match."
    - "[ASSUMPTION | spec-less edge IMP-03/ordering] When multiple candidate contacts tie on evidence, the returned candidate list is stably ordered (by descending evidence then ascending contact id) so review ordering is deterministic."
  artifacts:
    - "src/services/import/duplicate-evidence.ts — scoreImportCandidate(): deterministic bypass + pure weighted advisory scorer → { outcome, recommendation, evidence[], candidates[], deterministicContactId }"
    - "Top-of-file tunable weight/threshold constants (correlated-evidence dampening, name-only ceiling, birthday-supporting cap)"
  key_links:
    - "the deterministic check reads external_contact_links via the plan-03 findActiveExternalLink (or the same query) BEFORE any scoring — an already-linked contact is identity, never fed to the fuzzy scorer"
    - "canonical phone/email evidence reads contact_methods.canonical_value (the Phase-18.1 stored match key), never a re-normalization"
  prohibitions:
    - "Do not authorize an automatic link/merge from name similarity alone, or from birthday alone (advisory only)."
    - "Do not double-count phone+email that came from the same source record as two independent strong signals."
    - "Do not expose raw numeric scores to the UI; the surface is the five enumerated outcomes + an advisory recommendation."
    - "Do not perform any generic Orbit-to-Orbit merge; this engine only classifies a source record against existing Orbit contacts."
---

<objective>
Build the conservative duplicate-evidence engine: a deterministic external-linkage bypass followed by a pure, tunable, node-tested advisory scorer that maps to five colourless user-facing outcomes.

Purpose: This is IMP-03's core. Bulk driver (plan 06), card grid + interrupts (plan 07), and consolidation (plan 11) all consume it. Keeping it pure and heavily invariant-tested is what prevents a name-only auto-link or a correlated-evidence false "probable".
Output: `duplicate-evidence.ts` with the deterministic-first classifier and tunable weights, fully node-tested including the negative invariants.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-RESEARCH.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@src/db/migrations/011-contact-lifecycle-schema.ts
@src/db/imported-contact-dao.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic bypass + candidate gathering</name>
  <files>src/services/import/duplicate-evidence.ts, src/services/import/duplicate-evidence.test.ts</files>
  <behavior>
    - active external link on (provider,external_contact_id) → outcome 'already_linked' + matched contact_id; scorer not invoked
    - no active link → gather candidate contacts by canonical phone match, normalized email match, and name token overlap (each candidate carries which signals it hit)
    - empty candidate set → outcome 'new'
  </behavior>
  <read_first>src/db/imported-contact-dao.ts (findActiveExternalLink — plan 03), src/db/migrations/011-contact-lifecycle-schema.ts (contact_methods.canonical_value line 110; external_contact_links active index line 180), .planning/phases/19-system-contact-import/19-RESEARCH.md (Pattern 3, Pitfall 4)</read_first>
  <action>Write `scoreImportCandidate(exec, { externalContactId, methodDrafts, name, birthday })`. FIRST run the deterministic check via findActiveExternalLink(provider='android', externalContactId); on a hit return `{ outcome:'already_linked', deterministicContactId, candidates:[] }` immediately. Otherwise gather candidate contacts with node:sqlite reads: contacts sharing a canonical_value with any input phone/email method (join contact_methods), and contacts whose name token-overlaps the input name (a small pure normalize+token routine, not a fuzzy npm dep). Each candidate records its hit signals (phoneMatch/emailMatch/nameOverlap/birthdayMatch). Return the raw evidence-bearing candidate list for Task 2 to weigh. All lookups `?`-bound; externalContactId opaque.</action>
  <verify>
    <automated>npx vitest run src/services/import/duplicate-evidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "active external link short-circuits before scoring" passes (scorer/spy not called on a deterministic hit).
    - Test "no candidates → new" passes.
    - Candidate rows carry per-signal flags.
    - `npx vitest run src/services/import/duplicate-evidence.test.ts` exits 0.
  </acceptance_criteria>
  <done>Deterministic identity bypasses scoring; non-deterministic inputs produce an evidence-tagged candidate set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weighted advisory ladder + negative invariants</name>
  <files>src/services/import/duplicate-evidence.ts, src/services/import/duplicate-evidence.test.ts</files>
  <behavior>
    - canonical phone match (distinct source) → 'probable' (recommend Link to Existing)
    - weak/single soft signal → 'possible' (recommend Review, no auto-pick)
    - name-only overlap → never above 'possible'/'needs_review'; never a Link recommendation
    - phone+email from the SAME source record → counted once, not two independent strong signals
    - birthday present but no phone/email/strong signal → supporting only; cannot alone produce 'probable'
    - multiple credible candidates → 'needs_review' with a stably-ordered candidate list
  </behavior>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Confidence Ladder table — five outcomes + advisory copy), .planning/phases/19-system-contact-import/19-RESEARCH.md (Pitfall 4 invariants)</read_first>
  <action>Add top-of-file tunable constants: per-signal weights, a correlated-evidence dampening rule (methods from one source record contribute one combined weight), a name-only ceiling (name similarity alone caps below the Link threshold), a birthday-supporting cap (birthday adds only a small increment and never crosses the threshold alone), and the probable/possible thresholds. Compute a total per candidate, then classify into the five outcomes and attach the advisory recommendation copy (Recommend Link to Existing / Review / Import as New / Manual Review Required) per the UI-SPEC table. When 2+ candidates are credible, return 'needs_review' with the candidate list ordered by descending evidence then ascending contact id (stable). Encode the negative invariants as explicit tests (a name-only match must never yield a Link recommendation; two same-source methods must not produce a stronger outcome than one).</action>
  <verify>
    <automated>npx vitest run src/services/import/duplicate-evidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Invariant tests pass: name-only never recommends Link; same-source phone+email not double-counted; birthday-alone never 'probable'.
    - Every returned outcome is one of the five enumerated strings; no numeric score is on the returned object.
    - Multiple-candidate case returns a stably-ordered list and 'needs_review'.
    - `npx vitest run src/services/import/duplicate-evidence.test.ts` exits 0.
  </acceptance_criteria>
  <done>The advisory ladder maps evidence to five outcomes with the conservative invariants enforced by tests, weights tunable at the top of the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source record → identity classification | An advisory misjudgment must never silently link/merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Spoofing (mistaken identity) | advisory scorer | high | mitigate | Deterministic-first bypass; name-only never auto-links; birthday supporting-only; recommendations advisory (Cluster G/H) |
| T-19-01 | Tampering | candidate SQL reads | medium | mitigate | `?`-bound reads; opaque external id; canonical_value is the stored key (V5) |
| T-19-11 | Repudiation (silent action) | outcome → action | medium | mitigate | Engine only classifies; it never writes a link/merge — write decisions belong to the review UI |
| T-19-SC | Tampering | npm installs | high | accept | No package installs (name similarity hand-rolled per RESEARCH) |
</threat_model>

## Artifacts this plan produces

- `src/services/import/duplicate-evidence.ts`: `scoreImportCandidate()` + tunable weight/threshold constants + the five-outcome classifier + advisory copy mapping.
- Test: `duplicate-evidence.test.ts` (deterministic bypass, ladder, negative invariants).

<verification>
- `npx vitest run src/services/import/duplicate-evidence.test.ts` green including the three negative invariants.
- `npx tsc --noEmit --pretty false` and `npx biome check` pass.
</verification>

<success_criteria>
A pure, tunable engine classifies a source record deterministically-first then advisory, into five colourless outcomes, with name-only/birthday-only/correlated-evidence guards proven by tests.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-05-SUMMARY.md` when done.
</output>
