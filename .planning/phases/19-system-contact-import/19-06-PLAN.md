---
phase: 19-system-contact-import
plan: 06
type: execute
wave: 3
depends_on: [19-01, 19-03, 19-04, 19-05]
files_modified:
  - src/services/import/import-driver.ts
  - src/services/import/import-driver.test.ts
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportProgressScreen.tsx
  - src/navigation/RootNavigator.tsx
  - src/navigation/types.ts
autonomous: true
requirements: [IMP-02, IMP-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After the picker returns N>1 selections, bulk setup shows a selection count, the two locked read-only defaults (Unbound, Uncategorized), a single batch Category override, and one `Import N contacts` CTA — and NO per-person checkbox/exclusion list (IMP-02, Cluster E)."
    - "The chunked driver processes rows incrementally in one logical import, each row in its own transaction; a single row failure (or photo failure) marks that row failed/photo_failed and the batch continues — it never rolls back the whole batch (IMP-02/IMP-04, Cluster P)."
    - "Before importing, each row is scored by the duplicate engine: deterministic 'already_linked' rows are skipped (reported, not refreshed), 'new' rows import as Unbound, and ambiguous rows (probable/possible/needs_review) are left unimported for the card grid — safe imports are never blocked by ambiguous ones (IMP-03, Cluster L/N)."
    - "The progress screen shows determinate `Importing… X of N` (accent tint) plus a sub-status ('Photos may finish after contacts'), never a bare spinner, and never exposes chunk internals as separate imports (Cluster S)."
    - { statement: "Bulk `Import N contacts` and progress copy pluralize correctly (1 contact vs N contacts)", verification: backstop }
    - "[ASSUMPTION | spec-less edge IMP-02/boundary] The platform picker caps a single selection at ≤100 (default 50); Orbit imposes NO app-level cap — a user importing more runs the picker again; the progress screen is sized for one pick's N, not thousands from one call (Pitfall 5)."
  artifacts:
    - "src/services/import/import-driver.ts — runImportBatch(): score → classify → chunked per-row transactional import with progress callback + partial-failure accounting"
    - "src/screens/BulkImportSetupScreen.tsx — shared-defaults setup + batch category override"
    - "src/screens/ImportProgressScreen.tsx — determinate one-logical-import progress"
    - "BulkImportSetup + ImportProgress routes registered"
  key_links:
    - "the driver calls scoreImportCandidate (plan 05) per row and setRowMatchOutcome (plan 01) so outcomes are durable for the grid and completion counts"
    - "each safe import is one importContactRecord call (plan 03) = one transaction; photo persistence is deferred to plan 10 (post-commit), so a photo failure here only sets photo_failed"
  prohibitions:
    - "Do not render a per-person checkbox/exclusion list under bulk defaults (Cluster E)."
    - "Do not wrap the batch in one giant transaction; one transaction per contact (Cluster P)."
    - "Do not block safe imports on ambiguous candidates; ambiguous rows stay pending for review while safe rows import."
    - "Do not set EXTRA_PICK_CONTACTS_SELECTION_LIMIT above 100 or design progress UI implying one pick returns thousands."
    - "Do not seed/select an 'Uncategorized' category; the batch default is category_id null."
---

<objective>
Deliver bulk import: the chunked, partial-failure-safe driver plus the lightweight shared-defaults setup and the determinate progress screen — the IMP-02 batch path, layered on the verified single-contact tracer.

Purpose: Bulk is intentionally lighter than single review (per-person selection already happened in the system picker). The driver's per-row transaction + duplicate-first classification gives Cluster P/L/N safety; the setup/progress screens keep it one calm logical import.
Output: import-driver service + BulkImportSetupScreen + ImportProgressScreen, wired to plan 01/03/05 primitives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@src/services/import/import-acquire.ts
@src/services/import/duplicate-evidence.ts
@src/db/imported-contact-dao.ts
@src/db/import-session-dao.ts
@src/db/import-session-read.ts
@src/screens/CreateContactScreen.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: import-driver — chunked, duplicate-first, partial-failure batch import</name>
  <files>src/services/import/import-driver.ts, src/services/import/import-driver.test.ts</files>
  <behavior>
    - runs rows in chunks of CHUNK_SIZE (top-of-file tunable), emitting onProgress(done, total)
    - per row: score → already_linked ⇒ markRowStatus 'skipped' (reported); new ⇒ importContactRecord + setRowContact 'imported'; ambiguous ⇒ setRowMatchOutcome + row stays 'needs_review'
    - a thrown importContactRecord marks that row 'failed' with a reason; the loop continues (no batch rollback)
    - final accounting returns { imported, alreadyLinked, needReview, failed }
  </behavior>
  <read_first>src/services/import/duplicate-evidence.ts (scoreImportCandidate — plan 05), src/db/imported-contact-dao.ts (importContactRecord — plan 03), src/db/import-session-dao.ts (markRowStatus/setRowContact/setRowMatchOutcome — plan 01), src/db/import-session-read.ts (listSessionRows/sessionRowCounts — plan 01), .planning/phases/19-system-contact-import/19-RESEARCH.md (Code Example 3 chunked driver, Cluster P)</read_first>
  <action>Write `runImportBatch(exec, { sessionId, batchCategoryId, now, onProgress })`. Load pending rows via listSessionRows, iterate in chunks of CHUNK_SIZE with a yield between chunks (keep the JS thread responsive — Cluster S). For each row: call scoreImportCandidate; if 'already_linked' → markRowStatus(row,'skipped') (Cluster N: skipped not refreshed); if 'new' → mapPickedContact input with batchCategoryId (Unbound) → importContactRecord in its own transaction → setRowContact(row, contactId, 'imported'); else (probable/possible/needs_review) → setRowMatchOutcome(row, outcome, matchedContactId) and leave the row pending for the grid. Wrap each per-row import in try/catch → on throw markRowStatus(row,'failed', classify(err)); the batch continues. Call onProgress after each row. Return the final counts from sessionRowCounts. All within existing transaction primitives (never one batch transaction).</action>
  <verify>
    <automated>npx vitest run src/services/import/import-driver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test with a mixed batch (one new, one already-linked, one ambiguous, one that throws) asserts: 1 imported, 1 skipped, 1 needs_review, 1 failed, and that the failure did not roll back the imported contact.
    - onProgress is called total times; a fixture asserts monotonically increasing done up to total.
    - `grep -n "CHUNK_SIZE" src/services/import/import-driver.ts` matches a top-of-file const.
    - `npx vitest run src/services/import/import-driver.test.ts` exits 0.
  </acceptance_criteria>
  <done>The driver imports safe rows incrementally, skips already-linked, defers ambiguous, isolates failures, and reports counts — all node-verified.</done>
</task>

<task type="auto">
  <name>Task 2: BulkImportSetupScreen — shared defaults + batch category override</name>
  <files>src/screens/BulkImportSetupScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #3; Copywriting: `Import N contacts`; accent-selected chip idiom), src/screens/CreateContactScreen.tsx (pickerShell category select idiom), src/navigation/RootNavigator.tsx (Stack.Screen registration), src/navigation/types.ts, src/screens/use-import-leave-guard.ts (plan 04)</read_first>
  <action>Register a `BulkImportSetup` route (param: sessionId). The screen shows: the selection count `N contacts selected` (pluralized), the two locked read-only affordances `Unbound` and `Uncategorized`, and a single batch Category override control (Picker or accent-selected chip set) that resolves to a category_id or null. One primary CTA `Import N contacts` (accent, pluralized) that navigates to ImportProgress with the chosen batchCategoryId. NO per-person list. Reuse the leave-guard so an edited setup back-out confirms. All colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: after a multi-pick, setup shows count + two read-only defaults + one category override + `Import N contacts`; no per-person list.</human-check>
  </verify>
  <acceptance_criteria>
    - BulkImportSetup route registered in RootStackParamList + RootNavigator.
    - Screen has no per-row checkbox/list of contacts.
    - CTA text interpolates the count and pluralizes (1 contact vs N contacts).
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Bulk setup applies shared Unbound + Uncategorized defaults with a single batch category override and no per-person list.</done>
</task>

<task type="auto">
  <name>Task 3: ImportProgressScreen — determinate one-logical-import progress</name>
  <files>src/screens/ImportProgressScreen.tsx, src/navigation/RootNavigator.tsx, src/navigation/types.ts</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #7; Copywriting `Importing… X of N`), src/services/import/import-driver.ts (runImportBatch + onProgress — Task 1)</read_first>
  <action>Register an `ImportProgress` route (params: sessionId, batchCategoryId). On mount, run runImportBatch with an onProgress that updates a determinate progress line/bar (accent tint) showing `Importing… X of N` plus the sub-status `Photos may finish after contacts`. On completion, navigate to ImportComplete (route owned by plan 08; until then, navigate back to the dashboard/Unbound as a temporary landing — plan 08 replaces this target). Never a bare spinner; never surface chunk internals as separate imports. Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: a multi-contact import shows determinate X of N advancing to N, then lands (Unbound/dashboard until plan 08 wires completion).</human-check>
  </verify>
  <acceptance_criteria>
    - ImportProgress route registered; the screen renders `Importing… X of N` (determinate), not a spinner-only view.
    - The screen calls runImportBatch exactly once per mount.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>One logical bulk import shows determinate progress and completes without exposing chunk internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch of untrusted rows → contacts | Each row is validated + scored before it can become a contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-12 | DoS | oversized batch / giant transaction | high | mitigate | Chunked per-row transactions with yields; no single batch transaction; picker cap ≤100 per pick (Cluster P/S) |
| T-19-10 | Spoofing (mistaken identity) | ambiguous auto-import | high | mitigate | Ambiguous rows are deferred, never auto-imported; only 'new'/deterministic decisions write (Cluster L/N) |
| T-19-01 | Tampering | per-row SQL | high | mitigate | Reuses `?`-bound importContactRecord + session DAO (V5) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/services/import/import-driver.ts`: `runImportBatch()` + `CHUNK_SIZE`.
- `src/screens/BulkImportSetupScreen.tsx`, `src/screens/ImportProgressScreen.tsx`.
- Routes: `BulkImportSetup`, `ImportProgress`.
- Test: `import-driver.test.ts`.

<verification>
- `npx vitest run src/services/import/import-driver.test.ts` green (mixed-batch partial-failure case).
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: multi-contact import runs incrementally, skips already-linked, defers ambiguous.
</verification>

<success_criteria>
Bulk import applies shared defaults, imports incrementally with per-row failure isolation, skips already-linked, defers ambiguous to review, and shows one determinate logical-import progress.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-06-SUMMARY.md` when done.
</output>
