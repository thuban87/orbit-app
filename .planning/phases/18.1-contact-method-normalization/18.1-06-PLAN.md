---
phase: 18.1-contact-method-normalization
plan: 06
type: execute
wave: 4
depends_on: [18.1-01, 18.1-02, 18.1-03, 18.1-04, 18.1-05]
files_modified: [.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/migrations/009-contact-method-normalization.test.ts, src/db/contact-methods-dao.test.ts, scripts/audit-scalar-method-refs.mjs]
autonomous: true
requirements: [CDN-01, CDN-03, CDN-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Automated tests cover every 18.1 requirement's migration, method-DAO, method-read, and method-backup invariants."
    - "CDN-01 manual parser/migration edge review and the UI-SPEC method device backstops are completed on a real upgraded Android build."
    - "No PII method data reaches routine logs, and all OS handoff is actionability-gated."
  artifacts: [.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/contact-methods-dao.test.ts]
  key_links: ["The final 18.1 ledger maps every scalar-method consumer to its owner test, and the release build upgrades real v8 data before exercising method DAO/UI/backup round-trip against the completed v9 model."]
  prohibitions:
    - "Phase 18 must not ship picker/import/reconciliation, generic sync, rich merge management, passive listener detection, reorder polish, Interaction Assist, endpoint history, or provider analytics."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-08-PLAN.md` — **Task 1** (automated verification, scoped to method concerns) plus a **new 18.1 owner-led Android MIGRATION UAT** on the Pixel with a real v8 backup.

**Moved to 18.2-10:** the lifecycle UAT (old 18-08 Task 2's Bound/Unbind, Unbound screens, Orrery, notification/widget lifecycle, Streamlined Export) and every lifecycle/query-consumer test owner (`ai-context-read`, `digest-read`, `contact-status-read`, `impact`, `contact-lifecycle-dao`, `CaptureScreen`). This plan is scoped to **migration + method reads + method backup round-trip only** — the critical on-device proof of the irreversible v9 migration against real data.

<objective>Prove the 18.1 method-normalization half on its automated suite and a real Android migration/method handoff.
Purpose: backstop state-space and device-only behavior that a node SQLite or RN logic test cannot establish for the irreversible v9 migration.
Output: completed method edge audit and owner-approved migration UAT record.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-VALIDATION.md
@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/18.1-contact-method-normalization/18.1-01-SUMMARY.md
@.planning/phases/18.1-contact-method-normalization/18.1-04-SUMMARY.md</context>

## Artifacts this phase produces

- Requirement-mapped regression coverage and scalar-method source-retirement audit.
- Owner-reviewed Android v8 upgrade/migration UAT evidence.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove one upgraded contact across migration, method reads, and backup round-trip</name>
  <read_first>18-VALIDATION.md; 18-RESEARCH.md Validation Architecture; src/db/migrations/009-contact-method-normalization.test.ts; src/backup/restore-apply.test.ts</read_first>
  <files>.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/migrations/009-contact-method-normalization.test.ts, src/db/contact-methods-dao.test.ts, scripts/audit-scalar-method-refs.mjs</files>
  <behavior>
    - Requirement test matrix includes CDN-01 manual/probe marker; CDN-03 boundary/precision; CDN-04 boundary/precision/idempotency/concurrency on the method backup graph.
    - An EXECUTABLE audit artifact `scripts/audit-scalar-method-refs.mjs` (a real script, not a prose table) enumerates every production `contacts.phone`/`contacts.email` and scalar `phone`/`email` aggregate-contract reference and maps each to its migrated owner; it scans CODE ONLY, not comments — `src/ai/prompt-types.ts:93` and `src/db/ai-context-read.ts:13` name `phone`/`email` inside comments describing AI-context exclusions and must NOT trip the gate (LOW-2, cycle 4) — and it EXITS NON-ZERO on any production scalar-method reference with no migrated owner, allowlisting only the named migration v8 fixtures and the v1 backup forward-migration fixture. `<verify>` runs it so the gate fails on a stray reference instead of degrading to a markdown table.
    - `contact-methods-dao.test.ts` owns the logging-boundary spy: invalid, collision, and failed-save cases do not pass raw or canonical method values to Logger.
  </behavior>
  <action>Fill the Phase 18.1 validation matrix with final plan/task/wave ownership and add an EXECUTABLE audit artifact `scripts/audit-scalar-method-refs.mjs` (a runnable script, not a prose ledger). It must enumerate every `contacts.phone`, `contacts.email`, and scalar `phone`/`email` aggregate-contract reference found in production, map each to its migrated owner, and EXIT NON-ZERO when a production scalar-method reference has no migrated owner — scanning CODE ONLY (not comments; `src/ai/prompt-types.ts:93` and `src/db/ai-context-read.ts:13` mention the scalar columns in exclusion comments and must not trip it; LOW-2), allowlisting only the named migration v8 fixtures and the v1 backup forward-migration fixture. Wire it into `<verify>` so the gate actually runs it and fails on a stray reference; it must not degrade to a markdown table. Extend the logging-boundary test owner: spy on Logger through invalid/collision/failed method saves and assert no raw/canonical method value is passed. Add boundary assertions for the malformed-cadence migration clamp-plus-`field_history`-snapshot (M1, replacing the old rollback behavior; assert the exact clamped value 30 and the exact `field_history` row) and for the FK-safe rebuild's per-child row-count preservation across ALL SIX children including app_settings (HIGH-1: assert app_settings' `sun_contact_id` FK target is the new `contacts` and its exact non-null value survives). Assert `canonical_region` is recorded for a canonicalized national number and NULL for a stored non-actionable value (HIGH-5). As immediate feedback, run the named targeted tests first; only after they pass, run the full suite, TypeScript, and colors gate. Treat the spec-less edge probe as a coverage ledger: write defensible automated predicates, retain device-only items as explicit backstops, and flag no unresolved material assumptions. Preserve D-01 through D-04 and scope fences rather than broadening into deferred functionality. The nullable-cadence, orbiting-set, and favourite-set consumer ledger and the explicit-Unbound AI/status/digest coverage are owned by 18.2-10.</action>
  <verify><automated>First run `npm test -- src/db/migrations/009-contact-method-normalization.test.ts src/db/contact-methods-dao.test.ts`; only after it passes, run `node scripts/audit-scalar-method-refs.mjs && npm test && npx tsc --noEmit && npm run check:colors`.</automated></verify>
  <acceptance_criteria>CDN-01, CDN-03, and CDN-04 have executable evidence; the named targeted suite passes before the full regression gate; the executable `scripts/audit-scalar-method-refs.mjs` runs in `<verify>` and exits non-zero on any unowned production scalar-method reference (proving every consumer is migrated or explicitly fixture-allowlisted); Logger receives no raw/canonical method data; and all requirement-specific quick commands pass.</acceptance_criteria>
  <done>The 18.1 codebase has one traceable automated verification path for every method-normalization truth.</done>
</task>
<task type="auto" gate="none">
  <name>Task 2: Executor-driven on-device migration and method UAT (via ADB)</name>
  <read_first>18-VALIDATION.md Manual-Only Verifications; 18-UI-SPEC.md UI Considerations; docs/runbooks/desktop-build-pipeline.md; CLAUDE.md "Android device — you can drive the app yourself" (ADB/emu-connect/run-as)</read_first>
  <action>The owner directed that this UAT is EXECUTOR-DRIVEN over ADB, not a human checkpoint (the Pixel is connected at execution time). Build the release APK on the desktop pipeline (`ssh droid`, per the desktop-build-pipeline runbook; transport by rsync/scp, never git push), then connect with `emu-connect` and resolve the serial from `~/.local/bin/adb devices` (do NOT hardcode it; confirm exactly one device). Install the build over an UPGRADED v8 FIXTURE database (not the owner's real data) and drive the checks below via `run-as com.bwales.orbit` SQLite queries + `uiautomator dump`/`input`/`screencap`. **The app package is `com.bwales.orbit`** (owner-confirmed in `app.config.ts`). CRITICAL: `run-as` works ONLY on a DEBUGGABLE build, so the DB-query verification must run against a debug/dev-client variant (the migration logic is identical across variants, so this validly proves migration correctness); a pure release APK is non-debuggable and `run-as` will fail on it. Also note `android.allowBackup=false` in `app.config.ts`, so `adb backup` cannot pull the DB — `run-as` on a debuggable build is the only path. If the device is absent, only a non-debuggable build is available, or `run-as` fails, STOP and surface it to the owner rather than skipping the UAT. Capture query outputs + screenshots as evidence into `18.1-VALIDATION.md`/the SUMMARY so the owner can glance at the result. Bind/Unbind, Unbound screens, Orrery lifecycle, notifications/widgets, and Streamlined Export are 18.2's, not here.</action>
  <what-built>Full normalized-method migration, method editor/profile, and method backup/restore on a release build, verified on the Pixel by the executor.</what-built>
  <how-to-verify>Verify DATA by querying the migrated SQLite through `run-as <package>` (a rendered screen is NOT proof the data is correct): (1) `contact_methods` holds the migrated per-type primaries; existing contacts whose stored numbers are valid for the DEVICE region are actionable (assert the actionable/canonical fields are populated) — device-region parsing (H4) must NOT strip Text from the install base; a row is non-actionable only when it genuinely fails validation. (2) `app_settings.sun_contact_id` survives the rebuild at its exact pre-migration value (sixth FK child, HIGH-1) and points at the new `contacts`; then exercise an `app_settings` write (toggle a setting via UI) and confirm no `no such table: contacts_old` error in logcat. (3) A malformed legacy cadence (seed one in the fixture) did not wedge the upgrade — the app opens and the value is clamped to 30 with the original in `field_history` (M1). (4) Drive the UI (`uiautomator dump` + `input tap`) to create/edit invalid/extension/multiple methods; confirm Compose offers SMS only for an actionable primary and shows "Add a phone number" only when there is no actionable primary. (5) Run backup export then restore and query the DB: methods/extensions/primary/order preserved, per-method `canonical_region` SURVIVES a Replace-all restore (NOT reset to NULL; HIGH-3), `phone_region_override` round-trips; and the "changed primary phone" merge-restore case succeeds (no whole-restore rejection, no mid-transaction unique-index failure; HIGH-1/HIGH-2/HIGH-4). UI backstops moved here from node-untestable tests (MED-2): from `uiautomator dump` bounds, assert the ContactMethodsEditor controls meet the 44px touch-target minimum and expose accessibility labels; check 320dp overflow, long text, keyboard scroll, loading/error retry, and that no raw canonical/source field is shown. Perf claims (if any) are legitimate only on the physical Pixel, not an emulator, and cover the thread the evidence measures. Record PASS/FAIL with evidence per item.</how-to-verify>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>The executor records, with on-device evidence (query outputs + screenshots), a PASS for every method-related on-device validation item and UI backstop — migrated methods, actionability not stripped, `sun_contact_id` preserved, no `contacts_old` write failure, cadence clamp, Compose actionability, and the `canonical_region`/changed-primary restore cases — or STOPS and surfaces a FAIL to the owner. No method/history loss; no deferred import/reconciliation UI.</acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release build to real device database | Migration and method UI execute against user-like persisted relationship data. |
| actionability read to Android SMS | Device handoff can expose or use a contact endpoint. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-24 | Tampering/DoS | release migration | high | mitigate | Real v8 upgrade UAT after fixture/rollback tests and backup verification. |
| T-18-25 | Information disclosure | UI/logs | high | mitigate | Device check plus automated absence/actionability assertions; no canonical/raw source display. |
| T-18-26 | Elevation | Android handoff | high | mitigate | Exercise only actionable-primary Compose path and invalid fallback. |
| T-18-SC | Tampering | package provenance | high | mitigate | Confirm the 18.1-01 blocking human approval remains recorded. |
</threat_model>
<verification>Run the named targeted Task 1 suite as immediate feedback, then run the full suite before release UAT; final human checkpoint records device-only predicates and unresolved defects for the method half.</verification>
<success_criteria>Every 18.1 requirement is code-proven and the owner has seen the real Android migration and method behavior.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-06-SUMMARY.md` when done</output>
