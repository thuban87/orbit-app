---
phase: 18.1-contact-method-normalization
plan: 06
type: execute
wave: 4
depends_on: [18.1-02, 18.1-03, 18.1-04, 18.1-05]
files_modified: [.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/migrations/009-contact-method-normalization.test.ts, src/db/contact-methods-dao.test.ts]
autonomous: false
requirements: [CDN-01, CDN-03, CDN-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Automated tests cover every 18.1 requirement's migration, method-DAO, method-read, and method-backup invariants."
    - "CDN-01 manual parser/migration edge review and the UI-SPEC method device backstops are completed on a real upgraded Android build."
    - "No PII method data reaches routine logs, and all OS handoff is actionability-gated."
  artifacts: [.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/contact-methods-dao.test.ts]
  key_links: ["The final 18.1 ledger maps every scalar-method consumer to its owner test, and the release build upgrades real v8 data before exercising method DAO/UI/backup round-trip against the completed v9 model."]
  prohibitions:
    - "Phase 18 must not ship picker/import/reconciliation, generic sync, rich merge management, passive listener detection, reorder polish, Interaction Assist, endpoint history, or provider analytics."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-08-PLAN.md` — **Task 1** (automated verification, scoped to method concerns) plus a **new 18.1 owner-led Android MIGRATION UAT** on the Pixel with a real v8 backup.

**Moved to 18.2-10:** the lifecycle UAT (old 18-08 Task 2's Bound/Unbind, Unbound screens, Orrery, notification/widget lifecycle, Streamlined Export) and every lifecycle/query-consumer test owner (`ai-context-read`, `digest-read`, `contact-status-read`, `impact`, `contact-lifecycle-dao`, `CaptureScreen`). This plan is scoped to **migration + method reads + method backup round-trip only** — the critical on-device proof of the irreversible v9 migration against real data.

<objective>Prove the 18.1 method-normalization half on its automated suite and a real Android migration/method handoff.
Purpose: backstop state-space and device-only behavior that a node SQLite or RN logic test cannot establish for the irreversible v9 migration.
Output: completed method edge audit and owner-approved migration UAT record.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-VALIDATION.md
@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@docs/runbooks/desktop-build-pipeline.md
@.planning/phases/18.1-contact-method-normalization/18.1-01-SUMMARY.md
@.planning/phases/18.1-contact-method-normalization/18.1-04-SUMMARY.md</context>

## Artifacts this phase produces

- Requirement-mapped regression coverage and scalar-method source-retirement audit.
- Owner-reviewed Android v8 upgrade/migration UAT evidence.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove one upgraded contact across migration, method reads, and backup round-trip</name>
  <read_first>18-VALIDATION.md; 18-RESEARCH.md Validation Architecture; src/db/migrations/009-contact-method-normalization.test.ts; src/backup/restore-apply.test.ts</read_first>
  <files>.planning/phases/18.1-contact-method-normalization/18.1-VALIDATION.md, src/db/migrations/009-contact-method-normalization.test.ts, src/db/contact-methods-dao.test.ts</files>
  <behavior>
    - Requirement test matrix includes CDN-01 manual/probe marker; CDN-03 boundary/precision; CDN-04 boundary/precision/idempotency/concurrency on the method backup graph.
    - An evidence-backed rg ledger maps every production scalar-method (`contacts.phone`, `contacts.email`, scalar `phone`/`email` aggregate contract) consumer to its owning task/test; it fails on an unassigned reference outside explicitly listed migration/backup forward-compatibility fixtures.
    - `contact-methods-dao.test.ts` owns the logging-boundary spy: invalid, collision, and failed-save cases do not pass raw or canonical method values to Logger.
  </behavior>
  <action>Fill the Phase 18.1 validation matrix with final plan/task/wave ownership and add an evidence-backed rg ledger. The ledger must enumerate every `contacts.phone`, `contacts.email`, and scalar `phone`/`email` aggregate contract found in production and name its migrated owner task/test. Make the scoped audit fail on a production scalar-method reference with no ledger entry; allow only named migration v8 fixtures and the v1 backup forward-migration fixture. Extend the logging-boundary test owner: spy on Logger through invalid/collision/failed method saves and assert no raw/canonical method value is passed. Add boundary assertions for the malformed-cadence migration rollback and the FK-safe rebuild. As immediate feedback, run the named targeted tests first; only after they pass, run the full suite, TypeScript, and colors gate. Treat the spec-less edge probe as a coverage ledger: write defensible automated predicates, retain device-only items as explicit backstops, and flag no unresolved material assumptions. Preserve D-01 through D-04 and scope fences rather than broadening into deferred functionality. The nullable-cadence, orbiting-set, and favourite-set consumer ledger and the explicit-Unbound AI/status/digest coverage are owned by 18.2-10.</action>
  <verify><automated>First run `npm test -- src/db/migrations/009-contact-method-normalization.test.ts src/db/contact-methods-dao.test.ts`; only after it passes, run `npm test && npx tsc --noEmit && npm run check:colors`.</automated></verify>
  <acceptance_criteria>CDN-01, CDN-03, and CDN-04 have executable evidence; the named targeted suite passes before the full regression gate; the rg ledger proves every scalar-method consumer is migrated or explicitly fixture-allowlisted; Logger receives no raw/canonical method data; and all requirement-specific quick commands pass.</acceptance_criteria>
  <done>The 18.1 codebase has one traceable automated verification path for every method-normalization truth.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perform owner-led Android migration and method UAT</name>
  <read_first>18-VALIDATION.md Manual-Only Verifications; 18-UI-SPEC.md UI Considerations; docs/runbooks/desktop-build-pipeline.md</read_first>
  <action>Build the release APK through the documented desktop pipeline, install it over an upgraded v8 fixture, and prepare the named test contacts/settings. This validates D-01 and the normalized-method model on device, including parser display/region behavior, method creation/edit, actionable-primary Compose handoff, and method backup round-trip. Bind/Unbind, Unbound screens, Orrery, notification/widget lifecycle, and Streamlined Export are owned by 18.2-10.</action>
  <what-built>Full normalized-method migration, method editor/profile, and method backup/restore behavior are automated and ready on a release build.</what-built>
  <how-to-verify>On the Pixel: upgrade v8 data; inspect migrated primary methods; create/edit invalid/extension/multiple methods; confirm Compose only offers actionable primary SMS; run full backup export then restore and confirm methods/extensions/primary/order round-trip; inspect 320dp overflow, long text, keyboard scroll, loading/error retry, and no raw canonical/source fields. Approve only if no method/history loss and no deferred import/reconciliation UI appears.</how-to-verify>
  <resume-signal>Reply approved, or provide the failing behavior and device evidence.</resume-signal>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>Owner records the release-build result for every method-related manual-only validation item and UI backstop, including CDN-01 parser provenance confirmation.</acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release build to real device database | Migration and method UI execute against user-like persisted relationship data. |
| actionability read to Android SMS | Device handoff can expose or use a contact endpoint. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-24 | Tampering/DoS | release migration | high | mitigate | Real v8 upgrade UAT after fixture/rollback tests and backup verification. |
| T-18-25 | Information disclosure | UI/logs | high | mitigate | Device check plus automated absence/actionability assertions; no canonical/raw source display. |
| T-18-26 | Elevation | Android handoff | high | mitigate | Exercise only actionable-primary Compose path and invalid fallback. |
| T-18-SC | Tampering | package provenance | high | mitigate | Confirm the 18.1-01 blocking human approval remains recorded. |
</threat_model>
<verification>Run the named targeted Task 1 suite as immediate feedback, then run the full suite before release UAT; final human checkpoint records device-only predicates and unresolved defects for the method half.</verification>
<success_criteria>Every 18.1 requirement is code-proven and the owner has seen the real Android migration and method behavior.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-06-SUMMARY.md` when done</output>
