---
context: phase
phase: 18.1-contact-method-normalization
task: 2
total_tasks: 4
status: paused
last_updated: 2026-08-28T07:10:38.414Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Durable labels require durable storage — labels/custom labels were implemented in the method editor but v9's `contact_methods` schema and DAO have no label destination, so they are lost after Save/reload. Add forward migration 010, DAO/read/aggregate persistence, and its tests before completing Plan 18.1-03.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| UI draft without a storage contract | Plan 18.1-03 required labels/custom labels and Plan 18.1-02 described them as durable SQL, but migration 009 and the method DAO implemented only `extension`. The draft UI therefore discarded labels after Save/reload. | blocking | Implement migration 010 with nullable `contact_methods.label`, register it, widen DAO/read/aggregate contracts, and prove a create/edit/reload round trip before writing 18.1-03-SUMMARY.md. Keep 009 immutable. |
| Treating staged schema migrations as final-suite regressions | Immediately after 009, the full suite failed because Plan 04 has not yet removed its live scalar backup/restore SQL and several unrelated test harnesses still build v1/v7 schemas. | advisory | Finish the remaining Wave 3 consumers first; then update old fixtures to the current schema once and run Plan 06's full suite gate. |

<current_state>

Phase 18.1 is paused in Wave 3, Plan 18.1-03 Task 2. Plans 18.1-01 and 18.1-02 are complete with committed summaries. Plan 18.1-03's create/edit/Settings UI work is committed and focused tests, TypeScript, and `check:colors` passed before the storage gap was discovered. Do NOT create `18.1-03-SUMMARY.md` yet: the plan is incomplete until labels persist.
</current_state>

<completed_work>

- 18.1-01 complete: parser seam, migration 009, normalized-method schema, region settings seam, and focused migration tests. Commits end at `dd80662`; summary committed at `dd80662`/`dd80662`-series history.
- 18.1-02 complete: transactional contact-method DAO/read boundary and aggregate contact writers. Final commit `1b5dedc`; 61 scoped tests and Biome passed.
- 18.1-03 committed partial work:
  - `556c293` create method drafts.
  - `4872066` method-editor interaction tests.
  - `659a842` edit method wiring.
  - `cace0c3` phone-region setting.
  - `59ef364` UI formatting.
- `2898d7b` records the owner decision in Plans 03–05 and the Phase 18.2 context.
</completed_work>

<remaining_work>

1. Resume 18.1-03 Task 2. Add forward migration 010 with nullable `contact_methods.label`; register it in `database.ts`; widen contact-method DAO, reads, aggregate create/edit input/output, and tests. Labels must survive create, edit, collision collapse, and reload. Do not amend 009.
2. Finish 18.1-03, run its focused tests plus `npx tsc --noEmit` and `npm run check:colors`, write and commit its SUMMARY.
3. Execute 18.1-04 with the recorded label wire addition. It also removes live scalar backup/restore SQL.
4. Execute 18.1-05; profile must render the stored nullable label and Compose must use the actionable-primary read.
5. After Wave 3, repair the known full-suite test harnesses that create v1/v7 databases but call code now requiring v10 `phone_region_override`; avoid fixing them before 18.1-04 removes its planned scalar SQL.
6. Execute 18.1-06, including the full-suite/type/colors gate and the specified Android UAT. Before Phase 18.2, re-cut its lifecycle migration numbering because 010 is now labels, not lifecycle.
</remaining_work>

<decisions_made>

- Owner chose durable labels (2026-08-28): add a nullable `label TEXT` in a forward migration 010. Presentation-only labels were rejected because they violate Save/reload durability.
- Migration 009 remains the committed v8→v9 scalar-normalization step. Do not fold labels back into it.
- The label must round-trip through 18.1-04 backup/export/restore and render from stored data in 18.1-05.
- User explicitly preferred deferred cleanup of full-suite transitional failures after Wave 3, avoiding duplicate fixture work before Plan 04's intended scalar-SQL removal.
</decisions_made>

<blockers>

- Plan 18.1-03 cannot complete until durable labels are implemented. The owner resolved the product/architecture choice; the blocker is now implementation only.
- No human action, service, worktree, or external async job is outstanding.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured current state and exact commits.
2. `.planning/phases/18.1-contact-method-normalization/.continue-here.md` — this pause contract and failure history.
3. `.planning/phases/18.1-contact-method-normalization/18.1-03-PLAN.md` — owner decision and the exact continuation scope.
4. `.planning/phases/18.1-contact-method-normalization/18.1-04-PLAN.md` and `18.1-05-PLAN.md` — required downstream label wire/rendering.
5. `.planning/phases/18.2-bound-unbound-lifecycle/18.2-CONTEXT.md` — migration-number consequence to preserve.
6. `.planning/PROJECT.md`, `.planning/STATE.md`, and `AGENTS.md` — project instructions and current state.
7. `.planning/METHODOLOGY.md` (if it exists) — apply project analytical lenses before new assumptions.

## Infrastructure State

- Branch: `main`; commits are local only and must not be pushed.
- Worktrees and parallelization are disabled: run executors sequentially on the main working tree.
- No running servers, watchers, worktrees, or async external jobs.
- Preserve these unrelated untracked paths; do not stage or delete them: `.planning/debug/replace-apply-failure.md`, `.planning/handoffs/`, `.planning/milestone.lock`, `orbit-home.png`, `ui.xml`.
- `npm run build` is invalid in this repository (`Missing script: build`); use the plan-specific checks and final TypeScript/test/color gates instead.

<context>

The Wave 1 post-merge `npm test` failed because 009 correctly retires `contacts.phone`/`email` before Plan 04 removes remaining live backup/restore SQL. After Wave 2 it reported 45 failures: the backup scalar SQL plus test harnesses pinned to v1/v7 that call `getAppSettings` after `phone_region_override` was added. The user explicitly authorized carrying those known transitional failures through Wave 3 and cleaning the harnesses once Plan 04 lands. Distinguish those failures from new Plan 03 durable-label work.
</context>

<next_action>

Start with Plan 18.1-03 Task 2's owner-decision block. Implement the forward migration 010 and durable label persistence, run focused tests/typecheck/colors, then write the missing Plan 03 summary before starting the rest of Wave 3.
</next_action>
