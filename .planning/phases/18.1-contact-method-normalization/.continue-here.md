---
context: phase
phase: 18.1-contact-method-normalization
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-28T16:27:08.910Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: The direct **Restore a backup** flow must be repaired, not replaced by a Files → Share → Orbit detour. The owner explicitly rejected the share-only workaround.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Ship a workaround as a bug fix | The picker remained broken from Orbit's Restore button. A native inbound-share implementation could reach the preview, but it changed the required entry path rather than fixing it. | blocking | Keep the direct Restore button as the acceptance path. Before committing, deploy the actual APK and prove it selects `Download/orbit-uat-changed-primary.json` and opens the existing preview from that button. |
| Treat shell intent behavior as app behavior | A shell-launched wildcard `ACTION_OPEN_DOCUMENT` rendered Files, while the same app-owned result launch hung. | blocking | Reproduce every candidate through Orbit's installed APK, not only `adb am` controls. |
| Assume a source edit is deployed | The first failed UAT ran an old APK, although a later current-source APK still failed. | advisory | Verify installed APK timestamp/hash/version and rebuild/install before accepting a device result. |

<current_state>

Phase 18.1 Plan 06 is paused at Task 2 of 2. Task 1 is complete in `06531df`; all automated validation is green. The direct Android restore picker is still broken on the Pixel, so no Plan 06 summary or phase close-out exists.

Plans 01–05 are complete and committed. The current working tree contains an uncommitted native share-receiver experiment plus debug captures. It is explicitly rejected as the completion path and must not be committed merely because it works through Files sharing.
</current_state>

<completed_work>

- 18.1-01 through 18.1-05: complete with committed summaries.
- 18.1-06 Task 1: complete in `06531df` (`test(18.1-06): prove normalized method regression boundary`).
  - Full suite: 1,510 tests in 135 files passed.
  - Target tests: 16/16 passed; `npx tsc --noEmit`, `npm run check:colors`, scalar-reference audit, and diff checks passed.
  - Device UAT confirmed v8→v10 migration: valid/invalid methods, labels/extensions, primary/actionability, `sun_contact_id`, cadence history, settings region override, and readable export wire.
  - Debug and release APKs built and installed successfully.
- Debug evidence: `.planning/debug/restore-document-picker-blank.md` includes all direct picker controls and deployment checks.
</completed_work>

<remaining_work>

1. Fix the app-owned direct **Restore a backup** button path. Pixel DocumentsUI hangs with a blank, non-responsive `PickActivity` when Orbit launches `ACTION_OPEN_DOCUMENT` expecting a result.
2. Treat existing uncommitted share-receiver files as a rejected experiment. Preserve them until reviewed; do not commit them as the fix.
3. Rebuild/install the actual candidate APK, then prove the direct button can select `Download/orbit-uat-changed-primary.json` and reach `RestorePreviewScreen`.
4. Finish changed-primary Merge UAT: Ada's `312 555 6789` becomes actionable primary; original `312 555 1234 ext. 56` remains with extension `56`.
5. Only after all Task 2 UAT passes: create `18.1-06-SUMMARY.md`, update state/roadmap/requirements, and commit close-out metadata.
</remaining_work>

<decisions_made>

- The Pixel database is disposable test data; mutation during UAT is authorized.
- Migration 010 is durable nullable method labels; it is already shipped in completed Plan 03.
- The direct Restore button is the required product behavior. The owner rejected a share-only route even though it reaches preview safely.
- `FLAG_ACTIVITY_NEW_TASK` cannot be used with an activity-result picker: it returns immediate cancellation and therefore is not a direct fix.
</decisions_made>

<blockers>

- Technical: Pixel DocumentsUI hangs only in Orbit's app-owned `ACTION_OPEN_DOCUMENT` result/task context. Multi-MIME and wildcard requests, fixed portrait, bridge activity, and stale-build explanations were tested; see the debug session for exact evidence.
- No human permission is pending. The owner has authorized device/test-data mutation and expects a real direct-flow fix.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state.
2. This `.continue-here.md` — explicit owner rejection and resume contract.
3. `.planning/debug/restore-document-picker-blank.md` — full scientific evidence, rejected hypotheses, current uncommitted experiment.
4. `.planning/phases/18.1-contact-method-normalization/18.1-06-PLAN.md` — final UAT and close-out contract.
5. `.planning/phases/18.1-contact-method-normalization/18.1-01-SUMMARY.md` through `18.1-05-SUMMARY.md` — shipped normalization contracts.
6. `src/screens/BackupScreen.tsx`, `src/screens/backup-restore-logic.ts`, and current uncommitted restore-related files — inspect before retaining or reverting experiments.
7. `.planning/PROJECT.md`, `.planning/STATE.md`, `AGENTS.md`, and `.planning/METHODOLOGY.md` (if it exists).

## Infrastructure State

- Branch: `main`; local commits only, no push/PR.
- Worktrees and parallelization are disabled; execute sequentially in the main worktree.
- Pixel 6 Pro is connected; its Orbit database/test fixture is disposable and can be mutated.
- The uncommitted source experiment spans `app.config.ts`, `src/navigation/linking.ts`, `src/screens/BackupScreen.tsx`, `modules/orbit-backup-document-picker/`, `plugins/withBackupRestoreShareIntent.js`, and `src/navigation/backup-share-intent.*`.
- Preserve pre-existing `tsconfig.json` changes and original unrelated paths: `.planning/debug/replace-apply-failure.md`, `.planning/handoffs/`, `.planning/milestone.lock`, `orbit-home.png`, and `ui.xml`.
- Treat `files-*.xml`, `orbit-*.xml`, `share-chooser.xml`, and debug PNGs as generated debug captures: inspect them before retention/deletion; do not blindly stage them.

<context>

The direct `DocumentPicker` was originally configured with two JSON MIME types. Its array request produced a blank DocumentsUI activity. Shell-launched wildcard intents rendered normally, but a rebuilt Orbit APK still hung even with wildcard MIME. A dedicated bridge activity and portrait-orientation control also failed. The last experiment created an ACTION_SEND share receiver and told the user to choose Files → Share → Orbit; it successfully opened preview but is not the requested direct Restore button behavior, so it is rejected. The new agent should retain the scientific evidence but independently decide whether any uncommitted experiment is useful.
</context>

<next_action>

Start by acknowledging both blocking anti-patterns. Then inspect the uncommitted experiment and debug record, keep the direct Restore button as the sole acceptance path, and continue the direct Android picker investigation from the installed APK rather than shell-only controls.
</next_action>
