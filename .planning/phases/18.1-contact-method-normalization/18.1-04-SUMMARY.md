---
phase: 18.1-contact-method-normalization
plan: 04
subsystem: backup-restore
tags: [typescript, sqlite, backup, restore, reconciliation, tombstones]
requires:
  - phase: 18.1-01
    provides: normalized contact-method tables and portable phone-region setting
  - phase: 18.1-03
    provides: nullable durable contact-method labels in migration 010
provides:
  - v2 backup manifest arrays for methods, external links, and method provenance
  - validated v1-to-v2 scalar endpoint migration and transactional normalized restore
  - normalized-child purge and explicit tombstone coverage
affects: [18.1-05 profile method rendering, 18.1-06 final verification, 18.2 lifecycle backup extension]
actuals:
  tokens: 17661
  tasks: 3
  commits: 5
tech-stack:
  added: []
  patterns: [versioned manifest graph, post-reconciliation natural-key normalization, demotion-first partial-unique writes]
key-files:
  created: []
  modified:
    - src/backup/types.ts
    - src/backup/export-manifest.ts
    - src/backup/backup-schema.ts
    - src/backup/restore-apply.ts
    - src/backup/reconciliation.ts
    - src/db/purge-dao.ts
key-decisions:
  - "Backup format v2 keeps method label and canonicalRegion nullable on the durable wire."
  - "Natural-key collisions normalize the reconciliation plan rather than rejecting a whole restore."
  - "Method/link demotions write before promotions because SQLite partial unique indexes are statement-immediate."
patterns-established:
  - "Recompute survivor sets after action-plan normalization, including provenance UID re-parenting for collapsed methods."
  - "Use an exhaustive purge registry for every tombstoned normalized child."
requirements-completed: [CDN-01, CDN-04]
coverage:
  - id: D1
    description: "Lossless v2 export and strict parsing of normalized method, link, and provenance rows."
    requirement: CDN-04
    verification:
      - kind: unit
        ref: npm test -- src/backup/export-manifest.test.ts src/backup/backup-schema.test.ts
        status: pass
    human_judgment: false
  - id: D2
    description: "Transactional normalized restore, collision normalization, and explicit-delete tombstone semantics."
    requirement: CDN-04
    verification:
      - kind: integration
        ref: npm test -- src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts src/db/purge-dao.test.ts
        status: pass
    human_judgment: false
duration: 17m
completed: 2026-08-28
status: complete
---

# Phase 18.1 Plan 04: Normalized Backup and Restore Summary

**Backup format v2 now preserves normalized contact methods, source provenance, labels, and canonicalization regions through validated restore.**

## Performance

- **Duration:** 17m
- **Started:** 2026-08-28T07:18:00Z
- **Completed:** 2026-08-28T07:35:08Z
- **Tasks:** 3/3
- **Files modified:** 13

## Accomplishments

- Bumped the portable backup wire to v2 with first-class contact-method, external-link, and provenance arrays; v1 scalar endpoints forward-migrate to deterministic legacy method UIDs.
- Preserved nullable `label` and `canonicalRegion`, emitted/restored `phoneRegionOverride`, and removed all live backup/restore SQL references to retired contact `phone` and `email` columns.
- Registered normalized children in reconciliation, applies demotions before promotions under SQLite partial unique indexes, and preserves explicit-tombstone-only deletion semantics.
- Extended Replace-all reset and archived-contact purge to emit evidence and remove every normalized child in FK-safe order.

## Task Commits

1. **Task 1: Export and parse one normalized contact's methods losslessly** — `d0d06bc` (RED), `bf64ac9` (GREEN)
2. **Task 2: Reconcile normalized child entities through restore** — `a0974ff` (RED), `4bbf08d` (GREEN)
3. **Task 3: Cover explicit-delete tombstones and backup wire compatibility** — `471192c` (test)

## Files Created/Modified

- `src/backup/types.ts`, `src/backup/export-manifest.ts`, and `src/backup/backup-schema.ts` — v2 wire records, scalar-to-graph migration, and strict parent/tombstone validation.
- `src/backup/reconciliation.ts` and `src/backup/restore-apply.ts` — normalized entity policy, collision resolution, ordered writes, and graph upserts.
- `src/db/tombstones-dao.ts` and `src/db/purge-dao.ts` — explicit normalized child deletion evidence and purge registry.
- Focused backup, restore, reconciliation, and purge tests — schema, wire, restore, and partial-index regression coverage.

## Decisions Made

- `canonicalRegion` and `label` remain nullable wire data; absent pre-v2 values restore as `null` rather than being synthesized.
- Shared canonical values across contacts remain legal; same-contact canonical duplicates collapse and re-parent their provenance to the surviving method.
- Local dormant favourite rank remains outside the portable contract and is not clobbered by Merge.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Vitest emitted the repository's existing Vite native-config and Node SQLite experimental warnings.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 18.1-05 can consume durable labels and Plan 18.1-06 can run the phase-wide compatibility suite against the normalized backup graph.

## Self-Check: PASSED

- Task commits `d0d06bc`, `bf64ac9`, `a0974ff`, `4bbf08d`, and `471192c` exist in Git history.
- `npm test -- src/backup/export-manifest.test.ts src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts src/db/purge-dao.test.ts` passed (44 tests).
- `npx tsc --noEmit` and `git diff --check` passed.

---
*Phase: 18.1-contact-method-normalization*
*Completed: 2026-08-28*
