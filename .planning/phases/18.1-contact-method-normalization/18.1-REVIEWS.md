---
phase: 18.1-contact-method-normalization
type: reviews
cycle: 3
created: 2026-08-27
status: converged
---

# Phase 18.1 — Cross-AI Plan Review Record (Cycle 1)

Cross-AI peer review of the Phase 18.1 plan set (18.1-01 … 18.1-06) plus `18.1-CONTEXT.md`.
Both reviewers read the actual code on disk (not just diffs), grepping every writer/reader of
`contacts` and verifying claimed file:line references. All findings below are **Incorporated**;
the owning plan/task is named for each.

## Reviewer record

| Reviewer | Model / config | Mode | Result |
|---|---|---|---|
| Claude | claude-opus-5 | Direct, read-only subagent | 6 HIGH, 6 MEDIUM, 4 LOW; overall risk HIGH, "not ready to execute" |
| Codex | gpt-5.6-terra, reasoning=low | review-lane | 2 HIGH, 1 MEDIUM; overall risk HIGH, "not ready to execute" |

## Two owner decisions (applied verbatim intent)

These two findings are **owner-bucket** calls (risk/product posture and reversal of a recorded
migration policy), not planner calls. The owner decided both; the plans transcribe the decisions.

- **H4 — device-region parsing (REPLACES "raw-only, no assumed region").** National-format legacy
  phone numbers parse using the effective default region (device region via `expo-localization`,
  overridable by `phone_region_override`), canonicalized to E.164 when parseable; `isValid()`
  (already owner-chosen in 18.1-01 Task 1) gates actionability. Numbers that fail validation under
  the effective region remain **stored** but non-actionable (never discarded). Owner: existing
  contacts must not silently lose Text until re-edited.
- **M1 — clamp + snapshot, keep running (REPLACES "reject atomically and leave user_version at v8").**
  A malformed `interval_days` (0, negative, or non-integer — reachable only via a prior restore bug)
  is **clamped** to a safe default at migration time, with the **original value snapshotted to
  `field_history`** in the same transaction; the migration continues and the app opens normally.
  Atomic rollback still applies to genuinely unrecoverable errors. Owner: do not permanently wedge
  an install over recoverable bad data.

## Disposition — Claude findings

| ID | Finding (short) | Disposition | Owner plan / task |
|---|---|---|---|
| H1 | `getContactHeader` / `ContactEditRow` select dropped scalar `contacts.phone`/`email`; no plan owned `contact-read.ts` | Incorporated | 18.1-02 Task 1 — `contact-read.ts`(+test) added to `files_modified`; SELECT :125 and edit read :139/:187 stop exposing scalar phone/email; 5 callers do not throw |
| H2 | Create/Edit screens hold scalar state that 18.1-02 retires; deferred to 18.2-08 | Incorporated | 18.1-03 Tasks 1 & 3 — `CreateContactScreen.tsx`/`EditContactScreen.tsx` added; method wiring owned here; RE-CUT NOTE corrected here and in 18.2-08 |
| H3 | Merge restore can create two `is_primary=1` rows per `(contact, type)`; guard was manifest-internal only | Incorporated | 18.1-04 Task 2 — natural-key incompatibility path in reconciliation for duplicate primary and global active-link `(provider, external_contact_id)` collisions, resolved before SQLITE_CONSTRAINT |
| H4 | "raw-only" migration strips Text from nearly every existing contact | Incorporated (owner decision) | 18.1-01 Task 3 — device-region parsing; `defaultPhoneRegion` added to `MigrationDeps` (`types.ts`) and constructed in `database.ts` |
| H5 | `phone_region_override` had no DAO owner; restore silently discards it | Incorporated | 18.1-01 Task 3 (ordinary `getAppSettings`/`updateAppSettings`) + 18.1-04 Task 1 (PORTABLE seam: `PORTABLE_SETTINGS_KEYS`, snapshot projection, `COLUMN_OF`, v1→v2 default) |
| H6 | FK-safe rebuild left to executor discretion; `foreign_key_check` blind to a full cascade | Incorporated | 18.1-01 Task 3 — pinned `PRAGMA defer_foreign_keys = ON`; `legacy_alter_table` removed; per-child `COUNT(before)==COUNT(after)` load-bearing; `foreign_key_check` retained but noted insufficient |
| M1 | Malformed-cadence rejection permanently bricks the install | Incorporated (owner decision) | 18.1-01 Task 3 — clamp to safe default + `field_history` snapshot in-transaction + continue |
| M2 | No guard against same-contact canonical duplicates on restore | Incorporated | 18.1-04 Task 2 — canonical-duplicate guard via `incompatibleRows` machinery |
| M3 | Cluster D "user-overridable in Settings" region had no owning plan | Incorporated | 18.1-03 Task 4 (new) — `SettingsScreen.tsx`(+test); reads/writes `phone_region_override` via the app-settings DAO seam |
| M4 | rg ledger gating the phase had no executable artifact | Incorporated | 18.1-06 Task 1 — `scripts/audit-scalar-method-refs.mjs` (executable) added and run in `<verify>` |
| M5 | C3-M1 acceptance passes regardless; needs compile-time exhaustiveness | Incorporated | 18.1-04 Task 2 — compile-time exhaustive typed residual `Record` that fails to build on a new entity type |
| M6 | `ADD COLUMN` vs rebuild for `app_settings` unresolved | Incorporated | 18.1-01 Task 3 — resolved to `ADD COLUMN` for `app_settings`; child-preservation proof scoped to the `contacts` rebuild |
| L1 | Task 2 gate throws if package present; breaks resume | Incorporated | 18.1-01 Task 2 — gate asserts recorded provenance approval in SUMMARY, not package absence |
| L2 | `reserved-columns.ts:37-38` keeps "phone"/"email" reserved — annotate as decision | Incorporated | 18.1-01 Task 3 — annotated-decision note; `reserved-columns.ts` added to `files_modified` |
| L3 | Cluster B "single method is effectively primary" unstated for read owner/migration | Incorporated | 18.1-02 Task 1 — behavior + acceptance added |
| L4 | 18.1-06 `depends_on` omits 18.1-01 while editing 009 test | Incorporated | 18.1-06 — `18.1-01` added to `depends_on` |

## Disposition — Codex findings (a strict subset, mapped to Claude's)

| Codex finding | Maps to | Disposition | Owner plan / task |
|---|---|---|---|
| HIGH: no owner for wiring normalized methods into Create/Edit screens | Claude **H2** | Incorporated | 18.1-03 Tasks 1 & 3 |
| HIGH: `phone_region_override` has no executable DAO/portable-settings owner | Claude **H5** | Incorporated | 18.1-01 Task 3 + 18.1-04 Task 1 |
| MEDIUM: 18.1-03 "end to end" contradicted by the scalar `contact-read.ts` boundary | Claude **H1** | Incorporated | 18.1-02 Task 1 (read owner) + 18.1-03 Task 3 (edit seeds from the new read boundary) |

## Convergence

The two reviews **converged**: every Codex finding is a strict subset of Claude's finding set
(Codex HIGH→H2, HIGH→H5, MEDIUM→H1) and serves as independent confirmation of the three most
load-bearing defects — the unowned screen/read consumers and the region-setting round-trip. Codex
surfaced no finding absent from Claude's review. All 16 findings are Incorporated with named owners,
and the two owner-bucket decisions (H4, M1) were taken back to the owner and resolved.

> **Superseded by Cycle 2 (below).** A second cross-AI cycle WAS run and found that two of cycle 1's
> own incorporations — the FK-safe migration route (H6/M6) and the restore-collision handling
> (H3/M2) — each shipped a defect worse than the one they replaced. Cycle 1's *ownership* fixes
> (H1, H2, M3, M4, L1–L4) were re-verified as sound; its *migration/restore* incorporations were
> corrected in cycle 2.

---

# Phase 18.1 — Cross-AI Plan Review Record (Cycle 2)

Second cross-AI peer review of the revised Phase 18.1 plan set. Both reviewers again read the
actual code on disk (grepping every writer/reader of `contacts` and `app_settings`, and verifying
claimed file:line references). All cycle-2 findings below are **Incorporated**; the owning plan/task
is named for each. One new finding (**HIGH-5**) is an owner-bucket risk/security-posture decision and
was taken back to the owner.

## Reviewer record

| Reviewer | Model / config | Mode | Result |
|---|---|---|---|
| Claude | claude-opus-5 | Direct, read-only subagent | 5 HIGH, 5 MEDIUM, 5 LOW; overall risk HIGH, "not ready to execute" |
| Codex | gpt-5.6-terra, reasoning=low | review-lane | 2 HIGH, 1 MEDIUM; overall risk HIGH, "not ready to execute" |

## Owner decision (cycle 2, applied verbatim)

- **HIGH-5 — record the canonicalization region (risk-posture call).** The owner's cycle-1 decision
  (device-region parsing) canonicalizes national numbers under the device region and marks valid ones
  actionable. Cycle 2 surfaced that a *wrong* device region produces permanently wrong **actionable**
  numbers with no bulk remedy, and "canonical identity is never rewritten" means changing the region
  afterward does not repair them. Owner decision: keep device-region canonicalization and accept the
  residual wrong-actionable risk, AND **persist the region each number was canonicalized under** so a
  future explicit re-derivation is possible. Implemented as a `canonical_region TEXT` column on the
  normalized method row in the v9 migration (NULL for non-canonicalized/raw methods). Canonical
  identity is never auto-rewritten; re-derivation is a future explicit action.

## Disposition — Claude cycle-2 findings

| ID | Finding (short) | Disposition | Owner plan / task |
|---|---|---|---|
| HIGH-1 | H6 route + M6 resolution destroy `app_settings`; M6 rested on a false schema claim (`app_settings.sun_contact_id` IS an FK child, `003-orrery-settings.ts:38-40`) | Incorporated (reverses cycle-1 M6) | 18.1-01 Task 3 — `app_settings` rebuilt as the SIXTH FK child with all current columns + `phone_region_override`, `sun_contact_id` retargeted; per-child proof now covers six children; test asserts FK target + exact non-null `sun_contact_id` |
| HIGH-2 | `defer_foreign_keys` rationale wrong — it defers violation CHECKING, not FK ACTIONS | Incorporated | 18.1-01 Task 3 — corrected rationale; every `REFERENCES contacts` child re-pointed before `DROP TABLE contacts_old`; child list DERIVED from `PRAGMA foreign_key_list` (or asserted equal) |
| HIGH-3 | `MigrationDeps` fix unimplementable as sited (pre-v9 override read crashes launch) and breaks ~78 call sites | Incorporated | 18.1-01 Task 3 — device-region ONLY at migration (no override read); `defaultPhoneRegion?: string \| null` explicitly OPTIONAL; region resolved at `App.tsx` bootstrap and passed via `openAndMigrate()`; test covers set+unset |
| HIGH-4 | Restore collision routed through `incompatibleRows`, which rejects the ENTIRE restore (falsely rejects "changed primary phone") | Incorporated | 18.1-04 Task 2 — replaced with a POST-reconciliation normalization over `ReconciliationAction[]` (demote all-but-one primary; collapse same-contact canonical dup; keep one active link, mark other stale); tests assert what SURVIVES |
| HIGH-5 | One-shot device-region canonicalization irreversible; wrong region → permanently wrong actionable numbers | Incorporated (owner decision) | 18.1-01 Task 3 — `canonical_region TEXT` on the method row (NULL for raw); enables future explicit re-derivation; identity never auto-rewritten |
| MED-1 | `COLUMN_OF` double-owned; the writable-key union it depends on was unowned | Incorporated | 18.1-01 Task 3 owns `WritableSettingsKey`/`AppSettingsPatch`/`AppSettings`/`AppSettingsRow`/`getAppSettings`/`COLUMN_OF`; 18.1-04 owns ONLY the portable seam (PORTABLE_SETTINGS_KEYS, snapshot projection, restore patch) |
| MED-2 | New UI `.test.tsx` render tests can't run in node-only env | Incorporated | 18.1-03 — extract pure `contact-methods-editor-model.ts` + `settings-region-logic.ts` and test those; render-test requirements removed; 44px/a11y-render moved to 18.1-06 device backstop; redundant "no raw colors" dropped |
| MED-3 | M1 clamp snapshot transient/non-portable/unsurfaced; no deterministic data contract | Incorporated | 18.1-01 Task 3 — pinned default `CLAMP_DEFAULT_INTERVAL_DAYS = 30`; exact `field_history` fields; `field_col_name='interval_days'` (reserved, collision-safe) + free-text `operation` sentinel; framed as bounded ~30-day trace (not durable recovery) |
| MED-4 | `CaptureScreen` consumes `createContactFull`; no 18.1 plan owned it | Incorporated | 18.1-02 Task 2 — method-draft array and region context are ADDITIVE-OPTIONAL, so `CaptureScreen.tsx:488` and `data-revision-dao.test.ts` need no change |
| MED-5 | `expo-localization` in `database.ts` puts a native module on the node-test import path | Incorporated | 18.1-01 Task 3 — region resolved at `App.tsx` bootstrap and passed into `openAndMigrate()`; `database.ts` stays native-import-free; `App.tsx` added to `files_modified` |
| LOW-1 | 18.1-03 frontmatter stale after M3 (tasks/tokens) | Incorporated | 18.1-03 — `tasks: 4`, token estimate raised |
| LOW-2 | Plan never says to bump `TARGET_VERSION` 8→9 or register `migration009` | Incorporated | 18.1-01 Task 3 — explicit bump (`database.ts:43`) + append to `MIGRATIONS` (`:46-55`) |
| LOW-3 | Task 2 provenance gate greps a plan-end SUMMARY on a same-line regex (not resume-safe) | Incorporated | 18.1-01 Task 2 — SUMMARY written incrementally at task start; whole-file per-package scan |
| LOW-4 | `expo-localization` had no provenance gate; null-region case unspecified | Incorporated | 18.1-01 Task 2 (gate covers both packages) + Task 3 (null region fails closed — national numbers stay non-actionable) |
| LOW-5 | No plan owned `App.tsx`; 009 had no failure copy | Incorporated | 18.1-01 — `App.tsx` in `files_modified` for bootstrap region; dedicated 009 failure-copy branch noted OPTIONAL (M1 clamp removes the wedge) |

## Disposition — Codex cycle-2 findings (a strict subset, mapped to Claude's)

| Codex finding | Maps to | Disposition | Owner plan / task |
|---|---|---|---|
| HIGH: H6 fix internally contradictory; would lose `sun_contact_id` | Claude **HIGH-1** | Incorporated | 18.1-01 Task 3 — app_settings rebuilt as sixth child |
| HIGH: H4 override not effective for migrated/existing non-actionable methods; "fixable in bulk" claim unbacked | Claude **HIGH-3** (device-region only) + **HIGH-3(d)** | Incorporated | 18.1-01 Task 3 (device region only at migration) + 18.1-03 Task 4 ("fixable in bulk" claim removed; override governs future writes only) |
| MEDIUM: M1 clamp lacks a deterministic data contract | Claude **MED-3** | Incorporated | 18.1-01 Task 3 — pinned default + exact `field_history` row |

## Convergence (Cycle 2)

The two reviews **converged** again: both rated the set HIGH-risk / not-ready, and every Codex
finding is a strict subset of Claude's (Codex HIGH→HIGH-1, HIGH→HIGH-3, MEDIUM→MED-3). Codex surfaced
no finding absent from Claude's review. Cycle 1's *ownership* fixes (H1, H2, M3, M4, L1–L4) were
independently re-verified as sound; cycle 1's *migration* (H6/M6, the `MigrationDeps` region path) and
*restore* (H3/M2) incorporations were each found to introduce a worse defect and were corrected here.
All 15 cycle-2 findings are Incorporated with named owners, and the one owner-bucket decision (HIGH-5)
was taken back to the owner and resolved (persist `canonical_region`, accept residual risk).

---

# Phase 18.1 — Cross-AI Plan Review Record (Cycle 3)

Third cross-AI peer review of the revised Phase 18.1 plan set. Both reviewers again read the actual
code on disk (verifying every cycle-2 fix against `src/db/migrations/001–008`, `database.ts`,
`app-settings-dao.ts`, `restore-apply.ts`, `reconciliation.ts`, `backup-schema.ts`,
`export-manifest.ts`, and the headless/widget entry points, and confirming claimed file:line
references). All cycle-3 findings below are **Incorporated**; the owning plan/task is named for each.
No cycle-3 finding is an owner-bucket decision — HIGH-3 is an omission that stops an already-approved
owner decision (cycle-2 HIGH-5) at the plan boundary, not a reversal, so it is a planner fix, not a
new escalation.

## Reviewer record

| Reviewer | Model / config | Mode | Result |
|---|---|---|---|
| Claude | claude-opus-5 | Direct, read-only subagent | 3 HIGH, 4 MEDIUM, 4 LOW; overall risk MEDIUM, "not ready to execute" (narrowly; residue localized to restore) |
| Codex | gpt-5.6-terra, reasoning=low | review-lane | 1 HIGH, 0 MEDIUM; overall risk HIGH, "not ready to execute" (headless first-open region path) |

Cycle 3 is the first cycle where the two reviewers' HIGH sets are DISJOINT rather than
Codex-subset-of-Claude: Claude's three HIGHs are all in restore (18.1-04); Codex's single HIGH is the
migration's headless first-open region path (18.1-01), which Claude's cycle-3 pass did not surface.
The two are complementary, not contradictory — together they close the last restore residue and the
last migration first-open gap.

## Disposition — Claude cycle-3 findings

| ID | Finding (short) | Disposition | Owner plan / task |
|---|---|---|---|
| HIGH-1 | Restore applies rows one-at-a-time in sorted UID order; a LEGAL final state still trips the per-statement v9 partial unique index mid-transaction when the promoted UID sorts first (UNIQUE cannot be deferred) | Incorporated | 18.1-04 Task 2 — pin a WRITE ORDER (all demotions/deactivations before any promotion/activation, per `(contact_id, method_type)` and `(provider, external_contact_id)`) OR a two-pass write; INTEGRATION test through `applyRestore` with the promoted UID sorting first |
| HIGH-2 | A demotion applied to a `retain` action is filtered out of `writes()` (insert\|update only) → silent no-op → collision still fires | Incorporated | 18.1-04 Task 2 — a demotion/deactivation on a `retain` MUST convert it to an `update` (enter the write set); a canonical-dup collapse removing a retained row needs a `delete`; active-link stale-marking of a retained row likewise |
| HIGH-3 | `canonical_region` exists only in the migration; no plan carries it through backup/restore or writes it on DAO writes, voiding the cycle-2 HIGH-5 re-derivation mitigation on an ordinary restore | Incorporated | 18.1-04 (wire: export projection + schema validation + restore upsert; Tasks 1 & 2) + 18.1-02 Task 1 (DAO write contract, HIGH-3B) + 18.1-06 Task 2 (round-trip UAT assertion) |
| MED-1 | The MED-1 ownership split is not realizable: `AppSettingsPatch` IS `PortableSettingsSnapshot`; widening it in wave 1 breaks backup export until wave 3 | Incorporated | 18.1-01 wave 1 owns the `PortableSettingsSnapshot` DECLARATION (widened optionally) + the `PORTABLE_SETTINGS_KEYS` entry (inert until emission); 18.1-04 keeps the snapshot EMISSION + wire + restore patch |
| MED-2 | app_settings rebuild column enumeration omits `created_at`, and both named fallbacks (`AppSettingsRow`) miss it too → irreversible column loss | Incorporated | 18.1-01 Task 3 — strike `AppSettingsRow` as source; pin the literal list incl. `created_at TEXT NOT NULL` and `id`; `PRAGMA table_info` name-set == pre ∪ {`phone_region_override`} is load-bearing |
| MED-3 | Schema-preservation assertion omits CHECK constraints (`CHECK (id = 1)`, invisible to `table_info`); sqlite_master verbatim comparison unexecutable for the two shape-changing tables | Incorporated | 18.1-01 Task 3 — CHECK added to the asserted set; sqlite_master restated as "identical except the enumerated deltas" (app_settings +phone_region_override/FK retargeted; contacts −phone/−email) |
| MED-4 | Two live SQL statements in `restore-apply.ts` (`:61` SELECT, `:97` INSERT/ON CONFLICT) plus `export-manifest.ts:48` still name dropped scalar `phone`/`email` | Incorporated | 18.1-04 Tasks 1 & 2 — named and removed; post-v9 restore/export no longer throws `no such column: c.phone` |
| LOW-1 | 18.1-04 said to add snake_case `phone_region_override` to `PORTABLE_SETTINGS_KEYS`, which holds camelCase wire keys → `assertPortableSettings` rejects every v2 export | Incorporated | 18.1-01 (owns the entry now) — camelCase `phoneRegionOverride`; 18.1-04 emits the same camelCase key |
| LOW-2 | 18.1-03 `depends_on` omits 18.1-01 although Task 4 consumes its `getAppSettings`/`updateAppSettings` widening directly | Incorporated | 18.1-03 — `18.1-01` added to `depends_on` |
| LOW-3 | Index recreation on rebuilt children under-specified | Incorporated | 18.1-01 Task 3 — exactly one named index on a rebuilt child (`idx_interactions_recency`, `001:112-113`), zero triggers; drop the old table BEFORE `CREATE INDEX` to avoid a name collision |
| LOW-4 | Minor line-reference drift (none load-bearing) | Noted; no plan edit required | — |

## Disposition — Codex cycle-3 finding

| Codex finding | Maps to | Disposition | Owner plan / task |
|---|---|---|---|
| HIGH: device-region migration bypassed on headless first launch — `openAndMigrate()` is also called pre-React from `notification-actions.ts:122`, `widget-task-handler.tsx:90`, `widget-render.tsx:147`; the planned region parameter would be omitted there, so a killed-app notification/widget first-open migrates with null region and the one-shot canonicalization cannot re-run | New (no Claude analog this cycle) | 18.1-01 Task 3 — a platform-only `src/services/device-region.ts` provider imported by App AND the three headless/widget entry points, each passing region into `openAndMigrate()`; `expo-localization` stays out of `database.ts`; the three entry-point files added to `files_modified`; a HEADLESS first-open v8 upgrade probe/test added |

## Convergence (Cycle 3)

**Converging, clearly.** HIGH-count trajectory **6 → 5 → 3** (Claude), overall risk
**HIGH → HIGH → MEDIUM**. The character of the residue changed more than the count:

- **The migration is now HIGH-free from Claude's side and all six FK children are verified.** Every
  cycle-2 migration fix (HIGH-1/2/3, MED-3, LOW-2) was re-verified correct against the real schema —
  including the two claims most likely to be wrong: the six-child FK enumeration (contact_links,
  interactions, events, fuel, custom_field_values, app_settings — confirmed exhaustive, with
  `contact_custom_values` and `field_history` correctly excluded) and the M1 clamp `field_history`
  contract (exact column set, reserved sentinel, free-text `operation`, ~30-day bound, backup
  exclusion). Claude's three cycle-3 migration MEDIUMs (created_at, CHECK, sqlite_master) are
  proof-completeness gaps, not wrong behavior. Codex's one HIGH is a real first-open PATH gap
  (headless callers), not a destructive-rebuild defect.
- **The residue is concentrated in restore (18.1-04).** All three of Claude's HIGHs are in restore,
  and two of them (HIGH-1, HIGH-2) are the same underlying miss — the cycle-2 normalization pass
  reasons over the action plan as a SET while `applyRestore` executes it as an ORDERED statement
  stream, and the plan never crossed that gap. HIGH-3 is an omission that would silently void an
  already-approved owner decision (cycle-2 HIGH-5) rather than reverse one.
- **No regression pattern.** Unlike cycles 1 and 2 (each broke something while fixing something),
  cycle 3 found no instance of a fix reversing a sound prior fix.

Both reviewers rate the set not-ready-to-execute, but the remaining work is small and localized: one
targeted revision on 18.1-04 (HIGH-1/2/3, MED-4, LOW-1), two edits on 18.1-01 (MED-2, MED-3) plus the
MED-1 ownership shift and the Codex headless-region seam, the HIGH-3B write-contract line on 18.1-02,
and the LOW-2 `depends_on` on 18.1-03. All 11 cycle-3 findings are Incorporated with named owners; no
owner-bucket decision was triggered (HIGH-3 carries an existing owner decision to its conclusion, it
does not reverse one).
