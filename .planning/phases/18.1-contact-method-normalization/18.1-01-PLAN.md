---
phase: 18.1-contact-method-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, src/logic/contact-method-normalization.ts, src/logic/contact-method-normalization.test.ts, src/db/migrations/009-contact-method-normalization.ts, src/db/migrations/009-contact-method-normalization.test.ts, src/db/database.ts, src/db/types.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/reserved-columns.ts, src/backup/backup-schema.ts, src/services/device-region.ts, src/services/notifications/notification-actions.ts, src/services/widget/widget-task-handler.tsx, src/services/widget/widget-render.tsx, App.tsx]
autonomous: true
requirements: [CDN-01, CDN-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A v8 database migrates atomically to v9: legacy scalar phone/email become first-class primary method rows and the old scalar columns no longer exist."
    - "The normalization boundary stores nonblank malformed input as non-actionable and permits shared canonical values across contacts."
    - "Legacy national-format phone numbers parse at migration time under the DEVICE region ONLY (resolved at the App.tsx bootstrap and threaded into `MigrationDeps`; `phone_region_override` does not yet exist when 009 runs, so the migration never reads it) and canonicalize to E.164 when valid; numbers that fail validation under the device region remain STORED but non-actionable and are never discarded (owner decision H4). The persisted override affects only POST-migration DAO/UI writes."
    - "Every canonicalized method row records `canonical_region` — the region it was canonicalized under (NULL for non-canonicalized/raw methods) — so a wrong-region actionable number is re-derivable by a future explicit action; canonical identity is never auto-rewritten (owner decision cycle 2 HIGH-5)."
    - "A malformed legacy `interval_days` (0, negative, or non-integer) is clamped to a safe default at migration time with the ORIGINAL value snapshotted to `field_history` in the same transaction; the migration continues and the app opens normally (owner decision M1)."
    - "The migration schema makes local Orbit identity independent of system-contact identifiers and retains stale source evidence."
    - "The v9 migration owns the durable phone-region override default; `interval_days` stays NOT NULL as today and every contact remains Bound."
    - "CDN-01 manual edge review remains required for installed parser provenance and Android migration behavior."
  artifacts: [src/db/migrations/009-contact-method-normalization.ts, src/logic/contact-method-normalization.ts, src/db/migrations/009-contact-method-normalization.test.ts]
  key_links:
    - "Migration 009 invokes the same pure normalizer that later DAO writes use."
    - "The device region is supplied by EVERY first-open `openAndMigrate()` caller, not only `App.tsx` (Codex HIGH). `openAndMigrate()` also runs HEADLESS, before React mounts, from `src/services/notifications/notification-actions.ts:122`, `src/services/widget/widget-task-handler.tsx:90`, and `src/services/widget/widget-render.tsx:147` — a killed-app notification action or widget render can be the FIRST opener of an upgraded v8 database. Resolve the region through a small platform-only provider module (`src/services/device-region.ts`, a pure reader of `expo-localization`) imported by `App.tsx` AND by those three headless/widget entry points, and pass its result into `openAndMigrate()` → `MigrationDeps.defaultPhoneRegion` (declared OPTIONAL, `string | null`). `src/db/database.ts` never imports `expo-localization`, keeping it off the node-test import path (MED-5/LOW-5). A null device region fails closed — national numbers stay non-actionable (LOW-4). Without this, a headless first-open migrates with null region and the one-shot canonicalization cannot re-run; the test suite must cover a HEADLESS first-open v8 upgrade, not only the App bootstrap (Codex HIGH/HIGH-3b)."
    - "The contact-parent rebuild sets `PRAGMA defer_foreign_keys = ON` inside the transaction; the pragma only defers constraint-VIOLATION checking to COMMIT and does NOT suppress ON DELETE actions, so EVERY table with a `REFERENCES contacts` clause is re-pointed to the new parent BEFORE `DROP TABLE contacts_old`. The child set is SIX tables — contact_links, interactions, events, fuel, custom_field_values, and app_settings — derived at migration time from `PRAGMA foreign_key_list` (or the hardcoded list is asserted equal to the derived one). Preservation is proven by per-child row-count equality, not `foreign_key_check` alone (HIGH-1/HIGH-2)."
    - "`app_settings` is rebuilt as the SIXTH FK child (its `sun_contact_id` REFERENCES contacts) with `phone_region_override TEXT` added in the new `CREATE TABLE`; the rebuild preserves every existing column value and the exact non-null `sun_contact_id`. `getAppSettings`/`updateAppSettings` read/write `phone_region_override`."
    - "This plan (wave 1) OWNS the full type/DAO widening for the region key (MED-1): the `PortableSettingsSnapshot` DECLARATION (`app-settings-dao.ts:126-145`, widened OPTIONALLY so 18.1-04 wave 3 emits the value into `getPortableSettingsSnapshot`), `AppSettingsPatch` (`:148-150`, `= Partial<Omit<PortableSettingsSnapshot,\"modifiedAt\">>`), the `WritableSettingsKey` union, `COLUMN_OF` (`:254`, consumed by the `:568-574` write loop), and the `AppSettingsRow` read projection + `getAppSettings` projection. It ALSO adds the camelCase `phoneRegionOverride` entry to `PORTABLE_SETTINGS_KEYS` (`backup-schema.ts:46-52`) here in wave 1 — inert until 18.1-04's snapshot emission populates the key, so landing it early is free and prevents `getPortableSettingsSnapshot` emitting a key `assertPortableSettings` (`backup-schema.ts:65-71`) would reject, which would throw on EVERY backup export during waves 1–2. The PORTABLE snapshot EMISSION, export wire, and restore patch remain 18.1-04's (H5/MED-1)."
    - "`validateAppSettingsPatch` (`app-settings-dao.ts:487-525`) gains an `assertPhoneRegionOverride` validator (null or ISO 3166-1 alpha-2) — it is an allowlist with no default-deny, restore feeds it untrusted manifest content, and an unvalidated bad region persists and then breaks every contact save via the normalizer's `defaultCountry`. 18.1-01 owns the writable-key union, so it owns the validator (MED-1, cycle 4)."
  prohibitions:
    - "No Phase 18 artifact may make a system-contact identifier authoritative over Orbit identity; source evidence remains local provenance."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-01-PLAN.md` (Tasks 1, 2, and 3). This plan is the **method half** of the original 18-01: the data-bearing v9 contacts rebuild that retires scalar phone/email into normalized method/link/provenance rows, plus the pure phone/email normalizer.

**Stripped away to 18.2-01 (v10 pure-shape migration):** the entire Bound/Unbound lifecycle schema — nullable `interval_days`, the `tracking_enabled` column, both cadence CHECK constraints, the one-way `BEFORE UPDATE OF interval_days` trigger (finding **H3**), the `include_unbound_never_contacted`/`birthday_unbound_enabled` app_settings columns, and the `reserved-columns` update for `tracking_enabled`.

**Straddling finding ownership:** this plan **keeps C3-H2's method/child preservation proof** (FK-safe parent/child rebuild, every custom_field_values row and the Orrery sun reference preserved). The **app_settings-lifecycle-column half of C3-H2** (preservation proof for the two Unbound settings columns) plus **H3** (cadence CHECK/trigger DDL) both **moved to 18.2-01**.

`phone_region_override` is a method-parsing setting and is **kept here** — it is added to app_settings by the v9 migration. In v9 `interval_days` stays NOT NULL and there is no explicit `tracking_enabled` column, so all migrated contacts are implicitly Bound; the explicit lifecycle flag arrives with 18.2's v10.

<objective>
## Phase Goal

Normalize phone/email into first-class, sync-ready contact methods without losing relationship data or making system contacts authoritative.

Purpose: establish the durable, migration-safe data path that every later Phase 18.1 slice consumes.
Output: parser seam, migration 009, normalized child schema, and real-chain migration tests.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-contact-data-normalization/18-CONTEXT.md
@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@docs/dossier/18-contact-data-normalization.md
@src/db/migrations/001-initial.ts
@src/db/migrations/008-restore-photo-journal.ts
@src/db/migrations/runner.ts
</context>

## Artifacts this phase produces

- A v9 contacts-table rebuild, mergeable method/link/provenance entities, and migration proofs.
- One pure phone/email normalizer used at migration and DAO boundaries.

<tasks>

<task type="auto" gate="none">
  <name>Task 1: Ratify the v9 storage and phone-actionability contracts — PRE-APPROVED</name>
  <resolution date="2026-08-28">**BOTH decisions are owner-approved; this task no longer blocks. Record the pre-approvals in the SUMMARY and proceed directly to Task 3.** (1) **Actionability threshold: `isValid()`** — a parsed phone is actionable (Call/Text enabled) only when it matches a real numbering plan for its effective region; failures stay stored/editable but non-actionable (dossier Cluster D). Representative test matrix (finalize in SUMMARY): device-default national + E.164 round-trip; one explicit `+`-international; one with an extension; one right-length-but-invalid that stores non-actionable. (2) **v9 on-disk storage contract: APPROVED by the owner 2026-08-28** after four cross-AI review cycles — proceed to write the normalizer and migration 009. Execution runs against v8 fixtures (unit tests + the device UAT fixture), not the owner's real data, so no separate backup-first stop is needed inside the phase; the owner takes his own device backup when he later installs the shipped build.</resolution>
  <read_first>18-CONTEXT.md Locked Invariants; 18-RESEARCH.md Patterns 1, 3, and 5; docs/dossier/18-contact-data-normalization.md Clusters C, I, O, and P</read_first>
  <action>No human confirmation is required — both decisions are already made (see resolution). As the first action, append to `18.1-01-SUMMARY.md`: the approved v9 contract (rebuilt contacts table retires scalar phone/email, `interval_days` stays NOT NULL and every migrated contact remains Bound, child methods/links/provenance carry UIDs and tombstone types, partial active-link unique index on `(provider, external_contact_id)`), the `isValid()` threshold, and the representative test matrix. This implements D-01 and D-02 and leaves picker/import/reconciliation UI outside Phase 18; the Bound/Unbound lifecycle contract (nullable cadence, `tracking_enabled`, cadence CHECKs, one-way trigger) is deferred to 18.2-01's v10 migration. Then proceed to Task 3.</action>
  <verify><automated>node -e "process.exit(0)"</automated></verify>
  <acceptance_criteria>The approved v9 storage contract and `isValid()` threshold are recorded in the SUMMARY. Both were owner-approved before execution (2026-08-28); no runtime human stop.</acceptance_criteria>
</task>

<task type="auto" gate="none">
  <name>Task 2: Record package provenance approvals (PRE-APPROVED) and install</name>
  <resolution date="2026-08-28">**Owner-approved; no runtime human stop.** `libphonenumber-js@1.13.11` provenance was verified by the owner on npmjs.com and is PINNED at 1.13.11 deliberately: the newest `1.13.12` was published ~20 hours prior with 0 downloads, while `1.13.11` has ~3.5M downloads over ~14 days — the older, widely-vetted release is the safer supply-chain bet, so do NOT bump to 1.13.12. `expo-localization` is an official first-party Expo SDK package and is approved as standard. Record both approvals in the SUMMARY as the first action, then install.</resolution>
  <read_first>18-RESEARCH.md Package Legitimacy Audit and Environment Availability; package.json</read_first>
  <files>package.json</files>
  <action>No human prompt required (see resolution). As the FIRST action, create-or-append to `18.1-01-SUMMARY.md` the recorded provenance approvals for BOTH `libphonenumber-js@1.13.11` (owner-verified, pinned over 1.13.12 for supply-chain age/adoption) and `expo-localization` (official Expo SDK, approved). The SUMMARY is written incrementally so the gate below is resume-safe (L1/LOW-3). Then install exactly `libphonenumber-js@1.13.11` and `expo-localization` (do this here or at the start of Task 3, whichever the executor prefers).</action>
  <what-built>The two approved dependencies, with their provenance approval recorded.</what-built>
  <verify><automated>node -e "const fs=require('fs'); const f='.planning/phases/18.1-contact-method-normalization/18.1-01-SUMMARY.md'; const s=(fs.existsSync(f)?fs.readFileSync(f,'utf8'):'').toLowerCase(); const ok=(pkg)=>{const i=s.indexOf(pkg); return i>=0 && /approv|provenance/.test(s.slice(i,i+240));}; if(!ok('libphonenumber-js')||!ok('expo-localization')) throw new Error('record BOTH libphonenumber-js@1.13.11 and expo-localization provenance approvals in 18.1-01-SUMMARY.md before continuing')"</automated></verify>
  <acceptance_criteria>The human records provenance approval for BOTH libphonenumber-js@1.13.11 and expo-localization in the SUMMARY, written incrementally at task start. The gate asserts that RECORDED approval (each package name followed within a short window by "approv"/"provenance", scanned across the whole file rather than a single line), not the package's absence, so it does not fail on resume once Task 3 has installed the packages (L1/LOW-3).</acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Migrate one legacy contact end to end into normalized method data</name>
  <reversibility rating="one-way">Migration 009 transforms shipped SQLite data and changing its child contract later requires a forward migration.</reversibility>
  <read_first>src/db/migrations/001-initial.ts; src/db/migrations/003-orrery-settings.ts; src/db/migrations/007-tombstones.ts; src/db/migrations/008-restore-photo-journal.ts; src/db/database.ts; src/db/app-settings-dao.ts; src/db/contact-links-dao.test.ts; 18-PATTERNS.md Database migration and Normalization boundary</read_first>
  <files>package.json, package-lock.json, src/logic/contact-method-normalization.ts, src/logic/contact-method-normalization.test.ts, src/db/migrations/009-contact-method-normalization.ts, src/db/migrations/009-contact-method-normalization.test.ts, src/db/database.ts, src/db/types.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts, src/db/reserved-columns.ts</files>
  <behavior>
    - Representative national and international phone inputs plus extensions follow the human-selected actionability threshold, become E.164 canonical data when actionable, and retain a separate extension and derived display; a valid email uses trimmed ASCII case-fold canonical equality.
    - A nonblank unparsable method remains stored with no canonical/actionable value.
    - Running the real migration chain on v8 copies legacy phone/email once as per-type primary rows, preserves every retained relationship field including the existing NOT NULL positive cadence, and retires the scalar columns.
    - A source link made stale remains a provenance row and cannot cascade-delete its Orbit contact or method.
    - National-format legacy phone values parse at migration time under the DEVICE region ONLY — resolved at the `App.tsx` bootstrap via `expo-localization` and passed through `openAndMigrate()` into `MigrationDeps.defaultPhoneRegion`. Migration 009 does NOT read `phone_region_override`: that column does not exist until 009 itself creates it, so a pre-migration read would throw `no such column`/`no such table` and wedge every launch (HIGH-3a). The override governs only POST-migration DAO/UI writes. National values canonicalize to E.164 when `isValid()` passes; explicit `+`-international values ignore the region and parse as-is. A number that FAILS validation under the device region remains STORED but non-actionable (never discarded), fixable later by a region-aware edit or a `phone_region_override` change (owner decision H4, replacing the prior raw-only policy). If the bootstrap yields no device region code, the migration FAILS CLOSED: national numbers stay non-actionable rather than guessing a region on the irreversible path (LOW-4).
    - Each canonicalized method row stores `canonical_region` — the region code the number was canonicalized under — and NULL for raw/non-canonicalized methods. This is the one-line, in-migration mitigation for the irreversible one-shot device-region canonicalization (owner decision cycle 2 HIGH-5): a number canonicalized to the wrong country is re-derivable later by an explicit action, while canonical identity is never auto-rewritten. Tests assert `canonical_region` is set for a canonicalized national number and NULL for a stored-but-non-actionable value.
    - A v8 fixture with zero, negative, or non-integer `interval_days` (reachable only via a prior restore bug) migrates SUCCESSFULLY: the malformed value is clamped to the pinned default `CLAMP_DEFAULT_INTERVAL_DAYS = 30` (a named top-of-file constant per the tunable-constants convention; 30 is the app's canonical "Monthly" cadence — matching `INLINE_CREATE_INTERVAL_DAYS` in `CaptureScreen.tsx:94` and the FrequencyPicker's Monthly `UNIT_FACTORS.months` default). The ORIGINAL value is snapshotted to `field_history` in the SAME transaction via the EXACT column set `(contact_id, field_col_name, old_value, operation, created_at)` (verify `001-initial.ts` `CREATE_FIELD_HISTORY` — there is no `new_value` column), with `field_col_name = 'interval_days'` (a reserved fixed-column name in `RESERVED_COLUMN_NAMES`, so it can never collide with a user custom-field `col_name`) and a free-text `operation` sentinel such as `'migration-009:interval-clamp'` (the column is free text, per `field-type-change.ts`'s `type_change:<old>-><new>`). The `field_history` snapshot is a BOUNDED ~30-day local trace — the launch field sweep deletes it and it is not carried in backup — it is a diagnostic breadcrumb, NOT durable recovery, consistent with `006-normalize-custom-field-values.ts:8`'s framing. The app opens normally. Tests assert the migration succeeds, the stored value equals exactly 30, and `field_history` holds the exact original for each of zero, negative, fractional, and text-affinity malformed inputs. Atomic rollback still applies to genuinely unrecoverable migration errors, not to this recoverable cadence case (owner decision M1, replacing the prior reject-and-leave-v8 policy).
    - Migration 009 sets `PRAGMA defer_foreign_keys = ON` as the first statement inside its transaction. CORRECTED RATIONALE (HIGH-2): this pragma defers constraint-VIOLATION checking to COMMIT; it does NOT suppress FK ACTIONS — `ON DELETE CASCADE`/`ON DELETE SET NULL` still fire immediately whenever a parent row is deleted, INCLUDING the implicit `DELETE` inside `DROP TABLE contacts_old`. The real invariant is therefore: EVERY table carrying a `REFERENCES contacts` clause must be re-pointed to the new `contacts` BEFORE the drop; the pragma only tolerates the transient window while parent rows exist under both `contacts` and `contacts_old`. The child set is SIX tables, not five — contact_links, interactions, events, fuel, custom_field_values, AND app_settings (its `sun_contact_id` REFERENCES contacts per `003-orrery-settings.ts:38-40`). The child list is DERIVED at migration time from `PRAGMA foreign_key_list` over `sqlite_master` (or, minimum, the test asserts the hardcoded six-table list equals the derived list) so the per-child proof cannot silently go stale when a later phase adds a child. Per-child row-count equality is a LOAD-BEARING assertion: `COUNT(before) == COUNT(after)` for EACH of the six children, and `app_settings.sun_contact_id` is preserved at its EXACT non-null value. `PRAGMA foreign_key_check` is retained but is explicitly NOT sufficient alone: after a full cascade it returns zero rows on a self-consistent-but-emptied database, so it cannot observe the catastrophic-delete failure that the per-child row counts catch.
    - `app_settings` is REBUILT as the sixth FK child (NOT `ADD COLUMN`, reversing cycle-1 M6): create the new `app_settings` with ALL its current columns from a PINNED LITERAL column list (accumulated across migrations 002/003/004/005/007) PLUS `phone_region_override TEXT`, retarget `sun_contact_id` to the new `contacts`, copy every column value including the exact non-null `sun_contact_id`, then drop the old table. The `AppSettingsRow` read projection (`app-settings-dao.ts:194-226`) is NOT an acceptable column source (MED-2): it is a read projection missing BOTH `created_at` (`002-app-settings.ts:48` — `created_at TEXT NOT NULL`, no default) AND `id`; enumerating from it silently drops `created_at` from every user's database forever. The literal list MUST include `created_at TEXT NOT NULL` and `id INTEGER PRIMARY KEY CHECK (id = 1)`. `app_settings` is part of the per-child preservation proof (six children total); the migration test asserts app_settings' rebuilt schema, its `sun_contact_id` FK target (new `contacts`), and the EXACT non-null `sun_contact_id` value after commit, and makes "post-rebuild `PRAGMA table_info(app_settings)` name-set == pre-rebuild name-set ∪ {`phone_region_override`}" a LOAD-BEARING assertion (irreversible column loss otherwise). Rebuilding is REQUIRED because renaming `contacts` rewrites app_settings' stored `REFERENCES` to `contacts_old`; leaving it unrebuilt would (a) null the Orrery sun at `DROP TABLE contacts_old` via `ON DELETE SET NULL`, and (b) leave every later `app_settings` write raising `no such table: contacts_old`.
    - `getAppSettings`/`updateAppSettings` in `src/db/app-settings-dao.ts` read and write `phone_region_override`; the migration default is NULL. Widening this is the ORDINARY app-settings read/write and requires widening ALL of: the `WritableSettingsKey` union (`:174-191`), `AppSettingsPatch` (`:148-150`, `= Partial<Omit<PortableSettingsSnapshot,"modifiedAt">>`), the `PortableSettingsSnapshot` DECLARATION (`:126-145` — widened OPTIONALLY so 18.1-04's wave-3 snapshot emission is what actually populates the value; MED-1), the `AppSettings` interface (`:37-118`), `AppSettingsRow` (`:194-226`), the `getAppSettings` projection (`:280-335`), and `COLUMN_OF` (`:254-272`) — 18.1-01 OWNS `COLUMN_OF`, the writable-key union, AND the `PortableSettingsSnapshot` declaration (MED-1). This plan ALSO adds the camelCase `phoneRegionOverride` entry to `PORTABLE_SETTINGS_KEYS` (`backup-schema.ts:46-52`) in wave 1 — inert until 18.1-04's snapshot emission populates the key, preventing a backup-export throw during waves 1–2. The remaining PORTABLE backup side (the `getPortableSettingsSnapshot` snapshot EMISSION, the export wire, the restore patch mapping) is owned by 18.1-04 and does NOT re-claim `COLUMN_OF` or the declaration (H5).
    - Note: `src/db/reserved-columns.ts:37-38` keeps `"phone"`/`"email"` in the reserved custom-field-name list DELIBERATELY (they shadow existing user custom-field names, not the retired scalar columns) — annotate this as an intentional decision, not drift (L2).
    - A v8 fixture seeds every pre-v9 app_settings column with a distinctive non-default value; after v9, every pre-existing value is equal and only the new v9 phone_region_override setting uses its declared default.
    - Schema assertions cover PRAGMA table_info and sqlite_master table/index SQL before and after migration for app_settings and every rebuilt child, proving each retained column, NOT NULL/default, index, UNIQUE constraint, AND CHECK constraint survives. CHECK is in the asserted set because `PRAGMA table_info` does NOT report CHECK constraints, so a table_info-driven rebuild drops them silently — and `app_settings` carries `CHECK (id = 1)` (`002-app-settings.ts:40`), the single-row invariant the entire DAO depends on (`WHERE id = 1` everywhere; `updateAppSettingsCore` asserts `changes === 1`). Losing it lets a future insert create an `id=2` row that `getAppSettings`'s `WHERE id = 1` never reads, so settings appear to silently not stick. A verbatim sqlite_master match is IMPOSSIBLE for the two shape-changing tables, so the assertion is stated as "sqlite_master SQL identical EXCEPT the enumerated intended deltas" — app_settings: `+phone_region_override`, `sun_contact_id` FK retargeted to the new `contacts`; contacts: `−phone`, `−email` — with those deltas listed; every other table (the four unchanged FK children) must match verbatim (MED-3).
    - Direct SQL rejects two primary methods of the same method_type for one contact through the v9 partial unique index while allowing one primary per different method type.
  </behavior>
  <action>After Tasks 1 and 2 approval, install exactly `libphonenumber-js@1.13.11` and `expo-localization` with Expo-compatible install tooling. Add the pure normalization module and RED-to-GREEN tests; it owns trimming, conservative email equality, parser-backed phone canonicalization/extension/display, and the threshold selected in Task 1 so migration/UI/DAO code cannot fork semantics. Migration-time region policy is DEVICE-region parsing ONLY (owner decision H4). Declare `defaultPhoneRegion?: string | null` as an EXPLICITLY OPTIONAL field on `MigrationDeps` in `src/db/types.ts` (currently `{ now, newUid }` at :36-39) — optionality is REQUIRED so the ~78 `runMigrations(exec, MIGRATIONS, TARGET_VERSION, { now, newUid })` call sites across ~55 files (e.g. `runner.test.ts`, `full-chain.test.ts`, `export-manifest.test.ts`, and the DAO suites) keep compiling untouched; 18.1-01's migration test covers BOTH the set and the unset case (HIGH-3b). Resolve the DEVICE region through a small platform-only provider module `src/services/device-region.ts` (a pure reader of `expo-localization`), and supply it from EVERY first-open `openAndMigrate()` caller — not only `App.tsx:120` (which currently calls it with no args), but ALSO the three HEADLESS/pre-React entry points that each call `openAndMigrate()` directly and can be the FIRST opener of an upgraded v8 database on a killed app: `src/services/notifications/notification-actions.ts:122`, `src/services/widget/widget-task-handler.tsx:90`, and `src/services/widget/widget-render.tsx:147` (Codex HIGH). Add an optional region parameter to `openAndMigrate()` and thread it into the `MigrationDeps` object it builds at `database.ts:127-132`; each caller imports `device-region.ts` and passes its result. Do NOT import `expo-localization` in `src/db/database.ts` — four node test files import `MIGRATIONS`/`TARGET_VERSION` from that module and a native module on that path can break them (MED-5); `device-region.ts` is the only importer, kept off `database.ts`. Without supplying the region on the headless paths, a killed-app notification/widget first-open migrates with null region → national numbers non-actionable and the one-shot canonicalization cannot re-run. CRITICALLY, the migration NEVER reads `phone_region_override` (the column does not exist until 009 creates it; a literal `SELECT phone_region_override FROM app_settings` throws `no such column`/`no such table` and rejects `openAndMigrate()` on every existing install — HIGH-3a). The override applies only to POST-migration DAO/UI writes where the column exists. If the bootstrap resolves NO device region code, pass null and FAIL CLOSED: national numbers stay stored non-actionable rather than guessing a region on the irreversible path (LOW-4). National-format legacy phones parse under the device region and canonicalize to E.164 when `isValid()` passes; explicit `+`-international values ignore the region. A number that fails validation under the device region is retained as a STORED non-actionable row (never discarded), fixable by a later region-aware edit or a post-migration `phone_region_override` change. Record `canonical_region` (the region a number was canonicalized under, NULL for raw/non-actionable rows) on each method row so a wrong-region actionable number is re-derivable by a future explicit action; canonical identity is never auto-rewritten (owner decision cycle 2 HIGH-5). Implement migration 009 as the runner-owned transaction. The FK-safe rebuild route is PINNED, not an executor choice (H6): as the FIRST statement inside migration 009's transaction, set `PRAGMA defer_foreign_keys = ON`. This is the only pragma that can be set inside the already-open transaction (the runner opens `BEGIN` before `apply` at `runner.ts:56-61`, and `PRAGMA foreign_keys` is a no-op inside a transaction per `database.ts:121-124`, where FK enforcement is ON). CORRECT RATIONALE (HIGH-2): `defer_foreign_keys = ON` defers constraint-VIOLATION checking to COMMIT; it does NOT suppress FK ACTIONS. `ON DELETE SET NULL`/`CASCADE` still fire immediately on any parent-row delete, INCLUDING the implicit `DELETE` inside `DROP TABLE contacts_old`. So the pragma does not by itself make the drop safe — the real invariant is that EVERY table carrying a `REFERENCES contacts` clause must be re-pointed to the new `contacts` BEFORE the drop; the pragma only tolerates the transient window where parent rows exist under both `contacts` and `contacts_old`. Do NOT use `PRAGMA legacy_alter_table` — combined with `foreign_keys = ON` it is the configuration SQLite's own docs warn can leave dangling references. With `defer_foreign_keys = ON`: rename the old contacts parent, create the new contacts table, copy retained parent IDs, then rebuild EVERY retargeted child so replacements point at the new contacts IDs before dropping the renamed parent. The child set is SIX tables: contact_links, interactions, events, fuel, custom_field_values, AND app_settings (its `sun_contact_id` REFERENCES contacts per `003-orrery-settings.ts:38-40` — reversing cycle-1 M6, app_settings IS a retargeted child). DERIVE this child list at migration time from `PRAGMA foreign_key_list` over `sqlite_master` (or, minimum, assert in the test that the hardcoded six-table list equals the derived one) so a future added child cannot silently escape the per-child proof. Rebuild `app_settings` by creating a new table from a PINNED LITERAL column list (accumulated across migrations 002/003/004/005/007: `id INTEGER PRIMARY KEY CHECK (id = 1)`, notifications/decay/birthday/digest/lockscreen toggles, the three hours, `sun_contact_id`, `self_sun_colour`, the five `ai_*` + four `ai_ack_*`, the backup interval/retention/folder/health columns, `data_revision`, `last_backup_data_revision`, `encryption_enabled`, `backup_nudge_dismissed`, `created_at TEXT NOT NULL`, and `modified_at`) PLUS the new `phone_region_override TEXT` and `sun_contact_id` retargeted to the new `contacts`; copy every column value including the exact non-null `sun_contact_id`, then drop the old app_settings. Do NOT source the column list from the `AppSettingsRow` shape (`app-settings-dao.ts:194-226`): it is a READ PROJECTION missing BOTH `created_at` (`002-app-settings.ts:48`, `TEXT NOT NULL`, no default) AND `id`, so using it silently drops `created_at` from every user's database irreversibly (MED-2). Make "post-rebuild `PRAGMA table_info(app_settings)` name-set == pre-rebuild name-set ∪ {`phone_region_override`}" a LOAD-BEARING assertion. The migration test also asserts app_settings' rebuilt schema, its `sun_contact_id` FK target, and the EXACT non-null `sun_contact_id` value after commit. Assert `COUNT(before) == COUNT(after)` per child as a load-bearing check, and keep `PRAGMA foreign_key_check` while noting in the test that it returns zero rows after a full cascade and therefore cannot alone prove preservation. Preserve every custom_field_values ID, UID, contact_id, field_def_id, value, and timestamp as well as all other child rows and SQLite IDs; recreate indexes on the rebuilt children — concretely there is exactly ONE named index on any rebuilt child (`idx_interactions_recency`, `001-initial.ts:112-113`) and ZERO triggers anywhere in `src/db` (LOW-3), so a rename-based child rebuild must DROP the old table BEFORE `CREATE INDEX` to avoid an index-name collision — then create UID-bearing ordered method/external-link/provenance tables, active-link partial uniqueness, and a partial unique primary-method index on `(contact_id, method_type) WHERE is_primary = 1`. Add direct-SQL migration tests for both a duplicate same-type primary rejection and a distinct-type primary success. `phone_region_override TEXT` reaches app_settings through the app_settings REBUILD above (its new `CREATE TABLE`), NOT via `ADD COLUMN` (reversing cycle-1 M6). Wire `getAppSettings`/`updateAppSettings` in `src/db/app-settings-dao.ts` to read/write it, widening ALL of: the `WritableSettingsKey` union (`:174-191`), `AppSettingsPatch` (`:148-150`), the `PortableSettingsSnapshot` DECLARATION (`:126-145`, widened OPTIONALLY; MED-1), the `AppSettings` interface (`:37-118`), `AppSettingsRow` (`:194-226`), the `getAppSettings` projection (`:280-335`), and `COLUMN_OF` (`:254-272`) — with tests in `app-settings-dao.test.ts`. ADD a per-key validator `assertPhoneRegionOverride` (accepts `null` or a two-letter ISO 3166-1 alpha-2 region code) into `validateAppSettingsPatch` (`app-settings-dao.ts:487-525`): that function is an allowlist with NO default-deny, so an unlisted key currently binds UNCHECKED into the `:568-574` UPDATE, and restore builds the patch from untrusted manifest content (`restore-apply.ts:184`) while `assertPortableSettings` checks only key membership, not value shape. Without the validator a corrupt `"phoneRegionOverride":"not-a-region"` restores and persists, then breaks EVERY subsequent contact save because 18.1-02 passes it as the normalizer's `defaultCountry` and libphonenumber-js rejects an unknown region (MED-1, cycle 4). Since 18.1-01 owns the writable-key union, it owns this validator; 18.1-04's restore round-trip test must cover one malformed value being rejected before the write. ALSO add the camelCase `phoneRegionOverride` entry to `PORTABLE_SETTINGS_KEYS` (`backup-schema.ts:46-52`) in this wave-1 plan (inert until 18.1-04's wave-3 snapshot emission populates the key; without it early, `getPortableSettingsSnapshot` in 18.1-04 would emit a key `assertPortableSettings` rejects, throwing on every backup export). 18.1-01 OWNS `COLUMN_OF`, the writable-key union, the `PortableSettingsSnapshot` declaration, and the `PORTABLE_SETTINGS_KEYS` entry for this key (MED-1); the remaining PORTABLE-settings side (the `getPortableSettingsSnapshot` snapshot EMISSION, export wire, restore patch) is owned by 18.1-04 and must NOT re-claim `COLUMN_OF` or the declaration (H5). `interval_days` remains NOT NULL as today and no `tracking_enabled` column is added — the lifecycle shape change (nullable cadence, `tracking_enabled`, the two cadence CHECKs, the one-way trigger, and the two Unbound settings columns) is owned by 18.2-01's v10 migration. Copy scalar values into primary methods. Handle a malformed legacy cadence (zero, negative, or non-integer `interval_days`, reachable only via a prior restore bug) by CLAMPING it to the pinned default `CLAMP_DEFAULT_INTERVAL_DAYS = 30` (declare this as a named top-of-file constant in the migration per the tunable-constants convention; 30 is the app's canonical "Monthly" cadence, matching `INLINE_CREATE_INTERVAL_DAYS` at `CaptureScreen.tsx:94` and the FrequencyPicker's Monthly `UNIT_FACTORS.months` default — there is no single pre-existing shared constant to import) and snapshotting the ORIGINAL value to `field_history` in the SAME transaction, then continuing — the migration succeeds and the app opens (owner decision M1). `field_history` already exists (`001-initial.ts` `CREATE_FIELD_HISTORY`); snapshot through it with the EXACT column set `(contact_id, field_col_name, old_value, operation, created_at)` (there is NO `new_value` column), setting `field_col_name = 'interval_days'` (a reserved fixed-column name in `RESERVED_COLUMN_NAMES`, so it can never collide with a user custom-field `col_name`) and a free-text `operation` sentinel such as `'migration-009:interval-clamp'`. State that this snapshot is a BOUNDED ~30-day local trace (the launch field sweep deletes it; it is excluded from backup) — a diagnostic breadcrumb, not durable recovery — consistent with `006-normalize-custom-field-values.ts:8`. Tests assert the exact clamped value (30) AND the exact `field_history` row for each of zero, negative, fractional, and text-affinity malformed inputs. Reserve atomic rollback for genuinely unrecoverable errors. Use bound SQL parameters, run row-count/copy assertions plus `PRAGMA foreign_key_check`. Register v9: bump `TARGET_VERSION` from 8 to 9 (`database.ts:43`) AND append `migration009` to the `MIGRATIONS` array (`database.ts:46-55`) — without both the migration never runs (LOW-2). `App.tsx`, the new `src/services/device-region.ts` provider, and the three headless/widget entry points (`src/services/notifications/notification-actions.ts`, `src/services/widget/widget-task-handler.tsx`, `src/services/widget/widget-render.tsx`) are in `files_modified` for the device-region resolution passed into `openAndMigrate()` on EVERY first-open path (MED-5/LOW-5/Codex HIGH); a dedicated 009-specific failure-copy branch alongside `isMigration006IntegrityError` (`App.tsx:222-235`) is OPTIONAL — the M1 clamp removes the main wedge case, so the generic "Couldn't start Orbit. Your data is safe and unchanged." copy is acceptable for 009. Seed every pre-v9 app_settings column with a distinctive non-default value and assert full pre-existing-row equality after v9. For app_settings and each rebuilt child, compare pre/post `PRAGMA table_info` metadata and sqlite_master table/index SQL, asserting retained columns, defaults, NOT NULL, indexes, UNIQUE constraints, AND CHECK constraints are complete — CHECK is explicitly in the set because `PRAGMA table_info` does not report it and `app_settings` carries `CHECK (id = 1)` (`002-app-settings.ts:40`), the single-row invariant the DAO's `WHERE id = 1` and `changes === 1` assertions depend on; a table_info-only rebuild drops it silently and lets a later insert create an unread `id=2` row (MED-3). State the sqlite_master check as "identical EXCEPT the enumerated intended deltas" — app_settings: `+phone_region_override`, `sun_contact_id` FK retargeted to the new `contacts`; contacts: `−phone`, `−email` — with those deltas listed; the four unchanged FK children must match verbatim. A verbatim whole-table match is impossible for the two shape-changing tables, so do not assert one there. Prove the safe rebuild with a v8 fixture containing every cascaded child, populated custom_field_values rows, and an Orrery sun reference. Implement D-01, D-02, and D-04 and address Cycle 3 C3-H2's method/child preservation proof and C3-M6 without adding import, refresh, or reconciliation behavior.</action>
  <verify><automated>npm test -- src/logic/contact-method-normalization.test.ts src/db/migrations/009-contact-method-normalization.test.ts src/db/app-settings-dao.test.ts</automated></verify>
  <acceptance_criteria>Real v8 fixtures prove the FK-safe atomic parent/child rebuild under the pinned `defer_foreign_keys = ON` route with per-child `COUNT(before) == COUNT(after)` equality across all SIX children (contact_links, interactions, events, fuel, custom_field_values, AND app_settings) as a load-bearing assertion, with the six-table list asserted equal to the `PRAGMA foreign_key_list`-derived set; scalar retirement, primary rows, DEVICE-region legacy regional policy (national values parse under the device region; validation failures stored non-actionable; a null device region fails closed), `canonical_region` recorded per canonicalized row and NULL for non-actionable rows, invalid-value retention, canonical precision, `phone_region_override` present via the app_settings REBUILD (not ADD COLUMN) with the exact non-null `sun_contact_id` and its FK re-pointed to the new `contacts`, preserved child rows/Orrery sun reference, and `foreign_key_check` integrity (retained but noted as insufficient alone — zero rows after a cascade). A malformed-cadence fixture migrates successfully with the value clamped to exactly 30 and the original snapshotted to `field_history` (`field_col_name = 'interval_days'`, exact column set) in-transaction (no rollback for that recoverable case), asserted for zero/negative/fractional/text-affinity inputs. `getAppSettings`/`updateAppSettings` round-trip `phone_region_override` (writable-key union + `COLUMN_OF` widened here). The fixture proves complete app_settings data preservation across every pre-v9 column (the rebuild column list is a pinned literal INCLUDING `created_at TEXT NOT NULL` and `id`, NOT sourced from the `AppSettingsRow` read projection, with post-rebuild `PRAGMA table_info` name-set == pre-rebuild ∪ {`phone_region_override`} as a load-bearing assertion; MED-2) and schema preservation through table_info/sqlite_master comparisons for app_settings and every rebuilt child, including indexes, UNIQUE constraints, and CHECK constraints (`app_settings`' `CHECK (id = 1)` survives; sqlite_master asserted identical except the enumerated deltas; MED-3). `MigrationDeps.defaultPhoneRegion` is exercised both set and unset, AND a HEADLESS first-open v8→v9 upgrade (region supplied via `device-region.ts` from a widget/notification entry point, not the App bootstrap) is covered (Codex HIGH). Migration tests directly prove the partial same-contact/type primary uniqueness constraint.</acceptance_criteria>
  <done>One existing user’s contact data reaches the new durable method model through the actual migration runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| v8 SQLite data to migration 009 | Existing relationship data and PII are transformed into durable v9 rows. |
| npm registry to local dependency graph | A recently published parser package enters the application build. |
| system-contact evidence to Orbit rows | Opaque source identifiers are retained as non-authoritative provenance. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-01 | Tampering/DoS | migration 009 | high | mitigate | Transaction-owned rebuild, `?` binding, FK/copy proofs, v8 fixture rollback tests, and backup-first device UAT. |
| T-18-02 | Information disclosure | normalizer/migration logs | high | mitigate | Keep values out of Logger/AI context and assert test failures without printing raw PII. |
| T-18-03 | Tampering | external links/provenance | medium | mitigate | Partial active-link uniqueness; no cascade from evidence to Orbit-owned method/contact. |
| T-18-04 | Elevation | actionable phone handoff | high | mitigate | Store actionability separately; later callers may consume only approved actionable primary reads. |
| T-18-SC | Tampering | npm install | high | mitigate | Blocking human provenance checkpoint precedes exact libphonenumber-js installation. |
</threat_model>

<verification>Run the named normalizer and migration suite only; retain the v8 fixture for the phase-end Android upgrade review. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.1-06 after every method consumer migration.</verification>
<success_criteria>The method storage contract is durable, test-proven, source-non-authoritative, and ready for transactional writes.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-01-SUMMARY.md` when done</output>
