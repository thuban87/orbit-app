---
phase: 18.1-contact-method-normalization
plan: 04
type: execute
wave: 3
depends_on: [18.1-01, 18.1-02]
files_modified: [src/backup/types.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts]
autonomous: true
requirements: [CDN-01, CDN-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Lossless manifest carries methods, extensions, primary/order, cadence, external links, provenance, and their stable UIDs/tombstones."
    - "Merge restore does not clear a locally stored dormant favourite rank, while portable backup deliberately does not make favourite rank a new wire field; cross-contact shared canonical methods remain intact without scalar compatibility fields."
    - "Stale source evidence survives restore unless an explicit local tombstone authorizes removal."
    - "Pre-Phase-18 v1 backups deterministically forward-migrate to the v2 normalized method graph with the phone_region_override portable-settings default."
    - "`phone_region_override` round-trips through the PORTABLE seam THIS plan owns: PORTABLE_SETTINGS_KEYS and the getPortableSettingsSnapshot projection (plus the restore patch mapping and the v1→v2 default) — so a freshly exported v2 manifest carries it (not only the synthetic v1 path) and restore applies it instead of silently dropping it (H5). The `COLUMN_OF` widening and the writable-key union that let `updateAppSettingsCore` WRITE the value are owned by 18.1-01 (MED-1); this plan relies on them, it does not re-declare them."
    - "Merge restore resolves a natural-key collision — two `is_primary=1` rows for one `(contact_id, method_type)`, or two active links sharing `(provider, external_contact_id)` — deterministically as a POST-reconciliation normalization of the produced `ReconciliationAction[]` (demote all-but-one primary; keep one active link, mark the other stale) so the v9 partial unique index never raises SQLITE_CONSTRAINT, WITHOUT rejecting the restore. Tests assert WHAT SURVIVES, not that the restore was rejected (H3)."
    - "A same-contact canonical duplicate (two method UIDs with equal canonical value for one contact) collapses to one surviving row in the same post-reconciliation pass, so it does not restore as two endpoints — it is not routed through the whole-restore rejection path (dossier Cluster C; M2)."
    - "CDN-04 boundary, precision, idempotency, and concurrency cases are verified on the reconciliation graph."
  artifacts: [src/backup/types.ts, src/backup/export-manifest.ts, src/backup/backup-schema.ts, src/backup/reconciliation.ts, src/backup/restore-apply.ts]
  key_links: ["The closed restore entity map reconciles normalized children by UID after live parent survivor validation, and backup-schema forwards v1 snapshots before validation.", "One typed contact-purge registry drives child preview counts, tombstone emission, and explicit fan-out deletes, so adding a tombstone entity cannot silently skip purge evidence."]
  prohibitions:
    - "Backup/restore must not reinterpret a missing or stale system source as authorization to delete or overwrite Orbit-owned data."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-07-PLAN.md` (Tasks 1–3, **method parts only**). Carries the normalized backup/export/restore graph for **methods**: UID-bearing method/link/provenance manifest arrays, strict graph validation, UID reconciliation, tombstones, purge, and the v1→v2 wire forward-migration for normalized method rows plus the `phone_region_override` portable-setting default.

**Does NOT carry lifecycle fields:** `tracking_enabled` and nullable cadence do not exist until v10, so the contact record here keeps its existing NOT NULL positive cadence and no `trackingEnabled` wire field. The **lifecycle backup extension** (carry `tracking_enabled` + nullable cadence) plus **C3-M4** (reject illegal lifecycle/cadence combos before apply) **moved to 18.2-09**, which extends the v2 format on top of this graph.

**Dependency:** the spurious dep on old 18-05 is **severed** — backup serializes schema columns present from the v9 migration and does not need the Unbound UI or settings screen.

**Retained straddling findings:** keeps **C3-M1** (Replace-all removes seeded `field_history` in FK-safe order) and **C3-M6** (reject duplicate surviving primary methods by `(contactUid, type)` on restore).

**Cross-AI review additions (cycle 1, corrected in cycle 2):** owns the PORTABLE side of `phone_region_override` (**H5**) — the export projection, `PORTABLE_SETTINGS_KEYS`, the restore patch mapping, and the v1→v2 default — so a freshly exported v2 manifest carries it and restore applies it. It does NOT own `updateAppSettingsCore.COLUMN_OF` or the writable-key union (those are 18.1-01; **MED-1**). Cycle 2 also REPLACES the cycle-1 collision handling: the merge-restore natural-key collisions for duplicate `is_primary=1` and duplicate active-link keys (**H3**) and the same-contact canonical duplicate (**M2**) are now resolved by a POST-reconciliation normalization pass over the produced `ReconciliationAction[]` — NOT by routing them through `incompatibleRows`, which rejects the ENTIRE restore (**HIGH-4**). Also keeps the compile-time exhaustive residual-table list rather than a behavior-only `field_history` deletion test (**M5**).

**Shared-file note for the re-plan:** `src/backup/types.ts`, `src/backup/backup-schema.ts`, and `src/backup/restore-apply.ts` are also touched by 18.2-09 for the lifecycle extension — the re-plan must reconcile shared-file ownership. `src/db/app-settings-dao.ts` is also touched by 18.1-01, which owns the `phone_region_override` column (via the app_settings rebuild), the ordinary `getAppSettings`/`updateAppSettings` read/write, `COLUMN_OF`, and the writable-key union; this plan owns ONLY the PORTABLE seam of that file (the `getPortableSettingsSnapshot` projection and `PortableSettingsSnapshot` value flow), and 18.1-01 (wave 1) lands before this plan (wave 3).

<objective>Carry the normalized contact-method model through lossless export validation and transactional restore.
Purpose: ensure the sync-ready local method model remains recoverable without making provenance authoritative.
Output: versioned manifest graph, strict validation, and UID reconciliation support.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/backup/export-manifest.ts
@src/backup/backup-schema.ts
@src/backup/restore-apply.ts
@src/backup/reconciliation.ts</context>

## Artifacts this phase produces

- Backup manifest arrays and strict graph validation for normalized method children.
- Restore entity registration that preserves local/provenance semantics and tombstones.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Export and parse one normalized contact's methods losslessly</name>
  <read_first>src/backup/types.ts; src/backup/export-manifest.ts:39-83; src/backup/backup-schema.ts:77-175; src/backup/export-manifest.test.ts; 18-PATTERNS.md Backup, restore, and tombstones</read_first>
  <files>src/backup/types.ts, src/backup/export-manifest.ts, src/backup/export-manifest.test.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/db/app-settings-dao.ts, src/db/app-settings-dao.test.ts</files>
  <behavior>
    - Manifest uses first-class UID-bearing arrays for methods, links, and method provenance.
    - Contact record carries its existing positive cadence but no `tracking_enabled` field and no scalar phone/email compatibility fields.
    - Parse rejects orphan children, duplicate UIDs, invalid parent/tombstone relationships, unsupported entity types, and two surviving primary methods of one type before restore opens its transaction.
    - A v1 scalar-method fixture advances deterministically to v2 child rows, the phone_region_override portable-setting default, and every v2 required array before strict validation.
    - `phone_region_override` is a portable setting: THIS plan adds it to `PORTABLE_SETTINGS_KEYS` (`backup-schema.ts:46-52`) and to the `getPortableSettingsSnapshot` projection (`app-settings-dao.ts:344-375`), so a FRESHLY exported v2 manifest includes it, not only the synthetic v1-forward-migration path (H5). The `updateAppSettingsCore` `COLUMN_OF` (`app-settings-dao.ts:254-272`) and the writable-key union that let restore's patch actually WRITE the value are widened by 18.1-01 (MED-1); this plan relies on that wave-1 widening — without a `COLUMN_OF` entry the `:568-574` loop would silently drop the key, so the restore round-trip test here also proves the value is written, confirming 18.1-01's widening landed.
  </behavior>
  <reversibility rating="costly">Adding child entities and tombstone types to an already portable recovery graph requires durable schema/version compatibility support, but existing-version parsing remains supported.</reversibility>
  <action>Extend the existing manifest types/export snapshot/strict parser rather than introducing a second format. Bump BACKUP_FORMAT_VERSION to 2 and register the one deterministic v1-to-v2 FORWARD_MIGRATIONS entry before validation: convert each v1 scalar phone/email into a stable UID formatted `legacy-method:${contactUid}:${type}` (deterministic by parent and type, disjoint from newUid’s UUID shape), preserve the legacy presentation value, retain positive legacy cadence, remove the retired scalar `phone` and `email` keys from each migrated contact record, and add the absent `phoneRegionOverride=null` setting. The `includeUnboundNeverContacted`/`birthdayUnboundEnabled` lifecycle settings and `trackingEnabled` are added by 18.2-09 when v10 exists. Before validate runs, materialize every v2 required array, including empty external-link, method-provenance, and tombstone arrays where the v1 input has none. Add tests for derived UID determinism and disjointness from newUid output. In validate, require each contact `intervalDays` to be a positive integer; scan surviving contact-method rows by `(contactUid, type)` and reject more than one primary with BackupSchemaError before restore planning. Include D-01, D-02, and D-04 fields exactly, parent-reference and same-file survivor checks, allowed new tombstone entity types, and the closed portable-settings allowlist. Own the PORTABLE side of `phone_region_override` (H5): add it to `PORTABLE_SETTINGS_KEYS` and to the `getPortableSettingsSnapshot` projection so a fresh v2 export emits it, and map it in the restore patch. Do NOT add it to `updateAppSettingsCore`'s `COLUMN_OF` or the writable-key union — those are widened by 18.1-01 in wave 1 (MED-1); this plan relies on that so restore's typed patch applies the value instead of the `:568-574` loop dropping it. Prove in `app-settings-dao.test.ts` that a non-null override survives a full export→restore round trip (not only the v1 default path), which also confirms 18.1-01's `COLUMN_OF` widening is in place. Retain only formatted/structured backup data; do not add a system-contact refresh or delete interpretation. This addresses cycle-2 reviews L11 and L12 plus Cycle 3 C3-M6.</action>
  <verify><automated>npm test -- src/backup/export-manifest.test.ts src/backup/backup-schema.test.ts</automated></verify>
  <acceptance_criteria>Export→parse tests prove a single normalized contact retains all method/provenance fields, a v1 fixture upgrades with its phone/email methods intact, strips scalar compatibility keys, and has the deterministic phone_region_override default. A freshly exported v2 manifest includes `phone_region_override`, and a non-null override round-trips through export and restore via the portable seam this plan owns (PORTABLE_SETTINGS_KEYS + snapshot projection + restore patch), relying on 18.1-01's `COLUMN_OF`/writable-key widening to be written and never silently dropped (H5/MED-1). Negative parser tests return BackupSchemaError before restore planning for malformed child graphs and duplicate surviving primaries by contact/type.</acceptance_criteria>
  <done>One complete normalized method record has a lossless, validated backup representation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconcile normalized child entities through restore</name>
  <read_first>src/backup/restore-apply.ts:35-175; src/backup/reconciliation.ts; src/backup/restore-apply.test.ts; src/backup/reconciliation.test.ts; src/db/tombstones-dao.ts; src/db/purge-dao.ts; src/db/purge-dao.test.ts</read_first>
  <files>src/backup/restore-apply.ts, src/backup/restore-apply.test.ts, src/backup/backup-schema.test.ts, src/backup/reconciliation.ts, src/backup/reconciliation.test.ts, src/db/tombstones-dao.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts</files>
  <behavior>
    - Restore reconciles each UID-bearing child atomically after parent survivor validation.
    - Reapplying the same manifest is idempotent and concurrent/conflicting plan cases fail atomically rather than aliasing data.
    - Stale link/provenance rows remain unless an explicit tombstone is present; no source absence deletes a method/contact.
    - Merge restore RESOLVES a natural-key collision (it does NOT reject the restore): after `reconcileEntity` produces the `ReconciliationAction[]`, a POST-reconciliation normalization pass over that plan (a) demotes all-but-one deterministic winner per `(contactUid, method_type)` where `is_primary=1` (a local-retained row plus an incoming insert), so exactly one primary survives, and (b) for the global `(provider, external_contact_id)` active-link collision across contacts, deterministically keeps one active link (newer `modified_at`) and marks the other inactive/stale — a non-destructive resolution, not a whole-restore rejection. This runs BEFORE apply, so the v9 partial unique indexes never raise `SQLITE_CONSTRAINT_UNIQUE`. `reconcileEntity` is UID-keyed only (`reconciliation.ts:334-336` emits insert for incoming-only UIDs and retain for local-only UIDs, consulting no natural key), so the normalization is a NEW pass over its output — deliberately NOT `incompatibleRows` (`:229-267`/`:88`), whose `ReconciliationIncompatibility` result rejects the ENTIRE restore and would falsely reject the ordinary "user changed their primary phone" backup (local primary UID-A demoted, new UID-B promoted — a correct plan that violates nothing). Tests assert WHAT SURVIVES for the duplicate-primary and duplicate-active-link cases, not that the restore was rejected (H3/HIGH-4).
    - A same-contact canonical duplicate — two method UIDs whose canonical values are equal for one contact — collapses to ONE surviving row in that same post-reconciliation normalization pass, so it does not restore as two visible endpoints (dossier Cluster C). It is NOT routed through the `incompatibleRows` whole-restore rejection path; tests assert the single surviving endpoint (M2/HIGH-4).
    - Contact purge previews, tombstones, and explicitly deletes every UID-bearing normalized child; a typed registry makes every TombstoneEntityType disposition explicit.
    - Merge restore leaves an existing local dormant favourite_rank untouched because favourite rank is not promoted into the portable v2 contract.
    - Replace-all removes non-mergeable owned residual rows (currently `field_history`, already deleted at `restore-apply.ts:150`) in an explicit FK-safe order. The guarantee is enforced at COMPILE TIME by an exhaustive typed residual list — a `Record` over the entity-type union that fails to compile when a new entity type is added — NOT merely by a test asserting today's `field_history` deletion (which passes regardless of whether the exhaustive list exists) (M5, C3-M1).
  </behavior>
  <action>Extend the exhaustive MergeableEntityType/ENTITY_POLICIES contract in reconciliation.ts and its owner tests before registering contact methods, external contact links, and method provenance in restore-apply's closed entity map. Give every new child an explicit UID write mode and parent survivor policy; make the compiler/test reject an unregistered entity. Add a natural-key RESOLUTION pass (H3/HIGH-4) — NOT a rejection path. Do NOT route these through `incompatibleRows`/`ReconciliationIncompatibility` (`reconciliation.ts:229-267`, `:78-84`): per `:88` any incompatibility rejects the ENTIRE restore (`restore-apply.ts:171` returns `incompatible-destination` before the write transaction opens), which would falsely reject the ordinary "user changed their primary phone" backup and abandon the whole recovery over a cosmetic duplicate. Instead, add a deterministic POST-reconciliation normalization over the produced `ReconciliationAction[]` that: (a) per `(contactUid, method_type)`, if the reconciled plan would leave more than one `is_primary=1` row, demotes all but one deterministic winner so exactly one primary survives; (b) collapses same-contact canonical duplicates (two method UIDs with equal canonical value for one contact; Cluster C, M2) to one surviving row; and (c) for the global `(provider, external_contact_id)` active-link collision across contacts, keeps one active link (newer `modified_at`) and marks the other inactive/stale (non-destructive). This runs BEFORE apply so the v9 partial unique indexes never raise `SQLITE_CONSTRAINT_UNIQUE`. Add reconciliation tests that assert WHAT SURVIVES (one primary, one endpoint, one active link) for each case — not that the restore was rejected — and a test that the "changed-primary" backup restores successfully rather than being rejected. Derive replaceAllReset’s tombstone and mergeable delete sequences from required typed maps over MergeableEntityType, then pair them with an explicit exhaustively typed residual owned-table list for non-mergeable tables, currently `field_history`; preserve the complete FK-safe delete order (children before contacts/custom_field_defs). Test replace-all tombstones for a method, external link, and provenance UID. For the residual non-mergeable tables, make the guarantee COMPILE-TIME: an exhaustively typed residual list (a `Record` over the entity-type union) must fail to compile when a new entity type is added — do not rely on a `field_history` deletion test alone, which passes whether or not the exhaustive list exists (M5). Seeding `field_history` and observing its removal is a supporting check, not the primary acceptance. In purge-dao, derive preview counts, tombstone sources, and explicit child fan-out deletes from one `Record<TombstoneEntityType, PurgeChildSpec | null>` plus the same explicit exhaustively typed non-mergeable residual owned-table list, so every current and future tombstone type and owned residual has an explicit purge disposition; include contact_methods, external_contact_links, and contact_method_provenance in their appropriate child specs. Extend purge tests to prove the preview counts, explicit deletes, tombstone evidence, and residual-table deletion ordering. Keep a single outer transaction and respect existing data_revision conventions. For merge restore only, prove an existing local dormant favourite_rank is never cleared because the contact upsert does not write that non-portable field; replace-all remains the intentional portable-state reset and makes no rank-preservation claim. Implement D-04’s non-destructive provenance and D-01/D-02 data retention without auto-merge, source refresh, or generic sync logic. This addresses cycle-2 reviews H1, M7, M8, and L10 plus Cycle 3 C3-M1.</action>
  <verify><automated>npm test -- src/backup/restore-apply.test.ts src/backup/backup-schema.test.ts src/backup/reconciliation.test.ts src/db/purge-dao.test.ts</automated></verify>
  <acceptance_criteria>Integration tests prove export→parse→restore preserves methods/extensions/primary/order, stale evidence, shared canonical values, and history; merge restore RESOLVES a duplicate `is_primary=1` `(contactUid, type)` collision (one surviving primary) and a duplicate `(provider, external_contact_id)` active-link collision (one active link kept, the other marked stale) via a post-reconciliation normalization of the action plan, before the v9 unique indexes can raise SQLITE_CONSTRAINT, WITHOUT rejecting the restore — and a "user changed their primary phone" backup restores successfully rather than being rejected (H3/HIGH-4); a same-contact canonical duplicate restores as one endpoint, not two (M2); merge restore does not clobber a local dormant rank; replace-all emits tombstones for every new child, and the non-mergeable residual guarantee is enforced by a compile-time exhaustive typed residual list that fails to build when a new entity type is added — not by the `field_history` deletion test alone (M5); purge previews/deletes/tombstones every normalized child and handles residual owned tables in the declared FK-safe order; and repeat/concurrent conflicts meet the existing reconciliation contract.</acceptance_criteria>
  <done>The normalized method graph restores safely without inventing source authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Cover explicit-delete tombstones and backup wire compatibility</name>
  <read_first>src/db/tombstones-dao.ts; src/backup/backup-schema.ts; src/backup/restore-apply.test.ts; Phase 17 summaries for reconciliation contracts</read_first>
  <files>src/db/tombstones-dao.ts, src/backup/backup-schema.ts, src/backup/backup-schema.test.ts, src/backup/restore-apply.test.ts</files>
  <reversibility rating="costly">New tombstone entity types change the durable reconciliation graph; the established UID/version rules make the forward-compatible change recoverable without deleting existing backup data.</reversibility>
  <action>Add targeted BackupSchemaError cases for method/link/provenance tombstones, malformed same-UID/survivor combinations, intervalDays zero/negative/non-integer, and duplicate surviving primary methods by `(contactUid, type)`. Assert each invalid graph is rejected by parse before restore-apply opens its transaction or issues SQL. Preserve existing backup format-version handling, parameterized writes, transaction boundaries, and Phase 17 data-revision rules. Implement D-04 while keeping source disappearance separate from an explicit local delete. This addresses Cycle 3 C3-M6.</action>
  <verify><automated>npm test -- src/backup/backup-schema.test.ts src/backup/restore-apply.test.ts src/backup/reconciliation.test.ts</automated></verify>
  <acceptance_criteria>Negative and idempotency tests demonstrate only a valid explicit tombstone removes a normalized child; malformed cadence/primary graphs return BackupSchemaError before apply; and failed validation cannot partially mutate local data.</acceptance_criteria>
  <done>Deletion evidence is precise, sync-ready, and never inferred from a source’s current contents.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite export to portable manifest | Local PII and relationship graph become a recovery artifact. |
| Parsed manifest to transactional restore | Untrusted backup content proposes durable changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-21 | Tampering | manifest/restore graph | high | mitigate | Strict UID/parent/tombstone validation before a single restore transaction and integration tests. |
| T-18-22 | Information disclosure | export/logging | high | mitigate | Keep full data inside existing encrypted backup path; avoid raw record logging and route/privacy changes. |
| T-18-23 | Tampering | provenance reconciliation | high | mitigate | Explicit tombstones only; source absence has no destructive behavior. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in backup work. |
</threat_model>
<verification>Run the named manifest, schema, reconciliation, and restore integration suites at each task, keeping normalized graph work within the declared backup owners. The repository-wide test, TypeScript, and color compatibility gate is deferred to 18.1-06 after every method consumer has been migrated.</verification>
<success_criteria>Backup and restore preserve the whole normalized method graph with safe non-destructive provenance semantics.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-04-SUMMARY.md` when done</output>
