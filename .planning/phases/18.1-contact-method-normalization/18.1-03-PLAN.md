---
phase: 18.1-contact-method-normalization
plan: 03
type: execute
wave: 3
depends_on: [18.1-02]
files_modified: [src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/CreateContactScreen.tsx, src/screens/EditContactScreen.tsx, src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.test.tsx]
autonomous: true
requirements: [CDN-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Create/edit form logic owns reversible method drafts until Save."
    - "Method cards obey stable ordering, primary selection/promotion, labels/extensions, invalid helper copy, and 44px accessible controls."
    - "Canonical duplicates collapse on save without warning about other contacts."
    - "CreateContactScreen and EditContactScreen drop their scalar phone/email state, wire the ContactMethodsEditor, and pass method drafts (not scalar phone/email) into createContactFull/updateContactFull, so the phase compiles against the retired scalar contract (H2)."
    - "SettingsScreen exposes a phone-region-override control that reads/writes `phone_region_override` via the app-settings DAO seam from 18.1-01 (dossier Cluster D; M3)."
  artifacts: [src/components/ContactMethodsEditor.tsx, src/screens/create-contact-logic.ts, src/screens/edit-contact-logic.ts]
  key_links: ["After the effective region (saved override or device fallback) is resolved, form logic passes it to the aggregate DAO and renders its typed same-contact collision result without UI-side normalization."]
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-04-PLAN.md` (Tasks 1–3, **method-editor parts only**). Carries the controlled `ContactMethodsEditor` and the create/edit method-draft shaping logic.

**Moved to 18.2-08:** ONLY the Bound/Unbound lifecycle **choice** control in the create/edit form. The dependency on old 18-05 is **severed** — the method-editor half depends only on the method DAO (18.1-02), not on the Unbound settings screen.

**Screen wiring is owned HERE (H2 resolution):** the METHOD wiring of `CreateContactScreen.tsx` and `EditContactScreen.tsx` — removing scalar phone/email state, mounting the `ContactMethodsEditor`, and passing method drafts into `createContactFull`/`updateContactFull` — is owned by THIS plan and is in `files_modified`. This closes the loop 18.1-02 opens by retiring the scalar DAO contract: without it the phase cannot typecheck. 18.2-08 later ADDS only the Bound/Unbound lifecycle choice control onto these already-method-wired screens; it does not do the method wiring.

<objective>Deliver the create/edit method-editor slice for real normalized methods.
Purpose: users can maintain ordered contact methods without losing draft data.
Output: controlled method editor and create/edit method-draft shaping logic matching the UI contract.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/components/LinksEditor.tsx
@src/screens/CreateContactScreen.tsx
@src/screens/EditContactScreen.tsx
@src/screens/create-contact-logic.ts
@src/db/app-settings-dao.ts
@src/db/contact-methods-dao.ts</context>

## Artifacts this phase produces

- Reusable controlled Phone numbers and Email addresses editor.
- Create/edit method-draft shaping logic and atomic method-draft save adapters.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Shape one created contact with an editable phone draft</name>
  <read_first>18-UI-SPEC.md Create contact and Edit contact; src/components/LinksEditor.tsx:42-205; src/screens/create-contact-logic.ts; src/db/contacts-dao.ts</read_first>
  <files>src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/create-contact-logic.ts, src/screens/create-contact-logic.test.ts, src/screens/CreateContactScreen.tsx</files>
  <behavior>
    - The initial empty create-phone row disappears when blank, while a nonblank invalid value remains a visible stored draft.
    - One phone group uses controlled stable-UID rows, resolves saved override else device region through the effective-region seam, and yields the aggregate DAO method-array draft contract.
    - CreateContactScreen removes its scalar `phone` state (:87, :111-122) and the `phone` field passed into `buildCreateInput`/`createContactFull` (:140-146), mounts the `ContactMethodsEditor`, and passes the shaped method-array draft into `createContactFull` (H2).
  </behavior>
  <action>Build the controlled ContactMethodsEditor from LinksEditor’s draft/card model, theme and accessibility conventions. Shape one real Create path through method draft shaping, the effective-region seam (saved `phone_region_override` else device region), and createContactFull's method-array contract. Pass the effective region as aggregate-write input; do not invoke the normalizer in UI code. Wire `CreateContactScreen` to this method draft/editor path: remove its scalar `phone` state and the scalar `phone` argument into `createContactFull`, mount the `ContactMethodsEditor`, and pass the method-array draft into the aggregate save (H2). Implement D-01 and the exact UI-SPEC method copy/scan order; use no raw colors, DB calls in the component, parser details, or reorder affordance. Only the Bound/Unbound lifecycle **choice** control on these screens is owned by 18.2-08; the method wiring is owned here.</action>
  <verify><automated>npm test -- src/components/ContactMethodsEditor.test.tsx src/screens/create-contact-logic.test.ts</automated></verify>
  <acceptance_criteria>Form logic tests prove blank-row disposal, saved-region override, device fallback, and that later region choices do not rewrite an existing method identity; the shaped draft reaches createContactFull's method-array contract end to end.</acceptance_criteria>
  <done>The first UI-to-DAO method path is usable and production-quality.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expand editor groups, labels, primaries, and invalid-state feedback</name>
  <read_first>18-UI-SPEC.md Edit contact and Copywriting Contract; src/components/LinksEditor.tsx; src/logic/contact-method-normalization.ts</read_first>
  <files>src/components/ContactMethodsEditor.tsx, src/components/ContactMethodsEditor.test.tsx, src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts</files>
  <behavior>
    - Phone/email groups expose labels/custom labels, phone extension, stored order, Primary controls, and draft-only Remove.
    - Removing a selected primary selects the next draft row; after Save, the typed aggregate-save `canonicalDuplicate` result collapses a same-contact duplicate and displays exactly `This matches an existing {phone number/email address}; only one will be kept.`.
    - Invalid methods remain editable with exact helper copy and actionability stays DAO-derived.
  </behavior>
  <action>Expand the tracer editor to both type groups and implement draft reducer/shaping tests for ordering, primary selection, extension, label, and duplicate feedback. Consume only the typed `canonicalDuplicate` result returned by aggregate save: collapse the local draft to the returned surviving row and render the exact UI-SPEC helper after a same-contact collision. Follow D-01 exactly: a canonical match across a different contact receives no warning, helper, or client-side lookup. Keep removal reversible before Save and omit endpoint action/router/history features.</action>
  <verify><automated>npm test -- src/components/ContactMethodsEditor.test.tsx src/screens/edit-contact-logic.test.ts</automated></verify>
  <acceptance_criteria>Controlled-editor and shaping tests cover empty, multiple, primary promotion, invalid, extension, 44px/a11y state behavior, exact same-contact helper/collapse on Save, and absence of cross-contact feedback.</acceptance_criteria>
  <done>Both type groups satisfy the ordered first-class method interaction contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Shape full edit save and recoverable drafts</name>
  <read_first>src/screens/EditContactScreen.tsx; src/screens/edit-contact-logic.ts; src/db/contact-read.ts; 18-UI-SPEC.md Edit contact</read_first>
  <files>src/screens/edit-contact-logic.ts, src/screens/edit-contact-logic.test.ts, src/screens/EditContactScreen.tsx</files>
  <behavior>
    - Edit assembles seeded ordered methods from the new read boundary.
    - Back discards local edits; a save error preserves every draft for retry.
    - Save uses one aggregate write, receives the effective-region context and typed collision result, and never references legacy scalar fields.
    - EditContactScreen removes its scalar phone/email `TextInput`s and `form.phone`/`form.email`/`setField("phone"|"email", …)` state (:557-586), mounts the `ContactMethodsEditor` seeded from the ordered method read, and routes Save through `updateContactFull`'s method-array contract (H2).
  </behavior>
  <action>Replace EditContact’s scalar field state with seeded method drafts and route its Save through the aggregate DAO with the region-policy seam and returned typed collision result. Preserve existing non-optimistic loading/error posture and exact UI-SPEC error copy. Implement D-01 without adding a source/provenance settings surface. Wire `EditContactScreen` to this method path (remove scalar phone/email `TextInput`s and state, mount the editor, route Save through `updateContactFull`'s method-array contract; H2). Only the lifecycle seeding and Bound/Unbound choice control on the screen are owned by 18.2-08.</action>
  <verify><automated>npm test -- src/screens/edit-contact-logic.test.ts</automated></verify>
  <acceptance_criteria>Logic tests prove saved drafts round-trip through ordered reads, typed same-contact collision feedback preserves the surviving draft row, cross-contact matches remain silent, and failed saves retain the same draft state.</acceptance_criteria>
  <done>Users can edit existing contacts' methods without scalar endpoint storage or destructive draft loss.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 4: Add the phone-region-override control to Settings</name>
  <read_first>docs/dossier/18-contact-data-normalization.md Cluster D; src/screens/SettingsScreen.tsx; src/db/app-settings-dao.ts</read_first>
  <files>src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.test.tsx</files>
  <behavior>
    - Settings exposes a control that reads the persisted `phone_region_override` and writes it through the app-settings DAO seam exposed by 18.1-01 (`getAppSettings`/`updateAppSettings`).
    - A null override means "use the device region"; a set override becomes the effective default region for national-format phone parsing (dossier Cluster D "user-overridable in Settings").
  </behavior>
  <action>Add the phone-region-override setting to `SettingsScreen` per Cluster D, reading and writing `phone_region_override` via the app-settings DAO seam from 18.1-01 (do not add a second DAO). This makes the migration's non-actionable numbers (H4) fixable in bulk by changing the effective region. Use no raw colors and no direct SQL in the component; keep the existing Settings load/error posture. Implement D-02's user-overridable region without adding a picker/import/provenance surface.</action>
  <verify><automated>npm test -- src/screens/SettingsScreen.test.tsx</automated></verify>
  <acceptance_criteria>Tests prove the control reads a persisted override, writes a changed override through the DAO seam, and that a null override falls back to the device region; no component-level SQL and no raw colors.</acceptance_criteria>
  <done>The default phone-parse region is user-overridable in Settings, backing the device-region migration policy.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| React inputs to aggregate DAO | PII becomes durable state. |
| UI feedback to contact actions | Non-actionable values require safe presentation without use as handoff input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-12 | Tampering | form save | high | mitigate | Typed shaping delegates normalization/validation to DAO and uses atomic aggregate write. |
| T-18-13 | Information disclosure | UI/logging | medium | mitigate | Display formatted values only; no canonical/parser data in UI diagnostics or Logger. |
| T-18-14 | Elevation | invalid methods | high | mitigate | UI uses DAO-provided actionability and introduces no launcher/router. |
| T-18-SC | Tampering | dependency graph | high | accept | No package change; parser provenance was gated in 18.1-01. |
</threat_model>
<verification>Run the named component and logic suites only; device layout cases are carried to 18.1-06. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.1-06 after staged consumer migration completes.</verification>
<success_criteria>Create/edit method ownership is atomic, accessible, and non-destructive.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-03-SUMMARY.md` when done</output>
