---
phase: 18.1-contact-method-normalization
plan: 02
type: execute
wave: 2
depends_on: [18.1-01]
files_modified: [src/db/contact-methods-dao.ts, src/db/contact-methods-dao.test.ts, src/db/contact-methods-read.ts, src/db/contact-methods-read.test.ts, src/db/contacts-dao.ts, src/db/contacts-dao.test.ts]
autonomous: true
requirements: [CDN-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per-contact method drafts atomically collapse same-type canonical duplicates, return typed collision feedback to the caller, retain cross-contact sharing, preserve order, and promote a replacement primary."
    - "Aggregate create/edit writers accept typed method arrays and apply method differences in the same outer transaction as contact fields."
  artifacts: [src/db/contact-methods-dao.ts, src/db/contact-methods-read.ts]
  key_links: ["Create/edit aggregate writers resolve the effective region at their caller, pass it into the shared normalizer through method cores, and receive a typed same-contact collision result in their existing outer transaction."]
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-02-PLAN.md` — Task 1 (persist/read ordered method drafts) carried in verbatim as Task 1, and the **method half** of Task 3 (aggregate create/edit accepting method arrays) carried in as Task 2.

**Moved to 18.2-02:** the original Task 2 (Bind/Unbind state transitions / lifecycle DAO) and the **lifecycle half** of Task 3 (cadence gating, positive-to-NULL protection at the aggregate boundary, and CaptureScreen's inline create). `createContactFull`/`updateContactFull` here accept **method arrays only**; `tracking_enabled` defaults to Bound implicitly (there is no explicit lifecycle parameter until 18.2-02, and no v10 lifecycle columns exist yet).

<objective>
Make normalized methods transactional write capabilities rather than one-time migration data.
Purpose: preserve method-data invariants for every later form, profile, backup, and sync operation.
Output: tested method DAOs and updated aggregate contact writers.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/contact-links-dao.ts
@src/db/contacts-dao.ts
@src/db/transaction.ts</context>

## Artifacts this phase produces

- Transactional method diff/read owners with UID, ordering, primary, and tombstone semantics.
- Aggregate create/edit integration accepting typed method arrays.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist and read ordered contact-method drafts</name>
  <read_first>src/db/contact-links-dao.ts:29-284; src/db/contact-links-dao.test.ts:35-314; src/logic/contact-method-normalization.ts; 18-PATTERNS.md Ordered contact methods and provenance</read_first>
  <files>src/db/contact-methods-dao.ts, src/db/contact-methods-dao.test.ts, src/db/contact-methods-read.ts, src/db/contact-methods-read.test.ts</files>
  <behavior>
    - Removing a current primary promotes the next stored row of the same type.
    - Same-contact canonical duplicates converge to one ordered row and return a typed `canonicalDuplicate` result containing the method type and surviving draft identity; shared cross-contact canonical values persist independently and return no collision.
    - A failed later statement rolls back the complete seeded-vs-current diff and its tombstones.
  </behavior>
  <action>Create method cores and public transaction-owning wrappers from the contact-links DAO analog: stable UID, per-contact display order, parameterized SQL, exact row-count guards, method tombstones for explicit local deletes, and one revision bump for a changed aggregate. Define a typed normalization context whose `effectivePhoneRegion` is supplied by the caller and pass it unchanged into the shared pure normalizer before comparison. Persist incomplete nonblank values as non-actionable. Return a typed aggregate-save collision result only for a same-contact, same-type canonical collapse; never query or signal a cross-contact match. Expose ordered groups and primary actionable selection through a read owner, with source-adjacent tests covering ordered groups, selected actionable primaries, empty groups, and non-actionable exclusion. This implements D-01 and D-04 and addresses cycle-2 review L13 while retaining stale external evidence rather than treating it as a delete signal.</action>
  <verify><automated>npm test -- src/db/contact-methods-dao.test.ts src/db/contact-methods-read.test.ts</automated></verify>
  <acceptance_criteria>Method DAO/read tests prove effective-region input reaches the shared normalizer, ordering, primary promotion, typed same-contact collision, absence of cross-contact collision, invalid storage, source provenance survival, parameter binding, atomic rollback, and every actionable-primary read behavior.</acceptance_criteria>
  <done>Every contact can durably own zero-to-many normalized phone/email rows with deterministic per-type primary behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compose methods into contact create/edit saves</name>
  <read_first>src/db/contacts-dao.ts; src/db/contact-links-dao.ts; src/db/contact-methods-dao.ts; 18-UI-SPEC.md Create contact and Edit contact</read_first>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/contact-methods-dao.test.ts</files>
  <behavior>
    - Aggregate writes apply method differences in the same outer transaction as contact fields and return the DAO-owned collision result without doing UI normalization.
    - A saved contact either commits contact fields and method differences together or leaves all unchanged.
  </behavior>
  <action>Replace the old scalar phone/email input contract in createContactFull and updateContactFull with typed method drafts and the explicit effective-region normalization context; `tracking_enabled` defaults to Bound implicitly — the explicit lifecycle parameter and Bind/Unbind composition are added by 18.2-02. Call method cores inside the existing aggregate transaction rather than nesting public wrappers, and surface their typed same-contact collision result to the form caller together with the saved ordered methods. Implement D-01; preserve the stable coarse interaction model and omit endpoint/provider interaction changes.</action>
  <verify><automated>npm test -- src/db/contacts-dao.test.ts src/db/contact-methods-dao.test.ts</automated></verify>
  <acceptance_criteria>Aggregate tests prove a create/edit either commits contact fields and methods together or leaves all unchanged; a region override can affect later normalization while existing canonical identity is never rewritten.</acceptance_criteria>
  <done>Create/edit persistence supports the full normalized method model without a legacy scalar writer.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Form/restore draft to DAO | User-provided phone, email, and labels cross into durable SQL. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-05 | Tampering | method DAOs | high | mitigate | `?`-bound SQL, limited typed fields, transaction cores, exact row counts, and rollback tests. |
| T-18-07 | Information disclosure | errors/logs | medium | mitigate | Map save errors to generic UI paths; do not Logger-print methods or canonical values. |
| T-18-08 | Tampering | provenance rows | medium | mitigate | Explicit local-delete tombstones only; no source-link cascade. |
| T-18-SC | Tampering | package install | high | mitigate | Parser package was approved by the blocking checkpoint in 18.1-01. |
</threat_model>
<verification>Run the named targeted DAO suites after each task and keep execution within each task's declared owner files. The aggregate task intentionally retires scalar contracts before later form consumers migrate in 18.1-03, so the repository-wide test, TypeScript, and color compatibility gate is deferred to 18.1-06.</verification>
<success_criteria>All normalized method writes preserve atomicity, PII boundaries, and future sync provenance.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-02-SUMMARY.md` when done</output>
