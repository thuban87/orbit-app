---
phase: 18.1-contact-method-normalization
plan: 05
type: execute
wave: 3
depends_on: [18.1-02]
files_modified: [src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/screens/ComposeScreen.tsx, src/logic/compose-logic.ts, src/logic/compose-logic.test.ts]
autonomous: true
requirements: [CDN-01]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Profile shows ordered non-empty method groups, formats them for people, and never offers a handoff for a non-actionable primary."
  artifacts: [src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/ComposeScreen.tsx]
  key_links: ["Compose reads one actionable primary phone from contact-methods-read before calling expo-sms."]
  prohibitions:
    - "Contact methods must not create endpoint-specific interaction history, a method chooser, or a general endpoint router in Phase 18."
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-06-PLAN.md` — **Task 1 only** (primary-actionable method display on Profile + Compose handoff), carried in verbatim.

**Moved to 18.2-08:** the original Task 2 (Bound/Unbound profile states) and Task 3 (Bind/Unbind execution on profile with reload-safe failure handling). This plan renders and hands off primary methods only; it does not read or change lifecycle state.

**Dependency:** depends only on the method DAO + normalizer (18.1-02); the original deps on 18-09/18-11 (lifecycle effects / nullable-cadence impact) applied only to the moved lifecycle tasks.

**Shared-file note for the re-plan:** `ContactProfileScreen.tsx` and `contact-profile-logic.ts` are also owned by 18.2-08 for the lifecycle panel/actions — the re-plan must reconcile shared-file ownership.

<objective>Deliver the profile method-display and existing Compose handoff slice for normalized methods.
Purpose: a user can see one relationship's primary methods and hand off to Compose safely.
Output: profile method-group display and primary-actionable Compose integration.</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-UI-SPEC.md
@src/screens/ContactProfileScreen.tsx
@src/screens/ComposeScreen.tsx
@src/db/contact-read.ts
@src/db/contact-methods-read.ts</context>

## Artifacts this phase produces

- Profile ordered method groups sourced from normalized reads.
- Compose SMS handoff sourced solely from a primary actionable phone read.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Display one primary actionable phone on Profile and compose from it</name>
  <read_first>18-UI-SPEC.md Profile methods and lifecycle; src/screens/ContactProfileScreen.tsx; src/screens/ComposeScreen.tsx:426-440; src/logic/compose-logic.ts; src/db/contact-methods-read.ts</read_first>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/contact-profile-logic.ts, src/screens/contact-profile-logic.test.ts, src/screens/ComposeScreen.tsx, src/logic/compose-logic.ts, src/logic/compose-logic.test.ts</files>
  <behavior>
    - Profile renders phone first with label, formatted value, quiet Primary marker, extension, and complete accessibility label.
    - Compose receives exactly the selected actionable primary phone; no actionable phone retains Copy-first/add-number fallback.
    - Invalid rows remain readable with the exact helper and are never supplied to SMS.sendSMSAsync.
    - Compose no longer reads `header.phone` (the scalar column is dropped in v9 and removed from `getContactHeader` by 18.1-02): it derives `hasPhone` from the actionable primary phone read via `contact-methods-read`. `compose-logic`'s `hasPhone` (`compose-logic.ts:72-79`, driven today by `ComposeScreen.tsx:782-785`) now means "actionable primary phone present". Because the device-region migration (H4) makes most existing numbers actionable, the "Add a phone number" affordance (`addNumber`) appears only when there is genuinely no actionable primary — not for a legacy national number that is now canonicalized (H1/H4 consumers).
  </behavior>
  <action>Test profile/compose view-model logic first, then wire the existing profile and Compose shells through contact-methods-read. Replace Compose's `header.phone`-derived `hasPhone` with an actionable-primary-phone read (the scalar column is gone in v9 and removed from `getContactHeader` by 18.1-02), so `hasPhone` means "actionable primary present" and the add-number affordance only shows when there is genuinely no actionable primary. Implement D-01's sole-authority/actionability rules with format-only display values. Preserve the existing coarse message/history taxonomy and do not add Call/Email routing, a chooser, or raw method logging.</action>
  <verify><automated>npm test -- src/screens/contact-profile-logic.test.ts src/logic/compose-logic.test.ts</automated></verify>
  <acceptance_criteria>One primary actionable phone is rendered and passed to the existing SMS path end to end; invalid/missing primary cases prove no handoff occurs.</acceptance_criteria>
  <done>Profile and Compose now consume normalized method data safely.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| method read to Compose launcher | Contact endpoint data becomes an OS SMS handoff input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-18 | Elevation/Tampering | Compose handoff | high | mitigate | Consume only selected actionable primary read; tests cover invalid/null suppression. |
| T-18-20 | Information disclosure | profile/UI logs | medium | mitigate | Formatted visual display and no canonical/raw data in diagnostic paths. |
| T-18-SC | Tampering | package install | high | accept | No dependency changes in this plan. |
</threat_model>
<verification>Run the named profile and Compose suites only; complete device handoff proof in 18.1-06. The repository-wide test, TypeScript, and color compatibility gate belongs solely to 18.1-06.</verification>
<success_criteria>Profile method display and Compose handoff remain safe and actionability-gated while relationship data stays whole.</success_criteria>
<output>Create `.planning/phases/18.1-contact-method-normalization/18.1-05-SUMMARY.md` when done</output>
