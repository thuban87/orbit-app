---
phase: 06-interaction-log-status-impact
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/db/events-dao.ts
  - src/db/events-dao.test.ts
  - src/db/contacts-dao.ts
  - src/db/contacts-dao.test.ts
  - src/db/purge-dao.ts
  - src/db/purge-dao.test.ts
  - src/db/timeline-read.ts
  - src/db/timeline-read.test.ts
  - src/components/TimelineRow.tsx
  - src/screens/ContactProfileScreen.tsx
autonomous: true
requirements: [LOG-02]

must_haves:
  truths:
    - "Archiving a contact writes exactly one immutable 'archive' event; restoring writes exactly one 'restore' event — each composed INSIDE the function's existing single inWriteTransaction (no second transaction, no nested mutex)"
    - "The events DAO is insert-only: it exposes recordEvent/recordEventCore and NO update/edit path (events are immutable — 'a record of what the app did'); deletion happens only via the contact FK / purge fan-out"
    - "A real archive/restore event interleaves read-only in the profile timeline, ordered with touchpoints by occurred_at"
    - "Purging a contact removes its events (explicit DELETE FROM events, dossier [log → crud]) and the purge impact confirm surfaces the events count"
    - "The contact profile shows a full scrollable timeline of the contact's touchpoints, newest first"
    - "Touchpoints and events interleave in one list ordered by occurred_at DESC, id DESC, kind_order DESC (deterministic even though interactions.id and events.id are independent PK sequences)"
    - "Each row's React key and testID are ${kind}-${id}, so a touchpoint/event numeric-id collision cannot produce duplicate keys or testIDs"
    - "Events render read-only and visually distinct from editable touchpoints ('Archived' / 'Restored')"
    - "The timeline shows an honest empty state when a contact has no touchpoints"
  artifacts:
    - path: "src/db/events-dao.ts"
      provides: "Immutable events writer: EventType vocabulary (archive|restore|snooze|unsnooze) + recordEvent (mutexed) + recordEventCore (non-mutexed, composes inside an open transaction). Insert-only — no update/edit path."
      exports: ["recordEvent", "recordEventCore", "EventType"]
    - path: "src/db/contacts-dao.ts"
      provides: "archiveContact emits an 'archive' event and restoreContact a 'restore' event via recordEventCore inside their existing inWriteTransaction; the RESEARCH-A3 deferral note is removed"
      contains: "recordEventCore"
    - path: "src/db/purge-dao.ts"
      provides: "impactSummaryLines surfaces the events count in the purge impact copy (per the purge-dao.ts:49-51 Phase-6 note); purge already deletes events explicitly"
      contains: "events"
    - path: "src/db/timeline-read.ts"
      provides: "listTimeline(exec, contactId) — interleaved interactions (editable) + events (read-only) newest-first"
      exports: ["listTimeline"]
    - path: "src/components/TimelineRow.tsx"
      provides: "Renders a touchpoint row vs an event row, visually distinct, colour via tokens"
      exports: ["TimelineRow"]
    - path: "src/screens/ContactProfileScreen.tsx"
      provides: "Timeline stub replaced with a real list mapping listTimeline rows to TimelineRow"
      contains: "listTimeline"
  key_links:
    - from: "src/db/contacts-dao.ts"
      to: "src/db/events-dao.ts"
      via: "archiveContact/restoreContact call recordEventCore(exec, {...}) inside the existing inWriteTransaction (compose the non-mutexed core, never nest)"
      pattern: "recordEventCore"
    - from: "src/screens/ContactProfileScreen.tsx"
      to: "src/db/timeline-read.ts"
      via: "listTimeline(getExecutor(), contactId) loaded in the focus effect"
      pattern: "listTimeline"
    - from: "src/screens/ContactProfileScreen.tsx"
      to: "src/components/TimelineRow.tsx"
      via: "map timeline rows to <TimelineRow>"
      pattern: "TimelineRow"
---

<objective>
Build the events subsystem AND the profile's real timeline read surface. Two halves:

1. **Events WRITER (built this phase — OWNER-APPROVED 2026-08-15).** Add the net-new immutable
   events DAO (`src/db/events-dao.ts`) grounding the dossier's [DECIDED] v1 vocabulary — the
   `EventType` union is `archive | restore | snooze | unsnooze` ("a record of what the app did",
   [log → data] ~line 571). Retrofit `archiveContact` → an `'archive'` event and `restoreContact`
   → a `'restore'` event, each composed INSIDE the function's existing single transaction. Only
   archive/restore are WRITTEN this phase; snooze/unsnooze are RESERVED in the type union but have
   no producing feature until later phases (do not write them). The prior "writer deferred / events
   empty in v1" default is SUPERSEDED by this owner decision.

2. **Timeline READ + render.** Replace the profile's timeline stub with a scrollable list of the
   contact's touchpoints interleaved with read-only events, newest-first
   (`occurred_at DESC, id DESC, kind_order DESC`). This is the load-bearing correction surface for
   later slices (edit/delete land in Plan 03) and the honest answer to the plugin's fatal mistake —
   a log with zero readers. With the writer built, REAL archive/restore events now interleave (not
   just a directly-seeded test row).

Purpose: LOG-02 in full — the events writer (archive/restore) + the read half. Success criterion 2
wants touchpoints (editable) and events (read-only, visually distinct: "Archived" / "Restored") in
one view.

Output: `src/db/events-dao.ts` + test (immutable insert-only writer + vocabulary); retrofit of
`archiveContact`/`restoreContact` + contacts-dao test; `src/db/purge-dao.ts` events-count surfacing
+ test; `src/db/timeline-read.ts` + test (the interleaved UNION read); `src/components/TimelineRow.tsx`
(the row renderer); and the profile screen wired to load + render them.

CRITICAL — events are IMMUTABLE (dossier §"Event rows are immutable"). The DAO exposes ONLY an insert
path (recordEvent / recordEventCore); NO update/edit function exists. Compose recordEventCore inside
archive/restore's ONE existing `inWriteTransaction` — never nest the non-reentrant mutex, never open
a second transaction (transaction.ts non-reentrancy; mirror recency-dao's `*Core` composition).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interaction-log-status-impact/06-RESEARCH.md
@.planning/phases/06-interaction-log-status-impact/06-PATTERNS.md
@docs/dossier/04-log.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: events-dao.ts — immutable insert-only events writer + vocabulary + node:sqlite test</name>
  <files>src/db/events-dao.ts, src/db/events-dao.test.ts</files>
  <read_first>
    - src/db/migrations/001-initial.ts :115-125 (events columns: id / uid TEXT NOT NULL UNIQUE / contact_id ON DELETE CASCADE / type / occurred_at / detail / recorded_at / modified_at — the exact insert shape)
    - src/db/recency-dao.ts :145-217, :340-353 (the `*Core` composition pattern: a non-mutexed core that assumes BEGIN is open, exported alongside a mutexed wrapper that calls inWriteTransaction — mirror this exactly for recordEvent / recordEventCore)
    - src/db/transaction.ts :11-57 (inWriteTransaction + the NON-REENTRANCY rule — recordEventCore must be callable inside an already-open transaction without taking the mutex)
    - src/db/uid.ts (newUid — node-safe uid mint; the events uid is a merge-key, not a secret)
    - src/db/recency-dao.test.ts :1-60 (node:sqlite harness: openTestDb, nodeSqliteExecutor, seeding a contact, the uid() helper)
    - docs/dossier/04-log.md ~line 348 (separate events table: snooze/unsnooze/archive/restore) + "Event rows are immutable" (~line 699)
  </read_first>
  <behavior>
    - EventType is the union 'archive' | 'restore' | 'snooze' | 'unsnooze' (full dossier vocabulary; snooze/unsnooze RESERVED, no producer this phase)
    - recordEvent(exec, {contactId, uid, type, occurredAt, detail, now}) inserts exactly ONE events row with type/occurred_at/detail set, recorded_at=now, modified_at=now — every value `?`-bound
    - recordEventCore inserts the same row but takes NO mutex and opens NO transaction (it is the composition primitive: correct only inside an already-open inWriteTransaction)
    - The module exposes NO update/edit/mutate path for events (immutable — insert-only). Only recordEvent / recordEventCore / EventType are exported
    - detail may be null (archive/restore carry no detail)
  </behavior>
  <action>
    Create src/db/events-dao.ts. Export a `type EventType = 'archive' | 'restore' | 'snooze' | 'unsnooze'` (the full dossier v1 vocabulary — forward-compatible; snooze/unsnooze are RESERVED for later phases and have no producer here) and a `RecordEventInput` interface `{ contactId: number; uid: string; type: EventType; occurredAt: string; detail?: string | null; now: string }`. Implement a non-mutexed `recordEventCore(exec: SqlExecutor, input: RecordEventInput): Promise<number>` that runs ONE `INSERT INTO events (uid, contact_id, type, occurred_at, detail, recorded_at, modified_at) VALUES (?, ?, ?, ?, ?, ?, ?)` — recorded_at=now, modified_at=now, detail ?? null — every value `?`-bound, returning lastInsertRowId. Then a mutexed public `recordEvent(exec, input)` that wraps `recordEventCore` in ONE `inWriteTransaction` (mirror recordTouchpoint / the recomputeLastContactCore export split in recency-dao.ts). Export ONLY `recordEvent`, `recordEventCore`, `EventType`, `RecordEventInput` — deliberately provide NO update/edit function: events are IMMUTABLE ("a record of what the app did", dossier), the only removal path is the contact FK / purge fan-out. Header doc-comment: the immutable events writer; v1 vocabulary archive/restore/snooze/unsnooze (only archive/restore have producers this phase — the lifecycle retrofit in Task 2); recordEventCore is the non-mutexed composition primitive (call ONLY inside an open inWriteTransaction, never bare, never nested — see transaction.ts); `?`-bound, no interpolation.

    Write src/db/events-dao.test.ts (node:sqlite, reuse the recency-dao harness): (a) recordEvent inserts exactly one row with the exact type/occurred_at/detail/recorded_at/modified_at stored (assert a round-trip SELECT); (b) recordEventCore composes inside a caller-opened inWriteTransaction and inserts one row (proves it needs no mutex of its own); (c) an 'archive' and a 'restore' event both persist with their type verbatim; (d) recordEvent with detail omitted stores detail NULL.
  </action>
  <verify>
    <automated>npx vitest run src/db/events-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - events-dao.ts exports EventType ('archive'|'restore'|'snooze'|'unsnooze'), recordEvent, recordEventCore — and NO update/edit path (grep: no `UPDATE events` and no exported edit function)
    - recordEventCore inserts one `?`-bound events row without taking the mutex; recordEvent wraps it in ONE inWriteTransaction
    - recorded_at/modified_at set to `now`; detail defaults NULL; every value `?`-bound (no interpolation)
    - `npx vitest run src/db/events-dao.test.ts` passes
  </acceptance_criteria>
  <done>The immutable, insert-only events writer and its dossier-grounded vocabulary are node-tested, with a non-mutexed core ready to compose into the lifecycle retrofit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit archive/restore to emit events + surface the events count in the purge impact</name>
  <files>src/db/contacts-dao.ts, src/db/contacts-dao.test.ts, src/db/purge-dao.ts, src/db/purge-dao.test.ts</files>
  <read_first>
    - src/db/contacts-dao.ts :369-444 (the archive/restore header block — INCLUDING the DEFERRAL note at :382-386 that this task REMOVES — plus archiveContact :404-420 and restoreContact :428-444; note both bodies already capture `exec` in their inWriteTransaction closure, and newUid is already imported :60)
    - src/db/events-dao.ts (recordEventCore + RecordEventInput from Task 1 — the core to compose)
    - src/db/purge-dao.ts :46-59 (PurgeImpact + the :49-51 note "Phase 6 MUST surface [events] in the impact copy"), :126-152 (impactSummaryLines — currently omits events), :180-204 (purgeContact already `DELETE FROM events` explicitly :183 AND deletes the contact row :197)
    - src/db/purge-dao.test.ts :44-70 (seedContact + the events-seed helper), :172-206 (the impactSummaryLines cases that currently assert events is NOT rendered — these MUST be updated)
    - src/db/contacts-dao.test.ts :25-52 (harness + archive/restore imports)
    - docs/dossier/04-log.md [log → crud] (~line 620: "Purge must delete interactions and events explicitly" — cascade is decorative because FK are off inside the transaction; the explicit DELETE is the [DECIDED] mechanism, NOT cascade)
  </read_first>
  <behavior>
    - archiveContact emits exactly one events row type='archive' for the contact, INSIDE its existing single inWriteTransaction (occurred_at=now, detail null)
    - restoreContact emits exactly one events row type='restore', same transaction
    - A no-op/wrong-state transition emits NO event: archiveContact on an ALREADY-archived contact (and restoreContact on a LIVE contact) matches 0 rows via the archived-state predicate → the changes===1 guard throws → NO spurious event row is written (C2-#1)
    - Both still assert exactly one contacts row changed on a real transition and still leave last_contact untouched (single-writer DATA-04 intact)
    - impactSummaryLines renders an events line ("N event(s)") when events > 0, and omits it when 0 (same omit-zero style as interactions/fuel/links)
    - archive a live contact → an 'archive' event exists → purge → no events row remains for that contact (removed by purge's explicit DELETE FROM events)
  </behavior>
  <action>
    In src/db/contacts-dao.ts: retrofit `archiveContact` to call `recordEventCore(exec, { contactId: id, uid: newUid(), type: 'archive', occurredAt: now, detail: null, now })` INSIDE its existing `inWriteTransaction` closure — compose the non-mutexed core, do NOT nest a second inWriteTransaction and do NOT take the mutex again (transaction.ts non-reentrancy). Retrofit `restoreContact` identically with `type: 'restore'`. Import `recordEventCore` from `@/db/events-dao`; `newUid` is already imported.

    SPURIOUS-EVENT GUARD (Review C2-#1, MEDIUM — the events log is now the durable "record of what the app did", so a lifecycle event must fire ONLY on a real state transition): SQLite counts an identical-value UPDATE as `changes===1`, so today `archiveContact` on an already-archived contact (or `restoreContact` on a live one) would "succeed" and — post-retrofit — emit a FALSE event. Add the archived-state predicate to each UPDATE so a no-op transition matches 0 rows: `archiveContact` → `UPDATE contacts SET archived_at=?, modified_at=? WHERE id=? AND archived_at IS NULL`; `restoreContact` → `... SET archived_at=NULL, modified_at=? WHERE id=? AND archived_at IS NOT NULL`. Keep the `changes===1` guard: a no-op/wrong-state transition now matches 0 rows and THROWS before the event INSERT (so NO spurious event is written and the caller learns the transition didn't apply). This tightens Phase-4 archive/restore to reject a wrong-state call rather than silently re-stamping the timestamp — a structural guard mirroring purge-dao.ts's posture. **OWNER DISPOSITION (surfaced at the pre-execution pause):** this changes shipped Phase-4 behavior (a redundant archive/restore now throws instead of no-op-succeeding). Default = ship the guard. If the owner prefers, the fallback is to keep the id-only UPDATE and instead gate the event INSERT on a pre-read of the prior `archived_at` (event only when the state actually flips) — same no-spurious-event outcome, no throw on redundant calls. Do NOT leave the retrofit able to emit a spurious event.

    REMOVE the DEFERRAL note at :382-386 and rewrite the surrounding header (:369-388) to state that archive/restore now emit an immutable events row via recordEventCore composed inside the one existing transaction, that the UPDATE is archived-state-guarded so only a real transition emits an event, and that `last_contact` is still untouched (only the events INSERT is added; the SET list on contacts is otherwise unchanged). Do NOT add any last_contact write.

    In src/db/purge-dao.ts: purgeContact already deletes events explicitly (:183) and deletes the contact row (:197) — CONFIRM this in the header, do NOT change the deletion logic (the dossier [log → crud] decision requires the EXPLICIT delete; cascade is decorative, so keep it). Update `impactSummaryLines` to render the events count: add a line via `plural(impact.events, "event", "events")` when `impact.events > 0`, placed after interactions (ordering is your call — interactions, events, fuel, links reads naturally). Update the header/comment at :126-135 and the PurgeImpact note at :49-51 to reflect that events ARE now surfaced (drop "NOT rendered in Phase 4" / "deliberately not rendered" for events).

    Update src/db/contacts-dao.test.ts: add cases that archiveContact writes exactly one events row with type='archive' (SELECT COUNT + type) and restoreContact writes exactly one type='restore', each within the single transaction; AND a no-op-transition case — archiveContact on an already-archived contact (and restoreContact on a live contact) throws and writes ZERO events rows (the archived-state predicate matched 0 rows). Update src/db/purge-dao.test.ts: (a) fix the two impactSummaryLines cases (:172-206) that assert events is NOT rendered — now a non-zero events count DOES produce a line, and a zero events count produces none; (b) add a round-trip test: seed a LIVE contact, call archiveContact (which now emits an 'archive' event), assert one events row exists, call purgeContact, assert zero events rows remain for that contact.
  </action>
  <verify>
    <automated>npx vitest run src/db/contacts-dao.test.ts src/db/purge-dao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - archiveContact / restoreContact each emit exactly one events row ('archive' / 'restore') composed via recordEventCore inside their existing single inWriteTransaction (no nested mutex, no second transaction); the changes===1 contacts guard and the untouched last_contact are preserved
    - The UPDATE is archived-state-guarded (`AND archived_at IS NULL` / `IS NOT NULL`): a no-op/wrong-state archive/restore matches 0 rows, throws, and writes NO event (test proves zero spurious events) — C2-#1
    - The :382-386 DEFERRAL note is gone; the header states archive/restore now emit events
    - impactSummaryLines renders the events count when > 0 and omits it at 0; the old "events NOT rendered" test assertions are updated
    - purgeContact deletion logic unchanged (explicit DELETE FROM events kept per dossier [log → crud]); the archive→event→purge→gone round-trip test passes
    - `npx vitest run src/db/contacts-dao.test.ts src/db/purge-dao.test.ts` passes
  </acceptance_criteria>
  <done>Archive/restore now record immutable lifecycle events through the composed core, purge surfaces and removes them, and the deferral note is retired — all node-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Interleaved timeline read + TimelineRow render + wire into the profile</name>
  <files>src/db/timeline-read.ts, src/db/timeline-read.test.ts, src/components/TimelineRow.tsx, src/screens/ContactProfileScreen.tsx</files>
  <read_first>
    - src/db/queries.ts :31-54 (NEWEST_PER_CONTACT / NEWEST_FOR_CONTACT — the canonical `occurred_at DESC, id DESC` tiebreak to reuse verbatim; the `?`-bound read-SQL-const style + injection posture comment :9-13)
    - src/db/contact-read.ts :55-94 (getContactHeader — the pure read-fn signature shape: `exec: SqlExecutor`, `?`-bound, no transaction, returns typed rows)
    - src/db/migrations/001-initial.ts :96-125 (interactions + events column lists — interactions has channel/direction/connected/quality/note/occurred_at, events has type/occurred_at/detail)
    - src/db/recency-dao.test.ts :1-60 (node:sqlite harness; seeding contacts + interactions + events)
    - src/screens/ContactProfileScreen.tsx (the timeline stub :162-176 to replace; sectionStub/sectionHeading/sectionBody styles :214-228; useFocusEffect load pattern :62-82; useTheme colours)
    - .planning/phases/05-photos/05-UI-SPEC.md :26-60 (design system tokens + de-facto spacing/radius scale — conform; NO Phase-6 UI-SPEC exists)
  </read_first>
  <behavior>
    - listTimeline returns interactions for the contact tagged kind='touchpoint' with occurred_at/channel/direction/connected/quality/note/id
    - listTimeline returns events for the contact tagged kind='event' with occurred_at/type/detail/id
    - Rows are ordered newest-first by occurred_at DESC, id DESC, kind_order DESC (a static per-table kind_order discriminator gives a deterministic FINAL tiebreak across the two independent id sequences)
    - A REAL archive/restore event (from Task 2) interleaves at the correct position among touchpoints by occurred_at, read-only
    - An equal-occurred_at + equal-id cross-table pair (a touchpoint and an event sharing BOTH values — reachable because interactions.id and events.id are independent PK sequences) orders deterministically by kind_order and yields TWO distinct rows with distinct ${kind}-${id} keys
    - With no rows the result is an empty array (never throws); rows for OTHER contacts are excluded (contact_id `?`-bound)
  </behavior>
  <action>
    Create src/db/timeline-read.ts exporting `listTimeline(exec: SqlExecutor, contactId: number): Promise<TimelineItem[]>` and a `TimelineItem` discriminated-union type (`kind: 'touchpoint'` carrying id/occurred_at/channel/direction/connected/quality/note; `kind: 'event'` carrying id/occurred_at/type/detail). Implement as a read-only SELECT that UNION ALLs the two tables into a common column shape (SELECT a literal `kind` discriminator AND a static integer `kind_order` discriminator — e.g. touchpoint=1, event=0 — plus the shared columns, NULLing the columns that don't apply to each side), filtered `WHERE contact_id = ?` on both, ORDER BY `occurred_at DESC, id DESC, kind_order DESC` at the outer query — extend the queries.ts tiebreak with the `kind_order` final key so ordering is deterministic even when a touchpoint and an event share BOTH occurred_at and id. Every value `?`-bound; only column names are static text (no identifier interpolation). No transaction (pure read). Map each result row to the typed TimelineItem in TS. Header doc-comment: read-only, `?`-bound, no network; events are read-only + interleaved by occurred_at; REAL archive/restore events now populate this (the writer landed in Task 1/2 — no longer empty in v1).

    Note on cross-table ordering + identity: because interactions.id and events.id are INDEPENDENT PK sequences (verified migration 1), occurred_at + id alone can tie — or fully collide — between a touchpoint and an event. The static `kind_order` final ORDER BY key makes that tie deterministic. Separately, the row IDENTITY must NOT be the bare id: key rows (React key + testID root) by `${kind}-${id}` so a touchpoint and an event that share a numeric id never collide.

    Write src/db/timeline-read.test.ts (node:sqlite): seed a contact, insert several interactions (varying occurred_at incl. a same-day pair), INSERT one events row (type/detail) between two touchpoint dates AND a second events row that shares BOTH occurred_at and the numeric id of an existing touchpoint (independent sequences make this reachable), and assert the returned order (deterministic via kind_order) + two distinct rows with distinct ${kind}-${id} identity for the equal-timestamp+equal-id cross-table pair + kind tagging + contact scoping + empty case.

    Create src/components/TimelineRow.tsx exporting `TimelineRow` that takes a `TimelineItem`. For a `touchpoint` render occurred_at plus the present metadata (channel unless 'unspecified', direction, a "didn't connect" marker when connected===0 using non-judgemental copy — avoid "failed", per dossier deferred note, e.g. "no reply"), quality when set, and the note when present. For an `event` render read-only + visually distinct: a muted (textSecondary) row with a leading label mapped from the event type — 'archive' → "Archived", 'restore' → "Restored", 'snooze' → "Snoozed", 'unsnooze' → "Unsnoozed" — plus occurred_at (and detail when present), so it never looks editable. Every colour via `useTheme().colors.*` (textPrimary for touchpoint body, textSecondary for events/secondary metadata, border for the row divider/card). No hardcoded colour, incl. no raw hex. Give each row a stable testID root keyed by kind AND id (e.g. `timeline-row-{kind}-{id}`) — NOT the bare id, because interactions.id and events.id are independent sequences and a touchpoint and an event can share a numeric id (which would collide on both React key and testID). Keep it a pure presentational component (no data loading, no DAO import) so Plan 03 can add edit/delete affordances to it.

    In ContactProfileScreen, replace the `contact-profile-timeline-stub` block with a real Timeline section: load `listTimeline(getExecutor(), contactId)` inside the existing focus effect (add it to the single unified `load` alongside the header read so both refresh on focus), hold the rows in state, and render them mapped to `<TimelineRow>` under a "Timeline" section heading (reuse sectionHeading style) — use `${kind}-${id}` as each element's React key (never the bare id, per the cross-table collision note above). Render an honest empty state ("No touchpoints yet") when the list is empty. Do NOT add edit/delete affordances yet (Plan 03).
  </action>
  <verify>
    <automated>npx vitest run src/db/timeline-read.test.ts && npx tsc --noEmit && npm run check:colors && npx biome check src/components/TimelineRow.tsx src/screens/ContactProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - listTimeline returns a discriminated union of touchpoint + event items ordered occurred_at DESC, id DESC, kind_order DESC; contact_id `?`-bound; other contacts excluded; empty contact returns []; no transaction; no identifier interpolation
    - An equal-occurred_at + equal-id cross-table pair orders deterministically (kind_order) as two distinct rows with distinct ${kind}-${id} identity (test proves it)
    - TimelineRow renders touchpoint vs event distinctly (event muted/read-only with an "Archived"/"Restored" label); no DAO import in the component; each row's React key + testID root are `${kind}-${id}`
    - ContactProfileScreen loads listTimeline in the single unified `load` and renders rows newest-first with an empty state; the timeline stub is gone
    - Every colour resolves through useTheme().colors.* (npm run check:colors passes); `npx tsc --noEmit` and `npx biome check` pass
  </acceptance_criteria>
  <done>The profile shows the real interleaved timeline, newest-first, with a logged touchpoint (Plan 01) and a REAL archive/restore event (Task 2) both visible — events read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/lifecycle → DAO | archive/restore cross into the events INSERT (composed inside the existing transaction) |
| DAO → UI | contactId crosses into a read-only SELECT; rows render on the profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering (SQL injection) | timeline-read SELECT / events-dao INSERT | high | mitigate | contact_id + every value `?`-bound; only static column names in SQL, no identifier interpolation; read is transaction-free, the write composes inside the one existing transaction |
| T-06-11 | Tampering (event mutation / recency corruption via a second writer) | events-dao / archive-restore retrofit | medium | mitigate | events-dao is insert-only (no update/edit path — events are immutable); the retrofit composes recordEventCore inside archive/restore's ONE existing inWriteTransaction (no nested mutex, no second transaction) and adds NO last_contact write, so the single-writer DATA-04 invariant is untouched |
| T-06-08 | Information disclosure | timeline render | low | accept | Local-first, single-user device DB; no network on the read path; note text renders only on the owner's own profile |
| T-06-SC | Tampering (supply chain) | npm installs | n/a | accept | No packages installed this phase |
</threat_model>

<artifacts>
## Artifacts this plan produces

New files:
- `src/db/events-dao.ts` — immutable events writer: `EventType` ('archive'|'restore'|'snooze'|'unsnooze') + `RecordEventInput` + `recordEvent` (mutexed) + `recordEventCore` (non-mutexed composition primitive). Insert-only — no update/edit path.
- `src/db/events-dao.test.ts` — node:sqlite coverage: one immutable row inserted, core composes inside an open transaction, archive/restore types persist, detail defaults NULL
- `src/db/timeline-read.ts` — `listTimeline(exec, contactId)` + `TimelineItem` discriminated-union type (`kind: 'touchpoint' | 'event'`)
- `src/db/timeline-read.test.ts` — node:sqlite coverage: interleave order (incl. a real event), kind tagging, ${kind}-${id} identity, contact scoping, empty case
- `src/components/TimelineRow.tsx` — `TimelineRow` presentational component (touchpoint vs read-only event; 'Archived'/'Restored' labels)

Modified files / new symbols:
- `src/db/contacts-dao.ts` — `archiveContact` emits an 'archive' event and `restoreContact` a 'restore' event via `recordEventCore` inside their existing transaction; the :382-386 DEFERRAL note removed; `last_contact` still untouched
- `src/db/contacts-dao.test.ts` — new cases: archive emits one 'archive' event, restore emits one 'restore' event (each in the single transaction)
- `src/db/purge-dao.ts` — `impactSummaryLines` now renders the events count; `PurgeImpact`/header notes updated; deletion logic unchanged (explicit DELETE FROM events kept per dossier [log → crud])
- `src/db/purge-dao.test.ts` — updated impactSummaryLines cases (events now rendered) + an archive→event→purge→gone round-trip test
- `src/screens/ContactProfileScreen.tsx` — timeline stub replaced with a real list; the single unified `load` extended to fetch listTimeline; new timeline state + empty state

No new constants or migrations. No packages installed.
</artifacts>

<verification>
- `npx vitest run src/db/events-dao.test.ts src/db/contacts-dao.test.ts src/db/purge-dao.test.ts src/db/timeline-read.test.ts` green; `npm test` full suite green
- `npx tsc --noEmit`, `npm run check:colors`, `npx biome check` clean
- On-device (Pixel): open a contact with a logged touchpoint, confirm it appears in the timeline newest-first; archive then restore a contact and confirm the 'Archived'/'Restored' events appear read-only in the timeline; confirm the empty-state on a fresh contact; confirm the purge impact confirm lists the events count
</verification>

<success_criteria>
- LOG-02 (events writer): archive/restore write immutable events through the composed core; the events DAO is insert-only; purge deletes and surfaces them
- LOG-02 (read half): the profile shows a full scrollable timeline interleaving touchpoints (editable next plan) and read-only events, newest-first, with REAL archive/restore events now populating it (snooze/unsnooze reserved for later phases)
</success_criteria>

<output>
Create `.planning/phases/06-interaction-log-status-impact/06-02-SUMMARY.md` when done.
</output>
