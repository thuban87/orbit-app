---
phase: 21-interaction-assist-reach-out
plan: 06
type: execute
wave: 4
depends_on: [21-03, 21-04, 21-05]
files_modified:
  - src/screens/ContactProfileScreen.tsx
  - src/screens/ComposeScreen.tsx
  - .planning/phases/21-interaction-assist-reach-out/21-UAT.md
autonomous: false
requirements: [IAS-01, IAS-02, IAS-03, IAS-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The transitional raw interaction_assist_enabled reads (added in ContactProfileScreen by 21-02 and ComposeScreen by 21-03) are swapped to the canonical getAppSettings().interactionAssistEnabled BEFORE the full-suite gate, leaving exactly ONE read path to the toggle — no raw-column read of interaction_assist_enabled survives in either screen, and the behavior (Assist ON/OFF gating the handoff) is unchanged (Addresses review finding #4)."
    - "The full node suite (npm test), tsc, and check:colors are green across the whole phase before device UAT begins."
    - "On the Pixel, Reach Out routes Call/Text/Email through actionable methods with the primary emphasized and at most three taps when endpoint choice is needed (IAS-01)."
    - "On the Pixel, with Interaction Assist ON a pending assist is written before native handoff, a failed launch marks the assist failed, and the app-global non-modal banner appears on return with Back passing through it (IAS-02)."
    - "On the Pixel, confirming logs an outbound interaction at the original handoff time via the authoritative writer (Call Yes connected=1, Call No answer connected=0, Text/Email Yes attestation), verified against the on-device DB via run-as (IAS-03)."
    - "On the Pixel, the larger widget's Contact action deep-links into the shared router; merged/purged/archived/Unbound targets behave per the locked lifecycle rules without resurrection; process death/reboot preserves non-expired pending assists (IAS-02, IAS-03, IAS-04)."
    - "On the Pixel, confirming from the banner refreshes the home-screen widget's overdue/recency tile without an app restart (notifyWidgetDataChanged), and a widget REACH to an archived-but-existing target is silently dropped while a purged target shows 'This contact is no longer available.' → Dashboard (Addresses review — findings #1, #4)."
    - "The 15-second-buffer and 24-hour-expiry device rows are exercised via a documented time-travel technique (DEBUG-lowered constants or a run-as handoff_at backdate), and the owner sign-off + device-dependent handoff-failure rows are recorded as EXPLICIT blocked checkpoints, never auto-passed (Addresses review — clock manipulation + explicit blocked checkpoints)."
    - { statement: "On the Pixel, a long contact name and a long optional note render without clipping across the banner question, router route rows, and confirmation (the UI-SPEC long-text held-out visual check).", verification: backstop }
  prohibitions:
    - "Device verification is UI-observable AND DB-verified — seeing a screen render is not proof; confirm interaction rows and assist statuses against the on-device DB (run-as, WAL-aware) per CLAUDE.md 'Review the code, not the diff'."
  artifacts:
    - .planning/phases/21-interaction-assist-reach-out/21-UAT.md
  key_links:
    - "npm test full-suite gate → device UAT → owner sign-off (the release gate for the phase's native-handoff/banner/widget flows)."
---

<objective>
Prove the phase end-to-end on the real Pixel 6 Pro — the native Call/Text/Email handoff, the app-global banner lifecycle, confirmation writes through the authoritative recency path, the toggle, and the widget Contact deep-link — with an owner sign-off gate. The node suites cover the DAO/logic/allow-list correctness; this plan closes the device-only gaps (native intents, Hermes runtime, RemoteViews render, AppState banner timing).

Purpose: This is the only place the native handoff, banner timing, and widget render can be verified (they are not node-testable), and the owner-bucket gate before an irreversible migration reaches a real device. It is also the first wave-safe point to retire the two transitional raw `interaction_assist_enabled` reads (owned by Wave-3 screens) onto the canonical settings getter, so the phase does not ship with two read paths to one column (review finding #4).
Output: the raw-settings-read consolidation, a full green node gate, then a driven Pixel UAT with DB-verified evidence recorded in 21-UAT.md, then owner sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-interaction-assist-reach-out/21-RESEARCH.md
@docs/dossier/21-interaction-assist-reach-out.md
@.planning/phases/21-interaction-assist-reach-out/21-01-SUMMARY.md
@.planning/phases/21-interaction-assist-reach-out/21-02-SUMMARY.md
@.planning/phases/21-interaction-assist-reach-out/21-03-SUMMARY.md
@.planning/phases/21-interaction-assist-reach-out/21-04-SUMMARY.md
@.planning/phases/21-interaction-assist-reach-out/21-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire transitional raw settings reads + full-suite gate + UAT scaffold (autonomous; STOPS before device build)</name>
  <files>src/screens/ContactProfileScreen.tsx, src/screens/ComposeScreen.tsx, .planning/phases/21-interaction-assist-reach-out/21-UAT.md</files>
  <read_first>
    - src/screens/ContactProfileScreen.tsx (the raw interaction_assist_enabled read added by 21-02 Task 3 to pass assistEnabled into performReachOut — swap it to getAppSettings().interactionAssistEnabled)
    - src/screens/ComposeScreen.tsx (the raw interaction_assist_enabled read added by 21-03 Task 2 for onSend's assistEnabled — swap it to getAppSettings().interactionAssistEnabled)
    - src/db/app-settings-dao.ts (getAppSettings — 21-04 added interactionAssistEnabled to COLUMN_OF + the getAppSettings SELECT/mapping; this is the canonical read the swap targets)
    - .planning/phases/21-interaction-assist-reach-out/21-RESEARCH.md (Validation Architecture — the full test map; device-UAT-only gaps)
    - docs/runbooks (the desktop-build → Pixel install pipeline; the run-as WAL-aware DB read procedure)
    - CLAUDE.md (Android device section — emu-connect, adb serial resolution, adb input source taxonomy, DEBUG build required for run-as)
  </read_first>
  <behavior>
    - Retire the transitional raw reads FIRST (finding #4): in ContactProfileScreen and ComposeScreen, replace the defensive raw `interaction_assist_enabled` column read with getAppSettings().interactionAssistEnabled (the canonical DAO key 21-04 landed), so exactly one read path to the toggle remains. Behavior is unchanged — Assist ON still gates the pending-assist write, OFF still launches native only. This is done before the node gate so `npm test` + `npx tsc --noEmit` cover the consolidated read path.
    - Run npm test (full vitest), npx tsc --noEmit, npm run check:colors — all must be green. The DEBUG APK build/install, Metro + adb reverse, and device-local contact seeding are DEFERRED to Task 3 (the device UAT), which runs only AFTER the Task 2 owner checkpoint — this task does NOT touch the device.
    - Prepare a TIME-TRAVEL strategy for the time-dependent rows (the real 15-second buffer and 24-hour expiry are slow/flaky to wait out on-device, Addresses review — clock manipulation): either (a) a DEBUG-only lowered ELIGIBLE_AFTER_SECONDS / EXPIRE_AFTER_HOURS, or (b) a run-as UPDATE of the target assist's handoff_at (WAL-aware) to backdate it just before the foreground-return test — record which technique is used per time-dependent row in 21-UAT.md.
    - Create 21-UAT.md with the scoreboard scaffold covering the matrix in Task 2, with owner-sign-off and device-dependent handoff-failure rows pre-marked as explicit BLOCKED checkpoints (not auto-forceable).
  </behavior>
  <action>FIRST retire the transitional raw settings reads (Addresses review finding #4): in ContactProfileScreen and ComposeScreen swap the defensive raw `interaction_assist_enabled` column read for getAppSettings().interactionAssistEnabled (the canonical key 21-04 added), preserving the same ON/OFF gating behavior; this is safe here because Wave 4 runs after 21-03/21-05 have committed those Wave-3 files. Then run the three node gates and record their results. Create .planning/phases/21-interaction-assist-reach-out/21-UAT.md with a scenario scoreboard for the device matrix (each row DB-verifiable), with the owner-sign-off and device-dependent handoff-failure rows pre-marked as explicit BLOCKED checkpoints. Do NOT build the APK, start Metro, or touch the device in this task — the DEBUG build and the entire device UAT are deferred past the Task 2 owner checkpoint into Task 3.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit`, `npm run check:colors` all exit 0.
    - Both ContactProfileScreen and ComposeScreen read the Assist toggle via getAppSettings().interactionAssistEnabled (a `grep -n "interactionAssistEnabled" src/screens/ContactProfileScreen.tsx src/screens/ComposeScreen.tsx` match in each), and neither screen retains a direct raw settings-column read for the toggle (verify by reading the two handoff call sites) — one canonical read path (finding #4).
    - 21-UAT.md exists with the scenario scoreboard, owner-sign-off and handoff-failure rows pre-marked BLOCKED.
    - No APK build, Metro, or `adb` device interaction occurred in this task (the device build is Task 3, gated behind the Task 2 checkpoint).
  </acceptance_criteria>
  <reversibility rating="reversible">A read-path consolidation (no behavior change) + verification + docs.</reversibility>
  <done>The two transitional raw settings reads are consolidated onto the canonical getter, the full node suite is green, and the UAT scaffold exists — with the device build/UAT deferred to Task 3 behind the Task 2 owner checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Owner checkpoint — STOP before the device build / UAT (usage check + agent handoff)</name>
  <action>STOP here and hand back to the owner BEFORE building the DEBUG APK or driving any device UAT. Do NOT build the APK, start Metro, or touch the Pixel until this checkpoint is cleared. gate="blocking-human" — NEVER auto-approved, even under auto/chain mode.</action>
  <instructions>
    Everything up to this point is done and committed: Plans 01–05 executed, the transitional raw-settings reads consolidated (finding #4), and the full node gate (npm test, npx tsc --noEmit, npm run check:colors) green with 21-UAT.md scaffolded. The ONLY remaining work is the device UAT in Task 3 — build the DEBUG APK on the desktop (ssh droid), install on the Pixel, and drive + DB-verify the matrix.
    Owner action: verify remaining usage, then either resume execution to run Task 3 with this agent, or hand off to another agent (the plan is self-contained — a fresh agent re-invokes `/gsd-execute-phase 21` and continues from Task 3).
    USB caveat: if the Pixel shows `unauthorized`, only the owner can accept the on-device "Allow USB debugging" prompt before Task 3 can proceed.
  </instructions>
  <verification>Owner has confirmed usage and chosen the executing agent for the device UAT; the Pixel is plugged in and will show authorized under `adb devices` when Task 3 begins.</verification>
  <resume-signal>Owner types "continue" (or re-invokes /gsd-execute-phase 21 with the chosen agent) to proceed into Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Drive the Pixel device UAT matrix + owner sign-off (end-of-phase human-check)</name>
  <files>.planning/phases/21-interaction-assist-reach-out/21-UAT.md</files>
  <read_first>
    - docs/dossier/21-interaction-assist-reach-out.md (the test matrix, lines ~805-825 — drive every row)
    - .planning/phases/21-interaction-assist-reach-out/21-UI-SPEC.md (expected copy/colour/behavior per surface)
    - CLAUDE.md (adb input source taxonomy; verify controls against code first — adb tap false-negatives on small RN Pressables; performance claims only on the physical phone, and the render loop is Skia-orrery, not this phase)
  </read_first>
  <behavior>
    Drive and DB-verify (run-as, WAL-aware) each matrix row:
    - one endpoint vs multiple endpoints (≤3 taps; primary emphasized);
    - Assist ON vs OFF (OFF launches native only, no assist row, no banner; OFF while pending clears the queue immediately);
    - immediate return <15s (no eligible prompt) vs normal return >15s (banner appears);
    - Call Yes (connected=1), Call No answer (connected=0), Text/Email Yes (attestation), Don't log (no interaction);
    - optional Notes on a written row;
    - handoff failure (no compatible app / thrown launch → 'failed' + per-channel Alert, no prompt);
    - 24h expiry and sixth-pending pruning (oldest expires);
    - multiple same-contact intents (two separate assists);
    - process death / reboot (non-expired pending survive and resume via the banner);
    - archived target (a PENDING assist stays loggable via the banner — Cluster Z), merged target (redirects to survivor), purged target (assist gone; deep-link → "no longer available" → Dashboard);
    - widget Contact deep-link opens the router; widget REACH to an archived-but-existing target is SILENTLY dropped (NOT "no longer available"), while a purged target shows "This contact is no longer available." → Dashboard;
    - widget Contact deep-link consume-once (finding #5): after a widget Contact tap opens the Reach Out router, backgrounding then foregrounding the app does NOT reopen the router — the openReachOut param is cleared after one consumption (no deep-link reopen loop); DB/UI-verify the router does not re-present on the next focus;
    - widget freshness after confirmation — confirming from the banner updates the home-screen widget's overdue/recency tile WITHOUT an app restart (notifyWidgetDataChanged); DB-verify last_contact moved (finding #1);
    - Compose → Messages → banner → interaction write (draft preserved into Messages via the shared performReachOut(messageBody: draft));
    - Back passes THROUGH the banner (does not dismiss it);
    - long {name} + long note render without clipping (backstop).
    The 15-second-buffer and 24-hour-expiry rows use the prepared time-travel technique (DEBUG-lowered constants or a run-as handoff_at backdate), noted per row.
    Confirm each interaction row's occurred_at === the assist handoff_at, direction='outbound', source='assist', and connected per the action; confirm the only writer of contacts.last_contact was the recency recompute (recomputeLastContactCore) — no bespoke last_contact write fired.
  </behavior>
  <action>FIRST (this task owns the device build deferred from Task 1): build the DEBUG APK on the desktop (ssh droid) and install on the Pixel (install -r preserves data); bring up Metro on the orbit port + adb reverse per the device-UAT runbook; verify the target with `adb devices` (resolve the serial, do not hardcode — exactly one device, authorized). Seed a device-local test contact with an actionable phone + email (ContactsProvider content insert per the device-local-contact-fixtures note) if none exists. Then execute the full device matrix, capturing a screenshot + a run-as DB read as evidence per row into 21-UAT.md. For any control that appears unresponsive, verify against code first and use the correct adb input method (project MEMORY) before recording a failure. Mark the owner sign-off AND the device-dependent handoff-failure rows (failed Call / failed Text / failed Email — a device may always have a dialer/messages/mail app, so a genuine "no compatible app" failure may need a deliberately-broken intent or owner confirmation) as EXPLICIT blocked checkpoints in 21-UAT.md — they are not force-passable by automation and are recorded as blocked, never silently skipped (Addresses review — explicit blocked checkpoints). Use the prepared time-travel strategy for the 15s/24h rows. This is the pre-release verification for migration 014 reaching a real device. CONDITIONAL SIGN-OFF PRE-APPROVAL (owner, 2026-08-31): the owner has pre-approved sign-off (including the migration-014 pre-ship gate) ON CONDITION that the node gates AND every device matrix row pass with DB-verified evidence and ZERO deviations — in that case the agent records approval on the owner's behalf and writes a plain-language DB-state recommendation into 21-UAT.md summarizing exactly what it verified (occurred_at===handoff_at, single-writer last_contact, connected values, the fail-safes), since the owner cannot inspect on-device DB state directly. If ANY matrix row fails or deviates, OR a genuine handoff-failure row cannot be produced, OR USB debugging is unauthorized, the agent does NOT self-approve — it STOPS and surfaces the specific rows to the owner for a decision.</action>
  <verify>
    <automated>MISSING — device-only UI/DB verification; evidence (screenshots + run-as DB reads) is captured per row in 21-UAT.md. The node correctness gates run in Task 1 (npm test) and Plans 01/04/05.</automated>
    <human-check>CONDITIONALLY PRE-APPROVED (owner, 2026-08-31): if the node gates and EVERY matrix row pass with DB-verified evidence and zero deviations, the agent records approval on the owner's behalf plus a written DB-state recommendation in 21-UAT.md — no live owner reply required. The agent STOPS and surfaces to the owner only on a failure, a deviation, an unproducible handoff-failure row, or unauthorized USB. When surfaced, the owner reviews the DB-verified 21-UAT scoreboard and confirms on-device: every confirmed interaction row has occurred_at === the assist handoff_at, direction='outbound', source='assist', connected per the action (Call Yes=1, Call No answer=0); Back passes THROUGH the banner; toggle-off clears pending immediately; a purged deep-link target shows "This contact is no longer available." → Dashboard; non-expired pending assists survive a process kill/reboot; the larger widget's Contact action opens the router. Resume signal: owner types "approved" or lists deviations.</human-check>
  </verify>
  <acceptance_criteria>
    - A DEBUG build is installed on the Pixel and `adb devices` shows exactly one authorized device before the matrix is driven.
    - Every matrix row in 21-UAT.md has a PASS with a screenshot + run-as DB evidence (or a recorded, owner-accepted deviation).
    - If all rows passed with zero deviations, 21-UAT.md records the agent's approval-on-behalf plus a plain-language DB-state recommendation (per the conditional pre-approval); otherwise it records the specific rows surfaced to the owner.
    - Confirmed interactions show occurred_at === handoff_at, direction='outbound', source='assist', and connected matching the action.
    - Back is shown passing through the banner; toggle-off clears pending on-device; a purged deep-link target shows "This contact is no longer available." and routes to Dashboard; an archived-but-existing widget REACH target is silently dropped (no purged message).
    - The widget recency tile refreshes after a banner confirmation without an app restart (finding #1).
    - After a widget Contact deep-link opens the router, a background→foreground cycle does NOT reopen it (openReachOut consumed exactly once) — no reopen loop (finding #5).
    - The 15s/24h rows are exercised via the documented time-travel technique; the owner sign-off and device-dependent handoff-failure rows are recorded as explicit BLOCKED checkpoints (not auto-passed).
    - The end-of-phase human-check records owner sign-off (or accepted deviations).
  </acceptance_criteria>
  <reversibility rating="reversible">Verification + evidence capture only; no product-code change. The owner sign-off harvested here is the pre-release gate before migration 014 ships to a real device.</reversibility>
  <done>The full device matrix passes with DB-verified evidence and the owner sign-off is captured for end-of-phase review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ OS (dialer/messages/mail, launcher deep-link) | Exercised live on-device; verifies the no-egress, permissionless handoff and the strict deep-link allow-list hold in the real runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-18 | Integrity | migration 014 on a real device | high | mitigate | Owner-gated device UAT is the pre-ship verification of the irreversible migration; DB-verified invariants (single-writer, handoff-time occurred_at) before sign-off. |
| T-21-19 | Spoofing | on-device deep-link | high | mitigate | Drive orbit://reach spoof/purged cases on-device; confirm fail-safe to Dashboard, no crash/resurrection. |
</threat_model>

<verification>
- `npm test` + `npx tsc --noEmit` + `npm run check:colors` green.
- Every device matrix row DB-verified (run-as, WAL-aware) and owner-signed.
</verification>

<success_criteria>
The full node suite is green and the complete Pixel device matrix passes with DB-verified evidence and owner sign-off — the phase's native-handoff, banner, confirmation, toggle, widget, and lifecycle behaviors all proven on the real device.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates: `.planning/phases/21-interaction-assist-reach-out/21-UAT.md` (device UAT scoreboard + evidence). The only product-code change is a read-path consolidation (finding #4): ContactProfileScreen + ComposeScreen swap their transitional raw `interaction_assist_enabled` reads to `getAppSettings().interactionAssistEnabled` — no new symbol, no behavior change.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-interaction-assist-reach-out/21-06-SUMMARY.md` when done.
</output>
