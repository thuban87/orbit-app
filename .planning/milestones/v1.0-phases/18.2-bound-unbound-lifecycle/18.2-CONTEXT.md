---
phase: 18.2-bound-unbound-lifecycle
type: context
created: 2026-08-27
note: Phase-scoped orientation. The heavy context artifacts are shared (not bisected) and live in the preserved original phase dir; this file scopes which parts govern 18.2 and points at them.
---

# Phase 18.2 — Bound/Unbound Lifecycle: Context

Phase 18.2 is the **cross-cutting lifecycle half** of the original Phase 18: establish the
independent Bound/Unbound contact lifecycle across every proactive surface — dashboard, Orrery,
Never Contacted, notifications, widgets, favourites, and AI — without losing relationship history,
on top of Phase 18.1's normalized method model. It ships the **v11 pure-shape migration**.

**Depends on Phase 18.1** being executed and verified on-device first: 18.2's proactive surfaces and
Compose read the normalized **primary actionable method** that 18.1 creates, and v10 is a shape
change on the v9 table.

## Authoritative product source

`docs/dossier/18-contact-data-normalization.md` — **read in full.** The clusters that govern 18.2:
- **G** — Bound/Unbound lifecycle (existing contacts migrate Bound; unbinding preserves history).
- **H** — gravity independent of Bound state; intensity cadence-dependent.
- **I** — cadence nullability lifecycle (`interval_days = NULL` means never-assigned only; Bound
  requires positive cadence; once assigned, never cleared).
- **J** — Never Contacted (Unbound excluded by default; optional global include).
- **K** — favourites (Unbound not presented as favourite).
- **L** — notifications and birthdays (independent of `reminders_off`; birthday-for-Unbound global toggle).
- **M** — dedicated Unbound screen; dashboard/Orrery stay Bound-only.
- **N** — AI (explicit person-level allowed for Unbound; proactive cadence-driven excludes Unbound).
- **S** — widget / quick actions fail gracefully for now-Unbound targets.
- **R (lifecycle half only)** — backup carries Bound/Unbound state and dormant cadence.

Clusters **A–F, O, P, Q and the method half of R** govern **Phase 18.1** (the normalized model this
phase builds on) — read them to understand the substrate.

## Shared reference artifacts (in the preserved original dir — referenced in place, not copied)

- `.planning/phases/18-contact-data-normalization/18-CONTEXT.md` — original combined-phase context / locked invariants.
- `.planning/phases/18-contact-data-normalization/18-RESEARCH.md` — implementation research (lifecycle SQL guards, nullable-status fragments, Orrery population/candidate ownership, consumer audit).
- `.planning/phases/18-contact-data-normalization/18-PATTERNS.md` — pattern map (new files → closest analogs).
- `.planning/phases/18-contact-data-normalization/18-UI-SPEC.md` — UI design contract (Unbound screen, settings, neutral Never-Contacted rows, form lifecycle choice, profile Bind/Unbind).
- `.planning/phases/18-contact-data-normalization/18-VALIDATION.md` — Nyquist test matrix (Bound/Unbound × cadence-set/unset × contacted/never × archived).
- `.planning/phases/18-contact-data-normalization/18-REVIEWS.md` — the two committed external review cycles; findings kept by 18.2: C3-H1 (Orrery ring-seq Bound guard), C3-H2 app_settings-lifecycle-column half, C3-M2/M3/M4/M5, H3 (cadence CHECK/trigger), plus cycle-2 lifecycle findings.
- `.planning/phases/18-contact-data-normalization/18-SPLIT-PLAN.md` — the split rationale, plan-to-bucket map, and finding divisions.

## Migration boundary (Option A)

18.2 ships **v11** (`011-contact-lifecycle-schema`): a pure shape change on the normalized contacts
table (unchanged since migration 009's v9), applied over the v10 baseline that 18.1's label migration
(`010-contact-method-label`) established — make
`interval_days` nullable, add `tracking_enabled`, the two cadence CHECK constraints, the one-way
cadence trigger (owns finding **H3**), and the `include_unbound_never_contacted` /
`birthday_unbound_enabled` settings columns. It **moves no data** (row-count-in = row-count-out with
identical values) and owns the app_settings-lifecycle-column half of finding **C3-H2**.

## Known split seam to reconcile in the 18.2 re-plan

`contacts-dao.ts` is owned by **18.1-02** for the method-array aggregate; **18.2-08** needs that
aggregate extended with a lifecycle parameter for the create/edit Bound/Unbound choice. 18.2-08 must
claim that extension explicitly. Also: CreateContactScreen/EditContactScreen (18.1-03 ↔ 18.2-08),
ContactProfileScreen/contact-profile-logic (18.1-05 ↔ 18.2-08), and the backup files (18.1-04 ↔
18.2-09) are edited in both phases — sequential build-ons since 18.1 executes first, not conflicts.

# Execution dependency update — 2026-08-28

Phase 18.1 owner decision: method labels are durable. 18.1-03 now adds a forward migration 010 for nullable `contact_methods.label` after the committed v9 migration. Before Phase 18.2 executes, re-cut its plans so its lifecycle schema migration moves from 010/v10 to the next available migration/version, and update every v9→v10 reference accordingly. Do not overwrite or fold the label migration into 009.

**Status (2026-08-28): DONE.** The cross-AI convergence cycle 1 re-cut moved the lifecycle migration to `011-contact-lifecycle-schema` / user_version 11 across all 18.2 plans and updated every v9→v10 chain reference to v10→v11 (both codex and cursor independently confirmed the v10 collision against on-disk source). See `18.2-CONVERGENCE-LOG.md`.

# Cross-AI convergence — cycle 1 owner decisions (2026-08-28)

These were surfaced during the cross-AI review (codex + cursor) and decided by the owner; they are **locked** for 18.2 execution and were baked into the plans this cycle:

- **[LOCKED] Backup format bumps to v3 (Plan 09).** Adding lifecycle fields (`tracking_enabled` + nullable cadence) changes the portable JSON wire shape, so per the `types.ts:13` bump-on-shape-change contract it ships as `BACKUP_FORMAT_VERSION = 3` with a new **v2→v3** forward migration (existing contacts default to Bound with their existing positive cadence). It is **not** an additive v2 extension. Rationale: 18.1's method normalization already bumped v1→v2 under the same rule; lifecycle is the next shape change.
- **[LOCKED] Widget fail-closed scope (Plan 06 Task 3).** A widget tap on a now-Unbound target **fails closed only for active-cadence actions** (Compose / quick-action); a plain **Profile-open still navigates** to the live Unbound contact, which remains a complete, viewable relationship record (dossier Cluster M). "Fail gracefully" (Cluster S) means degrade the action, not dead-end a valid record. Only genuinely missing/archived targets resolve to no navigation.
- **[LOCKED] Restore merge-conflict rule — retain dormant cadence (Plan 09, cycle 2).** When a modified-at-winning backup contact is Unbound with NULL cadence but the local row has a positive cadence, restore **never nulls the assigned cadence**: it applies the winner's `tracking_enabled` but keeps the local positive cadence dormant (`interval_days = COALESCE(incoming, local)`). This honors the recorded cadence-once-assigned-never-cleared invariant (Cluster I) and never trips the v11 one-way trigger. Chosen over skipping/rejecting the conflicting contact.
- **[LOCKED] Notification ingress fails closed (Plan 06 Task 4 / Plan 10, cycle 4).** The widget fail-closed principle extends to the delivered-notification surface: a stale decay notification whose contact is now Unbound routes to the contact's **Profile (view) instead of Compose** on tap (fails closed on the active-cadence action, mirroring the widget Profile-open rule; the async guard lives in `notification-gate.tsx`, not the pure `resolveNotificationNav`); a birthday notification still opens Profile; `ACTION_MARK` still records the real touchpoint (Unbound preserves history) but does not re-arm cadence; snooze is a no-op for a now-Unbound contact. Chosen over permitting delivered-notification interactions as unrestricted explicit actions. Distinct surface from the widget decision — notification-nav.ts / notification-actions.ts were previously unowned.
- **[LOCKED] Plan 01 migration runs autonomous (owner decision, 2026-08-28).** 18.2-01 (`autonomous: true`, was `false`) proceeds on green with no success-pause: it is code + node-tested only and applies nothing to a real device (the irreversible on-device application is gated by 18.2-10 Task 2's blocking UAT + manual push). The executor still escalates on any test failure or deviation from the rebuild approach / child-FK inventory. Rationale: a human cannot independently audit node-test quality, and the genuine irreversibility gate is downstream; the success-pause was low-value friction.
- **[LOCKED] Plan 10 device UAT is agent-driven (owner decision, 2026-08-28).** 18.2-10 Task 2 changed from a blocking `checkpoint:human-verify` to an agent-driven UAT (`autonomous: true`): the agent backs up, builds, installs, drives the app on the Pixel, and captures evidence (screenshots, logcat, and `run-as` DB reads proving the lifecycle invariants — since UI rendering is not proof of data correctness), auto-proceeding only when every item passes and escalating on any failure/unverifiable invariant/unavailable device. The owner still must connect + authorize the Pixel, and the human sign-off moves to push time (push is always manual). It ships nothing on its own.
