---
phase: 18.2-bound-unbound-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [18.2-03]
files_modified: [src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/impact.ts, src/services/impact.test.ts, src/db/ai-context-read.ts, src/db/ai-context-read.test.ts]
autonomous: true
requirements: [CDN-02, CDN-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Gravity and relationship history remain available for an Unbound contact, while cadence intensity is unavailable instead of calculating against NULL cadence."
    - "Explicit AI remains usable for an Unbound contact with a neutral no-intensity aggregate and no raw or canonical method PII."
  artifacts: [src/db/impact-read.ts, src/services/impact.ts, src/db/ai-context-read.ts]
  key_links: ["The population predicates from 18.2-03 establish lifecycle state before impact-read exposes nullable cadence, then the impact service and explicit-AI owner make their distinct Bound-aware decisions without a NULL arithmetic path."]
---

<objective>
Complete the dependency-ordered nullable-cadence read and explicit-AI work after lifecycle population queries are stable.
Purpose: preserve a relationship's gravity and explicitly requested AI context without treating an Unbound contact as active cadence work.
Output: Bound-aware impact contracts, neutral Unbound intensity behavior, and closed AI-context regression tests.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/impact-read.ts
@src/services/impact.ts
@src/db/ai-context-read.ts
@.planning/phases/18-contact-data-normalization/18-03-SUMMARY.md</context>

## Artifacts this phase produces

- Nullable-cadence impact inputs and a Bound-gated intensity service contract.
- Explicit-AI context that remains intentionally available for Unbound contacts without endpoint PII.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Read one Unbound contact’s gravity without cadence intensity</name>
  <read_first>src/db/impact-read.ts; src/db/impact-read.test.ts; src/services/impact.ts; src/services/impact.test.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/impact.ts, src/services/impact.test.ts</files>
  <behavior>
    - An Unbound contact retains gravity and relationship history through the impact read.
    - Cadence intensity is unavailable for both dormant-cadence and never-assigned Unbound contacts rather than calculated with a nullable cadence.
    - A Bound contact with a positive cadence retains the existing intensity behavior.
  </behavior>
  <action>Expose `tracking_enabled` and nullable cadence through src/db/impact-read.ts so its caller can retain gravity and history without applying progress arithmetic. Change the ImpactInputs and impact-service contract in src/services/impact.ts to make intensity conditional on Bound plus positive cadence. Add RED-to-GREEN owner tests in src/db/impact-read.test.ts and src/services/impact.test.ts for Bound, dormant-cadence Unbound, and never-assigned Unbound contacts. Implement D-02 and D-03 with an explicit tagged unavailable-intensity result (e.g. `{ available: false }`) — never null coerced to 0 and never a fabricated `intervalDays` — rather than an invented paused/reset status, and pin this exact contract so the profile view-model (18.2-08) and ai-context-read (Task 2) consume the same shape.</action>
  <verify><automated>npm test -- src/db/impact-read.test.ts src/services/impact.test.ts</automated></verify>
  <acceptance_criteria>Impact tests prove both Unbound cadence states retain gravity/history, never invoke cadence arithmetic, and leave Bound intensity behavior intact.</acceptance_criteria>
  <done>An impact caller can present a relationship’s gravity while correctly treating its cadence as inactive.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build explicit Unbound AI context with neutral intensity and no method PII</name>
  <read_first>src/db/ai-context-read.ts; src/db/ai-context-read.test.ts; src/db/impact-read.ts; src/services/impact.ts; 18-RESEARCH.md Consumer audit</read_first>
  <files>src/db/ai-context-read.ts, src/db/ai-context-read.test.ts, src/db/impact-read.ts, src/db/impact-read.test.ts, src/services/impact.ts, src/services/impact.test.ts</files>
  <behavior>
    - Explicit AI for an Unbound contact receives the normal closed permitted context plus exactly `{currentCount: 0, intendedPerPeriod: 0, multiple: 0, trailingAvgGapDays: null}`.
    - No raw, canonical, or formatted phone/email method value enters PromptContext for either Bound or Unbound AI requests.
    - Explicit AI does not create a proactive-selection path or call cadence arithmetic for an Unbound contact.
  </behavior>
  <action>Update src/db/ai-context-read.ts to consume the Bound-aware impact contract and emit exactly `{currentCount: 0, intendedPerPeriod: 0, multiple: 0, trailingAvgGapDays: null}` for Unbound explicit-AI requests instead of calling cadence intensity with NULL; it must not fabricate an `intervalDays` (the current code substitutes 1) and instead consumes the Task 1 tagged unavailable-intensity contract. Preserve the strict closed PromptContext projection. Extend src/db/ai-context-read.test.ts with dormant-cadence and never-assigned Unbound fixtures that assert permitted context remains available, the neutral aggregate is exact, and no raw/canonical method value is present. Implement D-02 and D-03 without adding proactive AI selection, endpoint data, or a status reinterpretation.</action>
  <verify><automated>npm test -- src/db/ai-context-read.test.ts src/db/impact-read.test.ts src/services/impact.test.ts</automated></verify>
  <acceptance_criteria>Explicit-AI tests prove a requested Unbound contact remains usable, receives only the neutral intensity aggregate, has no method PII egress, and never enters nullable-cadence arithmetic.</acceptance_criteria>
  <done>Explicit AI preserves intentional relationship support without leaking contact methods or reviving inactive cadence behavior.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite impact/read data to profile and AI callers | Lifecycle and nullable cadence determine which relationship facts may be derived. |
| Explicit AI request to closed prompt context | User-requested contextual data crosses an egress boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-11 | Tampering | lifecycle/impact projection | medium | mitigate | Bound-aware nullable-cadence contracts and owner tests prevent arithmetic on Unbound data. |
| T-18-33 | Information disclosure | explicit AI context | high | mitigate | Closed PromptContext projection and raw/canonical method absence tests. |
| T-18-34 | Elevation | explicit AI eligibility | medium | mitigate | Preserve only user-initiated person-level context; do not add proactive candidate selection. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; 18.1-01 owns the parser provenance gate. |
</threat_model>
<verification>Run the named impact and AI-context suites only before Wave 3 completes; the repository-wide test, TypeScript, and color compatibility gate remains 18.2-10.</verification>
<success_criteria>Unbound relationship gravity and explicit AI remain available without nullable-cadence arithmetic or method PII exposure.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-04-SUMMARY.md` when done</output>
