---
phase: 18.2-bound-unbound-lifecycle
plan: 05
subsystem: database-ui
tags: [sqlite, lifecycle, orrery, sun-picker, vitest]
requires:
  - phase: 18.2-03
    provides: Lifecycle-bearing contact headers and Bound-aware query-owner conventions
provides:
  - Shared saved-sun self fallback for Unbound, missing, and archived contacts
  - Bound-only Orrery population, sun candidates, and ring-sequence write guards
affects: [18.2-07-settings, 18.2-08-profile-lifecycle, lifecycle]
actuals:
  tokens: 20488.5
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [pure lifecycle-header mapping, SQL-owned Bound population guards]
key-files:
  created: []
  modified: [src/logic/sun-occupant-logic.ts, src/screens/OrreryScreen.tsx, src/db/orrery-read.ts, src/db/ring-seq-dao.ts, src/db/sun-picker-read.ts]
key-decisions:
  - "A saved Unbound sun keeps app_settings.sun_contact_id intact and resolves visually to self through the shared predicate."
  - "Orrery reads and ring-sequence guards use the same Bound predicate, while never-contacted Bound contacts remain picker candidates."
patterns-established:
  - "Lifecycle-sensitive screen mapping must be pure and feed one shared policy predicate; eligibility remains at SQL query owners."
requirements-completed: [CDN-02, CDN-03]
coverage:
  - id: D1
    description: Saved Unbound sun renders self without changing its persisted selection and restores after Bind.
    requirement: CDN-02
    verification:
      - kind: unit
        ref: npm test -- src/logic/sun-occupant-logic.test.ts
        status: pass
    human_judgment: false
  - id: D2
    description: Orrery population, picker candidates, and ring reorder guards exclude Unbound contacts while retaining valid Bound behavior.
    requirement: CDN-03
    verification:
      - kind: integration
        ref: npm test -- src/db/orrery-read.test.ts src/db/ring-seq-dao.test.ts src/db/sun-picker-read.test.ts
        status: pass
    human_judgment: false
duration: 4min
completed: 2026-08-29
status: complete
---

# Phase 18.2 Plan 05: Bound-Aware Orrery Summary

**Orrery lifecycle policy now preserves a saved Unbound sun as a self rendering while Bound-only reads and reorder guards keep proactive orbit data consistent.**

## Performance

- **Duration:** 4min
- **Started:** 2026-08-29T00:30:25Z
- **Completed:** 2026-08-29T00:34:31Z
- **Tasks:** 2/2
- **Files modified:** 9

## Accomplishments

- Added one shared sun predicate that treats a saved Unbound contact as self without rewriting `app_settings.sun_contact_id`; rebinding restores the selected contact sun automatically.
- Extracted the Orrery header-to-sun mapping into a pure tested function, ensuring `getContactHeader().trackingEnabled` reaches the sole lifecycle policy.
- Restricted the active Orrery, sun candidates, and ring-sequence count/update guards to Bound contacts while retaining never-contacted Bound picker candidates and the intentional no-snooze-filter rule.

## Task Commits

1. **Task 1 (RED): failing saved-sun lifecycle coverage** — `0a52c7e` (`test`)
2. **Task 1 (GREEN): shared saved-sun lifecycle policy** — `eb6ad6e` (`feat`)
3. **Task 2 (RED): failing Bound Orrery coverage** — `99ff7cb` (`test`)
4. **Task 2 (GREEN): Bound-only Orrery query owners** — `a81c509` (`feat`)

## Files Created/Modified

- `src/logic/sun-occupant-logic.ts` and `src/logic/sun-occupant-logic.test.ts` — shared lifecycle-aware sun predicate and pure header mapping coverage.
- `src/screens/OrreryScreen.tsx` — passes the lifecycle-bearing direct header through the pure mapper without mutating the saved setting.
- `src/db/orrery-read.ts`, `src/db/ring-seq-dao.ts`, and `src/db/sun-picker-read.ts` — aligned Bound-only population, selector, and write guards.
- Their adjacent test suites — migrate fixtures through v11 and prove Unbound exclusion, Bound never-contacted eligibility, and successful reorder with a contacted Unbound row present.

## Verification

- `npm test -- src/logic/sun-occupant-logic.test.ts src/db/orrery-read.test.ts src/db/ring-seq-dao.test.ts src/db/sun-picker-read.test.ts` — passed (44 tests).
- `npx biome check` across all nine task files — passed.

## Decisions Made

- The database setting remains the durable user preference even when its current contact is Unbound; self is a rendering fallback only.
- `tracking_enabled = 1` is placed at each active Orrery SQL owner, including both ring reorder guards, rather than filtered in the screen.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Next Phase Readiness

The Settings sun picker can consume the same shared predicate in its later lifecycle UI work, and lifecycle form/profile work can Bind or Unbind without losing the saved sun preference.

## Self-Check: PASSED

- All nine modified task artifacts and this summary exist on disk.
- Task commits `0a52c7e`, `eb6ad6e`, `99ff7cb`, and `a81c509` exist in git history.
