---
phase: 18.2-bound-unbound-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [18.2-01]
files_modified: [src/db/contact-lifecycle-dao.ts, src/db/contact-lifecycle-dao.test.ts, src/screens/CaptureScreen.tsx, src/screens/CaptureScreen.test.tsx]
autonomous: true
requirements: [CDN-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Bind/Unbind only changes lifecycle state; existing cadence, favourite rank, and relationship history survive transitions."
    - "The four lifecycle cells are explicit: Bound+positive, Unbound+positive, Unbound+NULL, and forbidden Bound+NULL."
  artifacts: [src/db/contact-lifecycle-dao.ts]
  key_links: ["Bind and Unbind are named DAO operations exercising the immutable v11 CHECK/trigger constraints; Capture's inline create composes an explicit Bound lifecycle through the aggregate contract."]
---

## RE-CUT NOTE

Re-cut from `.planning/phases/18-contact-data-normalization/18-02-PLAN.md` — Task 2 (Bind/Unbind state transitions) carried in as Task 1, and the **lifecycle half** of Task 3 (Capture's inline Bound create) carried in as Task 2.

**Stayed in 18.1-02:** the method DAO (original Task 1) and the method half of Task 3 (aggregate create/edit accepting method arrays). The immutable cadence CHECKs and one-way trigger this DAO exercises are created and migration-tested by **18.2-01's v11 migration** (`011-contact-lifecycle-schema.ts`), not by this plan.

<objective>
Make Bound/Unbound transactional write capabilities and compose lifecycle into inline capture.
Purpose: preserve lifecycle invariants for every later form, profile, backup, and sync operation.
Output: tested lifecycle DAO and updated Capture inline create.
</objective>
<execution_context>@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/18-contact-data-normalization/18-RESEARCH.md
@.planning/phases/18-contact-data-normalization/18-PATTERNS.md
@src/db/contacts-dao.ts
@src/db/migrations/011-contact-lifecycle-schema.ts
@src/db/transaction.ts</context>

## Artifacts this phase produces

- Transactional lifecycle transitions (Bind/Unbind) with data-revision behavior.
- Capture inline-create integration supplying the explicit Bound lifecycle contract.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce Bind and Unbind state transitions</name>
  <read_first>src/db/contacts-dao.ts:101-178,281-330; src/db/migrations/011-contact-lifecycle-schema.ts; docs/dossier/18-contact-data-normalization.md Clusters G-I; 18-PATTERNS.md Bound/Unbound lifecycle writes</read_first>
  <files>src/db/contact-lifecycle-dao.ts, src/db/contact-lifecycle-dao.test.ts</files>
  <behavior>
    - Unbind leaves last_contact, interactions, cadence, and favourite_rank unchanged while toggling tracking state.
    - Bind accepts a dormant positive cadence or requires a supplied positive cadence when none was assigned.
    - SQL guard rejects Bound+NULL and any positive-to-NULL update, including a direct SQL attempt that rolls back.
  </behavior>
  <action>Create the lifecycle DAO with pre-transaction validation, `inWriteTransaction`, exact affected-row checking, and data-revision behavior matching contacts-dao (each successful Bind and Unbind bumps `modified_at` and increments `data_revision` exactly once). Bind and Unbind are named operations, never a nullability shortcut. Exercise D-02 and D-03: retain dormant favourite rank without making it visible here, preserve all relationship data, and never clear an assigned cadence. Exercise the immutable v11 constraints created and migration-tested by 18.2-01 through direct SQL in the lifecycle suite, proving Bound-plus-NULL and old-positive-to-NULL attempts reject and roll back; do not edit migration 011 in this plan. This exercises finding H3's v11 constraints.</action>
  <verify><automated>npm test -- src/db/contact-lifecycle-dao.test.ts</automated></verify>
  <acceptance_criteria>Lifecycle tests cover boundary values, adjacent transitions, empty-history contacts, cadence precision, and direct SQL rejection against the shipped v11 constraints; 18.2-01 owns the migration DDL and its source test.</acceptance_criteria>
  <done>Lifecycle writes make Bound/Unbound independent from cadence and history at both DAO and SQL layers.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compose the Bound lifecycle into inline capture create</name>
  <read_first>src/db/contacts-dao.ts; src/db/contact-lifecycle-dao.ts; src/screens/CaptureScreen.tsx; 18-UI-SPEC.md Create contact</read_first>
  <files>src/screens/CaptureScreen.tsx, src/screens/CaptureScreen.test.tsx</files>
  <behavior>
    - Capture's inline name-only create supplies the updated Bound lifecycle/method contract without scalar phone/email inputs.
    - A never-assigned Unbound may save with no methods or cadence, while a Bound create requires a positive cadence.
  </behavior>
  <action>Update CaptureScreen's inline name-only create and its owner test to pass an explicit Bound positive cadence plus empty method drafts through that aggregate contract; it must not retain scalar compatibility inputs. Implement D-02; preserve the stable coarse interaction model and omit endpoint/provider interaction changes.</action>
  <verify><automated>npm test -- src/screens/CaptureScreen.test.tsx</automated></verify>
  <acceptance_criteria>Capture tests prove inline creation remains Bound with an explicit positive cadence and no scalar contract, committing contact and lifecycle together or leaving all unchanged; a test also proves the inline create persists tracking_enabled=1 via the v11 DDL default without Capture writing it explicitly.</acceptance_criteria>
  <done>Inline capture creates a Bound contact through the normalized lifecycle contract without a legacy scalar writer.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| lifecycle command to contacts row | A state change affects proactive eligibility without changing owned relationship data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-06 | Tampering | cadence transition | high | mitigate | SQL checks/trigger plus DAO validation reject illegal lifecycle cells. |
| T-18-07 | Information disclosure | errors/logs | medium | mitigate | Map save errors to generic UI paths; do not Logger-print methods or canonical values. |
| T-18-SC | Tampering | package install | high | accept | No package installation occurs in this plan; parser was approved in 18.1-01. |
</threat_model>
<verification>Run the named targeted lifecycle DAO and Capture suites after each task and keep execution within each task's declared owner files. The repository-wide test, TypeScript, and color compatibility gate is deferred to 18.2-10.</verification>
<success_criteria>All lifecycle writes preserve atomicity, PII boundaries, lifecycle invariants, and future sync provenance.</success_criteria>
<output>Create `.planning/phases/18.2-bound-unbound-lifecycle/18.2-02-SUMMARY.md` when done</output>
