---
phase: 18.2-bound-unbound-lifecycle
plan: 03
subsystem: database
tags: [sqlite, lifecycle, dashboard, digest, status, vitest]
requires:
  - phase: 18.2-01
    provides: "v11 tracking_enabled lifecycle column and nullable cadence constraint"
provides:
  - "Direct profile/edit lifecycle reads and an alphabetical neutral Unbound population"
  - "Bound-only dashboard, favourite, status, and overlooked-digest SQL projections"
  - "Explicit status-fragment precondition for Bound positive-cadence callers"
affects: [18.2-04, 18.2-05, 18.2-06, 18.2-07, 18.2-10]
actuals:
  tokens: 33242
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [SQL-owned lifecycle eligibility, neutral retrieval projection, shared status-fragment precondition]
key-files:
  created: [src/db/unbound-read.ts, src/db/unbound-read.test.ts]
  modified: [src/db/contact-read.ts, src/db/dashboard-read.ts, src/db/contact-status-read.ts, src/db/digest-read.ts, src/db/status.ts, src/db/queries.ts]
key-decisions:
  - "Direct profile, edit, dashboard search, and Never Contacted reads retain lifecycle state but render Unbound status, progress, and favourite rank neutrally."
  - "Digest retrospective and gentle-line reads remain all-relationship-history; only the active overlooked projection is Bound-only."
  - "STATUS_SCAN callers must use the shared Bound positive-cadence precondition; benchmark.ts remains an audited positive-cadence fixture and fragment consumer for 18.2-10."
patterns-established:
  - "Lifecycle-sensitive population eligibility lives at each SQL owner, never in a screen-level filter."
requirements-completed: [CDN-02, CDN-03]
coverage:
  - id: D1
    description: "Profile/edit reads preserve normalized methods and lifecycle state while a dedicated alphabetical Unbound read returns neutral metadata."
    requirement: CDN-02
    verification:
      - kind: integration
        ref: "npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "Dashboard, favourites, status scans, and active digest projections exclude Unbound contacts while direct/all-history reads retain them safely."
    requirement: CDN-03
    verification:
      - kind: integration
        ref: "npm test -- src/db/dashboard-read.test.ts src/db/contact-status-read.test.ts src/db/digest-read.test.ts src/db/status.test.ts src/db/queries.test.ts"
        status: pass
    human_judgment: false
duration: 17min
completed: 2026-08-29
status: complete
---

# Phase 18.2 Plan 03: Lifecycle Query Owners Summary

**Bound-only proactive SQL projections now coexist with lifecycle-bearing direct retrieval and a neutral alphabetical Unbound population.**

## Performance

- **Duration:** 17min
- **Started:** 2026-08-29T00:02:00Z
- **Completed:** 2026-08-29T00:18:47Z
- **Tasks:** 2/2
- **Files modified:** 14

## Accomplishments

- Added `trackingEnabled` to profile/edit aggregates and a dedicated live-Unbound alphabetical read without restoring retired scalar endpoint fields.
- Constrained dashboard defaults, favourites, snoozed totals, header counts, status scans, and overlooked digest rows to Bound contacts at their SQL owners.
- Preserved direct search/Never Contacted retrieval as neutral for Unbound rows, and pinned retrospective/gentle digest reads as all-relationship-history.

## Task Commits

1. **Task 1 (RED): lifecycle direct-read coverage** — `1e06a27` (`test`)
2. **Task 1 (GREEN): lifecycle-aware direct reads** — `55b0528` (`feat`)
3. **Task 2 (RED): lifecycle query-owner coverage** — `4259b5d` (`test`)
4. **Task 2 (GREEN): Bound lifecycle query gates** — `076f936` (`feat`)

## Verification

- `npm test -- src/db/contact-read.test.ts src/db/unbound-read.test.ts` — passed (20 tests).
- `npm test -- src/db/dashboard-read.test.ts src/db/contact-status-read.test.ts src/db/digest-read.test.ts src/db/status.test.ts src/db/queries.test.ts` — passed (91 tests).
- Combined source-adjacent suite — passed (111 tests).
- `npx biome check` across all ten Task 2 files and all four Task 1 files — passed.

## Decisions Made

- Profile/edit and retrieval-oriented paths retain both lifecycle states, but Unbound rows expose null status/progress/favourite projections for neutral rendering.
- Retrospective and gentle-line digest reads deliberately preserve all relationship history; active overlooked rows are cadence-owned and therefore Bound-only.
- `STATUS_CADENCE_PRECONDITION` centralizes the Bound/non-null cadence contract for future fragment consumers. `benchmark.ts` already seeds a positive cadence before using `STATUS_SCAN`; its lifecycle regression audit remains owned by 18.2-10.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Next Phase Readiness

Plans 18.2-04 through 18.2-07 can consume `trackingEnabled` while relying on SQL-owned neutral direct retrieval and Bound-only proactive populations.

## Self-Check: PASSED

- All fourteen lifecycle source/test artifacts and this summary exist on disk.
- Task commits `1e06a27`, `55b0528`, `4259b5d`, and `076f936` exist in git history.
