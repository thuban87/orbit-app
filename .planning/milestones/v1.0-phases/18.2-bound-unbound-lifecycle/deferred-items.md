# Deferred Items

- `npm run check:colors` remains blocked by the pre-existing hardcoded test fixture colour in `src/db/migrations/011-contact-lifecycle-schema.test.ts:187`. It is outside Plan 18.2-07's files and was left unchanged.

- **Status:** acknowledged
