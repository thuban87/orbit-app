---
phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002
plan: "04"
subsystem: android-contact-import
tags: [android, permissions, react-native, async-storage, contact-import]
requires:
  - phase: "19.1"
    provides: "API <= 36 legacy picker route, shared import seam, and searchable custom picker"
provides:
  - "Privacy-forward first-request, recoverable denial, and permanent-denial permission UX"
  - "Durable permission presentation state with fresh OS checks on focus"
  - "Settings and dashboard contact-import entry points routed through one SDK seam"
affects: ["19.1-05 device-uat", "contact acquisition", "Android permissions"]
actuals:
  tokens: 5631
  tasks: 3
  commits: 5
tech-stack:
  added: []
  patterns:
    - "Permission permanence derives only from the typed NEVER_ASK_AGAIN result; AsyncStorage only restores presentation"
    - "Fresh focus checks recover Settings changes without automatic permission re-prompts"
key-files:
  created:
    - "src/services/contacts/read-contacts-permission-logic.ts"
    - "src/services/contacts/read-contacts-permission-logic.test.ts"
  modified:
    - "src/services/contacts/use-read-contacts-permission.ts"
    - "src/screens/LegacyContactPickerScreen.tsx"
    - "src/screens/SettingsScreen.tsx"
  deleted:
    - "src/screens/use-import-unsupported.ts"
key-decisions:
  - "Treat plain denial as recoverable and reserve permanent status exclusively for NEVER_ASK_AGAIN."
  - "Persist separate permanent, requested-before, and deny-count presentation flags while letting a fresh grant clear them."
  - "Use the existing startContactImport/contactImportMode seam from Settings as well as the dashboard FAB."
patterns-established:
  - "Permission screens prime before an intentional request and re-check only on focus after Settings."
  - "System-path import errors present the same Alert from every entry point."
requirements-completed: [IMP-01]
coverage:
  - id: D1
    description: "Typed request-result classifier keeps DENIED recoverable and detects NEVER_ASK_AGAIN as permanent."
    requirement: IMP-01
    verification:
      - kind: unit
        ref: "npx vitest run src/services/contacts/read-contacts-permission-logic.test.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "Permission-state picker views and the shared Settings routing compile with token-only styling."
    requirement: IMP-01
    verification:
      - kind: other
        ref: "npx tsc --noEmit; npm run check:colors"
        status: pass
    human_judgment: true
    rationale: "The Android denial, permanent-denial, deep-link, and focus-return sequences require the Pixel 3a UAT scheduled in plan 19.1-05."
duration: 6min
completed: 2026-08-30
status: complete
---

# Phase 19.1 Plan 04: Permission UX and Routing Summary

**Older Android contact import now primes consent, recovers from denial without a degraded picker, restores Settings escapes durably, and routes both entry points through the hybrid import seam.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-30T10:47:19Z
- **Completed:** 2026-08-30T10:52:27Z
- **Tasks:** 3/3
- **Files modified:** 6

## Accomplishments

- Added pure, tested typed permission classification and durable presentation flags for first-entry, repeat denial, and permanent denial.
- Gated the legacy picker behind token-driven priming, Grant access, and Open Settings views; returning from Android Settings performs a fresh check only.
- Retired the Android-17-only import state and made Settings dispatch through `contactImportMode()` / `startContactImport()` with dashboard-matched error feedback.

## Task Commits

1. **Task 1: Permission hook + pure classifier (grant | deny | permanent denial)** — `6518f87` (test, RED), `fc31575` (feat, GREEN), `7071c2b` (fix)
2. **Task 2: Screen permission states — prime, calm-deny + re-request, permanent → Open Settings** — `20fd16a` (feat)
3. **Task 3: Retire the unsupported state (D-05) + route Settings through the shared seam** — `104d5a2` (feat)

## Files Created/Modified

- `src/services/contacts/read-contacts-permission-logic.ts` — pure typed request-result classifier.
- `src/services/contacts/use-read-contacts-permission.ts` — guarded fresh check/request wrappers, durable presentation flags, and guarded app-settings deep link.
- `src/screens/LegacyContactPickerScreen.tsx` — priming, recoverable denial, persisted permanent-denial, and focus recovery UI.
- `src/screens/SettingsScreen.tsx` — always-enabled shared import dispatch and user-visible import error parity.
- `src/screens/use-import-unsupported.ts` — deleted obsolete Android-17-only capability gate.

## Decisions Made

- Plain `DENIED` stays recoverable; only Android's typed `NEVER_ASK_AGAIN` result establishes permanent denial.
- The deny-count Settings escape is persisted but remains a presentation affordance, never a classifier input.
- AsyncStorage persistence is best-effort and never overrides the current OS permission verdict.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved the typed OS verdict when AsyncStorage is unavailable**
- **Found during:** Task 1 (Permission hook + pure classifier)
- **Issue:** A storage write failure could route a successfully classified request through the generic denied fallback.
- **Fix:** Made durable presentation writes best-effort so they cannot alter the result returned by `PermissionsAndroid.request()`.
- **Files modified:** `src/services/contacts/use-read-contacts-permission.ts`
- **Verification:** Focused Vitest classifier, TypeScript, and token checks pass.
- **Committed in:** `7071c2b`

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** Correctness hardening only; no scope expansion.

## Issues Encountered

- Focused Vitest retains the repository's existing Vite CommonJS/ESM configuration deprecation warning; tests passed and no configuration change was in scope.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 19.1-05 can validate the Android 14/15/16 permission sequences, Settings deep link, and both import entry points on the Pixel 3a.
- No Android device or contact data was accessed during this plan.

## Self-Check: PASSED

- Created files exist; the unsupported-gate deletion is confirmed.
- Task commits `6518f87`, `fc31575`, `20fd16a`, `104d5a2`, and `7071c2b` exist in local history.

---
*Phase: 19.1-older-android-contact-picker-hybrid-two-picker-adr-002*
*Completed: 2026-08-30*
