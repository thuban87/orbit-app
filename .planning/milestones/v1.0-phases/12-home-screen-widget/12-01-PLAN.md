---
phase: 12-home-screen-widget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/theme-types.ts
  - src/theme/theme-presets.ts
  - src/theme/theme-presets.test.ts
  - src/components/ContactCard.tsx
  - src/components/ContactCard.test.tsx
autonomous: true
requirements: [WDG-01]
must_haves:
  truths:
    - "Dashboard ContactCards render a real status-colour ring (stable teal-green / wobble gold / decay coral / rogue amber / never-contacted neutral) instead of the opacity placeholder."
    - "The ring border weight escalates with overdue-ness (stable 2 / wobble 3 / decay 4 / rogue 3) as a colour-blind-safe redundant channel."
    - "The status accessibilityLabel (Stable/Wobbling/Decaying/Rogue/Not yet contacted) is preserved for uiautomator UAT."
    - "A pure ringVisual(status, colors) helper returns {color, opacity, width} and is unit-covered for all five status states (addresses Codex/Claude M1)."
  artifacts:
    - "src/theme/theme-types.ts declares statusStable/statusWobble/statusDecay token fields next to rogue."
    - "src/theme/theme-presets.ts seeds the three owner-approved hexes in the space-dark palette; theme-presets.test.ts asserts all three seeds."
    - "src/components/ContactCard.tsx consumes the new tokens via useTheme().colors.* through the pure ringVisual() helper; ContactCard.test.tsx covers ringVisual."
  key_links:
    - "ContactCard -> useTheme().colors.statusStable|statusWobble|statusDecay|rogue -> theme-presets space-dark seed."
    - "The same three tokens are consumed by the widget render (12-03/12-05) and inherited by Phase 13 — one source of truth."
---

## Phase Goal

**As a** person keeping people in orbit, **I want to** see my favourites on a home-screen widget with their status colours and act on them in one tap, **so that** I can mark contact or open someone without launching the app.

> MVP note: this phase is a native Android widget; its user-visible surface is gated on a device rebuild (12-08). Per the plan-phase reconciliation, the node-testable correctness cores are built and unit-tested ahead of the native surface. THIS plan is the first real, shippable user-facing slice: it upgrades the existing dashboard so every ContactCard shows its true status colour (retiring the OD-1 opacity placeholder) and lands the shared status tokens the widget will consume.

<objective>
Introduce the shared app-wide status palette — `statusStable` / `statusWobble` / `statusDecay` (joining the existing `rogue`) — as owner-approved theme tokens, and retire ContactCard's opacity-only status-ring placeholder (OD-1) to consume them with a redundant border-weight channel.

Purpose: WDG-01 requires "each avatar carrying its status colour." The palette is a single source of truth shared by the dashboard ContactCard (now), the widget bitmap (12-03/12-05), and the Phase-13 orrery. The hexes were owner-approved on 2026-08-16 (UI-SPEC ⭐ section): `statusStable #45B98A`, `statusWobble #E8C15C`, `statusDecay #E56A52`, `rogue #E0904A` unchanged.
Output: three new theme tokens seeded in space-dark; a ContactCard that reads real colour + escalating weight per status; accessibilityLabel preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-screen-widget/12-UI-SPEC.md
@.planning/phases/12-home-screen-widget/12-PATTERNS.md

# Analogs (verified on disk this planning session):
@src/theme/theme-types.ts        # rogue token shape at :66-75; avatarSwatches ordered-array idiom :58
@src/theme/theme-presets.ts      # rogue seed :54; gravityTiers ordered ramp :59; resolvePalette :96
@src/components/ContactCard.tsx  # OD-1 opacity placeholder :102-116; statusLabel switch :73-86; useTheme :100
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the three shared status tokens on ThemePalette</name>
  <files>src/theme/theme-types.ts</files>
  <read_first>src/theme/theme-types.ts:40-90 (the `danger` / `avatarSwatches` / `rogue` / `gravityTiers` token declarations — copy the flat-string-field + doc-comment shape of `rogue`).</read_first>
  <action>Add three new flat `string` fields to the palette colours type immediately after the existing `rogue: string;` declaration (theme-types.ts:75): `statusStable`, `statusWobble`, `statusDecay`. Give each a doc-comment mirroring rogue's — state it is a shared app-wide STATUS hue (distinct from `danger`, which is destructive-action red; status is a state, not an action), consumed by the dashboard ContactCard, the Phase-12 widget bitmap, and the Phase-13 orrery, and seeded only in theme-presets.ts (the one colour-literal file). Name the buckets from status.ts: stable = <80% of interval elapsed; wobble = 80-100%; decay = >100%. These are flat tokens (mirroring rogue/danger/accent), NOT an array. Do not touch any other field.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ThemePalette (or the colours interface) declares statusStable, statusWobble, statusDecay as string fields; tsc fails only where the presets must now seed them (fixed in Task 2), or passes if Task 2 lands together.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the owner-approved hexes in the space-dark preset + assert them</name>
  <files>src/theme/theme-presets.ts, src/theme/theme-presets.test.ts</files>
  <read_first>src/theme/theme-presets.ts:40-62 (the space-dark colours block — the `rogue: "#E0904A"` seed at :54 and the `gravityTiers` ramp at :59 are the exact insertion neighbourhood); src/theme/theme-presets.test.ts:91 (the existing palette-seed assertion block to extend, e.g. the rogue-token assertion — mirror it for the three new tokens).</read_first>
  <action>In the space-dark palette colours block (the ONLY preset shipped today; light falls back to dark via resolvePalette), seed the three owner-approved hexes next to `rogue`: `statusStable: "#45B98A"`, `statusWobble: "#E8C15C"`, `statusDecay: "#E56A52"`. Leave `rogue: "#E0904A"` unchanged. Add a short comment noting these are owner-approved (2026-08-16, UI-SPEC) shared status tokens, owner-tunable like the other seeds. These literals are permitted here because theme-presets.ts is the single file check:colors allows hex in. Then, in theme-presets.test.ts (extending the existing seed-assertion block near :91), assert resolvePalette(DEFAULT_PRESET_ID, "dark") returns each new token equal to its owner-approved hex — statusStable=#45B98A, statusWobble=#E8C15C, statusDecay=#E56A52 (addresses Codex/Claude M1: lock the three new tokens under test so a future seed drift fails). The assertion values are the source-of-truth hexes, and check:colors permits them in a .test.ts under the theme dir per the existing rogue-assertion precedent (confirm the existing rogue assertion at :91 already carries a hex literal; if check:colors would reject a hex in the test, assert against theme-presets' own exported preset object instead of a literal).</action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit && npx vitest run src/theme/theme-presets.test.ts</automated>
  </verify>
  <done>space-dark seeds all three hexes exactly (#45B98A / #E8C15C / #E56A52); theme-presets.test.ts asserts all three; check:colors passes; tsc clean; theme-presets test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retire the ContactCard OD-1 opacity placeholder via a pure ringVisual() helper</name>
  <files>src/components/ContactCard.tsx, src/components/ContactCard.test.tsx</files>
  <behavior>
    - ringVisual(status, colors) is a PURE helper returning { color, opacity, width } (addresses Codex/Claude M1 — extract the mapping so it is unit-testable without rendering the card):
      - color by status: stable -> colors.statusStable, wobble -> colors.statusWobble, decay -> colors.statusDecay, rogue -> colors.rogue, null (never-contacted) -> colors.border.
      - opacity is no longer the status signal: 1.0 for all four real statuses; ~0.5 for the null/never-contacted neutral so "no status yet" reads as absence.
      - width (border weight) escalates: stable 2 / wobble 3 / decay 4 / rogue 3; null keeps a thin neutral (2).
    - statusLabel() accessibilityLabel output is unchanged: Stable / Wobbling / Decaying / Rogue / Not yet contacted.
  </behavior>
  <read_first>src/components/ContactCard.tsx:73-116 (statusLabel switch + the OD-1 ringColor/ringOpacity placeholder) and :137-149 (the status-ring View that applies borderColor/opacity/testID/accessibilityLabel); src/services/widget/widget-colors.ts is the HEADLESS sibling (12-03) — ringVisual here is the in-app card version that also carries opacity; keep the two tables consistent but separate (the card has a theme provider + an opacity concept the widget lacks).</read_first>
  <action>Extract a pure, exported `ringVisual(status: ProfileStatus | null, colors: ThemePalette["colors"]): { color: string; opacity: number; width: number }` that encodes the behaviour table above, and replace the OD-1 placeholder (ContactCard.tsx:102-116) that derives ringColor from `colors.textSecondary`/`colors.rogue` and encodes status via ringOpacity with a single call to ringVisual(status, colors). Apply its color/opacity/width to the status-ring style (a small change to the existing styles.statusRing.borderWidth, driven by the returned width). PRESERVE the statusLabel() accessibilityLabel exactly (:140) and the testID `dashboard-card-status-${contactId}`. Keep every colour resolving through useTheme().colors.* (:100) — this is the in-app path with a ThemeProvider mounted, unlike the headless widget render. Do not change the card's content contract (avatar, name, fuel line, favourite star, category chip) — this is the single visual change. Write ContactCard.test.tsx importing the pure ringVisual helper (no render needed) and asserting color + opacity + width for all five states (stable/wobble/decay/rogue/null) against a fixture colors object — verifying the retired opacity-encoding cannot silently return.</action>
  <verify>
    <automated>npm run check:colors && npx tsc --noEmit && npx vitest run src/components/ContactCard.test.tsx</automated>
  </verify>
  <done>ringVisual is a pure exported helper unit-covered for all five states (color+opacity+width); ContactCard consumes it via useTheme().colors.*; accessibilityLabel + testIDs preserved; check:colors + tsc clean; ContactCard.test.tsx green (no masked-failure fallback in the verify command).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme literal surface | Colour hexes may only exist in theme-presets.ts; a literal elsewhere breaks the restyle-the-whole-app contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Tampering | ContactCard / widget colour literals | low | mitigate | All status colours resolve through tokens; hex confined to theme-presets.ts; check:colors gate enforced in Tasks 2 and 3 |
</threat_model>

<verification>
- `npm run check:colors` passes (no hex literal outside theme-presets.ts).
- `npx tsc --noEmit` clean.
- The three status tokens exist on the palette type and are seeded in space-dark.
- ContactCard status ring shows real colour + escalating weight; accessibilityLabel unchanged.
</verification>

<success_criteria>
- WDG-01 (partial): each avatar carries its status colour on the dashboard; the shared palette exists for the widget to consume.
- The OD-1 opacity placeholder is retired; the redundant border-weight channel is present for CVD safety.
</success_criteria>

## Artifacts This Phase Produces (this plan)

New symbols introduced here — exclude from source-grounding drift checks:
- Theme tokens: `statusStable`, `statusWobble`, `statusDecay` (field on the palette type in theme-types.ts; hex seed in theme-presets.ts space-dark).
- `ringVisual` (pure helper exported from ContactCard.tsx) + `src/components/ContactCard.test.tsx`.

<output>
Create `.planning/phases/12-home-screen-widget/12-01-SUMMARY.md` when done.
</output>
