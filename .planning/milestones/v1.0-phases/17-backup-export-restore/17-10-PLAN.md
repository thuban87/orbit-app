---
phase: 17-backup-export-restore
plan: 10
type: execute
wave: 7
depends_on: [17-08, 17-09]
files_modified: [src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/BackupScreen.tsx, src/screens/backup-restore-logic.ts, src/screens/backup-restore-logic.test.ts]
autonomous: true
requirements: [BKP-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Restore selection reaches an aggregate preview only after complete decrypt/parse/validation; Merge is the default."
    - "Replace-all requires explicit impact confirmation and reports whether its pre-restore automatic copy was verified or unavailable."
    - "Success/failure reveals only concise totals and always states unchanged local data for pre-commit failures."
    - "An expired/unresolvable RestorePreview token (process death) recovers to re-selection rather than crashing or showing stale data; a cold app start never shows a stuck non-dismissable applying state."
  artifacts:
    - "src/screens/RestorePreviewScreen.tsx"
    - "src/screens/RestoreResultScreen.tsx"
  key_links:
    - "Backup landing launches system selection; only validated route data reaches the preview screen."
    - "Result route returns to Backup and refreshes health after committed restore."
  prohibitions:
    - "Restore preview/result must not become a per-record audit browser or imply success before commit. [flagged-unverified]"
---

<objective>
Wire the restore engine into the dedicated selection, preview, confirmation, progress, and result UI flow.

Purpose: let users recover safely while preserving validation-before-write and calm privacy-preserving feedback.
Output: restore routes/screens and tested UI decision helpers.
</objective>

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-backup-export-restore/17-UI-SPEC.md
@src/screens/BackupScreen.tsx
@src/backup/restore-apply.ts
@src/services/backup/backup-service.ts
@src/navigation/types.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one validated plaintext backup from picker to Merge preview</name>
  <read_first>17-UI-SPEC.md Restore selection / preview / error states; src/screens/ArchivedContactsScreen.tsx; src/services/backup/backup-service.ts; src/backup/backup-schema.ts</read_first>
  <files>src/screens/BackupScreen.tsx, src/screens/RestorePreviewScreen.tsx, src/screens/backup-restore-logic.ts, src/screens/backup-restore-logic.test.ts</files>
  <behavior>
    - Restore a backup opens one cached system picker selection and shows progress while loadBackupForPreview runs.
    - Valid plaintext data reaches aggregate preview with Merge selected; encrypted files prompt before preview.
    - Picker cancellation is quiet; damaged/invalid/newer files return to the documented recovery point with unchanged-data copy and no record content.
    - RestorePreview opened with a token whose in-memory preview cache no longer exists (process death / cache eviction between navigation and screen mount) shows a quiet "preview expired, choose the file again" state and returns to the picker — never a stale-data claim or a crash.
  </behavior>
  <action>Test pure route/error/mode helpers first. Extend BackupScreen selection handling through expo-document-picker with one cached file, then add serializable RestorePreview route data containing only token/aggregate model—not file paths, callbacks, or passphrases. Review finding (MEDIUM): a route token is safer than raw file paths/passphrases, but React Navigation can restore a RestorePreview route after process death with a token whose backing in-memory preview cache is gone — the screen must explicitly check for a missing/expired cache entry on mount and render the expired-preview recovery state (re-select from the picker), never assume the token still resolves. Implement themed preview chrome, loading/accessible announcement, default Merge CTA, retry/choose-another transitions, and the exact UI-SPEC error copy. Invoke only the validated preview service; do not call restore-apply from selection.</action>
  <verify><automated>npm test -- src/screens/backup-restore-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>A valid selection permits Merge preview only after service validation, while every pre-preview failure maintains the no-local-change promise; a test proves an unresolvable/expired preview token renders the recovery state instead of throwing or showing stale data.</acceptance_criteria>
  <done>A user can safely see one non-sensitive Merge preview end to end, including safe recovery from a process-death-orphaned preview token.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm Replace-all, apply once, and present aggregate restore result</name>
  <read_first>17-UI-SPEC.md Replace-all confirmation, restoring, success, and failure states; src/backup/restore-apply.ts; src/screens/BackupScreen.tsx; src/navigation/types.ts</read_first>
  <files>src/screens/RestorePreviewScreen.tsx, src/screens/RestoreResultScreen.tsx, src/screens/backup-restore-logic.ts, src/screens/backup-restore-logic.test.ts</files>
  <behavior>
    - Replace-all is visibly destructive and opens the exact impact Alert; it states verified pre-backup or unavailable destination.
    - Apply progress blocks Back and duplicate actions until Merge/Replace completes.
    - Result shows only added/updated/local-retained/deletions totals plus approved Replace disclosure and returns to refreshed Backup.
    - Any pre-commit/apply failure maps to the correct calm recovery copy, never an optimistic result.
    - If the app is killed by Android during the non-dismissable applying state, the next app launch does not re-run or duplicate the apply; it lands on Backup, whose health/data reflects whichever outcome actually happened (either the transaction committed, in which case the data is already correct, or it did not, in which case local data is unchanged) rather than showing a stuck "restoring" state.
  </behavior>
  <action>Use the tested mode/result helpers to implement native confirmation, non-dismissable applying state, single-flight apply dispatch, and serializable aggregate RestoreResult route. Wire Replace-all to the restore engine's preflight result; keep Merge default. On successful commit reset navigation to Backup and reload its health; on errors route back to selection/preview by typed category. Review finding (LOW): a JS-side "non-dismissable" applying state cannot make the underlying SQLite/SAF operation truly uncancellable under Android activity/process termination — do not claim otherwise in copy or code comments. Because restore-apply's outer transaction (Plan 17-08) is the sole commit boundary, a killed process during apply leaves the operation either fully committed or fully rolled back at the SQLite level with no in-between; make the UI consistent with that by never persisting an "applying" flag across app restarts — on cold start, BackupScreen must simply reflect current data/health rather than attempt to resume or re-show a restoring state, and idempotent re-selection of the same file after such an interruption is safe (validation runs fresh and Merge naturally no-ops on already-applied rows via UID/modified_at). Preserve D-14/D-15 copy, accessibility labels/states, and no per-record audit list.</action>
  <verify><automated>npm test -- src/screens/backup-restore-logic.test.ts && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>UI mode/result tests cover Merge default, explicit Replace confirmation, single-flight apply, aggregate totals, and every failure recovery classification; a cold-start test proves no persisted "applying" UI state survives an app restart.</acceptance_criteria>
  <done>Complete previewed Merge/Replace restore is reachable, reports outcomes without exposing sensitive records, and never strands the user in a stale applying state after an interrupted process.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Android document picker → restore UI | Selected file path/content metadata enters user-visible restore orchestration. |
| restore service → result route | Durable result state is condensed into navigable UI data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-25 | Tampering | picker/preview transition | high | mitigate | Only typed service validation may construct preview route data. |
| T-17-26 | Information disclosure | preview/result screens | high | mitigate | Aggregate-only route data/copy; no paths, passphrases, or record audit list. |
| T-17-27 | Denial of service | restore apply UI | medium | mitigate | Single-flight non-dismissable progress and recoverable typed error routes. |
| T-17-SC | Tampering | package installs | high | mitigate | Document picker dependency was approved in 17-01. |
</threat_model>

<verification>Run restore UI logic tests, tsc, color check; device exercise is required in 17-11.</verification>
<success_criteria>Every visible restore transition preserves the restore engine's all-or-nothing, privacy-preserving safety contract.</success_criteria>
<output>Create `.planning/phases/17-backup-export-restore/17-10-SUMMARY.md` when done</output>
