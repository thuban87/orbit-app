# Continue Here — Phase 17 / Plan 17-11 / Task 2

**Paused:** 2026-08-26
**Status:** Automated gates and final-release regression checks are green. Device UAT is materially complete, with three environment-limited manual checks still open (listed below).

## Read first

- `.planning/HANDOFF.json`
- `17-11-PLAN.md`
- `17-VALIDATION.md`
- `docs/runbooks/desktop-build-pipeline.md`
- `.planning/debug/manual-export-failure.md`
- `.planning/debug/replace-apply-failure.md`

## Exact current state

- Task 1 is complete in `f9f2273`.
- `a49b925` fixes a genuine Replace-restore bug: after confirmation, the restore screen reread a module-scoped preview cache that could expire. The fix captures the validated candidate before confirmation. Focused checks and the full local verifier passed when it landed.
- `a24bcb5` added temporary, redacted pre-restore snapshot diagnostics. They were intentionally removed in `612ea7c`; do not bring them back without a controlled reproduction.
- A prior UAT fixture changed from `Preview expired` to a generic restore failure. Source tracing shows that generic error is from creation of the pre-restore safety snapshot, before restore apply. It is not evidence of a Merge/Replace transaction failure.
- A direct fresh control was eventually run: wiped data, made a no-photo contact, encryption off, then **Backup → Export now**. It passed and opened Android's native share chooser. Therefore the prior export failure is fixture-specific or photo-related, not a baseline export defect.

## Current build and confirmed fixes

- Current clean release build for regular-phone installation passed (SHA-256 `1fbc641667a46a1c0a27b26c32f14cfd4169971f1cfdcdf223d9cbc0e1d04b49`).
- Commit `0f31f1d` fixes two UAT-proven bugs: successful Merge navigation no longer becomes `Preview expired`, and a stable `FrequencyPicker` validity callback prevents the photo-save maximum-update-depth loop.
- Commit `0af7cdb` prevents stale persisted SAF provider URIs from blanking the Android folder chooser.
- Both fixes passed in the installed final release: Merge shows `Backup restored`, and a safe HTTPS photo import saves and renders without the loop.

## UAT outcome

Completed on the persistent debug loop and/or installed final release: plaintext and encrypted export/share presentation; SAF selection and initial automatic write; encryption setup/change/future-only/disable; encrypted automatic restore plus wrong-passphrase handling; damaged/newer-schema rejection; Merge and Replace (with and without safety destination); restored-photo freshness (byte hash preserved); recency and enabled digest schedule rebuild; 320dp/accessibility; final-release Merge and photo-save regression checks.

Still open, not product failures:

1. **True persisted SAF-grant revocation:** Android shell exposes grant inspection but no safe targeted revoke command. Deleting the disposable folder left the persisted grant active. This needs an owner-operated Android provider/settings revocation.
2. **Encrypted manual-file restore:** encrypted manual export reaches the native share chooser, but this device offered no Files/save target (only Quick Share, Gmail, Drive, Discord). Saving to a disposable destination then restoring it remains owner/environment dependent.
3. **Retention/progress visual proof:** rotation could not be conclusively triggered in the time-bounded date-expired fixture because the fresh automatic write did not occur; restore apply completed before an intermediate progress frame could be captured. Automated coverage and final outcomes are green, but these two visual/device observations are not closed.

## Remaining official UAT coverage

Nothing below is fully closed by the partial checks already performed:

- Actual Merge and Replace apply/results.
- Wrong-passphrase, damaged-file, and newer-schema handling.
- Passphrase change, re-encrypt/future-only choice, and encryption disable.
- Encrypted automatic/manual round trips and retention.
- Folder loss and recovery.
- Restored-photo freshness.
- Recency plus notification/digest rebuild.
- 320dp layout, accessible controls, and progress behavior.

Partial evidence already recorded: release launch, Backup entry/health state, SAF selection and persistence, foreground health refresh, plaintext/encrypted share chooser, encrypted preview aggregate-only metadata, and configured-destination disclosure. These reduce setup work only; they do not close the scenarios above.

## Device / safety notes

- Expected Pixel serial: `1A071FDEE002BU`; always query live ADB state rather than hardcode commands.
- The user permits wiping this test app's data, disposable contacts/photos/passphrases, and a local Documents SAF folder.
- Do not push or create a PR.
- Preserve unrelated untracked files: `.planning/milestone.lock`, `orbit-home.png`, and `ui.xml`. Preserve `.planning/debug/replace-apply-failure.md`; it records the unsuccessful diagnostic branch, but do not stage it in the handoff commit.

## User-critical constraints

The prior agent consumed excessive usage through fragmented debugging and premature instrumentation. Avoid that pattern: one owner per issue, direct control first, targeted source changes only when proven, and no repeat build/UAT cycles on a stale diagnostic fixture.

## UAT iteration policy — owner directive (2026-08-26)

- Do **not** let the small release-only/native subset dictate the whole Phase 17 iteration loop. Establish and keep one debug/dev-client build with Metro hot reload for JS/UI, restore-flow, encryption-flow, picker, SAF, layout, and accessibility iteration.
- Build a new release APK only when a tested batch is ready for final package validation, or when native dependencies/configuration change. Do not rebuild it for each JS-only bug or UAT hypothesis.
- Release-only final evidence is limited to installed-release/package behavior and final Android lifecycle checks (persisted grant/recovery and notification/background behavior). The functional flow may be proven first in the persistent debug build, then spot-checked once in the clean release artifact.

### Established debug UAT loop

- Debug APK: local `./app-debug.apk`, package `com.bwales.orbit`; installed on Pixel `1A071FDEE002BU` without a data reset.
- Start/recover Metro without another APK build: `npx expo start --port 8082 --clear`; `adb reverse tcp:8081 tcp:8082`; then `adb shell am force-stop com.bwales.orbit` and launch Orbit normally. A stale cached development process once showed an unrelated `ExpoWebBrowser` error; a force-stop plus fresh launch resolved it. The current source has no `auth.ts` or Expo WebBrowser dependency.
