---
phase: 08-dashboard-never-contacted-screen
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContactCard.tsx
  - src/components/FilterChipRow.tsx
  - src/stores/dashboard-prefs-store.ts
autonomous: true
requirements: [DASH-02, DASH-03, DASH-07]
must_haves:
  truths:
    - "ContactCard renders the LOCKED content contract: Avatar (recyclingKey=contactId + cacheBust=modified_at), a status ring keyed to the band (token-clean placeholder + accessibilityLabel naming the status), name (one line), the ranked fuel line OR a search snippet, a category label (hidden when null), and a favourite marker (favourite_rank IS NOT NULL) — nothing log-derived."
    - "The card carries the LOCKED testIDs and an accessibilityLabel naming its status so uiautomator UAT asserts it without colour."
    - "FilterChipRow renders a single-active chip row (all / needs-attention / category / battery / favourites / snoozed), active = accent/borderStrong, inactive = surface/border, the snoozed chip carries its count."
    - "dashboard-prefs-store persists the last-used sort + filter across launches via Zustand persist over AsyncStorage (copying theme-store)."
  artifacts:
    - src/components/ContactCard.tsx
    - src/components/FilterChipRow.tsx
    - src/stores/dashboard-prefs-store.ts
  key_links:
    - "ContactCard reuses the existing Avatar + RankedFuelLine unchanged; every colour resolves through useTheme().colors.* — no invented hex (check:colors)."
    - "The status-ring band colours (stable/wobble/decay) do NOT exist as tokens — ship a token-clean placeholder; the coloured-band variant is a FLAGGED owner decision (OD-1)."
---

<objective>
Build the three dashboard UI primitives as decoupled, presentational units (explicit props — no DB, no navigation): `ContactCard.tsx` (the LOCKED card content contract shared by the dashboard and never-contacted screens), `FilterChipRow.tsx` (the single-active chip control), and `dashboard-prefs-store.ts` (persisted sort+filter). These are the reusable pieces Plans 05/07/09 assemble.

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

Purpose: get the card contract + chips + persistence right once, so the screens that consume them stay thin.
Output: `ContactCard.tsx`, `FilterChipRow.tsx`, `dashboard-prefs-store.ts`.

FLAGGED owner decisions surfaced here (owner's bucket — do NOT invent hexes or binding taste to close them):
- OD-1 status-ring band tokens (stable/wobble/decay): none exist; ship a token-clean placeholder (shape/opacity using existing tokens + an accessibilityLabel naming the status). The coloured-band variant is the owner's.
- OD-2 favourite marker: provisional `accent` star, flagged.
- OD-4 category-label chip: `surfaceElevated` + `textSecondary` (no new token), flagged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md
@.planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md
@src/components/Avatar.tsx
@src/components/RankedFuelLine.tsx
@src/components/FuelSearchResultRow.tsx
@src/stores/theme-store.ts
@src/db/contact-status-read.ts
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan: `src/components/ContactCard.tsx` → `ContactCard`, `ContactCardProps`; `src/components/FilterChipRow.tsx` → `FilterChipRow`, `FilterChip`; `src/stores/dashboard-prefs-store.ts` → `useDashboardPrefs`.)

<tasks>

<task type="auto">
  <name>Task 1: ContactCard.tsx — the LOCKED shared card content contract</name>
  <files>src/components/ContactCard.tsx</files>
  <read_first>
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Card Content Contract (LOCKED), § Status Ring, § Accessibility & testIDs, § Owner Decisions Required (items 1, 2, 4). The card fixes WHAT it carries, not HOW it is arranged (arrangement = owner's design pass).
    - src/components/Avatar.tsx — props (photo, name, contactId, size, cacheBust) and the recyclingKey correctness wiring already in place.
    - src/components/RankedFuelLine.tsx — the surface-agnostic one-line fuel render (takes a plain string; renders nothing when empty).
    - src/components/FuelSearchResultRow.tsx — the name↔snippet stack idiom (2px gap, minHeight 44, tail-ellipsis) to reuse for the fuel-match snippet variant.
    - src/db/contact-status-read.ts — `ProfileStatus` (stable|wobble|decay|rogue) type for the status prop.
    - src/theme/theme-presets.ts — token inventory: `rogue` exists; there is NO stable/wobble/decay band token, and `surfaceElevated`/`textSecondary`/`accent`/`border`/`borderStrong` exist. Do NOT add tokens here.
  </read_first>
  <acceptance_criteria>
    - `ContactCardProps` is explicit (no DAO row import): contactId, name, photo (string|null), modifiedAt (string|undefined), status (ProfileStatus|null), categoryLabel (string|null), isFavourite (boolean), fuelText (string|null), snippet (string|null), onPress.
    - Renders Avatar with `contactId={contactId}` and `cacheBust={modifiedAt}` (recyclingKey correctness); name with `numberOfLines={1}`; when `snippet` is non-null renders the snippet (FuelSearchResultRow idiom) else renders `<RankedFuelLine text={fuelText} />` — the DAO (08-01) sets `snippet` non-null whenever the fuel text matches the term, INCLUDING a contact matching both name and fuel (review MEDIUM-6), so the card's rule stays simply "snippet present → show snippet"; the category label is hidden when `categoryLabel` is null; a favourite marker shows when `isFavourite`.
    - Full-row Pressable → `onPress`; `minHeight: 44`; NO long-press binding (reserved for 03-fuel).
    - testIDs present exactly: `dashboard-card-{contactId}`, `dashboard-card-name-{contactId}`, `dashboard-card-fuel-{contactId}`, `dashboard-card-category-{contactId}`, `dashboard-card-status-{contactId}`, `dashboard-card-favourite-{contactId}`; the status element carries an `accessibilityLabel` naming the status (e.g. "Needs attention"/"Stable"/"Rogue"; null status → a neutral label for never-contacted).
    - Status ring is token-clean: rogue uses `colors.rogue`; stable/wobble/decay use existing tokens differentiated by shape/opacity (NO new hex). A code comment marks the coloured-band variant as owner decision OD-1.
    - `npm run check:colors` passes (no hex literal outside theme-presets.ts); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <action>
    Create `ContactCard` as a purely presentational component (no DB read, no getExecutor, no useNavigation — the caller wires onPress). Take the explicit `ContactCardProps` above. Compose the LOCKED contract from UI-SPEC using ONLY existing reusable components and theme tokens: `Avatar` (pass contactId + cacheBust=modifiedAt for the anti-face-flash recyclingKey), the name (`numberOfLines={1}`, tail ellipsis, Body 15/400 textPrimary), the fuel region (snippet when a fuel-match, else RankedFuelLine), a category chip on `surfaceElevated` with `textSecondary` (hidden when null — OD-4 flagged), and a favourite star marker in `accent` when isFavourite (OD-2 flagged, provisional). Render the status ring as a token-clean placeholder: use `colors.rogue` for rogue and existing tokens (border/textSecondary) with shape/opacity for the three bands; attach the naming accessibilityLabel and the `dashboard-card-status-{contactId}` testID. Add a header comment: the card carries nothing log-derived (no recency string, no "N days ago", no channel glyph, no gravity/intensity/quality — profile-only), and the band-colour tokens are FLAGGED owner decision OD-1 (executor must not invent hexes). Keep arrangement simple and clearly owner-adjustable (reference layout is not binding).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>ContactCard renders the LOCKED contract with correct testIDs, recyclingKey wiring, token-clean status ring, and the flagged owner-decision placeholders, with no colour literal.</done>
</task>

<task type="auto">
  <name>Task 2: FilterChipRow.tsx — single-active chip control</name>
  <files>src/components/FilterChipRow.tsx</files>
  <read_first>
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Interaction States (Filter chips row), § Color (accent reserved list), § Accessibility & testIDs (`dashboard-filter-chip-{key}`). Single active filter in v1 (not multi-select); the snoozed chip carries its count.
    - .planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md "No Analog Found" (FilterChipRow) — build from RN Pressable/ScrollView; active = accent/borderStrong, inactive = surface/border/textSecondary label.
    - src/db/dashboard-read.ts (from Plan 01) — the `DashboardFilter` union the chip keys mirror. (If not yet merged, mirror the union: all | needs-attention | category-{id} | battery-{value} | favourites | snoozed.)
  </read_first>
  <acceptance_criteria>
    - Explicit props: the list of chips to render (key + label, with an optional count for snoozed/favourites), the active key, and an onSelect callback. No DB, no navigation.
    - A horizontal scrollable row of Pressables; the active chip uses `colors.accent`/`colors.borderStrong`, inactive uses `colors.surface`/`colors.border` with a `textSecondary` label; each chip has `minHeight: 44`.
    - Each chip carries `testID="dashboard-filter-chip-{key}"`, `accessibilityRole="button"`, and an accessibilityLabel; the snoozed chip label includes its count.
    - Single-active selection (tapping a chip calls onSelect with its key; the parent owns the active state).
    - `npm run check:colors` and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <action>
    Create `FilterChipRow` as a presentational horizontal chip control built from RN `ScrollView` (horizontal) + `Pressable`. Take an explicit chip list (key/label/optional count), the active key, and `onSelect`. Style active vs inactive strictly through theme tokens per UI-SPEC (accent/borderStrong active; surface/border/textSecondary inactive). Emit the LOCKED `dashboard-filter-chip-{key}` testIDs and accessibility labels; render the snoozed/favourites counts in their labels. Keep single-active semantics (the parent, Plan 09, holds active state + wires categories/batteries into the chip list). No DB or navigation here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>FilterChipRow is a token-clean, single-active, testID-complete chip control ready for the dashboard controls plan.</done>
</task>

<task type="auto">
  <name>Task 3: dashboard-prefs-store.ts — persisted sort + filter (Zustand persist over AsyncStorage)</name>
  <files>src/stores/dashboard-prefs-store.ts</files>
  <read_first>
    - src/stores/theme-store.ts — the EXACT persist + createJSONStorage(AsyncStorage) + partialize + onRehydrateStorage shape to copy verbatim.
    - .planning/phases/08-dashboard-never-contacted-screen/08-RESEARCH.md § Pattern 1 (default sort=status, filter=all) + § Code Examples (dashboard-prefs store) + Architectural Responsibility Map (AsyncStorage, not a SQLite row).
    - src/db/dashboard-read.ts (from Plan 01) — the `DashboardSort`/`DashboardFilter` types to store. (If not yet merged, mirror them.)
  </read_first>
  <acceptance_criteria>
    - `useDashboardPrefs` exposes `sort` (default "status"), `filter` (default "all"), `setSort`, `setFilter`.
    - Persisted with `name: "orbit-dashboard-prefs"`, `createJSONStorage(() => AsyncStorage)`, `version: 1`, `partialize` persisting only { sort, filter }, and an onRehydrateStorage warn-on-error handler — mirroring theme-store.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <action>
    Create `dashboard-prefs-store.ts` by copying the `theme-store.ts` Zustand `persist` pattern verbatim and adapting: state `{ sort: DashboardSort ("status" default), filter: DashboardFilter ("all" default) }` with `setSort`/`setFilter`; persist config name `"orbit-dashboard-prefs"`, AsyncStorage JSON storage, version 1, partialize to { sort, filter }, warn-on-rehydrate-error. This is a device-local UI preference, not contact data — no SQLite row, no migration.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>dashboard-prefs-store persists the last-used sort+filter across launches following the shipped theme-store pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered card → glanceable surface | The card must not surface log-derived or private data; it renders only what the caller passes (which the DAO already scoped). |
| Persisted prefs → AsyncStorage | A device-local, non-sensitive UI preference (no contact data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Information disclosure | ContactCard render | medium | mitigate | The card renders only the scoped props from dashboard-read (off_limits/ai already excluded in-query); it performs no DB read and no `.filter()` that could resurface private data; nothing log-derived is rendered. |
| T-08-11 | Information disclosure | dashboard-prefs persistence | low | accept | Only sort+filter enum values are persisted (partialize) — no contact content; local AsyncStorage, no network. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run check:colors` clean (no hex outside theme-presets.ts).
- Card, chip row, and store follow the shipped Avatar/RankedFuelLine/theme-store patterns.
- On-device UAT of the card contract is deferred to the end-of-phase Pixel pass (Plans 07/09).
</verification>

<success_criteria>
- The three primitives exist, are token-clean, carry the LOCKED testIDs, and surface the flagged owner decisions without inventing hexes.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-04-SUMMARY.md` when done.
</output>
