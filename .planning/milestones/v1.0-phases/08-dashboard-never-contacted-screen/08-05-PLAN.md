---
phase: 08-dashboard-never-contacted-screen
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-04"]
files_modified:
  - src/screens/NeverContactedScreen.tsx
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [DASH-04, DASH-01]
must_haves:
  truths:
    - "The Not-yet-contacted screen lists the inverse population (never-contacted, non-archived) using the shared ContactCard, renders each contact's fuel line, and offers all three sorts (Oldest added default / Newest added / Name A–Z)."
    - "The screen re-queries via useFocusEffect with an async cancelled-flag guard; it renders offline with a calm 'you've reached everyone' empty state."
    - "The NeverContacted route is registered on the native stack and reachable; the dashboard's Not-yet-contacted entry (Plan 07) pushes it."
  artifacts:
    - src/screens/NeverContactedScreen.tsx
  key_links:
    - "Reuses listNeverContacted (Plan 01) + ContactCard (Plan 04); mirrors ArchivedContactsScreen chrome; async-only reads."
    - "types.ts gains NeverContacted: undefined; RootNavigator registers the screen — the first of the two nav registrations this phase adds."
---

<objective>
Build the "Not yet contacted" sibling screen (domain 8 owns it) and register its route. It reuses the shared `ContactCard` and the `listNeverContacted` read (Plan 01), offers its own three-way sort (default Oldest added), refreshes on focus, and ships a calm empty state.

Phase goal (MVP user story): **As a** person keeping up with my people, **I want to** open Orbit to a home screen showing who needs attention — sorted, filterable, searchable, with birthdays and favourites — **so that** I can see at a glance who to reach out to and act without hunting.

After this plan: a real "Not yet contacted" screen exists and is reachable on the stack (the dashboard footer entry that pushes it lands in Plan 07).
Output: `NeverContactedScreen.tsx`, plus the NeverContacted route in types.ts + RootNavigator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md
@.planning/phases/08-dashboard-never-contacted-screen/08-PATTERNS.md
@src/screens/ArchivedContactsScreen.tsx
@src/screens/FuelSearch.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
@src/db/dashboard-read.ts
@src/components/ContactCard.tsx
</context>

## Artifacts this phase produces

(See 08-01-PLAN.md for the full phase list. This plan: `src/screens/NeverContactedScreen.tsx` → `NeverContactedScreen`; adds `NeverContacted: undefined` to `RootStackParamList`.)

<tasks>

<task type="auto">
  <name>Task 1: NeverContactedScreen.tsx — inverse-population list with its own sort + focus refresh</name>
  <files>src/screens/NeverContactedScreen.tsx</files>
  <read_first>
    - src/screens/ArchivedContactsScreen.tsx — the EXACT chrome to mirror: ScrollView/FlatList root over `colors.background`, header with a `goBack` Back control (borderColor colors.border, textSecondary), 24/700 title, useCallback load via getExecutor, empty state.
    - src/screens/FuelSearch.tsx (lines 50-64) — the cancelled-flag async guard to reuse for the load.
    - src/db/dashboard-read.ts — `listNeverContacted(exec, { sort })` and `NeverContactedSort` ("oldest" default / "newest" / "name").
    - src/components/ContactCard.tsx — the shared card (status null for never-contacted; still renders the fuel line).
    - .planning/phases/08-dashboard-never-contacted-screen/08-UI-SPEC.md § Copywriting Contract (title "Not yet contacted", empty "You've reached everyone — No one is waiting", sort labels "Oldest added"/"Newest added"/"Name (A–Z)") + § Accessibility (`never-contacted-root`, `never-contacted-sort-control`, `never-contacted-card-{contactId}`, `never-contacted-empty`).
  </read_first>
  <acceptance_criteria>
    - Root carries `testID="never-contacted-root"` and `backgroundColor: colors.background`; its own Back chrome (native-stack headerShown:false convention).
    - A sort control (`never-contacted-sort-control`) cycles/sets the three sorts; default is "oldest" (Oldest added); the list re-queries on sort change.
    - Renders one `ContactCard` per row (testID `never-contacted-card-{contactId}` on the row wrapper), each showing the fuel line; tapping a card navigates to `Profile { contactId }`.
    - Load uses `getExecutor()` + `listNeverContacted` with a `cancelled`-flag async guard in `useFocusEffect`; async reads only (no `...Sync`).
    - Empty state `never-contacted-empty` shows the calm copy; renders with no network.
    - `npx tsc --noEmit` and `npm run check:colors` clean.
  </acceptance_criteria>
  <action>
    Create `NeverContactedScreen` typed `RootStackScreenProps<"NeverContacted">`, mirroring ArchivedContactsScreen's chrome. Hold local state for `rows` and the active `NeverContactedSort` (default "oldest"). Load via `listNeverContacted(getExecutor(), { sort })` inside `useFocusEffect(useCallback(...))` with a cancelled-flag guard (FuelSearch pattern); re-run when sort changes. Render a small sort control (three options with the UI-SPEC labels) and a FlatList of `ContactCard` (pass the row's id/name/photo/modifiedAt, status null, categoryLabel, isFavourite from favourite_rank, fuelText, snippet null; onPress → `navigation.navigate("Profile", { contactId })`). Show the calm empty state when there are no rows. Every colour via `useTheme().colors.*`. No writer, no network, async reads only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
  </verify>
  <done>NeverContactedScreen lists the inverse population with three sorts, focus refresh, offline render, the calm empty state, and card taps to the profile.</done>
</task>

<task type="auto">
  <name>Task 2: Register the NeverContacted route (types.ts + RootNavigator)</name>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>
    - src/navigation/types.ts (lines 20-46) — `RootStackParamList` is a `type` alias (needs the index signature); add the new route as `undefined`.
    - src/navigation/RootNavigator.tsx (lines 48-64) — the `<Stack.Screen>` registration idiom; `initialRouteName="Home"` stays.
  </read_first>
  <acceptance_criteria>
    - `RootStackParamList` gains `NeverContacted: undefined` (FuelSearch stays for now — its retirement is Plan 10).
    - RootNavigator imports `NeverContactedScreen` and registers `<Stack.Screen name="NeverContacted" component={NeverContactedScreen} />`.
    - `initialRouteName="Home"` unchanged; `npx tsc --noEmit` clean; the app still typechecks with FuelSearch present.
  </acceptance_criteria>
  <action>
    Add `NeverContacted: undefined;` to `RootStackParamList` in types.ts. In RootNavigator.tsx, import `NeverContactedScreen` and add its `<Stack.Screen>` entry. Leave the FuelSearch route intact (Plan 10 retires it, after the dashboard search box exists). Do NOT change `initialRouteName`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The NeverContacted route is registered and reachable; the dashboard footer entry (Plan 07) can navigate to it and typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Never-contacted list read → screen | Reuses the in-query scoped read; the risk is surfacing archived/private data or a blocking network read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Information disclosure | listNeverContacted read | medium | mitigate | Predicate is `archived_at IS NULL AND last_contact IS NULL` (archived excluded); fuel line via the in-query-excluded ranked subquery; async on-device read, no network. |
| T-08-13 | Information disclosure | read path (offline) | medium | mitigate | Async SQLite reads only; ContactCard makes zero network calls for file:// avatars; local-first. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run check:colors` clean.
- On-device UAT (Pixel): the Not-yet-contacted screen lists never-contacted people with fuel, sorts three ways, and shows the empty state — verified at the end-of-phase pass.
</verification>

<success_criteria>
- The Not-yet-contacted screen exists, is route-registered and reachable, reuses the shared card + inverse read, and refreshes offline on focus.
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-never-contacted-screen/08-05-SUMMARY.md` when done.
</output>
