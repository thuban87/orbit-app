---
phase: 04-contact-crud-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - package.json
  - src/components/FrequencyPicker.tsx
  - src/components/TriStateLastSpoke.tsx
autonomous: true
requirements: [CRUD-01]
must_haves:
  truths:
    - "A user picks one of 7 frequency presets OR enters a custom 'every N' interval with a days/weeks/months unit, and the component emits a positive-integer interval_days."
    - "A non-positive / non-integer custom interval is rejected at entry with the locked copy 'Enter a whole number greater than 0.'"
    - "The last-spoke control is a tri-state Today / Pick date / Not yet; 'Not yet' is visually muted (not accent-filled); a future 'Pick date' is rejected at entry."
  artifacts:
    - "src/components/FrequencyPicker.tsx — presets + custom every-N → interval_days"
    - "src/components/TriStateLastSpoke.tsx — the Today/Pick date/Not yet segmented control + native date picker"
  key_links:
    - "FrequencyPicker maps preset labels through FREQUENCY_DAYS (src/types.ts) — no new constants"
    - "TriStateLastSpoke uses @react-native-community/datetimepicker and formats via formatLocalDate (never toISOString)"
    - "both components resolve every colour through useTheme().colors.* (check:colors)"
---

## Phase Goal

**As a** person tending my relationships, **I want to** add the people I track with how often I want to reach them and when we last spoke, **so that** the app can tell me who is drifting.

**This slice (Plan 03):** the two bespoke create-form inputs + the native modules they need. After this plan the FrequencyPicker and TriStateLastSpoke components exist and emit clean values; Plan 04 assembles them into the CreateContactScreen.

<objective>
Install the two native input modules (`@react-native-community/datetimepicker`, `@react-native-picker/picker` — both Approved in RESEARCH's Package Legitimacy Audit) and build the two form controls the 7-type custom `FieldType` union cannot express: the frequency picker (presets + custom "every N") and the tri-state last-spoke control.

Purpose: CRUD-01's frequency (incl. custom every-N → `interval_days`) and CRUD-02's tri-state last-spoke are the fixed-column widgets RESEARCH Pattern 1 says must be hand-built alongside the reused custom-field block.
Output: `FrequencyPicker`, `TriStateLastSpoke`, and the two installed native deps (require a desktop rebuild before device UAT).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-crud-lifecycle/04-UI-SPEC.md
@.planning/phases/04-contact-crud-lifecycle/04-RESEARCH.md
@.planning/phases/04-contact-crud-lifecycle/04-PATTERNS.md
@src/types.ts
@src/components/field-widgets/TextFieldWidget.tsx
@src/components/field-widgets/DropdownFieldWidget.tsx
@src/utils/dates.ts
</context>

## Artifacts this plan produces

- `src/components/FrequencyPicker.tsx` — `FrequencyPicker({ value: number; onChange: (intervalDays: number) => void })`; renders the 7 presets (from `FREQUENCY_DAYS`) plus a "Custom…" affordance revealing an integer input + a days/weeks/months unit toggle (×1 / ×7 / ×30). Exposes a validation state for the "Enter a whole number greater than 0." message.
- `src/components/TriStateLastSpoke.tsx` — `TriStateLastSpoke({ value, onChange })` where the emitted value models one of `{ kind: "today" } | { kind: "date"; date: string } | { kind: "not-yet" }`; "Pick date" opens the native date picker and rejects a future date at `onChange` with the locked copy.
- Modified `package.json` (+ `@react-native-community/datetimepicker`, `@react-native-picker/picker`).

<tasks>

<task type="auto">
  <name>Task 1: Install native input modules</name>
  <files>package.json</files>
  <read_first>
    - RESEARCH.md "Standard Stack → Supporting (NEW)" + "Package Legitimacy Audit" (both packages Approved — first-party RN community, no postinstall; no checkpoint needed)
    - RESEARCH.md "Pitfall 5: Native modules not rebuilt"
  </read_first>
  <action>
    Run `npx expo install @react-native-community/datetimepicker @react-native-picker/picker` (NOT bare `npm install` — expo pins the SDK-57-compatible versions: datetimepicker 9.1.0, picker 2.11.4). Both are native modules and will red-screen on a JS-only reload; they require a desktop-pipeline rebuild (docs/runbooks/desktop-build-pipeline.md) before device UAT — the owner runs it when testing the phase on the Pixel. Add nothing else.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d=p.dependencies;if(!d['@react-native-community/datetimepicker']||!d['@react-native-picker/picker'])throw new Error('picker deps missing');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `package.json` dependencies include both packages at expo-resolved SDK-57 versions.
    - Behavior: the node assertion prints `ok`.
  </acceptance_criteria>
  <done>Both native input modules are in package.json (rebuild deferred to the phase-gate desktop build).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FrequencyPicker</name>
  <files>src/components/FrequencyPicker.tsx</files>
  <read_first>
    - src/types.ts (FREQUENCY_DAYS 17-25 — Daily=1 … Yearly=365; the mapping to reuse, no new constants)
    - src/components/field-widgets/TextFieldWidget.tsx (input styling verbatim: radius 8, pad 12/10, size 15, surface bg, border) and DropdownFieldWidget.tsx (Pressable-list selection pattern)
    - UI-SPEC.md "Component Inventory → Frequency picker" + "Copywriting Contract → invalid custom interval"
  </read_first>
  <behavior>
    - Selecting a preset emits its FREQUENCY_DAYS value (e.g. "Monthly" → 30).
    - Choosing "Custom…" reveals an integer TextInput + a days/weeks/months unit toggle; entering N with a unit emits N×{1,7,30}.
    - Entering 0, a negative, a blank, or a non-integer surfaces the inline message "Enter a whole number greater than 0." and does NOT emit a value.
  </behavior>
  <action>
    Build `FrequencyPicker` as a controlled component. Render the 7 presets as selectable rows/chips (copy the DropdownFieldWidget Pressable-selection shape; selected = accent per UI-SPEC "Accent reserved for → selected state"). Add a "Custom…" option that reveals a `keyboardType="number-pad"` TextInput (TextFieldWidget styling) plus a three-way unit toggle (days ×1 / weeks ×7 / months ×30). Compute `interval_days = Number(n) * unitFactor`; validate `Number.isInteger(Number(n)) && Number(n) > 0` — on failure show the locked copy "Enter a whole number greater than 0." emphasised with `colors.danger` (the token now lands in Plan 01/wave 1 per the review — this plan depends on 04-01, so `useTheme().colors.danger` is available; do NOT inline a hex, do NOT fall back to textSecondary+borderStrong). Emit `onChange(interval_days)` only when valid; also expose the current validity to the parent (a `valid: boolean` in the emitted state or a sibling callback) so CreateContactScreen (Plan 04) can block Save while the custom entry is invalid. Every colour via `useTheme().colors.*`; every control has `testID` + `accessibilityLabel`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/components/FrequencyPicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: preset values come from `FREQUENCY_DAYS`, not re-declared numbers.
    - Source: the invalid-interval message string is exactly "Enter a whole number greater than 0.".
    - Behavior: typecheck + `check:colors` on the file exit 0.
  </acceptance_criteria>
  <done>FrequencyPicker emits a positive-integer interval_days for presets and custom every-N; invalid entry blocked with the locked copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TriStateLastSpoke</name>
  <files>src/components/TriStateLastSpoke.tsx</files>
  <read_first>
    - PATTERNS.md "No Analog Found → TriStateLastSpoke" (build fresh: three Pressable segments, selected accent EXCEPT "Not yet" muted, 44px min segment height)
    - UI-SPEC.md "Screen & Interaction Contracts → Create-contact form" (tri-state default Today; Not yet muted; future date rejected) + "Copywriting Contract → future last-spoke date"
    - src/utils/dates.ts (formatLocalDate — the ONLY date formatter; never toISOString)
    - RESEARCH.md "Code Examples → Native date picker"
  </read_first>
  <behavior>
    - Three segments Today / Pick date / Not yet. PURELY CONTROLLED: the rendered selection reflects the `value` prop — there is NO internal default that overrides it. Each consumer supplies its own initial state (create → Today; edit-of-never-contacted (Plan 06) → Not yet).
    - "Not yet" renders visually muted (textSecondary text + borderStrong border, NOT accent fill).
    - "Pick date" opens the native datetimepicker; a chosen date emits { kind: "date", date: formatLocalDate(chosen) }.
    - A future date is rejected at onChange with the locked copy "That date is in the future. Pick today or earlier." and no value is emitted.
  </behavior>
  <action>
    Build `TriStateLastSpoke` as three `Pressable` segments (min height 44px, `hitSlop` to reach target). Selected non-"Not yet" segment uses accent fill; the "Not yet" segment, even when selected, stays muted (`textSecondary` + `borderStrong`) so the backlog case reads honestly (UI-SPEC) — muting is deliberately NOT `danger` (Not-yet is a valid, non-error choice). Make the component PURELY CONTROLLED: derive the selected segment from the `value` prop and report every change via `onChange` (the parent owns the state); do NOT hold an internal default that overrides the prop. Each consumer seeds its own initial `value` — CreateContactScreen (Plan 04) passes `{ kind: "today" }`; the edit form's never-contacted path (Plan 06) passes `{ kind: "not-yet" }` — TriStateLastSpoke itself imposes no default. On "Pick date", show `@react-native-community/datetimepicker` (Android imperative `mode="date"`); in its `onChange`, if the picked `Date` is after today reject with the exact copy "That date is in the future. Pick today or earlier." shown in `colors.danger` (the validation-error token, available from Plan 01/wave 1) and keep the prior selection; otherwise emit `{ kind: "date", date: formatLocalDate(picked) }`. Emit `{ kind: "today" }` / `{ kind: "not-yet" }` for the other segments. Never use `toISOString()`. Every colour via theme tokens; `testID` + `accessibilityLabel` per segment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors src/components/TriStateLastSpoke.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: dates format through `formatLocalDate`; no `toISOString` anywhere in the file.
    - Source: the "Not yet" segment style never uses `colors.accent` as its fill.
    - Source: TriStateLastSpoke is purely controlled — the selected segment derives from the `value` prop with no internal default that overrides it; consumers pass the initial state.
    - Source: future-date copy is exactly "That date is in the future. Pick today or earlier.".
    - Behavior: typecheck + `check:colors` on the file exit 0.
  </acceptance_criteria>
  <done>Tri-state control emits today/date/not-yet, mutes "Not yet", and rejects future dates at entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Two native packages enter the supply chain (both pre-audited Approved) |
| user input → interval/date | Numeric/date entry that later drives status math |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | picker/datetimepicker installs | high | mitigate | Both packages verified Approved in RESEARCH's Package Legitimacy Audit (first-party RN community, no postinstall); `expo install` pins versions |
| T-04-05 | Tampering (data corruption) | custom interval / future date | medium | mitigate | interval validated `Number.isInteger && > 0` at entry (defence-in-depth ahead of the recency-dao write chokepoint); future last-spoke rejected at entry so a future occurred_at can never pin a contact `stable` |

No colour literal permitted (check:colors); no network/auth surface.
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npm run check:colors` green on both component files.
- Native modules require the phase-gate desktop rebuild before device UAT (VALIDATION.md manual-only) — component logic fast-refreshes but the native picker needs the rebuild.
- Device UAT (phase gate): frequency presets + custom every-N produce the right interval; tri-state shows muted "Not yet"; future date rejected.
</verification>

<success_criteria>
FrequencyPicker and TriStateLastSpoke exist, emit clean values, honour the locked copy + muting rules, and pass typecheck + colour gate; both native input deps are installed.
</success_criteria>

<output>
Create `.planning/phases/04-contact-crud-lifecycle/04-03-SUMMARY.md` when done.
</output>
