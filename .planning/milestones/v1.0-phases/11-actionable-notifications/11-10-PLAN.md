---
phase: 11-actionable-notifications
plan: 10
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-04, 11-05, 11-06]
files_modified:
  - src/services/notifications/notification-schedule.ts
  - src/services/notifications/notification-schedule.test.ts
autonomous: true
requirements: [NOTIF-01, NOTIF-04]
user_setup: []

must_haves:
  truths:
    - "reconcileSchedule PRE-SCHEDULES the next FUTURE occurrence for every eligible (non-suppressed) contact whose next fire instant is WITHIN a bounded HORIZON_DAYS window — not only currently-overdue ones — at max(dueDate, snooze_until) morning plus the weekly re-nag tick, and the next day-of morning for every non-archived birthday within the horizon, so an alert fires even if the app is never reopened (NOTIF-01) WITHOUT exceeding Android's pending-notification cap."
    - "The SCHEDULED set is HARD-BOUNDED: after horizon filtering, candidates are sorted by fire instant and only the soonest MAX_SCHEDULED_NOTIFICATIONS are scheduled, so the OS pending-notification cap can never SILENTLY DROP a schedule (item A); the launch/foreground reconcile rolls the horizon forward so far-future contacts are armed as they approach."
    - "reconcileSchedule diffs the FULL desired request (fire instant at HOUR granularity, channel, category, data, AND content body/title) against each currently-scheduled request and CANCELS+RESCHEDULES under the same identifier on ANY mismatch — so a delivery-hour / quiet-window / lock-screen-channel change, or a contact RENAME, actually applies to already-pending notifications (no privacy regression, no stale name; items C+F)."
    - "Every scheduled decay notification carries a generic body, the decay_actions category, autoDismiss, the private/public channel per the lockscreen setting, and {kind:'decay',contactId,occurrenceKey} data; birthday notifications carry the birthday channel and {kind:'birthday',contactId,occurrenceKey} data with no actions."
    - "When the master or a per-type toggle is off, that type is not scheduled (and its stale identifiers are cancelled)."
    - "registerNotificationScheduleSweep pushes ONE idempotent hook onto the launch-sweep registry and runs nothing at import."
  artifacts:
    - src/services/notifications/notification-schedule.ts
    - src/services/notifications/notification-schedule.test.ts
  key_links:
    - "Composes notification-read (eligible candidates + snooze base) + fire-instant (timing) + birthday-logic (next occurrence) + app-settings (gates/hours) + notification-ids (identifiers/copy/channels/occurrence data) + channels; registers on launch-sweep."
---

<!-- REVIEW-CYCLE-1 (11-REVIEWS.md) — H5 + H3:
     H5 (item 5): schedule FUTURE occurrences. The old reconcile scheduled only contacts already in
        [WOBBLE_MAX,ROGUE_K) and day-of birthdays, so a contact crossing overdue — or a birthday next
        week — while the app was CLOSED never fired. Now it reads listDecayEligibleCandidates (all
        non-suppressed, with snooze_until) and computes each fire base = max(dueDate, snooze_until),
        plus the next day-of birthday for every non-archived birthday.
     H3 (item 3): diff the FULL desired request (fire instant, channel, category, data), not identifier
        presence, and cancel+reschedule on mismatch — else delivery-hour/quiet-window/lock-screen edits
        never reach already-pending notifications (a privacy regression when re-privatising).
     Also mints data.occurrenceKey (the fire date) so 11-07's actionUid dedup is per-occurrence. -->

<!-- REVIEW-CYCLE-2 (11-REVIEWS.md) — items A, C, E, F (all consequences of the H5 pre-scheduling fix):
     A (HIGH): BOUND the pre-scheduled set. H5 made the eligible set the whole address book, but
        Android's pending-notification cap SILENTLY DROPS schedules past it (un-repairable local DB).
        Add top-of-file tunables HORIZON_DAYS (only pre-schedule fires within N days) and
        MAX_SCHEDULED_NOTIFICATIONS (a hard soonest-N cap below the platform pending limit). The
        reconcile filters to within-horizon fires, sorts by fire instant, and schedules the soonest
        MAX; the launch/foreground reconcile rolls the horizon forward so far-future contacts get
        armed as they approach. Pixel-UAT at ~200+ contacts asserting NO schedule is silently dropped.
     C (MED): stagger the fire minute by a DETERMINISTIC function of contactId (staggerFor(contactId)),
        NOT the array index — so a contact's fire-minute is stable across reconciles (no churn). And
        requestsEqual compares the fire instant at HOUR granularity (catches a delivery-hour/quiet-
        window change, invariant to the per-contact stagger minutes).
     E (MED): a malformed birthday makes daysUntilBirthday() return null → today+null is an Invalid
        Date (NOT a throw), which a per-candidate try/catch may not catch. SKIP the birthday candidate
        when daysUntilBirthday() is null.
     F (LOW): include content.body (+ title) in requestsEqual so a RENAMED contact's frozen generic
        body refreshes (else it stays stale until the fire instant/occurrenceKey changes).
     G (LOW, confirm intent) [DEFERRED — owner call]: an overdue-while-closed contact's first
        post-reopen nudge can be up to ~6 days out (the next weekly grid tick) instead of the next
        morning. NOT changed — the stateless "clamp to today" fix daily-nags under frequent reopens,
        violating the DECIDED flat-weekly cadence + anti-nag posture; a correct catch-up needs stored
        last-notified state the design deliberately omits. Full rationale in 11-05's cycle-2 comment;
        surfaced to the owner. -->

<objective>
Build the scheduling engine: `notification-schedule.ts` with the tunables at top, the `reconcileSchedule(exec)` FULL-REQUEST diff (cancel stale / cancel+reschedule on mismatch / schedule new, keyed on the stable per-contact identifier), and `registerNotificationScheduleSweep(getExec)` that installs the reconcile as a launch-sweep hook. This is the heart of NOTIF-01 (decay, pre-scheduled) and the scheduling half of NOTIF-04 (birthday, next-occurrence).

Purpose: launch/foreground is the source of truth for the schedule; this reconcile computes the desired FUTURE set from live data + settings and makes the OS's scheduled set match it exactly — generically-bodied, replace-not-stack, and pre-scheduled so a decaying/birthday contact fires without the app being reopened.
Output: `notification-schedule.ts` (+ test).

## Cycle-3 Review Fix — folded into execution (owner-approved 2026-08-16)

Incorporates the cycle-3 review's HIGH + the birthday-priority actionable (see 11-REVIEWS.md
§ "Orchestrator Merged Result — Cycle 3"). Apply BOTH inside Task 1 (`notification-schedule.ts`) and
add the two tests to Task 2.

1. **[HIGH — reconcile serialization race] `reconcileSchedule` MUST be self-coordinating (module-level
   DEFER-ONE), mirroring `src/services/launch-sweep.ts:49-90 (running / pendingRerun)`.** Multiple
   fire-and-forget callers exist (the launch-sweep hook here + the in-app callers in 11-09
   edit-save/profile-snooze/clear and 11-11 settings changes). Two `reconcileSchedule` invocations must
   NOT interleave — a stale in-flight reconcile that read the DB BEFORE a snooze/mute commit could
   otherwise finish AFTER a newer one and RE-ARM the cancelled notification (NOTIF-03 violation;
   falsifies T-11-STALE). Implement INSIDE `reconcileSchedule(exec)`: module-level
   `let reconcileRunning = false; let reconcilePending = false;`. On entry: if `reconcileRunning` → set
   `reconcilePending = true` and return (coalesce); else set `reconcileRunning = true`, then in
   `try { do { reconcilePending = false; <run the full reconcile body> } while (reconcilePending) }
   finally { reconcileRunning = false }`. The final pass RE-READS app_settings + the candidate reads +
   `getAllScheduledNotificationsAsync` via the same stable `exec` (getExecutor() returns the one cached
   connection), so it always reflects the NEWEST committed state; a burst of taps coalesces to exactly
   one trailing pass. This keeps ALL existing callers UNCHANGED (they keep calling
   `reconcileSchedule(getExecutor())`) — coordination is internal. Do NOT nest a DAO write mutex;
   reconcile stays read-only + expo calls. Add this to the "Artifacts this plan produces" list
   (`reconcileRunning`/`reconcilePending` coordinator).
2. **[actionable — birthday cap eviction] Within the soonest-N `MAX_SCHEDULED_NOTIFICATIONS` cap,
   RESERVE within-horizon birthdays.** A birthday is date-specific + one-shot (NOTIF-04) and cannot be
   caught up, so it must not be evicted by nearer-firing decay reminders. In step 3b: first take ALL
   within-horizon birthday candidates (few — ≤ one per contact per year), THEN fill the remaining slots
   (up to MAX) with the soonest within-horizon decay candidates. If birthdays alone exceed MAX
   (pathological), keep the soonest birthdays. Decay eviction beyond the cap stays the accepted T-11-CAP
   posture.

**Task 2 test additions:** (i) **coalescing / stale-reconcile test** — invoke `reconcileSchedule`
re-entrantly (a second call while the first's awaited read is pending) and assert exactly one trailing
pass runs and it reflects the newest seeded DB state (a contact snoozed between the two calls ends with
its `decay:<id>` CANCELLED, not re-armed). (ii) **birthday-survives-decay-influx test** — seed > MAX
within-horizon decay candidates plus one within-horizon birthday; assert the birthday IS scheduled (not
evicted). Add a `T-11-RACE` row to the `<threat_model>` (Integrity / concurrent reconciles / high /
mitigate → the DEFER-ONE coordinator).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/services/field-sweep.ts
@src/services/launch-sweep.ts
@src/db/notification-read.ts
@src/services/notifications/fire-instant.ts
@src/db/app-settings-dao.ts
@src/services/notifications/notification-ids.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notification-schedule — tunables, reconcileSchedule diff, sweep registration</name>
  <files>src/services/notifications/notification-schedule.ts</files>
  <read_first>src/services/field-sweep.ts:82-134 (registerXSweep + registerSweepHook + lazy getExec + per-item try/catch + the "never nest the mutex" note + localNow helper), src/services/launch-sweep.ts:10-24 + :45 (registerSweepHook; the negative rules — import runs nothing, fires once per real foreground launch), 11-RESEARCH.md §Pattern 1/2 (schedule with stable identifier + generic body; getAllScheduledNotificationsAsync diff) + §Pitfall 6 (stagger) + §Code Examples (the reconcile step list), src/services/notifications/fire-instant.ts (nextNudgeDate + nextAllowedFireInstant), src/db/notification-read.ts (listDecayEligibleCandidates {…,snooze_until} / listBirthdayNotificationCandidates {id,name,birthday}), src/logic/birthday-logic.ts:136 (daysUntilBirthday — reuse to compute the next birthday date), src/db/app-settings-dao.ts (getAppSettings), src/services/notifications/notification-ids.ts (identifiers, channel ids, category, body builders, NotificationData, occurrenceKey), src/utils/dates.ts (formatLocalDate for the occurrenceKey), 11-REVIEWS.md H3+H5.</read_first>
  <behavior>
    - `reconcileSchedule(exec)`:
      1. Reads app_settings. If notifications_enabled is off → cancel every currently-scheduled identifier that is a decay: or birthday: id and return.
      2. eligible = decay_enabled ? listDecayEligibleCandidates(exec) : [] ; birthdays = birthday_enabled ? listBirthdayNotificationCandidates(exec) : []. Both are already ORDER BY id (deterministic — item C).
      3. Builds the CANDIDATE list of {identifier, request, fireInstant}. For each decay candidate: dueDate = last_contact + interval_days (local date construction); base = max(dueDate, snooze_until-as-local-date) — a future snooze pushes the reminder past expiry, a null/past snooze leaves base = dueDate (review H5); fireInstant = nextAllowedFireInstant(nextNudgeDate(base, now, RE_NAG_DAYS), delivery_hour, quiet_start_hour, quiet_end_hour, staggerFor(contactId), now). The stagger arg is `staggerFor(contactId)` — a DETERMINISTIC function of contactId (item C: e.g. `(contactId % STAGGER_SLOTS) * STAGGER_MINUTES`), NOT the array index, so a contact's fire-minute is stable across reconciles and does not churn when an earlier contact changes. Because listDecayEligibleCandidates now returns STABLE/wobble contacts too, this computes a FUTURE fire for a not-yet-due contact. channel = lockscreen_public ? DECAY_PUBLIC_CHANNEL : DECAY_PRIVATE_CHANNEL; body = decayBody(name); category = DECAY_CATEGORY; autoDismiss = true; occurrenceKey = formatLocalDate(fireInstant); data = {kind:"decay",contactId,occurrenceKey}. For each birthday candidate: `days = daysUntilBirthday(birthday, today)`; **if `days === null` (malformed/calendar-invalid stored birthday) SKIP this candidate entirely (item E)** — `today + null` yields an Invalid Date, not a throw, so the per-candidate try/catch alone would not catch it; guard explicitly. Otherwise nextBday = today + days (reuse the parser — computes THIS year or next; H5 pre-schedules a birthday days/weeks out, not only day-of); fireInstant = nextAllowedFireInstant(nextBday-at-delivery-slot, …, staggerFor(contactId), now); channel = BIRTHDAY_CHANNEL; body = birthdayBody(name); NO category; occurrenceKey = formatLocalDate(fireInstant); data = {kind:"birthday",contactId,occurrenceKey}.
      3b. HORIZON + CAP (item A): FILTER the candidate list to those whose fireInstant is within `[now, now + HORIZON_DAYS]`; then SORT the survivors by fireInstant ascending (tie-break by identifier for determinism) and TAKE the soonest `MAX_SCHEDULED_NOTIFICATIONS`. This survivor set is the DESIRED map identifier→request. Candidates beyond the horizon, or past the cap, are intentionally NOT scheduled this cycle — the next foreground reconcile rolls the horizon forward and re-selects, so a far-future contact is armed as it approaches. This guarantees the scheduled count stays below Android's pending-notification cap so the OS never SILENTLY DROPS a schedule.
      4. Reads getAllScheduledNotificationsAsync(); for each existing decay:/birthday: identifier NOT in DESIRED → cancelScheduledNotificationAsync(id). (This also cancels an id that has fallen OUT of the horizon/cap window since the last reconcile.)
      5. FULL-REQUEST DIFF (review H3 + items C/F): for each DESIRED identifier, compare against the currently-scheduled request for that identifier. If it is NOT scheduled → schedule it. If it IS scheduled but ANY of {fire instant AT HOUR GRANULARITY, channelId, categoryIdentifier, data (contactId + occurrenceKey), content.body, content.title} DIFFERS → cancelScheduledNotificationAsync(id) then scheduleNotificationAsync under the SAME identifier (so a delivery-hour / quiet-window / lockscreen-channel change OR a contact rename re-arms the pending notification). If it is scheduled and the full request MATCHES → leave it untouched (stable identity, and the occurrenceKey stays constant so a double-tap dedups). Use a helper `requestsEqual(desired, existing)` that compares those facets, truncating the fire instant to the HOUR so the per-contact stagger minutes do NOT cause needless churn (item C) but a delivery-hour change IS caught, and including content.body + content.title so a renamed contact's stale generic body refreshes (item F).
      6. schedule uses a DATE trigger at the fire instant, the stable identifier, the generic content, category (decay only), the chosen channel, autoDismiss:true, and the {kind,contactId,occurrenceKey} data.
    - Each candidate's schedule/cancel is wrapped so one failure does not abort the whole reconcile.
    - `registerNotificationScheduleSweep(getExec)` pushes ONE hook (via registerSweepHook) that calls reconcileSchedule(getExec()); importing the module runs nothing.
  </behavior>
  <action>Create `notification-schedule.ts` with top-of-file tunables: `RE_NAG_DAYS = 7` (flat weekly), `STAGGER_MINUTES` + `STAGGER_SLOTS` (per-contact offset to dodge the Android 15 cooldown — see staggerFor below), `HORIZON_DAYS = 35` (item A: only pre-schedule a fire within this many days ahead; the reconcile rolls it forward each foreground; a value in the review's 30–45 range), and `MAX_SCHEDULED_NOTIFICATIONS` (item A: a hard ceiling BELOW Android's per-app pending-notification cap — e.g. 48 — so the OS can never silently drop a schedule; leaves headroom for both decay + birthday). Note the fixed snooze length lives in snooze-dao (not redefined here). Add a pure helper `staggerFor(contactId)` returning `(contactId % STAGGER_SLOTS) * STAGGER_MINUTES` (item C — a deterministic per-contact minute offset stable across reconciles; NEVER stagger by the array index, which shifts a contact's minute when an earlier contact changes and churns the diff). Implement `reconcileSchedule(exec)` per the behavior above, composing notification-read + fire-instant + birthday-logic + app-settings + notification-ids. Compute dueDate from last_contact + interval_days using LOCAL date construction (never toISOString); compute the snooze base by parsing the stored `snooze_until` YYYY-MM-DD with LOCAL components (`new Date(y, m-1, d)` — never `new Date("YYYY-MM-DD")`, the forbidden UTC path) and taking the later of it and dueDate. For birthdays, guard `daysUntilBirthday(birthday, today) === null` → skip the candidate (item E — Invalid-Date guard, not caught by a bare try/catch). Mint `occurrenceKey = formatLocalDate(fireInstant)` and put it in the data payload (this is what 11-07's actionUid keys on for per-occurrence idempotency). Apply the HORIZON + soonest-N CAP (step 3b, item A): keep only candidates with fireInstant in `[now, now + HORIZON_DAYS]`, sort by fireInstant asc (tie-break by identifier), take the soonest `MAX_SCHEDULED_NOTIFICATIONS` — that is the DESIRED set. Implement the FULL-REQUEST diff (step 5) with a `requestsEqual` helper comparing fire instant TRUNCATED TO THE HOUR (item C — catches a delivery-hour/quiet-window change, invariant to the stagger minutes) + channelId + categoryIdentifier + data + content.body + content.title (item F — a renamed contact's frozen body refreshes) — do NOT diff by identifier presence alone (the H3 bug). Post decay notifications to the private or public channel chosen by lockscreen_public; birthday to the birthday channel. Set `categoryIdentifier: DECAY_CATEGORY` on decay only; `autoDismiss: true` on all (orchestrator pick 5). Use `Notifications.SchedulableTriggerInputTypes.DATE`. Wrap each per-contact op in its own try/catch (field-sweep idiom) + Logger so one failure does not abort the sweep, and NEVER nest a DAO mutex inside the reconcile (the reads are async-only, no writes here). Implement `registerNotificationScheduleSweep(getExec)` mirroring `registerFieldSweep`: push ONE `registerSweepHook` that calls `reconcileSchedule(getExec())`; import-time side effects: none. Note the birthday/decay same-day-late edge (if today's computed delivery slot already passed `now`, fire-instant rolls it to the next allowed morning) — this is handled by nextAllowedFireInstant's past-slot roll; verify on-device at Pixel-UAT.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Pixel-UAT (phase gate, item A): with ~200+ contacts eligible, confirm the reconcile schedules at most MAX_SCHEDULED_NOTIFICATIONS (all within HORIZON_DAYS) and that NONE are silently dropped by the OS — read the pending set via getAllScheduledNotificationsAsync and assert scheduled count == the expected soonest-N and every scheduled fire is ≤ now+HORIZON_DAYS.</manual>
  </verify>
  <acceptance_criteria>
    - reconcileSchedule PRE-SCHEDULES a future occurrence for every eligible (stable/wobble/overdue) contact at max(dueDate, snooze_until) and the next day-of birthday for every non-archived birthday (H5) — BOUNDED to fires within HORIZON_DAYS and capped to the soonest MAX_SCHEDULED_NOTIFICATIONS (item A), so the OS pending cap can never silently drop a schedule.
    - The stagger is `staggerFor(contactId)` (deterministic per contact), never the array index (item C).
    - The reconcile does a FULL-REQUEST diff (fire instant AT HOUR GRANULARITY + channel + category + data + content.body + content.title) and cancels+reschedules under the same identifier on mismatch (H3 + items C/F) — not identifier-presence only; the hour-granularity compare is invariant to the stagger minutes.
    - A malformed birthday (daysUntilBirthday === null) is SKIPPED, not scheduled with an Invalid Date (item E).
    - occurrenceKey (formatLocalDate(fireInstant)) is minted into the data payload; generic body, stable identifiers, category (decay only)/channel/autoDismiss/data all set per behavior.
    - Master/per-type off gates scheduling (and cancels stale ids); registerNotificationScheduleSweep pushes one hook and imports run nothing.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>The reconcile engine (future pre-scheduling + full-request diff) + its launch-sweep registration exist, composing the foundation modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: notification-schedule.test.ts — reconcile diff + gating (mock expo-notifications)</name>
  <files>src/services/notifications/notification-schedule.test.ts</files>
  <read_first>11-VALIDATION.md (the NOTIF-01 reconcile-diff test row), the 11-01 expo-notifications double (seedable getAllScheduledNotificationsAsync), src/db/dashboard-read.test.ts (in-memory sqlite seeding for the candidate reads).</read_first>
  <behavior>
    - PRE-SCHEDULING (H5): a STABLE contact (not yet due, dueDate WITHIN the horizon) gets a FUTURE-dated decay schedule (its next dueDate morning); a future-SNOOZED contact (snooze_until within horizon) gets a schedule at/after snooze_until (not during the snooze); a birthday NEXT WEEK gets a dated schedule on its day-of morning — each proven by asserting a scheduleNotificationAsync with a DATE trigger at the expected future instant.
    - HORIZON (item A): a contact whose next fire instant is BEYOND HORIZON_DAYS (e.g. a stable contact due in 90 days) is NOT scheduled this cycle — assert no scheduleNotificationAsync for its identifier.
    - CAP (item A): seed more than MAX_SCHEDULED_NOTIFICATIONS eligible contacts all within the horizon; assert exactly MAX_SCHEDULED_NOTIFICATIONS schedules occur and they are the SOONEST-firing ones (a later-firing within-horizon contact is dropped this cycle) — proving no reliance on the OS to silently drop.
    - STAGGER (item C): two contacts get fire minutes derived from their contactId (staggerFor), not their position in the list — assert the minute offset is a function of id (stable when list order changes) and that reordering the seeded contacts does not change a given contact's scheduled minute.
    - MALFORMED BIRTHDAY (item E): a contact with a calendar-invalid birthday (e.g. "2021-02-29") is SKIPPED — assert NO birthday schedule for it and NO Invalid-Date trigger, and that a valid birthday in the same batch still schedules.
    - Given a seeded DB with one eligible contact and a currently-scheduled decay id for a no-longer-eligible contact: reconcile cancels the stale id and schedules the eligible one.
    - The scheduled decay call carries the generic body, the decay_actions category, autoDismiss, the private channel (lockscreen off), and {kind:"decay",contactId,occurrenceKey} data (occurrenceKey = the fire date).
    - FULL-REQUEST DIFF (H3 + item C): given a contact whose decay id is ALREADY scheduled, changing delivery_hour (crossing an HOUR — or quiet-window, or flipping lockscreen_public) makes reconcile CANCEL the stale request and RESCHEDULE under the SAME identifier with the new fire instant / channel — assert exactly one cancel + one schedule for that identifier (the timing-change→reschedule regression test the review requires).
    - BODY REFRESH (item F): given an already-scheduled decay id whose stored content.body is the OLD name ("Sam — time to reach out.") and the contact is now renamed ("Samir"), reconcile CANCELS and RESCHEDULES under the same id with the refreshed body — assert the reschedule fires on the body diff alone (fire instant/channel/category/data otherwise identical).
    - HOUR-GRANULARITY INVARIANCE (item C): an already-scheduled entry whose stored fire minute differs from the freshly-computed stagger minute BUT whose HOUR is identical (and body/channel/category/data match) is left UNTOUCHED — assert no cancel/reschedule (the stagger minutes must not churn the diff).
    - Flipping lockscreen_public on reschedules the decay id to the public channel (privacy: re-privatising reschedules back to private).
    - notifications_enabled off → all decay/birthday ids cancelled, nothing scheduled.
    - decay_enabled off but birthday_enabled on → only birthday scheduled.
    - An already-scheduled desired identifier whose FULL request is UNCHANGED is left untouched (no cancel, no reschedule) — occurrenceKey stays constant.
  </behavior>
  <action>Write the co-located test with `vi.mock("expo-notifications")` + the 11-01 double and the in-memory node:sqlite executor. Seed app_settings + contacts to drive each case; seed the double's getAllScheduledNotificationsAsync with a "currently scheduled" set whose entries carry `content.data`, `content.body`, `content.categoryIdentifier`, and `trigger.{channelId,date}` so the full-request diff (incl. the body facet) can be exercised; assert the exact scheduleNotificationAsync / cancelScheduledNotificationAsync calls (identifiers, body, category, channel, autoDismiss, data incl occurrenceKey) and the gating branches. Include: (a) the pre-scheduling cases (stable contact, future-snoozed contact, next-week birthday each getting a dated schedule — H5); (b) the horizon case (a 90-day-out contact NOT scheduled) and the cap case (>MAX within-horizon contacts → exactly MAX soonest scheduled) — item A; (c) the stagger-by-id case (reordering the seed does not change a contact's minute) — item C; (d) the malformed-birthday skip — item E; (e) the timing-change→reschedule case (seed a scheduled entry at 9am, set delivery_hour=10, assert cancel+reschedule under the same id — H3); (f) the body-refresh reschedule (renamed contact, item F); (g) the hour-granularity-invariance no-op (differing stagger minute, same hour → untouched, item C); (h) the unchanged-request case (identical scheduled entry → no cancel/schedule). Use fixed local dates so the fire instants are deterministic; set MAX_SCHEDULED_NOTIFICATIONS/HORIZON_DAYS-aware fixtures (or import the tunables) so the cap/horizon assertions are exact.</action>
  <verify>
    <automated>npx vitest run src/services/notifications/notification-schedule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The pre-scheduling cases (stable / future-snoozed / next-week birthday) each assert a dated future schedule (H5).
    - The horizon (beyond-horizon contact not scheduled) and cap (exactly MAX soonest scheduled) cases pass (item A); the stagger-by-id case proves the minute is id-derived not index-derived (item C); the malformed-birthday skip passes (item E).
    - The timing-change→reschedule case asserts cancel+reschedule under the same identifier; the body-refresh case reschedules on the body diff (item F); the hour-granularity-invariance case is a no-op despite a differing stagger minute (item C); the unchanged-request case asserts no-op (H3).
    - Reconcile diff, channel selection, gating branches, occurrenceKey data, and birthday scheduling are all asserted against the mock.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>The reconcile's bounded future pre-scheduling (H5 + item A horizon/cap), full-request diff + timing/body reschedule (H3 + items C/F), malformed-birthday guard (item E), gating, and content shape are proven against the mocked native module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live DB + settings → OS-scheduled alarms | the reconcile decides what is scheduled and on which channel |
| scheduled body → lock screen | body text is frozen at schedule time and may be glanced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information disclosure | scheduled decay body | high | mitigate | Body is generic (name-only via decayBody) — no fuel/PII is ever frozen into a scheduled notification; the private channel is the default. |
| T-11-LOCK | Information disclosure | channel selection + re-privatise | high | mitigate | Decay posts to the private channel unless lockscreen_public is explicitly on; birthday uses the single private channel (OQ-2). The FULL-REQUEST diff (H3) means turning lockscreen_public OFF again CANCELS the stale public request and reschedules on the private channel — a name cannot stay glanceable on the lock screen after re-privatising (the privacy regression the review flagged). |
| T-11-STALE | Integrity | frozen scheduled content / settings drift | medium | mitigate | Reconcile-on-launch + FULL-REQUEST diff (cancel+reschedule on any fire-instant/channel/category/data mismatch, H3) + cancel-on-mark/snooze/mute/purge + generic body ensure a pending nudge always reflects the current settings and never fires for a since-contacted contact with stale text. |
| T-11-SWEEP | Availability | sweep registration | medium | mitigate | Reconcile registers ONLY via registerSweepHook; import runs nothing and the headless action path can never reach it (Pitfall 5). |
| T-11-CAP | Availability | OS pending-notification cap | high | mitigate | The DESIRED set is bounded to fires within `HORIZON_DAYS` and hard-capped to the soonest `MAX_SCHEDULED_NOTIFICATIONS` (< Android's per-app pending limit), so the OS can never SILENTLY DROP a schedule (an un-repairable failure class in a local-only DB). The launch/foreground reconcile rolls the horizon forward so far-future contacts are armed as they approach. Proven by the unit cap/horizon tests + the ~200-contact Pixel-UAT (item A). |
</threat_model>

## Artifacts this plan produces
- **File:** `src/services/notifications/notification-schedule.ts` — exports `reconcileSchedule` (bounded future pre-scheduling + full-request diff), `registerNotificationScheduleSweep`, the `requestsEqual` diff helper (hour-granularity fire instant + channel + category + data + body + title), the `staggerFor(contactId)` helper; tunables `RE_NAG_DAYS`, `STAGGER_MINUTES`, `STAGGER_SLOTS`, `HORIZON_DAYS`, `MAX_SCHEDULED_NOTIFICATIONS`. Mints `data.occurrenceKey` (= formatLocalDate(fireInstant)) for per-occurrence action idempotency (11-07).
- **File:** `src/services/notifications/notification-schedule.test.ts`.

<verification>
- `npx vitest run src/services/notifications/notification-schedule.test.ts` green.
- Manual/Pixel-UAT (phase gate): real morning delivery, quiet-window roll, weekly re-nag replacing the prior shade entry, staggered burst — all OS-runtime.
- tsc + biome clean.
</verification>

<success_criteria>
The reconcile engine PRE-SCHEDULES each eligible contact's next future decay occurrence (at max(dueDate, snooze_until)) and every non-archived contact's next birthday from live data + settings, does a FULL-REQUEST diff against the OS set (cancel+reschedule under the same identifier on any fire-instant/channel/category/data mismatch), and schedules generically-bodied, stable-identifier, occurrence-keyed, correctly-channelled notifications — installed as a launch-sweep hook (NOTIF-01 fires without the app open; settings edits re-arm pending notifications).
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-10-SUMMARY.md` when done.
</output>
