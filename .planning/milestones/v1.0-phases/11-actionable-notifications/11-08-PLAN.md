---
phase: 11-actionable-notifications
plan: 08
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/services/notifications/purge-notification-cleanup.ts
  - src/services/notifications/purge-notification-cleanup.test.ts
  - src/screens/ArchivedContactsScreen.tsx
autonomous: true
requirements: [NOTIF-01]
user_setup: []

must_haves:
  truths:
    - "Purging a contact cancels that contact's scheduled decay and birthday notifications POST-COMMIT via the existing onPurgeExtensions adapter."
    - "The notification cancel runs alongside the existing photo cleanup — both best-effort, neither able to undo the committed deletes."
  artifacts:
    - src/services/notifications/purge-notification-cleanup.ts
    - src/screens/ArchivedContactsScreen.tsx (composed onPurgeExtensions)
  key_links:
    - "Registers on purge-dao's POST-COMMIT onPurgeExtensions (Phase 4 explicitly deferred the notification-cancel here)."
---

<objective>
Fulfill the Phase-4 deferral: register a notification-cancel on the purge fan-out so purging a contact clears their pending `decay:<id>` and `birthday:<id>` alarms, composed with the existing photo cleanup in the single `onPurgeExtensions` adapter slot.

Purpose: an OS-scheduled alarm outlives the DB row it points at unless explicitly cancelled — this closes that leak at the one place purge already fires POST-COMMIT best-effort cleanup.
Output: `purge-notification-cleanup.ts` (+ test) and the composed adapter in `ArchivedContactsScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/db/purge-dao.ts
@src/services/photos/purge-photo-cleanup.ts
@src/screens/ArchivedContactsScreen.tsx
@src/services/notifications/notification-ids.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildNotificationPurgeCleanup — cancel decay + birthday for a purged contact</name>
  <files>src/services/notifications/purge-notification-cleanup.ts, src/services/notifications/purge-notification-cleanup.test.ts</files>
  <read_first>src/db/purge-dao.ts:162 + :209-222 (POST-COMMIT onPurgeExtensions best-effort, logged-not-fatal), src/services/photos/purge-photo-cleanup.ts:56-62 (the analog adapter builder + idempotent/error-resilient shape), src/services/notifications/notification-ids.ts (decayIdentifier / birthdayIdentifier), 11-PATTERNS.md §Post-purge notification cancel.</read_first>
  <behavior>
    - `buildNotificationPurgeCleanup()` returns an adapter `(contactId) => Promise<void>` that cancels both decayIdentifier(contactId) and birthdayIdentifier(contactId).
    - The adapter is internally error-resilient (one cancel failing does not skip the other; it never throws out).
  </behavior>
  <action>Create `purge-notification-cleanup.ts` exporting `buildNotificationPurgeCleanup()` — mirroring `buildPhotoPurgeCleanup`'s adapter-builder shape but with no exec needed (cancel is OS-side). The returned adapter awaits `Notifications.cancelScheduledNotificationAsync(decayIdentifier(id))` and `...(birthdayIdentifier(id))`, each wrapped so a failure is logged (Logger) and does not prevent the other or throw out (best-effort, like the photo adapter). Rebuild identifiers from the contactId alone (the row is already deleted). Write the co-located test with `vi.mock("expo-notifications")` + the 11-01 double: assert both identifiers are cancelled for a given id, and that a rejected first cancel still attempts the second and does not reject the adapter.</action>
  <verify>
    <automated>npx vitest run src/services/notifications/purge-notification-cleanup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The adapter cancels both decay and birthday identifiers for the contact.
    - Error-resilient (a failed cancel does not skip the other or throw).
    - tsc + biome clean.
  </acceptance_criteria>
  <done>A best-effort notification-cancel adapter exists for the purge fan-out.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compose the notification cancel into ArchivedContactsScreen's purge</name>
  <files>src/screens/ArchivedContactsScreen.tsx</files>
  <read_first>src/screens/ArchivedContactsScreen.tsx:122-143 (the doPurge callback and the current `onPurgeExtensions: buildPhotoPurgeCleanup(exec)` single-slot).</read_first>
  <action>In `doPurge`, replace the single `onPurgeExtensions: buildPhotoPurgeCleanup(exec)` with a composed adapter that runs BOTH the photo cleanup and the notification cleanup, each awaited in its own try/catch so one failing does not skip the other (both are already best-effort; the purge itself has committed by the time onPurgeExtensions fires). Prefer a small inline composition: build `const photo = buildPhotoPurgeCleanup(exec); const notif = buildNotificationPurgeCleanup();` then pass `onPurgeExtensions: async (purgedId) => { try { await photo(purgedId); } catch {} try { await notif(purgedId); } catch {} }` — or a tiny local helper. Import `buildNotificationPurgeCleanup`. Do not change the two-stage archive→purge guard or any DB path.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:colors</automated>
    <manual>Pixel-UAT (phase gate): purge a contact that had a scheduled decay/birthday notification and confirm the pending notification no longer fires.</manual>
  </verify>
  <acceptance_criteria>
    - onPurgeExtensions runs both the photo and notification cleanups, each error-isolated.
    - The archive→purge safety guard and DB deletes are untouched.
    - tsc + check:colors clean.
  </acceptance_criteria>
  <done>Purging a contact now also cancels their scheduled notifications, POST-COMMIT and best-effort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| purged contact → OS-scheduled alarms | a scheduled alarm outliving its DB row would fire for a deleted contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-LEAK | Information disclosure | orphaned scheduled notification | medium | mitigate | Purge cancels both identifiers POST-COMMIT so a deleted contact's name can never surface in a later nudge. |
| T-11-PURGE | Availability | onPurgeExtensions composition | low | accept | Both cleanups are best-effort and error-isolated; a failed cancel is logged but cannot undo the committed purge (matches the existing adapter contract). The next launch reconcile also cancels any stale identifier as a backstop. |
</threat_model>

## Artifacts this plan produces
- **File:** `src/services/notifications/purge-notification-cleanup.ts` — exports `buildNotificationPurgeCleanup`.
- **File:** `src/services/notifications/purge-notification-cleanup.test.ts`.
- **Change:** `src/screens/ArchivedContactsScreen.tsx` — `onPurgeExtensions` now composes photo + notification cleanup.

<verification>
- `npx vitest run src/services/notifications/purge-notification-cleanup.test.ts` green.
- tsc + check:colors clean; purge-dao.test.ts still green (adapter contract unchanged).
</verification>

<success_criteria>
Purging a contact cancels their scheduled decay and birthday notifications via the composed POST-COMMIT adapter, without touching the two-stage purge safety.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-08-SUMMARY.md` when done.
</output>
