---
phase: 11-actionable-notifications
plan: 12
type: execute
wave: 3
depends_on: [11-01, 11-07]
files_modified:
  - src/services/notifications/notification-nav.ts
  - src/services/notifications/notification-nav.test.ts
  - src/navigation/notification-gate.tsx
autonomous: true
requirements: [NOTIF-02, NOTIF-04]
user_setup: []

must_haves:
  truths:
    - "A decay body tap resolves to a back-stack of [Home, Compose{contactId}] so Back always lands on the dashboard; a birthday body tap resolves to Profile{contactId}."
    - "The response gate handles both cold-start taps (getLastNotificationResponseAsync once nav is ready) and warm taps (addNotificationResponseReceivedListener), routing ONLY Expo's DEFAULT_ACTION_IDENTIFIER as a body tap and mark/snooze action taps to the shared handler; a body tap that arrives before nav isReady is QUEUED and routed once readiness settles."
    - "The foreground action-tap call is wrapped in a Logger-guarded catch (a stale/purged contactId or a benign idempotency rejection cannot become an unhandled promise rejection), and clearLastNotificationResponseAsync() is called after handling a cold-start response so a relaunch cannot re-route/re-write it."
    - "The routing decision is a pure, node-tested function separate from the OS listener wiring."
  artifacts:
    - src/services/notifications/notification-nav.ts
    - src/navigation/notification-gate.tsx
  key_links:
    - "Mirrors linking.ts's ShareIntentGate (navigationRef + isReady gating, including QUEUING a pre-ready response); foreground action taps funnel to the exactly-once handleNotificationAction (11-07)."
---

<!-- REVIEW-CYCLE-1 (11-REVIEWS.md) — item 8 / A3 + H2 (cold-replay):
     - Wrap the foreground handleNotificationAction call in a Logger-guarded catch (stale/purged
       contactId, or the benign UNIQUE-collision rejection the handler may surface).
     - QUEUE a body-tap response that arrives before nav isReady, then route once ready (the warm
       path was previously un-queued and could no-op on navigationRef.current).
     - Treat ONLY Notifications.DEFAULT_ACTION_IDENTIFIER as a body tap.
     - After handling a cold-start response, call clearLastNotificationResponseAsync() so a relaunch
       does not replay it (H2 cold-start replay guard). -->


<objective>
Build the tap-routing layer: a pure `resolveNotificationNav(data)` (decay → reset to [Home, Compose]; birthday → navigate Profile) and the `NotificationResponseGate` component that wires it to the OS — cold-start taps via `getLastNotificationResponseAsync`, warm taps via the response listener — routing mark/snooze action taps to the shared handler and body taps through the resolver.

Purpose: guarantee the body tap opens the right screen and Back reaches the dashboard (NOTIF-02), and route the birthday tap to the profile (NOTIF-04), mirroring the existing ShareIntentGate idiom.
Output: `notification-nav.ts` (+ test), `navigation/notification-gate.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-actionable-notifications/11-RESEARCH.md
@.planning/phases/11-actionable-notifications/11-PATTERNS.md
@src/navigation/linking.ts
@src/navigation/types.ts
@src/services/notifications/notification-actions.ts
@src/services/notifications/notification-ids.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveNotificationNav — pure routing decision</name>
  <files>src/services/notifications/notification-nav.ts, src/services/notifications/notification-nav.test.ts</files>
  <read_first>11-RESEARCH.md §Pattern 5 + §Pitfall 7 (reset to [Home, Compose] so Back → dashboard; TaskStackBuilder does not compose with singleTask — the back-stack is a JS concern), src/navigation/types.ts:20-77 (Compose { contactId } / Profile { contactId } route params), src/services/notifications/notification-ids.ts (the NotificationData type).</read_first>
  <behavior>
    - `resolveNotificationNav({kind:"decay",contactId})` returns a reset action shaped `{ type:"reset", index:1, routes:[{name:"Home"},{name:"Compose",params:{contactId}}] }`.
    - `resolveNotificationNav({kind:"birthday",contactId})` returns a navigate action `{ type:"navigate", name:"Profile", params:{contactId} }`.
    - An unknown/malformed data payload returns null (no navigation).
  </behavior>
  <action>Create `notification-nav.ts` exporting a pure `resolveNotificationNav(data)` that returns a serializable discriminated navigation-intent object (reset for decay, navigate for birthday) or null for a malformed payload. No react-navigation import here — return a plain description the gate applies (keeps it node-testable). Write the co-located test covering both kinds + the null case with exact shapes.</action>
  <verify>
    <automated>npx vitest run src/services/notifications/notification-nav.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - decay → reset [Home, Compose]; birthday → navigate Profile; malformed → null.
    - Pure (no react-navigation/expo import); tsc + biome clean.
  </acceptance_criteria>
  <done>The tap-routing decision is a pure, tested function.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NotificationResponseGate — cold-start + warm tap wiring</name>
  <files>src/navigation/notification-gate.tsx</files>
  <read_first>src/navigation/linking.ts:34-61 (navigationRef + ShareIntentGate: the isReady-gated effect, imperative navigate without useNavigation, and how a pending intent is held until readiness), src/services/notifications/notification-actions.ts (handleNotificationAction + ACTION_MARK/ACTION_SNOOZE), src/services/notifications/notification-nav.ts (resolveNotificationNav), src/services/notifications/notification-ids.ts (NotificationData), 11-RESEARCH.md §Pattern 5 (cold-start via getLastNotificationResponseAsync once nav ready), 11-REVIEWS.md item 8 / A3.</read_first>
  <action>Create `notification-gate.tsx` exporting a render-null `NotificationResponseGate({ isReady })` mirroring ShareIntentGate. On mount, register `Notifications.addNotificationResponseReceivedListener` (warm taps). Also, in an `isReady`-gated effect, read `Notifications.getLastNotificationResponseAsync()` ONCE for the cold-start tap. Classify each response by `response.actionIdentifier`: (a) ACTION_MARK or ACTION_SNOOZE → the foreground action path: `handleNotificationAction(data, actionIdentifier)` (the headless task covers killed state) — wrap this call in a Logger-guarded `.catch` (review item 8) so a stale/purged contactId, or the handler's benign UNIQUE-collision rejection, cannot surface as an unhandled promise rejection; (b) `Notifications.DEFAULT_ACTION_IDENTIFIER` (and ONLY that — review item 8: do not treat every non-mark/snooze id as a body tap) → the body tap: apply `resolveNotificationNav(data)` to `navigationRef.current` (`reset(...)` for a reset intent, `navigate(...)` for a navigate intent); (c) any other actionIdentifier → ignore. QUEUE a body tap that arrives before nav is ready: hold the pending response in a ref/state, and in the `isReady`-gated effect flush it once readiness settles (mirror ShareIntentGate keying work to reactive readiness — review A3: an un-queued warm body tap can no-op on `navigationRef.current` and never retry). Action taps (mark/snooze) do NOT need nav and can run immediately regardless of isReady. After handling a COLD-START response (the getLastNotificationResponseAsync one), call `Notifications.clearLastNotificationResponseAsync()` so a later relaunch does not replay/re-route/re-write it (review H2 cold-start replay guard). Clean up the listener subscription on unmount. Use `navigationRef` from linking.ts (do not create a second ref). No colours (config/logic only).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Pixel-UAT (phase gate): body tap on a decay notification opens Compose for the right contact and Back lands on the dashboard; a warm mark/snooze tap writes exactly once; a birthday tap opens the profile; a cold-start tap routes after launch and does NOT re-route on the next relaunch; a body tap that lands before the navigator is ready still routes once ready.</manual>
  </verify>
  <acceptance_criteria>
    - The gate routes ONLY DEFAULT_ACTION_IDENTIFIER as a body tap and mark/snooze to the shared handler; a pre-isReady body tap is queued and routed once ready.
    - The foreground handleNotificationAction call is wrapped in a Logger-guarded catch; clearLastNotificationResponseAsync() is called after a cold-start response.
    - Uses the existing navigationRef; cleans up its subscription.
    - tsc + biome clean.
  </acceptance_criteria>
  <done>The response gate routes cold-start and warm taps (queued until nav ready) to actions or navigation, guarded against unhandled rejections and cold-start replay (mounted by App.tsx in 11-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification tap → navigation / write | a tap routes to a screen or a DB write keyed on data.contactId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SPOOF | Spoofing | data.contactId on tap | medium | mitigate | data.contactId is app-minted at schedule time; Compose/Profile self-fetch by id and no-op on a missing contact (existing screen behaviour) — a stale id opens nothing harmful. The foreground action call is Logger-catch-wrapped so a stale/purged id (or a benign idempotency rejection) is swallowed, never an unhandled rejection (review item 8). |
| T-11-REPLAY | Integrity | cold-start response replay | medium | mitigate | `clearLastNotificationResponseAsync()` after a cold-start response prevents a relaunch from re-routing/re-writing it; combined with 11-07's deterministic-uid dedup this closes the cold-start double-write (review H2). |
| T-11-BACKSTACK | (UX correctness) | body-tap back-stack | medium | mitigate | reset to [Home, Compose] guarantees Back → dashboard regardless of the prior stack under singleTask/onNewIntent (Pitfall 7); a pre-isReady body tap is queued and routed once ready (review A3); verified on-device. |
</threat_model>

## Artifacts this plan produces
- **File:** `src/services/notifications/notification-nav.ts` — exports `resolveNotificationNav` (+ the nav-intent types).
- **File:** `src/services/notifications/notification-nav.test.ts`.
- **File:** `src/navigation/notification-gate.tsx` — exports `NotificationResponseGate` (warm + cold-start wiring; DEFAULT_ACTION_IDENTIFIER-only body tap; queued-until-isReady body routing; Logger-guarded action catch; clearLastNotificationResponseAsync after cold-start).

<verification>
- `npx vitest run src/services/notifications/notification-nav.test.ts` green.
- Pixel-UAT (phase gate): body-tap → Compose → Back → dashboard; birthday-tap → Profile; cold-start routing; warm action taps.
- tsc + biome clean.
</verification>

<success_criteria>
Notification taps route deterministically — decay body → Compose with Back to the dashboard, birthday body → Profile, action taps → the shared handler — via a pure resolver and a ShareIntentGate-style response gate.
</success_criteria>

<output>
Create `.planning/phases/11-actionable-notifications/11-12-SUMMARY.md` when done.
</output>
