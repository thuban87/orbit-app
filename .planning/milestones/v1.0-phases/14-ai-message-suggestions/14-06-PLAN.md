---
phase: 14-ai-message-suggestions
plan: 06
type: execute
wave: 6
depends_on: ["14-05", "14-07"]
files_modified:
  - .planning/phases/14-ai-message-suggestions/14-VALIDATION.md
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04]
must_haves:
  truths:
    - "Automated evidence covers the schema/registration, secret, provider, native-egress, privacy-context, and Compose lifecycle boundaries."
    - "A release-build device check confirms native SecureStore, native Custom egress enforcement, and interactive AI Suggest behavior without real personal data."
    - "The native HTTPS→private-address escape fixtures (IPv4 loopback/RFC1918, IPv6-ULA fd00::, IPv4-mapped — C2-H2) and the HTTPS-redirect fixture FAIL to leak, each with its specific sanitized error code and an independent observer confirming zero delivery (release approval is contingent on their pass — H3/C2-M3)."
  artifacts:
    - ".planning/phases/14-ai-message-suggestions/14-VALIDATION.md"
  key_links:
    - "Validation evidence links the focused Vitest suites + the Plan 07 native module to the release-build UAT checklist, including the on-device egress-escape fixtures a mocked test cannot prove."
---

<objective>
Record and run the Phase 14 automated and physical-device validation contract, including the on-device proof of the native connection-time egress enforcement.

Purpose: confirm the native dependency, the airtight Custom-egress guarantee, and the human-facing privacy workflow work together before release.
Output: reproducible validation evidence and owner-approved Pixel release-build UAT.
</objective>

<execution_context>
@/home/bwales/.codex-edu/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex-edu/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-message-suggestions/14-CONTEXT.md
@.planning/phases/14-ai-message-suggestions/14-AI-SPEC.md
@.planning/phases/14-ai-message-suggestions/14-REVIEWS.md
@.planning/phases/14-ai-message-suggestions/14-VALIDATION.md
@.planning/phases/14-ai-message-suggestions/14-01-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-02-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-03-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-04-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-05-SUMMARY.md
@.planning/phases/14-ai-message-suggestions/14-07-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 06)

- Updated `14-VALIDATION.md`: the requirement→test matrix extended to include the Plan 07 native module (`secure-fetch.test.ts`) and the multi-source coverage audit updated to list Plan 07, and the sanitized automated + on-device evidence.

<tasks>

<task type="auto">
  <name>Task 1: Update the Phase 14 validation matrix and capture automated evidence</name>
  <files>.planning/phases/14-ai-message-suggestions/14-VALIDATION.md</files>
  <read_first>
    - .planning/phases/14-ai-message-suggestions/14-VALIDATION.md (the existing matrix + coverage audit to extend)
    - the Plan 01-05 + Plan 07 SUMMARY files (the exact test file names to map)
    - 14-07-SUMMARY.md specifically — the recorded native `compileDebugKotlin`/`assembleDebug` + `testDebugUnitTest` results to transfer (C3-L2/C4-L1)
  </read_first>
  <action>Update the validation matrix before release UAT. Add Plan 07's native module to the multi-source coverage audit (a new RESEARCH/CONTEXT row for the native connection-time egress enforcement) and add `src/ai/secure-fetch.test.ts` to the AI-01 automated evidence. Map AI-01 through AI-04 to the precise test files from Plans 01-05 + 07 and record command output/status for: migration 004 registration + settings/key isolation; provider + Custom URL + native-egress + error behavior; outbound-context/prompt identity (incl. the M1 integration test); field-sharing create+edit persistence; and Compose lifecycle/navigation/ack. Include the full `npx vitest run`, `npx tsc --noEmit`, and `npm run check:colors` gates. Record NO production credentials, prompts, endpoint URLs, contact data, request bodies, headers, or provider response text.</action>
  <acceptance_criteria>
    - `npx vitest run && npx tsc --noEmit && npm run check:colors` all pass and their sanitized status is recorded in 14-VALIDATION.md.
    - The matrix references `secure-fetch.test.ts` (Plan 07) and `ai-suggestion-compose-integration.test.ts` (M1).
    - The coverage audit lists all seven plans (01-07) with no unplanned source item.
    - The named Plan 07 native results are transferred verbatim into 14-VALIDATION.md — `compileDebugKotlin`/`assembleDebug` AND `testDebugUnitTest` (the Kotlin address-predicate table test), each with pass/fail status; ABSENT or FAILED native evidence is a release-blocking condition, not a silent pass (C4-L1/C3-L2).
  </acceptance_criteria>
  <verify><automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <done>14-VALIDATION.md has a requirement-to-test matrix (incl. the native module + M1 test) and sanitized automated pass/fail evidence for every Phase 14 safety boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the release build on Pixel with synthetic data — including the native egress escapes</name>
  <files>.planning/phases/14-ai-message-suggestions/14-VALIDATION.md</files>
  <read_first>
    - .planning/phases/14-ai-message-suggestions/14-VALIDATION.md (Task 1 evidence must be green first)
    - 14-REVIEWS.md "Owner decisions" H3 (the on-device escape + redirect fixtures gate release; mocked tests cannot establish native transport)
    - docs/runbooks/ desktop-build-pipeline (prebuild on droid + install on the Pixel)
  </read_first>
  <action>Run this checkpoint only after Task 1 automated gates pass. Do a clean Expo prebuild/release build on the desktop pipeline and install on a physical Pixel. Record concise, sanitized release-build observations in 14-VALIDATION.md; never copy credentials, prompts, endpoint URLs, request bodies, headers, contact data, or raw responses into evidence.</action>
  <what-built>Migration 004 (v4), SecureStore-backed provider configuration, the native Custom-egress enforcement (Plan 07), prompt inspection/acknowledgement, and the Compose-only AI Suggest lifecycle.</what-built>
  <how-to-verify>
    1. Verify migration 004 is live (user_version=4) and SecureStore setup works after launch with no key displayed in Settings.
    2. With synthetic data + disposable test credentials only, exercise OpenAI, Anthropic, Gemini, and manual-model fallback; confirm Custom rejects cleartext/credentialed/`.local` URLs at save.
    3. NATIVE EGRESS FIXTURES (H3/C2-H2/C2-M3 — release-gating). Use CONCRETE, controlled fixtures with an INDEPENDENT observer so a native rejection cannot be confused with a generic TLS/DNS/connectivity failure, and so "no payload delivered" is positively proven:
       - Controlled resolution: a test hostname you control whose A/AAAA record (or an on-device test resolver / hosts override) points in turn at (a) `127.0.0.1`, (b) an RFC1918 IPv4 (e.g. `10.x`/`192.168.x`), (c) an IPv6 Unique-Local `fd00::…` address (C2-H2 — the case `isSiteLocalAddress()` misses), and (d) an IPv4-mapped IPv6 form of a private address. On each, attempt a suggestion — every one MUST fail before any bytes leave the device with the specific sanitized PRIVATE-RESOLUTION error code (not a generic transport error).
       - Independent observer: run a listener (e.g. a logging HTTP server / `nc`) bound to the targeted private address and to the redirect target; after each attempt, confirm the observer recorded ZERO inbound connection and ZERO payload bytes — this is what proves the prompt was not delivered.
       - Redirect fixture: a synthetic HTTPS origin with a VALID certificate (so the path exercised is redirect handling, not a cert error) that returns a 3xx to a second observed target; the redirect MUST be rejected natively with the sanitized REDIRECT error code, and the redirect-target observer MUST record zero payload.
       - Direct IP-literal escape (C4-H1): configure a Custom endpoint that is a numeric-IP LITERAL of a private/non-public address (e.g. `https://100.64.0.1`, `https://[fd00::1]`) — NOT a resolving hostname — and attempt a suggestion; it MUST fail-closed with the private-resolution code at the native pre-`Call` literal check, with the observer recording zero delivery (proves the OkHttp-Dns-skipped-for-literals path is guarded).
       - Proxy escape (C3-H2): with an Android system HTTP proxy (and, if feasible, a SOCKS proxy) configured pointing at an observed listener, repeat sub-case (b) — a private-resolving Custom host. Because the security client is built with `Proxy.NO_PROXY`, the request MUST still fail-closed with the private-resolution code and the proxy/target observers MUST record zero delivery (proving the proxy cannot resolve/connect the origin on the module's behalf).
       Release approval is contingent on every sub-case (a-d + redirect + proxy) failing-closed with its specific sanitized code AND the observer confirming no delivery.
    4. Inspect the exact prompt before the first request for each provider and again later; verify truncation disclosure and field opt-in (create + edit) behavior; cancel and navigate away mid-request.
    5. Start from a profile and from Compose; confirm a non-empty draft is never replaced silently, Copy/SMS remain unchanged, and no suggestion records a touchpoint or changes last_contact.
    6. Exercise offline, bad-key, and rate-limit outcomes; verify Retry is deliberate and visible errors/logs omit prompts, keys, headers, endpoints, request bodies, and response bodies.
  </how-to-verify>
  <acceptance_criteria>
    - 14-VALIDATION.md records a sanitized pass/fail for each numbered step, with step 3's sub-cases (private IPv4, RFC1918, IPv6-ULA `fd00::`, IPv4-mapped, redirect, AND the system-proxy escape — C3-H2) each explicitly PASS — specific sanitized error code observed AND independent observer recorded zero delivery — before approval.
    - The evidence log contains no credential, prompt, endpoint URL, request body, header, contact datum, or raw response.
    - An owner decision line (approved / blocked with the failing case) is recorded.
  </acceptance_criteria>
  <verify><automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:colors</automated></verify>
  <done>A physical Pixel release build has passed the listed synthetic-data checks — including the native HTTPS→private-address and HTTPS-redirect escape fixtures — and 14-VALIDATION.md records an owner decision with no sensitive payload retained.</done>
  <resume-signal>Type `approved` with build/device notes, or describe the failed case.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test/release evidence → project records | Validation artifacts can accidentally retain private prompt or credential material. |
| native device → human verification | SecureStore, native Custom-egress enforcement, and cancellation must be proven in the release environment, not only mocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-18 | Information disclosure | validation evidence | high | mitigate | Matrix records only command/status + sanitized synthetic observations; secret/prompt/raw-wire data excluded. |
| T-14-19 | Tampering | native release behavior | high | mitigate | Blocking Pixel release-build UAT verifies the observable privacy/lifecycle contract AND the native egress escapes after automated gates pass (H3). |
</threat_model>

<verification>
Automated gates must pass before the blocking device checkpoint. Any egress-boundary, secret, stale-overwrite, record-write, or native-escape failure blocks release.
</verification>

<success_criteria>
All AI-01…AI-04 automated evidence is recorded, and the owner has verified the release-build workflow — including the native private-resolution and redirect escapes — on a physical Pixel using synthetic data.
</success_criteria>

<output>
Create `.planning/phases/14-ai-message-suggestions/14-06-SUMMARY.md` when done.
</output>
