---
phase: 18.1-contact-method-normalization
plan: "01"
subsystem: database
tags: [sqlite, migrations, libphonenumber-js, expo-localization]
requires:
  - phase: 17-backup-export-restore
    provides: v8 schema, application settings, and migration runner
provides:
  - v9 FK-safe contact-method migration
  - pure phone and email normalization boundary
  - device-region migration input for foreground and headless bootstrap paths
affects: [18.1-02, 18.1-03, 18.1-04, 18.1-06, 18.2]
actuals:
  tokens: 12034
  tasks: 3
  commits: 4
tech-stack:
  added: [libphonenumber-js@1.13.11, expo-localization]
  patterns: [pure method normalization, FK-safe parent-and-child SQLite rebuild]
key-files:
  created:
    - src/logic/contact-method-normalization.ts
    - src/db/migrations/009-contact-method-normalization.ts
    - src/services/device-region.ts
  modified:
    - src/db/database.ts
    - src/db/app-settings-dao.ts
    - src/backup/backup-schema.ts
key-decisions:
  - "Phone actionability requires libphonenumber-js isValid(); malformed nonblank values remain stored and non-actionable."
  - "Migration 009 uses only the device region, failing closed when unavailable; phone_region_override applies only after v9 exists."
  - "Malformed legacy cadence clamps to 30 with an in-transaction field_history breadcrumb."
requirements-completed: [CDN-01, CDN-03]
coverage:
  - id: D1
    description: Normalized UID-bearing contact methods replace scalar phone and email on v8 to v9 upgrade.
    requirement: CDN-01
    verification:
      - kind: integration
        ref: src/db/migrations/009-contact-method-normalization.test.ts
        status: pass
    human_judgment: false
  - id: D2
    description: Phone and email normalization preserves invalid input separately from actionable canonical identity.
    requirement: CDN-01
    verification:
      - kind: unit
        ref: src/logic/contact-method-normalization.test.ts
        status: pass
    human_judgment: false
  - id: D3
    description: Orbit identity remains independent of stale external-contact evidence.
    requirement: CDN-03
    verification:
      - kind: integration
        ref: src/db/migrations/009-contact-method-normalization.test.ts
        status: pass
    human_judgment: false
duration: 11 min
completed: 2026-08-28
status: complete
---

# Phase 18.1 Plan 01: Contact Method Normalization Summary

**V9 safely rebuilds contacts into first-class normalized methods, with device-region phone parsing and lossless relationship-child preservation.**

## Performance

- **Duration:** 11 min
- **Tasks:** 3/3
- **Files modified:** 17

## Accomplishments

- Replaced v8 scalar contact phone/email storage with UID-bearing methods, external links, and provenance tables while preserving all six FK children and the Orrery sun reference.
- Added one shared normalizer for E.164 valid phones, extensions, conservative email equality, and retained non-actionable malformed input.
- Threaded the device region through React, notification, and widget first-open paths without importing native localization from the node-testable database module.

## Pre-approved v9 contracts

- **Storage:** The v9 rebuild retires `contacts.phone` and `contacts.email`; normalized, UID-bearing method/link/provenance rows are the sole authority. `interval_days` remains NOT NULL and every migrated contact remains Bound. Active external links are uniquely constrained by `(provider, external_contact_id)` while stale source evidence remains non-authoritative local provenance.
- **Phone actionability:** A parsed phone is actionable only when `isValid()` confirms it matches the applicable numbering plan. Invalid or incomplete nonblank input remains stored and editable, but non-actionable.
- **Representative test matrix:** device-default national and E.164 round trips; explicit `+` international input; an extension-bearing number; and a right-length-but-invalid number retained as non-actionable.

## Approved package provenance

- `libphonenumber-js@1.13.11` is owner-verified and deliberately pinned instead of the newly published `1.13.12`, prioritizing the established release's adoption and supply-chain age.
- `expo-localization` is an approved official Expo SDK package.

## Task Commits

1. **Task 1: Record approved storage and actionability contracts** — `0b3c95a` (docs)
2. **Task 2: Install approved dependencies** — `e64ade9` (chore)
3. **Task 3: Migrate legacy contact data end to end** — `1c820a9` (TDD RED), `69bb048` (feat)

## Verification

- `npx tsc --noEmit --pretty false` — passed.
- `npm test -- src/logic/contact-method-normalization.test.ts src/db/migrations/009-contact-method-normalization.test.ts src/db/app-settings-dao.test.ts` — passed (81 tests).

## Decisions Made

- Canonicalized migration phone rows retain the region under which they were canonicalized; raw/non-actionable rows keep `canonical_region = NULL`.
- The migration never reads `phone_region_override`, because it creates that column. The setting is validated as null or a two-letter ISO region for later writes.
- The malformed-cadence `field_history` record is a bounded diagnostic trace, not recovery data.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Build configuration] Registered expo-localization's config plugin**
- **Found during:** Task 2
- **Issue:** Expo's compatible installer reported that the native module needed an Expo config plugin entry.
- **Fix:** Added `expo-localization` to the existing deduplicated app config plugin list.
- **Files modified:** `app.config.ts`
- **Verification:** TypeScript compilation and focused tests passed.
- **Committed in:** `69bb048`

---

**Total deviations:** 1 auto-fixed (Rule 2).
**Impact on plan:** Required native build registration only; no behavior beyond the planned device-region provider.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

18.1-02 can add aggregate method writes and reads against the durable v9 schema. Android migration provenance and real-device behavior remain covered by the phase-end UAT plan.

## Self-Check: PASSED

- Migration, normalizer, and device-region provider files exist.
- All four task commits exist in git history.
