---
phase: 16-custom-field-value-normalization
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/db/field-ddl.ts
  - src/db/field-ddl.test.ts
  - src/db/field-defs-dao.ts
  - src/db/field-defs-dao.test.ts
  - src/screens/CustomFieldsScreen.tsx
  - src/services/field-sweep.ts
  - src/services/field-sweep.test.ts
autonomous: true
requirements: [CFN-02, CFN-04]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Creating a field creates a durable blank normalized record for every existing contact (including archived) without custom-field DDL."
    - "isFieldEmpty is re-keyed to a bound field_def_id and its sole production caller (CustomFieldsScreen.tsx:114) passes d.id; the screen's other five reshaped DAO calls are audited for signature drift and compile clean."
    - "A populated field quarantines non-destructively; permanent deletion snapshots non-NULL values under field_col_name and removes definitions/pairs atomically."
    - "The existing strict 30-day foreground sweep and restore-race recheck remain intact; restoreField needs no backfill because pair rows already exist for quarantined definitions."
  artifacts:
    - src/db/field-ddl.ts
    - src/db/field-defs-dao.ts
    - src/services/field-sweep.ts
  key_links:
    - "Lifecycle writers use Plan 01's normalized pair core inside the shared serialized transaction boundary."
    - "field_history continues to use the immutable definition col_name compatibility key."
    - "CustomFieldsScreen.tsx passes d.id to the re-keyed isFieldEmpty; all its other DAO call signatures are unchanged."
---

<objective>
Move definition creation, quarantine, permanent deletion, and the foreground expiry sweep from dynamic
columns to normalized pair-row lifecycle operations, and migrate the sole production management screen off
the retired col_name emptiness signature.

Purpose: after migration, every field lifecycle action must preserve the existing recovery behavior
without future ALTER TABLE or DROP COLUMN work, and the screen that drives every lifecycle action must
compile against the reshaped APIs.
Output: row-based lifecycle DAOs, the re-keyed emptiness read wired into CustomFieldsScreen, and focused
migration-006 fixture regressions.

Addresses review concerns: HIGH uncovered CustomFieldsScreen.tsx caller (add to files_modified + audit its
six reshaped calls), HIGH pair-row invariant (createField seeds every contact; restore is free), MEDIUM
permanent-delete photo filesystem lifecycle (documented decision).
</objective>

## Artifacts this phase produces (owned by this plan)

- **`field-ddl.ts`:** `createField` (INSERT def + blank pair rows for every contact, no ALTER TABLE), `deleteOrQuarantineField`, and the non-mutexed permanent-delete core (snapshot non-NULL values to `field_history` → DELETE pair rows → DELETE def), all row-based; no `ALTER TABLE`/`DROP COLUMN`.
- **`field-defs-dao.ts`:** `isFieldEmpty(exec, fieldDefId: number)` re-keyed off dynamic columns; `restoreField` unchanged in signature (no backfill needed).
- **`CustomFieldsScreen.tsx`:** line-114 call site passes `d.id`; its other DAO calls audited unchanged.
- **`field-sweep.ts`:** the strict 30-day expiry + restore-race recheck, calling the normalized permanent-delete core.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert field creation and delete-or-quarantine decisions to normalized pairs</name>
  <reversibility rating="costly">D-03, D-04, D-09, and D-10 make pair lifecycle and history compatibility shared persisted behavior.</reversibility>
  <read_first>
    - src/db/field-ddl.ts and src/db/field-ddl.test.ts (createField ALTER TABLE path, deleteOrQuarantineField, non-mutexed core/wrapper split, expireFieldIfStale)
    - src/db/field-defs-dao.ts and src/db/field-defs-dao.test.ts (isFieldEmpty current col_name signature; restoreField)
    - src/db/field-values-dao.ts (Plan 01 normalized pair *Core writer)
    - src/db/transaction.ts and src/db/migrations/001-initial.ts (transaction/history contracts; field_history verbatim fields)
  </read_first>
  <files>src/db/field-ddl.ts, src/db/field-ddl.test.ts, src/db/field-defs-dao.ts, src/db/field-defs-dao.test.ts</files>
  <behavior>
    - Creating a definition inserts a NULL pair record for EVERY existing contact (including archived) in its creation transaction; no ALTER TABLE.
    - isFieldEmpty takes a bound field_def_id and counts non-NULL normalized rows for that definition (empty-string remains a present value; only NULL is "empty").
    - Populated deletion quarantines; permanent deletion snapshots eligible (non-NULL) values into field_history under def.col_name, then deletes dependent pair rows and the definition together atomically.
    - restoreField remains a metadata-only quarantined_at=NULL write — no value backfill, because pair rows already exist for quarantined definitions (Plan 01/02 invariant).
    - New runtime custom-field operations issue no custom-field ALTER TABLE or DROP COLUMN SQL.
  </behavior>
  <action>Write RED lifecycle regressions using the normalized fixture. Replace dynamic table alteration in createField with INSERT def + one blank normalized pair per existing contact (including archived) in the same inWriteTransaction. Re-key isFieldEmpty from the interpolated col_name COUNT to a bound field_def_id COUNT over custom_field_values (non-NULL only); update its signature to (exec, fieldDefId: number) and its test. Keep deleteOrQuarantineField's population decision; convert the permanent-delete core to snapshot non-NULL normalized values into field_history with def.col_name as field_col_name (verbatim history fields: contact_id, field_col_name, old_value, operation, created_at) before deleting dependent pairs then the definition. Preserve the one outer inWriteTransaction pattern, NULL-versus-empty storage behavior, quarantine as non-destructive metadata, and the immutable-uid policy. DOCUMENTED DECISION (permanent-delete photo filesystem lifecycle, review MEDIUM): permanently deleting a populated custom-PHOTO field leaves at most one per-contact photo file on disk that purge-photo-cleanup (which enumerates surviving photo defs by col_name) can no longer rediscover. This is accepted as pre-existing, BOUNDED local orphan behavior for Phase 16 — it is on-device only (no egress; local-first) and bounded to one file per contact per deleted photo field. These files are NOT reclaimed by contact purge: purge-photo-cleanup.ts:71-98 enumerates only SURVIVING photo defs via listDefs (which cannot return a deleted def), so once the def is permanently deleted its per-contact files are unreachable to that adapter — they remain a bounded local orphan until a future cleanup mechanism. Do NOT claim purge removes them. A best-effort sweep-based cleanup from the field_history snapshot is a candidate for a later phase; record this corrected decision in the field-ddl.ts permanent-delete comment and it will also be captured in ADR-001 (Plan 16-08). This implements D-01, D-03, D-04, D-09, and D-10. NOTE (MEDIUM-2): this task re-keys isFieldEmpty's signature to (exec, fieldDefId: number), but its sole production caller (CustomFieldsScreen.tsx:114) is not fixed until Task 2 — so a project-wide `npx tsc --noEmit` cannot pass here. This task's verify is the targeted Vitest run ONLY; Task 2's `npx tsc --noEmit && npm run check:colors` is the plan's single compile gate (it runs after the screen call site is corrected).</action>
  <verify><automated>npm test -- src/db/field-ddl.test.ts src/db/field-defs-dao.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove creation inserts durable blanks for every existing contact (including archived) and permanent delete is atomic with field_history snapshots.
    - Tests prove isFieldEmpty(exec, fieldDefId) returns true only when no non-NULL row exists, and empty-string counts as present.
    - Tests prove populated fields quarantine and restore with no backfill (the pair rows already exist).
    - Lifecycle source has no executable custom-field ALTER/DROP after migration006; the photo-orphan decision is documented in-file.
  </acceptance_criteria>
  <done>Definition lifecycle works entirely through normalized pair rows and preserves recovery history.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CustomFieldsScreen to the re-keyed isFieldEmpty and audit its other reshaped calls</name>
  <read_first>
    - src/screens/CustomFieldsScreen.tsx (line 114 empties[d.id] = await isFieldEmpty(exec, d.col_name); and the createField/deleteOrQuarantineField/applyTypeChange/changeFieldOptions/preflightTypeChange/preflightOptionsChange/restoreField call sites)
    - src/db/field-defs-dao.ts (Task 1 re-keyed isFieldEmpty signature)
    - src/db/field-ddl.ts (createField / deleteOrQuarantineField signatures — confirm object/id args unchanged)
    - src/db/field-type-change.ts (applyTypeChange / preflightTypeChange / preflightOptionsChange — MEDIUM-3: Plan 04 WIDENS the preflight `Pick` from `col_name` to `id|col_name` and KEEPS the field-object parameter, so CustomFieldsScreen.tsx:184,189 which pass a full def stay source-compatible; audit against this — the screen's preflight call sites need NO change regardless of whether Plan 03 or Plan 04 lands first)
  </read_first>
  <files>src/screens/CustomFieldsScreen.tsx</files>
  <behavior>
    - The load() loop passes d.id (not d.col_name) to isFieldEmpty; the emptiness map is unchanged in shape and behavior.
    - No user-facing flow, label, layout, accessibility, or navigation changes.
  </behavior>
  <action>Change the CustomFieldsScreen load() call at line ~114 to pass d.id to the re-keyed isFieldEmpty. Audit the screen's other five reshaped DAO calls — createField, deleteOrQuarantineField, applyTypeChange, preflightTypeChange, preflightOptionsChange (and restoreField, changeFieldOptions) — and confirm they stay SOURCE-COMPATIBLE with the screen's existing call sites. Most keep their exact signatures (only internals move to rows); the ONE deliberate change is that Plan 04 widens preflightTypeChange/preflightOptionsChange's `Pick` from `col_name` to `id|col_name` while KEEPING the field-object parameter — the screen passes a full def at :184,189, so those calls need no change. Make no other change if they are source-compatible, and record in the summary that the audit found only the isFieldEmpty arg changed at the screen (the widened preflight Pick required no screen call-site edit). Do not alter any widget, label, control, or the FLD-02/03/04/05 flow. This resolves the HIGH uncovered-caller finding and implements D-12 (no UX change).</action>
  <verify><automated>npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>
    - `rg -n "isFieldEmpty\(exec, d.col_name\)" src/screens/CustomFieldsScreen.tsx` returns nothing; the call passes d.id.
    - The project typechecks with the screen included; no other CustomFieldsScreen call site changed.
    - check:colors passes (no colour literal introduced).
  </acceptance_criteria>
  <done>The custom-fields management screen compiles against every reshaped API with no user-facing change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retain strict expiry and restore-race protection in the field sweep</name>
  <read_first>
    - src/services/field-sweep.ts and src/services/field-sweep.test.ts (candidate scan, localtime cutoff, non-reentrant transaction discipline, under-lock recheck)
    - src/db/field-ddl.ts (Task 1 normalized expiry core)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-04 and D-10)
  </read_first>
  <files>src/services/field-sweep.ts, src/services/field-sweep.test.ts</files>
  <behavior>
    - Candidate discovery remains outside the mutex and the stale-state recheck occurs under it, so a restored field is not expired.
    - Strict older-than-30-day expiry and existing history pruning remain exactly local/foreground behavior.
  </behavior>
  <action>Convert sweep tests to the normalized lifecycle fixtures and retain the existing candidate scan followed by an under-lock expiry recheck. Call the Task 1 destructive core rather than recreating SQL or nesting inWriteTransaction. Preserve the fixed localtime 30-day cutoff and history pruning policy, and cover a restore committed between scan and lock. This implements D-04 and D-10 without adding notifications, backup, sync, or UI.</action>
  <verify><automated>npm test -- src/services/field-sweep.test.ts src/db/field-ddl.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - A stale quarantined field expires with normalized value snapshots; a concurrently restored one survives.
    - The sweep retains 30-day history pruning and issues no custom-field DDL.
  </acceptance_criteria>
  <done>The foreground sweep remains race-safe and preserves the established bounded local recovery trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Field-management UI → lifecycle DAO | User-originated metadata reaches only bound relation values. |
| Sweep candidate → destructive transaction | Time-based candidate discovery must not override a concurrent restore. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-06 | Tampering | field lifecycle | high | mitigate | Atomic snapshot/delete core and bound ids. |
| T-16-07 | Denial of service | sweep restore race | medium | mitigate | Under-lock recheck; no nested mutex. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2); node:sqlite is available.
- The target suites (field-ddl.test.ts, field-defs-dao.test.ts, field-sweep.test.ts) are currently pinned to `runMigrations(exec, [migration001], 1, …)` and would never see `custom_field_values`; re-pin each to migrate through 001..006 so the normalized table exists — "using the normalized fixture" REQUIRES v6, not v1.
- Run all named tests, then `npx tsc --noEmit` (project already green from Plan 02) and `npm run check:colors` after the screen change.
</verification>

<success_criteria>
CFN-02/04 lifecycle behavior persists: fields create blank rows for every contact, retain
quarantine/restore/expiry safeguards, record bounded field_history snapshots without any dynamic DDL, and
the management screen compiles against the reshaped emptiness read.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-03-SUMMARY.md` when done. Record the
CustomFieldsScreen signature audit result and the permanent-delete photo-orphan decision.
</output>
