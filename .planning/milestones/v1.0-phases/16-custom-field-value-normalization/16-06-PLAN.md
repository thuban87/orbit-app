---
phase: 16-custom-field-value-normalization
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/db/purge-dao.ts
  - src/db/purge-dao.test.ts
  - src/db/reserved-columns.ts
  - src/db/reserved-columns.test.ts
autonomous: true
requirements: [CFN-02, CFN-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Purging an archived contact removes all of its normalized value rows atomically and retains post-commit custom-photo cleanup behavior."
    - "purge-dao.test.ts drives migration 006 so its purge coverage is genuine v6 coverage; the v1-pinned purge-photo-cleanup.test.ts is retained but explicitly noted as giving no v6 coverage."
    - "Schema-derived reserved names reflect v6 literal tables instead of dynamic legacy value columns."
  artifacts:
    - src/db/purge-dao.ts
    - src/db/reserved-columns.ts
  key_links:
    - "Purge deletes custom_field_values as the multi-row contact child before post-commit filesystem cleanup."
    - "Photo paths continue to use contactId plus definition col_name, not value uid."
---

<objective>
Update deletion and schema-guard consumers for normalized multi-row values, preserving archive guard,
transactionality, and existing custom-photo path compatibility.

Purpose: child cleanup and reserved-name detection are easily missed consumers that must no longer describe
or depend on a per-contact dynamic value row.
Output: normalized purge accounting/deletion and v6 reserved-column regressions.

Addresses review concerns: LOW purge-photo-cleanup.test.ts pinned to schema v1 gives false v6 coverage —
noted and routed around (purge-dao.test.ts runs through migration 006 for the real coverage).
</objective>

## Artifacts this phase produces (owned by this plan)

- **`purge-dao.ts`:** multi-row `custom_field_values` fan-out deletion as the contact child, archived-only guard and rollback preserved; post-commit best-effort photo cleanup unchanged (paths from `contactId` + `col_name`).
- **`reserved-columns.ts`:** reserved-name discovery derived from the v6 literal tables (no live dynamic value columns treated as reserved schema).

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete normalized value children during archived-contact purge</name>
  <reversibility rating="costly">Purge is a destructive multi-row transaction; its child relationship and cleanup ordering must remain explicit.</reversibility>
  <read_first>
    - src/db/purge-dao.ts and src/db/purge-dao.test.ts (archived-only guard, child delete order, impact summary, rollback; the changes===1 loud-failure idiom)
    - src/services/photos/purge-photo-cleanup.ts and src/services/photos/purge-photo-cleanup.test.ts (note: the test is pinned to schema v1)
    - src/db/migrations/006-normalize-custom-field-values.ts
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-02 and D-09)
  </read_first>
  <files>src/db/purge-dao.ts, src/db/purge-dao.test.ts</files>
  <behavior>
    - An archived contact with multiple pair rows is reported and deleted accurately in one transaction (impact accounting reflects the multi-row child).
    - Rollback leaves children intact; post-commit file cleanup remains best-effort and derives custom-photo paths from contactId plus col_name.
    - purge-dao.test.ts seeds and runs migrations 001..006 so its purge assertions are genuine v6 coverage.
  </behavior>
  <action>Write RED purge cases with multiple normalized rows, seeding via migrations 001..006. Replace the retired legacy child target and one-row impact assumptions with literal custom_field_values fan-out/deletion, preserving the archived-only guard, explicit child order, rollback guarantees, no network work, and post-commit-only photo cleanup. Keep photo cleanup untouched: it continues to derive custom photo paths from contactId plus the definition col_name and never uses a value uid. NOTE for the summary: purge-photo-cleanup.test.ts is pinned to schema v1 (runMigrations(exec, [migration001], 1, …)) so it passes trivially without exercising v6 — do NOT rely on it for v6 photo-purge coverage; the real multi-row v6 coverage is in purge-dao.test.ts here plus the Plan 07 all-path matrix. LOW-8: hasCustomValues (purge-dao.ts:58,113-122) becomes universally true under D-01 (every contact now has a row for every definition), so keep it a BOOLEAN and keep it UNRENDERED (purge-dao.ts:129-132 already deliberately does not render it) — do NOT convert it to a non-blank count, which would tell a user with all-blank fields that purge destroys N custom values; state this in the summary. This implements D-02, D-09, and CFN-04.</action>
  <verify><automated>npm test -- src/db/purge-dao.test.ts src/services/photos/purge-photo-cleanup.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Purging removes every normalized pair row with no dangling relation, seeded through migration 006.
    - Impact and rollback tests model a multi-row child relationship.
    - The summary records the purge-photo-cleanup.test.ts v1-pin caveat.
  </acceptance_criteria>
  <done>Purge correctly owns normalized value children while photo compatibility remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive reserved field names from the v6 normalized schema</name>
  <reversibility rating="reversible">The schema-derived guard can be corrected by code change before any new user data representation is introduced.</reversibility>
  <read_first>
    - src/db/reserved-columns.ts and src/db/reserved-columns.test.ts (the v1-pinned fixture; the fixed-column collision contract)
    - src/db/migrations/001-initial.ts and src/db/migrations/006-normalize-custom-field-values.ts
  </read_first>
  <files>src/db/reserved-columns.ts, src/db/reserved-columns.test.ts</files>
  <action>Update reserved-column discovery and fixtures to derive from the actual version-six literal tables (run all six migrations in the fixture), removing assumptions that live dynamic value columns are reserved schema. LOW-9: explicitly re-point the drift guard's second half (reserved-columns.test.ts:31-46, which today reads `PRAGMA table_info(contact_custom_values)`) to `custom_field_values` — after migration 006 the legacy table is DROPPED, and `PRAGMA table_info` on a nonexistent table returns ZERO rows, so left unchanged that half of the guard passes VACUOUSLY. If a live fixed-column drift check on the normalized value table is not meaningful, DELETE that half with a one-line stated reason instead — do not leave it silently vacuous. Retain the existing collision-prevention contract for fixed columns and prove it against the migrated schema. This implements D-05 and CFN-04; do not change definition naming UX.</action>
  <verify><automated>npm test -- src/db/reserved-columns.test.ts && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Tests prove reserved names are a superset of relevant v6 fixed columns, seeded through migration 006.
    - The drift guard reads a table that still exists at v6 (custom_field_values), not the dropped contact_custom_values — no vacuous zero-row pass (or that half is deleted with a stated reason) (LOW-9).
    - No test fixture depends on altering contact_custom_values after migration006.
  </acceptance_criteria>
  <done>Schema guards correctly represent normalized storage and its naming constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Archived-contact purge → children/files | Destructive database work precedes best-effort post-commit filesystem cleanup. |
| Schema metadata → field naming | Version-specific fixed columns must be guarded without treating user fields as physical columns. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-12 | Tampering | normalized child purge | medium | mitigate | Explicit multi-row deletion and rollback regressions through migration 006. |
| T-16-13 | Tampering | reserved names | low | mitigate | Schema-derived v6 discovery tests. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2); node:sqlite is available.
- Run named purge/schema tests and `npx tsc --noEmit` (project green from Plan 02).
- Fixtures seed via migrations 001..006 or direct INSERTs into custom_field_defs/custom_field_values — NOT via createField or another sibling wave-3 DAO — so the four wave-3 plans (03/04/05/06) are order-independent within the wave.
</verification>

<success_criteria>
CFN-02/04 deletion and schema consumers understand the normalized child model and retain custom-photo and
archive safety behavior; purge coverage is genuine v6 coverage.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-06-SUMMARY.md` when done. Record the
purge-photo-cleanup.test.ts v1-pin caveat.
</output>
