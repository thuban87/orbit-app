---
phase: 16-custom-field-value-normalization
plan: 07
type: execute
wave: 4
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05, 16-06]
files_modified:
  - src/db/migrations/006-normalize-custom-field-values.test.ts
  - .planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md
autonomous: false
requirements: [CFN-01, CFN-02, CFN-03, CFN-04]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A real populated v5 fixture proves migration, normalized lifecycle, profile/edit, AI, custom-photo, quarantine→create→restore, and history behavior together; parameterized v1/v4/v5 upgrades all reach v6 cleanly."
    - "An upper-bound fixture (200 contacts × 15 definitions) migrates and its wall-clock is recorded, bounding the O(C·D) row-by-row cost that sits behind the bare spinner."
    - "The release APK proves a SILENT populated-data upgrade and preserved custom-field behavior; a separate DEBUG APK + Metro proves the loss-bearing fail-closed rollback AND the D-06a orphan-column snapshot-and-proceed, each with the correct APK variant named."
    - "Final evidence explicitly leaves backup/export/restore, sync transport, conflict policy, tombstones, identity UI, and custom-field redesign out of Phase 16."
  artifacts:
    - src/db/migrations/006-normalize-custom-field-values.test.ts
    - .planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md
  key_links:
    - "The all-path fixture runs migrations 001–006 through the real runner, then exercises the DAOs consumed by forms/profile/AI."
    - "The release-build success run and the debug-build failure run each cite the desktop-build-pipeline runbook section they use."
---

<objective>
Complete the cross-cutting proof matrix and the required physical Pixel upgrade UAT after every normalized
producer and consumer is implemented — split across the correct release/debug APK variants.

Purpose: unit paths cannot alone prove that real pre-006 device data survives a forward-only SQLite
migration with its established field experience intact; and the failure path physically cannot run on a
release APK.
Output: one rich automated upgrade regression (all-path + parameterized + upper-bound) and a signed
owner-facing device UAT record with a release success run and a debug failure run.

Addresses review concerns: HIGH release-APK malformed-fixture impossibility (split into release §1 + debug
§2/§3 runs, variants named), MEDIUM v5-centric proof (parameterized v1/v4/v5), MEDIUM unbounded migration
cost (200×15 upper-bound fixture + wall-clock), MEDIUM "sort/filter parity" unperformable observation
(removed — no UI exposes custom-field sort/filter; replaced with observable surfaces).
</objective>

## Artifacts this phase produces (owned by this plan)

- **`006-normalize-custom-field-values.test.ts` (expanded):** the all-path v5→v6 matrix, parameterized v1/v4/v5→v6 upgrades, the 200×15 upper-bound fixture with a recorded duration, and the isolated loss-bearing / D-06a-orphan / negative-identifier rollback cases.
- **`16-UPGRADE-UAT.md`:** the signed device record — a release-APK success run and a debug-APK failure run, each naming its runbook section, plus the explicit Phase-17-out-of-scope confirmation.

<execution_context>
@/home/bwales/.codex/gsd-core/workflows/execute-plan.md
@/home/bwales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-field-value-normalization/16-CONTEXT.md
@.planning/phases/16-custom-field-value-normalization/16-RESEARCH.md
@.planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md
@.planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
@docs/runbooks/desktop-build-pipeline.md
@HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the populated-profile migration and lifecycle proof matrix (all-path + parameterized + upper-bound)</name>
  <reversibility rating="reversible">This task expands automated evidence only; it does not change the migrated representation or user data contract.</reversibility>
  <read_first>
    - src/db/migrations/006-normalize-custom-field-values.test.ts (Plan 01 migration/rollback/orphan/negative-identifier fixtures)
    - src/db/field-values-dao.test.ts, src/db/contacts-dao.test.ts, src/db/field-ddl.test.ts, src/db/field-type-change.test.ts, src/db/field-sort.test.ts
    - src/services/field-sweep.test.ts, src/db/contact-read.test.ts, src/db/ai-context-read.test.ts, src/db/purge-dao.test.ts
    - src/db/migrations/runner.ts (runMigrations applies pending steps from the current version)
    - .planning/phases/16-custom-field-value-normalization/16-VALIDATION.md
  </read_first>
  <files>src/db/migrations/006-normalize-custom-field-values.test.ts</files>
  <behavior>
    - The all-path v5 fixture seeds all seven definition types, populated and blank values, NULL and empty text, unusual raw number/toggle text, a custom-photo relative path, an archived contact, a contact with no legacy row, and a quarantined definition, then proves create/edit/clear (UPSERT-on-pair), pair uid uniqueness, type-change preflight/history, quarantine/restore/strict expiry/permanent deletion, profile/edit compatibility, AI privacy projection, purge, and field_history key preservation after a real upgrade.
    - A quarantine→create→restore case proves no missing pair row (the HIGH invariant) end-to-end through the real DAOs.
    - Parameterized upgrades from v1, v4, and v5 starting versions each reach v6 and register/bootstrap cleanly (the forward-only "any version may jump to v6" rule).
    - An upper-bound fixture of 200 contacts × 15 definitions migrates successfully and its wall-clock duration is measured and recorded (bounding the O(C·D) row-by-row cost).
    - Isolated structural/value-correspondence, D-06a orphan-column, and negative-identifier cases remain.
  </behavior>
  <action>Expand the real v5 fixture rather than duplicating focused tests. Add the quarantine→create→restore end-to-end assertion. Add parameterized runMigrations upgrades from representative v1, v4, and v5 databases proving each reaches v6. Add a 200×15 upper-bound fixture that migrates and captures elapsed wall-clock (record the number in the summary and the UAT; if it is more than a few seconds, flag it for the owner as a spinner conversation — the UI-SPEC mandates a bare ActivityIndicator with no progress affordance). Assertions must prove no source byte is rewritten and must state that Phase 17 backup/tombstone/reconciliation and later conflict policy are not decided here. Keep the isolated loss-bearing rollback, D-06a orphan-proceed, and negative-identifier cases. This completes D-07, D-10, and D-11.</action>
  <verify><automated>npm test && npx tsc --noEmit && npm run check:colors</automated></verify>
  <acceptance_criteria>
    - The fixture proves every required type/value/lifecycle/profile/AI/photo/purge case after a real upgrade, including quarantine→create→restore.
    - Parameterized v1/v4/v5 upgrades each reach v6; the 200×15 upper-bound fixture migrates and its duration is recorded.
    - Failure cases prove rollback (loss-bearing / negative-identifier) with unchanged version, and the orphan case proves snapshot-and-proceed to v6.
    - The FULL suite passes plus tsc + check:colors (the wave-3/4 green gate; the per-module test suites for field-ddl/field-defs-dao/field-type-change/field-sort/purge-dao/reserved-columns/contact-read/ai-context-read all went green as their wave-3 plans landed).
    - Legacy-table completeness grep gate (deferred here from Plan 02): `rg -n 'contact_custom_values' src/ --glob '!**/migrations/001-initial*' --glob '!**/migrations/006-normalize-custom-field-values*' --glob '!**/*.test.ts'` prints ONLY comment lines (each begins with `*`, `//`, `--`, or `/*`) — no executable `SELECT`/`INSERT`/`UPDATE`/`DELETE`/`FROM`/`ALTER`/`DROP` reference to the retired table remains in runtime code outside the two migrations.
  </acceptance_criteria>
  <done>Automated CFN-01 through CFN-04 evidence covers a realistic upgrade, parameterized versions, and a bounded upper-scale run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: RELEASE-APK success run — silent populated-data upgrade on the Pixel</name>
  <what-built>Migration 006, lifecycle, read projections, and the automated suite (all-path + parameterized + upper-bound) have passed.</what-built>
  <how-to-verify>Build and install the RELEASE APK over populated pre-006 test-profile data via runbook §1 (desktop `droid` build → Pixel), and record PASS/FAIL for each observable surface.</how-to-verify>
  <precondition>The full Node 22 suite is green, populated pre-006 test-profile data exists on the Pixel, and docs/runbooks/desktop-build-pipeline.md §1 is operational.</precondition>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (§1 release-APK standalone build/install)
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md (silent-success + preserved surfaces)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-07 and D-08)
  </read_first>
  <files>.planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md</files>
  <action>Create the UAT record before testing (device/build identifiers, pre-upgrade state). Using runbook §1, install the migration-006 RELEASE APK over populated pre-006 test-profile data (a release APK launches standalone; it is the correct variant for the silent-success proof). Record PASS/FAIL with evidence for: silent launch (no migration/progress/success UI); all seeded values and blanks display correctly on the profile and edit form; the custom photo renders; create/edit/clear a value round-trips; an invalid retyped value shows Tap to fix; quarantine → restore round-trips; permanently deleting an empty field; the AI assembled-prompt inspector includes a share_with_ai field and EXCLUDES a non-shared and a quarantined field; long label/option/value wrapping shows without clipped recovery/action text; and record the observed upgrade wall-clock (approximate). Verify there is NO uid, field_def_id, col_name, table, migration-version, backup, sync, or conflict UI anywhere. Do NOT attempt the malformed-fixture failure path here (a release APK is not run-as-debuggable — that is Task 3). Do not use personal data. Do not add backup, sync, conflict policy, tombstones, identity display, or UI redesign. DROPPED observation (review): custom-field "sort/filter parity" — no UI exposes a custom-field sort or filter in this app, so it is unperformable and is intentionally NOT a UAT observation.</action>
  <verify><human-check>Owner or executor signs every release-run scenario with evidence in 16-UPGRADE-UAT.md.</human-check></verify>
  <acceptance_criteria>
    - The record captures PASS/FAIL evidence for each observable success-upgrade, lifecycle, and visual-preservation scenario, plus the observed upgrade duration.
    - Safe success is silent; no schema/identity/backup/sync UI appears.
    - The record names the runbook §1 release build variant used.
  </acceptance_criteria>
  <done>A physical Pixel has silently upgraded populated data on the release APK with evidence recorded.</done>
  <resume-signal>Record PASS with evidence, or record the failing scenario and stop for remediation.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: DEBUG-APK failure run — loss-bearing fail-closed rollback + D-06a orphan proceed</name>
  <what-built>The release success run (Task 2) has passed; the failure path requires a run-as-debuggable build.</what-built>
  <how-to-verify>Build and install a DEBUG APK + Metro via runbook §2, and use `run-as` to write malformed legacy fixtures into the app data dir BEFORE launch (runbook §3 caveat). Prove the classified fail-closed rollback and the D-06a orphan proceed on a DISPOSABLE test profile only.</how-to-verify>
  <precondition>A DEBUG APK (`assembleDebug`) + live Metro + `adb reverse tcp:8081` are available per runbook §2, and `run-as com.bwales.orbit` works (DEBUG builds only — runbook §3.1). Use a DISPOSABLE test profile; never personal data.</precondition>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (§2 debug+Metro loop; §3.1 "app-release.apk is NOT run-as-debuggable — data-layer inspection works only on a DEBUG APK")
    - .planning/phases/16-custom-field-value-normalization/16-UI-SPEC.md (classified failure copy, revised per D-06a)
    - .planning/phases/16-custom-field-value-normalization/16-CONTEXT.md (D-06, D-06a)
  </read_first>
  <files>.planning/phases/16-custom-field-value-normalization/16-UPGRADE-UAT.md</files>
  <action>On a DISPOSABLE test profile with the DEBUG APK from runbook §2 (Metro live, adb reverse set), use `run-as com.bwales.orbit` to seed a pre-006 database, then run two failure-path observations. (a) LOSS-BEARING: inject a malformed legacy fixture where a definition's backing value column is MISSING; launch; confirm the app renders the REVISED classified failure copy (heading/body per the updated UI-SPEC — no "contact support" promise), the navigator stays unmounted, and — via `run-as` sqlite inspection — user_version is still 5 and contact_custom_values and its data are byte-unchanged after rollback. (b) D-06a ORPHAN (non-loss): inject a pre-006 database with an extra dynamic column that has NO matching definition; launch; confirm the app opens SILENTLY (no classified failure), reaches v6, the orphan's data is present in field_history, and the rest of the custom-field data is intact. Record device/build identifiers and PASS/FAIL evidence (including the run-as DB inspection output) for both. Name the runbook §2 debug build + §3.1 run-as caveat explicitly in the record. Do not add backup, sync, conflict policy, tombstones, identity display, or UI redesign.</action>
  <verify><human-check>Owner or executor signs both failure-run observations with run-as evidence in 16-UPGRADE-UAT.md.</human-check></verify>
  <acceptance_criteria>
    - The loss-bearing observation proves the revised classified copy (no support-channel promise), an unmounted navigator, and — via run-as inspection — user_version 5 with byte-unchanged legacy data.
    - The D-06a orphan observation proves a silent proceed to v6 with the orphan data in field_history and other data intact.
    - The record names the DEBUG (`assembleDebug`) + Metro variant and cites runbook §3.1's run-as caveat.
    - The record explicitly confirms the phase did not decide Phase 17 backup/sync conflict work or redesign the custom-field UI.
  </acceptance_criteria>
  <done>A physical Pixel has proven both the fail-closed rollback and the D-06a orphan proceed on the correct debug build, with evidence ready for phase verification.</done>
  <resume-signal>Record PASS with run-as evidence for both observations, or record the failing scenario and stop for remediation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Node fixture → migration proof | Fixture must model real v5 edge cases, parameterized prior versions, and an upper-bound scale rather than fresh/empty state. |
| Release build → device database | Forward-only schema migration crosses into persistent on-device data (silent success proof). |
| Malformed fixture → user data | Failure-path testing runs only against a disposable test profile on a run-as-debuggable DEBUG build. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-14 | Tampering | cross-cutting migration proof | high | mitigate | Rich runner fixture, parameterized versions, upper-bound scale, rollback/orphan matrix, and full suite. |
| T-16-15 | Tampering | physical device upgrade | high | mitigate | Blocking populated-data Pixel release-APK success run before verification. |
| T-16-16 | Information disclosure | failure-path UAT | medium | mitigate | Isolated disposable profile on a debug build; data-free classified copy; run-as inspection proves rollback. |
| T-16-SC | Tampering | package installation | low | accept | No packages are installed. |
</threat_model>

<verification>
- Runtime is Node 22 (confirmed v22.22.2); node:sqlite is available. Run the full suite, tsc, and check:colors before the checkpoints.
- The signed UAT record (both a release success run and a debug failure run) must PASS before `$gsd-verify-work`.
</verification>

<success_criteria>
CFN-01 through CFN-04 are complete only when automated proof (all-path + parameterized + upper-bound) and a
populated-data physical Pixel upgrade — a silent release-APK success run and a debug-APK fail-closed +
D-06a-orphan run — all demonstrate lossless, correctly-classified normalization with unchanged custom-field
behavior.
</success_criteria>

<output>
Create `.planning/phases/16-custom-field-value-normalization/16-07-SUMMARY.md` when done. Record the 200×15
upgrade wall-clock and both device-run variants.
</output>
