---
phase: 19-system-contact-import
plan: 06
type: execute
wave: 4
depends_on: [19-01, 19-03, 19-04, 19-05]
files_modified:
  - src/services/import/import-driver.ts
  - src/services/import/import-driver.test.ts
  - src/screens/BulkImportSetupScreen.tsx
  - src/screens/ImportProgressScreen.tsx
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements: [IMP-02, IMP-03]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After the picker returns N>1 selections, the FAB routes to BulkImportSetup (route registered here) and bulk setup shows a selection count sourced from `sessionRowCounts.pending` (so it stays correct after plan 11 pre-batch consolidation, never the stale `total_rows`), the two locked read-only defaults (Unbound, Uncategorized), a single batch Category override, and one `Import N contacts` CTA — and NO per-person checkbox/exclusion list (IMP-02, Cluster E). The FAB→BulkImportSetup entry is verified end-to-end here (addresses review HIGH bulk entry never wired; cycle-4 LOW count-source)."
    - "The chunked driver processes rows incrementally in one logical import; each safe row is imported via importContactRecord with resolveRow so the contact AND the row's imported/contact_id transition are ONE atomic transaction; a single row failure (or photo failure) marks that row failed/photo_failed and the batch continues — it never rolls back the whole batch (IMP-02/IMP-04, Cluster P; addresses review HIGH atomic contact+session-row write)."
    - "A bulk row whose mapped name is blank/whitespace (mapPickedContact.nameRequired) is NEVER imported as a blank-named contact: the shared importRowAsNew seam checks nameRequired and routes such a row to a terminal 'failed' (name-required reason) instead of calling importContactRecord — consistent with single-import's block-on-empty-name (Cluster O). The gate lives at the SINGLE importRowAsNew chokepoint, so the driver's 'new' branch and plan 07's bulk 'Import as New' are both covered; importContactRecord's own NameRequiredError throw (plan 03) is the defense-in-depth backstop (IMP-02; addresses cycle-4 HIGH 1 — bulk had no name-required gate)."
    - "Before importing, each row is scored by the duplicate engine (with the session's phone_region), and EVERY classification transition that writes more than one column is written in ONE transaction via plan 01's composed cores — never two separately-mutexed writers: a deterministic 'already_linked' row goes through `resolveAlreadyLinked(row.id, deterministicContactId, now)` (row_status='skipped' AND match_outcome='already_linked' AND matched_contact_id in one UPDATE, so a process death can never leave skipped+match_outcome NULL); a 'new' row imports as Unbound via `importRowAsNew` (row_status='imported', match_outcome='new', contact atomic); an ambiguous row (probable/possible/needs_review) goes through `deferNeedsReview(row.id, outcome, matchedContactId, candidatesJson, now)` (row_status='needs_review' AND match_outcome AND matched_contact_id AND candidates_json in one UPDATE) so it appears in the review grid WITH its full candidate list — safe imports are never blocked by ambiguous ones (IMP-03, Cluster L/N; addresses cycle-2 HIGH 1 dual-field non-atomicity + HIGH 2 durable candidates + the original count-semantics/pending-vs-needs_review fixes)."
    - "runImportBatch accepts an explicit eligibleStatuses set (default ['pending']); Retry (plan 08) passes ['pending','failed'] so failed rows are actually re-processed, with a deterministic-bypass + contact_id idempotency guard preventing a double import (IMP-04; addresses review HIGH retry cannot reach failed rows)."
    - "The driver reads import_sessions.phone_region and threads it as effectivePhoneRegion into scoreImportCandidate AND the mapper, so bulk-imported national-format numbers normalize canonically (IMP-03; addresses review MEDIUM phone-region dropped)."
    - "BulkImportSetup DURABLY persists the chosen batch category to import_sessions.batch_category_id (via setSessionBatchCategory, plan 01) at the Import CTA, and the driver reads it back from the session (getSessionById) as the source of truth when the caller omits batchCategoryId — so the user's category survives the live pass, Retry, bulk Import-as-New, AND process death rather than reverting to Uncategorized (IMP-02/IMP-04; addresses cycle-3 HIGH batch-category durability)."
    - "The progress screen shows determinate `Importing… X of N` (accent tint) plus a sub-status ('Photos may finish after contacts'), never a bare spinner, and never exposes chunk internals as separate imports (Cluster S)."
    - { statement: "Bulk `Import N contacts` and progress copy pluralize correctly (1 contact vs N contacts)", verification: backstop }
    - "[ASSUMPTION | spec-less edge IMP-02/boundary] The platform picker caps a single selection at ≤100 (default 50); Orbit imposes NO app-level cap — a user importing more runs the picker again; the progress screen is sized for one pick's N, not thousands from one call (Pitfall 5)."
  artifacts:
    - "src/services/import/import-driver.ts — runImportBatch(exec, { sessionId, now, onProgress, eligibleStatuses, batchCategoryId? }): reads the session via getSessionById for phone_region + the DURABLE batch_category_id (used when batchCategoryId is omitted) → score (with session phone_region) → classify per the plan-01 transition table (composed resolveAlreadyLinked/deferNeedsReview writers) → chunked per-row ATOMIC import with progress callback + partial-failure accounting + idempotency guard"
    - "src/services/import/import-driver.ts — importRowAsNew(exec, { row, batchCategoryId, phoneRegion, now }): the SHARED per-row 'import one row as new' seam (maps the row → NAME-REQUIRED GATE: if mapPickedContact.nameRequired, mark row 'failed' (name-required) and create no contact; else importContactRecord{ birthday: mapped.birthday, resolveRow:{matchOutcome:'new'} }), fixed signature, whose body plan 10 enriches with the post-commit photo step; the driver's 'new' branch AND plan 07's bulk 'Import as New' both call it, so the review-grid import path is neither a photo bypass (cycle-2 HIGH 3) nor a blank-name bypass (cycle-4 HIGH 1)"
    - "src/screens/BulkImportSetupScreen.tsx — shared-defaults setup + batch category override (consolidation offer added by plan 11)"
    - "src/screens/ImportProgressScreen.tsx — determinate one-logical-import progress"
    - "BulkImportSetup + ImportProgress REAL screens replace their plan-04 themed placeholders (their param keys were declared up front by plan 04; this plan adds no new param key and does not edit navigation/types.ts)"
    - "the driver calls scoreImportCandidate (plan 05, with session phone_region) per row and writes outcomes via the plan-01 transition table using the COMPOSED single-transaction cores (resolveAlreadyLinked / deferNeedsReview) so outcomes + candidates are durable for the grid and the four completion buckets — never two sequential mutexed writers"
    - "each safe import is one importRowAsNew call → one importContactRecord({resolveRow}) transaction resolving contact + row together; photo persistence is inside importRowAsNew's body (enriched by plan 10, post-commit), so a photo failure only sets photo_failed"
    - "already_linked is written by resolveAlreadyLinked (row_status='skipped' AND match_outcome='already_linked' in one UPDATE) — the discriminator sessionSummaryCounts (plan 01) uses to count 'Already in Orbit' apart from user Skip, now crash-atomic"
    - "ambiguous rows are written by deferNeedsReview, which persists candidates_json (JSON.stringify of scoreImportCandidate's ordered candidates[]) so plan 07's grid renders every candidate after resume"
  prohibitions:
    - "Do not render a per-person checkbox/exclusion list under bulk defaults (Cluster E)."
    - "Do not wrap the batch in one giant transaction; one atomic transaction per contact (contact + row resolution together) (Cluster P)."
    - "Do not mark an already_linked row 'skipped' WITHOUT also setting match_outcome='already_linked' — that reintroduces the count-semantics collision the review flagged."
    - "Do not write already_linked or needs_review as two sequential mutexed writers (markRowStatus + setRowMatchOutcome); use the composed single-transaction resolveAlreadyLinked/deferNeedsReview (cycle-2 HIGH 1) so a crash between the two columns cannot miscount the row."
    - "Do not leave ambiguous rows as bare 'pending'; set row_status='needs_review' so the grid loads them, and persist candidates_json so the grid can render every candidate."
    - "Do not block safe imports on ambiguous candidates; ambiguous rows stay for review while safe rows import."
    - "Do not set EXTRA_PICK_CONTACTS_SELECTION_LIMIT above 100 or design progress UI implying one pick returns thousands."
    - "Do not seed/select an 'Uncategorized' category; the batch default is category_id null."
    - "Do not call importContactRecord for a bulk row without first checking mapPickedContact.nameRequired; a nameless row is routed to 'failed', never imported as a blank-named contact (cycle-4 HIGH 1)."
---

<objective>
Deliver bulk import: the chunked, atomic, partial-failure-safe driver plus the lightweight shared-defaults setup and the determinate progress screen — the IMP-02 batch path, layered on the verified single-contact tracer, writing against plan 01's canonical row state-transition table.

Purpose: Bulk is intentionally lighter than single review (per-person selection already happened in the system picker). The driver's per-row ATOMIC transaction (contact + row resolution) + duplicate-first classification with correct status/outcome writes gives Cluster P/L/N safety and makes the completion summary computable; the setup/progress screens keep it one calm logical import; and this plan verifies the FAB→BulkImportSetup entry the tracer wired.
Output: import-driver service (with eligibleStatuses + region) + BulkImportSetupScreen + ImportProgressScreen, wired to plan 01/03/04/05 primitives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/services/import/import-acquire.ts
@src/services/import/duplicate-evidence.ts
@src/db/imported-contact-dao.ts
@src/db/import-session-dao.ts
@src/db/import-session-read.ts
@src/screens/CreateContactScreen.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex/Cursor/Claude) — count-semantics collision.** The driver now writes `match_outcome='already_linked'` alongside `row_status='skipped'` for deterministic already-linked rows, per plan 01's transition table, so `sessionSummaryCounts` counts them under "Already in Orbit" distinctly from user Skip.
- **HIGH (Codex/Cursor) — ambiguous stays pending vs review expects needs_review.** The driver now sets `row_status='needs_review'` (plus match_outcome) on ambiguous rows so the grid loads them.
- **HIGH (Codex/Cursor/Claude) — non-atomic contact + row write.** Each safe import is `importContactRecord({ resolveRow })` — one transaction resolving contact + row.
- **HIGH (Codex/Cursor) — retry can't reach failed rows.** `runImportBatch` takes `eligibleStatuses` (default ['pending']; Retry passes ['pending','failed']) with an idempotency guard.
- **HIGH (Codex/Cursor/Claude) — bulk entry never wired.** The FAB (plan 04) routes N>1 → BulkImportSetup; this plan registers that screen/route and verifies the entry end-to-end.
- **MEDIUM (Cursor/Claude) — phone region dropped.** The driver reads `session.phone_region` and threads it into scoring + mapping.
- **MEDIUM (Codex) — no abort policy once a batch starts.** The progress screen owns a mounted-guard so a completed/failed row callback to an unmounted screen is a no-op (documented in Task 3).

### Cycle-2 review incorporated
- **HIGH 1 (dual-field row writes not atomic).** The driver now writes already_linked via `resolveAlreadyLinked` and ambiguous via `deferNeedsReview` (plan 01's composed single-transaction cores), so a kill between the two columns can no longer leave `row_status='skipped'` with `match_outcome IS NULL` (the reopened count-miscount window).
- **HIGH 2 (durable candidates).** `deferNeedsReview` persists `candidates_json = JSON.stringify(result.candidates)` so a resumed/bulk-reviewed needs_review row renders every candidate.
- **HIGH 3 (photo bypass on the review-grid import path).** Extracted the shared `importRowAsNew` seam (fixed signature, photo enriched by plan 10) that both the driver's 'new' branch and plan 07's bulk 'Import as New' call.

### Cycle-3 review incorporated
- **HIGH (Cursor/Claude) — bulk category override not durable.** BulkImportSetup now calls `setSessionBatchCategory` (plan 01) at the Import CTA to durably persist the chosen override to `import_sessions.batch_category_id` (existing migration-012 column — no schema change) BEFORE navigating; `runImportBatch` reads the session via `getSessionById` and uses the durable `batch_category_id` when the caller omits `batchCategoryId`. So the live pass, Retry (plan 08), and bulk Import-as-New (plan 07) all read the user's category from the session row, never a lost nav param.

### Cycle-4 review incorporated
- **HIGH 1 (Codex/Claude) — bulk import had no name-required gate; a nameless picked row silently became a blank-named contact** (`contacts.name` is TEXT NOT NULL with no non-empty CHECK — 001-initial.ts:65). Fixed at the SINGLE shared `importRowAsNew` seam: it checks `mapPickedContact(...).nameRequired` and routes a nameless row to a terminal `failed` (name-required reason) instead of calling importContactRecord. GENERALIZED: because both the driver's 'new' branch AND plan 07's DuplicateReview bulk 'Import as New' call this one seam, neither can bypass the gate; plan 03's `importContactRecord` NameRequiredError throw is the defense-in-depth backstop; plan 11's `combineCluster` gets its own nameless-cluster refusal — so no create-from-picked sibling can reintroduce the blank-name class.
- **MEDIUM (Cursor/Claude) — birthday handoff.** `importRowAsNew` now explicitly sources `birthday` from `mapPickedContact(...).birthday` (plan 03's return field), so bulk imports never drop the picked birthday.
- **LOW (carried from cycle 3) — BulkImportSetup selection-count source ambiguous.** Pinned to `sessionRowCounts.pending` (not `total_rows`), so the count stays correct after plan 11 pre-batch consolidation resolves clustered rows out of `pending`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: import-driver — chunked, duplicate-first, ATOMIC, partial-failure batch import with eligibleStatuses</name>
  <files>src/services/import/import-driver.ts, src/services/import/import-driver.test.ts</files>
  <behavior>
    - runs rows (whose row_status is in eligibleStatuses, default ['pending']) in chunks of CHUNK_SIZE, emitting onProgress(done, total)
    - reads the session (getSessionById) for phone_region AND the durable batch_category_id, threads phone_region into scoreImportCandidate + mapPickedContact, and uses session.batchCategoryId as the batch category when the caller omits batchCategoryId (so Retry / live-pass never lose the user's choice)
    - per row: score → already_linked ⇒ resolveAlreadyLinked(row.id, deterministicContactId, now) (skipped + already_linked + matched_contact_id in ONE transaction); new ⇒ importRowAsNew(exec, { row, batchCategoryId, phoneRegion, now }) (which calls importContactRecord{resolveRow:{matchOutcome:'new'}} — atomic contact+row); ambiguous ⇒ deferNeedsReview(row.id, outcome, matchedContactId, JSON.stringify(candidates), now) (needs_review + match_outcome + matched_contact_id + candidates_json in ONE transaction)
    - a row whose mapped name is blank/whitespace (mapPickedContact.nameRequired) is NOT imported as a blank contact — importRowAsNew marks it terminal 'failed' with a name-required reason and creates no contact (Cluster O; cycle-4 HIGH 1)
    - a thrown importContactRecord marks that row 'failed' with a reason; the loop continues (no batch rollback)
    - Retry mode (eligibleStatuses includes 'failed') re-processes failed rows; a row already carrying a contact_id or a deterministic already_linked hit is not re-imported (idempotency)
    - final accounting returns the four buckets from sessionSummaryCounts
  </behavior>
  <read_first>src/services/import/duplicate-evidence.ts (scoreImportCandidate({effectivePhoneRegion}) returning {outcome, candidates[], deterministicContactId} — plan 05), src/db/imported-contact-dao.ts (importContactRecord({resolveRow}) — plan 03), src/db/import-session-dao.ts (the COMPOSED single-transaction writers resolveAlreadyLinked/deferNeedsReview + markRowStatus + the canonical transition table header — plan 01; use the composed writers for the two multi-column transitions, never two sequential mutexed calls), src/db/import-session-read.ts (getSessionById — the session row incl. phone_region AND the durable batch_category_id; listSessionRows/sessionRowCounts/sessionSummaryCounts — plan 01), src/logic/picked-contact-map.ts (mapPickedContact({effectivePhoneRegion}) — plan 03), .planning/phases/19-system-contact-import/19-RESEARCH.md (Code Example 3 chunked driver, Cluster P)</read_first>
  <action>First extract the shared per-row 'import as new' seam: `importRowAsNew(exec, { row, batchCategoryId, phoneRegion, now })` — maps the row's source_payload via `const mapped = mapPickedContact(payload, { categoryId: batchCategoryId, effectivePhoneRegion: phoneRegion })`. NAME-REQUIRED GATE (cycle-4 HIGH 1): if `mapped.nameRequired` is true (the picked row has no usable name — e.g. an email-only or organization-only entry), the seam does NOT create a contact — it transitions the row to a TERMINAL `failed` via `markRowStatus(row.id, 'failed', 'name-required')` (mirroring single-import's block-on-empty-name behavior, Cluster O) and returns a discriminated no-import result (e.g. `{ contactId: null, skipped: 'name-required' }`), so `contacts.name` (TEXT NOT NULL, no non-empty CHECK — 001-initial.ts:65) never receives a blank string. Otherwise it calls `importContactRecord({ input: mapped.input, externalLinks:[{provider:'android', externalContactId: row.externalContactId}], birthday: mapped.birthday, now, resolveRow:{ rowId: row.id, matchOutcome:'new' } })` (atomic contact+row; `birthday` is sourced from `mapped.birthday` — the return field plan 03 exposes, since CreateContactFullInput has no birthday column) and returns { contactId }. Because BOTH the driver's 'new' branch AND plan 07's DuplicateReview bulk 'Import as New' call this one seam, the name gate is enforced at a SINGLE chokepoint — no bulk create-from-picked sibling can bypass it. Its signature is FIXED here; plan 10 enriches its BODY with the post-commit photo step (reading row.photoRelPath), and plan 07's bulk 'Import as New' calls this same seam — so no contact-creation path bypasses the photo hook (cycle-2 HIGH 3). Then write `runImportBatch(exec, { sessionId, now, onProgress, eligibleStatuses = ['pending'], batchCategoryId })` where `batchCategoryId` is OPTIONAL. Read the session via `getSessionById(exec, sessionId)` (plan 01) for BOTH phone_region AND the DURABLE batch_category_id, and use `session.batchCategoryId` as the source of truth for the batch category when the caller omits `batchCategoryId` (the caller almost always omits it — the durable session value persisted at setup-confirm is authoritative; a passed value is only ever an in-pass override). This is the cycle-3 HIGH fix: Retry (plan 08) and the live progress pass both call runImportBatch with only `{ sessionId, ... }` and get the user's chosen category from the session row, never a lost nav param. Then load rows whose row_status is in eligibleStatuses via listSessionRows (filter). Iterate in chunks of CHUNK_SIZE (top-of-file tunable) with a yield between chunks (keep the JS thread responsive — Cluster S). For each row: call scoreImportCandidate(exec, { externalContactId, methodDrafts, name, birthday, effectivePhoneRegion: session.phoneRegion }); classify per plan 01's transition table, writing each multi-column transition through plan 01's COMPOSED single-transaction cores (never two sequential mutexed writers — cycle-2 HIGH 1):
- 'already_linked' → `resolveAlreadyLinked(row.id, result.deterministicContactId, now)` (row_status='skipped' AND match_outcome='already_linked' AND matched_contact_id in ONE transaction — Cluster N: skipped-but-already-linked, NOT refreshed);
- 'new' → `importRowAsNew(exec, { row, batchCategoryId: session.batchCategoryId, phoneRegion: session.phoneRegion, now })` (atomic contact+row via importContactRecord resolveRow; the category comes from the durable session row, not a nav param);
- else (probable/possible/needs_review) → `deferNeedsReview(row.id, result.outcome, result.candidates[0]?.contactId ?? null, JSON.stringify(result.candidates), now)` (row_status='needs_review' AND match_outcome AND matched_contact_id AND candidates_json in ONE transaction) and leave for the grid.
Wrap each per-row import in try/catch → on throw markRowStatus(row,'failed', classify(err)); the batch continues. Idempotency: before importing, if the row already has a contact_id or scoreImportCandidate returns already_linked, do not create a new contact. Call onProgress after each row. Return sessionSummaryCounts. All within existing transaction primitives (never one batch transaction).</action>
  <verify>
    <automated>npx vitest run src/services/import/import-driver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test with a mixed batch (one new, one already-linked, one ambiguous, one that throws) asserts: 1 imported (row imported + contact_id set atomically), 1 skipped WITH match_outcome='already_linked' (both columns set — assert neither is left NULL), 1 needs_review (row_status set AND candidates_json populated with the ordered candidate list), 1 failed, and that the failure did not roll back the imported contact.
    - Test "already_linked and needs_review transitions are each a single UPDATE" — assert the driver calls resolveAlreadyLinked/deferNeedsReview (composed writers), not a markRowStatus+setRowMatchOutcome pair, for these two branches (e.g. a spy/mock asserting the composed writer is used, or a source grep that the branch does not call two separate mutexed writers).
    - Test "a bulk row with a blank/whitespace mapped name is marked 'failed' (name-required) and creates ZERO contacts" passes — assert no contacts row exists with an empty name after the batch and the row's row_status='failed' (cycle-4 HIGH 1 — bulk never persists a blank-named contact).
    - Test "an ambiguous row persists candidates_json and listSessionRows reads back every candidate" passes.
    - Test "Retry with eligibleStatuses ['pending','failed'] re-processes a failed row and does not double-import a row that already has a contact_id" passes.
    - Test asserts scoreImportCandidate + mapPickedContact (via importRowAsNew) receive the session's phone_region.
    - Test "runImportBatch called with only { sessionId } (no batchCategoryId) imports rows under the session's durable batch_category_id" passes (set it via setSessionBatchCategory, then run with the arg omitted, assert the created contact carries that category) — the cycle-3 category-durability fix.
    - onProgress is called once per processed row; a fixture asserts monotonically increasing done up to total.
    - `grep -n "CHUNK_SIZE\|eligibleStatuses\|importRowAsNew\|getSessionById" src/services/import/import-driver.ts` matches; `grep -n "resolveAlreadyLinked\|deferNeedsReview" src/services/import/import-driver.ts` matches.
    - `npx vitest run src/services/import/import-driver.test.ts` exits 0.
  </acceptance_criteria>
  <done>The driver imports safe rows atomically and incrementally, marks already-linked with the match_outcome discriminator, defers ambiguous as needs_review, isolates failures, supports Retry via eligibleStatuses, threads the region, and reports the four buckets — all node-verified.</done>
</task>

<task type="auto">
  <name>Task 2: BulkImportSetupScreen — shared defaults + batch category override (+ verified FAB entry)</name>
  <files>src/screens/BulkImportSetupScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #3; Copywriting: `Import N contacts`; accent-selected chip idiom), src/services/import/import-acquire.ts (routePickedImport → navigates BulkImportSetup — plan 04), src/screens/CreateContactScreen.tsx (pickerShell category select idiom), src/navigation/RootNavigator.tsx (Stack.Screen registration; the `BulkImportSetup` ImportRoutePlaceholder registration to REPLACE), src/navigation/types.ts (read-only: `BulkImportSetup` param already declared by plan 04 — do not edit this file), src/screens/use-import-leave-guard.ts (plan 04), src/db/import-session-dao.ts (setSessionBatchCategory — plan 01, the durable batch-category writer to call at the Import CTA), src/db/import-session-read.ts (sessionRowCounts — plan 01, source the `N contacts selected` count from `.pending`, not total_rows)</read_first>
  <action>REPLACE plan 04's `BulkImportSetup` themed-placeholder screen registration in RootNavigator with the real BulkImportSetupScreen component (swap the `component={ImportRoutePlaceholder}` for `component={BulkImportSetupScreen}` + its import). The param type `BulkImportSetup: { sessionId: number }` is already declared in RootStackParamList by plan 04 — do NOT re-declare it and do NOT edit navigation/types.ts. The screen shows: the selection count `N contacts selected` (pluralized), where N derives from `sessionRowCounts(exec, sessionId).pending` (the still-pending rows) — NOT `import_sessions.total_rows`, which goes stale after plan 11's pre-batch consolidation resolves clustered rows out of `pending`; sourcing the count from `sessionRowCounts.pending` keeps the displayed count correct after a Combine (cycle-4 LOW — count source was ambiguous). Then the two locked read-only affordances `Unbound` and `Uncategorized`, and a single batch Category override control (Picker or accent-selected chip set) that resolves to a category_id or null. One primary CTA `Import N contacts` (accent, pluralized) that, on tap, FIRST DURABLY persists the chosen override to the session row via `setSessionBatchCategory(exec, sessionId, chosenCategoryId, now)` (plan 01; the existing migration-012 column — no schema change), THEN navigates to ImportProgress — so the durable session `batch_category_id` (not a transient nav param) is the source of truth every downstream path reads via getSessionById (live pass, Retry, bulk Import-as-New). This is the cycle-3 HIGH fix: without the durable write, Retry and duplicate-review Import-as-New silently revert to Uncategorized. (The ImportProgress `batchCategoryId` route param declared by plan 04 stays as-is — do NOT edit navigation/types.ts — but it is now redundant with the durable value; the driver reads the session.) NO per-person list. Reuse the leave-guard so an edited setup back-out confirms (and discards the accepted session on untouched back, consistent with plan 04). All colours via theme tokens. Confirm the FAB→BulkImportSetup entry works: a device multi-pick reaches this screen with the correct count.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: from the dashboard FAB, pick TWO contacts → BulkImportSetup shows `2 contacts selected` + two read-only defaults + one category override + `Import 2 contacts`; no per-person list. This proves the previously-missing bulk entry point.</human-check>
  </verify>
  <acceptance_criteria>
    - BulkImportSetup REAL screen replaces plan 04's `ImportRoutePlaceholder` registration in RootNavigator (param type already declared by plan 04; this plan adds no param key and does not edit navigation/types.ts); the FAB's routePickedImport reaches it for N>1.
    - Screen has no per-row checkbox/list of contacts.
    - The selection count derives from `sessionRowCounts.pending` (not `total_rows`); `grep -n "sessionRowCounts" src/screens/BulkImportSetupScreen.tsx` matches (cycle-4 LOW — count survives pre-batch consolidation).
    - CTA text interpolates the count and pluralizes (1 contact vs N contacts).
    - The `Import N contacts` CTA calls `setSessionBatchCategory` to durably persist the chosen override BEFORE navigating to ImportProgress; `grep -n "setSessionBatchCategory" src/screens/BulkImportSetupScreen.tsx` matches (cycle-3 HIGH — category durability).
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>Bulk setup applies shared Unbound + Uncategorized defaults with a single batch category override (durably persisted to the session row at confirm) and no per-person list, reached from the FAB multi-pick.</done>
</task>

<task type="auto">
  <name>Task 3: ImportProgressScreen — determinate one-logical-import progress</name>
  <files>src/screens/ImportProgressScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen contract #7; Copywriting `Importing… X of N`), src/services/import/import-driver.ts (runImportBatch + onProgress + eligibleStatuses — Task 1), src/navigation/RootNavigator.tsx (the `ImportProgress` ImportRoutePlaceholder registration to REPLACE; `ImportComplete` is already a registered placeholder from plan 04), src/navigation/types.ts (read-only: `ImportProgress` param already declared by plan 04 — do not edit this file)</read_first>
  <action>REPLACE plan 04's `ImportProgress` themed-placeholder screen registration in RootNavigator with the real ImportProgressScreen (swap `component={ImportRoutePlaceholder}` for `component={ImportProgressScreen}` + its import). The param `ImportProgress: { sessionId: number; batchCategoryId: number | null }` is already declared by plan 04 — add no new param key and do NOT edit navigation/types.ts. On mount, run runImportBatch (eligibleStatuses default ['pending']) with an onProgress that updates a determinate progress line/bar (accent tint) showing `Importing… X of N` plus the sub-status `Photos may finish after contacts`. Guard the onProgress/completion callbacks with a mounted-ref so navigating away mid-batch does not call setState on an unmounted screen (review MEDIUM abort/unmount). On completion, navigate to `ImportComplete` with the sessionId — the route is already reachable as plan 04's registered placeholder (no dashboard hedge needed), and plan 08 both swaps in the real ImportCompleteScreen and refines this to navigation.replace. Never a bare spinner; never surface chunk internals as separate imports. Colours via theme tokens.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: a multi-contact import shows determinate X of N advancing to N, then lands on the ImportComplete placeholder (plan 08 swaps in the real summary); navigating away mid-batch does not crash.</human-check>
  </verify>
  <acceptance_criteria>
    - ImportProgress REAL screen replaces plan 04's `ImportRoutePlaceholder` registration (param pre-declared by plan 04; no navigation/types.ts edit); the screen renders `Importing… X of N` (determinate), not a spinner-only view.
    - The screen calls runImportBatch exactly once per mount and guards callbacks with a mounted-ref.
    - `npm run check:colors` passes.
  </acceptance_criteria>
  <done>One logical bulk import shows determinate progress, is unmount-safe, and completes without exposing chunk internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch of untrusted rows → contacts | Each row is validated + scored before it can become a contact; each import resolves its row atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-12 | DoS | oversized batch / giant transaction | high | mitigate | Chunked per-row ATOMIC transactions with yields; no single batch transaction; picker cap ≤100 per pick (Cluster P/S) |
| T-19-10 | Spoofing (mistaken identity) | ambiguous auto-import | high | mitigate | Ambiguous rows are deferred as needs_review, never auto-imported; only 'new'/deterministic decisions write (Cluster L/N) |
| T-19-21 | Tampering (data loss) | crash between contact create and row mark | high | mitigate | importContactRecord resolveRow makes contact + row one transaction |
| T-19-16 | Tampering (retry double-import) | Retry eligibleStatuses | medium | mitigate | Deterministic-bypass + contact_id idempotency guard prevents re-importing a resolved row |
| T-19-01 | Tampering | per-row SQL | high | mitigate | Reuses `?`-bound importContactRecord + session DAO (V5) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces
- `src/services/import/import-driver.ts`: `runImportBatch()` (reads the session via `getSessionById` for phone_region + durable batch_category_id; `eligibleStatuses`; composed classification writers) + `importRowAsNew()` (shared per-row import-as-new seam with the name-required gate + birthday from mapPickedContact; photo enriched by plan 10) + `CHUNK_SIZE`.
- `src/screens/BulkImportSetupScreen.tsx` (persists the batch category via `setSessionBatchCategory` at the Import CTA; selection count from `sessionRowCounts.pending`), `src/screens/ImportProgressScreen.tsx`.
- Screen swaps in `RootNavigator.tsx` (params pre-declared by plan 04, no navigation/types.ts edit): `BulkImportSetup` real screen replaces its placeholder; `ImportProgress` real screen replaces its placeholder.
- Test: `import-driver.test.ts`.

<verification>
- `npx vitest run src/services/import/import-driver.test.ts` green (mixed-batch partial-failure case, already_linked match_outcome, Retry idempotency, region threading).
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: multi-contact import runs incrementally from the FAB entry, skips already-linked (counted distinctly), defers ambiguous to needs_review.
</verification>

<success_criteria>
Bulk import is reachable from the FAB multi-pick, applies shared defaults, imports incrementally with per-row ATOMIC contact+row resolution and failure isolation, marks already-linked with the match_outcome discriminator, defers ambiguous to review, supports Retry, threads the phone region, and shows one determinate logical-import progress.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-06-SUMMARY.md` when done.
</output>
