---
phase: 19-system-contact-import
plan: 09
type: execute
wave: 5
depends_on: [19-01, 19-04, 19-06]
files_modified:
  - src/services/import/contact-import-resume-sweep.ts
  - src/services/import/contact-import-resume-sweep.test.ts
  - src/components/ResumeImportPrompt.tsx
  - App.tsx
autonomous: true
requirements: [IMP-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On foreground launch, if a durable pending import session exists, Orbit offers a non-tap-dismissable Resume Import / Discard Import prompt; Resume reopens the session's review flow from the snapshot (never re-querying the expired grant), and already-committed contacts stay committed regardless (IMP-04, Cluster R)."
    - "Resume routing is a decision tree keyed on import_sessions.mode + sessionRowCounts: mode='single' with a pending row → ImportReview(sessionId); mode='bulk' with ALL rows still pending (no imported/linked/skipped/failed/needs_review row exists — the batch never started) → BulkImportSetup(sessionId); mode='bulk' with pending>0 AND at least one NON-pending row (the batch was interrupted mid-run) → ImportProgress({ sessionId, batchCategoryId: null }) (continue the batch; the batchCategoryId nav field is required by plan 04's route param type but passed null because runImportBatch re-processes only the remaining pending rows and reads the durable batch_category_id from the session — cycle-4 MEDIUM) — do NOT route a mid-batch interrupt back through BulkImportSetup (that re-shows setup and, before the category was made durable, silently changed it; MEDIUM cycle-3 resume-routing split); mode='bulk' with no pending but needs_review>0 → DuplicateReview(sessionId) (mid/post-batch review); ANY mode with no pending AND no needs_review rows → ImportComplete(sessionId) — this branch covers BOTH a terminal-but-unmarked session (failed==0; e.g. a crash between the last resolution and finalizeSessionIfTerminal, which ImportComplete's mount then finalizes) AND a partial-failure session whose only unresolved rows are `failed` (failed>0; ImportComplete shows the Retry affordance and the session stays status='pending'/resumable until Retry resolves the rows or the user Discards) — so a failed-only session's Retry path is reachable after relaunch (addresses review MEDIUM 19-09 resume-routing state machine + never-started-vs-mid-batch split + cycle-2 HIGH 4 residual terminal window + revision WARNING failed-row resumability, Cluster R/P)."
    - "The launch sweep runs a REQUIRED orphan-staging reconciliation every foreground launch: any import-staging file with NO owning row in a live (non-discarded) session is deleted (listImportStagingPhotos minus every live session row's photo_rel_path), so files orphaned by getResumableSession's auto-sweep of stale pending sessions, by a Replace-all restore purge, or by a failed acceptance are always cleaned — while a live session's 'failed'/pending row keeps its staged photo for Retry (addresses cycle-2 MEDIUM — auto-sweep staged-photo leak; reconciliation is REQUIRED, not optional)."
    - "getResumableSession returns exactly ONE pending session (the most recent); any older pending sessions are swept to discarded in the same call, so the prompt never has to disambiguate multiple pending sessions (IMP-04; addresses review MEDIUM 19-09 multiple-pending)."
    - "Discard calls discardSession, which returns the deleted rows' photo_rel_path list; the sweep deletes those staged files AFTER the discard transaction commits (gathered-before-delete), and imported/linked rows keep their contacts (IMP-04, Cluster R; addresses review HIGH — after delete the paths are unrecoverable)."
    - "The launch-sweep staged-photo cleanup deletes orphaned import-staging files for DISCARDED sessions only; it must NOT delete staged photos of rows that are still 'failed' within a live (non-discarded) session, because Retry (plan 08) may re-process them and needs the staged photo (addresses review HIGH — cleaning 'failed' photos erases Retry inputs)."
    - "The resume sweep is registered as a launch-sweep hook (no background timer/listener) and runs once per real foreground launch (Cluster R, repo launch-sweep contract)."
    - { statement: "An unreadable/corrupt pending session offers Discard rather than crashing", verification: backstop }
    - "[ASSUMPTION | spec-less edge IMP-04/unclassified] Resume drives entirely from import_sessions/import_session_rows; a session whose snapshot cannot be parsed is treated as discard-only, and staged photos of DISCARDED sessions (not live-failed rows) are swept from app-private storage."
  artifacts:
    - "src/services/import/contact-import-resume-sweep.ts — registerImportResumeSweep (registerSweepHook consumer detecting one resumable session AND deleting the getResumableSession-swept discards' returned staged photos AND running the REQUIRED orphan-staging reconciliation) + cleanupDiscardedStagedPhotos (deletes a discarded session's returned staged photos) + reconcileOrphanStagedPhotos (deletes import-staging files with no owning live-session row) + describeResumable (corrupt-tolerant)"
    - "src/components/ResumeImportPrompt.tsx — app-root Resume/Discard modal (surfaceElevated sheet, Discard in danger) with the mode+counts resume routing"
    - "App.tsx wiring: register the sweep hook + render the prompt on a resumable session"
  key_links:
    - "the sweep uses getResumableSession (plan 01) — returns one pending session, sweeping older ones AND returning the swept discards' staged photo paths; the sweep passes those paths to cleanupDiscardedStagedPhotos so an auto-swept stale session never leaks staged files; it never opens the picker or re-reads a content:// URI"
    - "Discard calls discardSession (plan 01), which preserves rows carrying a contact_id AND returns the deleted rows' photo_rel_path list for post-commit staged-file deletion"
    - "cleanupDiscardedStagedPhotos uses deleteImportStaging (plan 04) and is scoped to a discarded session's returned paths, never live-failed rows (Retry needs them); reconcileOrphanStagedPhotos (REQUIRED, every launch) additionally deletes any import-staging file with no owning live-session row — the definitive backstop for auto-sweep/Replace-all/failed-accept orphans"
  prohibitions:
    - "Do not implement resume/expiry with a background timer or listener; use the launch sweep (SQLite has no scheduler)."
    - "Do not re-query the picker session URI on resume; the durable snapshot is the sole source."
    - "Do not let Discard remove already-committed contacts; it clears only unresolved state + its staged photos."
    - "Do not delete a row's staged photo before the discard transaction commits, and do not delete staged photos of 'failed' rows in a live session (Retry may need them) — sweep only discarded-session staging (review HIGH)."
    - "Do not add module-scope side effects to the sweep module (headless tap must not trigger it — repo launch-sweep rule)."
---

<objective>
Make import sessions durable and resumable across process death: a launch-sweep hook that offers Resume/Discard from the snapshot, plus staged-photo cleanup for discarded/failed sessions.

Purpose: This closes IMP-04's durability guarantee end-to-end — the tracer and bulk flows create durable sessions; this plan makes returning to Orbit (even after a kill/reboot) offer Resume/Discard, keeps committed contacts committed, and never leans on the expired grant.
Output: contact-import-resume-sweep + ResumeImportPrompt + App wiring, node-tested for the resumable/discard/corrupt cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-UI-SPEC.md
@.planning/phases/19-system-contact-import/19-REVIEWS.md
@src/services/launch-sweep.ts
@src/db/import-session-read.ts
@src/db/import-session-dao.ts
@src/services/photos/photo-storage.ts
</context>

## Review Feedback Incorporated
- **HIGH (Codex) — after discardSession deletes rows, cleanup can't find their photo paths.** `discardSession` (plan 01) now returns the deleted rows' `photo_rel_path` list; the sweep deletes those files AFTER commit (gathered-before-delete).
- **HIGH (Codex) — cleaning 'failed' rows' photos erases Retry inputs.** Staged-photo cleanup is scoped to DISCARDED sessions only; live-session 'failed' rows keep their staged photo so Retry (plan 08) can re-process.
- **MEDIUM (Cursor) — resume routing needs an explicit state machine.** Task 2 defines the mode + sessionRowCounts decision tree (single→ImportReview; bulk+pending→BulkImportSetup; bulk+needs_review→DuplicateReview).
- **MEDIUM (Codex) — multiple pending sessions unspecified.** `getResumableSession` (plan 01) returns one and sweeps older pending sessions to discarded; the prompt never disambiguates.

### Cycle-2 review incorporated
- **MEDIUM (auto-sweep staged-photo leak).** The launch hook now deletes getResumableSession's swept-discards' returned staged photos, AND runs a REQUIRED `reconcileOrphanStagedPhotos` every launch (import-staging files with no owning live-session row), preserving live pending/failed rows' photos for Retry. No longer optional.
- **HIGH 4 residual (terminal-but-unmarked resume).** Resume routing adds a "no pending AND no needs_review → ImportComplete" branch so a crash between the last resolution and finalize self-heals (ImportComplete's mount finalizes); the SAME branch also carries a partial-failure (failed-only) session to ImportComplete so its Retry path survives process death (revision WARNING — finalizeSessionIfTerminal now also requires failed==0, so a failed-only session stays status='pending' and getResumableSession re-surfaces it; Cluster R/P).

### Cycle-3 review incorporated
- **MEDIUM (Cursor/Claude) — bulk resume routing conflated never-started with mid-batch.** The `mode='bulk' + pending>0 → BulkImportSetup` branch is split: ALL rows still pending (batch never started) → BulkImportSetup; any non-pending row exists (batch interrupted mid-run) → ImportProgress to continue — so a mid-batch interrupt no longer re-shows setup (which compounded the category-override loss now fixed in plans 01/06).
- **MEDIUM hygiene (Codex/Claude) — wrong app-root file.** The three mechanical `src/App.tsx` references (files_modified, `<files>`, artifact list) are corrected to `App.tsx` (repo root; `src/App.tsx` does not exist). The `<action>` prose already named `App.tsx`.

### Cycle-4 review incorporated
- **MEDIUM (Codex/Claude) — resume-sweep registration lacked the one-shot guard.** Task 2 now adds a module-scope `importResumeSweepRegistered` boolean beside the existing App.tsx:77-103 guards and registers behind it inside the `ready`-gated effect, so a Strict-Mode/remount re-run cannot double-register the hook (which would double-run the sweep and double-prompt).
- **MEDIUM (Cursor/Claude) — resume nav to ImportProgress omitted the required `batchCategoryId`.** The mid-batch resume navigation now passes `{ sessionId, batchCategoryId: null }`, satisfying plan 04's non-optional route param so plan 09's own `npx tsc --noEmit` gate passes; `runImportBatch` still reads the authoritative category from the durable session row.
- **HIGH 2 dependency (Codex/Claude) — flat staging shape.** Plan 04 flattened the import-staging path so `listImportStagingPhotos()` actually enumerates staged files; Task 1's reconciliation test now explicitly asserts enumeration, closing the orphan-leak this plan's REQUIRED `reconcileOrphanStagedPhotos` depends on.

<task type="auto" tdd="true">
  <name>Task 1: contact-import-resume-sweep — resumable detection + staged-photo cleanup</name>
  <files>src/services/import/contact-import-resume-sweep.ts, src/services/import/contact-import-resume-sweep.test.ts</files>
  <behavior>
    - a pending session with unresolved rows → the sweep reports a resumable session (id + counts + mode)
    - no pending session → nothing to resume; multiple pending → getResumableSession returns the newest and discards the rest
    - discardSession removes only rows without a contact_id AND returns their photo_rel_path list; imported/linked rows (with contact_id) survive
    - staged photo files of DISCARDED sessions are deleted (using the paths discardSession returned) AFTER commit; a live session's 'failed' rows keep their staged photo (Retry needs it); committed contacts' photos untouched
    - getResumableSession's auto-swept stale pending sessions return their staged photo paths, which the sweep deletes in the same launch pass (no leak from the multiple-pending sweep)
    - the REQUIRED orphan reconciliation deletes any import-staging file not referenced by a live (non-discarded) session row, and NEVER deletes a file that a live pending/failed row still references
    - a session whose source_payload cannot be parsed → surfaced as discard-only (no throw)
  </behavior>
  <read_first>src/services/launch-sweep.ts (registerSweepHook contract; NEGATIVE rules — no module-scope side effect, DI'd AppState), src/db/import-session-read.ts (getResumableSession/sessionRowCounts — plan 01), src/db/import-session-dao.ts (discardSession returning photo_rel_path[] — plan 01), src/services/photos/photo-storage.ts (import-staging cleanup deleteImportStaging/listImportStagingPhotos — plan 04; the restore-pending idiom it mirrors)</read_first>
  <action>Write `registerImportResumeSweep()` that pushes a launch-sweep hook via registerSweepHook. The hook: (1) calls getResumableSession, deletes the swept-older-discards' returned staged photo paths via cleanupDiscardedStagedPhotos, and when a resumable session remains exposes it (id + mode + counts) for App.tsx to render the prompt (via an injected callback/state setter, keeping the module node-pure — no react-native import); (2) ALWAYS runs the REQUIRED orphan reconciliation. Add `cleanupDiscardedStagedPhotos(fs, paths)` that deletes the staged import-staging files whose paths were returned by discardSession (via deleteImportStaging) AFTER the discard transaction commits — scoped to those returned paths only; it MUST NOT enumerate-and-delete staged photos for 'failed' rows in a live session (Retry needs them). Add `reconcileOrphanStagedPhotos(exec, fs)` — REQUIRED, run every launch: enumerate listImportStagingPhotos(), compute the set of every photo_rel_path referenced by a row in a NON-discarded session, and deleteImportStaging every staged file NOT in that live set (so auto-sweep/Replace-all/failed-accept orphans are cleaned, while a live pending/failed row's staged photo is preserved). Add a `describeResumable` read that tolerates an unparseable source_payload by flagging the session discard-only rather than throwing (backstop 3). No background timer; no module-scope side effects. Tests cover resumable detection, multiple-pending → newest returned + older discarded + the swept discards' staged photos deleted, discard returning + deleting only its own staged photos, that a live-session failed/pending row's staged photo is NOT deleted by reconciliation, that a truly-orphaned staged file (no owning live-session row) IS deleted by reconciliation, and the corrupt-session discard-only path.</action>
  <verify>
    <automated>npx vitest run src/services/import/contact-import-resume-sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test "discard preserves rows with a contact_id, removes only contact_id-null rows, and the sweep deletes only their returned staged photos" passes.
    - Test "a live-session failed/pending row's staged photo is NOT deleted by the sweep or by reconciliation (Retry input preserved)" passes.
    - Test "reconcileOrphanStagedPhotos ENUMERATES a staged file via listImportStagingPhotos() and deletes an import-staging file with no owning live-session row" passes (the REQUIRED orphan reconciliation; stage a photo via the FLAT `import-staging/import-<sessionToken>-<rowToken>.<ext>` shape and assert listImportStagingPhotos() returns it, then assert reconciliation deletes the orphaned one) — proving the flat lister is not a no-op (cycle-4 HIGH 2: a nested path shape would enumerate nothing and silently leak).
    - Test "multiple pending sessions → newest resumable, older discarded, and the swept discards' staged photos are deleted in the same pass" passes.
    - Test "corrupt/unparseable session is discard-only, never throws" passes.
    - `grep -n "registerSweepHook" src/services/import/contact-import-resume-sweep.ts` matches; no module-scope invocation.
    - `grep -n "reconcileOrphanStagedPhotos" src/services/import/contact-import-resume-sweep.ts` matches and the launch hook calls it unconditionally (required, not optional).
    - `npx vitest run src/services/import/contact-import-resume-sweep.test.ts` exits 0.
  </acceptance_criteria>
  <done>A launch-sweep hook detects a resumable session and cleans staged photos, tolerating corrupt sessions as discard-only.</done>
</task>

<task type="auto">
  <name>Task 2: ResumeImportPrompt + App wiring</name>
  <files>src/components/ResumeImportPrompt.tsx, App.tsx</files>
  <read_first>.planning/phases/19-system-contact-import/19-UI-SPEC.md (Screen #9 Resume/Discard; Copywriting `Resume your import?` + destructive Discard copy), App.tsx (repo root — where installSweepTrigger / launch hooks are wired; the migration-resolved `ready`-gated effect AND the module-scope one-shot registration guards at lines 77-103 — `fieldSweepRegistered`, `backupSweepRegistered`, `photoReconcileRegistered`, etc. — add `importResumeSweepRegistered` in the same idiom), src/navigation/types.ts (read-only: ImportProgress requires `batchCategoryId` — plan 04's declaration; pass `null`), src/components/OverflowMenu.tsx (Modal sheet idiom), src/services/import/contact-import-resume-sweep.ts (Task 1)</read_first>
  <action>Create ResumeImportPrompt: a non-tap-dismissable Modal sheet (surfaceElevated, radius 12) with heading `Resume your import?`, a body explaining unresolved items are saved and that already-imported contacts stay in Orbit, and buttons `Resume import` (accent) and `Discard import` (danger). Resume routes via the decision tree (keyed on mode + sessionRowCounts): mode='single' with a pending row → navigate ImportReview(sessionId); mode='bulk' with ALL rows still pending (imported+linked+skipped+failed+needs_review == 0 — the batch never started) → navigate BulkImportSetup(sessionId); mode='bulk' with pending>0 AND at least one non-pending row (batch interrupted mid-run) → `navigation.navigate('ImportProgress', { sessionId, batchCategoryId: null })` to continue the batch — the `batchCategoryId` field is REQUIRED by the route's param type (plan 04 declared `ImportProgress: { sessionId: number; batchCategoryId: number | null }` and is the sole writer of navigation/types.ts, so this plan MUST pass it or its own `npx tsc --noEmit` gate fails; pass `null` because `runImportBatch` reads the authoritative category from the durable session row via getSessionById, making the nav param redundant — cycle-4 MEDIUM). Do NOT send a mid-batch interrupt back through BulkImportSetup (MEDIUM cycle-3 resume-routing split); mode='bulk' with pending==0 and needs_review>0 → navigate DuplicateReview(sessionId); ANY mode with pending==0 AND needs_review==0 → navigate ImportComplete(sessionId) — a terminal-but-unmarked session (failed==0; ImportComplete's mount completes it) OR a partial-failure session with only `failed` rows left (failed>0; ImportComplete offers Retry and the session stays resumable because finalizeSessionIfTerminal does not complete while failed>0) — so a failed-only session's Retry path is reachable after relaunch (revision WARNING, Cluster R/P). Discard → discardSession(sessionId) (which returns the staged photo paths) → cleanupDiscardedStagedPhotos(fs, paths) AFTER the discard commits → dismiss. In App.tsx, register the resume sweep hook inside the existing migration-resolved (`ready`-gated) effect, guarded by a NEW module-scope one-shot boolean `importResumeSweepRegistered` (declared beside the existing `fieldSweepRegistered`/`backupSweepRegistered`/`photoReconcileRegistered`/`restorePhotoFinalizeSweepRegistered`/`notificationScheduleRegistered`/`digestScheduleRegistered`/`widgetSweepRegistered` guards at App.tsx:77-103) — `if (!importResumeSweepRegistered) { registerImportResumeSweep(...); importResumeSweepRegistered = true; }`. This matches the existing idiom EXACTLY: the `ready`-gated effect can re-run (Strict Mode, remounts) and registering the same hook twice would double-run the resume sweep and double-prompt the user (cycle-4 MEDIUM). Never register at module top-level. Render ResumeImportPrompt when the sweep reports a resumable session. Colours via theme tokens; `danger` only on Discard.</action>
  <verify>
    <automated>npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Device: start an import, force-stop the app mid-session, relaunch → the Resume/Discard prompt appears from the snapshot; Resume reopens review; Discard clears pending state but leaves any already-committed contacts; a previously-committed contact is still present after Discard.</human-check>
  </verify>
  <acceptance_criteria>
    - ResumeImportPrompt is not dismissable by tapping the scrim (explicit buttons only).
    - Bulk resume routing distinguishes never-started (all rows pending → BulkImportSetup) from interrupted mid-run (any non-pending row + pending>0 → ImportProgress); a mid-batch interrupt does NOT route to BulkImportSetup (cycle-3 MEDIUM).
    - The mid-batch ImportProgress resume navigation passes `{ sessionId, batchCategoryId: null }` (satisfying plan 04's non-optional route param); `npx tsc --noEmit --pretty false` passes (cycle-4 MEDIUM — the resume nav no longer omits the required param).
    - `danger` token is used only for the Discard action; `check:colors` passes.
    - App.tsx registers the hook inside the migration-resolved (`ready`-gated) effect behind a module-scope `importResumeSweepRegistered` one-shot guard (matching the existing sweep-hook guards at App.tsx:77-103), not at module top-level; `grep -n "importResumeSweepRegistered" App.tsx` matches (cycle-4 MEDIUM — no double registration/double prompt).
  </acceptance_criteria>
  <done>Returning to Orbit with a pending session offers Resume/Discard from the durable snapshot, preserving committed contacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable session on relaunch → resume | Must never depend on the expired OS grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-17 | DoS (feature break) | resumed session referencing expired grant | high | mitigate | Snapshot-then-drive; an unreadable session offers Discard, never a crash (Pattern 1, backstop 3) |
| T-19-18 | Tampering (data loss) | Discard removing committed contacts | high | mitigate | discardSession preserves rows with a contact_id; only pending state + staged photos are cleared |
| T-19-02 | Info disclosure | staged photo files | medium | mitigate | Staged photos live in app-private storage and are swept for discarded/failed sessions (V8/V12) |
| T-19-SC | Tampering | npm installs | high | accept | No package installs |
</threat_model>

## Artifacts this plan produces

- `src/services/import/contact-import-resume-sweep.ts`: `registerImportResumeSweep()`, `cleanupDiscardedStagedPhotos()`, `reconcileOrphanStagedPhotos()` (REQUIRED launch orphan sweep), `describeResumable()`.
- `src/components/ResumeImportPrompt.tsx` (app-root Resume/Discard modal with mode+counts resume routing).
- `App.tsx` (repo root — NOT `src/App.tsx`, which does not exist): sweep-hook registration behind a new module-scope `importResumeSweepRegistered` one-shot guard (matching the existing App.tsx:77-103 guards) + prompt rendering; resume nav to ImportProgress passes `{ sessionId, batchCategoryId: null }`.
- Test: `contact-import-resume-sweep.test.ts`.

<verification>
- `npx vitest run src/services/import/contact-import-resume-sweep.test.ts` green (resumable/discard/corrupt cases).
- `npx tsc --noEmit --pretty false`, `npx biome check`, `npm run check:colors` pass.
- Device: process-death resume confirmed; Discard preserves committed contacts.
</verification>

<success_criteria>
Import sessions survive process death and offer Resume/Discard from the snapshot, keeping committed contacts and sweeping staged photos of discarded sessions — no timer, no re-query of the expired grant.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-09-SUMMARY.md` when done.
</output>
