---
phase: 19-system-contact-import
plan: 17
type: execute
gap_closure: true
wave: 3
depends_on: [19-12, 19-13, 19-14, 19-15, 19-16]
files_modified: []
autonomous: true
requirements: [IMP-01, IMP-02, IMP-04]
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "On an Android 17+ device, import launches the permissionless system Contact Picker and snapshots name/phone/email; cancel writes no session; a pre-17 device shows the unsupported state and Orbit stays usable (per IMP-01)."
    - "Resolved skips/links survive navigation with correct completion counts, a two-row-only consolidation reaches Import complete, mixed-batch resume works, and committed contacts survive process death (per IMP-04)."
    - "A successful photo import leaves no raw staged file on-device while a retryable failure keeps it; a malformed birthday is blocked in review (per IMP-02/IMP-04)."
  artifacts:
    - .planning/phases/19-system-contact-import/19-17-SUMMARY.md
  key_links:
    - "desktop debug APK → Pixel install → agent-driven UAT of the five closed gaps"
---

<objective>
Agent-driven device verification of the five gap fixes (19-12 native action + launch guard; 19-13
birthday validation; 19-14 leave-guard predicate; 19-15 cluster-only finalization; 19-16 staged-photo
retirement). Per the 19-CONTEXT execution directives, the AGENT drives the device UAT — build (debug)
on `droid`, install on the Pixel, and drive the app per `docs/runbooks/desktop-build-pipeline.md` +
the device-UAT run-as pattern. Do NOT mark this human_needed; the owner reviews the result, not the
mechanics.

This closes the three "Human Verification Required" items from 19-VERIFICATION.md, which cross Expo
native code and OS-owned UI and cannot be proven by static checks.

Purpose: Prove the closed gaps behave correctly on the real device — the only place the Android 17
picker, navigation/process-death recovery, and on-device filesystem staging can be observed.
Output: A 19-17-SUMMARY.md capturing evidence (screenshots / uiautomator dumps / run-as DB + staging
reads) for each scenario, or a clear environment-blocked flag where the Pixel's Android version
prevents a happy-path check.

**Environment reality (RESEARCH A6):** if the Pixel is NOT on Android 17+, the picker happy-path is
untestable on this hardware. In that case, verify the unsupported-state path and pre-17 usability,
prove the data-layer gaps via the node suite + on-device run-as reads where possible, and flag the
Android-17 happy-path as an owner-decision environment block — do NOT fabricate a pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-system-contact-import/19-VERIFICATION.md
@docs/runbooks/desktop-build-pipeline.md
</context>

<constraints>
- Worktrees are DISABLED and agents NEVER push. Any incidental commit is IN PLACE on `main`. No push.
- Build is a DEBUG build (release is not `run-as`-able; run-as is required to inspect the DB +
  staging). Use `install -r` to preserve data across reinstalls. Run the orbit Metro on its own port
  and set `adb reverse` per the device-UAT run-as memory; quest-board owns :8081.
- Resolve the device serial from `adb devices` (never hardcode); confirm exactly one device.
- No new packages, no schema change, no source edits expected in this plan — it is verification only.
  If a device finding requires a code change, STOP and surface it (it becomes a new gap plan), do not
  patch silently here.
- Perf claims are legitimate only on the physical Pixel; do not make any from an emulator.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Build + install, then verify the Android 17 picker launch (GAP A / IMP-01)</name>
  <files>(none — device verification)</files>
  <read_first>
    - docs/runbooks/desktop-build-pipeline.md (the build→install pipeline steps)
    - .planning/phases/19-system-contact-import/19-VERIFICATION.md (Human Verification Required item 1)
    - .planning/phases/19-system-contact-import/19-12-PLAN.md (what changed in the native module)
    - modules/orbit-contact-picker/android/src/main/java/expo/modules/orbitcontactpicker/OrbitContactPickerModule.kt (the fixed action + launch guard)
  </read_first>
  <action>
    Regenerate native (`expo prebuild --clean` if required for the module change), build the DEBUG
    APK on `droid`, and install on the Pixel (`install -r`). Resolve the serial from `adb devices`
    and confirm exactly one device. First confirm the device Android version (`adb shell getprop
    ro.build.version.sdk`); if it is < 37, follow the environment-block branch in the objective for
    the happy-path and instead verify the unsupported state + pre-17 usability. On Android 17+:
    drive both entry points — the dashboard speed-dial FAB "Import from Contacts" and Settings ▸
    Contacts Integration ▸ Import contacts — via `uiautomator dump` + `input tap`. Confirm: the
    system Contact Picker launches WITHOUT a broad contacts-permission prompt; a single pick and a
    multi-pick each return name/phone/email into review/setup; cancelling the picker writes NO
    session (verify via a run-as read of `import_sessions`); and after a deliberately cancelled/failed
    launch a second attempt still opens the picker (launch-guard, GAP A Task 2). Capture screenshots
    / uiautomator dumps as evidence.
  </action>
  <acceptance_criteria>
    - Evidence shows the system Contact Picker launching on the device (or, if pre-17, the "Contact import requires Android 17 or later." unsupported state with the rest of Orbit usable and the happy-path flagged environment-blocked).
    - A cancelled picker leaves no new row in `import_sessions` (run-as query evidence).
    - A second import attempt after a cancelled/failed launch still opens the picker (no permanent "already in progress").
  </acceptance_criteria>
  <verify>
    <automated>npm test && npx tsc --noEmit --pretty false && npm run check:colors</automated>
    <human-check>Agent-driven on the Pixel: picker launches permissionlessly (or unsupported-state on pre-17), name/phone/email snapshot present, no session on cancel, re-invocation works. Evidence captured in the SUMMARY.</human-check>
  </verify>
  <done>The Android 17 picker launch (or the pre-17 unsupported state) is device-verified with captured evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify resolved-flow, recovery, and completion state (GAP C.1 + C.3 / IMP-04)</name>
  <files>(none — device verification)</files>
  <read_first>
    - .planning/phases/19-system-contact-import/19-VERIFICATION.md (Human Verification Required item 2)
    - .planning/phases/19-system-contact-import/19-14-PLAN.md and 19-15-PLAN.md (leave-guard + cluster-finalize fixes)
    - src/screens/ImportCompleteScreen.tsx and src/screens/BulkImportSetupScreen.tsx (the completion + consolidation surfaces)
    - the device-UAT run-as pattern (MEMORY: WAL-aware DB reads via run-as on a debug build)
  </read_first>
  <action>
    On the installed debug build, exercise the durable-completion scenarios and read state via
    run-as (WAL-aware): (a) already-linked + user Skip in a bulk review, then navigate to Import
    complete — confirm the "Already in Orbit" and "Failed / skipped" counts SURVIVE the navigation
    and the session is `complete` not `discarded` (run-as query of `import_sessions.status` +
    `sessionSummaryCounts`); (b) a two-row-only consolidation (pick two contacts sharing a phone
    number, Combine) lands on "Import complete" — NOT a disabled "Import 0 contacts" screen; (c) a
    mixed-batch resume: background/kill the app mid-review and confirm Resume restores the pending
    session and committed contacts survive (query `contacts` + session rows); (d) Discard from the
    Resume prompt leaves already-committed contacts in Orbit while clearing unresolved review state.
  </action>
  <acceptance_criteria>
    - Resolved skip/link counts persist through navigation to Import complete; the session status is `complete` (run-as evidence).
    - A two-row-only cluster reaches the completion summary with the imported count reflecting one combined contact.
    - After process death, Resume restores the pending session and previously-committed contacts remain; Discard keeps committed contacts and clears only unresolved state.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>Agent-driven on the Pixel: counts survive navigation, cluster-only completion has a destination, mixed-batch resume + discard behave per the durable-completion contract. Evidence in the SUMMARY.</human-check>
  </verify>
  <done>Resolved-flow, cluster-only completion, resume, and discard are device-verified with run-as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Verify photo lifecycle + birthday validation on device (GAP C.2 + B / IMP-04, IMP-02)</name>
  <files>(none — device verification)</files>
  <read_first>
    - .planning/phases/19-system-contact-import/19-VERIFICATION.md (Human Verification Required item 3)
    - .planning/phases/19-system-contact-import/19-16-PLAN.md and 19-13-PLAN.md (staging retirement + birthday validation fixes)
    - src/services/photos/photo-storage.ts (the `import-staging` dir layout to inspect via run-as)
  </read_first>
  <action>
    Photo lifecycle (GAP C.2): import a contact WITH a photo successfully, then inspect the
    app-private `import-staging` directory via `run-as` — confirm the raw staged source file for that
    row is GONE and the row's `photo_rel_path` is NULL (run-as DB read), while the contact's Orbit
    master photo exists. Then deliberately fail one photo import (e.g. a corrupt/oversized source) and
    confirm the contact is still created (not invalidated) AND its staged source file + `photo_rel_path`
    REMAIN for Retry. Relaunch the app and confirm the launch sweep did not retain the consumed
    successful staging while preserving the retryable-failed staging. Birthday validation (GAP B):
    in single-import review, type an impossible date (e.g. 2021-02-29) — confirm the inline error
    shows and Import is disabled; correct it to a real date and confirm Import re-enables and the
    stored `contacts.birthday` matches the entered value (run-as read); confirm a year-unknown form
    (MM-DD) imports and stores unchanged.
  </action>
  <acceptance_criteria>
    - After a successful photo import, the raw staged file is deleted and the row's `photo_rel_path` is NULL (run-as evidence); the contact master photo exists.
    - A failed photo import keeps the contact and preserves its staged source + `photo_rel_path` for Retry; the relaunch sweep preserves it.
    - A malformed birthday is blocked with an inline error in review; a corrected/valid year-unknown birthday persists unchanged (run-as read of `contacts.birthday`).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>Agent-driven on the Pixel: success retires+deletes staging, retryable failure preserves it, malformed birthday blocked, valid year-unknown persists. Evidence in the SUMMARY.</human-check>
  </verify>
  <done>Photo-staging retirement and birthday validation are device-verified with run-as evidence.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces
- No source symbols. This plan produces `19-17-SUMMARY.md` with device-UAT evidence (screenshots,
  uiautomator dumps, run-as DB + staging reads) for each closed gap, plus any environment-block flag
  if the Pixel is not on Android 17+.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only) | No new code path introduced; observes the fixes from 19-12..19-16 on-device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-17-01 | Information disclosure | device UAT run-as reads | low | accept | Reads are local on the owner's device via a debug build; no data leaves the device (local-first). |
| T-19-17-SC | Tampering | package installs | n/a | accept | No package installs; verification only. |

No `high`-severity threat; no new attack surface. Local-first preserved (no network on any import path).
</threat_model>

<verification>
- Full regression green before device drive: `npm test`, `npx tsc --noEmit --pretty false`, `npm run check:colors`.
- Three device scenarios (picker launch / resolved-flow+recovery / photo+birthday) verified
  agent-driven per docs/runbooks/desktop-build-pipeline.md, with run-as evidence in the SUMMARY.
- Any Android-version environment block is explicitly flagged, not papered over.
</verification>

<success_criteria>
The five gap fixes are proven on the real device (or the picker happy-path is explicitly flagged as
Android-version-blocked with all node-verifiable gaps proven), evidence captured for owner review.
</success_criteria>

<output>
Create `.planning/phases/19-system-contact-import/19-17-SUMMARY.md` when done.
</output>
