# Todos for upcoming milestones

Things to track down for UI/UX milestone:
    Dashboard:
        -Filters/sorting: This section looks like garbage to be honest. It's a shitload of chips stacked on each other and spilling off to the sides and the whole section takes up maybe 15-20% of the page height. These should all be condensed into a new menu that lives in a single button at the top of the dashboard for "Filter/Sort". This then brings up a fullscreen modal that has a Filters section up top and Sorting options underneath, separated by a dotted line or a clear demarcation visual. Currently they're mashed together and you can't really tell what's what.
        -Maybe we also condense the search bar a bit. I don't love that it takes up a whole row unto itself but it does kind of make sense. I'm thinking we instead add a tiny square Search button next to the above new Filter/Sort button . Like Filter button on the left or right and the search button in whatever space remains, but obviously everything should be spaced nicely if possible. Then clicking the search can bring up a small modal window with a text bar and your contacts listed out in a dynamic drawer under the modal/search bar. What I mean is it should show your first 3-5 contacts by default when you open the search modal, and as you type it should filter down to what you're typing, showing only the contacts that still match in the drawer, meaning if you type the full name, it will likely filter everybody else out a litttle bit more with each letter you add, until you get to the person you want as the only one showing. Clicking these contact cards in the drawer should bring you straight to their profile page.
        -Contact cards: I do not like the current state of the cards, but I also don't have any great ideas myself on improvements. We could go to a grid of square cards with only the profile pictures prominently displayed with the name underneath (leaving out the cat badge and the conversation fuel tidbits which is fine by me honestly). We could do taller cards so the images are biggger/easier to see and then also include MORE details on the cards, though I don't know what details that would be. Or we could do the grid of square cards but actually not do squares at all and do just floating avatar bubbles with name title badges under each. That last one would be more like an orrery of itself kind of, more on theme than the square cards I think. We could also, probably the best route, include all of these views and allow the user to switch between them. This is probably the route we should go, just means more design work but it's otherwise all just pulling the same info from the db really. Another view option is start with big cards for category names, then clicking each brings you to just that category of contacts lsited out in one of the above views. Don't love this idea but it's a possibility.
    Orrery:
        -Desperately need view editing, like pinch to zoom, horizontal/vertical tracking, and filters:
            -Pinch to zoom is straightforward in concept but probably difficult in reality. MAkes the entire page a canvas instead of whatever it currently is so we can freely move around I would think. I'm not sure here though.
            -Tracking is primarily because of the zooming Iw ant but will also play a role when you just have a lot of contacts being tracked. Currently the orbit circles seem to adjust to however many I have on the page. Meaning if I have 2 contacts, the one contact ring is right outside the center sun and the other is almost to the edge of the screen. When I have 12 contacts, they all still completely fit the rings on the page, they're just tighter together (causing the profile pcitures to get smaller and overlap, neither of which is desirable). Both situations it's like they're splitting all the available distances among themselves to fit in whatever space is there. So I'm concerned when I have 50 contacts that this page will be useless at that point. Instead I'd rather have the circles do this adjusting up until a certain threshold, say 7 contact rings, at which point they become standard, equal widths from the center (ring 1 is 5px away, 2 is 10px away, 3 is 15px, etc for example). This means a lot of contacts are going to be off page, but it also actually let's us utilize the top and bottom of the page more. This whole idea hinges on the canvas idea above though. So let's say we're working with a canvas for this page now. We'd say that the default view is locked to whatever size can comfortably fit that threshold number on page at once. I said 7 because I think that's around the magic number for that. Meaning if you have 10 contacts, the left and right side of their rings would be offscreen on the sides of the page, while the top and bottom of the rings would be visible, at least in portrait view, it would be inverse in landscape view. Meaning if you wanted to see the offscreen portions of those rings, or the rings even further out than that, you ahve to either zoom as mentioned above, or do the tracking I mention for this point, shifting the canvas view left/right/up/down to view whichever sections of the greater orrery that you wnat to see.
            -Need better filters for this page to help with the higher number of contacts (I should mention I spitballed that I would have 10-15 contacts for myself for this personal-use app, but I'm now realizing it'll be closer to 25-30, and many others will have many more than that). I could use some helpw ith the options to show here but I'm thinking at least a category filter (only look at family or friends, etc), a date picker for last contact date would be interesting maybe, a toggle for "Has profile picture" could be nice, things like those. Currently the only view are "Status" and "Relationship" which are incredibly vague and to be honest even I don't know what those mean. So we shoudl clarify or clear that language up on the buttons. Anyways, for the filters, I was thinking of adding another button inline with the other 2, so 3 side by side, which the third being "Filters" which opens a fullscreen modal with the available filter options.
    Profile details page:
        -Snooze chips should be hidden until the contact is past due. No need showing snooze options for somebody you're up to date with.
        -There's 3 buttons at the top of the page that need changes:
            -First, let's make them square buttons rather than wide rectangular ones that stretch across the whole page. I'd rather they each be square and side by side. We'll be adding a 4th button so it should probably be a 2x2 grid of these square buttons
            -Consolidate the "AI draft" button into the "Message" button, or moreso just get rid of the AI draft button. It brings you to the same composition page but with the API call prefired so the draft loads shortly after the page loads. This is not ideal UX when API calls cost money, people will accidentally hit this. I'd rather the AI call be gated on the composition page by the "Draft with AI" button, so users have to hit the "Message" button on the profile first, THEN hit the AI option, which basically acts as a type of indirect confirmation that they want to make that API call.
            -I want to rename the "Log contact" button to "Quick Log" as it adds a non-descriptive log event, then add a new button (replacing the AI draft essentially) for "Log Contact" that does a more proper logging:
                -I want a contact logging form here. The point is it should bring you to a very quick, tap-through type menu where you can very quickly log the details of the contact if you want. So the Quick Log button keeps the functionality it currently has as "Log contact", but is named more aptly, but this new button uses the old button's name but has a very different functionality.
                -Bring up a full screen modal with the logging contact options: Date/time picker/s, Channel dropdown, direction dropdown, connected toggle, quality dropdown, and notes/details textarea.
            -Fourth, new button for "Update Contact" new flow/form.
                -My Obsidian Orbit plugin has this set up very nicely by leveraging another plugin called QuickAdd. QuickAdd basically marks certain parts of a markdown note, usually by header types, and then allows you to append text after those headers. So the QuickAdd menu there has a "Update Person" option, that allows you to update the details of the contact itzelf, not logging real-world contact details like "I talked to them today" but moreso "Add a new pet to this user". The QuickAdd menu has the following options:
                    -Update Birthday
                    -Update Social Battery
                    -Log interaction (will not include this in our new form as it has the new button lsited above)
                    -Update Location (Need to add a location field to all contacts as a default field)
                    -Add Gift Idea
                    -Add "Last talked About" (Last talked about is a field on contacts in Obsidian Orbit that we don't have here, would be worth looking at adding though)
                    -Add Safe Topic (Another field we should probably add to defaults)
                    -Add Pet/Key person (This should be two different fields in our app: Pets and Relationships. Pets we'll leeave for users to make as a custom field, Relationships should probs be a default field)
                -So I'd like to replicate the purpose behind this, but I'm torn on two things:
                    -Should this even belong on this page or add an "Update Contact" option to the Dashboard FAB which would actually more closely emulate the Obsidian plugin setup currently. There the QuickAdd is actually detached from individual contacts and the first page of the form is choosing which category of users to pull up, then it lists all the users in the cat on the second page, then gives the above edit options on the third page. My way above cuts this down to a 1-2-step form rather than a 3-4 step form, but there's pros and cons to both setups.
                    -You can choose to edit a contact from their profile page, which essentially gives you all of these editing options in one place, meaning a 1-location page rather than a form, with all the fields laid out. More complicated view but cleaner in a way.
            -Alternatively, we scrap the buttons altogether and add a FAB to this page, same location as the dashboard FAB so it appears to not move between page transitions, and make it a FAB that brings up options. Like I have a FAB on my Questboard homescreen and if you tap it it brings up 3 little optiosn to choose from and then those each bring you to different pages/forms. I'm actually leaning towards this. Keeps the FAB around and more universally used, cleans up the page of buttons which is kinda oldschool anyways, yeah this is sounding best so far.
    Profile add details page (needs to be renamed, the section near the bottom of the profile includes "Edit details" buttons for the time entries, so there's a "Add details" button above for contact details, then this "Edit details" button further down for a totally different thing to edit):
    Message/AI message pages:
    Settings pages:
    General:
        -Going back from any page goes through the string of pages you took to get there, rather than each page once. For example, if I click into a person's profile page, then click into add details, then save which brings me back to the profile page, going back brings me to the details page and then again to the profile page. It should go straight back to the dashboard when going back from a profile though. Going back from the details page should bring you to the profile, but back from profile should always go to dashboard.
        -Desperately need a bottom nav bar that persists across all screens across the whole app. Needs to include Dashboard, Orrery, Settings, and something else I can't think of yet. FAB for Dashboard will remain on that page but be offset to be above the nav bar, still near the bottom right of the screen though.
        -Need to offset edges of screen appropriately. For isntance, left and right sides look good, bottom looks good but could use like 10px of padding for good measure, but the top of the screen is bad. There's zero offset there currently so the options at the tops of most pages (Your Week, Orrery button, Settings button) are behind the notification bar, with only a sliver visible and clickable below that, making it very hard to swithc pages and looks horrible. Need to add enough regular offset for the notification bar and another offset for the camera cutouts some phones have. Same offset on the bottom for phones that have buttons down there, though I think that's rarer.
Things to track down for Sync milestone: