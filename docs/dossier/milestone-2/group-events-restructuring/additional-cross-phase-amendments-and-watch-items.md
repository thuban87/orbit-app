# Group Interaction Logging — Additional Cross-Phase Amendments & Watch Items

## Completed dossier that also needs reopening

### Phase 5 — Dashboard Control Surface

**Why:** Phase 5 currently defines Your Week as the Dashboard header's first-class destination and does not know about Group Events. Phase 12 now gives Group Events a prominent Dashboard header icon+label entry plus Dashboard overflow entry.

### Paste-ready Phase 5 prompt

We need a targeted amendment to the completed Phase 5 — Dashboard Control Surface dossier based on the newly settled Phase 12 — Group Interaction Logging subsystem.

Please preserve the existing Dashboard control-shell decisions except where extended below.

#### Required amendment

1. **Group Events becomes a first-class Dashboard-owned destination in the Dashboard header.**

2. The intended Milestone 2 header now supports both:
   - Your Week
   - Group Events

   using icon + short label where space permits and responsive/icon-only fallback where necessary.

3. Group Events also appears in Dashboard overflow as a redundant discovery/management path.

4. Do not add a permanent Group Events content module between Dashboard controls and contacts.

5. Do not give Group Events a permanent bottom-nav tab in this milestone.

6. Do not turn the Dashboard bottom-nav item into a radial/menu launcher in this milestone.

7. The canonical Group Events browse/detail/edit workflows belong to Phase 12. Phase 5 owns only Dashboard exposure/navigation into that subsystem.

8. Preserve Dashboard's lean contact-browser layout and existing Population/Filters/Sort/Search control architecture.

Please add a concise amendment section rather than reopening unrelated Phase 5 control-surface decisions.

---

## Planned phase watch item

### Later Settings & Personalization phase

This phase has not yet been interrogated/completed, so no completed dossier needs reopening yet. Carry this forward when that session begins:

- ordinary single-contact logging has a global Channel-default preference:
  - Message
  - Call
  - In Person
  - Remember last choice
- factory default is **Remember last choice**,
- **Group Log is explicitly exempt** from this setting,
- Group Log independently defaults to **In Person**.

### Paste-ready prompt for the future Settings session

When interrogating Settings & Personalization, incorporate this settled logging preference contract:

1. The ordinary single-contact logging Channel-default setting offers Message, Call, In Person, and Remember last choice.
2. Factory default is Remember last choice.
3. The setting applies only to ordinary single-contact logging.
4. Phase 12 Group Log is explicitly exempt and independently defaults to In Person.
5. Settings copy should make that scope clear enough that users do not expect changing the ordinary logging default to alter Group Log.

Do not reopen Group Event Channel semantics in Settings; Settings only exposes/explains the already-settled preference scope.

---

## Backup / Restore watch item

Backup/Restore is already a top-level product area and Group Events add durable state that must be preserved:

- Group Event identity,
- title/date-time,
- shared/default Channel/Tone/Duration,
- Group Note,
- child links,
- participant override/inheritance state,
- child Interaction data.

If a dedicated Backup/Restore planning dossier/session is created or reopened later, carry this contract into it. Phase 12 owns the semantic requirement; the Backup/Restore implementation must preserve it exactly rather than flattening Group Events into standalone Interactions.
